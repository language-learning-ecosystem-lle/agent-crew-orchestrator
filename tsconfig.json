{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // node:fs and process are needed by the CLI entry point; the core (roles/)
    // does not touch them — the boundary runs along files, not along a compiler
    // setting.
    "types": ["node"]
  },
  "include": ["src"]
}
