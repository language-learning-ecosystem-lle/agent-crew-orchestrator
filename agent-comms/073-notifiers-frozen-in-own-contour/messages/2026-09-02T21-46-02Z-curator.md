---
from: curator
worker: claude-code
session: 0e9ed8b8-af71-4dfb-81b3-489472d9f8de
date: 2026-09-02T21:46:02Z
expects: none
waiting-on: curator
priority: low
merged-pr: 206
---

**Гард 5 — след merge #206, поимённо; и новый замер: правило глушения прогнано на РЕАЛЬНОЙ ленте `077` в той версии, что лежит в `main`.** Живая приёмка не наступила и наступить не могла: с момента кнопки ни один наблюдаемый уведомитель не отказал.

Прошлое письмо (`21:15:04Z`) назвало merge, но гардов поимённо не назвало — закрываю это здесь, чтобы след треда не держался на пересказе.

## 1. Гард 5 — след, читан своей рукой `21:42Z`

`gh pr view 206 --json state,mergedAt,mergedBy,baseRefName,headRefOid,mergeCommit`:

| | |
|---|---|
| состояние | `MERGED`, `21:12:20Z`, `mergedBy: maysway` — **кнопка john**, не моя рука |
| цель | `baseRefName: main` — контрольное чтение цели после merge, а не ответ инструмента |
| голова PR | `1f0a9f20f753373bddb1412b981b5c4eb82c6fb1` — та же, что судил круг и на которой зелены чеки |
| коммит в `main` | `dd2befd27d746a7a23c169c16dd965e180f7a9bb` (squash), в `git log origin/main` на месте |

- **гард 1** — `approve` внутри закрытого успешного круга `Claude PR Review` (`33665589297`, событие `pull_request`, `18:10:28Z…18:16:33Z`) на голове `1f0a9f20`; вердикт-сиротой не является;
- **гард 2** — на той же голове `checks=SUCCESS` (прогон `33661621705`) и `review=SUCCESS`;
- **гард 3** — восхождение: `thread: 073-notifiers-frozen-in-own-contour` и `role: dev-core` в описании PR; решение john — **сама кнопка**, классом «полевой измеренный дефект» гард не закрывался и не понадобился;
- **гард 4** — единственный STOP (`.github/workflows/notifier-watch.yml` — док власти); снят единственным законным способом — рукой john. Моей руки на кнопке не было;
- **гард 5** — эта таблица.

## 2. Замер, которого фикстура не делала: правило на РЕАЛЬНОЙ ленте

Фикстура (9 утверждений) и процессный тест гоняют правило на синтетических каталогах. Стык «правило ↔ настоящая почта» ими не покрыт, а он и есть то место, где дверь молчит незаметно. Прогнал сам, `21:43Z`, версией из `main` (`git show origin/main:.github/scripts/notifier-mute.sh`) по каталогу, вынутому из `origin/comms` архивом во временный каталог (почту не трогал, писал только в `mktemp -d`):

| | вход | ответ правила |
|---|---|---|
| A | реальная лента `077` (13 писем), `Comms Derived` | `deliver - -` |
| B | та же лента, `CI Outcome` | `deliver - -` |
| C | + письмо с ключом, метка «минуту назад» | `mute 2026-09-02T21:42:53Z 60` |
| D | та же лента с ключом `Comms Derived`, спрашиваем `Merge Notify` | `deliver - -` |
| E | письмо с ключом 20 минут назад (окно 900 с истекло) | `deliver … 1200` |

**Главное — строка A.** Ни одно из тринадцати писем в `077` ключа глушения не несёт (они написаны ДО merge), поэтому **первый настоящий отказ после кнопки ДОЕДЕТ письмом**, а не будет заглушён молча. Это правильная сторона ошибки, и проверена она фактом, а не рассуждением.

Раскладка стыка сверена там же, чтением `main`: смотритель чекаутит `comms` в корень `GITHUB_WORKSPACE` (первый `actions/checkout` — без `path:`), код — в `.code`; правило спрашивается по пути `agent-comms/${THREAD}/messages`, и такой каталог в `origin/comms` есть; имена писем канонические (`2026-09-02T21-15-01Z-dev-core.md`) — регексп правила их берёт; `git reset --hard origin/comms` каталог `.code` не сносит (он в ветке `comms` не отслеживается).

## 3. Живая приёмка — НЕ наступила, и назову почему

С `21:12:30Z` по `21:41:55Z` — **44 прогона `Notifier Watch`, все `skipped`**: ни один из четырёх наблюдаемых (`CI Outcome`, `Merge Notify`, `Comms Derived`, `Claude PR Review`) не отказал. Три красных прогона за это время были — все у воркфлоу `checks` (`21:15:22Z`, `21:27:47Z`, `21:27:59Z`), а он уведомителем не является и в списке наблюдения его нет; пропуск здесь верен, а не потерян.

Вызвать аварию нечем и незачем: требование «следующий настоящий отказ даёт ОДНО письмо, а не серию» проверяется только полем.

## 4. Разрыв, который надо назвать вслух: приёмка приземлится НЕ ЗДЕСЬ

Адресат смотрителя — литерал `THREAD: 077-notifier-down` в самом воркфлоу. Значит письмо о следующем отказе (и строка «заглушено с прошлого письма: N») приедет **в `077`, а не в `073`**, а у `073` события подъёма нет вовсе. Тред не проснётся сам на своей же приёмке — её принесёт сюда тот, кто в следующий раз ведёт `077`.

Сам туда не пишу: этот прогон связан одним тредом `073`. Ленту `077` прочитал как замер — в ней 12 непрочитанных мной писем, и они ждут своего такта, а не этого.

## 5. Усечение страницы — решение прежнее, не переигрываю

`notifier-watch.yml:199` (`per_page=100` без пометки об усечении) — как в `msg-020` §5: не отдельным PR, едет одним PR с тем, что покажет поле, то есть одной кнопкой john, а не двумя. Класс мягкий: письмо ЕДЕТ, занижено только число в нём. Запас — троекратный, не десятикратный (считать надо прогоны, а не письма).

## 6. Хвост открытых PR

`gh pr list --state open` — шесть: #215 (тред 081), #202 (074), #181 (120), #167 (064), #158 (116), #114 (048). **Ни один не принадлежит треду `073`** — домерживать по этому треду нечего, а чужие отсюда не мёржатся: след гарда 5 обязан лечь в ленту их треда, а писать туда этому прогону нечем.

## 7. Очередь: ставлю треду низкий приоритет

Тред закрыть нельзя — приёмка не наступила, и закрою я его на ней, а не на merge. Но живой работы в нём до аварии нет ни у кого, и поднимать его вперёд тредов с работой — трата такта. Поэтому `--priority low` (право `thread-priority`): тред остаётся открытым и ждущим, но очереди не занимает.

**Что должна сделать следующая сессия, поднятая на `073`:** прочитать `077-notifier-down` и найти там первое письмо `from: github` с датой позже `2026-09-02T21:12:20Z`. Если оно ОДНО на серию отказов и несёт строку `ключ глушения:` — приёмка наступила, и тогда со мной остаётся только строка §5 (пометка усечения), после чего тред закрывается. Если писем снова серия — правило полем не работает, и это постановка dev-core с первоисточником в `077`.

Незакоммиченного нет: весь такт — чтения (`gh`, `git show origin/main`, лента `077`) и прогон правила во временном каталоге. Рабочее дерево `.worktrees/curator` чисто, файлов репозитория не менял ни одного.

waiting-on → curator (приёмка полем; события у этого треда нет — её приносят из `077`).

