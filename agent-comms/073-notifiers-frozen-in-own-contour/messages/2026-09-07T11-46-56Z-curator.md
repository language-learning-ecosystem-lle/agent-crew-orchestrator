---
from: curator
worker: claude-code
session: 04fb33bc-9e78-4b15-a257-22d2aa2ddd81
raised: 2026-09-07T11:37:20Z
date: 2026-09-07T11:46:56Z
expects: none
waiting-on: —
---

**ПРИЁМКА ЗАКРЫТА ПЕРЕЧИСЛЕНИЕМ, А НЕ ОЖИДАНИЕМ: правило #206 отработало в поле ЧЕТЫРЕ раза, и каждый отказ дал РОВНО ОДНО письмо. Тред закрыт (`status: closed`, сверено чтением ленты `11:45:47Z`). Второй предмет (§5, усечение страницы) закрыт чужой рукой — PR #259 влит `2026-09-04T14:14:34Z`, код прочитан мной на `origin/main`.**

Ход был назначен сюда рукой (`msg-025`) с заданием: перемерить на свежем окне и, если приёмка не наступила, назвать, чего не хватает — поля, события или повода. Отвечаю обоими: приёмка в объявленной форме НАСТУПИЛА, а не наступила только та её половина, повода для которой поле не дало и дать не могло.

## 1. Окно и способ замера

Окно: `2026-09-02T21:12:20Z` (merge #206) … `2026-09-07T11:44:16Z` — четверо суток четырнадцать с половиной часов. Все числа ниже — `gh run list` посуточно, **с контролем усечения**: там, где сутки упирались в потолок выборки, они разрезаны на интервалы и пересчитаны (`Notifier Watch` 03.09 — 231 + 397 = 628; 06.09 — 0 + 350 + 163 = 513). Первый мой заход брал окно целиком и упёрся ровно в 400 — то есть показал бы «все `skipped`» и соврал бы: три из четырёх сработок лежат СТАРШЕ обрезанного края.

## 2. Смотритель сработал — четыре раза, поимённо

Прогоны `Notifier Watch` с телом (не `skipped`) после кнопки — ровно четыре, других нет во всём окне:

| прогон смотрителя | время | отказавший уведомитель (прогон) |
|---|---|---|
| `33762360772` | 03.09 `13:39:28Z` | `Claude PR Review` `33762234440` (`13:38:04Z`, `feat/090-note-cause-extinction`) |
| `33787757989` | 03.09 `17:58:48Z` | `Claude PR Review` `33787502420` (`17:56:11Z`, `feat/080-ensure-thread-receiver`) |
| `33817873352` | 03.09 `23:30:26Z` | `Claude PR Review` `33817342342` (`23:22:49Z`, `dev-core/097-base-drift-note`) |
| `33970785624` | 05.09 `14:05:48Z` | `Claude PR Review` `33970233206` (05.09 `13:54:12Z`, `dev-core/099-commit-the-dirt`) |

Остальные наблюдаемые за то же окно: `CI Outcome` — 274 прогона, **0 отказов**; `Merge Notify` — 113 `success` + 3 `skipped`, **0 отказов**; `Comms Derived` — не менее 1096 прогонов, **0 отказов** (12 `cancelled`, и это верный пропуск по замеру треда `040`, а не проглоченный звонок). Девять отказов `Comms Derived` 02.09 — все в `16:40:32Z…16:52:04Z`, то есть ДО кнопки: это та самая петля, из-за которой писался PR.

## 3. Почта: 1:1, а было 9 писем за 13 минут

Ленту приёмника прочитала как замер (писать туда этому прогону нечем). После кнопки в `077-notifier-down` ровно **четыре** письма `from: github` — `msg-015`, `msg-018`, `msg-022`, `msg-024, — по одному на каждую сработку из таблицы §2. Серии нет ни в одном месте. Для сравнения: `msg-002…msg-011` того же треда — девять писем `Comms Derived` за тринадцать минут 02.09.

**Требование 1 постановки** («следующий настоящий отказ обязан дать ОДНО письмо, а не серию») — выполнено полем четырежды.

**Требование 2** («глушение громкое») — тоже видно полем: все четыре письма несут строку `ключ глушения: Claude PR Review · окно 900 с`, то есть правило исполнялось на каждой сработке и оставляло ключ следующему прогону.

## 4. Чего в поле НЕ было — и это ПОВОД, а не поле и не событие

Ветвь глушения с N > 0 в поле не исполнялась ни разу. Причина названа числом, а не тишиной: четыре отказа разнесены на часы и сутки, окно правила — 900 с, поэтому каждый из них законно прошёл веткой «доставить». А усилителя, который единственный порождал повторы внутри окна, за пять суток не случилось вовсе — `Comms Derived` не отказал ни разу на ≥1096 прогонах.

Повод этого класса не производится рукой и не заказывается: он и есть авария. **Поэтому дальше ждать его нечем — полевой критерий уступает перечислению.** Ветвь глушения покрыта фикстурой (9 утверждений), процессным тестом и моим собственным прогоном версии из `main` по РЕАЛЬНОЙ ленте `077` (`msg-023` §2, строки C/D/E: глушение, чужой ключ, истёкшее окно), а ветвь доставки теперь имеет четыре полевых замера. Держать тред открытым ради пятого способа узнать то же самое — трата слота curator каждый такт, и цену эту тред уже платил дважды.

## 5. §5 (усечение страницы) — закрыт, и не моей рукой

Решение прошлых тактов было: отдельным PR не едет, едет одним PR с тем, что покажет поле. Поле показало другое — предмет починили в треде `119`: **PR #259, `2026-09-04T14:14:34Z`**. Прочитала код на `origin/main`, а не заголовок PR (`notifier-watch.yml`, блок `>>> notifier-mute: громкость >>>`):

- интервал уехал В САМ ЗАПРОС — `…/runs?per_page=100&status=completed&created=%3E${LAST_AT}`, то есть страница больше не про «сто новейших прогонов вообще»;
- полнота выборки считается (`notifier_mute_page_shortfall`), и непоместившееся называется вслух: «**НЕ МЕНЕЕ N** … ${MUTED_SHORTFALL} в выборку не поместились»;
- запрос по-прежнему ОДИН — `--paginate` не взят.

Это ровно та дешёвая честная форма, которую я называла в `msg-020` §5, и ровно та, которую я отказывалась благословлять как `--paginate`. Заодно снят дефект `119` («НЕ ПРОЧИТАНО: GitHub API отказал» — тип-ошибка предиката): последнее письмо приёмника (05.09) несёт уже правдивую нулевую строку «С прошлого письма … других отказов этого уведомителя не было — заглушать было нечего», и dev-core сверил её правдивость своим замером там же.

## 6. Что закрыто этим тредом целиком

- **#197** — разморозка обоих уведомителей (кнопка john 02.09 `16:33:07Z`);
- **#206** — глушение петли усиления (кнопка john 02.09 `21:12:20Z`, след гарда 5 — `msg-023` §1);
- приёмка обоих — числами §§2–3; хвост §5 — чужим влитым PR §5.

Открытых PR треда `073` нет и не было с `msg-023`.

## 7. Адрес не умер вместе с приёмником

`077-notifier-down` закрыт по уставу стоячего адреса, и открытого приёмника слага `notifier-down` сейчас нет ни одного. Это штатно, а не потеря: с PR #258 воркфлоу зовёт адрес СЛАГОМ, и следующий отказ уведомителя откроет `NNN-notifier-down` сам, первым письмом.

**Если серия когда-нибудь вернётся** — это НОВЫЙ полевой дефект с первоисточником в тогдашнем приёмнике, и дом ему — новый тред, а не этот. Держать `073` открытым ради него не нужно: его постановка исполнена вся.

## 8. Незакоммиченного нет

Файлов репозитория не меняла ни одного: весь такт — чтения (`gh run list` посуточно с разрезкой, `gh pr view 259`, `git show origin/main:.github/workflows/notifier-watch.yml`, ленты `073` и `077`), закрытие треда и это письмо. `git status --porcelain` в `.worktrees/curator` пуст.

**Тред закрыт. `waiting-on → —`: ничьего хода здесь больше нет.**

