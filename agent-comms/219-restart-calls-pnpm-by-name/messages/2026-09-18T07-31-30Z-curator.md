---
from: curator
worker: claude-ai
date: 2026-09-18T07:31:30Z
expects: answer
waiting-on: curator
delivers: john
park-lifted: john
priority: high
---

**П-4 ПРИНЯТ ПОЛЕМ. Жест john исполнен `17.09 ~16:25Z`, вывод предъявлен им в чате `18.09 07:30Z`: рестарт из окружения БЕЗ nvm в `PATH` прошёл установку и поднял демона. Парк снимаю этим письмом.**

## 1. Вывод жеста — строками, дословно

```
restart — git pull --ff-only …, running '/usr/bin/git' (from PATH — nothing beside this node binary)
restart — git pull --ff-only — ok: Already up to date.
restart — pnpm install …, running '/home/lle/.nvm/versions/node/v24.18.0/bin/pnpm' (beside this node binary)
restart — pnpm install — ok: Already up to date · Done in 548ms using pnpm v10.34.5
restart — raising the daemon with the flags it was stopped with (…/daemon.pid.args): --ref origin/main --instance hetzner
the daemon is up in the background, pid 2303153
```

Вызов — тот же, каким дважды падало: `sudo -u aco-hetzner -i bash -lc '… secrets.env … node … cli.ts orchestrator restart --pull'`, **без** добавления nvm в `PATH`. Решающий демон нёс `db8a391d` — то есть код уже с #464.

## 2. Обе половины закрыты, каждая своим прежним провалом

- **половина П-1 (#462): резолюция пути.** Было `12:23Z`: `FAILED (code ?)` — `pnpm` не найден в `PATH` вызывающего. Стало: взят `beside this node binary`;
- **половина П-1 (#464): окружение ребёнка.** Было `15:49Z`: `ran and exited 127: /usr/bin/env: 'node': No such file or directory` — интерпретатор не найден. Стало: `install — ok` в том же окружении;
- **П-2: отказ называет себя.** Прибор уже сработал на обоих провалах выше — `code ?` → `ran and exited 127` + имя ненайденного интерпретатора. Третья формулировка (#464) положительного свидетеля не имеет и по построению иметь не может: она печатается только на провале.

**Различающая сила приёмки:** у неё было ДВА полевых провала на том же пути, с разными причинами, оба до правок. Это не «не могло провалиться» — это исход, форма которого известна.

## 3. Побочное, замеренное тем же выводом — и оно НЕ дефект этого треда

Дренаж ждал живую сессию **465 секунд** (`still waiting … 60 … 420s`, ушёл на 465). Это та самая семиминутная сессия `curator×161`, из-за которой john завёл тред `222`. Там правка влита и принята полем (`222` закрыт `17:23Z`): отчёт о самоперезапуске больше никого не поднимает. **Повторения этой задержки по той причине не будет**; если рестарт снова встанет на минуты — это другая причина и другой разбор.

## 4. Что ОСТАЁТСЯ за john — один вопрос, прежний

**Доводить ли стенды до настоящих node-скриптов** (msg-008 §4). Починка стои́т в четырёх местах, полем проверено ОДНО — путь рестарта. У трёх остальных заглушки не требуют интерпретатора: их тесты зеленеют одинаково с правкой и без неё, а полем они не проверятся никогда — живут в среде демона, где `node` в `PATH` есть. Рекомендация роли — доводить, низким приоритетом; довод: в этом же треде класс уже разъехался — рестарт починили, выравнивание осталось сломанным (тред `221`), и ни один стенд этого не поймал.

**Тред не закрываю:** предмет исполнен, но этот вопрос открыт, и закрытие без ответа на него потеряло бы находку.

waiting-on → curator.
