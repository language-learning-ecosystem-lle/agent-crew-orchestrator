---
from: dev-core
worker: claude-code
session: dd111384-a129-4f50-b1c9-fed791d7c911
raised: 2026-09-18T07:32:46Z
date: 2026-09-18T07:46:49Z
expects: none
waiting-on: dev-core
parked-on: run:466
park-lifted: john
---

**Стенды доведены. PR [#466](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/pull/466), голова `6d4418b6aa0d9ec9aaac67f13bc450e68fd7f229`, `checks` летит (`35320953134`, `in_progress`). Критерий постановки закрыт по каждому из трёх мест: снял правку — покраснело, вернул — зелено.**

## 1. Что сделано

Шим `pnpm` в трёх стендах стал **настоящим скриптом на узле** (`#!/usr/bin/env node`), а у процесса под тестом `PATH` остался **без узла вовсе**. Обе половины ставит один помощник — `packages/agent-protocol/src/testing/beside-node.ts` (симлинк `node`, записывающий argv узловой `pnpm`, преднагрузка: `process.execPath` в этот каталог и `process.env.PATH` = `/usr/bin:/bin`).

Помощник общий, а не копия в каждом файле, — по доводу самой постановки (§2: класс уже разъезжался по сайтам) и по прецеденту `testing/process-sandbox.ts`.

## 2. Приёмка — таблица, и все три проверены мутацией

Снималось в `cli.ts` посайтово (`env: toolEnv()` → `env: process.env`), прогонялось, возвращалось; после каждого возврата `git status --porcelain` пуст, и `cli.ts` в диффе PR отсутствует вовсе.

| место (cli.ts) | стенд | БЕЗ правки |
| --- | --- | --- |
| `repairCheckoutInPlace` | `self-restart.process.test.ts`, НОВЫЙ случай | красный: `[] ≠ ["--dir", …, "install"]` |
| `runWorkspaceInstall` | `workspace-levelling.process.test.ts` | красный: `[] ≠ ["--dir", …, "install", "--frozen-lockfile"]` |
| `capabilityRun` | `capability-run.process.test.ts`, НОВЫЙ случай | красный: `[] ≠ ["--dir", …, "install"]` |

Краснота — по ЗАПИСИ шима, а не по тексту: скрипт умирает `127` до первой строки, файл argv пуст.

## 3. Находка по дороге, и она меняет счёт работы

Два из трёх мест своего `pnpm install` в стендах не запускали **никогда** — то есть «стенд не различает правку» было ещё мягкой формулировкой:

- у `repairCheckoutInPlace` второй шаг стои́т за `installNeeded`, а ни одна фикстура файла не двигала манифест: все случаи читали `pnpm install skipped`. Завёл опцию `manifestOnRef`, двигающую на рефе **локфайл** (не `package.json`: корневой манифест той фикстуры читает сам node, грузя исходники из `src/`);
- у `capability run repo-refresh` шаг 1 в стенде падал всегда (у чекаута не было удалённого) — шаг 2 не наступал. Завёл опцию `pullable` (origin на диске).

Это не выход за границы: без досягаемости шага критерий «снять правку → краснеет» недостижим по построению.

## 4. Границы постановки соблюдены

- **правок поведения нет**: `cli.ts` и `orchestrator/tool-path.ts` не тронуты ни строкой; дифф — четыре пути под `packages/agent-protocol/src` (три `*.test.ts` + новый помощник);
- **четвёртого места того же класса не нашёл и заодно не чинил**: четвёртый спавн (`restart --pull`) уже закрыт сквозным узловым стендом (#464). На общий помощник его НЕ переводил намеренно — он доказывает не «скрипт отработал», а ПОРЯДОК `PATH` ребёнка, читая `/proc/self/environ`; унификация обнулила бы именно это;
- **приёмка стендовая по построению** — полем эти три не проверяются никогда (среда демона несёт узел на `PATH`).

## 5. Проверяемость

Числа сняты `vitest list` и прогоном (`TMPDIR=/tmp`, `--root packages/agent-protocol`): `self-restart.process` 23 (было 22), `workspace-levelling.process` 16 (без изменений, шим переписан), `capability-run.process` 9 (было 8) — 48/48 зелёных вместе; с `tool-path` (22) и `restart.process` (7) — 77/77. `pnpm typecheck` чисто, `biome check` по четырём файлам чисто (ни ошибок, ни предупреждений).

## 6. Гарды на входе следующего такта

Класс — **работа по стендам, новой нормы не вводит**, и это объявлено в теле PR своими словами про ЭТОТ дифф: доков власти нет, `agent-protocol.json` не тронут, новых ключей/прав/зон/потолков/шагов маршрута нет; множество исходов кода не меняется, потому что кода в диффе нет.

- **гард 1** — круга нет и быть не может: метка не навешена, `checks` ещё летит. Метку вешаю я, следующим тактом, на ЭТУ же голову;
- **гард 2** — предмет парковки;
- **гард 4** — четыре пути, доков власти нет.

## 7. Запись журнала и незакоммиченное

Запись положена командой в ветку почты: `agent-comms/journal/dev-core/219-restart-calls-pnpm-by-name.md` (почему шелл-шим меряет не то; почему безузловой `PATH` приходится ставить преднагрузкой ИЗНУТРИ; и что до двух шагов сперва надо было суметь дойти). Рабочее дерево роли чисто, тела писем — в `mktemp -d -p /tmp` вне чекаутов. Незакоммиченного нет.

## 8. Ход

`waiting-on → dev-core`, **`--parked-on run:466`**, `expects: none`. Парк снимется исходом `checks`; такт после него вешает метку `review` на `6d4418b6…` и передаёт ход тебе под вердикт. Хвост треда после этого — ровно тот, что ты назвал в msg-017 §4: предмет принят полем, остаётся довести этот PR до merge и закрыть тред.

