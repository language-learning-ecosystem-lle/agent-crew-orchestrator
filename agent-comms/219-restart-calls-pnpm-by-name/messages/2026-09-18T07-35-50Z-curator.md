---
from: curator
worker: claude-code
session: 558b106d-25b3-4f98-b020-96847eebb354
raised: 2026-09-18T07:32:00Z
date: 2026-09-18T07:35:50Z
expects: ack
waiting-on: curator
parked-on: john
park-ground: no-delivers-since:219-restart-calls-pnpm-by-name
---

**ТРЕБУЕТСЯ РЕШЕНИЕ JOHN, ОДИН ВОПРОС, ответ «нет» годится: доводить ли стенды трёх ОСТАВШИХСЯ точек `env: toolEnv()` до шимов с настоящим `#!/usr/bin/env node`? Предмет треда исполнен и принят полем; открыт только этот вопрос, и он чисто стендовый — дешёвого восхождения у него нет, поэтому он твой.**

Прошлое моё письмо (`msg-004`, `07:31:30Z`) оставило ход себе БЕЗ парка — тред стал голым кандидатом, и ящик поднял пару через полминуты в ленту, где ничего не изменилось. Один пустой подъём, цена моя. Этим письмом ставлю парк, которым то письмо должно было кончиться.

## 1. Состояние треда на `07:40Z` — закрыто всё, кроме §2

- **П-1, П-2, П-4 приняты полем** — вывод твоего жеста разобран в `msg-004` §1–2: `pnpm install — ok` из окружения БЕЗ nvm в `PATH`, оба прежних провала (`code ?` 12:23Z и `127` 15:49Z) закрыты каждый своим прибором;
- **П-3 закрыт твоим же решением** (`msg-008` §2) по `git`/`gh`/`du`/`id`/`ssh` — не пересматривала;
- **PR #462 и #464 смёржены**, следы гардов поимённо в `msg-005` и `msg-003` §1;
- **хвоста за мной нет:** `gh pr list --state open` на `07:33Z` — пусто. Рабочее дерево роли чисто, незакоммиченного нет.

## 2. Вопрос — и замер под ним теперь МОЙ, а не пересказ ревьюера

Починка `env: toolEnv()` стои́т в ЧЕТЫРЁХ местах (`cli.ts:1169` — починка на месте у поднадзорного демона, `:7414` — выравнивание рабочего места, `:16448` — оба шага `restart --pull`, `:16745` — право `repo-refresh`). Строка на всех четырёх текстуально одна.

Свидетелей у неё не четыре, и прибор для этого даровой — **первая строка шима в процессном стенде** (`grep -n '#!/' <стенд>`, исполнено мной `07:36Z`):

| точка | стенд | шим | что стенд меряет |
| --- | --- | --- | --- |
| `restart --pull` | `restart.process.test.ts:335` | `#!/usr/bin/env node` | **АДРЕС и СРЕДУ.** В файле комментарий прямо называет шебанг причиной существования шима |
| выравнивание | `workspace-levelling.process.test.ts:219–222` | `#!/bin/sh` | только АДРЕС: утверждение `:327` — строка выбора «beside this node binary» |
| починка на месте | `self-restart.process.test.ts` | шима с шебангом нет вовсе; про установку утверждается `pnpm install skipped` (`:1163`) | ни того, ни другого |
| `repo-refresh` | `roles/capability-run.process.test.ts` | шима с шебангом нет вовсе | ни того, ни другого |

**Читается это одной фразой: шим `#!/bin/sh` интерпретатора не просит, поэтому такой стенд не может покраснеть от снятия `env: toolEnv()`.** Ревьюер назвал ту же точку осторожнее («шимы `#!/bin/sh`»); мой замер даёт вывод тот же, но сильнее — на двух сайтах из трёх шима с шебангом НЕТ НИ ОДНОГО.

**И полем эти три не проверятся никогда** — все три исполняются в среде демона, где `node` в `PATH` уже есть. Различает их только стенд; это ровно то ограничение, из-за которого П-4 потребовала твоей руки.

## 3. Что стои́т «да» и почему гард 3 упирается в тебя

- **работа:** один PR dev-core, дифф **чисто тестовый** — три стенда получают шим вида `#!/usr/bin/env node` и `PATH` без node у ребёнка, как уже сделано в `restart.process.test.ts`. Пакетного кода правка не трогает;
- **восхождение:** полевого дефекта на тех трёх точках не замерено ни одного, значит класс «полевой измеренный дефект, новой нормы не вводит» на эту работу не натягивается. Стоячей нормы под «доведи стенд до различающего» тоже нет. Остаётся третья форма — твоё слово, и это письмо ровно то место, где оно кладётся в ленту;
- **цена «нет»:** записываю отказ в ленте, закрываю тред следующим тактом и больше предмет не поднимаю.

## 4. Рекомендация роли (рекомендация ≠ решение)

**Да, низким приоритетом.** Довод один и он из этого же треда: класс уже РАЗЪЕХАЛСЯ по сайтам — путь рестарта починили в #462, а путь выравнивания остался сломанным до `15:49Z` и отдельного разбора (тред `221`), и ни один зелёный стенд этого не поймал. Второй раз поймать это нечем, кроме стенда, который может покраснеть.

## 5. Ход

`waiting-on → curator`, **`--parked-on john`**, `expects: ack` — звонящая форма выбрана намеренно: без твоего слова тред не двинется, а тихий парк (`--expects none`) не позвонил бы ни разу. Тред **не закрываю**: закрытие есть приёмка, а вопрос §2 при закрытии потерялся бы молча. Парк снимется твоим словом — «да» уедет постановкой dev-core, «нет» закроет тред.

