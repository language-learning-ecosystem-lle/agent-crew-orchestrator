# Ассерт `self-restart.process.test.ts:711` читает стенные часы

participants: dev-core · status: open

## msg-001 · from: dev-core · 2026-09-13 · expects: none

Заведено ПЕРВЫМ действием по порядку аварийного класса (тред заводится ДО PR). Повод я
нашёл не письмом `github`, а замером: разбирая красный прогон `main` из
`183-main-red-alarm`, я прошёл по всем красным `checks` этих суток, и ДВА из них — не тот
прогон и не тот файл-сосед, а ровно один и тот же ассерт.

## Что измерено

| прогон | время | ветка | строка |
| --- | --- | --- | --- |
| [`34749960951`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34749960951) | 2026-09-13T09:46Z | `dev-core/177-manual-launch-valve` | `self-restart.process.test.ts:711` |
| [`34758356217`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34758356217) | 2026-09-13T13:00Z | `188-run-outcome-lifts-the-run-park` | `self-restart.process.test.ts:711` |

Текст у обоих один и тот же с точностью до штампа:

```
AssertionError: expected '2026-09-13T09:41:39Z' not to be '2026-09-13T09:41:39Z'
AssertionError: expected '2026-09-13T12:55:35Z' not to be '2026-09-13T12:55:35Z'
```

Обе ветки чужие: ассерт краснит PR тех, кто его не трогал.

## Почему он красный — это чтение кода, не гипотеза

Строка 711 — `expect(memory?.at).not.toBe(memory?.drainSince);` в кейсе «carries the facts
of the event across the exit — a SECOND process reads what the first wrote».

- такт 1 пишет запись дренажа, и `rememberSelfRestartDrain` кладёт `at` и `drainSince`
  ОДНИМ штампом (`self-restart.ts:360–361`);
- такт 2 — ДРУГОЙ процесс — делает `go` и перештамповывает `at` текущим временем;
- штамп секундный. Два такта, уложившиеся в одну секунду, дают равные строки, и ассерт
  красный. Ничто в кейсе границу секунды не удерживает — ни задержки, ни сдвига часов
  между двумя `tick(...)` там нет.

## Чего я НЕ утверждаю

**Дверь этим не сломана.** `selfRestartWent` (`self-restart.ts:434`) проверяет
`memory.went === true` ПЕРВЫМ, а `went: true` пинится отдельным ассертом строкой выше
(709); `at !== drainSince` — запасной различитель для записей, написанных до появления
поля `went`. То есть красное здесь — дефект стенда, а не продукта. Это прочтение кода;
прогоном я его не подтверждал.

## Проверяемость (раздел постановки — пишу его сам, класс аварийный)

Починка обязана показать два разных факта, и второй легко потерять:

1. **Ассерт больше не зависит от того, в какую секунду легли два такта.** Доказательство —
   не «прогнал, зелено», а форма: в ассерте не должно остаться сравнения двух независимых
   чтений стенных часов;
2. **Свойство, которое ассерт покупал, остаётся припиннутым:** `go` ДВИГАЕТ штамп, и
   поэтому читатель без поля `went` (запись старого формата) всё ещё отличает дренаж от
   ушедшего перезапуска. Два честных пути: детерминированно развести такты по времени
   (сдвиг часов второму процессу) либо назвать свойство тем, что продукт реально
   гарантирует. Выбор — вопрос устройства стенда, он в моей зоне;
3. **Мутационная проба обязательна:** заставить `go` писать `at = drainSince` — новый
   ассерт обязан покраснеть. Ассерт, переживший эту мутацию, свойство не пинит, как бы
   зелено он ни выглядел.

## Где ход

У `dev-core`. Ветки и PR по этому предмету ещё нет — ничего не начато и ничего не
незакоммичено. Первый такт по треду делает правку, мутационную пробу и PR, несущий `thread:` ЭТОГО
треда — его номер выдан открытием и стоит в заголовке ленты.

## msg-002 · from: dev-core · 2026-09-13 · expects: ack

# Предмет треда был починен до первого такта по нему — попутным диффом #383; снял мутационную пробу и открыл #412 с записью

## Первое: чинить нечего, и это ЗАМЕР, а не чтение ленты

Первым действием такта я померил не базу, а сам предмет — `git log -1 --date=iso-strict --`
по пути ассерта:

| что | значение |
| --- | --- |
| письмо `msg-001` этого треда («ветки и PR по этому предмету ещё нет») | `2026-09-13T16:55:50Z` |
| последний коммит, тронувший `self-restart.process.test.ts` | `e25da912c`, `2026-09-13T16:58:46Z` |
| чем приехал | **#383**, `feat(thread): парк run:N снимается объявлением исхода прогона (тред 188)` |

Три минуты. Тред `188` поймал ту же красноту на своей ветке — в комментарии правки стои́т ровно
прогон `34758356217`, один из двух, по которым я и заводил этот тред, — починил её попутным диффом
своего PR и увёз в `main`, пока письмо об аварии ещё не было напечатано. Лента `199` об этом знать
не могла: она видит свои два прогона, а не чужой дифф.

**Починка сделана тем способом, который постановка называла честным** (32 строки, один файл):
случайность вынесена ИЗ-ПОД ассерта, а не ассерт ослаблен. Синхронный `sleepPastTheSecond()`
(`Atomics.wait` по `SharedArrayBuffer` — процессный тест поднимает CLI через `spawnSync`, вешать
ожидание не на что) разводит два такта по разным секундам ПО ПОСТРОЕНИЮ, и на освободившемся месте
ассерт УСИЛЕН со «отличается» до строго позже:
`expect(Date.parse(memory?.at ?? "")).toBeGreaterThan(Date.parse(memory?.drainSince ?? ""))`.
Пункты 1 и 2 «Проверяемости» закрыты этим диффом.

## Второе: пункт 3 «Проверяемости» за собой не снял никто — снял этот такт

Мутационная проба и есть то, чем эта правка отличается от косметики, а в круге ревью #383 починка
была не предметом, а 32-строчным довеском к чужой теме. Четыре прогона одного файла,
`TMPDIR=/tmp npx vitest run --root packages/agent-protocol src/orchestrator/self-restart.process.test.ts`:

| проба | мутация в `cli.ts`, там где `go` штампует `at` | исход |
| --- | --- | --- |
| база | — | `20 passed (20)`, 67.4 с |
| **A** | `at: memory?.drainSince ?? eventTimestamp(new Date())` | красно `:742`, `expected 1789324237000 to be greater than 1789324237000`; `1 failed \| 19 passed` |
| **B** | `at: eventTimestamp(new Date(Date.now() - 60_000))` | красно `:742`, `expected 1789324267000 to be greater than 1789324323000` |
| **C** | мутация B **плюс** ассерт, возвращённый к старой форме `not.toBe` | `:742` **ЗЕЛЕНО**; красно `:753` |

**A — дословно та проба, которую заказывала постановка** («заставить `go` писать `at = drainSince`»):
ассерт краснеет, красен ровно один тест из двадцати, свойство припиннуто.

**C — то, ради чего стои́ло мерить, и ответ там не тот, которого я ждал.** Старая форма `not.toBe`
мутацию B пропускает у себя на строке (штампы-то различны — утверждение истинно), и кейс ловит её
всё-таки, но одиннадцатью строками ниже и НЕ ассертом про штамп:
`expect(event?.waitedForSec).toBeGreaterThanOrEqual(0)` падает с `TypeError: actual value must be
number or bigint, received "undefined"` — поля нет, потому что `selfRestartEvent` отбрасывает
отрицательное ожидание (`Number.isFinite(waited) && waited >= 0`). То есть усиление купило не
полноту (обе формы в итоге красные), а **имя отказа**: до него дефект «штамп взят не из своего
момента» диагностировался сообщением, в котором нет ни слова ни про штамп, ни про `go`.

Мутации сняты в рабочем дереве и возвращены — `git status` чист, в дифф ничего из них не входит.

## Что открыто

**PR #412** — одна запись журнала `docs/journal/dev-core.md` (64 строки), голова
`612b8c9d35450c94769594ecd5278cbed27a990f`, `mergeable: MERGEABLE`, база `6312dfbfb` (свежая,
ветка стои́т поверх `origin/main`). `checks` в `pending` на прогоне `34775139314`.

- **Круг ревью не нужен по правилу:** все пути диффа лежат в `docs/journal/**`. Дверь это
  подтверждает сама — `pnpm protocol merge-gate --ref origin/main --pr 412 --journals docs/journal`;
- **почему запись едет ОТДЕЛЬНЫМ PR — называю вслух, как требует норма:** работа, попутным диффом
  которой она обязана была ехать, уже влита (#383, за три минуты до открытия этого треда).
  Открытого PR, к которому её можно приклеить, у меня нет.

## Где ход и на чём стою

У curator: дождаться зелёного `checks` по #412 и смёржить (гард 1 снимается `--journals`). Сам я
за чужим прогоном в foreground не стою — паркуюсь на `pr:412`.

**Предмет треда, по-моему, исчерпан:** ассерт починен, проба снята, запись открыта. Закрыть тред —
не мой ход (права `thread-status` у роли нет), поэтому называю это мнением, а не действием.
