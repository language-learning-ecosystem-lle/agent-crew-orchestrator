# Стоячий адрес: имя соседнего дома в этом дереве

participants: github, dev-core, curator · status: open

## msg-001 · from: github · 2026-09-06 · expects: none

🏠 **Имя соседнего дома в этом дереве: новых мест — 3.**

голова `a7b5dadf239d54a2c1ad6e8e27ec27f63e250418` · прогон https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34037382205

Это ЗВОНОК, а не отказ: прогон смотрителя зелёный, `checks` не тронут, PR не краснеет. Класс — операционное знание соседнего дома в тексте этого репозитория (тред `064-forget-the-consumer`, слово john 2026-08-30 и 2026-09-05).

## Новое относительно родителя коммита

- `docs/install-notes.md:612` — `  /home/lle/projects/language-learning-ecosystem/node_modules/tsx/dist/loader.mjs`
- `docs/install-notes.md:613` — `  /home/lle/projects/language-learning-ecosystem/node_modules/agent-protocol/src/cli.ts`
- `docs/install-notes.md:630` — `| `/home/lle/projects/language-learning-ecosystem` | `lang-hetzner:contour-lang` | `drwxrwsr-x` | чекаут контура lang |`

Законные места вычтены машинно: собственный адрес — из `GITHUB_REPOSITORY`, остальное — списком с причинами в `.github/scripts/foreign-name-watch.allow`. Место в списке отсутствует, а стои́т законно — это правка списка с причиной, а не молчание.

## msg-002 · from: curator · 2026-09-06 · expects: none

# Звонок разрешён в сторону СПИСКА: имя в этих трёх строках несущее — PR #313

Развилка «список или текст» на голове `a7b5dadf239d54a2c1ad6e8e27ec27f63e250418`
(прогон 34037382205) разрешена одной проверкой — **врёт ли текст без имени.** Здесь врёт,
поэтому правится список исключений, а не док.

## Что замерено (локально, даром — ни CI, ни прогона роли)

```
. .github/scripts/foreign-name-watch.sh
GITHUB_REPOSITORY=language-learning-ecosystem-lle/agent-crew-orchestrator \
  foreign_name_watch_scan . .github/scripts/foreign-name-watch.names .github/scripts/foreign-name-watch.allow
```

| замер | ожидалось | получено |
| --- | --- | --- |
| рабочее дерево, ПРЕЖНИЙ список (копия `HEAD` в `mktemp -d -p /tmp`) | те самые три места | `docs/install-notes.md` 612, 613, 630 — строка в строку с письмом смотрителя |
| рабочее дерево, НОВЫЙ список | ноль | ноль |
| копия того же дока с ДРУГИМ вхождением имени («пин потребителя: тред 077 … v1.2.3») | находка | находка — исключение не блокетное |
| `bash .github/scripts/foreign-name-watch.test.sh` | зелено | все проверки прошли, включая «в боевом списке исключений нет адресации номером строки» |

Свой ноль подпёрт контролем намеренно: без него ноль доказывал бы узкий шаблон, а не решение.

## Почему имя несущее, а не класс `064`

Три строки стоят в разделе `docs/install-notes.md` «Оба контура исполняют код и рантайм из дома
ОПЕРАТОРА — разведены учётки, не раскладка». Предмет раздела — ЖИВОЙ ЯЩИК, а не соседний дом:

- 612–613 — строки запуска обоих демонов, снятые `ps` **дословно** («Строки запуска обоих
  демонов, полностью»). Переименовать значит соврать в дословной цитате;
- 630 — строка таблицы `stat`: путь здесь ключ строки, а её содержание — владелец
  (`lang-hetzner:contour-lang`) и режим (`drwxrwsr-x`), то есть права на НАШЕЙ машине;
- вывод раздела («любая уборка дома `lle` гасит ОБА контура», пункт 3: «чекауты лежат ПОД домом
  оператора») держится ровно на том, что эти пути лежат в `/home/lle/projects`.

Класс `064` — операционное знание чужого дома: его имена, пины, воркфлоу, номера тредов,
раскладка. Ничего этого в трёх строках нет; есть идентификатор нашей машины — тот же класс, что
уже назван причиной у `docs/box-setup.md` (весь файл) и у `agent-protocol.json` (сужение
`/home/lle/projects`, приёмка 2026-09-05). Строчное `lle` в набор имён не кладётся по той же
причине и тем же решением.

## Форма правки

Одна строка в `.github/scripts/foreign-name-watch.allow`, **суженная содержанием строки**
(`/home/lle/projects`), а не `-`: весь файл исключать нельзя — любое ДРУГОЕ вхождение имени
соседа в `docs/install-notes.md` обязано звонить, и это замерено контролем выше. Номеров строк в
исключении нет. Причина названа классом, а не случаем.

## PR и гарды

**PR #313** (`chore(foreign-name-watch): пути живого ящика в install-notes — законное место,
названное причиной (тред 148)`), ветка `curator/148-box-paths-in-install-notes`, голова
`cccb23ae`, открыт `pnpm protocol pr open` с `thread:`/`role:` в теле.

Гард 3 объявляется ДО merge классом **«полевой измеренный дефект, новой нормы не вводит»**, три
условия поимённо:

1. **первоисточник замера** — письмо смотрителя в ЭТОМ треде: голова
   `a7b5dadf239d54a2c1ad6e8e27ec27f63e250418`, прогон 34037382205, три места названы построчно;
2. **исполнитель (curator) подтверждает: новой нормы дифф не вводит** — ни поля, ни ключа
   конфига, ни формы сообщения, ни права, ни шага маршрута; запрета не снимает и не сужает:
   правка списка с причиной объявлена самим смотрителем штатным ответом на законное место
   («Место в списке отсутствует, а стои́т законно — это правка списка с причиной, а не
   молчание»), а граница класса `064` остаётся словом john 2026-08-30 и 2026-09-05;
3. **ревьюер подтверждает то же чтением диффа** — закрывается кругом ревью.

Доков власти дифф не трогает: `.github/scripts/**` — не `workflows`, гард 4 сюда не достаёт.

## Чего в этом ходе НЕ сделано и что дальше

`checks` на `cccb23ae` на момент письма **pending** (прогон 34048929905). Метка `review` вешается
ПОСЛЕ зелёного `checks` на той же голове — значит на следующем такте: зелёный прогон приедет в
тред событием, ход я не держу. Дальше — вердикт круга, merge по гардам со следом в этот тред.

Запаркован на `run:313`: у PR нет метки `review`, а зелёный `checks` на PR без метки — то самое
названное исключение, на котором `ci-outcome.yml` возвращает ход роли из строки `role:`
(`docs/install-notes.md`, §1). Красный исход вернёт ход тем же письмом.
