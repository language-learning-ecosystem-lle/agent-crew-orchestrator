---
from: curator
worker: claude-code
session: ab2306e6-ab80-45ee-98cc-ef8a5c4e253c
raised: 2026-09-07T15:47:10Z
date: 2026-09-07T15:49:33Z
expects: none
waiting-on: —
---

**Тред 141 закрыт этим же ходом — по слову john «ЗАКРЫВАЙ» (доставка чата, предыдущее сообщение curator в этой ленте). Ниже — ВТОРАЯ проверка приёмки, снятая моей рукой в этом такте, и хвост открытых PR.**

## 1. Приёмка перемерена, а не переписана из ленты

Закрытие треда — приёмка, и она не принимается по докладу, даже по своему собственному. Шесть пакетов постановки проверены командой сейчас (07.09, ~15:50–15:55Z), а не процитированы:

- `git merge-base --is-ancestor <sha> origin/main` — четыре названных коммита ЛЕЖАТ в `main`: `ae2db572` (мягкий дренаж демона до самопереза), `54c5c2ab` (письмо о самоперезапуске), `38c640c6` (`docs/install-notes`: минимальная версия systemd), `d6f5825d` (`stop` без `--mode` называет `down`);
- `gh pr view --json state,mergedAt,baseRefName` — **#301** MERGED в `main` 2026-09-06T16:24:38Z, **#310** MERGED в `main` 2026-09-06T18:36:54Z. Цель у обоих `base.ref: main`, прочитана, а не подразумевается;
- сухой прогон двери закрытия (`thread status … --status closed` без `--write`) ответил `would set '141-daemon-drift-needs-a-human' from 'open' to 'closed'` — то есть на момент проверки тред стоял `open`, и закрываю его я, а не кто-то до меня.

**Открытых PR по треду 141 нет** — проверено перечислением, а не памятью (см. §2).

## 2. Хвост открытых PR — прогнан, своих кандидатов нет

`gh pr list --state open` даёт РОВНО два PR, и оба чужого предмета: **#326** (`docs/155-version-bump-criterion`, тред `155`) и **#328** (`dev-core/157-pr-open-body-outside-checkout`, тред `157`). Ни один не принадлежит 141.

Дверь спрошена по обоим (`merge-gate --ref origin/main --pr <n> --review-workflow 'Claude PR Review'`), и оба отказывают сами по себе — домерживать нечего:

- **#326 на голове `d7974a0`** — `STOP` гарда 1 (`no approve verdict on d7974a0`) и `STOP` гарда 4 (`it changes PROTOCOL.md` → это кнопка john, не моя). Гард 2 зелёный, но с нотой переезда базы: `c9a9ca4` лёг 15:41:43Z, `checks` стартовали 14:57:59Z;
- **#328 на голове `d40f7fe`** — `STOP` гарда 1: `changes were requested on d40f7fe (github-actions)` — это новый круг, а не merge. Гард 4 чист (6 путей, доков власти нет), гард 2 зелёный с той же нотой базы плюс нота непромеренной пары по `docs/protocol-reference.md`.

И вторая причина не трогать их отсюда, независимая от гардов: **след гарда 5 обязан лечь в ленту треда-владельца**, а этот прогон связан треду 141 и в `155`/`157` не пишет. Оба PR — предмет подъёма их собственных тредов.

## 3. Что уезжает с закрытием ОСОЗНАННО

`stop --mode <опечатка>` отказывает строкой, которая не называет `down` и не печатает usage. Факт **известен, замерен и не починен**; пакет 7 не ставится, класс не заводится. Повод возврата — полевой, а не срок: кто-то реально спотыкается об опечатку и уходит в `graceful` вместо `down`. Довод целиком — в предыдущем сообщении curator, §1.

## 4. Чего я в этом такте не делала

Не мёржила ничего, меток не вешала, тел PR не правила, кода не писала, в треды `155`, `157` и `161` не писала. В рабочем дереве роли незакоммиченного нет (`git status --porcelain` пуст); тело письма — в `mktemp -d -p /tmp`, вне чекаута почты и вне рабочего дерева роли.

**Ход не передаётся никому: предмет закрыт.** Следующим действием этой же сессии дверь закрытия зовётся с `--write`, и статус перечитывается контрольно.

waiting-on → —

