---
from: dev-core
worker: claude-code
session: 147e190e-392e-4bcf-ac45-7c8384b24b32
raised: 2026-09-06T13:32:27Z
date: 2026-09-06T13:34:00Z
expects: answer
waiting-on: curator
parked-on: run:297
---

**Метка `review` повешена на ту же голову, что была зелёной, — `8dcdf000`. Круг ревью поднялся: прогон `34036426404`, событие `pull_request`, голова та же, идёт. Паркуюсь на `run:297` — до вердикта делать нечего никому.**

## 1. Что проверено ПЕРЕД меткой

Не «пришло письмо про зелёное», а состояние на момент действия:

| | замер |
|---|---|
| голова PR | `8dcdf000d71e0553d356a6667be4e000d164727e` — та же, что в зелёном прогоне `34035916258` |
| `mergeable` | `MERGEABLE` |
| `mergeStateStatus` | `CLEAN` |
| метки до | пусто — висящей метки со старой головы нет |
| рабочее дерево роли | чисто |

Перечитывание `mergeable` тут не формальность: зелёное письмо говорит о прогоне, а не о том, что база с тех пор не уехала. Уехала бы — метку вешать было бы не на что: вердикт был бы о дереве, которого больше нет.

## 2. Что метка сделала — проверено, а не предположено

`pull_request: types: [labeled]` поднял круг: прогон `34036426404`, `event: pull_request`, `headSha: 8dcdf000…`, `status: in_progress`, создан в `13:32:59Z`. Это ровно тот якорь, который потом ищет гард 1 `merge-gate`: прогон ревьюера с событием `pull_request` НА ТЕКУЩЕЙ голове, вердикт внутри его окна.

## 3. Что дальше и чей ход

Круг отчитывается в тред сам — ждать его в foreground нельзя и незачем. Дальше:

- **вердикт `approve`** → ход ваш под пять гардов и кнопку (дифф #297 — код, тесты, README и `docs/protocol-reference.md`; доки власти не тронуты, `.github/**` не тронут, значит кнопка ваша, не john);
- **вердикт `needs-fixes`** → ход возвращается мне, и я довожу до нового круга сам: после смены головы метку надо СНЯТЬ И ПОВЕСИТЬ ЗАНОВО, иначе висящая метка означает вердикт о дереве, которого больше нет.

Парк — `run:297`: впереди чужой ход (круг), поэтому `run:`, а не `pr:` — парк на кнопку заблокировал бы собственное снятие.

## 4. Предмет треда: оба вопроса к постановке СТОЯТ

Второй пакет — **письмо о самоперезапуске** (§3 постановки: какой код был, какой стал, сколько отставал, сколько ЖДАЛ сессию) — я без ответов не начинаю, и это не упрямство: от ответов зависит форма, а не только адрес.

1. **Куда звонит письмо о перезапуске** — в дайджест смотрителя (там же, где сейчас стоит дрейф) или письмом в стоячий адрес? Второе стоит выбора адреса, а открытый стоячий адрес никого не поднимает: `expects: none` человеку не звонит, а роль привязана к одному треду. Ровно этим кончился парк из msg-003 — его пришлось снимать рукой вне подъёма.
2. **§4.3 «починить `up` или назвать его поведение вслух»** — постановка говорит «рассмотреть», а не «сделать»; цена ошибки — упавшая служба у руки john в аварии. Форма (переименование, отказ по имени, флаг) — решение john через вас.

И то, что от этого зависит технически: **«сколько ждал сессию» придётся записывать в момент НАЧАЛА дренажа.** Сейчас начало дренажа не сохраняется никуда, кроме `daemon.log`, — после выхода 75 и подъёма на свежем коде этот факт взять неоткуда. Значит второй пакет несёт не только канал письма, но и свою память о событии; выбор канала меняет, что именно в ней хранить.

waiting-on → curator, parked-on run:297.

