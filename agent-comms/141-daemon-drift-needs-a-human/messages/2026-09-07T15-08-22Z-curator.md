---
from: curator
worker: claude-code
session: bc7d305c-f14b-404e-b1c3-386af2250e9d
raised: 2026-09-07T15:01:30Z
date: 2026-09-07T15:08:22Z
expects: ack
waiting-on: curator
parked-on: john
---

**ПАКЕТ 6 В `main` (сквош `d6f5825d`), приёмка перемерена МОЕЙ рукой на слитом дереве — вся объявленная работа треда 141 закончена, и тред готов к закрытию. Требуется решение john, одно слово: ОДИН факт, замеренный мной сегодня уже ПОСЛЕ merge, остался — `orchestrator stop --mode <опечатка>` отказывает строкой, которая `down` не называет ВОВСЕ и usage не печатает. «ЗАКРЫВАЙ» → закрываю тред, факт уезжает с ним; «ХВОСТ ЧИНИМ» → ставлю пакет 7 в этом же треде. Моя рекомендация — ЗАКРЫВАЙ, довод в §3.**

## 1. Гард 5 — след merge, гарды поимённо

`gh pr merge 325 --squash --match-head-commit 1129bf8cffcdced7cabeb3bd1be366dba254eacc`, `15:02:31Z`. Контрольное чтение цели (ответ инструмента фактом не считается): `state MERGED`, `mergedAt 2026-09-07T15:02:33Z`, **`base=main`**, сквош `d6f5825dd1eae9dab3be6fdec414d7bad8f4bac6`; `git fetch origin main` + `git log origin/main -1` — голова `main` это он же.

Гарды замерены дверью, а не глазом: `pnpm protocol merge-gate --ref origin/main --pr 325 --review-workflow 'Claude PR Review'`, `15:02:0xZ`:

- **гард 1 — `ok`.** `approved on 1129bf8 by github-actions — inside the round 34135426023 of 'Claude PR Review' on this head (2026-09-07T14:53:43Z…2026-09-07T14:58:54Z)`. То есть вердикт лежит В ОКНЕ закрытого успешного круга ревьюера на ТЕКУЩЕЙ голове, а не просто прицеплен к номеру PR. Сам вердикт — письмо `reviewer-pr` этой ленты, `verdict: approve`, `pr: 325`, «Находок по критериям нет», доставленное шагами прогона `34135426023`;
- **гард 2 — `ok`.** `2 check(s) green: review=SUCCESS, checks=SUCCESS` на голове `1129bf8cffcdced7cabeb3bd1be366dba254eacc` (сорок символов, прогон `checks` — `34134738735`, письмо `github` этой ленты). Голова merge — она же, сверена флагом `--match-head-commit`, а не глазом;
- **гард 3 — слово john, прямое.** «ЧИНИМ» (чат 2026-09-07 ~`13:29Z`), положено в ЭТУ ленту моим письмом `2026-09-07T13-29-32Z-curator.md` полем `delivers: john` — ленту я перечитала сама перед кнопкой, а не взяла из своей же постановки. Там же дословно: «Ставь пакетом 6 в этом же треде; тред остаётся открытым до его merge». Машинная половина на месте: тело PR #325 несёт `thread: 141-daemon-drift-needs-a-human` и `role: dev-core`. **Полевой класс «измеренный дефект» здесь НЕ объявлялся и не применялся** — надобности в нём нет;
- **гард 4 — `ok`.** `4 changed path(s), none of them a document of power`; список доков власти дверь вывела данными — 8 путей (`agent-protocol.json`, четыре карточки ролей, `REVIEWER.md`, `PROTOCOL.md`, `.github/workflows`), из них `PROTOCOL.md` и `.github/workflows` — полем `powerDocuments` конфига;
- **гард 5 — это письмо.**

Merge только squash, заголовок в Conventional Commits: `fix(orchestrator): `stop` без `--mode` называет `down` по имени (тред 141) (#325)`.

## 2. Приёмка §3 перемерена на СЛИТОМ дереве, а не на голове PR

Причина мерить заново: прогон PR судил базу СВОЕГО момента, а ветка отставала от `main` на один коммит — зелень на голове PR не есть зелень слитого дерева. Дерево `git worktree add --detach` от `d6f5825d` в `mktemp -d -p /tmp` (вне чекаута роли и вне чекаута почты, R17), симлинки на `node_modules`, `git rev-parse HEAD` = `d6f5825dd1eae9dab3be6fdec414d7bad8f4bac6` — почва названа, а не подразумевается; после замера дерево снято `git worktree remove --force`.

`15:04Z`, дословно:

1. **`orchestrator stop` без флагов** → `EXIT=2`; вывод несёт `'orchestrator down'` («SOFT, and the one usually wanted»), `'orchestrator stop --mode force …'` («HARD»), отдельную строку про `graceful` (тот же флаг, ни pid, ни строки про запуски) и обе формы, нарезанные из `USAGE`;
2. **`orchestrator stop --ref origin/main`** (тоже без `--mode`) → `EXIT=2`, `orchestrator down` — **2 совпадения**, `stop --mode force` — **1**;
3. **флаг не создан — проверено ДО и ПОСЛЕ обоих вызовов:** каталог `--stop-flag` пуст и до, и после; `git status --porcelain` во временном дереве после всех вызовов — только два моих симлинка `node_modules`. Это и есть отличие ОТКАЗА от наполовину сделанной остановки, ради которого пункт стоял в постановке;
4. **`stop --mode force --ref origin/main`** (без `--by`) → `EXIT=2`, `agent-protocol: --by is not set` с прежним блоком usage: дверь режима пройдена, ветка форса открывается как открывалась, буква в букву;
5. **`stop --mode graceful … --stop-flag <tmp>`** без `--write` → `EXIT=0`, `would create the stop flag '<путь>' … --write performs it`, флаг не создан — буква в букву как раньше;
6. **`down` и `restart` своей рукой не гоняла** (они ПИШУТ: stop-флаг, письмо трассы) — их закрывает сюита, перемеренная мной на том же слитом дереве: `stop-mode-refusal.process`, `restart.process`, `self-restart.process`, `force-stop-delivery.process` — **4 файла, 30 проверок, зелено, `EXIT=0`**; отдельным прогоном контроль числа нового файла — **7 проверок**, ровно как заявлено докладом dev-core и вердиктом ревьюера.

Итог: все шесть пунктов §3 держатся НА ТОМ ДЕРЕВЕ, КОТОРОЕ ЛЕЖИТ В `main`.

## 3. Единственный оставшийся факт — и почему он идёт к вам, а не в мой пакет

**Замер, моя рука, слитое дерево `d6f5825d`, `15:05Z`:**

```
$ … orchestrator stop --mode sideways --ref origin/main
agent-protocol: --mode 'sideways' — allowed values are graceful | force
EXIT=2
'orchestrator down' в этом выводе: 0 совпадений
```

Одна строка, `down` не назван, блока usage нет вовсе. То есть рука, ОПЕЧАТАВШАЯСЯ в значении `--mode`, получает совет «пиши graceful или force» — и уходит в `graceful`, ту самую мягкую форму, которая, по вашему же доводу, отвечает ХУЖЕ `down` (ни pid, ни строки про запуски).

**Почему я не решаю это своей рукой, хотя формально могла бы.** Гард 3 такой починке дался бы полевым классом «измеренный дефект, новой нормы не вводит» — первоисточник замера есть (эта лента, сегодня, тред 141, моя рука на слитом дереве), нормы дифф не вводит (ни флага, ни ключа, ни формы сообщения, ни права, ни шага маршрута, ни снятия запрета). Механизм есть, и в этом я не сомневаюсь.

Сомневаюсь я в другом, и это ваш вопрос, а не мой: **вы объявили концом треда merge пакета 6, и пакет 6 в `main`.** Открывать за ним седьмой — расход, о котором вы не говорили. Плюс правило моей же карточки: названный случай — образец, а не класс; честная постановка потребовала бы сперва перечислить ВСЕ отказы семейства `orchestrator`, которым место назвать `down`, а не чинить один найденный.

**Моя рекомендация — ЗАКРЫВАЙ.** Довод: дверь, в которую операторская рука бьётся штатно, — это голый `stop`, и она теперь лечит в момент ошибки. Опечатка в значении `--mode` — соседняя, более редкая дверь, и её отказ хотя бы называет допустимые значения, а не молчит. Рекомендация curator ≠ решение john: если вы читаете иначе, «ХВОСТ ЧИНИМ» — и я ставлю пакет 7 с перечислением всего семейства, а не одного образца.

**Правило последней строки применено сознательно:** факт замерен уже ПОСЛЕ вашего слова, вы его не видели, а закрытый тред никто не перечитывает. Молча увезти его закрытием я не стану — это ровно то, за что вы похвалили прошлый ход.

## 4. Состояние ВСЕГО предмета 141 — по фактам

| пакет | состояние |
|---|---|
| 1 · мягкий самоперезапуск (дренаж) | в `main`, сквош `ae2db572` |
| 2 · лечащий порядок в звонке о дрейфе | в `main` (#301) |
| 3 · письмо о событии самоперезапуска | в `main`, сквош `54c5c2ab` (#315) |
| 4 · вход/подъём | в `main` (#310); **живая приёмка ЗАКРЫТА** замером john на ящике `~10:00Z` 07.09 |
| 5 · минимальная версия systemd | в `main`, сквош `38c640c6` (#318) |
| 6 · `stop` без `--mode` называет `down` | **в `main`, сквош `d6f5825d` (#325), `15:02:33Z` — этим письмом** |

**§4.4 «окно самопочинки»** — закрыт пакетом 1 вашим словом 07.09; в списки несделанного не возвращается. **Открытых PR по треду 141 нет. Ненаписанного кода по объявленной постановке нет.**

## 5. Хвост открытых PR — прогнан, своих кандидатов в этом ходе нет

`gh pr list --state open --json number,title,headRefName,headRefOid,labels`, `15:05:57Z` — открыто три, и ни один не треда 141:

- **#327** `chore/release-0.2.11`, голова `513f8ff0`, меток нет — тред 158;
- **#326** `docs/155-version-bump-criterion`, голова `d7974a05`, меток нет — тред 155;
- **#322** `feat/063-review-round-state`, голова `7ee230d1`, метка `review` — тред 063.

Своей рукой я их в этом прогоне не трогаю и здесь не сужу: прогон связан ОДНИМ тредом, а след гарда 5 каждому нужен в СВОЕЙ ленте — доложить его отсюда нельзя. Называю их замером, чтобы следующая сессия не перемеряла хвост заново; головы указаны, потому что за час они уедут, и проверять надо будет их, а не мою память.

## 6. Чего я в этом такте НЕ делала

- кода не писала и в `packages/**` не входила — только читала и запускала;
- вердикта ревьюера не пересказывала как свой: гард 1 замерен дверью, окно круга названо числами;
- второй метки не вешала, тела PR не правила, у прогона не стояла;
- **тред не закрывала** — закрытие это приёмка, и оно ждёт вашего слова из §3;
- в рабочем дереве роли незакоммиченного нет (`git status --porcelain` пуст); временное дерево снято. Тело письма — в `mktemp -d -p /tmp`, вне чекаута почты и вне рабочего дерева роли.

Парк — на john, `expects: ack`; ход остаётся у curator (роль с `wake.mode: self` в `waiting-on` не принимается). Парк снимет ваше слово письмом-доставкой: «ЗАКРЫВАЙ» → тред закрывается тем же заходом, «ХВОСТ ЧИНИМ» → пакет 7.

waiting-on → curator; парк на john.

