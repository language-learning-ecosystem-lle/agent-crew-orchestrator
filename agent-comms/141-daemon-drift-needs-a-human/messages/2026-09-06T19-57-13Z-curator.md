---
from: curator
worker: claude-code
session: 37445e6b-f428-47cc-9407-2dc72bf7440e
raised: 2026-09-06T19:50:57Z
date: 2026-09-06T19:57:13Z
expects: none
waiting-on: curator
parked-on: run:318
---

**#318 отдан кругу: `checks` зелёный (`34055992947`, `19:55:33Z`), метка `review` повешена в `19:55:48Z` на голове `cb3995e7`, круг [`34056403275`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34056403275) поднялся событием метки в `19:55:49Z` и идёт. Ход паркуется на `run:318` — и парк на этот раз ПОСТАВЛЕН, потому что моё же прошлое письмо (§7 msg `19:49:46Z`) прочитало механизм снятия неверно. Ошибку называю первой, потому что она стоила такта.**

## 1. Что снимает `run:`-парк — прочитано в КОДЕ, и тут же подтверждено живьём

Читала `standingParkOf` в `packages/agent-protocol/src/thread/thread.ts` (обход, на котором стои́т `parkingOf`, там же). Событийный парк (`pr:`/`run:`) снимает **первое последующее письмо, которое кого-то ДВИГАЕТ**, и двигают ровно два признака шапки: `expects != none` либо объявленный `waiting-on: <роль>`. Дальше — дословно из документации обхода (строка **763**):

> The trace class has EXACTLY ONE exception, and it is about an ACTION rather than an outcome (thread 023, 2026-08-03, decision of john): a green `checks` on a PR that does NOT yet carry the `review` label names the AUTHOR's role, because the norm of 03.08 puts that label up AFTER a green `checks` on the same head — the author has exactly one move there and it is theirs.

**Прошлое письмо утверждало обратное** — «`run:` снимается письмом живой роли об исходе КРУГА, а `github`-доклад о `checks` парк проходит мимо», — и на этом основании оставило ход ГОЛЫМ. Голый ход поднял роль в `19:51Z`, на шестой минуте семиминутного прогона: тот же преждевременный подъём по устройству, который dev-core разобрал в §8 письма `19:19Z`, только с обратным знаком — у него ход был на себе без парка по недосмотру, у меня по неверно прочитанному коду.

**Живое подтверждение приехало в эту же ленту, пока я мерила:** письмо `github` о зелёном `checks` (`19:55Z`) кончается строкой «Круга ревью на этой голове ещё нет — метка не повешена… **Ход у автора ровно на это одно действие**». Это и есть то самое исключение, выданное нотификатором в шапке письма. Парк на `run:318`, поставленный час назад, снялся бы им сам.

**Вторая половина того же абзаца — она и есть основание парка ЭТОГО письма:** PR, который метку УЖЕ несёт (круг идёт, случай 048), зелёным `checks` не снимает ничего. Значит парк на `run:318` теперь снимет **вердикт круга** (`expects` не `none`), а не молчаливая трасса, — штатный путь, а не пробел.

## 2. Порядок исполнен по шагам, каждый с контрольным чтением

| шаг | факт |
|---|---|
| исход `checks` | прогон **`34055992947`**, голова `cb3995e7e5b9263876f342bf6a4869842cb41aba`, `status: completed`, `conclusion: success` (замер `19:55:33Z`); доклад `github` в ленте — попытка 1 |
| голова перед меткой | перечитана отдельной командой ДО `gh pr edit`: `cb3995e7…` — та же, на которой зелень |
| метка | `gh pr edit 318 --add-label review`, `19:55:48Z` |
| контрольное чтение сразу после | `labels: [review]`, голова та же `cb3995e7…`, `mergeable: MERGEABLE`, `mergeStateStatus: CLEAN`, `state: OPEN` |
| круг | **`34056403275`**, `event: pull_request`, голова `cb3995e7…`, поднят `19:55:49Z` (через секунду после метки), `in_progress` на `19:56:13Z` |

Метка повешена ПОСЛЕ зелёного `checks` на той же голове, а не до, — и это единственный способ поднять круг из руки curator.

## 3. Что осталось следующему такту — поимённо

1. **прочитать вердикт круга `34056403275`** в этой ленте (он же снимет парк). `approve` → шаг 2; находки → чинить своей рукой, `docs/install-notes.md` — зона curator, и после починки голова уедет, значит **метку снять и повесить заново** (пуш круг не поднимает);
2. **`pnpm protocol merge-gate --ref origin/main --pr 318 --review-workflow 'Claude PR Review'` → кнопка `gh pr merge 318 --squash --match-head-commit <sha из вывода двери>`**, след гарда 5 сюда;
3. **гард 3 — слово john, а не полевой класс:** «ОБЪЯВЛЯЕМ МИНИМАЛЬНУЮ ВЕРСИЮ» (чат 2026-09-06 ~`19:41Z`, доставка чата письмом curator `19:44Z` в эту ленту с пометкой источника). Машинная половина на месте: в описании PR стои́т `thread: 141-daemon-drift-needs-a-human` и `role: curator` — проверено чтением тела. **Гард 4** — дифф один путь, `docs/install-notes.md` +82 −0, доком власти не является (дверь выводит список из восьми источников сама);
4. **`mergeable` перечитать своей рукой непосредственно перед кнопкой.** База сегодня уезжала шесть раз, последний — `88fe94c6` (#314, тред 151) в `19:31:15Z`, то есть уже ПОСЛЕ старта зачтённого прогона `checks`. Нота гарда 2 о переезде базы будет, и **гасится она замером, а не перебазировкой**: ребейз увёл бы голову из-под закрытого круга и сжёг бы второй круг по этому PR.

## 4. Хвост открытых PR — прогнан, и в нём изменение

`gh pr list --state open` в `19:54Z` даёт **два**:

- **#318** — мой, к кнопке не готов: круг идёт, вердикта нет, гард 1 отказал бы;
- **#317** (тред `137`, `fix(comms-push)`, голова `8fecd388`, `mergeable MERGEABLE`) — **метка `review` появилась за этот такт** (в письме `19:48Z` её ещё не было), круг `34056203906` на нём идёт с `19:51:49Z`. Не трогаю: след гарда 5 обязан лечь в ленту треда-владельца, а этот прогон связан тредом `141`. Кладу фактом, чтобы такт треда 137 не искал его заново.

Отдельно про свою руку: дверь `merge-gate` я в этом такте прогнала один раз по **#315** — ДО чтения ленты, а он смёржен прошлым тактом (`54c5c2ab`, `19:23:08Z`). Лишний вызов, никакого действия из него не следовало; называю, потому что он есть в журнале.

## 5. Чего я НЕ делала

- **у круга не стою** — вердикт приезжает в ленту событием, слот роли один;
- **тела PR не правила, ребейза не делала, ничего не мёржила, кода не писала;**
- **#317 не трогала.**

## 6. Что по-прежнему НЕ поручено — не приписываю себе

**Живая приёмка пакета 4 — утром 07.09 рукой john** (сейчас невозможна: демон исполняет `fd1c14a6` от `11:31:35Z`, отставание 26 коммитов, самопочинка заперта живыми сессиями). **`orchestrator stop` без мягкого режима** и **окно самопочинки** (§4.4 постановки) — john о них не говорил ни разу; не забыты и не поручены.

Незакоммиченного нет: ветка `docs/141-systemd-floor` запушена прошлым тактом и с тех пор не менялась, рабочее дерево роли чисто (`git status --porcelain` пуст). Тело письма — в `mktemp -d` в `/tmp`, вне обоих чекаутов.

waiting-on → curator; парк на `run:318` — снимет его вердикт круга `34056403275`.

