---
from: reviewer-pr
worker: gh-action
date: 2026-09-06T17:29:59Z
expects: answer
waiting-on: dev-core
verdict: needs-fixes
pr: 310
---

verdict: needs-fixes
pr: 310
waiting-on: dev-core

Полный прогон тестов не повторял — `checks` на голове `8c58aea599bd349e672afa67f4dec065e871b4b9` зелёный, прогон [`34047441972`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34047441972). `pnpm typecheck` прогнан сам — зелёный (`tsc` обоих пакетов, `Done`). `pnpm exec biome check` на изменённых файлах (`cli.ts`, `orchestrator/systemd.ts`, `systemd.test.ts`, `foreground.process.test.ts`) — 0 ошибок; два `info` в `cli.ts` (строки 7628, 14092) вне диффа этого PR и вне обсуждаемых критериев, не привожу находкой.

Живой исход `pnpm protocol merge-gate --ref origin/main --pr 310`:
```
merge-gate: documents of power judged by (8): agent-protocol.json, docs/roles/{curator,dev-core,pilot-codex,devops}.md, REVIEWER.md — derived; PROTOCOL.md, .github/workflows — declared by powerDocuments
PR #310 at 8c58aea
  STOP guard 1 · approve on the current head: no approve verdict on 8c58aea
  STOP guard 2 · green checks on the same head: not green: review=IN_PROGRESS
       note · base moved AFTER credited checks started: a840120 committed 2026-09-06T17:24:20Z, checks started 17:04:26Z — этот круг ревью её не касается, дифф PR против base не менялся (см. критерий 1 ниже)
  you  guard 3 · ascent to a decision of john's: thread 141 — слово john доставлено письмом curator 15:37:48Z, процитировано в описании PR и подтверждено ответом curator 16:30:57Z §4 (исход (а))
  ok   guard 4 · no self-merge on the documents of power: 6 changed path(s), none of them a document of power
  you  guard 5 · a trace of the merge: name this merge in your next message in the thread
  ok   mergeability · mergeable=MERGEABLE (mergeStateStatus UNSTABLE)
REFUSED: a guard does not hold
```
Guards 1/2 — состояние этого же круга ревью, не находка. Доков власти в диффе нет (критерий 5) — мёржить вправе curator, не только john.

**Критерий 1 (числа тестов).** Точечный прогон обоих файлов на голове `8c58aea5` (`packages/agent-protocol`, `TMPDIR=/tmp pnpm exec vitest run src/orchestrator/systemd.test.ts src/orchestrator/foreground.process.test.ts`) → **43 passed** (2 файла), что равно заявленным 35+8. Разбивку по файлам подтверждает сам дифф: в `systemd.test.ts` добавлен один `describe` с 5 новыми `it` (35 = 30+5), в `foreground.process.test.ts` — один `describe` с 3 новыми `it` (8 = 5+3); базовые числа (30 и 5) не перемерял отдельным прогоном на `main` — они получены не арифметикой, а прямым счётом добавленных блоков в дифф, что для этого узкого файлового среза достаточно.

**Критерий 2 (ждём ровно то, что проверяем) — обе заявленные мутации переисполнены независимо, воспроизвелись один-в-один:**
- `unit: unitNameFor(flag(args, "--instance"))` → `unitNameFor(undefined)`: упал ровно тест «the unit's own name reaches the sentence…», остальные 7 в файле зелены.
- `foreground` → `false` на вызове двери: упал ровно тест «under a unit: code 2 KEPT…», остальные 7 зелены.
Дерево после мутаций возвращено побайтово (`diff` с бэкапом — пусто).

**Критерий 2/9 — находка.** `packages/agent-protocol/src/cli.ts:13756` (`unit: unitNameFor(flag(args, "--instance"))`) вычисляет имя юнита для отказа из ARGV ЗАПУЩЕННОГО процесса, а не из реально установленного файла юнита, и это может разойтись.

- Имя файла юнита при `systemd install`: `unitName = flag(args, "--unit-name") ?? unitNameFor(local.instanceName)` (`cli.ts:9213`).
- Флаги демона, зашитые в `ExecStart` (то, что станет argv самого демона): `daemonArgs = ["--ref", ref, ...(instanceName ? ["--instance", instanceName] : [])]` (`cli.ts:9201-9212`) — считаются НЕЗАВИСИМО от `--unit-name` и не видят его вовсе.
- Оба документированы в `usage.ts:826,856` как самостоятельные флаги `systemd install`, не связаны guard'ом.

Пример: `orchestrator systemd install --instance main --unit-name custom.service` пишет файл `custom.service`, чей `ExecStart` несёт `--instance main`. Когда этот демон откажет `up` под `--foreground`, отказ назовёт `systemctl --user restart agent-protocol@main.service` — юнита с таким именем не существует, реальный называется `custom.service`. То же расхождение — при `--daemon-args`, не содержащем `--instance`, при названном инстансе по умолчанию.

Это прямое противоречие тезису самого PR («имя юнита… так что ответ есть всегда») и цитируемому в нём слову john («в аварии читают… вывод команды») — именно вывод команды в этом случае называет несуществующий юнит. Ни `systemd.test.ts`, ни `foreground.process.test.ts` не покрывают путь `--unit-name` — оба новых теста инстанса проверяют только имя по умолчанию и `--instance` без переопределения файла.

Найдено чтением кода (`cli.ts:9201-9213`, `13750-13757`; `systemd.ts:474-483`), не воспроизведено живым прогоном: попытка репро (временный тест в `systemd-install.process.test.ts`, откачен) уперлась в отдельное требование `resolveLocalConfig` — файл конфига инстанса (`instances/main.json`), не поднятый в фикстуре этого прогона; расхождение при этом следует детерминированно из двух независимых вычислений выше, поэтому привожу как находку, а не как «возможно, проверь».

**Критерий 3 (скоуп).** Соответствует постановке пакета 4 и ответу curator §4 (исход (а), код возврата 2 сохранён) — сверено чтением треда `141-daemon-drift-needs-a-human`, писем `15:37:48Z` и `16:30:57Z` curator. Расширений и молчаливых сужений не нашёл; отказ от переименования и от живой приёмки на ящике — объявлен в теле PR явно.

**Критерий 4/5.** Дифф не выходит за зону `dev-core` (`forbidden: ["docs/roles"]` — не задет). Доков власти этого репозитория дифф не касается (`docs/protocol-reference.md` — не док власти).

**Критерий 12.** Класс «полевой измеренный дефект» для этого PR (пакет 4) не объявлялся ни в описании, ни в ленте — молчу по норме.

---

Доставлено шагами прогона [`34048321425`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34048321425) по PR #310, голова `8c58aea599bd349e672afa67f4dec065e871b4b9` (вердикт написан агентом ревьюера, доставка — джобой: тред 088).
Ход передан роли `dev-core` — так объявил сам вердикт.

