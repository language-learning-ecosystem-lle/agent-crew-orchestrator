---
from: reviewer-pr
worker: gh-action
date: 2026-09-06T14:09:28Z
expects: answer
waiting-on: dev-core
parked-on: john
verdict: needs-fixes
pr: 301
---

verdict: needs-fixes
pr: 301
waiting-on: dev-core

Полный прогон тестов не повторял — `checks` на голове `8f9a9b846a1e39f15419c80506666964ad2974af` зелёный, прогон [`34037442759`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34037442759).

Живой исход `pnpm protocol merge-gate --ref origin/main --pr 301`:
```
merge-gate: documents of power judged by (8): agent-protocol.json, docs/roles/{curator,dev-core,pilot-codex,devops}.md, REVIEWER.md — derived; PROTOCOL.md, .github/workflows — declared by powerDocuments
PR #301 at 8f9a9b8
  STOP guard 1 · approve on the current head: no approve verdict on 8f9a9b8
  STOP guard 2 · green checks on the same head: not green: review=IN_PROGRESS
  you  guard 3 · ascent to a decision of john's: thread '141-daemon-drift-needs-a-human' — read the feed
  ok   guard 4 · no self-merge on the documents of power: 4 changed path(s), none of them a document of power
  you  guard 5 · a trace of the merge: name this merge in your next message in the thread
  ok   mergeability · mergeable=MERGEABLE (mergeStateStatus UNSTABLE)
REFUSED: a guard does not hold
```
Guards 1/2 отказывают из-за состояния этого же круга ревью — не находка. Guards 3/5 — ручные гарды curator, не суждение ревьюера (см. REVIEWER.md).

**Критерий 1 (числа тестов) — точечный прогон, не по логу.** Проверил сам, каждый файл/группу отдельно, на голове `8f9a9b84`:
- `pnpm exec vitest run src/orchestrator/self-restart.test.ts` → 77 passed (заявлено 77, было 68 — совпадает);
- `pnpm exec vitest run src/orchestrator/self-restart.process.test.ts src/orchestrator/code-age.test.ts src/notify/notify.test.ts` → 169 passed (заявлено «169 зелёных» — совпадает);
- `pnpm typecheck` — чист (оба пакета);
- `pnpm exec biome check` по 4 изменённым файлам — 2 info (`useLiteralKeys`, `cli.ts:7612`, `cli.ts:13930`), обе строки вне диффа этого PR (диф трогает `cli.ts` только в районе ~12238-12264) — не находка.

**Мутация переисполнена мной, не только принята на слово.** Убрал конкатенацию ремонта в `describeSelfRestartBlock` (вернул только `describeSelfRestartCause`) — упал ровно заявленный случай («gives every block both halves … in ONE string») ровно на заявленной паре строк (`expected 'no self-restart while a stop is alrea…' to contain 'WHEN YOU BRING IT BACK: lift the stop…'`); мутация снята, дерево чисто.

**Критерий 2 — найдена находка.** `packages/agent-protocol/src/cli.ts:12241-12256` — новая логика двери: `flagDown` вычисляется чтением диска (`existsSync(stopFlag)`/`existsSync(forceFlag)`) и его путь прокидывается в `selfRestartVerdict` как `stopFlag`, чтобы блок `stopping` называл файл по имени (именно это и было ценой часа john). Но `self-restart.process.test.ts` этим пакетом НЕ тронут (12 тестов, как и было), и ни один процессный случай не создаёт реальный флаг-файл на диске и не проверяет, что итоговая строка дайджеста/лога называет его путь. Юнит-тест в `self-restart.test.ts` («carries the flag the tick actually found into the block») бьёт только в чистую функцию `selfRestartVerdict` с уже сконструированным `stopFlag` — это не проверка самой двери (слоя `existsSync`/приоритета `stopFlag` над `forceFlag` в `cli.ts`). Ровно тот класс, о котором предупреждает критерий 2: «тест на CLI-дверь проверяет саму дверь… слой между ними иначе непокрыт» — а весь смысл этого PR в том, чтобы устранить именно такую тихую деградацию до неопределённости («the stop flag» без имени), которая уже стоила часа руки john. Просьба: добавить процессный случай (по образцу существующих в файле — реальный `home.repo/.orchestrator/stop` или `force-stop` на диске), который проверяет, что итоговая строка называет фактический путь флага.

**Критерий 3 (скоуп).** Дифф реализует §4.2 и §5 постановки треда `141-daemon-drift-needs-a-human` (указан в описании PR), явно продолжая пакет #297. §4.3 (`orchestrator up`) и §3 (письмо о событии самоперезапуска) явно названы НЕ сделанными («Чего в пакете НЕТ») с обоснованием (решение john, отдельный канал/память) — доложенное сужение, легитимно и совпадает с текущим состоянием ленты (оба вопроса ещё стоят на john по msg `13:47:29Z`/`13:54:52Z`).

**Критерии 4/5.** Изменённые пути: `packages/agent-protocol/README.md`, `packages/agent-protocol/src/cli.ts`, `packages/agent-protocol/src/orchestrator/self-restart.test.ts`, `packages/agent-protocol/src/orchestrator/self-restart.ts` — все в зоне `dev-core`, ни один не в `docs/roles`. Доки власти (`PROTOCOL.md`, `docs/roles/**`, `REVIEWER.md`, `agent-protocol.json`, `.github/workflows/**`) не тронуты — подтверждено guard 4 merge-gate («4 changed path(s), none of them a document of power»). Секретов, ослабления гардов, расширения прав в диффе нет.

**Критерий 6.** Новое опциональное поле `flag`/`stopFlag` живёт только внутри процесса (аргумент чистой функции `selfRestartVerdict` и вариант объединения `SelfRestartBlock`), не сериализуется в файл дрейфа/почту и не пересекает границу версий демона — версия протокола не требуется.

**Критерий 9 (текст против факта).** Заявленные в PR команды и тексты сверены с кодом: `orchestrator restart --pull` (`cli.ts:13768`, флаг `--pull` на 13862) и `orchestrator hold --mode release --role <role> --write` (`cli.ts:13432` и короткая форма `13990`) существуют дословно так, как процитированы в `SELF_RESTART_BY_HAND` и `describeSelfRestartRepair`. Расхождений между описанием PR и диффом не найдено, кроме находки по критерию 2 выше.

**Критерий 12.** Класс «полевой измеренный дефект, новой нормы не вводит» в этом PR/треде не объявлен — критерий не поднимается (в отличие от #297, где он обсуждался применительно к другому дифу).

---

Доставлено шагами прогона [`34037836287`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34037836287) по PR #301, голова `8f9a9b846a1e39f15419c80506666964ad2974af` (вердикт написан агентом ревьюера, доставка — джобой: тред 088).
Ход передан роли `dev-core` — так объявил сам вердикт.

