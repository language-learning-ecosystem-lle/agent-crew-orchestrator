---
from: curator
worker: claude-code
session: 5d7cb075-8f6a-45ad-afde-39d37155269c
raised: 2026-09-06T20:15:44Z
date: 2026-09-06T20:17:47Z
expects: none
waiting-on: curator
parked-on: run:318
---

**Метка `review` повешена на #318 в `20:16:27Z` — после зелёного `checks` на ТОЙ ЖЕ голове `9db58611…`, и круг ПОДНЯЛСЯ: прогон `34057474834`, `event: pull_request`, голова `9db58611…`, старт `20:16:28Z`. Ход паркуется на `run:318` — вердикт приезжает в эту ленту сам.**

## 1. Что я сверила ПЕРЕД меткой, а не после

Норма 03.08 требует метку строго после зелёных чеков на той же голове, а не «после зелёных чеков вообще». Оба факта сняты своей рукой одной парой команд:

| факт | значение |
| --- | --- |
| голова #318 непосредственно перед `gh pr edit` | `9db58611108586daeadf94b1b263266c19920ff1` |
| метки до | `[]` (пусто — снятую прошлым тактом никто не вернул) |
| `checks` на ЭТОЙ голове | прогон `34056977695`, `event: pull_request`, `headSha 9db58611…`, `status completed`, `conclusion success` |
| `mergeable` / `state` | `MERGEABLE` / `OPEN` |
| единственный круг ревью в списке прогонов ветки до метки | `34056403275` на `cb3995e7…` — на ТЕКУЩЕЙ голове круга не было вовсе |

Голову я не брала из прошлого письма: `gh pr view --json headRefOid` вызван ЗАНОВО прямо перед правкой меток, потому что штамп чужого письма головы не гарантирует.

## 2. Метка — и контрольное чтение после неё

`gh pr edit 318 --add-label review` в `20:16:27Z`. Ответ инструмента фактом не считается, поэтому сразу за ним — чтение:

- `headRefOid` = `9db58611108586daeadf94b1b263266c19920ff1` — **не сдвинулась от метки**, значит вердикт круга ляжет на то же дерево, что круг и судит;
- `labels` = `review` (id `LA_kwDOT7Dfxs8AAAACxF7CDA`), `mergeable MERGEABLE`, `state OPEN`.

## 3. Круг ПОДНЯЛСЯ — это отдельный факт, и он проверен прогонами

Метка вердикта не означает; круг поднимает СОБЫТИЕ навешивания. Через 12 с после правки `gh run list --branch docs/141-systemd-floor` печатает новый прогон:

`34057474834` · `Claude PR Review` · `event: pull_request` · `headSha 9db58611108586daeadf94b1b263266c19920ff1` · `status in_progress` · `createdAt 2026-09-06T20:16:28Z`.

Событие сработало на первой попытке — перевешивать нечего. Прошлый круг `34056403275` остался стоять на `cb3995e7…` и к кнопке больше не относится: круг судит новое дерево целиком, а не дельту.

## 4. Почему я НЕ стою у круга

Железное правило (решение john 2026-08-28, тред `037-no-foreground-waiting`): роль не ждёт прогона, который и так напишет в тред. Метка — конец хода, а не начало ожидания; полевой случай 2026-08-28 (метка на #83, висящая сессия, непоставленные метки на готовых #81 и #82) — ровно про этот соблазн. Вердикт и исход `checks` приезжают сюда шагами `claude-review.yml` сами.

Парк — `run:318`: он ждёт любого прогона в полёте по этому PR и снимается первым письмом, которое кого-то двигает, то есть вердиктом круга. Парк закрывает минуты круга от преждевременного подъёма роли.

## 5. Хвост открытых PR — прогнан, и он по-прежнему пуст

`gh pr list --state open` в `20:16:40Z` даёт **один** PR — #318, мой, только что помеченный. Домерживать нечего, чужих кандидатов нет.

## 6. Следующему такту curator — поимённо

1. **прочитать вердикт круга `34057474834`** в этой ленте. `needs-fixes` → чинить своей рукой (`docs/install-notes.md` — зона curator), проверив каждую находку СВОИМ замером, а не приняв на веру; после починки голова уедет → метку снять и повесить заново уже на новой голове после зелёных чеков;
2. `approve` → `pnpm protocol merge-gate --ref origin/main --pr 318 --review-workflow 'Claude PR Review'` → `gh pr merge 318 --squash --match-head-commit <sha из вывода двери>` → след гарда 5 сюда;
3. **гард 3 — слово john** «ОБЪЯВЛЯЕМ МИНИМАЛЬНУЮ ВЕРСИЮ» (чат 2026-09-06 ~`19:41Z`, доставка чата письмом curator `19:44Z` в эту ленту с пометкой источника); полевой класс не объявляется и не нужен. Машинная половина на месте: `thread: 141-daemon-drift-needs-a-human` и `role: curator` в теле PR;
4. **гард 4** — один путь, `docs/install-notes.md`, доком власти не является (дверь выводит список сама, этого пути в нём нет);
5. **`mergeable` перечитать своей рукой непосредственно перед кнопкой** — база сегодня уезжала семь раз; ноту двери о переезде базы гасить ЗАМЕРОМ (дерево мержа + сюита), а не ребейзом;
6. вердикт относится к ТЕКУЩЕЙ голове: перед кнопкой сверить, что `headRefOid` всё ещё `9db58611…`.

## 7. Чего я НЕ делала

- у круга не стою, второй метки не вешала, тела PR не правила, ребейза не делала, ничего не мёржила, кода не писала;
- вердикта не предвосхищала: круг ещё идёт, и «зелёный чек с именем `review`» вердиктом не является;
- `merge-gate` не звала — на голове `9db58611…` закрытого круга ещё нет по построению, ответ двери был бы предрешён (гарды 1 и 2 отказали бы), а звать её положено под кнопку.

## 8. Что по-прежнему НЕ поручено — не приписываю себе

**Живая приёмка пакета 4 — утром 07.09 рукой john** (сейчас невозможна: самопочинка демона заперта живыми сессиями). **`orchestrator stop` без мягкого режима** и **окно самопочинки** (§4.4 постановки) — john о них не говорил ни разу; не забыты и не поручены.

Незакоммиченного нет: правок в рабочем дереве роли я в этом такте не делала вовсе, `git status --porcelain` пуст, PR стои́т на запушенном `9db58611…`. Тело письма — в `mktemp -d` в `/tmp`, вне обоих чекаутов.

waiting-on → curator; парк на `run:318` — снимет его вердикт круга `34057474834` в этой ленте.

