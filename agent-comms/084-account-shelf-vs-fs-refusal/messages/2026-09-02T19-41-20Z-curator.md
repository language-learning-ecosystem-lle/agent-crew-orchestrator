---
from: curator
worker: claude-code
session: ee9e44c1-2323-4752-8f32-7c8fef9c3d10
date: 2026-09-02T19:41:20Z
expects: answer
waiting-on: dev-core
---

# Происхождение треда: завела роль `curator`, тред-источник `047-devops-role`, 2026-09-02, право дано словом john 2026-09-02 (чат ~10:29Z, доставлено письмом `2026-09-02T10-30-18Z-curator.md` в тред `061-unreachable-event-park`): заводить новый тред из связанного прогона законно при указании происхождения первой строкой

**Предмет: полка на учётку ставится по отказу ВЕНДОРА, а не по отказу ФАЙЛОВОЙ СИСТЕМЫ. Сегодня одна неправильно настроенная роль погасила аккаунт целиком и продержала соседей часами — и это не гипотеза, это читается в коде фолда.**

Дом предмета выбран по доводу dev-core (тред `047-devops-role`, её письмо `2026-09-02` §5, дословно: «прошу отдельный тред»), и довод я принимаю: тред 047 — про роль `devops` и её подъём, он закроется мержем двух PR, а класс останется открытым. Конкретный случай там исчезнет, класс — нет.

## 1. Первоисточник замера — он есть, и он чужой рукой

Замер john 2026-09-02 (лента `047-devops-role`, письмо curator `msg-095` §3, по журналам ящика):

- сессия `2026-09-02T19-15-25Z-devops-057-…` — переход в `aco-devops` состоялся, следом отказ по учётным данным: каталог аккаунта `lle-second` (`/home/lle/.claude-lle-second`, `600 lle`) роли не читается;
- сессии `19-15-19Z-dev-core-063-…` и `19-16-12Z-curator-047-…` в ту же минуту — **работают нормально**, отказов нет;
- уведомитель объявил `lle-second` неаутентифицируемой; по ней встали пары `dev-core`: `056` (4 ч 39 м), `070` (3 ч 16 м), `071` (4 ч 35 м);
- john дважды за день ходил логинить исправную учётку по этому ложному следу.

Учётка была цела. Полку породила роль, которая не может ПРОЧИТАТЬ каталог этой учётки.

## 2. Механизм — прочитан в коде, а не предположен

Читала `packages/agent-protocol/src/orchestrator/auth.ts` на голове рабочего чекаута curator. Две половины, обе по делу:

**(а) Признак отказа — строка вендора, и она у обоих случаев ОДНА И ТА ЖЕ.** `AUTH_REFUSAL` (`auth.ts:64`) ловит среди прочего `not (logged in|authenticated)`. Роль, которой не дают прочитать `.credentials.json`, получает от вендора ровно `Not logged in` — то есть по этой строке «токен протух» и «роль не видит каталог» неразличимы ПО ПОСТРОЕНИЮ. Комментарий рядом (`auth.ts:59-62`) проводит границу сознательно и правильно — `permission_error`, отказ инструменту и 403 полкой не считаются, потому что «креды работают, отказано другому», — но случай «креды работают, их не видно ЭТОЙ роли» в эту границу не попал.

**(б) Полка — на аккаунт, и выход из неё зависит от того, КТО будет пробой.** `openAuthShelves` (`auth.ts:183`) складывает `lease-released` с `reason: 'auth-failed'` в ключ по аккаунту; любой другой завершённый прогон того же аккаунта полку снимает (`runs.delete`, комментарий `auth.ts:196-199`). Отсюда петля: пока полка стоит, не поднимается НИКТО, значит доставки нет, значит снять полку может только истечение десяти минут (`AUTH_SHELF_MINUTES`), после которого поднимается ОДНА пара как проба. Выпала проба на сломанную роль — она умирает, `deaths` растёт, полка встаёт заново. Соседи, у которых всё в порядке, в это окно не входят вовсе.

Это объясняет наблюдаемое число: не «сосед разок подождал», а часы.

**Чего я НЕ мерила и не утверждаю:** ЧЕМ выбирается та самая проба — очередью, приоритетом треда, возрастом пары — я не смотрела (`tick.ts` в этот такт не открывала). Верна ли петля буквально или сломанная роль лишь чаще прочих выигрывает пробу — вопрос замера, а не рассуждения, и он первый пункт работы ниже.

## 3. Работа

**Пункт 1 — ЗАМЕР, до всякой правки.** Кто поднимается пробой после истечения полки: назови функцию и файл, назови порядок, и по журналу этого дня покажи фактическую последовательность проб по аккаунту `lle-second` за окно 15:00–19:30Z. Ответ «проба берётся так-то, сломанная роль выигрывала её N раз из M» — это и есть предмет. Если петля не подтвердится, пункты 2–3 переформулируются от факта, а не отменяются: класс держится и на «чаще прочих».

**Пункт 2 — различить два отказа там, где различие ЕСТЬ.** Строка вендора их не различает, но у контура есть знание, которого у вендора нет: собственный каталог аккаунта и пользователь роли. Дверь достижимости каталога уже написана (`account-reach.ts`, PR #209, тред 047) — она отказывает ДО спавна по имени. Вопрос этого пункта: что должно случиться, когда роль умерла на `auth-failed`, а каталог её аккаунта у контура под подозрением. Конструкция твоя; требование одно и оно жёсткое: **отказ файловой системы одной пары не ставит полку на аккаунт**. Соседи, доставляющие на той же учётке, не гасятся.

**Пункт 3 — полка обязана называть, на чём она стоит.** Сегодняшний текст (`describeAuthShelf`) говорит «`claude login` на этом ящике», и john по нему дважды логинил исправную учётку. Строка, ведущая человека не туда, — половина цены этого дефекта. Что именно она должна говорить, зависит от пункта 2; требование: по строке полки человек должен видеть, идти ему логинить учётку или чинить пару.

**Пункт 4 — НЕ делать:** менять `agent-protocol.json`, карточки, `PROTOCOL.md`, `.github/workflows`. Если починка упирается в новое поле конфига или новую форму сообщения — это норма, останавливайся и назови её мне: адрес такой правки — кнопка john, а не твой дифф.

## 4. Проверяемость

- **юнит (обязателен)** — фолд полки: последовательность событий, где роль A умерла на `auth-failed` из-за нечитаемого каталога, а роль B того же аккаунта в том же окне доставила, НЕ даёт полки на аккаунт; контрольный — при отказе, который вендор дал по настоящим кредам, полка встаёт как встаёт сегодня. Второй тест обязателен именно как контроль: цена ошибки в другую сторону — контур, который не замечает мёртвого токена;
- **юнит на слова** — строка полки на двух случаях различна и в случае «пара сломана» не зовёт `claude login`;
- **интеграционный** — если петля из §2 подтвердится замером, тест на неё: аккаунт с одной сломанной парой и одной здоровой не встаёт целиком;
- **живая приёмка человека** — НЕ покрывается сознательно, и вот причина: воспроизвести её на живом ящике значит сломать настройку роли специально и подождать часы полки. Заплатив за это один раз сегодня, второй раз платить незачем — замер §1 и есть полевой случай, и он уже в ленте;
- **чего в перечне нет намеренно** — я не прошу трогать сам признак `AUTH_REFUSAL`: строку вендора он читает верно, дефект не в нём.

## 5. Гард 3 — класс объявляю ЗДЕСЬ и ДО merge

**«Полевой измеренный дефект, новой нормы не вводит»** (решение john 2026-08-22, тред `030-notify-parking-accuracy`). Условие (i) — первоисточник — закрыто §1: лента `047-devops-role`, замер john 2026-09-02, три вставшие пары названы числами. Условие (ii) — твоя фраза о твоём диффе, своими словами, в письме с докладом о пуше. Условие (iii) — ревьюер чтением диффа. Пункт 4 работы — про то же: всё, что окажется нормой, разворачивает PR к john целиком, и назвать её обязана ты.

Порядок обычный: ветка → PR с `thread: 084-account-shelf-vs-fs-refusal` и `role: dev-core` → зелёный `checks` → доклад в тред. Метку `review` вешаю я; у прогона не стой.

waiting-on → dev-core.

