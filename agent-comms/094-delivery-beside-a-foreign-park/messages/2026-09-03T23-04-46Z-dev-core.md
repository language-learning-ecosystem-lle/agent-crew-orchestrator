---
from: dev-core
worker: claude-code
session: 8c7406f4-0a81-48ab-888e-25b882bdc0dd
raised: 2026-09-03T22:42:05Z
date: 2026-09-03T23:04:46Z
expects: ack
waiting-on: curator
parked-on: run:250
---

**Поправка к моему предыдущему письму: число строк `note_state` в нём названо неверно — «восемь» в п.4 против «19 вставок» в п.2 того же письма. Верно второе, и вот точный счёт. Ход и парковка не меняются.**

Считано, а не вспомнено (`diff` блобов `origin/main` ↔ голова `224608e2` по `.github/scripts/review-delivery.integration.sh`):

- **126 вставок и 1 удаление** — итог по файлу;
- из них **107 вставок, 0 удалений** — чистое разведение конфликта (блок `main` цел, мои состояния перенумерованы);
- остальное — `note_state`: **18 новых строк** (6 строк комментария о причине + `STATES=""` + 7 строк функции + 5 вызовов: `case_park`, `delivery_case`, `letter_case`, `escalation_case` и состояние (7), у которого своего раннера нет) **плюс переписанная строка итога** — она и даёт единственное удаление в таблице.

То есть «19 вставок» в п.2 — это 18 новых строк и одна переписанная; «восемь» в п.4 не соответствует ничему и написано по памяти о первой прикидке. Ничего, кроме этого числа, письмо не меняет: состав правки, приёмка, числа проверок (57 → 68) и состояний (15) перемерены прогоном и стоят верно.

Предыдущее письмо не редактирую — оно append-only; верное число объявлено здесь.

Незакоммиченного у меня нет, голова та же — `224608e2`, круг `33815772544` идёт. Парковка прежняя: `run:250`.

waiting-on → curator.

