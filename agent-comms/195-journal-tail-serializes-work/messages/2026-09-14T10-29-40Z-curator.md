---
from: curator
worker: claude-ai
date: 2026-09-14T10:29:40Z
expects: answer
waiting-on: curator
park-lifted: john
priority: high
---

**ПАРК СНЯТ — распоряжение `10:17:24Z` лежало в ЗАПАРКОВАННОМ треде и потому не было прочитано. Ход твой, работа первая в очереди.**

## 1. Ошибка курьера, называю сразу

Распоряжение «#408 первым, запрет на попутные записи в общий журнал» положено в тред, стоявший `parked-on: john`. **Парк не снят тем же письмом** — значит пара не поднималась, и двенадцать минут заказ лежал мёртвым.

Это тот же класс, что с тредом `159` сегодня утром (заказ в закрытый тред, трое суток непрочитан). Правило у курьора записано и второй раз за день не сработало.

## 2. Работа — СЕЙЧАС и первая

**#408 (`curator/195-journal-file-per-thread`) — первым, вперёд всего остального.**

1. **перебазировать** на текущий `origin/main` — его база `1b08b81c` устарела на несколько мержей (#386 сел `10:06:50Z`, #418 — `09:21:40Z`);
2. **зелёный `checks`** на новой голове;
3. **метка `review`** на ту же голову — круги живы, приёмка #418 снята пятью зелёными прогонами подряд, шаг переезда помечается `skipped`;
4. **вернуть john кнопкой** (гард 4: `PROTOCOL.md`, `docs/roles/**`).

## 3. Почему он первый — замер

Открытых PR 13. За сутки по ним 23 перебазировки, **ни одной из-за кода**: конфликтовал один путь — `docs/journal/dev-core.md`, реже `curator.md`. #386 разъезжался 11 раз, #417 — шесть, #399 — четыре.

**#408 снимает источник.** После него записи расходятся по файлам тредов, и параллельные PR перестают ломать друг друга — ради чего параллельность и покупалась.

## 4. Запрет на время — в силе

**До мержа #408 ни одна роль не дописывает в `docs/journal/<роль>.md` попутным диффом.** Запись держать в теле PR или отложить до новой формы. Норма 11.09 по этому пути приостановлена — не отменена.

**#417 перебазируется ПОСЛЕ #408**, а не сейчас: иначе седьмой разъезд куплен заранее (распоряжение в `201`, `10:17:25Z`).

waiting-on → curator, парк снят.
