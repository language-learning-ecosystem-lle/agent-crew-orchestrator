---
from: curator
worker: claude-code
session: 3f6d1316-bd31-4a1e-98f8-9479c60da298
raised: 2026-09-13T17:58:25Z
date: 2026-09-13T18:08:06Z
expects: ack
waiting-on: curator
parked-on: john
---

**Требуется решение john: ЗАПАСНАЯ учётка ревьюера отвечает `401 Invalid bearer token` — переезд по лимиту, смёрженный вчера (#399, тред 193), сработал верно и упёрся в НЕГОДНУЮ креду. Круг ревью держится на одной учётке, и сегодня она ответила `429`. Токен — твоя рука: секрет репозитория и `.github/workflows/**`.**

## 1. Что я сделала и что из этого вышло

- `17:59:00Z` — метка `review` на голову `15ceb8d7` (после зелёного `checks` `34772018989`, `success`
  на той же голове — условие выполнено, а не предположено);
- `17:59:02Z` — круг родился: прогон **`34773208904`**, `Claude PR Review`, `event=pull_request`;
- `18:00:07Z` — круг **`failure`, 65 секунд от старта**. `verdict.md` не создан, формальный
  review-статус не выставлен; в PR лёг коммент «Ревью не состоялось» (`17:59:58Z`).

**Это письмо — ответ на `msg-002` от `reviewer-pr` (`18:00:01Z`, `expects: answer`).** Он назвал
класс верно и сам: вердикта нет, это не суждение о PR, причина — `api_error:401`, круг ехал на
ЗАПАСНОЙ учётке после переезда по лимиту основной, автоповтора нет намеренно. Я добавляю к его
докладу то, чего в нём нет и чего из него не вывести: **какая учётка какой ошибкой ответила** — и
именно это переводит случай из «круг не повезло» в измеренный дефект креды.

## 2. Почему он умер — замер транскриптом, не догадка

Шаг `Транскрипт ревьюера — артефактом` кладёт артефакт `reviewer-execution-408-34773208904`, и в нём
**два файла — по одному на учётку**. Запись `type=result` каждого называет причину дословно:

| учётка | файл | `api_error_status` | `result` | `duration_ms` |
| --- | --- | --- | --- | --- |
| основная | `reviewer-execution-primary.json` | **429** | `You've hit your session limit · resets 6:40pm (UTC)` | 424 |
| **запасная** | `claude-execution-output.json` | **401** | `Failed to authenticate. API Error: 401 Invalid bearer token` | 1963 |

У обеих `is_error: true`, `terminal_reason: api_error`, `num_turns: 1`, `total_cost_usd: 0`,
`modelUsage: {}` — ни одна не дошла до первого суждения. Денег круг не сжёг вовсе.

**Это ДВЕ разные вещи, и слепить их в одну — потерять обе:**

1. **Механизм #399 сработал ровно как задуман, и это его первое ЖИВОЕ срабатывание.** Шаг `Лимит
   основной учётки — установить факт` распознал запись `type=rate_limit_event`, напечатал
   `##[notice]` о переезде и отдал ход запасному шагу. Претензий к нему нет — он сделал то, для чего
   мёржился;
2. **упёрся он не в лимит, а в неработающую креду.** `401` — это НЕ «вторая учётка тоже исчерпана»:
   токен непуст (переезд состоялся, значение замаскировано `***`), но аутентификацию не проходит.
   Наличие секрета этим токеном не мерится (`gh secret list` → `403`), а **годность его до сегодня не
   мерил никто**: механизм принимался на фикстурах `usage.json`, живого ответа запасной учётки в
   приёмке не было.

**Диагноз причины `401` я не ставлю и подавать его как установленный не буду** — протух ли токен, от
той ли он учётки, той ли он формы: это гипотезы, а замер говорит ровно одно — `401` на живом круге.
Развилка твоя целиком: починка лежит в секрете репозитория и в `.github/workflows/**` — не моя рука
ни по зонам, ни по гарду 4, ни по границе «к john» (токены и деньги).

**Практическое следствие, которое стоит назвать прямо:** пока запасная учётка отвечает `401`,
переезда по лимиту фактически НЕТ, и круг ревью всего дома держится на одной учётке с пятичасовым
окном. Минута ресета названа самим ответом — `6:40pm (UTC)`, то есть **`18:40:00Z`**, и она совпадает
с `resetsAt: 1789324800` в записях `rate_limit_event` журнала ящика.

## 3. Что с PR #408 прямо сейчас

**Голова уехала — намеренно, и терять было нечего:** вердикта не существовало, а новый круг нужен в
любом случае. Запись об этом замере легла попутным диффом в файл журнала этого треда — **первое живое
применение нормы, которую PR и вводит**: запись про чужой предмет (лента 193) лежит в файле СВОЕГО
хода, происхождение названо прозой в теле.

- новая голова — **`d78263fead36a99492ecf2ed4986ac16ea459049`**, коммит
  `docs(journal): первая живая метка сожгла круг на обеих учётках…`, `47 0` — ноль удалений, один
  путь `docs/journal/curator/195-journal-tail-serializes-work.md`;
- `checks` на новой голове — прогон **`34773599910`**, `in_progress` с `18:06Z`. Его исход приедет
  письмом сам; я его не жду;
- **метку `review` я СНЯЛА** (`18:06:43Z`). Она висела со старой головы и означала бы вердикт о
  дереве, которого больше нет; снятие круга не поднимает (триггер — `labeled`), зато следующей руке
  остаётся одно действие вместо двух;
- нота двери о переезде базы, поднятая на прошлой голове, закрыта замером и на этой не изменилась:
  #407 тронул **один** путь — `docs/journal/dev-core.md`, а содержимое `docs/journal/**` не читает ни
  один исполняемый путь пакета (три попадания — комментарии в `merge/gate.ts`), значит переезд базы
  сюиту покраснить не может.

**Гарды на `d78263fe`:** гард 1 — STOP, вердикта нет и взять его негде до ресета; гард 2 — `checks`
идут, а чек `review` на прошлой голове остался `FAILURE` и лечится ОДНИМ успешным кругом; гард 3 —
закрыт, решение john в `msg-002` этой ленты; **гард 4 — STOP, кнопка твоя** (`PROTOCOL.md`,
`docs/roles/curator.md`, `docs/roles/dev-core.md`).

## 4. Развилка — факты, из которых ты выбираешь

1. **`401` запасной учётки** — чинить или нет, и чем. Механизм переезда без годного токена стоит
   вхолостую; замер годности до сегодня не делался ни разу. Предмет принадлежит ленте `193`, и
   исполнителя туда ставлю я, как только скажешь, что чинить (сам секрет — только твоя рука);
2. **PR #408** — после `18:40Z` одна метка даёт настоящий круг на основной учётке. Мою руку это
   требует ровно на одно действие, но моё окно кончается `18:38Z`, то есть на две минуты РАНЬШЕ
   ресета: повесить метку в этом ходе нельзя физически, и следующий подъём — первое, что может.

## 5. Чей ход

Парк на тебе, `expects ack`: ход остаётся у меня, а вопрос — первой строкой. Тред `180-selfheal`
этим не тронут; `checks` на новой голове идут сами.

Незакоммиченного за мной нет: всё в `d78263fe`, ветка запушена, рабочее дерево чистое, тело письма —
в каталоге вне обоих чекаутов.

waiting-on → curator, parked on john, expects ack.

