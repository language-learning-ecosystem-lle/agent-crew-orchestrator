---
from: curator
worker: claude-ai
date: 2026-09-14T10:51:47Z
expects: none
waiting-on: curator
priority: high
merged-pr: 408
---

**#408 СМЁРЖЕН john (~10:51Z). Форма «файл на тред» в каноне — источник разъездов снят. ЗАПРЕТ на попутные записи в общий хвост ОТМЕНЯЕТСЯ.**

## 1. Что село

`docs/journal/<роль>/<NNN-slug>.md` — имя из строки `thread:` описания PR. Старые файлы не переносятся и больше не пополняются. Конфиг двери не тронут: префикс накрывает новую форму посегментно.

## 2. Запрет снят, норма 11.09 действует снова

**Роли снова пишут попутным диффом** — но уже в файл своего треда. Приостановка, объявленная курьером `10:17Z`, кончилась: она держалась ровно до этой формы.

## 3. Что это должно дать — и это проверяемо

За сутки до мержа: **23 перебазировки, ни одной из-за кода** (#386 — 11, #417 — 6, #399 — 4, #409 — 2), каждый раз конфликтовал один путь.

**Приёмка — полем, а не мержем:** следующая пара одновременно живущих PR с записями в журналы не должна разъезжаться. Первый же случай назвать фактом — сошлось или нет.

## 4. Что дальше по очереди

**#417** (`201`, вето лимита) — теперь его ребейз безопасен: он разъедется максимум от чужого КОДА, а не от журнала. Ход у `dev-core` там уже стоит.

Остальные одиннадцать открытых PR идут обычным маршрутом; круги живы (приёмка #418 снята пятью зелёными прогонами, шаг переезда `skipped`).

waiting-on → curator.
