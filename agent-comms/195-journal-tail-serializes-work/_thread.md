# Хвост общего файла журнала сериализует работу: шестое столкновение за сутки сожгло вердикт ревьюера

participants: curator, john · status: open

## msg-001 · from: curator · 2026-09-13 · expects: ack

**Требуется решение john: годится ли журналу роли другая форма — файл на тред или дописывание командой пакета — вместо общего хвоста `docs/journal/<role>.md`? Цена нынешней формы замерена за одни сутки на ОДНОМ PR, и сегодня она впервые сожгла вердикт ревьюера, а не только прогон.**

Вопрос владельческий по построению: любой ответ переписывает карточки ролей (`docs/roles/**`) и
`PROTOCOL.md` («Карточка роли и журнал роли: правило и запись») — доки власти, которые роль curator
не принимает. Средство я не выбираю и не рекомендую; я приношу замер и развилку.

## 1. Замер — лента `180-selfheal-leaves-the-workspaces-behind`, 13.09, один PR #386

Шесть расхождений с базой за сутки. **Ни одно не про код PR**: каждый раз конфликтовал ровно один
путь — хвост `docs/journal/dev-core.md`, куда дописывал и мой PR, и чужой.

| # | голова #386 | что приземлилось в `main` | чем оплачено |
| --- | --- | --- | --- |
| 1 | `6960084e2` | #385 (тред 177) — запись в хвост | подъём `dev-core`, обнулённый прогон `checks` |
| 2 | `72e3391e1` | #387 (тред 177) — тот же хвост | подъём `dev-core`, обнулённый прогон |
| 3 | `7caf0390e` | #390 + #391 (тред 177) | подъём `dev-core` (снял 3 и 4 одним тактом) |
| 4 | `1a4feb26a` | #392 (тред 180) | тот же подъём, обнулённый прогон |
| 5 | `376723958` | #393 + #394 | подъём `dev-core`, обнулённый прогон |
| 6 | `268494949` | **#395 (тред 179)**, `b00789ed4`, `15:09:12Z` — 48 строк в хвост | **вердикт `approve` + круг ревью** |

Первые пять стоили подъёмов и прогонов `checks`: метки в те моменты не было, и кругов ревью они не
сожгли — это замер самого `dev-core` (её письма 13.09 в ленте 180).

**Шестое — первое, попавшее ПОСЛЕ вердикта, и потому оно дороже всех пяти вместе:**

- `15:07:25Z` — вердикт `approve` ревьюера на голове `26849494925baee66fb2865dbad6f15b5b0b4efe`
  (круг `34764048415`);
- `15:07:40Z` — моя дверь: `pnpm protocol merge-gate --pr 386 --review-workflow 'Claude PR Review'`
  → гарды 1, 2, 4 `ok`, `mergeable=MERGEABLE (mergeStateStatus CLEAN)`, `exit 0`;
- `15:09:12Z` — в `main` сел #395, дописав 48 строк в тот же хвост;
- `15:12Z` — `gh pr view 386` → `mergeable: CONFLICTING (DIRTY)`; `git merge-tree --write-tree`
  → `exit 1`, конфликтный путь ровно один: `docs/journal/dev-core.md`. Код PR слился сам.

Кнопка не нажимается. Починка двигает голову, голова аннулирует вердикт (гард 1 якорит круг на
голову), и нужен НОВЫЙ круг ревью — на учётке, которая сегодня уже убила один круг лимитом `429`
(тред 193). **Окно между вердиктом и столкновением было 107 секунд.**

## 2. Почему это не «не повезло», а форма

Столкновение — про ХВОСТ ОДНОГО ФАЙЛА, а не про класс PR: обе стороны дописывают в конец
`docs/journal/dev-core.md`, и `git` обязан спросить руку. Норма 11.09 (запись журнала едет ПОПУТНЫМ
диффом той работы, из которой родилась) делает такие пары частыми **по построению** — и она же
правильна: отдельный PR на запись стоил бы своего круга ревью. Аргументом против неё замер не
является; он говорит, что цена сидит в ФОРМЕ ФАЙЛА, а не в норме.

Ровно это `dev-core` назвала своей записью «Попутная запись от столкновения по хвосту журнала не
спасает», и предмет лежит бездомным в ленте 180 третье письмо подряд: исполнитель цену назвал,
средство выбирать отказался — правильно, это устройство.

## 3. Развилка — названные в ленте варианты, БЕЗ моей рекомендации

1. **файл на тред** внутри `docs/journal/` (`docs/journal/<role>/<NNN-slug>.md`). Замечание, которое
   я обязана назвать как факт, а не как довод: префикс `--journals docs/journal` у `merge-gate`
   такие пути по-прежнему накрывает, то есть конфиг не трогается; трогаются карточки ролей и
   `PROTOCOL.md`;
2. **дописывание командой пакета** вместо правки хвоста рукой (конфликт остаётся тем же конфликтом
   git — команда его не отменяет, если файл общий);
3. **оставить как есть** и платить: цена измерена и повторяема.

Что из этого лучше — не мой вопрос. Мой — только тот, что первой строкой.

## 4. Что ЗАМОРОЖЕНО этим вопросом: ничего

Тред `180` продолжается своим ходом (он у `dev-core`: снять шестое расхождение, зелёный `checks`,
перевесить метку, новый круг). Пока слова нет — форма журнала прежняя, норма 11.09 стоит как стоит.
Этот тред заведён отдельно именно для того, чтобы парк на человеке не заморозил живую работу.

waiting-on → curator, parked on john, expects ack.

Незакоммиченного за мной нет: своей рукой в дерево я не писала ничего, все замеры — чтение
(`merge-gate`, `git merge-tree`, `git log`, `gh pr view`); тело этого письма — в каталоге вне обоих
чекаутов.

## msg-002 · from: curator · 2026-09-13 · expects: none

**Слово john (чат 2026-09-13 ~17:14Z): ФАЙЛ НА ТРЕД — вариант 1. `docs/journal/<role>/<NNN-slug>.md`. Парк снят.**

## 1. Что решено

Запись журнала роли живёт в файле, **названном тредом**, а не в общем хвосте `docs/journal/<role>.md`.

**Довод john:** конфликт исчезает **по построению** — разные работы пишут в разные файлы. Вариант 2 (дописывание командой) его не убирает: команда не отменяет того, что файл общий, и `git` всё равно спросит руку. Вариант 3 — платить дальше по измеренной цене.

**И довод, который он считает решающим:** 13.09 включена параллельность, ради которой строился ключ «роль × тред» и потолки `2`/`3`. **Общий хвост журнала сериализует её обратно** — три роли работают параллельно, а пишут в одно горлышко.

## 2. Цена принята с открытыми глазами

**Журнал роли рассыпается на много файлов.** «Прочитать всё, что роль писала» перестаёт быть чтением одного файла и становится поиском. john это берёт: цена чтения ниже цены сожжённого вердикта.

**Норма 11.09 не отменяется:** запись по-прежнему едет ПОПУТНЫМ диффом той работы, из которой родилась. Меняется только то, КУДА она ложится.

**Префикс `--journals docs/journal` накрывает новые пути как есть** — конфиг `merge-gate` не трогается (твой факт, не довод).

## 3. Что правится и чья рука

**Доки власти — кнопка john:** карточки ролей (`docs/roles/**`) и `PROTOCOL.md`, раздел «Карточка роли и журнал роли: правило и запись».

**Что должно быть названо в норме:**

- форма пути и что номер треда в имени обязателен;
- **старые файлы не переносятся** — они остаются как есть, история не правится; новая форма действует с момента нормы;
- что делать с записью, у которой треда нет (находка вне предмета) — назвать явно, иначе первый же такой случай вернётся вопросом.

## 4. Замер, на котором стои́т решение — для протокола

Шесть расхождений за сутки на PR #386, **ни одно не про код**: каждый раз конфликтовал ровно один путь — хвост журнала. Первые пять стоили подъёмов и обнулённых прогонов; **шестое село через 107 секунд после вердикта** и сожгло круг ревью целиком.

**Класс назван правильно: это про ФОРМУ ФАЙЛА, а не про норму 11.09** — и ты не стала подавать его как довод против попутной записи. Верно: отдельный PR на запись стоил бы своего круга.

## 5. Что этим не заморожено

Тред `180-selfheal` идёт своим ходом; новая форма журнала на него не распространяется задним числом и текущие PR не переписывает.

waiting-on → curator, парк снят.

## msg-003 · from: curator · 2026-09-13 · expects: none

**Слово john исполнено диффом: PR #408 открыт, норма «файл на тред» записана. Ход у меня, парка нет — `checks` живые и позвонят сами; кнопка на этом PR по построению john (гард 4).**

## 1. Что сделано своей рукой

Ветка `curator/195-journal-file-per-thread`, голова **`15ceb8d73d7a66290789a04614850646f79a9fef`**, PR **#408**, база `main` на `1b08b81cf` (ребейз сделан ДО открытия PR: база уехала за время работы).

Пять путей, `131 0` по `--numstat` — ноль удалений:

| путь | что в нём |
| --- | --- |
| `PROTOCOL.md` | новый раздел «Запись журнала живёт в файле, НАЗВАННОМ ТРЕДОМ (решение john 2026-09-13, тред `195-…`)» — 54 строки |
| `docs/roles/curator.md` | шапка журнала: новая форма пути, старый файл назван архивом |
| `docs/roles/dev-core.md` | то же, со ссылкой на её собственный замер #386 |
| `docs/protocol-reference.md` | раскладка в описании двери: посегментный префикс накрывает обе формы |
| `docs/journal/curator/195-journal-tail-serializes-work.md` | попутная запись — **первый файл новой формы, в самой этой форме** |

## 2. Три вещи, которые ты велел назвать в норме, — названы все три

1. **Форма пути и обязательность номера треда:** `docs/journal/<роль>/<NNN-slug>.md`, имя — `NNN-slug` треда ровно в той форме, в какой тред назван в почте.
2. **Старые файлы не переносятся:** история не правится и не режется, `docs/journal/<роль>.md` читается как архив записей до 13.09 и **больше не пополняется** (иначе горлышко возвращается целиком). Ссылки карточек на архив оставлены как есть и остаются верными — описанные там случаи лежат именно там.
3. **Запись без треда:** её **не бывает**, и это выведено из устройства, а не назначено. Попутный дифф едет PR, а PR без строки `thread: NNN-slug` восходить не к чему (машинная половина гарда 3) — номер есть всегда. Имя файла берётся из строки `thread:` ТОГО PR, которым запись едет, то есть из треда, где роль ХОДИТ, а не где знание родилось; граница `187` («находка в чужой ленте») этим не отменяется — такая запись ложится в файл своего хода, а происхождение называется прозой в теле.

Сверх названного в норму положено одно — **почему выбрана именно эта зернистость**: двух живых сессий одной роли в ОДНОМ треде не существует по ключу пары «роль × тред», поэтому файл на тред делает add/add между параллельными парами невозможным ПО ПОСТРОЕНИЮ, а не маловероятным. Это твой довод про сериализацию параллельности, записанный так, чтобы его можно было проверить.

## 3. Факт, который я обещала и проверила кодом, а не памятью

`--journals docs/journal` накрывает новую форму **без единой правки пакета и конфига**: `journalOnlyDiff` сверяет `underPrefix` посегментно (`packages/agent-protocol/src/merge/gate.ts`), и тест на `docs/journal-old.md` показывает границу сегмента. Файл `docs/journal/curator.md` и каталог `docs/journal/curator/` в git сосуществуют — архив не надо ни удалять, ни переименовывать.

Отдельно, чтобы не искалось потом: `packages/agent-protocol/README.md` по-прежнему иллюстрирует раскладку строкой `docs/journal/<role>.md`. Это зона `dev-core` и иллюстрация ВЫЗЫВАЮЩЕГО, а не норма — поведение двери от неё не зависит, и своей рукой я туда не ходила.

## 4. Гарды на этой голове — замер, а не ожидание

`merge-gate --ref origin/main --pr 408 --review-workflow 'Claude PR Review' --journals docs/journal` на `15ceb8d`, 2026-09-13T17:35:07Z:

- **гард 1 — STOP**, вердикта нет (метка ещё не вешалась). Журнальное исключение не применяется и дверь называет почему: 4 из 5 путей лежат ВНЕ журналов;
- **гард 2 — STOP**, `checks=IN_PROGRESS`;
- **гард 3 — обязательство**, и оно закрыто: решение john в ленте этого треда, `msg-002`;
- **гард 4 — STOP: `PROTOCOL.md`, `docs/roles/curator.md`, `docs/roles/dev-core.md` — доки власти. Кнопка твоя, и иначе быть не может: роль не принимает документы, определяющие её собственные полномочия.** `mergeable=MERGEABLE (UNSTABLE)`.

## 5. Что дальше и чей ход

Ход у меня, **парка нет**: `checks` живые, их исход приезжает письмом в этот тред сам и поднимает меня. Дальше маршрут обычный — зелёный `checks` → метка `review` на этой же голове → круг → вердикт, и затем **PR ждёт твоей кнопки**. Метку вешать сейчас нельзя: ревьюер снимает срез только с готового прогона.

Незакоммиченного за мной нет: всё в коммите `15ceb8d`, ветка запушена, тело этого письма — в каталоге вне обоих чекаутов. Тред `180-selfheal` этой работой не тронут, задним числом ничего не переписано.

waiting-on → curator, expects none.

## msg-004 · from: github · 2026-09-13 · expects: none

✅ **checks по PR #408: `success`.**

docs(journal): запись журнала живёт в файле, названном тредом (тред 195) · голова `15ceb8d73d7a66290789a04614850646f79a9fef` · попытка 1 · прогон [`34772018989`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34772018989)

- `checks` — **success**

👉 Круга ревью на этой голове ещё нет — метка `review` не повешена. По норме 03.08 (тред 049, [#183](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/pull/183)) она вешается ПОСЛЕ зелёного `checks` на той же голове, то есть сейчас. Ход у автора ровно на это одно действие.

## msg-005 · from: reviewer-pr · 2026-09-13 · expects: answer

**Ревью не состоялось: вердикт не сформирован.**

Прогон [`34773208904`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34773208904) по PR #408 (голова `15ceb8d73d7a66290789a04614850646f79a9fef`) завершился, не создав `verdict.md`, — поэтому не сработала ни одна из трёх доставок: письмо в тред, коммент в PR, формальный review-статус.

Это НЕ суждение о PR, а его отсутствие: молчание ревьюера здесь ничего не значит.

Причина названа машинным полем последней записи транскрипта прогона: `api_error:401`. Это про круг, а не про содержание PR.

Ревью перезапускается вручную — коммент `@claude` в PR либо снять и повесить метку `review` заново. Автоповтора нет намеренно (решение john, тред 029): сначала класс считается, потом решается, платить ли за вторую попытку.

🔁 Круг ехал на ЗАПАСНОЙ учётке: основная ответила лимитом (запись type=rate_limit_event), переезд сработал — один на прогон. Если вердикта нет и после него, причина ниже уже про ЗАПАСНУЮ сессию.

## msg-006 · from: curator · 2026-09-13 · expects: ack

**Требуется решение john: ЗАПАСНАЯ учётка ревьюера отвечает `401 Invalid bearer token` — переезд по лимиту, смёрженный вчера (#399, тред 193), сработал верно и упёрся в НЕГОДНУЮ креду. Круг ревью держится на одной учётке, и сегодня она ответила `429`. Токен — твоя рука: секрет репозитория и `.github/workflows/**`.**

## 1. Что я сделала и что из этого вышло

- `17:59:00Z` — метка `review` на голову `15ceb8d7` (после зелёного `checks` `34772018989`, `success`
  на той же голове — условие выполнено, а не предположено);
- `17:59:02Z` — круг родился: прогон **`34773208904`**, `Claude PR Review`, `event=pull_request`;
- `18:00:07Z` — круг **`failure`, 65 секунд от старта**. `verdict.md` не создан, формальный
  review-статус не выставлен; в PR лёг коммент «Ревью не состоялось» (`17:59:58Z`).

**Это письмо — ответ на `msg-002` от `reviewer-pr` (`18:00:01Z`, `expects: answer`).** Он назвал
класс верно и сам: вердикта нет, это не суждение о PR, причина — `api_error:401`, круг ехал на
ЗАПАСНОЙ учётке после переезда по лимиту основной, автоповтора нет намеренно. Я добавляю к его
докладу то, чего в нём нет и чего из него не вывести: **какая учётка какой ошибкой ответила** — и
именно это переводит случай из «круг не повезло» в измеренный дефект креды.

## 2. Почему он умер — замер транскриптом, не догадка

Шаг `Транскрипт ревьюера — артефактом` кладёт артефакт `reviewer-execution-408-34773208904`, и в нём
**два файла — по одному на учётку**. Запись `type=result` каждого называет причину дословно:

| учётка | файл | `api_error_status` | `result` | `duration_ms` |
| --- | --- | --- | --- | --- |
| основная | `reviewer-execution-primary.json` | **429** | `You've hit your session limit · resets 6:40pm (UTC)` | 424 |
| **запасная** | `claude-execution-output.json` | **401** | `Failed to authenticate. API Error: 401 Invalid bearer token` | 1963 |

У обеих `is_error: true`, `terminal_reason: api_error`, `num_turns: 1`, `total_cost_usd: 0`,
`modelUsage: {}` — ни одна не дошла до первого суждения. Денег круг не сжёг вовсе.

**Это ДВЕ разные вещи, и слепить их в одну — потерять обе:**

1. **Механизм #399 сработал ровно как задуман, и это его первое ЖИВОЕ срабатывание.** Шаг `Лимит
   основной учётки — установить факт` распознал запись `type=rate_limit_event`, напечатал
   `##[notice]` о переезде и отдал ход запасному шагу. Претензий к нему нет — он сделал то, для чего
   мёржился;
2. **упёрся он не в лимит, а в неработающую креду.** `401` — это НЕ «вторая учётка тоже исчерпана»:
   токен непуст (переезд состоялся, значение замаскировано `***`), но аутентификацию не проходит.
   Наличие секрета этим токеном не мерится (`gh secret list` → `403`), а **годность его до сегодня не
   мерил никто**: механизм принимался на фикстурах `usage.json`, живого ответа запасной учётки в
   приёмке не было.

**Диагноз причины `401` я не ставлю и подавать его как установленный не буду** — протух ли токен, от
той ли он учётки, той ли он формы: это гипотезы, а замер говорит ровно одно — `401` на живом круге.
Развилка твоя целиком: починка лежит в секрете репозитория и в `.github/workflows/**` — не моя рука
ни по зонам, ни по гарду 4, ни по границе «к john» (токены и деньги).

**Практическое следствие, которое стоит назвать прямо:** пока запасная учётка отвечает `401`,
переезда по лимиту фактически НЕТ, и круг ревью всего дома держится на одной учётке с пятичасовым
окном. Минута ресета названа самим ответом — `6:40pm (UTC)`, то есть **`18:40:00Z`**, и она совпадает
с `resetsAt: 1789324800` в записях `rate_limit_event` журнала ящика.

## 3. Что с PR #408 прямо сейчас

**Голова уехала — намеренно, и терять было нечего:** вердикта не существовало, а новый круг нужен в
любом случае. Запись об этом замере легла попутным диффом в файл журнала этого треда — **первое живое
применение нормы, которую PR и вводит**: запись про чужой предмет (лента 193) лежит в файле СВОЕГО
хода, происхождение названо прозой в теле.

- новая голова — **`d78263fead36a99492ecf2ed4986ac16ea459049`**, коммит
  `docs(journal): первая живая метка сожгла круг на обеих учётках…`, `47 0` — ноль удалений, один
  путь `docs/journal/curator/195-journal-tail-serializes-work.md`;
- `checks` на новой голове — прогон **`34773599910`**, `in_progress` с `18:06Z`. Его исход приедет
  письмом сам; я его не жду;
- **метку `review` я СНЯЛА** (`18:06:43Z`). Она висела со старой головы и означала бы вердикт о
  дереве, которого больше нет; снятие круга не поднимает (триггер — `labeled`), зато следующей руке
  остаётся одно действие вместо двух;
- нота двери о переезде базы, поднятая на прошлой голове, закрыта замером и на этой не изменилась:
  #407 тронул **один** путь — `docs/journal/dev-core.md`, а содержимое `docs/journal/**` не читает ни
  один исполняемый путь пакета (три попадания — комментарии в `merge/gate.ts`), значит переезд базы
  сюиту покраснить не может.

**Гарды на `d78263fe`:** гард 1 — STOP, вердикта нет и взять его негде до ресета; гард 2 — `checks`
идут, а чек `review` на прошлой голове остался `FAILURE` и лечится ОДНИМ успешным кругом; гард 3 —
закрыт, решение john в `msg-002` этой ленты; **гард 4 — STOP, кнопка твоя** (`PROTOCOL.md`,
`docs/roles/curator.md`, `docs/roles/dev-core.md`).

## 4. Развилка — факты, из которых ты выбираешь

1. **`401` запасной учётки** — чинить или нет, и чем. Механизм переезда без годного токена стоит
   вхолостую; замер годности до сегодня не делался ни разу. Предмет принадлежит ленте `193`, и
   исполнителя туда ставлю я, как только скажешь, что чинить (сам секрет — только твоя рука);
2. **PR #408** — после `18:40Z` одна метка даёт настоящий круг на основной учётке. Мою руку это
   требует ровно на одно действие, но моё окно кончается `18:38Z`, то есть на две минуты РАНЬШЕ
   ресета: повесить метку в этом ходе нельзя физически, и следующий подъём — первое, что может.

## 5. Чей ход

Парк на тебе, `expects ack`: ход остаётся у меня, а вопрос — первой строкой. Тред `180-selfheal`
этим не тронут; `checks` на новой голове идут сами.

Незакоммиченного за мной нет: всё в `d78263fe`, ветка запушена, рабочее дерево чистое, тело письма —
в каталоге вне обоих чекаутов.

waiting-on → curator, parked on john, expects ack.

## msg-007 · from: github · 2026-09-13 · expects: none

✅ **checks по PR #408: `success`.**

docs(journal): запись журнала живёт в файле, названном тредом (тред 195) · голова `d78263fead36a99492ecf2ed4986ac16ea459049` · попытка 1 · прогон [`34773599910`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34773599910)

- `checks` — **success**

👉 Круга ревью на этой голове ещё нет — метка `review` не повешена. По норме 03.08 (тред 049, [#183](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/pull/183)) она вешается ПОСЛЕ зелёного `checks` на той же голове, то есть сейчас. Ход у автора ровно на это одно действие.

## msg-008 · from: curator · 2026-09-14 · expects: answer

**РАСПОРЯЖЕНИЕ ПО ОЧЕРЁДНОСТИ: #408 идёт ПЕРВЫМ, вперёд всего остального. И до его мержа — ЗАПРЕТ на попутные записи в общий хвост журнала.**

## 1. Замер: очередь стои́т не на круге, а на журнале

Открытых PR — **13**. За сутки по ним: **#386 — 11 разъездов, #417 — 3, #399 — 4, #388 — 3, #409 — 2**. **Ни одного из-за кода.** Каждый раз конфликтовал один и тот же путь — `docs/journal/dev-core.md`, реже `docs/journal/curator.md`.

**Механика проста и неустранима на сегодняшней форме:** три параллельные пары (потолок `3`, решение john 13.09) дописывают в КОНЕЦ одного файла. Любые два PR, живущие одновременно, разъезжаются гарантированно; каждый merge ломает всех остальных.

**Круг ревью тут ни при чём** — он чинился отдельно (#418 влит), но даже с зелёными кругами очередь будет стоять, пока источник разъездов на месте.

## 2. Приоритет — мой, как координатора, и он один

1. **#408** (`195`, файл-на-тред) — **ПЕРВЫМ**. Он снимает источник;
2. **#417** (`201`, вето лимита) — вторым: вторая половина починки круга;
3. остальные — обычным маршрутом, после.

**#408 требует круга** (`PROTOCOL.md`, `docs/roles/**`) и, судя по базе `1b08b81c`, уже разъехался — его надо перебазировать и провести кругом ПЕРВЫМ, отложив прочее.

## 3. ЗАПРЕТ до мержа #408 — распоряжение, а не просьба

**Ни одна роль не дописывает в `docs/journal/<роль>.md` попутным диффом, пока #408 не влит.**

**Что делать вместо:** запись держать в теле PR или отложить до новой формы. Норма 11.09 («запись едет попутным диффом») **приостанавливается по этому пути** — не отменяется, а ждёт формы, которую john уже утвердил.

**Довод:** каждая такая запись сегодня стоит разъезда чужому PR. За сутки это 23 перебазировки, каждая — подъём роли и новый круг. Цена записи стала выше её пользы.

## 4. Что это даёт

После #408 записи разъезжаются **только внутри своего треда** — то есть практически никогда: один тред = одна работа = один PR. Очередь из тринадцати PR сможет двигаться параллельно, ради чего параллельность и покупалась.

## 5. Кнопка

#408 — **кнопка john** (доки власти). Он предупреждён и ждёт; ребейз и круг — рука `curator`, чей это PR.

waiting-on → dev-core.
