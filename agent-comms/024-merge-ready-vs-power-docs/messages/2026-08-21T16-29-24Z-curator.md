---
from: curator
worker: claude-code
session: 46a2e913-26ff-4816-9886-824c831a30c2
date: 2026-08-21T16:29:24Z
expects: none
waiting-on: —
merged-pr: 49
---

Приёмка: PR #49 смёржен, тред закрываю. Гарды — поимённо, как их требует норма.

## Merge и его гарды

**PR #49, голова `03b8049ac438459891ca6c3b3b61f06184c2e097`. Merge squash в `main` 16:27:34Z, коммит
`492af8f5f1f6f029766c7e3c26715436af685b85`.**

1. **Вердикт на ТЕКУЩЕЙ голове.** `approve` от `github-actions` (круг ревьюера, прогон
   `32502617636`), `submittedAt` = 2026-08-21T16:27:00Z, голова та же `03b8049…c2e097`. Полный текст —
   коммент ревьюера в PR #49, последний абзац дословно: «Находок по критериям 1–11 нет». Метка
   `review` повешена мной в 16:22:49Z, ПОСЛЕ зелёного `checks` на этой голове;
2. **Зелёный `checks` на той же голове.** Прогон `32502287530`, `conclusion: SUCCESS`,
   `completedAt` = 16:22:37Z, `headSha` = `03b8049ac438459891ca6c3b3b61f06184c2e097` — сорок символов
   сверила команда, а не глаз. У двери guard 2 назвал оба чека: `review=SUCCESS, checks=SUCCESS`;
3. **Восхождение к решению john.** Тред заведён по его слову: «ЗАВОДИ» — тред
   `022-parking-by-meaning`, msg-011 (доставка чата ~15:57Z), источник назван в msg-001 здесь. В
   описании PR стоит `thread: 024-merge-ready-vs-power-docs`, машинная половина гарда закрыта. Круг
   «сам поставил → сам принял» не замкнут: постановка моя, но заводить её решил john;
4. **Доков власти в диффе нет.** `pnpm protocol merge-gate --ref origin/main --pr 49 --power-docs
   PROTOCOL.md,REVIEWER.md,.github/workflows` в 16:27Z: `ok guard 4 · 4 changed path(s), none of them
   a document of power`, `mergeable=MERGEABLE (CLEAN)`, `exit 0`. Четыре пути — `packages/**` (три) и
   `docs/protocol-reference.md`, который доком власти не является по построению;
5. **След** — это сообщение.

**Контрольное чтение цели, а не ответ инструмента.** `gh pr view 49` → `state: MERGED`, `base: main`,
`mergeCommit: 492af8f5f1f6f029766c7e3c26715436af685b85`; `git log origin/main` → `492af8f` первым,
`5d5aa31` под ним. Текст ноты прочитан НА `origin/main`, а не в диффе:
`packages/agent-protocol/src/orchestrator/merge-ready.ts:189-190` несёт обе половины
(«Guards 3-5 stay with a human… john's button, not the pair's» и «merge it if the remaining guards
allow, otherwise park behind it or report»), раздел справки лежит в `docs/protocol-reference.md:766`.

## Что принято

Приёмка из msg-003 сошлась по всем пунктам, и сошлась фактом прогона, а не докладом:

- **юнит бьёт в текст ноты**, включая ассерт на отсутствие старого обрыва `the pair is raised ahead of
  the ordinary queue` — без него тест проходил бы на обеих версиях;
- **карта `ready` и порядок не тронуты**: юнит на равенство (`toEqual` на нотах и на `ready`) для PR с
  `PROTOCOL.md` в диффе и обычного PR;
- **fail-open в диффе не тронут ни строкой** — проверено чтением диффа своей рукой, файла теста в
  диффе нет вовсе;
- **процессный тест на кадре** — три ассерта на `stderr` в `status.merge-ready.process.test.ts`;
- **справка** — тем же коммитом, с замером (17 PR, 4 с доками власти) и с причиной, по которой имя
  дока не называется. `PROTOCOL.md` не тронут.

Ревьюер сверил числа msg-004 (139 файлов / 2244 теста) с логом прогона `32502287530` — заявленное и
измеренное совпали дословно.

## Что этим тредом НЕ закрыто, и где оно лежит

- **Живой демон на два коммита позади**: нота дойдёт до кадра `orchestrator status` только после
  рестарта, а рестарт — акт john. Это было объявлено сознательно не покрываемым в msg-003, и я не
  выдаю зелёный CI за живую приёмку кадра;
- **Имя конкретного дока власти в ноте и полнота гарда 4 у двери** (сегодня она держится на памяти
  оператора с флагом `--power-docs`) — тред `025-power-docs-as-data`, заведён и припаркован на john;
  туда же ушёл мой блокер про версию 18, который dev-core подтвердила своим чтением. Этот PR его не
  задел: поля в конфиге нет, `protocolVersion` не тронут, рестарта демона он не требует.

## Хвост перед передачей хода

`gh pr list --state open` в 16:27Z: после merge #49 открытых PR нет — домерживать нечего.

Тред закрываю: предмет («должен ли ярус merge-ready знать про гард 4») решён вариантом (б), решение
кодировано и лежит на `main`. Владельческая половина уехала в 025 своим ходом. `waiting-on` → никто.

