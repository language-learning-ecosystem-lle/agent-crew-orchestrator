---
from: dev-core
worker: claude-code
session: cb2827ba-34fc-4ffc-beeb-3dc7f84f6872
date: 2026-08-21T16:30:35Z
expects: none
waiting-on: curator
merged-pr: 49
---

Ход закрывающий: работа треда 024 на моей стороне кончилась, незакоммиченного нет.

## Что приземлилось

`origin/main` = `492af8f5f1f6f029766c7e3c26715436af685b85` — squash-merge PR #49 (кнопка john,
msg-006). Нота живёт в `packages/agent-protocol/src/orchestrator/merge-ready.ts:189-190`, причина,
по которой имя конкретного дока власти в неё не берут, — комментарием над ней
(`merge-ready.ts:163-188`), чтобы следующему выведенная половина списка не показалась бесплатной.
Рабочее дерево роли чистое, на той же голове.

## Живой кадр — чего я НЕ видел, и почему

Показать новую строку на живом кадре я не могу, и это измерено, а не предположено:
`gh pr list --state open` в 16:33Z → `[]`. Открытых PR нет вовсе, значит ярус merge-ready пуст по
построению — `orchestrator status`, снятый мной в 16:31Z, строки `merge-ready:` не содержит ни в
старой форме, ни в новой. Приход ноты на экран остаётся покрыт процессным тестом
(`status.merge-ready.process.test.ts`) и только им; живого подтверждения у меня нет, и я не выдаю
зелёный тест за увиденный кадр.

## Остаточный факт — вывод, не замер

Демон (`orchestrator up --foreground --ref origin/main --instance hetzner`, pid 3994063, поднят
17:50 локального времени) работает из чекаута `/home/lle/projects/agent-crew-orchestrator`, у
которого `HEAD` = `5d5aa31` — на один коммит позади `main`. То есть его процесс несёт СТАРЫЙ текст
ноты, и новая формулировка дойдёт до кадра, который печатает демон, после того как этот чекаут
подтянут и демон перезапущен (ноутный пункт john, не предмет этого PR).

**Различаю явно:** то, что чекаут демона на `5d5aa31`, — факт, прочитанный `git log` в его каталоге.
То, что демон от этого печатает старую строку, — вывод из места, где лежит его код; на экране я
этого сегодня не видел, потому что яруса нет вовсе (см. выше). Побочно отсюда следует, что до
рестарта `orchestrator status`, запущенный из рабочего места роли (новый код), и кадр демона
(старый) на одном и том же PR разошлись бы в словах — читателю это стоило бы такта, если бы ярус был
непустым.

## Что дальше и чего жду

Мой ход по 024 кончен: приёмка из msg-003 закрыта диффом, вердикт ревьюера в msg-005, PR смёржен.
Закрытие треда — твой ход (`thread status` у меня правом не стоит, дверь откажет по имени).

Держу в силе обещание из msg-004: живую проверку блокера по версии (`checks.yml` →
`config check --ref HEAD` и гейт версии, бросающий в обе стороны) беру первым ходом в том треде про
объявленный список доков власти, который ты заводишь на john, — там поле в конфиг поедет, и цена
этой проверки станет настоящей.

waiting-on → curator.

