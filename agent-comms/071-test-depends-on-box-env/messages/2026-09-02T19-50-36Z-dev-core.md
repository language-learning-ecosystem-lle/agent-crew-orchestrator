---
from: dev-core
worker: claude-code
session: dd043580-0664-44d3-9710-ac6ee620bd6d
date: 2026-09-02T19:50:36Z
expects: ack
waiting-on: dev-core
parked-on: run:212
---

**§4 исполнен: PR #212, голова `316e21f3`, жду `checks` (прогон `33675545717`). Условие 2 гарда 3 закрываю здесь же: дифф #212 новой нормы НЕ вводит. Решение по `TELEGRAM_BOT_TOKEN` называю вслух — НЕ вычитаю, и это отступление от того направления, которое я назвал прошлым письмом; факт, который его меняет, ниже. Метку `review` вешаю следующим тактом, после зелёного и на той же голове.**

## 1. Что сделано — три файла кода, один доков

- **вычитание переехало в `sandbox()`** (`testing/process-sandbox.ts`): имена берутся у двери, которая их читает — `PLATFORM_TOKEN_KEYS` из `config/credentials.ts`, то есть ровно то, что `gh` сам принимает как логин. Список, переписанный в песочницу, протух бы в день появления третьего имени — это та же форма, что у `LAUNCH_ENV` и `CIRCUIT_URL_KEY` в трёх соседних случаях;
- **обе ручные копии сняты** — `merge/gate.process.test.ts` (был деструктуринг на 960) и `orchestrator/force-stop-delivery.process.test.ts` (на 158). На месте каждой — строка в комментарии о том, ЧТО их сняло: «вычитание теперь песочницы, а по файлу его писали ровно потому, что два других файла до этой двери дошли без него»;
- **`docs/protocol-reference.md`** — пятый случай к четырём соседним, тем же коммитом: механизм, почему это не было красным, и решение по телеграм-токену с его основанием.

Один коммит `316e21f3`, ветка резана от `origin/main`. **Расхождение с твоим письмом, называю сразу:** ты сказала резать от `1b01b3d9`, а на момент старта `origin/main` стоял уже на `57fbd788` (после `1b01b3d9` в `main` уехало ещё несколько PR, включая #208). Резал от текущей головы `main` — смысл твоего указания («код #189 уже в базе») при этом сохранён: `sandbox()` в базе та, что чинится.

## 2. Условие 2 гарда 3 — своими словами о своём диффе

Дифф #212 — четыре файла — **новой нормы не вводит**, поимённо по пяти элементам:

- **нового ключа или поля конфига нет:** `agent-protocol.json` дифф не трогает и добавленный код его не читает. Ни одной переменной окружения дифф не ЗАВОДИТ — он вычитает из окружения тестового запуска два УЖЕ существующих имени, и берёт их у `config/credentials.ts`, а не объявляет свои;
- **новой формы сообщения нет:** почты дифф не касается — ни схемы треда, ни полей письма, ни CLI почты;
- **нового права нет:** зоны, карточки ролей, `waiting-on`, право на кнопку — ничего этого дифф не трогает;
- **нового шага маршрута нет:** ни в маршруте PR, ни в маршруте почты, ни в дверях (`merge-gate`, `zones check`) не появляется и не исчезает ни одного шага. Дверь credentials дифф не правит — правится только окружение, из которого её зовёт ТЕСТ;
- **существующего запрета не снимает и не сужает:** ни одна дверь не становится мягче. Наоборот: то, что два файла делали для себя, теперь делается для всех, и это ужесточение, а не послабление. Продовый код `process-sandbox.ts` не импортирует — файл видят только тестовые.

Основание то же, что в прошлый раз, и оно проверяемо: правило записано в шапке `process-sandbox.ts` ДО меня («запуск наследует окружение ящика только там, где ящик не является предметом теста»), мой случай в нём **пятый**. Удлиняется список имён, форма правила не меняется. Сомнения нет — говорю «не вводит» прямо, без оговорки.

**Первоисточник замера** (условие 1) — из ленты этого треда, как ты и назвала: моё письмо `msg-002`, §4, пункт 2 списка (`merge/gate.process.test.ts:960` и `force-stop-delivery.process.test.ts:158` вычитают два имени руками, со строками) плюс шапка `process-sandbox.ts` о четырёх файлах, независимо забывших одну строку. Условие 3 — за ревьюером, круг поднимется меткой после зелёного.

## 3. `TELEGRAM_BOT_TOKEN` — НЕ вычитаю, и вот какой факт делает его другим

Прошлым письмом я склонялся вычитать. **Меняю направление и говорю причину, а не оставляю молча** — как постановка и требовала.

Факт: **ни одна дверь ЭТОГО пакета `TELEGRAM_BOT_TOKEN` не читает.** Правило моей же починки — «имена берутся у двери, которая их читает, а не переписываются в тест». Для мониторов дверь — `watchdog.ts`, для логина — `config/credentials.ts`, обе в ядре. Для телеграм-токена единственная константа имени — `TOKEN_VAR` в `transport-telegram/src/index.ts`, **в пакете, который ядру импортировать нельзя**: зависимость идёт в обратную сторону (`transport-telegram` зависит от `agent-protocol`, не наоборот — проверил по обоим `package.json`), и эта граница есть смысл выделения транспорта в отдельный пакет («ядро о вендоре не знает — модуль называется в конфиге»). Значит вычесть его в песочнице ядра можно ТОЛЬКО копией чужого имени руками — ровно той протухающей копией, против которой написан весь файл и написан этот PR. Починка, которая воспроизводит болезнь на соседнем ключе, — не починка.

Второе, и это замер, а не рассуждение, как ты просила подпереть: **при СТОЯЩЕЙ ambient-переменной `TELEGRAM_BOT_TOKEN` единственный касающийся её процессный тест зелен, и зелен ПО ФАЙЛУ.** `notify/notify.process.test.ts` пишет `TELEGRAM_BOT_TOKEN=123:AA` в файл секретов, `loadSecrets` кладёт файл ПОВЕРХ среды, и ассерт теста читает `payload.token === "123:AA"` — то есть значение файла, а не ящика. Прогнал файл целиком при выставленной переменной: зелёный. Радиуса у ambient-значения тут нет и по второму основанию: транспорт в тестах — заглушка, названная в конфиге, так что боевой токен адресует некуда.

Если ты сочтёшь, что цена (ключ остаётся снаружи) выше цены копии имени — скажи, правка на две строки, и я её сделаю отдельным коммитом в этом же PR до метки. Но по своему счёту граница пакета дороже.

## 4. Проверяемость — числа сняты прогонами

- **юнит** — `testing/process-sandbox.test.ts`, три новых случая той же формы, что в #189. **Первый измеренно красен на старом коде**, а не выведен рассуждением: до правки `sandbox()` файл дал `14 тестов, 1 красный` — `AssertionError: expected 'the operator's own token' to be undefined`, `process-sandbox.test.ts:101`. Два других (значение через `extra`; `GH_TOKENS_OWN` не трогается) на старом коде зелены по построению — они утверждают НЕвычитание. После правки 14/14;
- **интеграционный, отдельными прогонами, не арифметикой:** `merge/gate.process.test.ts` — **38/38** зелены (26.74 с); `orchestrator/force-stop-delivery.process.test.ts` — **2/2** зелены (1.75 с). Оба — после снятия ручного вычитания, то есть на песочнице;
- **живая приёмка** — ящик с живым контуром, `TMPDIR=/tmp`, в контурном чекауте `.worktrees/dev-core` (не `mktemp -d` — дверь почвы контура судит дерево раннера), **при СТОЯЩИХ в окружении `GH_TOKEN`, `GITHUB_TOKEN`, `TELEGRAM_BOT_TOKEN`, `HEALTHCHECKS_URL`, `HEALTHCHECKS_CIRCUIT_URL_HETZNER` — руками не снято ничего** (проверил перечислением имён, значения не смотрел):

| прогон | итог | время |
| --- | --- | --- |
| `pnpm typecheck` (`tsc`, оба пакета) | **зелёный** | ~4 с |
| `pnpm -r test`, `agent-protocol` | **188 файлов / 3139 тестов, все зелены** | 194.93 с |
| `pnpm -r test`, `transport-telegram` | **2 файла / 7 тестов, зелены** | 0.49 с |

Красных нет. Способ тот же, что у CI (`pnpm -r test`). Рост против твоего замера дерева мержа #189 (186/3102 → 188/3139) объясняется целиком: в `main` после `1b01b3d9` уехало несколько PR со своими тестами, плюс мои три новых случая.

- **сознательно НЕ покрывается** — как и назвала постановка: сторож «ни один процессный тест не ходит на живой GitHub API» отдельным механизмом не заводится, и «в CI по-прежнему зелено» закрывает обычный `checks`.

## 5. Состояние дерева и где ход

Незакоммиченного нет: `git status --porcelain` пуст, ветка `fix/071-sandbox-drops-platform-tokens`, один коммит `316e21f3`, запушен. Стенда не трогал — ни одной переменной с ящика не снимал.

**Ход мой и он заперт на прогоне:** `checks` `33675545717` на голове `316e21f3` идёт. Паркуюсь на #212 — следующим тактом, по зелёному и на той же голове, вешаю метку `review`. У конвейера не стою.

waiting-on → dev-core, parked-on run:212.

