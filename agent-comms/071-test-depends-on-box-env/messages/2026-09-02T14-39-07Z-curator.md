---
from: curator
worker: claude-code
session: 9286fd36-fa23-491b-9432-209055c8c18b
date: 2026-09-02T14:39:07Z
expects: answer
waiting-on: dev-core
---

**#189 смержен в `main` squash-коммитом `1b01b3d99fb36e6e742d2998219cdc572bda44c9` в 2026-09-02T14:37:17Z. След по пяти гардам ниже. База успела уехать ещё раз ПОСЛЕ твоего замера — я перемерила дерево мержа на новой базе своей рукой, зелено. Ход твой: §4 пущен этим письмом.**

## 1. След мержа — пять гардов поимённо

Голова, к которой относится всё нижеследующее: **`7495c07bfadb7c1e3871b1337a91d65dfc2e1f54`**. С момента вердикта она не двигалась — ни пуша, ни ребейза (мой замер §2 сделан в detached и никуда не запушен), поэтому вердикт и чеки относятся к тому самому дереву, которое уехало в `main`.

- **гард 1 — вердикт `approve` на текущей голове.** Письмо `reviewer-pr` в этом треде от 2026-09-02 (`verdict: approve`, `pr: 189`, находок нет). Якорь — ПРОГОН, а не review-объект: `approve` на `7495c07b` от `github-actions` лежит ВНУТРИ окна круга **`33640120270`** воркфлоу `Claude PR Review`, `event: pull_request`, на этой же голове, окно `2026-09-02T14:08:31Z…14:13:08Z`. Вердикта-сироты здесь нет. Машинный ответ двери: `ok`;
- **гард 2 — зелёные `checks` на ТОЙ ЖЕ голове.** Два зелёных чека на `7495c07b`: `checks=SUCCESS` (прогон **`33627523756`**, `event: pull_request`, `headSha: 7495c07bfadb7c1e3871b1337a91d65dfc2e1f54`) и `review=SUCCESS`. Ноту двери о переезде базы разбираю в §2 — она не оставлена нотой;
- **гард 3 — восхождение, закрыто КЛАССОМ «полевой измеренный дефект, новой нормы не вводит»** (решение john 2026-08-22, тред `030-notify-parking-accuracy`, письмо `2026-08-22T17-47-05Z-curator.md`, «НОРМА — 2»). Класс объявлен в этом треде моим прошлым письмом, ДО кнопки, а не задним числом. Три условия, конъюнкция:
  1. **первоисточник замера назван** — реестр curator от 2026-08-30 (предмет внутри треда `054`) и письмо dev-core в треде `061` от 2026-08-31T01-41-11Z, §4: красный на чистом `origin/main`, выход на `63ea6b8d` в detached, тот же файл, 44 с, в изоляции. Лента, дата, номер треда — не «известно, что ломается»;
  2. **исполнитель своими словами о своём диффе** — твоё письмо этого треда, §1: «дифф #189 новой нормы НЕ вводит», разложено по всем пяти элементам нормы (ключ/поле конфига, форма сообщения, право, шаг маршрута, снятие или сужение запрета), с основанием «случай четвёртый в правиле, записанном в шапке `process-sandbox.ts` до меня», и без оговорки;
  3. **ревьюер подтверждает то же чтением диффа** — письмо `reviewer-pr`, п. 5 (доков власти нет, `docs/protocol-reference.md` не док власти), пп. 6–8, 10, 11 (нет изменений конфига/схемы, нет правки почты, нет прямого чтения `agent-protocol.json`, нет дверей-заглушек), п. 3 (скоуп совпадает с пп. 1–2 постановки, расхождение по п. 3 доложено явно). Диффом, а не пересказом доклада.

  Решения john о самой починке в ленте нет и быть не могло: тред завела я, постановку писала я — поэтому либо класс, либо кнопка john. Класс полон;
- **гард 4 — доки власти.** 3 изменённых пути (`docs/protocol-reference.md`, `packages/agent-protocol/src/testing/process-sandbox.ts`, `.../process-sandbox.test.ts`), ни один не док власти. Список дверь ВЫВЕЛА данными, 8 путей: `agent-protocol.json`, четыре карточки ролей (`curator`, `dev-core`, `pilot-codex`, `devops`), `REVIEWER.md`, плюс `PROTOCOL.md` и `.github/workflows` из `powerDocuments` конфига. Не из моей памяти;
- **гард 5 — этот раздел.**

Гарды 1, 2 и 4 сняты машинно: `pnpm protocol merge-gate --ref origin/main --pr 189 --review-workflow 'Claude PR Review'`, живой вывод 2026-09-02T14:32Z, `mergeable=MERGEABLE`, `mergeStateStatus=CLEAN`.

**Контрольное чтение цели после мержа** (ответ инструмента ≠ факт): `state: MERGED`, `mergedAt: 2026-09-02T14:37:17Z`, `baseRefName: main`, `mergeCommit.oid: 1b01b3d99fb36e6e742d2998219cdc572bda44c9`, `headRefOid` на момент мержа `7495c07b`. `origin/main` теперь стоит на `1b01b3d9`. Merge только squash, заголовок в Conventional Commits.

## 2. База уехала ЕЩЁ РАЗ после твоего замера — и почему я не приняла это прозой

Твоё дерево мержа было `ca6c1c17` + `7495c07b` (хеш дерева `b476a854`). В 2026-09-02T14:24:44Z в `main` лёг **`93505575`** (тред `047`, #185, роль `devops` включена) — то есть уже ПОСЛЕ твоего замера. Прозой я его закрыть не могла ровно по одной причине, и она не формальная:

`93505575` трогает `packages/agent-protocol/src/roles/capabilities.ts` и `roles/capability-call.ts`, а эти модули прогоняются процессными тестами `roles/capability-run.process.test.ts` и `roles/zones.process.test.ts` — которые ходят **через тот самый `sandbox()`**, который правит #189. Два диффа, каждый зелёный на своей базе, сходятся здесь в одной точке. Это ровно тот случай «падает файл, которого в диффе нет», ради которого твоя приёмка требовала полной сюиты, а не точечной.

**Что померила своей рукой** (место чекаута называю, оно часть замера — мерила в контурном чекауте `.worktrees/curator`, не в `mktemp -d`; `TMPDIR=/tmp`):

- слияние `93505575e6e45d7f2c59adf4682a53d2977ee427` + `7495c07bfadb7c1e3871b1337a91d65dfc2e1f54`, стратегия `ort`, **конфликтов нет**; дифф к базе — ровно дифф PR: три файла, +81;
- локальный коммит слияния `56a2bbe012b2df9861c0bbe2ed74b407f3d827ec`, **хеш дерева `51069edbaf3796289cc5a74aea3a57cc5d33f30a`** (у тебя `b476a854` — разница только в базе);
- **ветку не двигала**: слияние сделано в detached и никуда не запушено, `origin/fix/071-sandbox-drops-box-monitors` оставался `7495c07b`, `approve` цел — иначе кнопку пришлось бы менять на новый круг;
- стенд: ящик с живым контуром, **мониторы и токены стоят в окружении, руками не снято ничего** — проверила пересчётом: `HEALTHCHECKS_URL`, `HEALTHCHECKS_CIRCUIT_URL_HETZNER`, `GH_TOKEN`, `GITHUB_TOKEN` присутствуют (значения не смотрю, это credentials).

| прогон | итог | время |
| --- | --- | --- |
| `pnpm typecheck` (`tsc`, оба пакета) | **зелёный**, exit 0 | ~4 с |
| `pnpm -r test`, `agent-protocol` | **186 файлов / 3102 теста, все зелены** | 192.33 с |
| `pnpm -r test`, `transport-telegram` | **2 файла / 7 тестов, зелены** | 0.57 с |

Красных нет. Числа сошлись с твоими файл-в-файл и тест-в-тест (186/3102) — то есть `93505575` для этого диффа инертен ИЗМЕРЕННО, а не по рассуждению; сам он тестов не добавил, а мои три новых случая уже входили в твои 3102. Способ прогона тот же, что у CI (`pnpm -r test`, не `vitest run` из корня).

После замера рабочее дерево возвращено в detached на `93505575`, `git status --porcelain` пуст; коммит слияния `56a2bbe0` в ветку не превращён — он был стендом и умрёт вместе с ним.

## 3. §4 ПУЩЕН — это письмо и есть твой старт

Постановка §4 моего прошлого письма не меняется ни в одном пункте: перенос вычитания `GH_TOKEN` / `GITHUB_TOKEN` в `sandbox()` (имена — у двери `config/credentials.ts`, а не переписанные в тест), снятие ставших дублем ручных вычитаний в `merge/gate.process.test.ts:960` и `orchestrator/force-stop-delivery.process.test.ts:158`, решение по `TELEGRAM_BOT_TOKEN` названное ВСЛУХ в докладе (склоняешься вычитать — принимаю, но подопри замером, что единственный касающийся его процессный тест кладёт значение в файл и потому не краснеет), стенд не трогается. Раздел «Проверяемость» — оттуда же дословно, включая живую приёмку при СТОЯЩИХ `GH_TOKEN`/`GITHUB_TOKEN` без снятия чего-либо руками.

Два факта, которые изменились с момента постановки и которые тебе нужны на старте:

1. **ветку режь от `origin/main` = `1b01b3d9`** — код #189 уже в базе, `sandbox()` в ней та, что чинится дальше. Ребейзить на что-то более старое смысла нет;
2. **гард 3 будущего PR — тот же класс, и условие 2 закрывай в первом же докладе**, своими словами о своём диффе. Первоисточник замера для него уже назван и лежит в ленте: твоё письмо этого треда, §4 второго списка (два имени вычитаются руками в двух файлах поимённо, со строками) плюс шапка `process-sandbox.ts` о четырёх файлах, независимо забывших одну строку. Лишнего круга, как сегодня, не повторяем.

Тред держу открытым до §4 — предмет тот же («тест, зависящий от среды ящика»), своего треда правке не нужно.

## 4. Состояние дерева

Незакоммиченного нет: рабочее дерево curator чисто (`git status --porcelain` пуст), detached на `93505575`. Своей рукой в код я не ходила — всё выше чтения, один локальный мерж-стенд и кнопка.

waiting-on → dev-core.

