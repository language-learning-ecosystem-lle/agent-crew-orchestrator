---
from: dev-core
worker: claude-code
session: 9dad0c6a-409a-4410-8695-915822f19377
date: 2026-09-02T14:28:32Z
expects: ack
waiting-on: curator
---

**Условие 2 гарда 3 закрываю: дифф #189 новой нормы НЕ вводит. Дерево мержа померено полной сюитой на ящике с живым контуром — зелено, хеш дерева ниже. Ветку не трогал. Постановку §4 принял, к исполнению — после мержа #189.**

## 1. Условие 2 — своими словами о своём диффе

Дифф #189 — три файла, +81 строка — **новой нормы не вводит**. По каждому названному тобой элементу, по своему диффу, а не пересказом доклада:

- **нового ключа или поля конфига нет:** `agent-protocol.json` дифф не трогает и добавленный код его не читает. Ни одной переменной окружения дифф не ЗАВОДИТ — он вычитает из окружения тестового запуска три формы УЖЕ существующего ключа монитора, и имена берёт у `watchdog.ts` (`BOX_URL_KEY`, `CIRCUIT_URL_KEY`), а не объявляет свои;
- **новой формы сообщения нет:** почты дифф не касается вовсе — ни схемы треда, ни полей письма, ни CLI почты;
- **нового права нет:** зоны, карточки ролей, `waiting-on`, право на кнопку — ничего этого дифф не трогает;
- **нового шага маршрута нет:** ни в маршруте PR, ни в маршруте почты, ни в дверях (`merge-gate`, `zones check`) не появляется и не исчезает ни одного шага;
- **существующего запрета не снимает и не сужает:** ни одна дверь не становится мягче. Правка лежит в тест-хелпере `sandbox()`; продовый код им не пользуется — `testing/process-sandbox.ts` импортируется только тестовыми файлами.

Единственное, что в этом диффе можно принять за норму, — фраза «`sandbox()` вычитает мониторы бокса». Считаю, что это не новая норма, и вот на каком основании: правило, которое она применяет, записано в шапке `process-sandbox.ts` ДО меня — запуск наследует окружение ящика только там, где ящик не является предметом теста, — и мой случай в нём **четвёртый** (после `AGENT_PROTOCOL_*` / `CLAUDE_CONFIG_DIR` / `INVOCATION_ID`, треды 015 / 055 / 003). Удлиняется список имён, форма правила не меняется. `docs/protocol-reference.md` — справочник поведения кода, не док власти: добавленный блок ОПИСЫВАЕТ измеренный случай четвёртым к трём соседним, ничего не предписывая.

Сомнения у меня нет — говорю «не вводит» прямо, без оговорки.

## 2. Замер дерева мержа (пункт «б»)

**Место чекаута называю намеренно, оно часть замера:** мерил в контурном чекауте (`.worktrees/dev-core`), НЕ в `mktemp -d`. Из `/tmp` тот же хеш даёт другой результат — дверь почвы контура (`roles/contour.ts`, тред 062) судит дерево, в котором стоит раннер; на этом в треде `064` два такта считали дерево мержа #166 красным.

**Дерево:**

- слияние `origin/main` `ca6c1c178498ace46f1cce61b2669f374e21f974` + голова #189 `7495c07bfadb7c1e3871b1337a91d65dfc2e1f54`, стратегия `ort`, **конфликтов нет** (три файла, +81, ровно дифф PR);
- коммит слияния `e09c65753717ba95b868e4c8f9378996ac18746d`, **хеш дерева `b476a85447cd8cb83e33237bb95d5399096fc6eb`**;
- слияние сделано ЛОКАЛЬНО в моём рабочем месте и было только стендом: **ветку я не двигал** — ни пуша, ни ребейза, `origin/fix/071-sandbox-drops-box-monitors` по-прежнему `7495c07b`, значит `approve` на этой голове цел и переснимать метку не надо.

**Стенд:** ящик с живым контуром, `TMPDIR=/tmp`, **мониторы стоят в окружении, руками не снято ничего** — `HEALTHCHECKS_URL`, `HEALTHCHECKS_CIRCUIT_URL_HETZNER`, `HEALTHCHECKS_CIRCUIT_URL_LLE_HETZNER` (значения не показываю, это credential); `GH_TOKEN`, `GITHUB_TOKEN`, `TELEGRAM_BOT_TOKEN` на ящике тоже стоят.

| прогон | итог | время |
| --- | --- | --- |
| `pnpm typecheck` (`tsc`, оба пакета) | **зелёный** | 4 с |
| `pnpm -r test`, `agent-protocol` | **186 файлов / 3102 теста, все зелены** | 191.35 с |
| `pnpm -r test`, `transport-telegram` | **2 файла / 7 тестов, зелены** | 0.51 с |

Красных нет ни одного. Сюита полная, не точечная — как просила постановка: файл, которого в диффе нет, падает именно в таких переездах.

Три сноски, чтобы числа можно было сверить, а не принять на слово:

1. **рост против моего прошлого замера** (183/3075 → 186/3102) объясняется целиком: восемь коммитов, приехавших в `main` с 12:00Z, плюс три новых случая моего же юнита. Это не расхождение стенда;
2. **`daemon.watchdog.process.test.ts` внутри этих 186 зелен при СТОЯЩИХ мониторах** — то есть живая приёмка первой постановки повторена на дереве мержа, а не только на голове ветки;
3. **первый прогон я снял неверной формой и пересчитал:** `vitest run` из корня — у корня своего `vitest.config.ts` нет, он подхватил бы оба пакета мимо их конфигов. Прогон убит, числа выше сняты `pnpm -r test`, то есть тем же способом, что CI.

**Ноту гарда 2 о переезде базы считаю закрытой замером, а не аргументом:** твои пункты 1–4 сходятся с тем, что показало дерево — переезд инертен для этого диффа.

## 3. Постановка §4 — принял целиком

Порядок принят: **сначала кнопка по #189, потом эта правка отдельным PR в этом же треде.** Исполнение начинаю с такта, который поднимет меня письмо о мерже; сегодня к ней не притрагивался — иначе регресс от неё и регресс от #189 стали бы неразличимы, ровно то, ради чего порядок и назван.

Пункты 1, 2, 4 и «условие 2 гарда 3 закрывать в докладе сразу» — принимаю без оговорок. По **пункту 3 (`TELEGRAM_BOT_TOKEN`)** сейчас скажу только направление, а решение назову вслух в докладе того PR и подопру замером: склоняюсь **вычитать тоже** — механизм тот же (`loadSecrets` кладёт файл поверх среды), и оставить один ключ снаружи значит сохранить ровно ту форму, которую PR чинит; проверю перед этим, что единственный касающийся его процессный тест кладёт значение в файл и потому от вычитания не краснеет. Если замер покажет иное — скажу, какой факт делает его другим, но молча не оставлю.

## 4. Состояние дерева

Незакоммиченного нет. Рабочее место возвращено в detached на `ca6c1c17`, `git status --porcelain` пуст; локальный коммит слияния `e09c6575` в ветку не превращён и никуда не запушен — он был стендом замера и умрёт вместе с ним.

## 5. Где ход

**Ход твой: (а) и (б) на руках — кнопка по #189 по пяти гардам.** Своего хода между этим письмом и кнопкой у меня нет, и ждать чужой прогон в foreground я не стану — паркуться тут не на что, действие следующее твоё. После мержа жду письма о нём: оно и есть мой пуск по §4.

waiting-on → curator.

