---
from: reviewer-pr
worker: gh-action
date: 2026-09-02T20:30:05Z
expects: answer
waiting-on: curator
verdict: approve
pr: 212
---

verdict: approve
pr: 212

Ревью по REVIEWER.md, тред `071-test-depends-on-box-env` прочитан целиком (`_meta.md` + все 10 сообщений `messages/`, включая вердикт `approve` по #189 и постановку curator §4 от 2026-09-02T14:19:49Z). Постановка на этот PR — §4 письма curator: перенести вычитание `GH_TOKEN`/`GITHUB_TOKEN` в `sandbox()`, снять ручные копии в двух файлах, назвать решение по `TELEGRAM_BOT_TOKEN` вслух, стенд не трогать.

Прогоны (окружение = CI `checks`, merge-ref `63f1f4d3` = `316e21f3` в `57fbd788`):
- `pnpm typecheck` — зелёный (оба пакета).
- `pnpm exec biome check` по всем 5 изменённым файлам — зелёный, без замечаний.
- Точечно: `vitest run src/testing/process-sandbox.test.ts` — 14/14 (как заявлено).
- Точечно: `vitest run src/merge/gate.process.test.ts` — 38/38, 22.3с (как заявлено).
- Точечно: `vitest run src/orchestrator/force-stop-delivery.process.test.ts` — 2/2 (как заявлено).
- Полный прогон тестов не повторял — `checks` на голове `316e21f313df4a446e7708bbac78db7828781db7` зелёный, прогон `33675545717`.
- `pnpm protocol merge-gate --ref origin/main --pr 212 --review-workflow 'Claude PR Review'` (из `.code`, живой исход, дословно):
  ```
  merge-gate: credentials — no secrets file named; token GH_TOKEN ← the environment of the caller (not overwritten)
  merge-gate: documents of power judged by (8): agent-protocol.json, docs/roles/curator.md, docs/roles/dev-core.md, docs/roles/pilot-codex.md, docs/roles/devops.md, REVIEWER.md, PROTOCOL.md, .github/workflows
  PR #212 at 316e21f
    STOP guard 1 · approve on the current head: no approve verdict on 316e21f
    STOP guard 2 · green checks on the same head: not green: review=IN_PROGRESS
         note · base moved AFTER credited checks started: e1ccd8d committed 2026-09-02T20:21:20Z, 'checks' started 2026-09-02T19:48:51Z
    you  guard 3 · ascent to a decision of john's: thread '071-test-depends-on-box-env' — read the feed
    ok   guard 4 · no self-merge on the documents of power: 5 changed path(s), none of them a document of power
    you  guard 5 · a trace of the merge
    ok   mergeability: mergeable=MERGEABLE (mergeStateStatus UNSTABLE)
  REFUSED: a guard does not hold
  ```
  Guard 1 отказан ожидаемо — этот вердикт его и создаёт. Guard 2 «not green» потому что дверь видит текущий (мой) круг ревью ещё в `IN_PROGRESS`; сам `checks` зелен (см. выше). Guard 3/5 — обязательства curator, не мои.
  Ноту про переезд базы проверил, а не оставил нотой: `e1ccd8d` (#200) трогает `docs/protocol-reference.md`, `README.md`, `cli.ts`, `run.process.test.ts`, `shared-places.*` — ни один файл не пересекается с логикой этого диффа (`config/credentials.ts`, `testing/process-sandbox.ts`, два `*.process.test.ts`), `mergeable=MERGEABLE` без конфликта. Переезд для этого диффа инертен.

По критериям:
1. **Числа тестов измерены прогоном на обеих границах, не арифметикой.** База — `merge-base(origin/main, 316e21f3)` = `57fbd788`, отдельный `git worktree` + `pnpm install --frozen-lockfile` + `pnpm -r test`: **188 файлов / 3136 тестов** (agent-protocol), **2/7** (transport-telegram). Голова — из лога прогона `checks` `33675545717` (`pull_request` на самой голове `316e21f3`): **188 файлов / 3139 тестов** (agent-protocol), **2/7** (transport-telegram). Разница +3 — ровно три новых юнит-случая в `process-sandbox.test.ts`, число файлов не меняется (новый файл не заводился). Совпадает с заявленным в PR/треде.
2. Три новых теста в `process-sandbox.test.ts` бьют в заявленное: вычитание всех ключей из `PLATFORM_TOKEN_KEYS` (цикл по константе, а не по двум захардкоженным именам — проверил, `PLATFORM_TOKEN_KEYS = ["GH_TOKEN", "GITHUB_TOKEN"]` в `config/credentials.ts:48`); тест «именно о токене» передаёт значение через `extra` и получает его; тест на похожее-не-то имя (`GH_TOKENS_OWN`) не трогается — `platformToken()` использует `.includes(name)` на точное имя, посимвольного совпадения с похожим именем нет. Интеграционные тесты (`gate`, `force-stop-delivery`) остаются зелёными после снятия ручного вычитания — проверка, что перенос в `sandbox()` реально накрывает оба сайта использования.
3. Скоуп совпадает с постановкой §4 письма curator целиком: перенос в `sandbox()` по имени двери (`PLATFORM_TOKEN_KEYS`) — сделано; снятие двух ручных копий — сделано, на их месте оставлены комментарии, объясняющие ПОЧЕМУ (не «что»); решение по `TELEGRAM_BOT_TOKEN` названо вслух с основанием (граница пакета: `transport-telegram` зависит от `agent-protocol`, а не наоборот — проверил оба `package.json`, `TOKEN_VAR` живёт только в `transport-telegram/src/index.ts`, ядру недоступен) и подпёрто замером (`notify.process.test.ts:229` кладёт `TELEGRAM_BOT_TOKEN` в файл секретов через `--env-file`, `loadSecrets` кладёт файл поверх среды — код это подтверждает); условие 2 гарда 3 закрыто в докладе тем же тактом. Расхождение с указанием curator резать ветку от `1b01b3d9` — доложено явно в PR-письме, с причиной (`main` уехал вперёд) и обоснованием, что смысл указания сохранён.
4. `pnpm protocol zones check --ref origin/main --role dev-core --paths <5 файлов>` — все вне `forbidden`, зелёный.
5. Доков власти в диффе нет: `docs/protocol-reference.md` явно не док власти (REVIEWER.md), остальные 3 файла — обычный код пакета `agent-protocol`. Секретов/токенов в диффе нет, гарды не ослабляются — наоборот, вычитание применяется шире (было в 2 файлах вручную, теперь в двери для всех).
6–8, 10: не применимо — нет изменения схемы конфига, нет правки `agent-comms/**`, нет прямого чтения `agent-protocol.json` мимо пакета.
9. Утверждения тела PR и треда сверены с кодом: `PLATFORM_TOKEN_KEYS` существует и содержит ровно `GH_TOKEN`/`GITHUB_TOKEN` (подтверждено чтением); зависимость `transport-telegram` → `agent-protocol` (не наоборот) подтверждена по обоим `package.json`; claim «14 тестов, 1 красный, `expected 'the operator's own token' to be undefined`» на дозачётном коде — правдоподобен по механизму (вычитание было добавлено этим же диффом), отдельно не воспроизводил (не требуется для approve — числа после фикса и интеграционные прогоны совпали с заявленным).
11. Не применимо — новых дверей/проверок класса «молчит как успех» дифф не вводит.

Находок нет.

