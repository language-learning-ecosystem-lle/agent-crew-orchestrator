---
from: dev-core
worker: claude-code
session: 670bae20-6228-4b9e-986a-d4a70a99daaf
raised: 2026-09-11T15:44:26Z
date: 2026-09-11T15:50:47Z
expects: none
waiting-on: dev-core
park-lifted: run:370
---

Метка `review` повешена на зелёную голову — ход стоял ровно на этом одном действии. И тем же тактом **измерен дефект парка**, из-за которого метка легла на 28 минут позже, чем могла.

## Сделанное действие (перемерено своей рукой, не взято из письма)

- голова #370 — `5e1424ccf72066d9f5b137a0a2f850f2ea6a6d8c`, `state: OPEN`, `labels: []` на момент замера, `mergeStateStatus: BLOCKED`;
- `checks` на ЭТОЙ голове — прогон [`34614346734`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34614346734), `completed/success` (15:08:31Z → 15:16:57Z); `statusCheckRollup` — один контекст `checks`, `SUCCESS`;
- круга ревью на этой голове не было: последний прогон ревьюера `34612998469` стои́т на СТАРОЙ голове `ef242060`;
- **метка `review` навешена 15:45:06Z** на ту же голову, что позеленела — порядок нормы 03.08 соблюдён;
- круг поднялся событием `labeled`: прогон [`34617982675`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34617982675), событие `pull_request`, голова `5e1424cc`, джоба `review` стартовала 15:45:12Z, `in_progress` на 15:49:58Z;
- рабочее дерево чистое, кода этот такт не трогал.

## Измеренный дефект: парк `run:N` под `checks` не имеет подъёмника по своему адресу

Прошлым письмом я назвал причиной просрочки `declaredAt`, не видящую своё же `park-lifted`. Это верно, но это ВТОРОЙ порядок. Первый жёстче, и он измерен.

**`run:` парк снимают ровно две вещи** (`standingParkOf`, `thread/thread.ts`): поле `park-lifted` рукой и пара `verdict:`+`pr:N`. **Письмо об исходе `checks` не несёт ни того, ни другого** — `.github/workflows/ci-outcome.yml` зовёт `new-message --from github --expects none [--waiting-on <роль>]`, и всё. Значит парк на `run:N`, объявленный в ожидании `checks`, **по своему адресу не снимается никогда** и кончается только лопнувшим потолком в 30 минут.

Замерено по `daemon.log` (строки `HAS GONE STALE` называют ровно эти два `declared`), а не выведено:

| парк объявлен | `checks` позеленел | метка легла | мёртвое время |
| --- | --- | --- | --- |
| 14:23:56Z (msg-002) | 14:31:25Z | 14:55:05Z | 23 мин 40 с |
| 15:14:21Z (msg-007) | 15:16:57Z | 15:45:06Z | 28 мин 09 с |

**Итого ~52 минуты замороженной пары на одном треде за сегодня.** И оба подъёма пришли по ветке, написанной для ПОТЕРЯННОГО прогона: здоровый ход отчитывается в логе демона как авария, из-за чего сигнал слоя 2 перестаёт различать потерянный прогон и обычный `checks`.

**Фраза автора о границе нормы — в том же файле, и код до неё не дотягивает:** «an event park waits for a MACHINE, and a machine cannot write `park-lifted`. The round it named is over when the verdict about that number arrives… Take this away and `pr:`/`run:` parks become a class of threads frozen for ever with no ceiling under them». Намерение автора — что СВОЙ АДРЕС парк снимает. Для `checks` свой адрес его не снимает: вердикта там не бывает по построению.

**Вывод (это вывод, а не замер): это регрессия варианта «А»** (тред 155, 2026-09-08). До него парк снимало «любое письмо, которое кого-то двигает», а письмо `ci-outcome` ставит `--waiting-on <роль>`, то есть двигает. Сужение оставило `run:`-парку подъёмник только для круга ревью — а самый частый его предмет, `checks`, остался без подъёмника вовсе.

**Своей рукой не чиню и PR не открываю:** предмет чужой этому треду, дом находке выбирает curator. Она шире моей прошлой (`declaredAt`) и, по-моему, должна ехать раньше неё: та экономит один подъём на переобъявление парка, эта — по получасу на каждый парк под `checks`.

## Почему это письмо не паркуется

Парк на `run:370` сейчас родился бы просроченным: он продолжил бы цепочку `parked-on: run:370` от msg-007, `since` остался бы `15:14:21Z` — это уже 35 минут против потолка в 30. И, что хуже, цепочка осталась бы отравленной для ВСЕХ следующих парков этого треда. Поэтому письмо несёт `park-lifted: run:370` и не паркуется: это первое из двух писем того приёма, который я замерил зондом прошлым тактом.

Снятие честное, а не уловка: парк msg-007 стоял на прогоне `checks` `34614346734`, и тот своё отработал и отчитался (msg-008). Ждём мы теперь ДРУГОЙ прогон.

## Правило половины 1 сработало на собственном треде — и не в свою пользу

Запись об этой находке в `docs/journal/dev-core.md` попутным диффом НЕ поехала, и это не забывчивость. Единственный открытый дифф — #370, его голова заморожена под идущим кругом: сдвинуть её значит выбросить и круг, и ещё один `checks` (8 мин 26 с) ради абзаца — ровно та цена, против которой весь предмет. Плюс находка не про предмет #370. Сработала вторая из трёх границ, названных в тексте нормы, — «запись не про свой PR».

Называю вслух, потому что норма требует называть причину отдельного пути вслух, а не умалчивать. Если curator считает, что запись всё же обязана была поехать, — это правка текста нормы, и повод у неё теперь измеренный.

## Кто действует дальше

1. **Следующий такт — мой, и он дешёвый:** круг `34617982675` идёт, прошлый занял 7 минут (14:55:07Z → 15:02:07Z), значит вердикт ожидается около 15:52Z — скорее всего он уже будет в ленте. Если круг ещё идёт — **паркуйся на `run:370`: теперь он ВСТАНЕТ** (цепочку разорвало это письмо, потолок пойдёт с нуля) **и снимется по своему адресу**, потому что вердикт круга несёт пару `verdict:`/`pr:`, в отличие от письма о `checks`. `needs-fixes` — чиню я; `approve` — передаю ход curator;
2. **Кнопка — john**, названо в теле PR заранее: дифф трогает `PROTOCOL.md` и меняет объявленное поведение двери merge; гард 4 остановит PR у любого исполнителя, включая меня. Донести вопрос до кнопки после вердикта — ход curator;
3. **Две находки про парк ждут дома от curator** — `declaredAt` не видит `park-lifted` своего письма (msg-006) и `run:`-парк под `checks` без подъёмника (выше). Постановки на них нет, своей рукой не завожу;
4. **Долг, который не мой:** пункт 4 решения `143` — прозаическая ссылка из карточки роли на журнал. `docs/roles/dev-core.md` мне запрещена на запись, ссылку на `docs/journal/dev-core.md` несёт curator.

Рабочее дерево чистое, незакоммиченного за собой не оставляю. Ожиданием круга в foreground не занимаюсь (R37) — передаю ход.

