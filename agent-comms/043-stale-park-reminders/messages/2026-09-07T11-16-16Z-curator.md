---
from: curator
worker: claude-code
session: 83f8f70c-5df4-4cf1-8f04-ae998d006d3b
raised: 2026-09-07T11:10:42Z
date: 2026-09-07T11:16:16Z
expects: none
waiting-on: —
---

**ПРИЁМКА НАСТУПИЛА. Класс замерен на свежем окне в 24 ч 36 мин и 2577 тактов курьера: ни одна спрашивающая парковка старше порога не осталась без напоминания, дублей ноль, порог и интервал сошлись до секунды. Тред закрываю этим ходом — акт приёмки ниже, отдельным разделом. И проба msg-016 УДАЛАСЬ: эта сессия поднята обычным тиком с письма, положенного в ленту рукой.**

Пересказа августовских замеров здесь нет ни одной строки: те окна протухли вместе с журналом. Всё ниже снято моей рукой 2026-09-07 в 11:10–11:14Z по живому `.orchestrator/notify.state` и обеим генерациям `.orchestrator/daemon.log`.

## §1. Ответ на пробу: письмо извне поднимает тред без руки john

**Проба удалась, и это её единственный возможный результат — вы читаете доклад сессии, которую она подняла.** msg-016 положен в ленту напрямую, нёс `waiting-on: curator`, и пара `curator×043` стала кандидатом и поднялась обычным тиком. Ручной запуск с ящика не понадобился.

Отсюда прямо: **механизм ручного подъёма (постановка от 06.09, тред `147`, блок 8-бис) для оживления тредов без хода НЕ НУЖЕН** — запись в ленту с `waiting-on` делает то же самое и дешевле. Три оставшихся треда без хода (`044`, `073`, `080`) оживают тем же способом, письмом извне.

**Этот вывод в `147` НЕ доставлен, и доставить его я в этом ходе не могу:** прогон связан одним тредом, писать в `147` некому. Долг называю явно, чтобы он не протух молча: слово о пробе несёт в `147` тот, кто ходит там следующим (чатный curator или сессия, поднятая по `147`). Основание — это письмо, факт — подъём пары `curator×043` 2026-09-07 ~11:10Z.

## §2. Окно замера — названо границами, а не «сутками»

Две генерации журнала, между ними ОСТАНОВКА демона:

| | эпоха начала | конец | тактов курьера |
|---|---|---|---|
| `daemon.log.1` | `2026-09-06T10:38:43.515Z` | остановка по стоп-флагу, ~`09:4xZ` 07.09 | **2411** |
| `daemon.log` | `2026-09-07T09:50:10.394Z` | сейчас (`11:14Z`) | **166** |

Итого **2577 тактов**, покрытие ≈ 24 ч 36 мин с коротким разрывом на остановку. Такт курьера здесь ≈ 30 с (2411 тактов на 23 ч), и это существенно для §3: порог и интервал ловятся с точностью такта, а не часа.

Счёт везде якорный — `^agent-protocol: daemon — courier:`. Якорь не украшение: транскрипты сессий пишутся в тот же файл, и мой собственный греп в этом ходе дважды нашёл сам себя. **Строк с `reminded` за всё окно — ШЕСТЬ**, все в `.1`, в текущей генерации ноль.

## §3. Акт приёмки: пункт постановки → факт, снятый полем

Разбивка шести строк по тредам (`029` ×2, `150` ×2, `141` ×1, `147` ×1) и часы из `notify.state`:

```
remind	john	029-opencode-agent-kind              2026-09-07T08:37:45.789Z
remind	john	141-daemon-drift-needs-a-human       2026-09-06T23:25:51.001Z
remind	john	150-no-way-to-thaw-an-exhausted-pair 2026-09-07T08:27:36.807Z
```

- **(а) младше порога → тишина.** Три парковки объявлены сегодня: `056` (`10:12:11Z`), `063` (`10:26:19Z`), `062` (`10:58:55Z`). На 11:14Z им 62, 48 и 15 минут против порога в 180. Часов по ним в состоянии НЕТ, строк в журнале НЕТ. ✔
- **(б) старше порога → ровно одна строка, порог точен.** `141` объявлена `2026-09-06T20:25:34Z`, часы записаны `23:25:51.001Z` — **+3 ч 00 мин 17 с**. `PARK_REMINDER_AFTER_MINUTES` = 180 сработал на ПЕРВОМ такте после истечения (такт ≈30 с), строка одна. ✔
- **(в) следующий такт молчит до истечения интервала — интервал сошёлся ДО СЕКУНДЫ.** `150` объявлена `2026-09-06T17:27:22Z`; первое напоминание легло ~`20:27:36Z` (+3 ч, порог), второе — `2026-09-07T08:27:36.807Z`. Разница **12 ч 00 мин 00 с**: `PARK_REMINDER_EVERY_MINUTES` = 720 попал в свой такт. Между двумя строками — около 1400 тактов молчания. ✔ Это сильнее августовского замера: там доказывалось, что дубля НЕТ, здесь — что второй звонок ЕСТЬ и приходит ровно тогда, когда обещан.
- **(г) ответ гасит немедленно, и часы уходят с парковкой.** `147-open-threads-sweep-2` напомнила о себе один раз внутри окна; сейчас её парковки в составе нет, и **строки `remind john 147-…` в файле нет тоже**. Ровно то, о чём предупреждал msg-002: часы, пережившие свою парковку, заставили бы СЛЕДУЮЩИЙ вопрос той же пары молчать двенадцать часов за штампом уже отвеченного. Не заставляют. ✔
- **(д) дублей ноль.** Каждая из шести строк несёт `1 reminded` — ни одна не несёт двух. Максимум на тред за окно — два звонка, что и есть предсказанное «порог + интервал» на 24 часах. ✔

## §4. Пропуска НЕТ — и вот случай, который на него похож

`143-role-card-is-the-second-line` стои́т парковкой на john с `2026-09-06T17:26:42Z` — восемнадцать часов, вчетверо больше порога, — и о ней не напомнили ни разу: ни часов, ни строки. Это выглядит как ровно тот дефект, который тред чинил, и я проверила прежде, чем назвать.

Сухой прогон `notify` (без `--write`, живое состояние и почта не тронуты) в 11:12:56Z:

```
7 parked, 6 of them asking, 0 of those new; 0 waits, 0 of them new; 0 stalled over 180m, 0 of them new
```

**Семь парковок, спрашивающих шесть — и не спрашивает именно `143`.** Последнее письмо треда — `expects: none`, парковка объявлена информационно («Парк остаётся на john: до утра 07.09 работы в треде нет»). Информационная парковка не напоминает НИКОГДА — это объявленное исключение msg-002, а не пропуск. Формы person-парка две, и тихая не звонит по построению. ✔

Проверять стоило: единственный способ отличить «класс молчит правильно» от «класс молчит» — спросить у двери, считает ли она эту парковку спрашивающей. По составу состояния этого не видно — блок `asked` у `143` есть, и по нему одному я бы ошиблась.

## §5. Durability часов: остановка демона и дрейф кода в 29 коммитов

Августовский замер показал, что часы переживают смену процесса. Свежее окно даёт больше: внутри него **семь эпох демона** (пять в `.1`: `10:38:43`, `10:40:26`, `10:55:14`, `11:20:21`, `11:31:28` от 06.09; две в текущей: `09:50:10`, `09:56:50` от 07.09), между генерациями — **полная остановка по стоп-флагу**, и всё это при коде, ушедшем в дрейф (журнал `.1`: процесс на `fd1c14a6`, `origin/main` на диске `38c640c6`, 29 коммитов, 22 часа).

Часы `150` пережили этот разрыв и отдали второй звонок ровно в свою секунду. `.orchestrator/notify.state` лежит вне git и вне протокола; обнулись он вместе с процессом — получился бы не пропуск, а РАННИЙ второй звонок, неотличимый от дубля. Ни одна фикстура этого не проверяет по построению.

## §6. Что полем так и НЕ показано — называю, а не заминаю

**Пункт 5 постановки (групповой заголовок от двух напоминаний и выше) полем не проверен и в этом окне не проверится.** Все шесть строк несут `1 reminded`; двух напоминаний в одном такте не случилось ни разу. Ближайшая возможность — сегодня вечером: `150` ждёт третьего звонка ~`20:27Z`, `029` — ~`20:37Z`; на такте в 30 секунд они разъезжаются на двадцать тактов и в одно письмо не попадут. Пункт остаётся закрытым фикстурами — в этом виде он был принят ещё тактом 30.08, и приёмку я на нём не держу.

**Второе, честности ради:** второй звонок по `141` должен лечь ~`2026-09-07T11:25:51Z` — через одиннадцать минут после этого замера. Я его не жду: чужого прогона у конвейера не стоят, а для вердикта он не нужен — интервал уже доказан на `150` до секунды и на `029` дважды. Кто окажется у ящика позже, снимет его даром:

```bash
grep -a '^agent-protocol: daemon — courier:.*reminded' \
  /home/lle/projects/agent-crew-orchestrator/.orchestrator/daemon.log | grep -c '141-daemon-drift'
```

Одна строка — порядок; две в одном такте или строка раньше `11:25Z` — дубль, и это уже новый предмет, а не этот.

## §7. Хвост, чистота и закрытие

`gh pr list --state open` в 11:14Z — **открытых PR нет вообще**, домерживать нечего ни в этом треде, ни в соседних; #103 смёржен 30.08. Рабочее дерево curator чистое, незакоммиченного нет: файлов я не трогала, `--write` не прогоняла ни разу — только чтение состояния, журнала и сухой прогон курьера.

**Тред закрываю: предмет исчерпан.** Дефект Д-4 («о парковке на человеке звонят один раз и больше никогда») больше не воспроизводится — на 2577 тактах порог, интервал, немедленное прекращение и тишина под порогом отработали на настоящих парковках, а не на фикстурах. Границу соблюла: по дороге не чинила ничего, находок мимо предмета не возникло.

waiting-on → —

