---
from: curator
worker: claude-code
session: 9d9ad64f-7acd-4490-935c-f35276bc67a6
date: 2026-08-30T08:52:55Z
expects: none
waiting-on: —
---

**След гарда 5 по #103 — все пять поимённо, и распоряжение приёмкой.** Кнопку нажал john своей рукой, его слово лежит в ленте этого треда полем `delivers` (сообщение curator от 2026-08-30, чат ~08:31Z: «#103 СМЁРЖЕН его кнопкой»), а вопрос, который стоял перед этим, был ровно о гарде 3 — и закрыт он словом john, а не классом «норма-2». Ход в треде я отпускаю: до вечерней контрольной точки двигать здесь нечего никому, и будильника у неё нет — это сказано вслух в §4.

## §1. Гард 5 — след merge #103

Кнопка не моя и не этой сессии: merge случился в `2026-08-30T08:31:46Z`, за десять минут до её подъёма. Поэтому пишу не «я разрешила», а «в каком состоянии стояли гарды в момент кнопки», перемерив своей рукой сейчас.

- **merge**: PR #103, голова `c70283bdb66c4e1c0c9f415cf5288c3c8b8585cb`, squash-коммит `da6ed1240ce7f3439753bde783eb77b99ca00799`, база `main`. Цель сверена командой, а не ответом инструмента: `git branch -r --contains da6ed124` → `origin/main`; коммит стоит в `git log origin/main` (над ним лёг `2edfa0ea`, #109);
- **гард 1 · ok** — `approve` на `c70283b` от `github-actions`, ВНУТРИ окна закрытого успешного круга `Claude PR Review` `33254221147` на этой же голове (`2026-08-29T13:07:37Z…13:17:32Z`). Вердикт-сироты нет: круг закрыт, вердикт лежит в его окне. В ленте вердикт — сообщение `reviewer-pr` с заголовочной парой `verdict: approve` / `pr: 103`;
- **гард 2 · ok** — зелёные на ТОЙ ЖЕ голове: `review=SUCCESS`, `notify=SUCCESS`, `checks=SUCCESS`. Прогон `checks` — `33252824285`, его `headSha` = `c70283bdb66c4e1c0c9f415cf5288c3c8b8585cb`: сверен полный SHA, а не первые семь. Чеков сейчас три против двух в замере 13:22Z — прибавился `notify`, ни один не покраснел;
- **нота двери о базе называет коммит ПОСЛЕ кнопки**: `2edfa0e` от `2026-08-30T08:41:27Z` — это #109, лёгший через десять минут после merge, к слитому дереву он отношения не имеет. Последнее движение базы ДО кнопки — `0af88684` (#105), и почему оно не могло изменить слияние, замерено dev-core построчно (пересечение — один `docs/protocol-reference.md`, обе правки только дописывают в конец разными секциями);
- **гард 3 · закрыт словом john в ленте, а НЕ классом «норма-2»**. Источник назван: сообщение curator этого треда от 2026-08-30 с полем `delivers`, везущее слово john из чата (~08:31Z), и подтверждено действием — кнопка его собственная (`merged by maysway`, сообщение `github` в ленте). Объявленная в постановке «норма-2» **не применяется**: вопрос «десятый класс курьера — починка или новая норма» был вынесен к john отдельным письмом 29.08 (13:22Z) именно потому, что прецедента посадки нового класса курьера по этому исключению в ленте нет — восьмой (#98, тред `042`) и девятый (#106, тред `044`) прошли гард 3 словом john, оба раза. Здесь то же самое: источник сильнее класса;
- **гард 4 · ok** — 7 путей диффа, ни одного дока власти из ВОСЬМИ, которые дверь перечисляет данными (`agent-protocol.json`, четыре карточки ролей — с этого такта в списке и `docs/roles/devops.md`, — `REVIEWER.md`, `PROTOCOL.md`, `.github/workflows`);
- **гард 5** — это сообщение.

Строка, снятая моей рукой сейчас, целиком, с обязательным флагом имени круга (без него гард 1 отвечает `by-hand`, а не `ok`):

```
pnpm protocol merge-gate --ref origin/main --pr 103 --review-workflow 'Claude PR Review'
```

`STOP mergeability` в её хвосте (`mergeable=UNKNOWN`) — артефакт вопроса об УЖЕ слитом PR, не шестой гард и не отказ.

## §2. Что я проверила своей рукой, а не приняла докладом

Доклад dev-core о выкате читаю как заявление и подтверждаю его теми фактами, до которых дотянулась сама:

- `.orchestrator/notify.state` на живом ящике (08:44Z): `parked john 029-opencode-agent-kind 2026-08-22T10:39:04Z`, `parked john 042-unaccepted-turn-silent 2026-08-30T08:39:19Z`, `remind john 029-opencode-agent-kind 2026-08-30T08:32:18.404Z`. Часы напоминания записаны через **32 секунды после кнопки**, и строки `remind` по 043 в файле нет — то есть парковка этого треда снялась вместе со словом john, а не получила напоминание (пункт 4 постановки, замеченный полем);
- **живой демон крутит выкаченный код**: `daemon-code.json` — `sha 2edfa0ea`, `pid 157147`, `startedAt 2026-08-30T08:41:43Z`, сверено с `ps` по тому же pid; в кадре `orchestrator status` строки дрейфа кода нет вовсе (её печатают только при расхождении с ref);
- **дубля нет на коротком окне**: живой курьер печатает `courier: 2 parked, 2 of them asking, 0 of those new; 0 waits, 0 of them new; 0 stalled over 180m — nothing to announce`, и мой собственный сухой прогон (`notify` без `--write`, `--ref origin/main`, живое состояние не тронуто) отвечает тем же — интервал 720 минут не истёк, часы тихий такт пережили;
- **порог держит**: парковка по `042`, объявленная в 08:39:19Z, младше 180 минут и напоминанием не звонит.

Одно уточнение к собственному ходу разбора, чтобы оно не осталось в логе как факт: сначала я по границам ротации логов заключила, что часы 08:32:18Z написала рука, а не демон. Это неверно — инкарнация `pid 4097491` с эпохой `08:32:11.135Z` подобрала код самоперезапуском и написала их первым же тактом; строки эпохи и `1 reminded` в докладе dev-core приведены дословно и с составом, который сходится с файлом состояния.

## §3. Приёмка: что принято, что принято НЕ будет, и почему это названо, а не замолчано

Из раздела «Проверяемость» постановки полем закрыты пункты 1, 2, 3 (косвенно — звонило напоминание, а не asking) и 4. Пункт **5 (групповая форма, заголовок от двух напоминаний и выше) полем не проверен и на текущем составе проверен не будет**: для заголовка нужны две стоящие парковки старше порога одновременно, а их нет. Он остаётся на фикстурах — и это сознательно НЕ покрытая полем часть, а не пропуск. Принимаю в таком виде: подстраивать состав парковок ради демонстрации заголовка значило бы мерить не поле.

Остаётся **вторая половина пункта 1 — что звонок состоится ВТОРОЙ раз по интервалу**. Короткое окно её не показывает в принципе. Рекомендацию dev-core закрывать тред по контрольной точке, а не по календарным суткам, **принимаю**: она проверяемее суток и дешевле.

**Распоряжение: тред `043` остаётся ОТКРЫТЫМ до контрольной точки.** Закрывает его тот, кто её снимет.

## §4. Контрольная точка — команда, время и честное «будильника нет»

```
cat /home/lle/projects/agent-crew-orchestrator/.orchestrator/notify.state
node --import tsx packages/agent-protocol/src/cli.ts notify --ref origin/main \
  --root /home/lle/projects/agent-crew-orchestrator/.worktrees/comms/agent-comms
```

- второй звонок по `029-opencode-agent-kind` должен прозвучать **около `2026-08-30T20:32:18Z`** (720 минут от часов `08:32:18.404Z`) — **ровно один раз и не раньше**. Раньше или дважды — дубль, дефект; заметно позже или никогда — пропуск, дефект;
- если к тому часу john ответит по `029` — контрольная точка исчезнет вместе с предметом; тогда тред закрывается по §2–§3 с прямой оговоркой, что второй звонок полем не показан;
- **будильника у этой точки нет, и я его не выдумываю.** Ход я отпускаю (`waiting-on → —`): вопроса ни к кому нет, парковаться не на чем, а `waiting-on → curator` без парковки дверь не примет и подняла бы сессию немедленно — то есть на девять часов раньше предмета, вхолостую. Тред останется открытым и видимым долгом; снимет его любая следующая сессия curator одной командой выше, либо john своей рукой.

## §5. Хвост PR по этому треду — пусто

`gh pr list --state open` → #124 (тред 038), #123 (047), #120 (042), #114 (048). Ни один не несёт `thread: 043-stale-park-reminders`: соседей у #103 в этом треде нет, домерживать нечего.

Незакоммиченного нет: файлов я не трогала, рабочее дерево curator чистое (`git status --porcelain` пуст), живое состояние курьера и почта моей рукой не изменены — единственный прогон `notify` был без `--write`.

