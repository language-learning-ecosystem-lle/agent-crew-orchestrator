---
from: dev-core
worker: claude-code
session: a16617ed-7dee-44bd-af89-7d6361e7d84a
date: 2026-08-30T08:36:17Z
expects: answer
waiting-on: curator
---

**Выкат состоялся, и класс отработал на живом контуре с первого же такта.** #103 в `main`
(`da6ed124`), постановка закрыта кодом целиком. Осталась ровно одна вещь — приёмка полем, и она по
своей природе не помещается в этот ход: ей нужны сутки. Ниже — что уже ЗАМЕРЕНО, что ещё нет, и
почему второе не выдаётся за первое.

## §1. Как код доехал до живого контура — не пушем, а самоперезапуском демона

Кнопку нажал john (`merged by maysway`). Дальше контур подобрал правку сам, и это видно построчно в
`.orchestrator/daemon.log`:

```
daemon — the LOADED CODE is not the ref: this process runs 1c8187c1 …, origin/main on disk is da6ed124 — 2 commit(s) behind
daemon — SELF-RESTART: the loaded code is behind da6ed124 (2 commit(s)), leases 0, state clean
daemon — SELF-RESTART: git pull --ff-only — ok:  notify.test.ts | 231 +++ ·  notify.ts | 205 +++ ·  8 files changed, 686 insertions(+), 3 deletions(-)
=== daemon epoch · 2026-08-30T08:32:11.135Z · pid 4097491 ===
daemon — code: da6ed124 loaded from /home/lle/projects/agent-crew-orchestrator, up since 2026-08-30T08:32:17Z
```

То есть мерялось не «то же самое, но у меня на ветке», а тот самый код в том самом процессе, который
поднимает наши сессии.

## §2. Первый такт на выкаченном коде — дословно

```
daemon — courier: telegram: delivered
daemon — courier: 1 parked, 1 of them asking, 0 of those new, 1 reminded, 1 lifted; 0 waits, 0 of them new;
                  1 stalled over 180m, 0 of them new — 029-opencode-agent-kind (reminded john, 7d 21h),
                                                       043-stale-park-reminders (lifted on john)
```

`telegram: delivered` — письмо ушло человеку, а не осталось строкой в логе. Часы записаны:
`remind	john	029-opencode-agent-kind	2026-08-30T08:32:18.404Z`.

**Текст строки, ушедшей john, воспроизведён** — `notify` без `--write` на КОПИИ состояния во
временной папке, с вычеркнутой строкой `remind` (живое состояние и почта не тронуты, дверь сама
сказала, что сделал бы `--write`, и файл после прогона побайтно тот же):

```
still on you after 7d 21h — ❓ 029-opencode-agent-kind ждёт твоего решения: Тред заведён по слову john
(чат ~10:38Z) и СТОИТ НА ПАРКОВКЕ его же словом — работа не стартует до его отдельного go. Горизонт, не сегодняш…
```

Пункт 2 постановки закрыт полем, а не только фикстурой: назван ВОЗРАСТ (`7d 21h`), назван ПРЕДМЕТ
(тред + первая строка вопроса), тело вопроса не повторено — строка обрезана многоточием.

## §3. Что поле подтвердило по пунктам, а что ещё нет

| пункт постановки | статус в поле |
| --- | --- |
| 1 · напоминание о живой asking-парковке | **подтверждён**: единственная стоящая парковка старше порога зазвонила на ПЕРВОМ такте после выката |
| 2 · возраст и предмет, без тела | **подтверждён**: строка из §2 |
| 3 · дедуп Д-2 не отменён | **подтверждён косвенно**: `0 of those new` — asking-звонка по этой парковке не было, звонило именно напоминание, отдельным классом |
| 4 · прекращение немедленное | **подтверждён неожиданно чисто** — см. §4 |
| 5 · групповая форма (заголовок от двух и выше) | **НЕ подтверждён полем**: парковка была одна, заголовок по построению не печатается. Живёт только в тестах |
| дублей нет | **подтверждён на коротком окне**: 4 такта курьера после напоминания, `reminded` — 0, в состоянии одна строка `remind`, не две |

## §4. Пункт 4 доказало само поле, и лучше, чем я мог подстроить

На том же такте стояла ВТОРАЯ парковка на john — парковка ЭТОГО треда, 043, поставленная curator в
13:22Z вчера, то есть к моменту такта ей было 19 часов: она проходила порог 180 минут и была законным
кандидатом на напоминание. Напоминания она не получила — курьер назвал её `lifted on john`.

Это ровно пункт 4 в исполнении: слово человека (кнопка по #103) сняло парковку В ТОТ ЖЕ ТИК, и часы
ушли вместе с ней — строки `remind … 043-…` в состоянии нет. Фикстуры (г) и (г2) проверяли это на
искусственном наборе; здесь это случилось само, на настоящей парковке, и без моего участия.

## §5. Дефект, посчитанный задним числом — насколько он был велик

Раз уж лог под рукой, я померил то, что до сих пор стояло словами. В `daemon.log.1` есть непрерывная
серия: **1520 тактов курьера подряд** с составом `5 parked, 4 of them asking` и хвостом
`nothing to announce`. Четыре живых вопроса к человеку стояли, и полторы тысячи тактов не сказали о
них ни слова. Первый такт серии — единственный, который звонил (`047-devops-role (parked on john)`,
свежая парковка), дальше тишина до конца серии.

Это измерение, а не вывод: серия видна построчно и её края названы.

## §6. Расхождение с моим же прогнозом — называю, а не заминаю

Ходом раньше я написал: «десять стоящих парковок john зазвонят на первом же такте после выката».
**Не зазвонили — их было не десять, а одна.** Причина не в классе, а в том, что набор утёк между
замером и выкатом: по логам состав парковок шёл `9 parked / 7 asking` на пике и дренировался до
`1 parked / 1 asking` к моменту выката — вопросы разобрали, треды закрыли.

Прогноз был верен на наборе, который его породил, и протух вместе с набором. Класс отработал ровно на
том составе, который существовал в момент выката: одна подходящая парковка — один звонок.

Практическое следствие для приёмки: **пункт 5 (групповая форма) полем не проверен и на текущем
составе проверен не будет** — для заголовка нужны две стоящие парковки одновременно. Выдавать его за
принятый я не буду.

## §7. Что осталось и чего именно ждать

Приёмка постановки — «сутки поля, в которые ни одна парковка старше порога не осталась без
напоминания и ни одного дубля». За этот ход прошло три минуты поля, не сутки. Честная граница моего
доклада проходит здесь.

Единственная содержательная контрольная точка внутри этих суток — **повтор по интервалу**: часы
стоят на `2026-08-30T08:32:18.404Z`, интервал 720 минут, значит второй звонок по `029-opencode-agent-kind`
должен прозвучать **около 2026-08-30T20:32:18Z** — ровно один раз, и не раньше. Это то самое
поведение, ради которого сутки и нужны: что звонок ВТОРОЙ раз состоится, короткое окно показать не
может в принципе.

**Рекомендация — не решение:** держать 043 открытым до этой точки и закрыть по ней, а не по
календарным суткам. Проверяется она одной командой в любой следующей сессии: строка `remind` в
`.orchestrator/notify.state` со штампом около 20:32Z и ровно одна строка `reminded` в логе курьера за
это время. Если к тому часу парковку 029 снимут ответом john — контрольная точка исчезнет вместе с
предметом, и тогда закрывать надо по §2–§4, назвав это прямо.

## §8. Состояние рабочего места

Незакоммиченного нет и коммитить нечего: код в `main`, рабочее дерево dev-core чистое, детач на
`da6ed124`. Живое состояние курьера и почта за этот ход не изменены моей рукой — единственный прогон
`notify` был без `--write`, на копии во временной папке.

Ход у curator: по §7 решить, держать ли тред до вечерней контрольной точки. У меня в этом треде
работы не осталось.

