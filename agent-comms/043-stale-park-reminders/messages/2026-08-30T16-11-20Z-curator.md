---
from: curator
worker: claude-code
session: 506ba962-3350-491e-80af-f4d24e1a6286
date: 2026-08-30T16:11:20Z
expects: none
waiting-on: —
---

**Промежуточный замер поля моей рукой, 16:08–16:10Z. Дубля по-прежнему нет — 858 тактов курьера. Но рецепт контрольной точки из §4 предыдущего сообщения СТАЛ НЕЧИТАЕМЫМ ПО СЧЁТУ, и это надо поправить до вечера, иначе следующая сессия снимет приёмку неверно.** Заодно два пункта проверяемости постановки, до сих пор закрытые только фикстурами, закрылись полем — на настоящей парковке, не на замере.

## §1. Рецепт §4 больше нельзя читать числом строк — измерено

Ожидание, записанное вчерашним ходом, звучало так: «сейчас команда отдаёт ровно одну строку, на контрольной точке ждём **две**». **Две строки уже есть — и вторая не про `029`:**

```
1  … 1 reminded, 1 lifted; … — 029-opencode-agent-kind (reminded john, 7d 21h), 043-stale-park-reminders (lifted on john)
2  … 5 parked, 5 of them asking, 0 of those new, 1 reminded; … — 045-ship-signal-fixes-to-lle (reminded john, 3h)
```

Строка 2 — ПЕРВОЕ напоминание по другому треду, а не второй звонок по `029`. Тот, кто вечером сверит рецепт по числу, увидит две строки до срока и прочтёт это как «второй звонок пришёл» (а после 17:44Z, см. §4, — три). Ошибка не в классе, а в форме приёмки: рецепт писался на составе из одной живой парковки, а состав подвижен — ровно то свойство, из-за которого форвардное обещание в доках уже переписывалось с числа на свойство.

**Исправленное чтение контрольной точки — по имени треда, а не по счёту:**

```bash
cd /home/lle/projects/agent-crew-orchestrator
grep -h '^agent-protocol: daemon — courier:.*reminded' .orchestrator/daemon.log.1 .orchestrator/daemon.log \
  | grep -c '029-opencode-agent-kind (reminded'      # ждём 2 — было 1, стало 2
grep '^remind.*029-opencode-agent-kind' .orchestrator/notify.state   # штамп уехал с 08:32:18.404Z на ~20:32Z
```

Ровно два раза по `029` и штамп, уехавший один раз, — класс держится. Три раза, или штамп, уехавший раньше 20:32Z, — дубль. Ни одного, и штамп прежний, — пропуск. Обе половины по-прежнему нужны обе (журнал говорит СКОЛЬКО, состояние — КОГДА), меняется только фильтр: по треду.

## §2. Дубля нет на окне в 858 тактов, и часы пережили ВТОРУЮ смену процесса

`.orchestrator/notify.state` в 16:10:10Z, дословно и целиком:

```
parked	john	029-opencode-agent-kind	2026-08-22T10:39:04Z
parked	john	047-devops-role	2026-08-30T14:44:09Z
remind	john	029-opencode-agent-kind	2026-08-30T08:32:18.404Z
```

Строка `remind` одна, штамп **исходный** — `08:32:18.404Z`, за семь с половиной часов не переписалась. Собственный счёт по строкам самого демона (якорь `^agent-protocol: daemon — courier:`, транскрипты сессий отсечены): **858 тактов курьера после такта напоминания, из них с `reminded` по `029` — 0.** Прошлый замер стоял на 91 такте, теперь окно почти вдесятеро шире.

Демон за это время перезапустился ещё раз: `daemon-code.json` → `sha 00507787`, `startedAt 2026-08-30T15:49:02Z`, `pid 869773` (прошлый замер называл эпоху 08:41:38Z / pid 157147). То есть durability часов подтверждена уже на ДВУХ сменах инкарнации со сменой кода, а не на одной.

## §3. Полем закрылись пункты (б) и (г) проверяемости — на настоящей парковке

До сих пор порог и немедленное прекращение проверялись только фикстурами; здесь их отработал живой ящик, и обе половины видны в фактах:

- **(б) старше порога → ровно одна строка, возраст назван.** `045-ship-signal-fixes-to-lle` объявлена парковкой в `09:00:12Z`, напомнила о себе один раз — `(reminded john, 3h)`. Порог `PARK_REMINDER_AFTER_MINUTES` = 180 минут сработал на настоящей парковке, возраст в строке назван, звонок ровно один: `grep -c` по этому имени даёт `1 reminded` и `1 lifted`, больше ничего;
- **(г) ответ гасит немедленно.** Тред `045` закрыт, парковка снята (`045-ship-signal-fixes-to-lle (lifted on john)`) — и **часов по ней в `notify.state` не осталось вовсе**: строки `remind john 045-…` в файле нет. Это ровно то, о чём предупреждал msg-002: не урони часы вместе с парковкой — и следующий вопрос той же пары молчал бы двенадцать часов за штампом уже отвеченного. Полем показано, что не молчит.

Ни один из этих двух фактов не взят из доклада исполнителя — оба сняты командами по живому состоянию и журналу.

## §4. Дешёвая контрольная точка РАНЬШЕ вечерней: ~17:44Z

В составе появилась новая парковка: `047-devops-role` на john с `2026-08-30T14:44:09Z`. Порог в 180 минут истекает около **17:44Z** — то есть первое напоминание по ней должно прозвучать за три часа до вечерней точки и внутри окна почти любой следующей сессии. Это проверка того же пункта (б), но на парковке, часов по которой ещё нет вовсе:

```bash
grep -h '^agent-protocol: daemon — courier:.*reminded' .orchestrator/daemon.log.1 .orchestrator/daemon.log | grep '047-devops-role'
```

Ровно одна строка после ~17:44Z с возрастом ~`3h` — порог держится. Ни одной к 18:30Z — пропуск, дефект. Две — дубль. Вечерняя точка по `029` (интервал 720 минут) при этом остаётся: это разные половины класса, порог и интервал, и одна другую не заменяет.

## §5. Ротация журнала: рецепт переживёт, но строки СМЕНЯТ файл — сказано, чтобы не искали не там

Замер, а не оценка: `daemon.log` — **8 661 265 байт**, то есть кэп `DEFAULT_LOG_MAX_BYTES` (8 МБ) уже перейдён; `daemon.log.1` — 14 500 920 байт, последняя запись в него `07:55:33Z`. **Обе строки `reminded` сейчас лежат в ТЕКУЩЕМ `daemon.log`** (в `.1` их ноль — проверено пофайлово), а не там, где их можно было бы ожидать по возрасту.

Отсюда прямо: ближайший старт демона ротирует (кэп смотрят на старте, и он уже перейдён) — обе строки уедут в `daemon.log.1`, а вечерняя ляжет в новый `daemon.log`. Рецепт грепает обе генерации, поэтому он это переживает. **Второй ротации до контрольной точки быть не может:** нынешний `daemon.log` набрал 8,66 МБ с `07:55Z` по `16:10Z` — 8,25 часа, ≈1,05 МБ/ч; чтобы новый файл снова перевалил 8 МБ, нужно ~7,6 часа против 4,4 оставшихся, а после ротации он стартует с нуля. Строка 08:32Z вдобавок процитирована в ленте дословно и от журнала уже не зависит.

## §6. Хвост и чистота

`gh pr list --state open` (16:09Z): #153 (тред 058), #152 (042), #148 (057), #143 (055), #142 (054), #134 (052), #114 (048). **Ни один не несёт `thread: 043-stale-park-reminders` — домерживать в этом треде нечего**, #103 смёржен и соседей у него нет. Метка `review` висит на #114 — это чужой тред 048, и след моей кнопки ушёл бы не в тот тред (гард 5), поэтому его я не трогаю.

Файлов я не трогала вовсе: рабочее дерево curator чистое, живое состояние курьера и почта моей рукой не изменены — ни одного прогона с `--write`, только чтение журнала и `notify.state`. Незакоммиченного нет.

**Ход отпускаю.** Вопроса ни к кому нет, парковаться не на чем, будильника ни у 17:44Z, ни у 20:32Z не существует — и я его не выдумываю: `waiting-on → curator` без парковки дверь не примет, а подъём вхолостую за час до предмета стоил бы такта. Тред остаётся ОТКРЫТЫМ и видимым долгом; снимет его любая следующая сессия curator командами из §1 и §4 — либо john своей рукой, ответив по `029`, и тогда вечерняя точка исчезает вместе с предметом.

waiting-on → —

