---
from: curator
worker: claude-code
session: 847433ce-c804-42b0-99bd-a5a9994efcff
date: 2026-08-30T09:26:26Z
expects: none
waiting-on: —
---

**Слово john по гарду 3 принято и записано; контрольный замер снят моей рукой — за 91 такт курьера после напоминания дубля нет, и часы пережили полный перезапуск демона.** Тред остаётся ОТКРЫТЫМ до вечерней контрольной точки (~`2026-08-30T20:32:18Z`), ход я снова отпускаю: вопроса ни к кому нет, будильника у точки нет, и я его не выдумываю.

## §1. Что слово john меняет, а что НЕ меняет

Слово («САМИ», чат ~09:16Z, привезено сообщением curator этого треда полем `delivers`) пришло ПОСЛЕ кнопки: #103 смёржен рукой john в `2026-08-30T08:31:46Z`, а класс он назвал починкой сорока пятью минутами позже.

- **След гарда 5 переписывать не нужно, и я его не переписываю.** Он закрыл гард 3 словом john в ленте (сообщение с `delivers`, чат ~08:31Z), а не классом «норма-2». Новое слово — того же человека и о том же предмете, только точнее: оно называет КЛАСС, а прежнее называло только исход. Источник не сменился, поэтому в следе менять нечего; append-only тут и не нарушается, и не требуется.
- **Прецедент теперь в ленте, но граница НЕ ослаблена — фиксирую это здесь явно, чтобы msg о слове john не читался следующей сессией как общее послабление.** Правило «новые поводы дёргать человека утверждает john» в силе; решение точечное — на десятый класс и на #103. Следующий класс курьера, если исполнитель не уверен, по-прежнему приносится вопросом. Ровно та же формулировка, что была дана вчера по девятому (`044`), и это и есть довод john: на одинаковые вопросы ответ обязан быть один.
- Практический вывод для будущих тактов: **посадка нового класса курьера по исключению «норма-2» без слова john прецедента по-прежнему НЕ имеет.** Восьмой (#98, `042`), девятый (#106, `044`) и десятый (#103, этот тред) прошли гард 3 словом john — все три раза.

## §2. Контрольный замер поля, 09:22–09:27Z, моей рукой

`.orchestrator/notify.state` живого ящика:

```
parked	john	029-opencode-agent-kind	2026-08-22T10:39:04Z
parked	john	045-ship-signal-fixes-to-lle	2026-08-30T09:00:12Z
parked	john	042-unaccepted-turn-silent	2026-08-30T08:39:19Z
remind	john	029-opencode-agent-kind	2026-08-30T08:32:18.404Z
```

Строка `remind` — ОДНА и с исходным штампом 08:32:18.404Z: за час она не переписалась.

Сухой прогон (`notify` без `--write`, `--ref origin/main`, живое состояние и почта не тронуты — дверь сама сказала, что сделал бы `--write`):

```
2 parked, 2 of them asking, 0 of those new, 1 restated, 1 lifted; 0 waits, 0 of them new;
0 stalled over 180m, 0 of them new
```

Две стоящие asking-парковки — `029` (её напоминание ждёт интервала) и `045` (объявлена в 09:00, звучит как `restated`); `042` названа `lifted`. О `029` курьер молчит — интервал 720 минут не истёк. Это пункт (в) проверяемости постановки, снятый полем: часы переживают тихий такт.

Собственный счёт по журналу, только по строкам самого демона (`^agent-protocol: daemon — courier:`, транскрипты сессий отсечены якорем):

- **91 такт курьера после такта напоминания, из них с `reminded` — 0.** Прошлый доклад dev-core стоял на окне в 4 такта; теперь окно в двадцать раз шире и по-прежнему чистое.

## §3. Факт, которого в треде ещё не было: часы пережили ПЕРЕЗАПУСК демона

Между напоминанием и этим замером демон полностью перезапустился: эпоха `2026-08-30T08:41:38.005Z · pid 157147 · foreground`, `daemon-code.json` — `sha 2edfa0ea`, `startedAt 08:41:43Z`. То есть инкарнация, написавшая часы (`pid 4097491`, эпоха 08:32:11Z), больше не живёт.

**Почему это не мелочь.** `.orchestrator/notify.state` лежит вне git и вне протокола — это машинная память ящика. Часы, обнулившиеся вместе с процессом, дали бы не «пропуск», а РАННИЙ второй звонок, неотличимый от дубля: новая инкарнация прочитала бы файл как «никому не напоминали». Замерено: файл пережил смену процесса со сменой кода, штамп прежний, дубля на 91 такте нет. Фикстура round-trip проверяла форму файла; поле проверило его durability между двумя процессами — этого ни одна фикстура не проверяет по построению.

## §4. Контрольная точка: рецепт, проверенный сейчас, и честная граница его доказательности

Ожидание: около `2026-08-30T20:32:18Z` (720 минут от часов) — **ровно один** второй звонок по `029-opencode-agent-kind`. Раньше или дважды — дубль, дефект; заметно позже или никогда — пропуск, дефект.

Команда (прогнана мной сейчас, вывод ниже — её настоящий, а не предполагаемый):

```bash
cd /home/lle/projects/agent-crew-orchestrator
grep -h '^agent-protocol: daemon — courier:.*reminded' .orchestrator/daemon.log.1 .orchestrator/daemon.log
grep '^remind' .orchestrator/notify.state
```

Сейчас она отдаёт ровно одну строку — ту самую, первую:

```
agent-protocol: daemon — courier: 1 parked, 1 of them asking, 0 of those new, 1 reminded, 1 lifted;
0 waits, 0 of them new; 1 stalled over 180m, 0 of them new — 029-opencode-agent-kind (reminded john, 7d 21h),
043-stale-park-reminders (lifted on john)
```

На контрольной точке ждём **две** строки и штамп `remind`, уехавший на ~20:32Z. Обе половины нужны обе: штамп в состоянии говорит КОГДА (строки демона времени не несут), а журнал говорит СКОЛЬКО РАЗ — состояние этого сказать не может, у него одна строка на пару «человек×тред», и дубль её просто перезаписал бы.

**Ротация журнала рецепту не угрожает, и это замерено по коду, а не по объёму файлов.** `logsize.ts`: ротация происходит ТОЛЬКО ПРИ СТАРТЕ демона и только если файл уже перевалил 8 МБ (`DEFAULT_LOG_MAX_BYTES`), одна генерация. Сейчас `daemon.log` — 1.3 МБ, `daemon.log.1` — 14.5 МБ (он и вырос сверх кэпа на ходу: кэп смотрят на старте, а не в процессе письма). Чтобы строка 08:32 пропала, нужны ДВА старта, каждому из которых предшествует восьмимегабайтный файл; ожидаемый до вечера самоперезапуск (корень стоит на `2edfa0ea`, `origin/main` уже `04777240` — один коммит, доки треда 042) даст в худшем случае ОДИН, а рецепт грепает обе генерации. Первая строка вдобавок процитирована выше дословно — тред несёт её независимо от журнала.

Если к вечеру john ответит по `029` — точка исчезает вместе с предметом, и тред закрывается по уже снятому, с прямой оговоркой, что второй звонок полем не показан. Пункт 5 постановки (групповой заголовок от двух напоминаний) полем не проверен и на текущем составе проверен не будет — принято в этом виде ещё прошлым тактом.

## §5. Хвост и чистота

`gh pr list --state open` (09:27Z): #125 (тред 036), #124 (038), #123 (047), #114 (048). Ни один не несёт `thread: 043-stale-park-reminders` — домерживать в этом треде нечего. Соседей у #103 нет, и этот ход к чужим тредам не привязан.

Незакоммиченного нет: файлов я не трогала вовсе, рабочее дерево curator чистое, живое состояние курьера и почта моей рукой не изменены — единственный прогон `notify` был без `--write`.

**Ход отпускаю.** Парковаться не на чем (вопроса нет), `waiting-on → curator` без парковки дверь не примет и подняла бы сессию немедленно — на одиннадцать часов раньше предмета, вхолостую. Тред остаётся открытым и видимым долгом; снимет его любая следующая сессия curator командой из §4, либо john своей рукой.

waiting-on → —

