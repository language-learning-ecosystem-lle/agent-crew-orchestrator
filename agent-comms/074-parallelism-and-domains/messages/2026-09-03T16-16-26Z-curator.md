---
from: curator
worker: claude-ai
date: 2026-09-03T16:16:26Z
expects: none
waiting-on: curator
delivers: john
priority: normal
merged-pr: 202
---

**Слово john полем `delivers` (чат ~16:16Z): #202 СМЁРЖЕН его кнопкой. Ступень 2 стои́т — исключительный ресурс, уехавший в базе, краснит прогон по имени файла.**

Список исключительных путей короткий и выведен из замера, а не из осторожности: `agent-protocol.json`, `schema/version.ts`, `schema/migrate.ts`. Остальное добавляется, когда столкнётся.

**Находка диффа, которая сама по себе стоит того, чтобы жить дальше:** на событии `pull_request` чекаут — это merge-ref, УЖЕ слитый с базой, поэтому наивная проверка «в базе уехало» была бы **пустой ВСЕГДА** и шаг зеленел бы молча. Роль это поймала и тянет голову PR явно (`refs/pull/<N>/head`). Класс тот же, что весь день: зелёный по построению читается как проверенный.

**Границы записаны в самом PR и не смягчаются:** ступень 2 НЕ видит два открытых PR, взявших один номер, пока ни один не влит (в базе ещё ничего не уехало) — это закрывает ступень 1, влитая вчера; ступень 3 (настройка «ветка свежая относительно `main`») по решению john держится в резерве.

## Замечание john по кнопке — и оно уточняет границу власти

Он спросил, почему жмёт он: «из-за CI или из-за `protocol-reference.md`». **Из-за CI**: `.github/workflows/**` объявлен доком власти целиком, а справочник им НЕ является (решение john 28.07) — то есть дифф только из справочника кнопки бы не потребовал.

Он же назвал и перекос: в списке доков власти стои́т **весь каталог воркфлоу**, поэтому кнопка нужна и на промпт ревьюера (где решается, что агенту можно), и на «добавить шаг, который просто краснеет». Разумная граница, предложенная в разговоре: **док власти — то, что даёт или ограничивает права**; шаг, который ничего никому не разрешает, — обычный код.

**Ревизию заводить john отказался, и правильно** — она уже заказана в контуре-потребителе (их тред `126`), и в её постановке прямо сказано проверить `.github/workflows`: каждый ли файл там что-то ограничивает. Второй дом предмету не нужен.

waiting-on → curator.
