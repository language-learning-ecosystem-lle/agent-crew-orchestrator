---
from: curator
worker: claude-code
session: a338978e-03db-45ae-9643-30cbcf428ad4
raised: 2026-09-03T23:15:34Z
date: 2026-09-03T23:18:10Z
expects: none
waiting-on: —
---

**ТРЕД `074` ЗАКРЫТ — приёмка ЦЕНОЙ, слово john доставлено письмом выше. Закрытие не заявлено, а ЗАМЕРЕНО: `thread status --status closed --write` в 23:17:00Z, контрольное чтение ленты в 23:17:05Z печатает `status: closed`. Ход не передаётся никому: `waiting-on → —`.**

Все факты ниже сняты в ЭТОМ ходу (23:17Z), а не унаследованы из прошлых писем.

## 1. Приёмка, перечисленная фактами — форма ЦЕНОЙ

Критерий, принятый john: **«ни одного случая, стоившего пропущенного круга ревью или отката чужой починки»**. Сходится, и вот чем именно:

- **единственный наблюдённый пятый случай класса 2** (#167 против уехавшего #211, msg-025 §3) **стоил нуля**: трёхсторонний слив удержал чужую починку, проверено на влитом дереве (msg-032 §4). Ни пропущенного круга ревью, ни отката;
- **ступень 1 — в `main`** (#203);
- **ступень 2 — в `main`**: #202, squash-коммит `09b89435c4286232f882b738123792fe2b2a67a9`, кнопка john. Перемерено сейчас: `state=MERGED`, `mergedAt=2026-09-03T16:15:52Z`, `baseRefName=main`, и `merge-base --is-ancestor 09b89435 origin/main` → **предок**, то есть коммит стои́т в линии, а не «где-то есть»;
- **искусственная приёмка msg-006 §4 закрыта на ВЛИТОМ артефакте**, не на фикстуре: четыре класса, коды выхода **1 / 0 / 0 / 2** — красный по имени файла, зелёный после подтягивания, «не трогает — не спрашиваю», «нет общего предка» (msg-042 §2);
- **стык на раннере измерен даром**: шаг стои́т седьмым в живом прогоне `33785441211`, обе `fetch`-строки прошли, скрипт нашёлся по пути, напечатал ИЗМЕРЕННУЮ ветку, а не молчание (msg-042 §3);
- **ступень 3** — резервная и незажжённая, цена названа; **доменные зоны** — отклонены john по числу и не возвращаются.

**Вторая проверка приёмки — дверью, а не глазом:** дверь закрытия отказывает треду, в котором объявлена незакрытая задача («an acceptance with undone work declared»). Она **не отказала** ни в сухом прогоне, ни при `--write` — то есть недоделанной объявленной работы на `074` нет машинно, а не по моей памяти.

## 2. Хвост открытых PR — прогнан, молчания не оставляю

`gh pr list --state open` в 23:17Z, шесть штук: **#260, #259, #256, #245, #243, #240**. Прочитала описание каждого — строка `thread:` называет `097`, `119`, `062`, `064`, `064`, `090`. **PR треда `074` среди открытых нет ни одного**: единственный, #202, влит. Мёржить в этом ходу нечего — не «не дошли руки», а нет кандидата.

## 3. Что уносится в другие дома, а не умирает с закрытием

- **первая полевая цифра о ложных тревогах ступени 2** (1 из 1 срабатывания пришлось бы на случай, который трёхсторонний слив разрешает сам) — дом `093-registry-tail-race`, там же класс хвоста общего реестра. Останется такой на десятке случаев — разговор о смягчении шага обопрётся на число, а не на ощущение;
- **находка метода** («одинаковое дерево во всех порядках доказывает бесконфликтность, а не целость») и **гонка за вершиной `docs/protocol-reference.md`** — тот же дом `093`;
- **красная половина шага НА РАННЕРЕ** не наблюдалась и наблюдаться будет, когда придёт: нужен PR, тронувший исключительный ресурс, уехавший в базе. **Это поле, а не долг, и дома ему не заводится** — сказано вслух, чтобы не всплыло потом как «забытый пункт»;
- **перекос списка доков власти** (весь каталог `.github/workflows` требует кнопки и на промпт ревьюера, и на шаг, который просто краснеет) — ревизию john заводить отказался: она уже заказана в контуре-потребителе, второй дом предмету не нужен. Здесь предмет **умер вслух**, а не переехал.

## 4. Состояние

- **незакоммиченного нет:** `git status --porcelain` пуст; в код, PR, метки и чужие треды в этом ходу не написано ни строки. Записей хода ровно две — `_meta.md` дверью закрытия и это письмо; тело письма писано в своём `mktemp -d`, временных ссылок в общем складе `refs/` не заводила;
- **парк снят** — и `delivers: john` предыдущего письма, и самим `status: closed`;
- **ход не передаётся:** `waiting-on → —`. Тред закрыт, поднимать по нему некого и незачем.

waiting-on → —.

