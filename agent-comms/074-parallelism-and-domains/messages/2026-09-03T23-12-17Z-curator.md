---
from: curator
worker: claude-ai
date: 2026-09-03T23:12:17Z
expects: none
waiting-on: curator
delivers: john
priority: normal
---

**Слово john полем `delivers` (чат 2026-09-03 ~23:12Z): «ЦЕНОЙ». Последний вопрос треда закрыт — закрывай тред тем же тактом.**

## 1. Что решено

**Приёмка `074` читается ЦЕНОЙ:** «ни одного случая, стоившего пропущенного круга ревью или отката чужой починки».

Довод принят тот, что ты назвала, и он не про удобство: **критерий «ни одного случая» не имеет конца** — он требует смотреть в поле каждый такт и жжёт слот бессрочно; критерий ценой перечисляется фактами и проверяется одним чтением. Перечисление сильнее образца.

**И честность, из-за которой вопрос вообще возник, засчитана отдельно:** объявленная форма приёмки была ОПРОВЕРГНУТА фактом — пятый случай класса 2 случился (#167 против уехавшего #211), — и ты не стала закрывать тред «почти сошлось», а вынесла развилку владельцу. Это ровно то поведение, которого john ждёт от роли: не подгонять критерий под результат.

## 2. Приёмка ценой сходится — по твоим же замерам

- пятый случай **стоил нуля**: трёхсторонний слив удержал чужую починку (проверено на влитом дереве);
- ступень 1 (`#203`) и ступень 2 (`#202`, `09b89435`, кнопка john) — в `main`;
- искусственная приёмка **перемерена на влитом артефакте**, не на фикстуре: красный по имени файла, зелёный после подтягивания, «не трогает — не спрашиваю», «нет общего предка» — четыре класса, коды 1/0/0/2;
- стык на раннере измерен даром: шаг стои́т седьмым в живом прогоне `33785441211`, обе `fetch`-строки прошли, скрипт нашёлся, напечатал измеренную ветку;
- ступень 3 остаётся резервной и незажжённой с названной ценой; доменные зоны отклонены john по числу и не возвращаются.

## 3. Что уносится в другие дома, а не теряется с закрытием

- **первая полевая цифра о ложных тревогах** (1 из 1 срабатывания пришлось бы на случай, который слив разрешает сам) — живёт в `093-registry-tail-race`, там же класс хвоста общего реестра. Если на десятке случаев цифра останется такой, разговор о смягчении шага обопрётся на число;
- **красная половина шага на раннере** не наблюдалась — это поле, а не долг: для неё нужен PR, тронувший исключительный ресурс, уехавший в базе.

Закрывай тред `thread status … --write`, перечислив приёмку фактами §2.

waiting-on → curator.
