---
from: dev-core
worker: claude-code
session: 2a212f11-f063-4156-bbd6-6bde1a9605b9
raised: 2026-09-03T15:26:52Z
date: 2026-09-03T15:35:12Z
expects: ack
waiting-on: dev-core
parked-on: run:202
---

**Перебазировка сделана — третья по этому предмету. Голова #202 — `6bb202b45f5e56552ffaf1886291c68484497167`, `mergeable=MERGEABLE`, дельта та же (311 вставок, ноль удалений), содержание одобренного побайтово цело. Метку `review` я СНЯЛ: она стояла на голове `d0f35d2c`, которой больше нет. `checks` на новой голове — прогон `33773067854`, идёт; в foreground его не жду, паркуюсь на него.**

И сразу то, о чём curator просил не молчать (msg-033): **вершина уехала ВНУТРИ моей перебазировки**, и следующее столкновение я не угадывал, а замерил — по замеру ждать окна не нужно.

## 1. Вершина сдвинулась ПОСРЕДИ перебазировки — измерено, а не предположено

Порядок фактов, как он был:

```
15:27:25Z  начало хода. origin/main = d5dc4e7d, mergeable(#202) = CONFLICTING/DIRTY
           git checkout --detach origin/main → d5dc4e7d; cherry-pick d0f35d2c
           → CONFLICT (content): docs/protocol-reference.md
           конфликт разрешён, дифф сверен
15:28:08Z  f74e2649 «feat(pr): поле role обязательно … (тред 052) (#216)» лёг в main
           git rev-parse origin/main → f74e2649 ≠ HEAD (d5dc4e7d)
           → cherry-pick --abort, перебазировка НАЧАТА ЗАНОВО на f74e2649
```

**ИЗМЕРЕНО:** база уехала за те ~40 секунд, что заняли фетч, чекаут, cherry-pick и разбор одного текстового конфликта. **ВЫВЕДЕНО:** это тот же класс, что curator положила в `093-registry-tail-race` (каденция `main` короче такта роли), но в форме на порядок жёстче замеренной там: там интервал между merge был короче ТАКТА РОЛИ, здесь он короче ОДНОЙ ОПЕРАЦИИ перебазировки. Первую разрешённую версию я не пушил — поймал сдвиг замером `git rev-parse` перед пушем, а не после.

## 2. Дефект в моём же разрешении конфликта — поймал дифф, а не глаз

Это стоит сказать прямо, потому что цена класса «хвост общего реестра» этим и набирается.

Первое разрешение я собрал руками из двух сторон маркеров: сторону `HEAD` оставил, сторону PR приписал. Дифф против базы показал **`39 insertions, 47 deletions`** — и 47 удалений оказались целым разделом, которого в моей ветке нет, а в базе он появился: `## Поле role: в описании PR стало обязательным` (тред `052`, #216). Ручное разрешение по маркерам съело чужую работу, выглядя при этом правильным: маркеры сняты, файл читается, тесты этого файла не касаются.

**Как починено и чем доказано, что цело.** Разрешение собрано не руками, а механически:

```
git show origin/main:docs/protocol-reference.md > docs/protocol-reference.md   # база целиком
cat <39 строк, добавленных d0f35d2c>          >> docs/protocol-reference.md   # раздел предмета
```

и проверено двумя замерами:

- `git diff --cached --stat HEAD` → **`4 files changed, 311 insertions(+)`**, НОЛЬ удалений. То есть из базы не потеряно ни строки, и число — то же, что засчитывали все четыре круга;
- `cmp` добавленного блока против блока, добавленного одобренной головой `d0f35d2c`, → **побайтово идентичны** (39 строк). Три остальных пути — `git diff --cached refs/pull/202/head` пуст, то есть `checks.yml`, `exclusive-freshness.mjs` и `exclusive-freshness.process.test.ts` идентичны одобренным байт-в-байт. Как и в прошлую перебазировку, у раздела справочника сдвинулась только позиция в файле.

**Урок, который я отдаю в `093`, а не расширяю им `074`:** на хвосте общего реестра дорого не «разрешить конфликт», а *доказать, что разрешение ничего не съело*. Доказательство здесь — дифф против базы с нулём удалений, и оно машинное. Ручной осмотр маркеров этот дефект пропустил.

## 3. Следующее столкновение — замерено, окна ждать не нужно

curator просила при третьем столкновении не перебазировать молча снова, а сказать фактом (msg-033), и намекнула на «дождаться окна, когда горячие файлы никто не трогает». Замер говорит, что окна ждать не надо:

`gh pr list --state open` (15:3xZ) — пять PR: **#240, #237, #233, #202, #114**. Путь `docs/protocol-reference.md` из них трогает ровно один, **#240** (второй — сам #202). #237 и #233 сидят в `PROTOCOL.md`, #114 — в `docs/box-setup.md`; общих путей с #202 у них нет.

Симуляция обоих порядков merge, не рассуждение:

```
git merge-tree --write-tree origin/main pr/240      → c4a31c36, exit 0
git commit-tree c4a31c36 -p origin/main -p pr/240   → 5beee3a4        (# «#240 уже влит»)
git merge-tree --write-tree 5beee3a4 6bb202b4       → b5d534e9, exit 0, ни строки CONFLICT
# и обратный порядок:
git merge-tree --write-tree origin/main 6bb202b4 → commit-tree → merge с pr/240 → exit 0
```

**ИЗМЕРЕНО:** #240 вставляет свои 34 строки на позиции 3707 (середина файла), #202 приписывает 39 строк в конец; хунки не соседние, и слияние чистое В ЛЮБОМ порядке. **ВЫВЕДЕНО:** из сегодня открытой работы #202 больше не столкнётся — ни от какого порядка нажатий. Остаточный риск честно называю: он не в открытых PR, а в НОВОМ предмете, который сядет на хвост того же файла после этой строки; против него замок `074` не помогает и не должен (это класс 62-х, дом — `093`).

## 4. Живой исход шага, который этот PR и добавляет

В прогоне `33773067854` на новой голове шаг **`исключительный ресурс не разошёлся с базой` — `success`**, и это правильное поведение: дифф #202 не трогает ни `agent-protocol.json`, ни `schema/version.ts`, ни `schema/migrate.ts`, значит шаг молчит и зеленеет. Первое живое подтверждение ступени 2 на настоящем прогоне, а не на тесте.

Локально на голове `6bb202b4` (чекаут контура, `pnpm install --frozen-lockfile` — «Already up to date»):

- `pnpm typecheck` — чисто, оба пакета;
- `pnpm exec biome check` по четырём путям — чисто (`.yml`/`.md` вне области биома, ожидаемо);
- `TMPDIR=/tmp pnpm exec vitest run src/merge/exclusive-freshness.process.test.ts` — **4/4 зелёных**;
- `pnpm protocol zones check --ref HEAD --role dev-core --paths <четыре пути>` → «none under a forbidden prefix» — зона не нарушена.

Полный `pnpm test` локально не гонял: его гонит сам прогон `33773067854`, и он на этом шаге сейчас и стоит.

## 5. Что дальше и почему это не сделано в этом ходу

Маршрут не сокращаю: метка `review` вешается ПОСЛЕ зелёного и на ту же голову, иначе круг судит дерево, которого нет, — ровно тот дефект, который я только что снял снятием метки с `d0f35d2c`. Поэтому:

1. `checks` `33773067854` зелён на `6bb202b4` → **навесить `review`** (пятый круг);
2. вердикт круга → перемерить дверь `merge-gate --pr 202 --review-workflow 'Claude PR Review'`; неустранимым останется только гард 4 (`.github/workflows/checks.yml` — док власти) → под кнопку john через curator;
3. если `checks` красен — читать, КАКОЙ шаг: у шага `comms-derived синхронен с каноном` есть своя причина и свой дом (`075`/`076`), и к предмету #202 она не относится. Метку в этом случае не вешать.

## 6. Состояние

- **незакоммиченного нет**, рабочее дерево чисто (`git status --porcelain` пуст); рабочее место в detached на `6bb202b4`. Локальных веток не оставлено, всё сделанное лежит в `origin/guard/exclusive-resource` (force-push с `--force-with-lease=guard/exclusive-resource:d0f35d2c…`, то есть чужой пуш поверх был бы отказан, а не перезаписан);
- **у #202 сейчас метки нет** — это НЕ «забыл», это снятый вердикт-сирота, и он же гард 1 двери;
- чужих PR и чужих тредов не трогал: #240/#237/#233/#114 названы только как ЗАМЕР класса, ни строки в них не написано;
- **паркуюсь на прогон `checks` по #202** (`--parked-on run:202`). Следующий такт начинается чтением исхода `33773067854`, а не перепрогоном: `gh run view 33773067854 --json status,conclusion` и `gh pr view 202 --json headRefOid` (голова не должна уехать — если уехала, метку не вешать, а мерить заново).

waiting-on → dev-core (мой же ход: навесить метку по зелёному), парк на `run:202`.

