---
from: reviewer-pr
worker: gh-action
date: 2026-09-02T15:20:52Z
expects: answer
waiting-on: dev-core
parked-on: john
verdict: needs-fixes
pr: 196
---

verdict: needs-fixes
pr: 196

Прогнано: `pnpm typecheck` (чисто, оба пакета), `pnpm exec biome check docs/collision-audit.md scripts/collision-audit.mjs` (чисто). Полный `pnpm test` не повторял — `checks` на голове `2c175564` зелёный, прогон `33641370416`. Живой исход `pnpm protocol merge-gate --ref origin/main --pr 196`: guard 1 (approve на текущей голове) и guard 2 (green checks на той же голове) не выполнены — `review=IN_PROGRESS`, плюс guard 2 отдельно отмечает, что база (`e28a64b`) сдвинулась ПОСЛЕ старта засчитанного прогона `checks`; guard 4 (доки власти) — ok, дифф их не трогает; guards 3/5 (ascent к решению john, след merge) помечены `you` — это дело curator при merge, не моё. Это состояние двери на момент ревью, не находка по критериям.

Скрипт я реально прогнал (`git fetch origin '+refs/pull/*/head:refs/remotes/pull/*'` + `node scripts/collision-audit.mjs --repo . --since 2026-08-18`): число текстовых столкновений (62, из них 58 между разными тредами), пересечений по исключительному ресурсу (4, те же четыре пары) и PR без merge (5, те же номера) воспроизвелись точно. Пары/пути/перебазировки чуть выросли (542/221/124 против 522/217/123) — ожидаемо, времени прошло больше, к находке не отношу.

1. **Критерий 9 (текст против факта), `docs/collision-audit.md:4`.** Раздел «Как считалось» заявляет: «Считает `scripts/collision-audit.mjs`; повторить — той же командой», и дальше единственный рецепт — `git fetch` + один вызов скрипта. Это неверно для класса 4: строки про «23 ленты сессий с `listen EINVAL`» (docs/collision-audit.md:64,80) и сверку `journal.jsonl`/`daemon.log` (docs/collision-audit.md:135) скрипт не производит вовсе — в `scripts/collision-audit.mjs` нет ни одного упоминания `EINVAL`, `daemon.log`, `session`, `TMPDIR` (проверено `grep`). Метод получения этих конкретных чисел в документе не назван нигде — то есть заявленная «той же командой» воспроизводимость для части отчёта ложна. Предлагаю: либо назвать реальный метод для класса 4 отдельной строкой (что именно грепалось, по какому корпусу лент), либо явно пометить эти числа как НЕ покрытые скриптом.

2. **Критерий 11 («дверь молчит»), `scripts/collision-audit.mjs:106`.** `readLaunchRefusals()`: `if (!existsSync(path)) return [];` — при отсутствующем `.orchestrator/journal.jsonl` функция молча отдаёт пустой список, и вывод («Отказов нет») неотличим от честного «файл прочитан, отказов ноль». Я это воспроизвёл вживую: в моей среде ревью каталога `.orchestrator` нет вообще, и та же команда из «Как считалось» напечатала «Отказов нет» — то есть класс 4 отчёта («0 отказов по грязному дереву») в точности повторяет то самое молчание, а не обязательно факт. Предлагаю: если файла нет — печатать это отдельной строкой («журнал не найден, класс не измерен»), а не сливать с нулевым результатом.

Оба пункта — про надёжность самого измерения (это единственный смысл PR), не про стиль. Остальное (скоуп — только скрипт+доки, зона `dev-core` не нарушена, доки власти не тронуты, числа классов 1–3 воспроизвелись) вопросов не вызывает.

