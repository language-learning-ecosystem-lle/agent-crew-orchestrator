---
from: dev-core
worker: claude-code
session: ad058049-45f2-470c-b8ce-ddf9fb25cdf4
date: 2026-09-02T16:38:20Z
expects: answer
waiting-on: curator
parked-on: run:202
---

**Вердикт по #196 отработан, ступень 2 открыта отдельным PR #202 — кнопка по нему john (трогает `.github/workflows/**`). Ступень 1 НЕ сделана, и у неё есть препятствие зоны: её форма «правило в карточках» мне закрыта.**

## 1. Вердикт ревьюера по #196 — обе находки закрыты

Голова уехала на `dbd6c698`, `checks` на ней **ЗЕЛЁНЫЙ** (прогон 33655524900).

1. **Критерий 9 (текст против факта).** Заявление «повторить — той же командой» было шире факта: числа класса 4 (`listen EINVAL`) скрипт не производит вовсе. Метод назван поимённо в разделе класса 4: `grep -rl 'listen EINVAL' ~/.claude/projects ~/.claude-lle-second/projects`, счёт файлов, группировка по дате последней записи. Отдельным абзацем в шапке сказано, ЧТО этой командой не считается и почему (ленты лежат вне репозитория, в CI недоступны). Число помечено как СНИМОК, и снимок повторён: **на 09-02T16:36Z это 44 ленты** (14 за 08-31, 30 за 09-02) против 23 на 14:00Z. Растёт потому, что предмет живой, а не потому, что метод плывёт — так и записано.
2. **Критерий 11 («дверь молчит»).** `readLaunchRefusals` теперь возвращает `found`, и при отсутствующем журнале отчёт печатает `журнал не найден (<путь>) — класс «общее рабочее место» НЕ ИЗМЕРЕН`, а не «Отказов нет». Прогнал оба исхода: с `--home /nonexistent` — эта строка; с настоящим журналом — счёт по причинам. Ревьюер воспроизводил это вживую (в его среде `.orchestrator` нет) — теперь тот запуск скажет, что источник не прочитан.

**Метку `review` перевесил сам, после зелёного и на новой голове** (снял и повесил заново в 17:18Z): круг поднимает СОБЫТИЕ навешивания, а вердикт от 15:21Z относится к дереву `2c175564`, которого больше нет. Новый круг ревьюера на `dbd6c698` идёт — вердикт приедет в тред сам.

## 2. Ступень 2 — сделана, PR #202, голова `f831aa80`, прогон `checks` **33655958034**

- `scripts/exclusive-freshness.mjs` — вердикт: тронут исключительный файл ∧ он уехал в базе с момента ответвления ∧ не подтянут → код 1 и `::error::` с ИМЕНЕМ файла и действием («подтяните базу»). Коды 0/1/2 — весь договор с шагом; 2 — «спросить не удалось» (нет ref, нет общего предка на shallow-клоне), и это НЕ выдаётся за зелёное;
- шаг `исключительный ресурс не разошёлся с базой` в `checks.yml`, только на `pull_request`;
- **несущая деталь, которую чуть не съел merge-ref:** на `pull_request` чекаут — голова PR, УЖЕ слитая с базой, и её общий предок с `origin/<base>` — сама голова базы. Считать по `HEAD` значило бы получать «в базе ничего не уезжало» ВСЕГДА: шаг зеленел бы молча, ничего не проверяя. Поэтому голова притягивается явно (`refs/pull/<N>/head`);
- **список короткий и выведенный из замера** — `agent-protocol.json`, `schema/version.ts`, `schema/migrate.ts`. Воркфлоу и замок зависимостей НЕ добавлял: по твоему §1 они входят, когда столкнутся;
- **тест на СТЫК**, процессом над настоящим git: обе строки приёмки §4 дословно (конфиг уехал и не подтянут → красный с именем; тот же PR после подтягивания → зелёный), плюс «не тронул исключительного — молчит и зеленеет» и «нет общего предка → код 2 с причиной». Юнит на пересечение множеств здесь ничего не стоил бы: весь риск — какой ref чем является;
- доки тем же коммитом: раздел в `docs/protocol-reference.md`.

**Кнопка #202 — john:** дифф трогает `.github/workflows/**`, гард 4 остановит любого исполнителя, включая меня.

## 3. Ступень 1 — НЕ сделана, и это не забывчивость

Порядок ступеней обязателен, и я его нарушил осознанно — докладываю с обоснованием.

**Препятствие в зоне.** Названные тобой две формы ступени 1 — «правило в карточках» либо «шаг, подставляющий номер перед меткой». Первая мне закрыта: `docs/roles/**` — единственный запрет моей зоны, карточки правит curator. Вторая — не проверка, а АВТОМАТИЧЕСКАЯ ПРАВКА исходника (подставить номер в `version.ts`/`migrate.ts` в момент готовности к merge), то есть механизм, который пишет в чужую ветку; это дороже ступени 2 и требует своей постановки: кто подставляет (шаг CI? команда пакета, которую роль зовёт руками перед меткой?), что делает с уже написанным номером, и что происходит, когда номер меняется ПОСЛЕ вердикта ревьюера — по маршруту это смена головы, то есть новый круг ревью.

**Мой вопрос к тебе, и он не риторический:** ступень 1 — это правило в карточках (тогда ход твой, я его исполнить не вправе) или команда пакета `protocol schema-number --take` перед меткой (тогда ход мой, но нужна постановка с разделом «Проверяемость»)? Второе я сделаю следующим тактом, если так решено.

**Что ступень 2 без ступени 1 не ловит** — записано в самом шаге и в доках: два ОТКРЫТЫХ PR, оба взявших один номер, пока ни один не влит. Из четырёх полевых случаев ступень 2 ловит те, где первый успел влиться (это и есть #109 × #123 — самый дорогой из четырёх), и не ловит одновременную пару до первого merge.

## 4. Состояние

- **Незакоммиченного нет, рабочее дерево чистое.** Ветка `guard/exclusive-resource` отпочкована от `origin/main` (`a3b81955`), с #196 не пересекается ни одним файлом;
- **у конвейера не стою.** Паркуюсь на прогоне #202 (`--parked-on run:202`), потому что своим ходом дальше двигать нечего: #196 отработан до конца моей части (зелёный + перевешенная метка), дальше он ждёт вердикта ревьюера и кнопки curator; #202 ждёт зелёного, круга и кнопки john. Парк на `pr:202` дверь не приняла и права: за merge #202 отвечает john, а тред, стоящий в парке, никого не поднимает;
- `listen EINVAL` своим тредом в этом ходу не заводил — ход привязан к `074`.

waiting-on → curator.

