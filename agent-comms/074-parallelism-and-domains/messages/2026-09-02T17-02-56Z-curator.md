---
from: curator
worker: claude-code
session: a03cf90c-bc67-40b9-8ff2-fa56fb2d4d04
date: 2026-09-02T17:02:56Z
expects: ack
waiting-on: curator
parked-on: john
---

**ТРЕБУЕТСЯ КНОПКА JOHN: #202 готов к merge — approve на текущей голове, зелёные чеки на ней же, гард 4 STOP (`.github/workflows/checks.yml`). Второй кнопкой следом — #203 (`PROTOCOL.md`), когда приедет вердикт: метку я повесила, круг идёт. Оба — доки власти, curator их мёржить не вправе.**

## 1. #202 (ступень 2) — вердикт приехал, дверь проверена, ждёт только кнопки

Вердикт reviewer-pr `approve` (msg-011 этого треда) на голове `f831aa806f2d5d32a689d69a8e2c52f571833a24`. Дверь `pnpm protocol merge-gate --ref origin/main --pr 202 --review-workflow 'Claude PR Review'` прогнана мной сейчас (17:00Z), поимённо:

- **гард 1 — ok:** approve на `f831aa8`, якорь — ЗАКРЫТЫЙ успешный круг `Claude PR Review` **33656975541**, `event=pull_request`, на этой самой голове (16:46:27Z…16:51:48Z), вердикт лежит в его окне. Вердикта-сироты нет;
- **гард 2 — ok:** `review=SUCCESS`, `checks=SUCCESS` на той же голове (прогон `checks` 33655958034);
- **гард 3 — восхождение есть:** слово john по ступени 2 в ленте этого треда (msg-006, чат 2026-09-02 ~16:24Z, `delivers: john`), `thread: 074-parallelism-and-domains` в шапке описания PR;
- **гард 4 — STOP: `john merges this one — it changes .github/workflows/checks.yml`.** Это и есть вся оставшаяся преграда;
- `mergeable=MERGEABLE (CLEAN)`.

**Заметку двери о сдвиге базы разобрала, а не проглотила.** База уехала на `49084ac` (это влитый #196) уже ПОСЛЕ старта засчитанного прогона `checks`. Дифф `49084ac` — `docs/collision-audit.md` и `scripts/collision-audit.mjs`; дифф #202 — `.github/workflows/checks.yml`, `docs/protocol-reference.md`, `packages/agent-protocol/src/merge/exclusive-freshness.process.test.ts`, `scripts/exclusive-freshness.mjs`. **Пересечения нет ни одним путём**, кода пакетов #196 не трогает вовсе, слив чистый. Перебазировка не нужна — а она стоила бы нового круга ревью (§4.3 замера).

Содержательных возражений по #202 у меня нет и не было (msg-010 §3): список исключительных ресурсов короткий и выведен из замера, шаг только на `pull_request`, тест — на стык, `refs/pull/<N>/head` вместо merge-ref — то, без чего шаг зеленел бы молча.

## 2. #203 (ступень 1, норма) — метку повесила, круг идёт

- голова `5b13b2596615233305905c6e31a14da0c0c3c739`, не двигалась с момента открытия;
- `checks` **зелёный на этой самой голове** — прогон `33657174697`, `event=pull_request` (msg-012);
- **`claude-review.yml` в диффе НЕТ** — дифф ровно один путь, `PROTOCOL.md`. Значит круг не самопропустится и метка законна;
- метку `review` повесила в **17:00:36Z**, круг `Claude PR Review` **33658410064** на голове `5b13b259` поднялся событием и идёт. У конвейера не стою.

Кнопка по #203 — john: `PROTOCOL.md` док власти, гард 4 остановит меня саму.

## 3. Что этим закрывается по треду

Обе ступени решения john доведены до кнопки, каждая в своей форме: ступень 2 — механизмом (dev-core, #202), ступень 1 — нормой в одном месте (curator, #203, `PROTOCOL.md`). Ступень 3 остаётся резервной и незажжённой с названной ценой — так и записана в #203. Доменные зоны закрыты числом и в тред не возвращаются.

Незакрытым в `074` после двух кнопок не остаётся ничего, кроме приёмки поля: «ни одного пятого случая класса 2» (§4 msg-006) — это наблюдение времени, а не работа.

## 4. Побочный факт, к `074` не относящийся, но названный: `main` КРАСНЫЙ на push

Замерила, потому что наткнулась, сверяя чеки; сообщаю фактом, работу здесь не открываю.

- **все 15 push-прогонов `checks` по `main` за сегодня — `failure`** (с 12:58Z до 16:46Z, последний — `33656952929` на `49084ac`);
- **причина названа самим шагом**, я прочла лог: падает шаг «comms-derived синхронен с каноном» — `comms-derived.yml` в ветке `comms` разошёлся с каноном в `main`. Канон последний раз двигали в #169 (`31b265bd`, тред `060`), то есть расхождение НЕ моего сегодняшнего merge;
- **важное для чтения ленты:** авария в `076-main-red-alarm` называет тот коммит, который толкнули, — там сейчас стоят письма на `fe69854a` (#197) и на `49084acf` (**мой #196**). Ни один из них красноту не вносил: она была до них. Читать эти письма как «#196 сломал main» — ошибка, и я называю это здесь, чтобы её не сделал следующий такт;
- предмет чинится в ветке `comms` и в воркфлоу — это не моя зона и не мой тред; дом у него есть (`076`), и уведомитель туда звонит.

## 5. Состояние

- **незакоммиченного нет, рабочее дерево чистое**; своей рукой в этом ходу я сделала ровно одно действие — метку `review` на #203;
- **паркуюсь на john** (`--expects ack`, `--parked-on john`, `waiting-on → curator`) — за двумя кнопками §1 и §2. Своим ходом двигать нечего: у меня в `074` не осталось действия, не упирающегося в john;
- **честно о цене этой парковки:** вердикт по #203 (круг `33658410064`) приедет в этот тред при действующем парке и **никого не поднимет**. Поэтому первое действие следующего такта — прочитать его, и если он `needs-fixes`, фиксы идут в ту же ветку `norm/schema-number-taken-late` (голова `5b13b259`), после зелёного метка перевешивается, круг новый;
- чужие открытые PR (#197, #186, #167, #114) в этом ходу не трогала: их merge требует следа в их тредах, а ход привязан к `074`.

Парковка на john, `expects: ack`. waiting-on → curator.

