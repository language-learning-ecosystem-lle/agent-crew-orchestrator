---
from: curator
worker: claude-code
session: 91d42f1b-9522-4255-8588-bc1a494c2780
raised: 2026-09-03T17:39:30Z
date: 2026-09-03T17:44:19Z
expects: ack
waiting-on: curator
parked-on: john
---

**ТРЕБУЕТСЯ РЕШЕНИЕ JOHN, ОДНО И ПОСЛЕДНЕЕ ПО `074`: приёмку поля читать ЦЕНОЙ («ни одного случая, стоившего пропущенного круга ревью или отката чужой починки») — да или нет? Скажешь «да» — закрываю тред тем же тактом; скажешь «нет» — тред остаётся открытым под наблюдением поля бессрочно. Всё остальное по треду ЗАКРЫТО, и приёмка искусственного случая из msg-006 §4 закрыта ЖИВЫМ замером на влитом артефакте (§2–§3), а не тестом.**

Кнопка нажата, и это перечитано моей рукой, а не взято из письма github. Всё ниже — замеры этого хода 17:39Z…17:44Z.

## 1. #202 в `main` — след merge с фактическим sha

```
gh pr view 202 → state=MERGED, mergedAt=2026-09-03T16:15:52Z, baseRefName=main,
                 headRefOid=6bb202b45f5e56552ffaf1886291c68484497167,
                 mergeCommit=09b89435c4286232f882b738123792fe2b2a67a9, mergedBy=maysway
git show -s 09b89435 → feat(ci): исключительный ресурс, уехавший в базе,
                       краснит прогон по имени файла (тред 074) (#202)
```

- **цель проверена, а не предположена:** `base.ref` = `main`, squash-коммит `09b89435c4286232f882b738123792fe2b2a67a9` стои́т в линии `origin/main` третьим снизу;
- **гарды на момент нажатия** (замер msg-010 §1, 16:03Z, голова та же `6bb202b4`, с тех пор не двигалась): гард 1 — `approve` reviewer-pr на этой голове, якорь — круг `Claude PR Review` **33774072786**, `event=pull_request`, окно 15:41:03Z…15:44:37Z, approve 15:44:32Z внутри окна; гард 2 — `checks` **33773067854** `success` на этой же голове; гард 3 — решение john, msg-006 этого треда; гард 4 — **`STOP`, и он не обходился: кнопку нажал john**; гард 5 — эта строка;
- **`mergedBy=maysway` руки не называет** — токен контура это PAT того же аккаунта. Руку john здесь доказывает `STOP` гарда 4 в двух независимых прогонах двери (dev-core 15:5xZ, мой 16:03Z) плюс его слово в msg-012, а не поле.

## 2. Приёмка искусственного случая — перемерена на ВЛИТОМ артефакте, четырьмя классами

Фикстура покрывает функцию; путь, список ресурсов и коды выхода влитого файла — нет. Поэтому взяла скрипт **из `origin/main`** (`git show origin/main:scripts/exclusive-freshness.mjs`, 121 строка, в `mktemp -d`, в дерево не писала) и прогнала его по НАСТОЯЩИМ ссылкам этого репозитория:

| класс | база → голова | вывод | код |
|---|---|---|---|
| **красный, полевой пятый случай** | `6f0a0bf7` (#211) → `ef1dde5c` (голова #167) | `::error::исключительный ресурс разошёлся: agent-protocol.json — эти файлы уехали в 6f0a0bf7 после ответвления… Подтяните базу` | **1** |
| **зелёный после подтягивания** | `6f0a0bf7` → `cdaa145e` (#167 в линии) | `тронуто (agent-protocol.json), в базе не уезжало — чисто` | 0 |
| **исключительных не трогает** | `09b89435` → `722d0f96` (#241) | `исключительных файлов этот PR не трогает — не спрашиваю` | 0 |
| **нет общего предка** | `6f0a0bf7` → не-коммит | `не считается общий предок … нужен полный клон (fetch-depth: 0)` | **2** |

**ИЗМЕРЕНО:** обе строки приёмки msg-006 §4 дословно — «уехал и не подтянут → красный ШАГ С ИМЕНЕМ ФАЙЛА» и «тот же PR после подтягивания → зелёный» — исполняются влитым файлом, и красный случай не выдуман: это ТОТ САМЫЙ пятый случай класса 2, который я замерила вчера (msg-025 §3, #167 против уехавшего #211). Механизм, будь он в `main` на 10:00Z, покраснел бы по имени `agent-protocol.json` — теперь это не расчёт, а прогон.

**Честная граница замера:** класс «нет общего предка» я вызвала не-коммитом, а не мелким клоном — код 2 и причину он печатает, но именно shallow-клон этим не проверен. Список исключительных путей внутри влитого файла прочитан глазами: `agent-protocol.json`, `packages/agent-protocol/src/schema/version.ts`, `packages/agent-protocol/src/schema/migrate.ts` — ровно три, как решено.

## 3. Стык на раннере — тоже уже измерен, и даром

Обвязка шага (два `fetch`, `refs/pull/<N>/head`, `github.base_ref`, путь до скрипта) живёт только в Actions, и её мой прогон не покрывает. Ждать её не пришлось:

```
gh run view 33785441211 (checks, event=pull_request, 17:35Z, ветка docs/052-pr-open-norm)
  шаг 7 «исключительный ресурс не разошёлся с базой» → success
  env: BASE_REF=main, PR_NUMBER=248
  вывод: exclusive-freshness: исключительных файлов этот PR не трогает — не спрашиваю.
```

**ИЗМЕРЕНО:** шаг стои́т в живом прогоне седьмым, обе `fetch`-строки прошли, скрипт нашёлся по пути, переменные подставились, и напечатал он ИЗМЕРЕННУЮ ветку, а не молчание. То есть зелёный тут — не «шаг отработал», а «шаг спросил и ответил». Красная половина НА РАННЕРЕ пока не наблюдалась и наблюдаться будет, когда придёт: для неё нужен PR, тронувший исключительный ресурс, уехавший в базе. Это не долг — это поле.

Чужой PR #248 я только ЧИТАЛА: ни строки в него и ни строки в тред `052`.

## 4. Что осталось — ровно приёмка поля, и почему её нельзя закрыть без john

msg-006 §4 объявляет её счётом: «Поле: ни одного пятого случая класса 2». **Эта форма опровергнута фактом** — случай был (msg-025 §3), поэтому закрыть тред объявленной приёмкой я не могу вовсе: она не сошлась. Форма ценой держится, и её замер полный: **ущерб пятого случая — ноль** (трёхсторонний слив удержал чужую починку, проверено на влитом дереве, msg-032 §4).

**Рекомендация curator (не решение):** читать ценой. Довод — не удобство, а цена формы: критерий «ни одного случая» не имеет конца и требует смотреть в поле КАЖДЫЙ такт, то есть жжёт слот роли бессрочно; критерий ценой перечисляет факты и проверяется одним чтением. Перечисление строго сильнее образца.

**Первую полевую цифру о ложных тревогах не теряю:** 1 из 1 срабатываний пришлось бы на случай, который трёхсторонний слив разрешает сам. Это не дефект — поведение консервативно по построению, как john и назвал, — но цифру надо копить. Дом ей — `093-registry-tail-race` (класс тот же: хвост общего реестра), и если она останется такой на десятке случаев, разговор о смягчении шага обопрётся на число.

## 5. Хвост открытых PR — прогнала, молчания не оставляю

`gh pr list --state open` в 17:42Z, восемь штук: **#248, #247, #246, #245, #244, #243, #240, #114**. **Моего кандидата среди них нет ни одного:** #202 влит, и это был единственный PR треда `074`. Остальные принадлежат чужим тредам — их merge требует следа гарда 5 в ленте ИХ треда, а этот ход привязан к `074`, писать туда мне нечем. Не «забыла», а не имею права здесь.

## 6. Состояние и чей ход

- **незакоммиченного нет:** `git status --porcelain` пуст, в код, PR, метки и чужие треды в этом ходу не написано ни строки; единственная запись хода — это письмо. Временных ссылок в `refs/` не заводила вовсе (склад refs у ролевых деревьев один), тело письма писала в своём `mktemp -d`;
- **что закрыто по `074`:** ступень 1 — в `main` (#203); ступень 2 — в `main` (#202, `09b89435`, кнопка john), приёмка искусственного случая закрыта живым замером на влитом артефакте и стыком на раннере; ступень 3 — резервная и незажжённая с названной ценой; доменные зоны — отклонены john по числу и не возвращаются; находки про хвост реестра и про метод — живут в `093-registry-tail-race`;
- **что осталось — одна строка сверху, и она не моя**;
- **паркуюсь на john** (`--expects ack`, `--parked-on john`). Проверила перед постановкой: строки на `074` в `notify.state` нет — прошлый парк снят, значит `--expects ack` звонит сразу и несёт первой строкой ровно этот вопрос, один. **Цена названа честно: у john на сегодня уже стоят три парка (`064` 16:55Z, `090` 15:17Z, `096` 16:20Z), этот четвёртый — и любое письмо, пришедшее в `074` при действующем парке, никого не поднимет;**
- **первое действие следующего такта:** прочитать ответ john в ленте. «Ценой» → закрыть тред `thread status … --write` с приёмкой, перечисленной фактами §1–§3. «Счётом» → оставить открытым и записать, что именно каждый такт смотрим в поле, чтобы наблюдение не стало бессрочным по умолчанию.

Парковка на john, `expects: ack`. waiting-on → curator.

