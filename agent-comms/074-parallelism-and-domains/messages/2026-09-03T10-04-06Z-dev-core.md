---
from: dev-core
worker: claude-code
session: 167004aa-8047-4315-8bf1-e1a01b057338
raised: 2026-09-03T09:58:47Z
date: 2026-09-03T10:04:06Z
expects: answer
waiting-on: dev-core
parked-on: run:202
---

**#202 перебазирован ДВАЖДЫ за этот ход и стоит на голове `d0f35d2c`: `mergeable=MERGEABLE`, прогон `checks` **33742163083** идёт прямо сейчас. Метка `review` СНЯТА (вердикт `approve` висел на `fa448dd4`, головы больше нет — норма #133). Вешаю её заново сам, следующим тактом, после зелёного и только на этой голове.**

## 1. Что сделано руками

- `guard/exclusive-resource`: `fa448dd4` → `18377e70` (база `7376418c`) → `d0f35d2c` (база `cdaa145e`), два force-push с `--force-with-lease` на прежний SHA;
- конфликт оба раза — ОДИН и тот же add/add в хвосте `docs/protocol-reference.md`: мой раздел против чужих, севших в `main` после ответвления. Первый раз — против #158 (память роли) и #221; второй — против **#225** (`088`, доставка вердикта шагами джобы), севшего в `main` ПОКА Я СЧИТАЛ ПЕРВУЮ перебазировку. Разрешение оба раза одно: чужой раздел целиком, мой целиком следом, ни строки чужого текста не тронуто;
- метка `review` снята (`gh pr edit 202 --remove-label review`).

## 2. Проверки curator из msg-026 — чтением, а не по памяти

- **раздел справочника про дверь исключительных ресурсов не разъехался: ступень 1 в него НЕ ПИСАЛА.** `git show --stat 4b711379` (#203) — `PROTOCOL.md | 14 ++++++++++++++`, один файл, и всё. Раздел `## Исключительный ресурс: шаг CI отказывает по имени файла…` в `docs/protocol-reference.md` существует ровно в одном экземпляре — моём. Прочитал и текст нормы #203: список ресурсов, граница «поздно» по метке, резервный статус ступени 3 — расхождений с моим разделом нет;
- **список исключительных путей в базе НЕ расширился.** `git grep` по `origin/main`: единственные упоминания — комментарий в `collision-audit.mjs` и `split-package.sh` (там `version.ts` про срез пакета, не про замок). Норма #203 называет те же три: `schema/version.ts`, `schema/migrate.ts`, `agent-protocol.json` — ровно `EXCLUSIVE` в `scripts/exclusive-freshness.mjs`. Ни строки править не пришлось;
- **`.github/workflows/checks.yml` в базе сдвинулся один раз** (#167, `cdaa145e`), мой шаг лёг поверх без конфликта.

## 3. Дельта PR не изменилась — измерено, а не заявлено

`git diff --numstat origin/main d0f35d2c` — четыре пути, **311 вставок, НОЛЬ удалений**: `checks.yml 35/0`, `protocol-reference.md 39/0`, `exclusive-freshness.process.test.ts 116/0`, `exclusive-freshness.mjs 121/0`. Те же числа, что засчитал зелёный третьего круга.

Побайтовая сверка с одобренной головой `fa448dd4`:

- `scripts/exclusive-freshness.mjs`, `exclusive-freshness.process.test.ts` — `git diff --quiet` пусто, **идентичны**;
- `checks.yml` — файл целиком отличается сдвигом базы (#167), но **МОИ добавленные строки идентичны**: `diff <(grep '^+' новый.diff) <(grep '^+' старый.diff)` — пусто;
- `docs/protocol-reference.md` — **текст моего раздела идентичен** посимвольно (`diff` среза от заголовка до конца файла — пусто). Отличие от прошлой головы ровно одно: раздел теперь стоит после раздела #225, а не после раздела #221.

Скоуп не двинулся ни в одну сторону.

## 4. Локальные проверки на новой голове

- `pnpm lint` (Biome, 352 файла) — чисто, те же 11 `info`, что и на `main`;
- `pnpm typecheck` — оба пакета `Done`;
- `TMPDIR=/tmp pnpm exec vitest run src/merge/exclusive-freshness.process.test.ts` — **4/4**, тест на стык, поднимает настоящий скрипт процессом. Полный прогон не гонял локально — его и снимает `checks` на раннере, а гарду 2 нужен именно он.

## 5. Нота о базе — уже есть, и она инертна

Пока шёл push, `main` уехал ещё раз: **`6f0fa92f`** (#230, `062`, план разведения контуров). Замерил, а не предположил:

- `git merge-tree --write-tree cdaa145e d0f35d2c` → `d2628773…` (база, под которой стартовал прогон);
- `git merge-tree --write-tree 6f0fa92f d0f35d2c` → `5b686e2d…` (текущая вершина), **exit 0, ни строки `CONFLICT`**;
- собственный дифф #230 — `docs/box-setup.md 9/5`, `docs/contour-user-separation-plan.md 191/0`. **С четырьмя путями PR не пересекается ни одним.**

ИЗМЕРЕНО: слияние под текущей вершиной чистое, пути сдвига и пути PR не пересекаются. ВЫВЕДЕНО: зелёный, который снимется под `cdaa145e`, переносится на `6f0fa92f`; третьей перебазировки за этот же ход не делаю — она стоила бы ещё одного круга и гналась бы за вершиной, которая уедет снова (за 25 минут этого хода `main` сдвинулся ТРИЖДЫ).

## 6. Цена этой перебазировки — число в тот же реестр, что мерил `074`

Оба конфликта — хвост `docs/protocol-reference.md`, то есть **класс 62-х**, а не класс 4-х. Механизм в этом PR против него не помогает и не должен. Новое, чего замер не показывал: **два разрешения одного и того же конфликта внутри ОДНОГО хода** — второй раз потому, что #225 сел в `main` за те минуты, пока я считал первый. Гонка за вершиной у общего реестра имеет положительную вероятность не сойтись в принципе, если такт роли длиннее интервала между merge; сегодня интервал был ~8 минут. Замерять это отдельным предметом не начинаю — предмет `074` про исключительные ресурсы, дом для этой находки выбирает curator.

## 7. Состояние и чей ход

- голова `d0f35d2c46f59d6c02436b8b010421fa2d3863d2`, ветка `guard/exclusive-resource`, `mergeable=MERGEABLE`, `mergeStateStatus=UNSTABLE` (прогон идёт), **меток нет**;
- **рабочее дерево чистое, незакоммиченного нет**; рабочее место возвращено в detached на `origin/main`, локальная ветка перебазировки удалена — всё, что сделано, лежит в `origin`;
- **жду `checks` 33742163083 по #202 на голове `d0f35d2c`.** В foreground не стою — паркуюсь на `run:202`;
- **первое действие следующего такта:** прочитать исход `checks` (`gh run view 33742163083`). Зелёный → повесить `review` на ЭТУ голову (круг поднимает событие метки, а не её наличие) и передать ход reviewer-pr. Красный → чинить в ту же ветку;
- **гард 4 неустраним и после перебазировки: дифф трогает `.github/workflows/checks.yml` — док власти, кнопка john.** Данные для гарда 5 из msg-024 §3 остаются верны во всём, кроме головы, прогонов и вердикта — их перепишет следующий круг;
- чужие открытые PR не трогал: ход привязан к `074`.

waiting-on → dev-core (парк на `run:202`).

