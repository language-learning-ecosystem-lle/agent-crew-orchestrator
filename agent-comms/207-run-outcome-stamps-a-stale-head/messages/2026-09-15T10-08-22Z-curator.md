---
from: curator
worker: claude-code
session: 3974ba48-1dc8-4903-8740-e9165f6bb4b1
raised: 2026-09-15T10:05:15Z
date: 2026-09-15T10:08:22Z
expects: ack
waiting-on: curator
parked-on: john
---

**Требуется решение john: нажми кнопку merge (squash) на [#439](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/pull/439) — голова `ba33c926`, гарды 1 и 2 зелёные, единственный STOP — гард 4 (`.github/workflows/ci-outcome.yml` — док власти, кнопка твоя).**

Маршрут назван тобой самим в этом треде утром (`2026-09-15T08-43-40Z-curator.md`, §2, пункт 4: «кнопка
john»). Метка повешена, круг отработал, вердикт стои́т на текущей голове. Дальше у роли ходов нет.

## 1. Гарды — замерены МОЕЙ рукой, в 10:06Z, а не приняты из доклада исполнителя

`pnpm protocol merge-gate --ref origin/main --pr 439 --review-workflow 'Claude PR Review'`:

```
  ok   guard 1 · approve on the current head: approved on ba33c92 by github-actions
       — inside the round 34954824198 of 'Claude PR Review' on this head (09:51:34Z…09:57:07Z)
  ok   guard 2 · green checks on the same head: 2 check(s) green: review=SUCCESS, checks=SUCCESS
       note · base: the base moved AFTER the credited checks started: 7dd53c7 committed 09:59:30Z,
              'checks' started 09:32:18Z
  you  guard 3 · ascent to a decision of john's: thread '207-run-outcome-stamps-a-stale-head'
  STOP guard 4 · no self-merge on the documents of power: john merges this one
       — it changes .github/workflows/ci-outcome.yml
  you  guard 5 · a trace of the merge: name this merge in your next message in the thread
  ok   mergeability · mergeable=MERGEABLE (mergeStateStatus CLEAN)
REFUSED: a guard does not hold
```

| поле | значение |
| --- | --- |
| PR | **#439** `fix(ci-outcome): поле run-outcome ставится только по ТЕКУЩЕЙ голове PR (тред 207)` |
| голова | `ba33c926ae352dfbf91e91f10f00b5b4f51ca410` (перечитана `gh pr view` в 10:06Z — не двигалась) |
| гард 1 | `approve` в круге `34954824198` (`Claude PR Review`, `event=pull_request`) НА ЭТОЙ голове |
| гард 2 | `checks` `34952979398` + `review` `34954824198`, оба `SUCCESS` на этой же голове |
| гард 3 | твоё слово в этой ленте 15.09 `08:43Z` («кнопка john») + постановка `2026-09-14T14-24-02Z-curator.md` |
| гард 4 | **STOP — и он не чинится: он и есть маршрут.** Кнопка твоя |
| дифф | `.github/workflows/ci-outcome.yml` (+29 −1), `packages/agent-protocol/README.md` (+10) — прочитан мной целиком, третьих файлов нет |
| форма | **squash**, заголовок — Conventional Commits (совпадает с названием PR) |
| база | `mergeable=MERGEABLE`, `mergeStateStatus=CLEAN` |

Класс исключения гарда 3 («полевой измеренный дефект») здесь НЕ объявляю и не нужен: гард 3 закрыт
твоим прямым словом в ленте, а кнопка всё равно твоя по гарду 4.

## 2. Нота о сдвиге базы погашена замером — тремя командами, ноль прогонов

Дверь права формально: база уехала ДВАЖДЫ с момента старта зачтённого `checks` — `fbc3fd1` (#444) и
`7dd53c7` (#428, моя же вчерашняя правка README). Перемерила сама против ТЕКУЩЕГО `origin/main`
(`7dd53c74`), а не против того, что мерил исполнитель час назад:

1. **пересечение путей пусто.** `git log 041730a8..origin/main -- .github/workflows/ci-outcome.yml packages/agent-protocol/README.md` → **пусто**: ни один коммит, приехавший на `main`, файлов диффа #439 не касался;
2. **слияние собирается без конфликта.** `git merge-tree --write-tree origin/main ba33c926a` → `923ff32c`, `rc=0`;
3. **атрибуция против ОБОИХ родителей сошлась строка в строку.** Против `origin/main` дерево слияния даёт РОВНО вклад #439 (`29 1 ci-outcome.yml`, `10 0 README.md`); против головы #439 — РОВНО вклад `main` (`26 0 protocol-reference.md`, `49 0 gate.test.ts`, `31 1 gate.ts` — это #444; `1 1 README.md` — это #428);
4. **сам блоб сверен с эталоном.** `git show 923ff32c:.github/workflows/ci-outcome.yml | git hash-object --stdin` = `cbf4cd1b` = тот же блоб, что на голове PR. Слияние не тронуло правку ни на байт.

**Чего не утверждаю:** что нота ложна — она консервативна по своему же тексту и сработала правильно.
Измерено ровно одно: ЭТОТ сдвиг базы результат слияния изменить не может.

## 3. Что PR делает — одной фразой, чтобы кнопка не требовала чтения диффа

Поле `run-outcome: N` в шапке письма об исходе снимает парк `run:N`, то есть ПОДНИМАЕТ роль. Сегодня
оно ставится по одному условию — «номер PR вообще есть», — и потому штампуется в том числе на исходе
по УЕХАВШЕЙ голове: то же самое письмо ТЕЛОМ пишет «⚠️ голова уже сдвинулась, действовать по этому
исходу нельзя», а ПОЛЕМ поднимает роль ради этого самого действия. Замер 14.09 (тред 196): три штампа
`run-outcome: 409` за девять минут, два по уехавшей голове, и первый из них поднял пару за 26 минут до
потолка парка.

#439 добавляет третьим вхождением тот же тест на устаревшую голову, что уже стои́т в этом файле дважды
(строки ~396 и ~478), с запасом в сторону «штампуем» при неизвестной голове, и печатает строку лога
при пропуске поля. Новой нормы не вводит: ветка «пустой набор аргументов» в файле уже есть.

## 4. Что остаётся ПОСЛЕ кнопки — и остаётся за мной

Приёмка §5 пункт 3 постановки: живой прогон по PR, у которого голова уехала во время `checks`.
Доказательство — пара писем: у письма об исходе по СТАРОЙ голове поля `run-outcome` в шапке НЕТ, у
письма по текущей — есть. До неё тред не закрыт, закрытие моё. Смёрженное ≠ работающее, и оракул тут
только живой прогон.

## 5. Риск ожидания — называю заранее, как в прошлый раз

`main` здесь движется быстро: за последние два часа три чужих merge. **Если голова #439 сдвинется до
кнопки — вердикт умирает вместе с ней** (`synchronize` круг ревьюера не поднимает), и цена — снять
метку, повесить заново, новый круг ревью плюс новый `checks`. Пока голова стои́т `ba33c926` — стои́т и
всё, что выше. Голову я сознательно не двигаю: влить `main` «ради успокоения ноты» значит убить
оплаченный вердикт ради замера, который уже сделан выше руками.

Если решения не будет вовсе — #439 простоит тихо, выглядя работой в процессе, а дефект продолжит
поднимать роли впустую на каждом PR, у которого голова обгоняет прогон.

## 6. Ход

Парк на john, `--expects ack` — звонок. `waiting-on → curator`: вопрос несёт роль, ответ (кнопка)
твой. Незакоммиченного у меня нет, рабочее дерево чистое, своей рукой за такт не тронуто ни одного
файла репозитория — только замеры чтением и это письмо.

