---
from: curator
worker: claude-code
session: a2660c3c-f0f6-4c81-b858-370d75b1130d
raised: 2026-09-15T12:44:23Z
date: 2026-09-15T12:49:43Z
expects: ack
waiting-on: curator
parked-on: john
---

**ТРЕБУЕТСЯ РЕШЕНИЕ JOHN: отрицательная ветвь #439 живьём так и не исполнилась (2 пустых чтения). Закрыть тред на измеренной половине — или заплатить двумя прогонами за изготовление события? Рекомендую ПЕРВОЕ, риск ограничен снизу.**

Чтение 2 из 2 по правилу остановки msg-022 §6 прогнано и пусто. Правило требует нести развилку —
несу. Но такт не сводится к «пусто»: за него нашлась структурная ошибка в САМОЙ приёмке, и она
меняет вес обоих исходов, поэтому развилка ниже сформулирована уже с её учётом.

## 1. Команда приёмки прогнана — после отсечки материала НЕТ

`origin/comms` свежий, шаблон перепроверен на заведомо положительном материале (это важнее вывода —
греп, ищущий не ту строку, молчит одинаково при любой истине):

- без отсечки по дате: **383** письма `github` за 09-1x, из них **27** со строкой «уже сдвинулась» —
  инструмент видит этот класс и работает;
- с отсечкой (строго после `2026-09-15T10:46:35Z`, момент первого исполнения нового кода на раннере):
  **ноль**. Все 27 — 13–14.09, то есть материал ДЕФЕКТА, а не приёмки.

**Сколько при этом смотрела** — называю, потому что молчание доказывает починку только у ЧАСТОГО
дефекта: окно наблюдения ≈ 2 часа (`10:46Z…12:45Z`), в нём **14** писем `github`, из них **8** —
письма об исходе прогона (по PR #448, #449, #442, #450, #451×2, #452, #453). Событие «вторая голова,
пока идёт прогон первой» в этом окне не наступало ни разу. 14.09 тот же класс дал 8 срабатываний за
сутки — условие частое, но не непрерывное.

## 2. ПОПРАВКА В (третья за тред, и самая тяжёлая): объявленная мною приёмка не умела возвращать отрицательный вердикт ВООБЩЕ

Прочитала `ci-outcome.yml` на `main` не по памяти, а построчно. Оба признака пары порождает **ОДИН
предикат** над одними переменными в одном прогоне шага:

| признак | место | условие |
| --- | --- | --- |
| строка «⚠️ Голова PR уже сдвинулась» | строка 478 | `elif [ -n "${PR_HEAD:-}" ] && [ "$PR_HEAD" != "$HEAD_SHA" ]` |
| поле `run-outcome` СТАВИТСЯ | строка 599 | `[ -n "$PR" ] && { [ -z "$PR_HEAD" ] \|\| [ "$PR_HEAD" = "$HEAD_SHA" ]; }` |

Второе — отрицание первого, сужённое непустотой `PR`. Значит комбинация «строка есть И поле стоит»
**недостижима по построению**, и вторая ветвь моего правила чтения («поле стоит → починка не
работает») не сработала бы никогда — ни при работающем коде, ни при сломанном. Исходов у приёмки на
деле два, а не три: «исполнилось» и «события не было».

Проверила и подавление: ветвь строки стоит в `elif`-цепочке, её перехватывает только `MAIN_RED`
(строка 462) — а `MAIN_RED` значит «открытого PR у прогона нет», при котором поле не ставится и так.
В целевом классе (письма ПО PR) строка — точный маркер. Инструмент годен, неверна была моя проза о
нём.

Правило, которое отсюда, записано в журнал роли по этому треду: признаки, выведенные из одного
предиката, — это один свидетель, а не два.

## 3. Что я домерила СВОБОДНО, чтобы развилка не стояла на голом «нет данных»

Ни то, ни другое не есть срабатывание по норме треда 202 — называю их отдельно от неё нарочно.

**(а) Выражение гейта прогнано на пяти классах входа** (bash, локально, даром):

| `PR` | `PR_HEAD` | `HEAD_SHA` | ответ |
| --- | --- | --- | --- |
| 448 | `09bef692` | `09bef692` | поле СТАВИТСЯ ✅ |
| 409 | `15afa1ca` | `4c0059fe` | поля нет ✅ (целевой класс) |
| 409 | `15afa1ca` | `15afa1ca` | поле СТАВИТСЯ ✅ |
| 409 | пусто | `4c0059fe` | поле СТАВИТСЯ — по объявленному правилу «неизвестная голова штампуется» |
| пусто | пусто | `4c0059fe` | поля нет ✅ |

**(б) Живость предиката доказана ДО починки.** Строка «уже сдвинулась» напечаталась 27 раз 13–14.09
— то есть `PR_HEAD` в реальных прогонах непуст и сравнение с `HEAD_SHA` в поле срабатывает. Ровно
это и был единственный способ, каким починка могла оказаться тихо мёртвой (строка 4 таблицы: пустой
`PR_HEAD` штампует поле при любой голове). #439 добавил к уже доказанному предикату ПОТРЕБИТЕЛЯ, а
не сам предикат.

## 4. Отчего риск ограничен СНИЗУ — и почему это главный аргумент развилки

Ветвь, где поле НЕ ставится, только УБИРАЕТ поле. Её отказ имеет ровно два исхода, и оба измерены
или опровергнуты:

- **не срабатывает** → поле по-прежнему штампуется на уехавшей голове, то есть состояние ДО #439:
  холостые подъёмы. Плохо, но не хуже прежнего, и класс частый — он объявится сам новым письмом;
- **срабатывает слишком широко** → поле отнимается у ВСЕХ PR, парк `run:N` теряет единственного
  подъёмника. Это и был настоящий риск, и он **опровергнут в поле**: 8 срабатываний положительной
  ветви на 7 разных PR за два часа.

Исхода «стало хуже, чем было» у принятия нет. Это не отменяет нормы 202 — она про то, что merge не
есть приёмка, — но именно поэтому решение о её разовом неприменении твоё, а не моё.

## 5. Развилка — три формы, прошу выбрать одну

1. **ПРИНЯТЬ на измеренной половине и закрыть тред.** Цена: отрицательная ветвь остаётся
   неисполненной; если она мертва, дефект вернётся холостым подъёмом и объявится сам. Выигрыш: тред
   перестаёт есть по такту curator за чтение, которое уже дважды ничего не дало.
2. **ЗАПЛАТИТЬ за изготовление события:** два быстрых пуша в один PR, чтобы голова обогнала прогон.
   Цена: два прогона раннера ради замера (расход без человека — `launch-params` роли не дано),
   плюс такт на чтение письма в чужой ленте. Выигрыш: норма 202 исполняется буквально.
3. **ОСТАВИТЬ как есть** — низкий приоритет, чтение на каждом подъёме. Цена называется честно: такт
   curator за подъём, бессрочно, без подъёмника и без даты окончания; два таких уже потрачены.

**Рекомендация curator (не решение): вариант 1.** Основание — §4: у принятия нет исхода хуже
статус-кво, а вариант 3 платит тем же тактом, что и вариант 1, но без конца. Если норма 202 для тебя
без исключений — тогда вариант 2, и я закажу событие следующим тактом своей рукой, назвав его
расходом заранее.

Проверила, не часть ли это уже стоящей эскалации: открытых парковок на тебе три (205, 206, 209) —
205 просит слово на ЗАКРЫТИЕ своего треда по состоявшейся приёмке, предмет другой; общей развилки
«чем заменяется первое срабатывание» ни в одной не стоит. Второй развилки не завожу.

## 6. Хвост открытых PR — прогнан, домерживать нечего

`gh pr list --state open` — пять: #449, #450, #451, #452, #453 (треды 200, 200, 180, 210, 211), все
чужие. Моих кандидатов **ноль**; #440 нажат тобою 15.09. `merge-gate` не гоняла: своего кандидата
нет, а чужой PR мою кнопку не зовёт.

## 7. Дерево, записи, ход

Рабочее дерево чистое (`git status --porcelain` пусто), `HEAD` детач на `d33a614a`. Файлов
репозитория своей рукой не тронуто ни одного: такт — чтение (`git ls-tree`, `git show`, `gh pr list`,
локальный прогон выражения гейта), запись журнала командой в ветку почты
(`agent-comms/journal/curator/207-run-outcome-stamps-a-stale-head.md`, запушено) и это письмо.
Незавершённого и незакоммиченного нет.

`--parked-on john`, `--expects ack`, `waiting-on → curator`: ход остаётся у меня как у несущей
вопрос. Приоритет треда оставляю **low** — он ждёт человека, а не очередь. Следующий подъём читает
твой ответ и исполняет выбранную ветвь; без ответа самостоятельного действия в треде не осталось.

