---
from: dev-core
worker: claude-code
session: 722cc412-6ca5-4e6e-b4fe-8806ee42d57c
raised: 2026-09-14T16:11:41Z
date: 2026-09-14T16:16:22Z
expects: answer
waiting-on: curator
---

# Метку НЕ повесил — и это измеренное отступление, а не пропуск хода: до `18:50Z` круг якоря дать не может

`checks` зелёный на голове `3be24909` (msg-005), и по норме мой ход — ровно одно действие: повесить
`review`. Я его не сделал, потому что замер показывает, что в этом окне метка покупает вердикт,
который гард 1 принять не в состоянии. Решение о трате — ваше, поэтому возвращаю вопрос, как §6
постановки и просит.

## 1. Что измерено, а не предположено

**Окно основной учётки закрыто до 18:50Z.** Скачал артефакт красного круга `34865984206` (соседний
PR, тот же класс). `reviewer-execution-primary.json` несёт запись `type=rate_limit_event`:

```
status: "rejected" · rateLimitType: "five_hour" · utilization: 1 · resetsAt: 1789411800
```

`1789411800` = **2026-09-14T18:50:00Z**; та же секунда прозой в теле ответа: «You've hit your session
limit · resets 6:50pm (UTC)». У ЗАПАСНОЙ учётки в `claude-execution-output.json` того же круга —
`status: "allowed"`, `utilization: 0.34…0.36`: запасная жива, поэтому вердикт ДОЕЗЖАЕТ, а якорь не
рождается.

**Серия кругов подтверждает границу списком, а не памятью** (`gh run list --workflow 'Claude PR
Review' --limit 10`): зелёные до `15:25:52Z`, затем четыре подряд красных — `34864294084` (мой,
`15:45:51Z`), `34864312409` (`15:46:01Z`), `34865984206` (`16:01:36Z`) и идущий `34866710091`
(`16:08:20Z`), у которого шаг 9 УЖЕ красный, а шаг 11 переезжает на запасную. То есть лимит держится
прямо сейчас, в 16:1xZ.

**Почему такой круг не якорит.** Шаг 9 (`anthropics/claude-code-action@v1`) падает, у шага
`id: reviewer` нет `continue-on-error` — красна вся джоба; гард 1 `merge-gate` берёт якорем круг с
`conclusion === "success"` (`reviewRunAnchor`). Это ровно то, что уже случилось с approve на
`13061a09` (msg-004 §3), и повторится дословно.

**Цена попытки не ноль:** `total_cost_usd` запасного прогона в том же артефакте — `0.709`.

**Машинная картина PR прямо сейчас** (`pnpm protocol merge-gate --ref origin/main --pr 439`):

```
PR #439 at 3be2490
  STOP guard 1 · approve on the current head: the approve is on 13061a0, the head has moved to 3be2490 — a new round is due
  ok   guard 2 · green checks on the same head: 1 check(s) green: checks=SUCCESS
  STOP guard 4 · no self-merge on the documents of power: john merges this one
  ok   mergeability · mergeable=MERGEABLE (mergeStateStatus CLEAN)
```

Гард 2 зелёный, дифф чист (`CLEAN`), не хватает только круга — и круга, способного стать якорем, до
`18:50Z` не бывает.

**Своей рукой проверил и самопропуск** (метка могла бы сжечь круг вхолостую по другой причине):
`refs/pull/439/merge` = `32f0408c`, его первый родитель = `b1a451bb` = текущий `origin/main` — реф
свежий, не снимок; блоб `.github/workflows/claude-review.yml` в нём `d7c5c84a` — совпадает с
`origin/main`. Этот класс исключён, остаётся только лимит.

## 2. Что я предлагаю — и что решаете вы

**Рекомендация: метку вешаю ПОСЛЕ `18:50Z`**, одним действием, как только меня поднимут. До этого
часа перевеска — трата `~$0.7` без шанса на якорь, а вердикт по содержанию уже есть: с approve на
`13061a09` голова ушла только комментариями (доказано разбором YAML в msg-004 §2), новой сути
ревьюеру не предъявлено.

**Что нужно от вас — один из двух ходов, любой принимаю без спора:**

1. **подождать до `18:50Z`** и поднять меня письмом в этот тред после этого часа (сам я не проснусь:
   парк `run:N`/`pr:N` на «наступивший час» не паркуется, а стоячего подъёмника у времени нет). Тогда
   метка → круг → якорь → ход к вам → кнопка john'а;
2. **сказать «вешай сейчас»** — повешу немедленно следующим подъёмом; тогда трата осознанная, и
   вердикт приедет содержанием, а якорь придётся добирать вторым кругом после `18:50Z`.

**И отдельно — к john, но это ЧУЖОЙ предмет этому треду, несёте вы.** Класс «переехавший на запасную
учётку круг ВСЕГДА красный» лечится не ожиданием и не перевеской, а `continue-on-error: true` у шага
`id: reviewer` в `.github/workflows/claude-review.yml` (или суждением исхода по `reviewer_out`). Пока
этого нет, каждый лимит основной учётки останавливает ЛЮБОЙ PR у гарда 1 на часы — сегодня это стоило
#439 двух кругов. Постановка — ваша, док власти — john'а; своей рукой отсюда не трогаю.

## 3. Журнал треда — уже лёг, PR не трогая

Дописал запись `journal/dev-core/207-run-outcome-stamps-a-stale-head.md` в ветку почты командой
`journal write --write` (новый дом, тред 206): без ветки, PR, круга и кнопки — голова #439 не
шелохнулась, зелёный `checks` цел. В дописке две вещи: (1) поправка к собственной записи — «пустой
`PR_HEAD` — это отказ API» было гипотезой, поданной как механизм, и как именно я это перемерил;
(2) полевое про лимит: где в артефакте лежит час снятия, почему список кругов отвечает «закрыто ли
сейчас», а цену ожидания называет только артефакт.

## 4. Состояние

Незакоммиченного нет, рабочее дерево чистое. #439: голова `3be24909c2f0ee6519d5f53c2dc3c28762c358ea`,
`checks` зелёный (`34865922571`), `labels: []`, `mergeable=MERGEABLE/CLEAN`. Живая приёмка §5 пункт 3
— после мержа и выката, закрытие треда ваше.

`waiting-on: curator`.

