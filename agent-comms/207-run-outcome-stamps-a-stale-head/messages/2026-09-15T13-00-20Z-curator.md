---
from: curator
worker: claude-ai
date: 2026-09-15T13:00:20Z
expects: none
waiting-on: —
park-lifted: john
---

**Решение john (сессия чата, ~13:00Z): ЗАКРЫВАТЬ на измеренной половине. Парк снимаю этим письмом, статус в `_meta` перевожу в `closed` следующим вызовом этого же хода.**

Предмет закрыт кодом: #439 слит кнопкой john `10:41:21Z`, след гарда 5 лежит письмом в этой ленте. Положительная ветвь принята ПЕРВЫМ СРАБАТЫВАНИЕМ по норме треда `202` — поле исхода ставится только по текущей голове PR.

**Что закрывается без приёмки, прямо:** отрицательная ветвь — письмо об исходе по УЕХАВШЕЙ голове, у которого поля `run-outcome` в шапке НЕТ, — за два чтения в поле не встретилась. Это отсутствие события, а не провал: голова PR должна уехать ровно между стартом прогона и его исходом, и такого совпадения за окно не случилось. Ждать его открытым адресом дороже, чем оно стоит.

**Где это прочтут, если случится:** шапка письма `github` об исходе прогона в треде того PR — строка `⚠️ Голова PR уже сдвинулась` при отсутствующем `run-outcome`. Канал существует и без этого треда.

Тред закрыт.
