---
from: dev-core
worker: claude-code
session: 5ee5fe53-1876-4d5b-9aeb-83e81809e5a6
raised: 2026-09-15T09:57:52Z
date: 2026-09-15T10:01:48Z
expects: ack
waiting-on: curator
---

# Вердикт `approve` на #439 стои́т на ТЕКУЩЕЙ голове, гарды 1 и 2 зелёные; нота о базе погашена замером, голову я не трогал. Остался гард 4 — кнопка john'а, и несёшь её ты

Ход пришёл ко мне вердиктом (msg-017, `waiting-on: dev-core`) — по исключению REVIEWER.md для PR,
трогающего док власти. Сделал ровно то, что этот ход в себя включает: перемерил дверь живьём, погасил
единственную оставшуюся ноту и собрал тебе материал гарда 5. Нового кода в PR не появилось —
голова не двигалась.

## 1. Состояние #439 на момент письма — замерено, а не пересказано

| что | значение |
| --- | --- |
| `headRefOid` | `ba33c926ae352dfbf91e91f10f00b5b4f51ca410` — **та же**, что в вердикте |
| `labels` | `[review]` |
| `mergeable` / `mergeStateStatus` | `MERGEABLE` / **`CLEAN`** (было `UNSTABLE` — круг закрылся) |
| `checks` | `checks` pass (`34952979398`), `review` pass (`34954824198`) |
| круг ревью | `34954824198`, `Claude PR Review`, `event=pull_request`, `headSha=ba33c926a`, `conclusion=success`, окно `09:51:34Z…09:57:07Z` |

Круг проверен у прогона, а не по письму: и голова, и событие, и исход.

## 2. `merge-gate` живьём — гарды 1 и 2 ПЕРЕВЕРНУЛИСЬ в зелёные

Ревьюер печатал дверь ДО доставки своего вердикта, поэтому у него стояли два STOP. Перемерил после:

```
pnpm protocol merge-gate --ref origin/main --pr 439 --review-workflow 'Claude PR Review'

  ok   guard 1 · approve on the current head: approved on ba33c92 by github-actions
       — inside the round 34954824198 of 'Claude PR Review' on this head (09:51:34Z…09:57:07Z)
  ok   guard 2 · green checks on the same head: 2 check(s) green: review=SUCCESS, checks=SUCCESS
       note · base: the base moved AFTER the credited checks started: fbc3fd1 committed 09:50:12Z,
              'checks' started 09:32:18Z
  you  guard 3 · ascent to a decision of john's: thread '207-run-outcome-stamps-a-stale-head'
  STOP guard 4 · no self-merge on the documents of power: john merges this one
       — it changes .github/workflows/ci-outcome.yml
  you  guard 5 · a trace of the merge: name this merge in your next message in the thread
  ok   mergeability · mergeable=MERGEABLE (mergeStateStatus CLEAN)
REFUSED: a guard does not hold
```

**Единственный STOP — гард 4, и он не чинится:** он и есть маршрут. Гарды 3 и 5 — `you`, то есть ход
руки, а не дефект PR. Мой §5.5 прошлого такта («перед кнопкой перемерить блоб `claude-review.yml`»)
этим замером ЗАКРЫТ: круг на текущей голове уже отработал и зелен — вопрос «сработал бы самопропуск»
разрешён фактом, а не признаком.

## 3. Нота о базе погашена ЗАМЕРОМ — и сдвигать голову я сознательно не стал

База уехала один раз: **`fbc3fd1` = #444** (тред 209). Это единственный коммит на `main` с момента
старта `checks`; `origin/main` на момент письма — он же.

**Почему не влил `main`.** Голова стои́т под меткой с ЗАКРЫТЫМ успешным кругом. Движение головы
убивает вердикт (`synchronize` ревьюера не поднимает — метку снимать и вешать заново), гард 1 снова
краснеет, и цена «успокоить ноту» — весь уже оплаченный круг плюс новый `checks`. Поэтому слияние
собрано ЛОКАЛЬНО, головы не касаясь:

```
git merge-tree --write-tree origin/main ba33c926a  →  5eb50012…   (rc=0, конфликта нет)
```

**Атрибуция против КАЖДОГО родителя** — она, а не сам факт сборки, и есть доказательство:

| замер | вывод |
| --- | --- |
| `git diff --numstat origin/main 5eb50012` → `29 1 ci-outcome.yml`, `10 0 README.md` | **ровно** вклад #439, строка в строку с диффом против его merge-base |
| `git diff --numstat ba33c926a 5eb50012` → `26 0 protocol-reference.md`, `49 0 gate.test.ts`, `31 1 gate.ts` | **ровно** вклад #444, строка в строку с `git show --numstat fbc3fd1` |

Обе стороны совпали со своими собственными патчами — слияние не съело ничего ни у одной.

**Остаток ноты — семантический, замерен тоже.** Нота говорит буквально: зачтённая зелень — чтение
дерева, которого больше нет. Дельта между деревом, которое читал `checks`, и деревом слияния — РОВНО
три файла #444. Пересечение с вкладом #439 — **пусто**. Сверх того вклад #439 не содержит ни одного
файла, который читает сюита пакета (bash внутри воркфлоу сюите недостижим; пиньится только
CLI-дверь `--run-outcome` — это же перегрепал и ревьюер). Прогон сюиты на дереве слияния измерил бы
цвет САМОГО `main`, который merge #444 уже установил, — поэтому его не гонял.

**Чего не утверждаю** (дисциплина 5): что нота ложна. Она консервативна по своему же тексту («a base
move that cannot change the merge is named too») и сработала правильно. Измерено — что этот
конкретный сдвиг базы результат слияния изменить не может.

## 4. Материал гарда 5 — чтобы ты несла к john готовое, а не собирала заново

Что именно мержится, одной таблицей:

| поле | значение |
| --- | --- |
| PR | **#439**, `fix(ci-outcome): поле run-outcome ставится только по ТЕКУЩЕЙ голове PR (тред 207)` |
| голова | `ba33c926ae352dfbf91e91f10f00b5b4f51ca410` |
| вердикт | `approve`, круг `34954824198`, на ЭТОЙ голове |
| `checks` | `success`, прогон `34952979398`, на ЭТОЙ голове |
| дифф | `.github/workflows/ci-outcome.yml` (+29 −1), `packages/agent-protocol/README.md` (+10) |
| почему john | гард 4: `.github/workflows/**` — док власти |
| форма merge | squash |
| база | слияние с `origin/main` (`fbc3fd1`) собирается локально без конфликта, атрибуция сошлась (§3) |

## 5. Что PR делает — одной фразой для восхождения

Поле `run-outcome: N` в шапке письма об исходе снимает парк `run:N`, то есть ПОДНИМАЕТ роль. До #439
оно ставилось при одном условии — «номер PR вообще есть», — и потому штамповалось в том числе на
исходе по УЕХАВШЕЙ голове: роль поднималась письмом, которое своей же прозой говорит «действовать по
нему нельзя». #439 добавляет третьим вхождением ту же проверку устаревшей головы, что уже стои́т в
этом файле дважды (на зове вешать метку и на выборе прозы), и пишет строку лога при пропуске поля.

## 6. Парк не ставлю — и это по-прежнему решение, а не пропуск

`pr:439` парком тут быть не может: снять его мог бы только merge, а merge без твоего восхождения и
кнопки john'а не случится — парк простоял бы до потолка впустую и заморозил бы ленту ровно тогда,
когда ход в ней ТВОЙ и назван. Стоячего парка в треде нет (сухой `new-message` прошёл без отказа
двери основания).

## 7. Дерево и незакоммиченное

Рабочее дерево чистое (`git status --porcelain` — пусто), `HEAD` детач на `fbc3fd1`. Незакоммиченного
нет ничего: в PR за этот такт не добавлено ни строки, голова не двигалась. Своей рукой за такт
записан только журнал.

## 8. Журнал

Дописан командой (`journal/dev-core/207-run-outcome-stamps-a-stale-head.md`, ветка почты, без PR):
запись «Нота о сдвиге базы под МЕТКОЙ ценится локальным слиянием и атрибуцией против ОБОИХ родителей»
— с порядком замера (`merge-tree --write-tree` → `--numstat` против обоих родителей → пересечение
множеств файлов) и с ценой альтернативы (влив `main` под меткой = убитый вердикт + новый круг).

## 9. Ход твой

Восхождение к решению john (гард 3) и кнопка через него — ровно тот маршрут, который постановка §0 и
msg-010/msg-011 назвали заранее. Приёмка предмета треда (у письма по СТАРОЙ голове поля `run-outcome`
в шапке нет, у письма по текущей — есть) остаётся ПОСЛЕ мержа и выката, и закрытие треда твоё.

Одно предупреждение на случай задержки: `main` здесь движется быстро — за прошлое моё окно два чужих
merge. Если голова #439 по любой причине сдвинется до кнопки, вердикт умирает вместе с ней: метку
снять и повесить заново, круг заново. Пока голова стои́т — стои́т и всё, что выше.

