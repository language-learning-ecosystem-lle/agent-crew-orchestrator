---
from: curator
worker: claude-code
session: 55f04d59-2848-40ef-8133-605d2b2c4dfc
date: 2026-08-18T21:20:33Z
expects: ack
waiting-on: curator
parked-on: john
---

**Требуется решение john: кнопка merge на PR #19** (`docs(wake): поле session и watchTargets() помечены как задел (тред 012)`) — голова `972225a63d1974d526edf126181edddf31ffef69`, база `main` = `9bd5a21`, `checks` зелёный НА ЭТОЙ голове, `mergeable: MERGEABLE`, дифф 33 строки, все добавленные, все — комментарий и абзац доки. Ревьюер заморожен (`docs/install-notes.md` §3), гарду 1 закрываться нечем, поэтому право merge у ролей выключено и кнопка твоя.

## 1. Сверка сделана своим чтением, не по докладу

Все факты ниже сняты curator заново 2026-08-18 21:21–21:27Z, командами, а не с слов dev-core.

| что проверено | как | исход |
| --- | --- | --- |
| голова PR и её зелёный | `gh pr view 19 --json headRefOid,statusCheckRollup,mergeable` | голова `972225a6…`, `checks` = SUCCESS (run `32186645242`, завершён 21:17:45Z) на ЭТОЙ голове, `MERGEABLE` |
| дифф — только текст | `git diff main...origin/pr-19 --stat` и полный `git diff` | `+33 −0` в трёх файлах: `docs/protocol-reference.md +11`, `roles/registry.ts +10`, `roles/schema.ts +12`. Все добавленные строки — внутри `/** … */` либо абзац markdown. Изменённых и удалённых строк нет вовсе, значит исполняемых изменений нет по построению |
| текст пометок = процитированному в докладе | чтение диффа целиком | совпадает дословно, все три. Слов «TODO»/«FIXME» нет; как устроят резидент — не написано нигде |
| потребители `watchTargets()` НА ГОЛОВЕ PR | `git grep -n watchTargets origin/pr-19 -- 'packages/*.ts'` | `registry.ts:70` (объявление), `:169` (реализация), `registry.test.ts:124`, `resident.test.ts:54`, комментарий в `v12-resident.ts:19` — продакшн-потребителей нет, картина та же, что до правки |
| ЗНАЧЕНИЕ `wake.session` в проде | `git grep -n 'wake.session' origin/pr-19 -- roles/registry.ts` | `:123`, `:126`, `:129` — кросс-проверка уникальности, и `:171` — сам `watchTargets()`. Больше нигде |
| поле и метод не удалены, типы не тронуты | тот же дифф (нет ни одной удалённой строки) | граница §4 постановки соблюдена |

## 2. Одно место, которое стоило проверить отдельно

Доклад назвал абзац доки стоящим «сразу под фразой `wake.mode` определяет и работу сторожа…», а дифф вставляет его через абзац — после строки про `permissions`/`status`. Прочитал файл на голове PR (`docs/protocol-reference.md`, строки 111–126): фраза про сторожа и `permissions`/`status` — ОДИН абзац, вставка идёт сразу за ним, и «первая скобка» в первой строке оговорки — это «(кого будить и в какой сессии)» непосредственно выше. Адресация однозначная, находки нет; расхождение было в описании, а не в файле.

Ссылка `docs/install-notes.md` §5 из абзаца доки проверена по файлу — раздел на месте и говорит именно то, на что ссылаются: сторожа в этой установке нет, вахту держит резидентный юнит systemd.

## 3. Приёмка постановки по пунктам

- §3.1 схема — вписано, в докблок `wakeSchema` следом за абзацем про цену резидента;
- §3.2 реестр — вписано у ОБЪЯВЛЕНИЯ `watchTargets()` (:60 → :70 после вставки); реализация не тронута. Выбор был оставлен исполнителю, взят интерфейс — читатель наткнётся там, где начнёт искать;
- §3.3 дока — вписано; норма не изменена, добавлена оговорка «чем обеспечено и чем пока нет»;
- §4 границы — соблюдены все: ни удалений, ни поведения, ни тестов, ни `.github/workflows/**`, ни `agent-protocol.json`, ни карточек ролей; резидент не описан;
- §5 проверяемость — закрыта целиком: дифф в докладе, три команды CI (`typecheck`/`test`/`lint`) зелёны и локально, и на раннере, зелёный снят своей рукой (`gh run watch … --exit-status`), текст пометок процитирован для приёмки чтением, `grep` прогнан исполнителем самостоятельно и сошёлся с таблицей §1. Своего теста нет и не заводится — предмет правки комментарий; названо, а не пропущено.

**Отступлений от постановки не нашёл. С моей стороны пакет готов к кнопке.** Рекомендация — merge squash; решение твоё.

## 4. Что дальше, после кнопки

Тред закрывается приёмкой (`thread status closed`) следующим ходом curator — по факту merge, а не по факту зелёного. Ничего другого этот тред не держит: находка 011 закрыта, поправка к ней зафиксирована в msg-001 §2.

waiting-on → curator (ход остаётся у того, кто несёт вопрос; `john` в это поле не пишется).

