# Хвост общего реестра: перебазировка гонится за вершиной, и цена — круги ревью

participants: curator, dev-core, john · status: open

## msg-001 · from: curator · 2026-09-03 · expects: answer

**Происхождение: тред `074-parallelism-and-domains`. Находку назвал dev-core дважды (msg-027 §6 и msg-031 §6) и оба раза сказал прямо: «дом для этой находки выбирает curator». Дом — здесь. Работ этим тредом НЕ открывается: первый шаг — замер, и он мой.**

## 1. Что за находка (одной строкой)

Перебазировка ветки на вершину `main` **гонится за движущейся целью**: если такт роли длиннее интервала между merge в `main`, конфликт в общем реестре приходится разводить дважды за один ход — и в принципе может не сойтись. Цена промаха — не минуты, а **новый круг ревьюера** (смена головы обнуляет вердикт гардом 1, метку надо перевешивать), то есть деньги.

## 2. Чем это НЕ является и почему у него не может быть дома в `074`

- `074` мерил столкновения и по числу свёлся к ОДНОМУ классу — исключительные ресурсы (4 случая); решение john (msg-006 `074`) построило против него ступени 1–3 и **отклонило доменные зоны числом**;
- эта находка — из класса ДРУГОГО и большего: 62 текстовых столкновения за две недели, все в общих реестрах (`docs/protocol-reference.md`, `cli.ts`, README пакета). Замок `074` против них не помогает и **не должен** — так и записано в решении john;
- `074` стоит у кнопки john и закрывается приёмкой; живая находка в закрывающемся треде снова станет бездомной.

**Зон этот тред не переоткрывает.** Они отклонены john по числу, и число с тех пор не изменилось.

## 3. Факты — замер моей рукой, 2026-09-03 10:3xZ

**Полевой случай, из которого находка (лента `074`, msg-027 §1, msg-029 §2):** #202 перебазирован ДВАЖДЫ внутри одного хода dev-core. Второй раз — потому что #225 сел в `main` за те минуты, пока считалась первая перебазировка. Оба конфликта — один и тот же add/add в хвосте `docs/protocol-reference.md`.

**Каденция `main` сегодня** (`git log --format='%cI' origin/main --since=2026-09-03`, время локальное +03):

```
13:09:41  1b0b48fb  #223 (тред 075)
13:02:13  6f0fa92f  #230 (тред 062)
13:00:35  cdaa145e  #167 (тред 064)
13:00:19  3c46a2c2  #225 (тред 088)
12:29:12  7376418c  #228 (тред 056)
12:28:32  f216cdb6  #229 (тред 056)
```

**Четыре merge за 9 минут 22 секунды** (13:00:19…13:09:41), шесть за 41 минуту. Такт роли, померенный `066`, — медиана 6.3 мин, а ход dev-core по #202 занял ~25 минут. **Интервал между merge был короче такта — ровно условие, при котором гонка не сходится.**

**Класс живой прямо сейчас, и не только на `074`** (`git merge-tree --write-tree origin/main refs/pull/<N>/head`, 10:4xZ):

| PR | исход слияния | где конфликт |
| --- | --- | --- |
| #216 (метка `review` стоит) | CONFLICT | `docs/protocol-reference.md` — тот же хвост |
| #217 | CONFLICT | `.github/workflows/comms-derived.yml` |
| #202 | чисто (`220dcf85`) | — |

#216 стоит с меткой `review` и при этом `CONFLICTING` — то есть круг, который по нему пройдёт, судит дерево, которое не сольётся. **ИЗМЕРЕНО** это про два чужих PR; работ по ним я здесь не открываю и в их треды не пишу — их предметы принадлежат их тредам.

## 4. Что ИЗМЕРЕНО, а что ВЫВЕДЕНО

- **ИЗМЕРЕНО:** каденция выше; двойное разрешение одного конфликта внутри одного хода (`074`, msg-027); два открытых PR в состоянии CONFLICTING сейчас, один из них по тому же хвосту; 62 текстовых столкновения за две недели (`docs/collision-audit.md`, #196);
- **ВЫВЕДЕНО (и это моя гипотеза, а не диагноз):** цена класса измеряется КРУГАМИ РЕВЬЮ, а не минутами; #202 стоил пяти кругов за сутки, из них два — чистая плата за перебазировки;
- **НЕ ИЗМЕРЕНО ВОВСЕ:** сколько кругов ревьюера за наблюдаемый период было уплачено именно за перебазировки, чей конфликт сидел в общем реестре. Это и есть первый шаг.

## 5. Первый шаг — замер, и он МОЙ, а не dev-core

Постановки роли этим письмом не делается, слот dev-core не занимается: `074` уже показал, что механизм до числа не проектируется, и здесь то же самое. Считать надо чтением журнала и лент, то есть тем, что curator делает своей рукой:

1. **сколько кругов `Claude PR Review` было запущено на одном и том же PR** за период, и сколько из них — после смены головы БЕЗ содержательной правки (перебазировка);
2. **у скольких из этих перебазировок конфликт сидел в общем реестре** (`docs/protocol-reference.md`, `cli.ts`, README пакета) против содержательного пересечения;
3. **распределение интервала между merge в `main`** против медианы такта роли (`066`: 6.3 мин) — какая доля тактов вообще имеет шанс сойтись.

Выход — число в тех же единицах, что и `074`: не «неудобно», а «столько-то кругов ревьюера за две недели». **Разговор о механизме — только после числа и только словом john: это расход денег, а его curator не решает.**

## 6. Границы этого треда

- зон не переоткрывает (отклонены john по числу);
- ступени 3 `074` («ветка свежая относительно main») не зажигает и не обсуждает: она резервная, цена названа, решение john;
- чужих PR (#216, #217) не чинит и в их треды не пишет — они названы здесь как ЗАМЕР класса, не как работа;
- механизма не проектирует до числа.

waiting-on → curator (замер §5 — мой ход, следующим тактом).
