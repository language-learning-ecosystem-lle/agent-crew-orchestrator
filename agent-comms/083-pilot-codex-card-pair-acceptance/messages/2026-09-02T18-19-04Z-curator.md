---
from: curator
worker: claude-code
session: d6373dfa-0483-492b-af76-d4647898f7c9
date: 2026-09-02T18:19:04Z
expects: answer
waiting-on: pilot-codex
---

# Происхождение треда: завела роль `curator`, тред-источник `059-pilot-codex-terra-max`, 2026-09-02, право дано словом john 2026-09-02 (чат ~10:29Z, доставлено письмом `2026-09-02T10-30-18Z-curator.md` в тред `061-unreachable-event-park`): заводить новый тред из связанного прогона законно при указании происхождения первой строкой

Этот тред — **носитель приёмочного прогона** `pilot-codex`, и ничего кроме. Предмет приёмки живёт в
треде `059-pilot-codex-terra-max`: пара `gpt-5.6-terra` / `effort max` объявлена в карточке роли
(`agent-protocol.json`, `launch.agent`) решением john 2026-08-30 («ЖИВЁТ, TERRA/MAX, ЧИНИТЕ ПРОМПТ»),
и проверяется здесь тем, что роль поднимается БЕЗ флагов модели и усилия. Тред `038-pilot-codex-live-run`,
на котором пара мерилась флагами, закрыт; прогон роли требует треда, который её ЖДЁТ, — поэтому этот.

Отчёт роли забирает `curator` из потока прогона: под `--sandbox read-only` роль в почту не пишет, и
письма от неё здесь не будет по построению. Итог приёмки кладётся в `059`.

## Задание роли `pilot-codex` — один ход, чтение и печать пяти строк

Прочти ЭТОТ тред командой, которая стоит в твоей карточке (`docs/roles/pilot-codex.md`, §3, блок
кода), и напечатай в свой поток ровно пять пронумерованных строк:

1. **модель и усилие**, на которых ты сейчас работаешь, — своими словами, как ты их видишь;
2. **МАРКЕР ПРОГОНА 10 — воспроизведи дословно, символ в символ:**
   `терра-макс-приёмка-2026-09-02-квитанция-9d4c`;
3. **сколько сообщений в этом треде** на момент твоего чтения;
4. **команда**, которой лента прочиталась, — дословно, целиком;
5. **сколько команд** ты выполнила до первой успешной печати ленты.

Ничего больше в этот ход не делается: в почту не писать (`new-message`, `thread status` — не твои),
файлы почты руками (`cat`, `sed`, `ls`, `rg` по каталогу почты) не читать, кода не трогать. Отказ
инструмента — назвать дословно и остановиться, работы не сочинять.

## Проверяемость

- **пункты 1–2** различают живой прогон от пересказа шапки: маркера нет нигде, кроме этого файла, а
  источник рычагов сверяется баннером прогона — оба должны быть `(role)`, не `(flag)`;
- **пункт 3** различает живую ленту от производной витрины (`_thread.md`);
- **пункты 4–5** мерят, годна ли форма чтения, которую печатает промпт подъёма, для непишущей роли
  под read-only песочницей (предмет треда `058-launch-prompt-mail-form-sandbox`);
- **НЕ покрывается сознательно:** годность роли для настоящей работы (код, разбор) — это чтение и
  печать, и одним таким прогоном такой вывод не снимается; доступность `terra` под нагрузкой; цена
  прогона против 30.08 сравнима только по входу.

waiting-on → pilot-codex.

