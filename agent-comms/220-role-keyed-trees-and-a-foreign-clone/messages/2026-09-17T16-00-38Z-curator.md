---
from: curator
worker: claude-code
session: 943a0e02-1fb2-4dc4-927e-643eb6d2ba39
raised: 2026-09-17T15:53:31Z
date: 2026-09-17T16:00:38Z
expects: none
waiting-on: curator
---

**ИСПОЛНЕНО РУКОЙ JOHN И СВЕРЕНО. Снятое снято, восстановимое восстановимо — тред закрываю приёмкой. И одна поправка, которую обязан назвать: цена остатков была не `199M`, а `47M` — заголовок этого треда завышал её вчетверо.**

Рука john шла параллельно этой сессии: первый мой замер в `15:53Z` застал все пять мест на диске, второй в `15:56Z` — уже ни одного. Поэтому чтение ДО снятия (шаг 1 порядка msg-002 §2) и сверка якорей ПОСЛЕ уложились в один такт.

## 1. Что снято — по факту, а не по плану

**Деревьев снято ЧЕТЫРЕ, а не три.** Сверх решения msg-002 §1 снято `.worktrees/pilot-codex`. Называю это отдельной строкой, потому что решение его не называло; никакой беды в этом нет — оно ровно того же класса (имя без треда, в опись попадало тем же `not a pair`), было чисто и якорь под ним стоит. Но запись обязана говорить, что произошло, а не что было решено.

| место | голова, прочитанная ДО снятия | `status --porcelain` | якорь ПОСЛЕ снятия |
| --- | --- | --- | --- |
| `.worktrees/curator` | `61a79818f1190cd97ed86bd8ea4cb57ad7c540c4` | пуст | `refs/tidy/curator` → тот же sha ✓ |
| `.worktrees/dev-core` | `9935a9b0c14a67ad5ea92e897f9db69a00388aa6` | пуст | `refs/tidy/dev-core` → тот же sha ✓ |
| `.worktrees/devops` | `2f17b9cd1c7e54e802da8e412a6032fa497c1ed0` | пуст | `refs/tidy/devops` → тот же sha ✓ |
| `.worktrees/pilot-codex` | `2f62e23a7…` (из `worktree list` до снятия) | — | `refs/tidy/pilot-codex` → `2f62e23a7f5d3c96363b1c35cf039a27c047d661` ✓ |
| `.worktrees/acc-180-374` | `2531e3b82` «acc-374 merged tree», чисто | пуст | якоря нет — см. §3 |

Ни в одном дереве незалитой работы не было: все четыре чисты, головы — смёрженные коммиты `main` (#362, #366, #348). `git worktree list`: **22 строки → 18**, `prune` отработал, висячих записей не осталось. Опись чиста от имён без треда по факту: в выводе `orchestrator status` не осталось ни одной строки `tidy-up: not a pair` — все оставшиеся места названы парой `<роль>@<тред>`.

## 2. Цена — поправка, и она существенна для чтения этого треда задним числом

Замер перед снятием, маржинально (`du` по корню контура минус `du` по нему же без кандидата):

| предмет | как названо в msg-001 | на самом деле |
| --- | --- | --- |
| три дерева старого образца | `35M` | **`35M`** — верно |
| `.worktrees/acc-180-374` | `164M` | **`12M`** |
| итог | **`199M`** | **`47M`** |

Первое число было маржинальным (его дала сухая опись уборки) и оказалось точным. Второе я взял из треда `218`, msg-016 §5.2 — там это `du -sh` по одному каталогу, и оно артефакт: `du -sh` по любому из этих мест печатает `163–164M`, но `158M` из них — жёсткие ссылки в общий склад pnpm (`/home/aco-hetzner/.local/share/pnpm/store/v10`, **вне репозитория**). У пробного файла `lightningcss.linux-x64-gnu.node` — **22 ссылки**. Склад переживает снятие любого числа деревьев, освобождается почти только сольная часть — `9M` на место.

**Класс ошибки тут не «сложили аддитивно» — это в моей памяти уже лежало.** Класс тоньше: **в одну сумму сложены число МАРЖИНАЛЬНОЕ и число АВТОНОМНОЕ**, добытые разными инструментами в разных тредах с разницей в пять дней. Каждое по отдельности защитимо, сумма — нет, и ошибается она ровно в ту сторону, в которую принималось решение: `199M` звучит как повод потратить руку владельца, `47M` — как повод не тратить. Правило записано в журнал роли и в память: перед сложением всё пересчитывается ОДНИМ способом.

Сверка по факту: репозиторий целиком `2613M → 2530M`. Это НЕТТО — внутри того же окна поднялись две сессии (`curator×220`, `curator×221`), каждая со своим новым рабочим местом; остаток сверх предсказанных `47M` относится к `pilot-codex`, которое я маржинально померить не успел — рука сняла его раньше замера. Диск ящика: `1.3T` свободных из `1.9T`, как и было.

**Это не упрёк решению и не повод его пересматривать** — решение исполнено, места сняты, вернуть их нечем и незачем. Это поправка к ЧИСЛУ, по которому такое решение будет приниматься в следующий раз.

## 3. Восстановимость — по местам, а не одним словом

- **четыре дерева** — восстановимы: якоря стоят и совпадают с головами, прочитанными до снятия (таблица §1). Восстановление — `git worktree add <путь> refs/tidy/<имя>`;
- **чужой клон** — якоря у него нет и быть не могло. Но его голова `2531e3b82` (merge-снимок приёмочного прогона PR #374 от 12.09, автор `github-actions[bot]`) лежит в складе объектов контура **висячим** объектом: ссылки на неё нет, `git gc` её выметет. По существу терять нечего — оба родителя достижимы (`bbab2d9a2` ← `refs/heads/179-narrow-failover-line-to-transition`, `9b35aee08` ← `refs/heads/dev-core/180-exhausted-folds-to-one`), дерево пересобирается их слиянием. **Пока объект жив, он закрепляется одной строкой: `git update-ref refs/tidy/acc-180-374 2531e3b82`.** Своей рукой не написал намеренно: `refs/tidy/*` — пространство автоматики, и имя, которое никогда не было рабочим местом роли, соврёт следующему читателю описи. Скажете «закрепи» — закреплю; промолчите — объект уйдёт со следующим `gc`, и это законный исход.

## 4. Норма не двинулась

Критерий уборки треда `218` НЕ расширен: имена без треда остаются вне его, форма 2 развилки (новая норма + PR + кнопка) отклонена. Появятся такие имена снова — опись назовёт их честно, а руку опять придётся звать.

## 5. Побочное следствие msg-002 §3 — состоялось

У пары `curator×220` рабочего места не было; снятие парка сделало её поднимаемой, и этот подъём создал дерево с нуля в `15:53:32Z` — `node_modules` в нём создан тем же моментом. То же у `curator×221` в `15:54:18Z`. Срабатывание, которого ждёт приёмка треда `221`, произошло; доказательство лежит в моей ленте, но предмет чужой, и разбор его — ход сессии `221`, которая в эту минуту жива со своим свежим местом. Сюда это пишу одной строкой, чтобы не потерялось, а не чтобы принять.

## 6. Хвост

Открытых PR нет (`gh pr list --state open` → пусто) — домерживать нечего. Запись журнала по этому треду отправлена командой (`journal/curator/220-role-keyed-trees-and-a-foreign-clone.md`), поправка про два способа замера легла в память роли.

**Тред закрываю приёмкой этим же ходом.** Предмет исчерпан: остатки сняты, восстановимость названа по местам, норма не тронута. Открытым остаётся ровно один необязательный вопрос — закреплять ли висячий `2531e3b82` (§3); если он вам нужен, откройте тред заново или скажите в любом другом — одна строка, и она моя.

waiting-on → curator.

