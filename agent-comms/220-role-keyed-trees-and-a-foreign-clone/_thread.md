# 199M, до которых уборка не достаёт: три дерева старого образца и чужой клон acc-180-374

participants: curator, john · status: open

## msg-001 · from: curator · 2026-09-17 · expects: ack

**ТРЕБУЕТСЯ РЕШЕНИЕ JOHN: забираем ли 199M, до которых уборка не достаёт по построению — три дерева старого образца (`curator`, `dev-core`, `devops`, 35M) и чужой клон `.worktrees/acc-180-374` (164M)? И если да — чьей рукой.**

Тред заведён затем, чтобы эти два остатка не умерли молча вместе с тредом `218`, который сегодня закрыт приёмкой. Ни один из них не является дефектом и ни один не горит: диск ящика — `1.3T` свободных из `1.9T`. Вопрос ровно один — считаете ли вы эти 199M стоящими вашего касания.

## 1. Почему автоматика до них не достаёт — по построению, а не по недоделке

Критерий уборки, который вы приняли `2026-09-17` (тред `218`, норма «Уборка мёртвых деревьев пары» в `PROTOCOL.md`), говорит о паре `<роль>@<тред>` с ЗАКРЫТЫМ тредом. Отсюда:

- **три дерева старого образца** — `curator`, `dev-core`, `devops` — названы одной ролью без треда. Закрывать нечего, и опись говорит это вслух: `tidy-up: not a pair — keyed by the role alone … this name carries no thread to close`. При `parallelism.pairsPerRole = 2` ни один прогон в них больше не сядет — они мертвы фактически, но не по критерию;
- **`.worktrees/acc-180-374`** — вообще не рабочее место роли, а отдельный клон со своим `.git` (остаток приёмочного прогона PR #374 от 12.09). Опись честно говорит `not any role's workspace — nothing here is claimed about it`. Якорь `refs/tidy/*` к нему неприменим: рефы контура ничего не держат в чужом репозитории, и снимается он не `git worktree remove`, а `rm -rf`. **Это другой класс необратимости, чем тот, что вы решили в `218`**, и своей рукой роль его не трогает.

## 2. Цена — машиной, маржинально

- `role-keyed trees (3): 35M would be freed while the other 67 registered place(s) stand` — замер `orchestrator status` сегодня, `12:30Z`;
- `acc-180-374` — `164M`, дерево чистое (замерено в треде `218`, msg-016 §5.2);
- вместе — `199M`. Для сравнения: уборка `218` за первые полчаса живого кода взяла `30` деревьев и `~300M`, и продолжает идти сама.

## 3. Формы ответа — любая одна строкой, и я исполню

1. **«не трогать»** — тред закрою, оба места останутся, опись будет и дальше называть их честно. Это законный исход, и он дешевле остальных;
2. **«снять три дерева старого образца»** — это НОВАЯ норма (расширение критерия на имена без треда), то есть ваша кнопка и PR у `dev-core`, а не моя рука;
3. **«снять три дерева старого образца РАЗОВО, рукой, без нормы»** — тогда назовите руку: моя (`git worktree remove` × 3, с якорем `refs/tidy/*` тем же жестом, что у автоматики) или ваша;
4. **`acc-180-374` — только ваша рука** либо ваше явное слово мне на `rm -rf` чужого клона: необратимость здесь не покрыта ни одним якорем контура.

Своей рукой до вашего слова не удаляю ничего — ни дерева, ни рефа, ни каталога.

waiting-on → curator (вопрос несу я, отвечаете вы).

## msg-002 · from: curator · 2026-09-17 · expects: answer

**СЛОВО JOHN (сессия чата, 2026-09-17 ~15:55Z): ОСТАТКИ УБИРАЕМ, РУКОЙ JOHN. Форма 3 + форма 4 развилки msg-001 §3. Парк снимаю этим письмом.**

## 1. Что решено

- **три дерева старого образца** (`curator`, `dev-core`, `devops`, `35M`) — **снимаются**, разово, **рукой john**, БЕЗ расширения нормы. Форма 2 (новая норма + PR + кнопка) отклонена john как несоразмерная одноразовой уборке;
- **`.worktrees/acc-180-374`** (`164M`, чужой клон) — **удаляется рукой john**, `rm -rf` + `git worktree prune`. Иначе и нельзя: якорь `refs/tidy/*` к чужому репозиторию неприменим, и роль этого не трогает.

**Критерий уборки из `218` НЕ расширяется.** Имена без треда остаются вне его; опись продолжает называть их честно, если такие появятся снова.

## 2. Порядок жестов, отданный john в чат — с якорем впереди снятия

1. **чтение** — по каждому из трёх: `rev-parse HEAD`, `status --porcelain`, `log --oneline -1`; непустое состояние = незалитая работа, дерево не снимается, разбор вместо уборки;
2. **якорь → снятие**, по одному дереву: `git update-ref refs/tidy/<роль> <sha>` и затем `git worktree remove`. Порядок тот же, что у автоматики: якорь раньше снятия, иначе необратимо;
3. **чужой клон** — `rm -rf` + `git worktree prune`, с замером `du -sh` до и после.

**На момент письма жесты ещё не исполнены** — john их запросил и получил. Факт исполнения с хешами якорей ляжет сюда следующим письмом; до него ничего не считается снятым.

## 3. Побочное следствие, ради которого парк снят именно сейчас

У пары `curator×220` **нет рабочего дерева**. Снятие парка делает её поднимаемой, первый подъём создаст дерево с нуля — и это ровно то срабатывание, которого ждёт приёмка треда `221` (выравнивание при установке). Приёмка приедет даром, побочным следствием этой работы; отдельного заказа она не требует. Слово john доставлено и в `221` тем же ходом.

## 4. Ход

**За curator:** записать исполнение (хеши якорей, `du` до/после, счёт деревьев) и закрыть тред приёмкой — снятое снято, восстановимое восстановимо.
**За john:** сами жесты, если он не передумает по дороге; отказаться в любой момент законно, «не трогать» остаётся годным исходом.

waiting-on → curator.

## msg-003 · from: curator · 2026-09-17 · expects: answer

**ИСПОЛНЕНО РУКОЙ JOHN (~15:56Z): четыре дерева старого образца сняты с якорями, чужой клон удалён. Замер до/после разошёлся с ожиданием — это в §3, и это находка, а не ошибка исполнения.**

## 1. Что снято — с якорями, поимённо

| дерево | коммит | якорь |
| --- | --- | --- |
| `curator` | `61a79818f` | `refs/tidy/curator` |
| `dev-core` | `9935a9b0c` | `refs/tidy/dev-core` |
| `devops` | `2f17b9cd1` | `refs/tidy/devops` |
| **`pilot-codex`** | `2f62e23a7` | `refs/tidy/pilot-codex` |

**Четвёртое — сверх решения msg-002:** `pilot-codex` того же класса (имя роли без треда), в замер `35M` не входило, снято тем же жестом и тем же порядком. Называю это прямо, чтобы список решения и список исполнения не разошлись молча.

**Чтение перед снятием** (по каждому: `rev-parse HEAD` + `status --porcelain`): у всех четырёх рабочее дерево ЧИСТО, незакоммиченного нет. Порядок «якорь → снятие» соблюдён на каждом: любое восстановимо из своего рефа.

**Занятые деревья не тронуты:** `curator@220` и `curator@221` помечены `locked` — в них живут сессии.

## 2. Чужой клон

`.worktrees/acc-180-374` удалён (`rm -rf` + `git worktree prune`). Якоря у него нет и быть не могло — рефы контура ничего не держат в чужом репозитории; **это необратимо**, и john пошёл на это сознательно. Реестр после `prune` чист: каталогов в `.worktrees/` — **17**.

## 3. НАХОДКА: замер освобождения не сошёлся — `12M` вместо `164M`

```
до  rm -rf: 599M    .worktrees/
после:      587M    .worktrees/
```

А сам клон мерился в `164M` (замер msg-001 §2, `du -sh .worktrees/acc-180-374`, подтверждён повторно сегодня).

**Разница — не в исполнении: каталог удалён, реестр чист.** Правдоподобное объяснение — **жёсткие ссылки**: pnpm связывает `node_modules` с общим хранилищем, а `du` в одном обходе считает такой файл ОДИН раз. Отдельный замер каталога насчитывает ему все его файлы; замер целого `.worktrees/` приписывает их первому встреченному дереву. Тогда «164M» — это не то, что освободится, а то, что каталог занимал бы в одиночестве.

**Это версия, а не факт: я её не проверял.** Проверяется одной командой (`du --count-links`, или `df` до/после). Важно другое, и это уже факт: **оценки освобождения по `du -sh <каталог>` завышены для всего, что несёт `node_modules`** — включая цифры уборки деревьев, которыми мы хвалились в `218`. Следующий такт, который будет считать выигрыш, обязан мерить иначе.

## 4. Что этим закрыто и что осталось

Предмет треда исполнен целиком: остатки, до которых уборка не достаёт по построению, убраны рукой john, норма `218` не расширялась. **За john по этому треду — ничего.**

**За curator:** назвать судьбу находки §3 — либо перемерить выигрыш честным способом и поправить числа в `218`, либо объявить, что перемер не стоит такта. Закрытие треда — после этого.

waiting-on → curator.
