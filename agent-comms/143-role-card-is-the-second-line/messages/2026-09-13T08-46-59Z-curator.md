---
from: curator
worker: claude-code
session: 92689424-a2c1-4ba2-a186-2e790187fa52
raised: 2026-09-13T08:42:15Z
date: 2026-09-13T08:46:59Z
expects: none
waiting-on: curator
---

**Неделя прошла — карточка НЕ выросла обратно: 0 коммитов, 0 знаков. Но и выход переезда оказался не 2 752 знака, а 548, и прибор из §3 п.4 показаний не снимает вовсе — это замеренный дефект.**

Проверка, объявленная в моём письме 12.09 («проверка решения не в том, сколько ушло сегодня, а в том, вырастет ли карточка обратно за неделю»), сделана. Мерил git'ом: `git show <sha>:docs/roles/curator.md | wc -m` по серии коммитов, время — `date -u`, замер 2026-09-13 08:43Z на `origin/main` = `c4ed32c98`.

## 1. Правило прироста держится — это главный результат

| момент | коммит | знаков в карточке |
| --- | --- | --- |
| 30.08 14:44Z | `c5127d530` | 19 087 |
| **06.09 15:12Z** (последний рост до решения) | `6cd904f40` | **24 717** |
| 09.09 18:03Z (#350 сел) | `d6909ebd8` | 24 169 |
| **13.09 08:43Z** (сейчас) | `c4ed32c98` | **24 169** |

**С момента мёржа в `docs/roles/**` не легло НИ ОДНОГО коммита** (`git log --since=2026-09-09T18:03:27 origin/main -- docs/roles/` — пусто). Решение доставлено 06.09 17:27Z, сейчас 13.09 08:43Z: **6,6 суток из семи, прирост 0 знаков** против **+5 630 (+29,5 %) восемью коммитами** за предыдущую такую же неделю. Довод john («экономию даёт не сокращение, а правило о том, что в карточку класть») своей проверкой пока подтверждён.

**Куда ушёл рост, который раньше шёл в карточку:** `PROTOCOL.md` за то же окно — 103 300 знаков (06.09 16:50Z) → 111 693 (09.09 18:03Z), **+8 393 знака четырьмя коммитами**. `PROTOCOL.md` в `base` не едет, то есть рост переехал туда, где не платится каждый такт. **Оговорка, и она моя, а не факта:** я не читал каждый из четырёх диффов на вопрос «а не должна ли была строка попасть в карточку» — это отдельное чтение, и «правило работает» здесь означает «карточка не растёт», а не «ни одна несущая строка мимо не проехала».

## 2. Экономия числом — и число меньше объявленного впятеро

**Нетто переезда — −548 знаков (−2,2 %), а не −2 752 (−11,1 %): 20 % от плана.**

Замер по диффу `d6909ebd8` (`docs/roles/curator.md`, 28 строк добавлено / 30 удалено): знаков в удалённых строках **3 005**, в добавленных **2 456**. Причина разрыва видна в самом диффе и она не ошибка исполнителя: **фрагменты не вырезались, а ПЕРЕПИСЫВАЛИСЬ** — на месте каждой вынесенной летописи осталась отсылающая проза («происхождение права — журнал роли», «полевой случай, из которого строка, — журнал роли»), а сверх того в карточку легло само правило прироста, которого раньше не было.

Деньги: **≈$0,9/неделю вместо объявленных ≈$4,7** — это производная арифметика от опубликованного γ = 0,6566 токена на знак, **не показание прибора** (почему — §3).

Развилкой это не считаю и решения не прошу: критерий был смысловым, а экономия — следствием, так john и решил. Фактом называю, потому что «перемерить прибором после — экономия числом, а не обещанием» был пункт порядка, и честное число такое. Если когда-нибудь захочется остальные 80 %, это новый PR по доку власти, то есть твоя кнопка, и заводить его сам я не буду.

## 3. Прибор §3 п.4 сломан — полевой дефект, замерен

`scripts/base-cost.mjs` (из #286) сегодня не даёт числа **ни на каком окне**:

- `node --import tsx scripts/base-cost.mjs --since 2026-09-07` → падает целиком:
  `ProtocolVersionError: 'agent-protocol.json' at 9c246a914: the repository declares protocol version 25, the package writes 27`
  (`schema/version.ts:136` ← `config/load.ts:218` ← `base-cost.mjs:240 configAt`);
- `--since 2026-09-12` → доходит до подгонки и отвечает `подгонка не сошлась — система вырождена`.

**Класс дефекта, а не случай:** прибор восстанавливает конфиг такта через `loadProtocolConfig`, а та требует `protocolVersion` РОВНО текущей версии пакета. Значит **каждый бамп схемы обнуляет всю историю прибора**: бампы v26 `f9562234c` (07.09 17:35Z) и v27 `2f17b9cd1` (09.09 14:33Z) отрезали ему всё, что до 09.09 14:33Z, — а окно короче четырёх суток вырождает МНК. Прибор бесполезен ровно тогда, когда нужен.

Починка на вид узкая (историческому чтению нужен `instructions`, а не текущая схема), но это **код, и он не моей рукой**: ставится задачей `dev-core`. Своего треда у предмета нет, а этот прогон связан тредом `143` и только им, поэтому постановку я здесь не открываю — называю её готовой к открытию: предмет «`base-cost.mjs` читает исторический конфиг снисходительно к версии схемы», первоисточник замера — это письмо, приёмка — прогон `--since 2026-09-01` даёт таблицу, а не стек.

## 4. Что дальше и кто ходит

Предмет треда 143 — «резать ли карточку и по какому критерию» — **исполнен и проверен**: решение принято, #350 сел кнопкой john 09.09, недельная проверка снята и пройдена. Закрывать тред сейчас не стал: закрытие — приёмка, а приёмку хочу поставить одним ходом вместе с открытием треда прибора, иначе хвост останется без владельца.

**Ход у curator.** Следующая сессия на этом треде делает ровно два действия: открывает тред под постановку `dev-core` о `base-cost.mjs` (§3) и закрывает 143.

waiting-on → curator.

