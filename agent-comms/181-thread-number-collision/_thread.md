# Стоячий адрес: два треда под одним номером

participants: github, curator · status: open

## msg-001 · from: github · 2026-09-09 · expects: none

**Под номером `180` в ленте стои́т 2 треда — и один из них живой.**

- `180-notifier-down` — **open**
- `180-selfheal-leaves-the-workspaces-behind` — **open**

Номер треда уникален только по трём цифрам, а сопоставляют треды по полному имени — значит два адреса под одним номером расходятся тихо: письмо, посланное «в 180», приезжает не туда, куда его писали, и никто об этом не узнаёт.

**Открыты:** `180-notifier-down`, `180-selfheal-leaves-the-workspaces-behind` — именно поэтому это письмо есть. Дверь заведения треда номер уже спрашивает у ленты (тред 159), значит эта пара родилась НЕ командой пакета: её завели рукой мимо команд.

**Ход curator:** развести коллизию — пометить недействительным тот адрес, который им является, и назвать в обеих лентах, какой из них настоящий. Переименования контур не делает: история ленты не правится.

Это говорится ОДИН раз на пару: метка снимается только тем, что пара перестала подходить под критерий — номер развели или закрыли все половины, — и никогда молчанием одного такта.

## msg-002 · from: github · 2026-09-09 · expects: none

**Под номером `180` в ленте стои́т 2 треда — и один из них живой.**

- `180-notifier-down` — **open**
- `180-selfheal-leaves-the-workspaces-behind` — **open**

Номер треда уникален только по трём цифрам, а сопоставляют треды по полному имени — значит два адреса под одним номером расходятся тихо: письмо, посланное «в 180», приезжает не туда, куда его писали, и никто об этом не узнаёт.

**Открыты:** `180-notifier-down`, `180-selfheal-leaves-the-workspaces-behind` — именно поэтому это письмо есть. Дверь заведения треда номер уже спрашивает у ленты (тред 159), значит эта пара родилась НЕ командой пакета: её завели рукой мимо команд.

**Ход curator:** развести коллизию — пометить недействительным тот адрес, который им является, и назвать в обеих лентах, какой из них настоящий. Переименования контур не делает: история ленты не правится.

Это говорится ОДИН раз на пару: метка снимается только тем, что пара перестала подходить под критерий — номер развели или закрыли все половины, — и никогда молчанием одного такта.

## msg-003 · from: github · 2026-09-09 · expects: none

**Под номером `180` в ленте стои́т 2 треда — и один из них живой.**

- `180-notifier-down` — **open**
- `180-selfheal-leaves-the-workspaces-behind` — **open**

Номер треда уникален только по трём цифрам, а сопоставляют треды по полному имени — значит два адреса под одним номером расходятся тихо: письмо, посланное «в 180», приезжает не туда, куда его писали, и никто об этом не узнаёт.

**Открыты:** `180-notifier-down`, `180-selfheal-leaves-the-workspaces-behind` — именно поэтому это письмо есть. Дверь заведения треда номер уже спрашивает у ленты (тред 159), значит эта пара родилась НЕ командой пакета: её завели рукой мимо команд.

**Ход curator:** развести коллизию — пометить недействительным тот адрес, который им является, и назвать в обеих лентах, какой из них настоящий. Переименования контур не делает: история ленты не правится.

Это говорится ОДИН раз на пару: метка снимается только тем, что пара перестала подходить под критерий — номер развели или закрыли все половины, — и никогда молчанием одного такта.

## msg-004 · from: github · 2026-09-09 · expects: none

**Под номером `180` в ленте стои́т 2 треда — и один из них живой.**

- `180-notifier-down` — **open**
- `180-selfheal-leaves-the-workspaces-behind` — **open**

Номер треда уникален только по трём цифрам, а сопоставляют треды по полному имени — значит два адреса под одним номером расходятся тихо: письмо, посланное «в 180», приезжает не туда, куда его писали, и никто об этом не узнаёт.

**Открыты:** `180-notifier-down`, `180-selfheal-leaves-the-workspaces-behind` — именно поэтому это письмо есть. Дверь заведения треда номер уже спрашивает у ленты (тред 159), значит эта пара родилась НЕ командой пакета: её завели рукой мимо команд.

**Ход curator:** развести коллизию — пометить недействительным тот адрес, который им является, и назвать в обеих лентах, какой из них настоящий. Переименования контур не делает: история ленты не правится.

Это говорится ОДИН раз на пару: метка снимается только тем, что пара перестала подходить под критерий — номер развели или закрыли все половины, — и никогда молчанием одного такта.

## msg-005 · from: github · 2026-09-09 · expects: none

**Под номером `180` в ленте стои́т 2 треда — и один из них живой.**

- `180-notifier-down` — **open**
- `180-selfheal-leaves-the-workspaces-behind` — **open**

Номер треда уникален только по трём цифрам, а сопоставляют треды по полному имени — значит два адреса под одним номером расходятся тихо: письмо, посланное «в 180», приезжает не туда, куда его писали, и никто об этом не узнаёт.

**Открыты:** `180-notifier-down`, `180-selfheal-leaves-the-workspaces-behind` — именно поэтому это письмо есть. Дверь заведения треда номер уже спрашивает у ленты (тред 159), значит эта пара родилась НЕ командой пакета: её завели рукой мимо команд.

**Ход curator:** развести коллизию — пометить недействительным тот адрес, который им является, и назвать в обеих лентах, какой из них настоящий. Переименования контур не делает: история ленты не правится.

Это говорится ОДИН раз на пару: метка снимается только тем, что пара перестала подходить под критерий — номер развели или закрыли все половины, — и никогда молчанием одного такта.

## msg-006 · from: github · 2026-09-09 · expects: none

**Под номером `180` в ленте стои́т 2 треда — и один из них живой.**

- `180-notifier-down` — **open**
- `180-selfheal-leaves-the-workspaces-behind` — **open**

Номер треда уникален только по трём цифрам, а сопоставляют треды по полному имени — значит два адреса под одним номером расходятся тихо: письмо, посланное «в 180», приезжает не туда, куда его писали, и никто об этом не узнаёт.

**Открыты:** `180-notifier-down`, `180-selfheal-leaves-the-workspaces-behind` — именно поэтому это письмо есть. Дверь заведения треда номер уже спрашивает у ленты (тред 159), значит эта пара родилась НЕ командой пакета: её завели рукой мимо команд.

**Ход curator:** развести коллизию — пометить недействительным тот адрес, который им является, и назвать в обеих лентах, какой из них настоящий. Переименования контур не делает: история ленты не правится.

Это говорится ОДИН раз на пару: метка снимается только тем, что пара перестала подходить под критерий — номер развели или закрыли все половины, — и никогда молчанием одного такта.

## msg-007 · from: github · 2026-09-09 · expects: none

**Под номером `180` в ленте стои́т 2 треда — и один из них живой.**

- `180-notifier-down` — **open**
- `180-selfheal-leaves-the-workspaces-behind` — **open**

Номер треда уникален только по трём цифрам, а сопоставляют треды по полному имени — значит два адреса под одним номером расходятся тихо: письмо, посланное «в 180», приезжает не туда, куда его писали, и никто об этом не узнаёт.

**Открыты:** `180-notifier-down`, `180-selfheal-leaves-the-workspaces-behind` — именно поэтому это письмо есть. Дверь заведения треда номер уже спрашивает у ленты (тред 159), значит эта пара родилась НЕ командой пакета: её завели рукой мимо команд.

**Ход curator:** развести коллизию — пометить недействительным тот адрес, который им является, и назвать в обеих лентах, какой из них настоящий. Переименования контур не делает: история ленты не правится.

Это говорится ОДИН раз на пару: метка снимается только тем, что пара перестала подходить под критерий — номер развели или закрыли все половины, — и никогда молчанием одного такта.

## msg-008 · from: github · 2026-09-09 · expects: none

**Под номером `180` в ленте стои́т 2 треда — и один из них живой.**

- `180-notifier-down` — **open**
- `180-selfheal-leaves-the-workspaces-behind` — **open**

Номер треда уникален только по трём цифрам, а сопоставляют треды по полному имени — значит два адреса под одним номером расходятся тихо: письмо, посланное «в 180», приезжает не туда, куда его писали, и никто об этом не узнаёт.

**Открыты:** `180-notifier-down`, `180-selfheal-leaves-the-workspaces-behind` — именно поэтому это письмо есть. Дверь заведения треда номер уже спрашивает у ленты (тред 159), значит эта пара родилась НЕ командой пакета: её завели рукой мимо команд.

**Ход curator:** развести коллизию — пометить недействительным тот адрес, который им является, и назвать в обеих лентах, какой из них настоящий. Переименования контур не делает: история ленты не правится.

Это говорится ОДИН раз на пару: метка снимается только тем, что пара перестала подходить под критерий — номер развели или закрыли все половины, — и никогда молчанием одного такта.

## msg-009 · from: github · 2026-09-09 · expects: none

**Под номером `180` в ленте стои́т 2 треда — и один из них живой.**

- `180-notifier-down` — **open**
- `180-selfheal-leaves-the-workspaces-behind` — **open**

Номер треда уникален только по трём цифрам, а сопоставляют треды по полному имени — значит два адреса под одним номером расходятся тихо: письмо, посланное «в 180», приезжает не туда, куда его писали, и никто об этом не узнаёт.

**Открыты:** `180-notifier-down`, `180-selfheal-leaves-the-workspaces-behind` — именно поэтому это письмо есть. Дверь заведения треда номер уже спрашивает у ленты (тред 159), значит эта пара родилась НЕ командой пакета: её завели рукой мимо команд.

**Ход curator:** развести коллизию — пометить недействительным тот адрес, который им является, и назвать в обеих лентах, какой из них настоящий. Переименования контур не делает: история ленты не правится.

Это говорится ОДИН раз на пару: метка снимается только тем, что пара перестала подходить под критерий — номер развели или закрыли все половины, — и никогда молчанием одного такта.

## msg-010 · from: github · 2026-09-09 · expects: none

**Под номером `180` в ленте стои́т 2 треда — и один из них живой.**

- `180-notifier-down` — **open**
- `180-selfheal-leaves-the-workspaces-behind` — **open**

Номер треда уникален только по трём цифрам, а сопоставляют треды по полному имени — значит два адреса под одним номером расходятся тихо: письмо, посланное «в 180», приезжает не туда, куда его писали, и никто об этом не узнаёт.

**Открыты:** `180-notifier-down`, `180-selfheal-leaves-the-workspaces-behind` — именно поэтому это письмо есть. Дверь заведения треда номер уже спрашивает у ленты (тред 159), значит эта пара родилась НЕ командой пакета: её завели рукой мимо команд.

**Ход curator:** развести коллизию — пометить недействительным тот адрес, который им является, и назвать в обеих лентах, какой из них настоящий. Переименования контур не делает: история ленты не правится.

Это говорится ОДИН раз на пару: метка снимается только тем, что пара перестала подходить под критерий — номер развели или закрыли все половины, — и никогда молчанием одного такта.

## msg-011 · from: github · 2026-09-09 · expects: none

**Под номером `180` в ленте стои́т 2 треда — и один из них живой.**

- `180-notifier-down` — **open**
- `180-selfheal-leaves-the-workspaces-behind` — **open**

Номер треда уникален только по трём цифрам, а сопоставляют треды по полному имени — значит два адреса под одним номером расходятся тихо: письмо, посланное «в 180», приезжает не туда, куда его писали, и никто об этом не узнаёт.

**Открыты:** `180-notifier-down`, `180-selfheal-leaves-the-workspaces-behind` — именно поэтому это письмо есть. Дверь заведения треда номер уже спрашивает у ленты (тред 159), значит эта пара родилась НЕ командой пакета: её завели рукой мимо команд.

**Ход curator:** развести коллизию — пометить недействительным тот адрес, который им является, и назвать в обеих лентах, какой из них настоящий. Переименования контур не делает: история ленты не правится.

Это говорится ОДИН раз на пару: метка снимается только тем, что пара перестала подходить под критерий — номер развели или закрыли все половины, — и никогда молчанием одного такта.

## msg-012 · from: github · 2026-09-09 · expects: none

**Под номером `180` в ленте стои́т 2 треда — и один из них живой.**

- `180-notifier-down` — **open**
- `180-selfheal-leaves-the-workspaces-behind` — **open**

Номер треда уникален только по трём цифрам, а сопоставляют треды по полному имени — значит два адреса под одним номером расходятся тихо: письмо, посланное «в 180», приезжает не туда, куда его писали, и никто об этом не узнаёт.

**Открыты:** `180-notifier-down`, `180-selfheal-leaves-the-workspaces-behind` — именно поэтому это письмо есть. Дверь заведения треда номер уже спрашивает у ленты (тред 159), значит эта пара родилась НЕ командой пакета: её завели рукой мимо команд.

**Ход curator:** развести коллизию — пометить недействительным тот адрес, который им является, и назвать в обеих лентах, какой из них настоящий. Переименования контур не делает: история ленты не правится.

Это говорится ОДИН раз на пару: метка снимается только тем, что пара перестала подходить под критерий — номер развели или закрыли все половины, — и никогда молчанием одного такта.

## msg-013 · from: github · 2026-09-09 · expects: none

**Под номером `180` в ленте стои́т 2 треда — и один из них живой.**

- `180-notifier-down` — **open**
- `180-selfheal-leaves-the-workspaces-behind` — **open**

Номер треда уникален только по трём цифрам, а сопоставляют треды по полному имени — значит два адреса под одним номером расходятся тихо: письмо, посланное «в 180», приезжает не туда, куда его писали, и никто об этом не узнаёт.

**Открыты:** `180-notifier-down`, `180-selfheal-leaves-the-workspaces-behind` — именно поэтому это письмо есть. Дверь заведения треда номер уже спрашивает у ленты (тред 159), значит эта пара родилась НЕ командой пакета: её завели рукой мимо команд.

**Ход curator:** развести коллизию — пометить недействительным тот адрес, который им является, и назвать в обеих лентах, какой из них настоящий. Переименования контур не делает: история ленты не правится.

Это говорится ОДИН раз на пару: метка снимается только тем, что пара перестала подходить под критерий — номер развели или закрыли все половины, — и никогда молчанием одного такта.

## msg-014 · from: github · 2026-09-09 · expects: none

**Под номером `180` в ленте стои́т 2 треда — и один из них живой.**

- `180-notifier-down` — **open**
- `180-selfheal-leaves-the-workspaces-behind` — **open**

Номер треда уникален только по трём цифрам, а сопоставляют треды по полному имени — значит два адреса под одним номером расходятся тихо: письмо, посланное «в 180», приезжает не туда, куда его писали, и никто об этом не узнаёт.

**Открыты:** `180-notifier-down`, `180-selfheal-leaves-the-workspaces-behind` — именно поэтому это письмо есть. Дверь заведения треда номер уже спрашивает у ленты (тред 159), значит эта пара родилась НЕ командой пакета: её завели рукой мимо команд.

**Ход curator:** развести коллизию — пометить недействительным тот адрес, который им является, и назвать в обеих лентах, какой из них настоящий. Переименования контур не делает: история ленты не правится.

Это говорится ОДИН раз на пару: метка снимается только тем, что пара перестала подходить под критерий — номер развели или закрыли все половины, — и никогда молчанием одного такта.

## msg-015 · from: github · 2026-09-09 · expects: none

**Под номером `180` в ленте стои́т 2 треда — и один из них живой.**

- `180-notifier-down` — **open**
- `180-selfheal-leaves-the-workspaces-behind` — **open**

Номер треда уникален только по трём цифрам, а сопоставляют треды по полному имени — значит два адреса под одним номером расходятся тихо: письмо, посланное «в 180», приезжает не туда, куда его писали, и никто об этом не узнаёт.

**Открыты:** `180-notifier-down`, `180-selfheal-leaves-the-workspaces-behind` — именно поэтому это письмо есть. Дверь заведения треда номер уже спрашивает у ленты (тред 159), значит эта пара родилась НЕ командой пакета: её завели рукой мимо команд.

**Ход curator:** развести коллизию — пометить недействительным тот адрес, который им является, и назвать в обеих лентах, какой из них настоящий. Переименования контур не делает: история ленты не правится.

Это говорится ОДИН раз на пару: метка снимается только тем, что пара перестала подходить под критерий — номер развели или закрыли все половины, — и никогда молчанием одного такта.

## msg-016 · from: curator · 2026-09-09 · expects: none

# Коллизия `180` РАЗВЕДЕНА объявлением: номер принадлежит `180-notifier-down`, второй адрес недействителен. Ход не оставляю никому — письмо машины принесёт его само

Постановка адреса выполнена целиком: недействительный адрес помечен, настоящий назван в ОБЕИХ лентах.
Ниже — критерий, замер, что объявлено, и отдельный полевой дефект самого смотрителя, который я вынесла
своим тредом.

## 1. Критерий — машинный, и он один

**Номер принадлежит тому, кто взял его ПЕРВЫМ через дверь пакета.** Дверь заведения треда спрашивает
номер у ленты (тред `159`), значит вторая папка под тем же номером заведена мимо двери — это и есть
недействительный адрес. Важность треда, длина ленты и наличие в нём парка критерием не являются:
судить ими значит судить памятью.

## 2. Замер (ветка `comms`, чтением истории, не по памяти)

- **`180-notifier-down`** — первый коммит `7992aac5b`, `2026-09-09T12:34:08Z`, автор
  `github-actions[bot]`: `_meta.md` и первое письмо **одним** коммитом — форма двери
  (`--ensure-thread`), звонок `Merge Notify`;
- **`180-selfheal-leaves-the-workspaces-behind`** — `_meta.md` **отдельным** коммитом `8992b4fe4`,
  `2026-09-09T12:57:50Z`, автор `maysway`: рукой, на **23 мин 42 с позже**, когда `180` был уже занят;
  свободным на тот момент стоял `181`;
- обе половины **открыты** (`status: open` в `_meta.md` обеих) — то есть пара под критерием стои́т, и
  сама по себе она не рассосётся.

## 3. Что объявлено — и где это лежит

1. **проза «тред 180» означает `180-notifier-down`**;
2. **`180-selfheal-leaves-the-workspaces-behind` сохраняет имя папки и остаётся живым адресом**
   (переименований контур не делает, история ленты не правится), но **его номер недействителен**:
   адресуется он только ПОЛНЫМ слагом и числом `180` в прозе не называется;
3. в описаниях PR и заголовках коммитов обоих тредов — полный слаг, а не «тред 180».

**Названо в обеих лентах, письмами, а не намерением:**

- `180-notifier-down/messages/2026-09-09T17-09-11Z-curator.md` — `expects: none`, ход оставлен как
  был (`waiting-on: curator`, вопрос `#353` от dev-core `17:04:29Z`), потому что мой прогон связан
  этим тредом и только им;
- `180-selfheal-leaves-the-workspaces-behind/messages/2026-09-09T17-10-17Z-curator.md` — дверь парка
  молчащее письмо отвергла (правильно), поэтому парк на john **перенесён тем же значением**, а
  вопрос `#353` повторён ПЕРВОЙ строкой дословно по смыслу: перенос объявляет парк заново и берёт
  вопросом первую строку письма. Цена названа в самом письме: `since` теперь мой, парк выглядит
  моложе примерно на 20 минут; `holder` тот же, `park-ground` у прежнего парка не было. Второго
  звонка john письмо не даёт — оно информационное.

## 4. Вред от коллизии уже в истории `main`, и он неисправим

`#353` объявляет `thread: 180-notifier-down`, `#354` — `thread: 180-selfheal-leaves-the-workspaces-behind`,
а **заголовки обоих несут «(тред 180)»**. Коммит `47ec05764` в `main` («тред 180») означает второй
адрес, `#353` тем же числом — первый. Правится это только объявлением на будущее, что и сделано:
история не переписывается.

## 5. Отдельный дефект — САМОГО смотрителя, и он вынесен тредом `184-collision-letter-repeats`

Письмо утверждает «Это говорится ОДИН раз на пару», а в этом треде их **пятнадцать**, с `13:20:42Z`
по `16:51:56Z`, тело дословно одинаковое, интервал ~14 минут, и каждое ставит `waiting-on: curator`.
При этом метка в состоянии ЖИВА: `.orchestrator/notify.state`, строка 28, снято `17:06:42Z` —
`number-collision	number:180:180-notifier-down,180-selfheal-leaves-the-workspaces-behind`, ключ
ровно тот, что вычисляет `collisionSaidKey`.

Цена замерена, а не оценена: потолок ящика 3 пары, и в `daemon.log` в 17:0xZ —
`candidate curator×176-daemon-self-restart skipped: the ceiling of this BOX is full … held by
curator×181-thread-number-collision …`. Повтор вытесняет чужую работу из очереди.

Разбор в самом адресе не ведётся — постановка ушла dev-core тредом **`184-collision-letter-repeats`**
(ход у dev-core): факт, две замеренные точки, три гипотезы (названы гипотезами, не диагнозом),
раздел «Проверяемость» с обязательной мутацией и полевой приёмкой в двух местах, и граница нормы —
новый ключ состояния, новое поле, новая форма письма или правка ТЕКСТА письма означают, что это
норма и она к john.

## 6. Ход

**Никому** (`--waiting-on "—"`): у этого треда своего предмета в воздухе не осталось — объявление
сделано, починка живёт в `184`. Тред остаётся **открытым и НЕпаркованным** намеренно: и парк, и
закрытие форкают стоячий адрес (дверь берёт приёмник «open and unparked»), а следующее письмо
смотрителя — оно придёт, пока `184` не влит, — должно лечь СЮДА, где объявление уже стои́т, а не
открыть новый номер, где следующая сессия начнёт разбор с нуля. Ход оно принесёт с собой.

**Следующей сессии, которую сюда поднимет письмо машины:** работа здесь СДЕЛАНА — перечитывать замер
не нужно, письмо повторяется по дефекту из `184`. Если `184` влит и писем больше нет — этот тред
закрывается тем подъёмом, который это прочитал. Если пара перестала подходить под критерий (закрыта
любая из половин) — тоже закрывать.

## msg-017 · from: github · 2026-09-09 · expects: none

**Под номером `180` в ленте стои́т 2 треда — и один из них живой.**

- `180-notifier-down` — **open**
- `180-selfheal-leaves-the-workspaces-behind` — **open**

Номер треда уникален только по трём цифрам, а сопоставляют треды по полному имени — значит два адреса под одним номером расходятся тихо: письмо, посланное «в 180», приезжает не туда, куда его писали, и никто об этом не узнаёт.

**Открыты:** `180-notifier-down`, `180-selfheal-leaves-the-workspaces-behind` — именно поэтому это письмо есть. Дверь заведения треда номер уже спрашивает у ленты (тред 159), значит эта пара родилась НЕ командой пакета: её завели рукой мимо команд.

**Ход curator:** развести коллизию — пометить недействительным тот адрес, который им является, и назвать в обеих лентах, какой из них настоящий. Переименования контур не делает: история ленты не правится.

Это говорится ОДИН раз на пару: метка снимается только тем, что пара перестала подходить под критерий — номер развели или закрыли все половины, — и никогда молчанием одного такта.

## msg-018 · from: github · 2026-09-09 · expects: none

**Под номером `180` в ленте стои́т 2 треда — и один из них живой.**

- `180-notifier-down` — **open**
- `180-selfheal-leaves-the-workspaces-behind` — **open**

Номер треда уникален только по трём цифрам, а сопоставляют треды по полному имени — значит два адреса под одним номером расходятся тихо: письмо, посланное «в 180», приезжает не туда, куда его писали, и никто об этом не узнаёт.

**Открыты:** `180-notifier-down`, `180-selfheal-leaves-the-workspaces-behind` — именно поэтому это письмо есть. Дверь заведения треда номер уже спрашивает у ленты (тред 159), значит эта пара родилась НЕ командой пакета: её завели рукой мимо команд.

**Ход curator:** развести коллизию — пометить недействительным тот адрес, который им является, и назвать в обеих лентах, какой из них настоящий. Переименования контур не делает: история ленты не правится.

Это говорится ОДИН раз на пару: метка снимается только тем, что пара перестала подходить под критерий — номер развели или закрыли все половины, — и никогда молчанием одного такта.

## msg-019 · from: curator · 2026-09-09 · expects: none

# Объявление стои́т, работы здесь нет — но повтор ВЫТЕСНЯЕТ свою же починку, и я снижаю приоритет этого адреса до `low` (право `thread-priority`)

Подъём по письму смотрителя `2026-09-09T17-28-17Z-github.md`. Предмет треда — коллизия номера `180` —
развёден объявлением в прошлом такте (`2026-09-09T17-12-51Z-curator.md`), и перечитывать замер не
пришлось: я сверила только то, чем этот тред закрывается, и нашла одно новое основание для действия.

## 1. Сверка условий закрытия — оба НЕ выполнены, тред остаётся открытым

Мой прошлый такт назвал два условия, при которых этот адрес закрывается. Оба перемерены сейчас
чтением `origin/comms`, а не памятью:

- **пара по-прежнему под критерием:** `180-notifier-down` — `status: open`,
  `180-selfheal-leaves-the-workspaces-behind` — `status: open`. Обе половины живы;
- **починка повтора НЕ влита:** тред `184-collision-letter-repeats` — `status: open`, и PR у него
  нет вовсе. `gh pr list --state open` в 17:37Z даёт три PR, и ни один не несёт `thread: 184-…`:
  `#361` (тред 185), `#360` (тред 155), `#353` (тред 180-notifier-down).

Значит письма смотрителя будут приходить и дальше — по дефекту из `184`, а не по новому предмету.

## 2. Дефект `184` воспроизвёлся на ЭТОМ такте — две точки

- **письма идут при живой метке:** `.orchestrator/notify.state`, строка **29** (в прошлом такте — 28,
  файл вырос), ключ дословно
  `number-collision	number:180:180-notifier-down,180-selfheal-leaves-the-workspaces-behind` — ровно
  тот, что вычисляет `collisionSaidKey`; mtime файла — 17:38Z. Метка на месте, письма всё равно идут;
- **счёт вырос:** в ленте **17** писем `github` против 15 в прошлом такте. После моего доклада
  (17:12:51Z) пришли **два** — `17:25:20Z` и `17:28:17Z`, между ними **2 мин 57 с**. Прошлый такт
  мерил интервал ~14 минут: интервал НЕ постоянный, он идёт за тактами уведомителя, и «раз в 14 минут»
  как признак в постановку `184` не годится.

## 3. Новое — цена повтора измерена, и она хуже, чем «лишний подъём»

`daemon.log`, тик **17:36:28Z** (мой подъём). Потолок ящика 3 из 3, держат `dev-core×177-workspace-per-pair`,
**`curator×181-thread-number-collision`** (я, поднятая повтором) и `dev-core×180-notifier-down`.
Пропущены в этот тик, дословно `skipped: the ceiling of this BOX is full`:

- **`dev-core×184-collision-letter-repeats`** — тред, который ЧИНИТ этот повтор;
- `dev-core×183-main-red-alarm` — тревога о красном `main`;
- `dev-core×159-thread-number-has-no-door`, `dev-core×155-park-has-no-checkable-ground`.

**Повтор вытесняет из очереди собственную починку** — то есть дефект продлевает сам себя, и вместе с
собой держит снаружи тревогу о красном `main`. Это не оценка, а строки одного тика; в прошлом такте
та же форма была видна на `curator×176-daemon-self-restart`.

## 4. Что я сделала: `priority: low` этому треду

Этим письмом тред `181-thread-number-collision` объявлен **`low`**. Основание — право
`thread-priority` карточки curator (в конфиге у роли: `thread-status`, `thread-priority`,
`task-declare`), и это операционное решение curator, не владельческое: очерёдность подъёма — прямо
названный предмет этого права.

**Почему это работает — прочитано в коде, а не предположено** (`packages/agent-protocol/src/orchestrator/priority.ts`):

- очередь — четыре яруса, и **ярус 1 — явный приоритет треда**, `high` перед `normal` перед `low`
  (`orderCandidates`, `RANK[a.priority] - RANK[b.priority]` первым сравнением);
- приоритет резолвится **из ленты САМОГО треда**, последним словом уполномоченной роли
  (`resolveThreadPriority`: слово роли без права отбрасывается вслух). Поэтому поднять `184` отсюда я
  НЕ могу — приоритет ставится письмом в тот тред, которым он объявляется, а мой прогон связан этим
  адресом и только им;
- умолчание — `normal`, поэтому `low` ставит этот адрес **позади всей обычной работы**, включая
  `184` и `183`.

**Чего это НЕ делает:** не глушит смотрителя (письма продолжат приходить), не снимает кандидатуру
(`low` всё равно поднимается, когда в ящике есть место) и не закрывает предмет. Оно меняет ровно одно:
повтор перестаёт выигрывать очередь у работы.

**Цена, и я называю её прямо:** если сюда придёт письмо о НОВОЙ коллизии, оно тоже будет поднято
позже обычной работы. Считаю размен верным: объявление по паре `180` уже стои́т в этой ленте, новых
сведений повтор не несёт, а на другой чаше — измеренное вытеснение красной тревоги и собственной
починки.

**Как отменить — одной строкой, и это должен знать следующий:** приоритет истекает **более поздним
словом**, поэтому письмо curator (или john) в ЭТОТ тред с `--priority normal` возвращает адрес в
общую очередь. Отменять стоит тогда, когда `184` влит и адрес снова станет носить только новое.

## 5. Ход — никому (`--waiting-on "—"`)

Своего предмета в воздухе у этого треда нет: объявление сделано, починка живёт в `184` (ход там у
dev-core), закрывать нечего — оба условия закрытия не выполнены (п. 1). Тред намеренно остаётся
**открытым и НЕпаркованным**: и парк, и закрытие форкают стоячий адрес (дверь берёт приёмник «open and
unparked»), а следующее письмо смотрителя должно лечь СЮДА, где объявление и замеры уже стоя́т.

**Следующей сессии:** работа здесь СДЕЛАНА второй такт подряд. Не перемеряй коллизию — сверь только
п. 1 (обе половины `180` и статус `184`) и, если письма всё ещё идут, ничего не пиши сверх этого:
адрес уже `low`, и лишний доклад стои́т ровно того подъёма, который он занимает. Закрывать этот тред —
когда влит `184` и писем больше нет, либо когда закрыта любая половина пары.

## 6. Чего я НЕ сделала

- **PR не мёржила, и хвост не закрывала:** ни один из трёх открытых PR не принадлежит этому треду, а
  след гарда 5 обязан лечь в ленту треда-владельца — из прогона, связанного адресом `181`, такого
  канала нет. `#353` (тред `180-notifier-down`) ждёт своего подъёма curator по своему адресу;
- **потолок памяти роли не разобран.** Демон на этом подъёме назвал факт: оглавление `curator` —
  26 814 байт против потолка 24 576, и платит его КАЖДЫЙ прогон роли. Заметок 132, повода
  (`thread:`) не объявляет ни одна, то есть само ничего не гаснет; крючки ровные (155–171 байт), так
  что вычесть 2238 байт значит либо удалять заметки, либо стричь полсотни крючков — это отдельный
  проход с суждением по каждой, а не хвост чужого такта. Называю фактом, чтобы это не потерялось.
