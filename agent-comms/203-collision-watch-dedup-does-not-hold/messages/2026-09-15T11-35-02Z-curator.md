---
from: curator
worker: claude-code
session: 12448ab9-3768-41ba-9541-06e4444800b8
raised: 2026-09-15T11:30:11Z
date: 2026-09-15T11:35:02Z
expects: ack
waiting-on: curator
parked-on: john
---

**ТРЕБУЕТСЯ РЕШЕНИЕ JOHN: критерий приёмки этого треда (`msg-036`) сработать уже не может — закрыть `203` по исполненной половине (три пункта приёмки исполнены в поле) или держать открытым до ветви, которая редка по построению? Рекомендация curator — закрыть; неизмеренная ветвь записана остатком в журнал.**

Ниже: исход правила остановки, дефект самого правила (он мой, и он измерен этим тактом) и состояние.

## 1. Правило остановки прошлого письма исполнено — не сработала НИ ОДНА из трёх ветвей

Чтение `2026-09-15T11:30:54Z`, файл `.orchestrator/daemon.log` потребителя, якорь `^agent-protocol: daemon — courier: number-collision` (без якоря корпус травится транскриптами сессий в том же файле), греп с `-a`.

| ветвь правила | мера | исход |
| --- | --- | --- |
| 1 — строка удержания метки на слепом такте | `grep -c "mark(s) held over a tick that did not read every half"` = **0** за весь лог | не сработала |
| 2 — объявление новее `08:50:48Z` без строки удержания | не-тихих строк сторожа после `08:50:48Z` — **0** | не сработала |
| 3 — дата `2026-09-17` или позже | сегодня `2026-09-15` | не наступила |

**Молчание только окрепло:** с эпохи `09:37:54.053Z` (строка `26038` лога) — **179** оценок сторожа, из них тихих с истинной причиной (`already told about … nothing new to ring about`) — **179**. Прошлый замер в `11:25:57Z` давал 171: за пять минут +8 тихих оценок и ни одного объявления. Объявление за весь день новой сборки по-прежнему ровно одно — `08:50:48Z`, первое срабатывание нового ключа.

## 2. Дефект, измеренный ЭТИМ тактом, — в моём же правиле остановки: дата тред не держит

**Ящик поднял пару `curator×203` через две минуты после моего письма `11:28:39Z`.** Вся работа подъёма — греп выше, с заранее известным исходом.

Причина механическая, и её надо назвать вслух, потому что я её в прошлом такте потерял: **даты среди оснований парковки нет.** Форм парка три — `<человек>`, `pr:N`, `run:N`; словарь `park-ground` закрыт тремя формами до перемера — `frozen:<роль>×<тред>`, `no-delivers-since:<тред>`, `until-pr-merged:<n>` (`PROTOCOL.md`, «Словарь оснований парковки закрыт тремя формами до перемера», решение john 2026-09-13, тред `155-park-has-no-checkable-ground`). Ни срока, ни события ЧУЖОГО контура среди них нет — а событие, которого этот тред ждёт, происходит именно в чужом контуре и подъёмника сюда не имеет.

Значит `waiting-on → curator` без парка означает не «подожду до `09-17`», а **«поднимай меня каждым тактом до `09-17`»**, и цена названа нормой: три холостых подъёма подряд — и ящик перестаёт поднимать пару вовсе. То есть правило, написанное ради экономии тактов, тратило бы их до самой даты.

**Отложенная работа здесь держится ТОЛЬКО парком на человеке** — а парк на человеке это вопрос john, а не таймер. Поэтому вопрос ниже стоит первой строкой, а не «подождём молча»: молчаливый парк (`--expects none`) был бы худшим исходом, а парка по времени не существует вовсе. Находка дозаписана в журнал роли (`agent-comms/journal/curator/203-collision-watch-dedup-does-not-hold.md`, ветка почты, командой — PR под записью не бывает с 14.09).

## 3. Почему развилка ваша, а не моя

Критерий `msg-036` дословно: «появится строка об удержанных на слепом такте метках — причина подтверждена; повтор без такой строки — причина другая». **Обе половины предполагают СОБЫТИЕ, а его не случилось** — и не потому, что окно коротко, а потому, что починка работает: повторов нет вовсе. Критерий написан до того, как сборка `0.2.15` доехала до потребителя, и случая «дефект прекратился, ни одна половина не спрашивается» он не покрывает.

Закрыть тред приёмкой своей рукой против названного вами критерия — это «сам поставил → сам принял», только с хорошими числами; я этого не делаю. Снять или смягчить свой критерий можете только вы, отсюда звонок.

## 4. Рекомендация curator (рекомендация, не решение)

**Закрыть `203` по исполненной половине.** Основание — три пункта приёмки постановки (`msg-001`, §4), исполненные в поле поимённо:

1. первое обнаружение пары — письмо: `08:50:48Z`;
2. второе и последующие в том же состоянии — молчание со строкой о нём: 179 из 179 после перезапуска (и 171 из 171 до него), строка называет ИСТИННУЮ причину, захардкоженного `every half closed` при открытой половине — 0;
3. отрицательный контроль: `029`, `076`, `082`, `092` стоят в переписи и не звонят ни разу.

Плюс мера сверх постановки: **метка пережила перезапуск демона** (`09:37:54Z`), носитель — `notify.state`, персистентность подтверждена фактом, а не обещанием. На сборке `0.2.14` тот же перезапуск покупал повтор.

**Остаток называю честно:** ветвь «слепой такт ДЕРЖИТ метку» в поле не сработала ни разу — такта, не прочитавшего обе половины, за окно не случилось. Она редка по построению, и её `0` — отсутствие события, а не провал. Остаток уже записан в журнал роли и закрытием треда не пропадает.

**Если ваш ответ «держать открытым»** — команда правила остановки прежняя и стоит в письме `11:28:39Z`, §4; тред при этом обязан стоять парком, иначе он поднимает роль каждым тактом впустую.

## 5. Хвост открытых PR

`gh pr list --state open` → те же **три**: #448 (`thread: 198`), #442 (`thread: 204`), #440 (`thread: 202`). **`thread: 203` не несёт ни один**, кнопку не нажимал ни по одному: след гарда 5 обязан лечь в ленту треда-ХОЗЯИНА, а этот прогон связан одним тредом `203`.

## 6. Состояние

Рабочее дерево чисто, незакоммиченного нет. Необратимого этим тактом — ничего: ни merge, ни метки, ни тега. В чужой контур не записано ни байта, только чтение. Тела письма и записи — в `mktemp -d -p /tmp`, вне чекаута почты и вне рабочего дерева роли.

**Парк: `john`** (`--expects ack` — звонит сразу и несёт первую строку). Лифт — ваше слово в ленте или закрытие треда. **waiting-on → curator:** ход остаётся у меня, потому что вопрос несу я.

