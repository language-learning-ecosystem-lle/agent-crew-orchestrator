---
from: curator
worker: claude-code
session: a85112c4-e0a2-47ab-bd51-fda253419c4e
raised: 2026-09-14T14:29:15Z
date: 2026-09-14T14:35:16Z
expects: ack
waiting-on: curator
parked-on: john
park-lifted: run:432
---

**john: объяви тег `agent-protocol-v0.2.15` потребителю — он срезан, лежит на `origin` и БЕЗ переезда пина не стои́т ничего. Через границу это несёт только человек, и это единственное, что осталось треду 203.**

## 1. Что именно просится — одно действие

Донести до контура-потребителя, что тег готов, и дать ему переехать пином. Сверку, которую он у себя запустит, исполнитель уже напечатал (`msg-025`, §5), и я её не переписываю:

```
agent-protocol schema version --package-ref 'agent-protocol-v0.2.15' --repo <репозиторий-потребитель> --ref main
```

Ожидание с нашей стороны — `27`.

Всё, что можно было доказать своей рукой, доказано и принято мной в `msg-028`:

| факт | значение |
| --- | --- |
| полный SHA среза = SHA тега (тег лёгкий, объекта нет) | `040544753045ba3e947c68f6cd1ce920c45f6aea` |
| `version` | `0.2.15` |
| версия схемы | `27` |
| тег на `origin`, имя не сожжено дважды | `git ls-remote --tags origin 'agent-protocol-v0.2.15'` — ровно одна строка, без `^{}` |

Границы исполнитель не пересёк: ни ветки, ни коммита, ни PR, ни комментария в чужом репозитории — постановка кончилась тегом и печатной строкой объявления, ровно как велит норма.

## 2. Почему это не «хвост», а сам предмет треда

**Тред 203 заведён потому, что дедуп сторожа коллизий объявлен его же текстом и не держится.** Замер, из которого он: семнадцать побайтово одинаковых писем в ленту контура-потребителя `11:37:08Z`–`13:57:23Z`, и сегодня класс подорожал — `09:07:32Z` повтор поднял их `curator`, то есть перестал быть шумом и стал расходом окна.

**Починка (#420) написана, влита и ЛЕЖИТ В СРЕЗЕ** — доказано содержимым, не доверием: четыре из пяти файлов побайтово тождественны своей ревизии, пятый (`src/cli.ts`) сверен тремя маркерами, полевая строка `mark(s) held over a tick that did not read every half` в срезе есть.

**Но контур-потребитель исполняет ту версию, на которую он запинен.** Пока пин не переехал, сторож у них бежит старым кодом, и **полевая проверка гипотезы невозможна в принципе** — мерить нечего. То есть без этого объявления тред 203 не может ни подтвердиться, ни закрыться, сколько бы тактов ему ни дали.

## 3. Цена ожидания названа числом, а не словом «дорого»

**Предыдущий тег ждал переезда шесть суток.** Ровно эта цена — причина, по которой ты 14.09 (`msg-014` этой ленты) сказал «ДА — режем `0.2.15` от ТЕКУЩЕЙ ГОЛОВЫ `main`»: рез был нужен не сам по себе, а чтобы починка доехала. Рез исполнен `~12:5xZ`; с этого момента тег простаивает.

## 4. Чего я НЕ прошу — чтобы звонок не размывался

- **#433 (тред `205`) к тебе уже позвонил сегодня в `14:19:16Z` и стои́т там парком** — второй звонок под тот же предмет не ускоряет, а обесценивает следующий. Не дублирую;
- **#431 (тред `201`)** — звонок был в `10:49:38Z`, стоячего парка сейчас нет; это ход треда `201`, и донести это может только он;
- **вопроса про вердикт на PR, правящем `claude-review.yml`, здесь нет:** dev-core принёс развилку, я её перемерила — она уже решена твоим словом от 2026-08-19 (тред `014`), записана четырьмя пунктами в `PROTOCOL.md`, строки 772–785, и нового решения не требует. Ответ ушёл ему в `msg-003` этого треда, тебе он на стол не едет.

## 5. Состояние треда на момент звонка

- **журнальный долг 203 закрыт целиком:** `43bd494f0` (#429, dev-core) и `9526febfe` (#432, curator, кнопка моя в `14:30:26Z`, гарды поимённо в предыдущем письме);
- открытых PR с `thread: 203` **нет ни одного**;
- рабочее дерево чисто, незакоммиченного нет; необратимое этим тактом одно — merge #432;
- тред не закрываю: предмет полем не проверен, а приёмка на закрытии убивает свой канал.

**Парк — `john`** (`run:432` гашу этим же письмом: PR влит, ждать нечего). **waiting-on → curator:** ход возвращается ко мне твоим ответом, и следующим шагом станет полевая проверка дедупа — как только пин переедет.

