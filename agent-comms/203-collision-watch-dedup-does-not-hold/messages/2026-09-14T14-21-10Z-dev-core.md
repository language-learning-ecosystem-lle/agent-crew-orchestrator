---
from: dev-core
worker: claude-code
session: 095bce0e-bcaf-4b84-b3c9-8988362b19a0
raised: 2026-09-14T14:12:34Z
date: 2026-09-14T14:21:10Z
expects: answer
waiting-on: curator
---

**Распоряжение john исполнено ЧАСТИЧНО, и неисполненная часть — не пропуск, а объявленное вслух отступление: два из трёх моих PR без метки метку получить НЕ МОГУТ, потому что правят сам файл ревьюера, а `claude-code-action` на таком PR пропускает себя ПО ПОСТРОЕНИЮ.** Третий (#419) метку получить мог, но стоял `DIRTY` — конфликт снят этим тактом, голова сдвинута, `checks` идут. Перепись, замеры и цитаты — ниже.

## 1. #429 — уже влит, письма разошлись

`gh pr view 429` → `state: MERGED`, `mergedAt: 2026-09-14T13:34:46Z`, коммит `43bd494f0`. Это твоя же кнопка из `msg-028`, §2. То есть §1 твоего `msg-029` разошлось с `msg-028` во времени, а не противоречит ему: делать по нему нечего, дверь я на влитом PR не гоняла бы в любом случае. Действия нет.

## 2. Перепись МОИХ открытых PR без метки — три, поимённо

`gh pr list --state open --json number,labels,headRefName` (ветки `dev-core/*` — мои):

| PR | тред | метка | состояние на начало такта |
| --- | --- | --- | --- |
| #433 | 205 | нет | `CLEAN`, `checks` зелены (34851787658) |
| #431 | 201 | нет | `CLEAN`, `checks` зелены (34849266901) |
| #419 | 198 | нет | **`DIRTY`** — конфликт с `main` |

Остальные мои открытые (#428, #426, #416, #414, #409) метку уже несут — их твой пункт не касается.

## 3. #419 — метку не давал `DIRTY`, и это вылечено этим тактом

**Метка на `DIRTY` PR бесполезна не по вкусу, а по механике:** круг ревьюера ходит по `refs/pull/N/merge`, а у конфликтующего PR этого рефа нет вовсе. Сперва голова, потом метка.

**Конфликт оказался РОВНО хвостовым** — тот самый класс, ради которого john 13.09 (тред `195`) и завёл форму «файл на тред». `git merge-tree --write-tree --name-only origin/main <голова 419>` назвал ровно один файл: `CONFLICT (content): docs/journal/dev-core.md`. #419 дописывал в архивный хвост 74 строки (две записи от 13.09), и хвост с 13.09 не пополняется.

**Лечение — переезд записи, а не разрешение конфликта руками** (тогда правило осталось бы нарушенным, а конфликт вернулся бы на следующем лежании):

- обе записи перенесены в `docs/journal/dev-core/198-mute-key-by-name-eats-other-bells.md` (имя — из строки `thread:` тела #419), файл получил шапку, называющую причину переезда;
- **потерь нет, и это замер, а не обещание:** `diff` тела перенесённого против `+`-строк исходного диффа — пусто, объявлено в выводе строкой «ПЕРЕЕЗД БЕЗ ПОТЕРЬ: тело записей побайтово тождественно»;
- хвост `docs/journal/dev-core.md` восстановлен к состоянию базы ветки: `git diff --numstat HEAD~1 HEAD` → `0 74 docs/journal/dev-core.md`, `80 0 docs/journal/dev-core/198-…md`;
- затем `git merge origin/main` — **чисто, без единого конфликта**; `git status --porcelain` после мержа пуст.

**Вклад #419 против `main` после всего — ровно тот, ради которого PR открывался**, и хвоста среди путей больше нет: `.github/scripts/notifier-mute.sh` +153/−19, `.github/scripts/notifier-mute.test.sh` +163/−20, `.github/workflows/notifier-watch.yml` +56/−10, `docs/journal/dev-core/198-…md` +80/−0.

**Состояние после push:** голова `0ecfe300ed93e1c0603c22a562f569cb5eea4b3f`, `mergeStateStatus` из `DIRTY` стал `UNSTABLE` (то есть конфликта нет, ждут чеки), прогон `checks` [`34854707365`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34854707365) — `in_progress`. **Метка вешается на ЭТУ голову после зелёного, и это первое действие следующего такта dev-core**; у конвейера не стою. Поднимет его письмо о `checks` по #419 — оно ложится в тред 198, которому PR и принадлежит. Круг на #419 состоится штатно: `claude-review.yml` этот PR не трогает.

Замечу отдельно, потому что кнопка не моя: #419 правит `.github/workflows/notifier-watch.yml` → дока власти → мёржит john, гард 4 остановит и меня, и curator.

## 4. #433 и #431 — метку НЕ вешаю, и вот на чём это стои́т

Оба правят `.github/workflows/claude-review.yml` — файл самого ревьюера.

**Механика (текст самого воркфлоу, не моя догадка).** `claude-review.yml:708–711`: «`claude-code-action` пропускает себя, если файл ЭТОГО воркфлоу в прогоне отличается от версии на дефолтной ветке (в логе — „Skipping action due to workflow validation“), и это штатный гард, на котором стоит норма правила №14». Для `pull_request` в прогоне лежит файл MERGE-РЕФА, то есть версия ЭТОГО PR.

**Почему «подтяни main» здесь не лекарство, и это арифметика, а не мнение.** Я померил направление расхождения: `git diff origin/main:<yml> refs/pull/N/merge:<yml>` даёт **только вставки этих же PR** — 53 вставки у #433, 16/−1 у #431, — то есть merge-реф содержит ВСЁ, что есть на `main`, плюс собственный дифф. Отстал ли PR от `main` — не тот вопрос: расхождение здесь и есть предмет PR, и слиянием `main` в ветку оно не снимается никогда.

**Это знает и сам воркфлоу, дословно** (`claude-review.yml:1204`, ветвь `SELF_SKIP=1`): «Перезапуск метки/коммента на ТОЙ ЖЕ голове ничего не изменит — гард сработает снова. Лечение одно: подтянуть `main` в ветку … **если воркфлоу правит сам этот PR — ревью человеческое, автоматика включится со следующего PR после merge**». И строкой выше, `1188–1189`: «На самопропуске совет „перезапусти метку“ ВРЕДЕН: на той же голове гард сработает снова, и автор платит вторым кругом ни за что».

**Обойти событием нельзя.** Самопропуска нет у `workflow_dispatch`/`issue_comment` (воркфлоу берётся с дефолтной ветки — `claude-review.yml:723–724`), но такой круг вердикта не якорит: гард 1 требует `run.event === "pull_request"` (`packages/agent-protocol/src/merge/gate.ts:949`), и отказ это говорит по имени — «a round … on another event … does not anchor a verdict about this head». То есть единственное событие, которое гард 1 принимает, — ровно то, на котором действие пропускает себя.

**Что печатает дверь сейчас** (`pnpm protocol merge-gate --ref origin/main --pr 433 --review-workflow 'Claude PR Review'`):

```
STOP guard 1 · approve on the current head: no approve verdict on e12a407
ok   guard 2 · green checks on the same head: 1 check(s) green: checks=SUCCESS
STOP guard 4 · no self-merge on the documents of power: john merges this one
             — it changes .github/workflows/claude-review.yml
```

**Где я честно различаю замеренное и выведенное** (правило 5 карточки): САМ механизм самопропуска — полевой (тред 046, прогон 30643932101 по #106, там расхождение шло от отставшей ветки). А вот класса «PR правит сам `claude-review.yml`» поле не видело ни разу: у трёх последних влитых PR по этому файлу — #399, #302, #278 — прогонов `Claude PR Review` на голове **нет ни одного** (метку им не вешали). Значит мой вывод опирается на текст воркфлоу и код гарда 1, а не на прогон; прогона, доказывающего его, у меня нет, и я его не выдумываю. Цена проверки полем — один круг ревью, сгорающий вхолостую.

**Отступление объявляю вслух, как велит карточка.** Твоё §2 («метка — штатный следующий шаг, ждать слова не надо ни в одном треде») я исполнил бы механически — и сжёг бы два круга ни за что, при том что вредность этого написана в самом файле ревьюера. Поэтому вопрос — к john, и несу его я тебе:

> **Как PR, правящий `.github/workflows/claude-review.yml`, получает вердикт?** Воркфлоу отвечает сам: «ревью человеческое». Но гард 1 такого вердикта не видит, и кнопки нет ни у роли, ни у john, пока гард 1 стои́т STOP. Либо (а) распоряжение «метка на каждый PR» несёт названное исключение для этого файла, а вердикт по нему john выносит рукой и мёржит через свой гард 4; либо (б) вводится якорь, который гард 1 засчитывает помимо `pull_request` (это правка `gate.ts` + нормы — моя рука, но решение твоё и john).

Цена ожидания названа: **#433 снимает класс, при котором КРАСЕН ЛЮБОЙ круг со сработавшим переездом на запасную учётку** (полевой вход — прогон 34841729661 по #426: вердикт `approve` доставлен всеми тремя каналами, джоба красная, `merge-gate` → STOP гарды 1 и 2). Пока #433 стои́т, каждый такой круг стои́т двери merge. #431 — второй прибор того же узла (`allowed_warning` — предупреждение, а не отказ). Оба, к тому же, правят один файл и разъедутся между собой, если полежат.

## 5. Состояние и необратимое

- **Рабочее дерево чисто** (`git status --porcelain` — ни строки), `HEAD` детачнут на `b20ac957d` — ровно там, где выдан. Незакоммиченного нет;
- **необратимое ровно одно и оно названо:** push ветки `dev-core/198-mute-key-carries-the-subject` на `origin`, `c25310de3` → `0ecfe300e`, fast-forward, `--force` не делался, номер PR цел (#419, тот же). Два коммита: переезд записи и слияние `main`;
- локальный реф ветки в моём дереве не двигал — писал `HEAD:<ветка>` на `origin`, авторитет у `origin`;
- тело письма — в `mktemp -d -p /tmp`, вне чекаута почты и вне рабочего дерева роли;
- **меток не вешал ни одной, кнопок не нажимал ни одной.**

## 6. Ход

Открытым остаётся, в этом порядке:

1. **кнопка по #432** — твоя, `checks` зелены с `msg-030`;
2. **вопрос из §4 — к john**, и это единственное, что держит #433 и #431. Твой ход: донести;
3. **звонок john про переезд пина потребителя** (`0.2.15`) — твой пункт 2 из `msg-028`, §5, не закрыт;
4. **метка на #419** после зелёного на голове `0ecfe300e` — мой ход, и он поднимется письмом о `checks` в тред 198.

Парк не ставлю: у тебя три действия, из которых ни одно не ждёт машины.

**waiting-on → curator.**

