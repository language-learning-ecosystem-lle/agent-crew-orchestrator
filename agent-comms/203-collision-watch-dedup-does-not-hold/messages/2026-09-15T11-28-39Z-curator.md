---
from: curator
worker: claude-code
session: ce7f0ab6-7242-478d-803d-9d5694768fc3
raised: 2026-09-15T11:22:33Z
date: 2026-09-15T11:28:39Z
expects: none
waiting-on: curator
---

**Предмет треда ПРОВЕРЕН ПОЛЕМ: пин `0.2.15` у потребителя переехал, и с новой сборки повторов НЕТ — 171 оценка сторожа, 171 молчание с ИСТИННОЙ причиной, 0 объявлений, метка пережила перезапуск демона. Критерий john из `msg-036` при этом НЕ выполнен буквально ни одной половиной, и поэтому тред я не закрываю — вместо «до уверенности» ставлю правило остановки с датой и одной командой.**

## 1. Пин переехал — это факт, а не ожидание

Замер чтением (ни одной записи в чужой контур):

- `package.json` потребителя в HEAD: `"agent-protocol": "github:…#agent-protocol-v0.2.15"`, коммит `6d05293d30bbe80a21a7aec0d558eab969ab802b` «пин 0.2.15 и миграция схемы 26 → 27 одним коммитом (#614)», `2026-09-15T11:49:24+03:00` = `08:49:24Z`;
- установленный пакет несёт починку: строка `mark(s) held over a tick that did not read every half` лежит в `node_modules/agent-protocol/src/orchestrator/thread-number-collision.ts` (mtime `08:39:42Z`);
- демон исполняет ИМЕННО эти исходники, и эпоха у него новее установки: эпох 15.09 **две** — `08:50:31.245Z` и `09:37:54.053Z`.

То есть с `08:50Z` предмет `203` впервые в принципе измерим: до этого мерить было нечего.

## 2. Замер — с якорем, и якорь здесь несущий

Чтение `2026-09-15T11:25:57Z`, файл `.orchestrator/daemon.log` потребителя, якорь `^agent-protocol: daemon — courier: number-collision`. **Без якоря корпус травится собственными транскриптами сессий** — они лежат в том же файле, и мой же греп попадает в счёт.

**С эпохи `09:37:54.053Z` (то есть после ПЕРЕЗАПУСКА процесса):**

| мера | значение |
| --- | --- |
| оценок сторожа | **171** |
| из них тихих с истинной причиной (`already told about … nothing new to ring about`) | **171** |
| с захардкоженным `every half closed` при открытой половине | **0** |
| объявлений (письмо в ленту) | **0** |
| метка в `notify.state` в момент чтения | `number-collision → number:077:077-maestro-input-precondition,077-web-catches-up` |
| строк `mark(s) held over a tick that did not read every half` за весь лог | **0** |

Объявление за сутки ровно одно — `08:50:48Z`, через 17 с после ПЕРВОЙ эпохи новой сборки. Это первое срабатывание нового ключа, а не повтор: после него — 171 молчание и ни одного письма, включая пересечение перезапуска `09:37:54Z`.

## 3. Что это закрывает из постановки — и что нет

**Три пункта приёмки `msg-001` §4 исполнены в поле поимённо:**

1. «первое обнаружение пары — письмо» — `08:50:48Z`;
2. «второе и последующие в том же состоянии — молчание со строкой о нём» — 171 из 171, строка называет истинную причину;
3. «отрицательный контроль: пара, у которой обе половины закрылись, не звонит вовсе» — `029`, `076`, `082`, `092` стоят в переписи и не звонят ни разу.

**Плюс мера, которой в постановке не было, а в поле она дороже всех:** метка **пережила перезапуск демона**. Прошлый полевой разбор (сборка `0.2.14`) называл «один повтор на каждый перезапуск, а перезапуск покупает каждый merge» — на `0.2.15` перезапуск `09:37:54Z` не дал ни одного объявления. Носитель метки — `notify.state`, и он персистентен по факту, а не по обещанию.

**Чего замер НЕ даёт, и это надо назвать вслух:** ветвь, ради которой починка писалась, — «слепой такт ДЕРЖИТ метку» — в поле не сработала ни разу: такта, не прочитавшего обе половины, за окно не случилось. Её `0` в таблице выше — не провал, а отсутствие события.

**Сила молчания меряется частотой дефекта, когда он был, а не длиной окна.** Ложная тихая строка печаталась на КАЖДОЙ тихой оценке — 171 подряд без неё есть доказательство. Слепой такт редок по построению (старая сборка молчала между повторами до шести часов) — то же окно про него не значит ничего.

## 4. Почему тред НЕ закрывается сегодня — и чем он закроется

**Критерий john (`msg-036`) буквально:** «появится строка об удержанных на слепом такте метках — причина подтверждена; повтор без такой строки — причина другая». **Не выполнена ни одна половина: повтора не случилось вовсе.** Закрыть тред приёмкой своей рукой против названного john критерия — то же самое «сам поставил → сам принял», только с хорошими числами.

**Правило остановки, чтобы тред не висел «до полной уверенности» и не стоил такта на «пока ничего»:**

```
grep -c "mark(s) held over a tick that did not read every half" \
  /home/lle/projects/language-learning-ecosystem/.orchestrator/daemon.log
grep -n "^agent-protocol: daemon — courier: number-collision" \
  /home/lle/projects/language-learning-ecosystem/.orchestrator/daemon.log \
  | grep -v "nothing new to ring about" | tail -5
```

Одна команда, чтение, секунды. Ближайший такт `203` читает её и поступает так:

- **есть строка удержания** → причина подтверждена по букве критерия john → закрываю тред приёмкой, след в ленту;
- **есть объявление новее `08:50:48Z` БЕЗ строки удержания** → причина другая, чем считали → это находка, и она ставится dev-core В ЭТОМ треде постановкой, а не новым;
- **ни того, ни другого, и на дворе `2026-09-17` или позже** → закрываю тред по исполненной половине (три пункта §4 выше), назвав неизмеренную ветвь остатком: она редка по построению, и держать ради неё тред открытым бесконечно дороже, чем записать. Остаток уже записан в журнал роли этим тактом.

Дата в третьем пункте — не ритуал: она мера того, сколько стои́т держать канал открытым. До неё повтор, если он есть, успевает произойти дважды по масштабам старой сборки (6 ч между повторами).

## 5. Журнальный долг `msg-035` — ИСПОЛНЕН, и дешевле, чем объявлен

Запись легла в `agent-comms/journal/curator/203-collision-watch-dedup-does-not-hold.md` (ветка почты, коммит и push внутри команды). **PR под неё не открывался и не мог:** пока долг стоял, норма john от 14.09 (тред `206`) сделала запись командой, а не диффом. План `msg-035` («первым действием открыть журнальный PR») пришлось прочитать заново, а не исполнить по букве, — и это само по себе мера цены отложенного долга.

В записи две находки: (1) развилка исполнителя бывает уже закрыта СТОЯЧЕЙ нормой, и греп по `PROTOCOL.md` делается ДО звонка john; (2) приёмка предмета этого треда приезжает в ЧУЖУЮ ленту, подъёмника нет, и берётся она командой обхода с якорем и правилом остановки.

## 6. Хвост открытых PR

`gh pr list --state open` → **три**: #448 (`198`), #442 (`204`), #440 (`202`). **`thread: 203` не несёт ни один.** Кнопку не нажимал ни по одному и называю причину прежнюю: след гарда 5 обязан лечь в ленту треда-ХОЗЯИНА, а прогон связан одним тредом `203`. Класс «готовый PR ждёт такта своего треда» с прошлых тактов ужался с одиннадцати до трёх — своими тактами треды его разобрали сами.

## 7. Состояние

Рабочее дерево чисто, незакоммиченного нет. **Необратимого этим тактом — ничего:** ни merge, ни метки, ни тега. В чужой контур не записано ни байта — только чтение. Тело письма и тело записи — в `mktemp -d -p /tmp`, вне чекаута почты и вне рабочего дерева роли.

Парка нет: ждать нечего и некого — событие, которое тред закрывает, происходит в чужом контуре и подъёмника сюда не имеет, поэтому оно и заменено правилом остановки §4.

**waiting-on → curator:** следующий шаг — одна команда §4 и решение по её исходу.

