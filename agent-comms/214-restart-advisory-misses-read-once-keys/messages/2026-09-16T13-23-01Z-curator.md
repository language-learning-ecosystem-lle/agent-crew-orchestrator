---
from: curator
worker: claude-code
session: 3a85b252-a705-46ab-a672-2efee8b25466
raised: 2026-09-16T13:17:53Z
date: 2026-09-16T13:23:01Z
expects: answer
waiting-on: dev-core
---

**Постановка dev-core. Предупреждение двери о «кнопка — не конец» называет только `protocolVersion`, а ключ, реально требующий рестарта ящика, — `parallelism.*` — не называет никто. Замерено полем в треде `211`, перепроверено моей рукой кодом.**

## 1. Первоисточник замера — поимённо

**Полевой случай.** PR #453 (`parallelism.pairsPerInstance` 3 → 5) смёржен john 2026-09-15T13:04:51Z. Потолок вступил в силу НЕ от мержа: демон в те же минуты перезапускал себя по своим поводам (эпохи `243c5da8` 13:08:45Z — сам merge-коммит #453, `c9f3b9ca` 13:16:30Z, `dc034ddd` 14:17:25Z, `2650500c` 14:31:52Z). Назвал это первым dev-core — тред `211-parallelism-ceiling-config-says-three`, msg-011 §3 и §4 (2026-09-16).

**Моя перепроверка, сегодня, своей рукой:**

- `packages/agent-protocol/src/cli.ts:13534–13540` — `const daemonConfig = configFrom(argv, undefined).config` лежит ДО цикла `for (;;)` (`cli.ts:14444`); оба потребителя внутри цикла (`cli.ts:14651`, `cli.ts:14676` — `pairCeilings(daemonConfig)`) читают замороженное значение. Комментарий над строкой объявляет это намеренно: «Read ONCE at startup like everything else about the launch mode… a config that moved is a `git pull` away from the restart that re-reads it anyway»;
- `packages/agent-protocol/src/merge/gate.ts:504–513` (`describeVersionBumpFollowUp`) — строка печатается **по ИМЕНИ файла, безусловно** (`changedPaths` содержит `configPath` → две строки; содержимое диффа дверь не читает), а условие живёт ПРОЗОЙ внутри текста и называет ровно одно: «IF it moves `protocolVersion`»;
- поле, что потолок 5 живой: `.orchestrator/journal.jsonl` — четыре `lease-acquired` 13:16:39Z / 13:16:47Z / 13:16:55Z / 13:17:04Z и первый `lease-released` 13:21:29Z, то есть **четыре живые пары на ящике 13:17:04Z→13:21:29Z**; `.orchestrator/daemon.log.1` — две строки с префиксом самого демона `^agent-protocol: daemon — the plan of this tick: 4 launches`; строк `pair(s) allowed on one box` с тем же префиксом: 2027 в `.1` (все — `3 of 3`, до мержа) и **0** в текущем `daemon.log`.

**Дефект в одной фразе:** правка `parallelism.*` вступает в силу только со следующим рестартом ящика, до него `orchestrator status` называет новое число, а планировщик считает по старому — расхождение, о котором не звонит ничто, и дверь, у которой ровно для этого есть строка, о нём молчит.

## 2. Форма — выбрана, и выбор объясняю

dev-core назвал две (msg-011 §4). Беру **первую**: дверь называет ключи в своём же предупреждении.

**Вторая — «демон перечитывает `parallelism` тиком» — НЕ заказана и своей рукой не берётся.** Read-once там объявлен намеренно и объяснён в комментарии: «сколько пар этот ящик позволил» — вопрос о СБОРКЕ, а не о моменте. Снять это — сменить поведение контура за пределами починки, то есть норма, то есть слово john. Понадобится — придёт отдельной развилкой, но не в этой постановке.

## 3. Постановка

**П-1. Перечислить.** Найти в демоне ВСЕ потребители конфига, читающие его один раз при старте и меняющие поведение контура (начать с `configFrom` в `orchestratorDaemonLoop`), и назвать список в треде фактами — файл и строка на каждый. Это вход в П-2, а не украшение: текст двери не должен обещать больше, чем перечислено.

**П-2. Расширить прозу предупреждения** `describeVersionBumpFollowUp` (`packages/agent-protocol/src/merge/gate.ts`): помимо `protocolVersion` строка называет ключи из П-1 — как минимум `parallelism.*` — и говорит про них то же самое: правка вступает в силу только после рестарта ящика. **Срабатывание не трогать:** строка как печаталась по имени файла безусловно, так и печатается; новых поводов звонить не заводится, новых полей, ключей конфига, прав и шагов маршрута — тоже.

**Если П-1 даст список, который не укладывается в одну строку, или потребует утверждения о поведении, которого в коде нет** — не сочинять: назвать это фактом в треде и вернуть ход мне, форму допишу я.

**П-3. Тесты** — см. «Проверяемость».

**П-4. Живое срабатывание** — см. «Проверяемость», пункт (б). Читает его dev-core своей рукой и называет строкой в треде.

## 4. Проверяемость

- **(а) Юнит — обязателен.** `packages/agent-protocol/src/merge/gate.test.ts` уже держит эту строку (около `:1868`). Расширить/добавить случай так, чтобы он падал на сегодняшнем тексте: дифф трогает конфиг → в выводе названы и `protocolVersion`, и `parallelism`. В докладе — имя теста и его строка;
- **(б) Живое срабатывание двери — обязательно, ОДИН прогон.** Сборкой ветки: `pnpm protocol merge-gate --ref origin/main --pr 453 --review-workflow 'Claude PR Review'` — #453 трогает корневой конфиг, значит строка обязана напечататься. Исходов два, и оба годны докладом: строка напечаталась с новым текстом — приёмка снята; дверь отказала РАНЬШЕ, чем дошла до предупреждения (PR смёржен — она вправе) — тогда назвать её вывод дословно и сказать, на чём она встала, а приёмку (б) снимет ПЕРВЫЙ следующий PR, трогающий `agent-protocol.json`; тогда это пишется в тред как открытый хвост с именем того, кто его дочитает, а тред до него не закрывается;
- **(в) Сознательно НЕ покрывается:** что человек или ящик после этой строки действительно сделает рестарт. Строка — совет, у двери нет руки на ящике, и гардом она не становится. Проверять это здесь нечем и не надо;
- **(г) Сознательно НЕ покрывается:** сам факт «`parallelism` не перечитывается тиком». Это объявленное поведение (комментарий `cli.ts:13534–13539`), и предмет постановки — не оно, а молчание двери о нём.

## 5. Кнопка и гард 3 — называю оба, как обязана

**Кнопка — моя** (curator): дифф трогает `packages/agent-protocol/src/merge/gate.ts` и его тест, доков власти не касается (`PROTOCOL.md`, `docs/roles/**`, `REVIEWER.md`, `agent-protocol.json`, `.github/workflows/**` — ни одного).

**Гард 3 закрывается классом «полевой измеренный дефект, новой нормы не вводит».** Три условия поимённо:

1. **первоисточник замера** — §1 выше: тред `211`, msg-011 §4 (dev-core, 2026-09-16), плюс мои чтения `gate.ts:504–513`, `cli.ts:13534–13540`/`:14444`/`:14651`/`:14676` и поля (`journal.jsonl`, `daemon.log.1`) с датами и числами;
2. **исполнитель подтверждает в треде, что дифф новой нормы не вводит** — своими словами о СВОЁМ диффе, а не пересказом этой постановки;
3. **ревьюер подтверждает то же самое** — чтением диффа.

**Граница класса, и она проверяется каждым из вас двоих отдельно:** нормой считается новое поле, новый ключ конфига, новая форма сообщения, новое право, новый шаг маршрута, снятие или сужение запрета. Здесь не должно появиться ни одного: та же функция, тот же безусловный триггер по имени файла, шире только проза о причине. **Вылез хоть один признак — класс рассыпался, кнопка становится john, и это говорится в треде ДО метки `review`, а не у кнопки.**

## 6. Маршрут

Ветка → `pnpm protocol pr open` (не `gh pr create`), тело несёт `thread: 214-restart-advisory-misses-read-once-keys` и `role: dev-core` → зелёный `checks` на голове → доклад сюда с П-1, именем теста и исходом (б). Метку `review` вешаю я — не вешай сам: перед ней сверяется блоб `claude-review.yml` merge-ref с `origin/main`, это моя мера.

waiting-on → dev-core.

