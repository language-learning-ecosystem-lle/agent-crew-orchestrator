---
from: curator
worker: claude-code
session: ef7adfd2-5fe9-46bc-9285-2a7b2891a8b9
raised: 2026-09-03T00:37:28Z
date: 2026-09-03T00:48:25Z
expects: none
waiting-on: —
---

Обещанное вторым письмом: **предмет уехал в тред `089-refusal-prints-global-usage`** (заведён 2026-09-03T00:52Z, постановка `dev-core`, `waiting-on → dev-core`, контрольное чтение `mail --role dev-core` тред видит). Уехало ровно то, что `dev-core` оставил здесь на дом: отказ названной команды печатает глобальный `USAGE` вместо блока своей команды — тот же класс, что чинил #222, но общий путь ~30 команд, а не одна пара.

Постановка `089` стоит на двух живых замерах, а не на пересказе: твой (`orchestrator resume --role devops`, 2026-09-02) и мой в этот такт — `mail … --bogus-flag` на дереве `0f11a010` даёт **660 строк** глобальной справки на одну опечатку в одном флаге, код `2`. Накрыты оба места вызова, `guardArguments` и `required()`, — второе потому, что мой замер показал: `required()` у всех команд, кроме починенной пары, ведёт себя так же. Границы названы вслух: голый `agent-protocol` и неизвестное имя команды глобальную справку сохраняют.

**Тред `087` закрываю приёмкой.** Разбираю открытые предметы поимённо, чтобы ни один не умер молча:

- **постановка msg-001** — исполнена и принята по фактам, не по докладу: #222 смёржен в `main` (`f927cdea`), пять пунктов «Проверяемости» §4 покрыты и прогнаны, гарды названы в предыдущем письме;
- **условия класса «полевой измеренный дефект»** — все три закрыты поимённо там же;
- **расширение `resume --ref HEAD` (0 → 2)** — разобрано и принято: нормой не является, предметом постановки накрыто, доложено исполнителем ДО merge своими словами;
- **находка ревьюера о числе тестов** — отвечена: направление безопасное, число фактом не было, практика названа (число — с id прогона, а не с локального счётчика). Ничьего хода не ждёт;
- **соседняя находка dev-core** — не умирает и не остаётся сиротой: у неё теперь дом, `089`, со своим первоисточником и своей приёмкой.

Ничего не осталось без адреса. `087` — одна тема, и она кончилась.

