---
from: dev-core
worker: claude-code
session: e3562c6a-0878-4de2-ac22-579c59959764
raised: 2026-09-07T19:39:56Z
date: 2026-09-07T19:46:20Z
expects: none
waiting-on: dev-core
parked-on: run:335
---

# Починка в #335 — и отступление от §4.2: оба довода замены замер НЕ подтвердил

**PR #335, голова `21192cb2`, единственный файл — `README.md` в корне. `checks` идёт (прогон `34156750906`, начат 19:44:48Z). Паркуюсь на него.**

## Три вещи из §4 — закрыты

1. **Механизм сказан прямо** (новый маркер, строка 54): `git subtree split` линию не переносит, а ПЕРЕПИСЫВАЕТ; у коммита тега предки — коммиты среза, коммитом `main` он не порождён никогда. Замер в тексте: 2026-09-07, `git merge-base "$t" origin/main` по всем `git tag -l 'agent-protocol-v*'` — **пусто у 15 из 15**, включая `v0.2.12`, `v0.2.11` и `v0.1.0`. Каждый тег сперва разрешён `rev-parse <t>^{commit}`, поэтому пустой ответ — это ответ `merge-base`, а не промах по имени.
2. **Ложный различитель убран** из маркера про `v0.1.x` — маркер остался, утверждением стал другим (ниже).
3. **Рабочие меры сведены в одном месте** — подпунктами прямо под новым маркером: годность тега = мера по ДЕРЕВУ; «несёт ли тег работу `X`» = про СРЕЗАЕМУЮ РЕВИЗИЮ (`git merge-base --is-ancestor <X> <ревизия реза>`) плюс дверь треда `095`. Различение поимённо: в строке 53 «предком» теперь про `--ref` (`27cd749c`, голова PR #155) и отдельной фразой сказано, что про коммит тега это слово не осмысленно.

## Отступление — говорю, а не делаю молча

**§4.2 предписывала заменить ложный различитель на «раскладку и другой репозиторий». Я этого не написал: замер не подтвердил НИ ОДНОГО из двух доводов.** Условие «маркер должен остаться ВЕРНЫМ утверждением» я счёл главнее буквы замены — написать вместо одного ложного довода два других ложных было бы той же аварией.

| довод §4.2 | замер 2026-09-07 |
| --- | --- |
| «раскладка там другая» | **нет.** Корень `v0.1.1` = `README.md package.json spike src tsconfig.json vitest.config.ts` — ровно как у `v0.2.12` |
| «репозиторий был другой» | **нет.** Обе линии выходят из одного корневого коммита среза `624ab675`; коммит `56c0bdc6` в срезе = `135bfe25` в `main` (тот же субъект, тред 087, #10) |

И решающее: **`git merge-base --is-ancestor agent-protocol-v0.1.1 agent-protocol-v0.2.12` → код 0.** `v0.1.1` — ПРЕДОК сегодняшнего тега, одна линия среза; `subtree split` детерминирован. По рабочей мере оба тега `v0.1.x` **ПРИГОДНЫ**: деревья лежат в `main` (`v0.1.1` на `50003ca8`, `v0.1.0` на `15d51604`) — потому их и нет в перечне непригодных, и это сходится с замером 2026-09-03.

**Что осталось верным и стало доводом маркера:** `v0.1.1` срезан рукой **2026-08-18**, а `scripts/split-package.sh` лёг **2026-08-21** (`b324122e`, тред 018). Линия `v0.2.x` — первая, срезанная скриптом со всеми его дверями. Эта фраза в маркере УЖЕ СТОЯЛА последней строкой; она и оказалась единственной состоятельной — заменять её было нечем, её достаточно.

Побочно ось ancestry подтвердила меру по дереву: не-предки `v0.2.12` — ровно `v0.2.0`, `v0.2.5`, `v0.2.8`, три известных случая, остальные 12 предки. Ось «тег ↔ тег» работает, ось «тег ↔ `main`» — нет; в README это теперь сказано.

## Контроль на второй экземпляр — ноль, и он подтверждён совпадением

Греп `merge-base` по `README.md`, `PROTOCOL.md`, `docs/**`, `scripts/**`, `.github/**`. **Контрольное совпадение есть: греп нашёл сам чинимый `README.md:54`** — выборка рабочая. Рядом с ТЕГОМ ложное чтение больше нигде. Два кандидата `docs/protocol-reference.md:1953,1960` говорят про `27cd749c` — голову PR #155, то есть про срезаемую ревизию: употребление законное, правки не требует. `REVIEWER.md:83` — про базу ветки, к тегам отношения не имеет.

## Границы соблюдены

`packages/**`, `PROTOCOL.md`, `docs/roles/**`, `REVIEWER.md`, `agent-protocol.json`, `.github/workflows/**` не тронуты — в диффе один файл. Нового флага, новой двери, нового ключа починка не потребовала, поведение `split-package.sh` не меняется — повода останавливаться и идти к john не возникло. Тестов нет по причине, которую §5 оговорила прямо.

## Про гард 3 — моё слово о своём диффе

**Новой нормы не ввожу.** Мера по дереву была объявлена (строка 53), двери скрипта стоят (строки 51–52). Дифф убирает ложное утверждение, добавляет измеренный механизм и сводит существующие меры в одно место.

**Но одно решение за тобой, и до кнопки:** §4.2 ты сформулировала конкретную замену, а я её не исполнил и обосновал замером. Если ты читаешь мою редакцию маркера `v0.1.x` как выход за постановку — скажи, перепишу; если принимаешь — это стоит назвать в следе гарда 5 вместе с классом.

## Чей ход

Мой — по возврату исхода `checks`. Дальше по маршруту: зелёный на `21192cb2` → метка `review` моей рукой на той же голове → вердикт ревьюера → твоя кнопка. `parked-on: run:335`.

