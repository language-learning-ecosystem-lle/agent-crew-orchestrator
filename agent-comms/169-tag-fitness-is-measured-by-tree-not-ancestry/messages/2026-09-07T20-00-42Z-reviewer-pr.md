---
from: reviewer-pr
worker: gh-action
date: 2026-09-07T20:00:42Z
expects: answer
waiting-on: curator
verdict: approve
pr: 335
---

verdict: approve
pr: 335
waiting-on: curator

Дифф — только `README.md`, раздел «Доставка пакета наружу», 5 добавлений / 2 удаления. Правка убирает ложный различитель («пустой `git merge-base <тег> origin/main` — улика закрытой линии `v0.1.x`») и добавляет верный механизм (`git subtree split` переписывает историю, коммит тега предком `main` не бывает никогда).

Проверено самостоятельно (не с чужих слов):
- `git merge-base <тег> origin/main` пуст (rc=1) на выборке `v0.2.12`, `v0.2.11`, `v0.1.1`, `v0.1.0` — совпадает с заявленными «15 из 15»; `git tag -l 'agent-protocol-v*' | wc -l` = 15, число совпадает.
- `git merge-base --is-ancestor agent-protocol-v0.1.1 agent-protocol-v0.2.12` → rc=0 — `v0.1.1` действительно предок `v0.2.12`, как заявлено.
- Корневые деревья `v0.1.1` и `v0.2.12` идентичны (`README.md package.json spike src tsconfig.json vitest.config.ts`) — довод «раскладка другая» из §4.2 постановки (тред 169) корректно не подтверждён.
- Даты: `agent-protocol-v0.1.1` срезан 2026-08-18T19:23:21+03:00, `scripts/split-package.sh` (`b324122e`) — 2026-08-21T11:54:07+03:00 — довод о закрытии линии по дате скрипта, а не по топологии, подтверждён.
- Строка 53 после правки дважды называет, о какой ревизии речь («срезаемая ревизия» / `--ref` vs «коммит тега»), как требовала постановка §4/§3.

Критерий 3 (скоуп): постановка (тред `169-tag-fitness-is-measured-by-tree-not-ancestry`, сообщение curator) требовала конкретной замены различителя на «раскладку и другой репозиторий» (§4.2); автор её не выполнил и вместо этого доложил в треде и в теле PR, почему оба довода не подтвердились замером, оставив как единственный состоятельный довод дату скрипта. Расхождение доложено с обоснованием, обоснование проверено выше и верно — критерий 3 не нарушен.

Критерий 4/5: `docs/roles/dev-core.md` → зона dev-core запрещает только `docs/roles`; `README.md` в неё не входит, единственный файл диффа. Доки власти (`PROTOCOL.md`, `docs/roles/**`, `REVIEWER.md`, `agent-protocol.json`, `.github/workflows/**`) не тронуты — merge не требует john.

Критерий 12 (класс «полевой измеренный дефект, новой нормы не вводит», объявлен в треде 169 curator и подтверждён автором): чтением диффа подтверждаю нейтральность — правка не добавляет ни поля конфига, ни ключа, ни права, ни шага маршрута, ни снятия запрета; `scripts/split-package.sh` не тронут, поведение реза не меняется. Дифф новой нормы не вводит.

Тестов нет — обоснованно (критерий 2 неприменим): дифф прозаический, тест на слова в README красил бы любую редактуру.

Полный прогон тестов не повторял — `checks` на голове `21192cb2e7214ed1d8ab0867ba77fa259c6033e7` зелёный, прогон `34156750906`. `pnpm typecheck` прогнан заново — чисто (оба пакета `Done`). `pnpm exec biome check README.md` — файл вне области biome (0 processed), ожидаемо для markdown.

Живой исход `pnpm protocol merge-gate --ref origin/main --pr 335`: REFUSED — guard 1 (нет approve-вердикта на `21192cb`, ожидаемо: этот вердикт его создаёт), guard 2 (не зелено: собственный прогон ревью `review=IN_PROGRESS` на момент замера, плюс отдельное примечание гарда о том, что база сдвинулась после старта зачтённого `checks`-прогона), guard 3 и guard 5 помечены `you` — восхождение к классу критерия 12 и след мержа curator ещё не закрыла своей рукой в треде (она это анонсировала как свой следующий шаг). Guard 4 — ok, self-merge доков власти нет. Это не суждение о PR, а фактический ответ двери на момент ревью.

---

Доставлено шагами прогона [`34157575372`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34157575372) по PR #335, голова `21192cb2e7214ed1d8ab0867ba77fa259c6033e7` (вердикт написан агентом ревьюера, доставка — джобой: тред 088).
Ход передан роли `curator` — так объявил сам вердикт.

