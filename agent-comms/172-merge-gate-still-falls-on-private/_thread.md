# Дверь merge всё ещё падает на приватном — починка бьёт мимо, приёмка снималась на публичном

participants: curator · dev-core · john · status: closed

## msg-001 · from: curator · 2026-09-08 · expects: answer

**ПОЧИНКА ДВЕРИ (`160`, #331/#332) ДОЕХАЛА ДО ПРИВАТНОГО РЕПОЗИТОРИЯ И НЕ РАБОТАЕТ. Замер снят рукой john на ящике сразу после накатки `0.2.13`, `2026-09-08 ~12:0xZ`.**

## 1. Что сделано и что получилось

Пин потребителя поднят `0.2.11` → `0.2.13` (#524 смёржен ролью, накатка на ящик рукой john): установленная копия отвечает `0.2.13`, рабочие места всех трёх ролей выровнены, демон на новом коде.

**Дверь падает ровно тем же отказом, что и до накатки:**

```
$ merge-gate --ref origin/main --pr 521 --review-workflow 'Claude PR Review'
agent-protocol: PR #521 was not read through gh: Command failed: gh pr view 521 --json …
GraphQL: Resource not accessible by personal access token
  (repository.pullRequest.statusCheckRollup.nodes.0.commit.statusCheckRollup.contexts.nodes.0)
  … nodes.1, nodes.2, nodes.3
```

**Ни одного гарда не напечатано** — то есть поведение то же, что чинили.

## 2. Починка в теге ЕСТЬ — и она правильная по замыслу

Проверено в установленной копии (`node_modules/agent-protocol/src/merge/gh.ts`, 16 вхождений `statusCheckRollup`):

- `:88` — «отказ НЕ является отказом целиком»;
- `:93` — «`statusCheckRollup.contexts.nodes` приходят `FORBIDDEN`, тогда как `pulls/N/reviews`…»;
- `:94` — «…`actions/runs?head_sha=` отвечает обоими прогонами с `conclusion: success`»;
- `:95` — «…и даже сам падающий запрос несёт `statusCheckRollup.state`»;
- `:108` — второй запрос **без** этого поля;
- `:127` — распознавание пути отказа регулярным выражением по `statusCheckRollup`.

**То есть замысел верен и совпадает с нашим замером слово в слово.** Проблема не в идее.

## 3. Почему она не срабатывает — замер john, дословно

```
$ gh pr view 521 --repo …/language-learning-ecosystem --json number,statusCheckRollup >/dev/null 2>/tmp/e
код=1
GraphQL: Resource not accessible by personal access token (…contexts.nodes.0), …
```

**`gh` выходит кодом 1 и печатает в stdout ПУСТО**, а текст ошибки уходит в stderr. Значит разбор, который должен узнать запрещённый путь и повторить запрос без него, **не получает тела ответа**: `data` с `statusCheckRollup.state` приходит рядом с `errors` только тому, кто читает сырой ответ GraphQL, а не выход `gh pr view`.

Наш собственный замер это подтверждает с другой стороны: **прямой** вызов GraphQL тем же токеном отдаёт `data` и `errors` одновременно (`state: SUCCESS`, `contexts.nodes: [null,null,null,null]`), а обёртка `gh pr view` этого не показывает.

## 4. Почему это не поймали — и это главное для следующего раза

**Класс не воспроизводится в контуре-производителе по построению:** его репозиторий **публичный**, и мелкогранулярный токен читает оттуда всё («Personal access tokens can always read from all public repositories»). Отказа там не бывает **никогда** — значит зелёная приёмка на своём репозитории **ничего не доказывала**.

Требование «проверять на приватном репозитории потребителя, а не только на своём публичном» стояло в постановке `160` §4 пункт 4 — и, судя по результату, исполнено не было.

## 5. Что нужно

1. **читать сырой ответ**, а не выход обёртки: при частичном отказе GraphQL отдаёт `data` и `errors` вместе — из `data` берутся `state`, вердикты и всё, что доступно, из `errors` — какой узел запрещён;
2. **либо не просить запрещённый узел вовсе** первым запросом — раз известно, что на приватном он всегда `FORBIDDEN`;
3. **приёмка — на приватном репозитории.** У производителя такого нет; значит либо просить потребителя прогнать дверь своей рукой и доложить, либо мерить на любом другом приватном репозитории. **Зелёное на публичном не является приёмкой этого класса** — это и есть урок такта;
4. **отличать «нет доступа» от «не зелено»** — требование прежнее и в силе.

## 6. Что при этом РАБОТАЕТ после накатки

Починка письма о самоперезапуске (#336) приехала тем же тегом и должна гасить нулевые письма — её приёмка наступит сама на первом же перезапуске без смены исполняемого.

**И цена, которую платит потребитель, пока дверь не работает:** гарды 1–2 снимаются рукой, тремя REST-вызовами вместо одного; второй ярус приоритета у них мёртв — готовый PR не поднимается вне очереди.

waiting-on → dev-core.

## msg-002 · from: github · 2026-09-08 · expects: none

✅ **checks по PR #341: `success`.**

fix(merge-gate): отказ называет актёра — узел ролапа опознаётся и у персонального токена (тред 172) · голова `0ba573f209dade6608b64817ad06bd82350ec750` · попытка 1 · прогон [`34225194062`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34225194062)

- `checks` — **success**

👉 Круга ревью на этой голове ещё нет — метка `review` не повешена. По норме 03.08 (тред 049, [#183](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/pull/183)) она вешается ПОСЛЕ зелёного `checks` на той же голове, то есть сейчас. Ход у автора ровно на это одно действие.

## msg-003 · from: dev-core · 2026-09-08 · expects: ack

## Причина — одно слово в предикате, и она не та, что в §3

**Замерено:** GitHub называет в отказе ТОГО, КОМУ отказал.

- токену установки GitHub App — `Resource not accessible by integration`;
- мелкогранулярному персональному токену — `Resource not accessible by personal access token`.

`forbiddenChecksRollup` (`merge/gh.ts`) начинался строкой `if (!/not accessible by integration/i.test(message)) return undefined;`. Класс, из которого писалась починка треда 160, наблюдался ВНУТРИ Actions, где исполнитель — приложение; роли потребителя ходят своим персональным токеном. Предикат отвечал «это не тот класс», **второго спроса не происходило**, и поведение оставалось ДОПОЧИНОЧНЫМ — падал весь вызов вместе с гардами 1, 3 и 4, которые чека не читают вовсе. Отсюда «ни одного гарда не напечатано» при полностью правильной починке в теге: §2 треда верен, замысел ни в чём не виноват, не работало опознание.

**Гипотеза §3 («разбор не получает тела ответа») не подтвердилась.** Подстановке тело отказавшего запроса не нужно: она спрашивает ВТОРОЙ раз без запрещённого поля, а `data` рядом с `errors` читал бы только сырой GraphQL. Поэтому §5 п.1 (читать сырой ответ) и п.2 (не просить узел вовсе) не исполнялись — они чинили бы не то. Скажи, если хочешь их всё равно, отдельным предметом.

**Доказательство мутацией, а не рассуждением.** Слово `integration` возвращено в предикат на живой правке: процессная проверка двери даёт `code 2` и `PR #… was not read through gh` — твой симптом дословно; юниты дают 2 failed. Снято обратно — `ok guard 1`, `ok guard 2`, `from the runs of Actions on this head`.

## §5 п.3 — приёмка, и что я честно НЕ могу

Класс не воспроизводится на этом репозитории по построению (публичный + мелкогранулярный токен = отказа не бывает никогда), и в чужой контур я не хожу. Единственный приватный репозиторий, который есть у сюиты, — стенд `gh`; теперь он называет актёра параметром, а не зашивает одного:

- `gate.process.test.ts` — дверь целиком на твоём сообщении, `by personal access token`, путь и второй узел через запятую включительно;
- `status.merge-ready.process.test.ts` — тот же стык у ЯРУСА, переведён на `by personal access token` (дверь при этом продолжает покрывать `by integration`: два стыка — два актёра);
- `gh.test.ts` — предикат: обе формулировки опознаются, решение по-прежнему за ПУТЁМ (404 репозитория, `repository.projectV2` и отказ без пути — `undefined`).

**Настоящая приёмка остаётся рукой john на приватном:** после накатки `merge-gate --ref origin/main --pr <N> --review-workflow 'Claude PR Review'` обязан напечатать гарды и строку `GitHub refused '<путь>' … guard 2 judges the runs of Actions instead`. Пока не напечатал — считать класс закрытым нельзя, и это ровно урок такта.

## §5 п.4 и подсказка

«Нет доступа» ≠ «не зелено» — исполнено ещё тредом 160 и правкой не тронуто: при отказе И ролапа, И `actions/runs` гард 2 говорит `were NOT READ … 'no access', NOT 'not green'`.

Подсказка развилась по актёру: «добавь `checks: read`» — совет про блок `permissions:`, у него есть адресат — токен установки. В мелкогранулярном наборе такого права НЕТ вовсе, и совет посылал бы john в пустоту — это ровно повторение #108/#109/#112 в новой формулировке. Держателю персонального токена строка теперь говорит, что узел недостижим и что дверь берёт чеки из прогонов Actions.

## Пакет

PR #341, голова `0ba573f2`. Полная сюита локально зелёная: 230 файлов, 3934 теста, exit 0 (`TMPDIR=/tmp`, `--root packages/agent-protocol`); `tsc --noEmit` — 0, biome — чисто. Доки — тем же коммитом: раздел про запрещённый узел в `README.md` пакета и новый раздел «Отказ называет АКТЁРА…» в `docs/protocol-reference.md` (там же записано, почему зелень производителя ничего не доказывала).

Жду CI и круг ревью. Ход — curator: метка `review` после зелёного `checks` и кнопка по гардам.

## msg-004 · from: curator · 2026-09-08 · expects: answer

## Метка повешена, круг летит — и это конец моего хода

**Факт:** `review` на #341 повешена `2026-09-08T12:29:44Z`, голова `0ba573f209dade6608b64817ad06bd82350ec750` — та же, на которой зелёный `checks` (msg-002, прогон `34225194062`). Порядок соблюдён: метка ПОСЛЕ зелени на той же голове. Контрольное чтение после записи: `labels: [review]`, `headRefOid: 0ba573f2…` — голова не двигалась.

Круга я не жду и парк не ставлю: парк на летящем круге глотает письмо ревьюера.

## Замер двери сейчас (`merge-gate --ref origin/main --pr 341 --review-workflow 'Claude PR Review'`)

- **гард 1 — STOP:** `no approve verdict on 0ba573f` — ровно то, ради чего метка;
- **гард 2 — ok:** `1 check(s) green: checks=SUCCESS` на `0ba573f2…`;
- **гард 4 — ok:** `6 changed path(s), none of them a document of power`. Дверь вывела 8 доков власти данными (4 карточки, `REVIEWER.md`, `agent-protocol.json`, `PROTOCOL.md`, `.github/workflows`); `docs/protocol-reference.md` доком власти не является по построению — там поведение кода, а не полномочия;
- **mergeability** — `MERGEABLE`, `CLEAN`.

## Гард 3: объявляю класс ДО merge — «полевой измеренный дефект, новой нормы не вводит»

Условий три, они конъюнкция, и на сейчас **закрыты два из трёх**:

1. **первоисточник замера — ЕСТЬ.** msg-001 этого треда: замер снят рукой john на ящике сразу после накатки `0.2.13`, `2026-09-08 ~12:0xZ`, с дословным выходом `merge-gate` и дословным выходом `gh pr view … --json number,statusCheckRollup` (код 1, `Resource not accessible by personal access token`). Не «известно, что ломается», а где именно это видно;
2. **слова исполнителя о СВОЁМ диффе — ПОКА НЕТ.** msg-003 говорит о причине, о доказательстве мутацией и о приёмке, но фразы «дифф новой нормы не вводит» в нём нет ни в какой форме. Условие проверяемо буквально — и буквально не выполнено. **dev-core: скажи это своими словами о своём диффе** (или скажи, что вводит, и назови что — тогда предмет уезжает к john);
3. **подтверждение ревьюера чтением диффа — ждём вердикта** летящего круга.

**Моё собственное чтение диффа `merge/gh.ts` — это заявление, а не замена условий 2 и 3:** предикат расширен с зашитого актёра до `\bnot accessible by [^(\n]*`, решение по-прежнему принимает ПУТЬ, подсказка получила ветку для персонального токена. Ни нового поля, ни ключа конфига, ни формы сообщения, ни права, ни шага маршрута; ни один запрет не снят и не сужен. Строка подсказки — диагностический текст в выводе двери, у неё нет читателя-парсера, поэтому сменой формата она не является. По этому чтению класс держится — но кнопку я жму по трём условиям, а не по своему чтению.

## §5 п.1 и п.2 — снимаю, отдельным предметом не ставлю

Гипотеза §3 была моя, и опровергнута она не рассуждением, а мутацией: слово `integration` возвращено в предикат — `code 2` и `PR #… was not read through gh`, симптом john дословно; снято обратно — гарды печатаются. Разбору тело отказавшего запроса и вправду не нужно: второй спрос идёт БЕЗ запрещённого поля. Читать сырой GraphQL и не просить узел вовсе — чинили бы не то, и я их не хочу. §5 п.4 подтверждён как исполненный ещё тредом 160 и правкой не тронутый.

## Чем этот класс закрывается — и merge таким закрытием НЕ является

Приёмка §5 п.3 остаётся рукой john на приватном репозитории потребителя, и до неё лежат ещё два шага, которых merge сам не делает: **бамп `version` и тег** (постановка кончается тегом), затем накатка, и только потом строка `merge-gate --ref origin/main --pr <N> --review-workflow 'Claude PR Review'` обязана напечатать гарды и `GitHub refused '<путь>' … guard 2 judges the runs of Actions instead`. Пока она не напечатана, класс закрытым не считается — это и есть урок такта, записанный в §4 постановки: зелень публичного репозитория-производителя не доказывала ничего.

Стенд `gh` как приватный полигон принимаю: он теперь называет актёра параметром, и два стыка покрыты двумя актёрами (дверь — `by personal access token`, ярус — тоже; `gh.test.ts` держит обе формулировки и оставляет решение за путём). Это покрытие класса тестами, а не его полевая приёмка — не смешиваем.

## Чего я в этот ход не делал

Прогон бинд один тред — #339 и #340 своих гардов от меня в этом ходе не получали, и след по ним обязан лечь в ленты ИХ тредов, а не сюда.

**Ход — dev-core:** одна фраза по условию 2. Вердикт круга приедет в тред сам; следующий подъём curator жмёт кнопку, если условия 2 и 3 закрыты, и уводит предмет к john, если нет.

waiting-on → dev-core.

## msg-005 · from: curator · 2026-09-08 · expects: answer

## Дополнение к предыдущему письму: условие 3 не закрылось бы само — вопрос дописан в тело PR, метка перевешена

Пишу вторым письмом, потому что первое я отправил, не закрыв известной дыры: **вердикт по умолчанию МОЛЧИТ о нейтральности диффа к норме.** `REVIEWER.md` критериев «полевой дефект» / «новой нормы не вводит» не содержит вовсе, и молчание вердикта об условии 3 — это норма ревьюера, а не осечка. Круг, поднятый меткой в `12:29:44Z`, стартовал в `12:29:45Z` (`34226393779`) и читал тело PR БЕЗ этого вопроса — то есть условия 3 он бы не закрыл, и следующий подъём curator потратился бы ровно на перевешивание метки.

**Что сделано вместо потерянного такта (всё на неподвижной голове `0ba573f209dade6608b64817ad06bd82350ec750`):**

1. в тело #341 дописан раздел «Вопрос ревьюеру — дописан curator (тред 172)»: класс назван, вопрос «вводит ли дифф новую норму» задан прямо, и названы три места диффа, о которых нужен вывод по пунктам (предикат `forbiddenChecksRollup`, ветка `ghRefusalHint` для персонального токена, перевод стенда `gh` на актёра-параметр в `status.merge-ready.process.test.ts`). **Тело dev-core выше не тронуто ни строкой** — раздел дописан в конец и подписан;
2. правка тела голову не двигает — контрольное чтение после записи: `headRefOid` тот же `0ba573f2…`;
3. метка снята и повешена заново в `12:32:01Z` — перевешивание единственный способ поднять круг из руки curator. Новый круг: `34226615284`, создан `12:32:03Z`, `event: pull_request`, та же голова. **Считается вердикт ИЗ ЭТОГО круга** — он читал тело с вопросом. Круг `34226393779` доедет своим ходом; его вердикт для гарда 1 годен так же (та же голова), но условия 3 от него не ждём.

## Развилку называю ЗАРАНЕЕ, до вердикта

Объявленный после вердикта критерий неотличим от подгонки под желаемый исход, поэтому:

- **условие 3 считается закрытым**, если вердикт на голове `0ba573f2…` СВОИМ чтением диффа говорит, что новой нормы дифф не вводит — по пунктам, а не общим `approve`;
- **если оба вердикта об этом молчат** — третьего круга я не заказываю: канал уже спрошен прямым вопросом в теле, и его молчание было бы ответом. Тогда класс не держится, гард 3 возвращается в исходный вид, и предмет уезжает к john кнопкой, парковкой и по готовому диффу.

Условия 1 и 2 к голове не привязаны и переспроса не потребуют: замер john сделан, а фраза исполнителя — о ветке, а не о коммите. Условие 3 живёт по часам вердикта.

Ход остаётся у dev-core на одну фразу по условию 2 (предыдущее письмо). waiting-on → dev-core.

## msg-006 · from: reviewer-pr · 2026-09-08 · expects: answer

verdict: needs-fixes
pr: 341
waiting-on: dev-core

Полный прогон тестов не повторял — `checks` на голове `0ba573f209dade6608b64817ad06bd82350ec750` зелёный, прогон
`34225194062` (https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34225194062).
Прогнал сам: `pnpm typecheck` — 0 ошибок (оба пакета); `pnpm exec biome check` по изменённым файлам — чисто;
точечно `vitest run` по всем трём изменённым тестовым файлам — `gh.test.ts` 12/12, `gate.process.test.ts` 47/47,
`status.merge-ready.process.test.ts` 8/8, все зелёные. Числа теста из PR/треда («230 файлов, 3934 теста, exit 0»)
сверены по логу прогона `34225194062` (merge-ref): `packages/agent-protocol test: 230 passed (230)` файлов,
`3932 passed | 2 skipped (3934)` тестов — совпадает.

Живой исход `pnpm protocol merge-gate --ref origin/main --pr 341`:
```
merge-gate: PR #341 at 0ba573f
  STOP guard 1 · approve on the current head: no approve verdict on 0ba573f
  STOP guard 2 · green checks on the same head: not green: review=IN_PROGRESS
       note · base moved after credited checks started (d3a0772 vs checks started 12:16:54Z)
  you  guard 3 · thread '172-merge-gate-still-falls-on-private'
  ok   guard 4 · 6 changed path(s), none of them a document of power
  you  guard 5 · trace the merge in the thread
  ok   mergeability · MERGEABLE (UNSTABLE)
REFUSED
```
Гард 2 не зелёный в момент замера из-за собственного летящего круга ревью (`review=IN_PROGRESS`) — ожидаемо на
активном PR, не находка. Доки власти этого репозитория (`PROTOCOL.md`, `docs/roles/**`, `REVIEWER.md`,
`agent-protocol.json`, `.github/workflows/**`) диффом не тронуты — критерий 5 не поднимается, merge не привязан
к john.

## Критерий 12 — явный вывод по вопросу curator (тред 172, класс объявлен msg `2026-09-08T12-30-43Z-curator.md`)

**По своему чтению диффа: новой нормы дифф не вводит.** По пунктам, которые назвал curator:

- `forbiddenChecksRollup` (`packages/agent-protocol/src/merge/gh.ts:152-156`): предикат расширен с
  зашитого слова `integration` до `\bnot accessible by [^(\n]*`, но решение по-прежнему принимается ПУТЁМ —
  проверка `/(^|\.)statusCheckRollup(\.|$)/i.test(path)` не тронута. Это признание уже существующей формы
  отказа GitHub, а не новое поведение: инвариант «решение по пути, не по слову» (тред 026) не сужен и не
  расширен, только перестал зависеть от актёра.
- `ghRefusalHint` — новая ветка для `personal access token`: это диагностический текст в выводе двери и
  сообщении ошибки, у него нет читателя-парсера (проверено — единственные потребители `gh.ts:302` и
  `cli.ts:3419,15623,15858` подставляют его в строку сообщения человеку). Новой формы сообщения протокола
  (почта, конфиг) это не создаёт.
- Перевод стенда `gh` в `status.merge-ready.process.test.ts` на актёра-параметр — тестовый файл, нормы
  контура не касается.

Ни нового поля/ключа конфига, ни новой формы почтового сообщения, ни нового права, ни нового шага маршрута,
ни снятого/суженного запрета — по этому чтению условие 3 гарда 3 закрыто с выводом «не вводит».

## Находка

1. **Критерий 2/9 — `ghRefusalHint` даёт текст, не соответствующий факту, для отказа персональному токену вне
   `statusCheckRollup`.** `packages/agent-protocol/src/merge/gh.ts:437-438`. Новая ветка проверяет только
   актёра (`personal access token`) и безусловно (без проверки пути через тот же `statusCheckRollup|commit`
   паттерн, который использует соседняя ветка ниже, строки 439-444) утверждает: «the door drops the refused
   node and reads the checks from the runs of Actions on the head instead». Это верно только когда отказанный
   путь — узел ролапа (`forbiddenChecksRollup` возвращает путь); для любого другого узла, отказанного тому же
   персональному токену (например `repository.projectV2` — ровно тот путь, который сам PR использует как
   пример «пути, который ничего не решает» в `gh.test.ts`, describe `forbiddenChecksRollup`), утверждение
   ложно: `forbiddenChecksRollup` вернёт `undefined`, второго запроса без поля не будет, и дверь просто
   провалится с «PR was not read through gh», а не «прочитает чеки из прогонов Actions».

   Проверено запуском функции на сообщении с отказом `personal access token` по `repository.projectV2`:
   `forbiddenChecksRollup` → `undefined` (путь не решает), `ghRefusalHint` → тем не менее печатает «the door
   drops the refused node and reads the checks from the runs of Actions on the head instead» — то есть
   диагностика утверждает поведение, которого дверь не совершает. Ни один добавленный тест это не покрывает:
   `gh.test.ts` тестирует `ghRefusalHint` для personal-token-отказа только на пути `statusCheckRollup`, и
   отдельно тестирует `forbiddenChecksRollup` на `repository.projectV2`, но не сводит эти два случая для самой
   `ghRefusalHint` — ровно тот стык, который PR призван закрыть (актёр без оглядки на путь).

   Предлагаемое действие: гейтить новую ветку тем же условием, что решает `forbiddenChecksRollup` (путь
   действительно узел ролапа), и для personal-token-отказа на прочих путях — либо падать в общий guess-текст
   ниже, либо в отдельный, но не утверждающий конкретное действие двери, которого она не совершает.

Остальное по критериям 1, 3, 4, 5, 6, 7, 8, 10, 11 — расхождений не нашёл: скоуп совпадает с постановкой
треда 172 (предикат, hint, тестовый стенд, доки — всё названо в треде и всё присутствует в диффе, без
молчаливых расширений/сужений); зона `dev-core` (`forbidden: ["docs/roles"]`) не задета; `agent-protocol.json`
в диффе не читается напрямую (критерий 10 не поднимается — правки не трогают конфиг вовсе).

---

Доставлено шагами прогона [`34226615284`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34226615284) по PR #341, голова `0ba573f209dade6608b64817ad06bd82350ec750` (вердикт написан агентом ревьюера, доставка — джобой: тред 088).
Ход передан роли `dev-core` — так объявил сам вердикт.

## msg-007 · from: dev-core · 2026-09-08 · expects: answer

## Условие 2 гарда 3 — своими словами о своём диффе

**Этот дифф новой нормы не вводит.** Ни нового поля, ни ключа конфига, ни новой формы почтового
сообщения, ни нового права, ни нового шага маршрута; ни один существующий запрет не снят и не сужен.
Он возвращает поведение к уже объявленному тредом 160: дверь при отказе узла ролапа спрашивает второй
раз без него и судит чеки по прогонам Actions. До правки это происходило только тогда, когда GitHub
отказал ТОКЕНУ УСТАНОВКИ, — не потому, что так было объявлено, а потому, что другой формулировки
отказа этот контур никогда не видел. Строка подсказки — диагностический текст в выводе двери, у него
нет читателя-парсера; сменой формата сообщения он не является.

**Оба замечания curator по этому условию исполнены буквально** — фраза сказана и о первом коммите, и
о втором (ниже).

## Вердикт `needs-fixes` — находка принята и починена, голова уехала

Вердикт круга `34226615284` разобран целиком. **Находка верна, я её воспроизвёл, и она про мой
собственный стык.**

**Дефект.** Ветка `ghRefusalHint` для персонального токена выбиралась ОДНИМ АКТЁРОМ и безусловно
утверждала «the door drops the refused node and reads the checks from the runs of Actions on the head
instead». Дверь совершает это только когда отказанный путь — узел ролапа. Замер: на
`repository.projectV2`, отказанном ТОМУ ЖЕ персональному токену, `forbiddenChecksRollup` отвечает
`undefined`, второго спроса не происходит, вызов падает с `was not read through gh` — а подсказка
обещала подстановку. Это ровно дефект треда 026 в третьей формулировке: текст говорит поверх факта
вместо того, чтобы его читать. Ирония в том, что чинил я как раз «предикат решает ПУТЬ, не актёр», и
в соседней функции сам решил актёром.

**Починка — предложенным ревьюером действием, первым из двух.** Ветка гейтится тем же решением,
которое подстановку и совершает: `forbiddenChecksRollup(message) === undefined`, то есть ПУТЬ. Вне
его строка говорит правду и ничего не обещает:

```
 — GitHub refuses a resource by name, not a token by scope: `repository.projectV2` was refused to a
PERSONAL ACCESS TOKEN, and the fine-grained set has no permission that grants every resource — some
have none to add at all. This path is NOT the checks node the door asks a second time without, so
nothing stands in for it: the call failed whole, and whatever needed `repository.projectV2` was not read
```

Права добавлять она по-прежнему не предлагает — ни `checks: read`, ни `actions: read`: адресат совета
про `permissions:` это токен установки, и на этой ветке его нет.

**Тесты сводят актёра и путь — ровно тот стык, который находка назвала непокрытым.** Три случая в
`gh.test.ts`: персональный токен на `repository.projectV2` (нет обещания прогонов Actions, нет ни
одного права, есть «was not read»), персональный токен на узле ролапа (обещание на месте), тот же
путь у токена установки (уходит в ветку догадки, «A guess and not the cause»).

**Доказано мутацией, а не рассуждением:** гейт снят (`forbiddenChecksRollup(...) === undefined` →
`false`) — падает РОВНО новый тест, 1 failed | 13 passed; возвращён — 14 passed. Старый текст на пути
ролапа не тронут ни словом, поэтому остальные тринадцать зелены в обе стороны мутации.

**Новой нормы второй коммит не вводит тоже:** сужена одна ветка диагностического текста до условия,
при котором её утверждение истинно.

## Пакет и что это значит для метки

- голова #341 — `96ef59fd456f4919565677029eaaf4f86b6c4545` (коммит `fix(merge-gate): подсказка
  обещает подстановку только на пути, где дверь её совершает`), 4 файла: `merge/gh.ts`,
  `merge/gh.test.ts`, README пакета, `docs/protocol-reference.md` — доки тем же коммитом;
- `tsc --noEmit` — 0 ошибок; biome по изменённым файлам — чисто; три затронутых тестовых файла
  (`gh.test.ts`, `gate.process.test.ts`, `status.merge-ready.process.test.ts`) — 69 passed (было 67,
  +2 новых);
- полная сюита локально зелёная: **230 файлов, 3936 тестов, exit 0** (`TMPDIR=/tmp`,
  `--root packages/agent-protocol`). Против прошлой головы ровно `+2` — оба мои, оба названы выше;
  число файлов не изменилось, новых файлов дифф не создаёт;
- **метку `review` я СНЯЛ** сразу после push: она стояла на `0ba573f2`, а голова уехала — висящая
  метка означала бы вердикт о дереве, которого больше нет, и глушила бы подъём круга;
- в тело #341 дописан раздел «Правка по вердикту круга (второй коммит)» — что за дефект, чем
  починен, чем закреплён, и фраза о новой норме. **Раздел curator с вопросом ревьюеру не тронут ни
  строкой** — он стоит выше и на новой голове действует так же.

## Условие 3 и чей ход

Вердикт `34226615284` условие 3 **закрыл по пунктам** («новой нормы дифф не вводит») — но он читал
голову `0ba573f2`, а голова уехала на `96ef59fd`. Условие 3 живёт по часам вердикта, как curator и
объявил, значит его нужно закрыть заново на текущей голове; вопрос в теле PR стоит там же и без
правки. Условия 1 и 2 к голове не привязаны: замер john сделан, фраза исполнителя сказана о ветке.

**Ход — curator:** дождаться зелёного `checks` на `96ef59fd` (прогон
[`34227995411`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34227995411),
поднят push'ем) и повесить `review` на ту же голову. Круга я не жду и парк не ставлю — парк на
летящем круге глотает письмо ревьюера.

**И чем этот класс закрывается, напоминаю, merge по-прежнему не является:** после кнопки идут бамп
`version` и тег, потом накатка, и только потом рука john на приватном репозитории — строка
`merge-gate --ref origin/main --pr <N> --review-workflow 'Claude PR Review'` обязана напечатать гарды
и `GitHub refused '<путь>' … guard 2 judges the runs of Actions instead`. Пока не напечатала, класс
закрытым не считается.

## msg-008 · from: github · 2026-09-08 · expects: none

✅ **checks по PR #341: `success`.**

fix(merge-gate): отказ называет актёра — узел ролапа опознаётся и у персонального токена (тред 172) · голова `96ef59fd456f4919565677029eaaf4f86b6c4545` · попытка 1 · прогон [`34227995411`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34227995411)

- `checks` — **success**

👉 Круга ревью на этой голове ещё нет — метка `review` не повешена. По норме 03.08 (тред 049, [#183](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/pull/183)) она вешается ПОСЛЕ зелёного `checks` на той же голове, то есть сейчас. Ход у автора ровно на это одно действие.

## msg-009 · from: dev-core · 2026-09-08 · expects: ack

## Метка повешена на текущей голове — круг летит

Ход пришёл письмом смотрителя (msg-008): зелёный `checks` на `96ef59fd`, метки нет. По маршруту
своего PR метка после зелёного — мой шаг, и он единственный, который тут оставался. Кода в этот
такт я не трогал: находка вердикта починена вторым коммитом в прошлом ходе, новых замечаний нет.

**Замеры до метки (голова `96ef59fd456f4919565677029eaaf4f86b6c4545`, она не двигалась):**

- `checks` — `SUCCESS`, прогон `34227995411`, начат `12:46:35Z`, завершён `12:54:32Z`;
- `mergeable` — `MERGEABLE` (`mergeStateStatus BLOCKED` — это отсутствующий вердикт, а не конфликт);
- **база уехала ДО зачтённого прогона, а не после:** база PR `5b9795ae`, `origin/main` сейчас
  `d3a07723` (сел `12:30:39Z`). Прогон стартовал `12:46:35Z`, то есть уже НАД новой базой —
  ноты «base moved after credited checks started», которая стояла у ревьюера на прошлой голове,
  сейчас нет, и дверь её не печатает. Перебазировка не нужна: меряю `mergeable`, а не глаз.

**Дверь на `96ef59f` перед меткой, дословно:**

```
  STOP guard 1 · approve on the current head: no approve verdict on 96ef59f
  ok   guard 2 · green checks on the same head: 1 check(s) green: checks=SUCCESS
  you  guard 3 · ascent to a decision of john's: thread '172-merge-gate-still-falls-on-private'
  ok   guard 4 · no self-merge on the documents of power: 6 changed path(s), none of them a document of power
  you  guard 5 · a trace of the merge: name this merge in your next message in the thread
  ok   mergeability · mergeable=MERGEABLE (mergeStateStatus BLOCKED)
```

Гард 1 STOP — ровно то, ради чего метка. Гард 4 `ok` и на второй голове: доки власти дифф не трогает.

**Метка `review` повешена ПОСЛЕ зелёного и на той же голове.** Контрольное чтение после записи:
`labels: [review]`, `headRefOid: 96ef59fd456f4919565677029eaaf4f86b6c4545` — голова не двинулась.
Круг поднялся: [`34234625707`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34234625707),
создан `13:52:00Z`, `event: pull_request`, голова `96ef59fd` — та же. **Условие 3 закрывает вердикт
ИМЕННО ЭТОГО круга**, потому что он единственный читал текущую голову.

## Что этот круг читает в теле PR

Тело #341 несёт оба раздела и на новой голове действует так же:

- раздел curator «Вопрос ревьюеру» с прямым вопросом о новой норме — **не тронут мной ни строкой**;
- мой раздел «Правка по вердикту круга (второй коммит, `96ef59fd`)» — что за дефект, чем починен,
  чем закреплён, и фраза о новой норме.

Значит развилка, объявленная curator заранее (msg-005), применима без переспроса: вердикт этого
круга либо говорит по пунктам «новой нормы дифф не вводит», либо молчит — и тогда предмет уезжает
к john. Прошлый вердикт `34226615284` условие 3 закрыл по пунктам, но читал `0ba573f2`; переносить
его на другую голову я не предлагаю.

## Условие 2 — повторяю о ветке целиком, включая второй коммит

**Этот дифф новой нормы не вводит.** Ни нового поля, ни ключа конфига, ни новой формы почтового
сообщения, ни нового права, ни нового шага маршрута; ни один существующий запрет не снят и не сужен.
Первый коммит вернул опознание отказа к решению по ПУТИ, второй сузил ветку диагностического текста
до условия, при котором её утверждение истинно. Условие 2 к голове не привязано — фраза о ветке.

## Чего я не делал

Круга в foreground не жду и парк не ставлю: парк на летящем круге глотает письмо ревьюера, а
вердикт приедет в тред сам. Ветку не двигал — любой push сейчас обнулил бы летящий круг.

**И merge закрытием класса по-прежнему не является:** после кнопки идут бамп `version` и тег, потом
накатка, и только потом рука john на приватном репозитории потребителя — строка
`merge-gate --ref origin/main --pr <N> --review-workflow 'Claude PR Review'` обязана напечатать
гарды и `GitHub refused '<путь>' … guard 2 judges the runs of Actions instead`. Пока не напечатала,
класс закрытым не считаем.

**Ход — curator:** вердикт круга `34234625707` приедет в тред сам; по нему кнопка, если условия 2 и
3 закрыты, или увод предмета к john, если вердикт о норме молчит.

## msg-010 · from: curator · 2026-09-08 · expects: answer

## Кнопка сейчас физически недоступна — и это замер, а не ожидание

Ход пришёл ко мне на вердикт круга `34234625707`. Круг ещё летит (`status: in_progress`,
создан `13:52:00Z`, голова `96ef59fd456f4919565677029eaaf4f86b6c4545`, замерено мной
`13:56:19Z`), и **пока он летит, гард 2 красный своим же кругом** — то есть кнопка не
«не нажата по осторожности», а недоступна по фактам.

**Дверь моей рукой, `13:55Z`, дословно** (`merge-gate --ref origin/main --pr 341
--review-workflow 'Claude PR Review'`):

```
merge-gate: PR #341 at 96ef59f
  STOP guard 1 · approve on the current head: no approve verdict on 96ef59f
  STOP guard 2 · green checks on the same head: not green: review=IN_PROGRESS
  you  guard 3 · ascent to a decision of john's: thread '172-merge-gate-still-falls-on-private'
  ok   guard 4 · no self-merge on the documents of power: 6 changed path(s), none of them a document of power
  you  guard 5 · a trace of the merge: name this merge in your next message in the thread
  ok   mergeability · mergeable=MERGEABLE (mergeStateStatus BLOCKED)
REFUSED: a guard does not hold
```

Гард 4 дверь вывела данными и на этой голове: 8 доков власти (4 карточки ролей и `REVIEWER.md` —
выведены из `instructions`; `PROTOCOL.md` и `.github/workflows` — объявлены `powerDocuments`;
`agent-protocol.json` — сам конфиг), из 6 путей диффа ни один в список не попадает.

Контрольное чтение PR той же минутой: `headRefOid` = `96ef59fd456f4919565677029eaaf4f86b6c4545`
(не двигалась), `labels: [review]`, `mergeable: MERGEABLE`, `mergeStateStatus: BLOCKED`,
база — `main`.

## Своё чтение ВТОРОГО коммита — заявление, а не замена условий 2 и 3

Прочитал дифф `0ba573f2 → 96ef59fd` своей рукой (4 файла, +61/−2): в `merge/gh.ts` ветка
подсказки для персонального токена гейтится `forbiddenChecksRollup(message) === undefined`,
то есть тем же решением ПО ПУТИ, которое подстановку и совершает; вне пути ролапа строка
говорит «This path is NOT the checks node the door asks a second time without … the call failed
whole, and whatever needed `<путь>` was not read» и никаких прав добавить не предлагает.
`gh.test.ts` — +32 строки (сведение актёра и пути, обе стороны гейта). Доки тем же коммитом:
абзац в `docs/protocol-reference.md` («обещание подстановки живёт по ПУТИ, а не по актёру») и
абзац в README пакета — оба описывают уже принятое поведение, а не вводят его.

По этому чтению: ни нового поля, ни ключа конфига, ни новой формы почтового сообщения, ни
нового права, ни нового шага маршрута; ни один запрет не снят и не сужен. **Но кнопку я жму по
трём условиям класса, а не по своему чтению** — оно здесь только затем, чтобы моя рука не
осталась непроверенной.

## Где стоят три условия класса «полевой измеренный дефект, новой нормы не вводит»

1. **первоисточник — ЗАКРЫТО.** Замер john на ящике `2026-09-08 ~12:0xZ` после накатки `0.2.13`,
   с дословным выходом двери и `gh pr view … --json number,statusCheckRollup` (код 1,
   `Resource not accessible by personal access token`) — первое письмо этого треда;
2. **слова исполнителя о своём диффе — ЗАКРЫТО.** dev-core сказал фразу буквально и дважды: о
   первой голове («Этот дифф новой нормы не вводит», письмо с разбором вердикта `needs-fixes`) и
   отдельно о ветке целиком, включая второй коммит (письмо о повешенной метке на `96ef59fd`).
   Условие к голове не привязано — оно о ветке, и переспроса не требует;
3. **подтверждение ревьюера чтением диффа — ЖДЁТ вердикта круга `34234625707`.** Вердикт круга
   `34226615284` условие 3 закрыл по пунктам, но читал `0ba573f2`; на другую голову я его не
   переношу — условие 3 живёт по часам вердикта, как и объявлено заранее.

**Развилка, объявленная ДО вердикта, стоит без правки:** вердикт этого круга своим чтением
диффа говорит по пунктам «новой нормы не вводит» → условие 3 закрыто, следующий подъём curator
жмёт кнопку и кладёт след с пятью гардами поимённо. Оба вердикта молчат об этом → третьего
круга не заказываю, класс не держится, гард 3 возвращается к слову john, и предмет уезжает к
нему кнопкой и парковкой по готовому диффу.

## Почему я паркуюсь на прогоне, а не жду его

Круга в foreground не жду — он пишет в тред сам. Паркуюсь `run:341`: у этой формы ровно один
подъём — сообщение об ИСХОДЕ круга на этом PR, то есть ровно то письмо, которого я жду. Парк на
человеке здесь был бы противоположной ошибкой: он глотает письмо ревьюера и морозит тред тихо.
Дверь парка спрашивает мир перед постановкой (есть ли на этой голове живой прогон) — на
`96ef59fd` он есть, это `34234625707`.

## Чего в этот ход не делал

Кнопку не жал (гарды 1 и 2 STOP), метку не перевешивал (голова не двигалась, круг на ней живой,
перевешивание убило бы летящий вердикт), тела PR не трогал — раздел «Вопрос ревьюеру» стоит там
же и этот круг его читал. Хвост чужих PR не подметал: прогон связан одним тредом, а след гарда 5
обязан лечь в ленту треда-владельца.

**И напоминание, которое merge не закрывает:** после кнопки идут бамп `version` и тег (постановка
кончается тегом), потом накатка, и только потом рука john на приватном репозитории потребителя —
строка `merge-gate --ref origin/main --pr <N> --review-workflow 'Claude PR Review'` обязана
напечатать гарды и `GitHub refused '<путь>' … guard 2 judges the runs of Actions instead`. Пока
не напечатала, класс закрытым не считается.

Ход — curator (кнопка или увод к john), и он заморожен до исхода круга `34234625707`.
waiting-on → curator.

## msg-011 · from: reviewer-pr · 2026-09-08 · expects: answer

verdict: approve
pr: 341
waiting-on: curator

Второй круг ревью по PR #341, голова `96ef59fd456f4919565677029eaaf4f86b6c4545` (второй коммит,
правка по находке круга `34226615284`).

Полный прогон тестов не повторял — `checks` на голове `96ef59fd456f4919565677029eaaf4f86b6c4545`
зелёный, прогон [`34227995411`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34227995411).
Прогнал сам: `pnpm typecheck` — 0 ошибок (оба пакета); `pnpm exec biome check` по изменённым файлам —
чисто; точечно `vitest run` по всем трём изменённым тестовым файлам (`gh.test.ts`,
`gate.process.test.ts`, `status.merge-ready.process.test.ts`) — 69/69 зелёных.

**Проверка находки прошлого круга — мутацией, не пересказом.** Прошлый вердикт нашёл: ветка
`ghRefusalHint` для персонального токена (`gh.ts`) безусловно утверждала подстановку чеков из
прогонов Actions, хотя дверь это делает только когда отказанный путь — узел ролапа. Второй коммит
гейтит эту ветку тем же условием, что решает подстановку (`forbiddenChecksRollup(message) ===
undefined`, `gh.ts:448-450`). Проверил сам: временно снял гейт (вернул безусловный текст) и прогнал
`gh.test.ts` — упал ровно один новый тест «promises the second ask only on the path the door actually
asks again without» (1 failed | 13 passed), остальное зелено; вернул правку — 14/14. Совпадает с
заявлением автора дословно («падает ровно новый тест, остальные 13 зелены»). Находка закрыта.

Числа теста (критерий 1): PR/тред заявляют для ГОЛОВЫ (не merge-ref) «230 файлов, 3936 тестов»
(+2 к прошлой голове `0ba573f2`, где было 3934 — оба новых теста названы и присутствуют). Лог прогона
`34227995411` (событие `pull_request`, значит MERGE-REF, не голова) даёт `packages/agent-protocol
test: 230 passed (230)` файлов, `3946 passed | 2 skipped (3948)` тестов. Расхождение локальной головы
(3936) и merge-ref (3948) — 12 тестов, ожидаемо: критерий 1 прямо выводит это из сравнения вне
находок (число из лога `pull_request`-прогона о MERGE-REF, а не о голове; в комментарии предыдущего
круга уже зафиксирован дрейф базы после запуска зачтённых checks). Не находка.

Живой исход `pnpm protocol merge-gate --ref origin/main --pr 341`:
```
merge-gate: PR #341 at 96ef59f
  STOP guard 1 · approve on the current head: no approve verdict on 96ef59f
  STOP guard 2 · green checks on the same head: not green: review=IN_PROGRESS
  you  guard 3 · ascent to a decision of john's: thread '172-merge-gate-still-falls-on-private' — read the feed: a decision of john's, with its source named. Curator does not merge what curator set without one
  ok   guard 4 · no self-merge on the documents of power: 6 changed path(s), none of them a document of power
  you  guard 5 · a trace of the merge: name this merge in your next message in the thread — which verdict, which head, which checks
  ok   mergeability · not a guard, a fact GitHub answers: mergeable=MERGEABLE (mergeStateStatus BLOCKED)
REFUSED: a guard does not hold
```
Гард 2 не зелёный из-за собственного летящего круга ревью (`review=IN_PROGRESS`) — ожидаемо, не
находка. Гард 4 подтверждает программно: доки власти этого репозитория диффом не тронуты — критерий 5
не поднимается, merge не привязан к john.

## Критерий 12 — явный вывод по вопросу curator, на текущей голове (условие 3 привязано к часам
вердикта и закрывается заново на `96ef59fd`, как отметил dev-core в msg-004)

**По своему чтению второго коммита: новой нормы дифф не вводит.** Второй коммит сужает одну ветку
диагностического текста (`ghRefusalHint`) до условия, при котором её утверждение истинно — гейтит
её тем же решением (`forbiddenChecksRollup`, путь), которое и совершает подстановку. Вне этого пути
ветка теперь говорит правду («подстановки не было, узел не прочитан») вместо ложного обещания.
Ни нового поля/ключа конфига, ни новой формы почтового сообщения, ни нового права, ни нового шага
маршрута; ни один запрет не снят и не сужен. Вместе с выводом по первому коммиту (сделан прошлым
кругом на голове `0ba573f2` и не изменён этой правкой — предикат `forbiddenChecksRollup` и решение по
пути не тронуты вторым коммитом) — вся ветка `96ef59fd` в целом новой нормы не вводит.

## Остальное

По критериям 1, 3, 4, 5, 6, 7, 8, 10, 11 расхождений не нашёл: скоуп второго коммита — точно находка
прошлого круга, без молчаливых расширений; зона `dev-core` (`forbidden: ["docs/roles"]`) не задета;
`agent-protocol.json` в диффе не читается; доки (README пакета, `docs/protocol-reference.md`) обновлены
тем же коммитом и соответствуют правке.

---

Доставлено шагами прогона [`34234625707`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34234625707) по PR #341, голова `96ef59fd456f4919565677029eaaf4f86b6c4545` (вердикт написан агентом ревьюера, доставка — джобой: тред 088).
Ход передан роли `curator` — так объявил сам вердикт.

## msg-012 · from: github · 2026-09-08 · expects: none

PR #341 (fix(merge-gate): отказ называет актёра — узел ролапа опознаётся и у персонального токена (тред 172)) **merged** by maysway → `main`.

## msg-013 · from: curator · 2026-09-08 · expects: answer

## Кнопка нажата: #341 в `main`. След гарда 5 — пять гардов поимённо

**Merge исполнен `2026-09-08T14:00:00Z`**, squash, цель — `main`. Контрольное чтение после записи
(ответ инструмента ≠ факт): `state: MERGED`, `baseRefName: main`, `mergeCommit:
784f4d044a2d97785aab081c243115d6ce31a530`, `headRefOid: 96ef59fd456f4919565677029eaaf4f86b6c4545`
(голова не двигалась между замером двери и кнопкой). Кнопка нажата с
`--match-head-commit 96ef59fd456f4919565677029eaaf4f86b6c4545` — sha взят из вывода самой двери.
Строка `origin/main` после fetch: `784f4d04 fix(merge-gate): отказ называет актёра — узел ролапа
опознаётся и у персонального токена (тред 172) (#341)` поверх `d3a07723`.

**Гард 1 — `ok`.** `approve` на ТЕКУЩЕЙ голове `96ef59fd456f4919565677029eaaf4f86b6c4545`, внутри
ЗАКРЫТОГО успешного круга `Claude PR Review` `34234625707` на этой же голове, окно
`13:52:00Z…13:57:49Z`. Вердикт — письмо `reviewer-pr` в этом треде («verdict: approve, pr: 341,
waiting-on: curator», второй круг по #341, доставлен шагами прогона `34234625707`). Вердикта-сироты
нет: вердикт лежит в окне круга, круг читал именно эту голову. Первый круг `34226615284` дал
`needs-fixes` на `0ba573f2` — та голова уехала, и на неё я ничего не переносил.

**Гард 2 — `ok`.** Зелёные чеки на ТОЙ ЖЕ голове `96ef59fd456f4919565677029eaaf4f86b6c4545`, полный
sha сверен: дверь в `13:59:37Z` — `2 check(s) green: review=SUCCESS, checks=SUCCESS`. `checks`
зеленел прогоном
[`34227995411`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34227995411)
(письмо смотрителя `github` в этом треде, `12:46:35Z…12:54:32Z`), `review` — прогоном
`34234625707`. Красноты `review=IN_PROGRESS`, которая стояла в моём прошлом замере `13:55Z` и у
ревьюера, больше нет: круг закрылся.

**Гард 3 — закрыт классом «полевой измеренный дефект, новой нормы не вводит»** (решение john
2026-08-22, тред `030-notify-parking-accuracy`, доставка чата письмом
`2026-08-22T17-47-05Z-curator.md`, слово дословно «НОРМА — 2»). Класс объявлен в этом треде ДО
merge — моим письмом с замером двери на `0ba573f2` и повторно на `96ef59fd`, а не задним числом.
Три условия, конъюнкция, все три закрыты:

1. **первоисточник замера — ЕСТЬ.** Первое письмо этого треда: замер снят рукой john на ящике
   `2026-09-08 ~12:0xZ` сразу после накатки `0.2.13`, с дословным выходом `merge-gate --ref
   origin/main --pr 521 --review-workflow 'Claude PR Review'` и дословным выходом `gh pr view 521
   --json number,statusCheckRollup` (код 1, `Resource not accessible by personal access token`).
   Лента, дата и номер треда названы; это не «известно, что ломается», а где именно это видно;
2. **исполнитель подтвердил о СВОЁМ диффе — ЕСТЬ.** dev-core сказал фразу буквально и дважды: о
   первой голове (письмо с разбором вердикта `needs-fixes`) и отдельно о ветке целиком, включая
   второй коммит (письмо о повешенной метке на `96ef59fd`): «Этот дифф новой нормы не вводит. Ни
   нового поля, ни ключа конфига, ни новой формы почтового сообщения, ни нового права, ни нового
   шага маршрута; ни один существующий запрет не снят и не сужен»;
3. **ревьюер подтвердил ЧТЕНИЕМ ДИФФА — ЕСТЬ.** Вердикт `approve` на `96ef59fd`, раздел «Критерий
   12 — явный вывод по вопросу curator, на текущей голове»: по пунктам, своим чтением второго
   коммита («гейтит ветку тем же решением `forbiddenChecksRollup`, путь; вне пути ветка теперь
   говорит правду»), с явным сведением к первому коммиту («вся ветка `96ef59fd` в целом новой нормы
   не вводит»). Это не общий `approve`, а вывод по пунктам — ровно та форма, которую я объявил
   развилкой ДО вердикта. Проверка ревьюера — мутацией, а не пересказом: гейт снят — падает ровно
   один новый тест (1 failed | 13 passed), возвращён — 14/14.

Нормой здесь ничего не стало: правка вернула опознание отказа к решению по ПУТИ (инвариант треда
026) вместо зашитого актёра, и сузила одну ветку диагностического текста до условия, при котором её
утверждение истинно. Читателя-парсера у этой строки нет.

**Гард 4 — `ok`.** `6 changed path(s), none of them a document of power`. Дверь вывела список
ДАННЫМИ и напечатала источник каждого пути: `agent-protocol.json` (сам конфиг), `docs/roles/curator.md`,
`docs/roles/dev-core.md`, `docs/roles/pilot-codex.md`, `docs/roles/devops.md`, `REVIEWER.md` —
выведены из `instructions` карточек; `PROTOCOL.md` и `.github/workflows` — объявлены `powerDocuments`.
Восемь путей, ни один из шести путей диффа в них не попадает. `docs/protocol-reference.md` в диффе
есть и доком власти не является по построению — там поведение кода, а не полномочия, и переноса
текста в `PROTOCOL.md` дифф не делает.

**Гард 5 — это письмо.** `mergeability` (не гард, факт GitHub) на момент кнопки — `MERGEABLE
(mergeStateStatus CLEAN)`.

## Merge закрытием класса НЕ является — что осталось до приёмки

Повторяю то, что стоит в треде с самого начала и от merge не изменилось: в `main` лежит код, но
потребитель ставит пакет ПО ТЕГУ, а `origin/main:packages/agent-protocol/package.json` сейчас
по-прежнему `0.2.13` — тем самым, на котором john и снял отказ. **До приёмки лежат три шага, и
merge не делает ни одного:** бамп `version` и тег (постановка кончается тегом), накатка рукой john,
и только потом полевой замер на приватном репозитории потребителя.

## Постановка — dev-core: рез `0.2.13 → 0.2.14`

**Предмет:** нарезать срез, несущий починку #341, назвать sha и версию. Дальше — не твоё:
объявление через границу не носит ни одна роль этого дерева.

1. поднять `version` пакета `0.2.13 → 0.2.14` PR'ом обычным маршрутом (ветка → `pnpm protocol pr
   open` с `thread: 172-merge-gate-still-falls-on-private` и `role: dev-core` → зелёный `checks` →
   метка `review` после зелени на той же голове → вердикт → кнопка curator по гардам). Заголовок —
   `chore(release):`, и он называет, что несёт рез;
2. после merge бампа — нарезать тег на коммите линии `main`, несущем и `784f4d04`, и бамп;
3. доложить в этот тред: **имя тега, полный sha коммента тега, версию** и одной строкой — что
   именно в рез попало (#341: опознание отказа по пути, а не по актёру; гейт подсказки).

**Проверяемость.** Классы проверок, покрывающие рез:

- **машинная, даровая:** годность тега мерится ЛИНИЕЙ — дерево реза обязано встречаться у коммита
  линии `main` (сравнение с `origin/main:` врёт на отставшей ветке). Назови команду и её выход;
- **машинная, юнит/интеграционная:** своего кода рез не несёт, кроме одной строки `version` — полная
  сюита на дереве бампа и `tsc --noEmit` обязаны быть зелены, и это весь объём;
- **живая приёмка человека — НЕ покрывается здесь и покрыта быть не может.** Класс не
  воспроизводится на этом репозитории по построению: он публичный, мелкогранулярный токен читает из
  публичного всё, отказа не бывает НИКОГДА. Стенд `gh` покрывает класс тестами (два стыка — два
  актёра), но это покрытие, а не полевая приёмка. Полевая приёмка — рука john на приватном
  репозитории потребителя после накатки: строка `merge-gate --ref origin/main --pr <N>
  --review-workflow 'Claude PR Review'` обязана напечатать ГАРДЫ (а не падение `was not read through
  gh`) и строку `GitHub refused '<путь>' … guard 2 judges the runs of Actions instead`. Пока она не
  напечатана, класс закрытым не считается — и это, дословно, урок такта из §4 первого письма: зелень
  публичного репозитория-производителя не доказывала ничего.

## Чего в этот ход не делал

- **#340 не трогал** (`feat(notify)`, тред 159, метки нет, круга на голове нет). Прогон связан одним
  тредом, а след гарда 5 обязан лечь в ленту треда-ВЛАДЕЛЬЦА — сюда его класть нельзя. Открытых PR
  кроме него нет: после мержа #341 хвост — ровно один этот;
- **§5 п.1 и п.2 первого письма остаются снятыми** (читать сырой GraphQL / не просить узел вовсе):
  гипотеза §3 была моя и опровергнута мутацией, отдельным предметом я их не ставлю. §5 п.4
  подтверждён исполненным ещё тредом 160 и правкой не тронут.

**Ход — dev-core:** бамп и тег. Когда тег назван, ход возвращается ко мне — я несу john строку
полевой приёмки и накатку.

waiting-on → dev-core.

## msg-014 · from: dev-core · 2026-09-08 · expects: none

## Бамп `0.2.13 → 0.2.14` открыт — PR #343, голова `5693e8c1`

Шаг 1 постановки исполнен. Шаг 2 (тег) требует мержа и в этот ход не делается; шаг 3 (доклад об
имени тега, sha и версии) едет вместе с ним.

**PR:** [#343](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/pull/343),
ветка `release/v0.2.14-172`, голова `5693e8c18f171476b99a06d170c460b67528352c`, база `origin/main`
= `784f4d04`. Открыт `pnpm protocol pr open --ref origin/main` (дверь напечатала
`thread '172-merge-gate-still-falls-on-private', role 'dev-core'`), заголовок —
`chore(release): version пакета 0.2.13 → 0.2.14 — имя тега реза, несущего опознание отказа по пути
(тред 172)`.

**Дифф — одна строка:** `packages/agent-protocol/package.json`, `"version": "0.2.13"` → `"0.2.14"`
(1 file changed, 1 insertion, 1 deletion).

## Почему бамп обязателен — замерено, а не выведено

- `git tag --contains 784f4d04` — **пусто**. Ни один тег не несёт #341;
- новейший тег ядра `agent-protocol-v0.2.13` = `6a1574f8cdd4a699ddcbf5dc62ef928a1eff39ad`, срезан
  с ревизии `a632d95e` (сам бамп `0.2.12 → 0.2.13`, тред 161). Над этой линией в `main` стоят три
  реза: `5b9795ae` (#338, тред 170), `d3a07723` (#339, тред 155), `784f4d04` (#341, тред 172);
- `origin/main:packages/agent-protocol/package.json` → `"version": "0.2.13"` — ровно та, на которой
  тег УЖЕ срезан;
- имя тега `scripts/split-package.sh` берёт из `version` НА СРЕЗАЕМОЙ РЕВИЗИИ
  (`scripts/split-package.sh:103-110`, `expected="$name-v$version"`) и отказывает при расхождении
  дословно: «Либо бампни версию пакета, либо назови тег так, как он есть»; существующий тег не
  переносится. Значит без бампа тега с текущей вершины не бывает — рез упрётся в существующий
  `agent-protocol-v0.2.13`.

## Что попадёт в рез `agent-protocol-v0.2.14`

Три реза над линией `v0.2.13`; десять исполняемых файлов пакета
(`git diff --name-only a632d95e..784f4d04 -- packages/agent-protocol/src | grep -v test`):
`cli.ts`, `fs/body-location.ts`, `fs/git-env.ts`, `merge/gh.ts`, `notify/notify.ts`,
`thread/message.ts`, `thread/park-seen.ts`, `thread/thread.ts`, `thread/write.ts`, `usage.ts`.

Одной строкой, как просила постановка: **рез несёт #341 — опознание отказа GitHub по ПУТИ, а не по
зашитому актёру (узел ролапа опознаётся и у персонального токена), и гейт диагностической подсказки
на тот же предикат пути**; попутно — #339 (парк как свойство треда, два снятия) и #338 (дверь места
тела письма у `new-message`/`new-thread`).

**`protocolVersion` эта ступень НЕ двигает** (тред 028): `CURRENT_PROTOCOL_VERSION = 26` и на
`a632d95e` (ревизия тега `v0.2.13`), и на `784f4d04`. Переезд пина `0.2.13 → 0.2.14` схему у
потребителя не меняет — это чистый релизный бамп, и число уедет в докладе об имени тега.

## Проверяемость — что уже замерено на дереве бампа

- **`pnpm typecheck` — 0 ошибок, оба пакета** (`packages/agent-protocol` Done,
  `packages/transport-telegram` Done), прогнан на дереве `5693e8c1`;
- полная сюита — `checks` PR #343, прогон
  [`34236373629`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34236373629),
  событие `pull_request`, голова `5693e8c1`, статус на момент письма — `in_progress`. Это весь
  объём машинных проверок этого PR: своего кода рез не несёт, поведения бамп не меняет, юнитов у
  него нет и быть не должно;
- **проверка «больше ничему совпадать не обязано» — замер, а не допущение.** `grep -n '"version"'
  package.json packages/*/package.json`: корневой манифест поля `version` не держит вовсе,
  `transport-telegram` стои́т на `0.1.0` своей линией и зависит от ядра `workspace:*`.
  `grep -c '0\.2\.13' pnpm-lock.yaml` → **0**: воркспейс-пакет лежит в локе линком
  (`version: link:../agent-protocol`). Остальные четыре вхождения `0.2.13` в дереве
  (`src/merge/gh.ts:123`, `src/merge/gate.process.test.ts:1465`, `README.md:3256`,
  `docs/protocol-reference.md:1916`) — исторические замеры установленной у потребителя версии, на
  которой john снял отказ; их правка была бы порчей замера;
- **машинная даровая проверка годности тега (линией, не сравнением с `origin/main:`) — ПОСЛЕ мержа
  и реза.** Команду и её выход несу отдельным письмом вместе с именем тега, полным sha и версией.

## Этот дифф новой нормы не вводит

Критерий 12 — прямо: ни нового поля, ни ключа конфига, ни новой формы почтового сообщения, ни
нового права, ни нового шага маршрута; ни один существующий запрет не снят и не сужен. Дифф —
одна строка `version`, поведения у неё нет: её единственный читатель — `scripts/split-package.sh`
при выборе имени тега.

## Где я стою и что дальше

**Незакоммиченного нет:** дерево `.worktrees/dev-core` чисто, всё лежит в `5693e8c1` на
`origin/release/v0.2.14-172`.

Метку `review` НЕ вешал — норма требует её ПОСЛЕ зелёного `checks` на той же голове, а прогон
идёт. Паркуюсь на `run:343`: когда `checks` позеленеет на `5693e8c1`, смотритель назовёт мою роль
(PR метки ещё не несёт), я вешаю метку на ту же голову и передаю ход на круг. Красный `checks` —
чиню и докладываю сюда же.

Ход не передаю: он стои́т на мне, заморожен прогоном.

## msg-015 · from: github · 2026-09-08 · expects: none

✅ **checks по PR #343: `success`.**

chore(release): version пакета 0.2.13 → 0.2.14 — имя тега реза, несущего опознание отказа по пути (тред 172) · голова `5693e8c18f171476b99a06d170c460b67528352c` · попытка 1 · прогон [`34236373629`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34236373629)

- `checks` — **success**

👉 Круга ревью на этой голове ещё нет — метка `review` не повешена. По норме 03.08 (тред 049, [#183](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/pull/183)) она вешается ПОСЛЕ зелёного `checks` на той же голове, то есть сейчас. Ход у автора ровно на это одно действие.

## msg-016 · from: dev-core · 2026-09-08 · expects: ack

## Метка `review` повешена на текущей голове #343 — круг летит

**Парк `run:343` снят этим письмом.** Он стоял на вопрос «`checks` по #343 на `5693e8c1`»
(msg-014, с `14:10:08Z`); прогон `34236373629` завершился `success` `14:16:16Z`, действие, ради
которого я парковался, исполнено — метка повешена, — и ход я передаю. Заново на летящий круг не
паркуюсь: парк на нём глотает письмо ревьюера.

Ход пришёл письмом смотрителя (msg-015): зелёный `checks` на `5693e8c1`, метки нет. По маршруту
своего PR метка после зелёного — мой шаг, и он был единственный, который тут оставался. Кода в этот
такт я не трогал ни строкой: дифф #343 как был, так и остался одной строкой `version`.

**Замеры ДО метки (голова `5693e8c18f171476b99a06d170c460b67528352c`, она не двигалась):**

- `checks` — `SUCCESS`, прогон
  [`34236373629`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34236373629),
  событие `pull_request`, `headSha` прогона сверен полным sha — та же голова, начат `14:08:39Z`,
  завершён `14:16:16Z`;
- `git ls-remote origin release/v0.2.14-172` → `5693e8c18f171476b99a06d170c460b67528352c` — ветка на
  той же голове, что и зачтённый прогон;
- `mergeable` — `MERGEABLE`, `mergeStateStatus` был `CLEAN` до метки и стал `UNSTABLE` после (это
  поднявшийся круг, не конфликт).

**Метка повешена, контрольное чтение ПОСЛЕ записи:** `labels: [review]`,
`headRefOid: 5693e8c18f171476b99a06d170c460b67528352c` — голова не двинулась.
**Круг поднялся:** [`34239888958`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34239888958),
создан `14:41:30Z`, `event: pull_request`, голова `5693e8c1` — та же. Он единственный, кто читал эту
голову: прошлые круги треда (`34226615284` на `0ba573f2`, `34234625707` на `96ef59fd`) — по #341,
`34236756914` на `63cc7490` — чужой PR. Вердикта на голову #343 сейчас нет ни одного, и переносить
на неё нечего.

## Дверь на `5693e8c1` после метки, дословно

```
merge-gate: PR #343 at 5693e8c
  STOP guard 1 · approve on the current head: no approve verdict on 5693e8c
  STOP guard 2 · green checks on the same head: not green: review=IN_PROGRESS
       note · base: the base moved AFTER the credited checks started: 445b288 committed
       2026-09-08T14:28:34Z, 'checks' started 2026-09-08T14:08:42Z. … Conservative: a base move
       that cannot change the merge is named too
  you  guard 3 · ascent to a decision of john's: thread '172-merge-gate-still-falls-on-private'
  ok   guard 4 · no self-merge on the documents of power: 1 changed path(s), none of them a document of power
  you  guard 5 · a trace of the merge: name this merge in your next message in the thread
  ok   mergeability · mergeable=MERGEABLE (mergeStateStatus UNSTABLE)
```

Гард 1 STOP — ровно то, ради чего метка. Гард 2 STOP — это `review=IN_PROGRESS`, то есть ИДУЩИЙ
круг, а не красный `checks`: `checks=SUCCESS` дверь зачла, чинить тут нечего. Гард 4 `ok`: один
путь диффа, доком власти не является.

## Нота двери о базе — ПОГАШЕНА замером, а не перебазировкой

База уехала уже после старта зачтённого прогона: #340 сел в `main` `14:28:34Z`
(`gh pr view 340 --json mergedAt` — `2026-09-08T14:28:34Z`, `mergeCommit` `445b288e`), прогон
`checks` #343 начат `14:08:42Z`. Нота честная, но это её консервативная ветвь — «a base move that
cannot change the merge is named too». Что переезд изменить ничего не может, я не вывел, а замерил
тремя независимыми способами:

1. **пересечение путей пусто.** `git diff --name-only 445b288e^ 445b288e` — семь путей #340
   (`docs/protocol-reference.md`, `packages/agent-protocol/README.md`, `src/cli.ts`,
   `src/notify/notify.ts`, три файла `src/orchestrator/thread-number-collision*`).
   `git diff --name-only 784f4d04 5693e8c1` — один путь: `packages/agent-protocol/package.json`.
   Общего пути нет;
2. **дерево слияния НАД НОВОЙ базой отличается от зелёного `main` ровно одной строкой.**
   `git merge-tree --write-tree 445b288e 5693e8c1` → `93055272b6fa8c322139268d7752fa1a34dbc2c5`,
   `git diff --stat 445b288e^{tree} 93055272` → `packages/agent-protocol/package.json | 2 +-`,
   `1 file changed, 1 insertion(+), 1 deletion(-)`; сама строка —
   `-  "version": "0.2.13",` / `+  "version": "0.2.14",`;
3. **новая база зелена своим push-прогоном.** `checks` на `main`, голова `445b288`, событие `push`,
   прогон [`34238498555`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34238498555),
   `completed/success`, создан `14:28:37Z`.

Отсюда вывод (называю выводом, а не замером): результат слияния — зелёное дерево `445b288` плюс одна
строка `version`, у которой в коде читателя нет вовсе — её единственный потребитель
`scripts/split-package.sh` при выборе имени тега. Ни один тест пакета литерал версии манифеста ядра
не читает: вхождения `package.json` в тестах — это фикстуры во временных каталогах
(`split-package.process.test.ts`, `probe.process.test.ts`, `workspace.process.test.ts`,
`restart.process.test.ts`) и проверка наличия скрипта `protocol` (`launcher-form.test.ts`).

**Перебазировку я НЕ делаю и делать не предлагаю:** конфликта нет (`MERGEABLE`), а любой push сейчас
обнулил бы летящий круг — метку пришлось бы снимать и вешать заново, и вердикт круга `34239888958`
оказался бы о дереве, которого больше нет.

## Этот дифф новой нормы не вводит

Критерий 12 — прямо и о ветке целиком: ни нового поля, ни ключа конфига, ни новой формы почтового
сообщения, ни нового права, ни нового шага маршрута; ни один существующий запрет не снят и не сужен.
Дифф — одна строка `version`, поведения у неё нет.

## Чего я не делал

- **круга в foreground не жду и парк не ставлю:** вердикт приедет в тред сам, а парк на летящем
  круге глотает письмо ревьюера;
- **ветку не двигал** — ни коммита, ни push после `5693e8c1`;
- **#340 не трогал** — он уже влит (`14:28:34Z`), и его след гарда 5 лежит в ленте треда 159;
- незакоммиченного нет: дерево `.worktrees/dev-core` чисто.

## Что остаётся после кнопки — и оно моё

Постановка (msg-013) кончается тегом, а не мержем. Шаг 1 (бамп) исполнен и стои́т на круге; после
кнопки остаются мои шаги 2 и 3: нарезать `agent-protocol-v0.2.14` на коммите линии `main`, несущем
и `784f4d04`, и бамп; замерить годность тега ЛИНИЕЙ (дерево реза обязано встречаться у коммита линии
`main`, а не сравнением с `origin/main:`); доложить сюда имя тега, полный sha и версию. Значит после
merge ход возвращается на dev-core, и только после доклада об имени тега — к тебе, за накаткой и
полевой приёмкой john.

И повторяю то, что от кнопки не изменится: **merge закрытием класса не является.** Пока рука john
на приватном репозитории потребителя не напечатала гарды и строку `GitHub refused '<путь>' … guard 2
judges the runs of Actions instead`, класс закрытым не считаем.

**Ход — curator:** вердикт круга `34239888958` приедет в тред сам; по нему кнопка по пяти гардам,
если условия закрыты, либо увод предмета к john, если вердикт молчит о норме.

## msg-017 · from: reviewer-pr · 2026-09-08 · expects: answer

verdict: approve
pr: 343
waiting-on: curator

Дифф — ровно одна строка, `packages/agent-protocol/package.json`: `"version": "0.2.13"` → `"0.2.14"`
(1 file changed, 1 insertion, 1 deletion) — совпадает с описанием буквально.

Полный прогон тестов не повторял — `checks` на голове `5693e8c18f171476b99a06d170c460b67528352c`
зелёный, прогон [`34236373629`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34236373629)
(проверил сам: `gh run view 34236373629` → `conclusion: success`, `event: pull_request`,
`headSha: 5693e8c1…` — совпадает). Прогнал сам, на дереве ровно этой головы (`git worktree add` на
`5693e8c1`): `pnpm typecheck` — 0 ошибок, оба пакета (`packages/agent-protocol` Done,
`packages/transport-telegram` Done); `pnpm exec biome check packages/agent-protocol/package.json` —
чисто. Своего кода рез не несёт, юнитов у бампа нет и быть не должно — весь объём машинных проверок
исчерпан typecheck/biome/checks.

Живой исход `pnpm protocol merge-gate --ref origin/main --pr 343 --review-workflow 'Claude PR Review'`:
```
merge-gate: PR #343 at 5693e8c
  STOP guard 1 · approve on the current head: no approve verdict on 5693e8c
  STOP guard 2 · green checks on the same head: not green: review=IN_PROGRESS
       note · base moved after credited checks started (445b288 vs checks started 14:08:42Z)
  you  guard 3 · thread '172-merge-gate-still-falls-on-private'
  ok   guard 4 · no self-merge on the documents of power: 1 changed path(s), none of them a document of power
  you  guard 5 · a trace of the merge
  ok   mergeability · MERGEABLE (UNSTABLE)
REFUSED: a guard does not hold
```
Гард 2 не зелёный из-за собственного летящего круга ревью (`review=IN_PROGRESS`) — ожидаемо, не
находка; примечание о сдвиге базы (`main` ушёл на `445b288e` двумя мержами позже старта `checks`) —
консервативная пометка двери, не находка. Доки власти этого репозитория диффом не тронуты —
критерий 5 не поднимается, merge не привязан к john.

Зона `dev-core`: `pnpm protocol zones check --ref HEAD --role dev-core --base origin/main` →
«1 path(s) of 'dev-core': none under a forbidden prefix» — критерий 4 чист, конфиг прочитан пакетом,
не глазами (критерий 10 не поднимается).

Критерий 6 (совместимость протокола): заявление «`protocolVersion` эта ступень не двигает» проверил
сам чтением `packages/agent-protocol/src/schema/version.ts` на трёх ревизиях — `a632d95e` (тег
`v0.2.13`), `784f4d04` (текущая база бампа) и `5693e8c1` (голова PR): везде
`CURRENT_PROTOCOL_VERSION = 26`. Заявление верно.

Критерий 3 (скоуп): дифф — ровно шаг 1 постановки dev-core в конце треда 172 («поднять `version`
пакета `0.2.13 → 0.2.14` PR'ом обычным маршрутом»), без молчаливых расширений. `thread:
172-merge-gate-still-falls-on-private` и `role: dev-core` в описании PR присутствуют, тред прочитан
целиком.

Критерий 1: PR тестов не трогает и числа тестов не заявляет — неприменимо.
Критерий 7, 8, 11: не поднимаются — флаков не упомянуто, `agent-comms/**` диффом не тронут, дверных
проверок в диффе нет.
Критерий 12: класс «полевой измеренный дефект, новой нормы не вводит» для этого PR не объявлялся
(он был объявлен и закрыт для #341); дифф чисто версийный, поведения не меняет.

## Находка (минорная, не блокирует approve)

9. **Число вхождений `0.2.13` в дереве, названное в описании PR, занижено на одно.** Описание
   PR (раздел «Проверяемость») перечисляет «остальные четыре вхождения `0.2.13` в дереве»:
   `src/merge/gh.ts:123`, `src/merge/gate.process.test.ts:1465`, `README.md:3256`,
   `docs/protocol-reference.md:1916`. Фактический `grep -rn '0\.2\.13'` по дереву даёт **пять**:
   к перечисленным добавляется `packages/agent-protocol/src/merge/gh.test.ts:32` (эта строка
   появилась в #341, коммит `784f4d04`, то есть уже была в базе бампа на момент открытия PR #343).
   Пропущенное вхождение — того же класса («исторический замер», не подлежит синхронизации), так что
   вывод описания («их правка была бы порчей замера») не меняется, но счёт назван неверно — заявленное
   число не равно факту. Действие: не требуется, дифф не несёт этой правки; для будущих PR той же
   формы (бамп версии) — считать `grep` без ручного перечисления пути, чтобы счёт не расходился.

Остальное по критериям — расхождений не нашёл.

---

Доставлено шагами прогона [`34239888958`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34239888958) по PR #343, голова `5693e8c18f171476b99a06d170c460b67528352c` (вердикт написан агентом ревьюера, доставка — джобой: тред 088).
Ход передан роли `curator` — так объявил сам вердикт.

## msg-018 · from: github · 2026-09-08 · expects: none

PR #343 (chore(release): version пакета 0.2.13 → 0.2.14 — имя тега реза, несущего опознание отказа по пути (тред 172)) **merged** by maysway → `main`.

## msg-019 · from: curator · 2026-09-08 · expects: answer

## Кнопка нажата — #343 в `main`, `1bac6218`. Ход — dev-core: шаги 2 и 3 постановки (тег и доклад)

Вердикт круга `34239888958` приехал `approve` на голову `5693e8c1` (msg-017), и это была последняя
недостающая опора. Кнопка нажата `2026-09-08T14:48:15Z`.

## След гарда 5 — пять гардов поимённо

Дверь моей рукой `14:47Z` (`pnpm protocol merge-gate --ref origin/main --pr 343 --review-workflow
'Claude PR Review'`), дословно:

```
merge-gate: PR #343 at 5693e8c
  ok   guard 1 · approve on the current head: approved on 5693e8c by github-actions — inside the
       round 34239888958 of 'Claude PR Review' on this head (2026-09-08T14:41:30Z…2026-09-08T14:45:57Z)
  ok   guard 2 · green checks on the same head: 2 check(s) green: review=SUCCESS, checks=SUCCESS
       note · base: the base moved AFTER the credited checks started: 445b288 committed
       2026-09-08T14:28:34Z, 'checks' started 2026-09-08T14:08:42Z. … Conservative: a base move
       that cannot change the merge is named too
  you  guard 3 · ascent to a decision of john's: thread '172-merge-gate-still-falls-on-private'
  ok   guard 4 · no self-merge on the documents of power: 1 changed path(s), none of them a document of power
  you  guard 5 · a trace of the merge
  ok   mergeability · mergeable=MERGEABLE (mergeStateStatus CLEAN)
nothing in the facts forbids this merge — guards 3 and 5 are yours to answer
```

1. **Гард 1 — `ok`, и якорь назван прогоном, а не review-объектом.** `approve` на
   `5693e8c18f171476b99a06d170c460b67528352c`, внутри ЗАКРЫТОГО успешного круга `34239888958`
   воркфлоу `Claude PR Review` (`event: pull_request`, окно `14:41:30Z…14:45:57Z`), вердикт лежит в
   окне этого круга. Вердикт — msg-017 этого треда, `from: reviewer-pr`, `verdict: approve`,
   `pr: 343`; сироты нет: круг единственный, читавший эту голову;
2. **Гард 2 — `ok`, полным sha:** `review=SUCCESS`, `checks=SUCCESS` на
   `5693e8c18f171476b99a06d170c460b67528352c`; зачтённый `checks` — прогон `34236373629`
   (`14:08:39Z…14:16:16Z`).

   **Нота двери о переезде базы погашена ЗАМЕРОМ, и замер мой, а не пересказ.** `git fetch origin
   main` → `origin/main` = `445b288eba59cf7e679c4a2597aea91668953c72` (не двигалась с момента
   письма dev-core), `git fetch origin pull/343/head` → `5693e8c18f171476b99a06d170c460b67528352c`.
   `git merge-tree --write-tree 445b288e 5693e8c1` → дерево
   `93055272b6fa8c322139268d7752fa1a34dbc2c5`; `git diff 445b288e^{tree} 93055272` —
   `packages/agent-protocol/package.json | 2 +-`, `1 file changed, 1 insertion(+), 1 deletion(-)`,
   и сама строка: `-  "version": "0.2.13"` / `+  "version": "0.2.14"`. Новая база зелена своим
   push-прогоном `checks` `34238498555` (`445b288e`, `event: push`, `completed/success`,
   `14:28:37Z`) — снято `gh run list --branch main --commit 445b288e…`. То есть результат слияния =
   зелёное дерево `445b288` плюс одна строка `version`, у которой единственный читатель —
   `scripts/split-package.sh` при выборе имени тега. Перебазировки не делал: конфликта нет, а push
   обнулил бы вердикт;
3. **Гард 3 — восхождение к решению john В ЛЕНТЕ, обычной формой. Исключения я НЕ применяю.**
   Основание — первое письмо этого треда (`msg-001`, `from: curator`, доставка чата), несущее замер
   и требования john с пометкой источника: «Замер снят рукой john на ящике сразу после накатки
   `0.2.13`, `2026-09-08 ~12:0xZ`», §5 «Что нужно», пункт 3 дословно: «**приёмка — на приватном
   репозитории.** У производителя такого нет; значит либо просить потребителя прогнать дверь своей
   рукой и доложить, либо мерить на любом другом приватном репозитории. **Зелёное на публичном не
   является приёмкой этого класса**».

   Приёмка, которой john требует, снимается на УСТАНОВЛЕННОЙ у потребителя копии — то есть после
   накатки, то есть после тега. А тега без бампа не бывает, и это замерено dev-core, а не выведено:
   `scripts/split-package.sh:103-110` берёт имя тега из `version` НА СРЕЗАЕМОЙ РЕВИЗИИ
   (`expected="$name-v$version"`) и отказывает при расхождении, существующий тег не переносится;
   `origin/main:packages/agent-protocol/package.json` стоял на `0.2.13`, а тег
   `agent-protocol-v0.2.13` уже срезан с `a632d95e`. Значит #343 — механически вынужденный шаг
   того, что john в этой ленте потребовал, и восходит он к слову john, а не к моей постановке.

   **Класс «полевой измеренный дефект, новой нормы не вводит» здесь не объявляется и не нужен** —
   он был объявлен и закрыт для #341, и ревьюер это отдельно отметил (критерий 12 msg-017). Класс
   Д-1 не применяется тоже: в этом репозитории он не принят;
4. **Гард 4 — `ok`, и список выведен данными, а не памятью.** Дверь напечатала 8 доков власти с
   источником каждого: `agent-protocol.json` (сам конфиг), четыре карточки ролей и `REVIEWER.md`
   (выведены из `instructions`), `PROTOCOL.md` и `.github/workflows` (объявлены `powerDocuments`).
   Дифф — 1 путь, `packages/agent-protocol/package.json`, в список не попадает;
5. **Гард 5 — это письмо.**

**Кнопка жалась с `--match-head-commit`:** `gh pr merge 343 --squash --match-head-commit
5693e8c18f171476b99a06d170c460b67528352c` — дверь и кнопка сошлись на одном дереве по построению, а
не по сверке сорока символов глазом.

## Контрольное чтение ПОСЛЕ записи — ответ инструмента ≠ факт

- `gh pr view 343 --json state,mergedAt,mergeCommit,baseRefName,headRefOid`: `state: MERGED`,
  `mergedAt: 2026-09-08T14:48:15Z`, `mergeCommit.oid: 1bac621894e5dde57556bcc8987de1854cc0d494`,
  **`baseRefName: main`** — цель сверена, `headRefOid` не двигался;
- `git fetch origin main` → `origin/main` = `1bac621894e5dde57556bcc8987de1854cc0d494`, заголовок
  squash — `chore(release): version пакета 0.2.13 → 0.2.14 — имя тега реза, несущего опознание
  отказа по пути (тред 172) (#343)`, Conventional Commits;
- `git show origin/main:packages/agent-protocol/package.json` → `"version": "0.2.14"` — в `main`
  лежит ровно та строка, которую я мерил в дереве слияния;
- `git merge-base --is-ancestor 784f4d04 origin/main` → **истина**: коммит `1bac6218` несёт И `784f4d04`
  (#341, опознание отказа по пути), И бамп — то есть он и есть та ревизия линии `main`, о которой
  говорит шаг 2 постановки;
- `git tag --contains 1bac6218` — **пусто**: тега на этой вершине ещё нет, резать есть что.

## Находка ревьюера — прочитана, действия не требует

Находка 9 (минорная, approve не блокировала): описание #343 назвало «четыре вхождения `0.2.13` в
дереве», фактический `grep -rn` даёт **пять** — пропущено
`packages/agent-protocol/src/merge/gh.test.ts:32` (появилось в `784f4d04`, то есть уже стояло в базе
бампа). Ревьюер сам говорит: пропущенное вхождение того же класса («исторический замер»), вывод
описания не меняется, правки дифф не несёт. **Действия не требует, тело PR я не правлю** — PR
смёржен, а переписывать описание слитого PR ради счёта значит портить запись такта. Рекомендация
ревьюера на будущее принимается и адресуется dev-core как норма ремесла, а не как правка: **в PR
формы «бамп версии» число вхождений называть выходом `grep`, а не ручным перечислением** — счёт,
набранный рукой, расходится с фактом ровно так, как здесь.

## Постановка стоит без правки: ход — dev-core, шаги 2 и 3

Постановка (моё письмо `14:01:59Z`) кончается ТЕГОМ, а не мержем, и шаги 2 и 3 остаются твои:

2. **нарезать тег на коммите линии `main`, несущем и `784f4d04`, и бамп.** Этот коммит теперь
   назван и замерен: `1bac621894e5dde57556bcc8987de1854cc0d494`. Имя тега скрипт возьмёт из
   `version` срезаемой ревизии — на ней стоит `0.2.14`;
3. **доложить в этот тред: имя тега, полный sha коммита тега, версию**, и одной строкой — что
   именно в рез попало (#341: опознание отказа по ПУТИ, а не по зашитому актёру, и гейт
   диагностической подсказки на тот же предикат; попутно #339 и #338).

**Приёмка шага 2 — машинная и даровая, и она ЛИНИЕЙ, а не сравнением с `origin/main:`:** дерево
реза обязано встречаться у коммита линии `main`. Назови команду и её выход. Сравнение с
`origin/main:<путь>` на отставшей ветке врёт — этим не мерить.

**`protocolVersion` эта ступень не двигает** — `CURRENT_PROTOCOL_VERSION = 26` и на `a632d95e`
(ревизия `v0.2.13`), и на голове; ревьюер это проверил чтением `schema/version.ts` на трёх ревизиях
(критерий 6 msg-017). Число всё равно едет в докладе шага 3.

## Что merge НЕ закрыл — повторяю, потому что это урок такта

**Класс закрытым не считается.** Пока рука john на приватном репозитории потребителя после накатки
не напечатала ГАРДЫ (а не падение `PR #… was not read through gh`) и строку `GitHub refused
'<путь>' … guard 2 judges the runs of Actions instead`, приёмка §5 п.3 не снята. Зелень публичного
репозитория-производителя не доказывает здесь ничего — это записано в §4 первого письма john и
остаётся в силе после кнопки ровно так же, как до неё.

Порядок оставшегося: **тег (dev-core) → доклад об имени тега (dev-core) → накатка и полевая приёмка
john (несу я)**. Когда тег назван, ход возвращается ко мне.

## Чего я в этот ход не делал

- **хвоста открытых PR нет:** `gh pr list --state open` перед кнопкой отдавал ровно один PR — #343.
  После кнопки открытых не осталось;
- **парк не ставлю:** ход передаётся на dev-core живым действием (рез тега), ждать событие не за чем;
- **тег своей рукой не режу** — это шаг постановки, адресованный dev-core, и `packages/**` мне
  запрещены; незакоммиченного у меня нет, файлов вне почты я не трогал.

waiting-on → dev-core.

## msg-020 · from: dev-core · 2026-09-08 · expects: answer

## Шаг 2 исполнен: тег `agent-protocol-v0.2.14` срезан. Шаг 3 — это письмо

Постановка (msg-013, повторена в msg-019) кончалась тегом. Он срезан с коммита линии `main`,
названного curator, и отправлен на `origin`.

## Объявление тега — три числа и одно, которого по имени не видно

- **имя тега:** `agent-protocol-v0.2.14`;
- **полный sha коммита тега:** `1fb92e83150704d8e6cf80d078f86ab4a6c3bb1c` (это коммит СРЕЗА —
  корень его дерева есть корень пакета, а не корень репозитория; `1bac6218` линии `main` он своим
  предком не является и быть не может: `git subtree split` переписывает историю поддерева);
- **версия пакета:** `0.2.14` — `git show agent-protocol-v0.2.14:package.json` даёт
  `"name": "agent-protocol"`, `"version": "0.2.14"`;
- **`protocolVersion`, который пишет этот тег: `26`** — печатает сам скрипт из
  `src/schema/version.ts` среза, и он не сдвинулся относительно `v0.2.13` (ревьюер проверил тем же
  чтением на трёх ревизиях, критерий 6 msg-017). Номер тега этого не показывает, поэтому число
  едет в объявлении. Сверка перед переездом пина у потребителя:
  `agent-protocol schema version --package-ref agent-protocol-v0.2.14 --repo <потребитель> --ref main`.

Команда реза, дословно:

```
bash scripts/split-package.sh --tag agent-protocol-v0.2.14 \
  --ref 1bac621894e5dde57556bcc8987de1854cc0d494 --base origin/main
```

Её вывод (существенное):

```
split-package.sh: protocolVersion этот тег пишет: 26 (из 'packages/agent-protocol/src/schema/version.ts')
split-package.sh: режу 'packages/agent-protocol' с 1bac6218… → тег 'agent-protocol-v0.2.14' (пакет agent-protocol@0.2.14)
split-package.sh: срез 1fb92e83150704d8e6cf80d078f86ab4a6c3bb1c, верхний уровень его дерева:
  README.md
  package.json
  spike
  src
  tsconfig.json
  vitest.config.ts
split-package.sh: тег 'agent-protocol-v0.2.14' создан на 1fb92e83150704d8e6cf80d078f86ab4a6c3bb1c (пакет agent-protocol@0.2.14, protocolVersion 26)
```

**Тег отправлен на `origin`** (без флага `--push` скрипт создаёт только локальный, а потребитель
пинится на удалённый), и запись прочитана после неё, а не принята на слово:

```
$ git push origin refs/tags/agent-protocol-v0.2.14
 * [new tag]           agent-protocol-v0.2.14 -> agent-protocol-v0.2.14

$ git ls-remote --tags origin 'refs/tags/agent-protocol-v0.2.14'
1fb92e83150704d8e6cf80d078f86ab4a6c3bb1c	refs/tags/agent-protocol-v0.2.14
```

Обе дверные проверки линии внутри скрипта прошли молча (отказа нет): срез несёт всю линию
`origin/main` И сама ревизия в неё влита — ту вторую проверку `1bac6218` проходит по построению,
это и есть голова линии.

## Приёмка шага 2 — машинная, ЛИНИЕЙ, а не сравнением с `origin/main:<путь>`

Мера: дерево реза обязано встречаться у коммита линии `main`. Команда и её выход:

```
$ tree=$(git rev-parse 'agent-protocol-v0.2.14^{tree}'); echo "$tree"
14711ef086043f013714441c0438dcc72c94dac9

$ git rev-list origin/main -- packages/agent-protocol | while read c; do \
    t=$(git rev-parse -q --verify "$c:packages/agent-protocol"); \
    [ "$t" = "$tree" ] && echo "$c $(git log -1 --format=%s $c)"; done
1bac621894e5dde57556bcc8987de1854cc0d494 chore(release): version пакета 0.2.13 → 0.2.14 — имя тега реза, несущего опознание отказа по пути (тред 172) (#343)
```

Перебор — по ВСЕМ коммитам линии, трогавшим `packages/agent-protocol`, а не по одной вершине:
совпадение найдено ровно одно и на нужном коммите. То есть содержимое тега — то самое, что прошло
дверь merge. `merge-base --is-ancestor` тут не мера и отказал бы всегда: subtree split переписывает
историю, у среза своя линия.

**Побочно тем же перебором подтверждено, что предком коммит тега не является и не обязан:** мерится
дерево, не родство.

## Что в рез попало — измерено, а не пересказано

Дерево `v0.2.13` стои́т на `a632d95e` (найдено тем же перебором). Коммиты, трогающие
`packages/agent-protocol`, между ним и `1bac6218` — `git log --oneline a632d95e..1bac6218 --
packages/agent-protocol`:

- **`784f4d04` — #341, предмет этого треда:** опознание отказа GitHub по ПУТИ узла ролапа
  (`forbiddenChecksRollup`, `src/merge/gh.ts:152`), а не по зашитому актёру, и гейт диагностической
  подсказки на том же предикате. В срезе обе половины на месте: предикат в `src/merge/gh.ts`,
  строка подсказки «`, and guard 2 judges the runs of Actions instead`» в `src/cli.ts`;
- `445b288e` — #340, сторож звонит на два треда под одним номером (тред 159);
- `46288a6b` — #342, ход самоперезапуска отмечается в памяти (тред 173);
- `d3a07723` — #339, парк как свойство треда, два снятия (тред 155);
- `5b9795ae` — #338, дверь места тела письма у `new-message`/`new-thread` (тред 170);
- `1bac6218` — #343, сам бамп `version`.

Содержимое пакета в теге и в `main` тождественно по построению (деревья равны), поэтому построчной
сверки файлов #341 не делал — она мерила бы то же самое второй раз.

## Зелень, на которой стои́т срез

**Push-прогон `checks` на самой голове `1bac6218` — зелёный, и прочитан рукой:** прогон
[`34240630302`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34240630302),
`event: push`, `headSha: 1bac621894e5dde57556bcc8987de1854cc0d494`, `status: completed`,
`conclusion: success` (`14:48:18Z…14:55:17Z`), джоба `checks` — `success`. Читал сам и в своём
такте: зелёный push-прогон на `main` письма смотрителя не рождает, и после передачи хода его не
прочтёт никто. Тег отправлен на `origin` ПОСЛЕ этой зелени, а не до неё.

Дерево `1bac6218` — `93055272b6fa8c322139268d7752fa1a34dbc2c5`, ровно то, что curator намерил
`git merge-tree 445b288e 5693e8c1` до кнопки: это база `445b288e` (зелёный push-прогон
`34238498555`) плюс одна строка `version`, у которой единственный читатель — выбор имени тега в
`scripts/split-package.sh`. Сюита и `tsc --noEmit` зеленели на голове PR `5693e8c1`
(прогон `34236373629`).

## Предупреждение скрипта, которое рука обязана знать

Скрипт печатает перечень тегов пакета, чьё дерево в линии `origin/main` НЕ встречается:

```
split-package.sh: ВНИМАНИЕ: теги пакета 'agent-protocol', чьё дерево в линии 'origin/main' НЕ встречается
(срезаны с невлитого; запинить такой — поехать на коде, не прошедшем merge-гарды): agent-protocol-v0.2.0 agent-protocol-v0.2.5
```

Это те же два известных тега (треды 016 и 095), новых в перечне не появилось; `v0.2.14` в него, как
и положено, не попал. Двигать их нельзя, действия не требуется — строка здесь потому, что её место
именно в объявлении следующего бампа.

## Чего я не делал

- **накатку у потребителя и полевой замер не делал и делать не вправе:** это чужой контур и рука
  john. Класс §5 п.3 первого письма (приёмка на ПРИВАТНОМ репозитории) остаётся НЕ закрытым: пока
  `merge-gate --ref origin/main --pr <N> --review-workflow 'Claude PR Review'` на приватном
  репозитории потребителя не напечатал гарды и строку `GitHub refused '<путь>' … guard 2 judges the
  runs of Actions instead`, зелень этого публичного репозитория не доказывает ничего;
- **тела слитого #343 не правил** — находка 9 ревьюера (счёт вхождений `0.2.13` занижен на одно)
  действия не требует, а правка описания слитого PR испортила бы запись такта. Рекомендацию принял
  как норму ремесла: в PR формы «бамп версии» число вхождений называть выходом `grep`;
- **незакоммиченного у меня нет:** рабочее дерево `.worktrees/dev-core` чисто, тег — единственная
  запись, которую этот ход сделал, и она в `refs/tags`, а не в дереве.

Ход — curator: остаётся накатка и полевая приёмка john, которую несёшь ты.

waiting-on → curator.

## msg-021 · from: curator · 2026-09-08 · expects: ack

**ТРЕБУЕТСЯ РЕШЕНИЕ JOHN И ЕГО РУКА: тег `agent-protocol-v0.2.14` с починкой срезан и отправлен — накати его у потребителя и прогони дверь на приватном PR, приёмку §5 п.3 снять больше некому.** В этом репозитории предмет треда доделан целиком: причина найдена, починка смёржена (#341), бамп смёржен (#343), тег нарезан и проверен моей рукой. Приёмка класса — на ПРИВАТНОМ репозитории, а у производителя такого нет по построению, и это твоё же требование из первого письма (§5 п.3, «Зелёное на публичном не является приёмкой этого класса»).

## Что накатывать — три числа и одно, которого по имени не видно

- **тег:** `agent-protocol-v0.2.14`;
- **коммит тега (полный sha):** `1fb92e83150704d8e6cf80d078f86ab4a6c3bb1c` — это коммит СРЕЗА, корень его дерева есть корень пакета; предком коммита линии `main` он не является и быть не может (`git subtree split` переписывает историю поддерева);
- **версия пакета:** `0.2.14`;
- **`protocolVersion`, который пишет этот тег: `26`** — не сдвинулся относительно `v0.2.13`, то есть переезд пина схемы у потребителя эта ступень не требует. Номер тега этого не показывает, поэтому число едет здесь.

## Приёмка §5 п.3 — что именно должна напечатать дверь, дословно

Строка та же, что стояла в постановке, на приватном репозитории и на PR, у которого чеки уже зелёные:

```
merge-gate --ref origin/main --pr <N> --review-workflow 'Claude PR Review'
```

**Провал (сегодняшнее поведение `0.2.13`) выглядит так, и его ни с чем не спутать — ни одного гарда не напечатано:**

```
agent-protocol: PR #<N> was not read through gh: Command failed: gh pr view …
GraphQL: Resource not accessible by personal access token (…statusCheckRollup.contexts.nodes.0)
```

**Приёмка — печатаются ГАРДЫ и строка про отказ узла.** Хвост у неё ДВА, и они означают разное — прочти именно хвост, а не факт наличия строки:

- `merge-gate: GitHub refused '<путь>' on this token — the pull request was re-read without that field, and guard 2 judges the runs of Actions instead` — **это приёмка**: узел опознан, второй спрос прошёл, гард 2 взял чеки из прогонов Actions;
- `…, and guard 2 has no source for the checks at all` — **это НЕ приёмка, но и НЕ провал класса**: опознание сработало (иначе строки не было бы вовсе), а подстановка не нашла источника — отказали и ролап, И `actions/runs`. Тогда гард 2 обязан сказать `were NOT READ … 'no access', NOT 'not green'` (§5 п.4), и предметом становится доступ к `actions/runs`, а не опознание.

Проверял по коду САМОГО тега, а не по памяти: `git show agent-protocol-v0.2.14:src/cli.ts` — обе ветви хвоста; `git show agent-protocol-v0.2.14:src/merge/gh.ts` — предикат `\bnot accessible by [^(\n]*` и путь `\bnot accessible by [^(\n]*\(([^)]*)\)`, то есть решение принимается по ПУТИ отказа, а актёр (`by integration` / `by personal access token`) в него больше не зашит. `package.json` тега — `"name": "agent-protocol"`, `"version": "0.2.14"`; `src/schema/version.ts` тега — `CURRENT_PROTOCOL_VERSION = 26`.

**Что доложить сюда, чтобы класс закрылся:** выход двери целиком (гарды и хвост строки) — этого достаточно; провал докладывать тем же выходом.

## Что я проверила своей рукой в этот ход — не приняла на слово

- **тег есть на `origin`:** `git ls-remote --tags origin` → `1fb92e83150704d8e6cf80d078f86ab4a6c3bb1c refs/tags/agent-protocol-v0.2.14` (рядом `v0.2.13` = `6a1574f8…`);
- **годность среза — ЛИНИЕЙ, а не сравнением с `origin/main:<путь>`:** дерево тега `14711ef086043f013714441c0438dcc72c94dac9`; перебор ВСЕХ коммитов `origin/main`, трогавших `packages/agent-protocol`, даёт ровно одно совпадение — `1bac621894e5dde57556bcc8987de1854cc0d494` («chore(release): version пакета 0.2.13 → 0.2.14 … (#343)»), то есть содержимое тега — то самое, что прошло дверь merge;
- **обе половины починки лежат В ТЕГЕ**, а не только в `main`: предикат по пути в `src/merge/gh.ts` и обе ветви хвоста подсказки в `src/cli.ts` (строки выше);
- **хвоста работы в репозитории нет:** `gh pr list --state open` → `[]`, рабочее дерево `.worktrees/curator` чисто, незакоммиченного у меня нет.

## Как выглядит предмет целиком — чтобы решение стояло на одной странице

1. **Твоя гипотеза §3 («разбор не получает тела ответа») не подтвердилась**, и опровергнута она мутацией, а не рассуждением: dev-core вернул слово `integration` в предикат живой правкой — дверь дала `code 2` и `PR #… was not read through gh`, твой симптом дословно; снял обратно — `ok guard 1`, `ok guard 2`. Причина была одна: `forbiddenChecksRollup` опознавал отказ ПО АКТЁРУ (`not accessible by integration`), а роли потребителя ходят персональным токеном, которому GitHub отвечает `by personal access token`. Второго спроса просто не происходило, и поведение оставалось допочиночным.
2. **§5 п.1 и п.2 я сняла и отдельным предметом не ставлю** (читать сырой GraphQL / не просить узел вовсе): они чинили бы не то. Скажи, если хочешь их всё равно — тогда это новый предмет и новый тред.
3. **§5 п.4 («нет доступа» ≠ «не зелено») исполнен ещё тредом 160** и этой правкой не тронут; ревьюер проверил чтением.
4. **Урок такта записан в доках** (`docs/protocol-reference.md`, раздел «Отказ называет АКТЁРА…»): почему зелень публичного репозитория-производителя не доказывала ничего. Приёмка класса, который на публичном не воспроизводится, снимается только на приватном — и это ровно то, чего я жду от тебя.

## Чего в этом теге ЕЩЁ нет — чтобы накатка не читалась шире, чем она есть

Тег несёт, кроме починки #341: #340 (сторож звонит на два треда под одним номером, тред 159), #342 (ход самоперезапуска отмечается в памяти, тред 173), #339 (парк как свойство треда, тред 155), #338 (дверь места тела письма, тред 170). Починка письма о самоперезапуске (#336) приехала ещё тегом `0.2.13`.

**Предупреждение скрипта реза, которое рука обязана знать и которое действия не требует:** теги `agent-protocol-v0.2.0` и `agent-protocol-v0.2.5` срезаны с невлитого — их дерево в линии `origin/main` не встречается (треды 016 и 095). Новых в перечне не появилось, `v0.2.14` в него не попал. Пинить эти два нельзя; двигать их — тоже нельзя.

## Ход и парк

Ход остаётся у меня, потому что вопрос несу я: жду твоего замера на приватном. Тред паркуется на тебе (`parked-on john`, `expects ack`) — работы, которую можно делать в этом репозитории, по этому предмету не осталось ни у одной роли, и поднимать сюда сессию не на чем. Как только выход двери ляжет в этот тред, класс либо закрывается, либо становится новым замером — и тогда ход уходит на dev-core.

waiting-on → curator (парк на john).

## msg-022 · from: curator · 2026-09-08 · expects: none

**ПАРК СНЯТ ЯВНО. Слово john дано, тег `agent-protocol-v0.2.14` нарезан и отправлен — предмет движется.**

**Что известно john:** тег несёт починку двери (#341), парк как свойство треда (#339), дверь места тела письма (#338); `protocolVersion` ступень не двигает.

**Что сделано:** потребителю доставлен толчок (`15:14:53Z`, их тред `172`) — пин на `0.2.14` следующим ходом, вперёд остального, с доводом их же выгодой: починка снимает ручное снятие гардов, которое они делают на каждом PR.

**И главное требование, записанное туда же: НАКАТКА НЕ ЕСТЬ ПРИЁМКА.** `0.2.13` привёз починку, которая по коду выглядела верной и не работала — `gh pr view` выходит кодом 1 и печатает пусто, разбор не получает тела. Не поймали потому, что приёмку снимали на ПУБЛИЧНОМ репозитории, где класс не воспроизводится по построению.

**Приёмка — рука john на приватном PR потребителя:** прогнать дверь и посмотреть, печатает ли она гарды. Печатает — работает; падает тем же отказом — предмет возвращается сюда с новым замером.

waiting-on → curator, парк снят.

## msg-023 · from: curator · 2026-09-08 · expects: ack

**ТРЕБУЕТСЯ РУКА JOHN: накатить `agent-protocol-v0.2.14` у потребителя и прогнать `merge-gate` на его ПРИВАТНОМ PR — приёмку §5 п.3 снять больше некому, и работы по этому предмету в этом репозитории не осталось ни у одной роли.**

## Этот подъём был ПУСТЫМ, и виновато моё же прошлое письмо

Замер, а не впечатление:

- `thread show --for curator` печатает `unread for curator: none — the last message in the thread is curator's own letter of 2026-09-08T15:21:19Z`; прогон поднят `15:22:24Z` — через 65 секунд после моей же записи;
- `notify.state`: строка `asked john 172-merge-gate-still-falls-on-private 2026-09-08T15:01:42Z` есть, строки `parked` по этому треду НЕТ — парк действительно снят;
- причина названа буквально в моём msg-022: «ПАРК СНЯТ ЯВНО». Я сняла парк (`park-lifted: john`), оставив `waiting-on: curator`, — а это ровно голый кандидат: ход у меня, парка нет, значит контур обязан поднимать пару каждый такт, пока я не отдам ход или не запаркуюсь.

**Ошибка не в двери, а в суждении, и она называется одной фразой: слово john ≠ рука john.** msg-022 доносил слово («тег накатывается, потребителю доставлен толчок»), и я прочла его как «предмет движется, парк не нужен». Но тред заморожен не согласием john, а его ЗАМЕРОМ на приватном: пока выход двери не лёг сюда, ходить некому. Правильная форма для того письма была «донести И перенести парк» (`--delivers john` вместе с `--parked-on john`), а не снять его. Дверь этого, к слову, уже не прощает по умолчанию: с 2026-09-08 (тред 155, #339, приехало этим же тегом `0.2.14`) `--delivers` парка НЕ снимает — снимает только явный `--park-lifted`, и я его написала своей рукой.

Цена — один подъём роли и место в очереди. В этом треде ошибка не повторится: настоящее письмо парк восстанавливает.

## Что я проверила в МИРЕ, прежде чем назвать ход пустым

- `gh pr list --state open` → `[]` — хвоста PR у меня нет, домерживать нечего;
- `git ls-remote --tags origin` → `1fb92e83150704d8e6cf80d078f86ab4a6c3bb1c refs/tags/agent-protocol-v0.2.14` (рядом `v0.2.13` = `6a1574f8…`) — тег на месте и на том самом sha, который объявил dev-core (msg-020);
- рабочее дерево `.worktrees/curator` чисто, незакоммиченного у меня нет.

С 15:21Z не изменилось ничего: предмет ждёт ровно одного факта, и этот факт — не в этом репозитории.

## Что нужно от john — компактно; полностью это msg-021

Накатить тег `agent-protocol-v0.2.14` (коммит среза `1fb92e83150704d8e6cf80d078f86ab4a6c3bb1c`, версия пакета `0.2.14`, `protocolVersion` `26` — ступень схемы НЕ двигается, пин потребителя переезжать не обязан) и на ПРИВАТНОМ PR потребителя, у которого чеки уже зелёные, прогнать:

```
merge-gate --ref origin/main --pr <N> --review-workflow 'Claude PR Review'
```

Три исхода, и различает их ХВОСТ строки, а не факт её наличия:

- гарды напечатаны, хвост `…, and guard 2 judges the runs of Actions instead` — **это приёмка, класс закрыт**;
- гарды напечатаны, хвост `…, and guard 2 has no source for the checks at all` — опознание сработало (иначе строки не было бы вовсе), а источника чеков нет: предметом становится доступ к `actions/runs`, а не опознание;
- гардов нет вовсе, `PR #<N> was not read through gh … Resource not accessible by personal access token` — починка снова бьёт мимо, и предмет возвращается сюда новым замером.

Доложить сюда достаточно выходом двери целиком — любым из трёх.

## Парк — звонящей формой, и это выбор, а не привычка

`--parked-on john --expects ack`: ход без человека не двинется, а тихая форма (`--expects none`) не звонит ни разу и ни через сколько — по коду `notify.ts` парк, объявленный информационным письмом, молчит с первого дайджеста. Звонок этот второй за час (первый — `15:01:42Z`, по msg-021), и он осознан: он же перезаводит порог напоминаний, чтобы предмет не замолчал совсем, если замер отложится.

Впервые в этом треде у парка названо ОСНОВАНИЕ машинно: `park-ground: no-delivers-since:172-merge-gate-still-falls-on-private` — форма из треда 155 (#339), приехавшая тем же тегом `0.2.14`. Читается она так: «жду письма с `delivers:` в ЭТОЙ ленте, считая от этого парка». Когда слово john доедет, ящик назовёт основание отпавшим сам, а не будет ждать, пока это вспомнит поднятая роль. Парк оно не снимает — снимает рука, и это в форме так и записано.

## Побочное наблюдение — треда ему не завожу

`usage` `new-message` сам себе противоречит про снятие person-парка: блок `--delivers` говорит «IT NO LONGER LIFTS THE PARK ON THEM (155, 2026-09-08)», а блок `--verdict` двадцатью строками ниже — «it lifts no park on a person ('--delivers' does)». Вторая скобка осталась от допочиночного мира. Цена сегодня НОЛЬ (я читала авторитетный блок и написала `--park-lifted` руками), поэтому по правилу реестра «по боли, а не по идее» треда не завожу — предмет записан в память и заводится при второй встрече или при первой с ненулевой ценой.

## Чего в этот ход не делал

- **не трогал ни строки кода и доков:** работы по предмету в этом репозитории не осталось, а выдумывать её, чтобы подъём выглядел непустым, — то самое «текст против факта», которое я ловлю у других;
- **не открывал тредов и не писал в другие** — прогон связан этим тредом;
- **приёмку класса не считаю закрытой:** ни merge (#341, #343), ни тег, ни зелень публичного репозитория её не закрывают по построению — класс не воспроизводится там, где нет приватного репозитория и персонального токена.

Ход — curator, тред запаркован на john.

waiting-on → curator (парк на john).

## msg-024 · from: curator · 2026-09-09 · expects: none

**ПРИЁМКА ВАШЕЙ ПОЧИНКИ НАСТУПИЛА — на ПРИВАТНОМ репозитории потребителя, рукой john, `2026-09-09 ~11:51Z`. Дверь работает. Парк снят, предмет закрывайте.**

## 1. Замер — дословно

PR #537 потребителя (приватный репозиторий), `merge-gate` на пине `0.2.14`:

```
merge-gate: GitHub refused 'repository.pullRequest.statusCheckRollup.nodes.0.commit.
statusCheckRollup.contexts.nodes.0' on this token — the pull request was re-read
without that field, and guard 2 judges the runs of Actions instead
  ok   guard 1 · approve on the current head: approved on d7a2d89 …
  ok   guard 2 · green checks on the same head: 3 check(s) green (from the runs of
       Actions on this head (`gh api actions/runs?head_sha=`), not 'statusCheckRollup')
  you  guard 3 · ascent to a decision of john's …
  ok   guard 4 · no self-merge on the documents of power: 5 changed path(s), none …
  you  guard 5 · a trace of the merge …
```

**Все пять гардов напечатаны.** До починки дверь выходила кодом 2 и не печатала ни одного — то есть класс закрыт.

## 2. Что это значит для вашего предмета

**Ваша вторая редакция сработала там, где первая не работала.** Напомню разницу: `0.2.13` вёз починку, которая по коду выглядела верной, и **не работала** — `gh pr view` выходит кодом 1 и печатает в stdout пусто. Теперь распознавание и перечитывание сделаны так, что тело доступно, и гард 2 судит прогоны Actions.

**И проверено там, где класс воспроизводится.** У вас репозиторий публичный — мелкогранулярный токен читает оттуда всё, отказа не бывает **по построению**. Зелёное на вашей стороне этого класса не доказывает; требование мерить на приватном стояло в постановке `160` §4 п. 4 и теперь исполнено — чужой рукой, но исполнено.

## 3. Что дверь дала сверх ожидания — засчитывается вам

**Отказ в конце прогона — не дефект:** `mergeability unsettled (UNKNOWN)`, спрошено трижды, дверь говорит «GitHub не досчитал, спроси ещё раз». Не выдумывает ответ там, где его нет.

**Нота о базе:** зачтённые проверки стартовали `08.09 16:24`, база уехала `09.09 11:32` — дверь называет, что зелёное относится к дереву, которое больше не результат этого слияния, **даже когда сдвиг заведомо безвреден**. Консервативно и вслух.

## 4. Как ваш тег доехал — без единой руки john

`11:11` ваш сторож тегов кладёт находку → `11:20` их роль открывает бамп → `11:21` CI → `11:30` вердикт → `11:35` merge → **`11:37` их демон подтянулся и перезапустился сам**, поставив зависимости. **26 минут от находки до работающего кода**, ни одного действия человека.

Это же и приёмка вашей самопочинки в чужом контуре — вчера дрейф в 22 часа требовал руки, сегодня не потребовал.

waiting-on → curator, парк снят.

## msg-025 · from: curator · 2026-09-09 · expects: none

**ПРИЁМКА §5 п.3 СНЯТА, ТРЕД ЗАКРЫТ.** Флип `2026-09-09T11:55:58Z`, контрольное чтение `11:56:03Z` печатает `status: closed` — закрытие названо замером, а не обещанием.

## 1. Что закрыло класс — замер на приватном, а не merge, не тег и не зелень

Класс закрыла ровно та строка, которую постановка требовала дословно: дверь на ПРИВАТНОМ PR #537 потребителя, пин `0.2.14`, напечатала **все пять гардов** и хвост `…, and guard 2 judges the runs of Actions instead` — то есть узел опознан, второй спрос прошёл, гард 2 взял чеки из прогонов Actions. Это первый из трёх объявленных заранее исходов, и он приёмочный; до починки дверь выходила кодом 2, не печатая ни одного гарда.

**Цепочку «код тега → накатанный пин → выход двери» я замкнула своей рукой, а не приняла на слово** — читала САМ ТЕГ, а не `main`:

- `git show agent-protocol-v0.2.14:src/cli.ts` → обе ветви хвоста подсказки на местах (`, and guard 2 judges the runs of Actions instead` / `, and guard 2 has no source for the checks at all`); принятый john хвост — первая из них;
- `git show agent-protocol-v0.2.14:src/merge/gh.ts` → `:134` предикат `\bnot accessible by [^(\n]*`, `:137` извлечение ПУТИ отказа, `:447` ветка подсказки для персонального токена — актёр в решение не зашит;
- `git show agent-protocol-v0.2.14:package.json` → `"version": "0.2.14"`;
- `git ls-remote --tags origin` → `1fb92e83150704d8e6cf80d078f86ab4a6c3bb1c refs/tags/agent-protocol-v0.2.14` (рядом `v0.2.13` = `6a1574f8…`) — тег стои́т на том же sha, что объявил dev-core в msg-020.

Урок такта записан и остаётся верным ровно в той форме, в какой был объявлен: **зелень публичного репозитория-производителя об этом классе не доказывала ничего** — отказ есть свойство пары «приватность репозитория × вид токена», а не кода, и приёмка снималась там, где класс воспроизводится.

## 2. Вторая проверка приёмки — открытые постановки треда поимённо

Тред перечитан целиком (24 сообщения), каждый состав работ получает дом, снятие или смерть вслух:

1. **§5 п.1 и п.2 (читать сырой GraphQL / не просить узел вовсе) — СНЯТЫ** named-решением curator в msg-004 («снимаю, отдельным предметом не ставлю»); основание — не рассуждение, а опровержение мутацией из msg-003 (слово `integration` возвращено в предикат → `code 2` и симптом john дословно; снято → `ok guard 1`, `ok guard 2`). Воскресить их может только новый первоисточник СВОИМ тредом;
2. **§5 п.3 (приёмка на приватном) — ЗАКРЫТ** замером, дословно приведённым выше;
3. **§5 п.4 («нет доступа» ≠ «не зелено») — исполнен ещё тредом 160**, диффом #341 не тронут, подтверждён чтением dev-core (msg-003) и ревьюером;
4. **постановка msg-013, повторённая в msg-019 (бамп + тег) — ИСПОЛНЕНА** dev-core (msg-020): тег `agent-protocol-v0.2.14`, коммит среза `1fb92e83150704d8e6cf80d078f86ab4a6c3bb1c`, версия пакета `0.2.14`, `protocolVersion` **26** — ступень схемы этот тег не двигает; годность реза замерена ЛИНИЕЙ (дерево тега встречается у коммита `1bac6218…` линии `main`);
5. **находка 9 ревьюера (msg-017: счёт вхождений `0.2.13` в описании #343 занижен на одно) — действия не требует** и закрыта нормой ремесла: в PR формы «бамп версии» число вхождений называть выходом `grep`, а не ручным перечислением. Тела слитого PR не правила намеренно — правка описания слитого PR испортила бы запись такта;
6. **побочное наблюдение msg-023 (`usage` `new-message` сам себе противоречит про снятие person-парка: блок `--delivers` против скобки в блоке `--verdict`) — УМИРАЕТ ВСЛУХ вместе с тредом.** Цена его сегодня ноль (авторитетный блок читается, а `--park-lifted` пишется руками), первоисточника поля у него нет, отдельного треда он не получал и не получает — по правилу реестра «по боли, а не по идее». Предмет держится памятью роли, а не этой лентой; воскресит его вторая встреча или первая с ненулевой ценой — новым тредом, а не хвостом закрытого.

## 3. Первая проверка приёмки — артефакты

- **PR треда:** `gh pr list --state all --search "172-merge-gate-still-falls-on-private in:body"` → #341 и #343, оба `MERGED`. Открытых PR этого треда нет;
- **веток треда на `origin` нет:** `git ls-remote --heads origin` не даёт ни одной со следом `172`;
- **нетерминальных объявленных задач нет:** сухой прогон `thread status --status closed` БЕЗ `--write` прошёл молча и напечатал будущий `_meta.md` — это машинная половина первой проверки, и её молчание здесь факт следа, а не догадка.

## 4. Хвост открытых PR — прочитан; кнопок не трогала НАМЕРЕННО

`gh pr list --state open` → три: **#345** (тред `178-zones-door-silent-pass`), **#346** и **#348** (тред `177-workspace-per-pair`). Ни один не принадлежит этому треду, а след гарда 5 обязан лечь в ленту ЕГО треда — прогон связан тредом 172, писать туда нечем, значит merge чужого PR отсюда рождал бы merge без названных гардов.

**Треды-владельцы ЖИВЫ — замерено, а не предположено:** `git show origin/comms:./177-workspace-per-pair/_meta.md` и `…/178-zones-door-silent-pass/_meta.md` → `status: open` у обоих. То есть «поднимут собственным тактом» здесь факт, а не надежда, и переоткрывать своим правом нечего.

## 5. Чего закрытие НЕ означает

- **постановка кончалась тегом и кончилась им.** Накатка, пин и жизнь `0.2.14` у потребителя — чужой контур и не предмет этого репозитория;
- **звонков в этот тред больше не будет:** он закрыт, а `waiting-on` в письме в закрытый тред не поднимает НИКОГО. Поэтому ход я не передаю никому намеренно — не потому, что забыла, а потому, что адресата у него нет;
- **предупреждение скрипта реза остаётся в силе и действия не требует:** теги `agent-protocol-v0.2.0` и `agent-protocol-v0.2.5` срезаны с невлитого (треды 016 и 095) — их нельзя ни пинить, ни двигать; `v0.2.14` в этот перечень не попал.

## 6. Чего я в этот ход не делала

- **кода и доков не трогала:** работы по предмету в этом репозитории не осталось ни у одной роли, а выдумывать её, чтобы подъём выглядел непустым, — то самое «текст против факта», которое я ловлю у других;
- **тредов не заводила и в другие не писала** — прогон связан этим тредом;
- **незакоммиченного нет:** рабочее дерево `.worktrees/curator` чисто; единственная запись вне почты за этот ход — обновление СВОЕЙ заметки памяти о классе (`.orchestrator/memory/curator/…`, вне репозитория): туда лёг замеренный путь погашения такой приёмки — тег с числами, дословный ожидаемый выход с разбором хвоста на три исхода и ЗВОНЯЩИЙ парк (`--parked-on john --expects ack`); тихий парк не позвал бы ни разу.

waiting-on → —
