---
from: curator
worker: claude-code
session: ef811b3a-3443-4d4f-9012-d625d5bd8813
raised: 2026-09-08T14:58:56Z
date: 2026-09-08T15:01:42Z
expects: ack
waiting-on: curator
parked-on: john
---

**ТРЕБУЕТСЯ РЕШЕНИЕ JOHN И ЕГО РУКА: тег `agent-protocol-v0.2.14` с починкой срезан и отправлен — накати его у потребителя и прогони дверь на приватном PR, приёмку §5 п.3 снять больше некому.** В этом репозитории предмет треда доделан целиком: причина найдена, починка смёржена (#341), бамп смёржен (#343), тег нарезан и проверен моей рукой. Приёмка класса — на ПРИВАТНОМ репозитории, а у производителя такого нет по построению, и это твоё же требование из первого письма (§5 п.3, «Зелёное на публичном не является приёмкой этого класса»).

## Что накатывать — три числа и одно, которого по имени не видно

- **тег:** `agent-protocol-v0.2.14`;
- **коммит тега (полный sha):** `1fb92e83150704d8e6cf80d078f86ab4a6c3bb1c` — это коммит СРЕЗА, корень его дерева есть корень пакета; предком коммита линии `main` он не является и быть не может (`git subtree split` переписывает историю поддерева);
- **версия пакета:** `0.2.14`;
- **`protocolVersion`, который пишет этот тег: `26`** — не сдвинулся относительно `v0.2.13`, то есть переезд пина схемы у потребителя эта ступень не требует. Номер тега этого не показывает, поэтому число едет здесь.

## Приёмка §5 п.3 — что именно должна напечатать дверь, дословно

Строка та же, что стояла в постановке, на приватном репозитории и на PR, у которого чеки уже зелёные:

```
merge-gate --ref origin/main --pr <N> --review-workflow 'Claude PR Review'
```

**Провал (сегодняшнее поведение `0.2.13`) выглядит так, и его ни с чем не спутать — ни одного гарда не напечатано:**

```
agent-protocol: PR #<N> was not read through gh: Command failed: gh pr view …
GraphQL: Resource not accessible by personal access token (…statusCheckRollup.contexts.nodes.0)
```

**Приёмка — печатаются ГАРДЫ и строка про отказ узла.** Хвост у неё ДВА, и они означают разное — прочти именно хвост, а не факт наличия строки:

- `merge-gate: GitHub refused '<путь>' on this token — the pull request was re-read without that field, and guard 2 judges the runs of Actions instead` — **это приёмка**: узел опознан, второй спрос прошёл, гард 2 взял чеки из прогонов Actions;
- `…, and guard 2 has no source for the checks at all` — **это НЕ приёмка, но и НЕ провал класса**: опознание сработало (иначе строки не было бы вовсе), а подстановка не нашла источника — отказали и ролап, И `actions/runs`. Тогда гард 2 обязан сказать `were NOT READ … 'no access', NOT 'not green'` (§5 п.4), и предметом становится доступ к `actions/runs`, а не опознание.

Проверял по коду САМОГО тега, а не по памяти: `git show agent-protocol-v0.2.14:src/cli.ts` — обе ветви хвоста; `git show agent-protocol-v0.2.14:src/merge/gh.ts` — предикат `\bnot accessible by [^(\n]*` и путь `\bnot accessible by [^(\n]*\(([^)]*)\)`, то есть решение принимается по ПУТИ отказа, а актёр (`by integration` / `by personal access token`) в него больше не зашит. `package.json` тега — `"name": "agent-protocol"`, `"version": "0.2.14"`; `src/schema/version.ts` тега — `CURRENT_PROTOCOL_VERSION = 26`.

**Что доложить сюда, чтобы класс закрылся:** выход двери целиком (гарды и хвост строки) — этого достаточно; провал докладывать тем же выходом.

## Что я проверила своей рукой в этот ход — не приняла на слово

- **тег есть на `origin`:** `git ls-remote --tags origin` → `1fb92e83150704d8e6cf80d078f86ab4a6c3bb1c refs/tags/agent-protocol-v0.2.14` (рядом `v0.2.13` = `6a1574f8…`);
- **годность среза — ЛИНИЕЙ, а не сравнением с `origin/main:<путь>`:** дерево тега `14711ef086043f013714441c0438dcc72c94dac9`; перебор ВСЕХ коммитов `origin/main`, трогавших `packages/agent-protocol`, даёт ровно одно совпадение — `1bac621894e5dde57556bcc8987de1854cc0d494` («chore(release): version пакета 0.2.13 → 0.2.14 … (#343)»), то есть содержимое тега — то самое, что прошло дверь merge;
- **обе половины починки лежат В ТЕГЕ**, а не только в `main`: предикат по пути в `src/merge/gh.ts` и обе ветви хвоста подсказки в `src/cli.ts` (строки выше);
- **хвоста работы в репозитории нет:** `gh pr list --state open` → `[]`, рабочее дерево `.worktrees/curator` чисто, незакоммиченного у меня нет.

## Как выглядит предмет целиком — чтобы решение стояло на одной странице

1. **Твоя гипотеза §3 («разбор не получает тела ответа») не подтвердилась**, и опровергнута она мутацией, а не рассуждением: dev-core вернул слово `integration` в предикат живой правкой — дверь дала `code 2` и `PR #… was not read through gh`, твой симптом дословно; снял обратно — `ok guard 1`, `ok guard 2`. Причина была одна: `forbiddenChecksRollup` опознавал отказ ПО АКТЁРУ (`not accessible by integration`), а роли потребителя ходят персональным токеном, которому GitHub отвечает `by personal access token`. Второго спроса просто не происходило, и поведение оставалось допочиночным.
2. **§5 п.1 и п.2 я сняла и отдельным предметом не ставлю** (читать сырой GraphQL / не просить узел вовсе): они чинили бы не то. Скажи, если хочешь их всё равно — тогда это новый предмет и новый тред.
3. **§5 п.4 («нет доступа» ≠ «не зелено») исполнен ещё тредом 160** и этой правкой не тронут; ревьюер проверил чтением.
4. **Урок такта записан в доках** (`docs/protocol-reference.md`, раздел «Отказ называет АКТЁРА…»): почему зелень публичного репозитория-производителя не доказывала ничего. Приёмка класса, который на публичном не воспроизводится, снимается только на приватном — и это ровно то, чего я жду от тебя.

## Чего в этом теге ЕЩЁ нет — чтобы накатка не читалась шире, чем она есть

Тег несёт, кроме починки #341: #340 (сторож звонит на два треда под одним номером, тред 159), #342 (ход самоперезапуска отмечается в памяти, тред 173), #339 (парк как свойство треда, тред 155), #338 (дверь места тела письма, тред 170). Починка письма о самоперезапуске (#336) приехала ещё тегом `0.2.13`.

**Предупреждение скрипта реза, которое рука обязана знать и которое действия не требует:** теги `agent-protocol-v0.2.0` и `agent-protocol-v0.2.5` срезаны с невлитого — их дерево в линии `origin/main` не встречается (треды 016 и 095). Новых в перечне не появилось, `v0.2.14` в него не попал. Пинить эти два нельзя; двигать их — тоже нельзя.

## Ход и парк

Ход остаётся у меня, потому что вопрос несу я: жду твоего замера на приватном. Тред паркуется на тебе (`parked-on john`, `expects ack`) — работы, которую можно делать в этом репозитории, по этому предмету не осталось ни у одной роли, и поднимать сюда сессию не на чем. Как только выход двери ляжет в этот тред, класс либо закрывается, либо становится новым замером — и тогда ход уходит на dev-core.

waiting-on → curator (парк на john).

