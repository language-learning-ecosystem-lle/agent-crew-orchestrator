---
from: curator
worker: claude-code
session: 9573b076-afe5-4612-a087-d47bd0edfcfa
raised: 2026-09-08T13:58:58Z
date: 2026-09-08T14:01:59Z
expects: answer
waiting-on: dev-core
---

## Кнопка нажата: #341 в `main`. След гарда 5 — пять гардов поимённо

**Merge исполнен `2026-09-08T14:00:00Z`**, squash, цель — `main`. Контрольное чтение после записи
(ответ инструмента ≠ факт): `state: MERGED`, `baseRefName: main`, `mergeCommit:
784f4d044a2d97785aab081c243115d6ce31a530`, `headRefOid: 96ef59fd456f4919565677029eaaf4f86b6c4545`
(голова не двигалась между замером двери и кнопкой). Кнопка нажата с
`--match-head-commit 96ef59fd456f4919565677029eaaf4f86b6c4545` — sha взят из вывода самой двери.
Строка `origin/main` после fetch: `784f4d04 fix(merge-gate): отказ называет актёра — узел ролапа
опознаётся и у персонального токена (тред 172) (#341)` поверх `d3a07723`.

**Гард 1 — `ok`.** `approve` на ТЕКУЩЕЙ голове `96ef59fd456f4919565677029eaaf4f86b6c4545`, внутри
ЗАКРЫТОГО успешного круга `Claude PR Review` `34234625707` на этой же голове, окно
`13:52:00Z…13:57:49Z`. Вердикт — письмо `reviewer-pr` в этом треде («verdict: approve, pr: 341,
waiting-on: curator», второй круг по #341, доставлен шагами прогона `34234625707`). Вердикта-сироты
нет: вердикт лежит в окне круга, круг читал именно эту голову. Первый круг `34226615284` дал
`needs-fixes` на `0ba573f2` — та голова уехала, и на неё я ничего не переносил.

**Гард 2 — `ok`.** Зелёные чеки на ТОЙ ЖЕ голове `96ef59fd456f4919565677029eaaf4f86b6c4545`, полный
sha сверен: дверь в `13:59:37Z` — `2 check(s) green: review=SUCCESS, checks=SUCCESS`. `checks`
зеленел прогоном
[`34227995411`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34227995411)
(письмо смотрителя `github` в этом треде, `12:46:35Z…12:54:32Z`), `review` — прогоном
`34234625707`. Красноты `review=IN_PROGRESS`, которая стояла в моём прошлом замере `13:55Z` и у
ревьюера, больше нет: круг закрылся.

**Гард 3 — закрыт классом «полевой измеренный дефект, новой нормы не вводит»** (решение john
2026-08-22, тред `030-notify-parking-accuracy`, доставка чата письмом
`2026-08-22T17-47-05Z-curator.md`, слово дословно «НОРМА — 2»). Класс объявлен в этом треде ДО
merge — моим письмом с замером двери на `0ba573f2` и повторно на `96ef59fd`, а не задним числом.
Три условия, конъюнкция, все три закрыты:

1. **первоисточник замера — ЕСТЬ.** Первое письмо этого треда: замер снят рукой john на ящике
   `2026-09-08 ~12:0xZ` сразу после накатки `0.2.13`, с дословным выходом `merge-gate --ref
   origin/main --pr 521 --review-workflow 'Claude PR Review'` и дословным выходом `gh pr view 521
   --json number,statusCheckRollup` (код 1, `Resource not accessible by personal access token`).
   Лента, дата и номер треда названы; это не «известно, что ломается», а где именно это видно;
2. **исполнитель подтвердил о СВОЁМ диффе — ЕСТЬ.** dev-core сказал фразу буквально и дважды: о
   первой голове (письмо с разбором вердикта `needs-fixes`) и отдельно о ветке целиком, включая
   второй коммит (письмо о повешенной метке на `96ef59fd`): «Этот дифф новой нормы не вводит. Ни
   нового поля, ни ключа конфига, ни новой формы почтового сообщения, ни нового права, ни нового
   шага маршрута; ни один существующий запрет не снят и не сужен»;
3. **ревьюер подтвердил ЧТЕНИЕМ ДИФФА — ЕСТЬ.** Вердикт `approve` на `96ef59fd`, раздел «Критерий
   12 — явный вывод по вопросу curator, на текущей голове»: по пунктам, своим чтением второго
   коммита («гейтит ветку тем же решением `forbiddenChecksRollup`, путь; вне пути ветка теперь
   говорит правду»), с явным сведением к первому коммиту («вся ветка `96ef59fd` в целом новой нормы
   не вводит»). Это не общий `approve`, а вывод по пунктам — ровно та форма, которую я объявил
   развилкой ДО вердикта. Проверка ревьюера — мутацией, а не пересказом: гейт снят — падает ровно
   один новый тест (1 failed | 13 passed), возвращён — 14/14.

Нормой здесь ничего не стало: правка вернула опознание отказа к решению по ПУТИ (инвариант треда
026) вместо зашитого актёра, и сузила одну ветку диагностического текста до условия, при котором её
утверждение истинно. Читателя-парсера у этой строки нет.

**Гард 4 — `ok`.** `6 changed path(s), none of them a document of power`. Дверь вывела список
ДАННЫМИ и напечатала источник каждого пути: `agent-protocol.json` (сам конфиг), `docs/roles/curator.md`,
`docs/roles/dev-core.md`, `docs/roles/pilot-codex.md`, `docs/roles/devops.md`, `REVIEWER.md` —
выведены из `instructions` карточек; `PROTOCOL.md` и `.github/workflows` — объявлены `powerDocuments`.
Восемь путей, ни один из шести путей диффа в них не попадает. `docs/protocol-reference.md` в диффе
есть и доком власти не является по построению — там поведение кода, а не полномочия, и переноса
текста в `PROTOCOL.md` дифф не делает.

**Гард 5 — это письмо.** `mergeability` (не гард, факт GitHub) на момент кнопки — `MERGEABLE
(mergeStateStatus CLEAN)`.

## Merge закрытием класса НЕ является — что осталось до приёмки

Повторяю то, что стоит в треде с самого начала и от merge не изменилось: в `main` лежит код, но
потребитель ставит пакет ПО ТЕГУ, а `origin/main:packages/agent-protocol/package.json` сейчас
по-прежнему `0.2.13` — тем самым, на котором john и снял отказ. **До приёмки лежат три шага, и
merge не делает ни одного:** бамп `version` и тег (постановка кончается тегом), накатка рукой john,
и только потом полевой замер на приватном репозитории потребителя.

## Постановка — dev-core: рез `0.2.13 → 0.2.14`

**Предмет:** нарезать срез, несущий починку #341, назвать sha и версию. Дальше — не твоё:
объявление через границу не носит ни одна роль этого дерева.

1. поднять `version` пакета `0.2.13 → 0.2.14` PR'ом обычным маршрутом (ветка → `pnpm protocol pr
   open` с `thread: 172-merge-gate-still-falls-on-private` и `role: dev-core` → зелёный `checks` →
   метка `review` после зелени на той же голове → вердикт → кнопка curator по гардам). Заголовок —
   `chore(release):`, и он называет, что несёт рез;
2. после merge бампа — нарезать тег на коммите линии `main`, несущем и `784f4d04`, и бамп;
3. доложить в этот тред: **имя тега, полный sha коммента тега, версию** и одной строкой — что
   именно в рез попало (#341: опознание отказа по пути, а не по актёру; гейт подсказки).

**Проверяемость.** Классы проверок, покрывающие рез:

- **машинная, даровая:** годность тега мерится ЛИНИЕЙ — дерево реза обязано встречаться у коммита
  линии `main` (сравнение с `origin/main:` врёт на отставшей ветке). Назови команду и её выход;
- **машинная, юнит/интеграционная:** своего кода рез не несёт, кроме одной строки `version` — полная
  сюита на дереве бампа и `tsc --noEmit` обязаны быть зелены, и это весь объём;
- **живая приёмка человека — НЕ покрывается здесь и покрыта быть не может.** Класс не
  воспроизводится на этом репозитории по построению: он публичный, мелкогранулярный токен читает из
  публичного всё, отказа не бывает НИКОГДА. Стенд `gh` покрывает класс тестами (два стыка — два
  актёра), но это покрытие, а не полевая приёмка. Полевая приёмка — рука john на приватном
  репозитории потребителя после накатки: строка `merge-gate --ref origin/main --pr <N>
  --review-workflow 'Claude PR Review'` обязана напечатать ГАРДЫ (а не падение `was not read through
  gh`) и строку `GitHub refused '<путь>' … guard 2 judges the runs of Actions instead`. Пока она не
  напечатана, класс закрытым не считается — и это, дословно, урок такта из §4 первого письма: зелень
  публичного репозитория-производителя не доказывала ничего.

## Чего в этот ход не делал

- **#340 не трогал** (`feat(notify)`, тред 159, метки нет, круга на голове нет). Прогон связан одним
  тредом, а след гарда 5 обязан лечь в ленту треда-ВЛАДЕЛЬЦА — сюда его класть нельзя. Открытых PR
  кроме него нет: после мержа #341 хвост — ровно один этот;
- **§5 п.1 и п.2 первого письма остаются снятыми** (читать сырой GraphQL / не просить узел вовсе):
  гипотеза §3 была моя и опровергнута мутацией, отдельным предметом я их не ставлю. §5 п.4
  подтверждён исполненным ещё тредом 160 и правкой не тронут.

**Ход — dev-core:** бамп и тег. Когда тег назван, ход возвращается ко мне — я несу john строку
полевой приёмки и накатку.

waiting-on → dev-core.

