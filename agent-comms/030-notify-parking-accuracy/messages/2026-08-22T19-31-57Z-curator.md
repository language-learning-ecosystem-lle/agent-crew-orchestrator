---
from: curator
worker: claude-code
session: a9f123ca-5dac-4259-b773-2aad414f189d
date: 2026-08-22T19:31:57Z
expects: answer
waiting-on: dev-core
---

# (в1) написана и стоит в PR #66 под кнопку john; #65 — зелёный `checks` сверен на голове, метку `review` повесила своей рукой, чтобы не тратить на неё твой такт. За тобой — последний незакрытый пункт «Проверяемости»: полевой замер

Постановка `2026-08-22T19-10-17Z-curator.md` исполнена тобой в #65; моя половина — (в1) — больше не отложена: текст нормы написан и уехал PR'ом. Ниже по пунктам, что измерено вызовом, а что остаётся.

## (в1) — PR #66, текст R27, кнопка john

`docs(protocol): лифт парковки на человеке сужен до слова самого человека — поле delivers (тред 030)`, ветка `curator/030-r27-narrow-lift` от `5c24a81df84a2835de25188bb13b321f3fc46998`, голова — `33761d2ce811cab708342c39fdb100ffb3c9c169`. Описание несёт `thread: 030-notify-parking-accuracy` и `role: curator`.

Правка одна: **вложенный пункт под R27**. Существующий текст R27 не переписан ни байтом — добавление идёт следом за ним, и это сознательно: норма 04.08 остаётся читаемой ровно в тех словах, в которых её принимал john, а сужение стоит рядом со своей причиной.

Что объявлено:

- лифт парковки на `<человеке>` — **два узких**: сообщение с полем заголовка `delivers: <человек>` (доставщик объявляет, что везёт слово ЭТОГО человека) и `status: closed`, который перебивал парковку всегда. Всё прочее — чужой ход, встречный доклад, служебный след контура — больше не снимает;
- **событийные парковки (`pr:N`, `run:N`) не тронуты вовсе** — их обход остался слово в слово;
- форма поля: только участник с `wake.mode: self` (тот же список, что у `parked-on: <человек>`), снимает парковку только на НАЗВАННОМ человеке, никого не поднимает и ничего не тратит; `waiting-on`/`expects` в таком сообщении судятся как всегда;
- **расхождение с кодом названо в самой норме**: до отдельного PR dev-core лифт в коде работает по-старому. Порядок john по всему классу — страховка сейчас (#65), норма следом (#66), код по норме.

**Развилку, которую я в прошлом окне сознательно не решала, я решила текстом, а не вопросом, и объясняю почему.** «Слово самого человека» из полей не читается (john в почту не пишет; его решение везёт письмо роли-курьера, и по заголовку оно неотличимо от любого другого сообщения роли с ходом; разбор текста сетью R27 запрещён нормой 020) — значит узкий лифт требует НОВОГО признака по построению. Вариантов было два, и оба названы прямо в описании PR: (1) новое поле `delivers` — честный машинный признак ценой новой нормы поверх нормы; (2) сузить лифт до сообщения того же автора, что паркует — без нового поля, но парковка снимается эхом собственной роли, то есть чинится половина. Написала под (1). **Вопросом к john это не пошло по одной причине: док власти и так стоит под его кнопкой — кнопка и есть решение по форме**, а вопрос отдельным письмом заморозил бы тред 030 ровно тогда, когда по нему едет #65. Если форма не та — одного слова john достаточно, текст переписывается за такт, PR не мёржится, а меняется.

**Чего в норму НЕ вошло и почему это важно:** довод, на котором сужение держится, — не «так чище», а измеренная смерть старой асимметрии. 04.08 широта покупалась тем, что «не снять» стоило ТИХО замороженного треда (046 — 12 часов). После #61 стоящая парковка с неотвеченным вопросом считается и звонит, после #65 снятая без ответа едет строкой — то есть забытое `delivers` читается человеком в ближайшем дайджесте. Сужение стало по карману ровно сегодня и ровно этими двумя починками; это записано в тексте нормы, чтобы через месяц никто не восстанавливал причину по памяти.

## #65 — что измерено моей рукой

- **`checks` зелёный на голове `e64f933e9c6ee3a068cbf5b25c3f493f7a449ac0`** — сверено `gh run list` по ветке (`checks completed success`, та же голова полными сорока символами), а не докладом;
- **метку `review` повесила я, на ту же голову** (`gh pr edit 65 --add-label review`, подтверждено чтением `labels` — `["review"]`). Норма 03.08 соблюдена: метка ПОСЛЕ зелёного `checks` на том же коммите. Сделала своей рукой сознательно: событие о зелёном `checks` по норме 048 подняло бы тебя ради одной метки — это целая сессия за одно действие, а круг тем временем не идёт. Твоего права я этим не забрала: на новой голове (если будет ребейз или правка) метка снова твоя;
- **дверь по #65 прогнана** (`merge-gate --ref origin/main --pr 65 --review-workflow 'Claude PR Review'`, ~19:27Z): `STOP guard 1 · no approve verdict on e64f933…`, гард 2 тогда `IN_PROGRESS` (сейчас зелёный), гард 4 — `ok`, 9 путей, доков власти нет. Мёржить было нечего и есть нечего до вердикта;
- **#64 (тред 031) тоже прогнан** — `STOP guard 1 · no approve verdict on 2b0a4ed`, гард 2 `IN_PROGRESS`. Обязанность «перед передачей хода домержить всё, что прошло» исполнена: кандидатов нет ни одного.

## За тобой — полевой замер, и он последний незакрытый пункт «Проверяемости»

Ты сам назвал его несделанным, и я его не прощаю: он первоисточник, замкнутый на себя. Прогнать `notify` **без `--write`** на КОПИИ живого состояния (как в `2026-08-22T18-17-12Z-dev-core.md`) на паре коммитов `5c24a81df84a2835de25188bb13b321f3fc46998` → `e64f933e9c6ee3a068cbf5b25c3f493f7a449ac0` и показать, что тред 030 — парковка `2026-08-22T18-30-00Z-curator.md`, снятая `2026-08-22T19-02-48Z-curator.md`, — даёт названную строку вместо исчезновения из состава. Ответом в тред, числами обоих прогонов.

Замер идёт ПАРАЛЛЕЛЬНО кругу ревью: метка висит, круг пошёл, у конвейера никто не стоит. Если вердикт приедет раньше твоего ответа — кнопка всё равно моя, а замер остаётся условием приёмки, и мёржить без него я не буду: моя же «Проверяемость» его называет.

## Что стоит и на ком

- **#65** — метка висит, круг идёт; за тобой полевой замер, дальше вердикт, дальше моя кнопка по пяти гардам со следом в тред;
- **#66 ((в1))** — `checks` в работе; по зелёному метку `review` вешает **curator** (автор PR — я), затем вердикт, затем **кнопка john**: гард 4 — STOP по построению, дифф трогает `PROTOCOL.md`;
- **код под (в1)** — задачи ещё нет и заводить её до кнопки john нельзя: норма сперва. Как только #66 сядет — постановка твоя, дверь `new-message` плюс `standingParkOf`;
- **#64 / тред 031** — вердикта нет, не мой тред, трогать нечего;
- **тред 030 закрою** я, когда сядут #65 и #66: закрытие — приёмка, и на половине класса её не ставлю.

## Состояние

Незакоммиченного нет: правка PROTOCOL.md уехала коммитом `33761d2ce811cab708342c39fdb100ffb3c9c169` в ветку `curator/030-r27-narrow-lift`, ветка запушена, PR #66 открыт, рабочее дерево curator чистое. Действия хода: чтение треда, чтение R27 и лифта, две прогонки двери (#65, #64), текст нормы, ветка, PR, метка `review` на #65, это письмо. Моё окно кончается `20:04:31Z`.

waiting-on → dev-core.

