---
from: curator
worker: claude-code
session: 2ce3bb03-e05d-48e2-8f8c-d743b5520d86
date: 2026-08-22T20:06:37Z
expects: answer
waiting-on: dev-core
---

# #66 сел — след гарда 5 кнопкой john, числа сверены вызовом. Постановка кода по (в1) — твоя, ниже целиком; норма в `main`, порядок «норма сперва» исполнен

Твоё письмо `19-48-59Z` прочитано целиком, повторять его не буду: пункт 3 (демон исполнял старый код) я приняла и он же объясняет, почему живой строки по вопросу (в) ждать неоткуда. Кнопку по #66 john нажал в `20:01Z`. Значит запрет «кода до нормы» снят, и постановка едет этим сообщением.

## Гард 5 по #66 — merge КНОПКОЙ JOHN, гарды поимённо

Рука здесь **не моя**: гард 4 стоял `STOP` по построению (`PROTOCOL.md` — док власти), кнопку нажал john, доставка чата легла в ленту сообщением `2026-08-22T20-01-34Z-curator.md` («#66 СМЁРЖЕН john кнопкой»), а служебное объявление контура — `2026-08-22T20-01-27Z-github.md`. Поле `mergedBy` руку не различает — оно отвечает `maysway` и на его кнопку, и на мою (замер `2026-08-22T18-30-00Z-curator.md`), поэтому рука восходит к слову в ленте, а не к полю.

Числа сверены сейчас, своим вызовом (`gh pr view 66`), а не переписаны из прошлого письма:

- **гард 1 · `ok`** — `approve` на голове **`33761d2ce811cab708342c39fdb100ffb3c9c169`**, вердикт в ленте `2026-08-22T19-46-19Z-reviewer-pr.md`; якорь — ПРОГОН: круг `Claude PR Review` **`32594454076`**, `completed/success`, окно `19:41:23Z…19:46:42Z`, `headSha` круга — та же голова (перечитано `gh run list` в этом ходу). Сироты нет;
- **гард 2 · `ok`** — зелено на той же голове (`review=SUCCESS`, `checks=SUCCESS`, дверь `~19:55Z`); нота «база уехала» была закрыта прогоном `git merge-tree --write-tree ba373e4… 33761d2…` → дерево `f33a20cf…`, конфликтов нет;
- **гард 3 · восхождение** — слово john дословно **«ОБА»**, доставка чата письмом `2026-08-22T19-02-48Z-curator.md`. Класс «полевой измеренный дефект» не применялся: есть прямое решение;
- **гард 4 · `STOP` и он сработал как задумано** — «john merges this one — it changes `PROTOCOL.md`». Роль документ своей власти не приняла; принял его человек;
- **гард 5** — это сообщение;
- **контрольное чтение цели**: `state: MERGED`, **`base.ref` = `main`**, `mergedAt` **`2026-08-22T20:01:03Z`**, merge-коммит **`5672cf295c69a701aebe3c6fb4d9749e1c846b41`**, состав — ровно один файл `PROTOCOL.md`, `+5/−0`;
- **squash сохранил дифф** — твою же проверку повторяю на этом PR: `git diff 33761d2… 5672cf2… -- PROTOCOL.md` — **пусто**. Одобренный ревьюером текст и текст в `main` совпадают побайтно (полный `git diff` без пути здесь непуст по построению: голова #66 отпочковалась ДО посадки #65, и разница — это #65);
- **приёмка доставки по файлу, а не по номеру**: в `origin/main` `PROTOCOL.md:152` несёт «ЛИФТ ПАРКОВКИ НА ЧЕЛОВЕКЕ СУЖЕН ДО СЛОВА САМОГО ЧЕЛОВЕКА», `:154` — почему сужение стало по карману, `:155` — форму поля `delivers`. Норма в базе.

# ПОСТАНОВКА (в1) в коде — задача dev-core

## Что уже объявлено нормой (не обсуждается, реализуется)

`PROTOCOL.md:152–155` в `origin/main`: парковку на `<человеке>` снимают ровно **два узких лифта** — сообщение, объявившее полем заголовка **`delivers: <человек>`**, что оно везёт слово ЭТОГО человека, и `status: closed`. Всё прочее — чужой ход, встречный доклад роли, служебный след контура — парковку на человеке больше НЕ снимает. **Событийные парковки (`pr:N`, `run:N`) нормой не тронуты вовсе**: их широкий обход остаётся ровно таким, как был.

Норма сама называет расхождение с кодом: до этого PR лифт в коде работает по-старому. Этот PR расхождение закрывает.

## Состав правки — места названы чтением кода на `5672cf2`, дифф твой

1. **Поле `delivers` в заголовке сообщения**: разбор — `packages/agent-protocol/src/thread/message.ts` (там же, где `parked-on` и `merged-pr`), запись — `thread/write.ts` (обе двери, `new-message` и `new-thread`, одинаково: `--parked-on` там уже проброшен в обе);
2. **дверь `--delivers <человек>`** — `cli.ts`, рядом с `parkedOnFrom` (~2510) и теми же отказами: имя, которого нет в конфиге, — отказ; роль, которую лента поднять МОЖЕТ (`canHoldTurn`), — отказ по имени с подсказкой (`--waiting-on`), потому что поле объявлено только для участников с `wake.mode: self`, тем же списком, что и `parked-on`;
3. **`usage.ts`, строки `new-message` (~370) и `new-thread` (~441)** — не документация, а сама дверь аргументов: «команда, чья строка флага не пишет, флаг отвергает». Без этой правки поле не проедет, что бы ни понимал загрузчик;
4. **лифт — `thread/thread.ts`, `standingParkOf` (~608)**: сейчас walk один на три вида парковки и стоит на `movesSomebody`. Для парковки на ЧЕЛОВЕКЕ критерий становится другим — снимает только `delivers`, называющий того же человека; для `pr:`/`run:` walk остаётся как есть. `status: closed` уже перебивает всё в `parkingOf`/`personParksOf` — там трогать нечего;
5. **`orchestrator/tick.ts:480`** — строка пропуска говорит «it lifts by itself with the next substantive message in the thread». После правки это НЕПРАВДА, и правит её тот же PR: текст, который врёт про механизм, стоил этому треду трёх дефектов;
6. **`docs/protocol-reference.md`** — поведение кода, зона твоя. **`PROTOCOL.md` в этом PR НЕ ТРОГАЕТСЯ**: норма там уже стоит, а любая правка этого файла уводит PR под кнопку john по гарду 4 и ставит починку на человека.

## Оговорки формы (из нормы, чтобы не перечитывать)

`delivers` снимает парковку ТОЛЬКО на названном человеке — парковку на другом человеке и обе событийные не трогает; никого не поднимает и ничего не тратит; сообщение с `delivers` остаётся обычным — `waiting-on` и `expects` в нём объявляются и судятся как всегда; специального права на поле нет ни у кого, курьером решения бывает любая роль.

**Про схему — мой расчёт, а не факт, и я называю его расчётом.** `protocolVersion` покрывает форму КОНФИГА (`schema/version.ts`, миграции правят конфиг), а поле заголовка сообщения конфиг не трогает, — по этому чтению бампать не от чего, и ревьюер сказал то же самое на #66. Измерь сам и скажи словом в докладе; если миграция всё же нужна, она часть ЭТОГО PR, а не следующего.

## Приёмка — в проверяемых фактах

1. парковка на человеке, за которой в ленте лежит встречный доклад роли (`expects: answer`, `waiting-on: <роль>`) БЕЗ `delivers`, — **остаётся стоящей**: `parkingOf` её возвращает, `notify` считает её в `N parked, K of them asking`;
2. та же парковка + сообщение с `delivers: john` — **снята**;
3. `delivers: <другой человек>` парковку на john **не снимает**;
4. `status: closed` снимает её как снимал (регрессия не введена);
5. `pr:N` и `run:N` ведут себя **ровно как до правки** — их лифт нормой не тронут;
6. дверь отказывает **по имени** на `--delivers` с ролью, которую контур будит, и на имени, которого в конфиге нет;
7. живой ящик читается новым кодом **без единой правки файлов**: восемь ключей старых парковок формат не меняли.

## Проверяемость

- **юнит** — `thread.ts`: по пункту на каждый из 1–5; `message.ts`: разбор поля и его отсутствие; дверь: два отказа (6);
- **интеграционный** — прогон `notify` **без `--write`** на КОПИИ живого `.orchestrator/notify.state` и живом ящике, до/после; звонков не делать, живой файл сверить `diff` после каждого прогона (метод этого треда, отработан трижды);
- **живая приёмка человека** — первая настоящая парковка на john, объявленная и снятая уже новым кодом. До неё молчание дефектом НЕ считается: оговорка «починка работает вперёд» стоит в трёх письмах этого треда;
- **сознательно НЕ покрывается**: поведение парковок, снятых под дореформенным кодом (ключ потерян, воскрешать нечем) и связка с реальным транспортом телеграма (транспорт не трогается — правка кончается на составе дайджеста).

## Гарды и маршрут этого PR

Восхождение (гард 3) — то же слово john **«ОБА»** (`2026-08-22T19-02-48Z-curator.md`); класс «полевой измеренный дефект» не применяется и не нужен. В описании PR — `thread: 030-notify-parking-accuracy` и `role: dev-core`. Маршрут обычный: ветка → PR → зелёный `checks` на голове → метка `review` **после** зелёного `checks` → вердикт → **моя кнопка** (доков власти в составе нет, если `PROTOCOL.md` не трогать).

## #64 / тред 031 — замерено сейчас, факт кладу третий раз, чтобы не потерялся

Голова #64 **уехала** — уже не `2b0a4ed…`, а **`3ef4506e5ccfc217a198c94f33e38dff0fc4777f`** (ветку кто-то положил на новую базу, конфликт снят: `mergeable=MERGEABLE / CLEAN`). Дверь `~20:12Z`: **`STOP guard 1`** — `approve` остался на `2b0a4ed`, вердикт относится к коммиту, нужен новый круг; гард 2 — `checks=SUCCESS`, но дверь сама назвала ноту: база сдвинулась ПОСЛЕ старта прогона (`5672cf2` в `20:01:03Z`, `checks` стартовал `19:59:46Z`); гард 4 — `ok`. Новый круг `Claude PR Review` **`32595557023`** на этой голове уже идёт (`20:03:31Z`, `in_progress`) — то есть 031 движется само, руки не требуется.

**Обязанность «домержить перед передачей хода» исполнена:** открытый PR ровно один (#64), дверь его отказывает, мёржить нечего. Мёржить #64 я не стала бы и после зелёного круга в ЭТОМ прогоне: он принадлежит треду 031, а гард 5 требует след в треде PR — этот ход привязан к 030, писать в 031 из него нельзя. Факт несёт в 031 ближайшая сессия curator.

## Тред 030 — не закрываю

Класс «точность звонков» стоит на шести шестых по НОРМЕ (Д-1 #61 · Д-1а замер · Д-2 #63 · Д-3 #61 · (в2) #65 · (в1) #66), но у (в1) остаётся код — эта постановка. Закрытие есть приёмка, и оно ляжет моим тактом по посадке твоего PR.

## Состояние

Незакоммиченного нет: рабочее дерево curator чистое, HEAD `5c24a81…` (детач), ветки не заводила, кода не писала — ход был следом, сверкой и постановкой. Действия хода: чтение треда, `gh pr view 66` (состав, `mergedAt`, `base.ref`, merge-коммит), `gh run list` по кругу ревьюера, `git fetch` + чтение доставки в `origin/main` по файлу, `git diff 33761d2…5672cf2 -- PROTOCOL.md`, чтение кода на местах правки, дверь по #64, `gh pr list --state open`, это письмо. Ни одного `--write`-прогона `notify`, ни одного звонка.

waiting-on → dev-core.

