---
from: dev-core
worker: claude-code
session: e72491a9-7739-4522-9fee-e3ee89c41769
date: 2026-08-22T20:31:38Z
expects: answer
waiting-on: curator
---

# (в1) в коде: #67 открыт, голова `0e87906`, жду `checks`. И ОДИН ЗАМЕРЕННЫЙ ЖИВОЙ ЭФФЕКТ, который надо знать ДО кнопки: правка ретроактивна на чтении — парковка треда 030 под новым кодом СТОИТ снова

Постановка `20-14-13Z` прочитана целиком и исполнена по составу (пункты 1–6), приёмка и «Проверяемость» — ниже числами. Пересказывать твоё письмо не буду; в нём мне ничего не осталось неясным, вопросов посреди задачи не возникло.

## 1. ГЛАВНОЕ — эффект, которого в постановке нет, и он измерен, а не выведен

`notify` **без `--write`**, новый код из чекаута ветки, почта — живой ящик, состояние — **копия** живого `.orchestrator/notify.state` во временном каталоге вне почты и вне рабочего дерева. Живой файл после прогона сверен `md5sum -c` — **OK, не тронут**. Звонков не сделано.

```
9 parked, 8 of them asking, 1 of those new; 0 waits… ; --write would update '<tmp>/notify.state'
❓ 030-notify-parking-accuracy ждёт твоего решения: Требуется решение john: кнопка merge на #66 …
```

Контрольный замер старого кода в этом же ящике — твой и мой прогоны 19:47–19:48Z: **8 parked, 7 asking, про 030 ни слова**. Разница ровно одна и она объяснима поимённо: **парковка msg `19-56-44Z` (твоё «требуется решение john по кнопке #66») под новым кодом СТОИТ**, потому что снявшие её сообщения — служебное `20-01-27Z-github` и твоя доставка `20-01-34Z` — написаны ДО существования поля и `delivers` не несут.

**Отсюда факт, который я называю фактом:** правка **ретроактивна на стороне ЧТЕНИЯ**. Лента append-only и перечитывается на каждом такте, поэтому парковки, снятые СТАРЫМ широким лифтом, после посадки #67 встают снова — все, где позже не легло `delivers` и тред не закрыт. В живом ящике такая ровно одна, и это парковка треда 030.

**Что это значит для контура после мёржа** (вывод, и я называю его выводом): планировщик пропускает запаркованный тред — значит на 030 он никого не поднимет, пока в тред не ляжет сообщение с `delivers: john` либо тред не будет закрыт. Твой такт приёмки по 030 — сам под этой парковкой.

**Дорогая половина при этом закрыта, и это видно в том же прогоне:** парковка не молчит — она считается (`9 parked, 8 asking`) и **звонит** (`1 of those new`), то есть john читает её в ближайшем дайджесте. Ровно та цена, которую норма назвала вместо прежней.

**Выходы, оба дешёвые, выбор твой:** (а) закрыть 030 своим тактом приёмки — `status: closed` перебивает парковку по построению, и вопрос снимается вместе с тредом; (б) если между кнопкой и закрытием нужен ещё ход — послать в 030 сообщение с `--delivers john` (слово john по #66 ты и правда привезла, `20-01-34Z`). Я бы шёл (а): он и так был твоим планом.

## 2. Что сделано в коде — по пунктам постановки

- **1 · поле** — `message.ts`: разбор и рендер `delivers` рядом с `parked-on`. Форма — имя участника; **событийные формы (`pr:5`) отвергаются с причиной**: мёрж ничьего слова не везёт, а парковка, снимаемая машинным событием, — тот самый класс, против которого лифт и сужали. Кривое значение ДРОПАЕТСЯ с предупреждением (старый читатель встречает новое поле, 023);
- **2 · дверь** — `cli.ts`, `deliversFrom` рядом с `parkedOnFrom`, те же два отказа ПО ИМЕНИ: имени нет в конфиге; названа роль, которую контур будит (за такой ролью ход не паркуют вовсе — отказ называет `--waiting-on <роль>`). Права на поле нет ни у кого, как и на парковку;
- **3 · `usage.ts`** — обе строки (`new-message`, `new-thread`); без этого дверь аргументов флаг бы отвергла;
- **4 · лифт** — `thread/thread.ts`, `standingParkOf`: один обход, ДВА критерия. `delivers` копится по именам и читается на парковке-человеке; «двигает кого-то» ПОМНИТСЯ и читается на событийной. Обход больше не останавливается на движущем сообщении — за ним может стоять парковка, которой оно не касается. Событийные (`pr:N`, `run:N`) ведут себя байт в байт как раньше;
- **5 · `tick.ts`** — строка пропуска больше не обещает «lifts with the next substantive message», а называет `delivers: <человек>` и закрытие треда. **Нашёл вторую такую строку и правлю тем же коммитом:** `priority.ts`, строка ОЧЕРЕДИ оператора («not raised until the next substantive message») — она читается из того же состояния в `orchestrator status` и в логе демона. Отступление от буквы состава, доложенное здесь: постановка называла одну строку, лгущих было две;
- **6 · доки** — `docs/protocol-reference.md` (три абзаца: норма, почему признак обязан быть полем, цена и «работает вперёд») и README, раздел «Commands», обе команды. **`PROTOCOL.md` не тронут** — состав #67 доков власти не содержит, гард 4 чист, кнопка твоя.

## 3. Схема — измерено, как ты и просила, и ответ словом

Бампать **не от чего, миграции нет**. `protocolVersion` (`schema/version.ts`) ключуется на форме КОНФИГА — версия читается из `agent-protocol.json`, миграции правят его же; `delivers` — поле заголовка СООБЩЕНИЯ, конфига не касается. Твой расчёт и слово ревьюера по #66 сошлись с чтением кода.

## 4. Приёмка — по твоим семи пунктам

1–3, 5 — юнитами `thread.test.ts`: встречный доклад роли парковку **не снимает** (`parkedOnOf → john`); `delivers: john` — **снимает**; `delivers: <другой>` — **не снимает**; отдельным тестом — доставка, за которой лежит более поздний рабочий трафик (обход помнит, а не спотыкается), и доставка ДО парковки (не снимает: парковка — более позднее заявление); событийные — прежние тесты плюс новый «доставка НЕ снимает `pr:127`»;
4. `status: closed` — прежний тест держится (регрессия не введена);
6. дверь — `new-message.process.test.ts` и `new-thread.process.test.ts`: оба отказа поимённо на ОБЕИХ командах (урок 075 применён в день рождения поля);
7. живой ящик читается новым кодом **без единой правки файлов** — прогон в пункте 1 это и есть.

**Стык** — `daemon.priority.process.test.ts`: парковка через живой демон, два такта пропуска без траты попыток, снятие СЛОВОМ (`delivers: john`) и подъём пары обычной очередью. Юнит этого не проверяет: парковка — факт почты, счётчик попыток — факт журнала.

**Прогон:** локально `vitest run` по пакету — **2327 тестов, 1 файл падал и починен** (см. пункт 5 ниже), biome и `tsc --noEmit` чисто.

## 5. Находка в чужом коде (в2), НЕ чинил — это продуктовое слово, и оно твоё

Сквозной тест (в2) снимал парковку автоматическим `github`-сообщением — ровно тем классом, который (в1) больше не признаёт; я переписал тест на новый лифт (`delivers`), механизм курьера не трогал. **Но из этого следует вопрос к тексту:** после сужения парковка на человеке уходит из состава **только** двумя путями — `delivers` (ответ НАЗВАН) и закрытие треда (строки не даёт по построению). Значит фраза пакета «the park was lifted with no answer named, the question stands» теперь описывает случай, которого больше не бывает: она печатается ровно тогда, когда ответ как раз назван. Строка сама по себе полезна (человек видит, что вопрос ушёл), врёт только формулировка. Менять слово курьера молча я не стал: это твоя граница, и отдельным PR это одна строка текста плюс тесты. Скажи — сделаю.

## 6. Маршрут и состояние

- **PR #67**, ветка `feat/030-delivers-lift`, голова **`0e87906cade5442815950c6ab511edc14196eb77`**, в описании `thread: 030-notify-parking-accuracy` и `role: dev-core`. Два коммита: функционал с тестами и доками, затем починка стыкового теста (в2);
- **жду `checks` по #67 на голове `0e87906`.** Метку `review` вешать нельзя до зелёного на этой же голове — норма 03.08; у конвейера не стою, поллинга нет;
- незакоммиченного нет: рабочее дерево чистое, всё в ветке и в origin. Живой `.orchestrator/notify.state` не тронут (сверено), `--write`-прогонов `notify` не делал, звонков ноль;
- **гарды #67:** доков власти в составе нет (`PROTOCOL.md`, `docs/roles/**`, `REVIEWER.md`, `agent-protocol.json`, `.github/workflows/**` не тронуты) — гард 4 чист, кнопка **твоя** после круга ревью.

Ход тебе: по #67 следующий шаг — метка после зелёного и круг, а до кнопки стоит решить пункт 1 (закрытие 030 против `--delivers john`) и пункт 5.

waiting-on → curator.

