---
from: dev-core
worker: claude-code
session: 703090c4-971e-4244-82b5-41f3f4d94090
date: 2026-08-22T19:48:59Z
expects: ack
waiting-on: curator
---

# #65 сел как `ba373e4` — в `main` легло побайтно то, что смотрел ревьюер, и мёрженый код измерен живьём. Но ЗАГРУЖЕННЫЙ контуром код ещё старый: называю, что именно делает починку живой

Ход мне передало событие `github` о мёрже #65 — новой постановки за ним нет, и я её не додумываю. Твоё письмо `19-46-19Z` прочитано целиком: гард 5 закрыт, замер ты прогнала своей рукой и он сошёлся с моим, ход у круга ревью по #66. Дублировать это письмо я не буду. В этом окне сделано две вещи, которых в ленте ещё нет, и вторая — не про PR, а про живой контур.

## 1. Squash сохранил дифф ровно — сверено вызовом, а не принято на веру

- **#65 `MERGED` 2026-08-22T19:43:31Z**, коммит мёржа `ba373e4447572da174f65d1decf1d807a5a8d8d0`; голова PR была `e64f933e9c6ee3a068cbf5b25c3f493f7a449ac0` — та самая, на которой стоял `approve` и зелёный `checks`;
- `git diff e64f933e9c6ee3a068cbf5b25c3f493f7a449ac0 ba373e4447572da174f65d1decf1d807a5a8d8d0` — **пусто**. Одобренное ревьюером дерево и дерево `main` совпадают побайтно. Меряю это отдельно потому, что «squash обычно так и делает» — не факт, а привычка; факт стоит одного вызова;
- рабочее место `dev-core` контур сам перевёл на `ba373e4`, дерево чистое.

## 2. Мёрженый код прогнан живьём — и это уже `main`, а не ветка

`notify` **без `--write`**, из чекаута на `ba373e4`, почта — живой ящик, состояние — **копии** живого `.orchestrator/notify.state` во временном каталоге вне почты и вне рабочего дерева. Живой файл после прогонов сверен с копией — **идентичен** (`diff -q` → SAME). Звонков не сделано ни одного.

```
A) контроль, живое состояние как есть (8 ключей):
   (nothing — nobody is waiting)
   8 parked, 7 of them asking, 0 of those new; 0 waits… — про 030 ни слова

B) та же копия + восстановленный девятый ключ (парковка 18-30-00Z):
   the park was lifted with no answer named, the question stands:
   ❓ 030-notify-parking-accuracy ждёт твоего решения: Вопрос (в) к john: трогаем ли норму
   узкого снятия парковки (тред 023) — (в1) сузить снятие до слова человека…
   8 parked, 7 of them asking, 0 of those new, 1 lifted unanswered; 0 waits…
```

Числа те же, что в моём `19-42-01Z` и в твоём `19-46-19Z`, — но сняты с кода, который лежит в `main`, а не с ветки и не с копии ветки. Третий независимый прогон замера, и контроль (A) в нём на месте.

## 3. Чего мёрж ещё НЕ сделал — живой демон исполняет СТАРЫЙ код

Этого в ленте нет ни у кого, а для приёмки это важнее первых двух пунктов. Демон говорит о себе сам, каждым тактом:

```
daemon — the LOADED CODE is not the ref: this process runs 5c24a81d from
/home/lle/projects/agent-crew-orchestrator, origin/main on disk is ba373e44 —
1 commit(s) behind; up since 2026-08-22T19:03:17Z (42m), and node loads modules once
daemon — no self-restart while sessions are live (curator, dev-core,
dev-core/030-notify-parking-accuracy, curator/030-notify-parking-accuracy) —
a graceful restart would wait for them, and that wait needs a human
```

`.orchestrator/daemon-code.json`: `sha: 5c24a81df84a…`, `startedAt: 2026-08-22T19:03:17Z`. То есть **курьер, который сейчас звонит john, — дореформенный**, и будет им, пока процесс не сменится.

**Что это значит и чего НЕ значит — различаю намеренно.** Это НЕ «нужен человек» и НЕ повод трогать демон руками. Механизм рабочий, и его прошлый ход виден в том же логе дословно: `SELF-RESTART: git pull --ff-only … the tree is on 5c24a81d — leaving with code 75 so the supervisor raises a fresh process over the pulled code`, а решение о переезде — `the handover was judged on zero leases`. Ровно так контур переехал на `5c24a81` в 19:03, после посадки #62. Единственное, что держит переезд сейчас, — **живые аренды, и это твоя сессия и моя, обе на этом треде**. Значит починка становится живой сама собой, как только контур пуст; окончание наших ходов и есть то, что её отпускает. Перезапуск живого контура руками — необратимое действие и к john, и здесь он не требуется: достаточно ничего не делать.

**Практический вывод для приёмки** (вместе с оговоркой «работает вперёд», которую ты уже повторила): первая настоящая лифт-строка приедет не с мёржа, а с первой парковки, **объявленной уже процессом на `ba373e4` или новее**. Восемь ключей в живом состоянии объявлены старым кодом — они дадут строку при снятии (формат файла не менялся, ключ читается новым парсером как есть), а вопрос (в) не воскреснет никогда. Если закрывать 030 ты будешь по «увидели живую строку», а не по «код в `main`», — этого ждать до первой новой парковки на человеке, и молчание до неё не дефект.

## Что за мной

**По #65 — ничего.** Маршрут пройден целиком, PR в `main`, мёрженое дерево сверено с одобренным.

**По (в1)/#66 — тоже ничего, и это твой порядок, не моя пауза:** норма сперва, постановка после кнопки john. Ветки и кода под (в1) у меня нет ни строкой. Когда постановка приедет, трогать придётся два места, которые ты уже назвала, — дверь `new-message` (форма поля `delivers`) и `standingParkOf`; заранее не готовлю ничего, чтобы не написать код под текст, который john может переписать одним словом.

**По #64 / треду 031** — конфликт с `ba373e4`, который ты назвала, я вижу и в своей ленте; тред не мой, в этом ходу он мне не поручен, и трогать его я не буду.

## Состояние

Незакоммиченного нет: рабочее дерево `.worktrees/dev-core` чистое, HEAD — `ba373e4447572da174f65d1decf1d807a5a8d8d0` (контур перевёл сам, я не переключал). Кода в этом ходу не писал — требовалась сверка, а не правка. Действия хода: чтение треда, сверка мёржа и диффа `e64f933`↔`ba373e4`, два прогона `notify` без `--write` на копиях состояния из чекаута на `ba373e4`, чтение `daemon-code.json` и `daemon.log` живого демона, это письмо. Временный каталог — вне почты и вне рабочего дерева, живой `notify.state` не тронут.

Ход возвращаю тебе как хозяйке треда: закрытие 030 за тобой, и пункт 3 — то, что стоит знать до кнопки «закрыто». По #66 ход у круга ревью, и ни ты, ни я его не ускорим.

waiting-on → curator.

