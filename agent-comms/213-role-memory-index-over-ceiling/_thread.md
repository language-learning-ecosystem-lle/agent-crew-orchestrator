# Указатель памяти curator перерос потолок — 27731 против 24576, и вклеивается в начало КАЖДОЙ сессии

participants: curator · dev-core · john · status: closed

## msg-001 · from: curator · 2026-09-16 · expects: answer

**СЛОВО JOHN (сессия чата, 2026-09-16 ~13:15Z): ЗАВЕСТИ ТРЕД ПОД ПЕРЕРОСТ ПАМЯТИ РОЛИ. Предмет — не «почистить указатель», а ДВА вопроса разом: что с ним делать и почему текста вообще так много.**

## 1. Факт, с которого всё

Ящик просит на КАЖДОМ подъёме curator: указатель роли — **27731 байт против потолка 24576**, и советует удалять заметки, переехавшие в карточку роли. Указатель вклеивается в начальный текст КАЖДОЙ сессии этой роли: двадцать с лишним килобайт уходят раньше, чем роль прочитала первую строку почты. Платится это на каждом подъёме, обеими учётками.

Дом памяти — `agent-comms/memory/<роль>/`; у curator в ACO там 141 запись указателя (замер msg-018 треда `210`).

## 2. Что УЖЕ проверено и не сработало — не повторять

Совет ящика («заметка, переехавшая в карточку роли, должна быть удалена, а не сокращена») проверен на трёх самых очевидных кандидатах чтением файлов целиком (`210`, msg-018 §6) и **не работает**: карточка несёт ЗАГОЛОВОК — норму одной фразой, — а заметка при ней несёт ЗАМЕР: чем норма доказана и где именно ломалась. Удаление оставляет утверждение без основания.

Рабочий приём виден внутри самих заметок — блоки «Из указателя (перенесено …, оглавление шло за потолок)»: прошлые сессии переносили прозу ХУКА из оглавления в тело заметки. Указатель худеет, содержимое не теряется.

## 3. Что заказано — ДВА вопроса, второй важнее первого

**В-1. Что делать с указателем сейчас.** Привести его под потолок так, чтобы ни один замер не пропал. Приём из §2 — известный кандидат, но не предписание: если роль видит лучший, называет его и объясняет, чем он лучше.

**В-2. Почему текста столько — и это главный вопрос john.** Ответ «потому что накопилось» не принимается. Нужен замер: на что распределены эти 27731 байт (сколько записей, какова медиана и хвост, что занимает верхние 10), какая доля указателя — проза хука, а не имя и адрес заметки, и **порождает ли механизм памяти избыток по построению**: пишется ли новая заметка там, где хватило бы строки в существующей, дублируется ли в указателе то, что уже лежит в карточке роли или в PROTOCOL.md, и есть ли записи, чей предмет давно закрыт тредом.

Из В-2 может выйти правка механизма, а не уборка. Если выйдет — это дифф в пакет, и он идёт обычным маршрутом через `dev-core`, отдельной постановкой.

## 4. Границы

Заметки **не удалять** ни одной в этом такте — ни по совету ящика, ни по своему суждению: пока не отвечен В-2, удаление есть потеря замера. Перенос прозы из указателя в тело заметки потерей не является и разрешён.

Потолок `24576` этим тредом не двигается: поднять его — предложение к john с числом, а не действие роли.

Память `dev-core` и `devops` в ACO и память ролей LLE — **вне этого треда**, хотя тот же класс возможен и там. Если замер В-2 покажет, что причина общая, это называется фактом здесь, а не чинится тихо по трём домам.

waiting-on → curator.

## msg-002 · from: curator · 2026-09-16 · expects: ack

**ТРЕБУЕТСЯ РЕШЕНИЕ JOHN: превращать ли замер В-2 в постановку dev-core — и какую из трёх правок механизма брать (или ни одной); и вторым вопросом — двигать ли потолок 24576, потому что после полной уборки его хватает менее чем на двое суток измеренного темпа.**

## В-1 — сделано: 27731 → 21009 при потолке 24576

Приём — тот, что в §2: проза хука уехала из строки указателя в ТЕЛО заметки, на которую строка указывает, блоком «Из указателя (перенесено 2026-09-16, тред 213, оглавление шло за потолок)». Лучшего приёма я не нашла, и замер ниже объясняет почему: сжимать в указателе больше нечего, не удаляя.

- тронуты 58 строк из 140 — те, чья проза ≥ 40 байт; короткие хуки оставлены как есть;
- **все 178 ссылок и все 178 заголовков остались в указателе дословно** (сверено до/после);
- проверка на потерю: ни одна из 140 исходных строк не отсутствует ОДНОВРЕМЕННО и в указателе, и в телах заметок — таких 0;
- не удалено ничего, потолок не двигался.

## В-2 — на что распределены 27731 байт

**Раскладка: заголовки 12389 (44.7 %), имена файлов и разметка 7932 (28.6 %), проза хука и связки 7410 (26.7 %).** То есть **три четверти указателя — имя и адрес, а не проза**, и совет ящика («удали заметку, переехавшую в карточку») целится в меньшую четверть по построению.

Распределение: 140 строк, 178 ссылок, 177 файлов заметок (строка ≠ заметка: 8 «кустовых» строк несут 46 ссылок). Медиана строки 138 байт, среднее 198, максимум 1595. **Верхние 10 строк — 29.1 % файла**, кустовые 8 — 26.3 %.

Темп по истории ветки почты: **создано 213 файлов, удалено 35, живы 177**. Последние дни: 13.09 — 29, 14.09 — 18, 15.09 — 19. Голая строка без прозы стоит ~116 байт → потолок 24576 вмещает ~212 строк. Сегодня ссылок 178, запас после уборки 3567 байт ≈ **30 голых строк ≈ менее двух суток**. И приём не новый: **83 заметки из 177 уже несут блок переноса** — половина корпуса через него прошла, потолок всё равно вернулся. **Уборка есть отсрочка, и она измеренно короткая.**

## В-2 — порождает ли механизм избыток по построению: ДА, три измеренных признака

1. **Единственный жнец, который даёт протокол, не используется ничем.** Заметка вправе объявить повод (`thread:` во фронтматтере под `metadata:`), и закрытие названного треда гасит её на следующем подъёме. Объявили повод **0 из 177**. При этом **138 из 177 называют номер треда в ТЕЛЕ** — там, где он поводом не является и никогда им не станет (`memory-cause.test.ts`). Данные для жнеца лежат в 78 % корпуса, записанные ровно там, где они не работают.
2. **Новый файл пишется там, где хватило бы строки в существующей заметке.** Свидетельство — сами кустовые строки: прошлые сессии опознали 46 ссылок из 178 как спутников 8 предметов и связали их рукой ПОСЛЕ появления файлов. Промпт памяти говорит «одна заметка — один файл, один факт» и пути «допиши строку в существующую» не предлагает: спутник — дешёвый ход на записи, сборка возможна только задним числом и только под давлением потолка.
3. **Указатель дублирует то, что и так в промпте.** 60 заголовков из 140 состоят на ≥80 % из слов, уже присутствующих в `docs/roles/curator.md` + `PROTOCOL.md`, которые грузятся в ту же сессию; 50 заметок из 177 прямо отсылают норму к PROTOCOL.md или карточке — это ЗАМЕРЫ при норме, уже лежащей в контексте, а заголовок пересказывает норму вместо замера.

## Полевая потеря — ровно та, которую запрещает §4, и она УЖЕ случилась

Указатель ссылался на `fallback-step-reddens-a-successful-round.md` — **файла не существует**: прошлая сессия удалила заметку и оставила строку. Проза хука была единственным уцелевшим следом; сегодняшний перенос увёл её в тело `review-round-mechanics.md`, так что след жив. Строка-сирота в указателе осталась висеть (100 байт, указывает в никуда) — своей рукой не трогала: это свидетельство, а не мусор.

## Класс общий — называю фактом, не чиню

Все три признака — свойства ПРОМПТА ЗАПИСИ и того, что потолок мерит ОГЛАВЛЕНИЕ. Ни один не специфичен для curator: память `dev-core`, `devops` и роли LLE подвержены тому же по построению. Их указатели в этом такте не мерились (§4 — вне треда).

## Что предлагаю к решению john (рекомендация, не решение)

Три кандидата на правку механизма; каждый — дифф в пакет и отдельная постановка `dev-core`, и каждый меняет поведение всех ролей, то есть НОРМА, то есть слово john:

- **К-1 (сильнейший).** Вопрос о поводе задаётся при КАЖДОЙ записи, а не опцией: ответ уже лежит в теле у 138 заметок из 177. Даёт жнеца, которого сейчас нет вовсе.
- **К-2.** Дать промпту памяти путь «допиши строку в существующую заметку» наравне с «заведи файл»: сегодня спутник всегда дешевле дописывания.
- **К-3 (дешёвый).** Предупреждение ящика печатает раскладку байт (заголовки / адреса / проза) вместо совета «удали переехавшее в карточку»: совет измеренно целится не в ту четверть.

**Потолок** не двигаю и числа не предлагаю: 24576 — деньги в начальном тексте каждого подъёма, и что именно эти 24 КБ стоят, знает john, а не я. Замер для решения: после полной уборки их хватает на ~30 заметок ≈ менее двух суток.

Ход держу на себе, `--parked-on john`. Запись роли по треду 213 в ветке почты — `journal/curator/213-role-memory-index-over-ceiling.md`. Рабочее дерево чистое, PR не открывался: правка лежит в доме памяти, а не в репозитории.

waiting-on → curator.

## msg-003 · from: curator · 2026-09-16 · expects: answer

**СЛОВО JOHN (сессия чата, 2026-09-16 ~16:44Z): БЕРЁМ К-1 И К-3. К-2 НЕ БЕРЁМ. Парк снимаю этим письмом — работа появилась.**

## 1. Что решено

- **К-1 — берём.** Вопрос о поводе задаётся при КАЖДОЙ записи в память, а не опцией. Основание john принял замером: жнец в механизме уже есть (`thread:` во фронтматтере гасит заметку при закрытии треда), им не воспользовалась **ни одна из 177**, при этом **138 из 177** номер треда называют в ТЕЛЕ — там, где он не работает никогда.
- **К-3 — берём.** Предупреждение о потолке печатает раскладку байт (заголовки / адреса / проза) вместо совета «удали переехавшее в карточку»: совет измеренно целится в меньшую четверть веса.
- **К-2 — не берём сейчас.** Слово john: путь «допиши строку в существующую заметку» не заводится, пока не видно, что К-1 не помог. Отклонён не навсегда — отложен до замера после К-1.

## 2. Вопрос о потолке john СЕГОДНЯ НЕ РЕШАЛ

`MEMORY_INDEX_LIMIT_BYTES = 24_576` **не двигается**. Ни вверх, ни вниз, ни в конфиг. Этой доставкой он не поднят и не подтверждён навсегда — вопрос отложен john и вернётся, когда будет видно действие К-1 на темпе.

## 3. Что дальше делает curator — и это ЕГО ход, не dev-core

К-1 и К-3 — правки пакета (`packages/**`), то есть зона `dev-core`. Постановку пишет роль этого треда: **разбей на две отдельные постановки** (у них разная цена и разная приёмка) и назови по каждой:

- **К-1:** где именно задаётся вопрос о поводе (промпт записи, а не флаг команды), что происходит с ответом «повода нет» — заметка без повода законна или отказ, и **не переписывает ли это задним числом 177 существующих**: старые заметки повода не объявляли, и К-1 не должен превратить их в мусор. Приёмка — полем: первая заметка после merge несёт повод, и закрытие названного треда её гасит на следующем подъёме.
- **К-3:** строка предупреждения — одна, с тремя числами; текст читает человек. Приёмка — первый подъём роли с переросшим оглавлением печатает раскладку, а не совет.

**Границы:** заметок по-прежнему не удалять; потолок не двигать; память `dev-core`, `devops` и ролей LLE этим тредом не мерить — если К-1 сработает, класс лечится у всех сам.

## 4. Хвост, названный в msg-002 и не закрытый ничем

Строка-сирота в указателе — ссылка на `fallback-step-reddens-a-successful-round.md`, файла нет. Своей рукой не трогать: это свидетельство полевой потери, и оно должно дожить до того такта, который будет мерить действие К-1.

waiting-on → curator.

## msg-004 · from: curator · 2026-09-16 · expects: none

**Постановки написаны и отправлены: К-1 — тред `216-memory-cause-asked-at-every-write`, К-3 — тред `217-memory-ceiling-alarm-prints-the-byte-split`, обе `waiting-on → dev-core`. Тред 213 закрываю: предмет исполнен, хвосты переехали в 216.**

## 1. Что отправлено

- **`216` (К-1).** Вопрос о поводе при каждой записи. Постановка отвечает на три вопроса поимённо, как заказано письмом `2026-09-16T16-45-40Z-curator.md`, §3:
  - **где задаётся.** Промпт ЗАПИСИ заметки — вендорский, и его у нас нет: греп по дереву («one file holding one fact», «persistent file-based memory») пуст, а весь наш разговор с вендором о памяти — ключ `autoMemoryDirectory` (`memory.ts:52-61,97-107`). Досягаемый дом — `buildLaunchPrompt` (`launch.ts:1716`), собственные слова пакета в начальном тексте каждого подъёма, там же, где абзац R19. Опция команды записи не встретит никогда: заметку пишет вендор в момент, который выбирает сам;
  - **«повода нет».** Законный ответ, и постановка требует, чтобы он стоял в абзаце наравне с «повод есть»: заметка без повода долгоживущая по построению (`memory-cause.ts:112-127`), а цена ошибочного повода необратима — `rmSync` плюс вычистка строки указателя (`:244-251`);
  - **177 задним числом не переписываются.** Это свойство кода, а не обещание текста: разбор пропускает заметку без повода (`memory-cause.ts:120-121`). Исполнителю велено подтвердить это своими словами о своём диффе, ревьюеру — чтением диффа.
- **`217` (К-3).** Одна строка, три числа, с определением трёх вёдер (адреса и разметка / заголовки / проза), пиннуемым тестом. Совет об удалении уходит и другим советом об удалении не заменяется. Сохранены два свойства сегодняшнего кода: чистое ядро, пиннуемое на тексте, и молчание вместо броска на нечитаемом указателе (`memory.ts:135-137,168-171`).

**Приёмки разведены намеренно.** У `216` путь исполнения НОВЫЙ — жнец собран, но в поле не срабатывал ни разу, — поэтому тред не закрывается до заказанного рукой срабатывания (заметка с поводом → закрытие названного треда → строка гашения `extinctionLine` в журнале ящика). У `217` путь старый, новый в нём только счёт: приёмка — прогон ядра по живому указателю плюс юниты, а полевое срабатывание приезжает само, когда указатель снова перерастёт потолок (заказать его рукой нечем: потолок не двигается).

**Кнопка у обеих — curator**, гард 3 закрыт словом john в ленте (`2026-09-16T16-45-40Z-curator.md`, §1), полевым классом не прикрываюсь ни там, ни там: обе правки меняют поведение всех ролей, то есть норма, и норма уже разрешена john поимённо. Обеим сказано **не трогать `PROTOCOL.md`**: гард 4 судит PR целиком и увёл бы его под кнопку john вместе с пакетным диффом. Нужна ли канону фраза про вопрос о поводе — мой отдельный вопрос, и он поедет отдельным PR под кнопку john, когда будет видно действие К-1.

## 2. Границы перенесены в постановки дословно

Заметок не удалять; потолок `24576` не двигать; К-2 не заводить; память `dev-core`, `devops` и ролей LLE не мерить; строку-сироту (`fallback-step-reddens-a-successful-round.md`, файла нет) рукой не трогать — она свидетельство и должна дожить до такта, который будет мерить действие К-1. Последние два пункта стоят в `216`, §7.

## 3. Почему тред закрываю, а не паркую

Предмет `213` — «что делать с указателем и почему текста столько» — исполнен: В-1 сделано (27731 → 21009 при потолке 24576, ничего не удалено), В-2 отвечено замером, john решил, постановки написаны. Дальше в этом треде работы нет, а формы, которыми его можно было бы держать открытым, все платные: `waiting-on` без парка жжёт подъёмы каждым тактом, `waiting-on: —` выводит тред из очереди до письма извне, парк на john с `--expects ack` звонит человеку тем, что он и так знает.

**Отложенный пункт дом получил, и он переживает это закрытие:** вопрос потолка возвращается к john из приёмочного такта К-1 — это записано в §8 постановки `216`, а тред `216` стоит открытым ровно до того срабатывания. Замер для того разговора уже лежит: запас после уборки 3567 байт ≈ 30 голых строк ≈ менее двух суток измеренного темпа.

Рабочее дерево чистое, PR не открывался, в репозитории этим тактом не изменено ничего: вся работа — две постановки, запись роли (`journal/curator/213-role-memory-index-over-ceiling.md`, дозапись) и это письмо.

waiting-on → curator (закрываю тред следующей командой).
