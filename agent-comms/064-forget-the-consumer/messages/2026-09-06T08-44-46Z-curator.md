---
from: curator
worker: claude-ai
date: 2026-09-06T08:44:46Z
expects: answer
waiting-on: curator
delivers: john
priority: high
merged-pr: 280
---

**Слово john полем `delivers` (чат ~08:44Z): #280 СМЁРЖЕН его кнопкой. Смотритель имени соседнего дома получил триггер — предмет `064` закрыт механизмом, а не рукой.**

Находка едет **письмом**, PR не краснеет — форма, выбранная john 05.09 («заводим: смотритель, набор имён минус свои адреса»). Область — всё дерево коммита (распаковка `git archive`, а не рабочий каталог раннера: рядом с ним лежат зависимости, и «всё дерево» значило бы другое). Свой адрес вычитается машинно, список исключений — путём и содержанием. Повтор глушится **родителем коммита**, а не файлом состояния.

**Отказов у смотрителя три, и каждый даёт письмо «НЕ ПРОЧИТАНО» с причиной** — молчаливого нулевого выхода нет ни на одной ветви. Третий появился по вердикту `needs-fixes`: отказ перечислителя схлопывался в тот же исход, что «совпадений нет», потому что ошибки чтения глушились. Роль **воспроизвела** находку, а не приняла на веру, и доказала починку **мутацией**: с откаченной правкой падают ровно шесть новых проверок.

**Живой шов проверен контролем работающего шаблона:** ноль находок на дереве ветки — и непустой список на том же дереве с подложенной строкой. Нулевой вывод от шаблона, который в ту же минуту дал совпадение, а не от сломанного.

**Что остаётся:** настоящая доставка письма из джобы — приёмка curator после merge, ручным пуском смотрителя.

waiting-on → curator.
