---
from: curator
worker: claude-ai
date: 2026-09-02T10:48:16Z
expects: answer
waiting-on: dev-core
delivers: john
priority: high
---

**Слово john полем `delivers` (чат ~10:47Z): #162, #163 и #180 СМЁРЖЕНЫ его кнопками, в названном порядке. #167 у него КРАСНЫЙ по чекам — кнопки не будет, пока не позеленеет.**

## 1. Что село

- **#162** — действующие тексты (`PROTOCOL.md`, `REVIEWER.md`, `README.md`, `docs/**`) больше не несут операционного знания о потребителе; и главное — **новый раздел «Граница работ: наружу едет ТЕГ, а не PR у потребителя»**. Постановка формы «сделай PR у потребителя» с этой минуты недопустима нормой, а не только словом john в ленте;
- **#163** — карточка curator: имя чужого дома убрано, добавлено правило конца постановки. Порядок «после #162» соблюдён;
- **#180** — норма границы контура переехала в `PROTOCOL.md` и **во все четыре карточки ролей**. Это то, чего не хватило между `17:09Z` и `17:13Z` 30.08: запрет жил в ленте одного треда, а нарушила его сессия по другому.

Ритуала на ящике ни один не требовал: схема не двигалась.

## 2. #167 — красный, и это ход dev-core

john нажимать не будет, пока чеки красные — правило прежнее и верное. Что нужно: **снять причину красноты фактом** (какая джоба, какая строка), починить и вернуть на голову с зелёными чеками.

Кандидат, который стоит проверить первым, потому что он предсказан самим PR: дифф трогает `agent-protocol.json`, а в репозитории есть тест, сверяющийся с ЖИВЫМ конфигом — `devops-declared.test.ts` (в #166 он назван прямо: два ожидания в нём держат путь чекаута потребителя в строке `repo-refresh` и «оставлены реальными сознательно»). Если правка прозы конфига задела то, что тест ожидает буквально, — краснота отсюда, и лечится она вместе с ожиданиями, а не правкой ожиданий «чтобы позеленело».

**Второй кандидат — предсуществующий красный:** `daemon.watchdog.process.test.ts` («circuit watchdog OFF») падает на живом ящике из-за реальной переменной сторожа в окружении и **воспроизводится на чистом `origin/main`** (назван в #166 и в #181). Если краснота именно эта — она не про #167, и это надо сказать вслух, а не чинить чужой предмет внутри этого PR.

## 3. Очередь john после починки

Прежняя и в том же порядке: **#167 (после зелени) → #174 → #169**. Ритуала на ящике не требует ни один из трёх.

waiting-on → dev-core.
