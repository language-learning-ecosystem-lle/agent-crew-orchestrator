---
from: curator
worker: claude-code
session: 624baec4-b716-4092-8cc9-bc03009aafe8
raised: 2026-09-03T10:46:30Z
date: 2026-09-03T10:50:56Z
expects: ack
waiting-on: curator
parked-on: john
---

**Требуется решение john, два предмета: (1) нажать кнопку на PR #233 — обезличивание `claude-review.yml` и `PROTOCOL.md`, доки власти, `checks` зелёный, круга ревью не будет по построению; (2) заводить ли ПОСТОЯННУЮ машинную проверку против возврата класса 064 — это новый шаг маршрута, то есть норма.**

## 1. #232 смержен моей кнопкой — гарды поимённо (гард 5)

`chore(package): урок из чужого контура называется без имени соседа (тред 064) (#232)`, merge-коммит `4930561d9ef7a6ca65eed1453b2b1f52450873dd`, `2026-09-03T10:47:30Z`, squash, `base.ref = main` — прочитано после мержа, не выведено.

- **гард 1** — `approve` на текущей голове `49f7e3260eb076934466fe1a26cf5899476522e9`: вердикт reviewer-pr, доставлен прогоном `33745406729`, и он ЛЕЖИТ В ОКНЕ закрытого успешного круга `Claude PR Review` на этой же голове (`2026-09-03T10:38:35Z…10:42:18Z`) — не вердикт-сирота. Круг повторный: первый (`33744891210`) дал `needs-fixes` по критерию 1, голова не двигалась, метка была снята и повешена заново;
- **гард 2** — зелёные на ТОЙ ЖЕ голове `49f7e326`: `review=SUCCESS`, `checks=SUCCESS` (прогон `checks` — `33744261689`);
- **гард 3** — восхождение: слово john в этом треде (2026-08-30, «пакет протокола не знает своих потребителей», рамка целевого состояния — msg-054 §4), постановка п. 3(A) от `2026-08-30T17-49-47Z-curator.md`. Исключением «полевой измеренный дефект» НЕ пользуюсь: слово john в ленте есть, и оно прямое;
- **гард 4** — `ok`, 4 пути диффа, ни один не док власти; список доков власти дверь вывела данными (8 путей, включая `.github/workflows` и `PROTOCOL.md` из `powerDocuments`), а `.github/scripts/**` в него не входит;
- **гард 5** — это сообщение.

Замер дверью: `pnpm protocol merge-gate --ref origin/main --pr 232 --review-workflow 'Claude PR Review'` → `guard 1 ok`, `guard 2 ok`, `guard 4 ok`, `mergeStateStatus CLEAN`, «nothing in the facts forbids this merge».

Находку ревьюера по критерию 1 (заявленные `12/12` против фактических `17/17` в `comms-derive.test.sh`) считаю закрытой правильно: она про ТЕКСТ, дерево не тронуто, голова не уехала, настоящая строка лежит в ленте письмом dev-core, а не правкой прошлого сообщения. Это ровно тот случай, ради которого критерий 1 стоит, и цена его — один повторный круг.

## 2. #233 — кнопка john, и вот что читать

| | |
|---|---|
| PR | **#233** `docs(power): ссылки на разговоры прежнего дома читаются без имени дома (тред 064)` |
| голова | `8be36b9cb72082499cee16e9cd736e31ac382ecb` |
| `checks` | **success**, прогон `33745289112`, на той же голове |
| круг ревью | **не будет по построению** — дифф правит `.github/workflows/claude-review.yml`, действие самопропускается; метка не вешалась намеренно |
| гард 4 | **STOP** — `.github/workflows/**` и `PROTOCOL.md` суть доки власти |

Гард 1 у такого PR закрывается ЧТЕНИЕМ ЧЕЛОВЕКА, названным в треде поимённо (кто прочёл, на какой голове, чем подтверждено) — норма `PROTOCOL.md`, «Ограничение ревьюера, заданное платформой». Поэтому прошу не «нажми», а «прочти и нажми», и читать тут ровно два места:

1. **`claude-review.yml`, 15 вхождений**: `LLE-тред 023` → `тред 023 прежнего дома`, `LLE-контур` → `прежний дом`. Ссылки на месте — это твоё решение из чата 10:30Z: помеченная ссылка границу укрепляет, лишним было только имя дома. Шапка по-прежнему объясняет, зачем маркер нужен, и добавляет строку «чей это дом, ревьюеру знать незачем»;
2. **`PROTOCOL.md`**: `#453`/`#454` → «случай `16:45Z`»/«случай `17:13Z`». Даты, окна и оба факта сохранены.

**Отступление исполнителя, названное им вслух ДО мержа и я его поддерживаю:** постановка указывала участок `366-383`, а dev-core взял ещё два вхождения того же класса — `тред 065.1, PR #266` и `на PR #15`. Довод измерен, а не эстетичен: `#15` в ЭТОМ репозитории существует и означает другое (`chore(config): docs/install-notes.md в zones.writes роли curator`, 2026-08-18), а `#266` здесь появится через три десятка номеров. Чужой номер в здешнем нормативном тексте читается как здешний — ровно та ловушка, ради которой маркер и заводился. Если считаешь расширение лишним, оно возвращается одним коммитом.

## 3. Предмет к решению: класс 064 течёт обратно, и рукой его не удержать

Факт, а не опасение (замер dev-core, msg-053 §1): приёмка класса была закрыта на `ae4deb13`, а на `1b0b48fb` имя соседнего контура в `packages/**` появилось СНОВА — принёс #220 треда `085`, лёгший после приёмки. То есть один греп рукой закрывает класс ровно до следующего PR любого треда, и это уже случилось, а не может случиться.

Плюс второй факт того же дня: шаблон грепа был верен, а ОБЛАСТЬ применения — нет. Ни один прежний обзор не смотрел `scripts/**`, и `split-package.sh:103,126` несли имя соседа ещё на самой приёмке.

**Постоянная форма — машинная проверка, краснящая PR на маркере.** Своей рукой не завожу и dev-core не ставлю: это новый шаг маршрута, то есть НОРМА по нашему же определению (новое поведение контура за пределами починки), а живёт она в `.github/workflows/**` — двойная кнопка твоя.

Что решаешь ты, а я подготовлю постановку под ответ:

- **заводим ли вообще** — цена: одна джоба на каждом PR и ложные срабатывания на собственном адресе (`language-learning-ecosystem-lle` — это МЫ, любой замер обязан вычитать свой remote, иначе два ложных из трёх; это измерено трижды в этом треде);
- **чем меряем** — набором имён (`LLE`, `dev-speech`, …) или ЯВНЫМ списком-исключением. Набор имён слеп к путям и к новым именам соседей;
- **где живёт** — джоба в `checks` (краснит PR) или отдельный воркфлоу-смотритель (звонит, не блокирует).

Готовой рекомендации не подаю как решённое: у меня есть склонность к первому варианту (краснить в `checks` — дешевле всего чинить в момент написания), но цена ложных срабатываний ложится на все треды сразу, и это твои деньги и твоя очередь.

## 4. #234 — мой PR, последний остаток за кнопкой curator

Открыт этим тактом: `docs(collision-audit): оговорка о соседнем контуре читается без имени дома (тред 064)`, голова `1d20cdf9d05f72624a588f406752231ee8b037ee`, база `origin/main = 4930561d` (после мержа #232), одна строка: `соседний контур (LLE)` → `соседний контур`, `в чужом репозитории` → `в другом репозитории`. Смысл оговорки цел: замер соседний контур не покрывает, потому что его журнала у сессии нет.

**Метку `review` на него этим тактом НЕ вешаю, и это осознанно.** Тред уезжает в парк на john (ниже), а вердикт ревьюера — письмо в тред: посылать круг в запаркованный тред значит проверять дверь парка чужой доставкой. Метка — первое действие следующего такта, когда парк снимется. `checks` по #234 на момент письма — `pending`, прогон `33746286402`; его исход приедет в тред сам.

## 5. Остаток класса 064 — весь перечислен, бесхозного нет

| что | за кем |
|---|---|
| `.github/workflows/claude-review.yml`, `PROTOCOL.md` | **#233, кнопка john** — п. 2 выше |
| `agent-protocol.json:200,208` + зеркальные `devops-declared.test.ts:190,204` | **john, едет мерой 5 треда `062`** — это действующее ПРАВО (какой чекаут devops вправе читать и обновлять), а не проза; отдельно не трогается |
| `docs/collision-audit.md:59` | **#234, кнопка curator** — п. 4 |
| `docs/contour-user-separation-plan.md:37`, имена учёток, пути `/home/lle/**` | **сознательно не вычищено** — идентификаторы живого ящика, правка ломает контур в момент правки |
| `README.md:42`, `docs/install-notes.md:160` | **не предмет** — это СВОЙ адрес, переименовать значит соврать в инструкции установки |
| возврат класса с новыми PR | **предмет 3 выше, решение john** |

`.github/workflows/**` после мержа #233 имени соседа не содержит вовсе — это и есть проверка, названная в msg-051, и мерить её надо ПОСЛЕ мержа, а не по ветке.

## 6. Ход

Паркуюсь на john: без его кнопки на #233 и без ответа по предмету 3 живой работы у треда не остаётся, а метка на #234 сознательно отложена на такт после снятия парка.

**Оговорка, которую обязана назвать:** прошлый парк этого треда на john в `notify.state` строкой `parked john 064-forget-the-consumer` НЕ появился, тогда как строки соседних тредов (`062`, `090`, `091`, `092`) там есть и моложе — замер dev-core `2026-09-03` ~`10:2xZ`, msg-053 §5. Диагноза не ставлю, но означать это может ровно одно: звонка по этому парку могло не быть, и john этого письма мог не услышать. Предмет по теме — тред `082-no-verdict-letter-vs-park`; если и этот парк не даст строки, различитель сработал дважды и предмет перестаёт быть гипотезой.

waiting-on → curator (john в это поле не принимается).

