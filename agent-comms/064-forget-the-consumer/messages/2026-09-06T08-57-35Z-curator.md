---
from: curator
worker: claude-code
session: 4bc70b29-93db-4055-a8e4-e78ebad997cd
raised: 2026-09-06T08:50:59Z
date: 2026-09-06T08:57:35Z
expects: ack
waiting-on: curator
parked-on: john
---

**ТРЕБУЕТСЯ РЕШЕНИЕ JOHN: Т9 (живая приёмка доставки смотрителя) моей рукой НЕ СНИМАЕТСЯ — у PAT роли `actions=read`, а `workflow_dispatch` требует `actions=write`. Нужна одна твоя кнопка: Actions → «Foreign Name Watch» → Run workflow → ветка `probe/foreign-name-watch-064` (она уже стоит и ждёт, с подложенной находкой). Либо скажи, что Т9 закрывается замером стыка из §2 — тогда ветку сношу и закрываю тред.**

Не «похоже, прав нет», а замер, и он называет право дословно:

```
$ gh workflow run foreign-name-watch.yml --ref probe/foreign-name-watch-064
could not create workflow dispatch event: HTTP 403: Resource not accessible by personal access token

$ gh api -i -X POST repos/…/actions/workflows/351436639/dispatches -f ref=probe/foreign-name-watch-064
HTTP/2.0 403 Forbidden
X-Accepted-Github-Permissions: actions=write
```

Токен — fine-grained PAT аккаунта `maysway`; ЧТЕНИЕ прогонов у него есть (`actions=read`, отсюда `gh run list` работает всю ленту). Обхода, аналогичного close/reopen для `checks`, здесь нет по построению: событие `workflow_dispatch` не поднимается ничем, кроме самого диспатча. Права и токены — твоя граница, не моя, поэтому расширять не прошу молча, а кладу развилкой в §4.

## 1. Что родил сам merge — и чего это НЕ доказывает

Три прогона `Foreign Name Watch` от `push` в `main`, все `success`: `34022650639` (`fd455bdb` — сам merge #280), `34022666469` (`38a2ec6c`), `34022679388` (`c701e58e`). Ровно то, что я обещала письмом до merge: **зелено и молча** — на живом дереве настоящих находок ноль, ветвь `[ ! -s "$FRESH" ]` кончается `exit 0`. Это доказывает путь замера и НЕ доказывает доставку письма. Читать зелёный молчаливый прогон как «смотритель работает» нельзя, и я его так не читаю.

## 2. Что измерено ДАРОМ, пока кнопка недоступна — стык на НАСТОЯЩЕЙ ленте

Ждать твоей руки, ничего не померив, было бы полевым критерием «материал появится сам». Поэтому прогнала весь путь письма руками, вне раннера, на РЕАЛЬНОМ дереве почты (`git archive origin/comms | tar -x` в `mktemp -d -p /tmp`), с настоящими коммитами и настоящей командой доставки. Пять фактов:

1. **Шаблон сверен контролем, а не своим нулём.** Подложенная строка даёт совпадение (`docs/probe-foreign-name-watch-064.md:4`), дерево без неё — ноль тем же вызовом в ту же минуту. Нулевой вывод от работающего шаблона, а не от сломанного;
2. **Дельта Т7 на настоящих коммитах:** голова `8faf1796` (ветка-пробник) — 1 совпадение, родитель `c701e58e` (живой `main`) — 0; `fresh=1`, `known=0`. Родитель прочитан, отказа нет;
3. **Тело письма собрано настоящим `foreign_name_watch_letter`** — называет место `путь:строка` с содержанием, говорит «звонок, а не отказ», ссылается на прогон и на класс;
4. **Доставка настоящей командой:** `cli new-message --ensure-thread foreign-name-in-the-tree --participants github,dev-core,curator --from github --waiting-on curator --write --no-push` → `no thread of this address exists yet — opening '136-foreign-name-in-the-tree' as its receiver`, легли `_meta.md` (`status: open`, участники те) и файл письма с `waiting-on: curator`. То есть адрес по слагу открывается, участники дверью приняты, ход ложится на curator;
5. **Строка `LANDED_IN` воркфлоу проверена ровно тем `sed`, что стоит в джобе** — из `git status --porcelain` она достаёт `136-foreign-name-in-the-tree`. Эту строку не покрывал ни один юнит: она живёт в YAML.

**Чего этот замер НЕ покрывает и почему он приёмкой Т9 не является:** среда раннера (два чекаута, `pnpm install`, путь `${GITHUB_WORKSPACE}/.code/...`) и **сам push в `comms` токеном джобы** через `comms_push`. Иначе говоря — доказана вся логика письма и его адресация, не доказан последний метр: что джоба реально запишет письмо в ленту. Ровно этот метр и есть Т9.

## 3. Ветка-пробник — стоит и ничего не стоит

`probe/foreign-name-watch-064`, голова `8faf1796`, один коммит поверх `c701e58e`: файл `docs/probe-foreign-name-watch-064.md`, внутри — подложенная строка и прямое объявление, что это проба треда 064 и в `main` она не едет. Push ветки **не поднял ни одного прогона** (`gh run list --branch probe/foreign-name-watch-064` → пусто: `checks.yml` стоит на `pull_request`), то есть ожидание твоей кнопки не жжёт раннер.

Когда кнопку нажмёшь — письмо обязано доехать в адрес `foreign-name-in-the-tree` (сейчас его нет вовсе, значит дверь ОТКРОЕТ приёмник; на живой ленте это будет `136-…`, если раньше не откроется что-то ещё). Прохождение мерится письмом в ленте адреса, а не цветом прогона; зелёный прогон без письма = провал Т9.

## 4. Развилка — тебе

1. **(рекомендую) Нажать Run workflow на `probe/foreign-name-watch-064`.** Одна кнопка, полминуты, и Т9 снимается по-настоящему. Дальше мой такт: читаю ленту адреса, пишу приёмку, сношу ветку-пробник, закрываю тред `064`;
2. **Выдать роли `actions=write`** — отдельный от (1) вопрос и, по-моему, назревший: класс «приёмка, чей единственный живой ход — диспатч» повторится (тот же `notifier-watch`), и каждый раз он будет стоить твоей руки и такта роли. Но это право и деньги — решение только твоё, и я его не подразумеваю;
3. **Объявить Т9 закрытым замером §2** — тогда живого последнего метра у смотрителя не будет проверено никогда, и первый настоящий звонок станет одновременно первой его проверкой. Я так делать не советую, но это твой выбор, и он законен.

При (1) и (3) от меня один такт; при (2) — я снимаю Т9 сама в тот же день.

## 5. Хвост открытых PR

`gh pr list --state open` этого прогона: **#285** (тред 134, метка `review`, `MERGEABLE`) — единственный. Моей кнопки кандидат не ждёт: след гарда 5 обязан лечь в ленту треда-хозяина, а этот прогон связан тредом `064` — писать в 134 мне нечем, поэтому и `merge-gate` по нему не гоняла: результат было бы некуда положить.

## 6. Состояние

Рабочее дерево `.worktrees/curator` — чистое, незакоммиченного нет, `HEAD` возвращён в детач на `ccf7691d`; локальная ветка-пробник удалена, удалённая (`origin/probe/foreign-name-watch-064`) намеренно оставлена под твою кнопку. Тело письма и весь замер §2 — в `mktemp -d -p /tmp`, вне обоих чекаутов; в `agent-comms` своей рукой не писала, замер доставки шёл в одноразовую распаковку ленты и никуда не пушился (`--no-push`, а у той распаковки и remote-то нет). Ни ветки, ни коммита, ни комментария в чужом репозитории. У конвейера не стою: живых прогонов моего предмета нет.

Память роли поправлена одной заметкой (не новой): класс «у PAT нет записи в Actions» теперь покрывает и `run rerun`, и `workflow_dispatch`, с точной строкой замера и следствием для ПОСТАНОВОК — приёмка через диспатч называет исполнителем john заранее, а не обнаруживает это в такте приёмки.

`waiting-on → curator` (john в это поле дверь не принимает; ход остаётся у того, кто несёт вопрос).

