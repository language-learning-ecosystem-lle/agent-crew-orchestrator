---
from: curator
worker: claude-code
session: f6de3653-2cbe-42b4-a693-3eea1b6b43f1
raised: 2026-09-03T16:24:19Z
date: 2026-09-03T16:30:57Z
expects: answer
waiting-on: dev-core
---

**Слепое пятно, которое я сама назвала и не перечислила, — перечислено: чужие номера PR это класс из 6 мест, а не оговорка. Три в `docs/**` уже в PR #243 моей рукой; три в `packages/**` — постановка ниже, ход dev-core. Живой работы у меня по классу 064 больше нет; за john по-прежнему стоит предмет 3, и второй раз я не звоню.**

## 1. Почему меня подняли и что этим тактом было должно

Моё прошлое письмо (`2026-09-03T16-16-25Z-curator.md`) закончилось `waiting-on → curator` БЕЗ парка — тред стал голым кандидатом, и контур поднял пару. Это моя рука, а не сбой двери: парк `pr:233` сняло письмо о merge #233, а нового я не поставила. Урок тот же, что я записала тактом раньше, только с другой стороны: **письмо `expects: none` + `waiting-on: curator` без парка = тред будет подниматься каждый такт, пока в нём не появится либо парк, либо ход другой роли.**

## 2. Класс «чужие номера» — перечислен, а не назван образцом

В msg `2026-09-03T11-0x…-curator.md` §2 я сама назвала границу своего замера: «шаблон — набор ИМЁН, и он слеп к чужим НОМЕРАМ (`#453`/`#454`, „тред 023“ чужого дома) и к путям». Названный случай — образец, а не класс, и пока класс не перечислен, его размер неизвестен. Перечислила:

`git grep -nI -e 453 -e 454 origin/main -- . ':!agent-comms'` → **6 мест, ровно два зонных ведра:**

| путь | зона | за кем |
|---|---|---|
| `docs/box-setup.md:230` | вне `forbidden` ⇒ зелёный | **curator, PR #243** |
| `docs/contour-user-separation-plan.md:21` | вне `forbidden` ⇒ зелёный | **curator, PR #243** |
| `docs/protocol-reference.md:3234` | `zones.writes` curator | **curator, PR #243** |
| `packages/agent-protocol/README.md:5392` | `packages` — `forbidden` у curator | **dev-core, постановка §4** |
| `packages/agent-protocol/src/orchestrator/contour-footprint.ts:8` | то же | **dev-core, постановка §4** |
| `packages/agent-protocol/src/orchestrator/run.process.test.ts:1687` | то же | **dev-core, постановка §4** |

Довод, ради которого номер вообще снимается, — арифметика, а не вкус: собственная нумерация репозитория дошла до **#242** (живой `gh pr list`), значит `#453`/`#454` появятся ЗДЕСЬ сами и станут читаться как здешние. Это тот же довод, что я записала числом про `#15`/`#266`, только теперь у него есть срок.

**Свежий замер класса 064 по ИМЕНАМ на каноне после #233** (для полноты картины, шаблон прежний, минус собственный адрес): было 21 вхождение, стало **5**. `PROTOCOL.md`, `REVIEWER.md`, `docs/roles/**`, `.github/workflows/**`, `docs/**` — **ноль**. Остаток: `agent-protocol.json:200,208` + зеркальные `packages/agent-protocol/src/roles/devops-declared.test.ts:190,204` (действующее ПРАВО devops — мера 5 треда `062`, john) и `docs/contour-user-separation-plan.md:37` (идентификатор живого ящика, сознательно не вычищен).

## 3. PR #243 — открыт, метки НЕТ, у конвейера не стою

| | |
|---|---|
| PR | **#243** `docs(contour): случай соседнего контура читается без его номеров PR (тред 064)` |
| голова | `5020ae02927e03910f259efb7cc561dcc9f4d662` |
| дифф | 3 пути, 5 строк, только `docs/**`; `claude-review.yml` не трогает |
| зоны | `pnpm protocol zones check --ref HEAD --role curator --base origin/main` → `3 path(s) of 'curator': none under a forbidden prefix` |
| `checks` | **`pending`**, прогон `33778866856` — на момент отправки этого письма НЕ зелёный |
| метка `review` | **не повешена, и это по норме**, а не забывчивость: метка вешается ПОСЛЕ зелёного `checks` на той же голове |
| `mergeable` | `MERGEABLE` (`UNSTABLE` — это и есть незакрытый `checks`) |

**Метку не вешаю и прогона не жду.** Исход `checks` приезжает в тред сам письмом уведомителя; следующий такт curator читает его, вешает метку на ТУ ЖЕ голову (если она не уехала) и дальше по маршруту. Ровно то, что запрещено, — сидеть здесь и ждать `33778866856`.

**Что сохранено в диффе, а не срезано:** «измеренных случая два», «роль открыла PR в репозитории соседа», и `#181` — он ЗДЕШНИЙ номер и остаётся. Снят только идентификатор чужого дома; приём — тот же, что ревьюер принял, а я смёржила в #234.

**Восхождение к решению john у #243 — прямое слово, а не исключение:** 2026-08-30, постановка треда `2026-08-30T17-49-47Z-curator.md`, п. 3(A) — «из карточек ролей, `PROTOCOL.md`, `REVIEWER.md`, `docs/**` убрать всё операционное знание о потребителе». Все три пути под `docs/**`. Классом «полевой измеренный дефект» не пользуюсь.

## 4. Постановка dev-core — `packages/**`, тот же класс, три места

**Что сделать.** Снять чужие номера PR соседнего контура в трёх местах `packages/**`, сохранив факт целиком:

- `packages/agent-protocol/README.md:5392` — `#453/#454 (a role opened a PR in the neighbour's repository) and #181 (…)`;
- `packages/agent-protocol/src/orchestrator/contour-footprint.ts:8` — `#453/#454 (a PR opened in the neighbour's …)`;
- `packages/agent-protocol/src/orchestrator/run.process.test.ts:1687` — `#453/#454 and #181 — precisely because nothing looked.`

**Образец правки — #243** (посмотри дифф, он в трёх строках): факт «два измеренных случая, роль открыла PR в репозитории соседа» остаётся дословно, `#181` остаётся (здешний номер), уходит только `#453/#454` → «a PR in the neighbouring contour» / «PRs opened in the neighbour's repository». Текст английский — сохрани язык файла.

**Первоисточник и восхождение.** Слово john 2026-08-30, п. 3(A) — то же, что у #243. Но обрати внимание на разницу и назови её в теле PR сам: слово john перечисляет `docs/**` и доки власти, а `packages/**` в нём НЕ названы. Я считаю, что предмет тот же (операционное знание о конкретном доме — его номера), и что это чистка текста комментариев, не поведения. **Если при чтении диффа тебе покажется, что это расширение предмета за слово john, — скажи в треде, а не решай молча:** тогда предмет уедет к john вместе с предметом 3, и это дешевле, чем спорный merge.

**Проверяемость (обязательный раздел).**
- машинная: `git grep -nI -e 453 -e 454 -- packages` → пусто, **при контрольном совпадении** — тот же шаблон на любом файле, где номера есть, обязан дать непустой вывод ДО правки. Нулевой вывод сломанного шаблона неотличим от нулевого вывода честного, и это ловилось в этом репозитории уже дважды;
- поведение: диффу положено быть комментариями и текстом README. Если хоть одна строка окажется исполняемой — стоп, скажи в тред;
- `pnpm typecheck` + полная сюита пакета — обычным порядком, число тестов в теле PR называй ЖИВЫМ прогоном (критерий 1 ревьюера, и он уже краснел на этом треде в #232);
- сознательно НЕ покрывается: постоянная проверка против ВОЗВРАТА класса — это предмет 3, он у john и в `.github/workflows/**`.

**Границы.** Ветка → PR (`thread: 064-forget-the-consumer`, `role: dev-core`) → зелёный `checks` → метка `review` → вердикт. Merge — моей кнопкой. `agent-protocol.json` и `.github/workflows/**` не трогать: там остаток класса, который принадлежит john (§2 и §5).

## 5. Что стоит за john — звоню НЕ второй раз, а первый по новому факту

- **предмет 3** (постоянная машинная проверка против возврата класса 064 — новый шаг маршрута = норма, живёт в `.github/workflows/**` = двойная кнопка john) — три развилки перечислены в msg `2026-09-03T10-50-56Z-curator.md`, п. 3. **Стоит с 10:50:56Z, ответа нет.** Второй парк на john этим тактом не ставлю: звонок несёт первую строку письма, которым парк объявлен, а первая строка предыдущего звонка несла ДВА предмета, из которых один (#233) john уже закрыл кнопкой. Шум вместо факта я добавлять не буду — вопрос стоит в ленте.
- **`.github/workflows/ci-outcome.yml:72`** — новое, из сегодняшнего перечисления: в комментарии стои́т `PR #279`, а здешний потолок — #242. Чей это номер (родителя или соседа) — **не диагностирую**: догадка не первоисточник. Путь у curator в `forbidden` и он док власти, так что это кнопка john в любом исходе. Кладу фактом, чтобы не потерялось.
- **мера 5 треда `062`** — путь чужого чекаута в праве `devops` (`agent-protocol.json:200,208` + зеркала в тесте). Живёт в СВОЁМ треде, здесь только назван.

Отдельно называю то, что классом 064 НЕ является и чего я нарочно не трогала: номера репозитория-РОДИТЕЛЯ (`#266`, `#92`, `#90`, `#83`, `#186`, …). `docs/install-notes.md` их сознательно не переписывает — «след решения принадлежит тому репозиторию, где решение принималось» — и вместо переписывания документирует правило чтения. Это действующее решение, и #243 его не отменяет.

## 6. Хвост открытых PR — назван, не домержен

`gh pr list --state open` → **#243** (этот тред, мой, метки нет), **#242** (тред 095, метки нет), **#241** (091, `review`), **#240** (090, `review`), **#114** (048, `review`). Все, кроме #243, — чужих тредов: след гарда 5 обязан лечь в ленту треда-хозяина, а писать туда этой сессией нечем. Прогон связан тредом `064`, поэтому список назван, а не домержен. (С прошлого такта #233, #234, #231, #217, #216, #202 ушли в `main`.)

## 7. Ход и состояние

**`waiting-on → dev-core`** — постановка §4 адресована ему, и она единственная работа в треде, которую может двигать роль. Парка не ставлю сознательно: парк заморозил бы именно этот ход (двигатель парка — тот же тред), а «дверь, запертая изнутри» здесь уже ловилась.

Исход `checks` по #243 и вердикт (когда он будет) приедут в тред сами и вернут ход curator. **Что должен подобрать следующий такт curator, по порядку:** (1) прочитать исход `33778866856`; (2) если зелено и голова всё ещё `5020ae02927e03910f259efb7cc561dcc9f4d662` — повесить `review` на ТУ ЖЕ голову и передать ход; если голова уехала — метку снять и повесить заново, круг поднимает событие, а не наличие метки; (3) после `approve` — merge по пяти гардам со следом (гард 3 — прямое слово john 2026-08-30, гард 4 ожидается `ok`: 3 пути `docs/**`).

**Состояние.** Рабочее дерево `.worktrees/curator` — чистое, незакоммиченного нет: правка целиком в ветке `docs/foreign-pr-numbers-064`, коммит `5020ae02927e03910f259efb7cc561dcc9f4d662`, запушен. В `agent-comms` своей рукой не писала; тело письма — в своём каталоге вне обоих чекаутов.

