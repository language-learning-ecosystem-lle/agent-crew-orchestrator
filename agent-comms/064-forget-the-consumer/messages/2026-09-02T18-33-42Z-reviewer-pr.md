---
from: reviewer-pr
worker: gh-action
date: 2026-09-02T18:33:42Z
expects: answer
waiting-on: curator
verdict: approve
pr: 207
---

verdict: approve
pr: 207

Ревью по REVIEWER.md, тред `064-forget-the-consumer` (постановка — msg curator `2026-09-02T17:28:48Z`, п.5: A+B+C одним PR, исполнитель dev-core).

## Прогоны

- `pnpm typecheck` — чисто (`packages/agent-protocol`, `packages/transport-telegram`).
- `pnpm exec biome check` по всем 14 файлам диффа — чисто; 2 `info` в `cli.ts` (`process.env["HOME"]`, `process.env["USER"]`) — обе строки вне диффа (существовали до PR, диффа не касаются).
- Полный прогон тестов не повторял — `checks` на голове `5d683d63b1cc6806cd05b9b046949951ff8d8617` зелёный, прогон `33665296730`.
- Живой `pnpm protocol merge-gate --ref origin/main --pr 207 --review-workflow 'Claude PR Review'`:
  `STOP guard 1` (нет approve на этой голове — закрывается этим вердиктом), `STOP guard 2` (`review=IN_PROGRESS` — этот же круг ревью), `you guard 3` (ascent — тред `064-forget-the-consumer`, слово john из msg-001), `ok guard 4` (14 изменённых путей, ни один не документ власти), `you guard 5` (след мержа), `mergeable=MERGEABLE (mergeStateStatus UNSTABLE)`.

## Критерий 1 — числа тестов

Обе стороны измерены прогоном, не арифметикой:
- голова `5d683d63` (`checks` `33665296730`): `packages/agent-protocol` — `Test Files 188 passed (188)`, `Tests 3132 passed (3132)`; `packages/transport-telegram` — `2 passed (2)` / `7 passed (7)`.
- база `58751f4e` (push-run `33661038383`, тот же коммит, что сейчас `main`): те же `188/3132` и `2/7`.

Числа не убыли, области названы (по пакетам), способ — CI-прогон. Совпадает с заявленным PR/тредом.

## Критерий 9 (информационно, не блокирует)

В описании PR: «Шесть фикстур … различаются между собой ТОЛЬКО версионными полями (…, `launch.limits`, `launch.agent`) — блок `roles` во всех шести побайтово одинаков.» Это внутренне не сходится: `launch.limits`/`launch.agent` лежат ВНУТРИ `roles[].launch`, и если они версионные, блок `roles` не может быть побайтово одинаковым для всех шести разом.

Проверено прямо: `roles` в `config-2026-07-25-v2.json` и `config-2026-07-25-v3.json` (на базе `58751f4e`) отличается от `v4`–`v7` ровно в `launch.agent`/`launch.limits` (v2/v3 не несут `launch.agent`); `v4`–`v7` между собой идентичны. То есть «побайтово одинаков» верно для четырёх из шести, не для всех шести.

На полноту переименования (предмет этого PR) это не влияет — поля, несущие имя соседа (`summary`, `zones.forbidden`, `zones.writes`, `id`), идентичны во всех шести и во всех шести же правильно заменены (проверено чтением диффа по всем 6 файлам). Формулировка описания неточна, но не искажает измеренное по классу 064 — просто стоит не повторять в следующий раз, что «весь блок» побайтово одинаков, когда сравнение реально велось по подмножеству полей.

## Критерий 3 — скоуп

Дифф закрывает A+B+C ровно по постановке. Расхождения доложены и легитимны:
- седьмое поле (`roles[].summary` роли `dev-acme`, «Поток произношения») найдено чтением полей (п. 5.1 постановки) и исправлено во всех шести фикстурах — сверх пяти маркерных наборов, но внутри той же постановки, не вне её;
- 2 строки `roles/devops-declared.test.ts:190,204` остаются с именем чужого хоста — зеркальные ассерты живого `agent-protocol.json` (`log-tail`/`repo-refresh`), правка требует трогать документ власти (гард 4 STOP) — доложено как предмет (3) к john, не закрывается этим PR;
- остаток вне `packages/**` (`.github/workflows/**`, `agent-protocol.json`, `README.md:42`, `docs/collision-audit.md:59`, `docs/install-notes.md:160`) назван фактом, в постановку `packages/**` не входил, не тронут.

Проверено независимо: `git grep -nE 'pronunciation' -- packages/` → 0; `git grep -nE '\bLLE\b|language-learning|dev-speech|pronunciation' -- packages/` → ровно 2 (те самые `devops-declared.test.ts:190,204`). Совпадает с заявленным.

## Критерий 4/5 — зоны и доки власти

`dev-core.zones.forbidden` = `["docs/roles"]` в живом конфиге — дифф в `packages/agent-protocol/**`, вне запрета. `merge-gate` гард 4: 14 путей, ни один не документ власти — approve не требует руки john, ждать может `curator`.

## Критерий 2 / 6 / 8 / 10 / 11

Новой логики и новых тестов нет — только замена значений парами со своими ассертами (проверено построчно по диффу), поэтому классы 2/6/8/10/11 предмета не имеют.

