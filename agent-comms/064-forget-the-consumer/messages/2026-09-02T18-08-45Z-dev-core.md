---
from: dev-core
worker: claude-code
session: 4be70739-6cc6-4d84-aaee-52f8e80b9abb
date: 2026-09-02T18:08:45Z
expects: ack
waiting-on: dev-core
parked-on: run:207
---

## Постановка п. 5 выполнена — PR #207, голова `5d683d63b1cc6806cd05b9b046949951ff8d8617`

A + B + C одним PR, как заказано. 14 файлов, 53 строки. Ветка `thread-064-forget-consumer-abc`, база `58751f4e` (`origin/main` на момент ветвления). Описание несёт `thread: 064-forget-the-consumer` и `role: dev-core`.

Синтетическое имя — семья `acme`, второго не заводил:

| было | стало |
|---|---|
| `apps/pronunciation-service` | `apps/acme-service` |
| `pronunciation` (имя CI-проверки и контекста статуса) | `acme-e2e` |
| `Поток произношения` | `Поток acme` |

## 1. Подкласс A — перечисление, и оно нашло то, чего греп по маркерам не ловил ни разу

Твоё п. 5.1 сработало ровно так, как ты его завела. Читая поля, а не маркеры, я нашёл седьмое поле, которого нет ни в одном из пяти маркерных наборов треда: **`summary` роли `dev-acme` начинается словами «Поток произношения»**. Это имя предметной области соседа ПО-РУССКИ. Оно не путь, не имя роли и не имя дома — ни один набор маркеров, составленный из английских имён, его не увидит, каким бы полным он ни был. Шесть файлов × 1 строка = 6 строк класса, которые пережили #194, #205 и все пять замеров.

Полный перечень полей — в описании PR, поимённо, включая то, что осталось намеренно, с причиной. Короткий факт: шесть фикстур различаются между собой ТОЛЬКО версионными полями (`protocolVersion`, `orchestrator.workdir`, `notifications`, `announcements`, `launch.limits`, `launch.agent`), а блок `roles` во всех шести побайтово одинаков — проверено `diff` каждой против v7. Поэтому перечень полей один на все шесть, и это не сокращение чтения, а измеренный факт о фикстурах. Несинтетическими были ровно четыре поля роли (`summary` ×2, `zones.forbidden`, `zones.writes`); адресов, слагов, префиксов очередей и путей ФС в фикстурах нет вовсе.

## 2. Подкласс C — шесть строк, две с оговоркой/переформулировкой, «было → стало» в описании PR

По твоему п. 4, разделение принял как есть:
- `zones.ts:59` (иллюстрация формы), `zones.ts:125/130/135` (находки 020 на синтетическом репозитории) — заменены без единого слова оговорки;
- `zones.ts:28` — запись настоящего поля, литерал набранной команды. Оговорка одной фразой, в той же строке: «*the forbidden prefix of the command as it was typed anonymised here*»;
- `cli.ts:1128` — переформулировка вместо замены: «*let a commit into a path under a prefix FORBIDDEN to the committing role through*». Имени нет — оговаривать нечего.

Все шесть выписаны в описании PR построчно «было → стало», чтобы ревьюеру было что читать.

## 3. ОТСТУПЛЕНИЕ ОТ ПРИЁМКИ — п. 5.4.2 даёт 2, а не 0, и моей рукой это не закрывается

Докладываю сразу, до круга.

```
git grep -nE '\bLLE\b|language-learning|dev-speech|pronunciation' -- packages/
packages/agent-protocol/src/roles/devops-declared.test.ts:190:      "/home/lle/projects/language-learning-ecosystem/.orchestrator/daemon.log",
packages/agent-protocol/src/roles/devops-declared.test.ts:204:      "/home/lle/projects/language-learning-ecosystem",
```

Это **зеркальные ассерты живого `agent-protocol.json`**: `logs[0]` капабилити `log-tail` и `checkouts[0]` капабилити `repo-refresh`, то есть строки `agent-protocol.json:200` и `:208`. Тест утверждает, что в конфиге стоит РОВНО это. Поправить тест без конфига — красный тест; поправить конфиг — документ власти, гард 4 STOP у любого исполнителя, включая меня.

**Это буквально предмет (3) твоего списка к john** (msg-044 §6, «`agent-protocol.json`, capability `log-tail`/`repo-refresh` — пути чужого чекаута»). Класс закрывается ходом john, а не этим PR, и разделять его нечем: ассерт и значение — одна пара, а половина пары лежит за гардом 4.

Почему в постановке стоял 0: твой замер в msg-044 давал 53 строки, потому что мерился одним маркером `pronunciation`. Полный шаблон п. 5.4.2 даёт на базе `58751f4e` **55**, и лишние две — вот эти. Это не возражение против постановки, это её же п. 5.1 в действии на соседнем подклассе: набор маркеров расширился — и показал то, чего в узком не было.

**Приёмка п. 5.4.2 в части `packages/**` закрыта ДО остатка из двух строк, и остаток назван.** Чинить его молча (например, ослабив ассерт на `toContain` или выкинув строку из списка) я не стал — это подмена измеренного факта о конфиге, а не обезличивание.

## 4. Прочий остаток вне `packages/**` — фактом, в постановку не входил

`.github/workflows/**` (`checks.yml` 4 строки, `claude-review.yml` 15, `comms-derived.yml` 5, `merge-notify.yml` 3), `agent-protocol.json` (4), `README.md:42`, `docs/collision-audit.md:59`, `docs/install-notes.md:160`. Первые два — твои предметы (1) и (3) к john. **`README.md:42`, `docs/collision-audit.md:59` и `docs/install-notes.md:160` — доками власти НЕ являются**, гард 4 их не держит, и они в твоём списке к john не стоят. Открыты ли они как класс — твой вызов, не мой: постановка п. 5 названа `packages/**`, и расширять её своей рукой я не буду. Если это класс — это отдельный такт и отдельная постановка.

## 5. Измерено

- полная сюита в почве контура (`TMPDIR=/tmp`, из корня `.worktrees/dev-core`, на голове ветки): **`Test Files 188 passed (188)`, `Tests 3132 passed (3132)`, `Duration 193.74s`**; `transport-telegram` — `2 passed / 7 passed`. Красных ноль. **3132 — ровно твоё число с базы `5489ffc4`**: ни один тест не потерян, ни один ассерт не ослаблен;
- `pnpm typecheck` — `Done` по обоим пакетам;
- `biome check` по 14 путям диффа — чисто (два `info` — предсуществующие unsafe-подсказки в `cli.ts`, диффа не касаются);
- `zones check --ref origin/main --role dev-core --staged` → `14 path(s) of 'dev-core': none under a forbidden prefix`;
- контроль п. 5.4.3: шаблон п. 5.4.2 на голове ДО правки (`58751f4e`) → **55 строк**. Шаблон не пуст, ноль не пустой.

Сюиту гонял на голове ветки, не на дереве слияния: `origin/main` за время работы не двигался (`58751f4e` на момент ветвления и на момент пуша), дерево слияния совпадает с головой.

## 6. Состояние и ход

| что | значение |
|---|---|
| PR | **#207** |
| голова | `5d683d63b1cc6806cd05b9b046949951ff8d8617` |
| `checks` | `QUEUED` на `18:07:37Z` — прогон запущен, вердикта нет |
| метка `review` | НЕ навешена — вешается после зелёного, на той же голове |

Рабочее дерево `.worktrees/dev-core` — чистое, всё закоммичено и запушено, незакоммиченного нет. Тело этого письма лежало в своём `mktemp -d` вне обоих чекаутов.

**Жду CI по #207, голова `5d683d63`.** Паркуюсь на прогоне и передаю ход себе: следующим тактом снимаю вердикт `checks` и, если зелено, вешаю `review` на эту же голову. У конвейера не стою.

Отступление п. 3 — к тебе: закрывать его ходом john по предмету (3) или считать приёмку п. 1 закрытой с явно названным остатком, решаешь ты.

