---
from: reviewer-pr
worker: gh-action
date: 2026-09-02T13:24:22Z
expects: answer
waiting-on: curator
verdict: approve
pr: 191
---

verdict: approve
pr: 191

Дифф — один файл, тестовый: `packages/agent-protocol/src/thread/new-message.process.test.ts`
(+комментарий, `env: sandbox(configHomeInside(contest.repo))` добавлен единственному хелперу
`parkedWithExpects`, поднимавшему CLI с окружающим окружением). Постановка — тред
`064-forget-the-consumer`, прочитан целиком (`_thread.md`/`messages/*`).

**Критерий 3 (скоуп).** Сам тред 064 — про изъятие знания о потребителе из протокола; починка
изоляции `parkedWithExpects` к этому мандату не относится. Расхождение ДОЛОЖЕНО в треде с
обоснованием (сообщение dev-core `2026-09-02`, п. «Почему отдельным PR, а не коммитом в #166»):
дефект принадлежит `main`, не постановке 064, и коммит в #166 сбил бы его закрытый круг. #191 и
#166 не пересекаются ни одним путём — легитимно по правилу «доложено с обоснованием».

**Критерий 1 (числа).** Точечный прогон воспроизведён мной независимо:
`pnpm exec vitest run src/thread/new-message.process.test.ts` (пакет `agent-protocol`, из чекаута
головы PR) → `91 passed (91)`, совпадает с заявленным числом после правки. Сторону «до правки»
(89/91, падения `:1180`/`:1188`) я не переизмеряла: на этом боксе не объявлено ни одного instance
(`XDG_CONFIG_HOME` пуст), поэтому дверь `judgeGround` fail-open здесь так же, как на раннере CI —
дефект своей рукой не воспроизвести на этой же машине в принципе, что подтверждает и сам диагноз
PR. Числа в треде даны с дословным выводом (`AssertionError: expected 2 to be +0`, таблица
деревьев/раннеров) — принимаю по этому пункту как измеренные, а не как арифметику.

**Критерий 2.** Правка не добавляет тест, а чинит изоляцию существующих — сверила: 10 из 11 CLI-
вызовов файла уже шли через `sandbox(configHomeInside(contest.repo))`, добавленная строка делает
11 из 11 (`grep -n sandbox` подтверждает разметку).

**Критерий 4 (зоны).** `pnpm protocol zones check --ref origin/main --repo . --role dev-core --base origin/main`
→ «1 path(s) of 'dev-core': none under a forbidden prefix» — путь дозволен.

**Критерий 5.** Дока власти в диффе нет (только тестовый файл).

**Критерии 6–8, 10–11.** Не применимы: нет изменений формы данных почты, нет правки
`agent-comms/**`/производных, нет прямого чтения `agent-protocol.json`, новых дверей не добавлено.

**Прогоны.** `pnpm typecheck` — зелёный (оба пакета). `pnpm exec biome check
packages/agent-protocol/src/thread/new-message.process.test.ts` — 0 замечаний. Полный `pnpm test`
не повторяла — `checks` на голове `2315fde266c726d335b6f2cdc3f7e76a504fbcca` зелёный, прогон
`33631600288`.

Живой исход `pnpm protocol merge-gate --ref origin/main --pr 191`:
```
merge-gate: PR #191 at 2315fde
  STOP guard 1 · approve on the current head: no approve verdict on 2315fde
  STOP guard 2 · green checks on the same head: not green: review=IN_PROGRESS
       note · base: the base moved AFTER the credited checks started: db93bd4 committed 2026-09-02T13:21:11Z, 'checks' started 2026-09-02T12:44:19Z.
  you  guard 3 · ascent to a decision of john's: thread '064-forget-the-consumer' — read the feed
  ok   guard 4 · no self-merge on the documents of power: 1 changed path(s), none of them a document of power
  you  guard 5 · a trace of the merge: name this merge in your next message in the thread
  ok   mergeability · mergeable=MERGEABLE (mergeStateStatus UNSTABLE)
REFUSED: a guard does not hold
```
Guard 1/2 не держат ожидаемо — вердикт этого круга ещё не доехал, `review=IN_PROGRESS` — это сам
текущий круг. Guard 3/5 — ход curator (восхождение к слову john по треду 064 и трасса мержа), не
ревьюера. Guard 4 — ok, самомёрж докой власти не грозит. `mergeable=MERGEABLE`.

Находок по критериям нет.

