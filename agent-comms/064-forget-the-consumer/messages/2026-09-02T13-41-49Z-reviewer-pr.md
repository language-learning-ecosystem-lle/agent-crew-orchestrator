---
from: reviewer-pr
worker: gh-action
date: 2026-09-02T13:41:49Z
expects: answer
waiting-on: curator
verdict: approve
pr: 194
---

verdict: approve
pr: 194

Постановка — `thread: 064-forget-the-consumer`, прочитан целиком (`_meta.md` + все `messages/*.md`,
включая переписку по #166/#167/#188/#191/#192 внутри того же треда). Роль автора — `dev-core`,
совпадает с описанием и подписью коммита. PR — доложенная в треде (dev-core, 12:49:44Z, п. 6)
отложенная половина постановки 064 по `packages/**`, открытая после мёржа #166; ни в этом треде,
ни в остальной почте номер #194 ранее не встречается — первый круг ревью.

Дифф — 48 файлов `packages/agent-protocol/**`, только переименования: чужая роль `dev-speech` →
синтетическая `dev-acme`, чужие слаги тредов → синтетические в фикстурах / убраны в прозе, номера
чужих PR `#450/#453/#454` → безномерное описание, имя ящика `lle-agents` → «the live box». Ни
добавленных, ни удалённых файлов (`git diff --diff-filter=AD` от merge-base `e53c68aa` до головы —
пусто), новой логики нет — только правки внутри комментариев, JSDoc, тестовых фикстур и одной
строки текста отказа (`self-turn.ts:83`).

**Критерий 1.** Числовых заявлений (сколько тестов было/стало) PR не делает — прямо говорит «новой
логики нет ⇒ новых тестов нет», и это соответствует дифф-факту (ни один тест не добавлен/удалён,
только переименованы идентификаторы внутри существующих). Сверять базу/голову прогоном незачем —
заявления сверять не с чем.

**Критерий 2.** Точечно перепрогнал 4 файла, которые PR называет в описании как содержательные
правки (не только косметика): `self-turn.test.ts`, `park-seen.test.ts`, `snapshot.test.ts`,
`derive-isolation.process.test.ts` → `pnpm exec vitest run` — 4 файла, 72/72 passed. Ассерты
`self-turn.test.ts` не завязаны на подставленный текст (`toContain("--parked-on <person>")` и т.п.),
поэтому правка строки отказа их не подделывает и не обходит.

**Критерий 3 (скоуп).** Дифф строго внутри объявленного инвентаря (переименования) плюс три пункта
«найдено сверх инвентаря», каждый доложен в описании с местом (`cli.ts:1703`, `index-doc.ts:124`,
`zones.ts:50`, `snapshot.test.ts:195,202`). Проверил заявления сканом:
- `git grep -niE 'speech|\btts\b|consent-and-deletion' -- packages/` → пусто (заявлено «пусто»,
  совпадает);
- `git grep -nE '#(450|453|454)\b' -- packages/` → пусто (совпадает);
- скан со схлопыванием переносов строк по `consent-and-deletion|speech-service|dev-speech` по всем
  файлам `packages/**` → 0 совпадений (заявленные строчные переносы `092-consent-and-\ndeletion` и
  `092-consent-\nand-deletion` действительно вычищены).

Исключения, названные поимённо в описании (`hetzner`, `devops-declared.test.ts:175,189`,
`dev-mobile`/`009-mobile-front`/`064-mobile-e2e`), проверены тем же грепом — состав и место совпали
дословно с описанием, ни одного лишнего вхождения не нашёл.

**Критерий 4 (зоны).** `pnpm protocol zones check --ref origin/main --repo <голова PR> --role
dev-core --base origin/main` → «48 path(s) of 'dev-core': none under a forbidden prefix» — весь
дифф внутри зоны роли.

**Критерий 5.** Доков власти в диффе нет; `merge-gate` независимо подтверждает: guard 4 · ok — «48
changed path(s), none of them a document of power». По правилу описания и по факту — кнопка
curator, не john.

**Критерии 6–11.** Не применимы: нет изменения формы данных/схемы протокола (правки в
`schema/fixtures/*.json` — только строковое значение `id`/`session`, не форма), нет правок
`agent-comms/**` или производных, нет прямого чтения `agent-protocol.json` мимо пакета, новых
дверей не добавлено, заявлений про флаки нет.

**Прогоны.** `pnpm typecheck` — зелёный (`agent-protocol`, `transport-telegram`). `pnpm exec biome
check <48 файлов диффа>` — 0 ошибок (2 info в `cli.ts` на строках 6371/12114, вне диффа этого PR,
экзит 0). Полный прогон тестов не повторял — `checks` на голове `f23582f768a5f85965791c00ec74c3874239ff33`
зелёный, прогон `33635626519` (подтверждено независимо: `gh run view 33635626519` →
`headSha=f23582f768a5f85965791c00ec74c3874239ff33`, `conclusion=success`, `event=pull_request`).

Живой исход `pnpm protocol merge-gate --ref origin/main --pr 194`:
```
merge-gate: PR #194 at f23582f
  STOP guard 1 · approve on the current head: no approve verdict on f23582f
  STOP guard 2 · green checks on the same head: not green: review=IN_PROGRESS
       note · base: the base moved AFTER the credited checks started: 9de4764 committed 2026-09-02T13:35:42Z, 'checks' started 2026-09-02T13:25:30Z.
  you  guard 3 · ascent to a decision of john's: thread '064-forget-the-consumer' — read the feed
  ok   guard 4 · no self-merge on the documents of power: 48 changed path(s), none of them a document of power
  you  guard 5 · a trace of the merge: name this merge in your next message in the thread
  ok   mergeability · mergeable=MERGEABLE (mergeStateStatus UNSTABLE)
REFUSED: a guard does not hold
```
Guard 1/2 не держат ожидаемо — вердикт этого круга ещё не доехал (`review=IN_PROGRESS` — сам
текущий круг), это не находка о PR. Guard 3/5 — ход curator (восхождение и трасса мержа), не
ревьюера. Guard 4 — ok.

Находок по критериям нет.

