---
from: curator
worker: claude-ai
date: 2026-09-03T10:00:52Z
expects: answer
waiting-on: curator
delivers: john
priority: high
merged-pr: 167
---

**Слово john полем `delivers` (чат ~10:00Z): #167 СМЁРЖЕН его кнопкой. Проза конфига и уведомителей больше не называет соседний дом.**

Дифф после вчерашнего разведения конфликта — четыре файла, четырнадцать строк: `agent-protocol.json` (поле `note` инстанса), `merge-notify.yml`, `checks.yml`, `comms-derived.yml`. Два файла (`ci-outcome.yml`, `notifier-watch.yml`) **вышли из диффа**, потому что их шапки переписал #197 и работа там оказалась уже сделанной чужой рукой — это названо в теле PR как закрытие предмета, а не отказ от него.

Идентификаторов дифф не трогал: ни одно значение, которое читает код, не менялось.

## Что осталось и почему это не хвост, а решение john

**Ровно один файл продолжает называть соседний дом — `claude-review.yml`**, около семнадцати вхождений, и они **меняют ПОВЕДЕНИЕ ревьюера** (промпт велит помечать такие ссылки), а не только прозу. Плюс класс гарда 1: метку на такой PR не вешают, круга не будет.

Развилка по нему стои́т в треде `064` (msg-008) и john её пока не решал.

**И два места сознательно НЕ вычищены, оба — не проза:**

1. **имена учёток (`lle-main`, `lle-second`) и пути `/home/lle/**`** — идентификаторы живого контура: аккаунт указывает на реальный каталог, путь — на реальное дерево. Правка ломает контур в момент правки и чинится переименованием на ящике, то есть рукой john;
2. **путь чекаута соседа в строке `repo-refresh` возможности `devops`** — это действующее ПРАВО, а не текст: убрать его значит изменить, что devops вправе обновлять. Предмет меры (C) «файловая граница» — и он теперь смыкается с радикальной мерой 5 (разные системные пользователи), заказанной john этим утром в треде `062`.

Проверка после merge — грепом: имя соседнего дома в `.github/workflows/**` встречается ровно в одном файле, названном выше.

waiting-on → curator.
