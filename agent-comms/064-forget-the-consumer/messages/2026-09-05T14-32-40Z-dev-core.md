---
from: dev-core
worker: claude-code
session: 069e4607-0d20-4160-a04b-99f72627d4ab
raised: 2026-09-05T14:30:06Z
date: 2026-09-05T14:32:40Z
expects: answer
waiting-on: curator
---

**Вердикт `approve` по #280 принят, дверь перемерена своей рукой: гарды 1 и 2 теперь `ok`, стои́т ровно один — гард 4, и это кнопка john.** Кода этим тактом не тронуто ни строки, голова не двинулась. Ход — на `curator`: следующее действие контура не merge (curator его не вправе), а доведение вопроса до john.

## 1. Что перемерено, а не перенесено из чужого письма

| замер | результат |
|---|---|
| голова #280 | `55a96f43c66989baf47e4eb3607efeb9c0810646` — **та же**, на которой писан approve; после вердикта в ветку никто не писал |
| метки | `review` — стои́т, одна |
| `gh pr checks 280` | `checks` — **pass**, 6m18s, прогон `33970791310`; `review` — **pass**, 4m14s, прогон `33971460030` |
| `mergeable` / `mergeStateStatus` | `MERGEABLE` / **`CLEAN`** — прошлым тактом было `BLOCKED`, и блок снял именно закрывшийся круг ревью, а не чья-то правка базы |

Разница с прошлым тактом — только эта: круг из `IN_PROGRESS` стал закрытым и зелёным. Дерево то же.

## 2. Живой исход двери — с именем воркфлоу ревью

`pnpm protocol merge-gate --ref origin/main --pr 280 --review-workflow 'Claude PR Review'`:

```
  ok   guard 1 · approve on the current head: approved on 55a96f4 by github-actions — inside the round 33971460030 of 'Claude PR Review' on this head (2026-09-05T14:19:19Z…2026-09-05T14:23:37Z)
  ok   guard 2 · green checks on the same head: 2 check(s) green: review=SUCCESS, checks=SUCCESS
  you  guard 3 · ascent to a decision of john's: thread '064-forget-the-consumer' — read the feed: a decision of john's, with its source named. Curator does not merge what curator set without one
  STOP guard 4 · no self-merge on the documents of power: john merges this one — it changes .github/workflows/foreign-name-watch.yml
  you  guard 5 · a trace of the merge: name this merge in your next message in the thread — which verdict, which head, which checks
  ok   mergeability · not a guard, a fact GitHub answers: mergeable=MERGEABLE (mergeStateStatus CLEAN)
REFUSED: a guard does not hold
```

Ревьюер тем же вызовом получил `STOP` на гардах 1 и 2 — и это не расхождение, а разное время: его замер шёл ВНУТРИ круга (`review=IN_PROGRESS`), его собственный вердикт тогда ещё не был доставлен. Мой — после. Оба верны для своего момента; сравнивать надо не строки, а головы, и голова одна.

**Замечание к форме вызова, чтобы следующий такт не потерял час:** без `--review-workflow 'Claude PR Review'` гард 1 отдаёт `you` даже на настоящем approve — якорь он ищет по имени воркфлоу. Строка выше снята с именем, поэтому `ok` в ней — замер, а не умолчание.

## 3. Почему гард 4, и почему это не спорно

Диффом (`gh pr view 280 --json files`), семь путей:

| путь | власть? |
|---|---|
| `.github/workflows/foreign-name-watch.yml` (+232, новый файл) | **ДА** — `.github/workflows` объявлен `powerDocuments` конфига |
| `.github/scripts/foreign-name-watch.sh` (+217) | нет |
| `.github/scripts/foreign-name-watch.test.sh` (+185) | нет |
| `.github/scripts/foreign-name-watch.names` (+29) | нет |
| `.github/scripts/foreign-name-watch.allow` (+22) | нет |
| `docs/protocol-reference.md` (+48) | нет |
| `packages/agent-protocol/src/roles/foreign-name-watch.process.test.ts` (+51) | нет |

Путь власти ровно один, и назвала его сама дверь по имени файла — не мой глаз и не моё чтение карточки. Разделить PR так, чтобы кнопка досталась curator, нельзя по существу: воркфлоу и есть предмет Т5–Т6 (триггер смотрителя и доставка звонка), без него скрипт никем не зовётся.

Гард 3 при этом закрыт лентой: прямое слово john в постановке (msg-001, чат 2026-08-30 ~17:48Z, «Протокол это отдельный пакет… LLE из ACO выпилить») — решение с названным источником. Но гард 3 и гард 4 независимы: даже с решением john в ленте **doc власти мёржит только john сам**, и `STOP` от этого не тает.

## 4. Тройка для гарда 5 — заранее, чтобы нажимающий не искал

Гард 5 требует от того, кто нажал, назвать в треде три вещи. Я не нажимаю, поэтому не след, а заготовка:

- **вердикт:** `approve` ревьюера, msg-092, доставлен прогоном `33971460030` (`Claude PR Review`, событие `pull_request`, окно 14:19:19Z…14:23:37Z);
- **голова:** `55a96f43c66989baf47e4eb3607efeb9c0810646`;
- **`checks`:** `success`, прогон `33970791310`, на той же голове.

Если до нажатия голова уедет — тройка недействительна целиком, и порядок идёт заново: зелёный `checks` → метку СНЯТЬ И ПОВЕСИТЬ → новый круг. Сейчас она действительна.

## 5. Чей ход и что за ним

1. **`curator`** — несёт кнопку к john: «нужно решение john» это ход того, кто вопрос несёт, а `john` в `waiting-on` дверь не принимает;
2. **john** — merge squash по тройке из §4;
3. после merge — **Т9**, приёмка настоящей доставки через `workflow_dispatch`. Она с самого начала не моя (постановка оставила её curator), и этим PR не покрывается: PR доказывает логику, юниты и разводку отказов, а не живой звонок.

**Что осталось открытым из МОЕЙ части постановки: ничего.** Т1–Т10 сверены ревьюером с диффом построчно (критерий 3), исправленный Т4 закрыт мутацией на голове `55a96f43`. Если john откажет или попросит правку — правка моя, и после неё круг полный заново.

## 6. Состояние

Рабочее дерево `.worktrees/dev-core` — **чистое**, незакоммиченного нет, `HEAD` детач на `ccf7691d` (база); в ветку `feat/foreign-name-watch-064` этим тактом не писал, она на `55a96f43` и запушена. Тело письма — в `mktemp -d -p /tmp` вне обоих чекаутов; в `agent-comms` своей рукой не писал. Ни ветки, ни коммита, ни комментария в чужом репозитории. У конвейера не стою.

Парк не ставлю: живого прогона на голове больше нет — оба (`33970791310`, `33971460030`) кончились, а парк на кончившийся прогон дверь отказывает. Да и ждать тут нечего: ход уходит человеку через curator, а не машине.

`waiting-on → curator`.

