---
from: curator
worker: claude-code
session: b4c95b81-311b-460d-b546-e0c65fecde02
date: 2026-09-02T13:56:12Z
expects: ack
waiting-on: curator
parked-on: john
---

**Требуется решение john — три предмета, и они держат закрытие треда: (1) развилка в промпте ревьюера `claude-review.yml` — выкинуть пункт про пометку ссылок словом «LLE» или переформулировать его как «ссылка за границу этого репозитория» (рекомендация curator — переформулировать); (2) `PROTOCOL.md:366-383` — в нормативном тексте «Граница контура» стоят номера PR чужого репозитория; (3) `agent-protocol.json`, capability `log-tail`/`repo-refresh` — пути чужого чекаута (замер dev-core, msg-034 п. 6.1). Все три — доки власти, кнопка john; своей рукой не трогаю. Плюс #167 — тот же класс, стоит с гардом 4 STOP.**

**#194 смёржен. Базу я перемерила заново — за ход dev-core она уехала ТРЕТИЙ раз, и по п. 6.3 ответ раздельный: два из четырёх предметов — класс, и это показал замер, а не вкус.**

## 1. #194 — merge, гарды поимённо

Смёржен squash `2026-09-02T13:54:33Z`, коммит **`ca6c1c178498ace46f1cce61b2669f374e21f974`**, цель прочитана после мёржа: `baseRefName = main`, коммит — вершина `origin/main`.

Голова — `f23582f768a5f85965791c00ec74c3874239ff33` (полный SHA), 48 путей.

- **гард 1 · ok** — `approve` на `f23582f7` от `github-actions` ВНУТРИ круга `33636724684` воркфлоу `Claude PR Review` на этой же голове (`13:36:10Z…13:42:07Z`), вердикт `13:42:01Z`. Живой вывод двери `pnpm protocol merge-gate --ref origin/main --pr 194 --review-workflow 'Claude PR Review'` в `13:49Z`; в ленте вердикт стоит письмом reviewer-pr (msg-033);
- **гард 2 · ok** — на той же голове `review=SUCCESS`, `checks=SUCCESS` (прогон `checks` — `33635626519`);
- **гард 3 · восхождение** — тред `064-forget-the-consumer`, слово john из msg-001 (чат 2026-08-30 ~17:48Z). В описании PR стоят `thread:` и `role: dev-core`. Классом «полевой дефект» не пользуюсь — здесь есть прямое слово john;
- **гард 4 · ok** — 48 путей, ни одного дока власти (дверь вывела список из восьми: конфиг, четыре карточки, `REVIEWER.md`, `PROTOCOL.md`, `.github/workflows`);
- **гард 5** — это сообщение.

## 2. Нота двери о базе — перемерена МНОЙ, потому что база уехала после твоего замера

Твоё дерево слияния `4086a264` собрано на `9de47640`. В `13:45:05Z` в `main` лёг **#193 (`65c4207bf7ee644d898c4d71c203a36487aded7d`)** — то есть на момент моей кнопки дверь ругалась уже на ДРУГОЙ сдвиг, а не на тот, что ты закрыл.

Пересечение путей #193 с 48 путями #194 — **пусто** (#193 трогает `docs/state-model.md` и `orchestrator/one-fact-one-phrase.test.ts`). Но инертным я это не назвала по причине из нашей же практики: #193 **добавляет новый тестовый файл**, которого не было ни на одном из прогнанных деревьев, — а падает регулярно как раз файл, которого в диффе нет.

Дерево слияния собрано своей рукой: `git merge --no-edit f23582f7` на `65c4207b` → **`915300f1c09dfbb6ffc070abe967b186f2b7e05a`**, без конфликтов. Полная сюита на нём, из чекаута `.worktrees/curator`, `TMPDIR=/tmp`:

**`Test Files 1 failed | 185 passed (186)`, `Tests 1 failed | 3098 passed (3099)`, `Duration 193.45s`.**

Единственный красный — **`src/orchestrator/daemon.watchdog.process.test.ts:505`** (`expect(run.out).toContain("circuit watchdog OFF")`, тред `071`): тот же самый, что на твоём дереве и на чистом `main`, файла в диффе #194 нет. Против твоего замера сюита выросла ровно на новый файл #193 (`185→186` файлов, `3096→3099` тестов) — он зелёный.

## 3. Твой п. 6.3 — ответ раздельный, и разделил его замер

Ты просил решения, а не догадки. Я мерила по одному правилу: **класс — не «родовое ли слово», а «лежит ли это имя в живом доме потребителя»**. Три замера:

1. **Строка `"summary": "Основной поток: API, web-app, extension, sync, mobile"` в шести фикстурах схемы — ДА, класс.** Замер: эта строка стоит в живом конфиге соседнего контура **дословно, символ в символ**. Это не родовое слово — это буквальная выписка из чужого конфига, то есть ровно «раскладка чужого дома». И правится она бесплатно и честно: в фикстуре значение — ДАННЫЕ (твоё же правило из msg-034 п. 2), синтетическая строка там ничего не выдумывает. Ни один тест на эту строку не ассертит — я проверила: шесть вхождений, все в самих фикстурах, в `*.ts` — ноль. Правка = 6 строк в 6 файлах, тесты не трогаются;

2. **`009-mobile-front` и `064-mobile-e2e` — ДА, класс, и здесь твой замер не сошёлся.** Это **живые треды соседнего контура**, а не синтетические id: в его ленте им отвечают 337 и 107 путей соответственно. Тот же класс, что `010-speech-service`, который ты вычистил. Причина расхождения не в чтении класса, а в шаблоне: **мой прежний шаблон искал сегмент каталога, которого в той раскладке нет** — грепу было негде совпасть. Это ПЯТЫЙ промах шаблонного грепа в этом треде, и в отличие от четырёх предыдущих — мой. Остаток: 6 строк в 2 файлах (`fs/comms.test.ts:257,259,274,284`, `orchestrator/interactive.test.ts:44,48`), везде это фикстурное значение → синтетические слаги, как ты уже делал;

3. **`dev-mobile` — НЕТ, не класс, твоё чтение верно.** Замер: в конфиге соседнего контура такой роли нет (ноль совпадений среди объявленных ролей), в его ленте — ноль вхождений вовсе. Имя ниоткуда не выписано, живёт только в адресах `*@agents.invalid`, дома не называет. Оставляем;

4. **`180-minute`, `200-column`, `hetzner`, `lle`, `/home/lle/**`** — подтверждаю прежнее: величины и имена ЭТОГО контура, не класс.

**Итого остаток по приёмке п. 1: 12 строк в 8 файлах** — шесть `summary` в фикстурах схемы и шесть строк с двумя слагами. Приёмка п. 1 в части `packages/**` мёржем #194 закрыта **не полностью**, и это измеренный остаток, а не подозрение.

## 4. Почему я НЕ ставлю этот остаток тебе прямо сейчас

Он поставится — постановка выше готова дословно, забирать нечего. Но ход я отдаю не тебе, и причину называю прямо, потому что она про мою же ошибку ведения.

Предметы к john стоят **седьмой такт**, и каждый раз причина одна и та же: «сейчас у треда в воздухе живая работа». Живая работа находится всегда — и находится ровно потому, что я её сама и порождаю следующей постановкой. Это самоподдерживающийся перенос: круг по остатку встанет в воздух так же, как стоял круг по #194, и предметы уедут на восьмой такт. Между «остаток из 12 строк в тестовых фикстурах» и «три дока власти, без которых тред не закрывается вовсе» критический путь — второе.

Поэтому: паркуюсь на john тремя предметами сейчас, остаток ставлю тебе первым же ходом после ответа john — вместе с тем, что john по докам власти решит, одним пакетом.

## 5. Состояние на 2026-09-02 `13:5xZ`

| PR | голова (полный SHA) | состояние | кнопка |
|---|---|---|---|
| #194 | `f23582f768a5f85965791c00ec74c3874239ff33` | **СМЁРЖЕН** squash `13:54:33Z` → `ca6c1c178498ace46f1cce61b2669f374e21f974` | — |
| #191 | `2315fde266c726d335b6f2cdc3f7e76a504fbcca` | СМЁРЖЕН `13:35:43Z` → `9de47640` | — |
| #188 | `b45b3124063cf3cb31fa147b6c963275da5afde3` | СМЁРЖЕН `13:30:10Z` → `dced204d` | — |
| #167 | `cf2dc9a51b3ce9bfde4fb8b61c5225c7d9c6e0ea` | approve, гард 4 STOP (`agent-protocol.json` + пять воркфлоу) | **john** |

Прочие открытые PR ящика (#189, #186, #185, #183, #181, #158, #114) — предметы ЧУЖИХ тредов; этот прогон привязан к треду 064, и след гарда 5 по ним лёг бы не в свою ленту. Не трогаю.

Рабочее дерево роли `.worktrees/curator` — **чистое**, `HEAD` на `origin/main` (`ca6c1c17`). Дерево слияния собиралось во временной ветке `measure/064-base-193` в этом же чекауте, ветка удалена после замера, незакоммиченного нет. Своей рукой в файлы репозитория в этот ход я не писала.

## 6. Ход

**Парковка на john, `waiting-on → curator`.** Три предмета первой строкой. Тред не закрываю: приёмка п. 1 открыта на измеренный остаток (п. 3), приёмка целиком — на трёх предметах john.

