---
from: dev-core
worker: claude-code
session: c49268d4-ccf5-4028-91b1-d9f5d851ae23
raised: 2026-09-05T12:59:21Z
date: 2026-09-05T13:07:11Z
expects: none
waiting-on: dev-core
---

**Смотритель начат и закоммичен ЧАСТЬЮ: логика, набор имён, список исключений и 17 юнитов — зелены и запушены (`feat/foreign-name-watch-064`, `9d35b91811052fa3e45c42d6b8407ddafb6902b7`). НЕ сделано: сам воркфлоу `.github/workflows/foreign-name-watch.yml` и процессный тест, поднимающий юниты сюитой. PR НЕ открыт. Свернулась по сроку аренды (13:59:21Z), ход — следующему такту dev-core.**

## 1. Что лежит в ветке (4 файла, все новые)

| файл | что в нём |
|---|---|
| `.github/scripts/foreign-name-watch.sh` | вся логика: замер по всему дереву, машинное вычитание собственного адреса, правило исключений, сравнение с родителем (Т7), сборка тела письма и тела «НЕ ПРОЧИТАНО» (Т4) |
| `.github/scripts/foreign-name-watch.names` | набор имён — ДАННЫЕ (Т10), каждое с причиной |
| `.github/scripts/foreign-name-watch.allow` | исключения: путь + сужение содержанием + причина; номеров строк нет |
| `.github/scripts/foreign-name-watch.test.sh` | 17 проверок по семи пунктам §4 постановки — **все зелёные** |

## 2. Набор имён — оба имени с причиной (Т10)

1. **`language-learning-ecosystem`** — репозиторий и каталог чекаута соседнего контура. Почему имя соседа: наш репозиторий — `agent-crew-orchestrator`; эта строка в дереве встречается либо ЧАСТЬЮ собственного адреса (владелец `language-learning-ecosystem-lle`), либо указанием на чужой контур.
2. **`LLE` ЗАГЛАВНЫМИ** — сокращение того же дома. **Регистр здесь не украшение:** строчное `lle` — это пользователь ЖИВОГО ЯЩИКА и его домашний каталог (`/home/lle/...`), то есть идентификатор нашей машины, который приёмкой 2026-09-05 сознательно не вычищается. Положить `lle` в набор значило бы завести звонок, звенящий на каждой строке `docs/box-setup.md`. Граница слова тоже замер, а не вкус: `LLE` без `\b` ловит `CALLED`.

Собственного имени в наборе НЕТ намеренно: адрес вычитается машинно из `GITHUB_REPOSITORY` (fallback — `git remote get-url origin`), литерал устарел бы молча при переименовании.

## 3. Живой шов — ПОЛНЫЙ перечень совпадений на рабочем дереве с классификацией каждого

Прогон по всему дереву головы ветки (`foreign_name_watch_scan .`), **тихого списка исключений нет** — вот он весь:

| место | класс |
|---|---|
| любое вхождение внутри `language-learning-ecosystem-lle/agent-crew-orchestrator` | **собственный адрес** — вычтен машинно, находкой не является |
| `docs/box-setup.md` (целиком) | **исключение:** инструкция по живому ящику называет оба контура на одном железе поимённо; переименовать — соврать в инструкции |
| `docs/contour-user-separation-plan.md` (целиком) | **исключение:** чужой контур — вторая сторона самого плана |
| `agent-protocol.json`, строки с `/home/lle/projects` | **исключение, сужено содержанием:** пути живого ящика в объявлении devops. **Это док власти, и я его не заминаю:** сужение оставлено намеренно — любое ДРУГОЕ вхождение имени соседа в этот файл обязано звонить. Расширить исключение на весь путь — решение john, а не моё |
| `packages/agent-protocol/src/roles/devops-declared.test.ts`, строки с `/home/lle/projects` | **исключение:** зеркало того же объявления, пути обязаны совпадать дословно |
| `.github/scripts/foreign-name-watch.names`, `.allow` | **исключение:** смотритель обязан содержать имя соседа, иначе не работает |
| `.github/scripts/foreign-name-watch.test.sh` | **исключение, найденное ЖИВЫМ ПРОГОНОМ, а не рассуждением:** его фикстуры обязаны нести имя соседа, иначе проверка «то же имя вне собственного адреса — находка» проверяет пустоту. **Без этой строки смотритель первым же звонком звонил бы на самого себя** — 14 мест. Строка добавлена, перезамер даёт ноль |

**Настоящих находок на живом дереве — НОЛЬ.**

**Контроль работающего шаблона — есть, и он не мой нулевой вывод.** Тот же прогон ДО добавления последнего исключения дал **14 совпадений** в `foreign-name-watch.test.sh` — то есть ноль получен от шаблона, который в ту же минуту дал непустой список. Плюс в юнитах отдельная проверка на заведомо-положительном входе (проверка 2).

## 4. Проверяемость §4 — что закрыто

Все семь пунктов закрыты юнитами (17 проверок, прогон `bash .github/scripts/foreign-name-watch.test.sh` → «все проверки прошли»): (1) имя внутри собственного адреса — не находка; (2) вне его — находка; (3) исключение по пути + сужение содержанием + грep по боевому `.allow` на отсутствие адресации номером строки; (4) пустой набор — писем нет; (5) отказ замера — ненулевой код, причина по имени, письмо «НЕ ПРОЧИТАНО»; (6) новое против родителя — ключ места это путь+текст БЕЗ номера строки, сдвиг новым не считается; (7) тело несёт путь+строку+текст и хвост «остаётся».

**Дефект собственного теста, пойманный и починенный:** ветвь «собственный адрес не определён» сначала была зелёной ложно — `TMPDIR` сессии роли стои́т ВНУТРИ чекаута, и `git remote get-url origin` из временного каталога находил НАШ же remote. Лечится `GIT_DIR` в никуда; строка помечена комментарием.

## 5. Что НЕ сделано — ровно это подбирает следующий такт

1. **`.github/workflows/foreign-name-watch.yml`** — тонкий триггер по образцу `notifier-watch.yml`: `on: push` в дефолтную ветку + `workflow_dispatch` (Т8, Т9), два чекаута (`comms` и `main`), pnpm, один шаг «Замерить и позвонить». Внутри шага: скан головы, скан родителя (`git archive HEAD^` во временный каталог), `foreign_name_watch_new_places`; непусто → тело письмом и доставка `comms-push.sh` + `cli new-message --ensure-thread foreign-name-in-the-tree --title … --participants github,dev-core,curator --from github --expects none --waiting-on curator --write --no-push` в цикле попыток (Т1, Т6). Отказ доставки — `exit 1` и `::error::`, всё прочее зелено.
2. **`packages/agent-protocol/src/roles/foreign-name-watch.process.test.ts`** — по образцу `notifier-mute.process.test.ts`: сюита гоняет `.test.sh`, ждёт строку «смотритель имени соседнего дома: все проверки прошли».
3. **PR** — `pnpm protocol pr open`, тело с `thread: 064-forget-the-consumer` и `role: dev-core`. **Гард 4 сработает объявленно:** PR трогает `.github/workflows/**` ⇒ merge — кнопка john.
4. Полная сюита не гонялась вовсе — этим коммитом код пакета не тронут, но перед PR прогнать обязательно.

## 6. Состояние

Рабочее дерево `.worktrees/dev-core` — **чистое**, незакоммиченного нет. Всё сделанное в ветке `feat/foreign-name-watch-064`, коммит `9d35b91811052fa3e45c42d6b8407ddafb6902b7`, запушен на origin. Тело этого письма — в `$TMPDIR` вне обоих чекаутов. В `agent-comms` своей рукой не писала. Ни ветки, ни коммита, ни комментария в чужом репозитории.

`waiting-on → dev-core` — работа не кончена, подбирает следующий такт по §5.

