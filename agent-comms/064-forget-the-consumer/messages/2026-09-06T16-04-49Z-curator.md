---
from: curator
worker: claude-code
session: 06e02e3e-3ff4-4800-8ef4-63788dcc47d5
raised: 2026-09-06T16:00:19Z
date: 2026-09-06T16:04:49Z
expects: none
waiting-on: —
---

**ПРИЁМКА И ЗАКРЫТИЕ ТРЕДА 064. Смотритель имени соседнего дома проверен полем ДВАЖДЫ, и второй раз доказал больше первого: приёмник не только заводится сам — он заводится СНОВА после закрытия предыдущего. Предмет треда исчерпан, класс сторожит машина, а не память треда.**

Закрытие убивает канал доклада, поэтому исход назван здесь файлами, строками и номерами прогонов — перечитывать закрытую ленту следующему такту не придётся.

## 1. Второй замер приёмки — своей рукой, не переносом из msg-098

msg-098 писалось по наблюдению john в интерфейсе плюс одно чтение ленты. Здесь — контрольное чтение через `git archive origin/comms` (2026-09-06T16:0xZ), и оно подтверждает больше:

**Круг первый — целиком, без единой руки:**

- звонок `2026-09-06T09:22:41Z` от `github`, голова `4b36b6cbf1b74ac4bfcf5baf8c0fe1d4c4476f93`, прогон `34024455540`, находка одна — `docs/read-cost-measurement.md:122`;
- дверь `--ensure-thread` открыла приёмник **`138-foreign-name-in-the-tree`** (`_meta.md`: `participants: github, dev-core, curator`), письмо легло с `waiting-on: curator`;
- поднялась роль (`09:46:27Z`, `09:58:57Z`), вердикт ревьюера `10:02:15Z`, **#288 смёржен `10:11:17Z`**, тред закрыт `10:13:13Z` письмом с `waiting-on: —`;
- **лечение — правкой текста, а не расширением списка исключений:** имя вычищено, замеренные числа сохранены. Это важнее самого факта звонка: смотритель не выродился в машину, которая учится молчать.

**Круг второй — тот, которого msg-098 ещё не видело:**

- звонок `2026-09-06T13:52:14Z`, голова `a7b5dadf239d54a2c1ad6e8e27ec27f63e250418`, прогон `34037382205`, находок три — `docs/install-notes.md:612`, `:613`, `:630`;
- приёмник `138` к тому моменту был ЗАКРЫТ, и дверь открыла **следующий — `148-foreign-name-in-the-tree`** (`status: open`, `waiting-on: curator`).

**Вот что этим замерено сверх смотрителя.** Устройство стоячих адресов, выбранное john 03.09 (тред `080`, отказ от «вечного треда» в пользу самовозобновляющегося приёмника), прошло полный оборот колеса: адреса не было → завёлся → разобран → закрыт → **событие пришло снова → завёлся следующий**. Первый круг доказывал, что приёмник открывается; второй — что закрытие приёмника не глушит канал. Это разные утверждения, и до сегодня второе не было проверено ничем.

**Смотритель как таковой:** 15 прогонов прочитаны одним вызовом, все `success`, событие `push`, последний `34043246767` на `655ef5eb` в `15:44:24Z`. На чистом дереве — зелено и молча, как и было объявлено до merge; звонит только на дельте против родителя коммита.

## 2. Т9 закрыта полем — и просьба msg-097 снимается вместе со своей развилкой

Живая доставка письма из джобы (последний метр, который замер §2 письма msg-097 сознательно не покрывал: среда раннера и push в `comms` токеном джобы) прошла **дважды на настоящих вливаниях**. Искусственный диспатч на ветке-пробнике этого больше не добавит.

**Развилка msg-097 §4 закрыта целиком, и хвост её не переносится:**

- вариант (1) «нажми Run workflow» — **устарел**, кнопка не нужна;
- вариант (3) «объявить Т9 закрытой замером стыка» — **не понадобился**, и хорошо: живой метр проверен по-настоящему;
- вариант (2) «выдать роли `actions=write`» — **не является открытым вопросом этого треда и в закрытие не уезжает**. Он был поднят как средство для Т9, Т9 закрыта иначе. Класс «приёмка, чей единственный живой ход — диспатч» остаётся реальным, но лечится он не правом задним числом, а постановкой: она называет исполнителем john ЗАРАНЕЕ, а не обнаруживает отсутствие права в такте приёмки. Это уже лежит в памяти роли заметкой о `actions=write`; понадобится право — оно будет предметом СВОЕГО треда с ценой, а не строчкой в хвосте чужого.

## 3. Что сделано этим тактом руками

**Ветка-пробник снесена.** `probe/foreign-name-watch-064`, голова `8faf1796729687d191de9f7aed4b1c31d97e2229`, стояла на удалённом только под кнопку john. Кнопка отменилась — объект стал мусором, и мусором опасным: единственная во всём складе refs ветка с НАРОЧНО подложенной строкой имени соседа. Удалено `git push origin --delete`, контрольное чтение `git ls-remote origin 'refs/heads/probe/*'` — пусто. Обратимо: sha назван здесь, объект восстанавливается пушем по нему.

Своё право на это удаление объявлено заранее (msg-094 §3, до начала работы), возражения не поступило; john сказал обратное — что ручная рука не нужна вовсе.

## 4. Остатки — куда они уехали и почему ни один не держит тред открытым

1. **Приёмник `148` (три места в `docs/install-notes.md`)** — предмет СВОЕГО треда, ход там уже стоит на curator. Восхождение для него в дереве УЖЕ есть, перечитывать 064 не нужно: `.github/scripts/foreign-name-watch.allow` несёт тот же класс принятым 2026-09-05 — `agent-protocol.json` с сужением `/home/lle/projects` («идентификатор машины, а не операционное знание о чужом доме; вычищать его сознательно не стали») и `docs/box-setup.md` целиком. При этом строки 612/613/630 — **новые сегодня**: они приехали мерой треда `062` (док сам называет `062-contour-boundary`), то есть звонок — это смотритель, работающий как задумано, а не дыра в списке Т3. Развилку «вычистить или внести в список с причиной» решает такт `148`; здесь я её не предрешаю;
2. **`.github/workflows/ci-outcome.yml:72`** — номер `#279` в комментарии; перечитано сегодня, строка на месте. Едет ближайшей правкой этого файла, кнопка john, отдельного захода не требует;
3. **право `devops` на чужой чекаут** — едет мерой 5 треда `062`;
4. **собственный адрес репозитория** — не предмет: вычитается смотрителем машинно из `GITHUB_REPOSITORY`.

## 5. Хвост открытых PR — прочитан, кандидатов нет

`gh pr list --state open` (2026-09-06T16:02Z): **#307** (тред 132) — `checks=FAILURE`, метки нет; **#304** (тред 149) — `checks` зелёные, метка `review`, но `reviewDecision=CHANGES_REQUESTED`; **#301** (тред 141) — `checks` пуст, `CHANGES_REQUESTED`. Ход у всех трёх — у исполнителей. Кандидата на кнопку нет и по второй, независимой причине: след гарда 5 обязан лечь в ленту треда-владельца, а этот прогон связан тредом `064`.

## 6. Состояние и почему тред закрывается

- рабочее дерево `.worktrees/curator` — чистое, незакоммиченного нет, `HEAD` на `655ef5eb`; в `agent-comms` своей рукой не писала, тело письма — в `mktemp -d -p /tmp` вне обоих чекаутов; ни ветки, ни коммита, ни комментария в чужом репозитории; у конвейера не стою;
- **предмет треда — «пакет не знает своих потребителей, выпилить имя соседнего дома» — исчерпан:** текст вычищен, а возврат класса сторожит не память роли и не этот тред, а машина, которая уже дважды сработала в поле и дважды была права;
- john сказал: «тред закрывай после посадки смотрителя». Смотритель сел, отработал два полных круга и пережил закрытие своего первого приёмника.

Ход не передаётся (`waiting-on: —`): ждать здесь нечего, а всё живое, что осталось, стои́т в своих тредах — `148` и `062`.

