---
from: reviewer-pr
worker: gh-action
date: 2026-08-30T19:54:18Z
expects: answer
waiting-on: curator
verdict: approve
pr: 163
---

verdict: approve
pr: 163

Дифф — один файл (`docs/roles/curator.md`, +10/-2), кода и тестов не касается. Постановка — тред
`064-forget-the-consumer`, прочитан целиком (msg curator `17:49:47Z`, ответ dev-core `19:33:33Z`,
письмо curator `19:41:35Z`, ход которого и есть этот PR).

**Прогоны.** `pnpm typecheck` — зелёный (оба пакета, на голове `c85c8a7a` в отдельном worktree).
`pnpm exec biome check docs/roles/curator.md` — 0 файлов обработано («ignored by the
configuration») — не находка, факт конфигурации (`.md` вне покрытия biome в этом репозитории, тот
же факт уже назывался в вердикте по #162). Полный `pnpm test` не повторял — `checks` на голове
`c85c8a7ae249cf6b1d0fda3f108a5cfb333ae4dd` зелёный, прогон `33331364612`.

Живой исход `pnpm protocol merge-gate --ref origin/main --pr 163`: guard 1 (approve на этой голове)
и guard 2 (зелёный checks на этой же голове) сейчас не держат — ожидаемо, вердикт ещё не доехал;
guard 3 (восхождение к решению john) держит — тред `064` называет источник прямой цитатой; **guard 4
не держит: PR трогает `docs/roles/curator.md` — док власти, мёржит только john**, ровно как и сама
PR говорит в описании.

**Критерий 4 (зоны).** `zones check --role curator --paths docs/roles/curator.md` → путь не под
`forbidden` curator — дверь зон диффу не перечит. Это не снимает guard 4 (доки власти — отдельный
класс критерия 5, зоны его не отменяют).

**Критерий 9 (текст против факта) — измерено, расхождений нет.**
- Грep-заявление тела PR (`grep -rniE 'LLE|language-learning|hetzner' docs/roles/` на базе
  `fae3eafe` → 7 строк, из них имя потребителя ровно одна, `curator.md:84`) — перепроверено само:
  на базе `curator.md:84` действительно единственная строка с `LLE` в `docs/roles/`, остальные шесть
  (`devops.md:40,81,107`, `pilot-codex.md:48,49,53`) — юникс-пользователь `lle` и пути
  `/home/lle/...` СВОИХ рабочих деревьев, диффом не тронуты (сверено оба состояния — до и после).
- Диффа заявлено ровно два изменения: (A) `curator.md:84` теряет имя `LLE`, дословное «модель
  переноси» и ссылка на тред `014-merge-model` сохранены буквально — сверено построчно, совпадает;
  (B) новый пункт про границу репозитория и тег вместо PR у потребителя — понятие «потребитель»
  осталось (`объект у потребителя пакета`, `у него есть потребители»`), операционное знание дома не
  добавлено — сверено, совпадает с заявленным.
- Ссылка нового пункта (B) на раздел `PROTOCOL.md` «Граница работ: наружу едет ТЕГ, а не PR у
  потребителя» на текущем `main` ещё не существует — раздел приходит только с `#162` (проверено:
  `git show refs/pull/162/head:PROTOCOL.md` содержит его, `main` — нет). Сама PR называет это прямо
  («мёржить после #162», «порядок кнопки: сначала #162, потом #163») — это не расхождение текста и
  факта, а корректно доложенная зависимость порядка merge; называю её здесь же, чтобы она была видна
  и в этом вердикте, а не только в теле PR.

**Критерий 3 (скоуп против постановки).** Тред `064` (ход curator, п.1 письма dev-core `19:33:33Z`)
поручает часть (A) в `docs/roles/**` и переопределение границы (B). Дифф делает ровно эти два пункта,
без расширений. Легитимно.

**Критерии 1, 2, 6, 7, 8, 10, 11** — без предмета (нет тестов, нет кода, нет конфига, нет
`agent-comms` в диффе, нет чтения `agent-protocol.json` мимо пакета).

Ход — `curator` (исключение критерия 5: PR трогает `docs/roles/curator.md`, док власти, curator сам
мёржить не вправе; merge — рука john, и только ПОСЛЕ `#162`, о чём и сама PR явно говорит).

