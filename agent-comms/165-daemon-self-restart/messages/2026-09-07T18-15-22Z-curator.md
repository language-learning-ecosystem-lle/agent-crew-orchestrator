---
from: curator
worker: claude-code
session: c78fdd8e-0a95-46e5-a583-fc4d40dfb398
raised: 2026-09-07T18:12:44Z
date: 2026-09-07T18:15:22Z
expects: ack
waiting-on: curator
parked-on: john
---

# john, к сведению (вопроса нет, нужен ack): ящик сам вылечил дрейф кода и поднялся на новом — от merge до работающей замены 8 минут, руки не потребовалось

Отчёт `github` (msg-001 этого треда) прочитан и **сверен фактами на ящике — сошлось всё, ничего не разошлось**. Ремонта здесь нет: дрейф закрыт до моего подъёма. Ниже — что заявлено, чем это подтверждено, и одна вещь, которой в отчёте НЕ было: доказательство, что замена не просто вышла, а работает.

## Что заявлено и чем подтверждено (мои замеры, 2026-09-07T18:13–18:16Z)

| Заявлено в отчёте | Чем подтверждено |
|---|---|
| был код `3c7d2b9194b4` | `.orchestrator/daemon.log`: 48 строк «the LOADED CODE is not the ref: this process runs 3c7d2b91… origin/main on disk is 0dca1bb9 — 1 commit(s) behind» |
| стал `0dca1bb9af3f` | главный чекаут `/home/lle/projects/agent-crew-orchestrator`: `HEAD = 0dca1bb9af3fcd28f4792958dd776f2c8100e076`, ветка `main`, `status --porcelain` пуст |
| отставал на 1 коммит | та же строка журнала; дрейф с `2026-09-07T21:03:32+03:00` = момент merge #330 |
| ждал сессии 8 мин (483 с) | ждал не впустую: в окне дрейфа в журнале живая сессия `curator×155-park-has-no-checkable-ground`; в момент хода строка самоперезапуска говорит `leases 0` |
| пошёл `2026-09-07T18:11:43Z` | новая эпоха журнала `=== daemon epoch · 2026-09-07T18:11:56.001Z · pid 2436964 ===`; `systemctl --user show agent-protocol@hetzner.service`: `ExecMainStartTimestamp = Mon 2026-09-07 20:11:55 CEST` (= 18:11:55Z). 12 с между решением и поднятой заменой |

## Чего в отчёте не было — а john это и есть интересная часть

**1. Механизм: демон НИЧЕГО не порождал.** Дословно из журнала: процесс увидел, что он супервизирован (`INVOCATION_ID` выставлен), **починил дерево на месте** (`git pull --ff-only`, 8 файлов), **проверил, нужен ли `pnpm install`** (не нужен: `package.json`/`pnpm-lock.yaml`/`pnpm-workspace.yaml` в диффе `3c7d2b91..0dca1bb9` не менялись) — и **вышел кодом 75**, оставив подъём замены супервизору. Своей строкой он же называет условие, на котором это держится: супервизор БЕЗ `Restart=` оставил бы ящик ЛЕЖАЩИМ, и потому выход сделан НЕуспешным — «замена, которой не случилось, не должна выглядеть чистым выключением».

**2. Условие на этом ящике выполнено:** `Restart=on-failure` на юните `agent-protocol@hetzner.service`. Код 75 — отказ, значит замена поднимается. Это не общее свойство протокола, а свойство установки ЭТОГО ящика.

**3. Замена не просто вышла — она работает, и доказательство перед вами.** Отчёт кончается на «поднялся на новом коде»; это утверждение, а не факт. Факт: `pid 2436964` жив, чекаут на `0dca1bb9`, и **этот самый прогон curator поднят уже новым процессом** — журнал его подъёма: «workspace — curator: … already at origin/main 0dca1bb9». То есть цикл замерен целиком: merge → дрейф → ожидание сессий → починка → замена → замена делает свою работу. Прошлый разбор («смёрженное ≠ работающее», «успешный перезапуск ≠ самолечение») здесь закрыт не рассуждением, а этим прогоном.

**4. Дрейф после замены молчит.** После строки новой эпохи в журнале ноль строк «LOADED CODE is not the ref» (контроль шаблона: по всему журналу их 364, после эпохи — только эхо моих же команд).

## Что это стоило и чего не стоило

Стоило: 8 минут отставания контура (в них — 1 такт с живой сессией, которую демон честно дождался) и один перезапуск демона. **Не стоило: ни руки john, ни руки роли, ни треда с постановкой.** Звонок о дрейфе (тред 141, #330 → механизм #301) на этот ящик действительно больше не придёт — потому что звонить не о чем.

## Ход

Требуется **только ack** — «прочитано». Решения здесь нет и развилки нет: ничего не менялось, доки власти не трогались, денег не потрачено. Если ack не нужен — тред можно закрывать как приёмку самого механизма самоперезапуска: он отработал в поле от начала до конца.

Ход остаётся у curator (`john` в `waiting-on` не принимается), парковка — на john.

