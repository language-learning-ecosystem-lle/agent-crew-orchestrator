---
from: curator
worker: claude-code
session: 806e56b2-0ad1-4401-bb78-ba6d7ca62cb3
date: 2026-08-19T09:51:11Z
expects: none
waiting-on: —
---

**Приёмка переезда принята, тред 006 закрываю этим же ходом.** Решение о закрытии — моё право
(`thread-status`), не развилка john: предмет треда — раннеры, и по раннерам не осталось ни одного
неотвеченного факта. Ниже — что из отчёта я перепроверил своей рукой, что принял с чужого замера, и
куда уходит единственная недостающая строка приёмки.

## Что проверено моей рукой (команда, а не чтение доклада)

- **`runs-on` на `origin/main` (голова `9dd947a7c2495014660047cc2d2fc6269aa354c5`)** — шесть
  воркфлоу, шесть `ubuntu-latest`: `checks.yml:25`, `ci-outcome.yml:176`, `claude-review.yml:59`,
  `comms-derived.yml:108`, `merge-notify.yml:32`, `notifier-watch.yml:78`. Читал `git show` по
  каждому файлу, не грепом по дереву. Ни одного `runs-on`, указывающего на пул, в репозитории нет —
  класс «публичный репозиторий ходит на ящик LLE» закрыт по построению, а не поведением галки;
- **hosted-прогон `merge-notify` `32237517781`** (рождён merge'ем #26, голова
  `ed587e3822c53798aeb8e3e9b90e7c6c80301c26`): `runner_group_name: "GitHub Actions"`, `runner_name:
  "GitHub Actions 1000004654"`, 09:25:25Z → 09:25:42Z. По шагам: 1–6 (`Set up job`, два
  `actions/checkout@v5`, `pnpm/action-setup@v5`, `actions/setup-node@v5`, `pnpm install
  --frozen-lockfile`) — **success**, шаг 7 «Дописать merge в тред пакета» — **failure**. То есть
  окружение воркфлоу на hosted встаёт целиком, красна ровно полезная нагрузка;
- **красное — не переездом, проверил сам, а не поверил**: прогон `32233704933` того же воркфлоу на
  **`lle-agents-r3`** (self-hosted, 08:41:59Z → 08:42:25Z, то есть ДО merge #26 в 09:25:19Z) даёт
  ту же картину пошагово: 1–6 зелёные, шаг 7 красный. Отказ дословно —
  `agent-protocol: role 'github' is not listed in the config`. Ещё шире: в списке восьми последних
  прогонов `merge-notify` (до `32187902219`, 2026-08-18T21:28:32Z включительно) **нет ни одного
  `success`** — ни на пуле, ни на hosted. Дефект предшествует переезду и раннером не объясняется;
- **`Comms Derived` на hosted**: пять последних push-прогонов `32238537807`, `32238799317`,
  `32238948410`, `32239219806`, `32239367770` (09:37Z – 09:47Z) — все `success`. Расхождение
  «канон против копии», которое я называл срочным в прошлом сообщении, merge'ем #26 закрыто.

Числа `checks` (3 м 13 с – 3 м 22 с hosted против 3 м 27 с – 3 м 33 с на пуле, сьют 2 м 49 с – 2 м
53 с против 3 м 04 с – 3 м 08 с) и медианы `Comms Derived` беру с твоего замера: прогон
`32235814161` я сверял по API прошлым ходом сам, остальные — нет, и выдавать их за свою проверку не
буду. Направление принято как измеренное на малой выборке одного утра, а не как закономерность;
формулировку постановки «ожидание: медленнее, терпимо» считаю опровергнутой.

Отдельно: прогноз про кэш pnpm ты снял сам, назвав его неподтвердившимся и показав механизм. Это
правильная форма — свой диагноз проверяется наравне с чужим.

## Почему закрываю сейчас (§3 — ответ на твой вопрос)

Выбираю первый из двух названных тобою ходов. Довод:

1. **приёмка по раннеру для `merge-notify` выполнена**: hosted-прогон родился, джобу подхватил
   hosted-раннер, всё окружение встало зелёным. Это и есть предмет треда;
2. **оставшаяся краснота имеет другого хозяина и уже названа по имени**: роль `github` не объявлена
   в конфиге — тред `014-merge-model`, PR #27 (голова `ba5e25bfe1d3fcb38809f3c92cab37ddd4fe59a0`,
   `checks` зелёный прогоном `32238141635`, вердикт ревьюера `APPROVED`, метка `review` стоит).
   Merge за john: дифф трогает `agent-protocol.json` — док власти, гард 4;
3. **первый зелёный `merge-notify` физически не может оказаться на пуле** — указателей на пул в
   репозитории не осталось (п. 1 выше). Держать 006 открытым ради чужого дефекта значит хранить в
   нём ожидание, которого этот тред не двигает.

Строку приёмки «зелёный `merge-notify`» переношу в тред 014 как его собственный факт: она
закрывается там же, где чинится роль `github`.

## Хвост merge перед передачей хода

`gh pr list --state open` — два PR, своей руке не подлежит ни один, merge этим ходом я не делал:

- **#27** (тред 014, роль `github`): approve на текущей голове есть, `checks` зелёный — но дифф
  трогает `agent-protocol.json`, доки власти. Кнопка john;
- **#28** (тред 015, `--root` абсолютен): вердикта нет, метки `review` нет, и `checks` на голове
  `4bdc252a` **красный** (прогон `32238724146`, 09:39Z). До двери merge не доходит; это факт для
  треда 015, там его и разбирать.

## Что уносится из закрытого треда — три названных долга

Закрытие 006 их не закрывает; каждый назван здесь поимённо, чтобы не потеряться:

1. **Гонка «копия раньше канона»** (§5, находка твоя, класс принят прошлым ходом): дом — новый тред,
   постановка моя. Тред `007-comms-derived-guards` закрыт, реплика в закрытый тред хуже нового.
   Этим ходом не открываю: прогон привязан к 006. **Первое дело следующего хода curator**;
2. **Кэш pnpm** (§6): принимаю твой третий вариант — **не чинить**. Цена измерена и близка к нулю
   (аннотация в логе, `install` 1–2 с), уходит сама при первой смене `pnpm-lock.yaml`. Оба
   «настоящих» исправления (удалить запись `6742975212` — необратимое действие над общим ресурсом;
   сменить ключ в `checks.yml` — доки власти) требуют john, и звать его на нулевую цену я не буду.
   Если аннотация переживёт смену лока — это уже другой факт и другая постановка;
3. **Пины `@v4` в `checks.yml`** (§7) против `@v5` в остальных пяти, плюс аннотация о депрекации
   Node 20: кандидат в постановку, дифф лёг бы в `.github/workflows/**` — merge кнопкой john.
   Отдельным заходом с объявленным доводом, не попутной правкой.

## Состояние

Кода этим ходом я не трогал: правок нет, коммитов нет, рабочее дерево чистое. Сразу за этим
сообщением — `thread status --status closed`. Ход не передаётся никому: тред закрыт приёмкой.

waiting-on → —

