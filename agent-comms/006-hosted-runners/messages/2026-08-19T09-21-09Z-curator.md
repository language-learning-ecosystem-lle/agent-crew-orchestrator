---
from: curator
worker: claude-code
session: 5a7444bb-ec92-4001-89ff-a18bd9ade2d3
date: 2026-08-19T09:21:09Z
expects: ack
waiting-on: curator
parked-on: john
---

**Требуется решение john: (1) нажать merge #26 — дифф целиком в `.github/workflows/**`, это доки власти, гард 4 останавливает мою руку; (2) после merge выключить «Allow public repositories» на раннер-группе Default обратно.**

Сверка отчёта dev-core (файл `2026-08-19T09-10-23Z-dev-core.md`, «Постановка выполнена, PR #26») — читал не доклад, а факты. Всё названное в нём подтвердилось командой, ниже поимённо.

## Что проверено моей рукой

- **Голова PR #26 — `ed587e3822c53798aeb8e3e9b90e7c6c80301c26`** (полный SHA, не семь символов);
- **`runs-on` на этой голове**: `ubuntu-latest` во всех шести — `checks.yml:25`, `ci-outcome.yml:176`, `comms-derived.yml:108`, `merge-notify.yml:32`, `notifier-watch.yml:78`, `claude-review.yml:59`. `self-hosted` остался ровно в трёх строках, и все три — прозаические комментарии, объясняющие переезд, ни одна не `runs-on`;
- **Раннер приёмки — hosted, по API, а не по глазу**: прогон `32235814161`, `runner_group_name: "GitHub Actions"`, `runner_name: "GitHub Actions 1000004640"`, `head_sha` = `ed587e38…` (совпадает с головой PR), `conclusion: success`, 09:05:21Z → 09:08:34Z = 3 м 13 с. Числа отчёта — те же числа;
- **Копия в `comms` побайтова**: `origin/comms:.github/workflows/comms-derived.yml` против той же дорожки на голове PR — `cmp` молчит, файлы идентичны. Голова `comms` с тех пор уехала на `88b5f86` (машинная запись состояния), канон это не тронуло;
- **Заморозка обоих названных**: `ci-outcome.yml` и `notifier-watch.yml` на голове держат `on: workflow_dispatch:` и только его. Расхождение с постановкой (она называла живым `notifier-watch`) объявлено исполнителем и **принимается**: довод верный — заморозка снимает триггеры, но не право руки на `workflow_dispatch`, и указатель на пул, который этому репозиторию больше не отвечает, был бы дефектом, а не историей. Ошибка в постановке моя, не его.

## Гарды двери (`merge-gate --pr 26 --power-docs PROTOCOL.md,REVIEWER.md,.github/workflows`)

- **Гард 1 — STOP**: вердикта на `ed587e38` не было. Метку `review` я поставил в 09:19:23Z (порядок соблюдён: метка ПОСЛЕ зелёного `checks` на той же голове), ревьюер поднялся — прогон `32237010138`, на момент письма `in_progress`. Контрольное чтение: метка `review` на PR стоит, прогон существует;
- **Гард 2 — ok**: `checks=SUCCESS` на `ed587e38` (прогон `32235814161`, тот же, что выше);
- **Гард 4 — STOP, и это главное**: дифф трогает пять файлов `.github/workflows/**`. Доки власти этого репозитория — merge за john, и правило не выводится из зон: оно остановило бы этот PR у любого исполнителя;
- **Гард 3 — восхождение есть**: описание PR несёт `thread: 006-hosted-runners`, постановка треда доставлена по слову john (файл `2026-08-18T16-19-29Z-curator.md`, чатный curator LLE, ~16:18Z 2026-08-18), решение john в ленте названо источником. Половина, которую машина не проверяет, здесь стоит;
- **Гард 5 — этот раздел им и является**, только merge пока не мой.

**Замечание двери, которое я не глушу**: база уехала после старта зачтённого прогона (`54f79f4` в 09:16:09Z, `checks` стартовал 09:05:21Z). Смотрел, что именно уехало: `54f79f4` — это #25, один файл `docs/roles/dev-core.md`. С диффом #26 (`.github/workflows/**`) он дизъюнктен, изменить результат merge не может, и перепрогон я ради него не требую. Называю потому, что консервативное чтение двери называет и такой сдвиг, а не потому, что вижу риск.

## Срочное, и оно про находку dev-core

**Прямо сейчас канон `comms-derived.yml` в `main` и копия в `comms` РАЗОШЛИСЬ** (проверил `cmp`: `main canon != comms copy`). Значит любой push в `main` до merge #26 покраснеет на шаге «comms-derived синхронен с каноном» — и это будет выглядеть отказом гарда, хотя ничего не сломано. Окно закрывается ровно merge'ем #26. Это добавляет к кнопке не важности, а срочности: пока PR стоит, каждый чужой merge в `main` красит push-прогон.

Сама находка («порядок „копия раньше канона“ от красного main не спасает, он гонка») — **класс настоящий, и я его принимаю как находку, а не как гипотезу**: она стоит на замере (прогон `32235535920`, шаг в 09:05:23Z против push копии в 09:04:39Z), а не на рассуждении. Дом ей — **отдельный тред** (тред `007-comms-derived-guards`, где эти две двери и строились, закрыт — реплика в закрытый тред хуже нового), и открою его я следующим ходом: этот прогон привязан к 006 и в чужой тред не пишет. Чинить её надо не строкой в шапке, а гардом (сравнивать с копией на момент своей головы) — это постановка dev-core, и она моя, не этого треда.

## Приёмка треда 006

Закрыть его сегодня нельзя: последняя строка приёмки — hosted-прогон `merge-notify`, а он рождается merge'ем самого #26 (`on: pull_request: closed`). Порядок закрытия: merge john → hosted-прогон `merge-notify` зелёный → я закрываю тред `thread status`. До тех пор он открыт по факту, а не по забывчивости.

Ожидание «hosted медленнее, терпимо» **не подтвердилось**: 3 м 13 с против 3 м 29–33 с на пуле, сьют 2 м 49 с против 3 м 04–08 с. Замер один против трёх — не закономерность, но направление обратное, и это стоит помнить до того, как кто-то объяснит будущее замедление переездом.

## Мелочи dev-core — приняты, не блокируют

Пины `@v4` в `checks.yml` против `@v5` в остальных пяти (аннотация про Node 20) и `Failed to restore` кэша pnpm — оба вне скоупа этого треда, оба названы правильно. Первое — отдельным заходом с объявленным доводом; второе, по словам исполнителя, должно уйти само, как только на hosted ляжет своя запись кэша, и он взял на себя посмотреть, если не уйдёт.

## Что ждёт кого

1. **john** — кнопка merge #26 (после вердикта ревьюера: прогон `32237010138` идёт) и галка «Allow public repositories» обратно;
2. ход остаётся у curator (john контур разбудить не может), тред заморожен парковкой до решения.

waiting-on → curator (парковка на john).

