# CI публичного репозитория переезжает на GitHub-hosted раннеры (ubuntu-latest)

participants: curator · dev-core · john · status: open

## msg-001 · from: curator · 2026-08-18 · expects: answer

(пишет чатный curator LLE по слову john, ~16:18Z; постановка — его решение в чате, доставляю с контекстом)

## Постановка: все воркфлоу этого репозитория переводятся с self-hosted пула на `ubuntu-latest`

**Мотив, измеренный сегодня**: после перевода репозитория в public (12:20Z) джоба `checks` PR #10 простояла в очереди **77 минут** (15:01:57Z → 16:18:56Z) — раннер-группа организации по умолчанию не обслуживает публичные репозитории. John временно включил «Allow public repositories» (16:18Z, очередь ожила), но целевое решение — hosted:

1. **Изоляция**: публичный репозиторий на self-hosted железе = потенциально чужой код (fork-PR) на ящике рядом с LLE-контуром и секретами; GitHub прямо не рекомендует self-hosted для public. Hosted снимает класс риска целиком;
2. **Деньги**: для public-репозиториев GitHub-hosted раннеры бесплатны без лимита минут;
3. **Смысл**: CI generic-пакета на голой ubuntu доказывает установку «у чужого» — ровно то, чем пакет теперь является.

## Скоуп

- `runs-on: [self-hosted, linux, x64]` → `ubuntu-latest` во ВСЕХ живых воркфлоу репозитория (`checks.yml`, `comms-derived.yml`, `merge-notify.yml`, `notifier-watch.yml`; замороженные — по месту, замер исполнителя). Симметрично откатывает #2 — тот делался на приватном репозитории, публичность легитимно меняет ответ;
- проверить пригодность окружения: node/pnpm ставятся шагами (setup-node), кэш pnpm store через actions/cache — замер исполнителя, что уже есть в воркфлоу, а чего не хватает;
- **особое внимание process-тестам** (спавны демонов, тайминги): hosted-железо слабее netcup (шареные 4 vCPU) — если флак проснётся, таймауты калибруются по месту, НЕ ослаблением утверждений;
- `comms`-копия `comms-derived.yml` — той же волной, побайтово (иначе шаг сверки красный);
- после merge: john выключает «Allow public repositories» на группе Default обратно (галка была временной — сказать ему в отчёте явным пунктом).

## Приёмка

Зелёный прогон каждого затронутого воркфлоу НА hosted-раннере (у джобы в логе runner name `GitHub Actions N`, не `netcup-rN`), включая один полный `checks` с тестами; время прогона назвать числом рядом с прежним self-hosted (ожидание: медленнее, терпимо).

## Граница

Раннеры организации остаются как есть — их потребитель LLE (приватный). Это развод по типу репозитория, не демонтаж.

waiting-on → dev-core.

## msg-002 · from: curator · 2026-08-18 · expects: answer

**Сброс исчерпания — эта доставка обнуляет счёт (3/3).** Диагноз трёх провалов снят по журналу демона: все — **API 529 Overloaded** (перегрузка сервера Anthropic, 10 ретраев, выход без передачи хода; квота цела, rate_limit allowed). Причина внешняя и временная — постановка msg `16-19-29Z` в силе без единой правки.

Уточнение контекста с момента постановки: main уехал до `be61c627` (#11–#19 за вечер) — воркфлоу переводи от свежей головы; `docs/install-notes.md` теперь существует (#14) — строку про исход CI там держи в уме при описании приёмки.

Приступай: `runs-on: ubuntu-latest` во всех живых воркфлоу + копия `comms-derived.yml` в `comms` побайтово той же волной.

waiting-on → dev-core.

## msg-003 · from: curator · 2026-08-19 · expects: answer

доставка из треда `013-exhausted-visibility` (решение john ~08:40Z, msg `2026-08-19T08-42-27Z-curator`) — эта пара стоит замороженной, и вот команда, которой она поднимается. Предмет треда 006 я этим ходом НЕ трогаю: прогон привязан к 013.

## Факт: пара `dev-core×006-hosted-runners` заморожена потолком

Снято кадром `orchestrator status` (мой прогон, 2026-08-19T08:48Z, ничего не тронуто):

```
dev-core · 006-hosted-runners · released · attempt 3/3 · deadline 2026-08-18T17:57:05Z
  · (exited-without-handoff)  ⚠ EXHAUSTED (substantive — the circuit will not raise it,
  a hand does (--max-attempts above the ceiling))
```

Три попытки 2026-08-18 умерли на API 529 (перегрузка вендора). Класс — `substantive`, потому что их журналы старше поля `external` (консервативное чтение, не ошибка классификатора): авто-оттаивания у этого класса нет.

**Письмо в этот тред пару НЕ размораживает** — это установленный факт кода треда 013, а не догадка: счётчик обнуляет `cur.attempt = 0` в `lease.ts` только под `isDelivery || isSelfTurnDelivery`, то есть на СОБЫТИИ ЖУРНАЛА ЭТОЙ ПАРЫ, а все три формы такого события пишет её собственный прогон. Мои письма 21:35Z 2026-08-18 счёт и не сняли. Изнутри контура выхода нет: `planLaunch` отказывает исчерпанной паре (`reason: exhausted`) ДО того, как прогон начнётся.

## Команда для руки на hetzner

С `/home/lle/projects/agent-crew-orchestrator` (`pnpm protocol` = `tsx packages/agent-protocol/src/cli.ts`):

```bash
pnpm protocol orchestrator run --ref origin/main \
  --role dev-core --thread 006-hosted-runners \
  --max-attempts 4 -d --write
```

- `--max-attempts 4` пропускает РОВНО ОДИН прогон (пара стоит на 3/3). Потолок 3 в конфиге не трогается: число живёт только в этой команде;
- `--write` — то, что делает запуск настоящим; `-d` кладёт супервизор в фон (иначе прогон висит на терминале весь срок аренды, и обрыв ssh его роняет);
- **цена: это запуск сессии** — реальные деньги и квота, не чтение. Одна проба, не открытая дверь;
- дошёл до передачи хода — handoff обнулит счётчик сам, и дальше пара живёт по обычным правилам. Умер снова молча — станет 4/4, и следующая рука пишет `--max-attempts 5`;
- роль должна быть СВОБОДНА в этот момент: кадр `status` печатает это строкой `parallelism: N of M role(s) live` / `free:` — на 08:48Z было `free: none`.

john просил начать с этой пары: за ней PR раннеров, стоящий дольше всех (queue 1/9, ждёт с 2026-08-18T16:19:29Z).

waiting-on → dev-core (как и стоял: рука снимает потолок, ход в треде не двигается).

## msg-004 · from: dev-core · 2026-08-19 · expects: answer

Постановка выполнена, PR #26 открыт и **уже зелёный на hosted** — прогон дождался, числа приёмки ниже. Голова `ed587e3`.

## Сделано

`runs-on: [self-hosted, linux, x64]` → `ubuntu-latest` во **всех шести** воркфлоу репозитория (PR #26, один коммит). Комментарии, обосновывавшие пул ящика, переписаны тем же коммитом: они были частью двери, а не украшением, и оставленные как есть врали бы про живой файл.

| воркфлоу | было | стало | состояние |
| --- | --- | --- | --- |
| `checks.yml` | self-hosted | `ubuntu-latest` | жив |
| `comms-derived.yml` | self-hosted | `ubuntu-latest` | жив (+ копия в `comms`) |
| `merge-notify.yml` | self-hosted | `ubuntu-latest` | жив |
| `claude-review.yml` | `ubuntu-latest` | без изменений | жив (#21) |
| `notifier-watch.yml` | self-hosted | `ubuntu-latest` | **заморожен** |
| `ci-outcome.yml` | self-hosted | `ubuntu-latest` | **заморожен** |

## Приёмка: числа

**`checks` на hosted — зелёный**, прогон `32235814161`, runner `GitHub Actions 1000004640` (группа `GitHub Actions`, не `netcup-rN`/`lle-agents-rN`), джоба **3 мин 13 с** (09:05:21Z → 09:08:34Z), из них `pnpm test` — **2 мин 49 с**. Полный сьют, включая треть, поднимающую настоящий git и спавнящую процессы.

**`Comms Derived` на hosted — зелёный**, прогон `32235756828` на пуше копии в `comms`, runner `GitHub Actions 1000004638`, **19 с**. Он исполняется из версии файла В ЭТОЙ ВЕТКЕ, то есть проверена ровно новая строка, а не канон.

Рядом — тот же `checks` на пуле того же утра (джоба целиком / `pnpm test`):

| прогон | раннер | джоба | `pnpm test` |
| --- | --- | --- | --- |
| `32235310795` | `lle-agents-r1` | 3 м 29 с | 3 м 04 с |
| `32234736997` | `lle-agents-r2` | 3 м 31 с | 3 м 05 с |
| `32230985716` | `lle-agents-r3` | 3 м 33 с | 3 м 08 с |
| **`32235814161`** | **hosted** | **3 м 13 с** | **2 м 49 с** |

**Ожидание постановки («медленнее, терпимо») не подтвердилось**: hosted оказался на ≈16–20 с БЫСТРЕЕ пула по джобе и на ≈15–19 с по сьюту. Это замер трёх self-hosted прогонов против одного hosted, а не установленная закономерность — но направление обратное ожидаемому, и это стоит знать до того, как кто-то будет объяснять будущее замедление переездом. Флак в process-тестах не проснулся; калибровать таймауты не понадобилось, утверждения не тронуты.

Что НЕ доказано прогоном и почему: `merge-notify` бьёт на `pull_request: closed`, его hosted-прогон родится merge'ем самого #26. `notifier-watch` и `ci-outcome` заморожены до `workflow_dispatch` — единственный способ их запустить дописал бы в почту постороннее сообщение, и ради галочки я этого делать не стал.

## Расхождение с постановкой — объявляю, а не молчу

Постановка называла живыми четверых, среди них `notifier-watch.yml`. По факту на голове `93c32ed` он **заморожен** (`on: workflow_dispatch`), а не названный в постановке `ci-outcome.yml` — тоже заморожен. Тронул обоих, довод: заморозка сняла триггеры, но НЕ убрала `workflow_dispatch`. Оставленный там `self-hosted` означает, что единственный оставшийся запуск (рука, знающая, что делает) встаёт в очередь навсегда и молча — раннер-группа этому репозиторию больше не отвечает. «Не поддерживается» — про поведение, а не про право указателю протухнуть.

## Пригодность окружения — проверена, правок не потребовала

Все шесть ставят тулчейн шагами: `pnpm/action-setup` + `actions/setup-node` с `node-version-file` (`.nvmrc` → 24.18.0) и `cache: pnpm`. Кэш pnpm-стора через `actions/cache` УЖЕ ЕСТЬ — он внутри `setup-node`; отдельный шаг добавлять не надо, и я не добавлял. Ящико-локального в шагах нет ничего.

Строки `dest: ${{ runner.tool_cache }}/setup-pnpm` **оставил** и переобъяснил: на hosted они инертны (свежая машина на джобу, общего `$HOME` нет), но удалить их значило бы, что возврат на любой пул с общим `$HOME` открывает измеренный класс (тред 049, #153) заново и молча. Откат переезда — одна строка `runs-on`; помнить две хуже, чем одну.

## Копия в `comms`

`.github/workflows/comms-derived.yml` положен в ветку `comms` побайтово коммитом `0d0ae63` ДО открытия PR — из временного клона, рабочее место роли и чекаут почты не тронуты. Файл не почтовый, порядок — тот, что предписан шапкой канона.

## НАХОДКА: порядок «копия раньше канона» от красного main не спасает, он гонка

**Факт.** Прогон `32235535920` — push в `main` (merge #25, старт 09:01:53Z) — **красный** на шаге «comms-derived синхронен с каноном». Шаг исполнился в 09:05:23Z, мой push копии в `comms` был в 09:04:39Z: сьют шёл 3,5 минуты, и к моменту сравнения ветка `comms` уже уехала вперёд.

**Вывод (мой, не замер).** Шапка канона обещает, что укладка копии ПЕРЕД merge'ем канона спасает push-прогон main от красноты. На деле она спасает лишь тогда, когда в окне «копия легла → канон влит» в main ничего не приезжает, а сравнение на push безусловно по построению. Окно у нас открыто прямо сейчас и закроется merge'ем #26; всё, что влетит в main до него, покраснеет тем же шагом и по той же причине — по коду это будет выглядеть как отказ гарда, хотя ничего не сломано.

Класс настоящий, но он НЕ этого треда и чинится не строкой: гард на push мог бы, например, сравнивать с копией на момент СВОЕЙ головы, а не с текущей `comms`. Дом находки — твой выбор, отдельным тредом или репликой в 007.

## Мелочи, которые видел и не чинил (скоуп)

- `checks.yml` держит `actions/checkout@v4`, `setup-node@v4`, `pnpm/action-setup@v4`, тогда как остальные пять уже на `@v5`. Прогон вешает аннотацию про депрекацию Node 20. Не трогал: это не про раннеры, и подтягивать пины стоит отдельным заходом с объявленным доводом;
- первый hosted-прогон вывесил `Failed to restore: "/usr/bin/tar" failed with exit code 2` — восстановление кэша pnpm, записанного раннерами ящика. Джоба зелёная, `install` занял 1 с. Ожидаю, что аннотация уйдёт, как только на hosted ляжет своя запись кэша; если не уйдёт через несколько прогонов — это уже дефект, и я на него посмотрю.

## Пункт john (тот, что просили назвать явно)

**После merge #26 — выключить «Allow public repositories» на раннер-группе Default обратно.** Галка включалась 2026-08-18 16:18Z как временная мера против 77-минутной очереди; с этим PR публичный репозиторий на пул больше не ходит, и держать её открытой — значит держать открытой ровно ту дверь, ради закрытия которой всё и делалось. Раннеры организации при этом остаются как есть: их потребитель — приватный LLE, это развод по типу репозитория, а не демонтаж.

## Состояние

Рабочее дерево чистое, всё закоммичено и запушено. Жду merge #26 (кнопка john) — голова `ed587e3`, `checks` зелёный. Копия в `comms` уже на месте, так что merge не требует ничего дополнительного; после него родится hosted-прогон `merge-notify`, и это последняя недостающая строка приёмки.
