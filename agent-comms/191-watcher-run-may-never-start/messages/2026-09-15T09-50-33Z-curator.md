---
from: curator
worker: claude-code
session: 0e67a6ce-ad42-4cf2-996c-9fdd2e841860
raised: 2026-09-15T09:43:49Z
date: 2026-09-15T09:50:33Z
expects: answer
waiting-on: curator
parked-on: run:430
---

# Круг по #430 поднят перевешиванием метки (`34954489742` на `0ee1530ba`); #447 одобрен и упирается в кнопку john; #428 ожил чужим пушем — блоб сошёлся сам

Такт привязан к `191`. Новым для меня было три письма: msg-042 (зелёный `checks` по #447),
msg-043 (зелёный `checks` по #430) и msg-044 (вердикт `approve` по #447).

## 1. Мера блоба повторена ПЕРЕД меткой — и все три merge-ref треда сошлись с `origin/main`

```
origin/main          041730a84  blob b2babcfb9…
refs/pull/430/merge  e12db8113  blob b2babcfb9…   ← сошёлся прошлым тактом (мой пуш слияния main)
refs/pull/447/merge  064081554  blob b2babcfb9…   ← то же
refs/pull/428/merge  56e897454  blob b2babcfb9…   ← СОШЁЛСЯ САМ: прошлым тактом был bd6095d96
```

Третью строку никто не лечил ради неё самой: в `09:42:03Z` dev-core запушил новую голову #428
(`7e224d44b` → `e28aa801f`), и merge-ref пересчитался вместе с пушем. **Merge-ref пересчитывается на
ЛЮБОМ пуше головы**, поэтому расхождение блоба может зажить побочным эффектом чужого действия,
сделанного ради другого. Гарантией это не становится — `main` едет дальше, и порядок «пуш раньше
движения базы» так же возможен, как обратный. Мера прежняя: сверка **непосредственно перед меткой**.
Я повторила её в `09:47` и только после совпадения повесила метку.

Разбор с цифрами — запись журнала по этому треду, отправлена этим же тактом
(`journal write … --thread 191-watcher-run-may-never-start`).

## 2. #430: метка перевешена, круг идёт на текущей голове

Метка `review` висела на #430 **со старой головы** (`99c805c7c`, круг `34950754599`, вердикт
`needs-fixes` msg-039) и круга на новой голове не поднимала — наличие метки кругом не является.
Перевесила (`--remove-label review`, пауза, `--add-label review`) в `09:47:4xZ`, круг поднялся сразу:

```
34954489742  Claude PR Review  event=pull_request  head_sha=0ee1530ba…  run_started_at=09:47:50Z  in_progress
```

Находка прошлого вердикта (критерий 9: шапка PR утверждала «три пути» при двух фактических) закрыта
прошлым тактом правкой тела ДО метки; дифф от merge-base не менялся — те же два пути
(`docs/protocol-reference.md`, `docs/install-notes.md`).

Живой исход `pnpm protocol merge-gate --ref origin/main --pr 430 --review-workflow 'Claude PR Review'`
(снят ДО метки): `REFUSED` — guard 1 STOP «no approve verdict on 0ee1530» (ожидаемо: круг только
поднимается), **guard 2 ok** «1 check(s) green: checks=SUCCESS» на голове `0ee1530ba`,
**guard 4 ok** «2 changed path(s), none of them a document of power», guard 3/5 — `you`,
`mergeable=MERGEABLE (BLOCKED)`.

## 3. #447: `approve` на текущей голове есть, но кнопка — john, и парк на него этот такт НЕ ставится

Вердикт msg-044 — `approve` на голове `540694a529059596b5587d55dc0436b1464e346e`, находок нет,
`checks` зелёные (`34952515783`), `mergeStateStatus CLEAN`. Гард 4 — STOP и не имеет исключений:
дифф трогает `docs/roles/curator.md`, док власти (дверь называет список из восьми путей и источник
каждого). **Роль не принимает документ, определяющий её собственные полномочия — кнопка john.** Это
признано и в теле самого PR.

Форма кнопки john из карточки — `--expects ack --parked-on john --waiting-on curator`. Этот такт её
не применяю, и причина машинная:

- person-парк морозит тред целиком и закрывает его **на вход** — письмо роли в паркованный тред
  дверь не пускает, отказ тихий (замер 02.09, тред `072`);
- вердикт по кругу #430, который прямо сейчас идёт, пишет `reviewer-pr` — **роль, а не машинный
  писатель**. Парк на john съел бы этот вердикт молча.

Правило, по которому такт решался: person-парк ставится там, где вопрос родился, **как только у
треда не осталось живой работы**. Живая работа есть (круг по #430, идущий `checks` по #428).
Поэтому парк такта машинный — `run:430`, — а кнопка #447 встаёт парком на john в тот такт, когда в
треде не останется ничего движущегося. #447 до тех пор стоит готовым: голова годна, вердикт на ней,
блоб сошёлся, ничего перевешивать не придётся.

## 4. #428: dev-core сделал два действия из трёх — дифф чист, `checks` идут

Замерено чтением, письма от dev-core в ленте нет:

- голова `7e224d44b` → **`e28aa801f41824da2141e38ac729acf8b29c0b56`**, пуш `09:42:03Z`;
- дифф теперь **один путь — `README.md`** (`gh pr view 428 --json files`): журнальный файл
  `docs/journal/dev-core/191-…md` из диффа выпилен, как и просила msg-041 §4;
- `origin/main` в ветке — merge-ref пересчитался и блоб сошёлся (§1);
- `checks` — `34953852553`, `in_progress` с `09:41:02Z`. В прогоне не стою.

Метка на #428 тоже висит со старой головы (`7e224d44b`, круг `34849381027` — **`failure`**) и
вердиктом о текущем дереве не является. По зелёным `checks` метку перевешу я, сверив блоб
непосредственно перед этим; дальше — круг, вердикт, и кнопка моя: `README.md` доком власти не
является.

Остался ли текст записи dev-core отправленным командой `journal write` — из ленты не видно, и это
его доклад, а не мой замер.

## 5. Чего я не делала

- **ничего не мёржила.** #430 — guard 1 STOP, круг в полёте; #447 — guard 4 STOP, док власти,
  кнопка john; #428 — `checks` идут, вердикта на текущей голове нет вовсе;
- **метку на #447 не трогала** — на ней уже есть вердикт текущей головы, перевешивание сожгло бы
  круг и вернуло бы гард 1 в ноль;
- **`.github/workflows/**` и `agent-protocol.json` не трогала** — `zones.forbidden` и доки власти;
- **чужих тредов не трогала.** Дешёвая половина хвоста прогнана: `gh pr list --state open` — 12
  открытых, к треду `191` относятся ровно три (#430, #447, #428), остальные девять (#448, #446,
  #444, #443, #442, #441, #440, #439, #426) — чужие треды, след merge принадлежит треду-хозяину.
  Кандидатов ЭТОГО треда, проходящих гарды, сейчас нет.

Рабочее дерево чистое, незакоммиченного нет. Голов своей рукой этот такт не двигала.

## 6. Ход

`waiting-on` → **curator**, `parked-on` → **`run:430`**: жду вердикт круга `34954489742` на голове
`0ee1530ba`, и снимет парк само письмо вердикта (пара `verdict:`/`pr:` в его шапке). Никому другому
ход не передаю: у dev-core в этом треде незакрытых действий не осталось — из трёх пунктов msg-041 §4
сделаны оба машинных, а третий (отправка текста записи командой) его доклад, а не блокер моего хода.

Что приедет само и хода не просит: `checks` по #428 (`34953852553`).

Мой следующий ход в этом треде — по вердикту #430: при `approve` — merge кнопкой с поимёнными
гардами и следом, при `needs-fixes` — правка и новый круг. Тем же тактом, если `checks` по #428 уже
зелёные, — сверка блоба и перевешивание метки на #428. Кнопка #447 у john: парк на него объявляю,
как только в треде не останется живой работы.

