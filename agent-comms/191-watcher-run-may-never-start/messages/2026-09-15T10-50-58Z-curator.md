---
from: curator
worker: claude-code
session: 55752d06-dad8-4168-895b-8742b78ce78e
raised: 2026-09-15T10:46:49Z
date: 2026-09-15T10:50:58Z
expects: none
waiting-on: —
---

**Приёмка треда `191`. Закрываю его этим же тактом — предмет исполнен целиком, живой работы и открытых PR у треда нет.**

Ход вернулся ко мне без непрочитанного (`--for curator`: «unread for curator: none — the last message is curator's own letter of 2026-09-15T10:42:10Z»), то есть после моего же письма о нажатой кнопке #447. Такт потратила на приёмку, а не на новую работу.

## 1. Исход назван ФАЙЛАМИ И СТРОКАМИ — читала `main`, а не письма о мёржах

Закрытие треда убивает его канал, поэтому исход тут, а не «см. выше». Мерила `git show origin/main:<путь>` с грепом по САМОМУ обещанию: письмо `github` о мёрже говорит, что кнопка нажата, и молчит о том, что приземлилось.

Решение john (чат 2026-09-13 ~10:51Z, письмо `2026-09-13T10-51-43Z-curator.md` в этой ленте) — **ветвь Б: записать предел, поясов не строить**. Записан он теперь в ЧЕТЫРЁХ местах, и все четыре прочитаны в `main` сегодня:

| место | чем приземлилось | что прочитано |
|---|---|---|
| `.github/workflows/notifier-watch.yml`, шапка, стр. 56–59 — **первоисточник** | PR #413, коммит `e55976b5a`, кнопка john | «отказ наблюдаемого → письмо, **ЕСЛИ GITHUB ПОСТАВИЛ ПРОГОН СМОТРИТЕЛЯ НА ИСПОЛНЕНИЕ**. Прогон смотрителя может не начаться НИКОГДА, и тогда не отказывает ничто: `if` не вычисляется, джоба не заводится, ни один шаг не исполняется» |
| `README.md`, строка 31, таблица почтовых воркфлоу | PR #428, коммит `7dd53c74c`, `+1 −1` | «**жив** — отказ любого из четырёх уведомителей звонит письмом, **ЕСЛИ GitHub поставил прогон смотрителя на исполнение**: гарантия условна… `queued` не попадает ни в отказ, ни в тишину» |
| `docs/install-notes.md` §2, стр. 103–108 — третье место обещания | PR #430, коммит `49653f41f`, `+5 −1` | «Закрыт он, однако, **НЕ ЦЕЛИКОМ**, и вторую половину надо читать здесь же: событие `workflow_run` доезжает до письма, только ЕСЛИ GitHub поставил прогон смотрителя на исполнение» |
| `docs/protocol-reference.md`, раздел «Предел гарантии смотрителя» — **ссылка, не копия** | тот же #430, `+22 −0` | «Эта запись ничего не устанавливает — она адресует… Читать надо ТАМ, и правится он тем же диффом, что и сам воркфлоу» |

Поясов (лестницы смотрителей, поллинга, внешнего пинга) не построено ни одного — ровно как сказала ветвь Б.

**Побочный продукт треда, переживающий его закрытие:** PR #447 (`docs/roles/curator.md`, хунк `@@ -274,6 +274,26 @@`, коммит `e1796651e`, кнопка john 2026-09-15T10:40:38Z) — перед меткой `review` блоб `.github/workflows/claude-review.yml` в `refs/pull/<N>/merge` сверяется с `origin/main`; не совпал — метки не вешать до движения головы. Правило стои́т в карточке, то есть грузится в промпт каждым подъёмом роли, и закрытием треда не гасится.

## 2. Хвост: своих кандидатов у треда нет

`gh pr list --state open` → четыре открытых PR: **#448** (тред 198), **#442** (204), **#440** (202), **#426** (180). **К треду `191` не относится ни один** — #430 и #447 смёржены, больше PR у треда нет. Чужие треды своей рукой отсюда не трогаю: след merge принадлежит треду-хозяину, а этот прогон привязан к одному треду.

## 3. Запись роли

`journal write … --thread 191-watcher-run-may-never-start --write` — §6 «Приёмка треда: закрытие меряется ЧТЕНИЕМ `main`, а не письмами о мёржах». Контрольное чтение после записи: файл в `origin/comms` 295 строк, §6 с 263-й. Там же поправка к §3 той записи: «три места» — это три места ОБЕЩАНИЯ, а справочник — четвёртое место и он АДРЕС; и практическое следствие для будущей приёмки такого класса — **греп гнать по обоим шаблонам** («ЕСЛИ GitHub поставил» и «не начаться НИКОГДА»): по одному только первому `docs/protocol-reference.md` не находится вовсе, и погашенное место можно счесть непогашенным.

## 4. Чего я НЕ делала

- **новых PR не открывала и чужих не трогала** — предмет исполнен, поводов нет;
- **`.github/workflows/**` и `agent-protocol.json` не касалась** — `zones.forbidden`;
- **заметок памяти с поводом `191` не гашу — их нет**: проверено грепом по фронтматтеру (`metadata.thread`) перед закрытием, ни одна моя заметка этот тред поводом не объявляла, так что закрытие не обнуляет ничего.

Рабочее дерево чистое, незакоммиченного нет.

## 5. Ход

`waiting-on` → **—**: тред закрыт приёмкой (`thread status --status closed --write`, моё право `thread-status`), и ждать в нём некого. Понадобится продолжение — оно заводится своим тредом либо переоткрытием этого.

