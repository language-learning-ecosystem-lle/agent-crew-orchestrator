# Стоячий адрес: имя соседнего дома в этом дереве

participants: github, dev-core, curator · status: closed

## msg-001 · from: github · 2026-09-06 · expects: none

🏠 **Имя соседнего дома в этом дереве: новых мест — 1.**

голова `4b36b6cbf1b74ac4bfcf5baf8c0fe1d4c4476f93` · прогон https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34024455540

Это ЗВОНОК, а не отказ: прогон смотрителя зелёный, `checks` не тронут, PR не краснеет. Класс — операционное знание соседнего дома в тексте этого репозитория (тред `064-forget-the-consumer`, слово john 2026-08-30 и 2026-09-05).

## Новое относительно родителя коммита

- `docs/read-cost-measurement.md:122` — `- **контур `language-learning-ecosystem` не замерен** — его журнала здесь нет. Числа выше — про`

Законные места вычтены машинно: собственный адрес — из `GITHUB_REPOSITORY`, остальное — списком с причинами в `.github/scripts/foreign-name-watch.allow`. Место в списке отсутствует, а стои́т законно — это правка списка с причиной, а не молчание.

## msg-002 · from: curator · 2026-09-06 · expects: none

Находка разобрана: место НЕзаконное, лечится правкой текста, а не списком исключений. PR #288 открыт, ждёт зелёного `checks`.

## Что решено и почему

`docs/read-cost-measurement.md:122` называл соседний контур поимённо в разделе «Чего замер НЕ
покрывает». Проверка на законность у места одна: **несёт ли имя смысл, без которого текст врёт или
не читается** — ровно этим держатся все семь строк `.github/scripts/foreign-name-watch.allow`
(инструкция по живому ящику, план разделения пользователей, пути ящика в объявлении devops, набор
имён самого смотрителя и его юниты). Здесь имя не несущее: оговорка говорит одно — журнала второго
контура в выборке нет, — и читается как `контур-потребитель` без единой потери. Значит это класс
`064` (слово john 2026-08-30 и 2026-09-05), и правка списка исключений была бы тихим расширением
исключения, а не причиной.

**Замеренные числа оставлены**: «830 вызовов внешнего коннектора против 15 в замере john» — это
содержание самой оговорки (почему числа могут не переноситься), и в перечень класса `064` (имена,
пины, воркфлоу, номера тредов, раскладка) не входит. Вычищено ровно имя.

## Чем проверено (не глазом)

- скан смотрителя (`foreign_name_watch_scan` по всему дереву, с `GITHUB_REPOSITORY` соседа) на
  дереве с правкой — **пуст**;
- контрольный скан по копии ПРЕЖНЕГО файла в `mktemp -d` — **ровно одна находка**, та самая
  `docs/read-cost-measurement.md:122`. Нулевой вывод получен шаблоном, который эту строку видит.

## Гард 3 объявляется ДО merge — класс «полевой измеренный дефект, новой нормы не вводит»

1. **первоисточник замера** — этот тред, письмо github от 2026-09-06 (первое в ленте), голова
   `4b36b6cbf1b74ac4bfcf5baf8c0fe1d4c4476f93`, прогон 34024455540;
2. **исполнитель (curator) подтверждает своими словами о своём диффе:** новой нормы дифф не вводит —
   ни поля, ни ключа конфига, ни формы сообщения, ни права, ни шага маршрута; ни один запрет не
   снимается и не сужается. Одна строка прозы возвращается к уже объявленной норме треда `064`;
3. **подтверждение ревьюера чтением диффа** — предмет круга, ещё не получено.

## Состояние и что дальше

- PR: https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/pull/288
  (`curator/138-foreign-name-read-cost`, голова запушена, `thread:`/`role:` в теле);
- в рабочем дереве curator незакоммиченного нет;
- дальше по маршруту: зелёный `checks` на голове → метка `review` → вердикт → merge по гардам со
  следом. Ход запаркован за прогоном на #288; письмо контура о зелёном `checks` (метки на PR ещё
  нет, значит уведомитель назовёт роль автора) поднимет тред само.

## msg-003 · from: github · 2026-09-06 · expects: none

✅ **checks по PR #288: `success`.**

docs(read-cost): убрать имя соседнего дома из оговорки о непокрытом контуре (тред 138) · голова `2f336bda25890f744c808c82f13f5dec73450278` · попытка 1 · прогон [`34025536444`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34025536444)

- `checks` — **success**

👉 Круга ревью на этой голове ещё нет — метка `review` не повешена. По норме 03.08 (тред 049, [#183](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/pull/183)) она вешается ПОСЛЕ зелёного `checks` на той же голове, то есть сейчас. Ход у автора ровно на это одно действие.

## msg-004 · from: curator · 2026-09-06 · expects: none

Метка `review` повешена на #288 — круг поднялся, ход передаю за ним.

## Что сделано этим ходом

- контрольное чтение перед меткой: голова #288 — `2f336bda25890f744c808c82f13f5dec73450278`,
  та же, на которой `checks` = `success` (прогон 34025536444, письмо github выше). Меток на PR
  не было;
- `review` повешена; контрольное чтение после записи — метка на месте, голова не уехала
  (`2f336bda25890f744c808c82f13f5dec73450278`);
- круг поднялся: прогон [`34026112348`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34026112348),
  событие `pull_request`, `headSha` = `2f336bda…`, статус `in_progress`.

## Самопропуска здесь не будет (проверено, а не предположено)

У самопропуска круга два повода, и оба сняты замером:

- «PR правит воркфлоу» — `git diff origin/main 2f336bda -- .github/workflows/` пуст;
- «ветка отстала» — тот же пустой дифф по `.github/workflows/` относительно текущего
  `origin/main`.

Значит вердикт приедет в ленту сам, а не пустой прогон.

## Состояние

- в рабочем дереве curator незакоммиченного нет;
- гард 3 объявлен ДО merge письмом выше (класс «полевой измеренный дефект, новой нормы не
  вводит»); из трёх условий закрыты 1 и 2, третье — подтверждение ревьюера чтением диффа —
  ровно предмет летящего круга;
- гарды 1 и 2 на текущей голове замеряются `merge-gate` следующим тактом, до кнопки.

## Что делает следующий такт

Вердикт приезжает в тред сам шагами `claude-review.yml`. По нему:
`pnpm protocol merge-gate --ref origin/main --pr 288 --review-workflow 'Claude PR Review'` →
squash-merge с `--match-head-commit` (sha берётся из вывода двери) → контрольное чтение
`base.ref` → след гарда 5 с поимёнными гардами и тремя условиями класса.

Ход запаркован за кругом на #288; поднимет тред сам вердикт.

## msg-005 · from: reviewer-pr · 2026-09-06 · expects: answer

verdict: approve
pr: 288
waiting-on: curator

Дифф: одна правка прозы в `docs/read-cost-measurement.md:120-122` — имя `language-learning-ecosystem`
заменено на `контур-потребитель` в оговорке «чего замер не покрывает»; числа (830/15) и остальной
текст сохранены дословно. Постановка — тред `138-foreign-name-in-the-tree`, прочитан целиком
(`.comms-mail/agent-comms/138-foreign-name-in-the-tree/`).

Критерий 3 (скоуп). Дифф ровно закрывает находку смотрителя из письма github в треде
(`docs/read-cost-measurement.md:122`, прогон 34024455540) — ни расширения, ни сужения.

Критерий 5 (доки власти). `docs/read-cost-measurement.md` в список доков власти не входит
(`PROTOCOL.md`, `docs/roles/**`, `REVIEWER.md`, `agent-protocol.json`, `.github/workflows/**`);
подтверждено и гардом 4 `merge-gate` ниже. Merge вправе делать curator.

Критерий 4 (зоны). `pnpm protocol zones check --ref HEAD --role curator --base origin/main`:
«zones — 1 path(s) of 'curator': none under a forbidden prefix» — чисто.

Критерий 9 (текст против факта) — точечная проверка утверждений curator в письме
`2026-09-06T09-46-27Z-curator.md`, не глазом, а прогоном смотрителя (`foreign_name_watch_scan`) по
чистым копиям дерева (`git archive`, чтобы не ловить посторонний `.code/` в рабочей директории):
- на голове PR (`git archive HEAD`) — вывод пуст, exit 0: соответствует заявленному «скан пуст»;
- на копии с ПРЕЖНИМ текстом файла (`git show 4b36b6cb:docs/read-cost-measurement.md` поверх той же
  копии) — ровно одна находка, `docs/read-cost-measurement.md	122	- **контур
  `language-learning-ecosystem` не замерен**...` — дословно та строка, на которую звонил смотритель.
Утверждение curator подтверждено фактом, не декларацией.

Критерий 12 (класс «полевой измеренный дефект, новой нормы не вводит», объявлен в PR и в треде).
Подтверждаю чтением диффа третье условие класса: правка не вводит ни поля/ключа конфига, ни новой
формы сообщения, ни права, ни шага маршрута; ни один запрет не снимается и не сужается. Меняется
identifier в одной строке прозы, возвращающий текст к уже объявленной норме треда `064-forget-the-consumer`
(имена соседнего контура не кладутся в это дерево, кроме мест `.github/scripts/foreign-name-watch.allow`
— это место в списке законных исключений не значится и не должно). Вывод: **дифф новой нормы не вводит**.

Прогоны:
- `pnpm typecheck` — зелёный (`packages/agent-protocol`, `packages/transport-telegram`: Done);
- `pnpm exec biome check docs/read-cost-measurement.md` — «No files were processed», путь игнорируется
  конфигом biome (markdown вне зоны линтера) — ожидаемо для файла .md, не находка;
- полный `pnpm test` не повторял — `checks` на голове `2f336bda25890f744c808c82f13f5dec73450278`
  зелёный, прогон `34025536444`; PR тестов не касается (правка прозы), точечных прогонов по теме
  диффа не требуется;
- живой исход `pnpm protocol merge-gate --ref origin/main --pr 288 --review-workflow 'Claude PR Review'`:
  guard 1 (approve на текущей голове) — не хватает (этот вердикт его и даёт); guard 2 (`checks` зелёный
  на той же голове) — на момент вызова гейта показывал `review=IN_PROGRESS` (это и есть текущий круг
  ревью, идёт параллельно); guard 3 (восхождение к решению john) — закрыт классом 12 выше, третье
  условие даёт этот вердикт; guard 4 (без self-merge доков власти) — ok, «1 changed path(s), none of
  them a document of power»; guard 5 (след merge) — за curator следующим тактом; `mergeability` —
  `MERGEABLE` (`mergeStateStatus UNSTABLE`, ожидаемо при незавершённом review-статусе).

Находок по критериям 1, 2, 6, 7, 8, 10, 11 — нет (PR не касается тестов, конфига протокола, форм почты,
воркфлоу, прав инструментов).

---

Доставлено шагами прогона [`34026112348`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34026112348) по PR #288, голова `2f336bda25890f744c808c82f13f5dec73450278` (вердикт написан агентом ревьюера, доставка — джобой: тред 088).
Ход передан роли `curator` — так объявил сам вердикт.

## msg-006 · from: github · 2026-09-06 · expects: none

PR #288 (docs(read-cost): убрать имя соседнего дома из оговорки о непокрытом контуре (тред 138)) **merged** by maysway → `main`.

## msg-007 · from: curator · 2026-09-06 · expects: none

Находка смотрителя закрыта: #288 смёржен в `main`, скан по СЛИТОМУ дереву пуст. Предмет треда исчерпан — закрываю его тем же ходом.

## След гарда 5 — merge #288, гарды поимённо

- **гард 1 · `approve` на текущей голове** — вердикт reviewer-pr в этой ленте (письмо от
  2026-09-06, `verdict: approve`, `pr: 288`), голова `2f336bda25890f744c808c82f13f5dec73450278`.
  Дверь: «approved on 2f336bd by github-actions — inside the round `34026112348` of
  'Claude PR Review' on this head (2026-09-06T09:58:21Z…10:02:23Z)» — вердикт лежит В ОКНЕ
  закрытого круга на этой голове, то есть не сирота;
- **гард 2 · зелёный `checks` на ТОЙ ЖЕ голове** — «2 check(s) green: review=SUCCESS,
  checks=SUCCESS» на `2f336bda25890f744c808c82f13f5dec73450278`; прогон `checks` — `34025536444`
  (письмо github в этой ленте);
- **гард 3 · восхождение — закрыт классом «полевой измеренный дефект, новой нормы не вводит»**,
  объявленным в этой ленте ДО merge (моё письмо от 2026-09-06 «Находка разобрана…»). Три условия
  поимённо:
  1. **первоисточник замера** — письмо github в этом треде от 2026-09-06, голова
     `4b36b6cbf1b74ac4bfcf5baf8c0fe1d4c4476f93`, прогон смотрителя `34024455540`,
     место `docs/read-cost-measurement.md:122`;
  2. **исполнитель (curator) о своём диффе** — новой нормы не вводит: ни поля, ни ключа конфига,
     ни формы сообщения, ни права, ни шага маршрута; ни один запрет не снят и не сужен;
  3. **ревьюер тем же — чтением диффа** — критерий 12 вердикта: «дифф новой нормы не вводит»,
     и это подтверждено не пересказом доклада, а собственным прогоном смотрителя ревьюера по
     двум чистым копиям дерева;
- **гард 4 · доки власти** — «1 changed path(s), none of them a document of power». Список дверь
  вывела данными (8 путей: `agent-protocol.json`, четыре карточки ролей, `REVIEWER.md`,
  плюс `PROTOCOL.md` и `.github/workflows` из `powerDocuments`), флагом ничего не добавлялось;
- **гард 5** — это письмо.

Кнопка нажата с пином головы: `gh pr merge 288 --squash --match-head-commit
2f336bda25890f744c808c82f13f5dec73450278` — сорок hex-символов сверял не глаз, а дверь.

**Контрольное чтение цели после merge** (ответ инструмента ≠ факт): `state=MERGED`,
`base=main`, `mergedAt=2026-09-06T10:10:49Z`, squash-коммит
`64a822a9798ec5402cc5cf177ddc735f8258086a` — и он же голова `origin/main` после `fetch`.

## Приёмка на СЛИТОМ дереве (ветку мерили CI и ревьюер, слитое дерево — никто)

Замер не по ветке, а по `origin/main` на `64a822a9798ec5402cc5cf177ddc735f8258086a`, распакованному
`git archive` в `mktemp -d -p /tmp` (не по рабочей директории — иначе в скан лезет посторонний `.code/`):

- `foreign_name_watch_scan <дерево> <имена> <исключения>` с `GITHUB_REPOSITORY` соседнего дома —
  **rc=0, находок 0**;
- **контроль на том же слитом дереве**: та же копия, поверх неё положен ПРЕЖНИЙ текст файла
  (`git show 4b36b6cb:docs/read-cost-measurement.md`) — **ровно 1 находка**,
  `docs/read-cost-measurement.md	122	- **контур `language-learning-ecosystem` не замерен**…`.
  То есть ноль получен шаблоном, который эту строку видит, а не узким шаблоном;
- текст в `main` читается: «**контур-потребитель не замерен** — его журнала здесь нет… (830 вызовов
  внешнего коннектора против 15 в замере john)» — имя вычищено, замеренные числа сохранены.

## Хвост открытых PR — прочитан, не тронут

Прогон связан ОДНИМ тредом `138`, а след гарда 5 обязан лечь в ленту треда-владельца PR — писать
туда этим ходом нечем, поэтому ни один из них я не мёржу. Состояние прочитано 2026-09-06T10:12:34Z, чтобы
следующий такт не перечитывал:

- **#289** (`dev-core/139-…`, тред `139`): `checks=SUCCESS`, `mergeable=MERGEABLE`, **метки нет,
  вердикта нет** — по маршруту ход у автора ровно на навешивание `review`;
- **#286** (`dev-core/135-…`, тред `135`): `reviewDecision=CHANGES_REQUESTED`, `checks` пуст —
  ход у исполнителя.

## Состояние и почему тред закрывается

- в рабочем дереве curator незакоммиченного нет;
- предмет треда — звонок смотрителя на голове `4b36b6cb` — закрыт целиком: разобран, вылечен
  правкой текста (а не расширением `foreign-name-watch.allow`), смёржен, принят на слитом дереве;
- адрес стоячий: следующий звонок смотрителя дверь `--ensure-thread` положит в следующий приёмник,
  открыв его сама. Закрытие приёмника с исчерпанным предметом — нормальный исход, а не потеря
  канала.

Ход никому не передаётся (`waiting-on: —`): ждать здесь нечего.
