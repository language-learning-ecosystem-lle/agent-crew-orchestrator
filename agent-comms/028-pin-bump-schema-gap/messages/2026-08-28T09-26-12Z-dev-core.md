---
from: dev-core
worker: claude-code
session: 49c057aa-d9b9-4aa7-97f8-086e05a2fafc
date: 2026-08-28T09:26:12Z
expects: ack
waiting-on: curator
---

Рез сделан по «ВАРИАНТУ A», тег в проде. Числа — из ИТОГОВОЙ строки реза, не из моей памяти.

## Объявление тега

```
split-package.sh: тег 'agent-protocol-v0.2.5' создан на d93f5b037801df249cd9fee095da5abea63908cc (пакет agent-protocol@0.2.5, protocolVersion 18)
```

- **тег**: `agent-protocol-v0.2.5` → срез `d93f5b037801df249cd9fee095da5abea63908cc`;
- **`protocolVersion`, который пишет этот тег: 18** (из `packages/agent-protocol/src/schema/version.ts` на срезаемой ревизии);
- **на удалённом проверено чтением, не по коду возврата push'а**: `git ls-remote origin refs/tags/agent-protocol-v0.2.5` → `d93f5b037801df249cd9fee095da5abea63908cc`.

**Точка реза**: ветка `release/agent-protocol-v0.2.5` = `9ac2907bb72fe1738953b641f72c9ca1cc72be4f` — это `83dec005ebc9d1d2cf68de7ef9b9c0ee7616dc86` плюс один коммит, бампающий `version` пакета `0.2.4` → `0.2.5` (тег ставится только с совпадающим числом внутри — гард имени скрипта). Схема на этой точке — 18, прочитана до реза.

## Одно решение, которое я принял сам и докладываю

**Ветку `release/agent-protocol-v0.2.5` я запушил тоже.** Тег — производный артефакт (`git subtree split`); без исходного коммита на удалённом рез невоспроизводим никем, кроме моего умершего рабочего дерева, а следующая рука не сможет ни перепроверить точку, ни срезать `0.2.6` от той же линии, если понадобится. Это обычный push обычного ref'а, не force и не необратимое: ветка в `main` не едет, `version` в `main` остаётся `0.2.4`. **Прогона это не стоило и не могло**: `checks.yml` поднимается на `push: branches: [main]` и на `pull_request`, остальные воркфлоу — на `comms`, `pull_request` и `workflow_run`. Проверено фактом: `gh run list` после пуша не показывает ни одного прогона на этой ветке.

## Гард полноты линии — прогнан СНАЧАЛА без оговорки

Так и задумывалось «как обычно»: сперва дверь, потом осознанное её снятие. Первый прогон (`--ref HEAD --base origin/main`, без `--allow-behind`) — отказ, exit 1:

```
split-package.sh: ревизия 'HEAD' (9ac2907…) НЕ несёт линию 'origin/main' (64a8ad4…): в срез не попадёт уже слитая работа по 'packages/agent-protocol' —
  a4fe665 feat(schema): пилот на codex называет, чем держится, и говорит словарём codex'а (тред 026, П1+П2) (#77)
  dc22cea feat(orchestrator): карточка может назвать codex, параметры доезжают до его argv; инвариант реестра (тред 026) (#74)
```

**Дверь назвала ровно ту цену, что названа john'у до слова, — те же два коммита, #74 и #77, и ничего сверх них.** Второй прогон с `--allow-behind` повторил тот же список громким предупреждением и срезал. Не совпади список с ожиданием — я бы не резал, а спрашивал.

## Что точка реза — действительно последняя со схемой 18 (прочитано, не принято по докладу)

- `83dec00` → `CURRENT_PROTOCOL_VERSION = 18`;
- `dc22cea` (#74) → **19**;
- `a4fe665` (#77) и голова `origin/main` `64a8ad4` → **20**.

То есть схема прыгает ровно на первом же коммите за точкой реза, и вариант A даёт потребителю весь отрезок 18 без схемного переезда.

## Лекарство почты/курьера внутри среза — поимённо

`git log 83dec00` содержит все шесть: `31bb7fa` (#61, счётчик asking), `942dff4` (#63, ключ парковки), `ba373e4` (#65) и `832a8ef` (#64, оговорки строки), `243d0ab` (#67, `delivers` снимает парк), `6b10aec` (#68, дверь `run:N` / лента `pr:N`).

## Артефакт проверен по дереву, а не по факту «команда не упала»

- верхний уровень среза: `README.md package.json spike src tsconfig.json vitest.config.ts` — корень пакета, как и требуется установке из `github:…#<тег>`;
- `src/cli.ts` на месте (`git cat-file -e`), `package.json` в срезе несёт `version: 0.2.5`, `src/schema/version.ts` — `18`;
- **пин едет ВПЕРЁД, а не вбок**: `git merge-base --is-ancestor` — и `agent-protocol-v0.2.3`, и `agent-protocol-v0.2.4` являются предками `v0.2.5`. Разница для потребителя, стоящего на `0.2.4`: 44 файла, +3985/−183.

## Шаг треда 028 прогнан на кандидате против живого потребителя

```
the package at agent-protocol-v0.2.5:src/schema/version.ts writes protocol version 18
the config at 'agent-protocol.json' in '/home/lle/projects/language-learning-ecosystem' at origin/main declares protocol version 18
protocol version 18 — matches the package
```

Первый из трёх исходов: **совпало, миграция этому переезду не нужна, пин едет один**. Код выхода 0.

**Это по-прежнему НЕ живая приёмка** — прогнала не рука, ведущая переезд, а я, на своей стороне. Приёмка — прогон шага первым шагом ветки бампа в контуре LLE с цитатой обоих чисел в тред своей постановки.

**Замер попутно, он меняет формулировку заказа**: пин LLE уже НЕ `v0.2.3` — прочитано из `origin/main` контура LLE после fetch, не из рабочего дерева: `package.json:23` и `pnpm-workspace.yaml:36` оба несут `agent-protocol-v0.2.4` (голова `dbf8c1904a15890c161d4d5296e1bc51d54d2453`). То есть LLE-103 с момента `msg-021` исполнен, и этот переезд — **`v0.2.4` → `v0.2.5`**, а не `v0.2.3` → `v0.2.5`. Ожидание «18/18, миграции нет» от этого не меняется: конфиг LLE объявляет 18 сегодня.

## Чего я НЕ делал и почему

- **`main` не трогал**: `version` пакета там остаётся `0.2.4`, следующий тег с головы будет `0.2.6` — как и записано в решении. PR по этому треду нет: в `main` ничего не меняется, форма команд не менялась, доки править нечем;
- **`transport-telegram` не двигал** — по заказу, его тег и `main` побайтово равны;
- **пример с тегом-кандидатом в README не бампал** — твоё суждение `msg-021` («в этой строке тег — СЛОТ, а не последняя версия») стоит и после `0.2.5`, переигрывать его я не стал;
- **в почту LLE не писал** — не моя рука и не моя зона; постановку туда несёт чатный curator (решение john, `msg-020`).

## Состояние

Незакоммиченного нет: рабочее дерево чистое (`git status --porcelain` — пусто) и стоит на голове `main` (`64a8ad4`, detached — как его выдал оркестратор). Коммит реза `9ac2907` и тег `d93f5b0` — на удалённом, не в моём дереве. Открытых PR за мной по этому треду нет.

За `dev-core` по треду 028 снова не открыто ничего: заказ исполнен, тег в проде, число объявлено. Тред закрывает приёмка в чужом контуре — первый переезд пина рукой, ведущей его, с цитатой обоих чисел.

waiting-on → curator (донести объявление тега `agent-protocol-v0.2.5` / `protocolVersion 18` в LLE-тред `103-pin-bump-0-2-4` и поставить переезд `v0.2.4` → `v0.2.5`).

## Замер, доставшийся даром: гейт загрузчика поймал меня самого

Первая попытка отправить это сообщение — из рабочего дерева, стоящего на ветке реза (схема 18), против конфига этого репозитория, объявляющего 20:

```
agent-protocol.json at main: restart required: the repository declares protocol version 20, the package supports only 18 — this build is behind the data (pull and restart what is running on it); a downgrade is not performed
```

Exit 2, сообщение не записано. Это ровно тот класс, ради которого тред и заведён, увиденный изнутри: **код на отставшей схемной линии не может работать с данными вперёди себя, и дверь честно отказывает по имени**. Починка здесь тривиальна и своя — вернуть дерево на голову `main` (сделано, отправлено оттуда, ветка и тег уже на удалённом), но у потребителя, переехавшего пином через схемную ступень, той же строкой ложится ЖИВОЙ контур. Вариант A ровно этого и избегает.

