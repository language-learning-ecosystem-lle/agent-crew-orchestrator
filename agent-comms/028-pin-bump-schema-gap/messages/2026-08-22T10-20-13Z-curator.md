---
from: curator
worker: claude-code
session: 046d7e17-91cb-4609-aec0-c4a4deebb23f
date: 2026-08-22T10:20:13Z
expects: ack
waiting-on: curator
parked-on: john
---

**Требуется решение john, одной строкой: тег `agent-protocol-v0.2.4` срезан и в проде, оба числа сверены (`18` / `18`) — от приёмки треда 028 остался ровно один шаг, и он в ЧУЖОМ контуре: бамп пина LLE `v0.2.3` → `v0.2.4` PR-маршрутом контура LLE. Из этого контура в почту LLE я не пишу — поставить эту задачу там можешь только ты (или сказать «ставит curator LLE сам»).** Ниже — доставка, след merge `#60` с гардами поимённо и состояние.

## Доставка `0.2.4` — сделана, проверена чтением

- **`#60` смёржен мной** squash'ем в `10:15:18Z`, коммит `8fc65d21c915fdf67e91b5f6ad27a5439cd1c580`, цель прочитана ПОСЛЕ merge (`base.ref = main`);
- **рез моей рукой из слитой `main`** (`8fc65d21`): `scripts/split-package.sh --tag agent-protocol-v0.2.4 --push`. Дословно из вывода реза:

```
split-package.sh: protocolVersion этот тег пишет: 18 (из 'packages/agent-protocol/src/schema/version.ts')
split-package.sh: тег 'agent-protocol-v0.2.4' создан на a5e261a43eb92d2b2e7b11a9256597e61dd05153 (пакет agent-protocol@0.2.4, protocolVersion 18)
split-package.sh: перед переездом пина сверь оба числа: agent-protocol schema version --package-ref 'agent-protocol-v0.2.4' --repo <репозиторий-потребитель> --ref main
```

Это ровно то, ради чего тред заводился: **число схемы приехало в объявление тега само, а не было найдено рукой в исходниках.** Тег на удалённом проверен не по коду возврата команды, а чтением: `git ls-remote --tags origin agent-protocol-v0.2.4` → `a5e261a43eb92d2b2e7b11a9256597e61dd05153`;
- **`main` зелёный на голове доставки**: прогон `checks` `32567135206` на `8fc65d21c915fdf67e91b5f6ad27a5439cd1c580` — `completed / success`, `10:15:20Z…10:19:02Z`, дочитан мной в `10:19Z`.

## Шаг сверки прогнан на кандидате `v0.2.4` против живого LLE

Не фикстура: `--repo /home/lle/projects/language-learning-ecosystem`, конфиг по `main`.

```
the package at agent-protocol-v0.2.4:src/schema/version.ts writes protocol version 18
the config at 'agent-protocol.json' in '/home/lle/projects/language-learning-ecosystem' at main declares protocol version 18
protocol version 18 — matches the package
```

Исход — первый из трёх: **совпало, миграция этому переезду не нужна**, пин едет один. Это НЕ живая приёмка (шаг прогнала я, а не рука, ведущая переезд), но это готовый ответ для той ветки: она обязана прогнать его у себя и процитировать оба числа — исход уже известен и должен совпасть.

Пин LLE сегодня стоит в ДВУХ местах, оба на `v0.2.3` — проверено чтением: `package.json:23` и `pnpm-workspace.yaml:36` (`overrides` перебивает `package.json`, правка одного места роняет контур). Слово john из `msg-010` это и называет: «правка ОБОИХ мест пина; честный install; лок».

## След merge `#60` — гарды поимённо (гард 5)

- **гард 1** — `approve` на голове `e46b55830563f81c076c1b9c2529e097568c8315`, автор `github-actions`, `10:14:41Z`, **внутри закрытого успешного круга `Claude PR Review` на ЭТОЙ голове**: прогон `32566970037`, окно `10:11:41Z…10:14:51Z` (якорь по прогону, тред `027`). Дверь сказала это сама: `ok guard 1 · approved on e46b558 by github-actions — inside the round 32566970037`;
- **гард 2** — на той же голове зелены оба: `checks` (прогон `32566781878`, `success`, `10:07:27Z…10:11:13Z`) и `review`. Метку `review` вешала я и ПОСЛЕ зелёного `checks` на этой голове (`10:11:40Z`), голову перед этим перечитала — та же `e46b5583`, ни коммита после прогона;
- **гард 3** — решение john в ленте ЭТОГО треда, `msg-010`, пункт 2: «доставка `0.2.3 → 0.2.4` — ДА». Постановка `msg-013` — моя, но восхождение идёт к слову john, а не к ней;
- **гард 4** — 2 пути, ни одного дока власти (дверь судила шестью: конфиг, три карточки-производные, `PROTOCOL.md`, `.github/workflows` — объявлением конфига);
- **гард 5** — это сообщение. Строка двери прогнана целиком, с флагом воркфлоу: `pnpm protocol merge-gate --ref origin/main --pr 60 --review-workflow 'Claude PR Review'` → «nothing in the facts forbids this merge — guards 3 and 5 are yours to answer».

## Почему вопрос, а не действие

Норма из `msg-010` уже назвала МАРШРУТ бампа (PR контура LLE, готовит dev-роль, мержит curator LLE) — но не назвала, **кто заводит эту задачу в почте LLE**. Я curator ЭТОГО репозитория: писать в чужую почту не моя рука и не моя зона, а сам собой бамп не поедет — у готового тега события нет ровно так же, как у готового к merge PR. Поэтому паркуюсь на тебе: скажешь «ставлю сам» / «пусть ставит curator LLE» — этого достаточно, дальше приёмка треда закроется первым же прогоном шага в той ветке.

## Состояние

- открытых PR по треду нет (`gh pr list --state open` — пусто на `10:19Z`), `main` зелёный на `8fc65d21`, незакоммиченного у меня нет, рабочее дерево чистое на `origin/main`;
- **тред НЕ закрываю**: приёмка неполна по собственной постановке — живая приёмка = первый настоящий переезд пина PR-маршрутом контура LLE с цитатой обоих чисел в треде постановки бампа. Ни мой прогон, ни прогон `dev-core` её не заменяют;
- по коду и прозе этого треда за `dev-core` и за мной больше ничего не открыто.

waiting-on → curator (вопрос несу я).

