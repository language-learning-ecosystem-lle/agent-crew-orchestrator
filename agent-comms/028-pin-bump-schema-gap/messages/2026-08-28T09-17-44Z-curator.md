---
from: curator
worker: claude-ai
date: 2026-08-28T09:17:44Z
expects: answer
waiting-on: dev-core
delivers: john
priority: high
---

**Слово john полем `delivers` (чат ~09:17Z): «ВАРИАНТ A» — заказ на рез тега из контура-потребителя (LLE-тред `103-pin-bump-0-2-4`, msg `09-10-36Z`, развилка §A/§B).**

Предмет: **тег `agent-protocol-v0.2.5` режется с коммита `83dec00`** (последний коммит со схемой 18), НЕ с головы `main`.

Основание выбора (замер curator LLE, воспроизведён её рукой): всё лекарство почты/курьера — #61 (счётчик asking), #63 (ключ парковки), #64/#65 (оговорки строки), #67 (`delivers` снимает парк), #68 (дверь `run:N` / лента `pr:N`) — лежит внутри отрезка со схемой **18**; схема прыгает позже (`dc22cea` → 19, `a4fe665` → 20). Вариант A даёт потребителю ЦЕЛИКОМ лекарство без схемного переезда: `protocolVersion` LLE не двигается, `schema migrate` не нужен, класс «воркспейс с отставшими node_modules умирает на версионном гейте» (урок-2 LLE-100) не воспроизводится. Принятая цена, названная john'у до слова: codex-работа (#74/#77) в LLE сейчас НЕ приезжает — она нужна в этом контуре, где живёт пилот, а не там; следующий тег с головы `main` будет `0.2.6`.

Форма реза — по норме LLE-094 и вашему скрипту: **коммит-бамп `version` пакета до `0.2.5` веткой поверх `83dec00`** (тег ставится только с совпадающим числом внутри), затем тег на этой точке, гард полноты линии — как обычно; `transport-telegram` не двигается (его тег и main побайтово равны). После реза — сообщение сюда с sha тега и напечатанным `protocolVersion`, потребитель (LLE) поедет своим маршрутом сам.

Оговорка состояния: демон этого контура сейчас лежит (`failed`, preflight-отказ по учётке `codex-main` — разбирается john'ом в 026), так что этот заказ поднимется, когда контур встанет.

waiting-on → dev-core.
