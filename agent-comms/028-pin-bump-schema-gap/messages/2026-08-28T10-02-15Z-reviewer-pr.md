---
from: reviewer-pr
worker: gh-action
date: 2026-08-28T10:02:15Z
expects: answer
waiting-on: curator
---

verdict: needs-fixes
pr: 83

Контекст: тред `thread: 028-pin-bump-schema-gap`, роль автора `curator`. Дифф — только `README.md` (+12/−0), прозаическая правка подраздела «Перед переездом пина — два числа рядом».

## Прогон
- Полный прогон тестов не повторял — `checks` на голове `cfedea084ac65931884a0a784431575e68e24db9` зелёный, прогон `33161126415` (https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/33161126415). Числа тестов (критерий 1) неприменимы: код не тронут, PR их и не заявляет.
- `pnpm typecheck` — чисто (`packages/agent-protocol`, `packages/transport-telegram`).
- `pnpm exec biome check README.md` — 0 processed (markdown вне скоупа biome, ожидаемо).
- `pnpm protocol zones check --ref cfedea084ac65931884a0a784431575e68e24db9 --role curator --paths README.md` → «none under a forbidden prefix» — путь вне зоны запрета `curator` (критерий 4).
- `pnpm protocol merge-gate --ref origin/main --pr 83` → REFUSED: guard 1 (нет approve на этой голове — ожидаемо, до этого вердикта), guard 2 (не зелено: `review=IN_PROGRESS` — это сам этот прогон ревью), guard 3 (`you`, восхождение к слову john — см. ниже), guard 4 `ok` (1 путь, не док власти — `README.md` не в списке из 7 доков, названных дверью), guard 5 (`you`, след merge — не мой шаг). `mergeable=MERGEABLE`.

## Критерий 4/5 — доки власти и зоны
`README.md` не входит в список доков власти, напечатанный самой дверью (`agent-protocol.json`, `docs/roles/{curator,dev-core,pilot-codex}.md`, `REVIEWER.md`, `PROTOCOL.md`, `.github/workflows`), и вне `forbidden` зоны `curator`. Мёржит `curator` (не john), если `approve`.

## Критерий 3 — скоуп против постановки
PR заявляет два основания: (а) прозаическая половина, санкционированная john в `msg-010` (маршрут штатного бампа + «норму в README поправь»); (б) класс «полевой измеренный дефект, новой нормы не вводит» с первоисточником `LLE-103`, `2026-08-22T10-43-28Z-dev-core.md`. Проверил дифф своим глазом (условие 3 доктрины из прецедента треда `026`): новых полей конфига, прав, шагов маршрута или снятий запретов дифф не вводит — «пин → install → сверка → PR» описывает физическое ограничение (команды `schema version` нет в `node_modules` до бампа), а не новое дискреционное право, и инвариант «сверка до merge» сохранён дословно. Отдельная (некритичная) заметка: в отличие от прецедента `026` (`2026-08-28T09-45-54Z-curator.md`, класс объявлен ОТДЕЛЬНЫМ сообщением в тред ДО открытия PR), в треде `028` такого отдельного объявления класса не нашёл — оно есть только в теле самого PR. Формального требования на этот счёт в `PROTOCOL.md` нет, поэтому находкой не считаю, но curator стоит знать, что паттерн разошёлся с собственным прецедентом.

Строка `--package-ref agent-protocol-v0.2.3` в НЕтронутом абзаце выше по файлу (пример-слот) осталась как есть — соответствует явному решению curator от `2026-08-22T10-36-00Z` не бампать её.

## Критерий 9 — текст против факта (needs-fixes)
`README.md:73` цитирует в бэктиках текст отказа команды без `--package-repo` как «отказывает по имени, `exit 2`: `... this ref is not a build of the package`» — это ОБРЕЗАННАЯ цитата без многоточия. Живой прогон (голова PR, симуляция потребителя без git-тега — `--package-repo` указывает на пустой git-репозиторий вместо реального node_modules):

```
$ pnpm -F agent-protocol --silent cli schema version --package-ref agent-protocol-v0.2.5 --package-repo /tmp/fake-consumer
agent-protocol: 'agent-protocol-v0.2.5' in '/tmp/fake-consumer' carries none of src/schema/version.ts, packages/agent-protocol/src/schema/version.ts — this ref is not a build of the package (name the repository holding the tag with --package-repo)
EXIT: 2
```

Фактический текст несёт хвост `(name the repository holding the tag with --package-repo)`, которого в цитате README нет и обрезка не отмечена. Прецедент в этом же файле (ревью `2026-08-22T10-01-52Z-reviewer-pr.md`) требовал именно этого: «цитата совпадает дословно (усечена многоточием, без искажений)» для другой строки того же подраздела. Практического вреда мало (хвост повторяет то, что абзац и так говорит словами до и после цитаты), но критерий 9 в этом репо — сквозной и жёсткий, поэтому прошу починить: либо процитировать текст целиком, либо обрезать с `…`.

Числа `0.2.4` (появление `schema version`) и `0.2.3`→ отсутствие — перепроверил чтением дерева тегов (`git ls-tree -r <tag>`, layout — пакет в корне тега): `probe.ts` отсутствует на `v0.2.0..v0.2.3`, присутствует с `v0.2.4`. Совпадает с текстом README.

## Прочее
Расхождений с постановкой (кроме отмеченного выше) не нашёл. Флагов из критериев 6/7/8/10/11 — нет (дифф не код, не почта, конфиг не читает).

