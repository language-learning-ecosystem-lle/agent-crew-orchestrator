---
from: curator
worker: claude-code
session: 9e5fa1ac-9d59-4123-ae6c-62fdf219f5d6
date: 2026-08-28T10:13:31Z
expects: ack
waiting-on: curator
parked-on: john
---

**Требуется решение john, одной строкой: тег `agent-protocol-v0.2.5` (срез `d93f5b03`, `protocolVersion 18`) готов и перепроверен мной — донести его объявление в LLE-тред `103-pin-bump-0-2-4` и поставить там переезд пина `v0.2.4` → `v0.2.5` может только чатный curator (твоё же решение, `msg-020`: в чужую почту пишет стыковая роль). Скажи «несу сам» или «ставит curator LLE» — больше по треду 028 в этом контуре не открыто ничего.**

Ниже: приёмка треда 028 — **состоялась, и я нашла её чтением чужой ленты, а не докладом**; перепроверка реза `dev-core`; замеренный дефект в моей же прозе и его починка (`#83`, смёржен, гарды поимённо).

## Приёмка треда 028 закрыта фактом — он лежал в LLE-103 с 2026-08-22, и в эту ленту его никто не донёс

Приёмка формулировалась так (`msg-003`, уточнение `msg-010`): «первый настоящий переезд пина PR-маршрутом контура LLE, шаг сверки первым шагом ветки, цитата обоих чисел в тред своей постановки». **Это произошло.** Прочитано мной в файлах почты LLE по `origin/comms` (не в рабочем дереве, не по чьему-либо пересказу):

- сообщение `agent-comms/103-pin-bump-0-2-4/messages/2026-08-22T10-43-28Z-dev-core.md`, раздел «Шаг 1 — сверка, оба числа цитатой»:

```
the package at agent-protocol-v0.2.4:src/schema/version.ts writes protocol version 18
the config at 'agent-protocol.json' in '.' at main declares protocol version 18
protocol version 18 — matches the package
```

  Прогнала его **роль, готовящая бамп**, первым шагом своей ветки — ровно тот, кто назван нормой;
- переезд состоялся PR-маршрутом: LLE `#374`, голова `760dc7e5`, squash-merge `a00a51fd0048a753ad7bbc9cafa09ab13ca8c8c7`, `mergedAt 11:07:42Z` — след гардов в `2026-08-22T11-12-33Z-curator.md` того же треда;
- сегодняшнее состояние пина подтверждает исполнение независимо: `package.json:23` и `pnpm-workspace.yaml:36` контура LLE (`origin/main`, голова `dbf8c190`) оба несут `agent-protocol-v0.2.4`; `protocolVersion` конфига LLE — `18`.

Строка о live-прогоне, обещанная в `msg-020` и запрошенная curator LLE (её `2026-08-22T11-31-25Z-curator.md`: «028 не поднимется сам ни одним тактом»), в эту ленту так и не приехала — **и это ровно тот класс, который тред уже знает: у готового факта в чужом контуре нет события.** Записываю замер: тред простоял с приёмкой в кармане шесть дней, потому что доставка факта была рукой, а не событием.

**Тред всё же не закрываю этим тактом** — по одной причине: внутри него живёт заказ john «ВАРИАНТ A» (`msg-022`), исполненный `dev-core` наполовину доставки — тег срезан, но объявление до потребителя не донесено. Закрою следующим тактом по слову из первой строки.

## Рез `dev-core` перепроверен моей рукой — всё держится, ни одного расхождения

Ни один факт не взят из `msg-023`; всё снято заново между `09:48Z` и `09:52Z`:

- **тег на удалённом**: `git ls-remote origin refs/tags/agent-protocol-v0.2.5` → `d93f5b037801df249cd9fee095da5abea63908cc`; ветка реза `release/agent-protocol-v0.2.5` → `9ac2907bb72fe1738953b641f72c9ca1cc72be4f`, и она — `83dec00` плюс ровно один коммит бампа `version`;
- **содержимое тега** (чтением git-объектов, не установкой): верх среза — `README.md package.json spike src tsconfig.json vitest.config.ts`; `package.json` → `"version": "0.2.5"`; `src/schema/version.ts` → `CURRENT_PROTOCOL_VERSION = 18`; `src/cli.ts` на месте;
- **точка реза действительно последняя со схемой 18**: `83dec00` → 18, `dc22cea` (#74) → **19**, `a4fe665` (#77) и голова `origin/main` `64a8ad4` → **20**. Схема прыгает на первом же коммите за точкой — вариант A даёт потребителю весь отрезок 18 без схемного переезда, как и обещано john'у до слова;
- **пин едет вперёд, а не вбок**: `git merge-base --is-ancestor` — и `v0.2.3`, и `v0.2.4` являются предками `v0.2.5`;
- **лекарство почты/курьера внутри среза, поимённо**: `31bb7fa` (#61), `942dff4` (#63), `ba373e4` (#65), `832a8ef` (#64), `243d0ab` (#67), `6b10aec` (#68) — все шесть предки `83dec00`, проверено `merge-base --is-ancestor` по каждому;
- **шаг сверки на кандидате против живого LLE** (`--repo /home/lle/projects/language-learning-ecosystem --ref origin/main`): `18` … `18` … `matches the package`, exit 0. Первый из трёх исходов: **миграция этому переезду не нужна, пин едет один**. Приёмкой это не является и не заявляется — прогнала не рука, ведущая переезд.

## Замеренный дефект в МОЕЙ прозе, найденный в той же ленте LLE-103 — и починенный

Тот же первый переезд назвал вещь, которой в норме не было, и она про мою половину (б): **строка ритуала у потребителя не исполняется вовсе.** Слова готовившей роли — «постановочная строка не исполняется вовсе»; место записи она явно оставила мне («проза CLAUDE.md / aco-028 / нигде — твоё»), и в `README.md` этого репозитория оно не было записано ничем.

**Воспроизвела сама, а не поверила докладу** (2026-08-28, тег `v0.2.5` против живого LLE):

```
agent-protocol: 'agent-protocol-v0.2.5' in '/home/lle/projects/language-learning-ecosystem' carries none of src/schema/version.ts, packages/agent-protocol/src/schema/version.ts — this ref is not a build of the package (name the repository holding the tag with --package-repo)
exit=2
```

Причина структурная: тег живёт в репозитории ПАКЕТА, а в `node_modules` потребителя git'а нет вовсе. Второй факт того же замера — порядок: `schema version` приехала в `0.2.4`, поэтому у контура, запиненного раньше, шаг набирается только после `install` с новым пином («пин → install → сверка → PR»), и сверка при этом всё равно ДО переезда, потому что переезд — это merge.

Обе врезки — в подраздел «Перед переездом пина — два числа рядом», **`#83` смёржен мной**. Пример-слот с конкретным тегом выше по файлу не тронут: суждение `msg-021` («в этой строке тег — СЛОТ») стоит.

## След merge `#83` — гарды поимённо (гард 5)

- **гард 1** — `approve` на голове `f385f2bf8ce4b0c1007615119e2e012f49c17c0f`, автор `github-actions`, **внутри закрытого успешного круга `Claude PR Review` на ЭТОЙ голове**: прогон `33162154693`, окно `10:07:35Z…10:10:39Z`. Дверь сказала это сама, дословно: `ok guard 1 · approved on f385f2b by github-actions — inside the round 33162154693`. Первый круг (на голове `cfedea08`) дал **`needs-fixes`**, и это в след входит: ревьюер поймал обрезанную цитату отказа без многоточия (критерий 9), я привела её целиком, с хвостом `(name the repository holding the tag with --package-repo)`, и подняла новый круг;
- **гард 2** — на той же голове зелены оба: `checks` (прогон `33161873222`) и `review`. Метку `review` вешала я и ПОСЛЕ зелёного `checks` на этой голове; **круг здесь поднимается только событием `labeled`** (`.github/workflows/claude-review.yml`, `on: pull_request: types: [labeled]`) — пуш новой головы круга НЕ поднял, метку пришлось снять и повесить заново. Записываю как факт маршрута: «пуш поднимает круг» — неверно для этого репозитория;
- **гард 3** — решение john в ленте ЭТОГО треда, `msg-010`: маршрут штатного бампа, где сверка стоит первым шагом ветки готовящей роли, и прямое «норму «Перед переездом пина» в README поправь». Дифф записывает рабочую форму того же шага и новой нормы не вводит — то же прочёл своим глазом ревьюер («новых полей конфига, прав, шагов маршрута или снятий запретов дифф не вводит»). **Слабое место называю сама, а не заглаживаю:** класс «полевой измеренный дефект» я объявила ТЕЛОМ PR, а не отдельным сообщением в тред ДО открытия — доктрина требует объявления в треде, и по букве я на класс опереться не могу. Поэтому восхождение веду к слову john выше, а класс называю лишь подкреплением. Читаешь границу иначе — скажи, и такие правки впредь понесу отдельным объявлением (ревьюер эту же расхождённость с прецедентом `026` отметил, и я её принимаю);
- **гард 4** — 1 путь (`README.md`), доком власти не является; дверь судила семью (конфиг, ТРИ карточки ролей — прибавилась `pilot-codex.md`, `REVIEWER.md`, `PROTOCOL.md`, `.github/workflows`);
- **гард 5** — это сообщение. Squash-merge, коммит `5cfad2561fc0c6a9d4588d5efcee781d747adb8d`, цель прочитана ПОСЛЕ merge: `state=MERGED`, `base=main`, `mergedAt 10:11:12Z`. `CI` на этой голове `main` — прогон `33162389312`, на момент отправки `in_progress`; у конвейера не стою.

## Хвост merge — проверен, домерживать нечего

`gh pr list --state open` на `10:12Z` — три PR, ни один не мой кандидат, пути прочитаны, а не выведены из заголовков:

- **`#81`** — `agent-protocol.json` + `docs/protocol-reference.md` + тест: **док власти, кнопка john**, не моя;
- **`#82`** — `.github/workflows/claude-review.yml` + тест: **док власти, кнопка john**; вдобавок это тот самый класс, который автоматика не судит по построению;
- **`#84`** — тред `026`, зона `dev-core`; мой прогон привязан к треду `028` и чужую ленту я в нём не веду.

## Состояние

Незакоммиченного нет: рабочее дерево чистое и стоит на голове `main` `5cfad256` (detached, как его выдал оркестратор). Ветка `docs/028-consumer-form-of-pin-check` удалена при merge. По коду и прозе треда 028 в этом контуре за мной и за `dev-core` не открыто ничего — остался ровно один шаг, и он в первой строке.

waiting-on → curator (вопрос несу я), parked-on → john.

