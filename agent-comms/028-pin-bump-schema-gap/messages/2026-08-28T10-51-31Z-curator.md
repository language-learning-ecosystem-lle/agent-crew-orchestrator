---
from: curator
worker: claude-code
session: 51d39773-5853-43e8-b23f-671431f3e5ce
date: 2026-08-28T10:51:31Z
expects: none
waiting-on: —
---

**Тред 028 закрыт этим тактом: приёмка состоялась и снята моей рукой — обе половины, и живая половина закрылась ДВАЖДЫ, второй раз сегодня в 10:48Z, уже после доставки объявления.**

Ниже: чем закрыта приёмка, что перепроверено чтением, хвост merge и моё суждение по классу «у готового факта в чужом контуре нет события».

## Приёмка — по разделу «Проверяемость» постановки (`msg-001`), пункт за пунктом

**Половина (а), код.** `#57` — `MERGED`, база `main`, коммит `d15ba1d952521dcf4f18f882e708382ae2ab4fb7`, `2026-08-22T09:31:24Z` (прочитано у PR сейчас, не по докладу). Юниты, тест на стык и интеграционный на рез объявлены в `msg-002` и приняты кругом ревью того PR.

**Половина (б), проза.** `#83` — `MERGED`, база `main`, коммит `5cfad2561fc0c6a9d4588d5efcee781d747adb8d`, `2026-08-28T10:11:12Z`. **Хвост своего merge закрываю здесь же, потому что событием исход CI не приезжает:** `checks` на этой голове `main` — прогон `33162389312`, `completed/success` (прочитано `gh run view` в `10:49Z`; в `msg-027` он стоял `in_progress`).

**Живая приёмка человека** — та, про которую постановка сказала «до этого приёмка неполна: юнит не доказывает, что шаг СПРОСИЛИ». Закрыта дважды, и оба раза прогоняла **рука, ведущая переезд**, а не я:

1. переезд `v0.2.3` → `v0.2.4`, LLE-103, сообщение `agent-comms/103-pin-bump-0-2-4/messages/2026-08-22T10-43-28Z-dev-core.md` — оба числа цитатой, PR LLE `#374`, squash-merge `a00a51fd0048a753ad7bbc9cafa09ab13ca8c8c7` (найдено и прочитано в прошлом такте, `msg-027`);
2. **переезд `v0.2.4` → `v0.2.5`, сегодня** — LLE-103, `2026-08-28T10-48-20Z-dev-core.md`, прочитано мной по `origin/comms` контура LLE в `10:49Z`. Шаг стоит ПЕРВЫМ шагом ветки `protocol/pin-bump-0-2-5` (голова `066045a727bc8ca7b29b676962871fbe53b474c7`), оба числа процитированы:

```
the package at agent-protocol-v0.2.5:src/schema/version.ts writes protocol version 18
the config at 'agent-protocol.json' in '.' at main declares protocol version 18
protocol version 18 — matches the package
exit=0
```

Этот второй прогон закрывает и **половину (б) отдельно от половины (а)**: рука у потребителя набрала команду с `--package-repo` и клоном тега в свой `mktemp -d` — ровно ту рабочую форму, которую `#83` вписал в `README.md`, — и назвала её подтвердившейся своими словами («у потребителя без неё команда ищет тег в нашем репозитории»). Дефект, который я замерила в своей же прозе, тем самым проверен исполнением, а не чтением. Она же независимо прочла sha тега — `d93f5b03…` совпал с объявленным.

Merge PR LLE `#406` — не мой шаг и приёмке 028 не нужен: критерий постановки был «шаг прогнан ДО переезда и оба числа в треде», и он исполнен.

## Перепроверено моей рукой сегодня (ничего не взято из докладов)

- `git ls-remote origin refs/tags/agent-protocol-v0.2.5` → `d93f5b037801df249cd9fee095da5abea63908cc`;
- `git show agent-protocol-v0.2.5:src/schema/version.ts` → `CURRENT_PROTOCOL_VERSION = 18`; `package.json` тега → `"version": "0.2.5"`;
- доставка объявления в чужой контур существует файлом, а не отчётом: `agent-comms/103-pin-bump-0-2-4/messages/2026-08-28T10-43-12Z-curator.md` в `origin/comms` контура LLE. Проверка эффекта, а не факта записи: через пять минут после него в той же ленте лёг ответ `dev-core` с исполненным переездом.

## Хвост merge — домерживать нечего

`gh pr list --state open` в `10:50Z` — четыре PR, **пути прочитаны, а не выведены из заголовков**:

- `#81` (тред 035) — `agent-protocol.json` + `docs/protocol-reference.md` + тест: **док власти, кнопка john**;
- `#82` (тред 035) — `.github/workflows/claude-review.yml` + тест: **док власти, кнопка john**; вдобавок класс, который автоматика не судит по построению;
- `#85` (тред 037) — `PROTOCOL.md` + `packages/**`: **док власти, кнопка john**;
- `#86` (тред 037) — `docs/roles/curator.md`, `docs/roles/dev-core.md`: **доки власти, кнопка john**.

Ни один не мой кандидат. **Попутный факт, доставшийся от этого чтения и отданный владельцу треда 035, а не мной чиненный:** в диффе `#81` лежит `packages/agent-protocol/tsconfig.tsbuildinfo` — артефакт сборки; в `main` он не отслеживается (`git ls-files | grep -c tsbuildinfo` → 0) и в `.gitignore` не назван. Мой прогон привязан к треду 028, поэтому дальше этой строки я его не несу.

## Класс «у готового факта в чужом контуре нет события» — суждение: тредом НЕ завожу, и вот почему

Замер стоит записи и я его записываю здесь целиком, чтобы он читался без раскопок:

> Приёмка треда 028 пролежала готовой в ленте LLE-103 с `2026-08-22T10:43Z` по `2026-08-28T09:00Z` — шесть дней. Ни одна сторона не бездействовала: в LLE шаг прогнали и переезд смёржили в тот же час, в 028 ждали строки о нём. Между контурами доставку несёт РУКА (роль, у которой есть оба чекаута, или чатный curator), а у руки нет тика: событий `mail`/`wake` через границу контура не ходит. Второй экземпляр того же класса — сегодняшний: объявление тега `v0.2.5` было готово в `09:52Z`, а поехало в `10:43Z` рукой john по вопросу с парковкой.

Тредом не завожу по границе «к john», а не по цене:

- **лечение — либо норма, либо механика, и обе половины владельческие.** Норма межконтурной стыковки живёт в `PROTOCOL.md` (док власти, кнопка john), а механика — это событие, ходящее между ДВУМЯ контурами, то есть работа сразу в чужом репозитории и трата чужих тактов. Это направление работ, а не операционное решение;
- **постановку писать не из чего, пока направление не названо:** у треда была бы формулировка дефекта и не было бы формы, а «Проверяемость» упёрлась бы в чужой контур, которым этот пакет не владеет — тот же довод, по которому 028 сознательно не делал дверь над чужим пином;
- правило последней строки применяю в его прямом виде: **не уверена, важно ли john направление — значит важно, и спрашивать надо словом, а не заведённым тредом.**

Вопрос john, если он захочет ему хода: заводить ли тред «межконтурная стыковка: доставка факта между контурами — рука без события» и в какой из двух форм (норма в `PROTOCOL.md` / механика уведомления). Отдельного сообщения-парковки под него не ставлю: тред закрывается этим тактом, а держать открытым тред ради чужого вопроса — это ровно тот тихий парк, который карточка называет худшим исходом.

## Состояние и закрытие

- незакоммиченного нет: рабочее дерево роли чистое, стоит на голове `main` `839590e` (detached, как выдал оркестратор); файлов вне почты я этим тактом не трогала, PR не открывала;
- за `dev-core` и за мной по треду 028 не открыто ничего;
- **тред перевожу в `closed` этой же рукой сразу за этим сообщением** (`thread status --status closed`), контрольное чтение — `thread show`, а не грепом по производной витрине.

waiting-on → —

