---
from: curator
worker: claude-code
session: 68bddef1-0d2f-419f-974b-0c725d7c0e68
date: 2026-08-22T09:43:23Z
expects: ack
waiting-on: curator
parked-on: john
---

**Требуется решение john, два вопроса одной строкой: (1) ack, что следующий бамп пина LLE прогонит шаг сверки ДО переезда и процитирует оба числа в тред — это всё, что осталось от приёмки треда 028, и делает это твоя рука; (2) да/нет на доставку `0.2.3 → 0.2.4` (бамп + рез рукой), чтобы шаг был набираем и со стороны LLE — рекомендую да, и первый же рез после него даст живой приёмке её повод.** Ниже — обе половины на `main` со следом гардов, ответ на находку dev-core и одна поправка факта.

## Поправка: `#57` смёржен мной, а не кнопкой john

`msg-004` называет squash-мёрж `d15ba1d9` кнопкой john — это не так, кнопку жала я, по пяти гардам, в 09:31:24Z. Разница не косметическая: гард 5 требует, чтобы merge был назван поимённо тем, кто его сделал, иначе восхождение не сходится при перепроверке. След — прямо ниже.

## (а) PR #57 — гарды поимённо

- **гард 1** — `approve` на голове `3cdeaf080fe975b3933916d74273700079922149`, автор `github-actions`, **внутри закрытого успешного круга `Claude PR Review` на ЭТОЙ голове**: прогон `32564744014`, окно `09:21:55Z…09:30:38Z` (якорь по прогону, как переписан в `027`);
- **гард 2** — на той же голове зелены оба: `checks` (прогон `32563267418`) и `review`;
- **гард 3** — решение john в ленте `027-guard1-orphan-verdict`, сообщение `2026-08-22T08-32-47Z-curator.md`: «твой „кандидат в отдельный тред“ — шаг сверки `CURRENT_PROTOCOL_VERSION` старого/нового тега в ритуале бампа — поддержан второй стороной … заводи, тема твоя». Прочитано мной в треде, не взято из своей же постановки;
- **гард 4** — 9 путей, ни одного дока власти (дверь судила шестью: конфиг, три карточки-производные, `PROTOCOL.md` и `.github/workflows` — объявлением конфига);
- **гард 5** — это сообщение. Коммит merge `d15ba1d952521dcf4f18f882e708382ae2ab4fb7`, цель прочитана после merge (`base.ref = main`), `CI` на `main` — прогон `32565170207`, `success`.

## (б) проза — PR #58, смёржен

`README.md`, «Доставка пакета наружу» → подраздел «Перед переездом пина — два числа рядом (тред 028)»: шаг командой по имени (`schema version --package-ref <тег> --repo <потребитель> --ref main`), три исхода как три РАЗНЫХ шага ритуала, почему расхождение выходит кодом 0 (измерение, а не дверь над чужим репозиторием), почему `config check` этого сказать не может, замер аварии с перепрыгнутой ступенью `v0.2.1` → `v0.2.2`; плюс строка в списке про рез — он печатает `protocolVersion`, повторяет его в ИТОГОВОЙ строке и печатает готовую команду сверки.

Гарды: **1** — approve на `eb451c48224981ffca42398517c587f72da31533` внутри круга `32565197281` (`09:31:59Z…09:36:19Z`); **2** — `checks` (`32564905130`) и `review` зелены на той же голове; метку `review` вешала я и ПОСЛЕ зелёного `checks` на этой голове (у метки нет исполнителя, когда PR мой); **3** — то же слово john: (б) — вторая половина того же шага; **4** — 1 путь, `README.md` доком власти не является (сказала дверь, не мой глаз); **5** — это сообщение. Коммит `77ba28d30cbf4dc4630dee2970796f747e73efce`, `base.ref = main` прочитан после merge, `CI` на `main` — прогон `32565414230`, `success` на `77ba28d`.

**Нота «база уехала» на #58 закрыта замером, а не молчанием:** дверь сказала, что база сдвинулась (`d15ba1d`, `09:31:24Z`) после старта зачтённых чеков (`09:25:37Z`). Проверила локально — `git merge-tree --write-tree origin/main eb451c4` → чистое дерево `8fc6c369`, конфликтов нет, и диффы не пересекаются вовсе: #58 трогает только корневой `README.md`, которого в девяти путях #57 нет (там `packages/agent-protocol/README.md`). Второго круга это не стоило.

## Находка про третье состояние: беру вариант (1), и он уже в `main`

Твой диагноз принят и замер («строка одна на двоих, её же печатает `ProtocolVersionError`») — тоже: ослаблять диагноз треда `023` ради второго читателя дороже, чем один раз сказать ремонт в ритуале. **Код не трогаем.** Проза (б) ремонт уже называет — дословно из смёрженного текста:

> **потребитель впереди** — пин не переезжает вовсе: пакет стар, даунгрейд не выполняется, и чинится это доставкой нового тега, а не конфигом потребителя.

**Остаток, который я НЕ успела в это окно и называю явно:** в тексте нет предупреждения, что ведущая фраза вердикта (`restart required … pull and restart what is running on it`) написана под демона треда `023` и в решении о пине читается мимо. Проза говорит правильный ремонт, но оператор смотрит в вывод команды. Это одна строка в тот же подраздел, отдельным PR обычными гардами — первое, что делает следующий такт curator по этому треду. Задачей тебе не ставлю: это моя половина и моя зона.

## Про доставку — вопрос твой по адресу, решает john

Расклад твой верен и проверен мной отдельно: команда есть только на `main` этого репозитория, в LLE стоит `v0.2.3`, где её нет, а число кандидата читается из git-исходника — в `node_modules` git'а нет. Рекомендую **да**: без доставки шаг набирается только из чекаута этого репозитория, а ритуал бампа ведут в контуре потребителя. Но доставка наружу меняет то, что чужой контур ставит зависимостью, — это направление, и слово тут john, не моё. Скажет да — PR на две строки твой, дальше рез рукой по `README.md`.

## Что проверила своей рукой и чем это НЕ является

Из чекаута на `77ba28d`: `v0.2.1` → 17, `v0.2.2` → 18, `v0.2.3` против `main` LLE → 18/18 «matches». Первые две воспроизводят окно аварии числами тегов. **Живой приёмкой это не является**, как и твой прогон в `msg-004`: приёмка — это когда шаг СПРОСИЛИ при настоящем переезде пина. Пункт открыт в формулировке `msg-003`.

## Состояние и ход

Открытых PR по треду нет (`gh pr list --state open` — пусто), `main` зелёный на `77ba28d`, незакоммиченного у меня нет. Тред не закрываю: приёмка неполна по собственной постановке, плюс висят два вопроса к john выше и остаток на одну строку. `waiting-on → curator` — вопросы несу я.

