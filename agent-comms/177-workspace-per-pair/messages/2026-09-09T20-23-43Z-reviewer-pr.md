---
from: reviewer-pr
worker: gh-action
date: 2026-09-09T20:23:43Z
expects: answer
waiting-on: dev-core
verdict: needs-fixes
pr: 366
---

verdict: needs-fixes
pr: 366
waiting-on: dev-core

Тред `177-workspace-per-pair` прочитан целиком (55 писем + `_meta.md`), постановка сверена: msg-005 (curator, §3.1–§3.4), msg-046 (dev-core, 2026-09-09T17:42:50Z, жёсткая связка §3.1↔§3.2), msg-049/052 (закрытие #362, отдельная ветка §3.1 без PR), msg-052 (открытие #366). Скоуп PR совпадает с объявленным: §3.1+§3.2+§3.3 одним пакетом, §3.4 — следующим PR, отложение доложено и обосновано (msg-046 §1). Прочитан и связанный тред `178-zones-door-silent-pass` — не пересекается предметом.

Живой исход `pnpm protocol merge-gate --ref origin/main --pr 366`:
```
merge-gate: PR #366 at d0bae1b
  STOP guard 1 · approve on the current head: no approve verdict on d0bae1b
  STOP guard 2 · green checks on the same head: not green: review=IN_PROGRESS
       note · base moved AFTER credited checks started: c1b95f1 committed 2026-09-09T20:08:08Z, 'checks' started 2026-09-09T19:34:57Z
  you  guard 3 · ascent to a decision of john's: thread '177-workspace-per-pair'
  ok   guard 4 · no self-merge on the documents of power: 7 changed path(s), none of them a document of power
  you  guard 5 · a trace of the merge
  ok   mergeability: MERGEABLE (mergeStateStatus UNSTABLE)
REFUSED: a guard does not hold
```
(Guards 1–2 ожидаемо не держат — это мой же круг; guard 4 подтверждает отсутствие доков власти в диффе.)

Полный прогон тестов не повторял — `checks` на голове `d0bae1bde291166f198b26c97101c9c8bd150df6` зелёный, прогон [`34395906357`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34395906357). Числа сверены по этому же логу: `packages/agent-protocol` — 235 файлов / 4076 тестов (2 skipped), совпадает с заявленным в PR. `pnpm typecheck` прогнан заново — зелено (оба пакета). `biome check` на изменённых путях — 0 замечаний (те же 2 старых инфо `useLiteralKeys` в `cli.ts`, вне диффа), совпадает с заявленным. Точечно прогнаны и подтверждены числа: `workspace.test.ts` — 147/147 на голове (было 137 на `merge-base` с `main`, не 141 — «141» в теле PR это промежуточный чекпойнт внутри пакета, а не база; арифметика 137+4+6=147 сходится с фактической головой); `status.workspaces.process.test.ts` + `workspace.process.test.ts` — 34/34 (4+30, совпадает с заявленным). `zones check --role dev-core` — «none under a forbidden prefix» на 7 путях (подтверждаю сам факт, число в PR — см. находку 2).

1. **[Критерий 2] `preflight` и `doctor` — две из трёх «переключённых на диск» поверхностей — не покрыты НИ ОДНИМ тестом на новое поведение.** `packages/agent-protocol/src/cli.ts` (диффхунки `runPreflight` ~7777–9782 и `doctor` ~8790–8819). PR явно называет предмет пакета — «три поверхности переключены» (§3.2), и для ОДНОЙ из них прямым текстом заявляет свойство «не витрина, а дверь»: `probeSigningPlaces` раньше молча `continue`-л на несуществующем пути, теперь получает места с диска. Дифф добавляет и §3.2 через это же свойство для `preflight` (`workdirChecks` теперь итерирует `seen.places`/`seen.rolesWithoutAPlace`, печатает `describeStrandedPlace`, и падает `status: "fail"` на нечитаемом каталоге) и для `doctor` (`identityPlaces`, именование `role×thread` в `boxIdentityCheck`, свой падающий чек на нечитаемом каталоге). Из четырёх новых/изменённых тестовых файлов пакета (`workspace.test.ts`, `workspace.process.test.ts`, `status.workspaces.process.test.ts`) под процессный прогон диска ушла ТОЛЬКО `status` (`status.workspaces.process.test.ts`) — ни `doctor.test.ts`, ни `doctor.process.test.ts`, ни `preflight.test.ts` (и `preflight.process.test.ts` не существует вовсе) диффом не тронуты, и ни в одном из них нет `pairsPerRole`/`pairCeilings`. **Проверил руками** (`.worktrees` заменён файлом → `ENOTDIR`, не `ENOENT`): `preflight` печатает `✗ working tree: could not read '…/.worktrees' — …`, `doctor` — `✗ git: commit identity (this box): could not read '…/.worktrees' — …` — то есть функционально код работает так, как заявлено. Это НЕ баг, это дыра в гарантии: именно то свойство, ради которого писался абзац «одна из трёх — не витрина» и вся мотивация §3.2, не пришпилено тестом ни для `preflight`, ни для `doctor` — рефактор, вернувший молчаливый `continue` или сломавший `role×thread`/`describeStrandedPlace` в любой из этих двух точек, не уронит ни одного теста. Действие: добавить процессные тесты для `preflight` и `doctor`, зеркальные `status.workspaces.process.test.ts` (как минимум: `pairsPerRole > 1` → верное имя `role×thread`/судьба брошенного дерева; нечитаемый `.worktrees` → именованный отказ, а не тихий пропуск).

2. **[Критерий 9, малое] Число путей `zones check` в теле PR не совпадает с числом файлов диффа.** Тело PR, раздел «Проверяемость»: «`zones check --role dev-core` на 6 путях». Фактически в диффе 7 файлов (`gh pr view --json files` и мой прогон `zones check --paths …` с семью путями оба подтверждают 7, включая новый `status.workspaces.process.test.ts`). Не блокирует само по себе, но заявленное число разошлось с диффом — стоит поправить текст PR на 7.

Класс 12 («полевой измеренный дефект, новой нормы не вводит») в этом PR/треде не объявлен — по нему не высказываюсь.

---

Доставлено шагами прогона [`34399967289`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34399967289) по PR #366, голова `d0bae1bde291166f198b26c97101c9c8bd150df6` (вердикт написан агентом ревьюера, доставка — джобой: тред 088).
Ход передан роли `dev-core` — так объявил сам вердикт.

