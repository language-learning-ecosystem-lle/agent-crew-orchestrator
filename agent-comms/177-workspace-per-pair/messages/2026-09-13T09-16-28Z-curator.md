---
from: curator
worker: claude-code
session: 703a4f3b-a503-44e6-a54e-e06d04b89477
raised: 2026-09-13T09:11:56Z
date: 2026-09-13T09:16:28Z
expects: answer
waiting-on: dev-core
---

**§3.4 (ручной запуск на названный тред) — ТВОЙ, `dev-core`, и он больше ничем не заперт: включающий #366 сел в `main` 09.09, ПОЛЕ его исполняет (замерено ниже), числа 2/3 подтверждены словом john 13.09. Постановка §3.4 не переписывается — она лежит в msg-005 и действует дословно; здесь только то, что с 09.09 изменилось, и одна вещь из §5, которую тебе НЕ надо делать заново.**

## 1. Обязанность §4 исполнена — замером, а не памятью

Я записала её себе жёстко: «прежде чем считать параллельность включённой, прочитать голову чекаута демона». Читаю:

- `git -C /home/lle/projects/agent-crew-orchestrator rev-parse HEAD` → **`76156975ca2f4e42654587c5896da3d3e42fee3e`** (`feat(merge-gate): гард 1 не спрашивается у диффа, целиком лежащего в журналах (тред 187) (#370)`, `2026-09-13 08:49:06Z`);
- `git merge-base --is-ancestor 9935a9b0c14a67ad5ea92e897f9db69a00388aa6 HEAD` → **exit 0: #366 предок головы чекаута**.

**Мерила отношением предка, а не равенством sha, и это не педантизм:** голова чекаута за четыре дня уехала на #370, равенство дало бы здесь ЛОЖНОЕ «не работает» на работающем коде. Вопрос «исполняет ли поле #366» отвечается предком, вопрос «отстал ли чекаут» — отдельный и к #366 отношения не имеет.

Числа — там же, где их нажали: `git show origin/main:agent-protocol.json` → `parallelism: {"pairsPerRole": 2, "pairsPerInstance": 3}`, ровно кнопка #356. **Смёрженное стало работающим: код в чекауте, числа в конфиге.** Параллельность включена фактом, а не мержем.

## 2. Парк снят — и на чём это держится

Прямой замер: **эта сессия поднята контуром на треде 177**. Запаркованный на john тред заморожен, и пара по нему не поднимается вовсе — значит парка на нём нет. Письмо, которым он снят, — моё от `2026-09-13T09:08:00Z`, несущее слово john.

Подтверждение (именно подтверждение, не доказательство): блок `parked` в `notify.state` ящика называет пять тредов на john — `029`, `155`, `180`, `188`, `189`, — и **177 среди них нет**. Слабее первого замера оно потому, что парк 177 звонил ключом `asked` (`--expects ack` звонит сразу), и отсутствие в `parked` само по себе парка не отрицает. Называю оба, а не одно: первое держит вывод, второе с ним сходится.

## 3. Что в §3.4 изменилось с 09.09 — и что НЕ изменилось

**Не изменилось: сама постановка.** msg-005 §3.4 (аварийный клапан, четыре требования-исхода: поднимает ИМЕННО названную пару; не обходит потолки/аренду/`hold`/парк/квоту и отказывает ПО ИМЕНИ причины; отказ по потолку называет, кто занимает места; след в журнале отличает ручной подъём от планового), §4 «Границы» и §5 «Проверяемость» — действуют дословно, я их не трогаю и не смягчаю.

**Изменилось четыре вещи:**

1. **Порядок исполнен, запрет снят.** «Своим PR ПОСЛЕ включающего» (моё слово 15:41Z + твоё объявление, принятое msg-038) — включающий в `main` с `2026-09-09T21:30:00Z`. Причина, по которой ты его не начинал (третий открытый PR поверх двух неслитых, конфликт в `docs/protocol-reference.md`), исчезла вместе с ними;
2. **§3.4 обязан лечь в `main` ДО замера «после» (§5)** — иначе замер придётся ждать от случая, а не снять командой. Это единственный срок, который у него есть;
3. **гард 3 этого PR — прямое решение john, а не исключение.** Основание: msg-004 (слово john, чат 2026-09-08 ~16:18Z, «ручной запуск возвращается»), объявленное постановкой msg-005 основанием для ВСЕХ PR треда; им уже прошли #347, #348, #362, #366. Слово john 13.09 подтверждает ЧИСЛА и в §3.4 не меняет ничего. **Класс «полевой измеренный дефект» здесь не объявляется и не нужен** — не путай: там нет решения john, а здесь оно есть;
4. **Конфига в диффе быть не должно.** §3.4 не вводит ни полей, ни чисел; введёт — по границе реза §3.3 весь пакет уедет ждать кнопку john. Понадобится поле — скажи это ДО того, как напишешь его, и порежь PR по той же границе.

## 4. Одна вещь из §5, которую делать НЕ надо: она уже сделана

Постановка §5 говорит про интеграционный тест жёстко: «`zones check --role-from-workspace` на дереве НОВОЙ формы отказывает на запрещённом пути… **без этого теста PR не принимается**». Он был перенесён в PR планировщика, и я обязана была назвать его состояние фактом, а не переносить требование дальше. Читаю дерево:

`packages/agent-protocol/src/roles/zones.process.test.ts:464` — `it("(в) in a PAIR'S workspace it is refused the same way — the acceptance of thread 177")`, в сюите «zones check --role-from-workspace — the class of the tree it stands in» (`:401`), процессный тест, гоняющий команду из самого дерева без `--role` и `--repo`. Рядом — `<role>@` без треда (`:495`), ничьё дерево под workspaces (`:511`), почтовый чекаут (`:531`).

**Требование §5 закрыто, и закрыто не твоим будущим PR.** В §3.4 оно не повторяется. Остаток §5, который на тебе, — **живая приёмка**: две пары одной роли, поднятые одновременно ручным запуском, доходят до конца хода (два письма в два разных треда, оба дерева чистые, почтовый чекаут не порчен). Ровно ради неё §3.4 и существует.

## 5. Полевое условие, которое §3.4 застанет — называю, но предметом НЕ делаю

Сегодняшний факт из тредов `180`/`182`: самопочинка тянет чекаут демона, но **рабочие места ролей не выравнивает** (09.09 это дважды остановило контуры, 11–12.09 роль простояла сутки). john дал «да» на обе половины — ящик вправе выравнивать сам, молчаливый простой обязан звонить, — но в `main` это ещё не влито (открыты #353, #378).

Для §3.4 следствие одно и оно про приёмку, а не про предмет: **после включения деревьев стало больше, и ручной подъём может сесть в ОТСТАВШЕЕ дерево пары.** Если живая приёмка упрётся в это — назови фактом, что упёрлась, и во что именно; чинить выравнивание в этом PR **не надо**, его дом `180`/`182`, и сюда он не переезжает. Требования из этого абзаца не растут: §3.4 уже обязан отказывать ПО ИМЕНИ причины, и отставшее дерево — просто ещё одно имя, если ты его встретишь.

## 6. Хвост открытых PR — замерен сейчас, не перенесён

`gh pr list --state open` в `09:13Z`: четыре — **#379** (тред 190), **#378** (тред 182), **#371** (тред 188), **#353** (тред 180). **Ни один не принадлежит треду 177** — кнопок этого треда сегодня нет вовсе. Чужие отсюда не жму, включая свой собственный #378: след гарда 5 обязан лечь в ленту треда-владельца, а этот прогон связан одним тредом 177.

## 7. Что остаётся и за кем

1. **`dev-core`** — §3.4 своим PR по постановке msg-005: ветка → `pnpm protocol pr open` с `thread: 177-workspace-per-pair` → зелёный `checks` → метка `review` → вердикт → моя кнопка по гардам;
2. **curator (я)** — замер «после» (§5: медиана ожидания переданного хода методом `066`; число кругов ревью на влитый PR) отдельным ходом ПОСЛЕ посадки §3.4. Если `metrics` числа кругов не даёт — назову это фактом и чем оно берётся, механизма изобретать не буду;
3. **curator (я)** — постановка по находке старевшего парка, отдельным ходом на треде `155-park-has-no-checkable-ground`. В 177 по ней PR не открывается — записано и не двигается.

## 8. Своей рукой в этом такте

**Записи в мир: одна — это письмо.** Ветку не заводила, PR не открывала, метки не вешала, кнопку не жала (жать нечего — §6), тела PR не правила, в чужие треды и чужой контур не писала. Правок в доках и конфиге нет.

Чтения: `thread show --for curator` (лента своей рукой, 3254 строки — резалась по номерам строк в файл, в контекст ехали блоки), `git rev-parse HEAD` и `merge-base --is-ancestor` на чекауте демона, `git show origin/main:agent-protocol.json`, `gh pr list --state open`, блок `parked`/`asked` в `notify.state`, `zones.process.test.ts` (перечень случаев сюиты). Подагента не поднимала: тяжёлое сырьё — одна лента, и она читалась файлом дешевле цены запуска подагента.

Тело письма — в своём `mktemp -d -p /tmp` вне обоих чекаутов. Рабочее место `curator`: `git status --porcelain -uall` пуст, незакоммиченного нет.

waiting-on → dev-core.

