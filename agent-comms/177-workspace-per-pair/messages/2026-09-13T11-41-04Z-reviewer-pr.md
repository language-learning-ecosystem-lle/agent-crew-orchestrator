---
from: reviewer-pr
worker: gh-action
date: 2026-09-13T11:41:04Z
expects: answer
waiting-on: curator
verdict: approve
pr: 387
---

verdict: approve
pr: 387
waiting-on: curator

Полный прогон тестов не повторял — `checks` на голове `9402ef5d09cbf17732dd714391fe8e180e44afcf` зелёный, прогон `34754279781`. Точечно прогнал сам: `pnpm typecheck` (exit 0, оба пакета); `pnpm exec biome check` на всех 7 путях диффа — чисто (2 info-находки в `cli.ts` вне диффа, на строках 8482/15666, предсуществующие — проверил по базе `7afb77fb2`, не введены этим PR).

`pnpm exec vitest run src/orchestrator/occupancy.test.ts` — 9/9 зелёных (7 прежних + 2 новых, совпадает с заявленным). **Мутацию доказал самостоятельно, не на слово PR:** вернул ключ схлопывания `leaseSpans` к роли (как было до диффа) — краснеют РОВНО два новых кейса, семь прежних зелены; мутация снята копией, дифф на диске не оставлен.

Числа сверил прогоном, не арифметикой: `occupancy.test.ts + notify.test.ts + alarms.test.ts + notify.process.test.ts` = 208/208 (точное совпадение с заявленным «occupancy + notify + alarms: 208»); `occupancy.test.ts + self-restart.test.ts` = 117/117 (совпадает с заявленным). `restart.process.test.ts` в одиночку — 5/5 зелёных, совпадает с заявленным «в одиночку зелен». Полную сюиту не гонял (исключение выше), но сверил лог CI-прогона `34754279781` дословно: `packages/agent-protocol test: Tests 4203 passed | 2 skipped (4205)` — в самом логе `restart.process.test.ts (5 tests)` зелёный. То есть диагноз PR «нагрузочный флак, а не находка» подтверждён логом джобы, а не статусом (критерий 7): в CI этот тест прошёл, локальная сюита автора поймала прочерк, которого CI не увидел.

`pnpm protocol zones check --ref HEAD --role dev-core --base 7afb77fb29805ca1c20df60a220daac881641a4c`: 7 путей, ни один не под `forbidden` — зелено (критерий 4).

Живой исход `pnpm protocol merge-gate --ref origin/main --pr 387`: `REFUSED` — гард 1 STOP (approve на этой голове ещё не опубликован, ожидаемо изнутри этого же круга), гард 2 STOP (`review=IN_PROGRESS`, тот же круг), **гард 3 `you`** (восхождение к решению john, тред `177-workspace-per-pair`), **гард 4 `ok`** (7 изменённых путей, ни один не док власти — дверь вывела 8 путей данными: `agent-protocol.json`, 4 карточки ролей, `REVIEWER.md`, `PROTOCOL.md`, `.github/workflows`), гард 5 `you` (след мержа), `mergeability=MERGEABLE` (`UNSTABLE`).

Основание гарда 3 (критерий 9) проверил чтением ленты треда `177-workspace-per-pair` целиком, а не пересказом PR: решение john — msg-004 (письмо curator `2026-09-08T16:19:06Z`, слово john полем `delivers`, «ПРИНИМАЮ ВСЕ ЧЕТЫРЕ РЕКОМЕНДАЦИИ», потолок 2 пары на роль), объявленное постановкой msg-005 (`2026-09-08T16:24:17Z`) основанием гарда 3 для ВСЕХ PR треда 177; тем же основанием уже прошли #346–#348, #362, #366, #382, #385. Класс «полевой измеренный дефект» (критерий 12) PR явно НЕ объявляет, и это верно: гард 3 стоит на прямом слове john, а не на исключении — вывод о норме поэтому не требуется.

Критерий 3 (скоуп): `thread: 177-workspace-per-pair` и `role: dev-core` в описании PR — есть. Дифф — прямое продолжение находки того же треда (лента: msg dev-core `2026-09-13T11:02:39Z` про метод сверки чтением был неверен → msg dev-core `2026-09-13T11:25:34Z` про находку тем же приёмом «грep по ключу схлопывания» в `leaseSpans`/`cli.ts`). Расширений вне находки не вижу.

Критерий 2: новый тест `pairs every lease with its OWN release…` бьёт в структуру (`leaseSpans` возвращает 5 пролётов, `dropped: 0`, вместо 3/16 у мутации), второй тест — в курьерское следствие (`freeTailMinutes`/`unacceptedTurns` не звонят на насыщенном ящике). Дверь курьера (`cli.ts`) отдельным юнитом не покрыта, но копия обхода полностью удалена и заменена вызовом ТОЙ ЖЕ `leaseSpans` — структурно это закрывает класс «дверь молчит» (второй реализации для расхождения больше не существует), а не обходит его; `pnpm typecheck` подтверждает, что сигнатуры совпали.

Критерий 6: полей конфига/форм почты дифф не меняет, `protocolVersion` не тронут — корректно, диффом подтверждено (нет изменений `agent-protocol.json`).

Критерий 5/10: доков власти в диффе нет (подтверждено гардом 4 и собственным чтением списка семи файлов); прямых чтений `agent-protocol.json` мимо пакета в диффе нет.

Проверил также утверждения текста докстрингов о поведении (критерий 9): `share` в `occupancy.ts:217` — простое деление без потолка (`Math.min` нет), подтверждает заявленные «200 % не режутся»; `foldDay` суммирует минуты по пересекающимся пролётам независимо (не сливает), подтверждает «роль с двумя парами даёт честные 200 %»; `freeTailMinutes`/`blockedSpans` в `notify.ts` (код не менялся этим PR, только докстринг) берёт МАКСИМУМ конца блокирующих интервалов, а не сумму — подтверждает заявленное «не спрятанное следствие» для курьера.

`docs/journal/dev-core.md`: правка чисто аддитивна (диффом подтверждено — только новые строки после конца файла базы, ни одной изменённой прежней строки).

Находок по критериям 1–12 нет.

---

Доставлено шагами прогона [`34754667469`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34754667469) по PR #387, голова `9402ef5d09cbf17732dd714391fe8e180e44afcf` (вердикт написан агентом ревьюера, доставка — джобой: тред 088).
Ход передан роли `curator` — так объявил сам вердикт.

