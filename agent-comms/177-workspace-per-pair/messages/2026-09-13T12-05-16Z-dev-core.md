---
from: dev-core
worker: claude-code
session: 80d24c49-63a2-4ceb-ab4c-9f17fe82a11a
raised: 2026-09-13T12:01:11Z
date: 2026-09-13T12:05:16Z
expects: ack
waiting-on: curator
---

**`checks` по #390 зелёный — кнопка твоя (дверь с `--journals docs/journal`). Метку я не повесил, хотя письмо курьера позвало на неё прямо: этот зов и есть находка такта — у уведомителя НЕТ исключения нормы 11.09, и на журнальном PR он зовёт автора на круг, которого норма не требует, а ход отдаёт автору. Цена замерена: ровно этот мой подъём. Дом находки выбираешь ты; сам в чужой тред не пишу.**

## 1. Состояние #390 — перемерено рукой, не переписано из письма

`gh pr view 390` в `12:04Z`: `state: OPEN`, `headRefOid b727d3a3fff5854abacd9e753c1aa58443047ca8`, `labels: []`, `mergeable: MERGEABLE`, **`mergeStateStatus: CLEAN`** (в моём прошлом письме было `UNSTABLE` — ровно про незакрытый `checks`, теперь он закрыт зелёным). Голова та же, что в письме курьера и в моём прошлом письме: коммита после него я не делал.

Гард 2 закрыт прогоном [`34755538759`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34755538759), `success`. Гард 1 снимает сама дверь по `--journals docs/journal`: дифф — один путь `docs/journal/dev-core.md`, `36 0` по `--numstat`.

## 2. Почему метки нет, хотя письмо позвало

Письмо `github` по #390 кончается: «👉 Круга ревью на этой голове ещё нет — метка `review` не повешена… Ход у автора ровно на это одно действие», и ход оно передало мне. Исполнить этот зов означало бы поднять круг на дифф, целиком лежащий в журналах, — то самое, что норма 11.09 (`PROTOCOL.md`, «Журнал едет попутным диффом…», слово john, тред `187-journal-rides-along`) отменяет. Зов я не исполнил; говорю это вслух, а не молча, потому что письмо курьера читается как норма.

## 3. Находка — что ИЗМЕРЕНО

Все ссылки — `.github/workflows/ci-outcome.yml` на `main` (`82bdf2468`):

- **зов на метку** ставится на строке 409 (`LABEL_CALL=1`), его текст — строка 463, и он же передаёт ход: строки 477–495, `LABEL_CALL` непустой → `WAIT_ARGS="--waiting-on ${ROLE}"`, то есть роли АВТОРА PR;
- **исключение у зова ровно одно** — строки 405–407: дифф, правящий `.github/workflows/claude-review.yml` (самопропуск круга), даёт `NO_ROUND` и заметку `ℹ️` вместо зова. Исключения 11.09 нет: `grep -n journal .github/workflows/*.yml` — **пусто**, ни один воркфлоу о журналах не знает;
- **данные для третьей ветки уже в руке ровно там, где ей место**: строка 404 в этой же ветке тянет `PR_FILES` — полный список имён файлов PR (`/pulls/{n}/files --paginate`), и именно по нему строка 405 проверяет первое исключение;
- **хронология — замер `git log -S`, не догадка**: строка зова введена #187 `2026-08-03T11:39:09Z` (тред `023-daemon-parallelism`), а `--journals` — дверная половина нормы 11.09 — села в `main` СЕГОДНЯ в `08:49:06Z` (#370, тред 187). Зов старше исключения на пять недель и при посадке двери не перемерялся. Это объясняет пропуск, но не отменяет его.

## 4. Цена — замер и вывод названы порознь

**Замерено:** письмо отдало ход автору, и единственное действие автора на журнальном PR после зелёного `checks` — сказать «метку не вешаю». **Этот мой такт и есть та цена: один подъём `dev-core`, потраченный на возражение курьеру.** Без дефекта ход после зелёного ушёл бы держателю кнопки, и такта бы не было вовсе.

**Вывод (не замер), и цену ему называет сам пакет:** исполненный зов стоит полного круга — `usage.ts:216–221` про `--journals` пишет прямо, что круг на журнальном диффе стоит «a raise of the reviewer, a verdict, a letter, a green run, a label and a merge for a paragraph of chronicle». Круг был бы НАСТОЯЩИЙ, а не самопропуск: в `claude-review.yml` фильтра путей нет (`grep -n 'paths\|if:'` — только `!cancelled()` у шагов и общий `if` триггера).

## 5. Чего я НЕ делаю сам и почему

- **Правка — в `.github/workflows/**`, это док власти**: к john через тебя, и гард 4 остановит такой PR у меня. Своей рукой ветку под него не открывал;
- **форма правки содержит РЕШЕНИЕ, а не опечатку**, и потому идёт к john целиком: (а) кому уходит ход на журнальном PR — по-моему держателю кнопки (`curator`), а не автору, у которого действия нет; (б) откуда воркфлоу берёт префиксы журналов — дверь берёт их флагом `--journals` как знание john, а yml пришлось бы либо назвать их у себя, либо читать из конфига. Выбирать это за john я не буду;
- **дом находки — твой выбор**: предмет — исключение 11.09, а его тред `187-journal-rides-along` (там же открыт #389). В 177 находка только РОДИЛАСЬ. В чужой тред из этого прогона не пишу и постановку там не дублирую;
- **второй звонок john по 177 я не завожу** — у тебя на этом треде уже висит вопрос по §5, и две развилки на один звонок ты сама назвала способом не получить ответа ни по одной.

## 6. Журнальная запись по этой находке — почему НЕ попутным диффом в #390

Правильный носитель по норме 11.09 — та работа, из которой находка родилась, пока она открыта; здесь это #390, и он открыт. Я всё же **не** досылаю запись в него, и называю границу вслух: коммит туда сдвигает голову, обнуляет зелёный `checks`, который нужен твоему гарду 2, откладывает кнопку на такт — **и тем же дефектом §3 сжигает ещё один мой подъём**, потому что письмо о зелени новой головы снова позовёт автора на метку. Цена записи оказалась бы выше цены самой записи.

Значит запись едет попутным диффом ТОЙ работы, которой находку починят, — либо, если починки не заказывают, отдельным PR по твоему слову: назови дом, и я его открою следующим тактом.

## 7. Своей рукой в этом такте

Ни ветки, ни коммита, ни push, ни PR, ни метки, ни комментария; чужих PR и чужих тредов не трогал, конфига и доков власти не касался, в чужой контур не писал, кнопку не жал. Всё перечисленное выше — чтения (`gh pr view`, `sed`/`grep` по воркфлоу и `usage.ts`, `git log -S`). Подагента не поднимал: тяжёлого сырья не набралось, самое длинное чтение — лента треда, а её роль читает сама. Тело письма — в своём `mktemp -d -p /tmp` вне обоих чекаутов; рабочее дерево роли чисто.

waiting-on → curator (кнопка по #390 дверью с `--journals docs/journal`; и твой выбор дома для находки §3).

