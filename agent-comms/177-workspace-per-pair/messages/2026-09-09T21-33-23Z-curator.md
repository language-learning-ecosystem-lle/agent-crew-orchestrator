---
from: curator
worker: claude-code
session: 70f8f95b-2d94-41a2-8ee8-18f752b3e9df
raised: 2026-09-09T21:26:46Z
date: 2026-09-09T21:33:23Z
expects: ack
waiting-on: curator
parked-on: john
---

**ТРЕБУЕТСЯ РЕШЕНИЕ JOHN: параллельность включается в ПОЛЕ следующим сливом — ящик начинает поднимать до 2 пар на роль и до 3 сессий разом, на числах, которые ты уже нажал (#356). Нажимать больше нечего: последний код сел в `main` этой кнопкой. Нужно одно твоё слово: 2/3 остаются — или назови другие числа, и PR по конфигу откроет dev-core.**

Это ровно тот момент, который ты потребовал держать видимым (msg-004 §2): расход умножается не планом, а сегодняшним мержем, и умножается он с точностью до слива.

## 1. #366 СМЁРЖЕН моей рукой — пять гардов поимённо

Squash `9935a9b0c14a67ad5ea92e897f9db69a00388aa6`, `2026-09-09T21:30:00Z`, кнопка нажата с `--match-head-commit 95e1c59f461089f33c32f86c503c5a2b6be8e286`. Контрольное чтение ПОСЛЕ (ответ инструмента ≠ факт): `state: MERGED`, `baseRefName: main`, голова `origin/main` = тот же squash.

- **гард 1 — ok, замерено дверью:** `approve` на `95e1c59f461089f33c32f86c503c5a2b6be8e286` внутри ЗАКРЫТОГО круга `Claude PR Review` **`34406417365`** (`event=pull_request`, окно `21:20:59Z…21:26:17Z`). Вердикт лежит в этой ленте письмом `reviewer-pr` (`verdict: approve`, `pr: 366`, `waiting-on: curator`) и назван прогоном в своём хвосте. Мёртвый круг `34402435394` (429, `total_cost_usd: 0`) якорем не является и не засчитан — вердикта он не создал вовсе; годен ровно один круг, и он на текущей голове;
- **гард 2 — ok:** на ТОЙ ЖЕ голове `95e1c59f461089f33c32f86c503c5a2b6be8e286` два зелёных чека — `checks=SUCCESS` (прогон **`34401571104`**, закрыт `20:39:39Z`) и `review=SUCCESS`. Сорок символов сверял не глаз, а дверь;
- **гард 3 — восхождение к решению john в ЭТОЙ ленте, перечитано моей рукой, а не взято из вердикта:** msg-004 (слово john полем `delivers`, чат 2026-09-08 ~16:18Z — потолок 2 пары на роль, цена кругов ревью принята, ручной запуск возвращается), и постановка msg-005 объявляет это основанием для ВСЕХ PR треда дословно. **Класс «полевой измеренный дефект» не объявлялся и не применялся** — он здесь не нужен;
- **гард 4 — ok:** 8 изменённых путей, ни один не док власти. Список доков власти дверь вывела ДАННЫМИ (8: `agent-protocol.json`, четыре карточки ролей, `REVIEWER.md`, `PROTOCOL.md`, `.github/workflows`), а не из моей памяти;
- **гард 5 — это письмо.**

**Ноты о переезде базы у этого PR не было, и это замер, а не отсутствие внимания:** `mergeStateStatus` перед кнопкой — `CLEAN`, а голова `main` до моей кнопки (`61a79818f`, #362, `20:29:32Z`) старше старта зачтённых `checks` (`20:31:46Z`). База после зачтённой зелени НЕ двигалась — значит полной сюиты на слитом дереве здесь не требуется, в отличие от #362.

## 2. Что этим село и почему это «включение», а не ещё один PR

Прогон садится в дерево ПАРЫ `<role>@<thread>` при потолке выше единицы (при 1 путь бит-в-бит прежний), и три поверхности — `preflight`, `doctor`, `status` — перестали предсказывать деревья по списку ролей и читают диск. Последняя из них не витрина, а дверь: `probeSigningPlaces` молча проходила мимо несуществующего пути и осталась бы зелёной, ничего не проверив.

Стек включения после этого мержа полон, и каждый кусок назван фактом:

1. **числа — в конфиге** (`parallelism: { pairsPerRole: 2, pairsPerInstance: 3 }`, кнопка john, #356 = `a6641fd17`, тред 179);
2. **такт считает к ним** (#355);
3. **кадр называет МЕСТА, а не роли** (#362 = `61a79818f`);
4. **прогон живёт в дереве пары** (#366 = `9935a9b0c`) — сегодня.

## 3. Чего от john НЕ требуется, чтобы он не отвечал на это дважды

Моя рекомендация `pairsPerInstance: 4` (замер: пик одновременных сессий на ящике 4, достигнут один раз 2026-09-03T00:25:19Z; три и больше — 0.2 % времени эры) **уже отвечена его кнопкой: он нажал 3**. Второй раз её не приношу и не переспрашиваю. PR из одной строки конфига не открывается ни по чьей руке, пока john не назовёт другое число.

## 4. Поле — замер, а не обещание

Обязательство «ящик подтянул `main`» я записывала пунктом этого звонка. Замер на `21:30Z`: чекаут демона (`/home/lle/projects/agent-crew-orchestrator`) стои́т на **`61a79818f`** — #362 в поле исполняется, **#366 ещё нет**. Слив за моей кнопкой подтягивает `main` сам, но происходит он ПОСЛЕ конца этой сессии, поэтому замерить его я не могу и не выдаю за сделанное.

**Обязанность следующего такта curator, и она названа здесь, а не всплывёт потом:** прежде чем считать параллельность включённой, прочитать `git -C /home/lle/projects/agent-crew-orchestrator rev-parse HEAD` и увидеть в нём `9935a9b0c` или новее. Смёрженное ≠ работающее: демон исполняет исходники чекаута.

## 5. Цена этого парка, названная прямо

Парк на john замораживает тред целиком, и вместе с ним замирает **§3.4** (ручной запуск на названный тред) — PR dev-core, который по порядку идёт сразу после посадки #366. Плачу эту цену сознательно: §3.4 ничего не включает и не выключает, а слово о деньгах опаздывать не должно — жёсткая запись этой ленты («звонок после посадки ОБОИХ PR, кадра и включающего») исполнена сегодня, и переносить её третий раз значит дать ей протухнуть.

**Как только слово john приедет — парк снимаю своей рукой тем же ходом и передаю §3.4 dev-core.** Ждать ответа в прогоне я не остаюсь.

## 6. Что остаётся и за кем

1. **john** — слово по §0/§3: 2/3 остаются или другие числа;
2. **curator (я)** — снять парк словом john, передать §3.4 dev-core, перемерить чекаут демона по §4;
3. **curator (я)** — постановка по находке старевшего парка на треде `155-park-has-no-checkable-ground`, отдельным ходом на ТОМ треде (в 177 PR по ней не открывается — это записано и не двигается);
4. **dev-core** — §3.4 после снятия парка.

**Хвост открытых PR замерен сейчас, а не перенесён:** `gh pr list --state open` в `21:33Z`, ПОСЛЕ кнопки — открытых два, #367 (тред 186) и #353 (тред 180). Оба принадлежат чужим тредам: их кнопки — такты своих тредов, отсюда не трогаю, потому что след гарда 5 обязан лечь в ленту треда-владельца, а этот прогон связан одним тредом 177.

## 7. Своей рукой в этом такте

Записи в мир: **две** — merge #366 и это письмо. Ветку не заводила, метки не вешала, тела PR не правила, в чужие треды и чужой контур не писала; текст squash — мой (`--subject`/`--body-file` на кнопке), тело PR не тронуто.

Чтения: `thread show --for curator` (лента своей рукой; msg-004 и msg-005 перечитаны блоками ради гарда 3, а не взяты из вердикта), `merge-gate --review-workflow 'Claude PR Review'` (вывод целиком), `gh pr view 366` до и после кнопки, `gh pr list --state open`, `git show origin/main:agent-protocol.json` (блок `parallelism`), `git show` коммита #356 по `agent-protocol.json`, `git rev-parse HEAD` чекаута демона, `git log -1 origin/main`.

Тело письма и текст squash — в своём `mktemp -d -p /tmp` вне обоих чекаутов. Рабочее место `curator`: `git status --porcelain -uall` пуст, незакоммиченного нет. Подагента не поднимала: тяжёлое сырьё (лента 3156 строк) читалось в файл и резалось по номерам строк — в контекст ехали блоки, а не выжимка чужой рукой.

waiting-on → curator (ход остаётся у меня, вопрос — к john).

