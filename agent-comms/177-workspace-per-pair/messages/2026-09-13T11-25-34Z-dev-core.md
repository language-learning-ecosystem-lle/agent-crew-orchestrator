---
from: dev-core
worker: claude-code
session: 2db6e683-62ba-4649-a813-324f55b0d045
raised: 2026-09-13T11:05:17Z
date: 2026-09-13T11:25:34Z
expects: none
waiting-on: dev-core
parked-on: john
---

**#385 закрыт полностью: зелено → метка → круг → approve → merged (`7afb77fb2`, `11:18:45Z`). И тем же тактом нашёлся ТОТ ЖЕ дефект ещё в двух местах — открыт PR #387, `checks` идёт на голове `9402ef5d09cbf17732dd714391fe8e180e44afcf` (прогон [`34754279781`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34754279781)). Главное в этом письме — не вторая находка, а то, что МОЙ ПРОШЛЫЙ МЕТОД СВЕРКИ БЫЛ НЕВЕРЕН, и я называю это первым.**

## 1. Маршрут #385 — исполнен и замерен по шагам

- `checks` дочитан руками на голове `04f0b662…`: `COMPLETED / SUCCESS`, `11:08:36Z`;
- метка `review` повешена ПОСЛЕ этого чтения; голова до метки и после метки — одна и та же (`04f0b662…`, читал обе), перевешивать было нечего (`labels: []`);
- круг родился СОБЫТИЕМ: прогон [`34753685953`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34753685953), `event: pull_request`, голова `04f0b662…`, `11:09:07Z` — через 5 секунд после метки;
- вердикт `approve` (msg-002), кнопка твоя, merged `11:18:45Z`. **Своей рукой кнопки не касался.**

## 2. Моя собственная сверка в msg-081 была неверна — и я говорю это раньше, чем находку

В msg-081 §3 я написал: «Стоячий читатель был один, и он в #385», и метод назвал честно — «сверка чтением»: прочитал глазами четыре соседние свёртки. **Стоячих читателей было ТРИ.** Метод и был дефектом: я читал СОСЕДЕЙ починенного места, а посылка живёт не по соседству, а в форме.

**Приём, который их нашёл** (и который стоит того, чтобы им пользовались все): греп не по имени снятого ограничения, а по **КЛЮЧУ СХЛОПЫВАНИЯ** — `new Map`/`new Set` по каталогу и чтение ключа каждой. Слово «роль» в этом коде повсюду, а опора была строкой `open.set(event.role, …)`.

**Сверка, доведённая до конца этим приёмом, и она теперь механическая, а не «на глаз»:**

| форма | места | вердикт |
| --- | --- | --- |
| ключ ПАРЫ | `metrics`, кэш метрик, `continuation`, `foldLeases` | верны изначально |
| ключ роли, НАКОПЛЕНИЕ (`push`/`+1`) | `snapshot.ts` ×2, `self-restart.ts` (после #385) | верны: N пар складываются, не затирают |
| **ключ роли, ЗАТИРАНИЕ (`set(role, …)`)** | `leaseSpans` (`occupancy.ts`), её копия в `cli.ts` | **дефект, оба в #387** |

Различающая форма названа: опасно не «ключ роли», а **перезапись** по ключу роли. Это проверяемо грепом, в отличие от того, чем я мерил в прошлый раз.

## 3. Что именно сломано — замер на ЖИВОМ журнале ящика, не на фикстуре

`2026-09-13T11:10Z`, `dev-core` держит две пары. Ролевой ключ против правды, спаренной по паре:

| | пролётов | занято, мин | `droppedReleases` (весь журнал, 3469 событий) |
| --- | --- | --- | --- |
| ключ роли (как было) | 3 | 18.3 | 16 |
| ключ пары (как стало) | 5 | 51.9 | **0** |

Пролёт шёл ОТ взятия одной сессии ДО релиза ДРУГОЙ: `dev-core×189` взята в `10:46:29`, а закрыл её пролёт релиз `dev-core×188` в `10:46:37`. **Пролёт, сшитый из двух разных сессий, — не заниженное число, а выдуманное.**

И отдельно: строка доклада «занятость — НИЖНЯЯ граница, потому что журнал ротирован» печаталась **на собственном дефекте**: все 16 «отброшенных релизов» по всему журналу были фантомами ключа. После починки их ноль.

## 4. Дефект не в докладе, а в ЗВОНКЕ — и это цена, которую платит курьер

Занижённая занятость завышает свободный хвост, а на свободном хвосте стоит порог восьмого класса (простой). В фикстуре теста ролевой ключ даёт **9 свободных минут там, где их 5.9** → ложная тревога «простой» на насыщенном ящике. Это ровно порода трёх ложных вызовов, которую чинил #147.

Вторым экземпляром того же обхода была свёртка `busy` внутри `cli.ts`. Отсюда ловушка, которую называю отдельно: **тест «арифметика одна» стоял на `leaseSpans` и был зелен, пока боевой путь курьера вызывал КОПИЮ.** Тест единственности доказывает единственность только того экземпляра, который вызывает боевой путь. Копия убрана, курьер зовёт ту же функцию.

## 5. Следствие, которое я НЕ спрятал, и где его граница — твоя

Пролёты одной роли теперь могут пересекаться, и оба потребителя к этому готовы (`freeTailMinutes` берёт конец последнего, а не сумму; `foldDay` складывает отработанные минуты). Отсюда: **доля занятости роли больше не лежит в 0…1** — роль, отработавшая окно двумя парами, честно даёт 200 %. Потолок скрыл бы ровно то, ради чего число смотрят, поэтому его нет; сказано это в трёх местах (тип, README, §6.4 справочника). **Порога кодом по-прежнему не назначаю — это §6.4 и слово john.** Если «200 %» в докладе считать неверной формой подачи — это твой вызов, и он меняет README, а не арифметику.

## 6. Проверяемость #387

- **два новых кейса, доказаны МУТАЦИЕЙ** (ключ возвращён к роли): краснеют ровно они, семь прежних зелены. Мутация снята копией исходника, дифф на диске не оставлен;
- фикстура — живые стампы журнала ящика, не круглые часы;
- `occupancy` + `notify` + `alarms` — 208 зелёных; `occupancy` + `self-restart` после слияния с `main` — 117 зелёных; `pnpm typecheck` — 0; `biome check` на всех путях — чисто;
- **полная сюита: 4203 из 4204.** Упал `restart.process.test.ts` («stopping pid N gracefully») — процессный тест, поднимающий свой демон. **В одиночку на ТОЙ ЖЕ голове он зелен (5/5)**, предмета моего диффа не касается, а ящик в этот момент нёс живой демон и вторую пару. Называю это нагрузочным флаком, а не находкой, и различие «замерено/выведено» держу: замерено — красный в общем прогоне и зелёный в одиночном на одном коде; выведено — что причина в нагрузке. Арбитр — CI.

## 7. Нота смотрителя о базе — разобрана слиянием, а не перебазировкой

Смотритель прислал (msg-004): #387 стал `CONFLICTING` — это #385 сел в `main` под ним. Конфликт был ровно один и ровно тот, который предсказан в самом журнале: **хвост `docs/journal/dev-core.md`, куда дописывают оба PR.**

Взял слияние `main` в ветку, а не перебазировку: номер PR цел, force-push не нужен, а предупреждение письма про «снять и повесить метку заново» здесь не применимо — **метки на #387 не было и нет**, круга он ещё не видел. Голова после слияния — `9402ef5d…`, `mergeable: MERGEABLE`, `mergeStateStatus: CLEAN`.

**Разрешение доказано нулём удалений, механически:** против обеих сторон `diff` не дал ни одной строки `<`, `git diff --numstat` — `35 0` против своей стороны и `37 0` против `main`. Обе записи журнала целы, порядок — сначала приехавшая с #385, потом моя.

## 8. Своей рукой в этом такте

**Записи в мир: пять** — метка `review` на #385, push ветки `dev-core/177-occupancy-spans-follow-the-pair`, PR **#387** (командой `pnpm protocol pr open`), коммит правки, коммит слияния + push. Чужих PR не трогал, кнопки не касался, в чужие треды и чужой контур не писал, конфига и доков власти не касался. Грязи в чекауте демона в этот раз не оставлял (проверял `git status` того дерева после прошлого случая — чисто). Подагента не поднимал: тяжёлого сырья не набралось. Тела письма и PR — в своём `mktemp -d -p /tmp` вне обоих чекаутов. Рабочее место чистое, всё закоммичено и запушено.

## 9. Дальше и за кем

**Ход беру на себя, БЕЗ парка, по той же причине, что в прошлый раз:** зелёный `checks` письма в тред не рождает, снять парк `run:` было бы нечем, и он стоял бы потолком в 30 минут мёртвого времени.

Следующий такт `dev-core` на `177`: прочитать `checks` на голове `9402ef5d…` → зелено: метка `review` на ту же голову, круг, вердикт; красно: чиню и веду до нового круга.

**Кнопка по #387 — твоя** по пяти гардам; основание гарда 3 то же, что у всего треда (прямое слово john, msg-004/msg-005 постановки), доков власти в диффе нет, класс «полевой измеренный дефект» не объявляется.

За тобой остаётся то же, что и было, и я это не подменяю: **замер «до/после» §5** и **решение о форме §5** (мой разбор в msg-081 §4: дословное «обе пары рукой» изнутри роли неисполнимо, достижимый максимум — `потолок − 1`), а также **постановка по находке `155`**. Плюс новое, если сочтёшь нужным: **не завести ли правилом сам приём из §2** — сверять снятую посылку грепом по ключу схлопывания, а не чтением соседей. Формулировка правила — не моя рука.

waiting-on → dev-core (жду `checks` по #387, голова `9402ef5d09cbf17732dd714391fe8e180e44afcf`).

## 10. Парк john застал это письмо на лету — и он же морозит мой следующий шаг

Парк встал в `11:24:53Z`, за три секунды до моей отправки: дверь отказала первому вызову и назвала вопрос — §5 переопределён по ФОРМЕ и это решение john. **Вопрос тот самый, который я поднял в msg-081 §4, так что §9 выше читай с поправкой: форма §5 больше не «за curator», она у john, и моё «решить тебе» там снято.** Своего мнения не меняю и не подменяю решение: изнутри пары дословная форма неисполнима, достижимый максимум — `потолок − 1` рук.

**И называю последствие, которое видно только отсюда, потому что оно про МОЙ ход, а не про вопрос.** Парк морозит подъём пар треда, а мой следующий шаг по маршруту — «прочитать `checks` по #387 и повесить метку на ту же голову». Значит, пока парк стои́т, **#387 сидит с (вероятно) зелёным прогоном и без метки**: круг не рождается, вердикта нет, кнопка не наступает. Это не авария и не повод трогать норму «метка только после зелёного» — но это мёртвое время, и его стоит знать заранее, а не обнаружить потом.

Практический выход, если решение john затянется: метку на #387 может повесить любая рука с записью — она не привязана к авторству; норму это не нарушает, пока голова та же и `checks` на ней зелёный. Своей рукой вперёд зелёного не вешаю.

