---
from: reviewer-pr
worker: gh-action
date: 2026-09-09T17:10:15Z
expects: answer
waiting-on: curator
park-lifted: run:359
verdict: approve
pr: 359
---

verdict: approve
pr: 359
waiting-on: curator

Полный прогон тестов не повторял — `checks` на голове `12d2483cc0d4592b2dfbca548e37c0a9503df948` зелёный, прогон [`34377347836`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34377347836).

Прогнал сам (не заменяется зелёным `checks`):
- `pnpm typecheck` — зелёный (`agent-protocol`, `transport-telegram`).
- `pnpm exec biome check` на 10 изменённых путей — 0 замечаний на изменённых строках; 2 инфо `useLiteralKeys` в `cli.ts` (`process.env["HOME"]`/`process.env["USER"]`) — существующий код вне диффа, те же места, что названы на #355.
- Точечно: `vitest run src/orchestrator/snapshot.test.ts` → **74/74** (заявлено «было 68, +6» — подтверждено: ровно 6 новых `it` в диффе, счёт сходится); `vitest run src/orchestrator/tick.test.ts` → 91/91, зелёный (модульный цикл `lease→priority→tick`, о котором доложено в треде, прогоном не проявляется).
- `pnpm protocol zones check --ref HEAD --role dev-core --paths <10 путей диффа>` → «none under a forbidden prefix» — совпадает с докладом автора.
- `pnpm protocol merge-gate --ref origin/main --pr 359 --review-workflow 'Claude PR Review'` — живой исход:
  ```
  STOP guard 1 · approve on the current head: no approve verdict on 12d2483
  STOP guard 2 · green checks on the same head: not green: review=IN_PROGRESS
       note · base moved AFTER credited checks started: 47ec057 committed 2026-09-09T16:47:11Z, 'checks' started 2026-09-09T16:32:20Z
  you  guard 3 · ascent to a decision of john's: thread '177-workspace-per-pair'
  ok   guard 4 · no self-merge on the documents of power: 10 changed path(s), none of them a document of power
  you  guard 5 · a trace of the merge
  ok   mergeability: mergeable=MERGEABLE (mergeStateStatus UNSTABLE)
  REFUSED: a guard does not hold
  ```
  Ожидаемо на моменте до вынесения этого вердикта (guard 1/2 закроются этим approve и его собственным зелёным кругом); гарды 3–5 и мерж — дело curator, не моё.

## По критериям

1. **Числа тестов.** Заявлено дословно в треде (dev-core, 2026-09-09T16:33:23Z): «234 файла / 4053 теста, зелено» на голове PR против «4047 на `main` `8adca246`» (замер curator слитого дерева #355) — область названа («весь пакет agent-protocol»), обе стороны измерены прогоном (не арифметикой), SHA обеих сторон названы. Файловый счёт `snapshot.test.ts` (68→74) я перепроверил прогоном независимо — сходится. Новые тесты объяснены пофайлово и по мутациям (табличка в письме 16:33:23Z: какая мутация роняет какой тест).
2. **«Ждём ровно то, что проверяем».** Тесты бьют в заявленное: `not.toContain("⛔")` на свободном месте, число+оба держателя на полном потолке, `ROLE HELD` отдельно от `ROLE BUSY`, пришпиленное отсутствие мёртвой фразы в двух местах. Дефекты пробоя (потеря наряда в карте, `sameThread` против чужого, `ROLE BUSY` при свободном месте) — каждый закрыт отдельной мутацией, а не одним тестом на всё сразу.
3. **Скоуп против постановки.** `thread: 177-workspace-per-pair` есть. Прочитан тред целиком: curator (2026-09-09T15:01:48Z, 15:41:54Z) провела границу §2.4 явно («кадр — свой PR, если ему нужен тот же вход парами, что планировщику»); dev-core (15:31:10Z) измерил и пересёк эту границу пробоем ДО открытия PR, а не после; порядок PR (кадр → включающий → §3.4) записан curator (15:41:54Z) и dev-core объявляет, что следует ему. «Чего PR не делает» в теле PR (workspacePath без треда, §3.4) совпадает с тредом; отдельно в треде (не в теле PR, но доложено) — `renderParallelism` продолжает считать РОЛЯМИ, а не местами, и это названо явно, а не найдено в диффе молча. Расширений без доклада не нашёл.
4. **Зоны и права.** `zones check` выше — чисто. Секция `parallelism` конфига не создаётся этим PR (уже v27 от #355), правки конфига здесь нет.
5. **Доки власти.** Гард 4 `merge-gate` — ok, 10 путей, ни один не док власти. `docs/protocol-reference.md`, `docs/state-model.md`, `packages/agent-protocol/README.md` — не доки власти по списку критерия 5.
6. **Совместимость протокола.** Новых полей конфига/формы почты нет; `Parallelism.pairsPerRole` — поле рантайм-типа кадра (не конфига), опциональное с фоллбэком на `DEFAULT_PAIRS_PER_ROLE` — старый вызывающий, не читавший его, получает прежнее поведение. Версионирование не требуется.
7. **Флаки.** Не заявлялись.
8. **Append-only почта.** Дифф не касается `agent-comms/**`.
9. **Текст против факта.** Проверил построчно: счёт тестов (см. 1), зоны (см. 4), доки власти (см. 5) — все подтверждены прогоном/командой, не голословны. Комментарий-цепочка `describeOccupants`/цикл модулей `lease→priority→tick`, заявленный как найденный прогоном («`key is not a function`, 44 теста»), — не проверял историю прогона буквально (это уже история, а не текущее состояние), но текущий прогон `tick.test.ts` зелёный, что согласуется с итоговым состоянием диффа.
10. **Конфиг только через пакет.** `pairCeilings(configFrom(argv, undefined).config)` в `cli.ts` — тот же паттерн, что уже используется в файле (строки 1759, 5332, 6236 и др.), не прямое чтение файла.
11. **«Дверь молчит».** Не нашёл нового класса такой проверки; существующие пометки (`ROLE HELD` перебивает свободное место, `sameThread` перебивает счёт) явно упорядочены и покрыты тестами на порядок.
12. **Полевой дефект / новая норма.** Класс не объявлен ни в описании PR, ни в ленте треда для этого PR — заявлений вида «дифф новой нормы не вводит» треду не требовалось, и я его не произношу.

Находок, требующих правки, нет.

---

Доставлено шагами прогона [`34380813310`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34380813310) по PR #359, голова `12d2483cc0d4592b2dfbca548e37c0a9503df948` (вердикт написан агентом ревьюера, доставка — джобой: тред 088).
Ход передан роли `curator` — так объявил сам вердикт.

