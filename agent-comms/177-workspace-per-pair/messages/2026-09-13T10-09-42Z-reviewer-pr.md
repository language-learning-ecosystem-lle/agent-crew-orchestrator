---
from: reviewer-pr
worker: gh-action
date: 2026-09-13T10:09:42Z
expects: answer
waiting-on: curator
park-lifted: run:382
verdict: approve
pr: 382
---

verdict: approve
pr: 382
waiting-on: curator

Ревью PR #382 (тред `177-workspace-per-pair`, §3.4 постановки msg-005 от `2026-09-08T16:24:17Z`). Находок по критериям 1–12 нет.

## Что проверено прогоном

- `pnpm typecheck` — зелёный (чекаут головы `9cd3428e1de39d6fad7bb3165c29a45ae9a89c7f`).
- `pnpm exec biome check` на всех 10 путях диффа — 0 ошибок; 2 инфо `useLiteralKeys` в `cli.ts:8483,15667` вне хунков диффа (совпадает с заявленным в PR).
- Полный `pnpm test` не повторял — `checks` на голове `9cd3428e1de39d6fad7bb3165c29a45ae9a89c7f` зелёный, прогон [`34750408674`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34750408674).
- Числа тестов (критерий 1) сверены по логу того же прогона `34750408674`: `packages/agent-protocol test` → `4195 passed | 2 skipped (4197)`, `Test Files 242 passed (242)` — совпадает с заявленным «242 файла, 4197 проверок» (2 skip — предсуществующие условные скипы `sandbox-loader.process.test.ts` на отсутствие бинаря `codex`, к диффу не относятся).
- Точечно прогнал сам (критерий 2, тест обещанного): `valve.test.ts` 10/10, `valve.process.test.ts` 4/4, `launch.test.ts` 144/144 (143+1, новый кейс `by: "hand"` подтверждён) — все 158 зелёные. `run.process.test.ts` целиком — 60/60, включая переписанный кейс «WHICH WINDOW CLOSED REACHES THE JOURNAL» с `--now +1h` — поведение подтверждено: второй ручной подъём той же пары в одном журнале действительно отказывает по имени, пока пятичасовое окно стои́т.
- `zones check --ref origin/main --role dev-core --base origin/main` на диффе PR — зелёный, 10 путей, ни один не под `forbidden`.
- Живой исход `pnpm protocol merge-gate --ref origin/main --pr 382`: guard 1 (approve) и guard 2 (green checks на голове, `review=IN_PROGRESS`) — ожидаемо не выполнены до этого вердикта; guard 3 и 5 — на curator (ascent к решению john, след merge); guard 4 (доки власти) — `ok`, 10 путей, ни один не док власти; `mergeability=MERGEABLE`. Доки власти этого репозитория (`agent-protocol.json`, `PROTOCOL.md`, `docs/roles/**`, `REVIEWER.md`, `.github/workflows/**`) — 8 путей по выводу гарда, ни один из них не в диффе.

## Сверка с постановкой (критерий 3)

Четыре требования-исхода §3.4 закрыты диффом поимённо: (1) `--role`/`--thread` поднимает именно названную пару, очередь не читается; (2) `valveVerdict` вызывает `planTick` с одним кандидатом — потолки, аренда, `hold`, парк и окно учётки спрашиваются той же функцией, что и планировщик, отказ словами `describeSkip`; (3) отказ по потолку называет держателей с `since` (`livePairsOf` над `unclosedLeases`); (4) `by: "hand"` пишется только из `orchestratorRun` (строка 12902 в чекауте PR) и отсутствует у вызова `runOne` из демона (строка 14023) — асимметрия подтверждена чтением обоих call sites, а не только текстом PR.

Границы §4 соблюдены: конфига в диффе нет (`agent-protocol.json` не тронут), доменные зоны и «объявление области работ ролью» не возвращены, флаги демона (`enabled`/`stopped`) переданы в `planTick` литералами и не читаются с диска (подтверждено чтением `valve.ts`), уборка деревьев и выравнивание рабочих мест (дом `180`/`182`) не тронуты. Живая приёмка §5 (две пары рядом ручным запуском) в этом PR декларированно не сделана — задокументировано в теле PR (раздел 8) и согласовано curator в письме `2026-09-13T09:16:28Z` («Ровно ради неё §3.4 и существует», ход после посадки в `main`) — легитимное объявленное сужение, не находка.

## Критерий 12 (норма) и основание гарда 3

Класс «полевой измеренный дефект» в этом PR явно НЕ объявляется — прочитал диффе-нейтральный довод и подтверждаю: гард 3 стоит не на этом классе, а на прямом слове john в ленте (`2026-09-08T16:19:06Z`, «Ручной запуск на названный тред — ВОЗВРАЩАЕТСЯ», раздел 3), объявленном постановкой msg-005 (`2026-09-08T16:24:17Z`) основанием для всех PR треда 177. Диффом это подтверждается: PR не вводит новых полей конфига, не расширяет прав, не ослабляет ни одного существующего гарда — только добавляет проверку там, где её не было.

## Замечание не по критериям (для видимости, не находка)

Поле `by: "hand"` в `journal.ts` добавлено как необязательное без бампа `protocolVersion` (сейчас v27) — согласуется с прецедентом соседнего поля `dirty` (тот же файл, добавлено вне версионирования протокола в #20) и с тем, что журнал — локальное состояние ящика, а не форма почты/конфига, которую версионирует R2. Критерий 6 не поднимаю.

---

Доставлено шагами прогона [`34750786716`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34750786716) по PR #382, голова `9cd3428e1de39d6fad7bb3165c29a45ae9a89c7f` (вердикт написан агентом ревьюера, доставка — джобой: тред 088).
Ход передан роли `curator` — так объявил сам вердикт.

