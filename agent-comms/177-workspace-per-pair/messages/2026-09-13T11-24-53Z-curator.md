---
from: curator
worker: claude-code
session: 3ee5cdff-bb40-44b3-aaf9-d293dbc9f291
raised: 2026-09-13T11:15:15Z
date: 2026-09-13T11:24:53Z
expects: ack
waiting-on: curator
parked-on: john
---

**ТРЕБУЕТСЯ РЕШЕНИЕ JOHN: §5 приёмки я переопределила по ФОРМЕ (дословная «обе пары рукой» изнутри роли неисполнима — достижимый максимум `потолок − 1`), а твой замер «до/после» сделан и роста кругов НЕ показывает: круги на PR 1.17/1.00 против доновведенческих 1.06–1.43, медиана ожидания хода 11.4 → 8.8 мин. Подтверди приёмку — и я закрываю тред `177`; либо назови, когда перемерить, если базовый снимок для тебя слишком ранний (реальная параллельность в поле живёт один день).**

#385 смёржен моей рукой — squash `7afb77fb29805ca1c20df60a220daac881641a4c`, `2026-09-13T11:18:45Z`, кнопка нажата с `--match-head-commit 04f0b662931b15b67b0cc268483e88e154752bad`. Пять гардов поимённо ниже, §4 перемерен — чекаут демона не отстал.

## 1. Пять гардов по #385

Дверь: `pnpm protocol merge-gate --ref origin/main --pr 385 --review-workflow 'Claude PR Review'`, `2026-09-13T11:17Z`, `exit 0`, вывод прочитан целиком.

- **гард 1 — ok, замерено дверью:** `approve` на `04f0b662931b15b67b0cc268483e88e154752bad` внутри ЗАКРЫТОГО успешного круга `Claude PR Review` **`34753685953`** (`event=pull_request`, окно `2026-09-13T11:09:07Z…11:14:15Z`), вердикт лежит в этом окне. В ленте он же письмом `reviewer-pr` (`verdict: approve`, `pr: 385`, `waiting-on: curator`), «находок по критериям 1–12 нет», и в своём хвосте назван тем же прогоном. Круг родился событием навешивания метки на ту же голову — перевешивать было нечего.
- **гард 2 — ok:** на ТОЙ ЖЕ голове `04f0b662931b15b67b0cc268483e88e154752bad` два зелёных чека — `checks=SUCCESS` (прогон `34753277725`, письмо `github` в этой ленте) и `review=SUCCESS`. Сверен полный SHA, а не семь символов.
- **гард 3 — восхождение к прямому решению john, и я прочла его САМА, а не пересказом вердикта.** Решение — msg-004 этой ленты (моё письмо `2026-09-08T16:19:06Z`, слово john полем `delivers`, чат ~16:18Z: «ПРИНИМАЮ ВСЕ ЧЕТЫРЕ РЕКОМЕНДАЦИИ», §1 — потолок 2 пары на роль). Постановка msg-005 (`2026-09-08T16:24:17Z`) объявляет это основанием дословно: «Основание гарда 3 для всех PR этого треда — решение john в ленте (msg-004). Класс „полевой измеренный дефект“ здесь не нужен и не объявляется». Тем же основанием прошли #346, #347, #348, #362, #366, #382. **Класс «полевой измеренный дефект» не объявляется и не применяется; класс Д-1 в этом репозитории не применяется вовсе.** Машинная половина на месте: описание PR несёт `thread: 177-workspace-per-pair` и `role: dev-core`. Предмет PR внутри предмета треда: строка слива опиралась КЛЮЧОМ на снятое этим тредом ограничение «одна роль — одна сессия», и починка возвращает отчёт к уже объявленному поведению. Дифф `self-restart.ts` прочитан своей рукой: меняются только ИМЕНА, которые печатает слив, `live.length > 0` судит как судил — новых полей, форм сообщений, прав и шагов маршрута нет.
- **гард 4 — ok:** 4 изменённых пути (`self-restart.ts`, `self-restart.test.ts`, `docs/protocol-reference.md`, `docs/journal/dev-core.md`), ни один не док власти. Дверь вывела список доков власти данными — 8 путей (4 карточки ролей, `REVIEWER.md`, сам `agent-protocol.json`, плюс `PROTOCOL.md` и `.github/workflows`, объявленные `powerDocuments`). `docs/protocol-reference.md` доком власти не является.
- **гард 5 — это письмо.**

Контрольное чтение цели после кнопки: `state=MERGED`, `base.ref=main`, `mergedAt=2026-09-13T11:18:45Z`, голова слияния `7afb77fb…`, и `origin/main` её же и несёт (`git fetch` + `log -1`, `11:19:48Z`). Ответ инструмента сверен фактом.

## 2. §4 — чекаут демона перемерен, он НЕ отстал

`2026-09-13T11:17:30Z`, главный чекаут: `HEAD = 316507841eb0060a0a94333fda1b929fb9ebee0c` (#378), `git status --porcelain -uall` пуст. Сверено принадлежностью, а не глазом: `git merge-base --is-ancestor` даёт **ancestor** для всех трёх — `9935a9b0c` (требование прошлого такта), `14127d030` (§3.4) и `316507841`. §3.4 в исполняемом коде демона, а не только в `main`.

Твою ноту про минуту грязного чекаута демона (`11:04`–`11:05`) принимаю как названную правильно: последствие названо верное (грязное дерево — это `stand` самоперезапуска), ты же его и снял, на момент моего замера дерево чисто. Класс в твоём журнале лежит — отдельной постановки не открываю.

## 3. §5 — закрываю по существу, и называю, чем именно

**Довод принят и он не отговорка: дословная форма «две пары одной роли, поднятые ОДНОВРЕМЕННО рукой» из-под роли неисполнима по построению.** Приёмку делает сессия, которая сама занимает одно из мест; достижимый рукой максимум изнутри роли — `потолок − 1`, и клапан напечатал это в лицо, назвав держателем первого места твою же сессию (`role-busy`, `exit 2`, `11:00:24Z`, оба держателя с временем). Требование-исход «отказ по потолку называет, кто занимает места» закрыто ЖИВЬЁМ, а не тестом.

**Что засчитано за §5:** две пары одной роли работали рядом на смёрженном коде всё окно (`dev-core×177` с `10:47:17Z`, `dev-core×189` с `10:46:29Z`), в двух РАЗНЫХ рабочих местах, при не порченном общем почтовом чекауте. Это и есть существо приёмки «рабочее место на пару»; способ подъёма (рука или планировщик) к проверяемому свойству отношения не имеет. **Форму приёмки переопределяю ЯВНО и в ленте** — автор постановки я, а молчаливое переопределение приёмки это ровно то, за что роль ловит исполнителей. Поэтому оно и вынесено первой строкой к john.

**Чего НЕ намерено, и я не выдаю это за сделанное:**

1. **отказ `box-busy` (потолок инстанса, `pairsPerInstance: 3`)** — единственная дверь клапана, не проверенная в поле: ей нужны три живые пары на ящике в момент замера, а при двух сухой `orchestrator run` честно не отказывает. **Ловится даром:** чья угодно сессия, видящая три живые пары, печатает сухой `orchestrator run` на четвёртую и кладёт вывод в свой тред (записей в мир сухой вызов не делает — замерено: журнал 6294 → 6294). Постановки под это не завожу: подъём ради замера дороже замера;
2. **дословная «обе рукой»** — не исполнена, и отдельным подъёмом исполнять её я не стала: это трата окна учётки ради формы, существо которой уже намерено. Если john считает иначе — это его кнопка, и он её назовёт.

## 4. Замер «до/после» — ОБЕ половины, `2026-09-13T11:18…11:30Z`

john держал видимым один факт: **если круги ревью после включения начнут расти быстрее, чем падает ожидание, это обязано быть НАЗВАНО.**

**(а) Круги ревьюера** (`gh run list --workflow 'Claude PR Review'`, только `event=pull_request`) против смёрженных PR по дням:

| день | кругов | смёржено PR | кругов на PR |
|---|---|---|---|
| 09-03 | 56 | 41 | 1.37 |
| 09-04 | 20 | 15 | 1.33 |
| 09-05 | 12 | 9 | 1.33 |
| 09-06 | 42 | 37 | 1.14 |
| 09-07 | 18 | 17 | 1.06 |
| 09-08 | 14 | 10 | 1.40 |
| **09-09** (конфиг 2/3 нажат в `16:03:10Z`) | 33 | 23 | 1.43 |
| 09-10 | 0 | 0 | — |
| 09-11 | 3 | 0 | — |
| 09-12 | 7 | 6 | 1.17 |
| 09-13 (неполный, до `11:18Z`) | 7 | 7 | 1.00 |

Удельная цена — круги НА PR — после включения не выросла: `1.17` и `1.00` против доновведенческого разброса `1.06…1.43`. Абсолютное число кругов упало вместе с пропускной способностью (6–7 PR в день против 9–41), то есть говорит про окно учётки, а не про перебазировки.

**(б) Ожидание хода.** Считано соединением почты и журнала ящика: письмо с `waiting-on: <роль>` в треде — это момент, когда ход передан, ближайший `launch` той же пары в `.orchestrator/journal.jsonl` — момент, когда его взяли; ожидание = разница, письма схлопываются к ПЕРВОМУ непотреблённому. 1594 замера на всём журнале.

| окно | n | медиана | p90 | медиана без «замороженных» (>6 ч) | p90 без них |
|---|---|---|---|---|---|
| 09-02 … 09-09T16:03 (до) | 868 | 14.8 мин | 505 мин | **11.4 мин** | 122 мин |
| 09-09T16:03 … сейчас (после) | 142 | 19.3 мин | 1435 мин | **8.8 мин** | 68 мин |
| только 09-13 | 34 | 44.2 мин | 5315 мин | 18.3 мин | 47 мин |

**Что эти числа держат:** очередь живых ходов после включения стала КОРОЧЕ — медиана 11.4 → 8.8 мин, p90 122 → 68 мин; при этом круги на PR не выросли. **Того размена, которого john велел не растворять, в данных не видно.**

**Границы замера называю сама, чтобы им не пользовались шире, чем он держит:**

- **сырая медиана ведёт себя ОБРАТНО** (14.8 → 19.3, а 09-13 — 44.2), и разрыв между сырой и очищенной колонкой — это парки и замёрзшие треды: письмо, переданное роли, «ждёт» столько, сколько тред стоит запаркованным, хотя очередь тут ни при чём. Доля ожиданий длиннее 6 часов: 11 % до, 13 % после, **38 % в один день 09-13**. Отсекать их порогом — моя рука, и я это называю, а не прячу;
- **«после» почти пустое.** Конфиг объявил 2/3 `09-09T16:03Z`, но ДВЕ живые пары одной роли впервые замерены сегодня в `10:46–10:47Z`; `09-10` и `09-11` — дни без единого мёржа. Честное «после параллельности» — один неполный день, и он же худший по сырой медиане (в нём два слива к самоперезапуску). **Поэтому это БАЗОВЫЙ СНИМОК, а не вердикт о размене**, и вопрос о перемере — первой строкой к john;
- `gh … list --limit N` отдаёт НОВЕЙШИЕ N и усекает молча: при `--limit 400` (круги) и `--limit 200` (PR) полны дни `09-03…09-13`, ранние в таблицу не взяты.

## 5. Хвост открытых PR — сметён, своих кандидатов нет

`gh pr list --state open`, `11:19Z`: открыты **#386** (тред 180), **#384** (тред 189), **#383** (тред 188), **#379** (тред 190). **Ни один не принадлежит 177, ни на одном не висит метка `review`** — закрытого круга на текущей голове нет ни у одного, гарду 1 нечего засчитывать. Кнопку не жму и по второй причине, которая сильнее первой: **след гарда 5 обязан лечь в ленту треда-ВЛАДЕЛЬЦА**, а этот прогон связан одним тредом — 177. Их кнопки берутся подъёмом на их тредах.

## 6. Дальше и за кем

**Парк на john с `--expects ack`: вопрос первой строкой, ход остаётся у меня.** Паркуюсь, а не передаю ход, потому что без слова john осталась ровно одна развилка — приёмка переопределённой §5 и достаточность базового снимка, — и поднимать под неё пару каждый такт нечем и незачем.

- **ответ john «принято»** → следующий такт `curator` на `177`: `thread status --status closed` и всё;
- **ответ john «перемерить позже»** → следующий такт `curator` на `177`: тот же замер (а) и (б) на названную дату, обе команды в этом письме воспроизводимы дословно;
- **за мной вне этого треда** — постановка по находке старевшего парка на треде `155-park-has-no-checkable-ground`, отдельным ходом на ТОМ треде; в 177 PR по ней не открывается, как записано и не двигается;
- **`dev-core` в этом треде работы не оставлено** — §3.4 в `main` и в коде демона, находка приёмки починена и слита, ход тебе не передаётся намеренно.

Своей рукой в этом такте: **две записи в мир — merge #385 и это письмо.** Веток, коммитов и PR не открывала, меток не вешала, чужих PR и чужих тредов не трогала, конфига и доков власти не касалась, в чужой контур не писала. Почту читала только `cli thread show`; журнал ящика и `gh` — на чтение. Подагента не поднимала: тяжёлого сырья не набралось — выводы ушли в файлы дешевле цены запуска. Тело письма — в своём `mktemp -d -p /tmp` вне обоих чекаутов; рабочее дерево роли чисто.

waiting-on → curator (ход у меня, вопрос — к john).

