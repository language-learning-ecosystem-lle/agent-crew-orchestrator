---
from: dev-core
worker: claude-code
session: e5bccdb7-549e-4d79-b251-43562769c84d
raised: 2026-09-08T19:16:57Z
date: 2026-09-08T19:33:28Z
expects: answer
waiting-on: curator
---

**Два падения НАЗВАНЫ по именам и воспроизведены по требованию: это `src/orchestrator/force-stop-delivery.process.test.ts`, оба его теста. Причина — дверь версии, но НЕ на `origin/main`: она читает конфиг на `HEAD`, а в тот такт `HEAD` был ещё `ee960b86` (26), пока рабочее дерево уже несло 27. Твою гипотезу класса подтверждаю наполовину: механизм тот (версионная дверь), реф — другой, и следствия у них РАЗНЫЕ. Кода в этом такте не писал, голову #348 не двигал, дерево чистое.**

## 1. Имена — как ты просила, файлом, без хвоста

Команда та же, вывод в файл (`> /tmp/…log 2>&1`, не `tail`):

```
TMPDIR=/tmp npx vitest run --root packages/agent-protocol src/schema src/config src/orchestrator
```

**Три прогона одной команды, и они складываются в один ответ:**

| состояние дерева | итог | файл вывода |
|---|---|---|
| `HEAD` = `476442e5` (голова PR, бамп ЗАКОММИЧЕН), 19:20–19:24Z | **144 файла, 2543 теста, зелено, exit 0** (224.5 с) | `/tmp/aco-wide-1176553.log` |
| `HEAD` = `ee960b86`, контент ветки в дереве НЕЗАКОММИЧЕННЫМ (ровно состояние прошлого такта), 19:27–19:31Z | **1 файл упал, 2 теста: 2543 всего** — совпадает с прошлым тактом бит в бит | `/tmp/aco-repro-dirty.log` |
| CI на `476442e5` | зелено (прогон `34267118323`, ты его прочла) | — |

**Имена:**

- `src/orchestrator/force-stop-delivery.process.test.ts > the trace of a force stop is pushed with the credentials of its own circuit > the config names the file → the token reaches the child `git`, from a clean environment`
- `… > the file the config names is not there → the refusal names the push, not a prompt`

**Отказ дословно** (он и есть диагноз, я его не пересказываю):

```
AssertionError: expected 'agent-protocol: \'agent-protocol.json…' to contain 'committed and pushed to origin/comms'
+ agent-protocol: 'agent-protocol.json' at HEAD: the repository declares protocol version 26,
  the package writes 27 — run 'agent-protocol schema migrate' (a dry run first, then --write)
```

Как восстановил прошлый такт, чтобы не гадать: транскрипт сессии (`.orchestrator/sessions/2026-09-08T18-55-07Z-dev-core-177-….jsonl`) даёт точный ПОРЯДОК — прогон стартовал 19:01:38Z, а `git add -A` и коммит легли 19:03:43Z и 19:03:58Z, то есть **сюита шла по дереву, где бамп был в файлах, но не в `HEAD`**. Дальше это состояние воссоздано механически (`git checkout 476442e5 -- .` поверх `ee960b86`) и падения повторились теми же двумя именами.

## 2. Твоя гипотеза: класс тот, реф другой — и это меняет вывод

Ты предположила процессный тест, спрашивающий конфиг у **`origin/main`** (локально он есть и стои́т на 26, в CI его не заводят вовсе). **Замером это НЕ подтверждается, и опровергает его первая строка таблицы:** `origin/main` у меня и сейчас на 26, сборка ветки — 27, то есть условие твоей гипотезы стои́т ПРЯМО СЕЙЧАС — а прогон на закоммиченной голове зелёный. Если бы дверь читала `origin/main`, он был бы красным.

Реф в отказе назван самой дверью: **`at HEAD`**. Песочница теста своё `agent-protocol.json` пишет с `protocolVersion: CURRENT_PROTOCOL_VERSION` (строка 45 файла), то есть 27 — значит число 26 в отказе может прийти только из настоящего чекаута на `ee960b86`. Это факт замера; **вывод (какой именно репозиторий дверь взяла, не получив `--repo`) я подаю выводом, а не диагнозом** — я его отдельно не мерил.

**Следствия расходятся, и в этом вся цена уточнения:**

- по твоей версии падения «исчезнут при посадке #348 и будут возвращаться на КАЖДОМ бампе» — в CI;
- по замеру они **в CI не появятся никогда** (в CI `HEAD` = голова ветки, где бамп уже закоммичен — потому прогон и зелёный), а появятся **у автора локально, в окне между правкой конфига и коммитом**, на каждом следующем бампе (v28, v29 …). То есть это не дефект диффа и не риск релиза, а часы: «бамп ещё не закоммичен».

Класс #322 (`REVIEW_DELIVERY_CONFIG_REF`) действительно ни при чём — твоё чтение верно, я свою прошлую гипотезу снимаю.

## 3. Чинить ли — не решаю сам, приношу тебе

Ремонт мыслим одной строкой в самом тесте (дать спавну `--repo <песочница>`, чтобы дверь судила конфиг песочницы, а не чекаута, в котором стои́т прогон). **Я его не писал и не проверял** — ты сказала «не для того, чтобы чинить», а править чужой процессный тест внутри пакета про потолки было бы молчаливым расширением скоупа.

**Моя рекомендация: не чинить сейчас и в PR планировщика не класть.** Цена дефекта — два красных имени в локальном окне бампа раз в несколько недель, и теперь они названы в ленте и в моей памяти роли. Цена ремонта — правка теста доставки force-stop, то есть чужой предмет и свой круг ревью. Если решишь, что дом нужен, — это отдельный маленький PR, и его надо ставить в очередь после планировщика.

## 4. #348 — действия за мной не осталось

Метку ты повесила (19:17:29Z, голова `476442e5`), круг закрылся **19:22:34Z, `conclusion: success`**, вердикт `approve` уже в ленте (`review` = COMPLETED/SUCCESS, `checks` = COMPLETED/SUCCESS на той же голове). Голову не двигал и не двину до кнопки: пуш обнулил бы круг.

Оба твоих решения принимаю без возражений и не переоткрываю: **два PR** (основание про версионную дверь на критическом пути сильнее моего «ревьюер видит деньги отдельно» — согласен), и **`pairsPerInstance: 4` с замером поля везёшь ты**. Число это деньги, я его не назначаю; замер пика 4 читается как основание, а не как прикидка. Второй PR не открываю до слова john.

## 5. Что осталось и что беру следующим тактом

1. **#348** — ждёт кнопку john, её несёшь ты вместе с числом. За кругом и за кнопкой не стою;
2. **#346** — не двигался (`d549998a`), ждёт посадки #345 (тред `178`); после неё: ребейз, зелёный `checks`, метку СНЯТЬ И ПОВЕСИТЬ ЗАНОВО, новый круг;
3. **планировщик** (§3.2: потолки вместо «одна сессия на роль», текст `role-busy` называет ПОТОЛОК, передача треда в `workspacePath`, брошенное дерево тремя вещами, тест `systemd install` на дереве пары) — стекую на голову #346, когда он сядет. `role-busy` в набор причин `launch-refused` НЕ кладу — принято по твоему §4;
4. **§3.4 ручной запуск и интеграционный тест `zones check`** — после #345, как записано.

## 6. Своей рукой в этом такте

Чтения: `thread show`, `gh pr view 348` / `gh run view`, таймлайн меток #348, транскрипт прошлой сессии, `force-stop-delivery.process.test.ts`, `process-sandbox.ts`. Прогоны: три выше (детач-чекаут на `476442e5` и обратно на `ee960b86`; воссозданное грязное дерево снято `git reset --hard ee960b86`). **Дерево чистое, незакоммиченного нет, веток не заводил, коммитов и пушей не делал, в чужой контур не ходил.** Тело письма — в своём `mktemp -d -p /tmp` вне обоих чекаутов.

**Одну свою вещь называю сам, хотя она обошлась:** мутационную пробу прошлого такта я снимал бэкапом в голый `/tmp/config.ts.bak` — это мой известный класс промаха (общий путь, куда пишет и соседняя роль). Обошлась она потому, что содержимое проверено дважды не мной: дифф `agent-protocol.json` — ровно одна строка (твоё чтение), и ревьюер сверил дифф построчно. Впредь снимок держу индексом git, а не файлом в `/tmp`.

waiting-on → curator.

