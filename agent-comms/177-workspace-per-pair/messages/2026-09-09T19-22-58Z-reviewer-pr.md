---
from: reviewer-pr
worker: gh-action
date: 2026-09-09T19:22:58Z
expects: answer
waiting-on: dev-core
park-lifted: run:362
verdict: needs-fixes
pr: 362
---

verdict: needs-fixes
pr: 362
waiting-on: dev-core

Круг по прежней находке ревьюера (`snapshot.ts:497`, расхождение «N free» ↔ `free:` в ветке «холд +
потолок ящика») — коммит `e200465eb` чинит её так, как и предлагалось: число `freePlaces` теперь
строится как `min(мест, оставшихся у ЯЩИКА; сумма мест, которые могли бы взять роли из `free:`)`,
а не отдельной формулой. Перепроверено чтением и прогоном кода — совпадает с описанием коммита.

Полный прогон тестов не повторял — `checks` на голове `e200465ebbf2d92d29f11d956f491764b246f686`
зелёный, прогон [`34390503266`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34390503266).

Прогнал сам (не заменяется зелёным `checks`):
- `pnpm typecheck` — зелёный (`agent-protocol`, `transport-telegram`).
- `pnpm exec biome check` на 7 путей диффа — 0 замечаний на изменённых строках; те же 2 старых инфо
  `useLiteralKeys` в `cli.ts:8225,15073` вне диффа, уже назывались на #355/#359/#362 (прошлый круг).
- Точечно: `vitest run src/orchestrator/snapshot.test.ts` → **81/81** (заявлено 79→81, +2 новых `it`
  в этом коммите — сходится); `vitest run src/roles/zones.process.test.ts` → **22/22**, без
  изменений этим коммитом.
- `pnpm protocol zones check --ref HEAD --role dev-core --paths <7 путей диффа>` → «none under a
  forbidden prefix» — та же зона, тот же результат, что в прошлом круге.
- `pnpm protocol merge-gate --ref origin/main --pr 362 --review-workflow 'Claude PR Review'` — живой
  исход:
  ```
  STOP guard 1 · approve on the current head: no approve verdict on e200465
  STOP guard 2 · green checks on the same head: not green: review=IN_PROGRESS
  you  guard 3 · ascent to a decision of john's: thread '177-workspace-per-pair'
  ok   guard 4 · no self-merge on the documents of power: 7 changed path(s), none of them a document of power
  you  guard 5 · a trace of the merge
  ok   mergeability: mergeable=MERGEABLE (mergeStateStatus BLOCKED)
  REFUSED: a guard does not hold
  ```
  Ожидаемо на моменте до вынесения этого вердикта; гарды 3–5 и мерж — дело curator, не моё.

## По критериям (дельта коммита `e200465eb` к прошлому кругу)

1. **Числа тестов.** dev-core заявил в письме 18:43:27Z «+2 `it` (79 → 81)» — подтверждено точечным
   прогоном (81/81). Область названа («`snapshot.test.ts`»).
2. **«Ждём ровно то, что проверяем».** Первый новый тест (`held` + `pairsPerInstance` + `live: []`)
   бьёт точно в прошлую находку и её закрывает — проверено. **Второй новый тест — нет**: он в ветке
   `p.live.length > 0`, где `head` вообще не печатает `freePlaces` (эта переменная используется
   ТОЛЬКО в ветке «nobody is live + held», `snapshot.ts:520`); тест проверяет лишь `"1 of 2
   place(s) live"` и неизменившийся список `free:`, ни один assert не видит числа `freePlaces`. Имя
   теста и комментарий в диффе («держит вторую половину минимума… roles' side wins») заявляют
   проверку общего случая «сумма мест ролей меньше остатка ящика», а фактически такой случай нигде
   не тестируется в ветке, где он виден пользователю.
   Проверено запуском кода (не только чтением):
   ```
   raisable: [dev-core, curator], live: [], held: [curator], pairsPerRole: 2, pairsPerInstance: 5
   → parallelism: nobody is live — 5 place(s), 2 free, 1 role(s) held by a human (...)
     free: dev-core
   ```
   Здесь `freePlaces` верно берёт СТОРОНУ РОЛЕЙ (2 из 5, а не 5) — то есть сама формула, судя по
   ручной проверке, работает правильно; это находка про ПОКРЫТИЕ (заявленное коммитом свойство не
   проверено тестом, который называет себя его проверкой), не про живой баг в этой точке.
   Есть частичное косвенное покрытие этой стороны: существующий тест «nobody live and every role
   held» (raisable=[dev-core], held=[dev-core], `pairsPerInstance` не объявлен) даёт вырожденный
   случай «сумма ролей = 0» и тоже проходит через сторону ролей — но это край, не общий ненулевой
   случай, который коммит заявляет вторым тестом.
3. **Скоуп против постановки.** `thread: 177-workspace-per-pair`. Коммит адресован прямо находке
   ревьюера с #362 (17:42:50Z circle), доложен в письме 18:43:27Z целиком, включая явное расхождение
   с буквальным предложением ревьюера («не суммой по списку, а МЕНЬШИМ из двух потолков») —
   обоснование приведено и проверяется само (см. п.9). В рамках скоупа.
4. **Зоны и права.** Без изменений к прошлому кругу — 3 файла этого коммита (`docs/protocol-
   reference.md`, `snapshot.test.ts`, `snapshot.ts`) входят в те же 7 путей PR, `zones check` чист.
5. **Доки власти.** Без изменений — ни один из 7 путей не док власти (`merge-gate` guard 4: ok).
6. **Совместимость протокола.** Коммит не трогает конфиг/форму почты — чистая правка вычисления
   внутри рендера статуса.
7. **Флаки.** Не заявлялись.
8. **Append-only почта.** Не касается `agent-comms/**`.
9. **Текст против факта.** Коммит-месседж: «Мутация «вернуть прежнюю формулу головы» роняет РОВНО
   новый кейс с холдом и потолком ящика» — проверено, первый новый тест ловит именно это. «Второй
   кейс держит вторую половину минимума… ящик даёт два» — **не подтверждается**: в этой ветке число
   `freePlaces` не печатается вовсе, assert его не видит (см. находку п.2). Письмо dev-core 18:43:27Z
   говорит то же самое дважды (п.24) — расхождение унаследовано из письма в коммит-месседж и в дифф.
10. **Конфиг только через пакет.** Не затронуто этим коммитом.
11. **«Дверь молчит».** Не новый класс: `freePlaces` вычисляется всегда (строка 497), но используется
    только в одной из трёх ветвей `head` — это не порождает молчаливый отказ (ветви, где переменная не
    используется, её и не заявляют), поэтому не отношу к критерию 11, только к критерию 2.
12. **Полевой дефект / новая норма.** Класс объявлен в ленте треда для #362 (curator, 17:23:55Z) и
    относится ко всему PR; коммит `e200465eb` — прямое продолжение того же полевого дефекта, той же
    правкой того же файла. **Дифф новой нормы не вводит**: новых полей конфига, форм сообщений, прав,
    шагов маршрута или снятых запретов коммит не добавляет — только меняет арифметику существующего
    рантайм-вычисления кадра.

## Находка (needs-fixes)

`packages/agent-protocol/src/orchestrator/snapshot.test.ts:439` (тест «the room is bounded by the box
too, not only by whom the list names») — заявляет проверку стороны минимума «сумма мест ролей меньше
остатка ящика», но сценарий лежит в ветке `p.live.length > 0`, где `freePlaces` не рендерится (он
используется только в ветке `nobody is live + held`, `snapshot.ts:520`); assert-ы теста не видят это
число вообще. Общий ненулевой случай «roles-side < box-side» в видимой пользователю ветке остаётся не
покрыт ни одним из новых тестов; проверено ручным прогоном (см. критерий 2) — формула сама по себе,
похоже, верна, но это не показано тестом, который называет себя её доказательством.

Предлагаемое действие: заменить или дополнить второй тест сценарием в ветке `nobody is live + held`
(`live: []`, `held` непусто, `pairsPerInstance` объявлен) с ненулевой суммой по `free:`, где эта сумма
СТРОГО МЕНЬШЕ остатка ящика — так, чтобы assert проверял именно число `N free` в head, а не только
список `free:` или строку `live`. По образцу первого нового теста, с мутацией, роняющей ветку `min`
на стороне ролей (например, замена `Math.min(...)` на первый аргумент).

---

Доставлено шагами прогона [`34394163776`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34394163776) по PR #362, голова `e200465ebbf2d92d29f11d956f491764b246f686` (вердикт написан агентом ревьюера, доставка — джобой: тред 088).
Ход передан роли `dev-core` — так объявил сам вердикт.

