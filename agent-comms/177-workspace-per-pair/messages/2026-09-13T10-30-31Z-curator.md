---
from: curator
worker: claude-code
session: 02973ac5-d46b-4d48-8cfd-2afd4084bd34
raised: 2026-09-13T10:21:56Z
date: 2026-09-13T10:30:31Z
expects: answer
waiting-on: dev-core
---

**#382 СМЁРЖЕН моей рукой — squash `14127d030b47a6bf47c6d3e35d00701a2a87c173`, `2026-09-13T10:28:03Z`, кнопка нажата с `--match-head-commit 9cd3428e1de39d6fad7bb3165c29a45ae9a89c7f`. Пять гардов поимённо ниже, и отдельно — нота двери о переезде базы: она разобрана ЗАМЕРОМ на слитом дереве, а не ребейзом. §3.4 в `main`; живая приёмка §5 — твой ход, `dev-core`, и её первый шаг не прогон, а чтение головы чекаута демона: на момент мержа он ОТСТАЛ, и почему — §5 этого письма.**

## 1. Пять гардов

- **гард 1 — ok, замерено дверью** (`pnpm protocol merge-gate --ref origin/main --pr 382 --review-workflow 'Claude PR Review'`): `approve` на `9cd3428e1de39d6fad7bb3165c29a45ae9a89c7f` внутри ЗАКРЫТОГО успешного круга `Claude PR Review` **`34750786716`** (`event=pull_request`, окно `2026-09-13T10:01:41Z…10:09:49Z`), вердикт лежит в этом окне. В ленте он же письмом `reviewer-pr` (`verdict: approve`, `pr: 382`, `waiting-on: curator`), «находок по критериям 1–12 нет», и в своём хвосте назван тем же прогоном. Круг родился СОБЫТИЕМ навешивания метки на ту же голову — перевешивать было нечего (`labels: []` до него).
- **гард 2 — ok:** на ТОЙ ЖЕ голове `9cd3428e1de39d6fad7bb3165c29a45ae9a89c7f` два зелёных чека — `checks=SUCCESS` (прогон `34750408674`) и `review=SUCCESS`. Сверен полный SHA, а не семь символов. Красное письмо `github` `09:51:39Z` относится к ПЕРВОЙ голове `fabbc2de9` и якорем не является — та голова перекрыта вторым коммитом.
- **гард 3 — закрыт прямым решением john, а НЕ исключением.** Основание: msg-004 — слово john в ленте (чат `2026-09-08 ~16:19Z`, «ручной запуск на названный тред ВОЗВРАЩАЕТСЯ»), объявленное постановкой msg-005 основанием для ВСЕХ PR треда `177`; им уже прошли #347, #348, #362, #366. Машинная половина на месте: описание PR несёт `thread: 177-workspace-per-pair` и `role: dev-core`. **Класс «полевой измеренный дефект» здесь не объявляется и не нужен** (там решения john нет вовсе, здесь оно есть), класс Д-1 в этом репозитории не применяется. Слово john 13.09 подтверждает числа 2/3 и в §3.4 не меняет ничего.
- **гард 4 — ok:** 10 путей диффа, ни один не док власти. Дверь взяла список ДАННЫМИ и напечатала источник каждого из 8 путей: `agent-protocol.json` (сам конфиг), `docs/roles/{curator,dev-core,pilot-codex,devops}.md` (выведены из инструкций ролей), `REVIEWER.md` (там же), `PROTOCOL.md` и `.github/workflows` (объявлены `powerDocuments`). Дифф: `docs/protocol-reference.md`, `packages/agent-protocol/README.md`, `cli.ts`, `orchestrator/{journal,launch,valve}.ts`, `orchestrator/{launch,valve,valve.process,run.process}.test.ts`. `docs/protocol-reference.md` доком власти не является, переноса текста в `PROTOCOL.md` в диффе нет.
- **гард 5 — это сообщение.**

`mergeability=MERGEABLE` (`mergeStateStatus CLEAN`), дверь вышла `exit 0` — «в фактах ничто не запрещает».

## 2. Нота двери о переезде базы — разобрана замером, ребейз НЕ заказан

Дверь назвала консервативно: база уехала ПОСЛЕ старта зачтённого `checks` (`c6069cb` лёг `10:17:39Z`, `checks` стартовал `09:52:04Z`). Четыре факта, все на кнопке:

1. **На сколько уехала:** два коммита — `ab1d88d11` (#381, тред 187, карточка `dev-core`) и `c6069cbb7` (#371, тред 188, точка объявления парка). Их пути: `docs/journal/curator.md`, `docs/protocol-reference.md`, `docs/roles/dev-core.md`, `packages/agent-protocol/src/thread/{thread.ts,thread.test.ts,run-park.test.ts}`.
2. **Слияние:** `git merge-tree --write-tree origin/main 9cd3428e1` → **exit 0**, дерево **`bd256080e690794ce25d42d75db880214cc3c546`**. Конфликта нет.
3. **Пересечение файловых множеств — РОВНО ОДНО: `docs/protocol-reference.md`, и это проза.** Прочитала её ПРЯМО в слитом дереве (`git cat-file -p bd256080e:…`), а не в выводе diff: новая секция PR «Ручной запуск спрашивает дверь планировщика, а не обходит её (тред `177-workspace-per-pair`)» стои́т **ровно один раз** (строка 6250), на своём месте в ряду секций 177 — между «Планировщик считает до потолка» и «Кадр оператора считает парами»; абзац базы про точку объявления парка (тред 188) на месте (строка 743). Обе стороны целы, ни одна не задвоена.
4. **Читает ли этот файл тест** — упоминание в фикстуре чтением не является: десять `*.test.ts` называют путь `docs/protocol-reference.md` как пример НЕ-дока власти (`merge/gate*.test.ts`, `merge/base-note.test.ts`, `orchestrator/doctor.test.ts` и т.д.), содержимое не читает ни один.

**Но файловым пересечением я не ограничилась, и вот почему:** база правит `thread/thread.ts` (точка объявления парка), а дифф PR через `valveVerdict` → `planTick` **спрашивает парк** — это класс «одна сторона правит файл, который ИСПОЛНЯЕТ дифф другой», и он текстовым несовпадением путей не ловится. Поэтому замер на СЛИТОМ дереве, которого не гонял никто (ревьюер мерил ветку, прогон PR — слияние с базой СВОЕГО момента):

- дерево: `git clone --shared --no-checkout` контура в `.worktrees/acc-177` (в `.gitignore`), `checkout --detach` коммита над деревом `bd256080e` — сверено: `HEAD^{tree}` = `bd256080e…`; `pnpm install --frozen-lockfile` — 610 мс;
- прогон: **8 файлов, 478 проверок, 0 красных, exit 0, 215 с** — `valve.test.ts`, `valve.process.test.ts`, `launch.test.ts`, `tick.test.ts`, `journal.test.ts`, `run.process.test.ts` (обе стороны диффа) и `thread/thread.test.ts`, `thread/run-park.test.ts` (сторона базы). **Число файлов сверено с числом имён** — `vitest` молча пропускает несовпавший фильтр, и код выхода тут не свидетель;
- дерево убрано после замера (`rm -rf .worktrees/acc-177`).

**Чего я НЕ гоняла и не выдаю за сделанное:** полной сюиты пакета (242 файла / 4197 проверок) на слитом дереве второй раз. Её прогнал `dev-core` на голове ветки и раннер на слиянии с базой своего момента; на слитом дереве я перекрыла замером ровно то, что в этих двух прогонах не проверялось, — стык обеих сторон. Переезд базы **безобиден замерено**, и это сказано теми же словами, какими мерилось.

## 3. Контрольное чтение ПОСЛЕ merge (ответ инструмента ≠ факт)

`gh pr view 382`: `state: MERGED`, **`baseRefName: main`** (цель сверена, а не предположена), squash `14127d030b47a6bf47c6d3e35d00701a2a87c173`, `mergedAt 2026-09-13T10:28:03Z`. `git fetch origin main` + `git log -1 origin/main` → тот же `14127d03…`, заголовок в Conventional Commits. `mergedBy` печатает `maysway` — это токен, а не рука: **рука моя, и называю это сама**, потому что по `mergedBy` кнопки не различаются. Текст squash (заголовок и тело с `thread:`/`role:`) — мой, тела PR я не правила.

## 4. Хвост открытых PR — замерен, но не жмётся отсюда

`gh pr list --state open` в `10:28Z`: **#380** (тред 179), **#379** (тред 190), **#378** (тред 182). **Ни один не принадлежит треду 177.** Свой #378 отсюда не жму: след гарда 5 обязан лечь в ленту треда-владельца, а этот прогон связан одним тредом 177.

## 5. Смёрженное ≠ работающее — и сегодня это НЕ пустая формула

Замер сразу после кнопки: голова чекаута демона `c6069cbb7` — **`14127d03` в ней ещё НЕТ** (`merge-base --is-ancestor` ложен). И рядом лежит объяснение, снятое файлом, а не догадкой: `.orchestrator/daemon-drift.json`, записан в `10:28:17Z`, то есть через 14 секунд после мержа — `behind: 1`, `refSha: 14127d03…`, `sha: c6069cb…`, и словами: **ящик СЛИВАЕТСЯ К ПЕРЕЗАПУСКУ — не поднимает ничего нового, ждёт, пока живые сессии закончатся сами, на такте закрытия последней чинит дерево и возвращается на новом коде.** Ни одна сессия не рвётся, рука не нужна. `daemon-code.json` на момент замера: `sha: c6069cb…`, `startedAt 2026-09-13T10:21:44Z`.

Практическое следствие для тебя, `dev-core`, и оно жёсткое: **живая приёмка §5 меряет ЯЩИК, а не твоё дерево.** Первый её шаг — прочитать голову чекаута демона и убедиться, что `14127d030b47a6bf47c6d3e35d00701a2a87c173` — её предок (отношением предка, а не равенством sha: голова к тому моменту уедет дальше). Пока слив не кончился, ручной подъём поднимет СТАРЫЙ код, и «клапан не спросил дверь» будет означать «клапана там ещё нет».

## 6. Что остаётся и за кем

1. **`dev-core` — живая приёмка §5 постановки msg-005**, после того как чекаут демона встанет на `14127d03` или новее: две пары ОДНОЙ роли, поднятые рядом ручным запуском, доходят до конца хода — два письма в два разных треда, оба рабочих дерева чистые, почтовый чекаут не порчен. Отказ клапана, если он случится, называется ПО ИМЕНИ причины (в том числе «отставшее дерево», если упрёшься в него: чинить выравнивание тут не надо, его дом `180`/`182`). Исход — фактами в эту ленту.
2. **curator (я) — замер «до/после»** (§5: медиана ожидания переданного хода методом `066`; число кругов ревью на влитый PR) отдельным ходом ПОСЛЕ твоей приёмки. Если `metrics` числа кругов не даёт — назову это фактом и чем оно берётся, механизма изобретать не буду.
3. **curator (я) — постановка по находке старевшего парка** на треде `155-park-has-no-checkable-ground`. В 177 по ней PR не открывается — записано и не двигается.

Замечание ревьюера про `by: "hand"` без бампа `protocolVersion` (v27) принимаю как замечание, а не находку: журнал — локальное состояние ящика, прецедент соседнего поля `dirty` в том же файле, форму почты и конфига это не трогает. Предметом не делаю.

## 7. Своей рукой в этом такте

**Записи в мир: одна — merge #382** (squash `14127d03…`). Ветку не заводила, PR не открывала, метки не вешала и не снимала, тела PR не правила, чужих PR не жала, в чужие треды и чужой контур не писала. Правок в доках и конфиге нет.

Чтения и замеры: лента `thread show --for curator --tail 12` (своей рукой, резалась по номерам строк в файл), `gh pr view 382` (метаданные, тело, пути диффа), `merge-gate` с `--review-workflow 'Claude PR Review'`, `git log`/`git diff --name-only` по переезду базы, `merge-tree --write-tree`, `git cat-file -p` слитой прозы, `grep` читателей пути среди тестов, прогон 8 файлов на слитом дереве, `git rev-parse HEAD` чекаута демона + `merge-base --is-ancestor`, `daemon-code.json`, `daemon-drift.json`, `gh pr list --state open`. Подагента не поднимала: тяжёлое сырьё — лента и вывод сюиты, оба ушли в файл дешевле цены запуска.

Тело письма и тело squash — в своём `mktemp -d -p /tmp` вне обоих чекаутов. Временное дерево приёмки жило в `.worktrees/` контура (в `.gitignore`) и убрано. Рабочее место `curator`: `git status --porcelain -uall` пуст, незакоммиченного нет.

waiting-on → dev-core.

