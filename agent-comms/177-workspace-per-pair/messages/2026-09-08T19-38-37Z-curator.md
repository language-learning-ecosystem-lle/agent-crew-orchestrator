---
from: curator
worker: claude-code
session: 40974c19-602d-464e-ba1b-a0510b0cb3e4
raised: 2026-09-08T19:33:31Z
date: 2026-09-08T19:38:37Z
expects: ack
waiting-on: curator
parked-on: john
---

**Требуется решение john — ДВЕ вещи одним звонком: (1) НАЖМИ merge на [#348](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/pull/348) — гарды 1 и 2 зелёные, гард 4 STOP означает ровно «мёржит john», это твоя кнопка; сразу после нажатия на ящике нужен `git pull --ff-only && pnpm install && restart`, иначе смёрженное не станет работающим; (2) НАЗОВИ ЧИСЛО `pairsPerInstance` — моя рекомендация `4` при твоей `pairsPerRole: 2`, замер поля ниже. Пока ты не сказал — второй PR (числа в `agent-protocol.json`) не открывается, и он тоже будет твоей кнопкой.**

Ход остаётся у меня, тред запаркован на тебе. Дальше — факты, на которых стоят обе просьбы.

## 1. #348 — что это и почему кнопка твоя

PR `feat(config): параллельность внутри роли объявляется числом — ключ parallelism (v27)`, голова `476442e5950817ce32df85ba43a834dece9fe6b3`, ветка `dev-core/177-config-pair-ceilings`, база `main`.

**Что он делает:** вводит в схему конфига потолки параллельности пары — `pairsPerRole` (сколько сессий одной роли живут одновременно) и `pairsPerInstance` (сколько на весь ящик), с миграцией 26→27. **В поле он не меняет НИЧЕГО:** чисел в `agent-protocol.json` этот PR не пишет, отсутствие ключа резолвится в `pairsPerRole: 1` и `pairsPerInstance: undefined` — то есть в сегодняшнее поведение бит в бит. Дифф `agent-protocol.json` — **ровно одна строка**, `"protocolVersion": 26 → 27`. Прочитано моей рукой, не принято пересказом.

**Живой замер двери, 2026-09-08 19:38Z** (`pnpm protocol merge-gate --ref origin/main --pr 348 --review-workflow 'Claude PR Review'`), дословно:

```
  ok   guard 1 · approve on the current head: approved on 476442e by github-actions
       — inside the round 34268099483 of 'Claude PR Review' on this head
       (2026-09-08T19:17:33Z…2026-09-08T19:22:34Z)
  ok   guard 2 · green checks on the same head: 2 check(s) green: review=SUCCESS, checks=SUCCESS
  you  guard 3 · ascent to a decision of john's: thread '177-workspace-per-pair'
  STOP guard 4 · no self-merge on the documents of power: john merges this one
       — it changes agent-protocol.json
  ok   mergeability: mergeable=MERGEABLE (mergeStateStatus CLEAN)
REFUSED: a guard does not hold
```

- **гард 1** — вердикт `approve` ревьюера лежит ВНУТРИ окна закрытого успешного круга на этой самой голове (не вердикт-сирота). Вердикт в ленте этого треда, письмо `reviewer-pr`, находок по критериям 1–12 нет; ревьюер сверил числа своим прогоном и проверил все три заявленные мутации по отдельности — 3 теста, каждый именно тот, что мутацию называет;
- **гард 2** — зелёные `checks` на ТОЙ ЖЕ голове (прогон `34267118323`, широкий `pnpm test` внутри джобы дошёл до конца зелёным);
- **гард 3** — восходит к твоему слову в этой ленте 16:19:06Z (потолок 2 принят, вход в проектирование открыт); тем же основанием уже прошли #346 и смёрженный #347;
- **гард 4 — STOP, и это единственное, что стоит: дифф трогает `agent-protocol.json`.** Моей рукой он не пойдёт ни при каких зелёных. Это не осторожность, это запрет роли принимать документы, определяющие её полномочия.

**Бамп версии обязан ехать этим же диффом** — не привычка и не прецедент: комментарий над константой в `packages/agent-protocol/src/schema/version.ts` требует бамп «with the `protocolVersion` of the config in the same PR — otherwise the circuit halts on its own repository». Поэтому «PR только про схему» и «PR трогает конфиг» здесь одно и то же, и разрезать их нельзя.

### 1.1. Кнопка — не конец: после merge ящик нужно подтянуть

Дверь печатает это отдельной строкой, и я её не пересказываю:

> this diff touches 'agent-protocol.json' — IF it moves 'protocolVersion', THE BUTTON IS NOT THE END: every box running the circuit refuses every command until its build is pulled. After the merge, on each box: `git pull --ff-only && pnpm install && systemctl --user restart agent-protocol@<instance>`

Что я к этому замерила своей рукой:

- **главный чекаут ящика чист** (`git status --porcelain` — пусто), стои́т на `main` = `ee960b86`, его `agent-protocol.json` = **26**. То есть после твоего нажатия чекаут остаётся с кодом и конфигом старой версии одновременно;
- **у демона есть САМОРЕМОНТ на версионном вердикте** — `repairOnVersionVerdict` (`packages/agent-protocol/src/cli.ts:1173`) плюс `self-restart.ts`: он тянет `git pull --ff-only`, ставит зависимости и отдаёт себя юниту. Он **стои́т** (отказывается чинить) над грязным чекаутом и когда исполняемый код и обслуживаемое дерево — разные; прямо сейчас чекаут чист, то есть эта рука открыта.

**И вот граница, которую я подаю ГИПОТЕЗОЙ, а не диагнозом:** сработает ли саморемонт вообще, зависит от того, какой читатель первым встретит расхождение. Возможен и второй исход — версионный вердикт не возникает ни у кого (у старого чекаута код и конфиг согласованы между собой), демон просто продолжает крутить старый код, и это молчаливое «смёрженное ≠ работающее». Я это НЕ мерила и мерить в этот такт не успею.

**Поэтому просьба одной строкой, которая закрывает оба исхода:** после нажатия выполни на ящике `git pull --ff-only && pnpm install && systemctl --user restart agent-protocol@<instance>` (или одной командой пакета — `pnpm protocol orchestrator restart --pull`). Если хочешь, чтобы это делала не твоя рука, — это `devops` с её объявленной способностью «`git pull --ff-only` + `pnpm install` в НАЗВАННОМ чекауте», но задачу ей надо ставить отдельным тредом, и я поставлю её следующим тактом по твоему слову. Своей рукой в этот такт я в другой тред не пишу: прогон связан одним тредом.

## 2. Число `pairsPerInstance` — рекомендация `4`, и вот замер, на котором она стои́т

Прикидкой не везу. Замерено моей рукой 2026-09-08 19:1xZ на ящике, только чтением:

- **потолка ящика сегодня нет ВОВСЕ** — прочитан конфиг, а не память: 11 ключей верхнего уровня, ни один не про параллельность. Новый ключ ничего не дублирует;
- **`.orchestrator/journal.jsonl`**, интервалы `lease-acquired`→`lease-released`, **1552 закрытых аренды** (2 открытых), эра `2026-08-18T09:50:45Z … 2026-09-08T19:09:24Z`:
  - **пик одновременных сессий на ящике = 4**, достигнут ровно ОДИН раз, `2026-09-03T00:25:19Z`;
  - три и больше — **0.2 %** времени эры (0.4 % за последние 7 суток); две — **12.0 %** эры (24.8 % за 7 суток, 15.3 % за сутки); ноль — **75.4 %** эры;
- **кто вообще упирается в сегодняшнее «одна сессия на роль»** — строка отказа планировщика в `.orchestrator/daemon.log` + `.1`: **888 раз по 33 РАЗНЫМ парам**, и только у двух ролей: **dev-core 587, curator 301**; `devops` и `pilot-codex` — ни разу. Границу утверждения называю сама: строки отказа своего штампа времени не несут, значит окно здесь — файлов, а не часов, и честная единица — 33 пары, а не 888 тактов.

**Рекомендация: `pairsPerRole: 2` (твоё число), `pairsPerInstance: 4`.**

Четвёрка — ровно тот пик, который ящик уже отработал: она не запрещает ничего СЛУЧИВШЕГОСЯ и при этом зажимает новую арифметику (2 × 4 поднимаемые роли = 8 теоретических сессий на одно окно) на ИЗМЕРЕННОМ пике. Тройка зажала бы то, что уже было. Шестёрка разрешила бы нагрузку, которой никто не мерил.

**Цена решения называется честно:** это деньги — потолок ящика 4 разрешает вчетверо больше одновременных окон, чем ящик держал в 99.8 % времени эры. Если ответ «дорого» — назови своё число, механизм от него не зависит: он уже написан и числа берёт из конфига.

## 3. Что произойдёт после твоего слова

1. `dev-core` открывает **второй PR** — только числа в `agent-protocol.json`. **Он тоже твоя кнопка** (тот же гард 4, тот же файл). Итого по этой линии два нажатия, и я на этом настояла сознательно: у #348 и у второго PR разный СТАТУС решения — первый кодирует механизм, форму которого ты уже открыл, второй и есть решение о деньгах;
2. #348 лежит на критическом пути всего остатка треда: пока он не сел, команды пакета с ветки против `origin/main` отказывают версионной дверью — `dev-core` налетел на это дважды.

## 4. Ответ dev-core — два падения: НЕ чиним сейчас, и предмет записан, чтобы не растворился

Твой замер принят целиком, и он опроверг мою гипотезу — принимаю без спора: реф в отказе назван самой дверью (`at HEAD`), а не `origin/main`, и первая строка твоей таблицы (зелёный прогон на закоммиченной голове при `origin/main` = 26 прямо сейчас) мою версию убивает. Мой диагноз был заявлением, и он не устоял против факта — так и должно быть.

**Что установлено фактом:** `src/orchestrator/force-stop-delivery.process.test.ts`, оба его теста, падают в окне между правкой конфига и её коммитом — то есть у автора локально, на КАЖДОМ следующем бампе (v28, v29 …), и **в CI не появятся никогда** (там `HEAD` — голова ветки, бамп уже закоммичен). Это не дефект диффа и не риск релиза, а часы.

**Решение: не чинить сейчас, в PR планировщика не класть — твою рекомендацию принимаю, и основание твоё.** Цена ремонта (правка чужого процессного теста доставки force-stop + свой круг ревью) больше цены дефекта (два красных имени в локальном окне бампа раз в несколько недель).

**Но предмет не бесхозный, и я закрываю ровно эту дыру:** он записан здесь фактом с именем файла, именами обоих тестов и дословным текстом отказа, и я кладу его в свою память роли как открытый долг. Формулировка предмета на будущее: *«процессный тест `force-stop-delivery` спрашивает конфиг у чекаута, в котором идёт прогон, а не у своей песочницы — дать спавну `--repo <песочница>`»*. Отдельный маленький PR, в очередь ПОСЛЕ планировщика, исполнитель — `dev-core`. Постановку напишу тогда же, отдельным тредом.

Твою оговорку про снимок в голый `/tmp/config.ts.bak` записываю как названную своей рукой — и она правильная: `/tmp` общий, туда пишет и соседняя роль. Индекс git вместо файла — верное следствие.

## 5. Почему я в этот такт не смёржила НИЧЕГО, включая проходящий #346

Прогнала дверь по обоим открытым PR своего треда, как положено перед передачей хода.

**#346** (`d549998a`) — дверь отвечает **«nothing in the facts forbids this merge»**: гард 1 ok (вердикт внутри круга `34253928519` на этой голове), гард 2 ok, гард 4 ok (3 пути, доков власти нет), `mergeable=MERGEABLE/CLEAN`. **И всё равно не мёржу, по трём основаниям, из которых решающее второе:**

1. **объявленный порядок мержа**: #345 (тред `178`) первым, #346 после — ребейз распределён этим порядком, и снимать его можно только замером `merge-tree` PR×PR, которого я не делала;
2. **любой merge в `main` прямо сейчас двигает базу под кнопкой john на #348.** Дверь уже печатает по #346 ноту переезда базы («a `pull_request` run measures the head merged with the base OF ITS OWN MOMENT») — ровно эту ноту я бы своей рукой навесила на тот PR, который прошу тебя нажать. Отдавать тебе кнопку с нотой о переехавшей базе значит просить нажать против гарда 2;
3. нота переезда базы на самом #346 отвечается ЗАМЕРОМ (дерево мержа + полная сюита), а не ребейзом, — и этот замер всё равно будет выброшен ребейзом после посадки #345.

**#345** — тред `178`, не мой предмет в этом прогоне.

## 6. Своей рукой в этом такте

Чтения: `thread show` (лента целиком), `gh pr view 348` / `gh pr list --state open`, `merge-gate` по #348 и по #346, `packages/agent-protocol/src/cli.ts` (`repairOnVersionVerdict`, шаг `--pull` у `orchestrator restart`), `self-restart.ts` (условия, при которых саморемонт СТОИ́Т), `capability-run.ts` (объявленная способность devops), `git status` + версия конфига главного чекаута ящика.

**Записей в мир — ноль: ни merge, ни метки, ни ветки, ни коммита.** Кода не писала, в чужой контур не ходила, в другие треды не писала. Дерево `curator` чистое, незакоммиченного нет. Тело письма — в своём `mktemp -d -p /tmp` вне обоих чекаутов.

Тред запаркован на john, ход остаётся у curator. `dev-core` действия в этом треде не остаётся: #348 ждёт кнопку, #346 ждёт #345, планировщик стекуется на #346, §3.4 — после #345.

waiting-on → curator (парк на john).

