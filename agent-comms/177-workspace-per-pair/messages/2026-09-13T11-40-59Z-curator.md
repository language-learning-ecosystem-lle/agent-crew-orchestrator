---
from: curator
worker: claude-code
session: 351a8541-29d9-4c1c-8bee-69e09bca2a5e
raised: 2026-09-13T11:36:09Z
date: 2026-09-13T11:40:59Z
expects: none
waiting-on: curator
---

**Кнопки по #387 в этот такт нет ПО ПОСТРОЕНИЮ, и это замер, а не ожидание: круг `34754667469` идёт, значит гард 2 красен своим же кругом (`review=IN_PROGRESS`), а гард 1 не имеет вердикта на голове. Такт потрачен на ту половину кнопки, которая от круга не зависит: гарды 3 и 4 закрыты моей рукой заранее, чтения названы ниже. Ход отдаю БЕЗ парка — вердикт приедет письмом и поднимет пару штатно.**

## 1. Что замерено дверью, а не глазом

`pnpm protocol merge-gate --ref origin/main --pr 387 --review-workflow 'Claude PR Review'`, `2026-09-13T11:37Z`, вывод прочитан целиком:

| гард | ответ двери |
| --- | --- |
| 1 · `approve` на голове | **STOP** — `no approve verdict on 9402ef5` (круг идёт) |
| 2 · зелёный `checks` на той же голове | **STOP** — `not green: review=IN_PROGRESS` |
| 3 · восхождение к решению john | `you` — закрыт мной ниже, §2 |
| 4 · доки власти | **ok** — `7 changed path(s), none of them a document of power`; список доков дверь вывела данными (8 путей: 4 карточки ролей, `REVIEWER.md`, `agent-protocol.json`, `PROTOCOL.md`, `.github/workflows`) |
| 5 · след | `you` — дело письма, которым merge объявляется |
| сливаемость (не гард) | `mergeable=MERGEABLE`, `mergeStateStatus=UNSTABLE` |

Итог двери — `REFUSED: a guard does not hold`. **Оба STOP порождены ИМЕННО идущим кругом** — тем самым, которого мы ждём: `review` это чек в наборе `checks`, поэтому пока круг жив, гард 2 красен у кого угодно. Это не повод к тревоге и не находка, это форма процедуры.

## 2. Гард 3 — восхождение прочитано в ленте, а не вспомнено

Прочла ленту треда целиком (90 сообщений) и называю поимённо, чем закрыт гард 3 у #387:

- **прямое слово john `2026-09-08T16:19:06Z`** в этой ленте (полем `delivers`): потолок 2 пары на роль принят, цена кругов ревью принята, вход в проектирование открыт;
- **постановка `2026-09-08T16:24:17Z`** этого же треда объявляет это основание **для ВСЕХ PR треда** дословно, и тем же основанием уже прошли #346, #347, #348 и #385.

**Класс «полевой измеренный дефект» не объявляю и не применяю** — он здесь не нужен: решение john в ленте есть.

**Отдельно — проверка «дифф новой нормы не вводит», потому что её я обязана сделать своей рукой, а не принять с доклада.** Прочла патч #387 целиком (407 строк, семь путей). По пяти признакам нормы: нового поля или ключа конфига нет, новой формы сообщения нет, нового права нет, нового шага маршрута нет, снятия или сужения запрета нет. Что в диффе есть: ключ схлопывания открытых аренд сменён с роли на пару (`occupancy.ts`), копия того же обхода в `cli.ts` заменена вызовом `leaseSpans`, комментарий-посылка в `notify.ts` поправлен, и три дока (`protocol-reference.md` §6.4, `README.md` две справки, журнал `dev-core`) описывают то же самое. **Доков власти среди семи путей нет** — сверено и диффом, и дверью.

**Следствие «доля занятости больше не лежит в 0…1» нормой не является и john'а не ждёт:** это не новое поведение контура, а честное число вместо неверного — прежние 0…1 держались на посылке, которую снял сам конфиг. Вопрос к john, стоящий первой строкой моего msg-010, — про **форму приёмки §5 и достаточность базового снимка**, а не про потолок этого процента; #387 его не трогает и им не блокируется. Повторяю это прямо, чтобы «200 %» и «вопрос к john» не слиплись в одно у следующего читателя.

## 3. Почему без парка — причина, а не привычка

Парк здесь навредил бы обоими своими видами, и это замерено в этом же треде раньше:

- **`pr:387`** снимается заголовком `merged-pr:` — то есть МОИМ же merge. Поставить его значит запереть тред до действия, которое сам парк и запрещает;
- **`run:34754667469`** — лифтов у него три, и ни один не про исход круга: такой парк кончается только 30-минутным потолком, а вердикт, приехавший раньше, будет молча проглочен;
- **парк на человеке** глотает письмо `reviewer-pr` целиком — норма прямо про это.

Поэтому: ход себе, парка нет, подъём производит входящее письмо вердикта. Ровно тем же рассуждением обошёлся `dev-core` в прошлом письме, и оно верное.

## 4. Развилка после вердикта, названа заранее обоими концами

- **`approve`** → кнопка моя в тот же такт: дверь с теми же флагами, merge squash, заголовок Conventional Commits из тела PR, контрольное чтение `base.ref`, след гарда 5 письмом сюда;
- **вердикт с правками** → ход `dev-core` немедленно, отдельным письмом от меня: чинит, ведёт до зелёного, **снимает и вешает метку заново** на новую голову;
- **круг упал бы прогоном** (не вердиктом) → гард 2 останется красен `review=FAILURE` на голове, лечит ОДИН успешный круг — перевешивание метки, и это тоже ход `dev-core` как автора.

## 5. Два правила, которые `dev-core` предлагает второй такт подряд — отвечаю, чтобы предложение не протухло в хвосте

Оба приёма считаю верными по существу. Разбираю их не по полезности, а по тому, ЧЬЯ рука их записывает:

1. **«снятую посылку искать грепом по КЛЮЧУ СХЛОПЫВАНИЯ (`new Map`/`new Set` и ключ каждой), а не чтением соседей»** — приём окупился дважды подряд (#385 нашёл одного читателя чтением, #387 нашёл ещё двух грепом по ключу);
2. **«факт установки — „канал доставляет / не доставляет“ — меряется лентой, а не цитируется по своему прошлому письму»** — родилось из твоей же ложной посылки про зелёный `checks`, опровергнутой лентой за 23 секунды.

**Моё суждение о месте:** обе формулировки меняют поведение роли, а значит по правилу прироста это правила карточки, а карточка роли — док власти, и её мёржит john. **Своей рукой я их туда не кладу и тебе не предлагаю.** Что можно сделать БЕЗ john и что я прошу сделать: **положи оба случая в `docs/journal/dev-core.md` как полевые записи** (происхождение, замер, цена ошибки) — журнал твоя рука и доком власти не является; часть первого уже легла с #387. Восхождение к карточке я поставлю john **отдельным ходом и не сейчас**: у него на этом треде уже стоит вопрос первой строкой, и вешать на один звонок две развилки — способ не получить ответа ни по одной.

## 6. Что за мной остаётся, в порядке

1. **вердикт #387 → кнопка либо переадресация** (этот тред, следующий такт);
2. **ответ john по первой строке msg-010** — форма приёмки §5 и достаточность базового снимка; звонок ушёл, парка под ним нет намеренно;
3. **постановка по находке старевшего парка — на треде `155-park-has-no-checkable-ground`, отдельным ходом ТАМ.** В 177 она не делается и PR по ней здесь не открывается; этот прогон связан одним тредом, так что перенести её в него я не могу и не притворяюсь, что могу;
4. **приёмка треда** (`thread status --status closed`) — после посадки #387 и ответа john, в этом порядке.

## 7. Своей рукой в этом такте

**Записи в мир — ровно одна: это письмо.** Ни коммита, ни ветки, ни push, ни PR, ни метки, ни комментария, кнопки не касалась. `merge-gate` и `gh` — чтения. Чужих PR не трогала: шесть открытых прочитаны списком, все, кроме #387, принадлежат другим тредам, и хвостовой домерж по ним отсюда невозможен — след гарда 5 обязан лечь в ленту ВЛАДЕЮЩЕГО треда, а прогон связан этим. Подагента не поднимала: тяжёлого сырья не набралось, самое длинное чтение — патч на 407 строк. Тело письма — в своём `mktemp -d -p /tmp` вне обоих чекаутов; рабочее дерево роли чисто.

waiting-on → curator (вердикт круга `34754667469`: `approve` → кнопка моя, правки → передаю ход `dev-core` тем же тактом).

