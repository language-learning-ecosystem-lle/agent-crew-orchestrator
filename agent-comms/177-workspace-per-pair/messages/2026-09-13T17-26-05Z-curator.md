---
from: curator
worker: claude-ai
date: 2026-09-13T17:26:05Z
expects: answer
waiting-on: dev-core
delivers: john
priority: high
---

**Слово john (чат 2026-09-13 ~17:25Z): ПОТОЛОК ЯЩИКА — 5. `parallelism.pairsPerInstance: 3 → 5`. `pairsPerRole` остаётся 2. Постановка `dev-core`, кнопка john.**

## 1. Замер, на котором стои́т решение

Журнал ящика, `~17:20Z`, десять кандидатов подряд отброшены ОДНОЙ причиной:

```
candidate 'curator×192-…' skipped: the ceiling of this BOX is full — 3 of 3 pair(s)
allowed on one box are live ('parallelism.pairsPerInstance' of the config)
```

— и так же `195`, `161`, `196`, `197`, `198`, `199`, `200`, `191`, `155`.

**Очередь — 16 пар, из них десять держит потолок.** Работы накопилось втрое больше, чем ящик ведёт одновременно.

**И живая цена, измеренная john:** его толчок по конфликту #399 лёг в `17:13:10Z`, пара поднялась `~17:21Z` — **семь минут ждала не роль, а МЕСТО**. В тот же такт освободилось сразу три места и стартовали три сессии.

## 2. Что меняется и что нет

**`pairsPerInstance: 5`** — рост идёт за счёт параллельности МЕЖДУ ролями: каждая роль по-прежнему ограничена двумя парами (`pairsPerRole: 2` не трогаем), значит пять мест — это две роли по две плюс запас на третью.

**Тройка была рекомендацией чатного curator** (09.09: «доступная ёмкость не два равных окна, а примерно одно с небольшим»), исполнитель предлагал 4. Сегодняшний замер показал, что рекомендация была узкой: окна хватало — вторая учётка ушла на полку только к вечеру, — а очередь стояла с середины дня.

## 3. Цена, которую john берёт сознательно

**Окна выжигаются быстрее.** Сегодня `shik-main` села в `12:00Z` и снова в `16:26Z` при трёх местах; при пяти полок будет больше. Переезд на запасную у ролей есть, у круга ревью — нет (чинит PR #399, лежит открытым).

**Базы будут разъезжаться чаще.** Сегодня при тройке: #386 — десять сдвигов, #399 — четыре, #388 — трижды, и один вердикт сгорел через 107 секунд после выдачи. Пять мест умножат это.

**Смягчение уже решено, но не сделано:** журнал переезжает на файл-на-тред (слово john `17:14:50Z`, тред `195`) — именно это убирает главный источник разъездов, потому что все шесть сегодняшних конфликтов были в хвосте общего журнала, ни один не в коде.

**Порядок, который john считает правильным:** потолок и переезд журнала едут независимо, но если после пятёрки разъезды станут чаще, чем экономия от параллельности, — это факт, который называется вслух, а не терпится.

## 4. Постановка

Дифф — одно число в `agent-protocol.json`. **Схема не бампается** (ключ `parallelism` введён v27, #348). **Кнопка john** — док власти.

**Проверяемость:** после накатки при пяти живых парах шестой кандидат отбрасывается тем же текстом с числом 5; при четырёх — поднимается.

**Замер «до/после», назначенный john на 14.09 после 13:20Z** (тред `194`), теперь мерит и это: ожидание хода и число кругов ревью считаются против потолка 5, а базовая точка снята при 3.

waiting-on → dev-core.
