---
from: curator
worker: claude-code
session: fda813b5-74cb-4471-b35d-743824562c25
raised: 2026-09-13T13:01:12Z
date: 2026-09-13T13:08:52Z
expects: ack
waiting-on: curator
parked-on: john
---

**ТРЕБУЕТСЯ РЕШЕНИЕ JOHN — тред `177` теперь БЛОКИРОВАН ТОЛЬКО твоим словом: работы в нём не осталось ни у кого. Подтверди приёмку переопределённой по ФОРМЕ §5 (дословная «обе пары рукой» изнутри роли неисполнима — достижимый максимум `потолок − 1`; замер «до/после» сделан и роста кругов НЕ показывает) — и я закрываю тред; либо назови, когда перемерить, если базовый снимок для тебя слишком ранний.**

**#394 СМЁРЖЕН** — squash в `main`, коммит `e22c5fa1679dfd956aa6862c4292128c1a990eba`, `13:05:33Z`; гарды поимённо в §1. **Оба предсказания из моего письма `12:58:29Z` §5 исполнились и оба замерены (§2, §3).** Открытых PR треда `177` больше нет — поэтому парк на john я СТАВЛЮ этим письмом, и это не пятое метание, а смена почвы: правило всё то же — парк на человеке ставится, когда в треде нет работы (§6).

## 1. След гарда 5 — чем закрыт каждый гард по #394

Дверь: `pnpm protocol merge-gate --ref origin/main --pr 394 --review-workflow 'Claude PR Review' --journals docs/journal`, `13:04:54Z`, `exit 0`, вывод прочитан целиком.

| гард | чем закрыт |
| --- | --- |
| 1 · `approve` на ТЕКУЩЕЙ голове | **не спрашивается** — дословно: `not asked: this diff is wholly a journal: all 1 changed path(s) lie inside the journals of the roles (docs/journal/curator.md)`. Основание — норма john 11.09 (тред `187-journal-rides-along`), условие проверила ДВЕРЬ по путям диффа, не я. Метки `review` на PR нет (`labels: []`) — круга не было и не требовалось |
| 2 · зелёный `checks` на ТОЙ ЖЕ голове | **ok** — `1 check(s) green: checks=SUCCESS`. Прогон [`34758437132`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34758437132), `completed/success`, `12:56:06Z → 13:04:40Z`, `headSha d7dd455711149c1bdef38874f09511d141c09aaf` — полный SHA сверен с `headRefOid` PR, а не первые семь |
| 3 · восхождение к решению john | то же основание, что у #390, #391, #393: прямое слово john `2026-09-08T16:19:06Z` в ЭТОЙ ленте (полем `delivers`, «ПРИНИМАЮ ВСЕ ЧЕТЫРЕ РЕКОМЕНДАЦИИ»), объявленное постановкой `2026-09-08T16:24:17Z` основанием гарда 3 для ВСЕХ PR треда `177`; плюс стоячие нормы john 2026-09-06 (тред `143`: полевой случай едет в журнал СРАЗУ) и 2026-09-11 (тред `187`: запись говорит, что БЫЛО, и нормы не несёт). Патч прочитан построчно, пять признаков нормы все пусты: ни нового поля или ключа конфига, ни новой формы сообщения, ни нового права, ни нового шага маршрута, ни снятия/сужения запрета. Дифф **`49 0 docs/journal/curator.md`**, один путь, удалений `--diff-filter=D` — **0**. Классы «полевой измеренный дефект» и Д-1 НЕ объявлялись и не применялись |
| 4 · доки власти | **ok** — `1 changed path(s), none of them a document of power`; список дверь вывела данными (8 путей), флагом ничего не добавлялось |
| 5 · след | это письмо |
| сливаемость (не гард) | `mergeable=MERGEABLE`, `mergeStateStatus=CLEAN`; ноты о переезде базы дверь не печатала — база не двигалась с `ef472dcf9` |

Кнопка: `gh pr merge 394 --squash --match-head-commit d7dd455711149c1bdef38874f09511d141c09aaf --delete-branch`, sha взят из вывода самой двери.

**Контрольное чтение цели:** `gh pr view 394` → `state: MERGED`, `mergedAt 2026-09-13T13:05:33Z`, `mergeCommit e22c5fa16`, **`baseRefName: main`**; `git fetch origin main` → голова `main` = `e22c5fa16`, заголовок тот самый.

## 2. Предсказание «кнопка вернёт EXIT=0» — ИСПОЛНИЛОСЬ, и механизм замерен ДВАЖДЫ до кнопки

`EXIT=0`, ветка `curator/177-branch-held-by-worktree` снесена и локально (`git branch --list` → пусто), и на `origin` (`git ls-remote --heads` → пусто).

Механизм §4 прошлого письма проверен **до** кнопки, двумя независимыми замерами:

1. **рефлог моего рабочего места:** `ef472dcf9 HEAD@{2026-09-13 15:01:14 +0200}: checkout: moving from curator/177-branch-held-by-worktree to ef472dcf9…` — отцепление в `13:01:14Z`, то есть **за 12 секунд до первой команды этой сессии** (`13:01:26Z`). Отцепил подъём пары, не моя рука;
2. **перечисление занятости**, `13:03:41Z`: ветки #394 в списке занятых НЕТ, при этом ПЯТЬ веток других пар стоя́т как стояли (`curator/187-journal-entry-rides-alone` #389, `curator/155-park-ground-vocabulary-canon` #388, `dev-core/180-dry-run-says-the-outcome` #386, `feat/189-dead-park-retry` #384, `fix/190-base-cost-reads-historic-config` #379).

**Уточнение к записи, которую я только что смёржила, и оно сужающее:** подъём освобождает **только своё собственное рабочее место пары**, а не раскладку вообще. Поэтому «кнопка по своему PR идёт по свободной ветке» верно ровно при условии «между коммитом и кнопкой был подъём ЭТОЙ пары» — а он был, и он же меня и поднял.

## 3. Пятый образец класса «курьер зовёт на метку, которой норма не требует» — цена нулевая по НОВОЙ причине

Письмо курьера пришло в `13:05:05Z` (прогон кончился `13:04:40Z`, задержка 25 с), тело — то же самое: «👉 Круга ревью на этой голове ещё нет … Ход у автора ровно на это одно действие». Зов не исполнен: журнальному диффу метка не нужна, и дверь это подтвердила сама (гард 1 «not asked»).

**Но цена нулевая здесь не потому, что автор и держатель кнопки — одна роль (так было у #393), а потому, что письмо легло ПОД живую сессию:** я уже была поднята (`13:01:14Z`), зелень прочла своей рукой дверью в `13:04:54Z` — на 11 секунд РАНЬШЕ письма — и нажала кнопку в `13:05:31Z`. Таблица цены дефекта после пятого образца:

| конфигурация | что делает зов курьера | цена |
| --- | --- | --- |
| автор `dev-core`, кнопка у curator (#390, #391) | `waiting-on: dev-core` — на PR, где действия у `dev-core` нет | подъём пары `dev-core × 177` вхолостую; дважды погашен рукой |
| автор = держатель кнопки, пара НЕ поднята (#393) | `waiting-on: curator` — ход идёт тому, у кого кнопка | ноль: подъём был нужен и так |
| автор = держатель кнопки, пара УЖЕ поднята (#394) | то же, но адресат уже в прогоне | ноль: письмо легло под живым ходом, зов прочитан и не исполнен |

Второе письмо (`13:06:08Z`, `merged-pr: 394`) приехало через 35 с после кнопки — тоже под живую сессию.

## 4. Свой побочный факт я перемерила ПЕРЕЧИСЛЕНИЕМ — и он оказался у́же, чем звучал

В прошлом письме я предложила треду `180-selfheal-leaves-the-workspaces-behind` факт: «мёртвая ветка переживает свой PR внутри рабочего места пары» (образец — `dev-core/177-courier-call-is-not-the-norm`). Перечислила поле целиком, `13:03:57Z`–`13:04:10Z`:

- **заняты деревом и при этом мертвы на `origin` — ровно ДВЕ:** `dev-core/177-courier-call-is-not-the-norm` (#391, смёржен `12:16:30Z`) в `.worktrees/dev-core@177-workspace-per-pair` и `dev-core/180-red-run-is-not-lost-mail` (#392, `12:33Z`) в `.worktrees/dev-core@180-notifier-down`. Обе — пары `dev-core`, не поднимавшиеся после своего merge;
- **локальных веток всего 408, на `origin` живы 14** — то есть ~390 мёртвых локальных веток, и они тянутся от тредов `002`–`006` до `190`, задолго до рабочих мест на пару.

**Вывод, обратный тому, как звучал образец: накопление мёртвых веток — не свойство рабочего места пары и не находка этого треда.** Подъём освобождает ХВАТ (`detached HEAD`), а саму ветку не удаляет никто, кроме `--delete-branch` на кнопке; на чужой паре её и не удалит. Треду `180` из этого едет не «утечка веток», а узкая строка: **кнопка по PR чужой пары вернёт `1`, пока эта пара не поднималась после своего коммита** — ровно то, что уже записано в журнале #394.

**Нового журнального PR я под это НЕ открываю, и называю причину:** урок здесь тот же самый, что смёржен в #394 («образец, названный классом, проверяется перечислением»), а второй PR стоил бы прогона `checks` и ещё одного подъёма пары ради дубля. Факт замерен и лежит в этой ленте — этого достаточно, тред `180` возьмёт его чтением.

## 5. Два прибора, которые соврали бы молча, — названы

1. **`FETCH_HEAD` затёрт `merge-gate` между двумя моими чтениями.** `git diff --numstat origin/main...FETCH_HEAD` дал **пустоту** — ни одной своей команды `fetch` между чтениями не было; `git rev-parse FETCH_HEAD` → `ef472dcf9`, то есть голова `main`, а не PR. Перемерено полным SHA головы → `49 0`. Голову держать сорока символами, а не именем.
2. **Ожидания прогона в foreground не было, и я называю это фактами, а не обещанием.** Пара поднята `13:01:14Z`, прогон шёл `12:56:06Z → 13:04:40Z`; минуты между подъёмом и зеленью потрачены на перечисления §2 и §4 (свои короткие чтения), дверь прочитана ОДИН раз — `13:04:54Z`, в момент решения и уже после конца прогона. Ни `sleep`, ни повторного опроса `gh run` в этом такте нет.

## 6. Парк на john — СТАВЛЮ, и вот почему это то же правило, а не новое

Правило треда с утра одно: **парк на человеке ставится, когда в треде НЕТ работы, и снимается, когда она есть.** Все предыдущие четыре раза сегодня работа была (#390, #391, #393, #394) — и парк заморозил бы пару против собственного открытого PR. Теперь её нет: `gh pr list --state open`, `13:06:49Z` → `389, 388, 386, 384, 383, 379`, и **ни один не принадлежит треду `177`** (владельцы по номерам: 187, 155, 180, 189, 188, 190 — их хвост я отсюда не трогаю, след гарда 5 обязан лечь в ленту треда-владельца).

Поэтому письмо идёт `--expects ack --parked-on john`: без парка пара поднимается на `waiting-on: curator` и не находит действия — это прямая трата подъёмов, замеренная в этом же треде. Звонок восьмой по счёту (`notify.state`: `asked john` по `177` — `08-09 16:12`, `08-09 19:38`, `09-09 21:33`, и четыре сегодня — `11:24:53`, `12:05:28`, `12:08:50`, `12:09:59`), но первая строка у него НОВАЯ по факту: раньше она говорила «подтверди приёмку», теперь — «в треде не осталось работы ни у кого, кроме тебя». Тред остаётся открытым: закрыть его — приёмка, а она твоя.

## 7. Своей рукой в этом такте

Записи в мир: merge #394 и это письмо. Ни ветки, ни коммита, ни нового PR, ни метки, ни комментария; конфига и доков власти не касалась, в чужой контур и в чужие треды не писала (чужих лент не читала вовсе). Подагента не поднимала: тяжёлых чтений не было — самое длинное — перечисление 408 локальных веток, дешевле цены запуска. Тело письма — в своём `mktemp -d -p /tmp` вне обоих чекаутов; рабочее дерево роли чисто.

waiting-on → curator (вопрос несёт роль; john в это поле не принимается). Парк — на john.

