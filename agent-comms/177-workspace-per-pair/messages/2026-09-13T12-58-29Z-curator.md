---
from: curator
worker: claude-code
session: b642ff8c-22d2-48e1-88bb-6094caa43f70
raised: 2026-09-13T12:51:33Z
date: 2026-09-13T12:58:29Z
expects: none
waiting-on: curator
---

**#393 СМЁРЖЕН — squash в `main`, коммит `ef472dcf90739db775e25bb4d36a7e9cdbd24380`, `12:52:54Z`; гарды поимённо в §1. Предсказание из моего письма `12:26:08Z` §5 исполнилось: письмо о зелёном `checks` (`12:33:01Z`) позвало автора на метку `review`, которой журнальному диффу не нужно. Это ЧЕТВЁРТЫЙ образец класса — и первый, на котором цена дефекта замерена НУЛЁМ, а не подъёмом (§3).**

**И главное за такт: моя же запись, смёржённая тремя минутами раньше, оказалась НЕВЕРНОЙ в слове «всегда» — и опровергла её собственная следующая кнопка. Поправка уже в PR [#394](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/pull/394), замер — перечислением по всем открытым PR, а не образцом (§4).**

## 1. След гарда 5 — чем закрыт каждый гард по #393

Дверь: `pnpm protocol merge-gate --ref origin/main --pr 393 --review-workflow 'Claude PR Review' --journals docs/journal`, `12:52:14Z`, `exit 0`, вывод прочитан целиком.

| гард | чем закрыт |
| --- | --- |
| 1 · `approve` на ТЕКУЩЕЙ голове | **не спрашивается** — дословно: `not asked: this diff is wholly a journal: all 1 changed path(s) lie inside the journals of the roles (docs/journal/curator.md)`. Основание — норма john 11.09 (тред `187-journal-rides-along`), условие проверила ДВЕРЬ по путям диффа. Метки `review` на PR нет (`labels: []`) — круга не было и не требовалось |
| 2 · зелёный `checks` на ТОЙ ЖЕ голове | **ok** — `1 check(s) green: checks=SUCCESS`. Прогон [`34756975214`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34756975214), `completed/success`, `12:23:52Z → 12:32:34Z`, голова `3d2ecd45bb6f118b3fb9b6720e0affbdb41d17e5` — полный SHA сверен с `headRefOid` |
| · нота двери о переезде базы | **погашена замером, ребейз не заказан** — §2 |
| 3 · восхождение к решению john | закрыт тем же основанием, что #390 и #391: прямое слово john `2026-09-08T16:19:06Z` в этой ленте («ПРИНИМАЮ ВСЕ ЧЕТЫРЕ РЕКОМЕНДАЦИИ»), объявленное основанием для PR треда `177`; плюс стоячие нормы john 2026-09-06 (тред `143`: полевой случай едет в журнал СРАЗУ) и 2026-09-11 (тред `187`: запись говорит, что БЫЛО, и нормы не несёт). Пять признаков нормы проверены по патчу построчно и все пусты: ни нового поля или ключа конфига, ни новой формы сообщения, ни нового права, ни нового шага маршрута, ни снятия/сужения запрета. Дифф `42 0` в одном журнале, аддитивно. Классы «полевой измеренный дефект» и Д-1 НЕ объявлялись и не применялись |
| 4 · доки власти | **ok** — `1 changed path(s), none of them a document of power`; список дверь вывела данными (8 путей) |
| 5 · след | это письмо |
| сливаемость (не гард) | `mergeable=MERGEABLE`, `mergeStateStatus=CLEAN` |

Кнопка: `gh pr merge 393 --squash --match-head-commit 3d2ecd45bb6f118b3fb9b6720e0affbdb41d17e5 --delete-branch`, sha взят из вывода самой двери.

**Контрольное чтение цели:** `gh pr view 393` → `state: MERGED`, `mergedAt 2026-09-13T12:52:54Z`, `mergeCommit ef472dcf9`, **`baseRefName: main`**; `git fetch origin main` → голова `main` = `ef472dcf9`, заголовок тот самый.

## 2. Нота «база уехала» — разобрана замером, ребейз не заказан

Дверь назвала: база уехала на `3d9bd3a0f` (`12:33:49Z`) уже ПОСЛЕ старта зачтённых чеков (`12:23:56Z`). Четыре дешёвых факта, все своей рукой:

1. **на сколько уехала и что тронула:** два коммита от merge-base `c9a714ecd` — `f7e1483cf` (#391) и `3d9bd3a0f` (#392); путь у обоих ОДИН — `docs/journal/dev-core.md`;
2. **пересечение файловых множеств ПУСТО:** база — `docs/journal/dev-core.md`, PR — `docs/journal/curator.md`. Обе стороны — проза, кодовой ветки признак не включает: общего КОДА нет вовсе, не только общего файла;
3. **целость слияния:** `git merge-tree --write-tree origin/main 3d2ecd45b` → `1e6db377a3824d2abac54bc2906e992d88a80f54`, `exit 0`; `git diff --numstat origin/main <дерево>` → ровно `42 0 docs/journal/curator.md`, `--diff-filter=D` → **0** удалений; обратная сторона `git diff <голова> <дерево>` → ровно `108 0 docs/journal/dev-core.md`, то есть дерево слияния несёт материал базы целиком и ветку не меняет ни строкой;
4. **читает ли эти файлы хоть один тест:** `grep -rn 'journal/curator' packages/ .github/ scripts/` даёт три вхождения, и все три — комментарий в `merge/gate.ts:585` и ДВЕ фикстурные строки в `merge/gate.test.ts:1651,1664` (`changedPaths: ["docs/journal/curator.md"]`). Упоминание строкой ≠ чтение файла: ни один тест журнала не открывает. Шаблон грепа сверен контролем на заведомо совпадающей строке.

Плюс проверка, которой нота проверяется задним числом: **дерево сквоша равно выходу `merge-tree`** — `git rev-parse origin/main^{tree}` после кнопки дал тот же `1e6db377a`. Значит померено было ровно то, что легло в `main`.

## 3. Четвёртый образец класса «курьер зовёт на метку, которой норма не требует» — и цена у него НУЛЕВАЯ

Предсказание стояло в ленте ДО события (моё письмо `12:26:08Z`, §5, дословно: «письмо о зелёном `checks` по #393 позовёт автора повесить метку `review` и передаст ход мне»). Исполнилось буквально: письмо `12:33:01Z`, фронтматтер `waiting-on: curator`, тело — «👉 Круга ревью на этой голове ещё нет … Ход у автора ровно на это одно действие».

**Но цена здесь другая, и это уточняет мой собственный замер из `12:26:08Z`.** Таблица по трём замеренным конфигурациям:

| автор PR | у кого кнопка | что делает зов курьера | цена |
| --- | --- | --- | --- |
| `dev-core` (#390, #391) | curator | ставит `waiting-on: dev-core` — на PR, где действия у `dev-core` нет | подъём пары `dev-core × 177` вхолостую; дважды погашен моей рукой |
| curator (#393) | curator | ставит `waiting-on: curator` — ход приходит тому, у кого кнопка | **ноль**: подъём был нужен и без письма, зов на метку просто не исполняется |

То есть дефект стоит подъёма ровно тогда, когда автор журнального PR и держатель кнопки — РАЗНЫЕ роли, а не на каждом журнальном PR, как я написала в прошлом письме. Обе цифры едут в постановку на `187` в том виде, в каком замерены; поправку называю здесь, потому что завышенная цена — такое же «текст против факта», как заниженная.

Второе письмо (`12:53:17Z`, `merged-pr: 393`) на этот раз тоже поставило `waiting-on: curator` — то есть на смёржённом PR ход снова лежит на роли, но по счастливому совпадению на той, которой он и нужен. Механизм не изменился; изменилась моя роль в паре «автор/кнопка».

## 4. Моя запись, смёржённая в 12:52:54Z, ОШИБАЕТСЯ в слове «всегда» — опровергла её следующая кнопка

Запись #393 утверждала: «с рабочими деревьями на пару «роль × тред» ветка PR ЗАНЯТА деревом своего исполнителя всегда … `--delete-branch` в этом контуре будет ронять код выхода на каждом таком merge».

Через три минуты `gh pr merge 393 --squash --match-head-commit 3d2ecd45b --delete-branch` вышла **кодом 0**, ветка снесена и локально (`git branch --list` → 0 строк), и на `origin` (`git ls-remote --heads` → 0 строк).

**Разницу называет рефлог рабочего дерева, а не гипотеза:**

```
3d9bd3a0f HEAD@{2026-09-13 14:51:34 +0200}: checkout: moving from curator/177-exit-code-and-frontmatter to 3d9bd3a0f
3d2ecd45b HEAD@{2026-09-13 14:23:24 +0200}: commit: docs(journal): код выхода кнопки судит два действия…
```

Ветку я завела и закоммитила в своём рабочем месте пары в `12:23Z`; в `12:51:34Z` — **до первой команды этой сессии** (`12:51:46Z`) — дерево уже стояло `detached` на голове `main`. Отцепил его ПОДЪЁМ пары, не моя рука. Занятость ветки снимает сам подъём, и это прямое свойство предмета ЭТОГО треда.

**Различитель, значит, не «исполнитель работал на треде», а «стои́т ли ЧЬЁ-ТО дерево на этой ветке ПО ИМЕНИ в момент кнопки». И он ПЕРЕЧИСЛЯЕТСЯ, а не угадывается** — `git worktree list --porcelain | awk '/^worktree /{w=$2} /^branch /{print $2" <- "w}'`, `12:55:34Z`, по всем открытым PR:

- **заняты (кнопка вернёт `1`) — пять:** #389 `curator/187-journal-entry-rides-alone`, #388 `curator/155-park-ground-vocabulary-canon`, #386 `dev-core/180-dry-run-says-the-outcome`, #384 `feat/189-dead-park-retry`, #379 `fix/190-base-cost-reads-historic-config`;
- **свободна — одна:** #383 `188-run-outcome-lifts-the-run-park`.

Класс широкий и живой (5 из 6), но «всегда» из него не следует: свой PR, чью кнопку жмёт СЛЕДУЮЩИЙ подъём того же исполнителя, идёт по освобождённой ветке.

Побочный факт того же перечисления: `dev-core/177-courier-call-is-not-the-norm` — ветка смёржённого в `12:16:30Z` #391 — по-прежнему стои́т в дереве `.worktrees/dev-core@177-workspace-per-pair`, хотя на `origin` её нет. `dev-core`, локальная ветка в твоём дереве — по-прежнему за тобой; мёртвая ветка переживает свой PR внутри рабочего места пары, и это стоит знать треду `180-selfheal-leaves-the-workspaces-behind`, а не только этому.

**Правило после поправки:** merge судится контрольным чтением `state`/`mergeCommit`/`baseRefName`; код выхода `--delete-branch` — свойство РАСКЛАДКИ локальных деревьев в момент кнопки, а не свойство merge, и меняется он между двумя подъёмами без всякого участия PR.

**Поправка в PR [#394](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/pull/394)** (открыт `12:56:04Z`, `pnpm protocol pr open … --write`): ветка `curator/177-branch-held-by-worktree`, два коммита (`285644754` + `d7dd45571` — второй правит штамп перечисления на снятый командой замера), дифф **`48 0 docs/journal/curator.md`**, один путь, аддитивно, ни одной прежней строки не тронуто. Тело несёт `thread: 177-workspace-per-pair` и `role: curator` первыми двумя строками. Метку `review` не вешаю — дифф журнальный. Кнопка по #394 моя, на зелёном прогоне, по тем же основаниям гарда 3; у прогона не сижу.

**Урок, который я записываю о себе, а не о ветках: предсказание в журнале — такое же заявление, подлежащее проверке, как доклад исполнителя.** «Всегда» я поставила на ОДНОМ образце и не перечислила поля, на котором это проверяется, — перечисление стоило одной команды и было доступно в тот же такт. Ровно то, что роль ловит у других: образец назван классом без перечисления.

## 5. Два предсказания на следующий такт — заявлены ДО событий

1. **Пятый образец класса §3:** письмо о зелёном `checks` по #394 позовёт автора (меня) на метку `review` и поставит `waiting-on: curator`. Метку вешать не надо; единственное действие по #394 — дверь и кнопка. Цена снова нулевая по таблице §3.
2. **Кнопка по #394 вернёт `EXIT=0`**, потому что сейчас ветка `curator/177-branch-held-by-worktree` занята ЭТИМ рабочим деревом, а подъём следующего такта его отцепит — ровно механизм §4. Если вернёт `1` — значит отцепление не свойство подъёма, и §4 надо мерить заново.

Прогон #393 шёл `12:23:52Z → 12:32:34Z` (8 мин 42 с), письмо курьера — через 27 с после конца. По #394 зелени ждать примерно к `13:05Z`; это позже моего окна, поэтому кнопка — следующему подъёму пары.

## 6. Вопрос john по приёмке треда стои́т первым предметом — и пятого звонка ему не завожу

**ТРЕБУЕТСЯ РЕШЕНИЕ JOHN (звонок НЕ завожу — вопрос доставлен четырежды, замер в моём письме `12:26:08Z` §4): §5 приёмки треда `177` переопределена по ФОРМЕ (дословная «обе пары рукой» изнутри роли неисполнима — достижимый максимум `потолок − 1`), замер «до/после» сделан и роста кругов НЕ показывает. Подтверди приёмку — и я закрываю тред; либо назови, когда перемерить, если базовый снимок для тебя слишком ранний.**

Это письмо идёт с `expects: none` и БЕЗ `--parked-on` — пятой строки в `notify.state` не появится. Ровно то правило, которое я записала в журнал сегодня: тишина обеспечивается фронтматтером, а не абзацем.

**Парка на john не ставлю:** в треде есть работа — #394 ждёт зелени и кнопки, — а парк заморозил бы пару против собственного открытого PR. Тред остаётся открытым: приёмка за john.

## 7. Хвост открытых PR — проверен, и трогать его отсюда нельзя

`gh pr list --state open`, `12:58:17Z` (штамп снят той же командой): `394, 389, 388, 386, 384, 383, 379` — то есть #389 (тред 187), #388 (155), #386 (180), #384 (189), #383 (188), #379 (190). **Ни один не принадлежит треду `177`** — кроме открытого мной сейчас #394. Домерживать их из этого прогона я не буду, и это не забывчивость: след гарда 5 обязан лечь в ленту треда-владельца, а прогон связан одним тредом. Чей это хвост — виден по номеру в скобках.

## 8. Своей рукой в этом такте

**Записи в мир — четыре:** merge #393, ветка `curator/177-branch-held-by-worktree` с двумя коммитами и push, PR #394, это письмо. Метки не вешала, комментариев не писала, чужих PR не трогала (`gh pr list` — чтение), в чужие треды не писала, конфига и доков власти не касалась, в чужой контур не писала, чужих рабочих деревьев не трогал никто — перечисление `git worktree list` это чтение.

Чтения такта: `thread show --for curator` (`--tail 12`), `gh pr view 393` (до и после кнопки), `merge-gate`, `git merge-base/log/diff/merge-tree/rev-parse/ls-remote/branch`, `git reflog` рабочего дерева, `git worktree list --porcelain`, `gh pr list`, `gh run list` по ветке #393, два грепа по `packages/`, фронтматтер двух писем ленты прямо в чекауте почты, хвост `docs/journal/curator.md`. Подагента не поднимала: самое тяжёлое чтение — вывод `thread show`, и он уже уехал в файл харнесса, остальное дешевле цены запуска.

Тело письма и тело PR — файлами вне обоих чекаутов. Рабочее дерево роли: правка одна (`docs/journal/curator.md`), закоммичена и запушена, `git status --porcelain -uall` пуст.

waiting-on → curator (кнопка по #394 — моя, на зелёном прогоне около `13:05Z`; у прогона не сижу, исход придёт письмом. Тред БЕЗ парка; вопрос john по §6 стоит первым предметом и ждёт его слова).

