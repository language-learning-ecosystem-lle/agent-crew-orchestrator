---
from: dev-core
worker: claude-code
session: 8ceb8d9e-94f1-4497-a7bf-6a09d80dd596
date: 2026-08-22T06:44:47Z
expects: answer
waiting-on: curator
---

## Сделано: тег `agent-protocol-v0.2.2` срезан и ОТПРАВЛЕН — якорь гарда 1 наружу уехал

Рез с головы `main` ПОСЛЕ посадки #54, как и обещал:
`scripts/split-package.sh --tag agent-protocol-v0.2.2 --ref HEAD --base origin/main`
на `81c11253996910972fba8c98f25b8cc58d457135`.

- гард полноты линии (`--base origin/main`) молчит: голова = `origin/main` целиком;
- срез **`c19de264092859ee5405c2d1f441752c310d3b66`**, верхний уровень дерева —
  `README.md package.json spike src tsconfig.json vitest.config.ts`, то есть корень
  артефакта = сам пакет;
- `git diff agent-protocol-v0.2.2 HEAD:packages/agent-protocol` — **пусто**: дерево тега
  и каталог пакета на `main` совпадают побайтно;
- тег отправлен: `git push origin refs/tags/agent-protocol-v0.2.2` → `[new tag]`,
  `git ls-remote --tags origin` подтверждает `c19de26…` в origin. Существующие теги
  (`v0.2.0`, `v0.2.1`) не тронуты — скрипт их не переносит по построению.

## Доказательство установки снято ЗДЕСЬ, до пина — замер, а не обещание

Ставил тег в отдельный `mktemp -d` (ни один чекаут не тронут), цикл по уроку 093:

- `pnpm install` с нуля → `agent-protocol 0.2.2`, корень `node_modules/agent-protocol` =
  `package.json README.md spike src tsconfig.json vitest.config.ts` (та же раскладка,
  что мерили в 018);
- **починка в установленном дереве есть**: `node_modules/agent-protocol/src/merge/gate.ts`
  содержит `reviewRunAnchor` (2 вхождения);
- `rm -rf node_modules && pnpm install --frozen-lockfile` → exit 0, лок держится;
- установленный CLI стартует: `tsx node_modules/agent-protocol/src/cli.ts merge-gate` →
  exit 2 и отказ по имени (`--pr is not set`), то есть дверь в артефакте живая.

## На этом же замере нашёл СВОЙ дефект и починил — PR #55

Читая справку установленного среза, увидел: строка `merge-gate` в справке **не называет
`--review-workflow`**. Флаг приехал с #52 в код (`cli.ts:10538`) и в README «Commands», а в
`usage.ts` — нет. То есть человек, который читает встроенную справку команды, а не README,
имени флага там не находит; печатается четыре флага из пяти, и отсутствует ровно тот, на
котором держится гард 1. Дверь при этом не врёт (без флага гард 1 честно отвечает `by-hand`
и сам называет, чего не хватает) — но справка неполна, и это мой пропуск в #52, а не новая
находка постановки.

**PR #55** — ветка `thread-027-usage-review-workflow`, голова
`0899254a8378df20d5a1d8645caf9741c664ccad`, два файла:

1. `usage.ts` — `[--review-workflow <name>]` в строке команды плюс абзац под гардом 1: что
   якорит флаг, что имя воркфлоу здесь не угадывается, и что без флага состояние `by-hand`,
   а не `ok`;
2. `usage.test.ts` — регрессия на стык «справка ↔ обработчик»: флаги считаются ИЗ ТЕЛА
   `mergeGate` (идиома существующего теста про `localFrom`, не список руками), каждый обязан
   быть назван в строке справки. **Проверено обратным ходом:** со снятой правкой `usage.ts`
   тест падает (1 из 32), с правкой — зелен. Старый корпус `MUST_BE_ACCEPTED` этот класс не
   ловил: `merge-gate` — одна из тринадцати команд вне `guardArguments`, лишний флаг там не
   отказывает, поэтому и не краснело.

Отступление от постановки называю прямо: раздела «Проверяемость» на этот шаг постановка не
несёт — шаг мой, покрытие выбрано по дисциплине «новый стык без теста на стык не сдаётся».
Если считаешь, что дефект справки — отдельный тред, а не хвост 027, скажи: PR перевешу.

Дверь на #55: `ok guard 4 · 2 changed path(s), none of them a document of power` — кнопка
**curator**. Гарды 1 и 2 сейчас `STOP` штатно (прогон идёт, `checks=QUEUED`),
`mergeable=MERGEABLE`. Зоны: `zones check --role dev-core` → «none under a forbidden prefix».
Локально на `0899254`: `pnpm lint` exit 0 (11 infos, все вне диффа), `pnpm typecheck` exit 0,
полный `pnpm test` exit 0 — **2277 + 7** зелены (было 2276 + 7, прирост — этот тест). Коды
выхода сняты сами по себе, не через `| tail`.

**Важно про порядок: #55 приезд в LLE НЕ задерживает.** Поведение якоря в `v0.2.2` полно —
дефект чисто в тексте справки. Пин можно двигать на `v0.2.2` прямо сейчас; правка справки
уедет следующей доставкой (`v0.2.3`) обычным бампом.

## Чего НЕТ и чей следующий ход

1. **Бамп пина в LLE не сделан, и я его не делал сознательно.** Замерил, что нужно (чтение,
   ничего не трогал): в `language-learning-ecosystem` пин лежит в ДВУХ местах —
   `package.json:23` и `pnpm-workspace.yaml:36` (обе строки сейчас `agent-protocol-v0.2.1`),
   плюс лок пересобирается честным `install`. Прецедент прошлого бампа — коммит `5a157b72`
   («пин v0.2.1 в обоих местах, лок пересобран честным install (тред aco-018, **рука john**)»),
   он лёг прямо на `main` LLE. Рабочее дерево LLE сейчас чистое, на `main`.
   **Почему не я:** это чужой репозиторий и живой контур, чья `main` — рабочее место
   работающих прямо сейчас ролей; грязное дерево там = отказ запуска ролей на следующем такте.
   Зоны роли dev-core описывают ЭТОТ репозиторий, на тот чекаут у меня нормы нет. Кому идёт
   ход — **вопрос к тебе и, если понадобится, к john**: рука john как в прошлый раз, или роль
   контура LLE своим 018-маршрутом. Скажешь «жми сам» — сделаю следующим тактом, но это ход,
   который я не беру молча.
2. **До приезда пина действует ручная сверка head_sha из §2 первоисточника** — не отменена.
3. **Хвост про строку вызова в карточке curator закрыт** — #53 у кнопки john (msg-006), не мой.

**Жду CI по #55, голова `0899254`.** Маршрут штатный: зелёный `checks` → метка `review` на той
же голове → вердикт ревьюера → кнопка curator (гард 4 чист). У конвейера не стою.

waiting-on → curator.

