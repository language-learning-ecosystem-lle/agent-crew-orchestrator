---
from: dev-core
worker: claude-code
session: a3004789-c6d3-48c5-bc1a-4953927c22c8
date: 2026-08-22T06:58:39Z
expects: answer
waiting-on: curator
---

## Ход: доставка сведена в ОДИН тег — PR #56 (`version` 0.2.2 → 0.2.3)

Состояние после посадки #55 замерено, не выведено: `origin/main` = `e94467c`, тег
`agent-protocol-v0.2.2` = `c19de26` (срезан с `81c1125`, то есть ДО #55), пин LLE
по-прежнему `agent-protocol-v0.2.1` **в обоих местах** (`package.json:23`,
`pnpm-workspace.yaml:36`), рабочее дерево LLE чистое, на `main`. Открытых PR, кроме #53
(кнопка john) и моего #56, нет.

Отсюда решение, и я называю его прямо, потому что беру шаг сам: **пин ещё не двигался, значит
бамп в LLE будет ОДИН — и пусть он несёт всё сразу.** В msg-011 я написал, что правка справки
уедет «следующей доставкой обычным бампом»; делаю эту доставку сейчас, ДО бампа пина, чтобы
контур LLE не платил за две ступени. Если ты или john уже двигаете пин на `v0.2.2` прямо
сейчас — это не ошибка и переделывать нечего: поведение якоря в `v0.2.2` полное, дефект там
чисто текстовый (встроенная справка `merge-gate` не называет `--review-workflow`).

## PR #56 — ветка `thread-027-release-v0.2.3`, голова `2aaadd9`

Дифф ровно два файла, кода не трогает: `packages/agent-protocol/package.json`
(`version` 0.2.2 → 0.2.3) и строка формы вызова реза в `README.md`
(`--tag agent-protocol-v0.2.3`) — тот же синхрон, что держали #41 и #54.

**Тестов не добавляю, и это решение, а не пропуск:** новой логики и нового стыка дифф не несёт,
а расхождение имени тега с `version` ловит уже покрытая дверь скрипта реза
(`split-package.process.test.ts`, «refuses a tag that disagrees with the package version»).

**Репетиция реза сделана на голове ветки, локальный тег удалён** (настоящий рез законен только
с головы `main`):

- срез `3c65bc3`, верхний уровень дерева — `README.md package.json spike src tsconfig.json
  vitest.config.ts`, то есть корень артефакта = сам пакет;
- в срезе `src/merge/gate.ts` несёт `reviewRunAnchor` (2 вхождения) **и** `src/usage.ts`
  называет `--review-workflow` (2 вхождения). Для сравнения тот же замер на уже отправленном
  `v0.2.2`: `git show agent-protocol-v0.2.2:src/usage.ts | grep -c -- '--review-workflow'`
  → **0**. Это и есть вся разница между двумя тегами;
- `git diff agent-protocol-v0.2.3 HEAD:packages/agent-protocol` — пусто.

Дверь на #56: `ok guard 4 · 2 changed path(s), none of them a document of power` — кнопка
**curator**. Гарды 1 и 2 сейчас `STOP` штатно (`checks=QUEUED`, вердикта ещё нет),
`mergeable=MERGEABLE`. Зоны: `zones check --role dev-core --paths
README.md,packages/agent-protocol/package.json` → «none under a forbidden prefix».
Локально на `2aaadd9`: `pnpm lint` exit 0, `pnpm typecheck` exit 0, полный `pnpm test` exit 0 —
**2277 + 7** зелены. Коды выхода сняты сами по себе, не через `| tail`.

## Вопрос из msg-011 стоит и ответа не получил — теперь он в двух частях

1. **Чья рука бампает пин в LLE?** Прецедент прошлого раза — коммит `5a157b72` прямо на `main`
   LLE, «рука john» (тред aco-018). Я этот ход молча не беру: чужой репозиторий и живой контур,
   чья `main` — рабочее место работающих ролей, а зоны роли dev-core описывают ЭТОТ репозиторий.
   Варианта два: рука john как в прошлый раз — или роль контура LLE своим 018-маршрутом.
   Скажешь «жми сам» — сделаю следующим тактом.
2. **На какой тег?** Рекомендую **`v0.2.3`** — он приедет после посадки #56 и несёт и якорь, и
   справку. `v0.2.2` уже в origin и поведенчески полон: если бамп уже начат на него, доводите
   его, справка догонит следующей доставкой.

## Чего НЕТ

- **Тег `agent-protocol-v0.2.3` не срезан** — законен только с головы `main` после merge #56.
  Это ход **dev-core**, тред надо вернуть сюда после посадки;
- **пин в LLE не бампнут** — см. вопрос выше;
- **до приезда пина действует ручная сверка `head_sha` из §2 первоисточника** — не отменена;
- **#53 — по-прежнему кнопка john**, не мой ход.

**Жду CI по #56, голова `2aaadd9`.** Маршрут штатный: зелёный `checks` → метка `review` на той
же голове → вердикт ревьюера → кнопка curator (гард 4 чист). У конвейера не стою.

