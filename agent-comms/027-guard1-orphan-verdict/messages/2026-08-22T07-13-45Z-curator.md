---
from: curator
worker: claude-code
session: af883cc5-c3a3-4191-892d-4a5dec000942
date: 2026-08-22T07:13:45Z
expects: ack
waiting-on: curator
parked-on: john
---

**Требуется решение john — работа этого треда упёрлась в две кнопки, и обе стоят.** Дев-работы здесь больше нет: якорь гарда 1 посажен, доставка наружу закончена, тег `agent-protocol-v0.2.3` в origin и починку несёт (проверила своей рукой, замеры ниже). Осталось ровно две ступени, и обе — рука john:

1. **Чья рука бампает пин `agent-protocol` в контуре LLE на `agent-protocol-v0.2.3`?** Прецедент прошлого бампа (`v0.2.1`) — рука john прямо в `main` LLE (коммит `5a157b72`, тред aco-018). **Рекомендация (рекомендация, не решение):** рука john тем же способом — правка в ДВУХ местах сразу (`package.json:23` и `pnpm-workspace.yaml:36`: `overrides` перебивает `package.json`, и правка одного места роняла `main` LLE), лок пересобрать честным `install`, доказательство — цикл `--frozen-lockfile`. Второй законный вариант — роль контура LLE своим 018-маршрутом. Выбор не операционный: он про то, чья рука работает в живом контуре, чья `main` — рабочее место работающих прямо сейчас ролей.
2. **Кнопка по #53** (`docs/roles/curator.md` — карточка догоняет якорь). Вопрос стоит с `2026-08-22T06-26-32Z-curator.md`, повторён в `2026-08-22T06-53-53Z-curator.md`; ответа на момент этой записи нет. PR готов целиком, гарды перемерены прямо сейчас (ниже) — блокирует только гард 4, то есть сам запрет self-merge на доках власти.

Тег на пин — **`v0.2.3`**, и это уже не выбор из двух: `v0.2.2` поведенчески полон, но его встроенная справка не называет `--review-workflow` — флаг, на котором держится гард 1, а в контуре LLE эту строку набирает рука. Замер разницы: `git show agent-protocol-v0.2.2:src/usage.ts | grep -c -- '--review-workflow'` → `0`, на `v0.2.3` → `2`.

## Доклад dev-core о резе проверен своей рукой, а не принят текстом

- **тег в origin и SHA совпадает посимвольно:** `git ls-remote --tags origin` → `c65f6104157a0e3419e9470dccc2658cdc8faebe refs/tags/agent-protocol-v0.2.3`. Прежние теги на месте и не перенесены: `v0.1.0` `5f6d75e`, `v0.1.1` `56c0bdc`, `v0.2.0` `b265199`, `v0.2.1` `b3839dc`, `v0.2.2` `c19de26`;
- **корень артефакта = сам пакет:** верхний уровень дерева тега — `README.md package.json spike src tsconfig.json vitest.config.ts`;
- **схема `<name>-v<version>` держится:** `git show agent-protocol-v0.2.3:package.json` → `"version": "0.2.3"`;
- **тег несёт ОБЕ починки:** `src/merge/gate.ts` → `reviewRunAnchor` 2 вхождения (#52), `src/usage.ts` → `--review-workflow` 2 вхождения (#55);
- **дерево тега и каталог пакета на `main` совпадают:** `git diff --stat agent-protocol-v0.2.3 origin/main:packages/agent-protocol` — пусто, код 0. `origin/main` = `9040247c0341445bcf658f80c2a32d80e3c7ae1a`;
- **состояние LLE прочитано (только чтение, ничего не тронуто):** пин по-прежнему `agent-protocol-v0.2.1` в обоих местах (`package.json:23`, `pnpm-workspace.yaml:36`), рабочее дерево чистое, на `main`. То есть ступень действительно не начата и первая рука, которая туда придёт, ни с кем не столкнётся.

Цикл установки (`pnpm install` с нуля → `--frozen-lockfile` exit 0 → старт CLI из установленного дерева) я не переснимала: он снят dev-core во временном чекауте, который уже удалён, и переснять его здесь значило бы повторить чужой замер, а не проверить его. Что проверяемо на артефакте — проверено выше на самом теге.

## #53 — дверь перемерена сейчас, изменилось только основание ноты

`pnpm protocol merge-gate --ref origin/main --pr 53 --review-workflow 'Claude PR Review'`, голова `fbc97227b573aa8e297211fb44083711ade9383a`:

- `ok guard 1 · approved on fbc9722 by github-actions — inside the round 32556674946 of 'Claude PR Review' on this head (2026-08-22T06:19:37Z…2026-08-22T06:24:29Z)`;
- `ok guard 2 · 2 check(s) green: review=SUCCESS, checks=SUCCESS`;
- `STOP guard 4 · john merges this one — it changes docs/roles/curator.md`;
- `mergeable=MERGEABLE (mergeStateStatus CLEAN)`;
- дверь вывела 6 доков власти сама: `agent-protocol.json`, `docs/roles/curator.md`, `docs/roles/dev-core.md`, `REVIEWER.md` — из конфига, `PROTOCOL.md` и `.github/workflows` — «declared by `powerDocuments`».

**Нота «база уехала» закрыта прогоном заново** (база всё та же `9040247` от `07:08:13Z`, зачтённый `checks` стартовал `06:15:34Z`): `git merge-tree --write-tree origin/main <голова #53>` → дерево `058fb0b72265056695492223800fd61cfcdbc549`, код 0, конфликтов нет; `git diff --stat origin/main 058fb0b` → ровно `docs/roles/curator.md | 21 ++++-----`, один файл. Ребейз не делаю: он стоил бы второго круга ревью ради ноты, которую закрывает прогон.

`gh pr list --state open` перед передачей хода — одна строка, #53. Своих кандидатов под кнопку curator нет.

## Чего НЕТ

- **пин в LLE не бампнут** — вопрос 1 выше, кнопка john; из этого чекаута в LLE никто не пишет до слова john;
- **#53 не смёржен** — вопрос 2 выше, кнопка john (гард 4);
- **ручная сверка `head_sha` из §2 первоисточника в контуре LLE действует** — она снимается не посадкой тега, а переездом пина; снимет её тот, кто закроет приезд;
- дев-работы в треде `027` нет: `main` = `9040247`, все четыре PR (#52, #54, #55, #56) смёржены, тег отправлен.

Ход держу я — обе ступени упираются в решение john.

waiting-on → curator (тред припаркован на john).

