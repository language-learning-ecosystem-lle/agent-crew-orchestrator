---
from: curator
worker: claude-code
session: 0f7faf66-be8c-418d-b190-2c3cf2ec6bd2
date: 2026-08-22T06:26:32Z
expects: ack
waiting-on: curator
parked-on: john
merged-pr: 52
---

**Требуется решение john: кнопка на PR #53** — карточка curator догоняет посаженный якорь гарда 1 (док власти `docs/roles/**`, гард 4 STOP, self-merge запрещён). PR готов целиком: approve на текущей голове, оба чека зелены, дифф — один файл.

## Гард 1 посажен: PR #52 смёржен

Гарды поимённо (гард 5):

- **гард 1** — вердикт `approve` в треде файлом `2026-08-22T06-18-17Z-reviewer-pr.md` («Находок по критериям REVIEWER.md нет»), он же `github-actions APPROVED 2026-08-22T06:18:32Z` на голове `534a7b19bbce21368eefc17bfbfd14b0b1edb927`. **Сирота исключена руками по форме §2 постановки** — той самой сверкой, которую эта дверь и заменяет: голова с момента открытия PR не двигалась, круг `Claude PR Review` `32556404989` шёл на НЕЙ (`06:13:34Z…06:18:38Z`), вердикт `06:18:32Z` лежит в его окне;
- **гард 2** — `2 check(s) green: review=SUCCESS, checks=SUCCESS` на той же голове; `checks` — прогон `32556210883`, `headSha` сверен командой и равен `534a7b19bbce21368eefc17bfbfd14b0b1edb927` целиком;
- **гард 3** — решение john в ленте с пометкой источника: маршрут «чинит dev-core aco», слово john, чат 2026-08-22 ~05:50Z (`2026-08-22T05-51-06Z-curator.md`, шапка постановки). Дефект измерен не мной и не здесь — curator'ом LLE-контура на PR #347;
- **гард 4** — `ok · 9 changed path(s), none of them a document of power` (живой прогон двери перед merge);
- squash, цель проверена после merge: `state MERGED`, `baseRefName main`, коммит `3668c1d5963231b133440ef62f26814ecf55568f`; уведомление о мёрже лежит в треде файлом `2026-08-22T06-20-47Z-github.md`.

Метку `review` на голову `534a7b1` повесила сама в `06:13:32Z` — после зелёного `checks` (`06:12:11Z`), круг стартовал `06:13:34Z` на той же голове (шаг без исполнителя, dev-core свой ход закончил на «жду CI»).

## Живая приёмка новой двери — не фикстурой, а на живом PR

Новая дверь с `origin/main` (после merge #52), вызов с флагом на PR #53:

```
ok guard 1 · approve on the current head: approved on fbc9722 by github-actions — inside the round 32556674946 of 'Claude PR Review' on this head (2026-08-22T06:19:37Z…2026-08-22T06:24:29Z)
```

Гард 1 больше не говорит «approved on <голова>» — он называет КРУГ и его окно. Это ровно то, чего не хватало 21.08 на #347. Ответ `by-hand` без флага я тоже видела своими глазами: старая строка вызова из карточки на #53 отвечает `you guard 1 … --review-workflow` — дверь не отказывает, гард молча возвращается к сверке руками. Отсюда и #53.

## PR #53 — что в нём и почему он к кнопке

Ветка `thread-027-card-review-workflow`, голова `fbc97227b573aa8e297211fb44083711ade9383a`, один файл `docs/roles/curator.md`. Две правки процедуры:

1. строка вызова несёт `--review-workflow 'Claude PR Review'` (это `name:` воркфлоу `.github/workflows/claude-review.yml`, сверено с файлом) — и сказано, что без флага гард 1 отвечает `by-hand`, а не `ok`;
2. у гарда 1 названо третье состояние и сам дефект-сирота: `by-hand` = «замерить не удалось» (обязанность сверить руками), `STOP` = «замерено и не держится».

Готовность (для кнопки, гарды 1/2/4 — живой прогон новой двери):

- **гард 1** — вердикт `approve` в треде файлом `2026-08-22T06-24-07Z-reviewer-pr.md` («Находок по критериям REVIEWER.md нет»), дверь: `approved on fbc9722 … inside the round 32556674946` (вердикт `06:24:21Z`, круг `06:19:37Z…06:24:29Z`);
- **гард 2** — `2 check(s) green: review=SUCCESS, checks=SUCCESS`; `checks` — прогон `32556496425`, `headSha` = `fbc97227b573aa8e297211fb44083711ade9383a`;
- **гард 4** — `STOP · john merges this one — it changes docs/roles/curator.md`. Это и есть причина кнопки;
- **нота «база уехала»** дверь печатает (база стала `3668c1d` в `06:20:23Z`, `checks` стартовали `06:15:34Z`) — закрыта прогоном, а не рассуждением: `git merge-tree --write-tree origin/main FETCH_HEAD` → код 0, конфликтов нет, дифф к новой базе — тот же один файл. Ребейз не делала: он стоил бы второго круга ревью ради ноты, которую закрывает merge-tree.

**Порядок соблюдён:** #53 садится ПОСЛЕ #52, и #52 уже на main — флаг в двери есть, карточка описывает вызов, который работает.

## Три самостоятельных решения dev-core — принимаю, одно с пометкой

Флаг вместо поля конфига (не тащить PR к кнопке john ради имени воркфлоу), `by-hand` вместо `STOP` при отсутствии `submittedAt` (иначе `STOP` значил бы две разные вещи), планировщик `merge-ready` Actions не спрашивает (иначе вызов Actions на каждый PR каждый такт). Все три доложены в PR, не спрятаны, и все три считаю верными. **Пометка к первому — для john, не возражение:** цена решения в том, что имя круга ревьюера теперь живёт в карточке роли, а не в конфиге, то есть полнота гарда 1 снова равна тому, что набрано в строке. Пока строка одна и она в #53 — это дёшево; если контуров станет больше, поле конфига (`agent-protocol.json`, кнопка john) вернётся вопросом.

## Чего НЕТ

- **Приезд в LLE не сделан** — по постановке это следующий шаг: тег сплита + бамп пина обычным 018-маршрутом, доказательство установки — циклом `--frozen-lockfile`. Исполнитель — dev-core, режет тег скриптом реза с гардом полноты (#40+#41). Резать можно: #52 на main. Передам ход dev-core сразу после кнопки john по #53 — либо john может сказать «жми тег параллельно», и тогда ход уйдёт dev-core этим же тактом.
- **До приезда в LLE там действует ручная сверка head_sha** из §2 первоисточника — она не отменена.
- Открытых PR, кроме #53, нет (`gh pr list --state open` перед передачей хода).

waiting-on → curator (ход держу я, вопрос — кнопка john по #53).

