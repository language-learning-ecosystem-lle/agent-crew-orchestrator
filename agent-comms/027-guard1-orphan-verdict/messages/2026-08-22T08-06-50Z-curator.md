---
from: curator
worker: claude-code
session: 0c42e720-2039-4b58-abab-af9f172d3499
date: 2026-08-22T08:06:50Z
expects: none
waiting-on: curator
---

**Обе кнопки нажаты рукой john — приняла своей проверкой, тред закрываю.** Вопросов к john в этом треде больше нет: оба, стоявшие в `2026-08-22T08-00Z-curator.md`, отвечены не словом, а действием, и я перемерила оба факта сама, а не приняла их по коммит-сообщению.

## Кнопка 1 — #53 смёржен

- `gh pr view 53` → `state=MERGED`, `mergedBy=maysway`, `mergedAt=2026-08-22T08:02:13Z`, `baseRefName=main`;
- **голова та самая, на которой мерены гарды:** `headRefOid=fbc97227b573aa8e297211fb44083711ade9383a` — посимвольно та, по которой дверь в прошлом сообщении дала `ok guard 1 · approved on fbc9722 … inside the round 32556674946` и `ok guard 2 · review=SUCCESS, checks=SUCCESS`. Голова после замера не уезжала;
- **контрольное чтение цели** (ответ инструмента ≠ факт): `mergeCommit.oid=d4b4bba4d3f3af5f4e72d724ed58e89701b38327`, и `git rev-parse origin/main` = ровно он же. Приехало в `main`, а не куда-то ещё;
- дифф коммита — один файл, `docs/roles/curator.md`, как и мерилось `merge-tree` перед этим.

Гард 4 отработал ровно так, как задуман: PR, меняющий карточку curator, дождался чужой руки. Мой чекаут уже стоит на `d4b4bba`, и карточка в нём совпадает с `main` (`git diff origin/main -- docs/roles/curator.md` — пусто).

## Кнопка 2 — пин в LLE бампнут, и приезд ЗАКРЫТ

Ступень, которую я в прошлом сообщении числила «не начата», сделана рукой john параллельно: коммит `e80498e1972a9bcd6a4eee16507e8d004a8e00e0`, `chore(protocol): пин v0.2.3 — якорь гарда 1 по прогонам (тред aco-027, рука john)`, `2026-08-22T08:03:03Z`. Замеры мои, чтением, в LLE ничего не тронуто (рабочее дерево там чистое, `pnpm ls` и `git` — только чтение):

- **пин переехал В ОБОИХ местах разом** — та самая ловушка, где правка одного роняла `main` LLE: `package.json:23` → `…#agent-protocol-v0.2.3`, `pnpm-workspace.yaml:36` (`overrides`) → `…#agent-protocol-v0.2.3`;
- **лок пересобран и указывает на ТОТ САМЫЙ артефакт:** `pnpm-lock.yaml` резолвит `agent-protocol` в `codeload…/tar.gz/c65f6104157a0e3419e9470dccc2658cdc8faebe` — посимвольно SHA тега `agent-protocol-v0.2.3`, который я сверяла с `git ls-remote` в прошлом сообщении. Специфаер в локе совпадает с обоими местами пина;
- **приезд проверен не по локу, а по УСТАНОВЛЕННОМУ дереву** — это и есть факт вместо косвенного признака: `node_modules/agent-protocol/package.json` → `"version": "0.2.3"`; `node_modules/agent-protocol/src/merge/gate.ts` → `reviewRunAnchor` 2 вхождения (#52, сам якорь); `node_modules/agent-protocol/src/usage.ts` → `--review-workflow` 2 вхождения (#55, справка). Обе починки лежат в живом контуре LLE, а не только в теге;
- `pnpm ls agent-protocol` в корне LLE → `agent-protocol@0.2.3`, exit 0; `origin/main` LLE = локальный `main` = `e80498e1` — бамп опубликован, не лежит на одном диске.

**Цикл `--frozen-lockfile` в живом дереве LLE я сознательно НЕ гоняла и говорю это прямо:** `pnpm install` там трогает `node_modules` контура, в котором прямо сейчас работают роли, а его цена — переустановка ради кода выхода. Воспроизводимость установки с этого тега снята dev-core честным циклом во временном чекауте (`msg-019`: `pnpm install` с нуля → `--frozen-lockfile` exit 0 → старт CLI из установленного дерева). Здесь снято то, что цикл и должен был доказать, — прямым чтением установленного кода.

## Хвост, который НЕ мой и потому назван поимённо

**Ручная сверка `head_sha` из §2 первоисточника снимается — условие её снятия («до приезда в LLE») выполнено.** Но живёт она в ленте контура LLE (`language-learning-ecosystem/agent-comms/096-guard1-orphan-verdict/`), и снимает её эта лента, а не я: в чужую почту из этого чекаута никто не пишет. Мост в 096 — john, он же переносил сюда постановку. Факт, на который там можно опереться, весь выше: дверь с якорем по прогонам стоит в LLE установленной, версия `0.2.3`.

## Приёмка треда — по фактам, не по докладам

Постановка (`msg-001`, §4 первоисточника) требовала трёх классов, и все три закрыты:

- **юниты фикстурами с реальными числами** — случаи а/б/б′/в/г/д плюс «чужой воркфлоу на голове не якорь», «без флага → by-hand», «без stamp → by-hand», «changes-requested отказывает ДО якоря»; стык — `gate.process.test.ts` настоящим процессом;
- **живая репетиция дискриминации** — #348 → `ok` (круг `32535411165`, вердикт в окне), путь `STOP` отработан живьём именем воркфлоу, круга которого на голове нет; расхождение с ожиданием по #347 разобрано числами (круг на новой голове стартовал `22:55:09Z`, на 36 с ПОЗЖЕ сироты `22:54:33Z` — в тот момент круга не было вовсе, новая дверь сказала бы `STOP`, старая говорила `ok`), а не списано;
- **приезд в LLE** — закрыт замерами выше.

Сверх постановки в треде закрылись два хвоста, которые она не предвидела: справка CLI не называла `--review-workflow` (#55) и карточка curator не несла ни флага, ни третьего состояния гарда 1 (#53). Оба — не косметика: без флага гард 1 честно отвечает `by-hand`, дверь не отказывает, и гард молча возвращается к сверке руками. Теперь строка процедуры в карточке и справка пакета говорят одно и то же.

Итог по репозиторию: `origin/main` = `d4b4bba`, все пять PR треда (#52, #54, #55, #56, #53) смёржены, тег `agent-protocol-v0.2.3` в origin, `gh pr list --state open` — **пусто**, кандидатов под кнопку curator нет.

**Тред закрываю** (`thread status --status closed`) — приёмка, право роли. Открывать заново есть чем: если 096 обнаружит, что якорь по прогонам где-то судит не так, это новый замер и он вернётся сюда.

waiting-on → curator (тред закрыт, ход никому не передаётся).

