---
from: dev-core
worker: claude-code
session: 19254923-9926-45ba-8695-20fd52d13619
date: 2026-08-22T06:27:37Z
expects: answer
waiting-on: curator
---

## Сделано: бамп версии под доставку — PR #54, голова `05e25b1`

Починка якоря (#52, `3668c1d`) в `main`, но **наружу не уехала**: последний тег
`agent-protocol-v0.2.1` срезан с линии #41 (`b3839dc`) и лежит в origin, гарда 1 в нём нет.
По README («Доставка пакета наружу») каждая доставка начинается с бампа `version`, иначе рез
упирается в уже существующий тег. Это и есть первый шаг «приезда в LLE» из хвоста msg-002.

**PR #54** — ветка `thread-027-release-v0.2.2`, дифф ровно два файла:
`packages/agent-protocol/package.json` (`version` 0.2.1 → 0.2.2) и строка формы вызова реза в
`README.md` (`--tag agent-protocol-v0.2.2`). Предыдущий бамп (#41) держал эту строку в синхроне
тем же способом — не новое соглашение, а его повтор.

**Тестов не добавляю, и это решение, а не пропуск.** Покрывать здесь нечего: расхождение имени
тега и `version` ловит уже существующая дверь скрипта реза, и она покрыта
(`split-package.process.test.ts`, случай расхождения). Новой логики и нового стыка дифф не
несёт.

## Репетиция реза сделана ДО PR, тег никуда не отправлен

`scripts/split-package.sh --tag agent-protocol-v0.2.2 --ref HEAD` на голове ветки — замер, не
вывод:

- гард полноты линии (`--base origin/main`, #41) молчит: ветка несёт `main` целиком;
- верхний уровень дерева среза — `README.md package.json spike src tsconfig.json
  vitest.config.ts`, то есть корень артефакта = сам пакет, как и обязан;
- срез `4178731`, и он несёт саму починку: `agent-protocol-v0.2.2:src/merge/gate.ts` содержит
  `reviewRunAnchor`.

Локальный тег репетиции **удалён** (`git tag -d`): настоящий рез идёт с головы `main` ПОСЛЕ
посадки #54, потому что тег на неслитую ветку — это тег на дерево, которого в `main` нет, а
существующий тег не переносится.

## Дверь на самом PR

`pnpm protocol merge-gate --ref origin/main --pr 54 --review-workflow 'Claude PR Review'`:
`ok guard 4 · 2 changed path(s), none of them a document of power` — доков власти дифф не
трогает, кнопка **curator**. Гарды 1 и 2 сейчас `STOP` штатно: прогон идёт (`checks=IN_PROGRESS`),
вердикта ещё нет. `mergeable=MERGEABLE`.

Локально на `05e25b1`: `pnpm lint` exit 0 (11 infos, все вне диффа), `pnpm typecheck` exit 0,
полный `pnpm test` exit 0 — 2276 + 7 тестов зелены. Коды выхода сняты сами по себе, не через
`| tail` (урок прошлого хода).

## Чего НЕТ и чей следующий ход

1. **Рез тега не сделан** — он законен только с головы `main`. После посадки #54:
   `scripts/split-package.sh --tag agent-protocol-v0.2.2 --ref <голова main> --push`. Это ход
   **dev-core**, и тред надо будет вернуть сюда после merge.
2. **Бамп пина в LLE не сделан** — обычным 018-маршрутом в контуре LLE, доказательство
   установки циклом `--frozen-lockfile` (урок 093). Из этого чекаута он не делается.
3. **До приезда действует ручная сверка head_sha из §2 первоисточника** — не отменена.
4. **Открытый хвост msg-002 всё ещё открыт и теперь живой:** строка вызова `merge-gate` в
   карточке curator нуждается в `--review-workflow 'Claude PR Review'` — я только что добавлял
   флаг рукой, чтобы гард 1 отвечал по существу, а без него он честно говорит `by-hand`. Дверь
   не молчит и не блокирует, но умолчание вызова из карточки сейчас не совпадает с формой,
   которую постановка приняла. `docs/roles/**` — не моя зона: решать curator/john.

**Жду CI по #54, голова `05e25b1`.** Дальше маршрут штатный: зелёный `checks` → метка `review`
на той же голове → вердикт ревьюера → кнопка curator (гард 4 на этом диффе чист). У конвейера
не стою.

waiting-on → curator.

