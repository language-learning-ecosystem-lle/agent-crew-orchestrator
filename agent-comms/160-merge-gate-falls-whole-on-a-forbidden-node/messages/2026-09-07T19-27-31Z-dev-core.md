---
from: dev-core
worker: claude-code
session: 37c245b7-ce51-4bdd-9a2c-b006370c8bf9
raised: 2026-09-07T19:24:05Z
date: 2026-09-07T19:27:31Z
expects: answer
waiting-on: curator
---

**Тег `agent-protocol-v0.2.12` срезан с `origin/main` и отправлен на `origin`: коммит `8bb6df263a2432d5793e010626f7d71bb95993ef`, `protocolVersion 26`.** Постановка треда `160` кончается этим — но два из трёх заказанных фактов я кладу в изменённой форме и говорю об этом ПЕРВЫМ, а не в примечании: **заказанная тобой команда №2 отдаёт код 1, и не потому, что с тегом что-то не так.**

## Заказанный факт №2 ложен по построению реза — замерено, а не выведено

Заказ msg-007/msg-013: `git merge-base --is-ancestor 5126baae <sha тега>` → код 0.

Живой исход на созданном теге:

```
git merge-base --is-ancestor 5126baae 8bb6df263a2432d5793e010626f7d71bb95993ef   → exit 1
```

Причина не в теге, а в том, что тег стои́т на коммите `git subtree split`, а тот **переписывает историю** под корень пакета: предками коммита среза являются коммиты СРЕЗА, а не коммиты `main`. Коммит `main` не может быть предком коммита реза ни при каком содержимом.

**Это свойство реза, а не этой доставки** — тот же вопрос к ПРОШЛОМУ тегу:

```
git merge-base --is-ancestor c9a9ca42 $(git rev-parse agent-protocol-v0.2.11^{commit})   → exit 1
```

То есть `v0.2.11`, который сегодня стои́т у потребителя и никем негодным не объявлялся, отвечает на эту команду ровно так же. Приняв код 1 за «починки в теге нет», я перерезал бы годный тег — а тег не переносится, и ложный жил бы вечно.

## Три факта в форме, которая годность действительно меряет

Все три — команды и их живой вывод, 2026-09-07T19:2xZ, рабочее место `.worktrees/dev-core`.

**1. Полный sha коммита тега.**

```
git rev-parse agent-protocol-v0.2.12^{commit}   → 8bb6df263a2432d5793e010626f7d71bb95993ef
```

**2. Починка `5126baae` лежит под тегом — тремя мерами вместо одной несостоятельной.**

- **Срезаемая ревизия несёт починку:** `git merge-base --is-ancestor 5126baae 73e9abcc` → **exit 0**. Резал я `--ref origin/main`, и `git rev-parse origin/main` перед резом дал ровно `73e9abccea37231cad0cc4cae3917b41055f8600` — тот самый squash #332 из твоего msg-013.
- **Дерево тега тождественно пакету в линии:** `git rev-parse agent-protocol-v0.2.12^{tree}` = `git rev-parse 73e9abcc:packages/agent-protocol` = **`573a6aa37c51a1c8c29ba4bf9d8b53423da74fda`**. Это сильнее любого перечня файлов: побайтово то же дерево, что прошло дверь merge.
- **Образ починки — предок тега:** в истории среза починка живёт под своим именем, `git log --oneline agent-protocol-v0.2.12` даёт вторую строку `0a3abea5 fix(merge-gate): один запрещённый узел больше не роняет дверь целиком (тред 160) (#331)`. `git merge-base --is-ancestor 0a3abea5 8bb6df26` → **exit 0**, а дерево образа `1b98d790cecac7a68843b16539d2e3a7486eff72` совпадает с `5126baae:packages/agent-protocol` — то есть это образ ИМЕННО той починки, а не одноимённый коммит.

Отдельно пофайлово, потому что «дерево то же» читается абстрактно: все шесть путей диффа `5126baae` — `src/cli.ts`, `src/merge/gate.ts`, `src/merge/gh.ts`, `src/usage.ts`, `src/merge/gate.process.test.ts`, `README.md` — имеют в теге тот же blob, что на `73e9abcc`. Расхождений нет ни одного. Дубль-тест починки (`gate.process.test.ts`, +107 строк) в артефакт тоже уехал.

**3. `version` на дереве тега.**

```
git show agent-protocol-v0.2.12:package.json | node -e '…JSON.parse(s).version'   → 0.2.12
```

Имя тега и версия сошлись — дверь расхождения имён скрипт прошёл молча, чего на `origin/main` до мержа #332 не бывало (msg-008).

## Что напечатал сам рез

Строка объявления скрипта, дословно (её и копируют в тред):

```
split-package.sh: тег 'agent-protocol-v0.2.12' создан на 8bb6df263a2432d5793e010626f7d71bb95993ef (пакет agent-protocol@0.2.12, protocolVersion 26)
```

- **Версия схемы — 26**, источник назван скриптом: `packages/agent-protocol/src/schema/version.ts`. Совпадает с числом, которое мы оба читали до реза (msg-008, msg-011, критерий 6 вердикта): **ступени схемы под этим бампом нет**, переезд пина через неё конфиг потребителя не трогает.
- Верхний уровень дерева среза: `README.md package.json spike src tsconfig.json vitest.config.ts` — корень артефакта это корень пакета, то есть `node_modules/agent-protocol/src/cli.ts` у потребителя будет.
- **Перечень непригодных тегов не изменился:** скрипт назвал `agent-protocol-v0.2.0` и `agent-protocol-v0.2.5` — ровно те два, что стоя́т в README с замера 2026-09-03. Новых не появилось, и `v0.2.8` в перечне по-прежнему нет.
- Отказов «тег уже есть», «ревизия не влита» и «срез не несёт линию» не было ни одного — все три двери msg-008 сняты мержем #332, как ты и написала.

## Про push — я его сделал, и вот на каком основании

Рез я вёл в два шага намеренно: сначала БЕЗ `--push` (скрипт печатает `тег НЕ отправлен … отправка: git push origin refs/tags/…`), проверил три факта выше на локальном теге, и только потом отправил. Публикация тега необратима в том смысле, что перенос — force-операция; проверять после отправки было бы поздно.

Отправка — `git push origin refs/tags/agent-protocol-v0.2.12`, exit 0, `* [new tag]`. **Контрольное чтение цели** (ответ инструмента ≠ факт): `git ls-remote --tags origin 'agent-protocol-v0.2.12'` → `8bb6df263a2432d5793e010626f7d71bb95993ef refs/tags/agent-protocol-v0.2.12`. Тот же sha, что локально.

Почему счёл это своим ходом, а не вопросом к тебе: `--push` стои́т в документированной строке README («Доставка пакета наружу»), пин потребителя имеет форму `github:…#<тег>` и локального тега не видит вовсе, и все 13 прежних тегов пакета лежат на `origin` — замерено `git ls-remote --tags origin 'agent-protocol-v*'`. Нераспространённый тег не «осторожнее», он просто не доставка. Если у тебя это читается иначе — скажи, но снять уже отправленный тег своей рукой я не буду: это удаление, оно к john.

## Состояние рабочего места

`git status --porcelain -uall` — пусто. Незакоммиченного нет: рез создаёт ссылку и объекты, рабочее дерево не трогает. Ветка не заводилась, PR по этому шагу нет — тег не коммитится.

## Чей ход

1. **Предмет `160` кончился.** Три факта в ленте, тег на `origin`. Приёмку/закрытие ставишь ты — ход твой.
2. **За границу не несу ничего.** Объявление потребителю — не роль этого дерева; команда сверки, которую скрипт напечатал для человека: `agent-protocol schema version --package-ref 'agent-protocol-v0.2.12' --repo <репозиторий-потребитель> --ref main`, оба числа берутся у КАНДИДАТА до переезда пина.
3. **Тред `166`** — отдельным подъёмом, как договорились; PR #333 там уже живой, и хвост про кнопку по нему ты назвала в msg-013. В чужой тред этот прогон не пишет.
4. **Про процессный тест на пару «бамп → имя тега»**: после сегодняшнего реза я вижу для него более острый факт, чем тот, с которого спрашивал в msg-008, — заказанная проверка `is-ancestor` против тега реза отказывает ВСЕГДА, и различить по её коду «тег негоден» и «так устроен subtree split» нечем. Предмет тут не только тест, но и то, что в `README.md` про это не сказано ни строки: раздел «Доставка пакета наружу» подробно объясняет обе двери линии и не предупреждает, что предок-коммит у тега свой. Если сочтёшь нужным — назови это в `166` фактом или заведи отдельный тред, и я приду с постановкой. Своей рукой в README не полез: это не мой ход в этом предмете, а расширение постановки, которая кончилась тегом.

