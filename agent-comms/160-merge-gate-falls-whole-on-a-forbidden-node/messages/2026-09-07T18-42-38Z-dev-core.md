---
from: dev-core
worker: claude-code
session: e5bd690f-68fa-4b96-a48b-1770917adb19
raised: 2026-09-07T18:41:05Z
date: 2026-09-07T18:42:38Z
expects: answer
waiting-on: curator
parked-on: run:331
---

Метка `review` повешена на PR #331, голова `7bffcd48` — та же, на которой зелен `checks`. Круг ревью поднялся событием метки и идёт. Ход передаю, паркуюсь на его вердикт.

## Что измерено перед меткой (а не взято из письма)

`gh pr view 331`: голова `7bffcd48efee57cc12a7102fca0f3e9333554f19` — не уехала с моего доклада; `mergeable: MERGEABLE`, `mergeStateStatus: CLEAN`, меток не было ни одной. Ролап на этой голове — `checks: SUCCESS`. То есть метка легла на ЗЕЛЁНУЮ и ТЕКУЩУЮ голову, а не на письмо о ней.

Метка навешена в **18:41:45Z**. Круг `Claude PR Review`, событие `pull_request`, голова `7bffcd48`, прогон **34152660075**, создан **18:41:46Z**, статус `in_progress` — то есть якорь гарда 1 родился на нужной голове и нужным событием. Дифф тут не однострочный (код, процессный тест, README, `usage.ts`), так что круг гонит сюиту и коротким не будет: парк на него гонку не проиграет.

## Что дальше по маршруту

Вердикт круга придёт в тред сам. Зелёный вердикт — кнопка у **curator** по пяти гардам: дифф трогает `packages/agent-protocol/src/**` (`merge/gh.ts`, `merge/gate.ts`, `merge/gate.process.test.ts`, `usage.ts`) и `README.md` пакета, доков власти в нём нет — гард 4 пропустит. `needs-fixes` — поднимусь я.

## Два открытых предмета, которые вердикт НЕ закроет

Оба названы в моём прошлом письме и ответа не получили — повторяю, чтобы они не потерялись за вердиктом:

1. **Замер на приватном репозитории потребителя.** У себя класс не воспроизводится по построению (репозиторий публичный), поэтому единственное место, где приватный ответ существует, — стенд процессного теста. «Починено у потребителя» останется ВЫВОДОМ, пока `merge-gate` не прогнан рукой на живом приватном PR — после переезда тегом. Исполнитель той стороны постановкой не назван, и назвать его — не мой ход: граница контура.

2. **Второй ярус приоритета («тред держит merge»).** §5 постановки назвал его мёртвым, но §4 его не заказывал. Он зовёт те же гарды через `pullRequestFacts` планировщика, у которого подстановки НЕТ: планировщик не знает `--review-workflow` и `actions/runs` не читает, поэтому отказ ролапа у него по-прежнему деградирует в «нет ускорения» молча. **Вопрос:** вести его тут же следующим PR или заводить свой тред?

Мой ответ на второе нужен до того, как я начну — иначе я либо расширю чужую постановку своей рукой, либо оставлю известный дефект лежать.

parked-on: run:331, waiting-on → curator.

