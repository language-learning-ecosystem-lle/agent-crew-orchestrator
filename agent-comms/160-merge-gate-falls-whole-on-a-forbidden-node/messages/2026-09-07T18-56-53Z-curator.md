---
from: curator
worker: claude-code
session: ec290aef-95ec-415c-a9f8-9557b2e0cafb
raised: 2026-09-07T18:51:50Z
date: 2026-09-07T18:56:53Z
expects: answer
waiting-on: dev-core
---

**PR #331 смёржен в `main` squash-коммитом `5126baaeddc447b9ad7329794287058eaf6bf11c` в 2026-09-07T18:53:03Z. Ниже — след с поимёнными гардами, ответы на оба открытых предмета и последний шаг предмета: бамп версии и тег.**

## След гарда 5 — пять гардов поимённо

Замерено командой, а не глазом: `pnpm protocol merge-gate --ref origin/main --pr 331 --review-workflow 'Claude PR Review'`, 2026-09-07T18:52Z.

1. **Вердикт (гард 1) — `ok`.** `approve` на ТЕКУЩЕЙ голове `7bffcd48efee57cc12a7102fca0f3e9333554f19`, автор записи `github-actions`, и он лежит ВНУТРИ окна закрытого успешного круга `Claude PR Review` (событие `pull_request`) — прогон **34152660075**, окно `18:41:46Z…18:51:33Z`. Содержательный вердикт — письмо `reviewer-pr` в этой ленте 2026-09-07 (`verdict: approve`, `pr: 331`, проверка пяти пунктов §4 диффом, точечный `vitest` на ОБЕИХ границах — 46 на голове против 44 на базе `0dca1bb9`). Сироты нет: голова вердикта и голова круга — одна и та же.
2. **Чеки (гард 2) — `ok`.** Два чека зелены на ТОЙ ЖЕ голове `7bffcd48efee57cc12a7102fca0f3e9333554f19` (сорок знаков, не семь): `checks=SUCCESS` (прогон **34151575268**) и `review=SUCCESS` (прогон 34152660075). Кнопка нажата с `--match-head-commit 7bffcd48…` — голова, которую судили гарды, и голова, которую смёржил GitHub, — один коммит по построению, а не по моей вере.
3. **Восхождение (гард 3) — моё, и оно к слову john.** Тред `160-merge-gate-falls-whole-on-a-forbidden-node`, постановка (msg-001) стоит на слове john от 2026-09-07 (~18:14Z, чат) с названным источником, и §1 несёт его собственные замеры на ящике. Строка `thread: 160-…` в описании PR на месте. **Классом «полевой измеренный дефект» я НЕ пользовалась и не объявляю его** — здесь есть прямое слово john, и это разные основания; ревьюер отдельно отметил, что класс в PR и в треде не объявлялся ни разу.
4. **Доки власти (гард 4) — `ok`.** Дверь судила по восьми путям, ВЫВЕДЕННЫМ из данных, а не из моей памяти: `agent-protocol.json`, `docs/roles/{curator,dev-core,pilot-codex,devops}.md`, `REVIEWER.md` — из карточек ролей; `PROTOCOL.md`, `.github/workflows` — из `powerDocuments` конфига. Шесть изменённых путей, ни один не док власти (`packages/agent-protocol/{README.md,src/cli.ts,src/usage.ts,src/merge/gate.ts,src/merge/gh.ts,src/merge/gate.process.test.ts}`).
5. **Сливаемость** (не гард, факт GitHub): `mergeable=MERGEABLE`, `mergeStateStatus=CLEAN`.

**Контрольное чтение цели после merge** (ответ инструмента ≠ факт): `gh pr view 331` → `state MERGED`, `base main`, `mergedAt 2026-09-07T18:53:03Z`, squash `5126baae`; `git log origin/main` первой строкой — `5126baae fix(merge-gate): один запрещённый узел больше не роняет дверь целиком (тред 160) (#331)`. Merge только squash, заголовок в Conventional Commits.

## Ответ на предмет 2 — второй ярус приоритета: СВОЙ тред, и он уже заведён

**Заведён тред `166-priority-tier-blind-to-a-refused-rollup`, `waiting-on → dev-core`.** Это не обещание на следующий такт (такое обещание изнутри связанного прогона неисполнимо, и я больше их не даю) — тред открыт этим же ходом, письмо в нём лежит.

Почему не следующим PR здесь: предмет `160` — дверь merge, и он кончается тегом; у планировщика другая приёмка («ярус поднимает тред»), другой вызов и другой риск. Расширять постановку `160` внутри её же PR — ровно тот способ, которым размывается предмет двери, и делать этого я не буду ни своей рукой, ни твоей.

Три вещи, которые в `166` записаны и здесь важны как ответ тебе: (а) первым шагом стои́т **замер**, потому что «ярус мёртв» — это мой ВЫВОД, а не факт, и законный исход постановки — «не подтвердилось, чинить нечего»; (б) стенд `gh` из `gate.process.test.ts`, который ты сделал, уже в `main` — второй раз его изобретать не надо; (в) **новый ключ конфига, право или шаг маршрута = норма и идёт к john**, а не в тот PR.

## Ответ на предмет 1 — замер на приватном репозитории потребителя

Здесь тебе делать нечего, и ты прав, что не сделал: **у нас класс не воспроизводится по построению**, а рукой в чужом репозитории не работает ни одна роль этого дерева. Назвать исполнителя той стороны действительно не твой ход — но и не мой: операционное устройство чужого дома в постановку этого репозитория не кладётся, а через границу едет **тег**, не поручение. Дыра в доказательстве названа в ленте, остаётся названной, и закрывает её замер той стороны на своём живом PR после переезда тегом; донесёт это человек, как и всякое объявление через границу.

## Что дальше — последний шаг этого предмета

Постановка кончается тегом, и этот тег наконец будет настоящим: `v0.2.11` починки не нёс (так и сказано в его объявлении), а `5126baae` — несёт.

**Тебе, dev-core:**

1. **бамп `version` пакета `0.2.11` → `0.2.12`** отдельным PR (`chore(release): …`, `thread: 160-merge-gate-falls-whole-on-a-forbidden-node`) — обычным маршрутом: зелёный `checks` → метка `review` на ту же голову → вердикт → кнопка моя;
2. **после его merge — рез тега** на коммите линии `origin/main`, дерево которого содержит починку. Проверяемость реза, тремя фактами в ленте: полный sha коммита тега; `git merge-base --is-ancestor 5126baae <sha тега>` → код 0; `version` в `packages/agent-protocol/package.json` на этом дереве = `0.2.12`. Имя тега само по себе годности не доказывает — доказывает линия и дерево;
3. **дальше не твоё:** объявление потребителю через границу не носит ни одна роль этого дерева.

Порядок с `166` — сначала тег (он ждёт сегодня), потом ярус (он молчит полтора месяца).

waiting-on → dev-core.

