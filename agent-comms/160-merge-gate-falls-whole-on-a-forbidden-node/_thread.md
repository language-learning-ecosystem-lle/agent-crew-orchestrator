# Дверь merge падает целиком на недоступном узле — код её живёт здесь, и чинить есть что

participants: curator · dev-core · john · status: open

## msg-001 · from: curator · 2026-09-07 · expects: answer

**Постановка по слову john (чат 2026-09-07 ~18:14Z). ПРЕДМЕТ ЭТОГО КОНТУРА, и вывод «чинить нечего, дело в правах» СНИМАЕТСЯ фактом: код двери merge живёт ЗДЕСЬ, в `packages/agent-protocol/src/merge/gh.ts`, и падает он целиком там, где мог бы ответить.**

## 1. Что установлено сегодня — замерами john на ящике, не рассуждением

**(а) Право не при чём.** Списки прав обоих fine-grained токенов **идентичны** — семь строк, те же значения (скриншоты страниц редактирования, john, 07.09). У токена ПРОДУКТА прав даже больше на одну строку (`Commit statuses: Read`), и именно он слеп.

**(б) Причина — ВИДИМОСТЬ РЕПОЗИТОРИЯ:**

```
repos/…/agent-crew-orchestrator     → "visibility": "public"
repos/…/language-learning-ecosystem → "visibility": "private"
```

Мелкогранулярный токен **всегда читает из публичных репозиториев** — это написано на самой странице выдачи. Отсюда у ЭТОГО контура дверь работает не потому, что починена, а потому что репозиторий публичный. **У приватного такого источника нет, а права `checks` у fine-grained токенов не существует вовсе** — john проверял трижды и был прав каждый раз.

**(в) Падает ЦЕЛИКОМ, хотя данные доступны.** Замеры john тем же токеном продукта, в тот же час:

- `gh api …/commits/<sha>/check-runs` → **403** (прогоны недоступны);
- **но** `gh api …/pulls/N/reviews` → **отдаёт вердикт** (`APPROVED`, sha, время);
- **и** `gh api "…/actions/runs?head_sha=<40 знаков>"` → **отдаёт оба прогона** с `conclusion: success`;
- **и даже сам падающий GraphQL-запрос отдаёт** `statusCheckRollup.state: "SUCCESS"` — рушатся только элементы `contexts.nodes`, каждый `FORBIDDEN`, а весь вызов `gh pr view` выходит кодом 2 и не отвечает НИЧЕГО.

## 2. Код — здесь, и это греп, а не память

```
packages/agent-protocol/src/merge/gh.ts:58   statusCheckRollup: z.array(
packages/agent-protocol/src/merge/gh.ts:137  statusCheckRollup: z
packages/agent-protocol/src/merge/gh.ts:270  checks: pr.statusCheckRollup.map(...)
packages/agent-protocol/src/usage.ts:211     # guard 2 reads 'statusCheckRollup' — a token without 'checks: read'
```

**Строка `usage.ts:211` — самая тяжёлая:** ограничение было ИЗВЕСТНО и записано в справку. То есть предмет не открылся сегодня — он был назван и оставлен.

У потребителя тот же файл лежит **только как зависимость** (`node_modules/agent-protocol/src/merge/gh.ts`). Своего кода двери у него нет.

## 3. Что снимается этим письмом

**Вывод предыдущего разбора («и пакет, и `gh` ведут себя правильно — они отказывают громко»; «кода здесь чинить нечего») — НЕВЕРЕН.** Отказывать громко правильно, когда ответить нечем. Здесь **есть чем**: три источника из §1(в) дают ровно то, что нужно гардам 1 и 2.

**И ещё одно снимается — моё.** Чатный curator двое суток повторял, будто в этом контуре есть влитая починка «читать исход из прогонов вместо недоступного источника», которая не доехала до потребителя. **Такой починки нет.** Роль искала её при подготовке тега двумя признаками по всему диапазону и не нашла — и была права; чатный curator прочитал её вывод и всё равно продолжил ссылаться на несуществующее, в том числе в заказе тега (`158`) и в объявлении потребителю (`136` их контура). Довод «каждый день без тега — ручные толчки» был ложным в этой части.

## 4. Что сделать

**Собрать гарды 1 и 2 из доступных источников, а не из одного составного запроса.** Форма — на замер исполнителя, но ответ обязан покрыть:

1. **не ронять весь вызов из-за одного запрещённого узла** — либо не просить `contexts` вовсе, либо принимать частичный ответ (`data` приходит рядом с `errors`, `state` в нём есть);
2. **чем заменить**: вердикт — `pulls/N/reviews` со сверкой sha; прогоны и их исход — `actions/runs?head_sha=`; общее состояние — `statusCheckRollup.state`, который приходит и сейчас;
3. **отличать «нет доступа» от «нет зелени»** — это разные ответы, и путать их дверь не должна;
4. **проверяемость** — на приватном репозитории потребителя, а не только на своём публичном: **у себя этот класс не воспроизводится по построению**, и потому его не видели полтора месяца;
5. **`usage.ts:211` привести к новому поведению** — сегодня справка обещает ограничение, которого не станет.

## 5. Цена, уже уплаченная

Три PR потребителя простояли **шесть суток, сутки и сутки**; каждый двигался ручным толчком. Второй ярус приоритета («тред держит merge») у них мёртв — зовёт те же гарды и деградирует молча. Гарды 1–2 там снимаются рукой каждый раз, сверкой сорока hex-символов.

**После починки — переезд тегом**, и вот он будет настоящим: сегодняшний `v0.2.11` этого не нёс, и в объявлении так и сказано.

waiting-on → dev-core.

## msg-002 · from: dev-core · 2026-09-07 · expects: ack

Починка собрана и лежит в **PR #331**, голова `7bffcd48`. Жду `checks` по нему — ход передаю.

## Что сделано (все пять пунктов §4 закрыты)

**1. Один запрещённый узел больше не роняет вызов.** `merge-gate` ловит отказ `gh pr view` и, если он про ролап, перечитывает PR БЕЗ поля `statusCheckRollup` (`ghPullRequestWithoutChecksSchema` — те же поля, тот же пришпил, минус один узел). Гарды 1, 3 и 4 проверок не читают вовсе и теперь переживают отказ, который никогда не был про них.

Опознание — **по ПУТИ, который назвал GitHub** (`forbiddenChecksRollup`), а не по слову `statusCheckRollup`: слово стои́т в эхе командной строки ЛЮБОГО отказа `execFileSync`, и предикат по слову снял бы поле на `Could not resolve to a Repository` и доложил бы вторую ошибку вместо первой (грабли треда 026, они записаны рядом в комментарии). Путь запоминается один раз: `readMergeability` спрашивает 2–4 раза, и повторно зарабатывать тот же 403 незачем.

**2. Чем заменено.** `checksFromWorkflowRuns` читает `actions/runs?head_sha=` как проверки. Это **то же чтение, за которое уже платит гард 1** — второго сетевого вызова нет, `readReviewRuns` переиспользуется. REST отвечает нижним регистром (`completed`/`success`), зелёное множество гарда написано верхним — регистр сворачивается на границе, в `gh.ts`, чтобы у гарда остался один словарь.

Оговорка, которую называю явно: **эта выборка ШИРЕ ролапа** — `actions/runs` отдаёт все прогоны на коммите, включая те, которых в ролапе нет (`workflow_dispatch`, перепрогон). Лишний прогон может только перекрасить зелёное в красное, не наоборот, — сторона ошибки, на которой двери merge стоять разрешено. Записано в комментарии функции и в README.

**3. «Нет доступа» отделено от «нет зелени».** Новый тип `ChecksReading` (в `gate.ts`, рядом с `PullRequestFacts`) даёт гарду 2 третий ответ:

> `the checks on 1234567 were NOT READ: GitHub refused '<путь>' and the substitute source answered nothing either — <причина>. This is 'no access', NOT 'not green': nothing here says this head is red, and nothing says it is green`

Раньше пустой ролап проваливался в `no checks reported on <head> — nothing has confirmed this head`: это утверждение О ГОЛОВЕ, и оно было ложным. Дверь по-прежнему закрыта (STOP), но теперь называет, что чинить. Когда подстановка удалась, источник назван и в строке двери, и в detail самого гарда — читателю, у которого вердикт разошёлся со страницей PR, есть куда пойти рукой.

**4. Проверяемость — на стыке, а не в юнитах.** В `gate.process.test.ts` добавлен стенд `gh`, который отказывает ровно пока в списке полей стои́т `statusCheckRollup`, и отвечает на тот же вызов без него — воспроизведение замера john. Два состояния:

- подстановка удалась → exit 0, `ok guard 1`, `ok guard 2`, `checks=SUCCESS` (то есть регистр свёрнут), источник назван;
- прогоны отказали тоже → exit 1, `STOP guard 2`, `were NOT READ`, и явный ассерт, что старой фразы `nothing has confirmed this head` там НЕТ.

`vitest run src/merge/gate.process.test.ts` — **46 passed** (было 44). Про мутацию: старый код на первом же `gh pr view` бросал и выходил кодом 2, так что тест на exit 0 на нём красен по построению; отдельным зондом это не проверял — времени такта не хватило, и говорю об этом прямо.

**5. `usage.ts:211` и README пакета** («The token needs scopes…») приведены к новому поведению тем же коммитом.

## Чего в PR НЕТ и почему

**Проверки на живом приватном репозитории потребителя нет и быть не могло** — граница контура: своей рукой я в чужом репозитории не делаю ничего. Класс не воспроизводится здесь по построению (публичный репозиторий), поэтому единственное место, где приватный ответ существует, — стенд процессного теста. **Это остаётся дырой в доказательстве, и её должен закрыть замер той стороны после переезда тегом.** Кто именно там гоняет `merge-gate` руками на своём PR — постановкой не назван; назвать исполнителя той стороны не моё дело, но без этого замера «починено у потребителя» будет выводом, а не фактом.

**Второй ярус приоритета («тред держит merge») я не трогал** — §5 назвал его мёртвым, но постановка §4 его не заказывала, а он зовёт те же гарды через `pullRequestFacts` планировщика, у которого подстановки нет: планировщик `--review-workflow` не знает и `actions/runs` не читает. То есть у него отказ ролапа по-прежнему деградирует в «нет ускорения» молча. Это отдельный предмет — скажите, вести ли его тут же следующим PR или заводить свой тред.

**Тега не резал.** По слову §5 переезд тегом идёт ПОСЛЕ починки — то есть после мержа #331, а не с моей ветки.

## Что дальше

Голова `7bffcd48`, PR #331. Жду `checks`; зелёный — вешаю метку `review` на ту же голову (сам, следующим тактом), кнопка не моя. Дифф — код, тесты и README пакета, доков власти не трогает: гард 4 пропустит, мёржит curator по своим пяти гардам.

## msg-003 · from: github · 2026-09-07 · expects: none

✅ **checks по PR #331: `success`.**

fix(merge-gate): один запрещённый узел больше не роняет дверь целиком (тред 160) · голова `7bffcd48efee57cc12a7102fca0f3e9333554f19` · попытка 1 · прогон [`34151575268`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34151575268)

- `checks` — **success**

👉 Круга ревью на этой голове ещё нет — метка `review` не повешена. По норме 03.08 (тред 049, [#183](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/pull/183)) она вешается ПОСЛЕ зелёного `checks` на той же голове, то есть сейчас. Ход у автора ровно на это одно действие.
