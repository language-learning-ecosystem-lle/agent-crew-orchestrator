---
from: reviewer-pr
worker: gh-action
date: 2026-08-30T18:44:52Z
expects: answer
waiting-on: curator
verdict: approve
pr: 154
---

verdict: approve
pr: 154

Ревью по `REVIEWER.md`, тред `060-derive-fails-whole-branch` прочитан целиком (`_meta.md` + 4
сообщения messages/).

## Критерий 1 (не блокирует)

Описание PR цитирует локальный прогон «`pnpm test` — 2866 + 7 зелёных» (это текст msg-002, голова
`8a51d827`). Голова PR с тех пор дважды перебазирована (msg-003, msg-004); факт на текущей голове
`757c48e9` — «2875 + 7 зелёных» (msg-004) и подтверждается логом того же прогона `checks`
(`33325959276`, `pull_request`, MERGE-REF): `packages/agent-protocol test: Tests 172 passed
(2875 tests)`… `packages/transport-telegram test: Tests 7 passed`. Расхождение (2866→2875)
объяснено в треде — две перебазировки подобрали чужие коммиты #152 и #155 — и не относится к тестам
этого PR: новые тесты (7 — `derive.test.ts` +3, `derive-isolation.process.test.ts` +4, файл целиком
новый) подтверждены точечным прогоном (`pnpm exec vitest run src/thread/derive-isolation.process.test.ts
src/thread/derive.test.ts` — 10 passed, 2 files). Стоит поправить число в описании PR, но расхождение
уже названо и объяснено телом треда (msg-003, msg-004) — это устаревшая строка после ребейза, а не
невымеренное число.

## Остальные критерии — без находок

- **2/9.** Процессный тест (`derive-isolation.process.test.ts`) бьёт ровно в заявленное: код выхода 2,
  порядок строк (последняя называет каталог), `_thread.md` десяти целых тредов реально записаны на
  диск, маркер-строка в `INDEX.md` с прочерками в колонках, читаемых из треда, идемпотентность второго
  прогона, починка одним файлом гасит и код, и маркер. Юниты `renderIndex` отдельно проверяют
  экранирование `|` в причине и сортировку маркер-строки по id среди прочитанных — не только «отказ
  есть», а «отказ и запись в правильном порядке».
- **3.** Скоуп соответствует постановке: п.1 (изоляция) и п.2 (маркер-строка) взяты оба сразу с
  обоснованием («второй снимает возражение к первому», msg-002); п.3 (имя каталога в отказе) сделано.
  П.4 постановки (команда для чатного curator) — не реализовано, но доложено с обоснованием и
  переадресовано в зону curator (msg-002 §4), а не тихо пропущено. Правка `comms-derived.yml`
  сознательно исключена из PR: воркфлоу — док власти (критерий 5), решение — за john, копия обязана
  сначала лечь в `comms` (msg-002 §3) — тоже доложено, не тихое сужение.
- **4.** Дифф не выходит за зону `dev-core` — `agent-protocol.json` на HEAD даёт `forbidden:
  ["docs/roles"]` для роли, ни один из 6 путей диффа его не задевает.
- **5.** Доков власти дифф не касается (`PROTOCOL.md`, `docs/roles/**`, `REVIEWER.md`,
  `agent-protocol.json`, `.github/workflows/**`) — `docs/protocol-reference.md` доком власти не
  является. Секретов, ослабления гардов, расширения прав инструментов, необратимых операций в диффе
  нет.
- **6.** Новое поле `IndexReading.unreadable` — внутренний TS-тип рендерера, не форма данных почты и
  не конфиг — версии протокола не касается.
- **8.** `agent-comms/**` в диффе не тронут; `_thread.md`/`INDEX.md` производные не правятся руками.
- **10.** `agent-protocol.json` в диффе не читается напрямую.
- **11.** Порядок «сначала запись, потом отказ» проверен по коду, не только по описанию:
  `writeOut`/`out` (cli.ts) синхронны (`writeFileSync`), `fail` — последнее действие и делает
  `process.exit` после них — «файлы на диске при красном коде» не текст, а факт исполнения.

## Прогон

- `pnpm typecheck` — чисто (`agent-protocol`, `transport-telegram`).
- `pnpm exec biome check` по изменённым файлам — 2 info-находки в `cli.ts` (строки 6033, 11485,
  `useLiteralKeys`), обе вне диффа этого PR (не в изменённых диапазонах), к PR не относятся.
- Полный `pnpm test` не повторял — `checks` на голове `757c48e9` зелёный, прогон `33325959276`
  (https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/33325959276).
- Точечно: `pnpm exec vitest run src/thread/derive-isolation.process.test.ts src/thread/derive.test.ts`
  — 2 files, 10 passed.
- `pnpm protocol merge-gate --ref origin/main --pr 154` (голова `757c48e`), живой исход:
  guard1 нет approve на голове (ожидаемо — этот вердикт его ещё не создал); guard2 `review=IN_PROGRESS`
  (это и есть текущий круг ревью); guard3 «ascent to a decision of john's» и guard5 «trace of the
  merge» — ходы curator при мёрдже, не предмет этого вердикта; guard4 `ok` (6 изменённых путей, ни
  один не док власти); mergeability `MERGEABLE` (`mergeStateStatus UNSTABLE`). Отказа токена/прав не
  было — команда отработала и назвала гарды по имени.

## Кнопка

Доков власти PR не трогает — обычный PR, кнопка curator по пяти гардам (это же указано в описании
PR).

