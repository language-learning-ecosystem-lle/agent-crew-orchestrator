---
from: curator
worker: claude-code
session: 327191a1-bb42-4a16-ac34-05bb6a3c1892
raised: 2026-09-07T20:22:01Z
date: 2026-09-07T20:29:24Z
expects: none
waiting-on: —
---

# Четвёртый и пятый отчёты на стоячий адрес сошлись; попутно ПОЧИНКА треда `168` подтверждена в поле — на слитом коде строка дренажа назвала живую сессию ОДИН раз. Ход снова не передаётся

Стоячий адрес, объявленный письмом `curator` со штампом 2026-09-07T19:04:37Z, отработал третий и
четвёртый раз подряд: два отчёта `github` о самоперезапуске пришли СЮДА и принесли ход собой, вместо
того чтобы открывать 170-й тред и звонить john седьмой раз. Ссылки в этом письме — автором, штампом
и фактом, а не ординалом (причина — моя же приписка к письму 20:02:06Z):

- **отчёт-1 этого хода** — письмо `github`, несущее `b507d3d535e5` → `f58a9afbe25e`, дрейн 174 с,
  пошёл 2026-09-07T20:16:24Z;
- **отчёт-2 этого хода** — письмо `github`, несущее `f58a9afbe25e` → `f06a4f9c7d19`, дрейн 173 с,
  пошёл 2026-09-07T20:21:35Z.

## 1. Отчёт-2 сошёлся весь — шесть чисел дословно (замеры 2026-09-07T20:22–20:26Z)

| заявлено в отчёте-2 | чем сверено | сошлось |
| --- | --- | --- |
| контур `/home/lle/projects/agent-crew-orchestrator` | `.orchestrator/daemon-code.json` → `"checkout"` дословно | да |
| было `f58a9afbe25e` | `.orchestrator/self-restart.json` → `"from":"f58a9afbe25e790e4b4466076105b140d394bc43"` | да |
| стало `f06a4f9c7d19` | там же `"target"`; `daemon-code.json` → `"sha":"f06a4f9c7d197e083b0a7785a3227646e4e810ae"`; главный чекаут: `HEAD = f06a4f9c…`, ветка `main`, `status --porcelain` ПУСТ | да |
| отставал 1 коммит | `"behind":1`; строка журнала «the LOADED CODE is not the ref: this process runs f58a9afb …, origin/main on disk is f06a4f9c — 1 commit(s) behind» | да |
| ждал сессии 2 мин (173 с) | `"drainSince":"2026-09-07T20:18:42Z"` → `"at":"2026-09-07T20:21:35Z"` = ровно 173 с | да |
| пошёл `2026-09-07T20:21:35Z` | `"at"` дословно; новая эпоха журнала `=== daemon epoch · 2026-09-07T20:21:47.919Z · pid 3046823 · foreground ===`, `daemon-code.json` → `"startedAt":"2026-09-07T20:21:54Z","pid":3046823`, `.orchestrator/daemon.pid` = `3046823`, `ps` — жив, строка `orchestrator up --foreground --ref origin/main --instance hetzner` | да |

Два контроля сверх таблицы:

- **дрейф закрыт, а не «перезапустился»:** строк `^agent-protocol: daemon — the LOADED CODE is not
  the ref` ПОСЛЕ последней эпохи (строка 4665 `daemon.log`) — **ноль** при 28 таких строках во всём
  файле, то есть ноль не от шаблона;
- **новый код исполняется, и доказательство — сам этот прогон:** `journal.jsonl`, строка 5337 —
  `{"kind":"launch","ts":"2026-09-07T20:22:01Z","role":"curator","thread":"167-daemon-self-restart",
  "world":{"base":"f06a4f9c7d197e083b0a7785a3227646e4e810ae"}}`. Это **восьмой** самоперезапуск в
  счёте ЭТОЙ серии писем и пятый, проверенный поштучно; сохранившиеся журналы знают больше — 14
  доведённых до конца (п.6), то есть серия писем считает не всё, что было.

## 2. Отчёт-1 сошёлся на трёх числах записью и на одном — ВЫВОДОМ; называю разницу

`self-restart.json` держит ОДИН эпизод и был перезаписан следующим, поэтому прямой записи по
отчёту-1 не осталось. Что сверено записью журнала:

- было/стало/отставание — «this process runs b507d3d5 …, origin/main on disk is f58a9afb — 1
  commit(s) behind» и завершение `daemon.log:4272` «SELF-RESTART: the tree is on f58a9afb — leaving
  with code 75»; `git pull --ff-only — ok` перечисляет `self-restart.ts` и `self-restart.test.ts`;
- пошёл `2026-09-07T20:16:24Z` — эпоха-преемник `=== daemon epoch · 2026-09-07T20:16:36.282Z · pid
  3041797 ===`, то есть 12 с спустя, ровно как во втором эпизоде (20:21:35Z → 20:21:47.9Z).

**Чего записью не сверено: дрейн 174 с.** Журнал печатает начало ДРЕЙФА
(`since 2026-09-07T23:13:06+03:00` = 20:13:06Z), а не начало дрейна. Зазор между ними замерен на
втором эпизоде и равен 25 с (`23:18:17+03:00` = 20:18:17Z → `drainSince` 20:18:42Z); 20:13:06 + 24 с
= 20:13:30, и 20:13:30 → 20:16:24 = 174 с. Сходится — но это **вывод по соседнему эпизоду, а не
запись**, и в приёмке он стоит как вывод. Дешёвая починка чтения, если она когда-нибудь понадобится:
печатать `drainSince` в самой строке дренажа. В работу не ставлю — цена дефекта тут нулевая, читатель
получает то же число из письма.

## 3. Починка треда `168` подтверждена В ПОЛЕ — даром, на живом журнале, до/после в одном файле

`f58a9afbe25e` — это squash PR **#334** («fix(self-restart): строка дренажа называет живую сессию
один раз (тред 168)»). Значит, эпизоды этого хода лежат по разные стороны от починки, и сравнение
стоило одного грепа:

| эпизод | код процесса | строк дренажа | список живых сессий (дословно) |
| --- | --- | --- | --- |
| отчёт-1 | `b507d3d5` — **ДО** #334 | 5 | `(curator, curator/168-drain-line-names-a-session-twice)` |
| отчёт-2 | `f58a9afb` — **ПОСЛЕ** #334 | 5 | `(curator/169-tag-fitness-is-measured-by-tree-not-ancestry)` |

Сравнение однородное: по обе стороны ровно ОДИН живой сеанс роли `curator`, привязанный к треду, и
все пять строк каждого эпизода несут один и тот же список. Изменился только код — и голое имя из
списка исчезло. Дефект, замеренный этим тредом (88 строк, 11 списков, во всех число голых имён равно
числу пар), в поле больше не воспроизводится.

**Границы, чтобы это не читалось шире, чем замерено:** по одному эпизоду с каждой стороны, и случай
«две роли сразу», который был в первоисточнике, после починки просто не встретился — он **не
воспроизведён, а не опровергнут**.

**Куда этот факт принадлежит:** приёмка #334 — предмет треда `168`, и след туда пишет тот, кто ведёт
`168`; этот прогон связан тредом `167`, и в чужой ленте я не пишу. Замер выше повторяется одной
командой из `daemon.log` и в ленту `168` едет за секунды. На адресах ролей треда `168` сейчас нет:
`cli mail --role curator` (20:25:59Z) и `cli mail --role dev-core` (20:26Z, список ПУСТ) его не
называют — значит, ход по нему не стоит ни у кого. Закрыт он или просто остался без хода — отсюда не
видно, и я этого не мерил.

## 4. john не зову — седьмой раз, и парки перемерены, а не вспомнены

`.orchestrator/notify.state` на 20:24Z: по john стоят **три парка ровно об этом механизме** —
`162` (16:21:58Z), `164` (17:35:49Z), `165` (18:15:22Z), — плюс `029`, `143`, `155`, `157`, `159`.
Метки не сдвинулись с прошлого хода: ни один не отвечен. Механизм при этом звонит сам без моей руки —
в файле лежат `remind john 162-daemon-self-restart 2026-09-07T19:22:18Z`, `… 157 … 19:30:27Z`,
`… 159 … 19:44:23Z`. Седьмой штатный срабат самоперезапуска к трём паркам ничего не добавляет, а
новый звонок утопил бы в себе настоящий вопрос `164`. Развилки, денег, доков власти, необратимого —
ни одного пункта. Правило последней строки применено: сомнения нет.

## 5. Хвост открытых PR ПУСТ — и это замер, а не «не нашёл»

`gh pr list --state open --json number,title,headRefOid,mergeable,labels` в **20:24:15Z** вернул
`[]`. Оба PR, стоявших в прошлом письме, уехали в `main` сами и стали кодом, который здесь
исполняется: **#334 = `f58a9afb`**, **#335 = `f06a4f9c`** (номера названы заголовками squash-коммитов
линии `main`). Мержить нечего, метку вешать не на что; метка на #335, которую прошлый ход сознательно
не повесил из-за живой сессии `dev-core×169`, от моей руки в итоге не потребовалась — PR закрыл свой
прогон. Своей рукой в этом такте не сделано ни merge, ни метки, ни закрытия — следа гарда 5 здесь
нет, потому что нет и кнопки.

## 6. Цена очереди — цифра пополнилась, в работу по-прежнему не ставится

В сохранившихся журналах **14 доведённых до конца самоперезапусков** (11 в `daemon.log.1`, 3 в
`daemon.log`; счёт пофайловый — см. п.7), из них три — в окне этого треда. Два эпизода этого хода
отстоят друг от друга на **5 мин 11 с** (20:16:24Z → 20:21:35Z): два мержа подряд стоили двух полных
дренажей по ~3 мин каждый. **Очередь при этом не встала намертво** — между эпизодами ящик поднял
сессию: `journal.jsonl`, `{"kind":"launch","ts":"2026-09-07T20:16:51Z","role":"curator",
"thread":"169-…","world":{"base":"f58a9afb…"}}`. Вопрос «должен ли ящик поднимать сессии во время
дренажа» остаётся **нормой и словом john** (разбор — моё письмо 20:02:06Z, п.5), новых оснований к
нему этот такт не даёт; здесь только цифра.

## 7. Своя рука мешала дважды, оба раза в чтении журнала — записываю, потому что ловится только контролем

- **живой `daemon.log` печатает ЭХО моих же команд и их вывода**, поэтому неякорный
  `grep 'daemon epoch'` выдал за «последние» старые эпохи из чужого эха. Правда берётся якорем
  `^=== daemon epoch` и ПОЛНЫМ перечислением, а не хвостом: в `daemon.log` их ровно четыре —
  19:23:50.850Z/2664787, 19:55:05.195Z/2863757, 20:16:36.282Z/3041797, 20:21:47.919Z/3046823;
- **многофайловый `grep -h` по паре журналов отдал счётчики не в том порядке, в каком файлы названы
  в строке**: «3 и 11» на деле 11 (`daemon.log.1`) и 3 (`daemon.log`), что видно только пофайловым
  прогоном с `-n`. Числа в п.6 — пофайловые.

Контроли, которыми это ловится, стоят рядом с каждым нулём: шаблон на невозможный sha дал 0, тот же
шаблон на реальный — 14 совпадений; ноль строк дрейфа после последней эпохи стоит рядом с 28 такими
строками во всём файле.

## Состояние на конец хода

Некоммиченного за мной нет: в рабочем дереве роли не правился ни один файл, работа хода — замеры и
это письмо (тело лежало в `/tmp/curator-167-Pka4`, вне обоих чекаутов). Ремонта в эпизодах не было и
не требовалось: оба самоперезапуска отработали от начала до конца сами.

**Ход не передаётся никому — намеренно, четвёртый раз подряд и по той же измеренной причине.** Этот
приёмник остаётся **открытым, незапаркованным и без `--waiting-on`**: `--ensure-thread` берёт
«whichever of its receivers is open and unparked», поэтому следующий отчёт `github` о
самоперезапуске придёт снова сюда и принесёт ход собой — это уже случилось четыре раза. Голым
кандидатом тред при этом не становится: без `waiting-on` пара не поднимается вовсе.

Что следующая сессия может взять отсюда, не перемеряя: хвост PR пуст (20:24:15Z), парков по john
восемь и ни один не сдвинулся (20:24Z), а приёмка #334 в поле — таблица п.3, если тред `168` окажется
открытым.

