---
from: curator
worker: claude-code
session: 5a3099fb-806d-4a90-9280-0196afb68e80
date: 2026-08-18T09:57:24Z
expects: ack
waiting-on: curator
parked-on: john
---

**Требуется внимание john, и звонка об этом по-прежнему не будет (курьер нем — тред `002-courier-mute`).** Поправка к предыдущему сообщению этого треда (`2026-08-18T09-55-19Z-curator.md`): там сказано, что постановок по двум прежним кандидатам в этом такте не написано и они уходят в следующий такт. Это перестало быть верным в том же такте — окна хватило, и обе выданы. Сообщение append-only и не правится; поправка стоит здесь.

## Выдано всего три постановки, ход по всем трём у dev-core

| тред | что | очередь |
| --- | --- | --- |
| `002-courier-mute` | `transport-telegram` не объявлен зависимостью — уведомления не уходят никому | первая |
| `003-daemon-self-restart` | демон не возвращается из self-restart, выходит кодом 0 и молчит | вторая |
| `004-init-github-host` | `init github --host` пишет нерезолвимый `HostName` | третья |

Очерёдность объявлена внутри самих постановок, отдельной директивой приоритета не выставлялась: `002` и так поднимается первым по возрасту ожидания и номеру треда, а `--priority` — флаг `new-message`, не `new-thread` (дверь отказала по имени, это верное поведение, а не помеха).

Каждая постановка несёт раздел «Проверяемость» — какие классы проверок покрывают функционал и что сознательно не покрывается, с причиной. В `003` первым шагом стоит **воспроизведение до правки**: причинная связь там остаётся выводом (сигнал не наблюдался), и если гипотеза не подтвердится — ход возвращается curator, а не ищется другая правка молча.

## Что осталось john — и это ровно один ack, не развилка

Ничего из выданного не требует владельческого решения: все три задачи — код пакетов, зона `dev-core`. Доков власти, воркфлоу, денег и внешних сервисов ни одна не трогает, и в каждой явно сказано исполнителю: упрётся в `agent-protocol.json` или в `.github/workflows/**` — остановиться и спросить, а не додумать.

Ack нужен на факт, а не на выбор: **уведомления из этого репозитория не приходят никому, включая тебя, и до починки `002` за почтой надо смотреть глазами.** Первый живой цикл полной приёмки (сессия поднялась → работа сделана → о ходе сообщили) здесь ещё ни разу не замкнулся — он упирается ровно в это.

## Ход

Тред остаётся припаркованным на john, ход у curator. Незакоммиченного за этим тактом не осталось: почтовый чекаут чист, рабочее дерево роли не тронуто, все четыре записи ушли в `origin/comms` своими командами.

