# Почта репозитория рождена — конфиг, роли, инстанс hetzner (день 2 Э-3′)

participants: curator · status: closed

## msg-001 · from: curator · 2026-08-18 · expects: none

Первое сообщение этой почты — журнальное. Ветка `comms` рождена 2026-08-18 в такте curator по LLE-треду `082-hetzner-migration` (день 2, Э-3′, слово john «давай дальше»).

## Что уже есть

- ветка `comms` с каталогом `agent-comms/` — этот тред первый;
- PR #3 в `main`: корневой `agent-protocol.json` (протокол v17, роли `john` / `curator` / `dev-core`, инстанс `hetzner`), карточки ролей `docs/roles/{curator,dev-core}.md`, абзац в `README.md`. **Кнопка john** — корневой конфиг есть док власти этого репозитория, а двери `merge-gate` здесь ещё нет вовсе.

## Чего ещё нет — и это следующий такт, а не забывчивость

Клона на ящике `hetzner`, машинного конфига `instances/hetzner.json`, резидентного юнита и приёмочной сессии полного цикла. Всё это упирается в одну вставку рук john: **deploy key ящика hetzner в этот репозиторий** (публичная половина доставлена в LLE-тред 082, сообщение `2026-08-18T08-32-…-curator.md`, §4.2). Ключ ящика принадлежит ровно одному репозиторию — до этого он доставал только до LLE.

## Два кандидата в первую содержательную работу — оба измерены, оба здешние

**1. Дефект self-restart демона под systemd-юнитом.** Измерено на `lle-hetzner` 17–18.08: тик увидел расхождение загруженного SHA с `--ref` на диске и запустил self-restart; дочерний процесс напечатал «stopping pid … gracefully» и «the stop flag is set» — и оборвался. Ни фазы ожидания, ни `--pull`, ни подъёма. Демон вышел **кодом 0**, поэтому `Restart=on-failure` его не поднял по построению, а оставшийся стоп-флаг убил бы любой поднятый вручную. Ящик простоял без демона ~11.5 часов.

Причинная связь названа как вывод, а не как факт: `KillMode=control-group` гасит остаток cgroup при уходе MainPID, то есть ребёнок убит ровно тем уходом, которого он ждал. Настройки юнита измерены (`systemctl show`: `Restart=on-failure`, `KillMode=control-group`, `Type=simple`), обрыв лога на строке ухода измерен; сам сигнал не наблюдался. Дешёвое подтверждение — воспроизвести с `KillMode=process`.

Довод срочности, а не символизма: если верен кандидат «ящик с живым демоном двигает свой `origin` сам» (наблюдение — `origin/main` на диске уехал без единой руки между стартом процесса и тиком), то **каждый merge в `main` убивает демона** примерно через тик. Скамейка переживает это молча, рабочий ящик — нет.

**2. `init github --host <alias>` пишет нерезолвимый `HostName`.** Команда кладёт в `~/.ssh/config` блок `Host <alias>` / `HostName <alias>` вместо хоста GitHub. На документированном применении (`--host github.com`) незаметно — alias совпадает с хостом; ломается ровно там, где флаг единственно и нужен: второй identity на одном ящике. Живой отказ: `Could not resolve hostname github-crew`, выход 2. На ящике поправлено рукой, в коде — нет.

## Ход

Постановок ни по одному кандидату ещё нет — они пишутся после того, как конфиг и роли приедут в `main`: до тех пор поднимать некого. Ход держит curator.

## msg-002 · from: curator · 2026-08-18 · expects: ack

**Требуется внимание john (и звонка об этом не будет — курьер этого репозитория нем):** уведомления отсюда не уходят никому. Пока это не починено, почту этого репозитория john видит только тем, что зайдёт и прочитает её сам; парковка вопроса к владельцу здесь по построению не доносит вопрос.

## Что изменилось с msg-001 — предпосылка постановок закрыта

- **PR #3 смёржен**: `mergedAt 2026-08-18T09:01:23Z`, `base.ref = main`, merge-коммит `8f78852`. Цель проверена командой, а не по памяти.
- **Ящик введён в строй, и это измерено, а не выведено**: deploy key работает (`origin git@github-crew:…` тянет и толкает), машинный конфиг `~/.config/agent-protocol/instances/hetzner.json` на месте рядом с `lle-hetzner.json`, резидентный юнит `agent-protocol@hetzner.service` — `active running`, старт `2026-08-18 11:50:22 CEST`, `NRestarts=0`. Эта сессия поднята им же. Список «чего ещё нет» из msg-001 в части клона, конфига и юнита закрыт фактом.
- Приёмочной сессии полного цикла всё ещё нет: цикл не замыкается ровно на курьере (ниже).

## Третий кандидат, измеренный сегодня, и он идёт первым

Живой журнал демона, 2026-08-18 11:51:58 CEST:

```
the courier is NOT delivering: the transport 'transport-telegram' was not loaded:
Cannot find package 'transport-telegram' … must be a dependency of the repository;
the box keeps working, nobody is being told that the turn has passed
```

Проверено рядом: `transport-telegram` назван в `agent-protocol.json` (`notifications.transport.module`), пакет в воркспейсе есть, но зависимостью его не объявляет ни корневой `package.json`, ни `packages/agent-protocol/package.json`.

Почему он вперёд двух прежних: молчащий курьер выключает наблюдаемость очереди целиком — «твой ход» не приходит, `stalled` не приходит, и **единственный путь эскалации curator к john не работает**. Роль, которой велено «сомневаешься — спроси john», спросить не может. Это не мнение о важности, а следствие из измеренного отказа.

**Постановка выдана: тред `002-courier-mute`, ход у dev-core.** Раздел «Проверяемость» в ней есть: юнит на загрузку транспорта по имени (успех и отказ ПО ИМЕНИ), тест на стык «конфиг → модуль → событие» без сети, живая приёмка человеком (факт доставки на телефон ни одним прогоном здесь не доказывается — у CI нет секретов), и явно НЕ покрывается поведение при недоступном Telegram API.

## Два прежних кандидата — ещё не выданы, и это остаток такта, а не отказ

- **Self-restart демона под юнитом.** Здесь он применим один в один: `systemctl --user show agent-protocol@hetzner.service` даёт ровно ту же тройку — `Type=simple`, `Restart=on-failure`, `KillMode=control-group`. Если верен кандидат «ящик с живым демоном двигает свой origin сам», каждый merge в `main` этого репозитория убивает его демона примерно через тик. Причинная связь остаётся выводом, не диагнозом.
- **`init github --host <alias>` пишет нерезолвимый `HostName`.** На ящике поправлено рукой, в коде нет.

Постановок по ним в этом такте не написано — окно роли кончилось. Следующий такт curator: выдать обе.

## Отчёт о собственной ошибке в этом такте

`new-thread` с `--repo`, разрешившимся в главный чекаут вместо почтового, упал на `git add` уже ПОСЛЕ записи файлов и оставил в почтовом чекауте незакоммиченный каталог `002-courier-mute/`. Грязный почтовый чекаут — отказ доставки для всех, поэтому я удалил этот каталог (untracked, ни разу не коммиченный) и повторил команду начисто; тред создан вторым, успешным заходом. Коммиченного не тронуто. Отдельно: `--priority` этой командой не понимается (отказ по имени — дверь сработала верно), поэтому приоритет `002` не выставлен; при нужде он ставится сообщением.

## Ход

Тред паркуется на john с `--expects ack`, ход остаётся у curator. Ack нужен ровно на первую строку: **уведомления отсюда не приходят, и до починки `002` за почтой этого репозитория надо смотреть глазами.**

## msg-003 · from: curator · 2026-08-18 · expects: ack

**Требуется внимание john, и звонка об этом по-прежнему не будет (курьер нем — тред `002-courier-mute`).** Поправка к предыдущему сообщению этого треда (`2026-08-18T09-55-19Z-curator.md`): там сказано, что постановок по двум прежним кандидатам в этом такте не написано и они уходят в следующий такт. Это перестало быть верным в том же такте — окна хватило, и обе выданы. Сообщение append-only и не правится; поправка стоит здесь.

## Выдано всего три постановки, ход по всем трём у dev-core

| тред | что | очередь |
| --- | --- | --- |
| `002-courier-mute` | `transport-telegram` не объявлен зависимостью — уведомления не уходят никому | первая |
| `003-daemon-self-restart` | демон не возвращается из self-restart, выходит кодом 0 и молчит | вторая |
| `004-init-github-host` | `init github --host` пишет нерезолвимый `HostName` | третья |

Очерёдность объявлена внутри самих постановок, отдельной директивой приоритета не выставлялась: `002` и так поднимается первым по возрасту ожидания и номеру треда, а `--priority` — флаг `new-message`, не `new-thread` (дверь отказала по имени, это верное поведение, а не помеха).

Каждая постановка несёт раздел «Проверяемость» — какие классы проверок покрывают функционал и что сознательно не покрывается, с причиной. В `003` первым шагом стоит **воспроизведение до правки**: причинная связь там остаётся выводом (сигнал не наблюдался), и если гипотеза не подтвердится — ход возвращается curator, а не ищется другая правка молча.

## Что осталось john — и это ровно один ack, не развилка

Ничего из выданного не требует владельческого решения: все три задачи — код пакетов, зона `dev-core`. Доков власти, воркфлоу, денег и внешних сервисов ни одна не трогает, и в каждой явно сказано исполнителю: упрётся в `agent-protocol.json` или в `.github/workflows/**` — остановиться и спросить, а не додумать.

Ack нужен на факт, а не на выбор: **уведомления из этого репозитория не приходят никому, включая тебя, и до починки `002` за почтой надо смотреть глазами.** Первый живой цикл полной приёмки (сессия поднялась → работа сделана → о ходе сообщили) здесь ещё ни разу не замкнулся — он упирается ровно в это.

## Ход

Тред остаётся припаркованным на john, ход у curator. Незакоммиченного за этим тактом не осталось: почтовый чекаут чист, рабочее дерево роли не тронуто, все четыре записи ушли в `origin/comms` своими командами.

## msg-004 · from: curator · 2026-08-18 · expects: none

доставка ack john (сессия чата LLE, ~11:52Z; пишет чатный curator LLE — доступ токеном владельца, письмо-доставка, в работу контура не вмешиваюсь):

**Ack на факт §«Что осталось john» получен — и факт закрылся жизнью в момент доставки**: уведомление, принёсшее этот вопрос john'у, само пришло курьером ЭТОГО репозитория — то есть немота (тред `002`) закончилась раньше, чем ack доехал. Хронология для ленты: #7 смёржен john 2026-08-17; `pnpm install` в постоянном чекауте — тактом curator LLE-контура (`082/10-43-18Z`); первый живой звонок — сегодня, эта самая уведа.

Сопутствующие факты от john тем же словом: **#8 (тред `003`) смёржен его кнопкой ~11:50Z**, после merge john исполнил на ящике `git pull --ff-only && pnpm install` в `~/projects/agent-crew-orchestrator` и `systemctl --user restart agent-protocol@hetzner.service` — демон контура на свежем коде с supervised-формой self-restart; это был последний ручной рестарт такого рода по построению.

Парковка треда снимается этим ack; дальнейшее — тактами контура.

## msg-005 · from: curator · 2026-08-18 · expects: none

**Сброс исчерпания — эта доставка обнуляет счёт (3/3).** Диагноз по журналу демона: провалы — **API 529 Overloaded** (внешнее, временное; квота цела). Ход треда прежний, содержание не меняю — этой ноте достаточно снять exhausted, дальше штатный такт.

waiting-on → curator.

## msg-006 · from: curator · 2026-08-19 · expects: answer

доставка из треда `013-exhausted-visibility` (решение john ~08:40Z, msg `2026-08-19T08-42-27Z-curator`) — эта пара стоит замороженной, и вот команда, которой она поднимается. Предмет треда 001 я этим ходом НЕ трогаю: прогон привязан к 013.

## Факт: пара `curator×001-mail-born` заморожена потолком

Снято кадром `orchestrator status` (мой прогон, 2026-08-19T08:48Z, ничего не тронуто):

```
curator · 001-mail-born · released · attempt 3/3 · deadline 2026-08-18T17:37:12Z
  · (exited-without-handoff)  ⚠ EXHAUSTED (substantive — the circuit will not raise it,
  a hand does (--max-attempts above the ceiling))
```

Три попытки 2026-08-18 умерли на API 529 (перегрузка вендора). Класс — `substantive`, потому что журналы старше поля `external` (консервативное чтение, не ошибка классификатора): авто-оттаивания у этого класса нет.

**Письмо в этот тред пару НЕ размораживает** — установленный факт кода треда 013: счётчик обнуляет `cur.attempt = 0` в `lease.ts` только под `isDelivery || isSelfTurnDelivery`, то есть на СОБЫТИИ ЖУРНАЛА ЭТОЙ ПАРЫ, а все три формы такого события пишет её собственный прогон. Мои письма 21:35Z 2026-08-18 счёт не сняли. `planLaunch` отказывает исчерпанной паре (`reason: exhausted`) ДО начала прогона — изнутри контура выхода нет.

## Команда для руки на hetzner

С `/home/lle/projects/agent-crew-orchestrator` (`pnpm protocol` = `tsx packages/agent-protocol/src/cli.ts`):

```bash
pnpm protocol orchestrator run --ref origin/main \
  --role curator --thread 001-mail-born \
  --max-attempts 4 -d --write
```

- `--max-attempts 4` пропускает РОВНО ОДИН прогон (пара стоит на 3/3); потолок 3 в конфиге не трогается;
- `--write` делает запуск настоящим; `-d` кладёт супервизор в фон;
- **цена: это запуск сессии** — деньги и квота;
- handoff обнулит счётчик сам; молчаливая смерть даст 4/4 и потребует `--max-attempts 5`;
- роль curator должна быть СВОБОДНА: кадр `status`, строки `parallelism` / `free:`.

waiting-on → curator (как и стоял: рука снимает потолок, ход в треде не двигается).

## msg-007 · from: curator · 2026-08-19 · expects: none

**Тред закрывается** (чатный curator, право thread-status; решение в связке с 013: john попросил решение без ожидания свободной роли и без лишних сессий).

Предмет треда — рождение почты и первый живой звонок курьера — закрыт жизнью ещё 2026-08-18 (msg `11-53-31Z`: ack john доставлен, немота 002 закончилась до его доставки, «дальнейшее — тактами контура»). Открытых предметов в ленте нет; exhausted-заморозка пары (3/3 на 529) держала в очереди тред, которому нечего было делать, — закрытие снимает пару из очереди без прогона, экономя сессию.

Перенос работ: НЕ ТРЕБУЕТСЯ — лента проверена, невыполненных обязательств нет.

status → closed (в _meta тем же тактом).
