---
from: curator
worker: claude-code
session: 13593080-d201-4a26-8ef6-7f5e42e56cb4
raised: 2026-09-03T15:26:08Z
date: 2026-09-03T15:31:34Z
expects: ack
waiting-on: curator
parked-on: john
---

# Требуется решение john: дверь `pr open` стоит и не спрашивается ни у кого — записывать ли её ролям, и одной нормой в `PROTOCOL.md` или правкой четырёх карточек? #216 смержен

**Вопрос первой строкой, потому что он — единственный остаток этого треда.** После merge #216 в
пакете есть команда, которой роль открывает PR и которая ОТКАЗЫВАЕТ без `thread:`/`role:` первыми
строками. Но ни одна карточка и ни один док не говорит роли ею пользоваться — там по-прежнему
`gh pr create --body-file`, а дверь, как назвал её сам исполнитель, ловит честную ошибку и обходится
тем, что ею не пользуются. Это §4 постановки (`2026-09-02T18-04-02Z-curator.md`), и он с самого
начала был назван кнопкой john: запись — это док власти.

**Что решать (два вопроса, второй — форма):**

1. **записывать ли вообще.** Не записывать — законный исход: дверь остаётся страховкой на merge
   (гард 3 `merge-gate` теперь отказывает без `role:` — это уже в `main`), а раннего отказа при
   открытии не получает никто. Тогда §4 закрывается отказом, и тред закрывается вместе с ним;
2. **если записывать — то где.** Моя рекомендация: **одной нормой в `PROTOCOL.md`** («роль открывает
   PR командой `pnpm protocol pr open`, а не `gh pr create`»), а не правкой карточек `dev-core`,
   `pilot-codex`, `devops`, `curator` по отдельности. Причина ровно та, что уже стоила нам
   раздваивания в других предметах: четыре копии одной фразы расходятся на первом же уточнении, а
   норма живёт в одном месте, и карточка на неё ссылается. Обе формы — доки власти, то есть кнопка
   всё равно твоя; разница только в том, сколько мест придётся держать синхронными.

Рекомендация curator ≠ решение john. Сама я в `PROTOCOL.md` не пишу и не мёржу.

---

## 1. #216 смержен. Гарды поимённо

Кнопка нажата 2026-09-03T15:28:08Z, squash, merge-коммит
**`f74e2649503ae62f26f6bf271b3ef22ad0110a50`**.

- **Гард 1 — approve на ТЕКУЩЕЙ голове.** Вердикт — письмо `reviewer-pr` с `verdict: approve` по
  #216 в этом треде, доставленное шагами прогона
  [`33771969429`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/33771969429).
  Голова — **`abf9fcb2cbb16b8ab8d4a6e4ee727adb334b2e7f`**. Якорь сошёлся с прогоном, а не с
  review-объектом: дверь напечатала «approved on `abf9fcb` by `github-actions` — inside the round
  `33771969429` of `Claude PR Review` on this head (2026-09-03T15:19:48Z…15:26:03Z)». Сироты нет,
  это третий круг того же PR (`bf704ce5` → `3d369893` → `abf9fcb2`), и первые два вердикта я не
  зачитываю.
- **Гард 2 — зелёные чеки на ТОЙ ЖЕ голове.** `review=SUCCESS`, `checks=SUCCESS` на `abf9fcb2`;
  зачтённый `checks` — прогон
  [`33756642364`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/33756642364).
  **Нота двери о базе не сглажена, а погашена замером:** база ушла на `d5dc4e7d` (14:47:15Z) после
  старта зачтённого `checks` (12:41:26Z), поэтому я своей рукой в 15:27:5xZ прогнала
  `git merge-tree --write-tree origin/main abf9fcb2` — дерево склейки
  **`9cf13d6fcdddec6b9742c29672779c08e616a71f`**, ровно то, на котором dev-core уже прогнал
  `pnpm typecheck` (чисто) и `vitest run src/merge src/usage.test.ts src/orchestrator` (**2082
  passed**, красных ноль) и назвал границу замера сам. То есть зелёное зачтено о дереве, которое и
  дала кнопка, а не о соседнем.
- **Гард 3 — восхождение к решению john.** Решение лежит в ленте этого треда дословно:
  `2026-09-02T17-06-28Z-curator.md` §1 — вариант **(B)** плюс уточнение по моменту отказа
  **«ТРЕТИЙ С ПЕРВЫМ»** (отказ при открытии PR + проверка на merge как страховка; второй момент, шаг
  в прогоне проверок, john не выбрал и назвал причину). Постановка
  `2026-09-02T18-04-02Z-curator.md` восходит к нему и называет merge действием curator (§6
  «Маршрут»). Класс «полевой измеренный дефект» здесь не объявлялся и не нужен — есть само решение
  john. Строка `thread: 052-pr-template` в описании PR на месте.
- **Гард 4 — доков власти в диффе нет, и это мой замер, а не пересказ.** Перечислила пути своей
  рукой (`gh pr view 216 --json files`): 11 — `docs/protocol-reference.md`,
  `packages/agent-protocol/README.md`, `src/cli.ts`, `src/usage.ts`, `src/usage.test.ts`,
  `src/merge/{gate.ts,gate.test.ts,pr-open.ts,pr-open.test.ts,pr-open.process.test.ts,pr-template.test.ts}`.
  Ни `agent-protocol.json`, ни карточек, ни `PROTOCOL.md`, ни `REVIEWER.md`, ни
  `.github/workflows/**`. `docs/protocol-reference.md` доком власти не является — это справочник
  поведения кода. Дверь напечатала список из восьми доков власти сама и сказала «11 changed path(s),
  none of them a document of power».
- **Гард 5 — этот след.** Голова, вердикт, прогоны и класс названы выше.

**Кнопка нажата `--match-head-commit`**, sha взят из вывода самой `merge-gate`, а не сверен глазом:
`gh pr merge 216 --squash --match-head-commit abf9fcb2cbb16b8ab8d4a6e4ee727adb334b2e7f`.
Контрольное чтение цели после merge (ответ инструмента ≠ факт): `state=MERGED`, `base.ref=main`,
`mergeCommit=f74e2649503ae62f26f6bf271b3ef22ad0110a50`, `mergedAt=2026-09-03T15:28:08Z`.

## 2. Своё заявление о «доке власти» снимаю — оно было неверным

В `2026-09-03T12-36-06Z-curator.md` я написала «дифф трогает док власти, кнопка john». **Это было
неправдой, и я снимаю это без оговорок.** Дифф не трогает ни одного дока власти — так печатала дверь
на всех трёх головах подряд, и так же вышло у меня сейчас своей рукой.

Откуда взялась ошибка, называю точно, потому что класс полезнее самого случая: строка стояла в
письме, посвящённом ПЯТОЙ за сутки перебазировке, и класс PR я назвала по памяти, не спросив дверь.
Это ровно то, чем моя же карточка судит исполнителей: **собственный диагноз — тоже заявление,
подлежащее проверке**, и «дифф трогает доки власти» — заявление о фактах, а не суждение.

Отдельно: **dev-core (`2026-09-03T12-43-48Z-dev-core.md` §4 и следующее его письмо, §4) и ревьюер
(вердикт, критерий 5) назвали разногласие вслух, а не закрыли его молчанием и не пошли за моей
формулировкой** — при том что у обоих был дешёвый путь «curator сказала кнопка john, значит стоим».
Это правильное поведение, и оно сэкономило кнопку john и такт. Довода за пределами диффа у меня не
было — только факт коллизии, а он к гарду 4 не относится вовсе.

## 3. Хвост открытых PR прогнан — и два кандидата этим прогоном НЕ домержены

`gh pr list --state open` + `merge-gate` по каждому (15:29Z), чтобы не оставить молчания:

- **#240** (тред `090`, голова `3ea5707`) — гарды 1, 2, 4 `ok`, `MERGEABLE/CLEAN`, «nothing in the
  facts forbids this merge»;
- **#114** (тред `048`, голова `1bc125a`) — то же: 1, 2, 4 `ok`, `MERGEABLE/CLEAN`;
- **#237** (тред `078`) — `STOP` гарда 4: правит `PROTOCOL.md`. Кнопка john;
- **#233** (тред `064`) — `STOP` гарда 4 (`PROTOCOL.md` + `.github/workflows/claude-review.yml`) и
  `STOP` гарда 1 (вердикта на `8be36b9` нет). Кнопка john, и по классу `claude-review.yml` круга там
  не будет по построению — гард 1 закрывается человеческим чтением;
- **#202** (тред `074`) — `STOP` гарда 4 (`.github/workflows/checks.yml`) плюс
  `mergeable=CONFLICTING`: нужен ребейз и новый круг, потом кнопка john.

**#240 и #114 я не домержила намеренно, и это не забывчивость.** Прогон связан ровно одним тредом —
`052`, — а гард 5 требует, чтобы след merge лёг в ленту ТОГО треда, которому принадлежит PR
(`090` и `048`). Писать туда этим прогоном нечем, а merge без следа в правильной ленте — нарушение
нормы, даже когда merge правильный. Оба готовы по фактам двери и ждут такта своего треда: их надо
поднять первыми, там осталась одна кнопка на каждый.

## 4. Состояние

- незакоммиченного нет: рабочее дерево `curator` чисто, detached на `d5dc4e7d` (в почту и её ветку
  своей рукой не лазила — только три команды `cli`);
- ветку `feat/052-pr-template` GitHub закрыл вместе с merge; метку с #216 не снимала — PR закрыт;
- **§1–§3 постановки закрыты merge'ом #216. Остаток треда — ровно §4, и он в первой строке этого
  письма.**

## 5. Ход

**Парк на john, `--expects ack`** — звонок несёт первую строку, то есть сам вопрос. Ход остаётся у
меня (`waiting-on → curator`), потому что вопрос несу я: john в это поле не принимается.

Если ответ «записывать» — предмет становится правкой `PROTOCOL.md`, то есть **дока власти: дифф
пишет рука (моя — путь у curator в зонах открыт — либо `dev-core` постановкой), а кнопку жмёт
john.** Следующий такт curator в этом случае пишет постановку и открывает PR; merge — не мой ни в
одной из двух форм ответа.

Если ответ «не записывать» — следующий такт закрывает тред `052` приёмкой, и остатка не остаётся.

