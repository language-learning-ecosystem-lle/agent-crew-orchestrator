---
from: curator
worker: claude-code
session: 57be9cd6-ac58-4713-ada1-6917010ad78e
raised: 2026-09-03T17:45:44Z
date: 2026-09-03T17:48:20Z
expects: none
waiting-on: curator
parked-on: run:248
---

# Метка `review` повешена на #248 — круг поднялся (прогон 33786557236). Паркуюсь на `run:248`, merge — кнопка john

Ход этого такта был ровно на одно действие, названное в `msg-033`, и оно сделано.

## 1. Метка — с проверкой головы ДО и ПОСЛЕ

Метка вешается после зелёного `checks` **на той же голове**, поэтому сначала замер, потом рука:

- **голова до метки** — `9158ba9bef11e1a8fb905d67baf3952b7dd38af6`, та же, что в `msg-032` и в письме
  бота; `state=OPEN`, `mergeable=MERGEABLE`, `mergeStateStatus=CLEAN`, меток нет;
- **`checks` на ЭТОЙ голове** — спрошено по sha, а не по номеру PR:
  прогон `33785441211`, имя `checks`, `event=pull_request`, `completed/success`, 2026-09-03T17:40:49Z.
  Других прогонов на голове на тот момент не было;
- **рука** — `gh pr edit 248 --add-label review` (17:46Z);
- **контрольное чтение после записи** (ответ инструмента ≠ факт): `headRefOid` по-прежнему
  `9158ba9b…`, `labels=[review]`, `state=OPEN`. Голова под меткой не уехала — метка судит то самое
  дерево, что зеленело.

## 2. Круг поднялся — и я у него не стою

Событие навешивания метки подняло круг: прогон
[`33786557236`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/33786557236),
имя **`Claude PR Review`**, `event=pull_request`, `in_progress`, создан 2026-09-03T17:46:41Z — то
есть через 12 секунд после метки. Это тот самый якорь, которого требует гард 1: прогон на текущей
голове с событием `pull_request`, а не просто зелёный чек с именем `review`.

**Вердикт приедет в тред сам** шагами `claude-review.yml`. Метка — конец хода, а не начало ожидания:
слот роли один, и ждущая сессия держит очередь. Паркуюсь на `run:248`.

## 3. Что я замерила, а не предположила: двери для метки в пакете нет

Прежде чем брать `gh` в руку, спросила пакет — потому что записываемая этим самым PR норма
говорит «роль открывает PR **командой пакета**», и естественный вопрос «а метку тоже?» надо было
закрыть фактом, а не памятью:

- `cli --help` знает **одну** команду про PR — `pr open`. Команды про метки нет;
- `LABEL_CALL`, которым бот позвал в `msg-033`, — не команда, а внутренний флаг шага
  `.github/workflows/ci-outcome.yml` (строки 365/384/437/453). Он рождает тот самый значок «👉»,
  и больше ничего.

Значит метка — рука по маршруту карточки, и никакого противоречия с нормой PR #248 тут нет: норма
про **открытие** PR, а не про метку. Называю это здесь, чтобы следующий такт не открывал вопрос
заново и, что важнее, чтобы никто не прочёл норму шире, чем она написана.

## 4. Хвост открытых PR — ПЕРЕМЕРЕН, и у него появился новый факт

`gh pr list --state open` (17:47Z) — восемь: #248 (этот), #247, #246, #245, #244, #243, **#240**,
**#114**. Два последних я несу третий такт подряд, поэтому прогнала `merge-gate` заново, а не
скопировала прошлый вывод — перенесённый пункт протухает молча:

- **#240** (тред `090`, голова `3ea5707`) — гарды 1, 2, 4 `ok`, `MERGEABLE/CLEAN`;
- **#114** (тред `048`, голова `1bc125a`) — гарды 1, 2, 4 `ok`, `MERGEABLE/CLEAN`.

**Новое с прошлого такта, и это и есть цена перемера:** дверь теперь печатает на ОБОИХ **ноту о
сдвиге базы** — `main` уехал на `054dca7` (2026-09-03T17:24:00Z) уже ПОСЛЕ старта зачтённых
`checks` (у #240 — 15:01Z, у #114 — 29.08 15:01Z). Прогон `pull_request` мерит голову, слитую с
базой СВОЕГО момента, и сдвиг базы его не перезапускает. Час назад этой ноты в выводе не было.
Нота — не отказ (гард 2 остаётся `ok`), и по норме сдвиг базы гасится **замером**, а не ребейзом;
но замер этот принадлежит такту треда-владельца, а не мне здесь.

**Я их снова не мержу, и причина та же, что и дважды до этого:** гард 5 требует, чтобы след merge
лёг в ленту треда, которому PR принадлежит (`090` и `048`), а этот прогон связан ровно тредом
`052` — писать туда мне нечем. Оба готовы по фактам двери и ждут собственного такта; там осталась
одна кнопка на каждый плюс ответ на ноту о базе.

## 5. Merge #248 — кнопка john, и это уже не заявление по памяти

Дифф трогает `PROTOCOL.md` и три карточки ролей — гард 4 `STOP`, роль не принимает документы,
определяющие её собственные полномочия. Класс читается из списка файлов §2 письма `msg-032`
напрямую. Проверю дверью на следующем такте, когда вердикт ляжет, — и тогда же понесу кнопку к john
одним письмом вместе с вердиктом.

## 6. Состояние

Рабочее дерево `curator` чисто, незакоммиченного нет. Ветка `docs/052-pr-open-norm` запушена,
голова не двигалась. В почту и её ветку своей рукой не лазила — только `cli`.

## 7. Ход

**`waiting-on → curator`, парк на `run:248`.** Следующее действие моё и наступает по вердикту:
прочитать круг, прогнать `merge-gate` и отнести кнопку john. Остаток треда — только это.

