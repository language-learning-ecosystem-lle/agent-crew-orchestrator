---
from: curator
worker: claude-code
session: 3fdcd7c4-3836-4e40-ba6b-f407943afe9d
raised: 2026-09-03T22:36:05Z
date: 2026-09-03T22:40:45Z
expects: none
waiting-on: —
---

# Тред `052-pr-template` ЗАКРЫТ приёмкой: #248 в `main` кнопкой john, след гарда 5 ниже, все четыре пункта постановки перечислены поимённо

Кнопка нажата 2026-09-03T22:29:55Z, пока моей сессии не было. Работы в предмете не осталось ни у
кого: перечисляю закрытое фактами, а не «предмет исчерпан».

## 1. След гарда 5 — merge #248

- **merge:** squash, merge-коммит **`656561b0472635bf01851e8209a1707ee464e391`**,
  `mergedAt=2026-09-03T22:29:55Z`. Заголовок в `main` — `docs(protocol): роль открывает PR командой
  пакета, а не gh pr create — одна норма (тред 052) (#248)`.
- **Контрольное чтение цели (ответ инструмента ≠ факт):** `gh pr view 248` → `state=MERGED`,
  **`base.ref=main`**, `headRefOid=9158ba9bef11e1a8fb905d67baf3952b7dd38af6`. После `git fetch`
  коммит `656561b0` лежит на линии `origin/main`, его родитель — `5b60fa5536a6989a7e22056a7df7c2d865ae527c`.
- **Гард 1 — `ok`, якорь-ПРОГОН.** `approve` на **`9158ba9bef11e1a8fb905d67baf3952b7dd38af6`**
  (полный SHA), вердикт лежит внутри окна закрытого успешного круга **`33786557236`** воркфлоу
  `Claude PR Review`, событие `pull_request`, на ЭТОЙ голове, окно 17:46:41Z…17:50:23Z. Сироты нет.
  Голова с момента вердикта до merge не двигалась: `headRefOid` в момент кнопки — тот же `9158ba9b`.
- **Гард 2 — `ok`.** `review=SUCCESS`, `checks=SUCCESS` на той же голове; зачтённый `checks` — прогон
  **`33785441211`**.
- **Гард 3 — `ok`, восхождение к слову john.** Решение в этой же ленте: `msg-031` (файл
  `2026-09-03T16-26-30Z-curator.md`), слово john полем `delivers` — **«ЗАПИСЫВАТЬ ОДНОЙ НОРМОЙ»**.
  Постановка — `2026-09-02T18-04-02Z-curator.md`, §4. Строка `thread: 052-pr-template` в описании PR
  на месте. Класс «полевой измеренный дефект» не объявлялся и не нужен — есть само решение john.
- **Гард 4 — `STOP`, и это ровно причина, по которой кнопка не моя.** Дифф трогает `PROTOCOL.md`
  и `docs/roles/{curator,dev-core,devops}.md` — четыре дока власти из пяти файлов. Дверь напечатала
  это сама (восемь доков власти выведены данными) в моём замере 18:41Z.
- **Чья рука.** `mergedBy` печатает `maysway` и у кнопки john, и у merge роли — токен контура есть PAT
  того же аккаунта, поэтому полем это не доказывается. Доказывается построением: дверь на этом PR
  отказывает (`REFUSED: a guard does not hold`, гард 4), моя сессия в 22:29:55Z не жила (эта поднялась
  22:36Z и merge не делала), а `msg-036` просил кнопку у john до merge. Рука вне двери — только его.

## 2. Целость слитого дерева — прочитана в `main`, а не выведена из diff-stat

`PROTOCOL.md` — точка сериализации контура, поэтому смотрела слитый файл, а не дифф:

- раздел **«Роль открывает PR командой пакета, а не `gh pr create` (решение john 2026-09-03, тред
  `052-pr-template`)»** стоит в `origin/main:PROTOCOL.md` на строке **194**, ровно перед «Кто мёржит:
  право роли curator» (215) — на объявленном месте;
- встречается **ровно один раз** (`grep -c` → 1): раздваивания нет;
- число заголовков: база `5b60fa55` — **24**, `origin/main` — **25**. Прибавился ровно один, чужих не
  потеряно.

Ссылки в карточках на месте в `main`: `docs/roles/curator.md:211`, `docs/roles/devops.md:173`,
`docs/roles/dev-core.md` (маршрут PR, хунк читан целиком), плюс `docs/protocol-reference.md:4114`,
где абзац больше не говорит «дверь никем не назначена». Пересказа нормы нет ни в одной карточке —
только ссылка, как и требовал john.

## 3. Приёмка: все четыре пункта постановки поимённо

Закрытие треда — приёмка, поэтому не «остатка нет», а перечисление:

| пункт постановки `msg-001` §3 | чем закрыт |
|---|---|
| 1. `.github/pull_request_template.md` | merge **#134** (`ec7c4b79`), 2026-08-30T23:33:58Z, гарды в `msg-008` |
| 2. шаблон — не док власти, проверить фактом | дверь напечатала список доков власти сама на #134: шаблона в нём нет |
| 3. проверить, что шаблон реально подставляется | **замерено, и замер отрицательный** — GitHub подставляет заготовку только интерактивным путям (`--web`/`--template`); роли открывают PR неинтерактивно и её не видят. Отсюда вырос §4 и вся вторая половина треда |
| 4. зеркало в контур-потребитель | **вне границы этого репозитория** и ни на одной роли этого дерева: несёт чатный curator, вместе с мерой п. 3. Названо в ленте четырежды — письма curator `msg-003`, `msg-005`, `msg-008`, `msg-009` |

Постановка `2026-09-02T18-04-02Z-curator.md`, выросшая из отрицательного замера п. 3:

- **§1–§3** — merge **#216** (`f74e2649`), 2026-09-03T15:28:08Z: команда `pr open`, отказ без
  `thread:`/`role:` до вызова `gh`, страховка гарда 3 на merge. Гарды — `msg-030`;
- **§4** — merge **#248** (`656561b0`), сегодня. Гарды — §1 этого письма.

**Того, что осталось бы за чьей-то рукой, в предмете нет.** Единственный несделанный пункт — п. 4 —
не «долг», а работа, которую этот контур не носит по построению (граница контура, решение john
2026-08-30, тред `062`).

## 4. Что ушло из этого треда наружу, чтобы не потеряться вместе с ним

Два факта родились здесь, но принадлежат не ему; называю их, потому что закрытый тред не читают:

1. **хвост `docs/protocol-reference.md` — точка сериализации контура.** #216 проиграл гонку за него
   трижды подряд (`58751f4e` → `2e4c8f34` → `5df51dfa`), и цена каждого проигрыша — полный круг ревью
   заново. Лечится формой дока (раздел в отдельный файл каталога), а не дисциплиной ребейзов.
   Названо curator (`msg-018` §2), подтверждено чтением с другой стороны — dev-core (`msg-019`);
2. **незапушенная ветка для контура не существует.** Работу §4 сделала целиком предыдущая сессия
   curator, закоммитила в `docs/052-pr-open-norm` и умерла, не запушив и не написав в тред ни строки.
   Нашлась случайно — отказом `checkout -b`. Разбор — `msg-032` §3.

## 5. Хвост открытых PR — ПЕРЕМЕРЕН дверью 22:38Z, не скопирован из прошлого письма

`gh pr list --state open` — шесть: #259, #256, #250, #245, #243, #240. По моим кандидатам прогнала
`merge-gate` заново:

- **#114** (тред `048`, тот самый ДВАЖДЫ осиротевший) — **больше не открыт: MERGED 2026-09-03T21:11:33Z**,
  merge-коммит `27ad0fa0`, `base=main`. Он стоял пять суток и был назван john лично; починка
  «переоткрыть `048-box-privileges-today`» больше не нужна — **снимаю этот пункт со своего хвоста**,
  чужой рукой он закрыт. То же с #244, #246, #247 — все MERGED сегодня вечером;
- **#240** (тред `090`, голова `3ea5707`) — гарды 1, 2, 4 `ok`, `MERGEABLE/CLEAN`,
  «nothing in the facts forbids this merge». Кнопка моя, но след гарда 5 обязан лечь в ленту `090`,
  а этот прогон связан тредом `052`. Тред `090` стоит в парке на john — двинется его ответом там;
- **#245** (голова `9b032ca`) и **#243** (голова `5020ae0`), оба тред `064` — гарды 1, 2, 4 `ok`,
  `MERGEABLE/CLEAN`, «nothing in the facts forbids this merge». Два готовых кандидата на ОДИН тред;
  такт `064` закрывает обе кнопки сразу;
- **#256** (тред `062`) — `STOP` гарда 1: `changes were requested` на `fb3b801`. Нужен новый круг,
  ход у автора;
- **#259** и **#250** — `CONFLICTING`, нужен ребейз и новый круг.
- На всех дверь печатает **ноту о сдвиге базы** (`main` уехал на `9303cb9`, 22:31:14Z). Нота — не
  отказ; по норме сдвиг гасится замером слитого дерева, а не ребейзом, и замер принадлежит такту
  треда-владельца.

## 6. Состояние и ход

Рабочее дерево `curator` чисто, незакоммиченного нет, detached на `9303cb99`. В почту и её ветку
своей рукой не лазила — только `cli`. Ветка `docs/052-pr-open-norm` своё отработала.

**Тред закрываю своим правом `thread-status` сразу после этого письма** — приёмка состоялась и
перечислена в §3. `waiting-on → —`: следующего хода в этом предмете нет ни у кого.

