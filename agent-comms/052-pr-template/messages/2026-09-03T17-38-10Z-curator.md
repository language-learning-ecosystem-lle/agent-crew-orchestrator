---
from: curator
worker: claude-code
session: 60b1e564-c20c-4a5c-ac0f-53e4b7522266
raised: 2026-09-03T17:32:07Z
date: 2026-09-03T17:38:10Z
expects: none
waiting-on: curator
parked-on: run:248
---

# §4 написан и открыт PR #248 — норма «роль открывает PR командой пакета» в `PROTOCOL.md`. Жду `checks` (прогон 33785441211), метка `review` — следующим тактом, merge — кнопка john

Слово john из `msg-031` исполнено буквально: **одна норма в `PROTOCOL.md` плюс ссылки на неё**, без
копий по карточкам. Это последний остаток треда — §1–§3 постановки закрыты merge'ом #216.

## 1. Что открыто

**PR #248** — `docs(protocol): роль открывает PR командой пакета, а не gh pr create (тред 052)`,
голова **`9158ba9bef11e1a8fb905d67baf3952b7dd38af6`**, база `main`,
https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/pull/248

**PR открыт командой `pnpm protocol pr open --write`** — то есть той самой дверью, которую норма и
назначает. Дверь напечатала о себе: `pr open — thread '052-pr-template', role 'curator'`. Это не
украшение: единственный способ показать, что записываемая норма исполнима, — исполнить её на её же
собственном PR.

## 2. Дифф — пять файлов, и все пять на месте по границам john

- **`PROTOCOL.md`** — норма в разделе «Ревью PR и merge», перед «Кто мёржит: право роли curator».
  Четыре абзаца: сама команда и что она отказывает без `thread:`/`role:` до вызова `gh`; что ранний
  отказ **не заменяет** поздний (гард 3 `merge-gate` стоит как стоял — страховка на обход рукой);
  почему это норма, а не просто существующая команда (механизм без единственного места, куда роль
  смотрит, не наступает никогда); и что **шаблон `.github/pull_request_template.md` не отменяется** —
  он для интерактивного человеческого пути (`--web`, `--template`), роль открывает PR явным телом и
  заготовки не видит вовсе;
- **`docs/roles/{curator,dev-core,devops}.md`** — ССЫЛКА на норму в маршруте PR каждой карточки,
  текст нормы не пересказан ни в одной. Одна фраза со ссылкой на `PROTOCOL.md`;
- **`docs/protocol-reference.md`** — абзац «Чего в этой работе нет и на ком это» больше не врёт:
  дверь назначена, источник решения назван.

**`pilot-codex` не тронут, и это замер, а не пропуск:** её карточка говорит «не мёржит, не ставит
меток, **не трогает PR**» — маршрута PR у роли нет, вешать ссылку не на что.

**Почему карточки вообще тронуты, хотя копий нельзя.** Твоя же формулировка: «только норма и **ссылки
на неё**». Отдельно замерила рукой, что противоречия убирать не пришлось: `grep -rn "gh pr create"`
по `docs/roles/`, `PROTOCOL.md`, `REVIEWER.md`, `.github/` — **пусто**. Ни одна карточка не
инструктировала `gh pr create`; они просто не называли команды вовсе, и норма стала первым местом,
где она названа.

## 3. Работу этого такта сделала НЕ эта сессия — и это стоит назвать

Начав с чистого дерева, я написала свой вариант нормы, и только при создании ветки git отказал:
ветка `docs/052-pr-open-norm` **уже существовала** — с коммитом `9158ba9b`, автор `curator`,
**2026-09-03T17:31:28Z**, то есть примерно за минуту до старта моего окна. Предыдущая сессия curator
сделала эту работу целиком и умерла, **не запушив ветку и не написав в тред ни строки**.

Я взяла её коммит, а свой (`bcf4d310`, только `PROTOCOL.md`) выбросила: её дифф **шире и точнее** —
он несёт ссылки в трёх карточках и закрывает абзац в `docs/protocol-reference.md`, чего у меня не
было. Прочла его целиком перед тем, как пушить; границы john соблюдены в нём построчно.

**Класс, который тут виден, дороже случая:** работа, закоммиченная в локальную ветку и не
запушенная, для контура не существует — ни почта, ни `gh`, ни следующая сессия её не видят. Нашлась
она случайно, отказом `checkout -b`. Если бы я назвала ветку иначе — контур заплатил бы за одну и ту
же правку дважды. Свою карточку это уже покрывает («закоммитить как есть» при сворачивании), но push
там не назван, а без него коммит невидим. Норму из этого не ввожу — называю факт.

## 4. Чего ещё нет и кто ходит следующим

- **`checks` на голове `9158ba9b`** — прогон
  [`33785441211`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/33785441211),
  `in_progress` на момент письма. У конвейера не стою — паркуюсь на круге этого PR;
- **метка `review` не повешена — намеренно.** Она вешается ПОСЛЕ зелёного `checks` на той же голове;
  зелёного ещё нет. Это первое действие следующего такта curator;
- **merge — кнопка john**, и называю это заранее, чтобы не выяснилось после круга: дифф трогает
  `PROTOCOL.md` и три карточки ролей, то есть доки власти, гард 4 `STOP`. Роль не принимает
  документы, определяющие её собственные полномочия. На этот раз это не заявление по памяти — класс
  читается из списка файлов §2 напрямую, и дверь скажет то же самое.

**Отдельно: дверь почты дважды отказала моему парку, и оба раза по делу.** Сначала я запарковалась
на `pr:248` — она ответила, что такой парк снимается ровно заголовком `merged-pr: 248`, что
состояние PR не сторожит никто, и что парк за событием, которому нужен СОБСТВЕННЫЙ следующий шаг
этого треда (метка — мой ход), есть дверь, запертая изнутри (тред `061`). Затем я подставила в
`run:` id прогона — она объяснила, что ОБЕ формы парка хотят номер PR, а `run:<pr>` ждёт круга НА
этом PR. Парк стоит правильной формой: `run:248`.

## 5. Хвост открытых PR

`gh pr list --state open` (17:35Z) — восемь: #248 (этот), #247, #246, #245, #244, #243 и два готовых
с прошлого такта — **#240** (тред `090`) и **#114** (тред `048`). Оба по-прежнему открыты и по
фактам двери были готовы к merge час назад. **Я их снова не мержу и снова по той же причине:** след
гарда 5 обязан лечь в ленту треда-владельца (`090`/`048`), а этот прогон связан ровно тредом `052`.
Их надо поднять их собственными тактами — там осталась одна кнопка на каждый.

## 6. Состояние

Рабочее дерево `curator` чисто, на ветке `docs/052-pr-open-norm` (запушена, отслеживает origin).
Незакоммиченного нет. В почту и её ветку своей рукой не лазила.

**Ход остаётся у curator** — следующее действие моё (метка после зелёного `checks`), парк на
`run:248`.

