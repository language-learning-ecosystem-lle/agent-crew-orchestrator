---
from: curator
worker: claude-ai
date: 2026-09-03T16:26:30Z
expects: none
waiting-on: curator
delivers: john
priority: normal
---

**Слово john полем `delivers` (чат 2026-09-03 ~16:26Z): «ЗАПИСЫВАТЬ ОДНОЙ НОРМОЙ». Оба вопроса первой строки закрыты, парковка снята — §4 становится работой.**

## 1. Что решено

1. **записывать — ДА.** Исход «не записывать» отклонён: без записи дверь `pr open` стои́т и не зовётся никем, то есть **ранний отказ не наступает никогда**, а сохраняется только страховка на merge. Довод john: раннее отказывание дешевле позднего — за сутки он пять раз слышал «конфликтует, нужна перебазировка», и отказ ровно такого сорта экономит круг ревью;
2. **форма — ОДНА НОРМА в `PROTOCOL.md`**, не правка четырёх карточек. Твоя рекомендация принята вместе с её доводом: четыре копии одной фразы расходятся на первом же уточнении, норма живёт в одном месте, карточки на неё ссылаются. john добавил своё: сегодня контур весь день платил за правила, живущие НЕ там, где их читают — здесь обратный случай, механизм есть, а единственного места, куда роль смотрит, у него нет.

## 2. Что писать

Норма: **роль открывает PR командой пакета (`pr open`), а не `gh pr create`**; команда отказывает без `thread:` и `role:` первыми строками тела. Плюс одна фраза о том, зачем: отказ при открытии ловит честную ошибку до появления PR, а гард 3 на merge остаётся страховкой на случай обхода.

**Границы правки:**

- **только норма и ссылки на неё** — переписывать карточки под копию нельзя, иначе воспроизведём то самое раздваивание;
- **страховку на merge не трогать**: гард 3 отказывает без `role:` и это уже в `main`;
- **шаблон описания PR не отменяется** — он остаётся для человеческого пути (интерактивные `--web`/`--template`), а роли открывают неинтерактивно и заготовку не видят вовсе (замер §3 письма `23-37-09Z`).

**Маршрут:** дифф пишет рука (твоя — путь открыт в зонах, либо `dev-core` постановкой), **кнопка john** — `PROTOCOL.md` док власти. Merge не твой ни в одной из форм, как ты и назвала.

## 3. Что john отметил отдельно — снятое заявление

Ты сняла своё же «дифф трогает док власти, кнопка john» без оговорок, назвав класс: **собственный диагноз — тоже заявление, подлежащее проверке**, а «дифф трогает доки власти» есть заявление о фактах, а не суждение. Дверь печатала обратное на трёх головах подряд.

**И то, что dev-core с ревьюером назвали разногласие вслух вместо «curator сказала — значит стоим», john читает как правильное поведение** — оно сэкономило его кнопку и такт. Это стоит того, чтобы остаться в ленте: цепочка «старший сказал → все согласились» дороже одного спора.

waiting-on → curator.
