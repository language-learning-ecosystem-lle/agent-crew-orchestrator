---
from: curator
worker: claude-code
session: 53bbafc7-507b-4d10-ac61-80fdd5c20a69
raised: 2026-09-03T18:40:37Z
date: 2026-09-03T18:44:14Z
expects: ack
waiting-on: curator
parked-on: john
---

# ТРЕБУЕТСЯ РЕШЕНИЕ JOHN: нажать squash-merge на #248 — гард 4 `STOP` (доки власти), остальные четыре гарда закрыты и названы ниже

Вердикт ревьюера приехал (`approve`, находок по критериям 1–11 нет), голова под ним не двигалась,
дверь перемерена мной 2026-09-03T18:41Z. Работы, которую двигала бы моя рука, в треде `052` больше
нет: остаток предмета — ровно одна кнопка, и она ваша по построению.

## 1. Что просится

Squash-merge PR **#248**, голова `9158ba9bef11e1a8fb905d67baf3952b7dd38af6`, заголовок коммита —
`docs(protocol): роль открывает PR командой пакета, а не gh pr create (тред 052)`.
Кнопка жмётся с `--match-head-commit 9158ba9bef11e1a8fb905d67baf3952b7dd38af6` — sha взят из
вывода самой `merge-gate`, ниже.

## 2. Почему кнопка ваша, а не моя — гард 4

Дифф — 5 файлов, и четыре из них доки власти:

| файл | правка |
|---|---|
| `PROTOCOL.md` | +21/−0 — сама норма |
| `docs/roles/curator.md` | +3/−1 — ссылка на норму |
| `docs/roles/dev-core.md` | +3/−1 — ссылка на норму |
| `docs/roles/devops.md` | +2/−0 — ссылка на норму |
| `docs/protocol-reference.md` | +6/−4 — доком власти НЕ является |

Роль не принимает документы, определяющие её собственные полномочия. Класс Д-1 здесь не объявлен,
исключений у гарда 4 нет.

## 3. Гарды поимённо — живой вывод двери

`pnpm protocol merge-gate --ref origin/main --pr 248 --review-workflow 'Claude PR Review'`
(2026-09-03T18:41Z, восемь доков власти выведены данными, не памятью):

```
  ok   guard 1 · approve on the current head: approved on 9158ba9 by github-actions
       — inside the round 33786557236 of 'Claude PR Review' on this head
         (2026-09-03T17:46:41Z…2026-09-03T17:50:23Z)
  ok   guard 2 · green checks on the same head: 2 check(s) green: review=SUCCESS, checks=SUCCESS
  you  guard 3 · ascent to a decision of john's: thread '052-pr-template' — read the feed
  STOP guard 4 · no self-merge on the documents of power: john merges this one — it changes
       PROTOCOL.md, docs/roles/curator.md, docs/roles/dev-core.md, docs/roles/devops.md
  you  guard 5 · a trace of the merge
  ok   mergeability · mergeable=MERGEABLE (mergeStateStatus CLEAN)
REFUSED: a guard does not hold
```

- **Гард 1** закрыт якорем-ПРОГОНОМ, а не review-объектом: вердикт лежит внутри окна закрытого
  успешного круга `Claude PR Review` (`event=pull_request`) на ТЕКУЩЕЙ голове. Сироты нет.
- **Гард 2** — оба чека зелены на той же голове; `checks` — прогон `33785441211`.
- **Гард 3** — восхождение есть и оно ваше: слово john полем `delivers` от 2026-09-03 в этой же
  ленте — «записывать ОДНОЙ НОРМОЙ». Дифф исполнил это буквально: одна норма в `PROTOCOL.md`,
  из карточек — ссылки, без пересказа текста по карточкам.
- **Гард 5** — след merge ляжет сюда же следующим тактом; при вашей руке он мой доклад, а не мой
  merge.
- **Контрольное чтение головы** (18:43Z, ответ инструмента ≠ факт): `headRefOid` =
  `9158ba9b…`, `state=OPEN`, `labels=[review]`, `MERGEABLE/CLEAN`. С момента метки голова не уехала —
  вердикт судит то самое дерево, которое мёржится.

## 4. Что говорит вердикт ревьюера

`approve`, находок нет. Из содержательного, что она проверила чтением, а не пересказом:
`grep -rn "gh pr create"` на голове PR даёт совпадения ТОЛЬКО внутри новых строк самого диффа —
то есть до этого PR ни одна карточка `gh pr create` явно не предписывала; форма `pr open` сверена
с README (`### \`pr open\``); зоны чисты (`5 path(s) of 'curator': none under a forbidden prefix`);
`pilot-codex` не тронута легитимно — её карточка прямо говорит «не трогает PR», маршрута PR у роли
нет. Красным у неё стоит ровно критерий 5 — доки власти, — и это то же самое, что говорит гард 4.

## 5. Хвост открытых PR — ПЕРЕМЕРЕН дверью, не скопирован

`gh pr list --state open` (18:42Z) — девять: #249, #248 (этот), #247, #246, #245, #244, #243,
**#240**, **#114**. Два последних несу четвёртый такт, поэтому прогнала `merge-gate` заново:

- **#240** (тред `090`, голова `3ea5707`) — гарды 1, 2, 4 `ok`, `MERGEABLE/CLEAN`. Кнопка МОЯ.
  Но тред `090` сам стоит в парке на john с 15:17Z (`notify.state`), так что двинется он вашим
  ответом там, а не моим тактом здесь.
- **#114** (тред `048-session-privileges`, голова `1bc125a`) — гарды 1, 2, 4 `ok`,
  `MERGEABLE/CLEAN`. **И он осиротел ДВАЖДЫ, это новый факт против прошлого такта:** тред, названный
  полем `thread:` его описания, закрыт как недействительный дубль, а тред-преемник
  `048-box-privileges-today`, куда предмет переехал и где 2026-09-03 разобрано само сиротство
  («PR ОСИРОТЕЛ — его тред закрыт, и пару никто не поднимает по построению»), **тоже закрыт**, при
  живом открытом PR. Гард 5 требует, чтобы след merge лёг в ленту треда-владельца, — а живого
  владельца у #114 сейчас нет ни одного. Своей рукой я его не мержу и в этом прогоне (он связан
  тредом `052`) не чиню; законная починка — переоткрыть `048-box-privileges-today` моим правом
  `thread-status` первым же тактом, который не связан чужим тредом, и домержить оттуда. Называю
  здесь, потому что PR вы уже поднимали сами («в aco пять суток стоит PR #114»), и с тех пор он не
  сдвинулся, а причина стала на одну дверь глубже.
- На ОБОИХ дверь печатает **ноту о сдвиге базы**: `main` уехал на `054dca7` (17:24Z) уже после
  старта зачтённых `checks`. Нота — не отказ (гард 2 `ok`), и по норме сдвиг базы гасится замером
  (дерево мержа + сюита), а не ребейзом; замер принадлежит такту треда-владельца.

## 6. Цена парка названа честно

Паркуюсь на john здесь же, где вопрос родился: живой работы в `052`, кроме этой кнопки, не осталось,
а значит морозить нечего. Но цену называю прямо: в `notify.state` контура уже стоят **пять** парков
на john — `029` (с 2026-08-22, двенадцать суток), `064`, `074`, `090`, `096`. Этот шестой.
Парк на `run:248` снимаю тем же письмом: круг закрылся, вердикт в ленте.

## 7. Состояние

Рабочее дерево `curator` чисто, незакоммиченного нет. Ветка `docs/052-pr-open-norm` запушена,
голова не двигалась. В почту и её ветку своей рукой не лазила — только `cli`.

## 8. Ход

**`waiting-on → curator`, парк на `john`.** Тред закрывается приёмкой после вашей кнопки: следующее
моё действие — прочитать `base.ref` после merge, положить след гарда 5 и закрыть `052`.

