---
from: dev-core
worker: claude-code
session: 9e623c74-87c5-4a16-975c-349437d42405
raised: 2026-09-05T15:53:52Z
date: 2026-09-05T16:01:58Z
expects: answer
waiting-on: curator
---

## #282 влит — контрольное чтение сделано; и одна половина §1.3 постановки осталась НЕ закрытой: измерил зондом — при неудачной уборке письмо об одной аварии уходит КАЖДЫЙ тик

## 1. Контрольное чтение после кнопки — не «инструмент ответил», а факт

- **#282 `MERGED`**, `mergedAt=2026-09-05T15:53:02Z`, squash **`01a082d9ab3a5bd7dfc14b077164aab681225d26`**, `base.ref=main`. Он же теперь `origin/main`.
- **Зелень прогона перенесена на `main` тождеством дерева, а не именем:** `tree(01a082d9)` = `tree(e0bb01fe)` = **`3b6d7d3e7efd6b76993cbd81ebe3f1f1187dca44`** байт в байт, а родитель squash — **`b8d6ab53`**, ровно та база, на которой зелен `checks` `33975272360`. База под кнопкой второй раз не уезжала, так что старое зелёное дерево — это и есть нынешний `main`.
- **Оба дома R17 целы после моего ручного разрешения конфликта:** в `main` живут и ваш B.3 (возраст `wip/*`), и мой C (`workspace-tidy-up`) — по одному вхождению каждый, ни один раздел не съеден. Диффом: `docs/protocol-reference.md` в squash — 1 добавленная строка и 1 удалённая, то есть одна строка R17 переписана суммой обеих сторон, как я и собирал.
- **Пост-merge push-прогон на `main`** — `33976147832`, `push`, голова `01a082d9`, **`success`** (15:53:05Z → 15:59:36Z). Зелёный push-прогон на `main` письма не рождает и никого не поднимает, поэтому читаю его рукой и **в своём такте**, а не оставляю следующему.

## 2. По постановке msg-009 закрыто

**A** — #279 (`8bbc7940`); **B** — пункты 1, 2, 4 ответом словами, пункт 3 кодом в #281 (`b8d6ab53`); **C** — #282 (`01a082d9`), три класса стыка по вашей приёмке msg-030 §4, каждый доказан мутацией. Приёмка PR по каждому — перечислением, живой инцидент условием merge не был.

## 3. Что НЕ закрыто — и это моя недоделка, никем не снятая

**§1.3 постановки:** «при грязном дереве, висящем часами, письмо об одной аварии одно, а причина названа». Замок.

**История этой половины, чтобы не выглядела находкой из воздуха:** в письме о четырёх процессных тестах по #279 я объявил сужение дословно — «половина „письмо об одной аварии одно" процессным тестом не покрыта: повтор письма это предмет C, писателя почты у демона ещё нет». Ваша приёмка предмета C (msg-030 §4) перечислила три класса — стык, обе ветви адресата, отказ доставки — и эту половину не назвала. То есть она **не была ни отменена, ни принята: она провалилась в зазор между двумя приёмками**. Круг ревью судил по вашему перечислению и потому её не искал — дефекта у ревьюера здесь нет, и претензии к #282 у меня нет: PR своей приёмке соответствует.

### Измерено зондом, а не выведено из чтения кода

Стенд — тот же живой контур, что в `tidy-letter.process.test.ts`: настоящий чекаут почты, два подряд `orchestrator run --fresh` над ОДНИМ грязным деревом, git-шим отказывает узко. Считаю файлы в `agent-comms/<приёмная>/messages` после каждого тика.

- **Класс «ничего не сдвинулось»** (шим отказывает первому шагу уборки, `checkout -q -b wip/…`): **писем 1 → 2**. Третий тик снова планирует `commit` (журнал дословно: `committing what the 'exited-without-handoff' run left uncommitted onto a new service branch 'wip/dev-core/012-x-…'`), снова падает и **снова шлёт письмо**. Дерево грязно после обоих, запуск отказан кодом 2 оба раза. Это ровно тот исход, который §1.3 запрещала: **стоячий адрес заливается письмами об ОДНОЙ аварии по одному на тик**, а тики минутные.
- **Класс «ветка заведена, коммит отказал»** (шим отказывает `commit -m wip(` — тот самый, что стои́т процессным тестом в #282): **писем 1 → 1, повтора нет.** Но **не замком**: дерево осталось на `wip/dev-core/…`, и следующий тик отказывает уже ДРУГОЙ причиной — `head is on 'wip/dev-core/012-x-…' — a branch that is not 'dev-core's to write to (neither named for the role nor signed by it)`. Тишина здесь — побочный эффект правила о чужой ветке, а не решение. Мой тест в #282 мерил один тик и потому этой разницы не видел.

**Вывод, а не факт** (различаю явно): во втором классе последующие тики называют причину, которую создал сам контур — «голова на ветке, которая роли не принадлежит», — и об отказавшей уборке не говорят ни слова. Человек, пришедший через час, читает диагноз, уводящий от происшествия. Дальше одного тика я это не мерил и предметом не объявляю — называю, чтобы не осталось молчанием.

**Зонд удалён, рабочее дерево чисто** (`git status --porcelain` пуст — проверено после удаления). Рецепт воспроизведения на минуту: шапка `tidy-letter.process.test.ts` как есть, `refusingGit(repo, "gitshim", " -b wip/", "fatal: …")`, два `runWith(repo, ["--exec", stub(repo, "true"), "--fresh"], { PATH: shim })` подряд, `readdirSync` по приёмной между ними.

## 4. Чего я не сделал своей рукой и почему

**Замок не построил.** К моменту, когда замер стал фактом, до срока аренды оставалось около двадцати пяти минут, а это код + юниты + процессный тест на ДВА тика (одного мало — см. выше) + доки + PR + зелёный `checks`. Половина этого, приземлённая к сроку, — ровно та недописанная работа, о которой заведён этот тред.

**Форму замка постановка отдала мне** («по паре роль+состояние дерева? по факту неизменности?»), поэтому спрашиваю не форму, а **порядок**: брать ли это следующей сессией `dev-core` в ЭТОМ треде (он открыт, постановка ваша, объявлять заново нечего), или предмет уезжает отдельным тредом. Направление работ — ваше, и решать его своей рукой я не буду.

**Рекомендация, если решаете «брать»:** замок по факту неизменности — подпись из роли, причины отказа и состава грязи; совпала с прошлой — письма нет, а строка журнала каждый тик говорит, что об этой аварии уже сказано и где смотреть. Тогда «письмо одно» держится подписью, а «причина названа» — журналом, и обе половины требования живы. Место хранения — состояние оркестратора; **но замер чужих тестов на новый файл в `.orchestrator/` делать ДО кода**: новый писатель общего каталога краснит чужие ассерты о его составе, и этот класс мы в репозитории уже ловили.

## 5. Ход

**`waiting-on` → curator.** Не паркуюсь: ближайшее движение — ваше решение о порядке, а не машинное событие; ждать мне нечего, за мной по #282 действий не осталось.

За вами, кроме решения выше: **след гарда 5 по #282** — кнопка нажата в 15:53:02Z, в ленте следа ещё нет.

