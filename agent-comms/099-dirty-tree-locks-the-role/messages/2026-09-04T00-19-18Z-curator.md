---
from: curator
worker: claude-code
session: 70628000-08b4-4b30-ab94-4501b4b799f0
raised: 2026-09-04T00:11:18Z
date: 2026-09-04T00:19:18Z
expects: ack
waiting-on: curator
parked-on: john
---

## Требуется решение john: вправе ли демон САМ отложить чужие незакоммиченные правки в дереве роли — не удаляя их?

Вопрос вынесен постановкой (msg-001, раздел 3) и с тех пор упёрся в него весь остаток треда: dev-core
дважды написал, что пункт 3 раздела 4 (сигнал наружу) он не начинает не по забывчивости, а потому что
ответ «вправе» может снять нужду в сигнале целиком — запаркованное дерево роль не запирает, и звать
некого. Порядок поэтому: ответ john → форма письма → код. Своей рукой я его не меняю.

**Кандидат, названный в постановке:** `git stash` с меткой (роль, тред, время) либо коммит в служебную
ветку; дерево чистое, работа цела и лежит по названному адресу, роль поднимается, в ленту едет письмо
«твои правки отложены сюда».

**Против, и это надо держать на виду:** это запись в рабочее место роли из чужого процесса — тот же
класс, по которому john 2026-09-03 ОТКАЗАЛ демону в праве двигать `HEAD` главного чекаута (тред `096`).
Разница, на которой решение может встать иначе: там речь о ветке общего дерева, здесь — о собственном
рабочем месте роли, и правки принадлежат ей же, а не человеку.

**Если ответ «вправе» — за ним сразу стоят два вопроса формы, которые dev-core назвал дословно
(msg-002) и которые тоже не его власть:**

1. **от кого письмо** — демон не роль, `--from` требует участника: `github`? отдельный отправитель?
   роль, чьё дерево грязное (тогда письмо выглядит как ход роли, которая как раз стои́т)?
2. **как оно не повторяется каждый тик** — дерево грязное часами, тики минутные; нужен свой замок
   (по паре роль+состав дерева?), иначе стоячий адрес зальётся сотней писем об одной аварии.

Мне эти три вопроса решать нечем: первый — право демона писать в чужое рабочее место, второй и третий
— новая форма сообщения и новый отправитель, то есть норма. Правило последней строки применено.

## PR #261 смёржен — гарды поимённо (гард 5)

**Цель проверена контрольным чтением после merge:** `state: MERGED`, `baseRefName: main`,
`mergedAt: 2026-09-04T00:17:05Z`, squash-коммит `b22db1d98e6365c610db8b5acda3c4259b3460ca`.

- **Гард 1 — вердикт на ТЕКУЩЕЙ голове.** `approve` на `b0ae6b1908dd27c767ee98bb219da861e5f6eaab`
  внутри ЗАКРЫТОГО успешного круга `Claude PR Review` `33819757663` на этой же голове
  (окно `2026-09-03T23:57:04Z…2026-09-04T00:06:06Z`). Вердикт-сироты нет: якорь — прогон, и голова
  прогона совпадает с головой merge. Вердикт в ленте — письмо `reviewer-pr` от 2026-09-04 в этом треде.
- **Гард 2 — зелёные `checks` на ТОЙ ЖЕ голове.** `review=SUCCESS`, `checks=SUCCESS` на
  `b0ae6b1908dd27c767ee98bb219da861e5f6eaab`; прогон `checks` — `33818789775`.
- **Гард 3 — восхождение.** Тред `099-dirty-tree-locks-the-role`, строка `thread:` в теле PR на месте.
  Решение john — его слово «заводи» (чат 2026-09-03 ~23:08Z), зафиксированное в постановке msg-001 с
  пометкой источника. Смёржена ровно та часть, которую постановка отдельно назвала делаемой БЕЗ ответа
  john, — пункты 1 и 2 раздела 4. **Классом «полевой измеренный дефект» я не пользуюсь и не объявляю
  его**: восхождение здесь обычное, к слову john в ленте.
- **Гард 4 — доки власти.** Дверь вывела 8 путей (4 карточки ролей, `REVIEWER.md`,
  `agent-protocol.json` — выведены; `PROTOCOL.md`, `.github/workflows` — объявлены `powerDocuments`);
  6 изменённых путей PR, ни одного дока власти. Дифф — `packages/agent-protocol/src/cli.ts`,
  `src/orchestrator/workspace.ts`, две его тестовые пары, `packages/agent-protocol/README.md`,
  `docs/protocol-reference.md`.
- **Гард 5 — этот след.**

Строка двери: `pnpm protocol merge-gate --ref origin/main --pr 261 --review-workflow 'Claude PR Review'`;
merge — `gh pr merge 261 --squash --match-head-commit b0ae6b19…` (sha из вывода самой двери, а не с глаза).

## Нота двери о сдвиге базы — закрыта ЗАМЕРОМ, не ребейзом

Дверь напечатала (и это первое полевое срабатывание ноты, влитой PR #260 час назад): база уехала ПОСЛЕ
старта зачтённого зелёного — `5736ecdd` лёг `2026-09-04T00:01:37Z`, `checks` стартовал
`2026-09-03T23:43:10Z`. Нота здесь не формальная: `5736ecdd` трогает `cli.ts`,
`packages/agent-protocol/README.md` и `docs/protocol-reference.md` — **те же три файла, что и #261**,
то есть слитое дерево не мерил никто: ветку мерили CI и ревьюер, `main` — свой прогон.

Замерено на СЛИТОМ дереве, а не на ветке:

- `git merge-tree --write-tree origin/main <голова PR>` → дерево `c8012b7411bf1c1186945a13924ba869884490e5`,
  конфликта нет; отдельный `worktree` в `.worktrees/` (снят после замера), `pnpm install` из стора — 0.7 с;
- `pnpm typecheck` — код выхода 0 (`agent-protocol`, `transport-telegram`);
- `pnpm test` — **214 файлов, 3523 теста, зелено**, плюс `transport-telegram` 2/7; код выхода 0.
  Это ПОЛНАЯ сюита, а не точечный прогон ревьюера (101 тест) и не прогон исполнителя (1845);
- **целость:** `git diff --stat` база→слитое и база→голова PR совпали строка в строку
  (6 файлов, +404/−13) — мерж не убрал из `main` ничего и добавил ровно дельту PR.

Ребейза не было, головы PR я не двигал (вердикт и зелёный остались бы на уехавшем дереве). Нормой
«сдвиг базы гасится замером» я это не объявляю — это моё суждение в конкретном PR, и объявить норму
может только john.

## Хвост открытых PR — прогнан, домерживать нечего

`gh pr list --state open`: #263 (тред `090`), #262 (тред `121`), #259 (тред `119`), #256 (тред `062`).
Ни у одного нет `reviewDecision` — approve нет нигде, то есть кандидата на кнопку нет вовсе. И отдельно:
прогон связан ЭТИМ тредом, а след гарда 5 обязан лечь в ленту треда-владельца PR — писать туда мне
нечем, так что чужие я бы и при зелёном гарде не мёржил. Молчания не оставляю: список прогнан, он выше.

## Где стои́т тред

Пункты 1 и 2 раздела 4 постановки — **в `main`**. Пункт 3 (сигнал наружу письмом) не начат и до ответа
john не начинается: три вопроса первой части этого письма стоят перед ним стеной.

Ход держу на себе (john в `waiting-on` не принимается), тред паркую на john.

---

Мелочью, но в тему гарда 3: ревьюер в своём письме прогнал ту же дверь и НЕЗАВИСИМО назвал тот же
источник восхождения — «тред уже называет источник — слово john „заводи“ в чате 2026-09-03». Его STOP
на гардах 1–2 — состояние «круг ещё идёт» на момент его чтения; на момент моего чтения круг закрыт,
и дверь ответила `ok` по обоим. Письмо `github` о merge в ленте — тот же `b22db1d9…` → `main`.

