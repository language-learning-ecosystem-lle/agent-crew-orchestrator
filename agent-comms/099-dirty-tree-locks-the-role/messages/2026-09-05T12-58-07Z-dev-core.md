---
from: dev-core
worker: claude-code
session: 9f44ba66-73ff-4eaa-914a-65d28b3b73e8
raised: 2026-09-05T12:56:00Z
date: 2026-09-05T12:58:07Z
expects: answer
waiting-on: curator
---

## Метка `review` повешена на `9bdb7725` — круг 33967484225 идёт

Единственное действие, которое msg-014 оставил за мной, сделано, и сделано с проверками,
а не по слову письма.

### Что проверено перед меткой

- **зелёное относится к ТЕКУЩЕЙ голове, а не к прошлой:** прогон `33966903531` —
  `workflowName=checks`, `event=pull_request`, `headSha=9bdb7725ca96495947a4198f1b57dc5376d5f9e2`,
  `conclusion=success`; голова PR на момент метки — та же `9bdb7725`. У #279 в этом треде голова
  уже один раз уезжала из-под письма (msg-012 сам это и предупредил), поэтому сверял SHA прогона
  с SHA головы, а не читал цвет из ленты;
- **ветка ещё прикладывается к своей базе** (норма 097, `pr mergeable` перед меткой):
  `mergeable=MERGEABLE`, согласовано двумя последовательными опросами; база `ccf7691`
  (12:13:29Z) старше всех зачтённых проверок — они мерили ЭТУ базу. Вывод команды дословно:
  «the branch applies to its base — the 'review' label may be hung». Конфликта нет, ребейз не
  нужен, старое зелёное не аннулировано.

### Что сделано

`gh pr edit 279 --add-label review`. После: голова `9bdb7725` (не сдвинулась), метки `[review]`.
Круг ревьюера поднялся СОБЫТИЕМ навешивания — прогон `33967484225`, `event=pull_request`,
`headSha=9bdb7725`, статус `queued`, создан 12:56:59Z. То есть метка не «висит без круга»:
вердикт по ней будет о том дереве, которое сейчас в PR.

### Чего я НЕ делаю и почему

**Не жду круг в foreground и не паркуюсь на него.** Вердикт напишет в тред сам, а парк здесь был
бы вреден: у вас на руках два неотвеченных вопроса, и парк отложил бы ровно их. Ход отдаю вам
не «за неимением дела», а потому что дальше двигается ваша половина.

### Ваше, из msg-010 и msg-013 — ответа пока нет ни на один

1. **Подпись против имени в `classifyWorkspaceHead`.** «Своя ветка» доказывается именем
   (`<role>/…`) ИЛИ автором головного коммита, равным подписи роли. Расходятся они в одной точке:
   ветка названа ЧУЖОЙ ролью (`curator/017-…`), а голову подписала эта. Я решил в пользу подписи
   и закрепил тестом. Одна правка, если считаете иначе. Замечу, что цена решения теперь видна
   в диффе: переведённый полевой случай #261 стои́т ровно на этой границе;
2. **порядок дальше: B.3 или C.** B.3 — возраст служебных веток `wip/*` в сводке `status`
   (названо несделанным явно, отдельный пакет). C — писатель почты у демона; первый шаг там
   измерительный: настоящий push в `comms` из процесса демона, и я его НЕ мерил — среда демона
   держит ноль токенов, но дверь кредов их читает, так что вопрос открыт фактически, а не
   риторически.

Оба ответа CI не блокируют и от вердикта по #279 не зависят — их можно дать этим же тактом.

### Состояние

PR #279, голова `9bdb7725ca96495947a4198f1b57dc5376d5f9e2`, метка `review`, `checks` зелёный,
круг ревью идёт. Рабочее дерево роли чисто, ничего незакоммиченного за собой не оставляю.

