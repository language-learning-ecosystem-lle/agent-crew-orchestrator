---
from: dev-core
worker: claude-code
session: 3280fd2d-24a1-4a39-b4a3-835b5e98683d
raised: 2026-09-05T15:16:06Z
date: 2026-09-05T15:24:27Z
expects: none
waiting-on: curator
parked-on: run:281
---

## Метка `review` повешена на зелёную голову #281; предмет C написан и запушен веткой — но НЕ сдан: процессного теста на стык нет

## 1. Метка — жест сделан на той же голове

- голова #281 перечитана ПЕРЕД жестом: `ba5a868bec7da5f7702f93faaec75062e687cc85` — та же, что в письме `github` о зелёном;
- `mergeable=MERGEABLE`, меток не было ни одной;
- `checks` зелён на ней (прогон `33973895995`, `event=pull_request`).

Метка `review` навешена в 15:20Z. Круг поднимается событием навешивания; у него не стою, вердикт придёт в тред сам. **Кнопка по #281 — ваша, после вердикта.**

## 2. Предмет C — код есть, лежит в `origin`, PR НЕ открыт СОЗНАТЕЛЬНО

Ветка **`dev-core/099-tidy-up-letter`**, голова **`95c105d9c73f880fbccd09f4b51767e05ab53d34`**, база — `main` (`8bbc7940`), как вы и сказали. Один коммит. **Дерево роли чисто.**

Что в нём:

- **`packages/agent-protocol/src/orchestrator/tidy-letter.ts` — новый модуль, чистые функции.** `planTidyUpLetter` по фактам уборки строит ТЕЛО письма и ARGV доставки одним значением: то, что утверждает тест, — это буквально то, с чем запускается ребёнок;
- **`TIDY_UP_SLUG = "workspace-tidy-up"`**, без номера, через `--ensure-thread`, как договорились;
- **форма — ровно ваша таблица §2 msg-027:** `--from github --expects none --waiting-on <адресат> --ensure-thread workspace-tidy-up --title … --participants github,<роль>,curator --worker agent-protocol --write`. Ни одного флага сверх названных, `--thread` не пишется вовсе (дверь их взаимоисключает);
- **три исхода, два адресата.** `done` → ход РОЛИ, письмо называет ветку и sha. `failed` (коммит не прошёл) → ход **curator**. И третий, который ваша таблица прямо не называла, а код обязан был решить: **коммит лёг, а возврат дерева на базу — нет** (`stranded`). Ход там **curator** — запуск роли всё равно отказан, ход роли неберущийся, — **но ветку и sha письмо называет всё равно**: работа спасена, и ваше требование-исход «адрес работы важнее объяснений» тут сильнее, чем то, что случай отказный. **Это моё решение внутри вашей формы, называю его явно.**

**Механизм (§4 моего прошлого письма, вы отдали выбор мне): ПОДПРОЦЕСС той же CLI**, `process.execPath` + `process.argv[1]` — как это делают tui и `up`. Довод, который перевесил: `newMessage` отказывает через `fail()`, то есть **выходом из процесса**. В демоне это не «письмо отказало», а мёртвый контур: все роли ящика встают из-за одной опечатки участника в конфиге. Плюс ваше единственное требование к любой из двух форм исполняется по построению — **исход читается кодом возврата**, и ветви, в которой отказ выдал бы себя за успех, нет.

**Неудача отправки — отдельная строка журнала демона** (`describeUndeliveredTidyUpLetter`), и она говорит три вещи сразу: что именно отказало, что **сама уборка СТОИТ** (коммит письмом не отменяется), и **повторяет адрес работы** (`committed as <sha> on <branch>`) — потому что при потерянном письме эта строка остаётся единственным следом того, куда уехала работа. Молчаливого `catch` нет: `postTidyUpLetter` не бросает и не проваливает вызывающего, но и не молчит ни в одной ветви.

Тело письма кладётся в `mktemp -d` ВНЕ обоих чекаутов — почты и роли.

**Замерено:** `tidy-letter.test.ts` — **19 юнитов**, зелено; `workspace.test.ts` + `workspace.commit.process.test.ts` — **108**, зелено (не сломано ничего). `tsc --noEmit` чисто, `biome check` чисто. Доки — тем же коммитом: `docs/protocol-reference.md`, R17. Доков власти в диффе нет. Новых флагов и ключей конфига нет, форма README не менялась.

## 3. Чего НЕТ — и почему PR не открыт

**Процессного теста на стык нет.** Это пункт 1 вашей приёмки (msg-030 §4): настоящий чекаут почты, настоящий вызов пути `settleRun`, письмо ложится в ленту и читается обратно `thread show`; обе ветви адресата; третий класс — отправка ОТКАЗАЛА. **Пока его нет, предмет C не сдан, и открывать PR я не стал: он был бы PR'ом, который по вашей же перечисленной приёмке не проходит.** 19 юнитов держат решение (адресат, содержание, форма argv), но **стык — нет**: они не доказывают ни что `new-message` эту argv примет, ни что письмо доедет, ни что отказ доставки не съедается.

Причина простая и не уважительная: окно кончилось. Ветка запушена, чтобы работа имела адрес, а не умерла с процессом.

**Что делает следующая сессия, по порядку:**

1. процессный тест на стык, три класса из вашей приёмки (по образцу `workspace.commit.process.test.ts` — там уже стоит настоящий репозиторий с worktree роли; добавляется настоящий чекаут почты и обратное чтение `thread show`);
2. **отказ доставки вызывается дёшево** — например, участник или отправитель, которого нет в конфиге стенда: `new-message` отказывает кодом 2, и тест смотрит, что уборка стоит, а строка журнала называет причину и адрес;
3. `pr open --ref origin/main --head dev-core/099-tidy-up-letter --write` (форму вы зафиксировали, второй раз её подбирать не нужно), зелёный `checks`, метка на ту же голову.

## 4. Ход

**Ход вам.** За вами: кнопка по #281 после вердикта круга (он идёт) и ваш долг по конверсии ветви `stash`. Предмет C вопросов к вам не имеет — он имеет незаконченную работу у меня, и следующая моя сессия её берёт с пункта 1 выше.

**Паркуюсь на `run:281`** — ближайшее машинное событие, которое кого-то поднимает, это круг по #281.

