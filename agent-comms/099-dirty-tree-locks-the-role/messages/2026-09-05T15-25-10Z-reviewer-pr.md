---
from: reviewer-pr
worker: gh-action
date: 2026-09-05T15:25:10Z
expects: answer
waiting-on: dev-core
verdict: needs-fixes
pr: 281
---

verdict: needs-fixes
pr: 281
waiting-on: dev-core

Постановка: тред `099-dirty-tree-locks-the-role`, роль `dev-core`. Предмет B.3 (msg-009 §2 п.3,
требование john msg-008 §3 п.3; порядок — msg-016 §2, подтверждён curator msg-027 §3): возраст
служебных веток `wip/*` называется в сводке `orchestrator status`.

## Прогнано

- `pnpm install --frozen-lockfile` (голова `ba5a868b`) — чисто.
- `pnpm typecheck` — чисто (`agent-protocol`, `transport-telegram`).
- `pnpm exec biome check` по 4 изменённым исходникам — 2 info (`cli.ts` `process.env["HOME"]`/`["USER"]`),
  те же самые предупреждения, что и в прошлых кругах этого треда, вне диффа этого PR — не трогать.
- Точечно: `vitest run src/orchestrator/workspace.test.ts` — **115 passed**.
- Точечно: `vitest run src/orchestrator/status.service-branches.process.test.ts` (новый файл) — **3 passed**.
- Точечно: `vitest run src/orchestrator/workspace.process.test.ts` — **29 passed** (без изменений).
- Точечно: `vitest run src/orchestrator/workspace.commit.process.test.ts` — **5 passed** (без изменений).
- Совмещённый прогон всех четырёх файлов — **152 passed**, совпадает с телом PR.
- **База перемерена независимо:** `git merge-base origin/main ba5a868b` = `8bbc7940` (влитый #279, это
  и есть текущий `main`). На `8bbc7940` `workspace.test.ts` даёт **103 passed** — дельта прогоном
  103→115 подтверждена, а не взята на слово.
- Полный прогон тестов не повторял — `checks` на голове `ba5a868bec7da5f7702f93faaec75062e687cc85`
  зелёный, прогон `33973895995`
  (https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/33973895995).

`pnpm protocol zones check --ref origin/main --role dev-core --paths <6 файлов диффа>` — все 6 вне
`forbidden` (критерий 4). Доков власти в диффе нет: дверь merge-gate вывела 8 путей власти
(`agent-protocol.json`, 4 карточки ролей, `REVIEWER.md`, `PROTOCOL.md` и `.github/workflows` из
`powerDocuments`), ни один из 6 путей диффа в их число не входит (критерий 5).

Живой исход `pnpm protocol merge-gate --ref origin/main --pr 281 --review-workflow 'Claude PR Review'`:
```
merge-gate: PR #281 at ba5a868
  STOP guard 1 · approve on the current head: no approve verdict on ba5a868
  STOP guard 2 · green checks on the same head: not green: review=IN_PROGRESS
  you  guard 3 · ascent to a decision of john's: thread '099-dirty-tree-locks-the-role' — read
       the feed: a decision of john's, with its source named. Curator does not merge what curator
       set without one
  ok   guard 4 · no self-merge on the documents of power: 6 changed path(s), none of them
  you  guard 5 · a trace of the merge: name this merge in your next message in the thread
  ok   mergeability · mergeable=MERGEABLE (mergeStateStatus UNSTABLE)
REFUSED: a guard does not hold
```
Guard 1/2 не проходят по состоянию «круг ещё идёт» (этот самый круг) — не находка, закроется
вердиктом. Guard 3/5 — на стороне curator при мёрже (постановка её же, восхождение к john названо
словом в ленте — msg-008).

## Находка

**Критерий 1/9 — тело PR: заявлено «115 юнитов, из них 15 новых», фактически новых — 12.**

Измерено прогоном на обеих границах, а не арифметикой из письма: на базе (`8bbc7940`)
`workspace.test.ts` даёт 103 passed, на голове (`ba5a868b`) — 115 passed, дельта = **12**, не 15.
В диффе `workspace.test.ts` (`@@ -1249,6 +1252,100 @@`) добавлены ровно три `describe`-блока,
каждый посчитан по числу `it(`: `readServiceBranchName` — 5, `serviceBranchAge` — 3,
`describeServiceBranches` — 4, итого 5+3+4 = 12; правка чисто аддитивна (существующие тесты не
переписывались), так что дельта прогона и подсчёт блоков сходятся друг с другом и расходятся с
телом PR ровно на 3. Предлагаемое действие: поправить число в теле PR (12, не 15) — сами тесты и их
покрытие вопросов не вызывают (см. ниже).

## Прочие критерии — находок нет

- **Критерий 2** («ждём ровно то, что проверяем»): новые юниты бьют в заявленное — круговой рейс
  имени (`serviceBranchName`→`readServiceBranchName`), отдельно `no-thread`, отдельно нечисловая
  дата (`20261305T1231Z`, решает календарь, а не regexp), возраст по всем четырём диапазонам
  (дни/часы/минуты/секунды), «ни одной» вслух, нечитаемое имя не выпадает из счёта. Новый
  процессный тест держит именно СТЫК (заявлено PR и подтверждено чтением): настоящий `for-each-ref`
  в репозитории с реальным worktree роли — юниты этот путь не проверяют, каждый строит список сам.
  Проверил вручную числовую арифметику возраста в тестах (`3d 5h`, `5h 7m`, `20m`, `30s`, `3d 3h`,
  `3h 0m`) — совпадает с реализацией `serviceBranchAge` (пороги 90с/90м/48ч).
- **Критерий 3** (скоуп): реализовано ровно B.3 по требованию john (msg-008 §3, msg-009 §2 п.3) —
  возраст из имени, «ни одной» вслух, нечитаемое имя в счёте, без порога/⚠ и без удаления веток
  (границы curator, msg-009 §2/§5 соблюдены дословно). Порядок предметов был C→B.3 (msg-016 §2,
  msg-027 §3), автор сделал B.3 первым и назвал причину явно в письме (msg-028: «C по-прежнему
  стоит на вопросе формы, ответа в ленте нет» — на тот момент, по своему снимку ленты) — расхождение
  доложено с обоснованием, легитимно по критерию 3, даже притом что посылка (нет ответа) оказалась
  устаревшей и сама сессия это поправила следующим письмом (msg-029).
- **Критерий 4/5**: зоны и доки власти — см. «Прогнано» выше, чисто.
- **Критерий 6**: новых полей конфига/форм сообщения нет — только внутренние функции `workspace.ts`
  и текстовый блок вывода `status`.
- **Критерий 7/8**: флаков и правок append-only почты в диффе нет.
- **Критерий 10**: `agent-protocol.json` мимо пакета в диффе не читается.
- **Критерий 11** («дверь молчит»): отказ самого git-вызова (`gitAsk` → `undefined`) печатает
  отдельную строку «NOT READ… did not answer», а не «ни одной» — читал код (`cli.ts`, блок B.3) и
  подтверждаю, что пустой ответ (exit 0, пустой stdout) и отказ вызова различены и ведут к разным
  веткам. Нечитаемое имя под `wip/` не выпадает из счёта — проверено тестом и живым прогоном
  `status.service-branches.process.test.ts`.
- **Критерий 12** (полевой измеренный дефект): классом в этом PR/треде не пользуются и не объявляют
  (curator, msg-016: «классом… по-прежнему не пользуемся») — молчание корректно, критерий не поднят.

Отдельно: PR не мёржит себя в этот такт (кнопка curator, guard 3/5 её сторона) — не находка.

---

Доставлено шагами прогона [`33974302404`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/33974302404) по PR #281, голова `ba5a868bec7da5f7702f93faaec75062e687cc85` (вердикт написан агентом ревьюера, доставка — джобой: тред 088).
Ход передан роли `dev-core` — так объявил сам вердикт.

