---
from: dev-core
worker: claude-code
session: c3294d6b-875d-4139-8e6e-f4bbc2e26025
raised: 2026-09-05T12:11:29Z
date: 2026-09-05T12:21:22Z
expects: answer
waiting-on: curator
---

## Предмет A сделан — PR #279, голова `532c55fd`; B отвечен словами; C замерен и не начат

Пакет разрезан по вашему же разрешению: **A + имя служебной ветки — PR #279**, предмет C —
отдельно, и замер ниже говорит, почему это правильный рез.

### A — новый исход `commit` (PR #279)

Грязь прогона, ЗАВЕРШИВШЕГО СВОЙ ХОД, коммитится за роль подписью роли, дерево встаёт на базу.
**Исход назван для каждого состояния головы, и состояния взяты замером** (ветки контура:
`dev-core/128-…`, `core/gate-checks-from-actions`, `feat/042-…`, `fix/122-…`, `main`,
`release/agent-protocol-v0.2.5`), а не по памяти:

- **отделённая голова** (обычное состояние — каждый подъём ставит дерево на базовый КОММИТ) →
  служебная ветка `wip/<role>/<thread>-<YYYYMMDDTHHMM>Z`;
- **своя ветка роли** → коммит туда же;
- **базовая ветка** → отказ, «общая, коммит запрещён дословно»;
- **чужая ветка** → отказ прежним текстом.

**Одно решение я принял своей рукой и называю его отдельно, потому что оно спорное.** «Своя
ветка» доказывается ДВУМЯ фактами: имя (`<role>/…`) **или** автор головного коммита, равный
подписи роли. Второй признак обязателен: ветки контура сплошь `feat/…`/`fix/…` и роли в имени
не несут — без него право покрывало бы почти ничего. Но два признака расходятся в одной точке:
ветка названа ЧУЖОЙ ролью (`curator/017-…`), а голову подписала эта. Я решил в пользу подписи
(имя — соглашение, подпись — факт о том, кто писал) и закрепил тестом с комментарием «одна
строка на изменение». **Считаете иначе — это одна правка, скажите.**

Подпись — `GIT_AUTHOR_*`/`GIT_COMMITTER_*` в среде одного вызова (старше конфига, работает и до
записи пер-worktree подписи — она пишется ПОСЛЕ применения плана). Push best-effort, условием
уборки не является, неудача названа строкой. Уборка не прошла — отказ С ПРИЧИНОЙ (ответом гита)
поверх состава, охвата и обеих лечащих команд.

**Границы:** грязь без известного прогона пары — отказ (тест-сторож); ветвь `stash` не тронута
(тест-сторож); доков власти в диффе нет — обычный PR, кнопка ваша.

**Чего в #279 НЕТ, и это сужение доложенное, а не молчаливое:** процессных тестов на настоящем
дереве (§4). Юнитов 89 (было 72), стык — нет. Я упёрся в срок аренды: код, юниты, доки и PR
уместились, четыре процессных сценария — нет. **Это первое, что должна взять следующая сессия**,
и до них PR я считаю неготовым к merge — не вешаю `review`, чтобы круг не судил половину.

### B — конец служебной ветки: ответ на каждый из четырёх

1. **кто и когда убирает — СДЕЛАНО ТАК:** кандидат john принят целиком. Ветка живёт, пока
   содержимое не влито и не отброшено; распоряжается рука роли, увидевшей свою ветку.
   Автоматического удаления в пакете нет — ваша граница соблюдена;
2. **как видно — СДЕЛАНО:** `wip/<role>/<thread>-<YYYYMMDDTHHMM>Z` плюс push, то есть ветка
   видна не только на этой машине. `wip/` первым сегментом — чтобы все они находились одним
   `git branch --list 'wip/*'` и ни одна не читалась как ветка пакета;
3. **что если роль не пришла — НЕ СДЕЛАНО, нужен отдельный пакет.** Возраст служебных веток в
   сводке `status` — это правка другой команды и другой её половины; в этот дифф я её не брал,
   чтобы не тащить в пакет о `planWorkspace` второй предмет. Называю несделанным явно;
4. **сколько их бывает — ЗАМЕРЕНО, и ответ «хватит правила»:** в журнале демона ЭТОГО контура
   (`.orchestrator/daemon.log`, 526 КБ, с 03.09) отказов по грязному дереву **ноль** — с якорем
   по началу строки, потому что в лог попадают транскрипты сессий и греп без якоря считает
   собственную команду. Известных случаев два, оба в контуре-потребителе, оба расшиты рукой john
   (03.09 `core/121-verdict-not-a-trace`, 05.09 `core/npmrc-fail-if-no-match`). **≈2 за трое
   суток по всем контурам → механизм уборки городить рано**, правила и видимости достаточно.

### C — замер сделан, код не начат, и замер меняет форму вопроса

**Факт, а не догадка:** процесс демона (`orchestrator up --foreground --instance hetzner`,
pid 1027167, пользователь `aco-hetzner`) держит в своей среде **ноль** переменных со словом
`token` — ни `GH_TOKEN`, ни `GITHUB_TOKEN`; remote почты — HTTPS. То есть **рукой среды демон
писать в почту не может**.

**Но рука есть другая, и она уже в пакете:** дверь кредов читает
`/home/aco-hetzner/.config/agent-protocol/secrets.env`, где `GH_TOKEN` и `GITHUB_TOKEN` лежат
(это напечатала команда `pr open` в этом же такте — «token GH_TOKEN ← the environment of the
caller»). Значит писателя почты у демона можно построить на существующей двери кредов, а не на
новом секрете. **Пушем я это не мерил** — на настоящий push в `comms` из процесса демона такта
не осталось, и подавать неизмеренное как измеренное я не буду. Это первое действие предмета C.

### Где стои́т и что дальше

PR #279, голова `532c55fd9e475d8ea01a60ca573ffa72295a2e89`, метки `review` НЕТ намеренно (см.
выше — без процессных тестов пакет неполон). Рабочее дерево чисто, всё закоммичено и запушено.

Следующая сессия по этому треду, по порядку: (1) четыре процессных теста §4 на настоящем дереве;
(2) зелёный `checks` → метка `review`; (3) предмет B.3 или C — по вашему слову.

Ход отдаю curator: спорное решение о подписи против имени (A), несделанный B.3 и порядок
дальнейшего — ваши.

