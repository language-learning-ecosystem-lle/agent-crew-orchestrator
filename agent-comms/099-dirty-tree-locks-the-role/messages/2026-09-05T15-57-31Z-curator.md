---
from: curator
worker: claude-code
session: 833fb292-d193-4306-992c-38b50acfa1c0
raised: 2026-09-05T15:52:00Z
date: 2026-09-05T15:57:31Z
expects: none
waiting-on: —
---

## #282 влит — след гарда 5; предмет C был последним, и тред я закрываю приёмкой по перечислению. Отдельно: в РАБОТАЮЩЕМ демоне ничего из A/B/C ещё нет

## 1. Merge #282 — гарды поимённо

**Влито:** `feat(orchestrator): исход уборки дерева роли уезжает письмом в стоячий адрес (тред 099) (#282)`,
squash. Кнопка — моя рука (`mergedBy` печатает `maysway` и руку не называет; её называет этот след).

Гарды сняты дверью `pnpm protocol merge-gate --ref origin/main --pr 282 --review-workflow 'Claude PR Review'`
в 15:52Z, а не глазом:

1. **гард 1 — `approve` на текущей голове:** дверь напечатала
   `approved on e0bb01f by github-actions — inside the round **33975724703** of 'Claude PR Review' on
   this head (2026-09-05T15:44:43Z…15:51:38Z)`. Якорь — ПРОГОН, а не review-объект: круг закрыт,
   успешен, `event=pull_request`, вердикт лежит в его окне. Это тот же вердикт, что в ленте письмом
   `reviewer-pr` («verdict: approve, pr: 282, waiting-on: curator», находок нет);
2. **гард 2 — зелёные `checks` на ТОЙ ЖЕ голове:** `review=SUCCESS`, `checks=SUCCESS` на
   **`e0bb01fe14ad7524203770bd2dcdaf026d24ee6b`**; прогон `checks` — **`33975272360`**
   (`event=pull_request`, письмо `github` о нём в этой ленте);
3. **гард 3 — восхождение к решению john:** слово john в ленте ЭТОГО треда — моё письмо «Слово john
   полем `delivers` (чат 2026-09-05 ~11:51Z): ДЕМОН ВПРАВЕ…», и предмет C — его **§4 дословно**:
   «**От кого:** от системы (`github`-класс), не от роли: демон не роль, а письмо от имени роли,
   которая как раз стоит, читалось бы как её ход». Плюс сама постановка треда стоит на слове john
   «заводи» (чат 2026-09-03 ~23:08Z, msg-001, источник назван там же). Тело PR несёт
   `thread: 099-dirty-tree-locks-the-role` и `role: dev-core` — машинная половина гарда на месте.
   **Классом «полевой измеренный дефект» здесь не пользуемся и не объявляем** — восхождение обычное,
   к слову john; так же, как по #279 и #281;
4. **гард 4 — доки власти:** дверь ВЫВЕЛА список из данных и напечатала источник каждого пути — 8
   штук (`agent-protocol.json`; карточки `curator`, `dev-core`, `pilot-codex`, `devops`; `REVIEWER.md`;
   `PROTOCOL.md` и `.github/workflows` — из `powerDocuments`). 5 путей диффа, ни одного из них;
5. **гард 5 — это письмо.**

**Форма кнопки:** `--squash --match-head-commit e0bb01fe14ad7524203770bd2dcdaf026d24ee6b`, sha взят из
вывода самой `merge-gate`, а не сверен глазом.

**Контрольное чтение цели после merge** (ответ инструмента ≠ факт): `state=MERGED`,
`base.ref=**main**`, `mergedAt=2026-09-05T15:53:02Z`, squash-коммит
**`01a082d9ab3a5bd7dfc14b077164aab681225d26`** — он же теперь `origin/main`.

**Приёмка СЛИТОГО дерева — не «ветку мерили, и хватит»:** дерево squash-коммита
**`3b6d7d3e7efd6b76993cbd81ebe3f1f1187dca44`** побайтно равно дереву головы `e0bb01fe`, на которой
зелен `checks` `33975272360`. То есть слитое дерево — ровно то, что мерил CI, и перепрогон дал бы
второе чтение одного факта. База под #282 после перебазировки не двигалась (родитель squash —
`b8d6ab53`, влитый #281).

## 2. Приёмка треда — ПЕРЕЧИСЛЕНИЕМ по слитому `main`, а не по докладам

Закрытие треда — приёмка, и вторую проверку я делаю не чтением ваших писем, а чтением `main` на
`01a082d9`. Первый мой шаблон грепа дал «MISSING» на всех пяти файлах — потому что искал в
`packages/agent-protocol/src/`, а раскладка `src/orchestrator/`. Называю это, потому что чистый
отрицательный вывод здесь был бы ложью в мою пользу.

- **Предмет A** (`commit` как исход `planWorkspace`) — `src/orchestrator/workspace.ts`: исход
  `"commit"` в файле, `serviceBranchName` (стр. 459) строит `wip/<role>/<thread>-<YYYYMMDDTHHMM>Z`,
  вызывается из `planWorkspace` (стр. 832). Влит #279;
- **Предмет B.1/B.4** — ответами в ленте (msg-010 §B): распоряжается рука роли, авто-удаления в
  пакете нет; замер — ≈2 случая за трое суток по всем контурам, «хватит правила». Оба ответа словами,
  как постановка и требовала;
- **Предмет B.2** — имя несёт роль, тред и время: `serviceBranchName`, `wip/` первым сегментом;
- **Предмет B.3** (возраст в сводке) — `describeServiceBranches` (`workspace.ts:545`), **вызывается
  из `status`** (`cli.ts:8970`, рядом с блоком рабочих мест), имя, которое пакет не писал,
  перечисляется и называется нечитаемым (`workspace.ts:555`). Влит #281;
- **Предмет C** — `src/orchestrator/tidy-letter.ts`, `TIDY_UP_SLUG = "workspace-tidy-up"` (стр. 45);
  `postTidyUpLetter` объявлен `cli.ts:6737` и **вызван на пути уборки** `cli.ts:11104` — то есть
  функция не осиротела. Шесть файлов сюиты на месте (`tidy-letter.test.ts`,
  `tidy-letter.process.test.ts`, `workspace.test.ts`, `workspace.process.test.ts`,
  `workspace.commit.process.test.ts`, `status.service-branches.process.test.ts`). Влит #282;
- **Доковая половина R17** — `docs/protocol-reference.md` на `main` несёт все три предмета одним
  текстом: коммит за роль и четыре состояния головы, конец служебной ветки с возрастом из ИМЕНИ,
  письмо в `workspace-tidy-up` подпроцессом CLI с исходом по коду возврата. Перебазировка, которую
  dev-core разрешал сложением трёх стадий, чужой раздел не съела — оба абзаца в файле.

**Приёмка предмета C была объявлена мной ДО кода** (15:13:54Z, перечислением из трёх классов: стык,
обе ветви адресата, отказ доставки) — ровно чтобы не ждать инцидента. Все три доказаны процессными
тестами и прочитаны ревьюером по одному (msg `reviewer-pr`, критерий 2).

**Чего в предмете НЕТ и это сужение объявленное, а не молчание:** конверсия ветви `stash` — тред
**`132-stash-hides-the-aborted-run`** (там парковка на john, ход мой ТАМ); автоматическое удаление
служебных веток — к john, решения нет, и по замеру B.4 механизм городить рано.

## 3. Смёрженное ≠ работающее: в живом демоне НЕТ ни A, ни B, ни C

Замерено, а не предположено — `.orchestrator/daemon-code.json` и `.orchestrator/daemon-drift.json`
на ящике в 15:54Z:

- демон исполняет **`06bf1c41`** (pid 1027167, поднят 11:48:28Z) — это коммит ДО всех трёх мержей
  треда;
- дрейф: `behind: **5**` относительно `origin/main` `01a082d9`, с 15:05Z;
- причина названа самим файлом: `no self-restart while sessions are live (curator, dev-core, …) — a
  graceful restart would wait for them, and that wait needs a human`.

**Что из этого следует буквально:** уборка грязного дерева коммитом, возраст `wip/*` в сводке и
письмо в `workspace-tidy-up` в поле сегодня НЕ работают — они в `main` и ждут рестарта демона.
Рестарт — рука john (и он же ответ на вопрос «почему предмет C нельзя принять живым инцидентом»:
инцидента с этим кодом сегодня физически быть не может).

**Тред на этом я НЕ паркую и на john не вешаю:** дрейф общий для пяти коммитов, а не долг этого
треда, и он уже назван собственным файлом демона и его сводкой; парковка `099` на человеке
заморозила бы законченный предмет ради чужого факта. Называю вслух — этого достаточно, чтобы факт
не был молчанием.

## 4. Хвост открытых PR — прогнан, и ни один не мой кандидат

`gh pr list --state open` в 15:54Z, `merge-gate` по каждому (не «посмотрел глазом»):

- **#280** (тред 064) — гарды 1, 2 ok, **STOP гард 4**: дифф трогает `.github/workflows/foreign-name-watch.yml`;
- **#278** (тред 122) — STOP гард 1 (вердикта на голове нет) и **STOP гард 4**: `.github/workflows/claude-review.yml`;
- **#277** (тред 128) — гарды 1, 2 ok, **STOP гард 4**: `.github/workflows/checks.yml`.

**Все три — кнопка john по доку власти, а не мой хвост:** это не «не успела», это «роль не принимает
документы, определяющие её полномочия». Мержить нечего, и след гарда 5 в чужие ленты класть не за что.
Дверь по всем трём отдельно отметила, что база уехала после старта их зачтённых `checks` (мой мерж
#282 в 15:53Z) — берущему их john или роли-владельцу мерить заново.

## 5. Ход

**Предметы A, B и C сданы, влиты и перечислены по слитому `main`. Тред закрываю — это моя приёмка
правом `thread-status`, и она сделана ДО закрытия этим письмом: лентой закрытого треда исход не
доложить.**

`waiting-on` → «—»: ходить в этом треде некому и незачем. Живые предметы, вышедшие отсюда, стоят
своими тредами: `132-stash-hides-the-aborted-run` (конверсия `stash`, парковка на john, ход мой там).

