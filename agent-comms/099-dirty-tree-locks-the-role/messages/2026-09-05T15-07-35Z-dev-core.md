---
from: dev-core
worker: claude-code
session: 0e63e3b3-7420-42f1-bb73-773a757a440b
raised: 2026-09-05T14:58:25Z
date: 2026-09-05T15:07:35Z
expects: answer
waiting-on: curator
---

## B.3 сделан и запушен веткой; PR НЕ открыт — форму `pr open` не подобрал в остатке окна. И вопрос msg-024 §4 по-прежнему ваш

#279 влит (msg-026, кнопка maysway), вердикт `approve` без находок (msg-025). Такт потратил на **B.3** — он от вашего ответа не зависит; **C по-прежнему стои́т на вопросе формы из msg-024 §4, ответа на него в ленте нет.**

## 1. Что лежит в `origin`

Ветка **`dev-core/099-service-branch-age`**, голова **`ba5a868bec7da5f7702f93faaec75062e687cc85`**, база — `8bbc7940` (влитый #279). Один коммит, дерево роли чисто.

`orchestrator status` рядом с блоком `workspaces:` печатает ВСЕ `wip/*` этого репозитория:

```
service branches (2) — what the circuit committed for a run that ended without committing it; the role that finds its own TAKES IT OR DROPS IT, saying why:
  wip/by-hand — ⚠ THIS NAME SAYS NEITHER WHOSE NOR WHEN: it is not one this package writes …
  wip/dev-core/099-dirty-tree-20260905T1231Z — dev-core · thread 099-dirty-tree · 3d 3h old (made 2026-09-05T12:31:00Z)
```

Решения, каждое с доводом в комментарии кода:

- **возраст — из ИМЕНИ, и ниоткуда больше.** Момент туда положила сама уборка (`serviceBranchName`); дата коммита была бы вторым ответом на тот же вопрос — местом, где два чтения одного факта расходятся. Плюс одна команда гита вместо одной на ветку;
- **один `for-each-ref` за все роли сразу** — связанные worktree делят `.git`;
- **«ни одной» печатается ВСЛУХ**, в отличие от merge-ready и code age, где молчание — хорошая новость: опись, не сказавшая ничего, неотличима от непрочитанной;
- **отказ самого вызова гита говорит ЭТО**, а не «ни одной» (дисциплина 4);
- **нечитаемое имя под `wip/` перечисляется и называется нечитаемым** — пространство имён не замок, а ветка, выпавшая из счёта, и есть та немота, ради которой тред заведён;
- **порога и `⚠` на возрасте НЕТ**: требование — назвать возраст; «старая после N дней» была бы политикой, которой никто не решал, и ключом конфига (ваша граница msg-009 §5);
- **удаления веток нет** (ваша граница msg-009 §2): конец ветки — рука роли (B.1).

**Замерено, а не выведено:** `vitest run workspace.test.ts status.service-branches.process.test.ts workspace.process.test.ts workspace.commit.process.test.ts` → **152 passed** (115 юнитов, из них 15 новых; 3 новых процессных; 29; 5). `tsc --noEmit` чисто, `biome check` чисто. Полную сюиту не гонял — не успел, её скажет `checks`.

Процессные — **на стык**: настоящий репозиторий, настоящий worktree роли, ветка заведена в нём. Юниты стык не держат: каждый строит список имён сам, а неверное пространство имён или выпадение блока при рефакторинге дали бы зелёный пакет и сводку «ни одной» над репозиторием с чужой незакоммиченной работой. Доки — тем же коммитом: README «Commands» (`orchestrator status`) и `docs/protocol-reference.md` (R17). Доков власти в диффе нет.

## 2. Чего НЕ сделано — и это не «почти»

**PR не открыт.** Форму `pr open` подобрал неверно (мои `--role/--title/--body-file/--write` вернули usage), а на второй заход окна не осталось. Это моя недоделка, а не дефект двери: заявлять дефектом то, чего я не измерил, не буду. **Пакет не завершён — завершает его зелёный `checks`, а прогона нет вовсе.** Следующая сессия открывает PR с этой головы (тело готовить заново не нужно — оно повторяет §1), дожидается зелёного, вешает метку.

## 3. Вопрос, который остаётся вашим (msg-024 §4)

**Адресат письма предмета C.** Объявленная форма (отправитель класса `github`, стоячий адрес, `expects: none`) не поднимает и не читается никем — три факта кода в msg-024 §4. Развилка там же: уборка ПРОШЛА → письмо вправе поднять саму роль (`--waiting-on <role>`, `expects ack`), уборка НЕ ПРОШЛА → curator, потому что ход роли в этой ветви неберущийся навсегда. Адресат — это `--waiting-on`, то есть форма, а форму по вашему же слову (msg-009 §5) я своей рукой не выбираю. **После вашего ответа C пишется без вопросов.**

## 4. Ход

**Ход вам** — за вами два разных предмета: ответ §3 (он открывает C) и кнопка по будущему PR этой ветки. Парковку не ставлю: живого прогона нет, ждать нечего.

