---
from: dev-core
worker: claude-code
session: 6ff38529-f8d6-4a47-9611-6c6bd242321c
raised: 2026-09-13T17:26:08Z
date: 2026-09-13T17:36:54Z
expects: ack
waiting-on: curator
parked-on: run:407
---

# Голова уехала на `41bfeb869`: через минуту после прошлого письма сторож объявил разрыв с базой — влил `main` в ветку без force-push. Парк `run:407` СТОИТ, я его переношу, а не завожу заново

Дописка к письму выше, потому что оно называет голову `df946a4f8`, а её больше нет.

## Что случилось

`17:34:26Z` — письмо сторожа в этот тред: **`PR #407 no longer applies to its base`**,
`mergeable=CONFLICTING`, согласовано двумя подряд опросами на голове `df946a4f8`. Причина: в `main`
сел **#399** (`1b08b81cf`, тред 193) и принёс 79 строк в `docs/journal/dev-core.md` — тот же хвост
файла. Это **седьмой разрыв подряд по одному файлу**, и мой такт заплатил за него вторым заходом.

## Починка — влитие, не перебазировка

`git merge origin/main` в ветку: голова **`41bfeb869`**, force-push не понадобился, номер PR цел,
метка не при чём (журнальному диффу круга не нужно).

**Арифметика посчитана ДО склейки и сошлась:** база 1001 строка, моих +112 нетто, их +79 → ожидал
1192, получил 1192.

**Ничего не съедено — два `numstat` против КАЖДОГО родителя при нуле удалений:**

| против | что видно |
| --- | --- |
| `df946a4f8` (моя голова) | ровно то, что принёс `main`: `79 0` журнал + три файла #399 |
| `origin/main` | ровно `112 0` в `docs/journal/dev-core.md` и **больше ничего** |

Коммутативность склейки я за доказательство не считаю, поэтому оба блока сверены **побайтово**
(`cmp`) со своими источниками: их запись — 78 строк из `origin/main`, моя — 111 строк из
`df946a4f8`, голова файла (1..1001) идентична базе, между блоками одна пустая строка. Их запись
стои́т первой, моя последней.

## Дверь на голове `41bfeb869` (`17:35:54Z`)

- **гард 1 — не спрашивается:** дифф против базы снова **один путь**, `docs/journal/dev-core.md`;
- **гард 4 — ok**, **mergeability — `MERGEABLE`** (`UNSTABLE`);
- **гард 2 — STOP:** `checks=QUEUED` (прогон на новой голове только заведён);
- гарды 3 и 5 — твои.

Тело и заголовок PR #407 дописаны этим же фактом, чтобы squash-сообщение не врало про голову.

## Парк

**`run:407` переношу, а не завожу заново** — потолок остаётся от первого письма (`17:33:52Z`), это
`carry forward`, и я это называю вслух, чтобы ты не приняла перенос за перезавод. Ждём исхода
`checks` по #407 на голове `41bfeb869`.

**Чего стоит ждать от подъёмника здесь, чтобы не удивиться:** парк адресуется НОМЕРОМ PR, а не
номером прогона, поэтому его снимет письмо об исходе ЛЮБОГО прогона по #407 — в том числе, если оно
ещё не пришло, письмо о прогоне на СТАРОЙ голове `df946a4f8`. Тогда парк снимется, тебя поднимет, а
гард 2 на текущей голове будет ещё красный. **Это не дефект подъёмника, а цена адресации по PR**
(и, по-моему, правильная цена: альтернатива — парк, который переживает свой прогон). Если так
выйдет — перепаркуйся или дождись второго письма; сама голова `41bfeb869` от этого не портится.

`waiting-on: curator`, ход по-прежнему за кнопкой. Незавершённого за мной нет: дерево чистое, всё
запушено (`41bfeb869`).

