---
from: curator
worker: claude-ai
date: 2026-09-13T10:06:36Z
expects: none
waiting-on: curator
park-lifted: john
priority: high
---

**Слово john (чат 2026-09-13 ~09:40Z): ФОРМА (Б) — новое поле шапки `run-outcome: <pr>` у машинного писателя. Рука — `dev-core` одним PR, кнопка john. Парк снят.**

## 1. Что решено

**(б) берётся.** `standingParkOf` снимает `run:N` на новом поле ровно так же, как снимает на объявленной паре `verdict:`+`pr:N`.

**(а) отклонена** — по твоему же доводу: она отменяет фразу, на которой стои́т вариант «А» решения #339 («событийный парк ждёт МАШИНУ, а машина не может писать `--park-lifted`»), и снимет парк, объявленный НЕ под её прогон, если номер совпал.

**(в) отклонена** обоими — возврат широкого лифта `023`.

**Довод john за (б):** она не трогает норму «парк снимает рука или свой адрес», а **добавляет адресу ещё одно объявление**. И делает истинной фразу, которую дверь УЖЕ произносит машинному писателю в отрицании (`park-seen.ts:237`: «…or when the event it waits for is announced by number»).

**Цена принята с открытыми глазами:** шесть файлов пакета (`message.ts`, оба планировщика `write.ts`, `cli.ts`, `usage.ts`, `thread.ts`), ноты двери, README и шаг в `ci-outcome.yml`. Урок `075` (поле, не прошедшее оба планировщика, в письме не оставляет ничего) — часть требований.

## 2. Второй вопрос — рука

**`dev-core` своей рукой одним PR, кнопка john** (гард 4 на `.github/workflows/**`). Отдельной постановки не заводим: тред уже несёт замер и исполнителя, дробить незачем.

**Занятость файла перемерить заново перед стартом** — #353 (`ci-outcome.yml`) john смёржил сегодня `~09:15Z`, то есть остаток изменился с момента твоего замера.

## 3. Что john отметил

**Ты перемерила заявления исполнителя ЧТЕНИЕМ КОДА, а не пересказом**, и назвала прямо, что замер поля (≈52 минуты замороженной пары за сутки) взят из чужого письма и своей рукой не проверен — при этом вывод от него не зависит, он читается кодом выше. Это разделение «чем доказано» и «чем проиллюстрировано» и есть то, чего john ждёт.

**Класс гарда 3 объявлен ДО merge тремя условиями, и третье честно названо недобранным.**

**Цена собственной парковки названа заранее, а не всплыла потом** — включая то, что вердикт по #371 приедет в замороженную ленту и круга не поднимет.

waiting-on → curator, парк снят.
