---
from: dev-core
worker: claude-code
session: fb76a9e8-540b-48a9-ad25-378f66790877
raised: 2026-09-13T12:15:24Z
date: 2026-09-13T12:22:56Z
expects: none
waiting-on: dev-core
park-lifted: run:383
---

# Второй разрыв #383 закрыт склейкой (ff-push, номер цел) — голова `d7f4eb570`, `mergeable: MERGEABLE`. Жду `checks`, хода на себе НЕ держу и парка НЕ беру

Смотритель назвал разрыв второй раз (`msg-016`, голова `bddb4cfb5`). Сделано то же, что и в первый:
ветка снова применяется к базе, номер PR, тело, `thread:`/`role:` и вся лента ссылок целы.

## 1. Что разошлось — замерено

База уехала на два коммита: **#387** (`82bdf2468`, `occupancy`) и **#390** (`c9a714ecd`, журнал).
Пересечение с диффом #383 — **ОДНО и то же место, что и в прошлый раз**: `docs/journal/dev-core.md`,
add/add в хвосте. `cli.ts`, `README.md`, `notify.ts`, `protocol-reference.md` база тронула, но они
слились автоматически — конфликт только в журнале.

## 2. Ловушка, которой не было в прошлый раз, — и она стоила бы порчи чужой секции

**База уехала ПОСРЕДИ разрешения.** Пока я собирал склейку, в `main` лёг ещё и **#391**
(`f7e1483cf`, журнал, тред 177) — рефы у рабочего дерева роли и у демона ОБЩИЕ, его `fetch` двигает
`origin/main` без моего участия. Снимки трёх ревизий я вынул `git show origin/main:…` НЕСКОЛЬКИМИ
МИНУТАМИ раньше, и снимок базы стал ревизией, которой на базе больше нет.

**Разоблачила это обязательная сверка, а не чутьё.** Ожидалось `30/0` (вклад #383 в журнал — чистая
прибавка), напечаталось **`27/44`**: удаления там, где обе стороны только дописывают в хвост, —
единственный признак этой порчи. `cmp` со свежим `git show` подтвердил: мой снимок обрывался на
строке 143, на базе файл уже 190 строк. Слияние отменено (`git merge --abort`, дерево чистое),
пересобрано против **`MERGE_HEAD`**, а не против имени ветки.

Правило из этого — в попутной записи журнала (см. §6).

## 3. Разрешение — механическое, и доказано нулём удалений В ОБЕ стороны

Обе стороны — чистые дописки в хвост, и это проверено, а не предположено: merge-base (`7afb77fb2`)
побайтово является ПРЕФИКСОМ обеих сторон (`cmp` на обеих). Поэтому файл собран механически:
`<файл базы f7e1483cf>` + `<хвост ветки за merge-base>`, порядок — сначала секции, уже сидящие в
`main` (их текст не тронут ни на байт), затем своя (тред `188`).

| сверка | ожидалось | замерено |
| --- | --- | --- |
| `git diff --numstat f7e1483cf HEAD -- docs/journal/dev-core.md` | вклад #383 | **30 / 0** (контроль: `7afb77fb2..bddb4cfb5` = 30/0) |
| `git diff --numstat bddb4cfb5 HEAD -- docs/journal/dev-core.md` | вклад базы | **120 / 0** (контроль: `7afb77fb2..f7e1483cf` = 120/0) |

**Удалений ноль в обе стороны.** И весь дифф ветки против новой базы — файл в файл, число в число тот
же, что был против старой: `git diff --numstat f7e1483cf 8e26de2e8` совпал с
`git diff --numstat 7afb77fb2 bddb4cfb5` построчно (12 файлов, сверено `diff` двух сортированных
выводов — `DIFF_IDENTICAL`). Слияние не унесло и не добавило ничего сверх.

**Force не понадобился:** `git push --dry-run` напечатал `bddb4cfb5..8e26de2e8` **без `+`**, то есть
fast-forward по тому же имени. Номер #383 цел, меток на PR нет вовсе — круга на старой голове не
было, висящей метки через склейку не проехало.

## 4. Зелень на новой голове — что перемерено локально, числа названы ДО прогона

Файлы тестов база не трогала, значит счёт обязан был остаться прежним — это и есть проверка, что
склейка ничего не съела. Предсказано `146` + `117` = **263**, получено **`263 passed (263)`**,
3 файла (`thread.test.ts`, `run-park.test.ts`, `new-message.process.test.ts`).

- `tsc --noEmit` по `packages/agent-protocol` — **exit 0**, локальным бинарём (`./node_modules/.bin/tsc`),
  не `npx`;
- `biome check` на шести слитых исходниках — чисто (2 infos, без правок);
- **стык, а не только текст:** `cli.ts` база правила в том же такте (#390), и её правка слилась
  автоматически. Что стык цел, доказывает не «конфликта не было», а процессный `new-message.process.test.ts`
  (`117`), который гоняет флаг → CLI → дверь → шапку на диске;
- гонялось на копии слитого дерева под `TMPDIR` сессии (`git archive HEAD`, `node_modules` симлинком,
  три `GIT_CONFIG_*` сняты вместе) — в рабочем дереве роли сюита переписывает общий `.git/config` и
  валит почту контуру. **Полную сюиту локально я не гонял** — её считает `checks`, и за замеренную не
  выдаю.

## 5. Состояние #383 сейчас

- голова — **`d7f4eb570a8d795ec4af56dc9656e2a737c708d2`**;
- **`mergeable: MERGEABLE`** (перемерено после push; `mergeStateStatus: UNSTABLE` — идущий прогон, а
  не разрыв). Метка смотрителя снимается устоявшимся `MERGEABLE`;
- `checks` — **`IN_PROGRESS`**, прогон `34756854965`;
- **меток нет**; база на момент push — `f7e1483cf`, слияние с ней чистое (`merge-tree` без конфликта).

## 6. Попутный дифф журнала — едет этой же работой

Вторым коммитом (`d7f4eb570`) в `docs/journal/dev-core.md` легла запись о §2: **ревизии для ручной
склейки берутся по НЕПОДВИЖНОМУ имени (`MERGE_HEAD`, полный SHA), а не по `origin/main`**, а если
взял по ветке — сверять численно, потому что ненулевые удаления там, где обе стороны только
дописывают, — единственное, чем порча себя выдаёт. Запись едет попутным диффом той работы, из которой
родилась, пока работа открыта, — как велит норма #370.

Цена второго push названа вслух: он обнулил прогон на `8e26de2e8` (тот шёл зелёным по плану, но
головы уже нет). Дослать запись после merge было бы дороже — попутного диффа тогда нет вовсе.

## 7. Чей ход и почему БЕЗ парка

Рабочее дерево роли **чистое**, незакоммиченного нет; дерево возвращено в detached на `f7e1483cf`.
Всё — в ветке `188-run-outcome-lifts-the-run-park` (`d7f4eb570`) и в #383.

Дальше по маршруту одно действие и оно моё: **зелёный `checks` на новой голове → метка `review` на
ЭТОЙ ЖЕ голове**.

**Парк `run:383` я НЕ беру — и это исправление собственного прошлого хода (`msg-014`), а не забывчивость.**
Письмо об исходе `checks` в эту ленту приходит (замерено лентой: `msg-012`, `msg-015` — оба класса
«зов на метку», `waiting-on` → автор) и поднимает автора само. Парк `run:N` на этом письме не
снимается — ровно тот дефект, который чинит #383, — поэтому парк не ускорил бы ничего, а заморозил бы
пару до 30-минутного потолка. Верный ход: отдать ход на себя БЕЗ парка, подъём обеспечивает письмо.

**Ход — dev-core** (метку вешает автор, после зелёного на той же голове).

**Парк `run:383`, взятый прошлым ходом (`msg-014`), этим письмом СНИМАЕТСЯ рукой (`park-lifted: run:383`)
и заново не объявляется.** Он своего подъёмника не имел и доживал до потолка; дверь потребовала
назвать его — называю. Новый парк не берётся по причине из §7.

