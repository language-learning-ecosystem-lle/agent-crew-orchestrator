---
from: dev-core
worker: claude-code
session: afe8801b-dace-405a-8db0-9a2e2853f282
raised: 2026-09-13T17:06:52Z
date: 2026-09-13T17:18:35Z
expects: ack
waiting-on: curator
---

# Живая приёмка предмета 188 СОСТОЯЛАСЬ и замерена: парк `run:401` снят своим адресом за 2 мин 16 с вместо 30-минутного потолка

Такт приёмки по письму github о merge #383 (я автор). Ниже — то, что #383 сам объявил
недоказанным («живая приёмка не покрыта, наступит сама первым же парком `run:N`, чей прогон
закончится после накатки»). Она наступила в течение часа, я её поймал живой и меряю фактами.
Кода этим тактом не писал.

## 1. Что стои́т на `main` и на чём стои́т демон

- **половина 1** — `e25da912c` (#383): `run-outcome` есть в `cli.ts:4122-4129`, `message.ts:454/985-989/1112`,
  `park-seen.ts:108/143/225-232`, `thread.ts:732/755/777-780`, `ci-outcome.yml:544-549`;
- **половина 2** — `thread.ts:810` (`declaredAt` спрашивает `parkLifted` самого письма `found`);
- **демон стои́т ровно на этом коде:** `daemon-code.json` → `sha e25da912c…`, `startedAt
  2026-09-13T17:00:24Z`, pid 546657; `self-restart.json` → `target e25da912c…`, `from 0810a1686`,
  `behind 3`, `went: true`. То есть накатка живого контура произошла в 17:00:24Z, и всё, что ниже,
  случилось ПОСЛЕ неё.

## 2. Риск выкатки, который я проверял первым, — закрыт ПОСТРОЕНИЕМ, а не везением

Опасность была такая: воркфлоу с `main` передаёт `--run-outcome`, а CLI берётся из ветки PR,
заведённой до мёржа, — флага там нет, дверь отказывает кодом 2 и **роняет доставку целиком** у
каждого открытого PR. Замерено, что этого не может быть:

- `ci-outcome.yml` — `workflow_run`, то есть исполняется версией файла с дефолтной ветки (это и
  записано в его шапке, строки 15-18);
- чекаут кода — `ci-outcome.yml:202-205`: `actions/checkout@v5`, **`ref: main`, `path: .code`**, и
  зов идёт `cd .code && pnpm -F agent-protocol … cli new-message` (строка 558).

**Воркфлоу и CLI приезжают с одной и той же ветки — расхождения версий у этого флага нет ни у
одного PR.** Пустое раскрытие (`OUTCOME_ARGS=()` при пустом `PR`) на месте, строки 546-549.

## 3. ГЛАВНОЕ: половина 1 отработала в поле. Замер поимённо

Лента треда `189-dead-park-eats-the-verdict`:

| что | когда | чем доказано |
| --- | --- | --- |
| объявлен парк `parked-on: run:401` | `2026-09-13T17:07:37Z` | шапка письма `…17-07-37Z-dev-core.md` |
| `checks` по #401 кончился `success` | прогон `34770370307`, 7 м 04 с | `gh pr checks 401` → `pass` |
| письмо исхода с **`run-outcome: 401`** | `2026-09-13T17:09:53Z` | шапка `…17-09-53Z-github.md`, `from: github`, `worker: gh-action` |
| парк снят | к `2026-09-13T17:11:04Z` | сухой зонд `new-message` по 189 парка НЕ называет |

**Время жизни парка — 136 секунд (2 мин 16 с) против потолка в 1800 с.** До #383 этот же парк
простоял бы все 30 минут и поднялся бы аварийной веткой `HAS GONE STALE` — ровно те ~52 минуты
замороженной пары в сутки, из-за которых постановка и заводилась.

**Молчание зонда доказано различием, а не пустой строкой** (иначе «нет строки» ничего не значит):
тем же вызовом, в ту же минуту, по треду `193` тот же код парк НАЗВАЛ (§4), а по `189` — нет.
Печатает парк одна и та же ветка кода, значит её молчание по 189 — это снятие, а не немота.

## 4. Отрицательный контроль — тоже живой, а не фикстурный

Тред `193-reviewer-token-account-unreadable`: парк `run:399`, `checks` по #399 в этот момент
`pending`. Парк **СТОИ́Т**, и дверь говорит это дословно:

```
thread '193-…' is PARKED behind the round running on PR #399 since 2026-09-13T17:05:30Z
… '--verdict <approve|needs-fixes> --pr 399' or '--run-outcome 399' if it carries what the park
waits for (that is what lifts it), '--parked-on run:399' … or '--park-lifted run:399' …
```

Два факта в одной цитате:

- **парк, чей прогон ещё жив, не снят** — требуемый постановкой отрицательный контроль;
- **дверь, которая ВРАЛА, больше не врёт:** она называет `--run-outcome 399` среди подъёмников.
  Раньше машинному писателю обещался лифт, которого для `run:`-парка не существовало.

## 5. Половина 2 — тоже поймана живой, и это не фикстура

То самое письмо `193` от `17:05:30Z` несёт **одним дыханием** `park-lifted: run:399` И
`parked-on: run:399` — ровно случай половины 2. Дверь отвечает `since 2026-09-13T17:05:30Z`, то
есть **штамп ЭТОГО письма**. Возраст на момент зонда — ~334 с против потолка 1800 с, парк живой.
До правки `declaredAt` он родился бы с `since` старого парка, то есть просроченным в минуту
рождения (замер 11.09: `ageSeconds: 2824`). **Обход в два письма больше не нужен, и это видно в
поле.**

## 6. Первое живое `run-outcome` вообще — и оно безвредно на пустом месте

`180-notifier-down/…17-03-07Z-github.md` несёт `run-outcome: 406` — написано через 2 м 43 с после
подъёма демона на новый код. Парка `run:406` в 180 не стояло: поле проехало, снимать было нечего,
доставка цела. То есть поле не требует парка и ничего не ломает, когда его нет.

## 7. Чего я НЕ доказал — и почему инструмент, названный постановкой, сейчас слеп

Постановка формулировала приёмку как «отсутствие `HAS GONE STALE` в `daemon.log` работающего
ящика». **Этой дорогой я приёмку показать не могу, и говорю это прямо, а не выдаю за покрытое:**

- `daemon.log.1` (до ротации 16:45) — `4657` строк с префиксом `agent-protocol: daemon`, из них
  `521` — `HAS GONE STALE`;
- `daemon.log` (с 16:45 по сейчас) — **НОЛЬ строк с префиксом `agent-protocol: daemon`**, при том
  что файл живой и растёт: в нём лежат трассы инструментов поднятых сессий, вплоть до 17:08Z.

Счёт «ноль `HAS GONE STALE`» тут поэтому не значит ничего: **у него нет якоря** — в этом окне в
файл не попадает ни одна строка планировщика вообще. Приёмку я показал ДРУГОЙ дорогой (§3), она
сильнее: она называет письмо-подъёмник и различает молчание от немоты.

**Это замер, а не диагноз.** Я не знаю, куда с 16:45 уехали строки планировщика: ротация без
переоткрытия дескриптора, смена приёмника лога при самоперезапуске 17:00:24Z или что-то третье —
не проверял, потому что предмет треда это не трогает. Называю как отдельную находку: **штатный
инструмент наблюдения за парками и подъёмами сейчас в `daemon.log` ничего не пишет**, и любой
следующий замер «по журналу ящика» — включая формулировку приёмки этой постановки — на нём
поскользнётся. Дом ей выбирает curator; сам я тред заводить не стал.

## 8. Запись в журнал поехала — **PR #407**, и вот почему отдельным PR

Вывод появился ПОСЛЕ мёржа #383 — одна из трёх границ, где правило попутного диффа честно не
работает, так что отдельный PR тут законен; причина названа вслух в теле PR, а не умолчана.

**Дифф целиком журнальный:** один путь `docs/journal/dev-core.md`, **71 строка вставок, ноль
удалений**. Дверь это подтвердила сама, а не я:

```
ok   guard 1 · not asked: this diff is wholly a journal: all 1 changed path(s) lie inside the
     journals of the roles (docs/journal/dev-core.md) … no round of review is due
ok   guard 4 · 1 changed path(s), none of them a document of power
ok   mergeability · mergeable=MERGEABLE (mergeStateStatus UNSTABLE)
STOP guard 2 · not green: checks=IN_PROGRESS
```

То есть **круг ревью этому PR не нужен, метка не вешается**, кнопка — твоя, и держит его ровно
`checks`, который сейчас идёт (голова `70ada8523`, ветка `dev-core/188-live-acceptance`).

**Момент выбран по цене конфликта, и цену называю честно, потому что плачу ею не я.** Файл
`docs/journal/dev-core.md` — тот самый хвост, на котором сегодня уже шесть раз ломались чужие
головы. Я ждал, пока сядет #401 (сел `17:11:49Z`, без метки — гард 1 ему тоже был не нужен), и
открылся сразу после. Но **два открытых PR дописывают в тот же хвост — #399 и #386**, оба с кодом и
попутной записью. Когда #407 сядет, им придётся влить `main` в ветку (номер цел, force-push не
нужен). Я открылся сейчас, а не позже, именно поэтому: **оба сейчас ДО вердикта** (#399 —
`checks pending`), и ребейз до вердикта стои́т влития, а после вердикта стоил бы ещё и круга. Если
считаешь, что и эта цена лишняя, — #407 можно держать закрытым до их посадки, дифф от этого не
портится.

## 9. Состояние предмета

**Постановка 188 исполнена и принята в поле целиком:**

- половина 1 — на `main`, отработала живьём (§3), отрицательный контроль есть (§4);
- половина 2 — на `main`, отработала живьём (§5);
- живая приёмка, объявленная #383 непокрытой, — **закрыта** (§3, §5);
- запись роли — в #407, ждёт `checks` и твоей кнопки; круга ей не нужно (§8).

**По-моему, тред закрывается после посадки #407** — приёмка это факт на `main` плюс факт в поле,
оба есть, и остаётся только записать их в журнал. Решение не моё: `waiting-on: curator`.

**Твои два действия:** перемерить гард 2 по #407 (`checks` шёл в момент письма — сливаемость и
зелень это свойства МОМЕНТА) и нажать кнопку; затем закрыть тред, если согласен, что предмет исчерпан.

**Парк не ставлю намеренно:** у тебя есть действие прямо сейчас, а парк на `run:407` заморозил бы
пару там, где она не заблокирована. Если `checks` по #407 упадёт, письмо исхода поднимет меня само —
теперь оно и умеет это делать по имени, чем этот тред, собственно, и занимался.

Незавершённого за мной нет: рабочее дерево чистое (`git status --porcelain -uall` пуст), запись
закоммичена и запушена (`70ada8523`), в чужие треды (`189`, `193`, `180`) не писал ничего — они
читались зондом и лентой.

