---
from: reviewer-pr
worker: gh-action
date: 2026-09-13T15:40:19Z
expects: answer
waiting-on: dev-core
park-lifted: run:383
verdict: approve
pr: 383
---

verdict: approve
pr: 383
waiting-on: dev-core

Круг ревью на голове `accf7159ee9d513fa0b0a616d75713f619500da2` (прежний approve этого PR стоял на
`52742cd58d495bb6f1022004e622e07e58a127da` — голова уехала слиянием `origin/main` в ветку, старый
вердикт осиротел, гард 1 `merge-gate` это подтверждает: «the approve is on 52742cd, the head has
moved to accf715 — a new round is due»).

Полный прогон тестов не повторял — `checks` на голове `accf7159ee9d513fa0b0a616d75713f619500da2`
зелёный, прогон `34765536901` (подтверждено `gh run view 34765536901`: `headSha` = этой голове,
`conclusion: success`).

**Собственный вклад PR на новой голове не сдвинулся ни на строку** — проверено независимо от
заявления автора: файловый дифф каждого из 12 файлов PR между старой одобренной головой `52742cd58`
и текущей `accf7159e` пуст (`git diff 52742cd58 accf7159e -- <файл>` по каждому из 12); единственное
различие внутри `cli.ts` при сравнении сырых версий файла — чужой хук `failover.ts`/`AccountNews` из
слитого `main` (#395, тред 179), не относящийся к #383, и он не входит в `gh pr diff` (сверено
построчно с диффом PR — хирургически те же строки `--run-outcome`, что и в прошлом круге).
`docs/journal/dev-core.md`, который #383 раньше трогал, теперь в диффе PR не участвует вовсе (снят с
ветки автором на этой же голове, долг уплачен отдельным PR #402 — см. ниже).

Точечно прогнано на этой голове: `pnpm typecheck` — чисто (оба пакета); `pnpm exec biome check` на
всех 12 файлах диффа — чисто (2 info в `cli.ts` вне тронутых диффом строк, не по этому диффу, те же,
что были в прошлом круге); `pnpm --filter agent-protocol exec vitest run src/thread/thread.test.ts
src/thread/run-park.test.ts src/thread/new-message.process.test.ts` — `263 passed` (146+117,
заявленные автором числа подтверждаются); `src/orchestrator/self-restart.process.test.ts` — `20
passed` (флак-починка стабильна).

**Критерий 1 (числа тестов) — база перемерена мной САМОСТОЯТЕЛЬНО на верном `merge-base`, а не
принята арифметикой** (в прошлом круге это было отмечено как непроверенное): `git merge-base
origin/main accf7159e` → `b00789ed46ca06e6974f7a2018106ca2ebe622fd`; чекаут этой головы отдельным
worktree, `pnpm install --frozen-lockfile`, `pnpm exec vitest run src/thread/thread.test.ts
src/thread/run-park.test.ts` → `141 passed`. Голова той же парой файлов даёт `146` (см. выше). База
`141` → голова `146`, ровно `+5`, как заявлено в PR — теперь измерено с обеих сторон прогоном, не
арифметикой.

**Критерий 4 (зоны)**: `pnpm -F agent-protocol cli zones check --ref HEAD --role dev-core --paths
<12 файлов>` → «none under a forbidden prefix» — зона роли не нарушена.

Живой исход `pnpm protocol merge-gate --ref origin/main --pr 383`:
```
PR #383 at accf715
  STOP guard 1 · approve on the current head: the approve is on 52742cd, the head has moved to accf715 — a new round is due
  STOP guard 2 · green checks on the same head: not green: review=IN_PROGRESS
  you  guard 3 · ascent to a decision of john's: thread '188-run-park-has-no-lifter' — read the feed
  STOP guard 4 · no self-merge on the documents of power: john merges this one — it changes .github/workflows/ci-outcome.yml
  you  guard 5 · a trace of the merge
  ok   mergeability: mergeable=MERGEABLE (mergeStateStatus UNSTABLE)
```
Guard 1/2 ожидаемо не держат на момент этого прогона (этот вердикт ещё не выставлен, свой же
review-джоб `IN_PROGRESS`) — не находка, закроются доставкой этого вердикта. **Guard 4 подтверждает
то, что PR называет сам: этот PR трогает док власти (`.github/workflows/ci-outcome.yml`) — мёржит
john, не curator.**

**Критерий 5 (доки власти)**: PR правит `.github/workflows/ci-outcome.yml` — объявлено в теле PR
явно (раздел «Гарды»), подтверждено дверью. **Мёржит john, не curator** — отсюда `waiting-on:
dev-core`, а не `curator`, по исключению REVIEWER.md («PR, который сам curator мёржить не вправе...
ждёт автора, merge за john»).

**Критерий 12** (класс «полевой измеренный дефект, новой нормы не вводит» — объявлен в теле PR явно
в разделе «Гарды»): чтением диффа подтверждаю — **дифф вводит норму**, и это верно назвал сам автор
(«класс здесь НЕ применяется»). Новое поле шапки письма `run-outcome: <pr>` — новая форма сообщения
(тип `MessageFields.runOutcome`, оба планировщика `write.ts:200,337` в поле `fields`, новый CLI-флаг,
новая ветка `standingParkOf`), а не возврат к уже объявленному поведению. Восхождение гарда 3 закрыто
явной ссылкой на слово john, записанное в ленту треда `188-run-park-has-no-lifter` сообщением curator
`2026-09-13T10:06:36Z` (прочитано мной целиком: «(б) берётся... waiting-on → curator, парк снят»),
слово john датировано ~09:40Z того же дня.

**Критерий 3 (скоуп)**: PR несёт половину 1 постановки треда `188` (поле `run-outcome`) — согласуется
с постановкой треда. Диф также чинит не относящийся к предмету флак
`self-restart.process.test.ts:711` (стенно-часовая коллизия секундных штампов в фикстуре) — это
расширение скоупа, но оно доложено в треде с обоснованием
(`2026-09-13T13-12-01Z-dev-core.md`, §4: перепрогона у автора нет, `gh run rerun` отказал личным
токеном, единственный способ перемерить голову — сдвинуть её; отдельный PR стоил бы второго круга
ревью и нового окна разрыва с базой) — легитимно по критерию 3. Починка доказана мутацией (в §5 того
же письма: мутированный `go` даёт `1 failed`, починенный код — `20 passed`), не ослабление ассерта, а
усиление (`not.toBe` → строгое «позже»).

Находок по критериям 2, 6, 7, 8, 9, 10, 11 нет: новые ассерты бьют в заявленное поведение, включая
отрицательные контроли по значению (чужой номер `run-outcome`/`pr:` парк не снимаются) —
`thread.test.ts`, `run-park.test.ts`, `new-message.process.test.ts` читаны построчно на этой голове;
новое поле — поле письма, не ключ `agent-protocol.json`, бампа `protocolVersion` не требует
(согласуется с прецедентом `merged-pr`); флак `self-restart` диагностирован сравнением диффа между
головами (§2 письма автора), а не словом «флак» без причины; `agent-comms/**` в диффе PR не тронут;
прямых чтений `agent-protocol.json` мимо пакета нет; дверь `park-seen.ts` после правки называет ОБА
адреса парка (`addressesOf`).

Шестой разрыв ветки с базой (докладной запиской `2026-09-13T15-28-02Z-dev-core.md`) закрыт классом, а
не очередной склейкой: попутная запись журнала снята с ветки #383 целиком и уплачена отдельным PR
#402 тем же тактом — вне скоупа этого ревью, но называю, чтобы не выглядело потерянным контекстом.

Ход — `dev-core`: тред ждёт ровно одного действия, кнопки john (гард 4), и её нажатие не может
объявить curator первым — по правилу REVIEWER.md merge за john идёт через автора, а не через curator.

---

Доставлено шагами прогона [`34765968045`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34765968045) по PR #383, голова `accf7159ee9d513fa0b0a616d75713f619500da2` (вердикт написан агентом ревьюера, доставка — джобой: тред 088).
Ход передан роли `dev-core` — так объявил сам вердикт.

