---
from: curator
worker: claude-code
session: 0aa662e0-238d-45e3-ba72-ed0c347cc2ef
raised: 2026-09-13T13:44:02Z
date: 2026-09-13T13:49:20Z
expects: ack
waiting-on: curator
parked-on: john
---

**ТРЕБУЕТСЯ ОДНО СЛОВО JOHN 2026-09-14 ПОСЛЕ 13:20Z — «мерь»: таймера у протокола нет, и этот парк на тебе И ЕСТЬ будильник для перемера, который ты назначил, закрывая тред `177`. Пока парк стои́т, пару на этом треде контур не поднимает — ни одного пустого подъёма за сутки.**

## 0. Откуда этот тред

Слово john в чате `2026-09-13 ~13:19Z`, доставлено моим письмом в тред `177-workspace-per-pair`
(последнее письмо ленты, `ПРИЁМКА ПОДТВЕРЖДЕНА, ТРЕД ЗАКРЫВАЙ — но замер «до/после» ПЕРЕМЕРИТЬ ЧЕРЕЗ
СУТКИ`), дословно оттуда же: «Перемерить через сутки — **в своём месте, не здесь**: тред закрывается.
Носитель замера назовёт исполнитель; john ждёт **числа**, а не ощущения». Этот тред — названный
носитель, и другого у предмета нет: `177` закрыт тем же ходом, что открыл этот.

**Основание гарда 3 для любого PR этого треда — то же слово john**, названное здесь построчно. Новой
нормы предмет не вводит по построению: это ЗАМЕР, а не механизм.

## 1. Предмет

Перемерить обе половины замера «до/после» параллельности внутри роли (ключ рабочего места «роль ×
тред», потолки `2`/`3`, включены конфигом `2026-09-09T16:03:10Z`) на окне, где параллельность
действительно жила, и сравнить с базовым снимком `2026-09-13T11:18…11:30Z`.

Почему перемер нужен — сказал john и я его довод не пересказываю, а повторяю фактом: **базовая точка
снята через два часа после первой замеренной пары живых сессий одной роли** (`dev-core×177` с
`10:47:17Z`, `dev-core×189` с `10:46:29Z` того же дня), а `09-10` и `09-11` — дни без единого мёржа.
Честного «после» в базовом снимке — один неполный день.

## 2. Приёмка — в проверяемых фактах

**Срок: не раньше `2026-09-14T13:20Z`** (сутки от слова john). Раньше — замер вырождается в тот же
неполный день, ради которого всё и перемеряется.

**(а) Круги ревью на влитый PR, ПОСУТОЧНО.** `gh run list --workflow 'Claude PR Review'`, только
`event=pull_request`, против числа смёрженных PR того же дня. Таблица обязана нести дни
`09-03 … 09-14` целиком, чтобы её было с чем сравнивать:

| точка сравнения | число |
|---|---|
| доновведенческий разброс `09-03 … 09-09` | `1.06 … 1.43` круга на PR |
| базовый снимок: `09-12` / `09-13` (до `11:18Z`) | `1.17` / `1.00` |
| перемер: `09-13` (полный) / `09-14` | **назвать** |

**(б) Ожидание переданного хода.** Соединение почты и журнала ящика: письмо с `waiting-on: <роль>` в
треде — момент передачи хода, ближайший `launch` той же пары в `.orchestrator/journal.jsonl` — момент
подъёма; ожидание = разница, письма схлопываются к ПЕРВОМУ непотреблённому. Колонок две и обе
обязательны — **сырая медиана и медиана без «замороженных» (>6 ч)**, плюс доля таких ожиданий:

| окно | медиана сырая | медиана без >6 ч | p90 без >6 ч | доля >6 ч |
|---|---|---|---|---|
| `09-02 … 09-09T16:03` (до) | 14.8 | **11.4** | 122 | 11 % |
| `09-09T16:03 … 09-13T11:30` (базовый «после») | 19.3 | **8.8** | 68 | 13 % |
| только `09-13` (базовый, неполный) | 44.2 | 18.3 | 47 | 38 % |
| `09-13` полный и `09-14` (перемер) | **назвать** | **назвать** | **назвать** | **назвать** |

**Порог назван john ЗАРАНЕЕ и в приёмку входит буквально:** если круги ревью растут быстрее, чем
падает ожидание хода, — это **факт, который называется вслух**, а не растворяется, и тогда потолок
`3` пересматривается. **Пересмотр потолка — кнопка john, не рекомендация исполнителя:** дело замера
кончается на числах и на прямом ответе «размен виден / не виден».

**Чего приёмка НЕ требует:** ни нового прибора, ни правки существующих. Если для (б) понадобится
тронуть `scripts/turn-latency.mjs` — это отдельный PR роли `dev-core` и отдельное основание; замер
ждёт его, а не обходит своей рукой.

## 3. Инструменты — названы дословно, потому что «той же командой» держится на этом

- `scripts/turn-latency.mjs` + `docs/turn-latency-measurement.md` (PR #347, тред `177`, слит
  `2026-09-08T18:52:18Z`) — команда замера оборота хода и её объявленные границы: левый край — момент,
  когда ход увидел ДЕМОН; ожидание меряет доступность РОЛИ, а не готовность треда; ход, не поднятый до
  правого края окна, в медиану НЕ входит и печатается строкой `never lifted`; аренда без
  `lease-released` закрывается правым краем, поэтому доля «занята» — оценка СНИЗУ;
- `pnpm protocol metrics --json`, секция `review` — счётчики кругов **накопительные**, `--since` их
  окном НЕ режет (замерено в `177`: числа с `--since` и без совпали до единицы). Значит «после»
  берётся **вычитанием двух снимков**, а не флагом;
- граница обоих `gh … list`: **`--limit N` отдаёт НОВЕЙШИЕ N и усекает молча.** Считать посуточно и
  называть, какие дни полны.

## 4. Проверяемость

- **юнит/интеграционный — НЕ применимы и это названо, а не забыто:** предмет не функционал, а чтение
  поля. Ни одна строка `packages/**` этим замером не трогается;
- **воспроизводимость — класс проверки здесь единственный:** каждое число обязано ехать с командой,
  которой снято, и с окном, на котором снято. Число без команды в докладе не принимается;
- **что сознательно НЕ покрывается:** числа поля невоспроизводимы вне ящика — журнал оркестратора
  (`.orchestrator/journal.jsonl`) не файл репозитория и не артефакт CI, у ревьюера его нет. Это
  объявленная граница класса (она же стои́т у `scripts/prose-cost.mjs` и `scripts/collision-audit.mjs`),
  а не находка;
- **носитель результата — письмо в ЭТОТ тред.** Журнальная запись — только если перемер даёт урок о
  поведении контура; PR ради самих чисел не открывается.

## 5. Исполнитель и ход

**Исполнитель — `curator`:** обе половины — чтения (`gh`, журнал ящика, лента почты), кода не требуют.
`dev-core` зовётся только через §2, если прибор придётся править, и отдельным PR.

**Почему тред стои́т ПАРКОМ НА ЧЕЛОВЕКЕ, а не ходом на curator, — три формы перечислены, выбрана
единственная непустая:**

1. `waiting-on: curator` без парка — контур поднимает пару **ближайшим тактом** и сутки подряд: подъём
   находит «ещё не время» и гаснет. Это прямая трата подъёмов, замеренная в `177`, и после третьего
   пустого пара перестаёт подниматься вовсе;
2. `--waiting-on "—"` — ход не у кого, пару никто не поднимает, но и **разбудить тред нечем**: тред без
   хода будит только письмо ИЗВНЕ, а писать его некому — я этот ход заканчиваю;
3. **парк на john с `--expects ack`** — контур тред пропускает (парки планировщик не поднимает), звонок
   уходит ОДИН и несёт первую строку письма, то есть сам звонок и есть напоминание о сроке. Выбрана
   она.

**Если участие john в будильнике тебе не нужно** — скажи, и я сниму парк своей рукой: тогда пара
поднимется ближайшим тактом и перемерит РАНЬШЕ суток, что снимок только ухудшит. Обратная сторона
честная: пока парк стои́т, перемер не случится без твоего слова.

waiting-on → curator (ход несёт роль; john в это поле не принимается). Парк — на john.

