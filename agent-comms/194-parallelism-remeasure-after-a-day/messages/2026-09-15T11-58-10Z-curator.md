---
from: curator
worker: claude-ai
date: 2026-09-15T11:58:10Z
expects: none
waiting-on: —
park-lifted: john
---

**Решение john (сессия чата, ~11:57Z): ЗАКРЫВАТЬ. Парк снимаю этим письмом, статус треда перевожу в `closed` следующим вызовом этого же хода.**

## Что принято

Заказанное «после» получено на годном окне, порог john отвечен прямо, и ответ отрицательный в обе стороны:

- **круги ревью на влитый PR не выросли** — `1,27` (до) → `1,25` (окно), чистых `1,03`;
- **ожидание хода не изменилось** — медиана `11,5` → `11,3` мин; упал только хвост, p90 `123,7` → `66,8` мин;
- **доля «замороженных» >6 ч** держится: `9,1 %` → `9,6 %`.

**Размена не видно ни в одну сторону. Пересмотра потолка `5` решение не требует, и он не пересматривается.**

Сопутствующее, принятое тем же тактом: вчерашняя невоспроизводимость закрыта и названа ошибкой самой роли (схлопывание пачки к последнему письму вместо первого непотреблённого); с исправленным правилом базовая таблица воспроизведена почти до десятых. Наблюдение john про ночной простой проверено и отведено: ящик не поднимает никого КАЖДУЮ ночь, включая базовые сутки, — перекоса в сравнение «до/после» это не вносит.

## Что закрытием НЕ объявляется принятым

- **замер мерит сегодняшний контур целиком, а не одну параллельность** — за сутки в него вошли ещё полдюжины починок; приписывать `1,25` и `11,3` одному потолку нельзя, и следующий читатель обязан прочесть это раньше таблиц;
- **прибора под измерение ожидания в пакете нет** — считается join почты и журнала рукой роли; `scripts/turn-latency.mjs` мерит другое. Долг остаётся, дома в этом треде у него нет, постановки на него сейчас тоже нет.

Тред закрыт.
