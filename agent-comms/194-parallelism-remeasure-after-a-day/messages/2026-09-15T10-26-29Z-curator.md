---
from: curator
worker: claude-code
session: 8074e493-c6e6-420c-913c-cd59685002e9
raised: 2026-09-15T10:20:09Z
date: 2026-09-15T10:26:29Z
expects: ack
waiting-on: curator
parked-on: john
---

**ОКНО ГОДНО, РАЗМЕНА НЕ ВИДНО, И БАЗОВЫЙ ЗАМЕР ВОСПРОИЗВЁЛСЯ — вчерашняя невоспроизводимость закрыта: ошибка была В МОЁМ вчерашнем фильтре, а не в базовой таблице. Ночной простой, который ты заподозрил, — НЕ класс `205`: ящик ночью не поднимал никого вовсе, и так каждую ночь, включая базовые сутки.**

Требуется одно слово john: **закрывать тред** (моя рекомендация) — или мерить ещё.

## 1. ПЕРВЫМ ШАГОМ, как велено: отравлено ли окно классом `205`

Окно `2026-09-14T09:21Z … 2026-09-15T10:20Z`. Команда:
```
gh run list --workflow 'Claude PR Review' --limit 400 --json databaseId,event,status,conclusion,createdAt,headSha
gh run view <id> --json jobs     # какой шаг упал
gh pr view <n> --json comments   # доставлен ли вердикт
```
Полнота: отдано 400 кругов, старейший `2026-08-31T02:36:10Z` — окно покрыто целиком, усечение в него не заходит.

| | число |
|---|---|
| кругов `event=pull_request` в окне | **40** |
| из них `completed/failure` | **7 (17.5 %)** |
| из них с ДОСТАВЛЕННЫМ вердиктом (класс `205`) | **7 из 7 — все** |
| для сравнения: `09-13` (отравленные сутки) | 18 красных из 36 (**50 %**) |

Все семь упали на одном шаге — `Run anthropics/claude-code-action@v1` в джобе `review`, — и у всех семи коммент `verdict:` лежит на PR в минутах от прогона. Это ровно класс `205`, влитый сегодня в `08:18:42Z` (#433).

| прогон | PR | вердикт доставлен | состояние сейчас |
|---|---|---|---|
| `34841729661` `12:07Z` | #426 | `12:17:58Z` approve | открыт |
| `34849381027` `13:27Z` | #428 | `13:31:16Z` approve | влит `09-15T09:59Z` по НОВОМУ кругу |
| `34850008811` `13:33Z` | #416 | `13:38:37Z` needs-fixes | влит `09-15T08:41Z` |
| `34864294084` `15:45Z` | #439 | `15:52:51Z` approve | открыт |
| `34864312409` `15:46Z` | #440 | `15:51:32Z` approve | открыт |
| `34865984206` `16:01Z` | #441 | `16:05:53Z` approve | открыт |
| `34866710091` `16:08Z` | #442 | `16:13:12Z` approve | открыт |

**Вывод — фактом, а не оговоркой: окно ГОДНО.** `205` бил ТОЧЕЧНО (семь PR из сорока кругов), а не закрывал дверь merge всему контуру: за окно влито **32 PR** (21 на `09-14` после `09:21Z`, 11 сегодня). Вчерашнее окно было негодным не из-за доли красных, а из-за **15 ч 48 мин, когда не мёржилось ничего**; здесь такого нет.

**Цена `205` всё же названа: четыре PR (#439, #440, #441, #442) стоят открытыми с approve-вердиктом от вчерашних `15:51…16:13Z` — восемнадцать часов.** Гард 2 краснеет от самого круга, поэтому НИКАКАЯ рука их не смёржит, пока на голове висит красный `review`; лечение — перевесить метку и купить новый круг, и сегодня оно уже сработало на #428 и #416.

## 2. Твоё наблюдение про ночь — проверено, и причина ДРУГАЯ

Круги ревью за окно: последний вечерний `09-14T16:08:20Z`, следующий — `09-15T09:07:22Z`. Семнадцать часов без единого круга. Но это не круги умирали, а **никого не поднимали**:

```
grep '"kind":"launch"' .orchestrator/journal.jsonl
```

| ночь | последний подъём | следующий | подъёмов внутри |
|---|---|---|---|
| `09-11 → 09-12` | `16:08:49Z` | `14:31:31Z` | — |
| `09-12 → 09-13` | `19:59:58Z` | `08:42:08Z` | — |
| `09-13 → 09-14` | `19:51:55Z` | `08:56:40Z` | — |
| **`09-14 → 09-15`** | **`16:59:12Z`** | **`08:04:08Z`** | **2 за 15 ч** (днём ~20/час) |

**Ночной простой — стоячее свойство ящика, а не авария:** он есть КАЖДУЮ ночь, в том числе в тех сутках, на которых снят базовый замер. Значит из чисел он не вычитается и в сравнение «до/после» не вносит перекоса. Вчерашняя авария была видна иначе — красными кругами и закрытой дверью merge; сегодня ночью не было ни того, ни другого, был выключенный ящик.

Передач хода за эту ночь — три штуки из 136 за окно: медианы она не двигает вовсе.

## 3. (а) Круги ревью на влитый PR

| точка сравнения | круга на PR |
|---|---|
| доновведенческий разброс `09-03 … 09-09` | `1.06 … 1.43`, агрегат `195/154 = 1.27` |
| базовый снимок `09-12` / `09-13` (до `11:18Z`) | `1.17` / `1.00` |
| вчерашний перемер, агрегат `09-12 … 09-14T11:47` | `1.20` |
| `09-14` полные сутки | `30/21 = 1.43` |
| `09-15` (неполный день, до `10:20Z`) | `10/11 = 0.91` |
| **ЭТОТ ПЕРЕМЕР, агрегат по окну `09-14T09:21 … сейчас`** | **`40/32 = 1.25`** |

**Круги ревью НЕ выросли:** `1.25` лежит ниже доновведенческого агрегата `1.27` и внутри его разброса. Красных кругов в окне 7, все — `205`, и они сидят в этом отношении как «рост», которым не являются: чистых кругов на влитый PR `33/32 = 1.03`.

## 4. (б) Ожидание переданного хода — и ЗАКРЫТИЕ ВЧЕРАШНЕГО ДОЛГА

Тот же join почты и журнала ящика:
```
git grep -a -m1 "^waiting-on:" origin/comms -- '*/messages/*.md'   # путь → тред+штамп, строка → роль
grep '"kind":"launch"' .orchestrator/journal.jsonl                  # подъём пары (role+thread в записи)
# ожидание = launch.ts − ts ПЕРВОГО НЕПОТРЕБЛЁННОГО письма пары; порог «заморозки» — 6 ч
```
Сырьё: 4183 письма с шапкой, 3962 адресованы роли, 2034 события `launch`, соединилось 1890 ожиданий.

**Вчерашнее расхождение вчетверо объяснено и оно моё:** вчера я схлопывала пачку писем к ПОСЛЕДНЕМУ письму перед подъёмом, а постановка и базовый замер берут **ПЕРВОЕ НЕПОТРЕБЛЁННОЕ**. Исправив одну эту строку, я получаю базовую таблицу почти до десятых: окно «до» — **14.4 / 11.5 / p90 123.7 / 9.1 %** против записанных базовых **14.8 / 11.4 / 122 / 11 %**. Базовый снимок НЕ невоспроизводим — невоспроизводим был мой вчерашний, и вчерашние `24.0 → 60.5` читать не следует вовсе.

Колонка та же, что у базовой таблицы (ВСЕ письма, минуты):

| окно | n | медиана сырая | медиана без >6 ч | p90 без >6 ч | доля >6 ч |
|---|---|---|---|---|---|
| `09-02 … 09-09T16:03` (до) | 856 | 14.4 | **11.5** | 123.7 | 9.1 % |
| базовая запись того же окна | — | 14.8 | 11.4 | 122 | 11 % |
| `09-09T16:03 … 09-13T11:30` (базовый «после») | 145 | 19.3 | 11.6 | 90.7 | 9.7 % |
| `09-13` полный (аварийные сутки) | 175 | 33.8 | 25.1 | 121.5 | 14.9 % |
| **ОКНО `09-14T09:21 … сейчас`** | 136 | **12.9** | **11.3** | **66.8** | **9.6 %** |
| — день `09-14T09:21…16:59` | 107 | 13.8 | 11.4 | 79.3 | 11.2 % |
| — ночь `09-14T16:59…09-15T08:04` | 3 | 23.6 | 12.5 | 21.4 | 33.3 % |
| — утро `09-15T08:04 … сейчас` | 26 | 10.7 | 10.7 | 23.4 | 0 % |

Только ПЕРЕДАЧИ хода (автор письма ≠ `waiting-on`), для полноты: до — 9.7 / 8.7 / p90 111.7 / 4.7 % (n=515); окно — 10.9 / 10.7 / p90 65.2 / 2.6 % (n=78).

## 5. Прямой ответ на порог, названный john заранее

Порог: **«если круги ревью растут быстрее, чем падает ожидание хода»**.

- **круги ревью не выросли:** `1.27` (до) → `1.25` (окно);
- **ожидание хода практически не изменилось:** медиана `11.5` → `11.3` мин, сырая `14.4` → `12.9`; заметно упал только хвост — p90 `123.7` → `66.8` мин, то есть **минус 46 %** на девятом дециле;
- **доля «замороженных» >6 ч** держится: `9.1 %` → `9.6 %`.

**РАЗМЕНА НЕТ НИ В ОДНУ СТОРОНУ.** Ни роста кругов, ни падения медианы ожидания; единственное измеренное улучшение — хвост p90. Потолок `5` эти числа пересматривать не требуют, и я его пересмотра не рекомендую — кнопка твоя, я её не нажимаю.

## 6. Оговорка, которая остаётся в силе (§3 постановки)

Базовая точка снята при `pairsPerInstance: 3`, а с `09-13T17:25Z` он `5`. Плюс за эти сутки сели: файл-на-тред (`195`), обе половины починки круга (#418, #417), вето предупреждения (#431), журнал в ветку почты (#435/#437), сам `205` (#433). **Замер мерит сегодняшний контур ЦЕЛИКОМ, а не одну параллельность** — приписывать `1.25` и `11.3` одному потолку нельзя, и следующий читатель обязан прочесть это раньше таблиц.

Отдельно: **прибора под (б) в пакете по-прежнему нет.** `scripts/turn-latency.mjs` меряет ДРУГОЕ (свой `handoff-detected`, не письмо). Долг никуда не делся, но он стал дешевле: правило схлопывания теперь названо однозначно («первое непотреблённое») и воспроизводит базовую таблицу. `scripts/**` — зона `dev-core`, своей рукой не трогаю; постановки на прибор сейчас нет.

## 7. Что сделано этим ходом и что НЕ сделано

Сделано: проверка отравленности окна (§1), проверка твоего наблюдения про ночь (§2), обе половины замера (§3, §4), закрытие вчерашнего долга о невоспроизводимости (§4).

**НЕ сделано и названо, чтобы не протухло:**
- **урок про «первое непотреблённое» едет журнальной записью** — пишу её командой в ветку почты тем же ходом, PR под это не открывается (норма 14.09, тред `206`);
- **четыре PR (#439–#442) стоят с approve и красным кругом `205`.** Этот подъём связан ровно одним тредом, чужого предмета я не мёржу и метки на них не перевешиваю: след лёг бы не в ту ленту. **Это хвост, у которого есть хозяева — их треды**, и если его надо снять моей рукой, это отдельный ход.

## 8. Вопрос john — один

**Закрывать ли тред на этих числах?** Рекомендация (рекомендация, не решение): **закрывать.** Заказанное «после» получено на годном окне, порог отвечен прямо, база воспроизведена. Дальнейший перемер даст только более длинный ряд той же величины, а цена — подъём пары за такт.

waiting-on → curator (ход несёт роль; john в это поле не принимается). Парк — на john.

