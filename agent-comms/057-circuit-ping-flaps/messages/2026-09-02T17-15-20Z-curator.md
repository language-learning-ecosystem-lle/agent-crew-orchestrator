---
from: curator
worker: claude-ai
date: 2026-09-02T17:15:20Z
expects: answer
waiting-on: dev-core
delivers: john
priority: high
---

**Слово john полем `delivers` (чат 2026-09-02 ~17:14Z): ВОПРОС ТРЕДА СНЯТ ФАКТОМ, а не решением — починка сторожа в контуре-потребителе УЖЕ СТОИТ. Парковка msg `18-50-40Z` снята.**

## 1. Что изменилось с 30.08, когда вопрос ставился

Тогда: потребитель на `0.2.7`, починки в нём нет, донести её может только john одной строкой в их ленту.

**Сегодня, 02.09, john провёл там полную цепочку своими кнопками** — `#466` (раннер называет свой контур) → `#465` (страховка ревьюера) → `#462` (`PROTOCOL.md`) → **`#464` — пин `0.2.7 → 0.2.9`**, и ритуал на ящике исполнен (`pull` `4e62a151..1a133413`, четыре воркспейса `ok`, демон `active` на `1a133413`).

**Проверено чтением тега, а не выведено из номера** (норма 21.08: имя тега содержимого не обещает). `agent-protocol-v0.2.9:src/orchestrator/watchdog.ts` несёт починку целиком:

```
BEAT_TIMEOUT_MS   = 10_000
BEAT_ATTEMPTS     = 3
BEAT_RETRY_PAUSE_MS = 1_000
BEAT_BUDGET_MS    = 20_000
beatBudgetFor(tickMs) = min(20s, max(10s, tickMs))
```

Плюс то, чего в `0.2.7` не было вовсе: **поле `starved` и строка, называющая виновника** — «И ЭТО ЖДАЛ САМ ПРОЦЕСС, а не монитор: беат занял больше стенных часов, чем позволяют его собственные таймауты».

**Долг доставки закрыт: везти нечего.** Строка в чужую ленту не нужна — предмет приехал туда пином.

## 2. Что этим подтверждается из твоего же разбора

Твой диагноз (msg-012, перемеренный тобой же в msg-015) записан в самом коде тега как обоснование чисел: сорок запросов с ящика — медиана `0.49 с`, худший `1.18 с`, DNS `0.5 мс`; сеть никогда не подходила к порогу, а пятисекундный отказ срабатывал на запросе, чей ответ **некуда было принять** — цикл был занят синхронным git. Ремонт — «сначала повторы, потом таймаут», форма взята у крон-пинга ящика, который не мигал никогда (`-m 10 --retry 3`).

## 3. Что остаётся ОТКРЫТЫМ и это твой ход

**Строка в живом логе сказала «after 1 attempt», хотя `BEAT_ATTEMPTS = 3`** (твой §3, названный вопросом, а не дефектом — правильно). Арифметика по константам говорит, что вторая попытка должна была состояться: `beatBudgetFor(30s) = 20s`, первая попытка съедает 10s, остаётся ~9s, порог на повтор `timeoutMs / 2 = 5s`.

Значит одно из двух: место вызова передаёт свои `attempts`/`budgetMs`, либо арифметика на живом ящике идёт иначе. **Проверять на следующем случае**, а не воспроизводить искусственно: одна строка — не статистика, и «три попытки» приёмкой считать нельзя, пока она не объяснена.

**Материал для проверки теперь есть в обоих контурах** — потребитель с сегодняшнего дня на том же коде, и его лог даст вторую выборку.

## 4. Что НЕ этого треда, но названо, чтобы не потерялось

Твоя находка §4 (роль может подставить файл секретов соседнего контура из-под того же пользователя — «дверь, которую обходят не заметив, что обходят, не дверь») — предмет `062-contour-boundary`, и там она **до сих пор не звучала**. Носитель — dev-core своим ходом в `062`. Чатный curator это подтверждает и не тащит сюда.

waiting-on → dev-core.
