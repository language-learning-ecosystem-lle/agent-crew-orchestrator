---
from: curator
worker: claude-code
session: 4a090c84-b6e6-44d4-8969-98819d90b976
raised: 2026-09-15T11:47:23Z
date: 2026-09-15T11:54:39Z
expects: ack
waiting-on: curator
parked-on: john
---

# Требуется решение john: заказывать ли срабатывание приёмки РУКОЙ — единственный измеренный производитель звонков убран починкой #433 за 22 минуты ДО того, как ключ из #419 стал живым

john: развилка одна и она про расход, поэтому несу её вам, а не решаю сама. Кнопка #448 нажата, след
гарда 5 закрыт целиком (§1). Осталась ровно приёмка — и она ждёт события, чей единственный
замеренный производитель убран соседней починкой (§3). Ниже — факты, развилка (§5) и команда, которой
приёмка читается любым тактом curator (§6).

## 1. Вторая половина следа гарда 5 — обещана в msg-037 §7, кладу её

Прочитано моей рукой в `11:52Z`, `gh pr view 448`:

- `state=MERGED`, `merged_at 2026-09-15T11:34:56Z`, `mergedBy maysway`;
- **контрольное чтение цели: `base.ref = main`** — ответ инструмента сверен с целью, а не принят;
- **squash-sha `c671cb2dfc9186c5087992dca6f513e8b52adf12`**, в линии `main` это `c671cb2df`
  («fix(notifier): у каждого прогона в хвосте глушения печатается его время (тред 198)»).

**Правка на `main` живая, а не потерялась в сжатии** — читано ссылкой, дерева не трогая
(`git show origin/main:<путь>`):

- `.github/scripts/notifier-mute.sh` — функция `notifier_mute_muted_at` на месте вместе с шапкой,
  называющей стык с тредом 209 («печатается ТО ЖЕ мгновение, по которому прогон отобран»,
  `.updated_at // .created_at`), и с отказом по имени на ненайденный id — то есть голое число
  вернуться не может;
- `.github/workflows/notifier-watch.yml` — сборка хвоста на месте (строки 353–390), включая обе
  ветви `MUTED_UNREAD`.

Гард 5 по #448 закрыт: первая половина — msg-048, вторая — этот абзац.

## 2. Поле молчит, и молчание замерено, а не предположено

- **Смотритель с момента мержа не звонил ни разу.** `gh run list --workflow notifier-watch.yml`:
  15 прогонов с `11:36:50Z` по `11:48:00Z`, **все `skipped`** — наблюдаемый не отказывал, джоба не
  заводилась;
- **у наблюдаемых с момента, когда ключ стал живым (`#419`, `2026-09-15T08:40:48Z`), отказов НЕТ
  ВОВСЕ.** `Claude PR Review` — последний отказ `2026-09-14T16:08:20Z`; `CI Outcome`, `Merge Notify`
  — ни одного в выдаче; `Comms Derived` — один не-успех, и он **не отказ**: `cancelled`
  (`34964158234`, `11:36:04Z`), прогон смотрителя в его окне — `skipped`, звонка не было и не
  должно было быть.

**Обе половины приёмки ждут ОДНОГО И ТОГО ЖЕ события — отказа наблюдаемого:** ключ (#419) требует
двух отказов РАЗНЫХ предметов в 900 с, времена (#448) — непустого хвоста, то есть двух отказов ОДНОГО
предмета в 900 с. Отдельно назову то, чего постановка не предвидела: **#419 сделал приёмку #448
реже по построению** — до него хвост копил отказы любых PR под одним именем, после него хвост
непуст только когда дважды за четверть часа падает круг ПО ОДНОМУ И ТОМУ ЖЕ номеру.

## 3. Почему это молчание значимо: класс был ЧАСТЫМ вчера, а его генератор убран сегодня

Молчание доказывает починку только у частого дефекта — поэтому сперва частота, и она вчерашняя.
`Claude PR Review`, 14.09: **семь отказов** с `12:07:51Z` по `16:08:20Z`. Условие приёмки ключа
(«два отказа РАЗНЫХ предметов в пределах 900 с») за эти четыре часа складывалось **не менее трёх
раз** — по времени ЗАВЕРШЕНИЯ, тем полем, которым судит отбор:

| пара | прогоны и ветки | завершения | разрыв |
| --- | --- | --- | --- |
| 1 | `34849381027` (`dev-core/191-…`) и `34850008811` (`dev-core/192-…`) | `13:31:38Z`, `13:38:52Z` | 434 с |
| 2 | `34864312409` (`curator/202-…`) и `34864294084` (`dev-core/207-…`) | `15:51:46Z`, `15:53:05Z` | 79 с |
| 3 | `34865984206` (`dev-core/208-…`) и `34866710091` (`feat/204-…`) | `16:06:05Z`, `16:13:24Z` | 439 с |

То есть вчера ключ звонил бы дважды в каждой паре, а старый ключ съедал вторую — ровно дефект
постановки, и он был бытовым, а не редким.

**А теперь причина отказов, и она несущая.** Три из трёх проверенных прогонов (`34864294084`,
`34865984206`, `34841729661`) отказали **одним и тем же шагом — `9 Run
anthropics/claude-code-action@v1`**. Это ровно тот шаг, на который **#433** («цвет круга решает
доставка вердикта, а не исход шага ревьюера», тред 205) поставил `continue-on-error: true`
(`claude-review.yml:271`, запасной — `:729`). #433 сел **`2026-09-15T08:18:42Z`**, ключ из #419 —
**`08:40:48Z`, на 22 минуты позже**.

**Вывод, и он в границах замера:** единственный производитель звонков, замеренный в поле за
последние сутки, перестал их производить за 22 минуты до того, как починка, которой эти звонки нужны
для приёмки, стала живой. Класс при этом **не умер**: вето пережило `continue-on-error` и стоит в
шаге «Итог доставок — что доехало и что нет» (`claude-review.yml:1176`, чтение `outcome` —
`:1197–1199`) — круг, чей вердикт НЕ доехал, краснеет по-прежнему. То есть срабатывание стало
редким, а не невозможным.

Границы моего замера называю сам: окно тишины — 3 ч 20 мин, причину я разрешила у трёх прогонов из
семи, и к причине «доставка не доехала» ни один из трёх не относится.

## 4. Мой открытый вопрос (msg-026 §2) больше не открыт — на него отвечает стоячая норма

«Чем закрывается приёмка, если авария не приходит вовсе» — с сегодняшнего дня отвечено не мной:
**#440** («новый путь исполнения принимается ПЕРВЫМ СРАБАТЫВАНИЕМ, а не мержем», тред 202) сел в
`main` (`35963a2e7`) и лежит в `PROTOCOL.md` и в моей карточке. Норма говорит дословно: срабатывание
**заказывается рукой** — само оно у редкого условия может не прийти сутками, — и **тред не
закрывается до него**. Вопрос тем самым перестал быть вопросом «чем закрыть» и стал вопросом
«платить ли за заказ», а это ваш.

## 5. Развилка — и обе ветви исполнимы

- **A. ЗАКАЗЫВАЕМ СРАБАТЫВАНИЕ РУКОЙ.** Нужны два намеренно красных круга на ДВУХ разных PR в
  пределах 900 с (и, для второй половины, два по одному и тому же). **Дешёвой формы заказа я не
  нашла:** после #433 круг краснеет только недоехавшим вердиктом, а все известные мне способы
  это устроить лежат либо в `.github/workflows/**` (док власти — ваша кнопка), либо тратят квоту
  ревьюера, то есть деньги. Приёмка закрывается сегодня, тред закрывается следом.
- **B. ЖДЁМ ПОЛЯ.** Цена ноль, срок неизвестен — генератор убран, и ждать можно сутками. Тред 198
  остаётся открытым, приёмка читается командой §6 в любом такте curator, включая такты по чужим
  тредам.

**Рекомендация curator (не решение):** **B со сроком** — если к концу 16.09 срабатывания нет,
возвращаемся к A и платим. Основание: класс не умер (§3), а плата за A — не разовая: заказ красного
круга сам по себе ничего не чинит и уедет в расход целиком.

## 6. Команда обхода и правило остановки — исполнимо любым тактом curator, подъёмника не требует

Приёмка приедет **не в эту ленту**: письмо смотрителя ложится в тред ТОГО PR, чей круг отказал.
Поэтому читается она обходом, а не ожиданием:

```
gh run list --workflow notifier-watch.yml --limit 30 \
  --json databaseId,createdAt,conclusion \
  --jq '.[]|select(.conclusion!="skipped")|"\(.databaseId) \(.createdAt) \(.conclusion)"'
```

**Правило остановки, чтобы такт не тратился впустую:**

- **пусто** — смотритель не заводился ни разу, приёмке взяться неоткуда: дальше не копать, 198 не
  поднимать, ленту не листать;
- **непусто** — открыть этот прогон смотрителя (шаг «Позвонить в тред-приёмник»), взять из него
  ключ, адресата и хвост, и судить по двум вопросам: (1) если в 900 с отказали РАЗНЫЕ предметы —
  писем должно быть **два**, и оба в своих тредах; (2) если хвост непуст — у **каждого** номера в
  нём напечатано своё время. Оба «да» — приёмка состоялась, и тред 198 закрываю я.

## 7. Чего я не делала

- **merge не жала и PR не открывала** — открывать не под что: §4 закрыт стоячей нормой, §3 — замер,
  а не правка;
- **головы не двигала, меток не трогала, красных кругов не заказывала** — заказ ждёт вашего слова;
- **в прогоне не стояла** — поллинга не было, все три выдачи `gh` разовые;
- **тред не закрывала** — приёмки нет ни у одной из двух половин;
- чужих PR не трогала; рабочее дерево чистое, незакоммиченного нет.

## 8. Ход и парк

**`waiting-on → curator`, `--parked-on john`, `--expects ack`.** Ход у меня, потому что вопрос несу
я; парк на человеке — потому что двигаться треду больше нечем: своей рукой в поле я звонок не рожу, а
без парка тред поднимет пару в такт, которому нечего делать.

Скажете **A** — ставлю постановку на заказ срабатывания и называю в ней, что считается провалом.
Скажете **B** — держу §6 как есть и закрываю тред первым же срабатыванием.

