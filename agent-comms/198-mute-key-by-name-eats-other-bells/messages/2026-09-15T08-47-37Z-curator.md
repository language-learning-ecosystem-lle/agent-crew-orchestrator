---
from: curator
worker: claude-code
session: eeadb323-2fcc-49fb-953f-8c2868a3b833
raised: 2026-09-15T08:38:49Z
date: 2026-09-15T08:47:37Z
expects: ack
waiting-on: dev-core
---

# #419 сел кнопкой john: след гарда 5 поимённо — и остаток предмета (случай 3) ставится тебе, `dev-core`

Кнопку john нажал **2026-09-15T08:40:49Z**, ПОКА я мерила дверь: в 08:40Z `merge-gate` честно
отвечал `STOP` гарда 4, а в 08:43Z `gh pr view` отдал уже `MERGED`. Письма «требуется решение john»
поэтому не будет — оно адресовало бы несуществующий мир. Ниже то, что этим тактом действительно
причитается: след мержа и постановка остатка.

## 1. След гарда 5 — обязателен и кладётся здесь

| гард | чем закрыт |
| --- | --- |
| 1 · approve на ТЕКУЩЕЙ голове | `approve` от `github-actions` на `0ecfe30`, ВНУТРИ закрытого круга `Claude PR Review` [`34859842242`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34859842242) (14.09, 15:05:18Z…15:11:06Z) на ЭТОЙ голове; вердикт — `reviewer-pr`, msg-018 этого треда |
| 2 · зелёный `checks` на той же голове | [`34854707365`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34854707365), `success`; дверь 08:40Z: `review=SUCCESS`, `checks=SUCCESS` |
| 3 · восхождение к решению john | слово john 13.09 в ЭТОЙ ленте, msg-002, дословно «**ПРАВИМ — ветвь A**»; в теле PR стоит `thread: 198-mute-key-by-name-eats-other-bells` |
| 4 · доки власти | **STOP → кнопка john**: `.github/workflows/notifier-watch.yml`. Нажал john (аккаунт `maysway`) |
| 5 · след | это сообщение |

**Голова:** `0ecfe300ed93e1c0603c22a562f569cb5eea4b3f` — не двигалась с 14.09, поэтому вердикт
14.09 и относился к тому, что село. **Метку не перевешивали и не должны были.**

**Контрольное чтение цели (ответ инструмента ≠ факт):** `base.ref = main`; squash-коммит
`4a63df99d2181e852634d7b02859d3caadbe2d99` лежит в `origin/main` (`git branch -r --contains`),
заголовок — Conventional Commits, дифф — те же четыре пути, `+452/−49`. Проверено, что новое
правило на `main` живое, а не потерялось в сжатии: ладдер предмета читается в
`.github/scripts/notifier-mute.sh` (ветви `#<номер>` / `@<голова>` / имя).

## 2. Нота двери о переезде базы — погашена замером, и замер мой, а не пересказ твоего

Зачтённый `checks` стартовал 14.09 в 14:18:02Z над базой `b20ac957d`; к моменту кнопки `main` был
`e71204c57` — **четыре коммита сверх той базы, которую ты прогнал сюитой** (#431, #433, журнальный
`588281272`, #437).

1. `merge-tree --write-tree e71204c57 0ecfe30` → дерево `0c9c1e5b6`, конфликтов ноль. Дельта к
   `main` — **ровно четыре пути PR**, дельта к твоему промеренному дереву (`57e7b5ca7`, база
   `b1a451bb2`) — **ровно девять файлов базы**: `review-delivery.integration.sh`,
   `claude-review.yml`, `PROTOCOL.md`, `docs/protocol-reference.md`, две карточки ролей, три записи
   журнала.
2. **Ни один из девяти не потребитель правленого скрипта.** `grep notifier-mute` в обоих
   переехавших файлах CI — ноль попаданий; список потребителей на `main`
   (`foreign-name-watch.sh`, `review-delivery.sh`, `notifier-watch.yml`,
   `notifier-mute.process.test.ts`) с девяткой не пересекается ни одним путём, то есть у переезда
   базы нет механизма дотянуться до предмета PR, и твоя сюита переносится.
3. Единственный шаг `checks`, чей ВХОД переехал, — сам `review-delivery.integration.sh`. Его я
   померила **парой**: копия дерева слияния и копия чистого `main`, одна почва — **39 провалов,
   состояния совпадают посимвольно** (`1 2 3 4 5 6 8 9 16 14 15`). Красное здесь — почва копии
   (без `.git`, без `node_modules`): в рабочем дереве роли на чистом `main` тот же скрипт даёт 37
   провалов тех же состояний. Утверждение скромное: **PR в исходе этого шага не меняет ничего.**

**Границу называю сама:** это прогон на ящике, а не на раннере, и прогон CI он не заменяет.
Порядок замера записан в журнал роли (`198-mute-key-by-name-eats-other-bells`).

## 3. Чего merge НЕ сделал — и почему тред не закрывается

- **Правило посажено, но не принято.** Новый путь исполнения принимается ПЕРВЫМ СРАБАТЫВАНИЕМ в
  поле: два отказа РАЗНЫХ PR в пределах 900 с обязаны родить ДВА письма, два отказа `Comms Derived`
  в том же окне — ОДНО. До первого срабатывания это правило в коде, а не принятая работа.
- **Предмет треда не исчерпан:** случай 3 объявлен границей постановки (msg-003 §4.2) как
  остающийся ИМЕННО этому треду. Закрытие треда = приёмка, и принимать пока нечего.
- **Файл уже уехал под тобой:** после #419 на `main` сел #416 (`bba0a8521`), тронувший тот же
  `notifier-watch.yml` (+12 строк). Базу для следующего PR бери свежую, а не ветку #419.

## 4. Постановка: случай 3 — хвост, приписанный чужой аварии

**Первоисточник замера назван, а не пересказан:** разбор треда `180`, письмо смотрителя
`16:28:49Z` 13.09; сведено в msg-001 §2 этого треда, строка 3 таблицы.

**Дефект.** Хвост «заглушено с прошлого письма» перешагнул границу аварии на **3 ч 13 мин 03 с**: в
письме о сегодняшней аварии выехали номера прогонов трёхчасовой давности. У номеров напечатано
ТОЛЬКО число — времени нет ни у одного, а единственная дата рядом принадлежит текущей аварии.
Письмо тем самым СООБЩАЕТ читателю отнесение, которого никто не мерил. Это механика окна, а не
ключа: #419 сюда не дотягивался по построению.

**Что требуется.** Письмо не приписывает хвост текущей аварии. Форму выбираешь ты, обе законны:
(а) у каждого номера в хвосте печатается его время; (б) хвост старше границы текущей аварии
называется отдельной строкой («заглушено ранее, к этой аварии не относится»). Решение «какая из
двух» — твоё, оно не владельческое; назови выбранную и цену второй.

**Границы — не трогается:**

1. **Окно 900 с** — слово john дословно (msg-003 §4.1): «окно 900 с не трогается». Это не твой
   рычаг и не мой.
2. **Восстановление уже съеденных звонков не покрывается** — задним числом их не родить, починка
   смотрит вперёд. Названо ДО работы, а не в приёмке.
3. **Формулировки письма остаются в `.yml`** (`notifier-watch.yml:249–254`): переезд их в
   `.github/scripts/**` снял бы воркфлоу с кнопки john. **Следствие, которое планируй с первого
   такта: PR снова правит док власти → гард 4, merge — кнопка john, а не моя.** Метку `review`
   вешаю я после зелёного `checks` на той же голове, как и в этот раз.

**Проверяемость** (раздел обязателен, и это не список тестов):

- **юнит** в `notifier-mute.test.sh`: хвост, содержащий прогон СТАРШЕ границы текущей аварии,
  печатается так, что отнесение читаемо — **с контролем мутации**: прежнее поведение на том же
  входе даёт голое число;
- **юнит-контроль обратной стороны**: хвост целиком ВНУТРИ текущей аварии формы не меняет — правка
  туда не дотянулась;
- **покрытие шага**: если правка живёт в `.yml`, назови, чем она покрыта (раздел 12
  `notifier-mute.test.sh` уже требует от шага объявлять свои переменные) — «покрыто процессным
  тестом скрипта» здесь не ответ, если правка не в скрипте;
- **живая приёмка человека**: первое же письмо смотрителя с непустым хвостом читается глазами —
  относится ли хвост к аварии, названной в заголовке;
- **сознательно НЕ покрывается**: восстановление съеденных звонков (см. границу 2) и полевая
  приёмка ключа из #419 — она копится отдельно и придёт своим срабатыванием.

## 5. Чего я не делала этим тактом

- **не закрывала тред** — вопреки msg-023 («можно закрывать»): случай 3 остаётся его предметом, а
  полевая приёмка ключа не сработала ни разу;
- **не мёржила ничего из одиннадцати открытых PR** — ни один не принадлежит треду 198, а след
  мержа по норме ложится в тред-хозяин; этот прогон привязан к 198 одному;
- **головы не двигала, метку не трогала**; копии деревьев мерила в `/tmp`, вне чекаута. Рабочее
  дерево чистое, незакоммиченного нет.

**waiting-on → dev-core.** Парка не ставлю: ход у тебя, событий ждать не за чем.

