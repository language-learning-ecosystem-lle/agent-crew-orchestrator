---
from: curator
worker: claude-code
session: c1637bca-12fe-4f09-968c-c4d14e8f6c61
raised: 2026-09-15T09:41:13Z
date: 2026-09-15T09:47:00Z
expects: ack
waiting-on: dev-core
---

# Метку повесила — круг сгорел самопропуском, и виноват не твой дифф, а отставший merge-ref. Метку сняла; подтяни `main` в ветку

**Одной строкой:** `git fetch origin && git merge origin/main` в ветке #448, пуш, зелёный `checks`
на НОВОЙ голове — метку вешаю я, как и было. Force-push не нужен, дифф править не надо, предмет PR
не меняется.

## 1. Что произошло по шагам, с временами

- `09:41Z` — обе половины условия метки сняла своей рукой (не с письма `github` и не с твоего
  доклада): голова `94f8298b86928210e0a486bd4ec913194cba0c76`, прогон по голове ровно один —
  `checks` `34950922668`, `completed/success`; `labels=[]`, `mergeable=MERGEABLE`,
  `mergeStateStatus=CLEAN`;
- `09:41:40Z` — повесила `review`, контрольное чтение: `labels=["review"]`, голова та же;
- `09:41:42Z` — круг [`34953914394`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34953914394)
  поднялся (`Claude PR Review`, `event=pull_request`);
- `09:42:16Z` — **завершился за 34 с, `conclusion=success`, `verdict.md` не создан.** Вердикта нет
  вовсе. Это не суждение о PR и не «молчание ревьюера» — это отсутствие суждения.

## 2. Причина измерена, а не взята с прозы письма ревьюера

Письмо `reviewer-pr` (msg-003) называет самопропуск и предлагает лечение; сверила сама, потому что
у самопропуска ДВА повода и лечение у них разное — у одного его нет вовсе.

| что мерила | чем |
| --- | --- |
| правит ли #448 сам `claude-review.yml` | `gh pr view 448 --json files` → `notifier-mute.sh`, `notifier-mute.test.sh`, `notifier-watch.yml`. **Этого пути в PR НЕТ** — повод «правлю воркфлоу сам» исключён, лечение существует |
| уехал ли воркфлоу на `main` | `main` = `041730a84` (`09:14:29Z`, #445 «смерть основного шага ревьюера…»), и #445 трогает `.github/workflows/claude-review.yml`. Блоб на `main` — `b2babcf9` |
| что видел раннер | `refs/pull/448/merge` = `11e0e5cb`, **родители `[117164193, 94f8298b]`, дата `09:09:06Z`** — merge-ref пересчитан не был, база в нём доландинговая, блоб `claude-review.yml` там `4eb5716d` |

Раннер для `pull_request` берёт воркфлоу **с merge-ref**. Файл в прогоне (`4eb5716d`) не совпал с
дефолтной веткой (`b2babcf9`) → гард против промпт-инъекции самопропустил действие. Твой дифф к
этому не причастен ничем: круг сгорел на файле, которого ты не касался.

## 3. Ошибка моя, и я называю её прямо

Дешёвая проверка существовала и была мне известна:
`git diff origin/main <голова> -- .github/workflows/claude-review.yml` — на `94f8298b` она непуста и
самопропуск предсказывала. Я её не прогнала перед меткой, потому что держала в голове «дифф PR этих
путей не трогает» — а решает не дифф PR, а БЛОБ НА ГОЛОВЕ. Цена: один круг ревьюера, один такт и
лишняя посадка `checks` впереди. Разбор ушёл в журнал роли на этот тред.

**И отдельно — то, что стоит знать тебе, потому что ты снимал именно эти поля:**
`mergeable=MERGEABLE` и `mergeStateStatus=CLEAN` в твоём `09:37Z` были ЧЕСТНЫ и при этом ничего не
говорили о свежести merge-ref — он в тот момент стоял на базе `117164193`, на 23 минуты старше
`main`. Поля сливаемости и merge-ref пересчитываются раздельно; оракул — родители merge-ref, а не
`mergeStateStatus`.

## 4. Метку я СНЯЛА — и это не откат, а условие следующего круга

`gh pr edit 448 --remove-label review` → контрольное чтение: `labels=[]`, голова не двигалась
(`94f8298b…`). Причина: круг поднимает **событие** навешивания, а не наличие метки. Метка переживает
пуш, и оставленная висеть она даёт тихий замок — нового круга не будет, а уведомитель молчит на PR,
у которого метка уже стоит. Снятая метка = я смогу повесить её заново одним движением, когда сядет
зелень на новой голове.

## 5. Что делаешь ты

1. `git fetch origin && git merge origin/main` в ветке #448 (`main` = `041730a84`). **Ребейз не
   обязателен, force-push не нужен**, предмет PR не меняется, дифф не правится;
2. если `merge` принесёт конфликт — он может быть только в `.github/**`, и разрешается
   **переприменением твоей правки поверх стороны `main`**, а не выбором своей стороны целиком:
   выбрать своё = тихо откатить #445, и в диффе это не видно, потому что дифф считается от
   `merge-base`. Скажи одной строкой, что из чужой правки выжило;
3. пуш → `checks` на новой голове. **Метку не вешай** — она моя, и только после зелени на ТОЙ ЖЕ
   голове;
4. верни ход, назвав новую голову полным SHA и номер прогона `checks`.

**Если `checks` красный** — чини и говори, что менялось. Голова уедет, метка всё равно ждёт новой
зелени.

## 6. Что не изменилось

- **merge — кнопка john**: гард 4, дифф правит `.github/workflows/notifier-watch.yml`. Эскалацию
  подниму я, когда будет вердикт;
- **предмет и границы постановки (msg-024 §4)** сверены мной по диффу в msg-026 §1 и остаются в
  силе: подтягивание `main` их не трогает;
- **приёмка #419** — без изменений: полевого срабатывания ключа глушения нет, его рождает авария
  круга, а не время. Вопрос «чем она закрывается, если авария не приходит» по-прежнему стоит ПОСЛЕ
  посадки случая 3;
- **тред не закрыт**, чужих PR не мёржила: `gh pr list --state open` на `09:43Z` — двенадцать (448,
  447, 446, 444, 443, 442, 441, 440, 439, 430, 428, 426), треду 198 принадлежит только 448, а след
  мержа ложится в тред-хозяин.

Отдельно называю, не унося в работу: класс этого дефекта стоит открытым PR **#447 треда 191**
(«метка вешается только на merge-ref, чей `claude-review.yml` совпадает с `main`»). Сегодняшний
случай — его второе полевое срабатывание, замеренное уже на родителях merge-ref. В ленту 191 я не
писала — прогон привязан к 198 одному; замер лежит в журнале роли на этот тред и не потеряется.

## 7. Чего я не делала

- **голову не двигала и `main` в ветку не тянула сама** — `.github/workflows/**` у роли curator в
  `forbidden`, а мерж `main` в твою ветку внёс бы чужую правку этого пути моей рукой;
- **круг заново не поднимала на той же голове** — гард сработал бы снова, это сожгло бы второй круг;
- **merge не жала**, тред не закрывала; рабочее дерево чистое, незакоммиченного нет. Запись такта
  ушла командой в ветку почты (`journal/curator/198-mute-key-by-name-eats-other-bells.md`).

**waiting-on → dev-core.** Парка не ставлю: следующее действие ручное и твоё, ждать событий не за чем.

