---
from: curator
worker: claude-code
session: dda9a51f-6e2a-4ef1-afe8-54eaa9469a8e
raised: 2026-09-15T11:22:24Z
date: 2026-09-15T11:26:13Z
expects: ack
waiting-on: curator
parked-on: john
---

# Требуется решение john: нажать merge на PR #448 — четыре гарда из пяти закрыты машиной и лентой, пятый (гард 4, док власти) — только твоя кнопка

john: единственное незакрытое звено этого треда — кнопка. Дверь перемерена моей рукой в `11:24:01Z`,
голова с момента вердикта не двигалась, сливаемость `CLEAN`. Ниже — гарды поимённо, ответ на ноту
двери о переехавшей базе и команда, которой это жмётся.

## 1. Что жмётся

**PR #448** — `fix(notifier): у каждого прогона в хвосте глушения печатается его время (тред 198)`,
голова **`09bef69202c72eda86fa9bf71b6b58db7754fd63`**, база `main`, `state=OPEN`,
`mergeable=MERGEABLE`, `mergeStateStatus=CLEAN`, метка `review` висит законно (круг закрыт и зелён).

```
gh pr merge 448 --squash --match-head-commit 09bef69202c72eda86fa9bf71b6b58db7754fd63
```

`--match-head-commit` здесь не украшение: он отказывает, если голова уехала между этим письмом и
твоим нажатием, — а именно на голову привязаны гарды 1 и 2 ниже. Заголовок squash — заголовок PR
выше (Conventional Commits, мой выбор как curator).

## 2. Гарды — `pnpm protocol merge-gate --ref origin/main --pr 448 --review-workflow 'Claude PR Review'`, прогнан мной в `11:24:01Z`

- **гард 1 `ok`** — `approved on 09bef69 by github-actions — inside the round 34960973615 of 'Claude
  PR Review' on this head (11:00:47Z…11:06:01Z)`. Вердикт `approve` лежит ВНУТРИ окна закрытого
  успешного круга на ТЕКУЩЕЙ голове — это письмо `reviewer-pr` в этой ленте от `2026-09-15`
  («Находок по критериям нет»), круг заведён событием `pull_request`, не `workflow_dispatch`;
- **гард 2 `ok`** — `2 check(s) green: review=SUCCESS, checks=SUCCESS` на той же голове
  `09bef6920` (прогон `checks` — `34960167537`);
- **гард 3 — закрыт ТВОИМ ЖЕ словом, а не классом.** Решение лежит в этой ленте, письмо curator
  `2026-09-13T17:16:30Z`, дословно: **«ПРАВИМ — ветвь A»**; постановка `dev-core` — следующим
  письмом там же. Это восхождение прямое, и полевой класс («измеренный дефект, новой нормы не
  вводит») ему не нужен. Отдельно про кусок, пришедший СВЕРХ постановки (стык с #443, §3 письма
  `dev-core`): исполнитель сказал своими словами о своём диффе, что новой нормы он не вводит, и
  ревьюер подтвердил то же самое ЧТЕНИЕМ диффа отдельным абзацем — «Дифф этого мерж-коммита новой
  нормы не вводит», с перечнем того, чего в нём нет (ни поля, ни ключа конфига, ни формы сообщения,
  ни права, ни шага маршрута, ни снятия запрета);
- **гард 4 `STOP`** — `john merges this one — it changes .github/workflows/notifier-watch.yml`.
  Это и есть причина письма: док власти, и моя рука здесь остановлена по построению. Объявлено это
  было ДО работы — в постановке `2026-09-13` («merge этого PR — кнопка john, и это замерено:
  `powerDocuments` = `["PROTOCOL.md","REVIEWER.md",".github/workflows"]`»), а не обнаружено у кнопки;
- **гард 5** — след merge пишу я ближайшим письмом в этот тред после твоего нажатия.

## 3. Нота двери о переехавшей базе — отвечена ЗАМЕРОМ, а не рассуждением

Дверь печатает ноту: `the base moved AFTER the credited checks started: 7547076 committed
11:07:49Z, 'checks' started 10:51:20Z` — то есть зелень гарда 2 читает дерево, которое уже не
является результатом ЭТОГО слияния. Это законная нота, и ответ на неё измерен `dev-core` локально
собранным слиянием, головы не трогая (`09bef6920` + `origin/main`, коммит `441a0ac48`, никуда не
запушен, дерево возвращено):

- конфликтов нет, пересечения по путям нет вовсе: севший #426 лежит в `packages/**` и `docs/**`,
  три пути этого PR — `.github/scripts/notifier-mute.sh`, `.github/scripts/notifier-mute.test.sh`,
  `.github/workflows/notifier-watch.yml`;
- `bash .github/scripts/notifier-mute.test.sh` на слиянии — **118/118**;
- `vitest` по шести затронутым файлам — **27/27**;
- **полная сюита на слиянии** — **248 файлов, 4285/4285**, 279 с.

Мой контроль в `11:24:01Z`: `origin/main = 7547076577f4a8751db93baa2e15b66ac2cda661` — тот самый
коммит, на котором слияние и мерилось. **Условие у кнопки ровно одно и оно читается одной строкой:**
если к моменту нажатия `origin/main` уже не `7547076`, замер §3 протух — тогда не жми, верни ход мне
письмом, и мерить слияние заново будет мой такт. `--match-head-commit` этого не ловит: он сторожит
ГОЛОВУ, а не базу.

## 4. Ответ `dev-core` на §3 его письма — предмет мой, и он ЗАКРЫТ без PR

Находка: переехавший на запасную учётку круг ревью теперь приходит ЗЕЛЁНЫМ и годится гарду 1
(круг `34960973615` — ровно такой, и он же якорит гард 1 выше); прежняя практика «упёрлись в лимит —
метку не вешай, жди сброса» стала неверной, а её обратная сторона дороже: «круг безнадёжен»
провоцирует ребейз, ребейз двигает голову и убивает круг с уже лежащим вердиктом.

**Замерено грепом, а не памятью:** этой практики НЕТ НИ В ОДНОЙ норме репозитория.
`grep -rniE "запасн|фолбэк|fallback|лимит|resets"` по `PROTOCOL.md`, `REVIEWER.md`, `docs/roles/**`
даёт ноль строк про цвет круга и про запрет метки: в карточках `метка НЕ вешается` встречается
трижды и все три — про другой класс (PR, правящий `claude-review.yml`, и несовпадение блоба в
merge-ref). Правило жило только в памяти curator и этим же тактом перевёрнуто там (заметка
`fallback-step-reddens-a-successful-round`, блок `15.09 10:53Z`: первое настоящее срабатывание
переезда после #433 дало зелёную джобу и годный вердикт).

Поведение кода при этом уже записано где положено — `docs/protocol-reference.md`, раздел «Лимит
учётки ревьюера»: «переезд с доставленным вердиктом → зелёный», и прямо названо, что **первое
срабатывание в поле приходит ЗЕЛЁНЫМ и merge его не ждёт**. Правка `continue-on-error` стои́т на
ОБОИХ шагах ревьюера — сверено по `origin/main`: `claude-review.yml:271` (основной) и `:729`
(запасной). Итог: **чинить нечего и PR открывать не под что** — ни одна норма отменённого правила не
несёт, а док, который несёт верное, уже в `main`. Предмет закрыт этим абзацем.

## 5. Чего я не делала

- **merge не жала** — гард 4 `STOP`, кнопка не моя ни при каких фактах;
- **головы не двигала**: ни коммита, ни push, ни метки (снимать/вешать нечего — голова с момента
  approve стои́т, круг закрыт и зелён);
- **в прогоне не стояла**, чужих PR не трогала, тред не закрывала;
- **PR под §4 не открывала** — измерено, что открывать не под что;
- рабочее дерево чистое, незакоммиченного нет.

## 6. Ход и парк

**`waiting-on → curator`, `--parked-on john`, `--expects ack`.** Ход остаётся у меня, потому что
вопрос несу я; тред заморожен на человеке намеренно — двигаться нечему, пока кнопка не нажата, и
любое действие до неё (ребейз, перевешивание метки) только убьёт годный вердикт.

**Что я сделаю после твоего слова:** нажал — пишу след гарда 5 и сверяю `base.ref` контрольным
чтением, затем закрываю тред приёмкой (полевая приёмка постановки — два отказа РАЗНЫХ PR в пределах
900 с рождают ДВА письма — читается уже на живом смотрителе, и это мой следующий такт). Сказал
«не жму» или база уехала — верни ход мне письмом, и я перемеряю слияние и доложу заново.

