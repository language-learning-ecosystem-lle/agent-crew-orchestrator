---
from: curator
worker: claude-code
session: 0501a865-bfa4-457b-8034-82a3f55b2c3b
raised: 2026-09-13T17:27:02Z
date: 2026-09-13T17:31:53Z
expects: ack
waiting-on: curator
parked-on: john
---

# ТРЕБУЕТСЯ РЕШЕНИЕ JOHN: #399 стои́т под твоей кнопкой — голова `66566e54a`, база догнана, дифф не тронут; жми merge (гард 4 — `.github/workflows/**`)

Твоё слово (чат ~17:12Z) исполнено: ветка перебазирована, по существу не изменено ничего. Проверила
ЭТО СВОЕЙ РУКОЙ, а не по докладу исполнителя — ниже факты с командами. Второй вопрос, ждущий тебя
пятый такт, — в §4; он отдельный и в кнопку не входит.

## 1. Что я перемерила сама на голове `66566e54a23f32dc3eb885c0d8d578b6c29995cf`

**База догнана — машинно, а не «перебазировал».** `git merge-base --is-ancestor origin/main
66566e54a` → YES; `origin/main` = `050e642a239b7209719bc29d6229e8251c96dd3b`. `mergeable: MERGEABLE`,
`mergeStateStatus: UNSTABLE` — единственная причина «unstable» на момент письма названа в §3.

**Дифф переезда учётки не изменился НИ НА СТРОКУ.** Против уже позеленевшей головы `2aa7d0ac`
(прогон `34770409091` → `success`) `git diff --name-only` печатает РОВНО ОДИН путь —
`docs/journal/dev-core.md`. Деревья по всем несущим путям тождественны по хешу:

```
.github                     01580758f5be6faa6397ba1abad2ae728e8aafcd  — ИДЕНТИЧНО
packages                    c159fb2aafdb1fa88b8ffabc288e1d7020646020  — ИДЕНТИЧНО
docs/protocol-reference.md  cd8bf42c27f252680cc0b79b255b9f14b6bfd686  — ИДЕНТИЧНО
PROTOCOL.md                 1955b297baf4f9a79e32a9f0b41a858ca0d65da9  — ИДЕНТИЧНО
REVIEWER.md                 731ddebf6ca7f66f8b98db66106d36f4f5dff6cb  — ИДЕНТИЧНО
agent-protocol.json         ef2f9b59832bace8d987316465fdeafba0d6b7ba  — ИДЕНТИЧНО
docs/roles                  a3679afc23296f26baff80f6a8b654af0989cc1a  — ИДЕНТИЧНО
```

Практическое следствие для твоей кнопки: **то, что ты будешь читать глазами, ты уже читал** — от
предыдущей головы тут отличается один markdown журнала и больше ничего.

**Моё же замечание из msg-017 («удалений быть не должно — иначе потеря чужой записи») выполнено.**
`numstat` по спорному пути против КАЖДОГО родителя merge-коммита: против ветки `2aa7d0ac` — `79 / 0`,
против `main` `050e642a2` — `79 / 0`. Ноль удалений с обеих сторон значит, что обе записи (#401,
тред 189, и запись треда 193) лежат целиком — ни одна не выбрана вместо другой. Маркеров конфликта
в дереве головы нет (`git grep` по трём формам — пусто).

**Прочие числа диффа против `main`** — это сам предмет PR, а не потеря журнала:
`.github/workflows/claude-review.yml` `242 / 4`, `.github/scripts/review-delivery.integration.sh`
`8 / 1`, `docs/protocol-reference.md` `38 / 0`.

## 2. Состояние двери — построчно, `merge-gate` на этой голове

`pnpm protocol merge-gate --ref origin/main --pr 399 --review-workflow 'Claude PR Review'`:

- **гард 4 — STOP, и это правильный STOP:** `john merges this one — it changes
  .github/workflows/claude-review.yml`. Дверь называет доки власти данными (8 путей, `.github/workflows`
  объявлен `powerDocuments`). Моей руки на этой кнопке нет по построению;
- **гард 1 — STOP машинно, и его закрываешь ТЫ ЧТЕНИЕМ.** Автоматический круг такому PR не положен
  по построению: действие самопропускается, пока файл ветки не совпал с дефолтной. Метку `review`
  я не вешала и вешать не надо — круг вернёт пустой прогон, а не вердикт (смотритель подтвердил это
  трижды: msg-008, msg-012, msg-015). Человеческое чтение здесь — твоё, и §1 говорит, на каком
  дереве: дифф побайтово тот же, что на `2aa7d0ac`;
- **гард 2 — по §3;**
- **гард 3 — восхождение есть:** тред `193-reviewer-token-account-unreadable` в описании PR, решение
  твоё, в ленте (msg-005 и msg-017);
- **гард 5 — след merge в треде.** Его несёт тот, кто нажал. Нажмёшь — скажи в тред или мне, я
  оформлю след поимённо.

## 3. `checks` — ЗЕЛЁНЫЙ, и он на ЭТОЙ голове

Перемерено моей рукой в 17:31:04Z, одной командой перед отправкой письма (у конвейера я не стояла —
письмо писалось всё это время):

- чек `checks` на коммите `66566e54a23f32dc3eb885c0d8d578b6c29995cf` — `conclusion: success`,
  `status: completed`, закрыт 17:31:00Z, id `103761516388`;
- прогон [`34771392536`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34771392536),
  заведён 17:22:24Z;
- `mergeStateStatus` перешёл `UNSTABLE → CLEAN`, `mergeable: MERGEABLE`.

**Гард 2 закрыт машинно:** `merge-gate` на этой голове печатает `ok guard 2 · green checks on the
same head: 1 check(s) green: checks=SUCCESS`. Сверялся полный SHA, а не семь символов.

**Стоя́т ровно два STOP, и оба — твои:** гард 1 (`no approve verdict` — закрывается твоим чтением,
автоматического круга этому PR не положено) и гард 4 (`john merges this one`). Гард 3 восходит,
мержабельность `CLEAN`. Больше в фактах не мешает ничего.

## 4. Второй вопрос, он ждёт тебя пятый такт — «делай» или «оставь»

Предмет: **если ЗАПАСНОЙ круг тоже умрёт лимитом, доклад назовёт причину неправильно.** Я
перемерила это чтением кода головы, а не пересказом:

- **переезд СРАБОТАЕТ на сегодняшней форме смерти — это уже в диффе.** Шаг `limit` ловит лимит по
  ТРЁМ признакам, и третий заведён именно сегодняшним фактом: запись `type=rate_limit_event`;
  `api_error_status=429` последней записи; **либо** `is_error == true` вместе с текстом
  `session limit|usage limit|rate limit|limit reached` в `result`. Сегодняшний круг (`subtype: success`,
  `is_error: true`, `total_cost_usd: 0`, текст про session limit, 429 не нёс вовсе) попадает под
  третий;
- **а вот ДОКЛАД о смерти запасного круга — нет.** `review_failure_cause()` в
  `.github/scripts/review-delivery.sh` (строки 467–481 головы `66566e54a`) знает класс `limit`
  только по `api_error_status == "429"`; сегодняшняя форма проваливается в ветку `is_error == true`
  и называется `error`. То есть человек прочтёт «ошибка круга» там, где на деле второй лимит.

Дефект не в кнопке: **#399 и без этой правки лучше сегодняшнего дня**, поэтому я НЕ предлагаю
задерживать merge. §3.8 постановки велит объявлять такую надобность ДО правки — объявлена dev-core
в msg-014 §6 и msg-018 §7, перемерена мной здесь. Нужно одно слово: **«делай»** (ставлю dev-core
отдельной работой после merge) или **«оставь»** (закрываю предмет и не возвращаюсь к нему).

## 5. Цена ожидания — замером, и она не риторическая

База #399 разъезжалась СЕГОДНЯ пять раз: 14:39Z → 15:37Z → 16:28Z → 17:03Z → и четвёртый ребейз
в 17:22Z. Стык каждый раз один и тот же — общий хвост `docs/journal/dev-core.md`, куда дописывают
все. Каждое расхождение стоит подъёма роли и полного прогона `checks` (≈10 минут). Интервал между
соседними — от ~9 до ~58 минут.

Отсюда простое следствие для порядка действий: **между зелёным `checks` и твоей кнопкой каждая
минута — реальный шанс шестого расхождения**, и тогда всё сначала. Форма проблемы — предмет
`195-journal-tail-serializes-work`, он тоже на тебе, но сюда не тянется: там форма журнала, здесь
кнопка.

## 6. Что сделала я в этот ход и чего не делала

Сделала: перечитала ленту, перемерила голову, базу, тождество деревьев, нулевые удаления и маркеры
своей рукой; прогнала `merge-gate`; прочитала код `review_failure_cause` и шага `limit` на голове PR;
сняла зелень `checks` контрольным чтением перед отправкой; несу под кнопку. Не делала: не мёржила
(гард 4), не вешала метку `review` (круг самопропустится и вернёт пустой прогон), диффа не касалась,
парка на прогоне не ставила и у конвейера не стояла — прогон дозрел, пока писалось письмо.

---

**Ход — тебе, john: `checks` зелёный на `66566e54a23f32dc3eb885c0d8d578b6c29995cf`, ждать нечего —
перечитай дифф (он побайтово тот же, что ты видел на `2aa7d0ac`) и жми merge. Вторым ответом —
«делай»/«оставь» по §4.** Merge только squash, заголовок Conventional Commits — годится заголовок
самого PR: `feat(review): круг ревью переезжает на запасную учётку, когда основная ответила лимитом
(тред 193)`. И **секрет `CLAUDE_CODE_OAUTH_TOKEN_FALLBACK` кладётся твоей рукой** — без него переезда
не будет, но и хуже сегодняшнего дня не станет: шаг назовёт причину словами (`fallback=0`,
«секрета в репозитории нет — переезжать не на что»).

`waiting-on → curator`: вопрос несу я, ход остаётся у меня до твоего слова.

