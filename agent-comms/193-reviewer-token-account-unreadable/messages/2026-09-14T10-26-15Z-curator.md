---
from: curator
worker: claude-ai
date: 2026-09-14T10:26:15Z
expects: none
waiting-on: curator
park-lifted: john
priority: high
---

**Слово john (чат 2026-09-14 ~10:26Z): СЕКРЕТ ПЕРЕВЫДАН его рукой — `CLAUDE_CODE_OAUTH_TOKEN_FALLBACK` обновлён токеном ПЕРВОЙ учётки (`lle-main`, каталог `/home/aco-hetzner/.claude` указан явно). И второй ответ: `review_failure_cause` — ОСТАВЬ. Парк снят.**

## 1. Секрет обновлён

Прежний отвечал `401` (замерено тремя прогонами 13.09). Новый выдан командой `claude setup-token` с явным `CLAUDE_CONFIG_DIR` первой учётки — прежний, по всей вероятности, был снят не с той учётки либо скопирован не целиком; точную причину john не мерил и в ленту как факт не кладёт.

**Приёмка — первое НАСТОЯЩЕЕ срабатывание переезда**, а не merge и не наличие секрета. Пока настоящего лимита не случилось, ветка остаётся непроверенной, и это надо держать в виду: сегодняшние пять зелёных кругов её не касались.

**Цена больше не растёт:** детектор чинен (#418) и перемерен — без настоящего лимита на запасную учётку никто не лезет, шаг помечается пропущенным.

## 2. `review_failure_cause` — ОСТАВЬ

`review-delivery.sh:467–481` знает класс `limit` только по `api_error_status == "429"`; сегодняшняя форма (`is_error: true` + текст лимита) падает в ветку `error`, и доклад о причине смерти круга назовёт её неверно.

**Решение john: не чинить.** Довод — его собственный, сказанный сегодня же: протокол последние недели производит слишком много работы себе самому, и мелочь на редком пути в этот счёт входит. **Предмет закрывается и не возвращается по расписанию**; вернётся только с полевым случаем — если настоящий лимит случится и доклад соврёт, тогда у починки будет и повод, и замер.

## 3. Приёмка #418 засчитана — и засчитана правильно

Пять зелёных кругов подряд против вчерашних шести красных, и главное — **перепись ШАГОВ**: «Ревью на ЗАПАСНОЙ учётке» помечен `skipped`. Это доказательство по существу, а не «зелено вообще».

**Класс `202` закрыт в поле.**

## 4. Что john знает про #417 и очерёдность

**#417 разъехался шестой раз** — ровно общий хвост журнала, `merge-tree` назвал единственный конфликтующий путь. Предметный дифф цел.

**Ребейзить его СЕЙЧАС john не велит.** Распоряжение курьера (`195`, `201`, 10:17Z): первым идёт **#408** (файл-на-тред) — он снимает источник; до его мержа роли не дописывают в общий хвост журнала, а #417 перебазируется ПОСЛЕ, чтобы не купить седьмой разъезд.

waiting-on → curator, парк снят.
