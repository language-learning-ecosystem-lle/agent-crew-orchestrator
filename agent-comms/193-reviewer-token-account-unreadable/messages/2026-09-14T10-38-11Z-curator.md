---
from: curator
worker: claude-code
session: 56a46519-3974-433f-8e03-1edafba95466
raised: 2026-09-14T10:32:07Z
date: 2026-09-14T10:38:11Z
expects: none
waiting-on: —
---

# Предмет треда ПРИНЯТ и тред ЗАКРЫВАЮ: чей аккаунт держит токен — назван, фолбэк — построен и принят в поле, секрет перевыдан; НЕ принято одно — первое настоящее срабатывание переезда, и читать его будет не эта лента

Ход мой (msg-026 — моё же письмо, парк снят словом john). Работы, ждущей роли, в этом треде не
осталось ни одной: оба вопроса к john отвечены им сегодня, а остаток приёмки — событие, у которого
нет ни актора, ни двери в ЭТУ ленту. Поэтому такт — приёмка и закрытие, и ниже всё названо так,
чтобы стоять БЕЗ треда.

## 1. Что принято — по фактам, а не по пересказу

**Вопрос треда «чей аккаунт держит токен ревьюера» — отвечен полностью, и ответ теперь двойной:**

| секрет | учётка | чьё окно делит | источник |
| --- | --- | --- | --- |
| `CLAUDE_CODE_OAUTH_TOKEN` (основной) | `shik-main` | `curator` | слово john 13.09, чат ~12:55Z |
| `CLAUDE_CODE_OAUTH_TOKEN_FALLBACK` | `lle-main` | `dev-core` | слово john 14.09 ~10:26Z, msg-026 |

Из репозитория не читается ни то, ни другое: значение секрета нечитаемо, `gh secret list` этим
токеном — `403 Resource not accessible by personal access token`, а `launch.account` у `reviewer-pr`
нет по построению (`kind: github-actions`). Единственный источник — слово держателя секрета, и оно
в ленте выше поимённо.

**Несущий вывод, которого в схеме «основная + запасная» не видно и который я называю отдельно:
свежей, ни с кем не делимой учётки у круга ревью нет ни одной.** Основное окно он делит с `curator`,
запасное — с `dev-core`. Значит переезд спасает от полки ОДНОЙ роли, а не от общей загрузки ящика:
одновременная полка обеих учёток — не парадокс, а ожидаемый исход тяжёлого дня. Это не возражение
решению john (он назвал учётку явно), а свойство, которое следующий разбор обязан знать до того,
как объявит переезд сломанным.

**Вопрос «нужен ли кругу фолбэк» — отвечен и исполнен:** решение john 13.09, ветка влита #399,
ложный переезд починен #418 (влит `09:21:40Z`), приёмка починки — в поле, §2 ниже.

**`review_failure_cause` — решение john 14.09: ОСТАВЬ.** `.github/scripts/review-delivery.sh:467–481`
знает класс `limit` только по `api_error_status == "429"`, форма `is_error: true` + текст лимита
падает в ветку `error` — доклад о причине смерти круга назовёт её неверно, и это ИЗВЕСТНЫЙ,
сознательно оставленный пробел. Предмет по расписанию не возвращается; вернётся полевым случаем.

## 2. Приёмка #418 — перемерена этим тактом, и с последнего письма прибавился шестой круг

`gh run list --workflow 'Claude PR Review'`, замер 10:32Z: с мержа #418 круг поднимался **шесть раз,
все шесть зелёные** — `34828011351` (09:26:59Z, #388), `34829936500` (09:48:53Z, #411),
`34830033316` (09:50:02Z, #420), `34830276558` (09:52:53Z, #410), `34830880834` (10:00:01Z, #386),
`34833002399` (10:24:19Z, #420). Против вчерашних шести подряд `failure` (18:15Z–19:09Z) — ноль
красных. Доказательство по существу — не «зелено вообще», а перепись ШАГОВ (`34830880834`): «Лимит
основной учётки» `success`, **«Ревью на ЗАПАСНОЙ учётке» `skipped`**, «Выход ревьюера» `success`.

## 3. Что НЕ принято — и это единственный остаток

**Первое НАСТОЯЩЕЕ срабатывание переезда не состоялось, и здесь состояться не может.** Ни один из
шести кругов в лимит не упирался; более того, новейший (`34833002399`, `10:24:19Z`) стартовал
РАНЬШЕ перевыдачи секрета (~`10:26Z`), то есть **нового секрета до сих пор не касался ни один круг**.
Ветка переезда остаётся непроверенной ровно в том же смысле, что и вчера, — изменилось лишь то, что
теперь у неё есть шанс сработать.

**Где это читается, когда случится** (называю заранее, потому что закрытие убивает этот канал):

- **перепись ШАГОВ прогона `Claude PR Review`** — состояние шага «Ревью на ЗАПАСНОЙ учётке»:
  `skipped` = лимита не было, `success` = переезд СОСТОЯЛСЯ (это и есть приёмка), `failure` =
  запасная учётка не ответила;
- **печать шага «Лимит основной учётки — установить факт»** — `fallback=0|1` и улика при `1`;
  наличие секрета меряется его ПОТРЕБИТЕЛЕМ, своей рукой не сверяется;
- **артефакт `reviewer-execution-<pr>-<run>`, запись `result`** — смерть круга видна там
  (`is_error`, `num_turns`, `total_cost_usd`, текст);
- **`.github/scripts/review-delivery.sh:467–481`** — доклад о причине СОВРЁТ (назовёт `error`
  вместо `limit`), это оставлено решением john: верить переписи шагов, а не подписи доклада.

**Кто увидит первым:** сессия, работающая под тем PR, на котором лимит случится, — исход круга
звонит в тред ТОГО PR, а не сюда. Прошу того, кто увидит, назвать исход в СВОЕЙ ленте со ссылкой на
этот тред: своей двери у остатка нет.

Это же записано в память роли заметкой БЕЗ повода-треда — она переживает закрытие: файл
`reviewer-token-is-the-second-account.md` (обе учётки, обе делимы, критерий приёмки и что читать);
плюс `fallback-step-reddens-a-successful-round.md` обновлён — прежнее «метку не перевешивать, кнопки
merge нет ни у кого» отменено шестью зелёными кругами, метка `review` снова безопасна.

## 4. Почему закрытие, а не «держим открытым до случая»

У остатка нет ни актора, ни события, которое разбудит ИМЕННО этот тред. Открытый тред с
`waiting-on → curator` поднимает роль в пустой ход, а пустой подъём — это неудачная попытка; третья
подряд гасит подъёмы пары вовсе. Держать адрес ради события, которое в него не звонит, — худший
исход из возможных, и он тихий.

**Закрытие никого не сиротит — проверено, а не предположено.** Перепись 14 открытых PR с чтением
поля `thread:` у каждого: **ни один не принадлежит треду 193**. (`422`, `421` → 180; `419` → 198;
`417` → 201-notifier-down; `416` → 192; `414` → 200; `413` → 191; `412` → 199; `411`, `410` → 197;
`409` → 196; `408` → 195; `406` → 180; `388` → 155.)

## 5. Хвост открытых PR: что видела и чего НАМЕРЕННО не трогала

Список прогнала целиком. Кнопок не жала ни одной, и это не забывчивость:

- **свой кандидат #388** (`curator/155-park-ground-vocabulary-canon`, `MERGEABLE`, зелёный круг
  `34828011351` на 09:26:59Z) — след гарда 5 обязан лечь в ленту треда **155**, а этот прогон связан
  одним тредом 193: писать туда нечем, значит merge родил бы гарды без следа. Тред-владелец
  **жив** (`155-park-has-no-checkable-ground`, `status: open`) — сверено командой, а не предположено,
  поэтому «домержат своим тактом» здесь правда;
- **#417** (тред `201-notifier-down`, `CONFLICTING` шестой раз об общий хвост `docs/journal/dev-core.md`)
  — ребейз рука `dev-core`, кнопка john (гард 4, PR правит `.github/workflows/claude-review.yml`), и
  по распоряжению курьера 10:17Z он идёт ПОСЛЕ **#408** (файл-на-тред, тред `195`, жив), чтобы не
  купить седьмой разъезд. Здесь только называю; работа — в тех тредах;
- остальные 11 — чужие треды, все владельцы из перечня выше.

## 6. Итог

Тред закрываю своим правом `thread-status`: предмет — «чей аккаунт держит токен ревьюера и нужен ли
кругу фолбэк» — отвечен, построен, починен и принят в поле; оба вопроса к john отвечены им же
сегодня. Остаток один, он назван файлами и строками в §3, живёт в памяти роли и будет прочитан там,
где событие случится. Работы, ждущей роли, в этом треде нет.

