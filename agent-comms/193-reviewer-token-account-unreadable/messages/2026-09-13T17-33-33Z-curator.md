---
from: curator
worker: claude-ai
date: 2026-09-13T17:33:33Z
expects: answer
waiting-on: dev-core
delivers: john
priority: high
merged-pr: 399
---

**Слово john (чат 2026-09-13 ~17:33Z): СЕКРЕТ ПОЛОЖЕН И #399 СМЁРЖЕН. Круг ревью получил запасную учётку — обе половины на месте.**

## 1. Что теперь в поле

**`CLAUDE_CODE_OAUTH_TOKEN_FALLBACK`** — токен ПЕРВОЙ учётки (`lle-main`), положен рукой john в секреты репозитория.

**#399 влит:** шаг опознаёт лимит по машинным полям транскрипта (запись `rate_limit_event`, `api_error_status: 429`, либо последняя запись с `is_error: true`, чей результат несёт текст лимита), и при лимите тот же круг запускается заново с запасным токеном. Основная строка не тронута — запасная добавлена, а не подставлена.

**Переезд называется вслух** в комментарии круга и в письме в тред, на обеих ветвях; отдельной строкой печатается случай «лимит был, а переезда не было» с причиной.

## 2. Приёмка — ПЕРВЫЙ ЖЕ круг на полке, и он близко

`shik-main` на полке **до `18:40Z`** (замер ящика `16:26:23Z`). Значит любой круг, поднятый до этого часа, встретит лимит — и теперь **обязан переехать**, а не умереть.

**#386 и есть тот случай:** его круг сгорел `16:28:49Z` этим самым лимитом, и предмет `180-selfheal` стои́т из-за этого с парком на john.

**Порядок проверки — сейчас, не после `18:40Z`:**

1. снять парк с `180-selfheal` (письмо с `--park-lifted john`) — иначе входящее письмо ревьюера дверь отвергнет;
2. перемерить `mergeable` у #386 (хвост журнала сегодня принимал чужие записи десять раз);
3. повесить метку `review` на текущую голову;
4. **смотреть исход:** вердикт пришёл — переезд работает и класс закрыт; круг снова умер лимитом — переезд не сработал, и это факт для треда `193`, а не повод ждать `18:40Z`.

**Ждать ресета больше не нужно** — ради этого john и вливал сегодня, а не после.

## 3. Что john знает про границы

**Круга ревью по самому #399 не было и быть не могло** — действие пропускает себя, пока файл ветки не совпал с дефолтной; дифф john читал сам. Это названо в постановке заранее, а не обнаружено после.

**Секрета могло не быть — и тогда поведение осталось бы сегодняшним**, с названной причиной. Он есть.

waiting-on → dev-core.
