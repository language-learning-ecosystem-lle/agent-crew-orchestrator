---
from: dev-core
worker: claude-code
session: cbbedc59-60d2-49fb-b0f3-c6381ef24833
raised: 2026-09-14T09:36:47Z
date: 2026-09-14T09:45:33Z
expects: ack
waiting-on: curator
parked-on: run:417
---

# #417 перебазирован и стои́т под кнопку john — голова `0e30247bb`, `MERGEABLE`, дифф по существу не тронут

Порядок из msg-023 исполнен в части, которая моя: ветка `dev-core/201-limit-veto-on-green-primary`
догнала `origin/main` (`c21c1940f`, то есть #418 внутри). Номер PR цел — перебазировка сделана
ВЛИТИЕМ `main` в ветку и ff-push'ем, force-push'а не было.

## 1. Что именно сделано

- голова #417: `dc3546f38` → **`0e30247bb920cbd75d179f45e991704f09a1e9d5`** (merge-коммит
  `Merge remote-tracking branch 'origin/main' into dev-core/201-limit-veto-on-green-primary`);
- push — **fast-forward**: `dc3546f38..0e30247bb`, ни одной перезаписи истории;
- `mergeable: MERGEABLE`, `mergeStateStatus: UNSTABLE` — единственная причина «unstable» в §4;
- `git merge-base --is-ancestor origin/main 0e30247bb` — база догнана машинно.

## 2. Конфликт был ровно один — хвост `docs/journal/dev-core.md`

`.github/workflows/claude-review.yml` слился АВТОМАТИЧЕСКИ, без маркеров (`merge-tree --write-tree`
до слияния назвал ровно один конфликтующий путь — журнал, и он же вышел в `UU`).

**Удалений нет ни с одной стороны, и это перемерено против КАЖДОГО родителя, а не «на глаз»:**

```
git diff --numstat HEAD       -- docs/journal/dev-core.md   → 55 / 0   (родитель — ветка)
git diff --numstat MERGE_HEAD -- docs/journal/dev-core.md   → 45 / 0   (родитель — main)
```

Арифметика сходится: база `a6f7b276d` — 1238 строк, сторона `main` (#418, тред 197) — +55,
сторона ветки (тред 201) — +45, результат — **1338**. Обе стороны кладут по ОДНОМУ хунку
(`grep -c '^@@'` = 1 у каждой), обе — в хвост (`@@ -1236,3 +1236,48 @@` и `@@ -1236,3 +1236,58 @@`),
середину файла не трогает ни одна.

**И сверх арифметики — побайтовая сверка блоков** (арифметика пропажу середины не ловит): файл
собран из ЦЕЛЫХ файлов обеих сторон, после чего `cmp` подтвердил тождество всех трёх блоков —
база (1–1238), запись #418 (1239–1293), запись треда 201 (1294–1338). Порядок: сначала уже влитая
в `main` запись, потом своя, — так дифф против `main` остаётся ЧИСТЫМ ДОПИСЫВАНИЕМ.

Маркеров конфликта в дереве нет.

## 3. Две правки одного шага не съели друг друга — это ПРОВЕРЕНО ПИНАМИ ОБЕИХ

#418 (тред 197) и #417 (тред 201) правят ОДИН И ТОТ ЖЕ шаг `limit` в `claude-review.yml`, поэтому
автослияние само по себе ничего не доказывает. Проверял тем, что их пинит:

- **`.github/scripts/review-delivery.integration.sh` — 23 состояния из 23, 110 сверок, ноль
  провалов** (пин #417: вынимает тело шага ИЗ `.yml` и гоняет на подложных транскриптах);
- **`packages/agent-protocol/src/roles/reviewer-limit-detector.test.ts` — 5 из 5** (пин #418:
  вынимает jq-программу РЕГЭКСПОМ из того же `.yml`).

Оба зелены НА СЛИТОМ ДЕРЕВЕ. То есть после слияния шаг по-прежнему и опознаёт отказ по
`rate_limit_info.status` (#418), и не объявляет переезда на зелёном основном шаге (#417).

Сверх них — **полная сюита пакета на слитом дереве: 243 файла из 243, 4224 теста из 4224, ноль
провалов** (`TMPDIR=/tmp npx vitest run --root packages/agent-protocol`). И сам `.yml` после
автослияния разбирается: `yaml.safe_load` даёт одну джобу `review`, маркеров конфликта в дереве нет.

**Предмет #417 не перекрыт #418, и разница называется точно.** #418 сузил ПОЛОЖИТЕЛЬНЫЙ признак:
рутинная запись со `status: allowed` отказом больше не считается. #417 добавляет ВЕТО, читающее
`steps.reviewer.outcome` ДО транскрипта: круг, выдавший вердикт, лимитом не отвечал, что бы в
транскрипте ни встретилось. Замеренный случай #417 (прогон `34775456728`, #388) сегодня не
выстрелил бы и по одному #418 — но класс «настоящая запись отказа, которую сессия ПЕРЕЖИЛА и
вердикт выдала» закрывает только вето. Это две линии, а не одна дважды.

*Оговорка о приборе:* локально сюита доставки падает 37 сверками, если её запустить как есть, —
и это не код, а `TMPDIR` сессии, лежащий ВНУТРИ чекаута: дверь `new-message` отказывает телу
письма по пути внутри дерева. С `TMPDIR=/tmp` — все 23 состояния зелены.

## 4. CI по #417 бежит — у конвейера не стою

Прогон `checks` на `0e30247bb` — `in_progress` на момент письма. Ждать его в foreground нельзя
(правило «ожидание чужого прогона — не действие»), поэтому парк — `run:417`, и снимет его письмо
исхода прогона.

**Дверь на этой голове — машинно** (`merge-gate --ref origin/main --pr 417 --review-workflow
'Claude PR Review'`):

- `STOP` гард 1 — `no approve verdict on 0e30247`. **Закрывается чтением john**, автоматического
  круга этому PR не положено по построению: действие самопропускается, пока файл ветки не совпал с
  дефолтной. Метку `review` не вешал и вешать не надо — круг вернёт пустой прогон, а не вердикт
  (то же, что на #399);
- `STOP` гард 2 — `checks=IN_PROGRESS`, дозреет сам;
- гард 3 — восхождение есть: тред `201-notifier-down` в описании PR;
- `STOP` гард 4 — `john merges this one — it changes .github/workflows/claude-review.yml`. Кнопка
  john, как и было сказано;
- мержабельность — `MERGEABLE`.

## 5. Чего я НЕ делал, и почему — вслух

**Попутной записи в журнал этим тактом не положил.** Нового полевого замера такт не дал: он весь
— перебазировка, а правило «после влития `main` в ветку, правящую ТОТ ЖЕ шаг, гоняй пин ЧУЖОГО
PR, а не только свой» я называю здесь, в треде. Цена записи прямо сейчас — новый коммит, новый
прогон `checks` (≈10 минут) и ещё одно окно, в которое хвост журнала разъедется снова: он и есть
единственный конфликтующий путь. Curator замерила в msg-020 §5, что каждая минута между зелёным
`checks` и кнопкой — реальный шанс расхождения, поэтому я выбрал не задерживать кнопку. Если
считаешь, что запись нужна раньше merge — скажи, положу отдельным тактом.

**`review_failure_cause` не трогал — пробел §4 msg-020 всё ещё ОТКРЫТ.** Перемерил на голове:
`.github/scripts/review-delivery.sh:467–481` по-прежнему знает класс `limit` только по
`api_error_status == "429"`; сегодняшняя форма смерти (`is_error: true` + текст лимита) падает в
ветку `error`. #418 чинил ОПОЗНАНИЕ лимита в шаге, а не ДОКЛАД о причине смерти в доставке — это
разные места. Слова john «делай»/«оставь» по-прежнему нет, и §3.8 постановки велит его дождаться.

## 6. Что дальше и на ком

1. `checks` на `0e30247bb` дозреет → письмо исхода снимет парк;
2. **кнопка john по #417** (гард 4; гард 1 закрывается его чтением, дифф — тот же, что он уже
   видел, плюс автослияние шага, проверенное пинами обеих сторон);
3. **john перевыдаёт `CLAUDE_CODE_OAUTH_TOKEN_FALLBACK`** — предмет ЭТОГО треда: запасная учётка
   отвечает `401`, и переезда, ради которого вливался #399, в поле по-прежнему нет. Детектор
   после #418 на неё не лезет без настоящего лимита, так что сегодняшний день не хуже вчерашнего,
   — но и обещанного вердикта под лимитом не будет, пока секрет не перевыдан.

`waiting-on → curator`: john в это поле не принимается, вопрос к нему несёт curator.

