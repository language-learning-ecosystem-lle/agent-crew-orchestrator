---
from: curator
worker: claude-code
session: 7f6f11d1-afdc-404c-8741-e482be0419b1
raised: 2026-09-03T12:40:24Z
date: 2026-09-03T12:46:15Z
expects: none
waiting-on: —
---

**Предмет треда закрыт приёмкой: норма стоит в каноне, фикция «стыковой роли» убрана репо-вайд, тег объявлен. Тред закрываю правом `thread-status`, ход не передаётся ни одной роли.**

Читаю канон, а не письмо о нём: `msg-010` от `github` и слово john в `msg-011` — источники, а приёмка ниже снята с `origin/main` руками.

## 1. Контрольное чтение мержа — цель, а не ответ инструмента

- `gh pr view 235`: `state=MERGED`, **`base=main`**, `mergedAt 2026-09-03T12:35:11Z`, squash-коммит `40ff560fbf315c6593874fdaa795a93c47d64b6f`;
- тот же коммит — голова `origin/main` в моём чекауте (`git log --oneline -1`), то есть дифф лежит в каноне, а не только «инструмент сказал merged».

**Руку кнопки полем `mergedBy` не доказываю** — оно печатает `maysway` и за кнопку john, и за merge роли (токен контура — PAT того же аккаунта). Доказательство здесь другое и оно ДО мержа: живой вывод двери в `msg-008` с гардом 4 `STOP` («john merges this one — it changes PROTOCOL.md, docs/roles/curator.md, docs/roles/dev-core.md») плюс слово john, доставленное в `msg-011`.

## 2. Приёмка нормы: что стоит в каноне, поимённо и построчно

`git show origin/main:PROTOCOL.md` — подраздел **«Кто пересекает границу с объявлением (слово john 2026-09-03, тред `092-who-announces-the-tag`)»**, строка `313`. Четыре куска слова john на месте, каждый читается отдельно:

- `315` — пробел назван: ни одна роль этого дерева объявление не носит, «стыковой роли» здесь нет и не было;
- `317` — **целевое (в)**: смотрит ПОЛУЧАТЕЛЬ; «чтение границу не нарушает» с замером `403` на запись при свободно читаемых публичных метаданных; механизм строится у получателя, и «этот репозиторий для него не делает ничего и знать о нём ничего не обязан»;
- `319` — **требование говорить при отказе**, отдельным абзацем: не смогла прочитать теги — сказать вслух, иначе «молчание, неотличимое от порядка»;
- `321` — **временное (б)** как правда о сегодняшнем дне, со сроком снятия в самой строке;
- `323` — **отклонённое (а)** с причиной («роль в конфиге есть то, что демон поднимает на ящике»);
- `325` — измеренная цена ручного моста (2026-09-03, три стоявших предмета).

Указатель на строке `309` («Граница работ: наружу едет ТЕГ») теперь ведёт в этот подраздел, а не в несуществующую роль.

## 3. Фикция убрана — замером по всему дереву, а не «поправила четыре места»

`git grep -n -i "стыков" origin/main`:

- `docs/roles/curator.md`, `docs/roles/dev-core.md` — **ноль** совпадений;
- `PROTOCOL.md` — три (`315`, `317`, `323`), все **внутри нового подраздела** и все называют стыковую роль несуществующей. Раздел «Граница контура» (`399`) её больше не поминает;
- вне канона одно ложное совпадение слова: `docs/codex-kind-inventory.md:15` — «восемь стыков» про спавн codex, не про роль.

## 4. Открытые предметы треда разобраны поимённо — вторая дверь приёмки

1. **п. 1 постановки** (строка нормы (б)) — в каноне, кнопкой john. Закрыт;
2. **п. 2** (ссылки на стыковую роль) — замер §3. Закрыт;
3. **п. 3** (объявление `v0.2.10`) — работ не было: тег `agent-protocol-v0.2.10` → `5ef13a7c`, дерево = `origin/main:packages/agent-protocol`, `protocolVersion 25`; объявление донёс чатный curator по норме (б). Закрыт;
4. **механизм (в)** — **уезжает ДОМОМ, а не остаётся здесь.** Он строится у получателя, постановка донесена в его почту чатным curator, и приёмка предмета — первое письмо о новом теге, которое получатель положит СЕБЕ сам. В нашей ленте этого события не будет никогда: канон прямо говорит, что этот репозиторий для него ничего не делает и знать о нём не обязан. Держать `092` открытым ради чужой приёмки — держать адрес, в который некому писать;
5. **снятие временной строки (б)** — единственный предмет с условием и без исполнителя, и я называю его вслух, чтобы он не пропал молча. Условие записано в самом каноне, строкой `321`: «снимается в тот день, когда у получателя заработает проверка». **Триггер этого условия наблюдаем НЕ ЗДЕСЬ** — ни одна роль этого дерева не может увидеть день, когда чужая проверка заработала, и по построению не должна. Значит открытый тред адресом ему служить не может: факт принесёт john или чатный curator, и принесённый факт заводит СВОЙ тред с однострочным диффом `PROTOCOL.md` — снова кнопка john. Предмет не теряется потому, что условие лежит рядом с той строкой, которую оно снимает, а не в закрытом треде.

## 5. Почему закрываю, а не оставляю «на всякий случай»

Открытый тред с `waiting-on: curator` — кандидат на КАЖДОМ такте: кандидатом его делает сам факт `waiting-on = роль`, а приоритет лишь упорядочивает очередь. Он жжёт слот роли, пока жив, а слот у curator один. **Эта сессия и есть замер той цены:** поднята на `092`, где работ по предмету не осталось ни у кого. Закрытие треда = приёмка, право у роли есть.

## 6. Хвост открытых PR — измерен дверью, ни один не тронут

`gh pr list --state open` + `merge-gate --ref origin/main --review-workflow 'Claude PR Review'` по каждому, снято **2026-09-03T12:43Z**. Открытых шесть (со вчерашнего счёта ушли в `main` #235, #236, #217; пришёл #238):

- **#114** (тред `048-session-privileges`) — **единственный, на ком дверь НЕ сказала `REFUSED`**: гард 1 `ok` (approve на `1bc125a` внутри круга `33260927219` от 2026-08-29), гард 2 `ok` (`review=SUCCESS, checks=SUCCESS`), гард 4 `ok` (2 пути, доков власти нет), `mergeable=MERGEABLE (CLEAN)`. Остались только суждения — гарды 3 и 5. **Approve стоит на текущей голове пятые сутки**;
- **#237** (`078-main-checkout-branch`) — гарды 1 и 2 `ok` (круг `33756207334`, голова `af4a992`), **гард 4 `STOP`: `PROTOCOL.md`** ⇒ кнопка john;
- **#202** (`074-parallelism-and-domains`) — гарды 1 и 2 `ok`, **гард 4 `STOP`: `.github/workflows/checks.yml`**, и вдобавок `mergeable=CONFLICTING (DIRTY)` ⇒ ребейз, а ребейз аннулирует вердикт и требует нового круга;
- **#233** (`064-forget-the-consumer`) — гард 1 `STOP` (вердикта на `8be36b9` нет), гард 4 `STOP` (`.github/workflows/claude-review.yml`, `PROTOCOL.md`). Метку на него вешать нельзя: круг самопропустится, гард 1 здесь закрывается человеческим чтением;
- **#238** (`062-contour-boundary`) — `checks=SUCCESS` на `a67d382`, вердикта нет, доков власти в диффе нет: ход — навесить `review`;
- **#216** (`052-pr-template`) — голова уехала (approve на `bf704ce`, `3d36989`; голова `abf9fcb`), `checks=IN_PROGRESS`: после зелёного метку **снять и повесить заново**.

**Ни одного не тронула и меток не вешала:** прогон связан одним тредом `092`, а след гарда 5 обязан лечь в ленту треда-ХОЗЯИНА PR — писать туда из этого прогона нечем. Это не отговорка, а названная цена связки: **#114 стоит готовым пятые сутки, и снять его может только сессия, поднятая на `048-session-privileges`.**

## 7. Состояние

Рабочее дерево роли чистое (`git status --porcelain` пуст), незакоммиченного нет. Своей рукой в чужой репозиторий не ходила: ни ветки, ни коммента, ни PR. В соседний контур не ходила и его кредов не брала: `GH_TOKEN` в среде прогона не было, и читала я только тот файл секретов, который объявлен ЭТИМ инстансом (`hetzner`, `repo` = `agent-crew-orchestrator`) — `secrets.aco.env`; `secrets.lle.env` соседнего инстанса не трогала.

**`waiting-on → —`: ход не передаётся никому.** Следом за этим письмом закрываю тред командой `thread status --status closed --write` и сверяю результат чтением.

