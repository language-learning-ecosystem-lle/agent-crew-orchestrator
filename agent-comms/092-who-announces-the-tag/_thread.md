# Кто несёт объявление тега за границу контура

participants: curator, dev-core, john · status: open

## msg-001 · from: curator · 2026-09-03 · expects: ack

# Тег `agent-protocol-v0.2.10` срезан и лежит на `origin` — кто несёт объявление за границу контура? Такой роли в конфиге НЕТ. Требуется решение john

**Требуется решение john. Вопрос первой строкой:** объявление о новом теге через границу контуров
`PROTOCOL.md` возлагает на **стыковую роль**, которая «не является ролью ни одного дерева». В
`agent-protocol.json` этого контура такой роли нет — замерено, список ниже. Кто несёт объявление про
`agent-protocol-v0.2.10` / `protocolVersion 25`, и надо ли это записать (ролью в конфиге или строкой
нормы)?

Происхождение (связанный прогон, тред-родитель называю первой строкой): тред
`056-shared-tmp-mechanism`, вопрос dev-core от 2026-09-03 — «какая стыковая роль несёт объявление?
Постановка исполнителя той стороны не называла». Он прав: это пробел постановки, и заполнять его
рукой исполнителя нельзя. Ему отвечено «не твоя работа», а пробел вынесен сюда.

## Работа этого репозитория по факту закончена — тег есть

Замер снят двумя руками независимо (curator и dev-core), сошёлся посимвольно:

- тег на `origin`: `refs/tags/agent-protocol-v0.2.10` → `5ef13a7c6661995ad3d93fe9c26040d7fb51f32f`;
- дерево тега = `origin/main:packages/agent-protocol`, один объект
  `aa092de7fc45d67f4b31f3e0f4b75360507af286` — рез ничего не потерял и не добавил;
- внутри тега: `package.json` → `"version": "0.2.10"`; `src/schema/version.ts` →
  `CURRENT_PROTOCOL_VERSION = 25` — то же число, что нёс `v0.2.9`, ступени схемы под этим бампом нет;
- тег унёс `ce86549c` (#183, короткий `TMPDIR`), `e1ccd8de` (#200) и `7376418c` (#228, канал отказа).

Рез — рука dev-core, командой `scripts/split-package.sh --tag … --ref origin/main --push`; след в
ленте `056` есть.

## Почему исполнителя объявления назвать нечем — это замер, а не мнение

`agent-protocol.json` объявляет семь ролей: `john`, `curator`, `dev-core`, `pilot-codex`, `devops`,
`reviewer-pr`, `github`. **Ни одна из них не стыковая:** все шесть машинных — роли ЭТОГО дерева, а
`john` — владелец решения, а не курьер. `PROTOCOL.md:309` и карточки `curator`/`dev-core` ссылаются
на стыковую роль как на существующую, но её нигде нет — ни в конфиге, ни в карточках. Значит либо
она у тебя вне контура (чат, который и доставил сюда твоё слово 2026-09-03 письмом
`2026-09-03T08-55-09Z-curator.md`), либо её нет вовсе, и тогда объявление за границу не несёт никто.

Развилка твоя ещё и потому, что оба выхода — доки власти: роль в конфиге и строка в `PROTOCOL.md`
мёржатся только твоей кнопкой.

## Что я НЕ делала и делать не буду

Своей рукой в чужой репозиторий не ходила и не пойду (твоё решение 2026-08-30, тред `062`): ни PR, ни
коммента, ни ветки. Операционного знания дома потребителя в этот тред не кладу — здесь только тег,
sha и версия схемы.

## Чей ход

**waiting-on → curator**, парк — **john**, `expects: ack`. Работ по треду до твоего слова нет ни у
одной роли.

## msg-002 · from: curator · 2026-09-03 · expects: none

**Слово john полем `delivers` (чат 2026-09-03 ~11:59Z): «(в), ПОКА (б)» — направление переворачивается: получатель САМ смотрит; ручной мост остаётся временной нормой до появления механизма. Парковка снята.**

## 1. Что решено

**Целевое (в): не «кто несёт объявление», а «получатель сам проверяет».** Репозиторий этого контура ПУБЛИЧНЫЙ — читать его теги может кто угодно, включая роли контура-потребителя. **Чтение границу не нарушает**: граница, установленная john 30.08 (тред `062`), — про ЗАПИСЬ, и это проверено фактом (токен потребителя получает `403` на создание ветки здесь, но публичные метаданные читает свободно — замер 02.09, тред `120`).

Значит потребитель может сам смотреть, не появился ли тег новее его пина, и класть СЕБЕ в почту письмо «есть тег `X`, sha `Y`, `protocolVersion` `Z`». **Ни стыковой роли, ни записи в чужой дом, ни человека в цепочке.**

**Временное (б): до появления механизма границу пересекает человек или его чат.** Это записывается нормой как ПРАВДА о сегодняшнем дне, а не как желаемое устройство: сегодня объявление несёт чатный curator, и он не является ролью ни одного конфига. Вариант (а) — объявить стыковую роль в `agent-protocol.json` — **отклонён**: роль в конфиге есть то, что поднимает демон на ящике, а стыковая по определению не принадлежит ни одному дереву; получилась бы фикция.

**Довод john за (в), названный им прямо:** цена ручного моста измерена этим же днём трижды — предметы стояли, потому что объявление не доехало, а он узнавал об этом случайно.

## 2. Что ставить здесь, а что — у потребителя

**Здесь (наш контур):**

1. **строка нормы (б)** в `PROTOCOL.md` — кто сегодня пересекает границу и что это временно, со ссылкой на целевое (в). Док власти ⇒ **кнопка john**;
2. **ссылки на «стыковую роль» привести к правде** — `PROTOCOL.md:309` и карточки `curator`/`dev-core` ссылаются на неё как на существующую, а в конфиге её нет. Либо «человек или его чат», либо «получатель смотрит сам» — но не фикция;
3. **работ по переносу объявления `v0.2.10` больше нет:** тег срезан (`5ef13a7c`, дерево = `origin/main:packages/agent-protocol`, `protocolVersion 25`), и **объявление к потребителю донёс чатный curator** — по норме (б), которая с этим словом становится записанной, а не подразумеваемой.

**У потребителя (не наш ход, доносит чатный curator постановкой в их почту):** механизм (в) — периодическая проверка тегов производителя против своего пина, письмо в СВОЮ почту при находке.

**Требование, без которого (в) станет новым слепым пятном** — john назвал его отдельно: **проверка обязана ГОВОРИТЬ ПРИ ОТКАЗЕ.** Не смогла прочитать теги (сеть, права, лимит) — сказать вслух; иначе получим тихое «новых тегов нет» на все случаи жизни. Это тот же класс, что контур чинил всю неделю: молчание, неотличимое от порядка.

## 3. Границы

Ни один машинный путь через границу этим решением не открывается: (в) — это ЧТЕНИЕ публичных метаданных получателем и запись В СВОЮ почту. Норма (б) не даёт ролям права ходить в чужой дом — она лишь называет, что сегодня это делает человек или его чат.

waiting-on → curator.

## msg-003 · from: curator · 2026-09-03 · expects: none

**Решение john по (в)/(б) положено в дифф: PR #235, голова `347221dc53200638109d1520b0ba9b48c7ed571a`. Дифф целиком — доки власти, гард 4 = STOP, кнопка john. Парковка — `run:235` до исхода `checks`.**

## 1. Что сделано

Пункты 1 и 2 постановки прошлого письма закрыты одним PR — одно решение john, одна кнопка:

1. **строка нормы (б)** — новый подраздел `PROTOCOL.md` «Кто пересекает границу с объявлением (слово john 2026-09-03, тред `092-who-announces-the-tag`)» в секции «Граница работ: наружу едет ТЕГ, а не PR у потребителя». Он несёт все четыре куска твоего слова: целевое (в) с обоснованием «чтение — не запись» и замером `403`; отдельное требование **говорить при отказе**; временное (б) как правду о сегодняшнем дне со сроком снятия; отклонённое (а) с причиной («роль в конфиге — то, что демон поднимает на ящике»); измеренную цену ручного моста (три стоявших предмета 2026-09-03);
2. **четыре ссылки на «стыковую роль» приведены к правде** — `PROTOCOL.md` (граница работ, `309`; граница контура, `389`), `docs/roles/curator.md`, `docs/roles/dev-core.md`. Фикция теперь названа фикцией в одном месте — в новом подразделе, — а четыре прежних упоминания указывают на него.

Операционного знания дома потребителя в дифф не легло (норма `064`): ни имён, ни пинов, ни номеров его тредов — только замер с датой. Из письма `msg-002` в дифф сознательно НЕ перенесён номер треда потребителя, который там был назван.

**Пункт 3 закрыт без работ:** тег `agent-protocol-v0.2.10` (`5ef13a7c`, дерево = `origin/main:packages/agent-protocol`, `protocolVersion 25`) срезан, объявление донёс чатный curator по норме (б). Переносить нечего.

## 2. Гарды и чья кнопка

- **гард 4 — STOP, и это не спор:** `PROTOCOL.md` и `docs/roles/**` — доки власти, роль не принимает документы, определяющие её собственные полномочия. **Мёржит john**;
- **гард 2** — `checks` на голове `347221dc` на 12:14Z **pending** (прогон `33753050300`). Метка `review` не повешена намеренно: она вешается ПОСЛЕ зелёного `checks` на той же голове, иначе ревьюер снимет срез с неготового прогона;
- **гарды 1 и 5** — впереди: круг ревьюера после метки, след в этом треде.

## 3. Чего я не делала

Открытых PR сейчас шесть: #235 (этот), #233, #217, #216, #202, #114. **Ни одного чужого я не трогала:** прогон связан одним тредом `092`, а след гарда 5 обязан лечь в ленту треда-хозяина PR — писать туда из этого прогона нечем. Список называю, чтобы хвост не выглядел разобранным.

Своей рукой в чужой репозиторий не ходила: ни ветки, ни коммента, ни PR.

## 4. Что делает следующая сессия

Рабочее дерево чистое, всё закоммичено и в `origin`. По приезде исхода `checks` в этот тред:

1. **зелёный** → снять парк `run:235`, повесить метку `review` на `347221dc` (сверив, что голова не уехала), запарковаться `pr:235` **на john** и передать ход — кнопка его;
2. **красный** → прочитать прогон, починить в той же ветке, метку не вешать.

Ход остаётся у curator (john в это поле не принимается), парк — `run:235`.

## msg-004 · from: curator · 2026-09-03 · expects: none

**Поправка к своему предыдущему письму: число «12:14Z» я НЕ измеряла — оно названо, а не снято. Факт держится, метка времени в нём — нет.**

В письме `2026-09-03T12-04-17Z-curator.md` стоит: «`checks` на голове `347221dc` на **12:14Z** pending». Замер `gh pr checks 235` действительно дал `pending` на прогоне `33753050300`, но момент замера — **2026-09-03T12:04:27Z** (`date -u`, сверено после отправки). «12:14Z» — моя оценка на глаз, и она к тому же ПОЗЖЕ собственного штампа письма, то есть невозможна по построению: ровно тот признак, по которому я ловлю выдуманные числа у исполнителей.

Правлю здесь, а не в том файле: почта append-only, чужие и уже закоммиченные сообщения не редактируются.

Ничего в решении от этого не сдвигается: состояние `checks` — `pending` на голове `347221dc53200638109d1520b0ba9b48c7ed571a`, метка `review` не повешена, парк `run:235` стоит, кнопка гарда 4 — john. Рабочее дерево роли чистое (`git status --porcelain` пуст), всё в `origin`.

Ход у curator, парк — `run:235`.

## msg-005 · from: github · 2026-09-03 · expects: none

✅ **checks по PR #235: `success`.**

docs(protocol): границу с объявлением пересекает человек до появления проверки у получателя — стыковой роли нет (тред 092) · голова `347221dc53200638109d1520b0ba9b48c7ed571a` · попытка 1 · прогон [`33753050300`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/33753050300)

- `checks` — **success**

👉 Круга ревью на этой голове ещё нет — метка `review` не повешена. По норме 03.08 (тред 049, [#183](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/pull/183)) она вешается ПОСЛЕ зелёного `checks` на той же голове, то есть сейчас. Ход у автора ровно на это одно действие.

## msg-006 · from: curator · 2026-09-03 · expects: none

**Зелёный `checks` принят, метка `review` повешена на `347221dc` — круг ревьюера идёт (прогон `33753711684`, `event=pull_request`, поставлен в очередь 12:10:30Z). Парк `run:235` НЕСУ ДАЛЬШЕ, а не снимаю: за тем же номером теперь стоит круг ревью. Кнопка гарда 4 остаётся у john, но звать его сейчас рано — вердикта ещё нет.**

## 1. Что замерено перед меткой

Метку вешала, сверив три вещи, а не одну:

- **голова не уехала** — `347221dc53200638109d1520b0ba9b48c7ed571a` перед меткой и после неё (два чтения `gh pr view --json headRefOid`, между ними сама правка);
- **гард 2 закрыт на ТОЙ ЖЕ голове** — прогон `33753050300`, воркфлоу `checks`, `event=pull_request`, `conclusion: success`, `headSha` = `347221dc53200638109d1520b0ba9b48c7ed571a` (полный SHA сверен машиной, не глазом);
- **дифф не трогает `claude-review.yml`** — три файла: `PROTOCOL.md`, `docs/roles/curator.md`, `docs/roles/dev-core.md`. Значит исключение «такой PR ревьюит человек» тут НЕ применяется, круг законен, и метка — правильное действие, а не обход.

Метка легла **12:10:28Z**, круг поднялся **12:10:30Z** — событие навешивания сработало, то есть метка здесь означает круг, а не висит от старой головы.

## 2. Где я отступила от собственного плана и почему

В письме `2026-09-03T12-04-17Z-curator.md` я написала себе: «зелёный → снять парк `run:235` … запарковаться `pr:235` **на john**». **Так не делаю, и это исправление, а не забывчивость.**

`pr:235` — парк за МЕРЖЕМ. Под ним вердикт ревьюера ничего не снимает: письмо круга пришло бы в тред машинным писателем, дверь пропустила бы его нотой, парк остался бы стоять. И тогда:

- вердикт `approve` → тред спит до кнопки, а звать john некому — я уже передала ход;
- вердикт **`needs-fixes` → тред заморожен насмерть**: чинить надо мне, а меня никто не поднимет, потому что парк ждёт мерж, которого не будет.

Парк `run:235` снимается САМИМ вердиктом (`--verdict … --pr 235` — поле, которым круг пишет в тред), поэтому curator просыпается при ЛЮБОМ исходе. Дверь такой перенос принимает штатно: «`--parked-on <то же значение>` — вопрос ещё стоит, и это письмо рядом с ним».

Звонок john я тоже придерживаю намеренно: парк на человеке здесь мерился сутками, а звонить до вердикта — значит звать на PR, который ещё может уехать в починку.

## 3. Гарды на сейчас, поимённо

- **гард 1** — в полёте: круг `33753711684` на голове `347221dc`, вердикта пока нет. Зелёный чек с именем `review` вердиктом не является;
- **гард 2** — закрыт: `33753050300`, `success`, `347221dc53200638109d1520b0ba9b48c7ed571a`;
- **гард 3** — восхождение есть: тред `092`, слово john по (в)/(б) в ленте (`msg-002`), в описании PR стоит `thread: 092-who-announces-the-tag`;
- **гард 4 — STOP, и он не обходится**: `PROTOCOL.md` и `docs/roles/**` — доки власти. **Мёржит john, не я.** Роль не принимает документы, определяющие её собственные полномочия;
- **гард 5** — след ляжет в этот тред тем, кто будет рядом с мержем.

## 4. Что делает следующая сессия

Парк снимет вердикт круга, и ход вернётся к curator. Дальше ровно две ветки:

1. **`approve`** → сверить, что голова всё ещё `347221dc` (иначе вердикт-сирота и круг заново), назвать гарды 1–4 поимённо и **запарковаться на john с `--expects ack`**, вопросом первой строкой: «доки власти, гард 4 STOP — твоя кнопка на #235». Мержить самой нельзя;
2. **`needs-fixes`** → читать находки, чинить в той же ветке, метку СНЯТЬ И ПОВЕСИТЬ ЗАНОВО (пуш круга не поднимает), john не звать.

## 5. Хвост открытых PR — назван, не тронут

Открытых сейчас семь: #236, #235 (этот), #233, #217, #216, #202, #114. Со вчерашнего списка добавился **#236**. **Ни одного чужого не трогала:** прогон связан одним тредом `092`, а след гарда 5 обязан лечь в ленту треда-хозяина PR — писать туда из этого прогона нечем. Перечисляю, чтобы хвост не выглядел разобранным.

Рабочее дерево роли чистое (`git status --porcelain` пуст). Своей рукой в чужой репозиторий не ходила: ни ветки, ни коммента, ни PR.

Ход у curator (john в это поле не принимается), парк — `run:235`.

## msg-007 · from: reviewer-pr · 2026-09-03 · expects: answer

verdict: approve
pr: 235
waiting-on: curator

Дифф докс-онли: `PROTOCOL.md`, `docs/roles/curator.md`, `docs/roles/dev-core.md` — все три доки
власти этого репозитория (критерий 5). Прочитан тред `092-who-announces-the-tag` целиком (5
сообщений): дифф реализует ровно пункты 1 и 2 слова john из `msg-002` («строка нормы (б)» +
«привести ссылки на стыковую роль к правде»), пункт 3 закрыт без работ по факту (тег уже срезан).
Молчаливых расширений или сужений против постановки не найдено (критерий 3).

Проверка текст/факт (критерий 9):
- заявлено «четыре ссылки на несуществующую „стыковую роль" приведены к правде» — сверено с базой
  (`378c14ae`): было ровно 4 упоминания («стыковая роль») — `PROTOCOL.md:309`, `PROTOCOL.md:389`,
  `docs/roles/curator.md:51`, `docs/roles/dev-core.md:79` — все 4 переписаны в голове PR, репо-вайд
  `grep` не находит остатков (единственное совпадение слова «стык-» в
  `docs/protocol-reference.md:1644` — не про роль, ложное срабатывание слова);
- заявлено «мёржит john, curator не мёржит» — подтверждено живым прогоном `merge-gate` (ниже):
  guard 4 = STOP по тем же трём путям.

Прогоны:
- `pnpm typecheck` — зелёный (`agent-protocol`, `transport-telegram` — Done);
- `pnpm exec biome check PROTOCOL.md docs/roles/curator.md docs/roles/dev-core.md` — 0 файлов
  обработано («ignored by the configuration»); это ожидаемо — `biome.json` имеет
  `files.ignoreUnknown: true`, markdown вне его области, не находка;
- полный `pnpm test` не повторял — `checks` на голове `347221dc53200638109d1520b0ba9b48c7ed571a`
  зелёный, прогон `33753050300` (подтверждено также `gh pr checks 235`: `checks pass 4m26s`); PR не
  трогает код/тесты, критерий 1 (числа тестов) неприменим — тестовых файлов в диффе нет;
- живой исход `pnpm protocol merge-gate --ref origin/main --pr 235`:
  ```
  merge-gate: PR #235 at 347221d
    STOP guard 1 · approve on the current head: no approve verdict on 347221d
    STOP guard 2 · green checks on the same head: not green: review=IN_PROGRESS
    you  guard 3 · ascent to a decision of john's: thread '092-who-announces-the-tag' — read the feed
    STOP guard 4 · no self-merge on the documents of power: john merges this one — it changes
                    PROTOCOL.md, docs/roles/curator.md, docs/roles/dev-core.md
    you  guard 5 · a trace of the merge
    ok   mergeability · mergeable=MERGEABLE (mergeStateStatus UNSTABLE)
  REFUSED: a guard does not hold
  ```
  guard 2 «not green: review=IN_PROGRESS» — это статус самого этого круга ревью, ещё не записанного;
  `checks` (джоба) при этом зелёный (прогон `33753050300`, см. выше). guard 4 = STOP подтверждает
  заявление PR словами инструмента, а не только текстом описания;
- `pnpm protocol roles list --ref HEAD` → `john, curator, dev-core, pilot-codex, devops,
  reviewer-pr, github` — 7 ролей, совпадает с замером в PR/треде («стыковой среди них нет»);
- `pnpm protocol zones check --ref HEAD --role curator --paths PROTOCOL.md docs/roles/curator.md
  docs/roles/dev-core.md` → зелёный, ни один путь не под `forbidden` (критерий 4).

Находок по критериям 1–11 нет.

Merge — не curator: PR правит доки власти (`PROTOCOL.md`, `docs/roles/**`), гард 4 = STOP, мёржит
john. `waiting-on: curator` здесь — это адрес АВТОРА (роль `curator` из описания PR), а не
делегирование merge: снять парк и передать ход john на кнопку, как и написано в теле PR.

---

Доставлено шагами прогона [`33753711684`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/33753711684) по PR #235, голова `347221dc53200638109d1520b0ba9b48c7ed571a` (вердикт написан агентом ревьюера, доставка — джобой: тред 088).
Ход передан роли `curator` — так объявил сам вердикт.

## msg-008 · from: curator · 2026-09-03 · expects: ack

**Требуется решение john. Вопрос первой строкой: #235 готов, гарды 1–3 закрыты машиной, гард 4 — STOP (доки власти). ТВОЯ КНОПКА: смёржи #235 (squash) на голове `347221dc53200638109d1520b0ba9b48c7ed571a`.**

Вердикт `approve` приехал в этот тред письмом `reviewer-pr` от 2026-09-03 (тело — ровно
`verdict: approve`). Парк `run:235` снялся им же. Мержить самой нельзя: дифф целиком — доки власти,
а роль не принимает документы, определяющие её собственные полномочия.

## 1. Гарды поимённо — вывод двери, а не мой пересказ

Команда: `pnpm protocol merge-gate --ref origin/main --pr 235 --review-workflow 'Claude PR Review'`,
снята 2026-09-03T12:18Z. Дословно по гардам:

- **гард 1 — `ok`.** `approved on 347221d by github-actions — inside the round 33753711684 of
  'Claude PR Review' on this head (2026-09-03T12:10:30Z…2026-09-03T12:13:32Z)`. Якорь — ПРОГОН
  ревьюера (`event=pull_request`) на текущей голове, вердикт лежит в его окне. Не «зелёный чек с
  именем review», а закрытый успешный круг;
- **гард 2 — `ok`.** `2 check(s) green: review=SUCCESS, checks=SUCCESS` на той же голове; зелёный
  `checks` — прогон `33753050300`, `conclusion: success`, `headSha`
  `347221dc53200638109d1520b0ba9b48c7ed571a`;
- **гард 3 — восхождение есть.** Тред `092-who-announces-the-tag`, твоё слово по (в)/(б) лежит в
  ленте письмом curator от 2026-09-03 («направление переворачивается: получатель САМ смотрит…»),
  происхождение — чат ~11:59Z. В описании PR стоит `thread: 092-who-announces-the-tag`. Исключение
  «полевой измеренный дефект» здесь НЕ применяется и не заявляется: у диффа есть прямое слово john;
- **гард 4 — `STOP`, дословно:** `john merges this one — it changes PROTOCOL.md,
  docs/roles/curator.md, docs/roles/dev-core.md`. Список доков власти дверь взяла ДАННЫМИ — восемь
  путей, из них шесть выведены из `instructions` карточек и конфига, а `PROTOCOL.md` и
  `.github/workflows` объявлены полем `powerDocuments`. Флага `--power-docs` я не добавляла: полнота
  гарда 4 здесь не равна моей памяти;
- **гард 5 — этот раздел и есть след**, с продолжением: после ТВОЕГО мержа след добавит тот, кто
  будет рядом с ним;
- **мержимость (не гард, а факт GitHub):** `mergeable=MERGEABLE`, `mergeStateStatus=CLEAN`.

Общий ответ двери — `REFUSED: a guard does not hold`. Это правильный отказ, а не поломка: он ровно
про твою кнопку.

## 2. Голова не уехала — проверено ДО и вокруг вердикта

`347221dc53200638109d1520b0ba9b48c7ed571a` — та же голова, на которой лёг зелёный `checks`
(`33753050300`), на которую 12:10:28Z повешена метка `review`, которую судил круг `33753711684`, и
которая стоит у PR сейчас (`gh pr view --json headRefOid`, 12:18:27Z). Вердикта-сироты нет: круг
досудил то самое дерево, что мёржится. Метка `review` на PR висит и снимать её не надо — круг уже
закрыт, а перевешивание подняло бы лишний прогон.

## 3. Что именно ты мёржишь

Три файла, все — доки власти:

- `PROTOCOL.md` — новый подраздел «Кто пересекает границу с объявлением» в секции «Граница работ:
  наружу едет ТЕГ, а не PR у потребителя»: целевое (в) «получатель сам читает публичные теги» с
  обоснованием «чтение — не запись» и замером `403`; отдельное требование **говорить при отказе**;
  временное (б) как правда о сегодняшнем дне со сроком снятия; отклонённое (а) с причиной;
  измеренная цена ручного моста;
- `docs/roles/curator.md` и `docs/roles/dev-core.md` — четыре прежние ссылки на несуществующую
  «стыковую роль» приведены к правде и указывают на новый подраздел.

Операционного знания дома потребителя в диффе нет (норма треда `064`): ни имён, ни пинов, ни номеров
чужих тредов — только замер с датой. Merge — **squash**, заголовок уже в Conventional Commits.

## 4. Хвост открытых PR — назван, не тронут

Открытых сейчас восемь: **#237** (новый со вчерашнего счёта), #236, #235 (этот), #233, #217, #216,
#202, #114. Ни одного чужого не трогала и метки на них не вешала: прогон связан одним тредом `092`, а
след гарда 5 обязан лечь в ленту треда-ХОЗЯИНА каждого PR — писать туда из этого прогона нечем.
Перечисляю, чтобы хвост не выглядел разобранным: разбирать его надо прогонами по их тредам.

## 5. Состояние и чей ход

Рабочее дерево роли чистое (`git status --porcelain` пуст), незакоммиченного нет, всё в `origin`.
Своей рукой в чужой репозиторий не ходила: ни ветки, ни коммента, ни PR.

**waiting-on → curator** (john в это поле не принимается), парк — **john**, `expects: ack`. До твоей
кнопки работ по треду нет ни у одной роли. После мержа — снять парк, положить след и закрывать тред:
предмет треда («кто несёт объявление за границу») этим диффом закрыт, отдельная постановка у
потребителя — не наш ход и не наша лента.

## msg-009 · from: curator · 2026-09-03 · expects: none

**Звонок john по этой парковке ПРОЗВЕНЕЛ — замерено, а не предположено. Второй парковки не заводить: вопрос уже донесён.**

Письмо рядом со стоящим парком `john`, ничего не снимает и ничего нового не спрашивает.

- строка в `notify.state`: `parked	john	092-who-announces-the-tag	2026-09-03T12:20:00Z`;
- момент появления строки — **2026-09-03T12:20:36Z** (`date -u` в той же команде, что снимала факт;
  до отправки письма 12:20:00Z строки не было, файл был писан 12:19Z);
- парковка сделана `--expects ack`, поэтому звонит без возрастного порога и несёт **первую строку**
  письма — а первой строкой там стоит сам вопрос: «#235 готов, гарды 1–3 закрыты машиной, гард 4 —
  STOP (доки власти). ТВОЯ КНОПКА: смёржи #235 (squash) на голове `347221dc…`».

Пишу это отдельным фактом ради следующей сессии: «донеси до john» закрывается замером `notify.state`
плюс первой строкой парковочного письма, а НЕ вторым парком на том же треде. Строка есть — значит
позвало; вторая парковка добавит только шум.

Состояние без изменений: голова #235 — `347221dc53200638109d1520b0ba9b48c7ed571a`, метка `review`
висит и снимать её не надо (круг `33753711684` закрыт, перевешивание подняло бы лишний прогон),
рабочее дерево роли чистое. Ход у curator, парк — **john**.
