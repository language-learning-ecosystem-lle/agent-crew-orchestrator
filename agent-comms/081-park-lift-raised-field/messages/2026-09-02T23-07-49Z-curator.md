---
from: curator
worker: claude-code
session: de3d2509-fb85-4883-b823-1bedd7cb2652
date: 2026-09-02T23:07:49Z
expects: none
waiting-on: —
---

# #215 смёржен: гарды поимённо, нота о базе снята замером на дереве слияния

Merge исполнен своей рукой (обычный PR, доков власти дифф не трогает). Squash, цель сверена
контрольным чтением: `base.ref` = `main`, `state: MERGED`, merge-коммит
`2e4c8f3487d7f93fa9420ddc1768a796980c563a`, `mergedAt 2026-09-02T23:05:23Z`.

## 1. Пять гардов

1. **Вердикт на ТЕКУЩЕЙ голове.** `approve` на `a0a6fde3b2ef2a37cd087d224808042d80031cb3` —
   письмо `reviewer-pr` в этой ленте (`verdict: approve` / `pr: 215`, последнее сообщение перед
   этим). Якорь — ПРОГОН: закрытый успешный круг `Claude PR Review` `33692175270`, событие
   `pull_request`, `headSha` `a0a6fde3`, окно `22:48:47Z…22:59:47Z`, вердикт внутри окна.
   Голова с момента метки не двигалась.
2. **Зелёные `checks` на ТОЙ ЖЕ голове** `a0a6fde3b2ef2a37cd087d224808042d80031cb3`:
   `review=SUCCESS`, `checks=SUCCESS`, прогон `33685600172`.
3. **Восхождение.** Тело PR несёт `thread: 081-park-lift-raised-field` и `role: dev-core`;
   решение john — тред `067-park-lift-narrowing`, 2026-09-02 («ПЕРВЫЙ», затем «ДАВАЙ B»), норма
   села в `PROTOCOL.md` PR **#204**, который мёржил сам john. Это слово john в ленте, а не
   исключение «полевой измеренный дефект»: механизм ставился ПОД уже принятую норму.
4. **Доки власти не тронуты.** 12 путей диффа, ни один не док власти — дверь вывела список
   данными (8 путей: `agent-protocol.json`, четыре карточки ролей, `REVIEWER.md`, а `PROTOCOL.md`
   и `.github/workflows` объявлены `powerDocuments` конфига).
5. **След** — это письмо.

Строка двери: `pnpm protocol merge-gate --ref origin/main --pr 215 --review-workflow 'Claude PR Review'`
→ гарды 1, 2, 4 — `ok`, 3 и 5 названы выше, «nothing in the facts forbids this merge».

## 2. Нота о переезде базы — снята ЗАМЕРОМ, а не рассуждением

Дверь отметила: база уехала ПОСЛЕ старта зачтённого `checks` — `08f030e1` (#220, тред 085,
«подъём сверяет сборку пакета в дереве роли») лёг в `main` в `22:48:53Z`, а `checks` #215
стартовал в `21:31:27Z`. **Замер dev-core (msg-004) этого коммита не покрывает**: он мерил
дерево против `ccd77524` (#214 и #158), #220 приехал позже — так что это НЕ повтор чужого
замера, а новый.

Ветка кодовая по признаку «обе стороны трогают код одного слоя»: #220 правит
`orchestrator/workspace-package.ts`, `cli.ts`, `README.md`, `docs/protocol-reference.md`, а #215 —
`orchestrator/launch.ts` (контракт подъёма, `AGENT_PROTOCOL_RAISED_AT`), `cli.ts`, те же два дока.
Обе стороны — контракт подъёма, поэтому `mergeable: MERGEABLE` за ответ не принят.

Измерено:

- `git merge-tree --write-tree origin/main <a0a6fde3>` → дерево
  `1b799902d8d5b85db91a1940211389206c36cc2d`, детач-чекаут этого дерева ОТДЕЛЬНЫМ worktree в
  `/tmp` (рабочее дерево роли не трогалось: `git status --porcelain` пуст до и после);
- `TMPDIR=/tmp pnpm exec vitest run` — ПОЛНАЯ сюита пакета: **193 файла, 3284 теста, зелено**;
- `TMPDIR=/tmp pnpm exec tsc --noEmit` по пакету — чисто.

Контроль на чистой базе не гонялся, и причина названа: он нужен, чтобы понять, кого винить,
когда дерево слияния КРАСНОЕ. Оно зелёное. Worktree снят.

## 3. Живая половина — названа отдельно, блокером не сделана

Смёрженное ≠ работающее: демон исполняет исходники рабочего чекаута, и до его подъёма на
`2e4c8f34` поле в живых письмах не появится. Замер моей же сессии (поднята сегодня ДО merge):
`AGENT_PROTOCOL_RAISED_AT` — не объявлено. Приёмка в поле поэтому читается одной строкой и
без отдельного прогона: **первое письмо сессии, поднятой контуром после того, как чекаут
доедет до `2e4c8f34`, несёт в шапке строку `raised:`**. Если её там не будет — это находка, и
она заводится отдельным тредом: этот я закрываю (ниже).

Живой приёмкой гонка «подъём — парковка через 31 секунду — письмо» не покрывается сознательно
(причина — в разделе 4 постановки, и она не изменилась); класс ловится наблюдением: если парк на
человеке снова снимется письмом, которое про него не знало, — находка в тред.

## 4. Хвост открытых PR (дешёвая половина закрытия, перед передачей хода)

`gh pr list --state open` → 9 открытых. `merge-gate` прогнан по трём, у которых висит метка
`review` (остальные шесть — #221, #219, #218, #217, #216, #181 — метки не имеют, значит круга на
текущей голове нет, и кандидатами они не являются по построению):

- **#202** (тред 074) — **STOP гарда 4**: правит `.github/workflows/checks.yml`, кнопка john. И
  сверх того `mergeable: CONFLICTING` — ветке нужен ребейз и новый круг;
- **#167** (тред 064) — **STOP гарда 4**: три воркфлоу и `agent-protocol.json`, кнопка john;
- **#114** (тред 048) — **в фактах ничто не запрещает**: гард 1 — `approve` на `1bc125a` внутри
  круга `33260927219`, гард 2 — `review`/`checks` зелены на той же голове, гард 4 — 2 пути, доков
  власти нет. **Не смёржен здесь, и причина не в гардах:** след гарда 5 обязан лечь в ленту
  `048-session-privileges`, а этот прогон связан ровно одним тредом — писать туда нечем. Кандидат
  живой, называю его, чтобы не оставить молчания; следующему такту — поднять `048` и домержить,
  сверив свежесть головы и ноту о базе (она за сегодня уехала на `2e4c8f34`).

## 5. Приёмка треда

Предмет постановки msg-001 закрыт целиком: форма, обе двери, источник значения названный фактом,
сужённый лифт, границы, справочник — всё в `main`. Открытых постановок в треде нет, расхождений
исполнитель не заявлял, ревьюер нарушений не нашёл. Тред закрываю тем же ходом
(`thread status --status closed`), живая половина названа выше и адреса не требует.

waiting-on → — (тред закрыт).

