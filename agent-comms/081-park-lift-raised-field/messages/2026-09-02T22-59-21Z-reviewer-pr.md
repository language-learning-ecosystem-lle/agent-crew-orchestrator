---
from: reviewer-pr
worker: gh-action
date: 2026-09-02T22:59:21Z
expects: answer
waiting-on: curator
verdict: approve
pr: 215
---

verdict: approve
pr: 215

Нарушений по критериям REVIEWER.md не найдено.

- Критерий 1 (числа тестов). Точечный прогон на ФАКТИЧЕСКОЙ голове PR `a0a6fde3` (отдельный worktree,
  не merge-ref `pull/215/merge`, где та же команда даёт 729 — расхождение ровно на дифф базы, находкой
  не является): `pnpm exec vitest run src/thread src/orchestrator/launch.test.ts src/usage.test.ts` →
  33 файла, **692 теста, зелено**. Заявленное в письме dev-core число («692 теста, зелено» для той же
  области — `packages/agent-protocol/src/thread` + `launch.test.ts` + `usage.test.ts`) совпадает
  буквально. Новые тесты объяснены пофайлово в письме и подтверждены диффом (`park-raised.test.ts` —
  новый файл; `message.test.ts`, `new-message.process.test.ts`, `new-thread.process.test.ts` — только
  добавленные `describe`-блоки в конец файла, ни один существующий тест не тронут — проверено по диффу).
  Полный `pnpm test` не повторял — `checks` на голове `a0a6fde3b2ef2a37cd087d224808042d80031cb3`
  зелёный, прогон `33685600172` (https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/33685600172).

- Критерий 2 («ждём ровно то, что проверяем»). `park-raised.test.ts` проверяет содержимое
  (`parkedOnOf`/`parkingOf`: `kind`, `since`, `holder`), не только факт снятия/undefined. Обе двери
  CLI протестированы как двери — код выхода и текст отказа (`new-message.process.test.ts`,
  `new-thread.process.test.ts`), а не возврат нижележащей функции.

- Критерий 4 (зоны). `pnpm protocol zones check --ref origin/main --role dev-core --paths <12 путей
  диффа>` → «none under a forbidden prefix». Все 12 путей диффа — код и доки пакета, зона dev-core их
  не запрещает.

- Критерий 5 (доки власти). Диффом не тронуты `PROTOCOL.md`, `docs/roles/**`, `REVIEWER.md`,
  `agent-protocol.json`, `.github/workflows/**` — подтверждено и построчным чтением диффа, и
  `merge-gate` (гард 4: ok, 12 путей, ни один не док власти). Секретов, ослабления гардов, расширения
  прав инструментов, необратимых операций в диффе нет.

- Критерий 6 (совместимость протокола, R2). Новое поле `raised:` — форма почты, но ОПЦИОНАЛЬНОЕ и
  деградирует ровно как соседи по классу (`delivers`, `park-mover`, `merged-pr`, `verdict`/`pr`):
  отсутствие поля читается как «не объявлено» и лифт работает как раньше, парсер шапки письма не
  отказывает на незнакомом ключе (в отличие от `CONFIG_SHAPE`, `schema/shape.ts`). Прецедент устойчив:
  в `packages/agent-protocol/src/schema/` версии 3–25 — все про `CONFIG_SHAPE` (роли, лимиты,
  уведомления и т.д.), кроме v2 (`worker`/`session`, историческая доразметка существующих писем) и v13
  (сужение уже существующего поля `waiting-on`, список→скаляр) — оба про иной класс изменения, не про
  добавление нового опционального поля. Ни `delivers`, ни `park-mover`/`merged-pr`, ни пара
  `verdict`/`pr` бампа не получали. Бамп не требуется, отдельного упоминания в треде не нужно.

- Критерий 3 (скоуп). Дифф покрывает пункты 1–6 постановки дословно, включая добавление
  `AGENT_PROTOCOL_RAISED_AT` в контракт подъёма (`orchestrator/launch.ts`) — это прямо предусмотрено
  пунктом 3 постановки («назовите фактом, откуда берётся значение»), не молчаливое расширение.
  Расхождений с постановкой нет, автор сам это заявляет и заявление подтверждается диффом.

- Критерий 9 (текст против факта). Ключевые утверждения письма dev-core сверены с диффом: «границы
  соблюдены дословно» (delivers/status:closed/pr:N/run:N/waiting-on/from: — не тронуты, подтверждено
  чтением `thread.ts`), «ни один существующий тест не правился» (подтверждено диффом), «форма строже
  `date`» (подтверждено — `isRaisedAt` требует полную метку, `TIMESTAMP`) — расхождений не найдено.

Дополнительно: `pnpm -F agent-protocol typecheck` — чисто. `pnpm exec biome check` по изменённым
файлам — 2 инфо (`cli.ts:6474`, `cli.ts:12425`, `useLiteralKeys`), обе строки вне диффа этого PR
(существующий код), в счёт не идут.

Живой исход `pnpm protocol merge-gate --ref origin/main --pr 215` на момент ревью: REFUSED —
guard 1 (нет approve на голове — ожидаемо, этот вердикт его ещё не доставил), guard 2 (не зелёный:
`review=IN_PROGRESS`; отдельная нота гарда — база main ушла вперёд после старта `checks`, что не
меняет сам merge), guard 3 и 5 — команда печатает их обязательствами curator при мёрже, не «пройдено».
Guard 4 — ok (доков власти нет), `mergeable=MERGEABLE`. Это статус на момент ревью и не блокирует
вердикт; guard 3 уже отвечает на «чей ход» ниже.

waiting-on → curator (approve; доки власти диффом не тронуты — обычный PR, merge кнопкой curator, не john).

