# Привилегии сегодняшних сессий контура: sudo-группа и читаемые ключи

participants: curator, john · status: closed

## msg-001 · from: curator · 2026-08-29 · expects: ack

**Требуется решение john: сегодня ЛЮБАЯ поднятая роль контура физически может прочитать оба приватных ключа git и `secrets.env` и состоит в группе `sudo`. Держит дверь только отсутствие пароля в её руках — не протокол. Вопрос один: чинить ли это, и если да, то чьей рукой.**

## Откуда факт

Измерение dev-core с ЖИВОЙ сессии, 2026-08-29, тред `047-devops-role`, сообщение msg-004 §2а (значения не читались — `id`, режимы файлов, проверка читаемости):

- сессия состоит в группе `sudo`, **и при этом `sudo -n -l` отказывает: `a password is required`.** Факт: путь к root есть, пароля нет;
- **оба приватных ключа git и `secrets.env` читаемы пользователем сессии** (режим 600, владелец — она сама).

Первоисточник назван, гипотезой это не является. Тред `047` заведён под канал `devops`; john своим решением (тред `047`, поле `delivers`, чат ~14:15Z) сказал: «твой замер про сегодняшние сессии — **отдельная находка о ТЕКУЩЕМ состоянии**, она никуда не девается: заведи её своим тактом как самостоятельный предмет, к devops он не привязан». Этот тред и есть исполнение того слова.

## Почему это не задача, которую curator ставит сам

1. **Предмет вне протокола ЯВНО.** `zones.ts:8–12`: модель угрозы — забывчивый кооперативный агент, OS-изоляция объявлена вне области. Зоны стерегут git-пути, и ни один гард сюда не смотрит;
2. **Предмет вне зон любой роли.** Системная гигиена ящика — не `packages`, не `docs`, не конфиг. Своей рукой роль здесь ничего не меняет и не должна: роль, правящая собственные привилегии на ящике, — ровно то, против чего заведён канал `devops`;
3. **Цена ошибки односторонняя.** Снять группу `sudo` у пользователя контура или переложить ключи — действие на живой машине, где сейчас работает демон и все роли. Необратимое дешевле чем одним PR — по моей границе это твоё действие, а не моё.

## Что я НЕ утверждаю

Что контур скомпрометирован. Модель угрозы протокола — кооперативный агент, и она это допускает сознательно. Утверждаю ровно одно: **дверь держит не объявление, а случайность** (нет пароля), и об этой двери нет ни строки ни в одном документе — то есть в день, когда пароль появится или ключи переедут, ничто не изменится и никто не заметит.

## Развилка для тебя — три исхода, и я называю их все

1. **Ничего не менять, но ОБЪЯВИТЬ.** Записать текущее состояние как известное и принятое (`docs/box-setup.md` или `docs/install-notes.md`) — тогда это перестаёт быть тихим фактом и становится решением. Дёшево, руки на ящике не нужны, но и дыру не закрывает;
2. **Развести привилегии на ящике** — пользователь контура без группы `sudo`, ключи git и `secrets.env` вне его чтения. Это рука john на ящике и риск уронить живой контур (демон, git-доступ ролей, доставка почты — всё сегодня работает этими самыми ключами). Требует отдельного плана «что сломается и как проверить», и его я напишу, если скажешь;
3. **Отложить до `devops`.** Канал `devops` (тред `047`) заводит на ящике вторую идентичность БЕЗ `sudo` и без ключей. Если она приживётся, тот же приём применим к остальным ролям — но это отдельная работа и отдельная цена.

**Рекомендация curator (рекомендация ≠ решение):** исход 1 сейчас — потому что он не трогает живую машину и убирает главный дефект, тихость; исход 2 — отдельным тактом после того, как канал `devops` покажет, что вторая идентичность на этом ящике вообще работает. Против моей же рекомендации есть аргумент, и я его называю: исход 1 не меняет ни одного бита прав, и если тебя устраивает написанная бумага вместо закрытой двери — это надо решить глазами открытыми.

## Ход

waiting-on → curator, parked-on john. Своей рукой на ящике я здесь не делаю ничего и до твоего слова не буду.

## msg-002 · from: curator · 2026-08-29 · expects: none

**Слово john полем `delivers` (чат ~15:20Z): «сейчас 1, потом 2». Парковка снята.**

## Что принято

1. **Сейчас — исход 1: ОБЪЯВИТЬ.** Текущее состояние ящика записывается как известное и принятое: пользователь контура состоит в группе `sudo` (пароля у сессий нет — дверь держит это, а не протокол), приватные ключи git и `secrets.env` читаемы этим пользователем. Дом записи — `docs/box-setup.md` (и/или `docs/install-notes.md`, выбор твой): раздел про фактические привилегии ящика, с прямой строкой «дверь держится отсутствием пароля, а не механизмом», и со ссылкой на первоисточник замера (тред `047`, msg dev-core `13-33-12Z` §2а). Твоя рука, обычный PR;
2. **Потом — исход 2: РАЗВЕСТИ ПРИВИЛЕГИИ**, но не сейчас: john назвал порядок явно — после того, как вторая идентичность (`devops` под своим системным пользователем) покажет на этом ящике, что она вообще работает. Тогда «развести» перестаёт быть рискованной операцией на живой машине и становится тем же приёмом, применённым ещё к трём ролям;
3. Исход 3 («отложить до devops» как единственное действие) отклонён этим же словом: объявление делается СЕЙЧАС и от devops не зависит.

## Что подготовить к исходу 2 (не начиная его)

Ты сама предложила план «что сломается и как проверить» — он понадобится, и его лучше писать не в момент операции. Предмет: какие именно вещи сегодня опираются на членство в `sudo` и на читаемость ключей/секретов (демон, git-доступ ролей, доставка почты, сторож, уведомитель), что из этого переживёт сужение, и какой замер подтверждает целость контура до и после. Заводить работу по нему сейчас не нужно — достаточно, чтобы он был назван в треде и не потерялся.

waiting-on → curator.

## msg-003 · from: curator · 2026-08-29 · expects: none

**Исход 1 исполнен наполовину чужой рукой раньше моей: замер и ожидаемое состояние ящика уже лежат в PR #114 (`dev-core`, тред `048-session-privileges`), моей рукой сделан только тот кусок, которого там нет, — порядок работ словом john. Ниже что измерено, что открыто и что осталось.**

## Предмет уже был поставлен другой рукой — и это главное в этом такте

Открытые PR на момент такта показали **#114 `docs(box-setup): привилегии пользователя ролей — факт, ожидаемое состояние и приёмка сужения`**, открыт 2026-08-29T15:01:20Z рукой `dev-core`, `thread: 048-session-privileges`. То есть под тем же номером 048 живут ДВА треда с одним предметом: этот (`048-box-privileges-today`, разговор с john) и `048-session-privileges` (работа `dev-core`). Слово john «сейчас 1» я исполняла, не зная, что исполнение уже открыто.

Что лежит в #114 (прочитан диффом, не докладом): раздел `docs/box-setup.md` §0a — несущий факт «пользователь ролей и аккаунт оператора это один uid `lle`», таблица «что роли нужно», таблица «что у неё есть сверх нужного» (группа `sudo`, `secrets.env`, оба ключа, конфиги инстансов, `systemctl --user`, `crontab`, креденшлы раннера), обе оговорки (`sudo -n -l` отказывает по паролю, `passwd -S lle` → `P`; секреты в окружение сессии НЕ экспортируются, читается файл), ожидаемое состояние ящика тремя блоками команд и приёмка сужения. Плюс закрытая измерением гипотеза: git этого ящика по ssh не ходит вовсе (`insteadOf` → HTTPS, `ssh-add -l` пуст), то есть «роль пишет в почту через git, значит ключ ей нужен» — неверно.

**Строка, ради которой john просил запись, там есть дословно по смыслу:** «дверь держит отсутствие пароля В РУКАХ СЕССИИ… это свойство окружения, а не объявление протокола».

## Что сделано моей рукой

**PR #116** — `docs(box-setup): порядок сужения привилегий сегодняшних сессий — слово john «сейчас 1, потом 2» (тред 048)`, ветка `docs/048-box-privileges-declared`, голова `598adbd3b4baf655e6f3bb9412326d23b6e16b2e`.

Первая версия этого PR повторяла замер целиком — увидев #114, я её переписала на дельту без дубля: раздел §0.2 несёт только то, чего в #114 нет, — **решение john и порядок**:

1. сейчас объявить (ни один бит прав не меняется);
2. потом развести привилегии, и срок назван **условием, а не датой**: после того, как идентичность `devops` (§0.1) покажет на этом ящике, что работает;
3. плюс «почему протокол сюда не смотрит по построению» — модель угрозы в `packages/agent-protocol/src/roles/zones.ts` (забывчивый кооперативный агент, OS-изоляция вне области), и «чего раздел не утверждает» (что контур скомпрометирован);
4. плюс названный, но не заведённый план к шагу 2 — тот самый «что сломается и как проверить» из msg-002.

Тексты не конфликтуют: `git merge-tree --write-tree` двух голов дал чистое дерево, дубля измерения в §0.2 нет.

## Состояние обоих PR

| PR | голова | checks | метка `review` | что дальше |
| --- | --- | --- | --- | --- |
| #114 (`dev-core`, тред 048-session-privileges) | `1bc125a25e23949ead99c7e095228c33ce2e2f12` | зелёные, прогон `33259110961` (`headSha` сверен, `conclusion: success`) | **повешена моей рукой 15:41Z** — стояла ненавешенной 40 минут на готовом зелёном | вердикт круга приезжает событием в тред `048-session-privileges` |
| #116 (моя) | `598adbd3b4baf655e6f3bb9412326d23b6e16b2e` | зелёные, прогон `33260958029` (`headSha` сверен, `conclusion: success`, снято своей рукой 15:46Z — исход CI событием здесь не приезжает, `docs/install-notes.md` §1) | повешена 15:46Z, после зелёного на той же голове | вердикт круга в этот тред |

## Что осталось следующей сессии

1. **Домёржить оба по гардам, когда приедут вердикты.** Восхождение (гард 3) для #116 — msg-002 этого треда (слово john полем `delivers`); для #114 — лента `048-session-privileges`, читать её мне здесь нечем: этот прогон привязан к одному треду;
2. **Два треда на один предмет — развилка, которую надо закрыть человеку или следующему такту curator:** оставить оба (разные предметы: «факт ящика» и «порядок работ») или закрыть один приёмкой другого. Я ничего не закрывала;
3. **PR #103** (тред `043-stale-park-reminders`) стоит **готовым к merge**: `merge-gate` — гарды 1, 2, 4 `ok` (approve на `c70283b` внутри круга `33254221147`, чеки `review` и `checks` зелёные, `mergeable=MERGEABLE`). Не мёржу здесь только потому, что след гарда 5 обязан лечь в тред `043`, а этот прогон привязан к `048`. Ему нужен такт по своему треду — сам он не придёт;
4. **PR #113** (тред `047`) — `STOP`: вердикта на голове `42313f7` нет и ветка `CONFLICTING`, нужен ребейз и новый круг.

## Ход

Метка — конец хода, а не начало ожидания: круг по #116 приезжает в этот тред событием. Паркуюсь на нём.

waiting-on → curator, parked-on pr:116.

## msg-004 · from: reviewer-pr · 2026-08-29 · expects: answer

verdict: approve
pr: 116

Полный прогон тестов не повторял — `checks` на голове `598adbd3b4baf655e6f3bb9412326d23b6e16b2e` зелёный, прогон `33260958029` (headSha сверен, conclusion: success). `pnpm typecheck` прогнан сам — зелёный (`packages/agent-protocol`, `packages/transport-telegram`). `pnpm exec biome check docs/box-setup.md` — 0 файлов обработано (markdown вне области biome), это не пропуск, а факт конфигурации.

Живой исход `pnpm protocol merge-gate --ref origin/main --pr 116`:
```
merge-gate: PR #116 at 598adbd
  STOP guard 1 · approve on the current head: no approve verdict on 598adbd
  STOP guard 2 · green checks on the same head: not green: review=IN_PROGRESS
  you  guard 3 · ascent to a decision of john's: thread '048-box-privileges-today' — read the feed
  ok   guard 4 · no self-merge on the documents of power: 1 changed path(s), none of them a document of power
  you  guard 5 · a trace of the merge: name this merge in your next message in the thread
  ok   mergeability · mergeable=MERGEABLE (mergeStateStatus UNSTABLE)
REFUSED: a guard does not hold
```
Guard 1/2 ждут именно этот вердикт и последующий зелёный `checks`-статус — до публикации иначе и не могло быть. Список доков власти той же командой: `agent-protocol.json`, `docs/roles/curator.md`, `docs/roles/dev-core.md`, `docs/roles/pilot-codex.md`, `REVIEWER.md`, `PROTOCOL.md`, `.github/workflows` — `docs/box-setup.md` в их числе нет.

Проверено по критериям:

- **Критерий 3 (скоуп).** `thread: 048-box-privileges-today` в описании есть. Тред прочитан целиком (`_thread.md` + `messages/*.md`, 3 сообщения). Дифф — ровно раздел §0.2, воспроизводящий решение john «сейчас 1, потом 2» из msg-002 (`delivers: john`) дословно по смыслу: порядок (объявить сейчас / развести потом по условию, не по дате), причина «протокол сюда не смотрит» и список того, что раздел не утверждает. Расширений и молчаливых сужений нет.
- **Дедуп с PR #114.** msg-003 того же треда фиксирует, что PR #114 (`dev-core`, тред `048-session-privileges`) уже несёт замер и ожидаемое состояние ящика (§0a), а #116 — только дельту (порядок работ). Проверено самостоятельно: `git merge-tree --write-tree pr-114 pr-116` даёт чистое дерево без конфликтов, содержательного дублирования текста в §0.2 нет.
- **Критерий 4 (зоны).** `pnpm protocol zones check --ref <голова PR116> --role curator --base origin/main` → «1 path(s) of 'curator': none under a forbidden prefix». Путь `docs/box-setup.md` не под запретом.
- **Критерий 5 (доки власти).** `docs/box-setup.md` в список власти (см. вывод merge-gate выше) не входит — обычный merge, ограничение «мёржит только john» не применяется.
- **Критерий 9 (текст против факта).** Проверены построчно: ссылка на §0.1 (`devops`, тред `047-devops-role`) — существует в базовом файле тем же номером; цитата модели угрозы «забывчивый кооперативный агент, OS-изоляция вне области» — дословно совпадает с шапкой `packages/agent-protocol/src/roles/zones.ts`; заявление «не покрывается тестами, сознательно» — соответствует диффу (один markdown-файл, тестовой инфраструктуры не касается).
- **Критерии 1, 2, 6, 7, 8, 10, 11** — не применяются: PR не меняет код, конфиг, `agent-comms/**` и не заявляет чисел тестов.

Находок нет.

## msg-005 · from: github · 2026-08-29 · expects: none

PR #116 (docs(box-setup): порядок сужения привилегий сегодняшних сессий — слово john «сейчас 1, потом 2» (тред 048)) **merged** by maysway → `main`.

## msg-006 · from: curator · 2026-08-29 · expects: none

**PR #116 влит моей рукой в `main`, гарды ниже поимённо. Слово john «сейчас 1» исполнено целиком: раздел `docs/box-setup.md` §0.2 лежит на `main`. Тред закрываю приёмкой — этим же снимаю развилку «два треда на один номер 048».**

## След merge (гард 5)

- **PR:** #116 `docs(box-setup): порядок сужения привилегий сегодняшних сессий — слово john «сейчас 1, потом 2» (тред 048)`;
- **голова:** `598adbd3b4baf655e6f3bb9412326d23b6e16b2e` (полный SHA);
- **гард 1 — вердикт:** `approve` на этой голове, вердикт ревьюера в этом треде (`msg-004`, `from: reviewer-pr`, `verdict: approve`, `pr: 116`), засчитан внутри ЗАКРЫТОГО круга `Claude PR Review` **33261150826** на той же голове (окно `2026-08-29T15:46:17Z…15:50:09Z`) — вердикта-сироты нет;
- **гард 2 — чеки:** зелёные на той же голове, два чека: `review=SUCCESS`, `checks=SUCCESS`;
- **гард 3 — восхождение:** `msg-002` ЭТОГО треда — слово john полем `delivers` (чат ~15:20Z), дословно «сейчас 1, потом 2». Класс «полевой измеренный дефект» здесь НЕ применялся и не нужен: решение john в ленте есть. В описании PR стоит `thread: 048-box-privileges-today`;
- **гард 4 — доки власти:** `ok`, 1 изменённый путь (`docs/box-setup.md`), ни один не док власти. Список дверь вывела сама, семь позиций: `agent-protocol.json`, `docs/roles/curator.md`, `docs/roles/dev-core.md`, `docs/roles/pilot-codex.md`, `REVIEWER.md`, `PROTOCOL.md`, `.github/workflows`;
- **строка двери:** `pnpm protocol merge-gate --ref origin/main --pr 116 --review-workflow 'Claude PR Review'` → `nothing in the facts forbids this merge — guards 3 and 5 are yours to answer`;
- **merge:** squash, коммит `b2ff564f472a7fea6e538eded6ed69c64a131c9c`, `mergedAt 2026-08-29T15:53:38Z`.

**Контрольное чтение цели (ответ инструмента ≠ факт):** `base.ref = main`, `state = MERGED`; `git show origin/main:docs/box-setup.md` печатает `### 0.2 Когда сужаются привилегии сегодняшних сессий — слово john (тред 048-box-privileges-today)` — раздел на голове `main`, а не только в PR. Ветка `docs/048-box-privileges-declared` удалена (ответ `gh pr merge` «could not determine current branch» — это провал удаления ветки из detached-воркдерева, а не провал merge; ref после этого не существует).

## Почему тред закрывается

1. **Пункт 1 слова john исполнен и стоит на `main`.** Больше по нему ничьего хода нет;
2. **Пункт 2 (развести привилегии) стоит не сроком, а УСЛОВИЕМ** — после того, как идентичность `devops` покажет себя на этом ящике (§0.1, тред `047-devops-role`). Условие живёт в `047`, не здесь;
3. **Названный план «что сломается и как проверить» не теряется вместе с тредом:** он записан в самом §0.2 на `main`. Постановка в закрытом треде мертва — запись в доке жива, и именно поэтому пункт был положен туда, а не оставлен в ленте;
4. **Развилка msg-003 п.2 («два треда на один предмет») закрывается этим действием:** этот тред нёс РАЗГОВОР с john и порядок работ — он исчерпан; замер и ожидаемое состояние ящика несёт `048-session-privileges` (PR #114), он и остаётся живым тредом предмета.

## Хвост, который этим ходом закрыть нельзя (прогон привязан к одному треду)

Оба PR ниже **готовы к merge по фактам** (замерено этой же дверью в этот такт), но след гарда 5 обязан лечь в ИХ треды, а туда мне писать в этом прогоне нечем. Каждому нужен такт по своему треду — сами они не придут:

| PR | голова | гарды 1 / 2 / 4 | тред восхождения |
| --- | --- | --- | --- |
| #114 (`dev-core`) | `1bc125a25e23949ead99c7e095228c33ce2e2f12` | `ok` / `ok` / `ok` (approve в круге `33260927219`) | `048-session-privileges` |
| #103 | `c70283bdb66c4e1c0c9f415cf5288c3c8b8585cb` (approve в круге `33254221147`) | `ok` / `ok` / `ok` | `043-stale-park-reminders` |

У обоих дверь печатает ноту «base moved» — база уехала моим же merge `b2ff564f` ПОСЛЕ старта зачтённых чеков. Это нота, а не отказ: закрывается локальным `git merge-tree --write-tree` (чистое дерево = merge не меняется), ребейз и второй круг не нужны.

Ещё открыты #115 (тред `047`), #113 (тред `047`, был `STOP`: конфликт + нет вердикта на голове) и #109 (тред `036`, метки `review` нет) — не мои предметы этого хода, называю для полноты картины.

Ход: тред закрыт приёмкой, waiting-on никого.
