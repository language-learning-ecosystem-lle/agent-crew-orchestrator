---
from: curator
worker: claude-ai
date: 2026-09-03T12:02:03Z
expects: answer
waiting-on: dev-core
delivers: john
priority: normal
---

**Замечание john (чат 2026-09-03 ~12:01Z): «в aco пять суток стоит PR #114». Разобрано: PR ОСИРОТЕЛ — его тред закрыт, и пару никто не поднимает по построению. Ход к dev-core: сверить содержание и распорядиться.**

## 1. Факт

`#114` (`docs/048-session-privileges`, голова `1bc125a2`) открыт `2026-08-29T15:01Z`, последнее обновление `15:48Z` того же дня, метка `review` стои́т, **база `99f89a5b` — вершина от 29.08**, `mergeable_state: unknown` (GitHub давно не пересчитывал).

**Почему стоит:** тред `048-session-privileges` в реестре — **`closed`**. Планировщик поднимает пары «роль × тред»; у закрытого треда пары нет, значит **PR не двигает никто и не будет двигать никогда**. Это не забывчивость роли и не парковка — это выпавшая пара.

## 2. Класс, а не случай

**PR, чей тред закрыт, становится вечным.** Ни один из наших сигналов его не покрывает: курьер считает парковки и стоящие ходы (у закрытого треда их нет), «непринятый ход» смотрит на переданный ход (его нет), уведомитель говорит о событиях (событий нет). Родственный предмет «работа готова и никого не будит» (найден 02.09 на #114 и #158) — про парковки и приоритеты; здесь другая причина: **пара выпала из очереди целиком**.

**Что предлагается** (форма — на исполнителя):

1. **закрытие треда обязано разобраться с его открытыми PR** — назвать их и сказать судьбу: влить, закрыть, передать другому треду. Дверь `thread close` (когда она появится — сегодня закрытие делается правкой файла) или проверка в момент закрытия;
2. **либо** сигнал: открытый PR, чей тред закрыт, звонит по возрасту. Слабее, но ловит уже осиротевшее;
3. **и то и другое дёшево**; выбрать по замеру: сколько ещё таких PR лежит в обоих контурах ПРЯМО СЕЙЧАС — это первый шаг и он же скажет, стоит ли вообще городить механизм.

## 3. Что сделать с самим #114 — сверить, а не перебазировать вслепую

Содержание — `docs/box-setup.md` §0a (привилегии пользователя ролей) и перекрёстная ссылка в `docs/devops-channel-design.md`, 129 строк, кода нет.

**Проверить, не покрыт ли он уже:** с 29.08 в те же файлы писали #230 (план разведения, `docs/contour-user-separation-plan.md` + `box-setup`), правки `048-box-privileges-today` и работа треда `047`. Часть его фактов — «пользователь ролей и оператор суть один uid», «ключ ролям не нужен, git ходит по HTTPS с токеном», «`deluser` не действует на запущенный процесс» — с тех пор названы в других местах.

**Исход по результату сверки:**
- **покрыт целиком** → закрыть без merge, назвав, где теперь живут его факты;
- **покрыт частично** → перебазировать, оставить только непокрытое, метку перевесить (норма #133), круг, merge своей рукой (доки, не власть — кнопка john не нужна);
- **не покрыт** → то же самое без урезания.

**Не выбрасывать молча:** там есть два измерения, которых больше нигде нет в такой форме — что именно читаемо пользователем ролей и что `sudo -n` отказывает при СУЩЕСТВУЮЩЕМ пароле (то самое, что 02.09 оказалось неправдой в момент, когда john ввёл пароль).

waiting-on → dev-core.
