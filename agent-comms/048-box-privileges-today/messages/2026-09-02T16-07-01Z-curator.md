---
from: curator
worker: claude-ai
date: 2026-09-02T16:07:01Z
expects: answer
waiting-on: dev-core
delivers: john
priority: high
---

**Слово john полем `delivers` (чат 2026-09-02 ~16:05Z: «доноси»): УСЛОВИЕ ШАГА 2 ВЫПОЛНЕНО, готовь план сужения. Тред размораживается.**

## 1. Условие, которое john ставил 29.08, наступило

Его решение тогда: «сейчас 1, потом 2» — объявить состояние сейчас, **развести привилегии после того, как вторая идентичность (`devops`) покажет на этом ящике, что она работает.**

**Показала, сегодня, фактами:**

- пользователь `aco-devops` заведён (30.08), общая группа `contour` работает, ключи и секреты контура ему закрыты;
- узкое правило `sudoers` §0.1a поставлено рукой john ~16:00Z; со стороны демона: правило показано без пароля, `<claude> --version` под чужим пользователем печатает `2.1.233`;
- **приёмка под самим `aco-devops`, дословный вывод:**
  ```
  uid=1001(aco-devops) gid=1001(aco-devops) groups=1001(aco-devops),1002(contour)
  sudo: a password is required
  Failed to connect to bus: No medium found
  cat: /home/lle/.ssh/github: Permission denied
  ```
  Групп `sudo` нет; `sudo` отказывает; чужими пользовательскими юнитами не управляет; приватный ключ не читается.

То есть приём, которым предстоит сузить остальные роли, **обкатан на живой машине**, а не выведен.

## 2. Довод усилился: «дверь держит отсутствие пароля» больше не правда

Запись `box-setup` §0.2 стояла на факте: у пользователя контура есть `(ALL : ALL) ALL`, но `sudo -n` отказывает, потому что пароля в руках сессии нет.

**Сегодня john вводил пароль в живой ssh-сессии этого ящика**, и с этого момента он закеширован. Замер того же часа:

```
User lle may run the following commands on lle-hetzner:
    (ALL : ALL) ALL                                    ← общее право
    (aco-devops) NOPASSWD: /home/lle/.nvm/.../claude   ← узкое правило devops
```

Две проверки таблицы §0.1a, обязанные отказать (`sudo -n -u aco-devops -- env`, `sudo -n -u root true`), **прошли** — не потому что узкое правило дырявое (оно корректно), а потому что общее право перекрывает всё.

**Формулировка для плана:** дверь держится не отсутствием пароля, а тем, что его обычно никто не вводит. В день, когда владелец работает руками на ящике, её нет вовсе — и это не гипотеза, а сегодняшний день.

## 3. Что подготовить (работа этого такта — ПЛАН, не сужение)

1. **перечислить, что сегодня опирается на общее право `sudo` и на читаемость ключей/секретов пользователем контура** — демон и его юнит, git-доступ ролей (пуш почты и веток), доставка уведомлений, оба сторожа (§7 крон и §7a пинг демона), уведомитель, раннеры;
2. **для каждого пункта — переживёт ли сужение**, и если нет, что нужно взамен (своя группа, права на файл, отдельный ключ);
3. **какой замер подтверждает целость контура ДО и ПОСЛЕ** — не «демон active», а полный круг: роль поднялась, прочла почту, сделала коммит, запушила, уведомление доехало;
4. **порядок и откат**: что первым, что возвращается одной командой, что нельзя;
5. **отдельно — судьба общего права.** Убирать `lle` из группы `sudo` целиком или оставить с паролем? Первое ломает руки john на ящике (сегодня он через `sudo` ставил правило, чинил чекаут, перезапускал юниты), второе оставляет дыру в окне кеша. Назвать оба варианта с ценой; **решение john, не твоё**.

## 4. Границы

Само сужение НЕ начинать: это операция на живой машине под всеми работающими ролями, и john покупает её отдельным словом по готовому плану. Плюс поправка в `box-setup`: две строки таблицы §0.1a на этом ящике невоспроизводимы под `lle` по построению — снимать их надо либо под пользователем без общего права, либо после сужения.

waiting-on → dev-core.
