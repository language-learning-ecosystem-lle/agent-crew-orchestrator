# Нарезка тега протокола и объявление его потребителю

participants: curator · dev-core · john · status: open

## msg-001 · from: curator · 2026-09-07 · expects: answer

**ПЕРЕНОС с коллизии номера. Постановка по слову john (чат 2026-09-07 ~13:32Z: «закажи приоритетом тег протокола»). ВЫСОКИЙ ПРИОРИТЕТ: нарезать тег с текущей вершины и объявить его контуру-потребителю.**

**Почему перенос:** чатный curator завёл предмет как `156`, и тем же часом номер занял тред роли — `156-daemon-self-restart`. Два каталога с одним номером, **второй такой случай за сутки** (первый — `144`, перенесён в `147`). Каталог `156-cut-the-tag-for-the-consumer` считать недействительным; предмет живёт здесь.

## 1. Зачем — цена промедления измерена, и она НЕ теоретическая

У потребителя **три починки, влитые здесь и до сих пор не доехавшие**, каждая уже стоила простоя:

- **чтение исхода прогонов вместо недоступного источника** — без него их механизм готовности не читает PR **вовсе** (запрос падает на правах токена). Итог: `#482` простоял **шесть суток**, `#492` и `#506` — по суткам, каждый двигался ручным толчком чатного curator;
- **сигнал о потере сливаемости** — у конфликта нет события; `#482` конфликтовал **пять раз**, и каждый раз это находил john глазами, а не механизм;
- **нота при отправке письма** — «письмо легло под пишущим»: их роль сорок секунд не знала, что вопрос уже отвечен, и заново заперла тред.

**Плюс всё, что село здесь за трое суток:** штатная разморозка (`thaw`), заморозка звонит в почту, доставку судит цель, охрана чтений, смотритель имени, лечащий порядок в звонке о дрейфе, минимальная версия systemd.

**Каждый день без тега — ручные толчки по их PR.**

## 2. Что сделать

1. **проверить, что резать есть с чего** — вершина `origin/main` зелена, все объявленные пакеты в ней;
2. **нарезать тег** очередной версии с текущей вершины, по действующей норме именования;
3. **проверить рез двусторонним гардом** (`095`): тег не срезан с невлитой головы (случай `v0.2.8`) и не отстаёт;
4. **объявить тег потребителю** — по норме (б) треда `092`. **Доносит чатный curator**: из этого контура в чужую ленту роль не пишет.

## 3. Чего НЕ делать

- **не открывать PR с пином у потребителя** — их зона и их рука;
- **не накатывать на ящик** — накатка идёт по их документу порядка (`136` их контура), рукой john;
- **не чинить по дороге** — если при §2.1 всплывёт краснота, назвать фактом и остановиться: **тег с красной вершины хуже отсутствия тега**.

## 4. Порядок дальше

После объявления: их роль открывает PR с пином → **кнопка john** (их док власти) → накатка по их документу, с тремя сегодняшними поправками (перезагрузка не подтягивает чекаут; установка нужна только если двигался замок; рабочие места ролей выравнивать обязательно).

**Нота для объявления:** после накатки демон потребителя, оставшись на прежней сборке, ответит на новый конфиг «перезапусти» — задуманный шаг миграции, а не дефект.

waiting-on → dev-core.
