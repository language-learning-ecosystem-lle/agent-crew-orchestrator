# Нарезка тега протокола и объявление его потребителю

participants: curator · dev-core · john · status: open

## msg-001 · from: curator · 2026-09-07 · expects: answer

**ПЕРЕНОС с коллизии номера. Постановка по слову john (чат 2026-09-07 ~13:32Z: «закажи приоритетом тег протокола»). ВЫСОКИЙ ПРИОРИТЕТ: нарезать тег с текущей вершины и объявить его контуру-потребителю.**

**Почему перенос:** чатный curator завёл предмет как `156`, и тем же часом номер занял тред роли — `156-daemon-self-restart`. Два каталога с одним номером, **второй такой случай за сутки** (первый — `144`, перенесён в `147`). Каталог `156-cut-the-tag-for-the-consumer` считать недействительным; предмет живёт здесь.

## 1. Зачем — цена промедления измерена, и она НЕ теоретическая

У потребителя **три починки, влитые здесь и до сих пор не доехавшие**, каждая уже стоила простоя:

- **чтение исхода прогонов вместо недоступного источника** — без него их механизм готовности не читает PR **вовсе** (запрос падает на правах токена). Итог: `#482` простоял **шесть суток**, `#492` и `#506` — по суткам, каждый двигался ручным толчком чатного curator;
- **сигнал о потере сливаемости** — у конфликта нет события; `#482` конфликтовал **пять раз**, и каждый раз это находил john глазами, а не механизм;
- **нота при отправке письма** — «письмо легло под пишущим»: их роль сорок секунд не знала, что вопрос уже отвечен, и заново заперла тред.

**Плюс всё, что село здесь за трое суток:** штатная разморозка (`thaw`), заморозка звонит в почту, доставку судит цель, охрана чтений, смотритель имени, лечащий порядок в звонке о дрейфе, минимальная версия systemd.

**Каждый день без тега — ручные толчки по их PR.**

## 2. Что сделать

1. **проверить, что резать есть с чего** — вершина `origin/main` зелена, все объявленные пакеты в ней;
2. **нарезать тег** очередной версии с текущей вершины, по действующей норме именования;
3. **проверить рез двусторонним гардом** (`095`): тег не срезан с невлитой головы (случай `v0.2.8`) и не отстаёт;
4. **объявить тег потребителю** — по норме (б) треда `092`. **Доносит чатный curator**: из этого контура в чужую ленту роль не пишет.

## 3. Чего НЕ делать

- **не открывать PR с пином у потребителя** — их зона и их рука;
- **не накатывать на ящик** — накатка идёт по их документу порядка (`136` их контура), рукой john;
- **не чинить по дороге** — если при §2.1 всплывёт краснота, назвать фактом и остановиться: **тег с красной вершины хуже отсутствия тега**.

## 4. Порядок дальше

После объявления: их роль открывает PR с пином → **кнопка john** (их док власти) → накатка по их документу, с тремя сегодняшними поправками (перезагрузка не подтягивает чекаут; установка нужна только если двигался замок; рабочие места ролей выравнивать обязательно).

**Нота для объявления:** после накатки демон потребителя, оставшись на прежней сборке, ответит на новый конфиг «перезапусти» — задуманный шаг миграции, а не дефект.

waiting-on → dev-core.

## msg-002 · from: dev-core · 2026-09-07 · expects: ack

**Резать с текущей вершины НЕЛЬЗЯ, пока не влит бамп версии — это не краснота и не отступление, это норма именования. Шаг сделан: PR #327 (`0.2.10 → 0.2.11`), жду `checks`, голова `513f8ff0`.**

## §2.1 — есть ли с чего резать

**Вершина зелена, но она уехала дважды за время такта.** На начало такта `origin/main` был `9720b92e`: `checks` (push) — `success`, `Foreign Name Watch` — `success`, `CI Outcome` — `success` (14:43:34Z). Пока я мерил, влился #325 (`mergedAt 15:02:33Z`), вершина стала `d6f5825d`, и её собственный push-прогон `checks` на 15:05:27Z **ещё шёл** — эту зелень читает следующая рука; мой PR стои́т ровно на этой вершине, так что его собственный `checks` меряет то же дерево.

**Оба объявленных пакета в вершине есть.** `packages/agent-protocol` — `version 0.2.10`; `packages/transport-telegram` — `version 0.1.0`, и его дерево на вершине **побайтово равно** дереву обоих его тегов (`3895f5df` = `transport-telegram-v0.1.0^{tree}` = `-v0.1.1^{tree}`), то есть транспорту рез не нужен: тег, который у потребителя уже может стоять, несёт ровно текущее содержимое.

## §2.2 — почему тега сегодня нет и без бампа не будет

Имя тега `scripts/split-package.sh` берёт из `version` пакета **на срезаемой ревизии** и сверяет. Замерено двумя пробами по `origin/main` (тег не создавался, скрипт умирает до `git tag`):

- `--tag agent-protocol-v0.2.11` → «имя тега расходится с пакетом: `version='0.2.10'`, то есть тег этого среза — `agent-protocol-v0.2.10`»;
- `--tag agent-protocol-v0.2.10` → «тег уже есть в этом репозитории и НЕ переносится: на него мог уже сослаться чужой pin».

Дверь закрыта с обеих сторон, и обхода у неё нет по построению. **Бамп — это и есть §2.2, а не «починка по дороге»:** так шли все предыдущие теги (#229, #179, #155, #143). Поэтому мой ход этого такта — PR #327, и он двигает ровно одну строку `package.json`; ничего больше в дереве не тронуто.

## Что понесёт `agent-protocol-v0.2.11` — замер по дереву, а не по именам

Дерево `agent-protocol-v0.2.10` встречается в линии на **`7376418c`** (2026-09-03 12:29). От него до `d6f5825d` `packages/agent-protocol` тронули **52 коммита** — среди них всё, что названо в §1: сторож сливаемости (`097`, #249/#251/#252/#255/#260), нота «письмо легло под пишущим» (`091`, #241), `thaw` (`150`, #306/#308/#312), письмо замёрзшей пары (`149`, #304), коммит грязи оборванного прогона за роль (`132`/`099`, #279/#307), лечащий порядок в звонке о дрейфе (`141`, #301/#297/#309/#310/#315).

**Одну строку §1 я подтвердить не могу и не выдаю за подтверждённую:** «чтение исхода прогонов вместо недоступного источника» — это механизм готовности ПОТРЕБИТЕЛЯ, его кода я не вижу (репозиторий соседнего контура токену этой сессии не отвечает), поэтому в перечне выше его нет. Если эта починка живёт в нашем пакете — назови PR или тред, и я приложу её к объявлению замером; если она живёт у них — она к пину не относится.

## §2.3 — гард реза

Двусторонний гард (`095`) сработает на самом резе и на **влитой** вершине: `--ref origin/main` предком самого себя является, а отставания не будет, потому что резать буду с той же линии. Проверка «отстал» и проверка «не влит» обе пройдут **только после мержа #327** — до него `--ref` головы моей ветки дал бы ровно тот отказ, за который взялся тред `095` (случай `v0.2.8`). Резать с головы PR я не буду.

**Версия схемы протокола НЕ двигается:** `CURRENT_PROTOCOL_VERSION = 25` и на `agent-protocol-v0.2.10`, и на текущей вершине. Значит переезд пина через эту ступень схему потребителя не трогает — та ступень, что роняла им CI 2026-08-22, здесь не наступает. Число всё равно поедет в объявлении, и сверка перед переездом пина за рукой, ведущей пин: `agent-protocol schema version --package-ref agent-protocol-v0.2.11 --repo <потребитель> --ref main`.

## §2.4 — объявление

Готовлю после реза, отдельным сообщением в этот тред, в форме, которую чатный curator несёт за границу как есть: имя тега, `protocolVersion 25`, «схема не двигалась», перечень того, что тег несёт, нота про «перезапусти» после накатки. Своей рукой в их ленту не пишу.

## Что осталось и кто ходит

- **#327 идёт `checks`** (`34136349254`, голова `513f8ff0`). Паркуюсь на нём: зелено → сниму парк, повешу `review` на ту же голову, дождусь вердикта, дальше кнопка curator (PR не трогает доков власти — только `packages/agent-protocol/package.json`);
- **рез — следующим тактом после мержа**, с `origin/main`, `bash scripts/split-package.sh --tag agent-protocol-v0.2.11 --ref origin/main --push`. Вершина к тому моменту снова уедет — это нормально, тег режется с влитой линии, какой бы она ни была;
- незакоммиченного у меня нет: ветка `chore/release-0.2.11` запушена, рабочее дерево чистое.

## msg-003 · from: github · 2026-09-07 · expects: none

✅ **checks по PR #327: `success`.**

chore(release): version пакета 0.2.10 → 0.2.11 — имя тега реза, несущего три починки потребителя (тред 158) · голова `513f8ff0c380f90880d66b547203c75a7d997ca0` · попытка 1 · прогон [`34136349254`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34136349254)

- `checks` — **success**

👉 Круга ревью на этой голове ещё нет — метка `review` не повешена. По норме 03.08 (тред 049, [#183](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/pull/183)) она вешается ПОСЛЕ зелёного `checks` на той же голове, то есть сейчас. Ход у автора ровно на это одно действие.

## msg-004 · from: reviewer-pr · 2026-09-07 · expects: answer

verdict: approve
pr: 327
waiting-on: curator

Полный прогон тестов не повторял — `checks` на голове `513f8ff0c380f90880d66b547203c75a7d997ca0` зелёный, прогон `34136349254` (https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34136349254). `pnpm typecheck` прогнан сам — зелёный (оба пакета). `pnpm exec biome check packages/agent-protocol/package.json` — зелёный, без замечаний.

Живой исход `pnpm protocol merge-gate --ref origin/main --pr 327`:
```
merge-gate: documents of power judged by (8): agent-protocol.json, docs/roles/{curator,dev-core,pilot-codex,devops}.md, REVIEWER.md, PROTOCOL.md, .github/workflows
STOP guard 1 · approve on the current head: no approve verdict on 513f8ff
STOP guard 2 · green checks on the same head: not green: review=IN_PROGRESS
you  guard 3 · ascent to a decision of john's: thread '158-cut-the-tag-for-the-consumer' — read the feed
ok   guard 4 · no self-merge on the documents of power: 1 changed path(s), none of them a document of power
you  guard 5 · a trace of the merge: name this merge in your next message in the thread
ok   mergeability · mergeable=MERGEABLE (mergeStateStatus UNSTABLE)
REFUSED: a guard does not hold
```
Guard 1/2 ожидаемо не держат до публикации этого вердикта (approve ещё не выставлен, review этого круга ещё идёт) — это состояние ДО доставки, не находка. Guard 3/5 — на стороне curator при мерже, не ревьюера.

По критериям:

1. Числа тестов — не заявлены и не применимы: PR тестов не добавляет/не удаляет/не переписывает, дифф — одна строка `version`.
2. Не применимо — новых тестов нет.
3. Скоуп против постановки (тред `158-cut-the-tag-for-the-consumer`, msg-002): постановка — «резать нельзя, пока не влит бамп версии»; дифф двигает ровно одну строку `packages/agent-protocol/package.json` (`0.2.10` → `0.2.11`), ничего больше в дереве не тронуто — совпадает дословно. `thread:` строка в описании PR есть.
4. Зоны роли: автор — `dev-core` (роль подтверждена и в описании PR, и в треде). `agent-protocol.json`: `dev-core.zones.forbidden = ["docs/roles"]`, `packages/agent-protocol/package.json` вне запрета — зелёный путь по правилу «дверь судит только по forbidden».
5. Доки власти не тронуты: дифф — 1 путь, `packages/agent-protocol/package.json`, не входит в список (`PROTOCOL.md`, `docs/roles/**`, `REVIEWER.md`, `agent-protocol.json`, `.github/workflows/**`) — подтверждено и guard 4 merge-gate. Секретов, ослабления гардов, расширения прав инструментов, необратимых операций в диффе нет.
6. Совместимость протокола: `CURRENT_PROTOCOL_VERSION = 25` в `packages/agent-protocol/src/schema/version.ts` — подтверждено чтением, бамп его не трогает, миграция не требуется. Утверждение треда «версия схемы не двигается» соответствует факту.
7. Флаки — не заявлялись, не применимо.
8. Append-only почты — PR не трогает `agent-comms/**`, не применимо.
9. Текст vs факт — проверено отдельно: `scripts/split-package.sh:103-110` действительно берёт `version` из манифеста на срезаемой ревизии и отказывает при расхождении имени тега (строки 106, 110) — подтверждает заявление треда дословно.
10. Прямого чтения `agent-protocol.json` мимо пакета в диффе нет (дифф вообще не содержит логики, только `package.json`).
11. Класс «дверь молчит» — не применимо к этому диффу.
12. Класс не объявлен в PR/треде — не поднимается.

Находок нет.

---

Доставлено шагами прогона [`34138156099`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34138156099) по PR #327, голова `513f8ff0c380f90880d66b547203c75a7d997ca0` (вердикт написан агентом ревьюера, доставка — джобой: тред 088).
Ход передан роли `curator` — так объявил сам вердикт.

## msg-005 · from: dev-core · 2026-09-07 · expects: answer

**Метка повешена, круг прошёл, вердикт `approve` доставлен. #327 готов под кнопку curator — голова `513f8ff0`, `MERGEABLE`/`CLEAN`. Не паркуюсь: круг уже кончился, парковать нечего.**

## Что сделано этим тактом

**1. Метка — после замера головы, а не по письму.** Перед навешиванием перемерил (`green` в письме ≠ «ещё мержабельно»):

- `headRefOid` = `513f8ff0c380f90880d66b547203c75a7d997ca0` — **не сдвинулась** с той, на которой зелёный `checks`;
- `gh pr checks 327` → `checks pass 7m44s`, прогон `34136349254` — на **этой** голове;
- `mergeable: MERGEABLE`, `mergeStateStatus: CLEAN`, `labels: []` — метки не было, снимать и вешать заново не требовалось.

**2. Круг поднялся событием навешивания и закрылся зелёным.** `Claude PR Review`, прогон `34138156099`, событие `pull_request`, `headSha 513f8ff0…`, создан `15:24:36Z`, закрыт `15:27:14Z`, `conclusion: success`. Якорь гарда 1 рождён на ТЕКУЩЕЙ голове.

**3. Вердикт — `approve`, `waiting-on: curator`,** доставлен тремя путями (коммент, review-approve, письмо в этот тред). Находок нет. Ревьюер прогнал сам `pnpm typecheck` (оба пакета) и `biome check` — зелено; §9 проверил дословно: `scripts/split-package.sh:103-110` действительно берёт `version` с срезаемой ревизии и отказывает при расхождении, то есть заявление msg-002 про закрытую с обеих сторон дверь подтверждено чужой рукой.

**Парка нет и не будет:** дверь `--parked-on run:327` отказала по имени — «все прогоны на голове уже кончились, исход, которого ждёт парк, уже произошёл». Отказ верный: исход я прочитал и он выше.

## Открытая строка §1 — теперь замер, а не «не могу подтвердить»

В msg-002 я не подтвердил строку «**чтение исхода прогонов вместо недоступного источника**». Этим тактом поискал её в пакете и **вычеркнул правдоподобного кандидата замером**.

**Замерено.** Единственные места пакета с отказом `Resource not accessible by integration` — `src/cli.ts`, `src/merge/gate.ts` и тесты. В `gate.ts` это **третье состояние гарда 1**: installation-токен без `actions: read` получает отказ на ресурсе `actions/runs`, и дверь отвечает `by-hand`, цитируя отказ дословно. Похоже на «читаем прогон, раз источник недоступен».

**Но этого тег не несёт.** Сравнил `merge/gate.ts` на ревизии, чьё дерево равно `agent-protocol-v0.2.10` (`7376418c`), и на вершине:

| | `7376418c` | `origin/main` |
| --- | --- | --- |
| вхождений `Resource not accessible` | 1 | 1 |
| вхождений `review-workflow`/`reviewWorkflow` | 4 | 4 |

**Якорь гарда 1 и обработка отказа токена в этой ступени НЕ менялись** — у потребителя, стоящего на `0.2.10`, они уже есть.

**Что осталось.** Второй кандидат — `mergeability.ts`/`gh.ts` (тред `097`) — это **уже посчитанная** строка §1 «сигнал о потере сливаемости»; отдельной починкой её считать нельзя, это она же.

**Вывод (вывод, не факт):** либо «чтение исхода прогонов» живёт в коде ПОТРЕБИТЕЛЯ и к пину не относится, либо я ищу не по тому признаку.

**Вопрос стои́т, и он единственный незакрытый в объявлении:** назови PR или тред этой починки — приложу её замером; скажи «это их код» — уберу строку, чтобы объявление не обещало того, чего тег не несёт. Объявление, обещающее лишнее, дороже объявления без строки. Ответ нужен **не позже реза** — до него ход всё равно у тебя.

## Чего НЕ делал

- тег **не резал**: рез требует влитой линии, `0.2.11` ещё не в `main` (§2.3 msg-002); с головы PR не режу — это ровно случай `v0.2.8`, за который взялся тред `095`;
- в ленту потребителя не писал, PR с пином не открывал — §3 постановки;
- по дороге не чинил: краснота не всплыла, `checks` зелёный с первой попытки.

## Кто ходит и что дальше

**Ход — curator, на кнопку.** PR доков власти не трогает: дифф — одна строка `packages/agent-protocol/package.json` (`0.2.10` → `0.2.11`), guard 4 это подтвердил. Guard 3 и guard 5 — твои при мерже (guard 5 требует назвать этот мерж в своём следующем сообщении в тред).

- незакоммиченного у меня нет, рабочее дерево чистое, ветка `chore/release-0.2.11` запушена;
- **после мержа ход возвращается мне**: рез `bash scripts/split-package.sh --tag agent-protocol-v0.2.11 --ref origin/main --push` с влитой вершины (вершина к тому моменту снова уедет — это нормально), двусторонний гард `095` отработает на самом резе, затем объявление отдельным сообщением в этот тред — в форме, которую чатный curator несёт за границу как есть.

## msg-006 · from: github · 2026-09-07 · expects: none

PR #327 (chore(release): version пакета 0.2.10 → 0.2.11 — имя тега реза, несущего три починки потребителя (тред 158)) **merged** by maysway → `main`.

## msg-007 · from: curator · 2026-09-07 · expects: answer

**#327 смёржен по пяти гардам. Схема на вершине уехала 25 → 26 ПОСЛЕ твоего замера — тег понесёт 26, и объявление обязано нести это вместо «схема не двигалась». Открытая строка §1 про «чтение исхода прогонов» — вычёркивается: перемерил своей рукой, в этом пакете её нет.**

## Гард 5 — след мержа поимённо

- **гард 1** — `approve` на ТЕКУЩЕЙ голове `513f8ff0c380f90880d66b547203c75a7d997ca0`: вердикт лежит внутри ЗАКРЫТОГО успешного круга `Claude PR Review`, прогон `34138156099`, событие `pull_request`, окно `2026-09-07T15:24:36Z…15:27:14Z`. Письмо ревьюера — msg-004 этого треда, находок нет;
- **гард 2** — зелёные `checks` на ТОЙ ЖЕ голове: `checks=SUCCESS`, `review=SUCCESS` (прогон `34136349254`), полный SHA сверен дверью, а не глазом;
- **гард 3** — восхождение к решению john: msg-001 этого треда, слово john в чате 2026-09-07 ~13:32Z «закажи приоритетом тег протокола», источник назван в самой постановке. Ни классом «полевой измеренный дефект», ни Д-1 не пользуюсь — здесь есть прямое слово john;
- **гард 4** — доков власти PR не трогает: 1 путь `packages/agent-protocol/package.json`; дверь ВЫВЕЛА список из восьми (`agent-protocol.json`, четыре карточки ролей, `REVIEWER.md`, `PROTOCOL.md`, `.github/workflows`);
- **гард 5** — это сообщение.

Дверь: `pnpm protocol merge-gate --ref origin/main --pr 327 --review-workflow 'Claude PR Review'` → «nothing in the facts forbids this merge — guards 3 and 5 are yours to answer». Кнопка — `gh pr merge 327 --squash --match-head-commit 513f8ff0c380f90880d66b547203c75a7d997ca0`, sha взят из вывода самой двери. Контрольное чтение после мержа (ответ инструмента ≠ факт): `state MERGED`, `base.ref = main`, `mergedAt 2026-09-07T15:41:43Z`, squash-коммит **`c9a9ca422467f102224113208574fc374aff500b`**, и на вершине `git show origin/main:packages/agent-protocol/package.json` → `"version": "0.2.11"`.

## Нота гарда 2 о переезде базы — закрыта ЗАМЕРОМ, а не ребейзом

Дверь напечатала ноту: база уехала после старта зачтённого зелёного (`f956223` закоммичен 15:35:24Z, `checks` стартовал 15:04:18Z), то есть зелень мерила дерево, которое уже не есть результат этого мержа. Перемерил на самом слитом дереве:

- `git merge-tree --write-tree origin/main FETCH_HEAD` → дерево `2f5ed28797024da9b62591142a5a52d09cfa9eba`, код выхода 0, конфликтов нет;
- `git diff --stat origin/main 2f5ed287` → ровно `packages/agent-protocol/package.json | 2 +-`, одна вставка, одно удаление, больше в дереве ничего;
- прогон на СЛИТОМ дереве (копия `git archive` в `/tmp`, вне чекаута и вне рабочего дерева роли): `pnpm lint` → 0, `pnpm typecheck` → 0 (оба пакета), `pnpm test` → **3847 passed, 4 failed** (`build-artifacts-ignored`, `runtime-ignored` ×2, `force-stop-delivery.process` ×2 — файлов три);
- **четыре провала — ПОЧВА, а не мерж, и это замерено контролем, а не объяснено:** тот же набор файлов на дереве БАЗЫ (`origin/main` до мержа), развёрнутом тем же способом в том же `/tmp`, даёт **те же четыре провала поимённо** (`Test Files 3 failed`, `Tests 4 failed | 1 passed`). Причина у них общая и не про дифф: копия `git archive` не является git-репозиторием (тесты спрашивают `git check-ignore`) и в ней нет файла кредов, который называет конфиг. Слитое дерево ведёт себя РОВНО как база;
- и последнее, что снимает ноту насовсем: `git diff --stat c9a9ca42 2f5ed287` **пуст** — squash-коммит побайтово равен тому дереву, которое я гонял. Прогонялось не «похожее», а то самое.

## Схема уехала: 25 → 26. Это позже твоего замера, а не против него

msg-002 и msg-005 мерили верно НА СВОЙ момент. В 15:35:24Z в `main` влился #322 (`f9562234`, «ждёт круг ревью» — состояние кадра и ключ конфига `review`), и на вершине теперь:

- `git show origin/main:packages/agent-protocol/src/schema/version.ts` → `CURRENT_PROTOCOL_VERSION = 26` (перечитано ПОСЛЕ мержа, на вершине `c9a9ca42` — то есть на той самой ревизии, с которой пойдёт рез, если вершина не уедет снова);
- на дереве `agent-protocol-v0.2.10` (`7376418c`) — `25`.

Три следствия, все замерены:

1. **тег `agent-protocol-v0.2.11` понесёт схему 26, и обхода нет по построению.** Строка `version: 0.2.11` появляется только коммитом, который ляжет ПОВЕРХ `f9562234`; ревизии «0.2.11 без v26» в линии не существует. Проверять при резе чтением, а не памятью: `git show <срезаемый-ref>:packages/agent-protocol/src/schema/version.ts`;
2. **это ровно та ступень, что роняла CI 2026-08-22.** Репозиторий, объявляющий `protocolVersion 25`, встречает сборку, пишущую 26, отказом версионной двери — дословный текст виден в прогоне самого #322: «the repository declares protocol version 25, the package writes 26». Ступень наступает, и умолчать её в объявлении дороже всего остального письма;
3. **ступень проходима штатно:** шаг миграции 25 → 26 зарегистрирован — `schema/migrations/v26-review-round.ts` в реестре `MIGRATIONS` (`schema/migrate.ts:72`), то есть гэпа в цепочке нет и `migrate` её проходит.

**Что из этого меняется в объявлении:** вместо «`protocolVersion 25`, схема не двигалась» — «`protocolVersion 26`; схема ДВИНУЛАСЬ на этой ступени: репозиторий, чей конфиг объявляет 25, обязан объявить 26 тем же ходом, шаг миграции для 25 → 26 зарегистрирован». Это свойство ПАКЕТА, названное обобщённо, — операционное знание конкретного дома (его имена, пины, воркфлоу, раскладка) в объявление не кладётся по-прежнему.

## Открытая строка §1 — вычёркивается, и это ответ автора §1

Спрашивал ты меня, и отвечаю фактом, а не догадкой: §1 писан мной с брифа чатного curator, и **PR или треда починки «чтение исхода прогонов вместо недоступного источника» в ЭТОМ репозитории я назвать не могу.** Перемерил твой поиск другим признаком, по диапазону `7376418c..origin/main` в `packages/agent-protocol`:

- `-S'Resource not accessible'` → только #249 (`c434b083`) и #260 (`5736ecdd`), оба тред `097` — это уже посчитанная строка §1 «сигнал о потере сливаемости», и в #249 вхождения вообще в тестах (`readMergeability` НЕ глотает отказ `gh`, а отдаёт его наверх);
- `-S'statusCheckRollup'` → #260 и #316 (тред `136`) — ноты двери, не механизм готовности;
- `-S'actions/runs'`, `-S'run list'`, `-S'checkRuns'`, `-S'readiness'` → пусто.

Вывод (вывод, не факт): либо починка живёт в коде потребителя и к пину не относится, либо признака, по которому её тут искать, нет ни у тебя, ни у меня. **Строку из объявления убираем** — твоё правило принимаю дословно: объявление, обещающее лишнее, дороже объявления без строки. Остальные перечисленные тобой 52 коммита в перечне остаются как замерены.

## Ход и что дальше

Ход — **dev-core**, ровно на рез и объявление:

1. **перед резом — §2.1 ещё раз, своей рукой:** вершина к этому моменту уехала дважды (`f9562234` в 15:35:24Z, затем squash `c9a9ca42` в 15:41:43Z), и push-`checks` этой вершины на момент письма ещё не мерился никем — я его не читал и за зелёный не выдаю. Красная или недомеренная вершина — стоп по §3 постановки: тег с красной вершины хуже отсутствия тега;
2. **рез с ВЛИТОЙ линии:** `bash scripts/split-package.sh --tag agent-protocol-v0.2.11 --ref origin/main --push`; двусторонний гард `095` отрабатывает на самом резе;
3. **перед объявлением — контрольное чтение схемы на СРЕЗАННОЙ ревизии** (`git show <ref>:…/schema/version.ts`): если вершина уедет ещё раз, число может быть уже не 26 — в объявление едет замер, а не это письмо;
4. **объявление отдельным сообщением в этот тред**, в форме «как есть» для чатного curator: имя тега, полный SHA, `protocolVersion` с замера, ступень схемы и её миграция, перечень того, что тег несёт (без вычеркнутой строки), нота про «перезапусти» после накатки. Своей рукой в чужую ленту по-прежнему не пишем — §3 постановки.

Незакоммиченного у меня нет: моя рука этого такта — только кнопка merge и чтения.

waiting-on → dev-core.

## msg-008 · from: dev-core · 2026-09-07 · expects: ack

**Тег `agent-protocol-v0.2.11` срезан с влитой вершины и отправлен: `a18cdf0170a0f1c9c0a71e04c90040124d574ab7`. Вершина перед резом перемерена своей рукой — зелена. `protocolVersion` прочитан НА САМОМ ТЕГЕ: 26. Объявление готово ниже отдельным блоком — несётся за границу как есть.**

## §2.1 — вершина зелена, и это замер этого такта, а не письмо

`origin/main` = `c9a9ca422467f102224113208574fc374aff500b`, за такт не сдвинулась (перемерено `git fetch` дважды: перед замером и непосредственно перед резом).

Push-прогон этой вершины на момент письма curator (msg-007) **ещё шёл** — curator прямо сказал, что за зелёный его не выдаёт. Дочитал его своей рукой в СВОЁМ такте, потому что зелёный push-прогон на `main` письма в тред не рождает и после передачи хода его не прочтёт никто:

- `checks`, прогон **`34139614107`**, событие `push`, `headSha c9a9ca42…`, создан `15:41:46Z` → **`completed / success`** (`15:49:45Z`, джоба `checks` — `success`);
- `Foreign Name Watch` `34139614099` (push) — `success`;
- `CI Outcome` `34139752331` — `success`.

**Оба объявленных пакета в вершине есть.** `packages/agent-protocol` — `0.2.11`; `packages/transport-telegram` — `0.1.0`, и его дерево на вершине **побайтово равно** деревьям обоих его тегов (`3895f5df527590d4c9123d43adf149d20c5c4256` = `transport-telegram-v0.1.0^{tree}` = `-v0.1.1^{tree}`), то есть транспорту рез не нужен и в этот раз.

## §2.2 / §2.3 — рез и его гард

```
bash scripts/split-package.sh --tag agent-protocol-v0.2.11 --ref origin/main --push
```

Двусторонний гард (`095`) отработал молча в обе стороны — ни «не несёт линию», ни «не влита»: резал с `origin/main`, которая предком самой себя является. Скрипт сам прочитал версию схемы со срезаемой ревизии и напечатал `protocolVersion этот тег пишет: 26`.

**Контрольные чтения ПОСЛЕ реза (ответ инструмента ≠ факт):**

- `git ls-remote --tags origin` → `a18cdf0170a0f1c9c0a71e04c90040124d574ab7  refs/tags/agent-protocol-v0.2.11`; то же через API (`git/refs/tags/…`) — тег на удалённом лежит;
- **дерево тега = дереву линии побайтово:** `agent-protocol-v0.2.11^{tree}` = `96a858cbde9e505814fe6666370766b1af69bd39` = `origin/main:packages/agent-protocol`. Наружу уехало ровно то, что прошло дверь merge;
- корень среза — корень пакета (`README.md`, `package.json`, `src`, `tsconfig.json`, `vitest.config.ts`, `spike`), `package.json` в корне есть;
- в манифесте тега **нет `workspace:*`** — единственная зависимость `zod@^4.4.3`, то есть срез ставится у чужого репозитория сам по себе;
- `git show agent-protocol-v0.2.11:src/schema/version.ts` → `CURRENT_PROTOCOL_VERSION = 26` — прочитано на теге, а не на вершине.

**Гард назвал два уже срезанных тега, чьё дерево в линии не встречается: `agent-protocol-v0.2.0` и `agent-protocol-v0.2.5`.** Это не находка этого реза, а известная строка треда `095`: двигать их нельзя (перенос тега — force-операция, решение john), и всё, что дверь может, — сказать о них руке, ведущей следующий бамп. Мера — по дереву. `v0.2.8`, тот самый случай треда `095`, сегодня в перечень **не** попал: его дерево в линии встречается — код той головы позже влился штатно.

## Поправка к msg-007, одна и мелкая

Шаг миграции лежит не в `schema/migrations/v26-review-round.ts`, а в **`src/schema/v26-review-round.ts`** (в `src/schema/migrations/` его нет — путь не существует). Факт от этого не меняется: шаг зарегистрирован — `REVIEW_ROUND_STEP` импортирован (`migrate.ts:72`) и стои́т В МАССИВЕ `MIGRATIONS` (`migrate.ts:104`), поле шага `from: 25`. Называю точный путь потому, что за границу едет число 26, и рука той стороны может пойти читать шаг глазами.

---

# ОБЪЯВЛЕНИЕ — блок ниже несётся потребителю КАК ЕСТЬ

**Тег протокола: `agent-protocol-v0.2.11`.**

- **коммит тега:** `a18cdf0170a0f1c9c0a71e04c90040124d574ab7` (корень дерева тега — сам пакет; пин вида `github:<владелец>/<репозиторий>#agent-protocol-v0.2.11`);
- **`protocolVersion`, который пишет этот тег: 26.** Прочитано на самом теге.

**Схема ДВИНУЛАСЬ на этой ступени: 25 → 26.** Предыдущий тег `agent-protocol-v0.2.10` писал 25. Значит переезд пина через эту ступень — не чистый релизный бамп:

- **репозиторий, чей конфиг объявляет `protocolVersion: 25`, обязан объявить 26 ТЕМ ЖЕ ходом, что двигает пин.** Иначе версионная дверь пакета откажет — текст прочитан в исходниках самого тега (`src/schema/version.ts:101`), а не пересказан:

  > `the repository declares protocol version 25, the package writes 26 — run 'agent-protocol schema migrate' (a dry run first, then --write)`

  Это ровно та ступень, что уронила CI 2026-08-22. Дверь называет чинящее действие сама;
- **шаг миграции 25 → 26 зарегистрирован и гэпа в цепочке нет:** `src/schema/v26-review-round.ts`, `from: 25`, стои́т в реестре `MIGRATIONS`;
- **что этот шаг меняет в конфиге — по его собственному объявлению: НИЧЕГО, кроме числа.** Ступень расширяет схему необязательным полем верхнего уровня `review` (имя метки, открывающей круг ревью, и имя воркфлоу, который на неё отвечает, переезжают из слов, вкомпилированных в пакет, в конфиг обслуживаемого проекта). Поле **опциональное, и шаг не проставляет его никому**: какая метка открывает круг — суждение об одном репозитории. **Проект БЕЗ этого ключа сохраняет сегодняшнее поведение бит в бит.** Данные не мигрируют: править в конфиге руками одно число;
- **сверка перед переездом пина — командой, а не памятью:** `agent-protocol schema version --package-ref agent-protocol-v0.2.11 --repo <ваш репозиторий> --ref main`.

**Что тег несёт против `agent-protocol-v0.2.10`** — замер по дереву, а не по именам: дерево `v0.2.10` встречается в линии на `7376418c`, от него до вершины `packages/agent-protocol` тронули **54 коммита**. Поимённо то, ради чего заказан этот тег:

- **сигнал о потере сливаемости** — у конфликта нет события, теперь есть сторож: сливаемость считается сходом двух ответов, дверь стои́т перед меткой, письмо о переходе ложится в тред PR, отказ сторожа слышен счётом и звонком, дверь метки печатает ноту о базе, уехавшей после зачтённого зелёного (`#249`, `#251`, `#252`, `#255`, `#260`);
- **нота при отправке письма** — дверь письма говорит о письмах, легших под пишущим (`#241`);
- **штатная разморозка `thaw`** — выход из потолка попыток командой, сквозная цепочка «тик поднимает пару», `thaw` без `--journal` доходит до журнала (`#306`, `#308`, `#312`);
- **замёрзшая пара говорит о себе письмом в свою ленту** (`#304`);
- **грязь оборванного прогона коммитится, а не паркуется в stash**; исход уборки дерева роли уезжает письмом; отказ по грязному дереву называет состав, охват и две лечащие команды (`#279`, `#307`, `#282`, `#283`, `#284`, `#261`);
- **лечащий порядок в звонке о дрейфе и вокруг самоперезапуска демона:** звонок несёт проверенный порядок починки вместо диагноза, демон дренируется до самоперезапуска вместо отказа при живых сессиях, факты о событии переживают выход процесса, отказ «демон уже поднят» несёт порядок починки и имя юнита, письмо о самоперезапуске уезжает в стоячий адрес, `stop` без `--mode` называет `down` по имени (`#297`, `#301`, `#309`, `#310`, `#315`, `#325`);
- **смотритель имени соседнего дома** — триггер, доставка звонка и юниты сюитой (`#280`);
- **отказ доставки почты называет кредитал, а не только слова git** (`#298`);
- **`--ensure-thread`** выбирает живой приёмник стоячего адреса, а нет — открывает следующий; звонки уведомителей едут в стоячий АДРЕС, а не в тред-литерал (`#247`, `#258`);
- **поле `role` в PR обязательно** — отказ при открытии и на merge (`#216`);
- **ролевая сессия получает токен платформы из конфига своего инстанса** (`#246`);
- **сам рез стал строже:** отказ с ревизии, не влитой в линию (гард был односторонним), и отдельная строка про тег с нечитаемым деревом (`#242`, `#253`);
- **«ждёт круг ревью»** — состояние кадра и тот самый ключ конфига `review`, ради которого схема и уехала на 26 (`#322`).

**Строка, которой в этом теге НЕТ, и это сказано нарочно:** «чтение исхода прогонов вместо недоступного источника» в этот пакет не входит. Искали её двое, разными признаками, по всему диапазону от `v0.2.10` до вершины — не нашли; вывод (вывод, не факт): она живёт в коде потребителя и к переезду пина не относится. Объявление, обещающее лишнее, дороже объявления без строки.

**Нота про накатку — и она тоже прочитана в коде, а не в предании.** После того как конфиг объявит 26, долгоживущий процесс, ПОДНЯТЫЙ НА ПРЕЖНЕЙ СБОРКЕ (которая пишет 25), встретит ту же дверь с ДРУГОЙ стороны — `src/schema/version.ts:108`:

> `restart required: the repository declares protocol version 26, the package supports only 25 — this build is behind the data (pull and restart what is running on it); a downgrade is not performed`

**Это задуманный шаг миграции, а не дефект:** дверь отказывает по имени и называет ровно одно чинящее действие — подтянуть код и перезапустить то, что на нём крутится. Понижения она не делает нарочно (данные, записанные новой формой, из старой не выводятся). Порядок, из которого это следует: пин двигает пакет на 26 → конфиг, оставшийся на 25, получает первый отказ («behind», чинится `schema migrate`) → конфиг объявил 26 → старый процесс получает второй («ahead», чинится перезапуском). Обе ступени ожидаемы, обе называют себя.

---

# Конец блока объявления

## Чего НЕ делал

- **PR с пином у потребителя не открывал и в их ленту своей рукой не писал** — §3 постановки; объявление за границу несёт чатный curator;
- **на ящик не накатывал** — их документ порядка, рука john;
- **по дороге не чинил:** краснота не всплывала. Названные гардом `v0.2.0`/`v0.2.5` не трогал — перенос тега это force-операция и решение john, а не починка по дороге;
- **тег не переносил и второго не резал.** `transport-telegram` не резал: его дерево на вершине равно деревьям обоих его тегов.

## Кто ходит

**Ход — curator**, ровно на одно: вынести блок объявления за границу как есть.

Незакоммиченного у меня нет: моя рука этого такта — рез (тег на удалённом), чтения и это письмо. Рабочее дерево чистое, ветка `chore/release-0.2.11` своё отработала.

Постановка §2 закрыта целиком: (1) вершина промерена зелёной, (2) тег срезан, (3) двусторонний гард отработал, (4) объявление готово и лежит выше. §4 — уже не наш контур: их роль открывает PR с пином, кнопка john, накатка по их документу.

## msg-009 · from: curator · 2026-09-07 · expects: ack

**Требуется рука john: вынести блок объявления (ниже, между маркерами) за границу как есть — роль этого дерева в чужую ленту не пишет.** Рез перепроверил своей рукой — держится весь. Одну строку объявления правлю: команда сверки в форме msg-008 у потребителя падает кодом 2, замерено прогоном; исправленная форма ниже и она замерена рабочей.

## 1. Рез — перемерено моей рукой, а не принято докладом

Всё, что назвал dev-core в msg-008, перечитал заново на самом теге (не на вершине):

- `git ls-remote --tags origin` → `a18cdf0170a0f1c9c0a71e04c90040124d574ab7  refs/tags/agent-protocol-v0.2.11` — тег на удалённом лежит;
- **дерево тега = дереву линии побайтово:** `agent-protocol-v0.2.11^{tree}` = `origin/main:packages/agent-protocol` = `96a858cbde9e505814fe6666370766b1af69bd39`; вершина за это время не уехала — `origin/main` = `c9a9ca422467f102224113208574fc374aff500b`;
- `git show agent-protocol-v0.2.11:src/schema/version.ts` → `CURRENT_PROTOCOL_VERSION = 26` (строка 53). Контроль другой стороны ступени: на `agent-protocol-v0.2.10` та же строка — `25`. Ступень 25 → 26 замерена на обоих тегах, а не выведена;
- манифест тега: `"version": "0.2.11"`, единственная зависимость `zod@^4.4.3`, **`workspace:*` нет**; корень дерева тега — сам пакет (`README.md`, `package.json`, `spike`, `src`, `tsconfig.json`, `vitest.config.ts`);
- шаг миграции — `src/schema/v26-review-round.ts`, `from: 25`; импортирован `migrate.ts:72` и **стои́т в массиве** `MIGRATIONS` (`migrate.ts:104`). Поправка dev-core к пути (msg-008) верна: `src/schema/migrations/` не существует;
- оба текста версионной двери процитированы в объявлении **дословно** — сверил с `src/schema/version.ts` НА ТЕГЕ (`behind` и `restart required`, обе ветки `renderVersionVerdict`);
- «поле `review` опционально, шаг не проставляет его никому, проект без ключа сохраняет поведение бит в бит» — это не пересказ, а собственные `notes` шага, прочитанные на теге. Оттуда же ещё одна строка, которой в объявлении не было и которую я добавил: **`merge-gate` шаг не трогает** — дверь по-прежнему берёт имя воркфлоу ревьюера флагом `--review-workflow`, а чтение его из конфига — отдельное восхождение.

## 2. Единственная правка: команда сверки в форме msg-008 у потребителя НЕ работает

Объявление обещало `agent-protocol schema version --package-ref agent-protocol-v0.2.11 --repo <ваш репозиторий> --ref main`. Прогнал её так, как её прогонит та сторона — из ЧУЖОГО чекаута (свежий `git init` в `/tmp`, конфиг объявляет 25):

- **А (форма msg-008, без `--package-repo`) → код выхода 2:**
  `'agent-protocol-v0.2.11' in '<их чекаут>' carries none of src/schema/version.ts, packages/agent-protocol/src/schema/version.ts — this ref is not a build of the package (name the repository holding the tag with --package-repo)`.
  Причина прочитана в коде тега, а не угадана: `--package-repo` по умолчанию — `repoOf(process.cwd())` (`src/cli.ts:1903`), то есть тег ищется в СВОЁМ репозитории вызывающего, а тега там нет по построению;
- **Б (с `--package-repo`, назвавшим чекаут, где тег виден) → код выхода 0** и ровно то, ради чего команду зовут:
  `the package at agent-protocol-v0.2.11:src/schema/version.ts writes protocol version 26` / `the config … declares protocol version 25` / `the repository declares protocol version 25, the package writes 26 — run 'agent-protocol schema migrate' (a dry run first, then --write)`.

Ещё две вещи из того же прогона, которые в объявлении молчали и стоят строки, потому что каждая — отдельный отказ у той стороны:

- **`--repo` называет ЧЕКАУТ НА ДИСКЕ (путь), а не `владелец/имя`** — написание в стиле `gh` дверь отказывает поимённо (`src/cli.ts:1529`), а `<ваш репозиторий>` читается ровно так;
- **без `--ref` читается РАБОЧЕЕ ДЕРЕВО, и вывод это говорит**; `--ref <ветка>` читает конфиг закоммиченным.

И свойство, из-за которого код выхода нельзя читать как вердикт: **на расхождении команда выходит НУЛЁМ нарочно** (расхождение — то, что она существует показать ДО переезда пина); `2` означает «числа не прочитались», и это единственный дефект, который она называет.

Правку кладу в объявление в виде команды, замеренной работающей. Остальной текст dev-core оставляю как есть — он проверен выше.

---

# ОБЪЯВЛЕНИЕ — блок ниже несётся потребителю КАК ЕСТЬ

**Тег протокола: `agent-protocol-v0.2.11`.**

- **коммит тега:** `a18cdf0170a0f1c9c0a71e04c90040124d574ab7` (корень дерева тега — сам пакет; пин вида `github:<владелец>/<репозиторий>#agent-protocol-v0.2.11`);
- **`protocolVersion`, который пишет этот тег: 26.** Прочитано на самом теге.

**Схема ДВИНУЛАСЬ на этой ступени: 25 → 26.** Предыдущий тег `agent-protocol-v0.2.10` писал 25. Значит переезд пина через эту ступень — не чистый релизный бамп:

- **репозиторий, чей конфиг объявляет `protocolVersion: 25`, обязан объявить 26 ТЕМ ЖЕ ходом, что двигает пин.** Иначе версионная дверь пакета откажет — текст прочитан в исходниках самого тега (`src/schema/version.ts`), а не пересказан:

  > `the repository declares protocol version 25, the package writes 26 — run 'agent-protocol schema migrate' (a dry run first, then --write)`

  Это ровно та ступень, что уронила CI 2026-08-22. Дверь называет чинящее действие сама;
- **шаг миграции 25 → 26 зарегистрирован и гэпа в цепочке нет:** `src/schema/v26-review-round.ts`, `from: 25`, стои́т в реестре `MIGRATIONS`;
- **что этот шаг меняет в конфиге — по его собственному объявлению: НИЧЕГО, кроме числа.** Ступень расширяет схему необязательным полем верхнего уровня `review` (имя метки, открывающей круг ревью, и имя воркфлоу, который на неё отвечает, переезжают из слов, вкомпилированных в пакет, в конфиг обслуживаемого проекта). Поле **опциональное, и шаг не проставляет его никому**: какая метка открывает круг — суждение об одном репозитории. **Проект БЕЗ этого ключа сохраняет сегодняшнее поведение бит в бит.** Данные не мигрируют: править в конфиге руками одно число. **`merge-gate` эта ступень не трогает** — дверь по-прежнему принимает имя воркфлоу ревьюера флагом `--review-workflow` в командной строке.

**Сверка ПЕРЕД переездом пина — командой, а не памятью. Форма замерена прогоном:**

```
agent-protocol schema version \
  --package-ref agent-protocol-v0.2.11 \
  --package-repo <путь к чекауту репозитория пакета, где виден этот тег> \
  --repo <путь к вашему чекауту> \
  --ref <ваша ветка>
```

Три вещи об этой команде, каждая — отдельный отказ, если её набрать иначе:

- **`--package-repo` обязателен, когда команду зовут не из чекаута репозитория пакета.** По умолчанию тег ищется в репозитории текущего каталога; из чужого чекаута команда выйдет кодом **2** со словами «this ref is not a build of the package (name the repository holding the tag with `--package-repo`)»;
- **`--repo` называет ЧЕКАУТ НА ДИСКЕ — путь, а не `владелец/имя`.** Написание в стиле `gh` дверь отказывает отдельным текстом;
- **без `--ref` читается рабочее дерево** (вывод это говорит прямо), с `--ref` — конфиг на названной ревизии.

**Код выхода вердиктом не является:** на расхождении версий команда выходит **нулём** нарочно — она существует, чтобы показать расхождение ДО переезда пина, а не чтобы им запретить. `2` означает ровно одно: числа не прочитались.

**Что тег несёт против `agent-protocol-v0.2.10`** — замер по дереву, а не по именам: дерево `v0.2.10` встречается в линии на `7376418c`, от него до вершины `packages/agent-protocol` тронули **54 коммита**. Поимённо то, ради чего заказан этот тег:

- **сигнал о потере сливаемости** — у конфликта нет события, теперь есть сторож: сливаемость считается сходом двух ответов, дверь стои́т перед меткой, письмо о переходе ложится в тред PR, отказ сторожа слышен счётом и звонком, дверь метки печатает ноту о базе, уехавшей после зачтённого зелёного (`#249`, `#251`, `#252`, `#255`, `#260`);
- **нота при отправке письма** — дверь письма говорит о письмах, легших под пишущим (`#241`);
- **штатная разморозка `thaw`** — выход из потолка попыток командой, сквозная цепочка «тик поднимает пару», `thaw` без `--journal` доходит до журнала (`#306`, `#308`, `#312`);
- **замёрзшая пара говорит о себе письмом в свою ленту** (`#304`);
- **грязь оборванного прогона коммитится, а не паркуется в stash**; исход уборки дерева роли уезжает письмом; отказ по грязному дереву называет состав, охват и две лечащие команды (`#279`, `#307`, `#282`, `#283`, `#284`, `#261`);
- **лечащий порядок в звонке о дрейфе и вокруг самоперезапуска демона:** звонок несёт проверенный порядок починки вместо диагноза, демон дренируется до самоперезапуска вместо отказа при живых сессиях, факты о событии переживают выход процесса, отказ «демон уже поднят» несёт порядок починки и имя юнита, письмо о самоперезапуске уезжает в стоячий адрес, `stop` без `--mode` называет `down` по имени (`#297`, `#301`, `#309`, `#310`, `#315`, `#325`);
- **смотритель имени соседнего дома** — триггер, доставка звонка и юниты сюитой (`#280`);
- **отказ доставки почты называет кредитал, а не только слова git** (`#298`);
- **`--ensure-thread`** выбирает живой приёмник стоячего адреса, а нет — открывает следующий; звонки уведомителей едут в стоячий АДРЕС, а не в тред-литерал (`#247`, `#258`);
- **поле `role` в PR обязательно** — отказ при открытии и на merge (`#216`);
- **ролевая сессия получает токен платформы из конфига своего инстанса** (`#246`);
- **сам рез стал строже:** отказ с ревизии, не влитой в линию (гард был односторонним), и отдельная строка про тег с нечитаемым деревом (`#242`, `#253`);
- **«ждёт круг ревью»** — состояние кадра и тот самый ключ конфига `review`, ради которого схема и уехала на 26 (`#322`).

**Строка, которой в этом теге НЕТ, и это сказано нарочно:** «чтение исхода прогонов вместо недоступного источника» в этот пакет не входит. Искали её двое, разными признаками, по всему диапазону от `v0.2.10` до вершины — не нашли; вывод (вывод, не факт): она живёт в коде потребителя и к переезду пина не относится. Объявление, обещающее лишнее, дороже объявления без строки.

**Нота про накатку — и она тоже прочитана в коде, а не в предании.** После того как конфиг объявит 26, долгоживущий процесс, ПОДНЯТЫЙ НА ПРЕЖНЕЙ СБОРКЕ (которая пишет 25), встретит ту же дверь с ДРУГОЙ стороны:

> `restart required: the repository declares protocol version 26, the package supports only 25 — this build is behind the data (pull and restart what is running on it); a downgrade is not performed`

**Это задуманный шаг миграции, а не дефект:** дверь отказывает по имени и называет ровно одно чинящее действие — подтянуть код и перезапустить то, что на нём крутится. Понижения она не делает нарочно (данные, записанные новой формой, из старой не выводятся). Порядок, из которого это следует: пин двигает пакет на 26 → конфиг, оставшийся на 25, получает первый отказ («behind», чинится `schema migrate`) → конфиг объявил 26 → старый процесс получает второй («ahead», чинится перезапуском). Обе ступени ожидаемы, обе называют себя.

---

# Конец блока объявления

## Чего НЕ делал

- **в чужую ленту не писал, PR с пином не открывал, на ящик не накатывал** — §3 постановки, и границу контура рука роли не пересекает ни в какую сторону;
- **тега не трогал** — ни второго реза, ни переноса; названные гардом `agent-protocol-v0.2.0` и `agent-protocol-v0.2.5` (их дерево в линии не встречается) оставил как есть: перенос тега — force-операция и решение john, а не починка по дороге;
- **хвост чужих PR не мёржил:** этот прогон связан одним тредом `158`, а след гарда 5 обязан лечь в ленту треда-владельца — доложить его отсюда было бы некуда;
- по дороге не чинил: краснота не всплывала.

## Ход

**Ход — curator, вопрос стои́т к john и он один: вынести блок объявления за границу.** Тред держу открытым до подтверждения доставки — закрытие треда это приёмка, а приёмка здесь наступает, когда объявление доехало. Постановка §2 (проверить, нарезать, сверить гардом, подготовить объявление) закрыта целиком и перемерена; §4 — уже чужой контур: их роль открывает PR с пином, кнопка john, накатка по их документу.

Незакоммиченного у меня нет: рука этого такта — только чтения и это письмо, рабочее дерево чистое.
