---
from: dev-core
worker: claude-code
session: c025a5a8-5b54-474d-a820-225c1925e9bc
raised: 2026-09-07T15:47:17Z
date: 2026-09-07T15:53:28Z
expects: ack
waiting-on: curator
---

**Тег `agent-protocol-v0.2.11` срезан с влитой вершины и отправлен: `a18cdf0170a0f1c9c0a71e04c90040124d574ab7`. Вершина перед резом перемерена своей рукой — зелена. `protocolVersion` прочитан НА САМОМ ТЕГЕ: 26. Объявление готово ниже отдельным блоком — несётся за границу как есть.**

## §2.1 — вершина зелена, и это замер этого такта, а не письмо

`origin/main` = `c9a9ca422467f102224113208574fc374aff500b`, за такт не сдвинулась (перемерено `git fetch` дважды: перед замером и непосредственно перед резом).

Push-прогон этой вершины на момент письма curator (msg-007) **ещё шёл** — curator прямо сказал, что за зелёный его не выдаёт. Дочитал его своей рукой в СВОЁМ такте, потому что зелёный push-прогон на `main` письма в тред не рождает и после передачи хода его не прочтёт никто:

- `checks`, прогон **`34139614107`**, событие `push`, `headSha c9a9ca42…`, создан `15:41:46Z` → **`completed / success`** (`15:49:45Z`, джоба `checks` — `success`);
- `Foreign Name Watch` `34139614099` (push) — `success`;
- `CI Outcome` `34139752331` — `success`.

**Оба объявленных пакета в вершине есть.** `packages/agent-protocol` — `0.2.11`; `packages/transport-telegram` — `0.1.0`, и его дерево на вершине **побайтово равно** деревьям обоих его тегов (`3895f5df527590d4c9123d43adf149d20c5c4256` = `transport-telegram-v0.1.0^{tree}` = `-v0.1.1^{tree}`), то есть транспорту рез не нужен и в этот раз.

## §2.2 / §2.3 — рез и его гард

```
bash scripts/split-package.sh --tag agent-protocol-v0.2.11 --ref origin/main --push
```

Двусторонний гард (`095`) отработал молча в обе стороны — ни «не несёт линию», ни «не влита»: резал с `origin/main`, которая предком самой себя является. Скрипт сам прочитал версию схемы со срезаемой ревизии и напечатал `protocolVersion этот тег пишет: 26`.

**Контрольные чтения ПОСЛЕ реза (ответ инструмента ≠ факт):**

- `git ls-remote --tags origin` → `a18cdf0170a0f1c9c0a71e04c90040124d574ab7  refs/tags/agent-protocol-v0.2.11`; то же через API (`git/refs/tags/…`) — тег на удалённом лежит;
- **дерево тега = дереву линии побайтово:** `agent-protocol-v0.2.11^{tree}` = `96a858cbde9e505814fe6666370766b1af69bd39` = `origin/main:packages/agent-protocol`. Наружу уехало ровно то, что прошло дверь merge;
- корень среза — корень пакета (`README.md`, `package.json`, `src`, `tsconfig.json`, `vitest.config.ts`, `spike`), `package.json` в корне есть;
- в манифесте тега **нет `workspace:*`** — единственная зависимость `zod@^4.4.3`, то есть срез ставится у чужого репозитория сам по себе;
- `git show agent-protocol-v0.2.11:src/schema/version.ts` → `CURRENT_PROTOCOL_VERSION = 26` — прочитано на теге, а не на вершине.

**Гард назвал два уже срезанных тега, чьё дерево в линии не встречается: `agent-protocol-v0.2.0` и `agent-protocol-v0.2.5`.** Это не находка этого реза, а известная строка треда `095`: двигать их нельзя (перенос тега — force-операция, решение john), и всё, что дверь может, — сказать о них руке, ведущей следующий бамп. Мера — по дереву. `v0.2.8`, тот самый случай треда `095`, сегодня в перечень **не** попал: его дерево в линии встречается — код той головы позже влился штатно.

## Поправка к msg-007, одна и мелкая

Шаг миграции лежит не в `schema/migrations/v26-review-round.ts`, а в **`src/schema/v26-review-round.ts`** (в `src/schema/migrations/` его нет — путь не существует). Факт от этого не меняется: шаг зарегистрирован — `REVIEW_ROUND_STEP` импортирован (`migrate.ts:72`) и стои́т В МАССИВЕ `MIGRATIONS` (`migrate.ts:104`), поле шага `from: 25`. Называю точный путь потому, что за границу едет число 26, и рука той стороны может пойти читать шаг глазами.

---

# ОБЪЯВЛЕНИЕ — блок ниже несётся потребителю КАК ЕСТЬ

**Тег протокола: `agent-protocol-v0.2.11`.**

- **коммит тега:** `a18cdf0170a0f1c9c0a71e04c90040124d574ab7` (корень дерева тега — сам пакет; пин вида `github:<владелец>/<репозиторий>#agent-protocol-v0.2.11`);
- **`protocolVersion`, который пишет этот тег: 26.** Прочитано на самом теге.

**Схема ДВИНУЛАСЬ на этой ступени: 25 → 26.** Предыдущий тег `agent-protocol-v0.2.10` писал 25. Значит переезд пина через эту ступень — не чистый релизный бамп:

- **репозиторий, чей конфиг объявляет `protocolVersion: 25`, обязан объявить 26 ТЕМ ЖЕ ходом, что двигает пин.** Иначе версионная дверь пакета откажет — текст прочитан в исходниках самого тега (`src/schema/version.ts:101`), а не пересказан:

  > `the repository declares protocol version 25, the package writes 26 — run 'agent-protocol schema migrate' (a dry run first, then --write)`

  Это ровно та ступень, что уронила CI 2026-08-22. Дверь называет чинящее действие сама;
- **шаг миграции 25 → 26 зарегистрирован и гэпа в цепочке нет:** `src/schema/v26-review-round.ts`, `from: 25`, стои́т в реестре `MIGRATIONS`;
- **что этот шаг меняет в конфиге — по его собственному объявлению: НИЧЕГО, кроме числа.** Ступень расширяет схему необязательным полем верхнего уровня `review` (имя метки, открывающей круг ревью, и имя воркфлоу, который на неё отвечает, переезжают из слов, вкомпилированных в пакет, в конфиг обслуживаемого проекта). Поле **опциональное, и шаг не проставляет его никому**: какая метка открывает круг — суждение об одном репозитории. **Проект БЕЗ этого ключа сохраняет сегодняшнее поведение бит в бит.** Данные не мигрируют: править в конфиге руками одно число;
- **сверка перед переездом пина — командой, а не памятью:** `agent-protocol schema version --package-ref agent-protocol-v0.2.11 --repo <ваш репозиторий> --ref main`.

**Что тег несёт против `agent-protocol-v0.2.10`** — замер по дереву, а не по именам: дерево `v0.2.10` встречается в линии на `7376418c`, от него до вершины `packages/agent-protocol` тронули **54 коммита**. Поимённо то, ради чего заказан этот тег:

- **сигнал о потере сливаемости** — у конфликта нет события, теперь есть сторож: сливаемость считается сходом двух ответов, дверь стои́т перед меткой, письмо о переходе ложится в тред PR, отказ сторожа слышен счётом и звонком, дверь метки печатает ноту о базе, уехавшей после зачтённого зелёного (`#249`, `#251`, `#252`, `#255`, `#260`);
- **нота при отправке письма** — дверь письма говорит о письмах, легших под пишущим (`#241`);
- **штатная разморозка `thaw`** — выход из потолка попыток командой, сквозная цепочка «тик поднимает пару», `thaw` без `--journal` доходит до журнала (`#306`, `#308`, `#312`);
- **замёрзшая пара говорит о себе письмом в свою ленту** (`#304`);
- **грязь оборванного прогона коммитится, а не паркуется в stash**; исход уборки дерева роли уезжает письмом; отказ по грязному дереву называет состав, охват и две лечащие команды (`#279`, `#307`, `#282`, `#283`, `#284`, `#261`);
- **лечащий порядок в звонке о дрейфе и вокруг самоперезапуска демона:** звонок несёт проверенный порядок починки вместо диагноза, демон дренируется до самоперезапуска вместо отказа при живых сессиях, факты о событии переживают выход процесса, отказ «демон уже поднят» несёт порядок починки и имя юнита, письмо о самоперезапуске уезжает в стоячий адрес, `stop` без `--mode` называет `down` по имени (`#297`, `#301`, `#309`, `#310`, `#315`, `#325`);
- **смотритель имени соседнего дома** — триггер, доставка звонка и юниты сюитой (`#280`);
- **отказ доставки почты называет кредитал, а не только слова git** (`#298`);
- **`--ensure-thread`** выбирает живой приёмник стоячего адреса, а нет — открывает следующий; звонки уведомителей едут в стоячий АДРЕС, а не в тред-литерал (`#247`, `#258`);
- **поле `role` в PR обязательно** — отказ при открытии и на merge (`#216`);
- **ролевая сессия получает токен платформы из конфига своего инстанса** (`#246`);
- **сам рез стал строже:** отказ с ревизии, не влитой в линию (гард был односторонним), и отдельная строка про тег с нечитаемым деревом (`#242`, `#253`);
- **«ждёт круг ревью»** — состояние кадра и тот самый ключ конфига `review`, ради которого схема и уехала на 26 (`#322`).

**Строка, которой в этом теге НЕТ, и это сказано нарочно:** «чтение исхода прогонов вместо недоступного источника» в этот пакет не входит. Искали её двое, разными признаками, по всему диапазону от `v0.2.10` до вершины — не нашли; вывод (вывод, не факт): она живёт в коде потребителя и к переезду пина не относится. Объявление, обещающее лишнее, дороже объявления без строки.

**Нота про накатку — и она тоже прочитана в коде, а не в предании.** После того как конфиг объявит 26, долгоживущий процесс, ПОДНЯТЫЙ НА ПРЕЖНЕЙ СБОРКЕ (которая пишет 25), встретит ту же дверь с ДРУГОЙ стороны — `src/schema/version.ts:108`:

> `restart required: the repository declares protocol version 26, the package supports only 25 — this build is behind the data (pull and restart what is running on it); a downgrade is not performed`

**Это задуманный шаг миграции, а не дефект:** дверь отказывает по имени и называет ровно одно чинящее действие — подтянуть код и перезапустить то, что на нём крутится. Понижения она не делает нарочно (данные, записанные новой формой, из старой не выводятся). Порядок, из которого это следует: пин двигает пакет на 26 → конфиг, оставшийся на 25, получает первый отказ («behind», чинится `schema migrate`) → конфиг объявил 26 → старый процесс получает второй («ahead», чинится перезапуском). Обе ступени ожидаемы, обе называют себя.

---

# Конец блока объявления

## Чего НЕ делал

- **PR с пином у потребителя не открывал и в их ленту своей рукой не писал** — §3 постановки; объявление за границу несёт чатный curator;
- **на ящик не накатывал** — их документ порядка, рука john;
- **по дороге не чинил:** краснота не всплывала. Названные гардом `v0.2.0`/`v0.2.5` не трогал — перенос тега это force-операция и решение john, а не починка по дороге;
- **тег не переносил и второго не резал.** `transport-telegram` не резал: его дерево на вершине равно деревьям обоих его тегов.

## Кто ходит

**Ход — curator**, ровно на одно: вынести блок объявления за границу как есть.

Незакоммиченного у меня нет: моя рука этого такта — рез (тег на удалённом), чтения и это письмо. Рабочее дерево чистое, ветка `chore/release-0.2.11` своё отработала.

Постановка §2 закрыта целиком: (1) вершина промерена зелёной, (2) тег срезан, (3) двусторонний гард отработал, (4) объявление готово и лежит выше. §4 — уже не наш контур: их роль открывает PR с пином, кнопка john, накатка по их документу.

