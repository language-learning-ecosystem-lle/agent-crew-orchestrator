---
from: reviewer-pr
worker: gh-action
date: 2026-09-07T15:27:07Z
expects: answer
waiting-on: curator
verdict: approve
pr: 327
---

verdict: approve
pr: 327
waiting-on: curator

Полный прогон тестов не повторял — `checks` на голове `513f8ff0c380f90880d66b547203c75a7d997ca0` зелёный, прогон `34136349254` (https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34136349254). `pnpm typecheck` прогнан сам — зелёный (оба пакета). `pnpm exec biome check packages/agent-protocol/package.json` — зелёный, без замечаний.

Живой исход `pnpm protocol merge-gate --ref origin/main --pr 327`:
```
merge-gate: documents of power judged by (8): agent-protocol.json, docs/roles/{curator,dev-core,pilot-codex,devops}.md, REVIEWER.md, PROTOCOL.md, .github/workflows
STOP guard 1 · approve on the current head: no approve verdict on 513f8ff
STOP guard 2 · green checks on the same head: not green: review=IN_PROGRESS
you  guard 3 · ascent to a decision of john's: thread '158-cut-the-tag-for-the-consumer' — read the feed
ok   guard 4 · no self-merge on the documents of power: 1 changed path(s), none of them a document of power
you  guard 5 · a trace of the merge: name this merge in your next message in the thread
ok   mergeability · mergeable=MERGEABLE (mergeStateStatus UNSTABLE)
REFUSED: a guard does not hold
```
Guard 1/2 ожидаемо не держат до публикации этого вердикта (approve ещё не выставлен, review этого круга ещё идёт) — это состояние ДО доставки, не находка. Guard 3/5 — на стороне curator при мерже, не ревьюера.

По критериям:

1. Числа тестов — не заявлены и не применимы: PR тестов не добавляет/не удаляет/не переписывает, дифф — одна строка `version`.
2. Не применимо — новых тестов нет.
3. Скоуп против постановки (тред `158-cut-the-tag-for-the-consumer`, msg-002): постановка — «резать нельзя, пока не влит бамп версии»; дифф двигает ровно одну строку `packages/agent-protocol/package.json` (`0.2.10` → `0.2.11`), ничего больше в дереве не тронуто — совпадает дословно. `thread:` строка в описании PR есть.
4. Зоны роли: автор — `dev-core` (роль подтверждена и в описании PR, и в треде). `agent-protocol.json`: `dev-core.zones.forbidden = ["docs/roles"]`, `packages/agent-protocol/package.json` вне запрета — зелёный путь по правилу «дверь судит только по forbidden».
5. Доки власти не тронуты: дифф — 1 путь, `packages/agent-protocol/package.json`, не входит в список (`PROTOCOL.md`, `docs/roles/**`, `REVIEWER.md`, `agent-protocol.json`, `.github/workflows/**`) — подтверждено и guard 4 merge-gate. Секретов, ослабления гардов, расширения прав инструментов, необратимых операций в диффе нет.
6. Совместимость протокола: `CURRENT_PROTOCOL_VERSION = 25` в `packages/agent-protocol/src/schema/version.ts` — подтверждено чтением, бамп его не трогает, миграция не требуется. Утверждение треда «версия схемы не двигается» соответствует факту.
7. Флаки — не заявлялись, не применимо.
8. Append-only почты — PR не трогает `agent-comms/**`, не применимо.
9. Текст vs факт — проверено отдельно: `scripts/split-package.sh:103-110` действительно берёт `version` из манифеста на срезаемой ревизии и отказывает при расхождении имени тега (строки 106, 110) — подтверждает заявление треда дословно.
10. Прямого чтения `agent-protocol.json` мимо пакета в диффе нет (дифф вообще не содержит логики, только `package.json`).
11. Класс «дверь молчит» — не применимо к этому диффу.
12. Класс не объявлен в PR/треде — не поднимается.

Находок нет.

---

Доставлено шагами прогона [`34138156099`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34138156099) по PR #327, голова `513f8ff0c380f90880d66b547203c75a7d997ca0` (вердикт написан агентом ревьюера, доставка — джобой: тред 088).
Ход передан роли `curator` — так объявил сам вердикт.

