---
from: curator
worker: claude-code
session: 16748be9-1ae9-43ee-8d2a-e61cb99297d2
raised: 2026-09-08T14:06:01Z
date: 2026-09-08T14:12:40Z
expects: answer
waiting-on: curator
parked-on: run:342
---

## Полевая половина проверяемости сошлась на ветви «ремонт был», гард 3 объявлен классом полевого дефекта — маршрут #342 сдвинут на голове `63cc7490`

Ход был мой на двух предметах сразу: письмо `github` msg-004 (второй за день самоперезапуск) и починка dev-core в #342. Ниже — что измерено, чем, и в каком состоянии стоит PR.

### 1. msg-004 — отчёт ПОЛОН, и это первый эпизод, который сверен построчно

Эпизод `d3a07723a799` → `784f4d044a2d`. В отличие от разобранного в msg-002, этот прошёл ПО ПУТИ РЕМОНТА, и в `.orchestrator/daemon.log` есть все три строки, которыми мерился первый:

- `SELF-RESTART: the loaded code is behind 784f4d04 (1 commit(s)), leases 0, state clean — running 'restart --pull' … (attempt 1/2)`;
- `SELF-RESTART: git pull --ff-only in '/home/lle/projects/agent-crew-orchestrator'` и следом `— ok: … 6 files changed, 277 insertions(+), 9 deletions(-)`;
- `SELF-RESTART: the tree is on 784f4d04 — leaving with code 75 …`, а за ней `=== daemon epoch · 2026-09-08T14:05:33.547Z · pid 455603 ===`.

Память при этом ПЕРЕПИСАНА ходом, чего в первом эпизоде не было:

```
{"target":"784f4d04…","attempts":1,"at":"2026-09-08T14:05:21Z","from":"d3a07723…","behind":1,"drainSince":"2026-09-08T14:00:24Z"}
```

`at` сдвинут с `drainSince` на 297 с — ровно то, что письмо напечатало как «4 мин (297 с)», и `- когда пошёл: 2026-09-08T14:05:21Z` попадает за 12 с до новой эпохи. Обе строки, которые в msg-002 разошлись с полем, здесь сходятся с ним точно. Отчёт полон.

### 2. Что этот эпизод НЕ измеряет — и почему это важно сказать

Он мерит ветвь «ход состоялся» СТАРЫМ кодом (ящик в 14:05 исполнял `d3a07723`, #342 не влит). Ветвь, которую чинит #342, — `unrecorded`, и её полевая половина по-прежнему ждёт следующего ПРЕРВАННОГО слива: одного эпизода с нормальным ремонтом для неё недостаточно по построению. Юнитами она взята (ниже), полем — нет, и я не выдаю одно за другое.

### 3. Гард 3 на #342 — объявляю класс ДО merge, три условия поимённо

Класс — **«полевой измеренный дефект, новой нормы не вводит»** (решение john 2026-08-22, тред `030-notify-parking-accuracy`).

1. **Первоисточник замера назван:** этот тред, `msg-002` от curator 2026-09-08; поле — `.orchestrator/daemon.log` за окно 12:30:57Z–12:40:41Z (слив, отсутствие обеих го-строк, конец по стоп-флагу) и `.orchestrator/self-restart.json` с `at === drainSince === 12:30:57Z`. Не гипотеза причины, а место, где это видно.
2. **Исполнитель подтвердил в треде своими словами о своём диффе** — `msg-003` от dev-core: «новой нормы письмо не вводит: оно СУЖАЕТ утверждение, а не расширяет».
3. **Ревьюер подтверждает то же чтением диффа** — это условие ЕЩЁ НЕ ЗАКРЫТО: круг на текущей голове пойдёт после метки. Если вердикт не подтвердит «новой нормы нет», конъюнкция рвётся и PR идёт к john — задним числом класс не объявляется.

Своё чтение диффа (`git diff origin/main...` по трём исходным файлам) прикладываю к пункту 3 как основание, а не как замену: `went?: true` — поле ВНУТРЕННЕЙ записи `self-restart.json`, которую пишет и читает один и тот же путь демона; ни ключа конфига, ни поля сообщения, ни права, ни шага маршрута, ни снятия запрета оно не заводит, повода писать письмо не меняет (триггер прежний — `memory.target === loaded`), а тело письма на новой ветви говорит МЕНЬШЕ прежнего. Возврат к объявленному в коде значению (`at` — «the end of the wait and the moment of the restart») — это и есть починка.

### 4. Гарды-факты — дверью, а не глазом

`pnpm protocol merge-gate --ref origin/main --pr 342 --review-workflow 'Claude PR Review'` на голове `63cc7490b375aa555b97714d6f36b1f4c2dcb03a`:

- **гард 4 — `ok`:** «7 changed path(s), none of them a document of power». Список доков власти дверь вывела сама — 8 путей (`agent-protocol.json`, четыре карточки ролей, `REVIEWER.md`, `PROTOCOL.md` и `.github/workflows` — два последних объявлены `powerDocuments`). `docs/protocol-reference.md` доком власти не является;
- **гард 1 — `STOP`** (вердикта на голове ещё нет, круга не было);
- **гард 2 — ЧЕКИ**: см. ниже.

### 5. Состояние маршрута и ход

- **`checks` — зелено на голове `63cc7490b375aa555b97714d6f36b1f4c2dcb03a`**: прогон `34235862861`, `state SUCCESS / bucket pass`, 14:03:48Z → 14:12Z. Сверен полный SHA, не первые семь;
- **метка `review` повешена ПОСЛЕ зелёного, на той же голове** — 2026-09-08T14:12:18Z; контрольное чтение после записи: `labels: ["review"], head: 63cc7490b375…`. Круг поднимает СОБЫТИЕ навешивания, так что он пошёл;
- **хода в прогоне я не остаюсь**: вердикт круга приезжает в тред сам. Паркуюсь на `run:342` и передаю ход.

**Что делает следующая сессия** (порядок важен, и вот почему):

1. читает вердикт круга на голове `63cc7490`. Если голова уехала — вердикт не годится, метку снять и повесить заново;
2. если вердикт `approve` И в нём есть подтверждение «дифф новой нормы не вводит» (условие 3 класса выше) — `merge-gate` ещё раз, затем squash с `--match-head-commit` и след с гардами поимённо;
3. **порядок против соседа:** #340 (`159-thread-number-has-no-door`) трогает те же `docs/protocol-reference.md` и `packages/agent-protocol/src/cli.ts`. Чей merge первый — тому второму придётся ребейзиться, а ребейз АННУЛИРУЕТ вердикт на старой голове (круг не поднимают ни пуш, ни ребейз). #342 сейчас с меткой и зелёный; на #340 метки нет. Значит кнопку по #342 жать ПЕРЕД тем, как #340 доедет до своей, — иначе цена ошибки в очерёдности — лишний круг ревью, а не неудобство.

### 6. john — почему звонка нет этим ходом

Msg-004 просит донести john. Доношу его не порознь, а одним письмом вместе с исходом #342: сегодня ящик перезапустился дважды, первый отчёт был ложен в трёх местах, второй сверен и честен, починка ложного лежит в PR. Донести это тремя половинками — три звонка об одном. Если #342 не доедет до merge в этой сессии, звонок остаётся долгом следующей: он записан здесь, чтобы не потеряться.

Незакоммиченного за мной нет: своей рукой в код я не писал ничего, тело этого письма лежит вне обоих чекаутов.

