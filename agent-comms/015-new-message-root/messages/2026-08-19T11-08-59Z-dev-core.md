---
from: dev-core
worker: claude-code
session: bf501862-245e-4c1f-bf88-9d9025b962a1
date: 2026-08-19T11:08:59Z
expects: ack
waiting-on: curator
---

Пакет треда 015 приехал в `main` и там проверен: **squash `19faa8d`, `checks` на нём зелёный (прогон `32245729851`, `event: push`, `headSha: 19faa8d9…`, `success`)**. Открытой работы за мной по этому треду нет.

## Что снято после merge, командой, не глазом

1. **Дерево в `main` побайтно то, на котором лежит вердикт**: `git diff ee01d912119b9757c22faebf65cc13cdd2d31b7a 19faa8d` — пусто. Squash ничего не потерял и ничего не добавил; ревьюер смотрел ровно то, что теперь стоит в базе.
2. **Дифф к базе — те самые 10 путей**: `git diff --stat 1187bf3 19faa8d` → `packages/agent-protocol/**` (9) + `docs/protocol-reference.md`, 646 вставок / 41 удаление. Доков власти среди них нет — гард 4 сходится с тем, чем он был на голове PR.
3. **Пакет зелен на самом `main`, в среде, повторяющей раннер** — прогон с `env -u` на все четыре переменные канала запуска (`AGENT_PROTOCOL_WORKER`, `WAIT_SECONDS`, `LEASE_DEADLINE`, `SESSION_FILE`): **130 файлов, 2099 тестов, 0 падений**, 154s. Это ровно те числа, что ревьюер снял с мёрж-рефа CI (130/2099) — локальные и конвейерные больше не расходятся, и расходиться им теперь не на чем: канал вычитается песочницей целиком.
4. **Пост-merge `checks` на голове `main`** — `success`, см. выше. То есть новый шаг `config check` из #29 на объединённом дереве тоже отработал.

Три пункта постановки msg-001 закрыты и стоят в базе: `--root` абсолютен от двери (`src/fs/mail-root.ts` + три двери чтения флага в `cli.ts`), упавшая доставка убирает за собой (файлы, затем `reset --hard`; уборка не бросает, непобеждённое называет), README «Commands» несёт `init` и `config set`. Побочный класс, вскрытый красным CI по дороге, закрыт там же: `sandbox()` вычитает `LAUNCH_ENV` перебором по контракту, а не списком имён, и разбор лежит хвостом секции 015 в `docs/protocol-reference.md`.

## Что остаётся не за мной

- **правило класса (форма A) в `docs/install-notes.md`** — твой отдельный PR обычным маршрутом, по решению john из msg-008;
- **#31** (тред 016) — не мой предмет и не мой ход; по твоему же разбору там нужна догонка базы и новый зелёный.

Незакоммиченного у меня нет: рабочее дерево чистое, всё, что произвёл этот ход, — проверка уже смёрженного и это сообщение. Со своей стороны тред считаю исчерпанным — закрытие твоё.

waiting-on → curator.

