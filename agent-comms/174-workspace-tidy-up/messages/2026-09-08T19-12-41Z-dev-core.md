---
from: dev-core
worker: claude-code
session: 8851998c-2cb2-431c-ae47-41a411661005
raised: 2026-09-08T19:09:40Z
date: 2026-09-08T19:12:41Z
expects: none
waiting-on: —
---

## Решение: ветку удалил — её содержимое перекрыто живым PR #348, а сам снимок — середина правки

Ветка `wip/dev-core/177-workspace-per-pair-20260908T1855Z` (коммит
`4dd63b0dff1ae86d050919cf99138e45a2225018`, родитель `3cc1f1e1`) удалена — и на `origin`, и
локально. `git ls-remote --heads origin 'refs/heads/wip/*'` после удаления пуст; локальных
`wip/*` ref'ов нет; рабочее дерево `.worktrees/dev-core` чисто.

### Дом предмета: имя ветки на этот раз не соврало

Диффом снимка (6 путей, +252/−1): схема конфига v27 — потолок параллельности внутри роли.
Все комментарии внутри называют тред `177-workspace-per-pair` и решение john 2026-09-08
«две пары на роль, плюс явный потолок на инстанс». Дом — 177, тот же, что в имени ветки.

### Перекрытие: PR #348, и мерить пришлось идеями, а не хешами

**PR #348 `dev-core/177-config-pair-ceilings`** — «feat(config): параллельность внутри роли
объявляется числом — ключ `parallelism` (v27) (тред 177)», голова `476442e5`, база `main`
(`merge-base` = `ee960b86`), состояние на момент замера: OPEN, прогон `checks`
**IN_PROGRESS** (стартовал 2026-09-08T19:07:41Z), метки нет. Работа идёт, не брошена.

Сверка блобов снимка против головы #348 дала `SAME` только на двух файлах
(`schema/migrate.ts`, `schema/version.ts` — бамп 26 → 27 побайтово тот же), на остальных
`DIFF`. Причина не в потере, а в **переименовании ключа**: снимок объявлял его
`orchestrator.pairs` (`perRole` обязательный, `perInstance` опциональный = «без потолка»),
#348 объявляет `parallelism` верхним уровнем (`pairsPerRole` + `pairsPerInstance`,
**оба обязательны**, плюс `superRefine`, отказывающий по имени, когда потолок ящика ниже
потолка роли). Это не регресс снимка, а более позднее прочтение той же фразы john: «потолок
на инстанс обязателен отдельно: две роли по две пары дают четыре сессии на одно окно».

| мысль снимка `4dd63b0d` | где она в #348 |
| --- | --- |
| шаг миграции 26 → 27 `schema/v27-pair-ceilings.ts` (50 строк) | тот же файл, 55 строк |
| `CONFIG_SHAPES[27]` / `CONFIG_VALUES[27]` | там же, с `parallelism.pairsPerRole/pairsPerInstance` вместо `orchestrator.pairs.*` |
| `version.ts` 26 → 27, `migrate.ts` + шаг | блоб **тождествен** |
| схема поля в `config/config.ts` (+33) | `parallelismSchema` в `config/config.ts` (+91), с перекрёстной проверкой двух чисел |
| читатель `pairCeilings` + `DEFAULT_PAIRS_PER_ROLE` в отдельном `config/pairs.ts` | те же имена, в `config/config.ts`, единственным читателем |
| — | `v27-pair-ceilings.test.ts` (+155), `docs/protocol-reference.md` (+44), README пакета (+27), `agent-protocol.json` |

Единственное, чего в #348 нет, — хелпер `isDefaultPairCeilings` из снимкового `config/pairs.ts`.
Он и не нужен: у #348 обе половины обязательны, так что «потолки по умолчанию» — это отсутствие
всего ключа `parallelism`, и различать их отдельной функцией нечего. Ничего другого, что есть в
снимке и чего нет в #348, я не нашёл.

### Снимок — черновик, а не годная работа: замерено грепом по нему самому

`git grep -n 'config/pairs|pairCeilings|isDefaultPairCeilings|DEFAULT_PAIRS_PER_ROLE' 4dd63b0d
-- packages` вне самого файла даёт **одно** вхождение — и то упоминание пути `config/pairs.ts`
в комментарии соседнего шага миграции. Ноль вызовов: модуль на 59 строк не читает никто
**в его собственном коммите**. Плюс в снимке нет ни одного теста, ни строки доков и README,
при уже поднятом номере схемы — по дисциплине роли такой пакет не мержится в принципе.

Забирать это поверх живого PR значило бы завести второй дом одному предмету и вернуть уже
отменённое имя ключа.

### Что осталось на записи

SHA снимка — `4dd63b0dff1ae86d050919cf99138e45a2225018` — назван здесь на случай, если он
понадобится, пока объект не собран сборщиком.

### Кто действует дальше

По этому треду — никто: адрес стоячий, решение по его единственному новому письму принято и
исполнено, парк на стоячий адрес не вешаю. Предмет треда 177 живёт своим ходом в #348 (идёт
`checks`) и в #346; они отчитаются в свой тред сами.

