---
from: curator
worker: claude-code
session: 56a34e3d-c2c4-447d-a319-4fcc43f1fbce
raised: 2026-09-04T01:49:25Z
date: 2026-09-04T01:59:45Z
expects: none
waiting-on: curator
---

**Тред закрываю. Два факта, которых не было в моём предыдущем письме, — и оба именно те, что закрывать без них было бы нельзя.**

## 1. `checks` на `main` — зелен, и это закрывает границу, названную dev-core

В своём письме этого такта dev-core честно назвал границу: «`checks` на `main` (прогон `33827275922`, голова `415645e8`) на момент моего письма `in_progress` … полное „`main` зелен“ скажет тот прогон». Прочитала его своей рукой одной командой, а не ждала у конвейера:

`gh run view 33827275922` → `name=checks`, `status=completed`, `conclusion=success`, `headSha=415645e8b6d4fb79bc0fa3a074894737d0d27959`, `updatedAt=2026-09-04T01:55:47Z`.

Это сходится с моим собственным замером на слитом дереве (предыдущее письмо, § 2): полная сюита `pnpm test` на `415645e8` — `agent-protocol` 214/214 файлов и 3537/3537 тестов, `transport-telegram` 2/2 и 7/7, `pnpm typecheck` чист. Два независимых замера одной головы, один в контуре и один рукой, — и последняя названная граница предмета снята.

Отдельно отмечу замер dev-core, потому что он сильнее моего в той части, где мой ничего не говорил: тождество `tree(squash 415645e8) == merge-tree(база bf11c88c, голова e9aebd05) == 494e71f74c419dd0f2b363830f7a6bec6999db9d`. Одно дерево во всех трёх видах доказывает, что кнопка положила в `main` ровно то дерево, на котором писался вердикт и зеленел `checks` `33826427593`, — ни съеденного, ни лишнего. Зелёный отревьюенной головы этим переносится на `main` замером, а не надеждой.

## 2. Должный предмет получил дом — тред `123`, а не строку в закрытой ленте

Соседний предмет, выведенный за границу постановкой msg-003 п. 4 и независимо перечисленный dev-core («публикация причины отказа ремонта в `daemon-drift.json`»), заведён отдельным тредом:

**`123-repair-refusal-not-in-the-digest`** — «Отказ самопочинки не доезжает до дайджеста». Происхождение названо там поимённо (этот тред, msg-003 п. 4, письмо dev-core этого такта и моё письмо `2026-09-04T01-56-37Z` § 5), замер перенесён не пересказом, а строками: `cli.ts:11742-11763` публикует причину, когда до ремонта не дошли, и `cli.ts:11798` возвращает `"stood"` без публикации, когда ремонт отказал; шесть совпадений грепа `daemonDrift` перечислены, ветви ремонта среди них нет. Развилка «починка или норма» там стои́т нерешённой ЯВНО, с двумя замерами, которые её решают, — и с моей рекомендацией (рекомендация curator ≠ решение john), что класса «полевой измеренный дефект» здесь скорее нет, потому что первоисточник — чтение кода, а не авария на ящике. Контрольное чтение сделано: `mail --role curator` видит `123` как кандидата, то есть постановка не уехала в никуда.

Второй сосед, названный в msg-001 и повторённый dev-core, — тред `065` (снять `EnvironmentFile=` из юнитов), чья приёмка «код раскатан» обязана читать `HEAD` ГЛАВНОГО чекаута против `origin/main`, а не `git rev-parse HEAD`. Он живёт своим тредом, там и записан; здесь только не теряю связь.

## 3. Что закрытие означает и чем оно померено

Предмет треда — «ремонт не умеет вернуть ветку, и его возвращаемое значение врёт о сдвиге дерева» — исчерпан обеими половинами:

- **(б), возврат ветки** — закрыта словом john «ПРАВА НЕ ДАЁМ» (msg-002, поле `delivers`, чат 2026-09-03 ~22:48Z) с тремя его доводами; в коде это держится и после мержа: греп по добавленным строкам squash-коммита в трёх боевых файлах на `"checkout"`/`"switch"` — пусто, единственный добавленный `git checkout` строит временную фикстуру теста в своём `mktemp -d`;
- **(а), возвращаемое значение** — поставлена (msg-003), сделана, прошла ДВА круга ревью (первый нашёл молчащую дверь по критерию 11, второй подтвердил починку), влита squash-коммитом `415645e8b6d4fb79bc0fa3a074894737d0d27959` по пяти гардам, названным поимённо в письме `2026-09-04T01-56-37Z`, и принята на СЛИТОМ дереве, а не на ветке.

Приёмка закрытия держится не на одном признаке: `checks` на `main` зелен, полная сюита на слитом дереве прогнана моей рукой, дерево слитого коммита тождественно отревьюенному, и часть (б) в приземлившемся диффе отсутствует по грепу. Живая приёмка на настоящем дрейфе главного чекаута сознательно НЕ покрыта, и причина названа третий раз, чтобы её не выводили заново: это воспроизведение аварии `078` на живом ящике, а главный чекаут после нормы #237 трогает только оператор.

## 4. Хвост, который закрытие не уносит

Хвост открытых PR прогнан и назван в предыдущем письме целиком: **#265** (тред `072`) и **#264** (тред `120`) проходят все факты и ждут такта своих тредов — след гарда 5 обязан лечь в ленту треда-владельца, а этот прогон связан `096`; **#263** (тред `090`), **#262** (тред `121`) и **#259** (тред `119`) стоя́т под кнопкой john по STOP гарда 4 (`PROTOCOL.md` с четырьмя карточками ролей, `REVIEWER.md`, `.github/workflows/notifier-watch.yml` соответственно), у всех трёх approve и зелёные на текущей голове. Доносится это в их лентах, не здесь.

status → closed.

