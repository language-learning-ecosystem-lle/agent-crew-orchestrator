---
from: curator
worker: claude-code
session: 56a34e3d-c2c4-447d-a319-4fcc43f1fbce
raised: 2026-09-04T01:49:25Z
date: 2026-09-04T01:56:37Z
expects: none
waiting-on: curator
merged-pr: 266
---

**Кнопка нажата: #266 в `main`, squash `415645e8b6d4fb79bc0fa3a074894737d0d27959`. Гарды — поимённо ниже; приёмка снята НЕ на ветке, а на слитом дереве.** Постановка msg-003 выполнена целиком, часть (б) в дифф не попала ни одной строкой — померено мной, а не принято по докладу.

## 1. След гарда 5 — пять гардов поимённо

Дверь: `pnpm protocol merge-gate --ref origin/main --pr 266 --review-workflow 'Claude PR Review'`, код выхода `0`, «nothing in the facts forbids this merge — guards 3 and 5 are yours to answer».

- **гард 1 — вердикт на ТЕКУЩЕЙ голове.** `ok`: «approved on e9aebd0 by github-actions — inside the round `33826895662` of 'Claude PR Review' on this head (2026-09-04T01:44:24Z…2026-09-04T01:49:03Z)». Якорь — прогон, а не review-объект: вердикт лежит В ОКНЕ закрытого успешного круга с событием `pull_request` на этой голове. Сам вердикт — сообщение `reviewer-pr` в этой ленте, `verdict: approve`, `pr: 266`, доставлено шагами прогона `33826895662`;
- **гард 2 — зелёные чеки на ТОЙ ЖЕ голове.** `ok`: «2 check(s) green: review=SUCCESS, checks=SUCCESS». Прогон `checks` — [`33826427593`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/33826427593) на голове `e9aebd057df859cc989b2b2747e21a915ddb1579`. Прошлые зелёные (`33824607835` на `95020783`, `33826113588` на `3fa0493d`) относятся к головам, которых больше нет, и в зачёт не шли;
- **голова — полным SHA:** `e9aebd057df859cc989b2b2747e21a915ddb1579`. Сорок символов глазом не сверялись: этот же SHA передан кнопке флагом `--match-head-commit`, так что гарды 1–2 держатся командой, а не чтением;
- **гард 3 — восхождение.** Закрыт ДВАЖДЫ, и это не избыточность, а две разные двери:
  1. **слово john в ленте.** msg-002, поле `delivers`, чат 2026-09-03 ~22:48Z: часть (а) названа им главной половиной предмета («функция врёт о результате»), дословно «Ставь `dev-core` своим ходом». Источник помечен. То есть решение john в ленте есть, и восходить есть к чему без всякого исключения;
  2. **класс «полевой измеренный дефект, новой нормы не вводит»**, объявленный в треде ДО merge (msg-003, п. 1) — три условия поимённо:
     - **условие 1, первоисточник замера:** лента треда `078-main-checkout-branch`, сообщения `curator` от 2026-09-03 — главный чекаут `orchestrator.workdir` простоял 5 ч 54 мин на чужой ветке, одиннадцать коммитов `origin/main` в него не приехали, оба контура упали; само расхождение кода с его док-блоком померено чтением кода в msg-001 этого треда;
     - **условие 2, исполнитель своими словами о своём диффе:** msg-004 п. 5 («дифф приводит функцию к контракту, объявленному в её собственном док-блоке, и ничего не разрешает; прав у демона не прибавилось»), повторено для правок второго круга в msg-009 п. 4;
     - **условие 3, ревьюер чтением диффа:** оба круга. msg-007, критерии 5–6: «ослабления гардов, расширения прав инструментов, необратимых операций в диффе нет», «новых полей конфига или схемы почты дифф не добавляет». msg-013 подтверждает то же самое повторным проходом уже на голове `e9aebd05`;
- **гард 4 — доки власти.** `ok`: «7 changed path(s), none of them a document of power». Список, которым дверь судила, она напечатала сама — восемь путей: `agent-protocol.json`, четыре карточки ролей (`curator`, `dev-core`, `pilot-codex`, `devops`), `REVIEWER.md` (все шесть — ВЫВЕДЕНЫ из `instructions` конфига), плюс `PROTOCOL.md` и `.github/workflows` (объявлены `powerDocuments`). Полнота гарда не равна моей памяти;
- **гард 5 — вот он.**

Кнопка: `gh pr merge 266 --squash --match-head-commit e9aebd057df859cc989b2b2747e21a915ddb1579`, заголовок в Conventional Commits. **Контрольное чтение цели после merge** (ответ инструмента ≠ факт): `state=MERGED`, `base=main`, `mergedAt=2026-09-04T01:50:27Z`, squash-коммит `415645e8b6d4fb79bc0fa3a074894737d0d27959` — и он же вершина `origin/main` после `git fetch`.

## 2. Приёмка — на СЛИТОМ дереве, а не на ветке

Ветку мерили CI и ревьюер дважды. Слитое дерево до этого момента не мерил никто, а мержится именно оно. Отдельный `worktree` на `415645e8`, `pnpm install` из стора (0,6 с):

- **четыре файла постановки:** `pnpm --filter agent-protocol exec vitest run src/orchestrator/self-restart.test.ts src/orchestrator/self-restart.process.test.ts src/orchestrator/workspace.test.ts src/orchestrator/preflight.test.ts` → **`Test Files 4 passed (4)`, `Tests 167 passed (167)`**. Ровно то число, которое ревьюер сверял на ветке, — на слитом дереве оно то же;
- **`pnpm typecheck`** на слитом дереве — `agent-protocol: Done`, `transport-telegram: Done`;
- **полная сюита** на слитом дереве (`pnpm test`, код выхода `0`) — `agent-protocol`: **`Test Files 214 passed (214)`, `Tests 3537 passed (3537)`**; `transport-telegram`: `2 passed (2)`, `7 passed (7)`. Ни одного упавшего файла, то есть правка не задела соседей за пределами четырёх названных.

## 3. Исход назван файлами и строками — на `main`, не по докладу

- `packages/agent-protocol/src/orchestrator/self-restart.ts:442-443` — поля `installFrom` / `installFromMeans` у ветви `moved`; `:487-488` — правило одной строкой: `installFrom: input.loaded ?? input.before`;
- `packages/agent-protocol/src/cli.ts:987` — `["-C", checkout, "diff", "--name-only", move.installFrom, move.to]`: дифф берётся по промежутку от загруженного кода, а не `before..HEAD`; `:999` — `describeInstallSkipped({from, fromMeans, to})` печатает промежуток, который померил;
- **часть (б) отсутствует — померено, а не принято по докладу.** Греп по добавленным строкам squash-коммита в трёх боевых файлах (`cli.ts`, `self-restart.ts`, `workspace.ts`) на `"checkout"`/`"switch"` — **пусто**. Единственный добавленный `git checkout` во всём диффе — `self-restart.process.test.ts`, где фикстура заводит ветку в СВОЁМ временном репозитории. Демон по-прежнему не двигает `HEAD` обслуживаемого дерева; слово john «ПРАВА НЕ ДАЁМ» (msg-002) в коде держится;
- объём squash-коммита: 7 файлов, +726 / −23.

Отдельно отмечаю ход исполнителя, потому что он редкий: находку ревьюера (критерий 11, msg-007) `dev-core` не закрыл прозой — воспроизвёл сценарий отказа по коду построчно, починил, **проверил оба теста на старом якоре** (вернул `installFrom: input.before` — краснеют оба, восстановил), и в msg-009 сам поправил СВОЙ же замер из предыдущего письма (ошибка чтения часов: локальное время принято за UTC), вместо того чтобы оставить удобное «времени не было». Процессный тест на второй сценарий после этой поправки написан и зелёный.

## 4. Хвост открытых PR — прогнан целиком, молчания не оставляю

`gh pr list --state open` + `merge-gate` по каждому, в этот ход:

- **#265** (тред `072-ci-outcome-not-delivered`, голова `957a052`) — гарды 1, 2, 4 `ok`, `mergeable=MERGEABLE/CLEAN`, «nothing in the facts forbids»;
- **#264** (тред `120-enclosing-repo-probe-silent`, голова `d4e313a`) — то же, всё проходит;
- **#263** (тред `090-what-kills-a-note`, голова `8acc534`) — **STOP гарда 4**: трогает `PROTOCOL.md` и четыре карточки ролей → кнопка john;
- **#262** (тред `121-reviewer-md-lacks-the-class`, голова `b53b11e`) — **STOP гарда 4**: `REVIEWER.md` → кнопка john;
- **#259** (тред `119-mute-count-blames-github`, голова `b18debf`) — **STOP гарда 4**: `.github/workflows/notifier-watch.yml` → кнопка john.

**#265 и #264 я НЕ домержила, и это не забывчивость.** След гарда 5 обязан лечь в ленту треда-ВЛАДЕЛЬЦА PR (`072` и `120` соответственно), а этот прогон связан одним тредом `096` — писать туда мне сейчас нечем, и merge без названных гардов есть нарушение нормы, даже когда merge правильный. Оба ждут такта своего треда, у обоих approve и зелёные на текущей голове.

**Три PR (#263, #262, #259) стоят под кнопкой john** — у всех вердикт `approve` и зелёные чеки на текущей голове, дальше их не двигает никто, кроме него. Донести это владельцу — не отсюда: доносится оно в лентах `090`, `121` и `119`, и там же стои́т парковка. Здесь названо, чтобы следующий такт по каждому не начинал с нуля.

## 5. Что остаётся должным — не теряется вместе с этим тредом

Постановка msg-003 п. 4 назвала соседний предмет и вывела его за границу: **отказ ремонта виден только в логе — `daemon-drift.json` о нём молчит**. Это не входило в #266 ни строкой, и правкой #266 не закрылось: теперь ремонт отказывает ЧЕСТНО и с названной причиной, но причина по-прежнему живёт только в потоке демона.

Асимметрию померила своей рукой на слитом дереве, чтобы предмет не пришлось выводить заново — она в одной функции и в пятидесяти строках друг от друга:

- **ветвь «не дошли до ремонта»** — `cli.ts:11742-11763`: `verdict.kind === "stand"` печатает причину в лог И публикует её `renderDriftStandoff` в `paths.daemonDrift`, причём комментарий рядом (`:11745-11748`) называет мотив дословно — «AND IT IS PUBLISHED WHERE THE COURIER CAN READ IT (thread 044). The log is read by whoever is already looking at the box, and the whole class this repair comes from is the drift NOBODY was looking at»;
- **ветвь «ремонт отказал»** — `cli.ts:11798`, одна строка: `if (form === "supervised") return repairInPlace(drift) ? "handback" : "stood";`. `"stood"` уходит наверх БЕЗ публикации: `daemonDrift` в этой ветви не упоминается вовсе (греп `daemonDrift` по `packages/agent-protocol/src` вне тестов — шесть совпадений: `:5309-5310` читатель дайджеста, `:11749` и `:11762` публикация и её отказ, `:12493` и `:12496` снятие файла, когда код догнал ref. Ветви ремонта среди них нет);
- то есть довод комментария на `:11745` относится к отказу ремонта ровно в той же мере, а дайджест о нём молчит. Курьер несёт причину, по которой демон НЕ ПОШЁЛ чиниться, и не несёт причину, по которой починка НЕ ВЫШЛА — а после #266 вторая наконец существует как честный факт, и потому именно теперь стои́т того, чтобы её было видно.

Довод и рекомендацию (это рекомендация curator, не решение) держу до постановки: класс похож на «полевой измеренный дефект», но первоисточник замера тут — чтение кода, а не авария на ящике, и это надо померить прежде, чем объявлять класс.

Обещание из постановки было «меряю его отдельно», и я его держу: предмет уезжает своим тредом, происхождение — эта лента. Здесь он назван, чтобы закрытие `096` его не похоронило.

## 6. Чей ход

Ничей: предмет `096` кончился. Развилка (б) закрыта словом john, часть (а) поставлена, сделана, отревьюена двумя кругами, влита и принята на слитом дереве. **Тред закрываю этим же ходом** — исход назван выше файлами, строками и числами именно потому, что лентой закрытого треда его потом не доложить.

waiting-on → curator (никто больше по этому предмету не ходит).

