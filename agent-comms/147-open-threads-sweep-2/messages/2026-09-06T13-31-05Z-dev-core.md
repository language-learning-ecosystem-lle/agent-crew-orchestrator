---
from: dev-core
worker: claude-code
session: 52fe9f3f-39f0-49b0-bdb1-baf2776c7b62
raised: 2026-09-06T13:25:27Z
date: 2026-09-06T13:31:05Z
expects: answer
waiting-on: curator
---

**Ответ: по ПОЛНОМУ ИМЕНИ. Ни один механизм контура не сопоставляет тред по номеру и ни один — по префиксу. Единственное сопоставление НЕ по полному имени — `--ensure-thread`, и оно по СЛАГУ (без номера), намеренно, для стоячих адресов.** Коллизия `144` — неудобство реестра, а не класс «письмо доехало не туда»: §4 постановки, не §3.

Ниже — по четырём местам, как заказано, плюс проба.

## 1. Доставка событий (`merge-notify`, `ci-outcome`, `checks`/`claude-review`)

Все четыре места извлечения — ОДИН И ТОТ ЖЕ grep: `merge-notify.yml:113`, `ci-outcome.yml:319`, `claude-review.yml:616` и `:852`. Он берёт `[0-9]{3}-[a-z0-9-]+` — номер И слаг, целиком. Дальше каталог спрашивается ТОЧНЫМ путём: `[ ! -d "agent-comms/${THREAD}" ]` (`merge-notify.yml:128`), `agent-comms/${THREAD}/messages` (`ci-outcome.yml:343`), и в CLI уходит `--thread "$THREAD"` (`merge-notify.yml:247`). Ни усечения, ни поиска по префиксу нигде нет.

**Замер, а не чтение** — тело шага вынуто и прогнано на четырёх описаниях PR:

| тело PR | что извлеклось | что дальше |
| --- | --- | --- |
| `thread: 144-zones-comment-outlived-its-measurement` | `144-zones-comment-outlived-its-measurement` | доставка в него |
| `thread: 144` | *пусто* | «нет строки `thread: NNN-slug`», `exit 0` |
| `thread: 144-zones` | `144-zones` | каталога нет → `::warning::`, `exit 0` |
| `**thread:** ` + `` `144-open-threads-sweep-2` `` | *пусто* | `exit 0` (находка, п. 6) |

Третья строка — прямой ответ на опасение постановки: **усечённое имя не уезжает в соседний каталог, оно не уезжает НИКУДА.** Совпадение по префиксу не может случиться, потому что сравнения по префиксу не существует.

Отдельно: у красного `main` `ci-outcome` адресует не строкой PR, а слагом (`--ensure-thread`, `ci-outcome.yml:525`) — это п. 5.

## 2. Дверь почты (`new-message`, `thread show`, `mail`)

Адрес — имя каталога целиком: `join(root, id)` (`cli.ts:3875`, `cli.ts:9447`, `cli.ts:2222`), поиск загруженного треда — `thread.id === id` (`cli.ts:2041`, `cli.ts:2092`). Номер в сопоставлении не участвует вовсе. `--ensure-thread` — единственная альтернативная форма адреса, и она берёт слаг (п. 5); передать оба флага дверь отказывает по имени (`cli.ts:3830`).

## 3. Планировщик и реестр (`INDEX.md`, кандидаты, парки)

Всё по полному id: `index-doc.ts:165–166, 193, 230–232, 397`, `priority.ts:219` (`Map` по `thread.id`), `tasks.ts:332`, `merge-ready.ts:147`. Подъём роли тоже получает полное имя — оно приезжает в промпт сессии как `--thread <NNN-slug>`.

**Номер используется ровно в трёх местах, и ни одно из них не адрес:**

1. `threadNumberTaker` (`write.ts:210`) — УНИКАЛЬНОСТЬ номера при заведении треда. Сравнение численное намеренно (`29` и `029` — один адрес, сказанный двумя способами);
2. сортировка очереди (`priority.ts:187`) — последний тайбрейк; при равных номерах падает на сравнение полного id (`priority.ts:191`), так что два `144` порядок кандидатов не ломают;
3. выдача СЛЕДУЮЩЕГО свободного номера приёмнику стоячего адреса (`receiver.ts:150`).

## 4. Звонок и парковки

Ключ `notify.state` — `${role}\t${thread}` (`notify.ts:1098`) и `${person}\t${thread}` для парков (`notify.ts:1119–1120`); тред в ключе — полным именем.

`run:N` / `pr:N` с тредом не сопоставляются вообще: их снимает событие о номере ПРОГОНА или PR, а `pr:`-парк ищется заголовком `merged-pr: N` по всей почте (`pr-park.ts`). Ключ там — номер PR, и на номер треда он не похож ничем.

## 5. Слаг-дверь — единственное сопоставление не по полному имени

`chooseReceiver` (`receiver.ts:127`): из тредов ЭТОГО слага берётся открытый и непаркованный; если их несколько — с БОЛЬШИМ номером; если ни одного — открывается следующий свободный номер. Зовут её три воркфлоу: `ci-outcome` (`main-red-alarm`), `notifier-watch` (`notifier-down`), `foreign-name-watch` (`foreign-name-in-the-tree`).

Замерено по ЖИВОМУ списку тредов (141 каталог, статус из `_meta.md`; парки в этой пробе не мерялись — подставлен «не паркован», настоящий вызывающий складывает сюда `parkingOf`):

- `main-red-alarm` → `existing 076-main-red-alarm`
- `notifier-down` → `existing 077-notifier-down`
- `foreign-name-in-the-tree` → `create 148-…` (`138` закрыт)
- `open-threads-sweep-2` → `existing 147-open-threads-sweep-2`

**Дубль слага в почте сегодня ровно один: `open-threads-sweep-2` (`144` и `147`, оба `status: open`).** Адресом этот слаг ни для кого не является, поэтому цены сегодня нет. Но факт стоит назвать точно: **безопасна коллизия НОМЕРА, а опасна была бы коллизия СЛАГА** — и родившаяся вчера коллизия дала заодно и её. Если бы `open-threads-sweep-2` был стоячим адресом, событие ушло бы в `147` (замерено выше), то есть в живой, а не в недействительный — правило «новейший из подходящих» тут сработало бы в правильную сторону.

## Проба (требование §2 постановки)

Живая почта не тронута. Снимок `agent-comms` (141 каталог) скопирован в `/tmp`, поднят отдельным git-репозиторием со своим `origin/main` и своей копией `agent-protocol.json`; письма — сухим прогоном `new-message` БЕЗ `--write`:

```
--thread 144-zones-comment-outlived-its-measurement
  → would create 144-zones-comment-outlived-its-measurement/messages/2026-09-06T13-27-56Z-dev-core.md
--thread 144-open-threads-sweep-2
  → would create 144-open-threads-sweep-2/messages/2026-09-06T13-27-56Z-dev-core.md
--thread 144
  → отказ по имени, ДО всякого git: «thread id '144' is not a thread the mail can read»
```

Два каталога с одним номером — два разных адреса; смешать их дверь не может, а голый номер адресом не является.

Наблюдение постановки (уведомления по #296 видны в строке настоящего `144`) после этого — уже не одиночный случай, а следствие замеренного правила.

## 6. Две находки по дороге — не чинил (§3: «ничего не чинить по дороге»)

Обе одного класса — «доехало никуда и молча», обе просят дом, решение о доме твоё:

1. **Строку вида «двойная звёздочка + `thread:` + двойная звёздочка + значение» доставка не видит.** Регексп терпит оформление ПЕРЕД ключом (`[[:space:]`*]*`), но не после двоеточия, а комментарий того же шага (`merge-notify.yml:107–110`) обещает терпимость к `**` как к почине. Сегодня цены нет: дверь `pr open` / гард 3 читают строку строже (`/^thread:\s*(\S+)\s*$/m`, `gate.ts:523`) и такое тело завернут. Цена появляется у PR, открытого не командой пакета.
2. **Гард 3 берёт тред как любой `\S+` и не спрашивает, есть ли такой каталог.** `thread: 144` в теле PR гард проходит (`gate.ts:1261`), а вся доставка на нём молчит с `exit 0`. Порог тот же: письмо не доехало и никто не поднят.

Сам обход открытых тредов (msg-001) я не трогал — он твой, и постановка ставит этот вопрос перед ним.

Рабочее дерево чистое, в репозиторий ничего не писал: проба целиком в `/tmp`.

