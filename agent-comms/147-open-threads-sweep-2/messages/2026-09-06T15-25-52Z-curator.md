---
from: curator
worker: claude-code
session: 30cbc6b6-fc2e-4669-b4fc-e6b24ccbffa7
raised: 2026-09-06T15:20:32Z
date: 2026-09-06T15:25:52Z
expects: ack
waiting-on: curator
parked-on: john
---

**Требуется решение john (одна строка): завтра утром, тем же заходом, что разморозка шести пар, — ПОДНЯТЬ curator рукой на четырёх тредах без хода (`043`, `044`, `073`, `080`). Другой двери у них нет: поле `waiting-on` пишут только `new-message` и `new-thread`, то есть письмо в СВОЮ ленту, а связанный прогон пишет ровно в один тред — значит ни один такт никогда не окажется тем, в котором это законно.**

Обход из `msg-004` закрыт и устарел за 80 минут: картина там снята `14:00Z`, эта — `15:2xZ`. Ниже сперва что изменилось, потом что осталось.

## 0. Чем мерено

- снимок почты `4ad6fa4dad8dfbafa6a21009695c17060b02e27b` (ветка `comms`, `15:22:16Z`), развёрнут в `/tmp` через `git archive`; живая почта чтением не тронута; **145 каталогов**;
- статус и ход — `status:` из `_meta.md` и фронтматтер ПОСЛЕДНЕГО письма каждого открытого треда;
- парк и заморозка — `.orchestrator/notify.state` (снят `15:23:29Z`);
- PR — `gh pr list` (`15:2xZ`), гарды по #301 — `merge-gate` живьём;
- тред `150` прочитан целиком (`thread show`), а не пересказан.

## 1. Что изменилось с `14:00Z` — предметов ушло больше, чем осталось

**Закрылись сами, работой своих тредов (4):** `140-silent-exit-exhausts-the-role`, `144-zones-comment-outlived-its-measurement`, `145-base-cost-…`, `146-…-credential-diagnosis`. Открытых тредов теперь **27** (было 28 после моего хода).

**Уехали в `main` шесть PR** — `#296`, `#298`, `#300`, `#302`, `#303` (`14:11`–`14:50Z`), `#305` (`15:13:20Z`). **Кандидат обхода не потерялся:** #296 (тред `144`, тот самый из `msg-004` §6) влит `14:11:17Z`, коммит слияния `3aa5ea62`, база `main` — проверено `gh pr view`; тред `144` после этого закрыт. Гарды по нему мерил такт его собственного треда, как и было сказано.

**Родились три треда (149, 150, 151)** — и два из них закрывают хвост ЭТОГО обхода (§2).

**Парков на john осталось три** (`029`, `062` `15:10:52Z`, `141` `14:09:28Z`) вместо пяти: `134` и `140` сняты. Заморозки — **все шесть стоят как стояли** (`devops` × `047`, `056`, `057`, `070`, `079`; `dev-core` × `063`), и это ожидаемо: разморозка объявлена на утро 07.09.

## 2. Хвост `msg-005` §2 ЗАКРЫТ — дом у предмета есть, и он не мой

Предмет «исчерпанную пару нечем разморозить штатно» john оставил на суждение curator (свой тред либо продолжение `140`). **Пока этот прогон стоял в очереди, дом ему завёл чатный curator: тред `150-no-way-to-thaw-an-exhausted-pair`** — постановка `msg-001`, слово john «заводи» (~15:00Z), происхождение названо первой строкой и ссылается ровно сюда: «обход открытых тредов (`147`, §2)».

**И он уже исполнен:** `dev-core` ответил командой `orchestrator thaw` — **PR #306**, голова `6a9598a2`, ждёт `checks`. Своего треда я не завожу: предмет один, дом один. Проверено чтением ленты `150`, а не ссылкой на неё.

Соседний `149-frozen-pair-writes-to-the-feed` (PR #304) — вторая треть того же цикла; оба теперь ведутся своими тредами и в хвосте обхода не числятся.

## 3. Четыре треда без хода — почему это НЕ лень такта, а отсутствие двери

`043-stale-park-reminders`, `044-selfheal-blind-spots`, `073-notifiers-frozen-in-own-contour`, `080-standing-address-catalog` — у всех четырёх в последнем письме `waiting-on: —`, и это единственные такие среди 27 открытых.

**Замер двери, а не догадка:**

- поле `waiting-on` пишут ровно две команды — `new-message` и `new-thread` (обе — письмо в ленту треда, флаг `--waiting-on`);
- **`thread status --turn` ходом НЕ распоряжается**: его два значения — `explicit` и `—`, и они объявляют ФОРМУ ответов треда («каждое письмо обязано нести `--waiting-on`»), а не того, кто ходит. Прочитано в usage самой команды, режим (c);
- читателя хода зовут `waitingOnOf` (`packages/agent-protocol/src/index.ts:360`), и он смотрит последнее письмо. Нет держателя — нет кандидата (замерено мной ранее, тред очереди).

Отсюда замок: **назначить ход этим четырём может только письмо в их ленту, а письмо может только прогон, связанный ИМЕННО ИМИ.** Связанным их прогон не станет, потому что кандидатом делает ход. Из тредов с ходом — включая этот — сдвинуть их нечем.

**Что в них лежит** (по одному факту, без рекомендаций):

- `043` — 15 писем, последнее `30.08 16:11:20Z`, промежуточный замер («дубля нет, 858 тактов курьера»), вывода нет;
- `044` — 10 писем, пакет влит `29.08` (`c33d7775`), приёмка не снята;
- `073` — 24 письма, ход снят НАМЕРЕННО (`02.09 22:22:22Z`): приёмка ждала поля, а поле не пришло (71 прогон, все `skipped`);
- `080` — 24 письма, последнее (`03.09 22:50:06Z`) само отказывается закрываться до полевой приёмки PR-2 и называет условие возврата хода в своём §5.

У последних двух к тому же полевой критерий приёмки, а он слабее перечисления фактов — но это уже работа ВНУТРИ них, и она ждёт того же подъёма.

## 4. Открытые PR — три, ни один не мой кандидат

| PR | тред | состояние на `15:2xZ` |
| --- | --- | --- |
| #301 | `141-daemon-drift-…` | **`merge-gate` REFUSED**: гард 1 STOP — на голове `8f9a9b8` вердикт `changes were requested` (`github-actions`, `14:09:28Z`), нужен новый круг. Гард 2 зелён, но с нотой: база уехала (`3a19243` в `15:13:20Z`) после старта зачтённых чеков (`13:53:05Z`). Ход у `dev-core` |
| #304 | `149-frozen-pair-…` | метки нет, ход у `dev-core` |
| #306 | `150-no-way-to-thaw-…` | голова `6a9598a2`, ждёт `checks`, ход у `dev-core` |

Ни одного PR, готового к кнопке curator, сейчас нет: два молодых и чужого хода, третий отказан гардом 1. `merge-gate` по #304/#306 не гоняла — у них нет ни метки, ни вердикта, мерить нечего.

## 5. Что ещё стоит на твоей руке и не сдвинулось

- **`047.1-devops-enablement-acceptance`** — удалить рукой в ветке `comms` (решение `msg-005` §3). Каталог на месте, `open`, дверь по-прежнему его не видит;
- **шесть заморозок** — утро 07.09, порядок из `msg-005` §1 (сперва средства доставки, потом подъём);
- **три парка** — `029` (твой go с 22.08), `062` (окно 07.09), `141` (сейчас там летит новый круг ревью).

## 6. Что этот ход НЕ сделал

- **не назначил ход четырём** — причина в §3, и она про дверь, а не про решение;
- **не мержил ничего** — кандидатов нет (§4);
- **не заводил треда предмету разморозки** — он уже заведён (§2), второй дом был бы дублем;
- **не чинил по дороге** (§3 постановки) — ни `047.1`, ни заморозок, ни замка §3.

## Ход

`waiting-on → curator`, парк на john: вопрос первой строкой, ответа хватит одной. **После него `147` закрывается** — хвоста, кроме этих четырёх, у обхода не осталось: всё остальное разошлось по своим тредам и названо здесь поимённо.

