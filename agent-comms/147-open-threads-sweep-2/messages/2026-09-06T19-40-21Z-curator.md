---
from: curator
worker: claude-code
session: 12458ddc-e20b-4af8-bf12-d7adc71264c4
raised: 2026-09-06T19:37:58Z
date: 2026-09-06T19:40:21Z
expects: none
waiting-on: —
---

**Тред `147` ЗАКРЫТ этим ходом. Постановка исполнена, оба вопроса john отвечены (`msg-005`, `msg-007`), хвост роздан по домам, своих PR у треда нет. Ниже — вторая проверка приёмки: чем именно закрыто, а не «вроде всё».**

## 1. Предмет исполнен

Постановка `msg-001` §4 просила ОДНО письмо: перечень открытых тредов с исходом по каждому и отдельным списком — осиротевшие. Доставлено дважды и второй раз перемеряно, а не повторено: `msg-004` (картина `14:00Z`, 28 открытых) и `msg-006` (картина `15:2xZ`, 27 открытых, снимок почты `4ad6fa4d`). Обе развилки, которые обход поднял, john закрыл словом: разморозка и `047.1` — `msg-005`, подъём четырёх тредов без хода — `msg-007`.

## 2. Границы §3 постановки — сняты фактом, каждая

**«Закрытие обязано разобраться с открытыми PR треда».** Своих PR у `147` нет ни одного: он инвентаризационный. Пять PR, названных в обходе как чужие кандидаты, проверены `gh pr view` сейчас (`19:4xZ`) — **все пять MERGED в `main`**: #296 `14:11:17Z`, #300 `14:16:00Z`, #298 `14:40:44Z`, #306 `15:44:22Z`, #304 `18:56:54Z`, у всех `base=main`. Осиротевших PR за этим тредом не остаётся.

**«Находки-хвосты обязаны получить дом ДО закрытия».** Домов три, и каждый проверен ЧТЕНИЕМ ленты адресата, а не ссылкой на неё:

- **четыре треда без хода** (`043`, `044`, `073`, `080`) → **тред `062`, блок 8-бис**. Прочитано `thread show --tail 1`: блок стои́т последним письмом `062` (104-м), с доводом «нет двери по построению» и с перечнем, что в каждом лежит. В окончательном порядке утра это **шаг 10**, точка невозврата — 11;
- **`047.1-devops-enablement-acceptance`** (удалить рукой) → тот же порядок утра, **шаг 11**, назван поимённо в строке точки невозврата;
- **«исчерпанную пару нечем разморозить штатно»** → тред `150`, и он уже **исполнен**: PR #306 влит `15:44:22Z`. Поэтому шаг 9 утреннего порядка теперь говорит «после перезагрузки доступна штатная команда `thaw`, обходной флаг — запасной путь». Хвост обхода закрылся чужой работой, второго дома заводить не пришлось.

**«Ничего не чинить по дороге»** — не чинилось ничего: ни заморозок, ни `047.1`, ни замка четырёх тредов.

## 3. Парк снят — замерено, а не заявлено

В `notify.state` (`19:4xZ`) блока `parked` на `147` **нет**: паркованных тредов пять — `029`, `062` (`19:36:46Z`), `141`, `143`, `150`. Оба ключа сверены, не один: `asked` по `147` — две записи (`14:04:58Z`, `15:25:52Z`), обе исторические, звонков без основания не осталось. Закрываю тред без висящего парка.

## 4. Открытых PR к кнопке curator — ноль

`gh pr list --state open` сейчас даёт **ровно один** PR — **#317** (`137-double-letter-on-lost-push-receipt`, рука `dev-core`). Прогнала по нему `merge-gate` (голова `6598b08`): **REFUSED** — гард 1 STOP (вердикта на голове нет), гард 2 STOP (`checks=IN_PROGRESS`), гард 4 ok (4 пути, доков власти среди них нет), гарды 3 и 5 — обязательства его треда. Кандидатом он не является дважды: и по фактам, и по маршруту — след гарда 5 обязан лечь в ленту `137`, а этот прогон связан одним тредом `147`.

## 5. Чего этот ход не делал

Не назначал ход четырём тредам без хода — это по-прежнему письма в чужие ленты, и решение john именно поэтому вынесло их в руку человека (шаг 10 утра). Не мержил ничего. Не трогал `047.1`.

## Ход

`waiting-on: —` — ход не передаётся никому, тред закрывается флипом `thread status --status closed` сразу за этим письмом. Всё живое, что обход нашёл, ведётся своими тредами и названо здесь поимённо.

