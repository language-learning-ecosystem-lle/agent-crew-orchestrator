---
from: curator
worker: claude-code
session: f57aed1a-6e57-40bd-8b9f-13c163ca3285
raised: 2026-09-03T14:05:18Z
date: 2026-09-03T14:10:46Z
expects: none
waiting-on: —
---

**Приёмка `058` и закрытие: `status: closed` этим же ходом. Все предметы треда разобраны поимённо — домом или смертью вслух, — и ни один не остаётся бесхозным. Полевой критерий §5 постановки я ЗАМЕНЯЮ перечислением и говорю это вслух, а не молча.**

Ссылки на письма — автором и фактом, а не ординалом.

## 1. Перемерено СВОЕЙ рукой, а не принято с прошлого письма

Предыдущее письмо в этой ленте (curator, 2026-09-03) утверждает, что ответ john уже пришёл и предмета не осталось. Утверждение верное по существу, но его я не принимаю как факт — оно чужое заявление ровно того же класса, что доклад исполнителя. Перемерено в этом ходе (сессия поднята `2026-09-03T14:05:18Z` — поле `raised:` в шапке этого письма):

| факт | чем измерено | результат |
|---|---|---|
| норма сужения лифта | `gh pr view 204` | `MERGED`, base `main`, merge-коммит `a72b718555cf04ac39cfb11ec765c186e8a8f9e9`, `2026-09-02T21:15:16Z` — кнопка john |
| текст нормы в `main` | `git show origin/main:PROTOCOL.md` | абзац сужения (R27) и абзац основания `raised:` на месте, вместе с вырожденным случаем и границей равенства. Читано в файле, не по докладу |
| **механизм** | `gh pr view 215` | **`MERGED`**, base `main`, merge-коммит `2e4c8f3487d7f93fa9420ddc1768a796980c563a`, `2026-09-02T23:05:23Z`. Прошлое письмо говорило «механизм ставится тредом `067`» — **дважды неточно**: дом механизма — тред `081-park-lift-raised-field`, и он не «ставится», а уже в `main` |
| код нормы | `git show origin/main:.../thread/thread.ts` | `sawThePark(claim, parkedAt) = claim.raised === undefined \|\| claim.raised >= parkedAt`, наложен в ДВУХ местах ветви person-парка: на список `handedTo` (пункт i) и на `outcomeFor` при том же держателе (пункт ii) |
| покрытие | `git show origin/main:.../thread/park-raised.test.ts` | файл в `main`, 9 кейсов; **инцидент ЭТОГО треда зашит до секунды** — `RAISED=14:24:19Z`, `PARKED_AT=14:24:50Z`, `WROTE_AT=14:26:53Z`, кейс «the incident, to the second: raised BEFORE the park — the park STANDS» |
| B.1 / B.3 / находка №11 | `gh pr view 153/170/171` | `MERGED` в `main`: `fae3eafe` (19:27:23Z 30.08), `550c0266` (22:20:34Z 30.08), `8ff7a103` (00:15:53Z 31.08) |
| хвост PR треда | `gh pr list --state open` | 6 открытых: #239 (`090`), #237 (`078`), #233 (`064`), #216 (`052`), #202 (`074`), #114 (`048`). **Ни один не несёт `thread: 058-…`** |

## 2. Композиция звонка (B.2) — измерена в коде, и она НИКОГДА не была дефектом

Это единственный пункт, где две ленты расходятся, и разрешаю я его замером, а не выбором более позднего письма.

- приёмка `067` (curator, 2026-09-02, §2 п.5) говорит: композиция звонка — **дом `058`, этим решением НЕ закрыта**;
- письмо в этой ленте (curator, 2026-09-03) говорит: **закрыта**, исполнимого нет.

Прочитано в `origin/main`:

- `notify/template.ts` — слот `parked: "your decision: {thread} — {question}"`;
- `notify/notify.ts:469` — `question` определён дословно как **«The first line of the parking message — the question, in the words it was asked in»**;
- `cli.ts` — живая композиция строит `parked` из `parkingOf(thread, merged)` и кладёт `question: parking.question`, то есть первую строку ПАРКУЮЩЕГО письма, а не последнего;
- `parseNotifyState` вопрос **не хранит** и говорит почему: «the text is re-read from it» — текст перечитывается из паркующего письма каждый такт, устаревшим он быть не может.

Значит B.2 никогда не был правкой курьера: курьер брал стоящий вопрос и до инцидента. Показать было НЕЧЕГО, потому что парка в композиции не было — его снимало письмо сессии, поднятой раньше парковки. Это и есть корень, названный dev-core 30.08 и мной перепроверенный, и он закрыт нормой #204 + кодом #215 в `main`. **Исполнимого остатка у B.2 нет ни строки.**

**И живая половина замера здесь же, даром:** механизм #215 не «влит», а РАБОТАЕТ — шапка этого самого письма несёт третьим полем провенанса `raised: 2026-09-03T14:05:18Z`, поставленное контуром при подъёме сессии. То есть поле, которым норма john сузила лифт, пишется в поле, а не только в тестах. Сверено на выводе двери перед отправкой (сухой прогон `new-message` без `--write`).

Оговорка PROTOCOL.md («композиция звонка остаётся своим предметом») этим не отменяется и не теряется: она лежит в ДОКЕ ВЛАСТИ рядом с резервным замком, а не в этой ленте, — закрытие ленты её не трогает, ровно тем же доводом, каким `067` оставила там замок.

## 3. Приёмка: замена полевого критерия перечислением — вслух

Постановка (`msg-001` §5) дала критерий ПОЛЕВОЙ: «звонок john несёт стоящий вопрос; john не задаёт вопросов вида „почему тред стоит, если в последнем письме про это ни слова“». Такой критерий держит тред открытым до случая, которого может не случиться, и жжёт слот каждый такт. **Заменяю его перечислением фактов, делающих класс невозможным** — по каждому из трёх вредов, названных `msg-001` §2:

1. **«звонок показал последнюю строку»** — невозможно: слот `parked` рендерит первую строку паркующего письма и перечитывает её из него каждый такт (§2). Условие, при котором звонок молчал, — снятый парк — закрыто `sawThePark` в `main`, с инцидентом этого треда в юните до секунды;
2. **«чатный curator построил ответ на последнем письме»** — закрыто B.1 (#153): `thread show --for <role>` называет непрочитанное с СОБСТВЕННОГО последнего письма читателя. Замерено прямо сейчас на этой ленте: первой строкой вывода стоит `unread for curator: none — the last message in the thread is curator's own letter`;
3. **«роль, поднятая следующей, пропустит предыдущее письмо»** — закрыто B.1 там же и B.3 (#170): письмо в паркованный тред обязано парк назвать, а дверь, не сумевшая прочитать ленту, говорит это вслух вместо молчания (#171, находка ревьюера №11).

Ни один из трёх вредов не имеет пути повторения при этих механизмах в `main`. Это сильнее образца и стоило шести чтений.

## 4. Предметы поимённо — вторая проверка приёмки

Закрытие треда — приёмка, а не «последнее письмо сказало, что всё»:

1. **B.1** (роль читает всё с момента своего письма) — **исполнен**, #153 в `main`;
2. **B.3** (письмо в паркованный тред называет парк) — **исполнен**, #170 в `main`;
3. **находка ревьюера №11** (дверь парка не молчит о непрочитанной ленте) — **исполнена**, #171 в `main`;
4. **B.2** (звонок несёт стоящий вопрос) — **исполнен корнем**: норма #204 (кнопка john) + код #215 (тред `081`), оба в `main`; композиции править нечего (§2);
5. **развилка (A), замок «одна живая сессия на тред»** — **дом не лента, а док власти**: `PROTOCOL.md` держит его в резерве вместе с триггером возврата («если класс воспроизведётся после сужения») — прочитано в файле сегодня. Закрытие `058` его не теряет;
6. **§4 моего письма от 31.08 — долг «канал исхода `checks` заморожен установкой»** — **МЁРТВ ПО ФАКТУ, и это не отсрочка**. Замерено сегодня: `docs/install-notes.md` в `main` несёт раздел «§1. Исход CI приезжает в тред событием — **с 2026-09-02**», `ci-outcome.yml` и `notifier-watch.yml` **разморожены 2026-09-02 решением john** (тред `073-notifiers-frozen-in-own-contour`). Предмета, который я пятый такт обещала унести своим тредом, больше не существует — уносить нечего, и треда под него я не завожу. Структурная половина того же §4 («прогон curator связан одним тредом, завести новый значит писать вне его») снята john 2026-09-02 (тред `061`): `new-thread` из связанного прогона законен при названном происхождении первой строкой.

Бесхозного после закрытия не остаётся ни одного предмета.

## 5. Что я в этом ходе НЕ делала — вслух

- **хвост открытых PR не трогала**: гард 3 восходит к решению в ИХ треде, гард 5 требует следа в НЁМ ЖЕ, а прогон связан одним тредом. Шесть открытых перечислены в §1 с их тредами — чтобы хвост был виден, а не чтобы за него отчитаться;
- **merge своей рукой в этом ходе нет вовсе** — PR треда все смёржены ранее, гарды #171 названы поимённо в моём письме от 31.08, и второй раз я их не пересказываю;
- **кода не писала**, `packages/**` не трогала: всё чтение шло `git show origin/main:<путь>`, рабочее дерево не писалось ни разу. `git status --porcelain` пуст, незакоммиченного за мной нет.

## 6. Закрытие

`thread status --thread 058-concurrent-writers-one-thread --status closed --write` — исполняю следующей командой этого же хода. Право `thread-status` роли дано, приёмка — §3 и §4 выше.

Следствие закрытия называю прямо: `status: closed` — узкий лифт, он снимает парковку при любом моменте подъёма. Стоящих парковок здесь нет (парк на john снят письмом от 2026-09-03), так что закрытие никого не будит и не зовёт.

waiting-on → — (тред закрыт; живых ходов по нему нет).

