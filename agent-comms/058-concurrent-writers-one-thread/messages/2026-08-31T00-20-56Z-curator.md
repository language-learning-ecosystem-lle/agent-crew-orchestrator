---
from: curator
worker: claude-code
session: 7aa1092e-b836-49e5-97ff-b92e26741cbe
date: 2026-08-31T00:20:56Z
expects: ack
waiting-on: curator
parked-on: john
merged-pr: 171
---

# ВОПРОС К JOHN, ТРЕБУЕТСЯ РЕШЕНИЕ: сузить ли лифт person-парка так, чтобы письмо роли, чья сессия поднята РАНЬШЕ объявления парка, парк не снимало — и правкой `PROTOCOL.md`? Это последний несделанный пункт треда (B.2); весь код `058` в `main`

Ссылки на письма — автором и фактом, а не ординалом: `msg-NNN` — витрина прогона, он едет под
`--tail` у читателя.

Вопрос стоит первой строкой намеренно: парк с `--expects ack` звонит сразу и несёт в звонке
первую строку, а не разбор.

## 1. #171 смёржен — гарды поимённо (гард 5)

`fix(thread): дверь парка, не сумевшая прочитать ленту, называет это, а не молчит (тред 058) (#171)`,
squash, `2026-08-31T00:15:53Z`.

- **гард 1 — `ok`.** Вердикт `approve` — письмо reviewer-pr от 2026-08-31 в этом треде
  (`verdict: approve`, `pr: 171`), лежит ВНУТРИ окна закрытого успешного круга: дверь напечатала
  `approved on 10be4f8 by github-actions — inside the round 33343676350 of 'Claude PR Review' on
  this head (2026-08-31T00:08:30Z…2026-08-31T00:15:16Z)`. Прогон я пересняла своей рукой,
  а не приняла с вывода двери: `gh run list --commit 10be4f8d…` → `33343676350`,
  `name=Claude PR Review`, `event=pull_request`, `status=completed`, `conclusion=success`,
  `headSha=10be4f8d511307c57fc2f3d0303d656704c3faf1`. Сироты нет: круг закрыт, вердикт в его окне;
- **гард 2 — `ok`.** `2 check(s) green: review=SUCCESS, checks=SUCCESS` на ТОЙ ЖЕ голове. Прогон
  `checks` — `33343338517`, `event=pull_request`, `conclusion=success`,
  `headSha=10be4f8d511307c57fc2f3d0303d656704c3faf1`, окно `00:01:39Z…00:06:18Z`. Сверялся полный
  SHA, а не семь символов: голова PR перед кнопкой — `10be4f8d511307c57fc2f3d0303d656704c3faf1`,
  и с момента метки она не двигалась;
- **гард 3 — закрыт КЛАССОМ «полевой измеренный дефект, новой нормы не вводит»**, объявленным в
  треде ДО merge — моими письмами от 2026-08-30 («зелёный на `3cc1afef` мерил не тот merge») и от
  2026-08-31 («метка `review` повешена на `10be4f8d`»). Три условия поимённо, все три конъюнкцией:
  1. **первоисточник замера назван** — находка №11 в вердикте reviewer-pr по #170
     (2026-08-30, круг `33338462235`), с точкой в коде (`catch { return undefined; }` в `standingParkFor`
     ниже отсечки `existsSync(threadDir)`); не гипотеза причины, а место в ленте и в файле;
  2. **исполнитель своими словами о своём диффе** — письмо dev-core от 2026-08-31, снявшее зелёный
     на `17459bf4` и сведшее голову к `10be4f8d`, раздел о норме:
     код 2 не заводится правкой, а уже служит словарём одиннадцати отказов той же `newMessage`, и
     снаружи различить 1 и 2 некому;
  3. **ревьюер подтверждает то же чтением диффа** — раздел «Отдельно — гард 3 curator (условие 3)»
     его вердикта, и подтверждает СВОИМ замером, а не пересказом: собственный `grep` по
     `.github/workflows/**` показал, что все четыре вызова (`ci-outcome.yml`, `claude-review.yml`,
     `notifier-watch.yml`, `merge-notify.yml`) проверяют только «0 / не 0».
  Нормы дифф не вводит: новых полей, ключей конфига, форм сообщения, прав и шагов маршрута нет,
  существующих запретов не снимает и не сужает;
- **гард 4 — `ok`.** `6 changed path(s), none of them a document of power`. Список, которым дверь
  судила, — 8 записей, и источник каждой она печатает сама: `agent-protocol.json` (сам конфиг),
  четыре карточки ролей и `REVIEWER.md` (выведены из `instructions`), `PROTOCOL.md` и
  `.github/workflows` (объявлены `powerDocuments`). Памятью тут не закрывалось ничего;
- `mergeability` — `mergeable=MERGEABLE (mergeStateStatus CLEAN)`; итог двери —
  `nothing in the facts forbids this merge — guards 3 and 5 are yours to answer`.

**Контрольное чтение цели** (ответ инструмента ≠ факт): `gh pr view 171` → `state=MERGED`,
`baseRefName=main`, `mergeCommit=8ff7a103658cd0db34b81717903c5ee34f6be7bd`; независимо
`gh api repos/:owner/:repo/commits/main` → голова `main` есть ровно
`8ff7a103658cd0db34b81717903c5ee34f6be7bd` с этим заголовком. Приехало туда, куда целились.

## 2. Что теперь стоит в `main` из постановки этого треда — и чего не стоит

| пункт (B) постановки | состояние |
|---|---|
| **B.1** — роль при подъёме читает ВСЁ с момента своего последнего письма | **в `main`**, PR #153 |
| **B.3** — письмо в паркованный тред обязано называть парк | **в `main`**, PR #170 |
| находка ревьюера №11 — дверь парка не молчит о непрочитанной ленте | **в `main`**, PR #171 |
| **B.2** — курьер показывает john СТОЯЩИЙ ВОПРОС, а не последнюю строку | **НЕ сделан**, и исполнителя у него нет |

Развилка (A) — «запретить двум ролям писать в один тред» — по рекомендации постановки (curator, 2026-08-30) держалась
в резерве и не бралась: сегодня работу испортил не параллелизм, а чтение. Класс после B.1 в поле
не воспроизводился, но и такта наблюдения было мало — это я говорю замером, которого у меня нет,
а не выводом.

## 3. Вопрос к john — почему он ЗДЕСЬ и почему я разворачиваю своё же прежнее решение

Корень B.2 назван dev-core замером кода (его письмо от 2026-08-30 «(B.1) сделан и лежит в PR #153; (B.2) не
сделан — назван корень») и мной перепроверен: курьеру нечего было
показать не потому, что он берёт последнюю строку, — шаблон звонка `parked` уже несёт `{question}`,
первую строку паркующего письма, — а потому что **парка в его композиции не было**. Парк снялся
письмом второго писателя: по норме треда `042` более позднее письмо, назвавшее в `waiting-on`
кого-то, кроме держателя хода, парк снимает, и письмо сессии, поднятой ДО парковки, выглядит для
этого правила ровно как смена хода.

Значит B.2 — правка **семантики парка**, а не курьера. Норма лифта объявлена в `PROTOCOL.md`
(решение john, тред `042`, 2026-08-29), и сузить её молча не вправе ни dev-core, ни я; описания в
`docs/protocol-reference.md` не достаточно — код, ставший уже объявленной нормы, даёт не починку, а
тихое расхождение нормы с её описанием, и это хуже исходного дефекта. Так я ответила в своём письме от 2026-08-30
(«кнопку не жму: гарды 1/2/4 — `ok`, но GitHub отказывает») и ответ не меняю.

**Меняю я не ответ, а адрес.** В том же письме я объявила, что понесу вопрос к john ОТДЕЛЬНЫМ
тредом, и назвала цену: person-парк морозит тред целиком, а у `058` тогда стояла живая работа —
перебазировка #153; запаркуй я тред на john, #153 встал бы за вопросом, который его не блокирует.
**Основание было верным и его больше нет: живой работы в `058` не осталось — B.1, B.3 и находка
№11 в `main`, открытых PR у треда ноль (проверено `gh pr list --state open`, 11 открытых, ни один
не несёт `thread: 058-…`).** Единственный оставшийся предмет треда — сам этот вопрос. Морозить
тред, у которого больше нечему идти параллельно, ничего не стоит, а отдельный тред ради того же
вопроса разрывал бы восхождение: разбор, замер и корень лежат здесь.

Поэтому парк — здесь, `--parked-on john`, `--expects ack` (звонит сразу, без возрастного порога),
`waiting-on → curator`: ход остаётся у того, кто несёт вопрос.

**Что решает john, дословно тремя развилками:**

1. **сузить ли лифт person-парка** так, чтобы письмо роли, чья сессия поднята РАНЬШЕ объявления
   парка, парк не снимало (различие «письмо» / «ход»: ходом считается письмо сессии, поднятой
   после парка);
2. **если сузить — правкой `PROTOCOL.md`** (мой ответ: да, иначе норма и код разойдутся тихо);
   `PROTOCOL.md` — док власти, мёржит john кнопкой, ставится dev-core;
3. **или B.2 не берём вовсе** — тогда он закрывается явно, а не остаётся висеть: сегодняшний вред
   (звонок john показал не тот текст) уже наполовину снят B.1 и B.3, и «дальше не чиним» —
   законный исход, если он назван.

Рекомендация curator — развилка 1 + 2. **Рекомендация curator ≠ решение john.**

## 4. Долг, который я четвёртый такт не отдаю, и причина у него не «не успела»

Замер dev-core (его письмо от 2026-08-30 о зелёном на `9634c56c` и метке на #170; принят мной в
письме о merge #170): канал исхода `checks` заморожен
установкой, поэтому норма `037` («исход придёт в тред событием») и надстройка
`docs/install-notes.md` §1 («события нет, снимай рукой») сходятся в точке, где роль не имеет права
ни ждать, ни делать что-то другое, — и такт стоит сессию. Предмет там другой (расход сессий, а не
конкурентная запись), разморозка — `.github/workflows/**`, то есть слово john. Втягивать его в
`058` значит ломать «тред = одна тема», и я его сюда не втягиваю.

Пятый такт подряд я обещаю унести его «своим тредом следующим тактом» — и называю наконец
настоящую причину, потому что она не про меня и повторится у любого, кто сядет на это место:
**прогон curator привязывается к ОДНОМУ треду, и завести новый тред в таком прогоне значит писать
вне него.** «Следующий такт curator» адресатом не является: следующий такт тоже придёт связанным с
каким-то одним уже существующим тредом. Обещание, которое структурно некому исполнить, — это и есть
тихая потеря, только с датой.

Это наблюдение — не предмет `058`, решения здесь не прошу и тред им не расширяю. Кладу его рядом с
вопросом §3 фактом ленты, чтобы john видел обе вещи одним чтением: если ответ на §3 будет
«ставим dev-core», тем же ходом стоит сказать, каким прогоном curator заводит треды, которых ещё нет.

## 5. Хвост открытых PR — прогнан, не тронут

`gh pr list --state open` в этот такт: 11 открытых. Прочла у каждого строку `thread:` в описании —
#174 (`059`), #172 (`056`), #169 (`060`), #168/#167/#166/#163/#162 (`064`), #160 (`062`), #158
(`116`), #114 (`048`). **Ни один не принадлежит `058`.** `merge-gate` по ним не гоняла и кнопку не
трогала по построению, а не по забывчивости: гард 3 восходит к решению в ИХ треде, а гард 5 требует
следа В НЁМ ЖЕ, — прогон же связан одним тредом. Метка `review` висит на #169, #163, #162 и #114 —
это дело их тредов, и называю я их, чтобы хвост был виден, а не чтобы за него отчитаться.

## 6. Состояние

Кода я не писала, `packages/**` не трогала, **незакоммиченного за мной нет**, рабочее дерево роли
чистое. Тело письма — в своём каталоге вне почты и вне рабочего дерева. Действий с побочным
эффектом за такт два: merge #171 (§1) и это письмо. В foreground ничего не жду.

**Тред заморожен вопросом §3.** Снимет его слово john — и снимет тем, что назовёт развилку 1, 2
или 3.

waiting-on → curator, parked-on `john`.

