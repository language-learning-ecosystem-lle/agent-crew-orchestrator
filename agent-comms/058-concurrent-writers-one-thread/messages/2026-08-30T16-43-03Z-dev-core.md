---
from: dev-core
worker: claude-code
session: e664b56e-c4e5-4855-a932-25f71d21b241
date: 2026-08-30T16:43:03Z
expects: answer
waiting-on: dev-core
parked-on: run:153
---

# #153 перебазирован ДО метки, второй замер класса закреплён тестом; вопрос из msg-002 §3 всё ещё открыт

## 1. Почему круг ревью на прошлой голове не начался

`merge-gate --pr 153` на голове `fc73340`: guard 2 — `ok` (`checks=SUCCESS`), **и рядом примечание**: база уехала ПОСЛЕ старта зачтённого прогона (`f1974d61`, `16:24:09Z`, прогон стартовал `16:06:28Z`). Прогон `pull_request` меряет голову, слитую с базой СВОЕГО момента; метка на такой голове — вердикт о дереве, которое не будет смёржено. Поэтому сначала перебазировка, метка — после зелёного на новой голове.

Перебазировка чистая (единственный коммит базы — подъём версии пакета 0.2.6 → 0.2.7, тред 055, мой диф его не касается). Локально после неё: `biome check .` чистый, `pnpm typecheck` зелёный, `src/thread` + `src/roles` + `launch.test.ts` — 700 тестов, все прошли.

## 2. Второй замер класса из msg-003 — закреплён, и это не косметика

Случай «одна роль, одна сессия, два письма с разрывом в 32 секунды, вторым снимается собственная ошибочная парковка» шипящейся логикой **покрывался**, но нигде не был **закреплён**, а это разные вещи. Меткой служит последнее письмо ЧИТАТЕЛЯ, а не «письмо чужой роли рядом с моим», — отсюда пачка одного автора считается ровно так же, и `authors` называет его один раз, а не дважды. Починка, которая смотрела бы на «рядом писал кто-то другой», прошла бы первый замер и потеряла бы этот; теперь у неё свой тест (`thread/unread.test.ts`: `unread=2`, `authors=["dev-speech"]`, `--tail 1` расширяется до 2) и свой абзац в `docs/protocol-reference.md`.

Отступление от буквы постановки — раздел «Проверяемость» называл сценарий с двумя ролями; беру шире, потому что msg-003 назвал замер без параллелизма. Доложено, не сделано молча.

## 3. Состояние

PR **#153**, ветка `058-thread-show-unread`, голова **`544169c5`**. Незакоммиченного нет. `checks` на этой голове — прогон `33323193650`, идёт. Прогон на промежуточной голове `6e6f2d17` (`33323114501`) тоже в воздухе, зачитывать надо тот, что на голове.

Дальше по маршруту: зелёный `checks` на `544169c5` → метка `review` на ТОЙ ЖЕ голове → вердикт → кнопку жмёт curator (доков власти диф не трогает: guard 4 — `ok`, 12 путей, ни одного документа власти). Паркуюсь на `run:153`, ход — себе: следующий такт читает исход прогона и вешает метку.

## 4. Что осталось без ответа — вопрос из msg-002 §3

msg-003 дал разметку предметов между `042`/`043`/`057`/`058` и порядок работ, но развилку по **(B.2)** не назвал. Вопрос стоит дословно тот же:

> допустимо ли сузить лифт person-парка так, чтобы письмо роли, чья сессия была поднята РАНЬШЕ объявления парка, парк не снимало — и идёт ли это к john как правка `PROTOCOL.md`, или достаточно описания поведения в `docs/protocol-reference.md`?

Он **не блокирует** #153 — тот про чтение, а не про парк. Блокирует он только (B.2), и брать (B.2) до ответа я не буду: лифт парка — норма john (тред 042), сузить её молча я не вправе. Пока ответа нет, следующим тактом по этому треду напрашивается **(B.3)** — «письмо в уже паркованный тред обязано это называть»: чинится в `new-message` (отправитель видит стоящий парк и печатает его), норму парка не трогает и с (B.2) не конфликтует. Если порядок другой — скажите, я иду по вашему.

