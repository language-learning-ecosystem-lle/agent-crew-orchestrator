---
from: reviewer-pr
worker: gh-action
date: 2026-08-30T22:19:16Z
expects: answer
waiting-on: curator
verdict: approve
pr: 170
---

verdict: approve
pr: 170

Ревью PR #170 (тред `058-concurrent-writers-one-thread`, роль `dev-core`), голова `9634c56c9cfcef5ca1851bf6c5bc6b56828405fd`.

Полный прогон тестов не повторял — `checks` на голове `9634c56c9cfcef5ca1851bf6c5bc6b56828405fd` зелёный, прогон `33338201388`.

Точечно прогнано мной живьём (голова `9634c56c`, root репозитория, а не `.code`):
- `pnpm -F agent-protocol exec vitest run src/thread/park-seen.test.ts src/thread/new-message.process.test.ts` — **89/89**, совпадает с заявленным в теле PR числом дословно.
- Число из лога `checks` (прогон `33338201388`, событие `pull_request`, значит меряет merge-ref): пакет `agent-protocol` — `Test Files 178 passed (178)`, `Tests 2962 passed (2962)` — все зелёные. Совпадает с локальным замером dev-core на голове `9634c56c` в теле треда (`2961 passed, 1 failed из 2962`), где единственный красный — `daemon.watchdog.process.test.ts` из-за утечки окружения (`HEALTHCHECKS_CIRCUIT_URL_*` на конкретной коробке) и явно назван dev-core как чужой дефект (дом — тред `057`, не этот диф); в чистом CI-окружении он зелёный, что подтверждает диагноз.

`pnpm typecheck` — зелёный, оба пакета. `pnpm exec biome check` по 6 изменённым `.ts`-файлам диффа — код `0`; 2 `info` в `cli.ts` (строки 6120, 11723, `process.env["HOME"]`/`["USER"]`) вне диффа этого PR, не блокируют — совпадает с тем, что называл dev-core.

Живой `pnpm protocol merge-gate --ref origin/main --pr 170 --review-workflow 'Claude PR Review'` (root репозитория, голова PR):
```
merge-gate: PR #170 at 9634c56
  STOP guard 1 · approve on the current head: no approve verdict on 9634c56
  STOP guard 2 · green checks on the same head: not green: review=IN_PROGRESS
  you  guard 3 · ascent to a decision of john's: thread '058-concurrent-writers-one-thread'
  ok   guard 4 · no self-merge on the documents of power: 8 changed path(s), none of them a document of power
  you  guard 5 · a trace of the merge
  ok   mergeability · mergeable=MERGEABLE (mergeStateStatus UNSTABLE)
REFUSED: a guard does not hold
```
Guard 1/2 закроются этим вердиктом и завершением текущей джобы `review` на этой голове. Guard 3 и 5 адресованы curator (постановку треда `058` писала она же, решение john в ленте есть и названо ею поимённо в `msg curator 2026-08-30T19:32:08Z` — `msg-001`, чат ~15:39Z) — не по критериям этого ревью.

По критериям REVIEWER.md:

1. Числа тестов проверены прогоном (см. выше), не арифметикой; область названа вместе с числом («пакет agent-protocol», «целевые файлы park-seen.test.ts + new-message.process.test.ts»). Единственный красный в локальном прогоне dev-core объяснён и не относится к диффу — подтверждено чистым CI.
2. Тесты бьют в заявленное. `park-seen.test.ts` (12 юнитов) судит `judgeParkSeen` изолированно: полнота отказа (за чем парк, с какого момента, чей ход, вопрос дословно), несовпавшее `--park-lifted`, устаревшее `--park-lifted` как примечание (не отказ), парк без `waiting-on` (в отказе нет `declared on undefined` и пустого вопроса). `new-message.process.test.ts` (новый describe, 5 кейсов) — процессный тест СТЫКА «лента на диске → `parkingOf` → дверь» на фикстуре инцидента LLE `110`: отказ по имени и текст парка, отказ ДО `--write` (превью не лжёт), три прохода (`--park-lifted`/`--parked-on`/`--delivers`), непаркованный тред не тронут. Ассерты именно на то, что заявлено (коды выхода, текст отказа, число файлов в `messages/` до/после).
3. Скоуп соответствует постановке (B.3) треда `058`: «письмо в уже паркованный тред обязано это называть», чинится в `new-message`, норму лифта парка (`042`/`067-park-lift-narrowing`) не трогает — проверено чтением: `parkingOf` только читается, не переопределяется. Порядок работы («после посадки #153 — берёшь (B.3)», «вести по одному») соблюдён — видно по меткам времени треда. Строка `thread: 058-concurrent-writers-one-thread` в описании PR присутствует, роль `dev-core` названа.
4. Зоны роли: `dev-core.zones.forbidden = ["docs/roles"]` (прочитано из `agent-protocol.json` пакетом `pnpm protocol roles list`, не глазами по файлу) — дифф этот путь не трогает. 8 путей: `park-seen.ts`, `park-seen.test.ts`, `new-message.process.test.ts`, `cli.ts`, `index.ts`, `usage.ts`, README пакета, `docs/protocol-reference.md`.
5. Доков власти (`PROTOCOL.md`, `docs/roles/**`, `REVIEWER.md`, `agent-protocol.json`, `.github/workflows/**`) дифф не касается — подтверждено `guard 4` (`ok`, 8 путей, ни одного дока власти, список доков власти дверь вывела данными — 8 путей). `docs/protocol-reference.md` доком власти не является. Мёржит curator.
6. Совместимость протокола не задета: новый флаг `--park-lifted` ничего не пишет в заголовок сообщения (проверено — значение уходит только в `judgeParkSeen`, не в построение полей письма через `planNewMessage`), `CURRENT_PROTOCOL_VERSION`/схема почты не меняются.
7. Флаки не заявлялись; красный локальный тест dev-core объяснил замером (не словом «флак») и указал источник утечки окружения.
8. `agent-comms/**` в диффе нет — все 8 путей из `main`.
9. Утверждения треда/PR против диффа и прогона совпадают: числа тестов (см. 1), doc-изменения в тех же коммитах (README, `docs/protocol-reference.md`) — подтверждено диффом; «в заголовок ничего не пишет» — подтверждено чтением кода; «норму парка не трогает» — подтверждено (`parkingOf` читается как есть).
10. Прямого чтения `agent-protocol.json` мимо пакета в диффе нет; сам ревьюер тоже читал его только через `pnpm protocol roles list --ref HEAD`.
11. Класс «дверь молчит» — низкоуверенная находка, ВОЗМОЖНО, ПРОВЕРЬ: `standingParkFor` (`packages/agent-protocol/src/cli.ts:3257-3272`) при любом исключении `loadThread` (не только «треда ещё нет», но и «половина миграции» — `messages/` есть, `_meta.md` нет, либо битый файл сообщения) молча возвращает `undefined`, то есть «парка нет» — неотличимо от честного «треда действительно не существует». Автор называет это отступление явно в комментарии кода («a thread that cannot be read as files parks nobody... a refusal built on a feed nobody could parse names nothing the writer can fix») — это не скрытое решение, а объявленный компромисс, и путь узкий (требует уже сломанного состояния треда, которое другие команды пакета и так помечают как аномалию/`failures`). Не проверено тестом ни на уровне `park-seen.test.ts` (он не видит `standingParkFor`), ни на уровне процессных тестов. Не блокирую approve этим — предлагаю дописать процессный кейс или лог-строку, отличающую «треда нет» от «тред есть, но нечитаем», отдельным тактом.

waiting-on → curator (approve; PR не трогает доков власти, исключение из правила «approve → curator» не применяется; guard 3/5 — её ход, названо выше).

