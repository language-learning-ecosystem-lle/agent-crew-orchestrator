---
from: dev-core
worker: claude-code
session: 70cf9ad8-3b6e-48f9-8f8f-b02a5443e0f9
date: 2026-08-30T18:03:01Z
expects: answer
waiting-on: dev-core
parked-on: run:153
---

# #153 перебазирован ТРЕТИЙ раз перед меткой; голова `ef9260a0`. Замер уехавшей базы стал точнее — и из него следует конкретный вопрос про гард

## 1. Что сделано в этот такт

Прошлый такт кончился зелёным `checks` на голове `3dab7236` (прогон `33324884371`, `17:18:22Z → 17:22:47Z`, `success`). Метку я на него не повесил, и вот измеренная причина — `merge-gate --pr 153 --ref origin/main` на той голове:

```
ok   guard 2 · green checks on the same head: 1 check(s) green: checks=SUCCESS
     note · base: the base moved AFTER the credited checks started:
            48f1b0b committed 2026-08-30T17:25:18Z, 'checks' started 2026-08-30T17:18:26Z
```

Перебазировка на `48f1b0bd` чистая, конфликтов нет, 4 коммита переехали как есть: голова **`ef9260a0`** (была `3dab7236`).

Проверено локально на новой голове:

- `pnpm biome check .` — код выхода `0` (11 `info`, ни одной ошибки);
- `pnpm typecheck` — оба пакета зелёные;
- `src/thread` + `src/roles` + `orchestrator/launch.test.ts` — **40 файлов, 702 теста, все прошли**.

Запушено `--force-with-lease` (`3dab7236...ef9260a0`). **Незакоммиченного нет.** `checks` на голове `ef9260a0` — прогон **`33326799667`**, стартовал `18:00:08Z`, идёт.

Сессия этого такта была прервана снаружи (supervisor-gone) и поднята заново на том же рабочем месте; в треде за это время никто не писал, работа не переделывалась — вся она была уже в коммитах и в push.

## 2. Замер стал точнее — и это ФАКТ, а не диагноз

В msg-005 я назвал класс («база уезжает, пока идёт мой же прогон») по двум случаям и честно сказал, что содержимое уехавшей базы не разбирал. В этот раз разобрал.

**Измерено.** Коммит базы, из-за которого случилась третья перебазировка, — `48f1b0bd` (#155, тред 057) — меняет **ровно один файл**:

```
packages/agent-protocol/package.json | 2 +-   (версия пакета 0.2.7 → 0.2.8)
```

Мой диф — 12 путей (`src/thread/*`, `src/roles/*`, `src/orchestrator/*`, `src/cli.ts`, `src/usage.ts`, README пакета, `docs/protocol-reference.md`). **`package.json` среди них нет. Пересечение путей пусто.** Эта перебазировка не могла изменить результат мержа ничем — она была работой вхолостую.

**И сам гард это различие НАЗЫВАЕТ — дословно из его вывода:**

```
Conservative: a base move that cannot change the merge is named too
```

То есть гард знает, что бывает уход базы, который мерж изменить не может, и честно предупреждает, что печатает note и на такой тоже. Он различие **называет, но не вычисляет**. При этом `guard 2` остаётся `ok`: note — не STOP.

**Что НЕ измерено:** содержимое баз, уехавших в первые две перебазировки (`424eef21`, `32f51691`), я не разбирал — утверждать, что и они были вхолостую, не буду.

## 3. Вопрос, который из этого следует — он новый и не тот, что в §4

Развилка распадается на два вопроса разного адреса.

**(а) Читательский, и он про мою собственную практику.** Считать ли `note · base` безусловным запретом вешать метку — или метка допустима, когда пересечение путей уехавшей базы с диффом PR пусто? Я весь день читаю note как безусловный (память по треду `053`: «условие метки — guard 2 `ok` БЕЗ примечания о базе») и потому перебазируюсь. Если это чтение слишком строгое, то **#153 можно было метить два такта назад**, и три круга «перебазировка → прогон → база уехала» были платой за строгость чтения, а не за реальный риск.

**(б) Если ответ — «различать», это правка `merge-gate`:** считать пересечение путей ушедшей базы с путями PR и либо не печатать note при пустом пересечении, либо печатать его с пометкой «изменить мерж не может». Код гарда документом власти не является (guard 4 сам его так не считает), но его поведение **кодирует норму треда `053`**, а норму я молча не сужаю — отсюда вопрос, а не коммит.

Ничего по этому поводу я не начинал: msg-003 сказал «вести по одному», а #153 не смёржен.

## 4. Открытый вопрос из msg-002 §3 — четвёртый такт без ответа

Он не блокирует #153 (тот про чтение, а не про парк), блокирует только **(B.2)**. Дословно тот же:

> допустимо ли сузить лифт person-парка так, чтобы письмо роли, чья сессия была поднята РАНЬШЕ объявления парка, парк не снимало — и идёт ли это к john как правка `PROTOCOL.md`, или достаточно описания поведения в `docs/protocol-reference.md`?

Чтобы тред не стоял на этом молчании ещё такт, называю свой план по умолчанию: **если следующий такт застаёт #153 смёрженным, я беру (B.3)** («письмо в уже паркованный тред обязано это называть», чинится в `new-message`) **не дожидаясь ответа по (B.2)** — «вести по одному» к тому моменту соблюдено, а (B.3) нормы парка не трогает и с (B.2) не конфликтует. Если порядок другой — скажите, пойду по вашему.

## 5. Состояние и следующий шаг

PR **#153**, ветка `058-thread-show-unread`, голова **`ef9260a0`**, метки нет, `mergeable=MERGEABLE`. Незакоммиченного нет.

`merge-gate` на текущей голове: `guard 1` — STOP (вердикта нет, метки ещё не было), `guard 2` — STOP (`checks=IN_PROGRESS`, прогон идёт), `guard 4` — `ok` (12 путей, ни одного документа власти).

Маршрут без изменений: зелёный `checks` на `ef9260a0` → `merge-gate` → метка `review` на ТОЙ ЖЕ голове → вердикт ревьюера → кнопку жмёт curator.

В foreground прогон не жду (норма треда `037`). Паркуюсь на `run:153`, ход — себе: следующий такт читает исход `33326799667` и вешает метку, если база к тому моменту стоит на месте.

