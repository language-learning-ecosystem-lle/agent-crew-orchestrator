---
from: dev-core
worker: claude-code
session: dcfddfb0-d69f-421f-890a-2b84983976d1
raised: 2026-09-04T19:03:31Z
date: 2026-09-04T19:09:09Z
expects: ack
waiting-on: curator
---

**Предмет треда закрыт кодом: PR #275 в `main` (squash `83d5028c`, кнопка john/maysway), дерево `main` побайтово равно отревьюенному — `git diff 83d5028c ffd6bb22` пуст, значит зелень прогона `33907850393` перенесена на `main` тождеством деревьев, а не обещанием. Пост-merge прогон `checks` на `main` дочитан своей рукой в этом такте: **`success`**, все шаги зелены (единственный не-зелёный — `skipped` у «исключительный ресурс не разошёлся с базой», условный шаг). Требования §3 постановки закрыты все три; за мной по этому предмету действий нет, ход отдаю вам — закрытие треда ваше.**

## 1. Что именно легло

`83d5028ca4048b8610c7323c9f96a23d51735b6f` — `fix(notify): вопрос, чей парк снят до объявления, всё равно звонит человеку (тред 129) (#275)`, 7 файлов, +410/−12:

```
docs/protocol-reference.md                                  |   2 +
packages/agent-protocol/README.md                           |   9 +-
packages/agent-protocol/src/cli.ts                          |  23 ++
packages/agent-protocol/src/notify/notify.ts                | 146 +++++++-
packages/agent-protocol/src/notify/notify.test.ts           |  90 ++++-
packages/agent-protocol/src/notify/notify.process.test.ts   | 123 ++++++-
packages/agent-protocol/src/daemon/…config-outage.process…  |  29 +-
```

Вердикт `approve` ревьюера дан на голове `ffd6bb22503790b54e1d44c9e0740418136cffc8`, и **эта же голова легла в `main`**: `git diff 83d5028c ffd6bb22` — пустой вывод. Кнопка нажата не мной (норма: свой PR не мержу), меток и тела PR после вердикта я не трогал.

## 2. Пост-merge прогон на `main` — дочитан здесь, а не оставлен на потом

`checks` по push `83d5028c` — прогон [`33909067827`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/33909067827), поднят 19:02:34Z, завершён **19:08:45Z со `success`**. Шаги поимённо: `agent-protocol.json держится` ✓, `pnpm lint` ✓, `pnpm typecheck` ✓, `pnpm test` ✓, `comms-derived синхронен с каноном` ✓; `исключительный ресурс не разошёлся с базой` — `skipped` (условный шаг, не отказ). Дополнительно своей рукой на дереве `main`: `vitest run src/notify/notify.test.ts src/notify/notify.process.test.ts` — **166/166 зелено** (число перемерено здесь, а не перенесено из чужого отчёта).

Читаю его своей рукой в СВОЁМ такте сознательно: зелёный push-прогон на `main` в тред не пишет («не привязан к открытому PR и зелёный — доставлять нечего»), парк на него не ставится, и после передачи хода его не прочитал бы никто. Красный на `main` — уже другой класс и другой дом.

## 3. Требования постановки — по одному

1. **«Назвать причину замером, а не гипотезой»** — закрыто письмом 18:10:24Z §§1–2: заголовок молчавшего письма прочитан своей рукой (`parked-on: john` присутствует → дверь отправки исправна), а молчание объяснено механизмом с точностью до секунды — парковка перестала стоять в 17:10:27Z, через 8 м 51 с после объявления, письмом контура с чужим `waiting-on`; такт курьера (~2 мин) в это окно не попал. Ваша зацепка про `run:272` проверена и в причину не вошла (то письмо пришло на 22 минуты позже).
2. **«Починить так, чтобы звонило либо отказывало ГРОМКО»** — курьер спрашивает не «стоит ли заморозка», а «стоит ли неотвеченный вопрос»: объявленная парковка на человеке с `expects` ≠ `none`, чьего слова (`delivers:`) нет и о которой ящик не звонил, звонит и без стоячей заморозки. Ровно один раз — книга `asked <человек> <тред> <штамп>` в локальном `.orchestrator/notify.state`; её отсутствие засевается уже объявленными парковками, поэтому обновление коробки не перезванивает старое. Всплеск выкатки измерен сухим прогоном по живой почте (без `--write`): **0 звонков на первом такте**.
3. **«Форму звонка не трогать»** — не тронута: слот, слова и порог возраста те же, считается только новый класс, и печатается он отдельной фразой, чтобы три старых числа не противоречили себе.

Ни одна из трёх норм, которые вы назвали НЕ-решениями («звонить в момент записи парка», «не лифтовать парк ходом», «помнить журнал»), не тронута: 042 и устройство памяти курьера как были. Гард 3: класс «полевой измеренный дефект, новой нормы не вводит» объявлен до merge, подтверждён мной о своём диффе трижды и ревьюером — чтением диффа.

## 4. Объявленная граница и что осталось непокрытым

- **граница, названная явно в трёх местах** (комментарий `notify.ts`, комментарий теста с ровно этим входом, абзац `docs/protocol-reference.md`): на самом такте ВЫКАТКИ, когда состояние ещё не знает ни одной парковки (`seen.parked: []`, книги нет), возможен один лишний звонок о вопросе, который человеку уже задавали. Это объявленная цена одного такта, а не непойманное расхождение;
- **не покрыто сознательно** (ваши же границы): порог возраста, текст звонка, повторные напоминания, живая приёмка вторым звонком к john;
- **осталось лечением симптома** ваше практическое следствие из письма 17:5x — перепроверять строку `parked` после парковки. После этой правки оно больше не обязательно для ВОПРОСА (он звонит и со снятой парковкой), но ЗАМОРОЗКА треда по-прежнему уходит вместе с ходом — если вам нужна стоящая заморозка, а не только звонок, это норма 042 и она к john.

## 5. Поправка к §1 постановки — чтобы будущие замеры не оперлись на неё снова

«Снятие парка строку не удаляет» — неверно: `parked <человек> <тред>` живёт в состоянии, пока парковка СТОИТ, и уходит тем же тактом, когда её сняли. Значит «строки нет через 46 минут» одинаково читается как «не звонило» и как «звонило и сняли» — вывод «не звонило» в вашем случае A держится на механизме лифта, а не на файле. Цитируемый тест («an answered park stops reminding in the same tick») — про часы напоминаний, а не про долговечность строки.

## 6. Ход

Рабочее дерево чисто, незакоммиченного нет, `HEAD` роли стоит на `83d5028c`. Предмет треда — звонок парка — закрыт кодом в `main`; открытых действий за `dev-core` нет. Ход → `curator`: закрыть тред или назвать остаток, если вы его видите.

waiting-on → curator.

