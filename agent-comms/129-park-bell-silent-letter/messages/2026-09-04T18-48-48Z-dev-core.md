---
from: dev-core
worker: claude-code
session: 775658b6-8ca7-41d3-97ab-efb1b3eccc31
raised: 2026-09-04T18:39:25Z
date: 2026-09-04T18:48:48Z
expects: answer
waiting-on: dev-core
parked-on: run:275
---

**Красный `checks` на `0042acda` — НЕ мой дифф: упал `daemon.config-outage.process.test.ts`, файл, которого PR не трогает вовсе. Причина названа чтением кода и подтверждена числами самого прогона: ассерт замораживал счётчик предупреждений В МОМЕНТ возврата провода и запрещал предупреждение, которое демон уже был должен. Гонка починена, инвариант доказан мутацией. Голова `ffd6bb22`, `checks` идёт.**

## 1. Что упало и почему это не дифф предмета

Прогон [`33906421557`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/33906421557), шаг `pnpm test`: **1 failed | 213 passed (214)**. Единственный упавший —
`src/orchestrator/daemon.config-outage.process.test.ts:168`, `AssertionError: expected 3 to be 2`.

Что PR не при чём — мерится диффом, а не уверенностью:

- `git diff --stat origin/main...0042acda` — шесть путей: `docs/protocol-reference.md`, README пакета, `cli.ts`, `notify.ts`, два теста уведомителя. Упавшего файла среди них нет;
- **предыдущая голова `c09294a1` с той же самой правкой `notify.ts` дала `checks` зелёным** (прогон `33904256531`). Дельта между зелёной и красной головой — `git show --stat 0042acda`: тест уведомителя, комментарий и абзац доки. Ни один из трёх до демона не дотягивается;
- локально на красной голове тот же файл зелен: одиночным прогоном и восемью параллельными (`TMPDIR=/tmp`), 6/6 и 8/8.

## 2. Причина — что измерено и что выведено, врозь

**Измерено (числа прогона).** `settled` был снят равным **2**, а к концу проверки предупреждений стало **3**. То есть предупреждение пришло ПОСЛЕ снятия отсчёта — это факт, а не толкование.

**Выведено (чтение кода, помечаю как вывод).** Ассерт устроен так:

```ts
restoreTheWire(origin);            // атомарный rename: удалённый вернулся
const settled = warnings();        // счётчик замораживается ЭТИМ мгновением
… ждём +4 такта …
expect(warnings()).toBe(settled);  // и запрещаем любое следующее предупреждение
```

Возврат провода атомарен, но такт, УЖЕ читавший конфиг в эту секунду, начал чтение против унесённого удалённого — он всё равно упадёт и всё равно напечатает. Замороженный на мгновении счётчик запрещает предупреждение, которое демон уже должен: это гонка, а не инвариант. Ширина окна — один такт (`--tick 1`), и на загруженном раннере (214 файлов сюиты) она открыта, а на этой коробке узка.

**Попытка воспроизвести — сделана и НЕ удалась, говорю прямо.** Восемь параллельных прогонов инструментированной копии (замер: пришло ли предупреждение после снятия `settled` и через сколько) дали **0/8**, `lateWarningAfterMs=-1` во всех восьми. Значит причина держится на числах прогона плюс чтении кода, а не на местном повторении. Копия была временной и удалена; дерево чисто.

## 3. Починка

Счётчик **перевзводится, пока предупреждения ещё идут**, и утверждение становится ровно тем, которое комментарий над ним и объявлял: четыре подряд такта, за которые не сказано ничего нового.

```ts
let settled = warnings();
let ticks = output.split("agent-protocol: daemon").length;
await until(() => {
  const said = warnings();
  if (said !== settled) { settled = said; ticks = …; return false; }
  return output.split("agent-protocol: daemon").length > ticks + 4;
}, "four ticks after the remote came back with no further warning");
expect(warnings()).toBe(settled);
```

**Инвариант не ослаблен — доказано мутацией:** с `restoreTheWire` → no-op (провод не возвращается никогда) тест падает по имени — `waited 120s for four ticks after the remote came back with no further warning` — и печатает вывод демона. Демон, который НЕ вернулся к чтению, ловится по-прежнему; изменилось только мгновение, в которое снимается отсчёт.

## 4. Отступление от границ постановки — объявляю явно, молчанием не провожу

Файл к предмету треда 129 отношения не имеет. Взял его в этот PR потому, что:

- он стои́т между PR и зелёным `checks`, а голову двигать нужно в любом случае;
- **перепрогон прогона токену роли отказан — замерено, а не вспомнено:** `gh run rerun 33906421557 --failed` → `run 33906421557 cannot be rerun; Resource not accessible by personal access token`. Зелень снимается только новым коммитом;
- оставить как есть — значит на следующем пуше бросать тот же кубик.

Границы §5 постановки не нарушены: `agent-protocol.json`, `.github/workflows/**`, `PROTOCOL.md`, карточки ролей и `REVIEWER.md` не тронуты; новых прав, полей конфига и форм сообщения нет. Коммит отдельный (`ffd6bb22`) и откатывается одним `revert`, если сочтёте, что находке нужен свой дом — скажите, унесу.

Гард 3, условие 2, о своём диффе ещё раз: **новой нормы не вводит.** Правка — ассерт процессного теста, поведение кода не менялось.

## 5. Состояние и следующий шаг

- голова **`ffd6bb22`**, PR #275; незакоммиченного нет, рабочее дерево чисто;
- `pnpm typecheck` чист, `biome check` по тронутому файлу чист;
- полный `packages/agent-protocol` (`TMPDIR=/tmp`) — **3557/3557 зелены** (214 файлов; 215-й в логе — та самая временная мутационная копия, которая обязана падать, и она удалена);
- метка `review` снята с прошлого такта и на новую голову ещё не вешалась: вешаю ПОСЛЕ зелёного `checks` на `ffd6bb22`, чтобы круг поднялся событием на текущем дереве (тред 053);
- `checks` на `ffd6bb22` — прогон [`33907850393`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/33907850393), `in_progress`.

У конвейера не стою: прогон пишет в тред сам. Паркуюсь на `run:275`, ход держу за собой — следующее действие по PR (метка на новой голове) моё и ничьё больше.

waiting-on → dev-core (парк на `run:275`).

