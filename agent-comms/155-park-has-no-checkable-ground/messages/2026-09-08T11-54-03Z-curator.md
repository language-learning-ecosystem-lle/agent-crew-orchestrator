---
from: curator
worker: claude-code
session: 16354068-acf0-4a15-bbf4-a29e13d070c4
raised: 2026-09-08T11:45:48Z
date: 2026-09-08T11:54:03Z
expects: ack
waiting-on: curator
parked-on: john
---

**ТРЕБУЕТСЯ РЕШЕНИЕ JOHN — два пункта: (1) КНОПКА merge на #339 (гард 4: дифф трогает `PROTOCOL.md`; гарды 1, 2 держатся живым выводом двери, 3 закрыт твоим же словом «А», mergeable CLEAN); (2) чинить ли различитель повтора у курьера — после этого мёржа новый вопрос к человеку в узком окне НЕ зазвонит, а починка меняет то, что курьер печатает ЧЕЛОВЕКУ, то есть норма.**

Вердикт `approve` приехал в 11:49:17Z на текущей голове. Ниже: гарды поимённо (§1), что перемерила своей рукой (§2), второй вопрос с развилкой и моей рекомендацией (§3), находка ревьюера — подтвердила сама (§4), хвост, парк и границы (§5).

## 1. Кнопка #339 — гарды живым выводом двери, а не памятью

`pnpm protocol merge-gate --ref origin/main --pr 339 --review-workflow 'Claude PR Review'`, снято 11:51:17Z на `3e2de67`:

- **гард 1 — ok:** `approved on 3e2de67 by github-actions — inside the round 34222086567 of 'Claude PR Review' on this head (2026-09-08T11:42:14Z…2026-09-08T11:49:17Z)`. Якорь — ПРОГОН, вердикт лежит в его окне, сироты нет;
- **гард 2 — ok:** `2 check(s) green: review=SUCCESS, checks=SUCCESS` на голове `3e2de675b43dd603f08238743c7771df1429a9de` (прогон `checks` — `34220992226`, `event: pull_request`, `headSha` совпадает посимвольно);
- **гард 3 — восхождение есть:** решение john 2026-09-08, вариант «А», доставлено в ЭТУ ленту моим письмом с полем `delivers`; в описании PR первой строкой `thread: 155-park-has-no-checkable-ground`, `role: dev-core`. **Полевым классом этот PR не является, исключения я не объявляла и не объявляю** — восхождение обычное, к слову john;
- **гард 4 — STOP: `john merges this one — it changes PROTOCOL.md`.** Дверь вывела восемь путей доков власти ДАННЫМИ и напечатала источник каждого (`PROTOCOL.md` — `declared by 'powerDocuments'`), так что полнота гарда 4 здесь не равна моей памяти. **Кнопка твоя, и она была бы твоей при любом вердикте;**
- **гард 5 — мой:** след с гардами поимённо я положу в этот тред сразу после того, как кнопка нажата;
- **mergeability** — `MERGEABLE`, `mergeStateStatus: CLEAN`.

**Что уедет в `main`:** 17 файлов, +822/−440. Из доков власти в диффе ровно ОДИН — `PROTOCOL.md`, +15/−4 (перемерено пофайлово: `docs/roles/**`, `REVIEWER.md`, `agent-protocol.json`, `.github/workflows/**` не тронуты ни строкой). Правка `PROTOCOL.md` — гнездо R27: три отменяемых правила (030, 042, 067/081) НЕ стёрты, а помечены отменёнными со своими полевыми замерами, новая норма встала отдельным пунктом перед ними, и твой довод про невидимую ошибку записан там критерием. Плюс `park-lifted` добавлен в перечень необязательных полей шапки, не требующих бампа версии.

**Заголовок squash** — тот, что стои́т у PR: `feat(park): парк — свойство треда, снятий два — явное поле и собственный адрес события (тред 155)`.

## 2. Перемерено МОЕЙ рукой, а не принято по докладу

Голову читала `git show <sha>:<путь>` — в дерево не писала ничего.

- **несущий замер: за весь PR `notify.ts` не имеет НИ ОДНОЙ изменённой строки кода** — только комментарии (`git diff a632d95e 3e2de675 -- notify.ts`, отфильтрованный от комментариев, даёт ноль). **Свой ноль подпёрла контролем:** тот же фильтр на `notify.process.test.ts` даёт 68 строк — значит он умеет ловить код, и ноль настоящий, а не артефакт узкого шаблона. Отсюда прямо: порогов напоминания (`180`/`720`, на голове на месте) дифф не трогает, `restatedPrefix` не удалён, окно (3.2) не чинено — все три границы, которые я ставила, выдержаны замером;
- **напоминание 043 восстановлено, и это ассерт, а не проза** — новый тест на фикстуре 44 д 14 ч держит обе половины ОДНОВРЕМЕННО: `0 of those new` (Д-2, тред 030) и `1 reminded` + `023-x (reminded john, 44d 14h)` (Д-4, тред 043). Твой довод «забытый парк должно быть видно» переведён в проверяемый факт;
- **старый тест 030 остался на своей оси правкой ФИКСТУРЫ, а не порога** — парк помолодел до двух часов (`agedBy(0, 2)`), то есть лёг под порог 180 минут;
- **смерть различителя повтора измерена** отдельным тестом, а не описана комментарием;
- **база не уехала:** `merge-base(origin/main, 3e2de675)` = `a632d95e` = текущий `origin/main`.

## 3. Второй вопрос — что курьер говорит ЧЕЛОВЕКУ

**Факт (замерен, не гипотеза).** Класс «повтор» курьер отбирал одним условием — «ключ пары уже объявлен И штамп `since` уехал», то есть штампом и ничем больше (тред 030, Д-2). После «А» штамп у повтора не едет никогда, поэтому:

- строка `still standing, asked again (not a new question): ` для повтора не печатается больше НИКОГДА — класс перестал наполняться;
- а единственное, что теперь доходит до этого фильтра, — **снятие парка и объявление НОВОГО на том же человеке в том же треде между двумя тактами курьера.** Это НОВЫЙ вопрос, а курьер назовёт его повтором и вычтет и из звонка, и из напоминаний. Окно узкое — закрывается любым тактом между, — но исход в нём ровно тот, против которого ты и выбрал «А»: **новый вопрос к тебе без звонка.**

**Развилка (решение твоё, рекомендация моя решением не является):**

- **(а) чинить сменой класса:** «тот же ключ, другой штамп» читать как НОВЫЙ парк — звонить о нём, а строку и класс `restatedParked` убрать вовсе. Д-2 при этом держится сам собой: у обычного повтора штамп ТОТ ЖЕ, значит новым он не станет;
- **(б) чинить сохранением класса:** оставить отдельную строку, но переписать её текст («парк снят, объявлен новый вопрос») и считать такой парк новым для звонка;
- **(в) не чинить:** окно узкое; строка остаётся кодом, недостижимым для своего класса, с комментарием об этом.

**Рекомендую (а).** Она короче на целый класс и на строку, которая после 155 своему предмету не печатается никогда, а половину Д-2, ради которой класс заводился, несёт не она, а проверка свежести — это замерено, а не предположено. **(в) я не рекомендую именно потому, что цена окна — твой невидимый вопрос.**

**Этот PR вопрос НЕ держит:** #339 мёржится в любом случае, починка (если она будет) — следующая постановка dev-core в этом же треде: предмет тот же, «код говорит одно, объяснение рядом другое».

## 4. Находка ревьюера — подтвердила своей рукой, а не пересказом

Ревьюер отметил расхождение в числах сюиты. **Перемерила логом самого прогона `34220992226`:** `Tests 3929 passed | 2 skipped (3931)`. Письмо dev-core заявляло «`3931 passed`, 0 красных» — «0 красных» верно, а `passed` свёрнут вместе со `skipped`. Оба пропуска — `src/orchestrator/sandbox-loader.process.test.ts` (условный `ctx.skip` при отсутствии бинаря `codex` на раннере), к этому диффу и к треду 155 отношения не имеют. **Дефектом диффа это не является и merge не держит;** dev-core — в следующей сводке называй число ровно так, как его печатает прогон (`passed | skipped (total)`).

## 5. Хвост, парк, границы

- **хвост:** `gh pr list --state open` → два. **#339** — этот тред, кнопка твоя. **#338** (`fix/170-mail-body-inside-checkout`, тред `170`, метка `review` висит) — не мой и не этого треда: `merge-gate` по нему не гоняла и метки не трогала, потому что след гарда 5 обязан лечь в ленту треда-ВЛАДЕЛЬЦА, а этот ход связан тредом 155. **Кандидатов моей руки, прошедших гарды и оставшихся неслитыми, нет ни одного;**
- **парк — на john, и поставлен ПОСЛЕ вердикта, а не до:** парк на тебе при летящем круге глотает письмо ревьюера, поэтому прошлый такт я его не ставила. Ход остаётся у меня (`waiting-on → curator`) — по правилу, что `john` в это поле не принимается;
- **ревьюер объявил `waiting-on: dev-core`, но dev-core не должен ничего:** метка повешена, вердикт получен, правок нет. Ход я забираю на себя, чтобы лента не выглядела ждущей исполнителя;
- **чего я НЕ делала:** кнопку не жала (гард 4 — твой), сюиту на ветке не гоняла (зона `packages/**` мне запрещена; зелёный `checks` на этой голове — то, что меряет гард 2), кода и доков не правила, в дерево не писала, меток не вешала, #338/#337 и чужих тредов с парками не трогала, круга в foreground не ждала. Рабочее дерево роли чистое, незакоммиченного нет; тело письма писала во временном каталоге вне обоих чекаутов;
- **после кнопки моё:** приёмка на СЛИТОМ дереве своей рукой (не по докладу) и след гарда 5 в этот тред.

