---
from: curator
worker: claude-code
session: 8126ae97-2703-4d0b-8928-fc2b18263506
raised: 2026-09-13T11:29:16Z
date: 2026-09-13T11:35:14Z
expects: none
waiting-on: curator
parked-on: run:388
---

**Слово john 13.09 записано в канон, и кнопка на этом PR — твоя: #388 трогает `PROTOCOL.md`, то есть гард 4 STOP по построению. Кода, тестов и конфига дифф не касается; своей рукой я его не мёржу и не буду.**

## 1. Что в диффе — три файла, два коммита

- **`PROTOCOL.md`** — новый раздел «Словарь оснований парковки закрыт тремя формами до перемера (решение john 2026-09-13, тред `155-park-has-no-checkable-ground`)»: обе развилки с твоим словом, названная граница механизма (контур-потребитель ≥26 и деньги/учётки/квота 10 — ящик их не спрашивает ничем сегодняшним, и поле для них завело бы проверку, которая никогда не выполняется), кандидат на перемер (рука владельца решения на ящике, ≥20), главный факт перечисления (не меньше 146 из 303 парковок на человека ждут ФАКТА, а не мнения) и поправка к форме следующего замера — живые парковки отдельно от исторических;
- **`docs/journal/curator.md`** — попутным диффом полевой замер роли: счёт по ШАПКАМ, а не грепом по телу (831 против 826); «не меньше» вместо «ровно»; возраст поля отдельно от неупотребления; и отрицательный результат, найденный на себе (форма «жду вердикта круга» закрыла бы дырку, которой нет: парк ставится ПОСЛЕ вердикта);
- **`docs/protocol-reference.md`** — вторым коммитом одна врезка под тремя формами основания: «четвёртой формы пока нет, и это РЕШЕНИЕ, а не незавершённость», со ссылкой на норму. Без неё dev-core, читающий справку о формах, видит словарь из трёх штук и ни слова о том, почему их три.

Новых полей, ключей конфига, форм сообщения, прав и шагов маршрута дифф не вводит — он записывает уже сказанное. Парсер `park-ground` не тронут: три формы в коде (`frozen:<роль>×<тред>`, `no-delivers-since:<тред>`, `until-pr-merged:<n>`) сверены чтением `packages/agent-protocol/src/thread/park-ground.ts` ДО того, как они названы в каноне.

## 2. Почему перемер записан В КАНОН, а не в хвост этого письма

Назначенная на неделю вперёд работа, живущая только в прозе письма, протухает молча: тред уедет на сорок сообщений, и «перемерить три формы» не вспомнит никто. Поэтому срок стои́т в самом разделе — **перемер не раньше 2026-09-20** (неделя от твоего слова 13.09), и там же сказано, против чего мерить: против пропорции §2, а не против числа парковок. Если неделю ты считаешь от другой точки — скажи на кнопке, поправлю строкой.

## 3. Гарды: чего на этом PR ещё НЕТ

Голова — `9de271ba4e51c9e08dcbb9b4de64e2c6d588ecd8` полным SHA. Прогон `checks` на предыдущей голове (`8d689a666…`, id `34754757001`) был `in_progress` в момент второго пуша; на текущей голове прогон только заводится. **Метку `review` вешаю СЛЕДУЮЩИМ тактом — после зелёного `checks` на этой самой голове**, как требует маршрут; вердикт круга и кнопка john — за ней. Гард 5 (след) ляжет в эту же ленту, когда кнопка будет нажата.

## 4. Чего этот ход не делал

Merge не жала ни по одному PR. Открытые PR ящика — `#379`, `#383`, `#384`, `#386`, `#387` — принадлежат чужим тредам (`190`, `188`, `189`, `180`, `177`), и след гарда 5 по ним обязан лечь в ленты ИХ тредов; этот ход связан тредом `155`. Чужих парков, веток и тредов не трогала. Вся моя работа запушена в `curator/155-park-ground-vocabulary-canon`, незакоммиченного в рабочем дереве роли нет.

Паркуюсь на `run:388` — жду, чтобы отчитался прогон на текущей голове; лифт снимет его исход. waiting-on → curator.

