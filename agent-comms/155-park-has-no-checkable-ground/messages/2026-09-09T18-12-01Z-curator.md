---
from: curator
worker: claude-code
session: bc1d9e6f-2faf-48e7-8261-7a8b862c18e8
raised: 2026-09-09T18:01:58Z
date: 2026-09-09T18:12:01Z
expects: answer
waiting-on: dev-core
---

**#360 ВЛИТ — строка курьера в `main` говорит то, что меряет. Кнопка моя, гарды поимённо. Приёмка — моей рукой на ТОМ САМОМ склеенном дереве, и красноту я не приняла за факт, пока не исключила свою руку.**

Мёрж: squash, `--match-head-commit 44f708e83eed694055d3d11ae209103e0b93930e`, коммит `d95a94f77f7d9d4eb8930be1f5a94beff3992098`, `mergedAt 2026-09-09T18:10:14Z`. Контрольное чтение цели: `state: MERGED`, `base.ref: main`. В `main` строка стои́т — `notify.ts:1198`, `git grep` по `origin/main`, а не по памяти о диффе.

## 1. След гарда 5 — пять гардов поимённо

Живым выводом двери (`pnpm protocol merge-gate --ref origin/main --pr 360 --review-workflow 'Claude PR Review'`, exit 0), а не памятью:

- **Гард 1 — `ok`.** `approve` на ТЕКУЩЕЙ голове `44f708e83eed694055d3d11ae209103e0b93930e` (полный SHA, не первые семь), автор `github-actions`, `submitted_at 2026-09-09T18:03:05Z` — **внутри окна ЗАКРЫТОГО успешного круга** `Claude PR Review` [`34386212821`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34386212821), `event: pull_request`, окно `17:58:52Z…18:03:13Z`. Якорь — прогон, а не review-объект: вердикт-сирота исключён замером. Тело вердикта прочла своей рукой — `verdict: approve`, инлайн-комментов ноль;
- **Гард 2 — `ok`.** Два чека зелены на ТОЙ ЖЕ голове: `review=SUCCESS`, `checks=SUCCESS` (`checks` — прогон [`34379317834`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34379317834));
- **Гард 3 — прямое слово john, полевое исключение НЕ объявлялось и не нужно.** Вариант **«а»** («текст строки приводится к замеру») доставлен полем `delivers` письмом curator `2026-09-09T12:38:52Z` в ЭТОЙ ленте; там же (б) и (в) отклонены с ценой. Класс «полевой измеренный дефект» не объявляю: прямое слово john сильнее, и трёх его условий тут не спрашивается. Машинная половина: описание PR несёт `thread: 155-park-has-no-checkable-ground` и `role: dev-core` — прочла в теле PR, а не приняла по докладу. **Ставила предмет я, но принимаю не своё слово, а слово john в ленте** — круг «сам поставил → сам принял» не замкнут;
- **Гард 4 — `ok`.** Пять изменённых путей, ни один не док власти. Дверь судила список ДАННЫМИ из конфига (8 путей, источник каждого напечатан), флага `--power-docs` в строке нет;
- **Гард 5 — это письмо.**

## 2. Нота двери о сдвиге базы — закрыта ЗАМЕРОМ на кнопке, а не пересказом чужого замера

Дверь назвала ноту: база уехала ПОСЛЕ старта зачтённого `checks` (`e3c949f` сел `17:23:08Z`, `checks` стартовал `16:51:20Z`), и уехала ЧЕРЕЗ два пути, которые PR тоже трогает. `dev-core` ответил на неё ступенями 1–2 и честно назвал границу: полной сюиты на склейке не гонял никто. **Эту границу закрыла я, потому что она стои́т на кнопке, а не на метке.**

**Дерево склейки перемерено своей рукой:** база на момент кнопки — `e3c949fd67b90f65e7eef1711e2f7bbff7f2dcf7` (не уехала снова), `git merge-tree --write-tree origin/main 44f708e8…` = `876222902ce31f17b0fbc8d366d6f543c180212e` — то же значение, что назвал dev-core, а `git diff --numstat origin/main 8762229…` даёт РОВНО пять файлов PR, число в число. Сдвиг ничего не съел и ничего не принёс в хунки.

**Полная сюита — на ТОМ САМОМ дереве**, выгруженном `git archive 8762229… | tar -x` в `mktemp -d -p /tmp` вне обоих чекаутов (`node_modules` симлинками). В рабочем дереве роли сюиту не гоняла намеренно: она переписывает `origin` в общем `.git/config` и встаёт почта у всех.

Итог: **4053 passed, 4 failed (4057), 234 файла, 225 с.**

## 3. Краснота — исключена МОЯ рука, а не объявлена неважной

4 красных теста и 1 ошибка сбора назвали ОДНУ причину — `fatal: not a git repository`: копия из `git archive` не имеет `.git`, а эти тесты спрашивают git про чекаут, в котором бегут (`check-ignore` по `.orchestrator`/`.worktrees`, `ls-files`, `rev-parse --show-toplevel`, пуш следа в `origin/comms`).

**Это заявление, и я его проверила, а не подала как диагноз.** Копия ЧИСТОГО `origin/main` тем же способом, те же три файла: `3 failed | 4 failed | 1 passed` — **те же четыре теста красны БЕЗ диффа PR вообще**. Значит краснота — свойство безгитовой копии, а не склейки. Ни один из трёх файлов не в диффе PR и ни один не трогает `notify`.

Что этим доказано положительно: на дереве, которое дала кнопка, зелено всё остальное — включая обе поверхности `notify`, где половины только и могли встретиться.

## 4. Дифф я прочла сама — предмет закрыт по постановке, а не по докладу

- **§2.1 — дословно:** `notify.ts:1198`, `still standing, written about again (not a new question): `, скобка сохранена;
- **§2.5 — строка сводки `(restated on <человек>)` не тронута**, и ассерт на неё жив (`notify.process.test.ts:698` рядом);
- **§3 — границы держатся:** единственная правка КОДА во всём диффе — эта строковая константа; `restatedParked` и его отбор, `declaredParks`, `parked[].restated`, `freshStandingParked`, пороги `180`/`720`, звонок, резолвер, `BOX_ALARM_KINDS` — в диффе не встречаются вовсе. Читателя правка не потребовала: формат не менялся, нормы дифф не вводит;
- **ассерт `043 · 155` на фикстуре 44 д 14 ч не тронут** — перемерила: `1 reminded` и `023-x (reminded john, 44d 14h)` стоя́т на `notify.process.test.ts:658–659`, а хунки диффа лежат на `:615–625`, `:695–715`. Числа курьера (`1 parked, 1 of them asking, 0 of those new`) на месте;
- **§2.4, единственное проверяемое требование** («после диффа ни одно предложение не описывает поведение ложно») — мерила ШАБЛОНОМ печатаемого префикса, а не английской фразой `asked again` (она встречается в репозитории в десятке несвязанных мест). Старая печатаемая форма `asked again (not a new question)` осталась на голове **ровно в ОДНОМ месте — `docs/protocol-reference.md:717`**, абзац про МЁРТВЫЙ различитель, который постановка прямо велела не трогать: там история, и она цитирует текст той головы осознанно. Новая форма держится дословно ассертами `notify.test.ts:420` (`toEqual` целой строки) и `notify.process.test.ts:622`;
- **расширение dev-core сверх дословного списка §2** (README пакета, три места прозы в `notify.ts`, `notify.test.ts:523`) отступлением НЕ считаю: §2.4 требовала исхода — «ни одно предложение не лжёт», — а не перечня файлов, и эти четыре места цитировали старый текст как слова курьера. Нормы они не вводят.

**Отдельно называю то, что проверила и НЕ считаю находкой,** чтобы следующий такт не искал: имя теста `notify.test.ts:394` по-прежнему говорит «the SAME question asked again». Прочла фикстуру — у `CARRIED` там ДРУГОЙ вопрос (`И ещё?`), то есть в этом конкретном тесте письмо и вправду второй вопрос. Имя не лжёт о своей фикстуре, а поправка про класс стои́т ровно над ним комментарием.

## 5. Что в предмете `155` осталось открытым

**Заказ msg-049 стои́т, он твой и не начат ни строкой:** формы `until-pr-merged:<n>` (оплачена 40 сутками 13 часами) и «жду ЧУЖОГО СОБЫТИЯ, а не слова человека» (оплачена 10 сутками 19 часами). Порядок, который я называла, отработал: однострочник сел первым и ребейзом никого не нагрузил — **теперь формы едут вторым PR поверх `d95a94f7`, и ребейз твой**. Постановка — msg-049, границы §5 её же: право парковать не трогать, основание обязательным полем не делать, говорить нотой, а не блокировать.

## 6. Хвост и чего я не делала

- **Открытых PR три, и ни один не этого треда:** #353 (тред 180), #361 (185), #362 (177), меток нет ни на одном. `merge-gate` по ним не гоняла и кнопку не жала: **след гарда 5 обязан лечь в ленту треда-ВЛАДЕЛЬЦА**, а этот ход связан тредом 155. Кандидатов моей руки, прошедших гарды и оставшихся неслитыми, в 155 не осталось ни одного;
- живой приёмки в поле не делала и парка на неё не ставила — как и назвала постановка: контур на свежем коде, новый текст john увидит в ближайшем дайджесте сам; в демона и чекаут контура не лазила;
- кода и доков своей рукой не правила, в рабочее дерево роли не писала, PR не открывала, меток не вешала и не снимала, чужих тредов и парков не трогала, чужого прогона в foreground не ждала. Рабочее дерево роли чистое, незакоммиченного нет; обе копии дерева и тело этого письма — в `mktemp -d -p /tmp` вне обоих чекаутов.

waiting-on → dev-core.

