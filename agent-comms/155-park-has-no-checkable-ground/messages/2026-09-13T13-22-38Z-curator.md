---
from: curator
worker: claude-code
session: 31c3ec61-8f15-4cf5-b881-0a20e2687d39
raised: 2026-09-13T13:13:38Z
date: 2026-09-13T13:22:38Z
expects: none
waiting-on: curator
priority: low
---

**Круг по #388 СГОРЕЛ на лимите аккаунта, а не на содержании: 52 секунды, `api_error_status: 429`, вердикт не сформирован — и красный прогон `review` на голове теперь роняет гард 2, то есть кнопки нет ни у кого, пока не пройдёт ОДИН успешный круг. Ресет назван самой сессией — `13:40Z`; до него перезапуск сожжёт вторую попытку впустую. Маршрут до круга я закрыла весь: зелёный `checks`, перемер сливаемости, метка на ту же голову. Ход передаю: перевесить метку после `13:40Z` — единственное оставшееся действие.**

## 1. Что сделано этим ходом — по шагам, каждый замером на ЭТОЙ голове

1. **Зелёный `checks` на текущей голове:** прогон [`34759064637`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34759064637), `conclusion: success`, `headSha: 6874550f78ba71f53b9bce82aabdc69bf0330e67` — полным SHA, из вывода самого прогона. Окно `13:09:54Z → 13:18:03Z`;
2. **Перемер сливаемости ПЕРЕД меткой** (как я и записала следующей сессии прошлым письмом): `mergeable: MERGEABLE`, `mergeStateStatus: CLEAN`, `labels: []`, `origin/main = e22c5fa1679dfd956aa6862c4292128c1a990eba` — база за весь ход не двигалась, третьего ребейза не потребовалось;
3. **Метка `review` на ТУ ЖЕ голову**, `13:18:48Z`, и контрольное чтение после записи (ответ инструмента ≠ факт): `head=6874550f78…` не сдвинулась, `labels=[review]`;
4. **Круг поднялся событием навешивания** — `Claude PR Review`, прогон [`34759487215`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34759487215), `event: pull_request`, старт `13:18:49Z`, на секунду позже метки;
5. **и умер в `13:19:45Z`, прожив 52 секунды.** Причина машинная и не про PR — её называет письмо `reviewer-pr` в этой ленте (`13:19:39Z`): `api_error_status: 429`, `terminal_reason: api_error`, дословная строка сессии «You've hit your session limit · resets 1:40pm (UTC)». Три доставки вердикта не сработали ни одна, потому что `verdict.md` не создан вовсе: **это не суждение о PR, а его отсутствие.**

**Состояние гардов после этого — читаю честно, не в свою пользу:** гард 1 нечем закрывать (вердикта нет), а гард 2 **красный**: на голове висит `review=FAILURE`, и лечит его только один успешный круг. Твоей кнопке #388 сейчас недоступен не только гард 4 — он STOP по построению и так, — но и два машинных.

## 2. Находка хода: мой парк `run:388` был мёртв 99 минут — и это НОРМА механизма, а не авария

Прошлым письмом (`13:11:45Z`, §4) я записала: «мой новый парк молод и держит». **Это неверно, и я называю ошибку прежде выводов из неё.** Замер журнала ящика — шаблон и результат:

```
grep -a -c 'the park on the round of PR #388 HAS GONE STALE' .orchestrator/daemon.log   →  103
grep -a -o '… declared [^,]*, standing [0-9]* min' | sort | uniq -c                     →  все 103 строки:
                                       declared 2026-09-13T11:35:14Z, возраст от 80 до 99 мин
```

**Оба моих «честных переноса» — `13:03:26Z` и `13:11:45Z` — парк не обновили.** Ящик все 103 такта читал один парк, объявленный в `11:35:14Z`, то есть пробивший 30-минутный потолок ещё около `12:05Z`; подъёмы `13:04:07Z` и `13:13:38Z` — оба по нему, и второй поднял эту сессию.

**Причина названа чтением кода, а не гипотезой** (`packages/agent-protocol/src/thread/thread.ts`, `standingParkOf` → `declaredAt`): walk идёт с конца ленты до первого `parked-on:` и затем шагает НАЗАД по письмам с тем же значением. Повтор `--parked-on run:388` — **носитель** стоящего парка: `since`, вопрос, `holder` и `park-ground` остаются письма-объявителя, потолок тикает от него.

**И это не дефект, а твоё же решение по ЭТОМУ треду** — вариант «А» от 08.09, §3.2 постановки; комментарий в коде называет его дословно: «AND IT STANDS WHERE IT WAS DECLARED, not where it was last mentioned». Раньше честное повторение переобъявляло парк и показывало человеку чужой вопрос и неверный возраст. Новый парк начинает только письмо, которое гасит по имени и объявляет в одном вдохе (`--park-lifted run:N` вместе с `--parked-on run:N`; правка треда 188 от 11.09).

**Что из этого следует для предмета треда — ровно одно, и оно в пользу принятого решения.** «Парк переживает свой повод» сработал здесь в дешёвую сторону: потолок снял заморозку и поднимал роль каждый такт вместо того, чтобы держать пару 30 минут над PR, готовым к метке. Ценой стали 103 строки `HAS GONE STALE` в журнале и один лишний подъём — **видимая** ошибка, ровно та, на которую вариант «А» и менялся. Постановки из этого не делаю; в память роли поправка записана (прежняя заметка утверждала обратное и была неверна).

## 3. Парк ставить НЕЧЕМ, и это тоже замерено дверью

Письмо это я сперва отправляла с `--park-lifted run:388` и `--parked-on run:388` в одной шапке (та самая форма из §2, дающая отсчёт с нуля). **Дверь отказала, и отказ верен:** «every run on head `6874550f7` of PR #388 has ALREADY FINISHED (2 runs, none queued or in progress) … the outcome this park waits for has already happened». К моменту отправки круг уже сгорел — парк ждал бы события, лежащего в ленте ПОЗАДИ него.

Мёртвый парк `11:35:14Z` при этом уже снят — не моей рукой, а курьером ревьюера: его письмо пришло с `--park-lifted run:388` (в логе прогона: «На адресате '155-…' стои́т парк 'run:388' — письмо идёт с '--park-lifted run:388'»). Тред сейчас непаркован.

**Поэтому вместо парка — единственный законный тормоз, который у меня есть: понижаю приоритет СВОЕГО треда до `low` до ресета.** Причина названа фактом, а не удобством: до `13:40Z` тред двигаться не может ничем — перезапуск круга раньше сожжёт второй круг за ничто (решение john о ручном перезапуске, тред 029), а других действий в предмете не осталось. Подъём до `13:40Z` был бы пустым, а пустые подъёмы — это цена роли. **Следующая сессия приоритет возвращает в `high` тем же письмом, которым вешает метку.**

Ждать ресета в foreground не стала намеренно: 19 минут — это удержанный слот при полном ящике (3 из 3 пар заняты, 14 кандидатов в очереди).

## 4. Чего этот ход не делал

- **Кнопку не жала ни по одному PR.** #388 — гард 4 STOP по построению (`PROTOCOL.md` в диффе), кнопка твоя; открытые #389 (тред 187), #386 (180), #384 (189), #383 (188), #379 (190) принадлежат чужим тредам, и след гарда 5 по ним обязан лечь в ленты ИХ тредов — этот ход связан тредом `155`;
- `merge-gate` по #388 не гоняла: вердикта нет, и гард 2 красный — спрашивать нечего;
- ветку не трогала: третий ребейз не понадобился, `origin/main` весь ход стои́т на `e22c5fa16`. Единственный оставшийся заряд по общему хвосту журнала роли — #389 (тред 187), он открыт;
- кода, тестов и конфига не касалась; сюиту в рабочем дереве роли не гоняла; чужих веток, тредов, парков и меток не трогала; чужого прогона в foreground не ждала; тело письма — в `mktemp -d` вне обоих чекаутов;
- **рабочее дерево роли ЧИСТОЕ, незакоммиченного нет**, вся работа запушена в `curator/155-park-ground-vocabulary-canon`.

## 5. Что делает следующая сессия по 155 — по шагам

1. **Сверяет время: строго ПОСЛЕ `13:40Z`** (`date -u`), иначе не делает ничего и передаёт ход обратно — ранний круг сгорит так же;
2. **перемеряет голову и сливаемость одной командой** (`gh pr view 388 --json headRefOid,mergeable,labels`): голова должна быть `6874550f78ba71f53b9bce82aabdc69bf0330e67`. Уехала база — сперва ребейз и зелёный `checks` на новой голове, и только потом метка;
3. **снимает метку `review` и вешает заново** — круг поднимает СОБЫТИЕ навешивания, а висящая метка события не даёт. **Коммент `@claude` не годится:** он поднимает круг с `headSha` дефолтной ветки, и гарду 1 такой прогон не засчитывается;
4. **тем же письмом возвращает приоритет треда в `high`** и паркуется на `run:388` — под ЖИВЫМ кругом парк законен и снимается своим адресом (пара `verdict:`+`pr: 388`);
5. по вердикту: **`approve`** — гонит `merge-gate --ref origin/main --pr 388 --review-workflow 'Claude PR Review'`, называет гарды 1, 2 и 4 по её выводу и зовёт тебя к кнопке письмом с `--expects ack` и парком на john (гард 4 STOP — своей рукой не жму ни при каком выводе двери); **`needs-fixes`** — чинит, пушит, ждёт зелёного `checks`, перевешивает метку.

waiting-on → curator.

