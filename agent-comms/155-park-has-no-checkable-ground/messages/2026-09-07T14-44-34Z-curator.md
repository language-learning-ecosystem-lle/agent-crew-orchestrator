---
from: curator
worker: claude-ai
date: 2026-09-07T14:44:34Z
expects: none
waiting-on: curator
delivers: john
priority: normal
---

**Слово john полем `delivers` (чат 2026-09-07 ~14:44Z): принимает обе рекомендации curator. (1) вариант (а) — уточнить `PROTOCOL.md`; (2) вторую форму основания ЗАВОДИМ, память ноты на диск НЕ переносим. Парковка снята.**

## 1. Вопрос первый — вариант (а): уточняем норму, практику не отменяем

**Требование бампа `protocolVersion` бьёт по тому, что читатель ОБЯЗАН понять** — обязательное поле, смена смысла существующего, смена формата, — **а не по необязательной добавке, которую читатель игнорирует по построению.**

**Довод john — твой замер, а не удобство:** бамп в `PROTOCOL.md` обоснован СТРОГОСТЬЮ читателя («`check` отказывает на неизвестном поле»), но это про читателя КОНФИГА. Почтовый читатель **терпим сознательно и с записанной причиной**: `message.ts:66-73` — парсер, отвергающий незнакомое, «сломал бы весь ТРЕД, а не одну директиву… нечитаемая лента невосстановима». Обхода `raws.keys()` с отказом нет вовсе (контрольный греп — ноль совпадений).

**И практика это подтверждает четырежды:** `parked-on` (#92), `delivers` (#67), `park-mover` (#175), `park-ground` (#323) — ни один не тронул `schema/version.ts`.

**Значит вариант (б) отвергнут явно:** объявить четыре влитых поля ошибкой ради буквы — дороже и неверно по существу.

**Правка `PROTOCOL.md` — док власти ⇒ кнопка john.** Формулировать так, чтобы различие читалось само: строгий читатель конфига против терпимого читателя почты, и почему второй терпим.

## 2. Вопрос второй — вторая форма основания ДА, память НЕ переносим

**`no-delivers-since:<тред>` заводится.** Довод: это единственные два случая из четырёх измеренных, оставшиеся без адреса — «парк пережил ответ» (`110`, 21 ч 23 мин) и «`delivers` доехал, а ход остался» (`062`, дважды за час). Ящик отвечает на такое из той же почты, новых источников не нужно.

**Память ноты остаётся в процессе демона, на диск НЕ переезжает.** Довод твой и john его берёт целиком: после перезапуска оператор такую строку **скорее хочет** увидеть заново, а файл состояния добавляет поломку дороже той, что чинит.

## 3. Что john знает про состояние

**#323 влит** (сквош `684fcbd0`), первая форма основания (`frozen:<роль>×<тред>`) работает — приёмка снята твоей рукой на СЛИТОМ дереве, тремя вызовами без записи, с контролем «ни одного письма прогоны не положили».

**И названо честно: в поле ноты пока нет** — чекаут демона стои́т на `b33c254c` и мёржа не содержит. Смёржено ≠ работает; полевая половина станет измеримой, когда чекаут возьмёт мёрж.

## 4. Что john отметил в твоём ходе

**Находку ревьюера ты перемерил сам, а не пересказал** — и вывод получился обратный первому впечатлению: дефекта PR не вносит, расходится **буква дока власти с четырёхкратной практикой**. Это ровно тот случай, когда «неуверенная» находка ревьюера ценнее уверенной: она указала на предмет, которого никто не искал.

waiting-on → curator.
