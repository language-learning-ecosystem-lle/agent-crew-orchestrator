---
from: curator
worker: claude-code
session: 2c16a03c-9630-4fbd-b8d1-0bd5b1a0d891
raised: 2026-09-13T13:00:57Z
date: 2026-09-13T13:03:26Z
expects: none
waiting-on: curator
parked-on: run:388
---

**Ветка перебазирована и запушена: конфликта больше нет.** Голова PR #388 — `5cfa61d4fa50ad2bdfb5de2ee62ba9a068635a88`, `mergeable: MERGEABLE`, `mergeStateStatus: UNSTABLE` (это идущий прогон, а не дефект). **Метку `review` этим ходом НЕ вешаю: она вешается после зелёного `checks` на ТОЙ ЖЕ голове, а прогон `34758702018` на ней сейчас `in_progress`.** Паркуюсь на нём и передаю ход.

## 1. Что сделано руками, по шагам твоего письма

1. **Перебазировано** на `origin/main` = `ef472dcf90739db775e25bb4d36a7e9cdbd24380` (за время жизни PR туда село пять чужих пакетов, последний — #393). Ребейз дал **один конфликт, в `docs/journal/curator.md`**, и он структурный, а не содержательный: обе стороны ДОПИСЫВАЮТ разделы в один и тот же хвост файла — в `main` приехали две мои же записи треда 177 (код выхода кнопки; фронтматтер судит звонок), в моей ветке лежит запись треда 155. **Разрешено объединением: обе стороны целиком, сначала записи 177, следом запись 155.** Ни строки не выброшено и ни строки не переписано;
2. **Запушено** `--force-with-lease` на старую голову `9de271ba4…` — то есть push отказал бы, если бы ветку за это время двинула чужая рука. Новые коммиты: `7a7e9041c` (канон) и `5cfa61d4f` (справка);
3. **Зелёный `checks` на новой голове** — ждёт прогона `34758702018`, он и есть мой парк;
4. **метка и круг** — следующим тактом, по исходу прогона.

## 2. Чем проверено, что «ничего по существу не изменилось»

Не прозой, а сравнением диффа ДО и ПОСЛЕ ребейза против своей базы:

- до: `PROTOCOL.md` +29, `docs/journal/curator.md` +21, `docs/protocol-reference.md` +11 — **61 вставка, 0 удалений**;
- после: те же три файла, те же числа — **61 вставка, 0 удалений**.

Числа сошлись **в том числе по файлу конфликта** (+21 к журналу с обеих сторон), то есть разрешение не съело ни моей записи, ни чужой: чужие две записи 177 в дифф против новой базы не входят вовсе — они уже в `main`. Тело канона перечитано после ребейза: раздел «Словарь оснований парковки закрыт тремя формами до перемера» и врезка справки на месте дословно.

## 3. О цене класса — моя ставка, а не постановка

Твой §2 (второй PR за день со сдвигом базы, четвёртый случай класса) на моей стороне подтверждается замером этого хода: **база уехала на ПЯТЬ чужих мержей за ~2,5 часа, а конфликт дал ровно один файл — общий хвост журнала роли,** куда дописывают и мои собственные PR из других тредов. То есть у сегодняшнего класса два разных слагаемых, и лечатся они разным:

- **общий хвост файла** (`docs/journal/curator.md`, `docs/protocol-reference.md`) — конфликт гарантирован при любом параллельном дописывании, и от потолка пар он не зависит;
- **сдвиг базы без конфликта** — дерево сходится, но круг ревью всё равно нужен заново, и вот это уже прямо цена параллельности.

Постановки не делаю и предмета не завожу: ты сказал, что сегодня только называешь. Если решишь заводить — скажи, и я поставлю его с перечислением за сутки (сколько сдвигов базы, сколько из них с конфликтом, сколько лишних кругов), а не с образцом.

## 4. Хвост

- **Открытых PR семь: #394 (тред 177), #389 (187), #388 (155, мой), #386 (180), #384 (189), #383 (188), #379 (190).** Кнопку не жала ни по одному: след гарда 5 обязан лечь в ленту треда-ВЛАДЕЛЬЦА, а этот ход связан тредом `155`. Мой единственный кандидат здесь — #388, и он гард 4 STOP по построению (`PROTOCOL.md`), то есть кнопка твоя и своей рукой я его не мёржу;
- **`merge-gate` по #388 не гоняла** — до зелёного `checks` и круга ей нечего спрашивать, а гард 4 её вывода не переживёт в любом случае;
- кода, тестов и конфига не касалась; чужих веток, тредов, парков и меток не трогала; меток не вешала и не снимала; чужого прогона в foreground не ждала; сюиту в рабочем дереве роли не гоняла (она переписывает `origin` в общем `.git/config`); тело этого письма — в `mktemp -d -p /tmp` вне обоих чекаутов;
- **рабочее дерево роли ЧИСТОЕ, незакоммиченного нет**, вся работа в `curator/155-park-ground-vocabulary-canon`.

**Что делает следующая сессия по 155:** читает исход прогона `34758702018`; зелёный — вешает `review` на голову `5cfa61d4f…` (ту же, что мерил прогон), передаёт ход и паркуется за кругом; красный — чинит и не вешает ничего. Кнопка на #388 в любом случае john.

Паркуюсь на `run:388`. Основания (`park-ground`) у парка нет сознательно: ждут отчёта идущего прогона, а формы для этого мы этим же тредом решили НЕ заводить — её работу и делает сам `run:<n>`. waiting-on → curator.

