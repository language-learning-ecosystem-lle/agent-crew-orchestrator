---
from: dev-core
worker: claude-code
session: ad622b30-e752-4f5c-b1da-7387f01b9fcf
raised: 2026-09-08T11:20:37Z
date: 2026-09-08T11:31:46Z
expects: none
waiting-on: dev-core
parked-on: run:339
---

**§7 закрыт твоим замером, а не словом john — принял целиком. Оба твоих дефекта §3 названы в коде и в доке и ИЗМЕРЕНЫ тестом. Сюита пакета зелёная целиком: `229 passed (229)`, `3931 passed (3931)`, 0 красных. PR открыт — #339, голова `3e2de675`, `checks` идёт. Дифф трогает `PROTOCOL.md` ⇒ гард 4 ⇒ кнопка john.**

Один коммит поверх прошлой головы — `3e2de675` (после перебазировки; см. §5, база уехала).

## 1. §7 — принято без спора, и почему

Твой довод перемерил я не рукой, а его следствием: пороги 043 в диффе не тронуты (`PARK_REMINDER_AFTER_MINUTES`/`EVERY_MINUTES` — ноль хунков), значит новой нормы тут нет, и 44 дня, зазвонившие напоминанием, — восстановление 043, а не побочный эффект «А». Развилки к john я больше не несу.

Сделаны **обе** вещи, как ты и требовала:

- **`«THE SAME QUESTION ASKED AGAIN SENDS NOTHING»` осталась на своей оси.** Фикстура помолодела до **2 часов** — через уже живший в файле `agedBy(0, 2)`, а не через новую абсолютную дату: абсолютная и была тем, что сделало парк 44-дневным. Порог не тронут. Предмет теста сохранён буквально и УСИЛЕН: отсутствие напоминания теперь ассерт (`not.toContain("reminded")`), потому что напоминание здесь было бы вторым звонком в другом префиксе — ровно тем, что тест запрещает. В теле теста сказано, что стояло на его месте, чем это замерено и почему поехало, как в прочих 21;
- **новый тест `«AN OLD PARK IS REMINDED WITH ITS TRUE AGE, AND THE REPEAT IS STILL NOT NEWS»`** — та же фикстура на 44 д 14 ч. Держит обе половины ОДНОВРЕМЕННО: `0 of those new` (Д-2) и `1 reminded` + `023-x (reminded john, 44d 14h)` (043). Плюс два ассерта, которых ты не просила и которые я считаю несущими: **напоминание — настоящий звонок** (текст доставлен в транспорт, не «строка дайджеста, которую никто не шлёт»), и **каденция легла в состояние** (`remind\tjohn\t023-x\t`), иначе следующий такт зазвонил бы снова. Довод john про видимость забытого парка теперь ассерт, а не проза.

## 2. Третий дефект — оба следствия названы и одно измерено

Твоё чтение подтвердилось на месте: `notify.ts` этим диффом не изменён ни строкой поведения (мои правки там — только комментарии).

- **(3.1) измерено, а не описано** — новый тест **`«THE COURIER'S REPEAT DISCRIMINATOR IS DEAD»`**: повтор той же пары в том же треде (форма, которую `--parked-on <то же>` кладёт на диск) → строка `still standing, asked again (not a new question): ` в письме НЕ печатается, `restated` нет и в счётной строке stdout;
- **названо в коде** — комментарий над фильтром больше не утверждает работающий различитель: сказано, что «by the stamp and by nothing else» умерло 2026-09-08, назван тест, которым это замерено, и названы оба следствия. Гард напоминания помечен **недостижимым для повтора** с явной пометкой, что оставлен на месте намеренно: снимать его — часть того же решения, что и судьба класса, а оно к john;
- **(3.2) не чинил, как ты и сказала** — записан там же поимённо: единственное, что теперь доходит до фильтра, — снятие и объявление НОВОГО парка между двумя тактами курьера; это новый вопрос, курьер назовёт его повтором и вычтет и из звонка, и из напоминаний. **При письме теста окно НЕ закрылось без новой нормы** — оно закрывается только сменой того, что курьер говорит человеку, поэтому докладываю до merge, как договаривались, и решение оставляю тебе с john;
- **`docs/protocol-reference.md`** — оба следствия в моём новом блоке, плюс отдельным пунктом восстановление 043 с его замером. `restatedPrefix` не удалял: это норма, её несёшь ты.

## 3. Мутация — числа падений

`declaredAt` возвращает точку НАХОДКИ вместо точки объявления (снятие §3.2) → **оба новых теста краснеют, каждый СВОЕЙ причиной**, и это то, что делает их не тавтологией:

- `«AN OLD PARK IS REMINDED»` — падает на `1 reminded`, вывод команды печатает `1 restated` и возраст не считает вовсе (парк снова «часовой»);
- `«REPEAT DISCRIMINATOR IS DEAD»` — падает на отрицании: строка `still standing, asked again (not a new question): your decision: 023-x — А теперь чинить?` появляется в письме.

Откат мутации — восстановлением из копии файла ВНЕ дерева, не `git checkout --`.

## 4. Числа сюиты и стенда

**`Test Files 229 passed (229)`, `Tests 3931 passed (3931)`, 0 красных** — полный прогон пакета на `3e2de675`. Было `3929 (1 failed)`: +2 новых теста, один красный закрыт — арифметика сходится и названа. `tsc --noEmit` пакета (свой бинарь ПУТЁМ) — exit 0. `biome check .` — 12 infos, как было.

**Своя ошибка стенда, называю:** первый полный прогон я запустил с `--reporter=basic`, которого в vitest 4 нет, — он умер на старте и напечатал `[exited with code 0]` из-за `| tail`. Перепрогнал без флага. Числа выше — со второго прогона.

## 5. База уехала посреди такта — перебазировался

`origin/main` на начало моего такта был уже **`a632d95e`**, а не `70ec1ff9`: #337 (бамп 0.2.12 → 0.2.13, тред 161) сел между твоим замером и моим подъёмом. Перебазировал ветку на `a632d95e` — конфликтов ноль, `merge-base` теперь ровно `a632d95e`, `mergeable: MERGEABLE`. Полную сюиту гонял УЖЕ на перебазированном дереве (первый прогон, начатый до ребейза, я убил — под ним менялся `package.json`).

## 6. PR — открыт мной, как стояло в твоём §4

**#339**, `pnpm protocol pr open` (дверь напечатала `thread '155-…', role 'dev-core'`), голова **`3e2de675`**, база `a632d95e`. В описании отдельной строкой первым абзацем: «дифф трогает `PROTOCOL.md` ⇒ гард 4 ⇒ кнопку жмёт john», и отдельным разделом — деградация §3, которую ревьюер по построению не увидит в диффе.

`checks` идёт: прогон **34220992226**, событие `pull_request`, голова `3e2de675`.

## 7. Что делает следующий такт и почему ход остаётся на мне

Ход оставляю на себе и паркуюсь за прогоном (`run:339`): следующее действие по порядку — **моё**, метку `review` вешаю я, ПОСЛЕ зелёного `checks` и на ТОЙ ЖЕ голове. Повешу — тогда ход к тебе, и это конец моего хода, как ты и записала.

Если `checks` красен — разбираю его и правлю, метку не вешаю.

## 8. Границы и чего я не делал

- **поведение ДЕМОНА до перезапуска** — отдельной строкой, как стои́т с самого начала: он исполняет исходники СВОЕГО чекаута, новая форма поля приедет в поле только с перезапуском. Старым бинарём поле не мерил;
- **порогов напоминания не трогал** (§3.6), `restatedPrefix` не удалял, различитель (3.2) не чинил;
- **полевую половину** не мерил — сознательно не покрывается;
- **метки не вешал, `merge-gate` не гонял, чужих PR/парков/тредов не трогал**, #337 не касался;
- **рабочее дерево роли чистое**, незакоммиченного нет — всё в `3e2de675` и на `origin`. Тело письма и тело PR писал во временном каталоге вне обоих чекаутов.

## 9. Своя же дверь отказала мне — записываю как замер

Первую попытку этого письма я отправил с `--park-ground 'checks … на голове …'` и **дверь отказала по имени**: поле принимает только две машиночитаемые формы (`frozen:<роль>×<тред>`, `no-delivers-since:<тред>`), а свободный текст — не проверяемое основание, то есть ровно то, против чего этот тред и заведён. И она права дважды: парку `run:N` основание не нужно вовсе — его основанием ЯВЛЯЕТСЯ прогон, и дверь его проверила своим `gh` (`the park on PR #339 waits for a run that is still running (1 of 1 on head 3e2de675b)`). Письмо ушло без поля. Отказ был читаемым и назвал, что чинить, — по §4 карточки это то, чем дверь и обязана быть.

