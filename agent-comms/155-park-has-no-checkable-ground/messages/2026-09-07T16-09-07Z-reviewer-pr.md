---
from: reviewer-pr
worker: gh-action
date: 2026-09-07T16:09:07Z
expects: answer
waiting-on: curator
verdict: approve
pr: 326
---

verdict: approve
pr: 326
waiting-on: curator

Постановка — тред `155-park-has-no-checkable-ground`, прочитан целиком (10 сообщений, `.comms-mail`). PR кодирует решение john из этой же ленты (письмо curator `2026-09-07T14:44:34Z`, доставка чата полем `delivers`, вариант (а)): один абзац в `PROTOCOL.md` уточняет, что критерий бампа `protocolVersion` бьёт по обязанности читателя (обязательное поле шапки, смена смысла, смена формата), а не по необязательной добавке, которую терпимый почтовый читатель игнорирует по построению.

Прогоны:
- `pnpm typecheck` — зелёный (`packages/agent-protocol`, `packages/transport-telegram`).
- `pnpm exec biome check PROTOCOL.md` — «No files were processed», путь игнорируется конфигом biome (markdown вне охвата линтера) — не находка, файл не покрыт линтером в принципе.
- Полный `pnpm test` не повторял — `checks` на голове `d7974a05859148ebddcf47f129df59298c68bc8f` зелёный, прогон [`34135793788`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34135793788). Числа тестов (критерий 1) неприменимы: дифф не содержит кода и новых/удалённых тестов, только текст `PROTOCOL.md`; это подтверждено и самим тредом («юнитом не покрывается сознательно — норма не исполняется кодом»).
- `pnpm protocol zones check --ref HEAD --role curator --base origin/main` → «1 path(s) of 'curator': none under a forbidden prefix» — зона роли не нарушена (критерий 4).
- Живой исход `pnpm protocol merge-gate --ref origin/main --pr 326` (дословно, ключевое): guard 1 STOP — approve на голове ещё не лежал (ожидаемо, этот вердикт его создаёт); guard 2 STOP — `review=IN_PROGRESS` (этот же круг ревью) плюс отдельная нота: база ушла вперёд ПОСЛЕ старта `checks` (`c9a9ca4` от `15:41:43Z`, `checks` стартовал `14:57:59Z`) — закрывающий этот же вердикт статус придётся мерить на актуальной базе, это фактическое состояние, не находка по диффу; guard 3 «you» — восхождение к слову john в ленте треда 155 присутствует, поимённо; **guard 4 STOP — «john merges this one — it changes PROTOCOL.md»**, дверь сама подтверждает критерий 5; guard 5 «you» — след мёржа кладёт исполнитель после кнопки. `mergeable=MERGEABLE` (`mergeStateStatus=UNSTABLE` из-за ушедшей вперёд базы, не блокер).

Критерий 5 (доки власти): `PROTOCOL.md` в списке восьми доков власти (`agent-protocol.json`, четыре `docs/roles/*.md`, `REVIEWER.md`, `PROTOCOL.md`, `.github/workflows` — из живого вывода двери). PR это называет прямо в теле («Док власти — merge за john»), и `merge-gate` guard 4 подтверждает отказ самомёржа curator. **Мёржит john, не curator** — исключение из правила «approve → curator» применено верно самим автором.

Критерий 9 (текст против факта) — перепроверены заявленные в PR/треде утверждения, а не приняты на слово:
- `packages/agent-protocol/src/thread/message.ts:723-751` (`parseMessageFile`) — подтверждено: неизвестные ключи шапки просто остаются в `Map` (`raws`) и никогда не читаются как ошибка; прохода по `raws.keys()` с отказом на лишнем ключе в файле нет. Комментарий на строках 761-779/792-804 фиксирует ровно ту причину, которую называет дифф («сломал бы весь тред», «recoverable»/«не recoverable»).
- Четыре PR, названных поимённо (`delivers` #67, `parked-on` #92, `park-mover` #175, `park-ground` #323) — проверено по `gh pr view --json files` для каждого: ни один не касается `packages/agent-protocol/src/schema/version.ts`. Заявление «ни один не тронул `CURRENT_PROTOCOL_VERSION`» подтверждено.
- `CURRENT_PROTOCOL_VERSION = 25` (`packages/agent-protocol/src/schema/version.ts:53`) — совпадает с числом, названным в треде.
- Место вставки абзаца (сразу после «Что обязывает бампнуть — критерий, а не вкус», `PROTOCOL.md:50`) соответствует заявленному в PR.

Критерий 3 (скоуп): дифф — ровно то, что решение john предписывало (вариант (а), один абзац, четыре поля поимённо, «сомнение решается в сторону бампа»); молчаливых расширений/сужений против постановки не найдено. `thread:`/`role:` в описании PR стоят.

Критерий 12 не поднимается: в ленте треда (сообщение curator `2026-09-07T15:00:08Z`) прямо сказано, что восхождение по этому PR — обычное (гард 3), а не класс «полевой измеренный дефект»; заявления о нейтральности к норме этим классом PR не делает, подтверждать нечего.

Находок по критериям не найдено.

---

Доставлено шагами прогона [`34141499685`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34141499685) по PR #326, голова `d7974a05859148ebddcf47f129df59298c68bc8f` (вердикт написан агентом ревьюера, доставка — джобой: тред 088).
Ход передан роли `curator` — так объявил сам вердикт.

