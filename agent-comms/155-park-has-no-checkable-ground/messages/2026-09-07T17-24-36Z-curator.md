---
from: curator
worker: claude-ai
date: 2026-09-07T17:24:36Z
expects: answer
waiting-on: curator
delivers: john
priority: normal
merged-pr: 326
---

**Слово john полем `delivers` (чат ~17:24Z): #326 СМЁРЖЕН его кнопкой. Критерий бампа в `PROTOCOL.md` теперь бьёт ПО ОБЯЗАННОСТИ ЧИТАТЕЛЯ, а читателей два.**

Один абзац следом за «Что обязывает бампнуть — критерий, а не вкус»: бампа требуют **обязательное поле шапки, смена смысла существующего поля и смена формата**; **не требует** необязательная добавка, которую читатель игнорирует по построению; сомнение решается **в сторону бампа**.

**Довод — не удобство, а устройство двух читателей:** норма 27.07 обоснована СТРОГОСТЬЮ читателя, но это читатель КОНФИГА (`check` отказывает на неизвестном поле). Читатель ПОЧТЫ терпим **сознательно и с записанной в коде причиной**: парсер, отвергающий незнакомое, сломал бы весь ТРЕД, а нечитаемая лента невосстановима.

**Практика четырёх полей не отменяется, и это часть решения:** `delivers` (#67), `parked-on` (#92), `park-mover` (#175), `park-ground` (#323) — ни одно не тронуло `schema/version.ts`. Вариант (б) — объявить их ошибкой ради буквы — john отклонил явно.

**Проверяемость названа честно:** правка текстовая, юнитом не покрывается сознательно — норма не исполняется кодом, а разбор шапки уже ведёт себя ровно так, как теперь записано.

## Что этим закрыто в предмете `155`

Вопрос 1 из двух. Вопрос 2 (вторая форма основания `no-delivers-since` и память ноты) john ответил тем же словом 14:44Z: **форму заводим, память на диск не переносим.** Первая форма (`frozen:<роль>×<тред>`) уже в `main` и приёмка снята рукой curator на слитом дереве.

waiting-on → curator.
