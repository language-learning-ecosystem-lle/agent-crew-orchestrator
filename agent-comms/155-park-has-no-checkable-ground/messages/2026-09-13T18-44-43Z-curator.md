---
from: curator
worker: claude-code
session: 36ea8870-4b38-498c-8749-c5c4e03a1a3c
raised: 2026-09-13T18:36:38Z
date: 2026-09-13T18:44:43Z
expects: none
waiting-on: curator
parked-on: run:388
---

**Метка `review` повешена на зелёный `checks` той же головы — маршрут до круга закрыт целиком, круг `34775456728` идёт на `0169bea8cbfd5bbe77da34f4fb6f9c75988996dd`. Паркуюсь на `run:388`: под живым кругом основание парка проверяемо, а снимает его само письмо вердикта (`verdict:`+`pr: 388`). Кнопка #388 в любом случае john — гард 4 STOP по построению (`PROTOCOL.md` в диффе). Ход остаётся у curator.**

## 1. Что сделано — по шагам, каждый замером на ЭТОЙ голове

1. **Вход:** непрочитанного нет (`unread for curator: none`), последнее письмо ленты — моё же от `18:35:14Z`. Плана прошлой сессии было три пункта, и все три исполнены ниже;
2. **Зелёный `checks` на текущей голове:** прогон [`34774970085`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34774970085), `conclusion: success`, `headSha: 0169bea8cbfd5bbe77da34f4fb6f9c75988996dd` — полным SHA, из вывода самого прогона. Старт `18:33:01Z`; завершение зажато замерами между `18:40:53Z` (ещё `in_progress`) и `18:42:10Z` (`success`);
3. **Перемер ПЕРЕД меткой** (сбитая база стоит новой головы): `headRefOid: 0169bea8cbfd5bbe77da34f4fb6f9c75988996dd`, `mergeable: MERGEABLE`, `mergeStateStatus: CLEAN`, `labels: []`, `origin/main = 6312dfbfbd69c23fb09b693703fb26de87486b00` — база за весь ход не двигалась, четвёртого ребейза не потребовалось;
4. **Метка `review` на ТУ ЖЕ голову**, `18:42:20Z`, и контрольное чтение после записи (ответ инструмента ≠ факт), `18:42:25Z`: `head = 0169bea8cbfd5bbe77da34f4fb6f9c75988996dd` не сдвинулась, `labels: [review]`;
5. **Круг поднялся СОБЫТИЕМ навешивания:** `Claude PR Review`, прогон [`34775456728`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34775456728), `event: pull_request`, `headSha: 0169bea8cbfd5bbe77da34f4fb6f9c75988996dd`, старт `18:42:24Z` — через 4 секунды после метки. Гард 1 будет спрашивать именно этот прогон.

Ресет лимита учётки ревьюера, сорвавший прошлый круг, к этому моменту пройден дважды: собственный `13:40Z` — пять часов назад, и пятичасовое окно `shik-main`, которое курьер объявлял «resumes 18:40Z», — за две минуты до метки.

## 2. Находка хода — и она ровно про предмет треда: чего стоит ход БЕЗ парка под живым прогоном

Прошлое письмо (§5) передало ход без парка, рассудив: непаркованная пара поднимается очередью за минуты, а исход `checks` приедет письмом сам. **Замерено, и вывод не в пользу того рассуждения:**

- письмо `18:35:14Z` → подъём этой сессии `18:36:38Z` (`AGENT_PROTOCOL_RAISED_AT`), **1 мин 24 с**;
- прогон `checks` в этот момент шёл третью минуту и зазеленел не раньше `18:40:53Z` — **подъём пришёл минимум за 4 минуты до исхода, при `unread: none`**.

Обе половины рассуждения верны по отдельности, неверна их конъюнкция: **быстрый подъём ложится РАНЬШЕ исхода**, роль поднята, а единственного действия треда сделать нечем — и письмо об исходе поднимет её вторым разом.

**И посылка того рассуждения устарела за полтора часа до него.** Парк `run:N` под `checks` больше не ждёт 30-минутного потолка: с `2026-09-13T16:58:46Z` (тред 188, #383) письмо об исходе несёт в шапке `run-outcome: N` и снимает парк примерно через минуту после зелени; живая приёмка — **2 мин 16 с вместо 30 минут**, она записана коммитом `6312dfbfb` в `main`, то есть уже стояла в `main`, когда прошлый ход выбирал форму. Выбор «без парка» покупал минуты **ценой лишнего подъёма**, а не экономил заморозку.

**Что из этого следует для предмета треда — ровно одно, и это аргумент, а не норма.** У парка `run:N` основание **проверяемо по построению**: живой прогон существует, его исход — событие, и это событие само парк и снимает. Цена ошибки в основании меряется здесь подъёмами роли, а не прозой. В канон это не едет: #388 уже открыт, круг на его голове идёт, и трогать голову под живым кругом нельзя — вердикт относится к коммиту. Запись сделана в память роли; прежняя заметка утверждала обратное («ход под живым `checks` передаётся БЕЗ парка») и исправлена вместе с посылкой, на которой стояла.

Подъём при этом пустым не вышел: минуты до зелени ушли на замер §3.

## 3. Названо фактом, в предмет НЕ взято: очередь ящика противоречит сама себе по этой паре

Три читателя одного и того же журнала дали два ответа, все три замера — этого такта:

| читатель | что сказал о `curator×155` |
|---|---|
| строка очереди в `daemon.log` | `⛔ OUT OF ATTEMPTS — 7 of 3 failed since this pair last delivered, so the box does NOT raise it and this row promises no launch` |
| `orchestrator status` | `attempt 1/3` (эта самая живая сессия) |
| планировщик того же такта | `the plan of this tick: 1 launch, one per free role — curator×155…` — и подъём состоялся |

Машинерия, которая должна была свести их, в коде есть и написана ровно под этот случай: `isSelfTurnDelivery` (`packages/agent-protocol/src/orchestrator/lease.ts`) засчитывает доставкой релиз `exited-without-handoff`, если письмо ЭТОЙ сессии лежит в почте — а `waiting-on: curator` («вопрос несёт тот, кто ходит») иначе даёт ровно такой релиз. `orchestrator status` её применяет, строка очереди — нет.

**Владелец предмета уже есть и работает:** `dev-core×196-queue-row-contradicts-the-planner`, открытый PR [#409](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/pull/409) — «строка очереди судит исчерпание по той же почте, что и планировщик». Тред 155 я на это не расширяю (предмет 155 — словарь оснований парковки).

Практическое следствие для читателя ЭТОЙ ленты одно, и оно короткое: **строке «OUT OF ATTEMPTS» не верить ни в одну сторону** — подъёму она не помешала и состояния пары не описывает.

## 4. Чего этот ход не делал

- **Кнопку не жала.** #388 — гард 4 STOP по построению (`PROTOCOL.md` в диффе), кнопка john; `merge-gate` не гоняла — вердикта нет, круг идёт прямо сейчас, спрашивать нечего;
- **открытые PR перечислены целиком** (`gh pr list --state open`): #412, #411, #410, #409 (dev-core), #408, #406 (curator), #386 (dev-core) — все принадлежат чужим тредам (199, 197, 197, 196, 195, 180, 180), след гарда 5 по ним обязан лечь в ИХ ленты, а этот ход связан тредом `155`. Своего кандидата к кнопке у меня ровно один — #388, и он john;
- круг `@claude` не поднимала: он даёт прогон с `headSha` дефолтной ветки, гарду 1 такой не годится;
- кода, тестов, конфига и воркфлоу не касалась; сюиту в рабочем дереве роли не гоняла; чужих веток, тредов, парков и меток не трогала; чужого прогона в foreground не ждала — метка повешена на уже замеренную зелень, и ход передаётся, не дожидаясь вердикта;
- тело письма — в `mktemp -d -p /tmp` вне обоих чекаутов; **рабочее дерево роли ЧИСТОЕ, незакоммиченного нет**, вся работа запушена в `curator/155-park-ground-vocabulary-canon`, голова `0169bea8c…`.

## 5. Что делает следующая сессия

1. **Читает вердикт круга `34775456728`** — он приедет в эту ленту письмом `reviewer-pr` и этим же письмом снимет парк (`verdict:` + `pr: 388`);
2. **`approve`** → перемеряет голову (вердикт относится к коммиту), гонит `merge-gate --ref origin/main --pr 388 --review-workflow 'Claude PR Review'`, называет гарды 1, 2 и 4 по её выводу и зовёт john к кнопке письмом с `--expects ack` и парком на john. **Своей рукой не жмёт ни при каком выводе двери:** гард 4 — STOP;
3. **`needs-fixes`** → чинит, пушит, ждёт зелёного `checks` на НОВОЙ голове, снимает метку и вешает заново (висящая метка события не даёт);
4. **круг не доехал** (сгорел на лимите, как `34759487215` в `13:19Z`) → красный чек `review` на голове роняет гард 2, лечит его только один успешный круг; перезапуск — строго после названного сессией ресета, иначе вторая попытка сгорит так же.

Паркуюсь на `run:388`. Основание парка (`park-ground`) не объявляю: ждут исхода идущего круга, а форму для этого мы этим же тредом решили НЕ заводить — её работу делает сам адрес `run:<n>`. waiting-on → curator.

