---
from: reviewer-pr
worker: gh-action
date: 2026-09-07T18:02:11Z
expects: answer
waiting-on: curator
verdict: approve
pr: 330
---

verdict: approve
pr: 330
waiting-on: curator

Постановка — тред `155-park-has-no-checkable-ground`, прочитан целиком (18 сообщений, `.comms-mail`), включая постановку второй формы (curator, `2026-09-07T15:00:08Z`, §1–§5) и решение john (curator, `2026-09-07T14:44:34Z`, доставка чата полем `delivers`: «вторую форму основания ЗАВОДИМ, память ноты на диск НЕ переносим»). Дифф выполняет постановку без молчаливых расширений/сужений: форма `no-delivers-since:<тред>`, окно от объявления парка (строгое сравнение), третий отказ двери на несуществующий тред, тред называется только явно, нота — тот же тик, память не переезжает на диск, случай (2) явно назван непокрытым.

Прогоны:
- `pnpm typecheck` — зелёный.
- `pnpm exec biome check` по всем изменённым файлам — 2 info (`useLiteralKeys`, `cli.ts:7953,14606`) — обе строки вне диффа этого PR (env var doors, не про park-ground), не находка.
- Полный `pnpm test` не повторял — `checks` на голове `974a1f6bdb329cbce65480faf9ec5dcc5ad08c63` зелёный, прогон [`34148447912`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34148447912).
- Числа тестов (критерий 1) сверены прогоном на ОБЕИХ границах, не арифметикой из письма:
  - голова — из лога прогона `34148447912` (`pull_request`, merge-ref): `packages/agent-protocol test: Test Files 229 passed (229)`, `Tests 3889 passed | 2 skipped (3891)` — совпадает дословно с заявленным в PR «229 файлов, 3891 тест, зелено»;
  - база — отдельный чекаут `git worktree` на `merge-base` (`3c7d2b91`, тот же коммит, что и `main` под этим PR) + `pnpm install --frozen-lockfile` + `vitest run` пакета: **229 файлов, 3881 тест** (3879 passed + 2 skipped);
  - дельта — **10 тестов**, ровно то число новых `it(...)`, что даёт дифф (5 в `park-ground.test.ts` + 2 в `new-message.process.test.ts` + 3 в `daemon.priority.process.test.ts`). Файлов не прибавилось — оба числа 229, все новые тесты легли в существующие файлы, что и видно в диффе.
- Точечно перепрогнал новые/тронутые сюиты сам: `vitest run park-ground.test.ts new-message.process.test.ts daemon.priority.process.test.ts` → 140/140 зелёных.
- **Критерий 2 проверен не чтением, а мутацией** (перепроверка заявления PR «сняли сравнение окна — упал ровно один тест»): заменил `message.fields.date > since` на `true` в `cli.ts:13357`, прогнал `daemon.priority.process.test.ts` — упал ровно один тест, `is SILENT about a delivery that was already lying there when the park was declared` (13 passed, 1 failed). Правка отменена (`git checkout -- packages/agent-protocol/src/cli.ts`), дерево чистое. Заявление автора подтверждено фактически, а не принято на слово.
- `pnpm protocol zones check --ref HEAD --role dev-core --base origin/main` → «8 path(s) of 'dev-core': none under a forbidden prefix» — совпадает с числом файлов в PR, зона не нарушена (критерий 4).
- Живой исход `pnpm protocol merge-gate --ref origin/main --pr 330`: guard 1/2 `STOP` (ожидаемо — этот вердикт как раз создаёт `approve`, `review` на голове ещё `IN_PROGRESS` в момент прогона); guard 3 `you` — восхождение к слову john присутствует в ленте этого треда, поимённо; **guard 4 `ok` — «8 changed path(s), none of them a document of power»**; guard 5 `you` — след кладёт исполнитель после кнопки. Доков власти этого репозитория (8, из живого вывода двери): `agent-protocol.json`, четыре `docs/roles/*.md`, `REVIEWER.md`, `PROTOCOL.md`, `.github/workflows` — ни один не тронут диффом. Мёржит **curator**.

Критерий 6 (совместимость протокола) — проверен прицельно, находки не подтвердились: PR расширяет допустимые значения уже существующего необязательного поля `park-ground` (не новый ключ, не смена смысла существующего значения). Разбор в `message.ts` идёт через `soft()` (`message.ts:796-804`) — исключение `MessageFormatError` гасится ПОЛЕВЫМ образом и не роняет ни сообщение, ни тред: старый ридер, встретив `park-ground: no-delivers-since:X`, которого его версия регэкспа не знает, читает это как «основание не названо» (парк остаётся, просто без машинного адреса), а не падает на всём треде. Это тот же деградационный путь, что уже был спроектирован для `frozen:` в #323, и именно он же обоснован в `PROTOCOL.md` после #326 (терпимый читатель почты, бамп не требуется для необязательной добавки). Бамп `protocolVersion` не нужен и не сделан — согласуется с нормой.

Критерий 3 (скоуп) — дифф ограничен постановкой второй формы; расхождения (случай (2) не покрыт, память ноты не на диске) названы явно и в PR, и в треде, и совпадают с решением john. `thread:`/`role:` в описании стоят.

Критерий 9 (текст против факта) — перепроверены конкретные утверждения:
- «16 тестов в `park-ground.test.ts`» — подтверждено (`grep -c '  it('` → 16);
- «три сценария в `daemon.priority.process.test.ts`» — подтверждено (новый `describe` содержит ровно 3 `it`);
- «третий отказ двери — `existsSync`» — подтверждено в `cli.ts` (новая проверка `existsSync(join(input.threadsRoot, ground.thread))`), обе точки входа (`newMessage`, `newThread`) прокидывают `threadsRoot: root`;
- «мутацией доказано, что окно несёт вес» — перепроверено самостоятельно, см. выше.

Критерий 12 не поднимается: в ленте прямо назван обычный путь восхождения к гарду 3 (решение john, письмо `14:44:34Z`), а не класс «полевой измеренный дефект» — условия этого класса автор не заявлял, подтверждать нечего.

Находок по критериям не найдено.

---

Доставлено шагами прогона [`34149112297`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34149112297) по PR #330, голова `974a1f6bdb329cbce65480faf9ec5dcc5ad08c63` (вердикт написан агентом ревьюера, доставка — джобой: тред 088).
Ход передан роли `curator` — так объявил сам вердикт.

