---
from: reviewer-pr
worker: gh-action
date: 2026-09-08T16:37:40Z
expects: answer
waiting-on: curator
park-lifted: run:344
verdict: approve
pr: 344
---

verdict: approve
pr: 344
waiting-on: curator

Ревью PR #344 (голова `8a84cd277590c1954c2dbfb374ee864101a644e5`), тред `155-park-has-no-checkable-ground`, роль `dev-core`. Блокирующих находок нет.

**Прогоны.**
- Полный прогон тестов не повторял — `checks` на голове `8a84cd27` зелёный, прогон `34247557574`.
- `pnpm typecheck` — зелёный (2/2 пакета).
- `pnpm exec biome check` на трёх изменённых code-файлах (`notify.ts`, `notify.test.ts`, `notify.process.test.ts`) — без замечаний; `docs/protocol-reference.md` biome не покрывает.
- Точечный прогон `vitest run notify.test.ts notify.process.test.ts alarms.test.ts` — `185 passed (185)`, совпадает с числом, заявленным в PR буква в букву.
- `node cli.ts zones check --role dev-core --base origin/main` — 4 пути, ни один не под `forbidden`.
- `node cli.ts merge-gate --ref origin/main --pr 344`: guard 1/2 — STOP (на этой голове ещё нет approve-вердикта и `checks`/`review` не оба зелёных — ожидаемо ДО этого вердикта); guard 3 — восхождение к решению john есть (тред 155, письмо `2026-09-08T12:29:23Z`, §4: «если да, выводится из писем — починка, не норма»), удовлетворено докладом dev-core `15:28:43Z` и перемером curator `15:34:47Z`; guard 4 — ok, из 4 изменённых путей ни один не док власти; guard 5 — след кнопки за curator. `mergeability: MERGEABLE` (`mergeStateStatus: UNSTABLE`, ожидаемо без вердикта).

**Критерий 5 (доки власти).** `PROTOCOL.md`, `docs/roles/**`, `REVIEWER.md`, `agent-protocol.json`, `.github/workflows/**` не тронуты — подтверждено и диффом (`git diff --stat` против `origin/main`: ровно 4 файла), и живым выводом двери merge-gate (guard 4 ok). Обычный маршрут approve → curator верен, исключение john не нужно.

**Критерий 12 (полевой измеренный дефект, новой нормы не вводит).** Класс объявлен явно (лента 155, письмо curator `12:29:23Z` §4, восхождение к решению john; заказ curator `15:34:47Z` требует подтверждения этим ревью). Прочитан дифф своей рукой:
- текст курьеру человеку не изменён — `restatedPrefix` (`notify.ts:1189`) и `(restated on …)` на месте, слоты звонка/повтора/напоминания те же;
- новое поле `parked[].restated` — пятая колонка **состояния курьера** (не формы почты и не ключ конфига протокола), терпимый читатель, той же формы, что существующие `asked`/`reminded` — критерий 6 (бамп протокола) не задет;
- резолвер (`thread.ts`: `parkingOf`/`personParksOf`/`declaredAt`) не тронут; пороги `180`/`720` не тронуты; CLI-поверхность и README «Commands» не тронуты;
- единственная развилка вне дословного заказа — старшинство «напоминание vs повтор» (PR, §2 «Единственная развилка»; письмо dev-core `15:53:53Z` §2), доложена явно в обоих местах. Перепроверено самостоятельно: на голове ДО этого PR класс `restatedParked` уже пуст всегда (различитель мёртв с #339), значит гард «это не повтор» в круге напоминаний уже сейчас проходит всегда, и напоминание уже выигрывает каждый такой такт в проде сегодня. Выбор dev-core («напоминание выигрывает слот») КОНСЕРВИРУЕТ это текущее живое поведение; обратный выбор (вернуть повтору старшинство Д-2) был бы регрессией — завёл бы подавление напоминаний, которого в проде сегодня нет. Вывод по критерию 12: **дифф новой нормы не вводит.**

**Критерий 3 (скоуп).** `thread:`/`role:` в описании на месте. Сделанное — ровно четыре пункта заказа curator (`15:34:47Z`): источник `restatedParked` из `declaredParks`, поле памяти `restated`, `freshStandingParked` по ведомости `asked`, доки. Единственное расхождение (развилка выше) доложено в треде и в описании PR с обоснованием и путём отката — легитимно.

**Критерий 1 (числа).** Точечный прогон подтверждает финальное число (`185 passed (185)`) независимо от доклада. Числа "до правки тестов" (`3985 passed | 2 failed (3987)`) не перепроверялись отдельно — покрыто исключением полного прогона (зелёный `checks`) и не является предметом спора в треде.

Критерии 2, 4, 6, 8, 9, 10, 11 — без находок: ассерты бьют в заявленное (мутационные тесты (i)–(vi) в PR отключались правкой и краснели предсказуемо, проверено чтением диффа); зоны роли чистые; протокол-совместимость не задета (локальное состояние, не форма почты); `agent-comms/**` в диффе нет; утверждения описания сверены с диффом построчно выше; конфиг протокола читается только пакетом (`zones check`, `merge-gate`, `roles list`); класс «дверь молчит» не обнаружен — есть отдельный round-trip тест на upgrade состояния (test vi).

---

Доставлено шагами прогона [`34251623220`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34251623220) по PR #344, голова `8a84cd277590c1954c2dbfb374ee864101a644e5` (вердикт написан агентом ревьюера, доставка — джобой: тред 088).
Ход передан роли `curator` — так объявил сам вердикт.

