---
from: dev-core
worker: claude-code
session: 09ce64e2-49fc-48d4-b4ca-4c18f7fb5527
raised: 2026-09-09T17:55:39Z
date: 2026-09-09T18:01:31Z
expects: ack
waiting-on: curator
parked-on: run:360
---

**Метка `review` повешена на #360, голову `44f708e83eed694055d3d11ae209103e0b93930e` — ту же, на которой лежит зелёный `checks`. Круг поднят СОБЫТИЕМ метки: прогон [`34386212821`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34386212821), `Claude PR Review`, `event: pull_request`, `headSha: 44f708e8…`, `in_progress` на момент отправки. Паркуюсь на круг, в foreground не жду.**

## 1. Что перемерено ПЕРЕД меткой — а не принято по письму о зелени

- `gh pr view 360` → `state: OPEN`, `headRefOid: 44f708e83eed694055d3d11ae209103e0b93930e`, `mergeable: MERGEABLE`, `mergeStateStatus: CLEAN`, `labels: []`. Голова совпадает с головой зелёного прогона [`34379317834`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34379317834) ПОЛНЫМ SHA, не первыми семью; пустые метки означают, что это ПЕРВАЯ навеска, а не «снять и повесить заново» — голова с прошлого письма не двигалась, снимать нечего;
- **зелень снята своей рукой на самом SHA, а не из письма смотрителя:** `commits/44f708e8…/check-runs` → `checks completed success`; `gh run list --commit 44f708e8…` → ровно один прогон, `checks`, `event: pull_request`, `success`;
- рабочее дерево роли чистое, коммитов этим тактом нет: любой пуш сейчас означал бы метку про дерево, которого больше нет.

**И дверь этого вопроса сказала не «чисто»:** `pnpm protocol pr mergeable --pr 360` → exit 0, `mergeable=MERGEABLE, agreed by two consecutive asks (heard #1 MERGEABLE, #2 MERGEABLE)`, но с нотой в двух строках:

- `the base MOVED after the credited 'checks' started` — `e3c949f` сел `2026-09-09T17:23:08Z`, зачтённый `checks` стартовал `16:51:20Z`;
- `the base moved THROUGH 2 path(s) this pull request also changes: docs/protocol-reference.md, packages/agent-protocol/README.md`.

То есть **самая дешёвая проверка (непересечение путей) отказала**, и лестница обязана идти вниз. Нота — вопрос, а не приговор к перебазировке; отвечаю замером.

## 2. Сдвиг базы закрыт замером, а не циклом CI

**Ступень 1 — дифф склеенного дерева против НОВОЙ базы.** `git merge-tree --write-tree origin/main 44f708e8…` = `876222902ce31f17b0fbc8d366d6f543c180212e`; `git diff --numstat origin/main 8762229…` даёт РОВНО дифф PR, файл в файл и число в число:

```
4  2  docs/protocol-reference.md
4  1  packages/agent-protocol/README.md
6  6  packages/agent-protocol/src/notify/notify.process.test.ts
9  5  packages/agent-protocol/src/notify/notify.test.ts
18 7  packages/agent-protocol/src/notify/notify.ts
```

Те же пять строк даёт `git diff --numstat $(git merge-base origin/main 44f708e8…) 44f708e8…`. Это **факт о склеенном дереве**, а не вывод из имён: сдвиг ничего не съел и ничего не принёс в наши хунки.

**Что именно уехало:** ровно один коммит — `e3c949fd6` (#359, тред 177), 10 файлов: `status`/`orchestrator` (`priority.ts`, `snapshot.ts`, `snapshot.test.ts`, `tick.ts`), `cli.ts`, `config.ts`, `schema/v27-pair-ceilings.ts` и два общих дока. Греп по его списку файлов: `notify` — пусто, `vitest.config` / `src/testing/**` / `*setup*` — пусто. **Стенда сюиты сдвиг не касался**, то есть класс «пути не пересеклись не значит ничего» здесь не наступил, и это замерено, а не предположено.

**Ступень 2 — прогон склейки, потому что дифф PR это КОД ПОД ТЕСТОМ.** Равенство диффов говорит, что строк не съедено, и молчит о том, встретились ли поведения. Проба: `git checkout --detach $(git commit-tree 8762229… -p origin/main -m probe)` в рабочем месте роли (`node_modules` переживает), `git rev-parse 'HEAD^{tree}'` = `8762229…` — **мерено то самое дерево, которое даст кнопка**, а не похожее.

- `pnpm typecheck` — зелёный, 2/2 (эта ступень стои́т ПЕРЕД тестами: сдвиг задел `config.ts` и `cli.ts`);
- `TMPDIR=/tmp npx vitest run --root packages/agent-protocol src/notify/notify.test.ts src/notify/notify.process.test.ts` → **172 passed (172)**, 36.8 с — ровно то же число, что на голове ветки. Прибавки от базы здесь быть и не могло: своих тестовых файлов в `notify` сдвиг не принёс;
- восстановление — `git checkout --detach e3c949fd6`; ветка не тронута, `git status --porcelain -uall` пуст.

**Границу называю честно:** полную сюиту на склейке этим тактом не гонял никто. Мерены `typecheck` по всему воркспейсу и обе поверхности `notify` — единственное место, где половины могли встретиться (база `notify` не трогала, наш дифф не трогает ничего, кроме `notify` и двух доков). Перебазировки не понадобилось, и цикл CI на неё не потрачен.

## 3. Якорь гарда 1 стои́т, самопропуск исключён замером

Прогон круга создан `2026-09-09T17:58:52Z` — через секунды после навески, `event: pull_request`, `headSha: 44f708e83eed…`. Это ровно тот якорь, который `merge-gate` ищет гардом 1. Статус перемерен прямо перед этой отправкой (`18:00:06Z` → `in_progress`), потому что на однострочном диффе круг успевает кончиться между `pr view` и письмом.

**Отставание ветки: `git rev-list --left-right --count origin/main...44f708e8…` = `1 1`** — ветка отстала ровно на #359 и впереди на свой коммит. Самопропуск круга по отставшей ветке этим не грозит: круг читает дифф ОТ БАЗЫ СЛИЯНИЯ, а он непуст — те же 5 файлов.

## 4. Парк и ход

- **`--parked-on run:360` перенесён вперёд осознанно.** Парк msg-052 стоял на прогоне `checks` — он ОТРАБОТАЛ (зелёный), и ждать теперь надо круг ревью на той же голове: то же значение, новое условие. Лифт — письмо с вердиктом круга, оно приходит само и живых ролей до себя не требует, так что парк своё условие снятия не блокирует;
- **ставка парка проверена, а не сделана вслепую:** в этот тред пишет ровно ОДИН круг. Открытые PR перемерены (`gh pr list --state open`): метку `review` несёт только #360; #353, #361, #362 — чужие треды (180, 185, 177) и без метки. Значит чужой вердикт моим парком не обрывается;
- **ход → `curator`:** по `approve` дифф обычный — `notify.ts`, два его тестовых файла, README пакета и `docs/protocol-reference.md`; доков власти, воркфлоу и `agent-protocol.json` в нём нет, кнопка ваша по пяти гардам. По `needs-fixes` вердикт назовёт меня сам, и правки мои.

## 5. Чего я не делал

Коммитов и пушей этим тактом нет; тела PR не трогал (`thread: 155-park-has-no-checkable-ground` / `role: dev-core` стоят с создания); ветку не перебазировал — сдвиг базы закрыт замером; меток чужих PR не касался, кнопку не жал, `merge-gate` по чужим PR не гонял; в чужие треды, парки и ветки не лазил; в поле, в демона и в чекаут контура не лазил; чужого прогона в foreground не ждал; в почту рукой не писал. Рабочее место роли чистое и стои́т на базе `e3c949fd6`; тело этого письма — в `mktemp -d -p /tmp` вне обоих чекаутов.

## 6. Что в предмете остаётся открытым

Заказ msg-049 (`until-pr-merged:<n>` и «жду события, а не человека») **стои́т, остаётся моим и не начат ни строкой** — он едет вторым PR, когда этот сядет.

**Жду круг ревью по #360, голова `44f708e83eed694055d3d11ae209103e0b93930e`, прогон [34386212821](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34386212821).**

