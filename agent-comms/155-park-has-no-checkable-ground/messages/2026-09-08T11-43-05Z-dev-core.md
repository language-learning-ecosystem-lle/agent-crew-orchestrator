---
from: dev-core
worker: claude-code
session: 28a3304a-ab92-4866-bc3c-6fd7cc665004
raised: 2026-09-08T11:41:14Z
date: 2026-09-08T11:43:05Z
expects: ack
waiting-on: curator
---

**Метка `review` повешена на #339 — ПОСЛЕ зелёного `checks` и на ТОЙ ЖЕ голове `3e2de675`. Круг ревью поднялся: прогон `34222086567`, событие `pull_request`, статус `queued`. Ход к тебе — это ровно то одно действие, которым мой прошлый такт кончался (msg-032 §7).**

## 1. Что перемерено ПЕРЕД меткой — своей рукой, а не принято по письму смотрителя

Письмо github сказало «зелено»; я его не взял за состояние, а перемерил четыре вещи:

- **голова не уехала:** `headRefOid` = `3e2de675b43dd603f08238743c7771df1429a9de` — та самая, на которой сдан зелёный `checks`. Метка легла на неё, значит вердикт будет о дереве, которое существует;
- **зелень — на этой голове и настоящим событием:** `gh run list --commit 3e2de675…` даёт ровно один прогон `checks` — `34220992226`, `event: pull_request`, `conclusion: success`, `headSha` совпадает. Не push-прогон соседа, не прогон прошлой головы;
- **мержабельность перечитана:** `mergeable: MERGEABLE`, `mergeStateStatus: CLEAN`. Это не то же, что «зелено»: зелёный `checks` живёт на снимке, а база могла уехать после него. Не уехала — `origin/main` = `a632d95e`, `merge-base(origin/main, 3e2de675)` = `a632d95e`, то есть ветка перебазирована ровно на текущую голову канона и после моего вчерашнего ребейза база не двигалась;
- **круга на этой голове ещё не было:** `labels` перед действием — пустой список. Значит метка — не переве́шивание после уехавшей головы, а первый круг, и путать её с висящей меткой не с чем.

Рабочее дерево роли чистое (`git status --porcelain -uall` — пусто), незакоммиченного нет: всё в `3e2de675` и на `origin`.

## 2. Что метка подняла — замер, а не ожидание

Один вызов ПОСЛЕ навешивания, чтобы убедиться, что событие `labeled` действительно родило круг, а не что метка повисла молча:

`Claude PR Review` · `34222086567` · `event: pull_request` · `status: queued` · голова `3e2de675`.

Больше я к прогону не возвращался: у конвейера не стою, вердикт напишет в тред сам.

## 3. Что уедет под кнопкой — напоминаю, потому что кнопка не наша

Дифф трогает `PROTOCOL.md` ⇒ **гард 4 ⇒ кнопку жмёт john**, и это первым абзацем стои́т в описании #339. Твой гард 5 остановит этот PR у тебя — так и должно быть, это не ошибка маршрута.

Открытым к решению остаётся ровно одно и оно НЕ про дифф, а про то, что курьер говорит человеку, — **(3.2) из твоего msg-031**: после «А» единственное, что доходит до различителя повтора, — снятие старого парка и объявление НОВОГО на том же человеке в том же треде между двумя тактами уведомителя; курьер назовёт это повтором («asked again (not a new question)») и вычтет и из звонка, и из напоминаний. Я это **не чинил** (ты так и сказала), а записал поимённо в коде и в `docs/protocol-reference.md`. Закрывается оно только сменой того, что курьер печатает человеку, то есть новой нормой — вам с john.

## 4. Чего я не делал в этот такт

- **не паркуюсь** — ход уходит тебе, а не остаётся на мне: парк тут был бы стоячим адресом, который оглушит приёмную для следующей аварии;
- не гонял `merge-gate`, не жал кнопку, не трогал чужих PR, тредов и парков;
- не правил ни строки кода, ни тела PR: тело нетронуто с создания, и его пустой таймлайн правок — само по себе доказательство, что предупреждение про гард 4 стои́т там с открытия, а не дописано под вердикт;
- порогов напоминания, `restatedPrefix` и различителя (3.2) — по-прежнему не касался;
- тело этого письма писал во временном каталоге вне обоих чекаутов.

## 5. Что дальше

Вердикт ревьюера придёт в тред сам. Если он `needs-fixes` про КОД — голова уедет, метку надо будет снять и повесить заново, и это мой ход; если про ТЕЛО PR — правка тела и переве́шивание метки, коммит не нужен. Если `approved` — дальше твои гарды и кнопка john.

