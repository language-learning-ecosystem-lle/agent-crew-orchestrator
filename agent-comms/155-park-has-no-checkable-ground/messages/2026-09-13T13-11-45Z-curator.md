---
from: curator
worker: claude-code
session: 2260f7a6-8f00-4e58-8fea-8519dc2b19ee
raised: 2026-09-13T13:04:07Z
date: 2026-09-13T13:11:45Z
expects: none
waiting-on: curator
parked-on: run:388
---

**Перебазировала ВТОРОЙ раз за восемь минут и запушена: голова #388 — `6874550f78ba71f53b9bce82aabdc69bf0330e67`, `mergeable: MERGEABLE`, база `e22c5fa16`. Первый ребейз (13:02) протух через три минуты: в `main` сел #394 (тред 177), и он трогает РОВНО ОДИН файл — `docs/journal/curator.md`, тот самый общий хвост. Метку не вешаю: прогон `34759064637` на новой голове только стартовал. Паркуюсь на нём.**

## 1. Что было сделано руками, по шагам

1. **Замер конфликта — не по чужому слову, а своей рукой:** курьер объявил письмом `13:06:51Z` (`mergeable=CONFLICTING`, два подряд спроса). Перемерила трижды подряд в 13:07:53Z — `CONFLICTING DIRTY` все три раза, голова `5cfa61d4f…`. Причина названа чтением ленты `main`: **`e22c5fa16` (#394, тред 177, влит `13:05:33Z`) — дифф одного файла, `docs/journal/curator.md`**;
2. **Перебазировано** на `e22c5fa16` — один конфликт, тот же самый и там же: обе стороны ДОПИСЫВАЮТ раздел в хвост журнала роли. **Разрешено объединением: сначала запись 177 целиком, следом запись 155 целиком, ни строки не выброшено и ни строки не переписано** (маркеры сняты тремя строками `sed`, разделяющая пустая строка восстановлена);
3. **Запушено** `--force-with-lease=…:5cfa61d4fa50ad2bdfb5de2ee62ba9a068635a88` — на СТАРУЮ голову, то есть push отказал бы, двинь ветку за это время чужая рука. Новые коммиты: `838e634ba` (канон) и `6874550f7` (справка);
4. **Контрольное чтение после пуша** (ответ инструмента ≠ факт): `head=6874550f78ba71f53b9bce82aabdc69bf0330e67 · MERGEABLE UNSTABLE · base=e22c5fa1679dfd956aa6862c4292128c1a990eba · labels=` (пусто). Прогон на новой голове — `34759064637`, `checks`, старт `13:09:51Z`.

## 2. Чем проверено, что содержание не поехало

Тем же способом, что и в прошлый раз, — числами диффа против СВОЕЙ базы, а не прозой:

- до обоих ребейзов и после первого: `PROTOCOL.md` +29, `docs/journal/curator.md` +21, `docs/protocol-reference.md` +11 — **61 вставка, 0 удалений**;
- после второго (`git diff --stat e22c5fa16 HEAD`): те же три файла, те же числа — **61 вставка, 0 удалений**.

Плюс построчная сверка трёх якорей в дереве после ребейза: `PROTOCOL.md:471` — раздел «Словарь оснований парковки закрыт тремя формами до перемера (решение john 2026-09-13, тред `155-…`)»; `docs/protocol-reference.md:6118` — врезка «Четвёртой формы пока нет, и это РЕШЕНИЕ, а не незавершённость»; `docs/journal/curator.md:183` — запись полевого замера. Рабочее дерево роли чистое (`git status --porcelain` пуст).

## 3. Поправила ТЕЛО PR — оно называло два файла, а их три

Тело #388 с самого открытия перечисляло `PROTOCOL.md` и журнал, а `docs/protocol-reference.md` приехал вторым коммитом и в перечень не попал. Переписала тело: три файла, +61/−0, у каждого — что в нём. Голову это не двигает (сверено: `headRefOid` до и после правки один и тот же), круга на PR ещё нет, так что терять нечего. **Это важно именно на ТВОЕЙ кнопке: тело PR — то, по чему читают дифф, а оно врало в минус.**

## 4. Поправка к себе: подъём в 13:04 — НЕ сломанный парк

Прошлый ход припарковался `run:388` в `13:03:26Z`, а роль поднялась в `13:04:07Z` — 41 секунда. Похоже на «парк не держит», и я чуть не записала это находкой. **Замер журнала ящика говорит другое, дословно:** «the park on the round of PR #388 HAS GONE STALE — declared 2026-09-13T11:35:14Z, standing 91 min against a ceiling of 30 min». То есть снят был СТАРЫЙ парк (объявленный в `11:35:14Z` вместе с первым сообщением о PR), по своему 30-минутному потолку, а не свежий. Мой новый парк молод и держит.

Правило из этого — то же, что и всегда в этом треде: **своя гипотеза о механизме — заявление, подлежащее проверке.** Стоило одной команды по журналу.

## 5. Источник столкновений перечислен, и он ОДИН

Не образец, а перечисление по всем открытым PR ящика (`gh pr view --json files` по каждому):

| PR | тред | трогает `docs/journal/curator.md` |
|---|---|---|
| #394 (влит 13:05:33Z) | 177 | **да — и это ЕДИНСТВЕННЫЙ его файл** |
| #389 | 187 | **да** |
| #386 | 180 | нет |
| #384 | 189 | нет |
| #383 | 188 | нет |
| #379 | 190 | нет |

**Вывод, который стоит твоего §2 в письме `12:57:02Z`: сдвиг базы САМ ПО СЕБЕ круга не стоит** — голова не двигается, гард 1 держится, дифф не пересекается. Дорог только сдвиг С КОНФЛИКТОМ, а он у меня ровно по одному каналу — общий хвост журнала роли, куда дописывают мои же PR из других тредов. Сегодня по этому каналу меня сбило дважды (#393 в первый ребейз, #394 во второй), и **из открытых остался ровно один заряд — #389 (тред 187)**: ляжет он в `main` раньше твоей кнопки — я перебазируюсь третий раз, механически и тем же объединением.

**Моя ставка, не постановка** (ты сказал, что сегодня только называешь): чинится это не потолком пар, а тем, чтобы запись журнала не ехала в одном диффе с каноном. Если скажешь — вынесу журнальный хунк из #388 отдельным PR, и тогда канон (`PROTOCOL.md` + справка) перестанет конфликтовать вовсе. Своей рукой не делаю: ты сказал «ничего по существу не менять», а вынос файла из диффа — это изменение формы поставки, и решать его тебе.

## 6. Хвост

- **Кнопку не жала ни по одному PR.** #388 — гард 4 STOP по построению (`PROTOCOL.md` в диффе), кнопка твоя; остальные шесть принадлежат чужим тредам (177, 187, 180, 189, 188, 190), и след гарда 5 по ним обязан лечь в ленты ИХ тредов, а этот ход связан тредом `155`;
- `merge-gate` по #388 не гоняла — до зелёного `checks` и круга ей нечего спрашивать, а гард 4 её вывода не переживёт в любом случае;
- кода, тестов и конфига не касалась; сюиту в рабочем дереве роли не гоняла (она переписывает `origin` в общем `.git/config`); чужих веток, тредов, парков и меток не трогала; меток не вешала и не снимала; чужого прогона в foreground не ждала; тело этого письма — в `mktemp -d -p /tmp` вне обоих чекаутов;
- **рабочее дерево роли ЧИСТОЕ, незакоммиченного нет**, вся работа запушена в `curator/155-park-ground-vocabulary-canon`.

**Что делает следующая сессия по 155:** читает исход прогона `34759064637`; зелёный — **вешает `review` на голову `6874550f78ba71f53b9bce82aabdc69bf0330e67`** (ту же, что мерил прогон) и паркуется за кругом; красный — чинит и не вешает ничего. Перед меткой перемерить `mergeable` одной командой: сбитая база стоит новой головы, а метка на старой голове — вердикт о дереве, которого нет. Кнопка на #388 в любом случае john.

Паркуюсь на `run:388`. Основания (`park-ground`) у парка нет сознательно: ждут отчёта идущего прогона, а форму для этого мы этим же тредом решили НЕ заводить — её работу делает сам `run:<n>`. waiting-on → curator.

