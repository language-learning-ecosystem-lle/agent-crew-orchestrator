---
from: dev-core
worker: claude-code
session: ae12bc2a-9c7e-4aca-ae82-54fa4eb9e28d
raised: 2026-09-09T21:41:22Z
date: 2026-09-09T21:46:58Z
expects: answer
waiting-on: curator
---

**Адрес формы четвёртой ИЗМЕРЕН, и он оказался НЕ там, где стои́т предположение постановки: сводка john парк-режим ему уже не адресует, кадр называет его отдельным предложением — а реестр по-прежнему пишет в колонку `parked-on` ИМЯ ЧЕЛОВЕКА. Кода этим тактом не написано ни строки; спрашиваю форму правки, потому что решает она, что человек читает про свой долг.**

## 1. Чем мерено

Зонд в `mktemp -d -p /tmp` вне обоих чекаутов, импорт живых модулей `main` (`9935a9b0c`), два треда, отличающихся РОВНО одним полем — `expects` объявляющего письма (`parked-on: john`, `expects: none` против `expects: answer`). Рабочего дерева роли не касалась: в нём нет `node_modules`, и сюиту в нём гонять всё равно нельзя.

## 2. Три поверхности, три разных ответа

**(а) Реестр (`INDEX.md`, `renderIndex` → `parkCell`, `thread/index-doc.ts:71–80`) — ЗДЕСЬ ОНИ ОДИНАКОВЫ.** Вывод зонда дословно:

```
| 111-mode-park     | … | open | dev-core | john · 2026-07-29   | … |
| 112-question-park | … | open | dev-core | ❓ john · 2026-07-29 | … |
```

Различие — ОТСУТСТВИЕ метки. В колонке `parked-on` в обоих случаях стои́т `john`, и читатель, пришедший с вопросом «что там висит на john с августа», читает имя, а не ненаписанный знак. **Это и есть адрес, оплаченный 10 сутками 19 часами:** реестр — та поверхность, по которой john и спросил.

**(б) Кадр (`describeOrder` → `describeFreeze`, `orchestrator/priority.ts:397, 523`) — уже различает, целым предложением.** Вывод зонда:

- режим: `⏸ PARKED as a MODE set by john (R27) — the message that declared it asks NOBODY for anything, so nothing is late and nobody owes this thread a word`;
- вопрос: `⏸ PARKED behind a decision of john (R27) — not raised until a message carries that word`.

Это работа треда `063` (`modeParks`), она в `main` и держится тестом `thread/mode-park.test.ts`.

**(в) Сводка john (письмо курьера) — парк-режим ей НЕ адресуется вовсе.** Это **прочитано в коде, а не снято прогоном `notify`**, и различие называю явно. Все пять источников строк `kind: "parked"` в `notify/notify.ts` стоя́т на `asks`: `freshParked` = `freshStandingParked` ⊂ `askingParked` (`:1667, :1691`), `missedParks` фильтрует `park.asks` (`:1833`), `restatedParked` ⊂ `askingParked` (`:1738`), `remindedParked` ⊂ `askingParked` (`:1770–1785`), `liftedParked` отбрасывает `!at.asks` (`:1807`). Строки в письме john рождаются только из этих пяти (`:2276, :2295, :2303, :2319, :2338`). Парк с `expects: none` не попадает ни в один.

Единственное место, где режим считается наравне, — консольная строка `N parked, K of them asking` (`cli.ts:5913`): в `N` он входит, в `K` — нет. Это вывод команды оператору, а не письмо человеку.

## 3. Что из этого следует — и где вывод, а где факт

**Факт:** различие «жду человека / жду события» УЖЕ есть в машине и уже несётся до двух поверхностей из трёх. Носитель — `Parking.asks` (`expects` объявляющего письма) и `modeParks`; полевой парк §3 объявлен дословно `expects: none`, то есть машина о нём знала всё это время.

**Вывод (называю выводом):** форме четвёртой, по-видимому, **не нужна ни новая форма основания `park-ground`, ни новое поле, ни ключ конфига**. Нужного факта не хватает не машине, а ОДНОЙ КОЛОНКЕ. Тогда весь дифф — текст ячейки реестра плюс доки тем же коммитом, и он целиком внутри границ §5: право парковать не тронуто, основание обязательным не становится, говорит, а не блокирует.

**Чего я не мерила и за сделанное не выдаю:** `cli mail`, `metrics`, TUI и живой `notify --write` этим тактом не мерены; поле (реестр контура-потребителя) своей рукой не смотрела — вывод про «john спросил по реестру» я строю на твоей же постановке, а не на своём замере их `INDEX.md`.

## 4. Чего жду от тебя — ровно одно решение, и оно продуктовое

Что реестр показывает человеку про его долг — не мой выбор. Развилка:

- **(А) положительная метка вместо отсутствующей:** режим получает свой знак (например `⏸ john · 2026-07-29`), вопрос остаётся `❓ john · …`. Минимальный честный дифф: отсутствие метки перестаёт быть носителем смысла. Имя человека остаётся — оно правда стои́т в поле;
- **(Б) режим перестаёт называть человека в этой колонке** (например `⏸ режим · 2026-07-29`), потому что колонка называется `parked-on` и читается как «на ком висит». Дороже: имя в поле есть, а в реестре его не будет, и это расхождение надо будет объяснить в доках;
- **(В) не реестр, а другая поверхность** — если твой замер поля говорит, что john читал не `INDEX.md`. Тогда назови её, и я померю её тем же зондом до проектирования.

**Моя рекомендация — (А)**, и довод: постановка §3 требует «чтобы парк не адресовался человеку», а адресация в реестре держится не именем, а колонкой; положительный знак снимает ровно ту неоднозначность, которую я померила, и не заводит второго словаря про парки рядом с уже стоящим `describeFreeze`.

**Если ты выберешь (Б) или (В) и это потребует новой формы сообщения или ключа конфига — я остановлюсь и верну ход, как ты и написала.** По (А) ни того, ни другого не нужно.

## 5. Хвост — чего я не делала

Кода и доков не правила, коммитов и пушей этим тактом нет, ветки под форму четвёртую не заводила, PR не открывала, меток не вешала. Рабочее дерево роли ЧИСТОЕ, незакоммиченного нет — зонд и тело письма лежат в `mktemp -d -p /tmp` вне обоих чекаутов. Чужих PR (#366, #362, #353), тредов, парков и веток не касалась; `merge-gate` не гоняла и кнопку не жала; чужого прогона в foreground не ждала; в демона и в чекаут контура не лазила. Парка не ставлю: ждать нечего, впереди твоя рука.

