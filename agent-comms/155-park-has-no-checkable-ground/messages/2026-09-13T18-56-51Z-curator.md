---
from: curator
worker: claude-code
session: c86dbbea-6358-44c7-a41e-a7af0bbed916
raised: 2026-09-13T18:48:21Z
date: 2026-09-13T18:56:51Z
expects: ack
waiting-on: curator
parked-on: john
park-ground: until-pr-merged:388
---

**ТРЕБУЕТСЯ РЕШЕНИЕ JOHN. Вердикт `approve` на #388 доставлен — но круг, который его выдал, кончился `failure`, и гарды 1 и 2 красные. Жать кнопку по доставленному вердикту (кнопка твоя, гард 4) — или сперва чинить переезд на запасную учётку? Второе больше, чем #388: с `16:00Z` ни один круг ревью в репозитории не зазеленел — семь подряд красных, и гард 2 сейчас закрыт для ВСЕХ одиннадцати открытых PR.**

## 1. Что приехало и что говорит дверь

Вердикт: `reviewer-pr`, `verdict: approve`, `pr: 388`, голова `0169bea8cbfd5bbe77da34f4fb6f9c75988996dd` — разбор по критериям 3, 4, 5, 6 плюс некритичное замечание 9 (строка описания PR про базу устарела за ребейзами; содержимое диффа она не меняет).

Дверь, живьём, `18:48:54Z` — `pnpm protocol merge-gate --ref origin/main --pr 388 --review-workflow 'Claude PR Review'`, дословно:

```
STOP guard 1 · approve on the current head: an approve is shown on 0169bea (github-actions) but no
     round of review on this head produced it: no CLOSED round of 'Claude PR Review' on 0169bea:
     run 34775456728 (pull_request, head 0169bea, completed/failure, 18:42:24Z…18:48:16Z)
STOP guard 2 · green checks on the same head: not green: review=FAILURE
you  guard 3 · ascent to a decision of john's: thread '155-park-has-no-checkable-ground'
STOP guard 4 · no self-merge on the documents of power: john merges this one — it changes PROTOCOL.md
you  guard 5 · a trace of the merge
ok   mergeability · mergeable=MERGEABLE (mergeStateStatus UNSTABLE)
REFUSED: a guard does not hold
```

Гард 4 — STOP по построению (`PROTOCOL.md` в диффе), кнопка твоя при любом исходе остальных. Чек `checks` на той же голове ЗЕЛЁНЫЙ отдельным прогоном [`34774970085`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34774970085), `conclusion: success`. Красен ровно один чек — `review`.

Перемер головы `18:48:4xZ`: `headRefOid: 0169bea8cbfd5bbe77da34f4fb6f9c75988996dd` (голова под кругом не двигалась), `baseRefOid: 6312dfbfbd69c23fb09b693703fb26de87486b00` = `origin/main`, `labels: [review]`, `MERGEABLE`.

## 2. Почему круг красный — по шагам джобы, замером, а не пересказом

**Упал РОВНО ОДИН шаг из семнадцати — одиннадцатый, и ревью он не делал.**

1. **Шаг 9, `anthropics/claude-code-action@v1` (основная учётка): `outcome=success`, `duration_ms=294290`.** Его запись результата — `is_error: false`, `num_turns: 36`, `total_cost_usd: 0.8893732`. **Ревью сделано основной учёткой, целиком и без ошибки, за 4 м 54 с и $0,89.**
2. **Шаг 10 «Лимит основной учётки — установить факт» объявил переезд:** `##[warning]Основная учётка ревьюера ответила ЛИМИТОМ: запись type=rate_limit_event`. Признак у шага — **НАЛИЧИЕ записи** `type=rate_limit_event` где угодно в транскрипте (правило шага: `[$rs[] | select((.type? // "") == "rate_limit_event")] | length > 0`), **а не исход основного шага**. Шаг 9 при этом успешен — то есть учётка ответила лимитом где-то в середине, прогон это пережил и ревью довёл.
3. **Шаг 11 «Ревью на ЗАПАСНОЙ учётке — переезд по лимиту основной» — единственный упавший.** Его вызов: `duration_ms: 2027`, `num_turns: 1`, `total_cost_usd: 0`, `modelUsage: {}`, `is_error: true` → `##[error]Claude execution failed: result is_error:true` → `Process completed with exit code 1`. **Нулевой расход модели за две секунды — ревью он не делал**; это тот же класс, что уже замерен на этом ящике дважды 13.09 (запасная учётка отвечает `401`), и здесь он третий раз.
4. **Шаг 12 «Выход ревьюера»:** `FALLBACK_OUTCOME: failure`, вывод — `ПЕРЕЕЗД состоялся (исход запасного шага: failure); транскрипт круга — fallback`. При этом `PRIMARY_FILE` и `FALLBACK_FILE` — **один и тот же путь** `/home/runner/work/_temp/claude-execution-output.json`: транскрипт основного шага перезаписан пустышкой запасного.
5. **Шаги 13–17 зелёные.** Шаг 16: `Доставки вердикта: все три доставки прошли: письмо в тред, коммент в PR, формальный review-статус`; флаги — `LIMIT_HIT: 1`, `REVIEW_MOVED: 1`, `LIMIT_EVIDENCE: запись type=rate_limit_event`, `SELF_SKIP: 0`.

**Итог: вердикт выдан, доставлен трижды — и круг покрасил себя шагом, который ревью не делал.** Один упавший шаг уронил `conclusion` прогона, `conclusion` уронил чек `review` на голове, а чек уронил гарды 1 и 2 — обоих сразу, потому что гард 1 спрашивает ЗАКРЫТЫЙ УСПЕШНЫЙ круг.

**Отдельно называю границу своего замера, потому что диагноз — тоже заявление.** Происхождение доставленного ТЕКСТА вердикта прямым замером я не установила: транскрипты обоих шагов лежат по одному пути, и запасной перезаписал основной. Установлено другое, и этого достаточно: **текстом запасного шага вердикт быть не может** — у того ноль расхода модели и один ход, — а по окну и объёму он совпадает с 36-ходовым прогоном основной учётки за $0,89. Отсюда же следует, что хвостовая строка письма ревьюера «🔁 Круг доехал на ЗАПАСНОЙ учётке» **называет не ту учётку**: она собрана из флагов `LIMIT_HIT`/`REVIEW_MOVED`, а не из того, чей текст доехал.

## 3. Это шире #388: гард 2 закрыт для всего репозитория

`gh run list --workflow 'Claude PR Review' --limit 12`, замер `19:00Z`:

| время | прогон | голова | исход |
|---|---|---|---|
| 18:42:24Z | 34775456728 | `0169bea8c` (#388) | **failure** |
| 18:40:55Z | 34775379733 | `9886b0272` (#409) | **failure** |
| 18:15:19Z | 34774059182 | `9886b0272` (#409) | **failure** |
| 18:14:40Z | 34774024470 | `d82346b27` (#410) | **failure** |
| 18:14:39Z | 34774023359 | `67b71e151` (#386) | **failure** |
| 17:59:02Z | 34773208904 | `15ceb8d73` (#408) | **failure** |
| 16:28:49Z | 34768699365 | `13cad1a6d` (#386) | **failure** |
| 16:00:23Z | 34767267092 | `b20f8d26f` | success |

**Последний зелёный круг — `16:00:23Z`. После него семь подряд красных.** Переезд на запасную учётку влит в `main` в `16:58:46Z` (коммит `1b08b81cf`, PR #399, тред `193`) — то есть внутрь этого окна, а не в его начало.

**Упавшие шаги трёх других прогонов, замерено:**

- **34775379733 (#409, 18:40)** — упал **ровно шаг 11**, и только он. Это ТОТ ЖЕ случай, что у #388: основной шаг отработал, запасной покрасил круг. **Два PR, одна форма** — класс, а не образец;
- **34773208904 (#408, 17:59)** — упали шаги 9 **и** 11, и с ними доставки (15, 16): основная учётка сгорела по-настоящему, запасная не подхватила, вердикт не доехал вовсе;
- **34768699365 (#386, 16:28, ДО переезда)** — упал шаг 9 и доставки; шага 11 в том прогоне ещё не было.

То есть форм две, и различать их надо: **(A) основной круг отработал и доставил вердикт, а переезд его покрасил** (#388, #409); **(B) основная учётка действительно сгорела, и запасная не спасла** (#408). Форма A — чистая потеря: работа сделана и оплачена, а гарды 1 и 2 потеряны на шаге, который ничего не считал.

## 4. Развилка — и почему она твоя

**(A) Кнопка по доставленному вердикту — твоя рука.** Вердикт `approve` разбирает ровно эту голову, по критериям, с живым выводом двери и `zones check` внутри; тестовый `checks` на голове зелёный; красен только `review`, и красен он шагом, который ревью не делал. Дверь при этом будет отказывать — `STOP guard 1`/`STOP guard 2` останутся, — то есть это решение ПОВЕРХ двери, и принять его может только владелец.

**(B) Новый круг.** Снять метку и повесить заново — единственный способ поднять круг из моей руки, и это моя рука, не твоя. Цена — ещё ~$0,89 и квота основной учётки; **риск прямой: ровно тот же признак `rate_limit_event` появится снова, переезд сработает снова, `401` снова покрасит круг.** Семь красных подряд говорят, что B сегодня — это не «попробуем», а «скорее всего то же самое».

**(C) Перезапустить упавшую джобу.** У меня этого нет — у PAT нет записи в Actions (замерено ранее). У тебя есть, и это самый дешёвый путь к зелёному `review` БЕЗ нового расхода на ревью — если перезапуск джобы не погонит ревью заново.

**Рекомендация curator (и она не решение john):** #388 сегодня не горит — он записывает в канон уже принятое тобой решение, и день-два ожидания ему ничего не стоят. **Горит другое: пока шаг 10/11 не починен, ни один PR в репозитории не может закрыть гарды 1 и 2, а открытых сейчас одиннадцать.** Я бы отдала приоритет починке переезда, а не кнопке на #388. Починка — это код в `.github/workflows/claude-review.yml`, то есть твоя же кнопка и зона `dev-core`: у переезда признак лимита судит НАЛИЧИЕ записи вместо ИСХОДА основного шага, и запасная учётка отвечает `401`. Любая из двух половин, починенная отдельно, снимает форму A.

## 5. Кому принадлежит дефект — и это НЕ тред 155

Предмет треда 155 — словарь оснований парковки; переезд в него не берётся, и я его сюда не расширяю. Владелец — тред `193`, которым переезд влит (PR #399, `1b08b81cf`); класс «запасная учётка отвечает `401`» замерен на этом ящике дважды 13.09 и этим прогоном третий раз. **Своей рукой в тред 193 не пишу: этот подъём связан тредом `155` и только им.** Поэтому маршрут называю явно, чтобы факт не сгнил хвостом письма: **звоню тебе первой строкой (парк с `--expects ack` звонит сразу), а следующая сессия curator обязана довести предмет в тред 193 или открыть под него свой** — с таблицей §3 и разбором §2 как первоисточником замера.

## 6. Чего этот ход не делал

- **кнопку не жала ни по одному PR** — по #388 гард 4 STOP по построению, и гарды 1, 2 к тому же красные;
- **метку не перевешивала.** Новый круг стоит денег и квоты, а поднимать его до решения о переезде — жечь вслепую (§4, путь B); метка `review` на #388 остаётся висеть с текущей головы, события она сама по себе не даёт;
- **головы PR не двигала, ветку не трогала.** `origin/main` за весь ход стои́т на `6312dfbfb`, ветка `curator/155-park-ground-vocabulary-canon` — на `0169bea8c…`, ребейза не потребовалось;
- **открытые PR перечислены целиком** (`gh pr list --state open`, `18:52:40Z`): #415, #414, #413, #412, #411, #410, #409, #408, #406, #388, #386 — треды 191, 200, 191, 199, 197, 197, 196, 195, 180, **155**, 180. Свой кандидат к кнопке один — #388, и он твой; по остальным след гарда 5 обязан лечь в ленты ИХ тредов;
- приоритет треда не трогала — он уже `high`, возвращён письмом `18:35:14Z`;
- кода, тестов, конфига и воркфлоу не касалась; сюиту в рабочем дереве роли не гоняла; чужих тредов, парков и меток не трогала; чужого прогона в foreground не ждала;
- **рабочее дерево роли ЧИСТОЕ, незакоммиченного нет.** Тело этого письма — в `mktemp -d -p /tmp` вне обоих чекаутов.

## 7. Парк — и он же первое употребление словаря, который #388 записывает в канон

Паркуюсь на тебе: `--parked-on john`, **`--park-ground until-pr-merged:388`**. Основание объявляю машинной формой намеренно — это ровно тот механизм, который данный PR кладёт в канон, и его повод проверяем без моей памяти: пока #388 не влит, парк жив; влился — тик спросит это сам и скажет, что основание отпало. `--expects ack` звонит сразу, поэтому вопрос стои́т первой строкой, а не в конце разбора.

**Что делает следующая сессия — по исходу твоего ответа:**

1. **«жми» (путь A)** — кнопку жмёшь ты (гард 4), сессия кладёт след гарда 5 в эту ленту, называя вердикт, голову `0169bea8c…`, зелёный `checks` `34774970085` и красный `review` `34775456728` **как есть**, без замазывания;
2. **«новый круг» (путь B)** — снимает метку, вешает заново на ту же голову (голову не двигать: вердикт относится к коммиту), паркуется на `run:<N>` нового круга;
3. **«сперва починка» (рекомендация §4)** — #388 стои́т под парком на тебе, а предмет переезда уезжает в тред 193 постановкой роли `dev-core` (§5);
4. **в любом из трёх** — довести §2/§3 до владельца предмета переезда, иначе замер, стоивший этого хода, пропадёт.

waiting-on → curator.

