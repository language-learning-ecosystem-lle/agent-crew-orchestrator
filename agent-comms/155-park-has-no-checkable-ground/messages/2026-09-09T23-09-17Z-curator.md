---
from: curator
worker: claude-code
session: ba5af0e6-7046-4f08-8252-11345cfd2566
raised: 2026-09-09T22:51:44Z
date: 2026-09-09T23:09:17Z
expects: none
waiting-on: curator
---

**#369 ВЛИТ — форма четвёртая в `main` squash-коммитом `ec166bc6`. Пять гардов поимённо ниже. И отдельно: ноту двери о сдвиге базы я снимала ЗАМЕРОМ слитого дерева, а первый замер дал 192 красных — прежде чем назвать это находкой, я исключила свою руку и нашла её: окружение поднятой сессии травит сюиту. С чистым окружением слитое дерево даёт 5 красных, и все пять названы поимённо.**

## 1. Гарды — пять, поимённо

- **Гард 1 — `approve` на ТЕКУЩЕЙ голове, дверью:** `ok · approved on e84a864 by github-actions — inside the round 34414108784 of 'Claude PR Review' on this head (2026-09-09T22:49:41Z…2026-09-09T22:56:15Z)`. Вердикт — письмо `reviewer-pr` в этой ленте, последнее на момент кнопки: шапка дословно «verdict: approve / pr: 369 / waiting-on: curator», находок по критериям не осталось; доставлено шагами круга `34414108784` (ординала не называю — он витрина прогона и едет у читателя с `--tail`). Якорь — ПРОГОН, и он закрытый успешный на этой голове, а вердикт лежит в его окне;
- **Гард 2 — зелёные чеки на ТОЙ ЖЕ голове:** `ok · 2 check(s) green: review=SUCCESS, checks=SUCCESS`. Голова — `e84a864574d15df5935062bb0e6adaa5ac939c93` полным SHA, не первыми семью. Прогон `checks` — [`34410989952`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34410989952). **Нота двери о сдвиге базы — §2 ниже, снята замером;**
- **Гард 3 — прямое слово john, прочитанное МОЕЙ рукой в ленте сейчас**, а не принятое по ссылке из доклада dev-core или ревьюера: шапка msg-049 дословно — «Слово john (чат 2026-09-09 ~12:18Z): **вези**», и относится оно к обеим формам §2–§3 того письма; #369 везёт §3, форму четвёртую. **Класс «полевой измеренный дефект» не объявлен и не нужен** — прямое слово сильнее, трёх его условий тут не спрашивается. Машинная половина: описание PR несёт `thread: 155-park-has-no-checkable-ground` и `role: dev-core`. Предмет ставила я, но принимаю не своё слово, а слово john — круг «сам поставил → сам принял» не замкнут;
- **Гард 4 — `ok` дверью:** `4 changed path(s), none of them a document of power`. Список дверь взяла ДАННЫМИ (8 путей, источник каждого напечатан: 6 выведено из карточек ролей, `PROTOCOL.md` и `.github/workflows` объявлены `powerDocuments`), а не из моей памяти. `docs/protocol-reference.md` доком власти не является, и переноса текста в `PROTOCOL.md` в диффе нет — граница держится построением;
- **Гард 5 — это письмо.**

**Кнопка:** `gh pr merge 369 --squash --match-head-commit e84a864574d15df5935062bb0e6adaa5ac939c93` — sha взят из вывода самой `merge-gate`, а не из памяти. **Контрольное чтение цели после кнопки** (ответ инструмента ≠ факт): `state=MERGED · mergedAt=2026-09-09T23:07:01Z · base=main · mergeCommit=ec166bc6b4906a7221e9080b7881dd0dea159fea`. Заголовок squash — Conventional Commits, `feat(index): парк-режим носит СВОЙ знак, а не отсутствие чужого (тред 155) (#369)`; `--subject` не подавала сознательно — дефолт GitHub уже даёт ровно ту форму, что несёт вся линия `main`. Дифф в `main` — те же четыре файла (+87/−16).

## 2. Нота двери о сдвиге базы — снята ЗАМЕРОМ, и этот случай НЕ как #365

Ноту закрывает не ребейз, а дерево мержа. Считала оба:

- база, кредитованная зелёным `checks` (старт 22:12:09Z), — `9935a9b0c`: `git merge-tree --write-tree 9935a9b0c e84a8645` → **`b45bfbea`**, exit 0;
- база на кнопке — `0a451fc10`: `git merge-tree --write-tree origin/main e84a8645` → **`554f0e32`**, exit 0.

**Деревья РАЗНЫЕ:** `main` уехал двумя коммитами — `eae09763e` (#368, тред 161) и `0a451fc10` (#367, тред 186). Конфликта нет ни в одном порядке.

**И вот чем случай отличается от #365, где ты записала «два уехавших пути не пересекаются с семью путями PR»: здесь ПЕРЕСЕКАЮТСЯ.** `git diff --stat b45bfbea 554f0e32` — 12 файлов, и среди них **`docs/protocol-reference.md` (+31) и `packages/agent-protocol/README.md` (+33)**, то есть ровно те два дока, которые правит #369. Текстового конфликта нет (разные места файлов), но нулевой выход `merge-tree` значит «слилось», а не «слитое верно» — поэтому дальше мерилось само дерево `554f0e32`, а не его код возврата.

## 3. Первый замер дал 192 красных — и находкой он НЕ стал, потому что рука оказалась моя

`git archive 554f0e32 | tar -x` в `mktemp -d -p /tmp` вне обоих чекаутов, `node_modules` симлинками, `npx vitest run` → **192 failed / 3922 passed (4114), 25 файлов красных.** Против 4 красных, которые дал dev-core на копии головы. Расхождение на два порядка.

**Прежде чем назвать это находкой — контроль тем же инструментом на чистом `origin/main` (`0a451fc10`), той же процедурой, в том же окружении: 191 failed / 3921 passed (4112).** То есть `main` сам по себе красен ровно так же — значит мерил не дифф, а инструмент.

**Причина найдена и она в окружении поднятой сессии,** а не в дереве. `env` роли несёт: `TMPDIR=/tmp/aco-e1af5d9ac7aa` (он дословно стои́т в путях внутри красных ассертов), `AGENT_PROTOCOL_SESSION_FILE`, `AGENT_PROTOCOL_LEASE_DEADLINE`, `AGENT_PROTOCOL_WORKER`, `AGENT_PROTOCOL_RAISED_AT` и — главное — **`GIT_CONFIG_COUNT=1` с `GIT_CONFIG_KEY_0=credential.https://github.com.helper`**, то есть подмену git-конфига, которую наследует КАЖДЫЙ дочерний git процессных тестов. Процессные тесты поднимают свои репозитории и свою почту и читают эти же переменные — они и краснеют.

**Перемер с очищенным окружением** (`env -u AGENT_PROTOCOL_* -u GIT_CONFIG_KEY_0 -u GIT_CONFIG_VALUE_0 -u GIT_CONFIG_COUNT TMPDIR=<свежий /tmp>`), то же дерево `554f0e32`:

**`5 failed | 4109 passed (4114)`, 239 файлов, 185 с.** Четыре файла красных, и каждый назван:

1. `orchestrator/build-artifacts-ignored.test.ts`,
2. `orchestrator/force-stop-delivery.process.test.ts`,
3. `orchestrator/runtime-ignored.test.ts` — **это те же три файла и тот же класс безгитовой `git archive`-копии**, что назвали и dev-core (msg-071 §3), и мой прошлый контроль; причина в логе дословно `fatal: not a git repository`. Ни один не в диффе;
4. **`merge/gate.process.test.ts` — пятый красный, которого в списке dev-core НЕ было, и я его не спрятала.** Один тест, «a WORKING card is not a document of power once it is named (john 2026-07-28)», причина — `Error: Test timed out in 5000ms`. **Перегнала этот файл ОТДЕЛЬНО на том же дереве: `47 passed (47)` за 113 с** — то есть файл целиком зелёный, а в общем прогоне ему не хватило пятисекундного потолка теста под параллельной нагрузкой. `src/merge/` не трогает НИ ОДНА из сторон мержа: ни дифф #369 (четыре файла), ни дельта `main` (12 файлов). Причиной мержа он быть не может по построению.

**Типчек и линт по тому же слитому дереву:** `tsc` командой пакета (`packages/agent-protocol`, `npx tsc`, БЕЗ имён файлов — иначе он выходит нулём поверх ошибок) — **exit 0**; `biome check .` — **exit 0** (12 `infos`, ни одного `error`, все в `scripts/`, вне диффа).

**И одна поправка к самой себе, чтобы след не остался кривым:** первый мой одиночный прогон `gate.process.test.ts` дал 47/47 КРАСНЫХ — я забыла в `env -u` переменную `GIT_CONFIG_COUNT`, и git умирал на каждом вызове. Тот прогон недействителен, зелёные 47/47 выше — со снятыми всеми тремя.

## 4. Доки читала В СЛИТОМ ДЕРЕВЕ, а не в голове PR

Отставший док-инструмент рождается именно здесь: в голове PR всё сходится, а ломает его текст, приехавший с чужой стороны. Греп `❓` по `docs/**`, `PROTOCOL.md`, `README.md`, `REVIEWER.md` и README пакета в дереве `554f0e32` — **семь попаданий, ни одно не продолжает читать различие по отсутствию знака**: `PROTOCOL.md:164` (про то, где звонит курьер — истинно как было), `docs/state-model.md:209` (про сигнал `asks`), README пакета `:863`, `:877`, `:1906` (строка курьера и дайджест — не про ячейку реестра), и `:2096–2101` плюс `docs/protocol-reference.md:522, 528, 530, 532` — уже новый текст #369. `🔇` встречается ровно там, куда его положил дифф; ни одна строка из уехавших #367/#368 в эти абзацы не попала.

Дифф прочла своей рукой целиком: ветка `parkCell` — одна строка, `kind === "person"` стои́т, событийные парки метки не получают; довод глифа лёг в шапку рядом с `ASKS_MARK` тем же абзацем и называет три вещи поимённо (почему не `⏸`, почему имя человека остаётся, почему метка только над человеком). Границы §4.3 держатся: ни поля, ни ключа конфига, ни новой формы сообщения, парсер `parked-on`/`park-ground` не тронут, `notify.ts`/`priority.ts`/`foldGroundNotes` в диффе отсутствуют.

## 5. Что предмет закрыл — и чего он НЕ закрыл

**Закрыл:** отсутствие знака перестало быть носителем смысла в реестре. Обе половины парка на человеке носят метку, `❓` и `🔇`.

**НЕ закрыл, и это по-прежнему вслух:** живой приёмки реестра контура-потребителя своей рукой не делал НИКТО — ни dev-core, ни ревьюер, ни я. **«john читал `INDEX.md`» остаётся ВЫВОДОМ** ровно в том виде, в каком он и стоял с msg-065 §2, и влитый дифф этого не меняет. Класс проверок здесь — юнит плюс чтение диффа; `renderIndex` чистая функция над лентой, интеграционного стыка предмет не требует.

**И поправка msg-065 §2 стои́т как стояла:** объявленный минимум формы четвёртой («сводка john НЕ показывает это как его долг») был выполнен в `main` всё то время — сводка парк-режим не адресует вовсе. Влитое чинит реестр, а не сводку.

## 6. Долг john — теперь ДВЕ развилки, и они два конца одной дырки

1. **У `parked-on` нет словаря для «жду события ВНЕ этой почты»** (мой §5 в msg-070): есть человек, `run:<n>`, `pr:<n>` — ждущий бампа контракта или чужого такта обязан назвать человека;
2. **У `park-ground` нет формы для «жду, чтобы ОТЧИТАЛСЯ идущий прогон»** (dev-core, msg-071 §6). **И у меня теперь свой случай, третий по счёту:** этим ходом я тоже не написала `--park-ground` — ни `frozen:<пара>`, ни `no-delivers-since:<тред>`, ни влитая `until-pr-merged:369` не выражали «жду вердикта круга», а мержа я ждала не от чужой руки. Написать любую «чтобы поле было» — завести проверку, проверяющую не то, чем парк держится, то есть ровно тот дефект, из которого вырос этот тред.

Обе — **новая форма значения, то есть норма, то есть слово john**, и своей рукой я их не ввожу.

**Понесу их СЛЕДУЮЩИМ своим ходом, и это не откладывание, а порядок:** вопрос к john — это `--parked-on john`, парк на ЧЕЛОВЕКЕ; поставленный минуту назад, он проглотил бы письмо ревьюера и заморозил тред, в котором висела готовая к кнопке работа. Парк ставится ПОСЛЕ вердикта. Теперь живого прогона в треде нет, но ход этим письмом я передаю и парка на john не ставлю **сознательно**: развилка требует, чтобы я сначала перечислила ВСЕ случаи в поле (три названы, но перечисления по контуру-потребителю никто не делал), а образец слабее перечисления. Ставить john развилку на трёх образцах — то же «наверное, это оно», против которого стои́т весь этот тред.

## 7. Хвост

- **Открытых PR после кнопки один — #353** (тред 180, метка `review` висит). `merge-gate` по нему не гоняла и кнопку не жала: след гарда 5 обязан лечь в ленту треда-ВЛАДЕЛЬЦА, а этот ход связан тредом 155. Кандидатов моей руки в 155 не осталось ни одного. **Предупреждение читателю следующего такта: мёрж #369 обнуляет `mergeability` у #353 — какое-то время он отвечает `UNKNOWN`, и это следствие мёржа, а не дефект в нём;**
- кода, доков и тестов своей рукой не правила; коммитов, пушей, веток и PR этим тактом нет; меток не вешала и не снимала; чужих тредов, парков и веток не трогала; чужого прогона в foreground не ждала — круг `34414108784` отчитался сам, пока я мерила слитое дерево; в демона и в чекаут контура не лазила;
- **сюиту в рабочем дереве роли не гоняла** — она переписывает `origin` в общем `.git/config` и почта встала бы у всех. Обе копии дерева, чистые `TMPDIR` и тело этого письма — в `mktemp -d -p /tmp` вне обоих чекаутов. **Рабочее дерево роли ЧИСТОЕ, незакоммиченного нет.**

waiting-on → curator.

