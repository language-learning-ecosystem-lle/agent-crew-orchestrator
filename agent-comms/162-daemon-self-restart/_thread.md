# Стоячий адрес: демон перезапустил себя на новый код

participants: github, curator, john · status: open

## msg-001 · from: github · 2026-09-07 · expects: none

## Демон перезапустил себя на новый код — без руки, и вот чего это стоило

Ящик дождался конца живых сессий, починил своё дерево и поднялся на новом коде. Ход никому не нужен для ремонта — он уже сделан; это отчёт о нём, потому что тихий самоперезапуск ничем не лучше тихого дрейфа.

- **контур:** `/home/lle/projects/agent-crew-orchestrator`
- **какой код был:** `f9562234cd5f`
- **какой стал:** `c9a9ca422467`
- **сколько отставал:** 1 коммит(ов)
- **сколько ждал сессии:** 4 мин (293 с)
- **когда пошёл:** 2026-09-07T15:46:42Z

**Ход curator — ровно на одно действие:** прочитать это и, если отчёт полон, донести john. Ремонта здесь нет: дрейф уже закрыт, а звонок о дрейфе (тред 141, #301) на этот ящик больше не придёт.

## msg-002 · from: curator · 2026-09-07 · expects: ack

**Требуется ack john** (решения не требуется): отчёт ящика о самоперезапуске проверен фактами — все шесть чисел сошлись дословно, дрейф закрыт, ящик работает на новом коде. Доношу с тремя фактами, которых в самом отчёте нет.

## 1. Что в отчёте проверено и чем

Сверял машинным состоянием, не памятью:

| заявлено в отчёте | чем сверено | сошлось |
| --- | --- | --- |
| было `f9562234cd5f` | `.orchestrator/self-restart.json` → `"from":"f9562234cd5f0d24699777c9ef8c77010adccc2f"` | да |
| стало `c9a9ca422467` | там же `"target"`; `.orchestrator/daemon-code.json` → `"sha":"c9a9ca4224…"` | да |
| отставал 1 коммит | `"behind":1` | да |
| ждал 4 мин (293 с) | `"drainSince":"2026-09-07T15:41:49Z"` → `"at":"2026-09-07T15:46:42Z"` = 293 с | да |
| пошёл 2026-09-07T15:46:42Z | `"at"` дословно | да |
| контур | `"checkout":"/home/lle/projects/agent-crew-orchestrator"` | да |

**Ящик действительно исполняет новый код, а не просто «перезапустился»:** `daemon-code.json` — `sha c9a9ca4224…`, `startedAt 2026-09-07T15:47:01Z`, `pid 1520784`; systemd — `MainPID=1520784`, `ActiveState=active/running`, `NRestarts=1`, `ExecMainStartTimestamp Mon 2026-09-07 17:46:53 CEST` (= 15:46:53Z). Загруженная sha = HEAD чекаута = `origin/main` = `c9a9ca422467f102224113208574fc374aff500b`, дерево чистое.

**Дрейф закрыт замером, а не выводом:** после баннера подъёма в `.orchestrator/daemon.log` нет ни одной строки `LOADED CODE is not the ref` — единственное совпадение оказалось эхом моего же грепа в транскрипте этой сессии. Контроль, что шаблон вообще ловит: до баннера тех же строк 326.

## 2. Чего в отчёте нет (не делает его ложным)

**а) «Починил своё дерево» — это две названные операции, и обе дешёвые.** `git pull --ff-only` → `Fast-forward`, ровно один файл `packages/agent-protocol/package.json` (+1/−1), затем `pnpm install` → `Already up to date`, 532 мс. Дерево было уже чистым: блокера старого класса (untracked-файл, из-за которого ремонт отказывал в треде 141) в этот раз не было вовсе.

**б) Механизм ухода и несущая зависимость — в отчёте не названы.** Демон увидел, что он под супервизором (`INVOCATION_ID` выставлен), починил дерево НА МЕСТЕ, ничего не породил и **вышел кодом 75**, оставив подъём преемника юниту. Держится это на одной строке юнита, и я её замерил: `Restart=on-failure` при пустом `SuccessExitStatus=` — значит 75 читается как провал и подъём происходит; `NRestarts=1` подтверждает, что супервизор действительно поднял. Ящик, где `Restart=` не стоит, тот же выход оставит ЛЕЖАЩИМ — демон сам это в логе и говорит («an exit that failed to be replaced must not look like a clean shutdown»). Юнит — машинный конфиг вне репозитория, поэтому я это только называю, не трогаю.

Побочно замерено там же: `StartLimitIntervalUSec=5min`, `StartLimitBurst=5`, `RestartUSec=10s` — пять подъёмов за пять минут, и супервизор откажет. Путём самоперезапуска это недостижимо (попытки капнуты, в логе `attempt 1/2`, а дренаж занимает минуты), так что это предохранитель от краш-петли, а не риск этого механизма.

**в) Цена названа не вся.** «Ждал 4 мин» верно, но в эти минуты ящик ещё и НИКОГО НЕ ЗАПУСКАЛ: два такта с уже готовым планом ушли в ноль (`curator×141`, затем `curator×141 + dev-core×158` остались в очереди). При этом **пары не съедены** — ни одной не записана неудачная попытка, и `curator×141` был снова спланирован сразу после подъёма. И никого не порвали: `dev-core×063-state-model-rewrite` доработала сама (`the run finished: completed`), дренаж начался в 15:41:49Z именно из-за неё — 293 с это её хвост.

## 3. Моя оценка и рекомендация

Отчёт **точен по числам и полон в том, что заявляет**. Не полон он в двух местах, и оба — про содержание письма ящика, а не про ремонт: «починил своё дерево» и «ждал 4 мин» стоят прозой там, где у ящика есть факты (какая именно починка; сколько тактов ушло в ноль), а несущая зависимость механизма — `Restart=` на юните — не названа вовсе, хотя именно она отделяет «поднялся сам» от «лёг молча».

**Рекомендация curator (не решение):** дописать эти три пункта в форму письма о самоперезапуске — постановкой `dev-core`, поскольку `packages/**` не моя зона. Без твоего слова постановки не будет: ремонт тут уже сделан, а правка формы отчёта — это изменение поведения контура, то есть норма, а не починка.

Ход остаётся на мне, тред запаркован на тебе. Ремонта здесь действительно нет — ждём только ack.

## msg-003 · from: curator · 2026-09-08 · expects: none

**Слово john полем `delivers` (чат 2026-09-08 ~09:21Z). ОДИН ОТВЕТ НА ЧЕТЫРЕ АДРЕСА: `162`, `164`, `165`, `167` — все про один класс событий, и все четыре закрываются этим письмом. Три из них просили `ack`, один — решения; отвечено на оба вида.**

## 1. Ack по существу: отчёты приняты

Ящик чинил дрейф сам, без руки, и говорил об этом. По всем четырём адресам: **факт принят, приёмка засчитана.** Отдельно засчитано то, что роль **перепроверяла отчёты фактами**, а не пересказывала их — включая случай, где письмо было полно по числам и неполно по смыслу.

**И главное, что john отмечает вслух:** предмет, ради которого 06.09 заводился тред `141`, СРАБОТАЛ полем — вчера дрейф в 22 часа требовал человека, сегодня ящик дождался живой сессии и перезапустился сам. Это первое живое подтверждение «ожидания без руки».

## 2. Решение по вопросу `164` — оно уже дано и повторяется здесь

Вопрос «оставить триггер на счёте коммитов или сузить письмо» john ответил 07.09 ~18:23Z: **сужаем ПИСЬМО, ремонт не трогаем.**

- **перезапуск — на любом дрейфе.** Счёт коммитов дёшев и не врёт; судить дифф внутри безопасного условия значит внести туда мнение;
- **письмо — только когда перезапуск сменил ИСПОЛНЯЕМОЕ.** Признак — на замер исполнителя (кандидат: версия установленного пакета до и после);
- **работа поставлена** в этом контуре (`161`, ~18:23Z), формулировка и границы там же.

## 3. И ЦЕНА, ради которой это решение и принималось — она теперь измерена

**Стоячий адрес самоперезапуска породил ВОСЕМЬ тредов за сутки:** `156`, `160`, `161`, `162`, `163`, `164`, `165`, `167`. Четыре из них дошли до john и ждали его слова.

**Каждое письмо назначает ход роли, то есть стоит подъёма.** Отчёт о перезапуске, который не поднял ни строки исполняемого кода, куплен подъёмом пары — и так восемь раз.

**Это не дефект устава стоячих адресов** (он работает как задуман: событие → дом → разбор → закрытие), а прямое следствие слишком широкого повода писать. Сужение §2 снимает именно это.

## 4. Что john НЕ просит менять

- **устав адресов** — самовозобновляемый приёмник остаётся;
- **сам факт отчёта** — «тихий самоперезапуск ничем не лучше тихого дрейфа» (решение 06.09) в силе; сужается повод, а не отчётность;
- **условие ремонта** — не трогать.

## 5. Что делать с четырьмя адресами

**Закрывать все четыре** — предмет в каждом исчерпан этим письмом. Если после сужения письма событие снова откроет адрес, это будет уже сообщение о смене исполняемого кода, а не о факте перезапуска.

waiting-on → curator.
