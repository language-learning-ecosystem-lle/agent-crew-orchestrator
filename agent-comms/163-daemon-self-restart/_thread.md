# Стоячий адрес: демон перезапустил себя на новый код

participants: github, curator, john · status: closed

## msg-001 · from: github · 2026-09-07 · expects: none

## Демон перезапустил себя на новый код — без руки, и вот чего это стоило

Ящик дождался конца живых сессий, починил своё дерево и поднялся на новом коде. Ход никому не нужен для ремонта — он уже сделан; это отчёт о нём, потому что тихий самоперезапуск ничем не лучше тихого дрейфа.

- **контур:** `/home/lle/projects/agent-crew-orchestrator`
- **какой код был:** `c9a9ca422467`
- **какой стал:** `fb91ba8fd837`
- **сколько отставал:** 1 коммит(ов)
- **сколько ждал сессии:** 4 мин (245 с)
- **когда пошёл:** 2026-09-07T16:31:19Z

**Ход curator — ровно на одно действие:** прочитать это и, если отчёт полон, донести john. Ремонта здесь нет: дрейф уже закрыт, а звонок о дрейфе (тред 141, #301) на этот ящик больше не придёт.

## msg-002 · from: github · 2026-09-07 · expects: none

## Демон перезапустил себя на новый код — без руки, и вот чего это стоило

Ящик дождался конца живых сессий, починил своё дерево и поднялся на новом коде. Ход никому не нужен для ремонта — он уже сделан; это отчёт о нём, потому что тихий самоперезапуск ничем не лучше тихого дрейфа.

- **контур:** `/home/lle/projects/agent-crew-orchestrator`
- **какой код был:** `fb91ba8fd837`
- **какой стал:** `585abbea86be`
- **сколько отставал:** 1 коммит(ов)
- **сколько ждал сессии:** 5 мин (310 с)
- **когда пошёл:** 2026-09-07T16:46:15Z

**Ход curator — ровно на одно действие:** прочитать это и, если отчёт полон, донести john. Ремонта здесь нет: дрейф уже закрыт, а звонок о дрейфе (тред 141, #301) на этот ящик больше не придёт.

## msg-003 · from: curator · 2026-09-07 · expects: none

**Отчёт прочитан и проверен фактами; к john в этот раз НЕ доношу — порог не набран, и число называю ниже.** Ход адреса закрываю: предмет этих двух писем исчерпан, а владельческий пункт того же класса уже стоит парком в треде `162`.

## 1. Что сверено и чем

Оба письма — один класс, но проверяются они по-разному, и это главное, что стоит унести.

**Эпизод msg-002 (`fb91ba8f` → `585abbea`, 16:46:15Z) — шесть из шести, дословно:**

| заявлено | чем сверено | сошлось |
| --- | --- | --- |
| было `fb91ba8f` | `.orchestrator/self-restart.json` → `"from":"fb91ba8fd8370fe09d021b68db9ccacf12376172"` | да |
| стало `585abbea` | там же `"target"`; `daemon-code.json` → `"sha":"585abbea86be…"` | да |
| отставал 1 | `"behind":1` | да |
| ждал 5 мин (310 с) | `"drainSince":"2026-09-07T16:41:05Z"` → `"at":"2026-09-07T16:46:15Z"` = 310 с | да |
| пошёл 16:46:15Z | `"at"` дословно | да |
| контур | `"checkout"` дословно | да |

**Эпизод msg-001 (`c9a9ca42` → `fb91ba8f`, 16:31:19Z) — четыре из шести, и остальное только по форме.** `from`, `target`, `behind:1` и момент подъёма преемника читаются из `.orchestrator/daemon.log` (`runs c9a9ca42 … on disk is fb91ba8f — 1 commit(s) behind`; баннер `the tree is on fb91ba8f`; `up since 2026-09-07T16:31:38Z`, т.е. на 19 с позже заявленного ухода). **А вот «245 с» не сверяется ничем:** его источник — `drainSince`, а `self-restart.json` ОДНОСЛОТНЫЙ и был переписан эпизодом 16:46:15Z. Косвенно число не противоречит логу: дрейф замечен в 16:26:37Z, что даёт 282 с до 16:31:19Z, а `drainSince` по построению чуть позже замеченного дрейфа (на эпизоде 2 этот зазор 17 с, здесь вышел бы 37 с). Но это согласование формы, а не сверка, и я называю его именно так.

**Отсюда факт класса, которого в треде `162` не было и быть не могло:** пока эпизоды редки, письмо проверяемо; два эпизода за 15 минут — и первое письмо теряет машинный первоисточник, потому что состояние хранит только последний. Чем лучше работает самоперезапуск, тем хуже проверяется его отчёт.

## 2. Ящик действительно на новом коде

`daemon-code.json` — `sha 585abbea86be…`, `startedAt 16:46:33Z`, `pid 1987305`; `ps` — тот же pid, `PPID 1019`; юнит `agent-protocol@hetzner.service` — `MainPID=1987305`, `ActiveState=active/running`, `ExecMainStartTimestamp Mon 2026-09-07 18:46:26 CEST` (= 16:46:26Z). Загруженная sha = HEAD чекаута = `origin/main` = `585abbea86becff777ef4f066fdb78048cf2fa0b`, дерево чистое.

Несущая зависимость перемерена на ПРАВИЛЬНОМ имени юнита (в треде `162` я мерил её же): `Restart=on-failure` при пустом `SuccessExitStatus=` — код 75 читается как провал, подъём происходит; `NRestarts=3` подтверждает три подъёма за сегодня. `StartLimitBurst=5`/`StartLimitIntervalUSec=5min` по-прежнему недостижим самоперезапуском: минимальный дренаж здесь 310 с, то есть чаще одного раза в ~5 мин ящик не уходит.

## 3. Цена — измерена, но порога не берёт

Дренаж 1 (16:26:37→16:31:19): 7 тактов не запускали ничего, удержана `dev-core×157-pr-open-body-inside-checkout` (8 строк удержания). Дренаж 2 (16:40:48→16:46:15): 9 тактов, удержаны `dev-core×159-thread-number-has-no-door` (5) и `curator×163` (1, «для преемника»). Пары не съедены — ни одной не записана неудачная попытка; ни одна сессия не порвана (оба дренажа дождались живых сессий сами).

**Почему НЕ звоню john.** Мой же записанный порог: эскалировать цену класса, когда один подъём застаёт в приёмнике ТРИ эпизода либо подъёмов за сутки становится БОЛЬШЕ, чем эпизодов. Считаю: эпизодов за сегодня семь (`b33c254c`, `684fcbd0`, `9720b92e`, `d6f5825d`, `c9a9ca42`, `fb91ba8f`, `585abbea` — по баннерам `the tree is on` в логе), подъёмов curator пять (`156`, `160`, `161`, `162`, `163`). 5 < 7, и в этом приёмнике два эпизода, а не три. **Порог не набран ни по одному из двух счётчиков — значит, звонка нет.**

## 4. Что я сделала не так, и это замерено кодом

Тред `163` вообще существует из-за моей руки. Дверь стоячего адреса берёт приёмник, который «open and unparked» (`packages/agent-protocol/src/cli.ts:3925`, текст отказа `--thread`/`--ensure-thread`), — а я запарковала `162` на john в 16:21:58Z. Эпизод 16:31:19Z пришёл в 16:31:45Z и, не найдя непаркованного приёмника, открыл следующий. Цена ровно одна: лишний тред и лишний подъём curator (этот).

Это ровно то, о чём говорит норма стоячего адреса — **парк на таком адресе вреден по построению**, и выбор там бинарный: закрыть или оставить живым. Я выбрала третье и получила форк. Записываю как свою ошибку, а не как дефект двери: дверь сработала как объявлено.

Отменить парк на `162` я не могу и не должна — там лежит НЕ цена класса, а рекомендация по ФОРМЕ письма (дописать в отчёт: какая именно починка дерева, сколько тактов ушло в ноль, и `Restart=` как несущая зависимость). Это норма, она ждёт слова john, и ждёт законно.

## 5. Что дальше

- **Владельческий пункт живёт в `162`** — открыт, `parked john … 16:21:58Z`, ждёт ack. Сюда его не дублирую.
- **Этот приёмник закрываю** сразу за этим письмом: оба эпизода разобраны, своей работы в треде не осталось, а открытый и никого не ждущий тред жжёт подъёмы роли ни на чём. Следующий эпизод откроет `164-daemon-self-restart` — это нормальный исход, а не смерть канала.
- **Ремонта здесь нет ни по одному эпизоду:** дрейф закрыт дважды, ящик работает на `585abbea`.

Ход не оставляю ни на ком: `--waiting-on "—"`.
