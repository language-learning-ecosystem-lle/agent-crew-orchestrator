---
from: curator
worker: claude-code
session: 40ad9ebf-326b-4c61-9465-634561ef27aa
raised: 2026-09-03T23:51:46Z
date: 2026-09-03T23:56:46Z
expects: none
waiting-on: curator
parked-on: run:263
---

**Постановка msg-020 §4 выполнена: право «поле повода + обязательная проверка» приземлено в документы, которые роль читает как свой промпт. PR [#263](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/pull/263) открыт, голова `8acc534e`. Гард 4 дал `STOP` — это назначенный приёмкой исход, а не авария: merge здесь кнопка john.**

## 1. Что написано и где

Пять файлов, кода ноль, все markdown:

- **`PROTOCOL.md`** — новый раздел «Память роли: повод заметки — ОТКРЫТЫЙ предмет, а не тред рождения (слово john 2026-09-03, тред `090-what-kills-a-note`)». Норма стои́т ОДНА и здесь, потому что она даёт роли ПРАВО;
- **`docs/roles/curator.md`, `dev-core.md`, `devops.md`, `pilot-codex.md`** — раздел «Память роли — повод заметки»: ссылка на норму и на `docs/protocol-reference.md` (#240) плюс четыре пункта коротко, строка-проверка — дословно.

Развилку «одним куском в четырёх карточках или норма + ссылка» решил так, как объявляла постановка: **норма одна, карточки ссылаются**. Копия нормативного текста в четырёх файлах раздваивается на первом же уточнении, а карточка обязана нести ровно то, чего по ссылке роль может не прочитать, — строку-проверку.

Что говорит текст (пункты 1–4 постановки, поимённо): право на поле `thread: <NNN-slug>` во фронтматтере под `metadata:`; **«предмет закрыт — заметка стала бесполезной? ДА → это повод; НЕТ → поля не писать вовсе»**; повод — предмет, ПОКА ОТКРЫТЫЙ которым заметка полезна, а не место, где её узнали; упоминание треда в ТЕЛЕ поводом не является. В `PROTOCOL.md` к ним добавлено ЧИСЛО, ради которого проверка и стои́т, — 22 % кучи `dev-core` (8 долгих заметок из 36 упоминают закрытый тред, замер msg-014), и абзац «чего это правило не делает»: сорт не уменьшает кучу из долгих заметок.

## 2. Приёмка — перемерена своей рукой, шаблон грепа сверялся с перечнем

Приёмка постановки требовала: все четыре пункта в каждой из четырёх карточек, **и шаблон грепа сверяется с перечнем, а не только своим выводом**. Сделано так:

- пять шаблонов — по одному на каждый пункт плюс цитата заголовка нормы — прогнаны по `docs/roles/*.md` **и по контрольному `README.md`**. По карточкам — 4 из 4 на каждом шаблоне, по контролю — **ноль**. То есть шаблон различает, а не просто молчит;
- первый прогон поймал СВОЙ дефект, а не чужой: цитата заголовка нормы была разорвана переносом строки и не находилась грепом. Перенос переставлен, цитата легла в одну строку — теперь правило проверяемо тем же способом, которым его будут проверять потом;
- `pnpm protocol zones check --ref origin/main --role curator --paths <все 5>` → `none under a forbidden prefix`;
- **гард 4 замерен дверью, а не предсказан:** `pnpm protocol merge-gate --ref origin/main --pr 263 --review-workflow 'Claude PR Review'` → `STOP guard 4 · john merges this one — it changes PROTOCOL.md, docs/roles/curator.md, docs/roles/dev-core.md, docs/roles/devops.md, docs/roles/pilot-codex.md`. Список из 8 доков власти дверь ВЫВЕЛА данными. Это и есть доказательство приёмки «правится именно док власти». Гарды 1 и 2 сейчас `STOP` штатно: вердикта нет, `checks=IN_PROGRESS` (прогон `33819632614`). Гард 3 — `you`: восхождение к слову john есть в этой ленте (msg-018 §1, «ПОЛЕ С ПРОВЕРКОЙ»), и PR я ставил себе сам — поэтому слово john здесь несущее, а не формальность.

## 3. Что СОЗНАТЕЛЬНО не вошло в дифф

**Слово john (б) — «потолок РАЗМЕРОМ, второго порога не заводить» (msg-018 §2) — я не приземлял.** Причина не в минутах: постановка msg-020 §4 предметом называла право на повод, а (б) — решение НЕ строить второй порог. Оно не даёт роли права, которого у неё нет, и ничего не меняет в поведении: мерило уже размерное в коде. Дописать его тем же PR значило бы расширить предмет двери без того, чтобы без починки дверь отказала. Если оно должно стоять документом — это отдельный ход и отдельная строка, и она пишется в тот же раздел `PROTOCOL.md`.

## 4. Приёмочный тест §3 — по-прежнему вооружён, и этим ходом я его не трогал

**В кучу памяти этим ходом не дописано ни одной заметки и ни одной не изменено.** Единственная заметка с полем повода во всей коробке — `dev-core/note-sort-has-no-subscribers-yet.md`, повод `090-what-kills-a-note`; закрытие этого треда обязано её погасить. Гасить больше нечего — подмены исхода не будет.

Напоминание из msg-020 §3 остаётся в силе: гашение случится на подъёме `dev-core` НА ДРУГОМ треде, и написать исход в `090` будет некому. Доказательство назначено файлами и строкой оглавления, не «посмотрите в ленту».

## 5. Состояние и что делает следующая сессия

- **#263** открыт (`pnpm protocol pr open`, не `gh pr create`), тело несёт `thread: 090-what-kills-a-note` и `role: curator`, голова `8acc534e`, `mergeable=MERGEABLE`, прогон `checks` **шёл** на момент письма;
- рабочее дерево роли **чистое**, незакоммиченного нет; ветка `docs/090-note-cause-right` запушена;
- хвост чужих PR не трогал по той же причине, что в msg-020: мой прогон связан одним тредом, след гарда 5 обязан лечь в ленту ТОГО треда;
- **ход следующей сессии — ровно одно действие: зелёный `checks` на `8acc534e` → повесить метку `review` и передать ход.** Метка — конец хода, а не начало ожидания. Если голова уедет (правки по вердикту) — метку снять и повесить заново: круг поднимает СОБЫТИЕ навешивания;
- **merge — кнопка john**, гард 4 `STOP` замерен. Мне на этом PR не принадлежит ничего, кроме метки и следа.

waiting-on → curator, parked-on → run:263 (жду зелёного `checks`/круга на своём свежем PR; следующий шаг после него — мой).

