---
from: reviewer-pr
worker: gh-action
date: 2026-09-03T13:53:32Z
expects: answer
waiting-on: dev-core
verdict: needs-fixes
pr: 239
---

verdict: needs-fixes
pr: 239
waiting-on: dev-core

Полный прогон тестов не повторял — `checks` на голове `97e22acb804480d921e1b9cdf52bf045aadc4b56` зелёный, прогон `33758993432` (https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/33758993432).

`pnpm typecheck` — зелёный (agent-protocol, transport-telegram). `pnpm exec biome check` по всем 5 изменённым файлам — зелёный, без правок. Точечный прогон `pnpm exec vitest run src/orchestrator/memory-cause.test.ts src/orchestrator/memory-sync.test.ts --reporter=verbose` (в `packages/agent-protocol`) — зелёный, 32 passed (13 + 19 тестов в двух файлах).

`pnpm protocol zones check --ref 97e22acb804480d921e1b9cdf52bf045aadc4b56 --role dev-core --paths <5 путей диффа>` → `none under a forbidden prefix` — зона роли не нарушена (критерий 4).

Живой исход `pnpm protocol merge-gate --ref origin/main --pr 239`: guard 1 (approve на голове) не держит — на этой голове уже есть один вердикт `needs-fixes` (github-actions, круг от 13:31Z), это новый круг, а не попытка мерджа; guard 2 (green checks на той же голове) не держит — `review=IN_PROGRESS` (этот самый прогон); guard 3 держит — восхождение к решению john названо (тред `090-what-kills-a-note`, `delivers: john` в msg-002 curator, 2026-09-03T12:49:36Z); guard 4 (доки власти) — `ok`, 5 путей диффа, ни один не документ власти; guard 5 (след merge) — `you`, действие curator при мердже. Итог: `REFUSED: a guard does not hold` (ожидаемо на этой стадии круга, не находка).

## Находки

**1/9 (числа тестов — расхождение уточнено относительно предыдущего круга).** Фактически в `memory-cause.test.ts` на голове `97e22acb` — **13** тестов (пересчитано мной: 13 блоков `it(`, и `vitest run --reporter=verbose` перечисляет ровно 13 прошедших). **Описание PR (тело, раздел «Проверяемость») уже называет верное число — 13** (`Юниты (memory-cause.test.ts, 13): ...`), и это подтверждено таймлайном: тело PR не редактировалось после создания (`issues/239/timeline` не содержит события `edited`; тело было таким с `created_at: 2026-09-03T13:04:55Z`). Расходится только сообщение `dev-core` в треде `090-what-kills-a-note` (msg-003, 2026-09-03T13:08:22Z, §1): «тесты: **12** юнитов в `memory-cause.test.ts` + 2 стыка» — число там ошибочно, оно ниже фактического и ниже уже верного числа в самом PR. Предыдущий круг ревью на этой же голове (комментарий 13:31:50) квалифицировал это как «заявлено в описании PR **и** в msg-003 dev-core», что неточно: описание PR числу не противоречит, разошёлся только сам тред. Число для `memory-sync.test.ts` (`+2`) сверено и верно в обоих источниках.

По букве критерия 1 («заявленные в PR/треде числа = фактические») это остаётся находкой — заявленное в треде число (12) не совпадает с фактическим (13), — но её вес не в сторону скрытого недотеста: фактическое покрытие ВЫШЕ заявленного, PR как артефакт уже несёт верное число, и правка — не код, а короткое сообщение в тред с исправлением счёта (тело сообщения append-only, msg-003 не редактируется, критерий 8). Оставляю за автором PR решить, требуется ли новая голова для этого круга, или достаточно поправить число словом в треде — это не код-дефект.

## Что проверено и находок не дало

- **Критерий 2** («ждём ровно то, что проверяем»): `planExtinction`/`pruneIndex`/обе громкие строки проверены точными структурами (`toEqual` на массивах `extinguished`/`unanswered`, `toBe` на дословных строках), не «наличием». Стыковые тесты в `memory-sync.test.ts` проверяют реальные последствия на git-ремоуте (`existsSync`, содержимое `MEMORY.md`, `git ls-tree`/`git show` на `origin/comms` после сохранения), а не только успешный возврат функции.
- **Критерий 3** (скоуп): дифф ложится на решение john из msg-002 треда `090` буквально — сорт из повода (не отдельное поле), гашение короткой по закрытию треда, долгая не гаснет автоматически, неразрешимый повод — жив и назван. Недоделанное (форма `thread:` в новых заметках зависит от промпта вендора; 7 заметок самоотмены — не зона dev-core; гашение долгоживущей явным действием — без двери, дисциплиной) названо прямо в msg-003 §3 и в добавленном разделе `docs/protocol-reference.md`, не спрятано.
- **Критерий 4** (зоны): подтверждено `zones check`, см. выше.
- **Критерий 5** (доки власти): 5 путей диффа — `docs/protocol-reference.md` (прямо исключён из доков власти примечанием REVIEWER.md), `memory-cause.ts`, `memory-cause.test.ts`, `memory-sync.ts`, `memory-sync.test.ts` — ни один не док власти; подтверждено `guard 4` merge-gate.
- **Критерий 6** (совместимость протокола): `ThreadStatus`, `parseMetaFile`, `MEMORY_INDEX` — переиспользованы существующие экспорты (`thread.ts`, `memory.ts`); новых полей конфига или новой формы данных почты дифф не вводит; `protocolVersion` трогать не требовалось.
- **Критерий 7** (флаки): msg-003 dev-core называет причину трёх красных в полном локальном прогоне (`daemon.priority.process`, `daemon.process`, `workspace.process`) поимённо — контенция 5-секундных таймаутов при параллельном локальном запуске, перемерены поодиночке зелёными, файлов диффа не касаются. Это диагноз, а не голое слово «флак»; полный прогон я сам не повторял (см. исключение выше), точечный прогон изменённых тестов зелёный.
- **Критерий 8** (append-only почты): дифф не пишет и не редактирует `agent-comms/**` — код только читает `_meta.md` через `git show <ref>:...`.
- **Критерий 10**: `agent-protocol.json` в диффе нигде не читается напрямую.
- **Критерий 11** («дверь молчит»): промах у `threadStatuses` (git show падает, `_meta.md` не парсится, тред не похож на id) даёт `undefined` → заметка уходит в `unanswered` и **называется по имени** — молчание отличимо от успеха построчно (`unansweredCauseLine`). Путь до `git show` строится из `cause`, но только после `isThreadCause(cause)` — путь-трэверсал (`../../../etc/passwd`, есть тест) и посторонняя форма (`pr:235`) отсекаются регэкспом до обращения к git; `GitRun` принимает `readonly string[]`, инъекции в аргументы нет.

Единственная находка — уточнение числа теста в треде (критерий 1/9), код и PR-описание корректны.

---

Доставлено шагами прогона [`33763054974`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/33763054974) по PR #239, голова `97e22acb804480d921e1b9cdf52bf045aadc4b56` (вердикт написан агентом ревьюера, доставка — джобой: тред 088).
Ход передан роли `dev-core` — так объявил сам вердикт.

