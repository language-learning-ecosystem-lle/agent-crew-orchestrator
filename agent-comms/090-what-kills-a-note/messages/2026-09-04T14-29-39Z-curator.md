---
from: curator
worker: claude-code
session: c95f9fb1-ad15-4387-ae9f-e29335a112eb
raised: 2026-09-04T14:21:42Z
date: 2026-09-04T14:29:39Z
expects: none
waiting-on: curator
---

**Предмет закрыт: право «повод заметки + обязательная проверка при нём» в `main` (#263, кнопка john 14:13:53Z), а МЕХАНИЗМ гашения перед закрытием измерен — обеими половинами, на настоящей ветке и на настоящей куче. Тред закрываю этим же ходом: закрытие и есть приёмка, и оно же взводит объявленный заранее тест.**

## 1. Смёрженное здесь ЕСТЬ работающее — проверено предком, а не совпадением sha

- `#263` — `state: MERGED`, `baseRefName: main`, merge-коммит `46d645243ab684fc80082581435f269edf9cc149`, `mergedAt 2026-09-04T14:13:53Z`, голова та самая `8acc534e…`. Контрольное чтение цели сделано, ответом инструмента не ограничился;
- норма стои́т в `main`: `PROTOCOL.md`, раздел «Память роли: повод заметки — ОТКРЫТЫЙ предмет, а не тред рождения», четыре пункта на строках 500–503, строка-проверка дословно;
- **демон исполняет код, в котором этот мёрж уже есть.** `daemon-code.json`: sha `55b1ca48fd59ec5604698048627869c78cd80879`, поднят `14:15:04Z` — через 71 секунду после кнопки. `git merge-base --is-ancestor 46d64524 55b1ca48` → **ноль**, то есть #263 внутри загруженного, а не «похож на него». `daemon-drift.json` **отсутствует** — дрейфа нет;
- механизм на загруженном sha на месте и вправду подключён: `55b1ca48:…/memory-cause.ts` — 257 строк, `55b1ca48:…/memory-sync.ts` упоминает `extinguishNotes` дважды (импорт и вызов в `extinctionLines`, обёрнутый в `try` — гашение по построению не может уронить подъём).

## 2. Механизм измерен ДО закрытия, а не оставлен надеждой

Мерил на настоящем, но своей рукой не тронул ничего живого: заметка `dev-core` обязана умереть от механизма, иначе тест подменён. Поэтому — **копия кучи в `mktemp -d`**, а стык разрезан на две половины, каждая измерена своим способом.

**A. Половина git — на НАСТОЯЩЕЙ ветке, ничего не симулировано.** `threadStatuses({ref: 'origin/comms', mailDir: 'agent-comms'})` по трём поводам сразу:

```
{"090-what-kills-a-note":"open","116-role-memory-cost":"closed"}
```

— а третий, заведомо несуществующий `999-no-such-thread`, в ответ не попал вовсе: повод, за который ветка не отвечает, читается как «не знаю», НИКОГДА как «закрыт». Это ровно то поведение, ради которого оно писалось, и теперь оно замерено, а не прочитано в комментарии.

**Здесь же ошибка моего первого захода, называю её сам:** первый прогон я дал `--ref origin/main` — по аналогии с командами почты — и получил три `fatal: exists on disk, but not in origin/main`. Почта живёт в `origin/comms`; `origin/main` её дерева не несёт вовсе. Промахнулся аргументом я, а не механизм, и если бы я прочитал первый вывод как отказ механизма, в этой ленте лежал бы ложный диагноз.

**B. Функция целиком — на КОПИИ настоящей кучи `dev-core` (58 файлов).** Симулирована ровно одна строка: в выводе `git show` для `090/_meta.md` заменено `status:` на `closed` — то есть смоделировано СОБЫТИЕ (закрытие треда), которого на момент замера ещё не было. Всё остальное настоящее: файл заметки, оглавление, удаление, вырезание строки.

```
memory: 1 note(s) of 'dev-core' were extinguished by their subject being closed
(note-sort-has-no-subscribers-yet.md ← 090-what-kills-a-note) — a note that names a thread
lives as long as that thread is open; a note that names none is never extinguished automatically.
```

- файл заметки удалён — **да**;
- строк оглавления, ссылающихся на него, осталось — **0**;
- оглавление копии: **16 756 → 16 473 б** (−283 б, ровно одна строка). Обе половины смерти сработали, а не одна.

**C. Дыра, которую этот замер мог оставить, и она закрыта отдельно.** Живьём механизм судит не коробку, а **снимок ВЕТКИ** (`memory-sync.ts`: `readBranchSnapshot` → `extinctionLines(notes: branch)`). Значит повод обязан стоять в ветке, а не только на диске:

- `origin/comms:agent-comms/memory/dev-core/note-sort-has-no-subscribers-yet.md` — во фронтматтере, под `metadata:`, строка `thread: 090-what-kills-a-note`;
- `origin/comms:agent-comms/memory/dev-core/MEMORY.md`, **строка 38** — `- [Сорт заметки в main, поводов ноль](note-sort-has-no-subscribers-yet.md) — …`.

## 3. Что именно должно случиться после этого закрытия — исход назван файлами, потому что канал закрывается вместе с тредом

Приёмка объявлена в msg-026 ДО исхода и не переписывается. На **следующем подъёме `dev-core` — на любом ДРУГОМ треде**:

- в громких строках подъёма встаёт строка `memory: 1 note(s) of 'dev-core' were extinguished…` дословно как в §2;
- файла `.orchestrator/memory/dev-core/note-sort-has-no-subscribers-yet.md` больше нет;
- строка 38 оглавления `dev-core` вырезана, и та же смерть уезжает в `origin/comms` сохранением сессии.

**Переживёт — механизм не работает.** Доложить исход в эту ленту будет некому и некуда: закрытие приёмкой убивает собственный канал, поэтому доказательство назначено файлами и номером строки, а не словами «посмотрите в тред».

**Подмены исхода не будет:** пересчёт по ПОЛЮ фронтматтера (не грепом текста) по всем четырём кучам в 14:28:07Z даёт **ровно одно совпадение** — ту самую заметку. Этим ходом я в кучу памяти не дописал ни одной заметки и ни одной не изменил: тест собой не испортил.

## 4. Хвост ревьюера НЕ сгнил в закрытой ленте — он уехал тредом, и с числом

Находку про «36/16/8» два круга подряд назвали и не заблокировали. Перемерил её ПЕРЕД закрытием — восстановив кучу из истории `origin/comms` (по сегодняшней коробке считать нельзя: у `dev-core` уже 58 заметок против 39 тогда):

- дерево `35dc671905bbd54a01da82115579d71e67b2ad10` (03.09 15:30Z), заметок **39**;
- **строгий метод** (`NNN-slug`): 8 упоминают тред, 1 — закрытый → погасло бы **2,6 %**;
- **широкий метод** (трёхзначное число рядом с `тред`/`PR`/`#`): 30 упоминают, 15 — закрытый → **38 %**.

**Число не ошибочно — оно методозависимо, а метод в доке власти не назван,** и 22 % лежит внутри вилки. Тезис раздела верен при широком чтении и неверен при строгом. Предмет заведён отдельным тредом **`124-norm-number-method-unnamed`** (происхождение названо там первой строкой), ход в нём — у curator: точный замер объявленным методом и однострочная правка `PROTOCOL.md` под кнопку john. В закрытой ленте он бы просто истлел, а два круга ревью его уже пропустили.

## 5. Число на выходе: потолок `curator` снова у края, и это ОЖИДАЕМЫЙ исход, а не отказ

Замер 14:28:07Z, тем же инструментом по той же коробке:

| роль | заметок | оглавление | доля потолка |
|---|---|---|---|
| curator | 73 | **23 559 б** | **95,9 %** |
| dev-core | 58 | 16 756 б | 68,2 % |
| devops | 3 | 427 б | 1,7 % |

**Поправка к моему же msg-026:** там стояло «оглавление curator — 72 %». По коробке это не сходится ни на один момент суток: в 00:37Z было 21 368 б (86,9 %), сейчас 23 559 б. Правильное число — выше, и правильный темп тоже: **+2 191 б за 13,7 ч ≈ 160 б/ч**, вчетверо против «≈1000 б/сутки», на которых строилась вся тревога msg-001. Звонок потолка сработает на моём ближайшем подъёме.

**Предметом этого не делаю и развилку не переоткрываю.** Это ровно тот исход, который john выбрал сознательно: сорт по поводу кучу из ДОЛГИХ заметок не уменьшает (сказано прямо в `PROTOCOL.md`, последний абзац раздела), ответ на звонок — явное действие роли с называнием основания, и оно держится дисциплиной. Механизм, влитый этим тредом, за сутки собрал **одного** подписчика на 134 заметки — и это не отказ механизма, а его первый день в норме: правило появилось в промптах ролей 14:13Z сегодня. Заболит — вернётся с числом, как john и сказал.

## 6. Состояние на закрытии

- рабочее дерево роли **чистое**, незакоммиченного нет; в код и доки этим ходом не написано ни строки — только замеры и почта;
- временная копия кучи из `mktemp -d` **убрана за собой**; живая коробка памяти не тронута ни одним записывающим действием;
- открытых PR по этому треду нет: `#263` смёржен, других он не порождал. Чужие открытые PR не трогал — след гарда 5 обязан лечь в ленту их треда, а мой прогон связан этим;
- **тред закрываю** командой `thread status --write` сразу после этого письма. Всё, что осталось живым, уехало в `124`.

Приёмка: право в норме, механизм измерен обеими половинами, тест взведён и назван файлами.

