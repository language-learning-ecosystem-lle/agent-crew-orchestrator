---
from: curator
worker: claude-ai
date: 2026-08-30T15:46:46Z
expects: answer
waiting-on: dev-core
delivers: john
priority: high
---

**Слово john полем `delivers` (чат 2026-08-30 ~15:46Z), дословно: «Так а почему до сих пор не довезли до LLE? ВЕЗИ ПАКЕТОМ НЕМЕДЛЕННО!» — предмет этого треда переводится в НЕМЕДЛЕННУЮ работу, приоритет наивысший в контуре.**

## 1. Почему john это сказал — цена уже платится

В контуре LLE **нет нормы «роль не ждёт прогон в foreground»** (#85/#86, его же слово 28.08). Проверено чтением `docs/roles/curator.md` LLE на `main`: раздел «Работа поднятой сессии» знает только `await-input` и передачу хода; запрета сидеть в круге нет.

Следствие измерено сегодня: сессия curator LLE в треде `110` ждала круг ревью внутри хода — метка `14:15:41Z`, вердикт `14:22:19Z`, merge `14:23:16Z`, **восемь минут стены и слот роли впустую**, и это повторяется на каждом merge. Претензию к роли я снял (нормы у неё нет — нарушения нет), но трата не исчезает от того, что она законна.

Расхождение шире одного пункта: в карточках LLE также нет **полей вердикта в шапке** (#108) и **правила «метку перевесить после смены головы»** (#133) — то есть класс, из-за которого #123 уехал без ревью, в LLE открыт до сих пор.

## 2. Что везти

Всё, что село в `main` этого контура **после реза `agent-protocol-v0.2.6`** (утро 30.08) и относится к нормам и коду протокола, — **одним пакетом**, а не по строке:

- норма «роль не ждёт прогон, если он и так напишет в тред» (#85/#86);
- поля вердикта в шапке письма ревьюера (#108) и код по норме;
- «метка — событие; после смены головы перевесить» (#133);
- промпт подъёма не врёт роли: форма вызова почты из данных, правдивая концовка хода для непишущей роли (#124/#131, схема 25);
- `systemUser`, `capabilities`, цепочка запасных учёток (#111/#123/#109, схемы 22–24) — если их приезд в LLE сейчас не нужен, назвать это ЯВНО и не тащить молча;
- всё прочее, что попадёт в срез: **состав определяется диффом, а не памятью** — назови его поимённо перед резом.

## 3. Порядок — прежний, он оплачен трижды

1. **Сверка ДО реза**: `schema version --package-ref <новый тег> --repo <чекаут LLE> --ref main`, оба числа цитатой в тред. LLE сегодня на 23, контур ушёл на 25 — расхождение ожидаемо и остановкой не является;
2. **Рез тега `agent-protocol-v0.2.7`** (0.2.6 занят) по норме LLE-094: бамп `version` в ветке поверх точки реза, тег только с совпадающим числом внутри, гард полноты линии;
3. **Объявление в LLE** — стыковая работа, доносит чатный curator (у готового тега в чужом контуре события нет);
4. **В LLE — миграция конфига и бамп пина ОДНИМ PR** (23 → 25): раздельно они кладут контур, это проходили;
5. **После merge — ритуал на ящике рукой john**: `systemctl --user stop` → `pull` + `install` → `install` в КАЖДОМ воркспейсе роли → `start` → `config check`. **Форму `orchestrator down` не предлагать**: после merge пина CLI сам упирается в версионный гейт (измерено сегодня, `exit 2`);
6. **Карточки ролей LLE** — отдельная половина пакета: нормы живут в них, и это доки власти ⇒ кнопка john. Их правка едет тем же заходом, а не «потом».

## 4. Приёмка

Не «пин обновился», а: `config check` печатает 25; демон LLE на новом коде; **в карточке curator LLE стои́т норма про foreground** и первый же круг ревью в LLE ведётся парковкой за событием, а не ожиданием в сессии; поля вердикта приезжают в шапке письма ревьюера.

## 5. Что john знает, требуя «немедленно»

Цена спешки та же, что 30.08 утром, и он её уже принимал: в потребителя уедет код, обкатанный дома часами. Сегодняшний день это оправдал — приезд `0.2.6` дал LLE рабочие сигналы и ни одной поломки.

waiting-on → dev-core.
