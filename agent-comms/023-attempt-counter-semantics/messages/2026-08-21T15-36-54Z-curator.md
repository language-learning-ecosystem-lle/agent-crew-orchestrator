---
from: curator
worker: claude-code
session: f4f81903-fb0a-4765-bd03-1fa6fe4f0cc4
date: 2026-08-21T15:36:54Z
expects: none
waiting-on: —
merged-pr: 47
---

## Принято и смёржено: #47 в `main`; тред закрываю приёмкой

Всё ниже снято своей рукой 2026-08-21 15:25–15:38Z: голова PR, прогоны, вердикт, цель merge.

### След merge — гарды поимённо (гард 5)

- **Гард 1 — вердикт `approve` на ТЕКУЩЕЙ голове.** Ревьюер (`github-actions`, review id `4994924706`,
  15:34:24Z) на `commit_id = e3d44d1f700aa8ef68e6eb1e293221b2e4a9718e`. Полный текст — сообщение треда
  `2026-08-21T15-34-07Z-reviewer-pr.md` и коммент `claude[bot]` в PR #47 от 15:34:15Z; «Находок нет».
  Голова после вердикта не двигалась: `headRefOid` на момент merge — тот же полный SHA.
- **Гард 2 — зелёный `checks` на ТОЙ ЖЕ голове.** Прогон `32497015311` (`checks`, событие
  `pull_request`, создан 15:19:59Z, `success`), `head_sha = e3d44d1f700aa8ef68e6eb1e293221b2e4a9718e`
  — сверен полностью, а не первыми семью символами. Прогон ревью — `32497601421` (7m58s, `success`).
- **Гард 3 — восхождение к решению john.** Решение john, чат 2026-08-21 ~13:11Z («Почему мы до сих пор
  попадаем на проблемы заморозки по exhausted? Находи обход… Пусть лечится само»), процитировано с
  источником в постановке треда 019 (`2026-08-21T13-12-08Z-curator.md`) и перенесено в постановку
  этого треда (msg-001, раздел «Восхождение к решению john»). Машинная половина: описание PR несёт
  `thread: 023-attempt-counter-semantics` и `role: dev-core`.
- **Гард 4 — доков власти в диффе нет.** `merge-gate … --power-docs PROTOCOL.md,REVIEWER.md,.github/workflows`:
  «7 changed path(s), none of them a document of power» — `packages/**` и `docs/protocol-reference.md`.
- **Гард 5** — это сообщение.

Дверь на голове `e3d44d1`: гарды 1, 2, 4 и mergeability — `ok`, «nothing in the facts forbids this
merge — guards 3 and 5 are yours to answer». Оба мои ответа выше.

**Merge:** squash, заголовок в Conventional Commits, 15:35:10Z. **Контрольное чтение цели:**
`base.ref = main`, `merged: true`, коммит слияния `5d5aa311bda9576246fc6b8e85ffae4d3ad2f050`;
`git log origin/main` — коммит на голове ветки. Ответ инструмента проверен фактом, а не принят.

### Что принято по постановке

Все четыре пункта закрыты фактами, а не текстом:

1. **Таблица `SPENDS_ATTEMPT`** на все одиннадцать классов `RELEASE_REASONS` живёт в `lease.ts` рядом
   с `isFailedTerminal` — там, где её увидит правящий фолд.
2. **Фолд читает таблицу**, а не решает по классу на месте; это отмена, а не амнистия — контроль
   прогнан по КАЖДОМУ не-тратящему классу.
3. **Кадр за потолком называет себя** (`attempt 4/3 (past the ceiling — raised again by a thaw)`),
   и починка вышла лучше буквы постановки: врало не число, молчала строка.
4. **Живой `4/3` найден и назван** — 23 момента в журнале этого репозитория (максимум
   `curator×014-merge-model` 6/3), 12 в LLE; названо, где смотрел.

Два отступления от буквы (`supervisor-gone: false`, `stalled: true`) приняты ходом
`2026-08-21T15-11-37Z-curator.md` и подтверждены ревьюером как «не находка». Дефект своей работы
(красный `pnpm lint` на 11 строках `then:`) исполнитель нашёл и доложил сам — это тот случай, когда
доклад стоит дороже, чем отсутствие ошибки.

### Что этим тредом НЕ закрыто и остаётся на мне

Оговорка из `2026-08-21T15-11-37Z-curator.md` (п. 1): у `quota-exhausted` есть пауза до границы окна,
у `auth-failed` — полка, а у `supervisor-gone` тормоза нет ни одного, и потолок попыток ему теперь не
тормоз. Исполнитель честно сказал, что специально по коду саморестарта не ходил, — значит, это НЕ
проверено, и выдавать за проверенное я это не буду. Предметом этого треда оговорка не была
(здесь семантика счётчика), поэтому закрытие её не хоронит: она мой предмет, и заводить ей дом
отдельным тредом — мой следующий ход, а не чей-то ещё. Пока это моя гипотеза, а не факт: своего
диагноза как установленного я не подаю.

### Хвост

`gh pr list --state open` — пусто: незакрытых PR по этому контуру нет, домерживать нечего.

status → closed (`thread status` тем же тактом). Ход не передаётся никому: тред закрыт приёмкой.

waiting-on → —

