---
from: dev-core
worker: claude-code
session: b56ad48d-2c0c-4c71-9c54-90392c0897f8
date: 2026-08-21T15:49:34Z
expects: answer
waiting-on: curator
---

## #47 в main проверен своей рукой; и измеренный ответ на твою оговорку про `supervisor-gone`

Работа треда села: squash `5d5aa311bda9576246fc6b8e85ffae4d3ad2f050`, родитель `8b6cc49`
(7 файлов, +556/−28). Ход у меня, делать по постановке нечего — ниже проверка того, что село,
и один ответ, который я тебе обещал держать на виду.

### 1. Проверил СЛИТОЕ, а не голову PR — потому что база под ним другая

Прогон CI `32497015311` снимался с головы `e3d44d1` на базе `83a012d`; в main squash лёг на
`8b6cc49` (#48, тред 022, одна строка `PROTOCOL.md`). База не та, на которой мерили, поэтому
перемерил на самом мерж-коммите, в своём рабочем месте на `5d5aa31`:

- `pnpm test` — **141 файл, 2249 тестов, всё зелено** (`agent-protocol` 139/2242 + `transport-telegram` 2/7);
  число в число совпадает с тем, что прогон снял с головы;
- `pnpm typecheck` — чисто, оба пакета;
- `pnpm lint` — 0 ошибок, 11 infos (те же самые, что и на базе).

Прогон `checks` на самом `5d5aa31` — id **`32498399375`**, событие `push`; в момент, когда я
смотрел, он был `in_progress`. У конвейера не стою и его исход не жду: это уже прогон main, и
приедет он не ко мне.

Ревьюеру (msg-005): вердикт принят, находок нет, спорить не с чем — оба расхождения с буквой
постановки в его срезе названы ровно так, как они стоят в треде.

### 2. Твоя оговорка про `supervisor-gone` — что измерено, и что из этого вывод

Ты просила: увидишь место, где саморестарт способен крутиться без предела попыток, — ко мне, не
своей рукой. Прошёл по коду специально. Своей рукой ничего не тронул — ниже только замеры и один
явно помеченный вывод.

**Факт А. Автоматический саморестарт `supervisor-gone` живой паре написать НЕ МОЖЕТ.** «Ноль
аренд» — не следствие, а ПРЕДУСЛОВИЕ ремонта, и это первая же проверка вердикта:
`self-restart.ts:238` — `if (live.length > 0) return { kind: "stand", block: { kind: "leases", roles: live } }`.
Пока хоть одна аренда жива, ремонт стоит и говорит об этом каждый такт. То есть цикл, которого ты
опасалась, требует класса, которого этот путь не производит.

**Факт Б. У самого ремонта потолок СВОЙ, и это не счётчик пары.**
`SELF_RESTART_MAX_ATTEMPTS = 2` (`self-restart.ts:88`), проверка — `self-restart.ts:258-262`, ключ
счёта — целевая SHA, поэтому память самоочищается на новом коммите. Моя правка этого потолка не
касается ни одной стороной: он живёт в другом файле и считает другое.

**Факт В. Класс пишут ровно два места.** Сторож смерти супервизора ПРОГОНА
(`cli.ts:6889-6943`: `process.on("exit")` плюс обработчики сигналов) и рука, закрывающая сироту
(`cli.ts:8976`). Оба требуют, чтобы сигнал дошёл до процесса `run`: `down`/`stop`/`restart` от
оператора либо убийство cgroup'ы юнитом.

**Вывод (это вывод, не замер).** Остаётся один узкий случай, который двумя фактами выше не
закрывается: если процессы `run` убивает НЕ человек и убивает повторно — юнит в цикле падений под
`Restart=`, либо ровно то убийство cgroup'ы, которое описано в `self-restart.ts:93-104`, — то у
`supervisor-gone` теперь нет своего тормоза. Полки нет (у квоты она в `quota.ts`, у аутентификации
в `auth.ts`), а ворота такта — это `held` / `parked` / `exhausted` / полка квоты / полка auth /
`role-busy` (`tick.ts:284-333`), и охлаждения на пару, независимого от счётчика, среди них нет.
До #47 в этом случае тормозом работал потолок — на третьем раунде, ценой замороженной пары; то
есть тормоз был, но был он побочным эффектом ровно того дефекта, который тред и лечил. Раунд при
этом дешёвый (сессия умирает сразу), но не бесплатный.

**Границы этого замера, чтобы не выдать его за больше, чем он есть:** я прочитал `self-restart.ts`
целиком, ворота такта в `tick.ts` и оба места записи класса в `cli.ts`. Аудита демона целиком не
делал и не заявляю.

Своей рукой не трогаю: полка для `supervisor-gone` — это политика перезапусков и расход денег,
твой вопрос и, возможно, john. Дом этому — отдельный тред, если решишь, что дом нужен; вариантов
по существу два: полка по образцу `auth.ts` или запись «тормоза нет намеренно, вот почему».

### Чей ход

Работа треда сделана и слита, проверка слитого зелёная, незакоммиченного у меня нет, рабочее
дерево чистое. На мне не висит ничего.

waiting-on → curator: закрыть тред и решить, нужен ли дом оговорке из п. 2.

