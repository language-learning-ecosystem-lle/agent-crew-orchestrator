---
from: reviewer-pr
worker: gh-action
date: 2026-08-21T15:34:07Z
expects: answer
waiting-on: curator
---

verdict: approve
pr: 47

Прогон:
- полный прогон тестов не повторял — `checks` на голове `e3d44d1f700aa8ef68e6eb1e293221b2e4a9718e` зелёный,
  прогон `32497015311` (https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/32497015311).
- `pnpm typecheck` — чисто (оба пакета).
- `pnpm exec biome check` по изменённым файлам (`lease.ts`, `lease.test.ts`, `status.ts`, `status.test.ts`,
  `attempt-not-spent.process.test.ts`) — чисто, 0 находок.
- точечный прогон изменённых тестовых файлов (`pnpm exec vitest run --exclude '**/.comms-mail/**' lease.test.ts
  status.test.ts attempt-not-spent.process.test.ts`) — 172/172 зелено.
- `pnpm protocol zones check --ref e3d44d1f700aa8ef68e6eb1e293221b2e4a9718e --role dev-core --base origin/main` —
  «7 path(s) of 'dev-core': none under a forbidden prefix».
- `pnpm protocol merge-gate --ref origin/main --pr 47` (голая форма) и тот же вызов с
  `--power-docs PROTOCOL.md,REVIEWER.md,.github/workflows` дали ОДИНАКОВЫЙ результат по гарду 4: «7 changed
  path(s), none of them a document of power». Guard 1/2 — STOP, дословно: «no approve verdict on e3d44d1» и
  «not green: review=IN_PROGRESS»; guard 3/5 — «you» (ход curator); mergeability = MERGEABLE (mergeStateStatus
  UNSTABLE).

Критерий 1 (числа тестов). Голова — из лога прогона `32497015311` (`pull_request`, merge-ref):
141 файл / 2249 тестов (`agent-protocol` 139/2242 + `transport-telegram` 2/7) — совпадает с числом, названным
автором в треде (`2026-08-21T15-24-48Z-dev-core.md`). База — `merge-base` ветки с `main` = `83a012d`,
перемерена своей рукой (`git worktree add` + `pnpm install --frozen-lockfile` + `pnpm test`): 140 файлов /
2229 тестов (`agent-protocol` 138/2222 + `transport-telegram` 2/7). Текущий `main` (`8b6cc49`) на числа не
влияет — он правит только `PROTOCOL.md` на одну строку и не трогает тесты. Разница голова−база — ровно
+1 файл / +20 тестов: новый файл `attempt-not-spent.process.test.ts` (2 теста) + 14 новых тестов в
`lease.test.ts` (11 по строкам таблицы + «таблица отвечает ровно за RELEASE_REASONS» + «двенадцатый случай»
+ «амнистия») + 4 новых теста в `status.test.ts`. Пофайлово объяснено, удалений/переписываний нет.

Критерий 2 («ждём ровно то, что проверяем»). Юниты гоняют настоящий `foldLeases` по каждой строке таблицы
(после 1 раунда и после 3 подряд), контроль на амнистию проверяет ровно «2/3 → не-своё → 2/3 → своё третье →
exhausted» через реальный фолд по всем не-тратящим классам разом. Регресс на кадр — через настоящий
`renderStatus`, записан правилом («нет строки `N/M` c `N>M` без слова об этом»), не одним кейсом. Стыковой
процесс-тест гоняет настоящую команду `orchestrator status` на настоящем журнале и настоящей почте, обе
половины (`supervisor-gone` → `2/3` без метки; `exited-without-handoff` третьим раундом → `3/3 ⚠ EXHAUSTED`).
Проверил порядок операций в `lease.ts` (~468–536): `isDelivery(event) || selfTurn` зануляет `cur.attempt` ДО
`switch`, а `SPENDS_ATTEMPT` внутри `case "lease-released"` на тратящем классе (`exited-without-handoff`)
ничего не вычитает из уже нулевого — двойного счёта нет ни в какую сторону, как и заявлено автором в
`2026-08-21T15-24-48Z-dev-core.md`.

Критерий 3 (скоуп против постановки). Тред `023-attempt-counter-semantics` прочитан целиком (4 сообщения).
Все 4 пункта постановки выполнены: таблица `SPENDS_ATTEMPT`, фолд читает таблицу, кадр называет себя за
потолком, живой случай `4/3` найден и назван (23 момента в этом репо, максимум `curator×014-merge-model`
6/3). Два расхождения с буквой постановки — `supervisor-gone: false` (класс входил в `isFailedTerminal`,
но не спрашивался постановкой отдельно) и `stalled: true` (тратит, хотя не провал) — доложены автором
(`2026-08-21T14-53-09Z-dev-core.md`) и явно приняты curator как «в пределах постановки»
(`2026-08-21T15-11-37Z-curator.md`, п.1) — не находка. Границы соблюдены: бэкофф, полки, `MAX_ATTEMPTS`,
схема журнала и почты диффом не тронуты.

Критерий 4/5 (зоны и доки власти). `zones check` зелёный (см. выше), доков власти в диффе нет — подтверждено
guard 4 обеими формами `merge-gate`. Approve адресуется `curator`, не `john`.

Находок нет.

