---
from: curator
worker: claude-code
session: f85b37aa-e918-40f7-aa4b-847ede93e4e4
date: 2026-08-21T15:11:37Z
expects: answer
waiting-on: dev-core
---

## `supervisor-gone` остаётся; маршрут #47 стоит на первом шаге, и стоит по двум причинам

Всё ниже снято своей рукой 2026-08-21 15:07–15:12Z на `origin/main` = `83a012d…`.

### 1. `supervisor-gone: false` — принято, это не выход за границы треда

Решение curator в пределах постановки (не решение john, отдельного решения здесь и не требуется —
восхождение то же, что у #44: john, чат 2026-08-21 ~13:11Z, процитирован в постановке треда 019).
Откатывать нечего, отдельного PR не надо.

Почему внутри треда, а не за ним: постановка велела судить **все одиннадцать** классов ОДНИМ
вопросом и прямо развела вердикт и счётчик — исключения для четвёрки `isFailedTerminal` она не
делала. Ты вердикт не тронул (`isFailedTerminal`, `lease.ts:227`, по-прежнему считает
`supervisor-gone` провалом), тронул только цену.

Основание проверил в коде, а не в твоём тексте: релиз пишет сам супервизор, уходя
(`cli.ts:6891`, плюс ручное закрытие сироты `cli.ts:8946`), и протокол уже сегодня считает такой
раунд незаконченным, а не проваленным — `RESUMABLE_REASONS = ["supervisor-gone", "stalled"]`
(`continuation.ts:73`) поднимает ТУ ЖЕ сессию. Ответ «нет, своей возможности сделать работу у пары в
этом раунде не было» здесь честный.

**Оговорка на запись, правку не блокирует.** У `quota-exhausted` есть пауза до границы окна, у
`auth-failed` — полка; у `supervisor-gone` тормоза нет ни одного, а потолок ему теперь не тормоз.
Демон в цикле саморестарта может поднимать пару без предела попыток. Смягчают это две вещи — класс
резюмируемый (поднимается та же сессия, а не новый круг с нуля), и без живого супервизора никто
никого не поднимает. Цену считаю правильной. Но если по ходу увидишь в коде место, где саморестарт
способен зациклиться, — это уже расход денег: ко мне, а не своей рукой.

`stalled: true` — принято, перекос назван в обе стороны и это правильно. `deliveredToSelf` —
принято: закрыт уровнем выше и закреплён тестом, чтобы не откатилось молча.

### 2. Живой `4/3` (п. 4 постановки) — принято как факт

Искал первым делом, назвал где смотрел, назвал числа. Механизм оказался другим, чем предполагал
автор #44 — и починка п. 3 через «строка называет себя» лучше буквы постановки: врало не число,
молчала строка. Зажимать законное число было бы враньём в другую сторону.

### 3. Маршрут #47 стоит, и до метки `review` дело не доходит

`merge-gate --ref origin/main --pr 47`: STOP гард 1, STOP гард 2, `ok` гард 4 (7 путей, доков власти
нет — подтверждаю: `packages/**` и `docs/protocol-reference.md`), STOP на mergeability. Метку
`review` не вешаю: её место — после зелёного `checks` на той же голове, а зелёного нет ни на какой.

**Факт А — на голове `3d45c797bfb33f1beb4ebb3823bba6bd8bc1dbf8` НЕ БЫЛО НИ ОДНОГО ПРОГОНА, вообще.**
Не «упал», не «идёт» — не создавался.

- `gh pr checks 47` → `no checks reported on the 'feat/023-attempt-counter-semantics' branch`;
- `gh api repos/:owner/:repo/actions/runs?head_sha=3d45c797…` → `total_count: 0`;
- `gh api …/commits/3d45c797…/check-suites` → сьют ровно один, приложения `claude`, создан
  14:51:43Z (это push, до открытия PR), с 14:51:43Z висит `queued`, `conclusion: null`. Сьюта
  GitHub Actions на этой голове нет ни одного.
- Для сравнения, тот же автор в тот же час: PR #48 открыт 14:54:52Z, прогон `checks` `32494730652`
  создан 14:54:56Z — через четыре секунды. `checks.yml` слушает `pull_request` без фильтров.

Причину не называю: у меня её нет, а свой диагноз — тоже заявление. Называю только, что прогона нет,
и что ждать его на этой голове бессмысленно — событие 14:52:06Z прошло.

**Факт Б — база уехала, и теперь это настоящий конфликт.** `mergeable=CONFLICTING`
(`mergeStateStatus DIRTY`). `git merge-tree --write-tree origin/main origin/feat/023-attempt-counter-semantics`:
конфликт ровно один, `packages/agent-protocol/src/orchestrator/lease.ts`; `docs/protocol-reference.md`,
`packages/agent-protocol/README.md` и `lease.test.ts` сливаются сами. Против базы момента твоего
пуша (`d3dd2e9`) ветка ложилась чисто — то есть конфликт приехал с #46 (тред 022, «парковка по
смыслу»), севшим в main 15:05Z и правившим тот же фолд.

### 4. Что делаешь

1. Ребейз ветки на `83a012d…`, руками развести `lease.ts` — там встретились твоя таблица
   `SPENDS_ATTEMPT` и отказ парковки на входе из 022. Пересобрать, прогнать пакет целиком, показать
   числа: чужой фолд в том же файле — ровно то место, где тихо теряется строка таблицы.
2. Force-push. Это событие `synchronize` — оно должно создать `checks` на новой голове. **Проверь
   своими глазами, что прогон СОЗДАН** (`gh api …/actions/runs?head_sha=<новая голова>`), и назови
   его id в докладе. Исход CI событием в тред не приезжает — зелёное снимаешь сам.
3. **Если и на новой голове прогона не создалось** — не жди у конвейера и не чини это своей рукой:
   скажи в тред. Тогда это факт про контур (`.github/workflows` — не твоя и не моя кнопка), и он
   мой, к john.
4. Метку `review` вешаю я — на зелёной голове, по маршруту.

waiting-on → dev-core: ребейз на `83a012d`, зелёный `checks` на новой голове с названным id прогона.
Вопрос про `supervisor-gone` закрыт — строка остаётся, откатывать не надо.

