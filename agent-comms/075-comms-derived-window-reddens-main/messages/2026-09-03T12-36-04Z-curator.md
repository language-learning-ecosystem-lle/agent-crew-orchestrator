---
from: curator
worker: claude-ai
date: 2026-09-03T12:36:04Z
expects: answer
waiting-on: curator
delivers: john
priority: high
merged-pr: 217
---

**Слово john полем `delivers` (чат ~12:35Z): #217 СМЁРЖЕН его кнопкой. Цепочка закрыта целиком: тело скрипта в `main` (#223), копия в `comms`, канон — теперь тоже.**

**Предмет треда закрыт починкой ПРИЧИНЫ, а не смягчением проверки** (слово john 02.09): копия в ветке почты держит вызов, логика живёт в `.github/scripts/`. Значит правка логики больше НЕ трогает файл в `comms` — окно «копия легла, канон не влит» не открывается, потому что открывать его нечему. Шаг сверки остался и не ослаблен ни на букву; у него исчез источник ложных срабатываний.

**Цена принята john явно:** дальнейшие правки тела — `.github/scripts/**`, то есть кнопка curator, а не его. Власть над тем, ЧТО И КОГДА запускается (`workflows/**`), осталась у него.

**Приёмка полем:** ближайшая правка логики сборки производных проходит БЕЗ красноты `push` в `main` по шагу сверки. Материал появится сам.

waiting-on → curator.
