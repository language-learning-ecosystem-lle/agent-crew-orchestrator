---
from: dev-core
worker: claude-code
session: b5de6b89-6d65-428f-89d9-bf04042b9841
date: 2026-08-18T18:01:42Z
expects: none
waiting-on: dev-core
---

Пакет положен: обе двери стоят, копия в `comms` легла, PR #12 открыт и зелёный. Ниже — измеренное, отдельно от выведенного; остаётся снять один контрольный факт (следующим сообщением, номером прогона), поэтому ход пока держу.

## Сделано

**PR #12**, голова `03f8e07`, ветка `fix/comms-derived-guards-007`, база `main`. Строки `thread: 007-comms-derived-guards` и `role: dev-core` в описании.

**Копия в `comms`** — коммит `4ed97da`, побайтно равная канону (`diff` пуст на обоих концах), положена ПОСЛЕ красного прогона и ДО зелёного, то есть ровно тем порядком, который постановка и просила измерить.

## 1. Шаг сверки копии с каноном — `.github/workflows/checks.yml`, джоба `checks`

Форма прототипа LLE взята целиком. Два места, которые постановка велела проверить здесь, а не переносить на веру:

- **`fetch-depth`**: чекаут `checks` вызывался без `with:` — клон был shallow. Поставлен `fetch-depth: 0`, и это проверено прогоном, а не рассуждением: трёхточечный дифф на обоих прогонах ниже сработал (на первом дал «канон тронут» и дошёл до сравнения, на втором — тоже, и сравнение совпало). Репозиторий маленький, полная история дешевле догадки о нужной глубине;
- **имя базы**: путь `push` до `github.base_ref` не доходит вовсе (ранний выход не берётся, сверка идёт безусловно); на `pull_request` `BASE_REF` пришёл `main` — видно в логе шага.

Шапка канона (строка 9), объявлявшая несуществующий `ci.yml`, правится тем же коммитом и называет то, что реально поставлено: файл, джоба, имя шага. Тем же коммитом — `docs/protocol-reference.md`.

**Живая приёмка, оба прогона — одна и та же джоба на одной голове `03f8e07`, различаются только состоянием ветки `comms`:**

- **КРАСНЫЙ до кладки копии — прогон `32167982489`, попытка 1** (17:56Z). Упал ровно на шаге «comms-derived синхронен с каноном», все восемь шагов до него — `success`. Дословно из лога: `##[error]comms-derived.yml в ветке comms разошёлся с каноном в main — синхронизируйте копию (тред 007). Порядок: копия в comms кладётся ДО merge'а канонической правки.` То есть дверь отказывает по имени и называет порядок лечения;
- **ЗЕЛЁНЫЙ после кладки — прогон `32167982489`, попытка 2** (18:00Z). Тот же шаг — `success`.

Замечание по механике, полезное впредь: здесь `gh run rerun` базу подхватывать и не должен был — шаг фетчит `origin comms` в момент своего исполнения, поэтому вторая попытка на замороженном merge-ref увидела новую копию. Это НЕ противоречит разделу «PR красный из-за состояния репозитория» в `protocol-reference` — там речь о правке, влитой в БАЗУ, а здесь изменилась третья ветка, которую шаг читает сам.

## 2. Гард от петли — по заголовку

Стало: `if: "!startsWith(github.event.head_commit.message, 'chore(comms): rebuild derived')"`. Префикс взят полным заголовком коммита генератора, а не куском: коммит однострочный и ровно один (строка 183), сообщение начинается со своего заголовка. `head_commit` пуст на `workflow_dispatch` — `startsWith` там даёт `false`, условие пропускает, ручной запуск как работал, так и работает.

**Живая приёмка, прямой факт:** push коммита `4ed97da` в `comms` — его ТЕЛО содержит слова `rebuild derived`, заголовок (`ci(comms-derived): копия канона…`) не содержит. Прогон **`32168328278`** — **`success`**, джоба исполнилась: чекауты, установка, шаг «Пересобрать производные…» отработали. Прежним гардом этот же push дал бы `skipped` — тот самый дефект, что и мерился на `f2b5ec7`.

**Оговорка, которую не прячу:** коммита `chore(comms): rebuild derived` этот прогон не породил — генератор дошёл до работы и сказал `agent-protocol: the derived files already match — nothing to write` → `производные уже актуальны — коммитить нечего`. Производные были свежими: их собрал прогон на твоих сообщениях (коммит `a9d0a2a` в `comms`). То есть приёмка снята чтением лога и ветки, а не цветом: доказано, что джоба ЗАПУСТИЛАСЬ, — а это ровно то свойство, которое правка меняла. Идемпотентность генератора здесь ни при чём и не трогалась.

## 3. Чего ещё нет — контроль на противоположном факте

Прогона, где push САМОГО коммита генератора остаётся `skipped` УЖЕ ПОД НОВЫМ ГАРДОМ, у меня пока нет: под новый гард такой коммит ещё не рождался (см. оговорку выше — генератору нечего было писать). Прогон `a9d0a2a` не годится: он шёл под старым выражением.

Push этого сообщения даёт материал даром: в `agent-comms` появляется новый файл, генератор обязан пересобрать `INDEX.md`/`_thread.md`, закоммитить `chore(comms): rebuild derived` и запушить — и вот ТОТ push обязан остаться `skipped`. Дождусь его и назову номером прогона следующим сообщением; без этого номера мы, как ты и написала, меняем немую дверь на бесконечную петлю.

## 4. Что не покрыто

- ветка «канон не тронут» (ранний выход по пустому трёхточечному диффу) живьём не снималась — постороннего PR рядом не подвернулось. Берётся с доверием к прототипу LLE (прогон 30788651443);
- юнит/интеграционного теста нет сознательно, той же причиной, что в 005: логика — bash внутри Actions;
- дверь зон в `checks.yml` не трогал — по постановке сюда не тащить.

Merge — кнопка john: `claude-review.yml` заморожен, гарду 1 закрываться нечем.

