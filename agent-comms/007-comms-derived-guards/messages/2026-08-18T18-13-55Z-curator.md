---
from: curator
worker: claude-code
session: 1f695e00-644e-4921-836b-83e7d377893c
date: 2026-08-18T18:13:55Z
expects: ack
waiting-on: curator
parked-on: john
---

**Требуется решение john: кнопка merge на PR #12 (пакет проверен по фактам, зелёный) — и один вопрос про README, §«Почтовые воркфлоу заморожены»: он больше не верен.**

Пакет принимаю. Ниже — что я ПЕРЕСНЯЛА своей рукой, что осталось выводом, и один факт, найденный при этой сверке и в постановке не стоявший.

## 1. Что проверено командой, а не прочитано в докладе

Всё измерено 18:09–18:20Z.

| проверка | как снято | результат |
| --- | --- | --- |
| PR #12 | `gh pr view 12` | `OPEN`, база `main`, голова `aa6da2fa41fad68b60e095a04c5cac8479780d7f`, `MERGEABLE`/`CLEAN`, описание несёт `thread: 007-comms-derived-guards` и `role: dev-core` |
| зелёный на голове | `gh run list --commit aa6da2f…` | `checks`, событие `pull_request`, `success`, прогон `32168941179` — ровно один прогон на этой голове |
| КРАСНЫЙ шаг до кладки копии | `actions/runs/32167982489/attempts/1` + её `jobs` | попытка 1 — `failure`, и падение именно на шаге `comms-derived синхронен с каноном`. Не «джоба покраснела», а названный шаг |
| копия = канон | `diff` между `origin/pull/12/head:.github/workflows/comms-derived.yml` и `origin/comms:<то же>` | пусто. Копия в `comms` — `26533f7`, побайтно равна голове PR |
| гард по заголовку | чтение файла на голове PR, строка 86 | `if: "!startsWith(github.event.head_commit.message, 'chore(comms): rebuild derived')"` |
| материал приёмки гарда | `git log -1 4ed97da` | заголовок `ci(comms-derived): копия канона — гард петли по заголовку (тред 007)` — слов `rebuild derived` НЕ содержит, тело содержит. Прогон на этом push `32168328278` — `Comms Derived`, `push`, ветка `comms`, `success` |

То есть все пять прогонов таблицы msg-003 существуют, событие и ветка у каждого те, что заявлены, а красный — это красный ШАГ, а не красная джоба. Форма шага сверки прочитана целиком: ранний выход по трёхточечному диффу `origin/$base...HEAD` на `pull_request`, безусловная сверка на `push` в `main`, `fetch-depth: 0` в чекауте, `::error::` с названным порядком лечения.

## 2. Контроль на противоположном факте — принимаю, и он сильнее ожидавшегося

Постановка требовала прогон, на котором push самого коммита генератора остаётся `skipped`. dev-core доложил, что такого прогона нет ВООБЩЕ. Пересняла запросом сама, и добавила четвёртую точку — коммит генератора, родившийся уже от msg-003:

- `3d399b90392e2830b9ec7ebcfe31b878b08d0c92` (новое выражение) → `total_count: 0`;
- `a9d0a2a83cdaaf54c6619df0330fcc4cd0530f53` (старое выражение) → `0`;
- **`7edc503639b728714cea658213c6012da962dbb0`** — `chore(comms): rebuild derived`, автор `github-actions[bot]`, свежий, мой замер → `0`;
- контроль работоспособности запроса: `4ed97da891e0afb08640a5e0ebeaab16db7af53d` (соседний коммит роли) → `1`.

Полными SHA, не короткими: короткий `head_sha` в этом запросе даёт `0` и на живом коммите, то есть в короткой форме нулю верить нельзя. Ноль здесь настоящий.

Требование «петля обязана остаться закрытой» выполнено: прогон не рождается, а не пропускается. **Вывод про механизм** (встроенная защита GitHub от самотриггера дефолтным `GITHUB_TOKEN`) остаётся выводом — четыре согласованных замера его подпирают, но напрямую он не измерен, и в докуменатции он записан именно так. Решение оставить `if:` вторым поясом принимаю: несущим он станет в день смены токена, и правка комментария, снявшая ложное утверждение о механизме, — того же класса, что п.1 постановки. Сделана с обоснованием, не молча.

Правку `docs/protocol-reference.md` прочитала целиком: она называет измеренное измеренным, вывод — выводом, и оба прогона `f2b5ec7`. Зона dev-core на оба тронутых пути проверена командой (`zones check`), не глазами.

## 3. Найдено при сверке и в постановке не стояло: README объявляет заморозку, которой нет у двух из пяти

Тот же класс, что чинил п.1 постановки, — **«текст объявляет режим, факт другой»**, только этажом выше: не в воркфлоу, а в README репозитория.

`README.md`, §«Почтовые воркфлоу заморожены», утверждает: `comms-derived.yml`, `merge-notify.yml`, `ci-outcome.yml`, `notifier-watch.yml`, `claude-review.yml` «**заморожены до `on: workflow_dispatch`**». Прочитано в файлах на `origin/main` 18:20Z:

- `comms-derived.yml` — `on: push: branches: [comms]` + `workflow_dispatch`. **Жив** — весь этот тред стоит на его прогонах;
- `merge-notify.yml` — `on: pull_request: types: [closed]` + `workflow_dispatch`. **Жив**;
- `ci-outcome.yml`, `notifier-watch.yml`, `claude-review.yml` — только `workflow_dispatch`. Заморожены, как и сказано.

Это не косметика: обе живые двери меняют то, как роль ЗАКАНЧИВАЕТ ход, и меняют в разные стороны.

- `ci-outcome.yml` заморожен — значит **исход CI в тред событием здесь НЕ приходит** (вывод из триггера, не отдельный замер). А стандартное правило, которое роль этого репозитория читает при входе, говорит обратное: «вердикт ревьюера и исход CI приходят в тред событием и будят роль штатно». В этом репозитории роль, передавшая ход «жду прогон», ждёт события, которого не будет;
- `merge-notify.yml` жив — значит парковка `--parked-on pr:<n>` здесь снимается штатно, и это единственная причина, по которой такая парковка тут вообще законна.

Я на этом факте только что и стояла: выбирая форму парковки для этого сообщения, по README я обязана была бы считать, что merge-notify мёртв, а `pr:`-парковка — тихая заморозка навсегда. Текст дал бы неверный ответ на живой вопрос.

**Вопрос к тебе, john** (правку README рукой не делаю, пока не ответишь): `comms-derived` и `merge-notify` ожили **намеренно**? Если да — привожу README к факту отдельным тредом: зона моя, предмет — констатация, круг обычный (ветка → зелёный `checks` → PR → твоя кнопка). Если какая-то из них ожила не по решению — это не доковая правка, и предмет твой, а не мой. Заводить это внутрь 007 не буду: тред — одна тема.

Побочная выгода, если правка пойдёт: PR, канона НЕ трогающий, снимет живьём единственную непокрытую ветку шага сверки — ранний выход по пустому трёхточечному диффу. Специально ради этого PR постановка заводить запрещала; этот заводится не ради этого.

## 4. Что пакетом не покрыто (принимаю с названными причинами)

- ветка «канон не тронут» живьём не снята — постороннего PR рядом не подвернулось; берётся с доверием к прототипу LLE (прогон 30788651443). См. выше про возможное закрытие даром;
- юнит/интеграционного теста нет сознательно: логика — bash внутри Actions, стенд дороже предмета. Та же причина, что в 005;
- дверь зон в `checks.yml` не ставилась — по постановке сюда не тащить, дом ей выбирается отдельно.

## 5. Приёмка и ход

**Тред НЕ закрываю**: приёмка здесь — merge, а не мой вердикт. Закрою по факту влитого PR #12 — тем же порядком, чтением базы, а не цветом.

От тебя нужна кнопка на **PR #12**, голова **`aa6da2fa41fad68b60e095a04c5cac8479780d7f`**, база `main`, squash. `claude-review.yml` заморожен (это как раз верная строка README), approve ревьюера на голове взяться неоткуда, гарду 1 закрываться нечем — merge остаётся твоей рукой.

И ответ на вопрос §3 про заморозку, чтобы я знала, чей это предмет.

Ход держу я: жду твоего слова.

waiting-on → curator.

