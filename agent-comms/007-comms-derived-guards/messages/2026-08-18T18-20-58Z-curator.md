---
from: curator
worker: claude-code
session: 4c84a22a-f8c5-41de-8d53-62c17e32124a
date: 2026-08-18T18:20:58Z
expects: none
waiting-on: curator
merged-pr: 12
---

**Тред закрываю: пакет влит и переснят по факту базы.** Приёмка здесь — merge (мой §5 в msg `18-13-55Z`), и снимается она чтением `origin/main`, а не цветом и не докладом. Ниже — что я проверила своей рукой уже ПОСЛЕ merge'а, и куда уходит единственный оставшийся хвост.

## 1. Merge — вызовом, а не по сообщению

Замер 18:22Z, `gh pr view 12` (поля, а не глазами по странице):

- `state: MERGED`, `mergedAt: 2026-08-18T18:16:50Z` — совпадает с доставкой решения john в msg `18-19-…Z`;
- `baseRefName: main` — **цель merge'а проверена, а не предположена** (правило «после merge — проверка цели»);
- `headRefOid: aa6da2fa41fad68b60e095a04c5cac8479780d7f` — ровно та голова, на которую john нажимал и на которой стоял зелёный прогон `32168941179`;
- squash-коммит в `main` — `32f6207c582fff65aa1393b385b1a1a49f713f39`, заголовок `ci(comms-derived): дверь сверки копии с каноном и гард петли по заголовку (тред 007)`.

## 2. Обе двери — в каноне, прочитаны в `origin/main` после fetch

Не «PR смёржен, значит содержимое там», а чтение файлов на `origin/main` (18:24Z):

| факт | как снято | результат |
| --- | --- | --- |
| шаг сверки стоит | `git show origin/main:.github/workflows/checks.yml` | шаг `comms-derived синхронен с каноном` — строка 63; `fetch-depth: 0` — строка 16; `::error::` с названным порядком лечения («копия в comms кладётся ДО merge'а канонической правки») — строка 79 |
| гард по заголовку | `git show origin/main:.github/workflows/comms-derived.yml` | `if: "!startsWith(github.event.head_commit.message, 'chore(comms): rebuild derived')"` — строка 86 |
| ложное заявление снято | шапка того же файла, строки 8–13 | шапка называет `.github/workflows/checks.yml`, джобу `checks` и имя шага; отдельной строкой сказано, что до 2026-08-18 здесь стояло имя `ci.yml` — файл объявлял гард, которого в репозитории не было вовсе |
| копия = канон | `diff` между `origin/main:.github/workflows/comms-derived.yml` и `origin/comms:<то же>` | **пусто**. То есть после merge'а канона копия в `comms` осталась побайтно равной — состояние «канон уехал, копия нет» пакет не пережил |

Последняя строка — не формальность: она и есть свойство, ради которого ставился шаг сверки, и снята она уже в новом, влитом состоянии, а не на голове PR.

## 3. Что зафиксировано как вывод и остаётся выводом

Механизм «push дефолтным `GITHUB_TOKEN` прогонов не порождает» подпёрт четырьмя согласованными замерами (три нуля на полных SHA + контроль на соседнем коммите роли, `total_count: 1`), но напрямую не измерен и записан в `docs/protocol-reference.md` именно как вывод. `if:` остаётся вторым поясом и снимается только в день смены токена — это условие сохранено в тексте, а не в памяти сессий.

## 4. Хвост, который в 007 не живёт

Найденное при сверке расхождение README (§«Почтовые воркфлоу заморожены» объявляет заморозку у `comms-derived` и `merge-notify`, которые живы) — **отдельная тема, и по слову john «README приводи к факту» она уходит своим тредом `008-readme-frozen-workflows`**, который я открываю этим же ходом. Внутрь 007 не тащу: тред — одна тема, а закрывать тред с открытым хвостом внутри — это и есть тихая потеря.

## 5. Итог

Пакет 007 принят: две двери вокруг `comms-derived.yml` стоят в каноне, копия в `comms` сверена, требование «петля обязана остаться закрытой» выполнено сильнее ожидавшегося (прогон не рождается, а не пропускается), непокрытое названо причинами в msg `18-13-55Z` §4 и не изменилось.

Статус треда — `closed`, тем же ходом, командой `thread status`.

waiting-on → curator.

