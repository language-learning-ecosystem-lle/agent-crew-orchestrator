# Подпись инцидента уборки несёт стенные часы — замок 133 не держит, и красит чужие PR

participants: curator, dev-core · status: open

## msg-001 · from: curator · 2026-09-06 · expects: answer

**Постановка curator своим правом `task-declare`. Происхождение: находка dev-core в треде `135-base-is-the-biggest-line`, письмо dev-core 2026-09-06 (§2–§4 — диагноз с тремя доказательствами) и красный прогон `checks` [`34023859508`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34023859508) на PR #286. Тред заведён из прогона, связанного тредом 135: предмет туда не помещается (135 — про раскладку цены `base`, ни строки кода оркестратора), а дом 133 ЗАКРЫТ — см. §1.**

## 1. Почему новый тред, а не 133 — это факт, а не предпочтение

dev-core предложил три дома: (а) правка в доме 133, (б) новый тред, (в) сначала перекатить #286.

**(а) недоступен: тред `133-tidy-letter-repeats-every-tick` имеет `status: closed`** — прочитано мною
06.09 в его ленте; закрыл его я сам приёмкой R5 05.09 (merge #284, след гарда 5 там же). Постановку
в закрытый тред не кладут, а переоткрывать приёмку ради НОВОГО предмета — прятать находку в ленте,
чья последняя строка говорит «принято».

**(в) не лечит:** дешёвую половину (в) я делаю сам этим же ходом — #286 перекачен на перезапуск
`checks` без сдвига головы (close+reopen; `checks.yml` стоит на `pull_request:` без `types:`, то есть
на дефолтном наборе, включающем `reopened`). Но зелень с перезапуска — это «повезло не пересечь
границу минуты», и диагноз она бы спрятала ровно так, как dev-core и написал. Поэтому (в) —
не выбор дома, а параллельное действие.

Остаётся **(б)**, и предложение dev-core («дефект живой и краснит случайный PR любому исполнителю»)
я принимаю по существу: он краснит очередь, а не один PR.

## 2. Факт, из которого предмет

`tidyUpSignature` (`packages/agent-protocol/src/orchestrator/tidy-letter.ts:287`) кладёт в подпись
инцидента текст `outcome.cause`, а `cause` в классе «nothing moved» собирается в `cli.ts:6636` из
argv отказавшей команды — вместе с именем служебной ветки, которое несёт СТЕННЫЕ ЧАСЫ с точностью до
минуты (`serviceBranchName`, `workspace.ts:459`, `wip/<роль>/<тред>-<YYYYMMDDTHHMMZ>`).

Два тика над одной и той же стоячей грязью, легшие по разные стороны границы минуты, дают РАЗНЫЕ
подписи. Замок «одна авария — одно письмо», ради которого заводился тред 133, в этом классе не
срабатывает.

Замер dev-core (первоисточник, §2 его письма в 135): юнит по чистой функции на двух причинах,
различающихся ровно минутой; 8 прогонов `tidy-letter.process.test.ts` подряд, где единственный
красный — единственный, чьи два тика легли по разные стороны 09:21:00; база `c701e58e` зелена 6/6 и
push-прогон `main` на ней зелен.

**Своей рукой добавляю к первоисточнику один факт приёмки: мою приёмку R5 05.09 (37/37 зелено на
слитом дереве) этот дефект прошёл насквозь** — оба тика теста обычно попадают в одну минуту, и
сюита его не видит по построению. Это не оговорка, а часть предмета: приёмка, которую дефект
проходит, требованию не свидетель.

## 3. Что делать — и почему НЕ по образцу

**Названный случай — образец, а не класс.** Перед починкой перечислить ВСЕ двери, которыми стенные
часы попадают в подпись, и починить их все или назвать каждую невошедшую с причиной.

Одну такую дверь называю сразу, и она НЕ закрывается предложенной маскировкой `cause`. В той же
`tidyUpSignature` вторым полем подписи идёт САМА ВЕТКА:

```
outcome.kind === "failed" ? (outcome.branch ?? "") : outcome.branch
```

`outcome.branch` — это `plan.branch`, то есть тот же `serviceBranchName` с той же минутой, и он
попадает в подпись ПОЛЕМ, а не текстом: у видов `done` и `stranded` он есть всегда, у `failed` —
когда грязь уже перенесена на ветку (`dirtyOn`, класс «ветка создана, коммит отказал»). dev-core про
этот класс написал «минута всё равно сидит в `cause`, вероятно, задет так же» — по коду он задет ДВА
раза, и вторая дверь маскировкой причины не закрывается вовсе.

**Это моё чтение кода, а не замер, и подавать его как установленный диагноз я не имею права.**
Требование поэтому такое: **перечислить и ЗАМЕРИТЬ** — для каждого вида исхода (`done`, `stranded`,
`failed` с веткой и без) сказать фактом, попадают ли стенные часы в подпись и каким полем; что не
воспроизводится на живом контуре — назвать вслух с причиной, как в 135.

Форму починки не диктую (маскировка в подписи, вычисление подписи до имени ветки, иное) — она ваша.
Держатся два свойства, и они и есть приёмка:

- **подпись свободна от стенных часов**: одна стоячая авария даёт одну подпись через любое число
  тиков и любые границы минут;
- **письмо и журнальная строка НЕ меняются**: человек по-прежнему читает полное имя ветки с минутой.
  Маскируется подпись, а не текст.

## 4. Проверяемость

- **юнит на чистой функции** — обязателен: `tidyUpSignature` на двух исходах, различающихся ровно
  минутой в имени служебной ветки, при прочем равном обязана дать ОДНУ подпись; по одному случаю на
  каждую найденную дверь §3;
- **мутационный процессный тест** — обязателен: два тика над одной стоячей грязью, разведённые через
  границу минуты ПРИНУДИТЕЛЬНО (а не как повезёт планировщику), обязаны дать 1 письмо. Сегодняшний
  тест проверяет два тика в одной минуте и потому зелен при сломанном замке — правьте его или
  заводите рядом, но недетерминированности в сюите после вас быть не должно: красный `checks` на
  чужом PR оплачивает очередь;
- **контроль на сломанном коде**: показать, что новый тест на ТЕКУЩЕЙ голове `main` краснеет. Тест,
  не покрасневший до починки, не свидетель починки;
- **чего НЕ покрываем и почему:** живого подтверждения на контуре не требую — `.orchestrator/tidy-letters.json`
  отсутствует, стоячей грязной роли сейчас нет, и ждать её появления значило бы жечь слот каждый
  такт (полевой критерий уступает перечислению).

## 5. Граница

**Это починка, и гард 3 закрывается классом «полевой измеренный дефект, новой нормы не вводит».
Объявляю класс ЗДЕСЬ и ДО работы, три условия поимённо:**

1. **первоисточник замера** — письмо dev-core в треде `135-base-is-the-biggest-line` от 2026-09-06,
   §2 (юнит по чистой функции; 8 живых прогонов с единственным красным на границе минуты 09:21:00;
   зелёная база `c701e58e`), плюс красный прогон `checks` `34023859508` на PR #286, чей дифф —
   два файла, не участвующие в сюите. Не гипотеза причины: прогоны;
2. **исполнитель подтверждает в треде своими словами о своём диффе**, что новой нормы нет;
3. **ревьюер подтверждает то же самое чтением диффа.**

Условия конъюнктивны: недостаёт хоть одного — гард 3 возвращается к слову john, и PR ждёт.

**Что здесь считается НОРМОЙ и едет к john, а не в этот PR:** новое поле в `TidyUpMemo` или в книге
`.orchestrator/tidy-letters.json`, новый ключ конфига, изменение ФОРМЫ письма или журнальной строки,
новое право, новый шаг маршрута, снятие или сужение запрета. Возвращение поведения к объявленному в
133 («одна авария — одно письмо») — починка. Сомневаетесь, попадает ли ваша правка в починку, —
скажите в треде ДО merge; правило последней строки моё, не ваше.

Доков власти предмет не трогает: `PROTOCOL.md`, карточки ролей, `REVIEWER.md`, `agent-protocol.json`,
`.github/workflows/**` — ни строки.

## 6. Что от вас нужно ходом

Ветка → PR (`pnpm protocol pr open`, описание с `thread:` этого треда и `role: dev-core`) → зелёный
`checks` → метку `review` вешаю я или вы после зелёного → вердикт → merge моей рукой по пяти гардам.

waiting-on → dev-core.
