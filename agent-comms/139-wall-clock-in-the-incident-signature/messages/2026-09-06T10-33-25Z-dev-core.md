---
from: dev-core
worker: claude-code
session: efe0bf7e-88e7-4bbc-b278-f0d36d44145d
raised: 2026-09-06T10:31:29Z
date: 2026-09-06T10:33:25Z
expects: answer
waiting-on: curator
parked-on: run:289
---

# Метка `review` повешена на #289 — круг идёт, паркуюсь на него

## 1. Что сделано ходом — одно действие, и оно замерено до и после

Ход у меня стоял ровно на одно действие: метка `review` после зелёного `checks` на той же
голове. Перед тем как её вешать, перемерил три вещи — не потому, что что-то подозревал, а
потому что зелёное письмо говорит о моменте прогона, а не о состоянии сейчас:

- **голова не уехала**: `headRefOid` = `f2b1217f54d50f0b71da41dbf2a3c3fd4b3256a1` — та же, что в
  моём прошлом письме и в зелёном прогоне;
- **база не разошлась**: `mergeable: MERGEABLE`, `mergeStateStatus: CLEAN`. Это важно именно
  здесь: `CONFLICTING` потребовал бы перебазировки, а перебазировка сдвинула бы голову и
  аннулировала зелень, с которой метка снимает срез;
- **зелень — на ЭТОЙ голове, а не на соседней**: прогон `checks` `34026216873` имеет
  `headSha: f2b1217f54…`, `conclusion: success`. Сверял по sha прогона, а не по письму о нём.

Метка повешена: `gh pr edit 289 --add-label review`, меток до этого не было ни одной (то есть
это событие `labeled`, поднимающее круг, а не висящая с прошлой головы метка, которая круг бы
НЕ подняла).

## 2. Круг действительно поднялся — и это замер, а не ожидание

Метка поднимает круг СОБЫТИЕМ навешивания, поэтому «повесил» и «круг идёт» — два разных факта,
и второй проверен: прогон **`Claude PR Review` `34027685093`**, событие `pull_request`,
`headSha: f2b1217f54…`, статус `in_progress`, заведён 10:32:03Z.

**Проверил и то, чем круг мог бы самоотмениться, не сказав ни слова:**
`git diff origin/main f2b1217f -- .github/workflows/` — **пусто**. Воркфлоу моей ветки и
`origin/main` совпадают побайтово, значит ни ветвь «PR правит воркфлоу», ни ветвь «ветка
отстала по воркфлоу» самопропуска не дадут. Это ровно тот случай, где вердикта можно ждать
неделю, а его не будет; поэтому мерил до парковки, а не после.

## 3. Где стои́т и что дальше

- PR **#289**, голова `f2b1217f`, ветка `dev-core/139-wall-clock-in-the-incident-signature`;
- `checks` — **зелен** (`34026216873`), метка `review` — **висит**, круг — **идёт**
  (`34027685093`);
- **паркуюсь на `run:289`**: пока машина судит, двигать нечего, а вердикт придёт в тред сам и
  парк снимет. В foreground круг не жду — он пишет сюда без меня.

Рабочее дерево чистое, незакоммиченного нет. Своей рукой в этом ходу не тронуто ничего, кроме
метки на PR: ни коммита, ни push, ни правки дерева.

## 4. Две вещи к вашему ходу — обе к merge, ни одна не к коду

**(а) Оговорка §4 моего прошлого письма всё ещё без ответа, и она из тех, что говорятся ДО
кнопки.** Книга `.orchestrator/tidy-letters.json`, записанная до этой правки, несёт
немаскированные подписи; первый тик после выкатки над уже названной стоячей аварией пошлёт
одно лишнее письмо и запишет новую подпись. Направление — «сказано лишний раз», не «промолчано
о новом». Считаю это частью починки, а не новой нормой, и поля совместимости не предлагаю. Если
читаете иначе — правило последней строки ваше, скажите до merge.

**(б) Гард 1 на этом PR без имени воркфлоу ревью соврёт.** `pnpm protocol merge-gate` якорит
вердикт на прогон ревьюера по имени; без `--review-workflow 'Claude PR Review'` гард 1
отдаёт отказ вида «судишь сам себя» даже на настоящем закрытом круге. Говорю не как о новости,
а чтобы отказ гарда, если он придёт, не был прочитан как дефект PR.

Гард 3, условие 2 (мои слова о своём диффе, что новой нормы нет) сказано в прошлом письме и
здесь не переигрывается — дифф с тех пор не менялся ни на строку, голова та же.

waiting-on → curator.

