---
from: curator
worker: claude-code
session: 256e7934-056a-4743-a45c-933c856c848f
raised: 2026-09-13T17:08:20Z
date: 2026-09-13T17:12:50Z
expects: none
waiting-on: —
---

# Красный `checks` по #406 — НЕ мой дифф: коллизия секунд в фикстуре `self-restart.process.test.ts`, УЖЕ починенная на `main` (#383, `e25da912c`), приехавшая в базу через 3 мин 05 с ПОСЛЕ старта прогона. Лечение — ребейз: новая голова `f637753c4`, прогон 34770776128 идёт

Новым мне было одно письмо — `github`, msg-058: `checks` по #406 = `failure`, голова
`43f7845353407117ce504eb847cc1ccecabb840f`, прогон `34770040174`. Разобрано, вылечено, ниже фактами.

## 1. Что именно упало — файл, строка и текст ассерта

Прогон `34770040174`, джоба `checks`, упал ОДИН шаг — `Run pnpm test`; шаги 1–10
(`agent-protocol.json`, исключительный ресурс, `lint`, `typecheck`, git-личность) зелёные, шаги 12–13
пропущены следом за красным. Счёт прогона: **1 файл упал, 241 прошёл (242)**, то есть падение
точечное, а не развал сюиты.

Место названо самим логом: `src/orchestrator/self-restart.process.test.ts:711:30`,
`AssertionError: expected '2026-09-13T16:57:11Z' not to be '2026-09-13T16:57:11Z'`.

Сверила строку 711 в дереве БАЗЫ этого PR (`git show cbf3a5006:…self-restart.process.test.ts`):

```
expect(memory?.went).toBe(true);
expect(memory?.at).not.toBe(memory?.drainSince);   ← строка 711
```

Два штампа ISO **с точностью до секунды**, два такта подряд; когда фикстура успевает за одну
секунду — штампы совпадают и `not.toBe` падает. Мой дифф — три пути доков (`docs/roles/curator.md`,
`docs/roles/dev-core.md`, `docs/journal/curator.md`), ни один из них в `packages/**` не заходит:
исполняемого кода этот PR не трогает вовсе.

## 2. Класс уже замерен и УЖЕ закрыт — вторым случаем оказалась моя же голова

На `origin/main` (`e25da912c`) в этом же месте стоит не тот текст, что в моей базе:

```
// … measured on run 34758356217 of PR #383,
// `expected '2026-09-13T12:55:35Z' not to be '2026-09-13T12:55:35Z'` …
sleepPastTheSecond();
```

То есть ровно эта коллизия была замерена в поле на прогоне `34758356217` (PR #383, `12:55:35Z`),
и там же оплачена: фикстура ждёт до следующей секунды, а ассерт усилен со «просто отличается» до
`Date.parse(at) > Date.parse(drainSince)`. Мой прогон — **второй полевой случай того же класса**,
и нового предмета он не заводит: лекарство лежит в `main`.

**Арифметика отставания, которая всё объясняет:** мой `checks` стартовал `2026-09-13T16:55:41Z`,
а `e25da912c` (#383) лёг в `main` в `16:58:46Z`. База PR (`cbf3a5006`) была на **3 мин 05 с** старше
починки. Ни дефекта в моём диффе, ни дефекта в коде — отставшая база.

## 3. Что сделано: ребейз, а не доклад о дефекте

- `git rebase origin/main` на `curator/180-red-notifier-letter-is-not-lost-mail` — конфликтов нет;
- **целость ребейза замерена, а не предположена:** `git diff --numstat origin/main...HEAD` →
  `27 0 docs/journal/curator.md`, `7 0 docs/roles/curator.md`, `7 0 docs/roles/dev-core.md` —
  **3 пути, 41 вставка, 0 удалений**, ровно как до ребейза (сверено с числом из моего письма
  `16:58:39Z`);
- `git push --force-with-lease` в `17:10:15Z`: `43f784535 → f637753c4`. Метки `review` на #406
  не было и нет — перезапись головы аннулировать было нечего;
- новая голова **`f637753c48778618dfc23c83ff0f61d1f6104ff2`**, прогон `checks` **`34770776128`**
  заведён сам в `17:10:19Z`, на момент письма `in_progress`. У него не сижу — исход приедет сюда
  письмом `github` на любом конце.

## 4. Правило чтения, которое отсюда следует, — называю, но своей рукой в карточку не кладу

Красный прогон на PR мерил дерево **СВОЕЙ базы**, а не сегодняшний `main`: прежде чем звать дефект
в чужой тред, падающее место сверяется в двух версиях — `git show <база>:<путь>` против
`git show origin/main:<путь>`; разошлись — лечение ребейз, и оно бесплатно. Строка ведёт себя как
правило (поведение роли без неё другое: тред на уже закрытый дефект), но в карточку её отсюда
не кладу по двум причинам: #406 уже стоит кнопкой john и несёт ДРУГОЙ предмет (красное письмо
уведомителя), а второй док-PR под то же решение — вторая кнопка john под одним решением. Записала
в память роли; если следующая сессия сочтёт, что правилу место в карточке, оно едет своим диффом
вместе со своим полевым случаем в журнал (правило прироста, тред `143`).

## 5. Куда ещё смотрела и почему пусто

`gh pr list --state open` — пять PR. Мой здесь один (#406). Остальные принадлежат чужим тредам, и
основание прежнее: восхождение и след гарда 5 обязаны лечь в ИХ ленты, а мой ход привязан к `180`.
Замер на `17:11Z` для того, кого поднимут в этих тредах: **#401** (`journal/189-…`) —
`MERGEABLE/CLEAN`, метки нет; **#399** (`193-reviewer-fallback-account`) — `UNSTABLE`;
**#388** — `CONFLICTING/DIRTY`, метка `review` висит со старой головы; **#386** —
`CONFLICTING/DIRTY`, метки нет. Про #388 и #386 добавить к письму `16:58:39Z` нечего: расхождение
с базой у обоих никуда не делось, и до `18:40Z` любой поднятый круг всё равно сгорит.

## 6. Дерево и ход

Незакоммиченного нет: рабочее дерево роли на ветке `curator/180-red-notifier-letter-is-not-lost-mail`,
голова `f637753c4`, всё запушено; тело письма — в `/tmp`, вне обоих чекаутов.

**Ход → `—`, парка ЗДЕСЬ по-прежнему нет.** Основание то же и оно в силе: `180` — стоячий адрес
аварий уведомителя, парк съел бы следующий звонок; сверх того парк `run:` под живым `checks` снимает
только потолок, а исход прогона и так звонит сюда сам.

Что ждёт следующего, кого сюда поднимут, — ровно три вещи, две из них прежние:
1. исход прогона `34770776128` по голове `f637753c4` (приедет письмом сам);
2. **после `18:40Z`** — повесить `review` на #406, но ТОЛЬКО поверх зелёного `checks` на текущей
   голове (если прогон выше красный — сначала читать его, а не вешать метку);
3. ответ john в `198-mute-key-by-name-eats-other-bells`.

