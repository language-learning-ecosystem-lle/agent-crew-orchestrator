---
from: curator
worker: claude-code
session: 6dd85050-29bc-488d-bb8f-40961e76d282
raised: 2026-09-14T13:52:52Z
date: 2026-09-14T13:56:00Z
expects: ack
waiting-on: curator
parked-on: run:406
---

**Действие, ради которого меня подняли, уже сделано ЧУЖОЙ рукой: метка `review` на #406 висит с `13:52:28Z`, круг идёт — прогон `34851961887` на той же голове `957e412b2`. Своей рукой я поправила только тело PR (диффа не трогает) и промерила предмет треда.**

Новым мне было одно письмо — `msg-065`, зелёный `checks`. Оно и говорит «ход у автора ровно на это одно действие». Действия к моменту подъёма уже не было.

## 1. Метку повесила не эта сессия — замер, а не догадка

```
gh api …/issues/406/timeline
  labeled review 2026-09-14T13:52:28Z by maysway
```

Это 2 секунды после того, как я отправила `msg-064`, и на 45 секунд раньше письма о зелёных чеках
(`msg-065`). То есть рука снаружи контура прочла мой план из `msg-064` и исполнила его пункт 1 сама,
пока подъём пары ещё стоял в очереди.

Круг на этой голове состоялся и идёт:

```
gh api …/actions/runs?head_sha=957e412b2
  34851961887  Claude PR Review  event=pull_request  status=in_progress  created=13:52:30Z
  34851005878  checks            event=pull_request  concl=success       created=13:43:26Z
```

`event=pull_request` — годный для гарда 1 якорь, не самопропуск и не `@claude`-коммент.
Голова под меткой та же, что несла зелёные чеки: `957e412b23616038e29d079f125707bec7221135`.
Перевешивать нечего, второй круг не нужен — а лишний круг здесь стоил бы денег впустую.

**Это не жалоба, а факт для следующего читателя:** совпади порядок иначе, я повесила бы метку
вторым разом и подняла бы ВТОРОЙ круг на той же голове. Дешёвая страховка от этого — та, что я и
применила: перед меткой читать `timeline` и прогоны головы, а не только поле `labels`.

## 2. Дверь на `13:56Z` — тот же вердикт, что и в `msg-064`

`pnpm protocol merge-gate --ref origin/main --pr 406 --review-workflow 'Claude PR Review'`:

| гард | ответ |
| --- | --- |
| 1 · approve на текущей голове | **STOP** — `no approve verdict on 957e412` (круг ИДЁТ, вердикта ещё нет) |
| 2 · зелёный `checks` | **STOP** — `not green: review=IN_PROGRESS` |
| 3 · восхождение | `you` — тред `180-notifier-down` |
| 4 · доки власти | **STOP** — `john merges this one — it changes docs/roles/curator.md, docs/roles/dev-core.md` |
| 5 · след | `you` |
| сливаемость | **`MERGEABLE (UNSTABLE)`** — `UNSTABLE` = живой круг ревью, не конфликт |

Гард 2 просел не назад, а вперёд: `checks` на этой голове зелёные (`msg-065`), а `IN_PROGRESS`
теперь у самого круга ревью. Дверь считает оба чека, и это её нормальное поведение под живым кругом.

**Кнопка #406 — john при любом исходе круга** (гард 4, обе карточки — доки власти).

## 3. Тело PR поправлено — дифф и голова не двигались

В теле #406 стояла строка «Дифф: … `docs/journal/curator.md` +27», описывающая дерево ДО вчерашнего
переезда записи. Ревьюер читает тело вместе с диффом, а дифф уже два часа как другой:

```
git diff --numstat origin/main(43bd494f0)...957e412b2
  59  0  docs/journal/curator/180-notifier-down.md
   7  0  docs/roles/curator.md
   7  0  docs/roles/dev-core.md
```

Отставшая строка указывала ровно на тот путь, который стоячая норма 13.09
(`195-journal-tail-serializes-work`) закрыла для пополнения, — то есть подсказывала ревьюеру дефект,
которого в диффе нет. Переписала строку по факту и добавила абзац о переезде.

Сверено после правки: `head=957e412b23616038e29d079f125707bec7221135` — **не двинулась**.
Правка тела головы не касается, значит идущий круг она не обнуляет и перевешивания не требует.

## 4. Предмет треда: уведомитель на `13:58Z` — по-прежнему ноль аварий

`180` — стоячий адрес, поэтому адресат мерится каждый ход, даже когда ход про PR.
Журнал ящика, та же эпоха (`09:36:18Z`, pid 2908309):

| счёт | `13:50Z` (`msg-064`) | `13:58Z` |
| --- | --- | --- |
| курьерских строк | 1343 | 1398 |
| `telegram: delivered` | 34 | **38** |
| `telegram:` с НЕ-`delivered` хвостом | 0 | **0** |
| `… nothing to announce` | 290 | 303 |

За восемь минут четыре новых доставки и ноль недоехавших. Оговорка прежняя и снимать её нечем:
счёт мерит гейт ПЕЧАТИ, а «не позвонил вовсе» им не ловится.

Последняя курьерская строка — `5 parked, 5 of them asking, 0 of those new; 0 waits, 0 of them new;
8 stalled over 180m, 0 of them new — nothing to announce`: `0 of those new` значит, что про эти
парки и простои уже звонили, а не что их нет.

## 5. Хвост открытых PR — тринадцать, своих под кнопкой ноль

`gh pr list --state open` на `13:55Z` — 13 штук. Моих к кнопке нет: #406 за гардом 4, а
#432 (`203`), #430 (`191`), #427 (`197`) — ветки curator ЧУЖИХ тредов, и восхождение со следом
гарда 5 по ним обязаны лечь в ИХ ленты; этот ход связан ровно одним тредом.

Одно замечание для читателя, чтобы номер не спутали: **#426 (`dev-core/180-suite-git-env-scrub`) к
этому треду НЕ относится** — в его описании `thread: 180-selfheal-leaves-the-workspaces-behind`.
Совпадает только ведущее число ветки.

## 6. Ход и парк

Остатка работы моей рукой в этом треде нет: метка висит, круг идёт, кнопка чужая. Парк — `run:406`,
его снимет само письмо вердикта, то есть минуты, а не часы; цена та же и называю её честно: пока
`180-notifier-down` запаркован, новая авария уведомителя приедет не сюда, а старшему открытому
непаркованному треду.

**Следующей сессии, которую поднимет исход круга:**

1. **`approve`** → сверить гарды дверью на `957e412b2` и **передать кнопку john**: гард 4 не снимается
   ничем, кроме его руки. Своей рукой мёржить нечего;
2. **`request changes` / красный круг** → читать вердикт, править ветку, зелёный `checks`, снять и
   повесить метку заново (перевешивание — единственный способ поднять круг);
3. **вердикт-сирота** (круг закрылся, вердикта в его окне нет) → не мёржить и не считать гард 1
   закрытым; поднимать круг перевешиванием метки.

Незакоммиченного нет. Рабочее дерево роли —
`/home/lle/projects/agent-crew-orchestrator/.worktrees/curator@180-notifier-down`, ветка
`curator/180-red-notifier-letter-is-not-lost-mail`, голова `957e412b2`, всё запушено; в репозиторий
эта сессия не писала вовсе. Тело письма — в `mktemp -d`, вне обоих чекаутов.

waiting-on → curator, парк → `run:406`.

