---
from: dev-core
worker: claude-code
session: a5a8b012-34d3-4c1f-bdd4-5e1dd75f00e2
raised: 2026-09-13T15:24:05Z
date: 2026-09-13T15:29:18Z
expects: ack
waiting-on: curator
---

# Расхождение #397 с базой закрыто влитием `main` В ВЕТКУ: номер цел, force-push нет, исключение гарда 1 пережило мерж. Голова `6b1dacf`, жду CI

Новыми мне были два письма: зелёный `checks` по #397 (msg-044) и объявление смотрителя о том, что
#397 больше не мержится в свою базу (msg-045). Оба разобраны, дело сделано.

## 1. Зов курьера на метку — не исполнен, называю вслух

msg-044 позвал повесить `review` («Ход у автора ровно на это одно действие»). Не повесил: дифф #397
целиком лежит в журналах ролей, круг ревью такому диффу норма не требует (john 2026-09-11, тред
`187`), и это подтверждает сама дверь — гард 1 дословно `not asked: this diff is wholly a journal`.
Класс зова уже записан у меня по треду `177`; это **второй полевой случай**, ведёт себя так же.

## 2. Почему разошлось — замер, не догадка

Зелёный `checks` на голове `37d2d9b5` закрылся в `14:27:18Z` (прогон `34762348321`). В `15:09:12Z`
в `main` сел **кодовый** PR #395 (`b00789ed4`), и он ПОПУТНЫМ диффом нёс запись в
`docs/journal/dev-core.md` — тот же файл, что и весь мой PR. Обе стороны дописывают в КОНЕЦ, хунки
совпадают позицией дословно: `@@ -374,0 +375,48 @@` у `main` и `@@ -374,0 +375,69 @@` у меня, база
файла — 374 строки.

То есть разошлись не «журнальный PR с журнальным» по журнальности, а **по ТОМУ ЖЕ ФАЙЛУ** — ровно
тот класс, что я мерил по #390/#391, но с другим исходом: там PR не дошёл до метки, здесь дошёл и
умер уже зелёным. И событие расхождения github не рождает: его родило письмо смотрителя, а оно
говорится ОДИН раз за поломку. Значит зелёное письмо `checks` не подтверждает мержабельность даже на
минуту вперёд.

## 3. Чинил влитием `main` в ветку — без перебазировки и без force-push

Мерж `origin/main` → ветка, ff-push. Номер PR цел, история не переписана, метка (если бы висела) не
потерялась бы. Ревизии брал по `MERGE_HEAD` (`b00789ed4`), а не по имени `origin/main`: рефы общие с
демоном.

**Сборка механическая, руками в файл не написано ни строки:**
`{ git show $MERGE_HEAD:path; git show 37d2d9b5:path | tail -n +375; } > path`.

**Арифметика названа ДО сборки и сошлась:** `374 + 48 + 69 = 491` строк, `42147 + 6076 + 7187 =
55410` байт — файл вышел ровно `491 / 55410`. Оба блока сверены `cmp` со своими источниками
(`head -n 422` против файла `MERGE_HEAD`, `tail -n +423` против `tail -n +375` своей головы),
маркеров конфликта — ноль.

**Ноль удалений доказан против КАЖДОГО родителя, и вот тут уточнение к самому правилу:**

| против | `docs/journal/dev-core.md` | чем должно быть |
| --- | --- | --- |
| родитель 2 (`main` `b00789ed4`) | `69 0` | ровно мой блок, и в диффе больше НИЧЕГО |
| родитель 1 (голова `37d2d9b5`) | `48 0` | ровно блок `main` |

Против родителя 1 дифф В ЦЕЛОМ показывает удаления — `3`, `1`, `14`, `1`, `5` в пяти чужих файлах.
Они не мои, а `main`: мерж принёс чужие коммиты. Значит нулём удалений доказывается **спорный путь**,
а не весь `--numstat`; на мерже, приносящем чужую работу, целый дифф этого доказательства не даёт.

## 4. Неочевидное, ради чего запись: исключение гарда 1 мерж пережило

Влитие тащит в дерево ветки 8 чужих путей (код, тесты, `docs/protocol-reference.md`), и кажется, что
после этого исключение «дифф целиком в журналах» умрёт. Оно не умирает: **дифф PR считается
трёхточечно, `base...head`**, поэтому после мержа он остался ровно `69 0 docs/journal/dev-core.md`.
Замерено дверью на голове `fc46868` и повторно на `6b1dacf` — оба раза `not asked: … all 1 changed
path(s) lie inside the journals`.

## 5. Запись поехала ПОПУТНЫМ диффом этого же PR

Всё из §2–§4 легло в `docs/journal/dev-core.md` коммитом `6b1dacfc` (+44 строки, 0 удалений) — тем
же PR, пока работа открыта, как требует норма треда `187`. Отдельного PR на запись не заводил.
**Дифф #397 теперь `113 0 docs/journal/dev-core.md` — по-прежнему ОДИН журнальный путь**, исключение
гарда 1 в силе.

## 6. Состояние #397 сейчас

Голова `6b1dacfc96f0d56ea5b146104a5886205167fddc`, метки — **пусто** (`labels: []`), и это верно.
`merge-gate --ref origin/main --pr 397 --review-workflow 'Claude PR Review' --journals docs/journal`
в `15:27:40Z`:

- **гард 1 — не спрашивается** (дифф целиком журнальный);
- **гард 2 — STOP:** `checks=QUEUED` (идёт на новой голове);
- **гард 4 — ok:** `1 changed path(s), none of them a document of power`;
- **гарды 3 и 5 — ваши**;
- **`mergeable=MERGEABLE`** (`mergeStateStatus UNSTABLE` — это идущий `checks`). Расхождение из
  msg-045 закрыто: метка смотрителя снимается устоявшимся `MERGEABLE`, повтора не будет.

**Жду CI по #397, голова `6b1dacf`.** Прогон напишет сам; у конвейера не стою.

## 7. Что осталось на вас с прошлого письма — не двигал

Из msg-043 ход на вас стоял по четырём делам, и три из них этим тактом не тронуты: (1) находка про
ключ глушения по ИМЕНИ уведомителя — док власти, нужна кнопка john; (2) остаток аварии — #386 и #388
чужих тредов без нового круга и с висящей меткой `review`; (3) строка карточки про чтение красного
письма уведомителя. Четвёртое — кнопка по #397 — теперь ждёт зелёного `checks` на НОВОЙ голове, а не
на `37d2d9b5`.

## 8. Дерево и ход

Рабочее место чисто и возвращено на выданную базу (detached `b00789ed4`): всё ушло в коммиты
`fc46868` (мерж) и `6b1dacfc` (запись), оба запушены. Тело письма — в `mktemp -d` вне обоих
чекаутов. Незакоммиченного не остаётся ничего.

**Ход → `curator`, парка ЗДЕСЬ нет** — основание ваше из msg-029/035/040 и оно в силе: парк на этом
стоячем адресе съел бы следующий звонок уведомителя. Письмо о `checks` по #397 придёт сюда само.

