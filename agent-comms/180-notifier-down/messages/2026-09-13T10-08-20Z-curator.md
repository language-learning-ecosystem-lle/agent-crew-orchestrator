---
from: curator
worker: claude-ai
date: 2026-09-13T10:08:20Z
expects: none
waiting-on: curator
delivers: john
priority: high
merged-pr: 353
---

**Слово john (чат 2026-09-13 ~09:15Z): #353 СМЁРЖЕН его кнопкой. Уведомитель пинит ветку — код и конфиг из одного коммита.**

*(доставка задержана: GitHub отказывал записью пятисотыми ошибками с ~09:15Z по ~10:06Z; текст лежал у чатного curator и записан первой удавшейся попыткой)*

## 1. Что село

Четыре места (`merge-notify.yml` ×2, `ci-outcome.yml` ×2, `notifier-watch.yml`, `comms-derive.sh`) читают конфиг с `--ref origin/main --no-fetch`.

**Дефект замерен по секундам:** между чекаутом кода (`12:33:32`) и чтением конфига (`12:33:41`) в `main` сел бампающий схему PR — код одной версии, конфиг другой, отказ ВЕРНЫЙ, **уведомление о merge #345 потеряно**. Тред `178` о своём merge так и не узнал.

**Два места НЕ пинятся, оба названы в тесте:** мелкий чекаут коммита (там ref создаёт сам fetch) и круг ревью (пара «код ⟷ конфиг» разорвана вторым чекаутом по построению).

**Тест доказан мутацией** — снятие флага краснит с именем файла, номером строки и текстом команды.

## 2. Остаток, названный и НЕ починенный

Проба роли в трёх воркфлоу читает **любой ненулевой код** как «роли нет в конфиге» — в том прогоне дверь соврала о роли, которая в конфиге ЕСТЬ. Коды различимы (1 — нет роли, 2 — отказ двери). Чинится в трёх местах сразу, вынесено отдельно; предмета john сейчас не заводит.

## 3. Соседнее по кнопкам

**#378** — john кнопку не нажал: метки нет, круг не проходил. Исключение #370 на `PROTOCOL.md` не распространяется (оно снимает гард 1 только с диффа, все пути которого в журналах ролей). Метку вешает curator, вердикт → кнопка john.

waiting-on → curator.
