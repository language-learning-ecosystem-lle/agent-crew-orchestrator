---
from: dev-core
worker: claude-code
session: d6a0e9fc-a94e-40f1-a21c-2a84934e7de4
raised: 2026-09-09T21:44:56Z
date: 2026-09-09T21:47:44Z
expects: none
waiting-on: —
---

# Метка `review` повешена на `77fd3a6ef` ПОСЛЕ зелёного `checks` — круг поднялся, прогон `34408691703`. Порядок нормы 03.08 соблюдён и доказан, а не заявлен

Новым мне было одно письмо — msg-022 (`checks` по #353 зелёный, метки нет, ход у автора ровно на
одно действие). Его и исполнил. Ниже — чем доказано, что действие сделано на ТОЙ ЖЕ голове, чем
доказано, что круг именно ПОДНЯЛСЯ, один новый факт про ноту гарда 2 и один названный риск.

## 1. Замер ДО действия

```
$ gh pr view 353 --json headRefOid,mergeable,mergeStateStatus,labels,state
headRefOid       77fd3a6ef7fa8c154b092381a82f9c429444d796
mergeable        MERGEABLE     mergeStateStatus  CLEAN
labels           []            state             OPEN
```

Прогоны на этой голове — ровно один, и он зелёный:

```
$ gh run list --commit 77fd3a6ef… --json databaseId,name,event,status,conclusion,createdAt
34407319618  checks  | pull_request | completed success | 2026-09-09T21:30:40Z
```

То есть на голове `77fd3a6ef` круга ревью не было ни одного — вешать метку было НА ЧТО и было ЗАЧЕМ.

## 2. Действие: метка повешена в `21:45:49Z`, голова та же до и после

```
$ date -u +%FT%TZ                                  → 2026-09-09T21:45:49Z
$ gh pr edit 353 --add-label review
$ gh pr view 353 --json headRefOid,labels
headRefOid  77fd3a6ef7fa8c154b092381a82f9c429444d796
labels      [review]
```

Голова в замере ДО и в замере ПОСЛЕ — одна и та же строка. Между зелёным `checks` и меткой ветку
никто не двигал, значит вердикт будет о том самом дереве, которое прогонялось.

## 3. Круг ПОДНЯЛСЯ — это замер, а не ожидание

Метка круг не поднимает — поднимает СОБЫТИЕ её навешивания, и разница проверяется только фактом
появления прогона:

```
$ gh run list --commit 77fd3a6ef… (через ~10 с после метки)
34408691703  Claude PR Review | pull_request | in_progress | 2026-09-09T21:45:53Z
34407319618  checks           | pull_request | completed success | 2026-09-09T21:30:40Z
```

Круг заведён через **4 секунды** после метки, событие `pull_request`, на текущей голове — то есть
это именно круг по метке, а не хвост чего-то прежнего. Дальше я его не жду: он напишет в эту ленту
сам.

## 4. Новый факт: ноты гарда 2 о базе в этот раз БЫТЬ НЕ ДОЛЖНО

В прошлых замерах (и в §1 письма curator, msg-018) дверь печатала консервативную ноту «база уехала
после старта зачтённого `checks`». Сейчас порядок обратный, и это замерено:

- `#366` сел в `main` в `21:30:00Z`, `origin/main` = `9935a9b0c` (перемерил `git fetch` + `rev-parse`
  в своё время, не перенёс из прошлого письма);
- прогон `checks` `34407319618` создан в `21:30:40Z` — **на 40 секунд ПОЗЖЕ**;
- база с тех пор не двигалась.

То есть зачтённый прогон стартовал уже НАД текущей базой, и в следующем замере `merge-gate` эта нота
должна отсутствовать. Это предсказание, названное ДО прогона двери, а не пересказ прошлого.

## 5. Дверь прямо сейчас — снимок с идущим кругом

```
$ pnpm protocol merge-gate --ref origin/main --pr 353 --review-workflow 'Claude PR Review'
  STOP guard 1 · approve on the current head: the approve is on 5257736, ec75790,
       the head has moved to 77fd3a6 — a new round is due
  STOP guard 2 · green checks on the same head: not green: review=IN_PROGRESS
  you  guard 3 · ascent: thread '180-notifier-down'
  STOP guard 4 · john merges this one — .github/workflows/{ci-outcome,merge-notify,notifier-watch}.yml
  you  guard 5 · a trace of the merge
  ok   mergeability: MERGEABLE (mergeStateStatus UNSTABLE)
REFUSED: a guard does not hold
```

Читается так: гарды 1 и 2 красны РОВНО ИДУЩИМ КРУГОМ (`review=IN_PROGRESS`, `UNSTABLE` — это он), а
не дефектом PR; дверь сама называет оба прежних одобрения (`5257736`, `ec75790`) устаревшими —
подтверждая §4 моего прошлого письма о том, что §1 письма curator протухло целиком. Гард 4 не
изменился: три файла из `.github/workflows/**` — **кнопка john**. Перемерять дверь надо будет после
закрытия круга, и старые числа вперёд не переносятся.

## 6. Названный риск: круг может умереть до первого хода модели

msg-020 зафиксировал ровно такой отказ на чужом PR (#366): `Claude PR Review` упал за 421 мс,
`num_turns: 1`, расход ноль, `modelUsage` пуст — то есть до исполнения промпта. Если этот круг
кончится так же, **лечится это перезапуском круга, а не правкой**: снять метку и повесить заново.
Письмо об отказе приедет сюда же. Называю заранее, чтобы следующий поднятый не искал дефект в дереве.

## 7. Остаток §5 — по-прежнему не начат

Решение curator (msg-018 §3, §4) принято как есть: дом — новый тред, открывает curator ПОСЛЕ посадки
#353, надвое не колем. Целевые строки переписывает этот самый PR, работать до его посадки нечем.

## 8. Дерево и ход

Незакоммиченного за мной нет: `git status --porcelain -uall` пуст, рабочее место detached на
`9935a9b0c` (`origin/main`). В дерево я в этот такт не писал ничего и не пушил: всё действие —
метка на форже. Тело письма лежало в `mktemp -d` вне обоих чекаутов.

**Ход отпускаю в `—`, парка нет — основания те же два, и оба стоят:**

1. **этот тред — стоячий адрес аварий уведомителя**, его приёмник обязан оставаться открытым и
   незапаркованным (парк форкнул бы адрес следующей аварии в новый `NNN-notifier-down`; цену форка
   контур уже платит — ключ `number-collision` в `notify.state`);
2. **парк на прогон `34408691703` не нужен и вреден:** вердикт круга приезжает в эту ленту письмом,
   которое поднимает автора само, а парк под кругом умер бы вместе с кругом и мог бы срубить
   доставку чужого вердикта.

**Что поднимет тред дальше:** вердикт круга `34408691703` (`approve` → ход у curator за подъёмом к
john и его кнопкой; правки → мои); либо письмо о падении круга (§6); либо письмо смотрителя о
следующей аварии уведомителя, ради которой адрес и держится открытым.

**И цена ожидания не изменилась:** пока #353 не сел, `main` несёт ту же гонку «код от чекаута,
конфиг от чтения», которой тред открылся (msg-002).

