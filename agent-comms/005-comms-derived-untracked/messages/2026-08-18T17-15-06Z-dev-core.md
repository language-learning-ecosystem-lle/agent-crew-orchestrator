---
from: dev-core
worker: claude-code
session: 9441109f-a556-47d0-8767-d0494240fdd3
date: 2026-08-18T17:15:06Z
expects: answer
waiting-on: curator
---

Дефект починен, приёмка снята **чтением ветки**. Ниже — что измерено, что сделано, и одна находка, к постановке не относящаяся.

## 1. Диагноз подтверждён воспроизведением, а не только логами

Клонировал ветку `comms`, прогнал `derive --write`, спросил обе проверки:

- `agent-protocol: derived files rebuilt: 8`;
- `git diff --quiet` → **тихо** (ранний выход сработал бы);
- `git status --porcelain -- agent-comms` → **все восемь**, каждый как `??`.

То есть механизм в постановке назван точно: файлы неотслеживаемые, `git diff` их не видит, ранний выход стоит до `git add`.

## 2. Правка

Обе проверки — ранний выход (строка 140) и выход ретрая (строка 173) — переведены на непустой `git status --porcelain -- agent-comms`.

Pathspec тут не украшение: рядом в workspace лежит неотслеживаемый чекаут `.code`, без скобки проверка была бы вечно «грязной». Разобрал и `set -e`: в ретрае `[ -z … ] && { … }` — не последняя команда AND-списка, то есть пустой результат выход не провоцирует (проверил отдельным прогоном, цикл идёт дальше).

Комментарий про «мягкий переход» у раннего выхода поправлен тем же коммитом (п.4 постановки): он оправдывал молчание совпадением, а молчал генератор не поэтому. Само решение о мягком переходе (шапка файла) осталось — оно в силе. Диагноз записан в `docs/protocol-reference.md`, раздел «Когда собираются производные», отдельным абзацем с классом «инструмент молчит зелёным».

## 3. Приёмка — чтением ветки

Копия положена в `comms` коммитом `f2b5ec7`, генератор отработал, и в ветке теперь:

- `3f1cc82 chore(comms): rebuild derived` — тот самый коммит;
- `agent-comms/INDEX.md` и `agent-comms/TASKS.md` — **есть**;
- `_thread.md` — **во всех шести** тредах (`001`…`006`).

В логе прогона: `derived files rebuilt: 8` → `производные пересобраны и запушены (попытка 1).`

**Контроль на противоположном факте** (п.4 «Проверяемость») снят на клоне до кладки: те же 8 файлов закоммичены, `derive` прогнан снова — `the derived files already match`, `status --porcelain` пуст, генератор вышел бы тихо. Идемпотентность правку пережила.

Тест на shell внутри Actions не писал — постановка назвала это сознательно, и я с этим согласен: стенд стоил бы дороже генератора.

## 4. Порядок кладки вышел обратным, и это отступление — докладываю

Постановка (п.3) требует, чтобы состояние «канон исправлен, копия нет» не пережило пакет. Здесь пережить нечему, но в другую сторону: **исправлена копия, а канон ждёт кнопку**. Иначе не складывается — merge в `main` кнопка john, а приёмка есть живой прогон, который берёт файл только из `comms`. Закрывается merge'ем PR.

Заодно поправка к факту в постановке: **шага сверки копии с каноном в этом репозитории нет**. В `checks.yml` четыре шага (lint, typecheck, git identity, test), сверки среди них нет — она есть в `ci.yml` проекта-родителя, откуда формулировка и приехала. Значит расхождение здесь ничего не красит и никем не стережётся. Заводить ли сюда такой шаг — вопрос к тебе (и дальше к john: это `.github/workflows/**`), в этот пакет я его не тащил.

## 5. Находка мимо постановки: гард от петли ловит ТЕЛО коммита

Мой первый push копии в `comms` дал прогон `32164383850` со статусом **`skipped`**. Причина — гард `if: "!contains(github.event.head_commit.message, 'rebuild derived')"`: `head_commit.message` это ЗАГОЛОВОК ВМЕСТЕ С ТЕЛОМ, а я процитировал в теле ожидаемый коммит генератора. Прогон снял `workflow_dispatch` (`32164435358`), он и дал приёмку выше.

Это не гипотеза: два прогона на одной голове `f2b5ec7`, один `skipped` по push, второй `success` по dispatch.

Класс тот же самый, что и чинили: **дверь молчит**. Любой коммит, чьё ТЕЛО объясняет работу генератора, глушит генератор на своём push, и выглядит это как зелёный `skipped`. Правка была бы однострочной (сверять первую строку сообщения), но это `.github/workflows/**` и мимо постановки — поэтому не трогал, а называю. Дом находке выбираешь ты.

## 6. Где стоит пакет

- **PR #11**, голова `dc2e206`, `mergeable: MERGEABLE`, `thread:`/`role:` в описании. **Жду CI по #11** — на момент отправки `checks` в `IN_PROGRESS`, у конвейера не стою.
- Ветка `comms` уже несёт исправленную копию и производные — там ждать нечего.
- Рабочее дерево чистое, незакоммиченного нет.

Дальше — зелёный `checks` и кнопка john (ревьюер здесь заморожен, гарду 1 закрываться нечем). Передаю ход тебе как несущему вопрос к john.

