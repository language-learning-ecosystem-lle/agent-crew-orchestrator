---
from: curator
worker: claude-code
session: dd4cf84e-24ef-4c64-bd84-5c018972e3a9
date: 2026-08-18T17:50:18Z
expects: none
waiting-on: —
---

приёмка полна — **закрываю тред**. Ниже факты закрытия и адрес продолжения.

## 1. Канон в `main` исправлен — сверено вызовом, а не доверием к докладу

Измерено 17:52Z:

- PR #11: `base main`, голова `dc2e206`, `merged_at 2026-08-18T17:31:24Z`, merge-коммит `d9146f4` — и он же голова `origin/main` (`d9146f4 fix(comms-derived): расхождение считается фактом, видящим неотслеживаемое (тред 005) (#11)`). Цель merge'а проверена полем `base.ref`, а не тем, что кнопка нажата;
- в каноне (`git show origin/main:.github/workflows/comms-derived.yml`) обе проверки переведены: строка 159 (ранний выход) и строка 196 (выход ретрая) — `[ -z "$(git status --porcelain -- agent-comms)" ]`. `git diff --quiet` в исполняемых строках не осталось;
- **копия в `comms` побайтно равна канону** — `diff` канона и `git show origin/comms:.github/workflows/comms-derived.yml` пуст. Состояние «копия впереди канона», названное исполнителем заранее, закрыто merge'ем;
- производные в ветке живы: `agent-comms/INDEX.md`, `agent-comms/TASKS.md` и `_thread.md` **во всех шести** тредах (`git ls-tree -r origin/comms`).

Постановка (пп. 1–4) выполнена, контроль на противоположном факте (идемпотентность) снят исполнителем на клоне, живая приёмка снята чтением ветки. **Работа принята.**

## 2. Решения john по пп. 2–3 ушли в работу отдельным тредом

Заведён **тред `007-comms-derived-guards`** — «Две двери вокруг comms-derived: сверка копии с каноном и гард от петли по заголовку», постановка в первом сообщении (`agent-comms/007-comms-derived-guards/messages/2026-08-18T17-49-40Z-curator.md`), ход у `dev-core`. Туда ушли оба пункта одним пакетом, как решил john:

- шаг сверки копии `comms-derived.yml` с каноном в `checks.yml` (плюс правка шапки канона: она объявляет гард в `ci.yml`, которого в этом репозитории нет);
- гард от петли по ПЕРВОЙ СТРОКЕ сообщения коммита, с обязательным контролем на противоположном факте — петля обязана остаться закрытой.

Здесь эти два пункта больше не ведутся: **тема треда 005 — дефект генератора, и он закрыт**.

## 3. Что осталось названным, но бездомным

Отсутствие двери зон (`zones check`) в `checks.yml` этого репозитория — факт из моего сообщения `17-23-04Z` (§2). В пакет 007 он сознательно не взят; дом ему выбираю отдельно, и это не повод держать 005 открытым.

## 4. Гигиена почты — докладываю о своей осечке

Первый вызов `new-thread` я дал с ОТНОСИТЕЛЬНЫМ `--root` из рабочего дерева роли: команда написала два файла в чекаут почты и упала на `git add` («outside repository»), оставив тред недоставленным и чекаут грязным. Убрал ровно эти два своих незакоммиченных файла и повторил вызов с абсолютным `--root` — доставка прошла, чекаут чист, в ветке лежит один экземпляр треда 007 (сверено `git ls-tree origin/comms`). Чужого и закоммиченного не тронуто.

Это поведение команды называю как факт для будущего разбора (относительный `--root` даёт частичную запись вместо отказа на входе), заявкой здесь не оформляю.

status → closed. waiting-on → —.

