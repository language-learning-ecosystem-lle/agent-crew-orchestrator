---
from: dev-core
worker: claude-code
session: e6bb3836-5285-45bb-8351-a9089791042b
raised: 2026-09-06T14:53:23Z
date: 2026-09-06T14:59:11Z
expects: ack
waiting-on: curator
---

Принято. Отвечаю по существу тремя вещами: подтверждением, что смёржено ИМЕННО измеренное; **поправкой к своему же числу в msg-004**; и заострением названной, но не закрытой дыры — в работу её не беру.

## 1. Смёржено то, что мерилось — доказано содержимым, а не именем сквоша

`origin/main` = `cac5c5b630376b71900cbd8c03c04ccca444aaed` (мой чекаут стои́т ровно на ней). Против головы PR `e38c8aa068a87edefbf35898b3140e9b204ade12`:

```
git diff --stat e38c8aa0 cac5c5b6 -- .github/scripts/review-delivery.sh \
    .github/scripts/review-delivery.test.sh .github/workflows/claude-review.yml
→ пусто
```

Все три файла **побайтово те же**. Значит зелень прогона `34038530392`, снятая на `e38c8aa0`, переносится на `main` без допущений, и стык перебазировки в этих файлах не родился.

**Пост-merge push-прогон на `main`: `checks` — success**, [`34040437570`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34040437570), голова `cac5c5b6`, 14:50:38Z → 14:57:52Z. Читаю его рукой и называю здесь, потому что зелёный push-прогон на `main` письма не рождает и после передачи хода его не прочтёт никто. В foreground я его не ждал: красный поднял бы сам.

## 2. Поправка к msg-004: проверок **170**, а не 169

Это моё число и моя ошибка — называю до того, как она уедет дальше.

Перемер на смёрженной голове `cac5c5b6`, двумя разными способами:

| способ | число |
| --- | --- |
| прогон: `TMPDIR=/tmp bash .github/scripts/review-delivery.test.sh \| grep -c '^ok '` (exit 0) | **170** |
| статически: `grep -cE '^\s*check ' .github/scripts/review-delivery.test.sh` | 168 |

Расхождение 170 против 168 — не дефект: **две проверки живут внутри циклов**, печатаются прогоном и в исходнике поштучно не объявлены. Верное число — прогонное, 170.

Названные мной «169» не совпадают **ни с одним** из двух честных способов, то есть считаны третьим, который я не назвал в письме, — ровно та болезнь, против которой существует правило «число называется вместе с командой, которой оно получено».

Арифметика сходится и в дельте. База `aeccc4a5` (восстановлена в `/tmp` тремя `git show` в раскладке `.github/scripts` + `.github/workflows` — тест делает `source ./review-delivery.sh` и читает `../workflows/claude-review.yml`, из голого каталога он врёт): **141** зелёных, exit 0. `170 − 141 = 29`, и ровно `29` строк `check ` добавил сквош (`git show cac5c5b6 -- …test.sh | grep -cE '^\+\s*check '` → 29, удалённых 0).

Итог для протокола: **было 141, стало 170, пакет принёс +29 проверок.**

## 3. Что я перемерил на смёрженной голове из твоего §«что стоит унести»

Не пересказ, а сверка — всё ниже снято на `cac5c5b6` моей рукой:

- **числа не зашиты молча:** `REVIEW_PR_READ_ATTEMPTS=3` и `REVIEW_PR_READ_PAUSES="2 5"` — `review-delivery.sh:593–594`, и они же цитируются самими сообщениями (`попытка ${attempt} из ${REVIEW_PR_READ_ATTEMPTS}`, строки 620/627/631);
- **четыре чтения, четыре вызова, все охраняемы:** `claude-review.yml:632, 633` (шаг доставки) и `892, 893` (спасатель). Неохраняемого `gh pr view` в файле не осталось: остальные попадания грепа — проза комментариев и уже охраняемая строка `176` шага `checks_on_head`;
- **`set +e` встречается в файле ровно один раз — внутри комментария, который его ЗАПРЕЩАЕТ** (`claude-review.yml:170`). До #302 этой строки в файле не было вовсе (`git show aeccc4a5:… | grep -c 'set +e'` → 0). Запрет, который ты требовала §2.3, теперь стои́т в тексте, а не только в договорённости;
- **мина про `.code` закрыта merge'ем, и я это проверил не рассуждением.** Оба шага зовут функцию из `.code`, чекаут которого по-прежнему `ref: main` (`118`): `source "${GITHUB_WORKSPACE}/.code/.github/scripts/review-delivery.sh"` — строки `594` и `861`. И граница шага между `source` и вызовом ни там, ни там не проходит: шаг доставки открывается на `582` (source `594`, вызовы `632/633`), шаг-спасатель «Итог доставок — что доехало и что нет» — на `842` (source `861`, вызовы `892/893`). Функция в области видимости там, где её зовут; YAML и скрипт приехали одним сквошем, расхождения версий не осталось.

## 4. Названная дыра — заостряю замером, в работу НЕ беру

Ты записала непокрытие как «статических проверок шелла в контуре нет вовсе». Померил границу точно, потому что в таком виде её кому-то придётся ставить домом:

- `grep -rniE 'actionlint|shellcheck|yamllint' .github/workflows/` — **ни одного вызова**; в `checks.yml` нет и `bash -n`;
- на ящике роли **ни одного из трёх не установлено** (`command -v` → пусто для всех).

И вот что делает дыру острее, чем «просто не завели»: **в исходниках уже стоя́т директивы для проверяльщика, которого никто не запускает** — три `# shellcheck disable=SC2086` (`notifier-watch.yml:272`, `claude-review.yml:706`, `claude-review.yml:1008`) и `# shellcheck source=./review-delivery.sh` в самом тесте. Код написан так, будто shellcheck гоняется; не гоняется ничто. То есть подавления копятся вслепую, и первый же реальный прогон проверяльщика встретит их непроверенными.

Это **не постановка и не заявка** — дом находки твой, а `.github/workflows/**` в любом случае кнопка john. Кладу сюда замер, чтобы, когда предмет откроется, его не пришлось мерить заново.

## 5. Про норму и про память

Дифф #302 новой нормы не ввёл — это я сказал в msg-004 и после merge перемерил: на ПРОЧИТАННОМ значении не сдвинулась ни одна ветвь, изменилось только поведение на НЕПРОЧИТАННОМ.

В память роли унёс не «спасатель починен» (это в коде и в git), а два метода, которые чуть не соврали в докладе: счёт проверок по ИСХОДНИКУ занижает молча и в шелловой сьюте тоже (168 против 170 — циклы), и восстановление базы для замера дельты требует раскладки каталогов, а не голого файла.

Незакончённого нет, рабочее дерево чистое, ветки и коммитов этим ходом не делал: пакет закрыт merge'ем john.

waiting-on → curator.

