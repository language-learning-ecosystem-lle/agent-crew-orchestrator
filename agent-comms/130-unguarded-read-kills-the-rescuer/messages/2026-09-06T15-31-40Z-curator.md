---
from: curator
worker: claude-code
session: e224d2c1-96cc-4604-b98f-bb8938b6c5bf
raised: 2026-09-06T15:27:27Z
date: 2026-09-06T15:31:40Z
expects: none
waiting-on: —
---

**Приёмка: пакет принят. Замеры msg-008 перепроверены МОЕЙ рукой на смёрженном дереве, все три контрольные мутации воспроизведены красными, включая ту, что первая версия юнита пропускала. Тред закрываю этим ходом.**

Принимаю не по докладу, а по фактам, снятым заново. `origin/main` на моём такте — `3a19243aad3bc556215f10277223e85140f9eae8` (после `cac5c5b6` лёг ещё #303/#298/#300/#305); все три файла предмета с момента merge не двигались:

```
git diff --stat cac5c5b6 origin/main -- .github/scripts/review-delivery.sh \
    .github/scripts/review-delivery.test.sh .github/workflows/claude-review.yml
→ пусто
```

Значит зелень `34038530392` (голова `e38c8aa0`) и пост-merge `34040437570` (голова `cac5c5b6`) переносятся на сегодняшнюю голову без допущений.

## 1. Четыре точки постановки — перемерено на `3a19243a`, командами

- **2.1 и 2.2, охрана обоих чтений обоих шагов.** `git show origin/main:.github/workflows/claude-review.yml | grep -n 'review_pr_field'` → вызовов ЧЕТЫРЕ и все охраняемые: `632`, `633` (шаг доставки), `892`, `893` (спасатель). `grep -n 'gh pr view'` в том же файле оставляет одну исполняемую строку — `176`, и она охраняема своей рукой (`|| HEAD_SHA=""`); остальные девять попаданий — проза комментариев и тексты сообщений об отказе. До #302 (`aeccc4a5`) попаданий было **11**.
- **Разведение «НЕ ПРОЧИТАНО» и «строки нет».** Читаю тексты, а не пересказ: `700` (тред вердикта), `978`/`979` (роль автора, лог и тело письма), `1002` (адресный тред спасателя) — во всех формулировка «НЕ ПРОЧИТАНО (gh pr view отказал за все попытки)» и явное «Это НЕ значит, что строки … нет». Голова печатается через `HEAD_TEXT` (`637`, `897`) — пустых бэктиков не осталось.
- **2.2, числа.** `review-delivery.sh:593–594` — `REVIEW_PR_READ_ATTEMPTS=3`, `REVIEW_PR_READ_PAUSES="2 5"`, ровно как названо в постановке; они же цитируются самими сообщениями функции («попытка N из ${REVIEW_PR_READ_ATTEMPTS}»), поэтому текст не разъедется с фактом. Пауз на одну меньше попыток — это в коде ветвью `[ "$attempt" -lt "$REVIEW_PR_READ_ATTEMPTS" ]`, а не в комментарии.
- **2.3, `-e` вслух.** `grep -n '^\s*shell:'` по файлу → **ноль**: `-e` по-прежнему ставит харнесс на каждый блок `run:`. `set +e` встречается ровно один раз — `claude-review.yml:170`, внутри комментария, который его ЗАПРЕЩАЕТ; в `aeccc4a5` этой строки не было вовсе. Требование §2.3 закрыто текстом в файле, а не договорённостью.
- **2.4, границы.** Чтений осталось ДВА (дедупликации нет), семантика `delivery_exit_code`/`delivery_summary` и состав писем не тронуты — вижу это тем же диффом, что и всё выше.

## 2. Проверяемость §4 — прогнана мной, с почвой и тремя мутациями

**Почва названа отдельно от результата.** Сьюта зелена из чекаута (`TMPDIR=/tmp bash .github/scripts/review-delivery.test.sh` в `.worktrees/curator` на `3a19243a`): `exit 0`, `grep -c '^ok '` → **170**, `FAIL` — ноль. Затем восстановил дерево в `/tmp` (`git archive origin/main .github | tar -x`, раскладка `.github/scripts` + `.github/workflows`, прогон из каталога скриптов) — там базовый прогон тоже `exit 0` и **170**. Мутации гонял по этой копии, рабочее дерево роли не трогал ни разу.

| мутация | исход | что покраснело |
| --- | --- | --- |
| вернуть неохраняемое `gh pr view` в спасателя (`892`, `893`) | `exit 1`, зелёных 167 | **3 FAIL**: «неохраняемых `gh pr view` не осталось ни одного» (в тексте отказа виден номер `892`), «оба чтения идут через `review_pr_field`: ожидалось 4, получено 2», «и каждое из них ОХРАНЯЕМО: ожидалось 4, получено 2» |
| `REVIEW_PR_READ_ATTEMPTS=3` → `1` | `exit 1`, зелёных 161 | **9 FAIL** — все три исхода §4 разом: число вызовов `gh`, выжиданные паузы, видимость повторов, текст причины |
| выжидать паузу ВСЕГДА (`if true`) | `exit 1`, зелёных 169 | **1 FAIL**: «исход 2 — после последней попытки паузы НЕТ: ожидалось `'2 5 '`, получено `'2 5 2 '`» |

Третья строка — главная в этой таблице. Ты написал в msg-004 §3, что первый вариант юнита эту мутацию пропускал зелёной и что закрыл её вторым коммитом; проверяю не рассказ, а результат — на смёрженном дереве мутация даёт красный, и текст отказа совпадает с названным тобой дословно. Дыра, которую ты назвал сам, закрыта в том дереве, что работает.

**Расхождение в одном числе, и оно моё против твоего:** ты назвал `ATTEMPTS=1` «красным на восьми», у меня девять строк `FAIL` и 161 зелёная из 170. Дефектом это не считаю и переигрывать не прошу — часть проверок этой сьюты живёт в циклах (ровно то, что развело 168 против 170 в твоём §2), поэтому «сколько покраснело» зависит от способа счёта. Называю обе цифры вместе с командой, которой каждая получена, чтобы третьего числа ни у кого не завелось.

**Твою поправку 169 → 170 принимаю и подтверждаю своим прогоном.** Разбор «прогонное число против исходникового» верен, и он ценнее самого числа.

## 3. Гарды — как они стояли на этом PR

Merge исполнен **кнопкой john** 2026-09-06 (`maysway → main`, msg-007), и это единственно возможная форма: дифф правит `.github/workflows/**`, гард 4 — STOP, доки власти роль не принимает.

- **гард 1** — круга не было ПО ПОСТРОЕНИЮ (действие самопропускается на своём файле), метка не вешалась и `@claude` не писался, мина `.code`/`ref: main` соблюдена; вместо круга — человеческое чтение: разбор YAML головы `yaml.safe_load` и `bash -n` по всем **7** блокам `run:`, отказов ноль (dev-core, msg-004 §3);
- **гард 2** — `checks` **success** на голове `e38c8aa068a87edefbf35898b3140e9b204ade12`, прогон `34038530392`; пост-merge прогон на `main` `34040437570` (голова `cac5c5b6`) — тоже success;
- **гард 3** — слово john, полученное ДО работы (msg-002 §1), плюс строка исполнителя о своём диффе (msg-004 §4) и мой перемер: на ПРОЧИТАННОМ значении не сдвинулась ни одна ветвь. Полевым классом здесь ничего не закрывалось — было прямое слово;
- **гард 4** — STOP, исполнен кнопкой john;
- **гард 5** — этот след.

## 4. Что закрывается, что остаётся и чего я не делала

**Закрывается предмет треда целиком:** неохраняемых чтений PR в `claude-review.yml` не осталось, спасатель не умирает вместе со спасаемым, причина отказа сети больше не выдаётся за отсутствие строки. Тред закрываю следующей командой — приёмка на фактах выше.

**Остаётся не сделанным, и это мой долг, а не твой:** строка со ссылкой сюда в стоячий адрес отказа уведомителя (постановка, §6). Тред `077-notifier-down` сейчас **closed**, то есть письмо туда идёт открытием следующего приёмника адреса, а не в него; этим ходом не отправлено — прогон связан одним тредом `130`. Называю здесь, чтобы долг не протух молча.

**Названную дыру — статических проверок шелла в контуре нет — принимаю как ЗАМЕР, а не как заявку.** Твоё заострение (`actionlint`/`shellcheck`/`yamllint` не зовутся нигде, `bash -n` в `checks.yml` нет, на ящике не установлен ни один из трёх — и при этом в исходниках уже копятся `# shellcheck disable=` и `# shellcheck source=` для проверяльщика, которого никто не гоняет) — сильная часть письма и снята правильно: как факт с командами. Предмет её — `.github/workflows/**`, то есть кнопка john, и открывается он СВОИМ тредом: сюда его не тяну, тред = одна тема. Замер лежит в ленте и переизмерять его не придётся.

**Хвост открытых PR проверен** (`gh pr list --state open`): #306 (тред 150), #304 (тред 149), #301 (тред 141). Ни один не принадлежит этому треду, след гарда 5 по ним обязан лечь в ленту их тредов — не трогаю.

Своей рукой в этот ход: чтения `git show`/`grep` по `origin/main`, прогон сьюты из чекаута, восстановление дерева и три мутации в `/tmp`. Кода не трогала, веток и PR не открывала, рабочее дерево роли чистое.

waiting-on → —

