---
from: curator
worker: claude-code
session: 79f3c132-3def-4ab5-a4ab-4ce8bf8b7e90
raised: 2026-09-07T11:27:36Z
date: 2026-09-07T11:33:32Z
expects: none
waiting-on: —
---

**ПРИЁМКА СНЯТА, ТРЕД ЗАКРЫВАЮ.** Оба пункта §5 письма `2026-08-29T12-33-…-curator` замерены на живой коробке сегодня, `2026-09-07`, и замер — не пересказ доклада, а строки из `.orchestrator/daemon.log`, `daemon.log.1`, `daemon-code.json` и `daemon-drift.json`. Ниже — чем именно, с честной границей окна и с одной находкой, которая этому треду НЕ принадлежит.

## §1. Критерий 1 — новая строка стои́т в загруженном коде и несёт ВОЗРАСТ, а не аптайм

Загруженный код: `daemon-code.json` → `sha 38c640c640a7a84d567b87d9542db47c094f56f3`, `startedAt 2026-09-07T09:56:57Z`, pid 6603 — то есть на девять суток свежее `c33d7775` (#106). Файл стояния на полу: `daemon-drift.json`, поля `refSha 59e06c30…`, `sha 38c640c6…`, `ref origin/main`, `behind 1`, `since 2026-09-07T13:55:02+03:00`, `why …`, `at 2026-09-07T11:27:36Z` — та самая проводка девятого класса.

Дословно из `daemon.log` (такт 11:2xZ, две соседние строки одного такта):

- `daemon — the LOADED CODE is not the ref: this process runs 38c640c6 …, origin/main on disk is 59e06c30 — 1 commit(s) behind, drifting for 33m (since 2026-09-07T13:55:02+03:00); up since 2026-09-07T09:56:57Z (2h), and node loads modules once`
- `daemon — the code is 1 commit(s) behind, drifting for 33m (since …) — no self-restart with untracked files in '/home/lle/projects/agent-crew-orchestrator' (?? .pr278-body.md) — 'git pull --ff-only' refuses over them; … CLEAN THAT TREE AND STOP THERE …`

Сверьте это со СТАРОЙ формой, которую 29.08 я цитировала в §4.2 как «то, против чего написан #106»: там был только аптайм (`up since … (20m)`) и ни слова о возрасте дрейфа. Теперь в строке стоят ТРИ факта, которых не было: **размер** (`1 commit(s) behind`), **возраст** (`drifting for 33m`) и **датированное начало** (`since …`), а причина названа лечащим порядком. Критерий 1 — **выполнен**.

## §2. Критерий 2 — за сутки ни один дрейф старше двух часов не остался немым

Окно замера — ретенция журнала: `daemon.log.1` открывается эпохой `2026-09-06T10:38:43.515Z`, `daemon.log` доходит до сейчас (`2026-09-07T11:3xZ`). Это ~25 часов, то есть больше суток, и внутри него дрейф перечислен ЦЕЛИКОМ, а не выборкой: `grep -o "drifting for [0-9]*[hm] (since [0-9T:+-]*)"` по обоим файлам даёт ровно четыре эпизода, различаемые датой начала.

| эпизод (`since`, UTC) | докуда дорос | звонок |
|---|---|---|
| `2026-09-05T12:05:47Z` | в окне виден уже как `23h` | **не измерим**: эпизод начался ДО окна, его порог (2 ч) пришёлся на 09-05, а тот кусок журнала ротирован |
| `2026-09-06T11:02:02Z` | `4m` — максимум | молчание ПРАВИЛЬНОЕ: полоса 120 мин не пройдена (`CODE_DRIFT_OVERDUE_MINUTES = 120`) |
| `2026-09-06T11:37:22Z` | `22h` | **ПОЗВОНИЛ** — см. ниже |
| `2026-09-07T10:55:02Z` (живой сейчас) | `33m` на такте замера | молчание правильное; полосу пройдёт ~`12:55Z` |

Звонок третьего эпизода — не вывод, а строка операторского журнала (`daemon.log.1`, строка 18677), хвост курьерской сводки того такта:

`daemon — courier: 3 parked, 3 of them asking, … — the box is behind its own ref (6 commit(s) behind, drifting for 2h (since 2026-09-06T14:37:22+03:00))`

Это `announcedOf` — список того, что УВЕЗЛО письмо, а не то, что напечатал экран: он живёт рядом с планировщиком именно затем, чтобы «класс в письме, но не в логе» ловился тестом, а не полевым отчётом (`notify/notify.ts`, комментарий над `announcedOf`). Звонок ровно ОДИН на 22-часовой эпизод — второе, что здесь замерено: `grep -c "behind its own ref"` по всему `daemon.log.1` = **1**. То есть дедупликация по `since` (`freshDrift = drift.since !== seen.drift`, `notify.ts:1886`) в поле работает: класс не превращается в строку каждые несколько минут, ради чего и написан.

Итог критерия 2: в измеримом окне (>24 ч) дрейфов старше двух часов было **один**, и он **позвонил**; два эпизода молчали внутри полосы — по построению; немых дрейфов старше порога — **ноль**. Граница честная: про эпизод 09-05 я не говорю ни «позвонил», ни «промолчал» — журнал за тот такт ротирован, и восстановить его нечем.

## §3. Живой контроль в полёте, и он сильнее исторического

Дрейф `since 2026-09-07T10:55:02Z` стои́т ПРЯМО СЕЙЧАС и полосу ещё не прошёл. Если класс работает — john получит про него строку около `12:55Z`, с размером, возрастом и причиной дословно, без чьей-либо руки. Это положительный контроль, который никто не подкладывал: он не нужен для приёмки (она снята §1–§2), но он проверяет её же в третий раз и приезжает сам.

## §4. Находка, которая ЭТОМУ треду не принадлежит

Причина сегодняшнего дрейфа названа демоном дословно: `no self-restart with untracked files … (?? .pr278-body.md) — 'git pull --ff-only' refuses over them`. **Та же самая причина, слово в слово, стои́т у эпизода 09-05** (`daemon.log.1`, строки 70–71: 13 коммитов отставания, 23 часа, тот же `.pr278-body.md`). То есть один забытый в обслуживаемом чекауте файл тела PR замораживает самопочинку ВСЕЙ коробки — дважды за трое суток, и оба раза надолго.

Чинить по дороге не стал (граница постановки), предметом 044 это не является и в закрываемый тред я его не прячу: **это отдельный предмет**, и он про руку, которая пишет тело PR внутрь обслуживаемого дерева, а не про дайджест. Ближайший тред-сосед — `141-daemon-drift-needs-a-human` — его не унесёт: он запаркован на john и вычерпал попытки (`⛔ OUT OF ATTEMPTS — 6 of 3`), то есть контур его не поднимает вовсе. Предмету нужен свой тред и свой исполнитель; сессия, которой хватит окна, поднимает его первой командой — факты для постановки собраны выше и повторного замера не требуют.

## §5. Что этот ход не делал и чем кончается

Кода своей рукой не написано, PR не открывалось, ничего не чинилось, рабочее дерево чистое, незакоммиченного нет. Хвост чужих открытых PR не трогал — прогон связан одним тредом.

Приёмка постановки #106 снята по §1–§2 — фактами, а не по косвенному признаку. Закрываю тред `cli thread status --status closed --write` сразу за этим письмом; ход не передаётся никому: работы в предмете не осталось.

waiting-on → —

