---
from: curator
worker: claude-code
session: 5aeae503-97a6-4c78-8be2-752b8dc31388
raised: 2026-09-06T15:33:17Z
date: 2026-09-06T15:37:27Z
expects: ack
waiting-on: dev-core
---

**Постановка dev-core: ветвь `stash` конвертируется в коммит.** Решение john — в этом треде, письмо curator от 2026-09-06T12:24:06Z (поле `delivers`, чат ~12:23Z, слово «ДА», вариант (a)); отдельного слова здесь больше не спрашивается.

Замеры ниже сняты с `origin/main` = `3a19243a` (`git show origin/main:<путь>`); номера строк — оттуда же, они ориентир, а не адрес.

## 0. Восхождение и гарды

PR несёт `thread: 132-stash-hides-the-aborted-run` и `role: dev-core`. Гард 3 закрывается решением john выше — **класс «полевой измеренный дефект» здесь НЕ заявляется и не нужен**: это смена объявленного поведения, и она восходит к слову john прямо. Форма обычная: ветка → PR командой `pnpm protocol pr open` → зелёный `checks` на голове → метка `review` **после** зелёного → вердикт → merge моей рукой с поимённым следом.

## 1. Предмет — что меняется в коде

`packages/agent-protocol/src/orchestrator/workspace.ts`, если не сказано иное.

**1.1. Ранний возврат на обрыве уезжает** (строки 820–829: `if (previousReason !== undefined && ABORTED_RUN_REASONS.includes(previousReason)) return { action: "stash", … }`). Грязное дерево с ЛЮБОЙ известной причиной прошлого прогона идёт дальше по той же дороге, что сегодня идёт причина «завершил ход сам»: `classifyWorkspaceHead` → своя ветка роли → `commit` туда же (`create: false`); отделённая голова при известном `at` → `commit` на служебную ветку `serviceBranchName`; отделённая без `at` → отказ; база или чужая голова → отказ. **Новой развилки не заводится** — предмет в том, чтобы развилка была одна.

**1.2. Тексты, которые от этого станут ложными, правятся в том же диффе — это часть предмета, а не косметика.** Отказы на строках 878 и 890 говорят `left by a run that ENDED ITS OWN TURN ('<reason>')`; после конверсии в них приезжает и оборванный прогон, о котором это неправда. То же в комментариях: шапка модуля (строки ~40–55, где две судьбы перечислены как две), док-комментарий `WorkspacePlan` (~168–181) и комментарий права (~838). **Требование одно и оно проверяемо чтением: ни один текст отказа, комментария или сообщения коммита не утверждает про причину того, чего эта причина не значит.** `dirtCommitMessage` (606) уже нейтрален — он называет причину строкой, и трогать его незачем.

**1.3. Мёртвой ветви после этого не остаётся.** Вместе с развилкой уезжают: вариант `action: "stash"` в типе `WorkspacePlan`, исполнитель `case "stash"` в `packages/agent-protocol/src/cli.ts` (~6713–6726) и `stashLabel` (228). Если у любого из трёх найдётся читатель вне этих мест — **назови его в треде, а не оставляй недостижимую ветвь молча**: недостижимый код читается следующим как работающий.

**1.4. Что НЕ трогается — и это сверяется диффом:**

- **`ABORTED_RUN_REASONS` (213) остаётся.** У списка второй читатель — `dirtLeftByFinish` (258), и он отвечает на ДРУГОЙ вопрос: оставил ли грязь прогон, завершивший ход сам (это ошибка завершения, и она называется в событии релиза). Тот вопрос никто не отменял, и удаление списка «за компанию» будет расширением предмета;
- отказ на неатрибутированной грязи (`previousReason === undefined`) — грязь может быть человеческой;
- отказы на базовой и на чужой голове — коммит в общую ветку john запретил дословно;
- `keep` на resume (R18);
- **главный чекаут** — отказ john 04.09, тред `096`, решение в силе: право по-прежнему только о рабочем месте роли;
- требование `099` §3: у служебной ветки есть конец, и он называется при заведении.

**1.5. Письмо об уборке начинает уезжать и на эту грязь — само, гейт не меняется.** `postTidyUpLetter` в `cli.ts` (~11206) стоит на `plan.action === "commit"` (предмет C треда `099`, `01a082d9`/#282 в `main`). После 1.1 оборванный прогон приходит в тот же гейт, и работа получает адрес в общем месте вместо `git stash list` на одной машине. **Это и есть исполнение довода john «спрятанное не видно никому» — не побочный эффект, а проверяемый исход (см. §3).**

## 2. Доки — тем же PR

Обе зоны тебе открыты (`forbidden` у dev-core — только `docs/roles`), и расходиться доку с кодом нельзя ни одного такта:

- `packages/agent-protocol/README.md` — ~2507, 4188–4196, 4224, 4247: парковка в stash описана как текущее поведение;
- `docs/protocol-reference.md:634` — тот же абзац, где уже описан коммитный исход #279, а ветвь обрыва в нём всё ещё «паркуется в `git stash push -u -m 'wip …'`».

После правки в обоих доках у грязи **одна судьба**, а различие причин остаётся ровно там, где оно остаётся в коде: ошибка завершения, называемая в релизе (§1.4).

## 3. Проверяемость

Классы и то, что сознательно не покрывается.

**Юнит (`workspace.test.ts`).** Переписывается сторож `1102` («dirt of a CUT-OFF run still stashes») — тот же вход теперь даёт `commit`; `312` («прогон без объявленной сессии») и `362` приводятся к новому поведению. **`328` (resume → `keep`) обязан остаться зелёным без единой правки** — покраснел, значит изменение шире предмета. И на оборванную причину ставится тот же перечень состояний головы, что уже есть у завершившей: своя ветка → `commit` туда же; отделённая с `at` → служебная ветка; отделённая без `at` → отказ; база → отказ; чужая → отказ. Не «один тест на конверсию», а перечень: одна судьба доказывается совпадением перечней, а не одним случаем.

**Сторожи ДРУГОГО вопроса остаются зелёными и по букве, и по смыслу:** `workspace.test.ts:846` и `run.process.test.ts:2310` («грязь после обрыва — не ошибка завершения»). Их текст правится, только если называет stash исходом; сам ассерт — нет.

**Процессный (`workspace.process.test.ts:248`, сегодня «прогон вышел по таймауту → дерево паркуется в stash»).** Переписывается на настоящем git-дереве: после подъёма грязь лежит **коммитом** на названной ветке, дерево чисто и стоит на базе, `git stash list` пуст. Ассерты `316`/`363`/`397` («stash пуст») остаются как есть — им и положено быть пустыми.

**Письмо (`tidy-letter.process.test.ts` / `workspace.commit.process.test.ts`).** Один случай с ОБОРВАННОЙ причиной, доказывающий, что письмо об уборке уезжает и на неё — тем же гейтом и без правки гейта (§1.5). Без него §1.5 остаётся рассуждением: гейт по `action` — не то же самое, что письмо, которое кто-то прочёл.

**Живой приёмки человека не требуется, и это сознательно:** исход целиком читается процессным тестом на настоящем дереве (ветка, коммит, чистота, пустой stash), а обрыв контура в этих тестах и так подделывается причиной релиза, а не настоящим обрывом — живой прогон не добавил бы факта.

**Что НЕ покрывается и почему:** настоящий `quota-exhausted` на живом ящике — цена такой проверки есть выжигание квоты, а первый же полевой случай отчитается письмом уборки сам, и его адрес будет читаться из ленты.

## 4. Приёмка

Принимаю по фактам, не по докладу: (1) в `planWorkspace` не осталось развилки по `ABORTED_RUN_REASONS`; (2) `stash` не встречается ни в типе плана, ни в исполнителе, ни как исход в доках — либо встречается с названной в треде причиной; (3) перечни состояний головы для обеих причин совпадают тестами; (4) процессный тест показывает коммит и пустой stash; (5) есть тест письма на оборванной причине; (6) `dirtLeftByFinish` и его сторожи не тронуты.

Вопрос по любому пункту — письмом в этот тред до работы, а не диффом.

waiting-on → dev-core.

