---
from: dev-core
worker: claude-code
session: 7193590b-f24d-46e1-b711-2f73875863ed
raised: 2026-09-06T15:37:44Z
date: 2026-09-06T15:51:39Z
expects: none
waiting-on: dev-core
---

**Постановка выполнена: ветвь `stash` конвертирована. PR #307, голова `890d9a68`, жду `checks`.**

## 1. Сделано, по пунктам постановки

- **§1.1** — ранний возврат по `ABORTED_RUN_REASONS` в `planWorkspace` снят. Новой развилки не заведено: грязь с любой известной причиной идёт по существующей дороге (`classifyWorkspaceHead` → своя ветка → `commit` туда же с `create: false`; отделённая с `at` → служебная ветка; отделённая без `at` → отказ; база или чужая голова → отказ).
- **§1.2** — оба отказа (`workspace.ts`, бывшие 878 и 890) больше не говорят `ENDED ITS OWN TURN`: теперь `left by the '<reason>' run of this pair`. Переписаны шапка модуля (две судьбы → одна, с доводом и с принятым риском), док-комментарий `WorkspacePlan` («Seven outcomes» → «Six»), комментарий права и заголовок `ABORTED_RUN_REASONS` (у списка назван его единственный оставшийся читатель). `dirtCommitMessage` не тронут.
- **§1.3** — уехали все три названных: вариант `stash` типа `WorkspacePlan`, `case "stash"` исполнителя в `cli.ts`, `stashLabel`. Читателей вне этих мест нет (грепом по `--include=*.ts`).
- **§1.4** — `ABORTED_RUN_REASONS` и `dirtLeftByFinish` на месте и не тронуты; отказ на неатрибутированной грязи, отказы на базовой и чужой голове, `keep` на resume — тоже. Главный чекаут не затрагивается.
- **§1.5** — гейт письма (`plan.action === "commit"`) не менялся ни строкой; на исход поставлен процессный тест (ниже).
- **§2** — доки тем же коммитом: `packages/agent-protocol/README.md` (развилка R17 переписана на одну судьбу с абзацем «ONE DIRT, ONE FATE»; плюс абзац о почте, где `stash` назывался исходом R17, абзац «два репаира» и абзац `dirtLeftByFinish`) и `docs/protocol-reference.md:634`.

## 2. Отступление, которое я обязан назвать: **четвёртая мёртвая вещь и правка теста `328`**

§1.3 назвала три предмета. Их оказалось **четыре**: вход `previousSession` у `planWorkspace`. Его единственным читателем была метка stash — после конверсии это поле, которое никто не читает, то есть ровно тот случай, о котором §1.3 говорит «недостижимое читается следующим как работающее». Я его удалил вместе с передачей на вызывающей стороне (`cli.ts`).

**Цена:** тест `328` (resume → `keep`), которому §3 предписывает остаться зелёным **без единой правки**, передавал `previousSession` аргументом — и без правки перестал бы компилироваться. Правка в нём ровно одна: удалена строка `previousSession: "8f3a2b1c-0d4e",`. **Ассерт не тронут** — `.toEqual({ action: "keep" })` стоит дословно как стоял. Если это чтение §3 неверно и поле следовало оставить живым, скажи — верну его вместе со строкой теста.

## 3. Проверяемость — что именно поставлено

- **Юнит `workspace.test.ts`.** Перечень состояний головы гоняется **дважды** — под `completed` и под `quota-exhausted` — и два плана обязаны совпасть после подстановки слова причины (сравниваются целиком: ветка, `create`, текст отказа). Пять состояний: своя ветка, отделённая с `at`, отделённая без `at`, база, чужая ветка. Рядом отдельный ассерт, что `from` и сообщение несут **свою** причину (иначе подстановка прошла бы и на плане, из которого причину вымыли). Плюс перечень всех десяти причин `RELEASE_REASONS` → `commit`, и неизвестная причина → `commit` с её именем в сообщении.
- **Процессный `workspace.process.test.ts`.** Случай `timeout` переписан на живом git: коммит на `wip/dev-core/012-x-…`, оба файла (изменённый отслеживаемый и **неотслеживаемый**) внутри, сообщение коммита называет прогон, дерево чисто и стоит на базе, **`git stash list` пуст**. Ассерты «stash пуст» в соседних случаях оставлены как были.
- **Письмо `tidy-letter.process.test.ts`.** Новый случай: та же настоящая грязь, причина релиза в журнале переписана на `timeout` (подделка — сознательная и названная в комментарии: настоящий обрыв стоил бы квоты или стенных часов сюиты, а читается ровно это одно слово). Письмо уезжает в стоячий адрес, несёт ветку, поднимает `dev-core`. Адрес работы — в письме, причина — на коммите ветки: тело письма причину не печатает, и я не стал её туда добавлять (это была бы правка гейта/шаблона сверх предмета).
- **Сторожи другого вопроса** (`workspace.test.ts` про `dirtLeftByFinish`, `run.process.test.ts:2310`) — **ассерты не тронуты**, поправлены только формулировки в названии и комментарии, называвшие stash исходом.

**Замерено локально (`TMPDIR=/tmp`, `--root packages/agent-protocol`):** `workspace.test.ts`, `workspace.process.test.ts`, `workspace.commit.process.test.ts`, `tidy-letter.process.test.ts`, `status.service-branches.process.test.ts` — **164/164 зелено**. `tsc --noEmit` и `biome check` чисты. Полную сюиту локально не гонял — её меряет `checks` PR.

## 4. Где стою

PR #307 открыт `pnpm protocol pr open`, тело несёт `thread:`/`role:`. Ход по своему PR кончается здесь: **жду `checks` по #307, голова `890d9a68`**. Зелёный прогон → метка `review` на той же голове → вердикт → кнопка твоя. Паркуюсь на прогон, чтобы следующий такт читал уже готовый цвет.

## 5. ПОПРАВКА к §4, снятая за минуту до срока: **`checks` по #307 КРАСЕН**

Прогон `34043560421` на голове `890d9a68` — `conclusion: failure`. Причину прочитать я уже не успел: срок аренды 16:37:44Z. **Метку `review` не вешал** — на красной голове её вешать нельзя.

**Следующей сессии `dev-core` (это я, следующим тактом):** взять `gh run view 34043560421 -R … --log-failed`, назвать ПЕРВЫЙ упавший шаг (typecheck роняет тесты и они не гоняются вовсе), починить, запушить в ту же ветку. Вероятные кандидаты, которые я локально не мерил: полная сюита — правка нормы `planWorkspace` могла уронить чужие тесты вне пяти файлов, что я прогнал (например ассерты в `daemon.*.process.test.ts` или в тестах статуса, ожидающие слово «parking»); греп по `parking what the` и по `ENDED ITS OWN TURN` по всему дереву — первое, что стоит сделать.

Рабочее дерево чистое, всё закоммичено и запушено: `890d9a68` на `origin/dev-core/132-stash-hides-the-aborted-run`. Незакоммиченного нет.

