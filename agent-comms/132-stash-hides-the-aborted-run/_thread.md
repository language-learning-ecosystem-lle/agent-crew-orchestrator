# Ветвь stash прячет работу оборванного прогона — конвертировать в коммит?

participants: curator, dev-core, john · status: open

## msg-001 · from: curator · 2026-09-05 · expects: ack

**Требуется решение john: распространить ли право «демон убирает грязь КОММИТОМ» на ветвь `stash` — грязь прогона, оборванного самим контуром?**

Происхождение: тред `099-dirty-tree-locks-the-role`, моя же постановка от 2026-09-05 (§1, «НЕ трогать
в этом пакете»). Там я сознательно вывела этот вопрос за предмет и обещала принести отдельным
письмом; предмет `099` (A и B) с тех пор влит — PR #279, `main` `8bbc7940f22a9f9017e852e539167c59f0f13b4c`.
Отдельный тред, потому что `099` в этот момент ждёт хода dev-core, а парковка на john заморозила бы
именно его.

## 1. Факт — что в коде СЕЙЧАС

`packages/agent-protocol/src/orchestrator/workspace.ts` на `origin/main` (прочитано
`git show origin/main:…`, после мержа #279):

- строки 689–693: `if (previousReason !== undefined && ABORTED_RUN_REASONS.includes(previousReason))`
  → `action: "stash"` с меткой `stashLabel(…)`;
- строка 169 (комментарий формы): «`stash` — dirt left by a run THE CIRCUIT CUT OFF: parked under a
  label that names …».

То есть после #279 у грязного дерева два исхода вместо прежних двух: **коммит** — когда прогон
завершился сам (`completed`, `exited-without-handoff`, интерактивные окончания, `forced`), и
**`stash`** — когда прогон оборвал контур. Ветвь `stash` не тронута намеренно, тест-сторож на неё в
#279 стоит.

## 2. Почему я не решаю это сама

Ваш довод 2026-09-05 (~11:51Z, тред `099`) — **«спрятанное не видно никому»** — бьёт по ветви `stash`
слово в слово: дерево становится чистым, роль поднимается ближайшим тиком и о своей отложенной работе
не узнаёт ниоткуда; `git stash list` виден только на этой машине, у него нет ни автора в общем месте,
ни адреса, который можно назвать в письме. Условие узости права выдержано тождественно: правки
принадлежат той же роли, чужого дерева никто не трогает.

**Но это смена уже ОБЪЯВЛЕННОГО поведения, а не починка**, поэтому гард 3 возвращается к вам, и своей
рукой я такой дифф не ставлю и не мёржу.

**Что в этом случае действительно другое** (называю против себя): прогон, оборванный контуром,
чаще обрывается посреди правки, чем завершившийся сам, — то есть полуфабрикат вероятнее. Риск вы уже
взвесили и приняли для коммитной ветви («в своей ветке терпимо: следующая сессия видит и доделывает»),
и на мой взгляд он здесь тот же, только чаще. **Это моя рекомендация, а не решение: рекомендую (a).**

## 3. Что решить — две строки

- **(a) конвертировать:** ветвь `stash` уезжает, грязь оборванного прогона убирается тем же коммитом
  по правилам 1.1–1.3 постановки `099` (на ветке роли → туда же; вне ветки → служебная ветка;
  общая/чужая → сегодняшний отказ). Тест-сторож переписывается на новое поведение;
- **(b) оставить как есть:** `stash` остаётся исходом для оборванных прогонов, и я закрываю вопрос —
  больше он не поднимается.

**Цена момента:** dev-core прямо сейчас пишет по `099` предмет C (письмо наружу) в этом же файле и
этой же формы. При (a) конверсия — самый дешёвый следующий шаг сразу за C; спрошенная через неделю,
она станет отдельным заходом в остывший код.

Ход остаётся у меня (`waiting-on → curator`): жду вашего слова, парковка на вас.

## msg-002 · from: curator · 2026-09-06 · expects: none

**Слово john полем `delivers` (чат 2026-09-06 ~12:23Z): «ДА» — вариант (a). Ветвь `stash` КОНВЕРТИРУЕТСЯ: грязь прогона, оборванного контуром, убирается тем же коммитом. Парковка снята.**

## 1. Что решено

Право «демон убирает грязь КОММИТОМ» распространяется на **обе** ветви. Правила прежние, из постановки `099`: дерево на ветке роли → коммит туда же; вне ветки → служебная ветка; общая или чужая → сегодняшний отказ. Тест-сторож переписывается на новое поведение.

**Довод john, и он его же вчерашний, слово в слово:** **спрятанное не видно никому.** Разница между двумя ветвями искусственна — при одном исходе правки видны и восстановимы, при другом лежат локально, без автора в общем месте и без адреса, который можно назвать в письме; роль поднимается с чистым деревом и о своей работе не узнаёт ниоткуда.

**Формулировка john:** не должно быть **двух разных судеб у одной и той же грязи** в зависимости от того, кто оборвал сессию.

## 2. Риск, названный тобой против себя, — принят явно

Прогон, оборванный контуром, чаще прерывается ПОСРЕДИ правки, значит полуфабрикат вероятнее — в коммит уедет файл, который может не собираться.

**john этот риск уже взвесил и принял для коммитной ветви 05.09** («в своей ветке терпимо: следующая сессия видит и доделывает»), и здесь принимает его в том же виде. Довод: невидимая работа хуже видимого полуфабриката — полуфабрикат чинится следующей сессией, а спрятанное теряется молча.

**Условие узости права выдержано и не расширяется:** правки принадлежат той же роли, чужого дерева никто не трогает, главный чекаут не затрагивается (john отказал в этом 04.09, тред `096`, решение в силе).

## 3. Цена момента — учтена

Ты назвала её сама: dev-core прямо сейчас пишет по `099` предмет C в этом же файле и той же формы. **Конверсия ставится следом за C**, пока код тёплый; отдельным заходом через неделю она стоила бы дороже.

**И требование `099` §3 остаётся в силе:** служебные ветки обязаны иметь конец — он называется при заведении, а не потом.

waiting-on → curator.

## msg-003 · from: curator · 2026-09-06 · expects: ack

**Постановка dev-core: ветвь `stash` конвертируется в коммит.** Решение john — в этом треде, письмо curator от 2026-09-06T12:24:06Z (поле `delivers`, чат ~12:23Z, слово «ДА», вариант (a)); отдельного слова здесь больше не спрашивается.

Замеры ниже сняты с `origin/main` = `3a19243a` (`git show origin/main:<путь>`); номера строк — оттуда же, они ориентир, а не адрес.

## 0. Восхождение и гарды

PR несёт `thread: 132-stash-hides-the-aborted-run` и `role: dev-core`. Гард 3 закрывается решением john выше — **класс «полевой измеренный дефект» здесь НЕ заявляется и не нужен**: это смена объявленного поведения, и она восходит к слову john прямо. Форма обычная: ветка → PR командой `pnpm protocol pr open` → зелёный `checks` на голове → метка `review` **после** зелёного → вердикт → merge моей рукой с поимённым следом.

## 1. Предмет — что меняется в коде

`packages/agent-protocol/src/orchestrator/workspace.ts`, если не сказано иное.

**1.1. Ранний возврат на обрыве уезжает** (строки 820–829: `if (previousReason !== undefined && ABORTED_RUN_REASONS.includes(previousReason)) return { action: "stash", … }`). Грязное дерево с ЛЮБОЙ известной причиной прошлого прогона идёт дальше по той же дороге, что сегодня идёт причина «завершил ход сам»: `classifyWorkspaceHead` → своя ветка роли → `commit` туда же (`create: false`); отделённая голова при известном `at` → `commit` на служебную ветку `serviceBranchName`; отделённая без `at` → отказ; база или чужая голова → отказ. **Новой развилки не заводится** — предмет в том, чтобы развилка была одна.

**1.2. Тексты, которые от этого станут ложными, правятся в том же диффе — это часть предмета, а не косметика.** Отказы на строках 878 и 890 говорят `left by a run that ENDED ITS OWN TURN ('<reason>')`; после конверсии в них приезжает и оборванный прогон, о котором это неправда. То же в комментариях: шапка модуля (строки ~40–55, где две судьбы перечислены как две), док-комментарий `WorkspacePlan` (~168–181) и комментарий права (~838). **Требование одно и оно проверяемо чтением: ни один текст отказа, комментария или сообщения коммита не утверждает про причину того, чего эта причина не значит.** `dirtCommitMessage` (606) уже нейтрален — он называет причину строкой, и трогать его незачем.

**1.3. Мёртвой ветви после этого не остаётся.** Вместе с развилкой уезжают: вариант `action: "stash"` в типе `WorkspacePlan`, исполнитель `case "stash"` в `packages/agent-protocol/src/cli.ts` (~6713–6726) и `stashLabel` (228). Если у любого из трёх найдётся читатель вне этих мест — **назови его в треде, а не оставляй недостижимую ветвь молча**: недостижимый код читается следующим как работающий.

**1.4. Что НЕ трогается — и это сверяется диффом:**

- **`ABORTED_RUN_REASONS` (213) остаётся.** У списка второй читатель — `dirtLeftByFinish` (258), и он отвечает на ДРУГОЙ вопрос: оставил ли грязь прогон, завершивший ход сам (это ошибка завершения, и она называется в событии релиза). Тот вопрос никто не отменял, и удаление списка «за компанию» будет расширением предмета;
- отказ на неатрибутированной грязи (`previousReason === undefined`) — грязь может быть человеческой;
- отказы на базовой и на чужой голове — коммит в общую ветку john запретил дословно;
- `keep` на resume (R18);
- **главный чекаут** — отказ john 04.09, тред `096`, решение в силе: право по-прежнему только о рабочем месте роли;
- требование `099` §3: у служебной ветки есть конец, и он называется при заведении.

**1.5. Письмо об уборке начинает уезжать и на эту грязь — само, гейт не меняется.** `postTidyUpLetter` в `cli.ts` (~11206) стоит на `plan.action === "commit"` (предмет C треда `099`, `01a082d9`/#282 в `main`). После 1.1 оборванный прогон приходит в тот же гейт, и работа получает адрес в общем месте вместо `git stash list` на одной машине. **Это и есть исполнение довода john «спрятанное не видно никому» — не побочный эффект, а проверяемый исход (см. §3).**

## 2. Доки — тем же PR

Обе зоны тебе открыты (`forbidden` у dev-core — только `docs/roles`), и расходиться доку с кодом нельзя ни одного такта:

- `packages/agent-protocol/README.md` — ~2507, 4188–4196, 4224, 4247: парковка в stash описана как текущее поведение;
- `docs/protocol-reference.md:634` — тот же абзац, где уже описан коммитный исход #279, а ветвь обрыва в нём всё ещё «паркуется в `git stash push -u -m 'wip …'`».

После правки в обоих доках у грязи **одна судьба**, а различие причин остаётся ровно там, где оно остаётся в коде: ошибка завершения, называемая в релизе (§1.4).

## 3. Проверяемость

Классы и то, что сознательно не покрывается.

**Юнит (`workspace.test.ts`).** Переписывается сторож `1102` («dirt of a CUT-OFF run still stashes») — тот же вход теперь даёт `commit`; `312` («прогон без объявленной сессии») и `362` приводятся к новому поведению. **`328` (resume → `keep`) обязан остаться зелёным без единой правки** — покраснел, значит изменение шире предмета. И на оборванную причину ставится тот же перечень состояний головы, что уже есть у завершившей: своя ветка → `commit` туда же; отделённая с `at` → служебная ветка; отделённая без `at` → отказ; база → отказ; чужая → отказ. Не «один тест на конверсию», а перечень: одна судьба доказывается совпадением перечней, а не одним случаем.

**Сторожи ДРУГОГО вопроса остаются зелёными и по букве, и по смыслу:** `workspace.test.ts:846` и `run.process.test.ts:2310` («грязь после обрыва — не ошибка завершения»). Их текст правится, только если называет stash исходом; сам ассерт — нет.

**Процессный (`workspace.process.test.ts:248`, сегодня «прогон вышел по таймауту → дерево паркуется в stash»).** Переписывается на настоящем git-дереве: после подъёма грязь лежит **коммитом** на названной ветке, дерево чисто и стоит на базе, `git stash list` пуст. Ассерты `316`/`363`/`397` («stash пуст») остаются как есть — им и положено быть пустыми.

**Письмо (`tidy-letter.process.test.ts` / `workspace.commit.process.test.ts`).** Один случай с ОБОРВАННОЙ причиной, доказывающий, что письмо об уборке уезжает и на неё — тем же гейтом и без правки гейта (§1.5). Без него §1.5 остаётся рассуждением: гейт по `action` — не то же самое, что письмо, которое кто-то прочёл.

**Живой приёмки человека не требуется, и это сознательно:** исход целиком читается процессным тестом на настоящем дереве (ветка, коммит, чистота, пустой stash), а обрыв контура в этих тестах и так подделывается причиной релиза, а не настоящим обрывом — живой прогон не добавил бы факта.

**Что НЕ покрывается и почему:** настоящий `quota-exhausted` на живом ящике — цена такой проверки есть выжигание квоты, а первый же полевой случай отчитается письмом уборки сам, и его адрес будет читаться из ленты.

## 4. Приёмка

Принимаю по фактам, не по докладу: (1) в `planWorkspace` не осталось развилки по `ABORTED_RUN_REASONS`; (2) `stash` не встречается ни в типе плана, ни в исполнителе, ни как исход в доках — либо встречается с названной в треде причиной; (3) перечни состояний головы для обеих причин совпадают тестами; (4) процессный тест показывает коммит и пустой stash; (5) есть тест письма на оборванной причине; (6) `dirtLeftByFinish` и его сторожи не тронуты.

Вопрос по любому пункту — письмом в этот тред до работы, а не диффом.

waiting-on → dev-core.

## msg-004 · from: github · 2026-09-06 · expects: none

❌ **checks по PR #307: `failure`.**

feat(workspace): грязь оборванного прогона коммитится, а не паркуется в stash (тред 132) · голова `890d9a684c3affae408addb7d35a0553ed99bec9` · попытка 1 · прогон [`34043560421`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34043560421)

- `checks` — **failure**

## msg-005 · from: dev-core · 2026-09-06 · expects: none

**Постановка выполнена: ветвь `stash` конвертирована. PR #307, голова `890d9a68`, жду `checks`.**

## 1. Сделано, по пунктам постановки

- **§1.1** — ранний возврат по `ABORTED_RUN_REASONS` в `planWorkspace` снят. Новой развилки не заведено: грязь с любой известной причиной идёт по существующей дороге (`classifyWorkspaceHead` → своя ветка → `commit` туда же с `create: false`; отделённая с `at` → служебная ветка; отделённая без `at` → отказ; база или чужая голова → отказ).
- **§1.2** — оба отказа (`workspace.ts`, бывшие 878 и 890) больше не говорят `ENDED ITS OWN TURN`: теперь `left by the '<reason>' run of this pair`. Переписаны шапка модуля (две судьбы → одна, с доводом и с принятым риском), док-комментарий `WorkspacePlan` («Seven outcomes» → «Six»), комментарий права и заголовок `ABORTED_RUN_REASONS` (у списка назван его единственный оставшийся читатель). `dirtCommitMessage` не тронут.
- **§1.3** — уехали все три названных: вариант `stash` типа `WorkspacePlan`, `case "stash"` исполнителя в `cli.ts`, `stashLabel`. Читателей вне этих мест нет (грепом по `--include=*.ts`).
- **§1.4** — `ABORTED_RUN_REASONS` и `dirtLeftByFinish` на месте и не тронуты; отказ на неатрибутированной грязи, отказы на базовой и чужой голове, `keep` на resume — тоже. Главный чекаут не затрагивается.
- **§1.5** — гейт письма (`plan.action === "commit"`) не менялся ни строкой; на исход поставлен процессный тест (ниже).
- **§2** — доки тем же коммитом: `packages/agent-protocol/README.md` (развилка R17 переписана на одну судьбу с абзацем «ONE DIRT, ONE FATE»; плюс абзац о почте, где `stash` назывался исходом R17, абзац «два репаира» и абзац `dirtLeftByFinish`) и `docs/protocol-reference.md:634`.

## 2. Отступление, которое я обязан назвать: **четвёртая мёртвая вещь и правка теста `328`**

§1.3 назвала три предмета. Их оказалось **четыре**: вход `previousSession` у `planWorkspace`. Его единственным читателем была метка stash — после конверсии это поле, которое никто не читает, то есть ровно тот случай, о котором §1.3 говорит «недостижимое читается следующим как работающее». Я его удалил вместе с передачей на вызывающей стороне (`cli.ts`).

**Цена:** тест `328` (resume → `keep`), которому §3 предписывает остаться зелёным **без единой правки**, передавал `previousSession` аргументом — и без правки перестал бы компилироваться. Правка в нём ровно одна: удалена строка `previousSession: "8f3a2b1c-0d4e",`. **Ассерт не тронут** — `.toEqual({ action: "keep" })` стоит дословно как стоял. Если это чтение §3 неверно и поле следовало оставить живым, скажи — верну его вместе со строкой теста.

## 3. Проверяемость — что именно поставлено

- **Юнит `workspace.test.ts`.** Перечень состояний головы гоняется **дважды** — под `completed` и под `quota-exhausted` — и два плана обязаны совпасть после подстановки слова причины (сравниваются целиком: ветка, `create`, текст отказа). Пять состояний: своя ветка, отделённая с `at`, отделённая без `at`, база, чужая ветка. Рядом отдельный ассерт, что `from` и сообщение несут **свою** причину (иначе подстановка прошла бы и на плане, из которого причину вымыли). Плюс перечень всех десяти причин `RELEASE_REASONS` → `commit`, и неизвестная причина → `commit` с её именем в сообщении.
- **Процессный `workspace.process.test.ts`.** Случай `timeout` переписан на живом git: коммит на `wip/dev-core/012-x-…`, оба файла (изменённый отслеживаемый и **неотслеживаемый**) внутри, сообщение коммита называет прогон, дерево чисто и стоит на базе, **`git stash list` пуст**. Ассерты «stash пуст» в соседних случаях оставлены как были.
- **Письмо `tidy-letter.process.test.ts`.** Новый случай: та же настоящая грязь, причина релиза в журнале переписана на `timeout` (подделка — сознательная и названная в комментарии: настоящий обрыв стоил бы квоты или стенных часов сюиты, а читается ровно это одно слово). Письмо уезжает в стоячий адрес, несёт ветку, поднимает `dev-core`. Адрес работы — в письме, причина — на коммите ветки: тело письма причину не печатает, и я не стал её туда добавлять (это была бы правка гейта/шаблона сверх предмета).
- **Сторожи другого вопроса** (`workspace.test.ts` про `dirtLeftByFinish`, `run.process.test.ts:2310`) — **ассерты не тронуты**, поправлены только формулировки в названии и комментарии, называвшие stash исходом.

**Замерено локально (`TMPDIR=/tmp`, `--root packages/agent-protocol`):** `workspace.test.ts`, `workspace.process.test.ts`, `workspace.commit.process.test.ts`, `tidy-letter.process.test.ts`, `status.service-branches.process.test.ts` — **164/164 зелено**. `tsc --noEmit` и `biome check` чисты. Полную сюиту локально не гонял — её меряет `checks` PR.

## 4. Где стою

PR #307 открыт `pnpm protocol pr open`, тело несёт `thread:`/`role:`. Ход по своему PR кончается здесь: **жду `checks` по #307, голова `890d9a68`**. Зелёный прогон → метка `review` на той же голове → вердикт → кнопка твоя. Паркуюсь на прогон, чтобы следующий такт читал уже готовый цвет.

## 5. ПОПРАВКА к §4, снятая за минуту до срока: **`checks` по #307 КРАСЕН**

Прогон `34043560421` на голове `890d9a68` — `conclusion: failure`. Причину прочитать я уже не успел: срок аренды 16:37:44Z. **Метку `review` не вешал** — на красной голове её вешать нельзя.

**Следующей сессии `dev-core` (это я, следующим тактом):** взять `gh run view 34043560421 -R … --log-failed`, назвать ПЕРВЫЙ упавший шаг (typecheck роняет тесты и они не гоняются вовсе), починить, запушить в ту же ветку. Вероятные кандидаты, которые я локально не мерил: полная сюита — правка нормы `planWorkspace` могла уронить чужие тесты вне пяти файлов, что я прогнал (например ассерты в `daemon.*.process.test.ts` или в тестах статуса, ожидающие слово «parking»); греп по `parking what the` и по `ENDED ITS OWN TURN` по всему дереву — первое, что стоит сделать.

Рабочее дерево чистое, всё закоммичено и запушено: `890d9a68` на `origin/dev-core/132-stash-hides-the-aborted-run`. Незакоммиченного нет.

## msg-006 · from: dev-core · 2026-09-06 · expects: none

**Краснота #307 разобрана и починена: упал ШАГ 8 `pnpm lint`, а не тесты. Новая голова `ae420076`, жду `checks` (прогон `34048189575`).**

## 1. Что именно было красным — по факту, не по догадке

Прогон `34043560421`, голова `890d9a68`. Первый упавший шаг — **8-й, `pnpm lint` (`biome check .`)**; шаги 9 (`typecheck`), 10, 11 (`pnpm test`), 12 и 13 — `skipped`. То есть **ни typecheck, ни сюита в том прогоне не гонялись вовсе**, и мои опасения прошлого такта («правка нормы могла уронить чужие тесты вне пяти файлов») тем прогоном не проверялись ни в какую сторону.

Диагностик в шаге ровно один — `Found 1 error`, остальные 12 это `infos`, и они стоят на `main` тоже (замерено: `pnpm lint` на `7db145ba` даёт **exit 0** и те же 12 `infos`, включая ноту про схему `biome.json` 2.5.4 против CLI 2.5.8 — она информационная и цвет не решает):

```
packages/agent-protocol/src/orchestrator/workspace.process.test.ts format
  × Formatter would have printed the following content:
  - expect(`${result.stdout}${result.stderr}`).toContain(
  -   "committing what the 'stalled' run left",
  - );
  + expect(`${result.stdout}${result.stderr}`).toContain("committing what the 'stalled' run left");
```

**Причина установлена, а не выведена:** переписывая случай `stalled` в `workspace.process.test.ts`, я укоротил текст ассерта, и вызов стал влезать в одну строку — форматтер требует её сложить. Мой прошлый доклад говорил «`biome check` чист»; это было верно **до** последней правки того файла и неверно после — я прогнал линтер не последним действием, и это моя ошибка порядка, а не расхождение инструментов.

## 2. Что сделано, коммит `ae420076`

`style(workspace): формат biome в переписанном процессном тесте (тред 132)` — **поведение не меняется ни строкой**, диффа кода в нём нет:

- `workspace.process.test.ts` — формат, применён самим biome (`biome format --write` по одному файлу; `--write` без `--unsafe`, так что 12 `infos` на `main` не тронуты и мой дифф их не втягивает);
- `tidy-letter.process.test.ts` — **название** нового случая приведено к тому, что он утверждает. Было «…the letter still goes, **with that reason in it**», а комментарий в двух строках ниже говорит обратное: письмо несёт **адрес** работы, причина прогона живёт на коммите ветки. Стало «…the letter still goes, **and names the work's address**». Ассерты не тронуты — оба (`shown).toContain(branch)` и `log -1 --format=%s` с `'timeout'`) стоят как стояли. Правлю, потому что название теста читается следующим как утверждение о поведении; расходящееся название — та же болезнь, что отказ, называющий не свою причину (§1.2 постановки).

## 3. Чем закрыто то, чего не мерил красный прогон

Всё на голове `ae420076`, локально, `TMPDIR=/tmp`:

- `pnpm lint` — **exit 0**, `Found 12 infos` (ровно как на `main`);
- `pnpm typecheck` — обе `tsc` чисты;
- **полная сюита `packages/agent-protocol`** (та самая, которой я в прошлый такт не гонял): `vitest run --root packages/agent-protocol` — **221 файл, 3678 тестов, всё зелено, exit 0**. Это и есть ответ на мой же вопрос из прошлого письма: правка нормы `planWorkspace` чужих тестов не уронила, слов «parking»/`ENDED ITS OWN TURN` в живых ассертах не осталось;
- `pnpm --filter transport-telegram test` — 2 файла, 7 тестов, зелено (в CI `pnpm -r test` гоняет и его);
- греп по дереву: `stashLabel`, `action: "stash"`, `case "stash"` — читателей нет; оставшиеся упоминания `stash` это либо исторический рассказ (шапка модуля, README, `docs/protocol-reference.md:634` — «до 06.09 судьбы было две»), либо **совет человеку** в тексте отказа («…or PARK it: `git stash push -u`»), который предметом не трогался.

Пункты приёмки §4 при этом не сдвинулись: дифф кода прежний, `ae420076` его не касается.

## 4. База

`gh pr view 307`: `mergeable: MERGEABLE`, `mergeStateStatus: UNSTABLE` (= идёт `checks`), меток нет. Ветка отстаёт от `main` на `7db145ba` (#309, тред 141), но `checks` поднят событием `pull_request` — GitHub меряет **слияние** головы с `main`, значит пара #309 + этот дифф прогоном покрыта. Ребейз поэтому не делаю: он аннулировал бы идущий прогон без выигрыша.

## 5. Где стою

Голова `ae420076` запушена в `origin/dev-core/132-stash-hides-the-aborted-run`, рабочее дерево чистое, незакоммиченного нет. **Жду `checks` по #307 на голове `ae420076`** — прогон `34048189575`, он на момент письма `pending`. Паркуюсь на #307 (`--parked-on run:307`: поле хочет номер PR, а не id прогона).

Зелёный прогон → метка `review` на той же голове (на красной вешать нельзя, и в прошлый такт я её не вешал) → вердикт → кнопка твоя.

waiting-on → dev-core.

## msg-007 · from: github · 2026-09-06 · expects: none

✅ **checks по PR #307: `success`.**

feat(workspace): грязь оборванного прогона коммитится, а не паркуется в stash (тред 132) · голова `ae42007620f8127aa72e4abc2aa2d3a6c99c5b17` · попытка 1 · прогон [`34048189575`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34048189575)

- `checks` — **success**

👉 Круга ревью на этой голове ещё нет — метка `review` не повешена. По норме 03.08 (тред 049, [#183](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/pull/183)) она вешается ПОСЛЕ зелёного `checks` на той же голове, то есть сейчас. Ход у автора ровно на это одно действие.
