# Ветвь stash прячет работу оборванного прогона — конвертировать в коммит?

participants: curator, dev-core, john · status: open

## msg-001 · from: curator · 2026-09-05 · expects: ack

**Требуется решение john: распространить ли право «демон убирает грязь КОММИТОМ» на ветвь `stash` — грязь прогона, оборванного самим контуром?**

Происхождение: тред `099-dirty-tree-locks-the-role`, моя же постановка от 2026-09-05 (§1, «НЕ трогать
в этом пакете»). Там я сознательно вывела этот вопрос за предмет и обещала принести отдельным
письмом; предмет `099` (A и B) с тех пор влит — PR #279, `main` `8bbc7940f22a9f9017e852e539167c59f0f13b4c`.
Отдельный тред, потому что `099` в этот момент ждёт хода dev-core, а парковка на john заморозила бы
именно его.

## 1. Факт — что в коде СЕЙЧАС

`packages/agent-protocol/src/orchestrator/workspace.ts` на `origin/main` (прочитано
`git show origin/main:…`, после мержа #279):

- строки 689–693: `if (previousReason !== undefined && ABORTED_RUN_REASONS.includes(previousReason))`
  → `action: "stash"` с меткой `stashLabel(…)`;
- строка 169 (комментарий формы): «`stash` — dirt left by a run THE CIRCUIT CUT OFF: parked under a
  label that names …».

То есть после #279 у грязного дерева два исхода вместо прежних двух: **коммит** — когда прогон
завершился сам (`completed`, `exited-without-handoff`, интерактивные окончания, `forced`), и
**`stash`** — когда прогон оборвал контур. Ветвь `stash` не тронута намеренно, тест-сторож на неё в
#279 стоит.

## 2. Почему я не решаю это сама

Ваш довод 2026-09-05 (~11:51Z, тред `099`) — **«спрятанное не видно никому»** — бьёт по ветви `stash`
слово в слово: дерево становится чистым, роль поднимается ближайшим тиком и о своей отложенной работе
не узнаёт ниоткуда; `git stash list` виден только на этой машине, у него нет ни автора в общем месте,
ни адреса, который можно назвать в письме. Условие узости права выдержано тождественно: правки
принадлежат той же роли, чужого дерева никто не трогает.

**Но это смена уже ОБЪЯВЛЕННОГО поведения, а не починка**, поэтому гард 3 возвращается к вам, и своей
рукой я такой дифф не ставлю и не мёржу.

**Что в этом случае действительно другое** (называю против себя): прогон, оборванный контуром,
чаще обрывается посреди правки, чем завершившийся сам, — то есть полуфабрикат вероятнее. Риск вы уже
взвесили и приняли для коммитной ветви («в своей ветке терпимо: следующая сессия видит и доделывает»),
и на мой взгляд он здесь тот же, только чаще. **Это моя рекомендация, а не решение: рекомендую (a).**

## 3. Что решить — две строки

- **(a) конвертировать:** ветвь `stash` уезжает, грязь оборванного прогона убирается тем же коммитом
  по правилам 1.1–1.3 постановки `099` (на ветке роли → туда же; вне ветки → служебная ветка;
  общая/чужая → сегодняшний отказ). Тест-сторож переписывается на новое поведение;
- **(b) оставить как есть:** `stash` остаётся исходом для оборванных прогонов, и я закрываю вопрос —
  больше он не поднимается.

**Цена момента:** dev-core прямо сейчас пишет по `099` предмет C (письмо наружу) в этом же файле и
этой же формы. При (a) конверсия — самый дешёвый следующий шаг сразу за C; спрошенная через неделю,
она станет отдельным заходом в остывший код.

Ход остаётся у меня (`waiting-on → curator`): жду вашего слова, парковка на вас.

## msg-002 · from: curator · 2026-09-06 · expects: none

**Слово john полем `delivers` (чат 2026-09-06 ~12:23Z): «ДА» — вариант (a). Ветвь `stash` КОНВЕРТИРУЕТСЯ: грязь прогона, оборванного контуром, убирается тем же коммитом. Парковка снята.**

## 1. Что решено

Право «демон убирает грязь КОММИТОМ» распространяется на **обе** ветви. Правила прежние, из постановки `099`: дерево на ветке роли → коммит туда же; вне ветки → служебная ветка; общая или чужая → сегодняшний отказ. Тест-сторож переписывается на новое поведение.

**Довод john, и он его же вчерашний, слово в слово:** **спрятанное не видно никому.** Разница между двумя ветвями искусственна — при одном исходе правки видны и восстановимы, при другом лежат локально, без автора в общем месте и без адреса, который можно назвать в письме; роль поднимается с чистым деревом и о своей работе не узнаёт ниоткуда.

**Формулировка john:** не должно быть **двух разных судеб у одной и той же грязи** в зависимости от того, кто оборвал сессию.

## 2. Риск, названный тобой против себя, — принят явно

Прогон, оборванный контуром, чаще прерывается ПОСРЕДИ правки, значит полуфабрикат вероятнее — в коммит уедет файл, который может не собираться.

**john этот риск уже взвесил и принял для коммитной ветви 05.09** («в своей ветке терпимо: следующая сессия видит и доделывает»), и здесь принимает его в том же виде. Довод: невидимая работа хуже видимого полуфабриката — полуфабрикат чинится следующей сессией, а спрятанное теряется молча.

**Условие узости права выдержано и не расширяется:** правки принадлежат той же роли, чужого дерева никто не трогает, главный чекаут не затрагивается (john отказал в этом 04.09, тред `096`, решение в силе).

## 3. Цена момента — учтена

Ты назвала её сама: dev-core прямо сейчас пишет по `099` предмет C в этом же файле и той же формы. **Конверсия ставится следом за C**, пока код тёплый; отдельным заходом через неделю она стоила бы дороже.

**И требование `099` §3 остаётся в силе:** служебные ветки обязаны иметь конец — он называется при заведении, а не потом.

waiting-on → curator.
