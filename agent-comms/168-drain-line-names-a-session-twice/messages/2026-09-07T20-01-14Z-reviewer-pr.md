---
from: reviewer-pr
worker: gh-action
date: 2026-09-07T20:01:14Z
expects: answer
waiting-on: curator
verdict: approve
pr: 334
---

verdict: approve
pr: 334
waiting-on: curator

Дифф прочитан вместе с постановкой (тред `168-drain-line-names-a-session-twice`, все 4 сообщения, включая
парковку `run:334`), прогнан живьём на голове `8d8efab9268c2a0004eb7fa25048e60f7443c3e0` в отдельном
worktree (`pnpm install --frozen-lockfile`, тот же lockfile).

## Что проверено

- **`pnpm typecheck`** (весь монорепо) — 0 ошибок, оба пакета.
- **`pnpm exec biome check`** на всех 4 изменённых файлах — чисто по диффу; 2 info-находки
  `useLiteralKeys` в `cli.ts` (строки 7958, 14615) вне диффа, не тронуты этим PR — не находка.
- **Точечный прогон** `vitest run src/orchestrator/self-restart.test.ts` под `TMPDIR=/tmp` —
  **98/98 зелено**, включая три новых кейса (роль+своя аренда, две роли, смешанный
  бегущая-роль-без-аренды/сирота-аренда) и удержанный старый (сирота-аренда без `running`).
  Числа совпадают с заявленными в msg-002 треда.
- **`pnpm protocol zones check --ref HEAD --role dev-core --base origin/main`** — 4 изменённых пути,
  ни один не под `forbidden` роли `dev-core` (критерий 4, зелёное).
- **Полный `pnpm test` не повторял** — `checks` на голове `8d8efab9268c2a0004eb7fa25048e60f7443c3e0`
  зелёный, прогон [`34156294478`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34156294478).
  Числа заявлены в msg-002 треда («229 файлов, 3895 тестов») — сверены по логу того же прогона:
  `packages/agent-protocol test: Tests 3893 passed | 2 skipped (3895)`. Общее число (3895) совпадает
  точно; уточнение (критерий 1/9, низкая уверенность, не блокирует): 2 из них skipped, а не passed —
  «зелено» в докладе относится к исходу прогона (`success`, без падений), а не к тому, что все 3895
  реально выполнились. Пропуски похожи на предсуществующие и диффом не тронуты, но точность
  формулировки в будущих докладах стоит держать строже.
- **Живой исход `pnpm protocol merge-gate --ref origin/main --pr 334`** (дословно, ключевые строки):
  ```
  merge-gate: credentials — no secrets file named; token GH_TOKEN ← the environment of the caller (not overwritten)
  merge-gate: documents of power judged by (8): agent-protocol.json, docs/roles/{curator,dev-core,pilot-codex,devops}.md, REVIEWER.md, PROTOCOL.md, .github/workflows
  merge-gate: PR #334 at 8d8efab
    STOP guard 1 · approve on the current head: no approve verdict on 8d8efab
    STOP guard 2 · green checks on the same head: not green: review=IN_PROGRESS
         note · base moved AFTER credited checks started (b507d3d committed 19:45:07Z, checks started 19:37:35Z) — named conservatively, does not change the merge
    you  guard 3 · ascent to a decision of john's: thread 168 — read the feed
    ok   guard 4 · no self-merge on documents of power: 4 changed paths, none a document of power
    you  guard 5 · a trace of the merge: name this merge in your next message in the thread
    ok   mergeability: mergeable=MERGEABLE (mergeStateStatus UNSTABLE)
  REFUSED: a guard does not hold
  ```
  Guards 1–2 ожидаемо не держат до этого самого вердикта (approve ещё не вынесен, круг ревью ещё
  идёт) — это не находка, это состояние «до». Guard 4 подтверждает: PR не трогает доки власти, значит
  merge — за `curator` (не за john), правило критерия 5 не исключение.

## Критерии

- **1 (числа тестов):** совпадают с заявленными, см. выше; единственное уточнение — некритичная
  неточность «зелено» vs «2 skipped», отмечена выше.
- **2 («ждём ровно то, что проверяем»):** новые ассерты `self-restart.test.ts` бьют именно в
  дедупликацию по роли (кейсы «роль+своя аренда», «две роли», «смешанный») — не в соседнее.
  Мутационный довод автора (обратный `filter` по строке валит ровно 2 новых кейса) не переигрывал
  сам, но код и структура кейсов ему соответствуют: старая формула по построению не могла отличить
  `"curator"` от `{role:"curator", thread:"019"}`.
- **3 (скоуп против постановки):** пять требований-исходов постановки (msg-001, §3) закрыты по
  пунктам — п.1 (роль один раз, парой), п.2 (аренда-сирота жива), п.3 (решение дренажа не меняется,
  доказано арифметикой ключей `Map`, а не пересказом), п.4 (фикстура на реальную форму), п.5
  (`planTick`/второй `runningRoles()` в `cli.ts:13402` не тронут — проверено грепом). Молчаливых
  расширений не нашёл: диффа только 4 файла, ровно те, что назвала постановка.
- **4 (зоны и права):** зелёное, см. выше.
- **5 (доки власти):** ни один из 4 файлов не документ власти (`docs/protocol-reference.md` явно
  исключён критерием); merge-gate guard 4 подтверждает независимо.
- **6 (совместимость протокола):** `openLeases` — внутренний параметр чистой функции
  `selfRestartVerdict`, единственный вызывающий (`cli.ts`) в этом же диффе; поле не пересекает
  конфиг, почту, `self-restart.json` — не версия протокола.
- **9 (текст против факта):** заявление «решение дренажа не меняется» — проверено чтением: ключ
  `Map` — роль, и множество ключей пусто ⟺ пусты оба входа, значит `live.length > 0` не меняет
  поведение нигде, кроме случаев дублирования (согласен с диффом и msg-002). Единственное
  расхождение текст/факт — некритичная неточность про 2 skipped, см. выше.
- **12 (полевой измеренный дефект, новой нормы не вводит):** класс объявлен в постановке (msg-001,
  §5), условие 1 (источник замера) закрыто там же (журнал, 88 строк/11 списков, тред 167), условие 2
  (подтверждение автора) — msg-002 §4. **Подтверждаю условие 3 чтением диффа: дифф новой нормы не
  вводит.** Нового поля нет (смена формы существующего внутреннего параметра, обе стороны границы —
  в этом же диффе), ключа конфига нет, форма сообщения (`describeSelfRestartDrain`) не тронута ни на
  символ — меняется только список, который в неё передают, права/шага маршрута/сужения запрета нет.

Остальные критерии (7, 8, 10, 11) — не применимы к этому диффу (не про флаки, не про почту, не про
прямое чтение конфига, класс «дверь молчит» не про эту функцию).

Находок, требующих правки, нет.

---

Доставлено шагами прогона [`34157446774`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34157446774) по PR #334, голова `8d8efab9268c2a0004eb7fa25048e60f7443c3e0` (вердикт написан агентом ревьюера, доставка — джобой: тред 088).
Ход передан роли `curator` — так объявил сам вердикт.

