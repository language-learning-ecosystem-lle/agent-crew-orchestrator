---
from: dev-core
worker: claude-code
session: 85b8815c-b364-4880-b591-1825d94ab88d
raised: 2026-09-07T19:30:35Z
date: 2026-09-07T19:38:46Z
expects: none
waiting-on: dev-core
parked-on: run:334
---

# Починка сделана, PR #334 открыт — жду `checks`, голова `8d8efab9`

Тип поля закрыл класс, а не только его экземпляр; решение дренажа не сдвинуто, и это доказано
арифметикой ключей, а не пересказом.

## 1. Что сделано (форма починки — построением, как предпочла постановка)

Дифф трогает три файла кода/доков:

1. **`self-restart.ts`** — `openLeases` принимает **пары** `{ role, thread }` вместо `string`.
   Это и есть починка класса: поле, куда контур клал `роль/тред`, больше НЕ МОЖЕТ нести голое
   имя, значит расхождение форм между двумя полями невозможно построением, а не соглашением.
   Склейка `${role}/${thread}` переехала внутрь `selfRestartVerdict` — в единственное место,
   которое видит оба поля сразу.
2. **`self-restart.ts`, склейка `live`** — ключом дедупликации стала **роль**, а не строка
   целиком: `new Map` по `roleOf`, последняя запись побеждает. Порядок вставки `Map` сохраняет
   прежний порядок вывода (сначала бегущие роли, затем аренды, которых никто не бежит).
   Побеждает форма с тредом (информативнее); голое имя остаётся ровно там, где пары нет.
3. **`cli.ts:12642`** — `.map((lease) => `${lease.role}/${lease.thread}`)` удалён, `unclosedLeases(…)`
   уходит в вердикт как есть. Второй вызов `runningRoles()` в `planTick` не тронут (п. 5 постановки).
4. **`docs/protocol-reference.md`** — абзац в разделе самоперезапуска: почему сравнение строк не
   ловило две формы, почему ключом стала роль, почему решение дренажа этим не двигается и чем
   именно фикстура старого теста пропускала дефект. README «Commands» не тронут: форма команд не
   менялась.

## 2. Требования-исходы постановки — по пунктам

- **п.1 (роль называется один раз, парой):** закрыт ключом-ролью. `running: ["dev-core"]` +
  `openLeases: [{role: "dev-core", thread: "063-…"}]` → `["dev-core/063-…"]`, одна запись.
- **п.2 (сирота-аренда по-прежнему живая сессия):** удержан. `openLeases: [{role: "curator",
  thread: "019"}]` без `running` → `drain`, `roles: ["curator/019"]`. Тест на месте, переписан
  только по форме входа.
- **п.3 (решение дренажа не меняется) — называю явно, как требует постановка.** Довод не
  «проверил тестами», а арифметика: `live` теперь есть значения `Map`, чьи ключи — РОЛИ входа.
  Ключ у каждого элемента непуст и берётся из непустого поля, поэтому **множество ключей пусто
  ровно тогда, когда пусты оба входных массива**. Значит `live.length > 0` меняет значение только
  там, где раньше оно тоже было бы `0`, — то есть нигде. Ветвь `self-restart.ts` `live.length > 0`
  судит как судила; ни один `stand` не тронут (дедупликация стоит ПОСЛЕ всех проверок руки только
  по расположению строки — на них она не влияет вовсе).
- **п.4 (фикстура перестроена на реальную форму):** сделано, и это половина починки, а не
  украшение — см. мутационную пробу ниже.
- **п.5 (`planTick` вне замера):** не тронут.

## 3. Проверяемость — что именно замерено

Юнит несёт всё, интеграционного нет (по вашему же разбору: путь от `cli.ts` до печати — передача
массива без ветвлений).

Случаи в `self-restart.test.ts`:

- **роль и её собственная аренда в форме контура** — обязательный новый (п.4 постановки);
- **две роли сразу** — тот самый список из журнала 2026-09-07, который печатал четыре записи:
  `(dev-core, curator, dev-core/153-…, curator/155-…)` → теперь две пары;
- **смешанный** — бегущая роль БЕЗ аренды + сирота-аренда без бегущей роли →
  `["dev-core", "curator/019"]`: дедупликация не съедает ни одну половину, и голое имя стои́т
  ровно там, где пары нет;
- **сирота-аренда** — удержан без изменения смысла.

**Мутационная проба (тест доказан, а не объявлен).** Вернул старую формулу — `filter` по целой
строке над теми же парами — и прогнал файл: падают **ровно два** новых случая дедупликации
(«роль и её аренда», «две роли»), остальные **96 зелёные**. То есть новые тесты ловят именно этот
дефект, а не срабатывают на чём-то соседнем. После пробы формула возвращена, файл снова 98/98.

**Полная сюита пакета** под `TMPDIR=/tmp --root packages/agent-protocol`: **229 файлов, 3895 тестов —
зелено**. `tsc --noEmit -p packages/agent-protocol` — 0. Biome check — чисто, правок не потребовал.

**Живую приёмку человека** не жду и не прошу ждать: следующий самоперезапуск напечатает строку сам,
грепается по `waits for the sessions that are live`.

## 4. Класс восхождения — подтверждаю условие 2 своими словами о своём диффе

**Дифф новой нормы не вводит.** По вашему ориентиру, по пунктам:

- **нового поля — нет.** `openLeases` существовал; сменилась ФОРМА существующего поля входа чистой
  функции. Поле пересекает ровно одну границу — `cli.ts` → `selfRestartVerdict`, оба конца в этом
  же диффе; наружу пакета оно не ходит и в конфиг, почту, журнал или память `self-restart.json` не
  попадает;
- **ключа конфига — нет.** Конфиг не тронут вовсе;
- **формы сообщения — нет.** `describeSelfRestartDrain` не изменён ни на символ: та же фраза, те же
  скобки, тот же `join(", ")`. Изменился СПИСОК, который в неё передают;
- **права — нет**, **шага маршрута — нет**, **снятия или сужения запрета — нет.**

Строка меняет ЧТО перечисляет; когда она печатается и что решает — прежнее (п.3 выше, с доводом).

## 5. Где стои́т

PR **#334** — https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/pull/334,
ветка `fix/168-drain-line-names-a-session-once`, голова **`8d8efab9`**, база `main` на `73e9abcc`
(перебазировки не потребовалось). Тело несёт `thread: 168-drain-line-names-a-session-twice` и
`role: dev-core`, PR открыт `pnpm protocol pr open --write`.

Прогон `checks` **34156294478** идёт на этой голове (событие `pull_request`, `in_progress` на момент
письма). Метка `review` НЕ повешена — по маршруту она вешается только после зелёных чеков на той же
голове; в foreground прогон не жду.

Незакоммиченного нет: рабочее дерево чистое, всё в одном коммите `8d8efab9`, запушено.

**Следующий ход — мой:** прочитать исход прогона, на зелёном повесить `review` на ту же голову и
передать ход на круг. Паркуюсь на PR формой `run:334` — впереди ход мой (метка), не кнопка. Если прогон краснеет — чиню и
гоняю заново; на зелёном после вердикта кнопка ваша, по пяти гардам.

