---
from: dev-core
worker: claude-code
session: 19f3e393-dfab-4c86-b918-8dadd31e7350
date: 2026-09-02T10:58:31Z
expects: answer
waiting-on: curator
parked-on: run:183
---

**Причина — механизм из `056` (PR #172), и это первая строка доклада, как просила постановка §4.3.** Каталог «своего временного» назван именем прогона — отметка времени, роль, слаг треда, — и это имя длиннее, чем может позволить себе unix-сокет, открытый под ним. Файлу такая длина безразлична, сокету нет; сессия есть то, что открывает сокеты. Чистый `/tmp` куплен ценой красного локального прогона у роли.

## 1. Замер (шаг 1) — числа, не «длинный»

Все четыре воспроизводятся на ящике роли; команды ниже.

- **Предел — 108.** Путь unix-сокета длиной 108 символов слушается, 109 отказывает `listen EINVAL`. Это `sizeof(struct sockaddr_un.sun_path)` в Linux, и предел стоит на СТРОКЕ, переданной в `bind()`, а не на том, во что она разрешается. Это же объясняет форму починки;
- **бюджет `TMPDIR` — 86.** `tsx` (загрузчик, через который спавнится каждый процессный тест пакета) открывает `<TMPDIR>/tsx-<uid>/<pid>.pipe` — 22 символа при uid `1000` и семизначном pid. Бисекцией: `TMPDIR` длиной 86 работает, 87 падает. 86 + 22 = 108;
- **переменная — ДЛИНА, а не место.** То же 142-символьное имя под `/tmp` падает; 71-символьное ВНУТРИ `.orchestrator/sessions/` работает. Каталог сессий ни при чём;
- **наблюдавшийся путь поимённо:** `TMPDIR=/home/lle/projects/agent-crew-orchestrator/.orchestrator/sessions/2026-09-02T10-36-15Z-dev-core-070-session-tmpdir-breaks-tests.tmp` — **131 символ**; сокет, который пытался открыться, — `…tmp/tsx-1000/1594502.pipe`, 151. Под ним `notify.process.test.ts` — 36 из 36 красных, воспроизведено в начале этого прогона.

**Воспроизводящие команды** (curator: любая из них, из корня чекаута):

```
# предел ядра — 108 слушается, 109 EINVAL
TMPDIR=/tmp npx vitest run packages/agent-protocol/src/orchestrator/run-tmp.process.test.ts

# одна переменная, разный исход — длина, не место
L=/tmp/pad-aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa.tmp
mkdir -p "$L" && env TMPDIR="$L" ./node_modules/.bin/tsx packages/agent-protocol/src/cli.ts --version   # EINVAL, длина 142, под /tmp
S=/home/lle/projects/agent-crew-orchestrator/.orchestrator/sessions/s.tmp
mkdir -p "$S" && env TMPDIR="$S" ./node_modules/.bin/tsx packages/agent-protocol/src/cli.ts --version   # работает, 71, в каталоге сессий

# порог бюджета TMPDIR: 86 работает, 87 падает
for N in 86 87; do D=/tmp/p/$(python3 -c "print('a'*($N-7))"); mkdir -p "$D"; env TMPDIR="$D" ./node_modules/.bin/tsx packages/agent-protocol/src/cli.ts --version >/dev/null 2>&1; echo "$N -> rc=$?"; done
```

## 2. Починка (шаг 2) — короткий алиас, и почему не короткое имя

Ни один путь, выведенный из чекаута, гарантированно коротким быть не может: `/home/…/agent-crew-orchestrator/.orchestrator/sessions/` — уже 65 символов ДО того, как прогон назван, а следующий ящик может лежать глубже. Укорачивание имени покупает запас и оставляет класс — следующее длинное имя вернуло бы его молча.

Поэтому: супервизор, увидев `TMPDIR` длиннее бюджета (108 − 32 резерва = **76**), создаёт симлинк `/tmp/aco-<12 hex от пути каталога>` на тот же каталог и отдаёт ребёнку ЕГО. Работает потому же, почему стоит предел: ядро копирует адрес, а разрешает путь после.

**`056` не отменяется и не судится.** Настоящий каталог по-прежнему лежит рядом с логом прогона, `mktemp -d` сессии рождается в нём, супервизор так же называет по именам оставшееся и сносит. В общем `/tmp` — ровно один симлинк, названный хешем самого каталога, снимается на закрытии прогона. Вопрос про `PrivateTmp=yes` в `056` этим не тронут.

**Дверь говорит:** подстановка пишется в лог супервизора вместе с числом, её вызвавшим; невозможность создать алиас (занято чужой целью, не симлинк, отказ ФС) прогон не роняет — ребёнку отдаётся настоящий каталог, а в логе стоит строка с длиной, бюджетом и тем, что именно сломается (`listen EINVAL`). Молчание здесь означало бы EINVAL в чужом инструменте, называющий путь и не называющий причину.

## 3. Проверяемость — по всем трём пунктам §6

- **Замер воспроизводится** — команды выше;
- **тест держит ИНВАРИАНТУ, а не зелёный набор.** Новый `run-tmp.process.test.ts` (процессный, потому что предел живёт в ядре и юнит над длинами строк его не заменяет): биндит сокет на 108 и на 109; строит каталог, **дополненный до 131 символа** (а не унаследовавший глубину ящика — тест, воспроизводящий класс только на глубоком ящике, молчит на мелком, и ровно так это прожило CI); проверяет, что под выданным значением сокет открывается, что родился он в каталоге ПРОГОНА, что под сырым путём — `EINVAL`, и что сам `tsx` под выданным исполняется, а под сырым умирает. **4/4.** Стык `056` в `run.process.test.ts` обновлён тем же коммитом: принимает обе формы выданной строки, а утверждение «записи ложатся в каталог прогона» держится сноской, как и держалось;
- **живая приёмка на ящике роли, БЕЗ ручного `TMPDIR=/tmp`.** Значение, которое чинёный лончер выдал бы этой самой сессии: каталог `…/.orchestrator/sessions/2026-09-02T10-36-15Z-dev-core-070-session-tmpdir-breaks-tests.tmp` (131) → выдано `/tmp/aco-2e60ebdd492a` (21). Под ним `vitest run packages/agent-protocol/src/notify/notify.process.test.ts` — **36/36 зелёных, дважды подряд**. Оговорка честная: эту сессию поднял ещё старый код, поэтому выданное значение вычислено чинёной функцией и подставлено вручную; после мержа и перезапуска демона так будет по умолчанию;
- **локально сверх того:** `run.process.test.ts` + `run-tmp.process.test.ts` — **59/59 после перебазировки**, `tsc --noEmit` и `biome check` чисты.

Постановка §4.4 (ручной обход не есть починка) соблюдена: `TMPDIR=/tmp` из процесса роли уходит.

## 4. Что сделано и где стоит

PR **#183**, ветка `feat/070-run-tmpdir-socket-budget`, голова **`f61da76619b3eb74893046a07693c03e7a8fb4e9`**.

Один коммит: новый `packages/agent-protocol/src/orchestrator/run-tmp.ts`, правка места спавна в `cli.ts`, новый процессный тест, правка стыка `056`, и доки тем же коммитом — `docs/protocol-reference.md` (новый раздел) и README пакета (S26a).

**Перебазирован в этом же ходу:** первая голова `e96af289` пришла в конфликт с уехавшим `main` (дверь парковки отказала по имени — «PR CONFLICTING, прогон не родится», и это её работа). Разошлись `cli.ts` и README с приехавшим тредом `069-session-path` (S27, `PATH` сессии) — разведены руками, обе правки на месте: `[RUN_TMPDIR_ENV]: runTmp.handed` и `SESSION_PATH_ENV` стоят рядом. После перебазировки тесты, типы и линт перепрогнаны, метки на PR не было — снимать и вешать заново нечего.

Дерево чистое, незакоммиченного нет. Дифф доков власти не трогает — по гарду 4 это обычный PR, кнопка curator.

**Жду CI по #183, голова `f61da766`.** У конвейера не стою, ход передаю: прогон отчитается сам, дальше метка `review` на зелёной голове и круг ревьюера.

