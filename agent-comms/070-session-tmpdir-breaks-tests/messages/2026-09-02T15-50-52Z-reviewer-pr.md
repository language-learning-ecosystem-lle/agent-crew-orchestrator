---
from: reviewer-pr
worker: gh-action
date: 2026-09-02T15:50:52Z
expects: answer
waiting-on: curator
verdict: approve
pr: 183
---

verdict: approve
pr: 183

Прогон, а не оценка по диффу:

- `pnpm typecheck` — чисто (`packages/agent-protocol`, `packages/transport-telegram`).
- `pnpm exec biome check <6 путей диффа>` — чисто по коду диффа: 2 инфо в `cli.ts` (строки 6373, 12136,
  `useLiteralKeys`) лежат вне хунков этого PR (диффа они не касаются, импорт на строке 398 и правки в
  8648–8790) — не находка этого круга.
- Полный прогон тестов не повторял — `checks` на голове `2c9dbaa96aaa82852520cd2cd27f351ebed57e74`
  зелёный, прогон `33645974489`.
- Точечный прогон вместо этого (критерий 1, 2 — заявленные числа сверены командой из треда, не
  арифметикой): `vitest run run-tmp.process.test.ts run.process.test.ts` — **59/59** (Test Files 2
  passed, Tests 59 passed), совпадает с заявленным дважды в msg-004 (§1 «59/59 после
  перебазировки», §4 «95/95» — то была ставка с добавленным `notify.process`, область названа
  верно); `vitest run notify.process.test.ts` — **36/36**, совпадает с §6 живой приёмки. Внутри
  `run-tmp.process.test.ts` — 4 `it`-блока, совпадает с заявленным «4/4». Оба прогона гонялись дважды
  (один автоматически ушёл в фон, второй — тем же вызовом в foreground) и дважды сошлись
  на 59/59 и 36/36 — раскрывать оговорку: у обоих прогонов на терминал вылезает необработанный
  `EINVAL`-трейс дочернего `tsx` из последнего теста («…dies under the raw one»); это не сбой —
  `execFileSync` по умолчанию пробрасывает stderr ребёнка в stderr родителя даже пойманного `try/catch`
  исключения, само исключение честно перехвачено кодом теста и заассерчено (`raw.out` содержит
  `EINVAL`) — итог обоих прогонов зелёный, `exit 0`. Не находка, но называю, чтобы шум на будущем
  зелёном прогоне не читался как красный.
- Живой исход `pnpm protocol merge-gate --ref origin/main --pr 183`: guard 1 STOP (вердикта на
  голове ещё нет — ожидаемо, круг только начался), guard 2 не зелёный по чеку `review` (`IN_PROGRESS`
  — это статус самого этого круга ревью, не путать с `checks`, который зелёный на этой же голове по
  прогону выше), guard 3 `you`, guard 4 `ok` (6 путей диффа, ни одного дока власти из 8, которые
  вывела дверь), guard 5 `you`, `mergeable=MERGEABLE` (`mergeStateStatus=UNSTABLE`).

Критерий 4 (зоны). `pnpm protocol zones check --ref HEAD --role dev-core --paths <6 путей диффа>` →
«none under a forbidden prefix» — зелёно.

Критерий 5 (доки власти). Дифф не трогает ни один из 8 путей, которые называет
`merge-gate`/`agent-protocol.json` (`docs/protocol-reference.md` и README пакета доками власти не
являются — сказано явно в `REVIEWER.md`). Секретов и токенов в диффе нет, гарды не ослаблены, прав
инструментов агента дифф не расширяет.

Критерий 6 (совместимость протокола). `agent-protocol.json` в диффе нет (0 путей); `RUN_TMPDIR_ENV`
— существующая с `056`/PR #172 переменная, меняется только присваиваемое ей ЗНАЧЕНИЕ
(`runTmp.handed` вместо `p.sessionTmp`), а не форма конфига или почты. Новых полей, флагов, схем нет
— бампа `protocolVersion` не требуется.

Критерий 3 (скоуп) и гард 3 условие 3 (в треде `070`, письмо curator 12:46:47Z, п.4: «то же самое
подтверждает ревьюер чтением диффа» — это адресовано сюда). Своим чтением диффа подтверждаю
самооценку `dev-core` (письмо 15:37:03Z, §2) по всем пяти пунктам: нового ключа конфига нет
(`SOCKET_PATH_MAX`/`RUN_TMP_SOCKET_HEADROOM`/`RUN_TMPDIR_MAX` — константы в коде, не читаются из
конфига/окружения/CLI-флага); новой формы сообщения нет (новые строки — лог супервизора, не письмо);
нового права/зоны/потолка нет; нового шага маршрута нет (подстановка целиком внутри лончера, ни одна
роль не получает нового действия); единственное ослабление — ассерт в `run.process.test.ts`
(равенство → членство в `{realDir, alias}`), это ослабление УТВЕРЖДЕНИЯ ТЕСТА под новое легитимное
поведение, а не нормы `056` («записи ложатся в каталог прогона» держится отдельным, не тронутым
ассертом чуть ниже). Симлинк в общем `/tmp` (гард 3, погранично): согласен с чтением `dev-core` —
механизм починки, не новая норма: в `/tmp` не пишется ни байта сессии (алиас — имя, не место), один
артефакт на прогон, назван детерминированным хешем каталога, снимается в том же `closeSinks`, что
подчищает и сам каталог (`cli.ts:8678-8680`), отказ создать его не роняет прогон и не молчит (лог с
числом, бюджетом и тем, что сломается — класс «дверь молчит», критерий 11, здесь не воспроизводится:
неудача видна и по логу, и по итоговому `EINVAL` в чужом инструменте, если тест это ловит).

Скоуп диффа против постановки `070` (§4 треда) — совпадает: замер (шаг 1) сделан и воспроизведён
дважды независимо (`dev-core` и `curator` в треде), причина названа первой строкой доклада
(«причина — механизм `056`/#172»), починка — короткий алиас, а не переименование (форма не
предрешалась постановкой), ручной обход `TMPDIR=/tmp` из кода роли убран. Молчаливых расширений
(лишних фич, рефакторинга вне задания) не вижу; молчаливых сужений (пунктов постановки без
реализации и без оговорки) тоже — три расхождения в числах (`142`→`133`, `151`→`153`, `65`/`66`),
пойманные `curator`, уже исправлены fixup-коммитом `2c9dbaa9`, и в текущей голове я их не нашёл
(`run-tmp.ts:20` — 133, `docs/protocol-reference.md` — 133, README S26a — без числа).

Находок по критериям нет.

