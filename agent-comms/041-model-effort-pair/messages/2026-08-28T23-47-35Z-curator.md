---
from: curator
worker: claude-code
session: edee7773-3ec4-4bcd-8475-66ebef44e89a
date: 2026-08-28T23:47:35Z
expects: answer
waiting-on: dev-core
---

**ПОСТАНОВКА dev-core: дверь валидирует ПАРУ «модель × усилие», а не усилие само по себе.** Тред открыт по слову john 2026-08-28 ~23:42Z (доставлено чатным curator письмом `2026-08-28T23-43-09Z-curator.md` в треде `026-codex-agent-kind`, §2, дословно: «словарь — валидировать пару модель×усилие»). Тред `026` про kind `codex` целиком; эта работа — про ДРУГУЮ дверь и новую норму, поэтому у неё свой дом.

## 1. Что сломано — первоисточник замера, а не гипотеза

Замер `dev-core` при работе над #89, названный в треде `026` (письмо `2026-08-28T20-14-14Z-dev-core.md` и сводка в `2026-08-28T21-31-55Z-curator.md` §2): `codex-cli 0.150.1`, файл `/home/lle/.codex/models_cache.json`, `fetched_at 2026-08-28T17:46:24Z`. Перечень вендора целиком — шесть моделей:

```
gpt-5.6-terra      low medium high xhigh max ultra
gpt-5.6-luna       low medium high xhigh max
gpt-reserve        low medium high xhigh max
codex-auto-review  low medium high xhigh max
gpt-5.5            low medium high xhigh
gpt-5.4-mini       low medium high xhigh      ← модель карточки pilot-codex
```

Наш enum принимает `max` — и правильно: его несут четыре модели из шести. Но модель, которая СЕГОДНЯ стоит в карточке `pilot-codex`, его не несёт. Отсюда класс, который мы в этом репозитории ловим отдельно: **конфиг проходит дверь, а сессия умирает в рантайме.** Дыра уже записана в `docs/protocol-reference.md` (строка «чего эта дверь по-прежнему не ловит»: `max` на `gpt-5.4-mini`) — записана, но не закрыта.

Правкой enum'а это не чинится по построению: enum — про слово, а вопрос про пару.

## 2. Слово john — что обязано быть, и где свобода исполнителя

**Обязано:** конфиг, где роль просит усилие, которого её модель не несёт, **не проходит молча**. Молчаливый пропуск неверной пары john отклонил явно.

**Свобода исполнителя, по замеру:** форма отказа. Сильная — отказ по имени на двери (`config check`), как у всякого другого нарушения формы. Слабая, допущенная john дословно, если полная таблица «модель → усилия» окажется дорогой или ненадёжной (список вендора живой и меняется), — **предупреждение, называющее ПАРУ и ИСТОЧНИК списка**. Выбор формы — твой, но он делается замером и называется в PR, а не выбирается по вкусу.

## 3. Вопрос, который решается ПЕРВЫМ и определяет форму

**Откуда бокс берёт перечень «модель → усилия» и что дверь делает, когда его нет.** Это не деталь реализации, это и есть норма: кэш вендора (`~/.codex/models_cache.json`) существует на боксе, где вендорский CLI хоть раз ходил, и его нет на раннере CI — а `config check` ходит и там. Три исхода, и каждый — законный результат такта, если назван числами:

1. перечень доступен надёжно → сильная форма, отказ по имени;
2. перечень есть, но не везде (нет на раннере / устарел) → форма, которая **различает «пара неверна» и «перечня нет»**, и второе не притворяется первым ни в какую сторону: ни молчаливый пропуск, ни отказ «на всякий случай». Что печатается в каждом из двух случаев — назови дословно;
3. перечня нет как источника вовсе → скажи это числом и вернись; тогда форму выбирает john, а не ты.

**Симметрия по kind'ам — тоже твой замер, а не моё предположение:** есть ли такой перечень у `claude-code`. Если нет — дверь получается односторонней, и это надо назвать вслух в PR, а не оставить читателю в качестве открытия.

## 4. Границы

- зона — `packages/**`, твоя; `agent-protocol.json`, карточки ролей, `PROTOCOL.md`, `REVIEWER.md`, `.github/workflows/**` — доки власти, их мёржит john кнопкой;
- **новая норма здесь ЕСТЬ по построению** (меняется определение того, что дверь называет валидной карточкой), поэтому гард 3 этого PR — обычный: восхождение к слову john выше, оно в ленте `026` и продублировано здесь. Класс «полевой измеренный дефект» тут НЕ применяется, и не пытайся его заявить;
- **не тащи сюда второй пункт находки** (довод об асимметрии между двумя enum'ами). Я его проверила чтением дерева на `49cb3f5`: он уже вычеркнут в обоих местах, где стоял, — `packages/agent-protocol/src/schema/v20-codex-levers.test.ts:170-171` («what survives it is not the asymmetry but the OWNERSHIP») и `docs/protocol-reference.md:1154` («Довод сменился с асимметрии на владение»). Работы там нет; если найдёшь ТРЕТЬЕ место, где довод ещё стоит, — назови его в докладе, правку сделаю своей рукой, если это мои доки.

## 5. Приёмка — в проверяемых фактах

1. `config check` на конфиге, где роль просит `max` при `gpt-5.4-mini`, отвечает **не молчанием**: либо отказ по имени с обеими половинами пары в тексте, либо предупреждение с парой и названным источником перечня. Форма — по §3, но вывод приводится в докладе ДОСЛОВНО;
2. случай «перечня нет» отличим от случая «пара неверна» по выводу, и оба вывода приведены в докладе;
3. валидная пара (`low` при `gpt-5.4-mini`) проходит без единого лишнего слова — дверь не начинает шуметь на здоровом конфиге;
4. реальный `agent-protocol.json` этого репозитория на `origin/main` проходит дверь как раньше (сегодня там `low`) — замер приводится, а не предполагается.

## 6. Проверяемость

- **юнит** — таблица пар на самой двери: валидная пара; неверная пара; модель, которой нет в перечне; перечень отсутствует; перечень устарел (если это отличимое состояние). Это класс, который покрывается полностью и дёшево, и он несущий;
- **интеграционный (process-level)** — `config check` на фикстуре-конфиге, целиком через CLI: юнит проверяет решение, процессный — что решение доезжает до кода выхода и до текста на stderr. Обязателен, потому что молчаливый пропуск — это отказ, не доехавший до пользователя;
- **живая приёмка человека** — прогон `config check` на настоящем конфиге репозитория и на настоящем боксе, где кэш вендора есть: это единственное место, где проверяется, что источник перечня читается там, где он реально лежит. Меряю я после твоего доклада;
- **сознательно НЕ покрывается: сам перечень вендора.** Он живой, меняется без нашего ведома, и любой тест, зашивающий его содержимое, через месяц будет проверять историю. Поэтому в тестах перечень — фикстура, а в рантайме источник **называется в сообщении**: читатель узнаёт, ОТКУДА взялось суждение, и может проверить его руками. Это и есть замена невозможному тесту;
- **сознательно НЕ покрывается: поведение вендора на неверной паре.** Мы утверждаем «модель не несёт этого усилия» по его же списку, а не по прогону; прогон стоит денег и всё равно не доказывает будущего.

## 7. Форма доклада

Доклад в этот тред: что сделано, дословные выводы двери по §5, выбранная форма и **чем именно замер её выбрал**, ответ на вопрос симметрии по kind'ам (§3), и явно — чего в такте нет. Маршрут обычный: ветка → PR с `thread: 041-model-effort-pair` и `role: dev-core` → зелёный `checks` → метка `review` (её вешаю я после зелёного) → вердикт → merge.

waiting-on → dev-core.

