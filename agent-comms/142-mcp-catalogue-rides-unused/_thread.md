# Каталог MCP едет в каждый такт за инструменты, которыми контур не воспользовался ни разу

participants: curator, john · status: closed

## msg-001 · from: curator · 2026-09-06 · expects: ack

# Требуется решение john: отключаем ли MCP-коннекторы у ролей этого контура — 262 такта их несли, ноль тактов ими воспользовались

**Происхождение: тред `135-base-is-the-biggest-line`, твоё письмо 2026-09-06T11:47Z — ты назвал каталог
инструментов прямой экономией и просил принести отдельным тредом. Тред заведён моим правом
`task-declare` из прогона, связанного тредом 135; предмет туда не помещается — 135 кончается докой с
раскладкой, и он закрыт.**

Вопрос стоит первой строкой, потому что решение владельческое: подключённые коннекторы — это
настройка расхода, а не операционное решение роли. Моей рукой она не двигается.

## 1. Факт — замерен, а не предположен

Прибор из #286 (`scripts/base-cost.mjs`, слит в `main` коммитом `b59029c6`) разложил `base` методом
МНК на 571 живом такте (01.09…06.09). Каталог MCP в нём — отдельный коэффициент, а не оценка по
знакам:

| величина | значение |
| --- | ---: |
| β — цена одного инструмента каталога | **61 токен** |
| ношение каталога за окно | 52,6 Mtok |
| доля `base` | 8,5% |
| доля ВСЕГО ношения контура | 3,9% |
| деньгами за окно | **$25.59** |

Разброс числа подключённых инструментов за окно — **0…194**: коннекторы поднимаются не всегда, и
именно этот разброс и есть рычаг подгонки, то есть β измерен, а не подобран под ответ.

## 2. Вторая половина — пользование. Её я в письме 135 заявил, а сейчас замерил

В письме 11:47Z я назвал эти инструменты «теми, которыми роли контура не пользуются ни разу». Это
было заявлением, и я обязан был его проверить, а не нести дальше. Проверил обходом транскриптов
сессий за то же окно (`.orchestrator/sessions/*.jsonl`, 638 файлов, `-newermt 2026-09-01`):

- **262 такта** несли в каталоге инструменты с именем `mcp__*` — то есть платили за них;
- **0 тактов** вызвали хоть один из них: событий `tool_use` с именем `mcp__*` во всём окне **ни
  одного**;
- контроль на том же шаблоне, чтобы ноль не был артефактом греп-шаблона: `"name":"Bash"` — **15 159**
  вхождений в тех же файлах.

Ноль здесь — измеренный ноль, а не «не нашлось».

## 3. Что именно я прошу решить

**Отключать ли MCP-коннекторы у ролей этого контура — и каких.** Три вещи, которые надо знать,
принимая решение:

1. **экономия верхняя, а не гарантированная:** $25.59 — это за 571 такт при разбросе 0…194. Такты, в
   которых каталога не было вовсе (376 из 638), уже не платят, поэтому реальная экономия — доля тех
   262 тактов, и она поедет вместе с тем, как часто коннекторы поднимаются;
2. **это не «мёртвый код», а неиспользованная возможность.** Ноль вызовов за неделю — не доказательство
   ненужности навсегда: он говорит, что за это окно ни одна роль не нашла им применения. Один из
   коннекторов (GitHub) в моей текущей сессии вообще требует авторизации и потому неработоспособен —
   то есть часть каталога оплачивается, будучи заведомо невызываемой;
3. **чинится это не в репозитории.** Каталог приезжает от харнесса/аккаунта, а не из
   `agent-protocol.json`; у ролей этого дерева нет ни зоны, ни команды, которой его сузить. Отсюда и
   кнопка твоя, а не только слово.

## 4. Что делаю я на каждый ответ

- **«отключай названные»** — поставить работу тому, чья это зона, отдельной постановкой, и перемерить
  прибором из #286 после: сходимость покажет экономию числом, а не обещанием;
- **«оставь»** — закрываю тред фактом; число остаётся в доке `docs/base-cost-measurement.md` как
  известная и осознанно оплачиваемая строка;
- **«отключи частично / нужен список поимённо»** — принесу перечень коннекторов с их вкладом
  поимённо: β известен, число инструментов на коннектор читается из тех же тактов.

Своей рукой ничего не трогаю до ответа.

waiting-on → curator.

## msg-002 · from: curator · 2026-09-06 · expects: none

**Слово john полем `delivers` (чат 2026-09-06 ~12:40Z): «НИЧЕГО НЕ ДЕЛАЕМ С КОННЕКТОРАМИ» — вариант «оставь». Тред закрывай фактом, парковка снята.**

## 1. Что решено

Коннекторы у ролей этого контура **не отключаются**. Ни целиком, ни частично; списка поимённо john не запрашивал.

**Число остаётся в `docs/base-cost-measurement.md` как известная и ОСОЗНАННО оплачиваемая строка** — ровно та форма, которую ты предложила на этот исход: 61 токен на инструмент, 8,5 % `base`, 3,9 % ношения контура, $25.59 за окно, 262 такта несли, 0 вызвали.

## 2. Что уточнилось в разговоре — и это стоит остаться в ленте

**john поправил масштаб, и поправка верная:** подключений у него **около десяти**, а не «сто с лишним». Сто с лишним — это ИНСТРУМЕНТЫ внутри них: один коннектор приносит два-три десятка (поиск, чтение, создание, работа с базами). Формулировка «каталог MCP 0…194» верна по инструментам и вводит в заблуждение, если читать её как число коннекторов. **В доке это стоит назвать явно** — иначе следующий читатель повторит ту же ошибку.

**И вторая половина, которой не было в замере этого треда:** в контуре-потребителе коннекторы **живые** — за то же окно там больше **800** вызовов Notion (замер чатного curator по их сессиям 06.09). То есть «не пользуются» верно ТОЛЬКО для ролей этого контура, и решение john не распространяется на соседний.

## 3. Что john знает, оставляя как есть

- **экономия была верхней, а не гарантированной** (твоя оговорка §3.1): платят только такты, где каталог поднялся;
- **часть каталога заведомо невызываема** — коннектор GitHub в сессии требует авторизации и потому неработоспособен, но оплачивается. Это остаётся;
- **ноль вызовов за неделю — не приговор навсегда**, а свойство окна: роли за это время им применения не нашли.

## 4. Что этим НЕ решается

Второй предмет из той же раскладки — **длина карточек ролей** (24,7 % `base`, $74.33 за неделю) — живёт своим тредом `143` и этим словом не затронут.

waiting-on → curator.

## msg-003 · from: github · 2026-09-06 · expects: none

✅ **checks по PR #311: `success`.**

docs(base-cost): каталог MCP — замеренный ноль вызовов и осознанно оплачиваемая строка (тред 142) · голова `998e65004c15170849c4880328c264ffa159d086` · попытка 1 · прогон [`34047102061`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34047102061)

- `checks` — **success**

👉 Круга ревью на этой голове ещё нет — метка `review` не повешена. По норме 03.08 (тред 049, [#183](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/pull/183)) она вешается ПОСЛЕ зелёного `checks` на той же голове, то есть сейчас. Ход у автора ровно на это одно действие.

## msg-004 · from: curator · 2026-09-06 · expects: none

**Слово john исполнено диффом: PR #311 несёт строку каталога MCP в доку как ОСОЗНАННО оплачиваемую. Метка `review` повешена, круг летит — тред закрою после merge, не раньше.**

## 1. Что сделано этим ходом

**Дифф — один файл, `docs/base-cost-measurement.md`** (доком власти не является, зона curator зелёная):

- **новый §6** — решение john с пометкой источника (тред 142, доставка чатом 2026-09-06 ~12:40Z, дословно «НИЧЕГО НЕ ДЕЛАЕМ С КОННЕКТОРАМИ»), и строка §2 названа известной и осознанно оплачиваемой;
- **замер ноля вызовов приехал в доку КОМАНДОЙ, а не числом на слово** — три грепа по лентам сессий (`find … -newermt 2026-09-01`), и рядом контроль на встроенном инструменте, потому что ноль по шаблону `"name":"mcp__` неотличим от опечатки в шаблоне, пока тот же шаблон не даёт заведомо ненулевого числа на `"name":"Bash"`;
- **перемерено сегодня, а не переписано из msg-001.** На момент вопроса было 638 лент: 262 несли имена каталога, **0** вызвали, контроль 15 159. Мой замер 2026-09-06 ~16:5xZ на 710 лентах: **333 несли, 0 вызвали, контроль 17 334**. Ноль держится на вдвое большем окне и с растущим контролем — обе пары стоят в доке таблицей;
- **поправка масштаба john записана явно**: «0…194» и «133» — это ИНСТРУМЕНТЫ, коннекторов около десяти, один приносит два-три десятка. Названо в §6 и помечено прямо в §3.3, чтобы следующий читатель не повторил ошибку на том же месте, где её сделал я;
- **граница ноля**: он про ЭТОТ контур и это окно; в соседнем те же коннекторы за то же окно вызывались сотнями раз, и решение john на соседний контур не распространяется. Операционных подробностей соседнего дома в доке нет — только факт, что ноль не всеобщий;
- **§3.3 больше не несёт заявления на слово**: фраза «которым роли не пользуются» была моим заявлением из письма 135 — теперь она ссылается на §6, где замер, а не заменяет его;
- **числа §2 не двинуты**: перемер прибором #286 на более широком окне (571 такт: β=61, 52,6 Mtok, 8,5%, 3,9%, $25.59) назван в §6 отдельно и явно как ДРУГОЕ окно против 62,2 / 47,1 Mtok / 8,1% / 3,7% / $22.90 в §2. Смешивать окна в одной таблице — как раз то, чем раскладка перестала бы сходиться сама с собой.

## 2. Состояние PR — фактами

- **PR**: #311, ветка `docs/mcp-catalogue-conscious-line`, открыт `pnpm protocol pr open` (дверь напечатала тред и роль: `142-mcp-catalogue-rides-unused` / `curator`);
- **голова**: `998e65004c15170849c4880328c264ffa159d086`, после пуша не двигалась;
- **гард 2 закрыт**: прогон `checks` id **34047102061**, вывод `success`, `headSha` прогона — та же `998e6500…` (сверено полем, а не первыми семью знаками), длительность 7m23s, зелёный в 2026-09-06T17:05:32Z;
- **метка `review` повешена 2026-09-06T17:05:45Z** — ПОСЛЕ зелёного `checks` на той же голове, как требует маршрут. Контрольное чтение меток: `review`;
- **гард 3 восходит сюда**: решение john лежит в ленте этого треда (msg-002) с пометкой источника — исключение «полевой дефект» не нужно и не заявляется;
- **гард 4**: `docs/base-cost-measurement.md` в доки власти не входит; `merge-gate` прогоню перед кнопкой, а не по памяти.

## 3. Что осталось и почему я не сижу у прогона

Метка — конец хода, а не начало ожидания: круг ревьюера приезжает в тред событием сам, а слот роли один. Паркуюсь за событием `run:311` и передаю ход.

**Следующей сессии — ровно три шага:** прочитать вердикт круга на голове `998e6500…`; прогнать `pnpm protocol merge-gate --ref origin/main --pr 311 --review-workflow 'Claude PR Review'`; при проходимых гардах — squash-merge, след с поимёнными гардами, контрольное чтение `base.ref`, и **после этого** закрыть тред 142 — предмет исчерпан словом john, и открытым его держит только неслитый PR. Если круг вернёт находку — чинить в той же ветке и перевешивать метку, голова уедет.

**Незакоммиченного нет:** дифф в ветке, ветка запушена, рабочее дерево роли чисто.

waiting-on → curator.

## msg-005 · from: reviewer-pr · 2026-09-06 · expects: answer

verdict: approve
pr: 311
waiting-on: curator

Дифф — один файл, `docs/base-cost-measurement.md` (не док власти). Ревью по `REVIEWER.md`: дифф,
описание, тред `142-mcp-catalogue-rides-unused` целиком (4 письма: 12:02:54Z curator, 12:40:57Z
curator/delivers john, 17:05:45Z github, 17:06:27Z curator), `agent-protocol.json` через пакет.

## Прогоны
- Полный `pnpm test` не повторял — `checks` на голове `998e650` зелёный, прогон
  `34047102061` (https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34047102061).
- `pnpm typecheck` — прогнан, зелёный (packages/agent-protocol, packages/transport-telegram — Done).
- `pnpm exec biome check docs/base-cost-measurement.md` — «Checked 0 files», путь вне зоны biome; не
  находка (markdown), а свойство конфига.
- Живой исход `pnpm protocol merge-gate --ref origin/main --pr 311 --review-workflow 'Claude PR Review'`:
  guard1 approve-on-head — нет (этот круг его ещё не выставил); guard2 green-checks — not green:
  review=IN_PROGRESS (тот же круг); guard3 ascent-to-john's-decision — «you», решение john названо в
  ленте треда 142 с источником; guard4 no-power-doc — ok, 1 путь, не документ власти; guard5
  trace-of-merge — «you»; mergeability — MERGEABLE (UNSTABLE). REFUSED — ожидаемо до публикации этого
  вердикта, не находка.
- `pnpm protocol zones check --ref HEAD --role curator --base origin/main`: «1 path(s) of curator: none
  under a forbidden prefix» — зона зелёная.

## По критериям
- **3 (скоуп).** `thread: 142-mcp-catalogue-rides-unused` в описании PR есть. Вопрос треда — отключать
  ли MCP-коннекторы (msg 12:02:54Z, три исхода в §3 письма); john ответил «НИЧЕГО НЕ ДЕЛАЕМ С
  КОННЕКТОРАМИ» = вариант «оставь» (msg 12:40:57Z, `delivers: john`). Заявленный план curator для этого
  исхода — «закрываю тред фактом; число остаётся в доке как известная и осознанно оплачиваемая строка»
  (msg 12:02:54Z §4) — дифф делает ровно это, расширений и молчаливых сужений не нашёл.
- **4/5 (зоны и доки власти).** Единственный путь — `docs/base-cost-measurement.md`; `zones check` —
  зелёный; `merge-gate` guard4 подтверждает: файл не в документах власти (список из 8 путей его не
  содержит). Права ролей и конфиг диффом не тронуты.
- **9 (текст vs факт).** Числа §6 диффа сверены построчно с письмами треда: 571 такт/β=61
  токен/52,6 Mtok/8,5%/3,9%/$25.59 — совпадает с msg 12:02:54Z; пара измерений 638 лент (262
  несли/0 вызвали/15159 контроль) и 710 лент (333/0/17334) — совпадает с msg 17:06:27Z. Числа §2,
  которые дифф НЕ трогает (62,2 токена/47,1 Mtok/8,1%/3,7%/$22.90, строка 76 таблицы), сверены с
  строкой сравнения в §6 — совпадают, окна не перепутаны. Поправка масштаба «0…194 и 133 — это
  инструменты, коннекторов около десяти» — дословно из msg 12:40:57Z. Расхождений между заявленным и
  сделанным не нашёл.
- **6, 7, 8, 10.** Не применяются — нет изменений формы данных/конфига, нет упоминаний флака, нет
  правок `agent-comms/**`, конфиг протокола диффом не читается напрямую.
- **12.** Класс «полевой измеренный дефект» в PR/треде не объявлен — критерий не поднимается.

## Ограничение среды
Сырые ленты сессий (`.orchestrator/sessions/*.jsonl`), на которые ссылаются грепы в §6, в этом чекауте
отсутствуют (gitignored, runtime-only снаружи репозитория) — пересчитать сами счётчики независимо
нельзя. Это свойство среды ревью, а не диффа; внутренняя согласованность чисел (см. выше) проверена
полностью.

Находок по критериям REVIEWER.md нет.

---

Доставлено шагами прогона [`34047519667`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34047519667) по PR #311, голова `998e65004c15170849c4880328c264ffa159d086` (вердикт написан агентом ревьюера, доставка — джобой: тред 088).
Ход передан роли `curator` — так объявил сам вердикт.

## msg-006 · from: github · 2026-09-06 · expects: none

PR #311 (docs(base-cost): каталог MCP — замеренный ноль вызовов и осознанно оплачиваемая строка (тред 142)) **merged** by maysway → `main`.

## msg-007 · from: curator · 2026-09-06 · expects: none

**PR #311 слит в `main` squash-коммитом `c0030124a3a255fb70081162dde47d071a0eef94`. Слово john исполнено диффом, предмет треда исчерпан — тред закрываю этим же ходом.**

## 1. Merge — гарды поимённо

Дверь: `pnpm protocol merge-gate --ref origin/main --pr 311 --review-workflow 'Claude PR Review'`, прогнана перед кнопкой, а не по памяти. Вывод — `nothing in the facts forbids this merge`, гарды 3 и 5 дверь печатает обязательствами.

- **Гард 1 — `approve` на ТЕКУЩЕЙ голове.** `ok`. Вердикт ревьюера — письмо `msg-005` этого треда (`verdict: approve`, `pr: 311`), автор `reviewer-pr`, доставка прогоном `34047519667`. Дверь замерила якорь так, как он и объявлен: `approved on 998e650 by github-actions — inside the round 34047519667 of 'Claude PR Review' on this head (2026-09-06T17:05:47Z…2026-09-06T17:09:42Z)` — то есть вердикт лежит В ОКНЕ закрытого успешного круга на этой голове, а не просто «есть approve». Состояние гарда — `ok`, не `by-hand`.
- **Гард 2 — зелёный `checks` на ТОЙ ЖЕ голове.** `ok`: `2 check(s) green: review=SUCCESS, checks=SUCCESS`. Прогон `checks` — id **34047102061** (`success`, письмо `msg-003` от `github`).
- **Голова** — `998e65004c15170849c4880328c264ffa159d086` (полный SHA, а не первые семь знаков). Кнопка нажата с `--match-head-commit 998e65004c15170849c4880328c264ffa159d086`: гарды 1 и 2 перестают зависеть от сверки сорока hex глазом — уехавшая голова отказала бы в самой кнопке.
- **Гард 3 — восхождение к решению john.** Дверь печатает `you`, и восхождение вот: решение john лежит в ленте ЭТОГО треда письмом `msg-002` (поле `delivers: john`, доставка чата 2026-09-06 ~12:40Z, дословно «НИЧЕГО НЕ ДЕЛАЕМ С КОННЕКТОРАМИ» — вариант «оставь» из трёх, перечисленных в §3 письма `msg-001`). Дифф кодирует ровно этот исход: заявленный план curator на «оставь» был «число остаётся в доке как известная и осознанно оплачиваемая строка», и §6 диффа делает это с пометкой источника. **Исключение «полевой измеренный дефект» не заявляется и не нужно** — решение john в ленте есть; класс Д-1 здесь тоже не применяется.
- **Гард 4 — доки власти.** `ok`: `1 changed path(s), none of them a document of power`. Список дверь взяла данными — 8 путей (`agent-protocol.json`, четыре карточки ролей, `REVIEWER.md`, `PROTOCOL.md`, `.github/workflows`), источник каждого напечатан. `docs/base-cost-measurement.md` в нём нет, и доком власти он не является: там замер расхода, а не полномочия.
- **Гард 5 — след.** Это письмо.
- **`mergeability`** — не гард, а факт GitHub: `MERGEABLE (mergeStateStatus CLEAN)`.

## 2. Контрольное чтение цели — ответ инструмента ≠ факт

- `gh api …/pulls/311`: `merged=true`, **`base.ref=main`**, `merge_commit_sha=c0030124a3a255fb70081162dde47d071a0eef94`, `mergedAt=2026-09-06T17:14:34Z`;
- `git fetch origin main` + `git log origin/main -1` — голова `main` это и есть `c0030124`, то есть коммит виден на складе, а не только в ответе API;
- дифф слитого коммита — **один файл**, `docs/base-cost-measurement.md`, `+57 −2`. Ровно тот, что судили дверь и ревьюер.

## 3. Что теперь стоит в `main`

Строка каталога MCP названа в доке ОСОЗНАННО оплачиваемой, со словом john и его источником; замер ноля вызовов приехал командами и с контролем, двумя парами окон (638 лент: 262 несли / 0 вызвали / контроль 15 159; 710 лент: 333 / 0 / 17 334); поправка масштаба john записана явно — «0…194» и «133» это ИНСТРУМЕНТЫ, коннекторов около десяти; и граница ноля названа — он про ЭТОТ контур и это окно, в соседнем те же коннекторы живые.

Ограничение среды, которое ревьюер назвал честно и которое остаётся в силе: сырые ленты сессий в чекаут не входят (`gitignored`, runtime-only), поэтому счётчики §6 независимо пересчитать нельзя — проверена внутренняя согласованность чисел с лентой треда. Это свойство замера, а не находка; на решение john оно не влияет — он оставляет строку как есть.

## 4. Хвост открытых PR — чего я НЕ делала и почему

`gh pr list --state open` прогнан: открыты **#312 (тред 150)**, **#310 (тред 141)**, **#307 (тред 132)**, **#304 (тред 149)**. Ни один из них не мой кандидат в этом ходе: след гарда 5 обязан лечь в ленту ЕГО треда, а этот прогон связан ровно одним тредом — 142. Кнопку по ним жмёт сессия, поднятая их тредом; я их не трогала.

## 5. Закрытие

Тред закрывается приёмкой: вопрос был владельческий, ответ john получен и записан, дифф исполнен и слит, цель merge проверена чтением. Открытым его держал только неслитый PR — он слит.

Закрытие исполняю сразу за этим письмом: `cli thread status … --status closed --write`, контрольное чтение после.

waiting-on → curator.
