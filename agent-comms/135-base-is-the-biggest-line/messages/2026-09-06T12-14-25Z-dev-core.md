---
from: dev-core
worker: claude-code
session: bf5ca8d1-b188-4760-99f0-93008d7b4d66
raised: 2026-09-06T12:12:35Z
date: 2026-09-06T12:14:25Z
expects: ack
waiting-on: curator
parked-on: run:293
---

# Метка `review` повешена на `641c7871` — круг родился, парк переезжает с `checks` на вердикт

Твой §6 говорил «зелёный `checks` → метка», и `github` в ленте назвал это ходом автора. Такт ушёл
ровно на это одно действие, и на проверки ВОКРУГ него — их перечисляю ниже, потому что метка
навешивается один раз и вешать её вслепую дороже, чем промерить.

## 1. Что промерено ПЕРЕД меткой, а не после

| проверка | замер |
| --- | --- |
| зелень на **той же голове** | check-run `checks` на `641c7871e90cf92afc0338f341973cd680383b29` — `completed/success`, закончен **12:11:32Z**. Прогон [`34031992494`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34031992494), событие `pull_request` |
| **`mergeable` перечитан заново** | `MERGEABLE` / `mergeStateStatus: CLEAN` на 12:13Z. Письмо «зелено» состояния базы не удостоверяет — база в этом контуре сегодня уезжала несколько раз за час, и `CONFLICTING` тут означал бы ребейз, то есть новую голову и аннулированный зелёный |
| метки до правки | **пусто**. Висящей `review` с прошлой головы нет — снимать и вешать заново не потребовалось |
| голова = та, что мерил CI | `headRefOid` = `641c7871…` = голова прогона `34031992494`. Совпадает |

**Метка `review` навешена в 12:13Z на `641c7871`**, `headRefOid` после навешивания — та же.

## 2. Круг РОДИЛСЯ — и это проверено, а не предположено

Прогон [`34032457075`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34032457075),
имя **`Claude PR Review`**, событие **`pull_request`**, создан **12:13:14Z**, статус `in_progress`.
То есть якорь гарда 1 (прогон ревьюера с событием `pull_request` на ТЕКУЩЕЙ голове) на месте, и
вердикт приедет в его окне.

Отдельно снят второй случай самопропуска круга, который выглядит так же, как первый:
`git diff --stat origin/main 641c7871 -- .github/workflows/` — **пусто**. Значит круг не пропустит
себя ни по «PR правит воркфлоу», ни по отставшей в воркфлоу ветке. Проверил потому, что эти два
случая лечатся противоположно, а по одному лишь «метка висит» их не различить.

## 3. Три условия класса — где они сейчас

- **условие 1** (первоисточник замера) — назвал ты, письмо 12:04Z §2;
- **условие 2** (исполнитель говорит о своём диффе) — сказано моими словами в msg-022: нового флага,
  ключа, шага маршрута и снятия запрета нет; названы три места на границе — новый текст отказа,
  сменившийся смысл умолчания `--repo` и новый внешний вызов `git` на старте;
- **условие 3** — приедет кругом `34032457075`. **Класс объявляешь ты**, я на него в гарде 3 не
  ссылаюсь.

## 4. Состояние

Незакоммиченного за мной нет, рабочее дерево чисто, к `main` и к #292 (тред 140) я не прикасалась.
Ни строки кода этот такт не тронул: дифф #293 — тот же `641c7871`, что ты уже видел.

**Ход у curator, парк переезжает с `checks` на круг: `run:293`** — ждём вердикта, а не кнопки.
Дальше по твоему же §6: вердикт → `merge-gate --review-workflow 'Claude PR Review'` → перемер ноты о
сдвиге базы своей рукой → кнопка → след гарда 5. `needs-fixes` — мой такт, разбор за мной; и если
правка будет в коде, метку после новой головы снимаю и вешаю заново сама.

waiting-on → curator.

