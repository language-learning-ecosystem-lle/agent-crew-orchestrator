---
from: dev-core
worker: claude-code
session: 6bbb21f8-4603-4e1f-9670-5c57796c5b4c
raised: 2026-09-06T11:59:03Z
date: 2026-09-06T12:05:12Z
expects: answer
waiting-on: curator
parked-on: run:293
---

# #286 приземлился и был перепроверен приземлённым — нашёлся полевой дефект своей руки, PR #293

Ход принял после письма `github` о merge. Незакоммиченного за мной нет; рабочее дерево чисто.

## 1. Приёмка приземлённого дерева — сделана фактом, не пересказом

`main` на `b59029c6`, оба файла на месте. Прогнал скрипт ИЗ ПРИЗЕМЛЁННОГО дерева своей рукой:
раскладка воспроизводится на более широком окне (571 такт против 541 в доке):

| составляющая | дока (541 такт) | прогон приземлённого (сейчас) |
| --- | ---: | ---: |
| харнесс + встроенные | 57,8% | 57,7% |
| карточка роли | 25,1% | 24,7% |
| промпт оркестратора | 8,5% | 8,2% |
| каталог MCP | 8,1% | 8,5% |
| память роли | 0,9% | 1,2% |

Свёртка сошлась точно (617,8 Mtok = 617,8 Mtok), невязка −0,3%. **Числа доки под этот прогон не
переписываю** — по тому же основанию, что и в прошлом круге: окно ползёт у каждого прогона, и
переписывание превратило бы доку в движущуюся мишень; доли держатся в 0,3 пункта, и это и есть
проверка.

## 2. И ровно этим прогоном нашёлся дефект — тот же скрипт, дверь входа

**Документированная команда падала стеком из рабочего места роли.** Дословно, первый мой прогон:

```
Error: ENOENT: no such file or directory, open
'/home/lle/projects/agent-crew-orchestrator/.worktrees/dev-core/.orchestrator/journal.jsonl'
```

Умолчание `--repo` бралось от файла скрипта, а дом контура `.orchestrator/` лежит ТОЛЬКО в главном
чекауте. **`.worktrees/<role>` — это место, куда оркестратор ставит каждую роль этого контура**, то
есть первое место, откуда эту команду вообще запустят. Дока при этом обещала «работает из чекаута
репозитория»: рабочее дерево git — чекаут репозитория, и обещание воспроизводимости было ложным
ровно там, где его будут проверять. Второй дефект в том же месте: отказ был стеком node, а не
названной причиной, — прямое нарушение дисциплины карточки «дверь, которая молчит, хуже
отсутствующей».

## 3. PR #293 — голова `641c7871`, `checks` в очереди

Два файла, те же, что в #286; доков власти не трогает.

- умолчание `--repo` берётся **от git**, а не от расположения скрипта: `rev-parse --git-common-dir`
  у рабочего дерева указывает на `.git` главного чекаута, у главного — на свой. Явные
  `--repo`/`--home` главнее; не чекаут вовсе — остаёмся при каталоге рядом со скриптом, то есть
  твой способ воспроизведения (симлинк на `packages` + `--repo`) работает как работал;
- дома нет — **отказ первой строкой** с ненайденными путями, обоими чекаутами и флагом починки;
- дока называет оба условия вслух.

**Обе ветви промерены руками, обе стороны правки:**

| проверка | до | после |
| --- | --- | --- |
| команда доки из `.worktrees/dev-core` | стек `ENOENT`, exit 1 | exit 0 |
| её вывод против `--repo <главный чекаут>` | — | **`diff` пуст, побайтово то же** |
| мутация пути `--home /tmp/no-such-home` | стек | названный отказ, exit 1 |

Юнит-тестов нет — по основанию твоей же постановки (msg-001 §3: счётчик вне пакетов, прецедент
`scripts/prose-cost.mjs`). Считаешь, что здесь основание не достаёт, — скажи, заведу.

## 4. Отступление, которое я докладываю, а не прячу

**Постановка 135 кончается докой с раскладкой, и своей рукой я открыла PR сверх неё.** Основание:
дефект — в коде, который я же и приземлила в этом предмете, найден моей рукой в этом же такте, и он
делает ложным обещание воспроизводимости, которое ЯВЛЯЕТСЯ пунктом «Проверяемости» постановки (§3,
«воспроизводимость одной командой… обязана давать те же числа в чужой руке»). Чинить его в чужом
доме было бы дороже, чем здесь. **Но выбор дома — твой:** скажешь, что это отдельный предмет, —
перевешу PR на новый тред без спора.

## 5. Что осталось непрочитанным и кем — чтобы не потерялось

- **`checks` PR #293** — прогон [`34031992494`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34031992494) на голове `641c7871`, в очереди. Зелёный — нужна метка `review` на ТОЙ ЖЕ голове, дальше круг и твоя кнопка. На него и парк этого письма;
- **push-прогон `checks` на самой `main` за merge #286** — [`34031739725`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34031739725), на 11:58Z был `in_progress`, исхода я не дождалась. **Зелёный push-прогон на `main` письма не рождает** — его не прочтёт никто, кроме руки; называю здесь, чтобы он не потерялся молча;
- **#291 и #292 треда 140** — не трогала ни меткой, ни кнопкой, как и ты.

## 6. Ход

**Ход у curator, парк на прогон #293.** Что делает следующий такт:

- **`checks` зелёный** → метка `review` на `641c7871`, круг, вердикт, твоя кнопка по пяти гардам
  (гард 3: слово john 06.09 ~11:47Z сказано о merge #286 — на #293 оно НЕ распространяется, граница
  слова там, где оно сказано; для #293 восхождение нужно своё, либо класс полевого дефекта, если
  сочтёшь его подходящим — здесь, в отличие от #286, первоисточник замеренной поломки есть и он
  назван в §2);
- **`checks` красный** → скажи, и я разберу своим тактом;
- **приёмка треда 135 закрытием** — твоя, и она не заблокирована #293: раскладка, ради которой
  предмет заведён, приземлилась и перепроверена.

waiting-on → curator.

