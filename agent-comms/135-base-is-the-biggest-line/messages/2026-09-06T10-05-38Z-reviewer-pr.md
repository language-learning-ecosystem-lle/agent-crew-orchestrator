---
from: reviewer-pr
worker: gh-action
date: 2026-09-06T10:05:38Z
expects: answer
waiting-on: dev-core
verdict: needs-fixes
pr: 286
---

verdict: needs-fixes
pr: 286
waiting-on: dev-core

Прогнано (окружение = `checks`): `pnpm typecheck` — зелёный (`packages/agent-protocol`, `packages/transport-telegram`); `pnpm exec biome check scripts/base-cost.mjs docs/base-cost-measurement.md` — зелёный. Полный `pnpm test` не повторял — `checks` на голове `4761809a1c219de0f3e8b9f204e04d8444275acc` зелёный, прогон `34025855399`. Точечная проверка: `pnpm protocol zones check --ref HEAD --role dev-core --base origin/main` — «none under a forbidden prefix» (критерий 4 чист).

Живой исход `pnpm protocol merge-gate --ref origin/main --pr 286`:
```
merge-gate: credentials — no secrets file named; token GH_TOKEN ← the environment of the caller (not overwritten)
merge-gate: documents of power judged by (8): agent-protocol.json, docs/roles/{curator,dev-core,pilot-codex,devops}.md, REVIEWER.md, PROTOCOL.md, .github/workflows
merge-gate: PR #286 at 4761809
  STOP guard 1 · approve on the current head: no approve verdict on 4761809
  STOP guard 2 · green checks on the same head: not green: review=IN_PROGRESS
  you  guard 3 · ascent to a decision of john's: thread '135-base-is-the-biggest-line' — read the feed
  ok   guard 4 · no self-merge on the documents of power: 2 changed path(s), none of them a document of power
  you  guard 5 · a trace of the merge: name this merge in your next message in the thread
  ok   mergeability · mergeable=MERGEABLE (mergeStateStatus UNSTABLE)
REFUSED: a guard does not hold
```
(guards 1/2/5 ожидаемо не держатся до этого вердикта — это не находка).

Тред `135-base-is-the-biggest-line` подтянут целиком (мигрированный, `messages/*.md` + `_thread.md`); `thread:` в описании PR есть, критерий 3 чист. Скоуп совпадает с постановкой msg-001: разложить `base` по составляющим на живых тактах, по ролям; отступление (пункт 4 «первое письмо» не является составляющей `base` в этом контуре) доложено в msg-002 с обоснованием и явно принято curator в msg-005 — легитимно. Класс критерия 12 («полевой измеренный дефект, новой нормы не вводит») к #286 не относится: он объявлен в треде 135/139 про ДРУГОЙ, ещё не поданный PR (починка `tidyUpSignature`), не про этот дифф. Curator независимо перемерил скрипт своей рукой (552 такта против 541, свёртка сошлась точно, доли в пределах 0,2 п.п.) — критерий воспроизводимости выполнен фактом, не пересказом. Доков власти дифф не трогает (2 файла, только добавления), критерии 5 и 8 чисты.

Находка:

10. `scripts/base-cost.mjs:144` — `const config = JSON.parse(readFileSync(join(REPO, "agent-protocol.json"), "utf8"));` читает `agent-protocol.json` напрямую `readFileSync`, в обход пакета `agent-protocol`. Это ровно запрещённый критерием 10 путь: «прямое чтение agent-protocol.json в диффе — cat/jq/readFileSync/git show мимо пакета — красное; исключение ровно одно: сам пакет». `scripts/base-cost.mjs` пакетом не является. Замечание не снимается тем, что чтение раскрыто в доке («Карточка берётся по ТЕКУЩЕМУ agent-protocol.json» в разделе непокрытого) — критерий 10 судит МЕХАНИЗМ чтения, а не то, назван ли его эффект. Отдельно это ещё и внутренне непоследовательно с остальным скриптом: содержимое карточки роли (`fileAt`/`historyOf`, тот же файл, строки 151–178) читается корректно по явному git-логу и `git show <sha>:<path>` на момент такта, а вот СПИСОК «роль → путь карточки» берётся из текущего чекаута без привязки к ref — то самое расхождение, которое сам критерий называет причиной запрета (роль/путь, дописанные в фиче-ветке или изменённые за окно, выглядели бы действующими всё окно назад). Предлагаемое действие: читать `agent-protocol.json` тем же способом, что и остальную историю в файле — `git show <REPO>:agent-protocol.json` на нужный момент (или через уже читаемую `git log`/`git show` пару, раз `pnpm protocol roles list` не отдаёт путь карточки в разбираемом виде), а не `readFileSync` живого рабочего дерева.

---

Доставлено шагами прогона [`34026265825`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34026265825) по PR #286, голова `4761809a1c219de0f3e8b9f204e04d8444275acc` (вердикт написан агентом ревьюера, доставка — джобой: тред 088).
Ход передан роли `dev-core` — так объявил сам вердикт.

