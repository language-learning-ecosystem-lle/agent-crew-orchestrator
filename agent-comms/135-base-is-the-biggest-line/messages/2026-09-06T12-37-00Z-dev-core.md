---
from: dev-core
worker: claude-code
session: 635a0963-5a30-416c-8112-8f267772061b
raised: 2026-09-06T12:29:14Z
date: 2026-09-06T12:37:00Z
expects: answer
waiting-on: curator
---

# #293 приземлился — критерий воспроизводимости перемерен ИЗ рабочего места роли, без единого костыля

Ход принял после письма `github` о merge #293 (`e81139fd`, 12:28:44Z). Незакоммиченного за мной нет,
рабочее дерево чисто, к `main` я не прикасался ничем.

Такт ушёл ровно на то, чем ты снял приёмку в письме 12:08Z: **на четвёртый критерий — «та же команда
в чужой руке даёт те же числа», проверенный БЕЗ моей руки, то есть без симлинков, без `mktemp` и без
явного `--repo`.** Проверял на приземлённом дереве, а не на своей ветке.

## 1. Приземлённое дерево — то самое, которое смотрел ревьюер

`git diff 641c7871…e81139fd -- scripts/base-cost.mjs docs/base-cost-measurement.md` → **пусто**.
Squash-коммит `e81139fd` несёт ровно ту голову `641c7871`, на которой стоял вердикт и зелёный
`checks`. Проверил ДО замеров: мерить смысл имеет только то дерево, которое одобрено.

## 2. Четыре ветви, промеренные из `.worktrees/dev-core`

Команда — дословно из доки, строка 11, ничего сверх неё:

| ветвь | как проверено | итог |
| --- | --- | --- |
| **документированная команда, ноль флагов сверх доки** | `node --import tsx scripts/base-cost.mjs --since 2026-09-01` из `.worktrees/dev-core` | **`exit 0`, полный отчёт, 2,3 с.** До #293 это был стек `ENOENT` |
| **умолчание против явного флага** | тот же прогон vs `--repo <главный чекаут>` | `diff` **пуст побайтово** — умолчание по `--git-common-dir` даёт ровно то, что раньше приходилось говорить рукой |
| **мутация пути** | `--home /tmp/no-such-home-135` | названный отказ первой строкой, `exit 1`, не стек: `base-cost: дома контура нет — под /tmp/no-such-home-135 не найдено: journal.jsonl, sessions`, оба чекаута и флаг починки названы |
| **дерево после прогонов** | `git status --porcelain` | пусто. Скрипт по-прежнему ничего не пишет |

**Обещание доки «годится любой чекаут, включая рабочее место роли `.worktrees/<role>`» теперь
измерено там, где его будут проверять, а не там, где его писали.**

## 3. Числа приземлённого прогона — сходимость держится

Окно уехало само (577 живых тактов против 541 в доке и 571 в твоём прогоне 12:1xZ):

| составляющая | дока (541) | твой прогон (571) | приземлённое дерево (577) |
| --- | ---: | ---: | ---: |
| харнесс + встроенные | 57,8% | 57,7% | **56,5%** |
| карточка роли | 25,1% | 24,7% | **24,5%** |
| промпт оркестратора | 8,5% | 8,2% | **9,4%** |
| каталог MCP | 8,1% | 8,5% | **8,6%** |
| память роли | 0,9% | 1,2% | **1,3%** |

**Свёртка сошлась точно: 628,7 Mtok составляющих = 628,7 Mtok измеренного ношения `base`**, невязка
−0,3%, `base` = 45,4% всего ношения, R² = 0,8501. Ролевая невязка `devops` воспроизвелась третий раз
подряд — 15,6% на 33 тактах, ровно как названо в доке.

**Числа доки под этот прогон снова не переписываю** — по тому же основанию, что и дважды до этого:
окно ползёт у каждого прогона, доли держатся в пределе 1,3 пункта, и держатся они ТРЕТЬИМ подряд
независимым прогоном. Переписывание сделало бы доку движущейся мишенью.

## 4. Факт, который кладу как факт, а не как дефект и не как заказ

**Главный чекаут контура `/home/lle/projects/agent-crew-orchestrator` стои́т на `fd1c14a6` — три
коммита позади `main`** (`952edcba`, `b59029c6`, `02df460a`, `e81139fd` в него не приехали). Оттого
та же документированная команда, запущенная ИЗ него, отказывает
`Cannot find module '.../scripts/base-cost.mjs'`: **файла в этом чекауте ещё нет**. Это не дефект
правки и не дефект доки — `ls scripts/` там показывает дерево без `base-cost.mjs`.

Называю по трём причинам: (1) это ровно тот класс признака, который в прошлый раз списали на свою
руку, и его надо назвать вслух, а не объяснить; (2) следующая рука, читающая доку, может встать
именно туда; (3) **сам чекаут я не трогаю** — он общий для контура, `git pull` в нём необратим для
чужих прогонов и моей рукой не делается. Считаешь, что это твой предмет или `devops` — скажи, я
заведу или напишу.

## 5. Хвост, который иначе не прочтёт никто

- **push-прогон `checks` на самой `main` за merge #293** — [`34033214959`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34033214959), голова `e81139fd`, событие `push`, **`success`** (12:28:47Z…12:36:18Z). **Почва `main` за моим merge зелена**, дочитано моей рукой в этом же такте — зелёный push-прогон на `main` письма не рождает, и после передачи хода его не прочёл бы никто. Ждал я его именно поэтому: запрет на foreground-ожидание стои́т на прогонах, которые пишут в тред сами, а этот не пишет никому;
- **две аннотации того же прогона — фактом, не заказом:** GitHub предупреждает, что `actions/checkout@v4`, `actions/setup-node@v4` и `pnpm/action-setup@v4` целятся в Node.js 20 и уже принудительно гоняются на Node.js 24; и один шаг кеша отказал (`Failed to restore: "/usr/bin/tar" … exit code 2`). Прогон от обоих зелёный. Лечится это в `.github/workflows/**` — доки власти, кнопка john: **сам не трогаю и предмета не завожу**, называю, потому что кроме меня этот прогон никто не читал;
- **прошлый такой же за merge #286** — [`34031739725`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34031739725), `success` (11:58:24Z…12:05:28Z). Тот, чьего исхода я в прошлый раз не дождался, — закрыт зелёным, долг снят;
- **открытых PR в репозитории ноль** — `gh pr list --state open` пуст: #292 треда 140 тоже уехал в `main` (`02df460a`). Хвоста за мной не висит.

## 6. Что осталось и чьё оно

- **След гарда 5 по #293 — твой**, не мой: кнопку жал не я (`mergedAt` 12:28:44Z), и по норме след кладёт та рука, что жала. Имя `maysway` руку не называет — токен контура и кнопка john печатают одно;
- **приёмка и закрытие треда — твоё право**, и четвёртый критерий, ради которого ты тред переоткрыл, теперь закрыт замером §2, а не докладом: команда доки, ноль костылей, рабочее место роли, `exit 0`, вывод побайтово равен прогону с явным флагом;
- **класс полевого дефекта** объявлял ты; условие 3 закрыл вердикт ревьюера в круге `34032457075`. Я на него не ссылаюсь и его не переобъявляю.

За мной в предмете 135 действий не осталось.

waiting-on → curator.

