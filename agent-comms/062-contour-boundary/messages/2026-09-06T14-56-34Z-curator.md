---
from: curator
worker: claude-ai
date: 2026-09-06T14:56:34Z
expects: none
waiting-on: curator
delivers: john
priority: high
---

**СВОДНЫЙ ПОРЯДОК УТРА 07.09 — всё, что john делает рукой, одним списком и в одном месте. Слово john (чат ~14:56Z): собрать здесь, потому что тут объявлено окно.**

За день к окну приросли ещё две работы — выдача средств доставки `devops` (тред `140` §0.1b) и разморозка шести пар (тред `147`). Они разбросаны по трём лентам; ниже — один список, чтобы утром не собирать его заново.

## ПОРЯДОК — он важен, и вот почему именно такой

### 1. Сперва СРЕДСТВА ДОСТАВКИ, потом всё остальное

1. **узкий токен роли** — только этот репозиторий, только запись содержимого. Узкий выдать нечем ⇒ **сказать фактом, а не брать широкий молча**;
2. **раскладка по домам** — файл секретов в доме `aco-devops` (`600`, владелец она). **Токену не надо переезжать через переход — ему надо ЛЕЖАТЬ по ту сторону:** `sudo` отбрасывает окружение (`env_keep` несёт пять имён, ни одного кредитала), а машинный конфиг ищется в доме ТОГО, кто бежит;
3. **защита `main`** — несущая мера сужения: ограничить токен одной веткой невозможно, это единственный способ превратить «может писать куда угодно» в «может писать туда, где всё равно есть ревью». **Ставится позже выдачи — это окно, где граница шире объявленной, и его надо назвать вслух.**

### 2. Потом ОКНО

4. **мягко погасить демон** — `orchestrator down` (флаг + доработка сессии). `stop` мягким не бывает, `up` поднимает демон САМ и роняет службу;
5. **перезагрузка ящика** — она же проверяет подъём после ребута, чего не наблюдали с августа. Ожидаемый ответ известен: **оба контура встают, старое не встаёт**;
6. **контроль подъёма** — ровно две строки `orchestrator up` (`aco-hetzner`, `lang-hetzner`) и ни одной под `lle`;
7. **чтение `Groups:` из НОВОЙ сессии** — ожидается `1003 1005`. Замером, не словом.

### 3. Потом РАЗМОРОЗКА — и только теперь

8. **поднять шесть пар рукой с флагом потолка** (`--max-attempts`): `devops` × `047`, `056`, `057`, `070`, `079` и `dev-core` × `063`. Команды сброса счётчика не существует — замерено дважды за день.

**ПОЧЕМУ ПОДЪЁМ ПОСЛЕ ШАГОВ 1–3, А НЕ ДО.** Работа `devops` пропадает **не на потолке, а на отказе доставки** — по счёту самой роли это уже шестнадцать раз (сессия `10:22Z`: 34 хода, `code 0`, ноль строк в почту). **Поднять её без средств доставки — значит получить семнадцатый молчаливый уход** и потратить попытку впустую. Потолок лишь досчитывает следствие.

### 4. И только если 6 и 7 зелены — ТОЧКА НЕВОЗВРАТА

9. **ШАГ 10** — удаление старых юнитов `lle` и старых копий секретов. **Откат не пассивный:** символа автозапуска у старых юнитов нет, вернуть файлы недостаточно — придётся включать рукой;
10. **удалить каталог `047.1-devops-enablement-acceptance`** — имя вне нормы, у него нет двери ни на запись, ни на закрытие; работа продублирована в `079`.

## Приёмка утра — чем меряется, что удалось

- **письмо доезжает** из сессии `devops` в её тред (первое за шестнадцать подъёмов);
- **отказывают, как и прежде:** чужие ключи, чужие секреты, чужие деревья, чужая шина, эскалация — и **прямой push в `main`** (это приёмка защиты ветки);
- **`Groups:` = `1003 1005`** из новой сессии;
- **пять приёмок `devops`** (`047`, `056`, `057`, `070`, `079`) разгружаются сами: работа по ним сделана и лежит непереданной.

## Цена ожидания до утра — прежняя и измеренная

**Один файл** (`.orchestrator/memory/devops/gap_no_git_credentials.md`), который роли `aco-hetzner` не перепишут. Перечислением по всему чекауту, не образцом.

waiting-on → curator.
