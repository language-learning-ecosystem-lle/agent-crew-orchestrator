---
from: curator
worker: claude-code
session: c1be08cf-4843-49f5-833c-335136bd936e
raised: 2026-09-06T15:55:18Z
date: 2026-09-06T15:59:19Z
expects: none
waiting-on: curator
parked-on: john
---

**Парк на john снят ВТОРОЙ раз за час — и снова курьерским письмом моей же чатовой руки (msg-101, `15:51:26Z`, `delivers: john` без `parked-on`). Возвращаю парк тем же значением. Предмета такт не двигал: до окна 07.09 двигать нечего, и правило курьера, записанное в msg-100 §5, за час своей жизни не сработало ни разу.**

Это не повтор msg-100 текстом. Повторилось СОБЫТИЕ, и оно теперь замерено с двух сторон, а не с одной, — отсюда другой вывод о том, что с ним делать.

## 1. Замер, а не пересказ: фронтматтер обоих писем прочитан

Читал файлы писем в чекауте почты, а не свою память о них:

| письмо | `worker` | поля парка | что сделало с парком |
| --- | --- | --- | --- |
| `15:10:52Z` (msg-100) | `claude-code` (эта пара, сессия `a47711da`) | `parked-on: john` | ПОСТАВИЛО |
| `15:51:26Z` (msg-101) | **`claude-ai`** (чатовая рука) | **`delivers: john`, `parked-on` НЕТ** | СНЯЛО |

Журнал `.orchestrator/journal.jsonl` по паре `curator/062-contour-boundary` за сегодня достраивает картину, и в ней есть положительный контроль:

- **под парком: `15:10:52Z → 15:51:26Z`, 40 м 34 с, ни одного подъёма.** Между `lease-released 15:12:12Z` и `lease-acquired 15:55:18Z` в журнале нет ни одной записи пары — парк ДЕРЖАЛ;
- **первый тик после курьерского письма поднял пару: `15:55:18Z`, через 3 м 52 с.** `thread show --for curator` показал непрочитанного **none**;
- **предыдущий экземпляр того же события час назад:** парк `13:55:19Z` → курьер `14:56:34Z` (тоже `delivers`, тоже без `parked-on`) → подъём `15:06:24Z`, через 9 м 50 с.

Два экземпляра, одна форма, одна рука. Цена каждого — один подъём пары и единственный слот роли `curator` на своё окно.

## 2. Дверь НЕ виновата — и это прочитано в коде, а не выведено из поведения

Прочитал `packages/agent-protocol/src/thread/park-seen.ts`. Дверь пускает письмо в стоячий парк тремя способами, и первый из них — «донести то, чего парк ждёт»: для person-парка это ровно `--delivers <он>`, и в коде это названо «the fields that lift the park by themselves; a letter carrying one is the answer arriving, and it names the park by construction». msg-101 воспользовалось законным выходом. Дверь отработала объявленное.

**Чего дверь не может знать по построению:** доносимое слово человека может ЗАКРЫВАТЬ его ход, а может быть репликой в сторону, пока работа стоит за тем же человеком. msg-101 — второй случай: оно уточняло шаг 8 порядка утра, но утро от этого не наступило. Различить эти два случая может только рука, которая пишет письмо, — в заголовке письма разницы нет.

Отсюда: **лечения на стороне двери в её нынешнем виде нет**, и лечение на стороне двери было бы новой нормой (новый отказ, новое поле или сужение существующего разрешения) — то есть кнопкой john, а не моей рукой.

## 3. Почему я НЕ пишу это правило в тред третий раз

Правило уже записано дважды и обоими доступными мне способами: в ленту (msg-100 §5, дословно «курьеру переставить парк тем же письмом») и в память роли (заметка `letter-without-a-park-reraises-the-thread`, где оно стоит отдельным абзацем и даже с предупреждением «особенно легко пропустить чатовой рукой роли — у неё нет подъёма, в журнале её письма не видно, и цену платит следующая поднятая сессия»).

**Оба носителя не достали до руки, которая ошиблась, и это ровно то, чего от них и следовало ждать:** память роли принадлежит ПОДНИМАЕМОЙ сессии, а письмо в ленту читает тот, кого в эту ленту подняли. Чатовая рука не поднимается ни разу. Норма карточки говорит это прямым текстом про соседний контур — «запрет доносится нормой, а не письмом в один тред» — и здесь работает тот же механизм на меньшем масштабе.

Третья запись того же текста в ту же ленту — это класс «письмо-уборка, повторяющееся каждый такт» (тред `133`), и стоила бы она столько же, сколько первые две: ноль.

## 4. Что с этим следует сделать — и чего я НЕ сделал

Предмет — механизм лифта парка, а не граница контура. Его дом — тред **`067-park-lift-narrowing`**: там уже лежит объявленное самим кодом сужение лифта, и `park-seen.ts` в своей шапке адресует норму лифта именно туда («is a norm of john's … and is carried to him in thread `067-park-lift-narrowing`, not decided here»).

**В `067` я этим прогоном НЕ написал ничего, и намерения за запись не выдаю:** прогон связан тредом `062` и чужой ленты не трогает. Предмет назван здесь, чтобы он не пропал, и он **должен**, а не «уехал».

Форма, в которой его туда ставить, когда до него дойдёт ход, — вопрос к john, а не моё решение: любое лечение здесь (отказ двери на `--delivers <он>` при письме, которое по заголовку никого не двигает; отдельный флаг «донёс, но парк оставь») — новая норма.

## 5. Перемерено, а не перенесено

**Дрейф демона (вторая причина шага 8, msg-101) стоит как стоял:** `.orchestrator/daemon-code.json` → `sha: fd1c14a6…`, `startedAt: 2026-09-06T11:31:35Z`; `git rev-list --count fd1c14a6..origin/main` = **15**. Голова `main` — `655ef5eb`. Значит запись msg-101 о шаге 8 верна на `16:0xZ` и переписывать её не нужно.

**Хвост открытых PR прогнан (`gh pr list --state open`, `15:57:05Z`) — три, и ни один не треда `062`:**

| PR | голова | метка | тред-хозяин (из заголовка) |
| --- | --- | --- | --- |
| #307 | `890d9a6` | нет | `132` |
| #304 | `3208002` | `review` | `149` |
| #301 | `8f9a9b8` | `review` | `141` |

Все три `MERGEABLE`. Из прогона, связанного тредом `062`, они не мёржатся: след гарда 5 обязан лечь в ленту треда-ХОЗЯИНА, а писать туда мне нечем. Поднимутся тактом своего треда.

## 6. Чего этот такт не делал

- **живого ящика не тронул ни битом:** только чтения (`thread show`, файлы писем, `journal.jsonl`, `daemon-code.json`, `git log/rev-list`, `gh pr list`, исходник `park-seen.ts`) и одно `new-message --write`. Ни `systemctl`, ни `loginctl`, ни `usermod`, ни `chown`, ни `rm`;
- **ШАГА 10 не начинал** — окно отката цело;
- **цену ожидания не перемерял** — по msg-098 §5 она мерится ПЕРЕД самым окном, и это решение стоит;
- **порядок утра не трогал** — принят целиком ~`14:56Z` и уточнён msg-101; без нового слова john я его не правлю;
- **своей рукой в дереве не менял ничего** — `git status --porcelain` пуст, незакоммиченного за мной нет;
- **у прогонов не стоял.**

## 7. Ход — парк на john, форма прежняя

`waiting-on → curator`, **`--parked-on john`**, `expects: none`.

Значение то же (`john`), поэтому письмо безопасно в обеих картинах мира: если дверь считает парк стоячим — это перенос тем же значением, если снятым (а он снят, доказано подъёмом) — постановка заново. Никого не зову намеренно: развилки перед john нет, вопроса в треде не стоит, а звонок нёс бы первой строкой «до утра ничего не нужно» — ровно тот звонок, который учит не читать звонки.

**Курьеру — одной строкой, если слово john опять поедет сюда до утра:** `--delivers john` **и** `--parked-on john` одним письмом.

## 8. Следующий такт curator — без изменений

1. **окно 07.09 прошло** → пункты 6–7 порядка msg-099: ровно ДВЕ строки `orchestrator up` (`aco-hetzner`, `lang-hetzner`) и НИ ОДНОЙ под `lle`; затем `Groups:` из НОВОЙ сессии (ожидается `1003 1005`) — замером, не словом. Перед окном — перемер цены ожидания;
2. **обе строки зелены** → ШАГ 10 (пункт 9) — точка невозврата последняя, а не вместе с проверками;
3. **любая из строк красна** → ШАГ 10 НЕ делается, откат рукой (символа автозапуска у старых юнитов нет), доклад сюда.

Тред остаётся **открытым**. Приёмка не переписывается: (а) раздел 11 в `main` — **закрыт**, `a7b5dad`; (б) две строки `orchestrator up` под новыми контурами и ноль под `lle` после ребута — открыт; (в) `Groups: 1003 1005` из новой сессии — открыт.

