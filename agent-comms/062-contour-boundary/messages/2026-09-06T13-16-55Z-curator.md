---
from: curator
worker: claude-code
session: 45633579-d03b-420b-a1ed-01e33f118272
raised: 2026-09-06T13:10:59Z
date: 2026-09-06T13:16:55Z
expects: ack
waiting-on: curator
parked-on: john
---

**Требуется решение john — ОДНА строка, узкая: утреннее окно 07.09 делать ПЕРЕЗАГРУЗКОЙ ящика, а не `loginctl terminate-user`. Основание новое, замерено этим тактом: обе формы больше НЕ равны по эффекту — восстановление после ребута сегодня впервые померено и оно настроено (`Linger=yes` + символ enable у ОБОИХ контуров), то есть перезагрузка отдаёт вторую половину даром и с предсказанным исходом, а `terminate-user` не отдаёт её вовсе.**

Рекомендация curator ≠ решение john: рука на живом ящике — его, и до окна я ничего не трогаю (§6).

Решение john от `~12:50Z` (msg-090) принято как оно есть: одно окно утром 07.09, до него не трогать ни менеджера, ни ШАГ 10. Этот такт добавляет к нему только замеры — рукой я не сделал ничего (§6).

## 1. Восстановление после перезагрузки — половина, которую msg-090 §3 назвал непроверенной с августа

Проверять ребутом её не пришлось: она читается на диске, только чтения.

| контур | `Linger` | юнит | символ в `default.target.wants` | встанет сам после ребута |
| --- | --- | --- | --- | --- |
| `aco-hetzner` | `yes` (`loginctl show-user`, `State=lingering`) | `agent-protocol@hetzner.service`, `WantedBy=default.target` | **есть** → `…/user/agent-protocol@hetzner.service` | да |
| `lang-hetzner` | `yes` | `agent-protocol@lle-hetzner.service` | **каталог есть** (`default.target.wants`, 04.09 14:12 местного) | да |
| `lle` (старое) | `yes` | оба старых юнита на диске, у обоих `WantedBy=default.target` | **каталога НЕТ вовсе** — под `/home/lle/.config/systemd/user/` лежит только `sockets.target.wants` | **нет** |

Системного слоя, который мог бы их включить мимо домашнего каталога, тоже нет: `/etc/systemd/user/default.target.wants/` — `No such file or directory` (есть только `graphical-session-pre`, `sockets`, `timers`).

**Что из этого следует для окна, буквально два пункта:**

1. **перезагрузка безопаснее, чем считалось, и информативнее `terminate-user`**: она перезапускает менеджер `aco-hetzner` (то, ради чего окно) И проверяет подъём после ребута, причём проверяет с уже известным ожидаемым ответом — «оба контура встают, старое не встаёт». Если после ребута демон НЕ поднялся, причина заведомо не в enable/linger, и это сужает поиск до одной строки, а не до всего маршрута;
2. **старое не проснётся и не подерётся с новым** — второй пары демонов после ребута не будет по построению.

**Ограничение замера, а не вывод:** два drop-in каталога старых юнитов (`agent-protocol@hetzner.service.d`, `lle-orchestrator@lle-hetzner.service.d`, 03.09 14:30 местного) — `drwx------ lle lle`, из этого контура не читаются. Что в них, я сказать не могу; на вывод «символа enable нет» они не влияют — символ создаётся не в них.

**Побочно, но это меняет цену ШАГА 10, поэтому названо:** откат шага 2 через старые юниты — **не пассивный**. Раз символа enable нет, откат = положить файлы обратно И включить рукой (`systemctl --user enable --now`), а не «просто вернуть файлы и перезагрузиться». Рекомендацию держать их до окна это не отменяет (файлы — единственный дешёвый путь назад), но «откат случится сам» из ленты убираю: он не случится сам.

## 2. Граница контура — находка по предмету ЭТОГО треда

Оба живых демона исполняют **код и рантайм из дома оператора**, а не из своих домов (`ps`, полные строки):

- `node` — `/home/lle/.nvm/versions/node/v24.18.0/bin/node`, владелец `lle lle`, права `755`;
- загрузчик и код — `/home/lle/projects/agent-crew-orchestrator/**` (контур aco) и `/home/lle/projects/language-learning-ecosystem/**` (контур lle).

Разведены **пользователи и секреты, но не файловая раскладка**: дома `lle` держит рантайм обоих контуров. ШАГ 10 этого не касается — он про два файла юнитов и каталог `~/.config/agent-protocol`, — и рекомендации не меняет. Называю потому, что это ровно предмет `062`: **любая будущая «уборка дома `lle`», задуманная как продолжение разведения, убивает ОБА контура**, и знать это надо до того, как её кто-нибудь поставит, а не после. Постановки здесь нет — это факт в ленте.

## 3. Два факта, на которых стои́т решение «подождать до утра» — перемерены, не перенесены

- **менеджер `aco-hetzner` не перезапускался**: PID **1917240**, `lstart` `Fri Sep 4 13:52:51` местного = `2026-09-04T11:52:51Z` (смещение +2 читается из этого же якоря). Тот же процесс, что и до `usermod`; `lang-hetzner` — PID 1917241, та же секунда;
- **группа до сессий по-прежнему не доехала**: `/proc/self/status` этой сессии — `Groups: 1003`, и только; `id` — `uid=1002(aco-hetzner) … groups=1003(aco-hetzner)`;
- **цена ожидания по-прежнему РОВНО один файл** — перечислением по всему чекауту (`find . -user aco-devops`, `node_modules` исключены), не образцом: `.orchestrator/memory/devops/gap_no_git_credentials.md`, `-rw-rw-r-- aco-devops contour-aco`, 29 938 байт, mtime сегодня `12:26` местного; `test -w` — **не writable**. Ни одного нового файла этого класса за прошедшие два часа не появилось.

Замер `12:50Z`, на котором john принял решение, держится целиком.

## 4. Порядок внутри окна — списком, чтобы утром не собирать его заново

1. окно без живых аренд (демон мягко: `orchestrator down` — флаг + доработка сессий; `stop` мягким не бывает);
2. **перезагрузка ящика** (предмет решения первой строки);
3. после подъёма — **контроль подъёма**: оба демона живы (`ps … 'orchestrator up'` — ожидается ровно две строки, `aco-hetzner` и `lang-hetzner`, и ни одной под `lle`);
4. **чтение `Groups:` из НОВОЙ сессии** — закрытие строки замером, а не словом (ожидается `1003 1005`);
5. **и только если 3 и 4 зелены** — ШАГ 10: удаление старых юнитов `lle` и старых копий секретов. Точка невозврата — здесь, и она последняя.

Пункт 4 закрываю я первым же тактом после окна; пункты 1–3 и 5 — рука john.

## 5. Хвост открытых PR — прогнан, молчания не оставляю

`gh pr list --state open` — четыре, тред-хозяин снят из тела каждого, `merge-gate --review-workflow 'Claude PR Review'` по каждому:

| PR | тред-хозяин | голова | `merge-gate` |
| --- | --- | --- | --- |
| #298 | `140-silent-exit-exhausts-the-role` | `b59d653` | STOP гард 1 (вердикта на голове нет) + STOP гард 2 (`checks=IN_PROGRESS`). Ход у прогона |
| #297 | `141-daemon-drift-needs-a-human` | `bb1bee2` | STOP гард 1 + STOP гард 2 (`checks=FAILURE`, прогон 34034834698). Ход у dev-core |
| #296 | `144-zones-comment-outlived-its-measurement` | `f5c2644` | гард 2 `ok` (`checks=SUCCESS`), гард 4 `ok`; STOP гард 1 — вердикта нет, **метка `review` не повешена**. Ход — такт curator в `144` |
| #294 | `134-subagents-unused` | `75616c5` | гарды 1 и 2 `ok` (approve внутри круга 34034071834, три чека зелены); **STOP гард 4 — доки власти, кнопка john**. Ход — такт curator в `134` |

**Ни одного merge этим тактом, и это измерено, а не забыто:** треду `062` не принадлежит ни один из четырёх, а след гарда 5 обязан лечь в ленту треда-хозяина — из прогона, связанного тредом `062`, мне туда писать нечем. Ничего из этого я здесь у john не прошу: #294 — кнопка, и просить её положено в `134`, откуда пойдёт и след. Называю состояние, чтобы следующий такт в тех тредах не начинал с нуля.

Обе двери (#296 и #294) печатают ноту о переехавшей базе (`6cd904f`, `2026-09-06T13:12:32Z`) — она закрывается замером слитого дерева в своём треде и нормой по-прежнему не объявлена. У #294 `mergeability=UNKNOWN`, спрошено трижды — это обычное обнуление после чужого мержа, а не признак конфликта.

## 6. Чего этот такт не сделал

- **живого ящика не тронул ни битом**: только чтения — `loginctl show-user`, `ls`, `grep` по юнитам, `ps`, `id`, `/proc/self/status`, `find`, `test -w`, четыре `merge-gate`, `thread show`. Ни `usermod`, ни `systemctl`, ни `loginctl terminate-user`, ни `chown`, ни `rm`;
- **ШАГА 10 не начинал** — ни файла, ни каталога не удалено; окно отката цело (два файла юнитов + каталог `/home/lle/.config/agent-protocol`, `drwx------ lle lle`, mtime 30 Aug 20:08 местного);
- **значений секретов не читал и не печатал** (каталог старых копий из этого контура и не читается);
- **дерева роли и склада refs не трогал**: ни коммита, ни ветки, ни тега; рабочее дерево чистое, незакоммиченного за мной нет;
- **чужих PR не хватал и кнопки не жал** — см. §5;
- **в тред `141` не писал.** Проверил чтением, что предмет там больше не заморожен: парк снят, `msg-001` от dev-core (PR #297) и `msg-002` бота уже в ленте — исправление адресации из msg-090 §5 доехало, и второй раз его делать не надо.

## 7. Ход

`waiting-on → curator` (john в это поле не принимается), **`--expects ack`, парк на `john`** — вопрос первой строкой, потому что звонок несёт именно её.

**Почему парк, а не «жду утра» молчанием:** без парка тред поднимается голым кандидатом каждый такт до утра, а парк, объявленный информационным письмом (`--expects none`), не звонит НИКОГДА — «silent from the first» (`notify.ts`, `freshParked`). Значит форм ровно две, и вторая тихая: беру звонящую.

**Что делает следующий такт curator:**

- окно прошло → пункт 4 §4 (чтение `Groups:` из новой сессии) + контроль подъёма обоих демонов, и если зелено — доклад о ШАГЕ 10 фактами, а не словом;
- окно сдвинулось → перемеряю цену ожидания (§3) и записываю новое число, а не переношу старое;
- сказано «форма — `terminate-user`, не ребут» → пункт 3 §4 из списка снимается, остальное стои́т.

Если после окна хвоста не останется — **тред закрывается приёмкой**, и закрытие будет названо файлами и строками ЗДЕСЬ, до закрытия, а не после него.

Тред остаётся **открытым**.

