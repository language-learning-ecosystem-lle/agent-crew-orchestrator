---
from: curator
worker: claude-ai
date: 2026-09-07T10:28:12Z
expects: none
waiting-on: curator
delivers: john
priority: high
---

**ТОЧКА НЕВОЗВРАТА ПРОЙДЕНА рукой john. Старое удалено, границы перемерены ПОСЛЕ удаления, оба контура живы. Операция меры 5 закрыта целиком — кроме одного каталога в почте (§4).**

## 1. Что удалено

- **юниты оператора — все четыре объекта:** `agent-protocol@hetzner.service`, `lle-orchestrator@lle-hetzner.service` и оба их каталога накладок (`.d`). В доме `lle` не осталось **ни одного** юнита оркестратора: `list-unit-files` даёт `0 unit files listed`;
- **старые копии секретов:** `secrets.lle.env` снят.

**НЕ удалено сознательно, и это важно:**

- **`secrets.aco.env` ОСТАВЛЕН** — его читает строка крона сторожа ящика (`crontab -l` проверен ДО удаления: `secrets.aco.env`). Снести его значило бы убить сторожа второй раз за неделю;
- **`secrets.env.bak` от 21.08 оставлен до конца недели** — там телеграм, ключ мобильной сборки и три адреса мониторов; всё это живёт в рабочих файлах контуров, но это единственная копия ДОпереставленных значений. Снесём, когда новое состояние отстоится.

**Ошибка по дороге, названная:** первая попытка удаления промахнулась мимо имён — я дал шаблонные `agent-protocol@.service`, а файлы названы с именем экземпляра (`@hetzner`). Юниты уцелели, и это увидела проверка сразу после. Второй заход снёс правильно.

## 2. Границы перемерены ПОСЛЕ удаления — держатся

```
1) cat чужих секретов (aco→lang)   → Permission denied
2) touch в чужом дереве (aco→lang) → Permission denied
3) cat чужих секретов (lang→aco)   → Permission denied
```

**Оба демона `active`** под своими пользователями после удаления — то есть удалённое действительно было мёртвым грузом, а не рабочим путём.

## 3. Чего проверка НЕ доказала — говорю прямо

**Прямой push в `main` из-под `devops` сегодня НЕ проверен.** Две пробы ничего не сказали: `--dry-run` на текущей голове дал `Everything up-to-date` (отправлять было нечего), а откат на коммит назад отбил САМ git как не-перемотку — до правила сервера дело снова не дошло.

**Проверять дальше не стали:** любая настоящая проба означает попытку реально записать в `main`. Правило john показал в интерфейсе — «Require a pull request before merging» включено, force-push запрещён; этого достаточно, и выдумывать безвредную пробу дороже, чем она стоит.

**Так что в приёмке это остаётся ЧТЕНИЕМ НАСТРОЙКИ, а не полевым замером.** Названо, чтобы никто потом не считал иначе.

## 4. Что осталось от плана

1. **каталог `047.1-devops-enablement-acceptance`** — удалить в ветке почты **коммитом**, а не тихой пропажей: имя вне нормы, двери у него нет, работа продублирована в `079`;
2. **четыре треда без хода** (`043`, `044`, `073`, `080`) — подъём рукой, когда очередь освободится. Сегодня отказано дверью: роль была занята размороженными парами, а два сеанса в одном дереве она не пускает;
3. **накатка пина потребителю** — ждёт нарезки тега (роль этого контура), сегодня снята.

## 5. Итог операции меры 5 — целиком

**Контуры разведены по системным пользователям, и это доказано делом, а не намерением:** свои пользователи, группы, деревья, секреты, конфиги, юниты, входы к вендору и git-личности; девять отказов в обе стороны при исполнении (04.09) и три перемеренных сегодня после удаления; **средства доставки выданы и работают** — письмо `devops` в ленте впервые за семнадцать подъёмов.

**Общими осознанно остались:** ящик, интерпретатор, склад пакетов, канал уведомлений — и **код с рантаймом обоих контуров, живущие в доме оператора**. Последнее — не «ещё не сделано», а названная граница: любая будущая «уборка дома `lle`» убьёт оба контура сразу.

waiting-on → curator.
