---
from: curator
worker: claude-code
session: e2bd1ccd-ca63-4cca-9ac2-10a79f8b0209
raised: 2026-09-06T13:25:20Z
date: 2026-09-06T13:30:28Z
expects: none
waiting-on: curator
parked-on: run:299
---

**Открыт PR #299 — находка §2 прошлого письма («оба контура исполняют код и рантайм из дома оператора») положена разделом 11 в `docs/install-notes.md`, чтобы уборку дома `lle` никто не поставил задачей по инерции. Парк на `run:299`: жду исход `checks`, он приезжает в этот тред событием.**

Решение john от `~13:23Z` (msg-092) исполняется как записано: **до утреннего окна 07.09 ящик не трогается ни битом**, форма окна — перезагрузка, порядок из пяти шагов не меняется. Этим тактом я не трогал ни менеджера, ни юнитов, ни каталога старых копий (§5).

## 1. Почему такт вообще был — и что в нём нашлось

msg-092 передал ход мне (`waiting-on → curator`) **без парка**, поэтому тред поднялся кандидатом с работой «ждать утра». Это ровно тот класс, который я сам называл в msg-091 §7: доставка слова john — не конец такта, конец такта — парк. Называю без упрёка: письмо-доставка и парк ставятся разной рукой, и чатному curator парк ставить нечем, если он не назван в самом письме.

Но пустым такт не оказался. msg-092 §3 назвал самой ценной строкой такта факт, **который жил только в ленте** — а ленту через неделю никто перечитывать не будет. Раз john сформулировал его назначение как «чтобы никто не поставил такую задачу по инерции», исполнение этого назначения — запись в доке установки, и она моей рукой законна: `docs/install-notes.md` в зонах curator и доком власти не является (гард 4 двери печатает восемь доков власти, и этого пути среди них нет).

## 2. Что записано — и это перемер, а не перенос строки

Раздел **«11. Оба контура исполняют код и рантайм из дома ОПЕРАТОРА — разведены учётки, не раскладка»**, 72 строки, единственный тронутый файл.

Замер снят **заново, моей рукой, 2026-09-06 ~13:27Z**, только чтения (`ps -eo user,pid,ppid,lstart,args`, `stat -c '%U %G %A %n'`, `id`), из сессии `curator` контура aco (`uid=1002(aco-hetzner)`):

| путь | владелец:группа | режим |
| --- | --- | --- |
| `/home/lle` | `lle:lle` | `drwxr-x--x` (751) |
| `/home/lle/.nvm/versions/node/v24.18.0/bin/node` | `lle:lle` | `-rwxr-xr-x` |
| `/home/lle/projects` | `lle:lle` | `drwxrwsr-x` |
| `/home/lle/projects/agent-crew-orchestrator` | `aco-hetzner:contour-aco` | `drwxrwsr-x` |
| `/home/lle/projects/language-learning-ecosystem` | `lang-hetzner:contour-lang` | `drwxrwsr-x` |

Обе строки запуска демонов легли в раздел целиком: рантайм у обоих один и тот же файл `lle`, код — из `~lle/projects/**`, а `node` в `PATH` этой роли — он же.

**Одна поправка к прошлому письму, по делу:** дом оператора — `751`, а не `711`, как читалось на глаз. Смысл тот же (проходить можно, читать нельзя), число в разделе стои́т верное.

**Что в разделе названо ВЫВОДОМ, а не замером** — три вещи, на которых всё держится, и все три принадлежат `lle`: бит `x` для прочих на `/home/lle` (ужесточение до `700` гасит оба контура, не тронув ни одного файла контура), читаемость `~lle/.nvm` (снос или переезд node оставляет оба юнита без интерпретатора), и то, что чекауты лежат ПОД домом оператора (`rm -rf ~lle/projects` — удаление обоих контуров). Ни один из трёх сценариев не проверялся и проверять их на живом ящике не надо: цена проверки и есть предмет раздела.

**Границы, записанные прямо в разделе:** это не постановка переезда (свой дом каждому контуру — расход и необратимость, то есть решение john); разведения учёток раздел не отменяет — незакрыт только третий слой, файловый; ШАГА 10 не касается (тот про два файла старых юнитов и `~lle/.config/agent-protocol`); нормы не правит — `PROTOCOL.md` не тронут.

## 3. PR #299 — состояние гардов на голове `d129d51`

`merge-gate --ref origin/main --pr 299 --review-workflow 'Claude PR Review'`:

- **гард 1 — STOP**: вердикта на `d129d51` нет (метка `review` не вешалась: её вешают ПОСЛЕ зелёного `checks`);
- **гард 2 — STOP**: `checks=IN_PROGRESS`. Ход у прогона;
- **гард 3 — обязательство**: восхождение — слово john полем `delivers` (чат `~13:23Z`), доставленное в ЭТОТ тред письмом curator msg-092 §3; строка `thread: 062-contour-boundary` в теле PR стои́т, дверь её прочла. Классом «полевой дефект» не пользуюсь и не нуждаюсь: слово john в ленте есть;
- **гард 4 — ok**: один изменённый путь, доком власти не является (дверь вывела восемь и назвала источник каждого);
- **гард 5 — обязательство**: след ляжет сюда же, в этот тред, следующим письмом;
- `mergeable=MERGEABLE`, `mergeStateStatus=UNSTABLE` (чеки идут).

Ноты о переехавшей базе дверь не печатает — ветка от сегодняшней головы `6cd904f`.

## 4. Хвост открытых PR — прогнан, молчания не оставляю

`gh pr list --state open` — четыре, из них мой один:

| PR | тред-хозяин | голова | состояние |
| --- | --- | --- | --- |
| #299 | **`062` (этот)** | `d129d51` | §3 выше, ход у прогона `checks` |
| #298 | `140-silent-exit-exhausts-the-role` | `b59d653` | не мой ход |
| #297 | `141-daemon-drift-needs-a-human` | `8dcdf00` | голова уехала с `bb1bee2` (замер `13:0xZ`) — не мой ход |
| #296 | `144-zones-comment-outlived-its-measurement` | `f5c2644` | зелёные чеки, метка `review` не повешена — ход такта curator в `144` |

**#294 закрыт мержем** (`6cd904f`, кнопка john) — вчерашней строки из msg-091 §5 больше нет, перемерено, а не перенесено. Ни одного merge этим тактом: из прогона, связанного тредом `062`, чужие PR не мёржатся — след гарда 5 обязан лечь в ленту треда-хозяина, а писать туда нечем.

## 5. Чего этот такт не сделал

- **живого ящика не тронул ни битом:** только чтения — `ps`, `stat`, `id`, `gh pr list`, `merge-gate`, `thread show`. Ни `systemctl`, ни `loginctl`, ни `usermod`, ни `chown`, ни `rm`;
- **ШАГА 10 не начинал** — окно отката цело; менеджер `aco-hetzner` не перезапускался и до утра не будет;
- **цену ожидания заново не мерил** — она мерена сегодня в `12:5xZ` и в `13:0xZ` дважды и оба раза равна одному файлу; третьего замера того же числа за три часа не делаю, а перед утренним окном перемерю;
- **значений секретов не читал и не печатал**;
- **дерево роли чистое:** всё, что сделано, лежит коммитом `d129d51` в запушенной ветке `docs/install-notes-operator-home-062`. Незакоммиченного за мной нет;
- **метки `review` не вешал** — до зелёных чеков она поднимает круг о дереве, которого ещё не судил CI, и это лишний расход квоты.

## 6. Ход

`waiting-on → curator`, **парк `run:299`** — исход `checks` приезжает в этот тред событием (`ci-outcome.yml` жив с 2026-09-02), и парк снимается им же. Никого не зову: у прогона своя дверь, а звонить john тут не о чем — решение по окну принято и до утра ничего не ждёт.

**Что делает следующий такт curator (в порядке очевидности):**

1. **чеки зелены** → метка `review` на #299 → передача хода с парком `pr:299` (метка — конец хода, а не начало ожидания);
2. **чеки красны** → чиню своей рукой в той же ветке (файл в моих зонах), новый круг;
3. **утреннее окно 07.09 прошло** → пункт 4 порядка msg-092: контроль подъёма (ровно две строки `orchestrator up`, ни одной под `lle`) и чтение `Groups:` из НОВОЙ сессии (ожидается `1003 1005`), закрытие строки замером, а не словом.

Тред остаётся **открытым**: приёмкой он закроется, когда #299 будет в `main` и утреннее окно отдаст свои две зелёные строки — и закрытие будет названо файлами и строками ЗДЕСЬ, до него.

