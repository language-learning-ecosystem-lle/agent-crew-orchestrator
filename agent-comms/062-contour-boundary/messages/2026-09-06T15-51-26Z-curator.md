---
from: curator
worker: claude-ai
date: 2026-09-06T15:51:26Z
expects: none
waiting-on: curator
delivers: john
priority: high
---

**ДОПИСКА К ПОРЯДКУ УТРА (письмо `14:56:34Z`): у шага 8 появилась ВТОРАЯ независимая причина стоять после перезагрузки. Порядок не меняется — меняется то, что о нём известно.**

## Шаг 8 (разморозка шести пар) идёт после шага 5 (перезагрузка) по ДВУМ причинам

**Первая, прежняя:** поднимать `devops` до выдачи средств доставки бессмысленно — сессия отработает и снова не сможет написать. Это будет семнадцатый молчаливый уход и потраченная попытка.

**Вторая, найденная замером curator в треде `150` (`15:47:45Z`):** команда `thaw` влита (#306, squash `655ef5eb`), но **демон исполняет старый код** (`fd1c14a6` от `11:31:35Z`, дрейф 15 коммитов, самопочинка заблокирована живыми сессиями). Старый разбор журнала на **незнакомый тип события отказывает ГРОМКО** — это его собственный комментарий, приведённый дословно: «молча проглоченная строка спрятала бы ровно тот сбой, ради которого журнал и существует». Разбор строгий, по типу события; типа `thaw` в старом коде нет.

**Значит:** `thaw` при живом старом демоне допишет в журнал строку, которую **работающий читатель не примет**. После перезагрузки вопрос снимается сам — в новом коде тип объявлен.

**Соблазн, от которого это предостерегает:** «сделаю разморозку сразу, пока помню» — до перезагрузки. Не делать.

## Граница замера — названа, чтобы не читалось шире

Куда именно приходит этот отказ — валит ли он тик, весь процесс, или ловится выше — **НЕ измерено**, и curator это не выдаёт за факт. Установлено ровно одно: **старый читатель строку не принимает**. Для порядка шагов этого достаточно.

## Порядок утра целиком — без изменений

1–3 средства доставки (узкий токен → раскладка по домам → защита `main`) · 4 мягко погасить (`down`) · **5 перезагрузка** · 6 контроль подъёма (две строки, ни одной под `lle`) · 7 чтение `Groups:` из новой сессии · **8 разморозка шести пар** (`--max-attempts`, либо `orchestrator thaw`, если код уже новый) · 9–10 точка невозврата (старые юниты, копии секретов, каталог `047.1`).

**Новое в шаге 8:** после перезагрузки демон будет на коде с #306, значит доступна штатная команда `thaw` — и разморозка делается ею, а не обходным флагом. Обходной флаг остаётся запасным путём, если что-то не поднялось.

waitings-on → curator.
