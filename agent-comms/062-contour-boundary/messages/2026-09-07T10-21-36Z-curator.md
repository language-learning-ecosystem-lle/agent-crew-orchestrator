---
from: curator
worker: claude-ai
date: 2026-09-07T10:21:36Z
expects: none
waiting-on: curator
delivers: john
priority: high
---

**ОПЕРАЦИЯ УТРА 07.09 ИСПОЛНЕНА рукой john, ~11:00–13:20 местного. Шаги 1–8 закрыты, приёмка средств доставки НАСТУПИЛА ЖИВЬЁМ. Ниже — что сделано, три находки, которых в плане не было, и что осталось.**

## 1. Что сделано — по шагам плана

| шаг | исход |
|---|---|
| 1 узкий токен | **готово** — fine-grained, только этот репозиторий, только `Contents: RW`, конечный срок |
| 2 раскладка по домам | **готово** — `/home/aco-devops/.config/agent-protocol/secrets.env`, `600`, владелец роль |
| 3 защита `main` | **уже стояла** — правило «Require a pull request before merging», force-push запрещён. Окна между выдачей и защитой НЕ БЫЛО: токен выдан в уже защищённый репозиторий |
| 4 мягкое гашение | **готово** — `down` на обоих контурах, дождались нуля процессов |
| 5 перезагрузка | **готово** |
| 6 контроль подъёма | **готово** — по одному демону у `aco-hetzner` и `lang-hetzner`, **ноль под `lle`** |
| 7 группы из новой сессии | **готово** — `aco-devops`: `1005 contour-aco` + `1006 contour-lang`; `aco-hetzner`: только `1005`; `lang-hetzner`: только `1006`. Ровно как задумано |
| 7-бис приёмка самопочинки | **готово** — `up` при живом демоне ОТКАЗАЛ, назвал pid и что делать, второго демона не поднял, служба осталась `active` |
| Н накатка пина | **СНЯТА** — тега ещё нет, PR никто не готовил (§4) |
| 8 разморозка | **готово** — шесть пар штатной командой `thaw`, каждая со следом `by john` и причиной |
| 8-бис четыре треда без хода | **отложено** — роль занята размороженными (§4) |
| 9–10 точка невозврата | **осталось** (§4) |

## 2. ПРИЁМКА СРЕДСТВ ДОСТАВКИ НАСТУПИЛА — впервые за семнадцать подъёмов

**Письмо `devops` лежит в ленте:** коммит `450836f0018015ec27ca2c039624f1aff206c08c`, автор `devops <devops@agents.invalid>`, тред `079-devops-enablement-acceptance`, `10:19:48Z`.

Все предыдущие шестнадцать раз роль отрабатывала полностью и не могла доставить. Трое суток мы читали это как «ушла молча» и завели предмет с НЕВЕРНЫМ диагнозом — он снят вчера, и вот его окончательное опровержение делом.

## 3. ТРИ НАХОДКИ, которых в плане не было

**(а) «Выдать роли токен» — это ТРИ действия, а не одно.** Файл секретов в её доме, настройка её `git`, и **вход `gh` под ней**. Третьего не было ни в постановке, ни в плане — а без него первые два бесполезны: наш `.gitconfig` говорит «за учётными данными иди к хелперу `gh`», а `gh` под этим пользователем не логинился ни разу. Первая же её сессия после выдачи токена умерла ровно на этом: `gh auth status shows not logged in`, аренда снята с `auth-failed`.

**Как удалось:** `gh auth login --with-token`, читая токен из файла и **сняв `GH_TOKEN` из окружения** — иначе `gh` видит переменную и отказывается класть вход в хранилище («To have GitHub CLI store credentials instead, first clear the value from the environment»). Проверка после: `git ls-remote` из-под роли **без токена в окружении вообще** — проходит.

**(б) Перезагрузка НЕ подтягивает главный чекаут.** Мы рассчитывали, что ребут снимет дрейф — он поднял демон на ВЧЕРАШНЕМ коде (`fd1c14a6`, тот же, что и до перезагрузки). Всё ночное лежало в `main` и не исполнялось; штатная команда `thaw` не находилась вовсе («not described in the usage block»). Подтягивание чекаута — **отдельный шаг**, и его надо вписать в порядок: `down` → `git pull` → установка → снять флаг файлом → поднять службой. После этого код `38c640c6`, и `thaw` заработала.

**(в) Установка зависимостей больше НЕ виснет.** Вчера та же команда требовала подтверждения на снос каталога; сегодня прошла за 619 мс (`Already up to date`). Разница — вчера каталог был чужим по владельцу после смены прав, сегодня свой. **Зависание было разовой ценой разведения, а не постоянной болезнью** — это надо поправить в описании порядка накатки (их тред `136`), иначе оно останется там как вечная страшилка.

## 4. Что осталось и почему

**Накатка пина снята с сегодняшнего окна:** тег с ночными починками **не нарезан**, PR с пином никто не готовил. Порядок остаётся прежним: роль этого контура режет тег → объявляет → роль потребителя открывает PR → кнопка john → накатка на ящике. Это несколько тактов, а не минуты.

**Четыре треда без хода (`043`, `044`, `073`, `080`) не подняты:** демон, увидев разморозку, поднял `curator` первым же тиком, и ручной подъём отказал — «два сеанса в одном дереве». Дверь права; мой блок был написан неверно, ручной подъём нельзя ставить сразу после разморозки. Делается, когда очередь опустеет.

**Точка невозврата (шаги 9–10) не сделана** — по слову john: сперва запись итога, потом удаление. Откат по-прежнему жив.

## 5. Приёмка операции — чем меряется

- **письмо `devops` в ленте** — есть (§2);
- **группы разведены** — есть (§1, шаг 7);
- **старые юниты не встали после ребута** — есть (§1, шаг 6);
- **отказ подъёма при живом демоне** — есть (§1, шаг 7-бис);
- **отказы границы** (чужие ключи, секреты, деревья, шина, эскалация) — **перемерить после точки невозврата**, вместе с шагом 9.

waiting-on → curator.
