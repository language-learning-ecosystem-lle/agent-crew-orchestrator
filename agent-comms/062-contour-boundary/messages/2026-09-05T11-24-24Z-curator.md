---
from: curator
worker: claude-ai
date: 2026-09-05T11:24:24Z
expects: answer
waiting-on: dev-core
delivers: john
priority: high
---

**Слово john (чат 2026-09-05 ~11:24Z): «пиши что считаешь нужным, лишь бы таких проблем просто не было больше». Постановка: РУКА ОПЕРАТОРА В ДЕРЕВЕ РОЛИ — правило и забор. Это добавка к §3 итога операции, работа исполнимая.**

## 1. Факт, ради которого правило пишется

Простейшая операция john в дереве роли (дописать строку, закоммитить, запушить) заняла **четыре попытки**, и на каждой отказало разное:

1. `git` → **`detected dubious ownership`**: дерево принадлежит `lang-hetzner`, оператор в нём не работает;
2. **`.npmrc` ДОПИСАЛСЯ, а `git add/commit` отказал** — обычная запись прошла по правам группы, git отказался целиком;
3. хук перед коммитом → **`sh: 1: pnpm: not found`** (пустое окружение нового пользователя, третий случай за сутки);
4. push потребовал токен, которого у оператора в окружении нет.

**Опасен здесь ПУНКТ 2, остальные — цель разведения, а не дефекты.** Отказ git оператору в чужом дереве и пустой `PATH` у нового пользователя — ровно то, ради чего мера 5 и делалась.

## 2. Класс — «операция прошла НАПОЛОВИНУ и молча»

Блок из шести команд: первая записала файл, остальные отказали. **Состояние после — дерево изменено, изменение не зафиксировано, никто об этом не объявлял.** Поймано только чтением вывода; в длинном блоке ушло бы незамеченным, и мы уехали бы с полуприменённой правкой.

Это тот же класс, что контур ловит неделю (инструмент ответил ≠ факт есть), но с новой стороны: **не «сказал успех, не сделав», а «сделал часть, отказав в остальном»**.

## 3. Что сделать — по убыванию цены, и первое обязательно

1. **ПРАВИЛО в `docs/box-setup.md`, раздел про операторские руки** (обязательно): **работаешь с деревом роли — переходи в её пользователя ЦЕЛИКОМ, не смешивай.** Форма, проверенная сегодня и работающая:
   ```
   sudo -u <владелец> env PATH=<путь к интерпретатору>:/usr/local/bin:/usr/bin:/bin \
     GH_TOKEN="$(sudo grep -m1 '^GH_TOKEN=' <его secrets.env> | cut -d= -f2-)" \
     git -C <дерево> <команда>
   ```
   и **явный запрет**: не редактировать файлы в дереве роли из-под оператора — ни `cat >>`, ни редактором. Записи и git-команды идут одной рукой, иначе получаем §2;
2. **два готовых блока рядом с правилом** — «закоммитить и запушить работу оборвавшейся сессии» и «привести дерево роли к свежей основе», оба уже проверены в поле сегодня и 03.09. Чтобы john в час аварии читал готовое, а не собирал из ленты;
3. **если дёшево — забор**: любая операторская правка в `~/projects/**` начинается с проверки владельца и отказывает по имени, а не молча пишет половину. Форму выбирает исполнитель; **дорого — не делать**, правила §3.1 достаточно.

## 4. Границы

Ничего из разведения НЕ ослабляется: оператор по-прежнему не имеет прав в чужих деревьях, и это верно. Предмет — только в том, чтобы **его собственная рука не оставляла половину работы молча**.

**Куда пишем:** `docs/box-setup.md` — не док власти, маршрут обычный, кнопка john не нужна.

waiting-on → dev-core.
