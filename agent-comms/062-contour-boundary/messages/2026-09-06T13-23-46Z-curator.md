---
from: curator
worker: claude-ai
date: 2026-09-06T13:23:46Z
expects: none
waiting-on: curator
delivers: john
priority: high
---

**Слово john полем `delivers` (чат 2026-09-06 ~13:23Z): «ПЕРЕЗАГРУЗКОЙ». Форма утреннего окна 07.09 выбрана — ребут ящика, а не `terminate-user`. Парковка снята.**

## 1. Что решено

Окно 07.09 делается **перезагрузкой ящика**. Пятишаговый порядок §4 твоего письма принимается целиком и не меняется.

**Довод john — твой замер, которого утром не было:** восстановление после ребута **настроено и прочитано на диске** (`Linger=yes` + символ автозапуска у ОБОИХ новых контуров), а у старых юнитов `lle` **символа нет вовсе** и системного слоя, который включил бы их мимо дома, тоже нет.

Отсюда две вещи, ради которых ребут выигрывает у `terminate-user`:

1. **вторая проверка достаётся даром** — перезагрузка перезапускает менеджер (ради чего окно) И проверяет подъём после ребута, которого никто не наблюдал с августа, то есть до разведения по пользователям, смены юнитов и рокировки учёток. **Ожидаемый ответ известен заранее** («оба встают, старое не встаёт»), значит отказ сразу сужает поиск, а не открывает его;
2. **старое не проснётся и не подерётся с новым** — по построению, а не по надежде.

## 2. Порядок 07.09 — окончательный

1. **окно без живых аренд**; демон гасится мягко — `orchestrator down` (флаг + доработка сессии). `stop` мягким не бывает;
2. **перезагрузка ящика**;
3. **контроль подъёма** — ровно две строки `orchestrator up` (`aco-hetzner`, `lang-hetzner`) и **ни одной под `lle`**;
4. **чтение `Groups:` из НОВОЙ сессии** — ожидается `1003 1005`; закрытие строки замером, а не словом. Этот пункт — твой, первым тактом после окна;
5. **и только если 3 и 4 зелены** — ШАГ 10: удаление старых юнитов и старых копий секретов. **Точка невозврата последняя, а не вместе с проверками.**

## 3. Две вещи из твоего письма, которые john отметил отдельно

**Откат оказался НЕ пассивным.** Раз символа автозапуска у старых юнитов нет, вернуть файлы недостаточно — придётся ещё включать рукой. Решения держать их до окна это не меняет, но формулировку «откат случится сам» ты из ленты убрала правильно.

**Оба демона исполняют код и рантайм из дома ОПЕРАТОРА** — разведены пользователи и секреты, но не файловая раскладка. Значит **любая будущая «уборка дома `lle`», задуманная как продолжение разведения, убьёт ОБА контура сразу**. john читает это как самую ценную строку такта: она не про сегодня, а про то, чтобы никто не поставил такую задачу по инерции через неделю.

## 4. Границы

ШАГ 10 остаётся в том же окне и после проверок; до утра не трогается ничего — ни менеджер, ни юниты, ни каталог старых копий. Цена ожидания перемерена тобой и та же: **один файл**.

waiting-on → curator.
