---
from: curator
worker: claude-ai
date: 2026-09-06T19:36:46Z
expects: none
waiting-on: curator
parked-on: john
priority: high
---

**ДОПИСКА К ПОРЯДКУ УТРА: блок 8-бис — подъём четырёх тредов без хода. Слово john (чат ~19:36Z), предмет — тред `147`.**

## Блок 8-бис (после разморозки шести пар, до точки невозврата)

**Поднять `curator` рукой на четырёх тредах:** `043-stale-park-reminders`, `044-selfheal-blind-spots`, `073-notifiers-frozen-in-own-contour`, `080-standing-address-catalog`. Форма — та же, что для заморозок: ручной запуск, флаг потолка если потребуется.

**Почему рукой, а не «когда дойдёт очередь»:** у этих четырёх **нет двери по построению**. Ход в треде назначает только письмо в его ленту; письмо может только сессия, поднятая ИМЕННО по этому треду; а поднимают только пары, у которых ход есть. Круг замкнут — ни один такт из других тредов их не сдвинет. Замерено curator: поле хода пишут ровно две команды, обе — письмо в свою ленту.

**Что там лежит:** промежуточный замер без вывода (`043`); влитый пакет с неснятой приёмкой (`044`); приёмка, ждавшая поля и не дождавшаяся — 71 прогон, все пропущены (`073`); тред, сам отказывающийся закрываться до полевой проверки (`080`).

## Порядок утра 07.09 целиком — окончательный

1. **узкий токен** роли `devops` (только этот репозиторий, только запись содержимого);
2. **раскладка по домам** — файл секретов в доме `aco-devops` (600, владелец она);
3. **защита `main`** — несущая мера сужения; ставится позже выдачи ⇒ назвать окно вслух;
4. **`orchestrator down`** — мягко, флаг + доработка сессии;
5. **перезагрузка ящика**;
6. **контроль подъёма** — ровно две строки `orchestrator up`, ни одной под `lle`;
7. **`Groups:` из НОВОЙ сессии** — ожидается `1003 1005`;
8. **Н1–Н6 — накатка пина контуру-потребителю** (влить PR → подтянуть чекаут из-под `lang-hetzner` → зависимости только если двигался замок → выровнять рабочие места ролей, дерево с собственной веткой не трогать → поднять службой → проверить код и дрейф);
9. **разморозка шести пар** — `devops` × `047`, `056`, `057`, `070`, `079`; `dev-core` × `063`. После перезагрузки доступна штатная команда `thaw`; обходной флаг — запасной путь;
10. **8-бис: подъём четырёх тредов без хода** — `043`, `044`, `073`, `080`;
11. **точка невозврата** — удалить старые юниты `lle`, старые копии секретов, каталог `047.1-devops-enablement-acceptance`.

**Точка невозврата последняя и только если 6 и 7 зелены.** Откат не пассивный: у старых юнитов нет символа автозапуска, вернуть файлы недостаточно — придётся включать рукой.

waiting-on → curator, parked-on john.
