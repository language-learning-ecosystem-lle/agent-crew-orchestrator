---
from: curator
worker: claude-code
session: ca62a5dd-ae22-41b8-b9db-512686d49445
raised: 2026-09-06T11:46:14Z
date: 2026-09-06T11:55:22Z
expects: ack
waiting-on: curator
parked-on: john
---

**Требуется решение john. Оба вчерашних вопроса замером сведены в ОДИН: назначить ли ОДНО окно без живых аренд (ближайшая перезагрузка или плановый `hold`), в котором разом делается перезапуск пользовательского менеджера `aco-hetzner` — за группами — и ШАГ 10 (удаление старых юнитов `lle` и старых копий секретов). Рекомендация: ДА, одно окно, и до него не трогать ни того, ни другого — цена ожидания измерена сегодня и равна ОДНОМУ файлу.**

Рекомендация curator ≠ решение john: и перезапуск менеджера, и точка невозврата ШАГ 10 — рука на живом ящике, роль его не трогает.

## 1. Группы — что померено сегодня (11:47…11:52Z, только чтения)

- **членство в БАЗЕ есть:** `getent group contour-aco` → `contour-aco:x:1005:aco-hetzner,aco-devops,lle`; `id aco-hetzner` → `…,1005(contour-aco)`;
- **до процесса группа НЕ доехала:** `/proc/self/status` моей живой сессии — `Groups: 1003`, и только;
- **менеджер не перезапускался ни разу:** `systemd --user` пользователя `aco-hetzner` — PID **1917240**, `lstart` = `Fri Sep 4 13:52:51` местного (= `2026-09-04T11:52:51Z`), то есть тот же процесс, что и до `usermod`. Сегодняшняя ручная возня john с дрейфом демона это не изменила и не могла: перезапускалась СЛУЖБА (её процесс — 856824, `PPid 1917240`), а менеджер — нет. Ровно это и предсказывал замер `/proc/*/status` от 2026-09-04.

## 2. Цена ожидания — измерена, и она не та, которой боялись

- **сессиям контура группа не нужна вовсе:** чекаут — `drwxrwsr-x aco-hetzner contour-aco`, то есть процессы `aco-hetzner` ходят по битам **владельца**;
- **`aco-devops` группу ПОЛУЧАЕТ, и менеджер тут ни при чём:** в логе его живой сессии сегодня (`2026-09-06T10:22:12Z`, тред `079`) стоит `uid=1001(aco-devops) gid=1001(aco-devops) groups=1001(aco-devops),1005(contour-aco),1006(contour-lang)` — `sudo -u` пересчитывает список групп заново, из базы;
- **остаётся ровно один класс:** файл, СОЗДАННЫЙ `aco-devops` внутри общего чекаута, для процессов `aco-hetzner` только на чтение (режим `664`, доступ был бы по группе). Такой файл сегодня **один**: `.orchestrator/memory/devops/gap_no_git_credentials.md` (`-rw-rw-r-- aco-devops contour-aco`, 29 938 байт); `test -w` из моей сессии — **не writable**.

**Это вся измеренная цена «группы не доехали»**: не сломанный контур, а один чужой файл, который роли `aco-hetzner` не перепишут. Найдено перечислением, а не образцом: `find . -user aco-devops` по всему чекауту (кроме `node_modules`) даёт ровно эту одну строку.

## 3. Почему перезапуск менеджера — не «просто рестарт», и почему в окно

- **менеджер — родитель демона и всех сессий контура** (`856824` → `PPid 1917240`). Его остановка рвёт живые сессии, а оборванная сессия засчитывается неудачной попыткой; три подряд — роль выключена по потолку (тред `140`, `devops` молчала трое суток). Это тот же класс опасности, что §2(б) треда `141`;
- **рестарт ЮНИТА группу не подтягивает** (замер 2026-09-04): список групп менеджер получает при своём собственном старте. Нужен `loginctl terminate-user` либо перезагрузка — и то, и другое рукой john;
- поэтому дешёвый порядок один: **окно без аренд → менеджер поднимается заново → `Groups:` перечитывается из НОВОЙ сессии** (закрытие строки замером, а не словом).

## 4. ШАГ 10 — состояние окна отката, замером

- **старые юниты `lle` на месте, путь отката шага 2 жив:** `/home/lle/.config/systemd/user/` — `agent-protocol@hetzner.service` (18 Aug), `lle-orchestrator@lle-hetzner.service` (19 Aug) и два каталога drop-in (3 Sep 14:30 местного);
- **и они не работают:** `ps` по всему ящику — процессов `orchestrator up` ровно два, оба под новыми пользователями (`856824 aco-hetzner`, `463653 lang-hetzner`), ни одного под `lle`;
- **старые копии секретов:** каталог `/home/lle/.config/agent-protocol` существует, `drwx------ lle lle`, mtime 30 Aug 20:08 местного. **Содержимое из этого контура не читается** (`Permission denied` на самом каталоге) — и это ограничение замера, а не вывод: «копии лежат такие-то» я сказать не могу, могу сказать «каталог на месте». Значений секретов не читала и не печатала.

**Рекомендация — держать до того же окна.** Держать стоит два файла юнитов и один каталог под `700` у владельца ящика. Закрыть — точка невозврата, и она снимает единственный дешёвый путь отката ровно тогда, когда операция ещё не доведена до конца (группы не доехали). В одном окне это один заход: перезапуск менеджера → зелёное чтение `Groups:` из новой сессии → и только после него удаление старого.

## 5. Хвост открытых PR — прогнан, молчания за собой не оставляю

`gh pr list --state open` — два, тред-хозяин снят из тела каждого:

| PR | тред-хозяин | `merge-gate --review-workflow 'Claude PR Review'` |
| --- | --- | --- |
| #292 | `140-silent-exit-exhausts-the-role` | **STOP гард 1**: `changes were requested on 898396c (github-actions)` — новый круг, не merge; гард 2 тоже не держит (`review=IN_PROGRESS`). Ход у dev-core |
| #286 | `135-base-is-the-biggest-line` | гарды 1, 2, 4 — `ok`, «nothing in the facts forbids this merge» |

**#286 готов к кнопке и НЕ смёржен этим тактом намеренно:** след гарда 5 обязан лечь в ленту треда-хозяина `135`, а из прогона, связанного тредом `062`, мне туда писать нечем. Это измерено и названо, а не забыто: кнопка закрывается за минуту первым же тактом curator в `135`. Обе двери печатают ноту о переехавшей базе (`952edcb`, `2026-09-06T11:37:22Z`) — она закрывается замером слитого дерева в своём треде, и это по-прежнему не норма.

## 6. Находка: слово john о самоперезапуске лежит НЕ в своём треде

Слово `delivers` от 2026-09-06 («пусть перезапускается сам, мягко и с письмом») записано **здесь**, моим письмом `2026-09-06T11:39:17Z`. А предмет живёт в треде `141-daemon-drift-needs-a-human`, где обе мои записи (`msg-001`, `msg-002` от 2026-09-06) — `expects: none` и «parked-on john». Это **стоячий парк без звонка**: тред заморожен, кандидатом не станет, а дверь парка принимает только письмо, назвавшее РОВНО этот парк (`--park-lifted`). Снять его изнутри подъёма нечем — сессия в `141` не поднимется, пока парк стоит.

**Значит, снимает его рука вне подъёма — чатный curator или john**, одним письмом в `141` с `--park-lifted`, несущим: слово john дословно, пять шагов мягкого пути и три ловушки (`up` поднимает демон сам; мягкая форма зовётся `down`, а не `stop`; установка нужна, только если двигался замок, и `CI=true` против `ERR_PNPM_ABORTED_REMOVE_MODULES_DIR_NO_TTY`) — всё это стои́т в моём письме `11:39:17Z` выше. Постановка dev-core пишется уже там, в `141`, а не здесь: `062` — про границу контура. Из прогона, связанного тредом `062`, я в `141` не пишу, и молчанием пункт не оставляю — он назван.

## 7. Чего этот такт не сделал

- **живого ящика не тронул ни битом:** только чтения — `getent`, `id`, `/proc/*/status`, `ps`, `ls`, `find`, `test -w`, два `merge-gate`, `thread show`. Ни `usermod`, ни `systemctl`, ни `loginctl`, ни `chown`;
- **значений секретов не читал и не печатал** (каталог старых копий и не читается);
- **дерева роли и склада refs не трогал:** ни коммита, ни ветки, ни тега; рабочее дерево чистое, незакоммиченного за мной нет;
- **чужих PR не хватал и кнопки не жал:** #286 и #292 остаются за своими тредами;
- **в тред `141` не писал** — см. §6.

## 8. Ход

`waiting-on → curator` (john в это поле не принимается), **`--expects ack`, парк на `john`** — вопрос первой строкой, потому что звонок несёт именно её.

**Почему парк возвращается, а не «стои́т»:** моё предыдущее письмо (`11:39:17Z`) парка не поставило вовсе — и тред тут же поднялся **голым кандидатом**, этим самым тактом, где работы было ровно на замер. Называю это, чтобы не повторить: доставка слова john — не конец такта, конец такта — парк.

**Что делает следующий такт curator:**

- сказано «окно назначено и прошло» → перечитываю `Groups:` из НОВОЙ сессии, закрываю строку замером и, если зелено, довожу ШАГ 10;
- сказано «до перезагрузки подождёт» → записываю признанным пределом приёмки со ссылкой на §2 (цена — один файл), а не недоделкой;
- сказано слово только по ШАГ 10 → исполняю его отдельно или снимаю окно отката с ленты.

Если по обоим пунктам решение принято и хвоста не останется — **тред закрывается приёмкой**, и закрытие будет названо файлами и строками ЗДЕСЬ, до закрытия, а не после него.

Тред остаётся **открытым**.

