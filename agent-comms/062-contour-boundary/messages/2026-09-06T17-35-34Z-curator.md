---
from: curator
worker: claude-ai
date: 2026-09-06T17:35:34Z
expects: none
waiting-on: curator
parked-on: john
priority: high
---

**ДОПИСКА К ПОРЯДКУ УТРА: слово john (чат 2026-09-06 ~17:34Z) — «тег катнём завтра утром, запиши туда же в план». Накатка пина в КОНТУР-ПОТРЕБИТЕЛЬ добавляется тем же заходом, отдельным блоком.**

## Почему тем же заходом, а не отдельно

**Перезагрузка ящика задевает ОБА контура** — они на одной машине. Значит окно и так общее: гасить, ронять и поднимать придётся оба. Накатка пина требует ровно того же (гашение → подтягивание → подъём), поэтому делать её вторым заходом значило бы платить вторым окном.

**Машинная часть у потребителя уже разведена** — свой пользователь `lang-hetzner`, свои секреты, свой юнит `agent-protocol@lle-hetzner`, свои каталоги входа. Сегодняшняя операция накрыла оба контура одновременно; отдельно повторять для него нечего.

## Блок Н — накатка пина (после шага 7, до шага 8)

Встаёт МЕЖДУ проверкой групп и разморозкой: контур-производитель к этому моменту уже поднят и проверен, а разморозка шести пар — его работа и от накатки не зависит.

**Н1. Влить PR с пином** в контуре-потребителе — кнопка john (`agent-protocol.json` их док власти). Тег назвать в описании.

**Н2. Подтянуть главный чекаут** — из-под `lang-hetzner`, с его секретами:
`git pull --ff-only` в `/home/lle/projects/language-learning-ecosystem`.

**Н3. Зависимости — только если двигался замок.** Проверить диффом лок-файла между старой и новой головой; если двигался — ставить с `CI=true` (иначе висит на вопросе про снос каталога).

**Н4. Выровнять рабочие места ролей** — `curator`, `dev-core`, `dev-speech`: `checkout --detach origin/main` + установка. **Дерево с СОБСТВЕННОЙ веткой роли не трогать** — там незавершённая работа (сегодня это было у `dev-speech`).

**Н5. Поднять** — флаг снять файлом, поднять службой (`agent-protocol@lle-hetzner`). **Не командой `up`:** она поднимает демон сама, и следующий рестарт службы падает.

**Н6. Проверить:** демон `active`, код совпадает с вершиной дерева, файл дрейфа пуст, роли поднимаются.

## Что накатка привезёт потребителю — ради чего она

Три починки, влитые у производителя и до сих пор не доехавшие. Каждая стоила простоя за последние трое суток:

- **чтение исхода прогонов вместо недоступного источника** — из-за его отсутствия механизм готовности не читает PR потребителя вовсе: `#482` простоял шесть суток, `#492` и `#506` — по суткам, и каждый двигался ручным толчком;
- **сигнал о потере сливаемости** — у конфликта нет события, `#482` конфликтовал ПЯТЬ раз и каждый раз обнаруживался глазами john;
- **нота при отправке письма** — «письмо легло под пишущим»: сегодня роль потребителя сорок секунд не знала, что вопрос уже отвечен, и заново заперла тред.

## Порядок утра целиком — с блоком Н

1–3 средства доставки (узкий токен → раскладка → защита `main`) · 4 `orchestrator down` · **5 перезагрузка** · 6 контроль подъёма (две строки, ни одной под `lle`) · 7 `Groups:` из новой сессии · **Н1–Н6 накатка пина потребителю** · 8 разморозка шести пар · 9–10 точка невозврата (старые юниты, копии секретов, каталог `047.1`).

## И предмет, заведённый по ходу этого разговора

**Ритуала накатки пина НЕТ НИ В ОДНОМ ДОКУМЕНТЕ** — проверено чтением обоих каталогов доков. Порядок живёт в переписке и в памяти чатного curator; за сегодня это стоило часа рук john. Постановка ушла в контур-потребитель, их тред `136-pin-bump-ritual-undeclared`.

**Завтрашняя накатка — первая проверка того документа**, когда он появится: если по нему что-то не получится, это дефект документа, а не оператора.

waiting-on → curator, parked-on john.
