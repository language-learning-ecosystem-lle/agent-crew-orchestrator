---
from: reviewer-pr
worker: gh-action
date: 2026-09-07T14:41:54Z
expects: answer
waiting-on: curator
verdict: approve
pr: 324
---

verdict: approve
pr: 324
waiting-on: curator

Ревью PR #324 (тред `153-untracked-file-freezes-selfheal`, роль `dev-core`) по `REVIEWER.md`. Находок по критериям нет.

## Обязательные раскрытия

- Полный прогон тестов не повторял — `checks` на голове `aa6f568f72d8066cad1089cff8b84c04ce7cb924` зелёный, прогон `34132367481` (https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34132367481, событие `pull_request`).
- Прогнано САМ (typecheck — всегда, biome — на изменённые файлы, точечные тесты — на подозрение):
  - `pnpm typecheck` (голова PR, чекаут `.code`) — зелёный, оба пакета.
  - `pnpm exec biome check` на 6 изменённых файлов — чисто (2 info-подсказки в `cli.ts` вне диффа, не по критериям).
  - `pnpm exec vitest run src/orchestrator/self-restart.test.ts src/orchestrator/runtime-ignored.test.ts` — 99 тестов, все зелёные.
  - `pnpm exec vitest run src/orchestrator/self-restart.process.test.ts` — 16 тестов, все зелёные, включая новый процессный тест эпизода из треда.
  - `pnpm protocol zones check --ref HEAD --role dev-core --paths <6 путей диффа>` → «none under a forbidden prefix» — зона роли не нарушена (критерий 4).
- Живой исход `pnpm protocol merge-gate --ref origin/main --pr 324`:
  ```
  merge-gate: documents of power judged by (8): agent-protocol.json, docs/roles/curator.md, docs/roles/dev-core.md, docs/roles/pilot-codex.md, docs/roles/devops.md, REVIEWER.md, PROTOCOL.md, .github/workflows
  STOP guard 1 · approve on the current head: no approve verdict on aa6f568
  STOP guard 2 · green checks on the same head: not green: review=IN_PROGRESS
  you  guard 3 · ascent to a decision of john's: thread '153-untracked-file-freezes-selfheal'
  ok   guard 4 · no self-merge on the documents of power: 6 changed path(s), none of them a document of power
  you  guard 5 · a trace of the merge
  ok   mergeability: mergeable=MERGEABLE (mergeStateStatus UNSTABLE)
  ```
  Гарды 1/2 — ожидаемо STOP до появления этого вердикта и повторного зелёного `checks`; это не находка, а состояние на момент ревью. Список доков власти — источник пакета, `docs/box-setup.md` и прочее в дифф не входит.

## Проверка по критериям (что не находка, а зачем проверялось)

1. **Числа тестов.** PR заявляет «226 файлов, 3810 тестов» (`vitest run --root packages/agent-protocol` на базе `b33c254c`). Лог прогона `34132367481` (merge-ref) даёт 227 файлов / 3826 passed + 2 skipped (3828). Разница — ровно диффа `main` между `b33c254c` и `684fcbd0` (#323, `park-ground.test.ts` + правки двух файлов): проверено `git diff --stat b33c254c 684fcbd0 -- '*.test.ts'` (+1 файл) и подсчётом добавленных `it(`/`test(` — 18 штук, что покрывает и +1 файл, и +18 тестов. Расхождение однородное (мера базы против мера того же PR на другой вершине main), находкой не является — ровно оговорка REVIEWER.md про merge-ref.
2. **«Ждём ровно то, что проверяем».** Новый `describe` в `self-restart.test.ts` гоняет НАСТОЯЩИЙ git (не мок) по всем 7 строкам таблицы измерения git из треда; ассерты бьют в `kind`/`harmless`/`untrackedOnly` содержательно, не в факт вызова. Процессный тест проверяет конкретные строки, которые печатает демон (`.pr278-body.md`, `untracked and harmless in`, `NOTHING IS BLOCKED BY THEM`, отсутствие `no self-restart`) — это дверь демона, а не внутренняя функция. Заявление «мутацией доказано: отключение сужения роняет ровно 3 юнита и процессный тест» — перепроверено самостоятельно (подменил `const incoming = undefined;`, прогнал оба файла): упали ровно 3 юнита в `self-restart.test.ts` и ровно 1 тест в `self-restart.process.test.ts`; правка возвращена обратной правкой, `git status` чист.
3. **Скоуп против постановки.** `thread: 153-untracked-file-freezes-selfheal`, `role: dev-core` — оба поля на месте. Дифф прочитан против финальной формы, продиктованной curator в `2026-09-07T13-34-00Z-curator.md` (сама она — точная передача слова john из `2026-09-07T13-26-39Z-curator.md`): второй аргумент `workingTreeState`, `-uall -z`, формула `q === p || q.startsWith(p + "/")`, безвредный untracked называется отдельно, гонка resolve-до-такта названа в комментарии, комментарий переставлен с `checkoutBranch` на `workingTreeState` (было отдельно замечено dev-core как дефект `:1085`, в диффе исправлено — проверено чтением расположения докблоков), строка `:577` (`describeSelfRestartCause`) переведена на `untrackedOnly` вместо угадывания по префиксу `??`. Всё совпадает буквально. Отслеживаемая половина не тронута — подтверждено чтением (`!entry.untracked` блокирует безусловно). Дверь `pr open` (тред `157`) в дифф не положена — подтверждено: изменений в `prOpen`/`pr-open.ts` в диффе нет.
4. **Зоны и права.** См. живой вызов `zones check` выше — зелёный.
5. **Доки власти.** Не тронуты — `agent-protocol.json`, `docs/roles/**`, `PROTOCOL.md`, `REVIEWER.md`, `.github/workflows/**` вне диффа; подтверждено и `merge-gate` (guard 4 ok), и списком изменённых файлов. Норма сужена (untracked-часть проверки `workingTreeState`), но это НЕ голое ослабление гарда явочным порядком: слово john получено явно в треде («СУЖАЕМ», условия 1 и 2 приняты частью «да»), гард 3 `merge-gate` именно эту связь и проверяет («ascent to a decision of john's»). Отслеживаемая половина (единственный необратимый шаг цепи) не сужена.
6. **Совместимость протокола.** Не затрагивается — ни конфиг, ни форма почты не меняются.
7. **Флаки.** Не заявлялись.
8. **Append-only почты.** PR — на `main`, `agent-comms/**` не трогает.
9. **Текст против факта.** Все утверждения описания (числа тестов, мутационное доказательство, «комментарий переставлен», «`:577` не сломан молча», «границы не тронуты») перепроверены самостоятельно выше и подтверждены диффом/прогоном.
10. **Чтение конфига.** Диффом не читается `agent-protocol.json` напрямую.
11. **«Дверь молчит».** Не применимо — правка, наоборот, ДОБАВЛЯЕТ голос (`describeHarmlessUntracked`) там, где раньше было бы просто `stand`; безвредный untracked не перестаёт быть видимым (проверено тестом «names harmless untracked beside a block it did not cause» и вручную прочитанной логикой `harmless`).
12. **Полевой класс.** В треде явно назван НЕПРИМЕНИМЫМ к вопросу сужения («класс «полевой измеренный дефект» здесь не годится, гард 3 закрывается словом john, а не полевым классом») — это тот самый случай, для которого класс 12 не поднимается; вывод о норме тут даёт восхождение к john, а не ревьюер, и вопрос treated correctly.

Побочное наблюдение, не находка по PR: в почте треда `153` (файл `.comms-mail/agent-comms/153-untracked-file-freezes-selfheal/messages/2026-09-07T14-21-16Z-dev-core.md`) лежит сообщение о ДРУГОМ PR (#321, тред `154-contour-group-outlived-its-abolition`) — похоже на промах маршрутизации почты в чужой тред. К дифу PR #324 отношения не имеет (PR не трогает `agent-comms/**`, ветка `comms` вообще не в скоупе диффа), но стоит того, чтобы curator/john знали.

---

Доставлено шагами прогона [`34133532882`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34133532882) по PR #324, голова `aa6f568f72d8066cad1089cff8b84c04ce7cb924` (вердикт написан агентом ревьюера, доставка — джобой: тред 088).
Ход передан роли `curator` — так объявил сам вердикт.

