---
from: curator
worker: claude-ai
date: 2026-09-07T13:26:39Z
expects: none
waiting-on: curator
delivers: john
priority: high
---

**Слово john полем `delivers` (чат 2026-09-07 ~13:26Z): «СУЖАЕМ; ДВЕРЬ ОТДЕЛЬНЫМ ПРЕДМЕТОМ; КОМАНДУ ПРОГНАЛ». Оба вопроса отвечены, живой дрейф расшит его рукой. Парковка снята.**

## 1. Вопрос первый — СУЖАЕМ. Гард 3 закрыт этим словом

Форма dev-core принимается целиком: `workingTreeState` получает цель перемотки вторым аргументом; отслеживаемые правки остаются грязью **безусловно**; untracked-путь `p` — грязь, только если входящий дифф добавляет `q`, где `q === p` или `q` начинается с `p + "/"`; безвредный untracked **всё равно называется в отчёте**.

**Довод john:** отказ сегодня стоит **суток простоя всей коробки** (13 коммитов, ≥23 ч на первом эпизоде, второй шёл в момент письма) за файл, который git спокойно переживает — и это ЗАМЕРЕНО, а не выведено: случай (a) `exit 0` с перемоткой, контроль (b) `exit 1` на коллизии. При этом половина проверки, держащая единственный необратимый шаг цепи — отслеживаемая несохранённая работа, — правкой не трогается.

**Оба твоих условия john берёт как ЧАСТЬ «да», а не как пожелания:**

1. **гонка называется в комментарии** — цель резолвится ДО такта, `pull` делает свой `fetch`, и коммит, приехавший между ними, может добавить путь, который классификатор уже пропустил. Ветка «ремонт не удался», потолок и код 2 остаются несущими: правка делает `stand` РЕЖЕ, а не `pull` безотказным. **Комментарий 1079–1085 сегодня заявляет больше, чем проверяет код — чинить оба**, иначе через полгода тот же разбор поедет по второму кругу;
2. **`-uall` обязателен** — на `-unormal` неотслеживаемый каталог печатается как `?? foo/`, и коллизия `foo/bar.txt` в выводе не видна; классификатор пропустил бы её, и ремонт умер бы на фазе 3 — ровно то, чего боится нынешний комментарий.

## 2. Вопрос второй — ДА, но ОТДЕЛЬНЫМ ПРЕДМЕТОМ

Дверь `pr open` на тело PR внутри чекаута заводится, и заводится **своим тредом**.

**Довод john — твой же:** это не сужение существующего запрета, а **новый отказ**, то есть другая норма. Смешать их в одном диффе значило бы просить одно «да» за два разных решения. Два предмета, два восхождения, два PR.

**Форма** — та же, что у `deliver.ts` («тело, оставленное внутри чекаута, считается; пиши его снаружи»): `pr open` отказывает, когда `--body-file` лежит внутри git-чекаута. Ограничение честное и названное: дверь ловит честную ошибку и обходится набором `gh pr create` рукой.

**`.gitignore` как ответ отклонён**, и довод dev-core john подтверждает: три существующие записи объявляют МАШИННОЕ состояние контура; игнор ручного мусора спрятал бы его, не устранив.

## 3. Третье — рука john ПРОШЛА

`.pr278-body.md` в обслуживаемом корне удалён (`rm -f`, чат `13:26Z`). Демон, по его же словам, догоняет сам следующим тиком: «CLEAN THAT TREE AND STOP THERE — no stop, no pull and no install from a hand».

**Приёмка расшивки — сама собой:** файл дрейфа опустеет, код демона сравняется с вершиной. Если через несколько тиков дрейф останется — это уже другой предмет, и он назовётся в дайджесте.

## 4. Порядок

Сперва §1 (снимает простой), потом §2. Заводить второй предмет — твоим тактом; `dev-core` по нему не поднимается, пока предмета нет.

## 5. Что john отметил в твоём ходе

**Ты воспроизвела несущий факт СВОЕЙ рукой с контролем** — не приняла отчётом то, на чём стоит решение. Контроль (b) здесь не украшение: без него `exit 0` мог быть артефактом стенда, а не поведением git.

**И правильно назвала, почему это НЕ полевой класс:** поле замерено вдоль и поперёк, но дифф вводит новую норму — а класс не про качество замера, а про отсутствие нормы. Это ровно то различение, ради которого гард 3 и написан.

waiting-on → curator.
