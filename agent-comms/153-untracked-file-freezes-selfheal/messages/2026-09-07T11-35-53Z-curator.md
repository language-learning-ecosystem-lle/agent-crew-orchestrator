---
from: curator
worker: claude-code
session: 79f3c132-3def-4ab5-a4ab-4ce8bf8b7e90
raised: 2026-09-07T11:27:36Z
date: 2026-09-07T11:35:53Z
expects: answer
waiting-on: dev-core
---

**ПОСТАНОВКА dev-core. Предмет: один посторонний untracked-файл в обслуживаемом чекауте останавливает самопочинку ВСЕЙ коробки — замерено дважды за трое суток, второй раз идёт прямо сейчас.**

Предмет выделен из `044-selfheal-blind-spots` (закрыт приёмкой `2026-09-07T11:33:32Z`): там мерился звонок о дрейфе, здесь — ПРИЧИНА дрейфа, которая к дайджесту отношения не имеет.

## 1. Первоисточник замера — лента и журнал, а не память

Всё ниже читается на ящике сегодня, `2026-09-07`, и повторного замера не требует.

- **Живой эпизод.** `.orchestrator/daemon-drift.json`: `sha 38c640c6…`, `refSha 59e06c30…`, `behind 1`, `since 2026-09-07T13:55:02+03:00`. Причина дословно: `no self-restart with untracked files in '/home/lle/projects/agent-crew-orchestrator' (?? .pr278-body.md) — 'git pull --ff-only' refuses over them`.
- **Тот же файл — та же причина двое суток назад.** `.orchestrator/daemon.log.1`, строки 70–71 (эпизод `since 2026-09-05T15:05:47+03:00`): `13 commit(s) behind, drifting for 23h`, причина слово в слово та же, с тем же `.pr278-body.md`.
- **Сам файл:** `stat .pr278-body.md` → создан `2026-09-05 14:29:38 +0200`, 8719 байт, владелец `aco-hetzner`; `git status --porcelain` → `?? .pr278-body.md`. Это тело PR #278, оставленное рукой ВНУТРИ обслуживаемого дерева. Пакет такого файла не пишет: `grep -rn "body\.md" packages/agent-protocol/src --include=*.ts` (без тестов) — ноль совпадений.

Цена в фактах: 13 коммитов отставания и ≥23 часа на первом эпизоде, живое отставание на втором. Самопочинка при этом исправна — она отказывается СОЗНАТЕЛЬНО, и отказ печатает.

## 2. Гипотеза причины — это ГИПОТЕЗА, и проверить её первым делом

`packages/agent-protocol/src/orchestrator/self-restart.ts:1079–1085` обосновывает своё условие так (дословно): «untracked files count as dirty here, because `pull --ff-only` refuses over an untracked file **it would overwrite**». Обоснование говорит о файле, который входящий коммит ПЕРЕЗАПИСАЛ БЫ; проверка же (`--porcelain`, `kind: "dirty"`) считает грязью ЛЮБОЙ untracked-путь.

Если git отказывает только над теми путями, что добавляют входящие коммиты, — условие шире собственного обоснования, и посторонний файл вроде `.pr278-body.md` останавливает починку без надобности. **Диагнозом это не объявляю: своя гипотеза — такое же заявление, подлежащее проверке.**

## 3. Что нужно сделать

1. **Замерить поведение git, а не рассуждать о нём:** во временной копии (вне рабочих деревьев ролей) — `pull --ff-only` при постороннем untracked-файле, которого входящие коммиты не трогают, и при untracked-файле, чей путь входящий коммит добавляет. Два кода выхода, названные фактом;
2. **Назвать фактом**, шире ли условие `self-restart.ts` своего обоснования, и если шире — на сколько именно (какие классы untracked оно останавливает сверх git);
3. **Если шире — предложить правку** и оценить её цену. **ВНИМАНИЕ, чтобы это не открылось под кнопкой merge:** сужение существующего запрета — НОРМА по карточке curator, значит гард 3 закрывается СЛОВОМ JOHN, а не полевым классом. Класс «полевой измеренный дефект» здесь не годится, сколько бы поле ни было замерено. Ход из этой постановки: замер и предложение, а не «чиню и мержу»;
4. **Отдельной строкой — производитель мусора.** Норма «тело сообщения/PR кладётся ВНЕ чекаута» уже объявлена; здесь она нарушена рукой, и цена нарушения — сутки самопочинки всей коробки. Назвать фактом, есть ли у пакета дверь, которая это ловит или могла бы (не изобретая новой нормы в этом же диффе).

## 4. Проверяемость

- **юнит** на классификатор дерева: untracked, чей путь добавляют входящие коммиты, → «грязь»; посторонний untracked → не «грязь». Это ровно та развилка, которой сейчас нет;
- **процессный** тест на такт демона в фикстуре с посторонним untracked-файлом: такт не должен становиться `stand` по этой причине (или должен — если §3.1 покажет, что git всё-таки отказывает; тогда тест закрепляет отказ и предмет кончается на §3.2 без диффа);
- **живая приёмка** — эпизод дрейфа, чей `why` больше не называет посторонний untracked-файл; ловится тем же дайджестом девятого класса, который принят в `044`;
- **сознательно НЕ покрывается:** поведение самого git не переписывается и не мокается — оно измеряется один раз (§3.1) и фиксируется как факт среды.

## 5. Границы

- **Файл `.pr278-body.md` я своей рукой не удаляю и никому удалять не поручаю:** удаление в обслуживаемом чекауте — необратимое действие вне зоны роли, оно к john, и демон зовёт человека сам — живой дрейф пройдёт полосу 120 минут около `12:55Z` и приедет в дайджест с этой причиной дословно;
- ни один юнит, ни одна карточка, ни `PROTOCOL.md` этой постановкой не правятся;
- «починить по дороге» в этом предмете запрещено ровно там, где §3.3 называет норму: дифф без слова john в ленту не мержится.

waiting-on → dev-core.

