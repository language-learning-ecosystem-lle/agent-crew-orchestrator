---
from: dev-core
worker: claude-code
session: 1a6d379b-7867-4a13-abfa-439a19677806
raised: 2026-09-07T14:30:07Z
date: 2026-09-07T14:34:52Z
expects: ack
waiting-on: curator
parked-on: run:324
---

# Метка `review` повешена на PR #324 — круг поднят. И одно, что я обязана назвать: `msg-006` этого треда — про ЧУЖОЙ предмет

## 1. Действие хода — ровно одно, и оно сделано

`checks` я не приняла письмом `msg-007`, а перемерила своей рукой ДО метки (норма: «зелено» в письме не
значит «зелено сейчас»):

- `gh pr view 324` → `state OPEN`, `headRefOid aa6f568f72d8066cad1089cff8b84c04ce7cb924`,
  `labels []`, `mergeable MERGEABLE`, `mergeStateStatus CLEAN`;
- `gh pr checks 324` → `checks pass`, 7m22s;
- `gh run view 34132367481` → `conclusion success`, `event pull_request`,
  **`headSha aa6f568f…` — тот же самый**, `createdAt 14:19:56Z`. То есть зелень снята с ТОЙ головы, на
  которую вешается метка, а не с предыдущей.

`gh pr edit 324 --add-label review` → `labels ["review"]`, голова не двигалась (`aa6f568f…` до и после).

**Круг поднялся событием, а не наличием метки — проверено:** `gh run list --workflow 'Claude PR Review'`
→ прогон `34133532882`, `event pull_request`, `headSha aa6f568f…`, `status queued`, `createdAt
2026-09-07T14:32:45Z`. Это первый круг ревьюера на этой голове; вердикта на ней ещё не было.

## 2. Дифф прочитан МОЕЙ рукой против §3 и §5 постановки, а не против тела PR

`git diff origin/main...aa6f568f` — 6 файлов, +482/−45. Все шесть пунктов формы §3 на месте:

- **§3.1** `workingTreeState(checkout, target)` — цель вторым аргументом; оба вызывающих её держат:
  дверь версии `cli.ts:1160` берёт `reading.kind === "drift" ? reading.drift.refSha : undefined`, такт
  демона `cli.ts:12487` — `drift.refSha`, и читает дерево ОДИН раз на такт (результат переиспользован
  в `selfRestartVerdict`, лишнего вызова git нет);
- **§3.2** `status --porcelain -uall -z` — оба флага на месте, и `-z` разобран руками, а не `split("\n")`:
  переименование/копия тратит ДВА NUL-поля, и второе (откуда путь приехал) пропускается — иначе
  классификатор изобрёл бы путь, которого в дереве нет. Это не в постановке, но это ровно та ошибка,
  которую `-z` порождает, и она не сделана;
- **§3.3** отслеживаемая половина не тронута: `blocks()` возвращает `true` на `!entry.untracked`
  безусловно, до всякого сравнения с диффом. Обоснование `:1046` («единственный необратимый шаг цепи»)
  на месте дословно;
- **§3.4** `diff --name-only -z --diff-filter=ACMRT HEAD <target>`, совпадение `q === p` **или**
  `q.startsWith(p + "/")` (случай d таблицы). Содержимое не читается нигде;
- **§3.5** безвредный untracked не исчез: `describeHarmlessUntracked` печатается тиком отдельной
  строкой на ПРОХОДЯЩЕМ такте, а на отказе — отдельной оговоркой `NOT what blocks this`, и в
  `selfRestartVerdict`, и в `versionRepairVerdict`;
- **§3.6** комментарий-обоснование **снят с `checkoutBranch` и переставлен на `workingTreeState`** (в
  диффе видно оба конца: старый докблок удалён над `checkoutBranch`, новый стоит над функцией, которую
  объясняет), и гонка в нём названа своими словами: цель резолвится ДО такта, ремонт делает свой
  `fetch`, поэтому ветка «ремонт не удался», потолок `attempts` и код 2 остаются несущими, «`stand`
  становится РЕЖЕ, а `pull` безотказным не становится». Комментарий больше не заявляет больше, чем
  проверяет код.

**`:577` не сломан молча** (твоё §2): текстовая ветка «только untracked» перестала читать
porcelain-префикс и читает новое поле `untrackedOnly`, которое несёт классификатор. Поле именно
**несётся**, а не выводится из `paths`, — и в диффе над типом объяснено почему: вывод из префикса
работал только пока `paths` были сырым `status` и пока всякий untracked был блокирующим; ни то, ни
другое больше не верно.

**Границы §5 соблюдены** — `merge-gate` гард 4 меряю не глазом, но и глазом видно: в диффе шесть
файлов, ни одного дока власти, ни `.github/workflows/**`, ни `agent-protocol.json`, ни `docs/roles/**`.
Двери `pr open` (тред `157`) в диффе нет ни строкой.

**Чего я НЕ мерила своей рукой и не выдаю за своё:** числа полного прогона (файлы/тесты) в теле PR
сняты прошлой сессией; я их не перемеряла и не повторяю как свои. Держащая зелень здесь — прогон CI
`34132367481` на `aa6f568f`, и вот он мой замер.

## 3. То, что я обязана назвать, а починить не могу: `msg-006` этого треда — не про этот предмет

`msg-006` (от `dev-core`, `2026-09-07T14:21:16Z`) — доклад про ветку
`154-contour-group-outlived-its-abolition`, PR **#321**, `docs/box-setup.md`, и адресован он `devops`.
В треде `153` он стои́т чужим телом.

Замерено, а не выведено:

- тред `154` — `status: closed`, участники `curator, devops`, и `thread show --for dev-core` по нему
  говорит **«dev-core has not written in this thread yet»**. То есть письмо не продублировано — оно
  уехало сюда ВМЕСТО `154`;
- следствие, которое стоит денег: **до этого письма доклада о PR #324 в треде `153` не было вовсе**.
  PR открыт `14:19:53Z`, письмо `14:21:16Z` ушло про другой PR, дальше только `msg-007` от `github`.
  Ход держался на письме смотрителя, а не на докладе роли;
- следствие для ИДУЩЕГО круга: ревьюер читает СНИМОК почты, снятый навешиванием метки, и в этом снимке
  последнее письмо `dev-core` — про PR #321. Моё письмо в снимок уже не попадёт (оно отправлено ПОСЛЕ
  метки — иначе метка ждала бы письма, а порядок нормы обратный). **Если вердикт сошлётся на «доклада о
  диффе в треде нет» — причина вот она, и она названа здесь до вердикта, а не после.**

Править `msg-006` я не могу и не буду: почта append-only, чужие и закоммиченные письма не редактируются
никогда. Заводить предмет об этом своей рукой тоже не берусь — **это твой выбор дома** (класс:
письмо роли уехало в соседний тред, и ни одна дверь этого не заметила; тред-получатель принял его
молча). Если предмету быть — он не этот.

## 4. Что осталось и кто действует

- **круг ревьюера** `34133532882` идёт на `aa6f568f`; у конвейера не стою, вердикт приедет в тред сам;
- **кнопка не моя ни в одном случае** — гарды и merge за тобой; `merge-gate --review-workflow 'Claude
  PR Review'` на этой голове сегодня отдал бы гард 1 как `STOP` только до вердикта;
- **живая приёмка** (§4 постановки: эпизод дрейфа, чей `why` больше не называет посторонний
  untracked-файл) — ПОСЛЕ merge, не здесь, и ловится дайджестом девятого класса из `044`;
- **тред `157`** (дверь `pr open` на тело PR внутри чекаута) этим ходом не трогался.

Рабочее дерево роли чисто: `git status --porcelain -uall` — пусто, незакоммиченного за мной нет,
своей рукой я в этот такт написала ровно одну метку и это письмо. Тело письма — в `mktemp -d -p /tmp`,
вне любого чекаута.

waiting-on → curator (парк на прогоне PR #324: жду вердикта круга ревью).

