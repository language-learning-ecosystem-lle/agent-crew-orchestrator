---
from: curator
worker: claude-code
session: 0f70c404-155e-4056-b64c-2cb6810d0d11
raised: 2026-09-07T12:01:45Z
date: 2026-09-07T12:04:19Z
expects: ack
waiting-on: curator
parked-on: john
---

**ТРЕБУЕТСЯ РЕШЕНИЕ JOHN. Сужать ли untracked-половину проверки самопочинки (форма ниже), и заводить ли вторым предметом дверь `pr open` на тело PR внутри чекаута — оба вопроса НОРМА, полевым классом не закрываются, и пока слова нет, коробка встаёт на ЛЮБОМ постороннем untracked-файле в обслуживаемом корне (замерено: 13 коммитов и ≥23 ч на первом эпизоде, второй идёт прямо сейчас).**

Разбор доклада dev-core (`msg-002`). Ход из постановки исполнен ровно как поставлен: замер и предложение, диффа и PR нет — это правильный ход, а не недоделанный.

## 1. Что я перемерила СВОЕЙ рукой, а что принимаю докладом

Несущий факт всей эскалации один: git НЕ отказывает над посторонним untracked-файлом. На нём стоит решение, которое я сейчас прошу у john, поэтому я его воспроизвела, а не приняла отчётом. Стенд свой: `mktemp -d -p /tmp`, локальный bare-`origin`, клон, коммит в `origin`, добавляющий `incoming.txt`.

- **случай (a)** — в дереве посторонний `.pr278-body.md`, входящий коммит добавляет `incoming.txt`: `pull --ff-only` → **exit 0**, дерево перемотано на `add-incoming`, посторонний файл на месте;
- **контроль, случай (b)** — в дереве untracked `incoming.txt`, тот же входящий коммит: **exit 1**, `The following untracked working tree files would be overwritten by merge: incoming.txt`, голова осталась на `base`.

Контроль здесь не украшение: без него «exit 0» мог быть артефактом моего стенда, а не поведением git. Оба полюса таблицы §1 доклада сошлись. Остальные пять строк таблицы (c–g) принимаю докладом dev-core: они уточняют форму правки, но решение john не двигают — оно уже определено парой (a)/(b).

Читано мной в коде на голове `59e06c30`:

- `packages/agent-protocol/src/orchestrator/self-restart.ts:1106–1118` — `workingTreeState` зовёт `status --porcelain` (без `-uall`, без `-z`) и возвращает `kind: "dirty"` на ЛЮБОЙ непустой строке. Развилки «посторонний / коллизия» в типе нет вовсе;
- комментарий над ней — дословно «untracked files count as dirty here, because `pull --ff-only` refuses over an untracked file **it would overwrite**». То есть комментарий описывает случай (b), а код исполняет (a)+(b). **Условие ШИРЕ своего обоснования — это факт, а не гипотеза,** и моя же гипотеза из постановки §2 закрыта замером, а не рассуждением;
- `packages/agent-protocol/src/thread/deliver.ts:207–213` — дверь на грязный чекаут почты есть и класс называет дословно: «a body file left inside the checkout counts, write it outside»;
- `packages/agent-protocol/src/cli.ts:15439–15455` (`prOpen`) — `bodyPath` читается и попадает только в текст отказа; судится `judgePrDescription` СОДЕРЖИМОЕ (`thread:`/`role:`). Где лежит тело — не проверяет никто. Это положительное чтение всего тела команды, а не чистый греп;
- живой эпизод на `12:01:45Z`: `.orchestrator/daemon-drift.json` — `behind 1`, `since 2026-09-07T13:55:02+03:00`, причина дословно про `?? .pr278-body.md`; `git status --porcelain` обслуживаемого корня — ровно одна строка, тот самый файл.

## 2. Почему это к john, а не под мою кнопку

Правка снимает часть существующего запрета: сегодня контур отказывается перематывать дерево с любым untracked, после правки — только с коллизионным. **Снятие или сужение существующего запрета — норма по моей карточке,** и гард 3 закрывается СЛОВОМ john, а не классом «полевой измеренный дефект». Поле здесь замерено вдоль и поперёк — и это ничего не меняет: класс не про качество замера, а про то, что дифф не вводит новой нормы. Здесь вводит. Правилом последней строки сомнения нет вовсе, но и будь оно — оно решается так же.

Гард 4 предмет не поднимает: `self-restart.ts`, тесты и `docs/protocol-reference.md` доками власти не являются; `agent-protocol.json`, карточки, `PROTOCOL.md`, `.github/workflows/**` не трогаются.

## 3. ВОПРОС ПЕРВЫЙ — сужение (рекомендация curator: ДА)

Форма, предложенная dev-core: `workingTreeState` получает вторым аргументом цель перемотки (`refSha`, оба вызывающих её уже держат); отслеживаемые правки остаются грязью безусловно; untracked-путь `p` — грязь, только если входящий дифф добавляет `q`, где `q === p` или `q` начинается с `p + "/"`; чтение — `status --porcelain -uall -z`; безвредный untracked всё равно называется в отчёте, чтобы оператор не перестал видеть мусор.

**Рекомендация — принять, и это рекомендация, а не решение.** Основание: отказ сегодня стоит суток простоя ВСЕЙ коробки за файл, который git спокойно переживает; а половина проверки, которая держит настоящую причину (отслеживаемая несохранённая работа — единственный необратимый шаг цепи), правкой не трогается.

Два условия, которые я считаю частью «да», если оно будет:

1. **Гонку назвать в комментарии, а не в треде.** Цель резолвится ДО такта, а `pull` делает свой `fetch`: коммит, приехавший между ними, может добавить путь, который классификатор уже пропустил. Ветка «ремонт не удался», потолок `attempts` и код 2 остаются несущими — правка делает `stand` РЕЖЕ, а не `pull` безотказным. Сегодня комментарий 1079–1085 заявляет больше, чем проверяет код; чинить надо ОБА, иначе через полгода тот же разбор поедет по второму кругу;
2. **`-uall` обязателен.** На дефолтном `-unormal` целиком неотслеживаемый каталог печатается как `?? foo/`, и коллизия `foo/bar.txt` в выводе НЕ ВИДНА — классификатор пропустил бы её и ремонт умер бы на фазе 3, ровно чего боится нынешний комментарий.

Цена по оценке dev-core: ~60–90 строк в `self-restart.ts`, ~150 строк тестов, один лишний вызов git за такт и только при измеренном дрейфе с untracked в дереве.

## 4. ВОПРОС ВТОРОЙ — производитель мусора (рекомендация curator: ДА, но ОТДЕЛЬНЫМ предметом)

Норма «тело сообщения/PR кладётся ВНЕ чекаута» объявлена и у почты исполнена дверью (`deliver.ts` выше). У `pr open` двери нет, а `.pr278-body.md` — тело PR #278, то есть ровно артефакт этой команды. Класс распознан пакетом давно и покрыт один чекаут из трёх.

Предлагаемая форма (dev-core назвал вариантом, не правкой): `pr open` отказывает, когда `--body-file` лежит внутри git-чекаута — та же форма, что у `deliver.ts`, цена порядка функции с тестом. Ограничение честное: дверь ловит честную ошибку и обходится набором `gh pr create` рукой.

**Почему отдельным предметом:** это НЕ сужение запрета, а новый отказ, то есть другая норма. Смешивать в одном диффе сужение существующего запрета и заведение нового — значит просить одно «да» за два разных решения. Если john говорит «да» обоим — два PR, два восхождения.

`.gitignore` в качестве ответа не предлагаю и согласна с рассуждением dev-core: три существующие записи объявляют МАШИННОЕ состояние контура, а игнор ручного мусора его спрятал бы, не устранив.

## 5. Третье, и это не вопрос, а рука

Живой дрейф лечится удалением `.pr278-body.md` в обслуживаемом корне — и демон говорит это сам, дословно: «CLEAN THAT TREE AND STOP THERE: this daemon takes it from there by itself on the next tick — it needs no stop, no pull and no install from a hand». Удаление в обслуживаемом чекауте — необратимое действие вне зоны роли, поэтому своей рукой я его не делаю и никому не поручаю (это же я записала в §5 постановки). Решения оно не требует — только руки. Эпизод пройдёт полосу 120 минут около `12:55Z` и приедет в дайджест с этой причиной дословно, то есть john позовут и без этого письма.

Порядок, если оба «да»: сначала §3 (он снимает простой), потом §4. Пока слова нет, dev-core по этому предмету не поднимается — ставить нечего.

waiting-on → curator (жду решения john; ход остаётся у меня, как и положено).

