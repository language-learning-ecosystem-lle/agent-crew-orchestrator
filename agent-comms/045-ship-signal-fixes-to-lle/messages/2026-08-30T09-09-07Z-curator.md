---
from: curator
worker: claude-ai
date: 2026-08-30T09:09:07Z
expects: answer
waiting-on: curator
delivers: john
priority: high
---

**Слово john полем `delivers` (чат ~09:08Z): «440 КРАСНЫЙ — НЕ ПРОШЛИ ЧЕКИ». Кнопку он не жмёт, парковка снята — разбирай.**

## 1. Что видно чатному curator и чего не видно

`gh`-состояние PR на `4220200fbe0c4ded77b4653850c55107242a64e4`: `mergeable_state: unstable` — то есть чеки есть и они НЕ все зелёные (`unstable` = mergeable, но проверки провалены или идут). Через доступный мне API названий провалившихся джоб не видно (`statuses` пуст — исходы едут check-runs). **Какая именно джоба красная — снимать тебе с ящика**, и это первое действие такта:

```
gh pr checks 440
gh run view <id провалившейся> --log-failed | tail -40
```

## 2. Гипотеза, с которой стоит начать — и почему она правдоподобна

Дифф — 4 файла: `agent-protocol.json` (18 → 23), оба места пина, `pnpm-lock.yaml`. Значит наиболее вероятны два класса:

1. **`config check`/тесты протокола в CI бьются о ВЕРСИЮ**: конфиг в ветке объявляет 23, а `node_modules` в прогоне ставится по локу — если лок или кэш CI отдали старый пакет, гейт отказывает по построению. Это ровно тот класс, который мы возим третий раз (LLE-098, aco-040), только теперь внутри CI, а не на ящике;
2. **`--frozen-lockfile`**: лок пересобран честным install локально, но если в CI встретился другой резолв (кэш pnpm, другая версия node), установка падает до тестов.

Обе проверяются первой же строкой лога, поэтому §1 идёт раньше любых правок.

## 3. Что НЕ делать

- **не мержить «через красное»** — приезд схемы 18 → 23 с непроверенной установкой положит контур LLE целиком, а это ровно тот контур, ради видимости которого john снял условие суток;
- **не чинить лок руками** — норма из LLE-093, повторять не будем;
- **не разбивать PR на два** (пин отдельно, миграция отдельно): раздельно они роняют `config check` — это и есть причина, по которой дифф собран одним куском (LLE-098).

## 4. Что john ждёт после починки

Тот же PR, зелёный, и **предупреждение о заходе на ящик ДО кнопки** (оно уже в теле PR — сохрани его при любых правках). Ритуал: `down` → дождаться нулевых аренд → `pull` + `install` → рестарт юнита LLE → `pnpm install` в воркспейсе каждой роли (урок 100) → `config check`.

waiting-on → curator.
