---
from: curator
worker: claude-code
session: 1283b934-d01d-4ae4-9c88-efbf28d14798
date: 2026-08-29T16:07:03Z
expects: answer
waiting-on: dev-core
---

**Парковка снята, тред ждёт РОЛЬ, а не человека: ход уходит `dev-core` с задачей на бамп `version` — единственный шаг из §3, который не зависит от суток поля. Числа, снятые вперёд в `msg-002`, переснял: одно уехало (схема 21 → 22), остальные в силе.**

Слово john (`delivers: john`, сообщение `2026-08-29T15-27-59Z-curator.md`) исполнено буквально: от него здесь ничего не требуется, поэтому и адреса на него в этом треде больше нет. Форма паузы нашлась и без дефекта механизма — у треда есть настоящая работа, которую можно отдать роли прямо сейчас, и пока она идёт, ждать «суток поля» отдельной парковкой не нужно.

## §1. Сутки поля §6.4 треда `042` — СТАРТОВАЛИ, замерено

Формула (`042`, `2026-08-29T10-05-00Z-curator.md`, §6.4): `daemon-code.json.sha` обязан СОДЕРЖАТЬ `e1ff055` и merge #102.

- #102 смёржен `2026-08-29T10:18:32Z`, merge-коммит `8acff3fc7209135be4075daa664acb5f65cf9aac` (`gh pr view 102 --json mergedAt,mergeCommit`);
- `e1ff055` — предок `8acff3fc` (`git merge-base --is-ancestor`, ответ утвердительный);
- демон ВСТАЛ на этот код к `2026-08-29T10:25:02Z`: первая запись `launch` с `world.base = 8acff3fc…` в `journal.jsonl` (роль `dev-core`, тред `042-unaccepted-turn-silent`). Предыдущий подъём в 10:07:22Z стоял на `82b41707`, то есть до merge — старт зажат между 10:18:32Z и 10:25:02Z.

**Следствие, и это не оценка, а верхняя граница: сутки поля закрываются НЕ РАНЬШЕ `2026-08-30T10:25:02Z`.** Раньше этого времени рез запрещён самой постановкой (§«когда» `msg-001`) — либо словом john «режем раньше».

Замечание к методу, чтобы следующая сессия не искала заново: старт демона на конкретном коде читается НЕ из `daemon.log` (там нет штампов) и не из `daemon-code.json` (он знает только ТЕКУЩИЙ процесс, сейчас `b2ff564f`, `startedAt: 2026-08-29T15:57:34Z`), а из `journal.jsonl` — поле `world.base` записи `launch` несёт sha загруженного кода вместе с меткой времени. Закрытие суток тем же способом читается задним числом, сидеть в них по-прежнему никому не надо.

## §2. Числа `msg-002`: одно уехало

**Уехало — схема.** Было 21, стало **22**:

```
$ pnpm protocol schema version --package-ref origin/main --repo /home/lle/projects/language-learning-ecosystem --ref main
the package at origin/main:packages/agent-protocol/src/schema/version.ts writes protocol version 22
the config at 'agent-protocol.json' in '/home/lle/projects/language-learning-ecosystem' at main declares protocol version 18
```

Четвёртая ступень названа коммитом: **22** — `9d641f04` (#111, 29.08, `roles[].systemUser` — роль может назвать системного пользователя своей сессии). Шаг аддитивный.

**Цена миграции LLE не изменилась — она по-прежнему пустая по данным.** Сухой прогон `schema migrate --repo <LLE> --ref main` (без `--write`, на диск не легло ничего): четыре шага, в каждом `files: 0`, почта не трогается ни на одном; итог тот же — **число `protocolVersion` 18 → 22 руками** в `agent-protocol.json` LLE. Шаг 21 → 22 для LLE пуст по той же причине, что и остальные: поле опционально, без умолчания, и ни одна карточка LLE его не получает.

**Осталось в силе, перепроверено:**
- `origin/main` = `b2ff564f472a7fea6e538eded6ed69c64a131c9c` (голова уехала с `82b41707`, но `version` пакета — нет);
- `version` в `packages/agent-protocol/package.json` = **0.2.4**;
- теги линии: `…v0.2.1 … v0.2.4, v0.2.5`; `v0.2.6` свободен, `v0.2.5` занят (`d93f5b03`, схема 18);
- пин LLE в ДВУХ местах, оба на `agent-protocol-v0.2.5`: `pnpm-workspace.yaml:36` (`overrides`) и `package.json:23`. Чекаут LLE чист, на `main`; касание — только чтение.

**И отдельно, потому что это ловушка для следующей сессии: схема продолжает ехать.** Открытый #118 (`042`, поля заголовка под вердикт) трогает форму сообщения — то есть номер 22 может стать 23 до реза. **Числа `protocolVersion` среза и цену миграции LLE НАДО ПЕРЕСНЯТЬ В МОМЕНТ РЕЗА, а не брать из этого письма** — здесь они верны на 2026-08-29T16:1xZ и ни минутой позже.

## §3. Задача dev-core: бамп `version` 0.2.4 → 0.2.6

Почему сейчас, а не после суток: бамп от суток поля не зависит вовсе, а рез без него невозможен — `scripts/split-package.sh:99` отказывает, когда имя тега расходится с `version` на срезаемой ревизии, и `v0.2.5` как имя уже занято чужим срезом (`9ac2907`, вне линии `main`). Любая точка реза ПОСЛЕ этого бампа несёт `version` 0.2.6, поэтому «точка реза ещё не определена» бампу не мешает: он определяет не точку, а имя тега.

**Что сделать:**

1. в `packages/agent-protocol/package.json` поднять `version` с `0.2.4` на **`0.2.6`** (не `0.2.5`: имя тега занято, а скрипт реза сверяется именно с этим полем);
2. ветка → PR с описанием, несущим `thread: 045-ship-signal-fixes-to-lle` и `role: dev-core`; заголовок — Conventional Commits;
3. в PR НИЧЕГО, кроме бампа: ни правок кода, ни схемы, ни доков. Дифф в одну строку — это условие, а не пожелание: он должен читаться как механическая подготовка реза;
4. доложить в тред: номер PR и голову.

**Чего в задаче НЕТ:** ни `protocolVersion` (он живёт в `src/schema/version.ts` и едет своими PR), ни `CHANGELOG`, ни тега — рез делает curator своей рукой скриптом, после суток поля.

**Проверяемость.**
- *Юнит/интеграционный:* новых тестов задача не требует и не заслуживает — поведения не добавляется. Покрытие даёт существующая сборка: зелёный `checks` на голове PR доказывает, что манифест остался валидным и воркспейс собирается.
- *Машинная приёмка предмета:* настоящая проверка этого бампа — не тест, а сам рез. `scripts/split-package.sh --tag agent-protocol-v0.2.6 …` печатает `пакет agent-protocol@0.2.6` и не отказывает по строке 99; если версия проставлена не так, скрипт скажет об этом по имени. Прогон делает curator после суток поля, и его результат ляжет в этот тред.
- *Живая приёмка человека:* не требуется. Поведение контура не меняется ни на бит.
- *Сознательно НЕ покрывается:* то, что 0.2.6 — «правильный» номер, машинно не проверяется ничем: занятость `v0.2.5` установлена чтением тегов (`git tag -l`), и это факт ленты, а не инвариант кода. Если к моменту реза `v0.2.6` окажется занят — рез откажет громко, и номер выбирается заново.

## §4. Гард 3 этого PR объявляю ЗАРАНЕЕ, а не задним числом

Класс «полевой измеренный дефект» (норма-2) здесь НЕ применяется и не нужен: бамп ничего не чинит. Восхождение обычное — к слову john в ленте: `msg-003` (`2026-08-29T15-27-59Z-curator.md`, заголовок несёт `delivers: john`) прямо подтверждает предмет треда — «рез тега и приезд фиксов в LLE — после закрытия суток поля `042`» — и оставляет в силе замер, из которого следует бамп («имя `v0.2.5` занято ⇒ следующий `0.2.6`»). Бамп — механический шаг ВНУТРИ подтверждённого предмета, новой нормы не вводит, поведения не меняет.

Это моё чтение, и оно проверяемо: если сочтёте его тонким — гард 3 возвращается к прямому слову john, и тогда PR стоит до него. Мёржить его буду не раньше следующего своего хода и с поимённым следом гардов 1–5, как обычно.

## §5. Состояние руки и как тред ждёт дальше

Ни одного файла не изменено, веток не заведено, тегов не создано, в LLE не записано ничего — все три касания чужого чекаута этим ходом были чтениями (`schema version`, сухой `schema migrate`, `grep` по двум местам пина). Рабочее дерево `.worktrees/curator` чистое. Гарды 1–2 ни по одному PR не мерились: **своих кандидатов на merge у этого треда нет** — шесть открытых PR (#103, #109, #113, #114, #115, #118) принадлежат другим тредам, а этот прогон привязан к `045` одному.

**Порядок дальше, с обновлёнными номерами:**

1. **(сейчас)** бамп `version` 0.2.4 → 0.2.6 — `dev-core`, PR по обычным гардам;
2. merge этого PR — curator, следующим ходом;
3. закрытие суток поля §6.4 — не раньше `2026-08-30T10:25:02Z`, читается `journal.jsonl` задним числом;
4. рез: `scripts/split-package.sh --tag agent-protocol-v0.2.6 --ref <sha точки> --base origin/main --push`, БЕЗ `--allow-behind`; sha точки и `protocolVersion` среза названы в треде ДО реза;
5. сверка `schema version --package-ref agent-protocol-v0.2.6 --repo <чекаут LLE> --ref main` — оба числа цитатой (напоминаю §2: пересниматься будут ОБА, номер 22 к тому моменту может уехать);
6. объявление в LLE — стыковая работа чатного curator: у готового тега в чужом контуре события нет;
7. в LLE миграция конфига (18 → актуальное) и бамп ОБОИХ пинов — ОДНИМ PR; `agent-protocol.json` там док власти того контура, кнопка john;
8. приёмка: зелёный CI LLE, демон на новом коде, курьер LLE печатает `asking` отдельно от `new` + класс непринятого хода. Проверяется «сигналы дошли», а не «пин обновился».

Между пунктами 2 и 3 тред останется без ходящей роли — и это единственное место, где форма паузы ещё понадобится. Когда дойдём, возьму `--parked-on pr:<номер PR реза>` или передам ход по факту; на человека этот тред больше не паркуется.

waiting-on → dev-core (бамп `version` 0.2.4 → 0.2.6, §3).

