---
from: curator
worker: claude-code
session: bb27f62a-5251-411f-8c13-9c090683b83a
date: 2026-08-30T09:00:12Z
expects: ack
waiting-on: curator
parked-on: john
---

**Требуется решение john: кнопка на LLE PR #440 (пин + миграция одним диффом) и ритуал на ящике сразу после неё — `git pull --ff-only && pnpm install && systemctl --user restart lle-orchestrator@lle-hetzner`. Без ритуала демон LLE упрётся в версионный гейт (конфиг 23, код на диске старый).**

Твоё «ВЕЗИ» исполнено до упора, который упирается в твою руку: **тег `agent-protocol-v0.2.6` вырезан и на удалённом, PR миграции в LLE открыт.** `agent-protocol.json` — док власти контура LLE, поэтому дальше не моя кнопка.

## §1. Рез тега — сделан, числа названы командой

Точка реза — голова `main` этого контура на момент реза: `2edfa0ea04639e3c10501f64252024f9ff061599`. `version` пакета на ней — `0.2.6` (сел мёржем #122, `74576782`). Резал строкой из постановки, **БЕЗ `--allow-behind`** — гард полноты линии отработал молча, то есть точка несёт `origin/main` целиком:

```
$ bash scripts/split-package.sh --tag agent-protocol-v0.2.6 --ref 2edfa0ea… --base origin/main --push
protocolVersion этот тег пишет: 23 (из 'packages/agent-protocol/src/schema/version.ts')
режу 'packages/agent-protocol' с 2edfa0ea… → тег 'agent-protocol-v0.2.6' (пакет agent-protocol@0.2.6)
срез f3210af1c94c1328f2f7079e3628acfd735f0007, верхний уровень его дерева:
  README.md  package.json  spike  src  tsconfig.json  vitest.config.ts
тег 'agent-protocol-v0.2.6' создан на f3210af1… (пакет agent-protocol@0.2.6, protocolVersion 23)
 * [new tag]  agent-protocol-v0.2.6 -> agent-protocol-v0.2.6
```

**Контрольное чтение цели, а не ответа инструмента** (`git ls-remote --tags origin`): `f3210af1c94c1328f2f7079e3628acfd735f0007  refs/tags/agent-protocol-v0.2.6`. Корень дерева среза — сам пакет, как и требует норма тега.

**Предупреждение прошлой руки исполнено: числа переснял в момент реза, из письма не брал.** `protocolVersion` среза — **23**, а не 22: между замером и резом сели #109 (`launch.fallback`, ступень 22 → 23) и остальные. Именно 23 и уехало в LLE.

## §2. Сверка схемы — оба числа цитатой, MISMATCH здесь НОРМА

```
$ pnpm protocol schema version --package-ref agent-protocol-v0.2.6 --repo /home/lle/projects/language-learning-ecosystem --ref main
the package at agent-protocol-v0.2.6:src/schema/version.ts writes protocol version 23
the config at 'agent-protocol.json' in '…/language-learning-ecosystem' at main declares protocol version 18
the repository declares protocol version 18, the package writes 23 — run 'agent-protocol schema migrate'
```

Пять ступеней разом (18→19→20→21→22→23) — это самый длинный переезд пина в истории стыка, и цена его замерена, а не оценена. Сухой прогон `schema migrate --repo <LLE> --ref main` (без `--write`, на диск не легло ничего): **в каждой из пяти ступеней `files: 0`, почта не трогается ни на одной.** Единственная СУЖАЮЩАЯ ступень (20 → 21, снятие `effort: minimal` у codex) для LLE пуста по данным: ни одна карточка того конфига не называет codex и ни одна не несёт `minimal`. Итог миграции — **число, и только число**.

## §3. LLE PR #440 — миграция и оба пина ОДНИМ диффом

`https://github.com/language-learning-ecosystem-lle/language-learning-ecosystem/pull/440`, ветка `curator/agent-protocol-v0-2-6-pin`, голова **`4220200fbe0c4ded77b4653850c55107242a64e4`**. Четыре файла:

- `agent-protocol.json:2` — `protocolVersion` 18 → 23;
- `package.json:23` **и** `pnpm-workspace.yaml:36` — оба места пина на `agent-protocol-v0.2.6`. Оба, потому что `overrides` перебивает манифест: правка одного роняет контур;
- `pnpm-lock.yaml` — **честным `pnpm install`, exit 0**, резолв на sha тега: `codeload…/tar.gz/f3210af1c94c1328f2f7079e3628acfd735f0007`, `version: 0.2.6`. Лок не правился рукой.

Одним PR — намеренно, по твоему же выводу и уроку LLE-098: раздельно пин на коде 23 при конфиге 18 роняет `config check` контура.

**Как это сделано, чтобы контур LLE не тряхнуло раньше времени:** работал во ВРЕМЕННОМ воркдереве LLE со своим `node_modules`. Живые чекауты LLE не тронуты — ни `main` (чистый, `d1ed6512b`), ни воркдеревья ролей, ни `node_modules` демона. Временное дерево снято после пуша.

**Оговорка, которую записываю как факт, а не прячу:** первая попытка коммита шла из воркдерева в `/tmp`, и **дверь зон там не отработала вовсе** — `git -C <workdir>/.. rev-parse --show-toplevel` вернул «not a git repository», хук упал кодом 2, коммита не было. Я НЕ обходил её `--no-verify`: перенёс дерево внутрь `.worktrees` репозитория LLE и получил настоящий вердикт — `✔️ zones`, вместе с `✔️ biome` и `✔️ typecheck` (10/10 пакетов). Дверь зон судит дифф из четырёх файлов, и она его пропустила.

Ещё одна: пуш сначала отбился (`push declined due to email privacy restrictions`) — автор перевыставлен на `maysway <308853635+…@users.noreply.github.com>`, как все коммиты того репозитория. Дифф от этого не изменился.

## §4. Что от тебя нужно, по шагам

1. **Кнопка на #440.** Дождись зелёного `checks` на голове `4220200f` — на момент письма `checks` шли (`pending`, прогон `33302886931`), `pronunciation` уже `pass`;
2. **Сразу после merge — ритуал на ящике**, до того как демон LLE поднимет следующую сессию: `git pull --ff-only && pnpm install && systemctl --user restart lle-orchestrator@lle-hetzner`;
3. **`pnpm install` в воркспейсе КАЖДОЙ роли LLE** (урок 100): пакет там внешний, рассинхрон `node_modules` роли кладёт её сессии.

## §5. Приёмка — что считать «доехало», и цена спешки

Проверяется не «пин обновился», а **сигналы дошли**: зелёный CI на новой голове LLE; демон LLE на новом коде; **курьер LLE печатает `asking` отдельно от `new`** и класс непринятого хода; первая парковка на john в LLE звонит и напоминает по возрасту. Последнее — то, ради чего ты снял сутки поля.

**Цена спешки, названная в msg-011, остаётся в силе и её надо помнить при чтении первой странности в LLE:** этот код обкатан дома часами, а не сутками. Первую же аномалию там читать как возможный дефект приезда, а не как «показалось».

## §6. Состояние руки и что НЕ сделано

Рабочее дерево `.worktrees/curator` чистое, незакоммиченного нет. В этом контуре я не открывал и не мёржил ничего: пять открытых PR (#114, #120, #123, #124, #125) принадлежат другим тредам, своих кандидатов на кнопку у `045` нет.

**НЕ сделано сознательно, и передаю следующей руке:**

- **объявление в почте контура LLE** — §3.3 постановки отдаёт его ЧАТНОМУ curator, и я его не подменяю. Сейчас у #440 в том контуре нет держателя: пока john не нажал кнопку, приёмке всё равно не за что взяться, поэтому объявление правильнее донести ПОСЛЕ merge — вместе с предметом приёмки;
- **приёмка §5** — она физически невозможна до merge и ритуала.

## §7. Форма ожидания и её цена

Парковка на john здесь НЕ режимная и не ложная: от него требуется действие, которого не сделает никто другой — кнопка на доке власти чужого контура плюс ритуал на ящике. Поэтому `expects: ack` и `parked-on: john`, вопрос первой строкой — звонок уходит сразу, без возрастного порога.

**На `pr:440` не паркуюсь, и это не забывчивость: вотчер парковок смотрит PR ЭТОГО контура, у чужого номера здесь будильника нет** — событие merge из LLE в этот тред не приедет никогда. Значит после ответа john предмет забирает рука curator и читает исход #440 своей командой, а не ждёт события.

waiting-on → curator (держатель предмета), parked-on john (кнопка #440 + ритуал).

