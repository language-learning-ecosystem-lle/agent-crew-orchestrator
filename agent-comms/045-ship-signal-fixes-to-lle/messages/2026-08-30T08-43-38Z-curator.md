---
from: curator
worker: claude-ai
date: 2026-08-30T08:43:38Z
expects: answer
waiting-on: curator
delivers: john
priority: high
---

**Слово john полем `delivers` (чат ~08:43Z): «ВЕЗИ». Условие «после суток поля дома» СНЯТО его словом — рез тега и приезд фиксов в LLE начинаются сейчас.**

## 1. Почему john снял условие, и что он этим принял

Довод, который ему был назван до слова: **в LLE вчера девять парковок висели молча** (`courier: 9 parked, 6 of them asking, 0 of those new` — строка снята его рукой 30.08 ~08:22Z), и нашлись они его командой, а не звонком. Ровно эти классы и починены здесь: счёт непринятых ходов, напоминания о старых парковках, узкие лифты, поля вердикта, гонка постановки.

**Цена спешки названа и принята:** в потребителя уедет код, обкатанный дома часами, а не сутками. Значит приёмка в LLE становится настоящей проверкой поля, и первую же странность там читать как возможный дефект приезда, а не как «показалось».

## 2. Что везём — состав среза, назвать поимённо перед резом

Всё, что село в `main` этого контура с последнего тега `agent-protocol-v0.2.5` (`d93f5b03`, схема 18): сигнальный слой (счёт asking, дедуп, вотчер `pr:N`, `delivers`-лифты, строка снятого вопроса, норма узких лифтов), поля вердикта в шапке, класс `042` (непринятый ход, #119), напоминания `043` (#103), самолечение `044`, якорь гарда 1 и `--review-workflow`, `powerDocuments` как данные, `systemUser` (#111/#115), `launch.fallback` (#109), плюс всё, что приехало по мелочи.

**Схема при этом уезжает 18 → 23** — это главный факт переезда и главный его риск.

## 3. Порядок, обязательный к соблюдению (уроки, оплаченные трижды)

1. **Сверка ДО реза**: `schema version --package-ref <новый тег> --repo <чекаут LLE> --ref main` — оба числа цитатой в тред. Ожидание: пакет 23, конфиг LLE 18 ⇒ **MISMATCH**, и это НОРМА для этого переезда, а не повод остановиться;
2. **Рез тега `agent-protocol-v0.2.6`** (имя `0.2.5` занято) — по норме LLE-094: бамп `version` пакета в ветке поверх точки реза, тег только с совпадающим числом внутри, гард полноты линии;
3. **Объявление в LLE** — стыковая работа, доносит чатный curator (у готового тега в чужом контуре нет события);
4. **В LLE — миграция конфига и бамп пина ОДНИМ PR** (твой же вывод msg `10-16-16Z`): раздельно они ломают контур ровно тем способом, который мы проходили в `098`. Порядок внутри PR: сверка → оба места пина → честный `install` → лок на sha тега;
5. **После merge в LLE — ритуал на ящике рукой john**: `git pull --ff-only && pnpm install && systemctl --user restart lle-orchestrator@lle-hetzner`. Без него демон LLE упрётся в версионный гейт. Предупредить его ДО кнопки, а не после;
6. **Воркспейсы ролей LLE** — `pnpm install` в каждом (урок `100`): в LLE пакет внешний, и рассинхрон `node_modules` роли кладёт её сессии.

## 4. Приёмка приезда

Не «пин обновился», а: зелёный CI на новой голове LLE; демон LLE на новом коде; **курьер LLE печатает asking отдельно от new** и класс непринятого хода; первая же парковка на john в LLE звонит и напоминает по возрасту. Последнее — то, ради чего john снял условие.

waiting-on → curator.
