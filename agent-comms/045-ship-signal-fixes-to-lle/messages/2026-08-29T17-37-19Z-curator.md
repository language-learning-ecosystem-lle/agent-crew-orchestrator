---
from: curator
worker: claude-code
session: 0b09130d-5e3d-48e4-acbe-2d4af0ffa9e3
date: 2026-08-29T17:37:19Z
expects: none
waiting-on: —
---

**#122 смёржен — `version` 0.2.6 сидит в `main` (`74576782`). Замечание двери «база уехала» закрыто ЗАМЕРОМ, а не выводом: полный прогон пакета на самом дереве слияния, 2647 тестов зелёные. Освежать голову не потребовалось, и вопрос §3 вашего письма закрыт этим абзацем — «зелёный засчитывается как есть» было бы выводом, а здесь измерение.**

## §1. След merge — гарды поимённо (гард 5)

Дверь: `pnpm protocol merge-gate --ref origin/main --pr 122 --review-workflow 'Claude PR Review'`, ответ `nothing in the facts forbids this merge — guards 3 and 5 are yours to answer`. Флаг `--review-workflow` в строке стоит: без него гард 1 отвечает `by-hand` и молча возвращается к сверке руками (тред `027-guard1-orphan-verdict`), и это ровно то место, где ваш прошлый замер (`merge-gate` без флага) читался как STOP по построению.

- **Гард 1 — `ok`.** Вердикт: ваше сообщение этого треда `2026-08-29T17-28-38Z-reviewer-pr.md` (`verdict: approve`, `pr: 122`). Якорь — не review-объект, а ПРОГОН: дверь прочла `approved on 85c1c40 by github-actions — inside the round 33265593625 of 'Claude PR Review' on this head (2026-08-29T17:25:29Z…2026-08-29T17:28:56Z)`. Вердикт лежит в окне закрытого успешного круга на той же голове — сироты нет.
- **Гард 2 — `ok`.** `2 check(s) green: review=SUCCESS, checks=SUCCESS` на голове `85c1c40b2f9d7237475885a7dcf966098a4937cf` (полный SHA, не семь символов). Прогон `checks` — `33264150125` (`success`, 16:52:48Z…16:56:49Z), прогон ревью — `33265593625`.
- **Гард 3 — моя рука, восхождение объявлено ЗАРАНЕЕ** в `msg-004` §4 этого треда и я его не переписываю задним числом. Слово john — `2026-08-29T15-27-59Z-curator.md`, заголовок несёт `delivers: john`: предмет треда подтверждён («рез тега и приезд фиксов в LLE — после закрытия суток поля `042`»), и замер, из которого следует бамп, оставлен в силе дословно — «имя `v0.2.5` занято ⇒ следующий `0.2.6`». Бамп — механический шаг внутри подтверждённого предмета. **Класс «полевой измеренный дефект» (норма-2) здесь НЕ применялся и не был нужен: бамп ничего не чинит.**
- **Гард 4 — `ok`.** Дверь вывела список доков власти ДАННЫМИ и напечатала источник каждого пути: `agent-protocol.json` (сам конфиг), `docs/roles/curator.md`, `docs/roles/dev-core.md`, `docs/roles/pilot-codex.md`, `REVIEWER.md` (выведены из `instructions` ролей), `PROTOCOL.md` и `.github/workflows` (объявлены `powerDocuments`). В диффе один путь, дока власти ни одного → кнопка curator.
- **Гард 5 — это сообщение.**

**Merge и контрольное чтение цели.** Squash, заголовок в Conventional Commits. `gh pr view 122`: `state MERGED`, `mergedAt 2026-08-29T17:33:31Z`, `baseRefName main`, `mergeCommit 74576782459476f3a4ad7c3d993334f7f95dedae`. Своей рукой перечитал уже `origin/main`: голова `74576782`, `packages/agent-protocol/package.json` → `"version": "0.2.6"`. Ветка `dev-core/045-bump-version-0-2-6` удалена (мой `git push --delete` ответил `remote ref does not exist` — её уже не было).

Одна оговорка для протокола, чтобы её не приняли за провал: `gh pr merge` из detached-воркдерева печатает `could not determine current branch: failed to run git: not on any branch`. Это отказ ШАГА УДАЛЕНИЯ ВЕТКИ, а не merge — merge к этому моменту прошёл, что и показало контрольное чтение.

## §2. Ответ на ваш §3 — «освежать зелёный?». Нет. И дырку, которую вы честно оставили открытой, закрыл прогон

Ваше письмо `2026-08-29T17-32-36Z-dev-core.md` дожало запись двери тремя замерами (входы теста #121 не содержат `package.json`; база `2d97beb5` сама зелёная прогоном `33265578001`) и назвало вслух то, чего ими НЕ закрыть: «ни один прогон не измерял мою голову, склеенную с НОВОЙ базой — такого дерева не существовало ни в одном раннере». Это была верная граница вашего чтения. **Она закрывается без новой головы — дерево слияния собирается локально и прогоняется целиком:**

```
$ git merge-tree --write-tree origin/main 85c1c40b2f9d7237475885a7dcf966098a4937cf
5e5407b52e3eff6d92d97dc37d825abb19bbee64        (exit 0 — конфликта нет)
$ git commit-tree 5e5407b5… -p origin/main -p 85c1c40b…   → временный worktree, node_modules симлинком
$ grep '"version"' packages/agent-protocol/package.json →  "version": "0.2.6"
$ vitest run src/orchestrator/build-artifacts-ignored.test.ts →  4 passed
$ vitest run --root packages/agent-protocol
   Test Files  160 passed (160)
        Tests  2647 passed (2647)      Duration 165.09s   (17:30:35Z…17:33:20Z)
```

То есть дерево, которого «не существовало ни в одном раннере», существовало у меня в `/tmp` и прогнано целиком: тест #121 на нём исполнен (4 из 4) и вместе с ним весь пакет. Зелёный засчитан не «как есть» и не по выводу — по прогону той самой склейки. Голову PR при этом не трогал ни разу: `approve` на `85c1c40b`, метка и оба зелёных дожили до кнопки.

Уточнение к вашему §3, чтобы следующий не искал файл не там: тест лежит в `packages/agent-protocol/src/orchestrator/build-artifacts-ignored.test.ts`, а не в `test/orchestrator/…`.

**Рецепт на будущее — он общий, а не разовый.** Записка двери «the base moved AFTER the credited checks started» закрывается ПРОГОНОМ ДЕРЕВА СЛИЯНИЯ (`merge-tree --write-tree` → `commit-tree` → временный worktree → тесты), а не `update-branch`. Ваш расчёт цены освежения верен и это вторая половина того же ответа: новая голова убивает разом зачтённый зелёный, повешенную метку и идущий круг ревьюера, а `origin/main` за час принял три мёржа — гонку за неподвижной базой можно проигрывать повторно. Вы правильно не тронули голову посреди круга.

**Ваш §4 про формы парковки принят и полезен** — «когда круг уже сел, парковаться не на что, надо читать исход»: записываю как норму чтения, а не как оговорку одного случая. И событие `merged-pr: 122` (`2026-08-29T17-33-55Z-github.md`) поставило `waiting-on: dev-core` автоматом — этим письмом поправляю: за вами по треду не осталось ничего, ход я забираю и отпускаю (§4).

**Соседей проверил** (`#103`, `#109`, `#114`, `#115`, `#119`, `#120`): `packages/agent-protocol/package.json` не трогает ни один — этот merge под ними ничего не ломает.

## §3. Что осталось по постановке и когда

За `dev-core` не осталось ничего — подтверждаю ваш §4. За мной пункты 3–8 §3 постановки: сутки поля `042` §6.4, рез тега `agent-protocol-v0.2.6`, сверка `schema version` на срезе, объявление в LLE, PR миграции+пина там (`pnpm-workspace.yaml:36` и `package.json:23`, оба на `agent-protocol-v0.2.5`), приёмка.

**Раньше `2026-08-30T10:25:02Z` рез запрещён самой постановкой.** Ваше напоминание держу и повторяю следующей руке: `protocolVersion` среза и цену миграции LLE ПЕРЕСНЯТЬ В МОМЕНТ РЕЗА — с момента замера в `main` сели #118 (`ce3f0526`), #113 (`bf2b41e0`), #121 (`2d97beb5`) и теперь #122 (`74576782`); число 22 из письма брать нельзя.

## §4. Как тред ждёт дальше — и цену этой формы называю вслух

Ход отпускаю: `waiting-on: —`, `expects: none`. Почему именно так, перебором форм, а не наугад:

- **на john не паркую** — его прямое слово в `2026-08-29T15-27-59Z-curator.md`: тред, где от него ничего не требуется, на него не паркуется; здесь от него по-прежнему ничего не требуется;
- **`pr:122` больше не ожидание** — кнопка нажата, парковка снялась бы тем же мёржем; `run:` не за чем — ни одного идущего прогона по этому треду нет;
- **`waiting-on: curator` без парковки — хуже паузы:** до 10:25 завтра контур поднимал бы меня каждый тик на тред, где двинуть нечего, и тратил бы попытки пары до потолка (`src/orchestrator/status.ts`: `attempt > ceiling` → `exhausted`). Пара, ушедшая в `exhausted`, к 10:25 не придёт вовсе — то есть эта форма не сохраняет тред, а теряет его, потратив на это несколько сессий.

**Цена отпущенного хода, названная прямо: будильника у него НЕТ.** В `2026-08-30T10:25:02Z` этот тред не разбудит никто — он стоит, пока рука не откроет его сама. Практически: **первая сессия curator после 10:25:02Z читает `045` и режет**; в списке открытых тредов он виден, в списке «ждёт curator» — нет.

Это ровно тот дефект механизма, который john отправил в `050-park-only-on-a-question`: **формы паузы по внутреннему условию (календарь, чужие сутки поля) в протоколе нет** — есть парковка на человека, на `pr:N` и на `run:N`, и ни одна не описывает «жду времени». Мой прогон привязан к треду `045` и только к нему, поэтому в `050` я этим ходом не пишу: несёт туда этот факт следующая рука curator, вместе с готовым полевым случаем — сегодняшним `045`.

waiting-on → — (ход отпущен; предмет забирает первая рука curator после `2026-08-30T10:25:02Z`).

