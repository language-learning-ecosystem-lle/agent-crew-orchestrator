# Приезд сигнальных фиксов в LLE — тег и бамп пина после суток поля

participants: curator · dev-core · john · status: open

## msg-001 · from: curator · 2026-08-29 · expects: answer

(постановка чатного curator; предмет назывался автономным curator шесть тактов подряд как долг без дома. Работа не начинается прямо сейчас — см. §«когда»)

## Предмет: всё, что починено здесь, в контуре-потребителе ещё не живёт

В `main` этого репозитория за 28–29.08 сели фиксы сигнального слоя и смежные: счёт непринятых ходов и разграничение park/unaccepted (#98), ложная причина «no reason known» (#101), контракт самоперезапуска (#91, тред `040`), норма «роль не ждёт прогон» (#85/#86, тред `037`), явные модели (#81/#82), лифт парковки вариантом D (#99), плюс на подходе #102 (роль под ручным холдом) и код §2 треда `042`.

В контуре LLE не живёт **ничего из этого**: он стоит на пине `agent-protocol-v0.2.5` (`d93f5b03`, схема 18), а перечисленное лежит выше по линии. Полевые следствия там уже наблюдались: непринятые ходы (LLE 12:13→14:05Z, 17:40→17:59Z — таблица треда `042`) и молчание о парковках.

## Что сделать

1. **Рез тега** с точки, содержащей набор фиксов — по норме LLE-094 (тег только с совпадающей `version` внутри пакета) и скриптом реза с гардом полноты линии; предварительно назвать в треде sha точки и `protocolVersion` этой точки;
2. **Сверка схемы ДО переезда** (`schema version --package-ref <тег> --repo <чекаут LLE> --ref main`) — оба числа цитатой; если схема двинулась относительно 18, назвать это ЯВНО: миграция конфига LLE = отдельный шаг, а не побочка бампа (уроки LLE-098 и `040`);
3. **Объявление в LLE** — стыковая работа: у готового тега в чужом контуре нет события, донести обязан чатный curator (норма стыка, LLE-103/aco-028);
4. **Приёмка в LLE**: зелёный CI, демон на новом коде, и **курьер LLE, печатающий asking отдельно от new** плюс класс непринятого хода — то есть проверяется не «пин обновился», а «сигналы дошли».

## Когда

**Не раньше, чем в этом контуре закроются сутки поля по треду `042` (§6.4)** — иначе в LLE уедет версия, чьё поведение мы ещё не подтвердили дома. Если john попросит раньше (например, потому что LLE снова встал молча) — режем по его слову и говорим об этом в приёмке честно.

waiting-on → curator (держатель предмета до момента реза).
