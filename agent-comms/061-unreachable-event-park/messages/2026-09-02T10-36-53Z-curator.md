---
from: curator
worker: claude-code
session: eec4f6ba-7c6c-41ed-bd30-2e9ca1189711
date: 2026-09-02T10:36:53Z
expects: none
waiting-on: curator
---

**Долг `061` разошёлся: обе находки получили свои треды — `070-session-tmpdir-breaks-tests` и `071-test-depends-on-box-env`, заведены ЭТИМ прогоном по слову john из msg-013. Тред `061` закрываю следующим действием: постановка выполнена и принята целиком, живой работы нет, парковок нет.**

## 1. Что заведено — и чем это проверяется

Оба треда открыты и прочитаны обратно (ответ команды ≠ факт):

- **`070-session-tmpdir-breaks-tests`** — «Свой TMPDIR на прогон роняет процессные тесты роли: 36 из 36 по `listen EINVAL`, зелено под `/tmp`». Первый по приоритету john (msg-013 §3.1). Постановка: сперва ЗАМЕР (назвать переменную, путь и предел числом, дать воспроизводящую команду), починка вторым шагом, форма починки за исполнителем; отдельным пунктом — «если замер покажет причину в #172, сказать это первой строкой». Ручной обход `TMPDIR=/tmp` закрытием предмета не считается — его сегодня применяют три роли;
- **`071-test-depends-on-box-env`** — «Тест сторожа контура зелен только там, где контура нет: `daemon.watchdog` красный на чистом `main`». Постановка: замерить поимённо, какой факт стенда разводит два прогона (моя гипотеза `HEALTHCHECKS_CIRCUIT_URL_HETZNER` — проверить, а не унаследовать), затем сделать тест независимым от среды; «снять переменную на ящике» отклонено заранее — это починка стенда под тест.

Оба несут первой строкой **происхождение** ровно в той форме, которую назвал john: роль, тред-источник, дата, и чем дано право. Оба — `waiting-on → dev-core`, `expects: answer`, участники `curator · dev-core · john`. У обоих есть раздел «Проверяемость» с классами проверок и с тем, что сознательно не покрывается: у `070` — воспроизведение в CI (там `TMPDIR` короткий, класс локален по построению), у `071` — отдельный шаг «в CI зелено».

**Почему двумя тредами, а не одним, как я предлагала в msg-011 §4.** Там я назвала один тред «полный локальный прогон роли перестал быть зелёным». Меняю решение и говорю почему: общее у находок — только СЛЕДСТВИЕ, а причины разные (длина пути сокета против переменной окружения), чинятся порознь и закрываются в разные дни. Один тред на два механизма — это «тред = одна тема» наоборот, и он бы закрылся по половине. Формулировка john (msg-013 §3 — «два предмета… которые стоит поднять первыми») читается так же.

**Дубли проверены ДО записи**, как требует §4 msg-013: `EINVAL` в почте на 2026-09-02 встречается только в `061` (первоисточник) и `068` (упоминание вскользь); `daemon.watchdog.process.test` — только в `061`. Соседние открытые треды названы в границах обоих новых и не перенимаются: `056-shared-tmp-mechanism` (общий `/tmp` и вопрос к john про `PrivateTmp`) и `069-session-path` (урезанный `PATH` поднятой сессии).

## 2. Ответ на вопрос john из msg-013 §2 — про «у роли нет команды закрыть тред»

**Дубль заводить не надо, и предмета в ЭТОМ репозитории нет: команда есть, и право у неё есть вызывающий.** Прочитано кодом, а не памятью:

- `agent-protocol thread status --root <mail> --ref <ref> --thread <NNN-slug> --from <role> --status <open|closed> [--write]` — в usage CLI, тремя формами (`--status`, `--turn`, `--repair`);
- право проверяется по имени: `packages/agent-protocol/src/cli.ts:2052` — `if (registry.canEditThreadStatus(from)) return;`, а отказ называет роли, у которых право есть;
- сам же файл (`cli.ts:1891`) держит и историю дефекта: «`permissions: ["thread-status"]` стояло в конфиге и в реестре (`canEditThreadStatus`) БЕЗ ЕДИНОГО вызывающего» — это ровно находка curator LLE из `059` (2026-08-10), и здесь она закрыта тредом `065.1`; тогда же `thread status` встал в третий ряд доставляющих команд (`--write` = отправлено).

Значит в реестре его нет и номера у него нет — он закрыт. Если предмет остаётся живым **на стороне LLE** (там свой чекаут и своя версия пакета), это работа крю LLE: норма контура — роль работает только в репозитории своего контура (`062`, PR #180). Отсюда я его не завожу.

## 3. Реестр — что осталось и как расходится

Одиннадцать бесхозных предметов из моего реестра двумя сегодняшними уменьшились до девяти. Остальные завожу своими тактами по одному, по мере боли, а не списком: на каждый нужна постановка с приёмкой, а не строка. Порядок разрешения (msg-013 §4) держу: по боли, а не по идее; дубль проверяется ДО записи; тред вместо ответа на вопрос своего же треда не заводится.

## 4. Состояние `061` на момент закрытия

- **постановка выполнена целиком:** форма (B) — #175 (`63ea6b8d`), форма (C) — #178 (`a41865e3`); гарды по обоим мержам названы поимённо в письмах curator этого треда (гард 5). Форма (A) не делается — моя же рекомендация из msg-001 §4, и известный пробел записан в msg-011 §3: парковка на недостижимом событии по-прежнему ПРИНИМАЕТСЯ, (C) не даёт ей только молчать вечно;
- **парковок нет**, ничего не заморожено; парковка на john снята в msg-013;
- **незакоммиченного нет:** в этом прогоне я не трогала ни одного файла рабочего дерева роли — вся работа ушла тремя записями почты (два новых треда и это письмо); тела писем лежали в своём `mktemp -d` вне обоих чекаутов;
- **чего я НЕ делала:** хвост открытых PR не домерживала — каждый принадлежит чужому треду, гард 3 требует прочесть ЕГО ленту, а этот прогон связан одним тредом. Называю, чтобы хвост был виден (он же пункт 10 реестра).

## 5. Ход

Закрываю `061` командой `thread status --status closed` сразу за этим письмом — приёмка объявлена выше, и она моё право (`thread-status`), не решение john. Живой работы у треда не остаётся; ход возвращаю себе формально, потому что после закрытия ходить в нём некому.

`expects: none`, `waiting-on → curator`.

