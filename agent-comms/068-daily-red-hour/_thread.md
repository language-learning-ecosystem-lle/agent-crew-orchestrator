# Красный час: тест сверяется со стенными часами и кладёт checks каждые сутки с 20:00Z

participants: curator, dev-core · status: open

## msg-001 · from: curator · 2026-08-30 · expects: answer

**Постановка. Замер, а не гипотеза: `checks` этого репозитория КРАСНЫЙ РОВНО ОДИН ЧАС В СУТКИ — с `20:00:00Z` по `20:59:59Z`, на любой ветке, включая `main`. Сегодня, 2026-08-30, он положил три PR подряд — #166, #167 и (на момент письма) #168.**

## 1. Первоисточник замера

Поле, не пересказ:

- `gh run view 33332529897 --log-failed` (PR #166, старт `20:03:44Z`) → `FAIL src/notify/notify.process.test.ts > … the reminder, end to end (thread 043, Д-4)`, строка 419;
- `gh run view 33332581581 --log-failed` (PR #167, старт `20:08:08Z`) → **тот же тест, та же строка**. Дифф #167 — три строки прозы в конфиге и уведомителе, кода не касается вовсе;
- **и главное — я выкатила это на чистом `main` своей рукой в `20:08Z`**: `9d015fd65369f225a620b955678716636ab115ef`, без единого диффа из тредов `064`/`065`, команда `pnpm --filter agent-protocol exec vitest run src/notify/notify.process.test.ts -t 'reminder'` → то же падение. Дифф ни при чём, и это не «локальное окружение ящика»: раннер и ящик падают одинаково.

Ассерт и полученное:

```
- Expected: /^still on you after \d+d \d+h — your decision: 023-x — Чинить ли гард 2\?$/
+ Received:  "still on you after 36d — your decision: 023-x — Чинить ли гард 2?"
```

## 2. Причина названа поимённо — это не флейк, это часы

Два места, и вместе они дают календарь:

1. **`packages/agent-protocol/src/notify/notify.ts:2027`** — `return hours % 24 === 0 ? \`${days}d\` : \`${days}d ${hours % 24}h\`;`. Формат ОСОЗНАННО опускает часы, когда их ноль: «36d», а не «36d 0h». Это объявленное поведение кода, и трогать его — не предмет;
2. **`packages/agent-protocol/src/notify/notify.process.test.ts:419`** — ассерт `\d+d \d+h`, то есть форма «дни И часы», всегда. Возраст берётся от **фиксированной даты фикстуры `2026-07-25T20:00:00Z`** (строка 120 того же файла) до РЕАЛЬНОГО «сейчас».

Отсюда календарь, и он точный: `hours % 24 === 0` истинно ровно тогда, когда стенные часы стоят в интервале `[20:00:00Z, 21:00:00Z)`. **Каждые сутки, один час, любая ветка.** Мина лежала с момента написания теста и до сегодня просто не совпадала с временем прогонов; сегодня три PR треда `064` пришли на конвейер в 20:03, 20:08 и 20:1xZ и легли все.

## 3. Что чинить

Врёт **тест**, а не код. Ассерт обязан быть независим от стенных часов — любой из двух путей, выбор твой, но выбранный назови:

- **(а)** фикстура даёт возраст детерминированно (фиксированное «сейчас» через ту же дверь, которой тест уже подменяет время, либо дата парковки, считанная от «сейчас» со сдвигом, гарантирующим ненулевые часы);
- **(б)** ассерт принимает ОБЕ объявленные формы (`\d+d( \d+h)?`) — тогда он проверяет то, что код обещает, а не то, что он печатает в 23 часа из 24.

**(а) сильнее** и я её рекомендую: (б) закрывает красноту, но перестаёт замечать, что форма вообще поехала. Если возьмёшь (б) — скажи почему, это не отказ.

**Соседей проверить тем же вопросом.** Один такой ассерт найден полем; сколько их всего — не знаю, и «наверное больше нет» здесь не ответ. Пройди `notify.process.test.ts` и соседние процессные тесты на предмет «ожидание зависит от стенных часов» и назови найденное числом — даже если число ноль.

## 4. Класс и кнопка

**Полевой измеренный дефект, новой нормы не вводит** — гард 3 по исключению (решение john 2026-08-22, тред `030-notify-parking-accuracy`), три условия:

1. **первоисточник замера** — назван выше: прогоны `33332529897` и `33332581581` от 2026-08-30, плюс мой прогон на чистом `main` `9d015fd6` в `20:08Z`. Не «известно, что флейчит», а где именно видно;
2. **исполнитель подтверждает в треде своими словами о своём диффе**, что нормы не вводит, — твой ход;
3. **ревьюер подтверждает то же чтением диффа** — круг.

Поведение уведомителя (`notify.ts:2027`) дифф менять не должен; если по ходу окажется, что чинить надо код, а не тест, — это уже смена объявленного поведения, то есть НОРМА, и тогда стоп и слово john. **Кнопка моя** по обычным гардам, доков власти в предмете нет.

## 5. Проверяемость

- **юнит**: `pnpm --filter agent-protocol exec vitest run src/notify/notify.process.test.ts` зелёный, и — несущее — **прогнанный дважды с разным стенным временем**: один раз как есть, один раз с часами внутри окна `20:00–21:00Z` (подменой времени, а не ожиданием суток). Тест, зелёный только вне окна, дефект не чинит;
- **интеграционный**: полный `pnpm test` зелёный на голове PR;
- **живая приёмка**: `checks` зелёный на голове PR, и если прогон случился ВНЕ окна — это не доказательство, и я его таким не приму: доказательство даёт пункт первый;
- **НЕ покрывается сознательно**: остальные классы зависимости тестов от среды (сеть, `$HOME`, переменные окружения ящика — тот же `HEALTHCHECKS_CIRCUIT_URL_HETZNER`, о который спотыкается `daemon.watchdog.process.test.ts`). Предмет этого треда — стенные часы; остальное отдельным разбором, иначе починка растечётся.

## 6. Почему отдельный тред, а не хвост `064`

Тред = одна тема. `064` — про то, что пакет не знает своих потребителей; красный час — про часы в тесте, и он бьёт по всем тредам сразу, а не по одному. Смешать их значит поставить вычистку в зависимость от починки теста в глазах читателя ленты.

Практическая связь всё же есть, и она в одну сторону: **пока это не починено, гард 2 (зелёный `checks` на той же голове) не берётся ни на одном PR, чей прогон попал в окно** — сегодня это #166, #167 и, вероятно, #168. Их перепрогон вне окна пройдёт, но мина останется, и завтра в 20:0xZ она сработает снова.

waiting-on → dev-core.

## msg-002 · from: dev-core · 2026-08-31 · expects: none

Сделано. PR #177, голова `3f9c4dd7`, `checks` идёт — прогон 33346723542.

## Выбор пути: (а), как рекомендовано

Врал тест. `notify.ts` в диффе нет — объявленное поведение `describeAge` не тронуто ни на строку.

Фикстура парковки теперь датируется ОТ «сейчас», а не константой: новый помощник
`agedBy(days, hours)` в `notify.process.test.ts`, и вызов — `agedBy(36, 7)`. «`36d 7h` назад» —
это `36d 7h` в любой час любых суток: остаток `hours % 24` равен 7 **по построению** и нулём не
бывает. Стенные часы из выражения ушли целиком, а не были обойдены.

Поэтому ассерт стал **точным** — `toBe` на полную строку
`still on you after 36d 7h — your decision: 023-x — Чинить ли гард 2?`, — а не разрешающим обе
формы. Это ровно тот запас, который (б) отдаёт: `\d+d( \d+h)?` продолжил бы проходить, если бы
форма напоминания поехала вовсе.

**Важное для ленты: путь (б) уже стоял в дереве до меня.** Он приехал 2026-08-30 с #164 (тред
`065`, коммит `5f36aca7`) — тем же ассертом `( \d+h)?` и комментарием со ссылкой на прогон
33334197543. То есть красноту кто-то уже закрыл попутно, хвостом чужого PR, и без письма в тред.
На момент моего хода `main` был не красным — мина была накрыта заплатой, но не снята. Мой дифф
меняет (б) на (а): не «ещё раз то же самое», а замена слабой формы на сильную.

## Класс: подтверждаю своими словами о своём диффе

**Новой нормы не ввожу.** Дифф — один тестовый файл: дата фикстуры и ассерт над ней. Ни одного
объявленного поведения кода он не меняет и ни одного нового не вводит; `describeAge` остаётся
ровно тем, чем был, включая опускание нулевого остатка часов — этот контракт как стоял, так и
стоит юнитом (`notify.test.ts:241`, `describeAge(60 * 48)` → `"2d"`). Кода в диффе нет вообще.

## Проверяемость: прогон ВНУТРИ окна, а не «повезло»

`faketime` на ящике нет, поэтому подмена сделана загрузчиком: `--import` через `NODE_OPTIONS`
патчит `Date` **одним общим оффсетом из переменной окружения**, так что родитель (vitest) и
дочерний CLI процессного теста стоят на одной ленте. Песочница процессных тестов наследует
окружение, поэтому смещение доезжает до реального процесса команды.

Первая версия инструмента считала оффсет в КАЖДОМ процессе заново — и врала: дочерний CLI
оказывался на минуту позади родителя, возраст выходил `36d 6h`, и я чуть не записал это на счёт
кода. Общий оффсет через окружение это снял. Называю, потому что цифра из кривого инструмента
выглядит как находка.

- `vitest run src/notify/notify.process.test.ts` **как есть** (`01:0xZ`) → **36/36**;
- он же **с часами на `20:30:00Z`, то есть внутри окна** → **36/36**.

Полный `pnpm test` **под сдвинутыми часами внутри окна** → **3013 из 3015**. Два красных, и оба
не про предмет:

1. `fs/git.test.ts:117` — `toBeInstanceOf(Date)`, и сообщение прямо говорит `to be an instance of
   Shifted`: мой загрузчик подменил `globalThis.Date`, тест сверяет прототип. Дефект
   ИНСТРУМЕНТА, на реальных часах зелёный;
2. `orchestrator/daemon.watchdog.process.test.ts:505` — падает и на РЕАЛЬНЫХ часах тоже
   (прогнал контролем отдельно). Это тот самый класс «переменные окружения ящика», который
   постановка сознательно вывела за скобки.

Обычный полный `pnpm test` на голове PR — за `checks`.

## Соседи: число

Класс, который искал: **ожидание, значение которого есть функция от (реальное «сейчас» − ФИКСИРОВАННАЯ
дата фикстуры)**. Два способа, оба прогнаны.

**Сплошной поиск по обоим пакетам** всех ассертов над отрендеренной длительностью (`\d+[dhm]`
в `toContain`/`toBe`) — 24 попадания. Из них:

- юнит-тесты (`notify.test.ts`, `code-age.test.ts`, `status.test.ts`, `tui.test.ts`,
  `self-restart.test.ts`) получают `now` явным аргументом — часов не читают вовсе;
- процессные тесты, которые часы читают (`daemon.process.test.ts`,
  `daemon.unaccepted.process.test.ts`, `daemon.account.process.test.ts`,
  `account.process.test.ts`, `attempt-not-spent.process.test.ts`, `quota-pause.process.test.ts`,
  `run.process.test.ts`), **все до единого** строят даты как `Date.now() ± n` — то есть идиома
  (а) в них уже стоит, и часть с запасом (`liftedPark` берёт `30 * 60_000 + 30_000` — тридцать
  секунд именно на дрейф). Порог, перейдённый в одну сторону, от часа суток не зависит: возраст
  только растёт.

**Итого найдено: 1** — тот, что чинится этим PR. **Осталось: 0.**

Эмпирическая половина совпала со сплошным поиском: полный прогон внутри окна не дал ни одного
падения этого класса.

Два наблюдения не из класса, называю как наблюдения, а не как находки:

- **прыжок часов вперёд на 19 часов** валит `instances.process.test.ts:454` («ancient digest
  raises no ⚠»): дайджест сверяется со временем, которое ставит git, а git мои часы не читает.
  Внутри окна при сдвиге НАЗАД (вчера, `20:30Z`) тест зелёный — значит дело в величине скачка, а
  не в часе суток. Свойство инструмента;
- `fs/git.test.ts:117` — см. выше, `toBeInstanceOf`.

Оба — не «тест зависит от стенных часов», а «тест не переживает подмену часов». Класс соседний и
предметом треда не является; если он нужен как отдельный разбор — это твой ход, не мой.

## Где стою

Рабочее дерево чистое, незакоммиченного нет. Дальше по маршруту: зелёный `checks` на `3f9c4dd7`
→ метка `review` (навешу я, на ту же голову) → вердикт → кнопка твоя. Паркуюсь на круг прогона,
в foreground не жду.

## msg-003 · from: dev-core · 2026-08-31 · expects: none

Голова уехала осознанно: PR #177 перебазирован, новая голова `8ca17a8d`, `checks` идёт — прогон 33348947654.

## Почему тронул голову, а не повесил метку

Зелёный `checks` на `3f9c4dd7` (прогон 33346723542) был, но метку я не повесил, потому что дверь
сказала не «CLEAN», а **ноту гарда 2**:

```
ok   guard 2 · green checks on the same head: 1 check(s) green: checks=SUCCESS
     note · base: the base moved AFTER the credited checks started: 63ea6b8 committed
     2026-08-31T01:31:35Z, 'checks' started 2026-08-31T01:09:39Z
```

Прогон `pull_request` мерит голову, слитую с базой СВОЕГО момента, и сдвиг базы его не
перезапускает. Между стартом моего прогона (`01:09:39Z`) и сейчас в `main` уехал #175
(`63ea6b8d`, `01:31:35Z`). То есть зелёное, которое я бы предъявил под метку, — чтение дерева,
которого больше нет. `mergeable=MERGEABLE (CLEAN)` при этом стоял: он отвечает на другой вопрос —
«склеится ли», а не «проверено ли то, что склеится». Метку решает нота.

Повесить метку на `3f9c4dd7` значило бы отправить ревьюеру ровно тот случай, из-за которого
переписан гард 1: вердикт о дереве, которое уже не то. Поэтому — перебазировка вперёд, и круг
ревью пойдёт с уже правильной базы, одним заходом, а не «вердикт → сдвиг базы → снять и повесить
метку заново».

## Что именно сделано

- `fix/068-daily-red-hour` перебазирована с `909c8c47` на `63ea6b8d`, **без конфликтов**;
- дифф не изменился ни на строку: `1 file changed, 40 insertions(+), 15 deletions(-)`, файл
  один — `packages/agent-protocol/src/notify/notify.process.test.ts`. Кода в диффе по-прежнему
  нет, `notify.ts:2027` не тронут;
- локально на перебазированной голове `vitest run src/notify/notify.process.test.ts` → **36/36**;
- `git push --force-with-lease` → `3f9c4dd7...8ca17a8d`, прогон `checks` 33348947654 родился на
  новой голове.

Класс дефекта и подтверждение из msg-002 в силе и перебазировкой не затронуты: новой нормы дифф
не вводит, объявленное поведение `describeAge` не меняет.

## Где стою и что дальше

Рабочее дерево чистое, незакоммиченного нет, всё запушено. Метки на PR нет — сознательно, её
ход мой и после зелёного.

Дальше по маршруту: зелёный `checks` на `8ca17a8d` → метка `review` (вешаю я, на ту же голову) →
вердикт ревьюера → кнопка твоя. Паркуюсь на круг прогона #177, в foreground не жду.

**Следующей сессии, если эта не доживёт:** посмотреть прогон 33348947654 на `8ca17a8d`; зелёный —
повесить `review`; если база успела уехать снова — перебазировать и повторить, метка идёт ЗА
нотой о базе, а не за `mergeable=CLEAN`.
