---
from: dev-core
worker: claude-code
session: fa45a9dd-c60a-44b0-be80-69982d17b181
date: 2026-08-31T02:57:13Z
expects: none
waiting-on: curator
---

Тред закрыт: #177 в `main` (`f9516a4d`), вердикт `approve` (msg-005), кнопку нажал john. Мой ход
здесь — не новая работа, а закрытие того зазора, который я сам объявил в msg-004, и два
наблюдения, которые я обязан назвать, но не забираю себе.

## Зазор из msg-004 закрыт замером, и теперь это факт, а не вывод

В msg-004 я честно написал границу: склеенное дерево целиком не мерил никто — `checks` на
`8ca17a8d` мерил голову с базой `63ea6b8d`, `checks` на `main` мерил `main` без моего файла, а
моё объединение половин было выводом из непересечения путей плюс точечным прогоном notify. После
мержа склеенное дерево существует по-настоящему — это `f9516a4d`. Померил его.

Стенд: рабочее место роли стоит detached ровно на `f9516a4d`, дерево чистое. Часы сдвинуты тем же
приёмом, что и раньше (общий оффсет `Date` через `NODE_OPTIONS`, одно значение на родителя и на
дочерний CLI), и оффсет проверен ДО прогона, чтобы снова не считать цифру кривого инструмента:
родитель `2026-08-31T20:30:00.020Z`, дочерний процесс `2026-08-31T20:30:00.057Z` — одна лента.
`TMPDIR=/tmp`, иначе процессные тесты падают по `listen EINVAL` на длинном сессионном пути.

- поверхность notify на `f9516a4d` **внутри красного часа** (`20:30:00Z`):
  `vitest run src/notify/notify.process.test.ts src/notify/notify.test.ts` → **156/156**;
- полный `pnpm test` на `f9516a4d` **внутри красного часа** (`20:40:00Z`) → **3053 из 3056**,
  прогнан дважды с одинаковым исходом (187 с). Три красных, и **ни один не из класса «ожидание
  зависит от стенных часов»** — разбираю каждый поимённо, потому что «наверное не про нас» здесь
  не ответ:
  1. `fs/git.test.ts:117` — `toBeInstanceOf(Date)`. Мой загрузчик подменил `globalThis.Date`
     подклассом, тест сверяет прототип. **Артефакт инструмента подмены**, не дефект дерева;
  2. `instances.process.test.ts:454` («ancient digest raises no ⚠») — дайджест сверяется со
     временем, которое ставит git, а git подменённые часы не читает; прыжок вперёд на ~17.8 часа
     делает файл «древним». **Артефакт инструмента подмены**;
  3. `daemon.watchdog.process.test.ts:505` — **и вот это я проверил отдельно, а не отнёс к часам
     по виду**: прогнал тот же файл на РЕАЛЬНЫХ часах, без всякой подмены → падает так же,
     `expected … to contain 'circuit watchdog OFF'`. Значит к часам отношения не имеет вовсе.
     Причина — переменная окружения ящика `HEALTHCHECKS_CIRCUIT_URL_HETZNER` установлена (значение
     не читал и не печатаю), тест ждёт выключенного сторожа. Это ровно тот класс, который
     постановка пунктом 5 назвала НЕ покрываемым сознательно, и назвала этой же переменной.

  **То есть в красном часе на слитом дереве пусто: 0 падений предмета треда.**

Это и есть та живая приёмка, которую постановка требовала пунктом 5 и которую прогон вне окна
дать не мог.

## Соседи: пересчитал на слитом дереве — по-прежнему 1, осталось 0

Пересчёт не формальный: после моей головы в `main` уехал #178, который переписал `notify.ts`
(+202 строки, новый класс тревоги `stale-event-park`), — то есть модуль под тестом стал другим уже
ПОСЛЕ моего замера. На слитом дереве класс «ожидание зависит от стенных часов» по-прежнему один —
тот, что починен, — и он зелёный в красном часе.

**И отдельно, чтобы это не нашли заново как недоделку:** `grep` по
`notify.process.test.ts` всё ещё показывает `\d+d( \d+h)?` — строка 428. Это **не живой ассерт**,
а цитата старой формы внутри комментария, объясняющего дефект. Живой ассерт в этом тесте один и
он точный: строка 454, `toBe("still on you after 36d 7h — …")`. Путь (б) в дереве не остался.

## Наблюдение 1, вне предмета треда: `checks` на `main` красный уже 12 прогонов подряд

Это не мой дифф и не мой класс, но факт, который в ленте нигде не назван, а выглядит он ровно как
«репозиторий зелёный», потому что PR-прогоны проходят.

Замер, поимённо:

- последний зелёный `checks` на `main` — `9d015fd6`, 2026-08-30T19:41Z (прогон 33331554831);
- `8eef719a`, 2026-08-30T**20:14**Z (прогон 33333092554) — красный на шаге `pnpm test`. Это **наш
  красный час на самом `main`**, четвёртый случай к трём из постановки;
- начиная с `4843d046`, 2026-08-30T22:01Z (прогон 33337986597) и по сейчас — **13 прогонов
  подряд** красные, и уже на другом шаге: `comms-derived синхронен с каноном` (шаг 11). Шаг
  `pnpm test` (шаг 10) в них **success** — тесты на `main` зелёные, красный не про них. Последний
  в этом ряду — прогон 33351685653 на `f9516a4d`, то есть на коммите мержа #177: `pnpm test`
  success, шаг 11 failure.

Причина названа замером, а не гипотезой:
`git diff origin/main:.github/workflows/comms-derived.yml origin/comms:.github/workflows/comms-derived.yml`
→ 53 вставки, 4 удаления. Копия в `comms` — `cf825a41` (тред 060), канон в `main` — `9dd947a7`
(#26). То есть копия ВПЕРЕДИ канона, ровно как предписывает порядок из самого сообщения об отказе
(«копия в comms кладётся ДО merge'а канонической правки»), а каноническая правка — это **открытый
PR #169** (тред 060, `ci(comms-derived): отказ derive доезжает до конца шага`). Пока #169 не
смержен, `main` красный по построению.

Вывод (называю выводом, не фактом): порядок «копия раньше канона» делает `main` красным на всё
окно между двумя действиями, и окно здесь уже пять часов. Хорошо это или нет — вопрос к дому
проверки, а не ко мне.

Себе не беру: `.github/workflows/**` — док власти, кнопка john через тебя. Нужен отдельный тред —
твой ход завести.

## Наблюдение 2: «тест не переживает подмену часов» — соседний класс, дома нет

Повторяю из msg-002, потому что оно так и не получило дома: `fs/git.test.ts:117`
(`toBeInstanceOf(Date)` против подменённого прототипа) и `instances.process.test.ts:454` (дайджест
сверяется со временем, которое ставит git, а git подменённые часы не читает) падают не от стенных
часов, а от инструмента подмены. Предметом этого треда они не были и по постановке сознательно не
покрывались. Если класс нужен разбором — дом выбираешь ты.

## Где стою

Рабочее дерево чистое, незакоммиченного нет, веток и PR за мной не висит: `fix/068-daily-red-hour`
слита и её ход кончился. Работы по треду 068 у меня не осталось.

waiting-on → curator.

