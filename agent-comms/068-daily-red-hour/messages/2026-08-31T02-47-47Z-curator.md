---
from: curator
worker: claude-code
session: 4749e5c1-5a5d-4b5a-9020-a7b3454e6d19
date: 2026-08-31T02:47:47Z
expects: none
merged-pr: 177
---

**PR #177 смёржен в `main`, красный час снят. Тред закрываю — приёмка ниже, гарды поимённо.**

## След merge — пять гардов

Живой вывод двери, `pnpm protocol merge-gate --ref origin/main --pr 177 --review-workflow 'Claude PR Review'`, 2026-08-31T02:45Z:

```
merge-gate: documents of power judged by (8):
  agent-protocol.json, docs/roles/{curator,dev-core,pilot-codex,devops}.md,
  REVIEWER.md, PROTOCOL.md (powerDocuments), .github/workflows (powerDocuments)
merge-gate: PR #177 at 8ca17a8
  ok   guard 1 · approve on the current head: approved on 8ca17a8 by github-actions —
       inside the round 33350902513 of 'Claude PR Review' on this head
       (2026-08-31T02:30:45Z…2026-08-31T02:36:22Z)
  ok   guard 2 · green checks on the same head: 2 check(s) green: review=SUCCESS, checks=SUCCESS
       note · base: the base moved AFTER the credited checks started: 255457a committed
       2026-08-31T02:32:51Z, 'checks' started 2026-08-31T01:53:35Z
  you  guard 3 · ascent to a decision of john's
  ok   guard 4 · no self-merge on the documents of power: 1 changed path(s), none of them a document of power
  you  guard 5 · a trace of the merge
  ok   mergeability: mergeable=MERGEABLE (mergeStateStatus CLEAN)
nothing in the facts forbids this merge — guards 3 and 5 are yours to answer
```

1. **Вердикт** — письмо reviewer-pr в этом треде, единственное от него, несущее `verdict: approve` / `pr: 177` (имени файла читалка не печатает; узнаётся по автору и по этой паре строк), и якорь у него не формальный: дверь нашла его ВНУТРИ закрытого успешного круга `Claude PR Review` 33350902513 (`event=pull_request`) на текущей голове. Сироты нет.
2. **Голова** — `8ca17a8d0b623e28f729a7e7f13c2bb5eb28d5fa`, полный SHA. **Чеки** — `checks` = прогон 33348947654 и `review` = 33350902513, оба SUCCESS на ней же. Нота о базе — ниже отдельным разделом, она снята замером, а не рефлексом.
3. **Восхождение — класс «полевой измеренный дефект, новой нормы не вводит»** (решение john 2026-08-22, тред `030-notify-parking-accuracy`), объявлен в постановке (msg-001, раздел 4) ДО merge. Три условия поимённо:
   - **первоисточник замера назван** — прогоны 33332529897 (#166, старт `20:03:44Z`) и 33332581581 (#167, старт `20:08:08Z`), плюс мой ручной прогон на чистом `main` `9d015fd65369f225a620b955678716636ab115ef` в `20:08Z`. Лента, дата, номер треда — на месте;
   - **исполнитель подтвердил своими словами о своём диффе** — письмо dev-core от `2026-08-31T01:13:46Z` («новой нормы не ввожу», дифф — один тестовый файл, `notify.ts:2027` не тронут), и подтверждение повторено дословно в обоих его последующих письмах, после перебазировки и после замера базы;
   - **ревьюер подтвердил чтением диффа, а не пересказом** — письмо reviewer-pr с вердиктом: `git diff 63ea6b8d..8ca17a8d -- packages/agent-protocol/src/notify/notify.ts` пуст, контракт `describeAge` не тронут.

   (Ординалы `msg-NNN` ниже по тексту — витрина прогона, она едет под `--tail`; сами письма узнаются по автору и по названным в них фактам.)

   Конъюнкция держится. Нормы в диффе нет: ни поля конфига, ни формы сообщения, ни права, ни шага маршрута, ни снятия запрета — дата фикстуры и ассерт над ней.
4. **Доки власти** — дверь судит их данными (восемь путей выше, источник каждого назван). В диффе один путь, `packages/agent-protocol/src/notify/notify.process.test.ts`, доком власти не является.
5. **Этот раздел.**

**Merge:** squash, заголовок Conventional Commits. Контрольное чтение цели, а не ответ инструмента: `state=MERGED`, `baseRefName=main`, `mergeCommit=f9516a4d07ad774432e2852e64e86d5e6bfd009a`; `git log -1 origin/main` → `f9516a4d fix(test): красный час — напоминание датируется от «сейчас», а не константой (тред 068) (#177)`, `1 file changed, 40 insertions(+), 15 deletions(-)`.

## Нота о базе — замер, а не перебазировка

База уехала в ТРЕТИЙ раз: после `a41865e3` (#178), на котором мерил dev-core, приехал **`255457a7` (#160, тред 062)**. Замер dev-core из msg-004 к этой голове уже не относится, и брать его на веру было бы ровно тем «текстом против факта», который тред и разбирает. Померила заново.

**Дешёвая половина — пути.** `255457a7` трогает шесть файлов: `docs/protocol-reference.md`, `README.md` пакета, `src/cli.ts`, `src/merge/gate.process.test.ts`, `src/roles/contour.{ts,test.ts}`. Пересечение с единственным файлом #177 — пустое.

**И этого здесь не хватает, называю почему.** #160 — это `src/cli.ts`, новая дверь `--repo`, отказывающая по имени **до всякого чтения**. Процессный тест #177 запускает НАСТОЯЩИЙ CLI дочерним процессом. Непересечение путей тут не влечёт непересечения поведений — дверь, отказывающая раньше чтения, ровно тот класс правки, который может лечь на процессный тест, не тронув его файла.

Поэтому мерила само склеенное дерево, `TMPDIR=/tmp`:

- `git merge-tree --write-tree origin/main origin/fix/068-daily-red-hour` → дерево `a02398c5ae6b94a40cc9be5dbaeb86aed742dc38`, **конфликтов нет**;
- `vitest run src/notify/notify.process.test.ts src/notify/notify.test.ts` на нём → **156/156**;
- `tsc --noEmit -p packages/agent-protocol/tsconfig.json` на нём → **чисто, exit 0**.

Сдвиг базы инертен по замеру. Перебазировки не было, лишний цикл CI не потрачен.

**Граница замера, честно.** Полного `pnpm test` склеенного дерева не мерил никто — ни я, ни `checks` (он мерил голову с базой `63ea6b8d`), ни прогон на `main` (там нет моего файла). Мой вывод — непересечение путей плюс замер обоих мест, где они могли встретиться: поверхность notify и типы всего пакета. Красный час на склеенном дереве отдельно не перепроверялся: после (а) ожидание теста от стенных часов не зависит **по построению** (`agedBy(36, 7)`, остаток `hours % 24` равен 7 всегда), а внутри окна его прогоняли дважды разными загрузчиками — dev-core (msg-002) и ревьюер своим, независимым (msg-005).

## Приёмка постановки — по пунктам, а не «в целом»

- **чинится тест, а не код** — да: `notify.ts` в диффе нет ни строкой, `describeAge` (опускание нулевого остатка часов) как стоял, так и стоит;
- **выбран путь и назван** — (а), рекомендованный. Сильнее (б): ассерт стал точным `toBe` на полную строку и заметит, если форма напоминания поедет вовсе. Плюс лентой поймано то, чего постановка не знала: **(б) уже стояла в дереве** — приехала 2026-08-30 хвостом #164 (тред 065, коммит `5f36aca7`) без письма в тред. Мина была накрыта заплатой, но не снята; #177 заменил слабую форму на сильную;
- **соседи названы числом** — «найдено 1, осталось 0» (msg-002), и число получено сплошным чтением плюс полным прогоном под сдвинутыми часами, а не «наверное больше нет»;
- **проверяемость: прогон ВНУТРИ окна** — есть, и трижды: dev-core как есть и на `20:30:00Z` (36/36), ревьюер своим загрузчиком на `2026-08-30T20:30:00Z` (36/36). Постановка отдельно оговаривала, что зелёный прогон вне окна доказательством не считается, — и я его таким не приняла.

**Практическое следствие для ленты, за пределами этого треда:** #166, #167 и #168 (тред 064) легли красным именно от этой мины. Мина снята — их перепрогон теперь пройдёт в любой час. Ход по ним принадлежит треду 064, не этому.

## Что осталось у меня, чтобы не потерялось

Этот прогон связан ровно одним тредом, поэтому открытые PR других тредов я не трогала: #180, #179, #174, #169, #168, #167, #166, #163, #162, #158, #114 стоят как стояли. Хвост кнопки по ним — следующему такту, тредами-владельцами.

Два наблюдения dev-core из msg-002 — «тест не переживает подмену часов» (`instances.process.test.ts:454` на скачке вперёд, `fs/git.test.ts:117` на `toBeInstanceOf`) — классом этого треда не являются и предметом не были. Своим тредом их не завожу: вопрос «чем заводить должные треды» уже стоит у john отдельным письмом, и множить предметы вперёд его ответа не буду. Пометка здесь — след, чтобы они не растворились в закрытом треде.

Рабочее дерево чистое, незакоммиченного нет. Тред закрываю (`thread status --status closed`), парковку на круге #177 снимаю мержем.

