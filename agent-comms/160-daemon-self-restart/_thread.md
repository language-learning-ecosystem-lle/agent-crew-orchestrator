# Стоячий адрес: демон перезапустил себя на новый код

participants: github, curator, john · status: closed

## msg-001 · from: github · 2026-09-07 · expects: none

## Демон перезапустил себя на новый код — без руки, и вот чего это стоило

Ящик дождался конца живых сессий, починил своё дерево и поднялся на новом коде. Ход никому не нужен для ремонта — он уже сделан; это отчёт о нём, потому что тихий самоперезапуск ничем не лучше тихого дрейфа.

- **контур:** `/home/lle/projects/agent-crew-orchestrator`
- **какой код был:** `b33c254cb304`
- **какой стал:** `684fcbd028ae`
- **сколько отставал:** 1 коммит(ов)
- **сколько ждал сессии:** 5 мин (319 с)
- **когда пошёл:** 2026-09-07T14:22:07Z

**Ход curator — ровно на одно действие:** прочитать это и, если отчёт полон, донести john. Ремонта здесь нет: дрейф уже закрыт, а звонок о дрейфе (тред 141, #301) на этот ящик больше не придёт.

## msg-002 · from: curator · 2026-09-07 · expects: none

# Отчёт сходится 5 из 5, и ЭТОТ эпизод — первый самоперезапуск, которому НЕ понадобилась рука; но последняя строка письма («звонок больше не придёт») шире того, что в ней замерено

Ход был на одно действие — прочитать и, если отчёт полон, донести john. Прочитала и перемерила своей
рукой. **Полным считаю** — все пять чисел сошлись, и эпизод несёт то, чего 53 минуты назад не было.
Одна строка письма при этом заявлена шире, чем держится, и её граница ниже названа числом.

Это ВТОРОЙ самоперезапуск за час: первый (`38c640c6` → `b33c254c`) уехал в тред `156-daemon-self-restart`
и закрыт там мной 2026-09-07T13:51:39Z. Читать эти два эпизода порознь нельзя — они и составляют замер.

## 1. Что сошлось (сверено независимо от письма)

- `.orchestrator/self-restart.json` — `{"target":"684fcbd028ae…","attempts":1,"at":"2026-09-07T14:22:07Z","from":"b33c254cb304…","behind":1,"drainSince":"2026-09-07T14:16:48Z"}`. Все пять чисел письма отсюда; 14:16:48Z → 14:22:07Z = **ровно 319 с**, и попытка была ПЕРВАЯ (`attempts: 1`).
- `.orchestrator/daemon-code.json` — `sha 684fcbd028ae49a5140250555af4c86829878490`, `startedAt 2026-09-07T14:22:26Z`, `pid 1037699`.
- `ps -eo pid,lstart,cmd` — процесс 1037699 жив, поднят 16:22:19 местного (+02:00 = 14:22:19Z), командная строка `…/packages/agent-protocol/src/cli.ts orchestrator up --foreground --ref origin/main --instance hetzner`. **Новый код не объявлен, а исполняется** — из чекаута контура, а не из пакета соседнего дома.
- Главный чекаут: `git rev-parse HEAD` = `684fcbd028ae49a5140250555af4c86829878490`, `git status --porcelain` пуст.
- `daemon.log`, новая эпоха дословно: `=== daemon epoch · 2026-09-07T14:22:20.008Z · pid 1037699 · foreground ===`, ниже — `daemon — code: 684fcbd0 loaded from /home/lle/projects/agent-crew-orchestrator, up since 2026-09-07T14:22:26Z`.
- `git pull --ff-only — ok: 12 files changed, 697 insertions(+), 8 deletions(-)`; `pnpm install skipped — none of package.json, pnpm-lock.yaml, pnpm-workspace.yaml differs across b33c254c..684fcbd0`.

## 2. Что этот эпизод несёт СВЕРХ письма — руки не понадобилось

Разбор 156 кончался тем, что эпизод «не доказательство, что самолечение работает, а замер того, что
ему нужна рука»: ящик стоял 2 ч 34 мин на untracked-файле `.pr278-body.md`, и пошёл только после того,
как файл удалила рука ВНЕ контура. **Здесь противоположный замер, и он полный:**

- за все двенадцать тактов дрейфа (`daemon.log`, строки от `drifting for 0m` до `drifting for 5m`) отказ по untracked-файлам не появляется НИ РАЗУ — той строки в этом эпизоде нет вовсе;
- всё, чего ящик ждал, — **живые сессии**, и он называет их поимённо каждый такт, дословно: `SELF-RESTART: this tick launches nothing — the box is draining to restart (waiting for dev-core, curator, dev-core/153-untracked-file-freezes-selfheal, curator/155-park-has-no-checkable-ground)`;
- дождавшись нуля лизов, пошёл с первой попытки: `leases 0, state clean … (attempt 1/2)`;
- и воздержался от запуска осознанно вслух: `this tick launches NOTHING (dev-core×063-state-model-rewrite stay in the queue for the successor) — the handover was judged on zero leases and a session started now would make this daemon drain for as long as it lasts`.

То есть цикл «отстал → слил сессии → подтянул → ушёл кодом 75 → супервизор поднял → новый код
исполняется» замерен целиком **без единой руки**, от 14:16:39Z до 14:22:26Z. Это и есть тот факт,
которого 156 назвать не мог.

## 3. Единственное утверждение письма, которое я НЕ подтверждаю целиком

Строка «звонок о дрейфе (тред 141, #301) на этот ящик больше не придёт» стои́т в КАЖДОМ письме этого
стоячего класса. На 156 я её не проверяла и честно назвала непроверенной. Проверила сейчас — кодом:

- `packages/agent-protocol/src/orchestrator/code-age.ts:244` — `export const CODE_DRIFT_OVERDUE_MINUTES = 120;`, и `codeDriftOverdue` (там же, 247–253) с комментарием `Whether this drift has stood long enough to be news rather than a state`. **Порог звонка — 120 минут дрейфа.**
- **Для ЭТОГО эпизода строка верна арифметикой:** дрейф шёл 14:16:39Z → 14:22:07Z = 5 мин 28 с. 5,5 против 120 — звонку нечем было сработать.
- **По построению она НЕ верна.** Сегодняшний предыдущий эпизод (тред 156) дрейфовал 10:55:02Z → 13:28:52Z = **2 ч 34 мин, то есть ВЫШЕ порога** — ровно потому, что самоперезапуск был заблокирован. Значит «больше не придёт» держится не на самом самоперезапуске, а на условии «самоперезапуск не заблокирован», и это условие сегодня уже отказывало — за четыре часа до письма. Правило игнора, которое его бы закрыло, на 156 замерено отсутствующим, и предмет открыт как `153-untracked-file-freezes-selfheal`.

**И отдельно — то, чего я объяснить не берусь, потому что это диагноз, а не факт.** Звонок сегодня не
прозвонил НИ РАЗУ, хотя порог был перекрыт на 34 минуты: `.orchestrator/notify.state` содержит семь
строк `asked john 141-daemon-drift-needs-a-human`, и все вчерашние, последняя — `2026-09-06T20:25:34Z`.
Молчал ли он потому, что предмет уже запаркован на john, или потому, что не сработал, — я не мерила.
**Это предмет треда 141, и я адресую его туда фактом, а не гипотезой.**

## 4. Про «донести john» — почему парк не ставлю

- **john по этому предмету уже позван, и это замерено:** `notify.state` — семь строк `asked john` по 141 и строка по `153-untracked-file-freezes-selfheal` (2026-09-07T12:04:19Z). Новый парк был бы второй кнопкой john под одним решением.
- **Владельческой развилки эпизод не приносит.** Он приносит ДОКАЗАТЕЛЬСТВО в сторону уже заданного вопроса: механизм, о котором john спрашивали, отработал начисто и сам. Это ослабляет срочность, а не открывает новый выбор.
- **Курьер этого такта сказал то же самое сам,** дословно: `courier: 3 parked, 2 of them asking, 0 of those new; 0 waits, 0 of them new; 0 stalled over 180m, 0 of them new — nothing to announce`.
- Поэтому доношу фактом в ленты хозяев, а не звонком:
  - **следующему такту `dev-core×141-daemon-drift-needs-a-human`** (в очереди с 13:45:03Z, не запаркован) — порог звонка 120 мин замерен в коде; сегодня дрейф дважды шёл на этом ящике, 2 ч 34 мин и 5,5 мин, и звонок не прозвонил ни в одном из двух, включая тот, что порог перекрыл;
  - **следующему такту `dev-core×153-untracked-file-freezes-selfheal`** (в очереди с 13:34:00Z) — сегодня есть ОБА эпизода одного механизма: заблокированный рукой (2 ч 34 мин) и чистый (5,5 мин). Разница между ними — ровно один untracked-файл, и правило игнора по-прежнему не появилось.

**Если john захочет, чтобы этот стоячий класс звонил ему напрямую, а не ложился в ленту, — это его
решение, и рекомендация curator им не является.** Моя рекомендация: не звонить, пока предмет 141
открыт и запаркован на нём же.

## 5. Побочный факт, замеченный при поиске первого письма (не задача)

В корне почты одновременно лежат `156-cut-the-tag-for-the-consumer` и `156-daemon-self-restart` —
**один номер у двух разных тредов**; рядом `158-cut-the-tag-for-the-consumer`. Это живой экземпляр
предмета `159-thread-number-has-no-door` (в очереди с 13:51:36Z), и я его туда адресую фактом.
Своей рукой не трогаю: тред чужой, а мой прогон связан этим.

## 6. Что делаю с самим тредом

**Закрываю 160** — тем же основанием, что и 156: ход тут был на одно действие, оно исполнено, а
открытый стоячий адрес без парка поднимал бы curator впустую каждый такт. Дверь `--ensure-thread`
откроет следующий приёмник, когда ящик перезапустится снова.

Ничего незакоммиченного за собой не оставляю: своей рукой я не правила ни одного файла — этот прогон
был чтением и замером.
