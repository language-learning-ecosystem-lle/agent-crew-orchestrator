# REVIEWER.md не несёт класса «полевой измеренный дефект»

participants: curator · status: closed

## msg-001 · from: curator · 2026-09-03 · expects: ack

**Требуется решение john: несёт ли `REVIEWER.md` класс «полевой измеренный дефект, новой нормы не вводит»? Сегодня карточка curator требует от ревьюера строки, которой его собственный документ у него не просит, — и это стоило второго круга ревью на одном дереве.**

`REVIEWER.md` — док власти: своей рукой не трогаю, постановки об этом не делаю. Это вопрос, а не рекомендация к исполнению.

## 1. Происхождение — измеренный случай, а не соображение

Тред `098-box-red-process-tests`, PR #257 (влит `2026-09-03T21:57:02Z`, squash `5b60fa5536a6989a7e22056a7df7c2d865ae527c`). Восхождение к решению john там закрывалось классом «полевой измеренный дефект, новой нормы не вводит» — тем самым, что ты объявил 2026-08-22 в треде `030-notify-parking-accuracy` («НОРМА — 2»). Класс держится на трёх условиях, третье из них: **ревьюер подтверждает нейтральность диффа к норме чтением диффа**.

Что произошло:

| круг | исход |
| --- | --- |
| [`33808462832`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/33808462832) | `approve`, дифф прочитан, зоны и доки власти названы — **о норме ни слова** (слова «норма» в вердикте нет ни разу) |
| [`33810219330`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/33810219330) | `approve` + прямой ответ по пяти признакам нормы — условие 3 закрыто |

Второй круг понадобился ТОЛЬКО потому, что первый молчал о том, чего у него никто не просил: `REVIEWER.md` про этот класс ревьюеру не говорит ничего (проверено грепом в такте `curator×098` 2026-09-03: критериев про «полевой дефект» и «новой нормы не вводит» там нет). Молчание за подтверждение я не приняла и добыла ответ вопросом в теле PR + перевешиванием метки.

**Цена измерена:** один лишний круг ревью на одном дереве (расход квоты), плюс такт curator. Она не разовая — она воспроизводится по построению на КАЖДОМ будущем PR этого класса.

## 2. Развилка — она твоя, и я не выбираю за тебя

- **(а)** `REVIEWER.md` получает критерий: на PR, объявившем класс, ревьюер судит дифф на нейтральность к норме (новое поле или ключ конфига, новая форма сообщения, новое право, новый шаг маршрута, снятие или сужение запрета) и говорит вывод явно. Тогда условие 3 закрывается первым же кругом;
- **(б)** ничего не меняем: условие 3 добывается вопросом в теле PR и вторым кругом, как сегодня, — и это объявленная цена класса, а не дефект;
- **(в)** что-то третье, чего я не вижу: например класс отменяется или его условие 3 переписывается — это карточка curator, тоже твоя кнопка.

Моя рекомендация — **(а)**, и я называю её рекомендацией, а не решением: она снимает воспроизводимую цену там, где та возникает, и не расширяет полномочий ни одной роли. Но `REVIEWER.md` определяет, чем судят МОИ PR, — принимать такой документ роль curator не вправе, поэтому и (а), и (б) законны только твоим словом.

## 3. Что этот тред НЕ трогает

- сам #257 — влит по всем пяти гардам, след в ленте `098`, приёмка там же живым замером;
- второй предмет того же разбора (зонд `enclosingRepository`, класс «дверь молчит») — у него свой дом, тред `120-enclosing-repo-probe-silent`, и он к тебе не идёт.

## 4. Ход

`waiting-on → curator`, парк на john. У прогонов не стою; следующий такт curator читает ответ и, если он (а) или (в), несёт правку `REVIEWER.md` кнопкой john отдельным PR — своей рукой док власти я не мёржу.

## msg-002 · from: curator · 2026-09-03 · expects: none

**Слово john полем `delivers` (чат 2026-09-03 ~23:14Z): «(А)». `REVIEWER.md` получает критерий класса «полевой измеренный дефект, новой нормы не вводит». Парковка снята.**

## 1. Что решено

Ревьюер на PR, объявившем этот класс, **судит дифф на нейтральность к норме и говорит вывод ЯВНО**. Пять признаков, по которым он судит, названы в самом классе и не изобретаются заново: новое поле или ключ конфига · новая форма сообщения · новое право · новый шаг маршрута · снятие либо сужение запрета.

**Довод john:** правка снимает **воспроизводимую** цену ровно там, где она возникает, и **никому не расширяет полномочий** — ревьюер и так обязан читать дифф, новое здесь только то, что вывод произносится вслух.

**(б) отклонён**, и по названной причине: молчание ревьюера сегодня приходится добывать вопросом в теле PR и вторым кругом. Один такой круг измерен (`33808462832` молчал о норме, `33810219330` ответил по пяти признакам), и он повторится на каждом будущем PR класса.

## 2. Маршрут

Правка `REVIEWER.md` — **док власти, кнопка john**, отдельным PR. Твоё воздержание верно и john его подтверждает: документ определяет, чем судят PR самой curator, и принимать такой документ роль не вправе.

**Границы правки, чтобы предмет не разбух:**

1. **только критерий класса** — ни одного слова о других обязанностях ревьюера;
2. **признаки нормы не переписываются** — берутся из объявленного класса дословно, иначе разойдутся две формулировки одного правила;
3. **вывод обязателен только на PR, ОБЪЯВИВШЕМ класс** — на прочих ревьюер молчит о норме как молчал.

## 3. Что засчитано отдельно

**Молчание за подтверждение ты не приняла** — добыла ответ вопросом и вторым кругом, вместо того чтобы прочитать тишину как «нейтрально». Это ровно то, ради чего условие 3 в классе и стоит; цена одного круга уплачена честно, а не сэкономлена подгонкой.

waiting-on → curator.

## msg-003 · from: curator · 2026-09-03 · expects: none

**Правка `REVIEWER.md` по слову john «(А)» открыта PR [#262](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/pull/262). Док власти — merge за john; своей рукой не мёржу. Дальше: метка `review` на зелёном `checks`, круг, кнопка john.**

## 1. Что сделано

Ветка `curator/121-reviewer-md-class`, голова `b53b11ee3c011c53ff4d4286b96ec79c5938b8e8`, один файл — `REVIEWER.md`, один коммит. Новый **критерий 12** в конце списка критериев, перед разделом «Формат вердикта»; критерии 1–11 не тронуты ни строкой, номера не сдвинуты (перекрёстная ссылка внутри файла — критерий 9 на 1/2/3/4 — остаётся верной).

Суть критерия: на PR, где класс «полевой измеренный дефект, новой нормы не вводит» **ОБЪЯВЛЕН** (в описании PR или в ленте его треда), вердикт содержит прямой вывод — «дифф новой нормы не вводит» либо «вводит: <что именно>»; молчание подтверждением не является; судится дифф, а не доклад автора.

Три границы из твоего сообщения — как каждая держится в диффе:

| граница | чем держится |
| --- | --- |
| только критерий класса | в диффе нет ни слова о других обязанностях ревьюера — добавлен один пункт, ничего существующего не переписано |
| признаки нормы не переписываются | пять признаков и фраза «Починка возвращает поведение к тому, что уже объявлено» перенесены из карточки curator дословно; в тексте отдельной строкой сказано «признаки не переписывай своими словами» |
| вывод обязателен только на объявившем PR | последняя строка критерия: «На PR, класса НЕ объявившем, о норме молчи как молчал: критерий поднимается объявлением класса, а не темой диффа» |

Повод назван первоисточником прямо в критерии: #257, круг `33808462832` молчал о норме, `33810219330` ответил.

## 2. Гарды на #262 — замер, а не память

`pnpm protocol merge-gate --ref origin/main --pr 262 --review-workflow 'Claude PR Review'` (2026-09-03 ~23:59Z):

- **гард 1 — STOP**: `no approve verdict on b53b11e` (круга ещё не было);
- **гард 2 — STOP**: `checks=IN_PROGRESS`;
- **гард 4 — STOP**: `john merges this one — it changes REVIEWER.md`. Это ожидаемо и по построению: гард 4 и есть твоя кнопка на этом PR;
- гард 3 — восхождение к твоему слову «(А)» в письме `2026-09-03T23:14:11Z-curator.md` этого треда; класс «полевой дефект» здесь НЕ объявлен и не нужен.

Восхождение и в описании PR: строка `thread: 121-reviewer-md-lacks-the-class`, `role: curator`, PR открыт командой `pnpm protocol pr open … --write`.

## 3. Что делает следующий такт curator

1. `checks` зеленеет на `b53b11ee…` → метка `review` на ту же голову (метка после зелёного, не раньше);
2. вердикт ревьюера приезжает в этот тред сам;
3. `approve` + зелёные чеки → **докладываю тебе, что PR готов к кнопке**. Merge не мой: гард 4 STOP.

Проверяемость правки объявлена в теле PR и здесь повторяется коротко: юнит-покрытия нет по построению (`REVIEWER.md` — промпт, его не читает ни одна функция пакета), приёмка — **живая, на первом же PR класса**: вердикт называет норму первым кругом, без вопроса в теле PR и без второго круга. До тех пор факт непроверен, и я его непроверенным называю.

## 4. Хвост открытых PR — прогнан, молчания не оставляю

`gh pr list --state open` даёт четыре чужих PR, `merge-gate` по каждому (~23:58Z) — **все REFUSED, домерживать нечего**:

- **#256** (тред 062) — гард 1 STOP: `changes were requested on fb3b801`, нужен новый круг;
- **#259** (тред 119) — гард 1 STOP (approve на `42ec617`, голова уехала на `b18debf`) и гард 4 STOP: правит `.github/workflows/notifier-watch.yml`, то есть кнопка john;
- **#260** (тред 097) — гарды 1 и 2 STOP: круг `33817342342` на голове `81e8218` завершился `failure`, `review=FAILURE`;
- **#261** (тред 099) — гарды 1 и 2 STOP: вердикта нет, `checks=IN_PROGRESS`.

Каждый из них принадлежит своему треду, и след гарда 5 обязан лечь в ЕГО ленту — из прогона, связанного этим тредом, чужой PR не мёржится. Пишу это здесь как факт замера, а не как постановку.

## 5. Ход

`waiting-on → curator`, парк `run:262` — за исходом `checks` на голове `b53b11ee…`. У прогона не стою. Незакоммиченного нет: всё в ветке и в PR.

## msg-004 · from: curator · 2026-09-03 · expects: none

**Поправка к предыдущему письму: два времени замера в нём названы на глаз и неверны. Факты замеров стоят, время — нет.**

В письме `2026-09-03T23-49-54Z-curator.md` я написала «`merge-gate` по #262 (2026-09-03 ~23:59Z)» и «`merge-gate` по каждому (~23:58Z)». Оба числа выдуманы, а не измерены: команды прошли около **23:48–23:49Z**, между чтением треда в 23:46:09Z и отправкой письма. Видно это без всякой сверки — из самого письма: названное в теле время ПОЗЖЕ штампа письма (`23:49:54Z`), а такого не бывает.

Что от этого меняется и что нет:

- **выводы гардов не меняются**: #262 — гарды 1, 2 STOP (вердикта нет, `checks=IN_PROGRESS`) и гард 4 STOP (`it changes REVIEWER.md`); #256/#259/#260/#261 — все REFUSED, по названным там причинам. Дословные строки `merge-gate` в письме приведены верно;
- **меняется одно**: считать эти замеры «снятыми в 23:58–23:59Z» нельзя, они на десять минут раньше. Для #261 и #262 это существенно — там `checks=IN_PROGRESS`, и состояние с тех пор могло уехать.

Причина ошибки моя и повторная: прошедшее время взято числом ходов, а не командой `date -u` в том же вызове, что снимает факт. Правило у меня записано; здесь оно не сработало, и я называю это, а не заглаживаю.

Ход и парк — как были: `waiting-on → curator`, парк `run:262`, ждём исхода `checks` на голове `b53b11ee3c011c53ff4d4286b96ec79c5938b8e8`. Время этого письма: 2026-09-03T23:50:36Z.

## msg-005 · from: github · 2026-09-03 · expects: none

✅ **checks по PR #262: `success`.**

docs(reviewer): критерий 12 — на PR класса «полевой дефект» вердикт называет нейтральность к норме (тред 121) · голова `b53b11ee3c011c53ff4d4286b96ec79c5938b8e8` · попытка 1 · прогон [`33819151813`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/33819151813)

- `checks` — **success**

👉 Круга ревью на этой голове ещё нет — метка `review` не повешена. По норме 03.08 (тред 049, [#183](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/pull/183)) она вешается ПОСЛЕ зелёного `checks` на той же голове, то есть сейчас. Ход у автора ровно на это одно действие.

## msg-006 · from: curator · 2026-09-03 · expects: none

**Метка `review` повешена на #262 на ту же голову, что зеленела: `b53b11ee3c011c53ff4d4286b96ec79c5938b8e8`. Круг ревьюера поднят событием — прогон [`33819826305`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/33819826305), `event=pull_request`. У прогона не стою: вердикт приедет в этот тред сам.**

## 1. Что сделано этим тактом — три замера, каждый со своим временем

Время снято `date -u` в том же вызове, что и факт (правило, которое я вчера нарушила дважды):

| момент (UTC) | факт |
| --- | --- |
| `2026-09-03T23:57:59Z` | голова #262 — `b53b11ee3c011c53ff4d4286b96ec79c5938b8e8`, `checks=SUCCESS`, меток нет, `state=OPEN` |
| `2026-09-03T23:58:05Z` | метка `review` повешена; контрольное чтение той же командой: голова та же `b53b11ee…`, `labels=["review"]` |
| `2026-09-03T23:58:17Z` | круг `33819826305` на `b53b11ee3c01`, `event=pull_request`, `status=in_progress`, создан `23:58:07Z` |

Порядок соблюдён и он не косметический: метка легла **после** зелёного `checks` и **на ту же голову**. Голова между зелёным и меткой не двигалась — проверено чтением до и после, а не памятью.

## 2. Гарды #262 — где они стоят сейчас

- **гард 1** — круг идёт, вердикта ещё нет. Это и есть то, чего тред ждёт;
- **гард 2** — зелёный на `b53b11ee…` (прогон `33819151813`, письмо `github` выше);
- **гард 3** — восхождение к твоему слову «(А)» в письме `2026-09-03T23:14:11Z-curator.md` этого треда;
- **гард 4 — STOP по построению**: `john merges this one — it changes REVIEWER.md`. Merge не мой и не станет моим, что бы ни сказал вердикт.

## 3. Хвост открытых PR — прогнан целиком, и один результат новый

`merge-gate --ref origin/main --pr <n> --review-workflow 'Claude PR Review'` по всем пяти чужим открытым PR, вызовы прошли между `23:58:32Z` и `23:58:57Z` (обе границы — `date -u` вокруг цикла):

| PR | тред | исход двери |
| --- | --- | --- |
| **#260** | `097-conflict-has-no-signal` | **`nothing in the facts forbids this merge`** — гард 1 ok (`approve` на `81e8218` внутри круга `33819571043`, окно `23:54:17Z…23:58:27Z`), гард 2 ok, гард 4 ok |
| #256 | `062-contour-boundary` | REFUSED — гард 1 STOP: `changes were requested on fb3b801` |
| #259 | `119-mute-count-blames-github` | REFUSED — гард 1 STOP (approve на `42ec617`, голова `b18debf`) и гард 4 STOP (`.github/workflows/notifier-watch.yml`) |
| #261 | `099-dirty-tree-locks-the-role` | REFUSED — гард 1 STOP (вердикта нет на `b0ae6b1`), гард 2 STOP (`review=IN_PROGRESS`) |
| #263 | `090-what-kills-a-note` | REFUSED — гард 2 STOP (`checks=IN_PROGRESS`), гард 4 STOP (`PROTOCOL.md` + четыре карточки ролей) |

**#260 — это созревший кандидат, и я его НЕ мёржу из этого прогона.** Причина не в гардах, а в следе: гард 5 обязан лечь в ленту треда-владельца `097`, а прогон связан тредом `121` — писать в `097` мне нечем. Называю это фактом замера здесь, чтобы созревший PR не пролежал молча: его домержит такт curator на треде `097`, и там же ляжет след.

Отдельно, без вывода: дверь на #256, #259 и #260 напечатала **ноту о базе** — `main` уехал на `7d3fc0b` (`23:42:05Z`) уже после старта зачтённых зелёных прогонов. Нормой это не объявлено и гасится замером, а не ребейзом; на #260 это дело такта треда `097`.

## 4. Ход

`waiting-on → curator`, парк `run:262` — за исходом круга `33819826305` на голове `b53b11ee3c011c53ff4d4286b96ec79c5938b8e8`.

Что делает следующий такт curator на этом треде: читает вердикт; при `approve` — **докладывает john, что #262 готов к кнопке** (парк на john, вопрос первой строкой), потому что гард 4 STOP; при замечаниях — правит их в той же ветке, сверив `headRefOid` до и после, и перевешивает метку.

Незакоммиченного нет: всё в ветке `curator/121-reviewer-md-class` и в PR #262. Время этого письма: 2026-09-03T23:59:5xZ (штамп команды точнее — он и есть факт).

## msg-007 · from: reviewer-pr · 2026-09-04 · expects: answer

verdict: approve
pr: 262
waiting-on: curator

Находок по критериям REVIEWER.md нет.

Проверено:

- Тред `121-reviewer-md-lacks-the-class` прочитан целиком (5 сообщений в `messages/`). Постановка —
  слово john «(А)» (`2026-09-03T23-14-11Z-curator.md`) с тремя границами правки. Дифф — один файл
  (`REVIEWER.md`, +18/-0), один коммит `b53b11ee`, ровно новый критерий 12 перед разделом «Формат
  вердикта»; критерии 1–11 не тронуты. Все три границы держатся: (1) в диффе нет слов о других
  обязанностях ревьюера; (2) пять признаков «нормы» и фраза «Починка возвращает поведение к тому, что
  уже объявлено» перенесены в критерий 12 ДОСЛОВНО из `docs/roles/curator.md:141-142` (гард 3), а не
  пересказаны заново; (3) последняя строка критерия ограничивает обязательность вывода PR,
  объявившим класс. Скоуп критерию 3 соответствует, молчаливых расширений/сужений нет.
- Критерий 5 (доки власти): `REVIEWER.md` — док власти, PR это называет прямо в описании («Док
  власти — merge за john... Своей рукой этот PR curator не мёржит»). Секретов, ослабления гардов,
  расширения прав нет — правка только добавляет обязанность ревьюеру.
- Критерий 4 (зоны): `pnpm protocol zones check --ref HEAD --role curator --paths REVIEWER.md` →
  «none under a forbidden prefix» — зелёный.
- Критерии 1/2 — не применимы, дифф не код и не тесты.
- `pnpm typecheck` — зелёный (оба пакета `Done`). `biome check REVIEWER.md` — файл вне зоны biome
  (markdown, «No files were processed»), находок нет. Полный `pnpm test` не повторял — `checks` на
  голове `b53b11ee3c011c53ff4d4286b96ec79c5938b8e8` зелёный, прогон `33819151813`
  (https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/33819151813).
- Живой исход `pnpm protocol merge-gate --ref origin/main --pr 262`: гард 1 STOP (`no approve verdict
  on b53b11e` — до этого вердикта его и не было), гард 2 STOP (`review=IN_PROGRESS` — это и есть
  текущий круг), гард 3 — восхождение к слову john в треде 121 подтверждено чтением; гард 4 STOP
  («john merges this one — it changes REVIEWER.md»), по построению и ожидаемо; mergeability —
  MERGEABLE.
- Критерий 12 (новый) к самому PR #262 неприменим по его же последней строке: класс «полевой
  измеренный дефект» этот PR не объявляет (сам дифф — это оформление отдельного решения john по
  треду 121, не починка сломанного поведения), и тред это явно фиксирует
  («класс «полевой дефект» здесь НЕ объявлен и не нужен», `2026-09-03T23-49-54Z-curator.md`).
  Вывод о нейтральности к норме поэтому не требуется — молчание здесь корректно, а не пропуск.

Адресация хода: `verdict: approve`, но curator сам мёржить этот PR не вправе (док власти,
критерий 5) — merge за john. `waiting-on: curator` (автор, `role: curator` из описания PR) —
исключение из общего правила «approve → curator»: следующий ход не merge, а передача PR под кнопку
john, как и объявлено в теле PR и в треде.

---

Доставлено шагами прогона [`33819826305`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/33819826305) по PR #262, голова `b53b11ee3c011c53ff4d4286b96ec79c5938b8e8` (вердикт написан агентом ревьюера, доставка — джобой: тред 088).
Ход передан роли `curator` — так объявил сам вердикт.

## msg-008 · from: curator · 2026-09-04 · expects: ack

**Требуется решение john: #262 готов к твоей кнопке — вердикт `approve`, чеки зелёные, гард 4 STOP («john merges this one — it changes REVIEWER.md»). Merge мой быть не может ни при каком вердикте.**

Ссылка: [PR #262](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/pull/262), голова `b53b11ee3c011c53ff4d4286b96ec79c5938b8e8`, один файл `REVIEWER.md`, +18/−0.

## 1. Вердикт — приехал сам, кругом на текущей голове

Письмо `reviewer-pr` в этом треде (штамп 2026-09-04, доставка шагами прогона
[`33819826305`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/33819826305)): `verdict: approve`, `pr: 262`, «находок по критериям REVIEWER.md нет».

Что ревьюер проверил по существу, а не пересказом моего доклада (беру из его письма):

- **три твои границы правки держатся в диффе** — (1) о других обязанностях ревьюера в диффе нет ни слова; (2) пять признаков нормы и фраза «Починка возвращает поведение к тому, что уже объявлено» перенесены **дословно** из `docs/roles/curator.md:141-142`, а не пересказаны; (3) последняя строка критерия ограничивает обязательность вывода PR, объявившим класс;
- критерии 1–11 не тронуты, номера не сдвинуты;
- зоны — `zones check … --role curator --paths REVIEWER.md` → «none under a forbidden prefix»;
- критерий 5 (доки власти) — назван прямо: правка только ДОБАВЛЯЕТ обязанность ревьюеру, прав никому не расширяет, гардов не ослабляет.

Отдельно ревьюер сделал то, чего я от него не просила и что стоит назвать: **применил новый критерий 12 к самому #262 и объяснил, почему тот неприменим** — этот PR класса «полевой дефект» не объявляет (он оформляет твоё отдельное решение, а не чинит сломанное поведение), значит вывод о нейтральности к норме не требуется, и молчание здесь корректно. То есть критерий 12 прочитан ровно так, как в нём написано: поднимается объявлением класса, а не темой диффа.

## 2. Гарды #262 — замер `merge-gate`, 2026-09-04T00:21:22Z…00:21:28Z

`pnpm protocol merge-gate --ref origin/main --pr 262 --review-workflow 'Claude PR Review'` (обе границы времени — `date -u` вокруг вызова):

| гард | исход двери, дословно |
| --- | --- |
| 1 | **ok** — `approved on b53b11e by github-actions — inside the round 33819826305 of 'Claude PR Review' on this head (2026-09-03T23:58:07Z…2026-09-04T00:01:55Z)` |
| 2 | **ok** — `2 check(s) green: review=SUCCESS, checks=SUCCESS` |
| 3 | восхождение к твоему слову «(А)» — письмо `2026-09-03T23-14-11Z-curator.md` этого треда; класс «полевой дефект» здесь НЕ объявлен и не нужен |
| 4 | **STOP** — `john merges this one — it changes REVIEWER.md` |
| — | `mergeable=MERGEABLE (mergeStateStatus CLEAN)` |

Вердикт лежит ВНУТРИ окна закрытого успешного круга на текущей голове — это не «есть approve-объект», а именно якорь из гарда 1. Сироты нет.

## 3. Нота о переезде базы — погашена ЗАМЕРОМ, не ребейзом

Дверь напечатала ноту: `main` уехал на `b22db1d9` (`2026-09-04T00:17:05Z`) уже после старта зачтённого зелёного `checks` (`2026-09-03T23:48:10Z`). Значит зелёное дерево — это голова, слитая с базой ТОГО момента, а не с нынешней. Ветку мерил CI и ревьюер; **слитое с новой базой дерево не мерил никто** — поэтому я его измерила, и вот чем:

| момент (UTC) | команда | результат |
| --- | --- | --- |
| `00:21:46Z` | `git merge-tree --write-tree origin/main origin/pr-262` | дерево `d534b1ed17510804037e24b3efd18c0f526e7bd9`, конфликта нет |
| `00:21:46Z` | `git diff --stat b22db1d9 d534b1ed` | ровно `REVIEWER.md \| 18 +`, **один файл**; удалений в диффе — **0** (проверено счётом строк `^-`) |
| `00:21:58Z…00:25:56Z` | `pnpm test` на слитом дереве (worktree по коммиту `40ec7f31`) | **214 файлов / 3523 теста + 2/7 — все passed, exit 0** |
| `00:26:01Z…00:26:06Z` | `pnpm typecheck` там же | оба пакета `Done`, exit 0 |

То есть переезд базы этот merge не меняет: слитое дерево отличается от нынешнего `main` ровно теми восемнадцатью строками, что ревьюер и читал, и на нём же зелена полная сюита. Пробный worktree снесён, рабочее дерево роли чистое (`git status --porcelain` пуст, `00:26:43Z`).

Нормой это по-прежнему не объявлено — я делаю это как замер под конкретную кнопку, а не как процедуру.

## 4. Приёмка правки — объявляю честно, что она ещё не наступила

Юнит-покрытия у критерия 12 нет по построению: `REVIEWER.md` — промпт, его не читает ни одна функция пакета. Приёмка **живая и одноразовая**: на ПЕРВОМ же будущем PR, объявившем класс «полевой измеренный дефект», вердикт первого круга обязан назвать нейтральность к норме — без вопроса в теле PR и без второго круга. До этого момента факт «цена снята» непроверен, и я называю его непроверенным. Если первый же такой круг снова промолчит — это дефект правки, а не повод переспросить.

## 5. Хвост открытых PR — прогнан целиком, 00:26:15Z…00:26:36Z

За время этого треда #260 и #261 ушли в `main` тактами своих тредов — в открытых их больше нет. Остальные:

| PR | тред | дверь |
| --- | --- | --- |
| **#256** | `062-contour-boundary` | **`nothing in the facts forbids this merge`** — гард 1 ok (`approve` на `9438499` внутри круга `33820624301`, окно `00:09:29Z…00:11:59Z`), гард 2 ok, гард 4 ok |
| #259 | `119-mute-count-blames-github` | REFUSED — гард 1 STOP (approve на `42ec617`, голова уехала на `b18debf`) и гард 4 STOP (`.github/workflows/notifier-watch.yml` → твоя кнопка) |
| #263 | `090-what-kills-a-note` | REFUSED — гард 1 STOP (вердикта нет на `8acc534`); гард 4 STOP (`PROTOCOL.md` + четыре карточки ролей → твоя кнопка) |
| #264 | `120-enclosing-repo-probe-silent` | REFUSED — гард 1 STOP (вердикта нет на `d4e313a`), гард 2 STOP (`checks=IN_PROGRESS`) |

**#256 созрел и я его НЕ мёржу из этого прогона** — причина не в гардах, а в следе: гард 5 обязан лечь в ленту треда-владельца `062`, а прогон связан тредом `121`, писать в `062` мне нечем. Называю фактом замера, чтобы созревший PR не пролежал молча; домержит его такт curator на треде `062`. Нота о переезде базы напечатана и на #256, и на #259, и на #263 — там это дело их тактов.

## 6. Ход

`waiting-on → curator`, парк на john. Прошу кнопку на #262 (squash, заголовок PR уже в Conventional Commits). У прогонов не стою.

Что делает следующий такт curator на этом треде: читает твой ответ; после merge — контрольное чтение `base.ref` и след в эту ленту; тред после этого закрывается приёмкой не по merge, а по первому живому кругу класса (раздел 4), и до тех пор остаётся открытым.

Незакоммиченного нет: всё в ветке `curator/121-reviewer-md-class` и в PR #262. Время письма — штамп команды.

## msg-009 · from: github · 2026-09-04 · expects: none

PR #262 (docs(reviewer): критерий 12 — на PR класса «полевой дефект» вердикт называет нейтральность к норме (тред 121)) **merged** by maysway → `main`.

## msg-010 · from: curator · 2026-09-04 · expects: answer

**Слово john полем `delivers` (чат ~14:14Z): #262 СМЁРЖЕН его кнопкой. `REVIEWER.md` получил критерий 12 — на PR, объявившем класс «полевой измеренный дефект», ревьюер называет нейтральность к норме ЯВНО.**

Один файл, новый критерий в конце списка; критерии 1–11 не тронуты, перекрёстные ссылки внутри файла остались верны. Пять признаков нормы перенесены **дословно** из класса, с прямой строкой «признаки не переписывай своими словами» — иначе разойдутся две формулировки одного правила.

**Три границы, объявленные john, держатся текстом:** только критерий класса; признаки не переписываются; вывод обязателен **только** на PR, класс объявившем — «на PR, класса НЕ объявившем, о норме молчи как молчал».

**Цена, которую это снимает, измерена:** PR #257 восходил этим классом, третье условие которого — подтверждение ревьюера. Первый круг прочёл дифф и о норме не сказал НИЧЕГО (слова «норма» в вердикте нет ни разу) — потому что документ ревьюера про класс молчал. Условие добывали вопросом в теле PR и **вторым кругом**. Это повторялось бы на каждом будущем PR класса.

## Приёмка — и она честно непроверена

Юнитом не берётся по построению: `REVIEWER.md` — промпт роли, в пакете его не читает ни одна функция. **Живая приёмка на первом же PR класса:** вердикт содержит прямой вывод о норме первым кругом, без вопроса в теле PR и без второго круга.

Сам этот PR класса НЕ объявлял (восхождение — слово john), поэтому его круг был контролем третьей границы: вывода о норме там не требовалось, и его не было.

**Тред закрывай своим тактом** — живой работы в нём не осталось.

waiting-on → curator.

## msg-011 · from: curator · 2026-09-04 · expects: none

**#262 в `main` кнопкой john; критерий 12 измерен в дереве `main`. Тред я ЗАКРЫВАЮ, переведя приёмку с полевого образца на перечисление фактов — и называю это прямо, потому что это ОТМЕНА моего же критерия из письма `2026-09-04T00-2*Z-curator.md` (раздел 4), а не его исполнение. Основание — в разделе 4 ниже.**

## 1. След гарда 5 — merge #262, пять гардов поимённо

Рука — не моя и стать моей не могла: гард 4 стоял `STOP` по построению (`REVIEWER.md` — док власти), а просьба о кнопке легла в эту ленту ДО merge. Поле `mergedBy=maysway` руки не называет само по себе — тем же логином печатается и merge роли, — поэтому руку доказывают эти два факта, а не поле.

| гард | чем закрыт |
| --- | --- |
| 1 | `approve` на `b53b11ee3c011c53ff4d4286b96ec79c5938b8e8` — письмо `reviewer-pr` в этой ленте, вердикт лежит ВНУТРИ окна закрытого успешного круга `33819826305` (`Claude PR Review`, `event=pull_request`, `2026-09-03T23:58:07Z…2026-09-04T00:01:55Z`) на той же голове. Сироты нет |
| 2 | зелёные на той же голове: `review=SUCCESS, checks=SUCCESS`; прогон `checks` — [`33819151813`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/33819151813) |
| 3 | восхождение к слову john «(А)» — письмо `2026-09-03T23-14-11Z-curator.md` этого треда. Класс «полевой измеренный дефект» здесь НЕ объявлялся и не был нужен |
| 4 | **STOP** — `john merges this one — it changes REVIEWER.md`. Merge исполнен кнопкой john 2026-09-04T14:14:11Z |
| 5 | это письмо |

## 2. Контрольное чтение цели — ответ инструмента ≠ факт

Время у каждого факта снято `date -u` в том же вызове, что и сам факт:

| момент (UTC) | команда | результат |
| --- | --- | --- |
| `14:15:47Z` | `gh pr view 262 --json state,mergedAt,mergeCommit,baseRefName,…` | `state=MERGED`, `base=main`, голова `b53b11ee…`, squash-коммит `08e7cc358c71284da75bda1969b329b0104fca0b`, `mergedAt=2026-09-04T14:14:11Z`, файлов — один: `REVIEWER.md` |
| `14:15:55Z` | `git merge-base --is-ancestor 08e7cc35 origin/main` | exit 0 — коммит лежит НА ЛИНИИ `main`, а не «где-то существует» |
| `14:15:55Z` | `git show --stat 08e7cc35` | `REVIEWER.md \| 18 +`, `1 file changed, 18 insertions(+)`, удалений 0 |
| `14:17:41Z` | `git show origin/main:REVIEWER.md \| grep -nE '^[0-9]+\. \*\*'` | двенадцать критериев; 1–11 на своих номерах, 12-й — последний (стр. 138), раздел «Формат вердикта» — стр. 157. Сдвига номеров нет |

То есть в `main` лежит ровно то дерево, которое читал ревьюер: восемнадцать добавленных строк, ни одной удалённой, критерии 1–11 не тронуты.

## 3. Дословность переноса — измерена моей рукой, а не пересказом доклада

Граница john (2) требовала перенести пять признаков нормы и фразу «Починка возвращает поведение…» ДОСЛОВНО. Ревьюер это подтвердил; я перемерила независимо, `14:17:33Z`, на дереве `origin/main`:

```
for f in REVIEWER.md docs/roles/curator.md; do
  git show origin/main:$f | tr '\n' ' ' | sed 's/  */ /g' | grep -o 'Нормой здесь считается.*уже объявлено'
done | ...  # два фрагмента → diff
```

Оба фрагмента — **479 байт**, `diff` пуст. Расхождение только в переносах строк (разная ширина обёртки в списке), текст побайтово один.

## 4. Приёмка: полевой образец уступает перечислению — и поэтому я его отменяю

В письме `…00-2*Z-curator.md` я объявила приёмку живой и одноразовой: «на ПЕРВОМ будущем PR, объявившем класс, вердикт первого круга обязан назвать нейтральность к норме». Сегодняшний замер говорит, что этот критерий жжёт слот каждый такт и ничего не добавляет:

- материала в полёте НЕТ: из трёх открытых PR класс не объявляет ни один — #264 отрицает его прямо в теле («класса «полевой измеренный дефект» здесь **нет**»), #265 и #267 о нём не упоминают вовсе (`14:16:16Z`);
- значит каждый следующий такт на `121` читал бы ленту, не находил нового и передавал ход, стоя в очереди рядом с одиннадцатью тредами, у которых работа есть.

Перечисление, доступное сейчас и строго более сильное, чем образец:

1. текст критерия 12 лежит в `main` (раздел 2), в конце списка критериев, перед «Форматом вердикта»;
2. он поднимается ОБЪЯВЛЕНИЕМ класса, а не темой диффа — это в его последней строке, и ревьюер уже прочёл его именно так: применил к самому #262 и объяснил, почему тот неприменим;
3. признаки нормы в нём побайтово те же, что в гарде 3 карточки curator (раздел 3) — вторая формулировка одного правила не заведена;
4. критерии 1–11 не тронуты, номера не сдвинуты.

## 5. Что остаётся неизмеренным — и где именно это проявится

Не доказано одно: что будущий круг на PR с объявленным классом действительно произнесёт вывод. Юнит-покрытия у этого нет по построению — `REVIEWER.md` промпт, его не читает ни одна функция пакета.

Но это НЕ открытый предмет треда `121`, а проверка одного чтения в том такте, где случай возникнет: PR объявил класс → вердикт первого круга обязан содержать «дифф новой нормы не вводит» либо «вводит: <что именно>». Молчание там — дефект критерия 12, и разбирается он в треде ТОГО PR, где есть и дифф, и круг, и вердикт. Держать ради этого открытым тред-родитель — это ждать образца, который придёт сам, а он не приходит.

## 6. Названный риск без владельца — и почему я не завожу под него задачу

Один и тот же абзац (479 байт) теперь лежит в двух файлах: `REVIEWER.md` и `docs/roles/curator.md`. Так велела граница john — переносить дословно, — и это правильно; но синхронность не держит ничто: тестов, читающих `REVIEWER.md`, нет. Расхождение ловится одной командой, той же, что в разделе 3 — сравнением двух фрагментов между собой.

Задачей это не заводится, и причина не в дешёвом молчании: измеренного дефекта нет (расхождение ещё не случилось), гард против него — тест в `packages/**` (зона dev-core) плюс правка формулировки в доке власти, то есть кнопка john. Называю фактом, чтобы у того, кто следующим тронет любую из двух фраз, была команда, а не память.

## 7. Хвост открытых PR — три созревших, все чужих тредов

`merge-gate --ref origin/main --pr <n> --review-workflow 'Claude PR Review'`, вызовы между `14:18:00Z` и `14:18:19Z`:

| PR | тред-владелец | дверь |
| --- | --- | --- |
| #264 | `120-enclosing-repo-probe-silent` | **`nothing in the facts forbids this merge`** — гард 1 ok (`approve` на `d4e313a` внутри круга `33825401985`, `01:20:42Z…01:24:54Z`), гард 2 ok, гард 4 ok (3 пути, доков власти нет) |
| #265 | `072-ci-outcome-not-delivered` | **`nothing in the facts forbids this merge`** — гард 1 ok (`approve` на `957a052`, круг `33823774836`, `00:56:05Z…01:08:59Z`), гард 2 ok, гард 4 ok (7 путей) |
| #267 | `123-repair-refusal-not-in-the-digest` | **`nothing in the facts forbids this merge`** — гард 1 ok (`approve` на `1479c5c`, круг `33829642739`, `02:28:22Z…02:33:24Z`), гард 2 ok, гард 4 ok (7 путей) |

Ни один из трёх я не мёржу из этого прогона, и причина не в гардах, а в следе: гард 5 обязан лечь в ленту треда-владельца, а прогон связан тредом `121`. Молчанием это не остаётся: все три треда стоят в очереди curator (`cli mail --role curator`, `14:18:34Z` — `120`, `072`, `123` в списке), то есть у каждого будет свой такт, где и гард 3, и след на месте.

Дверь на всех трёх напечатала ноту о переезде базы (`main` уехал на `55b1ca4` в `14:14:33Z` — это merge #259, уже после стартов зачтённых зелёных). Нормой это не объявлено, гасится замером слитого дерева, и это дело их тактов, не моего.

## 8. Ход

Тред закрываю: `thread status --thread 121-reviewer-md-lacks-the-class --status closed --write`, следом контрольное чтение статуса. `waiting-on → —`: следующего хода здесь нет, предмет исчерпан разделами 1–4.

Незакоммиченного нет: ветка `curator/121-reviewer-md-class` слита в `main` squash-коммитом `08e7cc35`, рабочее дерево роли чистое. Время письма — штамп команды.
