# Стоячий адрес: демон перезапустил себя на новый код

participants: github, curator, john · status: open

## msg-001 · from: github · 2026-09-07 · expects: none

## Демон перезапустил себя на новый код — без руки, и вот чего это стоило

Ящик дождался конца живых сессий, починил своё дерево и поднялся на новом коде. Ход никому не нужен для ремонта — он уже сделан; это отчёт о нём, потому что тихий самоперезапуск ничем не лучше тихого дрейфа.

- **контур:** `/home/lle/projects/agent-crew-orchestrator`
- **какой код был:** `585abbea86be`
- **какой стал:** `3c7d2b9194b4`
- **сколько отставал:** 1 коммит(ов)
- **сколько ждал сессии:** не записано — файл памяти писан кодом до `drainSince`
- **когда пошёл:** 2026-09-07T17:24:13Z

**Ход curator — ровно на одно действие:** прочитать это и, если отчёт полон, донести john. Ремонта здесь нет: дрейф уже закрыт, а звонок о дрейфе (тред 141, #301) на этот ящик больше не придёт.

## msg-002 · from: curator · 2026-09-07 · expects: ack

Требуется решение john — вопрос первой строкой: отчёт демона о самоперезапуске донесён, но ОДНА ЕГО СТРОКА ЛОЖНА, и слово john нужно ровно на одно — считать ли починку этой формулировки обычной починкой (тогда постановку dev-core пишу сама) или новой формой сообщения (тогда это кнопка john).

## 1. Что в отчёте подтвердилось — своими замерами, а не чтением письма

- **код стал `3c7d2b9194b4`** — `git log -1 HEAD` в контуре и `origin/main` дают ровно этот sha;
- **демон исполняет именно его:** `.orchestrator/daemon-code.json` → `{"sha":"3c7d2b91…","startedAt":"2026-09-07T17:24:32Z","pid":2312495}`, и живой процесс 2312495 (`ps -o lstart` → Sep 7 19:24:24 = 17:24:24Z) — это работающее, а не «смёржено»;
- **отставание было 1 коммит и ремонт прошёл:** `daemon.log` строки 31491–31497 — `git pull --ff-only — ok: Fast-forward · PROTOCOL.md | 16 +++`, `pnpm install skipped` (манифесты в `585abbea..3c7d2b91` не менялись), выход кодом 75 под супервизором;
- **письмо не задвоено:** `.orchestrator/self-restart-letters.json` → `signature: 3c7d2b91…\0 2026-09-07T17:24:13Z`, дальше в журнале только `SUPPRESSED, nothing new to say`.

Ремонта здесь действительно нет — дрейф закрыт.

## 2. Что в отчёте НЕ ДЕРЖИТСЯ (замерено)

Строка отчёта: «**сколько ждал сессии:** не записано — файл памяти писан кодом до `drainSince`». И абзацем выше: «Ящик **дождался конца живых сессий**».

Оба утверждения об ЭТОМ эпизоде неверны:

1. **Код, писавший файл памяти, `drainSince` ЗНАЛ.** Поле пришло коммитом `7db145ba` (#309, тред 141) — единственный коммит, трогающий `drainSince` в `self-restart.ts`. `git show 585abbea:packages/agent-protocol/src/orchestrator/self-restart.ts | grep -c drainSince` → **8**. То есть в том самом `585abbea`, который отчёт называет «какой код был», поле уже есть.
2. **Ждать было НЕЧЕГО — дрейна не было вовсе.** `daemon.log:31491`: «the loaded code is behind 3c7d2b91 (1 commit(s)), **leases 0**, state clean»; `daemon.log:31496`: «this tick launches nothing — there was nothing to withhold, and the **zero-lease condition** … holds by itself». Ни одной строки «the box is draining to restart» в окне этого эпизода нет — такие строки в журнале есть, но у прошлых перезапусков (строки 13868–13988).
3. **И это ровно то, что предсказывает код.** `self-restart.ts`, две последние строки `selfRestartVerdict`: `if (live.length > 0) return { kind: "drain", … }; return { kind: "go", … }`. `drainSince` пишет ТОЛЬКО ветка `drain` (`rememberSelfRestartDrain`, `drainSince: input.at`). Нет живых сессий → сразу `go` → поля в памяти нет никогда. Файл это и подтверждает: `.orchestrator/self-restart.json` = `{"target":"3c7d2b91…","attempts":1,"at":"2026-09-07T17:24:13Z","from":"585abbea…","behind":1}` — ключа `drainSince` в нём НЕТ.

**Дефект.** У отсутствия `waitedForSec` причин ТРИ — старый код, кривая пара стамов, **дрейна не было**, — а `waitedLine(undefined)` (`packages/agent-protocol/src/orchestrator/self-restart-letter.ts:87`) печатает одну фиксированную фразу, про старый код. Две первые причины в doc-комментарии там же названы («predates `drainSince` **or** the pair came out backwards»), третья — самая частая, потому что перезапуск на пустом ящике штатен — не названа вовсе. Заодно читателю сообщается, что ящик кого-то дожидался, чего не было.

Цена ровно та, о которой тред: тихий самоперезапуск лечится отчётом, а отчёт, объясняющий пропуск неверной причиной, отправляет читателя чинить возраст кода вместо того, чтобы прочитать «ждать было некого». Отчёт полон по составу фактов, но одна строка в нём — не «не знаю», а неверное «знаю почему».

## 3. Что предлагается и о чём вопрос

Постановка dev-core: `selfRestartEvent`/`waitedLine` должны различать «дрейна не было» (в памяти есть `at`, но нет `drainSince`) и «файл писан кодом до `drainSince`», а прозу «дождался конца живых сессий» ставить по факту, а не всегда. Проверяемость: юниты `self-restart-letter.test.ts` и `self-restart.test.ts` оба входа уже гоняют — добавляется случай «память без `drainSince`, `go` при нуле лизов»; живой приёмки человеком не требует; интеграционного покрытия сознательно не добавляется — доставка письма покрыта `self-restart-letter.process.test.ts`.

**Вопрос john ровно один.** По классу «полевой измеренный дефект, новой нормы не вводит» такую постановку curator ставит без слова john (первоисточник замера — этот тред и `daemon.log:31491–31497`, 2026-09-07). Но починка меняет ТЕКСТ письма, которое читает человек, а «новая форма сообщения» в карточке отнесена к норме. По правилу последней строки — спрашиваю: **это починка ложной строки (ставлю dev-core сама) или новая форма (кнопка john)?**

Ход остаётся у curator, тред заморожен парком до слова john. Некоммиченного за мной нет: своей рукой не правился ни один файл — работа этого хода — замеры и это письмо.

## msg-003 · from: curator · 2026-09-08 · expects: none

**Слово john (чат 2026-09-08 ~09:21Z). Ответ на ЭТОТ адрес дан общим письмом в `162` — четыре адреса самоперезапуска (`162`, `164`, `165`, `167`) закрываются одним словом, потому что все они об одном классе событий.**

**Коротко, чтобы адрес не требовал перехода:**

- **ack принят:** ящик чинил дрейф сам, дождавшись живой сессии, и говорил об этом. Приёмка предмета `141` состоялась полем;
- **решение прежнее и в силе (07.09 ~18:23Z): сужаем ПИСЬМО, ремонт не трогаем.** Перезапуск — на любом дрейфе; письмо — только когда перезапуск сменил исполняемое. Работа поставлена в `161`;
- **цена измерена:** стоячий адрес породил **восемь тредов за сутки** (`156`, `160`, `161`, `162`, `163`, `164`, `165`, `167`), четыре дошли до john. Каждое письмо назначает ход, то есть стоит подъёма роли — включая отчёты о перезапусках, не поднявших ни строки кода;
- **устав адресов не меняется**, сам факт отчёта не глушится — сужается только повод писать.

**Закрывай этот адрес.**

waiting-on → curator.
