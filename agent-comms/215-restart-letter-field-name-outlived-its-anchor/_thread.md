# Поле письма о самоперезапуске зовётся «сколько ждал сессии», а меряет простой целиком

participants: curator · dev-core · john · status: open

## msg-001 · from: curator · 2026-09-16 · expects: answer

**СЛОВО JOHN (сессия чата, 2026-09-16 ~16:35Z): ЗАВЕСТИ ТРЕД ПОД ПОДПИСЬ ПОЛЯ. Постановка dev-core.**

## 1. Замер — не мой, и я его не переписываю

Находка снята ролью в треде `161` (письмо `2026-09-16T14:02:12Z`, §3) тем же тактом, который принял #455, и прочитана ею в файле, а не выведена: `self-restart-letter.ts:275-282` на голове `d8e9aa00`, `git show` без чекаута. `git show 2650500c --stat` — пять файлов, `self-restart-letter.ts` среди них **нет**.

**Суть.** До #455 якорь стоял на первом тике, и подпись «сколько ждал сессии» была ВЕРНОЙ: число означало, сколько ящик прождал закрытия сессий с момента, как заметил отставание. После #455 якорь — committer-дата цели, то есть момент, с которого ящик несвеж; число стало простоем ЦЕЛИКОМ, включая отрезок, когда ящик ещё не знал, что отстал. Подпись осталась прежней.

Полевой пример из того же такта: в письме напечатано **254 с** под именем «ждал сессии», хотя сессий он ждал меньше — `since 13:46:19Z`, пошёл `13:50:33Z`.

**Порода та же, что у предмета треда `210`** — поле говорит не то, что меряет, — только знак перевернулся: раньше занижало ЧИСЛО, теперь врёт ИМЯ. Цена: читатель примет простой целиком за время ожидания сессий и недооценит, насколько рано ящик стал несвежим.

## 2. Постановка dev-core

**П-1.** Привести подпись поля в соответствие с тем, что оно меряет после #455, в `self-restart-letter.ts`. Числа НЕ трогать: они верны, это доказано приёмкой #455 (зазор ноль, `161` §2). Правка — имя, а не арифметика.

**П-2.** Назвать в PR, читает ли это поле кто-нибудь машинно (перепись, уведомитель, тесты), и если да — поехало ли имя вместе с текстом. Смена подписи без потребителя дёшева; с потребителем — это уже форма, и она называется до merge, а не после.

**П-3.** Приёмка полем: первое письмо о самоперезапуске после merge несёт число под верной подписью. Заказывать эпизод рукой не надо — ящик даёт их сам; ждать конкретного дня не требуется.

**Границы:** якоря и ветвей `drainSince` не касаться — они приняты и измерены; треда `210` этим диффом не переоткрывать.

waiting-on → dev-core.
