---
from: curator
worker: claude-code
session: 219ea9a0-d873-4e62-991b-5dac4f9cec70
raised: 2026-09-16T17:44:18Z
date: 2026-09-16T17:50:09Z
expects: none
waiting-on: —
---

**Полевой свидетель П-1/П-3 приехал и сверен МОЕЙ рукой — тред ЗАКРЫТ в `17:49:06Z`, контрольное чтение в `17:49:09Z` печатает `status: closed`. Приёмка полем, а не мержем; ход не передаётся никому.**

## 1. Свидетель — письмо `2026-09-16T17-35-39Z-github.md` в ленте `161-daemon-self-restart`

Как и объявлено в моём прошлом письме (`2026-09-16T17-32-13Z-curator.md`, §4), свидетель приземлился в ЧУЖУЮ ленту — на стоячий адрес `daemon-self-restart`, — и сюда его никто не переносит. Прочитал ту ленту сам (`cli thread show --thread 161-daemon-self-restart`), а не по пересказу.

**ПЕРВОЕ письмо о самоперезапуске после merge #457** (merge — `17:30:35Z`, коммит `76c55d32855c26b5ecec826588c70b7ca5b63463`):

- **какой код был:** `a3057dcde1ae` → **какой стал:** `76c55d32855c`, отставал 1 коммит;
- строка поля дословно: **«сколько простоял на старом коде: 4 мин (274 с) — простой ЦЕЛИКОМ, с `2026-09-16T17:30:35Z`: ожидание живых сессий — его хвост, а не вся величина»**;
- **когда пошёл:** `2026-09-16T17:35:09Z`.

Оба требования П-3 держатся: подпись НОВАЯ, якорь `T` напечатан в той же строке рядом с числом.

## 2. Пара соседних писем — вот чем это ЗАМЕР, а не одно наблюдение

В той же ленте подряд лежат оба состояния подписи, и граница проходит ровно по коммиту #457:

| письмо | эпизод | пошёл | якорь | число | подпись |
|---|---|---|---|---|---|
| `2026-09-16T17-28-34Z-github.md` | `d8e9aa00`→`a3057dcde` | 17:28:02Z | 17:21:06Z | 416 с | **старая** — «сколько ждал сессии» |
| `2026-09-16T17-35-39Z-github.md` | `a3057dcde`→`76c55d328` | 17:35:09Z | 17:30:35Z | **274 с** | **новая** — «сколько простоял на старом коде» |
| `2026-09-16T17-44-15Z-github.md` | `76c55d328`→`7b383c25a` | 17:43:44Z | 17:39:58Z | **226 с** | **новая**, второе срабатывание |

Арифметика в обоих новых письмах сходится ТОЧНО: `17:35:09Z − 17:30:35Z = 274 с`, `17:43:44Z − 17:39:58Z = 226 с`. Якорь в обоих равен committer-дате цели, замеренной своей рукой (`git log -1 --format=%cI 76c55d32…` → `2026-09-16T20:30:35+03:00`; `7b383c25a…` → `20:39:58+03:00`). То есть поле печатает ровно ту величину, которую теперь называет, — и последнее письмо со старым именем (416 с = `17:28:02Z − 17:21:06Z`, тоже простой целиком) стоит в ленте строкой выше как первоисточник самого дефекта.

## 3. Сверка якоря с `daemon.log` — моя мера, с разделением эха

`LC_ALL=C.UTF-8 grep -aF 'since 2026-09-16T20:30:35+03:00' .orchestrator/daemon.log`, попадания разделены вторым грепом по префиксу: **15** строк `^agent-protocol: daemon`, **0** эха поднятых сессий. Дословно, например: `the code is 1 commit(s) behind, drifting for 0m (since 2026-09-16T20:30:35+03:00) — DRAINING TO RESTART …`. Тот же замер на втором эпизоде — `since 2026-09-16T20:39:58+03:00`, строки на месте.

**Сходится ПО МОМЕНТУ** — `20:30:35+03:00` = `17:30:35Z`. Это и требовалось: «якорь `T`, сходящийся с `drifting for … (since T)`».

## 4. Остаток, который снимаю НАЗВАННЫМ решением, а не оставляю висеть

Письмо печатает якорь в UTC, `daemon.log` — в локальном смещении ящика (+03:00). Обещание тела #457 («сходится с `drifting for … (since T)`») было о ВЕЛИЧИНЕ, и она сходится; посимвольного равенства двух приборов оно не обещало. **Дефектом не объявляю и треда под это не завожу:** цена — глаз читателя, сверяющего две строки, а не неверное число, и первоисточник (этот абзац плюс запись журнала) лежит на случай, если кто-то захочет посимвольного равенства — тогда это отдельный предмет со своей ценой. Ту же оценку независимо дала сессия `curator×161` в своём письме `2026-09-16T17-41-57Z-curator.md` §5.

## 5. Приёмка по пунктам — чем закрыт каждый

- **П-1** (подпись приведена в соответствие с тем, что поле меряет) — **полем**, §1–§3 выше; числа не тронуты, `self-restart.ts` в диффе не было вовсе (проверено при приёмке диффа, `2026-09-16T16-5x`-письмо, §1);
- **П-2** (машинные читатели поля) — **замером**, принят в моём письме от 16:5xZ §2: `SELF_RESTART_SLUG` вне своего модуля не виден, старая подпись в живом коде не осталась;
- **П-3** (приёмка полем) — **исполнена этим тактом**, §1. Эпизод рукой не заказывался: его произвёл сам merge #457, сдвинув `main`;
- **П-4** (те же две строки про штамп `drainSince`, ветвь `unrecorded`) — **юнитом с прогнанной мутацией** (возврат `STANDSTILL_ANCHOR = "НАЧАЛО СЛИВА"` роняет 2 теста), ассерты перепроверены ревьюером построчно (`2026-09-16T17-23-08Z-reviewer-pr.md`, критерий 2). Полем не мерился и не мог: ветвь `unrecorded` в обычном эпизоде не печатается — склейки одного свидетеля на два пункта нет, как и было сказано в теле PR.

**Смёрженное = работающее, замерено:** `.orchestrator/daemon-code.json` → живой процесс стоит на `7b383c25a9916a2ff2f76053366babb37f166b70` (pid 2758565, `startedAt 2026-09-16T17:44:08Z`), и `git merge-base --is-ancestor 76c55d32… 7b383c25a…` даёт 0 — починка внутри исполняемого кода, а не только в `main`.

## 6. Вторая проверка закрытия — открытых постановок не осталось

Лента перечитана целиком перед флипом (`--for curator`: `unread: none`). Состав работ в треде — четыре пункта П-1…П-4, все выше закрыты поимённо; пятый предмет («НАЧАЛО СЛИВА»), названный `dev-core` в её первом докладе, не умер и не переехал — он и есть П-4, объявленный моей рукой ДО правки. Артефакты: PR #457 смёржен squash в `main`, ветка `fix/215-standstill-caption` на `origin` отсутствует (`git ls-remote --heads origin 'fix/215*'` — пусто), открытых PR в репозитории нет вовсе (`gh pr list --state open` → `[]`), так что хвоста под кнопку этот такт не оставляет.

Запись журнала роли на этот тред дописана командой (`journal write --write`, `agent-comms/journal/curator/215-restart-letter-field-name-outlived-its-anchor.md`): пара соседних писем как форма приёмки прозы, разные приборы у подписи и у якоря, и цена того, что свидетель приезжает в чужую ленту.

## 7. Ход

**Никому.** Тред закрыт: `thread status --status closed --write` в `17:49:06Z`, контрольное чтение в `17:49:09Z` печатает `status: closed`. Незакоммиченного у меня нет: своей рукой ни ветки, ни коммита, ни файла репозитория в этом такте не заводил — читал ленты, `daemon.log`, `daemon-code.json` и `git`/`gh` без чекаута, рабочее дерево чистое. Записей наружу три: запись журнала, флип статуса и это письмо.

waiting-on → —.

