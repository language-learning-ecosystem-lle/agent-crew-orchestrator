---
from: dev-core
worker: claude-code
session: 7da2ebac-f9ce-44cf-b3a0-55733521d54a
raised: 2026-09-16T16:38:16Z
date: 2026-09-16T16:47:55Z
expects: answer
waiting-on: curator
---

**Сделано: PR #457, голова `9ee231d6`. Жду зелёный `checks` — метка `review` вешается ПОСЛЕ него, на той же голове.**

## П-1 — имя, не арифметика

Строка зовётся **«сколько простоял на старом коде»** во всех четырёх ветвях (`waitedLine`, одна константа `STANDSTILL` на все — чтобы вторая копия имени не отстала от первой, как отстала эта). Диффа в `self-restart.ts` нет вовсе: якоря, ветви `drainSince` и числа не тронуты.

**Добавил одно, и не как украшение:** рядом с числом печатается ЯКОРЬ — «простой ЦЕЛИКОМ, с `T`: ожидание живых сессий — его хвост, а не вся величина». `T` — то же значение, которое `daemon.log` печатает в `drifting for … (since T)`, так что новая подпись проверяема ЛОГОМ, а не только исходником; на этой ветви якорь не бывает пуст (конечный `waitedForSec` — вычитание из распарсенного `drainSince`). Если считаешь это расширением постановки — скажи, сниму одной строкой.

**Бдение не потеряно вместе с подписью.** Что ящик дождался живых сессий и ни одной не порвал, говорит ПЕРВАЯ ФРАЗА письма (`drainSentence`) — из того же факта, что длительность вообще известна (`drainSince` ставится только на вердикте `drain`, а тот достижим лишь при живых сессиях). Четвёртый факт john'а в письме остался; ЧИСЛОМ он больше не заявляется. Отдельный тест это пиньит.

## П-2 — машинных читателей НЕТ (замер, не вывод)

Греп по репозиторию (`--include=*.ts,*.md,*.yml,*.json`, без `node_modules`):

- подпись «сколько ждал сессии» встречалась **ровно в двух файлах** — `self-restart-letter.ts` и его юнит. Оба едут этим диффом;
- адрес письма (`SELF_RESTART_SLUG`) не виден ни одному модулю, кроме самого `self-restart-letter.ts`: ни `notify`, ни `metrics`, ни `mail`, ни воркфлоу тела письма не разбирают — уведомитель везёт тело как есть;
- **имя в коде (`waitedForSec`) НЕ переименовано намеренно:** это ключ события, читаемый `waitedLine` и `drainSentence` того же модуля плюс тестами `self-restart*.test.ts`; на диске его нет вовсе — файл памяти несёт `at`/`drainSince`, а вычитание считается при чтении. Переименование двигало бы сигнатуры ради прозы;
- **формы наружу дифф не меняет:** ни команды, ни флага, ни поля файла памяти, ни строки `daemon.log`. Поэтому README («Commands») не правится; `docs/protocol-reference.md` правится — блок о памяти самоперезапуска несёт теперь и переезд подписи, и полевой пример.

Итог по П-2: смена подписи здесь ДЕШЁВАЯ, потребителя у неё нет.

## Тесты

Новых ассертов три: подпись (старая запрещена явным `not.toContain`), якорь рядом с числом, бдение словами первой фразы. Три существовавших («не записано», «никогда не ноль») переписаны на новое имя, смысл не тронут. **Мутации прогнаны обе:** возврат старой подписи роняет 4 теста, снятие якорной оговорки — 1; снимал мутации правкой файла, не `git checkout --`.

Прогон: `self-restart-letter.test.ts` + `self-restart-letter.process.test.ts` + `self-restart.test.ts` — **198 тестов, зелено**; `biome check` и `tsc --noEmit` чисто. На момент письма `checks` по #457 — `pending`; у конвейера не стою.

## П-3 — приёмка полем

Первое письмо о самоперезапуске после merge обязано нести число под именем «сколько простоял на старом коде» и якорь `T` в той же строке, сходящийся с `drifting for … (since T)` в `daemon.log`. Рукой эпизод не заказываю.

## Замечено и НЕ тронуто — решение за тобой

Тот же стамп строкой ниже зовётся **«НАЧАЛО СЛИВА»**: ветвь `когда пошёл:` у `unrecorded` и журнальная строка `NOT DELIVERED`. После #455 это неточно той же породой — стамп есть момент, с которого код НЕСВЕЖ, а дренаж мог открыться позже. Существенное утверждение той строки («это НЕ момент перезапуска») осталось верным, а границы постановки — «якоря и ветвей `drainSince` не касаться» — я не переходил, поэтому строку и пиньящий её тест (`toContain("НАЧАЛО СЛИВА")`) оставил как есть. Цена пропуска ниже, чем у предмета этого треда (там врало ЧИСЛО в подписи, тут — имя стампа в пояснении отсутствия), но порода одна. Открывать ли отдельным тредом — твой ход.

Запись журнала на тред `215` положена (`journal write --write`), там же — правило, ради которого тред стои́т помнить: **когда правка двигает ЯКОРЬ величины, потребителей у величины два — код, который её считает, и проза, которая её называет; приёмка #455 мерила первого и второго не видела вовсе.**

**Ход:** у curator — по вопросу о «НАЧАЛО СЛИВА» и по метке/кнопке на #457 после зелени. Я остановился здесь: рабочее дерево чистое, незакоммиченного нет, ветка `fix/215-standstill-caption` запушена, PR #457 открыт командой пакета.

