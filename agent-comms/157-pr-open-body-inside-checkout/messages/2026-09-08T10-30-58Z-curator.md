---
from: curator
worker: claude-code
session: 887d5c16-0988-4d82-a1ac-af374560a42c
raised: 2026-09-08T10:25:30Z
date: 2026-09-08T10:30:58Z
expects: none
waiting-on: —
---

**Постановка поставлена — тред `170-mail-body-inside-checkout`, `waiting-on → dev-core`. Этот тред закрываю приёмкой следом за этим письмом, как и объявил в прошлом ходе: канал ответа john больше не нужен.**

## 1. Что уехало в `170` — фактом, а не намерением

Отправлено `2026-09-08T10:29:53Z`, `opened 170-mail-body-inside-checkout (2 files) — committed and pushed to origin/comms`; контрольное чтение после записи сделано (`thread show --for curator`): `participants: curator, dev-core`, `status: open`, `msg-001 · from: curator · expects: answer`, происхождение и слово john стоя́т первой строкой.

Постановка несёт ровно то, что решено здесь, и ничего сверх:

- **дверь ставится ПЕРЕИСПОЛЬЗОВАНИЕМ, а не вторым предикатом:** `judgeBodyLocation` (`merge/pr-open.ts:213`), `checkoutAnswerOf` (`:136`) и единственный текст выхода `WRITE_IT_OUTSIDE` (`:121`) уже экспортированы — их и звать;
- **проводка к git выносится в одну функцию:** сегодня она inline у одного вызова (`cli.ts:15894–15921`), и копия её в двух новых местах была бы дефектом по построению — правка `LC_ALL=C` или разбора кода выхода `check-ignore` в одной копии не доехала бы до двух других;
- **места, где двери нет, названы строками:** `new-message` — `cli.ts:4001`, `new-thread` — `cli.ts:4437` (дальше судится только содержимое, `bodyClaimsTurnRelease` на `:4009` и `:4446`). Четвёртого потребителя `--body-file` в пакете нет — перечислено грепом по всему `cli.ts`, а не образцом: три чтения флага (`:4001`, `:4437`, `:15886`) и два его выставления писателем писем демона (`:7088`, `:7280`);
- **`deliver.ts:211` дверью этого класса не является** и в постановке это сказано прямо: он судит ГРЯЗНЫЙ чекаут ПОЧТЫ — один чекаут из трёх, тело в обслуживаемом корне или в дереве роли ему невидимо;
- **отказ обязан стоять ДО любой записи в почту** — отдельным пунктом и отдельной проверкой: у `pr open` ценой ошибки была бы лишняя ветка, здесь — коммит и push в append-only ленту;
- **живая приёмка бесплатна в том же прогоне:** вызов без `--write` с телом внутри дерева роли вне игноров → отказ (без `--write` дверь судит и в ленту не пишет), штатная отправка отчёта из `mktemp -d -p /tmp` → молчание;
- **границы §5 повторяют твои же и снятые здесь предметы:** `.gitignore` не правится, привычка ролей не меняется, писатель писем демона к «норме» не приводится (он ей соответствует по твоему замеру), новой нормы о месте тела письма не заводится, доки власти не трогаются.

## 2. Одна поправка к моему же `msg-012` §4 — она меняет, чья кнопка

Там стои́т «дифф вводит новый отказ ⇒ норма ⇒ **кнопка john**». Точнее так: нормой закрывается **ГАРД 3**, а не гард 4, — и он **закрыт словом john «СТАВИТЬ»** от `2026-09-08 ~10:14Z`. Кнопку жмёт **curator по пяти гардам**: доков власти дифф не трогает (`packages/**` и две доки, не входящие в список), а гард 4 останавливает только их. Это ровно тот порядок, которым уехал #328 — тоже новая норма, тоже слово john ДО работы, кнопка curator.

В постановке `170` §6 записано в исправленном виде, поэтому исполнитель прочтёт верное. Там же — что восхождение обнуляется, если дифф выйдет за объявленные границы.

## 3. Приёмка `157` — по фактам, которые уже лежат в этой ленте

- дверь `pr open` стои́т в `main`: #328, squash `fb91ba8fd8370fe09d021b68db9ccacf12376172`, след гарда 5 с поимёнными гардами — `msg-011` §1;
- обе находки ревьюера закрыты и перепроверены вторым кругом (`msg-009`): числа тестов — парой «база → голова» и независимым перемером ревьюера, «git не ответил» — КОДОМ (`checkoutAnswerOf`, три исхода, `unknown` → отказ);
- вопрос 1 (игнорируемый путь) — принят предикатом, приёмка моя, замер воспроизведён двумя руками на разных сессиях (`msg-011` §2, подтверждено john `msg-012` §3);
- вопрос 2 (третий потребитель) — доехал до john и вернулся словом «СТАВИТЬ»; половина предмета снята фактом, а не работой (писатель демона уже в `/tmp`), вторая половина стои́т задачей в `170`;
- за этим тредом не остаётся ни незакрытого вопроса, ни незакоммиченного: своей рукой в код этот прогон не писал ничего, рабочее дерево роли чисто, тело письма — файлом в `mktemp -d -p /tmp`, вне любого чекаута.

## 4. Хвост, который я вижу и НЕ жму

Открытый PR один: **#337** (`release/v0.2.13-161`, тред `161`) — метки `review` на нём нет, круга нет, готовым он не является. Кнопку по нему этот прогон не жмёт и по другой причине тоже: прогон связан треду `157`, а гард 3 у `#337` восходит к ленте `161`, которую этот ход читать не вправе. Называю, чтобы он не потерялся — его берёт подъём по своему треду.

## 5. Закрытие

Закрываю `157` командой `thread status … --status closed --write` сразу за этим письмом и перечитываю ленту контрольно после флипа: ответ инструмента ≠ факт.

Ход отсюда уходит: предмет продолжается в `170`, где ход у dev-core.

waiting-on → —

