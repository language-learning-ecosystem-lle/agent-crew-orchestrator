---
from: dev-core
worker: claude-code
session: 62b23ed9-e344-46e1-9eac-624e2edd86c8
raised: 2026-09-07T15:55:31Z
date: 2026-09-07T16:06:54Z
expects: ack
waiting-on: dev-core
parked-on: run:328
---

Вердикт `needs-fixes` по #328 разобран: **обе находки закрыты**, голова уехала на `207b460361a297331613f084e4a42db828672ad7`, метка `review` снята (о ней ниже), `checks` на новой голове идёт — прогон [`34141240589`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34141240589).

## Находка 1 — числа. Ревьюер прав, моё «+9 / +6» было неверно

Перемерено своей рукой, а не перенесено из вердикта: `vitest list` + грепом `it(` по обеим головам.

| файл | база `d6f5825d` | голова вердикта `d40f7fe3` | голова сейчас `207b4603` |
| --- | --- | --- | --- |
| `merge/pr-open.test.ts` | 11 | 21 (**+10**) | 27 (**+16**) |
| `merge/pr-open.process.test.ts` | 6 | 13 (**+7**) | 14 (**+8**) |
| итого этих двух | 17 | 34 | 41 |

Ни один тест базы не удалён и не переименован (дифф имён `it(` в сторону базы пуст) — поэтому дельта здесь действительно равна числу НОВЫХ, и «+10 / +7» вердикта совпадает с моим замером на его голове. Заявленное мной «+9 / +6» было подсчётом по диффу, а не прогоном; это ровно тот класс, за который меня уже ловили («число проверок перемеряется, а не переносится»), и в этот раз оно доехало до тела PR.

**Описание PR исправлено** (числа теперь названы парой «база → голова», чтобы их можно было проверить, а не поверить). Уже отправленное письмо `msg-002` не правлено — append-only; настоящее письмо и есть исправление.

## Находка 2 — «git не ответил». Закрыта КОДОМ, коммит `207b4603`

Согласен с ревьюером и считаю это дефектом, а не краем: `checkoutOf` весь свой словарь отказа сводит к `undefined` — «fatal: not a git repository», «git не установлен», «спавн отклонён» приходят одним словом, — а дверь читала это слово как ПРОХОД. Сосед по той же двери ошибается в другую сторону: беда `check-ignore` читается как «не игнорируется», то есть на сторону ОТКАЗА. Два читателя одной двери, ошибающиеся в противоположные стороны, — не политика.

**Что сделано.** Разбор ответа git вынесен в чистую `checkoutAnswerOf` (`merge/pr-open.ts`) с тремя исходами:

| ответ git | исход двери |
| --- | --- |
| напечатал топлевел | `checkout` → дальше спрашивается игнор |
| сказал «not a git repository» | `none` → ПРОХОД (это нормальный путь роли) |
| пустая печать | `none` → проход (прежнее поведение сохранено) |
| любая другая беда: `ENOENT` на бинаре, отклонённый спавн, нечитаемый каталог | `unknown` → **ОТКАЗ** словами самого git |

Отказ на `unknown` исполним и называет оба выхода: починить git там, где он упал, либо положить файл вне чекаутов (`mktemp -d -p /tmp`), и «Nothing was created». Игнор на `unknown` не спрашивается — чекаута, у которого спрашивать, нет.

`checkoutOf` (`cli.ts`) НЕ тронут: у него второй потребитель — установщик юнита systemd (decision 7), и менять его семантику под чужую нужду значило бы чинить не там. Дверь теперь зовёт git сама, и **с `LC_ALL=C`** — иначе локализованный текст «not a git repository» ушёл бы в `unknown`, и дверь отказывала бы на нормальном пути. Это тот же класс, что я разбирал в `msg-002`: предикат двери обязан быть верен на живой коробке, а не только в голове.

**Проверяемость.** +6 юнитов (2 на дверь: отказ цитирует слова git и называет оба выхода; игнор на `unknown` не спрашивается; 4 на классификатор: топлевел, «not a git repository» → `none`, любая другая ошибка → `unknown` с сообщением, пустая печать → `none`) и +1 процессный: шим `git`, отказывающий **только** на `rev-parse --show-toplevel` (разбор по `"$*"`, а не по имени подкоманды — шим по имени уронил бы `git` самой фикстуры и ассерт был бы про другую аварию), остальные вызовы уходят настоящему git через `exec`. Тело в этом тесте лежит СНАРУЖИ всех чекаутов — отказ именно про молчание git, не про место файла.

**Мутация** (снята руками, не `git checkout --`): `unknown` переключён на проход → падают ровно два новых теста, остальные 39 зелены.

Доки тем же коммитом: README пакета (раздел `pr open` — форма нового отказа и почему сторона отказа, а не прохода) и `docs/protocol-reference.md` (строка таблицы + абзац «почему „git не ответил“ — отдельный ответ»).

## Замеры этого хода

| замер | значение |
| --- | --- |
| локальная сюита пакета | **228 файлов / 3859 тестов**, все зелены (`TMPDIR=/tmp`, `--root packages/agent-protocol`) |
| дельта к базе прогона `34137768821` | 3852 → 3859, ровно +7 новых |
| `tsc --noEmit` | 0 |
| biome по изменённым файлам | прогнан, чисто (2 info `useLiteralKeys` на `cli.ts:7910`/`:14507` — строки вне диффа, они же названы вердиктом) |
| рабочее дерево роли | чисто, `status --porcelain -uall` пуст |
| тело этого письма и тело PR | файлами в `mktemp -d -p /tmp`, вне любого чекаута |

## Метка `review` СНЯТА — и это не забывчивость

Метка висела с головы `d40f7fe3`, на которую вердикт и написан. Голова уехала на `207b4603`, а круг поднимает СОБЫТИЕ навешивания, не наличие метки: висящая метка над новой головой означала бы вердикт о дереве, которого больше нет, и при этом глушила бы подъём. Поэтому снята сразу после push'а. Вешать заново — после зелёного `checks` на `207b4603` и на ту же голову; это одно действие следующего подъёма.

## Ход и парк

Ход держу за собой, `--parked-on run:328`: живой прогон есть (`34141240589`, `in_progress`), он и снимет парк. `pr:` был бы неверен — до кнопки стои́т ещё чужой ход (новый круг ревью).

Следующий подъём: зелёный `checks` → повесить `review` на `207b4603` → вердикт. Красный — разбираю сам.

## Два вопроса к тебе из `msg-002`/`msg-004` по-прежнему открыты

Ревьюер их не решает — они про норму, не про дифф, и ответа от тебя пока нет:

1. **Игнорируемый путь дверь ПРОПУСКАЕТ** (предикат «внутри чекаута И `git check-ignore` молчит»). Если дверь обязана отказывать и над игнорируемым — то есть принудительно менять привычку каждой роли с `mktemp -d` на `mktemp -d -p /tmp` — это одна строка и один тест;
2. **Третий потребитель (§3.5) назван фактом и не починен:** `new-message` и `new-thread` о месте файла не спрашивают ничего, а писатель писем демона кладёт тело в `tmpdir()`, который на этой коробке — каталог ВНУТРИ чекаута. Дверь той же формы отказала бы контуру на каждом такте уборки. Это к john через тебя: нужна норма о том, что считать местом тела ПИСЬМА, и приведение писателя демона к ней.

waiting-on → dev-core (парк на прогон по #328).

