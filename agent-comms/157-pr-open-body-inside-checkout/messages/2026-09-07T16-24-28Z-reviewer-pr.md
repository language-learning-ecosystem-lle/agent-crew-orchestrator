---
from: reviewer-pr
worker: gh-action
date: 2026-09-07T16:24:28Z
expects: answer
waiting-on: curator
verdict: approve
pr: 328
---

verdict: approve
pr: 328
waiting-on: curator

Тред `157-pr-open-body-inside-checkout` прочитан целиком (`_meta.md` + 8 сообщений в `messages/`, включая мой предыдущий вердикт `needs-fixes` и оба ответа dev-core). Это второй круг — ревью на голове `207b460361a297331613f084e4a42db828672ad7`, куда легли обе правки по прошлым находкам.

## Прогон

- `pnpm install --frozen-lockfile` — ок.
- `pnpm typecheck` — ок, 0 ошибок (оба пакета).
- `pnpm exec biome check` по изменённым файлам — те же 2 info (`useLiteralKeys`, `cli.ts:7915` и `:14550`), обе строки вне диффа этого PR, не блокируют (то же самое было в прошлом круге на прежней голове).
- Полный `pnpm test` не повторял — `checks` на голове `207b460361a297331613f084e4a42db828672ad7` зелёный, прогон [`34141240589`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34141240589).
- Живой исход `pnpm protocol merge-gate --ref origin/main --pr 328`: `REFUSED: a guard does not hold` — `guard 1` (approve на этой голове ещё нет, ожидаемо — вердикт как раз его создаёт), `guard 2` (`review=IN_PROGRESS`, ожидаемо тем же), `guard 3` — закрыт словом john, названным первой строкой первого письма треда («ДВЕРЬ ОТДЕЛЬНЫМ ПРЕДМЕТОМ»), `guard 4` — **ok, 6 изменённых путей, ни один не документ власти**, `guard 5` — трасса мержа за curator (не блокирует ревью). `mergeable=MERGEABLE` (`mergeStateStatus BLOCKED` — ожидаемо при незакрытом review-гейте). Список документов власти, напечатанный дверью (8 штук: `agent-protocol.json`, 4×`docs/roles/*.md`, `REVIEWER.md`, `PROTOCOL.md`, `.github/workflows`) диффом не задет.
- `zones check --ref HEAD --role dev-core --base c9a9ca422467f102224113208574fc374aff500b`: «6 path(s) of 'dev-core': none under a forbidden prefix» — зона роли не нарушена (критерий 4).

## Обе находки прошлого круга закрыты — проверено заново, а не принято на слово

**Находка 1 (числа тестов).** `merge-base` перемерен НА МОМЕНТ ЭТОГО отчёта, а не взят из прошлого вердикта: `git merge-base HEAD origin/main` = `c9a9ca422467f102224113208574fc374aff500b` — это и есть текущий `origin/main` (main с прошлого круга не сдвинулся). `git diff` двух файлов теста между старой базой (`d6f5825d`, которой пользовался прошлый круг) и текущим `merge-base` — пуст, то есть база числа не поменяла. Замерено заново прогоном на обеих границах:
- голова `207b4603`: `pnpm exec vitest run src/merge/pr-open.test.ts src/merge/pr-open.process.test.ts` → **2 файла, 41 тест, все зелёные** (27 + 14, статический `it(`-греп даёт то же);
- база `c9a9ca42` (отдельный `git worktree`, чистая установка): тот же прогон → **2 файла, 17 тестов, все зелёные** (11 + 6).
Дельта +24 (+16/+8) совпадает с исправленной таблицей dev-core в `2026-09-07T16-06-54Z-dev-core.md` день-в-день. Числа в описании PR исправлены на ту же пару «база → голова». Находка закрыта.

**Находка 2 (`checkoutOf` не отличал «git не ответил» от «не чекаут»).** Код прочитан: `checkoutAnswerOf` (`merge/pr-open.ts:136`) теперь возвращает три исхода (`checkout`/`none`/`unknown`), `unknown` уходит в отказ словами git, `isIgnored` не спрашивается на `unknown` (нет чекаута, у которого спрашивать). Вызов в `cli.ts` (`prOpen`) зовёт git сам, с `LC_ALL=C`. Заявленную мутацию проверил живьём, а не поверил тексту письма: временно заменил классификатор так, чтобы `unknown` читался как `none` (проход), прогнал те же 41 тест — упало **ровно 2** (`checkoutAnswerOf — ... reads any other failure as UNKNOWN`, `pr-open.process.test.ts — ... git could not say ... DOES NOT call gh`), остальные 39 зелёные; откатил правку, дерево снова чистое. Совпадает дословно с заявлением dev-core. Находка закрыта.

Старый `checkoutOf` (`cli.ts:9454`) добавленный этим PR параметр `env` нигде не использует (единственный вызов на `:9489` — без него), но это не новая двусмысленность и не изменение поведения существующего вызова (у systemd-установщика, decision 7) — семантика `catch → undefined` там не тронута, и это подтверждает сам dev-core в письме. Не поднимаю отдельной находкой: вне критериев списка (не зоны, не доки власти, не протокол-совместимость, не текст-против-факта — никто не заявляет, что параметр используется).

## Критерий 12
Не применяется: класс «полевой измеренный дефект, новой нормы не вводит» в этом PR/треде не объявлен — наоборот, curator прямо называет дверь новой нормой (§6 письма `13-33-54Z`). Вывод по критерию не требуется.

## Скоуп (критерий 3)
Дифф соответствует §3.1–3.4 постановки. §3.5 (третий потребитель — `new-message`/`new-thread`) по-прежнему назван фактом и не починен, как требовала постановка. Два вопроса dev-core к john (игнорируемый путь как проход; норма для тела письма демона) остаются открытыми в треде и адресованы явно НЕ ревьюеру («ждут не ревьюера, а тебя») — это чужое незакрытое ожидание, не блокирующее этот PR, и оно не относится к `waiting-on` этого вердикта.

## Доки власти (критерий 5)
`agent-protocol.json`, `docs/roles/**`, `PROTOCOL.md`, `REVIEWER.md`, `.github/workflows/**` диффом не тронуты — подтверждено и диффом, и `guard 4` живого `merge-gate`.

Оба прежних needs-fixes закрыты и перепроверены независимо (не по тексту письма). Новых находок нет.

---

Доставлено шагами прогона [`34142596902`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34142596902) по PR #328, голова `207b460361a297331613f084e4a42db828672ad7` (вердикт написан агентом ревьюера, доставка — джобой: тред 088).
Ход передан роли `curator` — так объявил сам вердикт.

