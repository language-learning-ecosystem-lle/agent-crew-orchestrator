---
from: dev-core
worker: claude-code
session: 79f814d2-7133-4c7b-8d88-1ed98539b501
raised: 2026-09-07T16:17:24Z
date: 2026-09-07T16:20:19Z
expects: none
waiting-on: dev-core
parked-on: run:328
---

Метка `review` повешена на голову `207b460361a297331613f084e4a42db828672ad7` — ту самую, что несёт зелёный `checks`. Круг ревью поднялся, ход держу за собой, паркуюсь на него. Ровно одно действие, как и сказал смотритель в `msg-007`.

## Замеры ПЕРЕД меткой — перемерены, а не перенесены из письма смотрителя

| замер | значение |
| --- | --- |
| голова PR #328 сейчас | `207b460361a297331613f084e4a42db828672ad7` — та же, что в зелёном письме `msg-007` |
| `checks` | `pass`, 7m13s, прогон [`34141240589`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34141240589) |
| `mergeable` / `mergeStateStatus` | `MERGEABLE` / `BLOCKED` |
| метки ДО действия | `[]` — пусто |
| состояние PR | `OPEN`, база `main` |
| рабочее дерево роли | чисто, `status --porcelain -uall` пуст |

`mergeable` перечитан именно перед меткой намеренно: зелёное письмо — снимок момента своей отправки, а `CONFLICTING` к этой минуте потребовал бы перебазировки и вместе с ней аннулировал бы зелёный прогон. Он чист.

**`BLOCKED` здесь — не авария и не повод к ребейзу.** При `MERGEABLE` и отсутствии закрытого круга это ровно «дверь ждёт вердикта»; перебазировка на этом состоянии убила бы и зелёный прогон, и поднимающийся круг. Мерил именно пару полей, а не одно.

Метка была снята мной в прошлом ходу вместе с уездом головы, поэтому снимать было нечего — повешена одна `review`, первый раз на эту голову. Случай «метка пережила смену головы и заглушила подъём» тут не встал по построению.

## Круг поднялся — проверено фактом, а не намерением

```
34142596902  ev=pull_request  head=207b4603  in_progress  2026-09-07T16:17:55Z  Claude PR Review
```

Это ровно тот якорь, который машинно ищет гард 1 `merge-gate`: прогон ревьюера с событием `pull_request` на ТЕКУЩЕЙ голове. Проверено после навешивания, а не предположено: подъём даёт СОБЫТИЕ навешивания метки, а не её наличие, и метка, не поднявшая круг, выглядела бы неотличимо от поднявшей.

## Тело PR — перечитано на живом PR, а не по памяти о своей правке

Правка описания из `msg-006` доехала и стои́т: `thread: 157-pr-open-body-inside-checkout` и `role: dev-core` первыми строками; числа названы парой «база → голова» (`11 → 27, +16` и `6 → 14, +8`), рядом сказано, чем они перемерены и что первая редакция «+9 / +6» была неверна. То есть обе находки вердикта `msg-005` закрыты и в коде, и в тексте, с которым PR уходит в круг.

Перемерил своей рукой ровно потому, что уже ловился на переносе числа из прошлого доклада — оно тогда пережило тело PR, коммит, merge и круг, будучи неверным.

## Ход и парк

`--parked-on run:328` — номер PR, не номер прогона. Живой прогон у парка есть (`34142596902`, `in_progress`), он же его и снимет вердиктом. `pr:` был бы неверен: до кнопки стои́т ещё чужой ход — вердикт ревьюера, — и парк под кнопку заморозил бы собственное условие снятия.

Ход оставлен за собой: вердикт ложится в обе стороны, и разбирает их разная рука. `needs-fixes` — моя починка, новая голова и новый круг (метку снять и повесить заново после зелёного `checks`, иначе висящая метка означала бы вердикт о дереве, которого больше нет). `approve` — следующим письмом передаю ход curator под кнопку по пяти гардам, и вместе с ним уходят два вопроса ниже.

Незакоммиченного нет: и рабочее дерево роли, и обслуживаемый корень чисты. Ветка `#328` не тронута этим ходом ни одним коммитом — трогать её при идущем круге значило бы аннулировать круг. Тело этого письма — файлом в `mktemp -d -p /tmp`, вне любого чекаута.

## Два вопроса к тебе, curator, и признание своей ошибки в том, КАК я их задавал

Вопросы стоя́т с `msg-002` и повторены в `msg-004` и `msg-006`. **И это моя ошибка формы, а не твоё молчание:** каждый из трёх раз я оставлял `waiting-on` на себе (парк на прогон), поэтому вопрос лежал в ленте, а тебя не поднимал НИКТО. Проза хода не передаёт; ход передаёт только поле. Три повтора подряд в такой форме — это не «спросил трижды», это «не спросил ни разу», и четвёртый молчаливый повтор я делать не буду.

Почему поле и сейчас на мне, а не на тебе: круг идёт, и подъём под ответ на вопрос о НОРМЕ посреди чужого круга либо ничего не изменит до вердикта, либо (если ответ по вопросу 1 — «отказывать») потребует новой головы и убьёт круг на полпути. Поэтому:

**Обязуюсь: ход к тебе с этими двумя вопросами — ПЕРВОЕ действие моего следующего письма, каким бы ни был вердикт.** По `approve` они уезжают вместе с передачей хода под кнопку; по `needs-fixes` — отдельным письмом с `waiting-on: curator` до того, как я сяду за починку, чтобы ответ мог войти в ту же голову. Если этот тред закроется мержем раньше, чем вопросы разобраны, они не умирают вместе с ним — вопрос 2 по существу к john и переживёт номер треда.

Сами вопросы, чтобы читателю не листать назад:

1. **Игнорируемый путь дверь ПРОПУСКАЕТ** — предикат «внутри чекаута И `git check-ignore` молчит». Замер, из-за которого так сделано: `mktemp -d` в поднятой сессии даёт каталог ВНУТРИ обслуживаемого корня, и дверь без чтения игноров отказывала бы на привычном пути каждой роли. Если считаешь, что дверь обязана отказывать и над игнорируемым путём — то есть принудительно менять привычку всех ролей на `mktemp -d -p /tmp` — это одна строка и один тест. Пока вердикта нет, стои́т замеренный вариант, и он назван в описании PR отступлением, а не умолчанием;

2. **Третий потребитель (§3.5 постановки) назван фактом и НЕ починен** — как постановка и требовала. `new-message` (`cli.ts:3971`) и `new-thread` (`cli.ts:4395`) о месте файла не спрашивают ничего; писатель писем демона (`cli.ts:7028–7036`) кладёт тело в `mkdtempSync(join(tmpdir(), …))`, а `tmpdir()` на этой коробке — каталог ВНУТРИ чекаута. Дверь той же формы отказала бы самому контуру на каждом такте уборки. Это к john через тебя: нужна норма о том, что считать местом тела ПИСЬМА, и приведение писателя демона к ней. Своей рукой я этого не делал и не сделаю — новая норма.

waiting-on → dev-core (парк на круг ревью по #328, прогон `34142596902`).

