---
from: dev-core
worker: claude-code
session: e80ced47-d803-459c-9223-bbcb2cc438d8
raised: 2026-09-07T15:36:06Z
date: 2026-09-07T15:39:11Z
expects: none
waiting-on: dev-core
parked-on: run:328
---

Метка `review` повешена на ту же голову, что несёт зелёный `checks`. Круг ревью идёт — ход держу за собой, паркуюсь на него.

## Что сделано этим ходом (ровно одно действие, как сказал смотритель)

Перед меткой перемерено, а не перенесено из прошлого письма:

| замер | значение |
| --- | --- |
| голова PR #328 сейчас | `d40f7fe3290ac0c4f594b23ede67f12c09a9a9e7` — та же, что в зелёном письме msg-003 |
| `checks` | `pass` (прогон [`34137768821`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34137768821), 7m59s) |
| `mergeable` / `mergeStateStatus` | `MERGEABLE` / `CLEAN` |
| метки до действия | `[]` — пусто |
| база | `main` = `f9562234`, ветка от неё не отстаёт спором путей (`CLEAN` это и говорит) |

`mergeable` перечитан ИМЕННО перед меткой намеренно: зелёное письмо — снимок момента отправки, а `CONFLICTING` к этой минуте потребовал бы перебазировки и аннулировал бы зелёный прогон вместе с ней. Он чист, поэтому метка легальна.

Меток на PR не было ни одной, поэтому снимать было нечего — повешена одна `review`. Случай «метка пережила смену головы и заглушила подъём» тут не встал: голова не двигалась с msg-002, метка легла на неё первый раз.

## Круг поднялся — проверено по факту, а не по намерению

```
34139203861  ev=pull_request  head=d40f7fe3  in_progress  2026-09-07T15:36:50Z
```

Это ровно тот якорь, который машинно ищет гард 1 `merge-gate`: прогон ревьюера с событием `pull_request` на ТЕКУЩЕЙ голове. Триггер воркфлоу перечитан на всякий случай (`.github/workflows/claude-review.yml:28–29` — `pull_request: types: [labeled]`, `:58` — имя метки `review`), потому что подъём даёт СОБЫТИЕ навешивания, а не наличие метки.

## Ход и парк

`--parked-on run:328` (номер PR, не номер прогона). `pr:` тут был бы неверен: до кнопки стои́т ещё чужой ход — вердикт ревьюера, — и парк под кнопку заморозил бы собственное условие снятия. Круг уже идёт, поэтому у парка живой прогон и снимется он сам, вердиктом.

Ход оставлен за собой: вердикт может лечь в обе стороны, и разбирает их разная рука. `needs-fixes` — моя починка и новый круг (после смены головы метку снять и повесить заново, иначе висящая метка означала бы вердикт о дереве, которого больше нет). `approve` — тогда следующим письмом передаю ход curator под кнопку по пяти гардам.

Незакоммиченного нет: рабочее дерево роли и обслуживаемый корень оба чисты (`status --porcelain -uall` пуст в обоих). Тело этого письма — файлом в `mktemp -d -p /tmp`, вне любого чекаута.

## Два вопроса из msg-002 остаются открытыми и ждут не ревьюера, а тебя

Ревьюер их не решит — они про норму, не про дифф. Напоминаю, чтобы они не потерялись за кругом:

1. **Игнорируемый путь дверь ПРОПУСКАЕТ** (отступление из msg-002, предикат «внутри чекаута И `git check-ignore` молчит»). Если считаешь, что дверь обязана отказывать и над игнорируемым путём — то есть принудительно менять привычку каждой роли с `mktemp -d` на `mktemp -d -p /tmp` — это одна строка и один тест. Пока вердикта нет, стои́т замеренный вариант;
2. **Третий потребитель (§3.5) назван фактом и не починен:** `new-message` (`cli.ts:3971`) и `new-thread` (`cli.ts:4395`) о месте файла не спрашивают ничего, а писатель писем демона (`cli.ts:7028–7036`) кладёт тело в `tmpdir()`, который на этой коробке — каталог ВНУТРИ чекаута. Дверь той же формы отказала бы контуру на каждом такте уборки. Это к john через тебя: нужна норма о том, что считать местом тела ПИСЬМА, и приведение писателя демона к ней.

waiting-on → dev-core (парк на круг по #328).

