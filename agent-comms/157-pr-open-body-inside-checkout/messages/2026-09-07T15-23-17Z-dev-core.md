---
from: dev-core
worker: claude-code
session: 2f80f72c-505f-44f5-9805-076663c4a7a0
raised: 2026-09-07T15:11:15Z
date: 2026-09-07T15:23:17Z
expects: ack
waiting-on: dev-core
parked-on: run:328
---

Дверь поставлена, PR открыт: **#328**, голова `d40f7fe3290ac0c4f594b23ede67f12c09a9a9e7`, ветка `dev-core/157-pr-open-body-outside-checkout`. `checks` идёт, метки `review` пока нет — вешаю после зелёного на этой же голове.

## Что стои́т (§3.1–3.3)

`pr open` отказывает, когда `--body-file` лежит внутри git-чекаута:

```
agent-protocol: pr open — the body file '<путь>' lies inside the git checkout '<топлевел>' —
write it OUTSIDE any checkout, in a directory of its own: `mktemp -d -p /tmp` … Nothing was created
```

Интонация и половина фразы — от `thread/deliver.ts:211`, чтобы класс читался как один и тот же. Отказ исполним: путь, чекаут, куда класть вместо, и «ничего не создано». `gh` не зван, exit 2.

Судейство пустое — новая функция `judgeBodyLocation` в `merge/pr-open.ts`, читатели git инжектированы; вызов в `cli.ts` (`prOpen`), git зовётся средой без переменных хука (`gitEnvOutsideHook`), иначе `--show-toplevel` в хуке отвечает про cwd и дверь отказывала бы на любом пути.

## Одно отступление от постановки — доложено, потому что оно меняет предикат

**Игнорируемый путь дверь пропускает.** Постановка §3.2 оставила определение «внутри чекаута» за исполнителем при условии замера — вот замер, сделанный в этой сессии, `2026-09-07`:

```
$ echo $TMPDIR                 → /tmp/aco-5c148899a7a0
$ d=$(mktemp -d); echo $d      → /tmp/aco-5c148899a7a0/tmp.m2e9LzPvwt
$ readlink -f "$d"             → /home/lle/projects/agent-crew-orchestrator/.orchestrator/sessions/<run>.tmp/tmp.m2e9LzPvwt
$ git -C "$d" rev-parse --show-toplevel → /home/lle/projects/agent-crew-orchestrator
$ git -C "$(mktemp -d -p /tmp)" rev-parse --show-toplevel → fatal: not a git repository
```

То есть **привычный роли `mktemp -d` кладёт файл внутрь обслуживаемого чекаута**. Дверь на одном `--show-toplevel` отказывала бы каждой роли на нормальном пути — и живая приёмка §4 («следующий PR контура открывается штатно, дверь молчит») стала бы красной прямо в постановке.

Но авария не в «файл в дереве», а в «файл, поверх которого `git pull --ff-only` отказывается писать», — а над игнорируемым он не отказывается. `.orchestrator/` объявлен машинным состоянием контура твоим же §2 (решение john в `153`). Поэтому предикат: **внутри чекаута И `git check-ignore` молчит**. Это НЕ починка игнором, которую john отверг: дверь читает игноры, которые уже есть, и ни одной записи не заводит. Рабочее дерево роли показывает разницу: `.worktrees/` игнорирует обслуживаемый корень, а внутри самого дерева (оно и есть внутренний чекаут, и его спрашивают) не игнорирует никто — тело там отказано.

Если считаешь, что дверь обязана отказывать и над игнорируемым путём (то есть менять привычку каждой роли на `-p /tmp` принудительно) — это одна строка и один тест, скажи.

## Края, названные фактом (§3.2)

| край | исход |
| --- | --- |
| файла нет | дверь не достигается: тело читается раньше, путь отказан как нечитаемый (тест) |
| каталог вне репозитория | проход |
| вложенный чекаут | отказ по ВНУТРЕННЕМУ (тест против настоящего git, топлевел не пересказан) |
| симлинк снаружи внутрь | отказ: `git -C` делает каталог своим cwd, cwd git разрешает сам — руками не резолвим |
| `--body-file -` | не форма этой команды: тело читается файлом по имени; `-` искался бы как файл `-` |
| игнорируемый путь | проход (выше) |
| git не ответил / ответил пустым | «не чекаут» → проход; ошибка `check-ignore` → «не игнорируется» → сторона отказа |

## Проверяемость (§4) — и доказательство мутацией

- **юниты** `merge/pr-open.test.ts` (+9): отказ называет путь и чекаут; называет `mktemp -d -p /tmp` и «Nothing was created»; проход вне репозитория; пустой ответ git; вложенный; спрашивается КАТАЛОГ файла (файл существовать не обязан); относительный путь резолвится; игнор спрашивается ТОЛЬКО внутри чекаута и ТОЛЬКО о самом файле;
- **процессные** `merge/pr-open.process.test.ts` (+6): тело внутри чекаута → exit 2 и `gh` НЕ зван; то же без `--write`; порядок дверей; вложенный чекаут; симлинк; нечитаемый путь; игнорируемый каталог → PR создаётся;
- **порядок объявлен намеренно:** место спрашивается ДО содержимого — файл придётся перенести, что бы в нём ни стояло, и судейство заголовка первым потратило бы следующий шаг вызывающего на не ту починку;
- **мутация (обе):** дверь выключена → падают ровно 5 новых процессных; двери переставлены местами → падает ровно тест порядка (`puts WHERE before WHAT`), остальные 11 зелены. Мутации сняты руками, не `git checkout --`;
- **фикстуры сюиты сами были формой ошибки:** `withBody` писал тело В репозиторий фикстуры. Теперь пишет в нейтральный temp сюиты (`testing/tmp-base.ts`), поэтому КАЖДЫЙ зелёный случай файла гоняет новую дверь на проходе;
- **живая приёмка:** #328 открыт `pnpm protocol pr open` с телом в `mktemp -d -p /tmp` — дверь промолчала, PR создан;
- **локальная сюита пакета зелена целиком:** `228 файлов / 3852 теста`, `tsc --noEmit` 0, biome прогнан.

## §3.5 — третий потребитель. Назван фактом, НЕ починен

`new-message` (`cli.ts:3971`) и `new-thread` (`cli.ts:4395`) читают `--body-file` тем же `readFile` и о месте файла не спрашивают ничего. Единственная защита — `deliver.ts:211`: он меряет `git status --porcelain` чекаута ПОЧТЫ. Значит тело, оставленное в рабочем дереве роли или в обслуживаемом корне, проходит обе команды молча.

**Той же дверью это не закрывается, и это замер, а не осторожность:** писатель писем демона (`cli.ts:7028–7036`, его же комментарий обещает «a file OUTSIDE both checkouts») кладёт тело в `mkdtempSync(join(tmpdir(), …))`, а `tmpdir()` на этой коробке — каталог внутри чекаута (замер выше). Дверь на `new-message` отказала бы самому контуру на каждом такте уборки. Игнор спасает не везде: почта пишется и из рабочего дерева роли, где `.orchestrator/` внутреннего чекаута может не игнорироваться.

Вывод (это вывод, не факт): чтобы закрыть почту, нужна норма о том, что считать местом тела ПИСЬМА, и приведение писателя демона к ней. Это к john через тебя — я не делал.

## Честное ограничение (§3.4)

Дверь ловит честную ошибку, обходится набором `gh pr create` рукой и не видит файла, положенного в дерево ПОСЛЕ её ответа. Гарантией не объявляется — ни в коде, ни в README, ни в справочнике.

## Границы (§5)

`agent-protocol.json`, `docs/roles/**`, `PROTOCOL.md`, `REVIEWER.md`, `.github/workflows/**` не тронуты. Сужение проверки самопочинки (`153`) сюда не положено. `.gitignore` обслуживаемого репозитория не правится. Тело #328 и это письмо — файлами в `mktemp -d -p /tmp`; рабочее дерево чистое.

Доки тем же коммитом: README пакета, раздел `pr open` (форма отказа, порядок, `-p /tmp`, игнор, края) и `docs/protocol-reference.md` (таблица двери, замер, факт о третьем потребителе).

Ход держу за собой и паркуюсь на прогон #328: зелёный `checks` → снимаю/вешаю `review` на этой голове и отчитываюсь. Красный — разбираю.

