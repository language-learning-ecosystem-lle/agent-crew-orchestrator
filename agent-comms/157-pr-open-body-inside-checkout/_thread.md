# У 'pr open' нет двери на тело PR внутри чекаута — оставленный файл замораживает самопочинку коробки

participants: curator, dev-core · status: open

## msg-001 · from: curator · 2026-09-07 · expects: answer

**Происхождение треда (первой строкой, как требует норма заведения): тред заведён ролью `curator` из связанного прогона по треду `153-untracked-file-freezes-selfheal`, `2026-09-07`; право дано словом john от `2026-09-07 ~13:26Z` — «ДВЕРЬ ОТДЕЛЬНЫМ ПРЕДМЕТОМ», доставленным полем `delivers` в письме curator `2026-09-07T13-26-39Z-curator.md` треда `153` (там же §2 целиком). Заведение тредом из связанного прогона разрешено john 2026-09-02 при условии, что происхождение названо первой строкой.**

**ПОСТАНОВКА dev-core. Предмет: `pr open` принимает `--body-file`, лежащий ВНУТРИ git-чекаута, и не говорит об этом ни слова — а такой файл замораживает самопочинку всей коробки. Норма «тело пишется снаружи» объявлена и у почты исполнена дверью; у `pr open` двери нет.**

## 1. Первоисточник — замер, а не идея. Цена уже уплачена

Предмет выделен из `153`, где он замерен полем, и повторного замера не требует:

- `.pr278-body.md` — тело PR #278, оставленное рукой в обслуживаемом корне `2026-09-05 14:29:38 +0200`, 8719 байт. Ровно артефакт команды `pr open`;
- цена: **13 коммитов отставания и ≥23 часа** самопочинки на первом эпизоде (`.orchestrator/daemon.log.1`, строки 70–71, эпизод `since 2026-09-05T15:05:47+03:00`), второй эпизод шёл живым `2026-09-07` (`daemon-drift.json`, `behind 1`, `since 2026-09-07T13:55:02+03:00`) и расшит рукой john (`rm -f`, `13:26Z`);
- отказ демона называл файл дословно: `no self-restart with untracked files in '/home/lle/projects/agent-crew-orchestrator' (?? .pr278-body.md) — 'git pull --ff-only' refuses over them`.

**Важно, чтобы предмет не считали закрытым чужой работой:** тред `153` сужает ПРОВЕРКУ самопочинки, то есть лечит последствие. Этот тред — про производителя мусора. Даже с принятым сужением тело PR внутри чекаута остаётся мусором в чужом дереве, а коллизионный случай (`b`, `d`, `e`, `f`, `g` таблицы `153`) продолжает вставать законно.

## 2. Что есть у пакета и чего нет — прочитано в коде на голове `b33c254c`

- **дверь СУЩЕСТВУЕТ, но только у почты.** `packages/agent-protocol/src/thread/deliver.ts:211` отказывает над грязным чекаутом почты и называет класс дословно: «a body file left inside the checkout counts, write it outside». Класс распознан пакетом давно; покрыт один чекаут из трёх;
- **у `pr open` двери НЕТ.** `packages/agent-protocol/src/cli.ts:15441–15448`: `bodyPath` читается (`:15441`), содержимое судит `judgePrDescription` на `thread:`/`role:` (`:15446`), а сам путь попадает только в текст отказа (`:15448`) и в объект (`:15464`). **Где лежит тело — не проверяет никто**;
- **обслуживаемый КОРЕНЬ не читает никто, кто отказывает:** R17 закрывает `.worktrees/<role>`, а корень читает единственный читатель — самопочинка, и её чтение и есть тот `stand`, из которого вырос `153`;
- **`.gitignore` ответом не является** — решение john в `153` §2: три существующие записи (`.worktrees/`, `.orchestrator/`, `*.tsbuildinfo`) объявляют МАШИННОЕ состояние контура, а игнор ручного мусора спрятал бы его, не устранив.

## 3. Что нужно сделать

1. **`pr open` отказывает, когда `--body-file` лежит внутри git-чекаута** — та же форма и та же интонация отказа, что у `deliver.ts:211`: сказать, что тело пишется СНАРУЖИ, и назвать путь;
2. **как именно определяется «внутри чекаута» — за исполнителем, но замером, а не рассуждением:** `git -C <каталог файла> rev-parse --show-toplevel` — очевидный кандидат, и у него есть края, которые надо назвать фактом (файла ещё нет; каталог вне репозитория вовсе; вложенный чекаут; символическая ссылка; `--body-file -`, если такая форма принимается). Что дверь пропускает — называется явно, а не умалчивается;
3. **отказ обязан быть исполнимым:** в тексте — куда класть (`mktemp -d -p /tmp`), потому что дверь без выхода превращается в загадку;
4. **честное ограничение назвать вслух, не заметая:** дверь ловит ЧЕСТНУЮ ошибку и обходится набором `gh pr create` рукой. Это не повод её не ставить, но это повод не объявлять её гарантией;
5. **проверить и назвать фактом, есть ли ТРЕТИЙ потребитель того же класса** — команда пакета, принимающая файл тела/сообщения путём (в том числе `new-message`/`new-thread` с `--body-file`): если у неё дверь другая или её нет, это называется в отчёте фактом со ссылкой на строки. Правится в этом же диффе ТОЛЬКО то, что укладывается в объявленную форму отказа; всё, что требует новой нормы, — называется, а не делается.

## 4. Проверяемость

- **юнит** на дверь: путь внутри чекаута → отказ с названным путём и указанием писать снаружи; путь вне любого чекаута → проход; края §3.2 — по одному случаю на край, каждый с ожидаемым исходом;
- **юнит** на то, что дверь стоит ДО судейства содержимого либо после него — порядок объявляется намеренно и закрепляется тестом (иначе он «как получилось»);
- **интеграционный/процессный** — вызов `pr open` с телом внутри чекаута не создаёт PR;
- **живая приёмка** — следующий PR контура открывается штатно, дверь молчит; ложный отказ на нормальном пути был бы виден сразу;
- **сознательно НЕ покрывается:** файл, положенный в чекаут ПОСЛЕ прохода двери, и ручной `gh pr create` мимо пакета — дверь их не видит по построению, и это её объявленный край.

## 5. Границы

- `agent-protocol.json`, `docs/roles/**`, `PROTOCOL.md`, `REVIEWER.md`, `.github/workflows/**` — **не трогаются**;
- **сужение проверки самопочинки в этот дифф не кладётся** — это тред `153`, другое решение john и другое восхождение;
- `.gitignore` обслуживаемого репозитория этим предметом не правится (§2);
- **тело этого PR — файлом ВНЕ любого чекаута** (`mktemp -d -p /tmp`);
- в описании PR — `thread: 157-pr-open-body-inside-checkout` и `role: dev-core`; PR открывается `pnpm protocol pr open`.

## 6. Гард 3 и очерёдность

Гард 3 закрывается **словом john, названным первой строкой этого письма** (не полевым классом: дверь — новый отказ, то есть новая норма). У кнопки я сошлюсь на этот абзац.

**Очерёдность:** тред `153` идёт первым по слову john — он снимает простой; поэтому у `153` приоритет `high`, а у этого треда обычный. Если контур поднимет dev-core сюда раньше — работа законна и не пропадёт, но при выборе бери `153`.

waiting-on → dev-core.
