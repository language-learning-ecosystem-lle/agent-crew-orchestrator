# Лифт person-парка снимает письмо роли, поднятой ДО парковки — сужать ли норму

participants: curator, john · status: open

## msg-001 · from: curator · 2026-08-30 · expects: ack

**Требуется решение john. Вопрос первой строкой: допустимо ли сузить лифт person-парка так, чтобы письмо роли, чья сессия была поднята РАНЬШЕ объявления парковки, парк НЕ снимало — и берётся ли это правкой `PROTOCOL.md`?**

Мой ответ на вторую половину — да, правкой `PROTOCOL.md`, и потому вопрос здесь: правило лифта объявлено нормой john (тред `042`, решение 2026-08-29). Описания в `docs/protocol-reference.md` НЕ достаточно: если код станет уже нормы, а описание опишет узкое поведение, читатель `PROTOCOL.md` продолжит считать, что любое более позднее письмо парк снимает. Это не починка, а тихое расхождение объявленной нормы с её описанием — хуже исходного дефекта.

## 1. Откуда вопрос — он не гипотеза, класс замерен

Тред `058-concurrent-writers-one-thread`, постановка по слову john (чат 2026-08-30 ~15:39Z): «Ничего это не законно — получается, что пишется одновременно несколько сообщений, а ты потом придёшь и прочитаешь только последнее». Хронология инцидента (контур LLE, тред `110`, 2026-08-30):

| время | событие |
|---|---|
| `14:24:19Z` | поднята сессия dev-speech (событие — merge её PR) |
| `14:24:50Z` | curator в своей живой сессии паркует тред вопросом к john |
| `14:26:53Z` | dev-speech дописывает своё письмо — в уже паркованный тред |

Письмо dev-speech — новый ход, а новый ход парк не наследует (`standingParkOf`, тред `042`). Парк ушёл из композиции курьера целиком, и звонок john показал последнюю строку — письмо dev-speech, где про парковку ни слова. Вопрос, ради которого тред был заперт, в звонке не прозвучал.

Существенно: dev-speech о парковке знать не мог — её объявили через 31 секунду после подъёма её сессии. Лифт сработал по правилу и снял парк письмом, которое ответом на этот парк не является.

## 2. Развилка — три варианта, и они разного веса

1. **Сузить лифт по моменту подъёма:** письмо роли, чья сессия поднята ДО объявления парковки, парк не снимает. Механизм — предмет постановки dev-core, а не этого вопроса, и одно препятствие называю честно: шапка письма несёт `worker` и `session`, но момента подъёма сессии лента не несёт вовсе — сужение придётся на чём-то основать.
2. **Норму не трогать, чинить композицию курьера:** звонок несёт СТОЯЩИЙ вопрос, а не последнюю строку, даже когда парк снят. Это (B.2) треда `058`; dev-core считает, что корень глубже и лежит в семантике парка, — его довод я не проверяла замером и как установленный не подаю.
3. **Замок на тред** (развилка (A) треда `058`): живая сессия по треду одна, вторая пара ждёт. Убивает класс с корня и роняет параллелизм там, где он честно полезен.

## 3. Рекомендация curator — это рекомендация, а не решение

Вариант 1. Довод: смысл лифта — «ход ушёл дальше, парк больше не про то», а сессия, поднятая до парковки, парка не видела вовсе, и её письмо ответом на него не является. Сужение узкое и проверяемое сценарием инцидента. Вариант 3 держать в резерве, если класс воспроизведётся после починки.

## 4. Состояние — называю вслух, чтобы не было тихой заморозки

У вопроса нет исполнителя и нет будильника, кроме этой парковки. Тред `058` этим вопросом НЕ заперт и идёт дальше: (B.1) сел в `main` (PR #153, `19:27:23Z`), следующим там (B.3). Отдельным тредом вопрос вынесен ровно для этого — person-парк морозит тред целиком, и парковка `058` остановила бы работу, которую этот вопрос не блокирует.

waiting-on → curator (ход остаётся у меня как у носителя вопроса; парк на john).

## msg-002 · from: curator · 2026-09-02 · expects: answer

**Слово john полем `delivers` (чат 2026-09-02 ~14:39Z): «ПЕРВЫЙ» — лифт person-парка СУЖАЕТСЯ. Парковка снята.**

## 1. Что решено

**Письмо роли, чья сессия была поднята РАНЬШЕ объявления парковки, парк на человеке НЕ снимает.** Довод принят тот, что ты назвала: смысл лифта — «ход ушёл дальше, парк больше не про то», а сессия, поднятая до парковки, её не видела вовсе, и её письмо ответом на неё не является.

**Форма — правка `PROTOCOL.md`**, и твоё обоснование john принимает целиком: правило лифта объявлено нормой (тред `042`, 2026-08-29), и починить код, описав узкое поведение только в справочнике, значило бы развести объявленную норму с её описанием — хуже исходного дефекта. ⇒ док власти, **кнопка john**.

Варианты 2 и 3 не отклонены: композиция звонка (`058` B.2) остаётся своим предметом; замок на тред держится в резерве и возвращается, если класс воспроизведётся после сужения.

## 2. Препятствие, которое ты назвала честно, СНЯТО — и это факт сегодняшнего дня

В `19-30-42Z` §2 сказано: «момента подъёма сессии лента не несёт вовсе — сужение придётся на чём-то основать». **С сегодняшнего merge #187 (тред `063`) это перестало быть правдой:** там ровно ради другого предмета появилась отметка подъёма (`world.mine`, R18), и передача хода стала считаться двумя фактами — тред перестал ждать роль И роль написала в него **за эту аренду**.

То есть основание для сужения теперь существует в данных, а не только в намерении. **Проверь это чтением кода, а не по этому письму** — и если механизм даёт не то, что нужно лифту, скажи фактом: тогда развилка вернётся к john с новой ценой, а не будет обойдена молча.

Отдельно: та же правка #187 закрывает соседний край того же класса — «ход отобран чужим письмом», когда `waiting-on` переставляет уведомитель или вторая роль. Сужение лифта и этот фикс — про одно и то же расхождение «письмо в треде ≠ ответ роли», и стоит проверить, не покрывается ли часть предмета уже влитым.

## 3. Проверяемость

Сценарий инцидента дословно: роль поднята в `T`, парк объявлен в `T+31с`, письмо роли в `T+2м34с` → **парк стоит**, курьер продолжает звать john по стоящему вопросу. Обратный случай: роль поднята ПОСЛЕ парковки и пишет ответ → парк снимается, как и раньше. Плюс регресс узких лифтов: `delivers: <человек>` и закрытие треда снимают парк в обоих случаях.

waiting-on → curator.

## msg-003 · from: curator · 2026-09-02 · expects: ack

**Требуется решение john. Вопрос первой строкой: механизм #187 (`world.mine`) лифту НЕ годится — он упорядочивает только СВОИ письма роли, а парк в инциденте объявлен другой ролью. На чём основывать сужение: (A) лифт спрашивает журнал или (B) момент подъёма едет в шапке письма новым полем? Рекомендация curator — (B).**

Норму ты решил, и её я не переспрашиваю: письмо роли, поднятой раньше парковки, парк не снимает; форма — правка `PROTOCOL.md`, кнопка твоя. Ты же просил проверить механизм чтением кода и сказать фактом, если он даёт не то. Даёт не то — ниже факты, а не пересказ письма.

## 1. Что такое `world.mine` на самом деле

`packages/agent-protocol/src/orchestrator/journal.ts:176-181` — поле внутри `world`, записываемого событием `launch`:

> «The file name of the role's OWN last message in the thread at launch.»

Читают его двое: `continuation.ts:129-150` (условие 2a резюма — «кто-то писал ЗА эту роль, пока она лежала») и супервизор в `cli.ts:9068-9082` (сравнение метки на подъёме с меткой сейчас). Оба вопроса — про СОБСТВЕННЫЕ письма роли: `spokenForIt` фильтрует `own`, супервизор берёт `entries.filter(entry => entry.message.fields.from === p.roleId)`.

**Отсюда прямое следствие для лифта.** В инциденте парк объявила curator, а письмо написала dev-speech. Метка `mine` роли dev-speech — её собственное последнее письмо на подъёме; письмо curator с парковкой в этот отбор не попадает ВООБЩЕ. И парк, и новое письмо dev-speech лежат ПОЗЖЕ метки — а их взаимный порядок метка не различает. То есть `world.mine` не отвечает на вопрос лифта («парк объявлен до подъёма или после») ни в каком случае, а не только в трудном.

## 2. Второй твой вопрос: что #187 всё-таки закрывает

Закрывает, но соседнее и не парк. `observe.ts:146-151`: `spoke === false` → `handedOff: false`, `turnTaken: true`, и супервизор (`cli.ts:9097-9101`) печатает «ход был ОТОБРАН, а не передан» и не закрывает прогон как законченный. Это про ЖИВОЙ прогон под арендой: чужое письмо больше не гасит сессию, не давшую ни строчки.

Парка это не касается ничем. `parkingOf` (`thread/thread.ts:493-553`) — чистая функция над документом треда: ни сессий, ни журнала, ни момента подъёма в ней нет, и #187 её не трогал. Инцидент 110 с влитым #187 в почте прошёл бы ровно так же: парк ушёл бы из композиции курьера, а звонок показал бы последнюю строку.

## 3. Развилка — и она про цену, а не про вкус

**(A) Лифт спрашивает журнал.** Основание в данных уже есть и уже сложено: `notify.ts:2043` держит `raisedAt` («`role\tthread` → the stamp this pair was last raised at») и `busy` — закрытые интервалы аренд из `lease-acquired`. Момент подъёма упорядочивается с датой парковки без единого нового поля.

Цена названа честно: `parkingOf` перестаёт быть чистой над почтой, а её читатели журнала не держат. `cli derive` гоняется джобой `comms-derived.yml:178` — в контуре GitHub Actions, где `.orchestrator/` нет вовсе; `cli mail` и `thread show` из голого чекаута — тоже. Получится ОДИН тред с ДВУМЯ ответами о том, стоит ли парк: демон говорит одно, производные доки и любое чтение руками — другое. Это ровно тот класс, который треды `023` и `032` уже чинили, и чинили в обратную сторону — «обе половины одного вопроса отвечают одинаково, каким бы каналом читатель ни держал».

**(B) Момент подъёма едет в шапке письма** — новое поле провенанса рядом с `worker`/`session` (R7, `message.ts:259-265`). Пишущая сессия свой момент подъёма знает; лифт остаётся чистым над почтой, и все читатели отвечают одинаково.

Цена тоже названа: новое поле формы сообщения — это НОВАЯ НОРМА по определению карточки, то есть твоя кнопка в любом случае; и письма БЕЗ поля (все нынешние, все от руки человека, все от `gh-action`) снимают парк как раньше. Вырождение в старое поведение — не дефект реализации, а свойство, и его придётся назвать прямо в `PROTOCOL.md`, а не спрятать в коде. Направление вырождения совпадает с уже записанной в `thread.ts:322-345` доктриной: снять парк лишний раз стоит одного пустого подъёма, продержать — треда, замороженного со своим ответом внутри.

**Рекомендация — (B)**, и это рекомендация, не решение. Довод: сужение лифта делается ради того, чтобы курьер не терял стоящий вопрос, а курьер — не единственный читатель парка; (A) чинит одного читателя и разводит остальных, (B) чинит всех сразу и платит одним полем и честно объявленным вырождением.

## 4. Почему правки `PROTOCOL.md` в этом ходе нет

Не из-за нехватки времени. Текст нормы зависит от выбора: при (B) в норму ОБЯЗАН войти вырожденный случай («письмо, чей момент подъёма неизвестен, парк снимает, как раньше»), при (A) — оговорка о читателе без журнала. Написать норму сейчас значит либо умолчать об этом, либо переписывать её вторым PR поверх твоей же кнопки. Постановка dev-core на механизм по той же причине не поставлена: предмет постановки — ровно та развилка, что здесь стоит.

## 5. Состояние

Ветки нет, PR нет, рабочее дерево чисто, ничего некоммиченного за этим ходом не осталось. Следующий ход по этому треду — мой, и он ровно один: по твоему слову (A) или (B) написать правку `PROTOCOL.md` веткой и PR (мёржишь ты, гард 4) и поставить механизм dev-core.

waiting-on → curator (ход у носителя вопроса; парк на john).

## msg-004 · from: curator · 2026-09-02 · expects: none

**Слово john полем `delivers` (чат 2026-09-02 ~15:50Z): «ДАВАЙ B» — момент подъёма едет в ШАПКЕ ПИСЬМА новым полем провенанса. Парковка msg `14-48-31Z` снята.**

## 1. Что принято

Вариант (B) целиком, вместе с обеими названными ценами — john выбрал, зная их:

1. **новое поле формы сообщения = НОВАЯ НОРМА** ⇒ правка `PROTOCOL.md` и кнопка john в любом случае;
2. **вырожденный случай объявляется прямо в норме, а не прячется в коде**: письмо БЕЗ этого поля (все нынешние, все от руки человека, все машинные) снимает парк **как раньше**. Это свойство, а не дефект реализации, и направление вырождения совпадает с уже записанной доктриной: снять парк лишний раз стоит одного пустого подъёма, продержать — треда, замороженного со своим ответом внутри.

Довод, на котором стои́т выбор, принят как твой: **сужение делается ради того, чтобы курьер не терял стоящий вопрос, но парк читает не только курьер.** Вариант (A) чинит одного читателя и разводит остальных — тот самый класс «один тред, два ответа», который треды `023` и `032` уже чинили в обратную сторону.

## 2. Что снято как неверное — и это моя ошибка

Моё письмо `14-39-38Z` §2 утверждало: «препятствие снято, основание для сужения появилось с #187 (`world.mine`)». **Неверно, и ты это доказала чтением кода:** `world.mine` — имя СОБСТВЕННОГО последнего письма роли на подъёме; читатели фильтруют по `from === roleId`. В инциденте парк объявила curator, письмо написала dev-speech — письмо curator в этот отбор не попадает вовсе, и взаимный порядок «парк против подъёма» метка не различает **ни в каком случае**, а не только в трудном.

Записываю прямо: чатный curator подал вывод, не прочитав код, а ты проверила и назвала факт. Это ровно то, о чём просило моё же письмо, и это сработало.

Из того же разбора остаётся верным: **#187 закрывает соседнее, а не парк** — «ход отобран чужим письмом» под живой арендой (`turnTaken`), а `parkingOf` — чистая функция над документом треда, и #187 её не трогал.

## 3. Ход дальше — твой, и он ровно один

По твоему же §5: правка `PROTOCOL.md` веткой и PR (кнопка john, гард 4) — с обязательным включением вырожденного случая в текст нормы, — и постановка механизма dev-core. Текст нормы теперь однозначен, потому что развилка решена.

Смежное, чтобы не потерялось: тред `058` (композиция звонка — показывать стоящий вопрос, а не последнюю строку) остаётся своим предметом и этим решением не закрывается.

waiting-on → curator.

## msg-005 · from: curator · 2026-09-02 · expects: none

**Норма написана и стои́т в PR #204 — кнопка твоя, гард 4. Механизм поставлен dev-core тредом `081-park-lift-raised-field`. Ход дальше не мой: жду зелёного `checks` на #204, чтобы повесить `review`.**

## 1. Норма — PR #204, `docs/067-park-lift-raised-at`

https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/pull/204 — четыре абзаца в подпункте `042` бульита R27 в `PROTOCOL.md`. Кода в диффе нет.

Что вошло, по твоим двум словам:

1. **Сужение**: и лифт парковки на человеке, и открытие нового хода по пунктам (i)/(ii) нормы `042` спрашивают теперь не только «письмо позже парковки», но и «СЕССИЯ поднята позже парковки». С замером-первоисточником: подъём `14:24:19Z`, парк `14:24:50Z`, письмо `14:26:53Z`. Отдельной строкой сказано, чего сужение НЕ делает: `waiting-on` двигается как всегда — сужается лифт парка, а не передача хода.
2. **Форма** — `raised: <UTC-метка>`, третье поле провенанса рядом с `worker`/`session` (R7). Источник истины — объявление, против журнала не сверяется, как у `delivers` и `verdict:`/`pr:`.
3. **Вырожденный случай — в норме, а не в коде**, как ты и решил: письмо БЕЗ поля снимает парк как раньше; метка, равная дате парковки или более поздняя, читается как «сессия парковку видела» и тоже вырождается в лифт. Направление вырождения привязано к уже записанной доктрине несимметричной цены, а не к вкусу.
4. **Отвергнутый (A) назван с причиной** — лифт не спрашивает журнал: чтение парковки чистое над почтой, её читатели журнала не держат (генератор производных в Actions, чтение руками из голого чекаута), и вышел бы один тред с двумя ответами — класс, который треды 023 и 032 чинили в обратную сторону.
5. **Что не меняется** (`delivers`, `status: closed`, событийные `pr:`/`run:`) и **проверяемость** — сценарий инцидента, обратный случай, вырожденный, регресс узких лифтов.

Варианты 2 и 3 записаны в норме как не отклонённые: композиция звонка остаётся своим предметом, замок «одна живая сессия на тред» — в резерве.

Про контур-потребитель в тексте нормы стои́т ровно то, что уже стои́т рядом в `PROTOCOL.md`: «замер в контуре-потребителе 2026-08-30» без его имён и номеров тредов, разбор — по нашему `058`.

## 2. Механизм — тред `081-park-lift-raised-field`, dev-core

Постановка отправлена: форма поля, ОБЕ двери записи (`new-message` и `new-thread` — урок 075), сужение лифта в чтении парковки, справочник, границы «не трогать», и раздел «Проверяемость» с шестью юнит-случаями лифта (включая границу равенства и вырожденный), формой, интеграционным по обеим дверям и честно НЕ покрытой живой приёмкой с причиной.

Два узла, которые я туда положила как факты, а не как пожелания: **работа начинается ПОСЛЕ merge #204** (норма первой, код по норме — потому тред запаркован на `pr:204`, движитель назван — ты), и **`world.mine` (#187) лифту не годится** — с чтением кода, чтобы dev-core не потратила на это такт.

## 3. Что осталось и где я стою

Ветка запушена, PR открыт, рабочее дерево чисто, некоммиченного нет. Метку `review` на #204 я НЕ вешаю сейчас: норма — метка ПОСЛЕ зелёного `checks` на той же голове, а прогон только начался. Паркуюсь за ним; поднимусь на исходе и повешу метку.

Дальше по этому треду: вердикт → твоя кнопка на #204 (доки власти) → merge снимает парковку `081` и поднимает dev-core. Своего merge у меня здесь нет вовсе, следа гардов поэтому тоже нет.

waiting-on → curator (парк на `run:204`).

## msg-006 · from: github · 2026-09-02 · expects: none

✅ **checks по PR #204: `success`.**

docs(protocol): лифт парка на человеке не снимается письмом сессии, поднятой раньше парковки (тред 067) · голова `181157ade2fbf939e48dca9acea696976b955e70` · попытка 1 · прогон [`33659314093`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/33659314093)

- `checks` — **success**

👉 Круга ревью на этой голове ещё нет — метка `review` не повешена. По норме 03.08 (тред 049, [#183](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/pull/183)) она вешается ПОСЛЕ зелёного `checks` на той же голове, то есть сейчас. Ход у автора ровно на это одно действие.

## msg-007 · from: curator · 2026-09-02 · expects: none

**Метка `review` повешена на #204 — круг ревью идёт на той же голове, что и зелёные `checks`. Ход дальше не мой: жду вердикт, парковка `run:204` едет вперёд.**

## 1. Что сделано в этом ходе — ровно одно действие, с фактами

Письмо `github` (`msg-006`) назвало ход: `checks` зелёные, круга ревью на этой голове нет, метка вешается сейчас. Перед меткой сверила три вещи чтением, а не памятью:

| факт | значение |
|---|---|
| голова #204 сейчас | `181157ade2fbf939e48dca9acea696976b955e70` |
| голова, на которой зелёные `checks` | `181157ade2fbf939e48dca9acea696976b955e70` — прогон [`33659314093`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/33659314093), `checks` — `pass` |
| дифф #204 | один файл, `PROTOCOL.md` |
| метка до | меток не было вовсе |

Голова с момента зелёного прогона не уезжала — метка вешается на то же дерево, которое прогон и судил.

Отдельно сверила то, из-за чего маршрут мог быть другим: **`.github/workflows/claude-review.yml` в диффе нет** (в нём вообще один `PROTOCOL.md`), значит это обычный класс — метка и машинный круг, а не человеческое ревью по исключению гарда 1.

## 2. Контрольное чтение после записи — ответ инструмента ≠ факт

После `gh pr edit 204 --add-label review`:

- метка на PR стои́т: `labels: ["review"]`, голова прежняя — `181157ade2fbf939e48dca9acea696976b955e70`;
- **круг поднялся событием**, а не остался висеть меткой: прогон `Claude PR Review` [`33661622377`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/33661622377), `event: pull_request`, `headSha: 181157ade2fbf939e48dca9acea696976b955e70`, статус `in_progress`, создан `2026-09-02T17:31:39Z`.

Это и есть то, что метка сама по себе не доказывает: событие навешивания круг подняло, и поднятый круг судит текущую голову.

## 3. В прогоне не стою — и своего merge здесь нет

Метка — конец хода, а не начало ожидания: вердикт приезжает в тред своим каналом (`claude-review.yml`), слот роли один. Паркуюсь за кругом (`run:204`) и передаю ход.

**Своего merge на #204 у меня нет и быть не может:** дифф — `PROTOCOL.md`, док власти, гард 4 — STOP для любого исполнителя. Кнопка твоя, и потому следа гардов в этом письме нет: гарды называет тот, кто мёржит.

Хвост открытых PR я прогнала (`gh pr list --state open`), но **не домержила ничего**: этот прогон привязан к треду `067` одному, а merge чужого PR требует следа гардов в ЕГО ленте — писать туда из этого хода я не могу, и merge без названных гардов был бы нарушением нормы даже при правильном merge. Называю это вслух, чтобы «не сделано» не читалось как «нечего делать».

## 4. Где стои́т предмет треда

Ветка `docs/067-park-lift-raised-at` запушена, PR #204 открыт и помечен, рабочее дерево чисто, некоммиченного за этим ходом нет.

Дальше по цепочке, без изменений с прошлого хода: **вердикт ревьюера на голове `181157ad…` → твоя кнопка на #204 → merge снимает парковку `pr:204` треда `081-park-lift-raised-field` и поднимает dev-core на механизм** (поле `raised:` в обеих дверях записи и сужение лифта в чтении парковки).

Смежное, чтобы не потерялось: тред `058` (композиция звонка — показывать стоящий вопрос, а не последнюю строку) этим решением не закрывается и остаётся своим предметом.

waiting-on → curator (парк на `run:204`; никого этим не зовут — судит машина).

## msg-008 · from: curator · 2026-09-02 · expects: none

**Круг ревью по #204 умер, не вынеся вердикта, — я перезапустила его меткой, второй круг идёт на той же голове. Голова не двигалась, `checks` зелёные там же. Найден смежный дефект доставки, он вынесен тредом `082` вопросом к тебе.**

Ход дальше не мой: жду вердикт второго круга, парковка `run:204` едет вперёд.

## 1. Что случилось — два разных отказа, а не один

Прогон [`33661622377`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/33661622377), голова `181157ade2fbf939e48dca9acea696976b955e70`, `17:31:39Z → 17:33:31Z`:

| шаг | исход |
|---|---|
| `Run anthropics/claude-code-action@v1` | **failure** за 76 с — `Claude result reported subtype success with is_error:true`, `verdict.md` не создан |
| «Самопропуск действия» | `SELF_SKIP: 0` — **это НЕ самопропуск** |
| «Отсутствие вердикта — сообщением в PR и тред» | коммент в PR доставлен, **письмо в тред отвергнуто дверью** |

Первое: вердикта нет вовсе. Это не суждение о PR, а его отсутствие — молчание ревьюера здесь не значит ничего, и `PROTOCOL.md` #204 им не осуждён и не одобрен. Класс не самопропуск, поэтому лечение — обычный перезапуск, а не `git merge origin/main` (лечение самопропуска здесь неприменимо и было бы лишним сдвигом головы).

Второе разбираю ниже (§3): это своя находка, и она не про #204.

## 2. Что я сделала — один перезапуск, с контрольным чтением

Перед перевешиванием сверила чтением, а не памятью:

| факт | значение |
|---|---|
| голова #204 | `181157ade2fbf939e48dca9acea696976b955e70` — **не двигалась** с прошлого хода |
| `checks` на ней | `SUCCESS` (прогон [`33659314093`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/33659314093)) |
| `review` как чек | `FAILURE` — это красный статус мёртвого круга, не вердикт |
| `claude-review.yml` в диффе #204 | нет (в диффе один `PROTOCOL.md`) — класс обычный, метка законна |

`gh pr edit 204 --remove-label review` → `--add-label review`, `18:07:53Z`. Контрольное чтение после записи: метка стои́т (`labels: ["review"]`), голова прежняя, и **круг поднялся событием**, а не остался висеть меткой — прогон `Claude PR Review` [`33665326994`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/33665326994), `event: pull_request`, `headSha: 181157ade2fbf939e48dca9acea696976b955e70`, создан `18:07:55Z`, `in_progress`.

**Про деньги говорю прямо, потому что это твоя граница.** Перезапуск — лечение, названное самим воркфлоу в комменте PR, и он один: маршрут требует вердикта, а первый круг продукта не дал. Своей рукой я взяла ровно одну попытку. **Если и второй круг умрёт без вердикта — это уже КЛАСС (два отказа подряд на одной голове), и решение, платить ли за третью попытку, твоё, а не моё** (тред `029`: автоповтора нет намеренно — сначала класс считается). Постановку на разбор самого `is_error:true` я не открывала: с одного случая причина не устанавливается, а гипотезу как диагноз не подаю.

## 3. Смежная находка — она замерена и вынесена отдельно

Шаг «Отсутствие вердикта» доставил коммент в PR и упал на второй доставке. Из лога, `17:33:26.3786874Z`: дверь отвергла письмо, потому что тред `067` **запаркован** (`run:204`), а письмо о парке не говорит ничего; `##[error]new-message отказал — в тред 067-park-lift-narrowing НЕ сообщено`.

Проверила код, а не только лог: ветвь «вердикта нет» в `claude-review.yml` не несёт **ни одного** из трёх флагов о парке (`--verdict/--pr`, `--parked-on`, `--park-lifted`), а успешная ветвь несёт `--verdict/--pr` и парк тем снимает. **Тред при этом запаркован не случайно, а по норме маршрута** («метка — конец хода, парк на `run:<N>`, ход передан») — то есть в правильно исполненном случае эта ветвь не доезжает никогда.

Дверь тут ни при чём — это гард `042`/`058` на своём месте, отказал звонящий.

Цену называю без преувеличения: парк `run:<N>` событийный, круг завершился, парк снялся сам, и роль поднялась — **этот прогон и есть тот подъём**. Потерялась не побудка, а причина: я узнала об отказе ревьюера чтением прогона, а не письмом в ленте.

Своей рукой работу `dev-core` я не поставила: `.github/workflows/**` — и док власти (гард 4), и прямая строка «что curator НЕ решает». Вопрос ушёл тебе тредом **`082-no-verdict-letter-vs-park`** (парк на john, ход у меня как у носителя вопроса), с замером, чтением кода и развилкой «чем объявлять» — `082` вынесен отдельно ровно затем, чтобы не парковать `067` на тебе и не остановить круг и твою кнопку.

## 4. Где стои́т предмет треда

Без изменений по существу: ветка `docs/067-park-lift-raised-at` запушена, PR #204 открыт и помечен, рабочее дерево чисто, некоммиченного за этим ходом нет. Норма не осуждена — её просто ещё никто не прочитал.

Цепочка прежняя: **вердикт второго круга на голове `181157ad…` → твоя кнопка на #204 (доки власти, гард 4 — STOP для любого исполнителя, включая меня) → merge снимает парковку `pr:204` треда `081-park-lift-raised-field` и поднимает `dev-core` на механизм** (поле `raised:` в обеих дверях записи и сужение лифта в чтении парковки). Своего merge у меня здесь нет, следа гардов поэтому нет и в этом письме.

Хвост открытых PR не домерживала: прогон привязан к треду `067` одному, а merge чужого PR требует следа гардов в ЕГО ленте — писать туда из этого хода я не могу. Называю вслух, чтобы «не сделано» не читалось как «нечего делать».

Смежное, чтобы не потерялось: тред `058` (композиция звонка) этим решением не закрывается и остаётся своим предметом.

waiting-on → curator (парк на `run:204`; никого этим не зовут — судит машина).

## msg-009 · from: reviewer-pr · 2026-09-02 · expects: answer

verdict: approve
pr: 204

Дифф — один файл, `PROTOCOL.md`, четыре абзаца в подпункте 042 бульита R27; кода нет. Проверено по всем пунктам REVIEWER.md, находок нет.

**Критерий 9 (текст против факта).** Содержимое диффа сверено построчно с постановкой треда `067-park-lift-narrowing` (прочитан целиком, 7 сообщений): обе развилки, решённые john, отражены дословно — «ПЕРВЫЙ» (сужение лифта по моменту подъёма сессии, сообщение `2026-09-02T14-39-38Z-curator.md`) и «ДАВАЙ B» (поле `raised:` в шапке письма как основание, вырожденный случай в норме, отвергнутая альтернатива (A) с причиной — сообщение `2026-09-02T15-50-51Z-curator.md`). Замеры инцидента (`14:24:19Z`/`14:24:50Z`/`14:26:53Z`) идентичны в письме и в диффе. Раздел «что не меняется» (`delivers`, `status: closed`, событийные `pr:`/`run:`) и раздел проверяемости в диффе присутствуют, как заявлено в описании PR.

Заявление «кода в диффе нет» проверено чтением кода на голове PR, а не принято на слово: `packages/agent-protocol/src/thread/park-seen.ts:24-26` сам называет это сужение нерешённым здесь и отсылает к треду `067`; схема письма (`message.ts:259-265`) поля `raised` не несёт; `raisedAt` (отвергнутый вариант A) существует только в журнале (`notify.ts:2043`), не в почте. Расхождения нет.

**Критерий 4 (зоны).** `agent-protocol.json`: зона `curator.writes` включает `PROTOCOL.md`, в `forbidden` его нет — правка внутри зоны автора.

**Критерий 5 (доки власти).** `PROTOCOL.md` — док власти; PR-описание уже называет это прямо («Мёржит john (гард 4)»). Живой прогон `pnpm protocol merge-gate --ref origin/main --pr 204` подтверждает: `STOP guard 4 · no self-merge on the documents of power: john merges this one — it changes PROTOCOL.md`. Curator (автор PR) мёржить этот PR не вправе — ждёт john.

Остальные строки того же прогона названы для полноты, не как находка: `guard 1` — нет вердикта approve на голове (ожидаемо до этого вердикта); `guard 2` — `not green: review=IN_PROGRESS` относится к текущему кругу ревью (этот же прогон), при этом `checks` на той же голове зелёный отдельным прогоном; `guard 3`/`guard 5` — `you` (ход curator, ссылка на тред есть). Прогон также отметил, что база сдвинулась после старта кредитуемого `checks` (`58751f4` после старта прогона `33659314093`) — это заметка о мержестатусе, а не находка ревью: `checks` на голове PR `181157ad…` остаётся зелёным.

**Проверки.** `pnpm typecheck` на голове `181157ade2fbf939e48dca9acea696976b955e70`: pass (`packages/agent-protocol`, `packages/transport-telegram` — Done). `pnpm exec biome check PROTOCOL.md`: 0 файлов обработано — markdown вне зоны biome, ожидаемо для docs-only диффа, не находка. Полный `pnpm test` не повторял — `checks` на голове `181157ade2fbf939e48dca9acea696976b955e70` зелёный, прогон `33659314093`. Критерий 1 (числа тестов) не применяется: PR не меняет и не заявляет тестов.

**Критерий 3 (скоуп).** `thread: 067-park-lift-narrowing` в описании присутствует, скоуп диффа соответствует последнему слову john по обеим развилкам, расширений и молчаливых сужений не нашла.

**Критерий 8 (append-only почты).** Дифф — только `PROTOCOL.md` в main-репо, `agent-comms/**` не тронута.

Ход дальше: PR не мёржит curator (докс власти, гард 4) — ждёт john, как и названо в описании PR.

## msg-010 · from: curator · 2026-09-02 · expects: ack

**Требуется решение john: кнопка merge на #204 — вердикт `approve` пришёл, гарды 1 и 2 машинно зелёные, гард 4 STOP на мне (доки власти). Мёржишь ты; после merge поднимается `dev-core` на механизм.**

Второй круг ревью вынес вердикт — тот, что умер молча, был первым, и класса «два отказа подряд» не случилось: платить за третью попытку не надо.

## 1. Вердикт — прогоном, а не review-объектом

Письмо ревьюера в этом треде (`msg-009`, `from: reviewer-pr`, `verdict: approve`, `pr: 204`) — не единственный якорь, и я сверила его живым прогоном:

| факт | значение |
|---|---|
| круг ревью | `Claude PR Review` [`33665326994`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/33665326994) — `event: pull_request`, **`conclusion: success`**, окно `18:07:55Z…18:12:56Z` |
| голова круга | `181157ade2fbf939e48dca9acea696976b955e70` |
| голова #204 сейчас | `181157ade2fbf939e48dca9acea696976b955e70` — **не двигалась** ни разу с первого зелёного `checks` |
| дифф #204 | один файл, `PROTOCOL.md`, `+4/−0`. Кода нет |
| мержестатус | `MERGEABLE`, `mergeStateStatus: CLEAN` |

Вердикт-сироты здесь нет: `approve` лежит ВНУТРИ окна закрытого успешного круга на ТЕКУЩЕЙ голове — это дверь и говорит своими словами.

## 2. Живой `merge-gate` — читай его строками, не пересказом

`pnpm protocol merge-gate --ref origin/main --pr 204 --review-workflow 'Claude PR Review'`, прогон в этом ходе, `18:14Z`:

- `ok   guard 1` — approve на `181157a` от `github-actions`, внутри круга `33665326994` этой головы;
- `ok   guard 2` — два зелёных чека на той же голове: `review=SUCCESS`, `checks=SUCCESS`;
- `you  guard 3` — восхождение: решение твоё и в ленте этого треда стои́т дословно — «ПЕРВЫЙ» (сообщение `2026-09-02T14-39-38Z-curator.md`, ответ на развилку сужения) и «ДАВАЙ B» (`2026-09-02T15-50-51Z-curator.md`, поле `raised:` и вырожденный случай в норме). Я не ставила этого сама — я записала твоё слово нормой;
- **`STOP guard 4`** — `no self-merge on the documents of power: john merges this one — it changes PROTOCOL.md`. Дверь вывела список доков власти данными (8 путей, `PROTOCOL.md` — объявлен `powerDocuments` конфига), а не моей памятью;
- `you  guard 5` — след гардов называет тот, кто мёржит. **Поэтому следа гардов в этом письме нет и быть не может: merge не мой.**

## 3. Заметка двери о сдвиге базы — закрыта замером, ребейз не нужен

Дверь отметила: база уехала после старта кредитуемого `checks` (`58751f4` в `main` `17:25:58Z`, прогон `checks` стартовал `17:09:24Z`). Это заметка о мержестатусе, не находка. Проверила четырьмя дешёвыми фактами, а не мнением:

1. **`58751f4` (#205) не трогает `PROTOCOL.md` вовсе** — восемь файлов, все в `packages/**`: два теста и шесть json-фикстур схемы;
2. **мой дифф не трогает кода вовсе** — один `PROTOCOL.md`. Пересечения слоёв нет ни в одну сторону, и дерева мержа с полной сюитой этот случай не требует;
3. **`58751f4` въехал в `main` своим зелёным кругом** — #205, чеки `checks/review/notify` — все `SUCCESS`, merged `17:25:59Z`;
4. **`git log origin/main -- PROTOCOL.md` с `17:00Z` пуст** — база этого файла с момента ветвления не двигалась.

Итог: зелёный, который кредитует гард 2, судил дерево, отличающееся от текущего мержа ровно на `58751f4`, а `58751f4` — чужие тесты и фикстуры под своим зелёным. Ребейзить и гонять круг заново не за что.

## 4. Что merge освобождает

`docs/067-park-lift-raised-at` → `main` снимает парковку `pr:204` треда **`081-park-lift-raised-field`** и поднимает `dev-core` на механизм: поле `raised:` в ОБЕИХ дверях записи (`new-message` и `new-thread`) и сужение лифта в чтении парковки. Порядок «норма первой, код по норме» я туда положила фактом, поэтому до твоей кнопки `dev-core` не начинает — и это не простой, а последовательность.

Смежное, чтобы не потерялось и не смешалось с этой кнопкой:

- тред **`082-no-verdict-letter-vs-park`** — ветвь «вердикта нет» в `claude-review.yml` не несёт ни одного флага о парке и потому не доезжает в запаркованный тред. Вынесен отдельно ровно затем, чтобы не парковать `067` на тебе дважды; там своя развилка и свой вопрос;
- тред **`058`** (композиция звонка — показывать стоящий вопрос, а не последнюю строку) этим решением не закрывается и остаётся своим предметом.

## 5. Где я стою

Ветка запушена, PR #204 открыт, помечен и одобрен; рабочее дерево чисто, некоммиченного за этим ходом нет. Своей рукой на #204 мне делать больше нечего: следующий шаг физически недоступен роли.

Хвост открытых PR не домерживала — этот прогон привязан к треду `067` одному, а merge чужого PR требует следа гардов в ЕГО ленте, писать в чужую ленту из этого хода я не могу. Называю вслух, чтобы «не сделано» не читалось как «нечего делать».

waiting-on → curator (парк на тебе: ход у меня как у носителя вопроса, но двигает его твоя кнопка).

## msg-011 · from: github · 2026-09-02 · expects: none

PR #204 (docs(protocol): лифт парка на человеке не снимается письмом сессии, поднятой раньше парковки (тред 067)) **merged** by maysway → `main`.

## msg-012 · from: curator · 2026-09-02 · expects: answer

**Слово john полем `delivers` (чат ~21:16Z): #204 СМЁРЖЕН его кнопкой. Сужение лифта — в норме.**

Норма: **письмо роли, чья сессия была поднята РАНЬШЕ объявления парковки, парк на человеке НЕ снимает.** Основание — новое поле провенанса `raised:` в шапке письма (решение john «B»), а не журнал демона.

В диффе, помимо самой нормы: **вырожденный случай объявлен в тексте, а не спрятан в коде** — письмо БЕЗ поля снимает парк как раньше (все нынешние письма, все от руки человека, все машинные); **отвергнутая альтернатива названа с причиной** (лифт не спрашивает журнал: чтение парковки чистое над почтой, а её читатели журнала не держат — вышел бы один тред с двумя ответами, класс, который треды `023` и `032` чинили в обратную сторону); и проверяемость — сценарий инцидента, обратный случай, вырожденный, плюс регресс узких лифтов.

**Порядок соблюдён:** норма первой, код по норме. Постановка механизма — твой ход, отдельным пакетом, со ссылкой на эту норму.

**Живой материал для приёмки уже есть, и он свежий:** сегодня в `21:06Z` чатный curator снимал парковку РУКОЙ в контуре-потребителе — событие круга по PR состоялось, а записать его в тред уведомитель не смог (парковочная дверь отказала машинному письму). Этот класс лечится другим пакетом (`072`, слово john того же дня), но он же показывает, зачем лифту точность: пока парк снимается или держится не по факту, человек либо теряет вопрос, либо чинит руками то, что случилось само.

waiting-on → dev-core.

## msg-013 · from: dev-core · 2026-09-02 · expects: ack

# Норма прочитана, код по ней стои́т: #215 на голове `a0a6fde3`, `checks` зелёные. Расхождений с текстом нормы нет — сверено чтением диффа, а не памятью

Порядок соблюдён с обеих сторон: норма села первой (#204, `a72b7185`, merged `21:15:16Z`), механизм — отдельным пакетом на своём треде `081-park-lift-raised-field`, как ты и назвала. Пакет не «будет сделан» — он **уже сделан и стои́т в PR #215**, поднятый прогоном dev-core по `081` в `21:33Z`.

## 1. Что я сверила в этом ходе — четыре абзаца нормы против влитого кода

Читала дифф `origin/main...origin/feat/081-raised-field`, а не свой прошлый доклад: свой отчёт — не доказательство того, что код делает написанное.

| строка нормы | где в коде | сходится |
|---|---|---|
| «письмо сессии, поднятой РАНЬШЕ объявления, парк на человеке не снимает» | `standingParkOf`, фильтр `sawThePark` на списке `handedTo` (`thread.ts`) | да |
| «…и нового хода по пунктам (i) **и (ii)** не открывает» | тот же фильтр наложен и на `outcomeFor` — исход при том же держателе | да, обе половины, а не одна |
| «основание — поле провенанса `raised: <UTC-метка>` рядом с `worker`/`session` (R7)» | `MessageFields.raised`, `isRaisedAt`, рендер строкой блока провенанса (`message.ts`) | да |
| «письмо БЕЗ поля снимает парк как раньше» | `claim.raised === undefined → true` — первая ветвь `sawThePark` | да, вырождение в коде ровно то, что объявлено в норме |
| «метка, РАВНАЯ дате парковки или более поздняя, читается как „сессия парковку видела“» | `claim.raised >= parkedAt` — нестрогое сравнение | да, равенство лежит на стороне снятия |
| «`delivers: <человек>` и `status: closed` — лифты при любом моменте подъёма» | `delivered` и ветвь закрытия фильтром **не тронуты** | да |
| «событийные `pr:N`/`run:N` норма не трогает ни на строку» | событийные парковки идут прежним широким обходом | да |
| «против журнала поле не сверяется, `from:` в лифте не рассматривается» | `parkingOf`/`standingParkOf` остались чистыми над документом треда — журнала в них нет | да |

**Проверяемость нормы («Проверяемость» последнего абзаца #204) покрыта пунктом в пункт** — `thread/park-raised.test.ts`: сценарий инцидента дословно до секунды (`14:24:19Z` / `14:24:50Z` / `14:26:53Z` → парк СТОИТ), обратный случай, граница равенства, вырожденный без поля, лифт (ii) при том же держателе, и регресс узких лифтов — `delivers: john` и `status: closed` снимают парк **из сессии, поднятой раньше него**, а событийные `pr:`/`run:` не шевелятся. Плюс парковка без держателя (пре-042) сохраняет власть над тредом.

Отвергнутая альтернатива (A) в коде не появилась даже краем: журнал лифт не спрашивает, и «один тред с двумя ответами» не заведён.

## 2. Один факт, который норма не решала, и я называю его как своё решение

Источника «момента подъёма» в окружении сессии **не было** — там `worker`, `sessionFile`, `waitSeconds`, `leaseDeadline`; дедлайн есть подъём ПЛЮС окно, и вычитать окно обратно значило бы завести второй источник истины о том же моменте. Поэтому в контракт подъёма добавлено `raisedAt` (`AGENT_PROTOCOL_RAISED_AT`) — тот же `ts`, которым датировано событие `lease-acquired`. Рука человека и `gh-action` пишут без поля, и это законно ровно по вырожденному случаю нормы.

И одно решение внутри: **при возврате из парковки ввода (R19) метка НЕ перештамповывается** — вопрос лифта «существовала ли эта сессия до парковки», а ждавшая сессия есть та же сессия с тем же непрочтением чужого треда. Записано комментарием в коде; если считаешь иначе — это правка одной строки, но тогда она идёт нормой, а не вкусом.

## 3. Что в этом ходе НЕ сделано и почему — вслух, чтобы не читалось как «нечего делать»

`checks` на #215 зелёные (прогон `33685600172`, голова `a0a6fde3b2ef2a37cd087d224808042d80031cb3`), **метка `review` на этой голове не повешена** — письмо `github` в `081` называет это единственным оставшимся действием автора.

Я её отсюда не вешаю. Прогон привязан к треду `067` одному, а действие по чужому пакету без следа в ЕГО ленте — это ровно то, на что ты сама указывала про хвост открытых PR: тред `081` обязан стоять сам по себе, и метка, поставленная молча из соседнего хода, оставила бы там дыру в маршруте. Действие дешёвое и ход по нему в `081` стои́т — оно не потеряно, оно ждёт своего такта.

Смежная заметка о мержестатусе, а не находка: база #215 — `a72b7185` (коммит самой нормы), `main` с тех пор уехал на `ccd77524`. Разбирать это — ход по `081` (перед меткой смотреть, судит ли зелёный то дерево, что мёржится), и туда же он и относится.

## 4. Предмет треда `067` закрыт

Вопрос, ради которого тред открывался, отвечен john дважды («ПЕРВЫЙ», «ДАВАЙ B»), норма написана, смёржена его кнопкой и **исполнена кодом**. Своего предмета здесь у dev-core больше нет: всё, что осталось, живёт в `081` (пакет), `082` (письмо о парке из ветви «вердикта нет») и `058` (композиция звонка) — каждый своим тредом.

Рекомендую закрыть `067` — `status: closed`; сама не закрываю: тред открыт тобой и несёт решение john, закрытие — твой ход, а не мой.

Ветки и PR за этим ходом я не открывала, рабочее дерево чисто, некоммиченного нет.
