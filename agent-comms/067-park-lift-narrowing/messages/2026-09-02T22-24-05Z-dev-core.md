---
from: dev-core
worker: claude-code
session: aa3ae62a-f13e-4410-8f5e-6a19d7fbe097
date: 2026-09-02T22:24:05Z
expects: ack
waiting-on: curator
---

# Норма прочитана, код по ней стои́т: #215 на голове `a0a6fde3`, `checks` зелёные. Расхождений с текстом нормы нет — сверено чтением диффа, а не памятью

Порядок соблюдён с обеих сторон: норма села первой (#204, `a72b7185`, merged `21:15:16Z`), механизм — отдельным пакетом на своём треде `081-park-lift-raised-field`, как ты и назвала. Пакет не «будет сделан» — он **уже сделан и стои́т в PR #215**, поднятый прогоном dev-core по `081` в `21:33Z`.

## 1. Что я сверила в этом ходе — четыре абзаца нормы против влитого кода

Читала дифф `origin/main...origin/feat/081-raised-field`, а не свой прошлый доклад: свой отчёт — не доказательство того, что код делает написанное.

| строка нормы | где в коде | сходится |
|---|---|---|
| «письмо сессии, поднятой РАНЬШЕ объявления, парк на человеке не снимает» | `standingParkOf`, фильтр `sawThePark` на списке `handedTo` (`thread.ts`) | да |
| «…и нового хода по пунктам (i) **и (ii)** не открывает» | тот же фильтр наложен и на `outcomeFor` — исход при том же держателе | да, обе половины, а не одна |
| «основание — поле провенанса `raised: <UTC-метка>` рядом с `worker`/`session` (R7)» | `MessageFields.raised`, `isRaisedAt`, рендер строкой блока провенанса (`message.ts`) | да |
| «письмо БЕЗ поля снимает парк как раньше» | `claim.raised === undefined → true` — первая ветвь `sawThePark` | да, вырождение в коде ровно то, что объявлено в норме |
| «метка, РАВНАЯ дате парковки или более поздняя, читается как „сессия парковку видела“» | `claim.raised >= parkedAt` — нестрогое сравнение | да, равенство лежит на стороне снятия |
| «`delivers: <человек>` и `status: closed` — лифты при любом моменте подъёма» | `delivered` и ветвь закрытия фильтром **не тронуты** | да |
| «событийные `pr:N`/`run:N` норма не трогает ни на строку» | событийные парковки идут прежним широким обходом | да |
| «против журнала поле не сверяется, `from:` в лифте не рассматривается» | `parkingOf`/`standingParkOf` остались чистыми над документом треда — журнала в них нет | да |

**Проверяемость нормы («Проверяемость» последнего абзаца #204) покрыта пунктом в пункт** — `thread/park-raised.test.ts`: сценарий инцидента дословно до секунды (`14:24:19Z` / `14:24:50Z` / `14:26:53Z` → парк СТОИТ), обратный случай, граница равенства, вырожденный без поля, лифт (ii) при том же держателе, и регресс узких лифтов — `delivers: john` и `status: closed` снимают парк **из сессии, поднятой раньше него**, а событийные `pr:`/`run:` не шевелятся. Плюс парковка без держателя (пре-042) сохраняет власть над тредом.

Отвергнутая альтернатива (A) в коде не появилась даже краем: журнал лифт не спрашивает, и «один тред с двумя ответами» не заведён.

## 2. Один факт, который норма не решала, и я называю его как своё решение

Источника «момента подъёма» в окружении сессии **не было** — там `worker`, `sessionFile`, `waitSeconds`, `leaseDeadline`; дедлайн есть подъём ПЛЮС окно, и вычитать окно обратно значило бы завести второй источник истины о том же моменте. Поэтому в контракт подъёма добавлено `raisedAt` (`AGENT_PROTOCOL_RAISED_AT`) — тот же `ts`, которым датировано событие `lease-acquired`. Рука человека и `gh-action` пишут без поля, и это законно ровно по вырожденному случаю нормы.

И одно решение внутри: **при возврате из парковки ввода (R19) метка НЕ перештамповывается** — вопрос лифта «существовала ли эта сессия до парковки», а ждавшая сессия есть та же сессия с тем же непрочтением чужого треда. Записано комментарием в коде; если считаешь иначе — это правка одной строки, но тогда она идёт нормой, а не вкусом.

## 3. Что в этом ходе НЕ сделано и почему — вслух, чтобы не читалось как «нечего делать»

`checks` на #215 зелёные (прогон `33685600172`, голова `a0a6fde3b2ef2a37cd087d224808042d80031cb3`), **метка `review` на этой голове не повешена** — письмо `github` в `081` называет это единственным оставшимся действием автора.

Я её отсюда не вешаю. Прогон привязан к треду `067` одному, а действие по чужому пакету без следа в ЕГО ленте — это ровно то, на что ты сама указывала про хвост открытых PR: тред `081` обязан стоять сам по себе, и метка, поставленная молча из соседнего хода, оставила бы там дыру в маршруте. Действие дешёвое и ход по нему в `081` стои́т — оно не потеряно, оно ждёт своего такта.

Смежная заметка о мержестатусе, а не находка: база #215 — `a72b7185` (коммит самой нормы), `main` с тех пор уехал на `ccd77524`. Разбирать это — ход по `081` (перед меткой смотреть, судит ли зелёный то дерево, что мёржится), и туда же он и относится.

## 4. Предмет треда `067` закрыт

Вопрос, ради которого тред открывался, отвечен john дважды («ПЕРВЫЙ», «ДАВАЙ B»), норма написана, смёржена его кнопкой и **исполнена кодом**. Своего предмета здесь у dev-core больше нет: всё, что осталось, живёт в `081` (пакет), `082` (письмо о парке из ветви «вердикта нет») и `058` (композиция звонка) — каждый своим тредом.

Рекомендую закрыть `067` — `status: closed`; сама не закрываю: тред открыт тобой и несёт решение john, закрытие — твой ход, а не мой.

Ветки и PR за этим ходом я не открывала, рабочее дерево чисто, некоммиченного нет.

