---
from: curator
worker: claude-code
session: 02876f4a-f4df-455c-9cae-4fac2bb3f9cb
date: 2026-09-02T17:11:27Z
expects: none
waiting-on: curator
parked-on: run:204
---

**Норма написана и стои́т в PR #204 — кнопка твоя, гард 4. Механизм поставлен dev-core тредом `081-park-lift-raised-field`. Ход дальше не мой: жду зелёного `checks` на #204, чтобы повесить `review`.**

## 1. Норма — PR #204, `docs/067-park-lift-raised-at`

https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/pull/204 — четыре абзаца в подпункте `042` бульита R27 в `PROTOCOL.md`. Кода в диффе нет.

Что вошло, по твоим двум словам:

1. **Сужение**: и лифт парковки на человеке, и открытие нового хода по пунктам (i)/(ii) нормы `042` спрашивают теперь не только «письмо позже парковки», но и «СЕССИЯ поднята позже парковки». С замером-первоисточником: подъём `14:24:19Z`, парк `14:24:50Z`, письмо `14:26:53Z`. Отдельной строкой сказано, чего сужение НЕ делает: `waiting-on` двигается как всегда — сужается лифт парка, а не передача хода.
2. **Форма** — `raised: <UTC-метка>`, третье поле провенанса рядом с `worker`/`session` (R7). Источник истины — объявление, против журнала не сверяется, как у `delivers` и `verdict:`/`pr:`.
3. **Вырожденный случай — в норме, а не в коде**, как ты и решил: письмо БЕЗ поля снимает парк как раньше; метка, равная дате парковки или более поздняя, читается как «сессия парковку видела» и тоже вырождается в лифт. Направление вырождения привязано к уже записанной доктрине несимметричной цены, а не к вкусу.
4. **Отвергнутый (A) назван с причиной** — лифт не спрашивает журнал: чтение парковки чистое над почтой, её читатели журнала не держат (генератор производных в Actions, чтение руками из голого чекаута), и вышел бы один тред с двумя ответами — класс, который треды 023 и 032 чинили в обратную сторону.
5. **Что не меняется** (`delivers`, `status: closed`, событийные `pr:`/`run:`) и **проверяемость** — сценарий инцидента, обратный случай, вырожденный, регресс узких лифтов.

Варианты 2 и 3 записаны в норме как не отклонённые: композиция звонка остаётся своим предметом, замок «одна живая сессия на тред» — в резерве.

Про контур-потребитель в тексте нормы стои́т ровно то, что уже стои́т рядом в `PROTOCOL.md`: «замер в контуре-потребителе 2026-08-30» без его имён и номеров тредов, разбор — по нашему `058`.

## 2. Механизм — тред `081-park-lift-raised-field`, dev-core

Постановка отправлена: форма поля, ОБЕ двери записи (`new-message` и `new-thread` — урок 075), сужение лифта в чтении парковки, справочник, границы «не трогать», и раздел «Проверяемость» с шестью юнит-случаями лифта (включая границу равенства и вырожденный), формой, интеграционным по обеим дверям и честно НЕ покрытой живой приёмкой с причиной.

Два узла, которые я туда положила как факты, а не как пожелания: **работа начинается ПОСЛЕ merge #204** (норма первой, код по норме — потому тред запаркован на `pr:204`, движитель назван — ты), и **`world.mine` (#187) лифту не годится** — с чтением кода, чтобы dev-core не потратила на это такт.

## 3. Что осталось и где я стою

Ветка запушена, PR открыт, рабочее дерево чисто, некоммиченного нет. Метку `review` на #204 я НЕ вешаю сейчас: норма — метка ПОСЛЕ зелёного `checks` на той же голове, а прогон только начался. Паркуюсь за ним; поднимусь на исходе и повешу метку.

Дальше по этому треду: вердикт → твоя кнопка на #204 (доки власти) → merge снимает парковку `081` и поднимает dev-core. Своего merge у меня здесь нет вовсе, следа гардов поэтому тоже нет.

waiting-on → curator (парк на `run:204`).

