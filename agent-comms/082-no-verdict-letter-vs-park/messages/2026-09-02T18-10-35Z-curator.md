---
from: curator
worker: claude-code
session: 6b848830-f97c-4b35-a01b-2232c7600cf9
date: 2026-09-02T18:10:35Z
expects: ack
waiting-on: curator
parked-on: john
---

**Требуется решение john. Вопрос первой строкой: чинить ли в `.github/workflows/claude-review.yml` ветвь «вердикта нет» так, чтобы её письмо доезжало в ЗАПАРКОВАННЫЙ тред — и если да, ставлю ли я эту работу `dev-core` своей рукой, или постановка тоже твоя?**

Происхождение: вопрос найден в ходе треда `067-park-lift-narrowing` (мой прогон 2026-09-02), где ровно этот отказ и сработал живьём. Вынесен отдельным тредом по той же причине, по какой в своё время был вынесен сам `067`: person-парк морозит тред целиком, а `067` стои́т на круге ревью по #204 и твоей кнопке — парковать его на тебе значило бы остановить работу, которую этот вопрос не блокирует.

Вопрос у тебя, а не у меня, потому что `.github/workflows/**` — и док власти (гард 4), и прямая строка моей карточки в разделе «что curator НЕ решает». Правило последней строки: не уверена, важно ли — значит важно. Своей рукой я `dev-core` эту работу не поставила и жду слова.

## 1. Замер — первоисточник, а не гипотеза

Круг ревью по PR #204, прогон [`33661622377`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/33661622377), голова `181157ade2fbf939e48dca9acea696976b955e70`.

Две вещи произошли подряд, и они РАЗНЫЕ:

1. шаг `Run anthropics/claude-code-action@v1` упал за 76 с — `Claude result reported subtype success with is_error:true`, `verdict.md` не создан. Это отказ ревьюера, класс известный, лечится перезапуском (я его перезапустила — см. `067`);
2. шаг **«Отсутствие вердикта — сообщением в PR и тред»** доставил коммент в PR и **упал на второй доставке**. Дословно из лога, `2026-09-02T17:33:26.3786874Z`:

> `agent-protocol: thread '067-park-lift-narrowing' is PARKED behind the round running on PR #204 since 2026-09-02T17:33:09Z, and this message says nothing about it. … Say what THIS letter does about the park: '--verdict <approve|needs-fixes> --pr 204' …, '--parked-on run:204' …, or '--park-lifted run:204' …`

> `##[error]new-message отказал — в тред 067-park-lift-narrowing НЕ сообщено (коммент в PR доставлен).`

Дверь отработала правильно — это гард `042`/`058`, и он на своём месте. Отказал ЗВОНЯЩИЙ.

## 2. Почему это не разовая неудача, а структурный случай

Читала код, а не лог: `.github/workflows/claude-review.yml`, ветвь «вердикта нет» (стр. ~780–800) зовёт `new-message` с `--from reviewer-pr --expects answer ${WAIT_ARGS} --worker gh-action` и **ни одного флага о парке**. Пары `--verdict`/`--pr` там нет намеренно, и причина в комментарии написана честно: исхода не сформировано вовсе, шапка с вердиктом была бы ложью. С этим я согласна — вердикта там взяться неоткуда.

Но флагов о парке у двери ТРИ, а не один, и ветвь не использует ни одного. Успешная ветвь (стр. ~427) кладёт `--verdict/--pr` и тем парк снимает — у неё отказа нет по построению.

**Существенное:** тред в этот момент запаркован НЕ случайно. Норма маршрута (карточка curator, «Метка — это конец хода»): повесил `review` → передал ход → запарковался на `run:<N>`. То есть в СТАНДАРТНОМ, правильно исполненном случае ветвь «вердикта нет» доезжает ровно до запаркованного треда — и не доезжает никогда. Молчит она не тихо (прогон краснеет, коммент в PR лежит), но в ленте треда об отказе ревьюера нет ни строки: я узнала о нём чтением прогона, а не письмом.

## 3. Цена сейчас — называю честно, не преувеличивая

Парк `run:<N>` событийный и снимается завершением круга сам, поэтому автор всё же поднимается. Теряется не подъём, а **причина**: роль поднята и не знает, что круг умер, — пока не прочитает прогон руками. Полный список того, что несёт письмо и не несёт подъём: класс отказа (самопропуск против дефекта), лечение (перезапуск метки против `git merge origin/main`), номер прогона.

## 4. Развилка, если ответ «чинить» — она про то, ЧЕМ объявлять

Механизма не выбираю за тебя, но обе двери назову, чтобы слово было информированным:

- **`--park-lifted run:<N>`** — «круг, за которым стоял парк, кончился». Правда по факту: ждали именно этот круг, он завершён, хотя и без продукта. Поднимает автора с письмом на руках;
- **`--parked-on run:<N>`** — «письмо стои́т рядом с парком, вопрос стои́т». Неправда здесь: круга больше нет, ждать нечего, и парк остался бы висеть на завершённом прогоне.

Моя рекомендация — первое, и это рекомендация, не решение.

## 5. Границы, чтобы не расползлось

Отказ ревьюера сам по себе (`is_error:true`) — НЕ предмет этого треда: класс известен, автоповтора нет намеренно по твоему решению (тред `029`). Здесь предмет один — доставка сообщения об отсутствии вердикта в тред, который запаркован по норме.

waiting-on → curator (ход у носителя вопроса; парк на john).

