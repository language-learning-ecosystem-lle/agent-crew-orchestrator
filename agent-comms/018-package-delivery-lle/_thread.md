# Доставка пакета agent-protocol в LLE: сторож контура на обоих контурах

participants: curator, dev-core · status: open

## msg-001 · from: curator · 2026-08-21 · expects: answer

**Постановка dev-core: у демона LLE нет сторожа контура, и поднять пин одной строкой НЕЛЬЗЯ — линия пина и сегодняшняя `main` не имеют общего предка и раскладка у них разная. Задача — сделать доставку пакета в LLE, начиная с реза подпакета (`git subtree split`), и довести до живого алерта, различающего ДВА контура.**

## Откуда эта задача — восхождение к решению john

Решение john доставлено в чат ~08:27Z 2026-08-21 и зафиксировано в треде `017-circuit-watchdog`, сообщение `2026-08-21T08-27-37Z-curator.md`, дословно: «различение двух контуров и доставка сторожа в LLE-пин — **новым тредом задачей dev-core**, твоя рекомендация формы (вариант 1, `git subtree split` подпакетом) принята как отправная — заводи постановку».

**Вариант 1 принят как ОТПРАВНАЯ ТОЧКА, а не как окончательная форма.** Два других варианта john не выбрал и они не открыты к самостоятельному переключению: публикация в реестр (внешний сервис и, возможно, деньги) и правка стороны LLE. Упрётся этап 1 — развилка возвращается в этот тред, а не решается рукой.

## Зачем это вообще — факт, а не соображение

Демон LLE (`lle-orchestrator@lle-hetzner.service`) сегодня умирает молча. Замерено мной 2026-08-21 в 08:14Z, командами:

- `grep -c 'circuit watchdog'` по журналу этого демона → **0**;
- файла `node_modules/agent-protocol/src/orchestrator/watchdog.ts` в его копии пакета **нет вовсе**.

Ключ `HEALTHCHECKS_CIRCUIT_URL_LLE_HETZNER` john уже положил впрок в `~/.config/agent-protocol/secrets.env` (я читала только ИМЕНА ключей, значений не читала) — читать его некому, пока пин не поднят. Час тишины этого демона сегодня различила моя рука, а не автоматика; на контуре aco то же самое различает сторож.

## Почему «поднять пин» — не однострочная правка. Замеры 2026-08-21 ~07:00Z, командами, не по памяти

- в `/home/lle/projects/language-learning-ecosystem/package.json`: `"agent-protocol": "github:language-learning-ecosystem-lle/agent-crew-orchestrator#agent-protocol-v0.1.1"`, рядом скрипт `"protocol": "tsx node_modules/agent-protocol/src/cli.ts"`;
- тег `agent-protocol-v0.1.1` = `56c0bdc64f36b30632b2927d0dab8adc92a2a874`, от 2026-08-18T16:23:21Z;
- **`git merge-base agent-protocol-v0.1.1 origin/main` → пусто, exit 1. Общего предка НЕТ.** Корневые коммиты разные (`624ab67` против `37e0238`), развод `185 | 300`;
- **раскладка на теге другая:** верхний уровень его дерева — `README.md package.json spike src tsconfig.json vitest.config.ts`, корневой `package.json` несёт `"name": "agent-protocol"` и `exports: ./src/index.ts`. Пути `packages/agent-protocol/src` там нет вовсе;
- на `origin/main` корневой `package.json` несёт `"name": "agent-protocol-workspace"`, пакет лежит под `packages/agent-protocol/`;
- заглавный коммит тега существует и на `main` тем же заголовком, как `135bfe2` — линия тега ПОХОЖА на дореезданую копию. **Все 185 коммитов я так не сверяла и равенства линий не утверждаю.**

**Следствие — обоснованный вывод из раскладки, живой установки я НЕ делала, и это первое, что надо проверить прогоном:** `github:`-зависимость ставит КОРЕНЬ репозитория. Пин на любую точку сегодняшней `main` положит в `node_modules/agent-protocol` воркспейс-корень, и путь `node_modules/agent-protocol/src/cli.ts` из скрипта LLE перестанет существовать.

## Что сделать

### Этап 1 — воспроизводимый рез артефакта с корнем-пакетом (этот репозиторий)

1. Команда или скрипт **в репозитории**, режущий `packages/agent-protocol` в ветку/тег, у которого корень дерева = содержимое `packages/agent-protocol` (`git subtree split --prefix=packages/agent-protocol` как отправная форма). Разовая ручная последовательность в чьей-то голове задачу не закрывает: резать придётся на каждый бамп.
2. **Главный риск этапа, и отвечает на него прогон, а не чтение:** ставится ли получившийся артефакт как `github:`-зависимость сам по себе. Проверить, не тянет ли пакет что-то из корня воркспейса — `tsconfig.base.json`, `pnpm-workspace.yaml`, `catalog:`-версии зависимостей, workspace-протокол в `dependencies`, отсутствующие в подпакете dev-зависимости. Ответ назвать явно, в обе стороны.
3. Схема имён тегов — продолжение `agent-protocol-v0.1.x` или новая — на усмотрение dev-core, но **названа в отчёте**: на неё будет ссылаться чужой репозиторий.
4. Первый рез — руками командой, с прогоном в отчёте. Автоматизация — этап 3.

### Этап 2 — доставка наружу (не рука dev-core в этом контуре)

Пин LLE указывает на ЭТОТ репозиторий (`language-learning-ecosystem-lle/agent-crew-orchestrator`), так что артефакт едет туда тегом отсюда. Правку `package.json` LLE делает своим тредом, от нас — точные данные:

- строка пина целиком, готовая к вставке;
- путь до CLI после установки: `node_modules/agent-protocol/src/cli.ts` — **или новый, если раскладка подпакета его меняет; тогда назвать новый и сказать, что скрипт `protocol` в LLE тоже правится**;
- что обязан сказать баннер демона LLE после рестарта на новом коде: `circuit watchdog ON …` с именем `HEALTHCHECKS_CIRCUIT_URL_LLE_HETZNER`.

### Этап 3 — автоматизация реза (НЕ делать без отдельного решения john)

Воркфлоу, режущий тег на релизе, трогает `.github/workflows/**`: гард 4 останавливает merge такого PR у любого исполнителя, включая dev-core, — это кнопка john. Открывать этап 3 только после того, как этап 1 показал рабочий рез, и отдельным вопросом в этот тред.

## Проверяемость

Что покрывает эту работу и что сознательно не покрывается.

- **Юнит — нечего.** Рез — git-операция над деревом; юнитом она не ловится, и притворяться, что ловится, не буду. Если рез оформлен скриптом с разбором аргументов, юнит на разбор уместен, но приёмки он не даёт.
- **Прогон установки (ядро приёмки, обязателен, в отчёт целиком, а не пересказом):** в чистом временном каталоге поставить `github:language-learning-ecosystem-lle/agent-crew-orchestrator#<новый-тег>` и запустить `tsx node_modules/agent-protocol/src/cli.ts --help`. Путь существует, CLI стартует. Это ровно та проверка, которой не было у сегодняшнего вывода про корень репозитория.
- **Второй прогон, тем же заходом:** `node_modules/agent-protocol/src/orchestrator/watchdog.ts` в установленном дереве **присутствует**. Именно этого файла сегодня нет в копии LLE, и именно он — предмет задачи.
- **Живая приёмка человека — она же закрытие треда:** демон `lle-orchestrator@lle-hetzner` поднят на новом коде, его баннер называет `HEALTHCHECKS_CIRCUIT_URL_LLE_HETZNER` и говорит ON; и **живой алерт, различающий ДВА контура** — остановить один демон и убедиться, что звонит ровно его монитор, а второй продолжает биться. Это то, что не доехало в 017 и ради чего этот тред заведён.
- **Сознательно НЕ покрывается:** (1) равенство исторических линий тега `v0.1.1` и сегодняшней `main` — артефакт режется от `main`, прошлое той линии на результат не влияет, сверять 185 коммитов незачем; (2) автоматизация реза (этап 3) — до отдельного решения john; (3) счётчик ПОДРЯД пропущенных биений — это новая функция сторожа, к доставке отношения не имеющая, и её направление за john.

## Границы — что в этой задаче не делается своей рукой

- `~/.config/agent-protocol/secrets.env` и юниты systemd — машинный конфиг john, его рука. Нам от него нужен только рестарт демона LLE на новом коде, и просить его надо явно.
- `.github/workflows/**` и `agent-protocol.json` — писать dev-core может, **мёржить — кнопка john** (гард 4, шире зон).
- Реестр/деньги (вариант 2) — граница «к john» прямо.

## Что уже известно про измерение и во что не надо упираться повторно

Отказы дед-ман-пинга контура aco (`the dead-man ping was NOT delivered: no answer in 5s`) — одиночные, строго чередуются с восстановлением, двух подряд за сутки нет ни одного, и четыре из пяти ложатся РОВНО на тик запуска сессии (замер 2026-08-21, тред 017). Это корреляция, названная корреляцией: механизм не измерен. При grace 300 s ложным звонком это не грозит. Если на контуре LLE увидите то же — это ожидаемый шум, а не дефект доставки.

## Ход

`waiting-on → dev-core`. Тред открыт до живого алерта, различающего два контура.

## msg-002 · from: dev-core · 2026-08-21 · expects: answer

**Этап 1 сделан: рез работает, артефакт ставится живой `github:`-зависимостью, сторож в нём есть. Тег `agent-protocol-v0.2.0` срезан и отправлен. PR #40 зелёный, метка `review` стоит. Этап 2 — данные для правки LLE ниже, рука не моя. Этап 3 — вопрос к john в конце.**

## Что сделано (PR #40, голова `a3c6289`, `checks` зелёный, метка `review` поставлена на этой голове)

- `scripts/split-package.sh` — воспроизводимый рез: `git subtree split --prefix=<пакет>` плюс двери. Отказы по именам: нет `--tag`; нет каталога или `package.json` по `--prefix`; тег уже существует (**не переносится** — на него мог сослаться чужой pin, перенос это force-операция и решает её человек); имя тега разошлось с версией пакета.
- **Схема имён тегов — `<name>-v<version>`, где версия берётся из `package.json` пакета НА СРЕЗАЕМОЙ РЕВИЗИИ, и скрипт отказывает при расхождении.** Практическое следствие, названное в доке: каждая доставка начинается с бампа `version` в `packages/agent-protocol/package.json`, иначе следующий рез упрётся в существующий тег.
- **Взята линия `v0.2.x`, а не продолжение `v0.1.x`.** Довод: `v0.1.1` лежит на дореезданой линии (общего предка с `main` нет — твой замер), и раскладка у неё совпадает с новой случайно, а не по правилу. `v0.2.0` — первая, срезанная скриптом с сегодняшней `main`. Версия пакета поднята `0.1.0` → `0.2.0` тем же коммитом.
- Тест — процессный (`src/release/split-package.process.test.ts`, 6 случаев): поднимает временный репозиторий формы «воркспейс + пакет под префиксом», режет, и спрашивает git, что **в корне тега лежит package.json пакета**, а корень воркспейса (`tsconfig.base.json`, `packages/`) с ним НЕ уехал; каждый отказ проверен по тексту. Юнита на разборе аргументов нет намеренно: он сказал бы, что флаги прочитаны, и промолчал бы ровно о том, ради чего скрипт существует.
- Доки тем же коммитом: раздел «Доставка пакета наружу: тег-срез, а не `main`» в корневом README (там же готовая строка пина и что получает потребитель) и правка «discipline of portability» в README пакета — там с самого начала стояло «переезд обязан быть механическим (`git subtree split` …)», теперь это скрипт, а не намерение.

## Рез, как он прошёл (команда и её вывод)

```
$ bash scripts/split-package.sh --tag agent-protocol-v0.2.0 --push
split-package.sh: режу 'packages/agent-protocol' с HEAD (a3c62890ca6d98041899e6bf8aa7942a80405b6e) → тег 'agent-protocol-v0.2.0' (пакет agent-protocol@0.2.0)
split-package.sh: срез b265199919811c94644ae467e4f8083ad6bee371, верхний уровень его дерева:
  README.md
  package.json
  spike
  src
  tsconfig.json
  vitest.config.ts
split-package.sh: тег 'agent-protocol-v0.2.0' создан на b265199919811c94644ae467e4f8083ad6bee371
 * [new tag]         agent-protocol-v0.2.0 -> agent-protocol-v0.2.0
split-package.sh: тег 'agent-protocol-v0.2.0' отправлен на 'origin'
```

**Оговорка, которую надо знать:** тег срезан с головы ВЕТКИ PR #40, а не с `main`, потому что бамп версии до 0.2.0 живёт в этом PR. Дерево артефакта отличается от того, что дал бы рез с `main`, ровно одним полем — `version` в package.json. Если #40 не будет смёржен, тег останется указывающим на содержимое неслитой ветки; после merge тот же рез с `main` даст то же дерево.

## Прогон установки — ядро приёмки. Чистый каталог, ЖИВОЙ `github:`-пин, pnpm (тот же менеджер, что в LLE)

```
$ cat package.json
{ "dependencies": { "agent-protocol": "github:language-learning-ecosystem-lle/agent-crew-orchestrator#agent-protocol-v0.2.0" },
  "devDependencies": { "tsx": "^4.23.1" } }
$ pnpm install --ignore-workspace
+ agent-protocol 0.2.0
+ tsx 4.23.12

$ ls node_modules/agent-protocol
package.json  README.md  spike  src  tsconfig.json  vitest.config.ts

$ ls -l node_modules/agent-protocol/src/orchestrator/watchdog.ts
-rw-rw-r-- 4 lle lle 19481 Aug 21 10:38 node_modules/agent-protocol/src/orchestrator/watchdog.ts

$ node_modules/.bin/tsx node_modules/agent-protocol/src/cli.ts --help
agent-protocol: usage (--ref is required everywhere except 'schema migrate', 'doctor', 'init', 'config set' …)
  agent-protocol config check --ref <ref> [--repo <path>] …
```

**Оба обязательных пункта «Проверяемости» закрыты: путь `node_modules/agent-protocol/src/cli.ts` существует и CLI стартует; `node_modules/agent-protocol/src/orchestrator/watchdog.ts` в установленном дереве ПРИСУТСТВУЕТ.**

Про exit code честно: `--help` завершается кодом 2 — так у этого CLI устроена справка, а не признак сбоя. Что модуль грузится ЦЕЛИКОМ (включая `zod` и схему), доказывает второй запуск: `cli.ts doctor` в каталоге вне git отвечает своим отказом по имени — `agent-protocol: '/tmp/…' is not inside a git repository`, то есть код дошёл до собственной логики.

Тем же заходом прогнал `npm install git+…#<ветка среза>` — ставится так же (`added 2 packages`, второй — `zod`). То есть `"private": true` в манифесте пакета установке из git не мешает ни у npm, ни у pnpm.

## Ответ на главный риск этапа — «не тянет ли пакет что-то из корня воркспейса», в обе стороны

**НЕ тянет из того, что мешало бы запуску:** `dependencies` пакета — ровно один `zod`; ни `workspace:`, ни `catalog:`, ни `pnpm-workspace.yaml` в них не участвуют (проверено grep'ом по манифесту и живой установкой обоими менеджерами). Dev-зависимости пакета (`tsx`, `vitest`, `typescript`, `@types/node`) потребителю не ставятся и не нужны — `tsx` у LLE свой.

**ТЯНЕТ, и это балласт, а не поломка запуска — называю явно:**
- `tsconfig.json` в артефакте продолжает ссылаться на `extends: "../../tsconfig.base.json"`, которого в срезе нет. `tsx` его для запуска не читает — прогон выше это и показывает; `tsc` внутри установленного дерева сломается. **Артефакт предназначен для ЗАПУСКА, не для сборки у потребителя**, и в доке это записано;
- тесты пакета едут внутрь артефакта и запускаться там не могут: они ссылаются на `../../../../node_modules/.bin/tsx` и на файлы корня репозитория (`agent-protocol.json`, `.github/workflows/**`). На потребителя это не влияет — он их не запускает;
- `spike/` едет тоже.

Резать эти три вещи из артефакта я не стал: любая чистка дерева — это уже не «срез пакета», а «сборка релиза», её содержимое надо решать отдельно, а сегодня она ничего не чинит.

## Этап 2 — точные данные для правки LLE (рука не моя)

- **строка пина целиком, готовая к вставке в `dependencies` `/home/lle/projects/language-learning-ecosystem/package.json`:**
  `"agent-protocol": "github:language-learning-ecosystem-lle/agent-crew-orchestrator#agent-protocol-v0.2.0"`
- **путь до CLI НЕ меняется:** `node_modules/agent-protocol/src/cli.ts`. Скрипт `"protocol": "tsx node_modules/agent-protocol/src/cli.ts"` в LLE править НЕ надо;
- после правки нужен `pnpm install` в LLE (обновится `pnpm-lock.yaml`) и рестарт `lle-orchestrator@lle-hetzner.service` — **рука john: юниты и машинный конфиг его**;
- **что обязан сказать баннер демона LLE после рестарта на новом коде** (`src/orchestrator/watchdog.ts`, `describeWatchdog`): строка вида
  `circuit watchdog ON — every tick pings the monitor named by 'HEALTHCHECKS_CIRCUIT_URL_LLE_HETZNER' (value not shown). It proves ONE thing: this process is ticking…`
  Имя ключа собирается из имени инстанса (`lle-hetzner` → суффикс `LLE_HETZNER`), значение URL не печатается никогда. Если вместо этого прозвучит `circuit watchdog OFF — …`, причина будет названа в той же строке: нет такого ключа в файле секретов, либо значение совпало с `HEALTHCHECKS_URL` или с монитором другого инстанса (это отказ по именам, а не молчание).

## Вопрос по этапу 3 — к john через тебя

Этап 1 показал рабочий рез, значит развилка открыта: **автоматизировать рез воркфлоу на релизе или оставить рукой?** Сегодня рез — одна команда с проверками, и цена ручного варианта — «не забыть бампнуть версию и позвать скрипт» на каждой доставке; цена автоматического — правка `.github/workflows/**`, то есть кнопка john на каждый такой PR. Моя рекомендация: **пока оставить рукой** — доставок мало, скрипт отказывает по именам на всех ошибках, которые я умею назвать, а первый же живой прогон покажет, чего в нём не хватает. Открывать этап 3 стоит, когда доставка станет частой.

## Чего НЕ сделано и почему

Живой приёмки (демон LLE на новом коде + алерт, различающий два контура) — нет: она стоит на правке LLE и рестарте демона, а это не моя рука. Тред остаётся открытым до неё, как и записано в постановке.

`waiting-on → curator`: ход по PR #40 — ревьюер по метке, дальше твоя кнопка по пяти гардам (диффа доков власти в нём нет: `scripts/`, `packages/**`, два README); ход по этапу 2 и по вопросу этапа 3 — твой.

## msg-003 · from: reviewer-pr · 2026-08-21 · expects: answer

verdict: approve
pr: 40

Постановка: тред `018-package-delivery-lle` (ветка `comms`), роль автора — `dev-core`. Прочитан целиком:
сообщение curator (постановка, этап 1/2/3) и ответный отчёт dev-core от 2026-08-21T08:47:14Z с числами и
командами. PR закрывает ровно этап 1 (скрипт реза + отчёт руками), этап 2 (данные для LLE) и этап 3
(автоматизация в `.github/workflows/**`) сознательно не в этом PR — согласуется с постановкой, скрытого
расширения или сужения скоупа не нашёл (критерий 3).

Прогон:
- `pnpm typecheck` — зелёный (`packages/agent-protocol`, `packages/transport-telegram`).
- `pnpm exec biome check` по изменённым файлам — зелёный, без замечаний.
- Полный `pnpm test` не повторял — `checks` на голове `a3c6289` зелёный, прогон `32464406443`.
- Точечно прогнал новый файл: `pnpm exec vitest run packages/agent-protocol/src/release/split-package.process.test.ts`
  → `Test Files 1 passed (1)`, `Tests 6 passed (6)` — совпадает с заявленными в треде «6 случаев»
  (критерий 1). Ассерты бьют в заявленное поведение: корень тега = корень пакета (`package.json`,
  `src`), корень воркспейса (`tsconfig.base.json`, `packages/`) в срезе отсутствует, каждый отказ
  проверен по тексту, а не по факту падения (критерий 2, критерий 11).
- Тег `agent-protocol-v0.2.0` реально существует в `origin` (`git ls-remote --tags`), дерево верхнего
  уровня совпадает с тем, что приведено в отчёте треда — заявление «тег срезан и отправлен» подтверждено
  фактом, а не только текстом (критерий 9).

Зоны: `pnpm protocol zones check --ref a3c6289 --role dev-core --base origin/main` →
«5 path(s) of 'dev-core': none under a forbidden prefix» — все изменённые пути зелёные (критерий 4).

Доки власти (критерий 5): дифф не трогает `PROTOCOL.md`, `docs/roles/**`, `REVIEWER.md`,
`agent-protocol.json`, `.github/workflows/**` — только `README.md` (корень и пакета),
`packages/agent-protocol/package.json`, новый тест и новый `scripts/split-package.sh`. Подтверждено
и дверью:

Живой исход, голая форма:
```
pnpm protocol merge-gate --ref origin/main --pr 40
  STOP guard 1 · нет approve на текущей голове (ожидаемо — вердикт ещё не доставлен)
  STOP guard 2 · checks не зелёный на этой голове ('review' сейчас IN_PROGRESS — это текущий прогон
       ревьюера; отдельно нота про сдвиг base ПОСЛЕ старта checks — не относится к этому PR)
  guard 3 (you) · восхождение к решению john — тред 018 назван
  ok   guard 4 · доков власти среди 5 путей нет
  guard 5 (you) · след слияния — впишет тот, кто мёржит
  ok   mergeability · MERGEABLE (mergeStateStatus UNSTABLE)
REFUSED: a guard does not hold
```
Живой исход, с флагом (маршрут merge называется в этом вердикте, поэтому оба вывода):
```
pnpm protocol merge-gate --ref origin/main --pr 40 --power-docs PROTOCOL.md,REVIEWER.md,.github/workflows
  (тот же результат: guard 4 — ok, доков власти нет)
REFUSED: a guard does not hold
```
Guards 1/2/5 держит не PR, а момент запроса (approve ещё не доставлен, `review` ещё выполняется, след
слияния впишет мёржащий) — это не находка о диффе. Guard 4 подтверждает: PR не трогает доки власти,
исключение критерия 5 (merge только за john) не применяется.

Находок по критериям 1–11 нет.

## msg-004 · from: github · 2026-08-21 · expects: none

PR #40 (feat(release): рез подпакета в тег с корнем-пакетом (тред 018)) **merged** by maysway → `main`.

## msg-005 · from: curator · 2026-08-21 · expects: ack

**Требуется решение john — два вопроса. (1) Ставить ли пин `agent-protocol-v0.2.0` в LLE прямо сейчас: тег несёт сторож контура, но отстаёт от `main` на один коммит (`2e62164`, notify треда 016; сторожа он НЕ касается — сверено деревом), альтернатива — дождаться среза 0.2.1 с текущей `main` и править LLE один раз. (2) Этап 3 — воркфлоу, режущий тег на релизе: открывать или оставить рез рукой? Мои рекомендации: ставить сейчас; этап 3 пока не открывать.**

Этап 1 принят, PR #40 смёржен. Ниже — след слияния с поимёнными гардами, потом моя собственная проверка сверх отчёта dev-core (она нашла факт, которого в отчёте нет), потом точные данные для руки john.

## Merge PR #40 — след, гарды поимённо

Смёржен squash в `main` 2026-08-21T08:54:08Z, коммит слияния `b324122eea24add20666df72e81b3cc7a7aeb3e4`. Контрольное чтение цели после merge: `base.ref` = `main`, `state` = `MERGED` (`gh pr view 40`).

- **гард 1 — вердикт `approve` на текущей голове.** Голова PR — `a3c62890ca6d98041899e6bf8aa7942a80405b6e`. Вердикт ревьюера: сообщение этого треда `msg-003 · from: reviewer-pr` (`verdict: approve`, `pr: 40`) плюс тот же approve в PR; дверь прочла его как `approved on a3c6289 by github-actions`. Исключение по `claude-review.yml` здесь не применяется — PR его не трогает;
- **гард 2 — зелёные `checks` на ТОЙ ЖЕ голове** `a3c62890ca6d98041899e6bf8aa7942a80405b6e`: `checks=SUCCESS` (прогон `32464406443`), `review=SUCCESS` (прогон `32464692135`). Полные SHA сверяла не глазом — дверью;
- **гард 3 — восхождение к решению john.** Тред `018-package-delivery-lle`, в описании PR стоит `thread: 018-package-delivery-lle`. Решение john — тред `017-circuit-watchdog`, сообщение `2026-08-21T08-27-37Z-curator.md`, дословно процитировано в постановке (`msg-001`): «различение двух контуров и доставка сторожа в LLE-пин — новым тредом задачей dev-core, твоя рекомендация формы (вариант 1, `git subtree split` подпакетом) принята как отправная — заводи постановку». Постановка моя, но решение о работе — john, с названным источником; круг «сам поставил → сам принял» не замыкается;
- **гард 4 — доки власти.** Дифф — 5 путей: `scripts/split-package.sh`, `packages/agent-protocol/**` (пакет + новый тест), два `README.md`. Дверь с `--power-docs PROTOCOL.md,REVIEWER.md,.github/workflows`: `ok guard 4 · 5 changed path(s), none of them a document of power`;
- **гард 5 — след.** Это он.

Полный вывод двери перед merge: `ok` на 1, 2, 4, `mergeable=MERGEABLE (CLEAN)`, `nothing in the facts forbids this merge`.

**Ноту двери передаю как есть, решением она не была:** «base moved AFTER the credited checks started: `2e62164` committed 08:47:05Z, `checks` started 08:42:28Z». То есть зелёный гарда 2 снят с дерева, в котором `2e62164` ещё не было. Merge я всё равно исполнила: `mergeStateStatus` = CLEAN, дифф `2e62164` (notify) и дифф #40 (скрипт реза + версия пакета + README) не пересекаются ни одним файлом — сверено. Но эта же нота оказалась входом в находку ниже.

## Что я проверила своей рукой сверх отчёта — и что нашлось

Отчёт dev-core говорит: «после merge тот же рез с `main` даст то же дерево». Я это не приняла на слово и прогнала рез сама, уже после merge:

```
$ git subtree split --prefix=packages/agent-protocol origin/main
0d2f65668ad1d2be820ed0814eeb4d1f7ff7cb1f
$ git rev-parse agent-protocol-v0.2.0^{tree}   → 3bbde781ccb0e2235779518bdba06710e6b6123b
$ git rev-parse 0d2f6566^{tree}                → 76501eeb3df2ee3757a266f8288c8ebcb9689183
$ git diff --stat agent-protocol-v0.2.0 0d2f6566
 13 files changed, 589 insertions(+), 46 deletions(-)
```

**Деревья НЕ совпали.** Причина названа командой, а не догадкой:

```
$ git log --oneline a3c6289..origin/main
b324122 feat(release): рез подпакета в тег с корнем-пакетом (тред 018) (#40)
2e62164 fix(notify): закрытый тред уходит из шестой категории; метка кадра — призыв, а не история (тред 016)
```

`2e62164` лёг на `main` в 08:47:05Z — за девять секунд до того, как dev-core отправил отчёт. То есть утверждение было неверным уже в момент написания, и не по небрежности: `main` уехала под ним. Разошлись ровно файлы этого коммита — `notify/**`, `status.ts`, `thaw.ts`, `tui.ts`, `index-doc.ts`, `cli.ts` и их тесты.

**Что при этом ПОДТВЕРДИЛОСЬ, и это главное для задачи:**

- **срез верен своему источнику:** дерево тега `3bbde781…` побайтно равно `a3c6289:packages/agent-protocol` (`git rev-parse` обоих даёт один SHA). Скрипт режет то, что обещает;
- **сторож в теге — тот самый, сегодняшний:** `git diff agent-protocol-v0.2.0 0d2f6566 -- src/orchestrator/watchdog.ts` пуст. Ключ по имени инстанса (`a97fb19`, тред 017) в теге ЕСТЬ: `git grep` по файлу тега даёт `HEALTHCHECKS_CIRCUIT_URL_<INSTANCE>` и прямым текстом `lle-hetzner → HEALTHCHECKS_CIRCUIT_URL_LLE_HETZNER`;
- **баннер проверила в файле тега, а не по пересказу** (`describeWatchdog`, строки 247–250): `circuit watchdog ON — every tick pings the monitor named by '<key>' (value not shown)…`, и при отказе — `circuit watchdog OFF — <причина>`. Совпадает с тем, что назвал dev-core;
- версия в манифесте тега — `agent-protocol@0.2.0`, `"private": true` (установке из git не мешает — это dev-core показал живой установкой обоими менеджерами).

**Итого чего LLE НЕ получит на пине `v0.2.0`:** поведения треда 016 в notify — закрытый тред продолжит попадать в шестую категорию, метка кадра останется историей, а не призывом. Сторожа, алерта и различения двух контуров это не касается ничем.

## Вопрос 1 — ставить пин сейчас или дождаться 0.2.1

**Моя рекомендация: ставить `v0.2.0` сейчас.** Довод: демон LLE сегодня умирает молча, а тег несёт ровно тот файл, ради которого тред заведён, — сверено выше деревом, не текстом. Цена ожидания — ещё один круг dev-core (бамп версии → PR → ревью → merge → рез), и всё это время LLE без сторожа. Пин у LLE сейчас — `v0.1.1` от 2026-08-18, так что `v0.2.0` в любом случае большой шаг вперёд, а не полшага. Отставание на `2e62164` закроется следующей доставкой, которая всё равно понадобится.

**Довод против, честно:** правка `package.json` LLE + `pnpm install` + рестарт — твоя рука, и делать её дважды хуже, чем один раз. Если для тебя эта цена весомее, чем сутки-двое молчащего демона, — говори, и я ставлю dev-core бамп до `0.2.1` и рез с текущей `main` отдельным ходом в этом треде.

## Вопрос 2 — этап 3 (автоматизация реза)

**Моя рекомендация совпадает с рекомендацией dev-core: пока оставить рукой.** Доставка сегодня первая, второго живого прогона у скрипта ещё не было, и чего в нём не хватает, покажет именно он.

**Одну цифру в доводе dev-core поправлю, чтобы решение стояло на верной арифметике.** dev-core написал, что цена автоматизации — «кнопка john на каждый такой PR». Это не так: кнопка john нужна ОДИН раз, чтобы приземлить сам воркфлоу (и потом — только когда правится он сам). Сами резы после этого идут без руки. То есть автоматизация дешевле, чем прозвучало; я всё равно рекомендую подождать, но по другой причине — не по цене кнопки, а потому что автоматизировать процедуру, у которой был один прогон, рано.

## Этап 2 — что нужно от твоей руки, точными строками

Всё ниже — вне зон ролей этого контура (чужой репозиторий, systemd, секреты), поэтому рука твоя.

1. В `/home/lle/projects/language-learning-ecosystem/package.json`, в `dependencies`, строка целиком:
   `"agent-protocol": "github:language-learning-ecosystem-lle/agent-crew-orchestrator#agent-protocol-v0.2.0"`
2. `pnpm install` в LLE (обновится `pnpm-lock.yaml`);
3. Скрипт `"protocol": "tsx node_modules/agent-protocol/src/cli.ts"` править НЕ надо — путь до CLI не меняется (раскладка подпакета его сохраняет; проверено установкой в отчёте dev-core и раскладкой дерева тега здесь);
4. рестарт `lle-orchestrator@lle-hetzner.service`;
5. **Приёмка баннером:** в журнале демона должна появиться строка `circuit watchdog ON — every tick pings the monitor named by 'HEALTHCHECKS_CIRCUIT_URL_LLE_HETZNER' (value not shown)…`. Если прозвучит `circuit watchdog OFF — …`, причина будет названа в той же строке (нет ключа в файле секретов; либо значение совпало с `HEALTHCHECKS_URL` или с монитором другого инстанса — это отказ по именам, а не молчание);
6. **Живая приёмка, ради которой тред заведён:** остановить один демон и убедиться, что звонит ровно ЕГО монитор, а второй продолжает биться.

Готова принять шаги 5–6 по журналу и по факту звонка, как только ты их прогонишь.

## Ход

`waiting-on → curator`, `parked-on john`. Тред остаётся открытым до живого алерта, различающего два контура. Открытых PR этого треда больше нет (`gh pr list --state open` после merge — пусто).

## msg-006 · from: dev-core · 2026-08-21 · expects: answer

**Свой замер после merge #40 нашёл дефект в артефакте: тег `agent-protocol-v0.2.0` уехал наружу БЕЗ уже слитой в `main` работы треда 016. Пин на него давать LLE нельзя. Причина — молчащая дверь в моём же скрипте; PR #41 её чинит и поднимает версию до `0.2.1`. Строка пина для этапа 2 меняется: `#agent-protocol-v0.2.1`.**

## Что измерено (команды, не память)

Тег `v0.2.0` срезан с головы ветки PR #40 (`a3c6289`), чья база — `06dcf68`. Пока PR ждал ревью, в `main` уехал `2e62164` (тред 016). Сравнение дерева тега с срезом сегодняшней `main`:

```
$ git subtree split --prefix=packages/agent-protocol origin/main   → 0d2f656
$ git diff --stat agent-protocol-v0.2.0^{tree} 0d2f656^{tree}
 13 files changed, 589 insertions(+), 46 deletions(-)
   README.md, src/cli.ts, src/notify/notify.ts(+test), src/orchestrator/{snapshot,status,thaw,tui}.ts(+tests),
   src/orchestrator/exhausted-closed.process.test.ts, src/thread/index-doc.ts, src/thread/thread.test.ts
```

**Недостающее — ровно один коммит `2e62164` и ничего кроме** (сверено с `git show --stat 2e62164`: те же 13 файлов пакета плюс `docs/protocol-reference.md`, которого в срезе нет по определению). То есть артефакт рабочий и сторож в нём есть — но потребитель получил бы код без исправления шестой категории уведомителя.

Это ровно та оговорка, которую я назвал в msg-002 («тег срезан с головы ветки, а не с `main`»), и я её недооценил: я написал «дерево отличается ровно одним полем `version`» — **это было верно на момент реза и перестало быть верным к моменту merge**, потому что база ветки за это время отстала. Утверждение о будущем дереве я подал как факт; факт был только про тот момент.

## Что сделано — PR #41, голова `ad0c250`

- **Дверь в `scripts/split-package.sh`: `--base <ref>` (по умолчанию `origin/main`).** Срезаемая ревизия не несёт эту линию → скрипт перечисляет недостающие коммиты, трогающие `--prefix`, **по именам**, и отказывает. `--allow-behind` превращает отказ в такое же громкое предупреждение (сознательный рез с отставшей линии остаётся возможным). Коммиты линии, не трогающие пакет, отказом не считаются — про них отдельная строка. Базы нет в чекауте вовсе → сказано вслух, что проверка ПРОПУЩЕНА, а не проглочено молча.
  Старая версия скрипта проверяла раскладку среза и согласие имени тега с версией — и молчала ровно о том, что сегодня сломалось. Дверь, которая молчит, хуже отсутствующей, тем более что рез идёт рукой на каждом бампе.
- **Версия пакета `0.2.0` → `0.2.1`.** `v0.2.0` НЕ переносится (перенос тега — force-операция, рука человека) и просто перестаёт быть тем, на что пинится LLE.
- Тест — тот же процессный файл, **+4 случая на живых временных репозиториях**: отказ с именем недостающего коммита и БЕЗ оставленного тега; рез под `--allow-behind` с тем же списком; движение линии мимо пакета (резу не мешает); отсутствующая база. Итого 10, локально зелёные. Помощник запуска переведён на `spawnSync`: предупреждение идёт в stderr и обязано попадать в разбор.
- Доки тем же коммитом: пункт про «срез несёт всю линию» в разделе «Доставка пакета наружу» корневого README, там же обновлённая форма команды.
- Локально: `pnpm typecheck` зелёный, `biome check` по изменённым файлам зелёный, `vitest run …/split-package.process.test.ts` → 10/10.

## Проверка будущего артефакта — сделана до отправки PR, тегом, который я НЕ оставил

Срезал `agent-protocol-v0.2.1` с головы `ad0c250` **локально, без `--push`**, и сверил дерево:

```
$ git diff --stat <срез ad0c250>^{tree} <срез origin/main>^{tree}
 package.json                              |  2 +-      (version 0.2.1 против 0.2.0)
 src/release/split-package.process.test.ts | 94 +--      (новые случаи этого PR)
$ git cat-file -e <срез>:src/orchestrator/watchdog.ts   → present
```

Ничего из `main` в артефакте не потеряно — расхождение ровно на этот PR. **Локальный тег я после сверки удалил**: пока PR не смёржен, любая правка головы сделала бы его лгущим, а перенести его уже нельзя. Отправлять его в `origin` до merge я не стал сознательно — это ровно та ошибка, которую чинит этот PR.

## Что осталось и чья рука

1. **Метка `review` на #41 не поставлена: `checks` на голове `ad0c250` в 09:02Z был `in_progress`** (прогон `32465857582`). У конвейера не стою — ставить метку надо на зелёной голове. Прошу поставить того, кто увидит зелёный: `gh pr edit 41 --add-label review`.
2. **После merge #41 — один рез, из чекаута со свежим `origin/main`:**
   `git fetch origin && bash scripts/split-package.sh --tag agent-protocol-v0.2.1 --ref origin/main --push`
   Новая дверь сама откажет, если чекаут окажется отставшим. Это рука dev-core (следующая сессия по этому треду) — скажи, если хочешь сделать сам.
3. **Этап 2, обновлённые данные для LLE** (остальное из msg-002 без изменений — путь `node_modules/agent-protocol/src/cli.ts` не меняется, скрипт `protocol` в LLE править не надо, баннер обязан сказать `circuit watchdog ON … HEALTHCHECKS_CIRCUIT_URL_LLE_HETZNER`):
   `"agent-protocol": "github:language-learning-ecosystem-lle/agent-crew-orchestrator#agent-protocol-v0.2.1"`
   **Строку из msg-002 (`#agent-protocol-v0.2.0`) в LLE не вставлять.**
4. Вопрос по этапу 3 (автоматизировать рез воркфлоу или оставить рукой) из msg-002 остаётся открытым и ждёт john через тебя. Сегодняшний дефект — довод в обе стороны, и это честнее сказать прямо: воркфлоу, режущий тег ПОСЛЕ merge, этой ошибки не сделал бы по построению; но ровно тот же результат даёт дверь `--base`, а она уже есть и кнопки john не стоит. Рекомендацию не меняю: пока рукой.

## Чего НЕ сделано

Живой приёмки (демон LLE на новом коде + алерт, различающий два контура) — по-прежнему нет: она стоит на правке LLE и рестарте `lle-orchestrator@lle-hetzner.service`, а это рука john. Тред открыт до неё.

`waiting-on → curator`: метка `review` на зелёной голове #41 и дальше кнопка по пяти гардам (доков власти в диффе нет: `scripts/`, `packages/agent-protocol/**`, корневой `README.md`); ход по этапу 2 и вопросу этапа 3 — твой.

## msg-007 · from: curator · 2026-08-21 · expects: none

доставка решений john (чат ~09:04Z) — парковка msg `08-57-22Z` снята, оба вопроса, обе твои рекомендации приняты дословно:

1. **Вопрос 1 — «СТАВИМ v0.2.0 СЕЙЧАС»**: пин на `agent-protocol-v0.2.0` без ожидания 0.2.1; довод john = твой (демон LLE молчит сегодня, тег несёт лекарство сверенным деревом; нотифай-хвост `2e62164` доедет следующей рутинной доставкой). Этап 2 — руки john по твоим шести строкам — исполнится его ноутным заходом (плюс там же: батч 091 и /wake-такт 087); приёмки 5–6 за тобой по журналу и факту звонка;
2. **Вопрос 2 — «ЭТАП 3 ПОКА НЕ ОТКРЫВАЕМ»**: рез остаётся рукой до второго живого прогона скрипта; твоя поправка арифметики (кнопка одна на воркфлоу, не по кнопке на рез) зафиксирована в ленте — при будущем открытии этапа 3 довод стоит на верных числах.

waiting-on → curator (тред держишь до живого алерта двух контуров, как и записано).

## msg-008 · from: reviewer-pr · 2026-08-21 · expects: answer

verdict: approve
pr: 41

Постановка: тред `018-package-delivery-lle` (ветка `comms`), роль автора — `dev-core`. Прочитан
целиком: постановка curator (msg-001), отчёт dev-core по PR #40 (msg-002), вердикт reviewer-pr по
#40 (msg-003), merge-нота curator с находкой дефекта (msg-005), и разбор дефекта dev-core с отчётом
по этому PR (msg-006). PR #41 — самостоятельно найденный dev-core дефект после merge #40 (тег
`v0.2.0` срезан с головы ветки, чья база `06dcf68` отстала от `main` на `2e62164`), чинится внутри
уже авторизованного треда 018, без нового восхождения к john — это продолжение той же задачи, а не
новый скоуп (критерий 3).

Прогон:
- `pnpm typecheck` — зелёный (`packages/agent-protocol`, `packages/transport-telegram`).
- `pnpm exec biome check` по изменённым файлам (`README.md`, `packages/agent-protocol/package.json`,
  `packages/agent-protocol/src/release/split-package.process.test.ts`, `scripts/split-package.sh`) —
  зелёный, без замечаний.
- Полный прогон тестов не повторял — `checks` на голове `ad0c2501cb41640f1527e543eebd21887b25e94e`
  зелёный, прогон `32465857582`.
- Точечно прогнал изменённый файл: `pnpm exec vitest run packages/agent-protocol/src/release/split-package.process.test.ts`
  → `Test Files 1 passed (1)`, `Tests 10 passed (10)` — совпадает с заявленными в треде «Итого 10»
  (6 старых + 4 новых, проверено `grep -c '  it('` → 10) (критерий 1).
- Ассерты новых случаев бьют в заявленное поведение, а не в соседнее (критерий 2): отказ проверен по
  тексту («НЕ несёт линию» + имя недостающего коммита) и по факту, что тег НЕ создан; `--allow-behind`
  проверен и по тексту предупреждения, и по дереву (файл, которого не хватает, реально отсутствует в
  срезанном теге); движение линии мимо `--prefix` проверено текстом «те коммиты не трогают» и тем, что
  рез всё равно состоялся; отсутствие `--base` в репозитории — текстом «ПРОПУЩЕНА». Прочитал сам скрипт
  (`scripts/split-package.sh`): логика `git merge-base --is-ancestor "$base" "$resolved"` +
  `git log "$resolved..$base_resolved" -- "$prefix"` корректно вычисляет ровно «что из линии `--base`
  не попадёт в срез и трогает ли это пакет» — молчания в духе критерия 11 не нашёл: и ветка «полный
  порядок», и ветка «ветка не тронута», и ветка «базы нет» — каждая печатает разный текст.
- Числа: тег `v0.2.0` действительно отставал ровно на 13 файлов / 1 коммит (`2e62164`) — это факт из
  merge-ноты curator (msg-005), не проверял заново живым `git subtree split`, доверяю измерению
  curator по её же дереву-сравнению, воспроизведённому dev-core независимо (msg-006) — оба замера
  совпали на 13 файлах.

Зоны: `pnpm protocol zones check --ref ad0c2501cb41640f1527e543eebd21887b25e94e --role dev-core --base origin/main`
→ «4 path(s) of 'dev-core': none under a forbidden prefix» — все 4 изменённых пути зелёные (критерий 4).

Доки власти (критерий 5): дифф — `README.md`, `packages/agent-protocol/package.json`,
`packages/agent-protocol/src/release/split-package.process.test.ts`, `scripts/split-package.sh`; ни
один не входит в `PROTOCOL.md`, `docs/roles/**`, `REVIEWER.md`, `agent-protocol.json`,
`.github/workflows/**`. Подтверждено дверью (маршрут merge называется ниже, поэтому оба вывода):

Живой исход, голая форма:
```
pnpm protocol merge-gate --ref origin/main --pr 41
  STOP guard 1 · нет approve на текущей голове (ожидаемо — вердикт ещё не доставлен)
  STOP guard 2 · checks не зелёный на этой голове (review=IN_PROGRESS — текущий прогон ревьюера)
  guard 3 (you) · восхождение к решению john — тред 018 назван
  ok   guard 4 · доков власти среди 4 путей нет
  guard 5 (you) · след слияния — впишет тот, кто мёржит
  mergeable=MERGEABLE (mergeStateStatus UNSTABLE)
REFUSED: a guard does not hold
```
Живой исход, с флагом:
```
pnpm protocol merge-gate --ref origin/main --pr 41 --power-docs PROTOCOL.md,REVIEWER.md,.github/workflows
  (тот же результат: guard 4 — ok, доков власти нет)
REFUSED: a guard does not hold
```
Guards 1/2/5 держит момент запроса (approve ещё не доставлен, review ещё выполняется, след слияния
впишет мёржащий) — не находка о диффе. Guard 4 подтверждает: исключение критерия 5 (merge только за
john) не применяется — мёржить может curator.

Находок по критериям 1–11 нет.

## msg-009 · from: github · 2026-08-21 · expects: none

PR #41 (fix(release): срез обязан нести всю линию main — отказ по именам недостающих коммитов (тред 018)) **merged** by maysway → `main`.

## msg-010 · from: curator · 2026-08-21 · expects: answer

**Решение john от 09:04Z СТОИТ — msg `09-03-30Z-dev-core` его не отменяет: нового факта о содержимом `v0.2.0` там нет, ровно тот факт (тег без `2e62164`) john и взвесил, принимая «ставим сейчас». Но строку пина выбирает момент твоего ноутного захода, и обе строки верные — подробности первым разделом.**

**PR #41 смёржен** — метка `review` легла на зелёную голову, вердикт `approve` приехал, гарды названы поимённо ниже. Дверь `--base` я не приняла по описанию: прогнала сама на сегодняшнем случае, и по дороге поймала собственный промах в постановке пробы — называю и его.

## Про пин: две верные строки, и никакого ожидания

dev-core написал «пин на `v0.2.0` давать LLE нельзя» и «строку из msg-002 не вставлять». Это его суждение, и оно разошлось с уже принятым решением john — а в одном треде две противоречащие инструкции про строку пина опаснее любой из них. Развожу по фактам:

- **чего `v0.2.0` не несёт — ровно `2e62164` (notify треда 016), и ничего кроме.** Это сверено дважды: моей рукой (msg `08-57-22Z`) и замером dev-core. Нового факта msg `09-03-30Z` про АРТЕФАКТ не добавил;
- **этот самый факт стоял в вопросе, на который john ответил** («нотифай-хвост доедет следующей рутинной доставкой» — его слова в доставке 09:04Z). Решение принято знающим;
- **сторож — тот самый:** `git diff agent-protocol-v0.2.0 <срез main> -- src/orchestrator/watchdog.ts` пуст, ключ `HEALTHCHECKS_CIRCUIT_URL_LLE_HETZNER` в теге есть. `v0.2.0` лечит то, ради чего тред заведён.

**Что ИЗМЕНИЛОСЬ с момента твоего решения и потому стоит одной строки:** починка уехала в PR #41, он одобряемый и минутах от merge, а рез `v0.2.1` — одна команда dev-core после него. Поэтому:

- **если твой ноутный заход СЕЙЧАС** — вставляй `#agent-protocol-v0.2.0`, как решено. Ждать нечего;
- **если он ПОСЛЕ того, как в треде прозвучит «тег `agent-protocol-v0.2.1` отправлен»** — вставляй `#agent-protocol-v0.2.1`: он строго больше (тот же сторож + `2e62164` + дверь реза), и вторая правка LLE тогда не понадобится вовсе.

Ни в одном из двух случаев ждать не надо — это не новая развилка, а выбор строки по часам. Остальные пять шагов этапа 2 из msg `08-57-22Z` без изменений: путь `node_modules/agent-protocol/src/cli.ts` не меняется, скрипт `protocol` в LLE не править, `pnpm install`, рестарт `lle-orchestrator@lle-hetzner.service`, приёмка баннером `circuit watchdog ON — … 'HEALTHCHECKS_CIRCUIT_URL_LLE_HETZNER'`.

Этап 3 закрыт твоим решением («пока не открываем») — к нему не возвращаюсь.

## Метка `review` на #41 — на зелёной голове, поимённо

Поставлена 2026-08-21T09:05Z, `gh pr edit 41 --add-label review`, контрольное чтение — метка на PR одна: `review`.

- голова #41 — `ad0c2501cb41640f1527e543eebd21887b25e94e`;
- `checks` = **success**, прогон `32465857582`. `head_sha` прогона сверен не глазом, а API: `gh api …/actions/runs/32465857582 --jq '{head_sha,conclusion}'` → тот же полный SHA, `conclusion: success`. То есть порядок соблюдён — метка легла на уже готовый зелёный прогон, а не поперёк него.

## Что я проверила своей рукой сверх отчёта dev-core

Дверь `--base` я не приняла по описанию, а прогнала — новым скриптом (голова `ad0c250`) на РЕАЛЬНОМ сегодняшнем случае: ревизия, с которой резался `v0.2.0`, против сегодняшней `main`.

- **отказ состоялся и назвал недостающее по именам** (exit 1): `b324122` и `2e62164` — обе строки, и обе трогают `packages/agent-protocol`. **Тега после отказа не осталось** (`git tag -l` пуст) — то есть отказ не оставляет полуфабриката;
- **`--allow-behind`** — то же перечисление громким предупреждением, рез идёт, тег создаётся (exit 0);
- **базы нет в чекауте** — сказано вслух: «проверка «срез несёт всю линию» ПРОПУЩЕНА», рез идёт. Молчаливого проглатывания нет.

**Свой промах называю, потому что он ровно того класса, который я ловлю у других.** Первый прогон пробы дал «дверь не сработала», и это было бы находкой — если бы я на ней остановилась. Проверила постановку: во временном клоне `origin/main` стоял на `06dcf68` (клон тянул локальную ветку `main`, а не `origin/main` рабочего репозитория), то есть проба сравнивала ревизию с ОТСТАВШЕЙ базой — дверь молчала правильно. После `git fetch origin '+refs/remotes/origin/main:refs/remotes/origin/main'` отказ состоялся. Диагноз был мой и не пережил первой проверки; в тред он едет уже проверенным.

**Проверила и будущий артефакт — то, чем он станет после merge:**

```
$ bash scripts/split-package.sh --tag agent-protocol-v0.2.1 --ref ad0c250 --base origin/main   → EXIT 0
$ git diff --stat <срез origin/main> <срез ad0c250>
 package.json                              |  2 +-
 src/release/split-package.process.test.ts | 94 ++++++++++++++++---
$ git cat-file -e <срез ad0c250>:src/orchestrator/watchdog.ts   → present
```

Дверь на самой голове #41 молчит — линия несётся; из `main` в артефакте не потеряно ничего, расхождение ровно на этот PR. Заявление dev-core «ничего из `main` не потеряно» подтверждено моей рукой, а не принято на слово. Резал во временном клоне, тег локальный и уехал вместе с клоном — в `origin` ничего не отправлялось.

**Одна ноздря, не блокер:** под `--allow-behind` предупреждение дословно повторяет текст отказа, включая хвост «или скажи `--allow-behind`» — совет, который уже выполнен. Читается как «сделай то, что ты сделал». Правка косметическая, merge из-за неё не держу; если dev-core будет трогать этот файл следующим ходом — пусть подрежет заодно.

## Merge PR #41 — след, гарды поимённо

Смёржен squash в `main` 2026-08-21T09:09:34Z, коммит слияния `28a3b1097dbb7f053b71fedfc4a600c537f795b2`. Контрольное чтение цели после merge: `base.ref` = `main`, `state` = `MERGED` (`gh pr view 41`).

- **гард 1 — вердикт `approve` на ТЕКУЩЕЙ голове.** Голова #41 — `ad0c2501cb41640f1527e543eebd21887b25e94e`. Вердикт: сообщение этого треда `2026-08-21T09-08-26Z-reviewer-pr.md` (`verdict: approve`, `pr: 41`, находок по критериям 1–11 нет) и тот же approve на PR (`github-actions`, `APPROVED`, 09:08:40Z). Дверь прочла его как `approved on ad0c250 by github-actions`. Исключение по `claude-review.yml` не применяется — PR его не трогает;
- **гард 2 — зелёные `checks` на ТОЙ ЖЕ голове** `ad0c2501cb41640f1527e543eebd21887b25e94e`: дверь — `2 check(s) green: review=SUCCESS, checks=SUCCESS` (прогоны `32465857582` и `32466179093`). Полные SHA сверяла дверью и API прогона, не глазом;
- **гард 3 — восхождение к решению john.** В описании PR — `thread: 018-package-delivery-lle`. Решение john о работе: тред `017-circuit-watchdog`, сообщение `2026-08-21T08-27-37Z-curator.md` (процитировано в постановке `msg-001`), плюс доставка решений john в этом треде — `2026-08-21T09-04-50Z-curator.md`. #41 — не новый скоуп, а починка внутри уже авторизованной задачи; круг «сам поставил → сам принял» не замыкается;
- **гард 4 — доки власти.** Дифф — 4 пути: `scripts/split-package.sh`, `packages/agent-protocol/package.json`, `packages/agent-protocol/src/release/split-package.process.test.ts`, корневой `README.md`. Дверь: `ok guard 4 · 4 changed path(s), none of them a document of power`;
- **гард 5 — след.** Это он.

Полный вывод двери перед merge: `ok` на 1, 2, 4, `mergeable=MERGEABLE (mergeStateStatus CLEAN)`, `nothing in the facts forbids this merge`. Ноты про сдвиг базы в этот раз нет — база чиста.

Открытых PR этого треда больше нет: `gh pr list --state open` после merge — пусто.

## Ход

`waiting-on → dev-core`. Один шаг, и он разблокирован именно этим merge:

**рез `agent-protocol-v0.2.1` из чекаута со свежим `origin/main`** —
`git fetch origin && bash scripts/split-package.sh --tag agent-protocol-v0.2.1 --ref origin/main --push`
Новая дверь сама откажет, если чекаут окажется отставшим; я её на этом прогнала — отказывает по именам. Отчитайся в тред строкой «тег `agent-protocol-v0.2.1` отправлен» с SHA среза: по ней john берёт строку пина без второго круга. Мой прогон реза был во ВРЕМЕННОМ клоне, тег локальный и уехал с клоном — в `origin` из-под меня ничего не отправлялось, `v0.2.1` за тобой.

Тег `v0.2.0` не переносить и не удалять — перенос тега force-операция, а на него мог уже сослаться пин.

Тред остаётся ОТКРЫТЫМ до живой приёмки, ради которой заведён: демон LLE на новом коде, баннер `circuit watchdog ON — … 'HEALTHCHECKS_CIRCUIT_URL_LLE_HETZNER'`, и алерт, различающий два контура (остановить один демон — звонит ровно его монитор, второй бьётся). Она стоит на руке john и принимается мной по журналу и по факту звонка.

## msg-011 · from: dev-core · 2026-08-21 · expects: answer

**Тег `agent-protocol-v0.2.1` срезан с `origin/main` после merge #41 и отправлен в `origin`. Артефакт проверен не пересказом, а живой установкой из самого удалённого тега: `agent-protocol 0.2.1`, `node_modules/agent-protocol/src/cli.ts` на месте, сторож с ключом по имени инстанса внутри. Строка пина для этапа 2 — `#agent-protocol-v0.2.1`.**

## Что сделано

Чекаут на `28a3b10` (= `origin/main`, голова после merge #41), рабочее дерево чистое:

```
$ bash scripts/split-package.sh --tag agent-protocol-v0.2.1 --ref origin/main --base origin/main
  режу 'packages/agent-protocol' с origin/main (28a3b10…) → тег 'agent-protocol-v0.2.1' (пакет agent-protocol@0.2.1)
  срез b3839dc38a80f35b09cae4c949c134dfea545909, верхний уровень его дерева:
    README.md  package.json  spike  src  tsconfig.json  vitest.config.ts
$ git push origin refs/tags/agent-protocol-v0.2.1   → * [new tag]
```

Новая дверь `--base` молчала — и это её «всё в порядке»: `--ref` и `--base` здесь одна и та же ревизия, отставать нечему.

## Проверка артефакта — четыре замера, каждый командой

1. **Срез верен своему источнику побайтно:** `git rev-parse agent-protocol-v0.2.1^{tree}` и `git rev-parse origin/main:packages/agent-protocol` дают один SHA `1010c4df43d696fd751bf255317074476c53db36`. Тег — это ровно сегодняшняя `main`, а не чья-то ветка.
2. **Дефект из msg-005/msg-006 закрыт.** `git diff --stat agent-protocol-v0.2.0^{tree} agent-protocol-v0.2.1^{tree}` → 15 файлов, и это ровно: 13 файлов коммита `2e62164` (тред 016, тот самый хвост, которого не было в `v0.2.0`), бамп `package.json` `0.2.0`→`0.2.1` и тест PR #41. Ничего лишнего, ничего недостающего. В установленном `src/notify/notify.ts` маркер треда 016 присутствует прямым текстом (`!input.closed.has(view.thread)` — закрытый тред уходит из шестой категории).
3. **Сторож треда 017 в теге:** `HEALTHCHECKS_CIRCUIT_URL_` и прямым текстом `HEALTHCHECKS_CIRCUIT_URL_LLE_HETZNER` в `src/orchestrator/watchdog.ts`. Баннер в файле тега (строки 249–250) — `circuit watchdog ON — every tick pings the monitor named by '<key>' (value not shown)…`, при отказе `circuit watchdog OFF — <причина>`. То есть строка приёмки из msg-002 п.5 остаётся дословно той же.
4. **Живая установка из удалённого тега** — в `mktemp -d`, вне рабочих деревьев, ровно тем менеджером, которым ставит LLE:
   ```
   $ pnpm add "github:language-learning-ecosystem-lle/agent-crew-orchestrator#agent-protocol-v0.2.1"
   + agent-protocol 0.2.1
   ```
   Раскладка потребителя: `node_modules/agent-protocol/{package.json,README.md,spike,src,tsconfig.json,vitest.config.ts}`; `node_modules/agent-protocol/src/cli.ts` **есть** — то есть скрипт `"protocol": "tsx node_modules/agent-protocol/src/cli.ts"` в LLE править по-прежнему НЕ надо. Транзитивная зависимость доехала: `zod@4.4.3` слинкован в пакет (`node_modules/.pnpm/agent-protocol@…/node_modules/zod → ../../zod@4.4.3/…`). `"private": true` установке из git не мешает, как и на `v0.2.0`.

`v0.2.0` не тронут: не перенесён и не удалён (то и другое — force-операция и рука john). Он просто перестаёт быть тем, на что пинится LLE.

## Расхождение, которое надо назвать вслух, а не проглотить

msg-007 доставил решение john «**ставим `v0.2.0` СЕЙЧАС**», а msg-006 говорит «`#agent-protocol-v0.2.0` в LLE не вставлять». Оба верны в своём моменте, и вот арифметика на сейчас:

- решение john принималось, когда `v0.2.1` ещё не существовало, а его довод был про **цену ожидания** — «ещё один круг dev-core, и всё это время LLE без сторожа». Этот круг с тех пор уже пройден и оплачен: john сам смёржил #41 (msg-009), а рез занял минуты;
- значит, цены больше нет. Обе строки стоят ОДНОГО И ТОГО ЖЕ движения руки john, но `v0.2.1` несёт всю `main`, а `v0.2.0` — без треда 016.

**Рекомендую пин `v0.2.1`.** Довод john («не ждать») она не нарушает — ждать уже нечего.

Честная оговорка про уже сделанное: если john на ноутном заходе успел вставить строку `v0.2.0` и прогнать `pnpm install`, то переход на `v0.2.1` — это правка одной строки, повторный `pnpm install` и тот же один рестарт, то есть лишняя, но маленькая рука. Если ещё не успел — вставляется сразу верная строка и лишнего движения нет вовсе.

## Этап 2 — данные для руки john, обновлены только в одной строке

1. `/home/lle/projects/language-learning-ecosystem/package.json`, в `dependencies`:
   `"agent-protocol": "github:language-learning-ecosystem-lle/agent-crew-orchestrator#agent-protocol-v0.2.1"`
2. `pnpm install` в LLE;
3. скрипт `protocol` НЕ править — путь до CLI проверен живой установкой выше;
4. рестарт `lle-orchestrator@lle-hetzner.service`;
5. приёмка баннером: `circuit watchdog ON — every tick pings the monitor named by 'HEALTHCHECKS_CIRCUIT_URL_LLE_HETZNER' (value not shown)…`; `circuit watchdog OFF — …` назовёт причину в той же строке;
6. живая приёмка: остановить один демон и убедиться, что звонит ровно ЕГО монитор, а второй продолжает биться.

## Мелочь для ленты, не дефект

msg-008 описывает дверь `--base` как печатающую текст в том числе на ветке «полный порядок». В коде это не так: когда срез несёт всю линию, дверь молчит. Молчанием это не является — пропуск проверки говорится вслух (`проверка «срез несёт всю линию» ПРОПУЩЕНА`), поэтому тишина однозначно читается как «проверка прошла и держит». Правки не прошу, пишу, чтобы в ленте стояло, как есть.

## Ход

Своей работы по треду у меня больше нет: этап 3 john закрыл до второго живого прогона (msg-007), этап 2 — рука john. Живой приёмки, ради которой тред заведён (алерт, различающий два контура), по-прежнему нет — тред держится открытым до неё.

`waiting-on → curator`: донести до john строку `v0.2.1` и шаги 1–6, принять 5–6 по журналу и факту звонка.

## msg-012 · from: curator · 2026-08-21 · expects: ack

**Требуется рука john: этап 2, строка пина — `#agent-protocol-v0.2.1` (шаги 1–6 ниже дословно). Решения не требуется: развилку «какую строку» твоё же правило от 09:10Z закрыло по часам — тег объявлен, значит `v0.2.1`. Требуется именно рука, потому что у контура LLE сторожа сейчас нет вовсе: в установленном пакете нет даже файла `watchdog.ts`.**

Тег `agent-protocol-v0.2.1` отправлен в `origin` (сообщение dev-core `2026-08-21T09-13-32Z-dev-core.md`). Отчёт я не приняла по описанию — перемерила своей рукой, четыре факта совпали.

## Строка пина: почему `v0.2.1`, и почему это не новая развилка

Твоё решение 09:04Z («ставим `v0.2.0` СЕЙЧАС») стояло на цене ожидания. Моё сообщение `2026-08-21T09-10-46Z-curator.md` записало правило выбора по часам: до объявления тега — `v0.2.0`, после — `v0.2.1`. Тег объявлен в 09:13Z. Довод «не ждать» не нарушается: ждать нечего, тег уже в `origin`.

**Лишней руки тоже нет — проверено фактом, а не предположением:** в `/home/lle/projects/language-learning-ecosystem/package.json` пин по-прежнему `#agent-protocol-v0.1.1`. То есть ноутного захода ещё не было, `v0.2.0` никуда не вставлен, переделывать нечего — сразу вставляется верная строка. Рабочее дерево LLE чистое (`git status --porcelain` пуст), правка одной строки ничем не заблокирована.

## Что я перемерила своей рукой (командой, из `/home/lle/projects/agent-crew-orchestrator` после `git fetch origin --tags`)

1. **Срез верен источнику побайтно:** `git rev-parse agent-protocol-v0.2.1^{tree}` = `git rev-parse origin/main:packages/agent-protocol` = `1010c4df43d696fd751bf255317074476c53db36`. Тег — ровно сегодняшняя `main`, полный SHA сверен, не первые семь символов;
2. **сторож треда 017 внутри:** `git grep -c HEALTHCHECKS_CIRCUIT_URL_LLE_HETZNER agent-protocol-v0.2.1 -- src/orchestrator/watchdog.ts` → `1`;
3. **хвост треда 016 внутри** (тот самый, которого не было в `v0.2.0`): `git grep -c 'closed.has(view.thread)' agent-protocol-v0.2.1 -- src/notify/notify.ts` → `1`;
4. **версия пакета в теге** — `0.2.1`.

Заявления dev-core подтверждены; расхождений с его замерами не нашла.

## Состояние контура LLE прямо сейчас — довод «сегодня, а не потом»

- пин в `package.json` — `#agent-protocol-v0.1.1`; **установлено при этом `agent-protocol` версии `0.1.0`** (`node_modules/agent-protocol/package.json`);
- **файла `node_modules/agent-protocol/src/orchestrator/watchdog.ts` нет вовсе** — сторожа в живом демоне нет, не «выключен», а отсутствует;
- `lle-orchestrator@lle-hetzner.service` — `active`, поднят 2026-08-21T09:07:35Z и тикает на этом коде (журнал живой, тики видны).

## Ловушка приёмки, которую называю ДО того, как ты её встретишь

**Номер версии признаком приёмки НЕ является.** Тег `agent-protocol-v0.1.1` несёт внутри `"version": "0.1.0"` — проверено: `git show agent-protocol-v0.1.1:package.json`. Поэтому «вижу 0.2.1 в `node_modules`» ничего не доказывает и в обе стороны врёт. Признак один — баннер:

```
journalctl --user -u lle-orchestrator@lle-hetzner.service --since "$(date +%H:%M)" | grep -i 'circuit watchdog'
```
Ждём `circuit watchdog ON — every tick pings the monitor named by 'HEALTHCHECKS_CIRCUIT_URL_LLE_HETZNER' (value not shown)…`. Если приедет `circuit watchdog OFF — …`, причина стоит в той же строке — пришли её мне, разберу.

## Этап 2 — шаги 1–6, рука твоя

1. `/home/lle/projects/language-learning-ecosystem/package.json`, в `dependencies`:
   `"agent-protocol": "github:language-learning-ecosystem-lle/agent-crew-orchestrator#agent-protocol-v0.2.1"`
2. `pnpm install` в LLE;
3. скрипт `protocol` **НЕ править** — путь `node_modules/agent-protocol/src/cli.ts` проверен живой установкой dev-core из удалённого тега;
4. рестарт `lle-orchestrator@lle-hetzner.service`;
5. приёмка баннером — команда и обе строки выше;
6. живая приёмка двух контуров: остановить один демон и убедиться, что звонит ровно ЕГО монитор, а второй продолжает биться.

Шаги 5–6 принимаю я — по журналу и по факту звонка; ради этого тред и держится открытым.

## Хвосты, закрытые в этот ход

- **открытых PR по треду нет:** `gh pr list --state open` (репозиторий `agent-crew-orchestrator`) — пусто. Кандидатов на кнопку не осталось, `merge-gate` гонять не по чему;
- этап 3 закрыт твоим решением 09:04Z («пока не открываем») — не возвращаюсь;
- косметическая ноздря из `2026-08-21T09-10-46Z-curator.md` (под `--allow-behind` предупреждение дословно повторяет хвост отказа «или скажи `--allow-behind`») стоит в ленте как есть, отдельной работы под неё не завожу — подрежется, когда dev-core в следующий раз тронет `scripts/split-package.sh`;
- уточнение dev-core про дверь `--base` (на ветке «полный порядок» она молчит, а не печатает) принимаю: молчанием это не является, пропуск проверки говорится вслух. Моя формулировка в `2026-08-21T09-10-46Z-curator.md` была неточна, факт в ленте теперь стоит верный.

`waiting-on → curator`, `parked-on john`: ход держу я, потому что несу вопрос; действие — твоё, шаги 1–6. Как только приедет баннер, снимаю парковку и принимаю 5–6.

## msg-013 · from: curator · 2026-08-21 · expects: none

**ЭТАП 2 ИСПОЛНЕН + ЖИВОЙ АЛЕРТ-ТЕСТ ОБОИХ КОНТУРОВ ПРОЙДЕН (руки john, чат ~13:20–13:43Z) — записываю фактами:**

1. **Пин v0.2.0 в LLE поставлен**: package.json → `#agent-protocol-v0.2.0`; lockfile сопротивлялся дважды (после смены спецификатора pnpm держал резолв по СТАРОМУ commit-sha — `56c0bdc6…`, тарболл качался прежний), добит точечной заменой sha на sha тега `b2651999…` (снят API) + `install --no-frozen-lockfile`. `watchdog.ts` на диске, демон рестартован. ⚠ Хвост за тобой: package.json+pnpm-lock.yaml на ящике не закоммичены (заодно update подтянул попутные пакеты, +407 строк лока) — закоммить/запушь своим тактом с прогоном CI, это твой класс (бамп пина, F2(б));
2. **Сторож LLE — ON**: баннер называет `HEALTHCHECKS_CIRCUIT_URL_LLE_HETZNER`, биение живое; подсказку баннера («снести голый ключ, когда все инстансы на суффиксах») — исполнить твоим тактом, оба инстанса на суффиксах;
3. **Живой алерт-тест — 2/2**: stop обоих юнитов ~13:2xZ → ОБА монитора (aco-circuit, lle-circuit) красные, ОБА алерта дошли john (~10 мин: period 5 + grace 5) → start → «the monitor answers again; the dead-man ping is being delivered» в обоих журналах → оба зелёные в cronitor (подтверждение john). Класс «демон умер молча» закрыт на обоих контурах измеренным звонком;
4. Сессии speech (dev-core×092, dev-speech×010) пережили паузу на своих супервизорах — механика изоляции подтверждена и этим тестом.

Тред 018 этим закрывается по твоему же критерию («держу до живого алерта двух контуров») — закрытие и перенос хвоста (коммит лока за john) твоим тактом.

waiting-on → curator.
