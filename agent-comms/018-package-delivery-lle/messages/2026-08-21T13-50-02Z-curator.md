---
from: curator
worker: claude-code
session: 587b121c-769c-485e-b529-1ea0f14532e2
date: 2026-08-21T13:50:02Z
expects: ack
waiting-on: curator
parked-on: john
---

**Требуется рука john (решения не требуется, если согласен с одним поворотом): доставку LLE НАДО ДОДЕЛАТЬ, а лок КАК ЕСТЬ коммитить нельзя. Факт, перемеренный моей рукой в 13:45–13:52Z: пин в LLE по-прежнему `#agent-protocol-v0.1.1` — В ОБОИХ местах, и оба файла ЧИСТЫ. Единственное, на чём сейчас держится сторож LLE, — НЕЗАКОММИЧЕННЫЙ, руками пропатченный `pnpm-lock.yaml`.**

Живой алерт-тест 2/2 (msg-013, п.3) принимаю — приёмка треда состоялась, тред при этом НЕ закрываю: доставка (то, ради чего тред заведён) не легла ни в один коммит. Ниже — что измерено, чем это опасно и что делает рука.

## Что я перемерила своей рукой (не приняла по описанию msg-013)

В `/home/lle/projects/language-learning-ecosystem`:

1. **Пин НЕ бампнут, и мест у него ДВА:**
   - `package.json:23` → `"agent-protocol": "github:…#agent-protocol-v0.1.1"`;
   - `pnpm-workspace.yaml:36`, секция `overrides:` → `agent-protocol: github:…#agent-protocol-v0.1.1`.
   Оба ЧИСТЫ: `git status --porcelain package.json pnpm-workspace.yaml` — пусто. Последний коммит, тронувший `package.json`, — `06262c7b` от 2026-08-18. То есть шаг 1 этапа 2 на диске отсутствует; mtime `package.json` — 13:20:40Z, файл в тот момент писали, но его содержимое равно HEAD, значит правку откатили.
2. **Грязен ровно один файл:** `pnpm-lock.yaml`, +411/−85. В нём: `specifier: …#agent-protocol-v0.1.1`, `version: …/tar.gz/b265199919811c94644ae467e4f8083ad6bee371`.
3. **Тег ↔ sha сверены полными SHA** (`git rev-parse <tag>^{commit}` в ACO после `git fetch --tags`):
   - `agent-protocol-v0.1.1` → `56c0bdc64f36b30632b2927d0dab8adc92a2a874`, внутри `"version":"0.1.0"`, `watchdog.ts` НЕТ;
   - `agent-protocol-v0.2.0` → `b265199919811c94644ae467e4f8083ad6bee371`, `0.2.0`, `watchdog.ts` есть — это и есть sha в патченном локе;
   - `agent-protocol-v0.2.1` → `b3839dc38a80f35b09cae4c949c134dfea545909`, `0.2.1`, `watchdog.ts` есть.
4. **Установлено сейчас:** `node_modules/agent-protocol/package.json` → `0.2.0`, файл `node_modules/agent-protocol/src/orchestrator/watchdog.ts` на месте. То есть живой демон LLE сторожа несёт — но несёт `v0.2.0`, без хвоста треда 016, а не рекомендованный `v0.2.1`.
5. **Приёмка п.5 (баннер) — моим чтением журнала, а не по докладу:** `journalctl --user -u lle-orchestrator@lle-hetzner.service` → 13:41:46Z `circuit watchdog ON — every tick pings the monitor named by 'HEALTHCHECKS_CIRCUIT_URL_LLE_HETZNER' (value not shown)…`, биение живое (13:42:33Z `the monitor answers again; the dead-man ping is being delivered`).
6. **Приёмка п.6 (два контура)** — принимаю по msg-013: цвет мониторов в cronitor видел ты, журнал обоих юнитов показывает связку stop→красный→start→`the monitor answers again`. Это моя граница: цвет внешнего сервиса моей рукой не перемеривается.

## Чем опасно текущее состояние — и почему тайл «закоммить лок как есть» неверен

- **Имя врёт содержимому.** В репозитории LLE написано `v0.1.1` (дважды), а на диске лежит `v0.2.0`. Коммит лока КАК ЕСТЬ вморозит это враньё в `main`: `specifier` останется `v0.1.1`, а тарболл — `v0.2.0`. Любой другой чекаут и CI поедут по имени, а не по содержимому.
- **Ничто из доставки не переживает `git checkout -- pnpm-lock.yaml`.** Лок не в индексе; сброс дерева (или ff-only pull, наткнувшийся на этот файл) — и `pnpm install` вернёт `56c0bdc6` (`0.1.0`, без `watchdog.ts`). Сторож LLE исчезнет МОЛЧА — ровно тот класс, ради которого заведён тред.
- **Проверяемая гипотеза механизма, почему «лок сопротивлялся дважды» (msg-013, п.1): дело не в упрямстве pnpm, а во ВТОРОМ месте пина.** `overrides` в `pnpm-workspace.yaml` перебивает спецификатор зависимости, и он всё это время говорил `v0.1.1`; правка одного `package.json` резолв изменить не могла в принципе. Гипотезу прогоном я НЕ проверяла (проверка мутирует чужое дерево и `node_modules` живого контура) — проверка в шаге 2 ниже: если бампнуть ОБА места, обычный `pnpm install` должен лечь без ручной хирургии, а в локе появиться `specifier: …v0.2.1`.

## Что делает рука (шаги 1–4, все в `/home/lle/projects/language-learning-ecosystem`)

1. `package.json:23` → `"agent-protocol": "github:language-learning-ecosystem-lle/agent-crew-orchestrator#agent-protocol-v0.2.1"`;
2. `pnpm-workspace.yaml:36` (`overrides`) → `agent-protocol: github:language-learning-ecosystem-lle/agent-crew-orchestrator#agent-protocol-v0.2.1`;
3. `pnpm install` — БЕЗ ручной правки sha и без `--no-frozen-lockfile`; приёмка шага: в `pnpm-lock.yaml` строка `specifier:` для `agent-protocol` читается `…#agent-protocol-v0.2.1`, а `version:` — `…/tar.gz/b3839dc38a80f35b09cae4c949c134dfea545909`; если install снова упрётся — не добивать руками, а прислать вывод мне;
4. рестарт `lle-orchestrator@lle-hetzner.service` + баннер `circuit watchdog ON` (приёмку баннера снимаю я).

Коммит этих трёх файлов — по классу контура LLE (в msg-013 назван `F2(б)`), с прогоном CI. **Своей рукой я в репозиторий LLE не хожу вовсе:** он не в зонах роли curator этого репозитория и живёт своим контуром.

**Развилка, которую несу тебе, — одна:** доделывает это твоя рука сегодня, или тайл уходит задачей в контур LLE (тогда назови тред, и я передам туда факты выше как есть). Пока не сделано — сторож LLE стоит на незакоммиченном локе.

## Хвосты, названные фактами

- **Голый ключ `HEALTHCHECKS_CIRCUIT_URL` в `/home/lle/.config/agent-protocol/secrets.env` НЕ снесён — сознательно.** Оба инстанса на суффиксах (в файле есть `HEALTHCHECKS_CIRCUIT_URL_HETZNER` и `HEALTHCHECKS_CIRCUIT_URL_LLE_HETZNER`; смотрела ИМЕНА ключей, значения не читала), то есть подсказка баннера применима. Но это удаление строки вне версионного контроля — необратимое дешевле чем одним PR, а такой класс карточка отправляет к john. Скажи «сноси» — снесу следующим тактом; удалять молча не стала.
- **Наблюдение не по теме треда, отдаю фактами без диагноза:** дед-мановский пинг контура ACO сегодня флапает. `journalctl --user -u agent-protocol@hetzner.service --since today`: 45 строк `the dead-man ping was NOT delivered: no answer in 5s` и 41 `the monitor answers again`, с 00:17 по 15:47 локального, в последние минуты — пара «упал/поднялся» каждые ~30 с. У LLE за те же сутки таких строк 4. Монитор при этом зелёный (твоё подтверждение в msg-013), то есть тревога не ложная — но 5-секундный таймаут на этом контуре срабатывает часто. Причину НЕ называю (не мерила) — предлагаю поднять это тредом 017 или новым; сюда не тащу.
- **Открытые PR (`gh pr list --state open`, репозиторий `agent-crew-orchestrator`): ровно один — #43 «test(orchestrator): припаркованный тред не тратит попытку пары (тред 020)», создан 13:45:24Z.** Кандидатом моей кнопки в этом ходу он не является: другой тред (мой прогон привязан к 018), вердикта на голове нет. `merge-gate` по нему не гоняла — гонять нечего, круг ревью только начался.

## Критерий закрытия треда 018 — обновляю одной строкой

Закрываю, когда пин `v0.2.1` стоит в ОБОИХ местах LLE в закоммиченном виде и демон на нём показал баннер `circuit watchdog ON`. Живой алерт-тест двух контуров (прежний критерий) уже выполнен и переспрашиваться не будет.

`waiting-on → curator`, `parked-on john`: ход держу я, потому что несу вопрос; действие — шаги 1–4 либо слово «уводим в контур LLE».

