---
from: curator
worker: claude-code
session: 828766f8-d6a8-41f7-86b8-824db7e5c8c2
date: 2026-08-21T18:48:51Z
expects: ack
waiting-on: curator
parked-on: john
---

**Требуется рука john (решения не требуется, если согласен с формой): `main` контура LLE КРАСНАЯ с 18:44Z, и уронила её ровно эта доставка — руками патченный `pnpm-lock.yaml`. `pnpm install --frozen-lockfile` падает на каждом пуше (`ERR_PNPM_UNEXPECTED_PKG_CONTENT_IN_STORE`, «Expected package: agent-protocol@0.1.0. Actual: agent-protocol@0.2.0»). Чинится бампом ОБОИХ мест пина до `v0.2.1` и ОБЫЧНЫМ `pnpm install` — шаги 1–5 ниже. Патченный лок больше не трогать руками.**

Хорошая половина: сторож в LLE живой и на закоммиченном коде — это принимаю фактами. Плохая: цена, которой он куплен, — лок, у которого метаданные врут содержимому, и с 18:44Z он блокирует все пуши в LLE.

## Что принято — измерено моей рукой, а не по докладу

1. **Пин доехал в `main` LLE:** `74b03fc0` + `1bbd7f57`, рабочее дерево ЧИСТО (`git status --porcelain` — пусто). Тайл msg `2026-08-21T18-32-39Z-curator.md` п.1 закрыт;
2. **демон подхватил код сам:** `18:40:13Z` — `SELF-RESTART: the tree is on 1bbd7f57 — leaving with code 75`, `18:40:32Z` — `code: 1bbd7f57 loaded from /home/lle/projects/language-learning-ecosystem, up since 2026-08-21T18:40:32Z`. Юнит `active`, тики живые;
3. **приёмка п.5 (баннер) — СНЯТА, моим чтением журнала:** `18:40:32Z` `circuit watchdog ON — every tick pings the monitor named by 'HEALTHCHECKS_CIRCUIT_URL_LLE_HETZNER' (value not shown)…`; биение живое (`18:40:41Z` NOT delivered → `18:41:14Z` `the monitor answers again` — одиночная пара, тот самый ожидаемый шум, про который сказано в постановке);
4. **на диске стоит `agent-protocol@0.2.0`, `node_modules/agent-protocol/src/orchestrator/watchdog.ts` на месте.** Ловушку «версия ≠ приёмка» помню — приёмка снята баннером, не номером.

Приёмка п.6 (два контура) снята раньше, msg `2026-08-21T13-43-46Z-curator.md`, и не переспрашивается.

## Что сломалось — команды и прогоны, не догадка

`pnpm install --frozen-lockfile` в CI LLE падает на обоих воркфлоу:

- `checks` (CI, прогон **32514882278**): `ERR_PNPM_UNEXPECTED_PKG_CONTENT_IN_STORE  Package name mismatch … tarball b265199919811c94644ae467e4f8083ad6bee371 … Expected package: agent-protocol@0.1.0. Actual package in the store with the given integrity: agent-protocol@0.2.0`;
- `extension-smoke` (E2E, прогон **32514882276**): та же ошибка, тот же шаг.

Оба — на пуше `18:44:29Z` (коммит про pronunciation, к доставке отношения не имеющий: он просто первый, кто пришёл после). **Собственные прогоны пиновых коммитов были ЗЕЛЁНЫЕ** (CI `32513760909`, `32513860305`; E2E `32513760968`, `32513860404`), и до них подряд зелено весь день.

**Почему зелено, а потом красно — гипотеза, и называю её гипотезой:** проверка бьёт при ЧТЕНИИ пакета ИЗ СТОРА, а пиновые прогоны стор этим тарболлом только наполняли. С этого момента отказ детерминирован — красным будет каждый следующий пуш, пока лок не пересобран.

## Причина — три закоммиченных значения, которые не согласны между собой

| где | что стоит |
|---|---|
| `package.json:23` | `#agent-protocol-v0.2.0` |
| `pnpm-workspace.yaml:36`, `overrides:` | `#agent-protocol-v0.1.1` |
| `pnpm-lock.yaml` (`overrides` и `specifier`) | `#agent-protocol-v0.1.1` |
| `pnpm-lock.yaml`, `version:` тарболла | `…/tar.gz/b265199919811c94644ae467e4f8083ad6bee371` (это тег **v0.2.0**) |
| `pnpm-lock.yaml`, метаданные пакета (стр. 3228) | `version: 0.1.0` — а в тарболле лежит `0.2.0` |

Последняя строка и есть та, на которую ругается pnpm.

**Гипотеза из msg `2026-08-21T13-43-46Z-curator.md` («лок сопротивлялся не из упрямства, а из-за ВТОРОГО места пина») подтвердилась фактом:** `overrides` в `pnpm-workspace.yaml` так и остался на `v0.1.1` — его не правили ни разу, и именно он перебивал спецификатор. Ручная замена sha в локе обошла overrides, но оставила рядом метаданные старой версии — отсюда сегодняшний отказ.

Второе следствие, важнее красной CI: **`pnpm install` без `--frozen-lockfile` пересоберёт резолв ПО OVERRIDES, то есть вернётся на `v0.1.1` — на `0.1.0` без `watchdog.ts`.** Сторож LLE исчезнет молча, ровно тот класс, ради которого заведён тред.

## Что делает рука — шаги 1–5, все в `/home/lle/projects/language-learning-ecosystem`

1. `package.json:23` → `"agent-protocol": "github:language-learning-ecosystem-lle/agent-crew-orchestrator#agent-protocol-v0.2.1"`;
2. `pnpm-workspace.yaml:36`, секция `overrides:` → `agent-protocol: github:language-learning-ecosystem-lle/agent-crew-orchestrator#agent-protocol-v0.2.1` — **это второе место, и без него первое ничего не решает**;
3. `pnpm install` — БЕЗ ручной правки sha, БЕЗ `--no-frozen-lockfile`. Приёмка шага, прямо в локе: `specifier:` читается `…#agent-protocol-v0.2.1`, `version:` — `…/tar.gz/b3839dc38a80f35b09cae4c949c134dfea545909`, а строка `version: 0.1.0` в метаданных пакета исчезает (станет `0.2.1`). **Если install упрётся — не добивать руками, прислать вывод мне**;
4. коммит трёх файлов (`package.json`, `pnpm-workspace.yaml`, `pnpm-lock.yaml`) — приёмка: зелёные `CI` и `E2E` на этом коммите. Красная `main` закрывается ими, а не чтением;
5. рестарт демону не нужен: он подхватит новый код сам, как подхватил сегодня (`SELF-RESTART` → `code: … loaded`), — но баннер `circuit watchdog ON` на новой голове я всё равно сниму сама.

**Почему `v0.2.1`, а не привести всё к уже стоящему `v0.2.0`:** оба варианта чинят красноту одинаково, но `v0.2.1` несёт хвост треда 016 (нотифай) и стоит ровно столько же руки. Полные SHA сверены мной сейчас (`git rev-parse` после `fetch --tags`): `v0.2.1` → `b3839dc38a80f35b09cae4c949c134dfea545909`, дерево `1010c4df…`, `watchdog.ts` в нём **побайтно тот же**, что на сегодняшней `main` ACO (`git diff --quiet` по файлу — чисто).

**Уточнение к прежней формулировке, чтобы решение стояло на верном числе:** `v0.2.1` — это `main` ACO по состоянию на ~15:00Z, а не «сегодняшняя `main`»: с тех пор пакет уехал на 36 файлов (+2747/−112, треды 022–025). Сторожа это не касается. Следующая доставка потребует бампа `0.2.1` → `0.2.2` (версия в `main` всё ещё `0.2.1`, и скрипт реза откажет на существующем теге) — это отдельный такт dev-core, не сегодняшний.

**Развилка, которую несу тебе, — та же, что в msg `2026-08-21T13-43-46Z-curator.md`, и теперь у неё есть цена:** доделываешь своей рукой сегодня, или тайл уходит задачей в контур LLE (назови тред — передам факты выше как есть). Пока не сделано, `main` LLE красная и блокирует чужие пуши, а сторож стоит на локе, который переживёт только `--frozen-lockfile`.

## Хвост, который я замерила по твоей просьбе — баг zones-хука ВОСПРОИЗВЁЛСЯ

Кандидат из msg `2026-08-21T18-32-39Z-curator.md` п.1 («тихо съеденный коммит») — не гипотеза, а факт. Команда хука (`lefthook.yml:16`), запущенная мной из корневого чекаута LLE, read-only:

```
$ pnpm exec tsx node_modules/agent-protocol/src/cli.ts zones check --ref origin/main --no-fetch --role-from-workspace --staged
agent-protocol: WARNING — 'origin/main' was not updated (--no-fetch), the config may be stale
fatal: not a git repository (or any of the parent directories): .git
agent-protocol: '/home/lle/projects/language-learning-ecosystem/..' is not inside a git repository:
  Command failed: git -C /home/lle/projects/language-learning-ecosystem/.. rev-parse --show-toplevel
EXIT: 2
```

`--role-from-workspace` из КОРНЕВОГО чекаута лезет в РОДИТЕЛЬСКИЙ каталог и умирает там, где git-репозитория нет вовсе, — то есть pre-commit отказывает по собственному багу на любом коммите руки, не по диффу. Дефект в коде пакета (`packages/agent-protocol/**`, зона dev-core ЭТОГО контура), конфиг хука — сторона LLE. **Отдельным тредом заведу постановку dev-core следующим тактом** (тред = одна тема, сюда не тащу); мерить повторно не надо — команда и вывод выше.

## Прочие хвосты

- **Голый ключ `HEALTHCHECKS_CIRCUIT_URL` не снесён** — жду твоего слова, как и записано. Баннер сегодня подсказку повторил дословно (`… is set in '/home/lle/.config/agent-protocol/secrets.env' as well and is IGNORED here … Remove the bare key once every instance on this box has its own`), оба инстанса на суффиксах. Скажи «сноси» — снесу;
- **открытых PR по этому репозиторию нет:** `gh pr list --state open` — пусто. Кандидатов моей кнопки в этом ходу не было, `merge-gate` гонять не по чему;
- флап дед-ман-пинга на LLE сегодня одиночный (пара 18:40:41Z / 18:41:14Z) — ожидаемый шум, отдельной работы не завожу.

## Критерий закрытия треда 018 — уточняю одной строкой

Закрываю, когда **оба** места пина LLE стоят на одном теге в закоммиченном виде, `CI` и `E2E` на этом коммите зелёные, и демон показал `circuit watchdog ON` на нём. Живой алерт двух контуров выполнен и не переспрашивается.

`waiting-on → curator`, `parked-on john`: ход держу я, потому что несу вопрос; действие — шаги 1–5 либо слово «уводим в контур LLE».

