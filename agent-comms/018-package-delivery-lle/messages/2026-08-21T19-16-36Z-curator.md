---
from: curator
worker: claude-code
session: 32f0bb56-09e4-455e-8a8a-6f44c94f71d5
date: 2026-08-21T19:16:36Z
expects: none
---

**Тред 018 ЗАКРЫТ приёмкой. Доставка легла в закоммиченный вид: оба места пина LLE на `v0.2.1` (`5a157b72`), `CI` и `E2E` на этой голове ЗЕЛЁНЫЕ — красная `main` закрыта прогоном, а не чтением; сторож LLE живой и называет свой ключ. Один остаток называю честно: живой процесс демона держит в памяти код на коммит позади новой головы, и это закрывается само — почему это не держит закрытие, разобрано ниже фактами.**

Приёмку снимаю по критерию из моего же `2026-08-21T18-48-51Z-curator.md`: «оба места пина на одном теге в закоммиченном виде, `CI` и `E2E` на этом коммите зелёные, демон показал `circuit watchdog ON` на нём». Доставку решений john `2026-08-21T18-51-46Z-curator.md` я не приняла по описанию — перемерила своей рукой, командами, полными SHA.

## Что измерено — три факта критерия

1. **Оба места пина стоят на одном теге и закоммичены.** В `/home/lle/projects/language-learning-ecosystem`:
   - `package.json:23` → `"agent-protocol": "github:language-learning-ecosystem-lle/agent-crew-orchestrator#agent-protocol-v0.2.1"`;
   - `pnpm-workspace.yaml:36`, секция `overrides:` → `agent-protocol: github:…#agent-protocol-v0.2.1` — то самое ВТОРОЕ место, из-за которого всё сегодня и сломалось;
   - `HEAD` = `5a157b72c55d0635debd948a88ddcb6faf0eb636`, `git status --porcelain` — ПУСТО. Ничто из доставки больше не стоит на незакоммиченном файле; класс опасности из msg `2026-08-21T13-50-02Z-curator.md` («ничто не переживает `git checkout -- pnpm-lock.yaml`») снят.

2. **`CI` и `E2E` зелёные на ТОЙ ЖЕ голове — сверено API по полному SHA, не глазом:**
   - `CI`, прогон **32515458273** → `conclusion: success`, `head_sha: 5a157b72c55d0635debd948a88ddcb6faf0eb636`;
   - `E2E`, прогон **32515458239** → `conclusion: success`, тот же полный `head_sha`.
   Это и есть тот самый шаг `pnpm install --frozen-lockfile`, который падал `ERR_PNPM_UNEXPECTED_PKG_CONTENT_IN_STORE` на прогонах `32514882278` (CI) и `32514882276` (E2E) в 18:44Z. Отказ был детерминированным — значит зелёный на честно пересобранном локе и есть доказательство, что метаданные больше не врут содержимому.

3. **Сторож живой и называет свой ключ.** `journalctl --user -u lle-orchestrator@lle-hetzner.service`, `18:49:08Z`:
   `circuit watchdog ON — every tick pings the monitor named by 'HEALTHCHECKS_CIRCUIT_URL_LLE_HETZNER' (value not shown)…`
   Биение живое; пары `NOT delivered` / `the monitor answers again` одиночные и чередуются (19:03:16Z → 19:03:48Z и раньше) — ровно тот ожидаемый шум, про который сказано в постановке.
   На диске установлено `agent-protocol@0.2.1`, `node_modules/agent-protocol/src/orchestrator/watchdog.ts` на месте.

Приёмка живого алерта, различающего два контура (то, ради чего тред заведён), снята раньше — msg `2026-08-21T13-43-46Z-curator.md`, 2/2 — и не переспрашивается.

## Остаток, который называю как остаток, а не выдаю за исполнение

**Третий факт критерия выполнен НЕ буквально: баннер `ON` сказан не на новой голове.** Живой процесс демона (pid 417515, up since `18:49:08Z`) держит в памяти код чекаута `00503cd4` — на один коммит позади `5a157b72`. Его собственная строка, последняя в 19:13:14Z:

```
the LOADED CODE is not the ref: this process runs 00503cd4 from
/home/lle/projects/language-learning-ecosystem, origin/main on disk is 5a157b72 —
1 commit(s) behind; up since 2026-08-21T18:49:08Z (24m), and node loads modules once
```

**Механизм — арифметика минут, а не дефект:** процесс стартовал в `18:48:58Z`, а честный `pnpm install` дописал дерево в `18:51:14Z` (mtime `node_modules/agent-protocol` и `node_modules/.modules.yaml`). То есть в памяти у него `v0.2.0`, на диске уже `v0.2.1`.

**Само-перезапуск не идёт по объявленной причине, и она не про доставку:**
`no self-restart while sessions are live (dev-core, dev-core/093-red-main-pnpm-pin)`. Я смотрела журнал в foreground 19:05–19:13Z — рестарта за это время не было, блокировка та же.

**Почему это не держит закрытие — фактом, а не рассуждением:**

```
$ git diff --stat agent-protocol-v0.2.0 agent-protocol-v0.2.1 -- src/orchestrator/watchdog.ts
(пусто)
```

Сторож в обеих версиях побайтно один. Баннер на новой голове скажет ту же строку, что уже сказана; разница `v0.2.0`→`v0.2.1` — хвост треда 016 (нотифай) и скрипт реза, к сторожу отношения не имеющие. Перезапуск случится сам, как случился сегодня в 18:40Z (`SELF-RESTART … leaving with code 75` → `code: … loaded`), как только сессии контура LLE стихнут.

**Своё изменение критерия называю изменением.** Закрываю на измеренном: пин закоммичен в обоих местах, обе проверки зелёные на полном SHA, сторож `ON` с именем ключа, `0.2.1` на диске. Остаток — перезапуск процесса на `5a157b72` — самозакрывающийся и ничего нового про сторож не скажет. **Если баннер на новой голове скажет `OFF` — это новый факт, и я переоткрою тред** (право `thread-status` у меня, закрытие обратимо одной командой).

## Хвосты, уезжающие из треда поимённо

- **Баг zones-хука — постановка dev-core за следующим тактом curator.** Факт воспроизведён и стоит в msg `2026-08-21T18-48-51Z-curator.md` (команда и вывод: `--role-from-workspace` из КОРНЕВОГО чекаута лезет в родительский каталог и умирает `fatal: not a git repository`, exit 2 — pre-commit отказывает по собственному багу, а не по диффу; дефект в `packages/agent-protocol/**`, зона dev-core ЭТОГО контура). Мерить повторно не надо. Новым тредом эту постановку я в ЭТОМ прогоне не завожу: прогон привязан к треду 018 и только к нему;
- **у контура LLE есть свой тред `093-red-main-pnpm-pin`** — вижу его имя в строках демона (сессии `dev-core` и `curator` шли по нему 18:53–19:13Z). Красную `main` уже закрыла рука john коммитом `5a157b72` с зелёными `CI`/`E2E`; тому, кто ведёт 093, стоит начать с этого факта, чтобы работа не делалась дважды. Своей рукой в чужую почту не хожу — передаю наблюдением;
- **голый ключ `HEALTHCHECKS_CIRCUIT_URL` снесён — проверено моим чтением, а не по докладу.** В `/home/lle/.config/agent-protocol/secrets.env` ключей с этим корнем ровно два, оба суффиксные: `HEALTHCHECKS_CIRCUIT_URL_HETZNER` и `HEALTHCHECKS_CIRCUIT_URL_LLE_HETZNER`; точного `HEALTHCHECKS_CIRCUIT_URL=` нет (смотрела ИМЕНА ключей, значения не читала). Хвост из msg `2026-08-21T13-50-02Z-curator.md` закрыт;
- **флап дед-ман-пинга контура ACO** (45 отказов/41 восстановление за сутки, замер в msg `18-48-51Z`) — не тема этого треда, поднимать 017 или новым; сюда не тащу;
- **следующая доставка потребует бампа `0.2.1` → `0.2.2`**: версия пакета в `main` ACO всё ещё `0.2.1`, а скрипт реза отказывает на существующем теге. С 15:00Z пакет уехал на треды 022–025 — сторожа это не касается, но доставлять придётся отдельным тактом dev-core;
- **кандидатов моей кнопки нет:** `gh pr list --state open` в `agent-crew-orchestrator` — ровно один PR, #51 (тред 025, правит `PROTOCOL.md`). Это док власти и чужой тред — кнопка john, `merge-gate` по нему я не гоняла.

## Итог треда

Класс «демон умер молча» закрыт на ОБОИХ контурах измеренным звонком (алерт-тест 2/2, msg `13-43-46Z`). Сторож LLE стоит на закоммиченном пине `v0.2.1`, обе проверки на нём зелёные. Рез пакета наружу перестал быть ручной последовательностью в чьей-то голове: `scripts/split-package.sh` с дверью `--base`, которая отказывает по именам недостающих коммитов, — PR #40 и #41, оба смёржены с поимёнными гардами в этом треде.

Цена, оплаченная по дороге и записанная в ленту как урок: руки в `pnpm-lock.yaml` — никогда; мест у пина в LLE ДВА, и правка одного не решает ничего.

`waiting-on → —`, тред закрыт (`thread status --status closed`).

