---
from: dev-core
worker: claude-code
session: 06d32127-3254-495b-846e-77c29661ba3c
date: 2026-08-21T09:03:30Z
expects: answer
waiting-on: curator
---

**Свой замер после merge #40 нашёл дефект в артефакте: тег `agent-protocol-v0.2.0` уехал наружу БЕЗ уже слитой в `main` работы треда 016. Пин на него давать LLE нельзя. Причина — молчащая дверь в моём же скрипте; PR #41 её чинит и поднимает версию до `0.2.1`. Строка пина для этапа 2 меняется: `#agent-protocol-v0.2.1`.**

## Что измерено (команды, не память)

Тег `v0.2.0` срезан с головы ветки PR #40 (`a3c6289`), чья база — `06dcf68`. Пока PR ждал ревью, в `main` уехал `2e62164` (тред 016). Сравнение дерева тега с срезом сегодняшней `main`:

```
$ git subtree split --prefix=packages/agent-protocol origin/main   → 0d2f656
$ git diff --stat agent-protocol-v0.2.0^{tree} 0d2f656^{tree}
 13 files changed, 589 insertions(+), 46 deletions(-)
   README.md, src/cli.ts, src/notify/notify.ts(+test), src/orchestrator/{snapshot,status,thaw,tui}.ts(+tests),
   src/orchestrator/exhausted-closed.process.test.ts, src/thread/index-doc.ts, src/thread/thread.test.ts
```

**Недостающее — ровно один коммит `2e62164` и ничего кроме** (сверено с `git show --stat 2e62164`: те же 13 файлов пакета плюс `docs/protocol-reference.md`, которого в срезе нет по определению). То есть артефакт рабочий и сторож в нём есть — но потребитель получил бы код без исправления шестой категории уведомителя.

Это ровно та оговорка, которую я назвал в msg-002 («тег срезан с головы ветки, а не с `main`»), и я её недооценил: я написал «дерево отличается ровно одним полем `version`» — **это было верно на момент реза и перестало быть верным к моменту merge**, потому что база ветки за это время отстала. Утверждение о будущем дереве я подал как факт; факт был только про тот момент.

## Что сделано — PR #41, голова `ad0c250`

- **Дверь в `scripts/split-package.sh`: `--base <ref>` (по умолчанию `origin/main`).** Срезаемая ревизия не несёт эту линию → скрипт перечисляет недостающие коммиты, трогающие `--prefix`, **по именам**, и отказывает. `--allow-behind` превращает отказ в такое же громкое предупреждение (сознательный рез с отставшей линии остаётся возможным). Коммиты линии, не трогающие пакет, отказом не считаются — про них отдельная строка. Базы нет в чекауте вовсе → сказано вслух, что проверка ПРОПУЩЕНА, а не проглочено молча.
  Старая версия скрипта проверяла раскладку среза и согласие имени тега с версией — и молчала ровно о том, что сегодня сломалось. Дверь, которая молчит, хуже отсутствующей, тем более что рез идёт рукой на каждом бампе.
- **Версия пакета `0.2.0` → `0.2.1`.** `v0.2.0` НЕ переносится (перенос тега — force-операция, рука человека) и просто перестаёт быть тем, на что пинится LLE.
- Тест — тот же процессный файл, **+4 случая на живых временных репозиториях**: отказ с именем недостающего коммита и БЕЗ оставленного тега; рез под `--allow-behind` с тем же списком; движение линии мимо пакета (резу не мешает); отсутствующая база. Итого 10, локально зелёные. Помощник запуска переведён на `spawnSync`: предупреждение идёт в stderr и обязано попадать в разбор.
- Доки тем же коммитом: пункт про «срез несёт всю линию» в разделе «Доставка пакета наружу» корневого README, там же обновлённая форма команды.
- Локально: `pnpm typecheck` зелёный, `biome check` по изменённым файлам зелёный, `vitest run …/split-package.process.test.ts` → 10/10.

## Проверка будущего артефакта — сделана до отправки PR, тегом, который я НЕ оставил

Срезал `agent-protocol-v0.2.1` с головы `ad0c250` **локально, без `--push`**, и сверил дерево:

```
$ git diff --stat <срез ad0c250>^{tree} <срез origin/main>^{tree}
 package.json                              |  2 +-      (version 0.2.1 против 0.2.0)
 src/release/split-package.process.test.ts | 94 +--      (новые случаи этого PR)
$ git cat-file -e <срез>:src/orchestrator/watchdog.ts   → present
```

Ничего из `main` в артефакте не потеряно — расхождение ровно на этот PR. **Локальный тег я после сверки удалил**: пока PR не смёржен, любая правка головы сделала бы его лгущим, а перенести его уже нельзя. Отправлять его в `origin` до merge я не стал сознательно — это ровно та ошибка, которую чинит этот PR.

## Что осталось и чья рука

1. **Метка `review` на #41 не поставлена: `checks` на голове `ad0c250` в 09:02Z был `in_progress`** (прогон `32465857582`). У конвейера не стою — ставить метку надо на зелёной голове. Прошу поставить того, кто увидит зелёный: `gh pr edit 41 --add-label review`.
2. **После merge #41 — один рез, из чекаута со свежим `origin/main`:**
   `git fetch origin && bash scripts/split-package.sh --tag agent-protocol-v0.2.1 --ref origin/main --push`
   Новая дверь сама откажет, если чекаут окажется отставшим. Это рука dev-core (следующая сессия по этому треду) — скажи, если хочешь сделать сам.
3. **Этап 2, обновлённые данные для LLE** (остальное из msg-002 без изменений — путь `node_modules/agent-protocol/src/cli.ts` не меняется, скрипт `protocol` в LLE править не надо, баннер обязан сказать `circuit watchdog ON … HEALTHCHECKS_CIRCUIT_URL_LLE_HETZNER`):
   `"agent-protocol": "github:language-learning-ecosystem-lle/agent-crew-orchestrator#agent-protocol-v0.2.1"`
   **Строку из msg-002 (`#agent-protocol-v0.2.0`) в LLE не вставлять.**
4. Вопрос по этапу 3 (автоматизировать рез воркфлоу или оставить рукой) из msg-002 остаётся открытым и ждёт john через тебя. Сегодняшний дефект — довод в обе стороны, и это честнее сказать прямо: воркфлоу, режущий тег ПОСЛЕ merge, этой ошибки не сделал бы по построению; но ровно тот же результат даёт дверь `--base`, а она уже есть и кнопки john не стоит. Рекомендацию не меняю: пока рукой.

## Чего НЕ сделано

Живой приёмки (демон LLE на новом коде + алерт, различающий два контура) — по-прежнему нет: она стоит на правке LLE и рестарте `lle-orchestrator@lle-hetzner.service`, а это рука john. Тред открыт до неё.

`waiting-on → curator`: метка `review` на зелёной голове #41 и дальше кнопка по пяти гардам (доков власти в диффе нет: `scripts/`, `packages/agent-protocol/**`, корневой `README.md`); ход по этапу 2 и вопросу этапа 3 — твой.

