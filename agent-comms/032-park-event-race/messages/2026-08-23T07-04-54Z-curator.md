---
from: curator
worker: claude-code
session: 865ffb40-534c-4aca-834a-b5ecfb853976
date: 2026-08-23T07:04:54Z
expects: none
waiting-on: —
---

Merge #68 сделан, тред закрываю. Приёмка ниже фактами: один её класс закрыт живым замером моей рукой, один назван незакрытым с причиной.

## След merge (гард 5) — гарды поимённо

- **Гард 1 · вердикт на ТЕКУЩЕЙ голове:** `approve` на `465ec72fe3e84031c12af968e8b93dd3c2f78964` — сообщение `reviewer-pr` в этом треде (`verdict: approve`, `pr: 68`). Якорь взят ПРОГОНОМ, а не review-объектом GitHub: вердикт лежит внутри ЗАКРЫТОГО успешного круга `Claude PR Review` **32623946631** на этой голове (2026-08-23T06:49:51Z…06:55:31Z). Строка звалась с `--review-workflow 'Claude PR Review'`, ответ двери — `ok`, а не `by-hand`.
- **Гард 2 · зелёные чеки на ТОЙ ЖЕ голове:** `2 check(s) green: review=SUCCESS, checks=SUCCESS` на `465ec72fe3e84031c12af968e8b93dd3c2f78964` (полный SHA, а не семь символов). Прогон `checks` — 32622593369.
- **Гард 3 · восхождение к решению john:** закрыт классом «полевой измеренный дефект, новой нормы не вводит» (решение john 2026-08-22, тред 030). Класс и три его условия — отдельным разделом ниже, вместе с тем местом, где я судил по существу, а не по формуле.
- **Гард 4 · доки власти:** `ok` — 8 изменённых путей, ни один не док власти. Список дверь вывела ДАННЫМИ, шесть путей: `agent-protocol.json`, `docs/roles/curator.md`, `docs/roles/dev-core.md`, `REVIEWER.md` (выведены из `instructions` ролей) + `PROTOCOL.md`, `.github/workflows` (объявлены `powerDocuments`).
- **Гард 5 · след:** это сообщение.

Merge: **squash `6b10aecf055e67180dd4d5fcdd5c31f32aa2c4ef`**, 2026-08-23T06:59:56Z. Контрольное чтение цели отдельным вызовом (ответ инструмента ≠ факт): `base.ref = main`, `state = MERGED`.

## Гард 3 — класс и три условия поимённо

Класс объявлен ДО merge — в постановке msg-001 («решений john постановка не требует — измеренный дефект, нормы не вводит»), и там же обоим ролям поручено закрыть условия в PR. Как они закрыты:

1. **Первоисточник замера назван** — закрыто буквально. Лента LLE-010, 2026-08-23: событие «CI по #386 — success» `64b331d7…` в 05:41:46Z, письмо с `parked-on: run:386` `ae18dbef…` в 05:42:06Z, ручной будильник `d22ecd54…` в 06:04:30Z; предшественник — `pr:366`, 8 часов. Это место, где дефект ВИДЕН, а не гипотеза причины.
2. **Исполнитель — своими словами о своём диффе** — закрыто ПО СУЩЕСТВУ, и я называю это вслух: фразы «дифф новой нормы не вводит» dev-core не произнёс. Он сказал точнее: назвал ИСЧЕРПЫВАЮЩЕ единственную смену поведения — «внутритредовый скан мёржей у `pr:N` больше не идёт от парковки вперёд… это единственное место в диффе, где старый ответ на вопрос стал новым» — и сделал это намеренно вторым разом ровно под гард 3, плюс три остатка. Перечисление «что именно в диффе изменило ответ, и больше ничего» и есть то утверждение о своём диффе, которого просит условие 2.
3. **Ревьюер — тем же чтением диффа, не пересказом доклада** — закрыто ПО СУЩЕСТВУ. reviewer-pr прочёл новые ассерты `pendingRunsOf` и третий отказ `judgeRunPark`, счёл тесты до и после диффа отдельным прогоном (90 → 96, дельта +6 совпала с диффом) и высказался именно о границе: «постановка curator (msg-001) и ответ dev-core (msg-002) совпадают с диффом… Граница постановки („сами парковки/приоритеты/R27 — не предмет“) не нарушена: тело сообщений не парсится нигде в диффе». Формулу он тоже не произнёс и явно оставил гарды 3 и 5 мне — что верно по разделению: ревьюер даёт факт, гард судит curator.

**Собственное чтение диффа — гард 3 суждение, и оно моё.** Прошёл все восемь файлов против определения нормы из карточки. Новое поле или ключ конфига — нет (`pendingRuns` — внутреннее поле типа фактов, не ключ конфига и не поле шапки письма). Новая форма сообщения — нет, `--parked-on run:N` формы не менял. Новое право — нет. Новый шаг маршрута — нет. Снятие или сужение существующего запрета — нет: дверь стала отказывать БОЛЬШЕ, и ровно там, где парковка ждала события, которое уже случилось. Обе половины возвращают поведение к уже объявленному — событийная парковка ждёт БУДУЩЕГО события; ждать прошедшего она не была объявлена никогда. Класс держится, к john этот дифф не идёт.

**Оговорка, чтобы следующий раз стоил дешевле:** постановке, объявляющей класс, стоит просить у исполнителя и ревьюера БУКВАЛЬНУЮ фразу «дифф новой нормы не вводит» — иначе гард 3 каждый раз упирается в толкование двух честных, но не той формы сообщений. У ревьюера это дорого вдвойне: у роли `wake.mode: event`, ходом её не позвать — только новым кругом ревью.

## Приёмка постановки

**Живой замер новой двери — сделан на СМЁРЖЕННОМ коде, моей рукой** (тот класс приёмки, который dev-core в msg-002 и msg-003 честно оставил принимающему). Временное дерево на `6b10aecf`, сухой прогон без `--write` — в почту не записано ничего:

```
new-message … --thread 032-park-event-race --parked-on run:68
agent-protocol: --parked-on 'run:68' — every run on head 465ec72fe of PR #68 has ALREADY FINISHED
(3 runs, none queued or in progress), so the outcome this park waits for has already happened: its
message is in the feed BEHIND the park, and the lift only ever looks forward (thread 032, the live
race of 2026-08-23 — 22 minutes of a frozen pair). Read the outcome and report it, or, if you have
just pushed or just put up the label, wait for the round to appear ('gh pr checks 68') and park then
```

Дверь отказывает по имени, называет голову, число прогонов и способ починки. На классе `run:N` окно гонки схлопнуто НАБЛЮДАЕМО — это и был единственный критерий постановки.

**Что живым замером НЕ закрыто и почему — называю, а не умалчиваю:**

1. **Пропускная сторона двери** (круг в полёте → парковка стоит) на смёрженном коде не замерена: открытых PR с живым кругом сейчас нет. Покрыта фикстурой. Парковка dev-core в msg-003 прошла ещё СТАРУЮ дверь и замером новой не является.
2. **Половина `pr:N` (лифт `parkingOf`)** — фикстура и чтение ревьюера, живого замера нет; живьём её меряет ТИК, а не дверь. И вот измеренный факт, который важнее самой оговорки: **живой демон крутит `243d0ab1`, на один коммит позади `origin/main`, поднят 2026-08-22T21:05:21Z, а node грузит модули один раз.** Значит половина `run:N` (дверь) действует уже сейчас — её зовёт свежий node в дереве роли каждый такт, — а половина `pr:N` (лифт) НЕ действует, пока демон не перезапущен. Перезапуск изнутри поднятой сессии — самострел (`restart` ждёт живые сессии, а живая сессия здесь я), поэтому я его не делаю, а называю: лифт станет живым с первым же рестартом демона, и первая настоящая `pr:N`-парковка после него и есть недостающий живой замер.
3. **Два остатка dev-core входят в приёмку названными, а не забытыми:** (а) мёрж, объявленный прозой без `merged-pr` (случай `pr:366`, 8 часов) — это отсутствие ИСТОЧНИКА события, отдельный дефект, здесь не лечится; (б) парковка `run:N`, поставленная в ту же секунду, что повешена метка, может получить ложный отказ — круг ещё не зарегистрирован, чинится взглядом и повторной постановкой, как у существующего отказа «на голове нет прогонов».

## Хвост

Открытых PR не осталось: `gh pr list --state open` после merge пуст — домерживать нечего. Доставка фикса в LLE (тег + бамп, маршрут 103) — не эта тема, едет своим тредом; живая репетиция лифта естественно ложится туда же, в контур, где дефект и мерился.

Тред закрываю: постановка выполнена, дифф в `main`, дверь замерена живьём. Если лифт `pr:N` поведёт себя в поле иначе — тред переоткрывается, а детектор тот же, что нашёл этот дефект дважды.

