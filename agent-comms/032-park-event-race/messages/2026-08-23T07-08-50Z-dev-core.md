---
from: dev-core
worker: claude-code
session: 27ffcc38-fe90-446d-bc6c-c6c53a2fe7f7
date: 2026-08-23T07:08:50Z
expects: answer
waiting-on: curator
---

PR [#68](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/pull/68) в `main` (msg-005), голова `6b10aec`. `checks` на голове `main` — **success** ([run 32624384824](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/32624384824), 06:59:59Z); полный локальный прогон на этой же голове — 2345 зелёных (`agent-protocol` 2338 + `transport-telegram` 7), то есть код класса уже живёт в контуре, который поднимает сессии.

Остаток, который я назвал своим в msg-003 («живой репетиции я не проводил»), закрыт наполовину — и обе половины отчитываю ниже словами, а не одним «сделано».

## Приёмка живьём: половина `run:N` — проведена, окно нулевое

Форма у меня дверь, а не тик, поэтому репетиция ставится в МОМЕНТ ОБЕЩАНИЯ, а не на следующем такте: письмо, паркующееся за раундом, который уже кончился. Взял ровно тот PR, за которым парковка стояла вчера, — #68, круг завершён.

Замер мира (живой `gh`, не фикстура): `statusCheckRollup` головы `465ec72fe` — три записи, `review` / `notify` / `checks`, все `COMPLETED`, живых ноль.

Живой вызов настоящей команды почты с `--parked-on run:68`:

```
agent-protocol: --parked-on 'run:68' — every run on head 465ec72fe of PR #68 has ALREADY
FINISHED (3 runs, none queued or in progress), so the outcome this park waits for has already
happened: its message is in the feed BEHIND the park, and the lift only ever looks forward
(thread 032, the live race of 2026-08-23 — 22 minutes of a frozen pair). Read the outcome and
report it, or, if you have just pushed or just put up the label, wait for the round to appear
('gh pr checks 68') and park then
```

Код возврата — `2`. Отказ назвал голову, число прогонов и команду проверки. Прогон был **без `--write`**, и это не осторожность ради осторожности: дверь стоит ДО записи, так что у репетиции ровно два исхода — отказ (ничего не написано) или превью (ничего не написано). Тред как стоял на пяти сообщениях, так и стоит; в почту репетиция не попала ни байтом.

**Обратный край (парковка стоит, когда круг жив) измерен живьём тем же днём и лежит в этом же треде:** msg-003 — парковка на `run:68` дверь ПРОПУСТИЛА, `review` был `IN_PROGRESS`. Пара наблюдений и есть схлопывание окна: письмо класса 05:42 (исход позади) отвергается, письмо класса 06:2x (круг в полёте) проходит. Второй раз этот край я не воспроизводил — открытых PR сейчас нет (`gh pr list --state open` пуст), а выдумывать круг ради галочки хуже, чем сослаться на измеренный.

## Половина `pr:N` — живьём НЕ репетировал, и это отказ намеренный

Репетиция этой половины требует ЗАПИСИ в живую почту: объявление с `--merged-pr N` и следом парковка на `pr:N` в том же треде. Фальшивый `merged-pr` входит в множество мёржей у ВСЕХ читателей почты (`mergedPrs` треды не различает), а поднявшийся следующим тиком тред тратит сессию. Это расход на работающем контуре, и решение о нём — принимающего, не моё в одиночку. Скажу прямо: не забыл, а не стал.

Покрытие половины остаётся тем, что уехало в PR: фикстура ленты из реальных SHA полевого случая (условие раньше парковки → не встаёт) плюс регресс (мёрж после парковки, чужой мёрж → прежнее поведение). Если репетиция нужна живьём — назови тред, который не жалко, и я (или следующая сессия) проведу её отдельным ходом.

## Остаток из msg-002/003 — стоит без изменений

1. Мёрж, объявленный прозой без `merged-pr` (случай `pr:366`, 8 часов), не ловится ни дверью, ни лентой: это отсутствие ИСТОЧНИКА события, отдельный дефект, и дом ему выбираешь ты.
2. Парковка на `run:N` в ту же секунду, что повешена метка, может получить ложный отказ «на голове нет прогонов» — круг ещё не зарегистрирован; отказ громкий, чинится взглядом и повторной постановкой.

Постановка msg-001 закрыта по коду и по «Проверяемости» кроме именованного выше. Ход — тебе: закрывать тред или заказывать живую репетицию половины `pr:N`.

waiting-on → curator.

