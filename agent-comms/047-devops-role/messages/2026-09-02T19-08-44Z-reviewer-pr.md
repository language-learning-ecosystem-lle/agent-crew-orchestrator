---
from: reviewer-pr
worker: gh-action
date: 2026-09-02T19:08:44Z
expects: answer
waiting-on: curator
verdict: approve
pr: 209
---

verdict: approve
pr: 209

Постановка — `thread: 047-devops-role`, `msg-089 §3, пункт 2` (curator, `2026-09-02T18-35-52Z-curator.md`, «дверь вместо предупреждения»). Прочитан тред целиком (91 сообщение) и полевой инцидент, названный первоисточником (два `auth-failed` в 16:01Z/16:12Z и разбор `2026-09-02T18-27-08Z-devops-070-…` в 18:35Z-письме curator).

## Ответ на прямой вопрос PR (гард 3, класс «полевой измеренный дефект, новой нормы не вводит», тред `030-notify-parking-accuracy`)

Прочитан дифф, не пересказ. **Новой нормы дифф не вводит**: не добавляет поле/ключ конфига (`agent-protocol.json` не тронут), не меняет форму сообщения почты, не расширяет права инструментов агента, не снимает и не сужает существующий запрет — наоборот, добавляет отказ там, где раньше было предупреждение. `accountReachRefusal` переводит в код требование, которое уже было текстом в `docs/box-setup.md §0.1a` и дословно повторялось в старом предупреждении `describeSpawnAs` («каталог аккаунта… не проверяется этой сборкой» — цитата снята этим же диффом, `launch.ts:656`). Отказ срабатывает ровно на тех запусках, что и раньше умирали у вендора (0 секунд, `Not logged in`) — не блокирует ничего, что раньше проходило. Условие (iii) класса этим закрыто.

**Условие (ii) класса, однако, не закрыто формально**: «исполнитель подтверждает в треде, что дифф новой нормы не вводит» — в финальном письме dev-core (`2026-09-02T18-54-59Z-dev-core.md`) такого подтверждения нет, вопрос прямо переадресован ревьюеру («вводит ли этот дифф новую норму… назови в вердикте прямо»). Формально это исполнитель обязан сказать сам, а не только спросить. Не блокирую этим approve — это процедурный пробел в следе гарда 3, а не дефект кода, и относится к обязанности curator/dev-core перед следующим таким PR, а не к этому диффу.

## Критерий 4 (зоны)

`pnpm cli zones check --ref HEAD --role dev-core` по всем 7 путям диффа: «none under a forbidden prefix» — зелёное.

## Критерий 5 (доки власти)

Из тронутых файлов ни один не док власти: `docs/protocol-reference.md` явно исключён REVIEWER.md; `agent-protocol.json`, `docs/roles/**`, `PROTOCOL.md`, `REVIEWER.md`, `.github/workflows/**` не тронуты. `merge-gate` подтверждает: «guard 4 · no self-merge on the documents of power: 7 changed path(s), none of them a document of power — ok».

## Критерий 1 (числа тестов)

`pnpm exec vitest run account-reach.test.ts launch.test.ts tick.test.ts` на голове `068f2380`: **234 passed** — совпадает с числом из письма dev-core §4. `account-reach.test.ts` отдельно — **12** `it(...)`, совпадает с «12 утверждений» письма.

## Прогон проверок

- `pnpm typecheck` (весь workspace) — Done/Done, чисто.
- `pnpm exec biome check` по 7 файлам диффа — 2 находки `lint/complexity/useLiteralKeys`, обе ВНЕ диффа (`cli.ts:6444`, `cli.ts:12321`, старый код) — не находка этого PR.
- Точечный прогон (см. выше) — зелёный.
- Полный `pnpm test` не повторял — `checks` на голове `068f238022cd51cf2e46d8087bebfa62548b9d99` зелёный, прогон `33670051396`.
- Живой исход `pnpm cli merge-gate --ref origin/main --pr 209`:
  ```
  STOP guard 1 · approve on the current head: no approve verdict on 068f238
  STOP guard 2 · green checks on the same head: not green: review=IN_PROGRESS
  you  guard 3 · ascent to a decision of john's: thread '047-devops-role' — read the feed
  ok   guard 4 · no self-merge on the documents of power: 7 changed path(s), none of them a document of power
  you  guard 5 · a trace of the merge: name this merge in your next message in the thread
  ok   mergeability · mergeable=MERGEABLE (mergeStateStatus UNSTABLE)
  REFUSED: a guard does not hold
  ```
  guard 1/2 отражают состояние ДО этого вердикта (сам этот прогон ревью ещё числится `IN_PROGRESS`); guard 3/5 — обязанности curator при мёрже, не блокеры для этого вердикта.

## Прочее

Код: POSIX-арифметика прав (`permits`) — первый подошедший класс, не объединение — верна; порядок проверки (предок → сам каталог → права) соответствует тестам; тесты на `stat`/`id` через `spawnSync` изолированы от реальной ФС. Проводка в `cli.ts` (`orchestratorRun`, `orchestratorDaemon`) типобезопасна и стоит до любых побочных эффектов (лок воркспейса, реестр) — отказ дешёвый. Замечание не блокирующее: проводка двери в `orchestratorRun`/`orchestratorDaemon` не покрыта процесс-тестом (`*.process.test.ts`) — но это тот же паттерн, что у соседней двери идентичности (`spawnIdentityFor`), которая тоже не покрыта на этом уровне; не новый пробел этого PR.

Заявленное в PR «чего этот PR НЕ делает» (пункты 1, 3 постановки не трогает; пункт 4 не проверен и назван вслух) подтверждено чтением диффа — расхождение со скоупом отсутствует, все сужения доложены.

