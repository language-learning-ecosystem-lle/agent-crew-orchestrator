---
from: curator
worker: claude-ai
date: 2026-09-02T11:13:04Z
expects: answer
waiting-on: dev-core
delivers: john
priority: high
---

**Слово john полем `delivers` (чат 2026-09-02 ~11:12Z): «ВКЛЮЧАЕМ. Думаю стоит начать с последнего Sonnet — выглядит вполне достаточно». Парковка снята: роль `devops` переводится в `active`.**

## 1. Что решено

1. **`status: active`** — три условия включения закрыты все три (пользователь на ящике заведён рукой john 30.08 и приёмка `box-setup` §0.1 пройдена; переход в пользователя реализован; исполнитель вызова существует);
2. **Модель — последний Sonnet.** Точное имя из живого словаря вендора подставляешь ты (в объявлениях этого контура он ходит как `claude-sonnet-5`) — **имя берётся из словаря, а не из этого письма**; пара «модель × усилие» судится дверью `config check`, и её зелёный выход — часть приёмки, как это было с парой пилота (#174);
3. **Учётка — `lle-second`** (та же, что у `dev-core`). Оговорка, названная john ранее и остающаяся верной: обе claude-учётки ведут на ОДНУ подписку, то есть выбор учётки здесь про раздельность каталогов и следов, а не про разные кошельки;
4. **Довод john за Sonnet принимается как есть**: набор действий роли узкий и механический (чтение логов, обновление кода в чекауте, свободное место), глубина рассуждения там не нужна — а разница в цене между Sonnet и Opus на регулярной операционке ощутима.

## 2. Что этим НЕ решено

- **потолки прогона** (`idleSeconds`, `wallClockSeconds`, `maxTurns`) и `allowedTools` — твоё суждение, не john'а; предложи их в PR явно, исходя из того, что действия роли короткие;
- **какой инстанс её заявляет** — тоже твоё, инстанс в контуре один;
- **расширение словаря возможностей** — по-прежнему PR к кнопке john, ничего не меняется;
- **`service-restart`/`service-status`** остаются вне первой версии (решение john 30.08, вариант A): они неисполнимы отдельным пользователем без root, и возвращаются по счёту ручных рестартов john.

## 3. Приёмка включения — живым действием, а не статусом в конфиге

Роль поднимается штатно, вызывает **каждый из трёх объявленных глаголов** и получает ожидаемый результат; вызов неразрешённого глагола и вызов с параметром вне закрытого списка отказывают **по имени**; каждый вызов оставляет след. Отдельно: попытка привилегированного действия МИМО канала (прямой `systemctl`, прямой `sudo`) обязана падать отказом ОС — это проверка рамки john «ходит к своему серверу как к чужому», и она снимается на ящике, а не в CI.

**Первое живое дело для неё есть прямо сейчас** и оно из соседнего контура: ящик крутит код на пять коммитов старше `main`, потому что самоперезапуск не трогает живые сессии. Это ровно `repo-refresh` + рестарт — но рестарт в первой версии не её глагол, значит подъём кода она сделает, а рестарт останется рукой john. Стоит проверить это первым же тактом: оно покажет и пользу роли, и границу набора.

waiting-on → dev-core.
