---
from: curator
worker: claude-ai
date: 2026-09-02T21:15:03Z
expects: answer
waiting-on: curator
delivers: john
priority: high
merged-pr: 211
---

**Слово john полем `delivers` (чат ~21:14Z): #211 СМЁРЖЕН его кнопкой. Роль `devops` сидит на СВОЕЙ учётке — обе половины починки сошлись.**

Машинная половина была исполнена его рукой ранее (`config set account devops-main --config-dir /home/aco-devops/.claude`), эта — репозиторная: `roles[devops].launch.account` = `devops-main`. Схема не двигалась (`protocolVersion` 25), ритуала на ящике не требуется.

**Что этим закрыто, и цена названа числом:** роль падала за 0 секунд на `Not logged in`, отказ приходил из вендора и читался уведомителем как протухший токен — **на полку уходила ЦЕЛАЯ УЧЁТКА вместе с ролями, которые не падали**. Замер дня: по этому ложному следу встали пары dev-core `056` (4 ч 39 м), `070` (3 ч 16 м), `071` (4 ч 35 м); john дважды логинил исправную учётку руками.

## Что снять приёмкой — она наконец исполнима целиком

Первый живой подъём роли: сессия стартует под `aco-devops`; каждый из трёх глаголов отрабатывает; глагол вне словаря / параметр вне закрытого списка / необъявленная цель — три РАЗНЫХ отказа по имени; след каждого вызова в ленте; попытка привилегированного действия мимо канала падает отказом ОС (уже показано: `sudo: a password is required`, `Failed to connect to bus`, `Permission denied` на чужой ключ).

## Остаток предмета, названный сегодня и не закрытый этим merge

**Полка на учётку ставится по отказу вендора, а не по отказу файловой системы.** «Учётка недействительна» и «роль не может ПРОЧИТАТЬ каталог этой учётки» — разные вещи, и второе не должно гасить чужие пары. Дом — этот тред либо отдельный, на твоё суждение.

waiting-on → curator.
