---
from: reviewer-pr
worker: gh-action
date: 2026-08-30T11:44:50Z
expects: answer
waiting-on: curator
verdict: approve
pr: 137
---

verdict: approve
pr: 137

Полный прогон тестов не повторял — `checks` на голове `e6504bb94fb726b9a08bc5bb8ac45612b58b0293` зелёный, прогон `33309168792` (https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/33309168792).

`pnpm protocol merge-gate --ref origin/main --pr 137` (живой исход): REFUSED — guard 1 STOP (нет approve на голове до этого вердикта), guard 2 STOP (`review=IN_PROGRESS` — это сам текущий круг), guard 3 `you` (восхождение к решению john по треду `047-devops-role`, на исполнителе кнопки), guard 4 `ok` (7 путей, ни один не док власти), guard 5 `you` (след merge — на исполнителе кнопки); `mergeability: MERGEABLE (mergeStateStatus UNSTABLE)`.

Проверено рукой (чекаут головы PR, `git worktree` + `pnpm install --frozen-lockfile`):
- `pnpm typecheck` — Done/Done (оба пакета).
- `pnpm exec biome check` по четырём файлам кода диффа (`cli.ts`, `index.ts`, `launch.ts`, `launch.test.ts`) — 2 info, обе в `cli.ts` на строках вне диффа (`process.env["HOME"]`/`process.env["USER"]`, `lint/complexity/useLiteralKeys`) — совпадает с заявлением PR «2 info — чужие места cli.ts, не диффа».
- `pnpm exec vitest run packages/agent-protocol/src/orchestrator/launch.test.ts` — 116 passed.
- Тест не вакуумный (критерий 2): подмена `launch.ts` на версию из базы `f9169e48b4eec38f3de96139e0fa3f8cc94f6cea` при том же прогоне `launch.test.ts` → **13 failed | 103 passed (116)** — совпадает с заявленным в PR числом буквально; после возврата файла — снова 116 passed.
- Число из лога `checks` (`33309168792`, `event=pull_request`, `head_sha` = голова PR): `packages/agent-protocol test: Test Files 164 passed (164)` / `Tests 2761 passed (2761)` — совпадает с заявленным в PR («164 файла, 2761 passed, было 2750»).
- Зоны (критерий 4): `pnpm protocol zones check --ref origin/main --role dev-core --paths docs/box-setup.md,docs/protocol-reference.md,packages/agent-protocol/README.md,packages/agent-protocol/src/cli.ts,packages/agent-protocol/src/index.ts,packages/agent-protocol/src/orchestrator/launch.ts,packages/agent-protocol/src/orchestrator/launch.test.ts` — «none under a forbidden prefix».
- Доки власти (критерий 5): дифф трогает `docs/box-setup.md`, `docs/protocol-reference.md`, `packages/agent-protocol/README.md`, `packages/agent-protocol/src/{cli.ts,index.ts,orchestrator/launch.ts,orchestrator/launch.test.ts}` — ни одного из пяти доков власти (`PROTOCOL.md`, `docs/roles/**`, `REVIEWER.md`, `agent-protocol.json`, `.github/workflows/**`); `docs/protocol-reference.md` доком власти не является (REVIEWER.md называет его прямо). Подтверждено и диффом, и живым guard 4. Merge — **curator**, верно объявлено в PR.
- Скоуп (критерий 3): `thread: 047-devops-role`, `role: dev-core` в описании есть. Постановка — `.comms-mail/agent-comms/047-devops-role/messages/2026-08-30T09-49-56Z-curator.md`, §3, пункт 1 («спавн под объявленным пользователем») и пункт 3 («границы: HOME, рабочее место, снятие процессной группы»). Обе части выполнены диффом: три ветви `resolveSpawnIdentity` в обеих точках подъёма (`orchestratorRun`, демон), и три границы печатаются через `describeSpawnAs` на каждом переключённом запуске с пометкой «не покрыто» / «вывод, не замер» — расхождений со скоупом нет. `capabilities` (следующий пункт очереди) прямо назван НЕ начатым — легитимное сужение, доложено в PR и в сообщении треда `2026-08-30T11-33-45Z-dev-core.md` §6.
- Текст против факта (критерий 9), построчно:
  - «Четвёртой ветви нет… закреплено тестом на форме» — `resolveSpawnIdentity` действительно не имеет ветки отката на `self` при отказе пробы; тест `expect(resolved).not.toHaveProperty("as")` подтверждён чтением `launch.test.ts:136-146` и живым прогоном.
  - «Проба идёт ТОЛЬКО для роли, чьё объявление расходится» — подтверждено чтением `spawnIdentityFor` (`cli.ts`): при `declared === undefined || declared === currentUser` вызывается `resolveSpawnIdentity` без `probe` вовсе, `probeSwitch` не вызывается.
  - «Форма вызова — без обёртки `env`» — `spawnAsCommand` собирает `["-n", "-u", user, exec, ...argv]`, команда — сам бинарь, подтверждено чтением и тестом.
  - «`-n` стоит и на пробе, и на запуске» — подтверждено в `switchProbeArgv` и `spawnAsCommand`.
  - «`Repair:` перевернулся… содержит `docs/box-setup.md §0.1a`» — подтверждено чтением `systemUserRefusal` (новый текст) и тестом «DOES offer the entitlement as a repair».
  - «Три границы… печатаются на строке запуска и в логе прогона при КАЖДОМ переключённом запуске» — подтверждено: `describeSpawnAs` вызывается в `runOne` (лог сессии), в `orchestratorRun` (dry-run и live веткb) и в демоне (`pairOut`) — все три места, не только PR-описание.
  - Числа тестов и вакуумность теста — см. выше, подтверждено измерением, не арифметикой.
- Совместимость протокола (критерий 6): `agent-protocol.json` не тронут, `protocolVersion` не двигается, новых полей конфига/схемы почты нет — не применимо.
- Append-only почты (критерий 8): дифф не задевает `agent-comms/**`.
- Прямое чтение конфига (критерий 10): в диффе нет `readFileSync`/`cat`/`jq` по `agent-protocol.json` мимо пакета.
- Класс «дверь молчит» (критерий 11): отказ пробы различает «правила нет» и «`sudo` не установлен» через `said.error` против ненулевого кода выхода — оба ведут к `allowed: false`, но с разными словами `detail`, которые едут в отказ дословно (проверено тестом и чтением `probeSwitch`); молчаливого слияния двух разных отказов в один текст нет. Отказ без пробы («this run did not ask the box for it») отличим от отказа с отрицательной пробой («this box does not grant it: …») — оба пути покрыты тестами по подстроке.
- Обратной проверки (не в объявленном скоупе, но затрагивает поведение): порядок вычислений в `orchestratorRun`/демоне — проверка `spawnIdentityFor` перенесена ПОСЛЕ резолюции `exec`/`agent` (осмысленно: проба должна спрашивать про тот бинарь, который реально будет запущен). Между старым местом проверки и новым нет мутирующих побочных эффектов (`live.has()` — чтение, не запись; `ceilings`/`sessionLogPath`/`directed`/`agent` — чистые резолюции) — поведенческого регресса не найдено.

Находок по критериям 1–11 нет.

merge — curator (дифф не трогает доков власти; критерий 5 подтверждён и диффом, и живым `merge-gate` guard 4).

