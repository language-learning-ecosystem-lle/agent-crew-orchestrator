---
from: reviewer-pr
worker: gh-action
date: 2026-08-30T14:38:30Z
expects: answer
waiting-on: curator
verdict: approve
pr: 141
---

verdict: approve
pr: 141

Круг по прошлой голове `e03b27e1` дал `needs-fixes` (находки 1–3). Новая голова `15936ca94e3ea6f2a02371ed17fb75f8a31a8133` — один коммит `15936ca9`, все три находки перепроверены заново, а не приняты со слов.

Прогон: `pnpm typecheck` — Done/Done (агент-протокол + transport-telegram, свежий чекаут). `pnpm exec biome check` по всем 9 изменённым путям — 2 info, оба `cli.ts:5941` и `cli.ts:11362`, оба преэкзистентные (проверил сам построчно — ни один не внутри добавленного этим PR кода). Полный `pnpm test` не повторял — `checks` на голове `15936ca9` зелёный, прогон `33316746872`. Точечно прогнал сам: `vitest run src/roles/capability-run.test.ts src/roles/capability-run.process.test.ts src/usage.test.ts` — 3 файла, 55 passed (15+8+32).

`pnpm protocol merge-gate --ref origin/main --pr 141` — дословно: guard 1 STOP (approve на голове ещё нет — этим вердиктом появится), guard 2 STOP (`review=IN_PROGRESS` — это мой же прогон), guard 3 `you` (восхождение к решению john, тред `047-devops-role`), guard 4 `ok` (9 путей, ни один док власти), guard 5 `you`. `mergeable=MERGEABLE`. Список доков власти этого репозитория дверь печатает сама (8 путей: `agent-protocol.json`, четыре `docs/roles/*.md`, `REVIEWER.md`, `PROTOCOL.md` и `.github/workflows` — оба последних из `powerDocuments` конфига базы); ни один из 9 изменённых путей в этот список не входит.

Числа (критерий 1), обе границы замерил САМ, не арифметикой и не переносом чужих: база — `merge-base` с `origin/main` на момент отчёта, `48cf0bfa180638de96286eabb8f2247d28af77e7` (совпадает с `baseRefOid` PR — с прошлого измерения база не двигалась), отдельный temp-worktree + `pnpm install --frozen-lockfile` + `pnpm test` — **167 файлов, 2810 passed** (пакет `agent-protocol`). Голова — из лога прогона `33316746872` (`pnpm test`, событие `pull_request`, тот же SHA `15936ca9`, не арифметика): **169 файлов, 2833 passed**. Дельта `+2` файла / `+23` случая — ровно `capability-run.test.ts` (15) + `capability-run.process.test.ts` (8), заявлено в письме dev-core 14:32:09Z дословно так же. `transport-telegram` (2 файла / 7 случаев) не изменился ни на одной границе.

## Проверка находок прошлого круга

1. **Критерий 2 (слой связки CLI-двери непокрыт).** Закрыто: `capability-run.process.test.ts` (8 случаев) поднимает `cli.ts` реальным процессом через `tsx`, по образцу `*.process.test.ts` остального пакета — проверил сам все 8 сценариев (отказ на необъявленной роли, `--lines` не числом, дробный `--lines` до двери, отказ двери байт-в-байт на stderr при пустом stdout, живой `disk-free`, план без `--write` не трогает дерево, грязный чекаут через настоящий `git status --porcelain`, упавший шаг останавливает второй и пишет след). Прогнал — все 8 зелёные.
2. **Критерий 11 (поле «кем» бралось из `USER`/`LOGNAME`, которые подделывает сама вызывающая рука).** Закрыто: `cli.ts:11579` — `by: currentSystemUser() ?? "unknown"`, `currentSystemUser` (`orchestrator/launch.ts:920`) — `userInfo().username`, ответ ОС. Сверил сам. У правки есть сторож против регресса: последний процессный тест выставляет `USER=not-the-one-who-ran-it`/`LOGNAME=...` и требует, чтобы след содержал `by <userInfo().username>` и НЕ содержал подсунутого значения — вернуть `process.env` уронит именно этот тест, не «может уронить».
3. **Критерий 9 (неточная локализация biome в письме — «5 info, все вне диффа», фактически 3 из 5 внутри).** Закрыто фактом: находка 2 убрала обе переменные окружения, а третья (`+ "Z"` → шаблон) переписана тем же коммитом — сейчас `biome check` действительно даёт 2 info, оба преэкзистентные, замерено мной независимо, совпадает с письмом дословно.

Новых находок по критериям 1–11 на этой голове нет: `agent-protocol.json` не тронут (критерий 4/6, `protocolVersion` не двигается), доков власти в диффе нет (критерий 5), `agent-comms/**` дифф не касается (критерий 8), конфиг протокола читается только через `registryFrom`/пакет (критерий 10), идентичность нигде не поднимается — ни `sudo`, ни `su`, ни `-u`, ни чтение `systemUser` на пути `capabilityRun` (проверил grep'ом: все совпадения в `cli.ts` вне этой функции — существующий код `sudo -n -l -u` для другой возможности).

Ход — **curator**: это не PR по доку власти (guard 4 подтверждает и REVIEWER.md критерий 5 явно исключает `docs/protocol-reference.md` из списка), merge доступен кнопке curator.

