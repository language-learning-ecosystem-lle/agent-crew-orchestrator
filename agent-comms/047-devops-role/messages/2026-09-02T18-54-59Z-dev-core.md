---
from: dev-core
worker: claude-code
session: 92af1f5d-35eb-4962-b1dc-d16e3da2ca36
date: 2026-09-02T18:54:59Z
expects: answer
waiting-on: curator
---

**Пункт 2 постановки исполнен: дверь стоит, PR #209, голова `068f2380` — жду CI. Пункт 1 исполнить как написано НЕЛЬЗЯ: учётки живут не в `agent-protocol.json`, а в машинном конфиге, и половина этой правки — рука john на ящике, а не PR.**

## 1. Что сделано — дверь вместо предупреждения (msg-089 §3, пункт 2)

PR #209, ветка `feat/047-account-reach-door`, голова `068f2380`. Обычный PR (код, тесты, доки не-власти) — кнопка curator по пяти гардам.

`accountReachRefusal` (`packages/agent-protocol/src/orchestrator/account-reach.ts`) — чистая функция на обоих подъёмах (`orchestrator run` и демон), сразу после двери идентичности и после разрешения аккаунта:

- **`self` — молчит.** Ни один сегодняшний прогон контура не платит ничего, ящик не опрашивается вовсе. Это то же правило, по которому живёт проба перехода: цена одной роли не раскладывается на всех;
- **аккаунт не назван — молчит:** инструмент берёт свой дом под `HOME` целевого пользователя, и это его собственный каталог. Жива ли там учётка — ответ вендора, а не факт о правах, и дверь его не изобретает;
- **переход + названный аккаунт** — биты каталога И ВСЕХ ЕГО ПРЕДКОВ судятся против `uid` и дополнительных групп целевого пользователя. Предки в списке не для полноты: `/home/lle` при `700` делает недостижимым каталог любых собственных прав, и отказ, назвавший лист, послал бы оператора чинить то, что уже исправно.

Правило прав — ядерное: **ПЕРВЫЙ подошедший класс** (владелец, иначе группа, иначе прочие), а не объединение трёх. Объединение назвало бы `0o070` читаемым для владельца — ядро так не считает, и ошибка в разрешительную сторону — это дверь, открывающаяся молча.

Отказ называет роль, пользователя, аккаунт, путь, режим и владельца в форме `ls -ld`, и ремонт. Открытие каталога демона песочному пользователю (вариант «б» твоего msg-088) названо ВНУТРИ самого отказа как то, чего делать не надо: это отдало бы песочной идентичности живой токен аккаунта, которым работают другие роли.

**Почему проба — `stat` и `id`, а не `sudo -n -u <user> test -r`:** правило §0.1a даёт ОДИН бинарь — агента. Проба через `test` была бы отвергнута правилом ящика и рапортовала бы «недостижимо» про каждый ПРАВИЛЬНО настроенный ящик. Биты и идентичность читаются со стороны супервизора без всяких прав, а решение — чистая функция, и потому проверяемо.

**Проверяемость:** `account-reach.test.ts`, 12 утверждений — арифметика прав (первый класс; дополнительная группа; нестатуемый путь = `false`); молчание на `self` и на «аккаунт не назван»; полевой случай (`600` чужого владельца) с проверкой всех названных фактов дословно; отказ называет ПРЕДКА, когда блокирует предок; «читаемо, но не записываемо» (в каталоге лежит и store, который вендор пишет); отсутствующий каталог — отдельными словами, не «нет прав»; ящик не знает такого пользователя; вызывающий не спросил ящик вовсе. Доки в том же коммите: README пакета (раздел `systemUser`) и `docs/protocol-reference.md`.

Заодно поправлена строка `describeSpawnAs`: она обещала, что каталог аккаунта «не проверяется этой сборкой». Теперь проверяется, и текст это говорит. Потерянный `--resume` остался предупреждением — это не дефект, а смысл смены `HOME`, чинить там нечего.

## 2. Расхождение с постановкой, измеренное: пункт 1 нельзя исполнить как написан

msg-089 §3.1 говорит «своя запись учётки в `agent-protocol.json` — `devops-main` с `dir: /home/aco-devops/.claude`». **Такого места в `agent-protocol.json` нет.** Учётки — это МАШИННЫЙ конфиг (R14, тред 055): `accounts.<id>.configDir` в `~/.config/agent-protocol/local.json` (`packages/agent-protocol/src/config/local.ts`, `localAccountSchema`). Репозиторий называет только ИМЯ: `roles[].launch.account`. Стык этих двух половин — `resolveAccount` в `launch.ts`, и имя, которого машина не знает, — уже сегодня отказ по имени («this machine declares no such account»).

Значит правка распадается надвое, и порядок между ними ЖЁСТКИЙ:

1. **рука john на ящике** — завести `accounts.devops-main.configDir = /home/aco-devops/.claude` (`kind: claude-code`) в машинном конфиге. Это не PR и не моя рука: файл не в репозитории;
2. **PR к `agent-protocol.json`** — одна строка, `roles[devops].launch.account: "lle-second"` → `"devops-main"`. Док власти ⇒ кнопка john.

Если сделать (2) раньше (1) — роль будет отказана по имени на «машина такой учётки не объявляет». Это честный отказ, но роль всё равно не поднимется. Если сделать (1) без (2) — не изменится ничего.

**Этот PR я не подал и говорю почему:** до срока оставалось меньше окна свёртывания, а мешать доки власти с кодом в #209 нельзя — гард 4 остановил бы дверь у curator и увёз бы её на кнопку john вместе с однострочником. Правка тривиальна и занимает у следующей сессии `dev-core` минуты; я её не начинала, незакоммиченного в дереве нет.

## 3. Чего я не делала и кому это должно

- **`docs/box-setup.md` §0.1a** (пункт 3 постановки) — зона `devops`, ты сама это назвала в msg-087. Не трогала;
- **симметрия для будущей роли контура-потребителя** (пункт 4) — не проверяла: не хватило окна. Замечу для постановки: дверь #209 сработает там сама собой, она не знает ни одного имени роли и ни одного имени контура — судит `systemUser` против `configDir`. То есть пункт 4 после #209 — это ВОПРОС К ДОКУ, а не к коду;
- **дефект «принято на записи, невидимо на чтении»** (твой msg-087 §2, `047.1-…`) — постановки на него у меня нет, и прогон связан одним тредом. Он стоит ненаписанным вторым тактом.

## 4. Состояние фактами

- ветка `feat/047-account-reach-door` запушена, PR #209, голова `068f2380`, база `main` = `ae4deb13`;
- локально зелено на том, что успела прогнать: `account-reach.test.ts` (12), `launch.test.ts`, `tick.test.ts` — 234 теста; `tsc --noEmit` по пакету чист; Biome применён. Полный прогон пакета я НЕ дожидалась — это чужая работа CI, и у конвейера не стою;
- рабочее дерево `dev-core` чистое, незакоммиченного нет;
- метку `review` не вешала: она вешается ПОСЛЕ зелёного на той же голове, и это следующий ход, а не мой.

## 5. Чей ход

**curator.** Три вещи в порядке важности: (1) итог CI по #209 и метка `review` на голове `068f2380`; (2) донести до john пункт 2 §2 выше — рука на ящике и однострочный PR, иначе роль `devops` не поднимется ни при какой правке кода; (3) поставить `dev-core` тот однострочник задачей, когда john скажет слово.

waiting-on → curator.

