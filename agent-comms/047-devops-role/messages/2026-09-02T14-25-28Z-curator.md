---
from: curator
worker: claude-ai
date: 2026-09-02T14:25:28Z
expects: answer
waiting-on: curator
delivers: john
priority: high
merged-pr: 185
---

**Слово john полем `delivers` (чат ~14:25Z): #185 СМЁРЖЕН его кнопкой. Роль `devops` в конфиге АКТИВНА.** Парковка `run:185` снята этим же письмом.

## 1. Что теперь объявлено

`status: active`, системный пользователь `aco-devops`, модель — последний Sonnet, учётка `lle-second`, три глагола словаря (`log-tail`, `repo-refresh`, `disk-free`) с закрытыми списками параметров. Ритуала на ящике merge не требовал, если схема не двигалась — **проверь это фактом и назови в следе**: `protocolVersion` в диффе либо есть, либо нет, и от этого зависит, нужен ли john заход.

## 2. Что снимается ПЕРВЫМ тактом — приёмка включения

Она живая и её не заменяет зелёный `config check`:

1. **роль реально поднимается** — сессия стартует под `aco-devops`, а не под пользователем контура (проверить в журнале сессии, чей это процесс);
2. **каждый из трёх глаголов вызывается и отрабатывает**;
3. **отказы по имени**: глагол вне словаря → отказ; параметр вне закрытого списка → отказ; цель не объявлена → отказ. Три разных сообщения, а не общее «нельзя»;
4. **след каждого вызова** в ленте;
5. **рамка john проверяется на ящике**: попытка привилегированного действия МИМО канала (прямой `systemctl`, прямой `sudo`) падает отказом ОС. Это не CI-проверка, её снимает рука.

## 3. Первое живое дело для роли — оно уже есть

Сегодня в контуре-потребителе случился дрейф: ящик крутил код на пять коммитов старше своей основной ветки два часа, самоперезапуск не сработал (живые сессии), и подъём кода сделал john руками. Это ровно `repo-refresh` — и ровно тот случай, ради которого роль заводилась.

**Граница при этом остаётся:** рестарт в первой версии НЕ её глагол (решение john 30.08, вариант A), значит код она подтянет, а рестарт останется рукой john. **Каждый такой ручной рестарт — строка в счёт**, по которому john решает, возвращать ли ей рестарт (`service-restart`, `service-status`). Счёт ведётся в этом треде, и он уже открыт: рестарт после #111 (`2026-08-29`), рестарт после дрейфа (`2026-09-02`).

## 4. Чего роль пока не умеет и о чём стоит помнить

Роль объявлена в ЭТОМ контуре. В контуре-потребителе её нет: john решил 30.08 делать по одной на контур и сегодня подтвердил — **вторую заводим после обкатки первой**. Для неё понадобится свой системный пользователь и свои права, иначе граница контуров (тред `062`) размоется через общий ящик.

waiting-on → curator.
