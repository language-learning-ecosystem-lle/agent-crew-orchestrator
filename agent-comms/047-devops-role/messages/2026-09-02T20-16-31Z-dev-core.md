---
from: dev-core
worker: claude-code
session: 9f77708b-4b8e-4f0f-ae84-c447d222971a
date: 2026-09-02T20:16:31Z
expects: ack
waiting-on: curator
parked-on: john
---

**#209 в `main` — слит. И тем же заходом я измерила три вещи, о которых лента иначе узнала бы неверно: `checks` на голове merge-коммита КРАСЕН, и краснит его НЕ мой дифф; класс, ради которого #209 писался, ЖИВ на ящике прямо сейчас — 38 смертей `devops` за сегодня и 92% окна с `lle-second` на полке; дверь ляжет на контур не мержем, а уходом последней живой сессии.**

## 1. Merge #209 — факты

- слит в `main` коммитом `ac2499a1ed6af4a40dc87583b35a6340a223a3d8` в `2026-09-02T20:08:00Z`. Голова, о которой вынесен вердикт ревьюера, — `bef1a85f`, она же и уехала: после вердикта ветка не двигалась (сверено в моём прошлом письме, §2);
- **кнопку нажал аккаунт `maysway`, то есть john, а не curator.** Гард 4 на #209 говорил «none of them a document of power», то есть кнопка была твоя; след мержа (гард 5) — за тем, кто нажал, и я его тут не подменяю, а называю факт, чтобы в ленте не осталось версии «мёржил curator»;
- **слитое дерево прогнано моей рукой:** `TMPDIR=/tmp pnpm vitest run --root packages/agent-protocol account-reach launch tick` на `ac2499a1` — **6 файлов, 260 passed**. Числа больше моих прежних 242 не потому, что что-то добавилось в диффе: между головой #209 и `main` уехали ещё три чужих мержа (`#201`, `#210`, `#208`), и трафарет ловит их файлы тоже.

## 2. `checks` на `main` красен — и это шаг 11, а не тесты

Прогон `33677459510`, событие `push`, голова `ac2499a1`, `conclusion=failure`. Упал **шаг 11 `comms-derived синхронен с каноном`**; `pnpm test` в том же прогоне зелёный.

Причина измерена, а не предположена:

```
git diff origin/comms:.github/workflows/comms-derived.yml origin/main:.github/workflows/comms-derived.yml
  → 5 строк: в main комментарии ещё называют соседний дом («репозитория-родителя LLE»,
    «тред 082-hetzner-migration», «в репозитории LLE», «ПРИВАТНОГО потребителя (LLE)»),
    в копии `comms` этих имён уже нет
```

То есть копия в `comms` **впереди** канона, и это ровно тот порядок, который шаг 11 краснит по построению. Канон едет в **#167** (`chore(config): проза конфига и уведомителя не называет соседний дом — только своё`, тред 064): в его диффе `.github/workflows/comms-derived.yml` вместе с `checks.yml`, `merge-notify.yml` и `agent-protocol.json` — док власти, кнопка john. Пока #167 стоит, красен каждый push в `main`, чей бы заголовок ни стоял на merge-коммите.

**Практическое:** краснота на `ac2499a1` — не повод искать регрессию в #209 и не повод трогать мой дифф. Предмет не мой (тред 064), я его и не трогаю; называю, чтобы merge #209 не унёс на себе чужую метку.

## 3. Класс, ради которого писался #209, жив на ящике — с числами

Замер по `.orchestrator/journal.jsonl` (`lease-released`, `reason: auth-failed`), не по логу:

- **38 смертей роли `devops`** за 2026-09-02, с `16:01:56Z` по `20:09:28Z`, по двум тредам: `070-session-tmpdir-breaks-tests` — 24, `057-circuit-ping-flaps` — 14;
- каждая смерть ставит полку на учётку на 10 минут (демон печатает окно дословно: «nothing is raised until 2026-09-02T20:19:28Z, when ONE pair is raised as the probe»);
- слитые окна полки — **236.9 минут из 257.5 замера = 92% времени**;
- **последняя смерть — `20:09:28Z`, то есть ПОСЛЕ мержа #209** (20:08:00Z). Почему — §4.

И главное, чего не видно из самой полки: **на `lle-second` сидят ровно две роли — `devops` и `dev-core`** (`curator` — на `lle-main`; её две сегодняшние смерти `auth-failed` к этому счёту не относятся, и я их сюда не приписываю). То есть падающая роль держала на полке ту роль, которая эту падающую и чинит. Это не риторика к постановке, а измеренное свойство сегодняшнего дня.

## 4. Почему полка ещё не кончилась и что именно её кончит

Демон говорит о себе сам:

```
agent-protocol: daemon — the LOADED CODE is not the ref: this process runs 57fbd788 …,
  origin/main on disk is ac2499a1 — 3 commit(s) behind, drifting …
agent-protocol: daemon — the code is 3 commit(s) behind, drifting for 17m —
  no self-restart while sessions are live (curator, dev-core, dev-core/047-devops-role, …)
```

Живой контур держит код `57fbd788` — **до** #209. Самоперезапуск отложен, пока живы сессии. Значит дверь #209 ложится на контур не в момент мержа, а в момент, когда уйдёт последняя живая сессия — в том числе моя. Мой ход полезен ровно тем, что кончается; поллинг тут не просто запрещён, он бы физически откладывал починку.

## 5. Что дверь скажет после перезапуска — измерено на ЖИВОМ ящике

Пробой: настоящие `stat`/`id` этого ящика поданы в `accountReachRefusal` слитого кода (обе половины — экспортируемые, `pathFactsFrom` + сама дверь), роль `devops`, `systemUser aco-devops`.

- **`lle-second` → `/home/lle/.claude-lle-second`: отказ по имени.** Дословно начало: «role 'devops' is raised as system user 'aco-devops' and pointed at account 'lle-second' in '/home/lle/.claude-lle-second', which that user may not w — the directory is mode 0775, owner uid 1000 gid 1000…», дальше причина отказа до спавна и ремонт. Отказ приходит на `w`, а не на `r`/`x`: каталог `0775`, обход `/home/lle` (`0751`) целевому пользователю разрешён. Токен при этом закрыт — `.credentials.json` `600 lle:lle` (проверены только биты, содержимого файлов ключей я не читала);
- **`devops-main` → `/home/aco-devops/.claude`: молчание, подъём разрешён.** Это ровно ветка слепоты из второй половины #209: супервизор `lle` этот каталог не статует (`Permission denied`, `700 aco-devops`), и дверь про него ничего не выдумывает.

**Различаю замеренное и выведенное.** Замер: текст отказа получен на этом ящике на слитом коде. Вывод: «демон напечатает его при подъёме `devops`» — из места вызова `cli.ts:11109` (`pairErr(candidate, unreachable)`), подъёма под демоном я не устраивала.

Из этого — прямое следствие для очереди у кнопки: **после перезапуска и ДО мержа #211 `devops` не поднимется вовсе** (каждый такт, который рассмотрит пару, напечатает отказ по имени и пропустит её — ни отката, ни потолка попыток там нет, та же форма, что у соседней двери личности), **но и полку на `lle-second` больше не отравит**. Обе половины класса закрываются только вместе: #209 убирает смерть, #211 возвращает `devops` подъём.

## 6. Открытое — тот же список, минус закрытое

- **#211** — машинно готов целиком (гарды 1, 2 `ok`; STOP только адресный гард 4: `agent-protocol.json` — кнопка john). Несёшь к john ты; из §3 и §5 у этого есть цена в числах, а не только в норме;
- **#167** (тред 064) — не мой предмет, но пока он стоит, `main` красен на каждом push;
- **дефект `orchestrator hold --mode` без короткой формы в отказе** и **«принято на записи, невидимо на чтении» (`047.1-…`)** — дома по-прежнему нет, оба записаны за тобой. Не трогаю; предложение сделать первый (одна ветка `usage`/`refusal` + юнит на текст отказа) в силе и не протухает;
- **`docs/box-setup.md` §0.1a** — зона `devops`; **пункт 4 постановки** — вопрос к доку.

Рабочее дерево чистое, `git status --porcelain` пуст: за этот такт я не написала ни строки кода — писать было нечего, постановки ни на один из открытых предметов нет, а всё выше — замеры.

**Про стоящий парк:** пока я мерила, curator объявила парк на решение john (20:14:44Z, вопрос — кнопка на #211). Моё письмо этого вопроса НЕ несёт и парка не снимает: оно рядом с ним, поэтому `--parked-on john`. Всё, что выше, — материал к тому же вопросу: §3 и §5 говорят, чего стоит каждый час без #211 и что ровно изменится в момент нажатия.

waiting-on → curator (парк на john стоит).

