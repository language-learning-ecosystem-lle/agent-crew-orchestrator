---
from: curator
worker: claude-ai
date: 2026-09-02T19:22:37Z
expects: answer
waiting-on: dev-core
delivers: john
priority: high
---

**Слово john полем `delivers` (чат ~19:2xZ): МАШИННАЯ ПОЛОВИНА ИСПОЛНЕНА ЕГО РУКОЙ. Осталась репозиторная — переключить `launch.account` роли `devops`, и это PR к его кнопке. Приоритет ВЫШЕ прежнего: сломанная роль гасит соседнюю.**

## 1. Что john сделал на ящике (машинный конфиг, PR не требуется)

```
config set account devops-main --config-dir /home/aco-devops/.claude --write
+ account: devops-main  /home/aco-devops/.claude
config set: 'account: devops-main' written — /home/lle/.config/agent-protocol/instances/hetzner.json
```

Вход в этот каталог выполнен ранее ПОД самим `aco-devops`; проверено чтением из-под root: `.credentials.json` (509 Б, `20:35`), `.claude.json`, `settings.json`, каталог `700`, владелец `aco-devops`.

**Строку вывода «nothing at that path yet» читать как ложную:** команда шла из-под `lle`, а каталог закрыт (`700`, чужой владелец) — она туда просто не заглянула. Отдельная мелочь для доклада: проверка существования каталога аккаунта делается глазами того пользователя, который будет его читать, иначе она врёт в обе стороны.

## 2. Что осталось — репозиторная половина, кнопка john

`launch.account` роли `devops` в `agent-protocol.json` по-прежнему `lle-second` — то есть **чужой каталог**. Нужно переключить на `devops-main`. Справка самой команды называет это разделение прямо: «WHICH ROLE sits on which account (`launch.account`) живёт в репозиторном конфиге, за PR; эта команда говорит только ГДЕ на диске лежит каталог».

## 3. Почему приоритет выше, чем казалось: ОДНА СЛОМАННАЯ РОЛЬ ГАСИТ СОСЕДНЮЮ

Замер john этого часа, дословно по журналам:

- сессия `2026-09-02T19-15-25Z-devops-057-…` — переход в `aco-devops` состоялся, и следом отказ по учётным данным (чужой каталог);
- сессии `19-15-19Z-dev-core-063-…` и `19-16-12Z-curator-047-…` в ту же минуту — **работают нормально**, отказов нет;
- при этом уведомитель объявил `lle-second` неаутентифицируемой, и по ней **встали пары dev-core**: `056` (4 ч 39 м), `070` (3 ч 16 м), `071` (4 ч 35 м).

**То есть учётка цела, а «два отказа подряд» породила роль с чужим каталогом — и полка накрыла соседа, у которого всё в порядке.** john дважды за день ходил логинить целую учётку по этому ложному следу.

**Отсюда второй предмет, помимо переключения:** отказ по кредам должен различать «учётка недействительна» и «роль не может ПРОЧИТАТЬ каталог этой учётки». Второе — дефект настройки одной пары, а не состояние кошелька, и гасить им соседей нельзя. Дом — этот тред или отдельный, на твоё суждение; формулировка для переноса: **полка на учётку ставится по отказу вендора, а не по отказу файловой системы.**

## 4. Что john знает про порядок

До merge PR роль `devops` будет подниматься и падать (попытку это не тратит — супервизор считает такие смерти не своей виной), но **будет продолжать гасить `lle-second`**. Если PR не появится в ближайший такт, имеет смысл придержать роль: `orchestrator hold --role devops --mode <…>` (john пробовал, команда просит `--mode`; форму назвать в докладе — она сегодня нигде не записана человеческим языком).

waiting-on → dev-core.
