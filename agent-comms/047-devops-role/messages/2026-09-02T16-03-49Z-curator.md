---
from: curator
worker: claude-ai
date: 2026-09-02T16:03:49Z
expects: answer
waiting-on: curator
delivers: john
priority: high
---

**Слово john полем `delivers` (чат ~16:03Z): СТРОКА §0.1a ПОСТАВЛЕНА ЕГО РУКОЙ, приёмка под новым пользователем снята и пройдена. Парковка msg `14-30-12Z` снята: из трёх рук закрыты все три.**

## 1. Что исполнено

**Рука 1 — правило `sudoers`.** `/etc/sudoers.d/aco-devops-spawn` создан через `visudo`, содержимое совпадает с §0.1a байт в байт (обе строки: `env_keep` и узкое право). Проверки со стороны демона:

- `sudo -n -l -u aco-devops <claude>` → правило показано, пароль не спрошен;
- `sudo -n -u aco-devops -- <claude> --version` → `2.1.233 (Claude Code)` — проход и исполнение живые.

**Руки 2 и 3 — чекаут на `main` и рестарт** — исполнены тем же john в ~15:04–15:05Z (см. тред `078`): контур снят с чужой ветки `core/gate-checks-from-actions`, демон поднят на `e28a64b5`. Живой конфиг с этого момента читает `devops.status: active`.

**Приёмка §0.1a под самим `aco-devops`, вывод дословно:**

```
uid=1001(aco-devops) gid=1001(aco-devops) groups=1001(aco-devops),1002(contour)
sudo: a password is required
Failed to connect to bus: No medium found          ← systemctl --user чужого юнита
cat: /home/lle/.ssh/github: Permission denied
```

Четыре факта: групп `sudo` нет; `sudo` отказывает; управление чужими пользовательскими юнитами недоступно (**ожидаемо при составе (A)**, а не дефект); приватный ключ контура не читается. **Рамка john «ходит к своему серверу как к чужому» держится делом.**

## 2. НАХОДКА, которая важнее приёмки: «дверь держит отсутствие пароля» больше не правда

Две проверки из таблицы §0.1a (`sudo -n -u aco-devops -- env` → отказ; `sudo -n -u root true` → отказ) **ПРОШЛИ вместо отказа**. Разбор — и он не про наше правило:

```
User lle may run the following commands on lle-hetzner:
    (ALL : ALL) ALL                                    ← членство в группе sudo
    (aco-devops) NOPASSWD: /home/lle/.nvm/.../claude   ← наше узкое правило
```

**Наша строка корректна и узкая.** Шире — старое общее право пользователя контура, известное с 2026-08-29 (тред `048`, `box-setup` §0.2). Тогда было записано: «дверь здесь держит отсутствие пароля в руках сессии, а не механизм протокола». **Сегодня john ввёл пароль в этой же ssh-сессии (`[sudo] password for lle`), и с тех пор он закеширован** — то есть в окне кеша любая команда под `lle` могла выполнить что угодно от кого угодно, включая root.

**Следствие для `048`:** довод усилился и стал измеренным. Дверь держится не отсутствием пароля, а тем, что его обычно никто не вводит; в день, когда владелец работает руками на ящике, её нет вовсе. Условие john «сужать после того, как вторая идентичность покажет, что работает» — **выполнено**: она показала (§1). Предмет `048` возвращается к john как готовый к шагу 2, и это должно быть донесено туда отдельным письмом — здесь только называю.

**Для приёмки §0.1a практическое следствие простое:** две проверки «правило не расширяется» на ЭТОМ ящике невоспроизводимы под `lle` по построению (общее право перекрывает), и таблицу в `box-setup` стоит поправить — снимать их надо под пользователем без общего права либо после сужения по `048`.

## 3. Что теперь исполнимо и чего john ждёт

**Живая приёмка включения роли (msg-006 §2) — впервые исполнима целиком.** Пять пунктов: роль поднимается под `aco-devops`; каждый из трёх глаголов отрабатывает; глагол вне словаря / параметр вне закрытого списка / необъявленная цель — три РАЗНЫХ отказа по имени; след каждого вызова в ленте; попытка мимо канала падает отказом ОС (§1 уже показывает, что упадёт).

**Развилка о видах дрейфа** (msg-014 §4) — john выбрал **(в) накапливать счёт**: `repo-refresh` чинит только отставание по своей ветке, стоянка на чужой остаётся рукой john; расширять словарь по одному наблюдению до первой живой приёмки он не хочет. Счёт ручных рестартов ведётся здесь и на сегодня равен трём (после #111; после дрейфа 02.09 ~13:4xZ; после #185 — исполнен john в ~15:05Z).

waiting-on → curator.
