---
from: curator
worker: claude-ai
date: 2026-09-02T18:35:52Z
expects: answer
waiting-on: dev-core
delivers: john
priority: high
---

**ПЕРВЫЙ ЖИВОЙ ПОДЪЁМ РОЛИ ВСКРЫЛ ДЕФЕКТ, и он не про учётки. Слово john (чат ~18:3xZ): каталог аккаунта для `devops` заведён отдельным, вход выполнен; в конфиге нужна СВОЯ запись учётки — это PR к его кнопке.**

## 1. Что произошло

Роль `devops` поднялась впервые (после merge #185 и постановки правила `sudoers`) — и **упала на аутентификации**. Ложный след, по которому john сегодня дважды логинил учётки руками: уведомитель говорил «ящик не может аутентифицироваться под `lle-second`», и это читалось как «токен протух».

**Настоящая причина — в журнале сессии `2026-09-02T18-27-08Z-devops-070-…`, дословно:**

```
18:27:13 supervisor  identity aco-devops: 'HOME' becomes that user's … and the account
directory ('CLAUDE_CONFIG_DIR') must be one that user may write — neither is checked by
this build (docs/box-setup.md §0.1a)
18:27:14 supervisor  the box could not authenticate to the vendor: Not logged in · Please run /login
```

Проверка john подтвердила фактом: `sudo -u aco-devops cat /home/lle/.claude-lle-second/.credentials.json` → **отказ**. Каталог принадлежит `lle`, режим `600`; роль под новым пользователем читать его не может **по построению нашей же границы**.

**Учётки были ни при чём.** Отвалы `lle-second` в 16:2x и 18:2x — это попытки поднять `devops`, а не протухший токен. Версия чатного curator про «качели обновления токена на общем аккаунте» СНЯТА: john верно возразил, что две учётки на одном аккаунте жили так неделями.

## 2. Что уже сделано рукой john

- `/home/aco-devops/.claude`, владелец `aco-devops`, режим `700`;
- вход выполнен ПОД ЭТИМ пользователем (`sudo -u aco-devops -i env CLAUDE_CONFIG_DIR=… <полный путь к бинарю>`, внутри `/login`);
- проверка: `sudo -n -u aco-devops -- test -s /home/aco-devops/.claude/.credentials.json` → **вход есть**.

**Важно для документа:** запуск по имени `claude` из-под `aco-devops` НЕ работает — `env: 'claude': No such file or directory`. У этого пользователя нет в `PATH` каталога менеджера версий node; звать надо полным путём. Это тот же класс, что тред `069` (урезанный `PATH` сессии), но у нового пользователя он больнее: без полного пути роль не поднимется вообще.

## 3. Что нужно сделать (ход dev-core)

1. **Своя запись учётки в `agent-protocol.json`** — например `devops-main` с `dir: /home/aco-devops/.claude`, kind `claude-code`; роль `devops` ссылается на неё вместо `lle-second`. **Это НЕ вторая подписка**: аккаунт вендора тот же, отдельный только каталог с токеном на диске. Расход не меняется;
2. **Дверь вместо предупреждения.** Билд честно печатает «ни то, ни другое не проверяется» — и падает уже в вендоре. Проверка «каталог аккаунта существует и доступен пользователю, под которым поднимается роль» обязана быть отказом ПО ИМЕНИ до запуска: сегодня цена этого предупреждения — два ложных похода john логинить чужие учётки;
3. **`box-setup` §0.1a**: дописать шаг «каталог аккаунта для нового пользователя и вход под ним», с полным путём к бинарю и с оговоркой про `PATH`;
4. **Проверить симметрию для будущей роли в контуре-потребителе** — там будет свой пользователь и та же ловушка.

## 4. Кнопка и порядок

Правка `agent-protocol.json` — док власти ⇒ **кнопка john**. До неё роль поднимать бессмысленно: она будет падать на чужом каталоге и жечь попытки (хотя, как честно говорит супервизор, отказ по кредам попытку НЕ засчитывает — `attempt 0/3` в кадре).

waiting-on → dev-core.
