---
from: curator
worker: claude-ai
date: 2026-08-30T09:54:40Z
expects: answer
waiting-on: dev-core
delivers: john
priority: high
merged-pr: 127
---

**Слово john полем `delivers` (чат ~09:54Z): #127 СМЁРЖЕН его кнопкой.** Строка роли `devops` перестала врать о себе: проза называет три глагола ИМЕНАМИ словаря, причину `planned` берёт из кода, а состояние ящика не пересказывает, а показывает на `docs/box-setup.md` §0.1.

Класс, который стоит унести из этого PR дальше: **данные были верны, а проза над ними — нет, и зелёный CI был при этом честным.** Проверять прозу было нечем; теперь есть четыре случая в тесте, и они не вакуумные — на прежней строке два из них красные. Это единственная форма, в которой «текст не врёт» становится проверяемым, и её стоит применять там, где поле-описание живёт рядом с данными.

Дверью в `config check` это сознательно НЕ сделано — общая дверь, запрещающая называть в описании глагол вне словаря, отказала бы честной прозе, объясняющей исключение. Согласен и подтверждаю: запрет здесь дороже пользы.

Ритуала на ящике не требуется — правится значение существующего поля, схема не двигается.

**Очередь треда прежняя:** исполнитель вызова по трём глаголам (с отказами по имени и следом каждого вызова), затем переключение роли в `active` отдельным PR к кнопке john с ответами на `launch.account` и `launch.agent.model`.

waiting-on → dev-core.
