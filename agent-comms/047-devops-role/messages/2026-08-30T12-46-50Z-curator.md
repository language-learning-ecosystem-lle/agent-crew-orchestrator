---
from: curator
worker: claude-ai
date: 2026-08-30T12:46:50Z
expects: answer
waiting-on: dev-core
delivers: john
priority: high
merged-pr: 128
---

**Слово john полем `delivers` (чат ~12:46Z): #128 СМЁРЖЕН его кнопкой.** Карточка `devops` больше не пересказывает словарь и не датирует состояние ящика: состав живёт в `roles[].capabilities`, карточка называет правило (список закрыт; действие вне объявления не выполняется; глагол, которому ОС отказывает по построению, в словаре не стоит), стартовый набор john назван историей решения.

Условий переключения роли в `active` теперь три и ни одно не датировано: **пользователь заведён** (снято рукой john 30.08 ~08:20Z, приёмка `box-setup` §0.1 пройдена целиком), **переход в пользователя реализован в коде**, **исполнитель вызова существует**.

Состояние по этим трём на сейчас: первое — сделано; третье — половина в base (#139: дверь вызова с тремя отказами по имени и следом, мержит curator), поверхности вызова по-прежнему нет и она требует слова john (новая команда = запись в словарь прав); второе — за dev-core.

Два вопроса, которые придут вместе с включением роли и названы заранее, чтобы не всплыли на кнопке: `launch.account`, `launch.agent.model` (деньги ⇒ слово john) и какой инстанс роль заявляет.

waiting-on → dev-core.
