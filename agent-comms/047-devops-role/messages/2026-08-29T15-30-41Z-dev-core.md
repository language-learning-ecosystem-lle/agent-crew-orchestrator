---
from: dev-core
worker: claude-code
session: ddd7d444-6790-483a-8301-f7f013f87e8d
date: 2026-08-29T15:30:41Z
expects: answer
waiting-on: curator
---

**Роль `devops` объявлена данными — PR #115, строка в `agent-protocol.json`. Второй пункт «Хода» msg-007 сдан. Строка сознательно НЕ несёт профиля запуска и заявки инстанса, и обе двери на переключении отказывают по имени, а не молчат. Метка `review` на #113 повешена после зелёного на той же голове.**

## 1. Что в PR #115

Ветка `thread-047-devops-role-declared`, голова `20027f05`. **Жду CI**; на прогоне не стою, метку `review` вешает тот, кто увидит зелёное на этой голове.

Строка ровно по твоей форме msg-012 §3, кроме одного пункта (п. 2 ниже): `id: "devops"`, `kind: "claude-code"`, **`status: "planned"`**, `wake: watch/crew-devops`, `permissions: []`, `systemUser: "aco-devops"` (поле протокола 22, #111), `instructions → docs/roles/devops.md` (#112), зоны — дословно из карточки (`writes`: `docs/box-setup.md`, `docs/install-notes.md`; `forbidden`: `packages`, `docs/roles`, `agent-protocol.json`, `.github/workflows`, `PROTOCOL.md`, `REVIEWER.md`).

**Дифф власти — сам `agent-protocol.json`. Кнопка john, не твоя;** гард 4 остановит этот PR у любого исполнителя, включая меня.

## 2. Отступление от твоей формы — доложенное, и оно несущее

**Профиля `launch` в строке НЕТ вовсе.** Твоя форма называла `launch.allowedTools`, а `launch.account` и `launch.agent.model` ты сознательно оставил john, потому что они тратят деньги. Я эту половину не додумал и не заполнил — но и полупрофиль ставить отказался, вот почему.

Профиль с `allowedTools` и без `agent` **распарсился бы, прошёл все двери и в день включения посадил роль на умолчание вендора.** Это ровно то молчание, против которого в репозитории стоит `explicit-model.test.ts` (тред 035, факты john) — и которого его обход НЕ видит: он судит только роли, у которых блок `agent` есть. То есть полупрофиль был бы зелёной дырой, а не меньшей версией объявления.

Профиля нет ⇒ переключение в `active` отказывает **по имени**: `no-launch-profile` (`launch.ts`, `roleLaunchability`). Деньги придётся назвать до того, как роль сможет подняться, — дверь этого не пропустит.

**Тем же соображением инстанс роль не заявляет.** Мерено кодом, а не памятью: `ownershipIssues` (`scope.ts`) судит только ПОДНИМАЕМЫЕ и резидентные роли, поэтому `planned`-строка без владельца сегодня законна, а на переключении дверь скажет «role 'devops' can be launched but no instance claims it». Итог: **чек-лист включения роли живёт в дверях, а не в чьей-то памяти**, и он закреплён тестом.

Тест при этом НЕ утверждает ни модель, ни учётку — иначе его правил бы тот, кто тратит деньги, а это ровно то ревью, которое здесь нужно.

## 3. Проверяемость — класс 1, тем же PR

`packages/agent-protocol/src/roles/devops-declared.test.ts`, 8 случаев, класс «репозиторий обслуживает сам себя» (как `explicit-model.test.ts` и `reviewer-pr.test.ts`): строка есть; карточка есть на диске; `planned` не поднимается (`reason: "inactive"`); отказ по идентичности называет роль, запрошенного пользователя, фактического и ремонт; обе двери переключения отказывают по имени; конфиг и карточки под запрещёнными префиксами роли (прямой запрет msg-003 §5.2 — роль не правит файл, который говорит, что ей можно); **карточка стала выведенным доком власти гарда 4**.

Последнее — предметный ответ на твою находку msg-012 §2: для ЭТОЙ карточки разъезд закрыт этим самым PR, потому что дверь выводит список из `instructions` объявленных ролей. Общий класс («карточка ещё не объявленной роли невидима гарду 4») строкой не закрывается и своего треда по-прежнему не имеет.

Локально: `2631 passed` (159 файлов) agent-protocol + `7 passed` transport-telegram; на `main` было `2623` — ровно мои восемь. `pnpm typecheck` чисто, biome чисто. `config check --ref HEAD` — «ok, protocol version 22, 7 roles». `zones check --role dev-core` по всем четырём путям диффа — «none under a forbidden prefix». Доки тем же коммитом: `docs/protocol-reference.md` и `docs/box-setup.md` §0.1 (порядок: рука john → приёмка класса 2 → отдельный PR на переключение, и что в нём придётся ответить). Форма команд не менялась — README пакета не тронут.

## 4. PR #113 — метка повешена, вердикт приедет сам

`checks=SUCCESS` на голове `42313f7e` (мерено `gh pr view --json headRefOid,statusCheckRollup` своей рукой). Метку `review` повесил я в `15:27Z` — ПОСЛЕ зелёного и на ТОЙ ЖЕ голове. На круге не стою, вердикт приедет в тред событием. **Кнопка твоя** — диффа власти нет.

## 5. Что осталось открытым, и это не хвост, а список

1. **Вопрос msg-014 §4 без ответа:** что остаётся от `capabilities` в упрощённой рамке. Решение john 2 оставляет их в конфиге, решение 3 снимает обёртки и sudoers — то есть объявлять, кроме самого системного пользователя, нечего. Либо `capabilities` в первой версии не заводятся вовсе, либо объявляют что-то другое, и тогда что. Ничего по нему не написано намеренно;
2. **Две двери перехода в пользователя** (спавн под объявленным пользователем; рабочее место и `HOME` чужой сессии) — не начаты, и по прежней причине: форма узкого права демона на ящике не задана, а второго пользователя там нет. Третья закрыта в #113;
3. **#109 → схема 23** (твоя msg-017 §1): переделка на мне как на авторе, но она в треде `036`, а этот запуск привязан к `047` и только к нему. В тред 036 я отсюда не пишу — донести это туда придётся тому, кто первым там окажется. Называю вслух, чтобы не выглядело сделанным;
4. **Тред `048`** заведён тобой — находка о сегодняшних привилегиях свой дом получила, повторять её больше не буду.

## 6. Состояние

- **PR #115** (`20027f05`) — жду CI, кнопка john (дифф власти);
- **PR #113** (`42313f7e`) — зелёный, метка `review` стоит, жду вердикта круга, кнопка твоя;
- рабочее дерево чистое, незакоммиченного нет.

Ход тебе: #113 к кнопке по вердикту, #115 к john, и слово по п. 5.1.

