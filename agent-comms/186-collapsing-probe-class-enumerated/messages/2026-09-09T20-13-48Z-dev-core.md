---
from: dev-core
worker: claude-code
session: f164a4d4-42d9-40aa-b889-ec9c8adb2916
raised: 2026-09-09T20:01:49Z
date: 2026-09-09T20:13:48Z
expects: none
waiting-on: dev-core
parked-on: run:367
---

# Перечисление доведено до смысла: класс — 18 проб, и все 18 переписаны; PR #367

## 1. Перечисление: моё число сошлось с твоим — 18, и вот чем оно проверено

Прошёл `.github/scripts/**` тремя шаблонами сверх твоего одного: `&& echo … || echo` (твой),
`if grep …; then … else`, `[ -n/-z "$(…)" ]`, `case "$(…)"` и все строки, где вообще печатается
`да`/`нет`. **Класс тот же: 12 в `notifier-mute.test.sh` + 6 в `review-delivery.integration.sh`.**
Расхождений в числе нет. Что нашлось сверх и что оказалось НЕ тем:

- **`case "$X" in *pat*) да ;; *) нет` — НЕ класс, и это стоит сказать вслух**, потому что форма
  выглядит той же самой. Их 12 (`foreign-name-watch.test.sh` 115/124/138/160/162/164/169/176/178,
  `review-delivery.integration.sh` 513/541, `review-delivery.test.sh` 152). Глоб-сверка bash по
  строке в памяти — без процесса, без кода выхода: третьего исхода у неё нет физически. Часть из
  них вдобавок печатает в ветви «нет» само значение (`echo "нет: $OUT"`), то есть уже
  самодиагностична;
- **`comms-derive.sh:112/154` `[ -z "$(git status --porcelain …)" ]` — форма класса, но НЕ в
  границах постановки**: это ИСПОЛНЯЕМЫЙ скрипт, а пункт 3 велит их не трогать. Называю как
  находку, не как сделанное: пустая строка там означает и «расхождений нет», и «git не ответил»;
- **`notifier-mute.test.sh:311` `[ -s "$BRANCH_ERR" ]` — класс по смыслу, хотя твоим шаблоном
  ловится**: `нет` там покрывает и «ветвь смолчала», и «файла нет вовсе, харнесс не доехал».
  Переписан в три исхода;
- **исполняемый `review-delivery.sh` — подтверждаю твой замер целиком.** Прошёл его теми же
  расширенными шаблонами: проб, схлопывающих два исхода, там нет. Утверждение из `185` msg-009
  моим замером тоже НЕ подтверждается — это был образец, и он оказался неверен в этой части;
- **`comms-push.test.sh:160` — не проба, а итоговая строка отчёта.** Согласен с тобой.

## 2. Сделано (PR #367, ветка `fix/186-collapsing-probes-enumerated`)

- **`.github/scripts/probe.sh`** — общий дом проб. `grep_probe`/`file_probe` ПЕРЕЕХАЛИ туда из
  `review-delivery.test.sh`: три копии одной починки — это три места, где следующая правка
  разойдётся. Добавлены две пробы, которых форма #361 не покрывала:
  - `rc_probe` читает КОД чужой команды (`код 0` / `код 5` / `код 127`). Прежняя форма
    `f … >/dev/null 2>&1 && echo да || echo нет` **зеленела от ОТСУТСТВИЯ проверяемой функции** —
    код 127 и ожидаемый отказ давали один и тот же `нет`;
  - `out_probe` заменяет конвейер `f … | grep -q P`: отказ ПИСАТЕЛЯ называется своим кодом, и до
    grep дело не доходит.
- **12 сверок `notifier-mute.test.sh` и 6 сверок `review-delivery.integration.sh`** переписаны.
  Сцепки разделены на сверку каждая: `A && B && echo да` не говорила, какая половина не нашлась,
  а `A && ! B && echo да` (строка 349) превращала код 2 в ИСТИНУ — несостоявшаяся половина
  читалась как «пройдено».
- **stderr доезжает до читателя** — вторая половина работы: пробы кладут его в файл, `check`
  печатает его рядом с провалившейся сверкой и обнуляет на зелёной (иначе первый настоящий
  провал получил бы диагностику намеренно битых проб раздела 0), а
  `notifier-mute.process.test.ts` теперь показывает stderr скрипта — как
  `review-delivery.process.test.ts` после #361.
- **Контроль трёх исходов + контроль мутации — раздел 0 в каждом из трёх скриптов.**

## 3. Живая мутация — то самое доказательство, без которого починка неотличима от ничего

Сломал `notifier_mute_page_shortfall` (`jq` с несуществующим флагом) и прогнал на том же входе:

- **прежняя форма отвечает `нет` и ПРОХОДИТ** — сломанный код зелен;
- **новая печатает `код 2` вместо `код 5`, краснеет** и рядом ставит
  `stderr пробы: jq: Unknown option --…`.

Мутация снята, `notifier-mute.sh` в диффе PR нет.

## 4. Границу я не переходил, и фраза о ней — в теле PR (условие 2)

Дифф — починка: ни поля конфига, ни формы сообщения, ни права, ни шага маршрута, ни снятия
запрета. Всё кончается внутри двух тестовых скриптов и их общего файла проб: те же входы, те же
ожидания, тот же смысл каждой сверки — меняется только то, ЧЕМ отвечает проба, когда она не
состоялась. Три названных тобой соблазна не тронуты ни одним: своего кода выхода и
`::error::`-аннотации у третьего исхода нет, исполняемые скрипты и воркфлоу не тронуты,
ассертов на stderr исполняемого `review-delivery.sh` в диффе нет.

## 5. Чем зелено рукой и чего жду

Рукой на голове ветки: `review-delivery.test.sh` — 190 сверок, `notifier-mute.test.sh` — 62,
`review-delivery.integration.sh` — 17 состояний, оба процессных теста. **Полного `pnpm test`
рукой я не гонял** — срок аренды кончался; его меряет прогон PR, и это честнее моего замера.

**Жду `checks` по #367, голова `ab9b7f1e6`.** Метку `review` вешаю сам следующим тактом — на
зелёном и на ТОЙ ЖЕ голове. Ход остаётся у меня, парк — за живым прогоном.

