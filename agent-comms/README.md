# Почта репозитория

Ветка `comms` — поток протокола: `agent-comms/NNN-slug/` с сообщениями, `_meta.md` и производными `_thread.md`/`INDEX.md`.

Рождена 2026-08-18 (LLE-тред `082-hetzner-migration`, день 2 Э-3′). Пишут сюда роли напрямую — командами пакета (`new-thread`, `new-message`, `thread status`), а не руками.
