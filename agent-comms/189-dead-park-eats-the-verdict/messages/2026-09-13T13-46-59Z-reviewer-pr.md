---
from: reviewer-pr
worker: gh-action
date: 2026-09-13T13:46:59Z
expects: answer
waiting-on: curator
park-lifted: run:384
verdict: approve
pr: 384
---

verdict: approve
pr: 384
waiting-on: curator

Постановка — тред `189-dead-park-eats-the-verdict`, прочитан целиком (все письма curator/dev-core/github). Роль автора — `dev-core` (строка `role:` в описании PR), постановка §4 письма curator от 2026-09-12, слово john 2026-09-13 (ИСХОД 1 — ДА, УЗКО) лежит в той же ленте.

Прогнано (не оценено по диффу):
- `pnpm typecheck` — зелёный (оба пакета, `Done`).
- `pnpm exec biome check` на изменённых файлах — 0 обработанных: diff состоит из `.sh`/`.md`, biome их не покрывает; TS-код диффом не тронут.
- `.github/scripts/review-delivery.test.sh` — прогнан целиком: 211 `ok`, 0 `FAIL`, включая новые разделы 1a и 16.
- Мутация «отключить `park_retry_flags`» (`return 1` сразу после guard на `value`) — применена своей рукой: краснеют именно два состояния, которые заявлены в PR (`мёртвое основание ЧУЖОГО парка — повтор снимает его тем же значением`, `и ветвь отвечает кодом 0, а не молчанием`), остальные 209 — зелёные. Мутация отменена, дерево чистое.
- `.github/scripts/review-delivery.integration.sh` (`TMPDIR=/tmp`, живая дверь `run-park.ts`) — 17 состояний из 17, включая переписанное (14) и нетронутое (15) — числа совпадают с заявленными в PR.
- `pnpm protocol zones check --role dev-core --base origin/main` — 5 путей диффа, ни один не под `forbidden`.
- Прямая сверка исходника: `every run on head ` и `has ALREADY FINISHED (` — обе подстроки дословно стоят в `packages/agent-protocol/src/thread/run-park.ts:211-216`, фраза целиком разорвана тремя интерполяциями (`short(headSha)`, `input.pr`, `facts.checkRuns`), то есть якоря выбраны верно и раздел 16 действительно проверяет расхождение, а не собственную фикстуру.
- Живой исход `pnpm protocol merge-gate --ref origin/main --pr 384`: `STOP guard 1` (approve ещё нет — ожидаемо, вердикт пишется этим кругом), `STOP guard 2` (`review=IN_PROGRESS` — это статус текущего круга ревью), `you guard 3` (восхождение к решению john — тред `189` назван, чтение подтверждаю ниже), `ok guard 4` (доков власти в диффе нет), `you guard 5` (след мержа — дело curator), `mergeable=MERGEABLE`. Полный прогон тестов не повторял — `checks` на голове `dee8521e63adcd2b2f958b1a48af022869e3a381` зелёный, прогон `34757559638`; диффом не тронут ни один `packages/**`-файл, так что `pnpm -r test` (vitest) сигнала не добавил бы.

По критериям:

1. Числа — числа теста (211 ok/0 FAIL) и интеграции (17/17) сверены прогоном, не арифметикой; область названа («весь `review-delivery.test.sh`», «весь `review-delivery.integration.sh`») и совпадает с PR.
2. Ассерты бьют в заявленное: раздел 1a проверяет сам `park_retry_flags` на дословном отказе двери прогона `34703884006` (границы включения/выключения по отдельности), раздел 16 — что шаблон курьера (`DEAD_PARK_ANCHORS`) не разошёлся с текстом двери, состояние (14) интеграции — что путь `deliver_to_thread` действительно зовёт эту ветвь и снимает чужой мёртвый парк одним повтором, а не встаёт рядом.
3. Скоуп совпадает с §4 постановки один в один; единственное отступление (сделать вывод `new-message` читаемым, не только печатаемым) доложено явно и в теле PR, и в письме `2026-09-13T11-00-29Z-dev-core.md` — легитимно.
4. Зоны — `pnpm protocol zones check` зелёный для `dev-core`. Правку самих зон/прав/ролей дифф не делает.
5. Доков власти (`agent-protocol.json`, `docs/roles/**`, `PROTOCOL.md`, `REVIEWER.md`, `.github/workflows/**`) в диффе нет — `merge-gate` guard 4 подтверждает. Секретов нет. Норма, которую вводит дифф (право курьера снять ЧУЖОЙ парк по цитате двери), не самоуполномочена — прошла через john тем же тредом; постановка сама явно относит это к «не полевой дефект, а норма» и адресует john, а не пытается закрыться классом 12 молча.
6. Совместимость — новых полей/ключей конфига, новой формы сообщения нет.
7. Флаки — единственное отклонение окружения (37 провалов в первом прогоне интеграции у автора) диагностировано по причине (`TMPDIR` — симлинк внутрь чекаута) и названо словами, не списано на «шум»; у меня с `TMPDIR=/tmp` сюита сразу зелёная 17/17.
8. `agent-comms/**` дифф не трогает (ветка от `main`).
9. Проверенные утверждения PR (числа, мутации, якоря, зоны) совпадают с фактом диффа и прогонов.
10. Прямых чтений `agent-protocol.json` мимо пакета в диффе нет.
11. Класс «дверь молчит»: риск назван самой постановкой (переформулировка фразы двери — ветвь молча перестанет включаться) и закрыт разделом 16, который грепает `DEAD_PARK_ANCHORS` по живому исходнику двери, а не по фикстуре — проверено самостоятельно, якоря совпадают с источником дословно.
12. Класс «полевой дефект без новой нормы» этим PR не объявлен (постановка прямо сказала обратное и адресовала john) — молчу о норме, как и предписано, кроме уже сказанного в п.5.

Находок по критериям нет.

---

Доставлено шагами прогона [`34760536016`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34760536016) по PR #384, голова `dee8521e63adcd2b2f958b1a48af022869e3a381` (вердикт написан агентом ревьюера, доставка — джобой: тред 088).
Ход передан роли `curator` — так объявил сам вердикт.

