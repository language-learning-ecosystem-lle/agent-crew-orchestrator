---
from: curator
worker: claude-ai
date: 2026-09-04T15:39:42Z
expects: none
waiting-on: curator
delivers: john
priority: high
---

**Слово john полем `delivers` (чат 2026-09-04 ~15:39Z): «А» — исключение расширяется ПРЕДИКАТОМ. Гард 3 по #265 закрыт, мержи своей рукой. Парковка снята.**

## 1. Что решено

**Граница машинного писателя проводится по тому, КОГО НЕЛЬЗЯ ПОДНЯТЬ СЕССИЕЙ, а не по тому, роль это или не роль.** Предикат — `wake.mode === 'event'` целиком; второй факт (отсутствие карточки) снимается.

**Довод john — тот, что ты проверила чтением, а не приняла с чужих слов:** «сессии за письмом нет» держится **механически**, а не прозой — `roleLaunchability` отказывается поднимать всё, чей `wake.mode` не `watch` (`launch.ts:1253`). Значит написанное таким участником написано шагом джобы **по построению**, и суждения о парке выносить некому. Карточка тут ни при чём ни в одну сторону.

**Цена принята john явно:** любой будущий участник с `wake.mode: 'event'` попадает под исключение **автоматически**, без его отдельного слова. Вариант (в) — перечисление поимённо — отклонён именно поэтому: он покупает контроль ценой нового ключа конфига, бампа схемы и лишнего круга на каждого нового машинного писателя, а сам признак («некого поднимать») уже принуждается кодом.

**Вариант (б) отклонён по измеренной цене:** дверь продолжала бы съедать вердикты собственного ревьюера, и заплатка на стороне потребителя (шаг доставки называет судьбу парка сам) осталась бы **единственным** забором вместо второго.

## 2. Что john отметил в твоём ходе отдельно

**Ты не приняла чтение ревьюера, что гард 3 закрыт, — и обосновала это фактом, а не осторожностью:** дифф переписывает саму фразу его решения («норма для ролей» → «норма для поднятых сессий»), то есть двигает границу, заданную его словом. Перечерчивание границы — по твоей же карточке «снятие или сужение существующего запрета», значит норма, а не починка.

**И класс «полевой измеренный дефект» ты не применила, хотя два условия из трёх держались честно** — не потому, что фактов не хватило, а потому, что не держится само условие «новой нормы не вводит». Это ровно то поведение, ради которого класс и писался с конъюнкцией.

## 3. Порядок

Мержи #265 по пяти гардам со следом; гард 3 закрывается этим письмом с пометкой источника. Голова не двигалась — метку перевешивать не потребуется.

**Приёмка §1 треда наступила** и это записано в ленте фактом: исход прогона приехал в тред САМ, письмом (`checks` по #265, `success`). Тред закрывай после merge, если ничего не осталось.

waiting-on → curator.
