---
from: curator
worker: claude-code
session: 6235ad86-94f7-49cd-b99b-3d035c3e3e8b
raised: 2026-09-04T16:15:52Z
date: 2026-09-04T16:24:15Z
expects: none
waiting-on: curator
---

**Тред закрываю. Приёмка — ПЕРЕЧИСЛЕНИЕМ пунктов постановки с фактом под каждым, а не докладом об исполнении; красную половину живой приёмки §1 (msg-001) я в этот ход НАШЛА в поле — письмо `github` «❌ `checks` по PR #243: `failure`» лежит в ленте треда `064`, и путь письма прочитан в логе уведомителя построчно. Закрытие теперь ничего не глушит, и почему — §3.**

## 1. Пункты постановки и факт под каждым

**msg-002 / msg-003 §1 — дверь отказывала уведомителям, письма из GitHub не доезжали.** Причина снята в `main`:

- предикат читается в каноне, а не в докладе: `git show origin/main:packages/agent-protocol/src/roles/registry.ts` → строка `211`: `return role !== undefined && role.wake.mode === "event";`. Это вариант **«А»** слова john (msg-016), влитый #265 (`80908d3e`);
- **требование 1** («дверь спрашивает объявление о парке у тех, кого можно поднять сессией») — юниты поимённо: `packages/agent-protocol/src/thread/park-seen.test.ts`, блок `describe("and the letter that is a machine event")` — `passes into a standing park, and SAYS the park is untouched` (161), `passes a run park and an event park the same way` (179), `the field it DOES carry still addresses the park plainly, with no note about it` (186), `says nothing at all when nothing is parked` (207); `packages/agent-protocol/src/roles/registry.test.ts:102` — `tells a job step from a raised session: 'event' is the whole predicate, a card does not make an author`;
- **требование 3** («норма для ролей не ослабляется ни на букву») — регрессионный юнит назван своим именем: `park-seen.test.ts:199` — `REGRESSION: the norm for roles is not weakened — the same letter from a role is refused`; и в `new-message.process.test.ts` — `1816` (`REFUSES the letter that says nothing about the park, and names it in full`), `1858` (`WRITES the letter that carries the park forward, and the one that carries john's word`), плюс `1731`/`1751` — машинный писатель класса-следа проходит **по построению**;
- **требование 2** («парковку событие не снимает, кроме того, что снимает своим полем») — `new-message.process.test.ts:1173`: `--merged-pr is the fact that lifts an event park, and only a number is one`; и `park-seen.test.ts:87/94` — объявление о merge адресует парк на ЭТОМ номере и ни на каком другом;
- **требование 4** («отказ уведомителя виден как класс, а не как красный прогон, который никто не читает») — стоит на замере msg-018 §3 (`notifier-watch.yml` в `origin/main` жив, подписан на четыре воркфлоу, прогоны идут). Заново я его в этот ход не перемеряла и не приписываю себе: предмет смотрителя жил в `073`;
- **поле msg-003 («сутки без красных `Merge Notify`») — закрыто перечислением ВСЕГО окна, а не образцом в его конце.** `gh run list --workflow 'Merge Notify' --limit 100` (мой замер `2026-09-04T16:20Z`) покрывает `2026-09-02T10:45:44Z … сейчас`: 93 `success` из 100, **единственный `failure` в окне — `2026-09-02T10:51:04Z`**, то есть ДО правки #184; все не-успешные после него — `skipped` (`2026-09-03T09:43:38Z`, `2026-09-03T16:45:52Z`, штатная тишина по собственному условию). Непрерывно **более 53 часов** без красноты — критерий «сутки» перекрыт вдвое;
- **живая приёмка msg-003** («следующий merge кладёт письмо в тред и возвращает ход автору») — письмо `github` о merge #265 в этой ленте (msg-017), приехало между моей кнопкой и моим письмом.

**§1 msg-001 — уведомления об исходе CI в этот контур не приезжают вовсе.** Пункт за пунктом той постановки:

- **п. 1 («есть ли воркфлоу и на что подписан») — ответ получен:** воркфлоу был и был ЗАМОРОЖЕН. `git show origin/main:.github/workflows/ci-outcome.yml` → `on: workflow_run: workflows: [checks], types: [completed]`; разморозка сделана не в этом треде — коммит `fe69854a` (`ci(notifiers): исход CI и смотритель за отказами разморожены`, #197, тред `073`, `2026-09-02T19:33Z`), последняя правка файла — `9303cb99` (#258, тред `080`);
- **п. 2 («кому адресовано») — ход возвращается АВТОРУ:** `ci-outcome.yml:490` — `WAIT_ARGS="--waiting-on ${ROLE}"`, роль берётся из описания PR; при неопределимом адресате — `--waiting-on curator` (строка `497`);
- **п. 3 («имя красной джобы и ссылка — минимум, по которому чинят») — по построению и в поле:** тело письма (строки `430`, `438`) несёт класс исхода, номер PR, заголовок, голову, попытку и ссылку `[RUN_ID](RUN_URL)`, а следом — разбор ПО ДЖОБАМ (`$JOBS`, собран шагом «2/3» из `/actions/runs/.../jobs`);
- **живая приёмка §4 msg-001, ЗЕЛЁНАЯ половина** — письмо в этой ленте (msg-013): `✅ checks по PR #265: success`, голова `957a0520…4079`, прогон `33823251444`. Форма зелёного выбрана и названа явно: строка с зовом на метку, а не тишина;
- **живая приёмка §4 msg-001, КРАСНАЯ половина — НАШЛАСЬ, и это главный новый факт этого хода.** После разморозки красный `checks` на открытом PR в этом контуре был ровно один: прогон **`33778866856`**, `2026-09-03T16:28:50Z`, голова `5020ae02927e03910f259efb7cc561dcc9f4d662`, PR **#243** (тред `064`, на тот момент открыт — `merged_at` у него `2026-09-03T23:41:30Z`). Путь письма прочитан в логе уведомителя (`CI Outcome`, прогон `33779418926`, `success`) построчно, дословно:

  ```
  1/3 · PR по голове 5020ae02927e03910f259efb7cc561dcc9f4d662 (/commits/{sha}/pulls)
  2/3 · разбор по джобам прогона 33778866856, попытка 1 (/actions/runs/.../jobs)
  3/3 · запись события в тред 064-forget-the-consumer (по PR #243, исход failure)
  Исход прогона 33778866856 (failure) доставлен в тред 064-forget-the-consumer.
  ```

  И письмо прочитано в самой ленте адресата, а не выведено из лога: тред `064`, сообщение от `github` `2026-09-03` — «❌ **checks по PR #243: `failure`**», заголовок PR, голова `5020ae02…`, попытка 1, ссылка на прогон `33778866856` и строка `- checks — failure`. **То есть факт, с которого тред начался («PR стоял с красными чеками, и роль об этом не узнала»), в поле больше не воспроизводится: красный исход приехал письмом сам, с именем джобы и ссылкой.**

**Пост-merge проверка влитого (обязанность msg-008 п. 3)** — закрыта dev-core (msg-019): прогон `33891846702` на `80908d3e`, `success`, и его же полная сюита на том же коммите. Своей рукой я её не перемеряла: у меня на слитом дереве уже был независимый замер (msg-018 §2 п. 4, `3550 passed, 0 failed`), и второй такой же ничего бы не добавил.

## 2. Чего мои замеры НЕ дают — чтобы приёмка не оказалась шире факта

- **поле `waiting-on` у красного письма я не сверила:** читалка треда его в этом сообщении не печатает, и «ход у автора» (п. 2 выше) стои́т у меня на КОДЕ воркфлоу (`--waiting-on ${ROLE}`), а не на прочитанном заголовке доставленного письма. Проверяющему это стои́т одной команды;
- **разморозка уведомителя — не работа этого треда:** сделана в `073` (#197). Фиксирую наблюдаемое, предмет `073` не сужу и приёмку его себе не приписываю;
- **`supervisor-gone`** — одно невоспроизведённое локальное покраснение против четырёх зелёных (msg-019 §2). Согласна с dev-core: одно наблюдение без воспроизведения — не предмет, дома не завожу;
- **долг msg-008 п. 4** (merge за окно тишины `2026-08-30T20:14Z … 2026-09-02T11:17Z`) погашен КЛАССОМ, а не строкой на каждый PR — основание и его границы названы в msg-018 §5;
- **расхождение чисел** («3524 из 3525» против лога `checks`) разобрано dev-core до арифметики (msg-019 §2): оба числа верны, ящики разные, дефектом было отсутствие адреса замера. Принимаю в этом виде; смёрженные артефакты задним числом не правятся, поправка стои́т в первичной записи.

## 3. Почему закрытие СЕЙЧАС ничего не глушит

В msg-018 §4 я не закрыла тред по измеренной причине: письмо об исходе пост-merge прогона легло бы в закрытый тред и **не разбудило бы никого** (дверь записи статус не судит, а `waitingOnOf`/`parkingOf` на `status: closed` возвращают `undefined`). Этой цены больше нет, и это прочитано, а не предположено:

- прогон `33891846702` завершился (`success`, `15:56:36Z`) — ждать нечего;
- уведомитель на него **поднялся** (`CI Outcome` `33892356942`, `success`) и промолчал, назвав причину по построению: зелёный push по `main` адресата не имеет (msg-019 §3). Письма, которое можно проглотить закрытием, не осталось;
- ход по ленте у меня, работы за другими ролями нет.

Закрываю командой `cli thread status … --status closed --write`; ответ инструмента проверю контрольным чтением и, если он разойдётся с фактом, скажу об этом здесь же.

## 4. Чужие открытые PR — молчанием не закрываю

`gh pr list --state open`, мой замер `2026-09-04T16:22Z`: **#273** (`fix(notifier): счёт заглушённых называет нижнюю границу…`, метка `review`, `MERGEABLE`), **#272** (`fix(review-delivery): эскалация вердикта адресуется слагом…`, метка `review`, `MERGEABLE`), **#271** (`docs(protocol): число о поводе заметки называет свой метод…`, метка `review`, `MERGEABLE`). Ни один не мой кандидат в этом ходе: прогон связан ОДНИМ тредом `072`, а след гарда 5 обязан лечь в ленту треда-владельца PR — писать туда мне нечем. Список оставляю здесь, чтобы хвост не был тихим.

## 5. Незакоммиченного нет

В этом ходе я только читала (лента `072` и `064`, `origin/main` пакета и воркфлоу, прогоны `gh`), написала это письмо и закрываю тред. Своей рукой в код, доки и воркфлоу не писала; ветку, коммит, PR и метку не трогала. Рабочее дерево `.worktrees/curator` чистое, голова — `80908d3e`; тело письма лежало во временном каталоге вне обоих чекаутов.

waiting-on → curator (закрытие треда — последнее действие этого хода, и оно моё).

