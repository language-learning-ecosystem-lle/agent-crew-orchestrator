---
from: curator
worker: claude-code
session: 4505cf34-a50f-48f9-8e44-5aa101dee031
raised: 2026-09-04T00:53:14Z
date: 2026-09-04T01:11:32Z
expects: ack
waiting-on: curator
parked-on: john
---

**ТРЕБУЕТСЯ РЕШЕНИЕ JOHN: распространяется ли ваше исключение от 02.09 («первый вариант») на письма `reviewer-pr` — их пишет ШАГ ДЖОБЫ, но подписаны они ролью, у которой есть карточка? PR #265 готов целиком, гарды 1, 2 и 4 закрыты машиной, и стоит он ровно на гарде 3; своей кнопкой я его не жму, и ниже сказано, каким замером я это решила.**

## 1. Что закрыто и чем — дверью, а не глазом

`pnpm protocol merge-gate --ref origin/main --pr 265 --review-workflow 'Claude PR Review'`, прогнана мной `2026-09-04T01:11Z`, дословно:

- **гард 1 — `ok`:** `approved on 957a052 by github-actions — inside the round 33823774836 of 'Claude PR Review' on this head (2026-09-04T00:56:05Z…2026-09-04T01:08:59Z)`. Вердикт `reviewer-pr` (`verdict: approve, pr: 265`) лежит в ленте этого треда, отправлен `01:08:52Z` — то есть ВНУТРИ окна закрытого успешного круга, якорь прогонный, сироты треда `027` здесь нет;
- **гард 2 — `ok`:** два чека зелёные на `957a052018a502cab3025a825d69bdba2e784079` — `review=SUCCESS`, `checks=SUCCESS` (прогон `checks` `33823251444`). Сорок символов сверила дверь;
- **гард 4 — `ok`:** 7 изменённых путей, ни один не док власти. Список дверь взяла данными и напечатала источник каждого из восьми: выведены из `instructions` — `agent-protocol.json`, карточки `curator`, `dev-core`, `pilot-codex`, `devops`, `REVIEWER.md`; объявлены `powerDocuments` — `PROTOCOL.md`, `.github/workflows`. Доклад dev-core (§4 msg-012) подтверждён дверью, а не пересказом;
- **`mergeable=MERGEABLE`, `mergeStateStatus CLEAN`**; итог двери — `nothing in the facts forbids this merge — guards 3 and 5 are yours to answer`.

Порядок хода был мой: зелёный `checks` прочла рукой (прогон `33823251444`, `success`, та же голова) → метка `review` повешена `00:56:03Z` на той же голове → круг поднят событием навешивания (`33823774836`, `event=pull_request`) → вердикт.

## 2. Почему гард 3 не закрывается моей рукой — это замер, а не осторожность

Ревьюер в своём письме читает гард 3 как закрываемый («решение john в треде есть, сообщение `2026-09-02T11-00-29Z`»). Я его чтение НЕ принимаю, и вот на каком факте.

**Слово john в ленте (msg-003 §3) покрывает не это.** Дословно там: требование 1 — «дверь спрашивает объявление о парке у писем **РОЛЕЙ**; событие от `github` — не роль»; требование 3 — «**Норма для ролей не ослабляется ни на букву**». `reviewer-pr` в `agent-protocol.json` — роль, и карточка у неё есть (`REVIEWER.md`). Решение 02.09 названо принципом «машинные события выводятся из-под двери», но ПРОВЕДЕНО по границе, которую #265 двигает.

**И двигает буквально: та самая фраза переписана диффом.** `docs/protocol-reference.md`, раздел двери: было «**норма для ролей не ослаблена ни на букву**» — стало «**норма для поднятых сессий не ослаблена ни на букву**». Это не пересказ доклада: я прочла дифф сама (`gh pr diff 265`, 7 файлов, содержательная правка — одна строка `registry.ts:211`, `&& role.instructions === undefined` снято). Перечерчивание границы, заданной решением john, — по моей карточке ровно «снятие или сужение существующего запрета», то есть НОРМА, а не починка: починка возвращает поведение к уже объявленному, а объявлено было «письма ролей спрашиваются».

**Класс «полевой измеренный дефект, новой нормы не вводит» я не применяю — и не потому, что условий не хватает.** Два из трёх закрыты честно: (1) первоисточник замера назван — msg-011, живой круг контура-потребителя по их PR #485, прогон `33751725081`, шаг «Доставка 1 из 3», 2026-09-03; (2) исполнитель подтвердил своими словами о своём диффе (msg-012 §4). Не держится сам класс: его условие — «новой нормы не вводит», а по абзацу выше норма вводится. Правило последней строки применяю к себе.

**Дефект настоящий, и рекомендация у меня есть.** Цена измерена: любой PR, чей тред в момент вердикта паркован, теряет `approve` в единственном канале, который БУДИТ роли — коммента в PR и review-статуса не читает ни планировщик, ни `cli mail`. Рекомендую вариант (а). Но рекомендация curator ≠ решение john, и здесь кнопка ваша именно потому, что дифф двигает вашу же границу.

## 3. Развилка — три ответа, расход назван

Правка написана, зелёная и отревьюена; выбор — про границу, а не про работу.

- **(а) Расширить, как в #265: предикат целиком `wake.mode === 'event'`.** Довод dev-core я проверила своей рукой, и он держится механически: `roleLaunchability` отказывается поднимать всё, чей `wake.mode` не `watch` (`packages/agent-protocol/src/orchestrator/launch.ts:1253`, ветка `wake-not-watch` — прочла файл). Значит «сессии за письмом нет» — принуждение кода, а не проза карточки. Расход: одна кнопка merge, моя, по вашему слову. Цена: любой будущий участник с `wake.mode: 'event'` попадает под исключение автоматически, без отдельного вашего слова;
- **(б) Не расширять — граница остаётся на `github`.** #265 закрывается; потерю вердикта у потребителя закрывает их собственный маршрут (а) — шаг доставки называет судьбу парка сам (ваше решение 03.09). Работ здесь не заводится, расход ноль; цена — у нас остаётся дверь, которая ест вердикты своего же ревьюера, а второй забор становится единственным;
- **(в) Расширить перечислением, а не предикатом:** исключение носят названные поимённо участники, и каждый новый машинный писатель приходит к вам отдельным словом. Расход: новый круг dev-core (новый ключ конфига и, значит, бамп `protocolVersion`) плюс новый вердикт. Цена — граница перестаёт расширяться сама.

## 4. Живая приёмка §1 постановки НАСТУПИЛА — и это надо записать фактом

В этой же ленте лежит письмо `github`: «✅ `checks` по PR #265: `success`», голова `957a0520…4079`, прогон `33823251444`. Проверила причину чтением, а не догадкой: `git show origin/main:.github/workflows/ci-outcome.yml` даёт `on: workflow_run: workflows: [checks], types: [completed]` — заморозка (`workflow_dispatch` и только он, msg-004 §2, msg-005 §1) СНЯТА. **То есть §1 этого треда — «уведомления об исходе CI в этот контур не приезжают вовсе» — больше не верен: исход приехал сам, письмом, в тред.** Снятие заморозки сделано не в этом треде (предмет жил в `073-notifiers-frozen-in-own-contour`), и разбирать его здесь я не берусь; фиксирую ровно наблюдаемое. **Тред всё равно не закрываю:** приёмка стоит на #265, а он не влит.

## 5. Что я НЕ трогала и почему — чтобы хвост не был тихим

- **Парк на john ставлю ТОЛЬКО сейчас, и это не формальность.** Пока #265 не влит, дверь ест письма `reviewer-pr` в паркованный тред — ровно тот дефект, который PR и чинит. Запаркуй я тред до вердикта, шаг доставки покраснел бы, а `approve` пропал бы молча. Порядок был: метка → вердикт В ЛЕНТЕ → парк. По той же причине msg-005 §3 в прошлый раз выносил вопрос к john отдельным домом (`073`); здесь дом не нужен — письмо ревьюера уже лежит;
- **чужие открытые PR не тронуты, и вот они поимённо.** `gh pr list --state open` на `2026-09-04T00:56Z`: #265 (этот тред), #264 (`120-enclosing-repo-probe-silent`, метки нет), #263 (`090-what-kills-a-note`, метка `review`), #262 (`121-reviewer-md-lacks-the-class`, метка `review`), #259 (`119-mute-count-blames-github`, метка `review`). Ни один не мой кандидат в этом ходе: прогон связан ОДНИМ тредом `072`, а след гарда 5 обязан лечь в ленту треда-владельца PR — писать туда мне нечем. Список оставляю здесь, молчанием не закрываю;
- **расхождение чисел, названное ревьюером, я не гашу и передаю дальше как есть:** тред и тело PR заявляют «3524 из 3525, краснота `supervisor-gone`», а лог `checks` той же головы даёт `3523 passed | 2 skipped (3525), 0 failed` и `supervisor-gone` зелёным. Ревьюер судит это не блокирующим, и я с этим согласна (существо диффа — +2 теста, 0 удалённых, подтверждено независимым замером базы); но цифра в письме разошлась с логом, и dev-core стоит это знать.

## 6. Незакоммиченного нет

В этом контуре я тронула метку `review` на #265 и почту этим письмом. Своей рукой в код, доки и воркфлоу не писала; рабочее дерево чистое, тело письма лежало во временном каталоге вне обоих чекаутов. Голова #265 не двигалась с `957a052018a502cab3025a825d69bdba2e784079` — метку перевешивать не понадобится, если ответ придёт на неё же; уедет голова — сниму и повешу заново, круг поднимает событие, а не наличие метки.

Ход остаётся у меня, потому что вопрос несу я: john отвечает — я закрываю гард 3 его словом с пометкой источника и жму кнопку, называя все пять гардов поимённо.

waiting-on → curator.

