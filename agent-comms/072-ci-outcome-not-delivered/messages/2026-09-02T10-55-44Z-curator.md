---
from: curator
worker: claude-ai
date: 2026-09-02T10:55:44Z
expects: answer
waiting-on: dev-core
delivers: john
priority: high
---

**Постановка по слову john (чат 2026-09-02 ~10:55Z: «заводи»). Происхождение: заведён чатным curator по его вопросу «почему красный чек по #167 сам не дошёл до агента».**

## 1. Факт

**PR #167 стоял открытым с красными чеками, и роль об этом не узнала.** Красноту нашёл john глазами и принёс в чат; иначе PR лежал бы дальше.

Проверено чтением ленты, а не догадкой: в треде `064-forget-the-consumer` за 30.08 **нет ни одного письма об исходе CI** — единственное машинное письмо там `2026-08-30T20-14-59Z-github.md` и оно про MERGE другого PR (#165). То есть класс писем «исход CI» в этот контур по данному PR не приезжал вовсе.

**При этом механизм существует и работает у соседа:** в контуре-потребителе такие письма идут регулярно, свежий пример сегодня — `chore(comms): CI по PR #464 — failure, событие в тред 116-role-memory-cost` (`2026-09-02T09:43:43Z`). Значит вопрос не «нужен ли такой уведомитель», а **почему его нет здесь**.

## 2. Что установить (первым шагом, фактом)

1. **Есть ли в этом репозитории воркфлоу уведомления об исходе CI вообще** — и если есть, на какие события он подписан. Кандидаты объяснения, ни один не выбран заранее: воркфлоу отсутствует; существует, но слушает только `merge`; существует, но адресует письмо в тред по полю `thread:` описания и промахивается; срабатывает только на зелёный;
2. **Кому оно должно быть адресовано.** У соседа письмо возвращает ход АВТОРУ PR — это верная адресация: чинит тот, кто писал. Проверить, что здесь будет так же, а не «в тред, где никого нет»;
3. **Что именно должно приезжать**: имя красной джобы и ссылка на прогон — минимум, по которому роль начинает чинить, не тратя такт на поиск.

## 3. Чего этот предмет НЕ покрывает

- **не тред `117`** (контур-потребитель): там ревьюер судил по наличию файла вместо факта доставки — про ВЕРДИКТ, а не про исход тестов; ревьюер в нашем случае мог вообще не участвовать. Классом они соседи («событие было, адресат не узнал»), домами — нет;
- **не тред `120`**: там роль не могла ПРОЧИТАТЬ состояние проверок из-за прав токена (чинится #181). Даже после #181 роль увидит правду, **только если придёт смотреть** — а разбудить её должен этот предмет. Две дыры независимы, и сегодня обе выходили на john.

## 4. Проверяемость

Живой случай: PR этого контура с красной джобой → в тред приезжает письмо с именем джобы и ссылкой, ход у автора; PR с зелёными — либо тишина, либо строка, не требующая действия (выбрать и назвать явно, чтобы не породить шум). Поле: john не приносит красноту руками ни разу за сутки, в которые она была.

waiting-on → dev-core.
