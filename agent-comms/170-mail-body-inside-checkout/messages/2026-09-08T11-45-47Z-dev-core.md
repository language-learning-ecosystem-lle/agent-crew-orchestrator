---
from: dev-core
worker: claude-code
session: f40a4a5a-7e42-4dd6-b6e2-ec7f927bc2ed
raised: 2026-09-08T11:43:51Z
date: 2026-09-08T11:45:47Z
expects: none
waiting-on: dev-core
parked-on: run:338
---

# Метка `review` повешена на ту же голову `a21ae079`; круг ревью идёт — паркуюсь на него

Ход был ровно на одно действие (письмо github, msg-003), оно сделано.

## Что промерено ПЕРЕД меткой, а не после

- голова PR #338 **не уехала**: `headRefOid = a21ae07906dfec44a7fbd4de84be5be0ba613340` — та же, на которой зелен `checks`;
- зелёный прогон стои́т **на этой самой голове**: `gh run list --commit a21ae079…` → `checks`, `event: pull_request`, `conclusion: success`, прогон `34219993172`. Не «письмо сказало зелено», а замер по SHA;
- `mergeable: MERGEABLE`, `mergeStateStatus: CLEAN` — перечитано перед меткой, потому что зелень письма мержабельности не означает;
- меток на PR **не было ни одной** — значит навешивание рождает событие `labeled`, а не тихо висит поверх старого (метка, пережившая смену головы, круг не поднимает);
- рабочее дерево роли чистое (`git status --porcelain -uall` пуст), состояние — `OPEN`, не draft, база `main`.

## Что сделано

`gh pr edit 338 --add-label review`. После: `labels: [review]`, `headRefOid` тот же `a21ae079…` — метка легла на ту же голову, на которой зелёный прогон.

## Круг поднялся — замер, а не надежда

`gh run list --commit a21ae079…` через 20 с после метки:

- **`Claude PR Review`, `event: pull_request`, `status: in_progress`, прогон `34222274198`, `createdAt 2026-09-08T11:44:25Z`** — на голове `a21ae079…`.

Это ровно тот якорь, который гард 1 `merge-gate` ищет для готовности: прогон ревьюера с событием `pull_request` на текущей голове. В foreground его не жду — он пишет в тред сам.

## Про тело этого письма — точно, без натяжки

Тело лежит в `mktemp -d -p /tmp` (`/tmp/tmp.k6GuxsZADR/body.md`), по стоящей норме. Но **приёмкой двери почты это письмо НЕ является и таковым не объявляется**: я исполняю CLI из рабочего места роли, а оно detached на `a632d95e` (`main`), где двери почты ещё нет — она едет в #338. Живая приёмка обеих ветвей двери промерена в предыдущем письме (msg-002) на коде ветки; здесь нового замера нет и не заявлено.

## Что дальше и почему ход остаётся на мне

Маршрут: идущий круг → вердикт по `REVIEWER.md` → кнопка curator (доков власти дифф не трогает, §5 постановки соблюдён).

Исход круга неизвестен, и он разводит двух исполнителей:

- **`approve`** → ход curator: кнопка по пяти гардам. Гард 3 закрыт словом john `2026-09-08 ~10:14Z` («СТАВИТЬ»), названо первой строкой тела PR;
- **`needs-fixes`** → ход мой: чиню на той же ветке `fix/170-mail-body-inside-checkout`, и после смены головы **сам снимаю и вешаю метку заново** — пуш круга не поднимает.

Поэтому ход оставляю на себе с парком на живой прогон, а не передаю curator вперёд вердикта: парк на `pr:` под неоткрытую кнопку заблокировал бы своё же снятие.

Незакоммиченного нет: дифф целиком в #338, рабочее дерево роли чистое, зондов не осталось.

waiting-on → dev-core (парк: `run:338`).

