---
from: curator
worker: claude-code
session: 952091bf-d888-4f4d-8a5a-db0085357895
raised: 2026-09-08T11:55:31Z
date: 2026-09-08T11:59:55Z
expects: none
waiting-on: —
---

# Дверь места тела письма в `main`: #338 смёржен, squash `5b9795ae` — след гарда 5 и приёмка предмета

Кнопка нажата, предмет треда закрыт. Ниже — гарды поимённо, вторая проверка своей рукой на СМЁРЖЕННОМ дереве и то, что за предметом не остаётся.

## 1. След гарда 5 — что именно нажато

**merge:** `gh pr merge 338 --squash --match-head-commit a21ae07906dfec44a7fbd4de84be5be0ba613340`; squash-коммит `5b9795aea461d9e9fb5abe0ecaa9822b16002192`, заголовок в Conventional Commits.
**Контрольное чтение цели после записи** (ответ инструмента ≠ факт): `state: MERGED`, `base.ref: main`, `mergedAt: 2026-09-08T11:56:35Z`, `mergeCommit.oid: 5b9795ae…`.

Дверь: `pnpm protocol merge-gate --ref origin/main --pr 338 --review-workflow 'Claude PR Review'` — прогнана ПЕРЕД кнопкой, дословный исход по гардам:

1. **Гард 1 — `ok`:** `approved on a21ae07 by github-actions — inside the round 34222274198 of 'Claude PR Review' on this head (2026-09-08T11:44:25Z…2026-09-08T11:49:45Z)`. Якорь — ПРОГОН ревьюера на текущей голове, вердикт внутри его окна; сам вердикт — письмо `reviewer-pr` в этой ленте (`verdict: approve`, `pr: 338`, голова названа полным SHA, находок нет).
2. **Гард 2 — `ok`:** `2 check(s) green: review=SUCCESS, checks=SUCCESS` на ТОЙ ЖЕ голове `a21ae07906dfec44a7fbd4de84be5be0ba613340` (прогон `checks` — `34219993172`). Сверялся полный SHA, а не семь символов: `--match-head-commit` принял ровно его.
3. **Гард 3 — суждение, закрыто словом john.** «СТАВИТЬ», `2026-09-08 ~10:14Z`, доставка чата полем `delivers` в письме curator треда `157-pr-open-body-inside-checkout`. **Источник перечитан своей рукой в ленте `157` перед кнопкой**, а не перенесён из моей же постановки: слово стоит там дословно («Слово john полем `delivers` (чат 2026-09-08 ~10:14Z): „СТАВИТЬ“ — дверь того же класса ставится на `new-message`/`new-thread`»), и там же — поправка о том, чья это кнопка. **Полевым классом «измеренный дефект, новой нормы не вводит» гард 3 НЕ закрывается и не заявлялся:** дифф вводит НОВЫЙ ОТКАЗ на двух командах, то есть норму, — потому слово john и спрошено ДО работы, тем же порядком, которым уехал #328.
4. **Гард 4 — `ok`:** `8 changed path(s), none of them a document of power`. Список доков власти дверь взяла ДАННЫМИ и напечатала источник каждого пути: выведены карточки `curator`, `dev-core`, `pilot-codex`, `devops` и сам `agent-protocol.json`, `REVIEWER.md`; объявлены `powerDocuments` — `PROTOCOL.md`, `.github/workflows`. `docs/protocol-reference.md`, тронутый диффом, доком власти не является.
5. **Гард 5 — это письмо.**

`mergeability` (не гард, факт GitHub): `MERGEABLE`, `mergeStateStatus CLEAN`.

## 2. Вторая проверка — на СМЁРЖЕННОМ дереве, а не на ветке

Доклад исполнителя и вердикт ревьюера сняты с ветки. Смёрженное ≠ работающее, поэтому предмет промерен ещё раз, уже по `origin/main` на `5b9795ae`:

- **файлы на месте:** `packages/agent-protocol/src/fs/body-location.ts`, `…/fs/body-location.test.ts`, `…/fs/git-env.ts` — перечислены `git ls-tree origin/main`;
- **проводка одна, вызовов три** — `git show origin/main:packages/agent-protocol/src/cli.ts | grep -n bodyFileLocation`: импорт `:70`, вызовы `:3991` (`new-message`), `:4437` (`new-thread`), `:15898` (`pr open`). Копий не заведено — ровно то, чего требовал §3.2 постановки;
- **живая приёмка обеих ветвей на коде из `main`** (временный worktree на `5b9795ae` в `/tmp`, оба вызова БЕЗ `--write` — дверь судит и в ленту не пишет):
  - **(а) тело внутри чекаута → отказ**, дословно: `agent-protocol: new-message — the body file '/tmp/acc-170-main/.acc-probe-body.md' lies inside the git checkout '/tmp/acc-170-main' — write it OUTSIDE any checkout, in a directory of its own: 'mktemp -d -p /tmp' … Nothing was created: move the file and run this again`. Отказ называет СВОЮ команду (`new-message — …`), путь и выход — как требовал §3.3;
  - **(б) тело в `mktemp -d -p /tmp` → дверь молчит**: команда прошла её насквозь и дошла до обычного сухого судейства (напечатала предупреждение о 5 письмах, легших под моё письмо `10:29:53Z`). Ложного отказа на нормальном пути нет.
- **зонд и временный worktree удалены**, рабочее дерево роли чисто (`git status --porcelain -uall` пуст).

**Честные границы ЭТОГО замера, не заметённые:**

- **это письмо приёмкой двери НЕ является:** рабочее место роли `curator` detached на `a632d95e` (до мёржа), и отправляю я его CLI своего чекаута — там двери ещё нет. Приёмка выше сделана отдельными вызовами кода из `main`;
- **запустить смёрженный CLI с `cwd` в `/tmp` нельзя** — замерено попыткой: отказывает дверь границы контура (тред `062`, дословно «typed in a tree that belongs to no contour of this box»). Работает только с `cwd` в собственном дереве роли, что я и сделал.

## 3. Что за предметом НЕ остаётся и что остаётся, но не моё

Не остаётся: незакрытых находок ревьюера (их нет), незакоммиченного (в код своей рукой этот прогон не писал ничего), неназванного края — ограничение §3.7 (файл, положенный в чекаут ПОСЛЕ ответа двери; запись в почту мимо пакета; коробка, дающая демону `TMPDIR` внутри чекаута вне игноров) записано фактом в `docs/protocol-reference.md`, а не в обещании.

Остаётся, но принадлежит другим тредам — называю, чтобы не потерялось, и **не жму**:

- **обслуживаемый чекаут `main` стоит на `a632d95e`**, то есть позади `5b9795ae`: демон исполняет исходники ЧЕКАУТА, и до его самоперезапуска дверь в поле не работает. Это не находка и не мой предмет — обычный такт самопочинки;
- **#338 уехал ПОСЛЕ бампа `v0.2.13`** ⇒ в нарезанном теге его нет. Релизный поезд — тред `161`, не этот;
- **открытые PR: #339** (`тред 155`, метка `review` висит) **и #340** (`тред 159`, меток нет). Кнопку по ним этот прогон не жмёт: прогон связан треду `170`, а гард 3 у обоих восходит к чужим лентам, которые этот ход читать не вправе. Каждый берётся подъёмом по своему треду.

## 4. Приёмка и закрытие

Предмет постановки исполнен целиком и проверен дважды — вердиктом ревьюера на ветке и моей рукой на смёрженном дереве. **Закрываю тред `170` командой `thread status … --status closed --write` сразу за этим письмом и перечитываю ленту контрольно после флипа.**

Ход отсюда не уходит ни к кому: работы в предмете не осталось.

waiting-on → —

