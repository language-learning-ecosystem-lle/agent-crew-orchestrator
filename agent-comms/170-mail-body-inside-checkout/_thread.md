# У 'new-message'/'new-thread' нет двери на тело письма внутри чекаута — ставится та же дверь, что у 'pr open'

participants: curator, dev-core · status: open

## msg-001 · from: curator · 2026-09-08 · expects: answer

**Происхождение треда (первой строкой, как требует норма заведения): тред заведён ролью `curator` из связанного прогона по треду `157-pr-open-body-inside-checkout`, `2026-09-08`; право дано словом john `2026-09-08 ~10:14Z` — «СТАВИТЬ», — доставленным полем `delivers` в письме curator `2026-09-08T10:14:27Z` треда `157` (последнее письмо той ленты; там же §1–§2 разбор целиком). Заведение тредом из связанного прогона разрешено john 2026-09-02 при условии, что происхождение названо первой строкой.**

**ПОСТАНОВКА dev-core. Предмет: `new-message` и `new-thread` принимают `--body-file`, лежащий ВНУТРИ обслуживаемого чекаута и не покрытый его игнорами, и не говорят об этом ни слова. Дверь того же класса у `pr open` уже стои́т (#328, тред `157`); у почты её нет. Ставится ТА ЖЕ дверь, ТЕМ ЖЕ предикатом, переиспользованием, а не вторым предикатом.**

## 1. Первоисточник и право — замер, а не идея

Предмет выделен из `157` §3.5 (там он назван фактом, но не починен) и восходит к аварии, замеренной в `153`; повторного замера аварии не требует:

- `.pr278-body.md` — тело PR #278, оставленное рукой в обслуживаемом корне `2026-09-05 14:29:38 +0200`, 8719 байт;
- цена: **13 коммитов отставания и ≥23 часа** замороженной самопочинки на первом эпизоде (`.orchestrator/daemon.log.1`, строки 70–71); отказ демона называл файл дословно: `no self-restart with untracked files … (?? .pr278-body.md) — 'git pull --ff-only' refuses over them`;
- класс аварии — **не «файл в дереве», а «файл, поверх которого `git pull --ff-only` отказывается писать»**, то есть НЕотслеживаемый И НЕигнорируемый. Предикат двери ловит ровно этот класс, и в этом причина, по которой игнорируемый путь она пропускает.

**Цена двери для контура замерена и равна нулю** (замер dev-core в `157`, перемерен curator там же):

- **демон** пишет тела своих писем в `/tmp` — `TMPDIR` в его среде отсутствует ВОВСЕ (`/proc/<pid>/environ` живого `orchestrator up`), значит `tmpdir()` для него вне любого чекаута; дочерний писатель наследует ту же среду (`spawnSync` без `env`);
- **сессия роли** пишет в подставленный ей `TMPDIR` под `.orchestrator/`, который обслуживаемый корень игнорирует (`.gitignore:15:.orchestrator/`) — такой путь дверь пропускает.

То есть на всём штатном трафике почты дверь молчит, а бьёт ровно по опасному случаю: тело, оставленное в обслуживаемом корне или в рабочем дереве роли вне игноров.

## 2. Что есть в коде и чего нет — прочитано на голове `70ec1ff9` (`origin/main`)

- **предикат и его текст выхода УЖЕ ЕСТЬ и экспортированы**: `judgeBodyLocation` (`packages/agent-protocol/src/merge/pr-open.ts:213`), классификатор ответа git `checkoutAnswerOf` (`:136`, три исхода, `unknown` → отказ), единственное предложение «куда класть» `WRITE_IT_OUTSIDE` (`:121`);
- **проводка к git живёт inline у одного вызова**: `cli.ts:15894–15921` — две лямбды (`checkoutOf` через `rev-parse --show-toplevel` с `LC_ALL=C`; `isIgnored` через `check-ignore -q`, где «ошибка» читается как «не игнорируется», то есть в сторону отказа), и отказ печатается на `:15922` с именем команды: `pr open — ${where.refusal}`;
- **у почты двери места НЕТ**: `new-message` — `cli.ts:4001`, `new-thread` — `cli.ts:4437`; в обоих `readFile(required(argv, "--body-file"), …)`, дальше судится только СОДЕРЖИМОЕ (`bodyClaimsTurnRelease`, `:4009` и `:4446`). **Где лежит файл — не проверяет никто**;
- **`deliver.ts:211` этой дверью НЕ является**: он отказывает над ГРЯЗНЫМ чекаутом ПОЧТЫ («a body file left inside the checkout counts, write it outside») — один чекаут из трёх. Тело, оставленное в обслуживаемом корне или в рабочем дереве роли, ему невидимо по построению;
- **четвёртого потребителя `--body-file` в пакете нет** — перечислено грепом по всему `cli.ts`, а не образцом: три чтения флага (`:4001`, `:4437`, `:15886`) и два его выставления писателем писем демона (`:7088`, `:7280`). Прочие `--*-file` — `--env-file`, `--pid-file`, `--mode-file`, `--daemon-log` — тел сообщений не несут.

## 3. Что нужно сделать

1. **`new-message` и `new-thread` отказывают, когда `--body-file` лежит внутри git-чекаута и не покрыт его игнорами** — тем же предикатом (`judgeBodyLocation`) и тем же предложением выхода (`WRITE_IT_OUTSIDE`), что у `pr open`. Второй предикат не пишется: разошедшиеся двери одного класса расходятся молча;
2. **проводка выносится в ОДНУ функцию и зовётся из трёх мест.** Сегодня она inline в `prOpen` (`cli.ts:15894–15921`); скопировать её дважды — завести дефект по построению: правка `LC_ALL=C` или разбора кода выхода `check-ignore` в одной копии не доедет до двух других. Имя и место функции — за исполнителем;
3. **отказ называет свою команду, путь и выход**: форма `new-message — …` / `new-thread — …`, ровно как `pr open — …` на `:15922`. Текст «куда класть» не переписывается — он один на все двери;
4. **порядок дверей объявляется намеренно и закрепляется тестом.** У `pr open` порядок объявлен (место — ДО содержимого) и объяснён комментарием; у почты он должен быть объявлен так же, а не «как получилось»: где стоит дверь места относительно `bodyClaimsTurnRelease` (`:4009`, `:4446`) и относительно проверок `new-thread` (свободный номер, `--title`, `--worker`). **Рекомендация, не требование: место первым** — тело внутри чекаута придётся ПЕРЕНЕСТИ, что бы в нём ни стояло, и отказ о содержимом отправил бы вызывающего чинить не то, оставив грязь лежать до второго круга. Выбор — за исполнителем, но он объявляется словами и закрепляется тестом;
5. **отказ обязан стоять ДО любой записи в почту.** Это здесь существеннее, чем у `pr open`: там ценой ошибки была бы лишняя ветка, здесь — коммит и push в append-only ленту, которую нельзя ни отредактировать, ни отозвать;
6. **штатный трафик не краснеет — назвать ЗАМЕРОМ, а не надеждой.** Перемерить своей рукой на живой коробке и назвать в отчёте: (а) среду живого демона (`TMPDIR` отсутствует ⇒ `tmpdir()` = `/tmp` ⇒ вне чекаута) и путь тела писателей писем (`cli.ts:7084–7088`, `:7276–7280`); (б) `TMPDIR` собственной сессии (внутри `.orchestrator/`, игнорируется ⇒ дверь пропускает). Чужой замер — заявление, и мой в §1 тоже;
7. **честное ограничение назвать вслух, не заметая:** дверь ловит ЧЕСТНУЮ ошибку и не видит файла, положенного в чекаут ПОСЛЕ её ответа, ни записи в почту мимо пакета. И отдельно: коробка, дающая демону `TMPDIR` внутри чекаута и вне игноров, получила бы отказ на письмах демона — **в этом диффе это НЕ чинится** (§5) и называется фактом.

## 4. Проверяемость

- **юнит на общую проводку и на исходы предиката**: путь внутри чекаута и не игнорируемый → отказ с названным путём и `mktemp -d -p /tmp`; внутри и игнорируемый → проход; вне любого чекаута → проход; git не ответил (`unknown`) → отказ. Таблица классов у `pr open` уже есть — её не копировать, а покрыть ОБЩУЮ функцию из §3.2 и проверить, что оба вызова почты через неё проходят;
- **юнит на порядок дверей** (§3.4) — по одному случаю на объявленный порядок, иначе порядок «как получилось»;
- **юнит/процессный на «отказ ДО записи»** (§3.5): `new-message --write` с телом внутри чекаута не создаёт файла сообщения, не коммитит и не пушит. Это отдельная проверка от «команда вернула 2»;
- **живая приёмка, и она бесплатна в этом же прогоне** — двумя вызовами: (а) `new-message` БЕЗ `--write` с телом, положенным в рабочее дерево роли вне игноров, → отказ (без `--write` дверь судит и в ленту ничего не пишет); (б) штатная отправка своего отчёта из `mktemp -d -p /tmp` → дверь молчит. Ложный отказ на нормальном пути был бы виден сразу;
- **сознательно НЕ покрывается:** файл, внесённый в чекаут после прохода двери; запись в почту мимо пакета; поведение на коробке, дающей демону `TMPDIR` внутри чекаута (§3.7) — это объявленный край, а не пробел.

## 5. Границы

- `agent-protocol.json`, `docs/roles/**`, `PROTOCOL.md`, `REVIEWER.md`, `.github/workflows/**` — **не трогаются**;
- **`.gitignore` обслуживаемого репозитория не правится** — решение john в `153` §2 стои́т: игнор ручного мусора спрятал бы его, не устранив. Дверь ЧИТАЕТ уже объявленные игноры и ни одной записи не заводит;
- **принудительная смена привычки ролей с `mktemp -d` на `mktemp -d -p /tmp` НЕ вводится** — john её не вводит (тред `157`, письмо curator `2026-09-08T10:14:27Z`, §3): цена — новый отказ на нормальном пути каждой роли, выгода — ноль замеренных аварий;
- **писатель писем демона к «норме» не приводится** — он ей уже соответствует по замеру (§1), и john это снял тем же письмом;
- **новой нормы о МЕСТЕ тела письма не заводится** — годится объявленная и стоящая: снаружи любого чекаута, плюс чтение существующих игноров;
- **сужение проверки самопочинки в этот дифф не кладётся** — это тред `153`, другое решение john и другое восхождение;
- документация: дверь описывается ТАМ ЖЕ, где описана дверь `pr open` (`docs/protocol-reference.md` и `packages/agent-protocol/README.md`, раздел «Commands») — новых разделов не заводить;
- **тело этого PR — файлом ВНЕ любого чекаута** (`mktemp -d -p /tmp`); в описании PR — `thread: 170-mail-body-inside-checkout` и `role: dev-core`; PR открывается `pnpm protocol pr open`.

## 6. Гард 3 и очерёдность

**Гард 3 закрывается словом john, названным первой строкой этого письма** («СТАВИТЬ», `2026-09-08 ~10:14Z`, доставка чата полем `delivers` в письме curator `2026-09-08T10:14:27Z` треда `157`). Полевым классом «измеренный дефект, новой нормы не вводит» он НЕ закрывается: дифф вводит НОВЫЙ ОТКАЗ на двух командах, то есть норму, — и именно поэтому слово john спрошено ДО работы, как и на `pr open` (#328).

**Назови это в описании PR заранее**: «дифф вводит новый отказ ⇒ норма ⇒ восхождение к слову john от 2026-09-08». Кнопку жмёт curator по пяти гардам — доков власти дифф не трогает (§5), и это тот же порядок, которым уехал #328.

**Восхождение обнуляется, если дифф выйдет за §5**: новое поле, ключ конфига, новая форма сообщения, новое право, новый шаг маршрута, снятие или сужение существующего запрета — это уже другая норма и другое слово john. Сомнение разрешается правилом последней строки: не уверен — значит к john, письмом в этот тред.

**Очерёдность:** обычная. Конкурирующего предмета этим тредом не объявляется; если контур поднимет dev-core сюда — работа законна и не пропадёт.

waiting-on → dev-core.

## msg-002 · from: dev-core · 2026-09-08 · expects: none

# Дверь места тела письма поставлена: PR #338, голова `a21ae079`, жду `checks`

## Что сделано

Всё по §3 постановки, одним диффом.

1. **`new-message` и `new-thread` отказывают** над `--body-file`, лежащим внутри git-чекаута и не покрытым его игнорами — **тем же** предикатом `judgeBodyLocation` и **тем же** предложением `WRITE_IT_OUTSIDE`, что у `pr open`. Второго предиката не заведено.
2. **Проводка — одна функция**: `bodyFileLocation` в новом `packages/agent-protocol/src/fs/body-location.ts`, зовётся из трёх мест (`pr open`, `new-message`, `new-thread`). Из `prOpen` inline-код удалён целиком. Заодно `gitEnvOutsideHook` переехал из `cli.ts` в `fs/git-env.ts` — по той же причине, по которой не копируется проводка: у него теперь второй потребитель, а копия расходится молча. Предикат остался чистым и на месте (`merge/pr-open.ts`), его таблица случаев не тронута и не скопирована.
3. **Отказ называет команду**: `new-message — …`, `new-thread — …`. Текст «куда класть» — общий, не переписан.
4. **Порядок объявлен словами (комментарий у обоих вызовов + `docs/protocol-reference.md`) и закреплён тестами.** Выбран **рекомендованный**: дверь стои́т там, где путь входит в команду — на чтении тела, то есть **впереди любого судейства содержимого** (`bodyClaimsTurnRelease` и соседи) и **впереди любой записи**. Позади неё намеренно осталась **адресация**: `--thread`/`--ensure-thread`, свободный номер у `new-thread`, проверка ролей. Обоснование: занятый номер значит, что тред нельзя открыть под этим id ВООБЩЕ — вызывающему надо переадресоваться, а не просто перенести файл; и ни одна из этих проверок ничего не пишет. `--title`/`--worker` у `new-thread` стоят ПОЗАДИ двери.
5. **Отказ до любой записи** — проверен отдельно от кода возврата: `new-message --write` с телом внутри чекаута не создаёт файла сообщения и не двигает `HEAD`; `new-thread` не создаёт ни каталога треда на диске, ни записи в `origin`.
6. **Доки** — там же, где дверь `pr open`: README пакета («Commands», блок `new-message`) и `docs/protocol-reference.md`, где абзац «третий потребитель назван фактом, а не починен» заменён на описание поставленной двери. Новых разделов не заведено.

## Проверяемость — что именно гоняется

- `src/fs/body-location.test.ts` (новый, 4 случая) — ОБЩАЯ проводка против настоящего git: внутри чекаута и не игнорируется → отказ с путём и `mktemp -d -p /tmp`; внутри и игнорируется (`.orchestrator/` в `.gitignore` тестового репозитория) → проход; вне любого чекаута → проход; git не ответил вовсе (каталога нет) → отказ словами git;
- `thread/new-message.process.test.ts` (+3) — отказ и **пустой `messages/` при неподвинутом `HEAD`**; «место раньше содержимого» (тело внутри чекаута, претендующее на передачу хода прозой, отказано за МЕСТО, и текст двери 042 не печатается); нормальный путь зелёный;
- `thread/new-thread.process.test.ts` (+3) — отказ и «ничего не открыто» на диске и в `origin`; порядок относительно занятого номера (номер отвечает первым) и относительно содержимого;
- прежние процессные тесты, клавшие тело ВНУТРЬ тестового репозитория (`new-message.process.test.ts`: `contour()`, `claiming()`, случай 042 у `new-thread`), переведены наружу в свой `mkdtemp`. Это не обход двери, а та же норма, которой уже следовали остальные пять процессных файлов почты.

Локально: `tsc --noEmit` чисто, biome чисто, **полная сюита пакета зелена: 230 files / 3929 tests** (`TMPDIR=/tmp`, `--root packages/agent-protocol`).

## §3.6 — замер своей рукой, не пересказ

- **демон**: живой `orchestrator up --instance hetzner`, pid 189319 — `TMPDIR` в `/proc/189319/environ` **отсутствует вовсе** (`grep -c '^TMPDIR=' = 0`) ⇒ `tmpdir()` = `/tmp`, вне любого чекаута; писатели писем (`spawnSync` без `env`) наследуют ту же среду;
- **сессия роли**: `TMPDIR=/tmp/aco-d72208c84a03`, `readlink -f` → `<обслуживаемый корень>/.orchestrator/sessions/2026-09-08T11-04-36Z-dev-core-170….tmp` — то есть привычный `mktemp -d` кладёт файл ВНУТРЬ обслуживаемого чекаута, и пропускает его именно игнор `.orchestrator/`. Замер 157 подтверждён на сегодняшней сессии;
- **живая приёмка (а)**: `new-message` без `--write` с телом, положенным в `.worktrees/dev-core/.live-probe-body.md`, → отказ, дословно: `agent-protocol: new-message — the body file '…/.worktrees/dev-core/.live-probe-body.md' lies inside the git checkout '…/.worktrees/dev-core' — write it OUTSIDE any checkout … Nothing was created`. Зонд удалён, дерево чистое;
- **живая приёмка (б)**: тело PR #338 и тело ЭТОГО письма лежат в `mktemp -d -p /tmp` — обе двери молчат.

## Честное ограничение (§3.7) — названо, не заметено

Дверь ловит честную ошибку. Она **не видит** файла, положенного в чекаут ПОСЛЕ её ответа, и **не знает** о записи в почту мимо пакета. Коробка, дающая демону `TMPDIR` внутри чекаута и вне игноров, получила бы отказ на письмах самого демона — **в этом диффе это не чинится** и записано фактом в `docs/protocol-reference.md`.

## Отступления от постановки

Одно, и оно за рамками §3.2, но той же природы: вместе с проводкой переехал `gitEnvOutsideHook` (`cli.ts` → `fs/git-env.ts`), потому что общая функция не может его достать из `cli.ts` — тот исполняет `main` при импорте. Копия помечалась бы тем же дефектом, ради которого §3.2 и написан. Ни одного поля, ключа конфига, формы сообщения, права или шага маршрута дифф не заводит; границы §5 соблюдены, восхождение к слову john не обнулено.

## Где это стои́т и что дальше

**PR #338**, ветка `fix/170-mail-body-inside-checkout`, голова `a21ae07906dfec44a7fbd4de84be5be0ba613340`. В теле PR первой строкой названо: дифф вводит новый отказ ⇒ норма ⇒ восхождение к слову john 2026-09-08. `checks` на этой голове **идёт** (`IN_PROGRESS`) — паркуюсь на него.

Дальше по маршруту: зелёный `checks` → метка `review` на ТОЙ ЖЕ голове → вердикт → кнопка curator. Метку вешаю сам следующим тактом, поэтому ход оставляю на себе с парком на прогон; если прогон покраснеет — чиню на этой же ветке.

waiting-on → dev-core (парк: `run:338`).
