---
from: curator
worker: claude-code
session: ff99a153-f348-4377-b3eb-cb195a051add
raised: 2026-09-06T17:50:52Z
date: 2026-09-06T17:52:52Z
expects: none
waiting-on: curator
parked-on: run:314
---

Метка `review` повешена на #314 — это и было единственное действие такта. Круг живой, за ним не стою: парковка `run:314`, ход возвращается ко мне исходом вердикта.

## 1. Что сделано, фактами

| факт | замер |
|---|---|
| голова #314 | `405c921fd489fac48bc9e55d94bbfe863bf1ab9c` — та же, что в зелёном `checks` (письмо github выше), сверена `gh pr view --json headRefOid` ДО метки и контрольным чтением ПОСЛЕ |
| `checks` | `pass`, прогон `34049306330`, 7 м 11 с |
| метка | `review` навешена `2026-09-06T17:51:3xZ`; контрольное чтение: `labels: [review]`, голова не сдвинулась |
| круг поднялся | прогон **`34049908267`**, `event=pull_request`, `headSha=405c921f…`, `status=in_progress`, `createdAt 2026-09-06T17:51:40Z` |

Порядок соблюдён: метка ПОСЛЕ зелени на ТОЙ ЖЕ голове (норма 03.08, тред `049`).

## 2. Самопропуск круга исключён замером, а не надеждой

У самопропуска два повода, и оба проверены ДО метки — иначе круг вернул бы пустой прогон вместо вердикта, а метка сгорела бы впустую:

- **«PR правит воркфлоу»** — `gh pr view 314 --json files`: в диффе ровно один путь, `PROTOCOL.md`. `.github/workflows/**` не тронут;
- **«ветка отстала по воркфлоу»** — `git diff origin/main 405c921f -- .github/workflows/` пуст: дерево воркфлоу на голове побуквенно то же, что на `origin/main`, то есть файл ветки совпадает с дефолтным и действие себя не пропускает.

Подтверждено исходом: круг действительно поднялся (§1), а не отметился пустым прогоном.

## 3. База уехала на один коммит — намеренно НЕ ребейзю

`git rev-list --count 405c921f..origin/main` = **1** (в `main` сел чужой PR после открытия #314). Сливаемость от этого не пострадала: `mergeable: MERGEABLE`.

Ребейза нет по расчёту, а не по недосмотру: ребейз двигает голову, а движение головы обнуляет и зелёный `checks` (гард 2), и вердикт, который сейчас считается (гард 1) — ценой стал бы второй круг ревью за то же дерево. Сдвиг базы гасится замером, а не ребейзом; если он окажется существенным, он проверяется на дереве мержа, и делать это положено ПОСЛЕ вердикта, а не вместо него.

## 4. Ход и что делает следующий такт

`waiting-on → curator`, парковка `run:314`: сессия за чужим прогоном не стоит (решение john 2026-08-28, тред `037`). Вердикт приедет в эту ленту сам письмом `reviewer-pr`.

Следующий такт, разбуженный вердиктом:

1. **`approve`** → `pnpm protocol merge-gate --ref origin/main --pr 314 --review-workflow 'Claude PR Review'` и **письмо john с кнопкой**: гард 4 даёт STOP — `PROTOCOL.md` док власти, merge своей рукой не делается ничем. Класса Д-1 здесь нет и полевой класс гарда 3 гарда 4 не трогает;
2. **`needs-fixes`** → чинить в этой же ветке, затем метку СНЯТЬ и повесить заново (пуш круг не поднимает);
3. парк на john ставится ТОЛЬКО после вердикта в ленте — парк на человеке при летящем круге глотает письмо `reviewer-pr`.

Незакоммиченного нет: рабочее дерево `.worktrees/curator` чисто, всё в `405c921f`, ветка запушена. Своей рукой в этот такт не тронуто ничего, кроме метки на #314.

