---
from: curator
worker: claude-code
session: d090a9a0-4e9e-46ea-a83a-5991293a09e7
raised: 2026-09-06T17:55:26Z
date: 2026-09-06T18:00:01Z
expects: ack
waiting-on: curator
parked-on: john
---

**Требуется решение john: нажать merge (squash) на PR #314 — `PROTOCOL.md`, док власти, гард 4 даёт STOP, своей рукой merge не делается ничем.** Всё остальное по PR закрыто: вердикт `approve` на текущей голове, `checks` зелёные на ней же, слияние с СЕГОДНЯШНЕЙ базой перемерено и чисто.

## 1. Что именно нажимается

| поле | значение |
|---|---|
| PR | **#314** — `docs(protocol): датированное ожидание пережило своё событие — четыре абзаца названы закрытыми (тред 151)` |
| голова | `405c921fd489fac48bc9e55d94bbfe863bf1ab9c` (не двигалась с момента зелени и вердикта) |
| ветка | `curator/151-dated-waiting-outlived-its-event` |
| форма | **squash**, заголовок уже в Conventional Commits |
| дифф | ровно один файл — `PROTOCOL.md`, четыре абзаца (`:156`, `:160`, `:161`, `:165`), 4 строки изменённых |

**Что меняется по существу:** четыре абзаца канона говорили в настоящем времени о переходном периоде («код идёт следом», «остаётся открытым», «до отдельного PR dev-core лифт работает по-старому»), а все четыре периода закрылись — самый старый 2026-08-22, самый свежий 2026-09-02. Дифф переводит констатацию в прошедшее время и называет, ЧЕМ она закрыта: PR, полный коммит, метка времени. **Ни одна норма не тронута** — исходные предложения нормы в каждом абзаце сохранены побуквенно, дописано только предложение о закрытии.

Отдельно названо то место, где «окно закрылось» легче всего прочесть как смену нормы, и оно сказано вслух прямо в тексте: «письма, отправленные до `2026-08-29T16:09:41Z`, шапок не имеют и нового хода не открывают — по-прежнему и навсегда».

## 2. Гарды — замером, а не памятью

Живой прогон `pnpm protocol merge-gate --ref origin/main --pr 314 --review-workflow 'Claude PR Review'`, `2026-09-06T17:5xZ`, дословно по пунктам:

- **гард 1 — `ok`:** `approved on 405c921 by github-actions — inside the round 34049908267 of 'Claude PR Review' on this head (2026-09-06T17:51:40Z…2026-09-06T17:54:57Z)`. Вердикт лежит В ОКНЕ закрытого успешного круга на ТЕКУЩЕЙ голове — сироты нет;
- **гард 2 — `ok`:** `2 check(s) green: review=SUCCESS, checks=SUCCESS` на той же голове (прогон `checks` — `34049306330`);
- **гард 3 — восхождение есть:** тред `151-dated-waiting-outlived-its-event` назван строкой `thread:` в описании PR; постановка треда — моя, и потому решение john на КОНЕЦ обязательно и запрашивается этим письмом. Полевым классом гард 3 не закрывается и закрывать его не требуется: гард 4 всё равно STOP;
- **гард 4 — `STOP`:** `john merges this one — it changes PROTOCOL.md`. Дверь вывела список доков власти данными (8 путей), `PROTOCOL.md` в нём — «declared by `powerDocuments` of the config». Класса Д-1 в этом репозитории не принято, исключений у гарда 4 нет;
- **гард 5 — за мной:** след merge с поимёнными гардами ложится в эту ленту после кнопки. Это письмо следом не является — оно запрос кнопки;
- **mergeability:** `MERGEABLE` (`mergeStateStatus CLEAN`).

## 3. Нота двери о сдвиге базы — перемерена, merge она изменить не может

Дверь отдельной строкой сообщила: база уехала (`cb72ba9`, `2026-09-06T17:47:45Z`) ПОСЛЕ старта зачтённого `checks` (`2026-09-06T17:39:55Z`), то есть зелёный гард 2 — чтение дерева, которое больше не является результатом этого слияния. Ноту гашу замером, а не ребейзом (ребейз двинул бы голову и обнулил и вердикт, и зелень — второй круг ревью за то же дерево):

- база опережает голову ровно на ОДИН коммит: `cb72ba91` (#313);
- пути, которыми он расходится с головой: **`.github/scripts/foreign-name-watch.allow`** — единственный. С `PROTOCOL.md` не пересекается;
- дерево слияния с сегодняшней базой построено: `git merge-tree --write-tree origin/main 405c921f` → `9b2aa24e8a7cb01cc658f2db4e81621f35df82b6`, конфликтов нет;
- целость слитого дерева: `git diff --stat origin/main 9b2aa24e` → `PROTOCOL.md | 8 ++++----`, **1 файл, 4 вставки, 4 удаления**; удалённых файлов — `0`. То есть merge в сегодняшнюю базу вносит РОВНО те же четыре строки и ничего сверх.

Полной сюитой на дереве слияния предмет не перемерялся сознательно: дифф не трогает ни строки кода (`docs-only`), а разошедшийся путь — файл списка `.github/scripts/**`, который `PROTOCOL.md` не читает. Если это сочтено недостаточным, скажи — прогон на дереве слияния стоит минуты.

## 4. Что уже проверил ревьюер своей рукой (не пересказ доклада)

Круг `34049908267` перепроверил **все пять пар PR/коммит/дата** из тела #314 через `git log -S` и `git show -s --format=%cI` — расхождений нет; прочитал дифф целиком и заключил: новых полей конфига, форм сообщений, прав, шагов маршрута, снятых или суженных запретов дифф не вводит; расширение предмета с одного абзаца до четырёх признал доложенным и легитимным (названо первым абзацем тела PR). Находок — ноль. Сам ревьюер прогнал `merge-gate` и получил тот же `STOP` гарда 4, назвав merge кнопкой john.

## 5. Ход

`waiting-on → curator`, парковка на **john**: ход остаётся у меня как у носителя вопроса, а вопрос — первой строкой. Своей рукой в этом треде больше нечего делать до кнопки.

После merge мой следующий такт: контрольное чтение цели (`base.ref`) и след гарда 5 в эту ленту с поимёнными гардами, затем закрытие треда.

**Хвост открытых PR (факт, не заявка на действие):** `gh pr list --state open` даёт четыре — #314 (этот), #310, #307, #304. Три чужих в этот прогон не трогаю: след гарда 5 по каждому обязан лечь в ленту ЕГО треда, а этот прогон связан одним тредом `151`. Называю числом, чтобы хвост не считался невидимым.

