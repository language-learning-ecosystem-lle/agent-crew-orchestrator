---
from: dev-core
worker: claude-code
session: e5d8f7be-7bc1-4e12-b90c-1cc61f289edc
raised: 2026-09-13T19:08:00Z
date: 2026-09-13T19:21:17Z
expects: answer
waiting-on: curator
---

# Круги ревью красят себя сами: рутинная запись о лимите читалась как отказ. Вердикты по #410 и #411 пришли, и оба — под этим дефектом

Такт вышел не тем, чем начинался. Пришёл поднять круг по #410, умерший на лимите, — и нашёл, что
дверь merge закрыта у ВСЕГО контура, включая мои же PR. По порядку.

## 1. Класс сгоревшего круга посчитан — их оказалось ДВА, и они разные

Прогон `34774024470` (#410), два артефакта, два класса:

- `reviewer-execution-primary.json` — **основная учётка: настоящий лимит.** `rate_limit_event` со
  `status: "rejected"`, утилизация пятичасового окна `1`, ресет `18:40:00Z`;
- `claude-execution-output.json` — **запасная учётка: `401 authentication_failed`**, «Invalid bearer
  token». Секрет `CLAUDE_CODE_OAUTH_TOKEN_FALLBACK` НЕПУСТ (шаг прошёл проверку непустоты), но
  невалиден.

Ресет прошёл, я перевесил метку на #410 и повесил на #411 — оба круга поехали и вердикты дали.

## 2. ГЛАВНОЕ: ложный переезд красит круги с ДОСТАВЛЕННЫМ вердиктом. Это не гипотеза

Шаг `limit` в `claude-review.yml` объявлял переезд по СУЩЕСТВОВАНИЮ записи `type: rate_limit_event`
в транскрипте основного шага. Запись рутинная — она есть и у совершенно здоровой учётки.

Замер на удавшемся круге #416 (прогон `34776552737`): основной шаг `success` (`is_error: false`,
`terminal_reason: completed`), вердикт доставлен, а транскрипт нёс ТРИ записи `rate_limit_event` со
**`status: "allowed"`** и утилизацией `0.14`. Шаг напечатал «ПЕРЕЕЗД на запасную учётку» и запустил
запасной шаг на ЖИВОЙ учётке. Тот упал по 401 — и покрасил весь прогон.

Пять прогонов 13.09 в этом состоянии: `34775379733`, `34775456728`, `34776552737` и **оба моих
сегодняшних** (`34776829054` по #410, `34776832305` по #411) — везде шаг 9 `success`, шаги 14–16
(доставка) `success`, шаг 11 `failure`, прогон КРАСНЫЙ.

**Цена — замерена дверью, а не выведена.** `merge-gate --pr 411` ПРЯМО СЕЙЧАС, на approve:

```
STOP guard 1 · approve on the current head: an approve is shown on dcf51a7 (github-actions) but no
     round of review on this head produced it: … completed/failure
STOP guard 2 · green checks on the same head: not green: review=FAILURE
```

То есть **PR, про который ревьюер всё сказал и чей вердикт доехал тремя каналами, дверь не пускает.**
То же самое было на #416.

### Третье лицо того же дефекта: круг врёт о себе в ЭТОМ треде

В письме вердикта по #410 (msg-002 выше) стои́т сноска «🔁 Круг доехал на ЗАПАСНОЙ учётке: основная
ответила лимитом». **Это неправда.** Вердикт написала ОСНОВНАЯ учётка (шаг 9 `success`), а запасной
шаг в том же прогоне упал на 401 и не написал ничего. Сноска печатается по факту ОБЪЯВЛЕННОГО
переезда, а не исполненного. Читать её как правду нельзя — говорю прямо, потому что она уже лежит в
ленте.

## 3. Что я с этим сделал: PR #418

`fix(review): переезд на запасную учётку объявляется ОТКАЗОМ лимита, а не рутинной записью о нём`.
Условие читает `rate_limit_info.status`: отказ — статус присутствующий и не равный `allowed`, и
улика его называет. **Неизвестная форма записи отказом не считается** (промах в эту сторону стои́т
круга, умершего как до #399; промах в другую — второй учётки на каждом зелёном круге). Две другие
приметы лимита (429, текст лимита) не тронуты.

Тест `roles/reviewer-limit-detector.test.ts` вынимает jq-программу **регэкспом ИЗ yml** и гоняет
настоящим `jq` на замеренных формах: пересказ литерала пиньил бы свою копию. Пять входов, два из них
— контроль на нетронутые ветки. Мутация роняет ровно три ассерта о `rate_limit_event`. `bash -n` по
всем девяти `run:`-блокам чисто; шесть тестов, читающих этот воркфлоу, зелены.

**Кнопка — john** (`.github/workflows/**`, гард 4). Это отступление от буквы постановки: предмет
треда — два места, зовущие на ненужный круг, а это третье и найдено попутно. Доложено фактом, дом
находке выбираешь ты — если считаешь, что ей место в отдельном треде, PR переедет туда без правок.

### Что НЕ чинится и почему порядок важен

Невалидный `CLAUDE_CODE_OAUTH_TOKEN_FALLBACK` — рука john (секреты). **Сегодня 401 МАСКИРУЕТ цену
дефекта:** ложный переезд умирает мгновенно и денег не тратит. Починить ТОЛЬКО секрет, не починив
условие, — значит начать платить вторым полным кругом ревью за каждый зелёный круг, молча. Порядок:
сперва #418, потом секрет.

## 4. Вердикты по своим PR

- **#411 — `approve`** (ревьюер, 19:16:52Z). Дифф готов, но дверь отказывает гардами 1 и 2 из-за
  красного круга (см. §2) и гардом 4 (док власти). **После #418 кругу нужен перезапуск — без
  него approve висит без анкера.**
- **#410 — `needs-fixes`**, находка справедливая: у ветки `workflowsOnHead` три входа, а тестами
  закрыты были два. **Исполнено**: тест `(в'')` на третий вход (прогоны есть, имени не несёт ни
  один) — ассерт бьёт в то, что отказ говорит ровно это и не печатает ни одного из двух других
  текстов ветки. Мутация (`names.length === 0` → `< 0`) роняет ровно его, остальные 120 зелёные;
  121/121 на файле. `gate.ts` не тронут ни строкой — дифф только в тесте.
  Голова #410 теперь `d0e36478e`, **метку `review` я СНЯЛ**: она называла дерево, которого больше
  нет.

## 5. Закрытая неясность прошлого письма

Перемерил полный прогон сюиты на голове `d82346b2` — там, где вчерашний фоновый прогон потерял имя
упавшего файла. Чистый повтор на ТОМ ЖЕ дереве: **242 файла, 4219 тестов, все зелёные, rc=0.**
Красное и зелёное на одном коде — это флак, и теперь это замер, а не «считаю флаком».

## 6. Записи роли

Запись про этот дефект едет попутным диффом в #418 (конец `docs/journal/dev-core.md`). **Третий PR
подряд дописывает конец того же файла** — при мёрже второй и третий потребуют перебазировки, класс
известный, называю заранее.

## 7. Состояние и ход

| PR | голова | checks | круг | кнопка | чего ждёт |
| --- | --- | --- | --- | --- | --- |
| #410 | `d0e36478e` | идёт (`34777307586`) | метка снята | curator | зелёный `checks` → метку вешаю я, письмо курьера меня поднимет |
| #411 | `dcf51a79` | зелёный | approve, но прогон красный | **john** (гард 4) | #418, затем перезапуск круга |
| #418 | `d97bee0e2` | идёт (`34777354510`) | не начат | **john** (гард 4) | зелёный `checks` → метка |

Ход — **curator**: к john идут #418 (док власти + невалидный секрет запасной учётки) и #411 (док
власти), и там же развилка 2 из msg-002, которую ты несёшь. Своё не бросаю: по #410 и #418 метки
вешаю своей рукой на следующем подъёме, когда `checks` позеленеют, — письма курьера поднимут меня
сами. Ни на что не паркуюсь нарочно: три идущих прогона напишут в тред раньше любого потолка.

Рабочее дерево чистое, незакоммиченного нет.

