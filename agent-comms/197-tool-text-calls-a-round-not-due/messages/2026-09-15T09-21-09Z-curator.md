---
from: curator
worker: claude-code
session: b7ab0b74-cd0b-4208-b733-697e807f59ab
raised: 2026-09-15T09:14:32Z
date: 2026-09-15T09:21:09Z
expects: ack
waiting-on: curator
parked-on: run:446
---

# Сверка блоба перед меткой ОТКАЗАЛА на живом замере — метку не повесила, а двинула голову #446 слиянием `main`. Новая голова `8c0cca9ac`, `checks` `34951694397` идёт; метка — следующим тактом, после зелени и ПОВТОРНОЙ сверки

Ход остаётся у curator, парк — `run:446`. Кнопка по #446 как стояла, так и стоит за john (гард 4,
`docs/roles/curator.md`), но спрашивать её сейчас не о чем: вердикта на голове нет.

## 1. Распоряжение msg-049 исполнено буквально — и именно поэтому метки нет

msg-049 §2 велит вешать метку на каждый свой PR без метки, не дожидаясь отдельного слова, а §3
ставит перед меткой одну обязательную сверку: блоб `.github/workflows/claude-review.yml` в
`refs/pull/<N>/merge` против `origin/main`; **не совпали — сперва двигать голову**. Сверка отказала:

```
09:15Z   merge-ref ec77a63b0 · claude-review.yml = 4eb5716d8e772f3b0b47b5849af5469003b21859
09:15Z   origin/main 041730a84 · claude-review.yml = b2babcfb972e992cde35a7256e4c8f841619ce46
         дифф по файлу: +35 / −6
```

Причина названа `git log`, а не догадкой: за утро в `main` легли три починки ревьюера — #431
(`11:02:23+03`), #433 (`11:18:42+03`) и **#445 (`12:14:29+03` = `09:14:29Z`)**. Последняя смёржена за
**18 секунд** до начала этого такта.

**Мой же прошлый доклад был верен и протух за 12 минут.** msg-051 §4 доложил эту сверку чистой
(оба блоба `4eb5716d`, замер ~`09:02Z`) и назвал её сделанной. Забираю правилом: сверка блоба —
часть ДЕЙСТВИЯ «повесить метку», а не факт, который приносят из прошлого письма. В контуре, где
воркфлоу ревьюера правится по три раза за утро, чистый замер прошлого такта не значит ничего.

## 2. Расхождение различается РОДИТЕЛЕМ merge-ref, а не повторным чтением

У `ec77a63b0` первый родитель — `117164193`, то есть merge-ref был посчитан на вчерашней голове
`main`. Это и отличает «merge-ref не пересчитан» от «ветка несёт старый файл»; здесь было и то и
другое сразу (голова PR `efef6399a` тоже несла `4eb5716d`). На исполнение различие не влияет — обе
беды лечит одно движение головы, — но без этого чтения доклад называл бы причиной гипотезу.

## 3. Замер, которого не было в #447: merge-ref не будится и ПРИНУДИТЕЛЬНЫМ чтением

- `gh pr view 446 --json mergeable` вернул `MERGEABLE/CLEAN` с первой попытки (`09:15:55Z`) — то есть
  сливаемость GitHub пересчитал;
- три подряд `git fetch origin refs/pull/446/merge` после этого (`09:16:20–09:16:21Z`) отдали
  ПРЕЖНИЙ `ec77a63b0` на прежней базе `117164193`.

Разбудил merge-ref **push, и только он**: после `efef6399a..8c0cca9ac` он стал `8068c0132` на базе
`041730a84`, блоб `b2babcfb` — **равен главному**, примерно через 20 секунд. Строка карточки из
#447 («пересчитывать merge-ref „подождав“ бесполезно») подтверждена со стороны, которой в ней не
было: бесполезно и принудительное чтение сливаемости, а не только ожидание.

## 4. Что сделано с головой #446

Слияние `origin/main` (`041730a84`) в `curator/197-statement-names-guard3` — своей рукой и только в
СВОЮ ветку. Конфликтов нет; слились три чужих пути (`claude-review.yml`,
`review-delivery.integration.sh`, `docs/protocol-reference.md`). **Собственный дифф PR цел и не
вырос:** против merge-base по-прежнему ОДИН путь `docs/roles/curator.md`, +8 строк — проверено
`git diff` после слияния, а не предположено.

- голова **`8c0cca9ac10e1c5c7bb58005f16a4efb360b48b6`**, push без `--force` (слияние, не ребейз);
- прогон `checks` **`34951694397`**, старт `09:17:03Z`, на момент письма `in_progress`;
- **в прогоне не стою** — исход придёт в эту ленту событием.

Дверь на новой голове (`merge-gate --ref origin/main --pr 446 --review-workflow 'Claude PR Review'`):

```
STOP guard 1 · no approve verdict on 8c0cca9      (метки нет — круга нет, так и должно быть сейчас)
STOP guard 2 · not green: checks=IN_PROGRESS
you  guard 3 · thread '197-…' — read the feed
STOP guard 4 · john merges this one — it changes docs/roles/curator.md
ok   mergeability · MERGEABLE (UNSTABLE)
```

Гард 4 стоит **по построению** и движением головы не снимается — это и есть ответ на вопрос «чья
кнопка»: твоя.

## 5. Запись роли по треду 197 дописана КОМАНДОЙ

`journal write --thread 197-… --from curator --write` → `agent-comms/journal/curator/197-tool-text-calls-a-round-not-due.md`,
коммит **`4171b22e5`** на `origin/comms`. Контрольное чтение сделано `git show` по ссылке: файл на
месте, 215 строк (было 143 — дозапись, не перезапись). Ни ветки, ни PR, ни прогона, ни метки под
записью (норма `206`).

## 6. Дома находке НЕ заводила — он уже стоит открытым PR

Правило целиком (сверка блоба перед меткой, включая бесполезность ожидания) уже лежит в **#447**
треда `191`, прозой карточки `curator`. Перечисление `gh pr list --state open` сделано ДО решения,
а не после; второго PR под тот же предмет не открыла, приросты §1–§3 легли записью в журнал.
Если сочтёшь, что замер §3 стоит строки в #447 — это правка чужого по предмету треда, и ставится
она в `191`, не здесь.

## 7. Что делает следующая сессия curator по #446

1. читает исход `checks` `34951694397` **событием в этой ленте**;
2. зелёный — **сверяет блоб ЗАНОВО** (`git rev-parse origin/main:.github/workflows/claude-review.yml`
   против `refs/pull/446/merge:…`), потому что за такт `main` двигают; совпали — вешает `review`
   ОДНИМ действием и передаёт ход, в круге не стоит;
3. не совпали — снова двигает голову слиянием, метку не вешает: круг самопропустится и вернёт
   пустой прогон, негодный гарду 1;
4. красный `checks` — предмет, а не формальность: смотреть глазами, ничего не перевешивать;
5. вердикт есть — **кнопка john** (гард 4), curator доводит до вердикта и докладывает.

## 8. Чего в этот такт НЕ делала

- **чужих открытых PR не судила** (#426, #428, #430, #439–#445, #447, #448): такт связан ровно этим
  тредом, а гард 3 у каждого восходит в СВОЕЙ ленте. Дешёвую половину хвоста (`merge-gate` по своим
  кандидатам) здесь исполнить нечем — своих готовых кандидатов в этом треде нет, #446 под кнопкой
  john;
- **тред не закрыла**: предмет постановки msg-001 закрыт с 14.09, но под тредом живой #446 и твои
  неотвеченные вопросы (§9). Закрытие — приёмка, принимать нечего, пока карточка не решена;
- **развилку 2** (кому курьер отдаёт ход на чисто журнальном PR) не поднимала — отложена тобой
  явно (msg-034 §4) до полевого случая.

## 9. Что висит на тебе

- **кнопка #446** — после вердикта (гард 4, `docs/roles/curator.md`). Сейчас нажимать нечего;
- **перевыдача секрета `CLAUDE_CODE_OAUTH_TOKEN_FALLBACK`** — вопрос из msg-035 без ответа с 14.09.
  Не горит, напоминаю, чтобы не протухло молча.

## 10. Моя рука в этот такт

Чтение ленты; `gh pr view` по #446 и #447 + `gh pr list --state open`; `git fetch` merge-ref и `main`
в отдельные ссылки, `git rev-parse` блобов, `git log` по `claude-review.yml`, чтение родителей
merge-ref; слияние `main` в свою ветку, `git diff` против merge-base, push; `merge-gate` по #446;
контрольное перечитывание merge-ref после пуша; `journal write … --write` + контрольное чтение
`git show`; это письмо.

Рабочее дерево чистое (`git status --porcelain` пуст), незакоммиченного нет, в foreground не стою.
Ход мой, парк — `run:446`.

