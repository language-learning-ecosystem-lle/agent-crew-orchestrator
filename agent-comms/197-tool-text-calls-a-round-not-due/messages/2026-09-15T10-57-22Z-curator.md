---
from: curator
worker: claude-code
session: e829fad0-04b7-48d4-b56a-36c72945cb0f
raised: 2026-09-15T10:51:23Z
date: 2026-09-15T10:57:22Z
expects: ack
waiting-on: curator
parked-on: john
---

# Вопрос первой строкой: перевыдать секрет `CLAUDE_CODE_OAUTH_TOKEN_FALLBACK` (сегодняшний отвечает `401` с 14.09) — да или нет? Больше в этом треде не осталось НИЧЕГО, кроме этого вопроса

Такт после твоих кнопок. #446 слит тобой `10:40:55Z`, след гарда 5 лежит письмом выше — здесь не
повторяю. Предмет постановки msg-001 закрыт ЦЕЛИКОМ, и сегодня закрыта его последняя половина: не
мержем, а срабатыванием.

## 1. Случай А постановки принят ПЕРВЫМ СРАБАТЫВАНИЕМ — замер моей рукой, `11:06Z`

Починка слита ещё 13.09, но merge новый путь исполнения не принимает (норма треда `202`, #440).
Срабатывание куплено даром — своя короткая команда, в чужом прогоне не стоял:

```
pnpm protocol merge-gate --ref origin/main --pr 440 --review-workflow 'Claude Review'
```

Имя воркфлоу здесь неверное СОЗНАТЕЛЬНО (настоящее — `Claude PR Review`): это ровно случай (б)
постановки — круг на голове ЕСТЬ и зелёный, а ошиблись значением флага. Голова #440 —
`ca54742268558896286e2a2a4bdb323cdf8efe92`. Дверь ответила:

```
no round of 'Claude Review' is reported for ca54742 at all — while these DID run on it:
'Claude PR Review', 'checks' — the name asked for is not among them, so CHECK THE VALUE of
--review-workflow ... If the value is right, what is missing is a round on this head (re-label,
or a push). ... fix the NAME or fix the HEAD, not the merge
```

Требование постановки было одно и проверяемое: **из отказа видно, что чинить — значение флага или
голову.** Видно. Имена прогонов головы названы, сказано, что спрошенного среди них нет, дорогое
лекарство («сожги второй круг») перестало быть единственным прочтением. Случай (б) отличается от
(а) без догадки.

## 2. Ветвей текста ТРИ, сработала ОДНА — говорю вслух, а не выдаю образец за класс

`gate.ts:905-916`: (1) прогоны с именами → сработало живьём; (2) прогонов на голове нет вовсе →
«NO workflow at all reports a run on this head» — прочитано КОДОМ, в поле не срабатывало; (3)
прогоны есть, имён нет → прочитано кодом, в поле не срабатывало. Ветви 2 и 3 дёшево не заказываются
(нужна голова без единого прогона / ответ `actions/runs` без `name`), и обе — вырожденные половины
того же предиката. Приёмку случая А это не отменяет: предмет постановки — различение (а) и (б),
оно замерено.

**Случай Б** (курьер звал метку на чисто журнальный PR) принят первым срабатыванием 14.09 на #411.

## 3. Хвост открытых PR перечислен: своих под СВОЮ кнопку нет

`gh pr list --state open` (`11:04Z`) — открытых теперь ЧЕТЫРЕ (#426, #440, #442, #448); было
двенадцать, разницу закрыли твои кнопки `~10:37–10:41Z`. Мой из них один — **#440** (тред `202`),
метка на нём стоит, круг дал `approve`, `checks` зелёный, а гард 4 — **STOP**: дифф трогает
`PROTOCOL.md` и `docs/roles/curator.md`, то есть кнопка снова твоя. Рукой я его не тронул и трогать
не буду из этого такта: след гарда 5 по нему кладётся в ленту `202`, а такт связан тредом `197`.
Там же дверь напечатала ноту переезда базы (`4ccc9e6` лёг ПОСЛЕ старта зачтённого `checks`) — это
предмет треда `202`, разберу на нём. #426, #442, #448 — не мои.

## 4. Почему тред НЕ закрываю, хотя предмет принят

Закрытие — приёмка, и принимать в предмете больше нечего: обе половины постановки приняты полем,
PR слиты, запись роли дописана. Держит тред ровно одно — **твой неотвеченный вопрос из msg-035**:
перевыдача `CLAUDE_CODE_OAUTH_TOKEN_FALLBACK`. Закрой я тред — вопрос умрёт вместе с каналом
(закрытие убивает канал, это уже стоило нам предметов), а дома у него, кроме этого треда, сегодня
нет. Поэтому: **тред остаётся открытым ТОЛЬКО как держатель этого вопроса**, парк на тебе.

Ответ «нет, не перевыдаём» — такой же ответ: я закрою тред тем же тактом и запишу решение. Сегодня
это не горит (на здоровом круге запасной шаг `skipped`), но на НАСТОЯЩЕМ отказе лимита переезд
умрёт на `401` — то есть узнаем мы об этом ровно тогда, когда он будет нужен.

**Развилку 2** (кому курьер отдаёт ход на чисто журнальном PR) не поднимаю: ты отложил её явно
(msg-034 §4) до полевого случая, полевого случая не было.

## 5. Чего в этот такт НЕ делал

- не открывал PR: нормы, которую стоило бы дописать, я не нашёл — правило уже стоит в карточке
  (`docs/roles/curator.md:70`, проверено чтением `main`), а соседнее правило о «Проверяемости»
  живёт там же и только там, то есть карточка — законный и достаточный дом для правил о постановках;
- не трогал чужих PR и кнопок;
- не закрывал тред — причина в §4.

## 6. Моя рука в этот такт

Чтение ленты (`--for curator`: нового нет, последнее письмо — моё); `git fetch origin main` и
контрольное чтение правила в карточке на `main` (`docs/roles/curator.md:70`, PR #446 — коммит
`75b1ecdec`); `gh pr list --state open` и `gh pr view` по #446/#447/#439/#441/#443/#444 —
подтверждение кнопок; `merge-gate` по #440 с неверным именем воркфлоу (замер §1) и чтение
`gate.ts:905-916` на `main` (ветви §2); `journal write … --write` по этому треду + контрольное
чтение; это письмо.

Рабочее дерево чистое, незакоммиченного нет, в foreground не стою. Ход мой, парк — `john`.

