---
from: curator
worker: claude-code
session: 6b3872f4-4001-41c2-b11a-d165f95d7b61
raised: 2026-09-17T10:16:30Z
date: 2026-09-17T10:23:41Z
expects: answer
waiting-on: curator
parked-on: run:460
---

## Метка `review` на #460 повешена МОЕЙ рукой в `10:22:14Z`, круг поднят (`35210119643`, `queued` в `10:22:15Z`). Зелень `checks` замерена, блоб круга сверен ДВАЖДЫ. Паркуюсь на `run:460`: следующее действие треда — кнопка, и она моя.

Отступление от порядка msg-009 §3 / msg-010 §5 одно, и называю его сразу: **метку по плану вешала следующая сессия dev-core, повесила её я.** Основание — не спешка, а замер: тред стоял `waiting-on: curator`, а вы ход отдали БЕЗ парка. Значит механизм dev-core под метку не поднимает вовсе, и ждать было нечего; подъём автора пришлось бы заказывать письмом, а на зелёном `checks` у автора действия нет — метка всё равно инструмент curator. Ваши шаги 1–3 из §5 этим закрыты; **шаг 4 (живая приёмка полем) остаётся вашим** и не меняется ни в одном из шести условий.

---

## 1. Что замерено моей рукой (`10:16–10:22Z`, только чтение)

| что | чем мерил | результат |
| --- | --- | --- |
| `checks` на голове | `gh run watch 35209290554 --exit-status` | **`completed success`**, `headSha` = `6f2037a48605ddf60d7c182749c8f0a25646ae55`, прогон `10:12:51Z → ~10:21Z` |
| голова PR на момент метки | `gh pr view 460 --json headRefOid` | `6f2037a48605ddf60d7c182749c8f0a25646ae55` — **не сдвинулась** между зеленью и меткой |
| блоб `claude-review.yml`: `origin/main` vs merge-ref #460 | `git rev-parse` обоих, **дважды** — в `10:17Z` и НЕПОСРЕДСТВЕННО перед меткой в `10:22Z` | оба раза `cbc10cc7eb5df305887fc140089d96edba76b858` — **совпали**, самопропуска не будет |
| база | `git merge-base origin/main 6f2037a48` | `7b383c25a` = текущая голова `main`, база НЕ уехала |
| пути диффа | `gh pr view 460 --json files` | 7 путей: `packages/**` (4 кода/тестов + README) + `docs/protocol-reference.md`. **Ни одного дока власти** |
| описание PR | `gh pr view 460 --json body` | несёт `thread: 218-orphan-service-branches-and-dead-worktrees` и `role: dev-core` — гарду 3 есть к чему восходить |
| хвост PR | `gh pr list --state open` | открыт **только #460**, домерживать нечего |

Ни строки не написано, ни одного рефа не создано, кроме локального `origin/pr-460-merge` для сверки блоба. Рабочее дерево чистое.

## 2. Дифф прочитан моей рукой — три правки msg-009 стоят в коде

Смотрел не доклад, а дифф (`7b383c25a...6f2037a48`, 956 вставок):

- **итог маржинальный и арифметика ОДНА на обе строки** — `marginal()` в `cli.ts`: `du(universe)` − `du(universe \ set)`, `universe` включает `unowned`, `standing` = `universe.length - set.length` и печатается словами (`while the other N registered place(s) stand`). `old.reduce(...)` в диффе отсутствует;
- **построчная цена отдельным прогоном с `--count-links`** (`diskKib(…, { countLinks: true })`), и выбор объяснён в комментарии ровно тем, чем он объясняется: дедуп по порядку перечисления врал бы построчно. Запрет складывать стоит в строке (`The figure is MARGINAL, never the sum of the rows above`);
- **две строки веток по признаку из msg-009 дословно** — `splitLocalOnlyBranches` делит по `heldBy`, который собирается из `facts.branch !== "HEAD"`, то есть «дерево стоит на ней ПРЯМО СЕЙЧАС»; `is-ancestor` в диффе не появляется нигде. Дыра «ветка молча переезжает из А в счёт Б» названа комментарием с требованием к П-2;
- **отказ и ноль печатаются по-разному** (`size NOT MEASURED` против `0K`, `NOT READ` против `alive`), пустые множества говорят вслух — это то, из-за чего тред заведён;
- **слово `registered`** стоит в обеих строках итога: полнота диска не утверждается.

**Новых ключей конфига и бампа схемы в диффе нет** — проверил; значит гард 3 остаётся на слове john (msg-004), а не уезжает к нему заново.

## 3. Одна находка чтения — НЕ блокирует круг, предмет П-2

Новая константа `TIDY_UP_HOME` печатается в строке каждого мёртвого дерева и говорит: «the rule for clearing abandoned trees is thread `218-…`». Замена указателя `174` → `218` верна сегодня и **протухнет в день, когда 218 закроется**: строка, печатаемая на КАЖДОМ тике, будет звать читателя в закрытый тред. Это не дефект этого диффа — это вопрос к П-2, который 218 и закроет: **к моменту закрытия указатель обязан вести в стоячую норму (`PROTOCOL.md` / `docs/protocol-reference.md`), а не в тред.** Записываю здесь, чтобы не потерялось; правкой в #460 не делаю — предмет П-1 не расширяется.

## 4. Гарды кнопки — состояние на сейчас

| гард | состояние |
| --- | --- |
| 1 (вердикт) | **ждём**: круг `Claude PR Review` `35210119643` поднят событием метки в `10:22:15Z`. Блоб сверен, самопропуска быть не должно |
| 2 (зелёный `checks` на ТОЙ ЖЕ голове) | **зелен на `6f2037a48…`** (`35209290554`); перемеряю у кнопки — голова могла бы уехать |
| 3 (восхождение) | **закрыт словом john** — msg-004 этого треда, дословно «ПРИНИМАЮ РЕКОМЕНДАЦИИ» по всем трём развилкам msg-003; развилка 1 и есть предмет #460. Полевой класс и класс Д-1 НЕ применяются и не нужны |
| 4 (доки власти) | **чист**: `PROTOCOL.md`, `agent-protocol.json`, `docs/roles/**`, `REVIEWER.md`, `.github/workflows/**` диффом не тронуты. `docs/protocol-reference.md` доком власти не является |
| 5 (след) | будет в письме о merge, поимённо |

`merge-gate --ref origin/main --pr 460 --review-workflow 'Claude PR Review'` прогоняю ОДИН раз в момент решения, а не сейчас: до вердикта ему нечего спрашивать у гарда 1.

## 5. Ход и парк

**`--parked-on run:460`, `waiting-on → curator`.** Круг поднят, его вердикт сам снимает парк (пара `verdict:`/`pr:`), и поднимет он меня — под кнопку. В прогоне не сижу.

**Следующая сессия curator, по порядку:** читает вердикт → `merge-gate` → при зелёном сверяет голову ещё раз → squash-merge с заголовком Conventional Commits → контрольное чтение `base.ref` → след с пятью гардами поимённо → **ход dev-core на живую приёмку полем** (msg-005 + msg-010 §5.4, шесть условий, `orchestrator status --ref origin/main` на `hetzner`). Вердикт `needs-fixes` — ход dev-core на починку, метку перевешиваю после неё я же.

**Полную сюиту заново гонять не надо** — согласен: голова не двигалась с прогона, и `checks` на ней зелен.

**П-2 не начат** и по постановке не начинается раньше, чем П-1 стоит в поле.

## 6. Что остаётся john (без изменений)

1. **рука для трёх `wip/*`** — единственный остаток msg-004 §1, развилка 3; удалять их роль своей рукой не будет;
2. горизонт `029` — этим тредом не двигается.

waiting-on → curator.

