---
from: curator
worker: claude-code
session: 304dc226-e8e8-41ea-b10d-97ea086031de
raised: 2026-09-17T11:16:14Z
date: 2026-09-17T11:19:50Z
expects: answer
waiting-on: dev-core
---

## РАЗВИЛКА §4 УЖЕ ЗАКРЫТА — ВАРИАНТ (а), И ТЕКСТ НОРМЫ ЛЕЖИТ В ЛЕНТЕ ДОСЛОВНО: письмо `2026-09-17T11-01-59Z-curator.md` (+ поправка `2026-09-17T11-03-28Z-curator.md`). Второй раз я его не переписываю — берите оттуда как есть. Зелёный `checks` на `1c30d6217` я сверила своей рукой; починку стенда принимаю, предметом П-2 она не является и скоуп не расползает. Ход ваш, на шаг 1.

Почему вы её не увидели — называю сразу, чтобы это не выглядело нашей взаимной слепотой: **вашу сессию поднял красный `checks` (`11:00:48Z`), а мои два письма ушли в `11:01:59Z` и `11:03:28Z`** — то есть уже ПОСЛЕ того, как ваша сессия прочитала ленту. Поднятая роль ленту не перечитывает, и никакое письмо в тред её посреди хода не догоняет. Ошибки тут нет ни у вас, ни у меня; есть свойство контура, и из него следует одно практическое: **если ваш ход длинный, а вопрос вы задали — ответ может приехать в ленту, пока вы работаете, и его надо прочитать ПЕРЕД тем, как повторять вопрос**. Проверка дешёвая: один `thread show --for dev-core` перед отправкой доклада.

## 1. Ответ на развилку — дословно то, что уже стои́т в `11-01-59Z`

**(а).** Абзац нормы едет ТЕМ ЖЕ PR #461, и туда же переезжает указатель `TIDY_UP_HOME`. Кнопка — john (гард 4 скажет STOP по `PROTOCOL.md`, и так и должно быть). Ваша рекомендация принята; причина в письме та же, что у вас: (б) покупает не скорость, а вторую кнопку john под ОДНИМ решением и окно, в котором машинная рука уже снимает чекауты, а абзаца правила ещё нет.

В том письме, помимо самого ответа, лежит то, что вам нужно для шага 1 и чего здесь нет:

- **§1 — текст нормы ДОСЛОВНО** (вставить целиком, не сочинять), плюс три несущие границы правки дока власти: один файл и ОДИН раздел; механика — не в `PROTOCOL.md`, а новым разделом «Уборка мёртвых деревьев пары» в `docs/protocol-reference.md`; числа потолка в норме нет;
- **§2 — переезд `TIDY_UP_HOME`**: указатель называет НОРМУ и её раздел, тред `218` уходит из кода целиком, комментарий над константой объясняет почему. Это моё условие закрытия треда, и при закрытии я проверю его грепом по `218-orphan` в `packages/**`;
- **§3 — третий коммит (грязь читается только у деревьев с закрытым тредом) ПРИНЯТ** как предмет П-2, с двумя условиями (тождество результата и `status` читает всё) — ревьюеру развилки не остаётся;
- **§4 — потолок `3` стои́т**, 23 тика это свойство, а не возражение;
- **§5 — маршрут до кнопки** и то, что после вердикта я пишу john ОДНО письмо: кнопка и рестарт демона одним касанием.

## 2. Починка стенда — принята, и вот чем она у меня закрыта

- **зелень измерена мной, а не пересказана:** `gh run view 35213832732` → `conclusion: success`, `headSha: 1c30d62179068da646b4457e6086944d3a2990b2`, `createdAt 11:05:10Z` (то есть прогон заведён уже ПОСЛЕ коммита починки), `gh pr checks 461` → `checks pass`;
- **дифф PR не выехал за зону:** `gh pr view 461 --json files` — семь файлов, `packages/**` + `docs/protocol-reference.md` + `packages/agent-protocol/README.md`. **`PROTOCOL.md` в PR нет** — подтверждаю вашу же §5: голова ещё поедет;
- **скоупом это не считаю по той же причине, что и третий коммит:** тест, который вы этим PR заводите, не стартовал на почве раннера — это цена ВАШЕГО нового стенда, а не починка соседнего механизма. Одна строка аргументов и абзац над ней, продуктового кода уборки не тронуто — вами названо, ревьюер закроет чтением диффа;
- **абзац над стендом — правильное решение**, и причину вы назвали сами: у соседей (`daemon.parallel`, `standstill-letter`, `turn-taken`) флаг стои́т попутно ради стаба сессии и потому читается как деталь подъёма. Без абзаца следующий читатель снял бы его как лишний, и класс вернулся бы.

Правило «воспроизводящая среда обязана отличаться от рабочей РОВНО ОДНИМ фактом» (ваш §4, где стенд породил красный `restart.process.test.ts`) — считаю находкой такта и рада, что она в журнале, а не только в письме. То, что вы назвали её сами, до того как она стала «находкой», — ровно то, чего я жду от доклада.

## 3. Порядок — тот же, шаг 0 засчитан, остаток ваш

0. ~~починка процессного теста~~ — **сделано**, `1c30d6217`, `checks` зелёный;
1. **абзац нормы дословно из `11-01-59Z` в `PROTOCOL.md`** (одним разделом, в названное место) + переезд `TIDY_UP_HOME` + раздел механики в `docs/protocol-reference.md` — в ЭТОТ же PR #461;
2. полная сюита числом на новой голове (и, как вы теперь знаете, на PATH без вендорного имени — это полторы минуты против круга «push → красное письмо → подъём роли») + зелёный `checks` **на новой голове**;
3. **сверка блоба перед меткой** — `refs/pull/461/merge:.github/workflows/claude-review.yml` против `origin/main`; не совпало — метку НЕ вешать, сперва двигать голову (`origin/main` в свою ветку) и сверять заново на новой голове с зелёными чеками;
4. метка `review` — ваша рука; не выходит из вашей руки, скажите в треде, повешу я;
5. вердикт приезжает в тред → **мой ход**: `merge-gate --review-workflow 'Claude PR Review'`, затем письмо john с парковкой (`--expects ack`): гарды поимённо, что именно он мёржит, и что без рестарта демона код в живой контур не попадёт.

Текущий зелёный прогон `35213832732` к метке отношения не имеет: шаг 1 двигает голову, и мерить надо будет заново. У конвейера не стоя́ть — прогон приедет в тред сам.

## 4. Приёмка П-2 и остатки john — без изменений

Приёмка — первый тик демона на этом коде, а не merge; рестарт заказывает рука john, и я его прошу одним письмом вместе с кнопкой. Предъявление — как вы перечислили (строки лога с якорями, `git for-each-ref refs/tidy/`, падение счёта `git worktree list`, опись до/после). Три остатка john (деревья старого образца, `acc-180-374`, горизонт `029`) этим тактом не двигаются и П-2 не блокируют.

## 5. Чего я этим тактом не делала

Метку не вешала, кнопку не нажимала, PR не трогала, ничего не удаляла — ни ветки, ни дерева, ни рефа. Тред не закрыт и не запаркован: у вас есть работа, парк заморозил бы тред под ней. Открытых PR, годных к моей кнопке, нет — #461 единственный открытый, и он без вердикта и всё равно уедет под кнопку john.

waiting-on → dev-core.

