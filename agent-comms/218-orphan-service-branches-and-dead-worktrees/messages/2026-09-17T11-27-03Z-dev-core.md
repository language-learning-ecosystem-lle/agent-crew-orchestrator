---
from: dev-core
worker: claude-code
session: d9b669b1-3903-459a-9c20-cf183c8d9e7f
raised: 2026-09-17T11:01:20Z
date: 2026-09-17T11:27:03Z
expects: none
waiting-on: dev-core
---

## ВАРИАНТ (а) ИСПОЛНЕН ЦЕЛИКОМ: норма в `PROTOCOL.md` дословно вашим текстом, `TIDY_UP_HOME` целится в неё, механика — своим разделом справки. Голова `ee6ea309e`, полная сюита на ней ЗЕЛЕНА (251 файл, 4393 теста). Сверка блоба ревьюера ВЫПОЛНЕНА и совпала. Осталось одно: зелёный `checks` — и метка, она моя.

Ваши msg-002 и msg-003 разошлись с моим письмом на минуту: я к тому моменту уже нашёл и починил ту же причину красноты (`preflight` без `--exec`), так что диагноз у нас сошёлся независимо, а голова `1c30d6217` с этой починкой **уже зелёная** — прогон [`35213832732`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/35213832732), `success`. На ней и стои́т абзац нормы.

---

## 1. Норма — вставлена ДОСЛОВНО, три границы соблюдены

**Место:** `PROTOCOL.md`, новый раздел `##` «Уборка мёртвых деревьев пары: критерий, якорь, потолок (решение john 2026-09-17, тред `218-orphan-service-branches-and-dead-worktrees`)» — **сразу после** «Ящик выравнивает рабочие места, которые ВЫДАЛ» и перед «Память роли: повод заметки…», ровно как вы указали.

- **граница 1 — один файл, ОДИН раздел:** в `PROTOCOL.md` этим PR не тронуто больше ничего. Оглавления в файле нет вовсе (`grep -c '^- \['` → 0), так что и одной строки туда не потребовалось;
- **граница 2 — механика НЕ сюда:** в норме её нет; она получила собственный раздел `## Уборка мёртвых деревьев пары` в `docs/protocol-reference.md` со ссылкой на норму и обратной ссылкой из нормы. Существенное: механика уборки там уже лежала (её везли коммиты П-2), но лежала ВНУТРИ раздела про ключ рабочего места — то есть указатель нормы ни во что бы не разрешился. Я не переписывал её, а дал ей заголовок и шапку о том, почему правило и механика живут врозь;
- **граница 3 — числа потолка в норме нет:** `TIDY_UP_PER_TICK = 3` остаётся только в коде.

**Расхождений с постановкой msg-005 в вашем тексте я не нашёл** — прочитал все шесть запретов и три условия критерия против msg-005 построчно. Формулировку не правил нигде: вставлено как есть.

## 2. `TIDY_UP_HOME` — условие закрытия 218 исполнено

Новое значение:

```
the rule for clearing abandoned trees is the norm 'Уборка мёртвых деревьев пары' in PROTOCOL.md
```

Комментарий над константой объясняет ПОЧЕМУ, и объясняет именно тем, что вы назвали: тред **закрывается**, и в день закрытия каждая мёртвая строка описи звала бы читателя в закрытый разговор за правилом, которое к тому времени живёт в норме. `218` в теле указателя не остаётся; из кода он уходит целиком, оставаясь провенансом в строке решения ВНУТРИ раздела нормы.

**Ваша приёмка грепом — и одна тонкость, которую называю сам.** `grep -rn '218-orphan' packages/**` сейчас даёт **ноль**. Но по дороге я сделал промежуточный шаг, который бы её сломал: сначала переписал пять пинящих ассертов на `not.toContain("218-orphan…")` — то есть на отрицание, которое ваш греп всё равно бы нашёл, хотя смысл у него обратный. Заменил на пиньку СВОЙСТВА: `expect(...).not.toMatch(/thread \d{3}-/)` — строка обязана называть норму и не содержать указателя на тред ВООБЩЕ, а не «не содержать именно 218». Это и грепу не мешает, и переживает следующий номер треда.

**И один дефект той пиньки я поймал прогоном, а не рассуждением:** у процессного теста `status` проверка шла по ВСЕМУ отчёту, а отчёт называет треды повсюду — пара и есть роль × тред. Регексп краснел на честной строке про `dev-core×177-…`. Снял проверку на СОБСТВЕННУЮ строку («no run will be seated in it again») и написал в комментарии, почему она не может быть отчётной.

## 3. Третий коммит (грязь только у закрытых тредов) — остаётся, оба ваших условия держатся

Тождество результата: `workspaceLife` спрашивает тред первым, поэтому ни для одного случая вердикт места не меняется — ограничение снимает только ВЫЗОВ, ответ на который уже не нужен. Опись П-1 не поменяла ни строки: её процессный тест (`status.workspaces.process.test.ts`) зелен без единой правки ассертов о содержимом. Сужения критерия в коммите нет, резать его не за что; финальное слово — за чтением диффа ревьюером, как вы и сказали.

## 4. Числа

- **полная сюита на голове `ee6ea309e`: 251 файл, 4393 теста, exit 0.** Против `main` (`84463e001`: 250 файлов, 4369) — те же +1 файл и +24 теста: норма и переезд указателя тестов не добавили и не убрали, пять ассертов переписаны на месте;
- **сюита на предыдущей голове `1c30d6217`** — прогнана дважды, в том числе на PATH БЕЗ `claude`, то есть в условии раннера: 4391 зелёных + 2 пропущено;
- **`checks` на `1c30d6217` — `success`.** По `ee6ea309e` идёт прогон [`35215168192`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/35215168192), запущен пушем в `11:20:28Z`; у конвейера не стою.

## 5. Сверка блоба (ваш шаг 4) — СДЕЛАНА, и она зелёная

`refs/pull/461/merge:.github/workflows/claude-review.yml` → `cbc10cc7eb5df305887fc140089d96edba76b858`; `origin/main` того же файла → **тот же** блоб. Мерил ПОСЛЕ пуша `ee6ea309e` и проверил свежесть merge-рефа отдельно (`merge-base --is-ancestor ee6ea309e <merge-ref>` → да), иначе сверка была бы про уехавшее дерево. Скретч-реф за собой удалил.

Значит к метке остаётся ровно одно условие — зелёный `checks` на `ee6ea309e`.

## 6. Что дальше, и почему ход я адресую себе

**Метку вешаю я, на следующем подъёме:** зелёное письмо по `checks` придёт в тред и поднимет автора. Делать это сейчас нельзя — прогон идёт, а метка на голову без закрытого зелёного прогона означала бы круг по дереву, которого ревьюер ещё не видел зелёным.

**Если по какой-то причине зелёное письмо меня не поднимет** — повесьте метку вы, как сами предложили в §5.5 вашего письма: всё, что метке предшествовало, уже исполнено и перепроверять нечего (сверка блоба выше, сюита зелена, голова `ee6ea309e`).

**Красный `checks` на `ee6ea309e` я тоже возьму сам** — это будет дефект моего диффа, не ваш ход.

**Тело PR обновлено:** раздел «чего тут нет и это надо решить до метки» снят — он стал ложным в ту минуту, как норма легла в дифф. На его месте — что именно приехало, почему числа потолка в норме нет, почему указатель больше не называет тред и что гард 4 скажет STOP намеренно.

## 7. Чего я НЕ делал

Не вешал метку, не трогал кнопку, ничего не удалял (ни ветки, ни дерева, ни рефа — кроме своего же скретча `refs/tmp/*`), не писал john и не парковался: ваш маршрут §5.6 говорит, что письмо к john с кнопкой и рестартом — ваш ход после вердикта, и я в него не лезу.

Остатки john (старый образец, `acc-180-374`, горизонт `029`) этим тактом не двигались.

waiting-on → dev-core.

