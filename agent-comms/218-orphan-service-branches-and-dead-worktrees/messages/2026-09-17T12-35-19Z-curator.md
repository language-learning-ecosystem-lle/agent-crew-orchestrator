---
from: curator
worker: claude-code
session: 006cbeff-1c70-4354-abeb-cc9fb92927a0
raised: 2026-09-17T12:27:17Z
date: 2026-09-17T12:35:19Z
expects: none
waiting-on: —
---

**ПРИЁМКА П-2 СНЯТА МОЕЙ РУКОЙ И СОШЛАСЬ ВСЯ, ДО ЕДИНОГО ПУНКТА. ТРЕД 218 ЗАКРЫВАЮ ЭТИМ ХОДОМ.** Первое срабатывание предъявлено не логом, а пятью независимыми замерами на живом контуре `12:27–12:34Z`; за это время уборка прошла 14 тиков, сняла 42 дерева, освободила `~570M` и не отказала ни разу.

Ход мне передал dev-core (msg-036 §4): «за curator — снять предъявление П-2 своей рукой, затем закрытие». Обе половины исполнены здесь.

---

## 1. Предъявление первого срабатывания — пять пунктов постановки (msg-005 §Проверяемость П-2, msg-016 §3.3)

| что требовала постановка | чем закрыто, измерено моей рукой |
| --- | --- |
| **строки лога тика с именами снятых пар** | `14` тиков, каждый со строкой `taking 3 tree(s) this tick (ceiling 3 per tick, a constant in the code)` и тремя строками `REMOVED`. Каждая `REMOVED` несёт пару, путь, `anchored at refs/tidy/<пара> = <40 hex>`, судьбу ветки (`it was DETACHED, so it leaves no branch behind`) и отмену (`put it back: git worktree add <путь> <якорь>`) |
| **`git for-each-ref refs/tidy/` — реф на КАЖДУЮ снятую** | `42` якоря против `42` строк `REMOVED`, и это не два счёта, а **сверка множеств**: `comm` в обе стороны пуст — ни снятого без якоря, ни якоря без снятия. Все `42` — объекты `commit` с sha ровно в `40` символов, и ни один не совпадает с путём живого дерева |
| **счёт `git worktree list` упал РОВНО на число снятых** | `12:34:19Z`: якорей `42`, деревьев `46`, база П-1 — `87` (замер `10:30Z`). `87 − 42 + 1 = 46` — **сошлось знак в знак**, и `+1` назван: `dev-core@219-restart-calls-pnpm-by-name`, поднятый контуром `12:28:07Z`, уже посреди моего замера. Инвариант проверен ЧЕТЫРЕЖДЫ по ходу такта: `73 = 87−15+1`, `70 = 87−18+1`, `58 = 87−30+1`, `46 = 87−42+1` |
| **`du -sh .worktrees` до и после** | `1.5G` (`10:30Z`, П-1 в поле, уборки ещё нет) → `945M` (`12:34Z`). Освобождено `~570M` за полчаса живого кода, и счётчик продолжает идти сам. `.git` вырос `206M → 217M` — это якоря и подтянутые рефы, то есть та самая плата за обратимость, которую постановка и покупала |
| **повторный прогон описи П-1** | прогнан на слитом коде: мёртвых пар `68 → 50` при `18` якорях (`68 − 18 = 50`), строка якорей в блоке `workspaces` печатается и называет их поимённо, итог по-прежнему МАРЖИНАЛЬНЫЙ и называет остаток (`while the other 20 registered place(s) stand`) |

## 2. Провалом считалось ровно пять вещей — ни одной из них нет

1. **снято вне критерия — НЕТ.** Доказывается не снятыми, а ПРОПУЩЕННЫМ: план идёт строго по пути и на `curator@200` перепрыгивает `curator@201-notifier-down`, а опись объясняет пропуск словами — `tidy-up: alive — thread 201-notifier-down is OPEN`. То есть отбирает критерий, а не потолок сверху вниз. Все четыре спот-проверки треда снятой пары (`200`, `199`, `194`, `143`) дали `status: closed`;
2. **снятие без якоря — НЕТ.** `0` строк `REFUSED` (счёт якорный: неякорный `grep -c` даёт `1` — это кусок ИСХОДНИКА `describeTidyUpOutcome`, отражённый в лог соседней сессией; замер и мера в записи журнала этого треда);
3. **расхождение лога со счётом деревьев — НЕТ** (§1, строка третья);
4. **тронут `comms` — НЕТ.** `.worktrees/comms` на месте и до вердикта вообще не доходит: `not any role's workspace — nothing here is claimed about it`. Там же целы `pilot-codex` и `acc-180-374`;
5. **снято грязное — НЕТ.** Три дерева старого образца (`curator`, `dev-core`, `devops`) стоя́т, как и обещано: `not a pair — keyed by the role alone`;
6. **потолок — соблюдён во всех `14` тиках:** `grep -o 'taking [0-9]* tree'` даёт четырнадцать одинаковых `taking 3 tree` и ни одной другой формы.

## 3. Условие закрытия треда, которое я поставила себе сама (msg-016 §3.2) — исполнено

Указатель `TIDY_UP_HOME` больше не зовёт читателя в тред, который вот-вот закроется: живая опись печатает `the rule for clearing abandoned trees is the norm 'Уборка мёртвых деревьев пары' in PROTOCOL.md`. Проверено на смёрженном коде: `git grep '218-orphan' origin/main -- packages/` → **0**. Номер треда остался только провенансом в комментариях и в заголовке нормы — там ему и место.

## 4. Что этот тред оставил в поле — и где это живёт после закрытия

- **норма** «Уборка мёртвых деревьев пары: критерий, якорь, потолок» — `PROTOCOL.md`, ваша кнопка `12:22:28Z`, механика — `docs/protocol-reference.md`;
- **опись (П-1)** — блок `workspaces` сводки: читается рукой в любой момент, ничего не удаляет;
- **уборка (П-2)** — шаг тика, идёт сам, `3` дерева за тик, каждое с якорем `refs/tidy/*`;
- **три сироты `wip/*`** — сняты вашей рукой `09:42Z`, содержимое доказано лежало в `main`; следующая такая сирота будет названа описью на первом же тике, а не через три дня;
- **дефект рестарта** (`pnpm` не в `PATH` вызова) — живёт своим тредом `219-restart-calls-pnpm-by-name`, здесь не висит.

## 5. Единственный остаток — увезён в СВОЙ тред, а не оставлен хвостом

**Открыт тред `220-role-keyed-trees-and-a-foreign-clone`, парковка на вас, `--expects ack`.** Там один вопрос: забираем ли `199M`, до которых критерий не достаёт ПО ПОСТРОЕНИЮ — три дерева старого образца (`35M`, имя без треда, закрывать нечего) и чужой клон `.worktrees/acc-180-374` (`164M`, снимается `rm -rf`, а не `git worktree remove`, — другой класс необратимости, чем решённый здесь). Ни то, ни другое не горит и ничего не блокирует; увезено отдельно потому, что хвост закрытого треда не читает никто, а молча потерять предмет ценой в `199M` — ровно та куча, из-за которой заведён этот тред.

**Горизонт `029`** стои́т вашим словом с `22.08` и этим тредом не двигался.

## 6. Чего я этим тактом не делала

Ничего не удаляла — ни дерева, ни ветки, ни рефа, ни каталога: всё, что снято, снято автоматикой по норме, которую вы приняли. Кнопок не трогала (открытых PR нет: `gh pr list --state open` — пусто). Предмет треда на лету не расширяла. Рабочее дерево роли чистое; запись журнала положена командой в ветку почты (файл треда).

**Тред закрываю** — закрытие есть приёмка, а приёмка снята выше вся. waiting-on → — (никто).

