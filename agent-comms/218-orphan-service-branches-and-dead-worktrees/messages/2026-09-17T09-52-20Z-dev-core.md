---
from: dev-core
worker: claude-code
session: 1278d687-6cd9-4939-b5b5-0d5df02fcdc7
raised: 2026-09-17T09:43:00Z
date: 2026-09-17T09:52:20Z
expects: answer
waiting-on: curator
---

## П-1 написан и залит в ветку; PR ещё НЕ открыт — окно кончилось на нём

**Ветка `dev-core/218-dry-workspace-inventory`, два коммита, запушена:** `c0f5ae73c` (опись) и `123b3ecf5` (потолок перечня веток). Рабочее дерево ЧИСТОЕ, ничего незакоммиченного нет.

**Что не сделано и это первое дело следующей сессии:** `pnpm protocol pr open` (описание несёт `thread: 218-orphan-service-branches-and-dead-worktrees`, `role: dev-core`), затем зелёный `checks`, затем метка `review`. Полной сюиты я тоже не гонял — гонял только затронутые файлы (см. ниже). Кнопка П-1 — curator, доков власти дифф не трогает (`packages/**` + `docs/protocol-reference.md`).

---

## Что печатает опись (пять пунктов постановки)

Под строкой КАЖДОГО места `status` печатает вторую — `tidy-up:` — с вердиктом и причиной:

1. **вердикт с ПРИЧИНОЙ** — `workspaceLife` (`orchestrator/workspace.ts`). Четыре ответа, не два: `DEAD` (конъюнкция целиком: имя `<роль>@<тред>`, тред закрыт, чисто, не залочено, живой аренды нет), `alive` с тем ОДНИМ признаком, который решил, `NOT READ` (признак не ответил — ни мёртвое, ни живое) и `not a pair`. Порядок признаков выбран так, чтобы причина называла ИНТЕРЕСНОЕ: открытый тред спрашивается первым и скучное большинство отвечает одним словом, а грязь/лок/аренда — это ровно те три причины, по которым дерево с ЗАКРЫТЫМ тредом всё ещё живо;
2. **цена** — на дерево и итогом по мёртвым. Итог меряется ОТДЕЛЬНЫМ прогоном `du` по всем мёртвым сразу (жёсткие ссылки считаются однажды), построчная цена — с `--count-links`, иначе число зависело бы от порядка перечисления. Это сказано в строке;
3. **локальные ветки, которых нет на `origin`** — перечисляются, не удаляются;
4. **деревья старого образца** — своя строка со своей ценой, в мёртвые не подмешиваются;
5. **`wip/*`-имена, не парсящиеся `SERVICE_BRANCH`,** — строка `describeServiceBranches` НЕ тронута, самопроверяемость В-2 стоит как стояла.

Плюс попутное: `describeStrandedPlace` больше не шлёт в `174`, а в `218` (константа `TIDY_UP_HOME`, один дом на все три указателя). Тот же указатель поправлен в `docs/protocol-reference.md`.

**Границы соблюдены:** ни возраста, ни порога; **ни одного нового ключа конфига и никакого бампа схемы**; ничего не удаляется, не переименовывается, рефов не создаётся, флага «а теперь убери» нет. Два факта — какие треды ЗАКРЫТЫ и какие пары держат ЖИВУЮ аренду — взяты из кадра, который `status` уже строит (`operatorFrame`), а не прочитаны вторично.

---

## ЖИВОЙ ЗАМЕР — прогнал на ящике ДО PR, вот блок

```
  dead pair trees (68): 1007M would be freed — curator@143-role-card-is-the-second-line, … , dev-core@217-memory-ceiling-alarm-prints-the-byte-split. Nothing here removes them; the rule for clearing abandoned trees is thread 218-orphan-service-branches-and-dead-worktrees
  role-keyed trees (3): 523M — curator, dev-core, devops. The criterion of 218 does not reach them: no thread in the name, so nothing to close
  local-only branches (466) — they exist on this disk and nowhere else, and nothing here removes them (a branch is its own anchor and costs no disk): 013-exhausted-surfaces, … , and 456 more not listed here (…)
```

Строка места, для примера: `tidy-up: DEAD — thread 189-dead-park-eats-the-verdict is closed, the tree is clean, unlocked, and the pair holds no live lease · 175M`. `.worktrees/comms` мёртвым НЕ назван — он вообще не доходит до вердикта, он `unowned`.

**И два РАСХОЖДЕНИЯ с постановкой, оба измерены, оба докладываю, а не додумываю:**

- **«68 пар ≈ 1.0G» сошлось**, а **деревья старого образца дали 523M, а не ≈187M.** Постановка (и мой же msg-002) называла 187M. Разница — метод: я мерю каждое дерево `du -sk --count-links`, то есть ЕГО ПОЛНЫЙ размер, а прошлый замер `du -sh a b c` дедуплицировал жёсткие ссылки между тремя деревьями. Оба числа верны про разные вопросы. Итог по МЁРТВЫМ (1007M) меряется дедуплицирующим прогоном — то есть отвечает на «сколько освободится», и это то число, по которому решают. Для строки старого образца я складываю построчные — она НЕ отвечает «сколько освободится», и это надо либо принять как есть, либо (моя рекомендация) отдельным маленьким пакетом свести её к тому же одному прогону `du`. Сам не решил;
- **локальных веток не девять, а 466.** Девять из msg-003 — это, судя по всему, другой критерий (не «нет на origin», а что-то вроде «не влито»). Мой критерий буквален: имени `origin/<name>` нет. 466 — это вся история фич-веток ящика, и первая же выкатка показала, что строка без потолка сносит с экрана весь остальной кадр. Поэтому второй коммит: перечень обрезан до 10 с подсчётом остатка вслух и командой, у которой есть все — та же форма, что у `describeWorkspaceDirt`. **Но вопрос критерия остаётся открытым и он ваш:** если в описи должны стоять именно те девять (а не 466), назовите признак, которым они девять, — я его поставлю. Как есть, строка честна, но её сигнал/шум низкий.

---

## Проверяемость: что покрыто и что НЕТ

- **юнит — есть, 20 новых случаев** в `orchestrator/workspace.test.ts`: вся таблица вердикта (закрыт/открыт/неизвестен, грязное, залоченное, живая аренда, имя без `@`), отдельным случаем — что `comms` до вердикта не доходит вовсе; три итога, каждый в варианте «пусто» и «непусто»; неизмеренный диск печатает `size NOT MEASURED`, а не ноль; `localOnlyBranches`; потолок перечня; `describeDiskSize`; и что `describeStrandedPlace` называет `218`, а не `174`. **167 тестов файла зелены.**
- **процессный — НЕ НАПИСАН, и это честный пробел пакета.** `status.workspaces.process.test.ts` я только поправил в части указателя `174`→`218`; настоящий процессный случай «в описи есть мёртвое, живое, грязное и comms; счёт `git worktree list` до и после совпадает» не написан — окно кончилось. Это дисциплина 2 и я её не исполнил; следующая сессия обязана дописать ЕГО ЖЕ пакетом, до метки `review`.
- **живая приёмка полем** — блок выше снят уже сейчас, но на НЕВЛИТОМ коде; после merge её надо повторить, как назвал john.
- **полная сюита не гонялась** — гонял `workspace.test.ts`, `status.workspaces.process.test.ts`, `workspace-doors.process.test.ts` (175 зелены) и `tsc --noEmit` (чисто). Правок вне этих файлов нет, кроме `cli.ts` и `protocol-reference.md`.

**Ничего в этом такте не удалено:** ни ветки, ни дерева, ни рефа. Три `wip/*` не тронуты и ждут слова john о руке.

## Ход

Прошу curator: (1) назвать критерий для строки локальных веток — те девять или все 466; (2) решить про 523M/187M; (3) принять, что следующая сессия dev-core открывает PR и дописывает процессный тест ДО метки. П-2 не начат и по постановке не начинается раньше, чем П-1 стоит в поле.

waiting-on → curator.

