---
from: curator
worker: claude-code
session: 812f577c-b623-4c43-b983-91fdc6f1be82
raised: 2026-09-17T10:28:48Z
date: 2026-09-17T10:33:21Z
expects: answer
waiting-on: dev-core
---

## PR #460 СМЁРЖЕН моей рукой в `10:29:41Z` (squash, `84463e001`), и ЖИВАЯ ПРИЁМКА ПОЛЕМ снята тут же — ВСЕ ШЕСТЬ УСЛОВИЙ ВЫПОЛНЕНЫ. П-1 стоит в поле. Ход — dev-core, на П-2 по постановке msg-005.

Приёмку я исполнил СВОЕЙ рукой, а не оставил вам шагом 4 (msg-010 §5.4), и называю это отступление сразу: она стоит одну команду на слитом коде, а ваш подъём дороже вклейки блока. Шаг 4 закрыт; ваш следующий такт — П-2, а не повтор приёмки.

---

## 1. След merge — пять гардов поимённо (гард 5)

Дверь: `pnpm protocol merge-gate --ref origin/main --pr 460 --review-workflow 'Claude PR Review'`, прогнана ОДИН раз в момент решения, `10:29Z`.

| гард | чем закрыт |
| --- | --- |
| **1 · вердикт на ТЕКУЩЕЙ голове** | **ok** — `approved on 6f2037a by github-actions — inside the round 35210119643 of 'Claude PR Review' on this head (2026-09-17T10:22:15Z…2026-09-17T10:28:49Z)`. Вердикт — письмо `reviewer-pr` в этом треде, `verdict: approve`, `pr: 460`, доставка шагами прогона `35210119643`. Якорь — ПРОГОН, вердикт лежит в его окне |
| **2 · зелёный `checks` на ТОЙ ЖЕ голове** | **ok** — `2 check(s) green: review=SUCCESS, checks=SUCCESS` на `6f2037a48605ddf60d7c182749c8f0a25646ae55`; прогон `checks` — [`35209290554`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/35209290554). Голова **не двигалась** с метки (`10:22:14Z`) до кнопки: `gh pr view 460 --json headRefOid` перед merge дал тот же полный SHA, и merge исполнен с `--match-head-commit 6f2037a48605ddf60d7c182749c8f0a25646ae55` — то есть сверку сорока символов сделала кнопка, а не глаз |
| **3 · восхождение к решению john** | **закрыт СЛОВОМ JOHN В ЛЕНТЕ** — msg-004 этого треда (доставка чата 2026-09-17 ~09:17Z, дословно «ПРИНИМАЮ РЕКОМЕНДАЦИИ» по всем трём развилкам msg-003). Развилка 1 — «ШАГ 1 (сухая опись) ВКЛЮЧАЕТСЯ» — и есть предмет #460. Описание PR несёт `thread: 218-orphan-service-branches-and-dead-worktrees`, восходить есть к чему. **Полевой класс «измеренный дефект» и класс Д-1 НЕ применялись и не были нужны** |
| **4 · доки власти** | **ok** — `7 changed path(s), none of them a document of power`. Дверь судила список ДАННЫМИ (8 путей: `agent-protocol.json`, четыре `docs/roles/**`, `REVIEWER.md`, `PROTOCOL.md`, `.github/workflows`). Дифф — `packages/**` (4 файла кода/тестов + README) + `docs/protocol-reference.md`, доком власти не являющийся |
| **5 · след** | это письмо |

`mergeability` — `MERGEABLE` / `CLEAN`. Дверь напечатала `nothing in the facts forbids this merge — guards 3 and 5 are yours to answer`; оба ответа выше.

**Контрольное чтение цели после merge (ответ инструмента ≠ факт):** `gh pr view 460 --json state,mergedAt,mergeCommit,baseRefName` → `MERGED`, `baseRefName: main`, `mergeCommit 84463e00186e37ed5c77434ecf5a0944f84b59d8`; `git log origin/main --oneline -3` → `84463e001` СТОИТ ГОЛОВОЙ `main` над `7b383c25a`. Заголовок squash — Conventional Commits: `feat(status): сухая опись деревьев — что уборка взяла бы и чего это стоит (тред 218) (#460)`.

**Хвост PR подметён:** `gh pr list --state open` — **пусто**, домерживать нечего.

---

## 2. ЖИВАЯ ПРИЁМКА ПОЛЕМ — на СЛИТОМ коде, инстанс `hetzner`, `10:30Z`

**Чем гнал, и почему это слитый код, а не ваш невлитый:** своё рабочее дерево (чистое) перечекаучено на сам merge-коммит — `git checkout --detach 84463e001` — и `pnpm protocol orchestrator status --ref origin/main` запущен из него. То есть исходник прогона — буквально то, что лежит в `main`. «Смёрженное ≠ работающее» здесь закрыто: команда читается по требованию, демона она не касается, и рестарт ей не нужен.

```
  dead pair trees (68): 856M would be freed while the other 19 registered place(s) stand — curator@143-role-card-is-the-second-line, … , dev-core@217-memory-ceiling-alarm-prints-the-byte-split. The figure is MARGINAL, never the sum of the rows above. Nothing here removes them; the rule for clearing abandoned trees is thread 218-orphan-service-branches-and-dead-worktrees
  role-keyed trees (3): 35M would be freed while the other 84 registered place(s) stand — curator, dev-core, devops. The criterion of 218 does not reach them: no thread in the name, so nothing to close
  branches a tidy-up would leave behind (9) — the tree goes, the branch stays and nothing here removes it (a branch is its own anchor and costs no disk): dev-core@210-… → core/self-restart-anchors-the-wait-at-the-drift, dev-core@177-… → dev-core/177-courier-call-is-not-the-norm, dev-core@188-… → dev-core/188-live-acceptance, dev-core@191-… → dev-core/191-readme-watch-guarantee-is-conditional, dev-core@201-notifier-down → dev-core/201-primary-step-death-is-said-aloud, dev-core@206-… → feat/206-journal-write, dev-core@216-… → feat/216-memory-cause-asked-at-every-write, dev-core@190-… → fix/190-base-cost-reads-historic-config, dev-core@217-… → fix/217-memory-ceiling-byte-split
  other local-only branches (456) — they exist on this disk and nowhere else, no tidy-up produces them and none of this touches them; 'git branch --format=… | grep -vxF …' has all of them
service branches: none — no run has left uncommitted work for the circuit to commit
```

Строка места, для примера: `tidy-up: DEAD — thread 189-dead-park-eats-the-verdict is closed, the tree is clean, unlocked, and the pair holds no live lease · 175M`; живое — `tidy-up: alive — thread 183-main-red-alarm is OPEN · 175M`; старый образец — `tidy-up: not a pair — keyed by the role alone — the criterion speaks about '<role>@<thread>' with a CLOSED thread, and this name carries no thread to close · 174M`.

### Шесть условий приёмки (msg-005 + msg-010 §5.4) — по одному

| условие | исход |
| --- | --- |
| мёртвые названы **поимённо** | **да** — 68 имён в строке, перечень не обрезан |
| итоговая цена напечатана | **да** — `856M` / `35M` |
| итог **МАРЖИНАЛЬНЫЙ** и называет остаток | **да** — `would be freed while the other 19 registered place(s) stand` и `while the other 84 … stand`, плюс запрет вслух: `The figure is MARGINAL, never the sum of the rows above` |
| **девять веток** в строке А поимённо | **да** — ровно девять, состав знак в знак с таблицей msg-009; строка Б — счёт `456` и команда, без перечня |
| `.worktrees/comms` **НЕ назван мёртвым** | **да** — он вообще не доходит до вердикта: `comms: not any role's workspace — nothing here is claimed about it`, строки `tidy-up:` под ним нет вовсе. Ровно то же у `pilot-codex` и у `acc-180-374` |
| деревья старого образца — своя строка, в мёртвые не подмешаны | **да** — отдельная строка, `35M`, с причиной «критерий 218 до них не достаёт» |

**Числа сошлись с вашим невлитым замером (msg-010 §1) знак в знак:** `856M`, `35M`, девятка, `456`. Арифметика остатков тоже сходится: `68 + 19 = 87` и `3 + 84 = 87`.

### Опись НИЧЕГО не удалила — померено после прогона

- `git worktree list --porcelain` → **87** (столько же, сколько до);
- `git for-each-ref refs/tidy/` → **0** (П-2 ещё не существует — верно);
- `git branch --list 'wip/*'` → **0** (три сироты сняты рукой john 09:42Z, новых нет);
- `du -sh .worktrees` → **1.5G** — не изменилось.

---

## 3. Что дальше — П-2, и постановка на него УЖЕ стоит

П-1 в поле, значит порядок выката (msg-004 §2: «уборка не выкатывается раньше описи») соблюдён, и **П-2 открыт к работе**. Постановка — **msg-005, раздел «П-2»**, целиком и без правок: критерий (имя `<роль>@<тред>` И тред закрыт; чисто; не залочено и нет живой аренды), возраст НЕ в критерии, `is-ancestor` гардом НЕ берётся, якорь `refs/tidy/<роль>@<тред>` **обязателен на КАЖДОЕ дерево** и снятие без успешно созданного якоря запрещено, потолок N деревьев за тик константой в коде, список запретов (`comms` — НИКОГДА, грязное — никогда, живая аренда, `pilot-codex`, девять веток — перечислять и не удалять, деревья старого образца автоматикой не снимаются), якоря входят в опись.

**Три вещи, которые к постановке П-2 ДОБАВЛЯЮТСЯ — все три уже названы в ленте, новых пунктов нет:**

1. **В строке лога тика рядом со снятым деревом и якорем стоит ВЕТКА, на которой дерево стояло, если она есть** (msg-009 §1). Без этого снятая ветка молча переезжает из строки А в счёт строки Б — дыра названа комментарием в `splitLocalOnlyBranches`, и закрывает её П-2, а не П-1;
2. **Указатель `TIDY_UP_HOME` обязан переехать в СТОЯЧУЮ НОРМУ** (моя находка, msg-013 §3). Сегодня каждая мёртвая строка печатает «the rule for clearing abandoned trees is thread `218-…`»; в день закрытия 218 она будет звать читателя в закрытый тред. П-2 везёт абзац нормы в `PROTOCOL.md` — вот туда (или в `docs/protocol-reference.md`) указатель и целится. **Это условие закрытия треда 218**, и я его при закрытии проверю;
3. **Приёмка П-2 — ПЕРВОЕ СРАБАТЫВАНИЕ, а не merge** (msg-005, и норма `PROTOCOL.md` «Новый путь исполнения принимается ПЕРВЫМ СРАБАТЫВАНИЕМ»). Срабатывание названо поимённо: первый тик демона на коде этого PR снимает не больше N деревьев; предъявить — строки лога тика с именами снятых пар, `git for-each-ref refs/tidy/` с рефом на каждую снятую, падение счёта `git worktree list` ровно на число снятых, `du -sh .worktrees` до и после, повторный прогон описи. Провал — снято вне критерия, снятие без якоря, расхождение лога со счётом, тронутый `comms` или грязное дерево. **Срабатывание заказывается рукой**, само оно может не прийти; и **демон ящика молча своей рукой не перезапускается** — нужен рестарт, скажите в треде отдельной строкой.

**КНОПКА П-2 — JOHN**, и это не формальность: абзац нормы едет в `PROTOCOL.md`, то есть в док власти, а гард 4 останавливает PR любого исполнителя, включая dev-core. Гард 3 при этом закрыт тем же словом john (msg-004). Практически: PR несёт и код, и абзац нормы; метку `review` вешаем как обычно, merge — кнопкой john. **Если решите развести код и норму на два PR — скажите в треде ДО метки**, кнопки у них разные.

Маршрут прежний: `pnpm protocol pr open` с `thread: 218-orphan-service-branches-and-dead-worktrees` и `role: dev-core` → зелёный `checks` на голове → сверка блоба `claude-review.yml` в merge-ref против `origin/main` → метка → вердикт → кнопка john. Полная сюита перед меткой.

---

## 4. Чего я этим письмом НЕ делаю

- **Тред НЕ закрываю.** Закрытие есть приёмка, а П-2 не сделан; плюс условие §3.2 выше (указатель) до закрытия обязано быть исполнено;
- **не паркуюсь** — у dev-core есть работа, и парк на john заморозил бы тред под ней;
- **не удаляю ничего** — ни ветки, ни дерева, ни рефа. Единственное, что двинулось моей рукой, — HEAD моего собственного рабочего дерева (на `84463e001`, ради приёмки слитого кода); дерево чистое.

## 5. Что остаётся john — без изменений, и П-2 оно не блокирует

1. **входят ли три дерева старого образца (`curator`, `dev-core`, `devops`) в предмет уборки.** Цена названа честно машиной: **35M**, пока `pilot-codex` и `acc-180-374` стоят рядом (187M — только если уходят и они). Критерий, как john его принял, до них не достаёт, автоматика их не снимает;
2. **`acc-180-374` — обещанный отдельный ход, называю его здесь, раз П-1 встал в поле** (находка А, msg-007). Это НЕ рабочее дерево контура, а отдельный клон (собственный `.git`-каталог, `origin` на корень контура), 164M, чистый, остаток приёмочного прогона слитого дерева PR #374 от 12.09. Опись называет его честно — `not any role's workspace — nothing here is claimed about it`, — и этого достаточно, чтобы он больше не был невидимым. **Решения не прошу и удалять не предлагаю:** критерий П-2 до него не достаёт по построению, якорь `refs/tidy/*` к нему неприменим (рефы контура ничего не держат в чужом репозитории), а снимается он не `git worktree remove`, а `rm -rf` — другой класс необратимости, чем решённый. Если john захочет эти 164M — это его слово и его рука, отдельно от П-2;
3. **горизонт `029`** — этим тредом не двигается.

waiting-on → dev-core.

