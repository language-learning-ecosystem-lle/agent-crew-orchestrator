---
from: curator
worker: claude-ai
date: 2026-09-17T12:26:40Z
expects: answer
waiting-on: curator
delivers: john
park-lifted: john
priority: high
---

**JOHN НАЖАЛ И ПЕРЕЗАПУСТИЛ. #461 слит `12:22:28Z` (голова `d02fbedf5`, та же, что несла вердикт). ПЕРВЫЙ ТИК НА НОВОМ КОДЕ СОСТОЯЛСЯ, УБОРКА СРАБОТАЛА. Парк снимаю этим письмом.**

## 1. Эпоха сменилась — предъявлено, а не предположено

```
daemon — code: 84463e00 … up since 2026-09-17T10:34:41Z
daemon — code: c1240cf6 … up since 2026-09-17T12:25:18Z
```

`daemon-code.json`: `sha c1240cf636bbbb20a85916cbd07448ef6c99e2cd`, pid `1992571`, `startedAt 12:25:18Z`. `git log origin/main -1` → `c1240cf63` — merge-коммит #461. **Живой демон исполняет то, что смёржено.**

Замечание о методе, и оно моё: сразу после подъёма `daemon-code.json` ещё показывал СТАРУЮ эпоху (`84463e00`, pid `1295552`). Я назвал это подозрением на дефект — **подозрение снято фактом**: запись обновляется первым тиком, а не стартом процесса. Читать её раньше первого тика — ошибка чтения, а не дефект записи.

## 2. Первое срабатывание — строки лога дословно

```
tidy-up: REMOVED curator@176-daemon-self-restart · anchored at refs/tidy/curator@176-…
tidy-up: taking 3 tree(s) this tick (ceiling 3 per tick, a constant in the code):
         curator@177-workspace-per-pair, curator@179-any-available-account, curator@180-notifier-down
tidy-up: REMOVED curator@177-workspace-per-pair · anchored at refs/tidy/curator@177-…
tidy-up: REMOVED curator@179-any-available-account · anchored at refs/tidy/curator@179-…
tidy-up: REMOVED curator@180-notifier-down · anchored at refs/tidy/curator@180-notifier-down
```

Видно поимённо: **каждое снятие несёт свой якорь**, потолок `3` назван в строке и соблюдён, снятое — пары `<роль>@<тред>` по закрытым тредам. Порядок жестов исполнен так, как объявлен: якорь раньше снятия.

**Приёмку П-2 я НЕ объявляю закрытой этим письмом** — предъявление по msg-016 §5 шире того, что видно с чата: нужны `git for-each-ref refs/tidy/`, падение счёта `git worktree list` РОВНО на число снятых, `du -sh` до/после и повторный прогон описи П-1. Это ход роли, и роль обещала снять его своей рукой. Строка john дана: **смёржил, рестартовал.**

## 3. Отказ по дороге, который стоил лежащего контура — предмет заведён отдельно

`orchestrator restart --pull` прошёл стоп и `git pull --ff-only`, а на `pnpm install` упал с `code ?` и демона не поднял (флаг стопа снял сам — верно). Причина измерена john'ом руками: в окружении вызова `pnpm` не в `PATH`; с добавленным `…/nvm/versions/node/v24.18.0/bin` та же установка проходит за `556ms` («Already up to date»). То есть **команда не запускалась вовсе** — отсюда и пустой код возврата.

Контур поднят вручную тем же составом флагов, что был у остановленного демона (`["--ref","origin/main","--instance","hetzner"]`, прочитано из `daemon.pid.args`, а не набрано по памяти).

**Дом дефекта — тред `219-restart-calls-pnpm-by-name`**, заведён этим же ходом. Здесь он только назван, чтобы объяснить разрыв во времени между merge и первым тиком.

## 4. Что остаётся

**За curator:** снять предъявление приёмки П-2 своей рукой (§2), затем закрытие треда — условие про указатель `TIDY_UP_HOME` уже исполнено и перепроверено на голове PR.

**За john:** ничего по этому треду. Прежние остатки не двигаются: рука для трёх веток `wip/*` — **исполнена** (`09:42Z`), три дерева старого образца, `acc-180-374`, горизонт `029`.

waiting-on → curator.
