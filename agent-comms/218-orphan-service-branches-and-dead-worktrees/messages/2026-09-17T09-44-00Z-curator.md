---
from: curator
worker: claude-ai
date: 2026-09-17T09:44:00Z
expects: none
waiting-on: curator
delivers: john
---

**ИСПОЛНЕНО РУКОЙ JOHN (~09:42Z): три ветки `wip/*` удалены. Развилка 3 закрыта; остаток по ней снят.**

## Что удалено — с хешами, чтобы возврат был возможен и через месяц

| ветка | коммит | отпечаток до удаления |
| --- | --- | --- |
| `wip/189-rebase` | `cb2d30ddc` | апстрим помечен `[gone]` |
| `wip/417-rebase` | `dc3546f38` | — |
| `wip/418-rebase` | `7ca4dea30` | — |

Вывод `git branch -D` назвал те же три хеша, что и опись до удаления, — сходится знак в знак.

**Возврат любой из них, пока коммиты не собраны сборщиком мусора:** `git branch <имя> <хеш>` из корня чекаута контура.

## Отпечаток, снятый john ПЕРЕД удалением (его рукой, `~09:41Z`)

- `git branch -r --list "origin/wip/*"` — **пусто**: на `origin` ни одной из трёх не было, удаление локальное;
- `git worktree list --porcelain` — **ни одна не занята рабочим деревом**, снятие никого не осиротило;
- содержимое проверено ДО решения (msg-003 §0): тождество деревьев по `418`, все 37 строк `417` найдены в слитом файле `main`.

## Находка отпечатка — новая, разбором не названная

У `wip/189-rebase` стоял `[gone]`: у неё БЫЛ апстрим, и он удалён. У двух других пометки нет. На решение это не влияло — содержимое всех трёх проверено против `main`, — но это ещё один факт о происхождении сирот: `189` осиротела не сама по себе, а пережила удаление своей удалённой ветки. Класс тот же, что назвал `dev-core` в В-2 (рецепт в памяти роли, уже починен); отдельного хода не прошу.

## Что по треду осталось

**За curator:** постановка реализации двумя пакетами — опись (ШАГ 1), затем уборка (ШАГ 2). Решение john по обоим доставлено письмом `09:18:30Z`; уборка не выкатывается раньше описи.

**За john по этому треду — ничего.**
