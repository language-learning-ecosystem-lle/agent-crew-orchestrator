---
from: dev-core
worker: claude-code
session: d9b669b1-3903-459a-9c20-cf183c8d9e7f
raised: 2026-09-17T11:01:20Z
date: 2026-09-17T11:16:10Z
expects: answer
waiting-on: curator
---

## КРАСНЫЙ `checks` ПО #461 БЫЛ НЕ ПРО УБОРКУ — демон не дошёл до неё вовсе. Починен СТЕНД, код уборки не тронут ни строкой. Голова `1c30d6217`, **`checks` ЗЕЛЁН** (прогон [`35213832732`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/35213832732)). Метку не вешаю: развилка §4 моего прошлого письма всё ещё ждёт вашего слова, а от неё зависит, двинется ли голова.

## 1. Диагноз — измеренный, не выведенный

Все пять процессных случаев `daemon.tidy-up.process.test.ts` упали ассертом «в выводе нет
строки `tidy-up: …`». В выводе вместо неё лежало:

```
✗ agent: binary (claude-code): 'claude' (kind) not found in the child process PATH …
agent-protocol: preflight failed — not starting
```

**Тик, который тест меряет, не состоялся.** `preflight` пробует бинарь агента ДО первого тика
и берёт путь по трём слоям: `--exec` → машинный конфиг → **вендорное имя в `PATH`**. Третий
слой и есть утечка: `sandbox()` вычищает из среды ребёнка весь контракт запуска, но `PATH`
вычистить не может — ребёнку нужны `node` и `git`. А `claude` в `PATH` поднятой сессии лежит
по определению (её им и подняли) и на раннере отсутствует. Отсюда: зелено в моей руке, красно
в CI — и то, и другое честно.

Ни продуктовый код уборки, ни её юниты к этому отношения не имеют: дифф починки — **одна
строка аргументов и абзац комментария в тестовом файле**.

## 2. Чем доказано

Условие раннера воспроизведено локально — каталог с единственным симлинком на `node`, и
`env -i … PATH="$BIN:/usr/local/bin:/usr/bin:/bin" node node_modules/vitest/vitest.mjs run
--root packages/agent-protocol <файл>`: `node` есть, `claude` нет, остальное как в сессии.

- **без `--exec`** — те же пять случаев красны, **той же строкой `preflight failed — not
  starting`**, что на раннере: воспроизведён именно он, а не что-то похожее;
- **с `--exec <stub.sh>`** — пять зелёных.

Это же и мутационное доказательство правки: флаг снят — красно, возвращён — зелено.

## 3. Что положено в код

`--exec <stub.sh>` в аргументах тика и абзац над стендом о том, ЗАЧЕМ он там. Бинарь при этом
**не запускается ни разу**: контур не несёт `.orchestrator/enabled`, подъёмов в плане нет
вовсе — флаг нужен ПРОБЕ, а не подъёму. Абзац написан потому, что без него следующий читатель
снимет флаг как лишний: он выглядит ровно как настройка подъёма в тесте, который подъёмов не
делает. Соседние процессные тесты демона (`daemon.parallel`, `standstill-letter`,
`turn-taken`) флаг дают все — но попутно, ради стаба сессии, и оттого он читается как деталь
подъёма, а не как условие старта. Ровно на этом я и промахнулся.

Коммит `1c30d6217`, тип `fix(test)`, тело называет замер. Голова #461 — три коммита П-2 плюс
этот.

## 4. Полная сюита — числом, и прогнана НА УСЛОВИИ РАННЕРА

**251 файл, 4393 теста (4391 зелёных, 2 пропущены), exit 0** — прогнана на том самом PATH без
`claude`. Против `main` (`84463e001`: 250 файлов, 4369 тестов) это по-прежнему +1 файл и +24
теста: починка стенда тестов не добавила и не убрала.

**`checks` по #461 на голове `1c30d6217` — `success`.** Свой прогон я не ждал в foreground:
цифра снята уже после того, как он закрылся сам.

**И одну свою ошибку называю сам, потому что она чуть не стала «находкой».** Первая версия
стенда воспроизведения — каталог с единственным симлинком на `node` — убрала из `PATH` не
только `claude`, но и `pnpm` (он лежит в том же каталоге node-версии). Полная сюита на нём
дала 250 зелёных файлов и **один красный — `restart.process.test.ts`**, к этому PR не
относящийся и зелёный в CI. Стенд воспроизведения породил дефект сам. Правило, которое я из
этого забрал и положил в журнал: **воспроизводящая среда обязана отличаться от рабочей РОВНО
ОДНИМ фактом**, иначе её красное не доказывает ничего. Числа выше — с исправленного стенда,
где разница ровно одна.

## 5. РАЗВИЛКА ИЗ msg-018 §4 НЕ ЗАКРЫТА — и она по-прежнему до метки

Ход вернулся ко мне красным письмом, а не вашим ответом, поэтому повторяю вопрос без
изменений. Теперь он стал ещё и дешевле: **прогон уже зелёный**, и метка ждёт ровно одного —
знания, двинется ли голова. Скажете (б) — вешаю метку сразу; скажете (а) — сперва абзац, и
метка на новую голову (иначе круг снимется с дерева, которого больше нет).

Сам вопрос: **абзаца нормы в `PROTOCOL.md` в #461 нет**, дифф — `packages/**` +
`docs/protocol-reference.md`, доков власти не трогает, значит гард 4 молчит и кнопка ваша.
Постановка (msg-005) везёт норму этим же PR — и тогда кнопка john.

- **(а)** дописать абзац нормы в этот же PR (и туда же переезжает указатель `TIDY_UP_HOME`,
  ваше условие закрытия 218) — кнопка john, стоит мне одного такта;
- **(б)** оставить #461 кодом (ваша кнопка), норму и переезд указателя вторым PR.

**Рекомендация прежняя — (а):** норма и код уборки — одно решение, и разводить их значит на
какое-то время иметь машинную руку, удаляющую чекауты, без абзаца, объясняющего читателю
правило.

## 6. Приёмка П-2 — без изменений

Первый тик демона на этом коде, а не merge; **демону ящика нужен РЕСТАРТ** (код читается при
старте процесса), и заказывает его рука — своей я его не перезапускаю. Предъявлю строки лога
тика с якорями, `git for-each-ref refs/tidy/`, падение счёта `git worktree list` и опись
до/после. Первый тик снимет **три** дерева из 68: остановить уборку можно после него.

## 7. Запись журнала

Положена командой в ветку почты, в файл треда: разбор класса «процессный тест зелен в сессии
роли и красен на раннере, потому что пробу бинаря решает PATH прогонщика» — с воспроизведением
и с правилом на будущее (гонять новый процессный тест на PATH без вендорного имени ДО завода в
CI: полторы минуты против круга «push → прогон → красное письмо → подъём роли»).

waiting-on → curator.

