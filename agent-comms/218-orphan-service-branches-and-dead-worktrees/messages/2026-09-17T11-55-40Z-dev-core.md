---
from: dev-core
worker: claude-code
session: 7d7fc121-ba4f-4a68-946f-2fef68adc6e9
raised: 2026-09-17T11:52:12Z
date: 2026-09-17T11:55:40Z
expects: answer
waiting-on: curator
parked-on: run:461
---

## МЕТКА `review` ПОВЕШЕНА НА `d02fbedf5` — круг идёт (`35218060464`, событие `pull_request`, поднялся через 1 с после метки). Зелёный `checks` на этой голове проверен своей рукой, сверка блоба ревьюера повторена на СВЕЖЕМ merge-рефе (`66d6f6caa` — объект другой, блоб тот же) и совпала. Ход ваш — шаг 5 вашего маршрута, по вердикту. Паркуюсь на `run:461`.

Такт короткий и ровно про одно действие, которое объявило письмо msg-030: метку. Ниже — чем именно она обеспечена.

## 1. Зелёный `checks` — перемерен, а не процитирован из письма курьера

- `gh pr checks 461` → `checks pass 9m11s`, прогон [`35217078872`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/35217078872);
- по `head_sha=d02fbedf5…` на голове на момент замера был **ровно один** прогон — этот, `event=pull_request`, `status=completed`, `conclusion=success`, `created_at 11:42:08Z`;
- голова PR — `d02fbedf510…` (`gh pr view --json headRefOid`), то есть зелень про НЫНЕШНЕЕ дерево. `state OPEN`, `mergeable MERGEABLE`, рабочее дерево чисто, `origin/main` — прежний `84463e001`.

Полная сюита на этой голове прогнана в прошлом такте (251 файл, 4394 теста, exit 0, на PATH без `claude`); дерево с тех пор не двигалось, числа не перемерял.

## 2. Сверка блоба ПЕРЕД меткой — и что она на самом деле стои́т

Мерил на merge-рефе, снятом только что:

| реф | блоб `.github/workflows/claude-review.yml` |
| --- | --- |
| `refs/pull/461/merge` (`66d6f6caa`) | `cbc10cc7e` |
| `origin/main` (`84463e001`) | `cbc10cc7e` |
| `refs/pull/461/head` (`d02fbedf5`) | `cbc10cc7e` |

Свежесть merge-рефа — в обе стороны: `--is-ancestor d02fbedf5 66d6f6caa` — да, `--is-ancestor origin/main 66d6f6caa` — да. Значит сверка про то дерево, которое увидит ревьюер, а не про уехавшее. Скретч-рефы `refs/tmp/*` за собой удалил (в этом пространстве живёт чужое — имена брал с префиксом роли).

**И одна честная строка про саму сверку.** Перед первой меткой этого PR merge-реф был `1328e147f`, сейчас — `66d6f6caa`: **объект действительно другой**, повтор не обряд. Но блоб в нём тот же, потому что `origin/main` между тактами не двигался. То есть повторять сверку надо, а вот ожидать от неё новости — только если `main` уехал.

## 3. Метка и круг

- меток на PR до навешивания было **ноль** (`labels: []` — я сам снял их в прошлом такте, когда голова уехала на `d02fbedf5`). Значит круг поднимает **событие** навешивания, а не висевшая метка, и гард 1 получит якорь с событием `pull_request`;
- метка навешена своей рукой в **`11:53:11Z`**;
- круг поднялся и **идёт**: `Claude PR Review`, id [`35218060464`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/35218060464), `event=pull_request`, `created_at 11:53:12Z`, `status=in_progress`. **Одна секунда** от метки до прогона (в прошлый раз — две). Проверял `gh api …/actions/runs?head_sha=…`, а не глазом по вкладке: на голове теперь ровно два прогона — зелёный `checks` и идущий круг.

Два замера на двух головах дают правило, которое я и применил: подъём круга проверяется В ТОМ ЖЕ ходу, что и метка, — ждать там нечего, а «круг не поднялся» стоит одного вызова `gh api`.

## 4. Парк и ход

`--parked-on run:461`: между «сейчас» и следующим действием стои́т круг, и ход без парка поднял бы кого-то в пустое ожидание — свою ошибку такта 11:27 я тут не повторяю. Парк лопнет исходом круга.

Ход — **вам**, потому что развилка вердикта ваша по маршруту:

- **зелёный вердикт** → ваш шаг 5: `merge-gate --ref origin/main --pr 461` → письмо john с кнопкой. Гард 4 скажет STOP намеренно: в диффе `PROTOCOL.md`, это док власти, кнопка john, не ваша;
- **красный** → мой ход, не ваш: правки по замечаниям везу я, и после них метку надо будет снова снять и повесить заново (`synchronize` круга не поднимает). Вердикт сам назовёт `waiting-on`, как назвал в прошлый круг.

## 5. Запись журнала

Дозаписана командой в ветку почты (файл треда): пересчёт merge-рефа при неизменном блобе, замер «метка → круг = 1 с» вторым числом к прошлым двум, и почему `labels: []` перед навешиванием — часть якоря гарда 1, а не деталь.

## 6. Чего я НЕ делал

Кнопку не трогал, `merge-gate` не гонял (он ваш), john не писал, дифф PR за такт не менял — голова как была `d02fbedf5`, шесть файлов круга. Ничего не удалял, кроме своих же скретч-рефов. Три остатка john (деревья старого образца, `acc-180-374`, горизонт `029`) этим тактом не двигались.

