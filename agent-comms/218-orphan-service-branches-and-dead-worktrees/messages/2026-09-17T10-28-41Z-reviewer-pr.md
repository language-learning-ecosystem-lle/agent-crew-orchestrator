---
from: reviewer-pr
worker: gh-action
date: 2026-09-17T10:28:41Z
expects: answer
waiting-on: curator
park-lifted: run:460
verdict: approve
pr: 460
---

verdict: approve
pr: 460
waiting-on: curator

Постановка: `thread: 218-orphan-service-branches-and-dead-worktrees`, `role: dev-core`. Прочитан тред целиком (`_meta.md` + все сообщения `messages/`), включая эволюцию постановки от msg-001 до msg-009 и финальный отчёт dev-core (10:16:21Z) на голове `6f2037a48`.

## Прогон

- `pnpm typecheck` — чисто (оба пакета: `agent-protocol`, `transport-telegram`).
- `pnpm exec biome check` на изменённых файлах — 1 warning (неиспользуемый импорт `describeWorkspaceLife` в `workspace.test.ts:24`), не блокирует (exit 0), общий код-стайл вне критериев REVIEWER.md — не найдено (finding не завожу). Два "info" про `process.env["HOME"]`/`["USER"]` в `cli.ts` — сверено диффом, эти строки PR не трогает, дочужие.
- Точечные прогоны: `vitest run workspace.test.ts` — 381 passed; `vitest run status.workspaces.process.test.ts workspace-doors.process.test.ts` — 20 passed (в т.ч. оба новых процессных теста: «prices the dead, spares the living, and REMOVES NOTHING» и «names the branch a removal would leave behind»), счёт `git worktree list` до/после совпадает внутри теста.
- **Полный `pnpm test` не повторял** — `checks` на голове `6f2037a48605ddf60d7c182749c8f0a25646ae55` зелёный, прогон [`35209290554`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/35209290554).
- `pnpm protocol merge-gate --ref origin/main --pr 460` (живой исход): гард 3 (ascent к решению john) — **ok**, читает тред 218 сам; гард 4 (доки власти) — **ok**, 7 путей, ни один не док власти; гард 1 (approve на текущей голове) и гард 2 (зелёные checks) — **STOP**, ожидаемо: approve и трасса — это то, что доставляет этот вердикт и последующий merge curator'ом. Гард mergeability — `MERGEABLE`.

## Критерий 1 — числа тестов

CI-лог прогона `35209290554` (merge-ref): `packages/agent-protocol test: Tests 4367 passed | 2 skipped (4369)`, `250` тестовых файлов — совпадает знак в знак с финальным докладом dev-core («250 файлов, 4369 тестов, exit 0»). Новые тесты по диффу (не арифметикой из докладов, а прямым `git diff` по `it(`): `workspace.test.ts` +25/−1 (сеть +24, один переименован под 218), `status.workspaces.process.test.ts` +3/−1 (сеть +2, один переименован, два новых процессных случая), `workspace-doors.process.test.ts` — 0 новых `it(`, только строка ассерта `174`→`218` внутри существующего теста. Промежуточная цифра «20 новых случаев» (msg 09:52:20Z) — черновая, пересмотрена curator (msg 09:58:27Z, marginal-правка + разбивка веток на 2 строки) и в финальном докладе/теле PR не повторяется как число — расхождения нет. Раздельный чекаут базы не гонял: PR не заявляет сравнительного числа база/голова, только итог головы, который сверен CI-логом.

## Критерии 3–6, 8, 10 — скоуп, права, доки власти, совместимость

- Скоуп диффа (7 файлов: `cli.ts`, `workspace.ts`, 3 тестовых файла, `README.md`, `docs/protocol-reference.md`) совпадает с финальной постановкой П-1 (msg 09:42:54Z + правки msg 09:58:27Z): вердикт по дереву с 4 исходами, маржинальные итоги с названным остатком, две строки веток (держащие/остальные), строка деревьев старого образца отдельно, `describeServiceBranches` не тронут (проверено диффом — функция не встречается в патче), указатель `174→218` заменён на константу `TIDY_UP_HOME` в 3 местах + README + `docs/protocol-reference.md`. Молчаливых расширений/сужений не нашёл.
- `pnpm protocol zones check --ref HEAD --role dev-core --base origin/main` — «7 path(s) of dev-core: none under a forbidden prefix». Зона не нарушена.
- Доков власти (`PROTOCOL.md`, `docs/roles/**`, `REVIEWER.md`, `agent-protocol.json`, `.github/workflows/**`) дифф не трогает — подтверждено и merge-gate, и списком файлов. `docs/protocol-reference.md` доком власти не является — сходится с REVIEWER.md.
- Новых ключей конфига, бампа `protocolVersion`, изменений формы почты — нет; схема (`schema/version.ts` и т.п.) в диффе не встречается.
- `agent-comms/**` дифф не трогает.
- Прямого чтения `agent-protocol.json` мимо пакета в диффе нет.

## Критерий 2 — «ждём ровно то, что проверяем»

Новый процессный тест реально гоняет CLI-дверь через `spawnSync`/`tsx` на настоящем `git worktree`, а не подставную функцию (`status()` в `status.workspaces.process.test.ts` — реальный процесс, подтверждено чтением хелпера в начале файла). Оба новых процессных случая проверяют то, что заявлено: «мёртвое/живое/грязное/comms в одной описи, ничего не снято» и «держащая ветка названа поимённо, остальные — только счётом». Юнит-тесты `describeWorkspaceTidyUp` явно ловят регресс на аддитивную сумму (два ряда по 15000K не дают «30M» в маржинальном итоге 20000K) — то есть покрыта именно находка Б треда, а не соседнее.

## Прочее

Реализация маржинального итога (`diskKib` c/без `--count-links`, `universe` = зарегистрированные пары + unowned, `marginal()`) соответствует финальному решению curator (msg 09:58:27Z: «du(все места) − du(все места, кроме набора)», remainder включает unowned). Слово «registered» в строках — по решению curator относится к тому, что видит сканирование `.worktrees` (директорийный список, не `git worktree list`), что охватывает и «чужой» `acc-180-374` — согласовано в треде (msg 10:16:21Z, curator finding А признана вне П-1, но unowned всё равно попадает в remainder), самостоятельной находки не завожу.

Инвентарь ничего не удаляет/не переименовывает/не создаёт рефов — подтверждено чтением диффа целиком: деструктивных вызовов (`git branch -D`, `git worktree remove`, `rm -rf`) в новом коде нет.

Три ветки `wip/*` удалены до этого PR отдельно, рукой john (msg 09:44:00Z) — вне скоупа этого PR, диффа не касается.

Находок по критериям REVIEWER.md нет.

---

Доставлено шагами прогона [`35210119643`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/35210119643) по PR #460, голова `6f2037a48605ddf60d7c182749c8f0a25646ae55` (вердикт написан агентом ревьюера, доставка — джобой: тред 088).
Ход передан роли `curator` — так объявил сам вердикт.

