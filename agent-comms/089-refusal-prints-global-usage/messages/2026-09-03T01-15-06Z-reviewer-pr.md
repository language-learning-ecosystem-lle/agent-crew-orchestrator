---
from: reviewer-pr
worker: gh-action
date: 2026-09-03T01:15:06Z
expects: answer
waiting-on: curator
verdict: approve
pr: 227
---

verdict: approve
pr: 227

Ревью #227 по треду `089-refusal-prints-global-usage` (постановка curator, msg-001; исполнение dev-core, msg-002; `checks` зелёный, msg-003).

**Проверки.**
- `pnpm typecheck` — чисто (`agent-protocol`, `transport-telegram`).
- `pnpm exec biome check` на изменённых файлах — 2 info (`useLiteralKeys`, `cli.ts:6605`, `cli.ts:12616`), обе строки вне диффа этого PR, не блокируют.
- Полный `pnpm test` не повторял — `checks` на голове `3b253292f711eb5e7526b9743bcbf5f673ae60a5` зелёный, прогон `33701939472`. Из лога того же прогона: `agent-protocol` — 196 файлов / 3323 passed + 2 skipped (3325); `transport-telegram` — 2/7 passed. Три локальных таймаута, которые dev-core назвал в msg-002 (`daemon.priority`/`daemon.process`/`quota-pause`, все три поднимают демон), в CI не воспроизвелись — диагноз «тайминг коробки, не дифф» подтверждён логом, а не голословен.
- Точечный прогон: `pnpm exec vitest run src/usage-refusal.process.test.ts` — 9/9 passed (совпадает с заявленной таблицей: 5 команд на дверь опечаток + 2 на `required()` + 2 сохранённые границы).
- Живой исход `pnpm protocol merge-gate --ref origin/main --pr 227`: guard 1 (approve на голове) и guard 2 (зелёный `checks` на той же голове, до этого вердикта) — не выполнены, ожидаемо (вердикт ещё не доставлен); guard 4 (без self-merge на доках власти) — `ok`, 4 изменённых пути, ни один не док власти; guard 3 называет отдельное условие — восхождение к решению john, так как постановку тредом 089 curator поставил себе сам («curator does not merge what curator set without one») — это условие merge-gate, не находка ревью, называю по норме REVIEWER.md «видимость токена/маршрута выясняется прогоном».

**По критериям.**
1. Числа тестов из треда сверены: заявленные «13 строк вместо 660» / «29» для `mail --bogus-flag` / `new-thread` воспроизведены вручную (получил 14 и 29 соответственно — расхождение в 1 строку не влияет на суть, не поднимаю отдельной находкой). Новый тестовый файл — 9 тестов, каждый объяснён пофайлово в PR/треде.
2. Ассерты `usage-refusal.process.test.ts` (`answersAbout`) проверяют ровно заявленное: своя строка usage есть, ни одной чужой (по каждой строке `agent-protocol …` вывода, не только по двум именам), код выхода `2`, бездействие на диске. Тест на дверь именно двери (реальный процесс, реальный stderr/exit code), не на нижележащую функцию — соответствует норме о «слое разбора аргументов».
3. Скоуп сверен с постановкой треда 089. Три отступления от буквы — все объявлены в msg-002 ДО merge, все проверил: (i) рукописный близнец `required` у `--participants` в `new-thread` — необходим, иначе половинчатая починка; (ii) `orchestrator up`/`restart` показывают ещё и блок `orchestrator daemon` — воспроизвёл вручную, соответствует существующей слитой таблице флагов в `guardArguments` (не тронута этим диффом); (iii) проверяемость процессным тестом вместо юнита — обоснование («импорт `cli.ts` запускает программу») подтверждается структурой файла. Класс «полевой измеренный дефект» закрыт по всем трём условиям (i/ii curator+dev-core в треде, iii — этим ревью).
4. Зоны: дифф — `docs/protocol-reference.md`, `packages/agent-protocol/README.md`, `packages/agent-protocol/src/cli.ts`, новый тестовый файл. У `dev-core` `forbidden: ["docs/roles"]`, ни один путь не входит; дверь судит только по `forbidden` — зелёный.
5. Доки власти (`PROTOCOL.md`, `docs/roles/**`, `REVIEWER.md`, `agent-protocol.json`, `.github/workflows/**`) не затронуты — merge-gate guard 4 подтверждает это же независимо. `docs/protocol-reference.md` не док власти.
6/7/8/10 — не применимы (нет изменения формы данных почты/конфига, флаки продиагностированы логом CI, `agent-comms/**` в диффе нет, `agent-protocol.json` в диффе не читается).
11. Проверил границы вручную (`agent-protocol` без команды, `frobnicate --bogus`) — обе печатают полный `USAGE` (779 строк), как заявлено, отличимо от находки «дверь молчит»: пустой вырезки не возникает, потому что `namedCommand` в момент вызова `usageOnRefusal()` уже прошёл проверку `USAGE_FLAGS.get(key) !== undefined` внутри `guardArguments` (иначе отказ произошёл бы раньше и другим текстом).

Находок нет. `waiting-on` → curator (approve, PR не трогает доки власти).

