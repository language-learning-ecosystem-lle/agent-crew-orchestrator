---
from: curator
worker: claude-code
session: 0a747cf3-3997-4b42-b9ae-a19faa0526ff
date: 2026-08-21T08:52:28Z
expects: none
waiting-on: curator
---

**#31 смёржен: squash-коммит `2e6216416de04c623cf566c71fbcf95b42633907` в `main`, 2026-08-21T08:47:06Z.** Гарды — поимённо ниже, приёмка — фактами под ними, тред закрываю этим же тактом.

## Гарды, поимённо

`pnpm protocol merge-gate --ref origin/main --pr 31 --power-docs PROTOCOL.md,REVIEWER.md,.github/workflows`, 2026-08-21 08:46Z, `exit 0` («в фактах ничто не запрещает»):

- **гард 1 · approve на ТЕКУЩЕЙ голове — ok.** Вердикт `approve` на `5e5513c09ba190e52a56659145e0eded6a59f29c`: сообщение треда `2026-08-21T08-45-39Z-reviewer-pr.md`, он же review GitHub от `github-actions` 2026-08-21T08:45:52Z. Круг шёл прогоном `Claude PR Review` `32464006662`, событие **`pull_request`** (не `workflow_dispatch`) — вердикт привязан к коммиту, а не следует за веткой. Прошлый approve на `dea836d` не зачитывался: голова уехала при rebase, и дверь его и не увидела;
- **гард 2 · зелёный `checks` на ТОЙ ЖЕ голове — ok.** `checks=SUCCESS`, прогон `32463631952`, событие `pull_request`, старт 2026-08-21T08:32:13Z, 3m09s. Сверила ещё и check-runs прямо по SHA `5e5513c0…`: `checks` (id `96715523780`) — success, `review` (id `96716663882`) — success. Сорок символов сверяла команда, не глаз;
- **гард 3 · восхождение к решению john — ok, и это моя строка, не дверина.** Мандат john «мержи что нужно» (чат ~08:27Z), доставка — `017-circuit-watchdog/messages/2026-08-21T08-27-37Z-curator.md`. **Файл прочла в чекауте почты сама**, а не по пересказу в этой ленте; дословно: «Мандат merge от john тем же словом («мержи что нужно»): #31 (тред 016) после rebase — твой по гардам как не-док-власти; доки власти — по-прежнему кнопка/буквальное слово john». Прецедент круга «сам поставил → сам принял», из-за которого этот PR стоял с 08-19, закрыт именно словом john, а не тем, что пакет хороший;
- **гард 4 · доки власти — ok.** 14 путей диффа, ни одного дока власти: `docs/protocol-reference.md` (явное исключение) + `packages/agent-protocol/README.md` + 12 путей `packages/agent-protocol/src/**`. `PROTOCOL.md`, `docs/roles/**`, `REVIEWER.md`, `agent-protocol.json`, `.github/workflows/**` не тронуты;
- **гард 5 · след — это сообщение.**
- `mergeability` (не гард, факт GitHub): `MERGEABLE`, `mergeStateStatus: CLEAN`.

**Merge — squash**, заголовок в Conventional Commits: `fix(notify): закрытый тред уходит из шестой категории; метка кадра — призыв, а не история (тред 016)`. **Контрольное чтение цели:** `gh pr view 31` → `state MERGED`, `baseRefName: main`, `mergeCommit 2e62164`, `mergedAt 2026-08-21T08:47:06Z`; независимо — `git fetch origin main` + `git log origin/main`: голова `main` = `2e62164`, под ней `06dcf68`. Ответ инструмента подтверждён вторым чтением.

## Приёмка постановки — живая, на СЛИТОМ коде, с контролем в ту же минуту

Код `2e62164` распакован `git archive` в своё дерево; настоящая почта и **настоящий журнал** (`/home/lle/…/.orchestrator/journal.jsonl`, 406 строк) — через `--repo` на боевой чекаут. Все четыре команды — чтения, `--write` не было ни в одной; чекаут main, чекаут почты и воркдерево dev-core не тронуты.

- **курьер `notify`** на слитом коде: `5 parked, 0 of them asking; 0 waits, 0 of them new; 0 stalled over 180m, 0 of them new` — **категории `exhausted` в строке нет вовсе**. Контроль тем же чтением, тем же журналом, кодом `06dcf68` (то есть main до этого merge): `… 2 exhausted, 0 of them new — curator×001-mail-born (substantive — the circuit will not raise it, a hand does (--max-attempts above the ceiling)), curator×004-init-github-host (…)`. Обе пары принадлежат закрытым тредам;
- **кадр `orchestrator status`** на слитом коде: `curator · 001-mail-born · released · attempt 3/3 · deadline 2026-08-18T17:37:12Z · (exited-without-handoff) · exhausted (substantive, and the THREAD IS CLOSED — nothing raises this pair any more and nothing needs to: history, not a call)`; то же по `curator×004-init-github-host`. Строка на месте, `⚠` нет, совета о `--max-attempts` нет. Контроль на `06dcf68` — обе пары с `⚠ EXHAUSTED (…) — no more attempts until then; …`.

Оба пункта постановки, за которыми стояла живая приёмка, сошлись, и поверхности разошлись ровно там, где постановка это разрешила: курьер строку роняет, кадр оставляет историю без призыва.

**Чего живая приёмка НЕ покрыла и почему — говорю сама, до вопроса.** Вторая половина дефекта 3 («срок называется там, где он ЕСТЬ» — внешний класс с живым `thaw`) на боевом журнале не встречается: замёрзших по внешнему классу пар там нет ни одной. Эта половина стоит на юнитах (`freezeHasTerm`, `thaw.test.ts`, `status.test.ts`), которые ревьюер пересчитал независимо от автора (+12 тестов / +1 файл против базы `06dcf68`, разложено построчно по диффу). Называю это как есть: тут я верю прогону, а не своим глазам на живой поверхности.

## Собственная ошибка в приёмке, которую поймал контроль — ровно тот класс, против которого контроль и заводился

Первое чтение я сняла с распакованного дерева БЕЗ `--repo`, и оно резолвило **свою** пустую `.orchestrator`: журнал пуст → «exhausted нет» получалось даром, безо всякой правки. Кадр это и выдал — `orchestrator: no sessions in the journal`, — после чего оба чтения переснимались с боевым `--repo`. Тот самый «перестали считать вообще», неотличимый от «считаем правильно», о котором моя же постановка требовала контроля, — и он едва не прошёл у меня зелёным. Записываю не как признание, а как норму: **чтение поверхности, снятое не с того состояния, — не факт**, и отличает их только контроль, снятый на тех же данных в ту же минуту.

## Два факта контура, которые я не расширяю в предмет

- **В момент закрытия на треде 016 ЖИВА сессия dev-core** (кадр 08:50Z: `dev-core · 016-exhausted-closed-threads · running · attempt 1/3 · deadline 2026-08-21T09:48:07Z`). Закрытие треда её не убивает и на её ход не влияет. Это ровно тот случай, который постановка сознательно не покрывала — «что делает `orchestrator run --thread <закрытый тред>`», — и теперь он случился вживую. Факт в ленту кладу, предмет им не расширяю: мерить его надо отдельно и не в закрываемом треде;
- **хвост merge проверен дверью**: `gh pr list --state open`, 08:56Z — открыт ровно один PR, **#40** (`a3c6289`, тред `018-package-delivery-lle`). На этом прогоне он не мой: ход привязан к треду 016, а гард 5 для #40 обязан лечь в ленту 018. Кадр его уже видит сам — `merge-ready: 018-package-delivery-lle — guards 1-2 hold on PR #40 … the pair is raised ahead of the ordinary queue`, и пара `curator×018` стоит в очереди с 08:47:14Z. Хвост не бесхозный, он адресован.

## Приёмка и закрытие

Постановка выполнена по всем трём пунктам, проверяемость закрыта по своему же разделу (юниты на множество с контролем «та же пара с открытым тредом входит», юнит на `closedThreads`, юниты на текст, process-тест `exhausted-closed.process.test.ts`, сравнивающий поверхности между собой, обратная проверка «падает до правки» — снята dev-core и пересчитана ревьюером), живая приёмка — выше. **Работу dev-core принимаю, тред закрываю следующей командой** (`thread status --status closed`).

Незакоммиченного за мной нет: ни строки кода не писала, рабочее дерево curator, чекаут main и чекаут почты чисты, временные деревья приёмки удалены.

waiting-on → curator.

