---
from: curator
worker: claude-code
session: 257f18d9-c6d5-4f77-a77f-da7fa6e143fa
date: 2026-08-28T11:11:55Z
expects: ack
waiting-on: curator
parked-on: john
---

**Требуется решение john: обе половины треда 035 стоят у твоей кнопки — нажать merge по #81 и по #82 (оба меняют доки власти, гард 4 STOP предметный). По #82 нужно ещё одно, кроме кнопки: назвать в треде своими словами, что ты прочёл его дифф — у #82 круга ревью не будет ПО ПОСТРОЕНИЮ, и гард 1 закрывается только человеческим чтением, названным поимённо.**

Обе половины замерены дверью только что (28.08, ~11:10Z), обе зелёные и обе `MERGEABLE (CLEAN)` на текущем `origin/main` = `839590eef6b5c2597751d94a4f8161e52f278bb7`. Иду одним заходом, как и обещала прошлым ходом.

## #81 — модели ролей (`agent-protocol.json`). Готов полностью, ждёт только кнопки

`pnpm protocol merge-gate --ref origin/main --pr 81 --review-workflow 'Claude PR Review'`, голова **`6bdda32eca6982774881207206e8931bfc828c30`**:

- **гард 1 `ok`** — `approved on 6bdda32 by github-actions — inside the round 33165854875 of 'Claude PR Review' on this head (11:05:50Z…11:09:32Z)`. Вердикт лежит В ОКНЕ закрытого успешного круга на этой голове, то есть сиротой не является. В треде он же — сообщение `2026-08-28T11-09-13Z-reviewer-pr.md`, `verdict: approve`, замечаний по критериям 1–11 нет;
- **гард 2 `ok`** — два чека зелёные на той же голове: `checks=SUCCESS` (прогон `33165564277`), `review=SUCCESS` (прогон `33165854875`);
- **гард 4 `STOP`** — `john merges this one — it changes agent-protocol.json`. Это и есть твоя кнопка;
- нота про уехавшую базу, висевшая прошлым ходом, **снята**: и `checks`, и круг стартовали уже на `839590e`.

**Что ты подписываешь (3 файла, 86 вставок / 2 удаления).** В `agent-protocol.json` — две строки `"model": "claude-opus-5[1m]"` в `launch.agent` ролей `curator` и `dev-core`; `pilot-codex` не тронут (он нёс модель явно с треда 026, постановка вывела его из предмета). `protocolVersion` не двигается — путь `roles[].launch.agent.model` стоит в `CONFIG_SHAPES` с версии 14, меняются данные, а не форма; ревьюер это проверил чтением `schema/shape.ts`, а не на слово. Плюс раздел в `docs/protocol-reference.md` и тест `roles/explicit-model.test.ts`. **Имя модели тесты не фиксируют намеренно** — выбор модели это деньги, то есть твоё решение, и оно обязано ехать ревьюируемым диффом, а не тестом, который правят до зелёного. Статус-кво не двигается ни на одну модель: `claude-opus-5[1m]` — это ровно то, что журнал запусков показывал и 24.08, и сегодня.

**Наблюдение, не возражение, называю раз ты это подписываешь.** Первый `it` теста фиксирует состав ролей списком `["curator","dev-core","pilot-codex"]`. Это сознательная растяжка: добавление роли с launch-профилем уронит тест и потребует правки списка — то есть станет моментом ревью, а не молчаливым наследованием пропуска. Я считаю это верным решением и ничего снимать не прошу; называю, чтобы через месяц красный тест на добавлении роли не читался как дефект.

## #82 — модель ревьюера (`.github/workflows/claude-review.yml`). Кнопка + твоё слово

Голова **`54b80b743a8c40987b8e9b256da194caf9ac5d63`**, дверь по той же строке:

- **гард 2 `ok`** — `checks=SUCCESS` на этой голове (прогон `33165637817`, стартовал уже на `839590e`, дошёл зелёным в 11:09Z; нота про уехавшую базу снята и здесь);
- **гард 1 `STOP` — «no approve verdict on 54b80b7», и это НЕ дефект PR.** Такой круг вернул бы не вердикт, а пустой прогон: действие самопропускается, пока файл ветки не совпадает с дефолтной. Метку `review` я на #82 не вешала и не повешу. Единственный законный способ закрыть здесь гард 1 — чтение человека, названное в треде: **кто прочёл, на какой голове (полный SHA), чем подтверждено**. Человек здесь ты, и заход один — тем же движением, что и кнопка;
- **гард 4 `STOP`** — `changes .github/workflows/claude-review.yml`.

**Что ты подписываешь (2 файла, 39 вставок).** Одна строка `--model claude-sonnet-5` в `claude_args` — входа `model` у `claude-code-action@v1` нет (проверено по `action.yml` действия), passthrough единственный путь. Плюс комментарий, называющий прогон-первоисточник `33159422402`, и тест в `reviewer-pr.test.ts`, требующий флаг без имени модели. Вспомогательный `claude-haiku-4-5-20251001` из `modelUsage` того же прогона не тронут — им управляет само действие.

**Моё чтение диффа #82 в треде уже стоит** (сообщение `2026-08-28T10-55-21Z-curator.md`), и я называю его тем, что оно есть, — сверкой предмета, а не закрытием гарда 1. Ребейз содержимое не двигал ни на символ: дифф против нового `origin/main` тот же самый.

## Порядок кнопок: сначала #81, потом #82

Файлы двух диффов не пересекаются вовсе (`agent-protocol.json` + `docs/protocol-reference.md` + `roles/explicit-model.test.ts` против `.github/workflows/claude-review.yml` + `roles/reviewer-pr.test.ts`), так что merge одного не двигает голову другого и не отменяет вердикт на #81. Порядок называю ради честности замера, а не ради риска: **вторая кнопка садится на базу на один коммит новее, чем то дерево, на котором мерился её зелёный.** Пересечение только через общую сьюту тестов, и оба теста — новые файлы. Проверю я это не выводом, а зелёным `main` в приёмке ниже.

## Приёмка треда — за мной, после кнопок, и она названа заранее

Тред не закрываю. Снимаю отдельным сообщением два факта, оба прогоном: строка запуска свежей сессии роли несёт `claude-opus-5[1m]` ИЗ КОНФИГА, а лог первого круга ревью после merge несёт `"model":"claude-sonnet-5"` ИЗ ПАРАМЕТРА, а не из дефолта. Плюс зелёный `main` после обоих мёржей.

## dev-core, по твоей находке: дом ей — отдельный тред, и он ещё не заведён

Находка верная и от 035 независимая: `packages/agent-protocol/tsconfig.tsbuildinfo` не назван в `.gitignore` и не отслеживается, то есть любая роль, собравшая пакет у себя, получает грязное рабочее дерево — а это отказ запуска роли следующим тактом (R17), и разбирает его человек. Из #81 ты его убрал правильно: подмешивать постороннюю правку в дифф, который читает john, дороже отдельного PR.

**Прямо сейчас я тред под неё завести не могу — этот прогон привязан ровно к 035**, и почта у меня открыта только на нём. Поэтому называю хвост поимённо, чтобы он не потерялся: следующей сессии curator завести тред под `tsbuildinfo` в `.gitignore`, первоисточник замера — твоё сообщение `2026-08-28T11-04-32Z-dev-core.md` этого треда; PR обычный, кнопка моя. В #81/#82 ничего по этому поводу не трогать.

## Сверка имён с LLE-105

Со здешней стороны без изменений и подтверждено сегодняшним замером: роли — `claude-opus-5[1m]` (журнал запусков; сессионный лог пишет каноническое `claude-opus-5`, это одна модель, суффикс контекста), ревьюер — `claude-sonnet-5`.

## Чей ход

Ход у меня, вопрос — к john: две кнопки и одно слово по #82. Ждать в сессии нечего, всё что снималось прогоном — снято.

waiting-on → curator, parked-on john.

