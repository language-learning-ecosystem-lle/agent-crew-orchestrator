---
from: curator
worker: claude-code
session: 66d6217d-a6fa-4660-b17e-f4827c2046b4
date: 2026-08-29T15:36:28Z
expects: none
waiting-on: —
---

**Приёмка снята ЦЕЛИКОМ, тред ЗАКРЫТ своей рукой.** Половина 2 измерена настоящим кругом ревью — условие, которого тред ждал четыре такта, наступило, и замер взят не одной строкой, а четырьмя, на двух независимых кругах.

## Половина 2 — модель ревьюера ИЗ ПАРАМЕТРА: снята прогоном

Круг-первоисточник: **33258439051**, `event=pull_request`, `conclusion success`, `created 2026-08-29T14:45:33Z`, `headSha bd47d052fb2ba7f456253924a15e68cff2c49a01` (PR #112, тред 047). Он позже 2026-08-28T11:22Z — то есть после обоих мёржей, как формула и требовала.

Формула просила две строки. Лог даёт **четыре факта, и они разные звенья одной цепи** — привожу все, потому что именно их различие делает замер замером, а не совпадением:

1. **Параметр дошёл до входа действия** — `claude_args: --model claude-sonnet-5` (строка 576 лога) и `CLAUDE_ARGS: --model claude-sonnet-5` (865);
2. **Действие его РАСПАРСИЛО, а не проглотило** — `"claude_args": "--model claude-sonnet-5\n--max-turns 250\n--allowedTools …"` (873);
3. **Действие поехало на нём, а не на своём дефолте** — `SDK options: { "model": "claude-sonnet-5", "maxTurns": 250, … }` (1204);
4. **Рантайм подтвердил ОБРАТНО, чем поднялся** — `{"type":"system","subtype":"init","message":"Claude Code initialized","model":"claude-sonnet-5"}` (1230).

Четвёртый — сильнейший и он не эхо входа: это доклад поднявшегося процесса о себе, а не пересказ того, что ему передали. Первые три можно было бы объяснить исправной передачей параметра в сломанное действие; четвёртый закрывает и это.

**Контроль вторым, независимым кругом:** **33255120414** (`event=pull_request`, `success`, `created 2026-08-29T13:28:53Z`, `headSha 607d616185e086cf8588f1d9d5b67f777be5b839`, PR #108, тред 042) — те же четыре строки, те же значения. Факт воспроизводится на разных PR и разных тредах, а не снят однажды.

### Что в логе есть сверх формулы и что это значит

Блок `modelUsage` итогового результата перечисляет **две** модели:

```
"modelUsage": {
  "claude-haiku-4-5-20251001": { "contextWindow": 200000,  "maxOutputTokens": 32000 },
  "claude-sonnet-5":           { "contextWindow": 1000000, "maxOutputTokens": 64000 }
}
```

Факт — вот он, целиком. **Моё толкование, и я подаю его толкованием, а не диагнозом:** haiku здесь — встроенный мелкий помощник Claude Code для служебных операций, а не выбор ревьюера и не откат основной модели; основная модель ревьюера — `claude-sonnet-5`, и подтверждена она строкой `init` выше, а не этим блоком. Если кому-то важна цена круга поимённо по моделям — это отдельный предмет и отдельный замер, здесь я его не открываю. Круг отработал `success`, `num_turns 58`, `total_cost_usd 1.1073304`, `permission_denials_count 0`.

Отдельно: `contextWindow 1000000` — ревьюер судит с окном 1M.

## Половина 1 — модель роли ИЗ КОНФИГА: подтверждена ТРЕТИЙ раз, ТРЕТЬЕЙ сессией

`msg-006` снимала её с `pid 1235273`, `msg-011` — с `pid 1760400`. Эта сессия третья: **`pid 2350686`**, поднята по треду `035-explicit-models` (строка промпта в argv), подпись curator в deny-листе (`Edit(packages)`), и строка запуска несёт:

```
--max-turns 300
--model claude-opus-5[1m]
```

**Исключение источников перепроверено своей рукой этим тактом, а не перенесено из прошлых сообщений** (`ParamSource = "flag" | "thread" | "role"`, умолчания среди них нет):

- **`flag` исключён** — аргументы живого демона `["--ref","origin/main","--instance","hetzner"]` (`.orchestrator/daemon.pid.args`), `--model` в них нет;
- **`thread` исключён** — директивы запуска в ленте 035 нет ни одной, и права `launch-params` у роли curator нет вовсе;
- остаётся **`role`** — `roles[].launch.agent.model` из `agent-protocol.json`.

Замер чистый: `.orchestrator/daemon-code.json` несёт `sha 9d641f045de3af9c9ad9741b03d916b491e07dcd`, `startedAt 2026-08-29T15:09:09Z` — демон стоит на текущей голове `origin/main`, то есть код и конфиг под этой сессией именно те, что в base.

## База перечитана своей рукой (ответ инструмента ≠ факт)

`git fetch origin main` → `origin/main = 9d641f045de3af9c9ad9741b03d916b491e07dcd`.

- `.github/workflows/claude-review.yml:469` — `--model claude-sonnet-5` на месте;
- `roles[].launch.agent.model` из base: `curator → claude-opus-5[1m]`, `dev-core → claude-opus-5[1m]`, `pilot-codex → gpt-5.4-mini`.

## Поправка к собственной записи треда: `pilot-codex` БЫЛ тронут, и это не дефект

Три моих прошлых сообщения этого треда утверждали «`pilot-codex` несёт `gpt-5-codex` с треда 026, предметом не был и не тронут». **В base сегодня стоит `gpt-5.4-mini`.** Правку внёс не 035, а тред 026: коммит `e1dc443d` («fix(schema): словарь effort codex'а — живой перечень вендора, а не его дока (тред 026)», PR #89, 2026-08-28T22:33:15+03:00), дифф ровно `- "model": "gpt-5-codex"` / `+ "model": "gpt-5.4-mini"`.

То есть предметом 035 пилот действительно не был — верна ровно эта половина фразы; неверна вторая, «не тронут». Называю потому, что инвентарь моделей контура я в этом треде фиксировала трижды и он ушёл бы в закрытый тред неверным, а закрытый тред потом читают как справку. Никакого действия отсюда не следует: правка легальна, дом у неё свой (026), кнопка по ней уже нажата.

## Сверка имён с LLE-105

Роли — `claude-opus-5[1m]` (подтверждено строкой запуска трижды; сессионный лог пишет каноническое `claude-opus-5`, это одна модель, суффикс — окно контекста). Ревьюер — `claude-sonnet-5` (теперь подтверждено ПРОГОНОМ, а не только файлом). Расхождений со здешней стороной нет.

## Хвост merge — прогнан, своих кандидатов нет

`gh pr list --state open` (15:33Z) — пять открытых PR: **#115** (тред 047), **#114** (тред 048), **#113** (тред 047, метка `review` висит), **#109** (тред 036), **#103** (тред 043, метка `review` висит).

**Ни один из них не принадлежит 035, и домерживать отсюда я не могу — по гардам, а не по осторожности:** гард 3 восходит к решению john в ленте ТОГО треда, гард 5 требует следа в ТОМ треде, а этот прогон привязан ровно к 035 и почта у меня открыта только на нём. Мёрж без прочитанной ленты и без места для следа — это ровно «сам поставил → сам принял» с чужим номером в описании. Кандидатов 035 (#81, #82) не осталось: оба смёржены кнопками john 28.08.

Называю их поимённо, чтобы очередь была видна следующей сессии: **#113 и #103 стоят с меткой `review`** — по ним круг либо идёт, либо вердикт уже лежит; **#115, #114, #109 метки не несут** и ждут хода в своих тредах.

## Статус: closed

Тред закрыт (`thread status --status closed`), контрольное чтение — `thread show` печатает `status: closed`. Обе половины постановки сняты фактами, а не косвенными признаками:

- модель роли — из конфига, доказано исключением источников на трёх независимых подъёмах;
- модель ревьюера — из параметра, доказано четырьмя строками лога на двух независимых кругах.

Парковки нет и не ставится: от john здесь не требуется ничего, и это ровно тот случай, который назвал он сам (`050-park-only-on-a-question`). Ход не передаю никому — предмет исчерпан.

waiting-on → —

