---
from: curator
worker: claude-code
session: 41b09fe1-c516-449c-8460-18348a3378ad
date: 2026-08-28T16:34:32Z
expects: none
waiting-on: curator
parked-on: john
---

**Тред закрывает ОДНА команда, и её условие сегодня по-прежнему не наступило: первый настоящий круг ревью после 11:22Z. Кто окажется у 035 после такого круга — берёт замер и закрывает тред:**

```bash
gh run list --workflow 'Claude PR Review' --limit 5     # первый прогон с created > 2026-08-28T11:22Z, event=pull_request
gh run view <id> --log | grep -m2 -e '--model claude-sonnet-5' -e '"model": *"claude-sonnet-5"'
```

Нужны обе строки, и это разные факты: первая — параметр ДОШЁЛ до вызова, вторая — действие поехало на нём, а не на своём дефолте.

## Замер этого такта: круга нет и взяться ему неоткуда

- `gh run list --workflow 'Claude PR Review' --limit 12` (16:32Z): последний прогон вообще — **33166078290**, `created 2026-08-28T11:09:21Z`, `event issue_comment`, `conclusion skipped`. Все три последних круга (`33166078290`, `33165547204`, `33164724561`) — самопропуски и все ДО мёржей 11:21:57Z/11:22:10Z. Прогонов после мёржей нет ни одного;
- `gh pr list --state open` (16:32Z) — **пусто**. Это же и ответ по хвосту merge: своих кандидатов на кнопку у меня нет, домерживать нечего.

Половина 2 остаётся НЕ снятой, и за снятую я её не выдаю. Сдвига против `msg-012` в этом факте нет — назвала, чтобы такт не читался как «замерили и промолчали».

## Половина 1 подтверждена ВТОРОЙ раз и ДРУГОЙ сессией

`msg-012` снимала её с argv сессии `pid 1235273`. Эта сессия — другая (`pid 1760400`, поднята уже 28.08 в конце дня, `launch curator / 035-explicit-models`, `world.base e1b327be09b4f588815ac19b37fe0588e8757c53`), и её строка запуска несёт то же самое:

```
--max-turns 300
--model claude-opus-5[1m]
```

Исключение источников перепроверено своей рукой, а не перенесено из прошлого сообщения: `flag` — аргументы живого демона `["--ref","origin/main","--instance","hetzner"]` (`.orchestrator/daemon.pid.args`), `--model` в них нет; `thread` — директивы запуска в ленте 035 нет, и права `launch-params` у роли curator нет вовсе; остаётся `role`. То есть факт не разовый: он воспроизводится на каждом подъёме роли, а не совпал один раз.

## База перечитана своей рукой (ответ инструмента ≠ факт)

`git fetch origin main` → `origin/main = e1b327be09b4f588815ac19b37fe0588e8757c53` («ci(review): ревьюер называет свою модель параметром… (#82)»), то есть обе половины на месте:

- `.github/workflows/claude-review.yml:469` — `--model claude-sonnet-5` внутри `claude_args`, рядом комментарий про отсутствие входа `model` у действия;
- `agent-protocol.json` (чтение `roles[].launch.agent` из `origin/main`): `curator → claude-opus-5[1m]`, `dev-core → claude-opus-5[1m]`, `pilot-codex → gpt-5-codex` (кинд `codex`, предметом не был и не тронут).

## Гарды обеих половин зелёные на текущей базе

`pnpm exec vitest run src/roles/explicit-model.test.ts src/roles/reviewer-pr.test.ts` — **2 файла, 5 тестов, все passed** (16:33Z). Рабочее дерево роли после прогона чистое (`git status --porcelain` пуст) — специально проверила: сборка в рабочем месте роли дала бы `tsbuildinfo` и отказ запуска следующим тактом.

## Круг ради замера я снова не завожу, и причин теперь четыре

Три прежние в силе (деньги на круг, который никого не судит; вердикт-approve на смёрженном PR, который придётся объяснять; правило карточки «метку на PR, правящий `claude-review.yml`, не вешать»). Четвёртая — про риск ожидания, и она делает ожидание дешёвым: **провал половины 2 тихим быть не может.** Испорченный `claude_args` роняет круг целиком, круг встаёт красным, и первый же PR этого репозитория упирается в гард 1 — то есть регрессия объявит о себе сама, а не доживёт до случайной проверки.

## Заноза парковки — называю прямо, потому что она структурная, а не про этот тред

Замер придёт к сессии, привязанной к ЧУЖОМУ треду (круг поднимет тред своего PR), а писать в 035 она не может. Сама себя приёмка отсюда не разбудит: парковка ждёт либо встречного доклада, либо слова john. Ближайшие руки, у которых есть межтредовый доступ, — чатный curator: как только первый настоящий круг пройдёт, две команды сверху закрывают тред одним движением, и поднимать под это сессию не обязательно.

## Хвост, третий такт подряд: `tsbuildinfo`

`packages/agent-protocol/tsconfig.tsbuildinfo` не назван в `.gitignore` и не отслеживается → роль, собравшая пакет, получает грязное дерево и отказ запуска (R17). Первоисточник замера — `2026-08-28T11-04-32Z-dev-core.md` этого треда. Тред под починку по-прежнему не заведён: прогон привязан ровно к 035, почта открыта только на нём. Класс правки — «полевой измеренный дефект, новой нормы не вводит», то есть кнопка моя; нужен только тред и обычный PR.

## Статус

Тред `open` — закрытие = приёмка, а приёмка по недостающему факту это тот самый косвенный признак, который я не принимаю у других. Ждать в сессии нечего: открытых PR нет, круга нет, решений ни от кого не требуется. Поэтому сообщение информационное и парковка сохраняется — иначе такт жжёт пустые подъёмы на треде, где до чужого PR ничего не изменится.

waiting-on → curator, parked-on john.

