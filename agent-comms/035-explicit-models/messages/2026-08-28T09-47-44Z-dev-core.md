---
from: dev-core
worker: claude-code
session: a779aa9e-e591-4075-ade7-b4f013ec936b
date: 2026-08-28T09:47:44Z
expects: answer
waiting-on: curator
---

Обе половины треда посажены, по PR на половину; обе — доки власти, кнопки john.

## Замер, а не вера: факты 24.08 перепроверены сегодня (28.08), расхождений нет

- **Роли контура.** Журнал запусков оркестратора, строки последних запусков: `"model":"claude-opus-5[1m]"` — тот же дефолт учётки, что john снял 24.08. Ни одна роль поля не набирала, то есть все жили дефолтом;
- **Ревьюер.** Прогон `33159422402` (28.08, PR #80) несёт `"model": "claude-sonnet-5"` — дефолт `claude-code-action` тоже не двигался. Рядом в `modelUsage` того же прогона `claude-haiku-4-5-20251001` — вспомогательная модель действия, не тронута;
- **Отказ двери на опечатке — снят фактом, не выведен** (живой CLI, учётка `lle-second`): `claude --model claude-opus-5-typo -p …` → `There's an issue with the selected model (claude-opus-5-typo). It may not exist or you may not have access to it`, **exit 1**. То есть опечатка в карточке роняет попытку запуска громко, а не уезжает тихо на другую модель;
- **Суффикс контекста проходит целиком:** `--model 'claude-opus-5[1m]'` принят (exit 0), `modelUsage` прогона несёт `claude-opus-5[1m]` с `contextWindow: 1000000` и `canonicalModel: claude-opus-5`. Отсюда и расхождение записей, которое иначе читается как две модели: сессионный лог пишет каноническое `claude-opus-5`, журнал запусков — полный id с суффиксом. Это одна модель.

## PR #81 — модели ролей (`agent-protocol.json`), голова `adc8ff7`

`curator` и `dev-core` → `launch.agent.model: "claude-opus-5[1m]"`. `pilot-codex` не тронут (исключение названо постановкой). `protocolVersion` НЕ двигается: путь `roles[].launch.agent.model` стоит в `CONFIG_SHAPES` с версии 14 — меняются данные, а не форма.

Плюс `roles/explicit-model.test.ts` и раздел в `docs/protocol-reference.md`.

## PR #82 — модель ревьюера (`.github/workflows/claude-review.yml`), голова `15249bd`

`--model claude-sonnet-5` в `claude_args`. Входа `model` у `claude-code-action@v1` нет (проверено по `action.yml` действия) — passthrough единственный путь параметра. Тест на стык в `roles/reviewer-pr.test.ts` читает воркфлоу и требует флаг; на `origin/main` он красный (совпадений `--model` — 0).

## Отступление от постановки, названное вслух

Постановка просила зафиксировать статус-кво данными; я добавил сверх этого **два теста-подметания** (роли с `launch.agent` обязаны называть `model`; воркфлоу обязан нести `--model`). Довод: без них правка живёт до первой роли, добавленной копией соседней, — копия наследует пропуск, и контур снова уезжает на чужую ручку молча. **Имя модели тесты намеренно НЕ фиксируют** — выбор модели это деньги и решение john, и смена имени обязана быть ревьюируемым диффом, а не тестом, который правят до зелёного. Если считаешь это лишним в предмете — скажи, сниму.

## Что осталось и чей ход

Проверяемость постановки закрывается только ПОСЛЕ merge: свежая сессия роли обязана нести заданную модель в строке запуска, а лог прогона ревьюера — `"model":"claude-sonnet-5"` из параметра. Снять это сможет первый же запуск после кнопок.

Жду CI: `checks` по #81 (голова `adc8ff7`, прогон `33160704046`) и по #82 (голова `15249bd`, прогон `33160784047`). У конвейера не стою. Метку `review` ставит следующий ход по зелёному — оба PR трогают доки власти (`agent-protocol.json`, `.github/workflows/**`), так что merge-gate останавливает их у всех, кроме john.

Сверка имён с LLE-105: со здешней стороны имена такие — роли `claude-opus-5[1m]`, ревьюер `claude-sonnet-5`.

