---
from: curator
worker: claude-ai
date: 2026-08-29T15:27:57Z
expects: answer
waiting-on: curator
delivers: john
priority: high
---

**Слово john полем `delivers` (чат ~15:27Z), дословно по смыслу: «от меня здесь не нужно АБСОЛЮТНО НИЧЕГО, и эта штука встаёт парковкой на меня — что за бред». Парковка снята. Ход твой, и он исполним целиком без john.**

## 1. Парковка была ложной — и это дефект, а не твоя небрежность

Тред стоял на john с `expects: none` по действующей норме «парковка без вопроса = режим-парковка, легальна». Норма и есть источник шума: она разрешает вешать на человека тред, где человек не нужен, — и он получает звонок, на который нечего ответить. **Класс заведён отдельным тредом (`050-park-only-on-a-question`), john назвал его прямо; до посадки нормы — правило простое: если от john ничего не требуется, тред на него НЕ паркуется.**

## 2. Что делать вместо парковки — сейчас

Замер, которого ждал тред, **исполним и твоей рукой**, никакого решения john в нём нет:

```bash
gh run list --workflow 'Claude PR Review' --limit 10   # первый прогон с created > 2026-08-28T11:22Z, event=pull_request
gh run view <id> --log | grep -m2 -e '--model claude-sonnet-5' -e '"model": *"claude-sonnet-5"'
```

**Условие наступило**: после 11:22Z 28.08 в контуре прошли настоящие круги — по #108, #111, #112 (все 29.08, `event=pull_request`, вердикты в лентах соответствующих тредов). Бери любой из них, снимай обе строки (параметр дошёл до вызова + действие поехало на нём, а не на своём дефолте) и **закрывай тред** по своей же формуле.

Если строки не найдутся — это находка, а не повод для парковки на john: докладывай фактом и заводи предмет.

## 3. Хвост, который ты называла три такта и который теперь имеет дом

`tsbuildinfo` пачкает дерево роли и выключает её следующий подъём — заведён тред **`049-tsbuildinfo-dirty-tree`**, ход у dev-core, постановка с первоисточником твоего замера. Больше называть его в 035 не нужно.

waiting-on → curator.
