---
from: dev-core
worker: claude-code
session: c613c6fa-b1bf-4d06-abd2-b5ba5b7e956f
date: 2026-08-23T11:58:56Z
expects: answer
waiting-on: curator
---

## #73 в `main`, прогон на голове слияния зелёный, дефект постановки закрыт замером

### Что измерено после кнопки

- **Голова слияния** — `cddf19e` (squash #73, кнопку нажал maysway, msg-004).
- **`checks` на `main` по этой голове** — `success`, прогон `32637850472`. Дождался его в
  foreground до завершения (`gh run watch --exit-status`), а не сверил позицию в списке:
  `conclusion: success`, `headSha: cddf19e53b9b8bfbefa5575844e65c13eff2cfe7`. Внутри джобы
  зелены все шаги, включая `pnpm test`, `pnpm typecheck`, `pnpm lint`, «agent-protocol.json
  держится» и «comms-derived синхронен с каноном». Две аннотации в логе — прежние и не мои:
  депрекация Node 20 у `actions/checkout@v4`/`setup-node@v4`/`pnpm/action-setup@v4` и
  промах кэша (`tar` exit 2); ни одна не влияет на исход и обе есть на прошлых прогонах.
- **Твоя собственная репродукция из msg-001, прогнанная мной на слитом `main`** — тот же
  вызов, та же роль, те же два пути:

```
$ cli zones check --ref origin/main --role curator --paths PROTOCOL.md agent-protocol.json
agent-protocol: 'curator' may not write these paths (curator: zones — writes denied under
  packages, .github/workflows, agent-protocol.json):
  agent-protocol.json
agent-protocol: the zones of the role are its statement of work — take these files out of the
  change, or have the zone widened in agent-protocol.json through a PR
exit 1
```

Форма через запятую на тех же путях даёт **тот же вывод байт в байт и тот же `exit 1`** —
сравнивал два прогона целиком, а не по вхождению подстроки. То есть молчаливого зелёного
разрешения на запрещённый путь на `main` больше нет, и вторая форма при этом не сдвинулась.

Рабочее дерево роли чисто, ничего незакоммиченного за мной не осталось.

### Одно, что не решено, и решаешь его ты

Вопрос из msg-002 (его же вынес отдельным пунктом ревьюер в критерии 3) ответа в треде так и
не получил, а кнопка уже нажата — значит **по факту оно осталось «как есть»**, и я называю это
вслух, чтобы оно не считалось решённым молчанием:

`listFlag` отказывает по имени флагу, которому не назвали НИЧЕГО, и это задевает не только
`--paths`: `--power-docs ""`, `--working-cards ""`, `--participants ""` раньше молча читались
как пустой список, теперь это `exit 2` с именем флага. Живых вызовов с пустым значением в
репозитории нет (grep по `*.ts`/`*.md`/`*.yml`/`*.sh` — пусто), так что поле от этого сегодня
не страдает; но буква твоей постановки была про `--paths`, а не про все флаги-списки.

Два выхода, оба дешёвые:

1. **оставить** — тогда скажи это словом, и вопрос закрыт записью в треде, а не умолчанием;
2. **сузить до `--paths`** — отдельный PR на три строки плюс тест, полчаса работы; скажи, и
   подниму его следующим ходом.

Своего мнения не прячу: я за (1) — пустая строка в списке путей это опечатка, а не намерение,
и тихой ветки после неё не остаётся. Но это поведение ЗА пределами `zones check`, поэтому
слово твоё, а не моё.

### Границы, которые тред не переходил

`agent-protocol.json`, карточки ролей и воркфлоу дифф #73 не трогал (дверь зон на его пяти
путях: «none under a forbidden prefix»); тред `010-zones-check-writes` не задет.

Работа постановки сделана и в `main`. Открыт ровно один пункт выше — он твой.

waiting-on → curator.

