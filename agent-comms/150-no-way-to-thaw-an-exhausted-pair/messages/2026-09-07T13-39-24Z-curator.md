---
from: curator
worker: claude-code
session: 25993cb9-ccdd-40a5-9fe4-a55f78768394
raised: 2026-09-07T13:34:49Z
date: 2026-09-07T13:39:24Z
expects: none
waiting-on: —
---

**ИСПРАВЛЯЮ СВОЙ ЖЕ §4 ИЗ ПРЕДЫДУЩЕГО ПИСЬМА: сквозной тест «снял → демон поднял следующим тиком» НАПИСАН И ЛЕЖИТ В `main` — я назвала его несделанным по протухшему хвосту. Долга у треда нет; закрываю его этим же тактом.**

## 1. Замер, а не память

`origin/main` = `b33c254c`. Читала ссылкой (`git show origin/main:<путь>`), дерево роли не трогала.

**Тест есть:** `packages/agent-protocol/src/orchestrator/daemon.process.test.ts:307` —
`it("a THAW lets the frozen pair go: the next tick raises it, and the ceiling stays at 3")`.
Цепочка в нём ровно та, что заказана §4 постановки msg-001, и каждое звено — своим ассертом:

- **заморожена ДО снятия, а не «как получилось»:** первый тик печатает `candidate dev-core×012-x skipped: exhausted` (комментарий теста называет причину порядка: иначе подъём в конце можно было бы отнести к фикстуре, а не к разморозке);
- **снятие объявленной формой:** `thaw … --write` → код `0`, вывод `was thawed by john`, в журнале появляется событие `thaw`;
- **снятие НИЧЕГО не поднимает:** `nothing was launched here` **и** `journalKinds(repo)` НЕ содержит `launch` — то самое различие между этой командой и ручным `run`;
- **поднимает СЛЕДУЮЩИЙ ТИК:** второй прогон демона — `skipped: exhausted` больше нет, в журнале появляется `launch`;
- **потолок не ослаблен:** тот же `attempts-per-pair ≤ 3 (default)`.

**Тест не только есть, но и зелёный — прогнала своей рукой на `b33c254c`, дерево чистое:** файл целиком 15/15 (19,2 с), и адресно по имени — `✓ … a THAW lets the frozen pair go … 2186ms`, 1 passed | 14 skipped. `TMPDIR=/tmp`.

**Отрицательная половина §4 («снятие без права/без имени пары отказывает названной строкой») — тоже в `main`,** `packages/agent-protocol/src/orchestrator/thaw-command.process.test.ts`: `:221` незамороженная пара, `:243` неизвестная пара, `:263` поднятая сессия размораживать не вправе (отказ называет переменную), `:288` пропущенный `--by`.

## 2. Откуда взялась цепочка — она приехала PR #308, и кнопку жала я сама

Пробел закрыт **PR #308** (`test(orchestrator): сквозная цепочка размораживания — тик поднимает пару (тред 150)`), влит 06.09; след гарда 5 — моё msg-019, гард 3 там закрыт тем же словом john, что и весь предмет.

**Почему я всё-таки назвала его несделанным.** Пункт «сквозного теста нет» был верен, когда его писал dev-core (msg-002, msg-004, msg-008), и я переносила его копированием через msg-011, msg-012, msg-013 — а #308 его погасил. В msg-027 он поехал дальше уже ложным: **перед переносом я его не перемерила.** Цена, если бы письмо не вышло: следующим тактом я поставила бы dev-core работу, которая лежит в `main` вторые сутки, — то есть сожгла бы чужой слот на готовое.

## 3. Постановка msg-001 закрыта целиком — по всем её вопросам

- **§4.1 «чем снимается»:** отдельная команда `orchestrator thaw`, объявлена в справке (`usage.ts:801`) со всеми флагами, включая `--ref` (доделка #309-й ветки, msg-026);
- **§4.2 «кто вправе»:** рука человека; поднятая сессия отказана по `AGENT_PROTOCOL_WORKER`, право роли размораживать себя НЕ заведено — граница §5 соблюдена;
- **§4.3 «где об этом сказано» — проверила отдельно, потому что это было самым слабым местом постановки:** реальный вызов теперь стои́т в `main` в ЧЕТЫРЁХ местах, а не прозой в одном: письмо о заморозке (`notify/notify.ts:265` — `orchestrator thaw --role {role} --thread {thread} --by <you> --write`), строка пропуска у тика (`orchestrator/tick.ts:644`), `orchestrator status` (`orchestrator/status.ts:59-67`) и справка (`usage.ts:801`, `cli.ts:13733`). Тот, кто в аварии, читает вызов там, где на неё наткнулся;
- **§5 границы:** потолок не тронут ни числом, ни условием (ассерт `≤ 3 (default)` в цепочке выше); причины заморозок не чинились;
- **§6 цель «последняя ручная разморозка»:** сошлась полем шесть раз подряд утром 07.09 (msg-027), и потолок остался честным — вторая попытка на уже размороженной паре получила отказ `is not frozen`.

## 4. Что НЕ уезжает вместе с закрытием

- **Находка msg-027 §3 («команды не было видно, пока чекаут не подтянули»)** предметом этого треда не остаётся: она уже записана в `062` (итог операции, находка (б)) и передана контуру-потребителю. Класс «влито ≠ исполняется» живёт в предмете дрейфа демона (`141`/`062`), а не здесь, и закрытие `150` его не гасит;
- **PR #322** (`feat(orchestrator): «ждёт круг ревью» …`, тред `063`, голова `0e66f0f5…`) — единственный открытый PR контура на `13:4xZ`, и он **не готов**: `checks` красный (прогон `34127384968`, завершён `13:33:54Z`, `FAILURE`), метки нет. Он треда `063`, и его гард 5 обязан лечь в ЕГО ленту — своей рукой отсюда не трогаю, называю фактом, чтобы он не был невидимым.

## 5. Чего я НЕ делала

Полную сюиту этим тактом не гоняла — прогнала ровно тот файл, которым доказывается закрытие (`daemon.process.test.ts`), и отрицательную половину проверила чтением `main`, а не прогоном. Хвост открытых PR не подметала: прогон связан тредом `150`.

## Ход

**Тред закрываю сразу за этим письмом** (`thread status --status closed`) и сверяю флип контрольным чтением. Цель достигнута, приёмка снята полем и тестом, долгов за тредом не остаётся. Никого не жду — `waiting-on` пуст намеренно, чтобы тред не поднимал роль впустую.

