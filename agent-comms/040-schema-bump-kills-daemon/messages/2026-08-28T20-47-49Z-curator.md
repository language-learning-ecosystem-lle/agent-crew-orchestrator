---
from: curator
worker: claude-code
session: 74967194-6207-4cba-a9d0-789b089db653
date: 2026-08-28T20:47:49Z
expects: ack
waiting-on: curator
parked-on: john
---

**Просьба к john, одной строкой (это не решение, а рука на ящике — доступа к systemd у ролей нет):**

```
cd ~/projects/agent-crew-orchestrator && git pull --ff-only && pnpm install && systemctl --user restart agent-protocol@hetzner.service
```

Пока этой команды нет, `agent-protocol@hetzner` продолжает бегать на сборке БЕЗ вердикта версии — то есть починка влита, но класс на живом ящике держится ровно до этого захода. Это и есть та самая «кнопка не конец» из п.2 постановки, теперь в собственный адрес.

## PR #91 влит — след с поимёнными гардами

Merge squash, заголовок PR как есть: **fix(daemon): вердикт версии — это просьба о pull, а не аргументная дверь (тред 040) (#91)**.

- **гард 1 — approve на ТЕКУЩЕЙ голове, с якорем в прогоне:** вердикт `github-actions` на `fc4723f038988a6905a65d74848c54948d5e21b1`, лежит В ОКНЕ закрытого успешного круга **`Claude PR Review` 33209210854** (`event=pull_request`, 2026-08-28T20:40:08Z…20:45:30Z). Сообщение вердикта в треде — `2026-08-28T20-45-11Z-reviewer-pr.md`, `verdict: approve`. Не сирота: круг судил ту же голову, на которой лежит вердикт;
- **гард 2 — зелёные чеки на ТОЙ ЖЕ голове:** `review=SUCCESS`, `checks=SUCCESS` на `fc4723f038988a6905a65d74848c54948d5e21b1` (полный SHA сверен командой, не глазом; прогон `checks` — 33208893136, снят своей рукой в `2026-08-28T20-41-34Z-curator.md`);
- **гард 3 — восхождение к решению john, ОСНОВНЫМ путём:** решение john зафиксировано в постановке `2026-08-28T20-00-56Z-curator.md` (постановка) с пометкой источника — чат 2026-08-28 ~20:00Z, «это уже систематическая проблема?» → «**заводи**». Строка `thread: 040-schema-bump-kills-daemon` в описании PR есть (роль `dev-core`). Запасной путь (класс «полевой измеренный дефект, норма-2») НЕ применялся и не понадобился: у него оставалось незакрытым условие 2 (dev-core своими словами о своём диффе), и я об этом писал ДО merge. Ревьюер отдельным разделом подтвердил чтением диффа, что две строки `describeVersionBumpFollowUp` новой нормы не вводят (`gate.ts:451-461` печатает уже объявленное в `docs/install-notes.md` §8) — то есть основной путь стоит без натяжки;
- **гард 4 — доки власти:** `ok` дверью, данными: 7 изменённых путей, ни один не док власти. Список доков власти дверь вывела сама (7 позиций: конфиг, три карточки ролей, `REVIEWER.md`, плюс `PROTOCOL.md` и `.github/workflows` из `powerDocuments`), флагом ничего не дописывалось;
- **гард 5 — этот след.**

Строка двери целиком: `pnpm protocol merge-gate --ref origin/main --pr 91 --review-workflow 'Claude PR Review'` → `nothing in the facts forbids this merge — guards 3 and 5 are yours to answer`.

**Контрольное чтение цели (ответ инструмента ≠ факт):** `base.ref = main`, `state=MERGED`, merge-коммит **`59960189c9f7bacbf570bcbb501c6bbc796b8ef1`**; `git fetch origin main` → голова `origin/main` = `5996018` с тем же заголовком. Событие merge приехало в тред само (`2026-08-28T20-46-33Z-github.md`, `merged-pr: 91`, by maysway → `main`) — сходится с моим чтением. Ветка PR удалена (сообщение `gh` «could not determine current branch» — это провал удаления ветки из detached-воркдерева, а не провал merge; ref на удалённом уже отсутствует).

## Хвост открытых PR

`gh pr list --state open` → один #92 (тред 026, `feat/thread-026-migrate-reflow-note`). Прогнал дверь и по нему: `REFUSED` — гард 1 `STOP` (нет вердикта на `7ea4211`), гард 2 `STOP` (`checks=IN_PROGRESS`). Домерживать нечего; он приедет в свой тред своим кругом.

## Состояние треда после merge

**Сделано:** п.1 постановки (в переформулированном виде — самопочинка вердикта версии вместо правки юнита, потому что факты john 20:05Z показали, что юнит настроен верно), п.2 (предупреждение `merge-gate` при диффе, трогающем конфиг), п.3 (замер класса в LLE-контуре — сделан мной, изложен в `2026-08-28T20-41-34Z-curator.md` §4).

**НЕ сделано и адресовано:**

1. **живой ящик не на новом коде** — просьба первой строкой выше; до неё приёмка «живая репетиция без руки» не измерима в поле;
2. **тот же класс в LLE-контуре** — собран целиком замером (тот же исходник `agent-protocol` v0.2.5, та же строка `fail(error.message, 2)` в `cli.ts:804`, тот же юнит с `RestartPreventExitStatus=2`), спусковой крючок там — бамп ПИНА, а не конфига, и починка #91 доедет туда ровно тем действием, которое класс и запускает. Отдать своей рукой отсюда не могу (там своя почта, прогон привязан к 040) — находка передана чатному curator для заведения треда в LLE, текст §4 — первоисточник;
3. **поле** — «неделя без единого „контур лёг после бампа"»; ближайший бамп схемы и будет замером. Тред держу открытым до него.

**Цена парковки называю вслух:** парковка на john замораживает тред целиком до его слова (`delivers`), и снять её докладом роли нельзя. Здесь это осознанно: в треде 040 больше нечему двигаться руками ролей — код влит, находка передана, остальное измеряется временем и одной командой на ящике.

waiting-on → curator (парковка на john: ждём захода на ящик).

