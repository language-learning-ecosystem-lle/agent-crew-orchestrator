---
from: curator
worker: claude-code
session: 794dc332-55db-4b27-96b4-97c14261fe0e
date: 2026-09-02T11:06:36Z
expects: none
waiting-on: curator
priority: low
parked-on: pr:183
park-mover: curator
---

# Такт пришёл на моё же письмо без единого нового факта — чиню это парком: тред ждёт merge #183, и до него здесь двигаться нечему

`thread show --for curator` первой строкой: «unread for curator: none — the last message in the
thread is curator's own letter of 2026-09-02T10:58:34Z». То есть контур поднял роль на тред, у
которого `waiting-on: curator`, а работы в нём нет ни одной: вторая половина приёмки упирается в
чужой предмет. **Это моя ошибка в предыдущем письме** — я оставила ход у себя, объявила `priority:
low` прозой (флагом не поставила) и парка не поставила вовсе, хотя блокер уже был назван. Такт сгорел
на чтении. Ниже — факты, которыми блокер измерен, и парк, который такое повторение закрывает.

## 1. Блокер назван предметом, а не ожиданием: PR #183 треда `070`

`dev-core` открыл починку сегодня, и я прочла её диффом и телом, а не по заголовку:

- **PR #183**, `fix(orchestrator): свой TMPDIR прогона обязан вмещать сокет — короткий алиас на
  длинное имя (тред 070)`, ветка `feat/070-run-tmpdir-socket-budget`;
- голова — **`f61da76619b3eb74893046a07693c03e7a8fb4e9`**, `MERGEABLE`;
- **`checks` ЗЕЛЁНЫЕ на этой голове** — прогон **33622134874**, `pass`, 4m39s (старт
  `2026-09-02T10:57:47Z`);
- **метки `review` на PR НЕТ** (`labels: []`), то есть круга ревьюера на этой голове не было, и
  готовым к кнопке PR не является;
- дифф — шесть файлов: новые `run-tmp.ts` и `run-tmp.process.test.ts`, `cli.ts` (+13/−1),
  `run.process.test.ts`, `docs/protocol-reference.md`, `README` пакета. `agent-protocol.json` не
  тронут.

Существо починки, словами её тела: предел `sun_path` — 108 символов, `tsx` добирает 22
(`<TMPDIR>/tsx-<uid>/<pid>.pipe`), бюджет `TMPDIR` — 86; супервизор, увидев `TMPDIR` длиннее 76,
подставляет ребёнку симлинк `/tmp/aco-<12 hex>` на тот же каталог. Механизм `056` не отменяется.

**Метку и кнопку здесь не трогаю, и это не забывчивость.** #183 принадлежит треду `070`, гард 3
восходит к его ленте, а след гарда 5 пишется в НЁМ; мой прогон привязан ровно к треду `059`. Тред
`070` стои́т в почте `curator` — этот ход у него есть, и метка на зелёной голове `f61da766` — первое,
что там делается. Называю факт здесь только затем, чтобы тот такт не перемерял его заново.

## 2. Чинит ли #183 мой §2 — это ГИПОТЕЗА, и приёмка `059` как раз она и есть

Различать обязательно, иначе мы примем починку по косвенному признаку:

- в `070` и в #183 замерено **`listen EINVAL`**: сокет не биндится, потому что путь длиннее 108;
- в моём прогоне 10:54Z (§2 предыдущего письма) роль умерла на **`mkdir … ENOENT`** — `tsx` не смог
  создать cache-каталог, и команда чтения почты не доехала до CLI. **Роль слепа**, а не «тесты
  красные».

Механизм общий — каталог, названный именем прогона, под `.orchestrator/sessions/`. Симптом разный, и
что короткий `/tmp/aco-<hex>` вылечит ИМЕННО `ENOENT` внутри песочницы — правдоподобно (короткое имя
в общем `/tmp` внутрь песочницы смонтировано скорее, чем длинный путь в каталоге состояния), но это
предположение, и диагнозом я его не подаю. **Проверкой служит сама приёмка этого треда:** если после
посадки #183 роль прочитает свой тред — гипотеза подтверждена этим же прогоном; если снова упрётся в
`ENOENT` — предмет возвращается в `070` замером, а не мнением.

## 3. Парк: `pr:183`, и почему именно он

Жду **кнопку** на #183, а не машину: `pr:` — «решение принято, осталось нажать», `run:` ждал бы
вердикта круга. Снимается парк заголовком `merged-pr: 183`, объявленным ГДЕ УГОДНО в почте
(читатели судят парк по мержам всей почты, не своей ленты), — то есть письмом треда `070`, где эта
кнопка и будет нажата. Снятие не зависит от того, кому я передаю ход: ход остаётся у `curator`, а
парк снимет чужое событие. Никого этот парк не зовёт и ничего не тратит — пара на треде не
поднимается, пока #183 не сядет. Ровно это и лечит сегодняшний пустой такт.

**Кто нажмёт — назван флагом `--park-mover curator`, и дверь права, что спросила.** #183 трогает
`packages/**` и доком власти не является ⇒ кнопка моя, по пяти гардам. Дверью проверяемое возражение
(«парк за событием, которое требует следующего шага ЭТОГО же треда, — замок изнутри», тред `061`)
здесь не срабатывает по построению: следующий шаг живёт в треде `070`, а он стои́т в почте `curator`
своим ходом и поднимается независимо от этого треда. То есть замок отпирается снаружи — другим
тактом той же роли, и парк на `059` эту дверь не держит.

Одновременно ставлю **`priority: low`** флагом (в прошлый раз он был только прозой): даже после
снятия парка этот тред уступает очередь тем, у кого работа своя.

## 4. Что делает СЛЕДУЮЩИЙ такт на `059` — рецептом, а не намерением

Парк снимется мержем #183; тогда, в один такт:

1. **завести тред-носитель приёмочного прогона** — `038` закрыт, а прогон роли требует треда,
   который её ЖДЁТ (на закрытом `handoff` ловится за 19 секунд и `exitCode 0` не значит ничего).
   Свободный номер на 2026-09-02T11:1xZ — **`073`** (`072-ci-outcome-not-delivered` занят). Заводить
   `new-thread` из связанного прогона **законно** — слово john 2026-09-02 (чат ~10:29Z, доставлено в
   тред `061` письмом `2026-09-02T10-30-18Z-curator.md`), условие одно: происхождение — роль,
   тред-источник, дата, чем дано право — первой строкой первого сообщения;
2. **прогон моей рукой, ровно в объявленных параметрах:**
   `orchestrator run --ref origin/main --role pilot-codex --thread <новый> --root <mail> --write`,
   **без `--model` и без `--effort`** (право `launch-params` роли не дано, и подменять им приёмку
   нельзя). `--ref origin/main` обязателен: рабочий чекаут может стоять на чужой ветке, и приёмка из
   него измерила бы не то дерево;
3. **читать два предмета:** баннер — источник обоих рычагов должен быть `(role)`, `(flag)` означает,
   что мерили не карточку; и пять пунктов постановки 30.08 — они в треде `038`, письмо
   `2026-08-30T13-52-34Z-curator.md`, §3–§4;
4. закрыть `059` по факту прогона — или вернуть предмет в `070`, если роль снова слепа.

Тред-носитель я НЕ завожу сейчас намеренно: он ждал бы того же события, что и этот тред, и мы
получили бы два такта на одно снятие парка вместо одного.

## 5. Что в этом такте НЕ делалось

Ни одного файла репозитория я не тронула: чтения, `gh` по #183 и это письмо. Незакоммиченного нет,
рабочее дерево `curator` чистое. Чужих PR не мёржила и меток не вешала — у каждого свой тред, гард 3
восходит к его ленте, след гарда 5 пишется в неё же.

`parked-on pr:183` · `priority: low` · `waiting-on → curator`.

