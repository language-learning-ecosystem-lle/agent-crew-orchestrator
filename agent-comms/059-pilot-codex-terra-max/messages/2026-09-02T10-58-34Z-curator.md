---
from: curator
worker: claude-code
session: d8af2187-e222-400f-b048-b7905ae4a9a3
date: 2026-09-02T10:58:34Z
expects: none
waiting-on: curator
priority: low
---

# Живая приёмка: пара из карточки ПРИНЯТА замером, вторая половина приёмки НЕ снята — роль слепа под сессионным `TMPDIR` (предмет треда `070`)

Прогон сделан моей рукой сегодня, `10:54:15Z → 10:54:56Z`, лог —
`.orchestrator/sessions/2026-09-02T10-54-15Z-pilot-codex-038-pilot-codex-live-run.log`. Ниже
разделено то, что замерено, от того, что замерить не удалось, и от моей собственной ошибки в выборе
треда прогона.

## 1. Что принято — пара «модель × усилие», и принята она словом баннера, а не памятью

Команда — ровно объявленная в §4 постановки, **без `--model` и без `--effort`**:

```
orchestrator run --ref origin/main --role pilot-codex --thread 038-pilot-codex-live-run \
  --root <mail> --write
```

Баннер прогона, строкой:

```
agent-protocol: agent — codex (role) · exec codex (kind) · account codex-main (role, /home/lle/.codex)
              · model gpt-5.6-terra (role) · effort max (role)
```

**Источник у обоих рычагов — `(role)`, `(flag)` нет ни у одного.** Постановка ждала слова `(card)` —
дверь печатает `(role)`; это одно и то же утверждение («значение пришло из `launch.agent` роли в
конфиге, а не из флага руки»), и приёмочный смысл §4 держится: мерили карточку. Слово `(card)` в
постановке было моим, не машинным, и я называю расхождение, а не подгоняю под него вывод.

Рядом, тем же баннером: рабочее дерево роли переведено на `origin/main 31b265bd`, аккаунт
`codex-main`, потолки `idle 180s (role) · wall-clock 900s (role)`, зоны — `no write ban`.
`park-lifted` нечего снимать, парка на этом треде нет.

## 2. Что НЕ снято: пять пунктов постановки. Роль не смогла прочитать тред вовсе

Прогон закончился `completed`, `exitCode 0` — и это ровно тот случай, когда код выхода ничего не
говорит. Роль выполнила предписанную ей команду чтения почты, и та **умерла до CLI**:

```
/bin/bash -lc 'node --import tsx packages/agent-protocol/src/cli.ts thread show … --thread 038-pilot-codex-live-run --for pilot-codex'
→ Error: ENOENT: no such file or directory, mkdir '/home/lle/…/.orchestrator/sessions/2026-09-02T1…'
   (вывод обрезан codex'ом на 1386 символах — полного пути в логе нет)
```

`TMPDIR` этого прогона лог называет полностью:
`/home/lle/projects/agent-crew-orchestrator/.orchestrator/sessions/2026-09-02T10-54-15Z-pilot-codex-038-pilot-codex-live-run.tmp`.

Собственный доклад роли, дословно: «Тред не прочитан: предписанная команда остановилась до CLI
(`tsx` не смог создать cache-directory в отсутствующем `.orchestrator/sessions/.../tsx-1000`,
`ENOENT`). Запись не предпринималась.»

**Это тот же механизм, что уже заведён тредом `070-session-tmpdir-breaks-tests`** (`sessionTmpPath`,
PR #172, тред `056`), но проявление ДРУГОЕ и оно сильнее того, что записано в постановке `070`:

- в `070` замерено «процессные тесты роли падают 36 из 36 по `listen EINVAL`» — портится инструмент
  приёмки;
- здесь — **роль не может прочитать свою почту**: команда, которую ей даёт сам промпт запуска, не
  доходит до CLI. Портится не приёмка, а работа.

**Чего я НЕ устанавливаю, и это не формальность.** Каталог `.tmp` этого прогона сейчас на диске
отсутствует — но это ничего не доказывает: из 630 сессий `.tmp` есть у 3, и две из них живые, то есть
каталог убирается при освобождении лизы. При этом лог перечисляет содержимое этого `TMPDIR`
(`codex-bwrap-synthetic-mount-targets-1000`), то есть СНАРУЖИ песочницы каталог существовал.
Почему `mkdir` внутри `sandbox-read-only` получил `ENOENT` — гипотеза (путь не смонтирован внутрь), и
диагнозом я её не подаю. Замер принадлежит `dev-core` и треду `070`; я его туда не несу только
потому, что прогон привязан ровно к одному треду — **следующая сессия `curator` обязана перенести
этот факт в `070` своими словами**.

## 3. Моя ошибка в постановке прогона — называю сама

Я подняла роль на треде `038-pilot-codex-live-run`, а он **ЗАКРЫТ и стои́т `waiting-on: —`** (я же его
и закрыла 30.08, и в том же письме написала, что настоящей задаче роли нужен НОВЫЙ тред). Следствие
измеримо: `handoff-detected` пришёл через **19 секунд** после `launch` — до того, как роль сделала
хоть что-то, — потому что сигнал завершения читается как «ход на этом треде не у роли», а он там ни у
кого. Прогон засчитан `completed` при нулевой доставке.

Дефектом контура я это не объявляю: у ручного подъёма роли на тред, который её не ждёт, сигнал
завершения вырождается по построению, и выбор треда был мой. Но факт называю, потому что он делает
`exitCode 0` этого прогона нечитаемым как приёмку, и потому что повтор приёмки **должен идти на новом
треде**, а не на этом.

## 4. Один факт в пользу пары, который дал даже сорванный прогон

Роль, упёршись в стенд, **доложила отказ точно и не сочинила работы**: назвала команду, назвала
ошибку, сказала «запись не предпринималась». В том же положении прогон 8 на `gpt-5.4-mini`/`low`
цитировал шапку постановки так, будто прочёл её. Это не приёмка (пять пунктов не сняты), но это
поведение ровно того класса, ради которого пара менялась.

Цена прогона: `input 35755` (cached 27136), `output 942`, `reasoning 674`. С числами 30.08
(`76745` / `62111`) **не сравнивается** — тот прогон работал, этот умер на первой команде.

## 5. Тариф — отказа вендора не было

Модель ответила, отказа по тарифу нет ни строкой. Договорённость 30.08 остаётся в силе целиком:
первый же отказ по тарифу докладывается john дословной строкой отказа, и роль на этом
останавливается. Доступность `terra` под нагрузкой этим прогоном по-прежнему не проверена — она и не
покрывалась.

## 6. «Смёрженное ≠ работающее» — проверила, и это едва не испортило замер

`origin/main 31b265bd` несёт пару `gpt-5.6-terra`/`max` (`protocolVersion` 25, не двигался). А
**рабочий чекаут `/home/lle/projects/agent-crew-orchestrator` стои́т на ветке
`core/gate-checks-from-actions`, и `agent-protocol.json` в нём всё ещё говорит
`gpt-5.4-mini`/`low`**. Мой прогон чист потому, что шёл `--ref origin/main`, а дерево роли было
переведено на `origin/main 31b265bd` самой командой. Называю явно: приёмка, снятая из рабочего
чекаута без `--ref`, измерила бы старую пару и выглядела бы при этом нормально.

## 7. Что берёт следующая сессия

1. **Перенести факт §2 в тред `070`** — «роль слепа», а не только «тесты красные»; это меняет цену
   предмета, а `070` про неё и заведён;
2. **после посадки починки `070` — повторить приёмку пяти пунктов**, на **НОВОМ** треде (`038`
   закрыт), той же командой без `--model`/`--effort`;
3. **тред `059` оставляю ОТКРЫТЫМ** и ставлю ему `priority: low` — половина приёмки не снята, но
   двигать её раньше `070` бессмысленно: она упрётся в тот же `ENOENT`. Парковку не ставлю: `pr:`/`run:`
   тут нечему соответствовать, а парковка с `expects none` — это заморозка, которая никого не зовёт.

## 8. Состояние моей руки

Ни одного файла репозитория за этот ход я не изменила: чтения, один прогон и это письмо.
Незакоммиченного нет, рабочее дерево `curator` чистое. Право `launch-params` не тратилось — флагов
модели и усилия в прогоне не было.

waiting-on → curator.

