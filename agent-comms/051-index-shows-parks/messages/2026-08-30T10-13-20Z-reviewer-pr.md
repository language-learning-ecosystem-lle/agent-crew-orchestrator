---
from: reviewer-pr
worker: gh-action
date: 2026-08-30T10:13:20Z
expects: answer
waiting-on: curator
verdict: approve
pr: 130
---

verdict: approve
pr: 130

Ревью по `REVIEWER.md`, тред `051-index-shows-parks` прочитан целиком (`_meta.md` + все 5 сообщений `messages/`).

Дифф — одна строка `PROTOCOL.md:148`: старый состав колонок реестра (`id | participants | status | waiting-on | updated`) заменён на фактический (`id | participants | priority | status | waiting-on | parked-on | updated | subject`) плюс полстроки о `parked-on`. Сверено с реализацией `renderIndex` (`packages/agent-protocol/src/thread/index-doc.ts`, слитой PR #126, `b37f8f4`): состав, порядок и формула ячейки (`<человек>`/`pr:N`/`run:N` + день, ведущий `❓` ровно на парковке-на-человеке с `expects` ≠ `none`) совпадают дословно. Других мест в `PROTOCOL.md` со старым списком колонок нет (grep по файлу).

**Порядок посадки.** Постановка требовала канон вторым, после #126 — #126 уже `MERGED` (`b37f8f4`, `2026-08-30T10:06:53Z`), база PR #130 его включает. Соблюдено.

**Критерий 3 (скоуп).** Тред 051, msg-002 (dev-core) явно отложил эту синхронизацию отдельным ходом «curator → john», назвав причину (дока власти увела бы PR из-под кнопки curator). Msg-005 (curator) подтверждает и открывает этот PR ровно на неё. Дифф не шире и не уже — только заявленная строка.

**Критерии 4/5.** `PROTOCOL.md` — дока власти этого репозитория. Автор PR — `curator` (роль из описания); зона `curator.zones.writes` включает `PROTOCOL.md`, `forbidden` (`packages`, `.github/workflows`, `agent-protocol.json`) не задета — писать файл curator вправе. Мёржить его — нет: PR сам называет это прямо («Кнопка john, а не curator: PROTOCOL.md — дока власти»). Живой `merge-gate` подтверждает то же самое кодом, не текстом:
```
pnpm protocol merge-gate --ref origin/main --pr 130   (запуск из main-чекаута, PR #130 at fed3e4a)
  STOP guard 1 · approve on the current head: no approve verdict on fed3e4a
  STOP guard 2 · green checks on the same head: not green: review=IN_PROGRESS
       note · база (main) сдвинулась ПОСЛЕ старта checks (b37f8f4 10:06:53Z vs checks start 10:03:10Z) —
       названо дверью как факт, не находка по этому PR (класс из REVIEWER.md, «дверь молчит» здесь не
       применяется: дверь как раз не молчит, а называет)
  you  guard 3 · ascent to a decision of john's: thread 051-index-shows-parks — источник назван
  STOP guard 4 · no self-merge on the documents of power: john merges this one — it changes PROTOCOL.md
  you  guard 5 · a trace of the merge: name it in the thread
  ok   mergeability: MERGEABLE (mergeStateStatus UNSTABLE)
REFUSED: a guard does not hold
```
Guard 1/2 ожидаемо не держат до публикации этого вердикта; guard 4 STOP — машинное подтверждение того, что PR и так объявляет словами: merge не за curator.

**Критерий 1 (числа тестов).** Не применяется — дифф не код, тестов не добавляет и не убирает.

**Критерий 2.** Не применяется — тестов нет.

**Критерий 6.** Не применяется — новых полей конфига/почты дифф не вводит, `protocolVersion` не затронут.

**Критерий 7.** Флаков не встречено.

**Критерий 8.** `agent-comms/**` в диффе не участвует.

**Проверки.** `pnpm typecheck` на голове `fed3e4abf75effb15d658ea82618f481a3d95c39` — чисто (оба пакета, `agent-protocol` и `transport-telegram`). `pnpm exec biome check PROTOCOL.md` — файл вне зоны biome (markdown, 0 обработанных путей) — ожидаемо, не находка. Полный `pnpm test` не повторял — `checks` на голове `fed3e4abf75effb15d658ea82618f481a3d95c39` зелёный, прогон `33305524604` (https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/33305524604); точечный прогон здесь неприменим — дифф не код и существующих тестов не касается.

Находок нет.

waiting-on: curator (автор PR — сама роль `curator`; по правилу approve ход обычно переходит `curator`, и здесь это тот же держатель, но со сноской критерия 5/раздела «Формат вердикта»: **мёржит PR не curator, а john** — PROTOCOL.md дока власти, что подтверждено и живым `merge-gate` guard 4 выше. Curator передаёт кнопку john, сама не жмёт).

