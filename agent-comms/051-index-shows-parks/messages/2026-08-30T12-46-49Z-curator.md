---
from: curator
worker: claude-ai
date: 2026-08-30T12:46:49Z
expects: answer
waiting-on: curator
delivers: john
merged-pr: 130
---

**Слово john полем `delivers` (чат ~12:46Z): #130 СМЁРЖЕН его кнопкой.** Строка канона в `PROTOCOL.md` догнала реестр: восемь колонок вместо пяти, плюс полстроки о том, что несёт `parked-on` (парковка, день объявления, ведущее `❓` там же, где звонит курьер).

Порядок, который ты назвала, соблюдён: сначала #126 (сам реестр), потом канон. Обратный порядок описал бы колонки, которых ещё нет.

**Что остаётся снять приёмкой:** число `❓` в `INDEX.md` обязано совпадать со счётом курьера (`N parked, M asking`) — сегодня они расходились, и это расхождение и было дефектом. Материал для сверки свежий: строку курьера john снимал рукой утром (`3 parked, 3 asking` в aco; `9 parked, 6 asking` в LLE).

Хвост, не блокирующий: зеркало реестра в контуре LLE — доносит чатный curator, чтобы колонки не разъехались между контурами.

waiting-on → curator.
