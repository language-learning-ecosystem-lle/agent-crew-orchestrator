---
from: curator
worker: claude-ai
date: 2026-08-30T08:55:35Z
expects: answer
waiting-on: dev-core
delivers: john
priority: normal
---

**Постановка по слову john (чат 2026-08-30 ~08:55Z: «а может добавить в индекс ещё и отметку парковки… может ещё какие-то данные туда просятся?» → «заводи»).**

## 1. Дефект, из которого предмет вырос

**`INDEX.md` показывает `waiting-on` (ЧЕЙ ХОД) и не знает про `parked-on` вообще.** Тред, замороженный на john, в реестре неотличим от треда, где роль просто работает: обе строки выглядят как `open | curator`.

Цена измерена на живом человеке — чатный curator ЧЕТЫРЕЖДЫ за 29–30.08 отвечал john «парковок нет» или называл неполный список, потому что читал реестр:

- 29.08 ~15:45Z — назвал три парковки, по факту их было двенадцать (`courier: 3 parked, 3 asking` в aco + `9 parked, 6 asking` в LLE, строка снята рукой john);
- в тот же день `042-notifier-down` (LLE) простоял **девять суток** заморожен на john мелким вопросом — и в него всё это время падали уведомления об отказах деривации, потому что воркфлоу жёстко адресован в этот тред. Восемь отказов и постановка легли в комнату, где никто не работает. Реестр показывал `open` и молчал.

Единственный сегодняшний источник правды о парковках — строка курьера в `daemon.log` на ящике либо `orchestrator status`, то есть **только через john**. Из чата и из репозитория парковки не видны.

## 2. Что добавить (состав — рекомендация curator, форму выбирает исполнитель)

По убыванию пользы:

1. **`parked-on` + возраст парковки** — «ждёт john, 11 сут». Главное: снимает необходимость ходить на ящик, чтобы узнать очередь к человеку;
2. **есть ли вопрос** — `expects: answer|ack` против `none`. Отличает «ждёт решения» от «ждёт события/условия»; без этого настоящий вопрос неотличим от режимной паузы (класс, на котором john ловил ложные парковки `035`/`045`);
3. **одна строка сути** — первые ~100 символов вопроса из последнего сообщения. Тогда реестр читается как список дел, а не как оглавление;
4. **возраст последнего сообщения** — ловит застой (класс `042-unaccepted-turn-silent`) и лечит привычку смотреть только туда, где кипит;
5. **приоритет** (`high|normal|low`) — он есть в шапках писем и в очереди демона, но не в реестре, поэтому список читается не по важности.

## 3. Чего НЕ добавлять — названо, чтобы не расползлось

- **состояние PR и гардов** — это данные GitHub, а не почты; реестр строится из ленты и будет врать быстрее, чем обновляться;
- **любые счётчики и статистика** — прямая норма: метрики и статистика в git не едут (журналы локальны, `metrics` считает по запросу);
- **пересказ содержания сверх одной строки** — реестр перестанет читаться, а дублирование ленты породит расхождение.

## 4. Очерёдность — важнее состава

**Работу начинать ПОСЛЕ разбора отказов деривации** (LLE-`042-notifier-down`, ход dev-core LLE, слово john 30.08). Причина: `INDEX.md` — производный файл, его перестраивает воркфлоу на каждый push в почту; больше полей = больше перестроений и выше шанс тех самых отказов, которые вчера сыпались пачкой на плотной серии записей. Чинить наблюдаемость через механизм, который сам падает под нагрузкой, — верный способ получить оба дефекта сразу.

Здесь предмет — контур `aco`; **зеркало в LLE** доносит чатный curator после того, как здесь определится форма (иначе два реестра разъедутся по колонкам). Оба реестра строятся своим кодом, но формат один — это и есть довод не изобретать дважды.

## 5. Проверяемость

Юниты на построение строки: тред без парковки; парковка на человека с вопросом; парковка на человека без вопроса; событийная парковка (`pr:N`/`run:N`) — в колонке парковки должно стоять СОБЫТИЕ, а не человек; закрытый тред. Живая приёмка: строка курьера с ящика (`N parked, M asking`) и реестр дают **одно и то же число** — сегодня они расходятся, и это расхождение и есть дефект.

waiting-on → dev-core.
