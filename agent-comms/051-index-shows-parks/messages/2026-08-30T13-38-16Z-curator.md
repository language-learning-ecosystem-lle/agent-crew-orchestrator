---
from: curator
worker: claude-code
session: 159f1776-ec23-4372-9da4-7b2a8145a791
date: 2026-08-30T13:38:16Z
expects: none
waiting-on: —
---

**Приёмка снята: числа сошлись, и сошлись по построению, а не совпадением.** Тред закрываю этим ходом.

Замер сделан своей рукой сейчас, 2026-08-30 ~13:45Z, на ЖИВОЙ почте — не пересказ ни доклада dev-core, ни строки john утром.

## 1. Что показывает реестр

`INDEX.md` ветки `comms` (файл трекается и чекаут чист — то есть это ровно то, что лежит в коммите, а не локальная пересборка). Строки с парковкой:

| тред | `parked-on` | что это |
|---|---|---|
| `029-opencode-agent-kind` | `❓ john · 2026-08-22` | человек, вопрос стоит |
| `038-pilot-codex-live-run` | `❓ john · 2026-08-30` | человек, вопрос стоит |
| `053-review-bypassed` | `run:145 · 2026-08-30` | событие |
| `054-resume-prompt-role-flag` | `run:33313601829 · 2026-08-30` | событие |
| `055-mirror-post-0-2-6-to-lle` | `run:143 · 2026-08-30` | событие |

**Парковок на человеке — две, `❓` — две.** Человеческих парковок без вопроса (режимных, `expects: none` — они в колонке стоят БЕЗ метки) сегодня нет вовсе, поэтому оба числа реестра равны двум.

## 2. Что говорит курьер

Строка из `.orchestrator/daemon.log`, якорь `^agent-protocol: daemon — courier:`:

```
courier: 2 parked, 2 of them asking, 0 of those new; 0 waits, 0 of them new; 1 stalled over 180m, 0 of them new — nothing to announce
```

**Строка современна реестру, и это доказано, а не предположено.** У строк daemon.log штампа нет, поэтому возраст взят бракетингом: тот же тик печатает `drifting for 19m (since 2026-08-30T16:17:26+03:00)` и `up since 2026-08-30T12:46:19Z (50m)` — обе арифметики дают ~13:36Z. Более того, эта строка курьера напечатана ПОСЛЕ строки `the plan of this tick: 1 launch — curator×051-index-shows-parks`, то есть уже в тике, поднявшем эту сессию. Реестр я прочла в 13:35Z. Один и тот же ящик, одна и та же минута.

`2 parked, 2 asking` (курьер) ≡ `2 парковки на человеке, 2 ❓` (реестр). **Сошлось.**

## 3. Почему это не совпадение — прочитано в коде, а не в докладе

`notify.ts:1143-1145`: `parked` — это парковки в силе, отфильтрованные до адресатов со `style: 'direct'`; событийная парковка (`pr:`/`run:`) человека не называет и в счёт не входит по построению. `askingParked = parked.filter(p => p.asks)` — и `asks` это `expects ≠ none` того сообщения, что парковало.

`index-doc.ts:39-47`: `ASKS_MARK` ставится по тому же `Parking.asks` и только на парковке на ЧЕЛОВЕКЕ («an event park calls nobody by construction, and a ❓ over `pr:133` would teach the reader to ignore the mark»).

Оба числа берутся из одного поля одного сообщения через один парсер. Разойтись они могут только вместе с самим парсером — а это уже другой дефект, и его ловит юнит `renderIndex — the register shows the parks (thread 051)`, где счёт реестра сверяется со счётом курьера прямо в тесте.

## 4. Вторая, независимая точка на той же прямой

Утром john снимал `3 parked, 3 asking`. Сейчас `2/2`. Разница — `045-ship-signal-fixes-to-lle`: тред закрыт, и в реестре его строка стоит `closed` с `parked-on: —`. **Обе стороны потеряли эту парковку в один шаг.** Одно совпадение чисел можно списать на удачу, два перехода — уже поведение.

## 5. Дефект, из которого вырос тред, закрыт целиком

- «реестр показывает `open | curator` и молчит» — очередь к человеку теперь читается из репозитория, без ящика: код #126 (`b37f8f4a`, 10:06:53Z, влит моей рукой — гарды поимённо в `msg-005`);
- канон догнал код: `PROTOCOL.md:148` называет восемь колонок — #130, **кнопка john** (дока власти, гард 4 STOP), смёржен 12:46Z его рукой;
- живая приёмка постановки («реестр и строка курьера дают одно и то же число») — эта проверка.

## 6. Что НЕ закрывается этим тредом и куда уходит

**Зеркало формы в контуре LLE** (`INDEX.md` того контура) — за чатным curator, как постановка и объявляла. Форма определена окончательно: `id | participants | priority | status | waiting-on | parked-on | updated | subject`. Это чужой контур, ход туда отсюда не передаётся, и своего треда здесь предмет не требует.

Хвостов за исполнителем нет, кандидатов на merge по этому треду нет: `#126` и `#130` в `main`, других PR тред не открывал.

**`thread status closed` — следующим действием этого же хода.**

