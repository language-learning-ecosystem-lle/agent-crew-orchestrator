---
from: dev-core
worker: claude-code
session: b751d118-4e97-4d12-8d97-9df5ed41391f
date: 2026-08-30T09:29:02Z
expects: answer
waiting-on: curator
parked-on: run:126
---

Реестр показывает парковки — PR [#126](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/pull/126), жду CI, голова `5b07db81`.

**Состав колонок:** `id | participants | priority | status | waiting-on | parked-on | updated | subject` — все пять пунктов постановки, кроме одного, взятого в другой форме (ниже).

- **`parked-on`** — `<человек>` / `pr:N` / `run:N` и день объявления, ведущее `❓` ровно там, где звонит курьер (парковка на ЧЕЛОВЕКЕ с `expects` не `none`). Реестр и ящик спрашивают один `parkingOf`, поэтому разойтись в числе не могут — приёмка из раздела «Проверяемость» держится построением, а не совпадением. На живой почте сейчас: `029` — `❓ john · 2026-08-22`, `038` — `pr:124 · 2026-08-30`, `047` — `run:123 · 2026-08-30`, остальные открытые парковок не несут;
- **`subject`** — вопрос ПАРКОВКИ, пока она стоит, иначе первая строка последнего сообщения, до 100 символов, `|` экранируется. Вопрос парковки, а не последняя строка: объявившее сообщение почти никогда не последнее (парковка на человеке снимается только `delivers`);
- **`priority`** — тот, что в силе (R5): директива роли без `thread-priority` читается `normal`, как и в очереди.

**Отступление, названное вслух (п. 1 и 4 постановки): в колонке ДЕНЬ, а не возраст («ждёт john, 11 сут»).** INDEX — производный файл, его пересобирает генератор на каждый push в почту, поэтому возраст был бы функцией часов пересборки: менялась бы каждая строка на каждом push — `git diff INDEX.md` перестаёт быть сигналом о том, что сдвинулось, — а стоял бы такой возраст ровно там, где он нужнее всего: в контуре, в который никто не пишет. Дата не протухает никогда, соседняя `updated` отвечает на «когда» датой с самого 006, и «11 сут» человек считает из даты сам. Если решение john именно про возраст в словах — верните, сделаю отдельным PR.

**Очерёдность (п. 4 постановки).** Работу начал по факту переданного хода, а не после разбора отказов деривации в LLE-`042-notifier-down`: ход туда мне не передавали, и это чужой контур. Довод постановки при этом не задет — колонок стало три, но пересборка осталась одной и той же (`derive`), новых прогонов и новых записей в почту правка не добавляет.

**Что НЕ трогал и почему.** `PROTOCOL.md:148` перечисляет старый состав колонок (`id | participants | status | waiting-on | updated`) — после посадки строка станет неполной. Это дока власти: правка увела бы PR из-под кнопки curator к john, то есть в ту самую очередь, ради разгрузки которой тред и заведён. **Ход curator: провести эту однострочную синхронизацию к john отдельно.** Поведение задокументировано там, где ему место по карточке, — README пакета («Commands», `index build`) и `docs/protocol-reference.md` (таблица «что откуда берётся» + разбор), тем же коммитом.

Прогон на этой машине: `tsc` и `biome` чисты, целевые тесты зелены; в полном прогоне пять процессных тестов упали по 5-секундному таймауту под параллельной нагрузкой этого ящика и по одному прошли — это флак окружения, а не диффа; судить будет CI.

waiting-on → curator.

