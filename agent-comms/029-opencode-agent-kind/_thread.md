# OpenCode как agent.kind — реализация поддержки в протоколе

participants: curator · dev-core · john · status: open

## msg-001 · from: curator · 2026-08-22 · expects: answer

**Тред заведён по слову john (чат ~10:38Z) и СТОИТ НА ПАРКОВКЕ его же словом — работа не стартует до его отдельного go.** Горизонт, не сегодняшняя очередь.

## Предмет: OpenCode как значение `agent.kind` — второй/третий исполнитель ролей рядом с claude-code

OpenCode — открытый (open-source) терминальный кодинг-агент с headless-режимом и поддержкой разных LLM-провайдеров. Для протокола он интересен тем же, чем Codex (тред 026), плюс своим: провайдеро-независимость (можно гонять роли на любой модели через один kind), открытый код (поведение харнесса проверяемо, а не угадываемо), потенциально дешёвые/локальные модели для лёгких ролей.

## Контур работы (для будущей постановки, НЕ для исполнения сейчас)

1. **Общий фундамент с 026-codex — сначала он**: инвентаризация claude-code-допущений и интерфейс исполнителя (спавн, стрим-парсер, признаки исхода/handoff, rate-limit-семантика per-kind, doctor-пробы, резолв бинаря/учёток) — ОДИН для всех kind; 026 и 029 делят его, и кто первый пойдёт в работу — тот фундамент и строит, второй приходит на готовое;
2. **OpenCode-специфика**: формат событий его headless-режима (стрим/JSON — снять с реальных прогонов, не с README), карта завершений на наши исходы (handoff / exited-without-handoff / external), конфиг модели-провайдера per-role (какая модель у какой роли — поле карточки или machine config — развилка постановки), слэш-команды/скиллы — есть ли аналог и нужен ли;
3. **Роль-пилот** — лёгкая небоевая роль (кандидаты обсудим при go), doctor для kind, паритетный замер: подъём → ход → письмо → передача;
4. **Вопросы john до старта** (лягут в постановку): какая модель/провайдер и чей ключ (деньги!), на каком ящике, что пилотирует, приоритет против продуктового потока и против 026 (порядок: codex-первый / opencode-первый / фундамент отдельно).

## Почему парковка

Слово john при заведении: тред стоит видимым, не жжёт ни попыток, ни квоты. Снятие — его сообщением здесь или доставкой через чатного curator.

waiting-on → curator, parked-on john.
