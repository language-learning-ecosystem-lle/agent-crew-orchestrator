---
from: curator
worker: claude-code
session: 96bbb763-fbb5-460d-b5cf-370d3fac9d7d
raised: 2026-09-14T14:50:49Z
date: 2026-09-14T14:54:56Z
expects: ack
waiting-on: curator
parked-on: john
---

**Требуется решение john: ОДИН ремонт написан ДВАЖДЫ — #436 (тред 201) и #433 (тред 205) делают одну и ту же правку в одном файле и КОНФЛИКТУЮТ в обоих своих файлах. Ни один не надмножество другого. Какой из них дом, и чей остаток дописывается вторым? Пока ты не сказал — с моей стороны под твою кнопку готова пара #431 → #436, и #436 несёт #431 в себе.**

Замер мой, `14:51–14:55Z`: `gh pr view` по четырём PR, `git merge-tree` по головам, `gh run view --log` по счёту состояний сюиты. Доклад dev-core (msg-005) проверен, а не пересказан, — и он держится (§3).

## 1. Развилка: два PR на один ремонт

Твоё слово `13:27Z` («ДЕЛАЙ ОБЕ ПОЛОВИНЫ») было исполнено в ДВУХ тредах независимо. Я поставила вторую половину здесь (msg-004 §3) и ленты 205 при этом не читала — это мой промах, и он назван: перечислять `gh pr list` ДО постановки я обязана была, а не после доклада исполнителя.

| | **#433** (тред 205) | **#436** (тред 201) |
| --- | --- | --- |
| голова | `e12a407128889dcde398e68d6d3a6162e70a06cf` | `06bc195e2ee305358908145e28938e070f77eda2` |
| `checks` | `success`, прогон `34851787658` (`13:58Z`) | `success`, прогон `34857241207` (`14:50Z`) |
| merge-state | `CLEAN` | `CLEAN` |
| база ветки | `main` | голова **#431** (`3cd64e456`) — несёт и его дифф |
| `continue-on-error` | на **обоих** шагах: `reviewer` (261) и `reviewer_fallback` (704) | только на `reviewer` (257) |
| печать исхода основного шага | **нет** | **есть**: `PRIMARY_OUTCOME` строкой `env:` шага итога, печать ДО раннего выхода |
| `docs/protocol-reference.md` | **+56 строк**, раздел «Цвет круга ревью решает ДОСТАВКА вердикта» | не трогает |
| сюита доставки | состояния (23)–(26), в CI **27 шт.** | состояния (25)–(27), в CI **28 шт.** |

**Конфликты — замер `git merge-tree --write-tree --name-only` по головам, не догадка:**

- `pr433 × pr436` → **CONFLICT в ОБОИХ**: `.github/scripts/review-delivery.integration.sh` И `.github/workflows/claude-review.yml`;
- `pr431 × pr433` → CONFLICT в `review-delivery.integration.sh` (`.yml` слился сам) — это уже замерено тредом 205 и записано в #434 §4;
- `main × pr436` → конфликта нет.

## 2. Что уникально у каждого — и чего это стоит вторым заходом

**Только у #436:** печать исхода основного шага (`PRIMARY_OUTCOME`) в шаге итога. Это ровно твоя граница №2 — «смерть основного шага молча не съедается». У #433 её нет: `outcome` он тоже не глушит и вето `PRIMARY_OUTCOME` шага `limit` сохраняет, но словом в лог исход не выносит.

**Только у #433:** (а) `continue-on-error` на ЗАПАСНОМ шаге; (б) раздел в `docs/protocol-reference.md`.

Пункт (а) — не украшение и не совпадение: **dev-core в 201 эту границу назвал и НЕ перешёл** осознанно (msg-005 §3), а в 205 перешёл, назвав довод вслух («запасная умеет кончиться `is_error:true`, успев доставить вердикт»). Довод я не оспариваю, но он **выведен, а не замерен**: прогона, где умирает запасной шаг поверх доставленного вердикта, нет ни у кого. Твоё же правило `10:53Z` — возвращаться с замером, а не с рассуждением.

**Цена не нулевая ни в какую сторону, и она в сюите, а не в `.yml`:** #436 пиньит сверку «запасной шаг ревьюера цвет джобы судит = **да**» (строка 129 его диффа) — именно для того, чтобы расширение границы краснило, а не проезжало молча. Значит остаток (а), приехав вторым, обязан эту сверку ПЕРЕВЕРНУТЬ, и это видимая правка, а не тихий доклад. Обратно: если домом станет #433, вторым заходом дописывается печать `PRIMARY_OUTCOME` поверх.

## 3. Доклад dev-core проверен — держится

Не цветом джобы, а тремя оракулами:

- **счёт состояний В CI** (правило из #434 §3: сюита умеет молча пропустить состояния и остаться зелёной): `gh run view 34857241207 --log | grep -a 'СОСТОЯНИ'` → `ВСЕ СОСТОЯНИЯ ПРОШЛИ — 28 шт.` — столько же, сколько исполнитель намерил рукой, то есть новые состояния проехали в CI;
- **ловушка читателей `conclusion`** (правило из #434 §1): `grep 'failure()\|job\.status\|\.conclusion'` по `.yml` головы `06bc195e2` → **одно** попадание, и оно не про шаги (строка 193, `jq` по `.jobs[].conclusion` ЧУЖОГО прогона в переписи `checks`). Читателей, которых `continue-on-error` переворачивает, в файле нет;
- **удаления**: `+324 −4`, и все четыре удалённые строки — модификации на месте (сигнатура `letter_case()`, номер `CURRENT_STATE`, строка `jq` из диффа #431). Снятия или сужения запрета среди них нет.

## 4. Состояние пары под твоей кнопкой (гарды поимённо)

`pnpm protocol merge-gate --ref origin/main --pr 436 --review-workflow 'Claude PR Review'` → `REFUSED`, и оба отказа ожидаемы:

- **гард 1 — `STOP`**, «no approve verdict on 06bc195». Круга не будет ПО ПОСТРОЕНИЮ: дифф правит `claude-review.yml`, действие самопропускается. Метку `review` я на него не вешаю и не вешала. **Гард 1 закрывается твоим чтением, названным здесь поимённо** (кто прочёл, полный SHA, чем подтверждено);
- **гард 2 — `ok`**, `checks=SUCCESS` на той же голове `06bc195e2ee305358908145e28938e070f77eda2`;
- **гард 3** — восходит к твоему слову `13:27Z` (тред `180`, msg-138), оно же основание и у #433;
- **гард 4 — `STOP`**, «john merges this one — it changes .github/workflows/claude-review.yml». **Кнопка твоя, и моей рукой тут не делается ничего**;
- **гард 5** — за тем, кто нажмёт.

**#431** стои́т как стоял: голова `3cd64e4563403d74c107455ab5d74e1ef10b3025`, `checks=success` (`34849266901`), merge-state `CLEAN`, метки нет. Та же пара гардов 1/4, та же кнопка.

## 5. Рекомендация curator (не решение)

**Домом взять #436, порядок #431 → #436, остаток (а)+(б) дописать тредом 205.** Довод — ценой, а не вкусом:

1. #436 несёт твою границу №2 печатью; у #433 её нужно дописывать в любом случае;
2. #436 стои́т НА голове #431, поэтому пара мержится двумя кнопками **без единого ребейза**: после squash #431 содержимое совпадает, и git одинаковую правку с двух сторон не конфликтит. Порядок #433-первым требует ребейза #433 поверх #431 (конфликт замерен) **плюс** отдельной дописки печати — два платежа вместо одного;
3. остаток #433 после #436 — маленький и разделимый: (б) `protocol-reference` конфликта не имеет вовсе, (а) — одна строка плюс переворот одной сверки.

**Чего я НЕ рекомендую и почему:** мёржить #436 «заодно с #431» одной кнопкой. Технически squash #436 втянет и дифф #431, но тогда два разных предмета уедут под одним заголовком, а след #431 (`allowed_warning` — признак лимита) в истории не назовётся. Две кнопки по очереди дешевле одной путаницы.

**Если ты выберешь иначе** — скажи домом #433, и я тем же ходом ставлю dev-core остаток на 201 (печать `PRIMARY_OUTCOME` поверх) вместо остатка на 205. Работа не пропадёт ни в одном исходе; пропадёт только та, которую начнут до твоего слова, — поэтому dev-core я сейчас ничего не ставлю.

## 6. Что на ком

- **john:** развилка §1 + человеческое чтение и две кнопки (#431 `3cd64e456`, #436 `06bc195e2`). Ход у меня, потому что вопрос несу я;
- **dev-core (тред 201):** работы нет до твоего слова — ставить остаток вслепую значит платить за него дважды;
- **тред 205:** #433 стоит открытым и зелёным, ничего не портится;
- **тред 180:** #406 — метка повешена мной в прошлый ход, круг `34851961887`; вердикт едет в 180, не сюда.

**Незакоммиченного за мной нет: в репозиторий этим ходом я не писала ни строки, рабочее дерево роли чистое (`git status --porcelain` пуст в `14:53:40Z`). Действий моей руки над PR за этот ход — ноль: только чтение.**

waiting-on → curator (парковка на john).

