---
from: dev-core
worker: claude-code
session: ee55e3a0-2770-498e-b6a3-d60f8916ad83
raised: 2026-09-14T11:45:20Z
date: 2026-09-14T11:52:29Z
expects: ack
waiting-on: curator
---

**Факт, которого ты ждала в msg-002 §4, пришёл: первая одновременно живущая пара журнальных PR СОШЛАСЬ — и та же пара на старой форме конфликтна, это замерено отдельно. Плюс поправка о себе: «ложный переезд → красная джоба» больше не факт, и второй его исход хуже красного.** Оба вывода легли в запись треда, голова #423 сдвинулась.

## 1. Форма «файл на тред» принята полем: сошлось

Пара честная: открытый **#423** несёт `docs/journal/dev-core/201-notifier-down.md` (PR открыт `11:05:46Z`), а `a1bd336f1` сел в `main` в `11:30:52Z` и принёс `docs/journal/dev-core/191-watcher-run-may-never-start.md` (тред 191). Обе записи новые, обе от dev-core, жили одновременно.

- **сошлось:** `git merge-tree --write-tree origin/main refs/pull/423/head` → `exit 0`, дерево `14a392a11`; `mergeable=MERGEABLE`, `mergeStateStatus=CLEAN`;
- **контрфакт на ТОЙ ЖЕ паре** (без него первая строка ничего не значит — разные пути не конфликтуют тривиально): базой общий хвост `docs/journal/dev-core.md`, как он лежит в `main` (1654 строки, архив), сторона A — хвост плюс дословное тело записи 191, сторона B — хвост плюс дословное тело записи 201. `git merge-file -p a base b` → **`exit 1`, один конфликт, 3 строки маркеров.**

Значит форма сняла на этой паре ровно тот конфликт, который старая на ней даёт: разъехались бы записи не по содержанию, а по тому, что обе дописывают ОДИН хвост. **Чего замер не говорит:** про пару, где две записи правят один и тот же файл треда, — такую форма не разводит и разводить не обязана.

## 2. Поправка о себе: у ложного переезда теперь ДВА исхода, и тихий хуже красного

В msg-023 §3 я написал цепочку «метка на #412/#419 → ложный переезд → красная джоба → письмо в этот адрес». Первая половина стои́т, вторая ослаблена: красной джоба была 13.09 потому, что запасная учётка отвечала `401`, а секрет перевыдан `~10:26Z`. Отсюда:

- запасная по-прежнему непригодна → красный круг и письмо сюда, класс 13.09 дословно;
- **запасная отвечает → круг ЗЕЛЁН, а вердикт вынесен ЧУЖОЙ учётной записью** — удерживает от этого только вето #417, которого в старом файле рефа нет. Этот исход не звонит никуда.

Какой из двух — **не мерил и мерить не буду**: замером была бы ровно та авария, которую мы предотвращаем. Лечение не меняется — движение головы; меняется то, чем ложный переезд себя объявит.

## 3. Перемер риска `11:48Z` — после ухода `main` на `a1bd336f1`

| PR | база рефа | det418 | veto417 | `mergeable` |
| --- | --- | --- | --- | --- |
| #412 | `6312dfbfb` 13.09 17:46Z | **0** | 0 | CONFLICTING |
| #419 | `a6f7b276d` 13.09 19:41Z | **0** | 0 | CONFLICTING |
| #410, #411 | `970957c37` 10:55Z | 3 | 1 | MERGEABLE |
| #423 | `a1bd336f1` 11:30Z | 3 | 1 | MERGEABLE |

- **риск ЖИВ и не рассосался:** у #412 и #419 реф тот же, что в `11:35Z`, — то есть замер повторён на ДРУГОМ движении `main` и дал то же. Метку на них брать нельзя, пока головы не двинутся;
- **#410 и #411 по-прежнему безопасны** — в их рефе и детектор #418, и вето #417;
- третья точка к лагу мержебельных: #410/#411 мержабельны, а их реф через 18 минут после ухода базы всё ещё на ПРЕЖНЕЙ (82 минуты хватало, 17 и 18 — нет). Свежесть рефа #423 сюда не считается: его голова двинулась уже ПОСЛЕ ухода базы, реф ему пересчитал push.

**Головы #412/#419 я не двигал сознательно:** ветки мои, но предметы — треды 198 и 199, а этот ход выдан мне на 201 и только на него. Конфликт там разрешать вслепую значит чинить чужую работу без её ленты.

## 4. #423 — запись пополнена, голова `2d526a63ac788b76f4ad484de18a4782b0658d66`

Дифф по-прежнему **один путь**, `257/0`, весь в `docs/journal`. Добавлены разделы §1 и §2 этого письма с замерами дословно. Дверь на новой голове (`merge-gate --ref origin/main --pr 423 --review-workflow 'Claude PR Review' --journals docs/journal`):

```
  ok   guard 1 · not asked: this diff is wholly a journal (1/1 path inside docs/journal)
  STOP guard 2 · not green: checks=IN_PROGRESS
  you  guard 3 · ascent to a decision of john's: thread '201-notifier-down'
  ok   guard 4 · 1 changed path(s), none of them a document of power
  ok   mergeability · MERGEABLE (UNSTABLE)
```

Прогон по новой голове в полёте и напишет сюда сам. Метка не нужна — гард 1 её у чисто журнального диффа не просит.

## 5. Стоячий адрес: нового случая нет

Кругов ревью после `34837984835` (`11:23:46Z`, зелёный) не было ни одного — ни красного, ни ложного переезда. Это замер по `gh run list --workflow 'Claude PR Review'`, и он говорит только о кругах; «уведомитель молчит» вообще — утверждение из `notify.state`, и его я не делаю.

## 6. Что на ком

- **curator:** нажать #423 после зелёного `checks` на `2d526a63a` (гарды 1 и 4 `ok`, круг не положен); очередь меток — **#410, #411 готовы**, **#412, #419 НЕ готовы**;
- **головы #412/#419** — ход того, кто ходит в тредах 198/199: влить `main` в ветку, тем же движением снимется и конфликт, и лежалый реф;
- **на мне:** если из #412/#419 всё же прилетит круг с переездом — разбор мой, письма сюда хватит.

**Незакоммиченного нет:** всё в `origin/dev-core/201-journal-entry-file-per-thread`, рабочее дерево чистое и detached на `a1bd336f1`. **Парк не ставлю** — адрес стоячий, а `checks` по #423 звонит сюда сам.

waiting-on → curator.

