---
from: dev-core
worker: claude-code
session: 9a9bf4c3-1e5a-4423-ac2c-842347a92ffa
raised: 2026-09-14T14:29:08Z
date: 2026-09-14T14:42:09Z
expects: ack
waiting-on: curator
---

**Постановка §3 исполнена: PR [#436](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/pull/436), голова `06bc195e2`. Цвет джобы круга теперь выносит доставка вердикта, а не жизнь основного шага. Все четыре границы решения john проверяются сверками, а не обещаны прозой; сюита — 28 состояний, `exit 0`, три мутационные пробы, каждая красит своё.**

## 1. Что сделано

**`continue-on-error: true` на шаге `id: reviewer`** — и это единственная строка поведения в диффе. Судья цвета — шаг «Итог доставок» и его `exit "$FINAL_CODE"`, который считал три канала поимённо и до этой правки.

**Вторая половина, без которой первая была бы молчаливым съеданием отказа:** шаг итога получает `PRIMARY_OUTCOME: ${{ steps.reviewer.outcome }}` строкой `env:` и печатает исход основного шага рядом с `REVIEW_MOVED`/`LIMIT_HIT`/`LIMIT_WHY` — **до раннего выхода**, потому что печатать после `exit` значит не печатать вовсе ровно на той ветви, ради которой правка и делается (всё доехало, джоба зелена). `continue-on-error` меняет `conclusion` шага, но не `outcome`: вето `PRIMARY_OUTCOME` в шаге `limit` читает тот же `outcome` и работает как работало, аннотация шага в UI остаётся красной.

**Три пути:** `.github/workflows/claude-review.yml`, `.github/scripts/review-delivery.integration.sh`, `docs/journal/dev-core/201-notifier-down.md`.

## 2. Границы решения john — сверками, не прозой

| граница | чем проверяется |
| --- | --- |
| недоставленный вердикт оставляет джобу КРАСНОЙ | (26): `delivery_exit_code 0 0 none none none` = `1`, свёртка даёт «красная» |
| это перенос значения, а не глушение; смерть шага видима | (27): тело шага итога из `.yml` гоняется с `PRIMARY_OUTCOME=failure`, в логе — `основной шаг ревьюера: failure` и `круг доработала ЗАПАСНАЯ учётка` |
| самопропуск действия остаётся ЗЕЛЁНЫМ (класс треда 046) | (26): `delivery_exit_code 1 0 none none none` = `0` |
| переезд называется вслух и на ветви БЕЗ вердикта | ветка не тронута; печать §1 добавлена РЯДОМ, а не вместо |

**Цвет джобы в сюите считается СВЁРТКОЙ по правилу Actions, а перечень «кто вправе красить» берётся РАЗБОРОМ `.yml`** — не переписанным в сюиту литералом: тест, пересказывающий чужой литерал, пиньит свою копию, а не файл. Код выхода шага итога — вызовом продового `delivery_exit_code` из `review-delivery.sh`.

**Мутационные пробы (три, каждая красит своё):**

- снять `continue-on-error` с `id: reviewer` — **2 провала**, `(25-26)` и `(25)`, первый «основной шаг ревьюера цвет джобы НЕ судит: ожидалось `нет`, получено `да`»; **(26) не тронут** — то есть краснота от недоставки держится отдельно;
- убрать `PRIMARY_OUTCOME` из `env:` шага итога — **1 провал**, `(27)`, пином переменной;
- вынуть печатающий блок из тела шага — **2 провала**, обе сверки лога `(27)`.

Сюита целиком на починенном коде: **28 состояний, `exit 0`**.

## 3. Граница, которую я НЕ перешёл — и почему это названо, а не сделано

**Запасной шаг `continue-on-error` не получает.** Постановка назвала предметом основной шаг, и я её не расширяю. Но граница не оставлена на честном слове: в (25-26) стои́т сверка **«запасной шаг ревьюера цвет джобы судит» = `да`** — расширение границы теперь краснит сюиту, а не проезжает молча.

**Что при этом остаётся открытым как знание** (вывод, не замер): если запасной шаг умрёт ПОВЕРХ уже доставленного вердикта, класс повторится ступенью ниже. Прогона такого у меня нет, и поводом для второй правки он станет полевым входом, а не рассуждением — ровно та форма, которой john закрыл этот вопрос 10:53Z и которой он же его открыл сегодня.

## 4. Дом диффа и цена, которую он несёт

Ветка **от головы #431** (`3cd64e456`), как ты и указала. Следствие названо честно: **пока #431 не влит, дифф #436 несёт и его изменения** — читающему #436 видно оба предмета, а после merge #431 ветку придётся подтянуть. Это дешевле конфликта в том же `.yml` при любом порядке.

## 5. Что на ком

- **curator:** отнести john #436 вместе с #431 — они один предмет в двух шагах, и второй без первого бессмысленен (признак лимита обязан не врать до того, как цвет начнёт зависеть от переезда);
- **john:** человеческое чтение и кнопка. **Метка `review` на #436 не положена по построению** — дифф правит `claude-review.yml`, действие самопропустится; гард 4 краснит PR (`.github/workflows/**`). Гард 1 закрывается человеческим чтением, названным в треде поимённо: голова **`06bc195e2ee305358908145e28938e070f77eda2`**;
- **на мне:** `checks` по #436 в полёте — у прогона не стою и не паркуюсь, адрес стоячий и письмо придёт сюда само. Красный `checks` — мой ход в следующем такте.

**Полевая приёмка — не merge:** код выхода джобы при доставленном вердикте на первом же настоящем лимите. До него цвет проверен только сюитой, и в записи это сказано этими же словами.

**Незакоммиченного нет:** всё в `origin/dev-core/201-red-means-verdict-not-delivered`, рабочее дерево чистое.

waiting-on → curator.

