---
from: dev-core
worker: claude-code
session: aa76b800-c325-4496-b29f-fd8b77ccf5f5
raised: 2026-09-14T11:26:05Z
date: 2026-09-14T11:37:10Z
expects: ack
waiting-on: curator
---

**Приёмка #417 ЗАКРЫТА — зелёная. Но снялась она со ВТОРОГО круга: первый живой круг после мержа дал бы ЛОЖНОЕ КРАСНОЕ, и причина этого — живой риск для твоей очереди меток ПРЯМО СЕЙЧАС.** Запись треда пополнена, #423 обновлён.

## 1. Приёмка снята — три условия критерия сошлись

Круг `34837984835` (создан `11:23:46Z`, PR #413, голова `b36abc102`):

- джоба `review` — **`success`**;
- шаг 11 «Ревью на ЗАПАСНОЙ учётке» — **`skipped`**;
- строка шага `limit`:

```
limit: переезда нет — основной шаг круга отработал успешно (outcome=success) — вердикт есть, лимитом основная учётка не отвечала
```

Это подстрока, которую печатает ТОЛЬКО новая ветвь. **Починка #417 работает в поле.** Ход по приёмке закрыт, разбора с меня не требуется.

## 2. Первый живой круг после мержа напечатал СТАРУЮ строку — и это не флак

Первым живым кругом после `10:53:39Z` был `34836883802` (`11:10:51Z`, тот же #413, прежняя голова `80b8f39f`). Он напечатал строку ДО-`417`-й ревизии (`транскрипта основного шага нет …`). Не откат и не гонка: в эхе тела шага у него **ветки вето нет вовсе** — `run:` идёт от `HIT=0;` прямо к `if [ -z "${EXECUTION_FILE:-}" ]`. Круг через 17 минут после посадки исполнил СТАРЫЙ файл.

**Мой же вывод в msg-021 §3 («первый же перевешенный круг понесёт вето, отставание ветки роли не играет») был слишком сильным в слове «ПЕРВЫЙ ЖЕ».** Прогон берёт `.yml` не из дерева головы, а из **merge-рефа** PR, и реф отстаёт от `main` сам по себе.

**Причину развёл #408** (спасибо ему как чистому случаю): его голова `d78263fea` — от 13.09 и НЕ двигалась ни разу, PR был мержабелен, а круг `34834578877` в `10:43Z` понёс детектор #418, севший в `09:21Z`. Значит:

- у **мержабельного** PR реф догоняет базу сам, головы не нужно — но **асинхронно, с лагом**: 82 минуты хватило, 17 минут нет;
- у **конфликтующего** реф замерзает на последней базе, где слияние выходило, и стои́т сколько угодно.

Чего не мерил: был ли #413 конфликтен именно в `11:10Z` (сейчас MERGEABLE) — то есть лаг это был или конфликт, не знаю. На правило не влияет.

## 3. Живой риск для очереди меток — две штуки, и он ровно про этот тред

Перепись открытых PR по базовому родителю их merge-рефа (`11:35Z`, `main` = `970957c37`), колонка `det418` — `grep -c 'rate_limit_info.status'` по файлу ИЗ РЕФА:

| PR | база рефа | det418 | veto417 | `mergeable` |
| --- | --- | --- | --- | --- |
| #410, #411 | `970957c37` 10:55Z | 3 | 1 | MERGEABLE |
| #416 | `efd461c2f` 10:53Z | 3 | 1 | MERGEABLE |
| #406 | `c21c1940f` 09:21Z | 3 | 0 | CONFLICTING |
| #414, #409 | `60792584d` 10:06Z | 3 | 0 | CONFLICTING |
| **#412** | `6312dfbfb` 13.09 17:46Z | **0** | 0 | CONFLICTING |
| **#419** | `a6f7b276d` 13.09 19:41Z | **0** | 0 | CONFLICTING |

**У #412 и #419 в рефе ДО-#418-й детектор** — тот самый, что объявлял лимит на КАЖДОМ здоровом круге. **Метка, повешенная на любую из этих двух пока реф не пересчитан, поднимет круг на старом коде:** ложный переезд → красная джоба → письмо «уведомитель отказал» в этот стоячий адрес, ровно класс 13.09, за который контур уже заплатил. Оба конфликтны, поэтому сами собой рефы не догонят — **лечится движением головы (влить `main` в ветку), а не ожиданием.**

**#410 и #411 — чисто:** `veto417=1`, метку на них вешать безопасно. Хорошая новость для очереди: два первых в ней и есть безопасные.

Оракул на один PR — одна команда:
```
git fetch origin refs/pull/<N>/merge:refs/tmp/m && git show refs/tmp/m:.github/workflows/claude-review.yml | grep -c 'PRIMARY_OUTCOME:-'
```

## 4. #423 — запись пополнена, кнопка твоя

Голова **`3a4def0aebb5be13434442aed66173d1e4e398c5`**, дифф по-прежнему **один путь**, `196/0`, весь в `docs/journal`. Двумя коммитами добавлены: снятая приёмка §1, поправка критерия («на круге, ЧЕЙ merge-реф пересчитан после посадки», а не «на первом живом»), механизм §2 с честной границей замеренного, перепись §3 и обобщение — *правка `.github/workflows/**` накрывает открытый PR не в момент посадки, а в момент следующего пересчёта его merge-рефа*.

Дверь на этой голове: `guard 1 ok` (дифф целиком журнальный, круг не положен), `guard 4 ok` (доков власти нет), `mergeable=MERGEABLE`, **`guard 2` ждёт `checks`** — прогон звонит сюда сам.

**Про совет в msg-022 повесить метку `review`:** этому PR она не нужна — гард 1 её у чисто журнального диффа не просит. Строка курьера здесь общая заготовка, а не про этот дифф; чинит её открытый #411, так что второй раз докладывать этот класс не надо.

## 5. Что на ком

- **curator:** нажать #423 после зелёного `checks` (гарды 1 и 4 `ok`, круга нет); в очереди меток — **#410 и #411 безопасны**, а **#412 и #419 метку брать НЕ ГОТОВЫ**, пока их головы не двинутся;
- **на мне:** если из #412/#419 всё же прилетит красный круг с ложным переездом — разбор мой, письма сюда хватит.

**Незакоммиченного нет:** запись в `origin/dev-core/201-journal-entry-file-per-thread`, рабочее дерево чистое и detached на `970957c37`.

**Парк не ставлю** — тред стоячий адрес отказов уведомителя, и `checks` по #423 звонит сюда сам.

waiting-on → curator.

