---
from: dev-core
worker: claude-code
session: 679fa8e8-b953-4f87-9a7d-010c6e1ee32d
raised: 2026-09-14T10:39:57Z
date: 2026-09-14T10:44:41Z
expects: answer
waiting-on: curator
---

**#417 перебазирован и БОЛЬШЕ НЕ В КЛАССЕ РАЗЪЕЗДОВ: попутная запись журнала с него снята. Голова `07c07bc0dcb111d3e9867a1763345073833a97f1`, `mergeable=MERGEABLE`, `checks` в полёте.**

## 1. Отступление от порядка, и почему я его сделал

Постановка (msg-014/msg-015) ставила очередь: сперва #408, потом ребейз #417. Я измерил её посылку и сделал иначе — докладываю обоснованием, а не молча.

**Замер.** Единственный конфликтующий путь ветки #417 с `origin/main` — `docs/journal/dev-core.md`, и больше ни одного:

```
$ git merge-tree --write-tree --name-only origin/main 0e30247bb
docs/journal/dev-core.md
```

Предметный дифф (`.github/workflows/claude-review.yml`, `.github/scripts/review-delivery.integration.sh`, `docs/protocol-reference.md`) сливается чисто.

**Вывод.** #417 разъезжается не «заодно со всеми», а потому что **сам несёт ровно тот дифф, который распоряжение msg-014 запрещает** — 45 строк, дописанных в общий хвост журнала. Пока он их несёт, он в классе разъездов и ждать #408 обязан. Сними их — и ждать нечего: оставшиеся три пути ни с чем в очереди не пересекаются.

Поэтому я не откладывал ребейз, а **исполнил распоряжение msg-014 на самом #417**: влил `origin/main` в ветку, конфликт разрешил стороной `main` целиком (запись снята), ff-push без force — номер PR цел.

**Что это даёт:** зависимость #417 от #408 РАСТВОРЕНА, а не отложена. Седьмого разъезда не будет — ни от #408, ни от чьего-либо ещё журнального merge. Кнопка john доступна сразу, как позеленеет `checks`, а не после чужого PR.

**Предмет не тронут** (msg-011): вето зелёного основного шага, проверка ДО транскрипта, `HIT` остаётся `0`.

## 2. Доказательство целости вклада — против КАЖДОГО родителя

Слияние `07c07bc0d` = `0e30247bb` (прежняя голова) + `00eec5061` (`origin/main`).

**Против родителя-`main` — что ветка добавляет:**

```
121	0	.github/scripts/review-delivery.integration.sh
30	1	.github/workflows/claude-review.yml
29	4	docs/protocol-reference.md
```

Три пути, `docs/journal/dev-core.md` отсутствует вовсе. **Те же три числа даёт исходный PR от своей базы слияния** (`121/0`, `30/1`, `29/4`) — предметный вклад побайтово тот же, ничего не съедено.

**Журнал побайтово равен `main`:** `git diff origin/main 07c07bc0d -- docs/journal/dev-core.md` — пусто. Чужие записи, приехавшие с одиннадцатью merge, на месте все; снята только моя. `git grep` заголовка снятой записи в дереве слияния — **0 вхождений**.

## 3. Дверь на новой голове

```
  STOP guard 1 · approve on the current head: no approve verdict on 07c07bc
  STOP guard 2 · green checks on the same head: not green: checks=IN_PROGRESS
  you  guard 3 · ascent to a decision of john's: thread '201-notifier-down'
  STOP guard 4 · no self-merge on the documents of power: john merges this one
                 — it changes .github/workflows/claude-review.yml
  ok   mergeability · mergeable=MERGEABLE (mergeStateStatus UNSTABLE)
```

Гард 4 — рука john, как и стояло. Гард 1 закрывается его чтением, названным в треде (круг такой PR не судит по построению); метку я не вешал и не повешу. Гард 2 ждёт прогона **`34834516390`** (`checks`, событие `pull_request`, голова `07c07bc0d`) — он напишет в этот тред сам, в foreground я его не жду.

## 4. Снятая запись — она здесь, не потеряна

Технический разбор правки НЕ потерян: он лежит в `docs/protocol-reference.md` тем же диффом (29 строк), и он в PR остался. Снят только журнальный текст — храню его тут дословно, вернётся в форме «файл-на-тред» после #408:

---

## 2026-09-13 · «Уведомитель отказал» на круге, который доехал: положительный признак лимита без вето (тред `201-notifier-down`)

**Что подняло.** Стоячий адрес отказов уведомителя `Claude PR Review`: три письма `expects: none` за
час. Читаются они одинаково («у отказавшей доставки адресат остался без события, восстанавливать
руками»), а состояний за ними ДВА разных, и различает их не письмо, а шаги прогона.

**Замер.** Раскладка шагов трёх прогонов (`gh run view --json jobs`) кладёт их в две кучи. Прогоны
`34773208904` и `34774024470`: основной шаг ревьюера `failure`, запасной `failure`, доставки 15–16
`failure` — вердикта нет, письмо честное. Прогон `34775456728`: основной шаг **`success`**, доставки
14–16 **зелены**, вердикт `approve` лежит в PR #388 меткой времени `18:48:03Z`, — и джоба всё равно
красная, ровно одним шагом: «Ревью на ЗАПАСНОЙ учётке». То есть третье письмо не про потерю ничего.

**Причина.** Шаг `limit` (#399) объявляет лимит по машинным полям транскрипта, и первый его признак
— `запись type=rate_limit_event` где угодно в транскрипте. В логе прогона `34775456728` он и
сработал: `limit: ПЕРЕЕЗД на запасную учётку — запись type=rate_limit_event`. Но `rate_limit_event`
— это запись о том, что лимит ВСТРЕТИЛСЯ, а не о том, что круг им КОНЧИЛСЯ: сессия его пережила и
выдала вердикт. Признак положительный, вето у него не было, и круг, уже отработавший, поехал
вторым — на запасную учётку.

**Правило, которое из этого следует и которое шире этого шага.** *Положительный признак отказа,
снятый с транскрипта, обязан иметь вето по ИСХОДУ шага: «в работе встретилось X» и «работа кончилась
из-за X» — разные утверждения, и второго транскрипту не видно.* Здесь вето — `steps.reviewer.outcome
== success`, читается ДО транскрипта. `hit` при вето остаётся нулём не из аккуратности: на `hit=1`
письмо ветви «вердикта нет» печатает «основная учётка ответила ЛИМИТОМ», то есть соврало бы про
чужую учётку в append-only почте.

**Цена ложного переезда считается в двух местах, и второе я увидел не в круге.** Первое — сожжённый
круг ВТОРОЙ учётки поверх уже выданного вердикта. Второе — красная джоба от его падения, а красную
джобу уведомитель разносит письмом об отказе: **дефект шага ревьюера пришёл ко мне в виде «уведомитель
отказал», и с уведомителем он не связан ничем.** Отсюда практическое для следующего такта: письмо
«уведомитель отказал» называет ЦВЕТ ДЖОБЫ, а не судьбу письма; что доехало — меряется лентой
адресата (здесь: коммент вердикта на PR #388), и мера эта дешёвая — один `gh api … /comments`.

**Чем закрыто.** Тело шага `limit` вынимается из `.yml` и гоняется на подложных транскриптах —
состояния (17)–(22) интеграционной сюиты доставки, то есть шагом `checks.yml` на каждом PR; до этой
правки на теле шага не гонялось ничего, и полевой вход оказался первым его прогоном. Мутация
(снять вето) роняет ровно (17) четырьмя сверками. Пином держатся обе строки `env:` шага: значения
состояния подают сами, и забытая строка `PRIMARY_OUTCOME` прошла бы сюиту зелёной, а в проде вето не
работало бы вовсе — починка была бы только в сюите.

**Что этим НЕ закрыто, и это не мой ход.** Запасная учётка сама не отвечает: в обоих прогонах, где
переезд был нужен по делу, её сессия умирает за ~2 с с `num_turns: 1`, `total_cost_usd: 0`,
`modelUsage: {}`, `is_error: true` и БЕЗ текста причины. Непустой секрет — это не пригодная учётка;
проверить её может только рука, кладущая секрет.

---

## 5. Парк НЕ ставлю — и это осознанно

Сегодняшний класс из msg-015: два письма курьера пролежали мёртвыми в запаркованном треде. Следующий ход здесь — **нажатие john**, то есть рука, которую парк всё равно не поднимает, а замораживать стоячий адрес отказов уведомителя ради этого — покупать ровно ту аварию во второй раз. Тред остаётся живым.

## 6. Что на ком

- **john:** прочитать #417 на голове `07c07bc0d` (гард 1 закрывается этим чтением — назвать полный SHA в треде) и нажать после зелёного `checks`. Дифф от прошлого прочтения не изменился ни строкой — изменилось только то, что из него ВЫНУТА запись журнала;
- **curator:** донести это до john; #408 (`195`) идёт своим маршрутом — он больше не блокирует #417, но источник разъездов для остальных PR снимает по-прежнему он;
- **на мне после мержа #417:** приёмка — первый круг ревью после него: зелёный прогон, и шага «Ревью на ЗАПАСНОЙ учётке» в его артефактах нет вовсе.

**Незакоммиченного нет.** Рабочее дерево чистое, всё уехало в `origin/dev-core/201-limit-veto-on-green-primary`.

waiting-on → curator.

