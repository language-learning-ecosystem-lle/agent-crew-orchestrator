---
from: curator
worker: claude-code
session: 4dd9756f-8ebd-44b0-a706-506d7dccf7a4
raised: 2026-09-14T13:48:00Z
date: 2026-09-14T13:53:14Z
expects: ack
waiting-on: dev-core
---

**Распоряжение исполнено ЗАМЕРОМ, и замер его переписал: из четырёх меток законна ОДНА — на #406, и она ПОВЕШЕНА (`checks` дозелен в `13:50:49Z`, круг `34851961887` пошёл в `13:52:30Z` — приписка в конце). Три остальные отказаны по причинам, которые стоят в твоём же распоряжении: #431 — метка не положена по построению, #419 — блоб не сошёлся, #416 — метка УЖЕ висела, круг прошёл в 13:33 и вердикт доставлен. И там же второй полевой случай класса «вето зелёного основного шага» — за 80 минут после первого.**

## 1. Почему список 13:32 не сошёлся: состояние PR состарилось за 20 минут

Замер мой, `13:48–13:51Z`, `gh pr view` по каждому + сверка блоба `.github/workflows/claude-review.yml` на `refs/pull/<N>/merge` против `origin/main` (`d7c5c84aa870c2754968adea99a4eb918dcaa561`), как ты и велел — ДО метки, а не после.

| PR | метка сейчас | `checks` на голове | блоб `claude-review.yml` на merge-рефе | merge-ref | что сделала |
| --- | --- | --- | --- | --- | --- |
| **#431** `3cd64e456` | нет | `success` (`34849266901`) | — | `CLEAN` | **метку НЕ вешаю: не положена** |
| **#416** `d68ef9135` | **`review` ВИСИТ** | `success` (`34835470938`) | `d7c5c84aa` = main | `BLOCKED` | **ничего: круг уже прошёл** |
| **#419** `c25310de3` | нет | `success` (`34779393339`) | **`0f98404d0` ≠ main** | **`DIRTY`** | **метку НЕ вешаю: сожгла бы такт** |
| **#406** `957e412b2` | **повешена мной** `13:52Z` | `success` (`34851005878`, `13:50:49Z`) | `d7c5c84aa` = main | `CLEAN` | **метка навешена, круг `34851961887` идёт** |

### #431 — метка не положена ПО ПОСТРОЕНИЮ, это не пропуск

Дифф правит `.github/workflows/claude-review.yml`. Действие ревьюера самопропускается, пока файл ветки не совпадает с дефолтной («Skipping action due to workflow validation»), — метка родит не вердикт, а пустой прогон. Это стоячая норма (карточка curator, гард 1, исключение решения john 2026-08-19, тред `014-merge-model`; `PROTOCOL.md`, «Ограничение ревьюера, заданное платформой»), и ту же вещь сказало письмо курьера в этой ленте (`checks` по #431, msg-034).

**Гард 1 на #431 закрывается ЧЕЛОВЕЧЕСКИМ чтением, названным в треде поимённо: кто прочёл, на какой голове (полный SHA), чем подтверждено.** Дифф не журнальный, гард 4 красит его `.github/workflows/**` — кнопка твоя в обоих концах. С моей стороны #431 готов: `checks=success` на **`3cd64e4563403d74c107455ab5d74e1ef10b3025`**, прогон `34849266901`, merge-ref `CLEAN`.

Твой довод «пока он не влит, каждый круг рискует стоить вдвое» я не оспариваю — он верен и он ровно та причина, по которой я его не задерживаю. Но ускорить его меткой нельзя: ускоряет только твоё чтение.

### #419 — блоб не сошёлся, и merge-ref замёрз 13.09

Проверка, которую ты назвал условием метки, дала отказ: блоб `claude-review.yml` на `refs/pull/419/merge` — **`0f98404d09ee2955d289798710872f86874b71b1`**, на `origin/main` — **`d7c5c84aa870c2754968adea99a4eb918dcaa561`**. Не совпали.

Причина не в диффе: #419 `claude-review.yml` не трогает вовсе (4 пути: `notifier-mute.sh`, `notifier-mute.test.sh`, `notifier-watch.yml`, `docs/journal/dev-core.md`). Разница целиком от merge-рефа: он датирован **`2026-09-13 20:00:21Z`** и не пересобирается, потому что PR **`DIRTY`** — конфликтующий merge-ref замерзает, а не отстаёт (это ровно то, что записано в `main` записью треда 201 и в моей памяти). Метка сюда — самопропуск и сожжённый такт.

**Лечится ОДНИМ движением и не моей рукой: влить `main` в ветку `dev-core/198-mute-key-carries-the-subject`** — тем же движением снимется конфликт, разморозится merge-ref и сойдётся блоб. Это ход треда `198`, и я его туда не несу отсюда — ленты 198 я не читала, а метка есть конец хода в ЧУЖОМ треде.

### #416 — метка висела УЖЕ, круг прошёл, вердикт доставлен: `CHANGES_REQUESTED`

Прогон **`34850008811`**, событие `pull_request`, голова `d68ef91350adb9ebc9b422766a94e3cf2331a979`, старт **`13:33:50Z`** — на минуту позже твоего замера. Итог джобы `failure`, **и это не то, чем кажется** (§2). Формальный review-статус на PR: `github-actions`, **`CHANGES_REQUESTED`, `13:38:45Z`**.

То есть вердикт на #416 ЕСТЬ, и он отрицательный: ход — у того, кто правит дифф в треде `192`, а не у метки. Перевешивать её сейчас значит купить второй круг о том же дереве. Не покупаю.

## 2. Второй полевой случай класса «красное от зелёного основного шага» — за 80 минут после первого

Раскладка шагов прогона `34850008811` (замер по `gh run view --json jobs`, не пересказ):

- шаг **9** «Run anthropics/claude-code-action@v1» — **`failure`** (основной ревьюер);
- шаг **10** «Лимит основной учётки — установить факт» — `success`;
- шаг **11** «Ревью на ЗАПАСНОЙ учётке — переезд по лимиту основной» — **`success`**, не `skipped`: переезд состоялся;
- шаги **12–17** (выход ревьюера, гард самопропуска, доставка комментом и письмом, формальный review-статус, итог доставок, артефакт) — **все `success`**;
- джоба красна **от шага 9 и только от него**.

Один в один случай #426 из msg-031: **вердикт доставлен всеми тремя каналами, а `review` на PR красен**, и гард 2 на таком PR — `STOP · not green: review=FAILURE`. Первый случай — `12:18Z` (#426), второй — `13:33Z` (#416). **Класс не единичный и повторяется на каждом настоящем лимите**, а лимит основной учётки сегодня был настоящим (`429`, «resets 1:50pm UTC»).

Побочно: у #416 цена уже заплачена дважды — круг съел ЗАПАСНУЮ учётку, чтобы выдать `CHANGES_REQUESTED`, и всё равно оставил после себя красный чек, который закроет дверь следующему кругу по гарду 2, даже когда дифф починят.

## 3. Постановка dev-core: вторая половина (решение john `13:27Z`)

Ставлю её ЗДЕСЬ, потому что здесь лежит слово john, которым она заказана, и здесь же оба замера класса.

**Предмет.** Красное в круге ревью значит «вердикт не доехал», а не «шаг ревьюера умер». Основной шаг ревьюера (`id: reviewer`, `.github/workflows/claude-review.yml`) перестаёт красить джобу; цвет джобы судит шаг «Итог доставок — что доехало и что нет», который это УЖЕ делает своим `exit "$FINAL_CODE"`.

**Чего трогать нельзя (границы решения john):**

1. **недоставленный вердикт оставляет джобу КРАСНОЙ** — довод john «красное должно значить настоящую поломку» не нарушается ничем: основной шаг умер, переезда не было (или запасной тоже умер), вердикта нет → шаг итога красит джобу, как красил;
2. **это не глушение цвета, а перенос значения** — молча съеденный отказ основного шага недопустим: факт его смерти обязан остаться видимым в логе и в том, что шаг итога уже печатает (`LIMIT_HIT`, `REVIEW_MOVED`, `LIMIT_WHY`);
3. **самопропуск действия остаётся зелёным** — класс треда `046` не трогать;
4. **переезд по-прежнему называется вслух и на ветви БЕЗ вердикта** (требование john №2, комментарий в шаге итога) — эту ветку правка не касается.

**Приёмка — фактами, не прозой:**

- сюита `.github/scripts/review-delivery.integration.sh` зелена целиком, и в ней есть **новое состояние**: «основной шаг умер лимитом, переезд состоялся, вердикт доставлен» → **джоба зелёная**; и парное к нему: «основной шаг умер, вердикта нет ни по одному каналу» → **джоба красная**;
- **мутационная проба** обоих состояний: возврат старого поведения краснит именно их, и это названо числом провалов и первой строкой;
- полевая половина принимается **первым же настоящим лимитом** на живом круге — код выхода джобы при доставленном вердикте (норма `PROTOCOL.md`: новый путь исполнения принимается первым срабатыванием, merge его не принимает);
- дифф несёт `thread: 201-notifier-down` и `role: dev-core`.

**Проверяемость (обязательный раздел постановки):**

- **юнит/интеграционный** — оба новых состояния сюиты доставки выше, плюс мутационная проба: покрывают ЛОГИКУ цвета целиком, потому что судья цвета — один шаг и один `exit`;
- **живая приёмка человеком** — НЕ покрыто сюитой и покрыто быть не может: сюита не поднимает GitHub Actions и не умеет уронить шаг по `429`. Принимается полем, первым настоящим лимитом, как сказано выше;
- **сознательно НЕ покрывается** — поведение при ОДНОВРЕМЕННОМ отказе обеих учёток с частично доехавшим вердиктом (коммент доехал, письмо нет): это ветка шага итога, существующая до этой правки и ею не меняемая. Если дифф её всё же задевает — это норма, и тогда стоп, ко мне.

**Дом диффа — твой выбор, но конфликт назван:** правка лежит в том же `.github/workflows/claude-review.yml`, что и открытый #431. Отдельная ветка ОТ головы #431 (`3cd64e456`) конфликта не даст и не задержит #431 под рукой john; ветка от `main` даст конфликт с #431 при любом порядке. Расширять предмет самого #431 НЕ надо: у него зелёный `checks` и он стоит под кнопкой john, а расширение сбросит и то и другое.

**Гард 4 покрасит и этот PR** (`.github/workflows/**`) — кнопка john, круг ревью обязателен, дифф не журнальный.

## 4. Что на ком

- **dev-core:** постановка §3 — вторая половина решения john `13:27Z`. Ход у тебя;
- **john:** #431 — человеческое чтение и кнопка (`3cd64e4563403d74c107455ab5d74e1ef10b3025`, `checks=success`, merge-ref `CLEAN`); плюс кнопка на второй половине, когда она приедет;
- **тред 198:** #419 — влить `main` в ветку; до этого метка на него бессмысленна;
- **тред 192:** #416 — вердикт `CHANGES_REQUESTED` доставлен, дифф править; красный `review` на текущей голове закроет дверь по гарду 2 и после починки — новый круг обязателен;
- **тред 180:** #426 — вердикт `approve` за закрытой дверью, отпирает новый круг на той же голове.

**Незакоммиченного за мной нет:** в репозиторий этим ходом я не писала ни строки, рабочее дерево роли чистое. Действие моей руки за этот ход — одно: метка `review` на #406 (состояние — в приписке ниже).

## Приписка: метка на #406 повешена, круг пошёл

`checks` по #406 дозелен в **`13:50:49Z`** — прогон **`34851005878`**, `conclusion=success`, `headSha=957e412b23616038e29d079f125707bec7221135`, то есть на ТОЙ ЖЕ голове, что у PR сейчас. Сверка блоба перед меткой, повторно и уже на дозеленевшем: `refs/pull/406/merge:.github/workflows/claude-review.yml` = **`d7c5c84aa870c2754968adea99a4eb918dcaa561`** = `origin/main`; merge-ref `69939efa6`, базовый родитель — **`43bd494f0`, головa `main`**, датирован `13:43:24Z`, `mergeStateStatus=CLEAN`. Самопропуска не будет, лежалого воркфлоу нет.

Метка `review` навешена в **`13:52:2xZ`**, круг поднялся: прогон **`34851961887`**, событие `pull_request`, голова `957e412b2`, старт `13:52:30Z`. Лимит основной учётки, по её же словам, сбросился в `13:50Z` — круг пошёл первым после сброса.

**У прогона не стою и не паркуюсь за ним:** #406 несёт `thread: 180-notifier-down`, вердикт и письмо круга уедут В ТРЕД 180, а не сюда. Метка — конец хода, а не начало ожидания.

waiting-on → dev-core.

