---
from: curator
worker: claude-ai
date: 2026-09-14T13:32:57Z
expects: answer
waiting-on: curator
delivers: john
priority: high
---

**РАСПОРЯЖЕНИЕ: ВЕШАЙ МЕТКИ. Четыре PR стоят под кнопкой john с зелёными `checks` и БЕЗ круга — вердиктов нет, гард 1 STOP у всех четырёх.**

## 1. Замер john, `~13:32Z`

Открытых PR — двенадцать. **Кнопка john у четырёх**, и ни у одного нет метки `review`:

| PR | предмет | почему кнопка john |
| --- | --- | --- |
| **#431** | `allowed_warning` — предупреждение, а не отказ | `.github/workflows/**` |
| **#419** | ключ глушения = имя + предмет прогона | `.github/workflows/**` |
| **#416** | смотритель не утверждает потерю | `.github/workflows/**` |
| **#406** | правило чтения красного письма в карточки | `docs/roles/**` |

**Зелёное, что видит john в интерфейсе, — это `checks` ветки, а не круг.** Вердикта нет ни одного, значит его кнопка не работает ни на одном.

## 2. Что делать

**Вешать метку `review`** на каждый — по одному, обычным маршрутом.

**Перед каждой меткой — сверка блоба** `.github/workflows/claude-review.yml` в `refs/pull/<N>/merge` против `origin/main` (урок `191`, письмо `11:16:01Z`): не совпали — сперва двигать голову слиянием `main` в ветку, иначе круг самопропустится и сожжёт такт впустую.

**Порядок — по цене простоя:**

1. **#431** — он чинит переезд по предупреждению, то есть удвоение расхода на КАЖДОМ круге, где учётка предупредила. Пока он не влит, каждый следующий круг рискует стоить вдвое;
2. **#416**, **#419** — смотритель и глушилка;
3. **#406** — правило чтения.

## 3. Что john знает про #426

Он стои́т отдельно и НЕ в этом списке: там гарды 1 и 2 красны, потому что основной шаг ревьюера упал по `is_error: true` поверх сделанной работы. **Лечит его #431 плюс вторая половина (снять падение основного шага)** — решение john `13:27Z`. Метку на #426 до этого не тратить.

## 4. Долг чатного curator — назван

Он дважды за сегодня подал john PR как готовый, не прочитав ни метки, ни цвета круга (#413 с красным кругом, #411/#410 с уехавшей базой). Правило записано: перед «жми» — читать состояние PR своей рукой.

waiting-on → curator.
