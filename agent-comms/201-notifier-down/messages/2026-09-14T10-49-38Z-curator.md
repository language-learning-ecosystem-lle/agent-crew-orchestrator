---
from: curator
worker: claude-code
session: 13899447-7f03-44f1-8dc8-307f1fb00791
raised: 2026-09-14T10:46:28Z
date: 2026-09-14T10:49:38Z
expects: ack
waiting-on: curator
parked-on: john
---

**Требуется решение john: нажми #417 на голове `07c07bc0dcb111d3e9867a1763345073833a97f1` — и ответь на 4.3 из msg-007, она единственная осталась без ответа.**

Ребейз dev-core (msg-016) проверен своей рукой целиком: все три его утверждения держатся. Ниже — чем именно проверены, состояние двери, и что осталось на тебе.

## 1. Что я перемерила, а не пересказала

**Дифф ветки против `origin/main` — три пути, журнала нет вовсе:**

```
$ git diff --numstat origin/main <голова 07c07bc0d>
121	0	.github/scripts/review-delivery.integration.sh
30	1	.github/workflows/claude-review.yml
29	4	docs/protocol-reference.md
```

`git diff origin/main 07c07bc0d -- docs/journal/dev-core.md` — **пусто**: журнал побайтово равен `main`, чужие записи все на месте, снята только его собственная. Числа `121/0`, `30/1`, `29/4` совпадают с теми, что PR давал от своей прежней базы, — предметный вклад не съеден ничем.

**Предмет прочитан в самом диффе, а не в докладе.** В `claude-review.yml` ровно то, что объявлено: ветка `if [ "${PRIMARY_OUTCOME:-}" = "success" ]` стоит **первой**, до чтения `EXECUTION_FILE`; `HIT` остаётся `0`; добавлена строка `env: PRIMARY_OUTCOME: ${{ steps.reviewer.outcome }}` и шапка с замером прогона `34775456728`. Пустое значение читается как «судить не по чему» и вето не даёт — поведение до правки сохранено. **Новой нормы дифф не вводит** (новых полей конфига, форм сообщения, прав и шагов маршрута нет; пустой `PRIMARY_OUTCOME` возвращает прежнюю ветку) — но гард 4 это не трогает, кнопка твоя по пути `.github/workflows/**`, а не по классу диффа.

**`mergeable=MERGEABLE`** (`mergeStateStatus UNSTABLE` — это `checks` в полёте).

## 2. Отступление dev-core от моего порядка — принято, и это было ИСПОЛНЕНИЕ распоряжения, а не обход

msg-014 ставила очередь «сперва #408, потом ребейз #417». dev-core сделал иначе и доложил обоснованием. Замер его посылки верен: единственный конфликтующий путь #417 был `docs/journal/dev-core.md`, то есть **PR разъезжался ровно потому, что сам нёс тот дифф, который распоряжение запрещало**. §3 распоряжения говорила «запись держать в теле PR или отложить до новой формы» — он ровно это и сделал, только на самом #417, и зависимость от #408 не отложил, а растворил.

Считаю правильным и подтверждаю: **ждать #408 этому PR больше не нужно.** #408 (тред `195`, файл-на-тред) по-прежнему снимает источник разъездов для ОСТАЛЬНЫХ PR и идёт своим маршрутом — но #417 он не блокирует.

## 3. Дверь на текущей голове — мой прогон, не пересказ

```
$ pnpm protocol merge-gate --ref origin/main --pr 417 --review-workflow 'Claude PR Review'
merge-gate: PR #417 at 07c07bc
  STOP guard 1 · approve on the current head: no approve verdict on 07c07bc
  STOP guard 2 · green checks on the same head: not green: checks=IN_PROGRESS
  you  guard 3 · ascent to a decision of john's: thread '201-notifier-down'
  STOP guard 4 · no self-merge on the documents of power: john merges this one
                 — it changes .github/workflows/claude-review.yml
  ok   mergeability · mergeable=MERGEABLE (mergeStateStatus UNSTABLE)
```

- **гард 4 — твоя рука**, у всех остальных STOP по построению;
- **гард 1 закрывается твоим чтением**, названным в этом треде поимённо (кто прочёл, полный SHA головы, чем подтверждено). Метку `review` на этот PR не вешали и не повесим: действие самопропускается, пока файл ветки не совпадает с дефолтным, и круг вернёт пустой прогон вместо вердикта;
- **гард 2 ждёт прогона [`34834516390`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34834516390)** (`checks`, событие `pull_request`, голова `07c07bc0d`, старт `10:42:33Z`). Он звонит в этот тред сам; в foreground я его не жду. Прежние две головы этой ветки давали `success` (`34829252494`, `34826105344`), но **вердикт гарда 2 относится к коммиту, а не к ветке** — нужен зелёный именно на `07c07bc0d`.

## 4. Три решения msg-007 — состояние на 10:49Z

| # | предмет | состояние |
| --- | --- | --- |
| 4.1 | кнопка на #417 | **ждёт тебя**, голова сменилась на `07c07bc0d` |
| 4.2 | секрет `CLAUDE_CODE_OAUTH_TOKEN_FALLBACK` | **сделано** — перевыдан ~`10:26Z`; проверяемым станет при первом настоящем лимите, раньше не мерится ничем |
| 4.3 | `continue-on-error: true` на запасном шаге | **без ответа** |

**4.3 напоминаю, потому что она переживает #417.** Вето закрывает класс «платим второй учёткой за СВОЙ успех». Остаётся класс шире: переезд нужен по делу, запасная падает — и цвет круга снова решает падение шага, а не судьба вердикта; красный круг при этом убивает **гард 1**, а не только гард 2 (замер в msg-007 §3: `approve` физически лежит в окне круга, дверь его не засчитывает). Рекомендация curator — не решение: это меняет смысл красного круга, то есть смысл гарда 2 двери merge, и потому норма, а не починка.

## 5. Цена парковки названа явно, и что делать, если `checks` покраснеет

Тред `201-notifier-down` — стоячий адрес отказов уведомителя; парк его замораживает: письма `github` сюда лягут, но подъёма роли не вызовут. Плачу эту цену сознательно — следующий ход здесь твоя рука, а её парк и так не поднимает; без парка звонка не будет вовсе.

**Один исход требует твоего действия помимо кнопки:** если прогон `34834516390` придёт **красным**, ход не твой, а dev-core — тогда **сними парк**, иначе письмо об отказе ляжет в замороженный тред и пролежит мёртвым (ровно класс, стоивший сегодня трёх случаев, msg-015 §1).

## 6. Что на мне после твоей кнопки

- перевешивание меток на три PR без вердикта — **#410**, **#386**, **#408** — и дальше по очереди меток, замороженной с 17:33Z 13.09;
- приёмка починки — на dev-core: первый круг ревью после мержа #417, зелёный прогон, и шага «Ревью на ЗАПАСНОЙ учётке» в его артефактах нет вовсе.

**Незакоммиченного у меня нет** — рабочее дерево чистое, своей рукой в этот такт я не правила ничего: вся работа была сверкой.

Ход остаётся у curator (`waiting-on → curator`), потому что ждём решения john.

