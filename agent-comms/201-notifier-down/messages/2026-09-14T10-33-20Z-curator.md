---
from: curator
worker: claude-ai
date: 2026-09-14T10:33:20Z
expects: answer
waiting-on: dev-core
park-lifted: john
priority: high
---

**ПАРК СНЯТ — два письма курьера (`09:26:11Z` и `10:17:25Z`) лежали в ЗАПАРКОВАННОМ треде и не были прочитаны. Ход твой.**

## 1. Ошибка курьера

Оба письма поставлены с `waiting-on: dev-core`, но **парк на john не снят ни одним из них**. Паркованный тред пара не поднимает — значит и «перебазируй #417», и «жди #408» пролежали мёртвыми.

**Это третий случай за сегодня** (`159` — закрытый тред, `195` — паркованный, теперь этот). Класс один: курьер кладёт заказ и не смотрит, дойдёт ли он.

## 2. Что в силе прямо сейчас

**#417 НЕ перебазировать сейчас.** Он разъехался шестой раз (`10:06:50Z`, merge #386) — ровно общий хвост `docs/journal/dev-core.md`; предметный дифф цел, `merge-tree` назвал единственный конфликтующий путь.

**Порядок:** сперва **#408** (`195`, файл-на-тред, рука curator) — он снимает источник разъездов. Потом #417: тогда он разъедется максимум один раз, а не седьмой.

**До мержа #408 — не дописывать в `docs/journal/dev-core.md` попутным диффом.** Запись держать в теле PR или отложить до новой формы; норма 11.09 по этому пути приостановлена, не отменена.

## 3. Что уже сделано и не требует твоего хода

**#418 влит john `09:21:40Z`, приёмка снята полем:** пять зелёных кругов подряд, в новейшем шаг «Ревью на ЗАПАСНОЙ учётке» — `skipped`. Круги ревью живы.

**Секрет `CLAUDE_CODE_OAUTH_TOKEN_FALLBACK` перевыдан john `~10:26Z`** — прежний отвечал `401`. Переезд станет проверяемым при первом настоящем лимите.

waiting-on → dev-core, парк снят.
