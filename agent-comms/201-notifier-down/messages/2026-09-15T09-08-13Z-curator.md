---
from: curator
worker: claude-code
session: d886abb6-3942-4ea8-b176-735762fa141f
raised: 2026-09-15T09:04:29Z
date: 2026-09-15T09:08:13Z
expects: ack
waiting-on: curator
parked-on: john
---

**Требуется решение john: человеческое чтение и кнопка на #445, голова `8da71432dab667db2d8de9aca7df946feacf23cd` — это ПОСЛЕДНЯЯ ЧЕТВЕРТЬ твоего слова `2026-09-14 13:27Z` (граница №2), а не новый предмет. Гард 4 — `.github/workflows/claude-review.yml`, моей рукой тут не делается ничего.**

Довод dev-core (msg-042 §1) я проверила своей рукой, а не пересказала. Замер мой, `09:05–09:10Z`.

## 1. Поглощение #436 было НЕПОЛНЫМ — моя ошибка, и она названа

В msg-041 §4 я написала «#436 поглощён #433: тот покрывает ОБА шага». Верно про строки `continue-on-error` — и неверно как вывод о ГРАНИЦАХ. Граница №2 («смерть основного шага молча не съедается») жила в ДРУГОМ шаге и ДРУГОМ файле, чем предмет спора, и с #433 не приехала. Мерила я пересечением файлов и предметом, а мерить надо было перечнем границ постановки — по каждой, чем она держится в оставшемся диффе. Правило записано dev-core в журнал; я его признаю своим промахом, а не спором о формулировке.

**Проверено по `origin/main` (`117164193`), чтением файла, не доклада:**

- шаг «Итог доставок» (строка 1166): в его `env:` строки `PRIMARY_OUTCOME` **нет** — `PRIMARY_OUTCOME` в файле ровно один, строка 601, и он в шаге `limit`;
- тело шага на ветви «все три доставки `ok`» делает `exit "$FINAL_CODE"` (строка 1213) **раньше любой печати про исход основного шага**.

Следствие полевое: круг, доработанный ЗАПАСНОЙ учёткой, сегодня в логе неотличим от круга, отработанного основной — ровно на зелёной ветви, ради которой перенос значения и делался.

## 2. Что в диффе #445 — читано мной по `git diff origin/main...8da71432d`

`+91 −12`, три файла: `.github/workflows/claude-review.yml`, `.github/scripts/review-delivery.integration.sh`, `docs/protocol-reference.md`.

- **`env:` шага итога** получает `PRIMARY_OUTCOME: ${{ steps.reviewer.outcome }}` — `outcome`, то есть ДО применения `continue-on-error`;
- **печать стои́т ДО раннего выхода** — вставка идёт сразу после `echo "Доставки вердикта: …"` и ПЕРЕД вычислением `FINAL_CODE` и `if … exit "$FINAL_CODE"`. Это я сверила позицией хунка, а не описанием: печатать после `exit` значит не печатать вовсе именно на той ветви, ради которой правка;
- **молчание сохранено там, где оно честное:** блок целиком под `if [ "${PRIMARY_OUTCOME:-}" != "success" ]` — основной шаг отработал, говорить нечего. Состоялся переезд → `::notice::` про доработавшую запасную; переезда не было → `::warning::`. Обе ветви под `${…:-}`, `set -uo pipefail` их не роняет;
- **шапка шага `id: reviewer`** — фраза «единственный читатель исхода этого шага» стала ложной ЭТИМ ЖЕ диффом и переписана перечнем обоих читателей. Утверждение о числе читателей — расходник, и оно уехало тем же коммитом, что и новый читатель;
- **`docs/protocol-reference.md`** — абзац про печать в разделе «Цвет круга ревью решает ДОСТАВКА вердикта», плюс починен номерной пин: было `(23)–(26)`, стало `(25)–(29)` (после merge #433 нумерация уехала).

**Сюита — оракулом CI, а не рукой исполнителя** (сюита умеет молча пропустить состояние и остаться зелёной): `gh run view 34947799872 --log | grep 'СОСТОЯНИ'` → `ВСЕ СОСТОЯНИЯ ПРОШЛИ — 30 шт.`, `08:44:42Z`. База на `main` — 29. Дельта ровно одна и она названа: состояние (29).

## 3. Гарды на #445 — поимённо

`pnpm protocol merge-gate --ref origin/main --pr 445 --review-workflow 'Claude PR Review'` → `REFUSED`, оба отказа ожидаемы:

- **гард 1 — `STOP`**, «no approve verdict on 8da7143». Круга не будет ПО ПОСТРОЕНИЮ: дифф правит `claude-review.yml`, действие самопропускается. Метки `review` на PR нет (`labels: []`) — и вешать её нельзя: круг вернёт пустой прогон. **Закрывается твоим чтением, названным здесь поимённо: кто прочёл, голова `8da71432dab667db2d8de9aca7df946feacf23cd`, чем подтверждено**;
- **гард 2 — `ok`**, `checks=SUCCESS` на той же полной голове, прогон `34947799872`;
- **нота о переезде базы — отвечена замером, а не согласием.** Дверь права: `checks` стартовал `08:35:39Z`, база уехала после (`4a63df9`, `bba0a85`, `1171641` — `08:40:52–08:41:24Z`). Мера: дельта базы `e71204c57..117164193` трогает `notifier-mute.sh`, `notifier-mute.test.sh`, `notifier-watch.yml`, `PROTOCOL.md`, два каталога журнала, `docs/roles/{curator,dev-core}.md` — **с тремя файлами #445 не пересекается ни одним путём**; `git merge-tree --write-tree origin/main 8da71432d` → чистое дерево `ff9892c5c`, `mergeable=MERGEABLE` (`CLEAN`); `main` на `117164193` зелен своим прогоном `34948342156`. **Остаток честно: ОДНОГО прогона по объединению не мерил никто** — свежий зелёный требует переезда головы, а он сожжёт и без того отсутствующий круг. Моя рекомендация — жать как есть; хочешь гард 2 на объединении — скажи, и я ставлю dev-core ребейз-пуш;
- **гард 3** — восходит к твоему слову `2026-09-14 13:27Z` (тред `180`, msg-138), граница №2 дословно про «молча съеденный отказ основного шага недопустим». Новой нормы дифф не вводит: ни поля конфига, ни формы сообщения, ни права, ни шага маршрута; снятия или сужения запрета среди 12 удалённых строк нет — это правка комментария-шапки и номерного пина. Печать в лог — исполнение уже принятой границы, а не новый повод звонить;
- **гард 4 — `STOP`**, «john merges this one — it changes `.github/workflows/claude-review.yml`». **Кнопка твоя**;
- **гард 5** — за тем, кто нажмёт.

## 4. Что на ком

- **john:** человеческое чтение #445 и кнопка (`8da71432d`). Ход у меня, потому что вопрос несу я;
- **dev-core (тред 201):** работы нет — `checks` зелен, остаток границы №2 дописан, незакоммиченного за ним нет. Поднимать его до твоего слова значит платить дважды;
- **на мне:** ничего в полёте.

**Хвост открытых PR я в этот ход НЕ подметала намеренно:** прогон привязан к треду `201-notifier-down` и только к нему, а след гарда 5 по чужому PR обязан лечь в ЕГО тред. Открытых сейчас 11 (#426…#446), из них к 201 относится один — #445.

**Незакоммиченного за мной нет: в репозиторий этим ходом я не писала ни строки, рабочее дерево роли чистое. Действий моей руки над PR — ноль: только чтение и замеры.**

waiting-on → curator (парковка на john).

