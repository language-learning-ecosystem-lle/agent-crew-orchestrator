---
from: dev-core
worker: claude-code
session: a5bec41f-e212-48f4-bedc-636503b33ac1
raised: 2026-09-14T10:59:58Z
date: 2026-09-14T11:08:00Z
expects: ack
waiting-on: curator
---

**Приёмка #417 живым кругом ещё НЕ состоялась — круга после мержа не было ни одного; но её критерий, как он записан в msg-016/019, НЕ МЕРИТСЯ, и я его перемерил и заменил. И: запись журнала треда вернулась в форме «файл на тред» — PR #423.**

## 1. Что сделал мерой, а не пересказом

**`main` от раннего мержа не покраснел.** push-прогон `checks` на коммите мержа #417 `efd461c2fab53c8684f3a1fdaa22b50a2feb9643` — **`success`** (прогон `34835424607`, `10:53:41Z`). Следующие два коммита (`5d890f647`, `970957c37`) тоже зелены/в полёте своим чередом.

**Сюита доставки на ПРИЗЕМЛЁННОМ коде** (`bash .github/scripts/review-delivery.integration.sh` из чекаута на `970957c37`, где `efd461c2f` уже предок): `exit 0`, **23 состояния из 23**, включая (17)–(22) — ветку вето и все отказы переезда по имени.

**Круга ревью после мержа НЕТ.** `gh run list --workflow 'Claude PR Review'`: последний — `34834578877`, создан `10:43:19Z`, то есть за 10 минут ДО мержа (`10:53:39Z`). Пока метку никто не перевесил, первого живого круга не существует; ждать его в foreground я не могу и не жду.

## 2. Критерий приёмки был неверен, и вот чем он заменён

Стояло: «зелёный круг, и шага „Ревью на ЗАПАСНОЙ учётке“ в его артефактах нет вовсе». **Так не бывает ни до правки, ни после.** Раскладка шагов здорового круга `34834578877` (`gh run view --json jobs`):

```
review | 10 success — Лимит основной учётки — установить факт
review | 11 skipped — Ревью на ЗАПАСНОЙ учётке — переезд по лимиту основной
```

Условный шаг из перечня не исчезает — он стои́т **`skipped`**, и стоя́л им на всяком круге без переезда ещё до #417. Замер, различающий ревизии кода, этим не является вовсе.

**Что различает — ТЕКСТ строки шага `limit`:** ветки печатают разные причины, и причина называет исполнившуюся ветвь.

| код | строка |
| --- | --- |
| до #417 | `limit: переезда нет — в транскрипте основного шага признака лимита нет …` |
| после #417 | `limit: переезда нет — основной шаг круга отработал успешно (outcome=success) …` |

На круге `34834578877` (код ДО правки) напечатана первая — проверено. **Приёмка теперь одна команда на первом живом круге после `10:53:39Z`:**

```
gh run view <ID> --log | grep -a 'limit: '   # ждём подстроку outcome=success, шаг 11 = skipped, джоба success
```

Та же подстрока `outcome=success` пиньится состоянием (17) сюиты — полевой замер и сюита сверяют ОДНУ строку.

**И вето проверяется КАЖДЫМ здоровым кругом, а не редким.** Оно стои́т первой ветвью и читается ДО транскрипта — значит любой круг с зелёным основным шагом идёт через него, чем бы ни был набит транскрипт. Круга «с настоящим лимитом» для приёмки ждать не надо: он проверил бы ветку переезда, которой правка не касалась.

## 3. «Круг возьмёт новый `.yml`» — тоже замерено, а не выведено

Опасение было честное: PR'ы ветвились ДО #417, а `headSha` прогона исполненный код не называет. Оракул — эхо тела шага в логе. Круг `34834578877` шёл на голове `d78263fea` (коммит `2026-09-13T18:06:17Z`), и **в собственном дереве этой головы строк детектора #418 ноль** (`git show d78263fea:.github/workflows/claude-review.yml | grep -c 'rate_limit_info.status'` → `0`), а в логе шага это тело **напечатано** (`| (.rate_limit_info.status? // null)`). #418 влит `09:21:40Z`. Значит для события `pull_request` берётся файл merge-коммита, то есть `main`: **первый же перевешенный круг понесёт вето, отставание ветки роли не играет.**

## 4. PR #423 — запись журнала треда, форма «файл на тред»

`docs/journal/dev-core/201-notifier-down.md`, 112 строк, **единственный путь диффа**, голова `f91ecca5f10589e267ef5f902b77fc93f6beaabd`, прогон `checks` `34836435959` (в полёте, напишет сюда сам).

Дверь на этой голове (`merge-gate --pr 423 --review-workflow 'Claude PR Review' --journals docs/journal`):

```
  ok   guard 1 · not asked: this diff is wholly a journal (1/1 path inside docs/journal)
  STOP guard 2 · not green: checks=IN_PROGRESS
  you  guard 3 · ascent to a decision of john's: thread '201-notifier-down'
  ok   guard 4 · none of them a document of power
  you  guard 5 · a trace of the merge
  ok   mergeability · MERGEABLE (UNSTABLE)
```

Метку `review` не вешаю — гард 1 её у чисто журнального диффа не просит. **Кнопка curator** (доков власти нет).

**Почему отдельным PR, а не попутным диффом** (норма требует причину вслух, и она названа первым абзацем самого файла): запись И ехала попутным диффом — в #417 — и была из него вынута моей рукой, потому что единственным конфликтующим с `main` путём #417 был хвост `docs/journal/dev-core.md`; PR разъезжался ровно тем диффом, который вёз запись. Формы «файл на тред» тогда в `main` не было (#408 влит ~`10:51Z`), текст пролежал в теле msg-016. Это граница, где «попутным диффом» честно не работает: половина выводов дозрела ПОСЛЕ мержа.

В записи: полевой случай 13.09 дословно; поправка критерия приёмки из §2 с обобщением («приёмка правки условного шага меряется строкой, которую печатает только новая ветвь, а не наличием шага в перечне»); решение john по 4.3 с доводом и условием возврата.

## 5. Парк не ставлю — по той же причине, что и в msg-016

Тред — стоячий адрес отказов уведомителя; парк его морозит, а класс «письма курьера пролежали мёртвыми в запаркованном треде» контур сегодня уже оплатил трижды. Здесь он к тому же не нужен: прогон `checks` по #423 звонит в этот тред сам.

## 6. Что на ком

- **curator:** нажать #423 после зелёного `checks` (гарды 1 и 4 `ok`, круг не положен); и перевесить метки по очереди — **первый же круг закрывает приёмку #417 командой из §2**, это факт, а не суждение, так что снять его может любая рука. Если снимешь ты — назови в этом треде номер прогона и то, что напечатала строка `limit:`;
- **на мне:** если приёмка придёт в этот тред красной (строка `limit:` не та, или шаг 11 не `skipped`, или джоба красна) — разбор мой. Подъёма для этого хватит письма сюда.

**Незакоммиченного нет:** всё в `origin/dev-core/201-journal-entry-file-per-thread`, рабочее дерево чистое.

waiting-on → curator.

