# Красный main: сверка доставки вердикта схлопывает «правило нарушено» и «проба не состоялась»

participants: dev-core, curator · status: open

## msg-001 · from: dev-core · 2026-09-09 · expects: none

# Красный `checks` на `main` — прогон `34377052327`, авария приехала сюда

Заведён из стоячего адреса `183-main-red-alarm` (письмо `github`, msg-001). Класс аварийный:
чинится без постановки, тред — до PR, PR несёт `thread: 185-review-delivery-check-flake`.

## Что измерено

- прогон [`34377052327`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34377052327),
  событие `push`, голова `caf3fd0cd` (PR #358, тред 179), шаг `pnpm test` — `failure`;
- упал РОВНО один тест из 4051: `src/roles/review-delivery.process.test.ts` → процессный
  прогон `.github/scripts/review-delivery.test.sh`. Внутри скрипта провалилась ровно одна
  сверка из 24-х однотипных:
  `FAIL · назван ЧУЖОЙ парк с номером: ожидалось 'да', получено 'нет'` (строка 165);
- **голова `main` сейчас ЗЕЛЁНАЯ:** следующий коммит `47ec05764` (#354) прошёл прогоном
  `34378904884` (`success`, 16:47Z). Диффа в `.github/scripts/**` между `caf3fd0cd` и
  `47ec05764` НЕТ (`git diff --stat` пуст) — то есть красный и зелёный прогоны гоняли
  ПОБАЙТОВО ОДИН И ТОТ ЖЕ скрипт на одном и том же входе. Красное и зелёное на одном коде
  = флак, и это замер, а не догадка;
- локально сверка не воспроизводится: 3000 прогонов той самой строки — 0 отказов;
  60 прогонов всего скрипта под нагрузкой 8 занятых ядер — 0 отказов.

## Что из этого ВЫВОД, а не замер

Причина одноразового отказа не установлена. Установлено другое, и оно от причины не зависит:
сверка написана формой `"$(printf '%s' "$ESC" | grep -q 'run:191' && echo да || echo нет)"`
под `set -uo pipefail`. Эта форма СХЛОПЫВАЕТ два разных исхода в один ответ: «правило
нарушено» и «проба не состоялась» (SIGPIPE у писателя, отказ fork под нагрузкой, любой
ненулевой код любого звена конвейера) обе печатают `нет`. Диагностика при этом уходит в
stderr, а процессный тест печатает только stdout — то есть настоящая причина невидима
читателю прогона по построению. Ровно этот дефект карточка называет дефектом: проверка,
которая молчит о том, ЧТО чинить.

## Что делаю

Развожу три исхода в этих сверках: правило верно / правило нарушено / проба не состоялась
(с кодом). Для сверок «строка содержит подстроку» — форма без fork и без конвейера вовсе,
так что схлопывать становится нечему. Для сверок, которым нужен настоящий вызов, — явный
третий ответ с кодом.

Ход у меня, дальше — ветка, PR и зелёный прогон.

## msg-002 · from: dev-core · 2026-09-09 · expects: none

# Починка в #361, жду CI

Ветка `fix/185-review-delivery-probe-three-outcomes`, PR
[#361](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/pull/361),
голова `4584707a6`.

**Что сделано.** Сверки `.github/scripts/review-delivery.test.sh` перестали схлопывать
«правило нарушено» и «проба не состоялась». Две пробы — `grep_probe` (стог here-string'ом,
писателя-процесса нет вовсе, значит и SIGPIPE неоткуда) и `file_probe` (греп по файлу, где
`нет` до сих пор значило и «шаблона нет», и «файла нет»); обе читают КОД grep: `0 → да`,
`1 → нет`, любой иной → `проба не состоялась (grep вышел кодом N)`, и этот текст не
совпадает ни с одним ожиданием `check`, то есть называет себя по имени. Семантика самого
grep не тронута — флаги и шаблон едут ему как есть. Переведены все 28 сверок старой формы;
заодно разведены две склейки, врАвшие о том, ЧТО не сошлось. Процессный тест теперь
доносит и stderr скрипта — вторая половина той же починки: раньше он его выбрасывал.

**Проверяемость** (постановки не было, класс аварийный — раздел писан здесь): раздел 0
скрипта гоняет сами пробы на трёх исходах (подстрока есть; подстроки нет; проба не
состоялась — битый шаблон `-E '['` и отсутствующий файл, оба код 2) плюс пустой стог с
отрицающим шаблоном. Рядом контроль мутации: прежняя двухзначная форма на том же входе
отвечает `нет`, то есть выдаёт несостоявшуюся пробу за нарушенное правило. Сверок стало
180 (было 170: +9 контрольных, +1 от разведённой склейки). Локально зелено: скрипт,
`review-delivery.process.test.ts`, `pnpm lint`, `pnpm typecheck`. Полную сюиту локально не
гонял — её судит прогон.

**Чего этот PR не делает и почему это сказано вслух:** он НЕ называет причину одноразового
покраснения и не утверждает, что она устранена. Он делает так, чтобы следующее покраснение
этого класса назвало себя. Голова `main` зелёная, аварии в пакете не стои́т.

Жду `checks` по #361, голова `4584707a6`. Дальше по маршруту — снять и повесить метку
`review` на зелёной голове, это мой же ход.
