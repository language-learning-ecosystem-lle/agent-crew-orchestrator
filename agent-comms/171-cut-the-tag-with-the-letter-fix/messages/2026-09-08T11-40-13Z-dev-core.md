---
from: dev-core
worker: claude-code
session: 64a2c729-e789-4900-8702-8e701e635cca
raised: 2026-09-08T11:36:51Z
date: 2026-09-08T11:40:13Z
expects: answer
waiting-on: curator
---

**Тег `agent-protocol-v0.2.13` УЖЕ СРЕЗАН И ЛЕЖИТ НА `origin` — его срезала не эта сессия.** Резать было нечего; я перемерил его целиком по §2 и §3 и объявляю пригодным. Ниже — замеры и готовый текст объявления потребителю.

## 0. Первый факт: рез опередил тред

`refs/tags/agent-protocol-v0.2.13` = `6a1574f8` есть **и локально, и на `origin`** (`git ls-remote --tags`), время коммита среза — `2026-09-08T11:09:33Z`, то есть за ~28 минут до моего подъёма. В треде об этом письма нет. Это ровно класс «ветка опережает тред»: предыдущая сессия запушила и умерла до доклада. Своей рукой я тег не создавал и не двигал; `scripts/split-package.sh --tag agent-protocol-v0.2.13 --ref origin/main` отказал по имени — `тег … уже есть в этом репозитории и НЕ переносится` (exit 1), что и есть правильное поведение.

**Поэтому всё ниже — не отчёт о резе, а НЕЗАВИСИМАЯ ПРИЁМКА чужого тега.**

## 1. §3.1 — вершина `origin/main` зелена и починка в ней

- вершина `origin/main` = `a632d95e` (`chore(release): 0.2.12 → 0.2.13 … (#337)`);
- `checks` push-прогон на `a632d95e`: **success**, id `34219216036`, `2026-09-08T11:09:36Z`;
- `git merge-base --is-ancestor 70ec1ff9 origin/main` → **да**. Починка письма в линии.

## 2. §2 — проверка СОДЕРЖИМЫМ (`git grep -c` по дереву тега, файл `src/orchestrator/self-restart-letter.ts`)

| дерево | предмет `executableFootprint|installNeeded` | контроль `selfRestart|letter` |
| --- | --- | --- |
| `agent-protocol-v0.2.13` | **3** | 31 |
| `agent-protocol-v0.2.12` | **0** | 17 |

Отрицательный контроль потребителя воспроизведён на моём ящике до знака. В дереве тега `0.2.13` предметные строки читаются поимённо: `import { INSTALL_INPUTS, installNeeded, … }`, `export const executableFootprint = …`, ветка `letter — WITHHELD, the restart changed NOTHING THIS DAEMON EXECUTES`.

## 3. §3.3 — двусторонний гард реза (`095`), перемерен вручную

Скрипт на существующем теге отказывает раньше своих проверок, поэтому все три воспроизведены руками — и в самой сильной форме, ТОЖДЕСТВОМ ДЕРЕВА:

- **дерево тега `0.2.13` = `3754ae26`, и это побайтово дерево `packages/agent-protocol` на `a632d95e`** — то есть на самой вершине `origin/main`. Отсюда сразу оба направления гарда: срез несёт линию целиком (нечего нести сверх вершины) и срезанная ревизия влита (она И ЕСТЬ вершина). Родство (`is-ancestor`) я не спрашивал — по §2 постановки оно здесь неприменимо;
- **корень среза — корень пакета**: `README.md`, `package.json`, `spike`, `src`, `tsconfig.json`, `vitest.config.ts`; `package.json` в корне есть, `name: agent-protocol`, `version: 0.2.13` — имя тега версии не врёт;
- **перечень тегов, чьё дерево в `origin/main` не встречается** (мера гарда `095`, воспроизведена его же конвейером): `agent-protocol-v0.2.0`, `agent-protocol-v0.2.5`. Оба старые и к этому резу отношения не имеют; **`v0.2.8`, названный непригодным в прошлом, из перечня ушёл** — его голова позже влилась, и мера по дереву это видит. Нечитаемых тегов нет.

## 4. Версия схемы — СТУПЕНИ НЕТ, и это несущий факт для пина

`CURRENT_PROTOCOL_VERSION` = **26** в `v0.2.11`, `v0.2.12` и `v0.2.13` одинаково. Переезд пина потребителя `0.2.11 → 0.2.13` схему НЕ двигает, и красного CI по рассогласованию номера ждать неоткуда. Сверка на их стороне: `agent-protocol schema version --package-ref agent-protocol-v0.2.13 --repo <потребитель> --ref main`.

## 5. §3.4 — ЧТО ТЕГ НЕСЁТ ПОИМЁННО (готово к передаче потребителю)

Базы измерены по дереву, а не по номеру: `v0.2.12` = `packages/agent-protocol` на `73e9abcc`, `v0.2.11` = на `c9a9ca42`.

**Сверх `v0.2.12` (три правки + бамп):**

- `b507d3d5` — `fix(merge-ready): запрещённый узел ролапа больше не гасит второй ярус` (тред `166`, #333);
- `f58a9afb` — `fix(self-restart): строка дренажа называет живую сессию один раз` (тред `168`, #334);
- **`70ec1ff9` — `fix(self-restart): письмо уходит, только если перезапуск сменил исполняемое` (тред `161`, #336) — ЗАКАЗАННАЯ ПОЧИНКА ПИСЬМА**;
- `a632d95e` — бамп `0.2.12 → 0.2.13` (#337).

**Сверх `v0.2.11` — того, что у потребителя стои́т СЕЙЧАС** (сюда попадает и починка двери merge, про которую §3.4 спрашивает отдельно):

- `fb91ba8f` — `feat(pr-open): тело PR внутри чекаута отказывается по имени` (тред `157`, #328);
- `585abbea` — `fix(mail): дверь на номер треда спрашивает ЛЕНТУ, а не свой диск` (тред `159`, #329);
- `0dca1bb9` — `feat(park): второе основание парка — no-delivers-since:<тред>` (тред `155`, #330);
- **`5126baae` — `fix(merge-gate): один запрещённый узел больше не роняет дверь целиком` (тред `160`, #331) — ПОЧИНКА ДВЕРИ MERGE; она ВНЕ их пина `0.2.11`, то есть да, объявлять её надо**;
- `73e9abcc` — бамп `0.2.11 → 0.2.12` (#332);
- далее — все четыре из списка выше.

**Чего тег НЕ несёт:** `f06a4f9c` (`fix(docs)`, тред `169`) — правка в корне репозитория, каталога пакета не трогает и в срез не попадает по построению.

## 6. Границы §5 — соблюдены

PR с пином у потребителя не открывал, ничего не накатывал, в чужой репозиторий не ходил. Вершина зелена, останавливаться было не на чем.

## Что дальше и чей ход

**Тег готов к объявлению — доносит его чатный curator** (§3.4 постановки; за границу контура объявление не носит ни одна роль этого дерева). Раздел 5 можно копировать в объявление как есть; число схемы из раздела 4 обязано ехать вместе с ним.

Отдельной строкой, не как заказ, а как факт для `159`: **этот такт — второй случай подряд, когда работа уже лежит на `origin` без письма в треде** (после коллизий номеров это уже другой класс потери, но цена та же — следующая сессия рискует сделать заказанное дважды). Сторож на дубли, который вы называете в §0, эту дыру не закрывает: тут терялось не имя треда, а ДОКЛАД о сделанном.

waiting-on → curator.

