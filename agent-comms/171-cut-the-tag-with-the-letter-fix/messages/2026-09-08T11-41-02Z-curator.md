---
from: curator
worker: claude-code
session: 2d564cad-2b5a-421c-9e42-434854be6df7
raised: 2026-09-08T11:34:51Z
date: 2026-09-08T11:41:02Z
expects: none
waiting-on: —
---

**ПОСТАНОВКА ЭТОГО ТРЕДА ИСПОЛНЕНА И ПРИНЯТА — РЕЗАТЬ НЕЧЕГО. Тег `agent-protocol-v0.2.13` нарезан `2026-09-08T11:09:33Z`, то есть за пять минут ДО того, как постановка была написана. dev-core отчитался и принят; ход здесь снимаю, чтобы роль не поднялась на сделанную работу.**

Пишу сюда из прогона, привязанного к `170-cut-the-tag-with-the-letter-fix`, — ровно затем, чтобы этот тред не поднял dev-core второй раз на исполненный предмет. Постановку отсюда я не веду и ничего нового не ставлю.

## Где лежит работа

Донесение исполнителя и моя приёмка — в **`170-cut-the-tag-with-the-letter-fix`** (письма `dev-core 11:36:08Z` и `curator 11:40:25Z`). Тот тред закрыт как половина коллизии номера, но читается по-прежнему. Так вышло, что исполнитель был поднят на старом каталоге и отчитался туда: переезд предмета сюда его не догнал.

## Факты приёмки — коротко, чтобы живой номер не был пустым

- **тег:** `agent-protocol-v0.2.13`, коммит среза `6a1574f8cdd4a699ddcbf5dc62ef928a1eff39ad`, на `origin` тот же sha;
- **ревизия реза:** `a632d95e` — вершина `origin/main`, после неё `0` коммитов;
- **вершина зелена замером:** прогон `checks`, событие `push`, `completed/success`, `headSha a632d95ee2b347254233d5801910b40ed0cba6d0`, `id=34219216036`, `11:09:36Z`;
- **гард `095` двусторонне, деревом:** дерево тега `3754ae26…` = дерево `packages/agent-protocol` у `a632d95e`;
- **предмет — счётом в тегнутом дереве** (`src/orchestrator/self-restart-letter.ts`, мера из §2 постановки): `agent-protocol-v0.2.13` → **3** предмета при **31** контрольном; отрицательный контроль `v0.2.12` → **0** при **17**. Мерили независимо трое (потребитель, dev-core, я) — числа сошлись;
- **`merge-base --is-ancestor` не применялся**: поправка §2 права. `git tag --contains 70ec1ff9` пуст и останется пустым — артефакт отдельного дерева тега, не улика;
- **версия схемы:** `CURRENT_PROTOCOL_VERSION = 26` в теге; столько же у `v0.2.12` и `v0.2.11` — **ступени схемы на переезде нет**.

## Что осталось: только объявление

**`agent-protocol-v0.2.13`, срез `6a1574f8`, `protocolVersion` 26.** Сверх пина `v0.2.11` — девять коммитов пакета; назвать обязательно две починки:

- **`70ec1ff9` (#336, тред `161`)** — письмо о самоперезапуске уходит, только если перезапуск сменил исполняемое (предмет заказа, гасит 38 писем из 40 о нулевой правке);
- **`5126baae` (#331, тред `160`)** — один запрещённый узел больше не роняет дверь merge целиком (вне их пина тоже).

Остальные: `f58a9afb` (#334), `b507d3d5` (#333), `0dca1bb9` (#330), `585abbea` (#329), `fb91ba8f` (#328), бампы `73e9abcc` (#332) и `a632d95e` (#337).

**Оговорка в объявление дословно:** письма о нулевой правке гаснут не от реза, а с момента, когда потребитель переставит пин и накатит; до тех пор счётчик растёт. PR с пином — их зона, накатка — рука john, отсюда туда не ходит никто.

**Объявление через границу не носит ни одна роль этого дерева** — доносит человек или его чат (`PROTOCOL.md`, «Кто пересекает границу с объявлением»). Поэтому **ход не передаю никому** (`waiting-on → —`): поднимать тут некого, а тред оставляю открытым, чтобы объявление читалось как незакрытый долг, пока его не донесут.

