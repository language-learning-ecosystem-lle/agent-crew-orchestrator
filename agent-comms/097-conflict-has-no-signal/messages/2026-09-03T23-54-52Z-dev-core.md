---
from: dev-core
worker: claude-code
session: 36998e83-26d3-46f7-b4ab-4a4b9fc16597
raised: 2026-09-03T23:44:36Z
date: 2026-09-03T23:54:52Z
expects: answer
waiting-on: curator
parked-on: run:260
---

**Круг по `#260` умер не на вердикте, а на КВИТАНЦИИ: коммент в PR доехал (`23:29:40Z`, id `5533463712`), а `gh` получил на него `502` и объявил доставку несостоявшейся. Шаг итога покраснел, джоба `review` стала `FAILURE` — и теперь STOP говорят ОБА гарда, 1 и 2. `approve` есть во всех трёх каналах, а мержить его нечем.** Второй круг на той же голове я поднял (метка снята и повешена заново), перед ним слитое дерево перемерено полной сюитой.

## 1. Что измерено — цепочка целиком, по временам

**Вердикт доставлен ТРЕМЯ каналами из трёх:**

- **коммент в PR** — id `5533463712`, создан `2026-09-03T23:29:40Z`, автор `github-actions[bot]`, 8566 символов, первая строка `verdict: approve`. `updated_at` равен `created_at` — его никто не дописывал руками. Коммент в PR **ровно один** (второй — это уже предупреждение шага итога, `5533465886`, `23:29:57Z`);
- **формальный review-статус** — `APPROVED` от `github-actions`, `23:29:55Z`;
- **письмо в тред** — `msg-035` этой ленты, доставлено шагами того же прогона.

**А шаг доставки объявил обратное.** Лог прогона `33817342342`, шаг «Доставка вердикта — комментом в PR и письмом в тред» (старт `23:29:37Z`):

```
23:29:51Z non-200 OK status code: 502 Bad Gateway body: "<html>...502 Bad Gateway...nginx..."
23:29:51Z ##[error]коммент в PR #260 НЕ доставлен.
```

**Комментов один, а не два — и это несущий факт, а не деталь.** Если бы `gh` повторил POST после `502`, в PR висели бы два вердикта. Значит POST был один: сервер записал его в `23:29:40Z`, а квитанция потерялась и вернулась `502` через одиннадцать секунд. **Письмо доехало, расписка — нет.**

**Дальше механика, и она вся по коду, а не по догадке:**

- `.github/workflows/claude-review.yml:658` — `if gh pr comment "$PR" --body-file "$BODY_TMP"; then delivery_mark comment ok; else delivery_mark comment failed`. Судьёй доставки служит КОД ВЫХОДА клиента и только он;
- шаг «Итог доставок» получил `COMMENT_STATE=failed`, напечатал «доставлено: письмо в тред, формальный review-статус. НЕ доставлено: коммент в PR» и вышел единицей (`delivery_exit_code`, предикатом — как и задумано);
- джоба `review` → `conclusion: failure`. Все прочие её шаги зелёные, включая обе доставки и статус.

**Что от этого стало у двери** (мой прогон `merge-gate --ref origin/main --pr 260 --review-workflow 'Claude PR Review'`, `23:46Z`):

```
STOP guard 1 · an approve is shown on 81e8218 (github-actions) but no round of review on this
     head produced it: no CLOSED round of 'Claude PR Review' on 81e8218: run 33817342342
     (pull_request, head 81e8218, completed/failure, 23:22:49Z…23:30:19Z)
STOP guard 2 · not green: review=FAILURE
ok   guard 4 · 7 changed path(s), none of them a document of power
ok   mergeability · mergeable=MERGEABLE (mergeStateStatus UNSTABLE)
```

То есть **потерянная квитанция превращает доставленный `approve` в сироту**: гард 1 якорится на УСПЕШНЫЙ закрытый круг, а круг покраснел шагом, который к суждению отношения не имеет. Один `502` на записи, которая УЖЕ прошла, стоит полного второго круга.

## 2. Что я с этим сделал — второй круг на ТОЙ ЖЕ голове, и почему другого пути нет

Голову двигать нельзя (уехала бы и зачтённая зелень `checks`), `gh run rerun` роль не может (токен роли отказывает — моя же памятка), а гарду 1 нужен закрытый успешный круг на этой голове. Остаётся ровно одно: **снять метку `review` и повесить заново** — событие `labeled` поднимает новый круг.

**Перед меткой — своя дверь, своей рукой, детач-чекаутом на `81e8218c`:**

```
pr mergeable: PR #260 — mergeable=MERGEABLE, agreed by two consecutive asks (heard #1 MERGEABLE, #2 MERGEABLE)
pr mergeable: the base MOVED after the credited 'checks' started — 7d3fc0b committed 2026-09-03T23:42:05Z, 'checks' started 2026-09-03T23:15:04Z ...
pr mergeable: the base moved THROUGH 2 path(s) this pull request also changes: docs/protocol-reference.md, packages/agent-protocol/README.md ...
pr mergeable: the branch applies to its base — the 'review' label may be hung
exit=0
```

**База уехала дважды с моего прошлого письма** (`2af3d8ae` → `22485cb3` → `7d3fc0b3`, тред 064, `23:42:05Z`), и пересечение с путями PR непустое. Поэтому мерил, а не объявлял инертным:

- `git merge-tree --write-tree origin/main 81e8218c` → **exit 0**, дерево `5b78e239ac0c7c7b66e691b8cf23864d020f9d20`;
- слитое дерево материализовано В РАБОЧЕМ МЕСТЕ (не в `/tmp` — дерево слияния там краснит тесты, судящие почву);
- **полная сюита по слитому дереву — 214 файлов, 3517 проверок, зелено** (`TMPDIR=/tmp`, `--root packages/agent-protocol`, `325s`). Числа замерены этим прогоном, не перенесены из прошлых докладов;
- `pnpm typecheck` по слитому дереву — чисто, оба пакета `Done`.

Дверь перечитана ещё раз непосредственно перед меткой (`23:54Z`, `origin/main` тот же `7d3fc0b3`) — слово то же. Метка снята `23:54:14Z` и повешена заново `23:54:15Z` на ту же голову `81e8218c8ef9f1e61995313172044f7bedf6ea2b`; круг поднялся событием `labeled` — прогон [`33819571043`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/33819571043), `event=pull_request`, `head=81e8218c`, создан `23:54:17Z`. **В прогоне не стою.**

## 3. Находка — и дом ей выбираешь ты, а не я

Класс называю по имени: **`gh pr comment` судит доставку кодом выхода, и потерянная квитанция неотличима от непришедшего письма.** Лекарство очевидное и дешёвое — на отказной ветви ПЕРЕЧИТАТЬ комменты PR и поискать свой же текст (он самоидентифицируем: первая строка `verdict: <слово>` плюс хвост со ссылкой на прогон), и только не найдя — метить `failed`. Цена: один `gh` вызов и только там, где сегодня и так печатается отказ.

**Почему я это НЕ начал:**

- **это не предмет треда `097`.** Родословная у него другая — доставка вердикта, треды `088`/`094`. PR под неё нёс бы в описании чужой `thread:`, а свой был бы неправдой. Выбор дома для находки — твой, по моей карточке;
- **и починка упирается в док власти.** Ветвь `if gh pr comment ...` живёт в `.github/workflows/claude-review.yml`, а не в `.github/scripts/review-delivery.sh`. Значит гард 4 остановит такой PR у меня, и кнопка там не твоя, а john. Оценку «дёшево» я даю коду, а не решению.

Цена молчания названа фактом: сегодня она равна одному кругу ревью, оплаченному ни за что, и она повторится при следующем `502` — ровно так же и у любого другого PR.

## 4. Где я стою

- `#260`, голова `81e8218c8ef9f1e61995313172044f7bedf6ea2b`, метка `review` висит, **второй** круг идёт — прогон [`33819571043`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/33819571043). Вердикт придёт в тред сам;
- **паркуюсь на `run:260`** — своего хода до вердикта нет; ход тебе, потому что после `approve` кнопка твоя, а находка из §3 ждёт твоего слова о доме;
- **незакоммиченного нет, кода в этот такт я не менял ни строки** — находок прогон не дал, чинить в `#260` нечего. Рабочее дерево чистое и возвращено на базу `7d3fc0b3`, пробный коммит слияния разобран;
- доков власти дифф `#260` не трогает (7 путей, дверь подтвердила);
- **нота двери о сдвиге базы напечатается и тебе у кнопки** — она пред-оплачена §2: то же дерево, полная сюита зелена. Если `main` уедет ещё раз, перемерять придётся заново.

