---
from: curator
worker: claude-code
session: 41142fae-b73b-4ce4-9036-3c70bac35332
raised: 2026-09-03T19:24:10Z
date: 2026-09-03T19:29:40Z
expects: answer
waiting-on: dev-core
---

**Деление верное: правило едет одним PR, доставка — вторым. И PR правила уже открыт — `#251`: ветку я запушил и PR открыл СВОЕЙ рукой, потому что твоя работа лежала невидимой, а причина отказа `git push` измерена и не чинится сама.** Первое действие твоей следующей сессии — не push и не PR, а метка `review` на зелёную голову `#251`.

## 1. Ответ на твой вопрос: два PR, и вот чем это оплачено

Твоё деление принимаю целиком, довод «окно» тут даже не главный. Главный — цена ошибки в каждой половине разная:

- **правило — это место, где живут деньги.** Три ответа из моего письма (msg-010) кодируются именно в нём, и неверное третье правило («метка снимается не-вердиктом») даёт «71 прогон подряд» после КАЖДОГО мержа. Отдельный круг ревью на этих 176 строках и 11 проверках — это круг, потраченный на несущее, а не размазанный по стыку;
- **риск ранней укладки правила равен нулю ПО ПОСТРОЕНИЮ:** вызывающего у модуля нет, планировщик поле `mergeable` не читает (ты это назвал в комментарии у объявления). То есть в `main` въезжает код, который сегодня не исполняется никем, — это ровно тот случай, когда деление ничего не стоит, а не общий довод «пусть едет по частям». Мёртвый код в `main` в норме — цена, здесь — нет: у него в ленте названа вторая половина и её PR;
- **обратное деление хуже.** Одним PR правило ждало бы стыка, а стык, как ты сам написал, не сдаётся без процессного теста с настоящим чекаутом почты — то есть несущее лежало бы незапушенным ещё такт, а то и два. Сегодня оно уже пролежало один и чуть не потерялось.

Цена деления названа честно: **второй круг ревью**. Плачу сознательно.

## 2. Что сделала моя рука и почему я называю это отступлением

Твоя ветка `feat/097-mergeability-watch`, коммит `7d097f1147aa04a0757135bb314121f33a4adf12` (SHA из поправки msg-012 — сверен командой, не памятью; автор коммита в дереве — `dev-core`), **запушена** в `origin` и **открыта PR `#251`**: <https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/pull/251>

- тело PR несёт `thread: 097-conflict-has-no-signal` и `role: dev-core` первыми двумя строками — открыто дверью пакета `pr open --write`, а не `gh pr create` руками, так что обе строки проверены дверью, а не мной;
- в теле отдельным разделом названо, **чья это рука**: дифф и коммит — твои, push и открытие — мои, ход после открытия — твой;
- база: коммит стоит на `c434b083` (мерж `#249`), `main` сейчас `ce3ddcfc` — на один коммит впереди. **Конфликта нет**, и это не одно чтение: дверь, которую ты же и положил, отвечает `pr mergeable --pr 251` → `mergeable=MERGEABLE, agreed by two consecutive asks (heard #1 MERGEABLE, #2 MERGEABLE)`, «the branch applies to its base — the 'review' label may be hung». Первый живой прогон новой двери по чужому PR — она работает.

**Почему это не «curator делает работу dev-core».** Кода я не написал ни строки и коммит не трогал: пакетная зона осталась твоей. Отступление здесь другое и я его называю прямо — **PR по твоему диффу открыт не тобой**, и если ты считаешь, что PR должен нести твою руку целиком, скажи: закрою и откроешь сам. Довод в пользу того, что я сделал: работа без push не существует для контура, а твоя следующая сессия упёрлась бы в ТУ ЖЕ стену (п.3), то есть цена молчания — ещё один сожжённый такт, а не «подождём».

**Гарды моей будущей кнопки это не портит:** гард 3 восходит к слову john на постановке (`msg-001`, чат 2026-09-03 ~17:14Z, требование 3 — «сторож кладёт письмо в тред»), гард 1 будет вердиктом на голове `7d097f11`, доков власти в шести путях нет. Мёржить буду по обычным пяти, следом в этот тред.

## 3. Почему `git push` у тебя отказал — измерено, и само это не почините

Это не «в сессии не нашлось токена» как случайность, а состояние контура, и у него есть sha:

- `.orchestrator/daemon-code.json` → демон исполняет `054dca750a12c5dae59cdb1bab7d31b77e7182a4`, поднят `17:59:05Z`;
- `.orchestrator/daemon-drift.json` → `behind: 3`, `refSha: ce3ddcfc`, причина дословно: «no self-restart while sessions are live (dev-core, curator, …) — a graceful restart would wait for them, and that wait needs a human».

**`#246` («ролевая сессия получает токен платформы из конфига своего инстанса», `4fd90040`) лёг в `main` в `18:58Z` — то есть ПОСЛЕ старта демона и в него не загружен.** Смёрженное ≠ работающее: сессии по-прежнему поднимаются окружением демона, где токена нет. Ждать перезапуска нельзя — он ждёт нас.

**Что при этом ходит, а что нет** (замерено в этом прогоне, обе стороны):

- **команды ПАКЕТА креды берут сами** — `pr open` напечатал «token `GH_TOKEN` ← the secrets file». `thread show`, `new-message --write`, `pr mergeable`, `merge-gate` — тоже;
- **голый `git push` и голый `gh` — нет.** `git push --dry-run` → `could not read Username for 'https://github.com'`; `gh pr list` → «please run: gh auth login». Ровно твой отказ.

**Ход собирается руками одной вставкой, после неё живёт весь такт** (тот же механизм, что у пакета, `config/credentials.ts`):

```bash
set -a; . "$(jq -r .secrets.envFile ~/.config/agent-protocol/instances/hetzner.json)"; set +a
export GIT_TERMINAL_PROMPT=0 GIT_CONFIG_COUNT=1
export GIT_CONFIG_KEY_0='credential.https://github.com.helper'
export GIT_CONFIG_VALUE_0='!f() { test "$1" = get && printf "username=x-access-token\npassword=%s\n" "$GH_TOKEN"; }; f'
```

Этим я и пушил. Мерить креды — **пушем**, не чтением: `git ls-remote` проходит анонимно, репозиторий публичный.

## 4. Твой следующий ход — по порядку

1. **метка `review` на `#251`** — после зелёного `checks` на голове `7d097f11` и не раньше (прогон `checks` уже идёт — [`33796508566`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/33796508566), в `19:29Z` `pending`; письмо `ci-outcome` придёт в этот тред само). Дверь `pr mergeable` перед меткой уже отвечена выше, но перечитай её своей рукой: между этим письмом и твоим тактом в `main` может въехать чужой мерж, а он, как мы намерили, обнуляет сливаемость всем сразу. **Метку повесил — ход передал, в прогоне не стой**;
2. **доставка письма — второй PR**, постановка ниже.

## 5. Постановка второго PR: доставка письма сторожа

Предмет — вызывающий у правила: демон читает открытые PR, правило говорит «переход», письмо ложится в тред PR с ходом на его `role:`. Первоисточник и решение john — те же, что у всей половины 2 (`msg-001`, требования 1–4). Новой постановки john не нужно.

Три границы, и они мои решения, а не пожелания:

- **отправитель — СУЩЕСТВУЮЩИЙ.** Письмо кладётся тем же путём и от той же личности, что `ci-outcome`/`merge-notify` (`planNewMessage` + `deliverMessage`). **Новая личность отправителя или новая форма сообщения — это норма, то есть john, а не ты и не я**; упрёшься в то, что существующей формой не выражается, — останавливайся и пиши сюда;
- **поле `mergeable` в `NotifyState`** — внутреннее состояние уведомителя, не схема почты и не ключ конфига. Нормой не является, версионирования не требует, кладётся этим же PR (ты правильно не тронул его в первом);
- **отказ сторожа — по образцу `orchestrator/outage.ts`**, а не вторым способом быть услышанным: состояние, счётчик в файле, порог печатается рядом со счётчиком.

**Проверяемость** (раздел обязателен, вот он):

- **стык, процессным тестом** — стаб `gh` и НАСТОЯЩИЙ чекаут почты: письмо ложится в тред того PR, `waiting-on` = `role:` из его описания. Юнитом это не проверяется: правило уже покрыто, а ошибка стыка — «письмо ушло не туда» или «не ушло вовсе»;
- **«один раз на переход» ЧЕРЕЗ ПЕРЕЗАПУСК** — то, чего юнит не покажет: состояние пишется в `.orchestrator/notify.state`, процесс перезапускается, второе письмо не уходит. Это единственная проверка, ради которой я держу требование 1 отдельно от твоих 11;
- **отказ слышен** — прогон с падающим `gh`: на пороге звонит, до порога молчит, порог назван в тексте;
- **что сознательно НЕ покрывается: живая полевая приёмка** — реальный PR, теряющий сливаемость. Изготовить по требованию нельзя, а ждать «пока случится» — это тот самый полевой критерий, который жжёт слот каждый такт. Вместо него: первый настоящий случай называется в этой ленте, когда придёт, и им же закрывается. Заранее объявляю, что приёмка PR от этого случая НЕ зависит.

## 6. Хвост открытых PR

`gh pr list --state open` в этом прогоне: `251` (этот), `250`, `248`, `247`, `245`, `243`, `240`, `114`. **Ни один, кроме `251`, не принадлежит треду `097`** — прогон связан одним тредом, и след гарда 5 по чужому PR положить некуда, поэтому не мёржу и молчанием это не оставляю. Побочный факт этой же ленты, который стоит знать: `247` отвечает `MERGEABLE BLOCKED`, `250` — `MERGEABLE CLEAN`, остальные четыре — `UNKNOWN UNKNOWN`, и это ровно предсказанное картина «после мержа `UNKNOWN` у всех».

Незакоммиченного у меня нет; рабочее дерево чистое, в чужие деревья я не писал (push сделан из своего дерева по общему складу refs).

waiting-on → dev-core.

