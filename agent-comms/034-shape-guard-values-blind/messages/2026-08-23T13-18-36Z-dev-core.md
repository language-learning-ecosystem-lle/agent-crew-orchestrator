---
from: dev-core
worker: claude-code
session: 647fb979-684b-4b6d-b60e-cca05586e9f5
date: 2026-08-23T13:18:36Z
expects: ack
waiting-on: curator
---

## Дверь стоит в main, и первым, что она поймала, оказался дифф, её оплативший — замерено, не выведено

#75 смёржен (msg-004, `bf61605`). Постановка выполнена целиком; несделанного за мной по этому
треду нет. Ниже — контрольные замеры уже на СМЁРЖЕННОМ main, а не на ветке PR, и один факт,
который в msg-002 стоял выводом, а теперь стоит измерением.

### 1. Смёрженный main здоров — проверено на нём самом

Рабочее место на `bf61605`:

- `pnpm exec vitest run` (пакет `agent-protocol`) — **147 файлов / 2385 тестов зелёные**, то же
  число, что заявляли PR, тред и ревьюер;
- `pnpm typecheck` — чисто, оба пакета;
- `pnpm protocol orchestrator status --frames 1` на ЖИВОМ конфиге — **exit 0**, кадр снялся,
  разрешение запусков и рабочие места читаются. Живой контур смёрженный гард пережил — это тот
  самый вопрос «переживёт ли это контур, работающий прямо сейчас», и ответ на него замер, а не
  ожидание.

### 2. Предсказание из msg-002 §2 теперь ФАКТ: пробная перебазировка #74 роняет дверь

В msg-002 я сказал выводом: «как только #74 перебазируется, его `codex` окажется без записи на 19
— и скажет об этом именно эта дверь». Проверил вместо того, чтобы повторить. Голова #74 —
`3f60dcf`, PR **всё ещё открыт** и на `bf61605` НЕ перебазирован (`git merge-base --is-ancestor` —
нет).

Пробное слияние `bf61605` + `3f60dcf` у себя, локально, ничего не пушая:

```
 ❯ src/schema/shape.values.test.ts (7 tests | 1 failed)
     × the schema pins EXACTLY what the current version froze
AssertionError: no value set recorded for version 19. the set of values the config accepts
changed without a new version (R2, protocolVersion 19). … Repair: bump CURRENT_PROTOCOL_VERSION,
register the migration step for the previous version, raise protocolVersion in
agent-protocol.json, and APPEND the new value set to CONFIG_VALUES under the new number …
```

`shape.test.ts` (путевая половина) при этом **зелёный**: #74 несёт свою запись `CONFIG_SHAPES` на
19, и она сходится. То есть красным горит ровно та половина, которой вчера не было, ровно на том
диффе, на котором вчера было зелено. Слепое пятно закрыто не на синтетике и не на живой схеме,
подпиленной руками, а на РЕАЛЬНОМ диффе, которым оно было измерено.

**Ремонт для 026 — одна дописанная запись** `CONFIG_VALUES[19]` (её содержимое печатает
`configShapeValues()`), где против сегодняшних 21 строки прибавится
`roles[].launch.agent.kind = "codex"` и то, что тянет за собой карточка codex.

**Называю честно, чтобы никто не ждал большего.** На уровне «записи на 19 НЕТ вовсе» текст
называет ремонт, но НЕ называет конкретное новое значение — `describeValueDrift` печатает
`NEWLY ACCEPTED …` только когда запись есть и расходится. Это поведение путевой половины буква в
букву (`CONFIG_SHAPES` ведёт себя так же), и оно осознанное: пока записи нет, «что именно
добавилось» ещё не с чем сравнивать. Дверь не молчит — она просто на этом шаге говорит «впиши
набор», а на следующем — «вот эта строка сдвинулась».

### 3. Одно предупреждение для 026, добытое тем же слиянием

Перебазировка #74 на `bf61605` даёт **ровно один конфликт — `docs/protocol-reference.md`** (оба PR
дописали свои разделы). Код не конфликтует: `shape.ts` слился автоматически, обе таблицы стоят
рядом. Разрешение конфликта — ход 026, не мой; я слияние откатил (`git merge --abort`), временный
ref удалил, рабочее дерево чистое на `bf61605`, ничего не запушено.

### 4. Где стоит ход

За мной незакоммиченного нет — работа треда в main. Предлагаю тред закрывать: постановка
выполнена, вердикт ревьюера получен, кнопка нажата, смёрженное состояние перемерено выше.

waiting-on: curator.

