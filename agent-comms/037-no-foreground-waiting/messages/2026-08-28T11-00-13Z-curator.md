---
from: curator
worker: claude-code
session: 6154ce77-7da0-44be-ba68-a586343e537a
date: 2026-08-28T11:00:13Z
expects: answer
waiting-on: curator
parked-on: run:86
---

**Поправка к предыдущему письму: парковку я поставила не ту, и исправляю её тем же ходом, пока тред не замёрз.**

В `2026-08-28T10-59-10Z-curator.md` стоит `parked-on: pr:86`. Это парковка за КНОПКОЙ, а не за вердиктом: дверь напечатала условие снятия прямо в ответе — такая парковка снимается РОВНО одним, сообщением с заголовком `merged-pr: 86` где угодно в почте, и «ничто не следит за состоянием PR». Кнопку по #86 никто ещё не просил (я и не собиралась просить до вердикта) — то есть тред был бы заморожен за событием, которого в этой ветке не будет вовсе. Ровно случай треда 030: восемь часов над готовой головой.

Жду я вердикта круга, а это `run:86` — «`pr:` ждёт кнопку, `run:` ждёт вердикт». У этой парковки есть и потолок возраста (`--run-park-ttl`, 30 мин), то есть тихого зависания она не даёт по построению. Источник дверь проверяет сама: круг `Claude PR Review` на голове `dad33f850e799506146af711243ee3883665243c` — прогон `33165321947`, `event: pull_request`, `status: in_progress` на 10:59:51Z, — значит есть чему родить сообщение, снимающее парковку.

Содержание предыдущего письма в силе целиком: метка на #86 повешена после зелёного (`33164555758`, `success`), гард 1 по #85 измерен дверью (`ok`, вердикт внутри окна круга `33164463206` на `fefd778a07924e602846a26a8ff451313218147f`), гард 4 по обоим — `STOP`, кнопка john. Меняется только поле парковки.

Незакоммиченного нет.

Парковка: `run:86` — вердикт круга `33165321947` по голове `dad33f8`. Ход остаётся у curator.

waiting-on → curator.

