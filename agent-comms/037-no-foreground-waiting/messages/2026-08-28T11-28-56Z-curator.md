---
from: curator
worker: claude-code
session: 60f606de-c373-4437-909f-b63af01163a8
date: 2026-08-28T11:28:56Z
expects: none
waiting-on: —
---

**След гарда 5 по #85 и #86 и приёмка треда: правило john стоит в base обеими половинами, замерено командой. Тред закрываю своим тактом — постановка исполнена целиком.**

Мёрж исполнен **кнопкой john** (его слово — msg-012 этого треда, доставка чата ~11:24Z полем `delivers`; событиями — msg-010 и msg-011). Я не мёржила ни один из двух: гард 4 `STOP` на доках власти, и он стоял до конца.

## Гард 5 — след, снятый вызовами, а не пересказом

`gh pr view <n> --json state,baseRefName,mergedAt,mergeCommit,headRefOid,mergedBy`, снято моей рукой 2026-08-28 ~11:33Z:

**#85** (`PROTOCOL.md`, `packages/agent-protocol/README.md` «S19», `launch.ts`/`launch.test.ts`)
- `state: MERGED`, **`base.ref: main`** — контрольное чтение цели после мёржа, как требует норма: ответ инструмента ≠ факт;
- `mergedAt: 2026-08-28T11:21:22Z`, коммит мёржа `0bc8a4a30dba3c858eba5a4291aed7a7058c7cce` (он же `0bc8a4a` в `origin/main`);
- голова, которую смёржили: `fefd778a07924e602846a26a8ff451313218147f` — **ровно та, на которой лежал `approve`** (гард 1, круг `Claude PR Review` `33164463206`, 10:43:50Z…10:48:09Z, `event: pull_request`; вердикт — msg-004) и на которой зелёный `checks` `33163283820` (гард 2). Голова после вердикта не двигалась;
- гард 3 — слово john в msg-001 (чат ~10:16Z, дословно приведено там же); гард 4 — `STOP`, снят кнопкой владельца, а не исполнителем.

**#86** (`docs/roles/curator.md`, `docs/roles/dev-core.md`)
- `state: MERGED`, **`base.ref: main`**;
- `mergedAt: 2026-08-28T11:21:37Z`, коммит мёржа `66044936b97fb0b70f9915084db98ce3ecca8d87` (`6604493`);
- смёржена голова `dad33f850e799506146af711243ee3883665243c` — та же, что несла `approve` (круг `33165321947`, 10:57:33Z…11:01:15Z, вердикт — msg-008) и зелёный `checks` `33164555758`;
- гарды 3 и 4 — те же, что у #85: один источник решения, одна кнопка john.

**Чью руку называет `mergedBy`, я не заявляю:** оба PR подписаны аккаунтом `maysway`, а этим аккаунтом приходят и кнопка curator, и рука john — поле руку не различает. Восхождение здесь идёт к СЛОВУ в ленте (msg-012), а не к полю, и это единственный честный якорь.

## Что реально лежит в `origin/main` — проверено чтением файлов, а не номерами PR

`git fetch` + `git show origin/main:<file>`, 2026-08-28 ~11:34Z:
- `PROTOCOL.md:161` — подпункт «**ЖЕЛЕЗНОЕ ПРАВИЛО: НИКАКАЯ роль не ждёт НИКАКОГО прогона, который и так напишет в тред**» со словом john дословно, полевым случаем (#83, #81/#82), ценой, границами (свои короткие операции, R19, аварийный класс со словом john), оговоркой про факт установки (`docs/install-notes.md`) и проверяемостью полем;
- `docs/roles/curator.md:205` — «Метка — это конец хода, а не начало ожидания», `:248` — правило в разделе работы поднятой сессии;
- `docs/roles/dev-core.md:87–92` — то же правило со ссылкой на `PROTOCOL.md`;
- `packages/agent-protocol/src/orchestrator/launch.ts:1028` — `runEndsNorm` с фразой «AND THE FOREGROUND ENDING IS NEVER FOR SOMEONE ELSE'S RUN…» и выходом (`--parked-on run:<N>`/`pr:<N>`).

## Первая полевая приёмка — на этой самой сессии

msg-009 замерял обратное: промпт той сессии ещё нёс старую формулировку, то есть контур авторизовал ровно то, что тред запрещает. **Эта сессия поднята в 11:25:50Z — после мёржа #85 в 11:21:22Z — и её промпт несёт новый текст дословно**, я сверила строку промпта с `origin/main:launch.ts` посимвольно (совпадение точное, включая `NEVER FOR SOMEONE ELSE'S RUN` и `--parked-on run:<N>`). До/после замерено на одном и том же артефакте: правило доехало не только до документа, но и до текста, который контур выдаёт каждой поднимаемой роли.

Сама я в этот такт у прогонов не стояла: все факты сняты по одному разу командами чтения.

## Хвост из msg-009 закрыт фактом, но не мной

`gh pr list --state open` → **`[]`**, открытых PR в репозитории нет. **#81 и #82 смёржены** (`11:21:57Z` и `11:22:10Z`, оба `base.ref: main`) — те самые «готовые зелёные», из-за которых очередь стояла за ждущей сессией. Чья рука — по полю не устанавливается (см. выше), и след гарда 5 по ним принадлежит треду `035`, а не этому: в чужой тред я в этом прогоне не пишу. Полевой случай, породивший правило, на стороне очереди исчерпан — незакрытых кандидатов на мёрж у меня нет.

## Что закрытие НЕ покрывает — называю явно, чтобы не потерялось

- **зеркало нормы в контур LLE** (там свой `PROTOCOL.md` и свои карточки) — по карте из msg-012 доносится стыковой ролью ПОСЛЕ приезда пина `v0.2.5`, иначе норма разъедется с кодом. Это работа чужого контура и живёт в его треде, не здесь;
- **полевая приёмка по определению не завершается**: «в дайджестах не должно появляться сессий, чей единственный смысл — ожидание CI». Проверка идёт разбором застоев: «сидела в foreground ради круга» с этой минуты — находка, а не объяснение. Отдельного треда под это не завожу: норма уже стоит, а нарушение будет иметь свой.

Незакоммиченного у меня нет, рабочее дерево роли чистое (`git status --porcelain` пуст), почта тронута только двумя командами.

Тред закрываю: `thread status --status closed` следом за этим сообщением. Ход отсюда не уходит.

