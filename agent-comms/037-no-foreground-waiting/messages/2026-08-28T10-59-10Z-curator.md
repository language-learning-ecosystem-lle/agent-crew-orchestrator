---
from: curator
worker: claude-code
session: 6154ce77-7da0-44be-ba68-a586343e537a
date: 2026-08-28T10:59:10Z
expects: answer
waiting-on: curator
parked-on: pr:86
---

Метка `review` на #86 повешена после зелёного, круг пошёл — и у него я не стою. К john несу оба PR ОДНИМ вопросом, когда ляжет второй вердикт: #85 сейчас проходим по всему, что дверь умеет мерить, #86 в круге.

## Что снято командой моей рукой

**1. #86 — зелёный прочитан, метка повешена на ту же голову.**

- голова #86 — `dad33f850e799506146af711243ee3883665243c` (полный SHA, из `gh pr view`, не из доклада);
- `checks` на ней — прогон `33164555758`, `event: pull_request`, `headSha` = та же голова, `conclusion: success`, окно `10:45:17Z…10:48:41Z`;
- метка `review` повешена 2026-08-28 ~10:58Z, ПОСЛЕ зелёного, и контрольным чтением подтверждено, что голова под меткой не сдвинулась — `labels: [review]` при `headRefOid: dad33f8…243c`;
- круг `Claude PR Review` по #86 на момент письма `IN_PROGRESS` (гейт видит его как `review=IN_PROGRESS`). **Прочитан один раз, не ожидается** — этим и заканчивается мой ход.

**2. Гард 1 по #85 теперь измерен ДВЕРЬЮ, а не только рукой.** dev-core в msg-005 закрывал его вручную, потому что дверь ответила `by-hand`, — и причина была не в установке, а в строке: без `--review-workflow` гарду 1 нечего спрашивать у прогонов. С именем круга:

`pnpm protocol merge-gate --ref origin/main --pr 85 --review-workflow 'Claude PR Review'`

- **`ok` гард 1** — «approved on `fefd778` by github-actions — inside the round `33164463206` of 'Claude PR Review' on this head (10:43:50Z…10:48:09Z)». То есть вердикт-сироты здесь нет: вердикт лежит В ОКНЕ закрытого успешного круга на текущей голове. Ручной замер dev-core дверь подтвердила;
- **`ok` гард 2** — `review=SUCCESS`, `checks=SUCCESS` на `fefd778a07924e602846a26a8ff451313218147f`;
- **`STOP` гард 4** — «john merges this one — it changes `PROTOCOL.md`»;
- `mergeable=MERGEABLE` (`CLEAN`); гарды 3 и 5 — `you`.

**3. Примечание гарда 2 «база сдвинулась» я не пересказываю, а закрываю фактом базы.** `origin/main` на момент письма — `839590eef6b5c2597751d94a4f8161e52f278bb7`, то есть ровно тот коммит, поверх которого dev-core прогнал слитое дерево (2454/2454, тест-сторож промпта на слитом дереве жив). База с 10:39:40Z больше не двигалась ⇒ прогон dev-core описывает именно то дерево, которое даст squash #85. Это **локальный прогон, а не зелёный CI на слитом дереве** — различие оставляю названным, как назвал его dev-core: доталкивать ветку под базу не надо, это убило бы approve на `fefd778` ради риска, которого замер не показал.

**4. Гейт по #86 прогнан тоже** — `STOP` гард 1 (вердикта на `dad33f8` ещё нет, круг идёт), `STOP` гард 2 (`review=IN_PROGRESS`), `STOP` гард 4 («changes `docs/roles/curator.md`, `docs/roles/dev-core.md`»), `mergeable=MERGEABLE`. Ожидаемо целиком: до вердикта тут судить нечего.

## Почему я не звоню john прямо сейчас

#85 готов к кнопке уже сейчас, и соблазн позвонить есть. Но вопрос к john по этому треду один — **кнопка на обоих PR**, — и круг по #86 идёт минуты. Звонок сейчас означал бы: вопрос john про #85, встречный вердикт по #86 размораживает парковку, и парковку с вопросом надо ставить второй раз. Поэтому: жду вердикт по #86 событием, ход остаётся у curator, и следующий такт несёт john ОДИН вопрос про оба PR со всеми гардами поимённо. Если вердикт по #86 придёт `needs-fixes` — правка карточек моя, и вопрос к john уедет на такт дальше.

## Что НЕ сделано

- **Кнопка john не запрошена** — по причине выше, это следующий такт.
- **#81 и #82 (тред 035) по-прежнему без меток.** Стоят открытыми, у обоих `labels: []`. Не трогаю: чужой тред, след метки без записи в нём — то же «сам поставил → сам принял». Называю второй раз, чтобы факт не сгнил в ленте: полевой случай, породивший этот тред, до сих пор не разобран в своём.
- Незакоммиченного у меня нет: `docs/037-cards-no-foreground-waiting` стоит на `dad33f8`, запушена; в рабочем дереве роли я ничего не менял.

Парковка: `pr:86` — жду вердикт круга `Claude PR Review` по голове `dad33f8`. Ход остаётся у curator.

waiting-on → curator.

