---
from: curator
worker: claude-code
session: 0576d157-dae8-41c9-b9af-89b74b43ed45
date: 2026-08-28T11:03:32Z
expects: ack
waiting-on: curator
parked-on: john
---

**Требуется решение john: нажать merge на двух PR — #85 (`PROTOCOL.md`) и #86 (`docs/roles/**`). Оба `approve` на текущих головах, оба зелёные, оба остановлены гардом 4 (доки власти — кнопка твоя, не моя). Больше по этому треду ждать нечего: обе половины постановки на столе.**

Оба вердикта легли, и я несу к тебе ОДИН вопрос про оба PR, как и обещала в предыдущем письме.

## #85 — норма (`PROTOCOL.md`, промпт запуска, README, тест-сторож)

Дверь: `pnpm protocol merge-gate --ref origin/main --pr 85 --review-workflow 'Claude PR Review'`, снято моей рукой 2026-08-28 ~11:06Z.

- **гард 1 `ok`** — approve на `fefd778a07924e602846a26a8ff451313218147f`, внутри окна ЗАКРЫТОГО успешного круга `Claude PR Review` `33164463206` (10:43:50Z…10:48:09Z), `event: pull_request`. Вердикта-сироты нет — вердикт про эту голову, а не про заменённое дерево. Вердикт в ленте: `2026-08-28T10-52-…-reviewer-pr.md` (msg-004 этого треда), «Находок, требующих правок, нет»;
- **гард 2 `ok`** — на той же голове `review=SUCCESS`, `checks=SUCCESS` (прогон `checks` — `33163283820`);
- **примечание гарда 2 «база сдвинулась» закрыто фактом, а не пересказом:** `origin/main` сейчас `839590eef6b5c2597751d94a4f8161e52f278bb7` — ровно тот коммит, поверх которого dev-core прогнал СЛИТОЕ дерево (msg-005): `pnpm vitest run` 2454/2454, typecheck и biome чисто, тест-сторож промпта на слитом дереве жив. База с 10:39:40Z не двигалась, я это перепроверила своей рукой перед этим письмом. Это **локальный прогон, а не зелёный CI на слитом дереве** — различие называю прямо. Доталкивать ветку под базу НЕ надо: это сдвинет голову, убьёт approve на `fefd778` и потребует нового круга ради риска, которого замер не показал;
- **гард 3** — восхождение к твоему слову: msg-001 этого треда, чат 2026-08-28 ~10:16Z, дословно «никакая роль не должна ждать никакого прогона, если он и так напишет в тред. Это должно быть железным правилом.» Постановку писала я, но решение — твоё и названо первоисточником;
- **гард 4 `STOP`** — «john merges this one — it changes `PROTOCOL.md`». **Кнопка твоя.** Я мёржить не вправе: роль не принимает документы, определяющие её собственные полномочия;
- **гард 5** — след мёржа за тем, кто жмёт; если нажмёшь ты, след напишу я следующим сообщением;
- `mergeable=MERGEABLE` (`CLEAN`).

## #86 — отражение в карточках (`docs/roles/curator.md`, `docs/roles/dev-core.md`)

Дверь той же строкой по `--pr 86`, снято 2026-08-28 ~11:05Z.

- **гард 1 `ok`** — approve на `dad33f850e799506146af711243ee3883665243c`, внутри окна закрытого круга `Claude PR Review` `33165321947` (10:57:33Z…11:01:15Z). Вердикт — msg-008 этого треда, «Находок, требующих правок, нет»;
- **гард 2 `ok`** — `review=SUCCESS`, `checks=SUCCESS` на той же голове (прогон `checks` — `33164555758`);
- **гард 3** — тот же источник: твоё слово в msg-001. #86 — вторая половина той же постановки (дом нормы — `PROTOCOL.md` + отражение в карточках);
- **гард 4 `STOP`** — «john merges this one — it changes `docs/roles/curator.md`, `docs/roles/dev-core.md`». **Кнопка твоя**, и здесь это особенно буквально: PR правит МОЮ собственную карточку;
- `mergeable=MERGEABLE` (`CLEAN`).

Порядок мёржа между ними безразличен: файлы не пересекаются, обе ветки `MERGEABLE` поверх `839590e`. Если после первого мёржа GitHub попросит обновить вторую — скажи, разберу я, руками ветку под базу не двигаю (см. выше про approve).

## Что этот тред уже изменил в контуре

Правило стало не только текстом: dev-core в #85 сузил foreground-концовку в самом промпте запуска (`runEndsNorm`) — до этого контур ВЫДАВАЛ каждой сессии разрешение «a watch, a command you run and wait out» без вопроса, чья это джоба. Я подтверждаю дефект как потребитель: промпт этой моей сессии всё ещё несёт старую формулировку — то есть до мёржа #85 контур авторизует ровно то, что тред запрещает.

Сама я в этом такте у прогонов не стояла ни разу: оба круга прочитаны по одному разу командой, ход передавался парковкой (`run:85`, `run:86`).

## Что НЕ сделано и остаётся на потом

- **#81 и #82 (тред 035) по-прежнему без меток `review`** — те самые готовые зелёные PR из полевого случая, породившего этот тред. Проверено сейчас: оба `OPEN`, `labels: []`. Не трогаю третий такт подряд, и причина та же — чужой тред, метка без следа в нём это «сам поставил → сам принял». **Называю в третий раз, потому что факт начинает тухнуть: полевой случай про застрявшую очередь до сих пор не разобран в своём треде.** Если считаешь, что тред 035 надо поднять вне очереди — скажи, у меня есть `thread-priority`;
- незакоммиченного у меня нет: обе ветки запушены и стоят на своих головах (`fefd778`, `dad33f8`), в рабочем дереве роли я ничего не меняла.

Парковка: `john` — жду кнопку на #85 и #86. Ход остаётся у curator (роль с `wake.mode: self` в `waiting-on` не принимается).

waiting-on → curator.

