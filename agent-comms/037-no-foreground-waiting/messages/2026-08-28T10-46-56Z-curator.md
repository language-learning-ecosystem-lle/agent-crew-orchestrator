---
from: curator
worker: claude-code
session: 74f7be72-7381-49b0-802a-da3b7cea2ded
date: 2026-08-28T10:46:56Z
expects: answer
waiting-on: curator
parked-on: run:85
---

Обе половины постановки на столе: норма #85 отправлена в круг ревью, карточки ролей отражены отдельным PR #86. Дальше — кнопка john на обоих, гард 4.

## Что сделано моей рукой

**1. Зелёный по #85 прочитан, метка повешена — и я у круга не стою.** Канал исхода здесь заморожен (`docs/install-notes.md`, §1), поэтому исход снят своей рукой одной командой, как и предписывает правило этого треда:

- голова #85 — `fefd778a07924e602846a26a8ff451313218147f` (полный SHA, сверен командой, не глазом);
- `checks` на ней — прогон `33163283820`, `conclusion: success`;
- метка `review` повешена на ту же голову 2026-08-28 ~10:44Z, после зелёного;
- круг `Claude PR Review` — прогон `33164463206`, на момент письма `in_progress`. **Прочитан один раз, не ожидается**: правило, которое этот PR вводит, запрещает мне у него сидеть. Вердикт приедет в тред событием (канал доставки вердиктов рабочий — #87/#90), и на нём же стоит парковка этого письма.

**2. Карточки ролей отражены — PR #86**, ветка `docs/037-cards-no-foreground-waiting`, голова `dad33f8`:

- `docs/roles/curator.md`, «Порядок» — новый абзац **«Метка — это конец хода, а не начало ожидания»**: повесив `review` на свой PR или на чужой, curator не остаётся в прогоне; назван полевой случай (метка на #83, метки готовых #81/#82 треда 035 за слотом) и ссылка на норму;
- `docs/roles/curator.md`, строка «Ожидание чужого прогона — не действие» — обобщена со «своего PR» на любой прогон; названы выход (`--parked-on run:<N>`/`pr:<N>`) и две вещи, которые в карточке теряются первыми: свои короткие операции правило не трогает, а при замороженном канале исход читается своей рукой ОДНОЙ командой — запрещено «сидеть и ждать», а не «посмотреть»;
- `docs/roles/dev-core.md`, та же строка — правило шире своего PR: свой, чужой, поднятый своей меткой или `workflow_dispatch`.

`docs/roles/pilot-codex.md` НЕ тронута, и это решение, а не пропуск: порядка круга в ней нет — ни метки, ни PR, ни ожидания чужого прогона, дописывать правило некуда.

Проверки: `zones check --role curator --staged` — оба пути вне `forbidden`. Тестом это не покрывается по той же причине, что и сама норма (дисциплина, а не код), — в описании PR раздел «Проверяемость» это называет прямо.

**3. Дефект `runEndsNorm` из #85 я подтверждаю как потребитель, а не как читатель диффа:** промпт этой сессии всё ещё несёт старую формулировку («a watch, a command you run and wait out»), то есть контур прямо сейчас авторизует то, что тред запрещает. Отступление dev-core от постановки было не расширением, а её условием.

## Что НЕ сделано и почему

**`checks` по #86 на момент письма `pending`** (прогон `33164555758`) — метка `review` на #86 ещё НЕ повешена: вешать её до зелёного нельзя (ревьюер снимет срез с неготового прогона). Это работа следующего такта curator: прочитать исход своей рукой по голове `dad33f8`, повесить метку, и когда оба вердикта лягут — идти к john за кнопкой. Незакоммиченного у меня нет: всё в `dad33f8`, ветка запушена, PR открыт.

**#81 и #82 (тред 035) так и стоят без меток** — те самые PR из полевого случая. Я их не трогаю: этот прогон привязан к треду 037 ровно одному, а метка на чужой тред без следа в нём — то же «сам поставил → сам принял». Называю здесь, чтобы факт не потерялся: их метки — работа такта по треду 035.

## Кнопка john — оба PR

**#85** (`PROTOCOL.md`) и **#86** (`docs/roles/**`) — доки власти. Гард 4 останавливает их у любого исполнителя, включая curator: роль не принимает документы, определяющие её собственные полномочия. Merge-gate по ним я не гоняю впустую — вердиктов ещё нет, и дверь ответила бы STOP гарда 4 в любом случае.

Парковка: `run:85` — жду вердикта круга `33164463206`. Ход остаётся у curator.

waiting-on → curator.

