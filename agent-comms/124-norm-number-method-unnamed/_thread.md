# Число в норме о поводе методозависимо, а метод в доке не назван

participants: curator · status: open

## msg-001 · from: curator · 2026-09-04 · expects: answer

**Происхождение: тред `090-what-kills-a-note` (закрыт приёмкой в этот же ход). Предмет заводится отдельным тредом, а не остаётся хвостом в закрытой ленте: он пережил два круга ревью подряд как «названо, не заблокировано», и в закрытом треде его не судит уже никто.**

Прогон, заводящий этот тред, связан тредом `090` — происхождение назвал, чтобы предмет не читался как бесхозный.

## 1. Что именно стоит в `main` и чем оно измерено

`PROTOCOL.md` (§ «Память роли: повод заметки — ОТКРЫТЫЙ предмет, а не тред рождения», влито #263 кнопкой john 2026-09-04T14:13:53Z) обосновывает несущую роль обязательной проверки числом, дословно:

> Из 36 заметок `dev-core` 16 упоминали тред, 8 — закрытый… Прочитай механизм упоминание как повод — **22 % кучи погасло бы на следующем подъёме**.

Число — первоисточник msg-014 треда `090` (замер dev-core своей рукой 2026-09-03). Круг ревью PR #240 (msg-017) назвал его не сходящимся при независимом пересчёте (выходило 9–10), круг PR #263 (msg-023) повторил ту же находку как перенесённую и не переподтверждённую. Оба раза — не блокирующей, оба раза правильно: право (поле повода + обязательная проверка) от числа не зависит ни в одну сторону.

## 2. Замер curator 2026-09-04 ~14:40Z: число не ошибочно, оно МЕТОДОЗАВИСИМО

Куча восстановлена из истории ветки почты, а не из сегодняшней коробки (сегодня у `dev-core` уже 58 заметок — пересчёт по ней измерял бы другую кучу):

- дерево `origin/comms` на 2026-09-03T15:30Z — `35dc671905bbd54a01da82115579d71e67b2ad10`, в нём `agent-comms/memory/dev-core` несёт **39 заметок** (40 файлов минус `MEMORY.md`), а не 36;
- **строгий метод** (упоминанием считается полная форма `NNN-slug`): **8 из 39** упоминают тред, и лишь **1** — тред, закрытый на сегодня. Доля кучи, которая погасла бы, — **2,6 %**;
- **широкий метод** (трёхзначное число рядом со словом `тред`/`PR`/`#`): **30 из 39** упоминают, **15** — закрытый на сегодня. Доля — **38 %**;
- «закрытый на сегодня» — оценка СВЕРХУ для «закрытого тогда»: с 03.09 треды только закрывались.

**Отсюда вывод, и он не «число неверно».** 22 % лежит внутри вилки [2,6 %; 38 %], и каждый из двух методов даёт свой ответ. Не сходится у трёх независимых счётчиков не арифметика, а **метод, который в доке не назван** — а без названного метода число невоспроизводимо по построению, и следующий, кто станет его проверять, потратит тот же час.

## 3. Что я предлагаю сделать (рекомендация curator ≠ решение john)

Тезис раздела верен при широком чтении и неверен при строгом — то есть правка нужна не числу, а **фразе, которая метод называет**. Минимальная форма: назвать в скобках, что считалось упоминанием, и дать вилку вместо одного числа. Право от этого не меняется ни на букву.

`PROTOCOL.md` — док власти: гард 4 `STOP` по построению, PR готовит роль, кнопку жмёт john. Поэтому предмет стои́т здесь, а ход — у curator: следующий такт меряет точно объявленным методом, готовит однострочную правку веткой → PR → круг, и приводит её под кнопку.

**Вопрос к john не ставлю и парковку не вешаю**: пока это работа роли, а не развилка. Развилкой станет, если john сочтёт, что неточное по методу число в доке власти не стои́т отдельного круга вовсе — тогда тред закрывается без правки, и это тоже законный исход.

## 4. Состояние

- дерево роли чистое, незакоммиченного нет; в код и доки этим ходом не написано ничего;
- замер §2 воспроизводим двумя командами по `origin/comms` от коммита `35dc6719` — метод в них назван явно, в отличие от исходного.

waiting-on → curator.

## msg-002 · from: curator · 2026-09-04 · expects: none

# Метод назван, вилка замерена, PR #271 готовится под кнопку john

Ход прошлого сообщения был у curator: «следующий такт меряет точно объявленным методом, готовит однострочную правку веткой → PR → круг, и приводит её под кнопку». Сделано.

## 1. Замер повторён — и он поправил САМ СЕБЯ

Куча та же: дерево `origin/comms` `35dc671905bbd54a01da82115579d71e67b2ad10` (2026-09-03T15:28:37Z), `agent-comms/memory/dev-core` — 40 файлов минус `MEMORY.md` = **39 заметок**. Статусы тредов взяты не на глаз, а из `_meta.md` каждой папки `agent-comms/NNN-*` на `origin/comms`: **115 тредов, 92 закрытых** на 2026-09-04, ноль папок без `_meta.md`.

- **строгий метод** (упоминание = полная форма `NNN-slug`, СОВПАДАЮЩАЯ с существующим тредом): **8 из 39** упоминают тред, **3** — закрытый → **7,7 %**;
- **широкий метод** (слово «тред» рядом с трёхзначным номером): **23 из 39**, **16** — закрытый → **41 %**.

**Строгое число закрытых в msg-001 было занижено: там стояло 1 (2,6 %), верно — 3 (7,7 %).** Поимённо, чтобы это перепроверялось чтением, а не доверием: `note-cause-is-not-the-birth-thread.md` → `090-what-kills-a-note` (закрыт), `note-sort-has-no-subscribers-yet.md` → `090-what-kills-a-note` (закрыт), `notifier-letters-collapse-to-one-cause.md` → `077-notifier-down` (открыт) и `116-role-memory-cost` (закрыт). Остальные пять строгих упоминаний висят на `076-main-red-alarm` (×3), `064-forget-the-consumer`, `077-notifier-down` — все открыты.

**Своя рука в промахе названа отдельно, потому что она же чуть не испортила и этот замер.** Голый шаблон `[0-9]{3}-[a-z][a-z0-9-]*` даёт не 8, а **14**: в него попадают куски UUID прогонов (`434-a168-…`, `326-e7ff-4281-…`, `737-a739-…`). Форма идентификатора шаблоном не проверяется — она проверяется принадлежностью РЕАЛЬНОМУ перечню (`grep -Fxf` по списку папок тредов). Узкий шаблон даёт ложный ноль, широкий — ложное число, и второе опаснее: оно выглядит как замер.

Вывод msg-001 замер не отменяет, а подтверждает в исправленных числах: **22 % лежит внутри вилки [7,7 %; 41 %]**, и не сходится у счётчиков не арифметика, а метод.

## 2. PR #271 — что в нём

Ветка `docs/protocol-124-name-the-method`, коммит `fdd3144b`, PR https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/pull/271 (открыт `pnpm protocol pr open`, несёт `thread: 124-norm-number-method-unnamed` и `role: curator`).

Дифф — одна фраза в `PROTOCOL.md` § «Память роли: повод заметки — ОТКРЫТЫЙ предмет, а не тред рождения»: метод назван вместе с числом, одно значение заменено вилкой, названы дерево и дата замера. **Пункты 1–4 раздела не тронуты, право не меняется ни на букву.**

Одна половина старой фразы не перенесена сознательно и это названо в теле PR: «ни одна из этих восьми короткой не была» принадлежала тому же неназванному методу и этим замером не перемерялась. Перенести неперемеренный хвост — ровно то, что этот PR чинит.

## 3. Гарды и чей ход дальше

`PROTOCOL.md` — док власти: **гард 4 `STOP` по построению, кнопку жмёт john, curator PR только готовит.** Гард 3 своей руки не касается по той же причине — merge не мой.

Состояние маршрута на 2026-09-04T15:36Z: `checks` на голове `fdd3144b` — **pending** (прогон `33890281472`). Метка `review` не повешена и повешена не будет до зелёного прогона — вешать её раньше значит снять срез с недоделанного. Ход в тред вернёт сам исход `checks` письмом `CI Outcome`; следующий такт curator вешает метку и передаёт ход за кругом.

## 4. Хвост открытых PR (перед передачей хода)

`gh pr list --state open`: два — **#271** (этот) и **#265** «feat(thread): парковочная дверь спрашивает поднятую сессию, а не шаг джобы (тред 072)». **#265 этой рукой не трогается**: прогон связан одним тредом, а след гарда 5 обязан лечь в ленту треда-владельца `072`, писать в который отсюда нечем. Молчания не оставляю — предмет назван здесь, ход по нему принадлежит такту на треде 072.

## 5. Состояние

- рабочее дерево роли чистое, незакоммиченного нет; всё, что сделано, — в `fdd3144b` и запушено;
- замер §1 воспроизводим двумя выписками (`git archive 35dc6719 agent-comms/memory/dev-core` и `git archive origin/comms agent-comms`), метод в обеих назван.

waiting-on → curator.
