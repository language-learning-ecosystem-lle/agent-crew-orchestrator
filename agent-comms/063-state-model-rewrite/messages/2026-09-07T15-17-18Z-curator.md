---
from: curator
worker: claude-code
session: cb96340b-5c8c-4523-bfb4-2646c3b4913f
raised: 2026-09-07T15:11:07Z
date: 2026-09-07T15:17:18Z
expects: ack
waiting-on: curator
parked-on: john
---

# Требуется решение john — КНОПКА: PR #322 стои́т на одном гарде 4, и после merge на ящике нужен `git pull --ff-only && pnpm install && systemctl --user restart agent-protocol@hetzner` (бамп `protocolVersion` 25 → 26)

Маршрут `msg-066` §4 пройден целиком, пунктами 1–4: ветка перебазирована дважды, PR открыт, `checks`
зелен, круг ревью дал `approve`, обе находки круга закрыты правкой текста без сдвига головы. Остался
пункт 5 — **кнопка john**, и она стои́т потому, что дифф трогает два дока власти
(`agent-protocol.json`, `.github/workflows/checks.yml`). Ни dev-core, ни я её не нажимаем.

## 1. Что нажимается

```
gh pr merge 322 --squash --match-head-commit 7ee230d1378862d7fdcf1bb2290b526e2e147177
```

SHA взят из вывода `merge-gate` ниже, а не из памяти. Заголовок squash — заголовок PR, он уже в
Conventional Commits: `feat(orchestrator): «ждёт круг ревью» — состояние кадра и ключ конфига review (v26)`.

## 2. Гарды поимённо — мой собственный прогон двери, а не пересказ доклада

`pnpm protocol merge-gate --ref origin/main --pr 322 --review-workflow 'Claude PR Review'`, ход curator
2026-09-07T15:12Z, ключевые строки дословно:

```
ok   guard 1 · approve on the current head: approved on 7ee230d by github-actions — inside the
       round 34133956540 of 'Claude PR Review' on this head (14:37:21Z…14:44:51Z)
ok   guard 2 · green checks on the same head: 2 check(s) green: review=SUCCESS, checks=SUCCESS
you  guard 3 · ascent to a decision of john's: thread '063-state-model-rewrite' — read the feed
STOP guard 4 · no self-merge on the documents of power: john merges this one — it changes
       .github/workflows/checks.yml, agent-protocol.json
ok   mergeability · mergeable=MERGEABLE (mergeStateStatus CLEAN)
REFUSED: a guard does not hold
```

- **гард 1** — `approve` лежит ВНУТРИ окна закрытого круга на ТЕКУЩЕЙ голове, вердикта-сироты нет;
- **гард 2** — зелены оба чека на той же голове, `checks` — прогон `34132794876`;
- **гард 3** — восхождение: постановка `msg-062`, порядок `msg-065` §4, принятый john дословно в
  `msg-066` §4 («кнопка john — `agent-protocol.json` док власти»). Решение john в ленте есть, и оно
  же назначает исполнителя кнопки;
- **гард 4** — единственный STOP, и он не чинится ничем, кроме руки john;
- **гард 5** — след merge кладёт curator следующим ходом, читая `base.ref` после кнопки; трасса для
  него — этот раздел.

Голова перемерена мной отдельно от двери: `7ee230d1378862d7fdcf1bb2290b526e2e147177`,
`state OPEN`, `mergeable MERGEABLE`, `mergeStateStatus CLEAN`, метка `review` висит — та же голова,
на которой лежит вердикт.

## 3. База уехала ЕЩЁ РАЗ после доклада dev-core — пару я перемерила своей рукой

dev-core мерила пару против `9720b92e` (`msg-076` §4). В **15:02:33Z** в `main` сел **`d6f5825d`**
(#325, тред 141), то есть зачтённый `checks` (старт 14:24:44Z) читает дерево, которого больше нет, и
нота гарда 2 о базе стои́т заново — уже на другой базе, чем та, что померена в `msg-076`.

Пересечение путей #325 × #322 — три файла: `packages/agent-protocol/src/cli.ts`,
`docs/protocol-reference.md`, `packages/agent-protocol/README.md`. Чистое слияние стыка не
опровергает, поэтому мерила прогоном:

- дерево слияния `origin/main` (`d6f5825d`) ⊕ `7ee230d1` = **`c15db3b2`**, конфликтов **0**;
- прогон в КОПИИ дерева (`/tmp`, `node_modules` симлинками из моего места), `TMPDIR=/tmp npx vitest run`
  из `packages/agent-protocol`;
- **число предсказано ДО прогона:** 228 файлов + 1 новый тестовый файл #325 = **229**;
  3848 тестов (`msg-076`) + 5 добавленных #325 (грепом `it(`/`test(` по диффу, удалённых ноль) = **3853**.

Исход (`/tmp/aco-pair-suite.log`, дословно):

```
Test Files  229 passed (229)
     Tests  3855 passed (3855)
EXIT=0
```

**Файлы сошлись, тесты разошлись на 2 — и расхождение моё, а не дерева.** Греп считал ОБЪЯВЛЕНИЯ, а
не случаи: `it.each(WITHOUT_MODE)` в новом файле #325 — это два теста, а не один, и один статический
`it(` шаблон вовсе не поймал. Перечисление вместо образца: `npx vitest list stop-mode-refusal` на том
же дереве даёт **7** тестов в этом файле, и 3848 + 7 = **3855** — ровно исход. Ошибки в дереве
разность не означала.

Что этот замер закрывает, а что нет: он говорит, что пара «PR #322 × `main` `d6f5825d`» сходится и
зелена, — и не заменяет прогона на раннере, который после кнопки будет ПЕРВЫМ настоящим замером
слитого дерева. Копия дерева в `/tmp` умеет краснить такой класс, а не зеленить его.

## 4. Нота к кнопке — merge меняет контур, который в этот момент работает

Дифф поднимает `protocolVersion` 25 → 26 и объявляет ключ `review`. Демон исполняет исходники
чекаута: `daemon-code.json` сейчас — sha `d6f5825d`, старт 2026-09-07T15:10:58Z, то есть ящик стои́т
ровно на сегодняшнем `main`. После merge сборка отстанет на версию, и демон ответит на конфиг 26
«перезапусти то, что на нём крутится» — **задуманный ответ шага миграции, не дефект.** Поэтому сразу
после кнопки, на ящике:

```
git pull --ff-only && pnpm install && systemctl --user restart agent-protocol@hetzner
```

Это тот единственный класс, который контур сам за собой не замечает до звонка сторожа: бамп, слитый
молча, отказывает каждой команде каждой роли. Второй контур, если его ящик тоже держит этот пакет, —
та же строка со своим именем инстанса; своей рукой я вижу только `hetzner`.

## 5. Приёмка треда — после кнопки, и она моя

Как объявлено в `msg-062` §7 и подтверждено в `msg-065` §6: живой работы у 063 после посадки PR не
остаётся, след гарда 5 и закрытие треда — мой следующий ход. Пока PR открыт, закрывать нечего.

**Ход у меня, парковка на john: без кнопки этот тред не двигает никто.** Ничего чинить перед кнопкой
не нужно — единственное, что здесь движется, это рука человека.

Незакоммиченного у меня нет: этим ходом я не тронула ни строки кода — вся работа — чтение ленты,
прогон двери и замер пары в копии дерева вне рабочего места. Прогон связан ОДНИМ тредом 063;
открытые #326 и #327 принадлежат другим тредам и в этот ход не берутся.

