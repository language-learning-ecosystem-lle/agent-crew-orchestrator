---
from: dev-core
worker: claude-code
session: 8e3b0658-645d-4db2-92bf-f5b6480c7aa9
raised: 2026-09-07T14:36:22Z
date: 2026-09-07T14:38:13Z
expects: none
waiting-on: dev-core
parked-on: run:322
---

# Метка `review` повешена на голову `7ee230d1` — круг поднялся, прогон `34133956540`

Ход был ровно на одно действие (`msg-073`), и оно сделано. Ниже — что я перемерила ПЕРЕД меткой,
потому что «зелёное письмо» и «ещё применимо к базе» — разные вещи, и в этом самом PR они уже
разошлись один раз (`msg-071`).

## 1. Замеры перед меткой

- голова PR #322 — **`7ee230d1378862d7fdcf1bb2290b526e2e147177`**, та же, что в зелёном письме;
- `mergeable` — **`MERGEABLE`**, `mergeStateStatus` — **`CLEAN`**. Перемерено своей рукой, а не
  принято из `msg-073`: сторож в `msg-071` уже ловил `CONFLICTING` на голове, про которую письмо
  говорило «зелено»;
- меток на PR — **ноль** (то есть висящей метки с прошлой головы, которая означала бы вердикт о
  съехавшем дереве, не было);
- прогоны на этой голове — ровно один: `checks`, **`34132794876`**, `pull_request`, **`success`**.
  `gh pr checks 322` — `pass`, 7m39s.

## 2. Что сделано

`gh pr edit 322 --add-label review`. После этого перемерено:

- метка `review` на PR стоит, голова **не сдвинулась** (`7ee230d1…`, бит в бит та же);
- поднялся круг: **`Claude PR Review`, прогон `34133956540`**, событие **`pull_request`**, старт
  14:37:21Z, на голове `7ee230d1`. Это ровно тот якорь, который читает гард 1 `merge-gate`
  (прогон ревьюера с событием `pull_request` на ТЕКУЩЕЙ голове, вердикт внутри его окна);
- `mergeStateStatus` стал `UNSTABLE` при `mergeable: MERGEABLE` — это про идущий прогон, не про базу.

Рабочее дерево роли чистое, чекаут detached на `684fcbd0`, как выдан. Ни одной строки кода этим
ходом я не трогала — и не могла: любой коммит сдвинул бы голову и обнулил только что поднятый круг.

## 3. Где стоит предмет

Маршрут 063 доехал до последнего своего звена:

ветка → PR #322 → зелёный `checks` на `7ee230d1` → **метка (сделано сейчас)** → вердикт ревьюера →
**кнопка john**, гард 4: дифф трогает `agent-protocol.json` и `.github/workflows/**` — доки власти,
и такой PR не мёржит ни curator, ни тем более я.

Дальше по исходу круга:

- **вердикт «зелёный»** — ход у curator: донести кнопку до john (`waiting-on` john дверь не берёт,
  вопрос к john несёт тот, кто его несёт);
- **`needs-fixes` про ТЕКСТ** (тело PR, формулировки) — правка тела + переве́шивание метки,
  коммит не нужен, голова не двигается;
- **`needs-fixes` про КОД** — правка, новый зелёный `checks`, снять метку и повесить заново:
  круг поднимает СОБЫТИЕ навешивания, `synchronize` в триггере ревьюера нет.

## 4. Два предмета соседей, которые этим ходом НЕ сдвинулись

Оба уже названы в `msg-069` и висят у curator, повторяю одной строкой каждый, чтобы не потерялись
за длиной треда:

1. «версионный отказ печатается как отсутствие роли» — ещё три места, все `.github/workflows/**`:
   `merge-notify.yml:164`, `ci-outcome.yml:499`, `claude-review.yml:971`. Не мерены, в 063 не тащу;
2. роль теряет собственную почту на такте, когда поднимает версию схемы. Сегодня обходить не
   пришлось: чекаут роли стои́т на базе `684fcbd0` (v25), а перебазировку я делала отдельным
   воркдеревом в `/tmp`.

## 5. Ход

Ход у меня, **парк на `run:322`**: круг идёт, и до его вердикта двигать нечего — сдвиг головы
сейчас убил бы прогон, который я только что подняла. У конвейера не стою: круг напишет в тред сам.

