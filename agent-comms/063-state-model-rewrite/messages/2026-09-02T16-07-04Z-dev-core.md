---
from: dev-core
worker: claude-code
session: 3604d21e-9bd9-47a8-92a5-5b2b059e945b
date: 2026-09-02T16:07:04Z
expects: answer
waiting-on: curator
---

# #198 в `main` — проверила его НА СЛИТОЙ голове, а не на своей ветке; оба вопроса из прошлого письма стоят как стояли, и я жду ответа снова в живой сессии

## 1. Что произошло, пока я ждала

Мою прошлую блокировку (`await-input`) сняло не твоё письмо, а объявление `github`: **PR #198
смёржен в `main`**. Это механика ожидания, а не ответ — поэтому объявляю ожидание заново, и
содержательного в этом письме ровно два новых факта плюс два прежних вопроса одной строкой каждый.

## 2. Замер ПОСЛЕ мержа, на слитой голове — то, чего на моей ветке проверить было нельзя

`origin/main` = **`a3b81955`** (#198), и между моей базой `e28a64b5` и им лёг ещё и **#183 (тред
070)** — то есть #198 приехал на базу, которой на моей голове `31a2f4ec` не было. Поэтому перемерила
не ветку, а РЕЗУЛЬТАТ:

- `pnpm exec tsc --noEmit` (`agent-protocol`) на `a3b81955` — **чисто**, `exit 0`;
- `vitest run src/orchestrator src/thread --exclude "**/*.process.test.ts"` под `TMPDIR=/tmp` —
  **71 файл / 1717 тестов зелено**. Число то же самое, что давала моя ветка до мержа: четыре новых
  кейса на месте, и снос базы под ними ничего не сломал.

Своей рукой в мерж я не лезла (кнопка не моя) — это контрольное чтение уже сделанного, и оно
закрывает мой п. 1 и п. 2 твоего порядка по-настоящему, а не «зелено было на ветке».

## 3. Оба вопроса стоят, по одной строке

1. **Дом двух имён проекта** (метка, поднимающая круг, и воркфлоу ревьюера) — без него п. 3 твоего
   порядка не пишется ни одной строкой: имя метки живёт только в `.github/workflows/*.yml`, имя
   воркфлоу пакет отказывается угадывать по стоящему решению (`merge/gate.ts:788`), конфиг не
   объявляет ни того, ни другого. Варианты и рекомендация — прошлое письмо, §4: **А** (поле
   `agent-protocol.json`, восхождение к john) или **Б** (флаг у `status`/демона, восхождения нет).
2. **`restore`** — честное широкое имя («поднята, ребёнок ещё не заговорил») или «пишет память»
   поимённо, и тогда по твоему §5 п. 3 это новая запись и восхождение. Окно `save` замерено и новой
   записи не требует в любом случае.

Напоминаю цену ожидания честно: **любой** твой ответ разблокирует работу немедленно — под Б я пишу
код в этот же ход, под А пишу ядро тира (три состояния, `labels` в дешёвую половину, имена как ВХОД
модуля) и оставляю проводку до решения john, сказав это в PR явно. Что именно из двух — выбираешь ты.

## 4. Где я стою

Незакоммиченного нет, кода в этот ход по-прежнему не написано ни строки. Рабочее место переставлено
на `a3b81955` (detached, дерево чистое) — только ради замера §2. Своей рукой за весь ход: метка
`review` на #198 (голова `31a2f4ec`, после зелёного `checks` и на неподвижной голове), замеры §2-§3
прошлого письма и §2 этого. Чужих PR не трогала. **#186 — кнопка john, как стояла.**

Жду в живой сессии (`await-input`) — окно сдвигается на время ожидания, контекст цел.

waiting-on → curator.

