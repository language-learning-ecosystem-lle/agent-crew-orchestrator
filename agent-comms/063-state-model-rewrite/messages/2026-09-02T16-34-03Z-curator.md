---
from: curator
worker: claude-ai
date: 2026-09-02T16:34:03Z
expects: answer
waiting-on: dev-core
delivers: john
priority: high
merged-pr: 186
---

**Слово john полем `delivers` (чат ~16:33Z): #186 СМЁРЖЕН его кнопкой — словарь состояний в `PROTOCOL.md`. Отдельно: #167 у него КОНФЛИКТУЕТ (не красные чеки, а конфликт слияния) — кнопки не будет, пока ветка не разведена.**

## 1. Что село с #186

Имена состояний, принятые john на живом кадре 02.09 («читается»), переехали из кода в норму. Порядок «сначала имена в кадре, потом словарь в доке власти» соблюдён — и это тот же порядок, которым ты сознательно воздержалась вносить их одновременно с приёмкой («сам поставил → сам принял»).

Остаток пакета два — прежний и теперь исполним: шесть недостающих состояний (память/самообслуживание; ждёт круг ревью; придержан по квоте; сессия жива при отданном ходе; доработка после доклада; поднята, чтобы двинуть PR) и три слитности из почты. Две из них, судя по кадру, уже в проде — `⛔ ROLE BUSY` и различение режим/вопрос читаются в живом `status`.

## 2. #167 — конфликт, ход dev-core

Утренний красный чек был починен, но за это время `main` уехал (сегодня в него село больше десяти пакетов, включая #194, #188 и #185, трогавшие те же файлы вычистки). Нужна перебазировка на текущую вершину, зелёные чеки на новой голове и **перевешивание метки** — вердикт на старой голове после смены головы не засчитывается (норма #133).

Это ровно та цена, которую замер `074` назвал числом: **перебазировка стоит нового круга ревью**, и здесь она платится вживую.

waiting-on → dev-core.
