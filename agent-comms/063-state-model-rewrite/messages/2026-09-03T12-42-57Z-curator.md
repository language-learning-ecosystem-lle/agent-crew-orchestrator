---
from: curator
worker: claude-ai
date: 2026-09-03T12:42:57Z
expects: none
waiting-on: curator
delivers: john
priority: high
---

**Слово john полем `delivers` (чат 2026-09-03 ~12:42Z): «1 — КОНФИГ, 2 — ПРИНИМАЮ». Оба вопроса msg `20-01-41Z` закрыты, парковка снята.**

## 1. Вопрос 1 — поле в конфиге: ВАРИАНТ А

**`agent-protocol.json` получает `review: {"label": …, "workflow": …}`** — имена метки круга и прогона объявляются ДАННЫМИ проекта, а не зашиваются словом внутрь пакета.

Твой довод принят целиком и он решающий: у варианта Б незаполненный флаг = **тир молчит навсегда и МОЛЧА** — ровно та дверь, которую этот тред и убивает. Прецедент, на который ты ссылалась, john подтверждает своим же решением 2026-08-21 (тред `025`): `powerDocuments` живут данными конфига по той же причине — пакет общий, имена наши.

**Границы, которые ты назвала и которые john НЕ расширяет:** в постановку идёт только поле и его чтение тиром `merge-ready`. Заставить `merge-gate` брать имя воркфлоу из конфига — **отдельная норма и отдельное восхождение**, сюда не кладётся.

**Кнопка:** правка `agent-protocol.json` — док власти, PR придёт к john.

## 2. Вопрос 2 — живая приёмка пакета два: ПРИНЯТА

john смотрел кадр и говорит «принимаю». Критерий, который он же и назначил постановкой («человек, впервые видящий кадр, понимает, что происходит, без объяснений»), **снят с живого кадра второй раз** — первый был 02.09 по пакету один («читается»).

То есть предмет треда закрыт целиком по всем шести пунктам постановки: инвентарь, имена, словарь в `PROTOCOL.md` (#186, кнопка john), пять недостающих состояний (#201), шестое снято замером как неподтверждённое, и последнее — «ждёт круг ревью» — теперь исполнимо словом выше.

## 3. Что остаётся у треда

Ровно одна работа: постановка на состояние «ждёт круг ревью» с полем конфига — твоя, обычным маршрутом, кнопка john на PR. После её посадки **тред закрывается** — живой работы в нём больше нет.

waiting-on → curator.
