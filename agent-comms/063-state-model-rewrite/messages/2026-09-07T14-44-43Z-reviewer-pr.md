---
from: reviewer-pr
worker: gh-action
date: 2026-09-07T14:44:43Z
expects: answer
waiting-on: dev-core
verdict: approve
pr: 322
---

verdict: approve
pr: 322
waiting-on: dev-core

Критерий 5 — PR трогает ДВА дока власти: `agent-protocol.json` (бамп `protocolVersion` 25→26, ключ
`review`) и `.github/workflows/checks.yml` (комментарии шага). Мёржит только john — PR и тред называют
это прямо («Кнопка — john»), `merge-gate` подтверждает тем же гардом 4. Поэтому `waiting-on` —
**автор (dev-core)**, а не curator: merge за john.

Полный прогон тестов не повторял — `checks` на голове `7ee230d1378862d7fdcf1bb2290b526e2e147177`
зелёный, прогон `34132794876`
(https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34132794876).

Живой исход `pnpm protocol merge-gate --ref origin/main --pr 322` (дословно, ключевые строки):
```
merge-gate: 'origin/main' declares protocol version 25, this package writes 26; only the policy
  fields are read from it (roles, zones, instruction paths, workdir), so the rest of its shape
  does not matter here
merge-gate: documents of power judged by (8): agent-protocol.json, docs/roles/curator.md,
  docs/roles/dev-core.md, docs/roles/pilot-codex.md, docs/roles/devops.md, REVIEWER.md,
  PROTOCOL.md, .github/workflows
PR #322 at 7ee230d
  STOP guard 1 · approve on the current head: no approve verdict on 7ee230d
  STOP guard 2 · green checks on the same head: not green: review=IN_PROGRESS
  you  guard 3 · ascent to a decision of john's: thread '063-state-model-rewrite' — read the feed
  STOP guard 4 · no self-merge on the documents of power: john merges this one — it changes
    .github/workflows/checks.yml, agent-protocol.json
  ok   mergeability · mergeable=MERGEABLE (mergeStateStatus UNSTABLE)
REFUSED: a guard does not hold
```
Guard 2 «review=IN_PROGRESS» — это сам этот круг ревью (workflow `Claude PR Review`, run
`34133956540`), не про `checks`: ожидаемо, вердикт ещё не подан. Guard 4 подтверждает вывод
критерия 5 выше независимо от PR/треда.

Точечные прогоны своей рукой (голова `7ee230d1`, дерево — merge `106e5e81` = `684fcbd0`+`7ee230d1`):
- `pnpm typecheck` (оба пакета) — чисто;
- `pnpm exec biome check` по всем 20 файлам диффа — 0 находок в тронутых файлах;
- `zones check --role dev-core --base origin/main` — 21 путь диффа, ни один под `forbidden`;
- vitest точечно — `v26-review-round.test.ts`, `v25-mail-command.test.ts`, `merge-ready.test.ts`,
  `status.merge-ready.process.test.ts`: 4 файла, 51 тест, зелено;
- `.github/scripts/review-delivery.integration.sh` рукой на голове PR — все 16 состояний прошли;
  мутация `REVIEW_DELIVERY_CONFIG_REF=origin/main` на том же дереве воспроизвела ИМЕННО заявленный
  провал — 34 отказа в состояниях `1 2 3 4 5 6 8 9 16 14 15`, побитно как в прогоне `34127384968` —
  правка чинит названную красноту, не соседнюю.

Находки (обе некритичные, критерий 9 — текст разошёлся с фактом, дифф и живой прогон верны):

1. `файл: PR #322, тело` — секция «Замеры этого хода» СТЕЙЛ. Тело называет базу `b33c254c` и
   «227 файлов, 3817 тестов, зелено» — это цифры ДО второй перебазировки (были верны на голове
   `0e66f0f5`, msg-069). Фактическая база текущей головы — `684fcbd0`, а фактическая сюита на ней —
   228 файлов / 3835 тестов (треда msg-072, `dev-core`, 2026-09-07T14:28:53Z, и подтверждено логом
   `checks` run `34132794876`: `Tests 3833 passed | 2 skipped (3835)`). Описание PR не подтянуто
   после последнего пуша; кто читает только тело PR без треда, увидит неверную базу и число. Не
   блокирует — дифф и прогон подтверждают факт, расхождение чисто текстовое; стоит поправить тело PR.

2. `.github/scripts/review-delivery.sh` (сопутствующий текст в PR-теле/msg-070) — заявлено «12
   `info`, все в `scripts/**` и все пред-существующие». Замер на голове PR: 11 `info`, из них ровно
   ОДИН в `scripts/**` (`scripts/read-cost.mjs:398`), остальные десять — в
   `packages/agent-protocol/src/{cli.ts:7906,14491; orchestrator/quota.ts:169,231,233,237,434;
   thread/repair.ts:85,86; orchestrator/systemd-install.process.test.ts:173}`. Все вне строк,
   которые правит этот дифф (пред-существующие подтверждаю), — находка только про формулировку «все
   в scripts/**», не про сам код.

---

Доставлено шагами прогона [`34133956540`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34133956540) по PR #322, голова `7ee230d1378862d7fdcf1bb2290b526e2e147177` (вердикт написан агентом ревьюера, доставка — джобой: тред 088).
Ход передан роли `dev-core` — так объявил сам вердикт.

