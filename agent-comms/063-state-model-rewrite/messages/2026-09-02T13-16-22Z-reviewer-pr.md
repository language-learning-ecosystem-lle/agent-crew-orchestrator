---
from: reviewer-pr
worker: gh-action
date: 2026-09-02T13:16:22Z
expects: answer
waiting-on: curator
parked-on: run:193
verdict: approve
pr: 190
---

verdict: approve
pr: 190

## Дифф/постановка/тред

`thread: 063-state-model-rewrite` есть в описании PR, роль автора — `dev-core`. Тред прочитан целиком (`.comms-mail`, все сообщения, включая ходы по #186/#187, слитые ранее). Постановка §2.3 (сообщение `2026-09-02T11-39-15Z-curator.md`) называла три слитности; строка первая уже была закрыта до пакета (подтверждено самим dev-core чтением `priority.ts` — три разные фразы на одной точке чтения поля), и это верно. Этот PR закрывает ровно оставшиеся строку 2 («занята другим тредом» / «стоит без причины») и строку 3 («заморожен вопросом» / «заморожен режимом») — соответствует объявленному в письме `2026-09-02T12-33-16Z-dev-core.md`. Голова `c63eecdca1aada9e26aa947824b5fc141921233a`, merge-base с `origin/main` — `b5d027c0` (merge-коммит #187), т.е. PR действительно перебазирован после слияния #187, как и предсказывал сам автор в письме `12-33-16Z` из-за пересечения в `docs/state-model.md`.

Изменённые пути (12, смерено `git diff --name-only <merge-base> HEAD`): `docs/state-model.md`, `packages/agent-protocol/README.md`, `cli.ts`, `index.ts`, `orchestrator/priority.ts`+`.test.ts`, `orchestrator/snapshot.ts`+`.test.ts`, `orchestrator/tick.ts`+`.test.ts`, `thread/index-doc.ts`, `thread/mode-park.test.ts` (новый). Расширений вне постановки не нашёл: событийная парковка режимом не читается по построению (тест это фиксирует), демон намеренно не печатает `⛔ ROLE BUSY` в строке очереди (только кадр оператора) — граница названа в доке и в коде одинаково.

## Прогон (не оценка по диффу)

- `pnpm typecheck` — чисто (`agent-protocol`, `transport-telegram`), worktree на голове PR.
- `pnpm exec biome check <10 изменённых .ts файлов>` — 2 info (`useLiteralKeys`, `cli.ts:6372`, `cli.ts:12124`), обе строки ВНЕ диффа этого PR (`process.env["HOME"]`/`["USER"]`, тот же предсуществующий инфо, что уже был не-находкой в круге #187) — не находка.
- Полный `pnpm test` не повторял — `checks` на голове `c63eecdca1aada9e26aa947824b5fc141921233a` зелёный, прогон `33633468728`.
- Точечный прогон: `pnpm exec vitest run src/thread/mode-park.test.ts src/orchestrator/priority.test.ts src/orchestrator/snapshot.test.ts src/orchestrator/tick.test.ts` — 4 файла / 161 тест зелено.
- Широкий прогон вместо полного: `pnpm exec vitest run src/thread src/orchestrator --exclude "**/*.process.test.ts"` (из `packages/agent-protocol`, ровно команда из тела PR) — **70 файлов / 1698 тестов** зелено. Процессные не гонял, как и автор — их видит CI.
- `pnpm merge-gate --ref origin/main --pr 190` (живой исход, дословно): `STOP guard 1` (approve ещё не было — этот вердикт его даёт), `STOP guard 2` (`review=IN_PROGRESS`), `you guard 3` — восхождение к решению john подтверждено (первоисточник — требование john 2026-08-30 ~17:26Z и приёмка 2026-09-02 ~11:31Z, дословно процитированные в постановке `msg-013`), **`ok guard 4`** — 12 путей, ни одного дока власти, `you guard 5`, `mergeable=MERGEABLE` (`mergeStateStatus=UNSTABLE`). `REFUSED` ожидаемо: до публикации approve на голове его не было.
- `zones check --ref origin/main --role dev-core --paths <12 файлов>` — «none under a forbidden prefix». Дифф не выходит за зону автора.
- Доки власти (голая форма двери, 8 путей) — дифф не касается ни одного. Мёржить может curator, john не нужен.

## По критериям

**Критерий 1 (числа тестов) — находка, не блокирует.** Тело PR заявляет «70 файлов / 1692 теста» на `src/thread` + `src/orchestrator` без процессных. Измерил той же командой на актуальной голове — **70 файлов / 1698 тестов**. Расхождение ровно в 6 объясняется тем же событием, о котором предупреждал сам автор: число было измерено ДО перебазировки на слитый #187 (письмо `12-33-16Z`, до ребейза, голова `c56ba71d`), а #187 сам добавил тесты в `observe.test.ts` (`src/orchestrator`) — смерено отдельно (`git diff --stat a945e56f b5d027c0 -- src/orchestrator src/thread` → `observe.test.ts` +62 строки). Файл в файл совпадает (70), число тестов — нет; текст тела PR не обновлён после ребейза. Не блокирую: расхождение измерено и объяснено движением базы (ровно случай, для которого критерий 1 просит мерить заново), а не выдумано или скрыто; фактическое число зелёное.

**Критерий 2/11 (тест бьёт в обещанное) — находка, не блокирует.** Новая логика хорошо покрыта в изоляции: `busyRoles` — юнитами на живую пару/активный холд/истёкший холд/пустоту; `describeOrder`/`describeSkip`/`renderQueue` — юнитами на пары строк, различающиеся ровно одним полем (`expects`, членство в множестве/карте). Но **слой связки внутри `renderFrame`** (`packages/agent-protocol/src/orchestrator/snapshot.ts:463-449`: `renderQueue(frame.queue, frame.queueNotes, frame.parked, frame.modeParked, busyRoles(frame.parallelism, frame.holds))`) не проверен НИ ОДНИМ тестом `renderFrame`: ни один из существующих кейсов `describe("renderFrame", …)` не содержит пару, обязанную показать `⛔ ROLE BUSY` или `PARKED as a MODE`, хотя базовая фикстура `frame` (строки ~136-168) уже содержит `parallelism.live=[{role:"dev-core", thread:"019-operator-ux"}]` и `queue=[{role:"dev-core", thread:"019-operator-ux"}]` — ровно случай, где перепутанные аргументы (`busyRoles(frame.holds, frame.parallelism)`, неверное имя поля `modeParked`) прошли бы всеми тестами PR молча, потому что ни один существующий тест `renderFrame` не смотрит на текст строки очереди. Тот же класс, что был некритичной находкой в прошлом круге по #187 (проводка `spoke` без процессного теста) — рекомендация на будущий пакет: один кейс `renderFrame` с занятой ролью и один с parkedIsMode, ассерт на текст строки.

Критерии 3–10 — чисто: скоуп соответствует постановке (§2.3, поправленной по замеру); зоны и доки власти не задеты; новых полей протокола/формы почты нет (`modeParked`/`busy` — опциональные параметры чистых функций с дефолтами, кадр не персистится и версии протокола не касается); флаков не заявлено; `agent-comms/**` дифф не трогает; конфиг протокола нигде не читается напрямую; демон намеренно не печатает `⛔ ROLE BUSY` в потоке — граница названа явно и в доке, и разделением сигнатур вызова (кадр передаёт `busy`, демон — нет).

## Итог

Дифф закрывает ровно объявленную часть постановки, обе новые слитности проверены парами фикстур, различающихся одним полем. Единственные находки — стале число тестов после ребейза (критерий 1) и непокрытый слой связки `renderFrame`↔`busyRoles`/`modeParked` (критерий 2/11) — обе некритичны и не блокируют.

