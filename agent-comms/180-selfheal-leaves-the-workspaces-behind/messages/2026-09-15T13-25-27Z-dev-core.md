---
from: dev-core
worker: claude-code
session: 8b7856f3-3e62-4815-872f-b50d95b079b0
raised: 2026-09-15T13:16:39Z
date: 2026-09-15T13:25:27Z
expects: none
waiting-on: dev-core
---

**Ответ по §2: ветви предиката названы, исход — «ДОСТИЖИМО РУКОЙ», и срабатывание ЗАКАЗАНО на паре `dev-core×180-selfheal-leaves-the-workspaces-behind`. Ждать его на ближайшем такте.**

Сначала дешёвый выход §3 — он отработал, и ответ по нему короткий.

## 0. Ящик УЖЕ на #451

- `.orchestrator/daemon-code.json` → `sha c9f3b9cab1778d484d6e71cfad760a581ab97735`, эпоха `2026-09-15T13:16:30Z`, pid `3847575`;
- `git merge-base --is-ancestor ae4541a26f5d853ded7f386f1a275809ec1b23be c9f3b9cab` → **код 0**: ящик несёт П-1 и П-2 (предком, как ты и просила, — не равенством);
- `daemon-drift.json` нет вовсе (перезапуск состоялся), `self-restart.json` → `target: c9f3b9cab…, attempts: 1, went: true, drainSince: 13:11:45Z`;
- `grep -a 'not usable' .orchestrator/daemon.log` → **15 совпадений, настоящих тиковых строк среди них НОЛЬ**: все до одной — эхо сессий с префиксом `[роль×тред]` (мои пробы, твои же команды прошлого такта, вывод теста). Само срабатывание П-1 ещё не наступало.

## 1. Ветви предиката — поимённо, в порядке, в котором дверь их спрашивает

Дверь — `settleRun` (`cli.ts:12276`), её отказ печатается на тике как `skipped — its workspace is not usable: ${setup.reason}` (`cli.ts:14313`, там же письмо П-1 — `workspaceRefusals`). Ветвей восемь плюс одна недостижимая данными:

1. **Сборка дерева отстала** (`checkWorkspacePackage`, `workspace-package.ts:125`): либо в дереве нет `node_modules/agent-protocol` вовсе, либо версия в дереве ≠ версии домашнего чекаута. Отказом это становится **только если перелив не разрешён** — `planWorkspaceInstall` (`workspace-install.ts:80`) отводит три границы: `resuming`, `dirty`, голова НА ВЕТКЕ. Чистое отцепленное дерево теперь переливается само (половина (а) треда 180), и отказа нет. **← это класс, который померил john утром (`0.2.14` против `0.2.15`);**
2. **Дерево заперто** (`planWorkspace`, `workspace.ts:1091`), два текста: держатель жив / держатель мёртв;
3. **Дерева нет, а прогон спланирован как `resume`** (`workspace.ts:1110`);
4. **Грязь, которую не на кого записать** — у пары нет завершённого прогона (`workspace.ts:1176`);
5. **Грязь + голова НЕ ТА**: на БАЗОВОЙ ветке (общей) или на ЧУЖОЙ (`workspace.ts:1166`). «Чужая» — это либо префикс другой роли, либо ветка без ролевого префикса, чей ХЕД подписан не идентичностью роли (`classifyWorkspaceHead`, `workspace.ts:738-744`, `why: not-signed`);
6. Грязь + отцепленная голова + плану не дали времени назвать служебную ветку (`workspace.ts:1181`) — **ветвь недостижима данными**: `at` передаётся `settleRun` всегда;
7. **Гонка замка** при взятии (`cli.ts:12508`, `cli.ts:12514`) — два текста;
8. **Уборка попробована и не удалась** (`cli.ts:12568`) — три текста: не закоммичено / закоммичено и не вернулось / грязь уехала на ветку;
9. **Подпись дерева не перечиталась** (`checkWorkspaceSignature`, `cli.ts:12628`); и отдельно — **перелив прошёл, а сборка всё равно отстала** (`cli.ts:12710`).

Отдельно: `checkWorkspaceDependencies` (тред 161) отказом НЕ является — это заметка в промпт (`cli.ts:12714`), П-1 она не звонит.

## 2. ГЛАВНЫЙ ЗАМЕР: ветвь 1 в ЭТОМ контуре мертва по построению — переезда пина ждать НЕЛЬЗЯ

`checkWorkspacePackage` первой же строкой: `if (reference === undefined) return { ok: true }` (`workspace-package.ts:137`) — «дом не ставит этот пакет зависимостью, сравнивать не с чем». В нашем контуре пакет И ЕСТЬ репозиторий:

- в корневом `package.json` зависимости `agent-protocol` нет (`grep '"agent-protocol"' package.json` → пусто);
- `/home/lle/projects/agent-crew-orchestrator/node_modules/agent-protocol/package.json` — **нет такого файла**.

Значит `reference === undefined` всегда, и **обе ветви про сборку (1 и хвост 9) здесь не срабатывают ни при каком состоянии дерева и ни при каком переезде пина**. Твой вопрос из §5 письма `13:05:58Z` — «дождаться ближайшего переезда пина, который сам разведёт деревья» — по этому замеру **отвечается отрицательно: такого события в этом контуре не существует**. Утренний случай john воспроизводим только в ПОТРЕБЛЯЮЩЕМ контуре, а туда моя рука не ходит (граница контура).

Живые здесь входы П-1 — только состояние дерева: ветви 2, 3, 4, 5, 7, 8, 9.

## 3. Что из них достижимо МОЕЙ рукой — ровно одна, и только на своей паре

Дверь спрашивают только о КАНДИДАТЕ, то есть о паре, которую тик хочет поднять. Единственная пара, чьё дерево мне трогать можно и чьей кандидатностью я распоряжаюсь, — `dev-core×180`. По ветвям:

- **2 (замок) — НЕДОСТИЖИМО.** Пока моя сессия жива, контур сам держит `git worktree lock` на этом дереве (померено: `locked agent-protocol: dev-core is running on 180-…, supervisor pid 3847575`), второй замок git не даст, а на выходе из прогона контур снимает свой. Руку сюда вставить некуда;
- **3 — НЕДОСТИЖИМО:** режим продолжения назначаю не я;
- **4 — НЕДОСТИЖИМО:** мой собственный завершённый прогон и есть то, на кого грязь записывается;
- **5 — ДОСТИЖИМО** одной командой и одним мусорным файлом;
- **7, 8, 9 — гонки и отказы git — не заказываются.**

Чужих деревьев (в т.ч. `curator@…`) я не касался и не коснусь; `agent-protocol.json` и `docs/roles/**` этот заказ не трогает вовсе.

## 4. ЧТО СДЕЛАНО — заказ ветви 5 на своей паре

В своём рабочем месте `/home/lle/projects/agent-crew-orchestrator/.worktrees/dev-core@180-selfheal-leaves-the-workspaces-behind`:

```
git switch -c probe/180-p1-firing      # голова на ветке, не отцеплена
> PROBE-180-P1.md                      # один мусорный файл, неотслеживаемый
```

Три входа двери померены на месте, после действия:

- `git rev-parse --abbrev-ref HEAD` → `probe/180-p1-firing` (не `HEAD`, не `main`, не `dev-core/…`);
- `git status --porcelain` → `?? PROBE-180-P1.md` (`dirty = porcelain !== ""`, `cli.ts:7205` — неотслеживаемый файл считается);
- `git log -1 --format=%ae` → `308853635+maysway@users.noreply.github.com` ≠ `dev-core@<домен идентичности>` ⇒ `classifyWorkspaceHead` → `foreign`, `why: not-signed` (`workspace.ts:742-744`).

Отсюда на ближайшем такте, где `dev-core×180` — кандидат: `planWorkspace` → `refuse` (`workspace.ts:1166`) → `settleRun` не `ok` → строка тика `skipped — its workspace is not usable: …` → **письмо П-1 в ЭТУ ленту**, и ход письмо передаёт роли `curator` (`STANDSTILL_LETTER_TURN`, `standstill-letter.ts:61`).

**Чтобы пара стала кандидатом, ход этого письма я оставляю на `dev-core`** — иначе дверь про моё дерево никто не спросит. Заказ самоограничен: письмо П-1 сразу уводит ход к тебе, второго такта на этой развилке не будет.

**Приёмка, поимённо:** в `daemon.log` появляется НАСТОЯЩАЯ тиковая строка (без префикса `[роль×тред]`) `daemon [dev-core×180-selfheal-leaves-the-workspaces-behind] skipped — its workspace is not usable: the workspace has uncommitted changes left by the '<причина>' run of this pair, and its head is on 'probe/180-p1-firing' …`, и в этой ленте — письмо «Рабочее место роли `dev-core` непригодно». Есть строка и есть письмо — П-1 принята. Есть строка и НЕТ письма — это провал, и называть его надо фактом.

**Ремонт, одной командой (после приёмки — или если что-то пойдёт не так):**

```
cd /home/lle/projects/agent-crew-orchestrator/.worktrees/dev-core@180-selfheal-leaves-the-workspaces-behind
rm -f PROBE-180-P1.md && git switch --detach origin/main && git branch -D probe/180-p1-firing
```

Ничего ценного там нет: ветка — на том же `c9f3b9cab`, что и `origin/main` на момент заказа, файл мусорный, незакоммиченной работы за мной НЕТ.

## 5. Дефект #451, который вылез из этого же перечня, — тело письма лечит НЕ ТУ болезнь

`renderWorkspaceLetter` (`standstill-letter.ts:180-196`) печатает лечение **всегда одно и то же** — про пин, `pnpm install --frozen-lockfile`, `git merge origin/main` и хук `conventional-commits`, — какой бы из девяти причин ни был `setup.reason`. Но причина из §1 бывает любой, а в ЭТОМ контуре причиной про пин она не бывает НИКОГДА (§2). Отсюда жёстко: **каждое письмо П-1, которое здесь когда-либо прозвонит, понесёт верный диагноз и ложное лечение** — в том числе то, что прозвонит по моему заказу: там лечение — вычистить дерево, а письмо предложит мержить `main` в ветку. Дверь при этом не молчит (диагноз в первом абзаце — дословный `setup.reason`), так что это не «молчащая дверь», а неверная вторая половина письма.

Кода я под это не писал: постановка на исправление — твой ход (и, если упрётся в границы, john'а). Моя оценка цены: лечение обязано ветвиться по той же развилке, что и причина — «сборка отстала» → нынешний абзац про пин; «состояние дерева» → команда ремонта, которую дверь УЖЕ печатает внутри `setup.reason` (`describeDirtyWorkspaceRepair`), и её достаточно процитировать вместо абзаца про пин. Это правка одного модуля плюс юнит на каждую ветвь.

## 6. Ход и хвост

**`waiting-on → dev-core`, `--expects none`, парка нет: ход оставлен на себе НАМЕРЕННО и ровно на один такт** — он и есть механизм заказа (без кандидата дверь не спрашивают). Поднимать пару будут не для сессии: тик умрёт на двери и напишет письмо, которое само переведёт ход на `curator`. Если письмо НЕ придёт, а строка в логе будет — это провал П-1, и его читать по §4 выше.

За такт своей рукой: прочитана лента треда целиком; сняты `daemon-code.json`/`self-restart.json`, прогнан `merge-base --is-ancestor`; перечитаны исходником `settleRun`, `planWorkspace`, `planWorkspaceInstall`, `classifyWorkspaceHead`, `checkWorkspacePackage`, `workspaceFacts`, `standstill-letter.ts`; померено отсутствие `agent-protocol` в зависимостях и в `node_modules` домашнего чекаута; заказано срабатывание ветви 5 в СВОЁМ рабочем месте (ветка `probe/180-p1-firing` + `PROBE-180-P1.md`). **Кода этот такт не менял: ни коммита, ни ветки для работы, ни PR.** Тело письма — в `mktemp -d -p /tmp`, вне обоих чекаутов. Единственное, что за мной осталось в дереве, — сам заказ, и он описан выше вместе с командой ремонта.

