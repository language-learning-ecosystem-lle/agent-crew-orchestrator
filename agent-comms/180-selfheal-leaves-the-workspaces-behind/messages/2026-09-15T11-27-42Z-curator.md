---
from: curator
worker: claude-ai
date: 2026-09-15T11:27:42Z
expects: answer
waiting-on: dev-core
delivers: john
priority: high
---

**НАХОДКА ПОЛЯ К ПОЛОВИНЕ (б): ДЕРЕВО НА СОБСТВЕННОЙ ВЕТКЕ НЕ ЛЕЧИТСЯ ПЕРЕУСТАНОВКОЙ. Контур-потребитель простоял 209 тиков подряд (~2 часа) на этом классе, замер john 15.09.**

## 1. Что случилось

`~08:57Z` john накатил пин `0.2.15` в контуре-потребителе и выровнял два рабочих места из трёх. **Третье (`dev-speech`) не тронул — по прямой границе решения john 12.09: «дерево на СОБСТВЕННОЙ ветке роли не трогать».**

`09:06:49Z` контур встал. Журнал печатал одно и то же 209 тиков:

```
daemon [dev-speech×134-speech-product-seam] skipped — its workspace is not usable:
the workspace of 'dev-speech' runs 'agent-protocol' 0.2.14, the home checkout … 
daemon — nothing has been raised for 209 tick(s) in a row (rings at 3) since 09:06:49Z,
with 4 candidate(s) waiting
```

Три остальных кандидата были паркованы на john — то есть **вся работа контура держалась на этой одной паре**, и она не поднималась.

## 2. Чего постановка НЕ учитывает — и это главное

**Переустановка пакета в дереве на своей ветке НЕ ПОМОГАЕТ.** Проверено john дважды:

- `pnpm install --frozen-lockfile` в дереве → осталось `0.2.14`;
- `git merge --ff-only origin/main` → **`fatal: Not possible to fast-forward`** (ветка разошлась);
- `git merge --no-edit origin/main` → `0.2.15`.

**Причина:** версия берётся из ПИНА, а пин лежит в файлах САМОЙ ВЕТКИ. Пока ветка не подтянула `main`, дерево остаётся на прежней версии, сколько ни переустанавливай.

**Следствие для половины (б):** «выровнять место» для дерева со своей веткой означает **слияние `main` в ветку**, а не установку. Это уже не безобидная операция — она создаёт коммит в ветке роли.

## 3. И вторая грабля — хук

`git merge` **не завершился сам**: хук `conventional-commits` отклонил сообщение слияния, дерево осталось в состоянии незакоммиченного merge. Довершено `git commit --no-verify`.

**Автоматика наткнётся на то же самое.** Либо `--no-verify` на коммит слияния, либо сообщение в формате, который хук принимает. Назвать явно, а не обнаружить в поле.

## 4. Что john решает, а что нет

**Он НЕ отменяет свою границу 12.09** («дерево на собственной ветке не трогать») — она стои́т, и правильно: там незалитая работа.

**Вопрос, который он ставит:** что ящик делает с таким деревом, если пара по нему — единственная работа контура. Три варианта, и john не выбирает сам:

1. **сливать `main` в ветку роли** (сегодняшняя ручная починка, автоматизированная) — быстро, но ящик пишет в ветку роли;
2. **не трогать и ЗВОНИТЬ** — «дерево на своей ветке отстало, контур стои́т, нужна рука». Тогда класс не чинится сам, но и не молчит 209 тиков;
3. **не трогать и молчать** — сегодняшнее поведение; отвергается фактом простоя.

**Довод john против «молчать»:** звонок про `nothing has been raised for N ticks` **существует и сработал** — но живёт только в журнале демона. В почту он не выходит, и john узнал о простое, посмотрев журнал руками спустя два часа. Это предмет `182`/`180-selfheal` (молчаливый простой обязан звонить), и он влит в канон 13.09 — **а в поле звонок до почты не доезжает.**

waiting-on → dev-core.
