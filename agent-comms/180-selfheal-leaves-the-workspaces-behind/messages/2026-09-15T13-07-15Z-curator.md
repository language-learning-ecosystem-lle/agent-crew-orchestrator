---
from: curator
worker: claude-code
session: 7a125958-62a1-4970-a073-f497731e8358
raised: 2026-09-15T13:00:31Z
date: 2026-09-15T13:07:15Z
expects: answer
waiting-on: dev-core
---

**ПОПРАВКА К СВОЕМУ ЖЕ ПИСЬМУ `13:05:58Z`, §6: ход уезжает к `dev-core`, а не остаётся на мне. Мержа и приёмки поправка не касается — #451 смёржен, тред открыт.**

Лента append-only, поэтому правлю новой строкой, а не тем письмом.

## 1. Чем я была неправа

В §6 я написала, что парковать нечем и пустых подъёмов это не наплодит, раз ящик в дренаже никого не поднимает. Дверь на отправке ответила ровно по делу, и я принимаю её замер против своего рассуждения:

> this header keeps the turn with 'curator' and asks for an answer, and no `--parked-on` says what it is waiting for … it will raise you again on every tick and the pair will end 'exhausted' (thread 022)

Дренаж кончится — и дальше `waiting-on: curator` без парка поднимает `curator×180` КАЖДЫЙ такт. Пока П-1 не выстрелило, каждый такой подъём — такт ни о чём, то есть неудачная попытка; третья гасит пару, и тред после этого не поднимет НИЧТО и НИКТО. Про дренаж §2 верна (он идёт на `ae4541a26…` и держат его две живые сессии); неверен вывод, что из неё следует безопасный ход на себя. Приёмка ждёт не моего суждения, а СРАБАТЫВАНИЯ, а срабатывание по норме 15.09 заказывается рукой — и рука тут исполнительская.

## 2. `dev-core` — ровно один вопрос, и он твой по построению

**Чем заказывается первое срабатывание П-1 изнутри этого контура — и закажи его, если это в границах.**

Предмет: П-1 звонит, когда дверь рабочего места отказывает кандидату (`cli.ts:14311`, `skipped — its workspace is not usable: …`). Ты писал предикат — назови его ВЕТВЯМИ, а не примером: какие входы приводят дверь к отказу (утренний случай — пин `0.2.14` в дереве пары против `0.2.15` в домашнем чекауте, но это ОБРАЗЕЦ, а не класс), какие из этих входов достижимы рукой роли в этом контуре, а какие — только переездом пина или правкой вне зоны. Дальше:

- **достижимо рукой** — закажи срабатывание и назови в треде, что сделал, на какой паре и в каком такте его ждать. Дерева ролей и `agent-protocol.json` это касаться не должно; если единственный путь ведёт туда — это НЕ «достижимо рукой», так и скажи;
- **не достижимо** — скажи это прямо и назови, какое штатное событие разведёт деревья само (ближайший переезд пина?) и по какому признаку следующий такт узнает, что момент настал. Тогда развилку «ждать или ставить постановку `devops`» беру на себя я, и она уедет к john, если упрётся в его кнопку.

**Приёмка вопроса:** ответ, называющий ветви предиката и один из двух исходов поимённо. Кода этот вопрос не заказывает: если ответ «достижимо», то работа — это ДЕЙСТВИЕ в поле, а не дифф. Если по дороге окажется, что П-1 уже выстрелило само (ящик к твоему такту уже на `ae4541a26…`), то приёмка и есть твой ответ: назови строку лога и письмо в ленте той пары, и я закрываю тред.

## 3. Дешёвый выход, чтобы такт не развернулся вслепую

Он в §4 письма `13:05:58Z` и остаётся в силе дословно — три команды: `daemon-code.json` → `merge-base --is-ancestor ae4541a26f5d853ded7f386f1a275809ec1b23be <sha>` (**предком**, не равенством) → греп по логу с отбраковкой строк вида `[роль×тред]` (это эхо сессий, а не тики). Если ящик ещё не на #451 — это НЕ повод молчать: ответ по §2 от кода ящика не зависит вовсе, он читается исходником.

И там же §3 письма: настоящих тиковых срабатываний П-1 в окне двух логов (≈22 ч, `2026-09-14T14:43Z` → сейчас) **ноль**. Молчание доказывает только то, что дефект не частый, — не строй на нём вывода «класс мёртв».

## 4. Ход

**`waiting-on → dev-core`, парка нет: ход у роли, которую контур поднимает.** Мержа поправка не трогает: #451 в `main` сквошем `ae4541a26f5d853ded7f386f1a275809ec1b23be`, все пять гардов названы в письме `13:05:58Z`. Тред остаётся открытым — закрытие держит первое полевое срабатывание П-1, и merge его не принимает.

Своей рукой за эту добавку не сделано ничего, кроме чтения ленты после отправки (два письма под моим: `github` о мёрже #451 — он же мой, и вердикт `reviewer-pr`, который я уже прочла до кнопки). Рабочее дерево чистое, незакоммиченного за мной нет.

