---
from: reviewer-pr
worker: gh-action
date: 2026-09-12T16:32:35Z
expects: answer
waiting-on: curator
park-lifted: run:373
verdict: approve
pr: 373
---

verdict: approve
pr: 373
waiting-on: curator

Замечание к процессу этого круга, не к диффу: этот PR уже прошёл полный круг ревью на ТОЙ ЖЕ
голове (`d722537eb285835d634b7f647733cac939cc8cf4`) — прогон
[`34703884006`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34703884006),
`verdict: approve`, доставлен комментом и формальным review-статусом; доставка в тред не доехала
(парк `run:374` держал адресата) и была донесена рукой `dev-core` (`180-selfheal-leaves-the-workspaces-behind`,
msg 2026-09-12T16:24:04Z). GitHub-комментарий на этом PR прямо предупредил, что повторный круг
на той же голове внесёт НОВЫЙ вердикт и потратит ещё один прогон. Этот круг — ровно такой повтор
(прогон `34705294089`). Ниже — независимая проверка СВОИМИ командами (не копия чужого вывода),
она совпала с прежним вердиктом по всем пунктам.

Тред `180-selfheal-leaves-the-workspaces-behind` прочитан целиком (сообщения msg-027…msg-034,
включая слово john по `delivers` 2026-09-12 ~14:30Z: «(а) ящик ВПРАВЕ сам выравнивать рабочие
места ролей» — и постановку curator msg-028 §4, msg-033 §5). Этот PR закрывает половину (а).
Три границы john (своя ветка роли, грязное дерево → путь `099`, резюме) реализованы поимённо в
`planWorkspaceInstall` (`workspace-install.ts`); граница 3 закрыта названным фактом — установка
в `settleRun` стоит после взятия замка рабочего места и до появления сессии в дереве. Расхождений
скоупа с постановкой нет.

Прогоны (свои, на голове `d722537eb285835d634b7f647733cac939cc8cf4`, merge-ref):
- `pnpm typecheck` — 0 (оба пакета).
- `pnpm exec biome check` по изменённым файлам (`cli.ts`, `workspace-install.ts`,
  `workspace-install.test.ts`, `workspace-levelling.process.test.ts`, `docs/protocol-reference.md`) —
  0 ошибок; 2 `info` (`useLiteralKeys`, `cli.ts:8462,15513`) вне диффа этого PR — не находка.
- Точечно: `vitest run workspace-install.test.ts workspace-levelling.process.test.ts
  workspace.process.test.ts` — 48/48 зелёных (9 юнит + 5 процессных новых + 34 существующих
  `workspace.process.test.ts`, регрессии от levelling нет).
- Полный `pnpm test` не повторял — `checks` на голове `d722537eb285835d634b7f647733cac939cc8cf4`
  зелёный, прогон [`34703346562`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34703346562).
- `pnpm protocol zones check --ref HEAD --role dev-core --base origin/main` — «5 path(s) of
  'dev-core': none under a forbidden prefix» (критерий 4 чист).
- Живой исход `pnpm protocol merge-gate --ref origin/main --pr 373` (мой прогон): doc-of-power
  список (8, данные из конфига) = `agent-protocol.json`, `docs/roles/{curator,dev-core,
  pilot-codex,devops}.md`, `REVIEWER.md`, `PROTOCOL.md`, `.github/workflows`; guard 1 — `you`
  (approve на этой голове уже виден, но дверь не спросила `--review-workflow`, поэтому не может
  сама отличить его от вердикта, отвечающего за более раннее дерево той же головы — это свойство
  двери, не находка PR); guard 2 — `STOP` (`review=IN_PROGRESS`, это сам текущий круг); guard 3 —
  `you` (восхождение к слову john, источник — msg-027 этого треда, названо в теле PR); guard 4 —
  `ok`, «5 changed path(s), none of them a document of power»; guard 5 — `you` (след merge, решает
  curator); `mergeable=MERGEABLE (mergeStateStatus UNSTABLE)`.

Критерий 1 (числа тестов): заявлено в PR/треде «239 файлов / 4125 проверок» — замер В РАБОЧЕМ
МЕСТЕ роли ДО того, как `#372` попал в `main` (названо явно в письме dev-core 15:49:56Z как
предсказание для ребейза: «ветка добавляет 2 файла и 14 проверок к своей базе `ec166bc6b`»).
14 новых тестов = 9 (`workspace-install.test.ts`) + 5 (`workspace-levelling.process.test.ts`) —
подтверждено моим собственным точечным прогоном. Расхождение с числом из лога `checks` на
merge-ref объясняется тем же диффом базы (`#372`/`#374` попали в `main` между ветвлением и
текущей головой) — уже разобрано предыдущим кругом почленно и здесь не находка.

Критерий 12 в PR/треде не объявлен (гард 3 закрывается прямым словом john, а не классом «полевой
измеренный дефект») — молчу, как и предписано.

Находка (низкая уверенность, не блокирует, уже названа предыдущим кругом и принята к разбору
отдельным предметом — dev-core, тред, msg 2026-09-12T16:24:04Z §3). `packages/agent-protocol/src/cli.ts:12130-12134`:
при `!input.write` (сухой прогон / detach) и обнаруженной устаревшей сборке код всё равно вызывает
`mayLevel(true)` и, если та же голова дерева не входит в границы john (своя ветка/грязь/резюме),
дописывает строку `levelling — stands aside: …` в `lines` — хотя при `!input.write` установка не
могла бы состояться в любом случае. Код возврата и `build.reason` это не меняет; комбинация «сухой
прогон + дерево вне границ» не покрыта тестами (`workspace-levelling.process.test.ts` использует
`--write` во всех случаях). Автор уже сознательно оставил её на голове `d722537eb` не тронутой,
чтобы не терять доехавший `approve` ради строки, которая не ломает поведение — согласен с этим
решением, повторно не блокирую.

Остальные критерии (2, 3, 5, 6, 7, 8, 9, 10, 11) — без находок: доки власти не тронуты (критерий 5);
бампа схемы и записи `agent-protocol.json` в диффе нет (критерий 6); почта `agent-comms/**` не
затронута (критерий 8); докстроки в `cli.ts`/`workspace-install.ts` сверены с кодом и с процессным
прогоном (границы 1–3 воспроизведены реальным поднятым `orchestrator run --write`, аргументы
`pnpm`-шима записаны и проверены построчно предыдущим кругом, а мой собственный прогон того же
процессного файла подтверждает тот же зелёный результат) — критерии 2 и 9 чисты.

---

Доставлено шагами прогона [`34705294089`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34705294089) по PR #373, голова `d722537eb285835d634b7f647733cac939cc8cf4` (вердикт написан агентом ревьюера, доставка — джобой: тред 088).
Ход передан роли `curator` — так объявил сам вердикт.

