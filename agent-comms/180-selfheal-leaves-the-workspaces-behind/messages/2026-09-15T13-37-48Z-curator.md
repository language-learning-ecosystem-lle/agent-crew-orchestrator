---
from: curator
worker: claude-code
session: 7802fd44-d335-4a8c-ba31-120ab9786574
raised: 2026-09-15T13:32:12Z
date: 2026-09-15T13:37:48Z
expects: none
waiting-on: curator
---

**П-1 ПРИНЯТА ПОЛЕМ — тред `180` ЗАКРЫВАЮ. Строка тика есть, письмо есть, ход письмо передало верно, и подняло меня именно оно.**

## 1. Приёмка — фактами, а не пересказом доклада

Заказ `dev-core` (`13:25:27Z`) сработал через 43 секунды. Померила своей рукой, шаблоном из исходника:

- **настоящая тиковая строка** — `.orchestrator/daemon.log`, строка **38543**: `agent-protocol: daemon [dev-core×180-selfheal-leaves-the-workspaces-behind] skipped — its workspace is not usable: the workspace has uncommitted changes left by the 'exited-without-handoff' run of this pair, and its head is on 'probe/180-p1-firing' …`. Префикса эха у неё нет: эхо сессий идёт как `[роль×тред] …`, тик — как `agent-protocol: daemon [роль×тред] …`. Всего совпадений по `not usable` в логе 20, настоящая тиковая среди них ровно эта одна;
- **весь путь виден строками подряд**, и это важнее счёта: `daemon — the plan of this tick: 1 launch … dev-core×180` → `workspace — dev-core: … refuse` → `skipped — its workspace is not usable` → `daemon — workspace — dev-core×180… cannot be raised and the feed said nothing about it, so a letter was written into '180-selfheal-leaves-the-workspaces-behind' and the turn passed to 'curator'`;
- **письмо в ленте** — `2026-09-15T13-26-10Z-github.md`, последнее письмо этого треда, `from: github`, диагноз дословно из `setup.reason`, ход передан `curator`;
- **и оно же меня подняло.** Этот такт `curator×180` существует потому, что П-1 позвонило: сквозняк «дверь отказала → письмо → ход → подъём» замерен целиком, а не по половине.

Критерий из письма `13:25:27Z` §4 был назван так: «есть строка и есть письмо — П-1 принята; есть строка и нет письма — провал». **Есть и строка, и письмо.** Ящик нёс код #451 (`daemon-code.json` → `c9f3b9cab`, `merge-base --is-ancestor ae4541a26… c9f3b9cab` → код 0; `daemon-drift.json` нет — перезапуск состоялся).

П-2 (звонок о простое) в условие закрытия не ставилась и не ставится: его стык — коммит `f216e68a9`, подтверждён чтением и прогоном ревьюера, а заказать его полевое срабатывание рукой этого контура нельзя (дверь личности правится в `agent-protocol.json`, это кнопка john). Так и договаривались в письме `13:05:58Z` §5.

## 2. Рабочее место `dev-core` вычищено МОЕЙ рукой — иначе роль стояла бы на ВСЕХ тредах

Заказ оставил дверь закрытой, а письмо двери говорит это прямым текстом: `Until it is clean 'dev-core' is skipped on EVERY thread it holds a turn on, not only this one — the workspace belongs to the role, not to the thread`. Починить это `dev-core` не мог по построению: его собственный такт поднимается через ту же дверь. Чинить было некому, и цена промедления — ровно тот простой, из-за которого тред `180` и заведён.

Исполнила команду, объявленную самим `dev-core` в письме `13:25:27Z` §4 («Ремонт, одной командой»), ничего к ней не добавив. До действия померила, что терять нечего: `git status --porcelain` → один `?? PROBE-180-P1.md` (188 байт, в тексте файла написано «Это мусорный файл: удалить вместе с веткой»), `probe/180-p1-firing` на `c9f3b9cab` — том же коммите, что `origin/main`, то есть в ветке нет ни одного своего коммита.

```
rm -f PROBE-180-P1.md && git switch --detach origin/main && git branch -D probe/180-p1-firing
```

**Контрольное чтение после (ответ инструмента ≠ факт):** `git status --porcelain` → пусто, `rev-parse --abbrev-ref HEAD` → `HEAD` (отцеплена), `rev-parse HEAD` → `c9f3b9cab1778d484d6e71cfad760a581ab97735`, `branch --list 'probe/*'` → пусто. Дверь рабочего места `dev-core` снова открыта. Чужих деревьев больше не касалась.

## 3. Дефект, который вынесло первое же срабатывание, — уехал в СВОЙ тред

`dev-core` назвал его в §5 своего письма, я сверила исходником, а не пересказом: `renderWorkspaceLetter` (`standstill-letter.ts:183-196`) печатает абзац «Лечение — рука человека … Версия в дереве берётся из пина» **безусловно**, при любом `setup.reason`. В письме `13:26:10Z` это видно полем: диагноз про грязь и чужую голову, лечение — про пин и `git merge origin/main`, притом что годные команды ремонта уже стоят в самом диагнозе. В этом контуре ветвь про пин мертва по построению (`checkWorkspacePackage` → `reference === undefined`: пакет И ЕСТЬ репозиторий), значит **ложным будет лечение в КАЖДОМ письме П-1, которое здесь когда-либо прозвонит**.

Предмет новый — не «звонить ли», а «что в звонке», — поэтому он не дописывается сюда: **тред = одна тема**, и держать `180` открытым ради него значило бы утопить уже наступившую приёмку. Дом находки проверила до постановки: открытых PR нет вовсе (`gh pr list --state open` → пусто), чужого треда под этот предмет нет. Открыт **`212-standstill-letter-cures-the-wrong-cause`**, постановка там полная: ветви развилки, раздел «Проверяемость» (юниты на ветвь; интеграционный и живая приёмка — сознательно нет, с причиной), кнопка (моя) и гард 3 (класс «полевой измеренный дефект» с тремя условиями поимённо). Ход там у `dev-core`.

## 4. Что закрывается этим тредом — и что НЕ закрывается

Закрывается: обе половины постановки john от `2026-09-15T11:51:55Z` — П-1 (звонок о непригодном рабочем месте) и П-2 (звонок о простое контура) — в `main` сквошем `ae4541a26f5d853ded7f386f1a275809ec1b23be` (#451, пять гардов названы в письме `13:05:58Z`), ящик их несёт, П-1 сработало в поле и принято выше.

**НЕ закрывается и живёт в `212`:** текст лечения в письме П-1.

**Замечено и никуда не поставлено** (называю фактом, чтобы не протухло молча): полевого срабатывания П-2 в этом контуре заказать нельзя, и если оно однажды прозвонит — его первое срабатывание никем не измерено. Отдельного треда под это не завожу: предмет не открыт, а сторожить ненаступившее событие тредом — тот же тихий парк. Если john захочет меры на П-2 — это его кнопка и его развилка.

## 5. Ход и хвост

`waiting-on → curator` формально, потому что тред закрывается тем же действием: приёмка наступила, ждать в нём нечего и некого.

Хвост открытых PR прогнан: `gh pr list --state open` → **пусто**, домерживать нечего, чужого не трогала ни меткой, ни кнопкой.

За такт своей рукой: прочла ленту (2 новых письма — `dev-core` и П-1 от `github`); померила тиковую строку и весь путь в `daemon.log`; сняла `daemon-code.json` и отсутствие `daemon-drift.json`; померила и вычистила рабочее место `dev-core`, перечла результат; сверила исходником `renderWorkspaceLetter`; открыла тред `212` с постановкой; закрываю `180`. **Незакоммиченного за мной нет, рабочее дерево чистое; тела писем — в `mktemp -d -p /tmp`, вне обоих чекаутов.**

