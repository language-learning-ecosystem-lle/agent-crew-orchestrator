---
from: curator
worker: claude-code
session: 7a125958-62a1-4970-a073-f497731e8358
raised: 2026-09-15T13:00:31Z
date: 2026-09-15T13:05:58Z
expects: answer
waiting-on: curator
merged-pr: 451
---

**#451 СМЁРЖЕН. Тред НЕ закрываю: приёмка — первое полевое срабатывание П-1, и merge его не принимает.**

## 1. След мержа — гарды поимённо (гард 5)

Кнопка нажата `2026-09-15T13:01:56Z`, `gh pr merge 451 --squash --match-head-commit f216e68a94aa8634d53615127fb4728a5bb881f2`.

- **гард 1 — `ok`.** `approve` на ТЕКУЩЕЙ голове `f216e68a94aa8634d53615127fb4728a5bb881f2`, автор вердикта `github-actions`, внутри ЗАКРЫТОГО круга `Claude PR Review` **34971110582** на этой же голове (`2026-09-15T12:49:07Z…12:59:28Z`). Вердикт лежит в ленте письмом `reviewer-pr` (последнее письмо этого треда, `verdict: approve` / `pr: 451`), доставка — шагами того же прогона. Прошлый `needs-fixes` на той же голове снят этим кругом, как ревьюер и назвал;
- **гард 2 — `ok`.** `2 check(s) green: review=SUCCESS, checks=SUCCESS` на том же полном SHA. Прогоны: `checks` — **34969255841** (job `104381200699`), `review` — **34971110582** (job `104387362775`);
- **гард 3 — закрыт словом john, и я прочла его своей рукой, а не пересказом.** Письмо `2026-09-15T11-51-55Z-curator.md` этой ленты (доставка решения john, сессия чата ~11:50Z, вариант 2 развилки) дословно: «**Гард 3 для обеих половин закрыт этим письмом: норма объявлена словом john здесь и сейчас**», и там же обе половины постановки — П-1 (звонок о непригодном рабочем месте) и П-2 (звонок о простое дотянуть до почты). Класс «полевой измеренный дефект, новой нормы не вводит» этот PR НЕ заявляет и заявлять не должен: норма тут объявлена прямо, а не выведена, — ревьюер прочёл так же (критерий 12). Машинная половина на месте: `thread: 180-selfheal-leaves-the-workspaces-behind` в теле PR;
- **гард 4 — `ok`.** `7 changed path(s), none of them a document of power`; список доков власти дверь взяла ДАННЫМИ — 8 путей, из них `PROTOCOL.md` и `.github/workflows` объявлены `powerDocuments`, остальные выведены из карточек;
- **гард 5 — это письмо.**

Дверь на голове `f216e68`: `nothing in the facts forbids this merge — guards 3 and 5 are yours to answer`.

**Контрольное чтение цели после кнопки (ответ инструмента ≠ факт):** `base.ref = main`, `state MERGED`, сквош **`ae4541a26f5d853ded7f386f1a275809ec1b23be`**, и он же первой строкой `git log origin/main` (`feat(daemon): непригодное рабочее место и простой контура звонят ПИСЬМОМ в ленту (тред 180) (#451)`). Заголовок — Conventional Commits, merge только squash.

## 2. Что кнопка завела на ящике — и это прямо относится к приёмке

Смёржено ≠ работает: демон исполняет ИСХОДНИКИ домашнего чекаута, а не срез `main`. Замер своей рукой сразу после кнопки:

- `.orchestrator/daemon-code.json` → загружен `d33a614afc6cf7f541987e99e15cafafaf64737f`, эпоха с `12:18:06Z`, pid `3210568` — то есть **#451 ящик ещё НЕ исполняет**;
- `.orchestrator/daemon-drift.json` → `refSha: ae4541a26…` (ровно мой сквош), `behind: 3`, и `why` дословно: «**DRAINING TO RESTART**: this box is behind and everything else about it is clean, so it launches NOTHING new and waits for the sessions that are live (`curator/180-selfheal-leaves-the-workspaces-behind`, `curator/211-parallelism-ceiling-config-says-three`) to finish by themselves; on the tick where the last one closes it repairs the tree and comes back on the new code»;
- `.orchestrator/self-restart.json` → `target: ae4541a26…`, `attempts: 0`, `drainSince: 2026-09-15T13:02:31Z`.

**Практическое следствие, и оно снимает страх самопетли:** дренаж уже идёт НА МОЙ ЖЕ сквош, и пока он идёт, ящик **не поднимает вообще ничего** — значит окончание этого хода не будет тут же съедено новым подъёмом `curator×180`, который сам же держал бы аренду и не давал перезапуску случиться. Уходят две сессии (моя и `curator×211`) — ящик чинит дерево и возвращается уже на коде с П-1 и П-2. Застать сам перезапуск ни один такт не может по построению: такт, пришедший проверить, и есть одна из аренд, которая его держит, — поэтому приёмка читается ПОСЛЕДСТВИЕМ, а не событием.

## 3. Замер, которого в треде ещё не было: генератор события П-1 в логах МОЛЧИТ

Приёмка П-1 — «первый отказ двери рабочего места ⇒ письмо в ленту». Значит надо знать, жив ли производитель этого события. Померила своей рукой, шаблоном из ИСХОДНИКА, а не по памяти (`cli.ts:14311` печатает `skipped — its workspace is not usable: …`):

- окно — два файла на диске: `daemon.log` (эпоха `2026-09-15T08:02:50Z` → сейчас) и `daemon.log.1` (`2026-09-14T14:43:45Z` → ~`07:02Z` 15.09), суммарно ≈ **22 часа**;
- **настоящих тиковых строк `skipped — its workspace is not usable` в этом окне НОЛЬ.** Все 7+3 совпадений по подстроке `not usable` — эхо СЕССИЙ в том же файле: три из прогонов `dev-core×180` (его собственная проба и вывод теста), остальные — мои же команды этого такта и слово `reusable` в чужих строках. Греп по значению без этой отбраковки дал бы «10 срабатываний» там, где их нет ни одного;
- утреннее полевое срабатывание, из которого john и получил развилку, в это окно, стало быть, не попадает — оно старше файлов на диске.

**Как это читать и как НЕ читать.** Молчание в 22-часовом окне не доказывает, что класс мёртв: оно доказывает ровно то, что дефект НЕ ЧАСТЫЙ. Но для приёмки вывод жёсткий и его надо назвать сейчас, а не у кнопки: **ждать, что П-1 выстрелит само, — не план, а срок неизвестной длины**, и норма 15.09 («новый путь исполнения принимается ПЕРВЫМ СРАБАТЫВАНИЕМ») прямо говорит, что срабатывание у редкого условия **заказывается рукой**.

## 4. Дешёвый выход для следующего такта `curator×180` — три команды, и они первой строкой работы

Чтобы такт не разворачивался вслепую и не стоил полного разбора:

1. `cat /home/lle/projects/agent-crew-orchestrator/.orchestrator/daemon-code.json` → взять `sha`;
2. `git -C /home/lle/projects/agent-crew-orchestrator merge-base --is-ancestor ae4541a26f5d853ded7f386f1a275809ec1b23be <тот sha>` — **предком, а не равенством**: самоперезапуск тянет `origin/main` ТИПОМ на момент срабатывания и в назначенный sha попадать не обязан. Код `1` ⇒ ящик #451 ещё не несёт: сказать это ОДНОЙ строкой вместе с `behind`/`why` из `daemon-drift.json` (или «файла нет» — это хорошая новость, а не сбой чтения) и передать ход, ничего больше не меряя;
3. код `0` ⇒ ящик несёт П-1 и П-2. Тогда: `grep -c 'skipped — its workspace is not usable' .orchestrator/daemon.log`, **отбрасывая строки с префиксом `[роль×тред]` — это эхо сессий, а не тики**. Есть строка и есть письмо в ленте той пары (`thread show` её треда) — П-1 принята; есть строка и письма нет — это **провал**, и он называется в треде фактом. Нет строки вовсе — приёмка ещё не наступила, и вопрос переходит в §5.

## 5. Что придётся решить, если молчание продлится, — и почему это МОЙ вопрос, а не чужой

П-1 стреляет, когда дерево пары разошлось с домашним чекаутом по пину версии (утром: `0.2.14` в дереве против `0.2.15` в чекауте). Заказать это рукой в этом контуре можно лишь двумя способами, и оба надо взвесить ДО того, как тред начнёт жечь такты: либо дождаться ближайшего переезда пина, который сам разведёт деревья, либо назвать постановку `devops`/`dev-core` на управляемое воспроизведение. Полевое срабатывание П-2 в условие закрытия не ставлю, как и договорились: дверь личности правится в `agent-protocol.json`, а это кнопка john — заказать его рукой этого контура нельзя, и стык П-2 (коммит `f216e68a9`, подтверждён и моим чтением, и прогоном ревьюера) остаётся тем, чем он был объявлен.

Ход держу на себе именно поэтому: приёмка — последствие, подъёмника у неё нет, а решение «ждать или заказывать» — моё, не исполнительское.

## 6. Ход и хвост

**`waiting-on → curator`, парка нет намеренно.** Ни одна из трёх форм (`<человек>`, `pr:N`, `run:N`) сюда не годится: `pr:451` уже смёржен и снялся бы сразу, круга в полёте нет, к john вопроса на этом такте нет. Пустых подъёмов это не наплодит: пока ящик в дренаже, он не поднимает НИЧЕГО, а первый подъём после перезапуска придёт уже на код с #451 — ровно тот такт, которому §4 и адресован.

Хвост открытых PR прогнала (`gh pr list --state open`): после моей кнопки открытыми остаются `#452` (тред `210`) и `#453` (тред `211`); `#449`/`#450` треда `200` уже в `main` (`f52995cc9`, `0b0aa0a2d`). Своим кандидатом здесь стоял только `#451`, он смёржен — домерживать нечего, чужого не трогала ни меткой, ни кнопкой. Этот прогон связан треду `180` одному.

За такт своей рукой: прочла письма `dev-core` и вердикт `reviewer-pr`; перечитала слово john `2026-09-15T11-51-55Z-curator.md` целиком; прогнала `merge-gate`; сверила чеки `gh pr checks`; нажала кнопку с `--match-head-commit`; перечла цель (`base.ref`, `mergeCommit`, `git log origin/main`); сняла три файла состояния ящика; померила генератор П-1 шаблоном из исходника в окне двух логов. **Незакоммиченного за мной нет, рабочее дерево чистое; тело письма — в `mktemp -d -p /tmp`, вне обоих чекаутов.**

