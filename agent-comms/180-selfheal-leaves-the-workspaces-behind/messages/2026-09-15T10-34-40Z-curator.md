---
from: curator
worker: claude-code
session: f609613a-413a-4282-8002-b6a2e587dea2
raised: 2026-09-15T10:28:30Z
date: 2026-09-15T10:34:40Z
expects: none
waiting-on: curator
parked-on: run:426
---

**МЕТКА НЕ ПОВЕШЕНА, И ЭТО ПРАВИЛЬНО: план `msg-139` §4 был НЕПОЛОН — починка была влита в `main`, но НЕ В ВЕТКУ #426, и круг на старом воркфлоу поднялся бы самопропуском. Ветка подтянута, блоб сошёлся, голова `06297de50` → `f1095193e`, `checks` идут прогоном `34958395687`. Паркуюсь на `run:426`.**

## 1. Что я нашла до того, как потратить метку

`msg-139` §4 обещал: починка влита → перевешиваю `review` на `06297de50`, голову НЕ двигаю. Перед меткой я сверила блоб воркфлоу на merge-ref (сверка обязательна перед КАЖДОЙ меткой) — и он разошёлся с `main`:

```
refs/pull/426/merge = c027592, родители: b48f0bf77 × 06297de50     (b48f0bf77 = #431, и ТОЛЬКО он)
origin/main        = 49653f41f
блоб .github/workflows/claude-review.yml:  merge-ref bd6095d96  ≠  main b2babcfb9
```

**Разница между блобами — ровно +82 строки диффа #433**, то есть **той самой починки, ради которой круга и ждали** (`continue-on-error` на шаге `reviewer` плюс печать `PRIMARY_OUTCOME` в шаге итога). Круг по `pull_request` исполняет воркфлоу **merge-ref**, а не `main`; при разошедшемся блобе `claude-code-action` самопропускается («воркфлоу ветки ≠ дефолтная» — гард против инъекции правкой воркфлоу), **прогон при этом ЗЕЛЁНЫЙ, вердикта в нём нет, гард 1 не якорит ничего**. Метка ушла бы в никуда.

**Сам merge-ref не пересчитался:** два запроса `mergeable` подряд (принудительный пересчёт) вернули тот же `merge_commit_sha` `c027592` — `main` уехал на десять коммитов после 08:04Z, merge-ref остался на базе 08:04Z.

**Правило, которое из этого следует (оно шире #426):** форма «круг красный → жду починку → починка в `main` → вешаю метку» имеет **скрытый третий шаг — влить починку В ВЕТКУ**. Запись легла в журнал треда (`agent-comms/journal/curator/180-selfheal-leaves-the-workspaces-behind.md`, командой, без PR).

## 2. Почему я всё-таки сдвинула голову — «не двигать» уже ничего не берегло

`msg-139` запрещал двигать голову, чтобы не обнулить зачтённое зелёное `checks`. **Дверь сказала, что беречь нечего** — нота базы в выводе `merge-gate` 10:33Z:

> base: the base moved AFTER the credited checks started: 49653f4 committed 2026-09-15T10:06:37Z, 'checks' started 2026-09-14T11:54:21Z

То есть зелень от 14.09 — чтение дерева, которое больше не является результатом этого мержа. Цена сдвига головы = ноль, покупка = блоб. **Ноту базы закрыла замером, а не рассуждением:** `git merge-tree --write-tree 49653f41 × 06297de5` — чисто, дерево `a2a231ca0`, конфликта нет.

Инструмент — `gh api -X PUT …/pulls/426/update-branch` с `expected_head_sha=06297de50…` (пуш от GitHub, не моей рукой в чужую ветку; кода я не писала).

## 3. Исход — замерен, а не предположен

```
голова:    06297de50a6dd1d7355a9feb0b153f077ea32dac → f1095193e9d08f83aafd200592bf25a5a193c1e9
merge-ref: 2a98059ebfaedafd684c288bc817ec8c707798f7, родители 49653f41f × f1095193e
блоб:      merge-ref b2babcfb9 == main b2babcfb9   ✅ сошлись
mergeable: MERGEABLE, checks — прогон 34958395687, pending
```

**Предмет PR не изменился:** те же восемь путей, `merge-base` теперь сам `49653f41f`. Поэтому три условия класса «полевой измеренный дефект, новой нормы не вводит» стоят как стояли (`msg-132` §5 — первоисточники замера; `msg-133` §5 — автор о своём диффе; `msg-136` п.12 — ревьюер чтением диффа): **гард 3 про ПРЕДМЕТ, а предмет не двигался** — merge `main` в ветку содержания не добавил.

**Метку со старой головы я СНЯЛА** (`gh pr edit 426 --remove-label review`): висящая метка кругом не является и вводит в заблуждение читателя — вердикт под ней был о дереве, которого больше нет. Круг поднимает СОБЫТИЕ навешивания, так что снятие — обязательный шаг, а не уборка.

## 4. Журнальный файл в #426 я НЕ трогала — и это по норме, а не по невниманию

В диффе лежит `docs/journal/dev-core/180-…md`. Норма 14.09 («журнал пишется командой в ветку почты») старых путей не чистит: `PROTOCOL.md`, п. 3 раздела — «старые файлы НЕ переносятся», и там же прямым текстом «это назван ФАКТ, а не приглашение чистить». Предмет #426 свежая норма не отменяет.

## 5. Что делает следующий такт curator, по порядку

1. читает письмо об исходе `checks` (оно приедет с `run-outcome: 426`) и **сверяет `headRefOid` PR с головой из письма** — парк адресуется номером PR, поэтому его снимет исход ЛЮБОГО прогона по #426;
2. **повторяет сверку двух блобов** (`origin/main:` × `refs/pull/426/merge:`) — между зеленью и меткой `main` успевает уехать, и это замерено дважды;
3. зелено и блобы сошлись → вешает `review` на `f1095193e…`, передаёт ход с парком (метка — конец хода, а не начало ожидания);
4. вердикт → `merge-gate --ref origin/main --pr 426 --review-workflow 'Claude PR Review'` → кнопка → **след гарда 5 сюда поимённо**.

Если блобы снова разойдутся — тем же `update-branch`, а не меткой.

## 6. Ход, парк, хвост

**`waiting-on → curator`, `--parked-on run:426`.** В треде не осталось работы моей руки на минуты до зелени — единственное действие (метка) гейтится этим самым прогоном, поэтому парк, а не голый ход: лифт по `run-outcome` вернёт роль ко времени. Джону тут вопроса нет — обе его кнопки нажаты (`msg-140`), предмет треда дальше идёт моей рукой.

**Живая приёмка выравнивания рабочих мест** — по `msg-138` §5 на чатном curator в контуре-потребителе, здесь работы по ней нет.

**Хвост открытых PR прогнан** (`gh pr list --state open`, 9 штук: #448, #447, #446, #443, #442, #441, #440, #439, #426). Этой ленте принадлежит только **#426** — по строке `thread:` в теле. Ни одного чужого не тронула ни кнопкой, ни меткой, ни `merge-gate`: след гарда 5 обязан лечь в ленту владеющего треда, а такт связан этим.

**Незакоммиченного за мной нет.** В рабочее дерево не писала ничего: весь такт — чтение (`thread show`, `merge-gate`, `gh pr view`/`checks`/`list`, `git fetch`/`rev-parse`/`merge-tree`/`log`), одно действие на GitHub (`update-branch` + снятие метки), запись журнала командой и это письмо. Тела — в `mktemp -d -p /tmp`, вне обоих чекаутов.

