---
from: github
worker: agent-protocol
date: 2026-09-15T13:26:10Z
expects: none
waiting-on: curator
---

**Рабочее место роли `dev-core` непригодно — пара `dev-core×180-selfheal-leaves-the-workspaces-behind` НЕ ПОДНИМАЕТСЯ, и тик её не починит.**

Дверь рабочего места отказала: the workspace has uncommitted changes left by the 'exited-without-handoff' run of this pair, and its head is on 'probe/180-p1-firing' — a branch that is not 'dev-core's to write to (neither named for the role nor signed by it): the circuit commits a role's leftovers onto the role's own head and never onto a common or a foreign one. What lies there: 1 path(s) — PROBE-180-P1.md (untracked, not counted). Until it is clean 'dev-core' is skipped on EVERY thread it holds a turn on, not only this one — the workspace belongs to the role, not to the thread. Read it, then either KEEP it — git -C /home/lle/projects/agent-crew-orchestrator/.worktrees/dev-core@180-selfheal-leaves-the-workspaces-behind checkout -b dev-core/180-selfheal-leaves-the-workspaces-behind && git -C /home/lle/projects/agent-crew-orchestrator/.worktrees/dev-core@180-selfheal-leaves-the-workspaces-behind add -A && git -C /home/lle/projects/agent-crew-orchestrator/.worktrees/dev-core@180-selfheal-leaves-the-workspaces-behind commit -m 'wip(180-selfheal-leaves-the-workspaces-behind): what the interrupted run left' && git -C /home/lle/projects/agent-crew-orchestrator/.worktrees/dev-core@180-selfheal-leaves-the-workspaces-behind push -u origin dev-core/180-selfheal-leaves-the-workspaces-behind — or PARK it: git -C /home/lle/projects/agent-crew-orchestrator/.worktrees/dev-core@180-selfheal-leaves-the-workspaces-behind stash push -u -m 'wip(180-selfheal-leaves-the-workspaces-behind): what the interrupted run left'. Either one leaves the tree clean and the role starts on the next tick

**Лечение — рука человека, и оно НЕ `pnpm install`.** Версия в дереве берётся из пина, а пин лежит в файлах САМОЙ ВЕТКИ: пока ветка не подтянула `main`, переустановка пакета не меняет ничего (замер john 15.09: `pnpm install --frozen-lockfile` → прежняя версия; `git merge --ff-only origin/main` → `fatal: Not possible to fast-forward`; `git merge --no-edit origin/main` → новая версия, и коммит слияния отклонил хук `conventional-commits`, довершено `git commit --no-verify`).

Дерево роли на СОБСТВЕННОЙ ветке ящик не трогает (граница john 12.09, подтверждена 15.09): автоматического слияния `main` в ветку роли не будет — вместо него звонит это письмо.

Ход передан роли `curator`: пара, о которой письмо, поднята быть не может, поэтому ход ей не адресуется.
