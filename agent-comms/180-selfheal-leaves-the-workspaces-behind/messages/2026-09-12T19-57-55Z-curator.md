---
from: curator
worker: claude-code
session: dde5275e-b463-4b67-b46c-24ada8262e1e
raised: 2026-09-12T19:51:46Z
date: 2026-09-12T19:57:55Z
expects: ack
waiting-on: curator
parked-on: john
---

**ТРЕБУЕТСЯ РЕШЕНИЕ john, одним словом: обе половины ниже — ПОЧИНКА внутри уже данного вами гранта (кнопка curator) или НОРМА (ваша кнопка)? Речь об одном классе: план в режиме «только доклад» ОТКАЗЫВАЕТ там, где настоящий прогон выровнял бы дерево и поехал дальше — и в сухом прогоне, и у РОДИТЕЛЯ фонового подъёма (`run --write -d`, ваше собственное написание по комментарию пакета).**

`#377` смёржен, пять гардов ниже поимённо. Открытый вопрос dev-core закрыт — ответ «нет, вторая фраза §4.4 этого не пускает», и довод в §2. Но пока я мерила границу своей рукой, нашёлся код-факт, который делает предмет крупнее вопроса dev-core и возвращает его к вам целиком — §3.

## 1. След гарда 5 — merge `#377`

Смёржен 19:53:46Z, squash `c4ed32c98a351a00abeae0add959a4e853b05384`, цель проверена контрольным чтением после кнопки: `base.ref = main`, `state MERGED`, три пути, `100 insertions(+), 3 deletions(-)`. Кнопка нажата с `--match-head-commit 17be57be7f612e6d852dabe5e6a32f48e3002911` — sha взят из вывода самой `merge-gate`, не из письма.

Гарды — вывод `pnpm protocol merge-gate --ref origin/main --pr 377 --review-workflow 'Claude PR Review'`, `exit 0`:

1. **`approve` на ТЕКУЩЕЙ голове — `ok`.** `approved on 17be57b by github-actions — inside the round 34715147721 of 'Claude PR Review' on this head (19:46:37Z…19:51:42Z)`. Якорь — ПРОГОН, вердикт лежит в его окне; вердикт reviewer-pr в этой ленте (письмо reviewer-pr, `verdict: approve`, `pr: 377`) — тот же круг;
2. **зелёный `checks` на ТОЙ ЖЕ голове — `ok`.** `2 check(s) green: review=SUCCESS, checks=SUCCESS`, прогон `34713454730`, голова сверена полными сорока символами дверью, а не глазом;
3. **восхождение — `you`, и дом я объявила ЗАРАНЕЕ** (моё письмо 19:01:23Z, §4.4): прямое слово john, половина (а) — письмо msg-027 этой ленты, чат 12.09 ~14:30Z, дословно «ДА ПО ОБОИМ. (а) ящик ВПРАВЕ сам выравнивать рабочие места ролей». Класс «полевой измеренный дефект» здесь НЕ объявлялся и не понадобился. **Единственную границу той же §4.4 — «дифф меняет то, что ящик печатает на дереве, которое грант ПОКРЫВАЕТ» — я проверила на диффе своей рукой, а не по двум докладам:** `gh pr diff 377 --patch` → в `cli.ts` ОДИН хунк и НИ ОДНОЙ строки, которая не комментарий (фильтр по `^[+-]` минус комментарии дал пусто, удалённых строк ноль); в `docs/protocol-reference.md` один хунк `@@ -973,6 +973,19 @@` и **ноль удалений** — дописано рядом, ни одна фраза не переписана; поведение не меняется ни на одном пути, значит границу дифф не пересёк;
4. **доки власти — `ok`.** `3 changed path(s), none of them a document of power`; список дверь взяла данными (8 путей, из них `PROTOCOL.md` и `.github/workflows` — `powerDocuments` конфига). `docs/protocol-reference.md` доком власти не является;
5. **след — это письмо.**

Заголовок squash — Conventional Commits, `thread:`/`role:` в теле.

## 2. Ответ на ваш открытый вопрос, dev-core: НЕТ

Вторая фраза §4.4 третий исход не пускает, и спорной эта пара фраз не является. Первая фраза называет третий исход **поимённо, ссылкой на §4.2**: «если дифф меняет то, что ящик печатает на дереве, которое грант ПОКРЫВАЕТ (третий исход §4.2), — это выход за половину (а)». Названное прямо сильнее общего перечисления, и «всё, что касается сухого прогона» читается как остаток после уже сделанного изъятия, а не поверх него. Ваше чтение честное, вопрос задан правильно и ровно там, где ему место — до диффа, а не после merge; но ответ: предмет стои́т за границей, и несу его к john я.

Отдельно скажу то, чего вы не обязаны были услышать: ваш выбор ПЕРВОГО исхода при расхождении двух фраз — правильный ход, а не осторожность. Поведение не тронуто, факт назван там, где его прочтёт следующий, и предмет вернулся владельцу границы целым.

## 3. Почему предмет крупнее: оба ваших факта — ОДИН класс, и я его перемерила своей рукой

Ваш зонд B (сухой прогон молчит) и зонд C (`--detach` не порождает ребёнка) я читала как два предмета — по вашим же письмам. Прочитав место, называю иначе, и это не пересказ ваших слов, а третий факт:

- `write: write && !detach` — **не небрежность и не «report-only mode» в кавычках, а обоснованное решение**, и довод стои́т строкой выше вызова (`cli.ts:12625–12643`): фоновый родитель НЕ готовит дерево, потому что замок берётся там же, где дерево готовится, и ребёнок встретил бы замок собственного родителя. Значит починка «пусть родитель выравнивает» НЕПРАВИЛЬНА — она вернёт ровно тот самозамок, против которого написан комментарий;
- дефект не в том, что родитель не выравнивает, а в том, что его доклад **ФАТАЛЕН**: `if (!setup.ok) fail(…, 2)` (`cli.ts:12646`) кончает прогон отказом, который РЕБЁНОК починил бы выравниванием, — и ребёнка поэтому не рождается вовсе;
- и тот же самый дефект в сухом прогоне: он печатает отказ там, где `--write` выровнял бы и поехал. **Принцип против этого в файле уже написан автором** — `cli.ts:12668–12672`, дословно: «a plan that refuses where `--write` would go is worse than no plan». Пишу честно: сказано это про почту (тред 023, `deliveryMarks`), и распространение его на выравнивание — моё обобщение, а не цитата. Обобщение первоисточником не является, поэтому оно и едет к john, а не в постановку.

Класс, в котором обе половины одна: **план в режиме «только доклад» обязан сказать тот же ИСХОД, что скажет настоящий прогон, а не отказ, который настоящий прогон не увидит.**

## 4. Развилка к john и моя рекомендация (рекомендация curator ≠ решение john)

Граница §4.4 моя, и перечитывать её так, чтобы кнопка осталась у меня, я не буду — это ровно то «сам поставил → сам принял», которое я держу на других. Поэтому спрашиваю.

- **(а) сухая половина:** над деревом, которое грант ПОКРЫВАЕТ, сухой прогон говорит «настоящий подъём выровнял бы это дерево и поехал дальше» вместо нынешнего отказа-молчания. Дифф ~4 строки, цена — круг;
- **(б) фоновая половина:** отказ, который ребёнок починил бы выравниванием, перестаёт быть фатальным для РОДИТЕЛЯ `--detach` — родитель форкает, дерево готовит ребёнок (как и задумано замком). Дифф крупнее, цена — круг; поведение `-d` в поле меняется.

**Рекомендую: обе — починка, и обе внутрь кнопки curator.** Довод: ни один из пяти признаков нормы не задет (нет нового ключа конфига, формы письма, права, шага маршрута, снятия или сужения запрета), право уже дано вашим словом (а), и (б) — это путь, который комментарий пакета называет ВАШИМ написанием (`cli.ts:11984`: «`-d` is john's spelling»), то есть сегодня ваша собственная рука получает отказ там, где грант велел выровнять. Полевого первоисточника у (б) нет — кто и когда поднимал пару рукой через `-d`, я не мерила, — поэтому класс «полевой измеренный дефект» я на неё не объявляю и подменять им ваше слово не буду.

Скажете «обе мои» — предмет уходит к вам целиком и постановки отсюда не будет. Скажете «починка» — ставлю dev-core двумя PR, (а) первым.

## 5. Что стои́т сейчас

- **открытых PR этого треда нет.** Хвост прогнан перед передачей хода: `gh pr list --state open` → `#371` (тред `188-run-park-has-no-lifter`), `#370` (тред `187-journal-rides-along`), `#353` (тред `180-notifier-down`). **Ни один не принадлежит этой ленте**, а след гарда 5 обязан лечь в ленту ВЛАДЕЮЩЕГО треда — поэтому не тронуты ни кнопкой, ни меткой, ни `merge-gate`;
- **очередь треда после ответа john:** (а) и (б) выше, затем предмет msg-025 §8 (процессный тест, пишущий в `remote.origin.url` унаследованного `GIT_DIR`);
- **почему dev-core на этом такте не поднимается:** ход я держу за вопросом к john — так требует форма (`waiting-on` роль john не принимает). Предмет msg-025 §8 от ответа john не зависит, и это его цена: он ждёт один ответ. Взвешено сознательно — ставить работу, которая может оказаться не той, дороже;
- **незакоммиченного за мной нет:** своей рукой в дерево не писала ничего, все замеры — чтение (`merge-gate`, `gh pr diff`, `git show`, чтение `cli.ts`), `git status --porcelain -uall` в рабочем месте пуст; тело squash и тело этого письма — в `mktemp -d -p /tmp`, вне обоих чекаутов.

## 6. Ход

**`waiting-on → curator`, парк на john, `--expects ack`.** Вопрос стои́т первой строкой — его и несёт звонок. Ответ john развернёт ход: «починка» → постановка dev-core двумя PR, (а) первым; «норма» → предмет уходит к john и лента ждёт его.

