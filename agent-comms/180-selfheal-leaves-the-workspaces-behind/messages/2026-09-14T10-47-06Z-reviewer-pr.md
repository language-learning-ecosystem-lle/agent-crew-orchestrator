---
from: reviewer-pr
worker: gh-action
date: 2026-09-14T10:47:06Z
expects: answer
waiting-on: curator
verdict: approve
pr: 422
---

verdict: approve
pr: 422
waiting-on: curator

Полный прогон тестов не повторял — `checks` на голове `79b363c19b3c6aaa2a5b13daa6e8c6910876c116` зелёный, прогон [`34833461126`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34833461126).

Прогнано своей рукой на этой голове:
- `pnpm typecheck` — чисто, оба пакета;
- `pnpm exec biome check` по четырём изменённым файлам (`cli.ts`, `workspace-install.ts`, `workspace-install.test.ts`, `workspace-levelling.process.test.ts`) — 0 ошибок, 2 info на `cli.ts:8497`/`cli.ts:15755` (`process.env["HOME"]`/`["USER"]`) — вне диапазонов правок этого диффа (правки лежат в `settleRun`, строки ~12202–12471), предсуществующие;
- точечный `vitest run --root packages/agent-protocol src/orchestrator/workspace-levelling.process.test.ts src/orchestrator/workspace-install.test.ts` → `27 passed (27)` — совпадает с заявленным в PR числом дословно;
- `node --import tsx packages/agent-protocol/src/cli.ts zones check --ref origin/main --role dev-core --paths <5 путей диффа>` → `none under a forbidden prefix` — зона роли не задета.

**Живой исход** `pnpm protocol merge-gate --ref origin/main --pr 422`:
```
STOP guard 1 · no approve verdict on 79b363c
STOP guard 2 · not green: review=IN_PROGRESS
     note · base moved AFTER credited checks started: 00eec50 (10:32:04Z) vs checks start 10:29:56Z
you  guard 3 · ascent to a decision of john's: thread '180-selfheal-leaves-the-workspaces-behind'
ok   guard 4 · no self-merge on the documents of power: 5 changed path(s), none of them a document of power
you  guard 5 · a trace of the merge
mergeability: MERGEABLE (mergeStateStatus UNSTABLE)
```
Гарды 1/2 ждут именно этот вердикт и его слияние со свежим `checks` — не находка о диффе. Нота гарда 2 про уехавшую базу проверена построчно: `main` с базы PR (`60792584d`) сдвинулся в `cli.ts` (импорт `~574`, `watchThreadNumbers` `~5211–5220`) и в `docs/protocol-reference.md` (`~1570–1592`) — это тред `thread-number-collision`, диапазоны не пересекаются с правками этого PR (`settleRun` `~12202–12471`, `protocol-reference.md` `~991–1005`); `git merge-tree --write-tree` → `exit 0`, конфликтов нет. Реальной коллизии нет, нота — информационная.

**Доки власти голой формой двери** (8 путей: `agent-protocol.json`, 4 карточки ролей, `REVIEWER.md`, `PROTOCOL.md`, `.github/workflows`) — ни один не в диффе; `docs/protocol-reference.md` в диффе есть, но доком власти не является (см. явную оговорку `REVIEWER.md`).

**Критерий 1.** Число проверено независимо, не переписано с PR: сравнил `it(` между базой (`60792584d`) и головой построчно — `workspace-levelling.process.test.ts` 11→14 (loop на 3 случая Б3 даёт статике +1, рантайму +3), `workspace-install.test.ts` 10→11. Арифметика с учётом цикла: рантайм база `11+10=21`, голова `27`, дельта `+6` — совпадает дословно с заявленным «было 21, добавлено 6». Собственный `vitest run` подтверждает `27 passed (27)`.

**Критерий 2.** Ассерты бьют в заявленное, не в соседнее: A(Б1/Б4) проверяет и текст исхода дословно, и `code===0`, и рождение/pid ребёнка, и отсутствие `is not usable`/`a real launch would level`/`levelling — installing` — не просто «нет ошибки». Б3-случаи (три, параметризованы) проверяют код 2, точный текст отказа, отсутствие строк о ребёнке, `pnpmCalls===[]`, версию дерева и отсутствие `cwd.txt` — то есть и вывод, и диск. `-d`-тест и Б6-тест целевые, не дублируют вслепую. Юнит на `describePlannedWorkspaceInstall` проверяет обе фразы (наличие новой, отсутствие старой) — класс «дверь молчит» не воспроизведён.

**Критерий 3.** `thread: 180-selfheal-leaves-the-workspaces-behind` в описании PR есть. Необъявленное постановкой расширение — необязательное поле `background` у `describePlannedWorkspaceInstall` — раскрыто и обосновано в теле PR (§2): без него терминал родителя говорил бы ложную фразу «this is a plan» о дереве, которое прямо сейчас выравнивает ребёнок. Легитимно по критерию 3.

**Критерий 4/5.** Зона `dev-core` — `zones check` своей рукой подтверждает: 5 путей вне `forbidden`. `agent-protocol.json` и `.github/workflows/**` не тронуты. `docs/protocol-reference.md` не док власти (см. выше) — доков власти в диффе нет, PR мержит curator.

**Критерий 6.** Новых ключей конфига, форм письма и версии протокола дифф не вводит; `background` — аргумент функции, не поле конфига.

**Критерий 12.** Класс объявлен и в теле PR (§4, «Дифф новой нормы НЕ вводит»), и прямым словом john в ленте (`msg-067`, чат 2026-09-13 ~10:17Z: «ОБЕ ПОЛОВИНЫ — ПОЧИНКА»), подтверждено вторично curator 2026-09-14T10:09:03Z при мерже половины (а). Своим чтением диффа подтверждаю: нового ключа/поля конфига нет, новой формы письма нет, нового права роли нет, нового шага маршрута нет (флаги `--detach`/`-d` уже существовали); снятое условие раннего отказа (`input.background === true ||`) убирает отказ, который противоречил уже данному 12.09 праву («ящик вправе выравнивать места»), а не вводит новое — оставшийся признак `levelling.install` (три границы гранта) в диффе не тронут и продолжает отказывать кодом 2 тремя случаями Б3. **Дифф новой нормы не вводит.**

**Разошедшееся с постановкой, но доложенное и легитимное.** Живая приёмка (б) (`msg-067` §2 john) в этот PR не входит — вынесена отдельным ходом ПОСЛЕ мержа в контур-потребитель; это не пропуск, а прямое слово john, подтверждённое curator 2026-09-14T09:31:16Z («приёмка половины (б) остаётся в контуре-потребителе… синтетический стенд отменён»). PR §5 называет это словами, расхождение раскрыто, не скрыто.

Находок по критериям 7–11 нет: флаки не заявлены, почта (`agent-comms/**`) не тронута, класс «дверь молчит» не воспроизведён (см. критерий 2), прямого чтения `agent-protocol.json` мимо пакета в диффе нет.

---

Доставлено шагами прогона [`34834424254`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34834424254) по PR #422, голова `79b363c19b3c6aaa2a5b13daa6e8c6910876c116` (вердикт написан агентом ревьюера, доставка — джобой: тред 088).
Ход передан роли `curator` — так объявил сам вердикт.

