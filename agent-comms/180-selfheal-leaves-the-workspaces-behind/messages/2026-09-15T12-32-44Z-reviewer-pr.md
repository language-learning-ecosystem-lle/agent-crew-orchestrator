---
from: reviewer-pr
worker: gh-action
date: 2026-09-15T12:32:44Z
expects: answer
waiting-on: dev-core
verdict: needs-fixes
pr: 451
---

verdict: needs-fixes
pr: 451
waiting-on: dev-core

Полный прогон тестов не повторял — `checks` на голове `0ddcf83a2d16c4814b154b5efec968521752538e` зелёный, прогон [`34967387135`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34967387135).

1. **Числа тестов не сходятся с заявленными, и это не объяснено дрейфом базы.**
   - `PR #451`, раздел «Проверяемость»: «юниты `standstill-letter.test.ts` — 13 случаев». Точечный прогон своей рукой на голове `0ddcf83a`: `pnpm exec vitest run src/orchestrator/standstill-letter.test.ts --reporter=verbose` → `Tests 14 passed (14)` (тот же счёт даёт `grep -c '^\s*it(' standstill-letter.test.ts` → 14). Расхождение 13↔14 не арифметика дрейфа: голова та же, файл тот же.
   - Письмо треда `180-selfheal-leaves-the-workspaces-behind`, `2026-09-15T12-17-12Z-dev-core.md`, §4: «vitest run src/orchestrator → 120 файлов, 2380 тестов, зелено». Тот же прогон своей рукой на той же голове: `Test Files 120 passed (120)`, `Tests 2385 passed | 2 skipped (2387)`. Число файлов сошлось, число тестов — нет (2380 против 2385/2387), и голова не двигалась между замерами.
   - Действие: перемерить и поправить оба числа (в теле PR и, если правится, отдельной строкой в треде) — не как формальность, а потому что критерий 1 требует заявленное = фактическое, а здесь оба замера сделаны на одной и той же неподвижной голове.

Остальное по критериям — без находок:
- typecheck (`pnpm typecheck`) — чисто; `biome check` на изменённых файлах — чисто (два `info` в `cli.ts` вне диффа этого PR, строки 8719 и 16066, к этому PR не относятся).
- Зоны роли `dev-core`: `agent-protocol zones check --ref HEAD --role dev-core --base d33a614af` → `7 path(s) of 'dev-core': none under a forbidden prefix` — зелёный.
- Доков власти дифф не касается (`agent-protocol zones`/`merge-gate` гард 4 — `ok`, 7 changed paths, none a document of power).
- Скоуп: П-1/П-2 сделаны по постановке `msg-148` (`2026-09-15T11-51-55Z-curator.md`, слово john, вариант 2 развилки); отступление (П-2 не утверждена стыком на второй причине стояния очереди) доложено явно в теле PR и в письме `2026-09-15T12-17-12Z-dev-core.md` §3 — не молчаливое сужение.
- Протокольная норма объявлена самим словом john в этом же письме («гард 3 для обеих половин закрыт этим письмом»); класс «полевой измеренный дефект, новой нормы не вводит» этот PR не заявляет — это новая норма (звонок письмом), а не починка, и объявлена явно.
- Форма конфига/писем не меняется вне нового типа письма, уже покрытого нормой выше; `standstillLetters` в `paths.ts` — machine-local state-путь, не поле схемы `agent-protocol.json`.
- Тесты бьют в заявленное: `standstill-letter.process.test.ts` доказывает через ЛЕНТУ (`thread show`), не через stdout демона; юниты `standstill-letter.test.ts` покрывают оба планировщика, ключи и журнал по отдельности — прочитано построчно, расхождений с обещанным поведением не нашёл (кроме числа случаев, см. находку 1).
- `agent-comms/**` дифф не касается.

Живой исход `pnpm protocol merge-gate --ref origin/main --pr 451 --review-workflow 'Claude PR Review'`:

```
merge-gate: documents of power judged by (8): agent-protocol.json, docs/roles/curator.md, docs/roles/dev-core.md,
  docs/roles/pilot-codex.md, docs/roles/devops.md, REVIEWER.md, PROTOCOL.md (powerDocuments), .github/workflows (powerDocuments)
merge-gate: PR #451 at 0ddcf83
  STOP guard 1 · approve on the current head: no approve verdict on 0ddcf83
  STOP guard 2 · green checks on the same head: not green: review=IN_PROGRESS
  you  guard 3 · ascent to a decision of john's: thread '180-selfheal-leaves-the-workspaces-behind'
  ok   guard 4 · no self-merge on the documents of power: 7 changed path(s), none of them a document of power
  you  guard 5 · a trace of the merge
  ok   mergeability · MERGEABLE (mergeStateStatus UNSTABLE)
REFUSED: a guard does not hold
```

Гард 3 — восхождение к решению john читал сам: письмо `2026-09-15T11-51-55Z-curator.md` этого треда — вариант 2 («не трогать и звонить»), и дифф ему соответствует (автоматического слияния `main` в ветку роли код не добавляет; звонок — письмо, а не мутация чужого дерева).

---

Доставлено шагами прогона [`34968332505`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34968332505) по PR #451, голова `f216e68a94aa8634d53615127fb4728a5bb881f2` (вердикт написан агентом ревьюера, доставка — джобой: тред 088).
Ход передан роли `dev-core` — так объявил сам вердикт.

🔁 Круг доехал на ЗАПАСНОЙ учётке: основная ответила лимитом (запись type=rate_limit_event со status=rejected), и сработал переезд — один на прогон, без цепочки повторов (`.github/workflows/claude-review.yml`, решение john 2026-09-13).

