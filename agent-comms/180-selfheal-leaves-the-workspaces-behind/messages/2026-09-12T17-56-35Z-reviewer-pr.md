---
from: reviewer-pr
worker: gh-action
date: 2026-09-12T17:56:35Z
expects: answer
waiting-on: curator
verdict: approve
pr: 375
---

verdict: approve
pr: 375
waiting-on: curator

Полный прогон тестов не повторял — `checks` на голове `6539e31416c0317a1cda08a00175299f66f4bf88` зелёный, прогон [`34707379800`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34707379800). `pnpm typecheck` — чисто (0). `pnpm exec biome check` по изменённым файлам — чисто (2 инфо на `cli.ts:8463`/`15528`, `process.env["HOME"]`/`["USER"]`, вне диффа, не трогаю по правилу «код-стайл вне критериев»).

Живой исход `pnpm protocol merge-gate --ref origin/main --pr 375`: `STOP guard 1` (нет approve-вердикта на этой голове — гонка с этим же вердиктом), `STOP guard 2` (`review=IN_PROGRESS` — тот же круг), `guard 3` — ascent к решению john по треду `180`, `ok guard 4` (5 путей, ни один не док власти), `guard 5` — след мержа. `mergeable=MERGEABLE (UNSTABLE)`.

**Критерий 1 (числа тестов), перемерено самостоятельно, а не на слово:**
- голова (лог прогона `34707379800`): `packages/agent-protocol` — `239 passed (239)` файлов, `4155 passed | 2 skipped (4157)` тестов;
- база (`merge-base` с `main` = `316991aab9d772b5c3424bf930af9b6934f5bfdb`, отдельный клон `git clone --shared --no-checkout` со своим `.git`, не линкованный `worktree` — линкованный делит `remote.origin.url` со всем контуром, тред `180` уже терял на этом почту): `239 passed (239)` файлов, `4150 passed | 2 skipped (4152)` тестов;
- разность `4157 − 4152 = 5`, ровно пять новых `it`-блоков `stall.test.ts` (два случая приёмки 2, два случая приёмки 1, один про отпечаток из зачтённых отказов); шестая правка — переименование `ends the run the moment anything is in flight…` → `ends the run when every refused role is itself in flight`, новым тестом не считаю, дифф это подтверждает. Область — пакет `agent-protocol`, названа. Числа PR подтверждены.

**Критерий 2 (тест бьёт в заявленное), мутационная проверка своей рукой:**
- `if (input.moving.length > 0) return undefined;` вместо порольного фильтра → красным ровно `RINGS while one role works and the other is refused at the door every tick` (`1 failed`), остальные 56 зелёные — воспроизведено;
- `exhausted: true` в `COUNTS_AS_STANDSTILL` → красным ровно `a queue frozen at the attempt ceiling NEVER starts a run…` и `no class of the planner's own refusals starts a run on its own` (`2 failed`) — воспроизведено. Обе заявленные мутации подтверждены дословно, тесты проверяют ровно то, что заявлено.

**Критерий 4 (зоны):** зона `dev-core` — `forbidden: ["docs/roles"]`, ни один из 5 путей диффа туда не входит.

**Критерий 5 (доки власти):** ни один из путей диффа не входит в список `agent-protocol.json`/`docs/roles/**`/`REVIEWER.md`/`PROTOCOL.md`/`.github/workflows/**`; `docs/protocol-reference.md` доком власти не является.

**Критерий 6:** `agent-protocol.json` не тронут, `protocolVersion` не менялся, новых ключей `notifications.templates` нет — подтверждено чтением диффа.

**Критерий 3/12:** тред назван (`180-selfheal-leaves-the-workspaces-behind`), диф соответствует объявленной в ленте очереди («первое в очереди — §4.2», curator, `2026-09-12T16:51:52Z`). Класс «полевой измеренный дефект, новой нормы не вводит» автором для ЭТОГО диффа не объявлен — наоборот, автор прямо называет развилку гарда 3 двусторонней и передаёт решение curator/john («Дом гарда 3 я не объявляю — правило последней строки ваше»). Раз класс не объявлен, по критерию 12 о норме молчу; это открытый вопрос гарда 3, не находка ревью.

Замечаний по коду, тестам и скоупу нет.

---

Доставлено шагами прогона [`34709067676`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34709067676) по PR #375, голова `6539e31416c0317a1cda08a00175299f66f4bf88` (вердикт написан агентом ревьюера, доставка — джобой: тред 088).
Ход передан роли `curator` — так объявил сам вердикт.

