---
from: curator
worker: claude-ai
date: 2026-09-14T13:27:14Z
expects: none
waiting-on: curator
park-lifted: john
priority: high
---

**Слово john (чат 2026-09-14 ~13:27Z): ДЕЛАЙ ОБЕ ПОЛОВИНЫ. Ставь `dev-core` тредом на воркфлоу; кнопка мержа его. Парк снят.**

## 1. Что чинится

**(а) Круг не краснеет, когда вердикт СДЕЛАН.** Основной шаг отработал 37 ходов, 6,5 минуты, $1,02 — и джоба всё равно `failure` по `is_error: true`. Снять падение основного шага с исхода джобы в случае, когда переезд отработал и вердикт доставлен: «вердикт доставлен» и «круг зелёный» не должны расходиться.

**(б) Переезд НЕ срабатывает на предупреждении.** Шаг №10 судит по НАЛИЧИЮ записи `rate_limit_event`, а `status: allowed_warning` означает «запрос пропущен, приближаешься к пределу». Судить по `status`, а не по наличию.

**Довод john по (б) — деньги, его граница:** сегодня предупреждение купило **второе полное суждение с нуля** ($0,7627 поверх $1,0177) и не спасло ничего. Это удвоение расхода на КАЖДОМ круге, где учётка просто предупредила.

**Довод по (а) — не деньги, а тупик:** пока основной шаг роняет джобу, **любой PR, чей круг переехал, не мёржится НИКАКОЙ рукой** — ни роли (гарды 1+2), ни john (красный `review` на голове стои́т для всех).

## 2. Это НЕ повтор #418 — и разделение john берёт целиком

#418 лечил зеркальную форму: краснел ШАГ ПЕРЕЕЗДА поверх успешного основного, и лечение сделало переезд `skipped`, когда он не нужен. **Здесь переезд отработал начисто, а краснит ОСНОВНОЙ шаг.** Шесть зелёных кругов после #418 — это круги, где переезд не срабатывал; сегодняшний первый, где сработал, и он красный.

## 3. Границы

- **правится только `claude-review.yml`**, шаги №9 и №10; `continue-on-error` на шаге 17 (телеметрия) не трогать;
- **переезд не выключать** — он нужен на настоящем лимите (`num_turns: 1`, `cost: 0`, стена);
- **приёмка (а) — полем:** круг, на котором переезд СРАБОТАЛ, приходит зелёным и даёт вердикт;
- **приёмка (б) — полем:** запись `allowed_warning` больше не рождает переезда; запись со статусом отказа — рождает.

## 4. Что делать с #426 — не тратить метку до починки

Решение john: **метку не перевешивать**, пока (а)+(б) не влиты. Твой расчёт верен — перевешивание сегодня вероятнее всего купит ещё $1,78 и ещё один красный круг.

**После мержа починки:** перевесить `review` на `06297de50` (голову не двигать — новый коммит обнулит зачтённое зелёное `checks`), поймать вердикт, нажать, положить след гарда 5 сюда.

## 5. Живая приёмка (б) в контуре-потребителе — берёт чатный curator

Твой звонок принят: из твоей руки её не бывает по границе контура, а в ЭТОМ контуре наблюдение бессмысленно по построению (`checkWorkspacePackage` возвращает `ok: true` при `reference === undefined`, а он `undefined` у всех трёх пар — пакет запускается из исходников).

**Постановку в контур-потребитель несёт чатный curator.** На тебе она не висит.

waiting-on → curator, парк снят.
