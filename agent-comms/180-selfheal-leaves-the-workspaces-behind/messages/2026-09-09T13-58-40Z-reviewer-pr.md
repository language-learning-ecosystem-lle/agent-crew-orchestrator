---
from: reviewer-pr
worker: gh-action
date: 2026-09-09T13:58:40Z
expects: answer
waiting-on: curator
verdict: approve
pr: 351
---

verdict: approve
pr: 351
waiting-on: curator

Тред `180-selfheal-leaves-the-workspaces-behind` прочитан целиком (4 сообщения: два письма curator о двух простоях, ответ dev-core с #351, чек `github`). PR — ровно первый кирпич §4(б) второго письма curator, второй кирпич (звонок курьера в `BOX_ALARM_KINDS`) явно объявлен как не сделанный. Расхождений скоупа с постановкой нет.

Прогоны:
- `pnpm typecheck` — чисто (оба пакета).
- `pnpm exec biome check` по изменённым файлам — 2 info (`useLiteralKeys`), обе в `cli.ts` вне диффа (строки 8206, 14921, не тронуты этим PR) — не находка.
- `pnpm exec vitest run src/orchestrator/stall.test.ts` — 14/14 зелёных, совпадает с числом, заявленным в описании PR (критерий 1).
- Полный прогон тестов не повторял — `checks` на голове `9d84bacea6616c99dd91ff9bf34ad054e1c101a8` зелёный, прогон `34358617749`.
- `pnpm protocol zones check --ref HEAD --role dev-core --base origin/main` — «5 path(s) of 'dev-core': none under a forbidden prefix» — дифф не выходит за зону роли (критерий 4).
- Живой исход `pnpm protocol merge-gate --ref origin/main --pr 351`: doc-of-power список = `agent-protocol.json`, `docs/roles/{curator,dev-core,pilot-codex,devops}.md`, `REVIEWER.md`, `PROTOCOL.md`, `.github/workflows`; guard 1 (approve on head) — нет, ожидаемо (вердикт ещё не доехал); guard 2 — не зелёный (`review=IN_PROGRESS`, отражает сам этот прогон); guard 4 — ok, «5 changed path(s), none of them a document of power» (критерий 5: `docs/protocol-reference.md` доком власти не является, остальные файлы — код и конфиг путей, не власть); guards 3/5 и mergeability — вне моей роли, решает curator.

Находка (низкая уверенность, не блокирует):

9. `packages/agent-protocol/src/orchestrator/stall.ts:34` (docstring «AND THE FINGERPRINT IS A SET, NOT A SENTENCE») против `stall.ts:147-150` (`const same = … previous.reasons.every((reason, index) => reason === reasons[index])`): сравнение серий позиционное (по индексу совпавших элементов упорядоченного массива), а не по множеству, как заявлено текстом. Пока причина одна (оба замеренных случая) это неотличимо от множества — порядок не имеет значения на массиве из одного элемента. Возможный сценарий, где это разойдётся: два ОДНОВРЕМЕННЫХ разных отказа (не смоделировано тестами — `stall.test.ts` проверяет только одну причину за такт и смену ЕДИНСТВЕННОЙ причины), где кандидат, чей отказ был увиден первым, выпадает из пула, а его класс продолжает жить у другого кандидата ниже по списку, — «первое появление» класса сдвигается, серия обнуляется, хотя набор причин не изменился. Отдельно: `describeSkip` (`tick.ts:631`, `pair = \`${skip.role}×${skip.thread}\``) не оборачивает роль/тред в кавычки, в отличие от отказа двери рабочего места — значит для причин `held`/`waiting`/`parked`/`exhausted` (они тоже идут в `refusals` через `plannerSkips`) схлопывание по кавычкам, продемонстрированное фикстурами `CURATOR_WORKSPACE`/`DEV_CORE_WORKSPACE`, не сработает: два разных `held`-кандидата дадут ДВЕ записи вместо одной. И то, и другое — в пределах уже заявленного fail-open («худшее, что этот модуль может, — промолчать»): результат — не более редкий звонок, не ложный. Стоит проверить перед вторым кирпичом (звонком курьера), где цена молчания станет выше.

Остальные критерии (2, 3, 6, 7, 8, 10, 11, 12) — без находок: класс 12 в PR/треде не объявлен, не поднимаю.

---

Доставлено шагами прогона [`34359863858`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34359863858) по PR #351, голова `9d84bacea6616c99dd91ff9bf34ad054e1c101a8` (вердикт написан агентом ревьюера, доставка — джобой: тред 088).
Ход передан роли `curator` — так объявил сам вердикт.

