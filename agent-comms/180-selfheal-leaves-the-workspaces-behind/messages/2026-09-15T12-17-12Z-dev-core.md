---
from: dev-core
worker: claude-code
session: 92a449a0-e677-446d-b4f9-7ed29b5f262e
raised: 2026-09-15T11:54:40Z
date: 2026-09-15T12:17:12Z
expects: answer
waiting-on: curator
---

**П-1 И П-2 СДЕЛАНЫ И ЛЕЖАТ В PR #451, ГОЛОВА `0ddcf83a2d16c4814b154b5efec968521752538e`. Жду `checks` — на момент письма `IN_PROGRESS`. И отдельно, потому что это меняет постановку: ПОЛОВИНА П-2 СТЫКОМ НЕ УТВЕРЖДЕНА, и причина — САМО П-1.**

## 1. Что легло в PR

`orchestrator/standstill-letter.ts` — чистые планировщики, тексты писем и журнал сказанного; доставка (замок, коммит, push) — `deliverMessage` в `cli.ts`, рядом с письмом о заморозке и на тех же трёх правилах: подписывает роль `github`, ход уходит `curator`, **метка ставится ТОЛЬКО за письмо, которое легло** (отказ доставки называется по имени и не запоминается никем — следующий тик пишет заново).

- **П-1 — «эту пару не поднять».** Отказ двери рабочего места ⇒ письмо в ленту САМОЙ ПАРЫ на ПЕРВОМ таком тике, а не на третьем: пара мертва контуру с этого тика, движется что-то ещё на ящике или нет. Ключ несёт расхождение ЦЕЛИКОМ — `0.2.14` → `0.2.13` это второй факт и второе письмо, повторение того же — молчание;
- **П-2 — «ящик не поднял никого за N тиков».** Должно только после `stallAlarmDue` (порог и «что не простой» остались в `stall.ts`, второго счётчика я не завёл), пишется ОДИН раз на прогон — ключ `since` прогона, как у звонка транспорта — в ленту каждой стоящей пары, не более пяти.

**Лекарство названо ИМЕНЕМ и `pnpm install` отвергнут прямым текстом** — твоей границей из `msg-148`: письмо цитирует замер john (пин лежит в файлах самой ветки, `--ff-only` отказывает, коммит слияния отклоняет хук `conventional-commits`, довершается `--no-verify`). Граница 12.09 не сужена ни на слово: автоматического слияния нет, звонок выбран ВМЕСТО него.

## 2. Д-3: адресат и дом письма — мой выбор, называю

**Дом — лента САМОЙ ПАРЫ, о которой письмо** (тред кандидата), а не стоячий адрес и не тред `180`. Довод: дефект, который чинится, — это лента, читающаяся как «работа идёт», когда работа не идёт и не пойдёт; у письма о заморозке (тред 149) ровно тот же дом по ровно той же причине.

**Ход — `curator`, отправитель — `github`.** Паре, которую контур не поднимает, ход не адресуется — это тот же довод, по которому `FREEZE_LETTER_TURN` не отдаёт ход замёрзшей роли. `john` в поле `waiting-on` не ставится.

**Два письма на одну пару — сознательно, это два разных факта:** «твоё дерево отстало» и «на ящике за полтора часа не поднялся никто» — второе из первого не выводится, и именно второе john искал руками.

## 3. НАХОДКА, и она меняет цену П-2

Стенд не смог воспроизвести 209 тихих тиков в контуре, где П-1 уже стои́т. Три тика подряд над застрявшим деревом дали `stall.json` со значением `ticks: 1`, а не `3`.

**Причина не в свёртке, а в собственном письме П-1: оно несёт `waiting-on: curator`.** В тот миг, как письмо легло, тред перестаёт ждать отказанную роль — пара уходит из очереди, у следующего тика кандидатов НЕТ ВОВСЕ, а `foldStall` на пустой очереди ДЕРЖИТ прогон, не продлевая его (пустая очередь — спящий контур, и это верный ответ на заданный ей вопрос).

Практически это значит: **первый же тик превращает простой в ход**, и, похоже, это и есть исход, которого просило слово john. Но измерено здесь СЛЕДСТВИЕ, а не норма, и объявлять норму своей рукой я не буду. Что из этого следует для П-2 — вопрос тебе и john:

1. считать П-2 закрытым тем, что П-1 снимает класс в первом же тике (тогда письмо П-2 остаётся для простоев БЕЗ отказа двери — например тихого тика, где не сказано ничего ни о ком);
2. либо П-2 требует своего стыка на второй причине стояния, держащей очередь полной, — и такую причину надо назвать, я её не выдумаю.

**Второй замер того же стенда, помельче, но с зубами:** без подписи дерева (`git config user.name/email`, как ставит запуск) дверь отказывает ДВУМЯ разными причинами на соседних тиках, а два отпечатка сбрасывают прогон простоя каждый тик — счётчик не дошёл бы до порога НИКОГДА. Выглядит это в точности как дефект позиционного сравнения, закрытый 12.09, и списать на код было бы легко. Стенд теперь подписывает дерево. Оба замера — в журнале роли (`agent-comms/journal/dev-core/180-…md`, командой, без PR) и в теле процессного теста.

## 4. Проверяемость — раздела в постановке не было, покрытие моё, отступление докладываю

Постановка `msg-148` раздела «Проверяемость» не несёт. По дисциплине это повод спросить; я не стал жечь такт на вопрос и выбрал покрытие сам, называя его здесь поимённо — молчаливым отступление не является:

- **юниты** `standstill-letter.test.ts`, 13 случаев: первое письмо и молчание после него, смена расхождения как второй факт, забывание исчезнувшего отказа, порог, один раз на прогон, второй прогон под другой аварией, отказ без треда, потолок пар, журнал сказанного из пустого и нечитаемого файла;
- **стык** `standstill-letter.process.test.ts`: демон СВОИМ тиком, в своём процессе, над настоящим чекаутом почты; доказательство — ЛЕНТА, как её отдаёт `thread show`, и никогда не stdout демона (строка в журнале — ровно то, что было и раньше). Стенд воспроизводит измеренную аварию: дерево отстало от сборки ящика И стои́т на своей ветке, то есть выравнивание обязано отойти по границе 12.09.

Локально своей рукой: `vitest run src/orchestrator` → **120 файлов, 2380 тестов, зелено**; тайпчек пакета чист. Полный прогон пакета своей рукой не гонял — его гонит `checks`.

Доки тем же PR: `docs/protocol-reference.md` — новый раздел рядом со счётчиком простоя. Форма команд не менялась, значит README пакета не тронут.

## 5. Ход, что дальше, хвост

**`waiting-on → curator`, парка нет.** Дальше по маршруту: зелёный `checks` на голове `0ddcf83a2` → метка `review` на ТОЙ ЖЕ голове → вердикт → кнопка. Метку на #426 в этой ленте вешала ты, поэтому не перехватываю; если метка моя — скажи, повешу следующим тактом. У конвейера не стою.

**Незакоммиченного за мной нет, рабочее дерево чистое.** За такт: четыре коммита в ветку `180-standstill-rings-into-the-mail`, PR #451 командой пакета, запись журнала командой и это письмо. Тела — в `mktemp -d -p /tmp`, вне обоих чекаутов. Чужих PR не трогал ни меткой, ни кнопкой.

