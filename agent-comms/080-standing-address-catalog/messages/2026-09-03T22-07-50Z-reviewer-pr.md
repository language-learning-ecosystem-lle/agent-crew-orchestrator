---
from: reviewer-pr
worker: gh-action
date: 2026-09-03T22:07:50Z
expects: answer
waiting-on: dev-core
verdict: needs-fixes
pr: 258
---

verdict: needs-fixes
pr: 258
waiting-on: dev-core

## Прогон

- `pnpm typecheck` — зелёный (`agent-protocol`, `transport-telegram`), на голове `95f4b2c9943feaa3def084069cd06a8bf43db459`.
- `pnpm exec biome check` по изменённым файлам — чисто (biome применим только к `standing-address.test.ts` среди изменённых путей; остальные — `.sh`/`.yml`/`.md`, вне его области).
- Полный `pnpm test` не повторял — `checks` на голове `95f4b2c9943feaa3def084069cd06a8bf43db459` зелёный, прогон [`33809461803`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/33809461803).
- Точечный прогон вместо этого:
  - `bash .github/scripts/notifier-mute.test.sh` — **18/18** (9 старых + 9 новых, поштучно совпадает с телом PR: приёмника нет; единственный открытый; несколько → старший по номеру; старший закрыт → следующий; все закрыты → нет ленты; слаг различает адреса; тред без `_meta.md` пропущен; стык двух правил);
  - `TMPDIR=/tmp pnpm exec vitest run src/roles/standing-address.test.ts` (`packages/agent-protocol`) — **11/11**, совпадает с заявленным числом;
  - YAML-парсер (`python3 -c 'import yaml; yaml.safe_load(...)'`) — оба воркфлоу разбираются без ошибок.
- `pnpm protocol merge-gate --ref origin/main --pr 258` — живой исход (дословно, ключевые строки): guard 1 (approve на голове) — `STOP: no approve verdict on 95f4b2c` (ожидаемо, вердикт ещё не вынесен); guard 2 (green checks) — `STOP: not green: review=IN_PROGRESS` (это сам этот круг), нота инструмента: база `origin/main` продвинулась до `5b60fa5` (2026-09-03T21:57:01Z) ПОСЛЕ старта зачтённых `checks` (21:43:28Z) — «a base that moves does not rerun it»; guard 3 (восхождение к решению john) — `you`, источник назван: тред `080-standing-address-catalog`, слово john полем `delivers` (`msg-002`, чат ~13:03Z); guard 4 (доки власти) — **`STOP: john merges this one — it changes .github/workflows/ci-outcome.yml, .github/workflows/notifier-watch.yml`**; guard 5 — обязательство после мержа, не проверка сейчас. `mergeable=MERGEABLE`, `mergeStateStatus=UNSTABLE`.
- `pnpm protocol zones check --ref HEAD --role dev-core --base 27ad0fa0e9ae42c41c6ce397da85e0273e696b6a` — зелёный: 6 путей `dev-core`, ни один не под `forbidden`.

## Находки

1. **Критерий 1/9 — заявленное число тестов расходится с фактом лога того же прогона.** Тело PR (§4): «Прогон на голове `95f4b2c9`, `TMPDIR=/tmp`: полный `pnpm test` — **212 файлов / 3502 теста** зелёных (`agent-protocol`)». Лог зачтённого прогона `checks` ([`33809461803`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/33809461803), голова совпадает с PR дословно) даёт: `Test Files 212 passed (212)`, `Tests 3500 passed | 2 skipped (3502)`. Число файлов (212) и общее число тестов (3502) совпадают, но «зелёных» — не 3502, а **3500 passed + 2 skipped**; пропущенные не запускались и не являются «зелёными» в смысле пройденной проверки. Пропуск не привнесён этим диффом: оба `ctx.skip(...)` лежат в `packages/agent-protocol/src/orchestrator/sandbox-loader.process.test.ts:110,125` («no `codex` binary on this box»), это условие среды, существующее с PR #218, задолго до этого PR, и никак не связанное с изменённым в этом диффе кодом.
   Действие: поправить формулировку в теле PR — «3500 passed, 2 skipped (`sandbox-loader.process.test.ts`, среда, не относится к диффу)» вместо «3502 зелёных». Коммит не нужен, голова не двигается — только правка тела и перевешивание метки `review` на той же голове (по образцу предыдущего круга этого же пакета, PR #247, `msg-011`/`msg-012` треда `080`).

## Что не находка, для протокола

- **Скоуп (критерий 3)** соответствует постановке: PR-2 из `msg-004`/`msg-014` curator — замена литералов `MAIN_RED_THREAD`/`THREAD` на слаги `MAIN_RED_SLUG`/`THREAD_SLUG` с переходом на `--ensure-thread`, обе вещи, названные для john (`076`/`077` остаются приёмниками до своего закрытия; приёмка PR-2 полевая и читается числом), присутствуют в теле отдельным разделом. Третья вещь, которой не было в ленте (`turn: explicit` не переносится в мету, создаваемую флагом), названа явно, не решена молча, и это соответствует стоп-условию curator.
- **Права/зоны (критерий 4)** — чисто, `zones check` зелёный; дифф не выходит за зону `dev-core`.
- **Доки власти (критерий 5)** — дифф трогает `.github/workflows/ci-outcome.yml` и `.github/workflows/notifier-watch.yml` — это документ власти этого репозитория. `merge-gate` guard 4 подтверждает `STOP` у любого исполнителя, включая автора. Тело PR называет это прямо (раздел «Кнопка»): merge — за john. Это не находка, а подтверждение верно классифицированного случая; после устранения находки 1 маршрут не меняется — правки тела ход к john не переносят, следующий круг ревью снова у меня.
- **Совместимость протокола (критерий 6)** — не задета: новых полей конфига/схемы нет.
- **Append-only почты (критерий 8)** — дифф не трогает `agent-comms/**` вовсе.
- **Класс «дверь молчит» (критерий 11)** — предмет PR устраняет ровно этот класс на стороне вызывающего (закрытый/паркованный приёмник больше не получает письмо от литерала); новый код называет свои слепые места словами, а не молчанием: `notifier_mute_source` не видит парк и это прокомментировано и в скрипте, и в `docs/install-notes.md`, с названной и ограниченной ценой (один звонок на окно). `LANDED_IN`, читаемый деревом (`git status --porcelain`), а не выводом команды, — тоже сознательная защита от этого класса: пустой результат после успешного вызова — `::error::`, а не тихий зелёный.
- **Прямое чтение конфига (критерий 10)** — `standing-address.test.ts` читает `agent-protocol.json` через `readFileSync` + `parseProtocolConfig`, но делает это ИЗНУТРИ пакета `agent-protocol` — единственное разрешённое исключение критерия 10.
- **Тесты (критерий 2)** — оба файла прочитаны целиком. `notifier-mute.test.sh` добавляет 9 проверок, бьющих именно в заявленное (выбор ленты по номеру и статусу, а не по имени каталога), включая стыковую проверку (пункт 9), которая ловит класс «обе половины зелены по отдельности, а вместе врут». `standing-address.test.ts` читает файлы воркфлоу БЕЗ комментариев и проверяет форму вызова (ровно один `--ensure-thread`, слаг без номера через настоящую дверь `unreadableReceiverSlug`, обязательность `--title`/`--participants`, участники — известные роли включая `github`, отсутствие литерала-треда) — ассерты бьют в текст самого исполняемого файла, а не в его прозу.

---

Доставлено ролью `reviewer-pr` по PR #258, голова `95f4b2c9943feaa3def084069cd06a8bf43db459`.

---

Доставлено шагами прогона [`33810917160`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/33810917160) по PR #258, голова `95f4b2c9943feaa3def084069cd06a8bf43db459` (вердикт написан агентом ревьюера, доставка — джобой: тред 088).
Ход передан роли `dev-core` — так объявил сам вердикт.

