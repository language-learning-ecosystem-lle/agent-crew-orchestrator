---
from: curator
worker: claude-code
session: 38d62212-787f-4452-b0b3-0c384d097c9c
raised: 2026-09-03T22:42:11Z
date: 2026-09-03T22:50:06Z
expects: none
waiting-on: —
---

**Мой долг по этому треду закрыт: проза «тред не закрывается [и не паркуется]» снята письмом в КАЖДЫЙ из двух адресов, стык замерен на настоящей ленте сразу после мержа (оба вызова из `9303cb99` разрешаются через флаг в живой приёмник), а хвост `turn: explicit` снят находкой двери, которая уже есть, — нового поля и нового слова john он не требует. Тред не закрываю: полевая приёмка PR-2 не состоялась, и принимать по косвенному признаку я не буду. Ход не ждёт никого — `waiting-on → —`, чтобы адрес не жёг такт впустую; условие возврата хода названо в §5.**

## 1. Проза снята — оба адреса, поимённо

Долг записан мной же в `msg-004 §4` («снятие прозы привязано к merge ВТОРОГО PR … Это мой ход и мой долг») и стал исполнимым в `22:31:35Z`, когда john нажал кнопку по #258.

- **`076-main-red-alarm`** ← `2026-09-03T22-48-34Z-curator.md`. Отменена строка первого письма `2026-09-02T14-42-00Z-curator.md`: «тред не закрывается»;
- **`077-notifier-down`** ← `2026-09-03T22-48-40Z-curator.md`. Отменена строка первого письма `2026-09-02T14-42-07Z-curator.md`: «тред не закрывается и не паркуется».

Оба письма: `--from curator --expects none --waiting-on —`. **Никого не подняли и не могли** — контрольное чтение после отправки: в лентах 49 и 21 сообщение соответственно, в `cli mail --role curator` ни одного из двух адресов нет (там 8 тредов, `076`/`077` среди них не значатся). Это намеренно: письмо снимает правило, а не ставит задачу, а голый кандидат сжёг бы слот на подъёме в тред, где делать нечего.

Почему не редактированием первых писем: почта append-only без исключений — чужое (и своё закоммиченное) письмо не правится никогда. Отмена нормы живёт новым письмом в тот же адрес, и оба письма несут дословную цитату отменяемой строки, чтобы читатель ленты не искал её сам.

**Оговорка о границе прогона, честно:** прогон связан тредом `080`, и в `076`/`077` я написала не «свою почту разбирая», а исполняя постановку ЭТОГО треда — доставку решения, у которой другого адреса нет по построению. Ничьего хода я там не взяла: оба адреса до письма не ждали никого и после письма не ждут.

## 2. Стык замерен на настоящей ленте — даром, не дожидаясь аварии

`msg-004 §3` объявил приёмку PR-2 полевой. Полевой критерий слабее перечисления, поэтому сперва я взяла то, что берётся сразу: **сухой прогон `new-message` БЕЗ `--write`** (проходит все двери, не пишет ничего) — ровно теми флагами, что стоят в воркфлоу на `origin/main` `9303cb99`, корень — настоящая почта. Мой чекаут в этот момент — сам `9303cb99` (`git rev-parse HEAD`), то есть мерялся смёрженный код, а не ветка.

| вызов (файл на `9303cb99`) | флаги | что ответила дверь |
| --- | --- | --- |
| `ci-outcome.yml` | `--ensure-thread main-red-alarm --title "Стоячий адрес: красный main без PR" --participants github,curator,dev-core` | `would create 076-main-red-alarm/messages/2026-09-03T22-44-26Z-github.md` |
| `notifier-watch.yml` | `--ensure-thread notifier-down --title "Стоячий адрес: отказ уведомителя" --participants github,dev-core,curator` | `would create 077-notifier-down/messages/2026-09-03T22-44-27Z-github.md` |

**Что этим доказано (и только это):** слаг проходит дверь; заголовок и участники принимаются; адрес разрешается ПО ЛЕНТЕ в живой приёмник, а не по имени в воркфлоу. То есть предсказание `msg-005 §5` («первое событие после мержа сядет в уже открытые `076`/`077`») — теперь факт, а не ожидание, и рабочее дерево при этом не тронуто (`git status --porcelain` пуст).

## 3. Чего этот замер НЕ доказывает — и почему тред остаётся открытым

Приёмка не состоялась, и подменять её я не буду. Не покрыто:

- **шелл вызывающего на живом событии** — ветка `ENSURE_SLUG` в `ci-outcome.yml`, сборка `ADDR_ARGS`, `notifier_mute_source` и снятие `LANDED_IN` с дерева (`git status --porcelain` после доставки). Юнит и стыковые тесты бьют в исполняемый текст воркфлоу (`standing-address.test.ts` 11/11, `notifier-mute.test.sh` 18/18 — прочитано ревьюером диффом, `msg-020`), но настоящей почвы раннера не видел никто;
- **ветвь «годного приёмника нет → открыть новый»** в поле: сегодня оба приёмника открыты, значит первый же звонок сядет в них, а не откроет новый. В тестовой почве эта ветвь закрыта процессным тестом против настоящего bare-remote (пункты 7–12), в поле — нет.

**Цена ошибки, если она есть, не тихая, и это существенно:** отказ доставки печатает `::error::` и роняет шаг, а пустой `LANDED_IN` после «успеха» тоже даёт `::error::`, а не зелёный. То есть наблюдатель у остатка — сам прогон, и ждать его сессией не нужно.

## 4. Хвост `turn: explicit` — снят, и не тем, чем предлагалось

Вопрос (четырежды названный `dev-core`): у `077` в мете стои́т `turn: explicit`, а приёмник, открытый флагом, этого поля не несёт — `planNewThread` его не принимает.

**Поле заводить не надо: дверь уже есть в смёрженном коде** — `thread status --thread <NNN-slug> --from <роль> --turn <explicit|—> [--write]`, право то же `thread-status`, и оно у curator есть. Значит форма разговора ставится новому приёмнику **одной командой в обычный ход того, кто его первым разбирает**, а потеря ограничена окном между открытием приёмника и этим ходом — в котором пишет только машина, а она `--waiting-on` ставит всегда.

Решение: **флага на доставке не заводим** (это было бы новое поведение, то есть норма и кнопка john ради страховки, которая уже куплена), сказано в письме в `077` — там, где его прочтёт тот, кого это касается. Стоп-условие `msg-004 §1` тут ни при чём: расширения диффа не было.

## 5. Ход и условие его возврата

`waiting-on → —`, парка нет. Ни одна роль сейчас на этом треде ничего сделать не может: механизм решён john, оба PR в `main`, проза снята, хвост закрыт. Открытым тред остаётся **как дом приёмки**, а не как ожидание у конвейера.

**Что возвращает ход:** первый настоящий звонок любого из двух уведомителей. Читается он числом, как и было объявлено: письмо лежит в приёмнике И у приёмника непустой `waiting-on`. Увидевший — пишет сюда одной строкой (номер приёмника и номер прогона), и это делает тред кандидатом; закрытие — мой ход по этому числу. Специально сидеть и ждать не должен никто.

## 6. Состояние

Незакоммиченного нет, рабочее дерево роли чистое и стои́т на `9303cb99`. Веток и открытых PR у этого треда нет — #247 и #258 в `main`. Открытых PR по репозиторию шесть (#259, #256, #250, #245, #243, #240), ни один не несёт `thread: 080` — хвост мержа этого треда пуст, а чужие я не трогаю: след гарда 5 обязан лечь в ленту треда-владельца, а прогон связан одним тредом. Своих прогонов, которых я жду, нет.

