---
from: curator
worker: claude-code
session: 3b46cfee-c491-4e20-846e-05f059e44910
raised: 2026-09-09T20:15:13Z
date: 2026-09-09T20:20:42Z
expects: ack
waiting-on: curator
parked-on: john
---

**ТРЕБУЕТСЯ РЕШЕНИЕ JOHN: условие, которое ты поставил вместо ack'ов, СРАБОТАЛО впервые — письмо `msg-021` приехало на перезапуске БЕЗ смены исполняемого (оба пути диффа — тесты). По твоему слову это «новый предмет с новым замером»; замер ниже, а предмета я своей рукой не завожу — скажи, куда он едет.**

Три новых листа адреса померены (`msg-021`, `msg-022`, `msg-023`), полны во всех шести полях, цепь без разрыва. Условие сработало ровно на одном из трёх.

## 0. Что мерено и чем

Три перезапуска: `b3f86e9f230c`→`886fcda1f3bb` (19:49:54Z), `886fcda1f3bb`→`22d62d9c1940` (20:01:10Z), `22d62d9c1940`→`c1b95f155880` (20:14:36Z). Источники: `.orchestrator/self-restart.json`, `daemon-code.json`, `self-restart-letters.json`, `.orchestrator/daemon.log` (заголовки эпох, строки дрейфа), `git log`/`git diff --name-only` в контуре. В дерево роли не писала ни одного файла; тело письма — в своём `mktemp -d -p /tmp` вне обоих чекаутов.

## 1. ГЛАВНОЕ: `msg-021` (#361) сдвинул отпечаток и НЕ сдвинул исполняемое

`git diff --name-only b3f86e9f230c..886fcda1f3bb` — **ровно два пути, и оба тестовые**:

- `.github/scripts/review-delivery.test.sh` — вне пакета, в отпечаток не входит (отброшен верно);
- `packages/agent-protocol/src/roles/review-delivery.process.test.ts` — под `packages/**`, поэтому письмо и говорит «сдвинулся ли отпечаток установки: да, 1 путь(ей)».

Коммит — #361, «сверка доставки вердикта различает „правило нарушено“ и „проба не состоялась“» (тред 185): починка целиком на стороне проверки.

**Двух критериев здесь ДВА, и в этом всё дело.** Критерий ПИСЬМА (отпечаток = пути под `packages/**`) говорит «сдвинулось». Критерий ТВОЕГО СЛОВА (смена ИСПОЛНЯЕМОГО — не тест, не README) говорит «не сдвинулось»: исполняемых файлов в диффе **ноль**. С `#352` письмо смены исполняемого не утверждает вовсе — оно прямо пишет, что установленная копия не мерена, — значит это условие мерит читатель, и я его меряю с `msg-016` каждым ходом. Двенадцать эпизодов подряд оно НЕ срабатывало (каждый раз находился `cli.ts`, `notify.ts`, `park-ground.ts` и т. п.). Здесь сработало.

Своим суждением это не подменяю: перезапуск демона состоялся, ремонт отработал штатно, дрейф закрыт. Померено ровно одно — **ящик перезапустил себя ради коммита, который ничего исполняемого в нём не менял**, и по твоему слову это отдельный предмет, а не строка отчёта.

## 2. Остальные два письма — исполняемое сдвинулось, вопроса нет

- **`msg-022`** (`886fcda1f3bb`→`22d62d9c1940`, #364, тред 184): дифф — `docs/protocol-reference.md` (отброшен верно), `cli.ts`, `daemon.process.test.ts`. Исполняемый — `cli.ts`. Письмо назвало 2 пути, обрезки нет.
- **`msg-023`** (`22d62d9c1940`→`c1b95f155880`, #365, тред 155): дифф — 7 путей, `docs/protocol-reference.md` отброшен, отпечаток 6. Исполняемые — `cli.ts`, `thread/message.ts`, `thread/park-ground.ts`. **Обрезка объявленная** (потолок `NAMED = 5`, письмо кончается «…»); необъявленный шестой — `packages/agent-protocol/src/thread/park-ground.ts`, то есть тело самой правки. Дефектом по-прежнему не считаю: из названного sha список выводится одной командой.

## 3. Шесть полей каждого письма сходятся с записью

| поле | `msg-021` | `msg-022` | `msg-023` |
|---|---|---|---|
| было → стало | `b3f86e9f`→`886fcda1f` | `886fcda1f`→`22d62d9c1` | `22d62d9c1`→`c1b95f155` |
| отставал | 1 коммит (#361) | 1 коммит (#364) | 1 коммит (#365) |
| ждал сессии | 8 мин (481 с) | 3 мин (190 с) | 6 мин (367 с) |
| пошёл | 19:49:54Z | 20:01:10Z | 20:14:36Z |
| преемник поднялся | эпоха `19:50:06.645Z`, pid 3334073 (**12,6 с**) | эпоха `20:01:22.978Z`, pid 3445403 (**12,9 с**) | эпоха `20:14:48.305Z`, pid 3552822 (**12,3 с**) |

Ряд подъёма преемника прежних эпизодов — 12–13 с, все три попадают в него. Живая запись сверена по последнему: `self-restart.json` — `from 22d62d9c…`, `target c1b95f15…`, `drainSince 20:08:29Z`, `at 20:14:36Z` → **ровно 367 с**, `went: true`, `attempts: 1`; `daemon-code.json` — `sha c1b95f15…`, `startedAt 20:14:55Z`, `pid 3552822`; замок повтора — `self-restart-letters.json`, `signature "c1b95f1558…\0 2026-09-09T20:14:36Z"`, `at 20:15:06Z`, то есть подпись именно этого эпизода. Дрейфа сейчас нет: `origin/main` = `c1b95f1558809d433b5b31df2cd0e63951ee77d7` = исполняемый код.

## 4. Цепь без разрыва: три эпохи — три письма

Заголовки эпох `daemon.log` после моего прошлого хода: `18:45:09.775Z` (pid 3123710) → `19:50:06.645Z` → `20:01:22.978Z` → `20:14:48.305Z`. Три перезапуска, три письма. Пропущенных нет, лишних нет, удержаний в этом окне нет.

Жизнь дрейфа: `msg-023` — ref уехал 20:08:08Z (дата коммита `c1b95f15`), дренаж записан 20:08:29Z = **21 с** (ряд: 9–40 с), перезапуск пошёл через 367 с = **5,1 % порога** `CODE_DRIFT_OVERDUE_MINUTES = 120`. Четырнадцатый эпизод подряд под порогом: механизм `#301` этим адресом по-прежнему не проверяется по построению.

## 5. Почему я НЕ ставлю парк, хотя вопрос к john — и это часть вопроса

Карточка велит вопрос к john нести парком (`--parked-on john`, звонок сразу). **Здесь парк убил бы сам адрес, и это не опасение, а строка кода:** `cli.ts:3957` — `--ensure-thread` «takes whichever of its receivers is **open and unparked** (opening one if none is)». Запаркуй я `176` — следующее письмо самоперезапуска форкнет новый приёмник, ровно то разрастание (`161`→`173`→`175`→`176`), которое ты закрыл. Хуже: парк на john в этом контуре в поле ЗАСТРЕВАЕТ — `173` и `175` стоят запаркованными и неподнимаемыми до сих пор, слово доставлено, парк не снялся (замерено в `msg-016`, §4 этого треда).

Я попробовала обойтись без парка — `--expects ack` с `--waiting-on curator` и без `--parked-on`, — и **дверь почты отказала, назвав цену**: «the turn is yours, so there is nobody to acknowledge it, and the only thing that can wake you is the circuit — which will, on every tick, until the ceiling of the pair is spent (thread 022)». То есть беспарковый вопрос к john выжигает потолок пары `curator×176` тактами вхолостую. Отказ признаю: **парк ставлю на тебя**, и делаю это зная, что следующее письмо самоперезапуска форкнет новый приёмник — машина откроет его сама (`--ensure-thread`, «opening one if none is»), адрес не потеряется, но `187` родится.

Своей рукой новый тред под «новый предмет» я не завела — прогон связан ровно `176`, а номер в этом контуре живая коллизионная опасность (треды `181`, `184` ровно об этом).

**Развилка, которую решаешь ты, а не я:**

1. предмет «перезапуск ради неисполняемого коммита» заводится отдельным тредом — тогда назови, кто его открывает, и он получит парк;
2. либо это не предмет вовсе, а строка отчёта — тогда скажи, и я снимаю проверку из ходов, а `176` живёт как жил;
3. либо предмет есть, но живёт здесь — тогда нужна форма звонка, не убивающая приёмник (сегодня её нет: парк = форк).

## 6. Чего этим ходом НЕ сделано

- `173` и `175` по твоему слову на закрытие не гашу — прогон связан ровно тредом `176`, их застрявший парк этим ходом не перемерян;
- хвост готовых PR не мету: след гарда 5 обязан лечь в ленту треда-ВЛАДЕЛЬЦА PR, писать в чужие ленты этим прогоном нельзя; своего PR у `176` нет;
- установленную копию (`node_modules`) руками не мерила: `#352` этого намеренно не мерит, я мерила ИСХОДНИК, который демон исполняет;
- гранулярность триггера (доковый путь снова в диффе двух из трёх эпизодов) никуда не маршрутизирую — честная дорога свой тред;
- потолок памяти роли (`27684` байт против `24576`) прошлым ходом назван и не починен; этим ходом тоже не чинён — окно ушло на замер трёх писем и на развилку §5.

`176` **впервые за адрес паркую** — на тебе, по причине §5: беспарковой формы у вопроса к john нет, дверь её отказывает. Следующее письмо самоперезапуска ляжет НЕ сюда, а в новый приёмник, который откроет машина. Незакоммиченного за мной нет: в дерево роли не писала ни одного файла, тело письма — в своём `mktemp -d -p /tmp` вне обоих чекаутов. Ход — у curator, вопрос стоит первой строкой.

