---
from: curator
worker: claude-code
session: e222c7d2-4abc-4e44-8c99-317aa4d95a68
raised: 2026-09-08T18:55:20Z
date: 2026-09-08T19:00:42Z
expects: none
waiting-on: —
---

**Третье письмо стоячего адреса перемерено: отчёт полон, john НЕ доношу — исполняемое перезапуск сменил.** И главное новое за этот такт: **ветка УДЕРЖАНИЯ письма впервые сработала в поле** — четвёртый перезапуск (уже случившийся, 18:54:41Z) письма НЕ породил, потому что не тронул ничего из отпечатка. Это ровно тот случай, о котором john говорил «письмо без смены исполняемого — новый предмет»: письма не будет, предмет не рождается.

## 0. Метод пришлось сменить, и это факт о читаемости, а не оговорка

Оба прошлых письма я сверяла с `.orchestrator/self-restart.json`. **Здесь записи уже нет: файл однослотовый, и к моменту моего подъёма его перезаписал СЛЕДУЮЩИЙ перезапуск** (в нём сейчас `from` `3cc1f1e1…` → `target` `ee960b86…`, `at` `2026-09-08T18:54:41Z`). Письмо `msg-006` легло 16:52:17Z, роль поднялась 18:55:20Z — за два часа его собственная запись протухла.

Поэтому шесть полей `msg-006` мерены **журналом ящика и репозиторием**, а не записью. Ниже везде названы строки `.orchestrator/daemon.log`, чтобы это можно было перепроверить, а не принять на веру.

## 1. Шесть полей сходятся с миром

- **сам ремонт** — `daemon.log:30983-30991`, эпизод целиком: `the LOADED CODE is not the ref: this process runs 1bac6218 …, origin/main on disk is 3cc1f1e1 — 1 commit(s) behind` → `leases 0, state clean` → `git pull --ff-only — ok: 4 files changed, 408 insertions(+), 153 deletions(-)` → `the tree is on 3cc1f1e1 — leaving with code 75`. «Какой был / какой стал / сколько отставал» — все три поля стоят в журнале дословно.
- **«какой стал» сходится с линией:** `3cc1f1e1763fa8610a935f296d17d2a9a080087e` = `fix(notify): различитель «спросили снова» берётся из писем … (#344)`.
- **преемник ПОДНЯЛСЯ и исполнял именно этот sha.** Процесса того эпизода уже нет (его сменил четвёртый перезапуск), поэтому `/proc` тут не свидетель — свидетелей два других, и оба независимые: заголовок эпохи `daemon.log:30993` — `daemon epoch · 2026-09-08T16:52:01.523Z · pid 97440`, то есть через **12 с** после `at`; и **следующий демон называет своё собственное дерево** — `daemon.log:37951`: `… differs across 3cc1f1e1..ee960b86 (the code this process is running → …)`. То же говорит поле `from` текущей записи.
- **«3 пути в исполняемом» — счёт верен.** `git diff --name-only 1bac6218..3cc1f1e1` даёт **четыре** пути; три лежат под `packages/agent-protocol/**` (`notify/notify.ts`, `notify.test.ts`, `notify.process.test.ts`), четвёртый — `docs/protocol-reference.md`, отброшен как неисполняемый. Отпечаток тут не мой вывод: ящик печатает его своими словами (§3) — `this daemon runs 'packages/agent-protocol' of '/home/lle/…'`. Обрезки имён не было (потолок `NAMED = 5` не достигнут).
- **установка правильно пропущена** — `daemon.log:30988`: `pnpm install skipped — none of package.json, pnpm-lock.yaml, pnpm-workspace.yaml differs across 1bac6218..3cc1f1e1`. Манифестов в диффе действительно нет.

## 2. «4 мин (289 с)» — проверено чужими часами, потому что своих у поля больше нет

Запись с `drainSince` перезаписана, так что вычитание письма проверяемо только независимым штампом. Журнал его даёт: строки дренажа этого эпизода несут `drifting for Nm (since 2026-09-08T19:46:56+03:00)` = **16:46:56Z**, ремонт пошёл в 16:51:49Z → **293 с**. Письмо говорит 289 с, то есть его `drainSince` был 16:47:00Z — на 4 с позже штампа дрейфа.

**Расхождением это не является, и вот чем это показано, а не объяснено:** у ЧЕТВЁРТОГО перезапуска живы ОБА числа — журнал даёт `since 2026-09-08T21:52:17+03:00` = 18:52:17Z, запись даёт `drainSince` 18:52:22Z. Тот же сдвиг, **+5 с**, в ту же сторону. Это два разных события одного такта (замечен дрейф → записано состояние дренажа), и на обоих эпизодах смещение одинаковое. Арифметика письма честная.

Дренаж эпизода — восемь тактов, и он ждал живых сессий поимённо (`curator/155-park-has-no-checkable-ground`, `dev-core/178-zones-door-silent-pass`), ни одна не порвана: `daemon.log:30598, 30667, 30739, 30790, 30813, 30836, 30886, 30947`.

## 3. НОВОЕ: удержание письма — первый полевой случай ветки «отпечаток не тронут»

Четвёртый перезапуск (`3cc1f1e1` → `ee960b86`, `at` 18:54:41Z, `went: true`, дренаж 139 с) сменил ровно два пути: `docs/turn-latency-measurement.md` и `scripts/turn-latency.mjs`. **Ни один не лежит под отпечатком**, манифесты не двигались (`daemon.log:37951`) — и ящик письма НЕ послал, сказав это в журнале дословно (`daemon.log:37995`):

> `letter — WITHHELD, the restart changed NOTHING THIS DAEMON EXECUTES: 3cc1f1e1763f..ee960b869098 moves no path of the footprint (this daemon runs 'packages/agent-protocol' of '…', plus whatever package.json, pnpm-lock.yaml, pnpm-workspace.yaml install), so the box is running the same program under a new sha. The restart itself STANDS and is not undone; what is not spent is the LETTER, and with it the turn of 'curator'`

Подтверждено вторым концом — **замок повтора не сдвинулся**: `.orchestrator/self-restart-letters.json` до сих пор несёт подпись ПРОШЛОГО письма — `3cc1f1e1763f… 2026-09-08T16:51:49Z`, `at 16:52:17Z`. То есть последнее отправленное письмо адреса — это `msg-006`, и письма за четвёртый перезапуск не будет.

Что этим сказано и чего нет:

- **сказано:** условие john «если письмо придёт на перезапуске БЕЗ смены исполняемого — новый предмет» проверено с другой стороны и в поле: такого письма не приходит, ящик молчит сам. Стоячий адрес на доковом перезапуске не растёт и такта роли не стоит;
- **НЕ сказано, что молчание тут ошибочно.** `scripts/turn-latency.mjs` — исполняемый файл, но ЭТОТ демон его не исполняет, а отпечаток мерит «что запускает этот процесс», а не «что вообще можно запустить». Называю явно, чтобы позже никто не прочёл «`scripts/` уехал, а ящик промолчал» как пропуск.

## 4. Наблюдение о журнале — фактом, без диагноза

Строка `letter — WITHHELD` печатается **на каждом такте**: `daemon.log:37995, 38096, 38188, 38268` — четыре раза за первые ~2,5 минуты. Денег это не стоит (письма нет, роль не поднимается), а замок повтора (`self-restart-letters.json`) относится к ОТПРАВЛЕННЫМ письмам. Причину не диагностирую: собственный диагноз — тоже заявление, и предмет журнальной повторяемости не этого треда. Записано здесь фактом, чтобы было на что сослаться.

## 5. Почему john не звоню

Слово john ~14:32Z (ленты `173` и `175`, `msg-002` в каждой): «приёмка самоперезапуска … второй раз не спрашивается»; «если письмо придёт на перезапуске БЕЗ смены исполняемого, это новый предмет». Исполняемое сменилось (§1) → нового предмета нет, ack уже дан. Парк на 176 к тому же форкнул бы адрес — то самое разрастание, которое john закрыл.

## 6. Почему этот ход приехал через два часа после письма

Не жалоба, а замер: `daemon.log:37953` — `this tick launches NOTHING (dev-core×177-workspace-per-pair, curator×176-daemon-self-restart stay in the queue for the successor)`. Пара `curator×176` была придержана дренажом ЧЕТВЁРТОГО перезапуска и отдана преемнику; подъём случился 18:55:20Z. Прямое следствие для чтения таких писем — §0: за это время запись письма протухает.

## 7. Чего этим ходом НЕ сделано

- `173` и `175` по слову john на закрытие не гашу — прогон связан ровно тредом `176`; их застрявший парк этим ходом не перемерян;
- механизм #301 («звонок о дрейфе больше не придёт») по-прежнему не доказан: его мерит первый дрейф, который ремонт НЕ закроет, а здесь дрейф снова не дожил (139 с);
- гранулярность триггера (доковый коммит поднимает дренаж, хоть письма и не будет) никуда не маршрутизирую — честная дорога свой тред.

`176` оставляю **открытым и незапаркованным**: он единственный такой приёмник адреса, следующее письмо ляжет сюда, а `177` от самоперезапуска не родится (номер `177` уже занят другим адресом — `workspace-per-pair`, он виден в очереди ящика). Незакоммиченного за мной нет: в дерево роли не писала ни одного файла, тело письма — в собственном `mktemp -d -p /tmp` вне обоих чекаутов. Ход никому.

