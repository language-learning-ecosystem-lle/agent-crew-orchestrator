---
from: curator
worker: claude-code
session: d508d0d5-aec7-494a-84b6-35166728ccdf
raised: 2026-09-09T17:28:36Z
date: 2026-09-09T17:34:58Z
expects: none
waiting-on: —
---

**Восемь писем адреса перемерены за ОДИН подъём: все восемь полны, john НЕ доношу — исполняемое сменил каждый из восьми перезапусков.** Главное новое за такт: с `#352` письмо БОЛЬШЕ НЕ УТВЕРЖДАЕТ смену исполняемого — оно называет сдвиг отпечатка и само говорит, что установленная копия не мерена. Условие john («письмо на перезапуске БЕЗ смены исполняемого — новый предмет») теперь мерит ЧИТАТЕЛЬ, и ниже оно померено рукой по всем шести письмам новой формы.

## 0. Что мерено и чем

Восемь писем (`msg-008`…`msg-015`) — девять перезапусков этого ящика за 14:26–17:28Z 09-09, из них один молчащий. Источники замера: `.orchestrator/daemon.log` (живая эпоха и восемь мёртвых), `daemon.log.1` (хвост предыдущей), `git log`/`git diff --name-only` в контуре, `/proc` живого процесса, `.orchestrator/self-restart.json`, `self-restart-letters.json`, `daemon-code.json`. Записи прошлых эпизодов протухли (файл однослотовый — `msg-007`, §0), поэтому шесть полей каждого письма сверены журналом и репозиторием, а не записью; записью сверено только последнее письмо.

## 1. Цепь sha сходится ВСТЫК и подтверждена процессом-преемником

Каждый следующий эпизод называет своим исполняемым кодом РОВНО цель предыдущего письма — `daemon.log`, первая строка дрейфа каждой эпохи (`this process runs <X> …, origin/main on disk is <Y>`):

`2f17b9cd` → `390d8085` → `d997e881` → **`8af91b89`** → `dbdff8e3` → `8adca246` → `e401ca55` → `caf3fd0c` → `47ec0576` → `e3c949fd`

Жирным — единственное звено без письма (§5). Все десять sha лежат на линии `origin/main` (`git log origin/main~14..origin/main`), «сколько отставал» верно в каждом письме, включая `3 коммит(ов)` у `msg-012` (`8adca246`→`a6641fd1`→`d6909ebd`→`e401ca55`).

**Преемник поднимался ВСЕГДА и быстро.** Выход `code 75` → заголовок следующей эпохи: 13, 13, —, 13, 12, 13, 12, 12, 12 с. Живой ящик мерен процессом, а не деревом: `daemon.pid` 2227647, `/proc/2227647` под `TZ=UTC` — старт `2026-09-09T17:28:01.67Z`, argv — `packages/agent-protocol/src/cli.ts orchestrator up --foreground --ref origin/main --instance hetzner` ЭТОГО чекаута, `cwd` → сам контур; `daemon-code.json` называет `e3c949fd…` и `startedAt 17:28:10Z`. То есть последнее письмо (`msg-015`) описывает перезапуск, преемник которого меня и поднял.

## 2. «Сколько ждал сессии» — честное вычитание, проверено чужими часами

У живого эпизода число проверяется записью: `self-restart.json` — `drainSince 17:23:27Z`, `at 17:27:50Z` = **263 с**, письмо говорит «4 мин (263 с)». Точно.

У семи протухших `drainSince` перемерян независимым штампом — временем, когда УЕХАЛ ref (`drifting … since` журнала = дата коммита цели). Разность «ref уехал → записан дренаж» это задержка замечания, и она мала и однородна:

| письмо | `at` | письмо говорит | значит `drainSince` | ref уехал | задержка |
|---|---|---|---|---|---|
| msg-008 | 14:26:47Z | 126 с | 14:24:41Z | 14:24:15Z | 26 с |
| msg-009 | 14:39:42Z | 84 с | 14:38:18Z | 14:37:52Z | 26 с |
| msg-010 | 15:24:45Z | 375 с | 15:18:30Z | 15:18:00Z | 30 с |
| msg-011 | 15:46:24Z | 365 с | 15:40:19Z | 15:39:53Z | 26 с |
| msg-012 | 16:16:30Z | 680 с | 16:05:10Z | 16:05:00Z (последний из трёх) | 10 с |
| msg-013 | 16:34:34Z | 298 с | 16:29:36Z | 16:29:27Z | 9 с |
| msg-014 | 16:55:04Z | 440 с | 16:47:44Z | 16:47:11Z | 33 с |
| msg-015 | 17:27:50Z | 263 с | 17:23:27Z (запись) | 17:23:08Z | 19 с |

`msg-012` выглядел выбросом (`since` журнала — 16:03:09Z, то есть 121 с), и это НЕ расхождение: строка дрейфа держит САМЫЙ РАННИЙ уехавший коммит, а дренаж начался через 10 с после последнего. Считано от последнего — тот же однородный ряд. Арифметика всех восьми писем честная.

## 3. Счёт путей отпечатка верен во всех восьми

`git diff --name-only <было>..<стало>` по каждому эпизоду: письмо всегда называет ровно те пути, что лежат под `packages/agent-protocol/**`, и отбрасывает `docs/**`, `PROTOCOL.md`, `agent-protocol.json`, `docs/roles/**` как вне отпечатка. Два пограничных случая названы явно:

- **`msg-014` — ровно 5 путей и обрезки НЕТ** (потолок `NAMED = 5` достигнут, но не превышен);
- **`msg-015` — 8 путей, названо 5 и стоит «…»**: обрезка объявленная, необъявленные три — `orchestrator/snapshot.ts`, `orchestrator/tick.ts`, `schema/v27-pair-ceilings.ts`; из названного sha список выводится одной командой, дефектом по-прежнему не считаю.

## 4. ГЛАВНОЕ: письмо сменило ФРАЗУ, и условие john теперь мерит читатель

`#352` (`dbdff8e3`, тред 161, слит 15:18Z) переписал строку письма: было «**что сменилось в исполняемом:** N путь(ей)», стало «**сдвинулся ли отпечаток установки:** да, N путь(ей) … Это пути, правка которых МОГЛА сменить программу; что установленная копия ДЕЙСТВИТЕЛЬНО другая, здесь не измерено — критерий консервативен намеренно» (`self-restart-letter.ts`, `executableLine`). Сам критерий не тронут — тронута фраза, и это правильная починка: прежняя выдавала консервативный замер за факт.

Следствие для ЭТОГО треда прямое и его надо назвать, а не подразумевать: **слово john ~14:32Z 08-09 (ленты `173`/`175`) поставило условием законности письма «смену исполняемого», а письмо с `msg-010` этого больше не утверждает.** Значит проверку делаю я, и она стоит одной команды. Померено по каждому письму — исполняемый (не тест, не README) файл, который перезапуск ДЕЙСТВИТЕЛЬНО сдвинул:

- `msg-008` — `cli.ts`, `orchestrator/paths.ts`, `orchestrator/stall.ts`;
- `msg-009` — `orchestrator/workspace.ts`;
- `msg-010` — `orchestrator/self-restart-letter.ts`;
- `msg-011` — `cli.ts`, `orchestrator/tick.ts`;
- `msg-012` — `orchestrator/quota.ts`;
- `msg-013` — `cli.ts`, `orchestrator/account-reach.ts`;
- `msg-014` — `cli.ts` (остальные четыре пути отпечатка — README и три теста);
- `msg-015` — `cli.ts`, `config/config.ts`, `orchestrator/priority.ts`, `orchestrator/snapshot.ts`, `orchestrator/tick.ts`, `schema/v27-pair-ceilings.ts`.

**Пусто ни у одного.** Условие john не сработало → нового предмета нет, приёмка самоперезапуска второй раз не спрашивается, **john не звоню и парк не ставлю** (парк на 176 форкнул бы адрес — ровно то разрастание, которое john закрыл).

## 5. Пропущенных писем НЕТ, и молчания здесь ДВА РАЗНЫХ СОРТА

- **`d997e881` → `8af91b89` — законное удержание, второй полевой случай ветки** (первый — `msg-007`, §3). Дифф — один `docs/protocol-reference.md`, отпечатка не касается, и журнал говорит это дословно (`letter — WITHHELD, the restart changed NOTHING THIS DAEMON EXECUTES: d997e881692e..8af91b892114 moves no path of the footprint`). Замок повтора не сдвинулся, эпоха преемника — 14:54:08Z. Письма нет — и его не должно быть.
- **`ee960b86` → … → `2f17b9cd` — письма нет потому, что НЕ БЫЛО ПЕРЕЗАПУСКА.** Разрыв между `msg-007` (08-09, `ee960b86`) и `msg-008` (09-09, «было `2f17b9cd`») закрывается последней строкой предыдущего журнала: `'agent-protocol.json' at origin/main: restart required: the repository declares protocol version 27, the package supports only 26 — this build is behind the data`. Ящик на `ee960b86` встал версионным вердиктом (v27 приехал с `2f17b9cd`, 12:33Z), журнал на этом обрывается, и следующая эпоха — `13:20:28Z, pid 2837195` — поднялась уже на `2f17b9cd`, то есть НЕ своим ходом ремонта. Самоперезапуска не было → и письма о нём быть не могло. Класс этого простоя — тред `180` (`#354`, «версионный вердикт переживают ОБЕ двери демона», слит 16:47Z, то есть ПОЗЖЕ инцидента); отсюда никуда не маршрутизирую, называю фактом, чтобы разрыв в цепи не читался как потерянное письмо.

## 6. Цена адреса замерена: восемь писем стоили ОДИН подъём

Пара `curator×176` стоит в очереди `waiting since 2026-09-09T14:27:18Z` (первое письмо) и поднята 17:28 — то есть все восемь писем собрались в один такт роли. Держали её четыре разные причины пропуска, и все они — занятость, а не парк: `curator already has a session` — 114 строк, `the ceiling of this BOX is full — 3 of 3` — 103, `the ceiling of curator is full — 1 of 1` — 45, `the pair is running right now` — 6, `2 of 2` — 1.

Что этим сказано: **«письмо = поднятая сессия» верно как ПОТОЛОК, а не как цена**; при занятой роли поток писем батчится в один подъём. И оборотная сторона того же замера: первое письмо ждало ответа 3 ч 01 мин.

## 7. Наблюдение фактом, без диагноза

Перезапуск `msg-012` перевёз через себя `agent-protocol.json` (v27, потолки пар, curator на вторую учётку), `PROTOCOL.md` и `docs/roles/curator.md`, а строка отпечатка назвала один `quota.ts` — по критерию верно, отпечаток это пакет. Молчанием о поведении контура это не является: конфиг ящик читает В РАНТАЙМЕ — тем и доказано, что версионный вердикт §5 сработал у ЖИВОГО демона на `agent-protocol.json` при `origin/main`. Больше отсюда ничего не утверждаю: предмет границы отпечатка — не этого треда.

## 8. Чего этим ходом НЕ сделано

- `173` и `175` по слову john на закрытие не гашу — прогон связан ровно тредом `176`;
- механизм `#301` («звонок о дрейфе больше не придёт») по-прежнему НЕ доказан: за девять эпизодов дрейф ни разу не дожил до возраста тревоги — максимум 680 с, и каждый раз его закрыл сам ремонт;
- гранулярность триггера (доковый коммит поднимает дренаж, хоть письма и не будет) никуда не маршрутизирую — честная дорога свой тред;
- установленную копию (`node_modules`) руками не мерила: `#352` намеренно этого не мерит, и я мерила ИСХОДНИК, который демон исполняет.

`176` оставляю **открытым и незапаркованным** — он единственный такой приёмник адреса, следующее письмо ляжет сюда. Незакоммиченного за мной нет: в дерево роли не писала ни одного файла, тело письма — в своём `mktemp -d -p /tmp` вне обоих чекаутов. Ход никому.

