---
from: curator
worker: claude-code
session: 9bd131c6-8d1e-4bfc-bafa-2ceacbc25cfd
raised: 2026-09-13T09:08:05Z
date: 2026-09-13T09:14:04Z
expects: ack
waiting-on: curator
parked-on: john
---

**ТРЕБУЕТСЯ РЕШЕНИЕ JOHN: премиса предмета, который ты объявил своим, ОПРОВЕРГНУТА замером — парк НЕ убивает стоячий адрес, пока у слага открыт второй приёмник. Четыре суток парка `176` стоили НУЛЬ потерянных писем и НУЛЬ новых тредов: 12 писем самоперезапуска легли в `161`, цепь по sha не рвётся. Скажи, остаётся ли предмет «беспарковая форма вопроса» и какой тред теперь адрес — `161` или `176`.**

## 0. Что мерено и чем

Ветка кода — `receiver.ts` (выбор приёмника), лента почты — `git show origin/comms:./<тред>/…` из `agent-comms` (чтение, не запись), поле — `.orchestrator/self-restart.json`, `daemon-code.json`, `self-restart-letters.json`, `git log origin/main`. В дерево роли не писала ни одного файла; тело письма — в своём `mktemp -d -p /tmp` вне обоих чекаутов.

## 1. ГЛАВНОЕ: парк форкнул НЕ вперёд, а НАЗАД — и это не форк вовсе

В `msg-002` §5 я написала: «следующее письмо самоперезапуска форкнет новый приёмник, `187` родится». **Это оказалось неверно, и вот чем мерено.**

Правило выбора приёмника — `packages/agent-protocol/src/thread/receiver.ts:132-139`: треды слага сортируются по номеру **по убыванию**, берётся ПЕРВЫЙ годный (`readable !== false && status === "open" && !parked`), и только когда не годен НИ ОДИН — открывается новый (строка 159). Комментарий там же (строки 121-123) говорит прямо: «Several receivers fit → the NEWEST by number… two open receivers of one slug is a state the mail can reach by hand».

`176` я запарковала 2026-09-09T20:20:42Z. Годных приёмников слага `daemon-self-restart` на тот момент было ровно два состояния: `162`–`167`, `173`, `175` — **closed**, `176` — **parked**, `161` — **open, unparked**. Машина взяла `161`. Нового треда не родилось: `git ls-tree origin/comms` по слагу даёт последним `176-daemon-self-restart`, `177+` с этим слагом НЕТ.

**Цепь через стык по sha не рвётся ни в одном звене** (12 писем, `от → стало`, каждое читано):

```
c1b95f155880 → 61a79818f119 → 9935a9b0c14a → eae09763e944 → 0a451fc10f55 → ec166bc6b490
  → 95ba16984221 → bbab2d9a2673 → 316991aab9d7 → fc2b9f3c6282 → 466fdb78d2fb → c4ed32c98a35
  → 76156975ca2f
```

Начало цепи `c1b95f155880` — ровно тот sha, которым кончился адрес ЗДЕСЬ (`msg-002` §3, последнее письмо `176` от 2026-09-09T20:15:05Z). Конец `76156975ca2f` — код, который демон исполняет сейчас (`daemon-code.json`: `sha 76156975ca2f…`, `pid 19982`, `startedAt 2026-09-13T08:57:45Z`; `self-restart.json`: `from c4ed32c98a35`, `at 08:57:24Z`, `went: true`, `attempts: 1`). Ни одного пропущенного звена, ни одного лишнего.

**Тишина 09-09T23:11 → 09-12T16:03 (≈65 ч) — не пропуск писем, а отсутствие кода:** `git log --first-parent origin/main` между `ec166bc6b490` (09-09T23:07:00Z) и `95ba16984221` (09-12T15:39:43Z) пуст. Перезапускаться было не на что.

В `176` за эти четверо суток от `github` не пришло НИ ОДНОГО письма — последнее здесь от 2026-09-09T20:15:05Z, дальше только мои два.

## 2. Что от твоего предмета остаётся, а что отпадает

Отпадает довод, на котором предмет стоял: **«парковый вопрос убивает адрес»** — неверно в общем виде. Парк убивает адрес **только когда парк выбивает ПОСЛЕДНИЙ годный приёмник слага**; при втором открытом письмо просто едет в него, и читатель ничего не теряет — письма самодостаточны, а стык мерится по sha за одну команду.

Остаётся более узкое: у адреса с ЕДИНСТВЕННЫМ приёмником (сегодня это, например, слаги `main-red-alarm` и `notifier-down`) парк по-прежнему заставит машину открыть следующий номер — с объявленной нотой (`receiverNote`, `receiver.ts:163-166`), то есть громко, а не тихо. Беспарковая форма при этом по-прежнему отказана дверью — та половина `msg-002` §5 стоит как стояла.

Своим суждением твоё решение не подменяю: **сузился ли предмет до «адрес с одним приёмником» или отпал совсем — твоё слово, не моё.**

## 3. Где адрес СЕЙЧАС и почему это развилка, а не деталь

Парк снят твоим словом 2026-09-13T09:04:30Z (`park-lifted: john` в шапке). С этой секунды годных приёмников слага ДВА, и `176` старше номером: **следующее письмо самоперезапуска по правилу «newest by number» ляжет СЮДА**, а четверо суток замеров останутся в `161`.

- `161` — открыт, ход отпущен (`waiting-on: —` в письме 2026-09-13T09:07:16Z), в нём живёт вся цепь из §1 и сегодняшний эпизод разобран живой сессией поимённо (семь полей, гард 4 — её работа, не моя, и я её не дублирую);
- `176` — открыт, ход у меня, содержания адреса в нём с 09-09 нет.

Один адрес в двух разговорах — состояние, которое код называет нежелательным прямым текстом. Развести его можно закрытием одного из двух, и **закрытие `161` мне этим прогоном недоступно: прогон связан ровно тредом `176`, писать в чужую ленту нельзя.**

## 4. Что я делаю этим ходом и почему парк здесь дёшев

**Паркую `176` на тебя** — и в этот раз не «зная, что адрес форкнется», а **зная, что не форкнется**: `161` открыт и не запаркован, значит следующее письмо машина положит туда, тем самым механизмом, который четверо суток работал. Если `161` когда-нибудь закроют — машина откроет следующий номер сама и скажет об этом нотой; потерять письмо в обеих ветках нечем.

То есть парк на `176` сегодня — не цена вопроса, а **демонстрация замера §1 в поле**: он и есть проверка того, что адрес переживает парк.

## 5. Чего этим ходом НЕ сделано — поимённо

- **Шесть/семь полей 12 писем поэпизодно не перемеряю**: их мерит живая сессия в `161` (письмо 2026-09-13T09:07:16Z), дублировать чужую ленту своей рукой нечем и незачем; мой замер здесь — стык и цепь по sha, а не эпизоды.
- **Хвост готовых PR не мету**: открыто четыре — `#379` (тред 190), `#378` (182), `#371` (188), `#353` (180-notifier-down). Ни один не принадлежит `176`, а след гарда 5 обязан лечь в ленту треда-ВЛАДЕЛЬЦА; прогон связан одним тредом.
- **Проверка «сменилось ли исполняемое в смысле john» снята из ходов** по твоему слову (`msg-003` §1) — этим ходом её не делала.
- **Потолок памяти роли перемерян и вырос**: `MEMORY.md` = **31 005 байт** против объявленных 24 576 (09-09 было 27 684 → +12 % за четверо суток). Не чинён; предмет не этого треда, никуда его отсюда не маршрутизирую.
- Незакоммиченного за мной нет.

Ход — у curator, парк — на тебе, вопрос стоит первой строкой.

