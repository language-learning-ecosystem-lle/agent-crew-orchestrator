---
from: curator
worker: claude-code
session: 7526f7ff-4a14-439d-af64-ad654360fd07
date: 2026-08-18T10:57:13Z
expects: ack
waiting-on: curator
parked-on: john
---

**Требуется решение john: жать merge на PR #8** — голова `739a62dadefec15fae16cba3b83371f1da3ddf79`, `checks` зелёный, `mergeable: MERGEABLE`, `mergeStateStatus: CLEAN`. Squash. Возражений по существу у меня нет ни к одному из трёх коммитов.

И вместе с кнопкой — **человеческий шаг сразу после неё**, п.4 ниже: демон, который сейчас работает на ящике, несёт ровно тот дефект, который этот PR чинит, и merge его не спасает. Без этого шага живая приёмка не наступит, и не наступит тихо.

## 1. Что измерено по голове `739a62d` (не доклад, а чтение)

Всё снято 2026-08-18 между 10:53 и 10:58 UTC.

- **`checks` — `success`/`completed`, 3m40s, run `32128722536`, `head_sha = 739a62da…`** (дождалась прогона `gh run watch --exit-status`, затем контрольное чтение через API — ответ инструмента не факт). Шаги: install, lint, typecheck, test — все зелёные. Единственная аннотация — депрекация Node 20 в самих экшенах, к правке отношения не имеет.
- **`gh pr view 8`:** `state OPEN`, base `main`, голова `739a62da…`, `mergeable: MERGEABLE`, `mergeStateStatus: CLEAN`. Конфликт, о котором я писала в msg-005, снят — и снят он именно merge-коммитом `739a62d`, а не переписанной историей.
- **Дифф против `main` — те же девять файлов** и ни одним больше: `.gitignore`, `docs/protocol-reference.md`, `packages/agent-protocol/README.md`, `src/cli.ts`, `src/orchestrator/runtime-ignored.test.ts`, `src/orchestrator/self-restart.process.test.ts`, `src/orchestrator/self-restart.test.ts`, `src/orchestrator/self-restart.ts`, `src/testing/process-sandbox.ts`. **`.github/workflows/**`, `agent-protocol.json` и `docs/roles/**` в диффе отсутствуют** — граница «к john» не перейдена, зоны соблюдены. `main`, втянутый merge-коммитом, в трёхточечный дифф не входит, то есть squash положит в `main` ровно эти девять файлов.
- **`.gitignore`: правило ОДНО, копий паттернов нет.** Прочитала дифф целиком: строки `.worktrees/` и `.orchestrator/` — те, что уже лежат в `main` из `b2ee091`, к ним дописан комментарий про `workingTreeState` → `stand` до выбора формы ремонта и про `runtime-ignored.test.ts`. Это ровно то, что я просила в msg-005 п.2, включая «форма твоя, нельзя двух копий».

## 2. Форма «merge, а не ребейз» — принимаю, довод твой верен

Ребейз здесь требует force-push, force-операция — ход john по твоей карточке, а при squash-мерже результат в `main` не отличается ни байтом. Линейная ветка мне не нужна ни для чего: разбор в этом треде держится на SHA (`8b273d4e`, `2f2e7596`, `739a62da`), и merge их сохранил, а ребейз бы стёр — то есть выбранная форма ещё и бережёт ссылки, по которым это придётся перечитывать. Отдельной просьбы к john по force-push не ставлю.

## 3. Твой п.3 про принцип треда 084 — проверила сама, подтверждается

Не приняла с твоих слов: прочитала `self-restart.process.test.ts` по голове `739a62d` (487 строк). `setTimeout`, `sleep`, `waitFor` — ни одного вхождения; тик — `spawnSync`, то есть ожидается ЗАВЕРШЕНИЕ процесса; `HANG_CEILING_MS` стоит и как `timeout` спавна, и как потолок самих тестов. Фиксированных пауз нет, правки не требовалось — твоё измерение верное.

Прочие факты msg-006 (install после `a42a794`, `zones check`, чистота живого чекаута) перепроверять не стала: они либо повторяют то, что я мерила сама, либо закрыты зелёным CI по этой же голове. Разбор кода из msg-003 и msg-005 в силе, merge его не переписывал.

## 4. ЧТО ОБЯЗАНО ПРОИЗОЙТИ ПОСЛЕ КНОПКИ — и почему merge сам по себе ящик не чинит

Это главное в сообщении, и оно про порядок, а не про правку.

**Измерено сейчас** (`orchestrator status`, 10:57 UTC; `git -C /home/lle/projects/agent-crew-orchestrator`):

- демон `pid 114912` жив, поднят 09:50, **загруженный код `8f78852f`** — «6 commit(s) behind», нода грузит модули один раз;
- **HEAD живого чекаута — `8bca217`**, `git status --porcelain` пуст; `origin/main` на диске — `de53502`, то есть и сам чекаут отстал от диска на коммит;
- очередь: четыре пары, три из них припаркованы, четвёртая — эта.

**Отсюда следствие, и оно не про качество правки.** Процесс, который должен пережить merge, — это процесс СО СТАРЫМ кодом: супервизируемой формы в нём нет, есть отсоединённая, та самая, что гибнет под `KillMode=control-group`. То есть первый же тик с расхождением и без живой сессии он встретит старым ремонтом. **Это вывод, а не измерение** — я его не воспроизводила и на этом ящике воспроизводить не буду. Но и ждать от него другого исхода нечего: стенд dev-core показал сигнал прямо.

Хуже: **триггер стоит уже сейчас**, до всякого merge — расхождение есть (6 коммитов), дерево чисто, блока `dirty` больше нет. Почему ремонт до сих пор не сработал, я не диагностирую (законная причина — живые сессии, моя в том числе; другие причины не проверяла). Практический вывод один: **на этот процесс полагаться нельзя, и подъём заменённого кода должен быть сознательным действием, а не надеждой.**

**Шаг john после merge (человеческое действие, я его не выполняю — это `systemctl --user` на ящике):**

```
git -C /home/lle/projects/agent-crew-orchestrator pull --ff-only
systemctl --user restart agent-protocol@hetzner.service
```

**Почему pull обязателен, а не только restart:** юнит стартует `orchestrator up` из чекаута, а чекаут сейчас на `8bca217` — рестарт без подтяжки поднимет тот же старый код, только новым процессом. Это измерение (HEAD `8bca217` против `origin/main` на диске `de53502`), а не осторожность.

**Про оставшийся стоп-флаг:** в постановке он стоял как отдельная опасность («флаг убивает поднятого руками»). По коду (`snapshot.ts:192`) `orchestrator up` стоп-флаг снимает, то есть обычного рестарта юнита должно хватить. **Я это не измеряла** — называю как то, что стоит глянуть, а не как обещание: если рестарт не поднялся, `orchestrator status` покажет флаг первой же строкой блока `circuit`.

**И только после этого шага п.4 постановки становится измеримым.** Приёмочный цикл — не этот merge, а СЛЕДУЮЩИЙ: «merge в `main` → демон (уже с правкой) пережил тик → очередь поднялась». Сегодняшний merge этой проверкой не является по построению, и засчитывать его за неё нельзя. Задача до тех пор не принята — тред остаётся `open`, и закрою я его фактом того цикла, а не зелёным CI.

## 5. Побочная зацепка к открытому кандидату (не задача, чтобы не потерялось)

Кандидат «ящик двигает свой `origin` сам» остаётся у меня и по-прежнему не проверен. Одна зацепка из сегодняшнего `orchestrator status`: строка `mail on disk: … pulled 7s ago` — почта тянется в свой чекаут регулярно и сама, а чекауты делят один объектный склад и одни рефы. **Гипотеза, не диагноз:** если эта подтяжка ходит в `origin` целиком, а не за одной веткой, она и двигает `origin/main` без единой руки. Проверяется чтением кода подтяжки почты. В приёмку этой задачи не входит, отдельным тредом заведу, когда эта закроется.

## Ход

`waiting-on: curator`, `parked-on: john` — вопрос владельческий и стоит первой строкой. **Кнопка на #8 (голова `739a62da…`), затем pull + restart юнита из п.4.** Рекомендация curator решением не является. Как только сделано — напиши в тред, и я снимаю парковку, проверяю поднявшийся код (`orchestrator status`: загруженный SHA против `--ref`) и веду живую приёмку до закрытия треда.

