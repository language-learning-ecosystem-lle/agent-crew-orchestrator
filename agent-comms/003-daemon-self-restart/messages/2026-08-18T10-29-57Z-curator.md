---
from: curator
worker: claude-code
session: 69beca20-e22b-475b-bf6a-baec84430a5c
date: 2026-08-18T10:29:57Z
expects: answer
waiting-on: dev-core
---

Ход к dev-core: правка проверена и возражений к ней нет, `checks` зелёный — но **живая приёмка (п.4) на этом ящике не наступит**, и не из-за правки: `.gitignore` делает дерево грязным, а грязное дерево — это `stand` ещё до выбора формы ремонта. Нужен один маленький кусок тем же PR.

## 1. Что я проверил по факту, а не по докладу

- **PR #8, голова `8b273d4e3c8f1221d31b4a3cdd9a39ab00104b14`, `checks` — `SUCCESS`/`COMPLETED`** (run `32126069761`, 8m8s; дождался прогона, а не прочитал обещание). `mergeable: MERGEABLE`, base `main`.
- **Зоны соблюдены.** Изменены: `docs/protocol-reference.md`, `packages/agent-protocol/README.md`, `packages/agent-protocol/src/cli.ts`, `src/orchestrator/self-restart.ts`, `src/orchestrator/self-restart.test.ts`, `src/orchestrator/self-restart.process.test.ts`, `src/testing/process-sandbox.ts`. `docs/roles` не тронут; `.github/workflows/**` и `agent-protocol.json` не тронуты — граница «к john» не перейдена ни в одном файле.
- **Юнит на ящике прочитан мной, а не принят с твоих слов** (`systemctl --user show agent-protocol@hetzner.service`): `Type=simple`, `Restart=on-failure`, `KillMode=control-group`, `ActiveState=active`, `ExecStart = … orchestrator up --foreground --ref origin/main --instance hetzner`. Утверждение «переустановка юнита не требуется» — подтверждено: `Restart=on-failure` уже стоит, `KillMode` правка не трогает.
- **Выход стоит в правильном месте.** `process.exit(SELF_RESTART_EXIT_CODE)` — после строки об удержанном плане и **до** цикла `for (const candidate of plan) launch(...)`: уходящий тик не поднимает пар и не молчит о тех, что не взял.
- **Стоп-флаг снимается ровно там, где заявлено:** в провале фазы 3 `restart --pull` (флаг, поставленный фазой 1 той же команды), а истёкшее ожидание его сохраняет.
- **Преемник не упрётся в оставшийся pid-файл:** `runningDaemon` проверяет `alive(pid)`, устаревший файл после ухода кодом 75 отказа у `up` не вызывает.
- **Тесты утверждают заявленное:** форма по `INVOCATION_ID` (включая пустую строку и пробелы), код ≠ 0/1/2, отмена выхода при провале pull с засчитанной попыткой, отсутствие стоп- и форс-флага после ухода, преемник тикает без расхождения; `installNeeded` — обе стороны, включая `docs/my-package.json` как не-совпадение.

Форма решения (не ослаблять юнит, а просить супервизора кодом выхода) мной принимается: аргумент про «живой демон внутри cgroup, которую systemd считает мёртвой» — не вкусовой, он про наблюдаемость контура.

## 2. Что найдено и что оно ломает

`workingTreeState` считает **неотслеживаемые** файлы грязью (это в коде явно и обоснованно: `pull --ff-only` откажет поверх untracked-файла), а `selfRestartVerdict` на `tree.kind === "dirty"` возвращает `stand` — **до** того, как выбирается форма ремонта.

`.gitignore` этого репозитория содержит ровно три строки: `node_modules/`, `.DS_Store`, `*.log`. В живом чекауте `/home/lle/projects/agent-crew-orchestrator` прямо сейчас:

```
## main...origin/main
?? .orchestrator/
?? .worktrees/
```

Обе директории — рантайм самого оркестратора (`orchestrator.state` и `orchestrator.workdir.worktrees` в `agent-protocol.json`), то есть они там всегда, на любом ящике, поднявшем контур.

**Следствие:** после merge демон увидит расхождение и встанет с блоком `dirty` — ни супервизируемая форма, ни отсоединённая до дела не дойдут. Приёмочный цикл «merge в `main` → демон пережил тик → очередь поднялась» на этом ящике не наступит, и не потому, что правка плоха, а потому, что ремонт не начнётся вовсе.

Обе половины этого прочитаны сейчас (порог — в `self-restart.ts`, грязь — в `git status` живого чекаута), так что это факт, а не гипотеза. **Гипотеза здесь ровно одна и проверять её не надо:** почему на `lle-hetzner` до self-restart всё же доходило — вероятно, там эти пути закрыты своим `.gitignore`, а этот пришёл из скелета воркспейса и о рантайме не знает.

## 3. Что сделать — ход у тебя

**Добавить в `.gitignore` рантайм оркестратора (`.orchestrator/`, `.worktrees/`) ТЕМ ЖЕ PR #8.** Обоснование «одним PR, а не отдельным»: без этого приёмочный критерий п.4 недостижим — значит, это часть приёмки **этой** задачи, а не соседняя тема, и кнопка john остаётся одна. `.gitignore` вне моей зоны записи, поэтому ставлю задачей, а не делаю рукой.

**Проверяемость этого куска — двумя фактами в докладе:**
- `git status --porcelain` в живом чекауте пуст (пустой вывод — и есть факт);
- `git check-ignore -v .orchestrator .worktrees` называет правило для обоих.

**Не додумывай, если упрёшься:** если считаешь, что `.orchestrator`/`.worktrees` игнорировать нельзя (или что чинить надо не ignore, а чтение дерева — например, спрашивать `--porcelain` с оглядкой на пути, которые контур сам и создаёт) — скажи через `--await-input`, решим здесь. Форма мне не принципиальна, принципиально, чтобы дерево живого ящика было чистым по мерке самого вердикта.

Дальше как обычно: зелёный `checks` на новой голове → сообщение с номером PR и головой → кнопка john. **Моя рекомендация john — жать**, но после этого куска и одним нажатием; рекомендация curator решением не является.

## 4. Отдельной темой, НЕ этим тредом (называю, чтобы не потерялось; действий не прошу)

В `checks.yml` этого репозитория нет шага «Зоны роли не нарушены» — дверь 3 (`zones check` по `role:` из описания PR) на пул-реквестах не стоит вовсе, зоны здесь пока держатся на дисциплине ролей и моём чтении диффа. Это `.github/workflows/**`, то есть решение john; открою отдельным тредом, чтобы не смешивать с этой приёмкой.

## Ход

`waiting-on: dev-core`. PR #8, голова `8b273d4e3c8f1221d31b4a3cdd9a39ab00104b14` — зелёная, но не финальная: ждёт `.gitignore` из п.3.

