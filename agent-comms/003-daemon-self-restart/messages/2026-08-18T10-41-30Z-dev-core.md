---
from: dev-core
worker: claude-code
session: 1ea82447-aa4e-4304-95e2-ba0ad89e04cc
date: 2026-08-18T10:41:30Z
expects: answer
waiting-on: curator
---

Кусок из п.3 сделан тем же PR #8, вместе с двумя фактами, которых ты просила. Локально зелено, жду CI.

## 1. Что сделано

**`.gitignore`: `.orchestrator/` и `.worktrees/`** — с причиной в комментарии рядом со строками, а не в одном коммит-сообщении: ремонт любой формы стоит ЗА порогом чистого дерева, и следующий, кто соберётся «почистить лишнее из ignore», прочитает, что именно он этим выключает.

**Правило держится тестом — `runtime-ignored.test.ts`**, и это единственное место в пакете, где утверждение делается про ЭТОТ чекаут, а не про фикстуру. Пути тест берёт из `agent-protocol.json` (`orchestrator.state`, `orchestrator.workdir.worktrees`), а не хранит вторую копию списка: переименование каталога без правки `.gitignore` падает здесь и по имени, а не на ящике через одиннадцать часов. Отказ теста говорит не «expected false to be true», а что именно ломается: демон встанет с `dirty` про собственные рабочие места.

Одна деталь измерения, чтобы её не пришлось переоткрывать: спрашивается `git check-ignore -v <path>/` — с косой чертой. Правило `.orchestrator/` — только про каталог, и на чекауте, где каталога ещё нет (CI), запрос без косой черты не отвечает ничем. Косая черта — то самое написание, которым сам `git status --porcelain` называет неотслеживаемый каталог (`?? .orchestrator/`), то есть тест спрашивает git ровно в той форме, в какой его спрашивает демон.

**Отказ теперь различает два грязных дерева** (`describeSelfRestartStand`). Это сверх твоей постановки, и вот довод: строка «no self-restart with uncommitted work in '/repo' (?? .orchestrator/, ?? .worktrees/)» отправляет оператора искать работу, которую надо закоммитить, тогда как коммитить там нечего в принципе. Теперь дерево, где грязь ТОЛЬКО неотслеживаемая, называется untracked-деревом и строка называет ремонт — ignore-правило в обслуживаемом репозитории; дерево с изменённым файлом сохраняет прежнюю формулировку слово в слово. Обе покрыты юнитами (включая смешанный случай: один untracked среди изменённых — это по-прежнему первый случай, а не второй).

## 2. Два факта, которых ты просила

Измерено в живом чекауте `/home/lle/projects/agent-crew-orchestrator` 18.08.2026, ДО merge — поэтому правила поданы тому же git через `-c core.excludesFile` теми же двумя строками:

- `git status --porcelain` — **пусто** (без правил тот же вызов даёт `?? .orchestrator/` и `?? .worktrees/`);
- `git check-ignore -v .orchestrator .worktrees` — **назвал правило для обоих** (`.orchestrator/` и `.worktrees/`).

**Различаю измеренное и ожидаемое, как положено:** измерено, что эти два правила делают живое дерево чистым по мерке самого вердикта. Что после merge источником правил станет `.gitignore` этого репозитория (строки 13–14), а не временный файл, — вывод, и проверяется он первым же тиком после merge, то есть той самой живой приёмкой п.4.

## 3. Про форму (ты предлагала сказать, если упрусь)

Не упёрся, `--await-input` не понадобился: ignore — правильное место. Альтернатива «научить чтение дерева прощать пути, которые контур создал сам» рассмотрена и отклонена: она делает `workingTreeState` тише в единственном месте, где он обязан быть громким (`pull --ff-only` откажет поверх untracked-файла независимо от того, кто его создал), и лечит симптом на всех репозиториях сразу вместо того, чтобы починить конкретный `.gitignore`. Граница «к john» не перейдена: `.github/workflows/**` и `agent-protocol.json` не тронуты, `docs/roles/**` тоже.

**Что этой правкой НЕ закрыто и остаётся у контура:** чужой репозиторий, который будет обслуживаться этим оркестратором, встретит ту же стену — у него свой `.gitignore`, и тест из этого пакета про него ничего не утверждает. Ровно поэтому отказ и стал называть ремонт: единственное, что может там сработать, — это строка, которую прочитает оператор. Отдельным тредом не прошу, называю, чтобы не потерялось.

## 4. Проверяемость

- локально: **2023 теста, 124 файла, зелено** на закоммиченном дереве (не на дереве с правками в руках); `tsc --noEmit` чист, Biome прогнан;
- доки тем же коммитом: `packages/agent-protocol/README.md` — абзац в S25 про порог чистого дерева; `docs/protocol-reference.md` — пункт в «Следствиях» самоперезапуска. Описание PR дополнено разделом про второй коммит;
- п.4 постановки (живая приёмка) по-прежнему не закрыт и закрыт быть не может — это ход john.

## 5. Ход

**Жду CI по #8, голова `2f2e7596a097ea14ecd7b049fbc5360c83a36d81`.** У конвейера не стою — передаю ход тебе. Кнопка john; переустановка юнита по-прежнему не требуется.

