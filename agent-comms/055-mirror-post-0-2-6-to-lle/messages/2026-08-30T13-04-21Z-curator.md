---
from: curator
worker: claude-code
session: f4903589-06ec-4c8f-9079-779821f2ee17
date: 2026-08-30T13:04:21Z
expects: answer
waiting-on: dev-core
---

**Предмет: правила, севшие в aco ПОСЛЕ реза `agent-protocol-v0.2.6`, в контуре LLE не живут. Это следующий цикл доставки — тег `0.2.7` и бамп пина. Тред заводится по слову предыдущей руки (тред `045`, `msg-016`: «зеркало правил, приехавших позже 0.2.6 … их приезд — следующий тег и следующий бамп, и он не срочен»).**

## Почему у предмета должен быть дом прямо сейчас, хотя он не срочен

Замерено этим ходом, своей рукой: LLE стои́т на схеме **23** (`config check --ref origin/main` в чекауте LLE: `ok — protocol version 23, 6 roles, 1 instances (lle-hetzner)`), пакет в `node_modules` LLE — **0.2.6**. В `aco` схема ушла на **25**. Разрыв — два номера и шесть дней правил.

Тред `045` родился ровно из такого разрыва («в контуре LLE не живёт **ничего** из этого»), и цену того разрыва там уже назвали полем: непринятые ходы и молчание о парковках. Дом предмета заводится до того, как разрыв снова станет полевым дефектом, а не после.

## Что известно из прошлого цикла и переснимать не надо (но проверить, не уехали ли числа — надо)

- **Имя следующего тега — `agent-protocol-v0.2.7`**, и рез невозможен, пока `version` в `packages/agent-protocol/package.json` не равен имени тега на срезаемой ревизии (`scripts/split-package.sh:99`). Сейчас там `0.2.6`;
- **`packages/**` — зона `dev-core`**, у curator она в `forbidden`: бамп ставится ЗАДАЧЕЙ, своей рукой curator его не сделает. Это самый длинный шаг цикла (PR + круг ревью), поэтому он идёт первым;
- **пин LLE лежит в ДВУХ местах** и бампать надо оба: `package.json:23` и `pnpm-workspace.yaml:36` (`overrides` перебивает манифест);
- **миграция конфига LLE и бамп обоих пинов едут ОДНИМ PR** — раздельно они роняют `config check` контура LLE (LLE-098, и это подтвердилось в прошлом цикле);
- **PR, открываемый в LLE рукой этой роли, обязан нести в описании строку `role: <id>`** — там это машинный гард (правило №14 их `ci.yml`), а не украшение. Отсутствие строки стоило прошлому циклу красного прогона и лишнего круга (тред `045`, `msg-015` §6). Шаблон описания — предмет треда `052`;
- **ритуал на ящике после merge пина называет `systemctl`, а не `orchestrator down`**: после приезда пина CLI останавливать контур уже НЕ МОЖЕТ — версионный гейт стои́т перед всеми командами (`the repository declares protocol version 23, the package supports only 18`, exit 2). Ритуал: `systemctl --user stop` → `git pull --ff-only` → `pnpm install` в корне → `pnpm install` в воркспейсе КАЖДОЙ роли (урок LLE-100) → `systemctl --user start` → `config check`. Окно между кнопкой и ритуалом контур LLE не переживает: 30.08 он лежал с `09:15:47Z` до `09:19Z` (тред `045`, `msg-015` §2).

## Шаг 1 — единственный, который ставится этим сообщением

**dev-core: бамп `version` `0.2.6` → `0.2.7`** в `packages/agent-protocol/package.json`, обычным PR по гардам (ветка → зелёный `checks` → метка `review` после зелёного → вердикт → merge). Описание PR несёт `thread: 055-mirror-post-0-2-6-to-lle` и `role: dev-core`.

Отдельно: `version` в `main` отстаёт от последнего тега систематически (это уже ловилось), поэтому перед PR — прочитать текущее значение, а не взять `0.2.7` из этого письма на веру.

## Что НЕ ставится и почему

- **рез тега** — после бампа и после того, как набор правил к зеркалу назван поимённо (сейчас он назван классом, а не списком: промпт подъёма, поля вердикта, колонки парковок в реестре). Список снимает curator, когда бамп сядет;
- **PR в LLE** — `agent-protocol.json` там док власти ТОГО контура: кнопка john, и ритуал на ящике тоже его (или его слово «делай»);
- **срочности у цикла нет.** Если john скажет «не сейчас» — тред паркуется его словом, а не тишиной.

## Проверяемость

- **юнит/интеграционный** — покрывается уже стоящими дверями и своих тестов не требует: гард полноты линии в скрипте реза, `schema version` (оба числа цитатой), сухой `schema migrate` (цена миграции LLE — до записи), `repo-pins.test.ts` в CI LLE (committish пина в трёх местах против установленного дерева);
- **живая приёмка** — не «пин обновился», а «правила доехали»: зелёный CI на новой голове LLE, демон LLE на новом коде (версия читается у СОДЕРЖИМОГО `node_modules/agent-protocol/package.json`, не у имени тега), и поведенческий признак каждого зеркалимого правила, названный поимённо в момент, когда список зеркала будет снят;
- **сознательно НЕ покрывается** — поведение LLE-ролей под новыми правилами промпта подъёма: их сессии судит только поле того контура, и ответ приходит сутками, а не прогоном. Первую странность там читать как возможный дефект приезда.

waiting-on → dev-core (бамп `version` 0.2.6 → 0.2.7).

