---
from: curator
worker: claude-code
session: d5345b76-c726-4d07-8007-f40c20a9c207
raised: 2026-09-03T13:40:14Z
date: 2026-09-03T13:46:44Z
expects: none
waiting-on: —
---

**Приёмка 055 наступила и тред ЗАКРЫВАЕТСЯ этим тактом. Критерий постановки был «правила доехали», и он сошёлся числами на живом дереве потребителя, снятыми ЭТОЙ рукой сейчас: схема **25**, содержимое `node_modules/agent-protocol/package.json` — **0.2.10**, пин `agent-protocol-v0.2.10` в ОБОИХ местах. Разрыва по носителю-пину нет вовсе: потребитель стои́т на том же теге, что и голова дома.**

Слово john о закрытии принесено предыдущим письмом этого треда (`2026-09-03T13-08-09Z-curator.md`, «тред закрывай своим тактом»). Ниже — не пересказ того письма, а перемер каждого пункта, который в закрытие переезжает, и поимённый разбор открытых предметов постановки.

## 1. Приёмка — замер своей рукой, а не перенос числа из своего же прошлого письма

Чтения файлов в дереве потребителя (только чтения — см. §5):

- `agent-protocol.json:2` → `"protocolVersion": 25`;
- `node_modules/agent-protocol/package.json:3` → `"version": "0.2.10"` — версия читается у СОДЕРЖИМОГО установленного пакета, как и требует «Проверяемость» постановки, а не у имени тега;
- пин в двух местах, оба на одном значении: `package.json:23` и `pnpm-workspace.yaml:36` → `#agent-protocol-v0.2.10`.

Дома, на этом дереве, тем же ходом: `packages/agent-protocol/src/schema/version.ts:53` → `CURRENT_PROTOCOL_VERSION = 25`; `agent-protocol.json:2` → `25`; `packages/agent-protocol/package.json` → `0.2.10`; последний тег на удалённом (`git ls-remote --tags`, сортировка `-V`) → `agent-protocol-v0.2.10`.

**Разрыв, из которого родился тред, закрыт целиком:** он был «схема 23 против 25, пакет 0.2.6», стал «25 против 25, 0.2.10 против 0.2.10».

## 2. Носитель оказался ДРУГИМ, и это надо сказать, а не замолчать

Тред резал `agent-protocol-v0.2.7` (`47da13db…`), а доехали правила тегами `v0.2.9` (переезд #464 в их контуре, кнопка john 02.09) и `v0.2.10` (объявлен потребителю чатным curator 03.09). Приёмка от этого не страдает — критерий был «правила доехали», а не «наш тег запинен», а `CURRENT_PROTOCOL_VERSION = 25`, за которым тред и заводился, лежит в обоих носителях. **Но шаг этого треда обогнали, и в закрытие это едет фактом, а не как «наш тег сработал».**

## 3. Тег потребителя не ложный — проверено деревом, потому что именно этот класс тред и вскрыл

`v0.2.10` мог оказаться таким же, как `v0.2.8`, — срезанным с невлитой головы. Замер:

- дерево реза `agent-protocol-v0.2.10` = `aa092de7fc45d67f4b31f3e0f4b75360507af286`;
- это дерево **встречается в линии `origin/main`**: `packages/agent-protocol` на коммите `7376418c5c399badb7eab2ad57a0ebf092920b8b` (`fix(orchestrator): TMPDIR прогона … (тред 056) (#228)`) равен ему побайтно;
- `origin/main:packages/agent-protocol` сейчас `8e50cd42…` — отличается, и это норма: `main` уехал вперёд тега сегодняшними мержами, а не тег ушёл в сторону от `main`;
- `v0.2.8` (`4e70d139…`) так и остался неиспользованным; непригодным он объявлен в ленте прошлым письмом, двигать его нельзя (`split-package.sh:86-88`).

**Сошлось по-прежнему РУКОЙ, а не дверью** — дверь этого не проверяет, и починка живёт в `095` (§4). Приписывать закрытию 055 заслугу механизма нельзя.

## 4. Открытые предметы постановки — поимённо, чем каждый закрыт

1. **Шаг 1, бамп `version` 0.2.6 → 0.2.7** — исполнен dev-core, PR #143, вердикт ревьюера `approve` в ленте, влит (`f1974d61`);
2. **рез `agent-protocol-v0.2.7`** — исполнен, `47da13db575fc1c135faa82210bd2ab4bc46a0a0`, `protocolVersion 25`, лежит на удалённом (перечитано этим ходом в списке тегов);
3. **список зеркала поимённо** — снят: 24 коммита, носителей два (17 едут пином, 7 не едут им вовсе);
4. **PR в контуре LLE (миграция 23 → 25 + оба пина одним диффом) и ритуал на ящике** — исполнены РУКОЙ ТОГО КОНТУРА и кнопкой john того контура; предметом этого треда они перестали быть решением `062-contour-boundary`. Приёмка этого пункта — §1 выше: числа на их дереве;
5. **прозаическое зеркало (7 коммитов, которые пином не едут НИКОГДА)** — **смерть вслух, и это решение**: предмет живёт в чужих доках власти (`PROTOCOL.md`, `docs/roles/**`, `REVIEWER.md` контура LLE), идёт их рукой и их кнопкой john, а завести ему дом в ЭТОЙ почте роль этого контура не должна. Дома в `agent-crew-orchestrator` он не получает — ни сейчас, ни задним числом;
6. **«гард линии реза односторонний»** — **дом ЕСТЬ, и это проверено чтением, а не словом**: тред `095-cut-guard-one-sided`, постановка curator 03.09 с происхождением первой строкой («тред `055…`, письмо `2026-08-30T17-16-54Z` §3»), четыре пункта работы и раздел «Проверяемость», `waiting-on → dev-core`. Пункт снят домом;
7. **объявление потребителю** («дальше находишь теги сам») — исполнено чатным curator в их тред `124`; следа в этой почте не имеет и иметь не может — стыковую работу не носит ни одна роль этого дерева.

Ничего из перечисленного не переезжает копированием в другое письмо: пункты 1–3 — исполнены, 4 — принят фактом, 5 — умер вслух, 6 — уехал в свой дом, 7 — вне этой почты.

## 5. За собой чисто

- рабочее дерево роли пустое: `git status --porcelain` — ни строки (проверено в начале хода и перед этой отправкой);
- в дереве потребителя — **только чтения файлов** (`agent-protocol.json`, `node_modules/agent-protocol/package.json`, `package.json`, `pnpm-workspace.yaml`): ни ветки, ни коммита, ни PR, ни комментария. Границу контура (`062`) этот ход не переступал ничем;
- в эту почту писал только командой.

## 6. Хвост PR — прогнан, кнопкой этого прогона не жму

`gh pr list --state open`: **#239** (тред 090, метка `review`), **#237** (078, `review`), **#233** (064, без метки), **#216** (052, без метки), **#202** (074, `review`), **#114** (048, `review`). **Ни один не несёт `thread: 055-…`** — кандидат чужого треда моей кнопкой не мёржится, след гарда 5 ушёл бы не в ту ленту, поэтому `merge-gate` по ним не гонял и метки не трогал. Список назван, чтобы молчания не осталось: хвост живой и ждёт руки в своих тредах.

## 7. Закрытие

Следом за этим письмом, тем же тактом: `thread status --thread 055-mirror-post-0-2-6-to-lle --from curator --status closed --write`, и контрольное чтение шапки после записи. Право `thread-status` у роли есть; приёмка — §1 и §4.

waiting-on → — (ходить в этом треде больше некому: предмет доехал, живые остатки стоят в `095` и в чужом контуре).

