---
from: curator
worker: claude-ai
date: 2026-08-30T16:16:03Z
expects: none
waiting-on: curator
delivers: john
priority: high
---

**Толчок словом john (чат ~16:15Z: «толкай!») — метку на #143 ПЕРВЫМ ДЕЙСТВИЕМ ближайшего такта. Приоритет этого треда в контуре — наивысший: он держит всю цепочку зеркалирования в LLE.**

## 1. Состояние на момент письма, сверено через API

`#143` (`chore/055-bump-0-2-7`, голова `ca0de66e`) **открыт с `13:16:47Z` — три часа — и метки `review` на нём нет**, то есть круг не запускался ни разу. Дифф — одна строка манифеста (`version` 0.2.6 → 0.2.7). Доков власти нет, кнопка john не нужна, мержит curator по гардам.

Порядок прежний и он твой же: зелёный `checks` на голове → метка `review` (снять и повесить заново, если голова уехала) → вердикт → merge по пяти гардам со следом.

## 2. Почему это блокирует именно john

Цепочка: **#143 → рез тега `agent-protocol-v0.2.7` → объявление в LLE (стыковая работа чатного curator) → PR в LLE с пином и миграцией (23 → 25) → кнопка john → ритуал на ящике LLE его рукой**. Первый шаг стоит три часа, и все остальные ждут его.

Цена простоя измерена и наблюдается john прямо сейчас: в LLE **нет норм**, севших в этом контуре после реза `0.2.6` — «роль не ждёт прогон», поля вердикта, «метку перевесить после смены головы», починенный промпт подъёма. Сегодня john видел это живьём: сессия `dev-speech` держала аренду в `draining` семнадцать минут, и упрекнуть её нечем — правила у неё нет.

## 3. Что не делать

**Не тащить в этот PR ничего сверх бампа.** Состав пакета для LLE определяется диффом при резе, а не памятью; сам рез, пин и миграция — отдельные шаги, как и записано в постановке треда.

waiting-on → curator.
