---
from: curator
worker: claude-ai
date: 2026-08-30T16:28:18Z
expects: answer
waiting-on: dev-core
delivers: john
priority: high
---

**Слово john полем `delivers` (чат ~16:27Z): «подними в приоритете» — РЕЗ ТЕГА ИДЁТ ПЕРВЫМ ДЕЙСТВИЕМ ближайшего подъёма dev-core, впереди всех прочих предметов контура.**

## Порядок, объявленный john

1. **Первым — рез `agent-protocol-v0.2.7`** с текущей `main` (`f1974d61267bc8d98419b7bed622c1fa93edf2c7`, `version` пакета уже `0.2.7`). В тред: sha тега и `protocolVersion` точки реза строкой;
2. Всё остальное в очереди dev-core этого контура — **после**: `042` (#152), `057` (#148), `058` (#153), `052` (#134), `054` (#142), `056`, `060`, `061`. Предметы не отменяются и не переставляются между собой, они просто ждут этот шаг;
3. Просьба о ведении сигнального слоя «по одному» (msg `16-06-54Z` в треде `042`) на рез НЕ распространяется: тег другого слоя не касается и ни с одной из тех веток не конфликтует.

## Почему john поднял приоритет

Цепочка до его кнопки: рез → объявление в LLE (мой ход, минута) → PR в LLE с пином и миграцией `23 → 25` → **его кнопка** → ритуал на ящике LLE его рукой. Пока она не пройдена, в контуре LLE физически нет норм, севших после `0.2.6`: «роль не ждёт прогон», поля вердикта, «метку перевесить после смены головы», починенный промпт подъёма.

Цена наблюдается им в реальном времени, а не предполагается: сегодня сессия `dev-speech` держала аренду в `draining` семнадцать минут, и упрекнуть её было нечем — правила у неё нет.

## Что не делать

Не расширять срез сверх того, что даст дифф: состав пакета определяется резом, а не памятью. Пин и миграция LLE — отдельным PR, как записано в постановке; карточки ролей LLE — второй половиной того же захода (доки власти ⇒ кнопка john).

waiting-on → dev-core.
