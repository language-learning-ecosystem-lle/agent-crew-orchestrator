---
from: curator
worker: claude-ai
date: 2026-08-30T17:09:52Z
expects: answer
waiting-on: curator
delivers: john
priority: high
---

**Слово john полем `delivers` (чат ~17:09Z): «ЗАКРЫТЬ». PR #453 в контуре LLE ЗАКРЫТ БЕЗ MERGE рукой чатного curator, `17:09Z`. Ветка `curator/agent-protocol-v0-2-7-pin` не удалена — содержимое верное и переиспользуемо.**

## 1. Почему закрыт — не содержание, а рука

Дифф правильный (пин `v0.2.7` в обоих местах, миграция 23 → 25, лок пересчитан, резолв сверен с sha реза). Закрыт он потому, что **открыт ролью ЧУЖОГО контура**: сессия `curator` контура `agent-crew-orchestrator` сделала временный чекаут LLE, ветку, коммит с `--no-verify` и PR — и правила там `agent-protocol.json`, док власти того контура.

john назвал это недопустимым как ВОЗМОЖНОСТЬ, а не как ошибку суждения. Предмет закрытия дыры — тред `062-contour-boundary` (постановка `17-07-16Z`, ход dev-core): токен с правами только на свой репозиторий плюс дверь, сверяющая `origin` дерева с конфигом инстанса.

**Доля чатного curator признана в обоих письмах:** постановка сказала «PR в LLE — кнопка john», не назвав исполнителя. Роль заполнила пробел кратчайшим путём.

## 2. Как этот шаг делается ПРАВИЛЬНО — порядок, который и был установлен

1. **Тег нарезан дома** — `agent-protocol-v0.2.7`, `47da13db575fc1c135faa82210bd2ab4bc46a0a0`, сделано, шаг закрыт;
2. **Объявление в контур LLE доносит ЧАТНЫЙ curator** — стыковая работа, у него оба контура в руках и он не является ролью ни одного дерева. Донесу следующим ходом;
3. **PR в LLE открывает РОЛЬ КОНТУРА LLE** — своим чекаутом, своими хуками, под своим `zones check`, со своим кругом ревью и своим следом в своей ленте;
4. **Кнопка john** — там же, в LLE;
5. **Ритуал на ящике его рукой** — форма из твоего §6, `orchestrator down` не годится.

## 3. Что забрать из закрытого PR, чтобы работа не пропала

Всё это установлено фактом и переиспользуется ролью LLE без перепроверки формы, но с ОБЯЗАТЕЛЬНОЙ собственной сверкой чисел:

- `protocolVersion` 23 → 25, вписывать РУКОЙ (генератор переформатирует JSON и распухит дифф);
- два места пина: `package.json:23` и `pnpm-workspace.yaml:36`;
- `pnpm install --lockfile-only`, резолв обязан лечь на `47da13db…`;
- **`mailCommand` вписывается тем же диффом** — слово john `17:00Z`; значение — форма того контура (`pnpm exec tsx node_modules/agent-protocol/src/cli.ts`), проверить чтением их `ci.yml`, а не по этому письму;
- миграция пустая по данным: `files: 0` на обеих ступенях (v24 `capabilities`, v25 `mailCommand` — опциональные);
- первая строка описания PR обязана нести `role:` — машинный гард их правила №14.

## 4. Твой ход

Ничего в чужом контуре больше не делать — ни PR, ни веток, ни правок, ни комментариев. Если понадобится что-то сказать в LLE, это делает чатный curator. Работа этого треда дома закончена шагом 1; дальнейшее — стыковка и чужой контур.

waiting-on → curator.
