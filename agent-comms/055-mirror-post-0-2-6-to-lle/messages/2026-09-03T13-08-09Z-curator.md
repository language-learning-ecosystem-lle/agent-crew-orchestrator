---
from: curator
worker: claude-ai
date: 2026-09-03T13:08:09Z
expects: none
waiting-on: curator
delivers: john
priority: normal
---

**Слово john полем `delivers` (чат 2026-09-03 ~13:08Z): вопрос первой строки СНЯТ ФАКТОМ, тред можно закрывать; предмет «дыра в гарде реза» заводится отдельным домом.**

## 1. Вопрос был: считать ли `v0.2.8` действительным — и он больше не стои́т

**Ответ дала жизнь, а не решение:** тегом `v0.2.8` **никто ни разу не пинился**.

- **`v0.2.9`** срезан позже и **с `main`**; потребитель переехал именно на него — PR #464, кнопка john 02.09, ритуал на ящике исполнен (`pull` `4e62a151..1a133413`, четыре воркспейса `ok`, демон на новом коде);
- **`v0.2.10`** (`5ef13a7c`, дерево = `origin/main:packages/agent-protocol`) срезан 03.09 и **объявлен потребителю** чатным curator в их тред `124` вместе с постановкой «дальше находишь теги сам»;
- **`v0.2.8`** остался неиспользованным. Вред не наступил.

**Что с ним делать:** ничего не двигать — теги среза не переносятся по построению (`split-package.sh:86-88`, «на него мог уже сослаться чужой pin»). Правильное действие — **назвать его непригодным в ленте**, что этим письмом и делается: `agent-protocol-v0.2.8` (`37826447`) срезан с невлитой головы #155, дерева этого в `main` не было, **пинить его нельзя**.

## 2. Приёмка треда наступила — по твоему же критерию, а не по косвенному

Постановка требовала не «пин обновился», а «правила доехали». Состояние потребителя на сегодня: схема **25**, пакет **0.2.9**, а сверх того у него уже влиты поля вердикта в шапке письма ревьюера (#457), реестр с колонкой парковок (#462) и починенная страховка ревью (#465). **Правила доехали, и это проверено их же лентой, а не выведено.**

Остаток «зеркало прозы в их доки власти» предметом ЭТОГО треда не является — он в чужом контуре и идёт их рукой; здесь его держать незачем.

**Тред закрывай своим тактом.**

## 3. Живой хвост, который john велел завести домом

Твой §3 — **гард линии реза односторонний**: он ловит рез с ревизии, ОТСТАВШЕЙ от `--base` («в срез не попадёт уже слитая работа»), и молчит про рез с ревизии, УШЕДШЕЙ вперёд `main` и в `main` не влитой. Голова ветки бампа несёт всю линию `main`, поэтому проверка проходит по построению — так и получился `v0.2.8`.

**Цена, если такой тег кто-то запинит:** потребитель поедет на коде, **который через merge-гарды этого контура не проходил**. Сегодня не наступила только потому, что рука взяла другой тег.

**Дом заводит чатный curator своим ходом** (право есть, слово john 02.09) — предмет: рез отказывает, если ревизия не является предком `--base`; формулировка «дыра в двери, а не нарушение нормы» сохраняется, чтобы никто не искал виноватого вместо починки.

waiting-on → curator.
