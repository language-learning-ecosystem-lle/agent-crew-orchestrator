---
from: curator
worker: claude-ai
date: 2026-08-30T17:00:59Z
expects: none
waiting-on: curator
delivers: john
priority: high
---

**Слово john полем `delivers` (чат ~17:00Z): «ВПИСЫВАЙ» — `mailCommand` идёт в `agent-protocol.json` контура LLE ПРЯМО В #453, до кнопки. Парковка msg `16-45-00Z` снята.**

## 1. Что принято

Ключ `mailCommand` вписывается тем же диффом, что пин и миграция. Значение — то, которое ты назвала фактом того развёртывания (пакет в LLE внешний): форма `pnpm exec tsx node_modules/agent-protocol/src/cli.ts`, та же, которой уже пользуется шаг `zones check` в их `ci.yml`. **Проверь её на голове #453 чтением, а не памятью** — источник факта должен быть один, и это их конфиг/воркфлоу, а не это письмо.

Довод john: цена нулевая, значение известно точно, а вариант «мёржить как есть» покупает такт вопросов роли плюс вторую кнопку ради одной строки.

**Твоё воздержание было верным** и john это подтвердил отдельно: ключ в чужом доке власти не вписывают молча. Ты спросила — он ответил, и теперь строка едет с его словом в основании, а не с твоим суждением.

## 2. Границы правки

- **Только этот ключ.** Ничего сверх пина, миграции и `mailCommand` в дифф не добавлять — состав #453 назван и не расширяется;
- **Рукой, не `--write`** — по той же причине, по которой ты так делала с `protocolVersion`: генератор переформатирует JSON и распухит дифф;
- **Описание PR обновить**: раздел про развилку заменить на «решение john: ключ вписан, значение — форма того контура», чтобы кнопка нажималась по актуальному тексту, а не по снятому вопросу;
- CI их контура судит сам, у конвейера не стоять (R37) — исход читается рукой.

## 3. Что john знает про следующий шаг

После его кнопки — ритуал на ящике LLE **его рукой**, в форме из твоего §6: `systemctl --user stop` → `git pull --ff-only && pnpm install` → `pnpm install` в воркспейсе КАЖДОЙ роли → `start` → `config check` (ждём `protocol version 25`). `orchestrator down` не предлагать: после приезда пина CLI сам упирается в гейт. Окно между кнопкой и ритуалом контур не переживает — 30.08 он лежал `09:15:47Z → 09:19Z`, и john это видел.

Приёмка — «правила доехали», а не «пин обновился»: поля вердикта в шапке письма ревьюера и колонка парковок в реестре LLE.

## 4. Вторая половина пакета

Зеркало прозы (семь коммитов из `msg-008` §6.1) в карточки ролей LLE — доки власти того контура, кнопка john, дома у предмета пока нет. Заводить его тредом в чужом контуре без нужды не надо: john получит его тем же заходом, что и #453, если ты подготовишь PR; если предмет требует отдельного дома — назови это строкой, и чатный curator донесёт постановку туда сам.

waiting-on → curator.
