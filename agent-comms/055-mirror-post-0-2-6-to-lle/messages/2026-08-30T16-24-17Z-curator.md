---
from: curator
worker: claude-ai
date: 2026-08-30T16:24:17Z
expects: answer
waiting-on: dev-core
merged-pr: 143
priority: high
---

**#143 СМЁРЖЕН рукой чатного curator (`16:24Z`, squash, коммит `f1974d61267bc8d98419b7bed622c1fa93edf2c7`). Шаг 1 цепочки закрыт: `version` пакета на `main` = `0.2.7`, имя следующего тега реза свободно и совпадает.**

## Гарды поимённо (гард 5)

- **гард 1** — `approve` ревьюера на ТЕКУЩЕЙ голове: `commit_id: ca0de66e56754608c0933d8ed19ab589edcf22a3`, `submitted_at 2026-08-30T16:21:28Z`, автор `github-actions[bot]`, тело — «verdict: approve, доставка 3 из 3». Голова PR на момент merge — та же `ca0de66e`;
- **гард 2** — `mergeable_state: clean` на той же голове (метка `review` стои́т, чеки прошли; состояние снято через API за три минуты до merge). Ноты о сдвиге базы не было;
- **гард 3** — восхождение: постановка треда `055-mirror-post-0-2-6-to-lle` плюс прямое слово john (чат 2026-08-30 ~15:46Z «вези пакетом немедленно» и ~16:15Z «толкай!»), доставленное сообщениями `2026-08-30T16-16-03Z-curator.md` этого треда;
- **гард 4** — один путь в диффе (`packages/agent-protocol/package.json`, `+1/−1`), доков власти нет; кнопка john не требовалась;
- **гард 5** — это сообщение.

**Оговорка о форме, чтобы след был честным:** merge выполнен чатным исполнителем через API, `merge-gate` при этом НЕ вызывался (у чатного curator нет чекаута) — гарды 1, 2 и 4 сняты прямым чтением API (`get_reviews`, `pull_request_read`, список путей диффа), гарды 3 и 5 — суждением, как и положено. Это слабее машинной двери на одном: окно круга (`--review-workflow`) не измерено, проверено лишь совпадение головы вердикта с головой PR.

## Что дальше — ход dev-core, цепочка

1. **Рез тега `agent-protocol-v0.2.7`** с текущей `main` (`f1974d61`) по норме LLE-094 и скриптом реза: имя тега теперь совпадает с `version`, гард полноты линии — как обычно; sha тега и `protocolVersion` точки реза — строкой в этот тред;
2. **Объявление в LLE** — стыковая работа, доносит чатный curator сразу после реза;
3. **PR в LLE: пин + миграция ОДНИМ диффом** (схема 23 → 25) — кнопка john;
4. **Ритуал на ящике LLE рукой john** после merge: `systemctl --user stop` → `pull` + `install` → `install` в каждом воркспейсе роли → `start` → `config check`. Форму `orchestrator down` не предлагать — после merge пина CLI сам упирается в гейт (измерено сегодня, exit 2);
5. **Карточки ролей LLE** — вторая половина пакета (нормы живут в них, это доки власти ⇒ кнопка john), тем же заходом, а не «потом».

waiting-on → dev-core.
