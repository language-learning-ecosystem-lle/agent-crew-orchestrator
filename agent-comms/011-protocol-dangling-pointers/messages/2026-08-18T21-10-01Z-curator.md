---
from: curator
worker: claude-code
session: 85b1e1db-0f1f-4b6c-a633-3abbcbed7d25
date: 2026-08-18T21:10:01Z
expects: none
waiting-on: —
---

закрытие треда: **контрольное чтение базы после merge сошлось, оба висящих указателя закрыты по факту; находка §4 вынесена постановкой в тред `012-wake-watch-reserved`.** Тред закрываю (`thread status --status closed`).

## 1. Контрольное чтение базы — не по ответу инструмента, а по содержимому `origin/main`

Читано 2026-08-18 21:06Z с `origin/main` после `git fetch`, голова = `9bd5a21` («docs: два висящих указателя PROTOCOL.md закрыты разделами надстройки (тред 011) (#17)»), squash — как и сказал john.

| проверка | команда (по голове `origin/main`) | результат |
| --- | --- | --- |
| правка в базе, а не только в ветке | `git log origin/main --oneline -3` | `9bd5a21` на голове, под ним `db12188` (тред 010), `50003ca` (тред 009) |
| дифф — ровно два файла | `git show 9bd5a21 --stat` | `PROTOCOL.md \| 5 ++--`, `docs/install-notes.md \| 74 +++++…`, итого «2 files changed, 65 insertions(+), 14 deletions(-)» — больше ничего. `docs/protocol-reference.md`, `agent-protocol.json`, `docs/roles/**`, `.github/workflows/**` не тронуты |
| ни одного висящего указателя | `git show origin/main:PROTOCOL.md \| grep -n "надстройк"` | три строки: **128**, **135**, **245** |
| каждый адрес существует | `git show origin/main:docs/install-notes.md \| grep -n "^## "` | `## 4. Номера PR: переезда не было — своя нумерация с #1, чужая в тексте канона` (строка 86) и `## 5. Режим вахты — механика ожидания` (строка 115) — плюс §1–3, на которые смотрит указатель 135 |
| сопоставление адресов поимённо | чтение строк 128 и 243–248 `PROTOCOL.md` | 128 → «`docs/install-notes.md`, раздел «Номера PR: переезда не было — своя нумерация с #1, чужая в тексте канона»» — имя совпадает с §4 буква в букву; 245 → «`docs/install-notes.md`, раздел «Режим вахты — механика ожидания»» — совпадает с §5 |
| запись о дыре снята, а не оставлена рядом с её закрытием | `git show origin/main:docs/install-notes.md \| grep -n "висящ"` | пусто (exit 1) — раздел «Чего этот файл ещё НЕ несёт — два висящих указателя» из `install-notes.md` ушёл вместе с закрытием |

**Приёмка постановки (msg-001) по пунктам:** п. 1 «Номера PR» — измерено, предмета переезда нет, вместо выдуманного раздела написан раздел о измеренном (§4); п. 2 «Режим вахты» — измерено на ящике, написан §5; п. 3 «указатели привести к существующему» — оба приведены, дифф `PROTOCOL.md` только адресный. Границы §3 соблюдены (таблица выше, строка про дифф). Требования §4 «Проверяемость» закрыты все, включая честно названное отсутствие юнит-теста.

## 2. Решение john по находке §4 — исполнено ровно как сказано

john: поле `wake.mode: watch` — **задел под будущий резидентный режим (R23-этаж: Telegram-фронт/council), не вычищать; в схему/доку — честная пометка**, чтобы следующий читатель не проходил путь находки заново. Постановка заведена: **тред `012-wake-watch-reserved`**, исполнитель dev-core (предмет в `packages/**`), `waiting-on → dev-core`.

Одно уточнение поехало туда вместе с постановкой, и его стоит знать здесь: **находка была сформулирована шире, чем факт.** Перед тем как ставить задачу, я перечитала код (голова `9bd5a21`, 21:08–21:11Z):

- `wake.mode === "watch"` **читается и держит дверь подъёма** — `orchestrator/launch.ts:731` (`roleLaunchability`: не `watch` → `reason: "wake-not-watch"`), плюс витрина `cli.ts:5973`;
- без потребителей в проде — **ЗНАЧЕНИЕ `session`** (единственный живой эффект: кросс-проверка уникальности имени, `roles/registry.ts:112–120`) и **`watchTargets()`** (`roles/registry.ts:60,159`, зовут только `roles/registry.test.ts:124` и `orchestrator/resident.test.ts:54`).

То есть задел — не режим, а адресат режима. Решение john это не меняет (оставить с пометкой), меняется адрес пометки, и в 012 он назван поимённо. Класс ошибки тот же, что уже разбирался в 009 и в §2 предыдущего сообщения: свой диагноз — тоже заявление, и до первого `grep` он не факт. Третий раз за день — записываю как повторяющийся, а не как случай.

## 3. Чего в этом треде НЕ осталось

Открытых хвостов нет. Незакоммиченного в рабочем дереве curator — нет (ветка `docs/011-dangling-pointers` смёржена и в дереве ничего своего). Единственное, что уехало дальше, — постановка 012, и она живёт своим тредом со своей приёмкой.

Статус треда: **closed** (приёмка curator по праву `thread-status`; владельческая половина — кнопка merge #17 — уже сделана john).

waiting-on → —

