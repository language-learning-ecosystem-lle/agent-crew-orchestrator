# PROTOCOL.md: два висящих указателя на надстройку установки — закрыть по измерению

participants: curator, john · status: open

## msg-001 · from: curator · 2026-08-18 · expects: none

постановка: **закрыть два висящих указателя `PROTOCOL.md` на надстройку установки** — оба ссылаются на разделы, которых нет НИ В ОДНОМ файле этого репозитория; исполнитель — curator.

Дыра названа, а не выдумана: она уже стоит отдельным разделом в `docs/install-notes.md` («Чего этот файл ещё НЕ несёт — два висящих указателя», строки 86+ на голове `main`), заведённом тредом 009. Здесь — её закрытие, отдельным диффом и отдельной приёмкой.

## 1. Измерено сегодня (2026-08-18 20:33Z, голова `main` = `50003ca`)

| указатель | текст в `PROTOCOL.md` | адресат |
| --- | --- | --- |
| «Номера PR» | «**Номера PR читаются с оглядкой на переезд репозитория** — граница и способ её применять: надстройка установки, раздел «Номера PR и переезд репозитория»» | раздела с таким именем НЕТ: `grep` по всем `*.md` репозитория даёт только сам указатель и строку-упоминание дыры в `install-notes.md` |
| «Режим вахты» | «Механика ожидания … и удержание вахты без присутствия человека держатся на скриптах установки — надстройка установки, раздел «Режим вахты — механика ожидания»» | то же самое |

Обе ссылки писались на надстройку РЕПОЗИТОРИЯ-РОДИТЕЛЯ (текст канона приехал оттуда), и в этой установке у них нет дома.

**Ссылаться на них номером строки нельзя, и это уже стоило точности:** `install-notes.md` называет «строку 244», а после вставки #14 в строку 135 второй указатель уехал на 246. В работе цитировать ТЕКСТОМ, не номером.

## 2. Что сделать

По каждому указателю сначала МЕРИТЬ, потом писать — и исход у двух указателей может быть разный:

1. **«Номера PR и переезд репозитория».** Померить, есть ли у этой установки предмет вообще: `git remote -v`, номера PR (`gh pr list --state all --json number --limit 100` — здесь нумерация начата с нуля и разрыва может не быть вовсе). Если предмета НЕТ — правильный исход не выдуманный раздел, а честная строка: либо раздел в `install-notes.md`, говорящий «переезда не было, номера сквозные с #1», либо снятие указателя из `PROTOCOL.md`. Что из двух — решает измерение, но НЕ выдумывание содержания;
2. **«Режим вахты — механика ожидания».** Предмет здесь есть: вахту держит резидентный юнит этого ящика, ожидание почты — блокирующий вызов с потолком (`AGENT_PROTOCOL_WAIT_SECONDS`, в живом запуске 3600). Померить по факту (юнит на ящике, значение потолка, кто его ставит) и написать раздел в `docs/install-notes.md`;
3. **Указатели в `PROTOCOL.md` привести к тому, что реально существует** — по образцу вставки #14 (строка 135): норма не меняется, меняется только адрес. Если раздел заведён — имя раздела и файл; если предмета нет — указатель снимается, и в сообщении треда сказано, почему снятие, а не заполнение.

## 3. Границы

- **`.github/workflows/**` не трогать** — предмет john;
- **`docs/protocol-reference.md` не трогать** — установочные факты живут в надстройке, ради чего надстройка и заведена (измерено в 008);
- **`agent-protocol.json` и карточки ролей не трогать** — доки власти;
- **содержание разделов не выдумывать:** раздел, написанный по памяти о репозитории-родителе, — это тот же висящий указатель, только длиннее.

## 4. Проверяемость

- **прямой факт по каждому утверждению нового текста** — команда и её вывод в доклад (`git remote -v`, список номеров PR, юнит/потолок вахты), таблицей, как в 008 и 009;
- **ни одного висящего указателя после правки:** `grep -n "надстройка установки" PROTOCOL.md` на голове ветки, и для каждой строки — имя существующего раздела в `docs/install-notes.md` (проверяется `grep "^## "`), а не глазом;
- **дифф `PROTOCOL.md` — только адреса**, ни байта нормы (`git diff` в доклад);
- **юнит/интеграционного теста нет** — предмет правки текст; названо, не пропущено;
- **приёмка — merge кнопкой john**: дифф трогает `PROTOCOL.md` (док власти), ревьюер заморожен, гарду 1 закрываться нечем.

## 5. Круг

ветка → правка → зелёный `checks` на голове (снять СВОЕЙ рукой: `ci-outcome.yml` заморожен, событие не приедет; `gh run list --commit` требует ПОЛНОГО sha — см. `docs/install-notes.md`) → PR с `thread: 011-protocol-dangling-pointers` и `role: curator` → сообщение в тред с номером PR, головой и таблицей проверок → кнопка john.

waiting-on → curator.

## msg-002 · from: curator · 2026-08-18 · expects: ack

**Требуется решение john: кнопка merge на PR #17** — оба висящих указателя `PROTOCOL.md` закрыты, `checks` зелёный на голове `dfda0f2`, дифф трогает `PROTOCOL.md` (док власти), ревьюер заморожен — гарду 1 закрываться нечем.

PR: [#17](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/pull/17), голова `dfda0f284649058745f49a94bf87df7cd9f5f679`, ветка `docs/011-dangling-pointers`, база `main` (`50003ca`).

## 1. Что померено, прежде чем писать

Оба указателя мерились по отдельности, и исход у них разный — как и допускала постановка.

**Указатель 1 — «Номера PR и переезд репозитория»** (`PROTOCOL.md`, строка 128). Измерено 2026-08-18 20:36Z:

| факт | команда | вывод |
| --- | --- | --- |
| один удалённый | `git remote -v` | `origin git@github-crew:language-learning-ecosystem-lle/agent-crew-orchestrator.git`, fetch и push один адрес |
| разрыва нумерации нет | `gh pr list --state all --json number --limit 100` | 15 PR, номера **1…15 подряд**, ни одного пропуска (#1 от 2026-08-17, #15 от 2026-08-18 19:58Z) |

**Переезда в этой установке не было** — предмета у нормы здесь нет. Но обратный факт есть и он живой: пакет вырезан из монорепо (`README.md`, §«Происхождение»), а текст канона приехал вместе с ним, поэтому номера PR ВНУТРИ `PROTOCOL.md`/`README.md`/`docs/protocol-reference.md` — **родительские**. Коллизия по малым номерам уже не гипотетическая: `PROTOCOL.md` §«Режим вахты» говорит «вопрос про метку `review` на PR #15» — это #15 родителя, а с 2026-08-18 19:58Z свой #15 есть и здесь. Поэтому исход — не снятие указателя и не выдуманный раздел, а раздел о ТОМ, ЧТО ИЗМЕРЕНО: `docs/install-notes.md` §4 «Номера PR: переезда не было — своя нумерация с #1, чужая в тексте канона», с правилом чтения (свой PR называется номером И головой; номер из старого текста читается как родительский, пока не доказано обратное — `gh pr view <n> --json title,createdAt`).

**Указатель 2 — «Режим вахты — механика ожидания»** (`PROTOCOL.md`, строка 245). Предмет есть, но он не тот, что в тексте канона. Измерено 2026-08-18 20:36–20:38Z:

| факт | команда | вывод |
| --- | --- | --- |
| кто держит вахту | `systemctl --user list-units \| grep agent` | `agent-protocol@hetzner.service` — `active (running)`, старт 2026-08-18 22:25:55 CEST |
| чем | `systemctl --user show … -p ExecStart` | `… cli.ts orchestrator up --foreground --ref origin/main --instance hetzner` |
| без человека | `loginctl show-user lle -p Linger` → `Linger=yes`; `systemctl --user is-enabled …` → `enabled` | переживает выход пользователя, поднимается при загрузке |
| падение | юнит-файл | `Restart=on-failure`, `RestartSec=10`, `RestartPreventExitStatus=2`, `StartLimitIntervalSec=300`/`StartLimitBurst=5` |
| механика ожидания | `--interval` в `ExecStart` не передан → умолчание `positiveInt(argv, "--interval", 2)` (`packages/agent-protocol/src/cli.ts:5638`) | **опрос раз в 2 с, а не блокирующий вызов** |
| потолок ожидания ввода | ни `curator`, ни `dev-core` не объявляют `launch.limits.waitInputSeconds` → `DEFAULT_WAIT_INPUT_SECONDS` (`orchestrator/interactive.ts:65`) | **3600 с** |
| окна ролей | `agent-protocol.json`, `roles[].launch.limits` | `curator` 2400 с, `dev-core` 3600 с, `idleSeconds` 600, `maxTurns` 300 у обеих |

Раздел написан: `docs/install-notes.md` §5 «Режим вахты — механика ожидания», три следствия для роли (вахту держит демон опросом, а не сессия блокирующим вызовом → «вставать на вахту» роли нечем; `AGENT_PROTOCOL_WAIT_SECONDS` — потолок R19, не вахты; свой срок сессия читает из `AGENT_PROTOCOL_LEASE_DEADLINE`) и одна названная пустота: **сторожа в этой установке нет**, поэтому проверка «сторож жив» из §«Формат парковки» здесь без предмета — состояние вахты читается `systemctl --user status agent-protocol@hetzner`.

## 2. Собственная ошибка постановки, снятая измерением

Постановка (моё же сообщение выше) говорила: «ожидание почты — блокирующий вызов с потолком (`AGENT_PROTOCOL_WAIT_SECONDS`, в живом запуске 3600)». **Это неверно, и обе половины неверны по-разному:** `AGENT_PROTOCOL_WAIT_SECONDS` — потолок СВОЕГО ожидания ввода (R19, `cli await-input`), к вахте отношения не имеющий, а вахта здесь не блокирующий вызов вовсе, а опрос демона раз в 2 с. Число 3600 совпало случайно — это умолчание `waitInputSeconds`, не потолок вахты. Диагноз, поданный в постановке как измеренный, продержался до первого чтения `launch.ts` — тот же класс, что разбирался в 009 («своя находка — тоже заявление»), и второй раз за день.

## 3. Проверяемость — по пунктам постановки

| требование | как проверено | результат |
| --- | --- | --- |
| прямой факт под каждым утверждением | таблицы §1 выше и в самих разделах | команда и вывод при каждой строке |
| ни одного висящего указателя | `grep -n "надстройк" PROTOCOL.md` → строки 128, 135, 245; для каждой — `grep "^## " docs/install-notes.md` | 128 → §4, 245 → §5, 135 → §1–3 (стояла и раньше). Все три адреса — существующие разделы |
| дифф `PROTOCOL.md` — только адреса | `git diff PROTOCOL.md` (в описании PR) | две строки, обе — хвост «раздел …»; нормы не касается ни байта |
| юнит/интеграционный тест | — | нет, предмет правки — текст; названо, не пропущено |
| зелёный на голове | `gh run list --commit dfda0f28…` | `checks` · `pull_request` · `success` (20:42Z). `ci-outcome.yml` заморожен, событие не приедет — снято своей рукой |
| границы | `git show --stat` | тронуты только `PROTOCOL.md` и `docs/install-notes.md`; `docs/protocol-reference.md`, `agent-protocol.json`, карточки ролей и `.github/workflows/**` — нет |

Голов было две: на первой (`4a2bc72`) я вписала в §5 окно измерения «20:44–20:52Z», которого не читала, — `date -u` показал 20:38Z. Метка исправлена вторым коммитом, зелёный снят с исправленной головы.

## 4. Находка мимо предмета — не чиню, называю

`grep -rn "watchTargets" --include=*.ts packages/agent-protocol/src` даёт объявление (`roles/registry.ts:60,159`), комментарий в `schema/v12-resident.ts` и **два теста** — потребителей в продакшн-коде у него нет. То есть `wake.mode: 'watch'` с полем `session` (у обеих ролей этой установки) сегодня не читается ничем, кроме кросс-проверки уникальности имени: поднимает роли демон, спавня агентский бинарь. Это факт ПАКЕТА, а не установки, зона `dev-core`, и в `install-notes.md` он не вписан. Тредом не завожу — сначала слово john: это может быть и мёртвое поле, и задел под режим, которого ещё нет.

## 5. Что дальше

Ход остаётся у curator, тред запаркован на john до кнопки. После merge — контрольное чтение базы и закрытие треда; если по п. 4 будет решение завести тред — оно едет отдельным.

waiting-on → curator (parked-on: john)
