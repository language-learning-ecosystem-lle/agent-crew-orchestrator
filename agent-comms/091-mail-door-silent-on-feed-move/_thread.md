# Дверь new-message молчит о письмах, легших под пишущим

participants: curator, dev-core, john · status: open

## msg-001 · from: curator · 2026-09-03 · expects: ack

# Дверь почты молчит о письмах, легших под пишущим, — чинить нотой или это норма? Требуется решение john

**Требуется решение john. Вопрос первой строкой:** `new-message --write` сегодня ничего не говорит
отправителю, когда в том же треде под его собственным последним письмом уже лежат письма, написанные
ПОСЛЕ него. Чинить это **нотой** двери («под твоим прошлым письмом легло N сообщений от `<роль>`,
последнее `<штамп>`») — или оставить как есть?

Происхождение (первой строкой, как положено связанному прогону): тред `056-shared-tmp-mechanism`,
письмо dev-core от 2026-09-03 (раздел «Находка в теле самого предмета этого треда»), в котором он
спросил, где дом находке. **Дом выбирала curator — этот тред и есть выбор.** Работ по нему сейчас нет
ни у одной роли: он стоит на слове john.

## Полевой первоисточник — наш собственный такт, а не гипотеза

| момент | что |
| --- | --- |
| 2026-09-03 09:29:30Z | curator читает `056` в начале своего такта |
| 2026-09-03 09:31:42Z | dev-core пишет в `056` письмо о срезанном теге |
| 2026-09-03 09:36Z | curator отправляет письмо «письма о теге в треде нет» — **не перечитав ленту** |

Разошлись не факты, а письма: curator написала в ленту заявление о ленте, которое было ложным уже
две минуты. Гонки push'ей не было (семь минут разбега), поэтому штатная нота
`(after N attempts: the feed moved underneath)` промолчала по построению — ей нечего было сказать.

## Почему это вопрос к тебе, а не «просто почини»

Правило «перечитай ленту перед отправкой» в текстах уже есть, и оно **не сработало** — это второй
случай того же класса, что и весь `056`: призыв не держит, держит предусловие у действия. Но
предлагаемая нота — это НОВАЯ строка на выходе двери, а не возврат поведения к объявленному, и я не
уверена, что она попадает в класс «полевой измеренный дефект, новой нормы не вводит». По правилу
последней строки не уверена — значит к тебе.

## Цена, померенная кодом (я перечитала сама, а не пересказываю доклад)

- примитив есть и уже используется: `unreadFor` — `packages/agent-protocol/src/thread/unread.ts:63`;
  это то же вычисление, которым `thread show --for <role>` печатает «unread for …: N of M»;
- лишнего чтения диска не появится: `new-message` и так парсит каталог сообщений на каждой попытке
  доставки, чтобы поставить штамп строго после последнего;
- нынешняя нота стоит в `cli.ts:3842` за условием `attempts > 1`, то есть ловит только гонку push'ей.
  Семиминутный разбег ей не виден по построению — проверено чтением строки, а не по памяти.

## Что этим вопросом НЕ предлагается

**Отказ вместо ноты не предлагается никем.** Бывает законное «пишу, не читая» (доклад в чужой тред,
письмо машины), а дверь, которая мешает писать, хуже молчащей. Спрашивается ровно нота.

## Чей ход

**waiting-on → curator** (john в это поле не принимается), парк — **john**, `expects: ack`. Пока слова
нет, работ по треду нет ни у кого: постановку dev-core я под этот предмет не пишу.
