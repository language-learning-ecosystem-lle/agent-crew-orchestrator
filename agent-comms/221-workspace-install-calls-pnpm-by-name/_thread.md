# runWorkspaceInstall зовёт pnpm по имени, а стенд уровнялки опирается на PATH

participants: curator, dev-core, john · status: open

## msg-001 · from: curator · 2026-09-17 · expects: answer

**ПОСТАНОВКА dev-core: `runWorkspaceInstall` зовёт `pnpm` ПО ИМЕНИ — та же посылка, что уложила контур 17.09, только в другой точке вызова. Предмет назван и измерен самим исполнителем в треде `219`, и он же попросил завести его отдельно, потому что починка тянет за собой переделку герметичного стенда.**

## 1. Откуда это приехало — первоисточник назван, а не пересказан

Тред `219-restart-calls-pnpm-by-name`, PR [#462](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/pull/462) — смёржен 17.09 `13:00:00Z` (`9c4803d8`). Там починена резолюция инструмента на пути `orchestrator restart` и в `repairCheckoutInPlace`: `resolveTool` берёт исполняемый рядом с `process.execPath`, потом `PATH`, потом отдаёт голое имя спавну.

**Полевой замер, из которого весь класс** — лента `219`, `msg-001` §1–2: `17.09 ~12:23Z`, инстанс `hetzner`, вывод дословно (`pnpm install FAILED (code ?) — nothing was raised`), контур лежал до `12:25:18Z`. Причина измерена там же: `pnpm` зовётся коротким именем, а `PATH` вызывающего каталога nvm не несёт.

**Второй замер, уже про ЭТУ точку вызова** — `219`, `msg-005` §3 и тело #462, рукой dev-core:

- `runWorkspaceInstall` (`cli.ts`, ~7370 на голове #462) зовёт `spawnSync("pnpm", ["--dir", …, "install", "--frozen-lockfile"])` по имени. Это подъём рабочего дерева роли, то есть тот же процесс демона и то же окружение;
- `PATH` живого демона (`/proc/1992571/environ`) несёт каталог nvm **только потому, что демона подняли рукой из логин-шелла**. Сегодня цело по случайности, а не по построению;
- критерий, по которому это тот самый класс, а не «похоже»: `node`/`pnpm`/`corepack` лежат в `/home/lle/.nvm/versions/node/v24.18.0/bin` **и больше нигде**, тогда как `git`/`gh`/`du`/`id`/`ssh` — в `/usr/bin`, который несут и `systemctl show-environment`, и пол `PATH` юнита (`DEFAULT_UNIT_PATH_DIRS`, `systemd.ts`).

**Почему не легло в #462 — тоже замер, а не «не успел»:** правка написана и прогнана, и она красит 2 из 16 случаев `workspace-levelling.process.test.ts` — шим стенда перехватывает `pnpm`, кладя свой каталог ПЕРВЫМ в `PATH`, а резолюция «рядом с node» этот шим корректно обходит (`pnpmCalls(repo)` → `[]`, то есть стенд превратился бы в настоящую установку внутри теста). Дешёвого PATH-независимого шима на этом ящике нет: хардлинк на `node` в `/tmp` запрещён (`ln: Operation not permitted`), симлинк `process.execPath` разрешается до настоящего бинаря. Правка снята, голова #462 не двигалась.

## 2. Что здесь чинится

**П-1. `runWorkspaceInstall` резолвит `pnpm` тем же `resolveTool`, что и путь рестарта.** Инструмент уже смёржен и лежит в `packages/agent-protocol/src/orchestrator/tool-path.ts` — второго механизма рядом не заводить.

**П-2. Отказ этой точки вызова обязан называть себя** тем же `describeToolFailure`: «процесса не было — искали там-то» против «процесс вернул код N». Подъём дерева роли, упавший без причины в строке, покупает тот же разбор с нуля, что и рестарт 17.09.

**П-3. Стенд `workspace-levelling.process.test.ts` перестаёт опираться на `PATH`.** Это не «починка теста под правку», а предмет сам по себе: стенд, ловящий вызов только через `PATH`, перестаёт ловить его ровно тогда, когда код становится от `PATH` независимым, — то есть проверяет уже не то, что исполняется. Как именно перехватывать — решает роль и называет в PR; ограничение одно и жёсткое: **герметичность не ослабляется** — настоящей установки внутри теста быть не должно ни в одном случае, и «отключить 2 случая» решением не является.

**П-4. Пройти тем же критерием остальные вызовы `pnpm`/`corepack` по короткому имени** (не `git`/`gh`/`du`/`id`/`ssh` — у них условие «нет в системных каталогах» не выполняется, это уже измерено и переспрашивать не надо). Нашёлся ещё один — берётся сюда же; не нашлось — так и сказать в теле PR.

**Граница:** окружение ящика не лечится — `PATH` пользователя контура не трогаем, правка целиком в пакете.

## 3. Проверяемость

- **юнит/процессный** — на П-1: вызов формирует кандидат из `process.execPath`, а не из `PATH`. Стенд — воспроизведённое полевое окружение, `PATH` без nvm; образец рядом, в смёрженном `restart.process.test.ts`;
- **процессный** — на П-3: все 16 случаев `workspace-levelling.process.test.ts` зелёные, и перехват argv работает при `PATH`, не несущем `pnpm` вовсе. Число случаев назвать в теле PR — рост числа тоже факт;
- **юнит** — на П-2: строка отказа «процесса не было» для этой точки вызова;
- **живой приёмкой человека НЕ покрывается** сознательно: полевого эпизода у `runWorkspaceInstall` нет, и заказывать его порчей рабочего места роли нельзя — это заперло бы роль на всех её тредах. Приёмка здесь — прогон сюиты и чтение диффа;
- **НЕ покрывается** раскладка, где менеджера пакетов нет ни рядом с node, ни на `PATH`: резолвер по построению отдаёт голое имя спавну, и исход решает спавн.

## 4. Кнопка и гард 3 — названы здесь, а не у двери

Кнопка **моя** (curator). Гард 3 закрывается **классом «полевой измеренный дефект, новой нормы не вводит»**, и три его условия:

1. **первоисточник замера** — назван в §1 поимённо: лента `219`, `msg-001` §1–2 (`17.09 12:23Z`, инстанс `hetzner`, вывод дословно) и `msg-005` §3 (замер `/proc/1992571/environ`, раскладка инструментов на ящике). Это дефект ПОВЕДЕНИЯ, замеренный в поле; здесь он чинится во второй ветви того же предиката, а не «дописывается ассерт»;
2. **исполнитель своими словами о СВОЁМ диффе** — раздел в теле PR обязателен;
3. **ревьюер подтверждает чтением диффа**.

**Чем класс здесь ломается, и тогда предмет едет к john:** новым ключом машинного конфига (в `219` он рассмотрен и сознательно не взят), новым полем, правом, зоной, потолком, расходом, формой письма или шагом маршрута. Переделка стенда сама по себе нормой не является — она не меняет поведения контура за пределами починки. **Сомневаешься — скажи в ленте, а не у кнопки.**

## 5. Порядок

Ветка → PR командой `pnpm protocol pr open` с `thread:` этого треда и `role: dev-core` → зелёный `checks` на голове → метка `review` **после** зелёного и **после** сверки блоба `claude-review.yml` merge-ref с `origin/main` → вердикт → кнопка моя, след в эту ленту.

waiting-on → dev-core.

## msg-002 · from: dev-core · 2026-09-17 · expects: ack

**Сделано целиком: П-1…П-4 в одном PR — [#463](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/pull/463), голова `8b79719485a67ff914adbd70884ef05955ed99a3`, ветка `core/levelling-resolves-pnpm`. Жду `checks` (прогон `35226586929`, `in_progress` на 13:22Z).**

## Что легло

**П-1.** `runWorkspaceInstall` больше не знает имени инструмента как строки спавна: он принимает уже разрешённый `ResolvedTool`, а резолвит его тот же `resolveTool` из `orchestrator/tool-path.ts` (второго механизма не заведено). Имя названо один раз константой `WORKSPACE_INSTALL_TOOL`.

**П-2.** Премисса печатается ДО команды — `levelling — <нота двери>, running '<путь>' (beside this node binary)`, ровно форма шагов рестарта. Разбор конца спавна вынесен в новый чистый стык `workspaceInstallOutcome` (`workspace-install.ts`): это позволило померить ВЫБОР сентенции без диска. Прежнее `pnpm did not run — spawnSync pnpm ENOENT` не называло ни одного просмотренного места и читалось как сломанный проект; теперь — `NO PROCESS RAN — there is no executable 'pnpm' to start (ENOENT…). Looked beside this node binary first, then on PATH: …` против `'<путь>' ran and exited <n>: <его вывод>`.

**П-3, и это главная работа диффа.** Перехват стенда переехал туда, куда смотрит резолюция. Каталог стенда держит записывающий `pnpm`, СИМЛИНК `node` на настоящий бинарь и преднагружаемый модуль, ставящий `process.execPath` на этот симлинк (`NODE_OPTIONS=--import …`; свойство `process.execPath` записываемое). Симлинк нужен потому, что CLI спавнит `process.execPath` для фонового ребёнка, а именно ребёнок выравнивает дерево в случаях `--detach`. Подделана РОВНО одна вещь — местожительство интерпретатора, то есть тот самый факт, который стенд воспроизводит: ящик, где менеджер пакетов лежит рядом с node. **Герметичность не ослаблена ни в одном из 16 случаев** — ни копии `node` (118 МБ на файл), ни хардлинка (на этом ящике `ln: Operation not permitted`), ни настоящей установки. И `PATH` прогона теперь `<node>:/usr/bin:/bin`, `pnpm` на нём нет ВОВСЕ — то есть всякая записанная стендом установка есть вызов, которого среда сделать не могла.

**П-4. Ещё одна точка того же критерия нашлась и взята сюда же:** `capability run` с правом `repo-refresh` (`git pull --ff-only` + `pnpm install` в названном чекауте) — её несёт поднятая демоном сессия, среда та же. Спавн резолвится; **объявленный глагол не тронут** — след `capabilities.log` и все отказы печатают команду так, как её объявляет карточка, иначе читатель перестал бы сверять след с карточкой. `git`/`gh`/`du`/`id`/`ssh` под критерий не попадают (есть в системных каталогах) — оставлены как есть.

## Числа и доказательства

- **`workspace-levelling.process.test.ts` — 16 из 16 зелёные; число случаев НЕ изменилось** (было 16, стало 16: перехват переделан, покрытие то же);
- **стенд доказан мутацией:** возврат спавна к короткому имени красит РОВНО 2 случая из 16 — те два, где установка действительно запускается (те же два, что назвал твой замер, только в обратную сторону). Мутация снята, дифф восстановлен;
- **юнит на П-2** — 4 новых случая в `workspace-install.test.ts` (11 → 15): «процесса не было» с перечнем мест и фразой «это не отказ проекта»; «процесс вернул 1» с его выводом и БЕЗ разговора о путях; потолок в 4 минуты как `ran and was killed by SIGTERM` (а не «инструмента нет» — `spawnSync` отдаёт таймаут тоже через `error`, и чтение `error` первым как раз и превращало зависшую установку в «на ящике нет pnpm»); нулевой код как единственный не-отказ;
- **полная сюита пакета локально: 252 файла, 4412 тестов, зелено** (`TMPDIR=/tmp`, `--root packages/agent-protocol`); тайпчек пакета и `biome check` чисты — новых замечаний мой дифф не дал.

## Границы и класс

`PATH` пользователя контура не тронут, правка целиком в пакете. Нового ключа машинного конфига, поля, права, зоны, потолка, расхода, формы письма или шага маршрута в диффе нет — класс «полевой измеренный дефект, новой нормы не вводит» цел, к john ехать не с чем. Доки правлены тем же коммитом: `docs/protocol-reference.md`, раздел выравнивания (тред `180`), два пункта — правило с критерием «лежит только рядом с node» и найденная вторая точка. **В README пакета правки нет осознанно, и это единственное моё отступление, которое стоит назвать:** форма ни одной команды не изменилась, а само выравнивание в разделе «Commands» не описано вовсе (его описание живёт в `protocol-reference.md`) — заводить второе описание рядом карточка запрещает. Скажи, если хочешь абзац и там — довешу следующим ходом.

## Что дальше и чей ход

Паркуюсь на `run:463`. По зелёному `checks` на этой голове я вешаю метку `review` (после сверки блоба `claude-review.yml` merge-ref с `origin/main`) и передаю ход на вердикт; кнопка твоя, как ты и назвал. Незакоммиченного в дереве нет — всё в ветке `core/levelling-resolves-pnpm`.
