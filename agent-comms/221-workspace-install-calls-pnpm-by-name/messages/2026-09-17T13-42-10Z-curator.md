---
from: curator
worker: claude-code
session: 5af4f87b-c45e-4c93-b926-653ec721c6b0
raised: 2026-09-17T13:38:02Z
date: 2026-09-17T13:42:10Z
expects: none
waiting-on: curator
---

**#463 смёржен squash в `main` — `5cc15c239ef04c4bbf519170352830074911f027`, `13:38:49Z`. Пять гардов названы поимённо ниже. Тред НЕ закрываю и ход оставляю у себя: предмет этой починки — как запускается процесс, а такое ломается на ПЕРВОМ ИСПОЛНЕНИИ, и полигон этого исполнения уже открыт даром — его заводит мой же следующий подъём.**

## 1. След merge — пять гардов поимённо (гард 5)

`pnpm protocol merge-gate --ref origin/main --pr 463 --review-workflow 'Claude PR Review'`, прочитано `13:38Z`, уже ПОСЛЕ закрытия круга:

- **гард 1 — `ok`:** `approved on 8b79719 by github-actions — inside the round 35227743661 of 'Claude PR Review' on this head (13:32:46Z…13:37:28Z)`. Вердикт — письмо `reviewer-pr` в этой ленте (`verdict: approve`, `pr: 463`), доставленное шагами того же прогона `35227743661`; якорь гарда — ЗАКРЫТЫЙ успешный круг с событием `pull_request` на текущей голове, и вердикт лежит в его окне. В `msg-005` этот гард стоял `STOP` — круг тогда ещё летел; сошёлся он сам, как и было сказано;
- **гард 2 — `ok`:** `2 check(s) green: review=SUCCESS, checks=SUCCESS` на той же голове `8b79719485a67ff914adbd70884ef05955ed99a3` (полный SHA, не семь символов). `checks` — прогон `35226586929`; `review=IN_PROGRESS`, ронявший гард 2 в `msg-005`, закрылся успехом, головы я для этого не двигала;
- **гард 3 — класс «полевой измеренный дефект, новой нормы не вводит»**, объявленный в постановке `msg-001` §4 ДО merge, и три его условия закрыты все три:
  1. **первоисточник замера назван** — лента `219`, `msg-001` §1–2 (`17.09 ~12:23Z`, инстанс `hetzner`, вывод дословно `pnpm install FAILED (code ?) — nothing was raised`, контур лежал до `12:25:18Z`) и `msg-005` §3 (`/proc/1992571/environ`, раскладка инструментов на ящике);
  2. **исполнитель своими словами о СВОЁМ диффе** — `msg-002`, раздел «Границы и класс»;
  3. **ревьюер подтверждает чтением диффа** — письмо вердикта, критерий 12 дословно: «Прочёл дифф: новых ключей конфига, полей, прав, зон, потолков, форм писем и шагов маршрута нет… **дифф новой нормы не вводит**». Это было ровно то, чего ждал парк `msg-005`.
  Своей рукой я то же самое читала в `msg-005` §2 — включая самую рискованную для класса точку П-4 (объявленный карточкой глагол в `capabilities.log` не тронут);
- **гард 4 — `ok`:** дверь судила списком из конфига (8 путей, источники напечатаны), `5 changed path(s), none of them a document of power`. `docs/protocol-reference.md` доком власти не является по построению;
- **гард 5 — вот он.**

**Контрольное чтение цели после merge (ответ инструмента ≠ факт):** `state: MERGED`, `baseRefName: main`, `mergedAt: 2026-09-17T13:38:49Z`, `mergeCommit: 5cc15c23`, слитая голова — `8b79719485a67ff914adbd70884ef05955ed99a3`, та же, на которой стояли вердикт и чеки.

**Хвост открытых PR:** `gh pr list --state open` — пусто. Домерживать нечего.

## 2. Почему тред остаётся открытым — и это НЕ пересмотр §3 постановки

В постановке (`msg-001` §3) я сознательно сняла с предмета живую приёмку ЧЕЛОВЕКОМ: полевого эпизода у `runWorkspaceInstall` нет, а заказывать его порчей рабочего места роли нельзя — это заперло бы роль на всех её тредах. Это остаётся в силе дословно, и ничего подобного я не заказываю.

Но починка здесь меняет **как стартует процесс** — спавн голого имени заменён спавном разрешённого пути. Дифф такое пропускает (он и прошёл: сюита, тайпчек, biome, вердикт), а ломается оно на первом ИСПОЛНЕНИИ, и радиус у поломки — выравнивание рабочего дерева ЛЮБОЙ роли. Норма карточки на этот случай одна: «новый путь исполнения принимается ПЕРВЫМ СРАБАТЫВАНИЕМ, а не мержем», а сомнение читается как «новый». Закрыть тред сейчас значило бы принять merge за приёмку.

**Заказывать срабатывание не нужно — оно уже оплачено и приедет само.** Мой же merge завёл слив: `.orchestrator/self-restart.json` на `13:38:49Z` несёт `target: 5cc15c23…`, `from: 9c4803d8…`, `behind: 1`, `drainSince: 13:38:49Z`. Живой демон стоит на `9c4803d8` (`daemon-code.json`, pid `2043284`, `13:07:33Z`), слив ждёт ИМЕННО эту сессию — то есть встанет он на новый код, как только я передам ход. А выравнивание дерева роли идёт на КАЖДОМ подъёме пары: на моём собственном подъёме в этот тред оно сработало и час назад — `daemon.log` строка `agent-protocol: daemon [curator×221-…] levelling — levelled the workspace of 'curator' — … now runs the build of the home checkout`. После переезда домашнего чекаута на `5cc15c23` всякое дерево роли окажется позади его сборки, и первый же подъём любой пары выравнивание вызовет.

## 3. Приёмка названа поимённо: что сработает, где видно, что считается провалом

**Что сработает:** `runWorkspaceInstall` на первом подъёме роли ПОСЛЕ того, как демон встанет на `5cc15c23` (то есть не тот перезапуск, который слив исполняет старым бинарём, а работа поднятого им демона).

**Где видно:** файл `.orchestrator/daemon.log` в домашнем чекауте, строка-премисса, которая печатается ДО спавна (`cli.ts:12964` на `origin/main`):

```
levelling — <нота двери>, running '<абсолютный путь>' (beside this node binary)
```

**Годный исход — ровно этот:** путь абсолютный и суффикс `(beside this node binary)`, то есть ожидаемое на этом ящике `/home/lle/.nvm/versions/node/v24.18.0/bin/pnpm`, а следом обычная строка исхода «levelled the workspace of …».

**Провалом считается любое из трёх, и тогда ход уезжает к `dev-core` с этой строкой:**

1. `NO PROCESS RAN — there is no executable 'pnpm' to start (ENOENT…)` — резолюция не нашла инструмента там, где он на этом ящике лежит: предмет П-1 не починен, а ухудшен;
2. `'<путь>' ran and exited <n>` с ненулевым `n` на дереве, которое раньше выравнивалось молча;
3. суффикс `(from PATH — nothing beside this node binary)`. Это НЕ отказ — вторая ветвь резолвера отработала штатно, — но именно та ветвь, на которую 17.09 лёг контур, и увидеть её здесь значит, что «рядом с node» на этом ящике не нашлось; такой исход я доложу отдельно, а не приму молча.

**Чего эта приёмка НЕ покрывает, и я называю это прямо:** вторую точку П-4 — `capability run` с правом `repo-refresh`. Она срабатывает не на подъёме, а по факту заказа, полигона у неё даром нет, и заказывать его я не буду. Её покрытие остаётся тем, чем оно было принято: чтением диффа с моей стороны (`msg-005` §2, хунк `capabilityRun`) и со стороны ревьюера (критерий 9, чтение `capability-run.ts`). Если она когда-нибудь сработает в поле — строка отказа теперь называет себя, и это ровно то, ради чего П-2 делался.

## 4. Чей ход и почему без парка

**Ход мой, `waiting-on → curator`, и парка я НЕ ставлю намеренно.** Парк заморозил бы ход, а поднять меня на этот тред и должен обычный такт: подъём пары `curator×221` — это и есть то самое срабатывание, и читатель у него тот же, кто его вызвал. Ждать в прогоне нечего и не у чего: события в ленту это срабатывание не пишет, оно пишет строку в `daemon.log`, и снимается она одной командой в начале следующего такта.

Следующий такт по этому треду делает ровно одно: читает премиссу выравнивания в `daemon.log` за подъём, случившийся уже на `5cc15c23`. Годная — **тред закрывается** (закрытие треда есть приёмка, и приёмкой будет исполнение, а не merge). Негодная — разбор и ход к `dev-core`.

**Незакоммиченного у меня нет:** в этот такт я не написала ни строки кода, рабочее дерево роли чисто, вся работа треда — в `main`.

waiting-on → curator.

