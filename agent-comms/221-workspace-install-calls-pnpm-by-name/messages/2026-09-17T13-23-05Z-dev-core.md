---
from: dev-core
worker: claude-code
session: e0dc6045-e2a2-43b8-8e8f-802b84f487d2
raised: 2026-09-17T13:07:42Z
date: 2026-09-17T13:23:05Z
expects: ack
waiting-on: dev-core
parked-on: run:463
---

**Сделано целиком: П-1…П-4 в одном PR — [#463](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/pull/463), голова `8b79719485a67ff914adbd70884ef05955ed99a3`, ветка `core/levelling-resolves-pnpm`. Жду `checks` (прогон `35226586929`, `in_progress` на 13:22Z).**

## Что легло

**П-1.** `runWorkspaceInstall` больше не знает имени инструмента как строки спавна: он принимает уже разрешённый `ResolvedTool`, а резолвит его тот же `resolveTool` из `orchestrator/tool-path.ts` (второго механизма не заведено). Имя названо один раз константой `WORKSPACE_INSTALL_TOOL`.

**П-2.** Премисса печатается ДО команды — `levelling — <нота двери>, running '<путь>' (beside this node binary)`, ровно форма шагов рестарта. Разбор конца спавна вынесен в новый чистый стык `workspaceInstallOutcome` (`workspace-install.ts`): это позволило померить ВЫБОР сентенции без диска. Прежнее `pnpm did not run — spawnSync pnpm ENOENT` не называло ни одного просмотренного места и читалось как сломанный проект; теперь — `NO PROCESS RAN — there is no executable 'pnpm' to start (ENOENT…). Looked beside this node binary first, then on PATH: …` против `'<путь>' ran and exited <n>: <его вывод>`.

**П-3, и это главная работа диффа.** Перехват стенда переехал туда, куда смотрит резолюция. Каталог стенда держит записывающий `pnpm`, СИМЛИНК `node` на настоящий бинарь и преднагружаемый модуль, ставящий `process.execPath` на этот симлинк (`NODE_OPTIONS=--import …`; свойство `process.execPath` записываемое). Симлинк нужен потому, что CLI спавнит `process.execPath` для фонового ребёнка, а именно ребёнок выравнивает дерево в случаях `--detach`. Подделана РОВНО одна вещь — местожительство интерпретатора, то есть тот самый факт, который стенд воспроизводит: ящик, где менеджер пакетов лежит рядом с node. **Герметичность не ослаблена ни в одном из 16 случаев** — ни копии `node` (118 МБ на файл), ни хардлинка (на этом ящике `ln: Operation not permitted`), ни настоящей установки. И `PATH` прогона теперь `<node>:/usr/bin:/bin`, `pnpm` на нём нет ВОВСЕ — то есть всякая записанная стендом установка есть вызов, которого среда сделать не могла.

**П-4. Ещё одна точка того же критерия нашлась и взята сюда же:** `capability run` с правом `repo-refresh` (`git pull --ff-only` + `pnpm install` в названном чекауте) — её несёт поднятая демоном сессия, среда та же. Спавн резолвится; **объявленный глагол не тронут** — след `capabilities.log` и все отказы печатают команду так, как её объявляет карточка, иначе читатель перестал бы сверять след с карточкой. `git`/`gh`/`du`/`id`/`ssh` под критерий не попадают (есть в системных каталогах) — оставлены как есть.

## Числа и доказательства

- **`workspace-levelling.process.test.ts` — 16 из 16 зелёные; число случаев НЕ изменилось** (было 16, стало 16: перехват переделан, покрытие то же);
- **стенд доказан мутацией:** возврат спавна к короткому имени красит РОВНО 2 случая из 16 — те два, где установка действительно запускается (те же два, что назвал твой замер, только в обратную сторону). Мутация снята, дифф восстановлен;
- **юнит на П-2** — 4 новых случая в `workspace-install.test.ts` (11 → 15): «процесса не было» с перечнем мест и фразой «это не отказ проекта»; «процесс вернул 1» с его выводом и БЕЗ разговора о путях; потолок в 4 минуты как `ran and was killed by SIGTERM` (а не «инструмента нет» — `spawnSync` отдаёт таймаут тоже через `error`, и чтение `error` первым как раз и превращало зависшую установку в «на ящике нет pnpm»); нулевой код как единственный не-отказ;
- **полная сюита пакета локально: 252 файла, 4412 тестов, зелено** (`TMPDIR=/tmp`, `--root packages/agent-protocol`); тайпчек пакета и `biome check` чисты — новых замечаний мой дифф не дал.

## Границы и класс

`PATH` пользователя контура не тронут, правка целиком в пакете. Нового ключа машинного конфига, поля, права, зоны, потолка, расхода, формы письма или шага маршрута в диффе нет — класс «полевой измеренный дефект, новой нормы не вводит» цел, к john ехать не с чем. Доки правлены тем же коммитом: `docs/protocol-reference.md`, раздел выравнивания (тред `180`), два пункта — правило с критерием «лежит только рядом с node» и найденная вторая точка. **В README пакета правки нет осознанно, и это единственное моё отступление, которое стоит назвать:** форма ни одной команды не изменилась, а само выравнивание в разделе «Commands» не описано вовсе (его описание живёт в `protocol-reference.md`) — заводить второе описание рядом карточка запрещает. Скажи, если хочешь абзац и там — довешу следующим ходом.

## Что дальше и чей ход

Паркуюсь на `run:463`. По зелёному `checks` на этой голове я вешаю метку `review` (после сверки блоба `claude-review.yml` merge-ref с `origin/main`) и передаю ход на вердикт; кнопка твоя, как ты и назвал. Незакоммиченного в дереве нет — всё в ветке `core/levelling-resolves-pnpm`.

