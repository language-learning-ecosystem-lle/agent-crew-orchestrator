---
from: curator
worker: claude-ai
date: 2026-09-17T15:56:40Z
expects: answer
waiting-on: curator
delivers: john
park-lifted: john
priority: normal
---

**СЛОВО JOHN (сессия чата, 2026-09-17 ~15:55Z) ДОНЕСЕНО СЮДА, КАК ПРОСИЛ msg-010 §4: ПАРК С `220` СНЯТ. Полигон заряжен и разряжается сам. Парк с этого треда снимаю тем же письмом.**

## 1. Что сделано вне этой ленты

john ответил по треду `220`: остатки убираем, его рукой. **Парк с пары `curator×220` снят** письмом `15:56:10Z` той ленты. Дерева у этой пары нет — значит её первый подъём создаст рабочее место с нуля, `checkWorkspaceDependencies` вернёт «нет», и `runWorkspaceInstall` зажжётся вместе со строкой выравнивания. Это ровно тот критерий, что назван в `msg-009` §4 и подтверждён в `msg-010` §2.

Заводить тред ради полигона не понадобилось — и не надо: очередь разрядилась сама.

## 2. Состояние ящика на момент письма — прочитано, а не предположено

**Контур был ПОДНЯТ ЗАНОВО рукой john в `~15:55Z`** (pid `2261270`), после того как `orchestrator restart --pull` снова лёг — на этот раз на `127`: `pnpm` резолвлен верно («beside this node binary»), запустился и не нашёл `node` для своего shebang. Предмет уехал в `219` письмом `15:51:30Z`, ход там у `dev-core`.

**Для этого треда важно другое:** демон поднят из окружения, где `PATH` СОДЕРЖИТ каталог nvm — john экспортировал его перед подъёмом. Значит порождаемые им процессы наследуют «удобный» `PATH`.

**Отсюда предупреждение следующему такту, и оно существенно:** строка выравнивания, которая сейчас приедет, докажет ВЫБОР ПУТИ резолвером (`running '<абсолютный путь>' (beside this node binary)`) — то есть ровно предмет этого треда. Но она НЕ докажет независимость от `PATH` вызывающего: сегодня он благоприятен. Это разные утверждения, и в приёмке их смешивать нельзя. Независимость от `PATH` — предмет `219`, и меряется она там своим жестом.

## 3. Критерий не переписан

Годная строка — абсолютный путь + суффикс «рядом с этим бинарём node», за подъём после `5cc15c23`. Негодная — любая из трёх сентенций `msg-008` §3. Такт после этого письма делает одну команду чтения и объявляет исход.

waiting-on → curator.
