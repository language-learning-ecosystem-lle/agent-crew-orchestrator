---
from: curator
worker: claude-code
session: 13aa4a16-2cb2-483b-8083-b070b9884fae
raised: 2026-09-17T13:45:15Z
date: 2026-09-17T13:50:47Z
expects: none
waiting-on: curator
---

**Полигон НЕ приехал, и это измерено, а не предположено: на моём подъёме в 13:45 на `5cc15c23` строки `levelling — …` в `daemon.log` НЕТ ВОВСЕ.** Провалом это не является — ни одна из трёх негодных сентенций §3 `msg-008` не напечаталась, — но и приёмкой не является тоже: срабатывания не было. Ошибочной оказалась моя собственная посылка в `msg-008` §2, и я называю её прямо.

## 1. Что именно печаталось на подъёме на новом коде

Демон встал на `5cc15c23` в `13:45:05Z` (`daemon-code.json`, pid `2178067`; `self-restart.json` — `went: true`, `attempts: 1`, `at: 13:44:18Z`). Пара `curator×221` поднята ЭТИМ демоном: `continuation — fresh`, `workspace — … moving to origin/main 5cc15c23`, `identity`, `ceilings`, `agent` — и между ними **ни одной строки `levelling —`**. Ни премиссы, ни исхода, ни даже `levelling — stands aside: …`.

## 2. Почему — и в чём была неверна моя посылка

В `msg-008` §2 я написала: «после переезда домашнего чекаута на `5cc15c23` всякое дерево роли окажется позади его сборки». **Это неверно.** Условие выравнивания — `mayLevel(staleBuild !== undefined || !dependencies.installed)`, и обе половины на этом ящике читаются так:

- **сравнение сборок здесь инертно ПО ПОСТРОЕНИЮ.** `checkWorkspacePackage` (`orchestrator/workspace-package.ts:155`) сравнивает **ВЕРСИЮ установленного пакета** в дереве роли с версией в домашнем чекауте и возвращает `{ok:true}` без ноты, когда `reference === undefined`, — дословный комментарий двери: «a contour that runs the package from its own source has nothing installed to compare». Наш контур ровно такой: `node_modules/agent-protocol` нет ни в одном дереве, включая домашнее. Значит `staleBuild` не выставляется НИКОГДА, и коммит `main` к этому вопросу не относится вовсе: «сборка» опознаётся версией пакета, а не головой;
- **остаётся одна половина — `checkWorkspaceDependencies`**, то есть наличие install-корней в дереве пары. Проверила все 18 деревьев `.worktrees/**` — `node_modules` есть у КАЖДОГО, включая моё.

Отсюда и молчание без `stands aside`: при `needed: false` план не даёт `why`, печатать двери нечего. То есть **выравнивание на этом ящике зажигает ровно одно — ПЕРВЫЙ подъём пары, чьё дерево создано заново** (новый тред; либо дерево, взятое уборкой и пересозданное). Мой собственный подъём час назад (`msg-008`, строка `levelling — levelled the workspace of 'curator'`) был именно таким — первым на свежем дереве этой пары, и второй раз он не повторится.

## 3. Что из диффа #463 в поле ВСЁ-ТАКИ сработало, а что нет

Половина механизма померена в поле на этом ящике даром — и это стоит записать, потому что она общая:

```
13:44:55Z  self-restart — git pull --ff-only in '…', running '/usr/bin/git' (from PATH — nothing beside this node binary)
13:44:55Z  self-restart — pnpm install in '…', running '/home/lle/.nvm/versions/node/v24.18.0/bin/pnpm' (beside this node binary)
```

Это шаги САМОПЕРЕЗАПУСКА, то есть починка треда `219`, исполненная СТАРЫМ бинарём (`9c4803d8`), — предметом #463 они не являются. Но резолвер у них один и тот же: П-1 `msg-002` говорит дословно, что `runWorkspaceInstall` резолвит «тот же `resolveTool` из `orchestrator/tool-path.ts` (второго механизма не заведено)». Значит на ЭТОМ ящике для ИМЕННО `pnpm` резолюция уже отработала в поле и выбрала `/home/lle/.nvm/versions/node/v24.18.0/bin/pnpm` — ровно тот путь, который §3 `msg-008` назвала годным исходом.

**Не померено ровно одно — место вызова:** `runWorkspaceInstall` в ветви `levelling.install`, то есть премисса `levelling — <нота>, running '<путь>'` и разбор конца спавна. Сомнение читается как «новый», поэтому общей половиной я предмет не закрываю.

## 4. Тред остаётся открытым; приёмка переписана под измеренный критерий

Критерий §3 `msg-008` («первый подъём роли после переезда демона») **заменяю измеренным**, остальное в нём остаётся дословно:

- **что сработает:** `runWorkspaceInstall` на первом подъёме ЛЮБОЙ пары, чьё рабочее дерево создано заново, — а это первый подъём любого НОВОГО треда на этом ящике;
- **где видно:** `.orchestrator/daemon.log`, строка-премисса `levelling — <нота двери>, running '<абсолютный путь>' (beside this node binary)` перед строкой исхода;
- **годный исход и три негодных** — как в `msg-008` §3, без изменений;
- **чего приёмка не покрывает** — `capability run` с правом `repo-refresh`, как и было названо.

**Честная цена, которую надо назвать вслух:** в очереди этого ящика сейчас 4 треда, три из них запаркованы на john, и единственная поднимаемая пара — `curator×221`, то есть я. Нового дерева ни у кого не создаётся, и пока в очередь не придёт НОВЫЙ тред, полигон не приедет сам — а тред без парка будет поднимать меня каждый такт на одну команду чтения. Заказывать срабатывание я по-прежнему не буду: единственный способ — снести `node_modules` своего дерева, а без них не стартует и CLI почты, то есть роль запрётся на всех тредах. Это ровно то, что `msg-001` §3 запретила, и запрет в силе.

**Поэтому следующий такт по этому треду делает ОДНУ команду:** грепает `daemon.log` на `levelling — .* running '` за подъёмы после `5cc15c23`. Есть годная строка — тред закрывается (приёмка = исполнение). Есть негодная — разбор и ход к `dev-core`. Нет строки — такт короткий, и он честно стоит того, что стоит.

## 5. Чей ход

Ход мой, `waiting-on → curator`, парка нет намеренно — поднять меня на этот тред должен обычный такт, и читатель у срабатывания тот же, кто его дождётся. Незакоммиченного нет: в этот такт я не написала ни строки кода, рабочее дерево роли чисто, вся работа треда — в `main` (`5cc15c239`).

