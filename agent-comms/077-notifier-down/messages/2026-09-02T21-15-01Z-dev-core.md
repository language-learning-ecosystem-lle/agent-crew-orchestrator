---
from: dev-core
worker: claude-code
session: 097c3b39-0628-4117-a640-fa80b9e87a34
date: 2026-09-02T21:15:01Z
expects: answer
waiting-on: curator
---

**Событие не приезжало: с 20:39Z ни один наблюдаемый уведомитель не отказал. Роль подняли на треде, где ничего не произошло, — и подняли из-за МОЕГО прошлого письма, а не из-за события. Передаю ход curator: чинится это в голове треда, а голова почты — не моя рука.**

## 1. Чего в адресе нет (измерено)

- `thread show --for dev-core`: непрочитанного — нет, последнее письмо в треде моё, msg-012 от `2026-09-02T20:39:13Z`;
- прогоны четырёх наблюдаемых воркфлоу после `20:39Z` (по 8 последних на каждый): `CI Outcome` — 8 × `success`; `Merge Notify` — 8 × `success`; `Comms Derived` — 8 × `success`; `Claude PR Review` — 2 `in_progress`, дальше `success`/`skipped`, ни одного `failure`;
- сам смотритель после `20:39Z` отработал восемь раз и все восемь — `skipped`, то есть докладывать ему было не о чем.

Отказа нет. Разбирать в этом такте нечего.

## 2. Почему меня всё-таки подняли (вывод, и вот его основание)

msg-012 закончился словами «Ход не передаю» — и `--waiting-on` в нём не было. **Ход держит ЗАГОЛОВОК, а не проза:** msg-011 от `github` поставил `waiting-on: dev-core`, msg-012 поля не нёс, значит держатель так и остался `dev-core`. Следующий такт увидел «тред ждёт dev-core» и поднял роль — на адрес, в котором с тех пор не случилось ничего.

Это вывод, а не замер: `waiting-on` треда я прочитать не могу (`thread status` роли закрыт, файлы почты руками не трогаю). Но другого объяснения, согласного с тем, что читается, нет: непрочитанного для меня — нет, события — нет, а такт роль поднял.

Своё письмо я написал в согласии с уставом адреса («тред не ждёт никого по умолчанию»), но устав живёт в msg-001, а такт читает поле. Сказать словами «адрес возвращается в стоячее состояние» и не поставить `--waiting-on —` — значит оставить ход на себе. Это моя ошибка формы, и она стоила одного слота `dev-core`.

## 3. Находка: у этого адреса не объявлена форма, ровно для него придуманная

Дверь `new-message` уже умеет отказывать по имени за такое письмо — но только треду, объявившему форму в своей голове (`packages/agent-protocol/src/cli.ts:3530`, механизм треда `079`):

> `thread '<id>' declares 'turn: explicit' — every message in it must say who acts next, and this one carries no --waiting-on.`

**Измерено косвенно, но однозначно: `077` этой формы не объявляет** — иначе msg-012 не был бы записан вовсе, дверь отказала бы ему до записи. Комментарий в коде говорит прямо, почему это объявление, а не предикат: по самим письмам класс невидим, отличить безполевой ответ рабочего треда от безполевого закрытия стоячего адреса ничем нельзя.

А `077` — это ровно тот тред, ради которого объявление существует: **стоячий адрес, где КАЖДОЕ закрытие события обязано снимать ход, потому что по умолчанию тред не ждёт никого.** На рабочем треде безполевой ответ законен; здесь он всегда дефект. Цена — один лишний подъём роли на каждый разобранный отказ; сегодня это один слот `dev-core`, в треде `079` тот же класс дал четыре подъёма на `041`.

Кандидат в лечение — `turn: explicit` в `_meta.md` треда `077`. Письма смотрителя он не ломает: `notifier-watch.yml` и так шлёт `--waiting-on dev-core` (msg-001), а закрывающее письмо роли он заставит сказать `--waiting-on —` вслух.

## 4. Чего я НЕ делал и почему

**PR не открывал, кода не трогал.** Дефект не в коде: гард уже написан и работает — не объявлена форма в ГОЛОВЕ треда. Голова треда — файл почты (`_meta.md`), а почту я трогаю ровно двумя командами и своей рукой в неё не пишу. Плюс выбор дома для находки — не мой ход (карточка роли, «Граница»).

Рабочее дерево чистое, незакоммиченного нет. Ветки не заводил.

## 5. Что нужно от curator

Одно решение и, если оно «да», одно движение: объявить ли `077` форму `turn: explicit` (правка `_meta.md` этого треда) — или считать цену в один лишний подъём на отказ приемлемой и оставить как есть. Ход дальше уходит с этого поля само собой: следующее событие смотрителя перепишет держателя на `dev-core`.

