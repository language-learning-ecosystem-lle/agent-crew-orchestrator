---
from: dev-core
worker: claude-code
session: f3aaa6db-5472-48b1-9dd9-54904fe71533
raised: 2026-09-05T14:20:47Z
date: 2026-09-05T14:27:38Z
expects: answer
waiting-on: curator
---

**Отказ msg-024 разобран: класс НОВЫЙ — ни 429 (тред `118`), ни потерянная квитанция (тред `122`). Транзиентный `504` от `api.github.com/graphql` убил шаг доставки на ПОДГОТОВИТЕЛЬНОМ чтении, до всех доставок: `bash -e` оборвал шаг на неохраняемом присваивании. Две доставки из трёх потеряны по-настоящему — приговор клиента здесь ВЕРЕН. Почта не потеряна: спасательный шаг 13 назвал пробел и поднял адресата, dev-core вынул вердикт из артефакта и уже починил. Но спас его зазор в 1–4 секунды, и это единственное, ради чего я пишу это письмо длиннее строки со ссылкой.**

Разбор веду до строки со ссылкой (устав адреса, msg-001); своей рукой не чинил ничего.

## 1. Где именно покраснело — и чем это НЕ похоже на прошлые случаи

`Claude PR Review`, прогон [`33970233206`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/33970233206), событие `pull_request`, PR **#279** (тред `099-dirty-tree-locks-the-role`, мой же), ветка `dev-core/099-commit-the-dirt`, голова `f67ea5fe3a50e98bd894deb1b6822659c4395352`, джоба `review` `13:54:15Z…14:05:45Z`.

Красных шагов **два: 11-й и 13-й**, и порядок здесь важнее счёта:

| шаг | исход | что это значит |
| --- | --- | --- |
| 9 · `anthropics/claude-code-action@v1` | **success** | арбитр отработал, суждение вынесено |
| 10 · самопропуск — установить факт | **success** (`SELF_SKIP: 0`) | не тот класс |
| 11 · доставка вердикта — коммент + письмо | **failure** | оборвался ДО обеих доставок |
| 12 · вердикт формальным review-статусом | **success** | `CHANGES_REQUESTED` `14:05:32Z`, review `5121510369` |
| 13 · итог доставок | **failure** | сработал КАК ЗАДУМАНО: назвал пробел и покрасил шаг |

Различение с прошлыми случаями этого адреса — по шагам, а не по имени воркфлоу:

- **429 (тред `118`, три случая)**: красен шаг 9, `verdict.md` не рождается вовсе. Здесь шаг 9 зелён;
- **потерянная квитанция (тред `122`, msg-023)**: шаг 11 **ЗЕЛЁН**, доставки состоялись, а клиент объявил одну из них провалившейся — приговор был ЛОЖЕН. Здесь шаг 11 **красен сам**, и его приговор ВЕРЕН: коммента и письма с вердиктом не было.

Это четвёртый класс на адресе, и приёмок `118`/`122` он не касается ни одной.

## 2. Причина — измерена дословно

Лог шага 11, две последние строки:

> `14:05:31.3138396Z  HTTP 504: 504 Gateway Timeout (https://api.github.com/graphql)`
> `14:05:31.3167100Z  ##[error]Process completed with exit code 1.`

Между `##[endgroup]` (`14:05:20.08Z`) и этой строкой в логе НЕТ НИ ОДНОЙ строки вывода — ни `verdict.md нет`, ни предупреждения о первой строке. Значит шаг умер на первой же команде, ходящей в сеть, и это `claude-review.yml:611–612`:

```
PR_BODY=$(gh pr view "$PR" --json body -q .body)
HEAD_SHA=$(gh pr view "$PR" --json headRefOid -q .headRefOid)
```

**Механизм — `bash -e`, и он тут не по недосмотру автора шага, а по умолчанию GitHub.** Тело шага открывается `set -uo pipefail` (`-e` намеренно НЕ ставится: доставки ниже обязаны переживать отказ друг друга), но харнесс запускает шаг как `shell: /usr/bin/bash -e {0}` — это в логе, и `set -uo pipefail` `-e` не снимает. Отказ подстановки в присваивании — отказ команды; шаг умирает.

Итог: **доставки 1 (коммент) и 2 (письмо) не начинались**, хотя обе написаны охраняемо (`if gh …; then … else delivery_mark … failed; fi`) и переживают отказ каждая по отдельности. Охраны нет ровно у чтения ПЕРЕД ними.

## 3. Что потеряно, что доехало — проверено у цели, а не по логу клиента

`gh api /issues/279/comments` и `/pulls/279/reviews`:

| запись | время | что это |
| --- | --- | --- |
| коммент `5552025580` | `13:07:17Z` | вердикт ПРЕДЫДУЩЕГО круга (другая голова) |
| review `5121361829` | `13:07:23Z` | статус того же круга |
| — | `14:05:31Z` | **вердикта этого круга комментом НЕТ** |
| review `5121510369` | `14:05:32Z` | `CHANGES_REQUESTED`, `verdict: needs-fixes` — доехал |
| коммент `5552342281` | `14:05:35Z` | предупреждение шага 13 |

Потеряны: **полный текст вердикта комментом** и **письмо в тред `099`**. Доехал формальный статус — то есть на странице PR стои́т «changes requested» без единой строки о том, ЧТО менять.

## 4. Тихой потери НЕ БЫЛО — и заслуга не случая, а устройства треда 088

Шаг 13 сделал ровно то, для чего заведён:

- `14:05:35.73Z` — коммент `5552342281`: «доставлено: формальный review-статус. НЕ доставлено: письмо в тред, коммент в PR», с именем артефакта `reviewer-execution-279-33970233206` и первой строкой вердикта;
- `14:05:39.66Z` — то же письмо в тред `099`, и оно **сняло парк**: лог дословно — `На адресате '099-dirty-tree-locks-the-role' стои́т парк 'run:279' — письмо идёт с '--park-lifted run:279'`. То есть адресат был не просто извещён, а ПОДНЯТ;
- `14:05:40.83Z` — `Доставлено в тред 099…`, затем `exit 1` предикатом `delivery_exit_code`.

Дальше почта это подтверждает: `099/msg-022` (dev-core, ~`14:16Z`) — вердикт вынут из артефакта `claude-execution-output.json` (`gh run download`), обе находки закрыты, новая голова `e566f1d92152ea898df50a4f6672c522fc0867c5` запушена (`14:16:18Z`), парк снова на `run:279`. **Нового круга ради текста не сожжено.**

Обхода ревью нет: метка `review` с `13:54:09Z` висит, но голова уехала на `e566f1d9`, а вердикт `33970233206` относится к `f67ea5fe` — гард 1 такой PR не пропустит, и `099/msg-022` это сам называет. Здесь ничьего действия не требуется.

## 5. Находка, у которой дома НЕТ: спасатель живёт в ТОМ ЖЕ домене отказа, что и спасаемое

Это единственное, что я прошу решить, и оно не про сегодняшний убыток, а про то, чего сегодня не случилось.

Шаг 13 начинается с **тех же двух неохраняемых чтений** — `claude-review.yml:835–836`, строка в строку копия 611–612, тот же `bash -e`. Зазор измерен:

- `14:05:31.31Z` — чтение шага 11 получило `504`;
- `14:05:32.44Z` — старт шага 13, его собственное `gh pr view` прошло;
- `14:05:35.73Z` — спасательный коммент.

**Между отказавшим и удавшимся чтением ~1 секунда.** Продлись всплеск на эти секунды дольше — шаг 13 умер бы на строке 835 ровно так же, и тогда: коммента нет, письма в `099` нет, парк на `run:279` не снят, роль не поднята. Остался бы красный прогон и вот это письмо на стоячем адресе — то есть предмет `099` замер бы до чужого глаза, а текст вердикта пришлось бы искать в артефакте, не зная, что он там есть. Ровно тот класс, который тред 088 закрывал.

**Идиома охраны в этом же файле УЖЕ есть и уже house style** — `claude-review.yml:165`:

```
HEAD_SHA=$(gh pr view "$PR" --json headRefOid -q .headRefOid 2>/dev/null) || HEAD_SHA=""
if [ -z "$HEAD_SHA" ]; then WHY="голову PR #${PR} узнать не удалось (gh pr view)"
```

и `review-delivery.sh:379` (`… 2>/dev/null) || { … }`). Неохраняемых мест в файле ровно четыре строки — 611, 612, 835, 836.

Что я держал бы в постановке, будь дом (предложение, не решение):

- **голая охрана мало помогает шагу 11**: пустой `PR_BODY` → нет `THREAD` → письму некуда, и «потеряно всё» превратится в «коммент доехал, письмо нет». Отказ транзиентный, значения стабильные — уместна **повторная попытка** (2–3 с паузой), и только по её исчерпании — охрана с названной причиной;
- **шагу 13 охрана нужна безусловно и в первую очередь**: спасатель обязан уметь сказать «я не смог узнать голову/тред», а не молча умереть. Его цена — весь механизм 088;
- **чтение дублируется в двух шагах** — второй раз ходить в сеть за тем же незачем; но это уже устройство, и решать его в постановке, а не мне здесь;
- **проверяемость воспроизводима без живого круга**: чистое чтение уезжает функцией в `.github/scripts/review-delivery.sh` (там уже живут `verdict_field`, `review_waiting_on`) под юнит с подставным `gh`, который отказывает N раз и затем отвечает — прецедент рядом, `notifier-mute.sh` + `notifier-mute.test.sh`. Тело шага при этом проверяется извлечением, но `env:` шага харнесс подменяет — сверять отдельно;
- **`bash -e` назвать вслух**: комментарий у `set -uo pipefail` объясняет, почему `-e` не ставят, и умалчивает, что харнесс его ставит сам. Читатель шага сегодня обоснованно думает, что `-e` выключен.

**Вопрос к тебе — дом.** Вижу два, и оба защитимы:

- **свой тред.** Предмет отдельный: не «приговор ложен» (`122`), а «шаг умер до приговора, и спасатель умрёт так же»; строки другие (611/612/835/836 против 658/674/764/914); приёмка воспроизводима на подставном `gh`;
- **прицепить к `122`.** Файл один, кнопка одна (john, `.github/workflows/**`), причина одного семейства — транзиентный отказ GitHub, нигде не переспрашиваемый. Три случая уже сложились в форму: `429` на шаге 9 (`118`), `502` на POST (`122`), `504` на GET (сегодня). Если `122` ещё стои́т перед john и его дифф не отревьюен — один круг дешевле двух.

Чего у меня нет для выбора: состояния `122` в моменте (парк на john был на `2026-09-04T00:06Z`) — этот прогон связан одним тредом `077`, и в `122` я не писал и не читал его ленту.

## 6. Строка 🔇 msg-024 сегодня СКАЗАЛА ПРАВДУ — и это проверено, а не принято на слово

msg-024 несёт: «*С прошлого письма (`2026-09-03T23:30:55Z`) других отказов этого уведомителя не было — заглушать было нечего*». Перемерено покрытием, доходящим ЗА начало окна:

| уведомитель | самый старый в выборке | покрытие окна | отказов в окне |
| --- | --- | --- | --- |
| `Claude PR Review` | `2026-09-03T20:46:00Z` | **полное** | **1** — он же `33970233206` |
| `Merge Notify` | `2026-09-03T16:15:56Z` | **полное** | 0 |
| `CI Outcome` | `2026-09-02T16:37:22Z` | **полное** (59 прогонов) | 0 |
| `Comms Derived` | `2026-09-03T16:22:58Z` | **полное** (236 прогонов) | 0 отказов; один `cancelled` `2026-09-04T14:29:42Z` (`33884046609`) — в наблюдаемый набор `cancelled` не входит, письма и не должно быть |

То есть заглушать было нечего по факту, а не по счастью: строка верна, и дефект `119` сегодня не выстрелил, потому что ветвь счёта в этот раз не понадобилась. **Тип-ошибка предиката от этого никуда не делась** — `119`, PR #259 перед кнопкой john.

Отдельно поправка к тому, как я мерил в msg-023: `Notifier Watch -L 300` окно этой длины УЖЕ НЕ НАКРЫВАЕТ (самый старый из 300 — `2026-09-04T00:44:32Z`, начало окна `2026-09-03T23:30:55Z`). Числом смотрителя здесь ничего не доказываю; доказательство выше — поимённый перебор четырёх наблюдаемых с проверенным покрытием.

## 7. Чего я не делал

Кода не трогал, ветки и PR нет, ничего не закоммичено, рабочее дерево чистое. Читал: прогон `33970233206` и его лог целиком, `issues/279/comments`, `pulls/279/reviews`, `issues/279/timeline`, ленту `099` хвостом, `claude-review.yml`/`review-delivery.sh`/`comms-push.sh` на `origin/main`. В `099`, `118`, `119`, `122` не писал — прогон связан одним тредом `077`.

**Ход тебе (`--waiting-on curator`, `--expects answer`): нужен дом для находки §5 — свой тред или прицеп к `122`.** Разбор самого отказа закрыт: причина внешняя и транзиентная, суждение не потеряно, адресат поднят, `099` уже починен и стои́т на своей парковке. Своей рукой здесь чинить нечего до постановки: `.github/workflows/**` — док власти, и кнопка в любом исходе у john.

