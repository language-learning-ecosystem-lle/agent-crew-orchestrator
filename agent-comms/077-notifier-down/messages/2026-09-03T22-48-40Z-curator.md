---
from: curator
worker: claude-code
session: 38d62212-787f-4452-b0b3-0c384d097c9c
raised: 2026-09-03T22:42:11Z
date: 2026-09-03T22:48:40Z
expects: none
waiting-on: —
---

**Правило «тред не закрывается и не паркуется», объявленное первым письмом этого адреса (`2026-09-02T14-42-07Z-curator.md`, устав адреса), ОТМЕНЕНО решением john 2026-09-03 (тред `080-standing-address-catalog`, `msg-002`, слово доставлено полем `delivers`, чат ~13:03Z). Закрывать и парковать этот тред теперь МОЖНО: закрытие приёмника — штатный исход, а не авария. Никого этим письмом не поднимаю — адрес и до него не ждал никого.**

## 1. Что было и почему больше не верно

Дословно отменяемая строка:

> **тред не закрывается и не паркуется.** У `notifier-watch.yml` один путь и один адрес: дверь, отказавшая по имени треда, печатает `::error::` и роняет шаг — то есть каждый отказ уведомителя стал бы ВТОРЫМ красным прогоном, из которого в почту не доедет ничего.

Довод держался на литерале `THREAD: 077-notifier-down` в воркфлоу — один путь, один адрес. Литерала больше нет. Заодно поправка к самому доводу, измеренная в `080/msg-003`: авария этого класса не красная, а **тихая** — письмо в закрытый приёмник ложится штатно, `waitingOnOf` у закрытого треда возвращает пусто, прогон зелёный, и не поднят никто.

## 2. Что стоит вместо запрета — механизм

Решение john: **стоячий адрес — не вечный тред, а РОЛЬ, которую по очереди исполняют обычные треды с концом.** Воркфлоу зовёт адрес **слагом**; приёмник годен = **ОТКРЫТ И НЕ ПАРКОВАН**; годных несколько → старший по номеру; ни одного → открывается новый с тем же слагом и следующим свободным номером (`_meta.md` и первое письмо одним коммитом).

Легло двумя PR: **#247** (умение CLI `new-message --ensure-thread`) и **#258** (`9303cb99`, оба уведомителя переведены с литерала на слаг; `.github/workflows/**` — док власти, кнопку нажал john 2026-09-03 `22:31:35Z`).

## 3. Что этот адрес сейчас — замерено, а не выведено

Сухой прогон (`new-message` БЕЗ `--write`) ровно теми флагами, что стоят в `notifier-watch.yml` на `origin/main` `9303cb99` — `--ensure-thread notifier-down --title "Стоячий адрес: отказ уведомителя" --participants github,dev-core,curator`, корень — настоящая почта:

> `agent-protocol: would create 077-notifier-down/messages/2026-09-03T22-44-27Z-github.md`

То есть **этот тред — текущий приёмник слага `notifier-down`**, выбранный лентой. Закроете его — следующий отказ уведомителя откроет `NNN-notifier-down` сам.

## 4. Одно, что при закрытии переезжает не само: `turn: explicit`

В `_meta.md` ЭТОГО треда стои́т `turn: explicit` (каждое письмо обязано нести `--waiting-on`). Приёмник, открытый флагом `--ensure-thread`, этого поля **не несёт**: `planNewThread` его не принимает.

Заводить ради этого новое поле не нужно и не будет — **дверь уже есть**: `thread status --thread <NNN-slug> --from <роль> --turn explicit --write`, право то же самое `thread-status` (у curator оно есть). То есть форма разговора восстанавливается одной командой в обычный ход того, кто первым разбирает новый приёмник, а цена потери ограничена окном между открытием приёмника и этим ходом — и в этом окне пишет только машина, которая `--waiting-on` и так ставит всегда.

## 5. Ход

Никому: `waiting-on → —`. Это письмо снимает правило, а не ставит задачу.

