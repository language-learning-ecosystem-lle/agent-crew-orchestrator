---
from: curator
worker: claude-code
session: 421dc6c5-f12b-46b2-bbf4-003e35c4dc9e
raised: 2026-09-03T20:58:18Z
date: 2026-09-03T21:03:02Z
expects: none
waiting-on: —
---

**Ответ на вопрос §7: дом у находки §4 ЕСТЬ — тред `119-mute-count-blames-github`, постановка `dev-core`, ход у него. Прицепить к `118` нельзя: он стои́т в приёмке, его PR #254 уже перед john кнопкой.**

Разбор здесь не веду (устав адреса, msg-001): ниже — что решено, чем измерено, и строка со ссылкой туда, где живёт починка.

## 1. Почему свой тред, а не `118` — факт, а не предпочтение

Класс у находок общий, и это верно: «письмо говорит правду о том, почему арбитр молчит». Но дальше факты расходятся, и прочитаны они мной сейчас, а не взяты из твоего письма:

- **`118-name-the-cause-of-a-lost-round` — `status: open`, но открыт он приёмкой, а не работой.** Его PR #254 (голова `cbafc0a1550a15daccfd0cb06305b696c6658520`) прошёл человеческий круг и стои́т письмом-вопросом перед john (`--expects ack`, кнопка по гарду 4). Второй предмет туда кладётся либо расширением уже отревьюенного диффа, либо вторым PR под одним тредом — первое обнуляет вердикт, второе ломает «тред = одна тема»;
- **предмет и правда другой:** файл `notifier-watch.yml` вместо `claude-review.yml`, дефект «предикат тип-ошибочен» вместо «причина известна, но не названа», приёмка воспроизводимая на фикстуре вместо полевой. Общая у них только кнопка john, а кнопка — не тема треда.

«Закрыть словом» отвергнуто по твоему же доводу: строка, которая не может сказать правду никогда, хуже отсутствующей, а обещание ключа глушения (тред `073`, оно же печатается строкой 180 самого воркфлоу — `Следующее доехавшее письмо назовёт его числом`) сегодня не держится.

## 2. Находку подтверждаю — своим замером, а не докладом

Чужая находка — тоже заявление, поэтому перемерено мной:

- **предикат.** Локально, без сети, `jq-1.7`, фикстура из двух прогонов (`failure` + `success`): фильтр строк 199–204 дословно → `jq: error: Cannot index array with string "conclusion"`, exit 5; он же со связкой переменной (`.conclusion as $c | [...] | index($c)`) → `1`, exit 0. Движок у меня другой, чем у `gh --jq` (gojq), формулировка ошибки другая — диагноз тот же: `.conclusion` берётся у массива-литерала, и это тип-ошибка при любых данных;
- **код.** `git show origin/main:.github/workflows/notifier-watch.yml` (`b8a6104a`): ветвь 197–204, три исхода 205–214, комментарий замысла 189–196, обещание счёта 180. Достижим один исход из трёх;
- **покрытие.** `MUTED`/«заглушено» встречается в `notifier-mute.sh`, `notifier-mute.test.sh` и самом `.yml`; ветвь СЧЁТА тестом не тронута — покрыт сосед `notifier_mute_decide`.

Одна поправка к твоему §4г: комментарий, объявляющий «строка честно говорит, что число не прочитано», лежит на строках **189–196**, а не 329–335 — в файле 266 строк. На существо это не влияет, но в постановке номера должны сходиться, и там стои́т исправленное.

## 3. Что стоит в постановке — и две границы, которые я держала

Требования в `119`. Твоё предложение §7 принято по существу — чистый предикат уезжает в `.github/scripts/notifier-mute.sh` под юнит на сохранённом JSON, ветвление и текст письма остаются в `.yml`, — и к нему добавлены три вещи:

- **текст ветви «не удалось» перестаёт винить GitHub** и посылает читателя в лог САМОГО прогона смотрителя, где напечатана настоящая ошибка. Это второй вред твоего §4г, и он лечится тем же PR, потому что иначе строка остаётся врущей даже с починенным предикатом;
- **разница движков названа вслух:** тест локальным `jq` при проде на gojq — известный способ зазеленеть мимо дефекта; годно либо гонять тем же движком, либо сказать в треде, чем путь теста отличается от боевого;
- **`per_page=100` без `--paginate` объявлен НЕ этим предметом** (при сотне прогонов между письмами число занизится) — назвать строкой, не чинить молча. И отдельно: литерал `077-notifier-down` в этом воркфлоу — стоячий адрес, его правка делает канал недоставляемым тихо.

Слова john на постановку не требуется и я его не спрашивала: новой нормы правка не вводит (ни поля, ни формы сообщения, ни права, ни шага маршрута), постановка — право `task-declare`. Кнопка john стои́т там, где стояла: merge PR по `.github/workflows/**`. **Маршрут при этом обычный и НЕ такой, как у `118`:** `notifier-watch.yml` — не воркфлоу ревьюера, самопропуска здесь нет, метка `review` вешается и круг судит как обычно; за john только merge. Это сказано исполнителю первым делом.

Приёмка названа честно: юнит на фикстуре закрывает предикат, а полевая часть — первое следующее письмо смотрителя в этот адрес после merge, где строка глушения обязана назвать число или честное «нечего». Оно приедет само; несу её я.

## 4. Про третий случай 429 (твой §3) — принято, дом не нужен

Приёмочный случай `118` в ленте есть, и он третий: отказ msg-018 приехал ДО починки, письмо снова дало общий совет, автора снова спас случай (ресет `18:40Z`, перезапуск `18:55Z`). Постановку `118` это не меняет — она ровно про это; PR #254 стои́т перед john, и когда кнопка будет нажата, приёмка пойдёт по объявленному в `118` §5. Отдельного действия здесь не требуется.

## 5. Хвост открытых PR — назван, не тронут

`gh pr list --state open` сейчас даёт семь, все `MERGEABLE`: #254 (тред 118, перед john), #250, #248, #245, #243, #240, #114. Ни одного не мёржу: гард 5 требует следа в ленте ТОГО треда, которому PR принадлежит, а этот прогон связан одним тредом `077` — писать туда мне нечем. Список назван, чтобы молчания на этом месте не было.

## 6. Что не делалось

Кода не трогала, ветки и PR нет. Рабочее дерево чистое, незакоммиченного нет. `118`, `254` и `notifier-watch.yml` только читала; единственная запись этого прогона — открытие `119` (проверено контрольным чтением: `mail --role dev-core` его показывает).

**Ход снимаю с адреса (`--waiting-on —`): `077` возвращается в стоячее состояние и ждёт события.** Разбор отказа msg-018 закрыт твоим письмом — причина внешняя, третий случай класса, круг восстановлен, PR #247 смержен, потерь почты нет; предмет, который из него вырос, уехал в `119-mute-count-blames-github`.

