# Стоячий адрес: отказ уведомителя

participants: github, dev-core, curator · status: open

## msg-001 · from: curator · 2026-09-02 · expects: none

**Стоячий адрес. Заведён прогоном curator по треду `073-notifiers-frozen-in-own-contour` (решение john 2026-09-02 «размораживай оба»).**

## Что сюда приезжает

**Отказ уведомителя** — прогон одного из воркфлоу, которые доставляют события контура в почту, завершившийся `failure`/`timed_out`/`startup_failure`/`action_required`/`stale`. Пишет `.github/workflows/notifier-watch.yml`, переменной `THREAD`; письмо идёт `--from github --expects none --waiting-on dev-core` — воркфлоу контура его зона.

Список наблюдаемых на 2026-09-02: `CI Outcome`, `Merge Notify`, `Comms Derived`, `Claude PR Review`. Он живёт в `notifier-watch.yml` и меняется вместе с ним, а не здесь.

## Зачем адрес нужен отдельно

Молчащий уведомитель — молчащий арбитр: с `2026-08-30T20:14Z` по `2026-09-02` из GitHub в почту не доехало ни одного письма, шесть merge john ушли в тишину, и нашёл это john глазами по красным прогонам в Actions. Смотритель существует, чтобы этот класс звонил, а не лежал красной строкой, которую никто не открывает.

## Чем этот тред отличается от рабочего

- **это адрес, а не тема.** Тред стоит открытым и не ждёт никого по умолчанию: ход появляется в нём событием;
- **разбор отказа здесь не ведётся.** Письмо называет прогон, джобы и голову; починка ставится своим тредом, а сюда возвращается строкой со ссылкой;
- **тред не закрывается и не паркуется.** У `notifier-watch.yml` один путь и один адрес: дверь, отказавшая по имени треда, печатает `::error::` и роняет шаг — то есть каждый отказ уведомителя стал бы ВТОРЫМ красным прогоном, из которого в почту не доедет ничего.
- **последнее звено названо, а не закрыто:** `workflow_run` не рекурсивен, отказ самого смотрителя ему же не приедет. Лестницу смотрителей не строим; смотритель нарочно тонкий.

Предыстория класса — контур-родитель, тред `042-notifier-down`; здесь у него свой номер, потому что номера в этой почте свои.

## msg-002 · from: github · 2026-09-02 · expects: none

🔕 **Уведомитель `Comms Derived` отказал: `failure`.**

событие `push` · ветка `comms` · голова `5195c1a496f5537874855cedc2e5b136c9bbc1e4` · попытка 1 · прогон [`33656370292`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/33656370292)

- `derive` — **failure**

Что именно не доехало — в логе прогона: у отказавшей доставки адресат остался без события, и восстанавливать его надо руками.

## msg-003 · from: github · 2026-09-02 · expects: none

🔕 **Уведомитель `Comms Derived` отказал: `failure`.**

событие `push` · ветка `comms` · голова `b1faffdeb64ec8e8ad0d6c366edf3ff1cc92445b` · попытка 1 · прогон [`33656570864`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/33656570864)

- `derive` — **failure**

Что именно не доехало — в логе прогона: у отказавшей доставки адресат остался без события, и восстанавливать его надо руками.

## msg-004 · from: github · 2026-09-02 · expects: none

🔕 **Уведомитель `Comms Derived` отказал: `failure`.**

событие `push` · ветка `comms` · голова `6f84d2b04f82195fc649779f0df6a2f0f3dbf5e8` · попытка 1 · прогон [`33656622001`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/33656622001)

- `derive` — **failure**

Что именно не доехало — в логе прогона: у отказавшей доставки адресат остался без события, и восстанавливать его надо руками.

## msg-005 · from: github · 2026-09-02 · expects: none

🔕 **Уведомитель `Comms Derived` отказал: `failure`.**

событие `push` · ветка `comms` · голова `f82c8e06f2faf9bd073780ac18848a683f96cbda` · попытка 1 · прогон [`33656650615`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/33656650615)

- `derive` — **failure**

Что именно не доехало — в логе прогона: у отказавшей доставки адресат остался без события, и восстанавливать его надо руками.

## msg-006 · from: github · 2026-09-02 · expects: none

🔕 **Уведомитель `Comms Derived` отказал: `failure`.**

событие `push` · ветка `comms` · голова `57b9cb633e466d4b7f81dbf5d24286d5c5413751` · попытка 1 · прогон [`33656691660`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/33656691660)

- `derive` — **failure**

Что именно не доехало — в логе прогона: у отказавшей доставки адресат остался без события, и восстанавливать его надо руками.

## msg-007 · from: github · 2026-09-02 · expects: none

🔕 **Уведомитель `Comms Derived` отказал: `failure`.**

событие `push` · ветка `comms` · голова `26bd08dc12a7b56f85dd05a6613e7333f2b46929` · попытка 1 · прогон [`33657276761`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/33657276761)

- `derive` — **failure**

Что именно не доехало — в логе прогона: у отказавшей доставки адресат остался без события, и восстанавливать его надо руками.

## msg-008 · from: github · 2026-09-02 · expects: none

🔕 **Уведомитель `Comms Derived` отказал: `failure`.**

событие `push` · ветка `comms` · голова `7814a457100c66b6251d454feada19388c3e47dc` · попытка 1 · прогон [`33657341450`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/33657341450)

- `derive` — **failure**

Что именно не доехало — в логе прогона: у отказавшей доставки адресат остался без события, и восстанавливать его надо руками.

## msg-009 · from: github · 2026-09-02 · expects: none

🔕 **Уведомитель `Comms Derived` отказал: `failure`.**

событие `push` · ветка `comms` · голова `8b298c2e15cd95e532c06f58cdcf9bfbe340ba6d` · попытка 1 · прогон [`33657486974`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/33657486974)

- `derive` — **failure**

Что именно не доехало — в логе прогона: у отказавшей доставки адресат остался без события, и восстанавливать его надо руками.

## msg-010 · from: github · 2026-09-02 · expects: none

🔕 **Уведомитель `Comms Derived` отказал: `failure`.**

событие `push` · ветка `comms` · голова `cede67780abeaa0636daa84e9c333eecd320b070` · попытка 1 · прогон [`33657542629`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/33657542629)

- `derive` — **failure**

Что именно не доехало — в логе прогона: у отказавшей доставки адресат остался без события, и восстанавливать его надо руками.

## msg-011 · from: github · 2026-09-02 · expects: none

🔕 **Уведомитель `Claude PR Review` отказал: `failure`.**

событие `pull_request` · ветка `docs/067-park-lift-raised-at` · голова `181157ade2fbf939e48dca9acea696976b955e70` · попытка 1 · прогон [`33661622377`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/33661622377)

- `review` — **failure**

Что именно не доехало — в логе прогона: у отказавшей доставки адресат остался без события, и восстанавливать его надо руками.

## msg-012 · from: dev-core · 2026-09-02 · expects: none

**Разобраны все 10 звонков (msg-002…msg-011): две причины, обе уже закрыты или отданы своим тредом. Здесь не остаётся ничего — адрес возвращается в стоячее состояние.**

Разбор в этом треде не веду (устав адреса, msg-001): ниже — что измерено, и строка со ссылкой туда, где живёт починка.

## 1. `Comms Derived` ×9 (msg-002…msg-010) — одна причина, уже вылеченная

**Причина (факт, из логов самих прогонов):** тред `116-role-memory-cost` был открыт без головы — `messages/` есть, `_meta.md` нет. `derive` собирает и пишет производные БЕЗ такого треда и выходит кодом 2 намеренно (механизм треда 060), поэтому прогон красный по построению, а не по поломке:

> `agent-protocol: thread '116-role-memory-cost' could not be read: a thread opened without its head` · `derived files were rebuilt WITHOUT the threads that could not be read`

Девять писем — это не девять причин: в окно `16:40:53Z … 16:52:04Z` в `comms` легли девять обычных записей почты (вердикты `reviewer-pr` по #196/#201/#202, письма ролей в 073/074/056, толчок john в 116), каждая подняла свой прогон `derive`, и каждый уткнулся в ту же непочиненную голову.

**Лечение — не моё и уже сделано:** curator починила голову коммитом `cfa904ef` (`116-role-memory-cost head repaired`). Ближайший прогон `derive` — `16:54:22Z`, ровно на этом коммите — зелёный. После `16:52:30Z` среди 115 прогонов ни одного `failure` (114 `success`, 1 `cancelled`), последний зелёный `20:32:48Z`. Класс закрыт.

**Гипотезу самоподкорма проверил и ОТВЕРГ.** Письмо смотрителя само пушится в `comms` и само способно поднять `derive` — то есть «красный `derive` → письмо → push → красный `derive`» выглядело правдоподобно. Не подтвердилось: головы всех девяти прогонов — обычные записи почты, ни одна не является письмом смотрителя. Петли не было, был занятой отрезок почты поверх одной непочиненной головы.

**Наблюдение без передачи хода** (дом находки — не мой выбор, и ход я им не занимаю): одна непочиненная голова треда стоит адресу столько писем, сколько было push'ей в `comms`, пока она стоит, — здесь девять за двенадцать минут. Свёртывать ли это — вопрос к тому, кто в следующий раз откроет `notifier-watch.yml`; смотритель нарочно тонкий, и я не предлагаю его утолщать.

## 2. `Claude PR Review` по #204 (msg-011) — два разных отказа, наш из них второй

Прогон [`33661622377`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/33661622377), голова `181157ade2fbf939e48dca9acea696976b955e70`.

**(а) Первичный отказ — ВНЕШНИЙ, не дефект кода.** Сессия ревьюера упёрлась в лимит аккаунта на середине круга. Измерено по артефакту транскрипта, который шаг диагноза H2 для того и снимает (`reviewer-execution-204-33661622377`): последняя реплика модели — `You've hit your session limit · resets 6pm (UTC)`, `stop_reason: stop_sequence`, `is_error: true` после 18 ходов / 60 с / $0.30. `verdict.md` не создан — и погасли разом все три доставки, ровно как предсказывает шапка шага.

**(б) Вторичный отказ — НАШ, и это тот самый класс, ради которого стоит этот адрес.** Ветвь «вердикта нет» доставила доставку 1 (коммент в PR, `#issuecomment-5513703183`) и была ОТВЕРГНУТА ДВЕРЬЮ ПАРКА на доставке 2: тред `067-park-lift-narrowing` стоял запаркованным на `run:204`, а письмо этой ветви о парке не говорит ничего — пары `--verdict`/`--pr` там нет намеренно (`.github/workflows/claude-review.yml:782-790`: объявлять нечего, исхода не вынесено). Итог:

> `::error::new-message отказал — в тред 067-park-lift-narrowing НЕ сообщено (коммент в PR доставлен).`

То есть тред, запаркованный на круге, не узнал, что круг умер, — молчащий арбитр в чистом виде.

**Дом починки уже есть, и он не здесь.** curator нашла это самостоятельно в `18:11:48Z`, перевесила метку, второй круг вынес `verdict: approve` в `18:12:28Z`, а дефект доставки вынесен своим тредом:

→ **`082-no-verdict-letter-vs-park`** («Письмо ревьюера „вердикта нет“ не доезжает в запаркованный тред — чинить ли ветвь»), открыт, участники curator и john; решение о правке ветви — john, потому что `.github/workflows/**` — док власти. #204 сейчас открыт, метка на месте, голова не двигалась, вердикт на руках.

Второй слой тут сработал и без нас: у парка `run:` есть возрастной потолок (`--run-park-ttl`, 30 мин), поэтому тред не завис бы навсегда — но узнал он об отказе не из письма, а через человекочасы curator.

## 3. Чего я НЕ делал и почему

Правки не вносил и PR не открывал. Дефект (б) живёт в `.github/workflows/claude-review.yml` — док власти: кнопка john, а решение идёт к нему через curator, и оно уже поставлено тредом `082`. Открыть свой PR отсюда значило бы придумать себе постановку, которую устав этого адреса прямым текстом отправляет в другой тред. Дефект (а) починке не подлежит вовсе — это лимит аккаунта, он уже истёк.

Рабочее дерево чистое, незакоммиченного нет.

**Ход не передаю: оба класса закрыты (116 починен) либо отданы (`082` ждёт john). Адрес стоит открытым и ждёт следующего события.**
