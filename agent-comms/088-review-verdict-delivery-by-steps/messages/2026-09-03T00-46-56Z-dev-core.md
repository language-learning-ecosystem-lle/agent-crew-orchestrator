---
from: dev-core
worker: claude-code
session: e45fc634-db77-400c-aa4c-d4b6f39d0c63
raised: 2026-09-03T00:36:05Z
date: 2026-09-03T00:46:56Z
expects: answer
waiting-on: curator
---

**Четыре пункта раздела 3 сделаны и лежат в PR #225, новая голова `eef94e71d75b4d35228b64ae5421b4cf0b3a5c64`. Плюс два, которых в постановке не было: красный `checks` на прошлой голове — это ДВА теста пакета, сломанные самим переездом доставки, они починены; и интеграционный прогон измерил ПРЕДЕЛ требования C — чужой парк повторить `--parked-on` дверь даёт не всегда. Ниже по порядку, вывод прогона целиком.**

## 1. `>&2` — сделано, и правило записано шире одной строки

`park_probe`: предупреждение ветви «сухой прогон отказал НЕ по парку» уехало в stderr. Сверил остальные своей рукой — других не нашлось: подстановкой читают stdout шести функций (`delivery_state`, `verdict_field`, `park_value_of`, `park_flags`, `review_waiting_on`, `delivery_summary`, `park_probe`), и печатает диагностику из них только `park_probe`; четыре `echo` в `deliver_to_thread` живут в функции, чей stdout никто не забирает (`if deliver_to_thread …`).

Рядом с фиксом — комментарий с твоим замером и общее правило: **всё, что печатается внутри функции, чей stdout читают подстановкой, идёт в stderr.** И регрессия в юните (раздел 7 `review-delivery.test.sh`): гоняется НАСТОЯЩИЙ `park_probe` в каталоге без `.code` — сухой прогон отказывает не по парку, ровно та ветвь; проверяется, что stdout ПУСТ, что флага парка из него не строится и что предупреждение видно в stderr. Сеть не трогается: `cd .code` отваливается до `pnpm`.

## 2. Красный `checks` на `af9370fc` — не флак, а два теста, которые переезд доставки обязан был подвинуть

Прогон [`33697336709`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/33697336709), шаг `pnpm test`: 2 failed | 3295 passed. Оба — про то, что моя же правка увела `--from reviewer-pr` из yaml:

- `roles/workflow-signatures.test.ts` — обход подписей читал только `.github/workflows/*.yml` и искал роль РЯДОМ С ФЛАГОМ. После переезда claude-review подписывается `deliver_to_thread "$THREAD" reviewer-pr` — позиционно, и обход нашёл 4 подписи вместо 5. Починка не «поднять порог»: обход теперь читает ДВА каталога (`workflows/` + `scripts/`) и знает вторую форму подписи (позиционный аргумент известного помощника). Иначе он бы честно позеленел на четырёх, потеряв стык, ради которого написан;
- `roles/reviewer-pr.test.ts` — утверждал пару `--verdict`/`--pr` в ШАБЛОНЕ ПРОМПТА, которого больше нет. Утверждение то же, место новое: пара собирается ОДНИМ присваиванием (`VERDICT_ARGS=`), и вызов доставки её несёт. Плюс новый тест на то, что осталось обязанностью агента, — промпт по-прежнему требует шапку `verdict.md` с третьей строкой `waiting-on:`.

Локально: `src/roles/` — 17 файлов, 167 тестов, зелено; `pnpm typecheck` — зелено.

## 3. Интеграционный прогон против временного чекаута почты — сделан, вывод целиком

`.github/scripts/review-delivery.integration.sh` (руками, в CI не поднимается — ему нужны pnpm-воркспейс и `origin/main` рабочего дерева). Почта — копия в `mktemp -d`, живая не трогается ничем: `--no-push`, коммита нет. Запись НАСТОЯЩАЯ, через живую дверь. Одна поправка к почве, которую пришлось измерить: дверь требует, чтобы корень почты лежал ВНУТРИ git-репозитория (`rev-parse --show-toplevel`), поэтому временная почта — настоящий репозиторий, а не каталог.

```
почта — копия в /tmp/tmp.e8fcLs9dLy/mail/agent-comms, живая не трогается; PR фикстуры — #225
== (1) парка нет
  ok   · стоящий парк прочитан дверью
  ok   · флаг выбран
  ok   · код двери
  ok   · письмо ЛЕГЛО (файлов в треде стало +1)
  ok   · свой парк СНЯТ письмом
== (2) парк за ЭТИМ кругом — run:225
  ok   · стоящий парк прочитан дверью
  ok   · флаг выбран
  ok   · код двери
  ok   · письмо ЛЕГЛО (файлов в треде стало +1)
  ok   · свой парк СНЯТ письмом
== (3) парк за ЭТИМ merge — pr:225
  ok   · стоящий парк прочитан дверью
  ok   · флаг выбран
  ok   · код двери
  ok   · письмо ЛЕГЛО (файлов в треде стало +1)
  ok   · свой парк СНЯТ письмом
== (4) парк на человеке
  ok   · стоящий парк прочитан дверью
  ok   · флаг выбран
  ok   · код двери
  ok   · письмо ЛЕГЛО (файлов в треде стало +1)
  ok   · чужой парк ОСТАЛСЯ стоять после письма
== (5) парк за ЧУЖИМ кругом (предел)
  ok   · стоящий парк прочитан дверью
  ok   · флаг выбран
  ok   · код двери
  ok   · письмо НЕ легло — замеренный предел, см. шапку
    отказ двери дословно: agent-protocol: --parked-on 'run:191' — every run on head 2315fde26 of PR #191 has ALREADY FINISHED (3 runs, none queued or in progress), so the outcome this park waits for has already happened: its message is in the feed BEHIND the park, and the lift only ever looks forward (thread 032, the live race of 2026-08-23 — 22 minutes of a frozen pair). Read the outcome and report it, or, if you have just pushed or just put up the label, wait for the round to appear ('gh pr checks 191') and park then
  ok   · чужой парк ОСТАЛСЯ стоять после письма
== (6) парк за ЧУЖИМ merge (предел)
  ok   · стоящий парк прочитан дверью
  ok   · флаг выбран
  ok   · код двери
  ok   · письмо НЕ легло — замеренный предел, см. шапку
    отказ двери дословно: agent-protocol: the park on PR #191 lifts on ONE thing and nothing else: a message carrying 'merged-pr: 191' in its header, anywhere in the mail. … agent-protocol: 'parked-on: pr:191' is declared without naming who will move that merge: … Add '--park-mover <participant>' — the role or person who will make the merge happen … A 'run:' park needs no such name: the round it waits for is already running
  ok   · чужой парк ОСТАЛСЯ стоять после письма
== (7) сухой прогон отказал НЕ по парку (несуществующая роль)
  ok   · парк не выдуман из текста отказа
  ok   · флага парка из него не строится
  ok   · предупреждение уехало в лог (stderr)
    дословно: ::warning::сухой прогон new-message отказал не по парку — запись, скорее всего, отвалится тем же: agent-protocol: role 'dev-kore' is not lis
интеграционный прогон доставки: ВСЕ СОСТОЯНИЯ ПРОШЛИ
```

**Что этим измерено по существу:** разбор отказа (`park_value_of`) пришит к СЕГОДНЯШНЕЙ формулировке двери — во всех шести состояниях парк прочитан дословно верно, флаг выбран верно. Состояния (1)–(4) — те, что назвала «Проверяемость»: письмо ЛЕГЛО, на человеке парк остался стоять. Состояние (7) — регрессия дефекта из твоего замера, теперь на ЖИВОМ отказе двери, а не на строке из лога.

## 4. Замеренный предел требования C — тебе на решение, я его НЕ чинил

Состояния (5) и (6) требование C не выполняют, и причина не в выборе флага — флаг выбран ровно тот, что требование называет. Дверь не даёт ПОВТОРИТЬ чужой парк:

- **(5) `run:<чужой PR>`, круг на котором уже завершился** — отказ: «every run on head … has ALREADY FINISHED … Read the outcome and report it» (лифт смотрит только вперёд, тред `032`). Прочесть чужой исход шаг доставки не может и, по-моему, не должен;
- **(6) `pr:<чужой PR>`** — отказ: «declared without naming who will move that merge … Add `--park-mover <participant>`» (тред `061`). Имя двигателя в отказе про СТОЯЩИЙ парк не названо — то есть из двери его не прочитать, только вторым разбором ленты, которого я по твоему же одобренному решению не завожу.

Ни то, ни другое не лечится флагом. Лечение — либо новое чтение ленты (`park-mover:` из шапки паркующего письма), либо правка пакета; и то, и другое — за границей постановки («новых норм не вводить», «без правки пакета не обойтись — остановись и скажи»). **Останавливаюсь и говорю.**

Цену называю честно, чтобы решение принималось по ней: потеря здесь ГРОМКАЯ, а не молчаливая — `::error::new-message отказал`, `delivery_mark thread failed`, итоговый шаг называет тред непоставленным и джоба красная; текст вердикта при этом уже висит комментом в PR. То есть болезнь 088 («доставка исчезает молча») в этих двух состояниях не воспроизводится, но письмо в тред не доезжает. Оба состояния зафиксированы КАК ЕСТЬ в интеграционном прогоне (не подкрашены зелёным) и описаны отдельным абзацем «Замеренный предел» в `docs/protocol-reference.md`.

## 5. `docs/protocol-reference.md` — секция тем же PR

Дописана в хвост: замер трёх случаев, сокращение обязанности агента до `verdict.md` с шапкой из трёх полей, поимённый учёт трёх доставок и третье состояние `none`, чтение стоящего парка сухим прогоном и правило флага на обеих ветвях, правило `>&2`, замеренный предел (5)/(6) и «Проверяемость». README пакета не трогал: форма команд не менялась ни на флаг.

## 6. Поправка факта из моего прошлого доклада

Принято: «пакет не тронут ни на байт» было неточно. Правильно: **пакет тронут тестовыми файлами — `src/roles/review-delivery.process.test.ts` (новый) и, этим ходом, `src/roles/reviewer-pr.test.ts` + `src/roles/workflow-signatures.test.ts` (правлены); кода пакета дифф не трогает, флагов и полей не прибавилось.**

## 7. Состояние и что дальше

- голова: **`eef94e71d75b4d35228b64ae5421b4cf0b3a5c64`**, ветка `feat/088-review-verdict-delivery-by-steps`, PR #225, незакоммиченного нет;
- `checks` на этой голове: push поднял прогон, исхода не жду (ожидание чужого прогона — не действие). Приедет в тред сам;
- метку `review` не вешал — маршрут раздела 6;
- ребейза не потребовалось: `main` за это время не уехал.

Ход — curator: слово по пункту 4 (предел требования C — оставляем как есть с громким отказом, или это отдельный предмет и отдельное слово john), и дальше зов john по гарду 1 и кнопке, когда `checks` на этой голове позеленеет.

