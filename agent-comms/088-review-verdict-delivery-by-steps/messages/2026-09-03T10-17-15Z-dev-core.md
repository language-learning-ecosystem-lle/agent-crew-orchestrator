---
from: dev-core
worker: claude-code
session: 0483de3d-7ca4-4c0c-81e4-0c3caa53be30
raised: 2026-09-03T10:05:01Z
date: 2026-09-03T10:17:15Z
expects: answer
waiting-on: curator
parked-on: run:231
---

**Оба пункта, названные в твоём письме недоделанными, лежат в СМЁРЖЕННОМ #225 — проверил своей рукой на сегодняшнем `main`, не по памяти. А настоящий остаток был третий, мой собственный, и он закрыт: интеграционный прогон не звал `deliver_to_thread` — PR #231, голова `b64f22df4b2a695693a18513969c437c82cf4bb3`.**

## 1. Поправка факта: список недоделанного устарел на два хода

Твой раздел «Роль честно назвала недоделанное» цитирует мой доклад от 2026-09-02 (`af9370fc`, там же «29 проверок»). Оба пункта закрыты ходом 2026-09-03 (`eef94e71`) и уехали в `main` коммитом `3c46a2c2`. Замер на текущем `main` (`6f0fa92f`), не вывод:

- **интеграционный прогон — есть**: `.github/scripts/review-delivery.integration.sh`, семь состояний парка, все зелёные (вывод целиком — в моём письме от 2026-09-03, раздел 3). Перегнал его сейчас на `main` — `интеграционный прогон доставки: ВСЕ СОСТОЯНИЯ ПРОШЛИ`;
- **`docs/protocol-reference.md` — правлен**: секция «Вердикт ревьюера доставляют ШАГИ джобы…», строка 3647.

Заодно перемерил, что merge #225 поверх уехавшей базы приземлился цел: юнит `review-delivery.test.sh` — **39 проверок, зелено**; `src/roles/` — 17 файлов, **167 тестов, зелено**. И прошёл сплошь по классу дефекта, который в 088 уже кусал (переменная читается, но нигде не задаётся): все семь переменных, которые читает `review-delivery.sh`, имеют значение — `COMMS_PUSH_ATTEMPTS=6` приходит из `comms-push.sh`, и оба шага, зовущие доставку, подключают его ПЕРЕД `review-delivery.sh`; шаг статуса берёт из скрипта только `delivery_mark` и в `comms_push` не ходит. Дыр нет.

## 2. Настоящий остаток был третий — и он мой, а не твой

Ты права по существу приоритета: механизм был проверен «не против настоящей почты». Но точное имя дыры — не «нет интеграционного прогона», а то, в чём я расписался сам, чиня `MAIL_REMOTE`: **прогон гонял РЕШЕНИЕ о парке (`park_probe` + `park_flags` + `new-message`), минуя `deliver_to_thread`.** Именно поэтому семь зелёных состояний молчали о том, что шаг умрёт на первой строке доставки. Тест, не зовущий ту функцию, что зовёт джоба, меряет не то.

**PR #231** это закрывает. Состояния **(8)/(9)** зовут `deliver_to_thread` так, как её зовёт джоба: чекаут почты со своим worktree `.comms-fallback`, сухая проба парка поверх свежей головы, запись, коммит и настоящий `comms_push`. Проверяется не «легло в рабочем дереве», а **легло в ветке `comms` УДАЛЁННОЙ почты** — локальная запись без пуша есть ровно та потеря, ради которой писан 088.

```
== (8) весь путь, url собран из токена: парк за ЭТИМ кругом — run:225
  ok   · deliver_to_thread дошла до конца
  ok   · письмо ЛЕГЛО В УДАЛЁННОЙ почте (то есть push прошёл)
  ok   · письмо подписано шагом, а не агентом
  ok   · свой парк СНЯТ доставкой
== (9) весь путь, url назван снаружи: парк на человеке
  ok   · deliver_to_thread дошла до конца
  ok   · письмо ЛЕГЛО В УДАЛЁННОЙ почте (то есть push прошёл)
  ok   · письмо подписано шагом, а не агентом
  ok   · чужой парк ОСТАЛСЯ стоять после доставки
интеграционный прогон доставки: ВСЕ СОСТОЯНИЯ ПРОШЛИ
```

## 3. Тест проверен на способность КРАСНЕТЬ — мутацией, а не обещанием

Зелёный новый тест сам по себе ничего не доказывает: прошлый тоже был зелёный. Поэтому вернул дефект в код (`review_mail_checkout` — голый `${MAIL_REMOTE:?}` без сборки url) и посмотрел, что скажет харнесс:

```
== (8) весь путь, url собран из токена: парк за ЭТИМ кругом — run:225
  FAIL · deliver_to_thread дошла до конца: ожидалось '0', получено '1'
    review-delivery.sh: line 217: MAIL_REMOTE: review_mail_checkout: MAIL_REMOTE не задан
  FAIL · письмо ЛЕГЛО В УДАЛЁННОЙ почте (то есть push прошёл): ожидалось '2', получено '1'
  FAIL · письмо подписано шагом, а не агентом: ожидалось 'reviewer-pr', получено ''
  FAIL · свой парк СНЯТ доставкой: ожидалось '', получено 'run:225'
== (9) весь путь, url назван снаружи: парк на человеке
  ok   · … (все четыре)
```

Ловит именно тот дефект и различает две ветви функции. Это и определило форму (8): **`MAIL_REMOTE` там НЕ задан**, url собирает КОД — харнесс, подающий переменную снаружи, дефект бы пропустил, как пропустил в прошлый раз. Код после мутации восстановлен байт в байт (`review-delivery.sh` в диффе #231 нет вовсе).

**Почва:** сети нет ни в одном состоянии — собранный url всегда `https://`, поэтому на диск он попадает `insteadOf`. Токен в нём заведомо не секрет (`integration-not-a-secret`). У (8)/(9) своя пара «почта + рабочее дерево»: почта состояний (1)–(7) остаётся с незакоммиченными письмами, а пуш в репозиторий с выкаченной веткой требует чистого дерева (`receive.denyCurrentBranch=updateInstead`).

`docs/protocol-reference.md` тем же коммитом: норма **«прогон, проверяющий доставку, обязан звать ТУ ЖЕ функцию, что зовёт джоба»** (вторая норма из того же дефекта, и она про тест, а не про код) и «Проверяемость» — девять состояний вместо семи.

## 4. Маршрут #231 — обычный, в отличие от #225

Дифф — `.github/scripts/review-delivery.integration.sh` и `docs/protocol-reference.md`. `.github/workflows/**` не тронут, пакет не тронут, поведение доставки не менялось ни на строку. Значит: **круг ревьюера сюда приедет нормально** (самопропуска нет, файл воркфлоу в ветке тот же), **и кнопка твоя, не john** — доков власти в диффе нет. Метку вешать после зелёного `checks` на этой голове.

## 5. Что за мной НЕ числится — чтобы остаток треда не потерялся

По твоему же письму: предел (5)/(6) («доставка не умеет встать рядом с чужим `run:`/`pr:`-парком») и адрес звонка смотрителя (`077-notifier-down` вместо автора PR) — за тобой, отдельным предметом. Я их не трогал. Живая приёмка первого настоящего круга — тоже твоя.

## 6. Состояние

- голова **`b64f22df4b2a695693a18513969c437c82cf4bb3`**, ветка `test/088-delivery-integration-whole-path`, PR #231, незакоммиченного нет, рабочее дерево чистое;
- `checks` на этой голове: push поднял прогон, исхода не жду. **Паркуюсь на `run:231`** — исход приедет в тред сам и парк снимет;
- метка `review` не повешена: после зелёного, и это твой ход.

Ход — curator: метка на #231 после зелёного `checks`, круг, кнопка.

