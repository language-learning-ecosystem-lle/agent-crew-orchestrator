---
from: dev-core
worker: claude-code
session: 0cdf43c1-e1d9-4315-a5a5-1d064183363d
date: 2026-08-28T17:54:13Z
expects: none
waiting-on: dev-core
parked-on: pr:88
---

**Сделано по постановке целиком, PR #88 открыт, голова `e712ba2`. Живая приёмка на этом боксе: `doctor: green — 15 checks passed, 2 facts, nothing failed` вместо `1 of 17 failed`. Жду CI по #88.**

## 1. Дефект №1 — бинарь берётся у кинда аккаунта

`resolvedExec ??= found` (первый разрешившийся по инстансу) заменён на карту «воркер → разрешённый бинарь», а строка аккаунта ходит в `accountBinary` (`doctor.ts`): бинарь КИНДА АККАУНТА через слой, который уже есть (#84, `resolveExec` — машинный конфиг, затем имя вендора), с переиспользованием того, что строки агента разрешили выше. Резолвинг заново не изобретал, как ты и просила.

Бинаря своего кинда на боксе нет → строка говорит это словами и с именем кинда: `the binary of kind 'codex' ('codex', kind) was not found — there is nothing to run`. Чужой при этом не запускается и в тексте не появляется (это отдельная проверка в юните).

## 2. Дефект №2 — правило причины, и КАКОЕ я выбрал (замер, а не догадка)

Форма ответа теперь принадлежит кинду: `AgentKind.probeFailure`, рядом с `probeArgv`. claude-code — первая непустая строка (ровно то, что команда делала всегда, только теперь это утверждение об ЭТОМ инструменте, а не обо всех).

Правило codex'а снял с живого потока: `codex exec --skip-git-repo-check --sandbox read-only "…"` с одноразовым `CODEX_HOME`, 2026-08-28, exit 1 за ~10с, 35 строк на stderr, на stdout пусто. Порядок такой:

```
WARNING: proceeding, even though we could not create PATH aliases: …
Reading additional input from stdin...
OpenAI Codex v0.150.1
… баннер, промпт, предупреждения песочницы, трассировка по попыткам …
ERROR: Reconnecting... 5/5
ERROR: unexpected status 401 Unauthorized: Missing bearer or basic authentication in header, url: https://api.openai.com/v1/responses, …
```

**Выбор: последняя строка, которую инструмент сам пометил `ERROR:`, иначе последняя непустая.** Почему так, а не иначе:

- «последняя строка» без предпочтения `ERROR:` — работает на этом замере, но ломается на любом хвостовом резюме вендора;
- счётчики ретраев (`ERROR: Reconnecting... N/5`) по имени я НЕ фильтрую: вендорская формулировка — ровно то, что меняется под нами, а прогон, умерший посреди ретраев, честнее описать словом «reconnecting», чем словом «reading stdin»;
- трассировочные строки (`…Z ERROR codex_api::…`) не помечены `ERROR:` и отсеиваются тем же правилом, а не вторым.

Весь замер записан над кодом в `codex.ts` — это единственная фикстура в том файле, пришедшая из РАБОТАВШЕГО codex'а, и в комментарии это сказано отдельно.

## 3. Проверяемость — по твоим четырём пунктам плюс стык

- **(а)** два кинда, claude-аккаунт объявлен ПЕРВЫМ (регрессия ловится порядком обхода) → codex получает бинарь codex'а;
- **(б)** аккаунт без объявленного кинда → бинарь claude, поведение не сдвинулось;
- **(в)** бинаря своего кинда нет → строка говорит словами, ничего не запуская;
- **(г)** причина не позиционная: кормлю захваченный поток, первая строка — прогресс, жду в детали строку 401;
- **стык (сверх постановки, докладываю)** — `doctor.process.test.ts`: настоящая команда `doctor` без `--offline` на боксе с двумя ПОДДЕЛЬНЫМИ бинарями (claude первым), каждый отказывает словами своего вендора. Юнит этого показать не может: дефект №1 — свойство ПОРЯДКА обхода воркеров реальной командой, дефект №2 — того, что реальный ребёнок пишет первым. **Оба случая проверены на падение:** подменил обе правки на старое поведение — оба красные, вернул — оба зелёные.

`pnpm -r test`: 2459 + 7 зелёных; `biome check` чисто; `tsc --noEmit` чисто.

## 4. Живая приёмка — уже снята моей рукой, твоя остаётся за тобой

С кодом ветки, `pnpm protocol doctor --ref origin/main` на этом боксе:

```
✓ agent: binary (codex): /home/lle/.nvm/versions/node/v24.18.0/bin/codex (kind)
✓ agent: headless run (codex): answered in 3.2s
✓ account: 'lle-main' token: answered in 2.6s
✓ account: 'lle-second' token: answered in 2.6s
✓ account: 'codex-main' token: answered in 3.3s
doctor: green — 15 checks passed, 2 facts, nothing failed
```

Тот же прогон с кодом `origin/main` (для сравнения, снят до правки): `✗ account: 'codex-main' token: error: unknown option '--skip-git-repo-check'`, `1 of 17 failed`. Состав и число строк не менялись — 17 в обоих случаях.

## 5. Класс восхождения — подтверждаю условия 2 и 3 своими словами о своём диффе

Дифф трогает пять файлов кода и тестов пакета плюс `packages/agent-protocol/README.md`. Он **не вводит нового ключа конфига** (`agent-protocol.json` не тронут, схема машинного конфига не тронута — `accounts.<id>.kind` существовал до меня), **не вводит новой формы сообщения** (состав и число строк `doctor` те же; изменился текст детали двух строк — ровно то, чего постановка требует явно), **не вводит нового права**, **не добавляет шага маршрута** и **не снимает ни одного запрета**. Доки власти (`PROTOCOL.md`, `docs/roles/**`, `REVIEWER.md`, `agent-protocol.json`, `.github/workflows/**`) не тронуты — по гарду 4 это обычный PR, кнопка твоя.

Третью находку (§6 постановки — зелёная проба при неработающей песочнице) не трогал.

## 6. Чего жду

CI по #88 на голове `e712ba2`. У конвейера не стою — метку `review` поставлю на следующем такте, когда `checks` будет зелёным на этой же голове; если прогон красный, чиню и докладываю. Рабочее дерево чистое, незакоммиченного нет.

waiting-on → dev-core.

