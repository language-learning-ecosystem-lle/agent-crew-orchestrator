---
from: curator
worker: claude-code
session: 61f5d660-5f96-447a-a71a-a5829e356c1a
raised: 2026-09-16T17:20:24Z
date: 2026-09-16T17:27:10Z
expects: answer
waiting-on: curator
---

**PR #458 смёржен моей рукой в `main` (`a3057dcde1ae31a0c1c3331b92e888beb8bbeb97`, 17:21:07Z). Приёмка §5: (а) исполнена — заметка с поводом написана, корпус впервые несёт повод (1 из 180 против 0 из 177). (б) этим тактом НЕ исполнима честно, и это замерено; порядок исправлен так, чтобы срабатывание пришло в первом же такте, который тред закрывает по-настоящему.**

## 1. Merge — след с поимёнными гардами (гард 5)

`pnpm protocol merge-gate --ref origin/main --pr 458 --review-workflow 'Claude PR Review'` на голове `6dbfa256c89367e389a567b17c26df8d0ee146c9`:

- **гард 1 — `ok`**: `approved on 6dbfa25 by github-actions`, внутри ЗАКРЫТОГО успешного круга `Claude PR Review` **35127103219** на ЭТОЙ голове (окно `2026-09-16T17:16:01Z…17:19:45Z`); вердикт — письмо `reviewer-pr` в этом треде, `verdict: approve`, `pr: 458`;
- **гард 2 — `ok`**: два зелёных чека на той же голове — `review=SUCCESS`, `checks=SUCCESS`; прогон `checks` — **35126332745**, `conclusion=success`;
- **гард 3 — закрыт словом john в ленте**: тред `213-role-memory-index-over-ceiling`, письмо `2026-09-16T16-45-40Z-curator.md`, §1, дословно: «К-1 — берём. Вопрос о поводе задаётся при КАЖДОЙ записи в память, а не опцией». Полевым классом («измеренный дефект, новой нормы не вводит») НЕ прикрываюсь: абзац меняет поведение всех ролей, то есть норма, и норма разрешена john поимённо;
- **гард 4 — `ok`**: «3 changed path(s), none of them a document of power»; список доков власти дверь взяла данными (8 путей; `PROTOCOL.md` и `.github/workflows` — объявлены `powerDocuments`);
- **гард 5 — вот он.**

Merge — squash, заголовок Conventional Commits. **Контрольное чтение цели после merge:** `base.ref = main`, `state = MERGED`, `mergedAt = 2026-09-16T17:21:07Z`, squash-коммит `a3057dcde1ae31a0c1c3331b92e888beb8bbeb97`; `git log --oneline -1 origin/main` показывает его же.

## 2. Приёмка §5, шаг (а) — исполнен

Заметка написана моей рукой в этом такте: `k1-extinction-firing-is-ordered-in-the-closing-tick.md`, во фронтматтере под `metadata:` стоит `thread: 216-memory-cause-asked-at-every-write`.

**Замерено прибором пакета, а не глазом** — `causeOf`/`isThreadCause` прогнаны по живому дому памяти `curator`:

- `cause: k1-extinction-firing-is-ordered-in-the-closing-tick.md -> 216-memory-cause-asked-at-every-write | thread-id form: true`;
- **notes with a declared cause: 1 of 180** — против замера постановки §1 (**0 из 177**).

То есть поле, которого корпус не нёс ни разу за всё время, теперь в нём есть и читается тем самым кодом, который его гасит. Повод открыт → `planExtinction` оставляет заметку живой и молчит: это нормальное состояние живой заметки, а не отсутствие механизма.

**Проверка повода дала ДА честно:** заметка говорит, чем закрывается приёмка К-1 и что делать такту, закрывающему тред; закроется `216` — она мусор.

## 3. Шаг (б) этим тактом НЕ исполним — замер, а не суждение

(б) требует закрыть тред ТЕМ ЖЕ тактом. Закрытие треда — это приёмка, а фальшивой приёмки не бывает: «закрыть что-нибудь ради срабатывания» — ровно то «сам поставил → сам принял», от которого стоят гарды. Перебраны все треды, где у меня есть ход (`cli mail --role curator` → `029`, `215`, `217`) плюс тред-источник:

- **`213-role-memory-index-over-ceiling` — УЖЕ закрыт** (прочитано: `status: closed`). Назвать его поводом нельзя: заметка, чей предмет уже закрыт, бесполезна в момент написания, и проверка §3 п.1 запрещает писать поле;
- **`215-restart-letter-field-name-outlived-its-anchor` — живая работа:** вердикт `approve` по PR #457 на голове `9be01b49408d9dbea0d6275276ba598f5b342f1e`, PR ждёт кнопки curator. Закрывать нечего;
- **`217-memory-ceiling-alarm-prints-the-byte-split` — живая работа** (К-3 ждёт хода curator). Сверх того: полевая приёмка К-3 рукой не заказывается вовсе — потолок `24576` не двигается, строка печатается, только когда указатель сам перерастёт его (тред `213`, msg-002). Поводом его брать — значит привязать срабатывание К-1 к условию, которое ждут сутками;
- **`029-opencode-agent-kind`** — чужой предмет, этим прогоном не судится: прогон связан ровно одним тредом.

**Этот прогон связан тредом `216` и только им**, поэтому чужие треды я не разбирала и ход в них не делала.

## 4. Порядок исправлен — срабатывание приходит в такте, который тред ЗАКРЫВАЕТ

Дефект порядка (а)→(б) виден теперь буквально: **заметка пишется, пока повод ОТКРЫТ, а закрытие — отдельный честный акт**, поэтому оба шага обязаны стоять в ОДНОМ такте — в том, который тред честно закрывает. Такт на `216` таким тактом не является по построению: `216` до (в) не закрывается, а чужие треды он не судит.

Исправление не меняет ни критерия (в), ни того, что считается провалом, — меняется только, ЧЕЙ такт исполняет (а)+(б). Это операционное решение curator внутри уже принятой john приёмки, а не новая норма.

**Как инструкция доезжает до того такта — тем же механизмом, который принимаем.** Заметка памяти вклеивается в начало КАЖДОГО подъёма роли, на любом треде. Заметка §2 несёт порядок дословно:

1. такт, закрывающий тред X, **ДО закрытия** пишет заметку с `thread: X` — и только такую, у которой проверка честно даёт ДА;
2. закрывает X;
3. **следующий** подъём роли печатает в журнале ящика `memory: 1 note(s) of 'curator' were extinguished by their subject being closed (<файл> ← X)` — это приёмка; она докладывается СЮДА, и только тогда `216` закрывается.

Ближайший кандидат виден уже сейчас: такт на `215` домерживает #457 и, когда предмет `215` будет исполнен, закрывает его — тогда (а)+(б) стоят рядом честно.

**Провал §5 читается как объявлено:** заметка пережила закрытие своего повода; строки гашения нет; исчезла заметка, повода НЕ объявлявшая — последнее есть необратимая потеря замера и откатывается немедленно.

## 5. Границы §7 — соблюдены

Заметок не удалено ни одной; потолок `MEMORY_INDEX_LIMIT_BYTES = 24_576` не тронут; К-2 не брался; память `dev-core`, `devops` и ролей LLE не мерилась; **строка-сирота указателя** (`fallback-step-reddens-a-successful-round.md`, файла нет) не тронута — она свидетельство полевой потери и доживает до такта, который будет мерить действие К-1. `PROTOCOL.md` этим PR не тронут (§6).

## 6. Что дальше и от кого

- **ход у curator**, парка нет: срабатывание (в) не звонит ни событием, ни прогоном — его печатает подъём роли в журнал ящика. Такт, поднятый на `216`, делает ровно одно: читает журнал ящика на строку `memory: … were extinguished …`; **есть** → доклад сюда и закрытие `216`; **нет** → называет, закрылся ли с 16.09 17:45Z хоть один тред curator, и передаёт ход. Заметка §2 несёт этот же порядок в каждый подъём, на любом треде;
- **`216` открыт до (в)** — здесь приёмка есть исполнение, а не merge;
- **хвост §8 жив:** после (в) вопрос потолка возвращается к john с числом — запас после уборки 3567 байт ≈ 30 голых строк ≈ менее двух суток измеренного темпа;
- в рабочем дереве роли незакоммиченного нет: весь след такта — merge #458, заметка памяти и это письмо.

waiting-on → curator.

