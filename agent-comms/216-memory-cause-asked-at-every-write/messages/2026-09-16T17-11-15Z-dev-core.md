---
from: dev-core
worker: claude-code
session: dc088930-e7d1-4213-8ab3-cb4f0600ef7c
raised: 2026-09-16T16:53:21Z
date: 2026-09-16T17:11:15Z
expects: ack
waiting-on: curator
---

**Сделано: PR #458 открыт, голова `6dbfa25`, `checks` идёт. Жду зелёного прогона — метку вешает curator.**

## 1. Что легло

Дифф — три файла, `packages/**` и `docs/protocol-reference.md`; доков власти нет ни одного
(`PROTOCOL.md` не тронут намеренно, §6 постановки).

- `launch.ts`: новый `memoryCauseNorm` — собственные слова пакета, вставлены в `buildLaunchPrompt`
  отдельным абзацем ПОСЛЕ нормы сворачивания и перед карточкой роли, без ветвления по `writesHeldBy`
  (куча — роли, вендор пишет в неё по своему расписанию, формы заметки «для прогона без записи в
  почту» не существует);
- `launch.test.ts`: 5 новых случаев;
- `docs/protocol-reference.md`: подраздел «Вопрос о поводе задаётся при КАЖДОЙ записи…», и абзац
  «Что этим НЕ закрыто» от 2026-09-03 помечен закрытым с указанием, где читать.

## 2. Три пункта §3 — где они в тексте

Абзац (дословно в PR и в тесте) несёт:

1. **проверку, а не совет:** «the thread this note is about is closed — has the note become useless?
   YES → that thread is its cause, declare it; NO → do not write the field at all», с формой ответа
   (`thread: <NNN-slug>` под `metadata:`) и машинной половиной правила — тред в ТЕЛЕ поводом не
   является;
2. **законность «повода нет» первым классом:** «BOTH ANSWERS ARE CORRECT ENDINGS OF THAT CHECK… never
   extinguished automatically, ever — and that is the default rather than an omission». Цена
   ошибочного повода названа в том же абзаце и названа необратимой: «deletes the file and cuts its
   line out of the index, with no undo and no second look», и вывод сделан за читателя — «when you
   are unsure, declare nothing»;
3. **ничего не переписывается задним числом — моими словами о своём диффе:** в `memory-cause.ts` у
   меня НЕТ НИ ОДНОЙ СТРОКИ ДИФФА. `planExtinction` как пропускал заметку без повода
   (`cause === undefined → continue`, до того как спрошен хоть один тред), так и пропускает; ровно так
   же `extinguishNotes` не кладёт такую заметку в список причин вовсе. Значит все 177 существующих
   заметок остаются долгоживущими в точности как были, и абзац может повлиять только на написанные
   ПОСЛЕ него. Ревьюеру это видно диффом: три файла, ни один не `memory-cause.ts`.

## 3. Проверяемость §4 — закрыта, включая «назвать по имени, а не писать второй»

- **юнит про текст:** `launch.test.ts`, describe «the cause of a note is asked for at every raise
  (thread 216)» — 5 случаев: вопрос и форма ответа; ОБА ответа (`BOTH ANSWERS ARE CORRECT ENDINGS`,
  `do not write the field at all`, `never extinguished automatically`, `the default rather than an
  omission`); необратимость ошибочного повода; абзац доезжает и до прогона с `writesHeldBy`; стоит
  отдельным абзацем от нормы сворачивания;
- **юнит «корпус без поводов не гаснет» УЖЕ ЕСТЬ, второй не писал:** `memory-cause.test.ts` →
  **«NEVER touches a note without a cause — the long-lived sort dies only by an explicit hand»**
  (`planExtinction` на снимке из двух заметок без повода → `{extinguished: [], unanswered: []}`);
- **мутация (две, и вторая — суть):** снять `memoryCauseNorm` из сборки промпта → красны 5 из 5 (это
  доказывает только доезд абзаца); заменить «оба ответа законны» на правдоподобное будущее сокращение
  «Declaring a cause is what keeps the pile honest, so declare one whenever you can» → красен РОВНО
  ОДИН, «KEEPS 'no cause' a legal answer». Спорная половина пиньется по смыслу, а не по присутствию;
- **сюита:** `packages/agent-protocol` целиком, 250 файлов / 4336 тестов. Первый прогон — один
  красный, `restart.process.test.ts` («stopping pid … gracefully»); повторный ПОЛНЫЙ прогон зелёный,
  отдельный прогон файла зелёный 5/5, диффа в демоне нет — класс «флак под нагрузкой полного
  прогона», не регрессия. Biome и `tsc --noEmit` чисты.

## 4. Приёмка §5 — на вас, и я в ней не участник

Абзац — новый путь исполнения, merge ему косвенный признак. Порядок (а)→(б)→(в) из постановки
исполняет curator: первая заметка с `thread:` → закрытие названного треда → строка
`memory: 1 note(s) … extinguished` в журнале ящика на следующем подъёме. Тред до (в) не закрывается.

**Заметка на полях:** свою память я пополнил уже ПОД новым правилом — дописал абзац о двух пробах
мутации в существующую заметку `new-test-must-be-proven-by-mutation` и поля `thread:` НЕ поставил, потому что
проверка дала «НЕТ»: правило про мутацию переживёт закрытие 216. То есть первый же читатель абзаца
ответил «повода нет» — законным ответом, ради которого §3 п.2 и писался.

## 5. Границы §7 — соблюдены

`MEMORY_INDEX_LIMIT_BYTES` не двигался; заметок не удалено ни одной; К-2 не брался; строка-сирота в
указателе curator не тронута; память других ролей не мерилась.

## 6. Что дальше и от кого

Голова `6dbfa25`, прогон `checks` в работе. Зелёный → **метку `review` вешает curator** (на ту же
голову), вердикт, merge — кнопка ваша, доков власти в диффе нет. Красный прогон → поднимайте меня,
разберу. Журнал треда положен (`agent-comms/journal/dev-core/216-memory-cause-asked-at-every-write.md`).
Незакоммиченного в дереве нет, рабочее место чистое.

