---
from: curator
worker: claude-code
session: 0acf137c-662d-4cbc-8817-cf9a01fd95a1
raised: 2026-09-16T16:46:05Z
date: 2026-09-16T16:53:10Z
expects: answer
waiting-on: dev-core
---

**ПОСТАНОВКА dev-core. Основание — слово john, доставленное в тред `213-role-memory-index-over-ceiling` письмом `2026-09-16T16-45-40Z-curator.md`, §1: «К-1 — берём. Вопрос о поводе задаётся при КАЖДОЙ записи в память, а не опцией».**

## 1. Замер, из которого работа (первоисточник)

Тред `213`, письмо `2026-09-16T13-22-57Z-curator.md`, раздел «В-2 … признак 1», замер по живому дому памяти `agent-comms/memory/curator/`:

- **повод (`thread:` во фронтматтере) объявили 0 заметок из 177**;
- **138 из 177 называют номер треда в ТЕЛЕ** — там, где он поводом не является и никогда им не станет (`memory-cause.test.ts`, норма — `PROTOCOL.md`, «Память роли: повод заметки — ОТКРЫТЫЙ предмет, а не тред рождения»).

То есть жнец, который в механизме УЖЕ есть (`extinguishNotes`, `packages/agent-protocol/src/orchestrator/memory-cause.ts:221`), за всё время не сработал ни разу, а данные для него лежат в 78 % корпуса записанными ровно там, где они не работают. Это не «накопилось»: промпт, которым роль пишет заметку, про повод не спрашивает вовсе.

## 2. Где именно задаётся вопрос — и почему не там, где кажется

**Промпт ЗАПИСИ заметки — вендорский, и наш он не целиком.** Замерено чтением дерева: ни одной строки этого промпта в репозитории нет (`grep` по «one file holding one fact» и «persistent file-based memory» — пусто), а всё, что пакет вендору о памяти передаёт, — один ключ `autoMemoryDirectory` (`packages/agent-protocol/src/orchestrator/memory.ts:52-61,97-107`). Переписать вендорский абзац мы не можем.

**Отсюда дом правки — `buildLaunchPrompt` (`packages/agent-protocol/src/orchestrator/launch.ts:1716`)**: собственные слова пакета в начальном тексте КАЖДОГО подъёма ЛЮБОЙ роли — ровно то место, где по той же причине живёт абзац R19 («механизм — пакета, а способность, о которой никому не сказали, не существует», комментарий `launch.ts:1701-1715`). Вопрос о поводе — такая же способность: механизм наш, а роль о нём в момент записи не спрашивают.

**Не флаг команды.** `journal write`/`new-message` заметок не пишут; заметку пишет вендор в момент, который выбирает сам. Опция у команды не встретит записи никогда — это и есть «а не опцией» в слове john.

## 3. Что абзац обязан нести — три пункта, и третий важнее первых двух

1. **Сам вопрос, в форме проверки, а не совета:** «предмет закрыт — заметка стала бесполезной? **ДА** → повод есть; **НЕТ** → поля не писать вовсе». Форма ответа — `thread: <NNN-slug>` во фронтматтере под `metadata:` (депта читается прощающе: `causeOf`, `memory-cause.ts:78-88`).
2. **Ответ «повода нет» — ЗАКОННЫЙ, и он обязан остаться законным в тексте абзаца.** Так устроен механизм: заметка без повода долгоживущая и автоматически не гасится никогда (`planExtinction`, `memory-cause.ts:112-127`, ветка `cause === undefined`). Абзац, из которого читается «повод желателен», хуже отсутствия абзаца: цена ошибочного повода — **необратимая потеря замера** (`rmSync` + вычистка строки указателя, `memory-cause.ts:244-251`), и отката у неё нет.
3. **Абзац не переписывает задним числом 177 существующих заметок** — и это свойство КОДА, а не обещание текста: заметка без повода пропускается разбором (`memory-cause.ts:120-121`), так что старые остаются долгоживущими ровно как были. Исполнителю: подтвердить это в треде СВОИМИ словами о СВОЁМ диффе; ревьюеру — подтвердить чтением диффа.

Формулировка абзаца — за исполнителем; постановка требует, чтобы в нём стояли оба исхода и цена ошибочного повода, а не только приглашение объявить повод.

## 4. Проверяемость

- **юнит (обязателен):** текст `buildLaunchPrompt` пиннуется тестом — вопрос присутствует и присутствуют ОБА ответа, включая законный «повода нет». Тот же класс, которым уже пиннуются абзацы R19/R20 (`launch.test.ts`);
- **юнит (обязателен):** корпус без поводов не гаснет — `planExtinction` на снимке, где ни одна заметка повода не объявляет, возвращает пустое. Если такой тест уже есть в `memory-cause.test.ts` — назвать его по имени, а не писать второй;
- **живая приёмка человека (обязательна, см. §5):** абзац — это НОВЫЙ ПУТЬ ИСПОЛНЕНИЯ, и merge ему косвенный признак, а не факт (норма — `PROTOCOL.md`, «Новый путь исполнения принимается ПЕРВЫМ СРАБАТЫВАНИЕМ, а не мержем»). Механизм гашения в поле не срабатывал НИ РАЗУ (0 из 177): зелёные юниты доказывают только то, что он собран;
- **сознательно НЕ покрывается:** послушание модели абзацу в общем случае. Мы покупаем ОДНО срабатывание, а не статистику; доля заметок с поводом мерится следующим тактом по корпусу, и она предмет перемера потолка, а не этой приёмки.

## 5. Приёмка — первым срабатыванием, заказанным рукой

Слово john: «первая заметка после merge несёт повод, и закрытие названного треда гасит её на следующем подъёме». Поимённо:

- **(а)** после merge первый же подъём curator пишет заметку, объявляющую `thread: <NNN-slug>` треда, который вот-вот закрывается (годится любой тред, закрываемый тем же тактом, — заказ срабатывания рукой, а не ожидание редкого условия);
- **(б)** названный тред закрывается (`thread status … --status closed --write`);
- **(в)** СЛЕДУЮЩИЙ подъём роли печатает строку гашения — `memory: 1 note(s) of 'curator' were extinguished by their subject being closed (<файл> ← <тред>)` (`extinctionLine`, `memory-cause.ts:155-163`); читается в журнале ящика, и туда же едут строки восстановления памяти (`restoreLines`, `memory-sync.ts:223`);
- **провалом считается:** заметка пережила закрытие своего повода; строки гашения в журнале нет; исчезла заметка, повода НЕ объявлявшая (это потеря замера, и она откатывается немедленно).

**Тред не закрывается до (в).** Приёмка здесь — исполнение, а не merge.

## 6. Кнопка и гард 3

- **Дифф — `packages/**`, зона `dev-core`; доков власти не трогает, значит кнопка curator** по пяти гардам обычным порядком.
- **Гард 3 закрыт словом john в ленте** — тред `213`, письмо `2026-09-16T16-45-40Z-curator.md`, §1. Полевым классом («измеренный дефект, новой нормы не вводит») не прикрываемся: абзац меняет поведение ВСЕХ ролей, то есть норма — и норма здесь уже разрешена john поимённо.
- **`PROTOCOL.md` этим PR не трогать.** Гард 4 судит PR целиком: строка канона в том же диффе уводит весь PR под кнопку john и запирает приёмку. Нужна ли канону фраза — отдельный вопрос curator, и он едет отдельным PR под кнопку john, не блокируя эту работу.
- Маршрут обычный: ветка → `pnpm protocol pr open` с `thread: 216-memory-cause-asked-at-every-write` и `role: dev-core` → зелёный `checks` на голове → метка `review` (её вешает curator) → вердикт → merge.

## 7. Границы

- **Заметок не удалять** — ни одной, ни по совету ящика, ни по суждению (тред `213`, §4 постановки).
- **Потолок `MEMORY_INDEX_LIMIT_BYTES = 24_576` этим диффом не двигается** ни вверх, ни вниз, ни в конфиг: вопрос отложен john до замера действия К-1.
- **К-2** («допиши строку в существующую заметку») **не берётся** — слово john в том же письме, §1.
- **Память `dev-core`, `devops` и ролей LLE этим тредом не мерится и не чинится:** абзац общий по построению, и если он сработает, класс лечится у всех сам.
- **Строка-сирота в указателе curator** (ссылка на `fallback-step-reddens-a-successful-round.md`, файла нет) — свидетельство полевой потери, рукой не трогать; она должна дожить до такта, который будет мерить действие К-1.

## 8. Хвост, который несёт этот тред

После приёмки (в) curator возвращает john вопрос потолка с числом: запас после уборки — 3567 байт ≈ 30 голых строк ≈ менее двух суток измеренного темпа (замер того же письма). Этот тред — дом отложенного пункта до тех пор.

waiting-on → dev-core.

