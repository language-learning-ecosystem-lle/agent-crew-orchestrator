---
from: reviewer-pr
worker: gh-action
date: 2026-09-16T17:19:38Z
expects: answer
waiting-on: curator
park-lifted: run:458
verdict: approve
pr: 458
---

verdict: approve
pr: 458
waiting-on: curator

Находок по критериям REVIEWER.md нет.

Проверено:
- `thread: 216-memory-cause-asked-at-every-write`, `role: dev-core` в описании PR — оба поля есть; тред
  прочитан целиком (`.comms-mail/agent-comms/216-memory-cause-asked-at-every-write/_thread.md`),
  постановка john (тред 213, §1) процитирована и исполнена без молчаливых расширений/сужений
  (критерий 3).
- Зоны (критерий 4): `pnpm protocol zones check --ref HEAD --role dev-core --paths docs/protocol-reference.md
  packages/agent-protocol/src/orchestrator/launch.ts packages/agent-protocol/src/orchestrator/launch.test.ts`
  → «none under a forbidden prefix».
- Доки власти (критерий 5): дифф — 3 файла, `PROTOCOL.md`/`docs/roles/**`/`agent-protocol.json`/
  `.github/workflows/**` не тронуты; `docs/protocol-reference.md` доком власти не является (REVIEWER.md).
- `pnpm --filter agent-protocol typecheck` — чисто.
- `pnpm exec biome check packages/agent-protocol/src/orchestrator/launch.ts
  packages/agent-protocol/src/orchestrator/launch.test.ts docs/protocol-reference.md` — «Checked 2
  files. No fixes applied.»
- Точечно (критерий 2): `pnpm exec vitest run src/orchestrator/launch.test.ts` (из
  `packages/agent-protocol`) — 149/149 зелёных, включая новый describe «the cause of a note is asked
  for at every raise (thread 216)», 5/5. Все строки, на которые бьют ассерты (`SUBJECT THAT CAN CLOSE`,
  `has the note become useless?`, `` `thread: <NNN-slug>` ``, `under `metadata:``, `never read as a
  cause`, `BOTH ANSWERS ARE CORRECT ENDINGS`, `do not write the field at all`, `never extinguished
  automatically`, `the default rather than an omission`, `no undo`, `when you are unsure, declare
  nothing`) сверены построчным чтением `memoryCauseNorm` (`launch.ts:1743`) — присутствуют дословно;
  тест бьёт в заявленное, не в соседнее.
- `pnpm exec vitest run src/orchestrator/memory-cause.test.ts` — 13/13 зелёных, включая названный по
  имени тест «NEVER touches a note without a cause — the long-lived sort dies only by an explicit
  hand» (`memory-cause.test.ts:70`) — второй тест на тот же факт не писался, заявление подтверждено.
- Чтением диффа (критерий 9, п.3 постановки): `memory-cause.ts` в диффе нет ни строкой — заявление
  «177 существующих заметок не трогаются» подтверждается отсутствием диффа в файле, несущем
  `planExtinction`.
- `memoryCauseNorm` вставлен в `buildLaunchPrompt` безусловно (после `windDownNorm`, перед карточкой
  роли), включая ветку `writesHeldBy !== undefined` — соответствует тесту «is said to a run that cannot
  write into the mail too» и заявлению постановки «форма для прогона без записи в почту не
  существует».
- Числа (критерий 1): голова PR — прогон checks `35126332745` (лог `pnpm test`) —
  `packages/agent-protocol test: Test Files 250 passed (250)`, `Tests 4334 passed | 2 skipped (4336)` —
  совпадает с заявленным «250 файлов, 4336 тестов», область названа («packages/agent-protocol
  целиком»). Полный `pnpm test` не повторял — `checks` на голове `6dbfa256c89367e389a567b17c26df8d0ee146c9`
  зелёный, прогон `35126332745`.
- Живой исход `pnpm protocol merge-gate --ref origin/main --pr 458`: `REFUSED` — guard 1 (approve на
  голове) не выполнен, потому что это ревью его как раз создаёт; guard 2 (`checks` зелёные) отвечает
  «not green: review=IN_PROGRESS» — это текущий прогон ревью, `checks` при этом зелёные
  (`35126332745`); guard 3 требует ссылки на решение john — оно названо (тред `216`, §6, со ссылкой на
  тред `213`); guard 4 — «none of them a document of power» (ok); guard 5 (трасса merge) — на curator
  при самом мердже. Блокеров для curator в этом выводе нет.

---

Доставлено шагами прогона [`35127103219`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/35127103219) по PR #458, голова `6dbfa256c89367e389a567b17c26df8d0ee146c9` (вердикт написан агентом ревьюера, доставка — джобой: тред 088).
Ход передан роли `curator` — так объявил сам вердикт.

