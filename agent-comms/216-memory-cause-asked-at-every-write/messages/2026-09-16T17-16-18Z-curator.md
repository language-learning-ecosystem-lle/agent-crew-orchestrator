---
from: curator
worker: claude-code
session: a305cfac-a816-4c73-8f30-f823bbcd2afa
raised: 2026-09-16T17:11:45Z
date: 2026-09-16T17:16:18Z
expects: answer
waiting-on: curator
parked-on: run:458
---

**Дифф прочитан, гарды 3 и 4 замерены заранее, метка `review` повешена на голову `6dbfa256c89367e389a567b17c26df8d0ee146c9`. Паркуюсь на круге, кнопка — моя, следующим ходом merge.**

## 1. Что прочитано своей рукой (не пересказ доклада)

- **Дифф — три файла**, `docs/protocol-reference.md`, `packages/agent-protocol/src/orchestrator/launch.ts`, `…/launch.test.ts`. Доков власти нет ни одного; `memory-cause.ts` в диффе отсутствует — то есть §3 п.3 постановки («задним числом не переписывает ничего») держится построением, а не обещанием: заметке без повода `planExtinction` не задаёт ни одного вопроса, а трогать его этот PR не может, потому что не трогает вовсе.
- **Абзац `memoryCauseNorm` несёт все три пункта §3** — прочитано в тексте, а не в докладе: вопрос стоит проверкой («the thread this note is about is closed — has the note become useless? YES → … ; NO → do not write the field at all»); форма ответа названа (`thread: <NNN-slug>` под `metadata:`) вместе с машинной половиной («naming a thread in the BODY is prose and is never read as a cause»); «повода нет» стоит первым классом — `BOTH ANSWERS ARE CORRECT ENDINGS OF THAT CHECK`, «never extinguished automatically, ever — and that is the default rather than an omission»; цена ошибочного повода названа необратимой («no undo and no second look») и вывод сделан за читателя — «when you are unsure, declare nothing».
- **Приборы §4 на месте:** в `launch.test.ts` — describe «the cause of a note is asked for at every raise (thread 216)» и ровно 5 случаев; второй юнит НЕ написан заново, назван существующий — `memory-cause.test.ts:70`, «NEVER touches a note without a cause …» (проверено грепом по файлу, а не по докладу).
- **Мутационная проба §3 прочитана как заявление исполнителя** и мною не перегонялась: важна вторая её половина — правдоподобное будущее сокращение «declare one whenever you can» красит РОВНО ОДИН тест. Это и есть прибор спорной половины абзаца; он в дереве.

## 2. Гарды — состояние до круга

`pnpm protocol merge-gate --ref origin/main --pr 458 --review-workflow 'Claude PR Review'` на голове `6dbfa25`:

- **гард 4 — `ok`**: «3 changed path(s), none of them a document of power», список доков власти дверь взяла данными (8 путей, из них `PROTOCOL.md` и `.github/workflows` — объявлены `powerDocuments`);
- **гард 3 — закрыт словом john в ленте**: тред `213-role-memory-index-over-ceiling`, письмо `2026-09-16T16-45-40Z-curator.md`, §1, дословно: «К-1 — берём. Вопрос о поводе задаётся при КАЖДОЙ записи в память, а не опцией». Полевым классом не прикрываюсь: абзац меняет поведение всех ролей, то есть норма — и норма здесь разрешена john поимённо;
- **гарды 1 и 2 — `STOP` ожидаемо**: вердикта на голове нет, `checks` на момент первого замера шёл. Метка повешена после зелени (§3).

**Перед меткой сверен блоб круга** (метка на несовпавшем merge-ref возвращает пустой прогон и тратит такт): `origin/main:.github/workflows/claude-review.yml` и `origin/pr-458-merge:…` — оба `cbc10cc7eb5df305887fc140089d96edba76b858`, совпали.

## 3. Метка

- **`checks` зелёный на голове**: прогон `35126332745`, `conclusion=success`, `2026-09-16T17:15:48Z`, голова `6dbfa256c89367e389a567b17c26df8d0ee146c9`; после зелени голова сверена заново — та же;
- **блоб круга сверен ПОВТОРНО уже на свежем merge-ref** (после зелени, перед меткой) — совпал;
- **метка `review` навешена `2026-09-16T17:16:00Z`** на ту же голову `6dbfa25`.

**Почему слот роли держался до зелени, а не паркуйся-передавай.** Под живым `checks` парк `run:458` снимает письмо об исходе, но тот же шаг ставит `--waiting-on` ролью из строки `role:` описания PR, то есть **dev-core** — а действия у dev-core на зелёном прогоне нет: метка моя. Передача хода стоила бы двух подъёмов (dev-core → curator) и тех же минут; прогон шёл 7 м 08 с (`17:08:40Z → 17:15:48Z`), в треде другой работы своей руки не было, и решение прочитано ОДИН раз в момент решения. Это не образец: правило «ход под живым `checks` передаётся без парка» держится там, где в треде есть работа на эти минуты.

## 4. Что дальше и от кого

- **вердикт ревьюера → merge моей рукой** по пяти гардам, след с поимёнными гардами в этот тред. Кнопка моя: доков власти в диффе нет;
- **дальше — приёмка §5, и она моя целиком**: (а) первый мой подъём ПОСЛЕ merge пишет заметку с `thread: <NNN-slug>` треда, закрываемого тем же тактом; (б) тред закрывается; (в) следующий подъём печатает строку `memory: 1 note(s) of 'curator' were extinguished …` в журнале ящика. **Тред `216` до (в) не закрывается** — здесь приёмка есть исполнение, а не merge;
- **заметка на полях dev-core засчитана как показание, а не как приёмка**: первый читатель абзаца ответил «повода нет» на существующей заметке про мутацию — это законный ответ, ради которого §3 п.2 писался, но срабатывания механизма (в) он не заменяет;
- границы §7 соблюдены и мною тоже: потолок `MEMORY_INDEX_LIMIT_BYTES` не трогался, заметок не удалено, К-2 не брался, строка-сирота в указателе `curator` не тронута.

Ход остаётся на curator, парк — на круге ревью PR #458.

waiting-on → curator.

