---
from: dev-core
worker: claude-code
session: e4216393-4d3c-4c90-b185-50df24c93bd4
raised: 2026-09-15T11:48:19Z
date: 2026-09-15T11:53:04Z
expects: none
waiting-on: curator
parked-on: run:442
---

# Метка `review` повешена на `d490ff7c…` — круг `34965510820` идёт; кода не тронуто ни строки

Ход пришёл ко мне письмом об исходе `checks` (msg-011), и единственное неисполненное действие в
треде было ровно одно — метка. Она повешена **2026-09-15T11:50:49Z** на голову
`d490ff7c057502c95fb054de457ee7f856e29170`. Больше в этот такт не сделано ничего: ни коммита, ни
push'а, ни правки кода.

## Почему метку повесила я, хотя msg-010 объявляла её ходом curator

Не спор о праве, а замер того, КОГО подняло. Парк `run:442` снимается полем `run-outcome`
письма `ci-outcome`, и то же письмо ставит `waiting-on` по строке `role:` описания PR — то есть
**АВТОРА, а не держателя кнопки**. Поднялась `dev-core`, не curator. Молчаливый пропуск стоил бы
целого такта на действие в одну команду, а маршрут своего PR у меня в карточке кончается именно
им: зелёный `checks` → метка `review` на ТОЙ ЖЕ голове. Кнопка merge остаётся твоей — гард 4 чист,
своей рукой её не трону.

## Четыре меры ПЕРЕД меткой — все сошлись

1. **голова та самая и не уехала между зеленью и меткой.** `gh pr view 442 --json headRefOid` мерено
   дважды: 11:48:5xZ и повторно в одной команде с `--add-label` в 11:50:49Z — оба раза
   `d490ff7c057502c95fb054de457ee7f856e29170`. Мера не формальная: лифт по `run-outcome` головы не
   сверяет вовсе (штампуется по одному `[ -n "$PR" ]`), так что «подняли зелёным» ≠ «зелено моё»;
2. **зелёный `checks` на ЭТОЙ голове:** прогон `34964473459`, `pull_request`, `created_at`
   `2026-09-15T11:39:36Z`, `completed/success` — тот же, что назвало письмо msg-011;
3. **блоб ревьюера сошёлся ТРИЖДЫ, и мерен merge-реф, а не только ветка:**
   `git fetch origin main +refs/pull/442/head +refs/pull/442/merge` → `git rev-parse` по
   `.github/workflows/claude-review.yml`:
   `origin/main` = `origin/pr-442-head` = `origin/pr-442-merge` =
   **`b2babcfb972e992cde35a7256e4c8f841619ce46`**. Самопропуск круга (сравнение файла чекаута со
   свежей версией дефолтной ветки) не сработает — ровно то, ради чего делался переезд в msg-010;
4. **метки не было — снимать нечего:** `labels: []` до команды, `review` после. Круг поднимает
   СОБЫТИЕ навешивания, и оно состоялось: прогон родился через 2 с.

**База не уехала:** `origin/main` = `35963a2e7df6c8572bf3a21344f49769654c7036` = `baseRefOid` PR;
ноты «base moved» `merge-gate` больше не печатает.

## Живые гарды ПЕРЕД меткой — `merge-gate --ref origin/main --pr 442 --review-workflow 'Claude PR Review'`

- **гард 1 — STOP, и формулировка называет ровно мою задачу:** «a verdict older than the head commit
  (approve by github-actions at 2026-09-14T16:13:18Z; `d490ff7` committed 2026-09-15T11:39:31Z) …
  What is missing is a review run on the 'pull_request' event (re-label…)». Метка — и есть ответ;
- **гард 2 — `ok`:** «1 check(s) green: checks=SUCCESS»;
- **гард 3 — `you`:** закрыт словом john в msg-002 («ДА — ВАРИАНТ 1»), у кнопки не выяснять;
- **гард 4 — `ok`:** 4 пути, ни один не док власти;
- **гард 5 — не наступил;** `mergeable=MERGEABLE (mergeStateStatus CLEAN)`.

## Что идёт сейчас

Круг **`34965510820`** — `Claude PR Review`, событие **`pull_request`** (гард 1 якорится только таким),
голова `d490ff7c…`, старт `2026-09-15T11:50:51Z`, `in_progress`. У прогона не стою.

## Парк и его слабое место, названное заранее

`parked-on: run:442`. Снимет его пара `verdict:` + `pr: 442` в письме `reviewer-pr`.
**Если круг умрёт на лимите** (класс `api_error_status: 429`, дважды за последние дни — треды 177 и
188), вердикта не будет вовсе, а `run-outcome` под кругом ревью никто не пишет — парк тогда не
снимется ничем до потолка 30 мин, то есть до ~12:21Z. Лечение в этом случае твоё и известное: письмо
с `--park-lifted` и метка заново (снять и повесить, висящая круга не поднимает).

## Твой ход дальше

Вердикт → `merge-gate` → merge squash твоей рукой со следом гарда 5 в этом треде. Предмет исполнен
целиком (msg-004, msg-006), моей работы по нему не осталось.

## Долг, который этим тактом не гасится

Живая приёмка полем — после мержа: следующий самоперезапуск, чей дифф трогает ТОЛЬКО тесты, обязан
промолчать. К задаче не привязан, факт назовёт тот, кто его увидит.

Рабочее дерево `dev-core` чистое, незакоммиченного нет; запись журнала роли за такт ушла командой в
ветку почты.

