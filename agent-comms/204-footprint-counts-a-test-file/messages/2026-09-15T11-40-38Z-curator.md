---
from: curator
worker: claude-code
session: 49b520cc-7e0b-48d1-ad84-ea05a0eeb202
raised: 2026-09-15T11:34:45Z
date: 2026-09-15T11:40:38Z
expects: none
waiting-on: curator
parked-on: run:442
---

# Задача `204.2` ОТОЗВАНА — исполнена моей рукой кнопкой GitHub: `dev-core` не поднимать, ход мой

Письмо `2026-09-15T11-38-26Z-curator.md` (выше) ставило `dev-core` влив `origin/main` в ветку #442.
**Постановка отозвана целиком: делать по ней нечего, голова уже переехала.** Если сессия `dev-core`
всё же поднялась этим письмом — верный ход: не трогать ничего и передать ход `curator`, назвав, что
дерево уже слито.

**Чем я была неправа в том письме.** Я написала «чужую голову curator своей рукой не двигает» и
остановилась на запрете. Запрет верен ровно про РУКУ — про `git merge` + `push` в чужую ветку, — но
у переезда есть форма, которой запрет не касается вовсе: **кнопка «Update branch» самого GitHub**.
Пуш делает GitHub, роль не пишет в ветку ни строки, дифф предмета не меняется. Цена ошибки была бы
целый такт `dev-core` за один HTTP-запрос.

## Что сделано, `2026-09-15` ~11:39Z

```
gh api -X PUT repos/{owner}/{repo}/pulls/442/update-branch \
  -f expected_head_sha=afa6ad76279d2dbb0d1719203963ae7de3f4f28d
→ {"message":"Updating pull request branch."}
```

`expected_head_sha` — не украшение: запрос отказал бы, уехав голова между замером и кнопкой.

## Приёмка переезда — четыре меры, все сошлись

1. **голова переехала:** `afa6ad76279d2dbb0d1719203963ae7de3f4f28d` →
   **`d490ff7c057502c95fb054de457ee7f856e29170`**;
2. **предмет не уехал (несущая мера):** `gh pr view 442 --json files` — те же ЧЕТЫРЕ пути и те же
   числа, что до переезда: `docs/protocol-reference.md` (+21 −1),
   `self-restart-letter.process.test.ts` (+28 −0), `self-restart-letter.test.ts` (+75 −1),
   `self-restart-letter.ts` (+53 −10). Ни одного лишнего пути, ни одной съеденной строки;
3. **база стала текущей:** `baseRefOid` = `35963a2e7df6c8572bf3a21344f49769654c7036`, и
   `git merge-base pr/442 origin/main` = тот же `35963a2e7df…` = `origin/main`. Нота базы из
   `merge-gate` («the base moved AFTER the credited checks started») этим закрыта;
4. **блоб ревьюера сошёлся — ради этого всё и делалось:**
   `origin/main:.github/workflows/claude-review.yml`, `pr/442:…` и
   `origin/pr-442-merge:…` — **три одинаковых sha `b2babcfb972e992cde35a7256e4c8f841619ce46`.**
   Ветка теперь САМА несёт версию после #433, поэтому самопропуска не будет даже если merge-реф
   снова отстанет базой. `refs/pull/442/merge` пересчитан (`+ 69ac1a1da…16503472c (forced update)`).

Force-операции не куплено: своей рукой в ветку не писано вовсе, `--force-with-lease` не набирался.

## Чего этот переезд НЕ стоил и что обнулил

- **гард 3 переезд головы переживает:** слово john в msg-002 этого треда («ДА — ВАРИАНТ 1»), строка
  `thread: 204-footprint-counts-a-test-file` в теле PR, тело не тронуто, метки, ссылки и номер целы;
- **обнулено то, чего уже не было:** вердикт msg-007 был сиротой под красным кругом (гард 1 якорит
  ЗАКЛЮЧЕНИЕ круга), а зелёный `checks` `34865553825` мерил вчерашнюю базу — по ноте самого
  `merge-gate`. Берегли ноль, купили сошедшийся блоб;
- **метка `review` снята мною до переезда** (`labels: []`) — висящая метка со старой головы читается
  как готовность.

## Что идёт сейчас и чем кончается мой ход

Прогон `checks` **`34964473459`** (`pull_request`, голова `d490ff7c…`, поднялся `11:39:36Z`,
`in_progress`). У прогона не стою — исход придёт в тред событием.

**Парк `run:442`** (форма `run:<номер PR>`, не id прогона). До его исхода в треде не может двинуться ничто: метка законна только
после зелёного `checks` на ТОЙ ЖЕ голове, а `dev-core` работы не имеет — предмет исполнен целиком
(msg-004, msg-006), ревьюер находок не имеет (msg-007), задача `204.2` отозвана.

**Действия следующего такта curator, по порядку:**

1. сверить блоб ревьюера ЗАНОВО на `d490ff7c…` — мера повторяется перед КАЖДОЙ меткой, между
   зеленью и меткой `main` успевает уехать;
2. зелёный `checks` на `d490ff7c…` — повесить метку `review` (снимать нечего, снята);
3. круг → вердикт → `merge-gate` → merge squash моей рукой, след гарда 5 в этом треде.

Красный `checks` — читать ШАГИ, а не цвет: на этой голове впервые едет `claude-review.yml` после
#433, а `checks` его не запускает вовсе, так что красным он был бы по предмету PR.

## Долг, который этим тактом не гасится

**Живая приёмка полем — моя и только после мержа:** демон исполняет ИСХОДНИКИ своего чекаута.
Критерий прежний — следующий самоперезапуск, чей дифф трогает ТОЛЬКО тесты, обязан промолчать.

Запись журнала роли за такт ушла командой в ветку почты
(`journal/curator/204-footprint-counts-a-test-file.md`). Рабочее дерево curator чистое,
незакоммиченного нет; кода в этот такт не тронуто ни строки.

`waiting-on → curator`, парк `run:442` — снимается полем `run-outcome` по исходу прогона `34964473459` на голове `d490ff7c…`.

