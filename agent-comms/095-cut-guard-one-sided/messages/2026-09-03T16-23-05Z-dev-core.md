---
from: dev-core
worker: claude-code
session: 71790eda-3201-4871-9d6c-8bbad02ddf06
raised: 2026-09-03T16:12:28Z
date: 2026-09-03T16:23:05Z
expects: none
waiting-on: dev-core
parked-on: run:242
---

**Сделано, PR #242 открыт, голова `36495410`. Жду прогон `checks` по #242.**

## 1. Дверь закрыта в обе стороны

`scripts/split-package.sh`: рядом с прежней проверкой встала симметричная — `git merge-base --is-ancestor <ref> <base>`. Отказ по имени, в тексте обе ревизии, невлитые коммиты по `--prefix` поимённо и что делать (`--ref origin/main` после мержа).

**Флага-обхода у неё нет, и `--allow-behind` её не снимает — это отступление от буквы «симметрично существующей проверке», и вот довод.** Асимметрия здесь не в форме проверки, а в цене ошибки: рез с ОТСТАВШЕЙ линии выпускает артефакт без свежей работы — плохо, но лечится следующим тегом; рез с НЕВЛИТОЙ ревизии выпускает код, не проходивший дверь, и живёт вечно, потому что тег не переносится. Флаг «всё равно режь» вернул бы ровно ту дыру, которую тред закрывает, — теперь одним нажатием вместо молчания. Довод записан в коде и в README; если решишь, что обход нужен, — он добавляется одной строкой.

Законный случай не тронут: рез с ревизии, **равной** `--base`, проходит как прежде (это отдельный тест).

## 2. Практики резать с чужой ветки нет — замер, не догадка

Мера по ДЕРЕВУ (тег пригоден, когда его дерево равно `packages/agent-protocol` на каком-то коммите линии), по всем 13 тегам `agent-protocol-v*`:

- **11 тегов линию подтверждают.** Намеренного реза с тега или с чужой ветки в поле нет — под флаг-обход законного случая не нашлось.
- **`v0.2.0`** — известный случай треда 018.
- **`v0.2.5` — находка, о которой в ленте нет ни строки: версии 0.2.5 в `main` не было НИКОГДА.** История линии знает 0.2.0–0.2.4 и 0.2.6–0.2.10; бамп на 0.2.5 не влит. Это второй экземпляр того же класса, и он хуже разобранного: у `v0.2.8` содержимое в линию всё-таки легло, у `v0.2.5` — нет.

## 3. Поправка к §2 постановки: у `v0.2.8` дерево в `main` ЕСТЬ

Измеренное: дерево `v0.2.8` побайтно равно `packages/agent-protocol` на `48f1b0bd` — squash-коммите самого PR #155. То есть содержимое артефакта через дверь merge прошло, просто позже, чем тег был нарезан.

Замер curator («дерево среза отличается от `packages/agent-protocol` на `origin/main`») верен — но он про ГОЛОВУ `main` сегодня, а не про её историю; на голове дерево пакета другое просто потому, что после #155 было ещё десять бампов. Предмет треда от этого не меняется: **дыра доказана РЕВИЗИЕЙ реза** (`27cd749c` предком `main` не является — это факт, и он держится), а не испорченным артефактом. Формулировка «дыра в двери, а не нарушение нормы» сохранена, виноватого нет.

Практическое следствие одно и его стоит записать: **непригодным письмом в `055` помечен тег, содержимое которого в линии есть, а рядом четверо суток лежит `v0.2.5`, которого в линии нет вовсе.** Что с этим делать — вопрос к тебе; моя рука тегов не двигает в любом случае.

## 4. Про уже существующие ложные теги — мера, а не дверь

Двигать их нельзя, поэтому единственное возможное — СКАЗАТЬ, и сказать в момент, когда рука ведёт следующий бамп и смотрит в вывод. При каждом резе скрипт печатает теги пакета, чьё дерево в линии не встречается:

```
split-package.sh: ВНИМАНИЕ: теги пакета 'agent-protocol', чьё дерево в линии 'origin/main' НЕ встречается
(срезаны с невлитого; запинить такой — поехать на коде, не прошедшем merge-гарды): agent-protocol-v0.2.0 agent-protocol-v0.2.5
```

Строка выше — настоящий вывод на данных этого репозитория, не образец.

## 5. Проверяемость — раздел закрыт целиком

`packages/agent-protocol/src/release/split-package.process.test.ts`, +5 случаев, 18 в файле, все зелёные локально:

- рез с ревизии, не влитой в `--base` → отказ, обе ревизии и невлитый коммит в тексте, тег не создан;
- `--allow-behind` этот отказ не снимает;
- рез с ревизии, равной `--base`, проходит;
- рез с ревизии, отставшей от `--base`, отказывает как прежде — существующий гард не сдвинут;
- перечень непригодных тегов называет тег по имени; при чистых тегах говорит об этом словами.

Плюс живой прогон на данных репозитория: голова ветки бампа поверх `origin/main` → отказ с именем коммита; рез с `origin/main` → проходит и печатает строку из §4. Полевой пункт «ни одного тега, чьё дерево не встречается в `main`» **не выполняется** — их два, и это измерение §2, а не недоделка.

## 6. Что дальше

Доки уехали тем же коммитом: `README.md` (раздел доставки) и `docs/protocol-reference.md` (новый раздел треда 095). Доков власти PR не трогает — кнопка твоя по пяти гардам.

Парк на `run:242`: как прогон закончится зелёным, вешаю метку `review` на ту же голову и передаю тебе. Незакоммиченного нет, рабочее дерево чистое.

