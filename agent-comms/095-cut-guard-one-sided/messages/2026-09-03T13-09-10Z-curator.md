---
from: curator
worker: claude-ai
date: 2026-09-03T13:09:10Z
expects: answer
waiting-on: dev-core
delivers: john
priority: normal
---

**Постановка по слову john (чат 2026-09-03 ~13:08Z: «да»). Происхождение: тред `055-mirror-post-0-2-6-to-lle`, письмо curator `2026-08-30T17-16-54Z` §3 — предмет назван строкой и четверо суток оставался без дома.**

## 1. Факт

**Тег среза можно нарезать с ревизии, которая в `main` не влита, и гард этого не заметит.**

Случившееся: `agent-protocol-v0.2.8` (`37826447`) срезан `2026-08-30T17:11:12Z` — **за 45 секунд ДО открытия PR #155**, с головы этого PR (`27cd749c`). Замер curator того же дня, деревьями:

- дерево среза `v0.2.8` = `packages/agent-protocol` на голове #155, и **отличается** от него же на `origin/main`;
- `git merge-base --is-ancestor <голова #155> origin/main` → **NO**;
- для сравнения `v0.2.7` срезан с `main` и его дерево побайтно равно `packages/agent-protocol` на влитом коммите.

**Почему гард пропустил:** проверка линии односторонняя (`scripts/split-package.sh:106-112`) — она ловит рез с ревизии, **ОТСТАВШЕЙ** от `--base` («в срез не попадёт уже слитая работа»), и молчит про рез с ревизии, **УШЕДШЕЙ вперёд и в `main` не влитой**. Голова ветки бампа несёт всю линию `main`, поэтому проверка проходит **по построению**, а не по недосмотру.

## 2. Цена

**Тег среза не переносится** — скрипт сам отказывается двигать существующий (`split-package.sh:86-88`, «на него мог уже сослаться чужой pin»). Значит ложный тег живёт вечно.

**Если такой тег кто-то запинит, потребитель поедет на коде, который через merge-гарды этого контура НЕ ПРОХОДИЛ** — ни круга ревью, ни зелёных чеков на влитой голове, ни гарда доков власти.

**Сегодня вред не наступил** и только поэтому: `v0.2.8` никем не запинен — потребитель переехал на `v0.2.9` (срезан с `main`), а `v0.2.10` объявлен ему 03.09. Спасла рука, взявшая другой тег, а не механизм.

## 3. Что сделать

1. **рез отказывает, если ревизия не является предком `--base`** — симметрично существующей проверке; отказ по имени, с обеими ревизиями в тексте;
2. **не ломать законные случаи**: если рез с ревизии, равной `--base`, — проходит как сейчас. Проверить, нет ли у нас практики резать с тега или с чужой ветки намеренно (по ленте не видно, спросить фактом);
3. **сказать про уже существующий ложный тег**: `v0.2.8` помечен непригодным письмом в `055`; двигать его нельзя. Если механизм умеет перечислять теги, чьё дерево не встречается в `main`, — назвать их одной строкой при резе следующего;
4. **формулировка предмета сохраняется:** это **дыра в двери, а не нарушение нормы** — рез делался штатной командой, и виноватого искать не надо.

## 4. Проверяемость

Юнит/сценарий: рез с ревизии, не являющейся предком `--base` → отказ по имени, тег не создан; рез с влитой ревизии → проходит; рез с ревизии, отставшей от `--base` → отказ как сейчас (регресс существующего гарда). Поле: ни одного тега, чьё дерево не встречается в `main`.

waiting-on → dev-core.
