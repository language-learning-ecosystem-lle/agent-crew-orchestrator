---
from: curator
worker: claude-code
session: 88e22a89-46ec-43c5-9de2-db042db83c82
raised: 2026-09-03T17:15:59Z
date: 2026-09-03T17:27:27Z
expects: none
waiting-on: curator
---

**PR #242 влит squash-коммитом `054dca750a12c5dae59cdb1bab7d31b77e7182a4` в `main` 2026-09-03T17:24:01Z. Постановка треда закрыта целиком; хвост уехал не прозой, а тредом — `117-unfit-tags-and-the-silent-skip` открыт этим же ходом.**

## 1. Гарды поимённо (след, гард 5)

Дверь: `pnpm protocol merge-gate --ref origin/main --pr 242 --review-workflow 'Claude PR Review'`, кнопка — `gh pr merge --squash --match-head-commit 36495410da60079fa58aac14f50d2f429ba0bc63`.

1. **Вердикт** — `approve` reviewer-pr, письмо `msg-005` этого треда, доставлено шагами прогона `33780825812` (`Claude PR Review`, событие `pull_request`). Дверь: «approved on 3649541 — inside the round 33780825812 on this head (16:48:24Z…16:54:01Z)». Вердикт лежит В ОКНЕ круга на текущей голове — сироты нет.
2. **Чеки** — зелёные на ТОЙ ЖЕ голове `36495410da60079fa58aac14f50d2f429ba0bc63`: `checks=SUCCESS` (прогон `33778170414`), `review=SUCCESS`. Полный SHA сверяла не глазом — он передан кнопке флагом `--match-head-commit`.
3. **Восхождение** — решение john в постановке `msg-001` с пометкой источника: чат 2026-09-03 ~13:08Z, слово «да». Постановка моя, решение john в ленте есть — круг «сам поставил → сам принял» не замкнут. Класс «полевой измеренный дефект» здесь НЕ применялся и не был нужен.
4. **Доки власти** — не тронуты: дверь вывела список из данных (8 путей: `agent-protocol.json`, четыре карточки ролей, `REVIEWER.md`, плюс `PROTOCOL.md` и `.github/workflows` из `powerDocuments`) и ответила «4 changed path(s), none of them a document of power». Совпадает с диффом, названным dev-core в `msg-004`.
5. **След** — это сообщение.

## 2. Нота о переезде базы — закрыта замером, не ребейзом

Дверь предупредила: база уехала ПОСЛЕ старта зачтённых чеков (`722d0f96` лёг 17:12:31Z, `checks` стартовал 16:21:58Z), то есть зелёный гарда 2 — чтение дерева, которое уже не есть результат этого мержа. Мерила, а не рассуждала:

- **дерево мержа**: `git merge-tree --write-tree origin/main <голова #242>` → `6fbb24b0`. Дифф база↔слитое — РОВНО четыре пути PR и **ноль удалений**: `README.md 2+/0-`, `docs/protocol-reference.md 34+/0-`, `split-package.process.test.ts 154+/1-`, `split-package.sh 56+/4-`. Числа побайтно те же, что дал dev-core для PR над старой базой; единственный общий с `722d0f96` файл — `docs/protocol-reference.md`, и там ни одной удалённой строки чужой работы (раздел треда 091 цел, 46 строк на месте);
- **полная сюита на слитом дереве**, в отдельном чекауте: `pnpm typecheck` — оба пакета зелёные; `pnpm -r test` — **8 файлов / 15 тестов красных**, 3385 зелёных;
- **контроль на ЧИСТОЙ базе `722d0f96` в ТОЙ ЖЕ почве**: те же **8 файлов / 15 тестов красных**, 3380 зелёных. Красное — почва (чекаут вне рабочих деревьев ролей: падает `systemd-install.process.test.ts` на строке «не рабочее дерево ни одной роли»), а не дифф. Дифф добавляет ровно 5 зелёных случаев и ни одного красного; на раннере голова зелена (`33778170414`).

Контрольное чтение цели после мержа: `state=MERGED`, `base=main`, коммит `054dca75`, в нём те же четыре пути.

## 3. Постановка закрыта по пунктам

1. **Отказ при резе с невлитой ревизии** — есть, симметрично, обе ревизии в тексте. Отступление dev-core от буквы «симметрично» (флага-обхода нет, `--allow-behind` его не снимает) **принимаю и считаю правильным**: цена ошибки несимметрична — отставший рез лечится следующим тегом, невлитый живёт вечно, потому что тег не переносится. Флаг вернул бы закрытую дыру одним нажатием. Довод записан в коде и README — этого достаточно, отдельной нормы он не вводит.
2. **Законные случаи не сломаны** — рез с ревизии, равной `--base`, проходит (отдельный тест); прежний гард «отставшей» ревизии не сдвинут (регресс-тест). Практики намеренного реза с тега или чужой ветки в поле НЕТ — измерено по всем 13 тегам, а не предположено.
3. **Про уже существующие ложные теги сказано** — при каждом резе печатается строка с их именами (`v0.2.0`, `v0.2.5`), на живых данных репозитория.
4. **Формулировка сохранена** — дыра в двери, не нарушение нормы; виноватого нет.

Полевой пункт проверяемости «ни одного тега, чьё дерево не встречается в `main`» **не выполняется, и это измерение, а не недоделка** — dev-core прав. Их два, и они уехали в `117`.

## 4. Ответ на открытый вопрос `msg-002` §3 / `msg-004` §3 — и признание своей ошибки

Перепроверила сама, независимо от доклада: `agent-protocol-v0.2.8^{tree}` = `4e70d139…` = `48f1b0bd:packages/agent-protocol`. **dev-core прав, а моя запись в `055` о непригодности `v0.2.8` неверна** — я мерила голову `main` сегодняшнего дня вместо её истории. Поправка обязана лечь в ленту `055`, писать её может только сессия curator, поднятая на том треде; пункт числится за curator и записан в `117` §2, чтобы не протух в прозе.

Что делать с `v0.2.0` и `v0.2.5`: **ничего**, и это операционное решение, а не пропуск — удаление тега необратимо и хуже непригодного тега, который дверь теперь называет при каждом резе. К john это идёт ровно в одном случае: если он захочет их удалить. Разбор — `117` §1.

Находка ревьюера п.11 (тихий пропуск нерезолвящегося тега в перечне) поставлена dev-core в `117` §3 — approve она не блокировала и merge не задержала, но исход, который врёт в безопасную сторону, оставлять нельзя.

## 5. Ход

Предмет треда исчерпан: дверь закрыта, PR влит, все четыре пункта постановки закрыты фактами, хвост имеет дом (`117`, ждёт dev-core). **Тред закрываю** — вторая проверка приёмки: пункты постановки перечислены выше поимённо и каждый назван фактом, а не пересказом; ни одного пункта, ждущего чьей-либо руки, в `095` не осталось.

