---
from: dev-core
worker: claude-code
session: 399ea1eb-fc3c-48d4-a9d2-e973f16c9546
date: 2026-08-29T15:18:11Z
expects: answer
waiting-on: dev-core
parked-on: run:109
---

**Переезд на схему 23 сделан по слову john: ветка перебазирована на влитый #111, номер версии 22 → 23, содержание PR не пересматривалось. Голова #109 — `51d0b251786583b4048e30344b09f742502c63e5`. Метку `review` я СНЯЛ — она висела с прошлой головы и в этом состоянии глушит зов уведомителя (тот же класс, что в моём письме `13-20-29Z` §3).**

## 1. Что сделано

Указание msg-019 выполнено буквально: двигался ТОЛЬКО номер версии, предмет не трогался.

- **База** — `9d641f04` (`origin/main` с влитым #111). Ветка `036-launch-fallback-schema` перебазирована, два коммита сохранены: поле+дверь и тесты шага версии;
- **`v22-launch-fallback.ts` → `v23-launch-fallback.ts`**, `from: 21` → `from: 22`; то же для тестового файла шага. Тесты «переехали номером, а не содержанием», ровно как ты и сказала;
- **`CONFIG_SHAPES` / `CONFIG_VALUES`: новая запись 23** — ключи 22 плюс одна строка `roles[].launch.fallback`, значения копией 22. Записи 22 от #111 не тронуты: released-запись это история;
- **`CURRENT_PROTOCOL_VERSION` = 23**, `protocolVersion` конфига = 23 тем же коммитом. Цепочек в конфиге по-прежнему нет ни у кого (`git grep fallback -- agent-protocol.json` пусто);
- **`docs/protocol-reference.md`:** мой раздел про цепочку и раздел #111 про `systemUser` встали рядом, порядок разрешён руками; «версия схемы 22» → 23. Разделы друг друга не перетёрли.

## 2. Одно решение сверх указания, докладываю отдельно

**`v22-role-system-user.test.ts` (файл #111) расслаблен: `toBe(22)` → `toBeGreaterThanOrEqual(22)`, с комментарием на месте.** Иначе тест #111 краснеет на самом факте моего бампа — он утверждал «22 это версия, которую пишет билд», а с 23 это перестало быть правдой. Приём не новый и не мой: ровно так #111 расслабил v21, а до того v18/v19/v20 расслабляли предшественников — файл владеет фактом «поле приехало НА свою версию и никогда не уезжало», а не «мир на ней остановился». Чужой карточки роли или дока власти правка не касается, файл тестовый; если считаешь, что трогать тест соседнего треда нельзя даже так — скажи, но тогда красный тест придётся чинить кому-то другому тем же способом.

Строка `v21`, которую я правил в первом круге, при перебазировании отдана #111 (он сделал ту же правку раньше) — конфликт разрешён в его пользу, моего дубля там нет.

## 3. Прогоны своей рукой на голове `51d0b251`

- `pnpm typecheck` — чисто, оба пакета;
- `biome check` по затронутым файлам схемы — чисто, 0 находок;
- **полный `pnpm test` — 160 файлов / 2646 тестов в `agent-protocol`, 2 / 7 в `transport-telegram`, зелено.** Против прошлой головы (`beeb103a`: 159 / 2633) прибавка — файлы и тесты влитого #111, ни одного падения от переезда номера;
- **живая приёмка одной командой, как в постановке:** `pnpm protocol config check --ref HEAD --no-fetch` → `ok — … protocol version 23, 6 roles, 1 instances (hetzner), mail in branch 'comms' …`, exit 0. То есть билд 23 читает конфиг 23 и молчит, а пустые цепочки не порождают ни строки.

Что НЕ перемерялось и почему: четыре отказа двери на нарочно кривой цепочке (твой замер msg-012 §2) — переезд не тронул ни строки `chainRefusals`, ни `failover.ts`, а юниты и сквозные на стык прошли в полном прогоне выше. Если хочешь снять живой замер заново своей рукой на новой голове — он стоит одну команду и ничего не жжёт.

## 4. Тело PR приведено в соответствие

Описание #109 переписано под 23: переезд назван причиной и словом john, добавлен раздел «что именно изменил переезд» (пять строк, все — номера), числа прогона заменены на фактические. Критерий 9 ревьюера читает описание против диффа — расхождения там оставлять нельзя.

## 5. Метка снята — это действие, а не уборка

Метка `review` стояла с головы `beeb103a`. Триггер `claude-review.yml` — `pull_request: types: [labeled]`: уже висящая метка нового круга не поднимает. Хуже второе: у уведомителя исхода первая ветка — «метка уже стоит, круг идёт, действия у автора нет», то есть зелёный `checks` на `51d0b251` со старой меткой не позвал бы никого. Поэтому метка снята ДО исхода `checks`, и дальше механизм замыкается сам: зелёный `checks` → строка «круга на этой голове ещё нет, метка за автором» → метку вешаю я → круг ревьюера.

## 6. Состояние и что дальше

- **Незакоммиченного за мной нет,** дерево роли чистое; всё в `51d0b251`, ветка запушена (`--force-with-lease`, прошлая голова `beeb103a`);
- **`checks` на `51d0b251` — прогон `33259804852`, `event=pull_request`, в процессе.** За ним не стою (правило треда `037`), паркуюсь на `run:109`;
- **кнопка не двигается:** #109 по-прежнему трогает `agent-protocol.json` — док власти, гард 4, мёржит john. Твоё письмо-вопрос john (msg-018) в силе целиком, у него меняются только числа: голова `51d0b251`, версия 22 → **23**, и строка на ящике после кнопки та же;
- **вердикт ревьюера снимается заново с этой головы** — прошлый approve был на `beeb103a`, гард 1 его на новой голове не засчитает;
- **часть 3 (Ш2-3, провод в планировщик) не начата** и стартует поверх влитого #109 — прежнее слово в силе.

Мой ход дальше — одно действие: повесить метку `review` на `51d0b251` по зелёному `checks`. Чужих открытых PR не трогал: прогон привязан к треду `036`.

