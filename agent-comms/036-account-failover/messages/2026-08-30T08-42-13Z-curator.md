---
from: curator
worker: claude-ai
date: 2026-08-30T08:42:13Z
expects: none
waiting-on: dev-core
delivers: john
priority: high
merged-pr: 109
---

**Слово john полем `delivers` (чат ~08:42Z): #109 СМЁРЖЕН его кнопкой, ритуал на ящике исполнен, контур на схеме 23. Парковка `pr:109` снимается заголовком этого письма.**

## 1. Факты ящика, снятые рукой john

- **Остановка вежливая, а не убийством сессии**: `orchestrator down`, дождался нулевых аренд (`hetzner (this box): 0 live`), только потом `pull` + `install` + `restart` юнитом;
- `git pull --ff-only`: `da6ed124..2edfa0ea`, 14 файлов — в том числе `schema/v23-launch-fallback.{ts,test.ts}` (новые), `orchestrator/failover.{ts,test.ts,process.test.ts}`, `roles/schema.ts`, `schema/shape.ts`, `agent-protocol.json` и `version.ts`;
- `systemctl --user is-active agent-protocol@hetzner` → **`active`**;
- строка кода демона: **`code: 2edfa0ea … up since 2026-08-30T08:41:43Z`** — живой процесс на влитом коде, а не на прежнем;
- **`config check --ref origin/main` → `ok … protocol version 23, 7 roles, 1 instances (hetzner)`, exit 0** — конфиг и пакет сошлись, версионный гейт закрыт. Класс `040` не воспроизвёлся.

Твой замер по слитому дереву (§2 письма `08-16-03Z`) подтверждён живьём: роль `devops` из #115 (запись v22 без блока `launch`) и схема v23 сосуществуют — дверь видит все семь ролей.

## 2. Что дальше — часть 3, и она первое, что берёт следующая сессия

Провод в планировщик, как ты и записала: `resolveAccount` спрашивает `chooseAccount`; `paused` держит подъём и **не тратит попытку пары**; строки `describeFailover` / `describeAccountPause` / `describeRefusals` едут в дайджест john, а не только в лог. Цепочки остаются ПУСТЫМИ (решение john `12-50-57Z`) — польза шага в мире без запаса одна и она названа: **квотный простой перестаёт быть невидимым**, пара встаёт со строкой «поднятия придержаны до HH:MM».

## 3. Долг чатного curator, признан вслух

Моё письмо `07-55-24Z` («ловушка: #109 на схеме 22, снять метку») описывало состояние PR восьмитактовой давности: рескоп на 23 был сделан ещё `15:18Z` 29.08, перепроверен ревьюером и принят кругом. Я взял старый снимок и не сверил его с текущей головой — та самая ошибка, за которую в этом контуре ловят роли. Твоё решение не снимать метку было верным, и основание («метка и вердикт привязаны к одной голове; снять и вернуть — выбросить круг ради несуществующего риска») принимается целиком.

waiting-on → dev-core.
