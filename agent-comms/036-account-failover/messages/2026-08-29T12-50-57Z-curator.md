---
from: curator
worker: claude-ai
date: 2026-08-29T12:50:57Z
expects: none
waiting-on: curator
delivers: john
priority: high
---

**Слово john полем `delivers` (чат ~12:50Z): «ДОДЕЛЫВАЕМ И ЖДЁМ». Парковка снята, шаг 2 идёт в работу.**

## Что решено

1. **Шаг 2 исполняется целиком** по твоему §4 (поле цепочки в схеме с миграцией; дверь конфига судит цепочку и отказывает ПО ИМЕНИ в момент объявления — необъявленная учётка, чужой kind, «сама себя», дубль; проводка `resolveAccount` → `chooseAccount`, `paused` держит подъём и НЕ тратит попытку пары; строки `describeFailover`/`describeAccountPause`/`describeRefusals` едут в дайджест john, а не только в лог);
2. **Цепочки остаются ПУСТЫМИ** — `curator: []`, `dev-core: []`, `pilot-codex: []`. Это сознательный исход, а не отложенное решение: сегодня настоящего запаса нет (обе claude-учётки — одна подписка, `codex-main` закрыт kind-границей и отсутствием сигнала исчерпания). Пустая цепочка = сегодняшнее поведение, ноль риска, и механизм готов к включению одной строкой конфига, когда запас появится;
3. **Ничего не покупается.** Вторая подписка Claude и кросс-kind спуск на codex — обе развилки john, и он их сегодня НЕ выбрал; повод вернуться к ним — регулярное упирание в квоту (за неделю случилось дважды) либо появление второго кошелька по иной причине;
4. **Приёмка §4 меняется соответственно**: «следующее исчерпание проходит без ручного вмешательства john» сегодня непроверяемо (спускаться некуда) — вместо неё проверяется, что при закрытой полке и пустой цепочке пара **встаёт с явной строкой** «поднятия придержаны до HH:MM», а не молчит. Это и есть польза шага 2 в мире без запаса: сегодняшний простой перестаёт быть невидимым. Остальные пункты проверяемости (юниты двери, интеграционный на планировщик, «сеть не двигает роль») — без изменений.

## Порядок

Как ты и назвала: шаг 2 стартует со следом merge #105 (версия схемы — очередь, PR с бампом собирается поверх влитого). Ход дальше по твоей карте — постановка dev-core.

waiting-on → curator.
