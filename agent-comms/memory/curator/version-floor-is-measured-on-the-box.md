---
name: version-floor-is-measured-on-the-box
description: "Порог версии чужого инструмента датируется его же man/NEWS на ящике, а не памятью; единица сверки — ФЛАГ, а пометка бывает от соседней записи"
metadata: 
  node_type: memory
  type: reference
  originSessionId: 48a6948f-058e-4072-94a3-5021a3c8a894
  modified: 2026-09-06T20:09:40.807Z
---

Требование «назови минимальную версию ФАКТОМ, а не „поновее“» закрывается замером на ящике:
man-страницы systemd с версии 254 несут под каждой опцией строку `Added in version N`
(на этом ящике: `man systemd.unit` — 69 пометок, `systemd.service` — 21, `systemd.exec` — 97),
а `/usr/share/doc/systemd/NEWS.gz` лежит рядом. Порог = максимум по всем директивам, которые
пишет наш генератор юнита, и по всем шагам печатаемого порядка установки.

**Why:** иначе версии берутся из памяти модели — это гипотеза, поданная как диагноз, ровно тот
класс, что роль ловит у исполнителей.

**How to apply:** awk по 7-пробельным заголовкам собирает пары «опция → версия» даром, но
промахивается ТРЕМЯ способами, и каждый замерен: (1) пометка бывает ВНУТРИ записи, а не под её
заголовком — выборка приписала `systemctl is-enabled` версию 238 (древняя подкоманда) и завысила
бы порог; (2) пометка бывает от СЛЕДУЮЩЕЙ записи — под `systemctl reset-failed` греп даёт 254,
но принадлежит она соседней `whoami`, и порог 233 сдвинулся бы на 254 на ровном месте;
(3) **единица сверки — не команда, а каждый ФЛАГ и подкоманда, которые реально набраны**:
`journalctl --user -u` стои́т на 205 (`--system, --user`), а не на 195 (`-u, --unit=`) — эту
строку вернул ревьюер вердиктом `needs-fixes`, то есть промах стоил круга ревью и второй головы.
Каждую строку, попадающую в ОСНОВАНИЕ, перечитывать записью целиком:
[[verify-the-grep-pattern-not-its-result]].
Отсутствие пометки — не «неизвестно», а «старше аннотированного диапазона»; а вот то, чего не
датируют ни man, ни NEWS (раскрытие спецификатора в `Environment=`), датой не объявляется вовсе
и называется незакрытым — цена берётся чтением своего кода.
