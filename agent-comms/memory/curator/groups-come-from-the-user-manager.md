---
name: groups-come-from-the-user-manager
description: "Смена групп не доезжает до сессии рестартом юнита — креды держит `systemd --user` (PPID 1), и мерится это `/proc/<pid>/status`"
metadata: 
  node_type: memory
  type: project
  originSessionId: 8e6e433d-12f1-4fcd-b251-3aea4c706b89
  modified: 2026-09-06T11:56:04.833Z
---

Дополнительные группы сессия наследует от `systemd --user` СВОЕГО пользователя, а не читает заново
при старте юнита. Замер 2026-09-04 (ящик hetzner, тред `062`): `/etc/group` записан `11:53:20Z`,
`systemd --user` (PID 1917240 `aco-hetzner`, 1917241 `lang-hetzner`) стартовал `11:52:51Z` — на 29 с
РАНЬШЕ; демон `orchestrator up` стартовал `12:44:41Z`, то есть на 51 мин ПОЗЖЕ записи, и всё равно
`Groups: 1003` во всех трёх `/proc/<pid>/status` (менеджер → демон → оболочка сессии).

**Why:** «демон поднят раньше `usermod`» — правдоподобный и НЕВЕРНЫЙ диагноз: из него следует
лечение `systemctl --user restart`, которое здесь уже случилось само и не помогло. Цена ошибки —
приёмка, объявленная закрываемой рестартом, который её не закрывает.

**How to apply:** мерить `Groups:` из `/proc/<pid>/status` у ТРЁХ звеньев (менеджер по `ps -o ppid`
до PPID 1, демон, своя оболочка) и сверять с mtime `/etc/group`; лечит переработка менеджера
(`loginctl terminate-user` при linger либо перезагрузка) — рука john, у ролей глагола нет.
Владельца пути это не касается: `drwxrwsr-x aco-hetzner contour-aco` пишется по ВЛАДЕЛЬЦУ, поэтому
отсутствие группы бывает латентным, а не живым.

**Вторая половина, перемеренная 2026-09-06 (тот же тред): через `sudo -u` группы ПРИЕЗЖАЮТ** — в
логе живой сессии `devops` (`10:22:12Z`) стои́т `uid=1001(aco-devops) … groups=1001(aco-devops),
1005(contour-aco),1006(contour-lang)`, то есть `sudo` пересчитывает список из базы заново, а
устаревший список менеджера наследуют только его собственные дети (демон и сессии своего
пользователя). Отсюда цена «группа не доехала» ПЕРЕЧИСЛЯЕТСЯ, а не оценивается: `find <чекаут>
-user <другой пользователь контура>` — файлы, созданные чужим пользователем в общем дереве, для
процессов владельца только на чтение (`664` + доступ был бы по группе), и `test -w` это
подтверждает. 2026-09-06 таких файлов был ровно ОДИН на весь чекаут — то есть отсутствие группы
стои́т одного файла, а не контура, и рвать живые сессии рестартом менеджера ради него дороже
(оборванная сессия = неудачная попытка, три подряд = роль выключена). Родня:
[[merged-code-is-not-running-code]], [[reproduce-with-the-tool-that-measured]],
[[reported-instance-is-a-sample]].

**Из указателя (перенесено 2026-09-06, оглавление шло за потолок):** рестарт юнита группу не подтягивает: креды держит `systemd --user` (PPID 1), мерится `/proc/<pid>/status` у трёх звеньев
