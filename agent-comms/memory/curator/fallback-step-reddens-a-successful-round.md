---
name: fallback-step-reddens-a-successful-round
description: Переезд ревьюера на запасную учётку краснит круг в ОБЕ стороны — до 14.09 краснил шаг переезда, с 14.09 краснит ОСНОВНОЙ шаг при сработавшем переезде; вердикт доставлен, а кнопки merge нет ни у кого
metadata: 
  node_type: memory
  type: project
  thread: 205-primary-step-reddens-a-delivered-verdict
  originSessionId: fbae4eb0-57dc-4d83-8ebb-e53928076316
  modified: 2026-09-14T14:26:23.809Z
---

С мержа #399 (`1b08b81cf`, 2026-09-13 17:33:21Z) зелёных кругов `Claude PR Review` нет вовсе: условие
шага «Лимит основной учётки» (`.github/workflows/claude-review.yml`, строка 530) судит по НАЛИЧИЮ
записи `type=rate_limit_event`, а SDK кладёт её в каждый здоровый прогон со `status: "allowed"`.
Отсюда цепочка: успешное ревью основной учётки → `fallback=1` → запасная отвечает `401` → шаг падает
→ джоба красная → на голове `review=FAILURE`.

**Why:** вердикт при этом ДОСТАВЛЕН и верен (артефакт `34775379733`: основная — `success`, 46 ходов,
$1.2175, вердикт записан; то же на чужом PR #388, круг `34775456728`, $0.8894), но гард 1 не видит
закрытого успешного круга, а гард 2 краснеет чеком `review` — [[failed-review-run-reddens-guard2]],
[[guard2-reddens-from-its-own-review-circle]]. Пока это не починено, merge в репозитории стои́т
целиком.

**Перепись 13.09 (замер curator, тред 201):** 8 красных кругов с 16:28Z, последний зелёный —
`34767267092`, 16:00:23Z. Из восьми **три** — ложная тревога с доехавшим вердиктом (`34776552737`/#416,
`34775456728`/#388, `34775379733`/#409), пять — честный отказ основного шага. Дверь на такой голове
отказывает ДВУМЯ гардами, а не одним: гард 1 не видит закрытого УСПЕШНОГО круга (`completed/failure`
вердикт не якорит, даже когда вердикт лежит в окне этого круга), гард 2 видит `review=FAILURE`.
Открытых PR 13, проходимых нет ни одного. Починка — детектор в PR **#417** (`fea0528ed`, `checks`
зелёные 19:13Z): шаг `limit` читает `steps.reviewer.outcome` ДО транскрипта. Кнопка у john (гард 4,
`claude-review.yml`), метка на #417 не вешается.

**Починок ДВЕ, и они разные (замер curator 19:40Z, тред 192):** #417 (`fea0528ed`, тред 201) — шаг
читает `steps.reviewer.outcome` ДО транскрипта; #418 (`d97bee0e2`, тред 197) — предикат по
`rate_limit_info.status`. У обоих `checks` зелёные, метки нет (правильно), кнопка у john. **Каждая в
одиночку оставляет путь ложного переезда** (у #418 — основная пережила настоящий отказ и всё же дала
вердикт; у #417 — основная упала по НЕлимитной причине, а рутинная запись в транскрипте есть), и
предикаты у них РАСХОДЯТСЯ: #418 считает отказом любой `status != "allowed"`, тогда как замер
`dev-core` даёт безопасную сторону `== "rejected"` — незнакомое значение переезда не вызывает.
**`continue-on-error: true` на запасном шаге не лежит НИ В ОДНОМ открытом PR** (мерено грепом по обоим
диффам), поэтому любой будущий отказ запасной учётки утопит круг с доставленным вердиктом снова.

**14.09 09:05Z — ОБЕ починки стали `DIRTY`, и кнопки нет уже НИ У КОГО, включая john.** `main` уехал
за ночь на `a6f7b276d` (#415), и `mergeStateStatus` у #418 (`d97bee0e2`) и #417 (`fea0528ed`) —
`DIRTY`/`CONFLICTING` при зелёном `checks` у обоих. `git merge-tree --write-tree --name-only` называет
ОДИН файл у обоих и у пары между собой: `docs/journal/dev-core.md`, общий append-only хвост;
`claude-review.yml` сливается сам даже #418×#417. То есть работа осталась не «написать детектор», а
«ребейзнуть хвост журнала», и второй PR платит ещё одним ребейзом при ЛЮБОМ порядке
([[declared-merge-order-allocates-the-rebase]], [[protocol-reference-tail-serializes-merges]]).
Слово john 14.09 (msg-002 треда 202): чинить ОБА, детектор первым, секрет — после мержа #418.
Оба PR ждут `dev-core` в СВОИХ тредах (197 и 201-notifier-down), в каждом лежит письмо смотрителя
про разрыв базы. Уточнение к абзацу выше: у #418 `null`-случай исключён явно (`. != null and
. != "allowed"`), незнакомое ОТСУТСТВИЕ поля переезда не вызывает — риск уже, чем сказано.

**14.09 09:14Z — тупик СНЯТ, обе ветки снова `MERGEABLE`, #418 стои́т под кнопкой john.** `dev-core`
разрешил общий хвост журнала БЕЗ force-push: влил `origin/main` **В ветку**, поэтому push вышел
fast-forward по тем же именам и номера PR уцелели. Новые головы — #418 `7ca4dea302d0cf13675fb40e080622e0cae76ecb`,
#417 `dc3546f38f87479c1888a1383e51b8409c1b91c3` (старые `d97bee0e2`/`fea0528ed` мертвы). Предмет ни
одного PR ребейзом не изменился — сверено `gh pr view --json files` против numstat исполнителя,
построчно. `merge-gate` на #418 в 09:14Z: **гард 2 `ok` (`checks=SUCCESS`, прогон `34826050546`),
`mergeStateStatus CLEAN`**, остались только гард 1 (чтение john) и гард 4 (его кнопка). У #417
`checks` на 09:14Z ещё `in_progress`. **#417 снова станет `CONFLICTING` от мержа #418** — тот же
общий хвост, второй ребейз обязателен при любом порядке.

**14.09 10:32Z — КЛАСС ЗАКРЫТ В ПОЛЕ, и «How to apply» ниже этим отменяется.** #418 влит `09:21:40Z`;
с него круг `Claude PR Review` поднимался ШЕСТЬ раз и все шесть зелёные (`34828011351` 09:26:59Z #388,
`34829936500` 09:48:53Z #411, `34830033316` 09:50:02Z #420, `34830276558` 09:52:53Z #410,
`34830880834` 10:00:01Z #386, `34833002399` 10:24:19Z #420) против вчерашних шести подряд `failure`.
Доказательство по существу — не «зелено вообще», а **перепись ШАГОВ** круга `34830880834`: «Лимит
основной учётки» `success`, **«Ревью на ЗАПАСНОЙ учётке» `skipped`**, «Выход ревьюера» `success`.
**Метка `review` снова безопасна и вешается как обычно; ~$1.2 за заведомо красный исход больше не
платятся.**

**Что из блоков выше ОСТАЛОСЬ живым:** #417 (вето по `steps.reviewer.outcome`, тред `201-notifier-down`)
по-прежнему ОТКРЫТ и `CONFLICTING` шестой раз об общий хвост `docs/journal/dev-core.md` — ребейз
рука `dev-core`, кнопка john (гард 4), и по распоряжению курьера 14.09 10:17Z он идёт ПОСЛЕ #408
(файл-на-тред, тред `195`), чтобы не купить седьмой разъезд. `continue-on-error` на запасном шаге
не лежит по-прежнему ни в одном открытом PR, но цена этого сузилась: без настоящего лимита шаг не
запускается вовсе. `review_failure_cause` (`review-delivery.sh:467–481` знает `limit` только по
`api_error_status == "429"`) — **решение john 14.09: ОСТАВЬ**, предмет закрыт и по расписанию не
возвращается; вернётся только полевым случаем.

**14.09 12:18Z — ЗАКРЫТА ПОЛОВИНА: переезд, сработавший ПО-НАСТОЯЩЕМУ, всё равно даёт КРАСНЫЙ круг,
и форма ЗЕРКАЛЬНАЯ прежней.** Круг `34841729661` (#426, голова `06297de50`, тред 180): упал РОВНО
шаг №9 — ОСНОВНАЯ учётка, `anthropics/claude-code-action@v1`; шаги 10–17 зелены все, три доставки
прошли, вердикт `approve` доставлен. Раньше краснил шаг ПЕРЕЕЗДА поверх успешной основной — теперь
краснит ОСНОВНАЯ поверх успешного переезда. Шесть зелёных кругов после #418 — это круги, где переезд
`skipped`; первый НАСТОЯЩИЙ переезд после починки красный.

**Два замера, оба несущие.** (1) `result` основного шага: `subtype: "success"`, **`is_error: true`**,
`num_turns: 37`, `total_cost_usd: 1.0177`, `duration_ms: 389866` — то есть основная ПРОЧЛА ДИФФ и
отработала (форма стены — `num_turns: 1`, `$0`, ~400 мс — здесь не совпадает ни одним полем), а шаг
всё равно `failure` по `is_error`. (2) Переезд сработал на записи **`status=allowed_warning`** — это
предупреждение о ПРИБЛИЖЕНИИ к лимиту, запрос ПРОПУЩЕН; предикат #418 (`!= "allowed"`) его ловит, а
безопасная сторона `== "rejected"` не поймала бы. Запасная отсудила дифф второй раз с нуля
(`num_turns: 28`, `$0.7627`). **Один вердикт — $1,7804, круг красный.**

**Why:** `continue-on-error` во всём `claude-review.yml` стои́т РОВНО ОДИН раз — шаг 17,
транскрипт-артефакт (строка 1326); у шага `reviewer` (строка 222) его нет. Отсюда правило шире
одного PR: **пока это так, ЛЮБОЙ круг с сработавшим переездом красный ПО ПОСТРОЕНИЮ**, и PR не мёржит
НИКАКАЯ рука — гарды 1+2 у curator, чек `review=FAILURE` на голове у john.

**How to apply:** увидев красный `review` при доставленном вердикте — переписать ШАГИ джобы
(`gh run view <id> --json jobs`), а не цвет круга: упавший №9 при зелёных 10–17 = эта форма, и
перевешивание метки её НЕ лечит (следующий переезд покрасит так же, цена ~$1,78). Предмет — к john
двумя границами сразу: `.github/workflows/**` (док власти + `zones.forbidden`) и деньги (удвоение
расхода на каждом `allowed_warning`). Разрез рекомендации: (а) снять падение основного шага с исхода
джобы, когда переезд отработал; (б) судить переезд по `status`, а не по наличию записи. Доставлено
john письмом `2026-09-14T13-18-57Z-curator.md` (тред 180, парк на john).

**14.09 13:37Z — ПОПРАВКА К ЗАМЕРУ (2) ВЫШЕ И РАСКЛАДКА ОБЕИХ ПОЛОВИН ПО ДОМАМ.** Переезд на круге
`34841729661` был **ЗАКОННЫЙ**: лимит настоящий — `api_error_status: 429`, `terminal_reason:
api_error`, текст про session limit, и запись `rejected` в том же транскрипте ЕСТЬ, десятью записями
позже. Раскладка `rate_limit_event`: `allowed_warning` ×4, `rejected` ×1, а шаг признака печатает
ПЕРВУЮ ПО ВРЕМЕНИ запись — отсюда мой разбор «переезд по предупреждению» (msg-137 треда 180) и был
ошибкой чтения одной напечатанной строки. **Правило: о лимите судят транскриптом целиком, а не
строкой `evidence` шага `limit`.** Дефект предиката при этом остаётся дефектом (транскрипт с одним
предупреждением и красным шагом по ЧУЖОЙ причине объявил бы переезд), но описанием ЭТОГО прогона он
не был. Дома: **(б) — PR #431** (`dev-core/201-warning-is-not-a-refusal`, тред `201-notifier-down`,
открыт 13:26:29Z, отказ читается ПРЕФИКСОМ `allowed`, а не равенством), **(а) — постановка curator
в новом треде `205-primary-step-reddens-a-delivered-verdict`** (`waiting-on dev-core`, приоритет
high): исход джобы перестаёт падать вместе с основным шагом, когда вердикт вынесен И доставлен, а
красным круг остаётся по уже существующему предикату `delivery_exit_code`
(`.github/scripts/review-delivery.sh`, шаг №16) — нет `verdict.md`, не доехала доставка, самопропуск.
Оба трогают один файл, порядок — #431 первым, ребейз платит ветка 205. Кнопка на обоих — john
(гард 4), метка `review` не вешается ни на один.

**14.09 13:41Z — ТРЕТИЙ круг подряд этой формы, и отсюда критерий ПЕРЕД меткой.** `34841729661`
(12:07Z #426), `34849381027` (13:27Z #428), `34850008811` (13:33Z #416) — все три `failure`, у двух
последних перепись шагов совпадает знак в знак (№9 `failure`, №10–17 `success`, вердикт доставлен);
до них шесть зелёных подряд (последний `34840576269`, 11:54Z). Дверь на #428 отказала ДВУМЯ гардами
при доставленном `approve` — гард 1 «no CLOSED round … completed/failure», гард 2 `review=FAILURE`.

**How to apply (действует):** **зелёный `checks` — не единственное условие метки; второе условие —
живой круг.** Перед меткой `gh run list --workflow 'Claude PR Review' --limit 8`: красные подряд с
переездом → метку НЕ вешать (купишь вердикт, не годный гарду 1, за ~$1,78), доложить и передать ход;
зелёные на свежих головах → вешать как обычно. **#431 этого не чинит:** он судит переезд по префиксу
`allowed`, а при ЗАКОННОМ переезде (настоящий 429) основной шаг падает по-прежнему — красноту снимает
только (а), тред `205`. Полевой случай и сам критерий — `docs/journal/curator/191-watcher-run-may-never-start.md`
§5 (PR #430, голова `c7fe24b39`); заложников уже два (#428, #430), парк треда `191` — на john.

**14.09 14:19Z — СЕРИЯ КРАСНЫХ РВЁТСЯ САМА: круг `34854162512` (#409, голова `15afa1ca`, тред 196)
`success`, вердикт доставлен, дверь дала гард 1 `ok` («inside the round 34854162512 … 14:13:04Z…14:19:14Z»)
и гард 2 `ok` (`review=SUCCESS, checks=SUCCESS`) — merge состоялся.** То есть после трёх подряд
красных (блок 13:41Z) следующий же круг зелёный, и ни #431, ни тред 205 к тому времени не влиты.
Причина та же, что и у зелёных после #418: **переезд просто не сработал** — красноту даёт НАСТОЯЩИЙ
`429` у основной, а он случается не на каждом круге.

**Уточнение к «How to apply (действует)»:** перепись `run list` перед меткой остаётся, но её ответ —
ВЕРОЯТНОСТЬ, а не запрет: «три красных подряд» не означает, что четвёртый красный. Стоит это $1,78 в
худшем случае и ноль в лучшем, поэтому решение принимается по цене хода, а не по цвету серии; когда
вердикт нужен ДЛЯ КНОПКИ и другой работы в треде нет — вешать и парковаться на `run:<pr>`.

**How to apply (до 14.09; отменено блоком выше):** метку `review` НЕ перевешивать «на удачу» — следующий круг покраснеет так же, и это
стоит ~$1.2 за заведомо красный исход. Вскрывать артефакт круга и смотреть ОБА файла: подпись
«круг доехал на ЗАПАСНОЙ учётке» врёт, ревью делала основная — [[round-artifact-names-both-accounts]],
[[fallback-account-answers-401]]. Починка только секрета удваивает цену каждого круга; чинить надо
детектор. PR по `claude-review.yml` круг не судит по построению, метка на него не вешается, кнопка —
у john.
