- [Парк замораживает тот самый ход](parked-on-freezes-the-turn.md) — `--parked-on` отменяет постановку, если снятие зависит от адресата; `pr:` ждёт кнопку, `run:` — вердикт; а вопрос к john паркуется там, где родился, как только у треда нет живой работы
- [Ссылка на письмо — автором и фактом](cite-letters-by-author-and-fact.md) — читалка не даёт имён файлов, а `msg-NNN` едет под `--tail`
- [Треды, должные после #160](threads-owed-after-160.md) — девять оставшихся бесхозных предметов; `new-thread` из связанного прогона john разрешил (2026-09-02) при указании происхождения первой строкой, и реестр расходится тактами по одному
- [Нота о переезде базы — замером, не ребейзом](base-move-note-answered-by-measure.md) — четыре дешёвых факта на прозе, дерево мержа с `tsc` и тестами на коде (всегда под `TMPDIR=/tmp`), чужой замер проверяется хешем дерева, ребейз только на конфликте или красном
- [Приёмка укладывается в объявленный `launch`](acceptance-must-fit-declared-launch-params.md) — прогон приёмки с чужими `--model`/`--effort` = право `launch-params`, которого у curator нет
- [Цена такта — это очередь, а не проза](tick-cost-is-the-queue-not-prose.md) — тридцатиминутного такта нет (медиана 6.3), а ход ждёт медиану 19.9 мин, и 84% ожидания роль занята на другом треде
- [«0 of those new» = уже позвонило](notify-zero-new-means-already-rang.md) — свою парковку на человеке проверяют строкой в `notify.state`, а не выводом сухого `notify`
- [«Зелено» — это команда раннера](green-is-only-the-runners-command.md) — `tsc`, названный файлами, выходит нулём поверх ошибок; спрашивать в докладе команду, которой мерили, а не результат
- [Постановка кончается тегом](statement-of-work-ends-at-the-tag.md) — «доставка потребителю» внутри постановки — ошибка формы, а 403 на чужой репозиторий это механизм границы, а не блокер; расширять токен и брать чужой `secrets.envFile` — оба отказ
- [Смёрженное ≠ работающее](merged-code-is-not-running-code.md) — демон исполняет исходники рабочего чекаута, чей `HEAD` может стоять на чужой ветке; живую половину приёмки называть отдельно, но блокером не делать, а `run --ref origin/main` половину этого чинит флагом
- [Приёмочный прогон нужен тред, который ЖДЁТ роль](acceptance-run-needs-a-thread-that-waits.md) — на закрытом треде `handoff` ловится за секунды и `exitCode 0` не значит ничего
- [Сверяй ШАБЛОН грепа, а не его вывод](verify-the-grep-pattern-not-its-result.md) — чистый греп исполнителя бывает чистым по слишком узкому шаблону; сверять шаблон с перечнем постановки, чужие имена — по `id` в конфиге
- [Копия в `comms` кладётся ДО мёржа канона](comms-copy-before-canon-merge.md) — джоба «comms-derived синхронен с каноном» красит PR, правящий этот воркфлоу, и чинит её dev-core, а не curator
- [Закрытие треда — третья дверь почты](closing-a-thread-is-a-third-door.md) — `thread status` у поднятой сессии есть (065.1 построена ровно для неё), а приёмка требует ВТОРОЙ проверки: открытые постановки разбираются поимённо, домом или named-снятием
