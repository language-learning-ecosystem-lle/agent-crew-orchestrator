- [Низкий приоритет всё равно поднимают](low-priority-still-gets-raised.md) — `--priority low` только упорядочивает очередь (`rankCandidates`), а кандидатом тред делает `waitingOnOf(thread) === role`: тред, ждущий СОБЫТИЯ, а не роль, жжёт слот за такт, пока держатель хода жив; вычёркивает ТОЛЬКО `--waiting-on "—"`, и тред при этом остаётся `open` в `INDEX.md`
- [Парк замораживает тот самый ход](parked-on-freezes-the-turn.md) — `--parked-on` отменяет постановку, если снятие зависит от адресата; `pr:` ждёт кнопку, `run:` — вердикт; а вопрос к john паркуется там, где родился, — и «когда не останется живой работы» не наступит само, потому что живую работу порождает моя же следующая постановка; но цена person-парка мерится `notify.state` и бывает в СУТКАХ (парки на john по 11 дней), так что работа на своей кнопке идёт вперёд вопроса
- [Дверь парка отказывает молчащему письму](park-door-refuses-silent-letters.md) — `new-message --write` в стоячий парк ОТКАЗЫВАЕТ, пока письмо не объявит отношение к нему: `--verdict … --pr N` (снимает), `--parked-on run:N` (доклад рядом) или `--park-lifted run:N`; флаг обязан назвать РОВНО стоящий парк — про чужой это тоже отказ, а не нота; заодно печатает живое состояние прогона и тем заменяет отдельную сверку; НО словарь `--verdict` — только `approve|needs-fixes`, так что письмо-след merge снимает парк `--park-lifted pr:N`, а не `--verdict merged`, и `--expects ack` с `--waiting-on curator` без парка дверь отвергает как состояние без исхода (след идёт `--expects none`); письмо `github` об исходе `checks` парк `run:N` НЕ снимает — снимает пишущий следом, а `--park-lifted run:N` + `--parked-on john` в одном письме дверь принимает
- [Лифт парка чист над почтой](park-lift-is-pure-over-the-mail.md) — `parkingOf` не знает журнала, а `cli derive` крутится в Actions без `.orchestrator/`: сужение через журнал даёт один тред с двумя ответами, и `world.mine` (#187) упорядочивает только СВОИ письма роли
- [Свежую ленту читают ДО подъёма роли](read-the-fresh-feed-before-raising-a-role.md) — тот же живой замер мог снять другая сессия часом раньше (058 повторил 083); и СВЕЖИЙ `TMPDIR` прогона (#172, тред `056`) убивает codex-роль под read-only песочницей на первой же команде — `ENOENT: mkdir '<TMPDIR>/tsx-1000'` до старта CLI; алиас #183 тут ни при чём, замерено
- [Чтение ссылки не должно писать в дерево](reading-a-ref-must-not-write-the-tree.md) — `git checkout <ref> -- .` ради грепа оставляет `M <path>` в индексе и тем готовит отказ запуска роли на СЛЕДУЮЩЕМ такте (R17), молча; читать `git show <ref>:<путь>`, а `git status --porcelain` перед передачей хода — в один ряд с проверкой, что письмо ушло
- [Вендорская песочница мерится даром](vendor-sandbox-measures-for-free.md) — `codex sandbox -- <команда>` гоняет что угодно под тем же confinement БЕЗ вызова модели: «это покажет только прогон роли» дважды оказалось неправдой и стоило слота, денег и неверного диагноза
- [Ссылка на письмо — автором и фактом](cite-letters-by-author-and-fact.md) — читалка не даёт имён файлов, а `msg-NNN` едет под `--tail`
- [«Уезжает в тред X» — намерение, а не запись](handoff-to-another-thread-is-prose.md) — переезд предмета проверяется чтением ленты адресата: прогон связан одним тредом, и написать в X некому
- [`await-input` будит любой записью](await-input-wakes-on-any-write.md) — снять блокировку может объявление бота, а не ответ; длинный замер между чтением и отправкой открывает окно, в которое ответ ложится незамеченным — перечитывать `--for` ПЕРЕД отправкой, а не только при пробуждении
- [Треды, должные после #160](threads-owed-after-160.md) — восемь оставшихся бесхозных предметов (пункт 4, `--parked-on run:<id>`, сверен кодом 2026-09-02 и закрыт тредом 061 — треда не заводить); `new-thread` из связанного прогона john разрешил (2026-09-02) при указании происхождения первой строкой, и реестр расходится тактами по одному
- [Нота о переезде базы — замером, не ребейзом](base-move-note-answered-by-measure.md) — кодовая ветка включается признаком «обе стороны трогают код одного слоя», а НЕ пересечением имён; на прозе — четыре дешёвых факта, на коде — дерево мержа с `tsc` и полной сюитой (всегда под `TMPDIR=/tmp`), чужой замер проверяется хешем дерева, ребейз только на конфликте или красном; НОРМОЙ не объявлена — в `PROTOCOL.md` этого нет, и в следе гарда 5 это своя практика, а не правило; на коде — ПОЛНАЯ сюита на обоих деревьях, потому что падает файл, которого в диффе нет
- [Класс полевого дефекта требует ДВУХ писем](field-defect-class-needs-two-letters.md) — «доков власти нет» это гард 4, а не условие 2 исключения; класс объявляется письмом РАНЬШЕ следа гарда 5, иначе он ярлык задним числом; а «Вопрос ревьюеру» в теле PR закрывает только условие 3 — фразу исполнителя проси отдельным предметом постановки
- [Приёмка укладывается в объявленный `launch`](acceptance-must-fit-declared-launch-params.md) — прогон приёмки с чужими `--model`/`--effort` = право `launch-params`, которого у curator нет
- [Цена такта — это очередь, а не проза](tick-cost-is-the-queue-not-prose.md) — тридцатиминутного такта нет (медиана 6.3), а ход ждёт медиану 19.9 мин, и 84% ожидания роль занята на другом треде
- [«0 of those new» = уже позвонило](notify-zero-new-means-already-rang.md) — свою парковку на человеке проверяют строкой в `notify.state`, а не выводом сухого `notify`; а ОТСУТСТВИЕ строки читается однозначно «не звонило», потому что снятие парка строку не удаляет (тест самой двери)
- [«Зелено» — это команда, дерево и ПОЧВА](green-is-only-the-runners-command.md) — `tsc`, названный файлами, выходит нулём поверх ошибок, а один хеш дерева краснеет из `/tmp` и зеленеет из чекаута контура; без контроля на чистой базе в той же почве виноватым назначается дифф
- [Постановка кончается тегом](statement-of-work-ends-at-the-tag.md) — «доставка потребителю» внутри постановки — ошибка формы, а 403 на чужой репозиторий это механизм границы, а не блокер; расширять токен и брать чужой `secrets.envFile` — оба отказ
- [Смёрженное ≠ работающее](merged-code-is-not-running-code.md) — демон исполняет исходники рабочего чекаута, чей `HEAD` может стоять на чужой ветке; живую половину приёмки называть отдельно, но блокером не делать, а `run --ref origin/main` половину этого чинит флагом; дрейф — ДВА класса, и `--ff-only` на разошедшейся ветке отказывает, так что «отстаёт на N» ещё не случай для `repo-refresh`; исполнитель отставания с 2026-09-02 — активная роль `devops`, но рестарта в её трёх глаголах нет, а «доехало ли» меряется одним `echo $TMPDIR` следующей сессии; демон САМ перезапускается на уходе последней сессии, поэтому дрейф на прямом `main` не парк на john, — но самоперезапуск встаёт на произвольном коммите и умеет разрезать починку пополам (#209 без #211), а «N behind» этого не показывает: сверять `merge-base --is-ancestor` по каждой половине
- [Приёмочный прогон нужен тред, который ЖДЁТ роль](acceptance-run-needs-a-thread-that-waits.md) — на закрытом треде `handoff` ловится за секунды и `exitCode 0` не значит ничего; но ОТКРЫТЫЙ тред с `waiting-on: curator` для приёмки годится (058, прогон 11): дверь печатает ноту `the turn was TAKEN, not passed`, прогон идёт до конца, а число приёмки читается из журнала сессии счётом `command_execution`
- [Сверяй ШАБЛОН грепа, а не его вывод](verify-the-grep-pattern-not-its-result.md) — чистый греп исполнителя бывает чистым по слишком узкому шаблону; сверять шаблон с перечнем постановки, чужие имена — по `id` в конфиге и по ленте чужого дома, а свой нулевой вывод по чужому дереву — контрольным непустым совпадением; набор из ИМЁН слеп к путям и к именам чужих CI-проверок, а выписка целиком (фикстура) мерится чтением полей; шире пакета тот же маркер бьёт по СВОЕМУ адресу (`language-learning-ecosystem-lle` — своя организация), и его надо вычитать
- [Копия в `comms` кладётся ДО мёржа канона](comms-copy-before-canon-merge.md) — джоба «comms-derived синхронен с каноном» красит PR, правящий этот воркфлоу, и чинит её dev-core, а не curator; самосинка копии НЕТ ни в одном воркфлоу, поэтому PR, выносящий логику из канона, красен своим же гардом — #217 влитый первым РАСШИРЯЕТ окно с 5 строк до 145, закрывает только #167; исполнитель копии — dev-core ЗАМЕРОМ (шесть его коммитов в `origin/comms`), и его собственный отказ «почту правят только команды» бьётся путём: копия вне `agent-comms/**`
- [Гард 4 кончается на `workflows/**`](guard4-reach-ends-at-workflows.md) — `powerDocuments` не покрывает НИ `.github/scripts/**`, НИ `packages/**`: вынос тела из воркфлоу снимает его с кнопки john в любой из домов, а выбор адреса решает лишь, может ли curator это тело ПИСАТЬ (`packages` у роли в `forbidden`, `.github/scripts` — нет)
- [Закрытие треда — третья дверь почты](closing-a-thread-is-a-third-door.md) — `thread status` у поднятой сессии есть (065.1 построена ровно для неё), а приёмка требует ВТОРОЙ проверки: открытые постановки разбираются поимённо, домом, named-снятием — или СМЕРТЬЮ ВСЛУХ, если у предмета нет цены и первоисточника (042); адрес ищется чтением кода, а не заведением треда
- [Dispatch у Merge Notify ничего не пишет](merge-notify-dispatch-is-a-noop.md) — джоба стоит на `if: pull_request.merged == true`, поэтому пропущенные письма о merge не переигрываются, а полевую приёмку уведомителя нельзя устроить по требованию
- [Норма едет в PROTOCOL.md, таблица остаётся](norms-go-to-protocol-tables-stay.md) — инвентарь пополняется, а док власти двигает только john: копия таблицы раздваивается на первом же пополнении
- [Проза карточки едет в PR конфига](card-prose-rides-the-config-pr.md) — отдельный PR = вторая кнопка john под одним решением и окно противоречия; значение статуса карточка не пересказывает вовсе, а два слова john в треде бывают «второе уточняет первое»
- [Перепроверяй тем инструментом, что мерил](reproduce-with-the-tool-that-measured.md) — свой `python3` дал 107/108 там, где `node` даёт 108/109: прежде чем сказать «замер не сошёлся», исключи собственную руку; у влитого PR пути — из squash-коммита, а не из `gh pr view --json files` (тот отдаёт разошедшийся дифф ветки)
- [Правка тела PR не убивает вердикт](pr-body-edit-keeps-the-verdict.md) — текстовую находку ревьюера закрывают в той же ветке, сверив `headRefOid` до и после; кодовую — нет
- [Канал вердикта — свой у ревьюера](reviewer-verdict-channel-is-its-own.md) — вердикт кладёт в тред сам `claude-review.yml`, а не `ci-outcome.yml`; с merge #197 (2026-09-02 16:33Z) исход `checks` тоже приезжает САМ и зовёт вешать метку (`LABEL_CALL`) — рукой зелёный больше не снимают, парковка `run:<N>` честна за обоими, а перед меткой сверяй, что `claude-review.yml` не в диффе; ветвь «вердикта нет» в ЗАПАРКОВАННЫЙ тред не доезжает вовсе — проснулся без письма, читай сам прогон
- [Звонок парка несёт СТАРУЮ первую строку](park-bell-carries-the-old-first-line.md) — «донеси X до john» закрывается замером `notify.state` + первой строки парковочного письма, а не вторым парком; цена, померенная позже, в звонок не попадает и дописывается в ленту ТРЕДА-ХОЗЯИНА; оговорку о сдвиге базы гасят `git log <merge-base>..origin/main -- <путь>` + `merge-tree --write-tree` + чтение самого дерева слияния
- [Стоячие адреса 076 и 077](standing-address-threads-076-077.md) — `076-main-red-alarm` и `077-notifier-down` вписаны литералами в два воркфлоу: закрытие или парковка делает канал недоставляемым, и молчит он по-разному тихо; вопрос о норме ушёл к john тредом `080`; а живая приёмка функционала приземляется В АДРЕС, и тред-хозяин на ней сам не просыпается — факт надо принести, назвав в последнем письме буквально, что искать; `turn: explicit` на таком адресе объявляет САМА curator (право `thread-status`, не кнопка john), но только замерив КАЖДОГО машинного писателя треда — `077` объявлен 2026-09-02, `076` нет
- [Стык мерится на реальной ленте даром](seam-measured-on-real-feed-for-free.md) — фикстура покрывает функцию, а путь/раскладку/имена — нет: `git show origin/main:<скрипт>` + `git archive origin/comms … | tar -x` в `mktemp -d` дают пять строк правды сразу после merge, не дожидаясь аварии
- [Безголовый тред красит ВСЮ почту](headless-thread-reddens-all-mail.md) — тред без `_meta.md` роняет `Comms Derived` на каждом push в `comms`, а письмо смотрителя об этом само есть push, то есть петля 1:1; чинится одной командой `thread status --repair --write`
- [Номера тредов плоские](thread-ids-are-flat.md) — `NNN.M` дверь записи принимает и пушит, а обходчик (`^\\d{3}-`) не видит: постановка уезжает в никуда, и после `new-thread --write` обязательно контрольное `mail --role <адресат>`
- [Системный пользователь не читает чужой аккаунт](system-user-role-cannot-read-shared-account.md) — роль под `systemUser` умирает за 0 секунд на `configDir` чужого владельца (`600`), а полка кредов АККАУНТНАЯ: падает одна роль — стоят все пары аккаунта, и `claude login` это не чинит
- [Решение может предполагать несуществующий механизм](decision-may-presuppose-a-missing-mechanism.md) — «команда, которой роль открывает PR» такой команды не было: сверяй механизм чтением кода ДО постановки, замер клади с якорем на sha
- [Расширение предмета внутри PR двери — по двум проверкам](scope-widening-inside-a-door-pr.md) — законно тем же PR, только если без починки дверь в момент включения отказала бы в форме, которую строка объявляет вслух, И починка не вводит нормы; иначе резать отдельным PR ПЕРЕД дверью, а расхождение исполнитель называет в треде ДО merge своими словами
- [Названный случай — образец, а не класс](reported-instance-is-a-sample.md) — чужая находка перечисляется скриптом по всему предмету ДО постановки: в 042 ревьюер назвала нулевой по цене случай, а тот же класс держал ЖИВОЙ ложный отказ и проглоченную опечатку в команде, которая уже за дверью
- [Машинный конфиг — это instances/hetzner.json](machine-config-is-instance-file.md) — `local.json` на ящике нет вовсе, а супервизор бежит как `lle` и слепнет (`EACCES`) над каталогом роли под `systemUser`: замеряй адрес до постановки
- [Хвост PR требует своего треда](pr-tail-sweep-needs-the-owning-thread.md) — в прогоне, связанном одним тредом, чужой PR не домерживается: след гарда 5 обязан лечь в ленту ЕГО треда, а писать туда нечем — список прогнать и молчание не оставлять
- [Хвост protocol-reference сериализует мержи](protocol-reference-tail-serializes-merges.md) — каждый кодовый PR дописывает раздел последним, поэтому PR в круге ревью становится `CONFLICTING` от первого чужого merge, а цена — не ребейз, а АННУЛИРОВАННЫЙ вердикт (гард 1 привязан к коммиту) и полный круг заново; approve на голове — жать в тот же такт
- [Переезд шага отменяет объявленное исключение](moved-step-repeals-declared-exception.md) — приёмка переписанного шага читается КОДОМ ВЫХОДА на каждом классе, что различал старый; инвариант бывает записан в шапке СОСЕДНЕГО воркфлоу (самопропуск → зелёный, тред 046, объявлен в `notifier-watch.yml`), и красный на нём звонит смотрителю и поднимает пару без предмета
