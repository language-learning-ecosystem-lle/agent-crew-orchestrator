---
name: statement-of-work-ends-at-the-tag
description: "Постановка кончается тегом — «доставка потребителю» внутри неё ошибка формы, а 403 на чужой репозиторий это граница, а не блокер"
metadata: 
  node_type: memory
  type: project
  originSessionId: 503796e6-2668-467a-bad9-43e8c412205d
  modified: 2026-09-14T10:36:45.951Z
---

Конечная точка работ этого контура — **тег и его объявление** (имя, полный sha среза,
`protocolVersion`), а не объект у потребителя. Форма «сделай PR/пин у потребителя» внутри постановки
недопустима: у роли в чужом контуре нет ни зон, ни карточки, ни круга ревью, ни гарда доков власти.

**Почему:** норма едет к кнопке john двумя моими же PR — #180 (тред `062-contour-boundary`,
`PROTOCOL.md` + четыре карточки) и #163 (тред `064-forget-the-consumer`, пункт в «Как curator
судит»). Обе с тех пор сели: на 2026-09-03 норма читается в `PROTOCOL.md`, «Граница работ»
(«Конечная точка любой работы этого репозитория — тег и его объявление»).

**Рез тега — рука РОЛИ, а не кнопка john, и это замеряется, а не выводится.** Та же строка нормы
объявляет «нарежь тег, назови sha и версию схемы, дальше не твоё» ЗАКОННОЙ формой постановки, а в
ленте `056` msg-014 (2026-08-31) dev-core сам прогнал
`split-package.sh --tag agent-protocol-v0.2.9 --ref origin/main --push` и положил вывод скрипта в
тред. Аргумент исполнителя «необратимое внешнее действие ⇒ ход john» этим и снимается: тег НОВЫЙ, а
ПЕРЕНОС существующего скрипт отказывает по построению. Прецедент в собственной ленте бьёт правило
последней строки — оно про сомнение, а не про то, что уже сделано один раз.

**«Резать сейчас» без бампа не существует.** `scripts/split-package.sh` (строки 97–99) сверяет имя
тега с `version` пакета НА СРЕЗАЕМОЙ РЕВИЗИИ и отказывает по именам — ДО `git tag` (строка 165), то
есть проба безопасна и мусорного тега не оставляет. Поэтому развилка «резать сейчас или после PR X»
всегда сначала коллапсирует в «PR бампа нужен под обеими ветками» — открывай бамп, а вопрос об X
решай базой. Обратное (имя сошлось) — проба уже создаст локальный тег и уведёт настоящий рез в отказ
«тег уже есть» (msg-009 того же треда).

**Как применять:**
- пишешь §«доставка» в постановке — кончай её на теге, дальше «не твоё»;
- исполнитель уперся в `403`/`404` на репозиторий-потребитель — это **механизм границы сработал
  штатно** (токен контура сужен на один репозиторий рукой john), а не препятствие к обходу. Ни
  расширение PAT, ни чужой `secrets.envFile` инстанса (#164) обходом не являются — оба снимают
  границу и оба отказ;
- работу у потребителя ставит крю потребителя, ход поднимает john в ТОМ контуре; постановки отсюда
  туда не едут;
- чтение файла в чужом чекауте ради приёмки (сверить объявленный `protocolVersion`, текущий пин)
  границей не считается — действием в чужом контуре считается. Край близко, называй вслух.

Полевой случай, из которого строка: тред `056-shared-tmp-mechanism`, §5 моей же постановки от
2026-08-30 назвал «приезд в LLE» частью работ; 2026-08-31 dev-core уперся в 403 и правильно
остановился. Сравни [[threads-owed-after-160]].

**Из указателя (перенесено 2026-09-03, оглавление шло за потолок):** «доставка потребителю» внутри постановки — ошибка формы, а 403 на чужой репозиторий это механизм границы, а не блокер; расширять токен и брать чужой `secrets.envFile` — оба отказ; сам РЕЗ — рука роли, не кнопка john (замер: msg-014 треда 056, dev-core резал с `--push`), а «резать сейчас» без бампа не существует вовсе — скрипт отказывает по расхождению имени с `version` ДО `git tag`, поэтому развилка «сейчас или после PR X» коллапсирует в «бамп нужен под обеими ветками»

**Из указателя (перенесено 2026-09-08, оглавление шло за потолок):** «резать сейчас» без бампа `version` не существует

**Линия тегов ОТСТАЁТ, и от этого заказ, рождённый болью контура-потребителя, приёмкой в `main` не
закрывается** (2026-09-14, тред `203`, merge #420). Замерено в тот же час, что и merge: последний тег
`agent-protocol-v0.2.14` нарезан **08.09** — шесть суток и много починок назад, — а `version` на
`origin/main` всё ещё `0.2.14`, то есть следующий рез отказан по имени, пока нет бампа. Оба звена
перепроверены кодом в тот день: поле `version` в `packages/agent-protocol/package.json` и строка
`die "тег '$tag' уже есть…"` в `scripts/split-package.sh` — она стои́т ДО `git tag`, так что абзац
выше про «без бампа не существует» подтверждён, а не унаследован.

**Why:** предмет `203` был заказан john в чат ровно потому, что суточный повтор ЖЁГ ОКНА в
контуре-потребителе. Merge в `main` там не меняет ничего: пакет ставится по тегу. Доложить «принято»
и закрыть тред значило бы закрыть заказ, не доставив его туда, где болит, — и узналось бы это только
следующей жалобой.

**How to apply:** принимаешь починку, заказанную болью ЧУЖОГО контура, — тем же ходом меряй линию
тегов (`git tag --sort=-creatordate | head` плюс `version` на голове) и называй в следе, доедет ли
исправление до потребителя без среза. Развилка «резать или ждать планового» — вопрос john, и
задавать его надо СОСТАВНЫМ: рез это рука роли, но бамп `version` — отдельный PR, чей гард 3 упирается
в слово john о приёмке ([[version-bump-ascends-to-johns-acceptance]]), поэтому «да» должно покрывать
оба звена, иначе ответ стоит второго захода. Связано: [[merged-code-is-not-running-code]] — тот же
класс разрыва «принято ≠ работает», только через пин, а не через чекаут.
