---
name: park-forms-both-take-the-pr-number
description: "`run:` и `pr:` в --parked-on берут НОМЕР PR (не id прогона); `pr:` снимается только заголовком `merged-pr: N`, и парк за событием, чей следующий шаг твой, дверь зовёт запертым изнутри"
metadata: 
  node_type: memory
  type: project
  originSessionId: 60b1e564-c20c-4a5c-ac0f-53e4b7522266
  modified: 2026-09-06T19:57:50.568Z
---

`--parked-on` знает две формы, и **обе хотят номер PR**: `pr:<n>` и `run:<n>` — последняя ждёт
КРУГА НА этом PR, а не прогона по его id. Одиннадцатизначный id из `gh run list` дверь отказывает
вслух («PR numbers do not reach 1000000»).

Разница форм не в написании, а в том, чем снимаются:
- `pr:<n>` снимается **ровно заголовком `merged-pr: n`** в любом письме почты. Состояние PR не
  сторожит никто: merge в GitHub и объявление о нём прозой тред НЕ размораживают;
- `run:<n>` снимается кругом на голове PR — тем, который уже бежит.

**Why:** парк на `pr:` за merge'ем, который жмёт john, при том что следующий шаг треда — ТВОЙ
(метка `review` после зелёного `checks`), дверь называет «запертой изнутри»: тред запаркован, никого
не поднимает, а ждёт действия, которое сделать некому (тред 061 — три таких парка за день).

**How to apply:** ждёшь КРУГА РЕВЬЮ (метка уже висит) — `--parked-on run:<НОМЕР PR>`. Ждёшь чужой
кнопки merge — `pr:<n>` плюс `--park-mover <кто нажмёт>`, иначе дверь откажет и будет права.
**Ждёшь ЗЕЛЁНОГО `checks` на своём PR без метки — спроси дверь, а не память** (уточнено замером
2026-09-06, #318): дверь `run:` считает ЛЮБОЙ прогон в полёте на голове, а не только круг ревью, —
пока `checks` идёт, `--parked-on run:<PR>` ПРИНИМАЕТСЯ («waits for a run that is still running,
1 of 2 on head …»), и снимает его само письмо о зелени, потому что на PR без метки нотификатор
пишет в шапку `waiting-on: <роль автора>` (единственное исключение трассового класса —
`thread/thread.ts`, обход `standingParkOf`, ~строка 763; решение john 2026-08-03, тред 023).
Формы парка нет только тогда, когда в полёте не осталось НИЧЕГО (замер #303) — тогда ход у себя
без парка ценой одного холостого подъёма. Номер берётся из хвоста URL PR или
`gh pr view --json number`, id прогона в парк не едет никогда. Смежное: [[parked-on-freezes-the-turn]], [[park-door-refuses-silent-letters]],
[[park-lift-is-pure-over-the-mail]].
