---
name: reported-instance-is-a-sample
description: "Названный чужой находкой случай — образец класса, а не класс; перечисляй класс скриптом ДО постановки, иначе живой дефект остаётся за кадром"
metadata: 
  node_type: memory
  type: feedback
  originSessionId: a62366c4-a6aa-47cb-be8d-25a81cb4f70e
  modified: 2026-09-15T10:44:03.906Z
---

Ревьюер в треде `042` назвала ОДИН случай («`--staged` у `zones check` разобран как флаг со значением, потому что следующий токен — разделитель `|`») и честно оценила цену: ноль, команда за дверью не стоит. Я взяла её находку как предмет постановки — и перед тем, как писать, перечислила ВЕСЬ класс скриптом: воспроизвела цикл `parseUsage` поверх литерала `USAGE` и напечатала каждый случай, где `takesValue` истинен, а следующий токен не плейсхолдер. 49 строк, два дефекта — и второй оказался не тем, что назвали: хвостовой комментарий строки формы (`# …`) читается как значение флага, и стоит это в `orchestrator up`, которая за дверью УЖЕ. Ложный отказ и проглоченная опечатка воспроизводятся двумя вызовами прямо сейчас.

**Why:** названный случай — тот, на который человек наткнулся, а не тот, который дороже всего. Ревьюер судит дифф, у неё нет обязанности перечислять класс; эта обязанность — на том, кто пишет постановку. Постановка по одному образцу чинит образец и оставляет живой дефект в коде вместе с обоснованием «это некритично».

**How to apply:** прежде чем писать постановку по чужой находке, сформулируй КЛАСС («токен, который не плейсхолдер, прочитан как значение») и перечисли его машинно по всему предмету — не проверяй названный случай. Дешевле всего скриптом, воспроизводящим спорный цикл поверх реальных данных, а не грепом по именам ([[verify-the-grep-pattern-not-its-result]] — там про слепоту шаблона, здесь про слепоту ОДНОГО экземпляра). Найденное клади в постановку первоисточником с якорем на sha и с воспроизведением вызовом ([[reproduce-with-the-tool-that-measured]]), и говори исполнителю перемерить: свой замер — заявление, а не факт.

**И перемер ходит В ОБЕ стороны — он бывает УЖЕ нуля, а не только шире (тред `208`, 15.09).** `dev-core` и ревьюер оба доложили «третье место того же класса молчания»: ветка `rounds.length === 0` в `reviewRunAnchor`, `gate.ts:1047`, «не называет идущий круг» — довод был в том, что она не вызывает вынесенную `roundsInFlightClause`. Перемерил чтением того, что ветка ПЕЧАТАЕТ: два её достижимых текста печатают `named.map(describeRun)`, а фильтр `named` — только по имени воркфлоу, без головы, события и статуса, значит идущий круг туда попадает и печатается со своим `in_progress/?`. Заперто их же тестом. Предмета нет; завёл бы — PR, круг и кнопка за ноль, причём гард 3 у него ушёл бы к john, потому что первоисточник тут ЧТЕНИЕ КОДА, а не эпизод в поле, и класс «полевой измеренный дефект» его не закрывает.

**How to apply (вторая половина):** «ещё одно место того же класса» проверяется тем, что место ПЕЧАТАЕТ читателю, а не тем, что оно не зовёт ту же функцию — тот же факт приезжает в текст другим путём. Отказ заводить предмет называй РЕШЕНИЕМ с остатком вслух («явной фразы X нет, но соседний текст уже предупреждает»), иначе он читается отсрочкой и всплывёт как скрытый ([[carried-tail-items-rot]]). И назови условие, при котором предмет всё-таки заводится: полевой эпизод даст ему настоящий первоисточник ([[field-defect-class-needs-two-letters]]).

**Из указателя (перенесено 2026-09-06, оглавление шло за потолок):** чужая находка перечисляется скриптом по всему предмету ДО постановки: в 042 ревьюер назвала нулевой по цене случай, а тот же класс держал ЖИВОЙ ложный отказ и проглоченную опечатку в команде, которая уже за дверью

**Из указателя (перенесено 2026-09-06, оглавление шло за потолок):** чужая находка перечисляется скриптом по всему предмету ДО постановки: названный случай был нулевым по цене, а класс держал живой отказ
