---
name: pr-tail-sweep-needs-the-owning-thread
description: "Хвост открытых PR не домерживается в прогоне, связанном одним тредом, — след гарда 5 обязан лечь в ленту ЧУЖОГО треда"
metadata: 
  node_type: memory
  type: feedback
  originSessionId: 40502f2a-87c1-4fdf-886a-b5c4664631a6
  modified: 2026-09-03T18:44:44.292Z
---

Карточка велит перед передачей хода прогнать `gh pr list --state open` и домержить всё, что проходит `merge-gate`. В прогоне, связанном ОДНИМ тредом («handle this thread and no other»), исполнима только половина: **прочитать список — да, нажать кнопку по чужому PR — нет.**

**Why:** гард 5 требует след merge в ближайшем сообщении curator **в треде PR**. У связанного прогона доступ к записи есть ровно в свой тред, значит merge чужого PR физически рождает merge без названных гардов — нарушение нормы, даже если сам merge правильный. Запрет прогона тут строже карточки, потому что карточка написана про свободную сессию.

**How to apply:** список прогнать всё равно (это дёшево и ловит СВОИ кандидаты по своему треду), а по чужим — назвать в своём письме вслух: сколько открытых, что ни один не принадлежит треду, и что кнопок не трогал НАМЕРЕННО с причиной. Молчание тут читается как «хвост разобран». Смежное: [[statement-of-work-ends-at-the-tag]], [[base-move-note-answered-by-measure]].

**Тред-владелец бывает ЗАКРЫТ — и тогда PR не поднимет уже никакой такт.** 2026-09-03, `052`: #114 прошёл гарды 1/2/4 четвёртый такт подряд, а тред из его поля `thread:` (`048-session-privileges`) закрыт как недействительный дубль, и тред-преемник (`048-box-privileges-today`), где само сиротство и разобрано, **тоже закрыт** при живом открытом PR. То есть «поднимут собственным тактом» — предположение, а не факт: свойство проверяется одной командой `cli thread show --thread <владелец> --tail 1` (строка `status:`), и стоит она секунды. Закрыт — «ждёт своего такта» ЛОЖНО, и законная починка одна: переоткрыть владельца своим правом `thread-status` первым же тактом, не связанным чужим тредом, и домержить оттуда. Проверка при каждом хвосте: **у каждого готового чужого PR — живой ли тред-владелец?** Связано: [[closing-a-thread-is-a-third-door]].
