---
name: notify-zero-new-means-already-rang
description: "«0 of those new» после своей парковки — это «уже позвонило»; читается блоком своего треда в notify.state, и ключей ТАМ ДВА — `asked` (немедленный звонок) и `parked`: греп по одному `parked` дал ложное «не звонило»"
metadata: 
  node_type: memory
  type: project
  originSessionId: f5cb23ea-7eb2-476b-81de-aa685e06d4af
  modified: 2026-09-06T14:05:57.255Z
---

Замер 2026-08-31T00:4xZ (тред `059`): отправила письмо к john с `--parked-on john --expects ack`,
через минуту прогнала `cli notify --ref origin/main --root <mail>` БЕЗ `--write` для контрольного
чтения — своего треда в выводе не оказалось вовсе, а итог был `14 parked, 14 of them asking,
**0 of those new**`. Выглядит как «парковка не позвонила».

Факт противоположный: `.orchestrator/notify.state` содержал строку `parked john
059-pilot-codex-terra-max 2026-08-31T00:41:38Z` — с меткой времени МОЕГО сообщения. То есть штатный
нотификатор контура успел отработать между отправкой и моим чтением и парковку уже объявил. Парк
звонит РОВНО ОДИН РАЗ, на сообщении, которое спросило (тред `051`), — поэтому объявленный парк в
последующих сводках строки не получает, и сухой прогон честно печатает `0 new`.

**Why:** сухой `notify` после своей же отправки — естественное контрольное чтение («ответ инструмента
≠ факт»), и он даёт ровно тот вывод, который читается наоборот. Цена ошибки — повторное письмо к john
или ложный вывод «звонок потерян».

**Отсутствие строки — это «не звонило», и различает это дверь.** 2026-09-02, тред `064`: строки
`parked john 064-forget-the-consumer` в состоянии не было вовсе, и читалось это двояко — «не
позвонило» либо «строка снялась вместе с парком, когда кончился объявивший его ход». Различитель — в
собственном тесте двери (`packages/agent-protocol/src/notify/notify.process.test.ts`, «an answered
park stops reminding in the same tick»): после снятия парка `remind\t` из состояния уходит, а
`parked\t<person>\t<thread>\t` ОСТАЁТСЯ — «the park's own key is still owed a LIFT line and stays in
the state until a letter carries it». Строка парка снятие ПЕРЕЖИВАЕТ, поэтому её отсутствие означает
ровно одно: звонка не было. Проверять при этом надо, что уведомитель вообще жив — соседние строки с
близкими метками (`067` `18:15Z`, `073` `18:36Z`) это и показывают.

**Дефект — на ПИСЬМЕ, а не на треде: тот же тред звонит следующим парком.** 2026-09-03, тред `064`:
в `10:2xZ` строки `parked john 064-forget-the-consumer` по-прежнему не было (перемер dev-core, при
живых и более молодых строках `062` `10:06:29Z`, `091` `09:42:55Z`), а мой новый парк того же треда
письмом `10:50:56Z` дал строку `parked john 064-forget-the-consumer 2026-09-03T10:50:56Z` через
двенадцать секунд после отправки. Значит «этот тред не звонит» — неверная гипотеза: не позвонило
КОНКРЕТНОЕ письмо, и вероятнее всего его заголовок не нёс `parked-on: john` (собственная оговорка
честности в том же треде). Один подтверждённый случай, а не класс.

**ПРИЧИНА НАЙДЕНА 2026-09-04 (тред `125`, дом класса — тред `129-park-bell-silent-letter`), и она не
в заголовке: ПАРК СНИМАЕТ ЧУЖОЕ ПИСЬМО РАНЬШЕ ТАКТА КУРЬЕРА.** Замер с контролем на ОДНОМ треде:
парк на john 17:01:36Z — строки нет и через 46 минут (соседи `126` 16:40:25Z и `128` 17:14:23Z берут
его в вилку, курьер жив, файл переписывается раз в ~2 мин); тот же тред, та же роль, те же флаги,
письмо 17:49:45Z — строка через 20 секунд. Разница ровно одна: за первым письмом легли пять чужих
(последнее 17:44:56Z), за вторым — ни одного. Механизм в коде, а не в гипотезе: `standingParkOf`
(`packages/agent-protocol/src/thread/thread.ts` ~775) — «a later message naming somebody ELSE in
`waiting-on` ended the turn the park was declared on», возврат хода той же роли парк НЕ воскрешает; а
`parkedKey` (`notify.ts` ~1073) — «THE COURIER'S MEMORY OF PARKS IS THE CURRENT COMPOSITION, NOT A
JOURNAL». Парк, снятый до такта, курьер не видел никогда и задним числом не объявит.

**Цена: вопрос к john не звонит НИ РАЗУ, и тишина неотличима от ожидания** — объявленный парк строки
в последующих сводках не получает по построению, так что `0 of those new` печатается одинаково в обоих
случаях. В поле это стоило 48 минут простоя готового к кнопке PR #272.

**Практика, которая из этого следует (до починки):** парк на человеке ставить ПОСЛЕДНИМ письмом такта
и после парковки читать `notify.state`; легло чужое письмо между парковкой и проверкой — **встать в
парк заново**. Второго звонка перепарковка не даст, пока ключ (человек, тред) в состоянии (Д-2, тред
`030`), а если парк снят — она и есть тот звонок, которого не было. Ср. [[park-goes-after-the-verdict-not-before]]:
там парк ПОСЛЕ вердикта, чтобы не проглотить письмо ревьюера; здесь — ПОСЛЕ всей чужой почты, чтобы
письмо ревьюера не проглотило парк.

**КЛЮЧЕЙ В `notify.state` ДВА, И ВСЁ ВЫШЕ ЧИТАЕТ ТОЛЬКО ОДИН — «нет строки `parked` ⇒ не звонило»
НЕВЕРНО** (тред `140`, 2026-09-06T11:1xZ, ошибка моя и стоила ДВУХ писем с ложным заголовком).
Рядом с `parked\t<person>\t<thread>\t<ts>` состояние держит `asked\t<person>\t<thread>\t<ts>` —
`NotifyState.asked`, «WHAT THE STATE MUST REMEMBER TELLING» (тред `129`, `notify/notify.ts` ~936,
запись ~1193, чтение ~1279). Именно `asked` — след НЕМЕДЛЕННОГО звонка парковки с `--expects ack`
(она звонит сразу, без возрастного порога), и пишется он, как вся ветвь `notify`, только после
успешной доставки. У треда `140` строки `parked` не было, а `asked john 140-… 2026-09-06T10:36:17Z`
БЫЛА — то есть john был позван, а я объявила обратное.

**Контрольное совпадение стоит одной команды и здесь было решающим:** у СОСЕДНИХ тредов (`064`,
`134`, `136`, `137`) в файле по ДВЕ строки — и `parked`, и `asked`, с одним штампом. Одна строка
вместо двух — это факт про второй ключ, а не про звонок. Поэтому греп по `parked` — узкий шаблон,
и это ровно [[verify-the-grep-pattern-not-its-result]]: читать надо ВЕСЬ блок состояния своего треда
(`grep <thread> notify.state`), а не одно слово. Что означает `parked` БЕЗ `asked` и наоборот —
не мерено; гипотеза «`asked` = звонок ушёл, `parked` = стоячая парковка не дожила до своего прохода»
так и называется гипотезой.

**СОСТОЯНИЕ ПЕРЕПИСЫВАЕТСЯ ТАКТОМ КУРЬЕРА, И ЧТЕНИЕ ЧЕРЕЗ 16 СЕКУНД ПОСЛЕ ОТПРАВКИ — ЛОЖНЫЙ
ОТРИЦАТЕЛЬНЫЙ** (тред `134`, 2026-09-06). Отправка 12:58:34Z; чтение в 12:58:5xZ показало у своего
треда ОДНУ строку `asked john 134-subagents-unused 2026-09-06T09:24:18Z` — чужой штамп, ЧЕТЫРЬМЯ
часами старше, то есть след прошлой парковки того же треда. Читается это ровно как «моё письмо не
позвонило», и `mtime` файла подтверждал алиби наоборот: 12:58:52Z, то есть курьер отработал ПОСЛЕ
отправки. Он и правда отработал — но на композиции, снятой до неё. На следующем такте (`mtime`
13:00:01Z, около минуты) появились ОБЕ строки со штампом моего письма: `parked john 134-… 12:58:34Z`
и `asked john 134-… 12:58:34Z`. **Различитель — не `mtime` сам по себе, а сравнение штампа СТРОКИ со
штампом своего письма**, и мерить надо после следующего такта, а не сразу.

**`asked` КЛЮЧУЕТСЯ ПАРОЙ ПЛЮС ШТАМП, поэтому перепарковка того же треда НОВЫМ письмом звонит
СНОВА** — это ответ на вопрос, который выше назван неизмеренным, и он правит строку про Д-2. В том
же файле сегодня лежали ДВЕ строки `asked john 134-…` с разными штампами (`09:24:18Z` и
`12:58:34Z`) — то есть немедленный звонок `--expects ack` ушёл дважды по одному треду за день.
Основание в самой двери: `notify.ts` ~831, «The stamp is part of the key here (unlike `parked`,
thread 030 Д-2), because what is remembered is a MESSAGE having been told, not a pair being in some
state». Д-2 («второго звонка перепарковка не даст, пока ключ в состоянии») — про ключ `parked`,
который парой и ключуется; на `asked` он не распространяется. Практическое следствие: **старая
строка `asked` с чужим штампом доказательством «не позвонило» не является вовсе** — она про прошлое
письмо, и сверять надо ШТАМП, а не наличие пары (человек, тред).

**В ТОМ ЖЕ ФАЙЛЕ ЛЕЖИТ ЕДИНСТВЕННЫЙ ДЕШЁВЫЙ ОТВЕТ «какой тред НИКОГДА не поднимется»** — ключ
`freeze\tfrozen\t<role>\t<thread>\t<ts>` (рядом с ним `unaccepted`, `remind`, `drift`). Замер
2026-09-06 (тред `147`, обход открытых тредов): шесть строк `freeze frozen` — пять пар `devops`
(`047`, `056`, `057`, `070`, `079`, все 02–03.09) и одна `dev-core` (`063`, 03.09). У ВСЕХ ШЕСТИ в
ленте стои́т `waiting-on: <эта роль>`, то есть **лента говорит «роль работает», а пара остановлена
потолком попыток**: по одной почте такой тред неотличим от живого, и это ровно класс «осиротел».
Обратное тоже верно: заморожена ПАРА, а не роль — `dev-core` в тот же день отработал пять других
тредов. Ср. [[merged-code-is-not-running-code]]: состояние контура читается его файлами, а не лентой.

**Почему пара мрёт, читается не в почте, а в ХВОСТЕ ЛОГА СЕССИИ.** Там же 2026-09-06: прогон
`.orchestrator/sessions/2026-09-06T10-22-12Z-devops-079-….log` вышел `code 0`, 34 хода, и не написал
в тред ни строки — последними словами сессии: «This run's task is fully complete, but delivery into
the thread is not possible … the established pattern already recorded 16 times before in memory for
this exact identity». Потолок попыток лишь ДОСЧИТЫВАЕТ следствие; причина (у роли под `systemUser`
нет кредов на push, [[contour-git-identity-lives-in-one-home]]) видна только в логе. Поэтому
«роль молчит» проверяется двумя чтениями: `freeze` в `notify.state` и `tail` последнего лога её
сессий — оба даром и оба вне почты.

**How to apply:** после парковки на человеке проверять НЕ выводом `notify`, а строкой своего треда в
`.orchestrator/notify.state`: есть строка `parked <person> <thread> <ts>` с меткой своего сообщения —
доставка ПОДТВЕРЖДЕНА транспортом (при неудаче состояние не трогается вовсе, тред `029`). Отсутствие
треда в сводке при `0 new` — норма, а не потеря; отсутствие СТРОКИ в `notify.state` — дефект пути
«парк → звонок», и прежде чем его называть, сверь, что заголовок своего письма нёс `parked-on:` того
человека. Связано с [[parked-on-freezes-the-turn]].

**Из указателя (перенесено 2026-09-03, оглавление шло за потолок):** свою парковку на человеке проверяют строкой в `notify.state`, а не выводом сухого `notify`; а ОТСУТСТВИЕ строки читается однозначно «не звонило», потому что снятие парка строку не удаляет (тест самой двери); но молчит ПИСЬМО, а не тред — следующий парк того же `064` дал строку через 12 секунд

**Из указателя (перенесено 2026-09-06, оглавление шло за потолок):** парковку проверяют БЛОКОМ своего треда в `notify.state`, и ключей там ДВА (`asked` = немедленный звонок, `parked`); греп по одному `parked` дал ложное «не звонило» — парк на человеке ставят ПОСЛЕДНИМ письмом такта; там же `freeze frozen <роль> <тред>` = «лента ждёт роль, а пара мертва»

**Из указателя (перенесено 2026-09-06, оглавление шло за потолок):** парк проверяют БЛОКОМ своего треда в `notify.state`, и ключей ДВА (`asked` / `parked`) — греп по одному дал ложное «не звонило»
