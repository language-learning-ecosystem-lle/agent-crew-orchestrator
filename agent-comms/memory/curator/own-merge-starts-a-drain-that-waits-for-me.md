---
name: own-merge-starts-a-drain-that-waits-for-me
description: "Своя кнопка merge заводит слив ящика в ту же минуту, и слив ждёт ИМЕННО эту сессию — после merge ход сворачивается, а не продолжается"
metadata: 
  node_type: memory
  type: project
  originSessionId: 7e0a5cd6-8e8f-4d9f-ba2a-fbc0274fcc75
  modified: 2026-09-13T10:31:32.082Z
---

Замерено 2026-09-08 (тред `173`, merge #342): squash лёг в `main` в `14:19:09Z`, а уже в `14:19:32Z`
— через 23 секунды — ящик написал `.orchestrator/self-restart.json` с `target` = МОЙ squash
(`46288a6b`, `from` 784f4d04, `behind: 1`, `at === drainSince`), и последняя строка
`.orchestrator/daemon.log`:

```
SELF-RESTART: this tick launches nothing — the box is draining to restart
(waiting for curator/173-daemon-self-restart) and there was nothing to withhold
```

То есть **слив держит ровно та сессия, которая нажала кнопку**, и очередь стоит, пока она не кончит
ход ([[drain-stalls-the-whole-queue]]). Никакого сигнала об этом в почте нет: узнаётся только
чтением `daemon.log`/`self-restart.json` в контуре.

**Второй случай, 2026-09-12 (тред `180`, merge #373): 23 с — не константа, а пример.** Squash
`bbab2d9a` лёг в `16:35:18Z`, `drainSince` — `16:35:56Z` (**+38 с**), и журнал ящика назвал живой
сессией `curator/180-selfheal-leaves-the-workspaces-behind` — тот же тред, которому принадлежит
смёрженный PR. Ход той сессии кончился в `16:36:44Z`, ящик пошёл в `16:37:42Z` (ждал **106 с**), а
преемник поднялся в `16:37:53Z`. В соседнем эпизоде того же дня (#372) ожидание вышло **22 минуты** —
разброс задаёт длина ЧУЖОЙ живой сессии, поэтому «через минуту всё равно перезапустится» не считается.
И withhold бывает НЕ пустым: первый такт слива напечатал `launches NOTHING
(dev-core×180-… stay in the queue)` — эта ветка старая (#297, тред 141), новой нормы в ней нет.

**Третий случай, 2026-09-12 (тред `180`, merge #374, squash `316991aa` в `16:51:29Z`): `drainSince`
`16:52:02Z` (**+33 с**), живая сессия снова `curator/180-…`, ожидание **72 с**, преемник в
`16:53:25Z` — кнопка→новый процесс 116 с. И здесь же закрыто обещание withhold'а, которое два
прежних эпизода проверить не дали: придержанная пара `dev-core×180` (`stay in the queue`,
ждёт с `16:51:52Z`) стоит ПЕРВОЙ строкой плана у преемника — `the plan of this tick: 1 launch …
dev-core×180-…`. Пара не теряется сливом, а дочитывается новым процессом.

**Четвёртый случай, 2026-09-12 (тред `180`, merge #375, squash `fc2b9f3c` в `17:58:18Z`):
`drainSince` `17:58:41Z` (**+23 с**), живая сессия **снова `curator/180-…` — четвёртый эпизод из
четырёх**, ожидание **209 с** (кнопка→новый процесс 244 с). Обещание withhold'а закрыто ВТОРОЙ раз
подряд и сильнее: придержанная `dev-core×180` поднялась на ПЕРВОМ же такте преемника — раньше, чем
`curator×161`, хотя письмо о перезапуске легло тем же тактом. Разброс ожидания по четырём эпизодам —
72–1320 с, то есть «минуты», а не «секунды».

Второе следствие, которое легко потерять: в перезапуск уходит процесс на СТАРОМ коде, поэтому
починка пути самоперезапуска не проверяется тем самым эпизодом, который её устанавливает
([[merged-code-is-not-running-code]]) — запись оставляет уходящий (старый) писатель, а читает её уже
новый преемник.

**Пятый случай, 2026-09-13 (тред `177`, merge #382, squash `14127d03` в `10:28:03Z`): запись легла в
`.orchestrator/daemon-drift.json` (**+14 с**, `at 10:28:17Z`, `behind: 1`, `refSha` = мой squash) —
то есть имя файла у этой записи не одно, и искать надо ОБА (`self-restart.json`, `daemon-drift.json`).
Её поле `why` прозой называет живые сессии, которых ящик ждёт, — и **моей среди них не было**
(`curator/189`, `dev-core/179`, при живой `curator×177`). Вывод «меня не ждут, можно копать дальше»
из этого списка НЕ делается: список читается как справка, а правило остаётся прежним.

**Why:** после merge остаётся соблазн «раз я тут, домержу хвост / докопаю ещё предмет». Цена этого
теперь не абстрактная: каждая лишняя минута такта — минута, когда ящик не поднимает НИКОГО, и платят
её все треды, а не мой.

**How to apply:** merge ставить как ПОСЛЕДНЕЕ действие хода — след гарда 5 письмом и передача хода
сразу за ним. Хвост чужих PR мержить ДО своей кнопки или отдельным подъёмом, а не после.
