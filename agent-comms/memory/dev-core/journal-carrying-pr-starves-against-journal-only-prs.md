---
name: journal-carrying-pr-starves-against-journal-only-prs
description: "КЛАСС ЗАКРЫТ #408 (14.09): запись живёт в docs/journal/<role>/<NNN-slug>.md, общий хвост — архив. Ниже — как было до этого и почему форма чинит структурно"
metadata: 
  node_type: memory
  type: project
  originSessionId: 95fe3c23-05be-44a2-9bec-94f247176198
  modified: 2026-09-14T10:55:53.965Z
---

**КЛАСС ЗАКРЫТ. #408 сел в `main` `1b47a5cb4` 2026-09-14 (норма john 13.09, тред `195`): новая запись
кладётся ФАЙЛОМ, НАЗВАННЫМ ТРЕДОМ — `docs/journal/<role>/<NNN-slug>.md`, `NNN-slug` из строки
`thread:` описания ТОГО PR, которым запись едет. Общий хвост `docs/journal/<role>.md` больше не
пополняется — он архив до 13.09, история не правится.** Имя файла уникально по треду, значит add/add
в общем хвосте не рождается вовсе: попутная запись перестала быть налогом на предметный PR, и
ПОРОГ «после второго разворота снять запись с ветки» БОЛЬШЕ НЕ НУЖЕН — она остаётся в своём PR.
Замер, на котором это поймано: #416 (тред `192`) развернуло трижды за 54 минуты (`09:55Z` #415/#418,
`10:19Z` #386, `10:49Z` #420), каждый раз конфликтовал ровно один путь — хвост; четвёртой склейки не
случилось только потому, что #408 сел ПОСРЕДИ третьей. **Отсюда же смежное: норма приезжает внутри
твоего такта — перед ручной починкой конфликта перечитай, во что ты его чинишь** ([[origin-main-can-advance-mid-tick]]);
поймал я подмену не глазом, а тем, что `git diff --numstat origin/main HEAD` вдруг показал удаления
в `PROTOCOL.md`.

Ниже — как было ДО 14.09; читать, когда разбираешь старые треды или архивный хвост.

PR, несущий попутную запись в `docs/journal/<role>.md`, разводится с базой каждый раз, когда в
`main` садится PR, пишущий в ТОТ ЖЕ файл: все записи дописываются в ОДИН хвост ОДНОГО файла, значит
стык всегда add/add в хвосте. Замер 13.09: за один такт база уехала 6 раз (#385, #387, #390, #391,
#392, #393), смотритель назвал разрыв #383 ТРИЖДЫ; четыре зелёных `checks` — и ни одной повешенной
метки `review`.

**Решает ФАЙЛ, а не «журнальность» PR:** #393 был чисто журнальным и сел прямо между моим коммитом и
push — но писал в журнал ДРУГОЙ роли, и `merge-tree` отработал чисто. Отсюда цена перемера базы
перед push мала: он либо ничего не стоит, либо спасает такт.

**Why:** чисто журнальному диффу круг ревью не требуется (норма 11.09, тред `187`), поэтому такие PR
садятся быстро и часто — раз в ~14,5 мин при `checks` 7–8,5 мин. Дешевизна журнального PR и
непроходимость журналонесущего PR — одно свойство с двух сторон. Усиливает это то, что окно метки
закрывает собственный парк: см. [[run-park-under-checks-has-no-lifter]].

**Порог: после ВТОРОГО разворота головы запись СНИМАЕТСЯ с ветки, а не переносится.** Склейки не
сходятся — замер 13.09 по #383: шесть писем «no longer applies», все шесть про ОДИН файл, ни одного
про код, пять тактов подряд ушли на склейки, предмет не сдвинулся ни на строку. Снятие делает
разрешение конфликта тривиальным и доказуемым (`git checkout origin/main -- <журнал>`, потом
`git diff origin/main -- <журнал>` ПУСТ), и код-PR больше не расходится с журнальными НИКОГДА.
Долг платится отдельным журнальным PR ТЕМ ЖЕ тактом (норма #370 сама разрешает исключение с
названной вслух причиной; тело нового PR и есть место, где причина называется). **Целость вклада
после такой склейки доказывает СОВПАДЕНИЕ `--numstat` против своей базы до и после**, а не дифф со
старой головой: та отличается ещё и всем, что принесла база.

**Голодает и ЧИСТО журнальный PR, и ломает его СВОЙ ЖЕ журнал из ДРУГОГО треда** — замер 13.09 по
#397 (тред `180`): сломал #402, запись `dev-core` по треду `188`, `173 0` в тот же файл; четыре
других журнальных PR того же окна (#396, #398, #400, #404) писали в журнал curator и не задели
ничего. Винить некого: у роли журнал ОДИН, а открытых тредов сколько угодно, и оба PR дописывают в
один хвост. **«Кто сломал» бери по ПУТЯМ диффа, а не по последнему коммиту в `main`:** #402 и #404
сели в ДВУХ секундах друг от друга.

**Окно мержабельности замерено — 28–42 мин, цена починки ~9 мин машины плюс целый такт.** #397:
голова `37d2d9b5` прожила от зелёного `checks` (`14:27:18Z`) до разрыва (`15:09:12Z`) 41 мин 54 с,
голова `6b1dacfc` — от `15:36:33Z` до `16:04:46Z` 28 мин 13 с; свежий `checks` идёт 8 мин 53 с.
Окно больше цены, но не на порядок, и сжимается с каждым новым открытым журнальным PR — цикл
«сломалось → починил → позеленел → сломалось» сходимости НЕ гарантирует. Разрыв говорится ОДИН раз и
только автору, поэтому каждый — отдельный ход роли.

**Порог «после второго разворота снять запись с ветки» к чисто журнальному PR НЕ применяется:** снимать
там нечего — запись и есть весь PR, после снятия остаётся пустой дифф. Порог живёт только у PR,
несущего запись ПОПУТНО коду. Для journal-only остаётся склейка, и она повторяема: исключение гарда 1
переживает и ВТОРОЙ мерж подряд (замер на `c29a4351`: `not asked: this diff is wholly a journal`),
потому что дифф PR трёхточечный. Структурная же причина — норма, а не аккуратность автора: это ход к
curator и кнопка john, своей рукой не чинится.

**How to apply:** закладывай в такт НЕСКОЛЬКО склеек, а не одну. Склейка — `git merge origin/main` В
ветку (push выходит ff по тому же имени, force покупать не надо: [[rebase-without-a-force-push]]).
Хвост собирать механически из двух родительских версий (`git show origin/main:<path>` + свой хвост от
базы слияния), доказывать числами: [[manual-conflict-fix-needs-zero-deletions-proof]]. Доказательство
«удалений ноль» обязано предъявлять ПРИБИТЫЙ SHA базы — `origin/main` уезжает под руками внутри такта
([[origin-main-can-advance-mid-tick]]); замерено: та же сверка до фетча демона дала 0 удалений, после
— 52, и оба числа были верны. Коммит журнала держи ПОСЛЕДНИМ и меняй только его: если
`git diff <зелёная-в-CI-голова> HEAD -- . ':(exclude)<журнал>'` пуст, код побайтово тот, что CI уже
принял, и перепрогонять сюиты незачем.
