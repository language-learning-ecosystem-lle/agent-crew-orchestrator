---
name: daemon-runs-the-checkout-source-so-grep-proves-the-fix-is-live
description: "Демон исполняет исходник ГЛАВНОГО ЧЕКАУТА через tsx, поэтому «новый код действительно исполняется» доказывается `ps` + `rev-parse HEAD` + грепом функции — а не отпечатком из письма"
metadata: 
  node_type: memory
  type: project
  originSessionId: c648822a-18b2-45d0-a497-3646bc778190
  modified: 2026-09-17T12:08:23.341Z
---

Письмо о самоперезапуске про установленную копию не утверждает ничего («критерий консервативен
намеренно» — он называет лишь пути, правка которых МОГЛА сменить программу). Замерить настоящее
состояние дёшево, потому что демон не ставит отдельную копию:

- `ps -o pid=,lstart=,args= -p <pid из daemon-code.json>` показывает
  `node --import …/tsx/dist/loader.mjs …/packages/agent-protocol/src/cli.ts orchestrator up` — то
  есть исполняется исходник ГЛАВНОГО чекаута;
- `git rev-parse HEAD` того же чекаута + `grep -c <имя новой функции>` по её файлам доказывают, что
  починка в исполняемом коде.

**Why:** приёмка «до/после» через границу эпох опирается на то, какой код породил письмо; отпечаток
из письма для этого слишком слаб, а `ps` закрывает вопрос одной строкой.

**Из того же свойства — ЭПОХУ КОДА ДВИГАЕТ `pull`, А НЕ РЕСТАРТ.** Главный чекаут стои́т на
ЛОКАЛЬНОЙ ветке `main` и за `origin/main` сам не едет: замерено 17.09 `git -C <главный чекаут>
reflog show main` — **каждая** запись за неделю `pull --ff-only: Fast-forward`, других движителей
нет. Ловушки тут нет ровно потому, что пакет везёт `pull` внутри своего пути рестарта
(`orchestrator/restart.ts`, `cli.ts:1132`: stop → `git pull --ff-only` → `pnpm install` → старт;
в reflog это видно парой pull `12:34:18` → старт процесса `12:34:28`), и ту же тройку печатает сам
`merge-gate` при диффе, трогающем конфиг (`merge/gate.ts:522`):
`git pull --ff-only && pnpm install && systemctl --user restart agent-protocol@<instance>`.
Опасен только рестарт ГОЛОЙ рукой (`systemctl restart` без `pull`) — он поднимает ТУ ЖЕ эпоху;
поэтому заказ рестарта называется тройкой целиком, а принимается не фактом рестарта, а новой
строкой `daemon — code: <sha>` в `daemon.log`.

**How to apply:** `ps` печатает время старта в TZ СЕССИИ (здесь CEST +02), а `daemon.log` — с
офсетом +03; прежде чем звать расхождение дефектом, сверь зоны. Дату эпохи брать из
[[daemon-log-dates-its-own-code]]; см. [[field-acceptance-is-a-before-after-across-code-epochs]].
