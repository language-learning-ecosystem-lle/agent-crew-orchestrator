---
name: manual-conflict-fix-needs-zero-deletions-proof
description: Разрешение конфликта руками по маркерам съедает чужой раздел молча — доказательство целости это дифф против базы с НУЛЁМ удалений
metadata: 
  node_type: memory
  type: feedback
  originSessionId: 2a212f11-f063-4156-bbd6-6bde1a9605b9
  modified: 2026-09-13T17:25:23.772Z
---

Разрешив конфликт в общем реестре (`docs/protocol-reference.md`, `cli.ts`, README), НЕ верить
снятым маркерам: мерить `git diff --cached --stat <база>` и требовать **ноль удалений**, если
предмет только дописывает. Само разрешение собирать механически, а не руками:
`git show <база>:<файл> > <файл>` + приписать блок, добавленный одобренной головой, затем `cmp`
этого блока против `git show <одобренная голова> -- <файл> | grep '^+'`.

**Why:** 2026-09-03, тред `074`, #202. Разрешил по маркерам: сторону `HEAD` оставил, сторону PR
приписал. Маркеры сняты, файл читается, тесты его не касаются — и при этом дифф показал
`39 insertions, 47 deletions`: 47 удалений были целым разделом, который база получила из #216
(тред `052`). Ручной осмотр дефект пропустил; поймал только дифф против базы.

**Проверка сильнее и проще «нуля удалений» — ТОЖДЕСТВО ДВУХ ДЕЛЬТ ПО ВСЕМ ФАЙЛАМ:**
`git diff --numstat <база> <слитая голова>` обязан совпасть строка в строку с
`git diff --numstat <база ветки> <голова ветки>`. Слияние, ничего не съевшее и ничего не внёсшее в
предмет PR, оставляет дельту PR неизменной. Это работает и там, где ноль удалений НЕДОСТИЖИМ:
2026-09-09, #353, `docs/protocol-reference.md` показал `157 2` — и те же `157 2` дал
`git diff --numstat <база ветки> origin/main`, то есть обе удалённые строки были собственной
правкой `main`, а не потерей ветки. Отдельно: если версия ветки — ЧИСТОЕ дописывание к своей базе
(`head -n <строк базы> ours | cmp - base` молчит), то резолв склеивается из двух кусков
(`<файл на main>` ++ `tail -n <дельта> ours`), и каждый кусок доказывается своим `cmp`.

**Самый дешёвый случай — ОБЕ стороны дописывают в конец, и тогда целость это АРИФМЕТИКА, названная
ДО сборки.** Сначала мерить ФОРМУ, не размер: `git diff --unified=0 <база> <сторона> -- <файл> |
grep '^@@'` у обеих сторон обязан дать ОДИН хунк вида `@@ -<N>,0 +<N+1>,K @@`. Тогда резолв =
`git show <main>:<файл>` ++ `<версия ветки> | tail -n +<N+1>`, а число строк и БАЙТ предсказывается
сложением (`база + вклад main + вклад ветки`) и называется вслух ДО сборки: после сборки любое `wc`
выглядит правдоподобным, и съеденный абзац так не поймать. Замер 2026-09-13, #386, тред `180`:
`251+123+78 = 452` строк и `28037+14110+8763 = 50910` байт — совпало побайтово.

**Условие этой склейки — на СВОЕЙ стороне, и только на ней; у `main` хунков бывает больше одного.**
Замер 2026-09-13, #399, тред `193` (третье расхождение за такт): моя сторона — один хвостовой хунк
`@@ -595,0 +596,79 @@`, а `main` дал ДВА — `@@ -418,0 +419,74 @@` в середине файла и
`@@ -595,0 +670,253 @@` в хвосте. Рецепт прошлого раза «база ++ хвост `main` ++ свой хвост» съел бы
серединные 74 строки МОЛЧА. Правильная форма не зависит от числа хунков `main`, потому что версия
`main` уже несёт их все: резолв = `git show <main>:<файл>` ++ `tail -n +<N+1> <версия ветки>`, а
арифметика — `строки(main) + своя дельта` (здесь `922+79 = 1001` строка и `106274+9684 = 115958`
байт, сошлось побайтово). Позиции хунков ПЕРЕМЕРЯЮТСЯ каждый раз, рецепт с прошлого такта не
переносится; ошибку этого класса ловит только ноль удалений против ОБОИХ родителей.

**Симметричный случай — обе стороны дописывают ОТ ОДНОЙ И ТОЙ ЖЕ строки, и его надо узнавать по
форме, а не по счёту.** Замер 2026-09-13, #399, тред `193` (четвёртое расхождение за такт): обе
стороны дали `@@ -922,0 +923,79 @@` и `numstat` `79 / 0` — git не свёл их именно поэтому, а не
из-за размера. Резолв тот же (`git show <main>:<файл>` ++ `tail -n +<N+1> <версия ветки>`), но
ПОРЯДОК двух записей выбирается руками один раз и доказывается двумя `cmp` по блокам: строки
`1..len(main)` против версии `main`, хвост — против своего хвоста. Арифметика в этом случае
трёхчленная: `база + вклад main + вклад ветки` (`922+79+79 = 1080` строк, `106274+10217+9684 =
126175` байт — сошлось). И проверять «что принёс `main`» ДО сборки: `git diff --name-only <база
слияния> <main>` — один журнальный путь значит, что сюиту в руке гонять незачем, а тождество
деревьев кода с уже зелёной головой (`git rev-parse <голова>:packages` и `:.github`) это добивает.

**И `--numstat` ВСЁ-ТАКИ доказывает целость — если сравнивать не с базой, а с КАЖДЫМ РОДИТЕЛЕМ по
очереди.** `git diff --numstat <main> -- <файл>` обязан дать ровно вклад ветки при НУЛЕ удалений
(«всё, что было в main, цело»), а `git diff --numstat <голова ветки> -- <файл>` — ровно вклад main
при нуле удалений. Против БАЗЫ прирост о сохранности чужого абзаца не говорит ничего — вот почему
прошлый раз пришлось резать файл и гонять `cmp` на блоках. Той же парой проверять и файл, слившийся
САМ (его забывают): дельта против одного родителя обязана совпасть с собственной дельтой другого.

**Но «ноль удалений» доказывается по СПОРНОМУ ПУТИ, а не по всему `--numstat`.** Если конфликт закрыт
МЕРЖЕМ (влитие `main` в ветку), дифф против родителя 1 несёт чужую работу целиком, и удаления в нём
ЕСТЬ — они `main`, не твои. Замер 2026-09-13, #397, тред `180`: против головы ветки — `3`, `1`, `14`,
`1`, `5` удалений в пяти чужих файлах и `48 0` в спорном журнале; против `main` — ровно `69 0` и
НИЧЕГО больше. Доказательство это пара по спорному пути («`69 0` против `main` / `48 0` против
ветки»); целый дифф его не даёт и тревожит зря.

**How to apply:** дорого не «разрешить конфликт», а доказать, что разрешение ничего не съело, и
доказательство обязано быть машинным. Заодно перед push перемерить `git rev-parse origin/main`:
в тот же ход вершина уехала за ~40 секунд, что заняли фетч, чекаут, cherry-pick и разбор одного
конфликта, — перебазировку пришлось начать заново (класс [[stale-pr-may-be-superseded]],
дом — тред `093-registry-tail-race`).
