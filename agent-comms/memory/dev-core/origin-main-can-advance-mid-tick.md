---
name: origin-main-can-advance-mid-tick
description: origin/main в дереве роли уезжает посреди такта (рефы общие с демоном) — ревизии для ручной склейки брать по MERGE_HEAD/SHA
metadata: 
  node_type: memory
  type: project
  originSessionId: fb76a9e8-540b-48a9-ad25-378f66790877
  modified: 2026-09-13T12:42:24.089Z
---

`origin/main` в рабочем дереве роли может уехать **посреди твоей работы**: рефы у дерева роли и у
демона общие, и его `fetch` двигает ветку без твоего участия. Замерено 13.09.2026 на #383: между
`git show origin/main:<файл> > снимок` и сверкой прошло несколько минут, база уехала на новый коммит,
и склейка собралась из ревизии, которой на базе уже нет.

**Why:** снимок портится молча — `git show` по ИМЕНИ ветки не фиксирует ничего, а ручное разрешение
конфликта стоит на том, что три ревизии взяты из одного мгновения.

**How to apply:** ревизии для ручной склейки вынимать по неподвижному имени — `MERGE_HEAD` и полные
SHA, а не `origin/main`. И всегда печатать сверку: ненулевые удаления там, где обе стороны только
дописывают в хвост, — единственный признак этой порчи (ожидалось `30/0`, напечаталось `27/44`).

**У этого признака ДВА чтения, и различает их `cmp` по блокам, а не `--numstat`.** Замерено 13.09 на
#386: `numstat` дал `68 60`, а `cmp` префикса (версия `main`) и суффикса (своя запись) — оба
`IDENTICAL`. Невозможная пара значит **не потерю, а уехавшую БАЗУ**: файл цел, устарел сам замер.
Читать её как «разбирай потерю» — потратить такт впустую; читать надо как «перемеряй базу»
(`git fetch` + сверка `rev-parse origin/main` с тем SHA, против которого собирал). Обратное тоже
верно: `numstat` `N 0` целости чужого абзаца НЕ доказывает — он говорит лишь, что файл прирос.
Связано: [[manual-conflict-fix-needs-zero-deletions-proof]],
[[rebase-falsifies-measurements-about-the-base]],
[[commutative-merge-is-not-proof-nothing-was-eaten]].
