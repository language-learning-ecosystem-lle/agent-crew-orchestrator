---
name: run-park-under-checks-has-no-lifter
description: "Парк run:N под `checks` С 13.09.2026 снимается полем `run-outcome` письма об исходе — лифт поднимает АВТОРА, и он срабатывает ДАЖЕ на исход по УЕХАВШЕЙ голове."
metadata: 
  node_type: memory
  type: project
  originSessionId: 670bae20-6228-4b9e-986a-d4a70a99daaf
  modified: 2026-09-14T14:16:58.496Z
---

**Почин­ка приземлилась: #383 (`e25da912c`, тред 188) влит 2026-09-13T16:58:46Z.** `judgeParkSeen`
(`thread/park-seen.ts`) снимает `run:N` тремя полями: `verdict:`+`pr:N`, `merged-pr: N` и
**`run-outcome: N`**; последнее пишет машинный писатель `.github/workflows/ci-outcome.yml`
(`OUTCOME_ARGS=(--run-outcome "$PR")`, пустой набор на красном main, где PR-адресата нет). До этого
дня парк под `checks` не имел подъёмника вовсе и стоял до потолка `RUN_PARK_TTL_SECONDS` (30 мин):
замерено в 187 двумя случаями — 23 мин 40 с и 28 мин 09 с, ~52 мин мёртвой пары за сутки.

**ЖИВАЯ ПРИЁМКА СДЕЛАНА, перемерять не надо:** демон встал на `e25da912c` в 17:00:24Z, парк
`run:401` (тред 189) объявлен 17:07:37Z → `checks` #401 `success` → письмо `17:09:53Z` с
`run-outcome: 401` → парка нет к 17:11:04Z. **136 с против потолка 1800.** Отрицательный контроль
живой: парк `run:399` при `checks pending` СТОИТ. Расхождения версий у флага нет по построению —
`ci-outcome.yml` это `workflow_run` с дефолтной ветки и чекаутит код себе `ref: main, path: .code`.

**Why:** до формы (б) дверь ОБЕЩАЛА машинному писателю лифт «по номеру события», и обещание было
правдой про `pr:` парки и ложью про `run:` под `checks` — исход `checks` вердикта не объявляет.

**How to apply:** парк `run:N` под идущим `checks` теперь честен — он гасит бесполезный подъём под
красный/идущий прогон и лопается ровно письмом об исходе, а не потолком. **Но считай, КОГО поднимет
лифт:** то же письмо `ci-outcome` ставит `--waiting-on <роль из `role:` описания PR>`, когда есть
`LABEL_CALL`/`ACTIONABLE` — то есть АВТОРА, а не держателя кнопки. На журнальном PR (где метка
`not asked`) это подъём автора ровно на «перемерить дверь и передать ход curator». `pr:N` парк
`run-outcome` НЕ снимает (сказано в README явно) — его адрес только `merged-pr: N`.

**И ЛИФТ НЕ СМОТРИТ НА ГОЛОВУ — подъём по `run-outcome` НЕ значит «мой прогон готов».** Поле
штампуется по ОДНОМУ условию `[ -n "${PR:-}" ]` (`ci-outcome.yml:573`), а `judgeParkSeen` сверяет
только НОМЕР PR. Тот же файл тест на устаревшую голову знает и применяет дважды —
`[ "$PR_HEAD" != "$HEAD_SHA" ]` глушит зов на метку (строка 396) и печатает «⚠️ голова уже
сдвинулась» (строка 478), — но на штамповке поля его нет. Зов к действию заглушен, подъёмник нет.
Замер 2026-09-14, тред 196, PR #409: парк `run:409` поставлен 14:04:41Z (потолок был бы ~14:34Z),
подъём пришёл 14:07:54Z из письма об исходе по голове `4c0059fe`, когда головой PR была уже
`15afa1ca`, — а прогон по НЕЙ закрылся только 14:12:44Z. За одно окно уведомитель выписал три
`run-outcome: 409`, и лишь ОДИН относился к текущей голове: голова двигалась быстрее, чем шёл
прогон (три головы за 15 минут).

**How to apply:** подняли по `run-outcome` — ПЕРВЫМ делом сверь голову письма с
`gh pr view N --json headRefOid`, а не верь «зелено, вешаю метку». Не совпали — твоего прогона ещё
нет: его надо найти по `head_sha=` и решать заново. Форма починки (к john через curator, это док
власти): обернуть штамповку тем же тестом, сохранив поле при ПУСТОМ `PR_HEAD`, — неизвестность не
должна отнимать у парка единственного подъёмника, а потери лифта не будет, ведь голову двигает
только push, и push всегда рождает свой `checks`.

Про требование живого прогона — [[do-not-park-on-a-green-ci-run]]; про потолок —
[[carrying-a-park-forward-does-not-restart-its-ceiling]]; выбор формы — [[park-pr-vs-run-choice]].
