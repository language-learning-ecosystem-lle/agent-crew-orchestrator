---
name: flake-is-proven-by-a-diff-between-heads
description: Красное и зелёное на побайтово одном коде — доказательство флака без воспроизведения
metadata: 
  node_type: memory
  type: feedback
  originSessionId: d7e44d77-1bc0-4527-8463-f8e6a6c095a0
  modified: 2026-09-13T14:43:56.664Z
---

Красный прогон доказывается ФЛАКОМ, а не воспроизведением: если следующая голова `main`
зелена, а `git diff --stat <красная> <зелёная> -- <путь упавшего>` ПУСТ, то оба прогона
гоняли побайтово один код на одном входе — и разный ответ и есть замер флака. Локальный
цикл (даже 3000 прогонов под нагрузкой) этого не добавляет и молчит одинаково при обеих
причинах.

**Why:** охота за воспроизведением одноразового отказа съедает такт и часто кончается
ничем, а вопрос «код тот же?» отвечается двумя командами и отделяет флак от регрессии
раньше первой гипотезы. Замер треда 185: прогон 34377052327 (`caf3fd0cd`) красен,
34378904884 (`47ec05764`) зелен, дифф в `.github/scripts/**` между ними пуст.

**Второй голове быть не обязано: ЛОКАЛЬНО флак берётся диффом ПОЛНОГО прогона против ОДИНОЧНОГО
файла на ТОЙ ЖЕ голове.** Полный прогон красен → перепрогнать УПАВШИЙ ФАЙЛ ОДИН
(`npx vitest run --root packages/agent-protocol <путь>`) до всякой гипотезы о стыке. Зелен —
значит тот же код дал две краски, и разница это НАГРУЗКА (у ящика рядом живёт работающий контур).
Замер 2026-09-13, тред `180`, слитая голова #386: полный `2 failed | 4208 passed`, 242 файла, 418 с,
оба падения в `src/orchestrator/run.process.test.ts` (`the release waited on the unlock`); тот же
файл один — `60 passed`, 215 с. Класс теста виден заранее: ассерт, ждущий снятия лока чужим
процессом, меряет ВРЕМЯ и под нагрузкой красен по построению (ср. [[assert-must-not-read-the-wall-clock]],
[[worktree-lock-and-lease-fold-are-two-instants]]). Независимая проверка того же вывода — ДОМЕН:
если слияние принесло только пути, которых упавший тест не исполняет, стыку взяться неоткуда ещё
до прогона. Авторитет всё равно `checks` на новой голове, и обратное («красно там же») — уже находка
о тесте, а не флак.

**How to apply:** увидел красный прогон на голове, которая уже не тип — сперва цвет
текущего типа (`gh run list --branch main --json headSha,conclusion`), потом дифф ПУТЕЙ
упавшего теста между двумя головами. Пусто → флак: чинится не причина (её замером не
взять), а СЛЕПОТА — форма сверки, которая не даёт назвать, что именно не состоялось
(см. [[probe-collapses-two-outcomes-into-rule-broken]]). Непусто → регрессия, и её несёт
именно этот дифф. Соседнее: [[red-main-checks-may-be-comms-sync]],
[[nonzero-probe-exit-means-check-did-not-happen]].
