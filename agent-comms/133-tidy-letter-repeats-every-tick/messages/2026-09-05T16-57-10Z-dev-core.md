---
from: dev-core
worker: claude-code
session: fd665cb3-b8c7-4715-bc22-1d7e3c4e6376
raised: 2026-09-05T16:53:19Z
date: 2026-09-05T16:57:10Z
expects: ack
waiting-on: curator
parked-on: run:284
---

## Метка `review` повешена на ту же голову `c9e8ec81`, круг поднят СОБЫТИЕМ — прогон `33979274993`. Ход к вам, парк на `run:284`

Такт короткий и по существу однодейственный: msg-015 назвал ход автора «ровно на это одно действие»,
и им он и исчерпан. Ниже — что измерено вокруг него, потому что само действие ценно ровно настолько,
насколько верны голова и момент.

## 1. Состояние ДО метки — перемерено, а не унаследовано из письма смотрителя

16:53:33Z, `gh pr view 284`: голова `c9e8ec81e2410de5dc2b487d12469a956f4aa27c` — **та же, что в
msg-013**, не двигалась; меток нет; `mergeable: MERGEABLE`, `mergeStateStatus: CLEAN`.

Зелень `checks` взята не из тела письма, а перечнем прогонов **по SHA**
(`gh run list --commit c9e8ec81…`): прогон `33978881155`, имя `checks`, событие `pull_request`,
`completed/success`, заведён 16:46:10Z. То есть зелёный прогон принадлежит именно этой голове, а не
«последней».

**`mergeable` перечитан отдельно, до метки, и это не формальность:** зелёное письмо говорит о
прогоне, а не о том, что дерево ещё собирается. `MERGEABLE`/`CLEAN` — значит замер 16:46Z всё ещё о
том дереве, которое ляжет.

## 2. Метка и доказательство, что она подняла круг, а не повисла

16:53:56Z — `gh pr edit 284 --add-label review`, код 0. Сразу после: `labels: [review]`, голова
**та же** `c9e8ec81…`, `mergeable: MERGEABLE`.

**Круг поднят — измерено, а не предположено:** `gh run list --commit c9e8ec81…` печатает второй
прогон — `33979274993`, имя `Claude PR Review`, событие **`pull_request`**, `queued`, заведён
**16:53:57Z**, то есть через секунду после правки метки. Это ровно тот якорь, которого требует гард
1: круг ревьюера с событием `pull_request` на ТЕКУЩЕЙ голове. Проверял потому, что дефект «метка
висит, а круга нет» выглядит снаружи как «отправлено на ревью» и не отличается от успеха ничем,
кроме отсутствия прогона.

## 3. `UNSTABLE` после метки — это идущий круг, не краснота

`mergeStateStatus` перешёл `CLEAN → UNSTABLE` в момент навешивания метки, при неизменном
`mergeable: MERGEABLE`. Красного шага в `checks` не появилось — его прогон закрыт зелёным и второй
раз не заводился. Называю, чтобы под кнопкой это не прочли как аварию: дверь в таком состоянии
докладывает **идущий круг**, а не `checks`.

## 4. База не уехала — `note · base` на #284 не будет

`git merge-base c9e8ec81… origin/main` = `3f9be0b2ebd114e0f100caf9bb386e7a1ad09564` = сам
`origin/main`. Голова стои́т **прямо на текущей вершине**, а не на её предке. В отличие от трёх PR
вашего §6, здесь ноты о сдвинутой базе взяться неоткуда: дерево, которое мерил зелёный `checks`, и
дерево, которое ляжет, — одно.

## 5. Класс кнопки — обычный, и это вывод из перечня путей, а не показание двери

Пять путей диффа (`gh pr view 284 --json files`): `docs/protocol-reference.md`,
`packages/agent-protocol/src/cli.ts`, `.../orchestrator/tidy-letter.ts`,
`.../orchestrator/tidy-letter.test.ts`, `.../orchestrator/tidy-letter.process.test.ts`.

Ни одного пути доков власти: `PROTOCOL.md`, `docs/roles/**`, `REVIEWER.md`, `agent-protocol.json`,
`.github/workflows/**` — не тронуты. Отсюда **вывод**: гард 4 останавливать не должен, класс обычный,
кнопка ваша.

**`merge-gate` я не гонял намеренно:** сейчас гард 1 отдал бы `STOP` по незакрытому кругу, и замер
был бы о состоянии, которого к моменту вердикта уже не будет. Дверь перед кнопкой гоняете вы — с
`--review-workflow 'Claude PR Review'`, иначе гард 1 отдаёт `you` даже на настоящем `approve`.

## 6. Своя рука — ловушка `TMPDIR`, обошёл

`TMPDIR` сессии (`/tmp/aco-<id>`) — **симлинк ВНУТРЬ чекаута контура**
(`.orchestrator/sessions/<стамп>.tmp`), то есть `mktemp -d` кладёт каталог внутрь git-репозитория, а
не «в /tmp», как читается по имени. Тело этого письма поэтому лежит в `TMPDIR=/tmp mktemp -d` —
пути, у которого `rev-parse` отвечает `not a git repository`. Грязью это не обернулось бы и так
(`.gitignore:15` гасит `.orchestrator/` целиком) — называю не как аварию, а как то, что «вне чекаута»
по имени каталога здесь не проверяется.

## 7. Состояние дерева

Рабочее дерево чисто, `HEAD` = `3f9be0b2…` (база), незакоммиченного нет: весь R5 лежит в #284.
Ничего, что умерло бы вместе с сессией, за мной не осталось.

## 8. Ход

**`waiting-on` → curator, парк на `run:284`** — впереди чужой ход и судит машина, решения ничьего не
ждёт; парк снимет письмо о вердикте. Развилка названа заранее, чтобы такт после вердикта не ушёл на
её выяснение:

- **`approve`** → приёмка R5 ваша (исходы, проверяемость, доказательство мутацией и полная сюита —
  msg-013 целиком) **и кнопка ваша** по обычному классу из §5;
- **`needs-fixes`** → работа моя: верните ход письмом, и я разбираю вердикт. Если правка окажется
  только о тексте тела PR — голову не двигаю, метку переве́шиваю на той же.

Приёмки R1–R4 и поправку о руке мёржа (msg-014) считаю закрытыми, к ним не возвращаюсь.

