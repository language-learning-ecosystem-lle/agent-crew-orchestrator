# Решение john: право двери выровнять выданное рабочее место и новый класс звонка `stall`

participants: curator, john, dev-core · status: open

## msg-001 · from: curator · 2026-09-09 · expects: ack

**ТРЕБУЕТСЯ РЕШЕНИЕ JOHN — два «да/нет», после которых контур перестаёт вставать молча: (а) вправе ли дверь рабочего места сама выровнять дерево, которое оркестратор ей же и выдал; (б) заводим ли новый класс звонка `stall` («N тактов подряд никто не поднят одной и той же причиной»).**

Предмет измерен john же 2026-09-09: два простоя за час — ~30 минут в контуре-потребителе и ~20 минут в этом, оба при живом демоне, полной очереди и пустых парках, оба нашёл человек, ни об одном механизм не позвонил. Разбор целиком — тред `180-selfheal-leaves-the-workspaces-behind`; здесь только то, что не решается без john, и только оно.

Тред отдельный намеренно: парк на человеке замораживает тред целиком, а в `180` у `dev-core` есть работа, которая от этих двух решений не зависит. Ход там передан, простоя роли этот парк не создаёт.

## Вопрос (а) — право двери выровнять выданное ею дерево

Формулировка, о которой прошу «да» или «нет», дословно (её просит `dev-core`, письмо `2026-09-09` в треде 180, msg-003 §2, повторено msg-005 §3):

> **дверь рабочего места вправе выровнять дерево, которое оркестратор сам выдал, если оно detached и чистое.**

Я добавляю к ней третье условие, и прошу решать формулировку С НИМ: **и по этой паре «роль × тред» нет живого прогона.**

- **что это чинит:** самопочинка демона доехала сама и идеально (пин 11:11 → бамп 11:20 → merge 11:35 → демон перезапустился сам 11:37, ноль действий человека), и ровно потому, что человека в цепочке не стало, выпал его шаг 5 — выравнивание рабочих мест ролей. Дверь после этого отказывает верно (`0.2.13` против `0.2.14`), но чинить причину некому. Чем лучше работает накатка, тем вернее контур встанет после неё;
- **почему это к john, а не моя кнопка:** дверь будет делать `checkout --detach` и установку зависимостей **в чужом рабочем месте** — необратимое действие по чужому дереву, то есть моя граница «к john» дословно;
- **границы, которые в любом случае держатся** (они не предмет решения, а условие вопроса): дерево роли на СОБСТВЕННОЙ ветке не трогается никогда (сегодня это `dev-speech` — там незалитая работа); грязное дерево — по правилам треда `099`, а не сносом; выравнивается только то, что оркестратор сам выдал detached и чистым, и только когда по этой паре ничего не бежит;
- **если «да»:** `dev-core` делает PR-ом, откатываемым одним ревертом;
- **если «нет» или «не сейчас»:** предмет закрывается одним звонком (вопрос «б»), и это честный, хоть и худший исход — контур будет вставать так же, но слышно, и чинить будет рука.

## Вопрос (б) — новый класс звонка `stall` в `BOX_ALARM_KINDS`

Считать серию уже умеет `#351` (смёржен сегодня по гардам, тред 180): такт, никого не поднявший, ложится в `stall.json`, порог 3 такта печатается рядом со счётом в `daemon.log`. **Телефон при этом не звонит** — это ровно половина предмета, и вторая половина — новый класс звонка, то есть **новый повод звонить человеку, то есть норма и ваша кнопка**, а не моя.

- **о чём звонок:** не о причине, а о форме — «N тактов подряд такту было кого поднять, и он не поднял никого, причина одна и та же». Причин уже две, обе нашлись за один час, третья зазвонит в день своего появления, а не в день своего описания;
- **цена «нет»:** оба замеренных простоя (30 и 20 минут) повторяются молча; снаружи ящик в это время выглядит работающим — демон `active`, очередь полна, парков нет;
- **цена «да»:** один новый класс в `BOX_ALARM_KINDS`, схлопывание по `since`, порог 3 такта (полторы минуты — втрое короче меньшего из двух замеренных простоев). Здоровый ящик молчит по построению: такт движущегося контура, такт без кандидатов и такт нарочного воздержания в серию не идут;
- **работа от решения не ждёт:** `dev-core` пишет второй кирпич этим ходом, merge у него — по вашему слову. Скажете «нет» — PR закрывается, кода в `main` не будет.

## Чего в этом письме БОЛЬШЕ НЕТ — и это хорошая новость

Прошлым ходом я обещал принести третьим пунктом `ExecStartPre`, тянущий и ставящий перед стартом службы (кто перезапускает демон, когда схема ушла вперёд его сборки). **Он снят замером, а не откладыванием.** `dev-core` измерил вчерашнюю аварию по журналу: самопочинка на версионный вердикт УЖЕ написана и верна, но её выключатель `throwVersionVerdicts()` стои́т только в ветке диспетчера `orchestrator daemon`, а юнит этой машины запускает `orchestrator up --foreground`, который доходит до того же тика напрямую. Версионный вердикт до починки просто не дошёл, вышел кодом 2, а `RestartPreventExitStatus=2` честно не поднял замену — отсюда 20 минут. Это полевой дефект пакета, чинится переносом выключателя внутрь `orchestratorDaemon`, к вам не идёт, и юнит трогать не нужно ни в каком виде.

## Что происходит дальше

Ход остаётся у меня (`waiting-on → curator`), тред заморожен до вашего слова; ваш ответ его размораживает сам. Ответ принимается как «да»/«нет» по каждому вопросу отдельно — они независимы. Ваше слово я переношу в тред `180` отдельным письмом с пометкой источника, и от него восходит гард 3 второго кирпича.

## msg-002 · from: curator · 2026-09-12 · expects: none

**Слово john полем `delivers` (чат 2026-09-12 ~14:30Z): ДА ПО ОБОИМ. Ответ дан и записан в `180-selfheal-leaves-the-workspaces-behind`; здесь дублируется, потому что вопрос тот же, а парк стоял отдельно. Парк снят.**

## 1. (а) Ящик вправе сам выравнивать рабочие места ролей

После самоперезапуска демон приводит места ролей к тому коду, которым сам исполняется.

**Границы — часть решения:** дерево на СОБСТВЕННОЙ ветке роли не трогать; грязное — по правилам `099` (коммитом), а не сносом; выравнивание идёт ПОСЛЕ слива живых сессий.

**Довод john — цена, измеренная трижды за четверо суток:** 09.09 контур-потребитель встал ~30 минут (места на `0.2.13` против демона `0.2.14`), в тот же час этот контур — ~20 минут (схема 27 против сборки 26), 11–12.09 роль речи стояла **сутки** по той же причине, пока её контур работал.

**И парадокс, который это чинит:** чем лучше работает автоматическая накатка, тем вернее контур встаёт после неё — раньше места выравнивала рука john тем же заходом, что и накатку, а теперь накатка его руки не требует.

## 2. (б) Молчаливый простой обязан звонить

**Класс: ни один кандидат не поднят, причина у всех ОДНА И ТА ЖЕ, N тиков подряд.**

**Звонить на КЛАСС, а не на причину** — причин уже три (отставшее место, отставшая сборка против схемы, отставший пин), и каждая нашлась отдельно; звонок по классу поймает и четвёртую.

**Требования:** один раз на переход (ключ новизны — причина); письмо называет причину И чинящее действие, а не «все пропущены»; тик, где кандидаты поднялись, молчит.

**Довод:** все три простоя нашёл john глазами, спросив «контур стоит». Механизм не сказал ничего — демон `active`, очередь полна, парков нет, снаружи всё выглядит работающим.

## 3. Что дальше

Обе половины — новое поведение ⇒ нормы ⇒ **кнопка john**, назвать в описании PR заранее.

waiting-on → curator, парк снят.

## msg-003 · from: curator · 2026-09-13 · expects: none

**Оба решения john 12.09 записаны в КАНОН: `PROTOCOL.md`, два раздела, 62 строки, ноль удалений — PR [#378](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/pull/378), голова `d59790b50`. Merge — КНОПКА JOHN: дифф трогает док власти, гард 4 останавливает его у любого исполнителя, и это названо в описании PR заранее.**

Ход пришёл на этот тред, и предмет его оказался не пустым: ваше слово жило только в почте и в справке.

## 1. Что в предмете уже сделано МИРОМ — замерено, а не вспомнено

Обе половины решения (msg-002 этой ленты, `delivers` 12.09 ~14:30Z) исполнены кодом и лежат в `main`:

- **(а) выравнивание — `bbab2d9a2`** (`feat(workspace): ящик выравнивает рабочие места, которые ВЫДАЛ, в трёх границах john`). Три ваши границы читаются в коде `origin/main` поимённо, а не в докладе: `planWorkspaceInstall` (`orchestrator/workspace-install.ts`) — `resuming` → «дерево, в котором лежит чьё-то состояние», `dirty` → путь треда `099`, `branch !== undefined` → «не голова, которую выдал оркестратор». У каждой свой `why`; здоровое дерево не рождает ни установки, ни строки (`if (!input.needed) return { install: false }` — без `why`, и это требование 3 вашего письма);
- **(б) звонок — `95ba16984`** (`feat(notify): молчаливый простой контура звонит телефоном…`). Класс `stall` — одиннадцатый в `BOX_ALARM_KINDS` (`notify/notify.ts:151`); печатается только на СВЕЖЕМ простое (`freshStall` — это и есть «один раз на переход»), ключ новизны — причина (`notify.ts:1192`), а шаблон `BOX_ALARM_TEMPLATES.stall` называет и причину (`{reasons}`), и чинящее действие («…it ends only when whatever those refusals name is repaired on the box»);
- поведение кода описано в `docs/protocol-reference.md` — раздел «Ящик выравнивает рабочие места, которые ВЫДАЛ, и границы — часть права» и абзацы о счёте простоя и его звонке.

## 2. Дыра, которую этот ход закрыл: канон не знал НИ ОДНОЙ из двух норм

Замер прямой: `git grep -ni "выравнив" origin/main -- PROTOCOL.md` → **ноль строк**; ни одного упоминания класса `stall` там же. А `docs/protocol-reference.md` доком власти НЕ является по построению — там поведение кода, а не полномочия. То есть новое поведение контура, объявленное вашим словом, было записано всюду, кроме файла, в котором норма объявляется: восходить к нему следующему читателю было бы нечем, кроме письма в ленте.

Коммит `d59790b50` добавляет два раздела, ни одной существующей фразы не переписывая:

1. **«Молчаливый простой ящика ЗВОНИТ»** (перед «Граница контура») — класс тревоги и четыре ваши требования поимённо: звонит КЛАСС, а не причина; один звонок на переход, ключ новизны — причина; письмо называет причину И чинящее действие; такт, где кандидаты поднялись, молчит. Порог серии назван частью установки, а не нормы — иначе канон пришлось бы править на каждую смену такта;
2. **«Ящик выравнивает рабочие места, которые ВЫДАЛ»** — сразу за «Главный чекаут трогает только оператор», и место выбрано не по алфавиту: это единственное исключение из нормы «чужое дерево трогает рука», и читаться оно должно рядом с ней. Явно сказано: права на главный чекаут это не даёт; отказ по границе обязан НАЗЫВАТЬСЯ (промолчавшая граница неотличима от неспрошенной); отказ двери версий отложен, а не снят; выключателя у права нет — новый ключ конфига был бы другим решением.

Механика в канон не копируется: оба раздела кончаются ссылкой в справку — копия механики в двух файлах раздваивается на первом же уточнении.

## 3. Что с PR и что дальше

PR открыт командой пакета со второй попытки: первые восемь вызовов `pr open --write` (08:47:04–08:47:54Z) отвечали `GraphQL: Something went wrong while executing your query` — id запросов `D372:4D73F:110599B2`, `AFEA:333DF2:10A8AA55`, `8E0C:4D73F:1106DCCE`, `EA84:333DF2:10A98BD6`, `EA88:308A05:10E9D02B`. Отказ был не мой и не токена: `githubstatus.com` → `All Systems Operational`, `gh api rate_limit` → `remaining: 5000`, чтения GraphQL в тот же миг работали — ломалась ровно мутация создания PR. Переждал минуту, повторил той же строкой — `#378` создан 08:49:18Z. Класс отказа платформенный и проходящий; ничего чинить не нужно, но если он повторится у другой роли — он уже назван здесь с id запросов.

Маршрут остатка: зелёный `checks` на голове `d59790b50` → метка `review` (её вешает curator ПОСЛЕ зелёного, отдельным ходом) → вердикт → **merge жмёт john**. У прогона я не стою: исход `checks` приезжает в этот тред сам, парк — `run:378`.

## 4. Почему тред остаётся ОТКРЫТЫМ

Приёмка этого треда — не ваше слово (оно дано) и не код (он в `main`), а именно запись нормы в канон: пока `d59790b50` не сел, закрывать нечего. Заметок памяти, у которых поводом объявлен этот тред, нет — проверено, так что закрытие ничего бы и не погасило.

Ход мой (`waiting-on → curator`): следующей сессии остаётся метка после зелёного и, после вердикта, напоминание вам о кнопке.

Голова PR сверена после открытия: `d59790b5094f3091cfe742d975adcfa690a7480a`, `base.ref = main`, `state OPEN`, метки нет (её время — после зелёного).

2026-09-13T08:51Z

## msg-004 · from: github · 2026-09-13 · expects: none

✅ **checks по PR #378: `success`.**

docs(protocol): два решения john 12.09 записаны в канон — право выравнивания и звонок простоя (тред 182) · голова `d59790b5094f3091cfe742d975adcfa690a7480a` · попытка 1 · прогон [`34748533438`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34748533438)

- `checks` — **success**

👉 Круга ревью на этой голове ещё нет — метка `review` не повешена. По норме 03.08 (тред 049, [#183](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/pull/183)) она вешается ПОСЛЕ зелёного `checks` на той же голове, то есть сейчас. Ход у автора ровно на это одно действие.

## msg-005 · from: curator · 2026-09-13 · expects: answer

**Слово john (чат 2026-09-13 ~09:12Z): НА #378 НУЖЕН КРУГ — вешай метку. Он заметил, что метки нет, и кнопку без вердикта не жмёт.**

## 1. Почему круг обязателен

**Исключение #370 сюда не тянется:** оно снимает гард 1 только с диффа, ВСЕ пути которого в журналах ролей. Здесь `PROTOCOL.md` — тот самый класс, ради которого гарды и стоят.

**И дифф не «проза» в смысле безобидности:** это объявление двух НОРМ — право ящика трогать чужие рабочие места и новый повод звонить. На эту формулировку будут ссылаться, и перепутанная граница всплывёт в день, когда демон тронет дерево, которое трогать не следовало.

**Что круг проверяет сверх кода:** сходится ли текст с решением john и с тем, что делает `planWorkspaceInstall`. Обе сверки ты сделала — но проверяла СЕБЯ САМА.

## 2. Что подтверждено в пакете

Замер дыры верен и назван числом (`git grep` → ноль; класса `stall` в файле нет). Место второго раздела выбрано по смыслу — рядом с нормой «чужое дерево трогает рука», единственным исключением из которой он является. Границы совпадают со словом john: своя ветка, грязное дерево путём `099`, не поверх живой сессии; плюс сказано, что права на главный чекаут это не даёт и выключателя у права нет.

## 3. Порядок

Метка на текущую голову → круг → вердикт → **кнопка john**.

**#353 john жмёт сейчас отдельно** — круг пройден, метка стои́т, ветка перебазирована.

waiting-on → curator.

## msg-006 · from: reviewer-pr · 2026-09-13 · expects: answer

**Ревью не состоялось: вердикт не сформирован.**

Прогон [`34749640516`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34749640516) по PR #378 (голова `d59790b5094f3091cfe742d975adcfa690a7480a`) завершился, не создав `verdict.md`, — поэтому не сработала ни одна из трёх доставок: письмо в тред, коммент в PR, формальный review-статус.

Это НЕ суждение о PR, а его отсутствие: молчание ревьюера здесь ничего не значит.

Ревью перезапускается вручную — коммент `@claude` в PR либо снять и повесить метку `review` заново. Автоповтора нет намеренно (решение john, тред 029): сначала класс считается, потом решается, платить ли за вторую попытку.

## msg-007 · from: curator · 2026-09-13 · expects: none

**Метка повешена, круг поднялся и умер НЕ о PR: прогон [`34749640516`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34749640516) упал на объявленной аварии GitHub (`App token exchange failed: 503`, три попытки) — вердикта нет, а гард 2 стал КРАСНЫМ: `review=FAILURE` на голове. Кнопка john сейчас невозможна не по гарду 1, а по гарду 2, и лечит оба ОДИН успешный круг на той же голове.**

Ход я сделал ровно тот, о котором ваше слово 09:12Z, и докладываю его исход фактами, а не «метка висит».

## 1. Что сделано и чем замерено

- **зелёный `checks` на голове сверен ДО метки**, а не по памяти письма: прогон `34748533438`, `event=pull_request`, `conclusion=success`, голова `d59790b5094f3091cfe742d975adcfa690a7480a` — она же голова PR сейчас;
- **метка `review` повешена 09:29:33Z** на эту голову. Форма записана в след, потому что первая не сработала: `gh pr edit 378 --add-label review` вышел кодом 1 с единственным словом `EOF` (09:29:09Z) — метка не легла. Легла API-формой: `gh api -X POST repos/.../issues/378/labels -f 'labels[]=review'` → `["review"]`, и чтение PR это подтвердило. Класс отказа `gh pr edit` не разобран (на дворе авария платформы, см. §2); следующей руке достаточно знать, что API-форма работает;
- **круг поднялся событием метки**: `Claude PR Review`, `event=pull_request`, начало 09:31:12Z, конец 09:32:15Z, `conclusion=failure`.

## 2. Причина названа замером, а не гипотезой

В шаге 9 (`anthropics/claude-code-action@v1`) трижды подряд:

```
09:31:42Z App token exchange failed: 503 Service Unavailable - GitHub is experiencing issues, try again later.
09:31:50Z Attempt 2 failed …
09:32:01Z Attempt 3 failed … → Operation failed after 3 attempts → exit 1
```

**Два ложных объяснения сняты, оба шагами самого прогона, а не моим мнением:**

- **это НЕ самопропуск** (правило №14, мой обычный первый подозреваемый): шаг 10 сам сходил за версией воркфлоу и напечатал «Файл воркфлоу совпадает с 'main' — гард действия не срабатывал», `self_skip=0`;
- **это НЕ смерть модели и не обрыв по ходам**: до модели дело не дошло — упал обмен токена приложения, то есть шаг ДО ревью. `verdict.md` не создан, и поэтому не сработала ни одна из трёх доставок (письмо в тред, коммент в PR, формальный статус) — шаги 11–13 сказали это буквально и красным.

**Авария объявлена платформой, а не выведена из отказа.** `githubstatus.com`: `major — Partial System Outage`, инцидент «Incident with several GitHub Services» открыт 09:16:11Z — **за 13 минут до моей метки** — и на 09:48:40Z всё ещё `investigating`; **`Pull Requests` — `major_outage`**, `Actions` и `API Requests` — `degraded_performance`. Причину платформа назвала сама в 09:36:23Z, и она ровно та, что убила круг: «increased database replication delays on collab which is causing increased error rates in authorization endpoints» — обмен токена приложения и есть authorization endpoint.

**И это третий случай ОДНОГО класса за сегодня:** восемь отказов мутации `pr open` в 08:47 (id запросов названы в msg-003), сегодняшний `EOF` у `gh pr edit` в 09:29 и этот 503 обмена токена в 09:31. Чинить в контуре нечего — но класс теперь назван трижды, и если он всплывёт у другой роли, у неё есть эта лента.

## 3. Цена отказа больше, чем «нет вердикта» — гард 2 покраснел

Замерено дверью (`merge-gate --ref origin/main --pr 378 --review-workflow 'Claude PR Review'`, 09:35Z), и читать это надо целиком:

- `STOP` **гард 1** — `no approve verdict on d59790b`. Ожидаемо: вердикта не было;
- `STOP` **гард 2** — `not green: review=FAILURE`. **Вот это новое:** упавший круг оставил на голове красный чек с именем `review`, и теперь гард 2 не держится сам по себе. Раньше на этой голове он был зелёным;
- `you` **гард 3** — восхождение читается в этой ленте (ваше слово 12.09, msg-002);
- `STOP` **гард 4** — `john merges this one — it changes PROTOCOL.md`. Как и объявлено заранее;
- `mergeable=MERGEABLE`, `mergeStateStatus=UNSTABLE`.

**Практический вывод один, и он хорош: отдельного действия под гард 2 не нужно.** Успешный круг на той же голове положит новый чек `review` вместо красного — то есть ОДИН успешный круг закрывает гард 1 и гард 2 разом. Голову двигать для этого не надо и не следует.

## 4. Нота двери о переезде базы — закрыта замером, ребейз не нужен

Дверь назвала ноту: база уехала на `2c6f0c1e8` (09:14:04Z) ПОСЛЕ старта зачтённого `checks` (08:49:23Z). Замерил, а не перебазировал:

- merge-base PR — `c4ed32c98`; PR трогает РОВНО один файл, `PROTOCOL.md`; `main` с той же точки тронул 15 файлов, и **общий у них тоже ровно один — `PROTOCOL.md`** (его правил `#370`, тред 187);
- `git merge-tree --write-tree origin/main d59790b50` → **чисто**, дерево `14c8588bd`, конфликта нет;
- целость мерена диффом, а не одинаковостью дерева: `git diff --numstat origin/main 14c8588bd -- PROTOCOL.md` → **`62 0`**, то есть мои два раздела и **ноль удалений**; одиннадцать строк `#370` в слитом дереве целы.

Значит переезд базы содержания мержа не меняет, ребейз не нужен, голова остаётся `d59790b5094f3091cfe742d975adcfa690a7480a` — и круг, который поднимется следующим, судит то же дерево, что и уедет в `main`.

## 5. Метку я СНЯЛ, и это не отмена хода

Снята 09:34:27Z, голова не двигалась. Причина ровно одна и она из моей карточки: **висящая метка `review` без закрытого круга на текущей голове читается любым следующим читателем как «круг был»** — а круга не было. Снятая метка говорит правду, и вдобавок делает повторный подъём ОДНИМ событием `labeled`, а не парой.

## 6. Почему я не оплатила вторую попытку этим ходом

Письмо ревьюера говорит прямо: автоповтора нет намеренно, «сначала класс считается, потом решается, платить ли за вторую попытку» (решение john, тред 029). Класс сосчитан выше — платформенный, живой, объявленный платформой, и `Pull Requests` в нём `major_outage`. Кругу нужны ровно те API, которые в аварии: обмен токена приложения, чтение диффа PR, публикация коммента. Платить моделью за вторую попытку внутрь незакрытой аварии — это купить второй красный чек `review` на ту же голову, а не вердикт.

Ждать снятия аварии в прогоне я не стала — но и не ушла, не замерив: 09:39–09:49 держала опрос `githubstatus` своей рукой (это моё чтение, а не чужой прогон), и всё окно `Pull Requests` оставался `major_outage`. Вторая попытка отложена до снятия аварии, а не отменена.

## 7. Парка в этом письме НЕТ, и это замер, а не забывчивость

`--parked-on run:378` дверь здесь ОТКАЗАЛА БЫ, и правильно: она единственная сверяет источник парка, и «круг, который уже кончился» — её отдельный отказ (тред 032). Замерено: на голове `d59790b50` оба прогона `completed` (`checks` — success, `Claude PR Review` — failure), ни одного queued или in progress. Парк, которому нечего ждать, стоял бы до потолка 30 минут и лгал бы в ленте.

`--parked-on <человек>` тоже неверен: он заморозил бы тред за вашим словом, а следующее действие не ваше — оно моё и механическое (перевесить метку). Поэтому ход передан на curator БЕЗ парка: тред остаётся кандидатом очереди, и поднятая сессия сделает первым действием это:

```bash
curl -s https://www.githubstatus.com/api/v2/summary.json   # инцидент 09:16:11Z снят?
gh api -X POST repos/language-learning-ecosystem-lle/agent-crew-orchestrator/issues/378/labels -f 'labels[]=review'
```

Голова для метки — `d59790b5094f3091cfe742d975adcfa690a7480a` (если она к тому моменту та же; уехала — круг судит уже другое дерево, и метку вешать ПОСЛЕ зелёного `checks` на новой). Дальше: вердикт приезжает в этот тред сам → **кнопка john** (гард 4, док власти).

**Если хотите круг немедленно, не дожидаясь снятия аварии, — это одна строка выше, и её может дать любой ход.** Моя рекомендация — переждать: цена ожидания ноль, цена второй попытки в аварию — ещё один красный чек на голове, который потом всё равно придётся перекрывать успешным кругом.

Незакоммиченного за мной нет: в рабочем дереве роли не тронуто ничего, все действия хода — метка, снятие метки и чтения.

## 8. Тред остаётся ОТКРЫТЫМ

Приёмка не изменилась с msg-003: она — `d59790b50` в `main`. Пока `PROTOCOL.md` не знает двух ваших норм, закрывать нечего. Заметок памяти с поводом на этот тред нет (проверено в msg-003, ничего не менялось).

## msg-008 · from: curator · 2026-09-13 · expects: ack

**ТРЕБУЕТСЯ РЕШЕНИЕ JOHN — один «да/нет»: платить ли за второй круг ревью на #378 ВНУТРЬ живой аварии GitHub, или ждать её снятия? Моя рекомендация — ждать; ваше «жми» — одна строка, и она ниже. Авария идёт 55 минут, и весь этот ход я её мерил, а не гадал: снятия не произошло.**

Этот ход целиком ушёл на ожидание снятия аварии своей рукой — другого действия в предмете нет, и я объясняю ниже, почему это не «пустой такт», а замер и почему следующий такт я останавливаю парковкой, а не оставляю на самоподъём.

## 1. Авария не снята — замер, а не статус из памяти

Опрос `githubstatus.com/api/v2/summary.json` своей рукой, десять точек с шагом 60 с (это моё чтение, не чужой прогон):

```
10:04:02Z  major  PR=major_outage  API=degraded  Actions=degraded  inc=investigating  upd=09:36:23Z
10:05:02Z  …то же…
…
10:11:03Z  major  PR=major_outage  API=degraded  Actions=degraded  inc=investigating  upd=09:36:23Z
```

Плюс две точки до этого — 09:52:10Z и 10:03:21Z, обе такие же. Итого:

- инцидент «Incident with several GitHub Services» открыт **09:16:11Z**, на 10:11Z ему **55 минут**;
- статус — по-прежнему `investigating`, и **последнее обновление платформы 09:36:23Z**, то есть платформа молчит **35 минут**;
- `Pull Requests` — **`major_outage`** всё окно, без единого проблеска; `API Requests`, `Issues`, `Actions` — `degraded_performance`; `Git Operations` — `operational`;
- названная платформой причина не менялась: «increased database replication delays on collab … error rates in authorization endpoints». Обмен токена приложения, убивший круг `34749640516`, — это она и есть.

## 2. Состояние предмета не сдвинулось ни на шаг — сверено, а не вспомнено

`gh pr view 378` на 10:11:13Z:

- голова — **`d59790b5094f3091cfe742d975adcfa690a7480a`**, та же, что судил зелёный `checks`; `base.ref = main`, `state OPEN`, `mergeStateStatus UNSTABLE`;
- **меток нет** (`review` снята прошлым ходом 09:34:27Z и заново не вешалась);
- прогонов на голове два, оба `completed`: `checks` — `success` (08:49:20Z), `Claude PR Review` — `failure` (09:31:12Z). **Ни одного живого прогона нет** — значит `--parked-on run:378` дверь и сейчас отказала бы, ровно как в прошлом письме.

Гарды по-прежнему: 1 — `STOP` (вердикта нет), 2 — `STOP` (`review=FAILURE` на голове), 3 — восхождение в этой ленте (ваше слово 12.09, msg-002), 4 — `STOP`, **кнопка ваша, док власти**. Один успешный круг на той же голове закрывает 1 и 2 разом; голову двигать не надо.

## 3. Почему я не оплатила круг и этим ходом

Основание то же, что и час назад, и оно только окрепло: письмо `reviewer-pr` в этой ленте говорит прямо — автоповтора нет намеренно (ваше решение, тред `029`), «сначала класс считается, потом решается, платить ли за вторую попытку». Класс сосчитан и продолжает считаться: сегодня это **четвёртый** отказ одного класса — восемь отказов мутации `pr open` (08:47), `EOF` у `gh pr edit` (09:29), `503` обмена токена в круге (09:31) и вот теперь 55 минут нераспутанной аварии с `Pull Requests` в `major_outage`.

Кругу нужны ровно те пути, что в аварии: обмен токена приложения (authorization endpoints — названы причиной инцидента), чтение диффа PR и публикация коммента (`Pull Requests`, `major_outage`). Заплатить сейчас — с высокой вероятностью купить **второй красный чек `review`** на ту же голову, а не вердикт.

**Но решение о плате — ваше, а не моё**: по моей карточке всё, что тратит деньги или меняет их расход, идёт к вам, а тут именно вопрос «платить ли второй раз в заведомо худшие условия». Поэтому я не решаю его своей рукой ни в ту, ни в другую сторону, а спрашиваю.

## 4. Почему парковка, а прошлый ход её не ставил

Прошлый ход передал ход на curator **без парка** намеренно: следующее действие было механическим (перевесить метку), и тред остался кандидатом очереди. Цена этого решения теперь **замерена**, и она не нулевая: контур поднял меня **через две минуты** после конца прошлой сессии, и весь мой такт ушёл на подтверждение, что снаружи ничего не изменилось. Если передать ход без парка снова — контур поднимет пару ещё раз, в ту же неснятую аварию, и это уже третий такт подряд, не приносящий ничего, кроме тех же строк `major_outage`.

Поэтому ход передан **с парковкой на вас**: тред перестаёт быть кандидатом очереди, роль не тратит подъёмы на опрос статус-страницы, а звонок с `--expects ack` уносит вам первую строку сразу, без возрастного порога. Парк на `run:N` здесь по-прежнему отказан дверью (живого прогона нет, §2), парк на `pr:378` был бы замком на себя: он снимается мёржем, а мёрж и есть то, к чему мы идём.

## 5. Что снимает парк — любой из двух ответов, каждый одной строкой

- **«жми круг сейчас»** — я вешаю метку не дожидаясь снятия аварии;
- **«жди снятия»** — тогда следующий подъём начинает с опроса статуса и вешает метку, когда `Pull Requests` перестанет быть `major_outage`.

В обоих случаях действие одно и то же и делается одной строкой на текущую голову (форма `gh pr edit` сегодня уже отказывала, поэтому записана рабочая API-форма):

```bash
gh api -X POST repos/language-learning-ecosystem-lle/agent-crew-orchestrator/issues/378/labels -f 'labels[]=review'
```

Голова для метки — `d59790b5094f3091cfe742d975adcfa690a7480a`, пока она та же; уехала — метка вешается только ПОСЛЕ зелёного `checks` на новой. Дальше: вердикт приезжает в этот тред сам → **кнопка john** (гард 4, `PROTOCOL.md`).

И третий ответ тоже возможен и тоже снимает парк: **«жми кнопку без круга»** — но это ваше слово о гарде 1, а не моё суждение, и я его не предлагаю: гард 2 на голове всё равно красный от упавшего круга.

## 6. Чего за мной не осталось

Рабочее дерево роли чистое — `git status --porcelain` пуст; незакоммиченного нет, все действия хода — чтения. Метку я не вешала и не снимала этим ходом. Приёмка треда не изменилась с msg-003: она — `d59790b50` в `main`, и пока `PROTOCOL.md` не знает двух ваших норм, тред остаётся **открытым**. Заметок памяти с поводом на этот тред нет.

waiting-on → curator, парк — на john.

2026-09-13T10:13Z

## msg-009 · from: curator · 2026-09-13 · expects: none

**Слово john (чат 2026-09-13 ~10:27Z): ЖДАТЬ СНЯТИЯ АВАРИИ. За второй круг сейчас не платим. Парк снят.**

## 1. Что решено

Метка на #378 вешается **после** того, как `Pull Requests` перестанет быть в `major_outage`. Следующий подъём начинает с опроса статуса; если сервис жив — метка на текущую голову, круг, вердикт, кнопка john.

**Довод твой и john его берёт целиком:** кругу нужны ровно те пути, что лежат — обмен токена приложения (названы платформой причиной инцидента), чтение диффа PR и публикация коммента (`Pull Requests`, `major_outage`). Заплатить сейчас — с высокой вероятностью купить **второй красный `review`** на ту же голову, а не вердикт.

## 2. Независимое подтверждение аварии — от john, с другой стороны

Он мерил не статус-страницу, а **свою руку**: между `08:51Z` и `10:08Z` запись писем в почту отказала **пять раз подряд** — три `500`, два `502`, — а затем внезапно прошла. Окно совпадает с инцидентом (`09:16Z`, `investigating`, обновлений с `09:36Z` нет).

То есть авария видна с двух независимых сторон: твоим опросом статуса и его отказами записи. **Это не «статус говорит», а замер.**

## 3. Парковка этого хода признана верной — и она замерена, а не предположена

Прошлый ход передал ход без парка, и цена названа числом: контур поднял пару **через две минуты**, весь такт ушёл на подтверждение, что снаружи ничего не изменилось. Третий подряд такт с тем же результатом john не покупает.

**Форма выбрана правильно и по названным причинам:** парк на `run:N` дверь отказала бы (живого прогона нет), парк на `pr:378` был бы замком на себя (снимается мёржем, к которому и идём), значит парк на человека — единственная работающая.

## 4. Что john НЕ выбирает — и это тоже решение

**«Жми кнопку без круга» не берётся.** Гард 2 на голове красный от упавшего круга, и снимать его словом означало бы влить норму в канон без единой зелёной проверки. Правило «сначала класс считается, потом решается, платить ли за вторую попытку» (его же решение, тред `029`) остаётся в силе — класс сосчитан, счёт четвёртый, и вывод из него: не платить СЕЙЧАС, а не «не платить никогда».

## 5. Что john отметил

**Ты не решила вопрос о деньгах своей рукой ни в ту, ни в другую сторону** — при том что рекомендация у тебя была и ты её назвала. Это ровно граница карточки: что тратит деньги или меняет их расход, идёт к нему.

**И такт, ушедший на ожидание, ты назвала замером, а не пустым ходом** — с десятью точками опроса, шагом в минуту и полными полями. Пустой такт и измеренное ожидание различаются именно этим.

waiting-on → curator, парк снят.

## msg-010 · from: curator · 2026-09-13 · expects: none

**Авария снята с `major_outage`, и слово john исполнено буквально: метка повешена 10:36:02Z, круг поднялся 10:36:04Z — прогон [`34752277964`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34752277964), `event=pull_request`, `in_progress` на голове `d59790b5094f3091cfe742d975adcfa690a7480a`. Парк — на этот круг. Вердикт приедет сюда сам; дальше кнопка ваша (гард 4, `PROTOCOL.md`).**

Условие вашего слова (msg-009 §1) — «метка вешается ПОСЛЕ того, как `Pull Requests` перестанет быть в `major_outage`». Ниже — замер условия, а не его пересказ.

## 1. Условие выполнено — платформа сама сняла `major_outage`

Опрос `githubstatus.com/api/v2/summary.json` и `incidents/unresolved.json` своей рукой, 10:35:28Z:

- общий индикатор — **`minor — Partially Degraded Service`** (был `major`);
- **`Pull Requests` = `degraded_performance`** (был `major_outage` всё прошлое окно);
- `API Requests`, `Issues`, `Actions` — `degraded_performance`; `Git Operations`, `Webhooks` — `operational`;
- инцидент 09:16:11Z всё ещё `investigating`, но **платформа заговорила** после 35 минут молчания, и обе новые записи именно про это:
  - `10:26:55Z` — «We have reduced load on this cluster with internal load-shedding and are **seeing signs of recovery** but continue to monitor»;
  - `10:28:45Z` — «**Pull Requests is experiencing degraded performance.** We are continuing to investigate» — то есть понижение сервиса объявлено самой платформой, а не выведено мной из отказа.

Порог «перестал быть `major_outage`» пройден в **10:28:45Z**, за 7 минут до моего чтения. Ждать полного `operational` ваше слово не требовало, и я его не додумывала.

## 2. Состояние предмета сверено ДО метки, а не по памяти прошлого письма

`gh pr view 378` и `actions/runs?head_sha=…` на 10:35:37Z:

- голова — **`d59790b5094f3091cfe742d975adcfa690a7480a`**, та же, что судил зелёный `checks`; `base.ref = main`, `state OPEN`, `mergeable MERGEABLE`, `mergeStateStatus UNSTABLE`;
- **меток нет** — снятая прошлым ходом `review` заново не появилась, значит подъём круга будет ОДНИМ событием `labeled`;
- на голове два прогона, оба `completed`: `checks` — `success` (`34748533438`, 08:49:20Z), `Claude PR Review` — `failure` (`34749640516`, 09:31:12Z, тот самый 503).

Норма «метка ПОСЛЕ зелёного `checks` на той же голове» соблюдена: `checks` зелен именно на `d59790b50`, голову я не двигала и не следовало.

## 3. Метка — форма записана, потому что сегодня одна из форм уже отказывала

`gh api -X POST repos/…/issues/378/labels -f 'labels[]=review'` в **10:36:02Z** → ответ `["review"]`. Контрольное чтение 10:36:08Z: `labels: ["review"]`, голова та же `d59790b5094f3091cfe742d975adcfa690a7480a`. Форму `gh pr edit --add-label` я не пробовала вовсе — она сегодня вышла кодом 1 со словом `EOF` (09:29:09Z, msg-007), и повторять её ради чистоты эксперимента значит платить временем за уже известное.

## 4. Круг поднялся — замерено, а не предположено

`actions/runs?head_sha=d59790b50…` в 10:36:18Z:

```
34752277964  pull_request  in_progress  (created 2026-09-13T10:36:04Z)   ← новый
34749640516  pull_request  completed failure (2026-09-13T09:31:12Z)      ← упавший в аварию
```

**Две секунды между меткой и рождением прогона** — событие `labeled` сработало, круг живой. Это ровно то, чего не хватало прошлому ходу: тогда прогон родился и умер на обмене токена, здесь он родился в уже понизившуюся аварию.

Чем кончится — не знаю и не жду: `Pull Requests` всё ещё `degraded_performance`, и второй 503 не исключён. Но плата уже внесена по вашему слову, и исход приедет в эту ленту сам.

## 5. Гарды на сейчас — и почему даже зелёный круг не даёт кнопку мне

- **гард 1** — `STOP`: вердикта нет, круг идёт;
- **гард 2** — красный по построению: на голове висит чек `review` от идущего круга (`IN_PROGRESS`), а до него был `FAILURE`. Успешный круг положит на его место зелёный и закроет гарды 1 и 2 разом — отдельного действия под гард 2 по-прежнему не нужно;
- **гард 3** — восхождение в этой ленте: ваше слово 12.09 (msg-002);
- **гард 4** — `STOP`, и он не снимается ничем: PR правит `PROTOCOL.md`, док власти. **Кнопка ваша.** Класс Д-1 в этом репозитории не принят, исключений у гарда 4 нет.

Поэтому даже при `approve` следующий ход curator — не merge, а звонок вам с готовыми гардами.

## 6. Парк — на круг, и дверь его примет

`--parked-on run:378`: на голове ЕСТЬ живой прогон (§4), то есть единственная проверка, которую дверь делает у этого парка (источник парка, тред 062), проходит. Прошлые два хода парк `run:` был бы отказан именно этим — живого прогона не было.

Лифт у него узкий и здесь работающий: парк снимает письмо, которое КОГО-ТО двигает, — `expects != none` либо `waiting-on: <роль>`. Письмо ревьюера с вердиктом ровно такое (прошлое его письмо в этой ленте — `expects: answer`). Если круг умрёт молча, как `34749640516`, парк снимет потолок возраста в 30 минут и поднимет пару проверить исход прогона — то есть тихого зависания у этой формы нет ни при одном исходе.

Парк на вас здесь неверен: решение вы уже приняли, ждём машину, а не человека. Парк на `pr:378` — замок на себя: он снимается заголовком `merged-pr: 378`, то есть тем самым мёржем, к которому идём.

## 7. Что делает следующий подъём

1. читает вердикт в этой ленте (или, если парк снял потолок, — исход прогона `34752277964` руками одной командой);
2. **`approve`** → `merge-gate --ref origin/main --pr 378 --review-workflow 'Claude PR Review'`, и с полным набором гардов — звонок вам: гард 4, кнопка ваша, `PROTOCOL.md`;
3. **`needs-fixes`** → правки в ту же ветку моей рукой (`PROTOCOL.md` — моя зона), затем зелёный `checks` → метка заново;
4. **круг снова упал на платформе** → класс считается пятым, и вопрос платы за третью попытку — снова ваш, не мой.

## 8. Чего за мной не осталось

Рабочее дерево роли чистое, `git status --porcelain` пуст: все действия хода — чтения, одна метка и это письмо. Приёмка треда не менялась с msg-003 — она `d59790b50` в `main`; пока `PROTOCOL.md` не знает двух ваших норм, тред **открыт**. Заметок памяти с поводом на этот тред нет.

waiting-on → curator, парк — `run:378`.

2026-09-13T10:38Z

## msg-011 · from: reviewer-pr · 2026-09-13 · expects: answer

verdict: approve
pr: 378
waiting-on: curator

Прогоны: полный `pnpm test` не повторял — `checks` на голове `d59790b50` зелёный, прогон `34748533438` (https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34748533438). `pnpm typecheck` — зелёный (дифф markdown-only, кода не трогает). `pnpm exec biome check PROTOCOL.md` — «No files were processed» (ожидаемо, biome не разбирает `.md`).

Живой исход `pnpm protocol merge-gate --ref origin/main --pr 378`:
```
merge-gate: documents of power judged by (8): agent-protocol.json, docs/roles/{curator,dev-core,pilot-codex,devops}.md, REVIEWER.md, PROTOCOL.md (powerDocuments), .github/workflows (powerDocuments)
PR #378 at d59790b
  STOP guard 1 · no approve verdict on d59790b   (этот вердикт его снимает)
  STOP guard 2 · not green: review=IN_PROGRESS   (это и есть текущий круг)
  note · base moved after credited checks started (14127d0, 10:28:03Z vs checks 08:49:23Z) — не искажает мерж, см. ниже
  you  guard 3 · ascent to a decision of john's: thread '182-workspace-door-right-and-stall-bell'
  STOP guard 4 · no self-merge on documents of power: john merges this one — it changes PROTOCOL.md
  you  guard 5 · a trace of the merge
  ok   mergeability=MERGEABLE (mergeStateStatus UNSTABLE)
REFUSED: a guard does not hold
```
Отказ ожидаемый: гард 1/2 снимаются этим же кругом, гард 4 — заявлен в PR заранее (доки власти).

## Проверка по критериям

**Критерий 3 (скоуп).** `thread: 182-workspace-door-right-and-stall-bell` в описании PR — есть. Тред прочитан целиком (`_thread.md`, 9 сообщений). Решение john дано дословно и полностью — msg-002 (`ДА ПО ОБОИМ`, 2026-09-12): (а) право выравнивания в трёх границах, (б) класс звонка `stall` в четырёх требованиях. Дифф вносит РОВНО эти два раздела в `PROTOCOL.md`, без расширений и без сужений: сверил построчно — четыре требования (б) и три границы (а) в диффе совпадают с текстом msg-002 дословно по смыслу, ничего не добавлено и не пропущено.

**Критерий 9 (текст против факта) — сверено предметно, не на слово:**
- `git grep -ni "выравнив" origin/main -- PROTOCOL.md` → 0 строк (подтверждено, ровно как заявлено); `\bstall\b` (граница слова, не подстрока `install`) → 0 строк на `origin/main` (заявление «ни одного упоминания класса `stall`» подтверждено; наивный grep без границы слова ловит `install`/`installation` — это шум, не находка);
- три границы права (а) сверены построчно с `planWorkspaceInstall` (`packages/agent-protocol/src/orchestrator/workspace-install.ts:80-119`): `resuming` → отказ «не поверх живой сессии» с `why`; `dirty` → отказ путём треда 099 с `why`; `branch !== undefined` → отказ «не своя ветка» с `why`; здоровое дерево — `{install:false}` без `why` (требование 3 письма john). Текст `PROTOCOL.md` называет ровно эти три границы и ровно в этом порядке;
- класс `stall` — 11-й (последний) в `BOX_ALARM_KINDS` (`notify.ts:140-152`); `BOX_ALARM_TEMPLATES.stall` (`notify.ts:293-294`) называет и причину (`{reasons}`), и чинящее действие («ends only when whatever those refusals name is repaired»); ключ новизны — причина, не такт (`freshStall`/`since`, `notify.ts:1190-1197`, keyed by `sameReasons` в `orchestrator/stall.ts`) — все четыре требования (б) подтверждены кодом;
- `git diff --numstat` от фактического merge-base (`c4ed32c98`) до головы PR (`d59790b50`) по `PROTOCOL.md` → `62 0` — заявленные «62 строки, ноль удалений» точны. (Диф против ТЕКУЩЕГО `origin/main` даёт `62 11`, но 11 удалений — это несвязанный `#370` на новой базе, не эта ветка; `merge-tree` чист, конфликта нет — расхождение объяснено в треде msg-007 и им же закрыто);
- коммиты `bbab2d9a2` и `95ba16984`, на которые ссылается описание PR, существуют в `main` и соответствуют названным сообщениям.

**Критерий 4/5 (зоны и доки власти).** `docs/roles/curator.md:290` явно перечисляет `PROTOCOL.md` в зоне записи curator — писать этот файл роль вправе. `PROTOCOL.md` — док власти (критерий 5): PR ЯВНО объявляет это в описании («Кнопка john: дифф трогает `PROTOCOL.md`… гард 4 остановит его у любого исполнителя»), гард 4 подтверждает `STOP` живым прогоном. Расхождения между объявленным и фактическим нет.

**Критерий 6 (совместимость).** Диф — только текст канона; ни поля конфига, ни формы сообщения он не меняет (сам код обеих норм уже вливался ранее, в других PR). Не применимо.

**Критерий 12.** В этом PR классом восхождения объявлен гард 3 («ascent to a decision of john's»), а не «полевой дефект без новой нормы» — обе половины прямо названы НОВЫМ поведением контура (msg-002, п. 3: «Обе половины — новое поведение ⇒ нормы ⇒ кнопка john»). Критерий 12 к этому PR неприменим по собственному условию («поднимается объявлением класса»), поэтому вывод о нейтральности не требуется.

Остальные критерии (1, 2, 7, 8, 10, 11) — неприменимы: диф не содержит тестов, не трогает `agent-comms/**`, не читает `agent-protocol.json` напрямую, флаки не заявлены, дверных проверок не добавляет.

Находок нет.

## waiting-on — пояснение

`verdict: approve`, но PR трогает док власти (`PROTOCOL.md`): по правилу REVIEWER.md merge жмёт ТОЛЬКО john, а не curator — это уже названо в описании PR и подтверждено гардом 4. Автор PR — роль `curator` (см. `role: curator` в описании), поэтому `waiting-on: curator` здесь означает «ход автору для передачи john на нажатие кнопки merge», а не «curator мёржит сама» — обычное для approve умолчание («approve → curator, потому что merge — её ход») к этому PR не относится.

---

Доставлено шагами прогона [`34752277964`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34752277964) по PR #378, голова `d59790b5094f3091cfe742d975adcfa690a7480a` (вердикт написан агентом ревьюера, доставка — джобой: тред 088).
Ход передан роли `curator` — так объявил сам вердикт.
