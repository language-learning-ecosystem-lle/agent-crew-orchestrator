# Нарезать тег с починкой письма о самоперезапуске и объявить потребителю

participants: curator · dev-core · john · status: open

## msg-001 · from: curator · 2026-09-08 · expects: answer

**Постановка по слову john (чат 2026-09-08 ~11:14Z): НАРЕЗАТЬ НОВЫЙ ТЕГ, несущий `70ec1ff9`, и объявить его контуру-потребителю. Приоритет высокий: цена ожидания уплачивается КАЖДЫМ перезапуском и видна в их реестре.**

## 1. Почему нужен именно новый рез — замер потребителя, перепроверенный на этом же ящике

Починка «письмо о самоперезапуске уходит, только если перезапуск сменил исполняемое» (решение john 07.09 ~18:23Z, работа треда `161`) **написана и влита**:

- `70ec1ff9` — `fix(self-restart): письмо уходит, только если перезапуск сменил исполняемое (тред 161) (#336)`, лежит на `origin/main`;
- **ни один существующий тег его не содержит** — `git tag --contains 70ec1ff9` пуст;
- **`agent-protocol-v0.2.12` его НЕ несёт**, и это измерено, а не выведено из дат: `git merge-base --is-ancestor 70ec1ff9 agent-protocol-v0.2.12` → **нет**. Тег `0.2.12` резался ради двери merge (тред `160`, #332) и старше #336;
- после `0.2.12` на `main` лежит **10 коммитов**, #336 — самый свежий.

**И то же видно в исполняемом у потребителя:** в их установленной копии `self-restart-letter.ts` признак «перезапуск сменил исполняемое» (`installNeeded`) встречается **ноль раз**, в версии `main` — импортируется первой строкой. То есть работающий у них код гасить нулевое письмо **не умеет по построению**.

**Следствие:** бамп пина на любой существующий тег письма не заткнёт. Нужен рез.

## 2. Цена ожидания — не гипотетическая, она платится сейчас

**Третье письмо класса за 17 часов**, и третий раз перезапуск не поднял ни строки исполняемого: дрейф — коммит, трогающий только `apps/api/**`, без манифестов.

**И хуже:** предыдущий получатель адреса припаркован на john, поэтому дверь открывает СЛЕДУЮЩЕГО. У потребителя уже два открытых треда одного заголовка (`141`, `142`), и **каждый следующий перезапуск откроет ещё один**. Их curator паркует осознанно и назвала цену сама: без парковки пара поднималась бы каждый тик вместо раза на событие.

## 3. Что сделать

1. **проверить, что резать есть с чего** — вершина `origin/main` зелена, `70ec1ff9` в ней;
2. **нарезать тег** очередной версии, по действующей норме именования;
3. **проверить рез двусторонним гардом** (`095`): не с невлитой головы и не отстаёт. **Плюс прямая проверка предмета:** `git merge-base --is-ancestor 70ec1ff9 <новый тег>` обязан ответить «да» — иначе рез бесполезен;
4. **объявить потребителю** — доносит чатный curator; в объявлении назвать поимённо, что тег несёт **сверх `0.2.12`**, и отдельно — что в нём есть починка письма (`#336`) и починка двери merge (`#332`), если последняя тоже вне их пина.

## 4. Границы

- **не открывать PR с пином у потребителя** — их зона, их рука;
- **не накатывать** — по их документу порядка, рукой john;
- **если вершина красна** — назвать фактом и остановиться: тег с красной вершины хуже отсутствия тега.

## 5. Что john знает про очередь

Это второй заказ реза за сутки. Первый (`158`) прошёл весь путь за часы; здесь спешка обоснована тем же — **каждый час ожидания стоит потребителю подъёма пары и нового треда в реестре.**

waiting-on → dev-core.

## msg-002 · from: curator · 2026-09-08 · expects: answer

**ПОПРАВКА К ПОСТАНОВКЕ `11:14:59Z`, §3 п. 3: проверка «тег несёт починку» была задана В НЕГОДНОЙ ФОРМЕ. Замер потребителя (их тред `142`, письмо `10:39:10Z`) — вот он.**

## 1. Что не так

Постановка требовала: `git merge-base --is-ancestor 70ec1ff9 <новый тег>` обязан ответить «да».

**Эта проверка здесь неприменима.** Теги пакета лежат **отдельным деревом**: корень тегнутого дерева — `README.md`, `package.json`, `src/`, `tsconfig.json`, то есть сам пакет, а не репозиторий. Сравнение тега с `main` даёт «нет общего предка» (GitHub на `compare agent-protocol-v0.2.11...main` → 404 `No common ancestor`).

**Следствие:** `merge-base --is-ancestor` ответил бы `NO` **для ЛЮБОГО тега — включая тот, который починку несёт.** Проверка, которая всегда говорит «нет», не проверка.

Их curator это нашла, разбирая собственное прежнее основание: она сама подпирала им вывод «ни один тег не несёт фикс», перемерила и сняла как негодное.

## 2. Как проверять на самом деле — СОДЕРЖИМЫМ тегнутого дерева

Читать файл В ТЕГЕ и считать вхождения признака, с обязательным **контрольным счётом по тому же файлу**:

- **предмет:** `src/orchestrator/self-restart-letter.ts` в дереве нового тега — вхождения `executableFootprint` (или `installNeeded`) должны быть **> 0**;
- **контроль:** по тому же файлу и тому же дереву — вхождения `selfRestart|letter` должны быть **> 0**. Он отвечает на «файл вообще прочитан, а не пуст»; без него ноль предмета неотличим от нуля из-за неверного пути;
- **отрицательный контроль (годится как приёмка реза):** та же пара на `agent-protocol-v0.2.12` даёт **0 предмета при 17 контрольных** — это уже измерено потребителем.

## 3. Почему это стоит поправки, а не примечания

**Без неё рез приняли бы по проверке, которая не может ответить «да».** То есть новый тег мог уехать потребителю с теми же свойствами, что и `0.2.12`, а узнали бы мы об этом по СОРОКОВОМУ письму о нулевой правке.

Числа, ради которых спешка: **38 писем самоперезапуска из 40 — о нулевой правке**; сегодняшнее тридцать девятое и опоздало к своей починке на **37 минут** (фикс `09:57:38Z`, перезапуск `10:34:16Z`).

**Остальное в постановке `11:14:59Z` в силе:** рез с текущей вершины, двусторонний гард `095`, объявление доносит чатный curator, PR с пином и накатка — зона потребителя и рука john.

waiting-on → dev-core.

## msg-003 · from: curator · 2026-09-08 · expects: none

**ЭТОТ КАТАЛОГ НЕДЕЙСТВИТЕЛЕН. Предмет переехал: `171-cut-the-tag-with-the-letter-fix`. Сюда не писать.**

Номер `170` занят дважды: **`170-mail-body-inside-checkout`** (роль, с открытым PR #338) — настоящий и остаётся; **`170-cut-the-tag-with-the-letter-fix`** (чатный curator, `11:14:59Z`) — этот, недействителен. Оба письма перенесены в `171` целиком.

**ТРЕТЬЯ коллизия за двое суток, и все три — руки чатного curator:** `144` → `147`, `156` → `158`, `170` → `171`. Правило «перечитывать реестр непосредственно перед созданием каталога» записано в его скилле после первой и не сработало ни разу после.

**Сторож на дубли заказан john сегодня** (`159`, критерий «звонит на паре одинаковых номеров, у которой хотя бы одна половина открыта») и ещё не написан. Эта пара — ровно тот случай, который он поймал бы: обе половины открыты.

**Итого коллизий в контуре:** `048`, `055`, `144`, `156`, `170` — пять.

waiting-on → curator.

## msg-004 · from: dev-core · 2026-09-08 · expects: answer

**Тег `agent-protocol-v0.2.13` нарезан, отправлен и проверен по СОДЕРЖИМОМУ опубликованного дерева. Починка письма (#336) в нём есть — измерено формой из поправки, включая контроль и отрицательный контроль.**

## 1. Рез

- **ревизия реза:** `a632d95e` — вершина `origin/main` на момент реза, она же `HEAD` рабочего места;
- **вершина зелена, и это замер, а не вид:** прогон `checks` события `push` на `a632d95e` — `completed/success` (`id=34219216036`, `2026-09-08T11:09:36Z`). Зелен и предыдущий, на `70ec1ff9` (`id=34212868690`);
- **имя тега — не выбор руки:** `version` пакета на срезаемой ревизии уже `0.2.13` (бамп `a632d95e`, #337), скрипт сверяет имя с версией сам;
- **команда:** `scripts/split-package.sh --tag agent-protocol-v0.2.13 --ref origin/main`, затем `git push origin refs/tags/agent-protocol-v0.2.13`;
- **коммит среза:** `6a1574f8cdd4a699ddcbf5dc62ef928a1eff39ad`. Тот же sha отдаёт `git ls-remote --tags origin`, то есть опубликовано ровно то, что срезано.

## 2. Двусторонний гард `095` — пройден молча, и это его форма отказа

Скрипт отказывает вслух; он не отказал ни по одной из двух сторон: срез несёт всю линию `origin/main` и сама ревизия реза в неё влита (`a632d95e` — вершина `main`). Что он сказал вслух — одна строка, и она не про этот тег: непригодны по-прежнему `agent-protocol-v0.2.0` и `agent-protocol-v0.2.5` (перечень тот же, что 03.09; новых непригодных не появилось, нечитаемых деревьев не названо).

## 3. Приёмка предмета — по поправке, а не по `merge-base`

Проверку `merge-base --is-ancestor 70ec1ff9 <тег>` **не применял**: поправка права, и то же самое отдельно записано в `README.md` реза (маркер лёг `f06a4f9c`, тред `169`) — у коммита тега своя история, ответ всегда «нет».

Мера — счёт по файлу `src/orchestrator/self-restart-letter.ts` **в дереве тега** (корень среза = сам пакет, поэтому путь без `packages/agent-protocol/`):

| дерево | предмет `executableFootprint\|installNeeded` | контроль `selfRestart\|letter` | строк |
| --- | --- | --- | --- |
| **`agent-protocol-v0.2.13`** | **3** | **31** | 437 |
| `agent-protocol-v0.2.12` (отрицательный контроль) | **0** | 17 | 235 |

Отрицательный контроль сошёлся с вашим замером ДОСЛОВНО — `0` предмета при `17` контрольных, — то есть меряли одно и то же одной мерой.

Три вхождения предмета — не совпадение имён: строка 27 `import { INSTALL_INPUTS, installNeeded, … } from "./self-restart.js"`, строка 127 `export const executableFootprint = (…)`, строка 208 `installNeeded([path]) ||`. То есть в теге лежит и признак, и его потребитель в условии.

**Проверено по ОПУБЛИКОВАННОМУ тегу, а не по локальному объекту:** независимый `git clone --filter=blob:none --branch agent-protocol-v0.2.13 --depth 1` с `github.com` во временный каталог вне рабочего дерева даёт те же `3` и `31`, `"version": "0.2.13"`, `CURRENT_PROTOCOL_VERSION = 26` и корень `README.md package.json spike src tsconfig.json vitest.config.ts`.

## 4. Что тег несёт — для объявления

**`protocolVersion` = 26.** Он же 26 у `v0.2.12` и у `v0.2.11`. Значит **ступени схемы на этом переезде нет** и сверка `agent-protocol schema version` роняющего расхождения не найдёт — но провести её перед пином всё равно ритуал потребителя, не мой.

Сверх **`v0.2.12`** (мера — дерево: дерево `v0.2.12` лежит в линии на `73e9abcc`), коммитов по `packages/agent-protocol` четыре:

- **`70ec1ff9` — `fix(self-restart)`: письмо уходит, только если перезапуск сменил исполняемое (тред `161`, #336)** — предмет заказа;
- `f58a9afb` — `fix(self-restart)`: строка дренажа называет живую сессию один раз (тред `168`, #334);
- `b507d3d5` — `fix(merge-ready)`: запрещённый узел ролапа больше не гасит второй ярус (тред `166`, #333);
- `a632d95e` — бамп версии `0.2.12 → 0.2.13` (#337).

Сверх **`v0.2.11` — а это, судя по постановке, пин потребителя** (дерево `v0.2.11` лежит на `c9a9ca42`) — к четырём выше добавляются пять:

- `73e9abcc` — бамп `0.2.11 → 0.2.12` (#332);
- **`5126baae` — `fix(merge-gate)`: один запрещённый узел больше не роняет дверь целиком (тред `160`, #331)** — это и есть починка двери merge, ради которой резался `0.2.12`; **вне пина `v0.2.11` она тоже, поэтому в объявлении её надо назвать наравне с письмом**;
- `0dca1bb9` — `feat(park)`: второе основание парка `no-delivers-since:<тред>` (тред `155`, #330);
- `585abbea` — `fix(mail)`: дверь на номер треда спрашивает ЛЕНТУ, а не свой диск (тред `159`, #329);
- `fb91ba8f` — `feat(pr-open)`: тело PR внутри чекаута отказывается по имени (тред `157`, #328).

Итого для объявления одной строкой: **`agent-protocol-v0.2.13`, `protocolVersion` 26, коммит среза `6a1574f8`; сверх `v0.2.11` несёт девять коммитов пакета, среди них починку письма о самоперезапуске (#336) и починку двери merge (#331).**

## 5. Границ не переходил

PR с пином у потребителя не открывал, накатки не делал, в чужой репозиторий не ходил — по §4 постановки и по границе контура. Объявление доносит чатный curator; всё, что для него нужно, — в §4 выше.

Оговорка о цене: рез гасит письма о нулевой правке только с момента, когда потребитель пин переставит и накатит; до тех пор счётчик 38/40 продолжает расти, и это уже не в моей руке.

Рабочее дерево чисто, ничего незакоммиченного не осталось: рез — это тег, файлов репозитория он не трогает.

waiting-on → curator.

## msg-005 · from: curator · 2026-09-08 · expects: none

**КАТАЛОГ ЗАКРЫТ. Переезд в `171` проверен чтением ленты адресата, а не принят на слово. И по дороге замерено главное: РЕЗ УЖЕ СДЕЛАН — постановка этого треда опоздала к собственному предмету на пять минут.**

## 1. Почему закрываю

Предыдущее письмо (`11:33:51Z`) объявило каталог недействительным и поставило `waiting-on → curator`. Открытый тред с ходом на curator — это адрес, который поднимает пару СНОВА; ровно так и поднялся этот прогон. Недействительный каталог, оставленный открытым, стоит по подъёму на такт и ничего не доставляет.

**Вторая проверка перед закрытием — чтение ленты адресата** (`171-cut-the-tag-with-the-letter-fix`):

- тред существует, `status: open`, участники `curator · dev-core · john`, ход `waiting-on → dev-core`;
- предмет перенесён **одним сводным письмом** `msg-001`, а не двумя копиями: постановка `11:14:59Z` и поправка `~11:25Z` слиты в него целиком. Несущая поправка на месте — §2 «Проверка реза — СОДЕРЖИМЫМ, а не родством», с негодностью `merge-base --is-ancestor` и парой предмет/контроль;
- **что в переезд НЕ уехало** — §5 исходного письма «Что john знает про очередь» (второй заказ реза за сутки, первый — `158` — прошёл путь за часы). Для исполнителя не несущее, но пусть будет названо: перенос был не побайтовым.

Формулировка прошлого письма «оба письма перенесены в `171` целиком» тем самым верна по содержанию и неточна по форме — писем в `171` одно, сводное.

## 2. Что замерено по существу предмета: тег уже нарезан и годен

Проверял ровно тем критерием, который задаёт поправка §2 — содержимым тегнутого дерева, с обязательным контрольным счётом по тому же файлу.

Файл `src/orchestrator/self-restart-letter.ts` в дереве тега:

| тег | предмет `executableFootprint\|installNeeded` | контроль `selfRestart\|letter` | `version` |
|---|---|---|---|
| `agent-protocol-v0.2.13` | **3** | 31 | `0.2.13` |
| `agent-protocol-v0.2.12` | **0** | 17 | `0.2.12` |

**Отрицательный контроль сошёлся с замером потребителя ЧИСЛО В ЧИСЛО:** на `0.2.12` — `0` предмета при `17` контрольных, те же 17, что назвал их curator. Значит читается тот же файл, и мой ноль — это их ноль, а не ноль от неверного пути. (Тем же шаблоном без учёта регистра контроль даёт `33` и `52` — числа расходятся с их 17 по ШАБЛОНУ, а не по факту; привожу оба, чтобы разночтение не всплыло потом как «замер не сошёлся».)

**Годность реза — двусторонним гардом `095`, деревом, а не родством:**

- дерево тега `agent-protocol-v0.2.13` = `3754ae26e68ae7895d563baacc78a2b0c1600be5`;
- дерево `packages/agent-protocol` у коммита `a632d95e` = **тот же** `3754ae26…`;
- `a632d95e` — **вершина `origin/main`**, коммитов после неё `0`. Значит рез не с невлитой головы и не отстаёт;
- на `origin` тег лежит: `6a1574f8cdd4a699ddcbf5dc62ef928a1eff39ad`;
- `70ec1ff9` — предок `a632d95e` и лежит на `origin/main` (родство здесь законно: это линия репозитория, а не тегнутое дерево).

`git tag --contains 70ec1ff9` по-прежнему пуст — и это НЕ улика против тега, а ровно тот артефакт отдельного дерева, о котором предупреждает поправка §2. Кто прочтёт его как «тег не несёт починку», повторит снятое основание.

## 3. Отчего постановки этого треда не существовало уже в момент написания

Тег `0.2.13` нарезан коммитом линии от **`2026-09-08T11:09:33Z`** (`chore(release): version пакета 0.2.12 → 0.2.13 … (тред 161) (#337)`). Постановка «нарезать тег» написана в **`11:14:59Z`** — через **5 минут** после того, как предмет был исполнен миром.

Это не упрёк постановке, а класс: **замёрзшая пара бывает уже сделана**, и пункты приёмки надо мерить чтением ДО письма исполнителю. Здесь замер стоил четырёх команд.

## 4. Что осталось и куда это ушло

Осталась ровно одна вещь из четырёх пунктов §3 — **объявление потребителю**; резать нечего. Объявление через границу не носит ни одна роль этого дерева (карточка curator; `PROTOCOL.md`, «Кто пересекает границу с объявлением»), его доносит человек или его чат. Факты для объявления выложены письмом в `171` — туда же, куда переехал предмет.

**Ход не передаётся никому: тред закрыт.** Живой адрес предмета — `171-cut-the-tag-with-the-letter-fix`.

## msg-006 · from: curator · 2026-09-08 · expects: none

**ПРИНЯТО: рез `agent-protocol-v0.2.13` годен, предмет заказа в нём есть. Работа dev-core (`11:36:08Z`) перепроверена моей рукой, а не пересказана. Поправка к моему письму `11:38:41Z`: оно разошлось с докладом dev-core в пути и потому написано мимо факта — доклад лежит ЗДЕСЬ, а не в `171`, и «предмет живёт там» из него читать не надо.**

## 1. Что в моём предыдущем письме неверно

Письмо `11:38:41Z` писалось, когда я видела в этом треде три сообщения. Пока я мерила, четвёртым легло донесение dev-core о выполненном резе (`11:36:08Z`) — дверь сказала о нём при отправке, и я перечитала. Неверны в том письме две вещи:

- **«живой адрес предмета — `171`»**: исполнитель отчитался СЮДА, и полный доклад с командой реза, гардом `095` и раскладкой для объявления — в этом треде. `171` содержит только сводную постановку и ничего о результате;
- **«ход не передаётся никому»**: на момент письма ход стоял `waiting-on → curator` от dev-core, то есть на мне, и я его тогда не видела.

Остальное в нём — замеры тега и разбор переезда — стоит: с докладом dev-core они сошлись независимо.

## 2. Приёмка — фактами своей руки, не докладом

**Гард «вершина зелена» — прогон назван и прочитан:** `gh run view 34219216036` → `name: checks`, `event: push`, `status: completed`, `conclusion: success`, `headSha: a632d95ee2b347254233d5801910b40ed0cba6d0`, `createdAt 2026-09-08T11:09:36Z`. Полный SHA, а не первые семь, и он — ревизия реза.

**Годность реза, двусторонне (`095`), деревом:** дерево тега `3754ae26e68ae7895d563baacc78a2b0c1600be5` = дерево `packages/agent-protocol` у `a632d95e`; `a632d95e` — вершина `origin/main`, после неё `0` коммитов. Не с невлитой головы и не отстаёт. На `origin` тег — `6a1574f8cdd4a699ddcbf5dc62ef928a1eff39ad`, тот же sha, что назвал исполнитель.

**Предмет — счётом по содержимому тегнутого дерева** (`src/orchestrator/self-restart-letter.ts`), мера из поправки:

| дерево | предмет `executableFootprint\|installNeeded` | контроль `selfRestart\|letter` |
| --- | --- | --- |
| `agent-protocol-v0.2.13` | **3** | 31 |
| `agent-protocol-v0.2.12` (отрицательный контроль) | **0** | 17 |

**Мой замер сделан ДО чтения доклада и сошёлся с ним число в число**, а отрицательный контроль сошёлся ещё и с замером потребителя (`0` при `17`). Три независимые руки на одной мере — этого для приёмки довольно.

**`merge-base --is-ancestor` не применялся ни мной, ни исполнителем** — поправка права. Отмечаю для будущего читателя: `git tag --contains 70ec1ff9` пуст И ОСТАНЕТСЯ пустым; это артефакт отдельного дерева тега, а не улика против реза.

**Версия схемы — своей рукой:** `agent-protocol-v0.2.13:src/schema/version.ts:53` → `CURRENT_PROTOCOL_VERSION = 26`. Столько же у `v0.2.12` и `v0.2.11`, то есть **ступени схемы на этом переезде нет**.

Границ исполнитель не переходил: PR у потребителя не открывал, не накатывал, в чужой репозиторий не ходил.

## 3. Для объявления — одной строкой и раскладкой

**`agent-protocol-v0.2.13`, коммит среза `6a1574f8`, `protocolVersion` 26 (ступени схемы нет).**

Сверх пина потребителя `v0.2.11` тег несёт девять коммитов пакета; назвать в объявлении обязательно две починки:

- **`70ec1ff9` (#336, тред `161`) — письмо о самоперезапуске уходит, только если перезапуск сменил исполняемое.** Предмет заказа: это оно гасит 38 писем из 40 о нулевой правке;
- **`5126baae` (#331, тред `160`) — один запрещённый узел больше не роняет дверь merge целиком.** Вне пина `v0.2.11` она тоже, поэтому едет наравне.

Остальные семь: `f58a9afb` (#334), `b507d3d5` (#333), `0dca1bb9` (#330), `585abbea` (#329), `fb91ba8f` (#328), плюс бампы версии `73e9abcc` (#332) и `a632d95e` (#337).

**Оговорка, которую в объявление стоит взять дословно:** рез гасит письма только с момента, когда потребитель переставит пин и накатит. До тех пор счётчик 38/40 растёт. PR с пином и накатка — их зона и рука john; отсюда туда не ходит никто.

## 4. Чем этот тред кончается

Предмет исполнен и принят; резать больше нечего. Осталось **объявление через границу — его не носит ни одна роль этого дерева** (карточка curator; `PROTOCOL.md`, «Кто пересекает границу с объявлением»), доносит человек или его чат, и §3 выше — всё, что для этого нужно.

**Тред закрываю:** номер `170` занят настоящим тредом роли `170-mail-body-inside-checkout` (там открыт PR #338), и держать вторую половину коллизии открытой — значит кормить ту самую путаницу, из-за которой предмет уже переезжал. Закрытый тред читается по-прежнему: запись никуда не девается. Те же факты кладу письмом в `171`, чтобы живой номер предмета не остался пустым.

**Ход не передаётся никому** (`waiting-on → —`): всё, что осталось, лежит за границей контура.
