---
from: curator
worker: claude-code
session: 0f6a9c95-3cba-4ebe-ab9f-c52b69e81414
date: 2026-08-21T17:51:22Z
expects: ack
waiting-on: curator
parked-on: john
---

**Требуется действие john: #50 по-прежнему стоит перед твоей кнопкой — merge (squash), затем `schema migrate --write`, затем рестарт демона.** Ход вернулся ко мне от dev-core, а не от тебя: с моей прошлой парковки (17:41Z) на PR ничего не двигалось. Перепроверила живьём в 17:46Z — гарды 1 и 2 держатся на той же голове, единственный STOP — гард 4, и он твой по построению. Ниже — свежие факты, банк работы, которую я успела сделать до посадки, и один вопрос, который встал по ходу и НЕ срочен.

## Гарды на голове `ef9bbc5cc354ea18e2c76473126feba46a8bbe15`, перечитано 17:46Z

`gh pr view 50 --json number,state,headRefOid,mergeCommit,mergedAt,mergeable,reviewDecision` → `state=OPEN`, `mergedAt=null`, `mergeCommit=null`, `headRefOid=ef9bbc5cc354ea18e2c76473126feba46a8bbe15`, `reviewDecision=APPROVED`, `mergeable=MERGEABLE`. Голова с прошлого раза не уехала, вердикт на ней тот же.

`pnpm protocol merge-gate --ref origin/main --pr 50 --power-docs PROTOCOL.md,REVIEWER.md,.github/workflows` (17:46:40Z, **exit 1**):

- `ok   guard 1` — approved on `ef9bbc5` by github-actions;
- `ok   guard 2` — 2 check(s) green: review=SUCCESS, checks=SUCCESS;
- `you  guard 3` — тред `025-power-docs-as-data` назван в описании PR;
- **`STOP guard 4` — john merges this one — it changes `agent-protocol.json`**;
- `you  guard 5` — след merge просится;
- `ok   mergeability` — mergeable=MERGEABLE (mergeStateStatus CLEAN).

Порядок после merge не менялся и остаётся жёстким: **merge → `pnpm protocol schema migrate --write` → рестарт живого демона.** Напоминаю замер dev-core, чтобы успешный вывод не прочитался как поломка: конфиг головы уже на 18, поэтому миграция после merge напечатает `protocol version 18 — nothing to migrate`, exit 0 — это её УСПЕХ. Реально меняет состояние контура только рестарт: поле `powerDocuments` заработает у двери с первого вызова после merge, а у планировщика — лишь после рестарта.

## Хвост merge

`gh pr list --state open` (17:45Z) — один #50, и он не моей кнопки: `merge-gate` REFUSED гардом 4. Домерживать нечего.

## Патч канона: написан и лежит веткой БЕЗ PR — намеренно

Я обещала в прошлом сообщении писать его после посадки #50, «потому что канон описывает то, что уже в коде». Обещание держу по существу и объясняю, что изменила: **текст написан и закоммичен, PR не открыт.**

- ветка `docs/canon-power-documents`, коммит `807dbd05a4976630d0b906812ea8652ec86e83d1`, запушена; сообщение коммита несёт строку «НЕ МЁРЖИТЬ ДО #50»;
- `git diff --stat` — `PROTOCOL.md` +46/−22, `REVIEWER.md` +9/−6, `docs/roles/curator.md` +12/−6 — три файла, кода и пакета не трогает вовсе;
- **PR не открыт по двум причинам, обе проверяемые.** Первая: текст описывает поле `powerDocuments`, которого на `main` ещё нет, — ревьюер судит PR против базы и был бы прав, назвав это расхождением с фактом; круг ревью сгорел бы впустую. Вторая, тяжелее: все три файла — доки власти, то есть та же твоя кнопка, что и #50, и открытый PR положил бы тебе рядом две кнопки, у которых **порядок обязателен**. Ветка без PR не мёржится ничем и не путает очередь.

Открыть PR — один ход curator после того, как #50 сядет. Ветка уже на удалёнке, так что это переживёт мою сессию.

## Что именно в патче — чтобы ты читал не диффом, а по смыслу

- **`PROTOCOL.md`, абзац про `--power-docs` (было 186–197).** Убрано «флаг стоит здесь вынужденно, потому что бамп версии не проходит собственную дверь зон»: неправда дважды — дверь зон стала полисной с треда `037` и версий не роняет, а шага `zones check` в конвейере нет вовсе (проверено `grep`, подтверждено dev-core; pre-commit-хука в рабочем дереве роли тоже нет). Стало: список доков власти из ДВУХ половин, и обе — данные (выводимая + поле `powerDocuments`), флаг остался и продолжает ДОБАВЛЯТЬ. Добавлен абзац про то, что дверь печатает ИСТОЧНИК каждого пути и отдельно говорит, когда конфиг не объявляет ничего, и что PR судится списком БАЗЫ — то есть PR, добавляющий путь, судится списком без этого пути;
- **`PROTOCOL.md`, гард 4 в списке гардов (строка 179).** «объявляется двери флагом `--power-docs`» → «полем `powerDocuments` конфига, а разово — флагом»;
- **`PROTOCOL.md`, абзац про то, где едет номер версии (было 60–70).** Норма «САМ номер в PR не едет» заменена измеренным фактом — см. следующий раздел, там же вопрос;
- **`REVIEWER.md` (68–73).** «Голая форма вызова отвечает про ВЫВОДИМЫЙ список, а не про список этого репозитория» становится неправдой после посадки: голая форма читает `powerDocuments` базы и отвечает и про этот репозиторий тоже. Переписано вместе с двумя оговорками, которые ревьюер обязан читать в выводе, а не додумывать (источник пути; список базы);
- **`docs/roles/curator.md` (116–123).** Из строки вызова убран флаг — после посадки процедура merge не должна держаться на том, что исполнитель помнит дописать `--power-docs`. Это ровно та дыра, ради которой заведён тред.

## Вопрос, который встал по ходу патча. НЕ срочный, кнопки не держит

Правя абзац про номер версии, я упёрлась в половину, которую замер dev-core НЕ закрывает, и не стала решать её своей рукой.

Замер закрыл случай шага, данных **не двигающего** (v18: `files: 0`): конвейер второго маршрута не оставляет — голова с отставшим конфигом даёт `config check --ref HEAD` exit 2, красный `checks` и недостижимый гард 2, так что номер обязан ехать тем же PR.

Для шага, который данные **двигает**, возражение v13 стоит в полный рост и никуда не делось: номер, приехавший вместе с кодом, лишает миграцию работы ровно тогда, когда работа есть (после merge конфиг уже на новой версии → `nothing to migrate` → данные остались в старой форме). Два требования сталкиваются: конвейер запрещает ОТСТАВШИЙ конфиг в голове, семантика миграции запрещает ОПЕРЕДИВШИЙ на момент merge. Развязка тут не сентенция канона, а код — чем шаг миграции планирует: рабочим деревом или ref предмёржевой базы.

**Что я сделала в патче:** записала открытое открытым. Норма нового абзаца распространяется только на шаг, данных не двигающий; первый же PR, чей шаг двигает данные, идёт вопросом к тебе, а не по этому абзацу. Это консервативный выход, а не решение развилки, и я называю его так.

**Чего я НЕ сделала и предлагаю:** отдельный тред на эту развилку — тема своя (порядок посадки версии + чем планирует миграция), и в 025 ей не место. Заводить его или нет — твоё; кнопку #50 это не держит и в патче канона дыры не оставляет.

## Состояние моего рабочего дерева

Чисто, HEAD `492af8f5f1f6f029766c7e3c26715436af685b85` (= `origin/main`), незакоммиченного за мной нет. Работа этой сессии целиком лежит в `origin/docs/canon-power-documents`, коммит `807dbd05a4976630d0b906812ea8652ec86e83d1`.

## Что подхватывает следующая сессия curator

1. **если #50 сел** — открыть PR из ветки `docs/canon-power-documents` (`thread: 025-power-docs-as-data`, `role: curator`), перед этим ПЕРЕЧИТАВ текст против уже посаженного кода: патч писался до посадки, и сверка с фактом на нём не выполнена; дальше зелёный `checks` → метка `review` на той же голове → вердикт → **merge кнопкой john** (три файла из трёх — доки власти);
2. **если #50 не сел** — ветку не трогать, повторить парковку;
3. развилка про шаг миграции, двигающий данные, — по ответу john.

waiting-on → curator (ход остаётся у меня, потому что вопрос к john несу я; парковка на john).

