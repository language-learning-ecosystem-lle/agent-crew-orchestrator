---
from: dev-core
worker: claude-code
session: 4426bb59-39fe-4088-b219-8d0d7adb807c
date: 2026-08-21T17:33:39Z
expects: answer
waiting-on: curator
---

Находка ревьюера по #50 закрыта. Новая голова `ef9bbc5cc354ea18e2c76473126feba46a8bbe15`, `checks` на ней идёт (прогон `32508672498`).

## Что сделано

Ровно то действие, которое назвал ревьюер: кейс в `packages/agent-protocol/src/merge/gate.process.test.ts` — дверь команды `merge-gate` как реальный процесс, с конфигом, где `powerDocuments` заполнено.

- `repoWithConfig` теперь принимает накладку на конфиг, поэтому поле едет в кейс тем же путём, каким приезжает в жизни: **закоммичено в `agent-protocol.json`, в строке вызова флага нет**;
- `PROTOCOL.md` в диффе PR → **exit 1, `STOP guard 4`**, и в `result.out` есть строка следа `PROTOCOL.md — declared by 'powerDocuments' of the config` — то есть до stdout процесса доезжает и вердикт, и названный источник пути;
- **тот же дифф против конфига БЕЗ поля** → exit 0 и вслух сказанное `the config declares no 'powerDocuments'`. Это половина «бит-в-бит сегодняшнее» из проверяемости, проверенная теперь на уровне двери, а не только юнитом.

Кода и поведения PR правка не трогает: дифф — один тестовый файл.

## Мутационная проверка кейса, и один факт, который я обязан назвать

Кейс не должен зеленеть сам по себе, поэтому я его убил дважды и докладываю оба исхода:

- **перекрыл `configured: loaded.config.powerDocuments` значением `undefined` в развязке CLI** (`cli.ts:10477`) → кейс падает (`expected 0 to be 1`). Слой разбора поля между схемой и печатью — ровно то, что ревьюер назвал недоказанным, — этим кейсом закрыт;
- **снял объявление `powerDocuments` в полисной схеме** (`config/policy.ts:81`) → **кейс остался зелёным.** Причина измерена, а не выведена: `policyConfigSchema` — это `z.looseObject`, неизвестные ключи проходят насквозь, поэтому в РАНТАЙМЕ поле доезжает до двери и без своей строки в схеме. Строку в полисной схеме принуждает `tsc` (без неё тип поля — `unknown`, и вызов `powerDocumentList` не собирается), а не процесс.

Практический вывод для чтения этого теста: процессный кейс доказывает ПРОВОДКУ (конфиг → список → вердикт → след), но не доказывает строку в полисной схеме — её держит typecheck. Свойство `looseObject` не мой этого PR, я его только назвал.

## Числа

`pnpm test` локально зелёный: **2262 + 7**, 0 упавших (было 2261 + 7, +1 — новый кейс). `pnpm typecheck` зелёный, `pnpm exec biome check` по изменённому файлу зелёный.

## Метка `review` снята мной — намеренно

Метка стояла на голове `314c836` (поставил john 17:20Z), а голова теперь другая. Триггер `claude-review.yml` — `pull_request: types: [labeled]`, то есть повторную приёмку заводит именно ПОВЕШЕННАЯ метка; оставленная стоять она нового прогона не запускает и при этом читается как «зелёный свет на этой голове». Сняв её, я вернул PR в положение маршрута: зелёный `checks` на `ef9bbc5` → метка `review` на той же голове → вердикт.

## Что дальше и чей ход

Мой ход по PR кончен: **жду `checks` по #50, голова `ef9bbc5`.** У конвейера не стою, поллинга нет.

Кнопка не изменилась: PR трогает `agent-protocol.json`, гард 4 останавливает его у тебя и у меня — **merge john'а**, дальше его же `schema migrate --write` и рестарт демона, порядок merge → миграция → рестарт.

waiting-on → curator.

