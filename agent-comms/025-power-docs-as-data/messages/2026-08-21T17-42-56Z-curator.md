---
from: curator
worker: claude-code
session: 5947ac23-0b60-4cff-8aca-54e7e87a98d8
date: 2026-08-21T17:42:56Z
expects: ack
waiting-on: curator
parked-on: john
---

**Требуется действие john: #50 готов к твоей кнопке — merge (squash), затем `schema migrate --write`, затем рестарт демона.** Четыре гарда сняты фактами (ниже, каждый прочитан моей рукой), пятый — этот след; гард 4 стоит и стоять должен: PR меняет `agent-protocol.json`, и это ровно тот случай, ради которого он есть. Другой руки у merge здесь нет — ни моей, ни dev-core.

## Гарды на голове `ef9bbc5cc354ea18e2c76473126feba46a8bbe15`

- **гард 1 — approve на ТЕКУЩЕЙ голове:** review `4995908274`, состояние `APPROVED`, `commit_id` = `ef9bbc5cc354ea18e2c76473126feba46a8bbe15`, 17:41:22Z (`gh api .../pulls/50/reviews`). Прогон `Claude PR Review` `32508991341`, завершился success 17:41:26Z. Вердикт приехал и сообщением треда — `2026-08-21T17-41-02Z-reviewer-pr.md`, `verdict: approve`, находка первого круга закрыта;
- **гард 2 — зелёный `checks` на ТОЙ ЖЕ голове:** прогон `32508672498`, `headSha` = `ef9bbc5cc354ea18e2c76473126feba46a8bbe15`, conclusion success 17:35:20Z. Все шаги зелёные, включая `agent-protocol.json держится` — голова с конфигом 18 и кодом 18 свой же гейт версии проходит (это и был кандидат в блокеры шага 1);
- **гард 3 — восхождение:** решение john «ЗАВОДИМ, вариант (1)» доставлено сообщением `2026-08-21T16-57-54Z-curator.md` этого треда (чат ~16:57Z), вопрос стоял в `2026-08-21T16-14-31Z-curator.md`. В описании PR стоит `thread: 025-power-docs-as-data`;
- **гард 4 — STOP, и это правильный STOP:** `merge-gate` (17:41:56Z, exit 1) — `ok guard 1`, `ok guard 2`, `you guard 3`, **`STOP guard 4 · john merges this one — it changes agent-protocol.json`**, `you guard 5`, `mergeable=MERGEABLE (mergeStateStatus CLEAN)`. Строка вызова: `pnpm protocol merge-gate --ref origin/main --pr 50 --power-docs PROTOCOL.md,REVIEWER.md,.github/workflows`;
- **гард 5** — это сообщение.

## Что я проверила сама, а не приняла из доклада

- **полный SHA новой головы существует:** `git cat-file -t ef9bbc5cc354ea18e2c76473126feba46a8bbe15` → `commit` (в прошлом круге хвост SHA в докладе был дописан — здесь он верен);
- **форс-пуша не было:** у PR ровно два коммита — `314c8362184f246b659308149b69e535a9e6dc3b` и `ef9bbc5cc354ea18e2c76473126feba46a8bbe15` (`gh api .../pulls/50/commits`), второй лёг поверх первого;
- **дифф второго круга — один тестовый файл:** `git show --stat ef9bbc5` → `packages/agent-protocol/src/merge/gate.process.test.ts`, +35/−2. Кода и поведения правка не трогает, как и сказал dev-core;
- **кейс бьёт в то, что было недоказано** (прочла дифф): конфиг с закоммиченным `powerDocuments: ["PROTOCOL.md"]`, вызов без флага → `exit 1`, `STOP guard 4`, в `result.out` строка `PROTOCOL.md — declared by 'powerDocuments' of the config`; тем же кейсом обратная половина — конфиг без поля → `exit 0` и `the config declares no 'powerDocuments'`;
- **числа `2262 + 7` — из лога прогона, а не из доклада:** `gh run view 32508672498 --log` → `packages/agent-protocol test: Tests 2262 passed`, `packages/transport-telegram test: Tests 7 passed`.

## Метку `review` на новую голову повесила я — называю явно

dev-core снял метку намеренно (его сообщение 17:33:39Z: повторный круг заводит именно ПОВЕШЕННАЯ метка) и его ход на этом кончился — то есть у шага маршрута «метка на новой голове» не осталось исполнителя, а вешать её раньше зелёного `checks` нельзя. Порядок соблюдён и проверяем: `checks` success 17:35:20Z на `ef9bbc5` → метка повешена мной 17:35:54Z (голова перед этим перечитана — та же) → круг ревью стартовал 17:35:56Z прогоном `32508991341` на той же голове. Дифф `.github/workflows` не трогает, поэтому исключение про `claude-review.yml` здесь не применяется и круг законен.

## Твой порядок после merge — он жёсткий

1. **merge #50 squash**, заголовок Conventional Commits (в PR он уже такой: `feat(merge-gate): объявленный список доков власти — данными конфига (тред 025)`);
2. **`pnpm protocol schema migrate --write`** — после merge репозиторий на версию младше своего кода; шаг v17→v18 данных не двигает, dev-core замерил: план `protocol version 17 → 18, steps: 1`, `files: 0`, единственная запись — сам конфиг, повторный прогон печатает `nothing to migrate`;
3. **рестарт живого демона** — он исполняет код и конфиг своего старта; ты совмещаешь это с уже висящим ноутным рестартом (демон отстаёт от main, факт (б) из 022/msg-012).

Порядок merge → миграция → рестарт менять нельзя: миграции негде отработать до merge, а демон до рестарта живёт старой сборкой.

## Хвост merge

`gh pr list --state open` (17:41:56Z) — один #50, и он не мой: `merge-gate` REFUSED по гарду 4. Домерживать нечего.

## Что остаётся за мной после посадки

Патч канона `PROTOCOL.md` отдельным PR (док власти → снова твоя кнопка), два абзаца:

- **строки 192–197** — объявляют флаг `--power-docs` вынужденным, потому что «бамп версии не проходит собственную дверь зон»: после посадки поля это неправда дважды — дверь стала полисной с треда 037, а шага `zones check` в конвейере нет вовсе;
- **строки 60–70** — «сам номер в PR не едет, его пишет шаг миграции»: живой замер dev-core (шаг 1) показал обратное — при отставшем конфиге `checks` красный, гард 2 недостижим, и шагу миграции просто негде отработать до merge. Номер едет тем же PR, как и говорит шапка `schema/version.ts`.

Пишу его после того, как #50 сядет: канон описывает то, что уже в коде, а не то, что собирается там быть.

waiting-on → curator (ход остаётся у меня, потому что вопрос к тебе несу я; парковка на john).

