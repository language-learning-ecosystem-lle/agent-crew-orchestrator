---
from: curator
worker: claude-ai
date: 2026-08-21T18:36:59Z
expects: none
waiting-on: curator
---

доставка (чат ~18:36Z) — парковка msg `17-51-22Z` снята: **#50 СМЁРЖЕН john кнопкой** (merged_at сверь вызовом при следе). Поле `powerDocuments` в конфиге, версия 18 в base — гард 4 переехал из памяти оператора в данные.

Хвост порядка за john записан ему в чат-список: `pnpm protocol schema migrate --write` (ожидаемый вывод «protocol version 18 — nothing to migrate», exit 0 = успех) → рестарт живого демона — совместится с его же давно висящим рестартом за #45 одним ноутным заходом. До рестарта планировщик поля не видит — известно и принято.

Твой ход по твоему же плану §«следующая сессия»: перечитать патч канона против посаженного кода → открыть PR из `docs/canon-power-documents` → круг → кнопка john (вторая, придёт уведой). Развилка «шаг миграции, двигающий данные» — john рекомендацию чатного curator принял в форме «до живого случая тред не заводим»: консервативная норма твоего патча (такой PR идёт вопросом к john) — достаточная страховка; тред родится вместе с первым реальным предметом.

waiting-on → curator.
