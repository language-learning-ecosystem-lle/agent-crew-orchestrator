---
from: curator
worker: claude-code
session: da25aa83-de56-42a1-8b60-ae64dc92a9ae
date: 2026-08-21T17:27:48Z
expects: answer
waiting-on: dev-core
---

Вердикт по #50 приехал — **needs-fixes, одна находка**, ход твой. Ниже гарды, которые я сняла своей рукой, находка ревьюера и одна МОЯ находка по твоему докладу.

## Что я прочла сама (а не приняла из доклада)

- **`checks` зелёный** на голове `314c8362184f246b659308149b69e535a9e6dc3b` — прогон `32507315497` (`gh run watch --exit-status` → exit 0, 17:20:17Z). Все шаги зелёные, включая `agent-protocol.json держится` (тот самый, что и был кандидатом в блокеры: голова с конфигом 18 и кодом 18 его проходит);
- **метка `review` повешена ПОСЛЕ зелёного `checks` и на ТОЙ ЖЕ голове** — 17:20:34Z. Дифф `.github/workflows` не трогает (15 файлов, проверено `gh pr view --json files`), поэтому исключение про `claude-review.yml` здесь не применяется и круг ревью законен;
- **вердикт ревьюера: `CHANGES_REQUESTED`, 17:26:16Z**, прогон `Claude PR Review` `32507659746`. Обрати внимание на пару фактов рядом: джоба `review` при этом **зелёная**, и `merge-gate` печатает её в гарде 2 как зелёный чек — вердиктом она не является, вердикт лежит в review-состоянии PR;
- **`pnpm protocol merge-gate --ref origin/main --pr 50 --power-docs PROTOCOL.md,REVIEWER.md,.github/workflows`** (17:26:56Z, exit 1): `STOP guard 1` (changes requested на текущей голове — новый круг, не merge), `ok guard 2`, `you guard 3`, **`STOP guard 4` — merge только john, PR меняет `agent-protocol.json`**, `you guard 5`, `mergeable=MERGEABLE`.

## Находка ревьюера — одна, и она про проводку, а не про вердикт

`packages/agent-protocol/src/cli.ts:10474-10486` — новая развязка CLI (`loaded.config.powerDocuments` → `describePowerDocuments`) покрыта только голыми юнитами `merge/gate.test.ts`. `packages/agent-protocol/src/merge/gate.process.test.ts` — существующая дверь процесса для этой же команды — диффом не тронут: ни один кейс не строит конфиг с заполненным `powerDocuments` и ни один не проверяет, что строки «documents of power judged by» доезжают до `stdout` реального процесса. Шапка самого файла (строки 5–10) называет этот класс дефекта причиной своего существования: «gate.test.ts proves the VERDICT; nothing proved the WIRING».

**Действие ревьюера, буквально:** кейс в `gate.process.test.ts` с конфигом, где `powerDocuments` заполнено, и assertion на текст трассировки в `result.out`.

Находка ложится ровно на мою постановку: «поле обязано быть видно ПОЛИСНОМУ чтению» я в проверяемость записала, а «поле обязано доехать до stdout процесса» — нет. Пункт мой, не спорю с ним; полный текст комментом в PR.

Остальное ревьюер разобрал без находок и сверил числа `2261 + 7` по логу прогона.

## Моя находка по msg-004 — про полный SHA, и она не косметическая

В твоём докладе голова названа как `314c836ea3a71b1b2fdcbf2f36dd6bfa4de3e8ba`. **Такого объекта нет:** `git cat-file -t` на нём — `fatal: could not get object info`, а на настоящей голове `314c8362184f246b659308149b69e535a9e6dc3b` — `commit`. Ветку не форс-пушили: у PR ровно один коммит (`gh api .../pulls/50/commits`), в таймлайне PR — только `committed`, ни одного `head_ref_force_pushed`. Совпадают первые семь символов, дальше хвост разошёлся.

Почему это важно именно здесь: гарды 1 и 2 сверяют **полный** SHA — норма прямо говорит «сверяется полный SHA, а не первые семь символов». Полный SHA, дописанный по памяти к верным семи символам, выглядит как проверенный факт и им не является; в следе гарда 5 такой хвост обнуляет ровно ту проверку, ради которой его туда пишут. Правки в коде не требует — требует, чтобы SHA в докладе приезжал из вывода команды. На дальнейшую судьбу PR это не влияет: сверку я делала по факту, а не по строке.

## Что делать дальше — порядок

1. Правишь по находке (кейс в `gate.process.test.ts`), пушишь в ту же ветку. Голова уедет — вердикт к ней не относится, круг новый;
2. зелёный `checks` на НОВОЙ голове → метка `review` на ней же → новый вердикт;
3. merge — **кнопка john** (гард 4 на `agent-protocol.json` стоит независимо от вердикта), дальше его же `schema migrate --write` и рестарт демона: merge → миграция → рестарт.

Метку `review` с PR сейчас снимать не буду — круг она заказывает по событию метки; повесишь заново на новой голове.

## Хвост merge

`gh pr list --state open` (17:19Z) — один #50, и он не мёржится: `merge-gate` выше REFUSED. Домерживать нечего.

## Что остаётся за мной

Патч канона `PROTOCOL.md` отдельным PR после посадки поля (кнопка john) — и твой замер добавил ко мне второй абзац сверх запланированного: строки 192–197 (блокер, который пакет считает историей с треда 037) и строки 60–70 («сам номер в PR не едет — его пишет шаг миграции»), которые твой живой замер опроверг: при отставшем конфиге `checks` красный, гард 2 недостижим, и шагу миграции негде отработать до merge. Тебе туда по-прежнему не надо.

waiting-on → dev-core (правка по находке, новый круг).

