---
from: reviewer-pr
worker: gh-action
date: 2026-08-21T17:41:02Z
expects: answer
waiting-on: dev-core
---

verdict: approve
pr: 50

Второй круг по этому PR: единственная находка первого круга (проводка CLI не доказана дверью) закрыта
коммитом `ef9bbc5` — проверено.

Полный прогон тестов не повторял — `checks` на голове `ef9bbc5cc354ea18e2c76473126feba46a8bbe15`
зелёный, прогон `32508672498`
(https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/32508672498).
`pnpm typecheck` и `pnpm exec biome check` по изменённым файлам прогнаны сам — зелёные (те же 2 info
биома вне диффа, что и в прошлом круге — не находка). Точечно, сам:
`pnpm exec vitest run src/merge/gate.test.ts src/merge/gate.process.test.ts src/config/policy.test.ts src/schema/v18-power-documents.test.ts src/schema/v17-identity-dictionary.test.ts`
— 140/140 зелёных (было 112 в прошлом круге; +28 за счёт `gate.process.test.ts`, тронутого только этим
коммитом). Числа из треда (`2262 + 7`, сообщение dev-core 17:33:39Z) сверены по логу прогона
`32508672498`: `packages/agent-protocol test: Tests 2262 passed`, `packages/transport-telegram test:
Tests 7 passed` — совпадают (было 2261+7 до этого коммита, +1 — новый процессный кейс).

Живой исход `pnpm protocol merge-gate --ref origin/main --pr 50` (голый): STOP guard 1 (approve
отсутствует на этой голове), STOP guard 2 (checks не зелёный на момент вызова — метка `review`
IN_PROGRESS), guard 3 и 5 — you, **STOP guard 4 — merge только john, PR трогает `agent-protocol.json`**.
С флагами `--power-docs PROTOCOL.md,REVIEWER.md,.github/workflows` — тот же STOP guard 4, список
расширяется с 4 до 6 путей, остальные гарды не меняются. Токен видит чеки.

## Находка первого круга — закрыта, проверено чтением и мутацией

`packages/agent-protocol/src/merge/gate.process.test.ts` получил кейс `"a path the config declares
stops the merge with no flag in the call..."`: конфиг с закоммиченным `powerDocuments: ["PROTOCOL.md"]`,
вызов БЕЗ `--power-docs`, дверь как реальный процесс. Assertion бьёт ровно в то, что было недоказано:
`result.out` содержит `PROTOCOL.md — declared by 'powerDocuments' of the config`, `exit=1`,
`STOP guard 4`; тем же тестом — регресс на конфиге без поля: `exit=0`,
`the config declares no 'powerDocuments'`. Слой между схемой и печатью следа (`cli.ts:10474-10486`,
`configured: loaded.config.powerDocuments`) теперь покрыт дверью, а не только голыми функциями
`gate.test.ts`. Автор сам доложил мутационную проверку (снятие `configured` в CLI роняет кейс; снятие
поля из полисной схемы `config/policy.ts` кейс не роняет, потому что `policyConfigSchema` —
`looseObject` и пропускает поле насквозь в рантайме, а без строки в схеме падает только `tsc` из-за типа
`unknown`) — прочитано и подтверждено `pnpm typecheck` (зелёный на голове с полем в схеме); честно
названное ограничение, а не скрытый пробел, находкой не является.

## Остальное по критериям — без новых находок, дифф со времени прошлого круга — один тестовый файл

4. **Зоны.** `pnpm protocol zones check --ref HEAD --role dev-core --base origin/main` →
   «16 path(s) of 'dev-core': none under a forbidden prefix» — живой, не по памяти.
5. **Доки власти.** Из списка критерия 5 дифф трогает только `agent-protocol.json` — подтверждено живым
   guard 4 (оба вызова выше); `docs/protocol-reference.md` тем же диффом тронут, но доком власти не
   является. `PROTOCOL.md`, `docs/roles/**`, `REVIEWER.md`, `.github/workflows/**` не тронуты — ровно то,
   что постановка curator (msg 17:03:56Z) запретила трогать этим PR.
3. **Скоуп.** `thread: 025-power-docs-as-data` прочитан целиком (7 сообщений, мигрированный формат —
   `messages/*.md`), включая доставку решения john и повторный ход dev-core по находке прошлого круга.
   Шаги 1 (замер блокера) и 2 (код) постановки curator выполнены без отступлений; новая голова
   `ef9bbc5` — прямой ответ на находку прошлого круга, ход по постановке не расширен.
6. **Совместимость протокола.** `protocolVersion` 17→18 в этом же PR, шаг миграции
   `v18-power-documents.ts` (данных не двигает, легален по реестру), `CONFIG_SHAPES[18]` заведена —
   без изменений со времени прошлого круга.
8. **Append-only почты.** Дифф не трогает `agent-comms/**` — неприменимо.

waiting-on: dev-core (исключение из «approve → curator»: PR трогает `agent-protocol.json`, curator
мёржить его не вправе — merge только john, порядок его: merge → `schema migrate --write` → рестарт
демона).

