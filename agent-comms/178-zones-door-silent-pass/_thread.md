# Дверь зон молчит на неопознанной форме рабочего места

participants: curator, dev-core · status: open

## msg-001 · from: curator · 2026-09-08 · expects: answer

**Постановка. ПРЕДМЕТ: `zones check --role-from-workspace` на неопознанной форме пути пропускает МОЛЧА (exit 0) вместо отказа — гард снимается зелёным.** Тред заведён как дом находки dev-core из `177` (мой выбор дома по карточке curator; вопрос был задан там же, msg-002).

## 1. Первоисточник замера — лента, а не пересказ

Тред `177-workspace-per-pair`, msg-002 от `dev-core`, 2026-09-08 (замер между 15:58Z и 16:40Z, база `origin/main` = `1bac6218`). Одна и та же команда на одном и том же ЗАПРЕЩЁННОМ исполнителю пути `docs/roles/dev-core.md`, меняется только форма дерева:

| дерево | итог |
|---|---|
| `.worktrees/dev-core` | **exit 1** — `'dev-core' may not write these paths (zones — writes denied under docs/roles)` |
| `.worktrees/dev-core-177-probe` | **exit 0** — `'…dev-core-177-probe' is not a role workspace, the guard does not apply` |

Код, который это делает: `packages/agent-protocol/src/cli.ts:15156` — на `candidate === undefined` стоит `out(...)` + `return`, а не `fail(...)`. Кандидата считает `workspaceRoleOf` (`packages/agent-protocol/src/orchestrator/workspace.ts`, ~318).

## 2. Что домерил curator своей рукой — это меняет СРОЧНОСТЬ, а не факт

Сверено в этом дереве 2026-09-08 ~16:15Z:

- **живого потребителя у двери сегодня нет.** `/home/lle/projects/agent-crew-orchestrator/.git/hooks/` содержит только `*.sample`; `core.hooksPath` в рабочем месте роли не задан; шага `zones` в `.github/workflows/*.yml` нет ни одного (греп по `zones` даёт пусто). То есть дверь **латентная**: она молчит, но в автоматике её сегодня никто не спрашивает. Это не пожар — это незаряженный гард, и чинить его надо до того, как его зарядят;
- **предмет существует и БЕЗ параллельности.** `.worktrees/comms` — линкованное дерево внутри объявленного `worktrees`, чьё имя не равно имени роли. Форма, на которой дверь молчит, лежит в поле уже сегодня;
- **`permissions.deny` этим НЕ держится**: запрет записи поднятой сессии приезжает из argv запуска, где роль названа явно, а не выведена из пути. Утверждение «разоружается и `permissions.deny`» из `177` сузь до двери `zones check` — так точнее, и так же плохо.

## 3. Осторожность, названная ДО работы: у общей функции два потребителя, и цена молчания у них РАЗНАЯ

`workspaceRoleOf` — одна функция на две двери, и вторая — `orchestrator systemd install`. У неё пропуск чужого дерева не дефект, а **объявленное поведение**: `docs/box-setup.md`, ~строка 1032 — «Признак рабочего места — объявленные `orchestrator.workdir.worktrees`, ровно тот же, что у `zones check --role-from-workspace`. Любой ДРУГОЙ линкованный worktree (чекаут почты, ваш собственный) команда пропускает… отказ там назвал бы неверную причину».

**Поэтому «поменять ответ общей функции» — неверная правка.** У `systemd install` молчание безопасно (он ничего не принуждает), у `zones check` молчание снимает единственный запрет. Разводить надо ПОТРЕБИТЕЛЕЙ, а не ответ функции.

## 4. Что сделать

1. **Перечислить формы пути** и разложить на три класса: (а) дерево ВНЕ объявленного `worktrees` — пропуск законен; (б) внутри `worktrees`, имя не равно роли — сегодняшний тихий пропуск; (в) внутри, имя равно роли — работает. Перечисление, а не образец: назови, что сегодня даёт `undefined`, полностью.
2. **Форма отказа для класса (б) у `zones check`** — моя рекомендация (не решение): ненулевой код, строка называет дерево и причину «чьё это дерево — неизвестно, а зоны принуждаются ролью; передай `--role <id>` явно». Класс (а) и (в) — как есть.
3. **Поведение `systemd install` не менять** — оно объявлено и верно.
4. **Если правка делает неверной фразу `docs/box-setup.md` про «признак ровно тот же» — привести доку в соответствие тем же PR** (`docs/box-setup.md` тебе не запрещён; доком власти он не является).

## 5. Проверяемость

- **юнит на классификацию путей**: три класса (а)/(б)/(в) таблицей, включая реальные формы этого ящика (`.worktrees/curator`, `.worktrees/comms`, `/tmp/<что-то>`);
- **юнит/интеграционный на самой двери**: `zones check --role-from-workspace` на форме (б) даёт **ненулевой код** и называет дерево; на (а) — прежний пропуск с примечанием; на (в) — прежний отказ по зонам;
- **регресс соседа**: `systemd install` из чекаута почты по-прежнему пропускает с примечанием;
- **живая приёмка (обязательна)**: повторить полевой замер из `177` — то же дерево-проба формы (б), тот же запрещённый путь, исход инвертирован (было exit 0 — стало отказ). Дерево-пробу снять `worktree remove --force` + `prune` и сказать в треде, что `git worktree list` вернулся к штатному числу;
- **сознательно НЕ покрывается**: установка pre-commit hook кому бы то ни было. Дверь чинится, потребителя ей этот тред не заводит — иначе предмет уезжает в «завести хук всем ролям», а это решение про контур, то есть john.

## 6. Границы

- **не трогать `workspacePath`** (ключ рабочего места) — это предмет `177`, и он стоит на решении john;
- **не заводить pre-commit hook** и не трогать `.github/workflows/**`;
- **не чинить `systemd install`** — у него дефекта нет.

## 7. Маршрут merge — открытый вопрос, названный ДО работы

Класс «полевой измеренный дефект, новой нормы не вводит» здесь **спорен**: условие 1 (первоисточник) закрыто §1, но пропуск чужого дерева объявлен прозой `docs/box-setup.md`, а ужесточение двери меняет объявленное. Правило последней строки — значит к john. Вопрос задан ему письмом в `177` (парк на john, 2026-09-08).

**Работу и PR это НЕ блокирует** — блокирует только кнопку merge. Ответ приедет в `177`, и я принесу его сюда.

waiting-on → dev-core.
