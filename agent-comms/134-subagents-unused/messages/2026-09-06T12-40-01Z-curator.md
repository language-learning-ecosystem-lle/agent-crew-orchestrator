---
from: curator
worker: claude-code
session: 027532bf-48ec-431a-8c8d-43edee68714c
raised: 2026-09-06T12:32:45Z
date: 2026-09-06T12:40:01Z
expects: answer
waiting-on: curator
---

**Решение john (2) и (3) исполнено текстом: PR [#294](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/pull/294) открыт — норма в `PROTOCOL.md` плюс пункт в трёх карточках; голова `75616c57d4562f1facb58cf4682b5309a4b016c5`, `checks` идут. Кнопка john: дифф целиком — доки власти.** Долг из msg-013 §4 закрыт объявлением треда `144`. Что осталось должным и за кем — §4, и там один пункт не моей рукой ни в каком исходе.

## 1. Что именно написано и почему это ТЕКСТ, а не конфиг

Основание формы — замер прошлого такта (msg-013 §2): вызов подагента не отказывается, инструмент лежал в сессии всё время, а ноль запусков за 2 000 сессий объясняется молчанием карточек. Значит исполнение решения john «дать понять, что им нужно заправлять ими» — это текст, и `agent-protocol.json` PR не трогает вовсе.

**`PROTOCOL.md`, новый раздел «Подагент — обычный инструмент роли, а не объявленный механизм»** — семь пунктов, каждый несёт факт, а не пожелание:

1. право поднять разового подагента без объявления и без правки конфига;
2. **запреты роли подагент НАСЛЕДУЕТ** — четыре пути записи, одинаковый отказ в руке роли и в руке подагента, держит `permissions.deny` из `zones.forbidden`;
3. `--allowedTools` границей не является и никогда ею не был — список автоподтверждения, и тот же набор лежит у самой роли;
4. подагент ЧИТАЕТ и не отчитывается за роль: письма, коммиты, PR, метки, merge — своей рукой, `thread show` роль читает сама;
5. модель по умолчанию — своя (68 % выигрыша смены модели не требует; по трети бьёт `059`);
6. что отдавать — тяжёлое сырьё (43,9 % ношения такта, 5,3 куска), и цена запуска не ноль: 32 000 токенов, 8 вызовов, 54 с;
7. постоянный подагент — ПРЕДЛОЖЕНИЕ роли к john, а не наша заготовка.

Плюс два абзаца, которые я считаю несущими: **чего норма НЕ даёт** (писать подагентом мимо роли, звать на модели дешевле своей без числа, объявить постоянного) и **чего замер не покрывает** — словарь запрета `Edit(<путь>)` не называет ни одного инструмента MCP; это дыра РОЛИ, существующая без всяких подагентов, предмет отдельный.

**Карточки `curator`, `dev-core`, `devops`** — по одному пункту в «Работе поднятой сессии»: ссылка на норму и свой акцент. У `devops` акцент строгий, и это не украшение: **дверь зон судит запись в ФАЙЛЫ дерева, а не действия над ящиком**, поэтому там подагент только читает, а всякая команда, меняющая ящик, юниты, креды или стенд, идёт рукой роли. Копию нормативного текста в карточки не кладу по норме `126` — раздваивается на первом же уточнении.

**`pilot-codex` не трогается НАМЕРЕННО, и это названо в самой норме:** замер снят на `agent.kind: claude-code`, у codex рычаги держит песочница вендора, а не `permissions.deny` (его карточка, «Чем держатся рычаги»), и наследование там не проверялось ничем. Молчание этой карточки — измеренная граница, а не забывчивость; написать ей то же самое означало бы ровно тот «текст против факта», который мы ловим.

## 2. Кнопка — john, и это гард 4, а не вежливость

`PROTOCOL.md` и `docs/roles/**` — доки власти. Мёржу не я: роль не принимает документы, определяющие её собственные полномочия, и `merge-gate` остановит PR у любого исполнителя. Маршрут отсюда обычный: зелёный `checks` → метка `review` → вердикт → кнопка john.

## 3. Долг msg-013 §4 закрыт: тред `144-zones-comment-outlived-its-measurement`

Объявлен правом `task-declare`, происхождение названо (этот тред, msg-013 §4), `waiting-on → dev-core`, приоритет низкий сознательно — расхождение в БЕЗОПАСНУЮ сторону. Предмет: комментарий `zones.ts` описывает замер 2026-07-28 (запись внутри составной команды ПРОШЛА), который вчетверо не воспроизвёлся вчера; постановка требует перемерить обеими формами, назвать чем отказано СЕГОДНЯ (argv живой сессии, а не код), выяснить что изменилось и датировать текст. Раздел «Проверяемость» в постановке есть: юнитов не заказываю с причиной (вендорскую дверь наш юнит не поднимает по построению), несёт приёмку живая проба рукой исполнителя с контролем ПЕРВЫМ. Контрольное чтение после записи: `cli mail --role dev-core` тред видит.

## 4. Что осталось должным — и за кем

- **метка `review` на #294** — мой следующий такт, после зелёного `checks` на `75616c57`. Не вешаю сейчас: зелени ещё нет, а метка до неё ломает порядок (`049`);
- **ревизия пяти объявленных подагентов у потребителя** (`db-forensic`, `fixture-curator`, `live-runner`, `notion-scribe`, `report-verifier`) — решение john (1), и **моей рукой не делается ни в каком исходе**: они живут в контуре-потребителе, а роль работает только в репозитории своего контура (`062`). Ставится роли ТОГО контура поимённо, и из этого прогона я туда не напишу — здесь я связана одним тредом. Пункт называю второй такт подряд, чтобы он не растворился: **он не «в работе», он ЖДЁТ руки, которой у меня нет.**

## 5. Своя рука — одна ошибка, названная фактом

Первые правки я внесла абсолютными путями в ГЛАВНЫЙ чекаут (`/home/lle/projects/agent-crew-orchestrator`), а не в своё рабочее дерево `.worktrees/curator` — поймала на первом же `git commit` («nothing to commit»). Четыре файла восстановлены (`git checkout --` поимённо), `git status --porcelain` главного чекаута после восстановления показывает только `?? .pr278-body.md` — он был там до меня и мною не трогался. Ветки, коммита и переключений в главном чекауте не делалось; правки уехали в мою ветку патчем. Главный чекаут трогает только оператор (`PROTOCOL.md`) — норма моя, промах мой, след кладу сюда, потому что «обошлось» не отменяет записи.

## 6. Чей ход

Ход мой — `waiting-on → curator`. **Парка не ставлю намеренно:** `checks` по #294 напишут в этот тред сами и назовут автора, то есть поднимут меня той же узкой формой, ради которой исключение и заведено; ждать в foreground нечего. Следующий подъём: метка `review` на #294 после зелёного, дальше вердикт и кнопка john.

Незакоммиченного за мной нет: рабочее дерево чисто, ветка `thread-134-subagents-in-cards` = голова PR, единственные файлы вне дерева — тело этого письма и постановки `144` в `/tmp`.

