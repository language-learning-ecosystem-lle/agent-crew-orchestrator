# Интеграционная сюита доставки вердикта вне checks — решение john о шаге маршрута

participants: curator, dev-core · status: open

## msg-001 · from: curator · 2026-09-04 · expects: ack

**Требуется решение john: заводить ли интеграционную сюиту доставки вердикта (`.github/scripts/review-delivery.integration.sh`) в прогон `checks` — это новый шаг маршрута и расход квоты на каждом PR, то есть не моё решение.** Ниже — почему вопрос стоит сегодня и чего он стоит в обе стороны. Решать надо ОДНО: да / нет / «не сейчас, вернуться тогда-то».

## 1. Происхождение — замеренный дефект, а не опасение

Тред `127-verdict-delivery-suite-red-on-main`, PR [#274](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/pull/274) (смержен 2026-09-04T17:09Z, `454ecf42`).

- 2026-09-04T15:51Z в `main` приземлился #265 (тред `072`), расширивший исключение машинного писателя парковочной двери до `wake.mode === 'event'` целиком;
- этим он МОЛЧА сломал чтение стоящего парка в доставке вердикта: письмо `reviewer-pr` пошло в ЧУЖОЙ парк без `--parked-on`, а парк снимается лентой у первого же письма, которое его не повторило. **Доставка вердикта съедала чужой парк** — в том числе парк на john;
- в боевом контуре этот дефект не поднял бы НИКОГО: письмо ложится, джоба зелёная, парк исчезает тихо;
- поймала его сюита `review-delivery.integration.sh` — 8 красных состояний из 15, 24 проверки, — и только потому, что ревьюер запустила её РУКОЙ по своему критерию 9, разбирая посторонний PR #272. Между мержем #265 и этой находкой прошло 39 минут случайности, а не процедуры.

**Факт, ради которого этот тред:** сюита не входит ни в один прогон `checks` — проверено грепом, её не упоминает ни один `.ts`, `.yml` или `package.json`. В CI едет только шелл-ЮНИТ (`review-delivery.test.sh` через `src/roles/review-delivery.process.test.ts`), а он по построению знает о двери лишь то, что о ней написано в его фикстурах: он кормит разбор строкой из лога и потому НЕ ловит момент, когда дверь начинает говорить иначе. Ровно этот класс #265 и сломал.

## 2. Что это стоит в обе стороны

**Оставить как есть.** Цена — сегодняшняя: следующая правка двери красит сюиту так же тихо, а стои́т она ровно настолько, насколько кто-то помнит её запустить. Дефект того же класса (письмо, съедающее чужой парк) в поле не звонит: он выглядит как успешная доставка.

**Завести в `checks`.** Цена — время раннера на КАЖДОМ PR (сюита гоняет 15 состояний, каждое — сухой прогон и настоящая запись через `pnpm cli`, порядок — минуты, точную цифру назовёт исполнитель замером, я её не выдумываю) и новый шаг маршрута, который придётся держать зелёным всем ролям. Шапка самой сюиты сегодня говорит «в CI не поднимается — ему нужны pnpm-воркспейс и `origin/main` рабочего дерева»: то есть работа не сводится к строчке в воркфлоу, у неё есть настоящее содержание.

**Третья форма, которую называю честно:** запускать её не на каждом PR, а по условию (например, когда дифф трогает `.github/scripts/review-delivery*` или `packages/agent-protocol/src/thread/park-seen.ts`) — дешевле, но ловит не всё: #265 правил `registry.ts`, и такое условие его бы НЕ поймало. Условие по путям тут выглядит защитой и ею не является — это надо решать зная.

## 3. Почему это не моё решение

- **`.github/workflows/**` — док власти и зона, запрещённая роли curator**: любая из трёх форм выше трогает воркфлоу;
- **новый шаг маршрута = норма**, а норма идёт к john;
- **расход квоты раннера** — деньги, а деньги решает владелец.

Постановку исполнителю (dev-core) я напишу сама, как только направление названо; без слова john она не пишется — писать её значит уже выбрать.

## 4. Чего этот тред НЕ предмет

Дефект #265 уже починен и принят: сюита зелена на `main` целиком («ВСЕ СОСТОЯНИЯ ПРОШЛИ — 15 шт.», exit 0 на дереве `454ecf42`, замер мой). Здесь не чинится код и не пересматривается #265 — здесь решается ТОЛЬКО судьба канала: остаётся ли проверка рукой или становится шагом маршрута.

waiting-on → curator (john в это поле не принимается; ход держу я, вопрос стоит первой строкой).

## msg-002 · from: curator · 2026-09-05 · expects: none

**Слово john полем `delivers` (чат 2026-09-05 ~11:41Z): ДА — сюита доставки вердикта заводится в `checks` НА КАЖДОМ PR, но с замером цены ДО включения. Парковка снята.**

## 1. Что решено

**Направление — вторая форма** (сюита в прогоне, безусловно), **но включение обусловлено числом**: исполнитель сперва называет замером, сколько сюита идёт на нашем раннере, и пишет это в PR.

- **цена мелкая (порядка минуты-двух)** — включаем, вопрос закрыт этим словом, второго обращения к john не нужно;
- **цена крупная** — не включаем молча, а возвращаетесь к john С ЧИСЛОМ. Раннер живёт на том же ящике, что и роли: время сюиты отнимается у их тактов, и john хочет решать это зная, а не оценивая.

**Третья форма (по условию на пути) ОТКЛОНЕНА**, и по доводу самой curator: вчерашняя поломка пришла из `registry.ts` — условие по путям доставки её бы НЕ поймало. «Защита, которая ею не является» — формулировка принята как есть.

## 2. Почему john берёт цену на себя

Класс дефекта не звонит в поле по построению: письмо ложится, джоба зелёная, **чужой парк исчезает тихо** — в том числе парк на самого john. Поймано это было рукой ревьюера, по её собственному критерию, между делом, через 39 минут после посадки. Между «поймали случайно» и «ловит маршрут» разница ровно в том, повторится ли случайность.

**И вторая половина, названная в постановке:** юнит в CI не заменяет сюиту принципиально — он кормит разбор своими фикстурами и потому не видит момента, когда ДВЕРЬ начинает отвечать иначе. Ровно этот класс и сломался.

## 3. Что нужно в постановке

1. **замер времени сюиты на нашем раннере** — числом, в теле PR, до включения;
2. **работа по подъёму в CI имеет содержание**, а не сводится к строке: шапка сюиты сегодня говорит, что ей нужны pnpm-воркспейс и `origin/main` рабочего дерева. Что именно потребуется — назвать;
3. **отказ сюиты должен быть читаемым**: какое из состояний упало и чем, а не «сюита красная»;
4. **дифф трогает `.github/workflows/**` ⇒ кнопка john**, и круга ревью на нём может не быть по построению — назвать это в описании заранее.

## 4. Границы

Предмет — только канал: рука против маршрута. Код доставки не пересматривается, #265 не отзывается, сюита на `main` зелена целиком (15 состояний, замер curator).

waiting-on → curator.

## msg-003 · from: curator · 2026-09-05 · expects: answer

# Постановка dev-core: сюита доставки вердикта — шагом прогона `checks`

Основание — слово john полем `delivers` (msg-002 этого треда, чат 2026-09-05 ~11:41Z): **вторая
форма, сюита в `checks` НА КАЖДОМ PR, включение обусловлено замером цены**. Третья форма (условие по
путям) отклонена там же. Постановка ниже — исполнение этого слова, не его пересмотр.

## 0. Поправка ФАКТА, которую обязан знать исполнитель (замер мой, сегодня)

Слово john несёт посылку: «раннер живёт на том же ящике, что и роли: время сюиты отнимается у их
тактов». **По факту это не так для прогона `checks`, и на этой посылке стоял способ мерить цену, а не
направление.**

- `.github/workflows/checks.yml:25` — `runs-on: ubuntu-latest`; **все шесть воркфлоу репозитория
  hosted** (`grep -rn 'runs-on' .github/workflows/` → шесть строк, все `ubuntu-latest`);
- шапка того же файла (тред 006) называет причину и цену прямо: hosted выбран после публикации
  репозитория 2026-08-18, и «для публичных репозиториев hosted-раннеры бесплатны без лимита минут».

**Что из этого следует.** Тактов ролей сюита не отнимает вовсе, минут ящика не тратит; цена, которую
она реально берёт, ровно одна — **wall-clock прогона `checks` на КАЖДОМ PR**, то есть задержка между
пушем и меткой `review` у всех ролей. Поправка направление НЕ меняет и делает решение дешевле, чем
john его принимал, поэтому вторым обращением к john я её не несу и тред не паркую. Замер john
потребовал — замер остаётся обязательным, но мерить надо именно эту величину (п. 1). Если john
захочет пересмотреть условие на исправленной посылке — это его ход и он открыт; работа до тех пор
идёт.

## 1. Замер — первое требование, и оно попадает к кнопке john ДО нажатия

В теле PR тремя числами, каждое — из ПРОГОНА, а не из оценки:

1. **сколько идёт сама сюита** — длительность нового шага в прогоне `checks` ЭТОГО PR;
2. **насколько выросла джоба `checks` целиком** — длительность до и после, на PR и его базе;
3. **сколько состояний прогналось** — число из итоговой строки сюиты (она его считает прогоном:
   `review-delivery.integration.sh:519`), сверенное с числом состояний в файле.

Числа берутся из прогона самого PR: на событии `pull_request` воркфлоу исполняется в версии ветки,
поэтому новый шаг гоняется на этом же PR — отдельного стенда для замера не нужно.

**Порог — слово john дословно:** цена порядка минуты-двух — включаем, вопрос закрыт, второго
обращения не нужно; цена крупная — **не включать молча**, а написать число в тред, и к john с ним иду
я. Здесь это требование держится ещё и построением: PR трогает `.github/workflows/**`, значит кнопка
всё равно john (гард 4), и число обязано лежать в теле PR РАНЬШЕ, чем john на неё смотрит.

## 2. Работа имеет содержание — что именно потребуется (замер мой, читай как отправную точку, а не как список)

Шапка сюиты (`.github/scripts/review-delivery.integration.sh:19-20`) говорит: «в CI не поднимается —
ему нужны pnpm-воркспейс и `origin/main` рабочего дерева». Я прочитала файл; вот что за этим стои́т.

1. **`pnpm`-воркспейс** — в `checks` уже есть (`pnpm install --frozen-lockfile` до всех шагов). Сюита
   зовёт дверь как `pnpm -F agent-protocol --silent cli new-message …` (строка 123). Работы нет.
2. **`origin/main`** (строка 124: `--repo . --ref origin/main`) — в чекауте Actions этого
   remote-tracking ref может не быть вовсе. Готовый образец лежит в том же файле: шаги
   «исключительный ресурс» и «comms-derived» тянут его явно —
   `git fetch --no-tags --quiet origin "+refs/heads/$base:refs/remotes/origin/$base"`.
   **Но сначала ответь на вопрос, который здесь важнее fetch'а:** конфиг какого ref обязана читать
   сюита в CI? `origin/main` — это конфиг БАЗЫ, то есть PR, меняющий конфиг, проверялся бы не своим.
   Довод в пользу `HEAD` уже записан в этом же воркфлоу, у шага `config check`, и записан как
   несущий: «вопрос CI — держится ли конфиг ТОГО, что будет влито». Реши явно, назови выбор и причину
   в теле PR; если `HEAD` не годится — назови, ЧЕМ измерено, что не годится.
3. **`MAIL_SRC` (строка 25) — мёртвый умолчатель, и его надо закрыть, а не унаследовать.** Переменная
   присваивается путём живого дерева ящика
   (`/home/lle/projects/agent-crew-orchestrator/.worktrees/comms/agent-comms`) и **в файле больше не
   встречается ни разу** (`grep -n MAIL_SRC` — одна строка). Почта у всех состояний строится
   фикстурами в `mktemp -d`. То есть «сюите нужна живая почта ящика» — не факт, а остаток. Убери или
   объясни, что она делает; заодно поправь шапку, если после работы она перестала быть правдой:
   строка «в CI не поднимается» после этого PR становится ложной и остаться не может.
4. **`python3` + PyYAML** — состояния (10)–(13) вынимают тело шага из `claude-review.yml`
   разбором YAML (строка ~290). Есть ли модуль на `ubuntu-latest` — **проверь прогоном, а не
   ожиданием**; нет — назови, чем закрыл.
5. **Секретов сюите не нужно, и это надо СОХРАНИТЬ.** Состояния (8)/(9) гоняют настоящий пуш, но
   удалённая почта — путь на диске, сеть не трогается, а токен сюита подделывает сама
   (`FAKE_TOKEN="integration-not-a-secret"`). Отсюда шаг обязан работать и на fork-PR, где секретов
   нет вовсе. Если для CI понадобится секрет — это не CI-годность, а смена предмета: остановись и
   напиши в тред.
6. **git-идентичность** — в джобе уже ставится глобально шагом `git identity`; свои временные
   репозитории сюита настраивает сама.
7. **Место шага в джобе.** Критерий, а не буква: сюита — самый дорогой шаг, и дешёвый отказ должен
   называть себя раньше (ровно этим аргументом в шапке `config check` объяснено, почему он стоит
   первым). Ставь после `pnpm test`; иное место — назови причину.
8. **Условия на шаге нет.** На каждом PR — это и есть выбор john; `if:` по путям отклонён его словом.

## 3. Отказ обязан быть читаемым — требование, закрываемое показом, а не обещанием

Сегодня сюита печатает строки `  FAIL · …` и выходит `1`; сводка PR у упавшего шага показывает
«exit code 1». Требование: **из аннотации видно, КАКОЕ состояние упало и чем** — по номеру состояния
и его строке, а не «сюита красная». Образец в этом же файле: шаг `config check` повторяет первую
содержательную строку отказа через `::error::`.

**Закрывается это так:** сломай ОДНО состояние нарочно (мутацией у себя, не в диффе PR), прогони, и
**вставь в тело PR фактический текст аннотации**, которую увидит читатель сводки. Обещание «отказ
читаем» без этого текста требованием не закрыто.

## 4. Проверяемость

- **Юнит** — новых не требуется, и это осознанно: предмет постановки и есть перенос уже написанной
  проверки в маршрут. Существующий шелл-юнит (`review-delivery.test.sh` через
  `src/roles/review-delivery.process.test.ts`) остаётся как есть — он проверяет разбор, а сюита то,
  что дверь СЕГОДНЯ отвечает так, как разбор ожидает.
- **Интеграционный** — сама сюита, теперь исполняемая шагом. Приёмка зелени: прогон `checks` этого
  PR, id прогона и имя шага — в теле PR, вместе с числом прошедших состояний.
- **Живая приёмка человека** — не требуется; ловится ли класс, показывает п. 3 (нарочно сломанное
  состояние и текст аннотации).
- **Что НЕ покрывается, с причиной:** сеть и настоящая удалённая почта — сюита их не трогает по
  построению (пуш в путь на диске), и заводить их в CI не предмет; поведение двери за пределами пути
  доставки — тоже не предмет; и класс «два открытых PR ломают дверь одновременно» маршрутом не
  ловится вовсе.

## 5. Границы

- **Код доставки не пересматривается**, #265 не отзывается, поведение сюиты не меняется — меняется
  только то, что мешает ей идти в CI. Расширение состава состояний в этот PR не кладётся.
- **Вынос тела шага из `claude-review.yml` в `.github/scripts/**` запрещён** — запрет записан в самом
  файле, в секции треда 118 (строка 289, дословно «запрещено постановкой»): вынос снял бы файл с гарда 4, то есть с
  кнопки john.
- **Один PR, не два.** Разделить на «скрипты отдельно, воркфлоу отдельно» можно (гард 4 кончается на
  `workflows/**`, `.github/scripts/**` доком власти не является), и я это рассмотрела: не делаем,
  потому что CI-годность скриптовой половины НЕЧЕМ подтвердить, пока шага нет — такой PR въехал бы
  никем не проверенным.

## 6. Форма PR и merge

- PR открывается `pnpm protocol pr open`, в описании — `thread: 128-delivery-suite-outside-checks` и
  `role: dev-core`;
- зелёный `checks` на голове → метка `review` → круг ревьюера. **Круг здесь обычный:** исключение о
  самопропуске касается только PR, правящих `claude-review.yml`, а этот правит `checks.yml`;
- **merge — кнопка john (гард 4, `.github/workflows/**` — док власти).** Моей рукой этот PR не
  мёржится ни при каком исходе замера; я довожу до john готовность и число.

## 7. Хвост PR при передаче хода — прогнан, молчания не оставляю

`gh pr list --state open` (2026-09-05T11:5xZ): открытых два, **оба принадлежат чужим тредам**, и в
этом прогоне я связана тредом 128 — домержить их отсюда нечем, потому что след гарда 5 обязан лечь в
ленту ИХ треда, а писать туда мне некуда:

- **#276** «docs(box-setup): рука оператора в дереве роли …» — тред 062, метка `review` висит,
  `MERGEABLE`;
- **#272** «fix(review-delivery): эскалация вердикта адресуется слагом стоячего адреса …» — тред 125,
  метка `review` висит, `MERGEABLE`.

Метка ≠ круг: готовность каждого проверяется гардом 1 на ТЕКУЩЕЙ голове в своём треде, и это работа
того такта, а не этого. Называю их здесь ровно затем, чтобы они не потерялись молча.

waiting-on → dev-core.

## msg-004 · from: dev-core · 2026-09-05 · expects: none

# Сюита доставки заведена шагом `checks`: PR #277 открыт, жду прогон, числа впишу его же головой

Постановка исполнена целиком, PR
[#277](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/pull/277),
голова `ceca8ad6`, ветка `dev-core/128-delivery-suite-in-checks`. Ниже — что сделано, что
измерено и что осталось на следующий такт.

## 1. Шаг

`.github/workflows/checks.yml`, после `pnpm test` (критерий постановки: сюита — самый дорогой
шаг, дешёвый отказ должен называть себя раньше), без `if:` — на каждом PR, как решил john.
Шаг тянет `origin/main` явно (`+refs/heads/main:refs/remotes/origin/main`) и зовёт сюиту.
Секретов у шага нет ни одного — свойство сохранено, значит он работает и на fork-PR.

## 2. Замер — ЯЩИК УЖЕ ЕСТЬ, РАННЕР ВПИШУ СЛЕДУЮЩИМ ТАКТОМ

**Ящик, прогон мой, дерево `ceca8ad6`:** сюита целиком **53.4 s** (`/usr/bin/time`),
**15 состояний, 76 проверок**, exit 0. Число состояний — из итоговой строки прогона, не
переписано.

**Раннер:** три числа постановки (длительность шага; рост джобы `checks` до/после на PR и на
базе; число прошедших состояний из лога шага) берутся из прогона `checks` САМОГО PR #277 и
ложатся в тело PR правкой описания — **до метки `review` и до того, как john посмотрит на
кнопку**. Ждать прогон в foreground нельзя (`PROTOCOL.md`, «Ожидание чужого прогона — не
действие»), поэтому такт передаю с парком на прогон; правка тела голову не двигает, круг ею
не аннулируется.

Порог слова john держу как названо: 53 s на ящике в «минуту-две» укладываются с запасом, но
`ubuntu-latest` слабее ящика (цена, названная шапкой джобы ещё тредом 006) — поэтому решает
число раннера, а не это. Крупным оно окажется — не включаю молча, несу число тебе.

Поправку про hosted-раннеры (msg-003) взял как есть и записал в тело PR и в
`docs/protocol-reference.md`: тактов ролей сюита не отнимает, цена ровно одна — wall-clock
`checks` на каждом PR.

## 3. Четыре пункта содержания — что с ними стало

1. **pnpm-воркспейс** — в джобе уже был, работы нет.
2. **`origin/main` против `HEAD` — решено `origin/main`, и вот ЧЕМ измерено**, что `HEAD` не
   годится: этот ref читает **не сюита, а продовый код доставки** — `review-delivery.sh:321`
   (`role exists`), `:466`, `:516` (`new-message`), — который состояния (8)/(9)/(14)/(15)
   гоняют целиком. Правка этих строк ради CI выходит за границу постановки, а перевод одной
   строки 124 на `HEAD` дал бы ДВА разных конфига в одном прогоне, то есть проверку
   полусобранного; fetch при этом всё равно остаётся обязательным, так что `HEAD` не
   экономит даже его. И довод за `HEAD` здесь уже отвечен раньше по джобе — шагом
   `config check --ref HEAD`: сюита проверяет не годность конфига, а то, что ДВЕРЬ отвечает
   так, как её разбор ожидает.
3. **`MAIL_SRC` убран** — подтверждаю твой замер: умолчатель на путь живого ящика, в файле
   больше не встречался ни разу, почта всех состояний строится фикстурами в `mktemp -d`.
4. **`python3` + PyYAML** — на ящике есть (`yaml 6.0.1`). На `ubuntu-latest` это **проверит
   прогон #277**, и я его не предвосхищаю: если модуля там нет, состояния (10)–(13)
   покраснеют ПО ИМЕНИ — провал вынимания тела шага адресован всей четвёрке отдельной
   строкой «тело шага не вынуто из yaml (нужен python3 с PyYAML)», зеленью он не
   притворится. Чем закрою в этом случае — назову числом и правкой в следующем такте, не
   молча.

Шапка «в CI не поднимается» стала ложной и переписана. `docs/protocol-reference.md` получил
раздел о решении, и там же поправлено стареющее «девять состояний (руками, не в CI)» из
треда 088 — теперь число состояний не переписано цифрой, а названо прогоном.

## 4. Отказ читаем — ПОКАЗАН, а не обещан

Состояние (4) сломано нарочно мутацией (ожидаемый флаг `--parked-on john` → `jane`; в диффе
её нет), сюита прогнана целиком. Фактический текст:

```
  FAIL · (4) флаг выбран: ожидалось '--parked-on jane', получено '--parked-on john'
интеграционный прогон доставки: ЕСТЬ ПРОВАЛЫ — 1 в состояниях: 4
::error::интеграционная сюита доставки вердикта: провалов 1, состояния: 4; первый — (4) флаг выбран: ожидалось '--parked-on jane', получено '--parked-on john'
```

Каждая строка `FAIL` теперь несёт номер состояния (раньше он читался глазом из заголовка
выше по логу), а итог повторяет число провалов, список состояний и ПЕРВЫЙ провал дословно —
форма взята у `config check` в той же джобе.

## 5. Границы

Код доставки не тронут, #265 не отозван, состав состояний не расширен, тело шага из
`claude-review.yml` не вынесено. Один PR, не два. Незакоммиченного у меня нет: всё в
`ceca8ad6`, ветка запушена, рабочее дерево чистое.

## 6. Что дальше и чей ход

Кнопка на этом PR — **john** (гард 4: `.github/workflows/**`), и я это в теле PR назвал
заранее. Маршрут: зелёный `checks` → числа замера в тело PR → метка `review` → круг
ревьюера (здесь он ОБЫЧНЫЙ: исключение о самопропуске касается только правок
`claude-review.yml`) → кнопка john.

Ход держу на себе с парком на прогон #277: следующий такт читает исход, вписывает три числа
и вешает метку. Если прогон красный — чиню и докладываю тем же тредом.

Хвост, названный тобой в п. 7, я не трогал: #276 и #272 — чужие треды, из этого такта их
домержить нечем.

## msg-005 · from: github · 2026-09-05 · expects: none

❌ **checks по PR #277: `failure`.**

ci(checks): интеграционная сюита доставки вердикта идёт шагом прогона (тред 128) · голова `ceca8ad6eacee8f406a2c607acf0544b059b4f0b` · попытка 1 · прогон [`33964609840`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/33964609840)

- `checks` — **failure**

## msg-006 · from: dev-core · 2026-09-05 · expects: none

# Прогон нашёл скрытую зависимость сюиты от токена ящика; починено, голова `51e7345c`, жду второй прогон

Первый прогон #277 (`33964609840`, голова `ceca8ad6`) — **красный**, и упал он не там, где
я ждал: не на `python3`/PyYAML (эта четвёрка прошла), а на состояниях **(5) и (14)**, пять
проверок. Числа замера, ради которых стои́т кнопка john, прогон при этом дал полностью и они
уже в теле PR.

## 1. Замер — СДЕЛАН, и порог пройден с запасом

| что | число |
| --- | --- |
| шаг сюиты в прогоне `checks` | **33 s** (12:01:19Z → 12:01:52Z, API Actions) |
| джоба `checks` на этом PR | **6m36s** |
| джоба `checks` на базе — три последних прогона `main` | 5m21s, 6m15s, 6m13s |
| рост джобы | **+33 s** к ~6 минутам, ~9 % |
| состояний прогналось | **15**, из итоговой строки шага |

**Вывод по слову john: включаем, второго обращения не нужно** — 33 s это «порядка
минуты-двух» с запасом. И поправка к моему же предположению: раннер оказался БЫСТРЕЕ ящика
(33 s против 47.7 s), а не медленнее. Доминирует в джобе `pnpm test` — 5m36s из 6m36s.

## 2. Что нашёл прогон — это находка, а не мой промах в переносе

Состояния (5)/(14) паркуются на ЧУЖОЙ `run:191`. На входе такого парка дверь задаёт GitHub
ровно один вопрос: `gh pr view 191 --json headRefOid,mergeable,statusCheckRollup`
(`cli.ts`, `runParkFacts`).

- **на ящике** у сессии токен стои́т в среде: вопрос проходит, дверь видит «круг на голове уже
  кончился» и отказывает кодом 2 — это и есть ожидание обоих состояний;
- **на раннере** токена нет, и `judgeRunPark` **по построению** деградирует в другую сторону:
  парк оставляет стоять, письмо ПРОПУСКАЕТ, код 0 («A VENDOR THAT COULD NOT BE ASKED DOES NOT
  REFUSE THE PARK» — деградация верная, дверь не дефектна).

**Факт, который из этого следует:** утверждение «сюита сети не трогает, секретов ей не нужно»
(п. 2.5 постановки, замер твой) было правдой НЕ ЦЕЛИКОМ — два состояния из пятнадцати молча
зависели от токена, стоящего в среде ящика, и рукой это не проявлялось никогда, потому что
рукой её гоняли только там. Маршрут показал это первым же прогоном; это ровно тот класс, ради
которого john и завёл шаг.

## 3. Чем закрыто (голова `51e7345c`)

Ответ вендора на этот единственный вопрос стал **фикстурой**: подставной `gh` в `$PATH`
отвечает «голова, MERGEABLE, один ЗАВЕРШЁННЫЙ прогон», а на любой другой вопрос отказывает
громко — новый поход кода доставки в сеть будет виден провалом состояния, а не тихим выходом
наружу. Дверь остаётся живой: проверяется ЕЁ решение, а не GitHub.

**Замер лечения:** 15/15 состояний, 76 проверок, **47.7 s**, прогнано
`env -u GH_TOKEN -u GITHUB_TOKEN` — то есть в условиях раннера, без единого креденшла.

**Отклонённая альтернатива, называю явно:** `GH_TOKEN: ${{ github.token }}` на шаге вернул бы
зелень дешевле, но привязал бы прогон КАЖДОГО PR к сети и к тому, что живой PR #191 и завтра
выглядит так же, а на fork-PR оба состояния всё равно потерялись бы. Твоё требование
«секретов не нужно, и это надо СОХРАНИТЬ» держится буквально: токенов у шага ноль.

**Отступление от границы, которое докладываю, а не прячу:** постановка говорит «поведение
сюиты не меняется — меняется только то, что мешает ей идти в CI». Подставной `gh` меняет почву
двух состояний и на ЯЩИКЕ тоже: там они теперь тоже не ходят в сеть. Считаю это внутри
границы — зависимость от среды и была тем, что мешало идти в CI, — но если ты читаешь иначе,
скажи, и я верну обсуждение.

## 4. Чего прогон НЕ подтвердил и что осталось

- **`python3` + PyYAML на `ubuntu-latest` — ЕСТЬ**: состояния (10)–(13) в красном прогоне
  прошли целиком. Вопрос п. 2.4 постановки закрыт прогоном, а не ожиданием.
- **Аннотация читаема — проверено настоящим отказом**, а не только нарочно сломанным
  состоянием: сводка прогона показала
  `интеграционная сюита доставки вердикта: провалов 5, состояния: 5 14; первый — (5) код двери:
  ожидалось '2', получено '0'` — состояние и причина названы без чтения лога.
- **Не подтверждена зелень целиком**: второй прогон (`33967301873`, голова `51e7345c`) идёт
  прямо сейчас.

## 5. Состояние работы и чей ход

Незакоммиченного нет: всё в `51e7345c`, ветка запушена, рабочее дерево чистое. Тело PR #277
несёт все три числа замера и разбор находки — то есть к кнопке john оно готово РАНЬШЕ, чем он
на неё смотрит.

Ход держу на себе с парком на прогон: следующий такт читает исход `33967301873` и, если
зелено, вешает метку `review` (круг здесь обычный — исключение о самопропуске касается только
правок `claude-review.yml`). Красный — чиню и докладываю тем же тредом. Кнопка на этом PR
по-прежнему john, гард 4.

## msg-007 · from: github · 2026-09-05 · expects: none

✅ **checks по PR #277: `success`.**

ci(checks): интеграционная сюита доставки вердикта идёт шагом прогона (тред 128) · голова `51e7345c34897fb5c36eb185bf542f8e2e02a703` · попытка 1 · прогон [`33967301873`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/33967301873)

- `checks` — **success**

👉 Круга ревью на этой голове ещё нет — метка `review` не повешена. По норме 03.08 (тред 049, [#183](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/pull/183)) она вешается ПОСЛЕ зелёного `checks` на той же голове, то есть сейчас. Ход у автора ровно на это одно действие.

## msg-008 · from: dev-core · 2026-09-05 · expects: none

# `checks` зелен на `51e7345c`, числа перемеряны с ЗЕЛЁНОГО прогона, метка `review` повешена — круг идёт

Такт короткий и делает ровно то одно действие, на которое указало письмо смотрителя: метку.
Но перед ней я перемерил цену, потому что числа в теле PR были сняты с КРАСНОГО прогона.

## 1. Зелень — и она снята с СЕГОДНЯШНЕЙ базы, а не с вчерашней

Прогон [`33967301873`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/33967301873),
голова `51e7345c`, `success`. Шаг «доставка вердикта — интеграционная сюита» — зелёный,
итоговая строка: «интеграционный прогон доставки: ВСЕ СОСТОЯНИЯ ПРОШЛИ — 15 шт.»

**Стык с чужими правками проверен прогоном, а не предположением.** Между базой моей ветки
(`06bf1c41`) и сейчас в `main` сели #276 (12:05:48Z) и **#272 (12:13:30Z) — а он трогал ТУ ЖЕ
`review-delivery.integration.sh`, что и я**. Что зелень не про вчерашнее дерево, говорит лог
самого прогона дословно:

```
HEAD is now at 6707ab73 Merge 51e7345c34897fb5c36eb185bf542f8e2e02a703 into ccf7691d91307632c7d87b58d88088aba0b2dd7a
```

`ccf7691d` — текущая голова `main`. На `pull_request` чекаут — merge-ref, то есть прогон судил
слияние моей головы с базой, уже включающей #272 и #276. Сверено сейчас: `mergeable`
`MERGEABLE`, `mergeStateStatus` `CLEAN`, голова с прошлого такта не двигалась.

## 2. Цена — перемерена, потому что прежние числа были ценой КРАСНОГО прохода

Прошлые 33 s / 6m36s сняты с прогона `33964609840`, где сюита обрывалась на провалах (5)/(14):
такой шаг короче настоящего, а кнопке john нужна цена зелёного прохода всех пятнадцати
состояний. Числа ниже — из API Actions (`/actions/runs/<id>/jobs`), тем же способом измерены и
три базовых прогона:

| что | число |
| --- | --- |
| шаг сюиты в `checks` | **46 s** (12:58:52Z → 12:59:38Z) |
| джоба `checks` на этом PR | **6m41s** (12:53:03Z → 12:59:44Z) |
| джоба `checks` на базе — три последних прогона `main` | 5m21s, 6m15s, 6m13s (`33965435726`, `33965081442`, `33963718245`) |
| рост джобы | **+46 s** к ~6 минутам, ~13 % |
| состояний прогналось | **15**, проверок **76** |

Состояния и проверки посчитаны В ЛОГЕ ШАГА, а не переписаны из прошлого доклада: заголовков
`== (N)` — 15, строк `ok ·` — 76, и это сходится с итоговой строкой сюиты.

**Вывод по порогу слова john не меняется: включаем, второго обращения не нужно.** 46 s — это
«порядка минуты-двух» с запасом; доминирует в джобе не сюита, а `pnpm test` (5m22s из 6m41s).
Раннер по-прежнему быстрее ящика (46 s против 53.4 s замера ящика).

Тело PR #277 переписано этими числами (правка тела голову не двигает, круг ею не
аннулируется) — то есть у кнопки john лежит цена зелёного прохода, а не красного. Заодно в
п. 5 «Проверяемость» приёмка зелени закрыта id прогона и именем шага, как требовала
постановка, а не оставлена обещанием на следующий такт.

## 3. Метка

`review` повешена ПОСЛЕ зелёного `checks` и на ТОЙ ЖЕ голове `51e7345c` — голова между
прогоном и меткой не двигалась, проверено перед навешиванием. Круг поднялся событием метки:
прогон [`33968945970`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/33968945970)
«Claude PR Review», событие `pull_request`, голова `51e7345c`, `in_progress` — это и есть
якорь гарда 1.

## 4. Чей ход и чего я жду

Жду вердикт ревьюера по #277 — парк `run:277`. Ход держу на себе: **зелёный вердикт — иду к
тебе, потому что кнопка на этом PR john (гард 4, `.github/workflows/**`), а к john с готовым
PR и числом идёшь ты; `needs-fixes` — чиню сам и докладываю тем же тредом.** В foreground
круг не жду — он пишет в тред сам.

Незакоммиченного у меня нет: всё в `51e7345c`, ветка запушена, рабочее дерево чистое; правка
тела PR — не коммит. Хвост из п. 7 постановки (#276, #272) с прошлого такта разрешился сам:
оба смержены (12:05:48Z и 12:13:30Z) и в `main` стоят.

## msg-009 · from: reviewer-pr · 2026-09-05 · expects: answer

verdict: approve
pr: 277
waiting-on: dev-core

Прогнано: `pnpm typecheck` — зелёный (оба пакета). `pnpm exec biome check` по изменённым файлам —
0 обработанных: biome не покрывает `.sh`/`.yml`/`.md`, находка не применима. Полный прогон тестов не
повторял — `checks` на голове `51e7345c34897fb5c36eb185bf542f8e2e02a703` зелёный, прогон
[33967301873](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/33967301873).

`pnpm protocol merge-gate --ref origin/main --pr 277` (живой исход): guard 1 (approve на голове) —
STOP, ждёт этот вердикт; guard 2 (green checks) — STOP, `review=IN_PROGRESS` (этот же круг); guard 3
(ascent к решению john) — держится, читает тред 128; guard 4 (self-merge на доке власти) — STOP,
«john merges this one — it changes `.github/workflows/checks.yml`»; guard 5 (trace of merge) —
держится; mergeability — `MERGEABLE` (`mergeStateStatus UNSTABLE`, ожидаемо при незавершённом круге).
Токен видит все восемь доков власти репозитория (`agent-protocol.json`, четыре `docs/roles/*.md`,
`REVIEWER.md`, `PROTOCOL.md`, `.github/workflows`) без отказа доступа.

Проверено по критериям:

- **Критерий 1 (числа).** Все три числа PR сверены с API Actions, а не с текстом PR: шаг «доставка
  вердикта — интеграционная сюита» на голове — `12:58:52Z→12:59:38Z` = 46 s (заявлено 46 s); джоба
  `checks` целиком — `12:53:03Z→12:59:44Z` = 6m41s (заявлено 6m41s); три базовых прогона `main`
  (job-level, не run-level) — 5m21s/6m15s/6m13s (заявлено то же). Число состояний и проверок — из
  реального лога шага, отфильтрованного по имени шага: 15 заголовков `== (N)`, 76 строк `ok ·`, 0
  `FAIL` (заявлено 15/76). Расхождений нет.
- **Критерий 2/9 (тест бьёт заявленное, текст = факт).** Прогнал сюиту локально
  (`env -u GH_TOKEN -u GITHUB_TOKEN bash .github/scripts/review-delivery.integration.sh` после
  `git fetch origin main`) — все 15/15 состояний зелёные без единого креденшла, подтверждает
  заявленное закрытие скрытой зависимости от токена ящика. Сверил argv фикстуры `gh` (`pr view 191
  --json headRefOid,mergeable,statusCheckRollup`) с реальным вызовом в `cli.ts:3337` — совпадает
  дословно; ответ фикстуры (`MERGEABLE`, один `COMPLETED`/`SUCCESS` прогон) действительно ведёт
  `judgeRunPark` в ветку отказа (не `checkRuns===0`, не «gh не спросить»), то есть состояния (5)/(14)/(15)
  проверяют именно тот путь двери, который заявлен.
- **Критерий 3 (скоуп).** `thread: 128-delivery-suite-outside-checks` и `role: dev-core` — на месте.
  Дифф — ровно три файла (`review-delivery.integration.sh`, `checks.yml`,
  `docs/protocol-reference.md`); `review-delivery.sh` (продовый код доставки) не тронут, состав
  состояний не расширен, тело шага из `claude-review.yml` не вынесено — границы постановки (тред 128,
  msg dev-core 11:56) соблюдены.
- **Критерий 4 (зоны).** `pnpm protocol zones check --role dev-core --base origin/main` — все 3 пути
  вне `forbidden`, зелёный.
- **Критерий 5 (доки власти).** Дифф трогает `.github/workflows/checks.yml` — merge только john,
  и это названо в описании PR явно («КНОПКА ЗДЕСЬ JOHN, И ЭТО ПО ПОСТРОЕНИЮ»); merge-gate (выше)
  подтверждает guard 4 тем же выводом.
- **Критерий 6/8/10/11** — не применимы/не нарушены: нет нового поля конфига, `agent-comms/**` не
  тронут, `agent-protocol.json` в диффе не читается напрямую, а добавленная фикстура `gh` отказывает
  громко на любой незнакомый вопрос (не молчит).

**waiting-on объяснён:** verdict approve, но PR трогает док власти (`.github/workflows/**`) — curator
мёржить не вправе (критерий 5, исключение из раздела «Формат вердикта»). Ход и merge — за автором,
который доводит готовность до john; adресат этого вердикта — `dev-core`, не `curator`.

---

Доставлено шагами прогона [`33968945970`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/33968945970) по PR #277, голова `51e7345c34897fb5c36eb185bf542f8e2e02a703` (вердикт написан агентом ревьюера, доставка — джобой: тред 088).
Ход передан роли `dev-core` — так объявил сам вердикт.

## msg-010 · from: dev-core · 2026-09-05 · expects: ack

# Вердикт по #277 — `approve`. Все гарды двери держатся, кроме гарда 4, и он STOP ПО ПОСТРОЕНИЮ: кнопка john

Ход к тебе ровно за одним: PR готов к кнопке, а к john с готовым PR идёшь ты. Ниже —
живой исход двери, а не мой пересказ вердикта, и то одно число, ради которого john
обусловил включение.

## 1. Дверь, прогнанная сейчас (13:42Z), а не переписанная из чужого письма

`pnpm protocol merge-gate --ref origin/main --pr 277 --review-workflow 'Claude PR Review'`:

| гард | исход |
| --- | --- |
| 1 · approve на текущей голове | **ok** — approve на `51e7345c` внутри круга `33968945970` «Claude PR Review» на ЭТОЙ голове (13:27:22Z…13:32:14Z) |
| 2 · зелёный `checks` на той же голове | **ok** — 2 проверки зелены: `review=SUCCESS`, `checks=SUCCESS` |
| 3 · восхождение к решению john | **you** — читает этот тред; решение john стоит в msg-002 полем `delivers` |
| 4 · нет самомержа на доке власти | **STOP** — «john merges this one — it changes `.github/workflows/checks.yml`» |
| 5 · след мержа | **you** — назвать мерж в письме тем, кто нажал |
| mergeability (не гард, факт) | `MERGEABLE`, `mergeStateStatus` **CLEAN** |

Итог двери — `REFUSED`, и это НЕ дефект PR: гард 4 отказывает любому исполнителю, включая
меня, кому `.github/workflows/**` открыт на запись. Единственная рука, которую он пропустит, —
john. Гарды 3 и 5 стоят `you` потому, что дверь гоняна мной: они адресованы тому, кто мержит.

**Голова с момента зелени не двигалась и база не уехала:** `origin/main` сейчас `ccf7691d` —
ровно тот коммит, с которым прогон `33967301873` собирал merge-ref («HEAD is now at `6707ab73`
Merge `51e7345c` into `ccf7691d`»). То есть зелень снята с ТОГО дерева, которое ляжет кнопкой,
а не с соседнего. Метка `review` на месте, круг закрыт, `mergeStateStatus` `CLEAN` (не
`UNSTABLE`, как было у ревьюера в момент идущего круга).

## 2. Число, которым john обусловил включение

Слово john (msg-002): «цена мелкая — включаем, второго обращения не нужно; цена крупная —
возвращаетесь С ЧИСЛОМ». Замер снят с ЗЕЛЁНОГО прохода всех пятнадцати состояний, из API
Actions:

- шаг сюиты в `checks` — **46 s**;
- джоба `checks` целиком на PR — 6m41s против 5m21s / 6m15s / 6m13s на трёх последних прогонах
  `main`, то есть **+46 s к ~6 минутам, ~13 %**;
- прогналось **15 состояний, 76 проверок**, доминирует в джобе не сюита, а `pnpm test`
  (5m22s из 6m41s).

**46 s — это «порядка минуты-двух» с запасом, значит по слову john вопрос закрыт и второго
обращения к нему по ЦЕНЕ не нужно.** К john остаётся ровно кнопка, а не решение.

Числа сверены ревьюером независимо (её критерий 1: все три против API Actions, состояния и
проверки — из реального лога шага, расхождений нет), а не приняты с моих слов.

## 3. Что john нажимает — и что при этом было найдено

Дифф — три файла: `.github/scripts/review-delivery.integration.sh`, `.github/workflows/checks.yml`,
`docs/protocol-reference.md`. Продовый код доставки (`review-delivery.sh`) не тронут.

Находка, которую стоит назвать john отдельно, потому что она меняет ОСНОВАНИЕ вопроса:
утверждение «сюите секретов не нужно» держалось не целиком — **два состояния из пятнадцати
молча зависели от токена, стоящего в среде ящика**, и рукой это не проявлялось никогда,
потому что рукой сюиту гоняли только там. Первый же прогон в CI это показал. Закрыто
фикстурой подставного `gh`, отвечающего на единственный вопрос двери и громко отказывающего
на любой другой, — токенов у шага **ноль**, требование «секретов не нужно» держится буквально
и на fork-PR тоже. Отклонённую альтернативу (`GH_TOKEN: ${{ github.token }}`) называю явно:
она вернула бы зелень дешевле, но привязала бы каждый PR к сети и к живому чужому #191.

Отступление, которое я доложил ещё в прошлом такте и повторяю здесь, чтобы john читал его у
кнопки, а не в архиве: фикстура меняет почву этих двух состояний И НА ЯЩИКЕ тоже — там они
теперь тоже не ходят в сеть. Считаю это внутри границы постановки («меняется только то, что
мешает сюите идти в CI»), но читающий вправе прочесть иначе.

## 4. Состояние работы и чей ход

Незакоммиченного нет: всё в `51e7345c`, ветка запушена, рабочее дерево чистое. Тело PR #277
несёт числа зелёного прохода и разбор находки — у кнопки лежит цена того прохода, который
джоба реально делает.

**Ход к тебе, `curator`:** отнести #277 к john кнопкой. Парка не ставлю — ход отдан человеку
через тебя, ждать машину здесь нечего. Если john по п. 3 читает границу иначе и хочет фикстуру
только под CI — верни ход мне, это правка одного условия в скрипте и новый круг.

## msg-011 · from: curator · 2026-09-05 · expects: ack

# Требуется решение john: нажать merge на PR #277 — сюита доставки вердикта шагом `checks`. Гарды 1, 2, 3 и 5 держатся, гард 4 STOP ПО ПОСТРОЕНИЮ (дифф трогает `.github/workflows/checks.yml`), цена замерена и равна **+46 s**, то есть в порог твоего слова укладывается с запасом.

Ход к тебе ровно за кнопкой, а не за решением: направление ты уже назвал (msg-002, поле
`delivers`), и условие «цена мелкая — включаем, второго обращения не нужно» замером ЗАКРЫТО.
Ниже — то, что я проверила СВОЕЙ рукой, отдельно от того, что доложили исполнитель и ревьюер.

**PR:** [#277](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/pull/277),
голова `51e7345c34897fb5c36eb185bf542f8e2e02a703`, `mergeable` `MERGEABLE`, `mergeStateStatus`
`CLEAN`. Заголовок сквоша — `ci(checks): интеграционная сюита доставки вердикта идёт шагом прогона (тред 128)`.

## 1. Дверь, прогнанная МОЕЙ рукой в 13:43Z — не пересказ чужого письма

`pnpm protocol merge-gate --ref origin/main --pr 277 --review-workflow 'Claude PR Review'`:

| гард | мой исход |
| --- | --- |
| 1 · approve на текущей голове | **ok** — approve на `51e7345c` внутри круга `33968945970` «Claude PR Review» на ЭТОЙ голове (13:27:22Z…13:32:14Z) |
| 2 · зелёный `checks` на той же голове | **ok** — 2 проверки зелены: `review=SUCCESS`, `checks=SUCCESS` |
| 3 · восхождение к решению john | **держится** — твоё слово стоит в msg-002 этого треда полем `delivers` (чат 2026-09-05 ~11:41Z), источник назван; PR несёт `thread: 128-delivery-suite-outside-checks` |
| 4 · нет самомержа на доке власти | **STOP** — «john merges this one — it changes `.github/workflows/checks.yml`» |
| 5 · след мержа | обязательство — след ляжет в этот тред следующим тактом curator, поимённо |

Итог двери — `REFUSED`, и это не дефект PR: гард 4 останавливает ЛЮБОГО исполнителя, включая
dev-core, кому `.github/workflows/**` открыт на запись. Пропустит его только твоя рука.

Дверь вывела восемь доков власти данными (`agent-protocol.json`, четыре карточки ролей,
`REVIEWER.md`, `PROTOCOL.md`, `.github/workflows`) — полнота гарда 4 не равна моей памяти.

## 2. Число, которым ты обусловил включение

Замер снят с ЗЕЛЁНОГО прохода всех пятнадцати состояний (прогон `33967301873`, голова
`51e7345c`), из API Actions, и независимо перепроверен ревьюером против того же API:

- шаг сюиты — **46 s**;
- джоба `checks` целиком — **6m41s** против 5m21s / 6m15s / 6m13s на трёх последних прогонах
  `main`, то есть **+46 s к ~6 минутам, ~13 %**;
- прогналось **15 состояний, 76 проверок**; доминирует в джобе не сюита, а `pnpm test`
  (5m22s из 6m41s).

**По твоему порогу дословно** («порядка минуты-двух — включаем, вопрос закрыт этим словом,
второго обращения к john не нужно») 46 s закрывают вопрос цены. Вторым обращением ПО ЦЕНЕ я к
тебе не иду — иду только за кнопкой, которой требует гард 4.

**Поправка посылки, на которой стоял способ мерить, и я её несу тебе явно, потому что она
делает решение дешевле, чем ты его принимал.** Твоё слово опиралось на «раннер живёт на том же
ящике, что и роли: время сюиты отнимается у их тактов». Для `checks` это неверно (замер мой,
msg-003): все шесть воркфлоу репозитория — `runs-on: ubuntu-latest`, а для публичных
репозиториев hosted бесплатен без лимита минут. **Тактов ролей сюита не отнимает вовсе и минут
ящика не тратит.** Цена ровно одна — wall-clock прогона `checks` на каждом PR, то есть задержка
между пушем и меткой `review`. Направление это не меняет, поэтому работа шла; но если ты
хочешь пересмотреть условие на ИСПРАВЛЕННОЙ посылке — это твой ход, и он открыт.

## 3. Что я проверила сверх докладов — чтением, а не доверием

- **Требование постановки «число обязано лежать в теле PR РАНЬШЕ, чем john на него смотрит» —
  закрыто:** тело #277 прочитано мной, все три числа в нём, и они числа ЗЕЛЁНОГО прохода
  (прежние 33 s / 6m36s были сняты с красного, где сюита обрывалась, — исполнитель перемерил
  сам, не дожидаясь замечания).
- **Дифф — ровно три файла**, как объявлено: `.github/scripts/review-delivery.integration.sh`
  (+84−10), `.github/workflows/checks.yml` (+42−0), `docs/protocol-reference.md` (+49−5).
  Продовый код доставки `review-delivery.sh` не тронут, состав состояний не расширен, тело шага
  из `claude-review.yml` не вынесено — три границы постановки держатся.
- **Требование «отказ читаем» закрыто ПОКАЗОМ, а не обещанием:** в теле PR лежит фактический
  текст аннотации сломанного нарочно состояния, и та же форма подтвердилась настоящим отказом
  первого прогона (`провалов 5, состояния: 5 14; первый — (5) код двери: ожидалось '2',
  получено '0'`). Читатель сводки видит номер состояния и причину без чтения лога.
- **Метка `review` на месте, круг закрыт**, вердикт `approve` лежит в окне круга на этой голове —
  вердикта-сироты здесь нет.
- **`python3` + PyYAML на `ubuntu-latest`** — вопрос постановки закрыт прогоном (состояния
  (10)–(13) прошли), а не ожиданием.

## 4. Развилка, которую dev-core вынес мне явно, и я её решаю — фикстура ВНУТРИ границы

Исполнитель дважды честно доложил отступление и просил прочесть: подставной `gh` меняет почву
двух состояний И НА ЯЩИКЕ тоже. **Читаю как внутри границы, и вот чем это меряется, а не «мне
так кажется»:**

1. **Граница постановки — «меняется только то, что мешает сюите идти в CI».** Мешала ровно эта
   зависимость: на ящике вопрос двери к вендору проходил живым токеном среды, на раннере токена
   нет, и дверь ПО ПОСТРОЕНИЮ деградировала в другую сторону — пять проверок покраснели именно
   на этом (прогон `33964609840`). Фикстура убирает то самое препятствие.
2. **Предмет проверки не подменён:** дверь остаётся живой, меряется ЕЁ решение; фикстура
   отвечает на единственный вопрос и громко отказывает (`exit 3`) на любой другой — новый поход
   в сеть проявится провалом состояния, а не тихим выходом. Ревьюер сверила argv фикстуры с
   реальным вызовом двери дословно и подтвердила, что ответ ведёт `judgeRunPark` в ТУ ЖЕ ветку
   отказа, что и живой ответ на ящике, — то есть путь тот же, а не похожий.
3. **Правка на ящике — это не расширение предмета, а снятие скрытой лжи.** До неё два состояния
   зеленели потому, что рядом случайно стоял токен и живой чужой #191 случайно выглядел нужным
   образом. Это мерило дикой природы, а не двери. Фикстура сближает почвы, а не разводит их.
4. **Новой нормы дифф не вводит:** ни поля, ни ключа конфига, ни формы сообщения, ни права, ни
   снятия запрета; шаг маршрута тут ровно один — тот, который назвал ты.
5. **Отклонённая альтернатива названа явно и отклонена правильно:** `GH_TOKEN: ${{ github.token }}`
   вернул бы зелень дешевле, но привязал бы прогон КАЖДОГО PR к сети и к тому, что живой #191
   завтра выглядит так же, а на fork-PR оба состояния всё равно потерялись бы. Требование
   постановки «секретов не нужно, и это надо СОХРАНИТЬ» держится буквально: токенов у шага ноль.

**Возвращать обсуждение не нужно** — если только ты, john, не читаешь п. 3 иначе; тогда это
правка одного условия в скрипте и новый круг, и ход возвращается dev-core.

## 5. Один хвост, который я НЕ считаю блокирующим и несу отдельно

`docs/protocol-reference.md` в этом диффе записывает свойство «секретов шагу не нужно» как
факт, но НЕ записывает саму находку — что свойство держалось не целиком и два состояния молча
зависели от токена среды, а держит его теперь громко отказывающая фикстура. Будущий читатель
может принять фикстуру за украшение и убрать её. Кнопку я на этом не держу: правка дока
двинула бы голову и аннулировала бы вердикт ради абзаца. Беру хвостом в следующий такт
отдельным PR (`docs/protocol-reference.md` доком власти не является — мержу сама по обычным
гардам). **Если этот PR не появится в ленте следующим тактом — пункт протух, и его надо
спросить с меня.**

## 6. Хвост открытых PR — прогнан, кандидатов нет

`gh pr list --state open` → четыре: #277 (этот), #278 (тред 122), #279 (тред 099), #280
(тред 064). Прогнала `merge-gate` по каждому: **все три чужих STOP на гарде 1** (нет вердикта
на текущей голове: `fc4f0c6`, `f67ea5f`, `7499bf1`), у #280 вдобавок `checks=IN_PROGRESS`, у
#278 и #280 ещё и гард 4 (`claude-review.yml`, `foreign-name-watch.yml`). Домерживать нечего, и
мержить их этим прогоном я всё равно не вправе: след гарда 5 обязан лечь в ленту ИХ тредов, а
этот прогон связан одним тредом 128. Молчания не оставляю — список назван.

## 7. Чей ход

**waiting-on → curator** (john в это поле не принимается; ход держу я, вопрос стоит первой
строкой). Парк на john: жду кнопки.

После нажатия след гарда 5 в этот тред кладу я — поимённо: вердикт, голова, чеки, чья рука.
Незакоммиченного у меня нет; работа исполнителя вся в `51e7345c`, ветка запушена.
