# Нарезка тега протокола и объявление его потребителю

participants: curator · dev-core · john · status: closed

## msg-001 · from: curator · 2026-09-07 · expects: answer

**Постановка по слову john (чат 2026-09-07 ~13:32Z: «закажи приоритетом тег протокола»). ВЫСОКИЙ ПРИОРИТЕТ. Предмет: нарезать тег с текущей вершины и объявить его контуру-потребителю.**

## 1. Зачем — цена промедления измерена, и она НЕ теоретическая

У потребителя **три починки, влитые здесь и до сих пор не доехавшие**, и каждая уже стоила простоя:

- **чтение исхода прогонов вместо недоступного источника** — без него механизм готовности не читает их PR **вовсе** (запрос падает на правах токена). Итог: `#482` простоял **шесть суток**, `#492` и `#506` — по суткам, и каждый двигался ручным толчком чатного curator;
- **сигнал о потере сливаемости** — у конфликта нет события; `#482` конфликтовал **пять раз**, и каждый раз это обнаруживалось глазами john, а не механизмом;
- **нота при отправке письма** — «письмо легло под пишущим»: их роль сорок секунд не знала, что вопрос уже отвечен, и заново заперла тред.

**Плюс всё, что село здесь за трое суток:** штатная разморозка, заморозка звонит в почту, доставку судит цель, охрана чтений, смотритель имени, лечащий порядок в звонке о дрейфе, минимальная версия systemd.

**Каждый день без тега — это ручные толчки моей рукой по их PR.**

## 2. Что сделать

1. **проверить, что резать есть с чего** — вершина `origin/main` зелена, `main` не красна, все объявленные пакеты в ней;
2. **нарезать тег** очередной версии с текущей вершины, по действующей норме именования;
3. **проверить рез двусторонним гардом** — тот, что заводился тредом `095`: тег не должен быть срезан с невлитой головы (случай `v0.2.8`) и не должен отставать;
4. **объявить тег потребителю** — по норме (б) треда `092`: получатель сам смотрит теги производителя, но объявление человеком остаётся, пока наблюдатель за тегами не отработал полем. **Доносит чатный curator** — из этого контура в чужую ленту роль не пишет.

## 3. Чего НЕ делать

- **не открывать PR с пином у потребителя** — это их зона и их рука;
- **не накатывать на ящик** — накатка идёт по их документу порядка (`136` их контура), рукой john;
- **не чинить ничего по дороге** — если при проверке §2.1 всплывёт краснота, назвать её фактом и остановиться: тег с красной вершины хуже отсутствия тега.

## 4. Что john знает про порядок дальше

После объявления: их роль открывает PR с пином → **кнопка john** (их док власти) → накатка на ящике по их документу, с учётом трёх сегодняшних поправок (перезагрузка не подтягивает чекаут; установка нужна только если двигался замок; выравнивание рабочих мест ролей обязательно).

**И нота, которую надо назвать в объявлении:** после накатки демон потребителя, оставшись на прежней сборке, ответит на новый конфиг «перезапусти» — это задуманный шаг миграции, а не дефект.

waiting-on → dev-core.

## msg-002 · from: curator · 2026-09-07 · expects: none

**ЭТОТ КАТАЛОГ НЕДЕЙСТВИТЕЛЕН. Предмет переехал: `158-cut-the-tag-for-the-consumer`. Сюда не писать.**

Номер `156` заняли дважды: **`156-daemon-self-restart`** (роль) — настоящий и остаётся; **`156-cut-the-tag-for-the-consumer`** (чатный curator, `13:32Z`) — этот, недействителен.

**Причина названа прямо:** чатный curator прочитал реестр, взял свободный номер и не перечитал его непосредственно перед созданием каталога. **Это ВТОРОЙ такой случай за сутки** — первый был `144` (перенесён в `147`), и после него правило «читать реестр непосредственно перед записью, а если между чтением и записью был любой другой вызов — перечитать» уже записано в его скилле. Правило не сработало: между чтением и созданием прошло больше десяти минут и несколько вызовов.

**Итого коллизий в контуре:** `048`, `055`, `144`, `156` — четыре. Тред с чужим номером теряется в реестре так же надёжно, как осиротевший.

Работы этот каталог не нёс: письмо было одно, перенесено в `158` целиком.

waiting-on → curator.

## msg-003 · from: curator · 2026-09-07 · expects: none

# Каталог закрываю: работы он не несёт, а открытым поднимает роль впустую

Ход пришёл сюда, потому что `msg-002` ушёл с `waiting-on → curator` и без парка — голый кандидат.
Работы в каталоге нет ни строчки: два письма, оба мои, второе — метка недействительности. Ниже —
чем это замерено, и куда уехало всё, что здесь стояло.

## 1. Предмет жив и поднимется сам — проверено чтением, а не памятью

- `158-cut-the-tag-for-the-consumer` — `status: open`, `msg-001` несёт постановку ЦЕЛИКОМ (все четыре
  пункта §2, оба запрета §3, порядок §4 и нота про «перезапусти» после накатки), `waiting-on → dev-core`.
  Ничего из `156` не потерялось: сверила пункт в пункт.
- `cli mail --role dev-core` (13:55Z) отдаёт очередь: `063`, `155`, `153`, `141`, **`158`**, `157`, `159`.
  То есть нарезка тега стоит в очереди dev-core как кандидат — её поднимет контур, а не моя рука.
- Настоящий `156` — `156-daemon-self-restart`, `status: closed`. Коллизия, названная в `msg-002`, реальна.

**Из этого следует, что закрытие этого каталога ничего не тормозит.** Высокий приоритет предмета
живёт в `158`; здесь остался бы только адрес, который врёт.

## 2. Коллизию номера здесь НЕ дублирую — у неё свой открытый предмет

`159-thread-number-has-no-door` — постановка по слову john (чат 13:51Z: «заводи»), `status: open`,
на dev-core. Она несёт ту же таблицу четырёх коллизий (`048`, `055`, `144`, `156`) и вывод, который
`msg-002` называл вскользь: дисциплиной не лечится, потому что правило скилла уже действовало, когда
случился `156`. Повторять это письмом в закрываемый каталог — множить адреса на один предмет.

## 3. Хвост merge перед передачей хода — замерен, кандидатов нет

`gh pr list --state open` даёт две PR, и обе — не мои кандидаты. Мерила дверью, а не глазом
(`pnpm protocol merge-gate --ref origin/main --pr <n> --review-workflow 'Claude PR Review'`, 13:56Z):

- **#323** «feat(park): парковка называет факт…» (тред `155`), голова `d00679f`:
  `STOP` гард 1 — нет `approve` на `d00679f`; `STOP` гард 2 — `checks=IN_PROGRESS`; гард 4 `ok`
  (12 путей, доков власти нет). Итог двери: `REFUSED`.
- **#322** «feat(orchestrator): «ждёт круг ревью»…» (тред `063`), голова `0e66f0f`:
  `STOP` гард 1 — нет `approve` на `0e66f0f`; `STOP` гард 2 — `checks=FAILURE`; **`STOP` гард 4 —
  дифф трогает `agent-protocol.json`, это кнопка john.** Итог двери: `REFUSED`. Дверь отдельно
  напомнила про `protocolVersion`: если бамп поедет, каждый ящик придётся подтянуть руками.

**Ни одна из двух не принадлежит этому треду**, поэтому след гарда 5 лёг бы не в ту ленту — метку не
вешаю и кнопку не жму. Факт называю здесь ровно затем, чтобы следующая сессия не перемеряла: на
13:56Z домерживать нечего, обе PR ждут своего зелёного и своего круга в СВОИХ тредах.

## 4. Что делаю прямо сейчас

`cli thread status … --status closed --write` на этом каталоге, сразу после этого письма. Рабочее
дерево роли чистое (`git status --porcelain` пуст), некоммиченного нет, тег не резала и не должна была.

`waiting-on → —`: действовать здесь некому и незачем. Кто ходит дальше — dev-core, но в `158`.
