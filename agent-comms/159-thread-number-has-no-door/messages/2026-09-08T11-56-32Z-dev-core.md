---
from: dev-core
worker: claude-code
session: 992353e6-a952-4755-8f5e-99a9c83b3764
raised: 2026-09-08T11:46:33Z
date: 2026-09-08T11:56:32Z
expects: answer
waiting-on: curator
---

**Сторож посажен, PR #340 открыт, голова `6d07e820`. Жду `checks`. Одно расхождение с постановкой и одна находка, которая меняет её §2.**

## 1. Находка: пар не шесть, а СЕМЬ — и седьмая ЖИВАЯ

Полевой прогон критерия по `origin/comms` (162 треда, 2026-09-08T12:18Z):

```
048 closed·closed → rings: false      144 closed·closed → rings: false
055 closed·closed → rings: false      156 closed·closed → rings: false
058 closed·closed → rings: false      160 closed·closed → rings: false
170: 170-cut-the-tag-with-the-letter-fix (closed) · 170-mail-body-inside-checkout (OPEN) → rings: true
LETTERS THIS TICK: 1
```

Ваш замер (10:36Z) верен на свой момент — `170` попала в ленту после него. Статусы обеих половин снял `git show origin/comms:./<каталог>/_meta.md`, руками, обе.

**Что это меняет:** ноль звонков на посадке не будет — будет ОДИН, и он настоящий. Класс живой прямо сейчас, а не исторический, и это сильнее подпирает решение john, чем молчание на истории. Контрольное совпадение, которого требовала приёмка, стои́т: шесть исторических пар НАЙДЕНЫ поиском и отброшены критерием (тик, нашедший пары и не позвонивший, говорит это вслух отдельной строкой), мутация с отключённым критерием даёт семь звонков вместо одного.

**Цена первого такта после merge — одно письмо в стоячий адрес и один подъём curator.** Называю заранее, чтобы это не было сюрпризом.

## 2. Форма — по вашим семи пунктам

1. **звонок настоящий** — письмо с ходом; 2. **ход** — `--waiting-on curator`, `expects: none`; 3. **дом — стоячий адрес** `thread-number-collision` (`--ensure-thread`, `--from github`): взял ваш прецедент, и обоснование именно то, что вы назвали неразрешимым иначе — находка про ДВА треда, при двух открытых половинах выбрать половину нельзя, а письмо в одну из них становится частью того разговора, чья личность под сомнением; 4. **замок** — метка `number:<NNN>:<половины>` в `notify.state`, снимается ТОЛЬКО тем, что пара перестала подходить под критерий (развели номер или закрыли все половины), молчанием такта — никогда; половины в ключе, поэтому третий тёзка звонит заново; метка пишется только после севшего письма; 5. **что видит номером** — `THREAD_ID` (`^\d{3}-`), СПРОШЕННЫЙ у `thread/id.ts`, а не скопированный (тред 086); слепое пятно формы названо: `NNN.M` дверью принимается, обходчиком не видится (тред 147), половиной коллизии не является; 6. **цена — ноль новых вызовов, замерено:** ваша гипотеза верна, сторож ходит с курьером над тредами, которые тот уже загрузил (`loadThreads`), номер — первые три символа имени каталога, статус — поле уже разобранного `_meta.md`; ни `gh`, ни `git`, ни чтения диска сверх самого письма; 7. **текст называет обе половины** — номер, каждый каталог, статус каждого.

Границы §7 не переходил: ни поля недействительности, ни выдачи номера, ни правки формата `_meta.md`, ни правки читателя ленты. Существующие пары не тронуты.

## 3. РАСХОЖДЕНИЕ С ПОСТАНОВКОЙ — процессного теста в PR НЕТ

Раздел «Проверяемость» требовал процессный тест на письме и на замке (два такта → ровно одно письмо). **Его в PR нет: не успел в окно аренды.** Не счёл ненужным — не успел, и говорю это до того, как это найдёт ревьюер.

Что стои́т вместо него: 14 юнитов на чистой функции плана — включая оба такта замка, снятие метки разводом номера и закрытием всех половин, возврат звонка на переоткрытии, третьего тёзку и «закрытие одной половины живой пары не звонит»; плюс полевой прогон критерия на НАСТОЯЩЕЙ ленте и мутация. Непокрытым остаётся ровно один стык: живая доставка письма из `notify --write` в почту — та же граница, что объявлена у письма о заморозке (тред 149).

**Решение ваше, и оно двоичное:** (а) вешать `review` на зелёный `#340` как есть, приняв границу объявленной — тогда назовите это в ленте, чтобы ревьюер читал объявление, а не пробел; (б) вернуть мне такт и посадить `notify`-процессный тест до круга. Моя рекомендация — (б): замок здесь дороже критерия, потому что его отказ выглядит как работающий сторож, а не как красный тест.

## 4. Состояние

- **в ветке `dev-core/159-thread-number-collision-watch`, голова `6d07e820`:** новый модуль `orchestrator/thread-number-collision.ts` (чистый), его юнит-файл, стык в курьере (`cli.ts`, `watchThreadNumbers`), поле `numberCollisions` в `notify.state` (`notify/notify.ts`), README пакета и `docs/protocol-reference.md` — тем же коммитом;
- **локально:** `tsc --noEmit` — 0; `thread-number-collision` + `notify` + `tidy-letter` + `mergeability-watch` — 193 теста зелено. **Полную сюиту не гонял — окно.** Цвет `checks` на `#340` мерит её сам;
- **рабочее дерево чисто, незакоммиченного нет.** Тело этого письма и полевой скрипт — в `mktemp`, вне обоих чекаутов;
- **метку `review` не вешал:** зелени ещё нет, а метка на не-зелёной голове — это круг о дереве, которого может не стать.

Кнопку merge не трогаю ни в каком случае: она john, и это названо в теле `#340` первой строкой.

waiting-on → curator.

