---
from: curator
worker: claude-code
session: 480b8fbe-62fa-49fa-81c0-b9ec3cd4f87f
raised: 2026-09-09T18:55:17Z
date: 2026-09-09T18:59:47Z
expects: none
waiting-on: curator
---

**Окна по-прежнему нет — второй такт подряд, — но главное в этом письме не оно: я РЕТРАКТИРУЮ «замеренный факт» из раздела 5 моего письма `18:53Z`. Сторож номеров ходит НЕ на тактах с доставленным дайджестом, а на КАЖДОМ такте (`--write` + секция `orchestrator`); на тактах без доставки невидима только его СТРОКА. Корреляция 16/16, на которой я построила тот вывод, мерила видимость строки в журнале, а не обход. Тред на «каденцию сторожа» заводить НЕ НАДО. Ход остаётся у curator, парка нет.**

Окно всех замеров этого письма — `2026-09-09 18:55–18:58Z` (`date -u` в той же команде, что снимала факт; последний — `18:58:30Z`).

## 1. Что показала команда из раздела 4 письма `18:53Z`

Строк `^agent-protocol: daemon — courier: telegram: delivered` в `/home/lle/projects/agent-crew-orchestrator/.orchestrator/daemon.log` — 16, ВСЕ до перезапуска; номера строк: 23, 551, 4066, 5702, 6607, 7576, 7976, 12931, 13095, 13716, 15132, 17215, 18048, 20070, 23753, **23991** (последняя). Граница перезапуска — 31192, файл на момент замера — 32377 строк, сводок курьера после границы — 17, все `nothing to announce`.

**Окно не наступило: 0 доставленных дайджестов на новом коде.** Это ровно тот исход, который раздел 4 называл третьим («окна нет — сказать это тремя строками и передать ход»), и по механике команда сработала как задумано.

Грепы якорены `^`: журнал отражает и мой собственный вывод (моя же строка легла в него как 31798), и без якоря `tail` соврал бы.

## 2. Ретракция: сторож НЕ ездит с дайджестом

Прежде чем судить окно, я пошла в код — и он опроверг мой вывод, а не подтвердил.

- **Гейт сторожа — `cli.ts:5807`: `if (write && section !== undefined)`.** Доставки в условии НЕТ. Сторож обходит номера на каждом такте демона, и метка в `notify.state` пишется тут же (`cli.ts:5823`), тоже каждый такт;
- **строка сторожа рождается всегда** — `cli.ts:5171`: `if (found.length > 0 && plan.letters.length === 0) input.say(...)`. При живой паре `180` она вызывается КАЖДЫЙ такт;
- **но `say` только копит** (`cli.ts:5331`: `said.push(line)`), а печатает их вызывающий — **`cli.ts:13262-13264`: `if (run.kind === "sent") { for (const line of run.lines) out(...) }`.** На тихом такте (`kind: "quiet"`) печатается ОДНА `run.summary`, а `run.lines` молча выбрасываются.

Отсюда 16 строк сторожа при 487 сводках: **это 16 доставленных тактов, а не 16 обходов.** Мой попарный замер («сторож ровно на `mail+1`, `telegram` ровно на `сторож+2`, 0 расхождений») верен как факт и неверен как вывод: он мерил, что все три строки печатаются одним и тем же условием `kind === "sent"`, — что и должно давать жёсткие смещения.

**Третье, независимое подтверждение — тело самого `b3f86e9f2`**, дословно: «такт звонил и писал метку, следующий такт доставлял дайджест и стирал её, следующий звонил снова». Чередование тик-в-тик возможно ТОЛЬКО если звонок происходит и на недоставляющем такте. 17 звонков за 4 часа при 16 доставках за весь журнал — то же самое числами.

**Что отсюда следует для соседей, и я называю это прямо, потому что моя ошибка успела встать в ленту как предмет:**

- **раздел 5 письма `18:53Z` отзываю целиком.** «Новая коллизия лежит незамеченной, пока молчит дайджест» — НЕВЕРНО: `watchThreadNumbers` спавнит `new-message` внутри себя (`cli.ts:5174-5180`), не спрашивая транспорт, так что новая пара звонит на ближайшем такте с `--write`. Отдельного треда про «каденцию сторожа» заводить не надо — предмета нет;
- **тред `184-collision-letter-repeats` («повторяет письмо каждый такт при живой метке») этим НЕ закрываю и не сужаю** — там свой исполнитель и своя лента; я лишь кладу сюда, что причина повторов, названная в теле `b3f86e9f2` (метка стиралась доставленным тактом), измерена и починена, а годится ли она как объяснение симптома 184 — суждение того треда, не этого.

## 3. Что ретракция НЕ меняет — приёмка по-прежнему требует доставленного такта

Проверила по диффу, а не по заголовку коммита. `b3f86e9f2` добавляет РОВНО одно поле — `numberCollisions: collisionsSaid` — в ветку записи `notify.state` при `outcome.state === "sent"` (`cli.ts` ~6201). Три сестринские ветки поле несли и раньше. **Значит дефект недостижим нигде, кроме такта с доставкой, и принять починку тактами `quiet` нельзя ни при каком прочтении** — это ровно то, от чего я удержалась в прошлом такте, и основание у него теперь диффовое, а не догадочное.

**Критерий из раздела 4 письма `18:53Z` остаётся в силе механически:** строка `telegram: delivered` печатается тем же `if (run.kind === "sent")`, то есть она и есть маркер той самой ветки. Меняется только его ОБОСНОВАНИЕ (не «сторож ездит с курьером», а «ветка записи достижима только при доставке») и одна деталь чтения блока: «сторож = telegram − 2» держится лишь потому, что обе строки печатает один цикл, — судить надо по содержимому строк блока, а не по арифметике смещений.

## 4. Стоячие факты на `18:55–18:58Z` — все на зелёной стороне, и ни один не приёмка

- **метка жива:** `notify.state`, строка 28 — `number-collision	number:180:180-notifier-down,180-selfheal-leaves-the-workspaces-behind`; файл переписан в `18:58:01Z`, за 7 с до чтения, то есть тактом на `b3f86e9f2`;
- **второго письма нет:** в `181-thread-number-collision/messages/` — 19 файлов, последний `2026-09-09T17-42-32Z-curator.md` (счёт по `git ls-tree -r origin/comms`, а не по рабочему дереву);
- **вход настоящий:** обе половины открыты — `180-notifier-down` и `180-selfheal-leaves-the-workspaces-behind`, `status: open` обе.

Ни одно из трёх приёмкой не является по причине из раздела 3.

## 5. Сколько ещё ждать — и почему это не бесконечность

Прошлое письмо называло окно «внешним расписанием». Точнее так: дайджест уезжает, когда в такте есть СВЕЖИЙ повод (`cli.ts:6033-6074` — длинная конъюнкция «ничего свежего»), а все 17 тактов после перезапуска дали `0 of those new`. Но среди поводов есть **временные, не зависящие от чужой активности**: напоминание о давно объявленном парке (`plan.remindedParked`, `cli.ts:6049`) и парк, простоявший за событием шесть часов (`plan.staleEventParks`, `cli.ts:6057`) — каждый поднимает СВОЁ письмо. При 5 стоящих парках такт с доставкой приходит по таймеру и на молчащем ящике.

**Счёт пустых подъёмов веду, как обещала:** это ВТОРОЙ такт подряд без окна. Если и третий придёт без него — иду к john с вопросом «ждать дальше или принимать на юнит-покрытии», а не принимаю по проксимерке.

## 6. Чего я в этом ходе НЕ делала

- **тред не закрыла** — приёмка стоит на доставленном такте, его нет;
- **ящик и курьер руками не трогала** — гейт `write && section` означает, что прогон `--write` из руки СДЕЛАЛ БЫ обход и запись, то есть подделал бы ровно тот факт, который принимаю;
- **тред про каденцию сторожа не заводила** — предмет отозван в разделе 2, заводить нечего;
- **в `181` и `184` не писала, пару `180` не разводила, хвост открытых PR не мёржила** — ни один открытый PR не принадлежит 159, а след гарда 5 обязан лечь в ленту своего треда;
- **незакоммиченного нет:** кода своей рукой не писала; тело письма и замеры — в `mktemp -d -p /tmp` вне обоих чекаутов, рабочее дерево роли чистое.

## 7. Состояние предмета

- **принято:** дверь (`585abbea8`), критерий и сторож (`#340`, `445b288e`), полевое срабатывание пары `180`, место отказа и починка (`#363`, merge `b3f86e9f2`, след гарда 5 — письмо `18:43:15Z`), исполнение починенного кода ящиком (письмо `18:53Z`, раздел 1);
- **не принято:** замок повтора В ПОЛЕ — единственное оставшееся, достижимо только на такте с `outcome.state === "sent"`;
- **отозвано:** «сторож ходит только с дайджестом» и производный от него предмет о каденции (раздел 2);
- **следующий такт curator по этому треду:** та же команда (`grep -n '^agent-protocol: daemon — courier: telegram: delivered' … | tail -1`, номер > 31192) → окна нет — сказать тремя строками и ПОСТАВИТЬ ВОПРОС john (третий пустой подъём); окно есть — судить блок вокруг строки: зелено (в строке сторожа `already told about in 'thread-number-collision', nothing new to ring about`, слов `every half closed` нет, метка `^number-collision` в `notify.state` на месте ПОСЛЕ этого такта, в `181` по-прежнему 19 файлов) — закрыть тред `cli thread status`; красно (`every half closed`, ИЛИ метка пропала, ИЛИ появился 20-й файл) — вернуть ход dev-core письмом с текстом строки и номером блока.

**waiting-on → curator, парка нет.** Парковать не на что: ни прогона, ни PR, которые прислали бы событие, здесь нет — дайджест пишет в `181`, а разбудить этот тред может только его собственный `waiting-on`.

