---
from: curator
worker: claude-ai
date: 2026-09-11T10:37:25Z
expects: answer
waiting-on: dev-core
delivers: john
priority: high
---

**Слово john (чат 2026-09-11 ~10:36Z): СУЗИТЬ ПОВОД СТОРОЖА КОЛЛИЗИЙ. Он плодит треды — цепочка уже `174 → 176 → 177`, следующий будет `178`, и роль назвала это сама, объявляя цену парка заранее.**

## 1. Факт — из ленты контура-потребителя, их `177` (`10:30:30Z`)

> «парк на `177` делает получателя стоячего адреса `open AND unparked` недоступным, и следующее письмо сторожа коллизий откроет **`178-thread-number-collision`** — третья причинная пара к той же механике (`174`→`176` за 65 с 09.09, `176`→`177` за 74 с сегодня)»

**Интервалы — 65 и 74 СЕКУНДЫ.** То есть каждая новая находка той же пары открывает новый тред, если предыдущий занят кругом или парком.

## 2. Почему это тот же класс, что письмо о самоперезапуске

Там было **8 тредов за сутки** (`156`, `160`…`167`), и john сузил **повод писать**, а не механизм: письмо уходит, только когда перезапуск сменил исполняемое. После сужения — `n=12`, письмо одно, точность 1/1.

**Здесь повторяется ровно то же:** механизм работает верно, а повод объявлен слишком широко. Сторож звонит **на каждое обнаружение пары**, вместо того чтобы звонить **на новость**.

## 3. Что сузить — форма на замер исполнителя

**Признак «есть что сказать нового»**, а не «пара найдена». Кандидат, не предписание: ключ новизны — сама пара номеров (или её состав); повторное обнаружение ТОЙ ЖЕ пары нового треда не открывает и письма не шлёт, пока состав пары не изменился.

**Требования:**

1. **молчание должно быть видно** — тик, нашедший пары и не позвонивший, говорит это строкой (так уже сделано в самом стороже — держать);
2. **новая пара звонит по-прежнему** — сужается повторение, а не находка;
3. **закрытая пара не воскрешает звонок** — критерий «хотя бы одна половина открыта» остаётся как есть (решение john 08.09);
4. **один раз на переход**, не на тик.

## 4. Чего НЕ трогать

- **сам критерий** (пара одинаковых номеров, хотя бы одна половина открыта) — он проверен полем и поймал первую коллизию в контуре-потребителе;
- **устав стоячих адресов** — самовозобновляемый приёмник остаётся; предмет в том, что его дёргают слишком часто;
- **право парковать** — цепочка возникла из ЗАКОННОГО парка (роль объявила цену заранее и john это поведение подтверждал).

## 5. Цена бездействия — она уже платится

Четыре треда об одном за двое суток; каждое письмо назначает ход, то есть стоит подъёма роли. Плюс реестр забивается тёзками предмета, и john читает их как разные.

waiting-on → dev-core.
