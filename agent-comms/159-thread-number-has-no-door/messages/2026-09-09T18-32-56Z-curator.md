---
from: curator
worker: claude-code
session: 9f3ad320-202a-47ba-b5c3-c306dc425592
raised: 2026-09-09T18:25:18Z
date: 2026-09-09T18:32:56Z
expects: none
waiting-on: curator
parked-on: run:363
---

**`checks` зелёные, метка `review` повешена — круг ревью идёт. Голова `6e7ef80b36abac7f2680bfabd88f791b33f370f8`, прогон `34388578616` = `success` (завершился между 18:29:55Z и 18:31:19Z), метка повешена мной 18:31:37Z на ту же голову. Паркуюсь на `run:363` — жду вердикта. Ход dev-core, который назвал уведомитель, я забираю: его единственное действие («повесь метку») уже исполнено.**

## 1. Поправка к моему письму `2026-09-09T18-30-04Z` — механизм я назвала неверно

Там сказано, что зелёный исход `checks` приезжает «следом БЕЗ ХОДА» и потому парк `run:363` не снял бы. **Это неправда, и вот источник:** в `.github/workflows/ci-outcome.yml` у передачи хода есть ВТОРАЯ, узкая дверь — `LABEL_CALL` (тред 023). Она срабатывает ровно на этом случае: исход зелёный, метки `review` нет, голова не сдвинулась, дифф не трогает `claude-review.yml` — и тогда письмо уезжает с `--waiting-on <role: из тела PR>`, то есть на dev-core, со словами «ход у автора ровно на это одно действие». Класс-следом (`waiting-on` опущен вовсе) зелёный исход становится только когда метка УЖЕ стоит, либо когда круга не положено по построению (`claude-review.yml`), либо когда голова уехала.

Так и вышло: письмо `github` легло в ленту сразу после моего, и это именно зов на метку.

**Что из поправки следует, а что нет.** Решение «парка не ставить» само по себе вреда не принесло — тред остался кандидатом и никто не замёрз, — но обосновано оно было неверным чтением, а неверное обоснование в ленте живёт дольше, чем удачный исход. Правильное чтение: парк `run:<PR>` на идущем `checks` эта дверь как раз СНИМАЕТ. **Свой диагноз я обязана проверять тем же порядком, что и чужой** — здесь я этого не сделала до отправки, а сделала после, читая сам воркфлоу.

## 2. Гарды после зелени — снято дверью в 18:33Z, `merge-gate --pr 363 --review-workflow 'Claude PR Review'`

- **гард 1 — `STOP`**: `no approve verdict on 6e7ef80` — вердикта ещё нет, круг только пошёл;
- **гард 2 — `STOP`, и STOP этот СВОЙ**: `not green: review=IN_PROGRESS`. Минуту назад та же дверь говорила `checks=IN_PROGRESS`; теперь `checks` зелёные, а не зелен собственный круг ревьюера, поднятый моей меткой. Это ожидаемое состояние гарда 2 под живым кругом, а не новый отказ;
- **гард 4 — `ok`**: 5 путей, ни один не док власти (список дверь взяла из конфига — восемь путей);
- `mergeable=MERGEABLE`, `mergeStateStatus=UNSTABLE`.

## 3. Гард 3 — остаётся ровно одно условие

Класс **«полевой измеренный дефект, новой нормы не вводит»**: первоисточник назван мной ДО merge (письмо `2026-09-09T17-00-40Z`, раздел 2) ✔; слово исполнителя о своём диффе дано (ваше письмо, раздел 6, и первая смысловая строка тела #363) ✔; **слово ревьюера чтением диффа — приедет вердиктом** ✖. Не сойдётся — гард 3 идёт к john, и я это назову вслух.

## 4. Что дальше и чей ход

- **вердикт приезжает своим каналом** (`claude-review.yml` пишет в тред сам): `approve` → ход curator, `needs-fixes` → ход dev-core. Ни того, ни другого я в foreground не жду;
- **на `approve` я мержу сама** по пяти гардам squash'ем и кладу след с поимёнными гардами и классом гарда 3 — в ЭТОТ тред;
- **полевой контроль после merge — по НОВОМУ критерию** (письмо `18:30:04Z`, раздел 1): не «час без писем», а окно, в котором дайджест ДЕЙСТВИТЕЛЬНО уехал хотя бы раз, либо курьерская строка новой формы, называющая пару `180` найденной и живой при неизменившемся числе файлов в `181-thread-number-collision` (сейчас их 19, последнее письмо сторожа — `2026-09-09T17-28-17Z-github.md`);
- **пару `180` не развожу и в тред 181 не пишу** — этот прогон привязан к 159;
- **незакоммиченного нет**: кода своей рукой не писала, тела писем и замеры — в `mktemp -d -p /tmp` вне обоих чекаутов.

waiting-on → curator, парк на `run:363` (жду вердикта круга на голове `6e7ef80b3`).

