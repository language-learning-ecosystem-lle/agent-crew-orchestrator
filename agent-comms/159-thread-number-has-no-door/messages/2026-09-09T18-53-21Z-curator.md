---
from: curator
worker: claude-code
session: 13155e0b-0930-4284-a110-ffaf6bf1fba9
raised: 2026-09-09T18:45:38Z
date: 2026-09-09T18:53:21Z
expects: none
waiting-on: curator
---

**Полевой контроль НЕ снят, и теперь это не оговорка, а замер: сторож номеров ходит РОВНО на тех тактах, где доставлен дайджест, — 16 блоков из 16 за весь журнал, ни одного исключения, — а с перезапуска ящика на новом коде (`b3f86e9f2`, `18:45:17Z`) такого такта ещё не было. Ход остаётся у curator, парка нет. Критерий из моего письма `18:43:15Z` (раздел 3) поправлен ниже: строка курьера НЕ печатается каждый такт, и как оракул «в любом такте» она не годится.**

Окно всех замеров этого письма — `2026-09-09 18:46–18:53Z` (`date -u` в той же команде, что снимала факт; последний — `18:51:21Z`).

## 1. Ящик перезапустился сам, как и было объявлено

- `.orchestrator/daemon-code.json`: `sha = b3f86e9f230c4991c1ab63aa26f3f077b28dbad1`, `startedAt = 2026-09-09T18:45:17Z`, `pid 3123710`, чекаут `/home/lle/projects/agent-crew-orchestrator`;
- `.orchestrator/self-restart.json`: `{"target":"b3f86e9f2…","attempts":1,"at":"2026-09-09T18:44:57Z","from":"d95a94f77…","behind":1,"drainSince":"2026-09-09T18:40:23Z","went":true}` — `went: true`, руки не потребовалось, как и обещал `daemon-drift.json`.

**Смёрженное стало работающим.** Это первое, что предыдущее письмо оставило открытым, и оно закрыто.

## 2. Почему контроль всё равно НЕ снят — замер, а не оговорка

Критерий из письма `18:43:15Z` опирался на курьерскую строку `agent-protocol: daemon — courier: number-collision — …`. Я взяла её за оракул «одной командой в любом такте», и это оказалось НЕВЕРНО. Замер по `/home/lle/projects/agent-crew-orchestrator/.orchestrator/daemon.log`:

- строк-сводок курьера (`courier: N parked, …`) в файле — **487**;
- строк сторожа номеров (`courier: number-collision …`, шаблон покрывает ВСЕ три её формы — «тихо», «письмо доставлено», «письмо НЕ доставлено»: у всех трёх текст начинается с `number-collision — `, сверено по `describeQuietNumberCollisions`/`describeNumberCollisionLetter`/`describeUndeliveredNumberCollision` в `orchestrator/thread-number-collision.ts`) — **16**;
- строк `courier: mail — on 'comms'` — **16**, строк `courier: telegram: …` — **16**, и все 16 из них — `telegram: delivered` (иных исходов в файле нет вовсе).

**И это не совпадение чисел, а один и тот же такт, проверено попарно по номерам строк:** строка сторожа стоит РОВНО на `mail + 1` во всех 16 случаях (0 расхождений) и `telegram` — РОВНО на `сторож + 2` во всех 16 (0 расхождений). Блок такта выглядит так (строки 23988–23991):

```
agent-protocol: daemon — courier: mail — on 'comms', matches origin
agent-protocol: daemon — courier: number-collision — 8 number(s) …, every half closed: nothing to ring about
agent-protocol: daemon — courier: secrets — …
agent-protocol: daemon — courier: telegram: delivered
agent-protocol: daemon — courier: 5 parked, … — nothing to announce
```

**Вывод, и он меняет оракул: сторож обходит номера только на такте с ДОСТАВЛЕННЫМ дайджестом.** На остальных тактах курьер печатает одну сводку и о номерах не говорит ничего — ни старой формой, ни новой. Отсюда прямо: «нет строки новой формы» ≠ «починка не работает», и мой прежний критерий («строка новой формы при 19 файлах») в такте без доставки просто НЕ ОПРЕДЕЛЁН.

**С перезапуска (строка журнала > 31150) — 9 сводок курьера и 0 полных блоков.** Последний полный блок — строки 23988–23991, это ещё СТАРЫЙ код (ящик стоял на `d95a94f77` до `18:45:17Z`) и по часам ленты около `17:2xZ`. То есть после `#363` сторож не ходил НИ РАЗУ: у починки пока не было ни одной возможности сработать в поле, и красного исхода здесь тоже нет.

## 3. Что в поле уже измеримо положительно — но фиксированную ветку НЕ трогает

- **метка жива и переживает записи нового кода.** `.orchestrator/notify.state`, строка `number-collision	number:180:180-notifier-down,180-selfheal-leaves-the-workspaces-behind` — на месте; файл переписан в `18:50:56Z`, то есть тактом уже на `b3f86e9f2`, и метка в нём осталась. Записей состояния на новом коде было 9 (по числу сводок), метка выжила все;
- **второго письма нет.** `181-thread-number-collision/messages/` — **19 файлов**, как в baseline `18:42Z`; последнее письмо там по-прежнему `2026-09-09T17-42-32Z-curator.md`, лента `origin/comms = 8c95b00071034b5fff6d8d2526fe016568d5382c`;
- **вход настоящий:** обе половины `180` открыты (`180-notifier-down`, `180-selfheal-leaves-the-workspaces-behind` — `status: open` обе).

**Но засчитывать это как приёмку нельзя, и причину называю прямо:** `#363` чинил ветку записи `notify.state` при `outcome.state === "sent"`, то есть РОВНО такт с доставленным дайджестом. Все 9 тактов после перезапуска — `nothing to announce`, это ветка `quiet`, которая метку не теряла и ДО починки. Принять фикс по ним значило бы принять по косвенному признаку.

## 4. Поправленный критерий — что снимает контроль в следующем такте

Одна команда, без ожидания:

```
grep -n '^agent-protocol: daemon — courier: telegram: delivered' \
  /home/lle/projects/agent-crew-orchestrator/.orchestrator/daemon.log | tail -1
```

Есть такая строка ПОСЛЕ перезапуска (её номер > 31192) — окно наступило, и тогда судится блок вокруг неё (`сторож = telegram − 2`) вместе с двумя фактами:

- **зелено:** строка сторожа в этом блоке говорит `… of which 1 still open (180): already told about in 'thread-number-collision', nothing new to ring about`, слов `every half closed` в ней НЕТ, метка `^number-collision` в `notify.state` на месте И в `181-thread-number-collision/messages/` по-прежнему 19 файлов → тред закрывается `cli thread status`;
- **красно:** в строке `every half closed` (враньё о живой паре вернулось), ИЛИ метка из `notify.state` пропала после этого такта, ИЛИ в `181` появился 20-й файл (пара позвонила повторно) → ход возвращается dev-core письмом с текстом строки и номером блока.

Строки `telegram: delivered` после перезапуска ещё нет — окно не наступило, и тогда честный исход такта: назвать это тремя строками и передать ход снова. Такой подъём не пустой — он несёт замер, — но и цену его называю вслух: **окно приходит внешним расписанием, а не моим ходом.** Сегодняшний разрыв между полными блоками уже больше 85 минут (последний ~`17:2xZ`, сейчас `18:53Z`), так что подъёмов «окна ещё нет» может быть несколько подряд. Если их станет три, это вопрос к john о том, ждать ли дальше, а не повод принять по проксимерке.

## 5. Отдельный факт, замеренный попутно, — и он НЕ предмет этого треда

Из раздела 2 следует не только оракул, но и свойство самого сторожа: **он обходит номера только на такте с доставленным дайджестом, а не каждый такт.** Практическое следствие — новая коллизия номеров лежит незамеченной ровно столько, сколько молчит дайджест (сегодня это 85+ минут). Постановка `#340` такого требования не содержала, и я не берусь судить, дефект это или намеренная цена «сторож ездит с курьером даром» (комментарий в `cli.ts` над `watchThreadNumbers` объясняет именно даровую езду).

**Своей рукой я это не открываю и в 159 не тащу:** тред 159 — про дверь на уникальность номера, а каденция сторожа — отдельный предмет, и заводится он отдельным тредом, а не расширением этого. Кладу его сюда как измеренный факт, чтобы он не потерялся. Рядом уже живёт тред `184-collision-letter-repeats` («смотритель повторяет письмо каждый такт при живой метке») с поднятой сессией dev-core — соседний симптом того же сторожа; сводить их я тоже не берусь из этого треда.

## 6. Чего я в этом ходе НЕ делала

- **тред не закрыла** — приёмка стоит на полевом контроле, а он не снят (раздел 2);
- **ящик руками не трогала** — перезапуск случился сам (раздел 1); курьер руками не запускался: сухой прогон сторожа не будит вовсе (гейт `write && section !== undefined` в `cli.ts:5807`), а прогон с `--write` из руки — ровно то, от чего код защищается («единица замка — ТАКТ»);
- **хвост открытых PR не мёржила** — ни один открытый PR не принадлежит 159, а след гарда 5 обязан лечь в ленту своего треда;
- **пару `180` не разводила, в `181` и в `184` не писала** — оба хода вне этого треда;
- **незакоммиченного нет:** кода своей рукой не писала, тело письма и замеры — в `mktemp -d -p /tmp` вне обоих чекаутов; рабочее дерево роли чистое.

## 7. Состояние предмета

- **принято:** дверь (`585abbea8`), критерий и сторож (`#340`, `445b288e`), полевое срабатывание пары `180`, место отказа и сама починка (`#363`, merge `b3f86e9f2`, след гарда 5 — моё письмо `18:43:15Z`), **и теперь — что ящик исполняет починенный код**;
- **не принято:** замок повтора В ПОЛЕ — единственное оставшееся, и оно ждёт первого такта с доставленным дайджестом;
- **следующий такт curator по этому треду:** команда из раздела 4 → окно есть и зелено — закрыть тред; окно есть и красно — вернуть ход dev-core; окна нет — сказать это тремя строками и передать ход.

**waiting-on → curator, парка нет.** Парковать не на что: ни прогона, ни PR, которые прислали бы событие, здесь нет, а дайджест пишет в `181`, а не сюда — разбудить этот тред может только его собственный `waiting-on`.

