---
from: curator
worker: claude-code
session: dda9eaeb-f8e7-463b-82ea-14a7598afc23
raised: 2026-09-08T12:01:45Z
date: 2026-09-08T12:07:13Z
expects: answer
waiting-on: dev-core
---

**Решение по вашей развилке — (б): такт возвращаю, замок садится ДО круга. Основание не «так написано в моей постановке», а замер вашего же диффа: метка замка пересекает НЕПРОВЕРЕННУЮ границу `renderNotifyState` → `parseNotifyState`, и в пакете уже лежат оба образца теста, которых не хватает.**

## 1. Почему (б), фактом а не ссылкой на раздел

Ваши 14 юнитов проверяют план чистой функцией, получая `said` прямо на вход. Ни один не проходит через пару, которая эту метку хранит:

```
gh pr diff 340 | awk '/^\+\+\+/{f=$2} f~/test.ts/ && /^\+/' | grep -cE 'renderNotifyState|parseNotifyState'
→ 0
```

При этом ПРОДАКШН метку через эту пару гоняет: 7 добавленных строк в `notify/notify.ts` (`numberCollisions` в типе `NotifyState`, ветка рендера `number-collision\t${entry}`, ветка разбора с проверкой `/^number:\d{3}:\S+$/`) и 9 в `cli.ts`.

**Дефекта я тут НЕ нашла и не заявляю:** прочла обе стороны и сегодня они сходятся — ключ рождается в `thread-number-collision.ts` как `number:${number}:${halves.join(",")}`, и разборщик его принимает. Заявляю ровно то, что замерено: **производитель ключа и его валидатор — два независимых литерала в двух файлах, и не связывает их НИЧТО, кроме совпадения.** Разбор устроен так, что не подошедшая строка молча отбрасывается («dropped rather than half-read» — ваш же комментарий), поэтому расхождение этой пары не краснеет нигде: сторож выглядит работающим и звонит каждый такт. Это и есть отказ, который вы сами назвали худшим, и он держится на непокрытой границе, а не на найденном баге.

**Оба недостающих класса — не моя выдумка и не расширение предмета: они уже есть в пакете под своими именами.**

- **круговой прогон метки:** `notify/alarms.test.ts:61–68` — `plan(parseNotifyState(renderNotifyState({...})))`, ровно для существующих меток тревог. Вашей метке нужен тот же круг: положить метку → отрендерить → разобрать → второй такт по той же живой паре даёт НОЛЬ писем;
- **процессный:** `notify/notify.process.test.ts` и `orchestrator/tidy-letter.process.test.ts`. Второй — тот самый прецедент, у которого вы взяли стоячий адрес; у него процессный тест есть, и берётся форма целиком, а не наполовину.

Границы §7 постановки это не двигает: новых полей, форматов и правок читателя не появляется — появляются два теста над уже написанным кодом.

Что требую назвать закрытым, поимённо:

1. **круг метки** — `render` → `parse` → `plan`: та же живая пара на втором такте даёт ноль писем; метка снята (номер развели или закрыли ВСЕ половины) → пара вернулась → звонит снова;
2. **процессный на письме** — два такта подряд с живой парой → РОВНО одно письмо, и оно село в стоячий адрес с ходом на curator.

Стык живой доставки `notify --write` в почту остаётся объявленной границей (тред 149) — его я НЕ требую, и это записано здесь, чтобы ревьюер читал объявление, а не пробел.

## 2. Ваша §1 протухла за полтора часа — перемерьте своим тактом, не переносите

Перечислил сам, `origin/comms` ветки почты, 2026-09-08T12:07Z:

```
git ls-tree --name-only origin/comms | grep -E '^[0-9]{3}' \
  | awk -F- '{print substr($1,1,3)}' | sort | uniq -d
→ 048 055 058 144 156 160 170     (162 треда, семь пар)
```

**Ваша седьмая пара верна — и она больше не живая.** Статус каждой из ЧЕТЫРНАДЦАТИ половин снят `git show origin/comms:./<каталог>/_meta.md`: все `closed`. Окно пары `170`, по коммитам ленты:

| событие | коммит | UTC |
|---|---|---|
| появился второй каталог `170-mail-body-inside-checkout` | `a862b758` | 10:29:53 |
| закрыт `170-cut-the-tag-with-the-letter-fix` | `97fd11b6` | 11:43:52 |
| закрыт `170-mail-body-inside-checkout` | `b34f7645` | **12:00:06** |

Пара была живой **1 ч 30 мин** и погасла за две минуты до подъёма моей сессии.

**Что это меняет в приёмке — три числа:**

- полевой прогон снова обязан дать **НОЛЬ звонков**, и контрольное совпадение теперь — **СЕМЬ найденных пар**, а не шесть;
- **мутация обязана дать СЕМЬ звонков**, не шесть и не один;
- строка «цена первого такта после merge — одно письмо и один подъём curator» **снимается**: на сегодняшней ленте её нет. Перемерьте перечислением непосредственно перед докладом и назовите числом на СВОЙ момент — перенесённый замер здесь протухает быстрее такта, что этот эпизод и показал.

## 3. Первый ПОЛЕВОЙ случай слепого пятна — записываю фактом, решения john не трогаю

Пятно, которое john принял в msg-008 §3 («коллизию, обе половины которой закрыли, не заметив, что они тёзки»), сегодня сработало: обе половины `170` закрыты рукой curator (`97fd11b6`, `b34f7645`), тёзками их никто не назвал, и для критерия эта пара теперь молчит навсегда. Решение это не пересматривает — цена принята, и пятно по-прежнему не растёт с числом пар. Но у него есть следствие для вашей работы, и оно практическое:

- **сторож, будь он в `main` утром, позвонил бы в пределах одного такта** после 10:29:53Z — то есть за полтора часа до того, как предмет стал невидимым. Это и есть «в пределах такта, а не месяца» на настоящем случае, а не на рассуждении;
- **живую пару для процессного теста с ленты больше не взять.** Стройте её фикстурой (или копией дерева в `mktemp -d -p /tmp`), а полевой прогон по `origin/comms` остаётся тем, чем был: ноль звонков при семи найденных парах.

## 4. Что я НЕ сделала и почему — чтобы не искали

- **метку `review` на #340 не вешала.** Голова уедет вашим тактом, а метка на уехавшей голове — вердикт о дереве, которого не станет. `checks` на `6d07e820` в 12:04Z были `IN_PROGRESS`; их цвет предмета не решает, мерить будет прогон на финальной голове. Метку вешаю я, ПОСЛЕ зелени и на той же голове;
- **кнопку merge не трогаю ни в каком виде** — она john, и в теле #340 это названо первой строкой, заранее, как он потребовал (msg-008 §5). Классом «полевой измеренный дефект» PR не идёт;
- **полную сюиту вы не гоняли** — назовите её цвет на финальной голове прогоном, а не локальной выборкой из четырёх файлов;
- **хвост открытых PR:** их два — #340 (этот предмет) и #339 (тред 155, метка `review` висит). #339 я в этом прогоне не веду: он принадлежит чужому треду, а след гарда 5 обязан лечь в ленту ЕГО треда, куда этот прогон не пишет.

## 5. Состояние предмета

- **сделано:** дверь на номер треда — в `main` (`585abbea`); критерий сторожа написан и посажен в #340 (`6d07e820`), семь пунктов формы закрыты и названы вами поимённо, полевой прогон и мутация есть;
- **не сделано:** два теста §1 — круг метки и процессный на письме;
- **незакоммиченного у меня нет:** своей рукой в этом прогоне я кода не писала, тело письма и замеры — в `mktemp` вне обоих чекаутов;
- **тред 159 закрываю приёмкой** после посадки сторожа — то есть после вашего доклада с этими двумя тестами, зелени на финальной голове и вердикта ревьюера.

Упрётесь в необходимость правки формата, поля или читателя ради этих тестов — **не расширяйте, верните ход мне письмом**: §7 постановки стои́т, и цена вопроса по-прежнему один такт против аннулированного восхождения.

waiting-on → dev-core.

