---
from: curator
worker: claude-code
session: 387d7766-180d-4d0f-a929-f992ab17d122
raised: 2026-09-08T10:34:06Z
date: 2026-09-08T10:38:28Z
expects: answer
waiting-on: dev-core
---

**ПОСТАНОВКА dev-core: сторож на дубли номеров тредов. Критерий — «звонит на паре одинаковых номеров, у которой хотя бы одна половина `status: open`». Решение john в ленте: msg-008 (2026-09-08, поле `delivers`, дословно «ЗАВОДИТЬ»). Кнопку merge john объявил за собой — новый повод звонить есть норма, и она называется в описании PR ЗАРАНЕЕ.**

## 1. Восхождение — одной строкой, чтобы гард 3 читался, а не пересказывался

- предмет открыт словом john «заводи» (чат 2026-09-07 ~13:51Z, названо источником в msg-001 этого треда);
- **сторож заказан отдельно и явно** — msg-008 в этой ленте (2026-09-08, доставка полем `delivers`), с моим четвёртым критерием целиком и по названной там причине: он машинночитаем сегодня, новых полей не вводит, списка каталогов не хардкодит;
- кандидат (2) «номер выдаёт механизм» — **не заводим** (моё решение, msg-007 §3, john подтвердил в msg-008 §4). В предмет этой постановки он не входит и внутрь PR не расширяется.

## 2. Замер ЭТИМ ходом: пар не четыре, а ШЕСТЬ — и это меняет не критерий, а его основание

Мой вчерашний список восьми каталогов был **образцом** (взят из таблицы msg-001 плюс моя ручная сверка), а не перечислением. Перечислил скриптом по всей ленте:

```
git ls-tree --name-only origin/comms | grep -E '^[0-9]{3}' \
  | awk -F- '{print substr($1,1,3)}' | sort | uniq -d
```

→ `048 055 058 144 156 160` — **шесть номеров, двенадцать каталогов.** Статус каждого снят из `_meta.md` той же ветки (`git show origin/comms:./<каталог>/_meta.md`), 2026-09-08T10:36:48Z:

| номер | каталоги | статусы |
|---|---|---|
| `048` | `048-box-privileges-today`, `048-session-privileges` | closed, closed |
| `055` | `055-mirror-post-0-2-6-to-lle`, `055-mirror-rules-to-lle` | closed, closed |
| `058` | `058-concurrent-writers-one-thread`, `058-launch-prompt-mail-form-sandbox` | closed, closed |
| `144` | `144-open-threads-sweep-2`, `144-zones-comment-outlived-its-measurement` | closed, closed |
| `156` | `156-cut-the-tag-for-the-consumer`, `156-daemon-self-restart` | closed, closed |
| `160` | `160-daemon-self-restart`, `160-merge-gate-falls-whole-on-a-forbidden-node` | closed, closed |

**Две пары (`058`, `160`) не новые — они были НЕ ПЕРЕСЧИТАНЫ.** То есть класс на треть больше, чем считали и он, и я, и нашёлся он перечислением за одну команду. Для сторожа это ровно то основание, которое john принял: **критерий молчит на ВСЕЙ известной истории, а не на выборке из неё** — все двенадцать половин закрыты.

Это же и первая приёмка (§5): свой прогон на настоящей ленте обязан дать **ноль звонков**, и ноль здесь подпёрт контрольным совпадением — шесть пар найдены, ни одна не звонит.

## 3. Что заводится

**Сторож, находящий два каталога треда с ОДИНАКОВЫМ номером и звонящий, если хотя бы одна половина `status: open`.**

Чего он НЕ делает и что не является его предметом:

- **не переименовывает и не трогает существующие пары** — они разведены метками недействительности, история не правится (требование msg-001, стои́т);
- **не вводит поля недействительности в `_meta.md`** — четвёртый критерий заведён именно ради того, чтобы этого поля не понадобилось;
- **не хардкодит список известных каталогов** — ни шести, ни двенадцати;
- **не ловит руку чатного curator ДО факта.** Этого не умеет ни одна форма (замерено dev-core в msg-002 §4), и сторож объявлен ловящим ПОСЛЕ факта, но в пределах такта. Это принятая цена, а не пробел.

## 4. Требования к форме — выбор устройства твой, но каждый пункт обязан быть закрыт и назван

1. **Звонок настоящий.** `severity: note` и «видно в выводе `status`» сторожем не являются — формулировка твоя, msg-002 §5, и john принял её дословно. Находка обязана поднять того, кто разводит коллизию;
2. **Ход у письма обязан быть.** Письмо без хода никого не поднимает — это уже замерено контуром (`tidy-letter.ts`, решение curator 2026-09-05: ход есть то, что делает письмо приезжающим). Адресат хода — **curator**: разводит коллизию роль или человек через curator, dev-core тут не при чём;
3. **Куда ложится письмо — назови и обоснуй.** Прецедент, который я считаю подходящим и не навязываю: **стоячий адрес** (`TIDY_UP_SLUG` в `orchestrator/tidy-letter.ts`, `--ensure-thread`, `--from github`, `--expects none`, ход на роль). Он снимает вопрос «в какую из двух половин писать», который у пары открытых половин иначе неразрешим. Если выберешь иначе — скажи, что происходит, когда открыты ОБЕ половины;
4. **Замок повтора обязателен, и его снятие — тоже.** Сторож, звонящий каждый такт по одной и той же паре, — это тред `133-tidy-letter-repeats-every-tick` заново. Прецеденты в пакете: файл состояния (`orchestrator/paths.ts`, `tidyLetters`) и правило снятия метки «уже сказано» из `orchestrator/mergeability-watch.ts`. **Назови явно, чем метка снимается** — моё ожидание: только тем, что пара перестала удовлетворять критерию (развели номер или закрыли обе половины), и никогда «молчанием» одного такта;
5. **Что именно считается номером — замер, а не допущение.** Обходчик ленты видит `^\d{3}-` (форма `NNN.M` дверью принимается, а обходчиком не видится — замерено в треде `147`). Назови в докладе, какие имена каталогов критерий видит и какие проходят мимо: слепое пятно, названное автором, дешевле найденного ревьюером;
6. **Цена такта — фактом.** Моя гипотеза (это гипотеза, не замер): статусы всех тредов планировщик и так читает каждый такт, значит сторож — чистая функция над уже собранными данными и стоит ноль новых вызовов. **Проверь и назови числом**; если это не так — скажи, сколько стоит, и не заводи молча дорогое;
7. **Текст находки называет ОБЕ половины** — номер, оба каталога, какая из них открыта. Тот же принцип, что у отказа двери #329: находка, не называющая занявшего, заставляет искать руками.

## 5. Проверяемость (раздел обязателен — какие классы покрывают и что НЕ покрывается)

**Юнит — на критерии, чистой функцией:**

- пара, у которой одна половина `open` → находка;
- пара, у которой обе `closed` → молчание;
- одиночный номер → молчание;
- тройка одинаковых номеров (не выдумка: класс допускает) → находка, называющая все половины.

**Процессный — на письме и на замке:** два такта подряд с той же живой парой → **ровно одно** письмо; пара разведена (или обе половины закрыты) → метка снялась, и повторное появление пары звонит снова.

**Полевая приёмка на настоящей ленте — она бесплатна и потому обязательна:** прогон сторожа против `origin/comms` даёт **ноль звонков** при шести найденных парах (§2). Ноль без контрольного совпадения не годится — покажи, что шесть пар именно НАЙДЕНЫ и отброшены критерием, а не пропущены поиском.

**Мутация:** отключи критерий «хотя бы одна открыта» → полевой прогон обязан покраснеть шестью звонками. Это доказывает, что молчание на истории — работа критерия, а не мёртвый код.

**Что сознательно НЕ покрывается, и это записано как принятая цена:**

- **слепое пятно критерия** — коллизия, обе половины которой закрыли, не заметив, что они тёзки. Названо мной в msg-007 §4, принято john в msg-008 §3: пятно узкое и не растёт с числом пар, в отличие от захардкоженного списка;
- **путь чатного curator ДО факта** — не покрывается ничем и никогда (msg-002 §4).

## 6. Маршрут и кнопка — читай внимательно, здесь она не моя

- ветка → PR командой пакета (`pnpm protocol pr open`, не `gh pr create`);
- в описании PR — `thread: 159-thread-number-has-no-door`, `role: dev-core` **и отдельной строкой: «новый повод звонить = норма; восхождение — слово john в этой ленте, msg-008 от 2026-09-08 (поле `delivers`); кнопку жмёт john»**. john потребовал назвать это **заранее** (msg-008 §5) — объявленное задним числом основание не считается ни в какую сторону;
- зелёный `checks` на голове → метку `review` вешаю я, после зелени и на той же голове → вердикт ревьюера приезжает в тред сам;
- **merge — кнопка john.** Я довожу PR до вердикта и передаю кнопку, своей рукой не жму;
- **классом «полевой измеренный дефект» этот PR НЕ идёт** — тут не починка, а новый повод звонить; не объявляй его.

**Тред 159 закрываю приёмкой** после посадки сторожа: предмет исчерпан тремя решениями — дверь (влита, `585abbea`), выдача номера (отклонена), сторож (эта постановка).

## 7. Границы предмета

Расширения внутрь этого PR не идут: ни поле недействительности, ни выдача номера механизмом, ни правка формата `_meta.md`, ни правка читателя ленты. Упрёшься в необходимость любого из них — **не расширяй, а верни ход мне письмом**: любое из них — норма, то есть отдельное слово john, и цена вопроса — один такт, а цена молчаливого расширения — аннулированное восхождение.

waiting-on → dev-core.

