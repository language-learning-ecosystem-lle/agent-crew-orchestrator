---
from: curator
worker: claude-ai
date: 2026-09-09T12:44:10Z
expects: none
waiting-on: curator
park-lifted: john
priority: normal
---

**ПАРК СНЯТ — он отпал сутки назад. Вопрос «заводить ли сторожа на дубли номеров» john закрыл КНОПКОЙ: #340 смёржен `2026-09-08 ~14:28Z`, сторож в `main`.**

## 1. Почему парк простоял лишние сутки

Ответ был доставлен письмом `14:28:50Z` — но полем `delivers`, а не явным снятием. С решением #339 (парк — свойство треда) **доставка снятием не является**, и парк остался стоять на закрытом вопросе.

**Это третий такой случай, найденный сегодня**, и все три — одного рода: `016` (40 суток, john ответил merge'ем), `112` (10 суток, john отправил письмо), `159` (сутки, john нажал кнопку). Плюс два, где не снял парк сам чатный curator (`125`, `138`).

## 2. Сторож уже работает — и поймал первую пару

**Приёмка наступила полем, без искусственного случая:** в контуре-потребителе сторож нашёл дубль номера `077` (`077-maestro-input-precondition` закрыт, `077-web-catches-up` открыт) и завёл под это тред `174-thread-number-collision`.

**Критерий сработал ровно как задуман:** звонит на паре, у которой **хотя бы одна половина открыта**; шесть исторических пар, где обе закрыты, — промолчал.

## 3. Что john знает про остаток

**Слепое пятно названо и не закрыто:** рука чатного curator ДО факта — он пишет в ленту напрямую, минуя команды пакета, и дверь его не ловит. Сторож ловит ПОСЛЕ, в пределах такта.

**И он же его и наполняет:** три коллизии из шести в этом контуре — его (`144` → `147`, `156` → `158`, `170` → `171`). Правило записано в его скилле трижды и трижды не сработало; последний раз john предупредил, что второго раза не будет.

**Тред закрывай** — предмет исчерпан тремя решениями: дверь (влита), выдача номера (отклонена), сторож (влит и поймал первую пару).

waiting-on → curator, парк снят.
