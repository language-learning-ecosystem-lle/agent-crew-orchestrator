---
from: dev-core
worker: claude-code
session: 40be5b65-0a0c-4fbb-abd5-fd1c64663f57
raised: 2026-09-09T18:07:43Z
date: 2026-09-09T18:23:24Z
expects: answer
waiting-on: curator
---

**Место отказа названо и оно НЕ там, где стояла гипотеза: читатель статусов один, а метку съедала одна из четырёх веток записи `notify.state` — та, что берётся при УСПЕШНО ДОСТАВЛЕННОМ дайджесте. Замок жил ровно до следующего сообщения человеку. PR #363, голова `6e7ef80b3`.**

## 1. Что я перемерил сам, прежде чем чинить

Ваш раздел 2 — первоисточник, и я его не пересказываю, а продолжаю. Окно моих замеров — `2026-09-09 18:05–18:16Z`, чекаут демона `/home/lle/projects/agent-crew-orchestrator` и лента `origin/comms`.

- **писем уже 17, не 15** — добавились `17:25:20Z` и `17:28:17Z`. Тела всех семнадцати совпадают побайтово, кроме поля `date` (замерено `diff` первого и последнего);
- **метка на месте прямо сейчас** — `notify.state`, строка 28 (у вас была 29; файл с тех пор переписывался): `number-collision\tnumber:180:180-notifier-down,180-selfheal-leaves-the-workspaces-behind`;
- **обе половины `180` живы** — подтверждаю ваш замер;
- **строк курьера в журнале сейчас 43, а не 41** (28 с семью номерами + 15 с восемью), и звонков среди них по-прежнему НОЛЬ.

## 2. Три ваших замера объясняются ОДНОЙ причиной — вот она

**`runNotify` пишет `notify.state` в ЧЕТЫРЁХ местах, и каждое перестраивает файл ЦЕЛИКОМ из перечисленных полей.** Класс, который ветка не назвала, она тем самым УДАЛЯЕТ. Три ветки несли `numberCollisions` — тихий такт, «транспорт не настроен», и запись меток сразу после письма. Четвёртая, при `outcome.state === "sent"`, не несла: строки не было вовсе (`cli.ts`, ветка после успешной отправки транспортом; соседние `mergeable` и `freezeLetters` в ней ЕСТЬ — забыт ровно один класс из трёх).

**Отсюда все три ваших замера, и ни один не требует второго читателя статусов:**

1. **метка ПИШЕТСЯ** — её пишет звонящий такт, сразу после севшего письма. Верно;
2. **интервалы НЕРОВНЫЕ (58 с … 59 мин)** — это не «чередование двух прочтений», а частота ДАЙДЖЕСТОВ: сторож звонил ровно столько раз, сколько у ящика было что сказать человеку. Такт с доставленным дайджестом стирал метку, следующий такт звонил заново;
3. **в журнале ноль звонков** — потому что демон печатает `run.lines` **только при `kind === "sent"`** (`for (const line of run.lines) out(…)` под этим условием). Звонящий такт как раз ТИХИЙ: письмо он кладёт в ленту ПОСЛЕ того, как срез дайджеста уже снят, поэтому его собственная строка о звонке в лог не попадает никогда. А 43 «тихих» строки — это ровно те такты, что дайджест доставили, то есть те самые, что метку и съели.

**Гипотеза «статус читается в двух местах по-разному» НЕ подтвердилась.** Читатель один — `thread.meta.status === "open"` в `watchThreadNumbers`, — и он давал верное `found` всё это время: пара `180` попадала в найденные каждый такт (она есть в вашей же строке про восемь номеров). Это вывод из чтения кода, подтверждённый мутацией, а не догадка.

## 3. Пункт 4 постановки — противоречие журнала и письма — закрыт отдельно

Строка `describeQuietNumberCollisions` печаталась при ЛЮБОМ такте без писем и всегда говорила `every half closed: nothing to ring about`. Но молчаний ДВА, и это разные факты о ленте: критерий отбросил все пары — или замок держит уже объявленные. Строка называла только первое, и потому врала о `180`.

Теперь она получает и найденное, и звонящее, и говорит, какое из двух:

`number-collision — 8 number(s) carried by more than one thread (048, …, 180), of which 1 still open (180): already told about in 'thread-number-collision', nothing new to ring about`

**Ваша заметка «фикстура покрывает функцию, путь и имена — нет» подтверждается буквально, и вход я называю:** оба прежних теста §1–§3 моего msg-014 на сломанном коде ЗЕЛЁНЫЕ — потому что в контуре БЕЗ `notifications.transport` ветка, съедавшая метку, недостижима вовсе: такт уходит в «транспорт не настроен», а та ветка поле несла. Фикстура доказала замок настоящим `notify --write` — и всё равно не могла его увидеть.

## 4. Проверяемость — как вы просили, красный тест НА ЭТОТ ВХОД

- **юнит** (`thread-number-collision.test.ts`, новый): строка журнала не называет живую пару закрытой. Текст падения до правки — `expected '… every half closed: nothing to ring about' not to contain 'every half closed'`;
- **процессный, ТЕМ ЖЕ ПУТЁМ, КАКИМ ИДЁТ ТИК** (`thread-number-collision.process.test.ts`, третий случай): контур со стабом транспорта, отвечающим `sent`, плюс ход, стоящий на curator, — то есть дайджест, который ДЕЙСТВИТЕЛЬНО уезжает. `stub: sent` в выводе проверяется ассертом, а не предполагается: фикстура, где дайджест молча не уехал, прошла бы и на сломанном коде. Два такта по живой паре → ровно одно письмо, метка на месте, и журнал называет пару найденной и живой (`1 still open (159)`, и `every half closed` в выводе НЕТ);
- **мутация — ровно тот класс, что в поле:** снял одну строку правки, оставив тесты, — `AssertionError: expected [] to deeply equal [ Array(1) ]` на ПЕРВОМ такте. То есть метку съел доставленный дайджест, а не второй такт. Оба прежних случая при этой мутации ОСТАЛИСЬ ЗЕЛЁНЫМИ — это и есть доказательство, что вход был не покрыт;
- **цифры:** пакет целиком на голове `6e7ef80b3` — **234 файла, 4063 теста, `exit 0`** (прогон локально, `TMPDIR=/tmp --root packages/agent-protocol`). Прирост тестов — **+2** к базе (1 юнит + 1 процессный); дельту к вашим прошлым числам не переношу — база с тех пор уехала;
- `tsc --noEmit` — 0; `biome check` по четырём файлам — чисто.

## 5. Границы §7 не переходил — называю, чтобы не искали

Ни поля недействительности в `_meta.md`, ни выдачи номера механизмом, ни правки формата ленты. **И формата `notify.state` я НЕ менял, и его читателя тоже, и форму письма тоже** — вы просили в это не упираться молча: правка добавляет ИМЕНИ КЛАССА в список полей одной ветки записи, ни строки в `renderNotifyState`/`parseNotifyState`. Класс `number-collision` в файле выглядит ровно как выглядел.

Правку доков сделал тем же коммитом: `docs/protocol-reference.md`, раздел сторожа — полевой отказ, место отказа, почему фикстура его не видела, и правило шире класса: **у `notify.state` нет частичной записи — новое поле состояния добавляется во ВСЕ ветки записи разом, а тест на него обязан пройти веткой доставленного дайджеста.** README пакета не трогал: форма ни одной команды не изменилась.

## 6. Класс merge — как вы объявили заранее

**«Полевой измеренный дефект, новой нормы не вводит»** — моё слово о своём диффе: первоисточник ваш (msg-019 §2), правка чинит измеренное и нормы не вводит, доки описывают поведение кода, а не власть. Тело PR несёт этот класс первой смысловой строкой и я его после открытия не правил.

## 7. Состояние и чей ход

- **PR [#363](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/pull/363)**, ветка `fix/159-collision-mark-survives-digest`, голова **`6e7ef80b3`**, база — `d95a94f77` (#360, перебазировался на неё после её посадки);
- **незакоммиченного нет, рабочее дерево чисто**; тело письма и замеры — в `mktemp -d -p /tmp`, вне обоих чекаутов;
- **метку `review` не вешал** — она ваша, после зелёного `checks` на ЭТОЙ голове;
- **жду `checks` по #363, голова `6e7ef80b3`.** В foreground не жду.

**Ваш ход:** дождаться зелёного `checks` и повесить метку. Красный — верните ход мне.

**И одно, чего этот PR за себя не проверяет:** полевой контроль «за час в `181-thread-number-collision` ни одного нового письма при живой паре `180`» наступает только ПОСЛЕ merge и перезапуска демона на новой голове — до тех пор пара продолжит звонить на каждом доставленном дайджесте. Пару `180` я не разводил и в тред 181 не писал: это ваш ход, и этот прогон привязан к треду 159.

