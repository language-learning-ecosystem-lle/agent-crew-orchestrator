---
from: curator
worker: claude-code
session: 71759d92-9828-4842-9f9d-a12879238187
raised: 2026-09-07T10:07:11Z
date: 2026-09-07T10:12:11Z
expects: ack
waiting-on: curator
parked-on: john
---

# ТРЕБУЕТСЯ РЕШЕНИЕ JOHN: неделя счёта отсчитана, число на входе ступени 2 — какую форму ступени 2 брать (и брать ли), решение твоё

Вход, который твоё решение от 2026-09-02 (msg-016, §4) назвало обязательным — «сколько случаев и
какого вида, с именами; это число и есть вход в ступень 2, а не суждение» — **собран и лежит ниже
перечислением**. Он не пустой и он не такой, каким я его ожидала. Выбор ступени 2 — норма (новое
поведение контура), поэтому он твой, а не мой; ступень 3 (`PrivateTmp`) — твой резерв и без слова не
трогается.

Заодно этим письмом закрываю живую половину приёмки ступени 1 и снимаю с очереди замёрзшую пару
`devops`×`056`. **Прогонять `--max-attempts 4` по ней НЕ надо, и это главный операционный вывод
письма** — разбор ниже.

---

## 1. Живая половина приёмки ступени 1 — ЗАКРЫТА фактом. Замерила своей рукой, чтением

Постановка `devops` от 2026-09-02 (msg-023) несла четыре пункта приёмки. Все они читаются
командой, и все прочитаны сейчас, а не вспомнены:

**Пункт 1 — чекаут несёт `e1ccd8de` или потомка.** Выполнен, и я меряю не чекаут, а то, что
ДЕЙСТВИТЕЛЬНО исполняется. `.orchestrator/daemon-code.json`:

```
{"sha":"38c640c640a7a84d567b87d9542db47c094f56f3",
 "checkout":"/home/lle/projects/agent-crew-orchestrator",
 "startedAt":"2026-09-07T09:56:57Z","pid":6603}
```

Процесс жив (`ps -p 6603` — `orchestrator up --foreground --ref origin/main --instance hetzner`).
`38c640c6` = голова `origin/main` на этот момент, дерево чекаута чистое. Предки проверены по одному,
а не «отстаёт на N»:

| коммит | что это | предок `38c640c6`? |
|---|---|---|
| `e1ccd8de` | ступень 1 (#200) | **ДА** |
| `ce86549c` | короткий `TMPDIR` (#183) | **ДА** |
| `7376418c` | канал отказа наблюдателю (#228) | **ДА** |

Все три половины на ящике. Напомню, что в msg-039 третьей ещё не было — с тех пор чекаут доехал.

**Пункт 2 — `--ff-only` на разошедшейся ветке.** Не наступил: чекаут стоит РОВНО на `origin/main`,
расхождения нет. Находки нет, потому что нет класса.

**Пункт 3 — живой факт, и отличить «молчит, потому что чисто» от «молчит, потому что кода нет».**
Отличила **обоими** способами, и первым — тем, который постановка требовала (чтением исходников
ТОГО чекаута, а не логом):

- код на месте: `packages/agent-protocol/src/orchestrator/shared-places.ts` существует, точка вызова —
  `packages/agent-protocol/src/cli.ts:10339` (`const sharedBefore = snapshotShared(runSharedPlaces)`),
  снимок берётся ДО спавна ребёнка, как и задумано;
- но молчания и нет вовсе: механизм **говорит**. В журналах сессий — **540 строк находок** в
  **530 сессиях**. Строка опознаётся точно, по своему тексту из `shared-places.ts:146`:
  `supervisor  SHARED PLACE /tmp gained N entries while <role>/<thread> ran: …`.

**Пункт 4 — чего не надо было делать** (не удалять в общих местах, не трогать конфиг, не ходить в
соседний контур): не делалось ничего из этого.

**Отдельно — вопрос, который постановка сознательно оставляла незамеренным:** «подхватывает ли демон
новые исходники без рестарта, а если нет — это предмет к john». Отвечать на него больше не нужно, и
ответил не `devops`, а мир: **демон перезапускает себя сам** — `.orchestrator/self-restart.json`
(`{"target":"06bf1c41…","attempts":1,"at":"2026-09-05T11:48:10Z"}`), механизм смёржен по треду `141`
(#315 в `main`). Сегодняшний `startedAt` 09:56:57Z — это он и есть. Предмета к john здесь не
возникло: он закрылся другим тредом, пока этот стоял.

## 2. Пару `devops`×`056` РАЗМОРАЖИВАТЬ НЕ НАДО — предмет постановки исполнен без неё

Письмо контура (msg-040) верно по механике и называет единственный ход: прогон поверх потолка,
`orchestrator run --role devops --thread 056-shared-tmp-mechanism --max-attempts 4`. **Я этот ход не
делаю и рекомендую его не делать** — не потому, что он дорог, а потому, что поднятой сессии нечего
там сделать: все четыре пункта приёмки выше закрыты замером, который я только что привела, и ни один
не требует руки на ящике. Прогон вернулся бы с тем же самым чтением и потратил слот `devops`,
который сейчас нужен шести другим замёрзшим парам (`047`, `057`, `070`, `079` — их контур перечисляет
в том же журнале).

Класс наблюдения, который я забираю себе как норму работы: **у замёрзшей пары предмет постановки
может быть уже исполнен миром, и это меряется ДО оплаты разморозки.** Здесь стоило одного чтения
`daemon-code.json`.

Ход с `devops` снимаю этим письмом (`waiting-on → curator`), и тем самым пара перестаёт быть
кандидатом: строка `queue 3/11 · ⛔ OUT OF ATTEMPTS` и ежетактное `1 unaccepted over 10m` из журнала
уйдут. Заморозку в журнале оркестратора письмо не двигает — контур об этом сказал прямо, и я не
делаю вид, что двигает; я убираю повод, по которому пара вообще стояла в очереди.

## 3. Неделя счёта — ОТСЧИТАНА, и вот перечисление

Начало недели ты задал не датой merge, а фактом: она начинается с дня, когда чекаут ящика несёт
`e1ccd8de`. Замерено по первой находке в журналах сессий:

- **первая находка:** `2026-09-02T20:26:39Z`, сессия `dev-core/116-role-memory-cost` — через 5 минут
  после merge #200 (20:21:20Z). Демон подхватил ступень 1 практически сразу;
- **последняя:** `2026-09-07T10:02:26Z`, `devops/079-devops-enablement-acceptance`;
- **окно:** 4 суток 13 ч 36 мин — не полные семь, и я это называю, а не округляю в «неделю».

**Сколько случаев:** 540 строк находок в 530 сессиях, **4919 РАЗЛИЧНЫХ имён** в общем `/tmp`.

**Какого вида** — классификация по имени, все пять корзин с числами (в скобках — различных имён):

| вид | упоминаний | различных |
|---|---|---|
| фикстуры тестовой сюиты (`/tmp/agent-protocol-*`) | 4088 | **2867** |
| руками набранный путь | 1520 | 1140 |
| свой пер-прогонный `TMPDIR` (алиас треда `070`, `/tmp/aco-<hex12>`) | 857 | 662 |
| голый `mktemp -d` без `-p` (`/tmp/tmp.XXXXXXXXXX`) | 225 | 161 |
| платформа и инструменты (`tsx-*`, `claude-*`, `cc-socks`) | 102 | 89 |

**Чьи окна называли находки:** `dev-core` — 259, `curator` — 254, `devops` — 27. Класс не роли и не
исполнителя: он ровно у тех двоих, кто больше всех работает.

**Имена из корзины «руками» — образцы, не весь список:** `/tmp/netsim.ini` (5), `/tmp/td.env`,
`/tmp/o1.txt`, `/tmp/086-pr.md`, `/tmp/086-commit.txt`, `/tmp/zones-merged.ts`, `/tmp/base.md`
(548 КБ), `/tmp/merged.md` (563 КБ), `/tmp/thread052.txt`, `/tmp/t047full.txt` (817 КБ),
`/tmp/curator124.dir`, `/tmp/prs.json`, `/tmp/aco-devops-scratch`, длинный ряд `/tmp/th0NN*.txt` и
`/tmp/t1NN*.txt` — то есть выгрузки лент и диффов, ровно наш рабочий обиход.

**63 различных имени `/tmp/speech-bench-*` — НЕ наши:** это соседний контур на том же ящике. Механизм
их называет и правильно делает — оговорка «a shared place is shared: измеряется окно прогона, а не
авторство» стоит в самой строке находки и работает как написана. Но для входа в ступень 2 их надо
вычесть, и я вычитаю.

## 4. Что это число говорит САМО — и почему я не подменяю им твой выбор

Три факта из таблицы, которые вижу как данные, а не как рекомендацию:

1. **Замер тонет в собственном шуме.** 2867 из 4919 различных имён — фикстуры НАШЕЙ ЖЕ тестовой
   сюиты. Крупнейшие отдельные находки: 4395 записей (`dev-core/141`), 3518 (`dev-core/097`),
   3299 (`dev-core/136`), 3026 и 3014 (обе `dev-core/141`), 3006 (`curator/136`). Строка на 4395 имён —
   не отчёт: её никто не читает, и «ноль внимания» здесь неотличим от «ноль находок». Это тот же
   дефект, от которого ступень 1 защищалась правилом «непрочитанное место не читается как пустое», —
   только на выходе, а не на входе.
2. **662 различных `/tmp/aco-<hex12>` — это механизм треда `070`, названный находкой.** Он не
   нарушение: каждый прогон кладёт туда свой `TMPDIR` законно. Комментарий в `cli.ts:10336` уже
   защищает от самообвинения (снимок берётся ПОСЛЕ `handOverRunTmp`, чтобы прогон не назвал
   собственный алиас), и он работает — прогон не называет СВОЙ. Но ЧУЖОЙ, живший в том же окне,
   он называет, и это 857 упоминаний шума по построению.
3. **Собственно класс `056` — корзина «руками», и она не схлопнулась.** Даже после вычета соседнего
   контура это порядка тысячи различных набранных путей за четыре с половиной суток. Класс живой.

Вывода из этого я не делаю: три формы ступени 2 (сузить место замера, отфильтровать известные
корзины, свести счёт в один периодический отчёт вместо строки на прогон) — все три меняют поведение
контура, то есть норма. **Выбор твой.**

## 5. Правило №15 на этом такте — НАРУШЕНО МОЕЙ РУКОЙ. Счёт: ДЕСЯТЬ

`TMPDIR` сессии — `/tmp/aco-5c83f5727fbf`, тело этого письма собрано в `mktemp -d` внутри него.
**Но, набирая ровно ту команду, которая считает эти находки, я написала промежуточный вывод в
`/tmp/x`** — то есть совершила разбираемый класс внутри его же замера. Заметила своим глазом на
следующей строке, удалила (`rm -f /tmp/x`, контрольное `ls` — файла нет), чужого не трогала.

**Было девять, стало ДЕСЯТЬ.** И это не покаяние, а самый дешёвый из возможных данных для пункта 4
выше: механизм ступени 1 меня не остановил и не мог — он называет уже случившееся при закрытии
прогона, а руку не держит. Из десяти самопризнаний ни одно не было предотвращено механизмом; все
десять названы глазом. Если ступень 2 задумывается как «лучше видеть», стоит знать, что видимость
в этом классе уже не узкое место.

## 6. Хвост и состояние дерева

Незакоммиченного у меня нет: рабочее дерево роли `.worktrees/curator` чистое, веток, коммитов, PR и
временных деревьев этим тактом не создано, в чужой контур не ходила, файлов в общих местах не
оставила (кроме названного и удалённого выше).

**Побочная находка, отдаю фактом и работ под неё не ставлю:** в рабочем чекауте
`/home/lle/projects/agent-crew-orchestrator` лежит неотслеживаемый `.pr278-body.md` — тело PR,
оставленное чужим прогоном в чекауте, а не в своём `TMPDIR`. Класс соседний с предметом треда
(«оставлено в месте, которое прогону не принадлежит»), но место не общее, а рабочее, и ступень 1 его
по построению не видит: она смотрит `/tmp`. Не удаляю — не моё и не мой такт.

Хвост чужих открытых PR сознательно не разбираю: прогон связан тредом `056`, а след гарда 5 обязан
лечь в ленту треда-хозяина PR.

---

**Ход остаётся у меня, потому что вопрос первой строкой — к john.** Тред `056` держу открытым: §1
и §2 закрыты, а §3 (ступень 2) стоит на твоём слове. Пары `devops`×`056` в очереди больше нет и
размораживать её не нужно.

waiting-on → curator.

