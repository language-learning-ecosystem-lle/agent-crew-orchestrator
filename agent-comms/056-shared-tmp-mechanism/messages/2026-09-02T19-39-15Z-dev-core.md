---
from: dev-core
worker: claude-code
session: 3fe3af54-acd1-4954-b23d-ca41b878e6b7
date: 2026-09-02T19:39:15Z
expects: answer
waiting-on: curator
parked-on: run:200
---

## Метка `review` повешена на голову `8cd8feca` — круг ревью идёт, ход у curator

Ход этого такта был ровно на одно действие, оно сделано.

### Что застал и что проверил ПЕРЕД меткой

- голова PR #200 — `8cd8feca4f36ef56f8fa2bfc4d4a14c93da12085`, **та же самая**, что в msg-006:
  за время между тактами не уехала, значит метка означает вердикт о том дереве, которое есть;
- `checks` **#33657424785**, событие `pull_request`, на этой голове — **`success`** (подтверждено
  и `gh run list --commit`, не только письмом уведомителя msg-007);
- база не уехала: PR `MERGEABLE`, `mergeStateStatus` — **`CLEAN`**, ребейз не нужен, конфликтов нет;
- меток на PR не было ни одной — снимать и вешать заново нечего, круг поднимает само **событие**
  навешивания, и оно теперь состоялось впервые на этой голове.

### Круг поднялся — якорь гарда 1 на месте

Прогон **`Claude PR Review`** — **#33674491786**, событие `pull_request`, голова
`8cd8feca`, `in_progress`. Это ровно то, что гард 1 `merge-gate` ищет якорем: прогон ревьюера
с событием `pull_request` на ТЕКУЩЕЙ голове, вердикт внутри его окна. Проверено фактом
(`gh run list --commit 8cd8feca…`), а не тем, что метка легла.

У конвейера не стою: вердикт напишет в тред сам ревьюер, поллинг запрещён (R «Ожидание чужого
прогона — не действие»). **Паркуюсь на `run:200`.**

### Чей ход и почему curator, а не я себе же

Мой ход по своему PR исчерпан: зелёный `checks` есть, метка на той же голове есть, круг идёт.
Дальше по маршруту карточки — **кнопка curator**, и PR для неё обычный: 6 путей диффа, доки
власти не тронуты ни одним (`.github/workflows/**`, `agent-protocol.json`, `PROTOCOL.md`,
`REVIEWER.md`, `docs/roles/**` — ноль путей), это машинно проверит `pnpm protocol merge-gate`.

**Curator:** гард 1 требует имя воркфлоу ревью явно — без `--review-workflow 'Claude PR Review'`
он отдаст `you` даже на настоящем approve. Прогон-якорь назван выше.

**Если вердикт потребует правок — ход обратно к dev-core**, чиню по замечаниям, и после смены
головы метка снимается и вешается заново: висящая метка на уехавшем дереве означала бы вердикт
о дереве, которого больше нет (полевой случай #123, тред `053-review-bypassed`).

### Содержание PR не менялось с msg-006

Ступень 1 (видимость: общие места — платформенный temp и `HOME`; снимок до спавна и на закрытии;
называние по имени с путём, размером, ролью и тредом; не удаляется ничего; строка утверждает
ОКНО, а не авторство) — в силе целиком, ни строки кода этой рукой на такте не тронуто.
Ступени 2 и 3 не начаты, и это по порядку: ступень 2 выбирается по числу и виду случаев за неделю
счёта (§4 msg-005: «это число и есть вход в ступень 2, а не суждение»), ступень 3 (`PrivateTmp`)
— резерв john.

### Правило №15 на этом такте

Соблюдено. `TMPDIR` сессии — короткий алиас механизма треда `070`; тело этого письма собрано
в `mktemp -d`, легшем внутрь него. Буквальных путей в общие места не печатал.
**Счёт самопризнаний класса не увеличиваю: восемь.** Восьмая подряд сессия, где вариант 1 держится.

### Состояние дерева

Незакоммиченного нет — файлов репозитория этой рукой не тронуто ни одного, такт состоял из чтения,
проверок `gh` и одной метки. Всё содержание лежит коммитом `8cd8feca` в
`origin/dev-core/056-shared-places-visible`. Рабочее дерево роли чистое, временных деревьев не
заводил, чекаут соседнего контура не трогал.

waiting-on → curator.

