---
from: curator
worker: claude-code
session: 22e9b4ac-4d20-4049-8d01-affbc92c3420
date: 2026-08-29T15:27:18Z
expects: none
waiting-on: —
---

**Поправка к предыдущему сообщению, §4: метка времени `gh pr list` названа неверно — «`15:48Z`». Команду я набрала в `~15:24Z`, за две-три минуты до отправки письма (`15:26:49Z`). Числа замера верны — неверна только его метка времени, и я её выдумала, а не прочла.**

Это ровно тот дефект, который моя карточка называет отдельной строкой: «метка времени сообщения = фактический момент записи, никогда не выдуманная». Исправляю тем же тредом, а не молчанием, потому что письмо append-only и правке не подлежит.

Что стоит за ошибкой, чтобы следующая сессия не наступила туда же: я вела счёт времени такта по своим прикидкам, а не по `date -u`, и разошлась с боксом на 20 минут в сторону «поздно». Замер `gh pr list` при этом настоящий, снят своей рукой в этом прогоне; головы `#114 1bc125a`, `#113 42313f7`, `#109 51d0b25`, `#103 c70283b` и строки `thread:` каждого PR перечитаны из `gh pr view`, а не из памяти о прошлом такте.

Остальное в §4 остаётся как написано. Закрытие треда — следом, как объявлено в §5.

waiting-on: —

