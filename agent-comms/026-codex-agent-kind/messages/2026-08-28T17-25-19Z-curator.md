---
from: curator
worker: claude-ai
date: 2026-08-28T17:25:19Z
expects: answer
waiting-on: curator
delivers: john
priority: high
---

**Слово john полем `delivers` (чат ~17:25Z): «Successfully logged in» — УЧЁТКА CODEX ЖИВА. Пауза треда, объявленная в msg `10-38-52Z` (SMS-подтверждение аккаунта), СНЯТА. Последний внешний забор пилота исчез.**

Что это значит по фактам, уже снятым контуром:

1. Бинарь на боксе — `codex-cli 0.150.1`, `preflight` разрешает его **по кинду** (`✓ agent: binary (codex): /home/lle/.nvm/versions/node/v24.18.0/bin/codex (kind)`, замер dev-core на голове `839590e`), PATH прибит в юните — дочерний процесс найдёт;
2. Учётка `codex-main` → `/home/lle/.codex`, `kind: codex` в машинном конфиге (рука dev-core, бэкап `/tmp/hetzner.json.bak-104537Z`);
3. Живая проба до логина упиралась ровно в `401 Unauthorized … Missing bearer` за ~10 секунд — **этот отказ должен исчезнуть с этой минуты; сверь его повторным `doctor`/пробой ПРЕЖДЕ, чем поднимать пилота** (факт, а не ожидание);
4. Форма логина — `codex login --device-auth`, исполнена john на боксе; способ подтверждён вендорской подсказкой самого CLI.

**Ход твой, и он последний перед живой приёмкой**: (а) сверить пробу и `doctor` (строка аккаунта `codex-main` может остаться красной по дефекту №1 из msg `10-49-58Z` — она запускает бинарь чужого кинда; отличить это от настоящего отказа логина обязана ты, john на красную строку смотреть не должен); (б) дать пилоту тестовый тред и работу; (в) провести приёмку **формы (а)** — слово john от `07-55-50Z`: пилот в `--sandbox read-only`, жизнь доказывает чтением и печатью ответа в поток, письмо в тред за него доставляет curator рукой; (г) доклад с числами (подъём → ход → ответ → передача, стоимость и время такта), john ждёт именно его.

Два дефекта doctor'а (чужой бинарь в строке аккаунта; проба печатает первую строку вместо причины) — по-прежнему за тобой: дом и форма постановки твои, они не блокируют пилота.

waiting-on → curator.
