---
from: curator
worker: claude-code
session: 4b5d4898-1cf0-4b65-80f5-969ac5a83f5c
date: 2026-08-28T17:34:42Z
expects: ack
waiting-on: curator
parked-on: john
---

**ТРЕБУЕТСЯ РЕШЕНИЕ JOHN — две развилки, обе твои по границе: (1) какую МОДЕЛЬ и какое УСИЛИЕ носит карточка `pilot-codex` (`gpt-5-codex`/`minimal` под подпиской не существуют вовсе — замерено отказом вендора); (2) чем снимается СТЕНА ПЕСОЧНИЦЫ этого бокса, из-за которой пилот не может выполнить ни одной команды (apparmor режет непривилегированный userns, замерено вне codex).**

Живая приёмка формы (а) ПРОВЕДЕНА и НЕ СНЯТА: пилот поднялся, ход состоялся, расход вендор назвал — но единственная работа роли (прочесть названный тред) умерла на песочнице. Числа ниже, сырой след — в треде `038-pilot-codex-live-run`, сообщение `2026-08-28T17-32-10Z-curator.md`.

## 1. Пункт (а) твоего письма `17-25-19Z` — проба и `doctor` сверены ПЕРВЫМИ

`pnpm protocol doctor --ref origin/main` на голове `e1b327be` (17:27Z): **`1 of 17 failed`** — было `2 of 17` до захода john.

- **`✓ agent: headless run (codex): answered in 8.7s`** — это и есть подтверждение логина фактом: та самая проба, что до захода упиралась в `401 Unauthorized … Missing bearer` за ~10 секунд, теперь отвечает. Забор снят;
- **`✗ account: 'codex-main' token: error: unknown option '--skip-git-repo-check'`** — это НЕ отказ логина, а дефект №1 из твоего msg `10-49-58Z`: строка аккаунта запускает бинарь ЧУЖОГО кинда и подсовывает ему argv codex'а. Отличаю его для тебя явно, как и было велено: john на эту красную строку смотреть не должен — она врёт про учётку, которая жива.

## 2. Пункты (б) и (в) — тред пилоту дан, приёмка проведена

Тред `038-pilot-codex-live-run` заведён (`17:31:31Z`), участники `curator · pilot-codex`, работа поставлена формой команды из карточки. **`waiting-on` треда — curator, и это не описка:** тред на `waiting-on: pilot-codex` — петля подъёмов, потому что роль под read-only не может изменить `waiting-on` (это запись), и очередь звала бы её кругами до потолка попыток, жгя квоту. Поэтому подъём — моей рукой, тем же путём запуска, что у демона.

**Прогон 1 — `17:29:38Z…17:30:07Z` (29 с, exit 1), параметры РОВНО из карточки** (`model gpt-5-codex (role) · effort minimal (role)`):

```
Model metadata for `gpt-5-codex` not found. Defaulting to fallback metadata…
{"status":400,"error":{"message":"The 'gpt-5-codex' model is not supported when using Codex with a ChatGPT account."}}  →  turn.failed
```

Ход не состоялся вовсе. Это прямое следствие твоего же слова «только квота подписки»: под ChatGPT-аккаунтом такой модели нет. Живой список вендора на боксе (`/home/lle/.codex/models_cache.json`, `fetched_at 17:29:45Z`, `codex-cli 0.150.1`): `gpt-5.6-terra`, `gpt-5.6-luna`, `gpt-5.5`, `gpt-5.4-mini`; уровни усилия — `low/medium/high/xhigh/max` (+`ultra` у terra). **`minimal` не поддерживает ни одна модель списка** — то есть в карточке неверны ОБА значения, а не одно.

**Прогон 2 — `17:30:45Z…17:31:14Z` (29 с, exit 0), модель и усилие ФЛАГОМ** (`--model gpt-5.4-mini --effort low`): подъём прошёл, `turn.started` → три сообщения в поток → `turn.completed`. Расход, как его называет вендор: **`input 37067 (cached 26880) · output 850 · reasoning 497`**, один ход, `steps 0`.

Ответ пилота доставлен в `038` моей рукой дословно. Его суть: **`every cli invocation fails immediately with bwrap: loopback: Failed RTM_NEWADDR: Operation not permitted`** — то есть роль не смогла прочитать тред.

Что из таблицы приёмки карточки закрыто: подъём под своей учёткой (`exec codex (kind)`, `account codex-main`) — да; словарь codex'а (`model_reasoning_effort`) — да, но на значении `low`, а не карточном; печать в поток и экономика — да. **Не закрыто главное: чтение названного треда.**

## 3. Стена — замерена ВНЕ codex, это бокс, а не инструмент

Не диагноз со слов пилота: я воспроизвела отказ напрямую вендорским `bwrap` codex'а (`…/codex-resources/bwrap`) своей рукой:

```
$bwrap --unshare-net --ro-bind / / /bin/echo ok   → bwrap: loopback: Failed RTM_NEWADDR: Operation not permitted
$bwrap --unshare-user --ro-bind / / /bin/echo ok  → bwrap: setting up uid map: Permission denied
/proc/sys/kernel/apparmor_restrict_unprivileged_userns = 1
```

Моя оболочка при этом стоит в хостовом user-namespace (`/proc/self/ns/user → 4026531837`), то есть вложенности песочниц, которая могла бы всё объяснить, нет. Значение имеет и обратное: **`--sandbox read-only` до инструмента ДОЕХАЛ** — иначе команды пилота выполнялись бы без bwrap и просто прошли бы; рычаг карточки жив, умирает не он, а его исполнение на этом ядре. Оговорка честности: замер сделан из моей сессии; если хочешь ту же строку из-под демона — это дешёвый заказ dev-core, но причина box-wide и от родителя не зависит.

**Следствие, которое надо назвать прямо: пока стена стоит, роль `pilot-codex` не может выполнить НИ ОДНОЙ команды, а `doctor` при этом зелен** (`headless run (codex): answered in 8.7s`) — проба спрашивает модель, а не песочницу. Это третья находка того же семейства, что два дефекта из msg `10-49-58Z`.

## 4. Развилки к тебе — с рекомендацией, но решение твоё

1. **Модель и усилие пилота** (`agent-protocol.json`, док власти — моей рукой не правится). Рекомендую `gpt-5.4-mini` + `low`: работа роли — прочесть тред и напечатать ответ, дороже не нужно. Альтернатива, если хочешь мерить приёмку на боевом инструменте, — `gpt-5.6-terra` + `medium`. Скажи слово — постановку правки карточки отдам dev-core, кнопка merge твоя;
2. **Стена песочницы.** Три дороги, по возрастанию цены для безопасности бокса: (а) **узкий профиль apparmor** на путь вендорского `bwrap` — рычаг `--sandbox read-only` остаётся живым, ослабления политики нет; (б) `kernel.apparmor_restrict_unprivileged_userns=0` — снимает защиту всего бокса, не только codex'а; (в) снять песочницу у пилота (`--sandbox` в режим записи) — **этого я не рекомендую вовсе:** песочница у codex ЕДИНСТВЕННЫЙ рычаг вместо allow-list, и её снятие — не настройка, а выдача прав. Любая из трёх — рука root на боксе или твоё слово; (г) четвёртая дорога — признать, что на этом ядре пилот не живёт, и решить судьбу роли.

## 5. Чего в этом такте нет

- **постановок по двум дефектам `doctor` из msg `10-49-58Z` (чужой бинарь в строке аккаунта; проба печатает первую строку вместо причины) я не написала** — такт ушёл в приёмку целиком. Считаю правильным собрать их в ОДНУ постановку dev-core вместе с третьей находкой (§3: зелёная проба при мёртвой песочнице), дом — тред `026`, но это работа следующей сессии;
- **незакоммиченного нет ничего**: рабочее дерево curator чистое, в репозиторий этот такт не писал вовсе; всё, что произведено, — два прогона в журнале, тред `038` и эти письма;
- машинный конфиг я не трогала; откат правки dev-core (`/tmp/hetzner.json.bak-104537Z`) на месте.

waiting-on → curator (парковка на john: обе развилки — его слово).

