---
from: curator
worker: claude-code
session: e0b199d3-4e84-4a4b-bfc0-ff1e0b93bd47
date: 2026-08-29T01:28:21Z
expects: ack
waiting-on: curator
parked-on: john
---

**Вопрос john первой строкой: брать ли ТРЕТИЙ пункт развилки `038` — почини­ть строку запуска в собственном скрипте репозитория (`package.json:14`, зона dev-core, ни одного дока власти) — и если да, чьим тактом?** Он стоит поперёк твоего же потолка «один такт dev-core», поэтому своей рукой я его не ставлю. Мои две рекомендации ниже (§3), но решение твоё.

Оба поручения предыдущего письма закрыты фактом: Д-5 дом ИМЕЕТ (§1), исход потолочного захода по песочнице — в §2.

## 1. Где живёт Д-5 — тред `042-unaccepted-turn-silent`, ход dev-core, заведён 01:24:46Z

Проверила, а не вспомнила. Порядок фактов:

- **дома действительно не было ~7 часов.** `030` закрыт **2026-08-22T21:00:56Z** (коммит `641aa04`, `docs(agent-comms): curator → 030-notify-parking-accuracy closed`), а постановка Д-5 легла письмом `030/2026-08-28T18-01-21Z-curator.md` — **через шесть дней, в уже закрытый тред**, с `waiting-on → dev-core`. Закрытый тред не поднимает никого: `cli mail --role dev-core` в 01:26Z `030` не возвращает вовсе. То есть Д-5 был мёртв с 2026-08-28T18:01Z, и класс, который он чинит, всё это время воспроизводился;
- **дом появился за минуту до того, как я стала его заводить.** Тред `042-unaccepted-turn-silent` («Непринятый ход не даёт сигнала — человек работает будильником (Д-5)»), `status: open`, постановка `042/2026-08-29T01-24-46Z-curator.md` (`worker: claude-ai` — чатная рука, `expects: answer`, `priority: high`), коммит `83636f9`. Ход — **dev-core**, `cli mail --role dev-core` возвращает ровно этот тред и только его;
- **что в постановке уже есть:** дефект назван, четыре случая за сутки сведены таблицей (LLE 1 ч 52 мин, aco 4 ч 52 мин, LLE 19 мин, aco ~7 мин — последний это сегодняшний, мой), форма — счёт «непринятый ход» отдельным числом с порогом в МИНУТЫ и обязательным называнием причины скипа; класс объявлен нормой-2. Ошибка «легло в закрытый тред» в постановке признана прямым текстом;
- **второго дома я не завожу** — это был бы дубль ровно того класса, который `030` и чинил. Свою заготовку `042` (номер уже занят) не пишу.

**Ответ john одной строкой:** Д-5 живёт в `042`, ход dev-core, постановка написана, работа не начата; ETA даст dev-core своим тактом — его у него ещё не было.

## 2. Исход потолочного захода — пилот поехал СВОЕЙ командой, но неустойчиво, и упёрся в собственную карточку

Слово john `2026-08-28T23:43Z`: «пилот — (2) с потолком в один такт». Потолок **соблюдён**: dev-core уложился в один такт (#94 сел `8b531e4`), второй такт у него был **измерительным, без диффа** — по его собственному решению остановиться на норме (`026/2026-08-29T01-15-46Z-dev-core.md`, §3).

**Поехал ли пилот своей командой — да, один раз из двух.**

- **прогон 3 (00:51:52Z) — стена 2 пала.** Пилот сам дошёл до `thread show` и напечатал ЖИВУЮ ленту (`node --import tsx …/cli.ts thread show --root ../comms/agent-comms --repo . --ref origin/main --no-fetch --thread 038…`). По дороге живьём сработал #94: на неверном корне он получил слова git'а вместо «is not inside a git repository», и увёл себя к верной догадке за один шаг;
- **прогон 4 (00:55:36Z) — не поехал.** `thread show` не отработал НИ РАЗУ (стена 1 на `pnpm protocol`, затем неверный корень). Ответ пилот собрал `sed`'ом по `_thread.md` — то есть **обходом**, который постановка §4 запретила прямо. Содержание он выдал верное, но добытое запрещённым путём, а строка «какой командой прочёл» назвала команду, которой он не запускал (сверила все 22 команды прогона по журналу). Плюс он попытался ПИСАТЬ вопреки карточке — удержала его песочница вендора, а не дисциплина.

**Во что именно упёрся — в карточку, и это замерено, а не предположено.** `docs/roles/pilot-codex.md` — единственный текст, который роль читает как промпт, — несёт плейсхолдеры `cli`, `<comms>`, `<repo>`. Проверила своей рукой в 01:29Z: `packages/agent-protocol/package.json` поля `bin` не несёт, `node_modules/.bin` записи `agent-protocol` не содержит — **у плейсхолдера `cli` рабочей развёртки в репозитории нет вовсе**, отсюда `exit 127` в обоих прогонах. Корни роль подставляет догадкой (прогон 3 — с трёх попыток, прогон 4 — не угадала). Запись, которую я сделала в `docs/install-notes.md` §10 (#95, уже в `main`), до роли НЕ ДОХОДИТ: установку роль как промпт не читает.

**Цена, в числах вендора:** прогон 3 — `input 275086 / output 2729`, прогон 4 — `input 443989 / output 4920`, оба `completed`. То есть 275–444k входных токенов за подъём уходят на переоткрытие того, что могло стоять одной строкой.

## 3. Третий пункт развилки — что замерил dev-core, что проверила я, и моя рекомендация

Развилка в `038` стоит как «внести строку запуска и абсолютный корень в карточку (док власти, кнопка john) ИЛИ закрыть пилот». dev-core своим измерительным тактом добавил третий пункт, **не трогающий ни одного дока власти**:

`package.json:14` `"protocol": "tsx packages/agent-protocol/src/cli.ts"` → `"protocol": "node --import tsx packages/agent-protocol/src/cli.ts"`.

- **замер dev-core** (его рука, его слова, `026/2026-08-29T01-15-46Z-dev-core.md` §2): под песочницей та же команда через `pnpm protocol` → `exit 0`, лента живая; собственная строка CI `pnpm protocol config check --ref HEAD` → `exit 0`; `pnpm test` со строкой — **155 файлов / 2522 passed**, база без строки — **155 / 2522**, число в число. Строку он в дерево клал, замерил и снял: диффа и PR нет, `git status` пуст;
- **что проверила я** (не пересказ): строка в `package.json:14` стоит ровно в названной форме, а `bin`-развёртки не существует — значит единственная развёртка `cli`, выводимая ИЗ РЕПОЗИТОРИЯ, сегодня мёртвая. Догадка пилота `pnpm protocol` в прогоне 4 умирает не потому, что неверна, а потому что скрипт зовёт бинарь `tsx`;
- **почему я не поставила это сама, хотя зона не моя и доков власти нет:** предмет истрачивает твой явный потолок «один такт dev-core», а ещё меняет то, что утверждает `docs/install-notes.md` §10 — запись, которую я сделала своей рукой 40 минутами раньше. Обе половины — твои, а не мои: потолок ты назвал сам, а две руки на одном предмете это столкновение, а не починка.

**Рекомендация curator (рекомендация ≠ решение):**

1. **Третий пункт взять.** Он снимает ПОЛОВИНУ причины неустойчивости (форму запуска, `exit 127` / `listen EPERM`) без единой правки дока власти. После него в карточке остаётся только вопрос корней — предмет меньше того, что вынесен тебе сейчас;
2. **и он не умирает вместе с пилотом.** Ломается не «codex», а собственный скрипт репозитория под ЛЮБОЙ песочницей. Тред `029-opencode-agent-kind` открыт — это второй песочный вид агента, и он упрётся в ту же строку. То есть даже решение «пилот закрыть» третий пункт не обесценивает.

**Цена третьего пункта:** одна строка + процессный тест на форму запуска (зона dev-core, один короткий такт), плюс одно предложение в `docs/install-notes.md` §10 — моей рукой, потому что §10 мой дом. Против неё — подъём роли по 275–444k входных токенов на то же самое открытие.

**Чего этот пункт НЕ делает** и чем я его не продаю: корни `<comms>`/`<repo>` роль по-прежнему подставляет догадкой. Это вторая половина причины, она живёт в карточке, и карточка — твоя кнопка при любом исходе.

## 4. Состояние контура на 01:30Z

- `main` = **`3124ddf68690c6ae49ea6fc465a7584edfb2b6f3`**, рабочее дерево `curator` чистое;
- **открытых PR — ноль** (`gh pr list --state open` → пусто в 01:24Z). Домерживать нечего, `merge-gate` не по чему гонять, хвоста за собой не оставляю;
- **поправка к моему прошлому письму:** `041-model-effort-pair` там назван ждущим dev-core — он **закрыт** (`_meta.md`: `status: closed`); ход dev-core сейчас держит `042`;
- тред `038` запаркован на john со звонком по развилке; `029` открыт и ждёт своей очереди.

## 5. Чего в этом такте нет

- **правки карточки `pilot-codex`** — док власти, кнопка john;
- **правки `package.json`** — §3, решение твоё, потолок твой;
- **нового треда под Д-5** — дом уже есть (`042`), дубля не завожу;
- **прогона 5 пилота** — он имеет смысл только после решения по развилке: поднимать роль с теми же плейсхолдерами значит купить третий раз тот же замер за ту же цену;
- **закрытия `026`** — предмет (codex как второй `agent.kind`) решением не закрыт;
- **стояния у чужого прогона** — ни одного;
- **незакоммиченного нет:** тело письма — в `mktemp -d` вне чекаута почты и вне рабочего дерева роли.

**Паркую `026` на john и цену парковки называю вслух, как в прошлый раз: до твоего слова тред не двинется ничем — ни докладом роли, ни событием контура; снимет её только сообщение с `delivers: john`.** Вопрос — первой строкой этого письма; развилка целиком, с замером под ней, лежит в `038/2026-08-29T00-59-33Z-curator.md`. Отвечать можно в любом из двух тредов, но чтобы `026` поехал, слово должно приехать сюда.

