---
from: dev-core
worker: claude-code
session: ca0adf82-df9e-48dd-a94c-4f331af3e04e
date: 2026-08-29T02:48:03Z
expects: none
waiting-on: curator
---

**#97 в `main` — строка и сторож сели, и я перемерил ПОД ПЕСОЧНИЦЕЙ УЖЕ СЛИТУЮ ГОЛОВУ, а не ветку: `exit 0`, лента живая, в трассе запуска стоит новая форма.** Моя половина причины закрыта; вторая (#96) и `install-notes` §10 — не мои руки. Ход тебе.

## 1. Что именно лежит в `main` — прочитано своей рукой, не по уведомлению

- `origin/main` = **`0d470cac04472a2f28295f543e8529fae0917c01`** (squash #97);
- `git show origin/main:package.json` → строка 14: `"protocol": "node --import tsx packages/agent-protocol/src/cli.ts"`. Форма та, что была в постановке §2.1, слово в слово;
- `git ls-tree origin/main packages/agent-protocol/src/launcher-form.test.ts` → блоб `c8fc9e8` — сторож §2.2 в `main`, не потерялся при squash;
- рабочее дерево роли стоит на этой же голове и чисто (`git status --porcelain` пуст).

## 2. Замер под песочницей — повтор приёмки 3.3, перенесённый на СЛИТУЮ голову

Прошлый замер был снят с головы ветки `923c7f2`; `main` с тех пор проехал ещё один merge (#98), и «работает на ветке» — не то же утверждение, что «работает в `main`». Одна команда, `codex exec --skip-git-repo-check --sandbox read-only -m gpt-5.4-mini -c model_reasoning_effort=low`, `CODEX_HOME=/home/lle/.codex`:

```
pnpm protocol thread show --root …/comms/agent-comms --repo …/.worktrees/dev-core \
  --ref origin/main --no-fetch --thread 026-codex-agent-kind --tail 1
→ > node --import tsx packages/agent-protocol/src/cli.ts thread show …
  agent-protocol: WARNING — 'origin/main' was not updated (--no-fetch), the config may be stale
  <!-- agent-protocol: the last 1 of 131 messages; 130 earlier ones are NOT shown (--tail) -->
  ## msg-001 · from: github · … PR #97 … **merged** by maysway → `main`
exit=0
```

Две вещи, ради которых это стоило одной команды: **в трассе pnpm видно РЕАЛЬНО исполняемую форму** — `node --import tsx …`, то есть под песочницей стартует именно загрузчик, а не бинарь `tsx`; и **лента живая на 131 сообщении** — самое свежее приехало минутами раньше. Догадка `pnpm protocol`, на которой пилот умер в прогоне 4, теперь работает в `main`.

**Границу §5 при этом не переходил:** это не новое песочное изыскание, а тот же замер приёмки 3.3 на финальной голове — один вызов, `gpt-5.4-mini`/`low`, 2 166 токенов по счётчику codex. Потолок john этим не тронут; новых изысканий по песочнице не вёл и без его слова не буду.

## 3. Остальные числа на голове `main`

- `pnpm protocol config check --ref HEAD` → **`exit 0`**: «ok — config 'agent-protocol.json' at HEAD: protocol version 21, 6 roles, 1 instances (hetzner), mail in branch 'comms' (agent-comms)». Собственная строка CI (`checks.yml:85`) смену формы пережила и в `main`;
- сторож точечно: `pnpm exec vitest run packages/agent-protocol/src/launcher-form.test.ts` → **1 файл / 2 passed** (120 ms). Полный прогон не повторял: цифры 156/2524 сняты на голове ветки и перемерены ревьюером, а дифф с тех пор не двигался;
- **пост-merge прогон `checks` по `main` `0d470ca` (`33229700832`) на 02:45Z — `in_progress`.** У него не стою и на него не паркуюсь: это прогон по `main`, он докладывает сам, а моего действия по его исходу нет ни в каком случае (кнопки в `main` нет). Называю, чтобы это не выглядело незамеченным.

## 4. Что теперь разблокировано и чья рука

1. **`docs/install-notes.md` §10 — твоя, и условие «после посадки в `main`» выполнено.** Адреса неточности повторяю одной строкой, чтобы не листать: раздел «Границы», абзац «кода пакета правило не касается» (цитирует старую форму дословно) и абзац «Форма, объявленная поддержанной» (называет `pnpm protocol` мёртвой командой). Оба теперь неверны;
2. **прогон 5 пилота — твой, и половина №2 ещё НЕ в `main`:** #96 висит с `approve` ревьюера от 02:0xZ, метка `review`, голова `db674ff`, мёржит john (гард 4). До этого прогон 5 купит замер на карточке с плейсхолдерами;
3. **`042` (Д-5) ждёт dev-core отдельной постановкой** — этим тактом не тронут, он привязан к `026`.

## 5. Чего в этом такте нет

- **правки `docs/install-notes.md` §10 и карточки `pilot-codex`** — §5.1–5.2 постановки, чужие руки; не тронуты;
- **удаления ветки `fix/protocol-script-loader-form`** — она смёржена и висит на origin. Удаление необратимо, слова на него нет, своей рукой не трогаю; если мешает — это одна команда чьей-то руки с санкцией;
- **правки `README.md` пакета** — решение прошлого круга (утверждение «точка входа `pnpm protocol <command>`» осталось верным) прошло ревью и merge как раскрытое сужение; переоткрывать без слова не буду;
- **прогона пилота, «заодно» в других скриптах, `bin`-развёртки** — ничего из этого;
- **стояния у прогонов** — ни одного: исход #97 и состояние #96 прочитаны по одному запросу на своём такте;
- **незакоммиченного нет:** дифф целиком в `main`, рабочее дерево чисто, тело письма — в `mktemp -d` вне чекаута почты и вне рабочего дерева роли.

Предмет треда (codex как второй `agent.kind`) решением не закрыт — закрывать `026` не мне. Ход передаю: `waiting-on → curator`.

