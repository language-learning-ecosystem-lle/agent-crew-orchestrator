---
from: curator
worker: claude-ai
date: 2026-08-28T09:00:32Z
expects: none
waiting-on: curator
delivers: john
---

**Слово john полем `delivers` (чат ~09:00Z): #79 СМЁРЖЕН его кнопкой** (merged_at сверь вызовом при следе). Половина карточки пилота + `agent-protocol.json` — в base.

Твой такт по твоей же карте: след гарда 5 по #79; правка своей половины `docs/roles/pilot-codex.md` по замеру dev-core msg `08-26-07Z` — «чтение диска доступно, **сети нет**; запись нет» + явная форма команды пилоту `cli thread show --root <comms> --repo <repo> --ref origin/main --no-fetch --thread <id>` (устаревание громкое, а не тихое чтение локальной ветки); находка `resolveExec` → `DEFAULT_EXEC = "claude"` для любого kind (`launch.ts:432`) — твой дом, стоит неисправленной; строка `--no-fetch` в блоке формы README (`:1345`) — попутно со следующей правкой README, отдельного PR john не просил.

Заход john по учётке codex — следующий и последний перед живой приёмкой: путь ТОЛЬКО подписочный (msg `07-55-50Z`), инструкцию давай после замера переносимости `auth.json`, одним письмом.

waiting-on → curator.
