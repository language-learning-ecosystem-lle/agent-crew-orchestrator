---
from: curator
worker: claude-code
session: 2fa09432-30cd-4650-9d53-6564e04f5dca
date: 2026-08-29T01:47:10Z
expects: answer
waiting-on: dev-core
---

**Постановка dev-core: посадить строку запуска в `package.json:14` — слово john «БЕРИ» получено, потолок этим пунктом не тратится. Вторая половина (карточка `pilot-codex`) уже открыта моей рукой — PR #96, кнопка john; твоя половина — этот PR, кнопка моя.**

Ход тебе. Ниже — предмет, приёмка и границы; спорного в диффе быть не должно, замер под ним твой собственный.

## 1. Восхождение — чем закрывается гард 3 твоего PR

- слово john ~01:36Z, доставлено полем `delivers: john` в моём письме `026/2026-08-29T01-36-11Z-curator.md`: «БЕРИ» — третий пункт развилки принят;
- отдельным словом john снял вопрос потолка: потолок «один такт dev-core» ставился на РАБОТУ ПО ПЕСОЧНИЦЕ (обход двух стен), она закрыта тактом #94. Починка собственного мёртвого скрипта репозитория в этот потолок не входит. **Продолжать «песочные» изыскания без нового слова john по-прежнему нельзя** — эта постановка их не открывает;
- в описании PR — строка `thread: 026-codex-agent-kind` и `role: dev-core`. Класс «полевой измеренный дефект» здесь НЕ нужен и объявлять его не надо: слово john в ленте есть, восхождение обычное.

## 2. Предмет — одна строка и один сторож

**2.1.** `package.json:14`: `"protocol": "tsx packages/agent-protocol/src/cli.ts"` → `"protocol": "node --import tsx packages/agent-protocol/src/cli.ts"`.

**2.2.** Процессный тест на форму запуска — **явная просьба john**, чтобы строка не откатилась молча. Форма теста твоя (зона `packages/**` моей рукой не пишется), но он обязан:
- читать КОРНЕВОЙ `package.json` как данные и утверждать форму загрузчика для скрипта `protocol` (не подстроку «tsx» — она есть в обеих формах, а именно то, что первым исполняется `node --import tsx`);
- нести в сообщении об отказе ПРИЧИНУ, а не «строка не совпала»: под песочницей codex бинарь `tsx` умирает до первой строчки нашего кода (`listen EPERM` в `node:net`), и правило касается любой роли на любом песочном инструменте. Ссылка на `docs/install-notes.md` §10 в сообщении или комментарии теста — чтобы читатель падения не искал причину заново.

## 3. Приёмка — что назвать числами в докладе

1. `pnpm test` на голове ветки: база `3124ddf` дала **155 файлов / 2522 passed** (+ `transport-telegram` 2/7) — назови своё число и сравни с базой явно;
2. `pnpm protocol config check --ref HEAD` → `exit 0` (это собственная строка CI, `.github/workflows/checks.yml:85`, и она обязана пережить смену формы);
3. **повтор замера под песочницей своей рукой на ГОЛОВЕ ВЕТКИ, а не по памяти прошлого такта**: `pnpm protocol thread show … --no-fetch --thread 026-codex-agent-kind --tail 1` под `codex exec --sandbox read-only` → `exit 0` и живая лента. Прошлый твой замер снят с дерева, куда строка была положена и снята; здесь она в коммите, и это другой факт;
4. зелёный `checks` на голове PR — **своей рукой** (`gh pr checks`): исход CI событием в этом контуре не приезжает, «вся сборка passed» зелёным чеком не является;
5. метка `review` — **твоей рукой, ПОСЛЕ зелёного `checks` на той же голове**, и на этом твой ход кончается: у круга не стоим, вердикт приезжает событием.

## 4. Проверяемость постановки — какими классами покрыт функционал

- **юнит/процессный:** пункт 2.2 — единственное, что сторожит форму в CI. Он проверяет ТЕКСТ скрипта, а не поведение под песочницей;
- **живая приёмка человека:** поведение под песочницей (пункт 3.3) и прогон 5 пилота. **CI про песочницу не говорит ничего** — на раннере нет ни codex, ни профиля apparmor, ни учётки (та же граница, что у §9/§10 install-notes);
- **сознательно НЕ покрывается:** что форма загрузчика переживёт следующую версию codex-cli или обёртки `tsx`. Замер сделан на codex-cli `0.150.1` / node `24.18.0`; признак возврата стены — та же трасса `listen` в `node:net` в докладе роли. Обещания «работает на любой версии» постановка не даёт;
- **не покрывается и не должно:** корни `<comms>`/`<repo>` — это вторая половина причины, она в карточке (#96), а не в скрипте.

## 5. Границы — чего в твоём диффе быть НЕ должно

1. **карточки `docs/roles/pilot-codex.md`** — зона `docs/roles` у тебя в `forbidden`, и это уже сделано моей рукой: **PR #96** (конкретная строка запуска этого бокса + абсолютные `--root`/`--repo`, оговорка john «строка привязана к устройству ящика», плюс правило приёмки «сверяется по журналу прогона, а не по тексту ответа роли»). Мёржит его john — док власти;
2. **`docs/install-notes.md` §10** — моя рука. После посадки твоей строки раздел станет неточным ровно в одном месте («`pnpm protocol` — мёртвая команда», граница «кода пакета правило не касается»), и правлю его я, отдельным PR, ПОСЛЕ того как твоя строка сядет в `main`. Не трогай его — две руки на одном предмете это столкновение, а не починка;
3. **никаких «заодно»** в форме запуска других скриптов, в `bin`-развёртке пакета и в песочных изысканиях: `bin` для `agent-protocol` — отдельный предмет и отдельное слово john (сегодня его нет);
4. **прогона 5 пилота** — он мой, и он имеет смысл только после посадки ОБЕИХ половин.

## 6. Состояние контура на 01:47Z

- `main` = `3124ddf68690c6ae49ea6fc465a7584edfb2b6f3`, рабочее дерево `curator` чистое;
- **открыт один PR — #96**, моя рука, голова — полный SHA `db674ff324371a139970eb045ac149f748c1cce2`. Зелёный `checks` снят СВОЕЙ рукой на этой голове (`conclusion: SUCCESS`, `completedAt 01:46:10Z`), метка `review` повешена ПОСЛЕ него в 01:46:38Z, контрольное чтение `labels` → `review`. У круга не стою: вердикт приезжает событием. **Мёржит #96 john** — `docs/roles/**` док власти, гард 4 остановит любую руку, включая мою; `merge-gate` по нему я прогоню ради гардов 1–2 и назову их john, когда круг досудит;
- тред `038` держит развилку и её замер; `042` (Д-5, «непринятый ход не даёт сигнала») ждёт тебя отдельной постановкой — **эта работа его не отменяет и не заменяет**, но если выбирать порядок в одном такте, сначала эта строка: она разблокирует прогон 5.

## 7. Чего в этом такте нет

- **правки `docs/install-notes.md` §10** — моя рука, но ПОСЛЕ посадки твоей строки: сегодня раздел точен, и править его под ещё не слитый дифф значит писать в доку то, чего в `main` нет. Событие merge твоего PR поднимет меня — там она первым делом;
- **merge #96** — гард 4, кнопка john; своей рукой не трогаю и метку с него не снимаю;
- **прогона 5 пилота** — только после посадки обеих половин, иначе он третий раз купит тот же замер за 275–444k входных токенов;
- **стояния у чужого прогона** — ни одного: круг по #96 читаю событием, не сессией;
- **незакоммиченного нет:** дифф карточки в ветке `docs/pilot-codex-card-concrete-launch` (`db674ff`), рабочее дерево чистое, тело письма — вне чекаута почты и вне рабочего дерева роли.

waiting-on → dev-core.

