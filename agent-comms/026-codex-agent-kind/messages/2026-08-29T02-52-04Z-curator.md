---
from: curator
worker: claude-code
session: 4bbfb517-1326-4fe4-9c54-97be7e9561e0
date: 2026-08-29T02:52:04Z
expects: ack
waiting-on: curator
parked-on: john
---

**Требуется решение john: кнопка на #96** — карточка `pilot-codex`, `approve` есть, гарды 1, 2 зелёные моей рукой, гард 4 STOP (док власти). Это последнее, что держит прогон 5 пилота: моя половина (#97) в `main`, вторая половина — за твоей кнопкой.

## 1. Merge #97 — гарды поимённо

Мёрж squash, **02:44:44Z**, `base.ref: main` (контрольное чтение `gh pr view 97 --json state,mergedAt,mergeCommit,baseRefName` → `state: MERGED`), коммит слияния — полный SHA `0d470cac04472a2f28295f543e8529fae0917c01`.

1. **Гард 1** — `ok`: `approve` на текущей голове `923c7f21acd987b0c29b0db471dddb8c5126c699`, вердикт `github-actions` ВНУТРИ закрытого успешного круга `33228967195` воркфлоу `Claude PR Review` на этой же голове (окно `02:26:48Z…02:34:39Z`). Вердикт-сироты нет. В ленте он лежит сообщением `026/2026-08-29T02-34-21Z-reviewer-pr.md`;
2. **Гард 2** — `ok`: `review=SUCCESS, checks=SUCCESS` на той же голове `923c7f21ac…` (прогон `checks` — `33227587201`). **Дверь подняла ноту «база уехала»**: `checks` стартовали 01:53:52Z, а база сдвинулась `e4a8f8c` в 02:33:27Z, то есть зачтённый зелёный снят с дерева, которое уже не результат этого слияния. Ноту закрыл ПРОГОНОМ, а не рассуждением: `git merge-tree --write-tree origin/main 923c7f2` → чистое дерево `f3ac2841…` без конфликтов, materialised во временный worktree, и на нём — `pnpm --filter agent-protocol test` → **156 файлов / 2543 passed**, `config check --ref HEAD` → `exit 0`, точечный прогон сторожа → 1 файл / 2 passed. Дельта к числу dev-core (156 / 2524) — `+19` тестов, и это ровно тесты сдвинувшей базы `e4a8f8c` (`notify.test.ts`, `notify.process.test.ts`, `thread.test.ts`), а не мои. Регрессии от сдвига нет. **Честная оговорка:** `pnpm test` целиком во временном дереве упал на `transport-telegram` с `Cannot find module 'agent-protocol'` — это артефакт МОЕЙ сборки временного дерева (я симлинкнул `node_modules` корня и `agent-protocol`, а рабочую ссылку `transport-telegram` — нет), а не находка: на голове ветки CI прогнал этот пакет зелёным (2 файла / 7 passed), и сдвиг базы его файлов не касается;
3. **Гард 3** — восхождение: тред `026-codex-agent-kind`, решение john «БЕРИ» ~01:36Z, доставлено полем `delivers: john` в сообщении `026/2026-08-29T01-36-11Z-curator.md`; постановка — `026/2026-08-29T01-47-10Z-curator.md`. В описании PR — строки `thread: 026-codex-agent-kind` и `role: dev-core`. **Класс «полевой измеренный дефект» здесь НЕ объявлялся и не нужен**: слово john в ленте есть, восхождение обычное;
4. **Гард 4** — `ok`: 2 изменённых пути (`package.json`, `packages/agent-protocol/src/launcher-form.test.ts`), доков власти среди них нет. Дверь при этом вывела список из СЕМИ доков власти данными (4 карточки ролей + сам конфиг + `PROTOCOL.md` и `.github/workflows` полем `powerDocuments`) — судила полным списком, а не моей памятью;
5. **Гард 5** — это сообщение.

Ветка `fix/protocol-script-loader-form` на origin **уже не висит** — поправка к §5 твоего письма `02-4xZ`, dev-core, и она в твою пользу: удалять нечего. `git push origin --delete` ответил `remote ref does not exist`, контрольное чтение `git ls-remote origin refs/heads/fix/protocol-script-loader-form` → пусто. Чем именно она снята — настройкой автоудаления репозитория или чем-то ещё — я НЕ мерил и диагноза не подаю; замерен только исход: ветки нет, и ничьей руки на необратимое действие не требуется.

## 2. Вторая половина — §10 `docs/install-notes.md` правлен, PR #100

Ветка `docs/install-notes-loader-form-landed`, голова — полный SHA `3943fa6e82750ec4b75610250d2b74fc9924ac46`, база `0d470ca` (уже с посаженной строкой). Это §5.2 и §7 моей же постановки: «моя рука, ПОСЛЕ посадки строки в `main`».

Исправлены три неточности и добавлен один факт:

1. **«`pnpm protocol` — мёртвая команда»** (шапка §10 и абзац про карточку `pilot-codex`): неверно с `0d470ca`. Отклонением осталась не команда, а ФОРМА РАЗВЁРТКИ — любая строка, зовущая бинарь-обёртку `tsx` напрямую. Добавлен абзац «Что изменилось 2026-08-29» с обоими состояниями;
2. **цитата старой строки `package.json`** в «Границах» заменена на живую, с указанием, чем она была и каким коммитом сменилась. Перемерено и соседнее утверждение того же абзаца: `pnpm protocol` в `.ts` пакета встречается теперь ДВАЖДЫ и оба раза в комментарии (`orchestrator/doctor.process.test.ts:205`, `launcher-form.test.ts:19`) — напечатанной строки нет ни одной (проверено `grep`, а не памятью: старая формулировка «ровно один раз» уже была бы неточной);
3. **«CI это правило не сторожит»**: половина умерла вместе с диффом dev-core. Теперь — CI сторожит ТЕКСТ строки (`launcher-form.test.ts`), а поведение ПОД ПЕСОЧНИЦЕЙ не сторожит по-прежнему никто, кроме живого прогона роли. Две границы разведены явно;
4. **добавлен замер** dev-core на голове `923c7f2` (его письмо `026/2026-08-29T01-55-49Z-dev-core.md`, §2.3) в список «Что ЗАМЕРЕНО» — он снят с дерева, где строка лежит В КОММИТЕ, и именно он отменяет «мёртвую команду».

**`checks` по #100 — зелёный, метка `review` повешена моей рукой ПОСЛЕ него на той же голове.** Числа сняты своей рукой (канал `ci-outcome` заморожен, событием исход не приезжает): `gh run view 33229801548` → `status: completed`, `conclusion: success`, `headSha 3943fa6e82750ec4b75610250d2b74fc9924ac46`, `updatedAt 02:51:09Z`; `gh pr checks 100` → `checks pass 3m55s`. Метка — в **02:51:14Z**, контрольное чтение `gh pr view 100 --json labels,headRefOid` → `labels: [review]`, голова та же `3943fa6e…`. У круга не стою: вердикт приезжает событием, и его ждёт не сессия.

**`packages/agent-protocol/README.md` не правлю, и отказ dev-core (его §3) считаю правильным:** README объявляет точкой входа `pnpm protocol <command>`, и это утверждение осталось верным — сменилась развёртка скрипта, а не форма команды. Отдельного круга под него не завожу.

## 3. #96 — что замерено моей рукой и чего ждёт

`pnpm protocol merge-gate --ref origin/main --pr 96 --review-workflow 'Claude PR Review'`:

- **гард 1 `ok`** — `approve` на голове `db674ff324371a139970eb045ac149f748c1cce2`, внутри закрытого круга `33227296640` воркфлоу `Claude PR Review` на этой же голове (`01:46:39Z…01:51:26Z`); вердикт в ленте — `026/2026-08-29T01-51-09Z-reviewer-pr.md`;
- **гард 2 `ok`** — `review=SUCCESS, checks=SUCCESS` на той же голове. Нота «база уехала» (мой же merge `0d470ca` в 02:44:44Z) закрыта прогоном: `git merge-tree --write-tree origin/main db674ff` → дерево без конфликтов; дифф #96 — один файл `docs/roles/pilot-codex.md`, с посаженной строкой `package.json` не пересекается ничем;
- **гард 4 `STOP`** — `docs/roles/pilot-codex.md`, док власти. **Кнопка твоя, john, и никакой другой рукой она не нажимается** — включая мою, хотя PR мой;
- `mergeable: MERGEABLE`, `mergeStateStatus CLEAN`, метка `review` висит с 01:46:38Z.

## 4. Цена парковки, которую называю вслух

Паркуюсь на тебя, john, и **это замораживает тред целиком** — включая круг ревью по #100: вердикт по нему приедет в ленту, но поднять по нему никого парковка не даст, пока ты не скажешь слово. То есть готовый к merge #100 будет стоять без исполнителя ровно столько, сколько стоит парковка. Плачу эту цену сознательно: без кнопки на #96 прогон 5 пилота всё равно не стартует, а держать тред живым ради merge #100 значит звонить тебе на такт позже и потратить лишний подъём. Разморозка — твоё слово здесь или доставка через чатного curator; после неё следующая сессия делает три вещи подряд: merge #100 по гардам (если круг досудил), проверка что #96 в `main`, и прогон 5.

## 4-бис. Прогон 5 — команда готова, и одну развилку закрываю здесь, чтобы её не открывали заново

Первой строкой следующей сессии, СРАЗУ после того как #96 сядет в `main`:

```
pnpm protocol orchestrator run --ref origin/main --role pilot-codex \
  --thread 038-pilot-codex-live-run --wall-clock 420 --idle 120 --write
```

Рычаги — из карточки, без флагов (`gpt-5.4-mini` / `low` / аккаунт `codex-main`), и приёмка
сверяется **по ЖУРНАЛУ прогона, а не по тексту ответа роли** (прогон 4 показал, что роль может
отчитаться формой, которой не запускала).

**Соблазн, который отвергаю явно:** поднять прогон 5 прямо сейчас на ref ветки #96
(`--ref origin/docs/pilot-codex-card-concrete-launch`), как делался прогон 4. Нельзя: ветка стоит
на `db674ff`, то есть на базе БЕЗ посаженной строки `package.json` — обе половины на этом ref не
встречаются. А догнать базу ребейзом значит сдвинуть голову и обнулить `approve` гарда 1, купив
john второй круг ревью ради одного замера. Дешевле кнопка.

## 5. Состояние на момент письма

- `main` = `0d470cac04472a2f28295f543e8529fae0917c01`, рабочее дерево `curator` чистое, незакоммиченного нет — обе правки в коммитах и запушены;
- **открытых PR по этому треду два**: **#96** (моя рука, `approve`, гарды 1–2 зелёные, кнопка john) и **#100** (моя рука, §10; зелёный `checks` и метка `review` на голове `3943fa6e…`, ждёт вердикта круга — кнопка по нему МОЯ: доков власти дифф не трогает);
- **#99** (тред `042`) — не мой предмет и этим ходом не тронут;
- **прогона 5 пилота нет** — он мой и только после посадки ОБЕИХ половин; поднимать его на одной половине значит третий раз купить тот же замер за 275–444k входных токенов;
- **стояния у чужого прогона нет ни одного**: исход `checks` по #100 прочитан ОДНОЙ командой перед отправкой этого письма (канал `ci-outcome` в этом контуре заморожен — §1 `docs/install-notes.md`), круги ревью по #96 и #100 сессией не ждались;
- тело письма — в `mktemp -d` вне чекаута почты и вне рабочего дерева роли.

