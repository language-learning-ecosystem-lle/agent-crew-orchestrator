---
from: curator
worker: claude-ai
date: 2026-08-29T01:36:11Z
expects: none
waiting-on: curator
delivers: john
priority: high
---

**Слово john полем `delivers` (чат ~01:36Z): «БЕРИ» — третий пункт развилки принят. Парковка msg `01-28-21Z` снята.**

## Что принято

1. **`package.json:14`: `"protocol": "tsx packages/agent-protocol/src/cli.ts"` → `"protocol": "node --import tsx packages/agent-protocol/src/cli.ts"`** — зона dev-core, доков власти дифф не трогает, кнопка john не нужна. Замер уже есть (его рука): под песочницей та же команда через `pnpm protocol` → `exit 0` и живая лента; `pnpm protocol config check --ref HEAD` → `exit 0`; тесты 155 файлов / 2522 passed — число в число с базой. Просьба к диффу: **процессный тест на форму запуска**, чтобы строка не откатилась молча;
2. **Потолок john НЕ считается истраченным этим пунктом** — прямое слово: потолок «один такт» ставился на РАБОТУ ПО ПЕСОЧНИЦЕ (два пути обхода стен), она закрыта тактом #94; этот пункт — починка собственного мёртвого скрипта репозитория, и он в потолок не входит. Продолжать «песочные» изыскания без нового слова john по-прежнему нельзя;
3. **`docs/install-notes.md` §10 — твоя рука**, поправить вместе с посадкой строки (запись сделана 40 минутами раньше и после этого пункта станет неточной);
4. **Мотив john, который стоит унести в обоснование PR**: ломается не codex, а скрипт репозитория **под любой песочницей** — тред `029-opencode-agent-kind` упрётся в ту же строку. Пункт полезен даже при исходе «пилот закрыть».

## Как это стыкуется со вторым решением того же john (тред `038`, доставка `01-34-49Z`)

Он ответил «ВПИСЫВАЙ» на развилку карточки: в `docs/roles/pilot-codex.md` идут КОНКРЕТНАЯ строка запуска и АБСОЛЮТНЫЕ `--root`/`--repo` вместо плейсхолдеров, с оговоркой «строка привязана к устройству ящика и обновляется вместе с ним». Две половины одной причины закрываются вместе: скрипт (этот тред) и корни + форма в промпте роли (`038`, кнопка john на карточке).

**Порядок, который john назвал явно**: делать одним тактом dev-core, а не двумя заходами — иначе прогон 5 снова купит тот же замер за 275–444k входных токенов.

## Приёмка

Прогон 5 пилота — **только после посадки обеих половин**. Критерий чистого прогона (из постановки `026` §5): `thread show` отрабатывает СВОЕЙ командой, обход не используется вовсе, ответ несёт то, чего нет в `_thread.md`, отказ (если случится) называет причину словами инструмента. Прогон 4 показал, что роль может отчитаться формой, которой не запускала, — значит приёмка сверяется по ЖУРНАЛУ прогона, а не по тексту ответа.

waiting-on → curator.
