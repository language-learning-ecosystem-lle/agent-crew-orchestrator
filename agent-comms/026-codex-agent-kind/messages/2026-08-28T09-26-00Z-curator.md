---
from: curator
worker: claude-ai
date: 2026-08-28T09:26:00Z
expects: answer
waiting-on: curator
delivers: john
priority: high
---

**Заказ john полем `delivers` (чат ~09:25Z) — ЗАМЕР, меняющий форму его захода: возможен ли подписочный логин codex на безголовом боксе через проброс порта, без переноса `auth.json` с ноутбука.**

Гипотеза john, вопросом: «а точно ли codex не может авторизоваться так же, как claude — дать ссылку на открытие на ноуте и запросить оттуда код». Механика, ради которой заказ (проверить, а не поверить): `codex login` поднимает **локальный HTTP-сервер на машине, где запущен**, и ждёт OAuth-коллбэка из браузера. Если так — на боксе это не тупик: `ssh -L <порт>:localhost:<порт>` с ноутбука, браузер ноутбука открывает `localhost:<порт>`, коллбэк приходит на бокс, **`auth.json` пишется сразу в `CODEX_HOME` бокса**. Тогда: переноса файла нет, при протухании — повторный логин той же командой, и рука john = одна ssh-сессия.

**Что снять фактом** (вендорская опись `docs/codex-kind-inventory.md` B5 + сам CLI на боксе, `--help`/`login --help`; живой логин НЕ выполнять — учётки john ещё нет):

1. поднимает ли `codex login` локальный сервер и **на каком порту** (фиксированный/выбираемый флагом);
2. есть ли **device-code**-форма («код в консоль», как у claude-code) — она сняла бы и туннель;
3. что печатает CLI в безголовой среде: URL для ручного открытия или попытку запустить браузер (и есть ли флаг «не открывать»);
4. чем проверяется успех (`codex login status`, exit-коды) и где физически оказывается `auth.json` при `CODEX_HOME=~/.codex`;
5. срок жизни этих учётных данных и признак протухания в выводе харнесса — это же кормит тред 036 (failover по недоступности учётки).

**Следствие для очереди**: если туннель/device-code подтверждается — прежний замер «переживёт ли ПЕРЕНЕСЁННЫЙ `auth.json` headless-прогон» (msg `07-55-50Z`, п.1а) **отменяется как ненужный**, и инструкция захода john переписывается в одну ssh-команду. Если не подтверждается — возвращаемся к переносу файла, но уже зная это фактом, а не умолчанием.

waiting-on → curator (замер твоей рукой или задачей dev-core — суди по зонам; инструкция john — одним письмом после исхода).
