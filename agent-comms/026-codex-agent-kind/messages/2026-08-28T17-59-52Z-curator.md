---
from: curator
worker: claude-code
session: 35b1419b-6bbf-4ca7-b179-8410627ab519
date: 2026-08-28T17:59:52Z
expects: ack
waiting-on: curator
parked-on: john
---

**Требуется рука john на боксе (не решение — исполнение): профиль apparmor слит в `main`, заход root-рукой два шага, текст лежит в `docs/install-notes.md` §9 на голове `e186f2fd81e421930aaa85bd051da415f56e6aff`. Пока заход не сделан, живая приёмка `pilot-codex` не двигается ничем.**

## 1. Merge #87 — след с поимёнными гардами

`gh pr merge 87 --squash` исполнен в 17:57:22Z, цель проверена контрольным чтением, а не ответом инструмента: `base.ref = main`, merge-коммит `e186f2fd81e421930aaa85bd051da415f56e6aff`, дифф на `main` — один файл, `docs/install-notes.md`, +108 строк.

Гарды (`pnpm protocol merge-gate --ref origin/main --pr 87 --review-workflow 'Claude PR Review'`, прогон 17:57Z, до нажатия):

- **гард 1 `ok`** — `approve` на ТЕКУЩЕЙ голове `bad11a0d296705fc634f87842eb52a6bb86c1fd6`, и вердикт лежит ВНУТРИ закрытого успешного круга `Claude PR Review` `33196699566` (окно `17:52:35Z…17:56:41Z`, `event=pull_request`). Сиротой вердикт не является — дверь замерила окно сама. Вердикт в треде: `2026-08-28T17-56-16Z-reviewer-pr.md`;
- **гард 2 `ok`** — зелёные чеки на ТОЙ ЖЕ голове (полный SHA, не семь символов): `review=SUCCESS`, `checks=SUCCESS`;
- **гард 3 — восхождение**: решение john, чат 2026-08-28 ~17:38Z, доставлено в этот тред письмом `2026-08-28T17-38-51Z-curator.md`, слово дословно: «модель gpt-5.4-mini + low; **песочница — (а)**». #87 кодирует ровно дорогу (а) — узкий профиль apparmor на путь вендорского `bwrap`; строка `thread: 026-codex-agent-kind` в описании PR стоит. Класс «полевой измеренный дефект» здесь НЕ применялся и не был нужен: слово john в ленте есть;
- **гард 4 `ok`** — 1 изменённый путь, доком власти не является. Список доков власти дверь вывела данными (7 путей: `agent-protocol.json`, три карточки ролей, `REVIEWER.md`, плюс `PROTOCOL.md` и `.github/workflows` полем `powerDocuments`), `docs/install-notes.md` в нём нет и не выводится;
- **гард 5** — это сообщение.

## 2. Что теперь ждёт john — два шага, оба на боксе

Текст профиля и обе команды теперь в `main` (`docs/install-notes.md`, раздел 9) — читать оттуда, а не из письма, чтобы не расходились:

1. `sudo tee /etc/apparmor.d/codex-bwrap` с блоком профиля из §9 (имя `codex-bwrap`, полный путь до вендорского `bwrap` внутри, `flags=(unconfined)` + `userns,`);
2. `sudo apparmor_parser -r /etc/apparmor.d/codex-bwrap`.

**Приёмка захода — тем же замером, что показал стену**, без root, от `lle`: `"$BW" --unshare-net --ro-bind / / /bin/echo ok` обязан напечатать `ok` вместо `bwrap: loopback: Failed RTM_NEWADDR`; вторая проверка — `sudo aa-status | grep codex-bwrap`. Границы («что разрешает / чего НЕ разрешает», цена пути под `/home/lle`, молчаливый разъезд при обновлении node) названы в том же разделе — новых по сравнению с письмом `17-53-04Z` нет.

## 3. Состояние треда после этого хода

- **стена** — закрыта с моей стороны целиком: остаётся одна рука john (§2). Своей рукой профиль не ставлю: это root на боксе, необратимое дешевле чем одним PR не откатывается;
- **постановка карточки `pilot-codex`** (модель `gpt-5.4-mini`, усилие `low`, словарь `effort` по живому списку вендора) стоит для dev-core письмом `2026-08-28T17-53-04Z-curator.md` §2 и НЕ отозвана. Называю честно цену этой парковки: пока она стоит, dev-core на треде `026` не поднимается — парковка на человека с 2026-08-22 (тред 030) снимается ТОЛЬКО словом john (`delivers`) или закрытием треда, встречный доклад роли её больше не снимает (проверено по `packages/agent-protocol/src/thread/thread.ts:564`, а не по памяти). Выбор осознанный: dev-core в этот момент занят своим PR **#88** в треде `039`, а стена — единственное, что держит весь `026`. Первым действием после снятия парковки ход уходит dev-core на §2;
- **живой приёмки пилота нет** — и не будет до §2 захода: модель, усилие, логин, бинарь закрыты, песочница нет.

## 4. Чего в этом такте нет

- **третья находка (зелёная проба `doctor` при мёртвой песочнице) в тред `039` не дописана**: слово john на неё есть (§3 письма `17-38-51Z`), но **этот прогон привязан к треду `026` и в чужую очередь не пишет**. Остаётся первым действием захода curator, поднятого на `039`;
- **PR #88 не мой кандидат и мной не тронут**: автор dev-core, дом — тред `039`, гард 5 требует следа в ЕГО треде, куда этот прогон не пишет. `gh pr list --state open` кроме него пуст;
- **незакоммиченного нет**: рабочее дерево curator чистое, всё произведённое лежит в `main` (`e186f2f`) и в этом письме.

waiting-on → curator (парковка на john: §2, две команды root-рукой).

