---
from: reviewer-pr
worker: gh-action
date: 2026-08-28T08:20:21Z
expects: answer
waiting-on: dev-core
---

verdict: approve
pr: 79

Постановка — `thread: 026-codex-agent-kind`, `role: dev-core`; постановка треда — `2026-08-28T07-43-57Z-curator.md`, §1 (девять пунктов П1). Дифф сверен с ней построчно, находок нет.

Полный прогон тестов не повторял — `checks` на голове `9675ac7114b8865683d4af5d62e620bc57edf018` зелёный, прогон `33154233196`. `pnpm typecheck` прогнан живьём (exit 0, оба пакета). `pnpm exec biome check agent-protocol.json docs/roles/pilot-codex.md` — «Checked 1 file… No fixes applied» (markdown вне biome).

Живой исход `pnpm protocol merge-gate --ref origin/main --pr 79`:
```
merge-gate: documents of power judged by (6): agent-protocol.json, docs/roles/curator.md,
docs/roles/dev-core.md, REVIEWER.md, PROTOCOL.md, .github/workflows
  STOP guard 1 · approve on the current head: no approve verdict on 9675ac7
  STOP guard 2 · green checks on the same head: not green: review=IN_PROGRESS
  you  guard 3 · ascent to a decision of john's: thread '026-codex-agent-kind'
  STOP guard 4 · no self-merge on the documents of power: john merges this one — it changes agent-protocol.json
  you  guard 5 · a trace of the merge
  ok   mergeability · mergeable=MERGEABLE (mergeStateStatus UNSTABLE)
REFUSED: a guard does not hold
```
Гард 1/2 ожидаемо не держат до этого вердикта. **Гард 4 подтверждает независимо от текста PR**, что `agent-protocol.json` — док власти: критерий 5 применяется, кнопка — john, `waiting-on` — не curator (см. ниже).

**Критерий 1.** Числа из отчёта dev-core (`149 файлов, 2442 теста` в `packages/agent-protocol`; `2 файла, 7 тестов` в `transport-telegram`, всё зелёное) сверены с логом прогона `33154233196` дословно (`grep -iE 'Tests|Test Files'`) — совпадают точно. Новых/удалённых тестов PR не заявляет и не вводит — заявлено явно («отдельного юнита нет», §3 постановки) и подтверждено диффом (только `agent-protocol.json` + `docs/roles/pilot-codex.md`, пакетов нет).

**Критерий 2.** Заявленное покрытие существующим `v20-codex-levers.test.ts` — прочитан файл целиком: тест на `WAIVER = { kind: "codex", toolsHeldBy: "sandbox-read-only" }` бьёт ровно в то, что объявляет запись роли — `leversHeldOutside(WAIVER)` даёт `["allowed-tools", "zone-deny"]` и НЕ включает `max-turns` (`v20-codex-levers.test.ts:89`), `kindLeverRefusal` с этим набором отказывает по `max-turns`, но не по зонам (:97-113), argv несёт `--sandbox read-only` (П1-3). Тест не про соседнее — про ровно то, что заявлено.

**Критерий 3.** Скоуп не расширен и не сужен молча: девять пунктов §1 постановки — `id/kind/status`, `launch.agent` с `toolsHeldBy`, отсутствие `allowedTools`, `account: codex-main`, `limits` (900/180, обоснование — «нет фазы ожидания» в отчёте), `instances[hetzner].roles` пополнен, `permissions: []`, `zones` отсутствует по суждению («не изображать границу»), `wake: watch` по образцу — сверены с фактическим `agent-protocol.json` (`jq '.roles[] | select(.id=="pilot-codex")'`) один в один. П2 (замер по `--sandbox read-only`) доставлен фактами в тред, кодом не оформлен, как требовала постановка. Находка §4 отчёта (`resolveExec` падает в `DEFAULT_EXEC="claude"` для любого kind, `launch.ts:432`) не тронута кодом — постановка §3 это запрещала прямо, дом находки за curator, не за этим PR.

**Критерий 4.** `agent-protocol.json` не в `forbidden` роли `dev-core` (`zones.writes: [], forbidden: ["docs/roles"]`) — живой `pnpm protocol zones check --ref origin/main --role dev-core --paths agent-protocol.json` → «none under a forbidden prefix». `docs/roles/pilot-codex.md` — отдельный коммит `0f0f7f1`, автор `curator` (в его `zones.writes`), не `dev-core`; `git log --format='%an'` подтверждает разных авторов на двух коммитах одной ветки. Правка самих зон/прав/потолков — не этот PR (запись новой роли, не правка существующих).

**Критерий 5.** `agent-protocol.json` и `docs/roles/**` — доки власти; PR прямо называет это в описании («Кнопка — john, гард 4») и подтверждено живым `merge-gate` выше. Мёржит john, не curator.

**Критерий 6.** `protocolVersion` не тратится — заявление проверено по коду, не на слово: `roles[].launch.agent.toolsHeldBy`, значение `"sandbox-read-only"` и значение `effort = "minimal"` уже стоят в таблицах версии 20 (`schema/shape.ts:576,694,690`). Запись PR использует существующие пути/значения, новых не добавляет — гард формы не обязан заговорить, и не заговорил (`config check` зелёный на этой голове).

**Критерии 7, 8, 10, 11** — не применимы: флаков не заявлено, `agent-comms/**` в диффе нет, конфиг не читается напрямую мимо пакета (сам файл — данные, не код), дверей-заглушек в диффе нет.

**Критерий 9.** Отдельно сверенные утверждения текста против факта: «doctor/preflight отказывают по имени, пока учётки codex на боксе нет» — не переснято дословно в этой среде (здесь раньше `pilot-codex` в списке ролей отказывает `curator`/`lle-main` тем же классом сообщения — `role 'curator': the role is to be raised on account 'lle-main', and this machine declares no such account`), но идентичный шаблон отказа воспроизведён живьём для другой роли, и механизм (отказ по отсутствующему `accounts.<id>` в машинном конфиге) общий для всех ролей одним и тем же путём кода — расхождения с заявленным не найдено. Остальные проверяемые утверждения («не тратит protocolVersion», «покрытие даёт существующий тест», числа тестов, `config check`/`typecheck` вывод) — все подтверждены живым прогоном дословно.

waiting-on → dev-core (не curator: PR меняет доки власти — `agent-protocol.json` и `docs/roles/**` — и curator мёржить его не вправе; кнопка за john, что уже сказано в описании PR и подтверждено гардом 4 живого `merge-gate`).

