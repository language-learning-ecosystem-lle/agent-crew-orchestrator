---
from: curator
worker: claude-ai
date: 2026-08-28T23:38:02Z
expects: none
waiting-on: curator
delivers: john
---

**Толчок словом john (чат ~23:37Z, поле `delivers` — чтобы лифт сработал): ход этого треда лежит непринятым с `21:33:14Z`, два часа. Поднимайся и веди такт.**

Состояние, сверенное по ленте, чтобы такт не начинался с чтения:

- **#92 сел** (`main` = `49cb3f5`), приёмка повторена dev-core на СМЁРЖЕННОМ main: цикл «рендер → форматтер → зелёный lint» замкнут (179 → 212 → 179 строк, `biome` exit 1 → exit 0), целевые тесты 20/20, открытых PR в репозитории нет, дерево роли чистое;
- **#91 (тред 040) сел чужой рукой** — контракт самоперезапуска пошёл в работу;
- у dev-core в этом треде **не осталось ничего своей рукой**: стоят ровно два предмета, и оба несутся к john ЧЕРЕЗ ТЕБЯ — (i) развилка по пилоту (читатель / постановка «CLI под песочницей codex» / закрыть пилот) и (ii) две находки словаря усилий: **дыра `max`** (его несут четыре модели из шести, но НЕ `gpt-5.4-mini` из карточки пилота — дверь пропустит значение, которого модель карточки не знает) и смерть довода об асимметрии между инструментами.

**Собери оба одним письмом john'у** — он именно так и просил, и он у ноутбука не постоянно: письмо должно быть самодостаточным (вопрос, варианты, цена каждого, твоя рекомендация), чтобы ответ был одной строкой. Живой проход пилота на карточке (`gpt-5.4-mini` / `low`, песочница работает с 18:05Z) — твоя половина приёмки, её john ждёт числами.

waiting-on → curator.
