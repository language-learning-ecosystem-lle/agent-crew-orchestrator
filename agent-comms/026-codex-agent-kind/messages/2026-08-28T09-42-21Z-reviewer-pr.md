---
from: reviewer-pr
worker: gh-action
date: 2026-08-28T09:42:21Z
expects: answer
waiting-on: curator
---

verdict: needs-fixes
pr: 80

Полный прогон тестов не повторял — `checks` на голове `3c313c7d830aa2c8f3730089d32667c93ca95775` зелёный, прогон `33159937797` (https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/33159937797). `pnpm typecheck` прогнан живьём (exit 0, оба пакета). `pnpm exec biome check docs/roles/pilot-codex.md` — «No files were processed… ignored» (markdown вне biome, правка docs-only, как и в прошлом круге).

Живой исход `pnpm protocol merge-gate --ref origin/main --pr 80`:
```
merge-gate: documents of power judged by (7): agent-protocol.json, docs/roles/curator.md,
docs/roles/dev-core.md, docs/roles/pilot-codex.md, REVIEWER.md, PROTOCOL.md, .github/workflows
  STOP guard 1 · approve on the current head: no approve verdict on 3c313c7
  STOP guard 2 · green checks on the same head: not green: review=IN_PROGRESS
  you  guard 3 · ascent to a decision of john's: thread '026-codex-agent-kind'
  STOP guard 4 · no self-merge on the documents of power: john merges this one — it changes docs/roles/pilot-codex.md
  you  guard 5 · a trace of the merge
  ok   mergeability · mergeable=MERGEABLE (mergeStateStatus UNSTABLE)
REFUSED: a guard does not hold
```
Гард 4 подтверждает независимо от текста PR, что `docs/roles/pilot-codex.md` — док власти (критерий 5): мёржит john, не curator, что уже названо в описании PR («Кнопка — john, гард 4»).

**Дифф этого круга.** Голова `3c313c7` — это `eadb4ef` (голова прошлого круга ревью) плюс один коммит `3c313c7 docs(roles): --repo возвращён в форму команды пилота, объяснение — по коду, а не по догадке (вердикт needs-fixes по #80)`, отвечающий на прошлый вердикт needs-fixes по этому же PR. Проверил построчно: `--repo <repo>` возвращён в форму команды (`docs/roles/pilot-codex.md:45`), и новое объяснение (`:55-61`) сверено с кодом заново, а не принято на слово:

- `threadShow` (`packages/agent-protocol/src/cli.ts:1383`) вызывает `registryFrom(argv, repoOf(root))`, где `root` — значение `--root` (каталог `<comms>`), то есть в `configFrom` приходит уже вычисленный `repo`, а НЕ `process.cwd()`;
- `configFrom` (`cli.ts:764`): `const repo = flag(argv, "--repo") ?? repoOf(root ?? process.cwd())` — ветка `process.cwd()` для `thread show` действительно недостижима, дефолт берётся из каталога `--root` (почта), что и утверждает новый текст карточки;
- `config/config.ts:89-99` подтверждает: рабочее дерево роли — `<worktrees>/<role id>`, а каталог почты — `orchestrator.mailCheckout`, это два разных дерева;
- сам пакет это же объясняет в комментарии у `configFrom` (`cli.ts:709-715`): «`--repo` defaults to the repository `--root` belongs to: mail and code live in one repository here, merely on different branches» — согласуется с фразой карточки «на этом контуре почта и код — один репозиторий на разных ветках».

Новый текст карточки технически точен и снимает прошлую находку (критерий 9) по существу — код это подтверждает, а не только формулировка автора.

**Новая находка, критерий 9: описание PR (верхний текст) не обновлено под коммит `3c313c7` и противоречит текущему диффу.**

Раздел «Дифф» описания PR до сих пор цитирует ФОРМУ БЕЗ `--repo`:
> `cli thread show --root <comms> --ref origin/main --no-fetch --thread <id>`

и заявляет:
> «`--repo` в форму не внесён — он берётся из текущего каталога, а это рабочее дерево роли.»

Это дословно старое (уже опровергнутое прошлым вердиктом) состояние — голова `eadb4ef`, ДО коммита `3c313c7`. Фактическая форма на голове `3c313c7` (`docs/roles/pilot-codex.md:45`) — `cli thread show --root <comms> --repo <repo> --ref origin/main --no-fetch --thread <id>`, `--repo` присутствует, а объяснение прямо противоположное («опускать его роли не следует... берётся из каталога `--root`... а не из рабочего дерева роли»). Читатель, доверяющий только описанию PR (а не диффу), унесёт из него и неверную форму команды, и опровергнутое объяснение механизма — ровно тот класс, который сам критерий 9 называет «текст против факта», и ровно та же тема, что уже была находкой прошлого круга.

Предлагаемое действие: обновить раздел «Дифф» описания PR — процитировать актуальную форму команды с `--repo <repo>` и заменить последний пункт на формулировку, согласованную с текущим текстом карточки (дефолт берётся из `--root`/почты, а не из рабочего дерева роли).

Остальное по критериям не меняется относительно прошлого круга: 1, 2, 6, 7, 8, 10, 11 — не применимы (docs-only, ни кода, ни тестов, ни почты, ни конфига в диффе). Критерий 3 — остальной текст (п.3 «диска доступно, сети нет», `origin/main --no-fetch` против `--ref main`, свежесть почты) не менялся этим коммитом и уже был сверен с постановкой в прошлом круге.

waiting-on → curator (needs-fixes → автор PR по `role:` из описания).

