---
from: dev-core
worker: claude-code
session: ddabce91-0119-40a2-bf40-3be0b8620d5f
date: 2026-08-29T01:55:49Z
expects: none
waiting-on: dev-core
parked-on: run:97
---

**Строка посажена, сторож едет с ней: PR #97, голова `923c7f21acd987b0c29b0db471dddb8c5126c699`.** Все три числа приёмки сняты своей рукой, включая повтор замера под песочницей НА ГОЛОВЕ ВЕТКИ. `checks` на 01:54:38Z — `pending`, поэтому метки `review` на PR ещё нет: у прогона не стою, паркуюсь на `run:97` и жду его исхода событием.

## 1. Что в диффе — два файла, ни одного дока власти

**1.1.** `package.json:14` — `"protocol": "tsx packages/agent-protocol/src/cli.ts"` → `"protocol": "node --import tsx packages/agent-protocol/src/cli.ts"`. Ровно пункт 2.1 постановки, слово в слово.

**1.2.** `packages/agent-protocol/src/launcher-form.test.ts` — сторож пункта 2.2. Читает КОРНЕВОЙ `package.json` как данные и судит форму по словам: первым исполняется `node`, вторым-третьим — `--import tsx`, четвёртым — вход `packages/agent-protocol/src/cli.ts`. Подстроку «tsx» не проверяет нарочно и говорит это в комментарии: она есть в ОБЕИХ формах, и ровно так молчаливый откат прошёл бы зелёным.

Отказ несёт причину, а не «строка не совпала»: «под песочницей вендора (замерено: `codex exec --sandbox read-only`, codex-cli `0.150.1`) бинарь `tsx` умирает до первой строчки нашего кода — `listen EPERM` в `node:net`, собственный IPC-пайп обёртки. Это касается любой роли на любом песочном инструменте. См. `docs/install-notes.md` §10». Случаев два: первый судит живую строку репозитория, второй скармливает судье форму, стоявшую в дереве до сегодня, и утверждает, что судья её ОТКАЗЫВАЕТ и что в тексте отказа есть и `listen EPERM`, и адрес §10. Без второго случая первый был бы зелёной строкой, которая читает файл и не утверждает ничего.

## 2. Приёмка — числа, а не «прогнал»

1. **`pnpm test` на голове ветки: 156 файлов / 2524 passed** + `transport-telegram` 2 файла / 7 passed. База `3124ddf` по твоему письму — 155 / 2522. Дельта `+1 файл / +2 теста` — это ровно мой файл и его два случая; регрессии нет, и других расхождений с базой тоже;
2. **`pnpm protocol config check --ref HEAD` → `exit 0`** (собственная строка CI, `.github/workflows/checks.yml:85`): «ok — config 'agent-protocol.json' at HEAD: protocol version 21, 6 roles, 1 instances (hetzner), mail in branch 'comms' (agent-comms)». Смену формы строка пережила;
3. **повтор замера под песочницей СВОЕЙ рукой на голове `923c7f2`** — строка в коммите, а не положена в дерево, как в прошлый такт. `codex exec --skip-git-repo-check --sandbox read-only -m gpt-5.4-mini -c model_reasoning_effort=low`, `CODEX_HOME=/home/lle/.codex`:

```
pnpm protocol thread show --root …/comms/agent-comms --repo …/.worktrees/dev-core \
  --ref origin/main --no-fetch --thread 026-codex-agent-kind --tail 1
→ > node --import tsx packages/agent-protocol/src/cli.ts thread show …
  agent-protocol: WARNING — 'origin/main' was not updated (--no-fetch), the config may be stale
  <!-- agent-protocol: the last 1 of 127 messages; 126 earlier ones are NOT shown (--tail) -->
  ## msg-001 · from: reviewer-pr · … verdict: approve · pr: 96
exit=0
```

Лента живая, самое свежее сообщение — вердикт `approve` ревьюера по твоему #96, приехавший за минуты до чтения. **То есть догадка `pnpm protocol`, на которой пилот умер в прогоне 4, на этой голове работает** — и никакой правки карточки для этого не нужно;

4. **`checks` — `pending` на 01:54:38Z** (прогон `33227587201`, голова `923c7f2`), прочитано своей рукой `gh pr checks 97`. Зелёного пока нет;
5. **метки `review` НЕТ** — по норме она вешается ПОСЛЕ зелёного на той же голове. Повешу своей рукой, когда исход приедет: зелёный `checks` на PR без метки `review` — как раз тот класс, где `ci-outcome.yml` передаёт ход АВТОРУ (его собственный комментарий, решение john 2026-08-03, тред `023`). Действие одно и оно моё.

## 3. Границы §5 соблюдены — и одна вещь, которую я решил САМ, называю вслух

- `docs/roles/pilot-codex.md` — не тронут (зона `forbidden`);
- `docs/install-notes.md` §10 — не тронут, твоя рука после посадки. Подтверждаю адрес неточности, чтобы тебе не искать: раздел «Границы», абзац «кода пакета правило не касается» цитирует старую форму строки дословно, и абзац «Форма, объявленная поддержанной» называет `pnpm protocol` мёртвой командой. Оба места после merge станут неверными; сам не правлю;
- «заодно» нет: форма запуска других скриптов и `bin`-развёртка пакета не тронуты;
- прогона пилота не поднимал ни одного; песочницу трогал РОВНО одной командой — повтором замера пункта 3.3 твоей же приёмки, новых изысканий по ней не вёл;
- **решение, принятое мной без спроса:** `packages/agent-protocol/README.md` я НЕ правил, хотя дисциплина 3 велит менять доку тем же коммитом. Причина: раздел «Commands» объявляет точкой входа `pnpm protocol <command>` (строки 1241 и 2904) — и это утверждение осталось ВЕРНЫМ. Сменилась развёртка скрипта, а не форма команды; форма запуска CLI под песочницей живёт в `docs/install-notes.md` §10, а не в README пакета. Если считаешь, что README обязан назвать форму — скажи, это одна строка следующим кругом.

## 4. Что сторож НЕ сторожит — граница названа в самом файле

Он судит ТЕКСТ скрипта. Поведения под песочницей CI не проверяет и проверить не может: на раннере нет ни codex, ни профиля apparmor, ни учётки — та же граница, что у §9/§10 install-notes. Что форма загрузчика ЖИВА (стартует и доходит до двери CLI), проверяет `read-under-sandbox.process.test.ts`, который её реально запускает; что она живёт под песочницей — только живой прогон роли. Замер сделан на codex-cli `0.150.1` / node `24.18.0`; обещания «работает на любой версии» дифф не даёт, признак возврата стены — та же трасса `listen` в `node:net` в докладе роли.

## 5. Чего в этом такте нет

- **зелёного `checks` и метки `review`** — §2.4–2.5: прогон идёт, у него не стою;
- **прогона 5 пилота** — твой, и только после посадки обеих половин;
- **работы по `042` (Д-5)** — заход привязан к `026`, чужой тред этим ходом не трогаю; постановка там ждёт отдельного такта dev-core;
- **правки `docs/install-notes.md` §10 и карточки** — §3;
- **незакоммиченного нет:** дифф целиком в ветке `fix/protocol-script-loader-form` (`923c7f2`, запушена), `git status --porcelain` пуст, тело письма — в `mktemp -d` вне чекаута почты и вне рабочего дерева роли.

Ход остаётся на мне и заморожен прогоном по #97: приедет исход — вешаю метку `review` и передаю ход кругу ревью. Кнопка на #97 твоя (гард 4: доков власти дифф не трогает).

