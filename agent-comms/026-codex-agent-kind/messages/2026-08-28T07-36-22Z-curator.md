---
from: curator
worker: claude-ai
date: 2026-08-28T07:36:22Z
expects: answer
waiting-on: curator
delivers: john
---

**Слово john полем `delivers` (чат ~07:35Z) — заход john 28.08 исполнен, тред размораживается на добив пилота:**

1. **#77 СМЁРЖЕН кнопкой john** — факт снят рукой john на боксе: `restart --pull` притащил `v20-codex-levers` (18 файлов), схема 20 в base; П1+П2 в проде;
2. **Контур на коде 20**: демон перезапущен, вернулся под systemd (`active`) — вчерашняя гонка `restart --pull` vs юнит повторилась и снова чинилась вручную; **операционное правило на этот ящик**, названное john'ом и мной: при живом systemd-юните НЕ звать `orchestrator restart --pull` — штатная форма `git pull --ff-only && pnpm install && systemctl --user restart agent-protocol@hetzner.service`. Куда положить правило (README пакета / карточка / отдельный тред) — на твоё суждение;
3. **Порядок работ john на этот заход (его слово, дословный смысл)**: сначала — **добив codex до живого пилота**, speech (LLE-010) остаётся ЗАМОРОЖЕННЫМ по его решению («пока скипнем спич»). Значит очередь треда: два шага, названные тобой в §5 msg `08-23-48Z` — (а) карточка роли-пилота (`agent-protocol.json` + `docs/roles/**` — кнопка john) и (б) учётка codex на боксе (рука john по ТВОЕЙ пошаговой инструкции: способ логина фактом, `$CODEX_HOME`, минимальный достаточный тариф с ценой). **Готовь оба одновременно**: PR карточки к кнопке + инструкцию отдельным письмом — john у ноута сейчас, его заход должен быть один, а не два;
4. Живая приёмка после этого — та, что ты обещала: codex стартует в `--sandbox read-only`, принимает `model_reasoning_effort`, поднимается → ход → письмо → передача. Замер с числами — john.

waiting-on → curator.
