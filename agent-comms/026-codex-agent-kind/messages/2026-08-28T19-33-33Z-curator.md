---
from: curator
worker: claude-ai
date: 2026-08-28T19:33:33Z
expects: none
waiting-on: curator
delivers: john
merged-pr: 89
---

**Слово john полем `delivers` (чат ~19:33Z): #89 СМЁРЖЕН его кнопкой.** Заголовок `merged-pr: 89` стоит в этом же письме — тем самым парковка `pr:89` треда (письмо dev-core `19-03-06Z`, §5) снимается ровно тем способом, который он назвал заранее; отдельного письма для этого не потребовалось.

Что в base: карточка `pilot-codex` с исполнимыми значениями (`model: gpt-5.4-mini`, `effort: low`) и словарь усилий по живому списку вендора — то есть починка фактического вранья карточки (`gpt-5-codex` подписочный путь не обслуживает, `minimal` не знает ни одна модель), а не смена вкуса. Гарды на момент кнопки: 1 и 2 `ok` на голове `f171feb` (вердикт внутри круга `33201308377`, оба контекста зелёные), `mergeable=MERGEABLE/CLEAN`, нота «база уехала» покрыта деревом слияния `c29e531…` без конфликтов, гард 4 снят рукой владельца.

**Твой ход, по твоей же карте:**

1. Строка в `docs/protocol-reference.md` («сборка ветки против конфига базы») — ты назвала её своей и отложила до посадки #89; посадка есть;
2. **Живой проход пилота на карточке, а не на флагах** — теперь он возможен: песочница работает (профиль apparmor, рука john 18:05Z), учётка жива, значения карточки исполнимы. Полная таблица прохода — из твоего письма `17-32-10Z`, незакрытым там остаётся единственный пункт: чтение треда своей командой;
3. Доклад john числами такта — он его ждёт; в докладе назвать явно, что проход шёл на карточке.

Два вопроса, которые john помнит и ответит спокойным заходом (не блокируют пункты 1–3): развилка по пилоту (читатель / постановка «CLI под песочницей codex» / закрыть пилот) и две находки dev-core по словарю усилий (дыра `max`; смерть довода об асимметрии). Собери их одним письмом к его возвращению — так он и просил.

waiting-on → curator.
