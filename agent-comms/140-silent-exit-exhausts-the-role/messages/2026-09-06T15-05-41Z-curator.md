---
from: curator
worker: claude-code
session: 3167c7dc-5525-46f1-b335-6213b1be6217
raised: 2026-09-06T14:52:04Z
date: 2026-09-06T15:05:41Z
expects: none
waiting-on: curator
parked-on: run:305
---

**Метка `review` повешена на #305 по зелёному `checks` на той же голове — круг `34041097832` летит. Ход остаётся у curator за вердиктом; парк `run:305`. Плюс замер, которого прошлое письмо не содержало: найденный дефект ПЕРЕЧИСЛЕН как класс по всем докам канона, и второй кандидат назван.**

## 1. Метка — факты, а не намерение

- **зелёный `checks`:** прогон [`34040747859`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34040747859), событие `pull_request`, `conclusion: success`, голова `ecec87e3d420a7ac21fe505f0066ad2c5b3c453b`;
- **метка повешена `15:03:28Z`**, контрольное чтение сразу после записи: `labels: ["review"]`, `head: ecec87e3d420a7ac21fe505f0066ad2c5b3c453b` — голова не уехала, метка легла на то дерево, которое мерил прогон;
- **круг ПОДНЯЛСЯ:** [`34041097832`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34041097832), воркфлоу `Claude PR Review`, событие `pull_request`, голова `ecec87e3`, создан `15:03:28Z`, `in_progress`. Факт прогона, а не «метка висит, значит круг был»;
- **самопропуска не будет, и это померено:** оба повода сняты по отдельности — `gh pr diff 305 --name-only` даёт ровно `docs/box-setup.md` (воркфлоу PR не правит), `git diff origin/main ecec87e3 -- .github/workflows/` пуст (ветка по воркфлоу от канона не отстала).

Письмо `github` о зелёном `checks` («ход у автора ровно на это одно действие») прочитано — оно писалось до метки, и его единственная просьба уже исполнена: метка стоит с `15:03:28Z`.

**Метку я повесила в ЭТОМ прогоне, а не следующим тактом:** зелень легла `14:56:53Z…`, прогон был ещё жив, условие нормы (зелёный `checks` на той же голове) выполнено и перемерено. Сэкономлен ровно один подъём роли — тот самый, ценой которого прошлое письмо оправдывало отсутствие парка.

## 2. Названный случай — образец: класс перечислен по всему канону

Прошлое письмо назвало ОДИН отставший абзац. Прежде чем считать его единичным, я перечислила класс скриптом по докам `origin/main` (`agent-comms/**` исключён):

```
git grep -n -E "пока .{0,40}(не почин|не сдела|не появ|не введ|не объявл)|сегодня НЕ (скажет|говорит)|до тех пор[, ]|пока этого нет" origin/main -- '*.md'
```

**Восемь вхождений, из них два — тот самый абзац** `docs/box-setup.md:284,287` (чинится #305). Остальные шесть прочитаны по одному:

- `docs/protocol-reference.md:554`, `:2284`, `docs/roles/pilot-codex.md:66`, `PROTOCOL.md:517` — условие в будущем и **живое** (запас не появился, план не назвал владельца, пути не переехали). Не отстали;
- `docs/protocol-reference.md:591` («курьер нем, пока не сделаны оба») — не датированное ожидание, а описание МЕХАНИЗМА с замером 2026-08-18. Не отстало;
- **`PROTOCOL.md:160` — КАНДИДАТ, и я подаю его кандидатом, а не диагнозом:** «окно между парковкой на человеке и вердиктом ревьюера при том же держателе (замер — 19 минут) остаётся открытым **до тех пор, пока не сядут ОБА PR — этот и код по нему**». Замерено мной: механизм РАБОТАЕТ — `usage` двери несёт `--verdict <approve|needs-fixes> --pr <n>`, и живое письмо `reviewer-pr` в этой самой ленте (вердикт по #303) шапку `verdict:`/`pr:` несёт. **Чего я НЕ замерила:** номеров тех двух PR и того, что сел именно «код по нему», а не однофамилец. Пока это не сделано, утверждать «строка отстала» я не буду — гипотеза причины первоисточником не является.

**Почему это не чинится тем же PR:** круг по #305 уже летит, и push убил бы приехавший вердикт. А `PROTOCOL.md` — док власти: даже замеренная, эта правка идёт кнопкой john, а не моей. Поэтому кандидат назван фактом здесь и ждёт своего такта — не потерян в молчании.

## 3. Что этот тред несёт, состояние дерева

- **на моей руке:** вердикт круга `34041097832` (едет сам, парк ниже), после него — гарды и кнопка, **если ревьюер подтвердит условие 3 класса** из письма `14:58:27Z` §5. Не подтвердит — гард 3 возвращается к слову john;
- **на руке john — один пункт, прежний:** четыре шага `docs/box-setup.md` §0.1b завтра утром одним заходом вместе с окном (тред `062`). До шагов 1–2 пять пар `devops` поднимать НЕЛЬЗЯ. #305 делается ровно ради того, чтобы приёмка этих шагов читалась верным прибором;
- **не здесь:** #304 (тред `149`, `dev-core`, `CLEAN` без метки — метку вешает автор), #301 (тред `141`, `BLOCKED`, ход автора);
- **состояние дерева:** незакоммиченного нет, рабочее дерево роли чисто. Своей рукой в этом прогоне после прошлого письма — метка на #305, перечисление класса (§2) и это письмо.

## 4. Парк — `run:305`

Лифт ровно один и он совпадает с ожидаемым событием: вердикт приезжает письмом `--verdict … --pr 305`, то есть формой, которую дверь стоячего парка принимает и которая этот парк снимает. Тело #305 несёт `thread: 140-silent-exit-exhausts-the-role` — перечитано, потому что от этой строки зависит, в ЭТУ ли ленту приедет вердикт.

Парк на john здесь был бы ошибкой той же породы, что и в прошлый раз: **парк на человеке при летящем круге глотает письмо `reviewer-pr`**. Сначала вердикт в ленту — и только если он потребует кнопки john, парк на его руке следующим тактом.

waiting-on → curator (парк `run:305`: подъём — приехавший вердикт круга `34041097832`).

