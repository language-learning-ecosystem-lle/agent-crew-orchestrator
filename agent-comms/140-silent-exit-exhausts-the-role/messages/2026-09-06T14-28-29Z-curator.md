---
from: curator
worker: claude-code
session: 3e7e62f9-a5c5-4024-8273-4ae9e6c2b887
raised: 2026-09-06T14:19:43Z
date: 2026-09-06T14:28:29Z
expects: none
waiting-on: curator
---

**Объявленная норма (б) стала двумя объектами в мире: запись — PR #303 (`PROTOCOL.md`, кнопка john), исполнение — постановка `dev-core` в новом треде `149-frozen-pair-writes-to-the-feed`. Ход по коду ушёл туда по измеренной причине; здесь остаётся метка на #303 по зелёному и утреннее окно john.**

## 1. Норма записана — PR #303, и он кнопкой john

Абзац в `PROTOCOL.md` рядом с нормой (а) (#291): «**ТЕРМИНАЛЬНАЯ ЗАМОРОЗКА ПИШЕТ В ПОЧТУ**». Ветка `docs/140-terminal-freeze-writes-to-mail`, голова `5ab1f823ee65b96c9a7719d1b457da3e1afa2482`, один коммит, один файл. PR открыт командой пакета (`pr open --write`), тело несёт `thread: 140-silent-exit-exhausts-the-role` и `role: curator`.

Абзац несёт ровно то, что назвал john, и ничего сверх: замер (трое суток, пять пар, узнали случайно); **кто пишет** — `notify` под учёткой контура, новых прав норма не требует; **форма** — рядом с ветвью `frozen`, ключ новизны серия, один раз на ПЕРЕХОД, а не каждый тик; **что письмо обязано нести** — пара, попытки, чем кончилась последняя, что снимает заморозку; **границы** — потолок не ослабляется, звонок потолка (тред 013) остаётся, норма ДОБАВЛЯЕТ ленту.

Дверь на этой голове, живьём: `STOP guard 4 — john merges this one — it changes PROTOCOL.md`; `STOP` гардов 1 и 2 — вердикта нет, `checks=IN_PROGRESS`; `mergeable=MERGEABLE (UNSTABLE)`. **Кнопка не моя ни при каком исходе кругов** — это док власти, и я его не принимаю.

**Мой следующий ход по нему ровно один: метка `review` по зелёному `checks` на той же голове.** У конвейера не стою: по норме 151 зелёный `checks` на PR без метки называет роль автора, то есть поднимет меня сам. Парка под это состояние нет ни одного — см. §6, дверь померила это в лоб, — поэтому письмо оставляет ход у curator без парковки, и цена честно названа: возможен один холостой подъём, если обходчик придёт раньше зелени.

## 2. Исполняющая половина уехала в тред 149 — записью, а не намерением

Открыт тред **`149-frozen-pair-writes-to-the-feed`**, постановка там целиком: пять пунктов работы, границы («не трогать»), раздел «Проверяемость» (стык на ПЕРЕХОД: ровно одно письмо, на втором тике ноль; мутация; непокрытая половина — живая доставка при настоящем исчерпании) и гард 3, восходящий к слову john «ОБЪЯВЛЯЕМ» из письма `14:17:16Z` этой ленты. Класс «полевой измеренный дефект» там **не объявлен и не применён**: дифф исполняет ОБЪЯВЛЕННУЮ норму, основание — прямое слово john.

**Причина переезда измеренная, а не вкусовая, и она та же, что у 146:** норма 151 велит зелёному `checks` без метки называть роль АВТОРА — то есть письмо о зелени #303 заберёт ход у `dev-core` и отдаст мне в ближайшие минуты. Постановка кода, оставленная в этой ленте, была бы снята с хода машинным письмом. Ход у `dev-core`, контрольное чтение сделано: `mail --role dev-core` называет `149-frozen-pair-writes-to-the-feed` — тред виден обходчиком, а не только записан.

## 3. #298 — гарды замерены, готов к кнопке, но кнопка принадлежит ленте 146

Перемерено этим прогоном на голове `b59d653720e6dfadef82fc02a1aa3e0ad4ecccf5`:

- **гард 1 — `ok`:** «approved on b59d653 by github-actions — inside the round `34037978834` of 'Claude PR Review' on this head (14:03:38Z…14:07:43Z)». Якорь — ПРОГОН, вердикт лежит в его окне, сироты нет;
- **гард 2 — `ok`:** `2 check(s) green: review=SUCCESS, checks=SUCCESS` (`checks` — прогон `34035340605`);
- **нота двери о переезде базы** названа честно: `ce5a690` лёг `14:15:59Z`, `checks` стартовал `13:11:34Z`. Это нота, а не гард; гасится замером на слитом дереве, а не ребейзом, и `mergeStateStatus` при этом `CLEAN`;
- **гард 4 — `ok`:** 4 пути диффа, доков власти среди них нет (дверь вывела список из 8 сама);
- **гард 3 — дверь называет тред `146-delivery-refusal-swallows-the-credential-diagnosis`**: `dev-core` исправил тело PR на живой дом ДО навешивания метки, круга этим не аннулировав.

**Кнопку в этом прогоне не жму, и вот основание, а не осторожность:** след гарда 5 обязан лечь в ленту треда, которому PR принадлежит, — а это 146, и там же объявлен класс с тремя условиями. Мой прогон связан ровно одним тредом (140), писать в 146 мне нечем. **Молчания не оставляю:** 146 стои́т в почте curator, ход там мой, парк `run:298` снят приехавшим вердиктом — ближайший такт по 146 мёржит #298 по уже замеренным гардам, и перемерять их придётся только на предмет уехавшей головы.

## 4. Хвост — открытые PR названы все

`gh pr list --state open` + `merge-gate` по каждому, живьём:

- **#301** (тред 141): гард 2 `ok`, **`STOP` гарда 1 — `changes were requested` на `8f9a9b8`**, то есть новый круг, а не merge. Ход у автора, не у кнопки;
- **#302** (тред 130): гард 2 `ok`, **`STOP` гарда 4 — кнопка john** (правит `.github/workflows/claude-review.yml`), и гард 1 на нём закрывается ЧЕЛОВЕЧЕСКИМ чтением: круг по такому PR самопропускается по построению, метка `review` не вешается;
- **#303** (этот тред): выше, §1;
- **#298** (тред 146): выше, §3.

Ни один моей кнопкой в этом прогоне не проходит.

## 5. Что не сделано, чей ход, состояние дерева

- **не сделано и стои́т на john:** четыре шага `docs/box-setup.md` §0.1b — его рука завтра утром одним заходом вместе с окном (тред 062). Пять прогонов `devops` до шагов 1–2 по-прежнему НЕЛЬЗЯ: роль умирает на первой команде такта. Плюс кнопка по #303 и кнопка по #302;
- **не сделано и уехало с ходом:** исполнение нормы (тред 149, ход `dev-core`), merge #298 (тред 146, ход curator — ближайший такт), круг по #301 (тред 141, ход автора);
- **незакоммиченного нет:** в рабочем дереве роли чисто, вся работа такта — абзац `PROTOCOL.md` в ветке `docs/140-terminal-freeze-writes-to-mail` (запушена), новый тред и это письмо;
- **тред 140 своей работы больше не несёт**, кроме метки на #303 по зелёному: обе половины его постановки объявлены нормой и обе имеют исполнителя. Закрывать нельзя — приёмку утренних шагов john не доложить в закрытую ленту.

## 6. Замер по дороге: парк `pr:N` НЕ ждёт зелени и требует имени руки

Я собиралась запарковать это письмо на `pr:303` и получила от двери отказ — с двумя фактами, которых у меня в голове не было и которые здесь остаются записью, а не устным знанием:

1. **лифт `pr:N` — РОВНО ОДИН:** сообщение с заголовком `merged-pr: 303` где угодно в почте. **Состояние #303 не смотрит НИЧТО** — ни закрытие, ни merge на GitHub, ни объявление мёржа прозой парк не снимают. То есть зелёный `checks`, который по норме 151 называет автора, из-под этого парка меня НЕ поднял бы, и я заморозила бы собственный следующий ход;
2. **дверь требует `--park-mover <участник>`**, если парк ждёт РУКИ на кнопке: «дверь, запертая изнутри» (тред 061, живой случай — роль запарковалась на merge своего же PR, чьи метка и вердикт были следующим ходом её же curator). У `run:` такого имени не спрашивают: круг уже идёт.

Отсюда и решение §1: парка нет, ход остаётся у curator голым кандидатом. Это дороже парка ровно на один возможный холостой подъём и дешевле замороженного треда с готовой работой внутри.

waiting-on → curator (парка нет намеренно: следующий ход — метка на #303 по зелёному, и ни одна форма парка этого события не ждёт).

