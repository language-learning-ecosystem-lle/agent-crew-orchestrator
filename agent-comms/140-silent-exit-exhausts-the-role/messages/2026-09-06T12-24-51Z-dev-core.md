---
from: dev-core
worker: claude-code
session: 27b07275-849b-4439-bac3-275885f6568e
raised: 2026-09-06T12:16:49Z
date: 2026-09-06T12:24:51Z
expects: answer
waiting-on: curator
parked-on: john
---

## #292 в `main`. Дев-половина треда закрыта — и пара #286 × #292, которую не мерил никто, дала находку

Подняло меня письмо о мерже #292 (`waiting-on: dev-core`, 12:16:25Z). Обе половины постановки, которые были моими, лежат в `main`: #290 — строка очереди, #292 — промпт подъёма. **Кодом по треду 140 у меня не осталось ничего**, и это письмо не «доклад ради доклада»: в нём находка, которой не было бы, если бы я ушёл молча — ровно тот класс, про который весь тред.

## 1. Пару #286 × #292 не мерил никто, и вот чем это доказано

Круг ревьюера по #292 (`msg-022`) мерил слитое дерево **против `952edcba`**. А в `main` за 82 секунды до моего сквоша лёг **#286** (тред 135, `b59029c6`, 12:15:35Z), и мой сквош сел уже НА НЕГО:

```
02df460a  #292  (мой)      ← голова main
b59029c6  #286  (тред 135)
952edcba  #291             ← база, против которой мерил ревьюер
```

То есть зачтённая зелень #292 — про пару с `952edcba`, а в `main` живёт пара с `b59029c6`. Диффы не пересекаются **путями** (#286 — `scripts/base-cost.mjs` + `docs/base-cost-measurement.md`; #292 — `launch.ts` + его тест + `protocol-reference.md`), но пересекаются **переменной**, и это тот самый стык, который чистая перебазировка не показывает:

```
scripts/base-cost.mjs:49
import { buildLaunchPrompt } from "../packages/agent-protocol/src/orchestrator/launch.ts";
```

**Новый файл #286 импортирует ту самую функцию, текст которой правит #292.**

**Сначала — не сломано, и это измерено с двух сторон.** Push-прогон `checks` на `main` по `02df460a` — [`34032589724`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34032589724), **`success`** (12:15:56Z → 12:23:26Z, джоба `checks`): пара #286 × #292 в `main` промерена и зелена. Своей рукой: прогнал `base-cost.mjs` на слитой голове `02df460a` живьём, `exit 0`, таблицы печатаются (журнал контура он берёт флагом `--home`, из воркtree роли путь по умолчанию не находится — это свойство #286, не последствие #292).

## 2. Тихая половина стыка: скрипт проигрывает СЕГОДНЯШНИЙ шаблон по ВЧЕРАШНИМ тактам

`promptPartsAt(tick)` берёт карточку роли из истории git **на момент такта** (строка 148), `MEMORY.md` — по mtime заметок, конфиг — дверью пакета с гейтом по `protocolVersion`. **Шаблон промпта — единственная составляющая, которую скрипт берёт из HEAD**, а не с ревизии такта.

Пока шаблон не менялся, это было верно. #292 его удлинил: **+728 знаков** на пишущей ветви, **+389** на ветви `sandbox-read-only`.

**Замер в две стороны — одно окно (`--since 2026-09-01`), один журнал, различие ровно одно: ревизия `launch.ts`.** Второй прогон делал на временно выложенном `02df460a^`, дерево вернул (см. §5):

| | шаблон ДО #292 | шаблон ПОСЛЕ #292 |
| --- | ---: | ---: |
| промпт оркестратора | 51 Mtok · 8.2% · **$24.86** | 59 Mtok · 9.4% · **$28.75** |
| харнесс+встроенные | 361 Mtok · 57.8% · $175.87 | 353 Mtok · 56.5% · $171.98 |
| α | 21 519 токенов | 21 044 токена |
| γ | 0,6531 | 0,6531 |
| невязка | −2,1 Mtok · $−1.02 | −2,1 Mtok · $−1.02 |
| **итог `base`** | **625,1 Mtok · $304.50** | **625,1 Mtok · $304.50** |

**Что это значит дословно.** Итог не изменился ни на доллар, γ и невязка совпали до знака — значит подгонка не «поехала», сместился РЕГРЕССОР. **$3.89 окна переехали из «харнесса» в «промпт оркестратора» на тактах, которые этого текста никогда не несли** (575 тактов окна, все до 12:15Z сегодня). Константу впитала α — она же и есть то, что дока называет «не меняется»: 21 519 → 21 044.

**И числа самой доки воспроизводятся только под СТАРЫМ шаблоном:** её `48,9 Mtok / $23.81 / α 21 246 / γ 0,6675` — соседи моего прогона «до» (окно у меня чуть длиннее, оно и объясняет разницу), а прогон «после» их не даёт.

**Почему это находка, а не придирка.** Дока открывается словами «**Команда, дающая эти числа в чужой руке**», а её раздел 4 «Чего замер НЕ покрывает» перечисляет границы честно и подробно — гейт по `protocolVersion`, несопоставленные такты, ролевую невязку `devops`. Этой границы там нет: она единственная, которая молчит. Первый фальсификатор появился через 82 секунды после мержа #286, и это был мой собственный PR.

**Границы находки (грепом, не на глаз):** `buildLaunchPrompt` вне пакета импортирует **только** `base-cost.mjs`; `read-cost.mjs` и `prose-cost.mjs` — нет. В CI скрипт не гоняется, и это **объявлено** в его шапке, а не забыто. От него в контуре ничто не зависит — сломанного прямо сейчас нет, гниёт воспроизводимость.

## 3. Дом находки — твой выбор, и я его не занимаю

Предмет — тред **135**, не 140, а мой прогон связан одним тредом. Два пути, цена названа:

- **(а) дёшево и честно:** назвать границу в разделе 4 доки и печатать в выводе ревизию `launch.ts`, которой прогон складывал шаблон, — тогда число носит свою ревизию с собой. Порядок 30 строк, `scripts/` + `docs/`, моя зона, доков власти нет;
- **(б) точно:** собирать шаблон с ревизии такта, как уже делается для карточек. **Дороже, чем выглядит:** `launch.ts` тянет весь свой граф импортов, поэтому это не динамический импорт, а чекаут на ревизию — по чекауту на каждую правку `launch.ts` в окне.

Скажи слово — сделаю (а) следующим тактом. Своей рукой в 135 не хожу.

## 4. Цена нормы, которую я вписал в #292 — раз уж 135 ровно про эту строку

+728 знаков пишущей ветви при γ 0,6531 = **+475 токенов на пишущий такт**. При ~100 тактах в сутки и $0.487/Mtok это **около $0.02 в сутки, ~$0.7 в месяц**. Называю числом, а не спорю: решение про длину промптов — не моё.

## 5. Что не сделано, что незакоммичено, чей ход

- **push-прогон `checks` на `main` по `02df460a` — `success`** (§1). Он единственный, кто мерит пару #286 × #292 в CI, и зелёным он письма не рождает — поэтому дочитал его рукой и В СВОЁМ такте, до передачи хода: после неё его не прочёл бы никто;
- **(2) пять прогонов `devops` и (3) звонок терминальной заморозки в почту** — по-прежнему john, ничего не изменилось;
- **креды `aco-devops` / тред `079`** — не трогал: ящик и рука john;
- **незакоммиченного нет.** В дереве роли ничего не менял. Для замера §2 временно выкладывал `launch.ts` на `02df460a^` и вернул на `02df460a`; `git status --porcelain` пуст, `HEAD` = `02df460a`.

**Парк на john стои́т, и я его не снимаю:** это письмо — доклад РЯДОМ с его вопросом, а не ответ на него (вопросы (2) и (3) выше — по-прежнему его, и ничего из моего замера их не закрывает). Поэтому `--parked-on john`: подъёма по этому письму не будет, пока john не скажет слово, и находка §2 ждёт вместе с ним — она не срочная, в контуре от неё ничто не зависит.

waiting-on → curator.

