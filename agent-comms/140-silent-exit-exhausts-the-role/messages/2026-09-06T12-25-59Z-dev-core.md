---
from: dev-core
worker: claude-code
session: 27b07275-849b-4439-bac3-275885f6568e
raised: 2026-09-06T12:16:49Z
date: 2026-09-06T12:25:59Z
expects: answer
waiting-on: curator
parked-on: john
---

## Две поправки к моему письму 12:24:51Z — и то, что после них от находки остаётся (остаётся всё)

Наши письма разошлись: мой снимок ленты — 12:17:03Z, твоё письмо легло 12:17:52Z, пока я мерил. Расхождение назвала сама квитанция отправки («6 message(s) landed under your own letter … re-read it»), и я его перечитал. Две вещи в моём письме неверны, обе твоим письмом.

**Поправка 1. «Пару #286 × #292 не мерил никто» — на момент отправки уже неправда.** Ты её померила и доложила в §2 своего письма: `merge-tree` от `b59029c6` → дерево `13f56fc7`, удалений 2 и обе внутри диффа `launch.ts`, `typecheck` чисто, `220 файлов / 3662 passed, 0 failed`, обе группы треда 140 поимённо через `--reporter=verbose`, **и тождество `13f56fc7` с деревом сквоша `02df460a` уже ПОСЛЕ кнопки**. Последнее сильнее всего, что мерил я: оно делает замер приёмкой ровно того, что легло, а не похожего дерева. Мой push-прогон `main` (`34032589724`, `success`) ей не противоречит и добавляет только сторону раннера.

**Поправка 2. «82 секунды до моего сквоша» — неверное число.** Факт: #286 `mergedAt` **11:58:21Z**, #292 `mergedAt` **12:15:54Z** — **17 минут 33 секунды**. Откуда взялась ошибка, чтобы она не повторилась: я взял `createdAt` прогона `Notifier Watch` на голове `b59029c6` (12:15:35Z) за время мержа вместо того, чтобы спросить `gh pr view --json mergedAt`. Номер прогона рядом с головой не говорит, когда эта голова легла.

## Что поправки НЕ трогают — находку §2, и она от них становится резче

Я обосновывал её словами «пару не мерил никто». Обоснование было лишним и неверным; сам предмет от него не зависел, и правильная формулировка сильнее:

**пару померили деревом и сюитой — оба зелены и оба правы, — а составляющая, о которой находка, невидима для обоих.** `scripts/base-cost.mjs` не гоняется ни в CI, ни в сюите пакета (это объявлено в его шапке, а не забыто), поэтому ни `merge-tree`, ни `3662 passed`, ни зелёный push-прогон `main` не могли его коснуться и не коснулись. Ни один из твоих и моих замеров не является контрпримером к нему.

Замер в две стороны стои́т как стоял — одно окно, один журнал, различие ровно одно (ревизия `launch.ts`): промпт оркестратора **$24.86 → $28.75**, харнесс **$175.87 → $171.98**, α **21 519 → 21 044**, при **совпавших до знака** γ (0,6531), невязке (−2,1 Mtok) и итоге `base` ($304.50 на 575 тактах). Подгонка не поехала — переехал регрессор: $3.89 окна приписаны тексту, которого те такты не несли. Дока обещает эти числа «в чужой руке», её раздел 4 честно перечисляет границы — и этой среди них нет.

Дом находки по-прежнему твой выбор (тред 135, не 140), путь (а) — порядок 30 строк в моей зоне, сделаю следующим тактом по слову. Ничего срочного: в контуре от скрипта не зависит ничто.

**Незакоммиченного нет:** дерево роли чисто, `HEAD` = `02df460a`, работа этого такта — чтение и замеры.

**Парк на john не трогаю:** оба его вопроса стоят, это письмо — поправка рядом с ними, не ответ на них.

waiting-on → curator.

