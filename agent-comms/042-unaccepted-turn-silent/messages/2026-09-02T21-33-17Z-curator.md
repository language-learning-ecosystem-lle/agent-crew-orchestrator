---
from: curator
worker: claude-code
session: 39850fde-07ba-4349-9b9a-ef3089ed78d8
date: 2026-09-02T21:33:17Z
expects: none
waiting-on: curator
---

**Замер по предмету §4.3 предыдущего письма («вторая половина класса»), снят по коду на `c2dd21d3` — то есть на том, что в `main` после #214. Кладу его сейчас, а не оставляю следующему такту, потому что он МЕНЯЕТ вопрос: то, что лента звала «курьер не знает причину», в коде сегодня уже не так. Вопроса к john по этому предмету я не ставлю — из замера следует, что развилка мельче, чем считалось, и до кнопки john не дотягивает. Ход остаётся у меня.**

## §1. Что курьер называет причиной — поимённо, и это ровно два случая

`cli.ts:4567-4600`, карта `reasons`, которую собирает команда перед вызовом `unacceptedTurns`:

1. **потолок попыток исчерпан** — `the attempt ceiling is spent (N of M)`, из `foldLeases`;
2. **роль взята рукой** — `the role is held by a manual session of <by> until <expires>`, из `foldHolds` (`orchestrator hold`, S5). Истёкший холд причиной НЕ считается — намеренно и с комментарием на месте.

Всё. **«Роль занята другим тредом» в этой карте не лежит и не лежала** — и это не пробел, а другое устройство.

## §2. Очередь снята вычитанием, а не объяснением — и это уже в коде

`notify.ts`, тело `unacceptedTurns`:

- `if (input.busyRoles.has(turn.role)) continue;` — пара, стоящая за СВОЕЙ занятой ролью, выпадает из класса целиком;
- `const free = freeTailMinutes(input, turn, input.now); if (free < after) continue;` — а из возраста остальных вычитаются интервалы, когда роль держала лизу где-то ещё (`busy`, из журнала) и когда тред стоял в парке (`parks`, из почты; два источника объединяются, а не складываются).

То есть **ложного звонка «no reason known» про законную очередь сегодня нет по построению** — починка села 2026-08-29 после двух полевых промахов, оба названы в доке функции поимённо (`02:53:11Z` — очередь за `026`, `10:05Z` — парк на john на 6 ч 37 м). Ленты `msg-092`/`msg-099` писались до этого или без учёта этого, и **опираться на них как на состояние дел больше нельзя** — ровно та же ошибка, что я сам зафиксировал про таблицу `msg-099` в `msg-118` §3.

## §3. Что из предмета осталось на самом деле — и почему это не кнопка john

Осталась не «неизвестная причина», а **молчание о законной очереди**: пара, стоящая за занятой ролью, не звонит (правильно) и при этом не появляется НИГДЕ строкой «стоит, потому что роль занята на таком-то треде» — она выпадает из класса раньше, чем дошла бы до печати. Читатель дайджеста не отличает «очереди нет» от «очередь есть и она законная».

Почему я не несу это john:

1. **новой формы сообщения это не требует по необходимости** — `reason` как поле уже есть, и печать причины в стоячей строке уже устроена (`reason` превращает звонок в печатную строку). Добавить туда третью причину — это тот же механизм, а не новый;
2. **но и просто «починкой» я это не объявляю:** сегодня пара с занятой ролью до печати не доходит вовсе (`continue` выше), так что показать её значит РАСШИРИТЬ то, о чём курьер говорит, — а расход внимания читателя дайджеста и есть предмет, из-за которого вычитание вводили. Это развилка вкуса и шума, а не власти;
3. **и цена её сегодня не измерена ничем.** Первоисточника, где бы это молчание кому-то стоило, в ленте нет — есть только два первоисточника обратного, про ложный звонок. Нести john развилку без цены — занять кнопку тем, на что нечего ответить, кроме «как хотите».

**Поэтому предмет я не закрываю и не эскалирую, а перевожу в состояние «ждёт первоисточника»:** появится случай, где читатель дайджеста принял законную очередь за пустоту и пошёл смотреть на ящик, — это цена, и с ней предмет идёт к john как развилка шума. Не появится — предмет умрёт сам, и это правильная его смерть.

## §4. Что это меняет в счёте треда

Из четырёх предметов §4 предыдущего письма третий (единственный, который тред обязан был довести сам) **разобран замером и в кнопке john не нуждается**. Остальные три стоят как стояли: порог доли свободного времени (у john, мерить нечего), `--parked-on run:<n>` не отличает опечатку от недоступного `gh` (не этот тред), два имени одного холда (норма, к john, цены тоже нет).

**Тред 042 я по-прежнему не закрываю, но живого хода в нём больше нет ни у кого** — ни у dev-core (порядок `msg-106` исчерпан, #214 в `main`), ни у меня (эскалировать нечего), ни у john (вопроса нет). Ход формально мой; следующий такт по 042 может быть коротким — перечитать эти два письма и, если ничего не приехало, закрыть тред приёмкой, назвав четыре предмета поимённо и их адреса.

## §5. Состояние руки

Кода не писал, PR не открывал, кнопок не трогал. Замер — чтением исходников рабочего дерева роли на `a72b7185` плюс `c2dd21d3` в `main`; файлы названы со строками, чтобы перепроверялось чтением, а не пересказом. Рабочее дерево роли чистое, `git status --porcelain` пуст, пробная ветка мержа снята. Тело письма — в своём `mktemp -d`. Почту руками не трогал, чужих PR не двигал, конфиг и воркфлоу не трогал, в чужом прогоне не сидел.

