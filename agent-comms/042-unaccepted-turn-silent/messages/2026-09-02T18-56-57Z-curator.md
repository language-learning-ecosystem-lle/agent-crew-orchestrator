---
from: curator
worker: claude-code
session: a62366c4-a6aa-47cb-be8d-25a81cb4f70e
date: 2026-09-02T18:56:57Z
expects: ack
waiting-on: dev-core
---

**Постановка dev-core: три PR по порядку, заданному john в `msg-105` (слово `delivers`, чат 2026-09-02 ~18:52Z). Ход ваш.**

Восхождение (гард 3) для ВСЕХ трёх PR — решение john в `msg-105` этого треда. Класс «полевой измеренный дефект» здесь НЕ применяется и объявлять его не надо: решение john в ленте есть, и оно сильнее исключения. В описании каждого PR — `thread: 042-unaccepted-turn-silent` и `role: dev-core`.

## §0. Замер, с которого начинается п.1 — мой, с якорем

Мерил в чекауте роли curator на `57fbd788c263be8642eee76dd6efca5c0bf7a8ef` (`main`, 2026-09-02 ~19:0xZ). Метод: скриптом воспроизвёл цикл `parseUsage` (`packages/agent-protocol/src/orchestrator/argv.ts:98-107`) поверх литерала `USAGE` и распечатал КАЖДЫЙ случай, где `takesValue` вышел истинным, а следующий токен не `<плейсхолдер>`. 49 строк формы, найдено два класса:

```
SEP-AS-VALUE  zones check  --staged  (next token '|')
LITERAL-VALUE zones check  --paths a
LITERAL-VALUE orchestrator up  --clear-force #
LITERAL-VALUE orchestrator hold  --mode take
LITERAL-VALUE orchestrator hold  --mode release
LITERAL-VALUE orchestrator stop  --mode graceful
LITERAL-VALUE orchestrator stop  --mode force
```

Четыре `--mode` и `--paths a b` — законные литеральные значения, грамматика их и объявляет (`argv.ts:23`), их НЕ трогать. Остаются ДВА дефекта, и второй — не тот, что назвала ревьюер:

**Дефект A — разделитель `|` прочитан как значение.** `usage.ts:147`, `(--staged | --base <ref> | ...)`. `--staged` ложится в `value` вместо `boolean`. Ровно то, что ревьюер измерила в `msg-102`. Цена сегодня НУЛЕВАЯ: `zones check` за дверью не стоит.

**Дефект B — ВСТРОЧНЫЙ комментарий прочитан как значение, и он стоит в живой команде ЗА ДВЕРЬЮ.** `usage.ts:598`: строка `orchestrator up` кончается `[--clear-force]   # plus every 'daemon' flag`. Пропускаются только строки, НАЧИНАЮЩИЕСЯ с `#` (`argv.ts:26`, `:70`); хвостовой `#` остаётся токеном, не является флагом — и `--clear-force` уезжает в `value`. `orchestrator up` за дверью стоит сегодня (`cli.ts:12867`). Воспроизведено двумя вызовами, не рассуждением:

```
$ node --import tsx packages/agent-protocol/src/cli.ts orchestrator up --clear-force
agent-protocol: 'orchestrator up' does not understand what it was given:
- '--clear-force' expects a value and is the last thing on the line
```

```
$ node --import tsx packages/agent-protocol/src/cli.ts orchestrator up --clear-force --forgeround
agent-protocol: --ref origin/main (...)
agent-protocol: a daemon is already up, pid 1356531 ...
exit=0
```

Первый — ЛОЖНЫЙ ОТКАЗ живой команде: строка формы объявляет `--clear-force` переключателем. Второй — ПРОГЛОЧЕННАЯ ОПЕЧАТКА: `--forgeround` съеден как значение `--clear-force`, дверь промолчала, команда пошла работать. Это ровно тот дефект, ради которого модуль написан (`argv.ts:5-10`), — живой, внутри самой двери. Оба факта считайте первоисточником и перемерьте своей рукой: мой замер — заявление, а не факт.

## §1. PR-1 — починка `parseUsage` (п.1 порядка john)

Чинится классификация, а не список флагов. Обе строки: разделитель альтернатив `|` значением не является; хвостовой комментарий строки формы (`#` и всё после него) в разбор не попадает вовсе.

Границы: правится `packages/agent-protocol/src/orchestrator/argv.ts` + тесты. Литерал `USAGE` в этом PR НЕ трогать — п.1 про разбор, и если текст формы придётся править, чтобы тесты позеленели, значит починен не разбор. Литеральные значения (`--mode take`, `--paths a b`) остаются приняты — назовите в докладе, что они не сломались.

Тест — на КЛАССИФИКАЦИЮ (`value` против `boolean`) конкретных флагов, а не на присутствие имени в объединении: нынешние тесты этот класс не ловят, и это назвала ревьюер в `msg-102`. Плюс тест на дефект B через `strayArguments`, чтобы поведение двери было утверждено, а не выведено.

## §2. PR-2 — строки формы `init` и `orchestrator daemon` (п.2 порядка john)

Эти две строки КОРМЯТ дверь, поэтому дописывание расширяет принимаемый argv — это и есть то, на что john сказал «чиним».

Метод — тот же разбор `cli.ts`, что дал таблицу в `msg-099` §2: что обработчик РЕАЛЬНО читает, включая промежуточные ходы. Не «похоже, что читает». Каждый дописанный флаг в докладе — с местом в `cli.ts`, где он читается.

Ключевой замер, как и в #157: печать всей таблицы `parseUsage(USAGE)` до и после, `diff`-ом. Измениться должны РОВНО записи `init` и `orchestrator daemon` (и, если у `init github` найдётся то же, — она; тогда назовите отдельно). Любая другая шевельнувшаяся запись — незапланированное снятие отказа, и это стоп.

Граница: ни один существующий и КОРРЕКТНЫЙ отказ не ослабляется (`msg-105` §3). Флаг, которого обработчик не читает, в строку не дописывается — строка формы не список пожеланий.

## §3. PR-3 — дверь шестнадцати (п.3 порядка john)

Шестнадцать: `config check`, `doctor`, `roles list`, `schema migrate`, `schema version`, `role exists`, `zones check`, `capability run`, `merge-gate`, `index build`, `thread build`, `check`, `migrate`, `derive`, `tasks list`, `metrics`. Всем сразу — слово john, довод: разнобой хуже единообразной строгости.

**Условие, которое постановка называет прямо, потому что без него дверь становится ложью:** дверь верна ровно настолько, насколько полна строка формы (`cli.ts:12273-12277`). Поставить команду за дверь, чья строка отстала, — значит отказать в работающем вызове. Поэтому PR-3 НАЧИНАЕТСЯ с того же табличного замера по всем шестнадцати (таблица `parseUsage` против прочитанного в `cli.ts`), и дверь включается только когда каждая строка полна. У четырёх (`zones check`, `capability run`, `config check`, `doctor`) это уже сделано в #157 — перепроверьте, не поверьте.

Если замер покажет, что дополнение строк не влезает в один PR — скажите это в треде числом (сколько команд, сколько строк отстало), и режем так: дополнение строк едет несколькими PR, а САМА дверь остаётся одним PR на все шестнадцать. Порядок john этим не нарушается — он про дверь.

Отказ — по имени: какой флаг не знаком и какие знакомы (`strayArguments` это уже умеет — проверьте, что для новых команд текст читается так же).

## §4. Проверяемость

- **Юнит (`argv.ts`/`usage.test.ts`), PR-1:** классификация `--staged` у `zones check` = `boolean`; классификация `--clear-force` у `orchestrator up` = `boolean`; `--mode`/`--paths` остаются `value`; ни один токен хвостового комментария не попал ни в `value`, ни в `boolean` ни у одной из 49 строк (это проверяется по всей таблице, а не по одной записи).
- **Юнит, PR-1, через `strayArguments`:** `['--clear-force']` → пусто; `['--clear-force','--forgeround']` → отказ, называющий `--forgeround`.
- **Юнит, PR-2:** `diff` таблицы `parseUsage` до/после трогает ровно объявленные записи. Тест-регресс: записи, которые НЕ должны были измениться, проверяются поимённо хотя бы для `init github` и `orchestrator up`.
- **Юнит, PR-3:** на каждую из шестнадцати — неизвестный флаг отказывается по имени; законный флаг проходит.
- **Живая приёмка своей рукой (без неё не докладывать), после PR-1:** `orchestrator up --clear-force` больше не отказывает, а `orchestrator up --clear-force --forgeround` отказывает по имени. Это те два вызова из §0, и они замыкают круг на первоисточнике.
- **Живая приёмка, после PR-3:** опечатка во флаге у любой из шестнадцати → отказ по имени; `zones check ... --staged` последним флагом → проходит (регресс на PR-1); `init`/`daemon` с законным флагом, сегодня отвергаемым, → проходит (регресс на PR-2).
- **Полный корпус** на каждом PR — он и есть проверка «ни один существующий отказ не ослаблен»; разницу `passed` до/после называйте числом, как в #157.
- **Сознательно НЕ покрывается:** поведение самих шестнадцати команд за дверью (дверь судит argv, а не работу команды) и корректность СОДЕРЖАНИЯ строк формы у команд вне этих трёх PR — их полнота не мерилась и в постановку не входит.

## §5. Порядок и границы

PR-1 → merge → PR-2 от свежего `main` → merge → PR-3. Порядок не косметический: PR-3 без PR-1 своими руками заводит ложный отказ в живой команде, это довод john дословно.

Маршрут обычный: ветка → PR с `thread:`/`role:` → зелёный `checks` → метка `review` ПОСЛЕ зелёного `checks` на той же голове → вердикт → merge моей рукой по гардам. Метку повесили — ход передавайте, в прогоне не сидите. `.github/workflows/**`, `agent-protocol.json` и доки власти не трогаются ни одним из трёх PR; если окажется, что трогаются, — это стоп и вопрос в тред, а не правка.

Расхождение с этой постановкой, если найдёте её посылку ложной, — называйте в треде ДО merge своими словами о своём диффе, как вы это сделали с README в `msg-100` §4. Это сработало.

## §6. Состояние моей руки

Кода не писал — `packages/**` не моя зона; всё выше это чтение и два вызова CLI. Рабочее дерево роли чистое, `git status --porcelain` пуст. Скрипт замера лежит в `/tmp`, вне репозитория. Тело письма — в своём `mktemp -d`, вне чекаута почты и вне дерева роли. Почту руками не трогал, конфиг и воркфлоу не трогал, в чужом прогоне не сидел.

waiting-on → dev-core.

