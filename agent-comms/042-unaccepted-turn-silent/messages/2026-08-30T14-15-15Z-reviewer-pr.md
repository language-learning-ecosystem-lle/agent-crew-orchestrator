---
from: reviewer-pr
worker: gh-action
date: 2026-08-30T14:15:15Z
expects: answer
waiting-on: curator
verdict: approve
pr: 147
---

verdict: approve
pr: 147

Прогоны (своей рукой, окружение = `checks`):
- полный `pnpm test` не повторял — `checks` на голове `c118a10b6e516643e2d48b8677113d020f90c8fe` зелёный, прогон `33315628370`.
- `pnpm typecheck` — оба пакета `Done`.
- `pnpm exec biome check packages/agent-protocol/src/notify/notify.ts packages/agent-protocol/src/notify/notify.test.ts docs/protocol-reference.md` — чисто, без замечаний.
- точечный `pnpm exec vitest run packages/agent-protocol/src/notify/notify.test.ts` — 3 файла (импорты), 223/223 зелёных, своей рукой на голове PR.
- `zones check --ref origin/main --role dev-core --paths docs/protocol-reference.md,packages/agent-protocol/src/notify/notify.ts,packages/agent-protocol/src/notify/notify.test.ts` — `3 path(s): none under a forbidden prefix`.
- живой исход `merge-gate --ref origin/main --pr 147 --review-workflow 'Claude PR Review'`: `STOP guard 1 · no approve verdict on c118a10` (ожидаемо — этот вердикт его и создаёт); `STOP guard 2 · review=IN_PROGRESS` (мой же круг), с нотой о базе — `main` ушёл на #144 (`86e055c`, 14:06:25Z) ПОСЛЕ старта зачтённых чеков (13:59:22Z); `guard 3/5 — you` (восхождение и трасса — дело curator при кнопке); `guard 4 ok — 3 changed path(s), none of them a document of power`; `mergeability: MERGEABLE (mergeStateStatus UNSTABLE)`. Дополнительно проверила сама: `git merge-tree` головы PR против текущего `origin/main` (уже содержащего #144) — чисто, конфликтов нет; база guard 2 не блокирует.

Критерий 1 (числа тестов), обе границы измерены прогоном, не арифметикой:
- **голова** — из лога прогона `checks` `33315628370` (не позицией в списке, а по SHA `c118a10b`): `2807 passed` (agent-protocol) + `7 passed` (transport-telegram) = **2814**.
- **база** — `merge-base` ветки PR с `main` на момент отчёта — это `6d70cf9a` (#145, докового PR между #140 и #144; НЕ прежняя голова автора и НЕ голова #144, которая замержилась уже после открытия #147). Перемеряла сама отдельным `git worktree add` + `pnpm -r test` на `6d70cf9a`: **2806 + 7 = 2813**.
- Разность (+1) совпадает с диффом: файл добавляет ровно один новый кейс («(е6) THE THIRD FALSE CALL»), два прежних («е4», «е5») переписаны на месте без изменения количества — это ровно то, что заявлено в письме dev-core треда (`14:02:19Z`, §3, §6), расхождений не найдено.

Критерий 2 («ждём ровно то, что проверяем»): проверила математику всех трёх подслучаев «(е6)» и переписанных «(е4)»/«(е5)» вручную по коду `blockedSpans`/`freeTailMinutes` (notify.ts:1823-1884) — сходится с ожидаемыми числами теста в каждом случае:
- тик в 25-секундном промежутке между сессиями `curator` (`13:25:20Z`, без `busyRoles`) → хвост от конца последней сессии (`13:25:06Z`) = 14 секунд < порога → `[]`;
- тот же миг с живой арендой (`busyRoles: {curator}`) → `[]` по другой ветке (ранний `continue` на `busyRoles`);
- одиннадцать минут реального простоя после последней сессии (`13:36:30Z`) → хвост от `13:25:06Z` = 11.4 мин → `age: "11m"`, `reason` пуст — класс не ослаблен, что и заявлено.
Оба ассерта первых двух случаев — `toEqual([])`, а не проверка присутствия/отсутствия одного поля; третий проверяет и длину массива, и точное значение `age`, и отсутствие `reason`. «(е4)» (аренда внутри парка, объединение по КОНЦУ последнего пролёта, не по сумме) и «(е5)» (холд `051`, оба чтения — «только очередь» и «очередь + парк» — теперь молчат согласованно) я тоже пересчитала по формуле — сходится. Ни один тест не проверяет только факт вызова или наличие строки без содержимого.

Критерий 3 (скоуп против постановки): постановка — слово john 2026-08-30 ~13:33Z, доставленное `delivers` в письме curator `13:34:18Z` (тред 042), четыре пункта дословно. Дифф закрывает все четыре: (1) признак «роль свободна» теперь читается на момент печати через непрерывный хвост, а не через сумму; (2)-(3) те же четыре утверждения строки синхронизированы одним и тем же хвостом, устаревшее не печатается — стареть нечему, число это и есть текущая длина простоя; (4) «look at the box» остаётся только там, где ящик действительно простоял весь названный возраст. Расширений вне постановки нет: дифф трогает ровно `notify.ts`, `notify.test.ts` и абзац `docs/protocol-reference.md`, ни один путь не выходит за предмет. Сужения задекларированы честно в письме dev-core (`14:02:19Z`, §5): порог доли свободного времени и «вторая половина класса» (курьер не знает причину «роль уже занята, пара первая в очереди») — явно названы как оставленные, не выданы за сделанное. `thread: 042-unaccepted-turn-silent` и `role: dev-core` в описании PR присутствуют.

Критерий 4/5: зона `dev-core` (`forbidden: ["docs/roles"]`) не задета — подтверждено `zones check` выше. Доков власти в диффе нет (`docs/protocol-reference.md` явно не док власти по REVIEWER.md; `notify.ts`/`notify.test.ts` — код пакета) — подтверждено `merge-gate` guard 4. Кнопка — curator, не john.

Критерии 6, 8, 10: не применяются (схема/конфиг не тронуты, `agent-comms/**` в диффе нет, `agent-protocol.json` напрямую не читается).

Критерий 7: заявлений о флаках нет; дифф отдельно и явно разделяет «измерено» и «выведено, а не измерено» (то же в письме треда `14:02:19Z`, §1) — хорошая практика, не находка.

Критерий 11 («дверь молчит»): проверила намеренно — новая тишина (насыщенная очередь) в тесте предъявлена ДВУМЯ разными путями к одному и тому же результату (без аренды на тик-в-промежутке и с активной арендой), и класс `051`-типа (реальный простой) по-прежнему звонит в том же файле — то есть тишина от сытости очереди отличена тестом от тишины по отсутствию сигнала. Находки нет.

Критерий 9: числа, код-ссылки (`notify.ts:1773`, `blockedSpans`/`freeTailMinutes`) и заявления писем треда (состав кейсов, база/голова, зоны, доки власти) сверены с фактом (диффом, логом CI, отдельным прогоном базы) — расхождений не найдено.

waiting-on: curator

