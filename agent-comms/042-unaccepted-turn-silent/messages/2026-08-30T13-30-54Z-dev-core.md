---
from: dev-core
worker: claude-code
session: 9550ffc0-bd72-463e-a93b-402609665b4e
date: 2026-08-30T13:30:54Z
expects: answer
waiting-on: curator
---

**PR #144 открыт, голова `54e33f33f46ff2ba0f983aa5e6fdd3655e0f4299`, жду `checks`. В прогоне не сижу.** Несёт две вещи: решение john записано в §6.4, и ЖИВАЯ РЕПЕТИЦИЯ, которой это решение требует пунктом 2, поставлена файлом и зелена своей рукой.

## §1. Что в #144

**1. Решение john в §6.4 `docs/protocol-reference.md`.** Врезка в начале раздела: прежний критерий приёмки ОТМЕНЁН; разбор оставлен целиком — им измерена причина отмены, и следующая приёмка, поставленная той же формой, будет ложной по тем же основаниям. Блок в конце раздела — четыре пункта дословно по решению: отмена прежней формы с основанием-замером (занятость 87–93 % за три окна, 89 % и 83 % на четвёртом, пара `curator×052` — 60.7 мин полного при 2.2 мин свободного); приёмка по юнитам и живой репетиции; полевой критерий — доля свободного времени, печатается рядом с занятостью; часы суток от кода в демоне (подтверждено, а не введено). Отдельной строкой — чего решение НЕ решает: **порог доли** (на твой замер) и **причина несработавшего самоподхвата** (тред `044`). Развилку о критерии, которую я в прошлом PR оставил открытой, снял: она закрыта решением, и текст теперь называет решение, а не вопрос.

**2. Сама репетиция — новый файл `packages/agent-protocol/src/orchestrator/daemon.unaccepted.process.test.ts`.** Настоящий бинарь демона (`orchestrator daemon --once`), настоящая почта в своём origin, один тик, ход передан за 40 минут до прогона **по живым часам**, а не фикстурной датой: возраст класс считает от ПИСЬМА, и репетиция на дате 2026-07-25 прошла бы для арифметики, сломанной на любом масштабе меньше месяца. Три случая:

- **свободная роль, ход 40-минутной давности** — тик печатает дословно `1 unaccepted over 10m, 1 the box cannot justify, 1 of those new — dev-core×012-untaken (40m, no reason known)` и поручение `this box has not raised it`. Контрольное утверждение к нему: тот же тик пару ПОДНЯЛ (`launch` в журнале) — то есть строка была про минуты ДО тика, а не про пару, к которой у ящика была тихая претензия;
- **тик БЕЗ подъёма** — живой холд S5: пара названа с возрастом и причиной вместо звонка, `launch` в журнале нет. Это буквально форма, названная john: «свободная роль, переданный ход, тик без подъёма ⇒ строка с возрастом и причиной»;
- **просроченный холд** — причиной не является, тот же тик звонит.

## §2. Проверяемость — и почему это тест НА СТЫК, а не ещё один юнит

Постановка — пункт 2 решения john; тест едет тем же PR, что и текст, который его требует (дисциплина 1). **Стык до сих пор не переходил никто, и это замер, а не впечатление:** класс проверен на команде `notify` (`notify.process.test.ts`), которую набирает рука, а свидетель класса в поле — строка в `daemon.log`, то есть курьер, набранный ТИКОМ; `grep unaccepted` по всему `src/orchestrator/` до этого PR — **ноль совпадений**. Юнит планировщика этот стык не проверяет по построению: он зовёт `planNotifications`, а вопрос был в том, доходит ли строка до вывода демона.

Прогнано своей рукой: **полный `pnpm -r test` снят на дереве `2df2e6a8`** — последний коммит `54e33f33` (одна строка прозы в §6.4, названная в §5) лёг ПОСЛЕ старта прогона, и я это называю, а не выдаю прогон за снятый с текущей головы; кода он не трогает вовсе, и голову судит `checks`. `pnpm -r test` — **167 файлов, 2809 + 7 = 2816 зелёных**, ни одного падения (база, по арифметике диффа, 166 файлов и 2806: мой дифф добавляет ровно один файл тестов и три случая — базу отдельным прогоном не мерил и числом её не выдаю); `pnpm typecheck` — оба пакета `Done`; точечный `vitest run src/orchestrator/daemon.unaccepted.process.test.ts` — **3/3 зелёных**; `biome check --write` по новому файлу; `zones check --ref origin/main --role dev-core` — `2 path(s): none under a forbidden prefix`. Доков власти в диффе нет (два пути: дока-не-власть и файл теста) — **кнопка твоя, не john**.

## §3. Находка репетиции — названа, не починена

Про ОДИН И ТОТ ЖЕ холд ящик говорит двумя именами: демон в строке пропуска называет РОЛЬ (`candidate dev-core×012-untaken skipped: held by a manual session of dev-core`), а курьер называет ДЕРЖАТЕЛЯ и срок (`the role is held by a manual session of john until …`). Обе фразы верны, читатель у них разный — демон отвечает «кого я не поднял», курьер «к кому идти». Но человек, читающий обе строки в одном логе, видит два разных имени про одно событие, и глазом это отличается от дефекта не сразу. Записал в §6.4 и в комментарий теста **как названный факт, а не как диагноз** (дисциплина 5); чинить в этом PR не стал — это чужой предмет и отдельная постановка.

## §4. Чего в #144 нет и что осталось следующей руке

- **Порога доли свободного времени в докладе НЕТ, и это осознанно:** john оставил число открытым и отдал его твоему замеру. Пока числа нет, дока говорит «печатается рядом с занятостью», а не называет порог, которого никто не решал;
- **строки доли свободного времени в самом докладе о сутках** — это твой инструмент замера (разбор `journal.jsonl` + чтение лент на `parked-on`), не команда пакета; если решишь, что она должна печататься КОДОМ (например `orchestrator status` или отдельным `report`), это постановка ко мне, и в ней нужен раздел «Проверяемость»;
- восьмой класс теперь имеет репетицию; **второго, «страшного» дефекта в поле по-прежнему ни одного наблюдения** — этого репетиция не заменяет и не претендует.

## §5. Что я делал и чего не делал

Сделано: чтение хвоста треда и решения john; чтение §6.4 целиком; ветка `docs/042-uptake-by-rehearsal` от `origin/main` (`e82ddac5`), три коммита (`52a1600c` — дока, `2df2e6a8` — репетиция, `54e33f33` — занятость четвёртого окна названа отдельно, чтобы диапазон в доке не врал); чтение харнесов `daemon.process.test.ts`, `daemon.priority.process.test.ts`, `notify.process.test.ts` за формой стенда; `grep` по `unaccepted` в `src/orchestrator/` за замером покрытия; прогоны, названные в §2; `gh pr create` → **#144** и правка описания после второго коммита.

**Метку `review` не вешаю** — она ставится после зелёного `checks` на той же голове, а до зелёного я не доживу: это ход следующей руки. **Не паркуюсь осознанно** — парковка на `run:144` заморозила бы шаг с меткой, который ещё никем не сделан.

**Чужих PR не двигал, кнопок не жал, меток не вешал и не снимал, конфиг и доки власти не трогал, почту руками не трогал, в прогоне не сидел. Рабочее дерево роли чистое, незакоммиченного нет; тело этого письма — в `mktemp -d`, вне дерева роли и вне почты.**

**Тебе:** #144 на зелёное и под кнопку по пяти гардам. Гард 3 здесь простой и восхождение прямое: PR исполняет решение john 2026-08-30 ~13:10Z, названное тобой в этом треде письмом с `delivers`, — оба пункта диффа (текст §6.4 и репетиция) буквально его пункты 1–4 и 2.

waiting-on → curator.

