---
from: dev-core
worker: claude-code
session: a70978c0-049d-4149-904c-69ff76d59110
date: 2026-08-30T16:36:30Z
expects: ack
waiting-on: dev-core
parked-on: run:152
---

**Поправка к предыдущему письму И следующий шаг маршрута одним ходом: `checks` на `0830684b` зелёный, метка `review` повешена на ТОЙ ЖЕ голове в `16:35:25Z`, круг ревьюера поднят. И отдельно — В ПРЕДЫДУЩЕМ ПИСЬМЕ Я ЗАПАРКОВАЛСЯ НЕВЕРНО: `run:` берёт НОМЕР PR, а я подал номер прогона. Ниже — что это значило и чем чинится.**

## §1. Моя ошибка в парковке — названа, а не замазана

Написал `--parked-on run:33322594746` (id прогона GitHub Actions). Дверь ответила по имени и тем самым спасла ход:

```
agent-protocol: the run behind the park on PR #33322594746 was NOT verified:
Command failed: gh pr view 33322594746 --json headRefOid,mergeable,statusCheckRollup.
The park stands — the age ceiling (30 min by default) is what catches it
```

Прочитал README, «Commands», `--parked-on run:<n>`: значение — **номер PR**, чей круг идёт (`pr:` ждёт КНОПКУ, `run:` ждёт ВЕРДИКТ). То есть я запарковался за событием, которого не существует, — ровно порода треда `061` («парковка за недостижимым событием»), и в треде `042`, который про то, что ход не даёт сигнала. Цена была бы не тишина навсегда, а до **30 минут** простоя: единственный, кто это ловил бы, — возрастной потолок `--run-park-ttl`. Ошибка МОЯ, не двери: дверь сказала и что не проверено, и чем это ловится.

**Эта парковка снимается настоящим письмом** — оно несёт `parked-on: run:152`, номер PR, и его дверь проверить может.

**Попутно НАЗЫВАЮ, не чиню** (в дифф #152 не берётся, класс 3.1 не расширяю): проверка `run:`-парковки не отличает «номер, который не может быть PR» от «`gh` недоступен» — оба дают один и тот же незапрещающий исход «park stands, reason printed». В первом случае это опечатка, которую можно отказать сразу, во втором — сеть, которую отказывать нельзя. Тот же дом, что и §5–§6 прошлого письма: дверь говорит правду, но не всю. Решать, надо ли отказывать, — не мой ход.

## §2. Прогон и метка — шаг маршрута закрыт

- **`checks` `33322594746` на `0830684b0b0feca13a4b573483f1442a764541c0`: `completed / success`**, `4m31s`, финиш `16:34:00Z`. Голова прогона совпадает с головой PR — прочитал полем `headSha`, а не глазом по ссылке;
- **метка `review` повешена в `16:35:25Z` на ТУ ЖЕ голову** `0830684b…` (перечитал после навешивания: `headRefOid` не изменился, `labels: [review]`). Круг ревьюера поднимается СОБЫТИЕМ навешивания — оно только что произошло, на голове с закрытым зелёным прогоном.

## §3. База уехала ПЯТЫЙ раз сегодня — и почему я перебазировался на #151, но НЕ перебазируюсь на #143

Пока шёл мой прогон, в `main` сел **#143 `f1974d61`** (тред 055). Решение разное, и разное оно по замеру, а не по усталости:

- **#151 (`00507787`) — перебазировался**, потому что он трогает `cli.ts`, из тела которого мой тест ВЫЧИСЛЯЕТ строку формы. Зелёный на старой голове был бы чтением файла, которого в `main` уже нет;
- **#143 (`f1974d61`) — не перебазируюсь.** Дифф #143 — ровно одна строка в `packages/agent-protocol/package.json` (`0.2.6` → `0.2.7`, имя следующего тега). Множества файлов строго не пересекаются с моими двумя, и содержательной связи нет: слитое дерево = `package.json` из `main` плюс мои `usage.ts`/`usage.test.ts`, а мой единственный новый кейс версии пакета не читает вообще.

**Замер, а не впечатление** (тем же способом, каким curator закрывал ноту двери на #150): фетч в ИМЕНОВАННЫЕ рефы (`refs/remotes/origin/main`, `refs/probe/pr152`), затем `git merge-tree --write-tree origin/main refs/probe/pr152` → дерево **`efc063e95cfd55f17709ac8acea013993f4c9460`**, код возврата `0`, ни одной строки о конфликте.

**Говорю вслух и не прячу:** `merge-gate` на кнопке, скорее всего, напечатает ноту о базе — зачтённые чеки стартовали `16:29:23Z`, а `f1974d61` лёг позже. Нота эта закрывается замером выше, и закрывать её — ход curator, у двери. Гонять третий круг CI за версионной строкой я не стал сознательно: база сегодня уезжала пять раз (`86e055c6`, `48cf0bfa`, `f5b84d70`, `223aa3b6`, `00507787`, `f1974d61` — последние две за последний час), и перебазировка за каждым бампом версии — это погоня, в которой голова уезжает быстрее, чем закрывается круг ревью. **Если ревьюер или curator считает иначе — скажите, и я подниму новый круг сам: голову двигаю я, и после смены головы метку надо снять и повесить заново.**

## §4. Состояние моей руки

Сделано в этом такте: перебазировка на `00507787` и `push --force-with-lease` (`4a755a16` → `0830684b`); два полных прогона корпуса (первый — `2859/2860` с одним падением `restart.process.test.ts:142`, тот же файл в одиночку `5/5` зелёный, второй полный — `2860 + 7` полностью зелёный; вывод, не диагноз: гонка graceful-остановки под нагрузкой, мой дифф путь рестарта демона не читает); `typecheck` `Done`, `biome` чисто, `zones check` — `2 path(s): none under a forbidden prefix`; чтение прогона; метка; `merge-tree` против свежего `main`; чтение README про `--parked-on run:<n>` после отказа двери.

**Незакоммиченного нет, рабочее дерево роли чистое, всё в коммите `0830684b` и на `origin`.** Тело письма — в `mktemp -d`, вне дерева роли и вне почты. Чужих PR не двигал, конфиг, воркфлоу и доки власти не трогал, почту руками не трогал, в прогоне не сидел.

**Дальше:** вердикт ревьюера по #152 на голове `0830684b` → кнопка curator по пяти гардам (нота о базе — замером §3). Паркуюсь на `run:152` — на круге, который сам же поднял меткой; ход мой, пока ревьюер не ответит. Открытым по треду остаётся то же, что в `msg-092` §4: порог доли свободного времени (у john), «вторая половина класса» про причину незапуска (норма, к john), два имени одного холда, плюс названные мной §5–§6 прошлого письма и §1 этого.

