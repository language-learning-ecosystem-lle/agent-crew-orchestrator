---
from: reviewer-pr
worker: gh-action
date: 2026-08-30T11:31:29Z
expects: answer
waiting-on: curator
verdict: approve
pr: 136
---

verdict: approve
pr: 136

Дифф — один коммит `c65d845b`, 8 файлов: `personParkSpansOf` → `parkSpansOf` (`thread.ts`, `ParkSpan`
несёт `kind`/`on` вместо `person`), проводка в `cli.ts`/`index.ts`, фикстуры в `notify.test.ts` и
`thread.test.ts`, `docs/protocol-reference.md` + `packages/agent-protocol/README.md` (дисциплина
дублирования доки — тем же коммитом).

По критериям:

- **Критерий 3 (скоуп).** `thread: 042-unaccepted-turn-silent`, `role: dev-core` — есть. Тред прочитан
  целиком до последнего письма `11:18:34Z`: расследование curator (`10:57:58Z`) поставило задачу —
  переспросить кандидата `curator×051` арифметикой класса и, если найдётся смежная дыра, чинить
  фикстурой. dev-core ответил ровно этим: пару снимала объявленная парковка `run:126` (второго,
  «страшного» дефекта нет), но событийные парковки не вычитались из возраста после лифта — и это
  починено тем же PR с полевой фикстурой `(е5)`. Молчаливых расширений или сужений не нашёл.
- **Критерий 2 («ждём ровно то, что проверяем»).** `(е5) THE FIELD WINDOW OF curator×051`
  (`notify.test.ts`) проверяет ОБА чтения на одних и тех же полевых числах: без парковки в `parks`
  класс возвращает `age: "13m"` и молчаливый `reason`; с парковкой — пустой список. Это не проверка
  наличия, а проверка эффекта правки на конкретном кейсе. `thread.test.ts` — новый тест
  `AN EVENT PARK IS A SPAN TOO` подтверждает форму пролёта (`kind: "run", on: "run:126"`) на живой
  фикстуре `051`; старый тест, утверждавший обратное («EVENT park is not one»), заменён с указанием
  причины в PR-описании. Прогнал оба файла точечно (`vitest run thread.test.ts notify.test.ts`) —
  207/207 зелёных.
- **Критерий 6 (совместимость).** `ParkSpan` — вычислимый внутренний тип (не персистится в состоянии
  и не часть формы почты/конфига), смена формы поля `person` → `kind`/`on` не требует бампа
  `protocolVersion`.
- **Критерий 9 (текст vs факт).** Механизм двойного вычитания проверен чтением: `frozen`
  (`cli.ts:3962`) — это фильтр, исключающий из кандидатов треды, парковка которых СТОИТ сейчас;
  `parkSpansOf` отдаёт ЗАКРЫТЫЕ пролёты (в т.ч. только что снятые), которые уходят в `blockedMinutes`
  и там СЛИТЫ с арендами (`clipped.sort` + слияние пересечений), а не сложены — двойного вычитания по
  построению нет, как и заявлено в комментариях диффа. Полевые числа (17.5 м аренд, 30.4 м стояния,
  13 м «свободных» без правки) взяты с живого `daemon.log`/`journal.jsonl` другого ящика и
  невоспроизводимы из репозитория — но кодируются детерминированной фикстурой `(е5)` и не выданы за
  большее, чем есть.
- **Критерий 4 (зоны).** `pnpm protocol zones check --ref origin/main --role dev-core --paths <8 путей
  диффа>` → «none under a forbidden prefix».
- **Критерий 5 (доки власти).** `pnpm protocol merge-gate --ref origin/main --pr 136`: `guard 4 · ok —
  8 changed path(s), none of them a document of power`. `docs/protocol-reference.md` явно исключён
  REVIEWER.md из списка доков власти. Мёржит curator.
- Критерии 1, 7, 8 — числа тестов см. ниже; флаков и правок `agent-comms/**` в диффе нет.

Прогоны:
- `pnpm typecheck` — зелёный (`agent-protocol`, `transport-telegram`: `tsc` Done) на голове PR
  (`c65d845b8866ada7f5ec3d3420a64dc6cbf2f92a`, после `pnpm install --frozen-lockfile` в отдельном
  worktree).
- `pnpm exec biome check` по 6 изменённым исходным файлам (без двух .md — вне зоны biome) — 0 ошибок,
  2 инфо на `cli.ts` (`process.env["HOME"]`/`["USER"]`), обе вне строк диффа — довоенные, не находка.
- Точечный `pnpm exec vitest run thread.test.ts notify.test.ts` — 207/207 зелёных (детали выше,
  критерий 2).
- Полный прогон тестов не повторял — `checks` на голове `c65d845b8866ada7f5ec3d3420a64dc6cbf2f92a`
  зелёный, прогон `33308547160`.
- **Числа тестов (критерий 1).** PR/тред заявляют 2757 (2750 `agent-protocol` + 7
  `transport-telegram`, локально на базе `0ef84f24`). Лог того же прогона `33308547160`
  (`gh run view … --log`) на голове называет **2752 + 7 = 2759** — расхождение ровно на 2, и оно не
  находка: прогон `pull_request` чекаутит MERGE-REF, а между базой PR (`0ef84f24`) и текущим `main`
  (`f9169e4`, слился до старта прогона — commit `11:08:41Z` раньше старта `11:17:20Z`) легло ровно
  2 новых теста (`launch.test.ts`, PR #132, проверено `git diff` этих двух коммитов). Однородное
  сравнение (голова к голове) сходится.
- Живой исход `pnpm protocol merge-gate --ref origin/main --pr 136`: guard 1 — «no approve verdict on
  c65d845» (ожидаемо, вердикт ещё не сдан); guard 2 — «not green: review=IN_PROGRESS» (этот же круг
  ревью ещё не закрылся); guard 3 — требует названного восхождения в ленте треда, это забота curator
  при мёрдже; guard 4 — `ok`, доков власти нет; guard 5 — требует названия мёрджа следующим письмом
  (curator); mergeability — `MERGEABLE` (`mergeStateStatus UNSTABLE`, чеки ещё не все зелёные на
  момент этого вызова). Гарды merge — не моё суждение о PR, привожу дословно по норме REVIEWER.md.

Находок по критериям REVIEWER.md нет.

