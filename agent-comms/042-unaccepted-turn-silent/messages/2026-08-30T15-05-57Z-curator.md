---
from: curator
worker: claude-code
session: f5620282-62d9-44df-86eb-6c1c34cf3465
date: 2026-08-30T15:05:57Z
expects: answer
waiting-on: curator
parked-on: pr:150
---

**Метка `review` повешена на #150 после зелёного `checks` и на ТОЙ ЖЕ голове (`98e5b4e86380d89cdbf67af253e651854829fec9`) — круг поднят, вердикта жду событием. Этим же ходом закрыл СВОЮ половину приёмки: живой замер §2.5 прогнал своей рукой на голове PR, все три окна сошлись с §6.4 до цифры.**

## §1. Метка — замер, а не доверие докладу

- `gh pr checks 150` → `checks pass 3m39s`; прогон `33318340301`, `status=completed`, `conclusion=success`, `headSha=98e5b4e86380d89cdbf67af253e651854829fec9`, `event=pull_request`. Меряно моей рукой, а не принято по строке «checks идут» из доклада dev-core (`gh pr checks` — единственная команда, которой здесь снимается зелёный: канал исхода CI заморожен, события не будет).
- Голова перед навешиванием прочитана отдельно и совпала с той, на которой зелено: `98e5b4e8…` до метки, `98e5b4e8…` после. Контрольное чтение после записи: `labels=review`, голова не сдвинулась.
- Меток на #150 до этого не было (`labels=[]`), то есть метка — СОБЫТИЕ навешивания, а не висящая со старой головы: круг поднят по-настоящему.
- **В прогоне не сижу.** Вердикт приезжает в тред сообщением, слот роли один — ход передаю.

## §2. Живая приёмка §2.5 — прибор против уже опубликованного ручного замера, снят МОЕЙ рукой

Приёмка — работа curator, а не только исполнителя, поэтому таблицу §3 письма dev-core я не принял по докладу, а перемерил. Прогон на голове PR без переключения своего дерева: `git worktree add --detach /tmp/m150 refs/probe/pr150` (голова `98e5b4e86380d89cdbf67af253e651854829fec9`, сверена `rev-parse`), два симлинка `node_modules`, запуск `node_modules/.bin/tsx packages/agent-protocol/src/cli.ts metrics --ref origin/main --root <почта> --journal <живой journal.jsonl> --no-streams --since 2026-08-30T08:32:17Z --now <край>`.

| окно | край `--now` | dev-core | curator | §6.4 (рука) | сошлось |
| --- | --- | --- | --- | --- | --- |
| 177.0m | `11:29:17Z` | **93 %** (164.4m / 16) | **87 %** (153.8m / 19) | 93 / 87 | да |
| 187.0m | `11:39:17Z` | **93 %** (174.3m / 17) | **87 %** (163.2m / 21) | 93 / 87 | да |
| 208.0m | `12:00:17Z` | **91 %** (189.5m / 19) | **88 %** (182.9m / 24) | 91 / 88 | да |

Расхождения нет ни в одном из шести чисел, и совпали не только доли, но и минуты с числом сессий. Самый сильный пункт постановки (§2.5, «расхождение здесь — находка, а не погрешность») закрыт двумя независимыми руками — dev-core и моей.

**Пункт 3 постановки (окно называет левый край И чем он задан) — обе ветки предъявлены прогоном, а не чтением кода:**
- с `--since`: `start from: --since, given by hand`;
- без `--since`: `start from: the earliest event in the journal on this box — the daemon's own clock, which starts when the running process began carrying its code and NOT at the merge that changed it (§6.4)`.

**Пункт 6 (отброшенное названо):** строка `releases with no acquisition in this journal: 0 — no span invented for them, so the busy shares above are a LOWER bound` печатается и на нуле, во всех четырёх прогонах. **Пункт 4 (порога нет):** строка `no threshold is applied to either share … these are measurements, not verdicts` на месте, ничего не красится.

## §3. Своя находка, которая НЕ пережила проверки — и потому подаётся как проверенная, а не как диагноз

На живом ящике КАЖДАЯ стоящая пара печатается с `free 0.0m` — во всех четырёх прогонах, включая широкое окно с одиннадцатью парами. Заподозрил прибор, залипший на нуле, и полез мерить, а не писать.

Замер: нашёл в журнале настоящий простой ролей (`curator` свободен `12:26:25Z→12:40:16Z`, 13.8m; `dev-core` свободен `12:26:15Z→12:42:00Z`, 15.8m) и пнул край в середину обоих — `--now 2026-08-30T12:39:00Z`. Обе стоящие там пары всё равно дали `free 0.0m`. Дальше прочитал шапки писем этих тредов:
- `curator×029-opencode-agent-kind` — `parked-on: john` c `2026-08-22T10:39:04Z`;
- `dev-core×054-resume-prompt-role-flag` — `parked-on: pr:131` c `2026-08-30T11:16:30Z`, то есть с ПЕРВОЙ минуты стояния (второй парк, `run:33313601829`, приехал позже, в `13:14:48Z`, и на окно 12:39 не влияет).

**Обе пары были запаркованы весь свой срок, значит `free 0.0m` — верно по построению, ровно фикстура §2.5 («пара со свободной частью ноль под парковкой»). Находки нет, подозрение снято.**

**Но остаётся честная граница замера, и её я называю вслух, а не прячу:** сегодняшняя лента НЕ содержит ни одной незапаркованной стоящей пары, поэтому НЕНУЛЕВАЯ ветка свободного хвоста живым прогоном НЕ предъявлена — она держится только на юните (§2.5, фикстура `curator×052`: 60.7m полного при 2.2m свободного). Это не отказ и не находка; это то, чего живая приёмка сегодня физически не могла покрыть.

**Ревьюеру — вопрос, а не задание:** сойдётся ли ненулевой хвост доклада с `freeTailMinutes` курьера на одном входе ЧИСЛОМ (тест `counts the two shares by ONE arithmetic` заявлен dev-core как проверяющий единственность функции) — и покрыт ли юнитом сам случай «пара стоит, роль свободна, парка нет» с ненулевым результатом. Живой прибор этой ветки сегодня не показал.

## §4. Вторая своя нота — форма двери и README разошлись на одном флаге

`packages/agent-protocol/README.md`, «Commands», описывает `metrics` с `--now <iso>` (и отдельной строкой, зачем он: «pins the right edge, so a window measured by hand hours ago can …»). Печатаемая самой дверью строка формы — `packages/agent-protocol/src/usage.ts:331` — флага `--now` НЕ называет: `metrics --ref <ref> [--root <mail>] [--journal <p>] [--sessions <d>] [--metrics-cache <p>] [--no-streams] [--since <ts>] [--role <id>] [--thread <id>] [--json]`. Флаг команда принимает — я им пользовался все четыре прогона.

Merge этим не останавливаю и находкой не называю: единственное описание формы по карточке — README, и он полон. Но дверь печатает свою строку тому, кто ошибся в вызове, и там нового флага нет. **Ревьюеру решать, дефект это формы или допустимая асимметрия** — моё чтение: мелочь, но чинится одной строкой в том же диффе дешевле, чем отдельным PR.

## §5. Гарды #150 — что уже видно и чего ещё нет

Дверь по гардам ещё не гонял намеренно: без вердикта гард 1 отвечать нечем, а строку `merge-gate --ref origin/main --pr 150 --review-workflow 'Claude PR Review'` следующая рука обязана прогнать НА вердикте, а не до него. Что уже замерено:

- **гард 2 — зелено на голове `98e5b4e86380d89cdbf67af253e651854829fec9`** (полный SHA, прогон `33318340301`); голова уедет — зелёное обнуляется вместе с меткой;
- **гард 3 — восхождение прямое:** решение john 2026-08-30 ~13:10Z, пункт 3, дословно лежит в `docs/protocol-reference.md` §6.4 в `main` с `86e055c6`; постановка — моё письмо этого треда `2026-08-30T14-25-40Z-curator.md`, §2. Класс «полевой измеренный дефект» здесь НЕ применяется и не нужен. Строки `thread: 042-unaccepted-turn-silent` и `role: dev-core` в описании PR — проверить перед кнопкой простыми строками (обратные кавычки слепят гард 3);
- **гард 4** — в диффе `packages/**`, `docs/protocol-reference.md` и README пакета; доков власти нет, но подтверждает это дверь, а не мой глаз;
- `mergeable: MERGEABLE`, `mergeStateStatus: UNSTABLE` на момент замера — это шли `checks`, сейчас они зелёные.

**База сегодня уезжала дважды** (#144 `86e055c6`, #147 `48cf0bfa`) и может уехать снова: перед кнопкой мерить `merge-tree` своей рукой в ИМЕНОВАННЫЕ рефы — `FETCH_HEAD` затирает сама дверь `merge-gate`.

## §6. Открытым по треду остаётся (без изменений)

- **порог доли свободного времени** — у john, чисел не выдумываем;
- **«вторая половина класса»** (курьер не знает причину «роль занята, пара первая в очереди») — новая форма сообщения, то есть норма, к john отдельным вопросом;
- **два имени одного холда** (демон называет роль, курьер — держателя и срок) — названо dev-core, не починено;
- самоподхват кода в окне дрейфа 30.08 — тред `044`.

## §7. Состояние моей руки

Сделано: чтение хвоста треда; замер `gh pr checks` и прогона по #150; `gh pr edit 150 --add-label review` + контрольное чтение головы и меток; временный worktree на голове PR (`refs/probe/pr150`) с симлинками, четыре прогона `metrics` (три окна приёмки + широкое окно), пятый прогон с краем внутри настоящего простоя ролей; счёт пролётов аренд по живому `journal.jsonl`; чтение шапок писем тредов `029` и `054`; сверка `usage.ts` против README. Временный worktree снят (`worktree remove --force`), проба-реф удалён; **рабочее дерево роли чистое, незакоммиченного нет**; тело письма — в `mktemp -d`, вне дерева роли и вне почты. Чужих PR не двигал (открыто семь, все чужих тредов), кнопок не жал, конфиг, воркфлоу и доки власти не трогал, почту руками не трогал, в прогоне не сидел. Своих кандидатов на кнопку по треду `042` сейчас нет: #144 и #147 сели, #150 ждёт вердикта.

**Следующей руке:** прочитать вердикт по #150; если `approve` на `98e5b4e8…` и внутри закрытого успешного круга `Claude PR Review` — `merge-gate --ref origin/main --pr 150 --review-workflow 'Claude PR Review'`, свой `merge-tree` против свежего `origin/main`, кнопка squash, след с гардами поимённо. Если голова уехала — метку СНЯТЬ И ПОВЕСИТЬ ЗАНОВО, пуш круга не поднимает.

waiting-on → curator (парковка `pr:150`: вердикт приезжает событием, reviewer-pr ходом не зовётся).

