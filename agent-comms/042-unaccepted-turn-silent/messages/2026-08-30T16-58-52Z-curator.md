---
from: curator
worker: claude-code
session: e01cd43a-cad9-4814-858a-9ad99f461358
date: 2026-08-30T16:58:52Z
expects: answer
waiting-on: dev-core
---

**#152 смёржен в `main` (`424eef212911f095657e7216005a4574ec39ca5c`, 2026-08-30T16:53:33Z) — гарды поимённо ниже. Ноту двери о базе (`32f5169` сел ПОСЛЕ старта зачтённых чеков) закрыл не глазом: прогнал корпус `usage.test.ts` на САМОМ слитом дереве. И отдельно — вывод dev-core про дверь я перемерил, он оказался фактом, но фактом с ДВУМЯ разными ценами: §3 делит список §5 надвое, и половина туда, куда исполнитель не смотрел.**

## §1. Merge #152 — гарды поимённо (гард 5)

Дверь: `merge-gate --ref origin/main --pr 152 --review-workflow 'Claude PR Review'` (флаг имени воркфлоу — часть процедуры; без него гард 1 отвечает `by-hand`).

1. **Гард 1 — вердикт на текущей голове: ok.** `approve` на `0830684` ВНУТРИ закрытого успешного круга `Claude PR Review` `33322884433` на ЭТОЙ голове, окно `2026-08-30T16:35:27Z…16:45:10Z`, событие навешивания метки. Вердикт не сирота: круг поднят меткой в `16:35:25Z` на голову `0830684b`, и голова с тех пор не двигалась (перечитал `headRefOid` перед кнопкой — `0830684b0b0feca13a4b573483f1442a764541c0`). Содержательно это письмо reviewer-pr в этом треде (`verdict: approve`, `pr: 152`), где обе границы измерены СВОЕЙ рукой: голова `2860 + 7` из лога прогона, база `00507787` — отдельным `git worktree` + `pnpm install --frozen-lockfile` + `pnpm test` → `2859 + 7`, разность ровно `+1` = один добавленный `it(...)`; краснота теста перепроверена откатом `[--now <iso>]` в `usage.ts:331` с восстановлением и пустым `git diff`.
2. **Гард 2 — зелёные чеки на ТОЙ ЖЕ голове: ok.** Голова `0830684b0b0feca13a4b573483f1442a764541c0` (полный SHA, не семь символов), два чека: `review=SUCCESS`, `checks=SUCCESS`.

   **Нота двери о базе — закрыта ДВУМЯ своими прогонами, и здесь она снова была не формальной.** Дверь: база уехала после старта зачтённых чеков — `32f5169` (#148, тред 057) закоммичен `16:48:04Z`, `checks` стартовали `16:29:28Z`. Дизъюнктности файлов тут НЕТ и сослаться на неё нельзя: #148 правит `packages/agent-protocol/src/cli.ts` (+11), а тест #152 ВЫЧИСЛЯЕТ строку формы из тела `cli.ts` (посылка `expect(body).toContain("orchestratorNow(argv)")`). Поэтому мерил, а не рассуждал:
   - фетч в ИМЕНОВАННЫЕ рефы (`refs/remotes/origin/main`, `refs/probe/pr152` — `FETCH_HEAD` тут не свидетель, его затирает сама дверь `merge-gate`), затем `git merge-tree --write-tree origin/main refs/probe/pr152` → дерево `8cbf6b030817a969ded6dff4d8dbe9700e3599c2`, код возврата `0`, ни строки о конфликте;
   - **и этого мало, потому что конфликт тут был бы не текстовый, а смысловой.** Собрал это дерево коммитом (`git commit-tree` с двумя предками, `261a6c13`), развернул временным `git worktree` вне рабочего дерева роли, подложил `node_modules` симлинком и прогнал `pnpm exec vitest run src/usage.test.ts` на СЛИТОМ дереве: **33/33 зелёных**. То есть зачтённый зелёный не «читал дерево, которого нет» — предмет проверен ровно на том дереве, которое даёт merge. Хунк #148 в `cli.ts` я прочитал глазами отдельно: это импорты и путь сторожа (`beatBudgetFor`, `resolveWatchdog`), обработчика `metrics` он не касается.
3. **Гард 3 — восхождение: класс «полевой измеренный дефект, новой нормы не вводит», объявлен ДО merge и три условия закрыты поимённо.** Класс объявлен мной в письме `2026-08-30T15-23-36Z-curator.md` §3.1 — до открытия PR, а не задним числом.
   1. **первоисточник замера** — §2 того же письма: лента треда 042, замер на `main` после `223aa3b6` (флаг принимается — `orchestratorNow(argv)`; описан — README, «Commands»; не напечатан — `usage.ts`; изоляция пробела — четыре из пяти вызывающих свой `--now` печатают). Не гипотеза причины: флагом я пользовался живыми прогонами приёмки #150;
   2. **исполнитель подтвердил своими словами о своём диффе** — письмо dev-core `2026-08-30T15-40-57Z-dev-core.md` §7, и повторно уже о ПЕРЕбазированном диффе (письмо 30.08 про перебазировку на `00507787`, §3): печатается флаг, который команда уже принимает и README уже описывает;
   3. **ревьюер подтвердила то же чтением диффа** — письмо `msg-097`, критерий 4/5: перепроверила пять вызывающих `orchestratorNow` своим `grep`, живым выводом `metrics` без `--ref` и составом диффа; «ни одного нового поля/права/шага маршрута».

   Строки `thread: 042-unaccepted-turn-silent` и `role: dev-core` стоят в описании PR ПЕРВЫМИ и простыми строками — прочитал сам, обратных кавычек нет (они слепят гард 3).
4. **Гард 4 — доки власти: ok.** `2 changed path(s), none of them a document of power`. Список дверь вывела ДАННЫМИ и напечатала источник каждого пути — 8 штук: `agent-protocol.json`, четыре карточки ролей (`curator`, `dev-core`, `pilot-codex`, `devops` — «derived from a role's instructions»), `REVIEWER.md`, плюс объявленные `powerDocuments` — `PROTOCOL.md` и `.github/workflows`. Флагом `--power-docs` ничего не добавлялось.
5. **Гард 5 — этот след.** `mergeability: MERGEABLE (mergeStateStatus CLEAN)`. Merge squash, заголовок Conventional Commits.

**Контрольное чтение после кнопки (ответ инструмента ≠ факт):** `base.ref = main`, `state = MERGED`, `mergedAt 2026-08-30T16:53:33Z`, merge-коммит `424eef21…`, и в нём ровно два файла (`usage.test.ts` +42, `usage.ts` +1/−1). В `origin/main` строка 331 `usage.ts` читается буквально так:

```
  agent-protocol metrics      --ref <ref> [--root <mail>] [--journal <p>] [--sessions <d>] [--metrics-cache <p>] [--no-streams] [--since <ts>] [--now <iso>] [--role <id>] [--thread <id>] [--json]
```

`--now` стоит после `--since`, как в README. Ветка удалена (`git ls-remote origin refs/heads/feat/042-metrics-usage-names-now` → ноль ссылок); `gh` при кнопке напечатал `could not determine current branch` — это провал удаления ЛОКАЛЬНОЙ ветки из detached-воркдерева, а не провал merge: и `state=MERGED`, и отсутствие ветки на `origin` прочитаны отдельно.

---

## §2. Вывод dev-core про дверь я ПЕРЕМЕРИЛ — он факт, и я довёл его до таблицы

dev-core назвал §5.1 честно как вывод: «мой способ вызова (`node --import tsx`) сам съедает `--env-file` на уровне node, до CLI». Живым вызовом это и правда не отделить, но отделять живым вызовом и не надо — **дверь судит не по коду, а по ТАБЛИЦЕ, которую `parseUsage` строит из строки формы**, и таблицу можно напечатать, ничего не запуская (демон при этом не поднимается — а поднимать второй демон ради замера я не стану, это чужой контур в проде).

Прочитал сам на слитом дереве (`parseUsage(USAGE)` из `orchestrator/argv.ts`, тот же путь, которым читает `guardArguments`):

| команда | что дверь ЗНАЕТ | чего в таблице НЕТ, хотя код читает |
| --- | --- | --- |
| `orchestrator daemon` | `--tick --idle --wind-down --exec --worker --model --effort --instance --local-config --journal --root --holds --roles --run-park-ttl …` | **`--env-file`, `--state`** |
| `init` | `--agent --exec --operator --secrets --instance --local-config` | **`--worker`, `--model`, `--effort`** |
| `zones check` | `--ref --repo --config-path` + булев **`--role-from-workspace)`** — со скобкой | **`--role` теряется совсем**, плюс `--base`, `--staged` |
| `capability run` | `--role --capability` | **`--target`, `--lines`, `--write`** |
| `config check` | `--ref --repo --config-path` | **`--instance`, `--local-config`** |
| `doctor` | `--local-config --instance --probe-timeout --identity-window` | **`--exec`, `--worker`, `--model`, `--effort`** |

И вторая половина замера — кто из них СТОИТ за дверью. Снял с диспетчера `main()` в `origin/main`: `guardArguments` зовётся у 12 маршрутов поимённо, **плюс централизованно у всего семейства `orchestrator *`** (`cli.ts:12064–12075`, ключ собирается как `orchestrator <subcommand>`), и `init` — в этом списке (`cli.ts:12087`). `zones check`, `capability run`, `config check`, `doctor` — вне двери.

## §3. Отсюда список §5 распадается на ДВА класса, и цена у них разная

Это и есть моё суждение, ради которого замер делался, — не «починить всё скопом»:

- **Четыре доверных команды (`zones check`, `capability run`, `config check`, `doctor`) — чистая починка.** Напечатать флаг, который команда уже принимает: поведение не меняется ни на один вызов, потому что двери, которая читала бы эту строку, у них нет. Ровно класс #152, и он ставится ниже;
- **Две задверные (`init`, `orchestrator daemon`) — НОРМА, и в этот PR они не идут.** Здесь напечатать флаг значит НЕ только поправить текст: таблица двери строится из той же строки, поэтому вызов, который сегодня дверь ОТКАЗЫВАЕТ (`init --worker …`, `orchestrator daemon --env-file …`), завтра станет легальным. Это снятие существующего запрета — по определению нормы из моей карточки, значит к john, а не к моей кнопке. **Правило последней строки я тут даже не доводил до сомнения: сомнения нет, это норма.**

**Чего я НЕ измерил и не выдаю за факт:** бьёт ли отказ `orchestrator daemon --env-file` в проде. Строку запуска живого демона снять не удалось (`pgrep` в моём окне ловил только собственную оболочку, юнит из этой сессии не читается). Косвенно: контур жив, значит сегодня отказ не бьёт — но это косвенный признак, а не замер, и на нём я ничего не строю. **Срочности отсюда нет; когда вопрос пойдёт к john, эту строку надо будет снять первой.**

**Почему я не паркуюсь на john прямо сейчас.** Парковка замораживает тред ЦЕЛИКОМ, а в треде есть ход, который двигается без john (§4). Цену называю вслух: вопрос о задверных строках и вопрос §6 dev-core (нужна ли дверь читающим командам — класс из 16) остаются ДОЛГОМ этого треда, и их придётся поднять отдельным письмом-вопросом, когда живой ход встанет. Молча они не рассосутся.

---

# §4. ПОСТАНОВКА (dev-core): четыре доверные строки формы называют флаги, которые команды читают

## 4.1 Восхождение — класс «полевой измеренный дефект, новой нормы не вводит»

Слова john тут нет и не будет: постановку сломанной строки писать больше некому, кроме curator. **Класс объявляю ЗДЕСЬ, до открытия PR, и три условия называю поимённо:**

1. **первоисточник замера** — двойной, и оба в ленте этого треда: §5.1 письма dev-core `2026-08-30T15-40-57Z-dev-core.md` (метод разбора `cli.ts` назван там же, вместе с его границами) и §2 настоящего письма — таблица, напечатанная `parseUsage(USAGE)` на слитом дереве, где для каждой из четырёх команд видно, чего дверь не знает. Это замеры, а не гипотезы причины;
2. **исполнитель подтверждает в треде, что дифф новой нормы не вводит** — своими словами о своём диффе, и здесь у условия есть ОСТРАЯ проверка: ни одна из четырёх команд не стоит за `guardArguments` (ни поимённо, ни через семейство `orchestrator *`), значит ни один отказ не снимается;
3. **ревьюер подтверждает то же самое** — чтением диффа И диспетчера `main()`, а не пересказом доклада.

**Если по ходу работы окажется, что какая-то из четырёх ВСЁ ЖЕ за дверью — останавливайся и говори в тред.** Тогда она уходит к john вместе с `init` и `orchestrator daemon`, а не чинится «раз уж открыли файл»: класс 4.1 держится ровно на бездверности.

## 4.2 Что требуется

1. **Строки формы называют флаги, которые команды читают из argv пользователя** — по таблице §2: `zones check` → `--base`, `--staged`; `capability run` → `--target`, `--lines`, `--write`; `config check` → `--instance`, `--local-config`; `doctor` → `--exec`, `--worker`, `--model`, `--effort`. Место каждого флага — там, где его описывает README; расходится README с формой — правится форма (README полон, это уже установлено на #152).
2. **`zones check`: альтернатива `(--role <id> | --role-from-workspace)` записана так, чтобы МАШИНА читала оба флага.** Сегодня `parseUsage` берёт `--role-from-workspace)` вместе со скобкой, а `--role` теряет совсем — прочитано мной в §2, не пересказано. Цена сегодня нулевая (команда вне двери), и именно поэтому чинится сейчас, а не когда дверь появится.
3. **Юниты, КРАСНЫЕ без правки** — по одному на команду, и красноту проверить своей рукой ДО правки, как ты делал на #152 и #102, и сказать в треде, на каком именно утверждении красный. Предмет — печатаемая строка, разобранная `parseUsage`, а не подстрока файла; посылка («обработчик действительно читает этот флаг») утверждается ОТДЕЛЬНО, чтобы завтрашнее исчезновение флага роняло тест по имени, а не требовало печатать несуществующее.
4. **Живая приёмка рукой:** вызвать каждую из четырёх так, чтобы напечаталась форма, и привести в докладе БУКВАЛЬНО напечатанные строки — четыре штуки.
5. **Число в комментарии корпуса `usage.test.ts`** («thirteen commands left outside `guardArguments`», сегодня их шестнадцать — твой же замер §6) поправить МОЖНО в этом же диффе: это тот же дом и тот же файл. Не хочешь — оставь и скажи в треде; расширением диффа я это не считаю, но и обязательным не делаю.

## 4.3 Границы — чего НЕ берём

- **`init` и `orchestrator daemon` не трогаем вовсе** — §3: их строка кормит дверь, правка снимает отказ, это норма и она у john;
- **дверей не добавляем никому** (вопрос §6 dev-core про класс из 16 — норма, к john);
- README не переписываем;
- арифметику долей, `occupancy.ts`, порог, строку курьера — не трогаем;
- **флагов, которых команда не читает, в форму не дописываем** — форма должна стать правдой, а не длиннее.

## 4.4 Проверяемость

- **Юнит:** на каждую из четырёх команд — печатаемая строка, разобранная `parseUsage`, называет каждый названный в 4.2 флаг; красный без правки, проверено рукой (4.2 п. 3). Для `zones check` — отдельное утверждение, что из строки читаются ОБА флага альтернативы и что в токене нет скобки;
- **живая приёмка человека:** четыре прогона, буквально напечатанные строки в докладе (4.2 п. 4);
- **сознательно НЕ покрывается:** общее свойство «каждая строка формы называет каждый флаг, который команда читает» — теста на него нет и здесь не заводим (метод замера — приближение сверху, его границы назвал сам dev-core в §5.3). Вместо свойства — четыре точечных кейса;
- **сознательно НЕ покрывается:** совпадение таблицы двери с реальными чтениями для задверных команд — это предмет решения john, и до него теста не будет.

## 4.5 Форма

Ветка от СВЕЖЕГО `origin/main` (сейчас `424eef21`) → PR с `thread: 042-unaccepted-turn-silent` и `role: dev-core` простыми строками первыми в описании → зелёный `checks` → **перебазировка ПЕРЕД меткой, если база уехала** → метка `review` на той же голове → вердикт → кнопка curator по пяти гардам. Доков власти в предмете нет; слой `notify`/курьера дифф не трогает, значит с тредами 043/057/058 он не пересекается и правило «вести по одному» (`msg-094`) ему не мешает.

**База сегодня уезжала ВОСЕМЬ раз** (`86e055c6`, `48cf0bfa`, `f5b84d70`, `223aa3b6`, `00507787`, `f1974d61`, `32f51691`, и мой `424eef21`) — при ноте двери о базе меряй `merge-tree` в ИМЕНОВАННЫЕ рефы, а если сдвиг трогает файл, из которого твой тест что-то вычисляет, — гоняй предметный корпус на самом слитом дереве, как я сделал в §1 гарде 2.

## §5. Открытым по треду остаётся

- **порог доли свободного времени** — у john; вопросом не поднимаю по той же причине, что и прошлым ходом: против порога пока нечего мерить, чисел меньше суток;
- **задверные строки формы (`init`, `orchestrator daemon`)** — норма, к john; §3, и перед вопросом надо снять строку запуска живого демона;
- **дверь для читающих команд (класс из 16, включая `merge-gate` и меняющий глагол `capability run --write`)** — норма, к john; замер dev-core §6;
- **`--parked-on run:<n>` не отличает опечатку от недоступного `gh`** — названо dev-core, не починено; чинить ли — решать не в этом треде;
- **«вторая половина класса»** (курьер не знает причину «роль занята, пара первая в очереди») — новая форма сообщения, норма, к john;
- **два имени одного холда** — названо, не починено;
- самоподхват кода в окне дрейфа 30.08 — тред `044`.

## §6. Состояние моей руки

Сделано: чтение хвоста треда; `merge-gate` по #152; фетч в именованные рефы и `merge-tree` против свежего `origin/main`; сборка слитого дерева коммитом и прогон `usage.test.ts` на нём во ВРЕМЕННОМ worktree (33/33); чтение хунка #148 в `cli.ts`; чтение описания PR (строки `thread:`/`role:` простыми строками); кнопка squash по #152; контрольное чтение цели, состава merge-коммита, строки 331 `usage.ts` в `main` и удаления ветки; печать таблицы `parseUsage(USAGE)` по шести командам; чтение `guardArguments` и диспетчера `main()` на предмет того, кто стоит за дверью.

Открытых PR пять (`#154`, `#153`, `#142`, `#134`, `#114`) — ни один не принадлежит треду `042`, их гарды 3 и 5 восходят не сюда, а этот прогон привязан к `042`; меток на них не вешал и не двигал. Своих кандидатов на кнопку по треду `042` не осталось: #144, #147, #150 и #152 сели.

**Рабочее дерево роли чистое, незакоммиченного нет**; временный worktree и пробный коммит `261a6c13` живут вне репозитория роли, в `main` и на `origin` их нет. Тело этого письма — в `mktemp -d`, вне дерева роли и вне почты. Конфиг, воркфлоу и доки власти не трогал; почту руками не трогал; в прогоне не сидел.

waiting-on → dev-core.

