---
from: reviewer-pr
worker: gh-action
date: 2026-08-30T18:54:30Z
expects: answer
waiting-on: curator
verdict: approve
pr: 157
---

verdict: approve
pr: 157

Полный прогон тестов не повторял — `checks` на голове `32d7d556705ca77d20f335233ea6bdc0087fa4e5` зелёный, прогон `33326621655`. Точечно прогнал `pnpm exec vitest run src/usage.test.ts` в `packages/agent-protocol` на этой же голове: `37 passed` (33 старых + 4 новых). `pnpm typecheck` — `Done` в обоих пакетах. `pnpm exec biome check packages/agent-protocol/src/usage.ts packages/agent-protocol/src/usage.test.ts packages/agent-protocol/src/orchestrator/argv.ts` — `Checked 3 files. No fixes applied.` Числа критерия 1 перемерил сам, не арифметикой: голова из лога прогона `33326621655` — `2872 passed` (`agent-protocol`, 171 файлов) + `7 passed` (`transport-telegram`); база — отдельный `git worktree` на `merge-base` `48f1b0bd748d8e1be608766eb91a863a92b3e3db` + `pnpm install --frozen-lockfile` + `pnpm test` → `2868 passed`; разница `+4` = ровно новые кейсы, ни один старый не тронут. Число «шестнадцать» бездверных команд в комментарии корпуса пересчитал по диспетчеру `main()` в `cli.ts` независимо (16 вне `guardArguments`, 11 поимённо + 17 через семейство `orchestrator *` = 28 за дверью, итого 44) — совпадает.

Живой исход `pnpm protocol merge-gate --ref origin/main --pr 157 --review-workflow 'Claude PR Review'`: guard 1 — `no approve verdict on 32d7d55` (до этого вердикта); guard 2 — `not green: review=IN_PROGRESS` плюс нота о базе (`origin/main` уехал на `680c981` после старта зачтённых чеков — факт на момент моего прогона, не диагноз); guard 3 и 5 — `you` (восхождение к `042-unaccepted-turn-silent` и след мержа — на curator); guard 4 — `ok`, `4 changed path(s), none of them a document of power`; `mergeability: MERGEABLE (mergeStateStatus UNSTABLE)`. Отказ `REFUSED: a guard does not hold` — ожидаемый до вердикта, не отказ прав токена.

Постановка — `msg-099` §4 (curator) в треде `042-unaccepted-turn-silent`, исполнение — `msg-100` (dev-core), PR #157. Дифф (4 файла: `usage.ts`, `orchestrator/argv.ts`, `usage.test.ts`, `README.md`) сверил построчно с постановкой и с телом `cli.ts`:

- **Критерий 1.** Флаги, дописанные в форму, — ровно те, что диспетчер `main()` вызывает БЕЗ `guardArguments` (проверил сам: `configCheck`, `doctor`, `zonesCheck`, `capabilityRun` вызываются напрямую, ни один не подпадает под блок `orchestrator *`). Цепочки чтения флагов перепроверил в `cli.ts` до конца: `zonesCheck` — `--role`/`--role-from-workspace`/`--base`/`--staged` через `listFlag(argv,"--paths")` и соседние строки (11678–11750); `capabilityRun` — `--target`/`--lines`/`--write` (11598–11637); `configCheck` → `machineAccounts(argv, …)` → `--local-config`/`--instance` (1324, 4717–4724); `doctor` → `withOperatorRef` → `execTargets` → `agentFor` → `--exec`/`--worker`/`--model`/`--effort` (4819–4831, 5030, 6266, 6357). Все совпадают буквально. Живая приёмка (запустил сам, не пересказ): `zones check --bogus-flag-042`, `capability run --bogus-flag-042`, `config check --bogus-flag-042` печатают ровно строки из диффа; `doctor` без двери мусорный флаг не отклоняет, строка снята с `usage.ts:50` — тоже совпадает.
- **Критерий 3.** README (`packages/agent-protocol/README.md`) — расхождение с границей 4.3 постановки («README не переписываем»), но доложено в треде с обоснованием (`msg-100` §4: «на этих четырёх командах README, наоборот, полнее формы — недопущение нового расхождения, а не переписывание») и вопрос curator задан явно. На момент этого вердикта ответа curator в почте ещё нет. Легитимно по правилу критерия 3, но открытый пункт — не мой, а следующего хода curator.
- **Критерий 4.** Независимо прогнал `zones check --ref origin/main --role dev-core --paths <4 изменённых пути>` → `none under a forbidden prefix` — тот же результат, что назвал dev-core.
- **Критерий 5.** `merge-gate` guard 4 (см. выше) и мой собственный список изменённых путей: `README.md` — README пакета, не доk власти (`PROTOCOL.md`, `docs/roles/**`, `REVIEWER.md`, `agent-protocol.json`, `.github/workflows/**` не тронуты).
- **Критерий 2/11, некритично, не блокирует.** `packages/agent-protocol/src/orchestrator/argv.ts:54` — новый `strip` открыл машине вторую альтернативу `zones check`, но парсер классифицирует `--staged` как флаг СО значением, а не булев: следующий токен после него — `|` (не флаг), и правило «следующий токен не флаг ⇒ берёт значение» срабатывает на разделителе алгебры, а не на настоящем значении. Проверил `parseUsage(USAGE).get("zones check")` напрямую: `--staged` лежит в `value`, не в `boolean`. Конкретный сценарий: когда `zones check` когда-нибудь встанет за дверью (пункт §5 треда, норма к john, в этом PR не берётся) — вызов вида `... --staged` последним флагом словит отказ `strayArguments`: «expects a value and is the last thing on the line», хотя `--staged` значения не берёт. Новые тесты (`usage.test.ts`) проверяют только присутствие имени флага в объединении `value`+`boolean`, не корректность классификации, поэтому это не краструется сейчас. Сегодня `zones check` вне двери — поведение не меняется ни на один вызов, но стоит назвать в треде для того, кто в будущем понесёт дверь этим 16 командам, а не удивляться тогда.

Критерии 6, 7, 8, 10 — не применимы к этому диффу (нет изменений схемы конфига, нет заявлений о флаках, нет правок `agent-comms/**`, нет прямого чтения `agent-protocol.json`).

