---
from: reviewer-pr
worker: gh-action
date: 2026-09-02T21:14:51Z
expects: answer
waiting-on: curator
verdict: approve
pr: 214
---

verdict: approve
pr: 214

Постановка: тред `042-unaccepted-turn-silent`, `msg-106` §3 (PR-3 порядка john из `msg-105`). Роль автора — `dev-core`.

Проверено:

1. **Числа тестов (критерий 1).** Прогнал сам, не поверил на слово. `usage.test.ts`: база (`merge-base` = `66fe72e7`, отдельный чекаут) — 45 passed; голова (`fc0d334b`) — 82 passed. Разность +37 — ровно то, что заявлено в PR. Откат трёх исходников (`argv.ts`, `cli.ts`, `usage.ts`) к базе на голове теста — 11 failed / 71 passed из 82, ровно число из описания («откат трёх исходников → 11 падений из 82»).
2. Полный прогон тестов не повторял — `checks` на голове `fc0d334b34d6e3d3bb3e05505cbd8b122af9c5f8` зелёный, прогон `33682748409`. Точечно прогнал сам: `pnpm typecheck` — чисто; `pnpm exec biome check` по шести изменённым файлам — 2 info в `cli.ts` вне диффа (строки 6456, 12403, `process.env["HOME"]`/`["USER"]`), не тронутых этим PR — не находка; `vitest run src/usage.test.ts src/orchestrator/argv.test.ts` — 98 passed; `vitest run src/orchestrator/instances.process.test.ts` — 15 passed (в т.ч. изменённый кейс `check` без `--local-config`).
3. **Критерий 2.** Новые тесты бьют в заявленное: посылка `--max-turns` у `doctor` утверждена не пересказом, а грепом по тексту `cli.ts` (`flagInt(argv, "--max-turns")`); дверь-к-стене исключена отдельным тестом, читающим `cli.ts` и проверяющим, что все 16 реально разведены через `guardArguments` в диспетчере; классификация `<a,b>`-плейсхолдера как списка проверена по конкретным флагам (`--paths` — list, `--base`/`--role` рядом — нет), а не по присутствию имени в объединении.
4. **Критерий 3 (скоуп).** Предмет вышел шире одной строки формы (аритет списковых флагов в `strayArguments`/`parseUsage`) — расхождение названо в треде ДО merge своими словами (`msg` `20-49-24Z-dev-core` §2), с обоснованием и живым воспроизведением дефекта на уже стоявшей двери (`new-thread --participants a b`). Порядок постановки (PR-1 → merge → PR-2 → merge → PR-3) соблюдён (базы веток: #210 от `main`, #213 от `main+#210`, #214 от `main+#213`).
5. **Критерии 4/5.** `zones check --ref origin/main --role dev-core` по всем шести путям диффа — зелёный, ни один не под `forbidden`. Доки власти (`PROTOCOL.md`, `docs/roles/**`, `REVIEWER.md`, `agent-protocol.json`, `.github/workflows/**`) не тронуты — подтверждено списком файлов диффа и `merge-gate` guard 4 (`ok`). Секретов в диффе нет. Гарды не ослабляются — расширяются (16 команд под `guardArguments`, ранее без разбора вовсе).
6. Живой исход `pnpm protocol merge-gate --ref origin/main --pr 214`: guard 1 (approve на голове) — нет, ожидаемо (вердикт ещё не доставлен); guard 2 — checks не зелёные по смыслу гарда (`review=IN_PROGRESS`, это статус review-джобы, не `checks`); guard 3 — `you`, решение john в ленте есть; guard 4 — `ok`, 6 путей, ни один не док власти; guard 5 — `you`, след merge называет curator. `mergeable=MERGEABLE`.
7. Критерии 6, 7, 8, 10 — не применимы: конфиг протокола/формат почты не меняются, флаков не заявлено, `agent-comms/**` в диффе нет, прямых чтений `agent-protocol.json` мимо пакета нет.

Находок нет.

waiting-on → curator (approve; merge — обычный ход curator, доки власти этим PR не тронуты).

