---
from: curator
worker: claude-ai
date: 2026-09-02T18:52:28Z
expects: none
waiting-on: curator
delivers: john
priority: high
---

**Слово john полем `delivers` (чат 2026-09-02 ~18:52Z): «ДА, И СНАЧАЛА РАЗБОР ОПИСАНИЙ». Обе развилки §5 решены. Парковка msg `19-09-36Z` снята, тред размораживается.**

## 1. Что решено

**Вопрос 1 — дверные строки `init` и `orchestrator daemon`: ЧИНИМ.** Довод, принятый john: сегодня дверь **отказывает в том, что команда реально умеет** — строка формы отстала от кода, и это ложь в другую сторону. Правка не ослабляет проверку, а приводит объявленное к действительному. Класс — норма (расширяется принимаемый argv), поэтому и спрашивали; ответ получен.

**Вопрос 2 — дверь шестнадцати бездверным командам: ВВОДИМ, но ПОСЛЕ починки разбора описаний, и сразу всем шестнадцати.**

Порядок задан john дословно («сначала разбор описаний») и по причине, которую принёс замер ревьюера: `parseUsage` классифицирует `--staged` у `zones check` как флаг СО ЗНАЧЕНИЕМ, потому что следующий токен в строке — разделитель `|`, а правило «следующий токен не флаг ⇒ берёт значение» разделителя от значения не отличает. **Пока двери нет — цена нулевая; в день включения `zones check … --staged` последним флагом получит ЛОЖНЫЙ отказ.** Включить дверь раньше починки — значит своими руками завести ложный отказ в живой команде.

**Всем шестнадцати сразу, а не частями** — довод john: иначе получится «у половины команд опечатка ловится, у половины нет», и читателю придётся помнить, у какой как. Разнобой хуже единообразной строгости.

## 2. Порядок работ

1. **починить `parseUsage`**: разделитель альтернатив (`|`) не является значением флага; проверить, нет ли других мест, где токен-разделитель читается как значение. **Тест писать на классификацию** (`value` против `boolean`), а не на присутствие имени в объединении — нынешние тесты этот класс не ловят, и ревьюер это назвала;
2. **дописать строки формы** `init` и `orchestrator daemon` до того, что команды действительно читают (метод — тот же разбор `cli.ts`, что дал таблицу в `msg-099` §2);
3. **завести дверь шестнадцати** — одним пакетом, с отказом по имени: какой флаг не знаком и какие знакомы;
4. приёмка: опечатка во флаге у любой из шестнадцати → отказ по имени; законный флаг у `init`/`daemon`, сегодня отвергаемый, → проходит; `zones check --staged` последним флагом → проходит (регресс на п. 1).

## 3. Что john этим НЕ решал

Ни один отказ, существующий сегодня и корректный, не ослабляется. Речь только о двух классах: объявленное отстало от кода (п. 2) и объявленного нет вовсе (п. 3).

waiting-on → curator.
