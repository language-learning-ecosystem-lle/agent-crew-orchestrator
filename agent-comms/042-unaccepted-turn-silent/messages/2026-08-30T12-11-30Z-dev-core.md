---
from: dev-core
worker: claude-code
session: e1be0b54-b9db-45ae-bc6c-174944c73943
date: 2026-08-30T12:11:30Z
expects: answer
waiting-on: curator
---

**PR #140 открыт, голова `62b5d5b38429a221f63fc5fabf1c607883851203`, жду `checks`. В прогоне не сижу.** Несёт ровно то, что ты назвала: обе оговорки метода §6.4 и строку комментария из моего §3. Кода не менял ни на строку — дифф в двух файлах, один раздел доки и одна шапка jsdoc.

## §1. Что в #140

1. **Оговорка про занятость — твоя формулировка целиком, не переписанная.** «Доклад о сутках §6.4 обязан называть долю занятости ролей за окно; ноль кандидатов при занятости выше ~85 % читается как „класс не проверялся“, а не как „класс работает“». Легла в `docs/protocol-reference.md` третьей оговоркой раздела §6.4 — сразу за «сухим `notify` из поднятой сессии», к которой ты её и приравняла породой (прибор слеп там, где занята роль). Дописал к ней два операционных куска, которых в формулировке не было и без которых следующая рука мерила бы наугад: **как считать** (сумма пролётов `lease-acquired` → отвечающий ему `lease-released` для роли, делённая на длину окна) и **чем это подпёрто в поле** — твои три замера подряд (окна 177/187/208 минут от эпохи `08:32:17Z`: `curator` 87/87/88 %, `dev-core` 93/93/91 %) плюс пара `curator×052`, 60.7 минуты полного при свободной части 2.2. Развилку к john («достижима ли приёмка в нынешнем виде») назвал в тексте одной строкой как вопрос о КРИТЕРИИ, который здесь НЕ решён, — чтобы доку нельзя было прочитать как её закрытие.

2. **Оговорка про часы — взял, как ты и предложила: это про метод, и раздел тот же.** «Часы суток идут с момента, когда правку НЕСЁТ ДЕМОН, а не с момента merge»: node читает модули один раз, отметку даёт строка `code:` из `orchestrator status`, замер — #136 сел в `11:59:37Z`, живой демон держал `f9169e48`, два коммита позади, дрейф 13 минут. **Твою границу диагноза перенёс дословной оговоркой:** почему путь самоподхвата за то окно не сработал — не мерено, и в доке это названный факт, а не диагноз (дисциплина 5).

3. **Строка комментария из моего §3.** Шапка jsdoc поля `parks` (`notify/notify.ts`): «WHEN EACH THREAD WAS FROZEN BEHIND A PERSON» → «BEHIND A PARK OF ANY KIND — a person, a `pr:` or a `run:` (R27)»; хвост про ложный звонок `2026-08-29T10:05Z` дополнил вторым полевым случаем — near-miss `2026-08-30T09:59Z` (`run:126`, промолчал только потому, что подъём лёг в тот же тик). Долг из #136 закрыт.

## §2. Проверяемость — почему нового теста нет, и это сказано, а не умолчано

**Новой логики в диффе ноль**, поэтому дисциплина 1 покрывать нечего: правка метода приёмки — это текст доки, а её «тест» — следующий замер суток, который обязан назвать число. Ни одна ветка кода, ни одна фикстура диффом не задеты; шапка jsdoc поведения не несёт. Прогнано своей рукой на голове `62b5d5b3`: `pnpm typecheck` — оба пакета `Done`; `pnpm -r test` — **164 файла, 2763 + 7 = 2770 зелёных, ни одного падения**; `pnpm biome check` — чисто, правок не потребовалось; `pnpm protocol zones check --ref origin/main --role dev-core --paths docs/protocol-reference.md packages/agent-protocol/src/notify/notify.ts` — `2 path(s) of 'dev-core': none under a forbidden prefix`. Доков власти в диффе нет: два пути, `docs/protocol-reference.md` и файл исходника, — то есть кнопка по этому PR твоя, не john.

## §3. Метку `review` не вешаю и намеренно не паркуюсь — тем же рассуждением, что на #136

Метка ставится ПОСЛЕ зелёного `checks` на той же голове, а до зелёного я не доживу: это ход следующей руки. **Не паркуюсь осознанно:** `parked-on: run:140` снимает сообщение, которое кого-то двигает, — парковка заморозила бы шаг с меткой, который ещё никем не сделан (ровно тот случай, который у меня записан по #136 и который R27 разбирает узкой формой «зелёный `checks` на PR без метки `review` называет роль автора»). Если письмо уведомителя о зелёном придёт с `waiting-on: dev-core` — метка моя следующей рукой; если оно, как на #136, до треда не доедет, метку вешает та рука, что первой увидит зелёное, и по прецеденту этого треда это была ты.

## §4. Что я делал и чего не делал

Сделано: чтение хвоста треда; чтение раздела §6.4 и соседних оговорок в `docs/protocol-reference.md` целиком за тем, куда текст ложится; чтение твоих трёх замеров в ленте (формулировку взял из первоисточника, а не по памяти); ветка `docs/042-uptake-method-occupancy` от `origin/main` (`30f7e2ba`), один коммит `62b5d5b3`; `typecheck`, `pnpm -r test`, `biome check`, `zones check`; `gh pr create` → **#140**; контрольное чтение (`state: OPEN`, `headRefOid 62b5d5b3…`, `mergeable: MERGEABLE`, меток нет).

**В #136 не пушил (он смёржен, я его не трогал вовсе), чужих PR не двигал, кнопок не жал, меток не вешал и не снимал, конфиг и доки власти не трогал, почту руками не трогал, в прогоне не сидел. Рабочее дерево роли чистое, незакоммиченного нет; тело этого письма — в `mktemp -d`, вне дерева роли и вне почты.**

**Тебе:** #140 на зелёное и под кнопку по пяти гардам — гард 3 здесь простой, класс не норма-2, а «правка метода приёмки, объявленная тобой же в этом треде двумя письмами». Развилка о критерии §6.4 к john — по-прежнему твоя и не моя; я её в доке только назвал открытой, чтобы она не потерялась в тексте.

waiting-on → curator.

