---
from: curator
worker: claude-code
session: 892fcbbc-315a-46ae-b261-d661efbdd015
date: 2026-08-30T15:23:36Z
expects: answer
waiting-on: dev-core
merged-pr: 150
---

**#150 смёржен в `main` (`223aa3b64d7e8adade149135092efe22bf0cec3a`, 2026-08-30T15:18:24Z) — гарды поимённо ниже. Обе ноты, которые я прошлым ходом адресовал ревьюеру, закрыты ФАКТОМ, а не молчанием: ненулевой свободный хвост юнитом покрыт (две точки), а `usage.ts` действительно не называет `--now` — и это ставится dev-core отдельной постановкой в §3.**

## §1. Merge #150 — гарды поимённо (гард 5)

Дверь: `merge-gate --ref origin/main --pr 150 --review-workflow 'Claude PR Review'` (флаг имени воркфлоу — часть процедуры; без него гард 1 отвечает `by-hand`).

1. **Гард 1 — вердикт на текущей голове: ok.** `approve` на `98e5b4e` ВНУТРИ закрытого успешного круга `Claude PR Review` `33318586516` на этой голове, окно `2026-08-30T15:04:05Z…15:15:36Z`, событие `pull_request` — вердикт не сирота. Содержательно это письмо reviewer-pr в этом треде (`verdict: approve`, `pr: 150`), где названы: база пересчитана ЗАНОВО (`git merge-base origin/main origin/feat/042-day-report-both-shares` → `8a25f089`) и перемеряна отдельным `git worktree` + `pnpm test` — `2833 + 7 = 2840`, против головы ветки `2847`, разность ровно +7 = семь кейсов одного нового файла; расхождение лога `checks` (`2861`) объяснено НЕ арифметикой, а предками: #145 и #146 не являются предками ветки (`git merge-base --is-ancestor`), а `pull_request`-прогон мерит голову, слитую с `main` своего момента. Плюс точечный `vitest` 7/7 своей рукой, `zones check` по шести путям, и намеренная проверка критерия 11 (счётчик отброшенных релизов печатается ВСЕГДА, включая явный ноль; пара с хвостом позже правого края теперь молчит, а не печатает неотличимый `0.0m in all · free 0.0m`).
2. **Гард 2 — зелёные чеки на ТОЙ ЖЕ голове: ok.** Голова `98e5b4e86380d89cdbf67af253e651854829fec9` (полный SHA, не семь символов), два чека: `review=SUCCESS`, `checks=SUCCESS`.

   **Нота двери о базе — закрыта СВОИМ прогоном, и здесь она была не формальной.** Дверь: база уехала после старта зачтённых чеков — `f5b84d7` (#149, тред 053) закоммичен `15:13:27Z`, `checks` стартовали `14:58:59Z`. **Файлы здесь НЕ дизъюнктны:** #149 добавил 62 строки ровно в `docs/protocol-reference.md`, который правит и #150. Глазом этого не закрыть, поэтому мерил прогоном: фетч в ИМЕНОВАННЫЕ рефы (`refs/remotes/origin/main`, `refs/probe/pr150` — `FETCH_HEAD` тут не свидетель, его затирает сама дверь `merge-gate`), затем `git merge-tree --write-tree origin/main refs/probe/pr150` → дерево `71b2311d89a809ba1a6ae15186ed367ffa7540ca`, ни одной строки о конфликте. Врезки #149 и #150 стоят в разных местах файла.
3. **Гард 3 — восхождение: решение john ЕСТЬ в ленте.** Слово john 2026-08-30 ~13:10Z, пункт 3, доставленное полем `delivers` в этот тред и лежащее дословно в `docs/protocol-reference.md` §6.4 в `main` с `86e055c6`: доля свободного времени обязана печататься в докладе РЯДОМ с долей занятости. Постановка — моё письмо этого треда `2026-08-30T14-25-40Z-curator.md`, §2; ревьюер сверила дифф против всех шести пунктов 2.3 и границ 2.4 и расширений не нашла. Строки `thread: 042-unaccepted-turn-silent` и `role: dev-core` в описании PR стоят простыми строками (прочитал сам, обратных кавычек нет — они слепят гард 3). **Класс «полевой измеренный дефект» здесь НЕ применялся и не нужен: решение john прямое.**
4. **Гард 4 — доки власти: ok.** `6 changed path(s), none of them a document of power`. Список дверь вывела ДАННЫМИ и напечатала источник каждого пути — 8 штук: `agent-protocol.json`, четыре карточки ролей (`curator`, `dev-core`, `pilot-codex`, `devops` — «derived from a role's instructions»), `REVIEWER.md`, плюс объявленные `powerDocuments` — `PROTOCOL.md` и `.github/workflows`. Флагом `--power-docs` ничего не добавлялось.
5. **Гард 5 — этот след.** `mergeability: MERGEABLE (mergeStateStatus CLEAN)`. Merge squash, заголовок Conventional Commits.

**Контрольное чтение после кнопки (ответ инструмента ≠ факт):** `base.ref = main`, `state = MERGED`, `mergedAt 2026-08-30T15:18:24Z`, merge-коммит `223aa3b6…`; в нём ровно шесть файлов (`docs/protocol-reference.md` +30, `packages/agent-protocol/README.md` +28, `cli.ts` +56/−3, `notify/notify.ts` +8/−3, новые `orchestrator/occupancy.ts` +267 и `occupancy.test.ts` +211). В `origin/main` §6.4 несёт абзац, ИМЕНУЮЩИЙ команду и функцию (`leaseSpans` + `foldDay`, `orchestrator/occupancy.ts`, строка 1056; `freeTailMinutes` из `notify/notify.ts`, «вызываемая, а не переписанная», строка 1060), а README, раздел «Commands», несёт `metrics` с `--since`/`--now` и описанием обеих долей. Ветка удалена (`git ls-remote origin refs/heads/feat/042-day-report-both-shares` → ноль ссылок); `gh` при кнопке напечатал `could not determine current branch` — это провал удаления ветки из detached-воркдерева, а не провал merge.

## §2. Мои две ноты прошлого хода — закрыты замером

**Нота §3 (ненулевая ветка свободного хвоста): вопрос снят, юнитом покрыта, и не в одной точке, а в двух.** Прочитал сам `occupancy.test.ts` на голове PR, а не поверил ответу:

- `it("prints 60.7 minutes of standing under 2.2 minutes of free tail (curator×052)")` — `expect(standingMinutes).toBeCloseTo(60.7, 1)` и `expect(freeMinutes).toBeCloseTo(2.2, 1)`;
- в кейсе про парковку **ненулевая ветка проверяется ДО нулевой**: `withoutPark` даёт `freeMinutes` `12.9` (`curator×051`), и только `withPark` — явный ноль со строкой `standing curator×051-merge-ready  30.4m in all  free 0.0m`.

То есть «пара стоит, роль свободна, парка нет ⇒ хвост ненулевой» юнитом закрыто. **Остаётся ровно та граница, которую я назвал вслух и которая merge не меняет:** ЖИВЫМ прогоном эта ветка сегодня не предъявлена — в ленте нет ни одной незапаркованной стоящей пары. Это не долг и не находка, это то, чего живая приёмка физически не могла покрыть; появится такая пара — прибор её и покажет.

**Нота §4 (`usage.ts` не называет `--now`): подтверждена замером на `main` ПОСЛЕ merge, и решение по ней — моё, потому что ревьюер её не тронула.** Три факта, все прочитаны, ни один не вспомнен:

1. флаг **принимается**: `metrics` зовёт `orchestratorNow(argv)` (`cli.ts`, `now: orchestratorNow(argv)`), а это тот же `flag(argv, "--now")`, что у `status`/`record`/`run`/`hold`; я пользовался им во всех четырёх прогонах приёмки;
2. флаг **описан** в README, «Commands», отдельной строкой с причиной («pins the right edge, so a window measured by hand hours ago can be re-measured»);
3. в строке формы, которую печатает сама дверь, его **нет**: `agent-protocol metrics --ref <ref> [--root <mail>] … [--since <ts>] [--role <id>] [--thread <id>] [--json]`.

**И пробел изолирован — это тоже замер, а не впечатление:** у `orchestratorNow` пять вызывающих (`metrics`, `operatorFrame` за `status`/`tui`, `record`, `run`, `hold`), и ЧЕТЫРЕ из пяти свой `--now` в строке формы печатают. Не называет его один `metrics`.

Мой вердикт по своей же ноте: **это дефект формы, а не допустимая асимметрия**, и порода у него ровно этого треда — ящик говорит человеку о себе меньше, чем правда, причём говорит именно тому, кто уже ошибся в вызове и читает строку как справку. Merge #150 я этим не останавливал и не должен был: дифф сдавался против шести пунктов постановки, строки формы среди них не было.

---

# §3. ПОСТАНОВКА (dev-core): строка формы `metrics` называет `--now`

## 3.1 Восхождение — класс «полевой измеренный дефект, новой нормы не вводит»

Отдельного слова john тут нет и не будет: постановку сломанной строки писать больше некому, кроме curator. **Класс объявляю ЗДЕСЬ, до merge, и три его условия называю поимённо:**

1. **первоисточник замера** — §2 этого письма: лента треда 042, 2026-08-30, замер на `main` после `223aa3b6` (принимаемый флаг прочитан в коде, описанный — в README, печатаемая строка — в `usage.ts`, изоляция пробела — по пяти вызывающим `orchestratorNow`). Это не гипотеза причины: флагом я пользовался живыми прогонами, он работает;
2. **исполнитель подтверждает в треде, что дифф новой нормы не вводит** — своими словами о своём диффе;
3. **ревьюер подтверждает то же самое** — чтением диффа.

Условия 2 и 3 — на вас двоих; без любого из них гард 3 возвращается к слову john, и кнопки не будет.

**Проверка на норму, которую я делаю вслух:** напечатать уже принимаемый и уже описанный флаг — это возврат поведения к объявленному, а не новое поведение. Ни нового поля, ни ключа конфига, ни формы сообщения, ни права, ни шага маршрута, ни снятия запрета. **Если по ходу работы окажется, что дешевле поправить не строку, а что-то ещё — это уже не этот класс:** останавливайся и говори в тред, а не расширяй дифф.

## 3.2 Что требуется

1. **Строка формы `metrics` в `usage.ts` называет `--now <iso>`** — на том же месте, где он стоит в README (после `--since`).
2. **Юнит, который БЕЗ правки красный.** Предмет — печатаемая строка формы, а не факт существования флага. Проверить его на падение своей рукой до правки (как ты делал для #102) и сказать в треде, что проверил: тест, не проверенный красным, меряет сам себя.
3. **ЗАМЕРИТЬ и НАЗВАТЬ, но НЕ чинить:** есть ли другие строки формы, не называющие флага, который команда реально читает. Мой замер покрывает ровно один флаг (`--now`) по пяти вызывающим — про остальные я не знаю ничего. Список — в тред. **Если он длиннее одной строки, в этот PR его НЕ берём:** размер подметания неизвестен, пока не измерен, а починка «заодно» ломает класс 3.1, на котором держится гард 3.
4. **Назвать, тоже без починки:** у `metrics` нет двери `guardArguments` (прочитано в диспетчере `cli.ts`: `metrics(argv.slice(1))` без гарда, в отличие от `new-message`/`mail`/`wake`), то есть опечатка во флаге проглатывается молча. Нужна ли дверь читающей команде — отдельный вопрос, и добавление двери меняет поведение за пределами починки, то есть **норма и идёт к john**. От тебя — только строка в треде, что это названо.

## 3.3 Границы — чего НЕ берём

- не трогаем арифметику долей, доклад о сутках и `occupancy.ts` — #150 сел, предмет закрыт;
- не трогаем порог (он у john) и строку курьера;
- не переписываем README — он полон, расходится с ним `usage.ts`;
- не чиним «два имени одного холда»: по замеру dev-core обе фразы верны и адресованы разным читателям, значит любая правка там — новая форма сообщения, то есть норма, к john.

## 3.4 Проверяемость

- **Юнит:** печатаемая строка формы для `metrics` называет `--now`; красный без правки, проверено рукой (п. 2 выше). Это весь класс проверок, который тут нужен;
- **живая приёмка рукой:** вызвать `metrics` так, чтобы дверь напечатала форму, и прочитать строку глазами — один прогон, в докладе назвать буквально напечатанное;
- **сознательно НЕ покрывается:** общее свойство «каждая строка формы называет каждый флаг, который команда читает» — теста на него нет и в этом диффе не заводим; вместо него — замер и список по п. 3 постановки.

## 3.5 Форма

Ветка от СВЕЖЕГО `origin/main` → PR с `thread: 042-unaccepted-turn-silent` и `role: dev-core` простыми строками в описании (обратные кавычки слепят гард 3) → зелёный `checks` → метка `review` ПОСЛЕ зелёного и на той же голове → вердикт → кнопка curator по пяти гардам. Доков власти в предмете нет. **База сегодня уезжала четыре раза** (#144 `86e055c6`, #147 `48cf0bfa`, #149 `f5b84d70`, #150 `223aa3b6`) — ветку поднимай от свежей головы, иначе `merge-tree` придётся мерить лишний раз.

## §4. Открытым по треду остаётся

- **порог доли свободного времени** — у john. Вопросом сегодня его не поднимаю намеренно: john оставил порог открытым СВОИМ решением, и это лежит в `main` (§6.4, «Чего этим решением НЕ решено») — то есть предмет назван, а не заморожен молча. Прибор напечатал первые числа час назад; спрашивать порог, пока против него нечего мерить, — это шум, против которого дано слово 13:33Z. Вопрос созреет, когда чисел наберётся за несколько суток;
- **«вторая половина класса»** (курьер не знает причину «роль занята, пара первая в очереди») — новая форма сообщения, то есть норма, к john отдельным вопросом;
- **два имени одного холда** — названо dev-core, не починено; см. 3.3 — правка там норма;
- самоподхват кода в окне дрейфа 30.08 — тред `044`.

## §5. Состояние моей руки

Сделано: чтение хвоста треда; `merge-gate` по #150; фетч в именованные рефы и свой `merge-tree` против свежего `origin/main` (#149 трогает тот же файл — потому и мерил); кнопка squash по #150; контрольное чтение цели, состава merge-коммита, содержимого §6.4 и README в `main`, удаления ветки; чтение `occupancy.test.ts` на голове PR по ноте §3; чтение `usage.ts`, README и всех пяти вызывающих `orchestratorNow` в `cli.ts` на `main` по ноте §4.

Чужих PR не двигал и меток не вешал: открыто шесть (`#151`, `#148`, `#143`, `#142`, `#134`, `#114`), все принадлежат другим тредам — их гарды 3 и 5 восходят не сюда, а этот прогон привязан к `042`. Своих кандидатов на кнопку по треду `042` не осталось: #144, #147 и #150 сели. Конфиг, воркфлоу и доки власти не трогал; почту руками не трогал; в прогоне не сидел. **Рабочее дерево роли чистое, незакоммиченного нет**; тело этого письма — в `mktemp -d`, вне дерева роли и вне почты.

waiting-on → dev-core.

