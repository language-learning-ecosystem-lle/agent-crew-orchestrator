---
from: curator
worker: claude-code
session: d9ab62c1-7429-4f3a-a417-28d56658a284
date: 2026-08-29T12:41:28Z
expects: none
waiting-on: curator
parked-on: run:108
---

**Норма по варианту 3 написана и открыта PR #108 — `docs/042-verdict-header-field`, голова `d2c0285b47367946884006dc61be8639adaea6b7`; три развилки, которые john оставил мне, закрыты В ТЕКСТЕ нормы, а не умолчанием.** Дифф — один вложенный блок в `PROTOCOL.md`, кода не тронуто ни строки. Метка `review` НЕ повешена: круг `checks` на голове ещё идёт, метка вешается после зелёного на ТОЙ ЖЕ голове. Паркуюсь за прогоном и передаю ход — у конвейера не стою.

Отдельно и первым делом, потому что это меняет состояние двух пунктов, которые предыдущие руки оставили открытыми: **часы §6.4 ПОШЛИ, и живая приёмка #107 сняла первый показатель — класс молчит.**

## §1. Что открыто PR #108 и на каких решениях он стоит

- ветка `docs/042-verdict-header-field`, голова **`d2c0285b47367946884006dc61be8639adaea6b7`**, база `main`, состояние `OPEN`, меток нет (проверено `gh pr view 108` после ответа `gh pr create` — ответ инструмента ≠ факт);
- описание несёт первыми двумя строками `thread: 042-unaccepted-turn-silent` и `role: curator` — машинно проверяемая половина гарда 3 закрыта;
- дифф — один файл `PROTOCOL.md`, абзацный блок ВНУТРИ нормы 29.08 («ПАРКОВКА НА ЧЕЛОВЕКЕ ОБЪЯВЛЯЕТСЯ НА ХОД»), потому что он закрывает её же пункт (ii), а не заводит отдельную норму. Форма та же, что у #99: текст нормы, вложенный в текст нормы.

Восхождение: слово john «ВАРИАНТ 3», доставленное чатом письмом `2026-08-29T12-26-53Z-curator.md` этого треда. Мотив отвергнутых вариантов перенесён в норму дословно из этого письма — вариант 2 покупает постоянный второй источник истины (замер dev-core `2026-08-29T11-51-33Z-dev-core.md`, второй `kind` `codex` в контуре уже существует), вариант 1 оставляет обещание протокола шире реальности.

## §2. Три развилки john отдал мне — вот они, поимённо

1. **Имена и словарь полей.** `verdict: approve|needs-fixes` — РОВНО словарь `REVIEWER.md`, не новый; `pr: <номер>` — номер PR обслуживаемого проекта. Это те же две строки, что сегодня едут телом, поднятые в шапку, а не новый жанр письма: ревьюер продолжает писать доклад как писал.
2. **Обязательность.** Поля объявляются **парой** — заголовок с одним из них дверь письма отвергает (вердикт без адреса — не исход, а реплика). На прочих письмах поля не объявляются. Права на них нет ни у кого специального, и **роль отправителя против конфига НЕ сверяется** — это ровно отвергнутый вариант 2; источником истины остаётся объявление, как у `delivers`.
3. **Поведение при отсутствии поля у старых сообщений.** Не дописываются ни рукой, ни миграцией (почта append-only), «дочитать» вердикт из тела запрещено нормой 020. Отсутствие читается как «вердикт не объявлен» — нового хода такое письмо не открывает, ровно как сегодня. **Цена названа в самой норме:** окно замера LLE (19 минут) остаётся открытым, пока не сядут ОБА PR — этот и код по нему; цена конечна, в отличие от постоянного второго источника истины.

Что норма НЕ трогает, и это сказано в ней явно: лифт парковки на человеке (`delivers`, `status: closed`), лифт событийных `pr:N`/`run:N`, разметку `waiting-on`/`expects` на письме вердикта и текст `REVIEWER.md` — ни на строку.

## §3. Часы §6.4 ПОШЛИ — замерено, а не выведено

`.orchestrator/daemon-code.json` на 12:39Z: `sha=b27780cc3fa0d26febd511ea9e53c0677aabeaea`, `startedAt=2026-08-29T12:11:22Z`, `pid=1415843`. Формулу из письма `12-01-47Z` прогнала командой, а не глазом: `git merge-base --is-ancestor b27780cc… b27780cc…` → **истина**. Предыдущий замер (11:20:41Z, `pid=587078`, `sha=ec1b155d`) — тот же демон до самолечения.

**Старт суток §6.4 = 2026-08-29T12:11:22Z** — момент, когда демон встал на код с #107. Самолечение отработало третий раз за сутки без руки (10:24:58Z → `8acff3f`, 11:20:41Z → `ec1b155d`, 12:11:22Z → `b27780cc`), задержка от merge (11:59:10Z) до подхвата — 12 минут.

## §4. Живая приёмка #107 — первая курьерская строка после лифта парковки

Парковка на john снята письмом `12-26-53Z-curator.md` (`delivers: john`), пара `curator×042` поднята этой сессией. Последняя курьерская строка `daemon.log` на 12:41Z:

```
courier: 5 parked, 3 of them asking, 0 of those new, 2 lifted; 0 waits, 0 of them new; 0 stalled over 180m, 0 of them new — nothing to announce
```

**Восьмой класс молчит.** До #107 та же пара, выйдя из-под парковки, позвонила бы возрастом ВСЕЙ заморозки с `no reason known` (третья ложная причина, `daemon.log:19561`). Сейчас — ни строки про непринятый ход.

Разделяю замеренное и выведенное, как требую от исполнителей: **это СЛАБАЯ половина приёмки.** Норма #107 обещала одно из двух — тишину, если пару поднимут раньше порога, либо звонок возрастом СВОБОДНОЙ части. Наблюдалась первая ветка: лифт и подъём уложились в порог, поэтому тишина здесь ЗАКОННА и предсказана, но она НЕ доказывает арифметику вычета на живых данных. Вторая ветка (звонок свободной частью) в поле ещё не наблюдалась — это остаётся несделанным, и следующей руке я его называю ниже. Побочно: `5 parked, 3 of them asking` против `10 parked, 8 of them asking` в 12:01Z и `11 parked, 9` в 12:06Z — завал парковок (Д-4) за сорок минут упал вдвое; причину не называю, потому что не мерила.

## §5. Метка на #108 не повешена — и это норма, а не забывчивость

`gh pr checks 108` на 12:39Z: `checks pending`, прогон **33253034239**. Метка `review` вешается ПОСЛЕ зелёного `checks` на той же голове — ревьюер снимает срез дубль-тестов с готового прогона. Круг я не жду: зелёный `checks` на PR без метки `review` называет роль автора и поднимает её сам — это тот самый узкий список действенных исходов. Паркуюсь `run:108` и передаю ход.

**Дверь merge по #108 — не моя.** `PROTOCOL.md` — док власти (гард 4), `merge-gate` ответит `STOP`, и это правильный ответ: роль не принимает документы, определяющие её полномочия. После вердикта ревьюера PR ждёт кнопки john — это надо назвать ему явно, когда вердикт приедет.

## §6. Хвост открытых PR — список снят, чужого не трогаю

`gh pr list --state open` на 12:37Z → **#105** (тред 036, метка `review`), **#103** (тред 043, метки нет), **#100** и **#96** (тред 026, метки `review`); #106 и #107 со вчерашнего счёта уже смёржены. Ни одного моего кандидата: гард 5 требует след merge в ТРЕДЕ этого PR, а прогон привязан к 042. Мой единственный PR — #108, и его дверь за john.

## §7. Находка dev-core (четвёртая ложная причина) — дома не получила, и я называю почему

Разбор `12-06-45Z-dev-core.md` принят целиком: замер живой (903 тика скипа `parked behind the merge of PR #96`), форма фикса и её единственная ловушка (`mergedElsewhere` при переигрывании событийных пролётов) описаны — переоткрывать не нужно. **Дома дать не могу: прогон привязан к треду 042, новых тредов эта рука не заводит.** Срок годности находки, названный dev-core, подтверждаю: он равен времени до кнопки на #96 — после merge четвёртый ложный звонок уйдёт про пару `curator×038`.

## §8. Состояние моей руки

Написан текст нормы в `PROTOCOL.md`, ветка запушена, PR #108 открыт. Рабочее дерево роли чистое, незакоммиченного нет; `agent-comms` руками не трогала; тело этого письма лежит в `/tmp`, вне дерева и вне почты.

**Что подхватывает следующая рука** (в этом порядке):

1. **зелёный `checks` на #108** → повесить метку `review` на ту же голову `d2c0285b…` → передать ход, за кругом не стоять;
2. **вердикт** → PR ждёт КНОПКИ JOHN (док власти, гард 4), сказать это john явно вместе с тем, что норма без кода — половина;
3. **после посадки нормы — постановка dev-core на код**: дверь `new-message` принимает пару полей, `standingParkOf` читает её как новый ход при том же держателе. Приёмка в фактах — вердикт, легший в тред, запаркованный на человеке при том же держателе, поднимает держателя без ручного толчка;
4. **дом для находки dev-core** (§7) — новый тред, с разделом «Проверяемость»; срок годности — до кнопки на #96;
5. **вторая ветка приёмки #107** (§4) — звонок возрастом СВОБОДНОЙ части: наблюдать на паре, которую после лифта НЕ подняли раньше порога;
6. **сутки §6.4 идут с 12:11:22Z** (§3) — считать от этого момента, а не от merge.

waiting-on → curator, parked-on → run:108.

