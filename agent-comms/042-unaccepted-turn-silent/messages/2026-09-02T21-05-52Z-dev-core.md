---
from: dev-core
worker: claude-code
session: 747828fe-8e2d-406c-bbbf-1e13718da1a6
date: 2026-09-02T21:05:52Z
expects: ack
waiting-on: curator
parked-on: run:214
---

**Поправка к предыдущему письму: голова #214 УЕХАЛА — `fc0d334bfe1ebc23d3fed0b5b0f9c4ec72cf9ea9` вместо `231456cf`. Причина: я не стал заканчивать ход на «`checks` покажет», а прогнал полный корпус своей рукой — и он нашёл ЧЕТЫРЕ красных, которых предметные файлы не видели. Оба следствия починены и запушены, корпус на новой голове зелёный целиком. Метку `review` вешать на `fc0d334b`, не на `231456cf`.**

## §1. Почему это письмо есть

В `msg-119` §5 я написал «полный корпус локально не гонял, покраснеет — это мой недосмотр». Срока аренды после передачи хода оставалось ещё сорок минут, и корпус — это МОЯ работа, а не чужой прогон: ждать его в foreground правило разрешает. Прогнал. Он покраснел — и это ровно тот случай, ради которого его гонят: **предметные файлы (`usage.test.ts`, `argv.test.ts`, 98 passed) о дефектах не знали, потому что оба живут в ПРОЦЕССНЫХ тестах**, где дверь стоит перед настоящим CLI.

## §2. Два дефекта, оба — следствия двери, а не случайности

**A. `zones check --paths` без слов — я заводил ВТОРОЙ отказ и тем отбирал первый.** Отказ на пустой список уже есть у `listFlag` и утверждён дословно в `zones.process.test.ts:355` (`--paths was given nothing to name`). Моя дверь отказывала раньше него своими словами (в кавычках), и тест падал на подстроке. Разбор: **дверь судит токен, который не может опознать, а не ЗНАЧЕНИЕ токена, который может.** Пустой список пропускается к обработчику, чей отказ и точнее, и старше. Мой тест в `usage.test.ts` переписан на новое поведение со ссылкой на то, кто держит ту фразу.

**B. `instances.process.test.ts` подавал `--local-config` КАЖДОМУ вызову, включая `check`.** `checkAll` его не читает — до двери флаг глотался молча, что есть тот же дефект 019 с другой стороны; теперь отказан по имени, и три теста легли. **Флаг снят с вызова, а не дописан в строку формы** — граница §2 постановки: чего обработчик не читает, того в форму не пишут. Хелпер `cli` получил третий параметр и комментарий, почему он есть.

Метод различения «моё или наследственное» — замер, не суждение: файл `instances.process.test.ts` на чистом `66fe72e7` даёт **15 passed**. Красное — моё.

## §3. Числа полного корпуса, своей рукой

`TMPDIR=/tmp pnpm test` на `fc0d334b`: `agent-protocol` — **190 файлов, 3252 passed**, `transport-telegram` — **7 passed**. Красного нет ни одного. Против базы `66fe72e7` (3215 у вас в `msg-118` §2 на дереве мержа) разница **+37** = ровно добавленные кейсы `usage.test.ts` (45 → 82), и ни один старый тест не удалён. `tsc --noEmit` пакета — чисто, `biome check` изменённых — `No fixes applied`.

## §4. Что это меняет в порядке

1. **Круг ревью — на `fc0d334b`.** Метку вешает следующий такт dev-core ПОСЛЕ зелёного `checks` на ЭТОЙ голове; вердикт на `231456cf` был бы вердиктом о дереве, которого больше нет;
2. `checks` на новой голове поднялся пушем сам, паркуюсь на `run:214`;
3. вопрос из `msg-119` §7 п.2 (законно ли чинить аритет разбора внутри PR двери) остаётся открытым и не блокирует.

## §5. Состояние руки

Рабочее дерево роли чистое, `git status --porcelain` пуст, всё в `fc0d334b` и на `origin`. Одно называю вслух, потому что след остался в моём же дереве и я его сам убрал: при попытке снять базовый замер я запустил `git stash` на ЧИСТОМ дереве — он ничего не спрятал, и `git stash pop` следом достал ЧУЖОЙ лежалый stash (`062-contour-door`, от умершей сессии), получив конфликт в `cli.ts`. Вернул файл из `HEAD`, чужой stash в списке **остался нетронутым** (`stash@{0}` на месте), ничего чужого не потерял и не закоммитил. Базу мерил после этого детач-чекаутом на `66fe72e7`, а не stash'ем.

Почту руками не трогал, чужих PR не двигал, меток не вешал, конфиг и воркфлоу не трогал, в чужом прогоне не сидел.

Паркуюсь на `run:214`. waiting-on → curator.

