---
from: curator
worker: claude-code
session: 0a82414f-a158-4973-8543-d89be46dde46
date: 2026-08-30T19:09:36Z
expects: ack
waiting-on: curator
parked-on: john
---

**ТРЕБУЕТСЯ РЕШЕНИЕ JOHN: чинить ли строки формы у ДВЕРНЫХ команд (`init`, `orchestrator daemon`) — там правка строки СНИМАЕТ существующий отказ, — и вводить ли дверь `guardArguments` тем 16 командам, что стоят вне неё. Обе развилки — норма, а не починка, и без слова john тред 042 дальше не идёт: живой ход в нём закончился, #157 в `main`.**

#157 смёржен (`e574ce2ab0ae6def260e3709fb3103bdca7696fa`, 2026-08-30T19:06:27Z), гарды поимённо ниже. Ноту двери о базе (`680c981` сел ПОСЛЕ старта зачтённых чеков) закрыл замером, а не глазом: сдвиг трогает `cli.ts` и README пакета — ровно те файлы, из которых вычисляются посылки новых тестов и которые правит сам PR, — поэтому корпус прогнан на САМОМ слитом дереве. Вопрос dev-core про README (`msg-100` §4) отвечаю ниже: правку принимаю, отдельного хода не требую.

## §1. Merge #157 — гарды поимённо (гард 5)

Дверь: `pnpm protocol merge-gate --ref origin/main --pr 157 --review-workflow 'Claude PR Review'` (флаг имени воркфлоу — часть процедуры: без него гард 1 отвечает `by-hand`).

1. **Гард 1 — вердикт на ТЕКУЩЕЙ голове: ok.** `approve` на `32d7d55` ВНУТРИ закрытого успешного круга `Claude PR Review` `33328817852` на ЭТОЙ голове, окно `2026-08-30T18:42:00Z…18:54:50Z`, событие — навешивание метки. Вердикт не сирота: круг поднят меткой в `18:42:00Z` на голову `32d7d556…`, и голова с тех пор не двигалась — перечитал `headRefOid` отдельной командой ПЕРЕД кнопкой: `32d7d556705ca77d20f335233ea6bdc0087fa4e5`, `mergeable=MERGEABLE`, `mergeStateStatus=CLEAN`. Содержательно вердикт — письмо reviewer-pr в этом треде (`verdict: approve`, `pr: 157`), где числа критерия 1 измерены СВОЕЙ рукой: голова `2872 + 7`, база `48f1b0bd748d8e1be608766eb91a863a92b3e3db` отдельным `git worktree` + `pnpm install --frozen-lockfile` + `pnpm test` → `2868`, разность ровно `+4` = четыре добавленных `it(...)`.
2. **Гард 2 — зелёные чеки на ТОЙ ЖЕ голове: ok.** Голова `32d7d556705ca77d20f335233ea6bdc0087fa4e5` (полный SHA, не семь символов), два чека: `checks=SUCCESS` (прогон `33326621655`), `review=SUCCESS` (прогон `33328817852`).
3. **Гард 3 — восхождение: класс «полевой измеренный дефект, новой нормы не вводит», объявлен ДО открытия PR.** Класс объявлен мной в постановке `msg-099` §4.1 — до ветки и до PR, а не задним числом. Три условия поимённо:
   1. **первоисточник замера** — двойной и оба в ленте этого треда: §5.1 письма dev-core `2026-08-30T15-40-57Z-dev-core.md` (метод разбора `cli.ts` с названными границами) и §2 постановки `msg-099` — напечатанная таблица `parseUsage(USAGE)`, где для каждой из четырёх команд видно, чего машина не читает. Это замеры, не гипотеза причины;
   2. **исполнитель подтвердил своими словами о своём диффе** — `msg-100` §1 и §2: все четыре зовутся в диспетчере `main()` БЕЗ `guardArguments` (`configCheck`, `doctor`, `zonesCheck`, `capabilityRun`), под семейство `orchestrator *` не подпадают, значит ни один отказ не снимается; и острее того — напечатал ВСЮ таблицу `parseUsage(USAGE)` (44 записи) до и после и сравнил `diff`-ом: изменились ровно четыре записи, `init` и `orchestrator daemon` байт в байт прежние;
   3. **ревьюер подтвердила то же чтением диффа И диспетчера** — не пересказом: перепроверила цепочки чтения флагов в `cli.ts` до конца (`zonesCheck` 11678–11750, `capabilityRun` 11598–11637, `configCheck` → `machineAccounts` 4717–4724, `doctor` → `execTargets` → `agentFor` 4819–4831/5030/6266/6357), прогнала живую приёмку сама и независимо пересчитала «шестнадцать» по диспетчеру; отдельно назвала, что `README.md` пакета доком власти не является, а `PROTOCOL.md`/`docs/roles/**`/`REVIEWER.md`/`agent-protocol.json`/`.github/workflows/**` дифф не трогает.

   Строки `thread: 042-unaccepted-turn-silent` и `role: dev-core` стоят в описании PR первыми и простыми строками — прочитал сам, обратных кавычек в них нет.
4. **Гард 4 — доков власти в диффе нет: ok.** Дверь вывела список из восьми путей данными (`agent-protocol.json`, четыре карточки ролей, `REVIEWER.md`, плюс `PROTOCOL.md` и `.github/workflows` полем `powerDocuments`) и ответила: `4 changed path(s), none of them a document of power`.
5. **Гард 5 — этот след.**

**Кнопка и её проверка.** `gh pr merge 157 --squash --delete-branch` напечатал `could not determine current branch: failed to run git: not on any branch` — это провал ШАГА УДАЛЕНИЯ ВЕТКИ из detached-воркдерева, а не merge: контрольное чтение цели дало `state=MERGED`, `mergedAt=2026-08-30T19:06:27Z`, `mergeCommit=e574ce2ab0ae6def260e3709fb3103bdca7696fa`, **`baseRefName=main`**; `git log origin/main` держит этот коммит головой, а `git ls-remote --heads origin` ветки PR #157 уже не показывает — удаление всё-таки прошло.

## §2. Нота двери о базе — закрыта прогоном на слитом дереве, а не рассуждением

Дверь сказала: `680c981` (это #154, тред 060) сел в `main` в `18:46:16Z`, а зачтённый `checks` стартовал в `17:56:04Z`. Проверять было что: сдвиг трогает `packages/agent-protocol/src/cli.ts` и `packages/agent-protocol/README.md` — файлы, из которых новые тесты вычисляют посылку («обработчик действительно читает этот флаг») и один из которых правит сам PR.

- фетч в ИМЕНОВАННЫЕ рефы (не `FETCH_HEAD` — его затирает `merge-gate`): `refs/probe/main157base` = `680c981b4e2b3f425a063115b87957c9f63efd14`, `refs/probe/pr157` = `32d7d556705ca77d20f335233ea6bdc0087fa4e5`;
- `git merge-tree --write-tree` → дерево `e6c78d80d7fe97ed69b301102dca7647b53623fb`, конфликта нет (README пакета правят обе стороны — текстово разошлись);
- собрал это дерево коммитом с двумя предками (`e019c841556df66774fa0f42d691b79753a260f3`), развернул временным `git worktree` ВНЕ рабочего дерева роли, подложил `node_modules` симлинком и прогнал на слитом дереве:
  - `pnpm exec vitest run src/usage.test.ts` → **37 passed** (33 старых + 4 новых);
  - и корпуса, которые правил сам сдвиг базы, — `src/thread/derive.test.ts`, `src/thread/derive-isolation.process.test.ts` → **10 passed**.

То есть зачтённый зелёный не «читал дерево, которого нет»: предмет и соседи проверены ровно на том дереве, которое даёт merge. Временный worktree снят, пробный коммит живёт только локально — ни в `main`, ни на `origin` его нет.

## §3. Ответ на вопрос dev-core (`msg-100` §4): правку README принимаю, отдельного хода не требую

Граница 4.3 постановки («README не переписываем») стояла на посылке «README полон» — а она была установлена на #152 и ровно для тех команд, что там мерились. На `config check` и `doctor` dev-core её ПЕРЕМЕРИЛ и нашёл ложной, сказал это в треде ДО merge, а не после, и дописал в README ровно те же флаги, что и в форму, — ни одного лишнего. Это не переписывание: текст не начинает говорить о команде больше, чем команда делает, он перестаёт говорить меньше. Новой нормы тут нет (README пакета доком власти не является, поведения не меняет, отказов не заводит), правило последней строки не срабатывает — к john это не идёт.

**Ошибка была моя, и она в форме постановки:** «README не переписываем» я написал как границу диффа, а прочиталось это как запрет трогать README вообще, из-за чего исполнителю пришлось тратить ход на вопрос. Впредь эта граница у меня звучит так: **README правится только там, где он ОТСТАЁТ от прочитанного в коде, и каждое такое место называется в докладе поимённо.**

## §4. #156 закрыт как перекрытый

У одной постановки стояли две открытые головы: #156 (`eeb86945d4d6c7c7102e2c1c6be1c5d54d686b84`, открыт `17:27:39Z`, два файла) и #157 (уехал в `main`). Закрыл #156 комментарием, назвав в нём merge-коммит #157; ветку НЕ удалял — reopen дешёв. Ни один чужой PR (#161, #160, #159, #158, #153, #142, #134, #114) я не двигал и меток на них не вешал: они принадлежат другим тредам, а след гарда 5 от моей кнопки ушёл бы не в тот тред.

## §5. Что осталось открытым — и почему это к john

Живого хода в треде 042 больше нет: обе починки (#152 — `metrics --now`, #157 — четыре бездверные строки) в `main`. Осталось ровно то, что я назвал долгом ещё в `msg-099` §3, и оно всё — норма:

1. **Дверные строки: `init` и `orchestrator daemon`.** Их строка формы КОРМИТ дверь `guardArguments` — дописать в неё флаг значит расширить принимаемый argv, то есть снять существующий отказ. Это изменение поведения контура, а не возврат к объявленному, и класс «полевой измеренный дефект» на них не распространяется по построению. **Вопрос john: чиним или оставляем как есть?**
2. **Дверь для 16 бездверных команд** (`config check`, `doctor`, `roles list`, `schema migrate`, `schema version`, `role exists`, `zones check`, `capability run`, `merge-gate`, `index build`, `thread build`, `check`, `migrate`, `derive`, `tasks list`, `metrics`). Сегодня опечатка во флаге у них не отказывается вовсе. Введение двери — новые отказы, то есть норма. **Вопрос john: вводим ли, и если да — сразу всем шестнадцати или частями?**
3. **Замер, который к этому вопросу прилагается и который принесла ревьюер** (некритично сегодня, блокирующим не был): после починки альтернативы `zones check` парсер классифицирует `--staged` как флаг СО ЗНАЧЕНИЕМ — следующий за ним токен в строке это разделитель `|`, а правило «следующий токен не флаг ⇒ берёт значение» разделителя от значения не отличает. Проверено ей прямым чтением `parseUsage(USAGE).get("zones check")`: `--staged` лежит в `value`, не в `boolean`. Пока `zones check` вне двери, цена ровно нулевая — поведение не меняется ни на один вызов. В день, когда дверь этим командам появится (вопрос 2), вызов `... --staged` последним флагом словит ложный отказ «expects a value and is the last thing on the line». Новые тесты этого не ловят: они проверяют присутствие имени флага в объединении `value`+`boolean`, а не классификацию. Это не отдельная развилка, а цена, которую надо знать, отвечая на вопрос 2.

**Цену парковки называю вслух:** `--parked-on john` замораживает тред ЦЕЛИКОМ, и разморозит его только доклад с пересечением контуров, а не ход роли. Плачу её сознательно: живого хода, который двигался бы без john, в 042 сейчас нет — ровно поэтому в `msg-099` §3 я не парковался, а сейчас паркуюсь.

## §6. Состояние руки

Кода не писал (`packages/**` — не моя зона; всё пакетное сделано dev-core в #157). **Рабочее дерево роли чистое, незакоммиченного нет:** `git status --porcelain` пуст, временный worktree снят, пробный коммит `e019c841` живёт вне репозитория роли и на `origin` его нет. Тело этого письма — в своём `mktemp -d`, вне чекаута почты и вне дерева роли. Конфиг, воркфлоу и доки власти не трогал; почту руками не трогал; в чужом прогоне не сидел ни секунды. Ход мой, вопрос к john — первой строкой.

waiting-on → curator.

