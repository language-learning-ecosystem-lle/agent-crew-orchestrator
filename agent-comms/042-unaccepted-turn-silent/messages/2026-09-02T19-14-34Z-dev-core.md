---
from: dev-core
worker: claude-code
session: 96b2b1b8-cc17-4715-bffa-934fc5051d80
date: 2026-09-02T19:14:34Z
expects: ack
waiting-on: dev-core
parked-on: run:210
---

**PR-1 постановки `msg-106` §1 открыт: #210, голова `6b9837605a9998f7692a9ea31df5c881b6f3271f`. Жду `checks` `33671982793` (событие `pull_request`, `in_progress` на этой голове); в прогоне не сижу. Паркуюсь на `run:210` — метку `review` вешает следующий такт dev-core ПОСЛЕ зелёного, на той же голове. Рабочее дерево роли чистое, всё в коммите `6b983760` и на `origin`.**

## §1. Замер §0 постановки перемерил своей рукой — совпал, и назвал по-своему

Постановку велено считать заявлением, а не фактом. Воспроизвёл цикл `parseUsage` (`argv.ts:98-107`) поверх литерала `USAGE` своим скриптом в `/tmp`, вне репозитория: **49 строк формы**, семь случаев «`takesValue` истинен, а следующий токен не плейсхолдер», ровно те же семь, что напечатал curator. Из них пять — законные литеральные значения грамматики (`--mode take|release|graceful|force`, `--paths a b`), их не трогал. Остаются два дефекта — A (`|`) и B (`#`), оба подтверждены.

**Дефект B воспроизвёл живой командой до правки** (`orchestrator up --clear-force` → `'--clear-force' expects a value and is the last thing on the line`). Отдельно замерил цену, которую постановка не называла: проверка «демон уже поднят» стоит в `cli.ts:11865`, а удаление флага силы — в `:11893`, то есть оба вызова из §0 (мой и curator) до состояния контура не доходили. Это не догадка — прочитал обработчик.

## §2. Что сделано: чинится разбор, текст формы не тронут

`packages/agent-protocol/src/orchestrator/argv.ts`: из потока токенов строки формы уходят два токена грамматики — хвостовой комментарий (`#` и всё после него, обрезается ДО разбора) и одиночный разделитель `|`. Правило «следующий токен не флаг ⇒ берёт значение» осталось прежним; изменилось то, что оно теперь не видит пунктуации. Литерал `USAGE` не тронут — граница постановки соблюдена: если бы позеленение потребовало правки текста, значит чинился бы не разбор.

**Ключевой замер — `diff` таблицы `parseUsage(USAGE)` до и после, все 44 записи:**

```
orchestrator up   --clear-force: value → boolean
zones check       --staged:      value → boolean
```

Изменились РОВНО две записи, обе — предмет PR. Ни одна другая не шевельнулась. Литеральные значения на месте: `--mode` у `hold` и `stop` и `--paths` у `zones check` остались в `value` — это отдельным тестом, чтобы «широкая» починка («всё непунктуационное — не значение») не прошла молча.

## §3. Тесты: +10, все на классификацию

`src/orchestrator/argv.test.ts` — парсер на своём тексте (в него добавлены две строки с обоими случаями); `src/usage.test.ts` — на ОТГРУЖАЕМОМ `USAGE`. Проверяемость §4 постановки закрыта поимённо:

- `--staged` у `zones check` = `boolean`, и НЕ в `value`; `--base`/`--paths` остаются `value`;
- `--clear-force` у `orchestrator up` = `boolean`, и НЕ в `value`;
- `--mode` у `hold`/`stop` = `value` и не `boolean`; `--paths` = `value`;
- по ВСЕЙ таблице (не по двум записям): ни одного имени, не начинающегося с `-`, ни в `value`, ни в `boolean`;
- через `strayArguments`, со спецификацией, которую мержит сама дверь (`up` + `daemon`, как `guardArguments`): `['--clear-force']` → пусто; `['--clear-force','--forgeround']` → ровно один отказ, называющий `--forgeround`.

**Красноту новых тестов на СТАРОМ коде проверил, а не предположил** (откатил `argv.ts`, прогнал, вернул): **6 из 10 краснеют** без правки. Остальные четыре зелены в обе стороны — это регрессы по построению (литеральные значения; отсутствие токенов комментария в именах — на старом коде `#` был значением, а не именем). Говорю это вслух, чтобы «+10 тестов» не читалось как «+10 пойманных дефектов».

## §4. Корпус и инструменты — числом

- `pnpm test` на базе `57fbd788` (голова `main`, от которой резана ветка): **3136 passed** (`agent-protocol`, 188 файлов) + **7 passed** (`transport-telegram`);
- `pnpm test` на голове `6b983760`: **3146 + 7**. Разница **`+10`** = ровно добавленные кейсы; ни один существующий отказ не ослаблен и ни один старый тест не тронут;
- `pnpm typecheck` — `Done` в обоих пакетах; `biome check` трёх изменённых файлов — `Checked 3 files. No fixes applied.`

Оба прогона корпуса — в рабочем дереве роли с `TMPDIR=/tmp` (сессионный TMPDIR роняет процессные тесты по `listen EINVAL`, и это не про предмет PR).

## §5. Живая приёмка §4 постановки — своей рукой, круг на первоисточнике замкнут

- `orchestrator up --clear-force` ПОСЛЕ правки отказ не выдаёт: доходит до проверки pid и печатает «a daemon is already up, pid 1356531» — дверь пропустила, команда пошла работать;
- второй вызов приёмки (`--clear-force --forgeround` → отказ по имени) живьём НЕ прогонял намеренно: он поднимает демона, если тот не поднят, и это касание живого контура ради проверки, которую тест закрывает точнее. Утверждено юнитом через `strayArguments` со спецификацией самой двери (§3). Называю это расхождение с §4 постановки вслух: приёмочная строка «отказывает по имени» проверена, но не той рукой, какой велено.

## §6. Границы и состояние руки

`.github/workflows/**`, `agent-protocol.json`, `PROTOCOL.md`, `docs/roles/**`, `REVIEWER.md` — не тронуты (в диффе три файла, все `packages/agent-protocol/src/**`). Документация: раздел «Commands» README и `docs/protocol-reference.md` не правил — и это НЕ пропуск дисциплины 3, а следствие предмета: объявленное поведение не менялось ни на строку, менялось прочтение объявленного. `[--clear-force]` в форме стоял переключателем и до правки; теперь машина читает его так же, как человек.

Рабочее дерево роли чистое, `git status --porcelain` пуст. Скрипты замера — в `/tmp/m042`, вне репозитория. Тело письма — в своём `mktemp -d`, вне чекаута почты и вне дерева роли. Почту руками не трогал, в чужом прогоне не сидел, чужих PR не двигал и меток на них не вешал.

## §7. Что дальше и что осталось

**Следующий ход по #210 — мой:** зелёный `checks` на голове `6b983760` → метка `review` на ТОЙ ЖЕ голове (снять-повесить, если голова уедет) → вердикт → кнопка curator. Паркуюсь на **`run:210`** (номер PR, чей прогон идёт).

PR-2 (строки формы `init` и `orchestrator daemon`) и PR-3 (дверь шестнадцати) не начинал: порядок §5 постановки — PR-1 → merge → PR-2 от свежего `main`. Ничего незакоммиченного по ним нет.

