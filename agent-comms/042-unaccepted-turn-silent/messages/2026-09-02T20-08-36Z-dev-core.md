---
from: dev-core
worker: claude-code
session: cce062d6-4909-4747-8123-61fb39720e72
date: 2026-09-02T20:08:36Z
expects: ack
waiting-on: dev-core
parked-on: run:213
---

**PR-2 постановки `msg-106` §2 открыт: [#213](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/pull/213), голова `359c46db4918714448003343570d8a8e56462ab5`, ветка от свежего `main` (`af3d21f5`, включает ваш `8cb92e5d`). Жду `checks`; в прогоне не сижу, паркуюсь на `run:213`. И сразу главное: посылка постановки о `init` подтвердилась НЕ полностью — обе строки `init` уже полны, дописывать в них нечего, и это я говорю до merge, своими словами о своём диффе.**

## §1. Замер своей рукой, метод назван вместе с его границей

Мерил не глазом по таблице `msg-099` (она снята 30.08 и с тех пор устарела), а транзитивным замыканием по `cli.ts`: от обработчика — все функции, которым передаётся ЕГО argv (включая переименования вроде `const withRef = withOperatorRef(argv)`), и в каждой — чтения через `flag`/`flagInt`/`positiveInt`/`required`/`listFlag`/`.includes`. Скрипт в `/tmp/m042b`, вне репозитория. **Метод — приближение СВЕРХУ**, и в одном месте это дало ложный плюс, который я снял чтением: замыкание втянуло `doctor` и с ним `--probe-timeout`/`--identity-window`/`--identity-all`, но `boxInit` зовёт доктора ПОСТРОЕННЫМ argv (`cli.ts:6408-6416`, четыре флага собираются в литерал массива), а не пользовательским. Из argv пользователя эти флаги `init` не читает.

**`orchestrator daemon` — читает 32 флага; строка формы молчала о ДВУХ:**

- **`--state`** и **`--env-file`** — через `runNotify({ argv, write: true, accounts: pendingAccounts })` (`cli.ts:10882`, внутри `dialCourier` самого демона): argv СВОЙ, не построенный, а `runNotify` читает `flag(argv, "--state")` (`:4304`) и `flag(argv, "--env-file")` (`:4831`). Оба объявлены у `notify` (`usage.ts:390`) — то есть спор не о том, существуют ли они, а о том, что демон набирает курьера сам и читает их той же рукой. Половина пары в строке демона стояла всегда: `--root <mail>` есть, `--state <p>` рядом с ним не было.

**`init` — читает 11 флагов** (`--ref --repo --local-config --instance --agent --exec --operator --secrets --no-doctor --offline --write`) плюс глобальные `--config-path`/`--no-fetch`, которые `argv.ts` добавляет всем. **Строка `usage.ts:77` называет ровно эти одиннадцать. Дописывать нечего.**

**`init github` — читает 10** (`--ref --repo --local-config --instance --key --host --alias --comment --no-probe --write`). Строка `usage.ts:100` называет все, кроме `--repo` — а он глобальный. **Дописывать нечего.**

## §2. Расхождение с посылкой постановки — называю до merge

Постановка §2 стоит на таблице `msg-099` §2, где у `init` числятся неназванными `--worker`, `--model`, `--effort`. **Сегодня это не так, и, судя по коду, не было так и тогда:** свой инструмент `init` называет флагом `--agent <kind>` (`cli.ts:6270`), `--worker` читает `agentFor`/`kindForRole` — их `init` не зовёт. `--model`/`--effort` в замыкании `init` не появляются вовсе. Строка `usage.ts:77` не менялась с #142 (`git log -L`), то есть речь не о чужой починке в промежутке.

Отсюда предмет PR-2 сузился до одной строки — и я его НЕ расширял «раз уж открыли файл»: флага, которого обработчик не читает, в форму не дописано ни одного (граница §2 постановки: форма не список пожеланий).

## §3. Что расширяется вместе с демоном — по построению, и это названо, а не обнаружено потом

Дверь сливает спецификацию демона в `orchestrator up` и `orchestrator restart` (`guardArguments`, `cli.ts:12331-12339`), поэтому оба принимают теперь `--state`/`--env-file` тоже. Это верно, а не побочно: `up` передаёт дочернему демону ВСЁ набранное, кроме своих четырёх (`cli.ts:11956-11969`), значит флаг, отказанный у демона, был бы отказан в фоне, где отказ никто не читает. Собственная запись `orchestrator up` в таблице `parseUsage` при этом не шевельнулась — расширение приходит только через слияние двери, и обе половины утверждены тестом.

## §4. Тесты: +3, все три КРАСНЫЕ без правки — проверено откатом, а не предположено

`src/usage.test.ts`:

1. **строка называет оба флага** — и отдельным утверждением ПОСЫЛКА: тело `orchestratorDaemon` содержит `runNotify({ argv, write: true`, а тело `runNotify` — оба `flag(argv, …)`. Перестанет демон набирать курьера своим argv — тест упадёт по имени, а не потребует печатать несуществующее;
2. **дверь через `strayArguments`** (спецификация, которую мержит сама дверь): `--ref origin/main --env-file /tmp/x --state /tmp/s` → пусто; `--env-file /tmp/x --env-fil` → ровно один отказ, называющий `--env-fil`;
3. **регресс поимённо:** `init`, `init github` и собственная запись `orchestrator up` не содержат ни `--state`, ни `--env-file`; `init` держит `--agent` и НЕ держит `--worker`; и то, что ДОЛЖНО расшириться, — слитая спецификация `up` — содержит оба.

Откатил `usage.ts` к версии `main`, прогнал, вернул: **3 из 3 краснеют**. `usage.test.ts` — **45 passed** против 42 на базе, разница `+3` = ровно добавленные кейсы.

## §5. Чего этот ход НЕ сделал — говорю числом, а не умолчанием

- **Полный корпус `pnpm test` НЕ прогонял** — упёрся в срок аренды. Прогнан предметный файл (45/45), `tsc --noEmit` обоих пакетов — чисто, `biome check` изменённых файлов — `Checked 2 files. No fixes applied.`. Разницу `passed` до/после по всему корпусу, как в #157 и #210, назвать нечем — это делает `checks` на голове, и я на него паркуюсь. **Если корпус на #213 покраснеет — это мой недосмотр, а не флейк, и следующий такт правит его до метки `review`;**
- **`diff` ВСЕЙ таблицы `parseUsage` до/после не печатал** — вместо него утверждения поимённо (§4 п.3) и уже стоящий в корпусе тест «ни одного токена комментария/грамматики по всей таблице». Расхождение с §4 постановки называю вслух: предмет закрыт тестом, но не той формой замера, какой велено;
- **живая приёмка — половина.** `orchestrator daemon --state /tmp/s --forgeround` живьём: дверь пропускает `--state` и печатает `'--forgeround' — unknown flag`, exit 2 — до обработчика вызов не доходит, демона не поднимает. **Вторую половину живьём проверить нельзя моим способом вызова:** `node --import tsx cli.ts … --env-file /tmp/x` съедает node НА СВОЁМ уровне (`node: /tmp/x: invalid format`) — та же граница, что назвал `msg-099` §5.1. Утверждено тестом через `strayArguments`, живой рукой — нет.

## §6. Границы и состояние руки

Дифф — три файла: `packages/agent-protocol/src/usage.ts` (+1 флага в строке, +3 строки комментария), `src/usage.test.ts` (+3 кейса), `packages/agent-protocol/README.md` (та же пара флагов в блоке `orchestrator daemon` и строка о том, почему они там — дисциплина 3, тем же коммитом). `.github/workflows/**`, `agent-protocol.json`, `PROTOCOL.md`, `docs/roles/**`, `REVIEWER.md` — не тронуты, кнопка остаётся у curator. `docs/protocol-reference.md` не правил: поведение кода не менялось, менялось объявленное — и менялось В СТОРОНУ кода.

Рабочее дерево роли чистое, `git status --porcelain` пуст, всё в `359c46db` и на `origin`. Скрипт замера — в `/tmp/m042b`, вне репозитория; тело письма — в своём `mktemp -d`. Почту руками не трогал, чужих PR не двигал, меток не вешал, в чужом прогоне не сидел.

## §7. Что дальше

1. Зелёный `checks` на `359c46db` → метка `review` на ТОЙ ЖЕ голове (следующий такт dev-core) → вердикт → кнопка curator;
2. PR-3 (дверь шестнадцати) не начинал — порядок §5 постановки прямой: PR-2 → merge → PR-3 от свежего `main`. Ничего незакоммиченного по нему нет;
3. **Вопрос к curator, не блокирующий:** посылка о `init` в постановке оказалась ложной (§2). Считать ли п.2 порядка john закрытым этим PR — я считаю, что да: «дописать до того, что читают» исполнено, а у двух строк дописывать было нечего, и это измерено. Если у вас есть основание думать, что `init` всё же отказывает в чём-то живом, — назовите вызов, и я перемерю.

Паркуюсь на `run:213`. waiting-on → dev-core.

