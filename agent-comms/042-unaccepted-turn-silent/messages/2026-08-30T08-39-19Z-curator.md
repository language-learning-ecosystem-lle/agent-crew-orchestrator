---
from: curator
worker: claude-code
session: a8d2032b-360f-43a3-824f-6b40baec0d95
date: 2026-08-30T08:39:19Z
expects: ack
waiting-on: curator
parked-on: john
---

**След гарда 5 по #119 + замер суток §6.4 (сутки НЕ чистые, и главный разрыв — не Д-5) + #120 по-прежнему ждёт кнопку john. Своей рукой ничего не жала.**

## §1. След гарда 5 — посадка #119, кнопка john

- **что село:** `600e49bb` в `main` — `docs(reviewer): вердикт объявляется парой полей заголовка … (тред 042) (#119)`. Контрольное чтение цели своей рукой: `git merge-base --is-ancestor 600e49bb da6ed124` → истина, голова `main` сейчас `da6ed1240ce7f3439753bde783eb77b99ca00799`;
- **рука:** john, кнопка. Слово доставлено полем `delivers: john` письмом `2026-08-30T08-31-59Z-curator.md` (чатный curator), событие — `2026-08-30T08-31-52Z-github.md`, `merged-pr: 119`, `merged by maysway`;
- **гард 1:** `approve` на голове `46156318f30fb33b8630f8dacb92ce51a87400bc`, вердикт письмом `2026-08-29T16-20-21Z-reviewer-pr.md`, В ОКНЕ круга `33262497826` (`Claude PR Review`, `event=pull_request`, success) — замерено дверью прошлым ходом, повторно не переигрывала;
- **гард 2:** `review=SUCCESS, checks=SUCCESS` на той же голове (та же дверь);
- **гард 3:** тред `042-unaccepted-turn-silent`, восхождение — слово john «ВАРИАНТ 3» (доставка `2026-08-29T12-26-53Z-curator.md`), севшее нормой `PROTOCOL.md` кнопкой john в #108;
- **гард 4:** STOP — `REVIEWER.md` в выведенном данными списке доков власти. **Именно поэтому кнопка john, а не моя**, и это не обход гарда, а его исполнение;
- **гард 5:** эта запись.

## §2. #120 — стоит, и без john не двинется

`gh pr view 120` моей рукой сейчас: `OPEN`, голова `d058035503c64218917861503a567ce4654d7a47`, `mergeable=MERGEABLE`, `mergeStateStatus=CLEAN`, меток нет (и не будет — круг на такой PR вернёт пустой прогон, не вердикт). Гард 2 замерен прошлым ходом на этой же голове (`checks pass`, прогон `33262930697`); голова с тех пор не двигалась — SHA совпадает буквально.

**Требуется решение john, конкретно:** прочитать дифф #120 (гард 1 этого класса — человеческое чтение: автоматика PR, правящий `claude-review.yml`, не судит по построению) и назвать чтение в треде поимённо — кто прочёл, на какой голове (`d058035503c64218917861503a567ce4654d7a47`), чем подтверждено; затем кнопка squash. Гард 4 на `.github/workflows/**` — STOP у любой руки, кроме твоей.

## §3. Предусловие живой приёмки ЗАКРЫТО — и по пути снята ложная тревога о ночи

- `.orchestrator/daemon-code.json`: `sha=da6ed1240ce7f3439753bde783eb77b99ca00799`, `startedAt=2026-08-30T08:32:17Z`, `pid=4097491`. Ancestry проверена командой: `ce3f0526` (#118, лифт) и `600e49bb` (#119, инструкция) — оба предки. **Лифт и инструкция теперь В ПРОЦЕССЕ демона, а не только в чекауте**, то есть §4 письма `16-36-07Z` (замер соврёт раньше самолечения) больше не действует;
- ложная тревога, которую я чуть не привезла находкой и сняла замером: ротированный лог `daemon.log.1` обрывается строкой `SELF-RESTART … код 75`, а следующая эпоха — `2026-08-30T07:55:44Z`, что читается как «ночью бокса не было». **Это не так.** `journalctl --user -u agent-protocol@hetzner.service`: процесс `3399030` жил с `17:43:59Z` до `2026-08-30T07:55:33Z` (там же его `SELF-RESTART` на `1c8187c1`), `systemd[1245]` записал `status=75/TEMPFAIL`, `Scheduled restart job, restart counter is at 4` и `Started …` через 10 секунд. Юнит: `Restart=on-failure`, `RestartUSec=10s`. **Ночного простоя контура не было; путь самоперезапуска отработал сам, руки john не потребовалось.**

## §4. Сутки §6.4 — замер, и он оказался мягче моего первого чтения

Считала из `.orchestrator/journal.jsonl` с начала суток `2026-08-29T12:11:22Z` (232 события): `lease-released` — 36 `completed`, **24 `exited-without-handoff`** (все `exitCode 0`), 4 `supervisor-gone`.

По каждому `exited-without-handoff` смотрела, когда та же пара (роль×тред) поднялась снова:

- **внутри рабочего дня** возвраты идут в основном за ~30 минут, и россыпь `29.8–31.1 мин` слишком ровная для случайности — это такт, а не разброс. Выбросы: **111.7 мин** (`curator×042`, `13:36:34Z → 15:28:18Z`) и **54.8 мин** (`curator×047`);
- **три пары не поднялись вовсе** — и вот тут мой первый вывод был неверен, я его проверила прежде, чем привезти находкой. Ни одна из трёх не дефект: `dev-core×048-session-privileges` — тред **закрыт** (`_meta.status: closed`, «НЕДЕЙСТВИТЕЛЬНЫЙ дубль»), поднимать нечего; `curator×036-account-failover` — законная парковка `parked-on: pr:109` в последнем письме; `curator×043-stale-park-reminders` — ход ушёл ДРУГОЙ роли: `dev-core×043` поднят в `08:32:25Z` и написал в `08:36:17Z`. То есть счёт по ключу «роль×тред» показывает стоящую пару там, где стоит не предмет, а конкретная рука;
- **ночной разрыв 14ч11м** (`17:44:11Z → 07:55:55Z`, ноль подъёмов) — тоже НЕ дефект: в тике `17:40Z` в очереди стояло 6 кандидатов, и все скипнуты названной причиной (`skipped: the turn is parked behind a decision of john (R27)`; одна — `parked-on: pr:109`). По §5 постановки это законное молчание и предмет Д-4.

**Что из этого следует для приёмки §6.4, называю ровно:** молчаливых стоянок за сутки я НЕ нашла — каждая долгая стоянка объяснена причиной, названной в логе или в шапке письма. Но и «сутки чистые» сказать нельзя по двум причинам, и обе мои, а не контура:

1. **порог не выбран.** §3 постановки называл кандидата 15–20 минут «выбрать замером». Замер теперь есть: после `exited-without-handoff` пара возвращается тактом ~30 минут. При пороге 15–20 мин КАЖДЫЙ из 24 случаев — превышение; при пороге 35 — ни один. **Пока порог не назван числом, критерий §6.4 не судится вовсе** — и это первый предмет следующего такта, а не мелочь формулировки;
2. **мерила я симптом, а не класс.** `exited-without-handoff` — это сессии, вышедшие без передачи хода. Пара, чей `waiting-on` указывает на свободную роль и которая не поднялась ВОВСЕ, в журнале следа не оставляет: её видно только строками тиков в логе. Такой сверки за сутки я не делала — окно кончилось.

Точку старта суток поэтому переношу на **`2026-08-30T08:32:17Z`** (эпоха демона на `da6ed124` — первая, где в поле стоят ОБА севших куска, #118 и #119), а прежнюю `2026-08-29T12:11:22Z` закрываю замером выше, а не молча.

## §5. Что подхватывает следующая рука curator

1. **#120** — чтение диффа john + его кнопка (§2). До этого шаблон промпта пары не несёт;
2. **живая приёмка №1 — у следующего же круга ревью.** Замер моей рукой сейчас: во всём ящике писем `reviewer-pr` с парой В ШАПКЕ по-прежнему **ровно одно** — `042/2026-08-29T16-20-21Z-reviewer-pr.md`. Сверять шапку следующего письма (кандидат — #124, метка стоит). **Гипотеза, а НЕ факт, и я её так и подаю:** первый чекаут воркфлоу (`actions/checkout@v5` без `ref`, строка 92) на событии `pull_request` берёт merge-ref, то есть голову PR, слитую с базой, — если так, инструкцию из `main` теперь видит ЛЮБОЙ круг, а не только PR, несущий её сам. Проверяется это ОДНИМ наблюдением: пара в шапке у PR, чья ветка #119 в себе не несёт. Пока не наблюдалось — не утверждать;
3. **сутки §6.4 идут с `2026-08-30T08:32:17Z`** (§4). Перед следующим замером — НАЗВАТЬ ПОРОГ ЧИСЛОМ (§4.1: такт возврата ~30 мин замерен, кандидат постановки 15–20 мин — они несовместимы) и сверять не только журнал, но и тики лога (§4.2);
4. **четыре бездомные ноты** без изменений: «доставка отказала при живом `verdict.md`»; расхождение читателей `VERDICT_LINE`/шапки; находка про четвёртую ложную причину (`2026-08-29T12-06-45Z-dev-core.md`); вторая ветка приёмки #107;
5. **счёт вести по ПРЕДМЕТУ, а не по ключу «роль×тред»** — §4 показал, как этот ключ рисует стоянку там, где тред движется другой рукой.

## §6. Состояние моей руки

Сделано: чтение треда, `gh pr list/view` по #120 и открытым PR, чтение `_meta.md` и последних шапок тредов `043`/`036`/`048-session-privileges` (проверка §4), ancestry `ce3f0526`/`600e49bb` над `da6ed124`, разбор `journal.jsonl` за сутки, `journalctl` по юниту за ночное окно, чтение `daemon-code.json`/`self-restart.json`, скан шапок всех писем `reviewer-pr` в ящике, чтение шагов чекаута `claude-review.yml`. **Кода не писала, веток не заводила, PR не открывала, меток не вешала, кнопок не касалась, почту руками не трогала. Рабочее дерево роли чистое, незакоммиченного нет; тело письма — в `mktemp -d`, вне дерева роли и вне почты.**

waiting-on → curator, parked-on → john.

