---
from: curator
worker: claude-ai
date: 2026-08-30T16:06:54Z
expects: none
waiting-on: dev-core
delivers: john
priority: high
---

**Перекрёстная ссылка по слову john (чат ~16:06Z: «не перепутаются ли фиксы, делая одно и то же?»). Проверено чатным curator: предметы РАЗНЫЕ, дублей нет — но четыре из них правят ОДИН слой, и это надо знать до открытия веток.**

## Общий слой: курьер и уведомитель

| тред | предмет (симптом) | НЕ предмет |
|---|---|---|
| **042** (этот) | ход передан свободной роли, подъёма нет, никто не звонит | форма строк, старые парковки, доставка пинга |
| **043** | о ЖИВОЙ парковке на человеке звонят один раз и больше никогда | непринятый ход |
| **057** | пинг сторожа мигает (5 с без повторов), john получает ЛОЖНЫЙ алерт о смерти ящика | содержание дайджеста |
| **058** | две роли пишут в один тред; звонок и читающий видят последнее письмо, а не стоящий вопрос | сам параллелизм ролей |

Пересечения по смыслу нет: каждый отвечает на свой вопрос и имеет свою проверку. **Пересечение по КОДУ есть**: `notify`/курьер и их состояние трогают все четыре.

## Что из этого следует для исполнения

1. **Вести по одному.** Две ветки в этом слое одновременно будут конфликтовать между собой, а не с `main` — и разбор конфликта съест больше, чем сама правка;
2. **Перебазироваться ПЕРЕД меткой**, а не после вердикта. Сегодня уже измерено на `057`: зелёный стоял на голове, чью базу сдвинули, и круг пришлось гонять заново;
3. **Версия схемы — называть в треде ДО открытия PR.** Два случая за двое суток, когда номер занимали параллельно (`22` в #109/#111, `23` в #109/#123): второй PR выглядел готовым и положил бы контур. Кто трогает версию — пишет номер в свой тред первой строкой, чтобы соседний предмет увидел занятость;
4. **Приёмки не смешивать**: `042` закрывается «перестало стоять» (по слову john 30.08 — по юнитам и репетиции, полевой критерий в долях свободного времени), `043` — сутки без пропущенного напоминания, `057` — сутки без ложного алерта, `058` — john не спрашивает «почему тред стоит, если в последнем письме об этом ни слова».

Смежные, но в другом слое и в общий порядок не входят: `060` (сборка производных падает целиком), `061` (парковка за недостижимым событием), `056` (общий `/tmp`), `052` (шаблон описания PR).

waiting-on → dev-core.
