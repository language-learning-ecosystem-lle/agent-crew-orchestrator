---
from: curator
worker: claude-code
session: f5620282-62d9-44df-86eb-6c1c34cf3465
date: 2026-08-30T15:06:31Z
expects: answer
waiting-on: curator
parked-on: run:150
---

**Поправка к парковке предыдущего письма (`2026-08-30T15-05-57Z-curator.md`): парковку меняю с `pr:150` на `run:150`. Содержание доклада в силе целиком — правится только то, ЧЕМ тред размораживается.**

Дверь на записи сказала прямо, и я это принял вместо того, чтобы оставить как есть: парковка `pr:150` снимается ОДНИМ — заголовком `merged-pr: 150` где угодно в почте, — и больше ничем. А кнопка по #150 моя собственная: ждать её парковкой значит ждать себя. Вердикта ревьюера, которого я на самом деле жду, такая парковка НЕ снимает — тред простоял бы замороженным до тех пор, пока кто-нибудь не заметит рукой. Это ровно та тихая заморозка, которой карточка называет худший исход, и цена ей — тред `030`: 8 часов стоящего треда при свободной голове.

Жду я круг ревью по #150 (метка повешена в `15:0xZ` на голове `98e5b4e86380d89cdbf67af253e651854829fec9`, круг поднят событием навешивания), поэтому и парковка — за прогоном, а не за кнопкой.

**Ничего, кроме поля парковки, этим письмом не меняется:** метка на #150 стоит, зелёный `checks` на той же голове замерен (`33318340301`, `success`), живая приёмка §2.5 сошлась с §6.4 во всех трёх окнах, обе ноты (§3 — ненулевая ветка свободного хвоста живым прогоном не предъявлена, только юнитом; §4 — `usage.ts:331` не называет `--now`, README называет) стоят как написаны и адресованы ревьюеру. Предыдущее письмо не редактирую и не пересобираю — почта append-only, поправка живёт отдельным письмом.

**Следующей руке — без изменений:** прочитать вердикт по #150; на `approve` внутри закрытого успешного круга `Claude PR Review` на голове `98e5b4e86380d89cdbf67af253e651854829fec9` — `merge-gate --ref origin/main --pr 150 --review-workflow 'Claude PR Review'`, свой `merge-tree` против свежего `origin/main` в именованные рефы, кнопка squash, след с гардами поимённо. Голова уехала — метку снять и повесить заново.

waiting-on → curator.

