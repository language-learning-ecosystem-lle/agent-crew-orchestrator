---
from: curator
worker: claude-code
session: cde0c846-a003-4dde-a5b1-17d72d1124c7
date: 2026-08-29T13:16:17Z
expects: none
waiting-on: curator
parked-on: run:108
---

**Метка `review` повешена на #108 после зелёного `checks` на той же голове — круг ревью идёт (прогон `33254433131`, `event=pull_request`, голова `d2c0285b…`, стартовал 13:12:41Z). Паркуюсь `run:108` и передаю ход: у круга не стою.**

И отдельно, первым делом, потому что это ЗАМЕР, а не наблюдение по дороге: **эту сессию подняла не приёмка круга, а ПОТОЛОК парковки `run:108` — она простояла 31 минуту за кругом, которого не существовало.** Разбор в §2; там же грабли, которые следующая рука обязана не повторить.

## §1. Метка на #108 — гарды процедуры названы поимённо

- зелёный `checks` замерен своей рукой, а не «вся сборка passed»: `gh pr checks 108` → единственная строка `checks pass 4m1s`; прогон **`33253034239`**, `gh run view` → `conclusion=success`, `event=pull_request`, `headSha=d2c0285b47367946884006dc61be8639adaea6b7` — тот же полный SHA, что у головы PR;
- голова на момент метки — та же `d2c0285b47367946884006dc61be8639adaea6b7` (сверено `gh pr view` ПОСЛЕ `gh pr edit --add-label`, ответ инструмента ≠ факт: метка `review` стоит одна, голова не двинулась);
- круг поднят **перевешиванием метки**, а не пушем и не комментом (норма треда `036`): `gh run list --workflow 'Claude PR Review'` → прогон **`33254433131`**, `event=pull_request`, `headSha=d2c0285b…`, `status=in_progress`, создан 13:12:41Z — то есть якорь гарда 1 будет законный, а не сирота.

## §2. Почему поднялась эта сессия — и грабли, стоившие 31 минуты

`.orchestrator/daemon.log`, строка дословно:

```
thread 042-unaccepted-turn-silent: the park on the round of PR #108 HAS GONE STALE — declared
2026-08-29T12:41:28Z, standing 31 min against a ceiling of 30 min. It no longer freezes the pair:
the role is raised to check the outcome of that run itself (thread 062, layer 2)
```

Читаю это буквально, разделяя замеренное и выведенное.

**Замерено:** предыдущая рука объявила `parked-on: run:108` в 12:41:28Z, а метку `review` НЕ вешала — и написала об этом честно (§5 письма `12-41-28Z`: «метка вешается ПОСЛЕ зелёного `checks`»). Круга ревью в тот момент не было и появиться он не мог. Парковка `run:` ждала машинного события, которого никто не запускал.

**Отсюда — грабли, называю их для следующей руки:** `--parked-on run:N` **паркует за КРУГОМ, а не за прогоном `checks`**. Пока метка не повешена, круга не существует, и такая парковка ждёт события, которое никогда не наступит. Единственное, что её сняло, — потолок 30 минут (слой 2). Правильных ходов в той развилке было два: либо дождаться зелёного `checks` своей рукой ОДНОЙ командой и повесить метку в том же ходу (круг `checks` в тот момент шёл 4 минуты), либо парковаться `pr:108`.

**И это же — ЖИВАЯ ПРИЁМКА потолка `run:`-парковок в поле, впервые в этом треде.** Слой 2 отработал ровно по объявленному: не позвонил john, не заморозил пару навсегда, а поднял роль проверить исход самой. Дефект (парковка за несуществующим кругом) стоил 31 минуты вместо суток немоты. Это сильная половина приёмки — в отличие от §6 ниже.

## §3. Дверь по #108 — прогнана, отвечает STOP, и это правильный ответ

`pnpm protocol merge-gate --ref origin/main --pr 108 --review-workflow 'Claude PR Review'`:

- **STOP гард 1** — `no approve verdict on d2c0285` (круг идёт, вердикта ещё нет);
- **STOP гард 2** — `not green: review=IN_PROGRESS`, плюс **нота: база уехала после зачтённого зелёного** — `0af88684` лёг 12:52:01Z, а `checks` стартовал 12:39:03Z;
- **STOP гард 4** — `john merges this one — it changes PROTOCOL.md`. Дверь вывела семь доков власти данными, `PROTOCOL.md` в списке как `declared by 'powerDocuments'`;
- гарды 3 и 5 напечатаны обязательствами, как и должно.

**Ноту «база уехала» закрываю чтением, а не ребейзом** (ребейз стоил бы второго круга ревью), и называю это чтением, а не «гард 2 держится»: дифф #108 против merge-base — **ровно один файл, `PROTOCOL.md`**, кода не тронуто ни строки; `git merge-tree --write-tree origin/main pr108` → `exit 0`, дерево `28899c4d9ebbe6241d01a0efddbf37a8c3987731`, конфликтов нет. То есть уехавшая база (код в `packages/schema`) физически не может изменить того, что этот дифф ломает. **Решение — за тем, кто жмёт кнопку**, и это john.

## §4. Что надо сказать john, когда приедет вердикт

Дверь #108 — не моя по построению (гард 4). После вердикта ревьюера PR ждёт **кнопки john**, и вместе с кнопкой ему надо назвать две вещи: (1) норма без кода — половина, вторая половина это постановка dev-core на дверь `new-message` и `standingParkOf`, и она не может быть открыта до посадки нормы; (2) ноту гарда 2 из §3 с её чтением. Сама я его сейчас не зову: вердикта ещё нет, звонок без вердикта — пустой подъём.

## §5. Часы §6.4 идут — сверено командой

`.orchestrator/daemon-code.json`, прочитан этим ходом: `sha=0af88684f950bfa4f85d1f353c52c9c1320775f5`, `startedAt=2026-08-29T13:10:18Z`, `pid=1933791`. `git merge-base --is-ancestor b27780cc… 0af88684…` → **истина**. Демон самолечился четвёртый раз за сутки (10:24:58Z → 11:20:41Z → 12:11:22Z → 13:10:18Z), код с #107 в нём стоит непрерывно. **Старт суток §6.4 остаётся 2026-08-29T12:11:22Z** — новый рестарт его не сдвигает, потому что счёт идёт по наличию #107 в коде, а не по pid.

## §6. Вторая ветка приёмки #107 — в поле ЕЩЁ НЕ наблюдалась

Курьерские строки `daemon.log` после 12:41Z — шесть подряд: `5 parked, 2 of them asking, 0 of those new; 0 waits; 0 stalled over 180m — nothing to announce`. Грепом по `no reason known` за это окно — ни строки. **Восьмой класс молчит**, но молчит законно: пары из-под парковок за это окно не выходили. Звонок возрастом СВОБОДНОЙ части по-прежнему не наблюдался, и это остаётся несделанным пунктом приёмки #107, а не закрытым.

## §7. Хвост открытых PR — снят списком, чужого не трогаю

`gh pr list --state open`, снят этим ходом → **#109** (метка `review`, новый со вчерашнего счёта), **#108** (мой, метка повешена этим ходом), **#103** (тред 043, метка `review` — появилась после счёта 12:37Z), **#100** и **#96** (тред 026 и 038, метки `review`). Ни одного своего кандидата на merge: #108 за john, остальные принадлежат чужим тредам — гард 5 требует след в ТРЕДЕ своего PR, а прогон привязан к 042.

Побочно, из очереди демона той же минуты: **#100 объявлен `merge-ready` (гарды 1–2 держатся), но пара `curator×026` запаркована на john** — то есть подъёма по нему нет и не будет до слова john. Пять парковок очереди из девяти стоят на john (`026`, `047`, `029`, `035`, `045`) — завал Д-4 не рассосался, он в долгах john с прошлого такта.

## §8. Находка dev-core (четвёртая ложная причина) — дома по-прежнему нет

Дома дать не могу по той же причине, что и две предыдущие руки: прогон привязан к треду `042`, новых тредов эта рука не заводит. **Срок годности находки сократился**: #96 несёт метку `review`, его круг может отдать вердикт в любой момент, а после merge четвёртый ложный звонок уйдёт про пару `curator×038`. Форма фикса и её единственная ловушка (`mergedElsewhere` при переигрывании событийных пролётов) описаны в письме `12-06-45Z-dev-core.md` — переоткрывать не нужно, нужен только дом и раздел «Проверяемость».

## §9. Состояние моей руки

Кода не написано ни строки, веток не заводила, PR не открывала. Сделано: чтение треда, замер `checks` на #108 и метка `review` на ту же голову, чтение прогона круга, `merge-gate` по #108, локальная проверка дерева слияния, чтение `daemon-code.json` и ancestry командой, курьерские строки `daemon.log`, `gh pr list`, это письмо. **Рабочее дерево роли чистое, незакоммиченного нет; `agent-comms` руками не трогала; тело письма лежит в `mktemp -d`, вне дерева и вне почты.** Локально создан ref `pr108` в главном чекауте (для проверки дерева слияния) — он не в рабочем дереве роли и грязи не даёт.

**Что подхватывает следующая рука** (в этом порядке):

1. **вердикт круга `33254433131` на #108** → если `approve`: звонок john кнопкой с §4 (норма без кода — половина; нота гарда 2 из §3 с её чтением); если `needs-fixes` — правка моей рукой, `PROTOCOL.md` в зонах curator;
2. **после посадки нормы — постановка dev-core на код** (дверь `new-message` принимает пару полей, `standingParkOf` читает её как новый ход при том же держателе; приёмка в фактах — вердикт, легший в запаркованный на человеке тред, поднимает держателя без ручного толчка);
3. **дом для находки dev-core** (§8) — новый тред с разделом «Проверяемость»; срок годности — до кнопки на #96;
4. **вторая ветка приёмки #107** (§6) — звонок возрастом СВОБОДНОЙ части, на паре, которую после лифта НЕ подняли раньше порога;
5. **сутки §6.4 идут с 12:11:22Z** (§5).

waiting-on → curator, parked-on → run:108.

