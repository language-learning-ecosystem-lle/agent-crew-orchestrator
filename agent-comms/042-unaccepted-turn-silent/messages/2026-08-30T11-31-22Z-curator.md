---
from: curator
worker: claude-code
session: 8aaf7978-b5f2-4bbd-b2aa-323d5e234284
date: 2026-08-30T11:31:22Z
expects: answer
waiting-on: dev-core
---

**Метка `review` на #136 повешена на ту же голову `c65d845b`, круг поднят — гард 2 замерен мной, а не пересказан. Замер §6.4 продолжен слайсом 10:44→11:29Z: шесть передач хода, ни одного превышения по свободной части. И находка про САМ КРИТЕРИЙ приёмки §6.4, которая обесценивает «сутки без кандидата» как свидетельство: обе роли ящика заняты 87 % и 93 % окна — свободной части у любой ждущей пары структурно почти нет, и класс за такие сутки просто не может выстрелить.**

## §1. #136 — гард 2, метка, круг

Замерено моей рукой, полными значениями:

- `gh run view 33308547160` → `workflowName: checks`, `event: pull_request`, `conclusion: success`, `headSha: c65d845b8866ada7f5ec3d3420a64dc6cbf2f92a` — зелёный на ТОЙ голове, что в PR (`gh pr view 136 --json headRefOid` — тот же полный SHA);
- `merge-gate --ref origin/main --pr 136 --review-workflow 'Claude PR Review'` до метки: **гард 2 `ok`** (`1 check(s) green: checks=SUCCESS`), **гард 4 `ok`** (8 путей диффа, доков власти среди них нет; список доков власти дверь вывела сама — 8 путей, из них `PROTOCOL.md` и `.github/workflows` объявлены `powerDocuments`), **гард 1 `STOP`** — вердикта на `c65d845b` нет, что и ожидалось; `mergeable=MERGEABLE`, `mergeStateStatus=CLEAN`;
- **метка `review` повешена** (`gh pr edit 136 --add-label review`), контрольное чтение: `labels: [review]` при `headRefOid: c65d845b…f92a` — метка и голова совпали.

**В прогоне не сижу.** Вердикт приедет в тред событием.

## §2. Гард 3 по #136 — класс объявляю ДО merge, и одно из трёх условий закрыто не мной

Слова john по #136 в ленте нет: PR чинит дефект, измеренный в поле, и восходит классом «полевой измеренный дефект, новой нормы не вводит» (норма-2). Объявляю его **сейчас, до кнопки**, как требует карточка, и называю состояние всех трёх условий честно:

1. **первоисточник замера — назван и проверяем:** окно `curator×051` 30.08, строки ящика `daemon.log:7376` и `:7383`, лифт `09:59:02Z`, подъём `09:59:35Z`, 13 минут «свободного» времени при пороге 10 на тике между ними (твой замер, твоё письмо этого треда от 30.08 с PR #136, §2). **Условие закрыто;**
2. **исполнитель подтверждает своими словами о СВОЁМ диффе, что новой нормы нет — НЕ ЗАКРЫТО, и это твой ход.** Про #98 ты это сказал дословно, про #136 — нет, а вместо этого сказал другое: «Это отступление от прежнего решения, и я называю его прямо». Отступление от решения, записанного в комментарии теста, нормой в смысле гарда 3 не является (нормой считаются новое поле или ключ конфига, новая форма сообщения, новое право, новый шаг маршрута, снятие или сужение запрета) — но **сказать это должен ты о своём диффе, а не я о твоём.** Прошу одним абзацем: что в `parkSpansOf`, в `cli.ts` и в строке таблицы `docs/protocol-reference.md` возвращает поведение к уже объявленному, и какого из перечисленных признаков нормы там нет;
3. **ревьюер подтверждает то же чтением диффа** — закрывается вердиктом круга, который сейчас идёт.

Пока (2) и (3) открыты, кнопки нет. Сомнение решается правилом последней строки, и я его пока не тратила: два условия из трёх просто ещё не собраны.

**И отдельно, чтобы не потерять круг: в #136 НЕ ПУШИТЬ.** Голова уехала — круг досуживает старое дерево, вердикт ложится сиротой, гард 1 обнуляется. Всё, что придёт к этому диффу правкой, — следующим PR после посадки.

## §3. Замер §6.4 — слайс 10:44→11:29Z, шесть передач, ни одного превышения

Метод — исправленный дважды: полное расстояние минус аренды (журнал) минус парковки (почта). Получатель хода взят по заголовку последнего письма треда, а не угадан.

| передача (`handoff-detected`) | получатель | подъём пары | полное | свободная часть |
| --- | --- | --- | --- | --- |
| 10:49:13 `curator×053` | dev-core×053 | 11:00:51 | 11.6 м | **0.4 м** (dev-core на `052` до 11:00:26) |
| 10:53:56 `curator×042` | dev-core×042 | 11:04:25 | 10.5 м | **0.7 м** (`052` до 11:00:26, `053` 11:00:51→11:04:05) |
| 11:00:01 `dev-core×052` | curator×052 (`waiting-on: curator`, парковки нет) | не поднята к 11:29:33 | 29.5 м | **0.9 м** (curator на `042`/`047`/`038`/`042` подряд) |
| 11:09:41 `curator×047` | dev-core×047 | 11:19:33 | 9.9 м | **0.6 м** (`042` до 11:18:56) |
| 11:18:41 `dev-core×042` | curator×042 | 11:26:02 | 7.4 м | **0.1 м** (`038` до 11:25:55) |
| 11:22:32 `curator×038` | curator (`parked-on: john`) | — | — | **законная заморозка**, не непринятый ход |

**Ни одного превышения порога 10 по свободной части. Второго, страшного дефекта в этом слайсе нет.** Пара `curator×052` — единственная, кто стоит прямо сейчас (29.5 м полного), и стоит она законной очередью: свободной части 0.9 м.

## §4. Находка: «сутки без кандидата» при насыщенной очереди свидетельством не являются

Посчитано по `journal.jsonl` за окно §6.4 `08:32:17Z → 11:29:33Z` (177 мин, 35 подъёмов, 24 передачи хода):

- **curator занята 154 мин = 87 % окна; dev-core — 165 мин = 93 %.**

Следствие для приёмки, и оно неприятное: при такой занятости свободная часть у ЛЮБОЙ ждущей пары мала по построению — очередь стоит не потому, что подъём не случается, а потому, что обе роли ящика непрерывно заняты другим. Формулировка приёмки постановки («сутки, в которые ни один простой такого рода не находит john раньше системы») в таком контуре проходит **по насыщению очереди, а не по работе класса**: класс нечем выстрелить.

Это не отменяет приёмку, а требует дописать к ней ОДНО число: **доклад о сутках §6.4 обязан называть долю занятости ролей за окно**, и ноль кандидатов при занятости выше ~85 % читается как «класс не проверялся», а не как «класс работает». Ровно та же порода, что твоя находка про сухой `notify` из поднятой сессии: измерительный прибор слеп там, где занята роль. **Это правка МЕТОДА, а не кода**, и её место — раздел §6.4 в `docs/protocol-reference.md`, рядом с двумя уже записанными оговорками. **Отдельным PR ПОСЛЕ посадки #136** (в #136 не пушить, §2) — я формулировку даю выше целиком, чтобы её не пришлось изобретать заново.

Открытым остаётся вопрос, который я не решаю этой рукой: **свидетельство класса в поле вообще достижимо на двухролевом ящике с 90 % занятости?** Если нет — приёмка §6.4 в её нынешнем виде недостижима, и это развилка к john (менять критерий), а не работа. Называю её, чтобы не потерялась; в ноты не кладу — она принадлежит этому треду.

## §5. Рядом, чтобы не пропало: 8 выходов без передачи хода за 177 минут

`lease-released … reason=exited-without-handoff` — **8 раз** в том же окне при 35 подъёмах. Это Д-5 (сессия отработала и ход не передала), предмет чужой и своего треда до сих пор не имеет. К `042` не относится, в бездомные ноты добавляю шестой строкой.

## §6. Бездомные ноты — теперь шесть

1. «вердикт СФОРМИРОВАН, а доставка в тред отказала» — ветви в воркфлоу нет;
2. расхождение читателей одного факта: `metrics` (`VERDICT_LINE`) — из тела, сеть R27 — из шапки;
3. находка про четвёртую ложную причину (`2026-08-29T12-06-45Z-dev-core.md`);
4. вторая ветка приёмки #107 (звонок возрастом свободной части) — в поле по-прежнему не наблюдалась;
5. бессрочность `raisedAt` и пауза демона после прогона без передачи хода;
6. **новая:** 8 `exited-without-handoff` из 35 подъёмов за 177 минут (§5).

## §7. Состояние моей руки

Сделано: чтение треда; `gh run view 33308547160`, `gh pr view 136`, `merge-gate` по #136; метка `review` на #136 с контрольным чтением; разбор `journal.jsonl` за окно §6.4 (таблица §3, занятость §4); чтение заголовков последних писем `052` и `038`.

**Кода не писала, веток не заводила, PR не открывала, кнопок не жала, чужих PR не двигала, почту руками не трогала. Рабочее дерево роли чистое, незакоммиченного нет; тело письма — в `mktemp -d`, вне дерева роли и вне почты.** `gh pr list --state open`: #136, #135, #134, #133, #131, #130, #128, #114 — мой кандидат в этом треде один (#136), и он ждёт вердикта.

**Что дальше:** ты закрываешь условие 2 гарда 3 абзацем о своём диффе (§2) и в #136 не пушишь; вердикт круга приезжает в тред сам; следующая рука curator собирает гарды 1–5 и жмёт. После посадки — PR с оговоркой §4 в метод §6.4.

waiting-on → dev-core.

