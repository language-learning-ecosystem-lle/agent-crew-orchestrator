---
from: curator
worker: claude-ai
date: 2026-08-30T13:34:18Z
expects: answer
waiting-on: dev-core
delivers: john
priority: high
---

**Слово john полем `delivers` (чат ~13:33Z), требование дословно: «делай так, чтобы шума не было, а была только правдивая информация». Повод — ЛОЖНЫЙ ЗВОНОК нового класса, случившийся сегодня.**

## 1. Факт

john получил строку:

> `curator×052-pr-template has been standing for 20m and this box has not raised it: the role is free, the thread is not parked, the pair is not out of attempts, and the daemon names no reason to skip it. Nothing in the mail is wrong — look at the box (is the daemon up, are launches enabled, is this role in its scope)`

Проверка ящика его же рукой (13:2xZ): демон `active`, флагов `stop`/`force`/`holds` нет, `enabled` на месте, **и при этом `2 of 3 role(s) live`** — `curator` занята прогоном по `053` (с `13:25:33Z`), `dev-core` — по `042` (с `13:19:33Z`). То есть утверждение «the role is free» на момент ЧТЕНИЯ было неверным, и адресат был отправлен искать поломку там, где её нет.

Цена уже уплачена: john потратил три захода на ящик и разбор, чтобы установить, что всё в порядке.

## 2. Что требуется (слово john)

**Ни одного звонка, содержащего утверждение, которое на момент печати неверно.** Не «поменьше шума», а «в строке не должно быть лжи». Практически это значит:

1. **признак «роль свободна» проверяется В МОМЕНТ ПЕЧАТИ строки**, а не в момент подсчёта возраста; если роль занята — строка не печатается вовсе, потому что стояние законно;
2. то же для остальных трёх утверждений строки (`не паркован`, `попытки не исчерпаны`, `демон не назвал причины`) — каждое обязано быть верным на момент отправки;
3. **если между замером и печатью состояние изменилось — печатается новое состояние либо не печатается ничего**; выбор за исполнителем, но «напечатать устаревшее» отпадает;
4. предложение «look at the box» уместно ТОЛЬКО когда после всех проверок причина действительно неизвестна; иначе строка обязана называть причину («роль занята другим тредом с HH:MM»), а не отправлять человека искать.

## 3. Граница

Класс не отменяется и не ослабляется: `051` действительно стоял 3 ч 16 м, и это правильный звонок. Отменяется только ложь в его формулировке. Порог, дедуп и остальная механика — прежние.

## 4. Проверяемость

Юнит: пара стоит дольше порога, роль ЗАНЯТА другим тредом → строки нет (сегодня она есть — это и есть регресс-тест на живом случае); пара стоит, роль свободна, причины нет → строка есть. Поле: john не получает ни одного звонка, отправляющего его на ящик, где всё в порядке.

waiting-on → dev-core.
