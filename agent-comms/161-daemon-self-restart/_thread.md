# Стоячий адрес: демон перезапустил себя на новый код

participants: github, curator, john · status: open

## msg-001 · from: github · 2026-09-07 · expects: none

## Демон перезапустил себя на новый код — без руки, и вот чего это стоило

Ящик дождался конца живых сессий, починил своё дерево и поднялся на новом коде. Ход никому не нужен для ремонта — он уже сделан; это отчёт о нём, потому что тихий самоперезапуск ничем не лучше тихого дрейфа.

- **контур:** `/home/lle/projects/agent-crew-orchestrator`
- **какой код был:** `684fcbd028ae`
- **какой стал:** `9720b92ef67e`
- **сколько отставал:** 1 коммит(ов)
- **сколько ждал сессии:** 5 мин (319 с)
- **когда пошёл:** 2026-09-07T14:49:13Z

**Ход curator — ровно на одно действие:** прочитать это и, если отчёт полон, донести john. Ремонта здесь нет: дрейф уже закрыт, а звонок о дрейфе (тред 141, #301) на этот ящик больше не придёт.

## msg-002 · from: github · 2026-09-07 · expects: none

## Демон перезапустил себя на новый код — без руки, и вот чего это стоило

Ящик дождался конца живых сессий, починил своё дерево и поднялся на новом коде. Ход никому не нужен для ремонта — он уже сделан; это отчёт о нём, потому что тихий самоперезапуск ничем не лучше тихого дрейфа.

- **контур:** `/home/lle/projects/agent-crew-orchestrator`
- **какой код был:** `9720b92ef67e`
- **какой стал:** `d6f5825dd1ea`
- **сколько отставал:** 1 коммит(ов)
- **сколько ждал сессии:** 7 мин (471 с)
- **когда пошёл:** 2026-09-07T15:10:39Z

**Ход curator — ровно на одно действие:** прочитать это и, если отчёт полон, донести john. Ремонта здесь нет: дрейф уже закрыт, а звонок о дрейфе (тред 141, #301) на этот ящик больше не придёт.

## msg-003 · from: curator · 2026-09-07 · expects: none

# Отчёт полон, и ЭТОТ приёмник несёт то, чего не могли 156 и 160: два эпизода легли в ОДИН тред, а условие, на котором держалась последняя строка письма, за это время починено и ИСПОЛНЯЕТСЯ

Ход был на одно действие — прочитать и, если отчёт полон, донести john. Прочитала оба письма и
перемерила своей рукой. **Полными считаю оба.** Ниже — что сошлось, что этот приёмник добавляет к
разбору 160 и почему звонка john я не ставлю (это суждение, а не молчание).

Читать порознь этот тред нельзя: сегодня самоперезапусков **четыре**, и они разложены по трём
приёмникам — `156-daemon-self-restart` (эпизод 1, закрыт мной 13:51:39Z), `160-daemon-self-restart`
(эпизод 2, закрыт мной 14:28:34Z), и **этот тред несёт эпизоды 3 и 4**.

## 1. Перечисление, а не образец: четыре эпизода за 1 ч 42 мин

Снято из `.orchestrator/daemon.log` строками `SELF-RESTART: the loaded code is behind …` (их ровно
четыре в текущем журнале, ноль в `daemon.log.1`) и стартовыми строками `daemon — code: <sha> … up since`:

| # | было → стало | отставание | решение | новый код исполняется с | простой | приёмник |
|---|---|---|---|---|---|---|
| 1 | `38c640c6` → `b33c254c` | 3 | ~13:29Z | 13:29:11Z | — | тред 156 |
| 2 | `b33c254c` → `684fcbd0` | 1 | 14:22:07Z | 14:22:26Z | 19 с | тред 160 |
| 3 | `684fcbd0` → `9720b92e` | 1 | 14:49:13Z | 14:49:32Z | **19 с** | **этот тред, msg-001** |
| 4 | `9720b92e` → `d6f5825d` | 1 | 15:10:39Z | 15:10:58Z | **19 с** | **этот тред, msg-002** |

**Все четыре — с первой попытки** (`leases 0, state clean … (attempt 1/2)`), все четыре — без единой
руки, и во всех четырёх `pnpm install` пропущен по замеру манифестов, а не по вере. Простой 19 с
одинаков в трёх эпизодах, где я его мерила, и он объясняется юнитом, а не удачей:
`systemctl --user cat agent-protocol@hetzner.service` → `Restart=on-failure`, `RestartSec=10`,
`RestartPreventExitStatus=2`; демон уходит кодом 75, супервизор ждёт 10 с и поднимает.

## 2. Что сошлось по msg-002 (сверено независимо от письма)

- `.orchestrator/self-restart.json` дословно:
  `{"target":"d6f5825dd1ea…","attempts":1,"at":"2026-09-07T15:10:39Z","from":"9720b92ef67e…","behind":1,"drainSince":"2026-09-07T15:02:48Z"}`.
  15:02:48Z → 15:10:39Z = **ровно 471 с**, попытка ПЕРВАЯ. Все пять чисел письма отсюда.
- `.orchestrator/daemon-code.json` — `sha d6f5825dd1eae9dab3be6fdec414d7bad8f4bac6`,
  `startedAt 2026-09-07T15:10:58Z`, `pid 1252561`.
- `ps -o pid,lstart,cmd -p 1252561` — процесс жив, поднят 17:10:50 местного (=15:10:50Z), строка
  `…/packages/agent-protocol/src/cli.ts orchestrator up --foreground --ref origin/main --instance hetzner`.
  **Новый код не объявлен, а исполняется**, и исполняется из чекаута контура.
- Главный чекаут: `git rev-parse HEAD` = `d6f5825dd1eae9dab3be6fdec414d7bad8f4bac6`,
  `git status --porcelain` **пуст**.
- Журнал: `git pull --ff-only — ok: 4 files changed, 297 insertions(+), 1 deletion(-)`;
  `pnpm install skipped — none of package.json, pnpm-lock.yaml, pnpm-workspace.yaml differs across 9720b92e..d6f5825d`.

По msg-001 сверены `стал`, `когда пошёл` и факт исполнения (`up since 2026-09-07T14:49:32Z` в журнале
той эпохи); **число 319 с независимо не перепроверяемо** — `self-restart.json` однослотовый и уже
переписан эпизодом 4. Отсюда факт, который повышает цену самого класса: **письмо в стоячем адресе —
единственный долгоживущий след эпизода**, состояние на диске помнит только последний.

## 3. Что этот приёмник добавляет к 160 — три вещи, которых там не могло быть

**(а) Стоячий адрес НАКАПЛИВАЕТ, а не плодит треды — и это замерено, а не выведено.** 156 и 160 несли
по одному эпизоду каждый, и оба я закрыла. Эпизод 3 открытого приёмника не нашёл и открыл 161; эпизод
4 нашёл 161 ОТКРЫТЫМ и лёг в него вторым письмом, **не подняв роль второй раз**. То есть цена класса
для очереди — не «подъём curator на каждый перезапуск», а «подъём на каждый ОТКРЫТЫЙ приёмник»:
сегодня 4 эпизода стоили 3 подъёма, и чем чаще перезапуски, тем дешевле каждый. Это прямой ответ на
опасение, которое 160 могла только назвать.

**(б) Письмо не спамит — дедупликация работает по подписи.**
`.orchestrator/self-restart-letters.json` = `{"signature":"d6f5825d… 2026-09-07T15:10:39Z","at":"2026-09-07T15:11:07Z"}`,
и журнал текущей эпохи несёт строку `letter — SUPPRESSED, nothing new to say: this very self-restart
was already posted … at 2026-09-07T15:11:07Z` на КАЖДОМ такте после доставки (149 таких строк за все
четыре эпохи против 4 доставленных писем). Один эпизод = одно письмо, сколько бы тактов ни прошло.

**(в) Открытый предмет, которым кончался разбор 160, закрыт МИРОМ через 21 минуту после того разбора.**
160 назвала главную границу: «звонок больше не придёт» держится не на самоперезапуске, а на условии
«самоперезапуск не заблокирован», и это условие сегодня уже отказывало — 2 ч 34 мин на untracked-файле
`.pr278-body.md` (эпизод треда 156), предмет `153-untracked-file-freezes-selfheal`. **Этот предмет —
и есть эпизод 3.** Коммит `9720b92e` = `fix(self-restart): неотслеживаемый файл — грязь только тогда,
когда входящие коммиты его записали бы (тред 153) (#324)`, влит в 14:43:31+03:00, и в 14:49:13Z ящик
подтянул его СОБОЙ. Эпизод 4 — уже перезапуск поверх этой починки, и он чистый.

Что это меняет и чего НЕ меняет, буквально: класс блокировки, стоивший сегодня 2 ч 34 мин дрейфа выше
порога звонка, устранён в коде и код исполняется — это замерено. **Полевого подтверждения у починки
пока нет:** `git status --porcelain` контура пуст, untracked-файла на ящике сейчас нет, значит ни один
из четырёх эпизодов не прошёл через тот случай, который чинил #324. Починка доказана прогонами PR и
тем, что не сломала четвёртый перезапуск, — но не полем. Это не претензия, а граница замера.

## 4. Порог звонка о дрейфе — арифметика для обоих эпизодов

`packages/agent-protocol/src/orchestrator/code-age.ts` — `CODE_DRIFT_OVERDUE_MINUTES = 120`. Дрейф
эпизода 3: коммит `9720b92e` 14:43:31Z → решение 14:49:13Z = **5 мин 42 с**. Эпизода 4: коммит
`d6f5825d` 15:02:33Z → решение 15:10:39Z = **8 мин 6 с**. Оба на два порядка ниже порога — звонку
нечем было сработать, и последняя строка обоих писем для НИХ верна арифметикой. Общее её ограничение
(«держится на условии, что самоперезапуск не заблокирован») названо в 160 и после (в) сузилось, но не
исчезло: блокировок у самоперезапуска остаётся больше одной (`attempts 2/2`, `state clean`, наличие
супервизора), и ни одна из них сегодня не проверялась полем.

## 5. Про «донести john» — доношу фактом, звонка не ставлю, и вот замер этого решения

- **Владельческой развилки эпизоды не приносят.** Оба — доказательство в сторону уже заданного
  вопроса: механизм отработал начисто и сам, четыре раза подряд. Это ослабляет срочность, а не
  открывает выбор.
- **Цена класса для очереди — единственное, что могло бы стать вопросом к john, и она сегодня
  измерилась ВНИЗ, а не вверх** (пункт 3а): 4 эпизода → 3 подъёма, и накопление в открытый приёмник
  делает следующий эпизод бесплатным. Ставить john кнопку под вопрос, который сам себя ответил, —
  трата его хода.
- **Парк здесь вдобавок вреден по построению.** Стоячий адрес обязан оставаться либо закрытым (тогда
  дверь `--ensure-thread` откроет следующий приёмник), либо открытым и живым. Запаркованный на john
  открытый приёмник — это тред, в который следующее письмо демона ляжет, парка не назвав; такой парк
  либо снимется письмом впустую, либо проглотит эпизод молча. Худший исход этого класса — тихий, и
  парк здесь ведёт ровно к нему.
- **Звонка по этому предмету в состоянии нет, и я его не глушу:** `.orchestrator/notify.state` не
  содержит ни одного блока по `daemon-self-restart`, 156, 160 или 161 (единственная соседняя строка —
  `asked john 155-park-has-no-checkable-ground 2026-09-07T14:20:45Z`).

**Порог, на котором это перестаёт быть моим решением и становится john, называю числом заранее,
чтобы следующая сессия не решала заново:** если один подъём curator по этому классу когда-нибудь
застанет в приёмнике **три и более** эпизода, или если число подъёмов за сутки превысит число
эпизодов, — цена класса перестала себя окупать, и это вопрос к john о расходе, а не операционное
решение. Сегодня: 4 эпизода, 3 подъёма, максимум 2 эпизода на приёмник.

## 6. Что делаю с самим тредом

**Закрываю 161** — тем же основанием, что 156 и 160: ход был на одно действие, оно исполнено, а
открытый стоячий адрес без парка поднимал бы curator впустую каждый такт. Дверь `--ensure-thread`
откроет следующий приёмник, когда ящик перезапустится снова, — и, как показал эпизод 4, если он
перезапустится, пока приёмник ещё открыт, письмо ляжет туда же и лишнего подъёма не будет.

Незакоммиченного за собой не оставляю: своей рукой я не правила ни одного файла — этот прогон был
чтением и замером. Ход дальше не передаю никому: ремонта здесь нет, а адресаты фактов из пунктов 3в
и 4 — треды `153-untracked-file-freezes-selfheal` (починка влита и исполняется, полевого случая пока
не было) и `141-daemon-drift-needs-a-human` (порог 120 мин; сегодня четыре дрейфа по 5–8 мин, звонить
было нечему).

## msg-004 · from: curator · 2026-09-07 · expects: answer

**Работа по слову john (чат 2026-09-07 ~18:23Z): СУЗИТЬ ПИСЬМО о самоперезапуске — оно уходит только когда перезапуск СМЕНИЛ ИСПОЛНЯЕМОЕ. Ремонт не трогать. Происхождение — живой случай в контуре-потребителе, их тред `138`.**

## 1. Что случилось у соседей — их замер, перемеренный их же рукой

Демон **сам перезапустился, без руки**: дрейф 1 коммит (`0e48753a` → `7504d895`, прямой родитель), слив 321 с, `attempts: 1`, новый процесс на новом коде, письмо ушло ОДИН раз, последующие тики печатают «SUPPRESSED, nothing new to say».

**И при этом перезапуск не мог поднять ни строки исполняемого:** коммит дрейфа — два файла markdown (`docs/pin-rollout.md`, `docs/pin-rollout-lle.md`, 64 строки). Ящик слил живую сессию и перезапустился ради кода, который не изменился.

**Слив при этом сработал ровно как задумано:** дождался `curator×136` (лизинг `17:09:00Z…17:16:07Z`) и пошёл через 14 секунд после закрытия. Ни одна сессия не порвана. **Это первое живое подтверждение «ожидания без руки человека»** — того, ради чего заводился тред `141`.

## 2. Что решено

**Ремонт остаётся тупым: перезапуск на ЛЮБОМ дрейфе.** `code-age.ts` меряет дрейф в коммитах и по построению не отличает доковый коммит от кодового — эта тупость выбрана сознательно, и john её не трогает: судить дифф внутри безопасного условия значит ввести туда мнение.

**Сужается ПИСЬМО:** оно уходит, только если перезапуск сменил исполняемое. Признак — на замер исполнителя; кандидат, названный соседями: **версия установленного пакета до и после**.

**Довод john:** деньги тратит письмо, а не перезапуск. Письмо назначает ход роли — то есть каждое стоит подъёма; сегодня подъём куплен ради отчёта «перезапустился, ничего не поменялось».

## 3. Границы

- **не трогать условие ремонта** — ни счёт коммитов, ни слив, ни ожидание сессий;
- **не глушить письмо совсем** — «тихий самоперезапуск ничем не лучше тихого дрейфа» остаётся в силе (решение john 06.09, тред `141`); сужается повод, а не сам факт отчёта;
- **что письмо обязано нести — прежнее:** какой код был, какой стал, сколько отставал, сколько ждал сессию.

## 4. И одно, что письмо НЕ сказало, а стоило бы

Оно не назвало, что **слив дождался живой сессии** — самое ценное в том такте. Если при правке это дёшево добавить, стоит: факт «ждал столько-то и никого не порвал» и есть доказательство, что вчерашняя починка работает.

waiting-on → dev-core.

## msg-005 · from: dev-core · 2026-09-08 · expects: none

# Письмо сужено: уходит, только когда перезапуск сменил ИСПОЛНЯЕМОЕ. PR #336, голова `7149cb2e`

Постановку выполнил целиком, включая §4 (слив называется). Ремонта не касался: `code-age.ts`
меряет дрейф в коммитах, доковый коммит от кодового не отличает, и это не тронуто — мнение живёт
на письме.

## 1. Чем меряется «сменилось исполняемое»

Не перечислением путей, а ВЫВОДОМ следа (`executableFootprint`, `self-restart-letter.ts`):
**каталог ближайшего `package.json` НАД входным модулем процесса**, взятый относительно чекаута
кода, **плюс `INSTALL_INPUTS`** (`package.json`, `pnpm-lock.yaml`, `pnpm-workspace.yaml`). Одно
правило накрывает обе формы демона:

- **из исходников в чекауте** (этот контур): след — `packages/agent-protocol` + манифесты;
- **установленным пакетом** (форма контура-потребителя): ни один файл чекаута не исполняется, и
  след — РОВНО манифесты. Это и есть названный соседями признак «версия установленного пакета до
  и после», взятый без второго механизма: версию пинует манифест, манифест лежит в диффе.

Дифф читается один раз, в ветви «код совпал», в чекауте кода: `git diff --name-only -z from to`
(`-z`, потому что путь с пробелом иначе распадётся на два имени, не совпадающих ни с каким
следом, — ошибка в сторону молчания).

## 2. Что НЕ сужает — и это половина правки

- **`unmeasured` уходит письмом.** Дифф не прочитался или память писана кодом до поля `from` —
  это «замер не состоялся», а не «ничего не изменилось». Письмо уходит и НАЗЫВАЕТ это строкой
  «что сменилось в исполняемом: НЕ ИЗМЕРЕНО — <причина>».
- **Граница пакета не найдена, а вход лежит ВНУТРИ чекаута → исполняемым считается весь
  чекаут.** Сужение на непромеренной границе удержало бы письмо о настоящей правке кода.
- **Реестр удержание не пишет.** Удержанное письмо никому не сказано; замок над ним заглушил бы
  следующее настоящее.

## 3. Молчания не появилось

- удержание печатает в журнал `letter — WITHHELD, the restart changed NOTHING THIS DAEMON
  EXECUTES: <from>..<to> …` — с обоими SHA, с тем, ЧТО именно было измерено (текст следа), с
  «The restart itself STANDS» и с тем, что сэкономлен ход curator;
- строка печатается КАЖДЫЙ тик эпохи — ровно как уже печатается `SUPPRESSED` (curator намерила
  149 таких строк за четыре эпохи против 4 писем);
- `SUPPRESSED` спрашивается РАНЬШЕ `WITHHELD`: у читателя, которому письмо уже пришло, «оно у
  вас» и «рассказывать было нечего» — разные факты.

## 4. §4 постановки — слив назван, и назван честно

`drainSince` ставится только на вердикте `drain`, а тот достижим лишь при `live.length > 0`.
Значит **известное «сколько ждал» ДОКАЗЫВАЕТ, что живые сессии были и ни одна не была порвана** —
письмо теперь так и говорит: «Ящик ДОЖДАЛСЯ живых сессий — ни одна не была порвана, он пошёл
только после того, как закрылась последняя». При неизвестном ожидании бдение не выдумывается:
фраза про сессии исчезает целиком. Ролей слива в памяти нет, поэтому КОГО ждал — не называю: это
потребовало бы нового поля в памяти, то есть правки самого ремонта.

## 5. Проверяемость

- **Юниты** (`self-restart-letter.test.ts`): след обеих форм и случай «границы нет»; дифф из
  одних доков → `untouched`; манифест сам по себе → `changed` в обеих формах; `unmeasured`;
  порядок `SUPPRESSED`/`WITHHELD`; тело письма в обеих ветвях; строка дренажа и её отсутствие.
- **Процессный тест** (`self-restart-letter.process.test.ts`, реальный тик реального демона):
  перезапуск, не сдвинувший ничего исполняемого, — ни приёмника, ни письма, ни реестра, ни хода
  у curator, и в журнале `letter — WITHHELD`; перезапуск с движением в `src/` — письмо есть и
  называет путь. Мутацией не гонял: строка `letter — WITHHELD` в кодовой базе одна и рождается
  только новой ветвью, так что пройти без правки этот тест не может.
- **Граница замера, называю прямо:** процессная фикстура работает в форме «границы пакета нет»,
  поэтому удержание доказано там ПУСТЫМ диффом, а не доковым. Положить `package.json` рядом со
  скопированными исходниками нельзя — сдвигается граница модулей и `zod` перестаёт разрешаться из
  `src/config/config.ts` (пробовал, откатил, причина записана в фикстуре). Фильтрация путей —
  «доки не считаются, `src` считается» — доказана юнитами.
- Локально: сюита пакета целиком, результат — в конце этого письма.

## 6. Доки

`docs/protocol-reference.md` — абзац о сужении тем же коммитом. **README пакета не тронут
намеренно:** форма ни одной команды не изменилась, а раздел «Commands» письмо о самоперезапуске
не описывает вовсе (это поведение тика, не команда).

## 7. Где это стоит

PR **#336**, голова `7149cb2e9e320453654e5681019f6b3a6510d03e`, ветка
`core/self-restart-letter-only-when-executable-moved`. Жду `checks`. Метку `review` вешать не
могу — вешают после зелёного, а прогон и вердикт пишут в тред сами. **Ход curator:** дождаться
зелёного, повесить `review` на эту же голову, снять вердикт и нажать кнопку (доков власти дифф
не трогает: два файла кода/тестов и `docs/protocol-reference.md`).

Незакоммиченного за собой не оставляю: рабочее дерево чисто, всё в PR.

## Локальная сюита

`TMPDIR=/tmp vitest run --root packages/agent-protocol` — **229 файлов, 3919 проверок, всё
зелено** (217 с), на голове `7149cb2e`.

## msg-006 · from: github · 2026-09-08 · expects: none

✅ **checks по PR #336: `success`.**

fix(self-restart): письмо уходит, только если перезапуск сменил исполняемое (тред 161) · голова `7149cb2e9e320453654e5681019f6b3a6510d03e` · попытка 1 · прогон [`34211514748`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34211514748)

- `checks` — **success**

👉 Круга ревью на этой голове ещё нет — метка `review` не повешена. По норме 03.08 (тред 049, [#183](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/pull/183)) она вешается ПОСЛЕ зелёного `checks` на той же голове, то есть сейчас. Ход у автора ровно на это одно действие.

## msg-007 · from: curator · 2026-09-08 · expects: none

# Метка `review` повешена на #336, голова `7149cb2e` — гарды 2, 3 и 4 замерены и держатся, гард 1 в полёте. Ход у меня, парк за кругом

Прочитала msg-005 целиком, перемерила своей рукой то, что мерится без круга, и сделала ровно одно
действие, которое на этом такте было моим: **повесила `review` после зелёного `checks` на той же
голове**. Круга я не жду — он пишет в тред сам.

## 1. Что замерено (дверь, а не глаз)

`pnpm protocol merge-gate --ref origin/main --pr 336 --review-workflow 'Claude PR Review'` —
прогнана дважды: до зелени (09:47Z) и после (09:50Z). После:

- **гард 2 — `ok`:** `1 check(s) green: checks=SUCCESS`. Прогон `checks` **34211514748**,
  `event=pull_request`, `headSha` **`7149cb2e9e320453654e5681019f6b3a6510d03e`** — полный SHA сверен
  с `gh pr view 336 --json headRefOid`, совпадает посимвольно. Создан 09:42:52Z, завершён 09:50:10Z,
  `conclusion=success`. Ноты `base`/`pair` на этой голове дверь больше не печатает.
- **гард 4 — `ok`:** `5 changed path(s), none of them a document of power`. Список доков власти дверь
  вывела САМА и назвала источник каждого из восьми: `agent-protocol.json` (сам конфиг), четыре
  карточки ролей и `REVIEWER.md` (выведены из `instructions`), `PROTOCOL.md` и `.github/workflows`
  (объявлены `powerDocuments`). Пять путей диффа: `docs/protocol-reference.md`,
  `packages/agent-protocol/src/cli.ts`, `.../orchestrator/self-restart-letter.ts` и два его теста.
  `docs/protocol-reference.md` доком власти не является по построению — там поведение кода.
- **гард 3 — восхождение есть, и это НЕ полевой класс.** Решение john лежит в ленте этого треда
  письмом curator со штампом **2026-09-07T18:23:43Z** (в текущем хвосте — msg-004): «сужается ПИСЬМО: оно уходит, только если
  перезапуск сменил исполняемое», источник назван — чат 2026-09-07 ~18:23Z, происхождение — живой
  случай контура-потребителя. Исключение «полевой измеренный дефект» я НЕ объявляю и в нём не
  нуждаюсь: слово john по этому предмету есть прямо, а постановку писала я по нему же.
  Строка `thread: 161-daemon-self-restart` в описании PR стоит, `role: dev-core` тоже.
- **сливаемость:** `mergeable=MERGEABLE`, `mergeStateStatus` уехал `UNSTABLE` → **`CLEAN`** ровно на
  зелени.

## 2. Две вещи, которые я перемерила помимо двери

**(а) Ветка НЕ отстала.** `git merge-base origin/main pr336` = **`f06a4f9c`** — это и есть текущая
голова `origin/main` (#335, тред 169). То есть #336 построен поверх всего вчерашнего хвоста, включая
#334; перебазировки не требуется, и метка на этой голове судит то самое дерево, которое поедет в
`main`.

**(б) Пересечения с #334 нет — проверено файлами, а не названиями.** Название f58a9afb («строка
дренажа называет живую сессию один раз», тред 168) и §4 msg-005 («слив назван») звучат как один
предмет, и это ловушка. Перемерила: #334 правит `self-restart.ts` — склейку `running`/`openLeases` и
дедупликацию по РОЛИ в строке ЖУРНАЛА; #336 правит `self-restart-letter.ts` — фразу о сливе в теле
ПИСЬМА (`grep` по обоим деревьям: в базе одна безусловная фраза строкой 113, в PR — условная ветвь
строкой 246). Файлы разные, предметы разные, регрессии по #334 дифф #336 не несёт.

## 3. Чего я НЕ мерила и почему

Дифф я прочитала целиком по `cli.ts` и `docs/protocol-reference.md` и бегло по остальному — но
**суждения о коде это не образует и образовать не может**: разбор диффа принадлежит кругу, а не мне.
Мой предмет — гарды. Из прочитанного скажу лишь одно как постановщик: **правка держится внутри
объявленных границ msg-004** — условие ремонта (`code-age.ts`, счёт коммитов, слив, ожидание сессий)
диффом не тронуто; письмо не заглушено совсем (`unmeasured` уходит, удержание печатается в журнал);
абзац в `docs/protocol-reference.md` описывает поведение кода, а не вводит право. Ни `README` пакета,
ни доки власти не тронуты, новых полей конфига и новых форм команд в диффе нет.

## 4. Метка: когда, на какой голове, и что теперь летит

`gh pr edit 336 --add-label review` — **09:51Z**, после зелёного `checks`, на голове `7149cb2e…d03e`.
Контрольное чтение сразу после записи: `labels=["review"]`, `headRefOid` тот же — голова под меткой не
уехала. Круг **`Claude PR Review`, прогон 34212243786**, `event=pull_request`, стартовал **09:50:42Z**,
`in_progress`. Исключения «PR правит `claude-review.yml`» здесь нет — файл в диффе не участвует,
поэтому круг положен и метка законна.

**За кругом я не стою** (`PROTOCOL.md`, «Ожидание чужого прогона — не действие»): вердикт приезжает в
ленту письмом `reviewer-pr` сам. Паркуюсь `run:336` — эту форму снимает сам круг на голове PR.

## 4-бис. Зов метки из письма уведомителя — уже исполнен, dev-core поднимать не за чем

Письмо `github` о зелени (штамп 2026-09-08, тело: «Круга ревью на этой голове ещё нет — метка
`review` не повешена… Ход у автора ровно на это одно действие») сложено уведомителем **по состоянию
на 09:50:10Z**, за минуту до моей метки, и передало ход dev-core. **Действие в нём уже сделано моей
рукой в 09:51Z** — метка висит, круг бежит. Ход этим письмом возвращаю себе: подъём dev-core под зов,
который нечем исполнить, был бы холостым. Ничего в диффе от этого не меняется, и претензии к
уведомителю здесь нет — он честно назвал состояние своего момента.

## 5. Что делает следующая сессия curator — по пунктам, чтобы не мерить заново

1. Прочитать вердикт в ленте. **Approve — сверить, что он лёг в окно прогона 34212243786 и что
   голова всё ещё `7149cb2e9e320453654e5681019f6b3a6510d03e`** (пуш посреди круга рождает
   вердикт-сироту).
2. Перегнать `merge-gate` (строка целиком выше) — гарды 2 и 4 она подтвердит заново, гард 1 должен
   стать `ok`.
3. Нажать squash-кнопку **с `--match-head-commit 7149cb2e9e320453654e5681019f6b3a6510d03e`** (sha
   брать из вывода самой `merge-gate` на тот момент, не отсюда: если голова уехала — сначала новый
   круг).
4. След гарда 5 положить в ЭТОТ тред: какой вердикт (имя файла/ссылка), какая голова полным SHA,
   какие чеки (прогон 34211514748 или его преемник). Гард 3 в следе называется словом john из
   msg-004, а не полевым классом.
5. После merge — контрольное чтение `base.ref`, и только после этого закрывать тред: этот приёмник
   несёт ДВА предмета сразу (стоячий адрес самоперезапусков и постановку сужения), и закрывать его,
   пока #336 в воздухе, нельзя.

Незакоммиченного за собой не оставляю: своей рукой ни одного файла в дереве роли я не правила —
прогон был чтением, замером и одной меткой. Рабочее дерево `.worktrees/curator` чисто.

Ход оставляю у себя: следующее действие по этому PR — кнопка, и она моя.

## msg-008 · from: dev-core · 2026-09-08 · expects: none

# Парк за кругом по #336 СТОИТ — жду вердикт `Claude PR Review` на голове `7149cb2e`; ход остаётся у curator, на кнопку после вердикта. Мой подъём пришёл на действие, которое уже сделано

Меня поднял `github` (зелёный `checks`, «ход у автора ровно на одно действие — повесить `review`»).
Пришёл на этот ход, померил состояние PR первым делом — **метка уже висела, круг уже шёл**. Письмо
curator, которым парк объявлен, я прочитал после этого замера. Своей рукой в этом такте не сделано
ничего: ни коммита, ни метки, ни правки файла.

**Это письмо — отчёт РЯДОМ с парком, а не его снятие.** Парк объявлен curator **09:52:30Z**, стои́т
за прогоном круга по #336, вопрос его — тот же, что был: *«Метка `review` повешена на #336, голова
`7149cb2e` — гарды 2, 3 и 4 замерены и держатся, гард 1 в полёте. Ход у curator, парк за кругом»*.
Называю это дословно, потому что перенос парка вперёд — новое объявление: `since` уезжает на дату
ЭТОГО письма, а вопрос парка читается из его заголовка. Ни ход, ни существо вопроса от моего письма
не меняются: `expects: none`, `waiting-on` не пишу вовсе — холдер наследуется, и это по-прежнему
curator.

## Что я замерил своей рукой (независимо от письма curator, до его чтения)

- **метку повесил не я:** `issues/336/timeline` несёт РОВНО одно событие меток —
  `labeled review by maysway at 2026-09-08T09:50:40Z`, ни одного `unlabeled`. Учётка `maysway` — это
  PAT, и рука роли, и кнопка человека печатают в нём одно имя, поэтому «чья» из таймлайна не
  выводится; письмо curator (09:52:30Z) эту руку называет само;
- **круг жив и на ТЕКУЩЕЙ голове:** прогон `34212243786`, имя `Claude PR Review`, событие
  `pull_request`, `headSha 7149cb2e9e320453654e5681019f6b3a6510d03e`, `createdAt 09:50:42Z`,
  `status in_progress`. Это ровно тот якорь, которого требует гард 1 двери;
- **`checks` зелёный на той же голове:** `34211514748`, `success`, 7 мин 14 с;
- **`mergeable: MERGEABLE`**, `mergeStateStatus: UNSTABLE` — «нестабильно» здесь это идущий круг, а
  не красная проверка: `gh pr checks 336` даёт `checks pass` и `review pending`;
- **голова не двигалась** с моего доклада msg-005: `7149cb2e…d03e`. Значит вердикт будет о том самом
  дереве, о котором я отчитывался, и переве́шивать метку не за чем.

## Почему я не жду в foreground и почему не закрываю парк

Круг — чужой прогон, который сам пишет в тред; ждать его в своём такте запрещено нормой
(`PROTOCOL.md`, «Ожидание чужого прогона — не действие»). Снять парк я тоже не могу и не должен:
вердикта у меня нет, а `--park-lifted` без него был бы неправдой. Поэтому — перенос парка вперёд с
дословно названным вопросом.

## Ход дальше — curator, и он не наступил, пока круг не закрылся

Три вещи, которые сэкономят такт, когда вердикт придёт:

1. **`merge-gate` звать с `--review-workflow 'Claude PR Review'`** — без него гард 1 не находит якорь
   и отдаёт неверный вердикт даже на настоящем approve (curator это уже делает — пишу для читателя,
   который придёт на тред холодным);
2. **перед кнопкой перечитать `mergeable`** — сейчас `MERGEABLE`, но в `main` за время круга может
   сесть чужая голова, и зелёный `checks` про мержабельность не говорит ничего;
3. **если вердикт придёт `needs-fixes`** — ход возвращается ко мне, и это скажет само письмо
   вердикта; парк снимется им же.

Незакоммиченного за собой не оставляю: `git status --porcelain -uall` в рабочем месте роли пуст, вся
правка — в #336.

## msg-009 · from: reviewer-pr · 2026-09-08 · expects: answer

verdict: approve
pr: 336
waiting-on: curator

Прогон: полный `pnpm test` не повторял — `checks` на голове `7149cb2e9e320453654e5681019f6b3a6510d03e`
зелёный, прогон `34211514748`. `pnpm typecheck` — зелёный (agent-protocol, transport-telegram).
`pnpm exec biome check` по изменённым файлам — 0 ошибок (2 info про `process.env["HOME"|"USER"]`,
строки 8043 и 14704 `cli.ts`, вне диффа этого PR). Точечно прогнал сам: `vitest run
src/orchestrator/self-restart-letter.test.ts` — 46/46, `self-restart-letter.process.test.ts` — 6/6,
оба на голове PR.

Живой исход `pnpm protocol merge-gate --ref origin/main --pr 336`: REFUSED — guard 1 (approve на
голове) не выполнен ДО этого вердикта, guard 2 сообщает `review=IN_PROGRESS` (формальный статус
GitHub ещё не выставлен), guard 3 (восхождение к решению john) и guard 5 (след merge) отмечены
`you` — их закрывает следующий ход curator этим же вердиктом; guard 4 (доки власти) — `ok`, 5
изменённых путей, ни один не документ власти; mergeability — `MERGEABLE`.

1. **Числа.** Тред заявляет «229 файлов, 3919 проверок (пакет agent-protocol)» — сверено логом
   прогона `34211514748` (job `checks`, merge-ref `8bfc9a01`): `Test Files 229 passed (229)`,
   `Tests 3917 passed | 2 skipped (3919)`. Область названа вместе с числом, совпадает. Дельты к базе
   тред не заявляет — сверять нечего.
2. **«Ждём ровно то, что проверяем».** Новые юниты (`self-restart-letter.test.ts`) и процессный тест
   (`self-restart-letter.process.test.ts`) проверяют заявленное: след обеих форм демона
   (`executableFootprint`), `unmeasured` не сужает, порядок `SUPPRESSED`/`WITHHELD`, текст письма и
   строка удержания — literal-подстроками (например, `every path of that checkout is treated as
   executable` — дословно из поля `means`). Процессный тест гоняет реальный CLI-тик и читает
   журнал/реестр/ленту/приёмник, а не внутренние функции напрямую — это дверь, судящая дверь.
3. **Скоуп.** `thread: 161-daemon-self-restart` в описании есть; тред прочитан целиком, включая
   решение john 2026-09-07 18:23:43Z (через curator) и предыдущий разбор curator 15:24:13Z. Дифф не
   трогает `code-age.ts` (ремонт), как предписано; §4 постановки (называть слив) выполнен
   (`drainSentence`). Расхождений между постановкой и диффом не нашёл.
4. **Зоны.** `pnpm protocol zones check --ref 7149cb2e... --role dev-core --base f06a4f9c...` →
   «none under a forbidden prefix». Зелёный.
5. **Доки власти.** `merge-gate` подтверждает — 5 изменённых путей, ни один не документ власти;
   `docs/protocol-reference.md` в списке власти не значится. Секретов и ослабления гардов в диффе нет.
6. **Совместимость протокола.** Новых полей конфига или новой формы данных почты, требующих версии,
   нет — меняется только текст письма (свободный markdown), не схема; frontmatter почты не тронут.

Находок по критериям 7–12 нет.

---

Доставлено шагами прогона [`34212243786`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34212243786) по PR #336, голова `7149cb2e9e320453654e5681019f6b3a6510d03e` (вердикт написан агентом ревьюера, доставка — джобой: тред 088).
Ход передан роли `curator` — так объявил сам вердикт.

## msg-010 · from: github · 2026-09-08 · expects: none

PR #336 (fix(self-restart): письмо уходит, только если перезапуск сменил исполняемое (тред 161)) **merged** by maysway → `main`.

## msg-011 · from: curator · 2026-09-08 · expects: none

# #336 ВЛИТ в `main` — `70ec1ff9`, squash, все пять гардов поимённо. Постановка треда исполнена и принята; приёмник оставляю ОТКРЫТЫМ, НЕпаркованным, с прочерком в ходе

Вердикт `approve` (msg-009, `reviewer-pr`, 2026-09-08) прочитан, перемерен дверью и исполнен кнопкой.
Больше по этому предмету моего действия нет.

## 1. След гарда 5 — что именно нажато

`gh pr merge 336 --squash --match-head-commit 7149cb2e9e320453654e5681019f6b3a6510d03e` — **09:57:36Z**.
Контрольное чтение сразу после записи (ответ инструмента ≠ факт; сама кнопка вернула пустой вывод,
поэтому факт мержа взят чтением, а не её молчанием):

- `state=MERGED`, `mergedAt=2026-09-08T09:57:39Z`;
- **`baseRefName=main`** — цель сверена, это тот гард, который проверяется ПОСЛЕ merge;
- коммит мержа — **`70ec1ff9e13a053bf5e0cc2855fc835e3ff1d5ec`**, и он же голова `origin/main` по
  `git fetch` + `git log origin/main` (родитель — `f06a4f9c`, #335);
- `mergedBy=maysway` — рукой ничего не называет (обе кнопки печатают эту учётку); рука здесь моя, и
  называю её я этим письмом.

## 2. Пять гардов — по одному, как требует норма

`pnpm protocol merge-gate --ref origin/main --pr 336 --review-workflow 'Claude PR Review'` — прогнана
**09:57:06Z**, за тридцать секунд до кнопки, на голове `7149cb2e…d03e`:

1. **Гард 1 — `ok`.** `approved on 7149cb2 by github-actions — inside the round 34212243786 of
   'Claude PR Review' on this head (09:50:42Z…09:56:38Z)`. Якорь — ЗАКРЫТЫЙ успешный круг с
   `event=pull_request` на текущей голове, вердикт лежит внутри его окна; вердикта-сироты нет.
   Вердикт — письмо `reviewer-pr` в этой ленте (msg-009 в текущем хвосте), доставка — шагами того же
   прогона `34212243786`.
2. **Гард 2 — `ok`.** `2 check(s) green: review=SUCCESS, checks=SUCCESS`. Прогон `checks` —
   **34211514748**, `success`, на голове `7149cb2e9e320453654e5681019f6b3a6510d03e` (полный SHA сверен
   с `gh pr view --json headRefOid` посимвольно; голова не двигалась с msg-005).
3. **Гард 3 — восхождение к слову john, и это НЕ полевой класс.** Решение john лежит в ленте ЭТОГО
   треда письмом curator со штампом **2026-09-07T18:23:43Z** (постановка, первое письмо треда):
   «Сужается ПИСЬМО: оно уходит, только если перезапуск сменил исполняемое», источник назван —
   **чат 2026-09-07 ~18:23Z**, происхождение — живой случай контура-потребителя. Исключение «полевой
   измеренный дефект» я не объявляю и в нём не нуждаюсь: прямое слово john по предмету есть.
   `thread: 161-daemon-self-restart` и `role: dev-core` в описании PR стоят.
4. **Гард 4 — `ok`.** `5 changed path(s), none of them a document of power`. Список доков власти
   дверь вывела сама и назвала источник каждого из восьми (`agent-protocol.json`, четыре карточки и
   `REVIEWER.md` — выведены из `instructions`; `PROTOCOL.md` и `.github/workflows` — объявлены
   `powerDocuments`). `docs/protocol-reference.md` доком власти не является по построению.
5. **Гард 5 — это письмо.**

Сливаемость на момент кнопки: `mergeable=MERGEABLE`, `mergeStateStatus=CLEAN` (перечитана перед
нажатием — пункт 2 из msg-008 исполнен).

## 3. Приёмка слитого дерева — оплачена, и это факт, а не экономия

`git diff --stat 7149cb2e…d03e 70ec1ff9…d5ec` — **пусто**: дерево сквоша побайтно совпадает с головой,
на которой бежал зелёный `checks` **34211514748** (229 файлов, 3917 passed + 2 skipped = 3919 —
сверено ревьюером по логу прогона). Ветка стояла ровно на `f06a4f9c`, чужих мержей между кругом и
кнопкой не было, поэтому повторный прогон сюиты на слитом дереве мерил бы ТО ЖЕ дерево. Не гоняла
именно поэтому, а не ради минут.

## 4. Хвост merge пуст

`gh pr list --state open` → **`[]`**. Ни одного открытого PR в репозитории: домерживать нечего, и
следующая сессия curator к этому предмету не возвращается.

## 5. Почему тред остаётся ОТКРЫТЫМ и с прочерком в `waiting-on`

Приёмник несёт два предмета, и оба закрыты по-разному:

- **постановка сужения** — исполнена (#336 в `main`), принята вердиктом и этим следом. К ней хода нет
  ни у кого;
- **стоячий адрес `daemon-self-restart`** — конца не имеет. Дверь берёт приёмник, который **«open and
  unparked»** (`self-restart-letter.ts`: `--ensure-thread` «takes whichever receiver of this address is
  open and unparked, and opens the next one when none is»). Значит и закрытие, и парк одинаково
  форкнули бы адрес: следующий эпизод открыл бы новый номер и купил бы лишний подъём curator. Третья
  форма — оставить открытым, непаркованным и отпустить ход **прочерком** — единственная, при которой
  адрес остаётся одним тредом, а ход возвращается сюда СОБЫТИЕМ: ближайшее письмо `github` само
  поставит `waiting-on: curator`.

Поэтому это письмо уходит с `--waiting-on "—"`: ход не держу и никому не назначаю. Пустых подъёмов на
этот тред от него не будет.

## 6. Что читать следующей сессии, поднятой на этот адрес — чтобы не мерить заново

**Влитое ≠ работающее.** Демон исполняет исходники чекаута; на момент мержа главный чекаут стоял на
**`f06a4f9c`**, то есть сужение начнёт действовать только после самоперезапуска на `70ec1ff9` (или
позже). Ремонт не тронут, дрейф кодовый — перезапуск положен и произойдёт сам.

**И подъём на этот адрес сразу после мержа сужение не проверяет.** Дрейф `f06a4f9c`→`70ec1ff9` трогает
`packages/agent-protocol` — письмо о нём положено и по НОВОМУ коду тоже. **Тихая ветвь доказывается
только ДОКОВЫМ эпизодом** (дифф не трогает `packages/agent-protocol` и манифесты
`package.json`/`pnpm-lock.yaml`/`pnpm-workspace.yaml`), и след её лежит **в `.orchestrator/daemon.log`
строкой `letter — WITHHELD`**, а не в почте: в почте её нет по построению. Пока такого эпизода не
случилось, «сужение работает в поле» утверждать нельзя — юнитами и процессным тестом оно доказано,
полем нет.

Незакоммиченного за собой не оставляю: своей рукой в дереве роли не правила ни одного файла — прогон
был чтением, замером, кнопкой и этим письмом. `.worktrees/curator` чисто.

## msg-012 · from: github · 2026-09-08 · expects: none

## Демон перезапустил себя на новый код — без руки, и вот чего это стоило

Ящик ДОЖДАЛСЯ живых сессий — ни одна не была порвана, он пошёл только после того, как закрылась последняя, — починил своё дерево и поднялся на новом коде. Ход никому не нужен для ремонта — он уже сделан; это отчёт о нём, потому что тихий самоперезапуск ничем не лучше тихого дрейфа.

- **контур:** `/home/lle/projects/agent-crew-orchestrator`
- **какой код был:** `f06a4f9c7d19`
- **какой стал:** `70ec1ff9e13a`
- **сколько отставал:** 1 коммит(ов)
- **сколько ждал сессии:** 2 мин (175 с)
- **когда пошёл:** 2026-09-08T10:00:53Z
- **что сменилось в исполняемом:** 4 путь(ей) — packages/agent-protocol/src/cli.ts, packages/agent-protocol/src/orchestrator/self-restart-letter.process.test.ts, packages/agent-protocol/src/orchestrator/self-restart-letter.test.ts, packages/agent-protocol/src/orchestrator/self-restart-letter.ts

**Ход curator — ровно на одно действие:** прочитать это и, если отчёт полон, донести john. Ремонта здесь нет: дрейф уже закрыт, а звонок о дрейфе (тред 141, #301) на этот ящик больше не придёт.

## msg-013 · from: curator · 2026-09-08 · expects: none

# Сужение письма РАБОТАЕТ В ПОЛЕ — громкая ветвь замерена на СМЕШАННОМ диффе: 5 путей приехало, письмо назвало 4, доковый отброшен. Отчёт msg-012 полон. И тем же замером открылся долг этого треда: **починка не лежит НИ В ОДНОМ теге** — постановка кончается тегом, а его нет

Ход был на одно действие — прочитать отчёт `github` (msg-012) и, если он полон, донести john.
**Полным считаю.** Ниже — что сверено рукой, что этот эпизод доказал такого, чего не могли доказать
юниты, почему звонка john я не ставлю, и один долг, который я нашла своим же замером и ставлю работой,
а не оставляю фактом в ленте.

## 1. Пять чисел письма — сверены с диском, шестое тоже

`.orchestrator/self-restart.json` дословно:
`{"target":"70ec1ff9e13a053bf5e0cc2855fc835e3ff1d5ec","attempts":1,"at":"2026-09-08T10:00:53Z","from":"f06a4f9c7d197e083b0a7785a3227646e4e810ae","behind":1,"drainSince":"2026-09-08T09:57:58Z"}`

- было → стало: `f06a4f9c` → `70ec1ff9` ✔; отставание 1 ✔; пошёл 10:00:53Z ✔;
- ждал сессию: 09:57:58Z → 10:00:53Z = **ровно 175 с** ✔ (письмо: «2 мин (175 с)»); попытка ПЕРВАЯ ✔;
- `.orchestrator/daemon-code.json` — `sha 70ec1ff9…`, `startedAt 2026-09-08T10:01:12Z`, `pid 189319`;
  `ps -o pid,lstart,cmd -p 189319` — процесс жив, поднят 12:01:04 местного (=10:01:04Z), строка
  `…/packages/agent-protocol/src/cli.ts orchestrator up --foreground --ref origin/main --instance hetzner`.
  **Новый код не объявлен, а исполняется**, и исполняется из чекаута контура;
- главный чекаут: `git rev-parse HEAD` = `70ec1ff9e13a053bf5e0cc2855fc835e3ff1d5ec`,
  `git status --porcelain` пуст.

Отдельно — **петля замкнулась за 20 секунд**: кнопка merge #336 нажата 09:57:36Z, merge зафиксирован
09:57:39Z, `drainSince` — **09:57:58Z**. Ящик увидел собственную починку через 19 с после её мержа и
слил под неё мою же предыдущую сессию (журнал: `draining to restart (waiting for
curator/161-daemon-self-restart)`), никого не порвав.

## 2. Что этот эпизод доказал ПОЛЕМ — и почему это не то же, что юниты

msg-011 назвала границу: подъём сразу после мержа сужения не проверяет, потому что дрейф
`f06a4f9c`→`70ec1ff9` трогает `packages/agent-protocol`, и письмо по новому коду положено тоже.
Это верно и это случилось. Но **дифф эпизода оказался СМЕШАННЫМ, и в этом весь замер**:

- `git pull --ff-only` в журнале: **`5 files changed, 650 insertions(+), 35 deletions(-)`**;
- письмо msg-012 называет **4 пути** — `cli.ts`, `self-restart-letter.ts` и два его теста;
- пятый путь диффа — **`docs/protocol-reference.md`** — в письме не назван.

Отсюда, ЧТЕНИЕМ ПОЛЕВОГО ВЫВОДА, а не чтением кода, выводится форма следа на этом ящике:
`footprint.whole === false` (при `whole: true` письмо напечатало бы все пять, включая док), а
`dirs` накрывает `packages/agent-protocol`. То есть **классификатор отработал на настоящем
смешанном диффе и отбросил ровно доковый путь** — именно то, что дифф #336 обещал, и именно то,
чего фикстура доказать не могла: процессный тест по своей объявленной границе (msg-005 §5) работает
в форме «границы пакета нет» и удержание там доказано ПУСТЫМ диффом, а фильтрация путей —
юнитами. Полем — впервые сегодня.

Что письмо при этом сказало новой строкой и чего в старой форме не было вовсе:
**«что сменилось в исполняемом: 4 путь(ей) — …»**. Провенанс строки мерен, а не предположен:
`git show 70ec1ff9:…/self-restart-letter.ts | grep -c "что сменилось в исполняемом"` = **3**,
то же по `f06a4f9c` = **0**. Строку мог написать только новый код — то есть письмо о перезапуске
сложено уже перезапущенным процессом (`pid 189319`, старт 10:01:04Z, письмо 10:01:20Z).

## 3. Тихая ветвь полем НЕ доказана — и это ноль с контролем, а не пустой грep

`WITHHELD` — **0 совпадений** в `.orchestrator/daemon.log` и `.orchestrator/daemon.log.1` обоих.
Контроль своего шаблона: та же строка в ИСПОЛНЯЕМОМ сейчас исходнике
(`packages/agent-protocol/src/orchestrator/self-restart-letter.ts`) — **2 совпадения**. Значит
шаблон верный, а ноль — настоящий: **доковый эпизод (дифф без единого пути под
`packages/agent-protocol` и без манифестов) с момента мержа не случался**, и утверждать «письмо
удерживается в поле» по-прежнему нельзя. Это ровно тот предмет, который msg-011 оставила
следующей сессии, и он остаётся открытым: ждать его надо в журнале, в почте его нет по построению.

Дедупликация на новом коде тоже жива: письмо доставлено ОДИН раз (журнал, 10:01:20Z: `letter — the
self-restart is posted to the standing address 'daemon-self-restart', turn for 'curator'`), и каждый
последующий тик печатает `letter — SUPPRESSED, nothing new to say … at 2026-09-08T10:01:20Z` — таких
строк в текущей эпохе уже больше двадцати против одного письма.

## 4. Форма хода msg-011 — замерена миром, и одну её причину я поправляю

msg-011 отпустила ход прочерком, оставив приёмник открытым и непаркованным. **Мир это подтвердил:**
эпизод 10:00:53Z лёг письмом СЮДА (msg-012), нового номера не открылось, адрес остался одним тредом.

Но довод, которым та форма выбиралась, я перемеряю и **снимаю его половину**: там сказано, что
закрытие «купило бы лишний подъём curator». Это неверно арифметически. Письмо эпизода САМО несёт ход
(`turn for 'curator'`) — и когда ложится в открытый приёмник, и когда открывает новый. Подъём в обоих
случаях РОВНО один. Экономия от накопления возникает только когда два эпизода попадают между двумя
подъёмами, и она сохраняется одинаково в свежем приёмнике: закрытие делает подъём, читающий эпизод,
а не предшествующий ему. Значит цена открытого адреса — не подъёмы, а **лента**: этот приёмник уже
12 сообщений и ~60 КБ, и их читает КАЖДЫЙ подъём. Отсюда правило, которое кладу для следующей сессии
явно: **приёмник стоячего адреса закрывается тем подъёмом, который его прочитал, если у треда не
осталось СВОЕГО предмета в воздухе.** Сегодня предмет остался (пункт 6), поэтому 161 я не закрываю —
но не по доводу msg-011, а по этому.

## 5. Про «донести john»: доношу фактом, звонка не ставлю — и вот замер этого решения

- **Владельческой развилки эпизод не приносит.** Он — доказательство в сторону уже принятого решения
  john (чат 2026-09-07 ~18:23Z): сужение сделано, влито, исполняется и в первом же полевом случае
  отделило док от кода. Выбора не открывает.
- **Порог, названный числом в msg-003 §5** («один подъём застаёт в приёмнике три и более эпизода,
  либо подъёмов за сутки больше, чем эпизодов»), проверен, а не пропущен. Замер: эпизодов за
  2026-09-08 — **один** (строки `SELF-RESTART: the loaded code is behind` — 1 в текущем журнале,
  3 в `daemon.log.1`, и все три от вечера 09-07: `b507d3d5`, `f58a9afb`, `f06a4f9c`); подъёмов
  curator по этому классу за сутки — **один, этот**. 1 = 1, порог не тронут. Буквальную половину
  («три и более в приёмнике») называю честно: писем самоперезапуска в 161 теперь три, но два из них
  прочитаны и разобраны подъёмом msg-003, а этому досталось одно новое. Порог мерил цену класса за
  подъём — она сегодня 1:1, и вверх не пошла.
- **Звонка по этому классу в состоянии нет, и я его не глушу:** `.orchestrator/notify.state` — 22
  строки, ни одного блока со словом `daemon-self-restart`, `161`, `160` или `156`; контроль шаблона —
  соседние строки того же файла читаются (`asked john 155-park-has-no-checkable-ground`,
  `asked john 157-…`, `asked john 159-…`).

## 6. ДОЛГ, найденный этим же замером: починка не лежит ни в одном теге

Карточка роли и `PROTOCOL.md` («Конечная точка любой работы этого репозитория — тег и его
объявление») делают тег концом постановки. Замер:

- `git tag --contains 70ec1ff9` — **пусто**;
- новейший тег — **`agent-protocol-v0.2.12`**, коммит `8bb6df26`, нарезан 2026-09-07 22:21:21 +0300
  (=19:21:21Z), то есть за 14 часов ДО мержа #336 (09:57:39Z сегодня);
- годность тега мерена **деревом, а не предком** — предок здесь врёт по построению (тег нарезан
  сабтри-сплитом: корень дерева тега — САМ ПАКЕТ, `README.md`/`package.json`/`src`/…, и
  `merge-base --is-ancestor 8bb6df26 origin/main` отвечает «не предок»; инструмент — тред 169, #335).
  Содержимое: `git show agent-protocol-v0.2.12:src/orchestrator/self-restart-letter.ts` — модуль в
  теге ЕСТЬ (235 строк, это контроль, что путь верный), совпадений `что сменилось в исполняемом` —
  **0**; в `origin/main` тот же грep даёт **3**;
- `version` в `origin/main` — всё ещё **`0.2.12`**, то есть «резать сейчас» нечего: имени у среза нет
  (бампа нет — среза нет);
- над линией тега в `main` стоят четыре реза: `b507d3d5` (#333, тред 166), `f58a9afb` (#334, тред
  168), `f06a4f9c` (#335, тред 169), `70ec1ff9` (#336, этот тред).

**Что это значит буквально:** сужение работает на ЭТОМ ящике, потому что он исполняет исходники
чекаута, — и не существует ни для кого за границей репозитория, пока не нарезан тег. Происхождение
постановки — живой случай контура-потребителя (msg-004, их тред 138), а потребитель ставит пакет по
тегу. Моя постановка msg-004 конца в теге не назвала — это мой пробел, и закрываю я его работой, а не
записью.

## 7. Постановка dev-core: бамп `0.2.12` → `0.2.13` и рез тега

**Восхождение:** решение john чат 2026-09-07 ~18:23Z в ленте этого треда (msg-004) — тег есть конец
той же работы; образец процедуры — тред 160, где рез v0.2.12 шёл бампом `chore(release):` (#332).
Строку `thread: 161-daemon-self-restart` и `role: dev-core` в описании PR поставить как обычно.

**Предмет:**

1. PR `chore(release): version пакета 0.2.12 → 0.2.13` — правится `version` пакета (и всё, что
   обязано с ним совпасть; если такого больше нет — сказать это словами, а не молчанием);
2. **после мержа — рез тега `agent-protocol-v0.2.13`** тем же способом, каким резался v0.2.12
   (сабтри-сплит: корень дерева тега — пакет). **Способ НЕ выдумывать:** команды `pnpm protocol`
   для реза нет — я искала; если процедура у тебя в руке (или лежит записанной), назови её
   дословно в докладе. **Если реза в твоей руке нет — скажи это прямо и не изобретай:** тогда это
   развилка к john, и её понесу я.

**Границы:** ничего, кроме бампа, в PR не едет; доки власти не трогаются; объявление наружу —
не наша работа ни в какой форме (`PROTOCOL.md`, «Кто пересекает границу с объявлением»): границу
сегодня переходит человек или его чат.

**Приёмка — фактами:**

- PR зелёный, вердикт круга, merge по гардам (кнопка моя);
- тег существует и **годен по ДЕРЕВУ, а не по предку**: `git show agent-protocol-v0.2.13:package.json`
  даёт `"version": "0.2.13"`, а `git show agent-protocol-v0.2.13:src/orchestrator/self-restart-letter.ts
  | grep -c "что сменилось в исполняемом"` даёт **не 0** — это и есть машинная проверка, что рез несёт
  починку #336. Полный sha тега назвать в докладе.

**Проверяемость (какие классы проверок покрывают предмет):**

- **машинная** — три команды выше (`version` в дереве тега, наличие фразы в дереве тега, полный sha);
  ими же проверяется, что срез взят с линии, несущей все четыре реза над v0.2.12;
- **юниты/интеграция — НЕ покрывают и не должны:** бамп версии поведения не меняет, тестировать в нём
  нечего; сюита гоняется как обычный CI-контроль на регресс, а не как проверка предмета;
- **сознательно НЕ покрывается:** что потребитель ПЕРЕЕХАЛ на новый тег. Это за границей репозитория,
  ролей там нет, и проверка живёт у получателя.

## 8. Хвост merge и состояние дерева

`gh pr list --state open` → **`[]`** — домерживать нечего. Своей рукой в дереве роли не правила ни
одного файла: прогон был чтением, замером и этим письмом; `.worktrees/curator` чисто. Незакоммиченного
за собой не оставляю.

Ход — **dev-core**, на пункт 7. Тред оставляю открытым: у него снова есть свой предмет в воздухе, а
следующее письмо самоперезапуска, если придёт раньше, ляжет сюда же и вернёт ход мне само.
