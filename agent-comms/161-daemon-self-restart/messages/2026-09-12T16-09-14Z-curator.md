---
from: curator
worker: claude-code
session: 5cdf6c74-74a6-4914-8e74-e2725e71864b
raised: 2026-09-12T16:03:44Z
date: 2026-09-12T16:09:14Z
expects: none
waiting-on: —
---

# Отчёт полон по семи полям, исполняемое СМЕНИЛОСЬ — ветвь та же: доклад без звонка; сверх письма померено, чего оно не несёт — решение об установке и ИМЯ сессии, которую дренаж ждал 22 минуты

Ход пришёл на `msg-052` (новых для меня — `1 of 52`, письмо `github` в 16:02–16:03Z). Развилка john
закрывается в ту же сторону, что в прошлом эпизоде, и закрывается замером, а не строкой письма.

## 1. Развилка john: исполняемое СМЕНИЛОСЬ — мерено диффом и импортами

`git diff --name-only ec166bc6b490..95ba16984221` — **15 путей** (`+783/-23`). Вне пакета ровно один
(`docs/protocol-reference.md`), он отброшен ВЕРНО, и **15 − 1 = 14** — то самое число, которое назвало
письмо. Среди 14 — четыре НЕ теста, и это настоящие исходники:

- `packages/agent-protocol/src/cli.ts` (`+51`) — **сама точка входа поднятого процесса**, доказывать
  её участие импортом не нужно: `ps` печатает её в командной строке ящика;
- `orchestrator/tick.ts` (`+19`) ← `cli.ts:579`;
- `notify/notify.ts` (`+131`) ← `cli.ts:150`;
- `orchestrator/stall.ts` (`+28`) ← `cli.ts:545`.

Остальные десять — `*.test.ts`. **Условие john («письмо на перезапуске БЕЗ смены исполняемого — новый
предмет с новым замером») здесь НЕ срабатывает**, значит ветвь нормальная: доклад в ленту, ни парка,
ни `--expects ack`. Приёмка самоперезапуска подтверждена полем и второй раз не спрашивается — слово
john ~14:32Z 2026-09-08.

## 2. Отчёт полон: семь полей, и вычисляемое сходится точно

```
{"target":"95ba1698422103859e53a0174ce727a9831e9008","attempts":1,"at":"2026-09-12T16:02:35Z",
 "from":"ec166bc6b4906a7221e9080b7881dd0dea159fea","behind":1,"drainSince":"2026-09-12T15:39:46Z","went":true}
```

Поле в поле: `from` = «какой код был», `target` = «какой стал», `behind: 1`, `at` = «когда пошёл
16:02:35Z», `went: true`, `attempts: 1` (журнал: `attempt 1/2`). **«Ждал сессии 22 мин (1369 с)»:
16:02:35Z − 15:39:46Z = 1369 с ровно**, округления в свою пользу нет. **Цепь не рвётся:** `from`
этого письма = `target` предыдущего (`msg-051` ← `msg-050`, `ec166bc6b490`).

**Ящик исполняет новый код — мерено процессом, а не деревом:** `daemon-code.json` — `sha
95ba16984221…`, `pid 3165139`, `startedAt 16:02:55Z`; `ps -o pid,ppid,lstart,etime,cmd` — процесс ЖИВ,
поднят **16:02:46Z** (11 с после решения — как в прежних эпизодах), `PPID 1019` (`systemd --user`),
точка входа `…/packages/agent-protocol/src/cli.ts orchestrator up --foreground --ref origin/main
--instance hetzner`, то есть исходники контура. **Дрейфа сейчас нет, и мерено с двух сторон:**
`git rev-parse origin/main` = `95ba16984221…` = исполняемый sha, и `git ls-remote origin
refs/heads/main` даёт тот же sha — отставания нет ни от локальной ссылки, ни от пульта.

## 3. Откуда взялся дрейф и почему ожидание вышло 22 минуты, а не 3

Дрейф завела **не моя кнопка** (в прошлом эпизоде — моя): `#372` («молчаливый простой контура звонит
телефоном», тред `180-selfheal-leaves-the-workspaces-behind`), `mergeCommit 95ba16984221…`, `mergedAt
15:39:44Z`. Руку `mergedBy` не называет (печатает `maysway` у обеих кнопок), и для этого письма она и
не нужна. Журнал ящика видит дрейф в `15:39:43Z` (это дата коммита), `drainSince` — `15:39:46Z`,
**+3 с**: штамп записи, как и прежде, чуть позже штампа обнаружения.

**«Ни одна сессия не была порвана» — замер, а не вежливость.** Журнал ящика все 22 минуты печатает
одно и то же, и живую сессию он называет ПОИМЁННО:

```
daemon — the code is 1 commit(s) behind, drifting for 19m … — DRAINING TO RESTART: … it launches
NOTHING new and waits for the sessions that are live (dev-core/180-selfheal-leaves-the-workspaces-behind)
to finish by themselves … No session is interrupted …
daemon — SELF-RESTART: this tick launches nothing — the box is draining to restart … and there was
nothing to withhold
```

Та сессия дописала своё последнее письмо в **16:01:15Z**, и дренаж отпустило в **16:02:35Z** — 80 с
спустя, на такте, где лизов стало ноль (`leases 0, state clean`). Дальше — `restart --pull` рукой
ящика, `INVOCATION_ID` установлен, поэтому ремонт **на месте** и выход кодом **75** для супервизора
(и журнал прямо говорит, почему не `0`: «an exit that failed to be replaced must not look like a
clean shutdown»).

**Письмо легло РОВНО одно:** замок `self-restart-letters.json` — подпись `95ba16984221…\0
2026-09-12T16:02:35Z`, `at 16:03:07Z`; последующие такты печатают только `letter — SUPPRESSED,
nothing new to say … already posted … at 2026-09-12T16:03:07Z`. Двоения нет.

## 4. Сверх письма: чего оно не несёт — и почему требовать этого от него нельзя без john

Поле называется «отпечаток **установки**», но **самого решения об установке в письме нет**. Строка
есть только в журнале ящика:

```
SELF-RESTART: pnpm install skipped — none of package.json, pnpm-lock.yaml, pnpm-workspace.yaml
differs across ec166bc6..95ba1698 … so what is installed already matches what the tree declares
```

Там же, и только там, лежат ещё два факта: **кого** дренаж ждал поимённо и что `there was nothing to
withhold` — письмо даёт лишь длительность. Вывод для читателя такой почты, и он не претензия к
письму: **«отчёт полон» судится по его семи полям, а «ни одна сессия не порвана» и «установка»
мерятся журналом ящика**; требовать этих полей у письма — это новая ФОРМА сообщения, то есть норма и
кнопка john, а не починка. Отсюда я ничего такого не ставлю. Перенесено это единственным
инструментом, который читает каждый подъём роли: заметка `self-restart-letter-names-the-footprint`
получила «Следствие» с обеими строками журнала. **Цена по потолку памяти: +0 б** — правлено только
тело заметки, крючок в оглавлении не тронут (`MEMORY.md` до и после — `29 912` б).

## 5. Чего этим ходом НЕ сделано — поимённо

- **тред не закрываю и не паркую.** Приёмники стоячего адреса перечислены по ленте почты только что
  (`git ls-tree origin/comms:` по маске `*-daemon-self-restart` + `status:` из `_meta.md`) — их **12**,
  открытых **два**: `176` и `161`. `176` запаркован на решении john, и это мерено journal'ом ЭТОГО
  такта: `queue: curator×176-daemon-self-restart … ⏸ PARKED behind a decision of john (R27)`,
  `waiting since 2026-09-09T19:50:25Z`. Значит **`161` по-прежнему единственный открытый и
  незапаркованный приёмник** — парк или закрытие здесь форкает адрес;
- **хвост готовых PR не мету.** Дешёвая половина померена (`gh pr list --state open`, 16:07Z):
  открыто **пять** — `#374` (тред 180, метки нет), `#373` (180, `review`), `#371` (188, `review`),
  `#370` (187, `review`), `#353` (180, `review`, открыт с `2026-09-09T14:05Z` — свыше трёх суток). **Ни один не
  принадлежит треду `161`**, а след гарда 5 обязан лечь в ленту треда-владельца — прогон же связан
  ровно `161`. Автор у всех пяти — `maysway`, то есть учётка, а не рука: владельца называют ветка и
  номер треда, и они указывают в `180`/`187`/`188`;
- **потолок памяти роли не починен и вырос:** `MEMORY.md` = **29 912 б против 24 576 (121,7 %)**,
  **146 заметок** (три такта назад было `29 016` и `141`). Прирост +896 б за трое суток — чужими
  подъёмами роли. Вывод прошлого такта не изменился (оба механических сита пусты, класс «уехало в
  карточку» опровергнут перечислением), а развилка — `memoryIndexAlarmFor` в конфиге, то есть кнопка
  john, и **отсюда её по-прежнему не поставить**: парк последнего незапаркованного приёмника форкает
  адрес, а нового треда этот прогон не заводит.

## 6. Куда идёт ход

**Никому** (`waiting-on: —`). Это измеренное «незачем»: семь полей сошлись, исполняемое сменилось,
условие john не сработало, ремонт уже сделан ящиком, звонка отсюда нет. Следующее письмо в эту ленту
напишет сам ящик, когда снова сядет на новый код, — `161` открыт и незапаркован специально ради этого.

**Незакоммиченного за собой не оставляю:** ни одного файла репозитория этим ходом не правила —
читала ленту, мерила дифф, импорты, запись, процесс, оба журнала ящика, `_meta.md` всех 12 приёмников
и открытые PR. Правлен один файл ПАМЯТИ РОЛИ (`.orchestrator/memory/curator/`,
`self-restart-letter-names-the-footprint.md`, только тело) — каталог `.orchestrator/` в `.gitignore`,
на ветку почты его синхронизирует сам демон концом хода; перечитан с диска после записи.
`git status --porcelain -uall` пуст и в главном чекауте, и в дереве пары
(`.worktrees/curator@161-daemon-self-restart`). Тело письма — в своём `mktemp -d -p /tmp`, вне
чекаута почты и вне дерева роли.

