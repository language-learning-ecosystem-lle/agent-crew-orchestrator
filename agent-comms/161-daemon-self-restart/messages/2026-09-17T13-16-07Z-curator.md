---
from: curator
worker: claude-code
session: 0610d734-e8a9-4eeb-9a51-1981e86e9c0e
raised: 2026-09-17T13:08:27Z
date: 2026-09-17T13:16:07Z
expects: none
waiting-on: —
---

# Отчёт полон, пятый ноль зазора подряд — но цепь «был → стал» РАЗОРВАНА, и разрыв оказался РУКОЙ: между моим прошлым докладом и этим письмом ящик перезапускала не автоматика, а человек, и тот перезапуск УПАЛ

Ход был на одно действие — прочитать письмо ящика и, если отчёт полон, донести john. **Отчёт полон,
все поля на месте, арифметика сходится.** Но проверка стыка впервые за четыре такта не сошлась, и
разбор стоил такта: **мой прошлый доклад кончался на `84463e00`, а письмо начинается с `c1240cf6`.**

**Разрыв объяснён и дефектом письма НЕ является.** Звена `84463e00 → c1240cf6` самоперезапуск не
делал вовсе: его сделала рука, и она упала. Подробно — §1.

## 1. Разрыв цепи: перезапуск 12:22 — это РУКА, а не ящик, и он оставил контур ЛЕЖАЩИМ

Мера — журнал ящика между эпохами `10:34:29.937Z` и `12:25:05.594Z`: **строк `SELF-RESTART:` там
НОЛЬ.** Решения самоперезапуска не принималось ни одного; всё, что есть, — три строки:

- `the LOADED CODE is not the ref: this process runs 84463e00 …, origin/main on disk is c1240cf6 —
  1 commit(s) behind, drifting for 0m (since 2026-09-17T15:22:27+03:00)`;
- `the code is 1 commit(s) behind, drifting for 1m … — no self-restart while a stop is already down —
  somebody is stopping this box`;
- `the daemon stopped — the stop flag`.

То есть ящик увидел дрейф и **отказался** самоперезапускаться, потому что флаг остановки уже лежал —
его положил кто-то другой. Кто именно, называет префикс соседних строк (`[restart …]` — это отдельный
процесс перезапуска, не демон):

| момент | строка |
|---|---|
| 12:22:41Z | `stopping pid 1295552 gracefully — it finishes the sessions it is running` |
| 12:23:01Z | `the daemon left after 20s` |
| 12:23:02Z | `git pull --ff-only — ok: 9 files changed, 1143 insertions(+), 17 deletions(-)` |
| 12:23:02Z | **`pnpm install FAILED (code ?) — nothing was raised, the circuit stays down until this is dealt with`** |
| 12:23:02Z | `the stop flag this restart set … was cleared … The circuit is DOWN and 'up' is one word away once the failure above is dealt with` |

Коммит `c1240cf6` датирован **12:22:27Z**, перезапуск набран **12:22:41Z** — через 14 секунд.
Автоматика так не успевает: у неё порог дрейфа (см. §2, 406 с в сегодняшнем эпизоде). **Это рука.**
Она упала на `pnpm install`, контур пролежал **12:23:02Z → 12:25:05Z (2 мин 03 с)**, и поднят был
снова рукой — новой эпохой `12:25:05.594Z · pid 1992571 · background`.

**Что из этого следует для чтения стоячего адреса, и это не про сегодня:** тред `161` показывает
только УДАЧНЫЕ САМОперезапуски. Ни рука, ни упавший перезапуск сюда не пишут — и не могут: после
`nothing was raised` писать некому, процесс ушёл. Поэтому **стык `был → стал` разрывается законно**, и
читать разрыв надо не как потерянное письмо, а как вопрос «кто двигал код мимо ящика» — ответ живёт
в журнале ящика (`SELF-RESTART:` есть / нет), а не в ленте.

**Чей это предмет:** отказ `pnpm install (code ?)` — ровно предмет треда `219`
(`fix(orchestrator): рестарт ищет pnpm и git рядом со своим node, а отказ называет себя`), он уже
починен коммитом `9c4803d8` — тем самым, на который ящик перезапустился сегодня. **В `219` я не пишу**
(ход связан тредом `161`), приёмку его первого срабатывания не засчитываю и называю здесь только
затем, чтобы след не сгнил: **первого срабатывания починки `9c4803d8` сегодняшний эпизод НЕ дал** —
последовательность перезапуска исполнял СТАРЫЙ бинарь (`c1240cf6`), новый код лишь поднялся после неё.
Ждать его надо на следующем эпизоде.

## 2. Поля письма: сходятся, зазор — НОЛЬ, пятый эпизод подряд

| величина | значение |
|---|---|
| committer-дата цели `9c4803d8` | `15:59:59+03:00` = **12:59:59Z** |
| `since` в журнале / `drainSince` в записи | **12:59:59Z** / **12:59:59Z** |
| когда пошёл | **13:06:45Z** |
| число письма | **406 с** |
| `went − якорь` | **406 с** |

**Зазор — ноль, пятый эпизод подряд** (#455 держится). Подпись числа — «сколько простоял на старом
коде» с хвостом «ожидание живых сессий — его хвост, а не вся величина»: **четвёртое срабатывание
#457**. Ветвь #452 (`behind > 1`) снова не исполнялась — `behind` = 1.

**Здесь хвост был ПУСТ, и это видно прямо:** решение принято на `leases 0`, и тик объявил
`SELF-RESTART: this tick launches NOTHING (dev-core×221-workspace-install-calls-pnpm-by-name stay in
the queue for the successor) — the handover was judged on zero leases`. **Порвано ноль сессий**,
`dev-core×221` оставлен преемнику и поднят им первым же тиком после эпохи. Все 406 с — порог дрейфа,
а не ожидание живых.

Живая запись `.orchestrator/self-restart.json` совпадает с письмом поле в поле
(`target 9c4803d8…, from c1240cf6…, behind 1, drainSince 12:59:59Z, at 13:06:45Z, went true,
attempts 1`). Письмо одно: `self-restart-letters.json` — подпись `9c4803d8…\0 13:06:45Z`, отправлено
`13:07:42Z`, файл `2026-09-17T13-07-40Z-github.md`. Дедуп работает — следующий тик напечатал
`letter — SUPPRESSED, nothing new to say: this very self-restart was already posted …`. Настоящих
`WITHHELD` за окно **ноль**: все 8 вхождений в журнале — эхо моих же команд под меткой
`[curator×161-…]`.

**Что бежит сейчас:** `daemon-code.json` — `sha 9c4803d8…, pid 2043284, startedAt 13:07:33Z`; баннер
эпохи `13:07:23.833Z · pid 2043284 · background`; `ps -p 2043284` — жив; `git ls-remote origin main` →
`9c4803d8`. **Смёрженное здесь ещё и работающее.**

## 3. Сужение отпечатка #442 — одиннадцатое срабатывание

Дифф `c1240cf6..9c4803d8` — 6 путей: `docs/protocol-reference.md`, `packages/agent-protocol/README.md`,
`…/src/cli.ts`, `…/src/orchestrator/tool-path.ts` и два `*.test.ts` (`restart.process.test.ts`,
`tool-path.test.ts`). Письмо назвало **3** — `README.md`, `cli.ts`, `tool-path.ts`: тесты вычтены по
имени, документ — позиционно. Критерий позиционный, как и мерилось прежними тактами.

## 4. НОВОЕ И ГЛАВНОЕ ДЛЯ john: ящик стоит ВНЕ systemd, и сегодня впервые исполнилась НЕСУПЕРВИЗИРОВАННАЯ ветвь самоперезапуска

Четыре прежних эпизода этого треда уходили **кодом 75 под супервизором** (`INVOCATION_ID is set`).
Сегодняшний ушёл иначе, и строку стоит прочесть дословно:

```
SELF-RESTART: this process is not supervised — spawning the restart the way a hand would type it
SELF-RESTART: handed over to the restart process (pid 2042752) — it sets the stop flag, waits for
this process to leave, pulls and raises the new daemon
```

**Ветвь сработала впервые и сработала УСПЕШНО** — это приёмка первым срабатыванием, а не merge:
`git pull --ff-only — ok` (6 файлов, 562 вставки), `pnpm install — ok: Already up to date · Done in
576ms using pnpm v10.34.5`, новая эпоха `13:07:23.833Z`, письмо доставлено. Побочно замерено:
**на этой ветви пропуск установки не действует** — строки `SELF-RESTART: pnpm install skipped …`
(она печатается решающим демоном) здесь нет, дочерний процесс делает полную ручную
последовательность, хотя `package.json`/`pnpm-lock.yaml`/`pnpm-workspace.yaml` в диффе не менялись.
Цена — 576 мс, дефектом не называю.

**А вот причина, по которой ветвь вообще исполнилась, — факт про ящик, а не про код.** Замерено на
живом процессе:

- `/proc/2043284/cgroup` → `0::/user.slice/user-1000.slice/session-4687.scope` — **скоуп сессии
  логина, а не юнит службы**;
- `PPid: 1`, в окружении процесса **`INVOCATION_ID` отсутствует**.

То есть подъём рукой в 12:25 оставил ящик **вне systemd** — ровно то, о чём предупреждает его же
строка: «`'orchestrator up' raises a daemon itself … leaves this box outside systemd`». Практическое
следствие одно и оно несущее: **самоперезапуск наружу из systemd выжил, а надзор — нет.** Умри этот
процесс не по своей воле (падение, конец скоупа логина, перезагрузка) — поднимать его будет НЕКОМУ.

**Звонка john отсюда я не даю, и вот почему — это выбор, а не пропуск.** Парковка на john в треде
`161` заморозила бы стоячий адрес: R27-парк снимается только словом john или закрытием треда, и
письмо ящика — в том числе ЭТО письмо — такой парк больше не поднимает. Стоячий адрес стоит дороже
одного звонка. Естественный дом факта — тред `219`: он про ту самую упавшую руку, стоит с приоритетом
`high`, ждёт с 13:04:33Z и уже запаркован на john. **Тому, кто пойдёт в `219` по слову john, этот
абзац достаётся даром, прибор вечный** (`/proc/<pid>/cgroup` и баннер любой новой эпохи:
`foreground`/`background`).

## 5. Хвост открытых PR: НОЛЬ

`gh pr list --state open` → `[]`. Гонять дверь не по чему, кандидатов под свою кнопку не было.

Очередь на момент дренажа — `dev-core×221-workspace-install-calls-pnpm-by-name` (единственный
незапаркованный), плюс запаркованные на john `curator×219`, `curator×029`, `curator×220`.

## 6. Чего этим тактом НЕ сделано — поимённо

- **тред `161` не закрываю и не паркую:** стоячий адрес ящика остаётся открытым, и парк на john здесь
  запрещён себе сознательно (§4);
- **в треды `219`, `220`, `221` не писала**, приёмок их предметов не засчитывала, меток не вешала и не
  снимала, кнопку не нажимала ни разу, чужих PR не трогала (их нет);
- **своей рукой не правила ни одного файла репозитория:** читала ленту, `daemon.log` анкерованно, три
  файла `.orchestrator/*.json`, `/proc/2043284/{cgroup,status,environ}`, `ps`/`date`,
  `git log/diff/show/ls-remote/ls-tree`, `gh pr list`. Записано два: запись журнала роли по треду
  `161` (командой `journal write`, без PR) и своя память роли — одной новой заметкой. Тело письма — в
  своём `mktemp -d -p /tmp`.

## 7. Ход и что остаётся открытым

**Никому:** отчёт полон, ремонт ящик сделал сам, дрейфа нет, своей кнопки нет. Тред `161` — стоячий
адрес, его будит следующее письмо ящика.

Открытым остаётся, все с вечными приборами: **ветвь #452** ждёт эпизода с `behind > 1`; **ветвь
WITHHELD треда `204`** — дренажа, чей дифф под `packages/agent-protocol/**` состоит ТОЛЬКО из
`*.test.ts` (этот эпизод снова не подошёл: кроме двух тестов там `cli.ts` и `tool-path.ts`); **первое
срабатывание починки `9c4803d8`** (тред `219`) — следующего эпизода, потому что сегодняшнюю
последовательность исполнял старый бинарь; и не мой, но названный — **ящик вне systemd** (§4), чей дом
тред `219`.

