---
from: curator
worker: claude-code
session: d8eca365-8883-42fc-9798-6d0bfe344cbf
raised: 2026-09-12T16:38:49Z
date: 2026-09-12T16:42:17Z
expects: none
waiting-on: —
---

# Второй эпизод за 35 минут: отчёт полон по семи полям, исполняемое СМЕНИЛОСЬ — ветвь та же, звонка отсюда нет; сверх письма померено, ЧТО стоил слив очереди и ЧЬЯ сессия его держала

Ход пришёл на `msg-054` (новых для меня — `1 of 54`, письмо `github` в 16:37–16:38Z). Это второй
самоперезапуск подряд в одном такте часа: предыдущий (`95ba1698`) разобран в моём `msg-053` в
16:09Z, этот ушёл на `bbab2d9a`. Развилка john закрывается замером в ту же сторону.

## 1. Развилка john: исполняемое СМЕНИЛОСЬ — мерено диффом и точкой входа

`git diff --name-only 95ba16984221..bbab2d9a2673` — **5 путей** (`+712/−11`). Вне пакета ровно один
(`docs/protocol-reference.md`, `+43/−2`), отброшен ВЕРНО, и **5 − 1 = 4** — то самое число, которое
назвало письмо; имена совпадают пофайлово. Среди четырёх — два НЕ теста:

- `packages/agent-protocol/src/cli.ts` (`+143/−9`) — **сама точка входа поднятого процесса**, и
  доказывать её участие импортом не нужно: `ps` печатает её в командной строке ящика;
- `packages/agent-protocol/src/orchestrator/workspace-install.ts` (`+140`, новый файл) ←
  `cli.ts:657`.

Остальные два — `workspace-install.test.ts`, `workspace-levelling.process.test.ts`. **Условие john
(«письмо на перезапуске БЕЗ смены исполняемого — новый предмет с новым замером») не срабатывает**,
значит ветвь нормальная: доклад в ленту, ни парка, ни `--expects ack`. Приёмка самоперезапуска
подтверждена полем и второй раз не спрашивается — слово john ~14:32Z 2026-09-08.

## 2. Отчёт полон: семь полей, вычисляемое сходится точно, цепь не рвётся

```
{"target":"bbab2d9a2673b937bbcdac6518576b0f6d414b91","attempts":1,"at":"2026-09-12T16:37:42Z",
 "from":"95ba1698422103859e53a0174ce727a9831e9008","behind":1,"drainSince":"2026-09-12T16:35:56Z","went":true}
```

Поле в поле: `from` = «какой код был», `target` = «какой стал», `behind: 1`, `at` = «когда пошёл
16:37:42Z», `went: true`, `attempts: 1` (журнал: `attempt 1/2`). **«Ждал сессии 1 мин (106 с)»:
16:37:42Z − 16:35:56Z = 106 с ровно.** **Цепь не рвётся:** `from` этого письма = `target`
предыдущего (`msg-054` ← `msg-053`, `95ba16984221`).

**Ящик исполняет новый код — мерено процессом, а не деревом:** `daemon-code.json` — `sha
bbab2d9a2673…`, `pid 3197522`, `startedAt 16:38:02Z`; `ps -o pid,ppid,lstart,etime,cmd -p 3197522` —
процесс ЖИВ, поднят **16:37:53Z** (11 с после решения, как в двух прежних эпизодах), `PPID 1019`
(`systemd --user`), точка входа `…/packages/agent-protocol/src/cli.ts orchestrator up --foreground
--ref origin/main --instance hetzner`. **Дрейфа сейчас нет, мерено с двух сторон:** `git rev-parse
origin/main` = `bbab2d9a2673…` = исполняемый sha, и `git ls-remote origin refs/heads/main` даёт тот
же sha. **Письмо легло РОВНО одно:** `self-restart-letters.json` — подпись `bbab2d9a2673…\0
2026-09-12T16:37:42Z`, `at 16:38:13Z`; дальше журнал печатает только `letter — SUPPRESSED, nothing
new to say … already posted … at 2026-09-12T16:38:13Z`.

## 3. Откуда дрейф и чья сессия держала слив — здесь эпизод отличается от прошлого

Дрейф завёл **#373** («ящик выравнивает рабочие места, которые ВЫДАЛ, в трёх границах john», тред
`180`), `mergeCommit bbab2d9a2673…`, `mergedAt 16:35:18Z`. Руку `mergedBy` не называет (у обеих
кнопок печатает `maysway`), но живую сессию журнал ящика называет ПОИМЁННО, и это **сессия МОЕЙ
роли**:

```
… DRAINING TO RESTART: … waits for the sessions that are live
(curator/180-selfheal-leaves-the-workspaces-behind) to finish by themselves …
```

То есть слив держала та же пара, которой принадлежит смёрженный PR, — прежний замер «слив ждёт
ИМЕННО эту сессию» подтверждён вторым случаем. Цифры этого эпизода: merge `16:35:18Z` →
`drainSince 16:35:56Z` (**+38 с**, а не 23 с прошлого замера — это пример, не константа) → ход той
сессии кончился `16:36:44Z` (её письмо запарковало тред `180` за кругом #374) → ящик пошёл
`16:37:42Z` → преемник `16:37:53Z`. **Весь цикл кнопка→новый процесс — 155 с**, против 22 минут у
эпизода #372 часом раньше: разброс задаёт длина живой сессии, а не ящик.

**Слив в этот раз НЕ был бесплатным для очереди, и это новое против прошлого эпизода.** Первый такт
напечатал не «нечего придерживать», а:

```
SELF-RESTART: this tick launches NOTHING (dev-core×180-selfheal-leaves-the-workspaces-behind stay in
the queue) — … a session started now would extend the wait by its whole length; the pairs are not
consumed and the tick that repairs, or any tick after the drain ends, reads them again
```

Ветка эта **старая** (`self-restart.ts:746`, пришла с `#297`, тред `141`) — новой нормы не появилось,
просто впервые за эти эпизоды нашлось что придерживать. Обещание «прочтёт снова» проверено:
после перезапуска очередь `12/12` — `dev-core×180` в ней НЕТ, потому что за 48 с до ремонта ход
треда `180` ушёл к curator и встал за кругом на `#374`. Пара не потерялась — она перестала быть
кандидатом по ленте, а не по сливу. Установка пропущена законно: `pnpm install skipped — none of
package.json, pnpm-lock.yaml, pnpm-workspace.yaml differs across 95ba1698..bbab2d9a`. Выход — код
`75` для супервизора, `INVOCATION_ID` установлен, ремонт на месте.

## 4. Чего этим ходом НЕ сделано — поимённо

- **тред не закрываю и не паркую:** `161` по-прежнему единственный открытый и незапаркованный
  приёмник стоячего адреса (`176` — `⏸ PARKED behind a decision of john (R27)`, `waiting since
  2026-09-09T19:50:25Z`, мерено очередью ЭТОГО такта). Парк или закрытие здесь форкает адрес;
- **хвост готовых PR не мержу.** Открыто четыре: `#374` (тред 180, `review`, круг на голове идёт),
  `#371` (188, `review`), `#370` (187, `review`), `#353` (180, `review`, открыт с
  `2026-09-09T14:05Z`). У `#371` и `#370` ящик сам печатает `guards 1-2 hold`, но оба треда стоят
  `⏸ PARKED behind a decision of john`, то есть гард 3/4 у человека; **ни один PR не принадлежит
  треду `161`**, а след гарда 5 обязан лечь в ленту треда-владельца — прогон же связан ровно `161`;
- **потолок памяти роли не починен:** `MEMORY.md` = **29 929 б против 24 576 (121,8 %)**, **146
  заметок** (в 16:09Z было `29 912`; +17 б за полчаса — чужим подъёмом роли). Развилка прежняя —
  `memoryIndexAlarmFor` в конфиге, кнопка john, и отсюда её не поставить: парк последнего
  незапаркованного приёмника форкает адрес, нового треда этот прогон не заводит.

## 5. Куда идёт ход

**Никому** (`waiting-on: —`). Измеренное «незачем»: семь полей сошлись, исполняемое сменилось,
условие john не сработало, ремонт сделан ящиком, звонка отсюда нет. Следующее письмо в эту ленту
напишет сам ящик.

**Незакоммиченного за собой не оставляю:** ни одного файла репозитория этим ходом не правила —
читала ленту, мерила дифф, импорт, запись, процесс, оба журнала ящика, очередь и открытые PR.
Правлен один файл ПАМЯТИ РОЛИ (`.orchestrator/memory/curator/own-merge-starts-a-drain-that-waits-for-me.md`,
только тело: второй случай с цифрами этого эпизода) — каталог `.orchestrator/` в `.gitignore`,
на ветку почты его синхронизирует сам демон концом хода. **Цена по потолку памяти: +0 б** — крючок в
оглавлении не тронут (`MEMORY.md` до и после — `29 929` б). `git status --porcelain -uall` пуст и в
главном чекауте, и в дереве пары. Тело письма — в своём `mktemp -d -p /tmp`, вне чекаута почты и вне
дерева роли.

