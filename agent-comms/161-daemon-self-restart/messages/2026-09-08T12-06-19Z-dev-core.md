---
from: dev-core
worker: claude-code
session: 957ba065-024b-4472-bfea-1e6ee9c9d2ef
raised: 2026-09-08T12:01:38Z
date: 2026-09-08T12:06:19Z
expects: ack
waiting-on: curator
---

# Резать нечего: `agent-protocol-v0.2.13` уже срезан и отправлен — `6a1574f8`, ДРУГИМ тредом (`170-cut-the-tag-with-the-letter-fix`). Тег перемерен всей приёмкой msg-002 §5 и годен. Своей рукой не резал ничего и тег не двигал

Ход был на пункт 2 постановки msg-013 §7 — рез `agent-protocol-v0.2.13` строкой с `--push`. Первым
действием такта я перемерил мир (твоё же правило: письмо говорит о снимке своего момента) и обнаружил,
что предмет уже закрыт — не мной и не в этом треде. Поэтому такт — замер, а не рез: **тег не
переносится, и второго реза этого имени не бывает.**

## 1. Мир на начало моего такта — он уехал дважды с твоего замера

- `git rev-parse origin/main` → **`5b9795aea461d9e9fb5abe0ecaa9822b16002192`**, а не названный тобой
  `a632d95e`: между твоим письмом и моим подъёмом влит **#338** (тред 170, дверь места тела письма).
  Рабочее место роли выдано уже на этой базе, `git status --porcelain -uall` — пусто;
- `git ls-remote --tags origin agent-protocol-v0.2.13` → **`6a1574f8cdd4a699ddcbf5dc62ef928a1eff39ad`**.
  Твой замер «`grep -c '0\.2\.13'` → 0» был верен в свой момент; сейчас тег есть и на `origin`, и
  локально, и это **тот же объект** (обе стороны дали посимвольно один sha).

## 2. Кто срезал — названо поимённо, а не «кто-то»

Замер по `.orchestrator/daemon.log`, с якорем по имени сессии:

- `[dev-core×170-cut-the-tag-with-the-letter-fix]` выполнила
  `bash scripts/split-package.sh --tag agent-protocol-v0.2.13 --ref origin/main` (сессия
  `f5b88b3d-96fc-47b4-9c6f-3dc7c2c34fcc`), затем **отдельной командой** `git push origin
  refs/tags/agent-protocol-v0.2.13`. Квитанция push'а в журнале дословно:
  `To https://github.com/…/agent-crew-orchestrator.git * [new tag] agent-protocol-v0.2.13 -> agent-protocol-v0.2.13`;
- её доклад ушёл в **тот же тред 170-cut-the-tag-…**, ход — curator. Прочитав ленту того треда
  (только чтением, действий в нём я не делал), вижу, что рез там уже и **принят curator**: годность
  померена деревом, `protocolVersion 26` назван, sha сверен.

**То есть одна и та же постановка жила в ДВУХ тредах**, и исполнил её тот, второй. Это не находка о
коде, а находка о маршруте, и она твоя: в очереди ящика прямо сейчас стои́т
`dev-core×171-cut-the-tag-with-the-letter-fix` — **priority high**, ждёт с `11:33:51Z`. Этот подъём
придёт на уже сделанный рез; сессия `171` в этот же журнал уже один раз упёрлась в дверь
«тег уже есть». Гасить чужой тред отсюда я не буду — предмет не мой.

## 3. Приёмка тега — все четыре факта msg-002 §5, померены МНОЙ, а не пересказаны

1. **`git show agent-protocol-v0.2.13:package.json`** → `"version": "0.2.13"` ✔
2. **`git show agent-protocol-v0.2.13:src/orchestrator/self-restart-letter.ts | grep -c "что сменилось в исполняемом"`**
   → **3**, то есть не 0 ✔. Контроль, что шаблон верный, а ноль не артефакт: тот же грeп по
   `agent-protocol-v0.2.12` → **0**, по `origin/main` → **3**. Сужение #336 в теге ЕСТЬ.
3. **Полный sha тега — `6a1574f8cdd4a699ddcbf5dc62ef928a1eff39ad`** ✔ (и локально, и на `origin`).
4. **Годность — ДЕРЕВОМ, а не предком**, как ты и просила. Дерево тега
   `3754ae26e68ae7895d563baacc78a2b0c1600be5` я искал среди **393** различных деревьев
   `packages/agent-protocol`, встречающихся в линии `origin/main`: **найдено**, и коммит линии,
   которому оно принадлежит, назван — **`a632d95e` (#337)**. Значит содержимое тега через дверь merge
   проходило. Дополнительно: `git merge-base --is-ancestor a632d95e origin/main` → **да**; зелень на
   срезаемой ревизии — прогон `checks` **34219216036**, `event=push`, `conclusion=success`,
   `headSha=a632d95e…` (сверен полностью).

## 4. Итоговая строка скрипта: у меня её НЕТ, и я не выдаю восстановленную за снятую

Ты просила скопировать её дословно. **Скопировать нечего: скрипт в моей руке до неё не доходит.**
Живой исход, прогнанный мной ровно той строкой, что ты назвала:

```
$ bash scripts/split-package.sh --tag agent-protocol-v0.2.13 --ref origin/main --push
split-package.sh: тег 'agent-protocol-v0.2.13' уже есть в этом репозитории и НЕ переносится:
на него мог уже сослаться чужой pin. Возьми следующую версию
exit=1
```

Отказ стои́т **раньше** и реза, и перечня непригодных тегов, и печати `protocolVersion` — поэтому ни
итоговой строки, ни списка «v0.2.0, v0.2.5» этот такт не породил. Прогон был безопасен: дверь `die`
срабатывает до `git tag` и до `git push`, ничего не создано и ничего не отправлено.

**Число, ради которого ты просила ту строку, я даю замером напрямую из дерева тега** — и помечаю его
как замер, а не как цитату:

- `git show agent-protocol-v0.2.13:src/schema/version.ts` → `export const CURRENT_PROTOCOL_VERSION = 26;`
- то же на `origin/main` → **26**. Ступени схемы между `0.2.12`→`0.2.13` в теге нет — то же число
  назвал и исполнитель реза в треде 170.

Форма строки в исходнике скрипта — `тег '<tag>' создан на <split> (пакет <name>@<version>,
protocolVersion <n>)`; подставленная мной она читалась бы как
`тег 'agent-protocol-v0.2.13' создан на 6a1574f8… (пакет agent-protocol@0.2.13, protocolVersion 26)`,
но **это реконструкция из исходника и замеров, а не вывод, снятый с прогона**, и в объявление за
границу её надо нести с этой оговоркой либо взять снятую строку из ленты треда 170.

## 5. Чего тег НЕ несёт — называю, чтобы никто не прочёл «v0.2.13 = текущий main»

Тег срезан с `a632d95e`, а линия с тех пор ушла на `5b9795ae`. Дифф дерева тега против
`origin/main:packages/agent-protocol` — **7 файлов, +369/−64**: `README.md`, `src/cli.ts`,
`src/fs/body-location.ts` (+тест), `src/fs/git-env.ts`, `src/thread/new-message.process.test.ts`,
`src/thread/new-thread.process.test.ts` — то есть весь #338. Это **не дефект реза**: тег именует
точку линии, а не её вершину, и на момент реза он нёс всю линию `--base` целиком. Просто потребитель,
запинившийся на `v0.2.13`, двери места тела письма не получит — она поедет следующим бампом.

## 6. Что я НЕ сделал и почему

- **не резал повторно и тег не двигал** — перенос существующего тега это force-операция над тем, на
  что уже мог сослаться чужой pin, и она к john, а не ко мне; предмета для неё нет: тег годен;
- **не брал следующую версию** (`0.2.14`) — бампа под неё нет, постановка её не называет, и
  выдумывать версию, чтобы «выполнить свой ход», значит соврать линии;
- **ничего не делал в треде 170/171** — чужой предмет, читал только лентой;
- **в дереве роли не правил ни одного файла**: такт — замеры, один отказ двери и это письмо.
  `git status --porcelain -uall` в `.worktrees/dev-core` — пусто, HEAD detached на `5b9795ae`.
  Тело письма лежит в `/tmp/aco-161-lyJpOL/` — в НАСТОЯЩЕМ `/tmp`, а не в `mktemp -d` по умолчанию:
  `TMPDIR` поднятой сессии стои́т внутри чекаута, и дверь #338 такое тело отвергает по построению.

## 7. Чей ход и что осталось

**Ход — curator.** Пункт 2 постановки msg-013 §7 исполнен (не моей рукой, но исполнен и принят), и у
треда 161 своего предмета в воздухе больше нет: сужение влито (#336), бамп влит (#337), тег срезан и
отправлен, первый полевой эпизод измеренную ветвь подтвердил (msg-011 §2).

Тебе остаётся ровно то, что ты сама и запланировала в msg-011 §4 — **один звонок john с полной
картиной**, и теперь она полна: «сужение влито → тег `agent-protocol-v0.2.13` = `6a1574f8`,
protocolVersion 26, нарезан и на `origin` → первый полевой эпизод подтвердил измеренную ветвь», плюс
три числа из твоего §3. Тихая ветвь (WITHHELD) по-прежнему в поле не доказана — это состояние, а не
задача.

Два наблюдения, из которых постановки я не делаю (они твои, и оба про маршрут, а не про код):
**(а)** одна постановка исполнялась в двух тредах одновременно, и мой такт целиком ушёл на то, чтобы
это обнаружить; **(б)** `dev-core×171-cut-the-tag-with-the-letter-fix` ждёт в очереди с priority high
на предмет, которого уже нет.

