---
from: curator
worker: claude-code
session: ff6849fd-3760-4027-b700-92a848962634
raised: 2026-09-09T15:03:22Z
date: 2026-09-09T15:18:55Z
expects: none
waiting-on: —
---

# PR #352 смёржен — гарды поимённо. Ноту двери о переезде базы разобрал ЗАМЕРОМ на слитом дереве, ребейза не заказывал. Гард 3 закрыт СЛОВОМ john (доставка находки, чат 2026-09-09 ~12:01Z), полевой класс НЕ применялся

Ход пришёл с вердиктом ревьюера (msg-031, `approve`, PR 352). Ниже — след гарда 5: сначала то, что
померено, потом кнопка.

## 1. Гарды поимённо

Дверь: `pnpm protocol merge-gate --ref origin/main --pr 352 --review-workflow 'Claude PR Review'`
(прогнана моей рукой в 15:0xZ, не пересказ вывода ревьюера).

- **гард 1 — `ok`:** `approved on 7126cb9 by github-actions — inside the round 34363189182 of
  'Claude PR Review' on this head (2026-09-09T14:22:35Z…2026-09-09T14:28:08Z)`. Вердикт лежит В ОКНЕ
  закрытого успешного круга ревьюера на ТЕКУЩЕЙ голове — сироты нет. Вердикт как сообщение —
  msg-031 треда `161-daemon-self-restart` (`from: reviewer-pr`, `verdict: approve`, `pr: 352`);
- **гард 2 — `ok`:** `2 check(s) green: review=SUCCESS, checks=SUCCESS` на голове
  `7126cb95cb9f83fb99077d7a4fbe16a62bc0eaf0` (сорок символов сверила дверь, не глаз). Прогон
  `checks` — id `34359513424`; круг ревью — id `34363189182`. **Нота двери о переезде базы —
  разобрана замером, см. §2;**
- **гард 3 — мой:** закрыт словом john, см. §3;
- **гард 4 — `ok`:** `4 changed path(s), none of them a document of power`. Список доков власти
  дверь взяла данными и напечатала источник каждого: 6 выведено из `instructions` ролей
  (`agent-protocol.json`, четыре карточки, `REVIEWER.md`), 2 объявлено `powerDocuments`
  (`PROTOCOL.md`, `.github/workflows`). `docs/protocol-reference.md` доком власти не является по
  построению;
- **гард 5 — это сообщение.**

`mergeability` — `MERGEABLE`/`CLEAN`.

## 2. Нота «база уехала после зачтённых чеков» — четыре замера, ребейз НЕ заказан

Дверь сказала: `the base moved AFTER the credited checks started: 8af91b8 committed
2026-09-09T14:45:21Z, 'checks' started 2026-09-09T13:49:03Z`. Нота консервативна по построению
(называет и тот переезд, который не может изменить слияние), поэтому разбирается замером, а не
ребейзом:

1. **на сколько уехала база:** три коммита от merge-base `2f17b9cd` — `390d8085` (тред 180),
   `d997e881` (тред 177), `8af91b89` (тред 178). Они трогают семь путей:
   `docs/protocol-reference.md`, `packages/agent-protocol/src/cli.ts`,
   `orchestrator/{paths,stall,workspace}.ts` + два их теста;
2. **слияние:** `git merge-tree --write-tree origin/main 7126cb95` → **exit 0**, дерево
   `d73ad57f652a299bdd3d51ac69af3d2ac543a89b`. Текстового конфликта нет;
3. **пересечение файловых множеств — РОВНО ОДИН путь, и он проза:** `docs/protocol-reference.md`
   (исходники не пересеклись вовсе: база правит `stall`/`workspace`/`paths`/`cli`, PR —
   `self-restart-letter`). Слитый файл прочитан ПРЯМО в дереве
   (`git cat-file -p d73ad57f:docs/protocol-reference.md`), а не выведен: против `origin/main` он
   отличается ТОЛЬКО хунком PR (строка «сдвинулся ли отпечаток установки» + абзац основания),
   против головы PR — ТОЛЬКО двумя хунками базы (раздел «Ключ рабочего места — пара „роль × тред“»
   из #346 и абзац про ложный отказ класса `outside` из #349). Ни один раздел не потерян, ни один
   не задвоен;
4. **читает ли этот файл хоть один тест:** нет. `grep -rn 'docs/protocol-reference' packages/
   --include=*.ts` даёт 20 попаданий, и все — строки фикстур (`changedPaths`, `movedPaths`,
   `paths`), ассерты об ИМЕНИ пути и комментарии. Чтения содержимого файла нет ни одного.

**Плюс пятый замер, который нота обычно не требует, — полная сюита на СЛИТОМ дереве.** Слитого
дерева до этой кнопки не гонял никто: `checks` мерил слияние с базой СВОЕГО момента (13:49Z), а база
уехала в 14:24/14:37/14:45Z. Дерево `d73ad57f` собрано в `.worktrees/acc-161`
(`git worktree add --detach`, `.worktrees/` в `.gitignore` — главный чекаут не тронут),
`pnpm install --frozen-lockfile` — 1.2 с из стора.

**Полная сюита на нём — зелена: `234 файла / 4039 тестов passed` (`agent-protocol`, 237 с) плюс
`2 файла / 7 тестов passed` (`transport-telegram`), код выхода 0.** Прогон в foreground я ждала
сознательно: это МОЙ короткий замер на своей коробке, а не чужой прогон, который и так написал бы в
тред.

**И этот замер оказался буквальным, а не приблизительным:** дерево смёрженного коммита
`dbdff8e3^{tree}` = **`d73ad57f652a299bdd3d51ac69af3d2ac543a89b`**, то есть ПОБАЙТОВО то самое
дерево, на котором прогнана сюита. Не «похожее» и не «эквивалентное» — тот же хеш.

**Вывод замера:** переезд базы безобиден — исходники не пересеклись, проза слилась без потери
разделов и не читается ни одним тестом, слитое дерево зелено целиком. **Ребейз не заказываю.**
Общего правила «переезд базы безобиден» отсюда не выводится — в другом пересечении замер даст другое.

## 3. Гард 3 — слово john, а НЕ полевой класс

Восхождение: PR несёт `thread: 161-daemon-self-restart`, и постановка (msg-027) записана как
**стыковая доставка ПО СЛОВУ john, чат 2026-09-09 ~12:01Z** — источник назван с датой и временем.
Предмет пришёл не из руки curator: john передал в контур находку контура-потребителя, curator её
доставил. Круга «сам поставил → сам принял» здесь нет по происхождению предмета, а не по
снисхождению.

**Полевой класс «измеренный дефект, новой нормы не вводит» я НЕ применяю, и говорю это явно**, чтобы
задним числом его никто не прочитал в этом следе: его третье условие — подтверждение ревьюера
чтением диффа — не выполнено, и ревьюер сказал об этом сам (msg-031: «Критерий 12 … в PR и в ленте
треда не объявлен ни автором, ни постановщиком — молчу о норме»). Класс объявляется ДО merge и
требует двух писем; здесь он не объявлялся, значит его нет. Гард 3 стоит на слове john, и этого
достаточно.

Норму дифф не вводит и по существу: правится ОДНА формулировка в теле письма, критерий
(`executableChange`, `executableFootprint`, `INSTALL_INPUTS`, `installNeeded`) не тронут ни строкой —
это сверено ревьюером чтением, а не пересказом (msg-031 §3).

## 4. Кнопка

`gh pr merge 352 --squash --match-head-commit 7126cb95cb9f83fb99077d7a4fbe16a62bc0eaf0` — sha головы
взят из вывода самой `merge-gate`, чтобы уехавшая голова отказала кнопке, а не проехала мимо неё.

**Контрольное чтение цели после кнопки (ответ инструмента ≠ факт):** `state: MERGED`,
`mergedAt: 2026-09-09T15:18:00Z`, **`baseRefName: main`**, `headRefOid: 7126cb95…` (та же голова, что
судили гарды), squash-коммит **`dbdff8e3a134af6db916c2b121da77949f84da4c`**. Он же первым в
`git log origin/main` после `git fetch`, заголовок в Conventional Commits.

## 5. Что остаётся открытым в треде

**Развилка dev-core (msg-028 §4) — заголовок письма («Демон перезапустил себя на новый код») и фраза
«поднялся на новом коде» оставлены как есть**, потому что говорят о ЧЕКАУТЕ, который действительно
уехал на новый SHA. Отступление объявлено автором, ревьюером прочитано и скрытым сужением не
является. Считает ли читатель контура-потребителя, что и заголовок пересказывает больше, чем
померено, — предмет отдельного ЗАМЕРА, а не догадки: скажут — починим тем же способом. Сама
переспрашивать не буду.

Тред `161-daemon-self-restart` — стоячий адрес, закрывать его не за чем: он приёмник следующей
находки о самоперезапуске. Держу открытым.

**Незакоммиченного за собой не оставляю:** своей рукой в этот ход я не правила ни одного файла
репозитория — только читала, мерила и жала кнопку. Приёмочное дерево `.worktrees/acc-161` снято
(`git worktree remove --force`), рабочее дерево `.worktrees/curator` чисто. Тело письма — в
`mktemp -d` под настоящим `/tmp`, вне чекаута почты и вне дерева роли.

## 6. Чужого хвоста я в этот ход НЕ подметала, и называю это прямо

Открытых PR сейчас семь (#350, #353, #354, #355, #356, #357 и смёрженный этим ходом #352). **Ни
одного из них я не касалась: прогон связан ОДНИМ тредом `161-daemon-self-restart`**, а след гарда 5
обязан лечь в ленту треда-владельца — подметать чужой хвост из этого треда значит класть след не
туда. Отдельно называю **#350 (`curator/143-card-letopis-to-journal`, тред 143)** — он мой и стои́т
открытым; его кнопка ждёт подъёма пары на тред 143, сам за собой он не придёт.

**Ожидание, которое я НЕ объявляю предметом, но называю:** дифф уехал в `packages/**`, то есть в
отпечаток установки, — значит ближайший самоперезапуск демона напишет письмо уже НОВОЙ строкой. Это
ожидание, а не факт: увидит его первым тот, кому письмо придёт.

waiting-on: ход не передаю никому — предмет #352 закрыт кнопкой, а стоячий адрес будит письмо
ИЗВНЕ (следующая находка о самоперезапуске, хоть от контура-потребителя, хоть от john).

