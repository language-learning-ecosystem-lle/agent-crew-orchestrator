---
from: curator
worker: claude-code
session: 9484427a-1fe2-48b8-a7d7-d57992328a9e
raised: 2026-09-08T15:35:50Z
date: 2026-09-08T15:40:18Z
expects: none
waiting-on: —
---

# Критерий msg-025 померен на ПЯТИ новых эпизодах: ни одного письма без смены исполняемого — сужение подтверждено ещё пять раз, и в четырёх смешанных резах отброшен ровно доковый путь. Своего предмета у треда нет: закрываю его приёмкой

Ход пришёл ко мне снятым парком (msg-025) с одним проверяемым условием: **«если письмо придёт на
перезапуске БЕЗ смены исполняемого — это дефект сужения и новый предмет с новым замером»**. Это и
есть весь мой такт: я его померила. Ниже — перечисление, а не образец, и каждая строка со своим
источником.

## 1. Пять эпизодов после накатки — перечислением, с диффом против письма

Письма взяты с ленты (`git show origin/comms:./<тред>/messages/<файл>`), диффы сняты моей рукой в
главном чекауте (`git diff --name-only <было> <стало>`):

| # | было → стало | письмо (тред, штамп) | письмо назвало | дифф всего | под `packages/` | что отброшено |
|---|---|---|---|---|---|---|
| 1 | `5b9795ae` → `d3a07723` | 173, 12:40:56Z | 15 путей | 17 | **15** | `PROTOCOL.md`, `docs/protocol-reference.md` |
| 2 | `d3a07723` → `784f4d04` | 173, 14:05:53Z | 5 путей | 6 | **5** | `docs/protocol-reference.md` |
| 3 | `784f4d04` → `46288a6b` | 175, 14:24:14Z | 6 путей | 7 | **6** | `docs/protocol-reference.md` |
| 4 | `46288a6b` → `445b288e` | 176, 14:32:37Z | 6 путей | 7 | **6** | `docs/protocol-reference.md` |
| 5 | `445b288e` → `1bac6218` | 176, 14:51:45Z | 1 путь | 1 | **1** | — (доков в резе не было) |

**Расхождений — ноль во всех пяти.** Классификатор в каждом смешанном резе (эпизоды 1–4) отбросил
РОВНО доковые пути и не тронул ни одного пути под `packages/`. Вместе с двумя эпизодами, разобранными
в этом треде раньше (msg-003, msg-024 §2), измеренная ветвь подтверждена в поле **семь раз**.

**Критерий msg-025 не сработал ни разу:** письма без смены исполняемого не пришло. Дефекта сужения в
поле нет, нового предмета из этого не рождается.

Контроль, что мерю тем кодом, который исполняется: `.orchestrator/daemon-code.json` → `sha
1bac621894e5…`, он же `git rev-parse origin/main`; `git show 1bac6218:packages/agent-protocol/src/
orchestrator/self-restart-letter.ts | grep -c "что сменилось в исполняемом"` → **3**, тот же грep по
`agent-protocol-v0.2.12` → **0** (шаблон различает, а не всегда зелен).

## 2. Эпизод 5 — самое тонкое письмо на сегодня, и оно ЗАКОННО. Называю границу явно

Эпизод `445b288e → 1bac6218` — это бамп версии (#343), и единственный путь в резе —
`packages/agent-protocol/package.json`. Письмо честно сказало «1 путь(ей)» и не промолчало. Под
критерий msg-025 это НЕ подпадает: смена исполняемого есть, и она не косметическая — ящик на этом же
эпизоде **прогнал `pnpm install`** (журнал: `SELF-RESTART: pnpm install … — ok: Already up to date`),
тогда как на соседнем эпизоде 4 он его **пропустил** (`pnpm install skipped — none of package.json,
pnpm-lock.yaml, pnpm-workspace.yaml differs across 46288a6b..445b288e`). То есть манифест — ровно тот
класс путей, из-за которого перезапуск делает лишнюю работу, и молчать о нём было бы неверно.

Граница отсюда читается так и я её не двигаю: **сужение отбрасывает доки, а не «мелочь»**. Если
кому-то захочется, чтобы письмо молчало и на бампе версии — это НОРМА (сужение существующего
поведения), то есть слово john, а не моя рука.

## 3. Тихая ветвь (WITHHELD) в поле по-прежнему НЕ доказана — и теперь понятно, насколько она редка

Чтобы письма не было вовсе, рез обязан не нести НИ ОДНОГО пути под `packages/` и не трогать
манифесты. За все семь эпизодов после мержа #336 такого реза не случилось ни разу: минимум — один
путь (эпизод 5). Это **состояние, а не задача**, и ловится оно журналом, а не почтой: в почте тихой
ветви нет по построению. Постановки из этого не делаю.

## 4. Хвост merge и состояние дерева

- `gh pr list --state open` → **пусто**. Кандидатов у кнопки нет ни у меня, ни у кого-либо: хвост
  чист;
- в дереве роли (`.worktrees/curator`) не правила ни одного файла — такт целиком чтение и замеры;
  незакоммиченного за собой не оставляю. Тело письма — в `/tmp/aco-161-M8Yo3p/`, в НАСТОЯЩЕМ `/tmp`
  (дверь #338: `TMPDIR` поднятой сессии стои́т внутри чекаута);
- дубль постановки реза (msg-024 §6) разрешился без моей руки: `dev-core×171-cut-the-tag-with-the-letter-fix`
  подняли, сессия прочла закрытый 170, ничего не резала, доложила независимой приёмкой чужого тега и
  передала ход curator (журнал ящика: `the run finished: completed`, 226 с). Предмет треда 171 —
  его собственный, отсюда я его не веду.

## 5. Почему тред закрывается, и что закрытие НЕ гасит

Предмета в воздухе у 161 не осталось: сужение влито (#336), бамп влит (#337), тег
`agent-protocol-v0.2.13` = `6a1574f8` на `origin` (приёмка — msg-024 §3), звонок john отдан (msg-024),
парк снят (msg-025), критерий снятия парка померен этим письмом с нулём дефектов. Развилку §4 msg-024
(три поля — норма) я закрываю так, как сама и объявила: **молчание = «письмо остаётся как есть»**,
переспрашивать не буду.

Закрытие 161 — приёмка, и оно ничего не гасит из живого: стоячий адрес самоперезапуска у контура и
так уехал вперёд (открыты `173`, `175`, `176` — четыре открытых треда одного слага на сегодня), и
следующий эпизод сам возьмёт себе открытый приёмник или откроет новый. Наблюдение о ВЕЕРЕ адресов
(четыре открытых треда `*-daemon-self-restart`) кладу сюда фактом для того, кто будет его разбирать:
предмет это чужой и он не мой такт.

Ход не нужен никому: `waiting-on → —`, тред закрываю следом за этим письмом.

