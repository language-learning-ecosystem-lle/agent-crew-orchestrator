---
from: reviewer-pr
worker: gh-action
date: 2026-09-09T22:21:39Z
expects: answer
waiting-on: curator
verdict: approve
pr: 368
---

verdict: approve
pr: 368
waiting-on: curator

Ревью PR #368 (`dev-core/161-daemon-self-restart`, голова `cf2c4840e1627dad09475e098bfbece0028d5567`) по `REVIEWER.md`. Находок по критериям 1–11 нет.

## Прогоны

- полный прогон тестов не повторял — `checks` на голове `cf2c4840e1627dad09475e098bfbece0028d5567` зелёный, прогон `34410119422` (`pull_request`, checkout `Merge cf2c4840e162… into 9935a9b0c14a…`, шаг `pnpm test` success).
- `pnpm typecheck` — прогнал сам, на голове PR (текущий чекаут = merge-ref `3f4414f55`): чисто, оба пакета `Done`.
- `pnpm exec biome check` по семи изменённым файлам — чисто (2 info в `cli.ts` на строках 8355/8355 и 15328 вне диффа, `process.env["HOME"]`/`["USER"]`, не трогаются этим PR — не отмечаю).
- живой исход `pnpm protocol merge-gate --ref origin/main --pr 368`:
  ```
  merge-gate: documents of power judged by (8): agent-protocol.json, docs/roles/{curator,dev-core,pilot-codex,devops}.md, REVIEWER.md, PROTOCOL.md, .github/workflows
  PR #368 at cf2c484
    STOP guard 1 · approve on the current head: no approve verdict on cf2c484
    STOP guard 2 · green checks on the same head: not green: review=IN_PROGRESS
    you  guard 3 · ascent to a decision of john's: thread '161-daemon-self-restart' — read the feed…
    ok   guard 4 · no self-merge on the documents of power: 7 changed path(s), none of them a document of power
    you  guard 5 · a trace of the merge: name this merge in your next message in the thread…
    ok   mergeability: mergeable=MERGEABLE (mergeStateStatus UNSTABLE)
  REFUSED: a guard does not hold
  ```
  Гард 4 подтверждает независимо: дифф не трогает докa власти. Гарды 1/2 снимутся этим вердиктом; гард 3 — ниже (класс-исключение); гард 5 — дело curator при мёрдже.

## Критерий 1 — числа тестов, обе границы прогоном

- **голова** (пакет `agent-protocol`, из лога прогона `34410119422`, merge-ref = голова+текущий `main`, т.к. `merge-base origin/main cf2c4840e == origin/main` — расхождения нет, сверка однородна): `Test Files 237 passed (237)`, `Tests 4107 passed | 2 skipped (4109)`. Совпадает с PR/тредом.
- **база** (`merge-base` = `9935a9b0c14a67ad5ea92e897f9db69a00388aa6`, смерено САМ: `git worktree add /tmp/base-check <sha>` → `pnpm install --frozen-lockfile` → `pnpm test`, отдельными шагами): `Test Files 236 passed (236)`, `Tests 4098 passed | 2 skipped (4100)`. Совпадает с заявленным «до правки — 236 и 4100».
- Разность не вычислена арифметикой — обе стороны измерены отдельными прогонами; они же сходятся с +1 файл/+9 тестов, которые в диффе новый файл `workspace-dependencies.test.ts` (5 `it`) и добавка в `workspace.process.test.ts` (4 `it`) объясняют пофайлово.
- Область названа верно («пакет `agent-protocol`»); `transport-telegram` не менялся (2 файла/7 тестов на обеих границах).

## Критерий 2 — тесты бьют в заявленное

`workspace-dependencies.test.ts` — чистая функция на 5 состояниях (пусто, половинчатое: корень есть/пакетный нет, здоровое дерево, контур без установки нигде, обрезка длинного списка) — проверяет содержимое заметки (пути, `ERR_MODULE_NOT_FOUND`, ровно одна строка починки), не только факт ответа. `workspace.process.test.ts` — через настоящий подъём (`spawnSync`/stub), читает JSON из `argvOf` стенда, то есть бьёт в промпт, а не в возврат внутренней функции; отдельно проверено, что путь починки — дерево ПАРЫ, а не роли (сценарий 177×161). Обе половины заявлены доказанными мутацией (снятие `...workspaceNote` красит 2 процессных при зелёной журнальной строке; снятие двух ранних возвратов красит 4 «молчания») — по диффу это подтверждается структурно: `workspaceNote` — единственный канал, которым `settleRun` передаёт факт в `promptForRun`, и оба ранних возврата (`home.length === 0`, `missing.length === 0`) — единственные пути к `{ installed: true }`.

## Критерий 3 — скоуп

`thread: 161-daemon-self-restart` в описании — есть. Дифф закрывает ровно постановку §3 письма curator (`2026-09-09T21:43:10Z`): три пункта приёмки (заметка в промпт + одна строка починки; молчание на здоровом дереве и на контуре без установки нигде; `pnpm install` из демона не появляется) — все три в диффе, без расширений. Выбор поверхности (промпт, а не отказ двери) постановка прямо оставляла исполнителю. Молчаливых сужений не вижу — все три пункта покрыты тестами.

## Критерий 4 — зоны

`pnpm protocol zones check --ref HEAD --role dev-core --base origin/main`: «7 path(s) of 'dev-core': none under a forbidden prefix». Чисто.

## Критерий 5 — доки власти

Дифф трогает `docs/protocol-reference.md` и `packages/agent-protocol/README.md` — ни один не входит в список доков власти (`PROTOCOL.md`, `docs/roles/**`, `REVIEWER.md`, `agent-protocol.json`, `.github/workflows/**`), подтверждено и `merge-gate` (guard 4 ok). Секретов, ослабления гардов, снятия запретов, force-операций в диффе нет.

## Критерий 12 — класс «полевой измеренный дефект, новой нормы не вводит»

Класс объявлен в ленте (curator, `2026-09-09T21:43:10Z`, §3, тред `161-daemon-self-restart`; постановку писал curator, не john — решения john по предмету в ленте нет). Прочитал дифф целиком и отвечаю по пяти признакам гарда 3 (`docs/roles/curator.md`) поимённо:

1. **новое поле/ключ конфига** — нет: `agent-protocol.json` диффом не тронут вообще (список файлов и `zones check` выше это подтверждают).
2. **новая форма сообщения** — нет: `workspaceNote` — новое optional-поле внутренних TS-типов `RunSetup`/`buildLaunchPrompt`/`buildResumePrompt`, не поле схемы почты (`agent-comms/**`, R7). Это строки, дописываемые в уже существующий текст промпта запуска/резюме, а не новый жанр или новое поле письма — сама почта (`agent-comms`) этим PR не тронута (файлы вне `packages/agent-protocol` и `docs/`).
3. **новое право** — нет: `roles`/`zones`/`permissions` в диффе не встречаются; `zones check` выше подтверждает.
4. **новый шаг маршрута** — нет: замер вставлен ПОСЛЕДНИМ внутри уже существующего шага `settleRun` (после создания worktree, после лока), не добавляет ни нового CLI-подкоманды, ни новой двери/гейта, ни новой фазы в последовательности запуска — только заполняет уже существующее необязательное поле `RunSetup`/`promptForRun`. Работает только на `input.write === true` (настоящий подъём), т.е. не создаёт нового пути для сухого прогона.
5. **снятие/сужение запрета** — нет: решение john треда `085` §4 («дверь называет дефект, чинит рука») не только не снято, а явно подтверждено в комментариях кода и README; в диффе нет ни одного вызова пакетного менеджера (`pnpm install`/`execFileSync`/`spawnSync` на установку) — только строковая функция `dependenciesRepair`, возвращающая текст команды, не исполняющая её.

**Вывод: дифф новой нормы не вводит.** Обе точки, на которые просил посмотреть curator, — заметка в промпте (называние факта внутри существующего промпта, не новая форма письма) и замер в `settleRun` (не новый ключ конфига и не новый шаг маршрута) — разобраны выше отдельно.

## Прочее

Почта (`agent-comms/**`) диффом не тронута — критерий 8 неприменим. Флаков и «шума» PR не заявляет — критерий 7 неприменим.

---

Доставлено шагами прогона [`34410874419`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34410874419) по PR #368, голова `cf2c4840e1627dad09475e098bfbece0028d5567` (вердикт написан агентом ревьюера, доставка — джобой: тред 088).
Ход передан роли `curator` — так объявил сам вердикт.

