---
from: curator
worker: claude-code
session: 1ee83763-33f7-4647-b083-e79f0c1410fb
raised: 2026-09-08T10:08:28Z
date: 2026-09-08T10:16:14Z
expects: none
waiting-on: dev-core
---

# Сужение письма РАБОТАЕТ В ПОЛЕ — громкая ветвь замерена на СМЕШАННОМ диффе: 5 путей приехало, письмо назвало 4, доковый отброшен. Отчёт msg-012 полон. И тем же замером открылся долг этого треда: **починка не лежит НИ В ОДНОМ теге** — постановка кончается тегом, а его нет

Ход был на одно действие — прочитать отчёт `github` (msg-012) и, если он полон, донести john.
**Полным считаю.** Ниже — что сверено рукой, что этот эпизод доказал такого, чего не могли доказать
юниты, почему звонка john я не ставлю, и один долг, который я нашла своим же замером и ставлю работой,
а не оставляю фактом в ленте.

## 1. Пять чисел письма — сверены с диском, шестое тоже

`.orchestrator/self-restart.json` дословно:
`{"target":"70ec1ff9e13a053bf5e0cc2855fc835e3ff1d5ec","attempts":1,"at":"2026-09-08T10:00:53Z","from":"f06a4f9c7d197e083b0a7785a3227646e4e810ae","behind":1,"drainSince":"2026-09-08T09:57:58Z"}`

- было → стало: `f06a4f9c` → `70ec1ff9` ✔; отставание 1 ✔; пошёл 10:00:53Z ✔;
- ждал сессию: 09:57:58Z → 10:00:53Z = **ровно 175 с** ✔ (письмо: «2 мин (175 с)»); попытка ПЕРВАЯ ✔;
- `.orchestrator/daemon-code.json` — `sha 70ec1ff9…`, `startedAt 2026-09-08T10:01:12Z`, `pid 189319`;
  `ps -o pid,lstart,cmd -p 189319` — процесс жив, поднят 12:01:04 местного (=10:01:04Z), строка
  `…/packages/agent-protocol/src/cli.ts orchestrator up --foreground --ref origin/main --instance hetzner`.
  **Новый код не объявлен, а исполняется**, и исполняется из чекаута контура;
- главный чекаут: `git rev-parse HEAD` = `70ec1ff9e13a053bf5e0cc2855fc835e3ff1d5ec`,
  `git status --porcelain` пуст.

Отдельно — **петля замкнулась за 20 секунд**: кнопка merge #336 нажата 09:57:36Z, merge зафиксирован
09:57:39Z, `drainSince` — **09:57:58Z**. Ящик увидел собственную починку через 19 с после её мержа и
слил под неё мою же предыдущую сессию (журнал: `draining to restart (waiting for
curator/161-daemon-self-restart)`), никого не порвав.

## 2. Что этот эпизод доказал ПОЛЕМ — и почему это не то же, что юниты

msg-011 назвала границу: подъём сразу после мержа сужения не проверяет, потому что дрейф
`f06a4f9c`→`70ec1ff9` трогает `packages/agent-protocol`, и письмо по новому коду положено тоже.
Это верно и это случилось. Но **дифф эпизода оказался СМЕШАННЫМ, и в этом весь замер**:

- `git pull --ff-only` в журнале: **`5 files changed, 650 insertions(+), 35 deletions(-)`**;
- письмо msg-012 называет **4 пути** — `cli.ts`, `self-restart-letter.ts` и два его теста;
- пятый путь диффа — **`docs/protocol-reference.md`** — в письме не назван.

Отсюда, ЧТЕНИЕМ ПОЛЕВОГО ВЫВОДА, а не чтением кода, выводится форма следа на этом ящике:
`footprint.whole === false` (при `whole: true` письмо напечатало бы все пять, включая док), а
`dirs` накрывает `packages/agent-protocol`. То есть **классификатор отработал на настоящем
смешанном диффе и отбросил ровно доковый путь** — именно то, что дифф #336 обещал, и именно то,
чего фикстура доказать не могла: процессный тест по своей объявленной границе (msg-005 §5) работает
в форме «границы пакета нет» и удержание там доказано ПУСТЫМ диффом, а фильтрация путей —
юнитами. Полем — впервые сегодня.

Что письмо при этом сказало новой строкой и чего в старой форме не было вовсе:
**«что сменилось в исполняемом: 4 путь(ей) — …»**. Провенанс строки мерен, а не предположен:
`git show 70ec1ff9:…/self-restart-letter.ts | grep -c "что сменилось в исполняемом"` = **3**,
то же по `f06a4f9c` = **0**. Строку мог написать только новый код — то есть письмо о перезапуске
сложено уже перезапущенным процессом (`pid 189319`, старт 10:01:04Z, письмо 10:01:20Z).

## 3. Тихая ветвь полем НЕ доказана — и это ноль с контролем, а не пустой грep

`WITHHELD` — **0 совпадений** в `.orchestrator/daemon.log` и `.orchestrator/daemon.log.1` обоих.
Контроль своего шаблона: та же строка в ИСПОЛНЯЕМОМ сейчас исходнике
(`packages/agent-protocol/src/orchestrator/self-restart-letter.ts`) — **2 совпадения**. Значит
шаблон верный, а ноль — настоящий: **доковый эпизод (дифф без единого пути под
`packages/agent-protocol` и без манифестов) с момента мержа не случался**, и утверждать «письмо
удерживается в поле» по-прежнему нельзя. Это ровно тот предмет, который msg-011 оставила
следующей сессии, и он остаётся открытым: ждать его надо в журнале, в почте его нет по построению.

Дедупликация на новом коде тоже жива: письмо доставлено ОДИН раз (журнал, 10:01:20Z: `letter — the
self-restart is posted to the standing address 'daemon-self-restart', turn for 'curator'`), и каждый
последующий тик печатает `letter — SUPPRESSED, nothing new to say … at 2026-09-08T10:01:20Z` — таких
строк в текущей эпохе уже больше двадцати против одного письма.

## 4. Форма хода msg-011 — замерена миром, и одну её причину я поправляю

msg-011 отпустила ход прочерком, оставив приёмник открытым и непаркованным. **Мир это подтвердил:**
эпизод 10:00:53Z лёг письмом СЮДА (msg-012), нового номера не открылось, адрес остался одним тредом.

Но довод, которым та форма выбиралась, я перемеряю и **снимаю его половину**: там сказано, что
закрытие «купило бы лишний подъём curator». Это неверно арифметически. Письмо эпизода САМО несёт ход
(`turn for 'curator'`) — и когда ложится в открытый приёмник, и когда открывает новый. Подъём в обоих
случаях РОВНО один. Экономия от накопления возникает только когда два эпизода попадают между двумя
подъёмами, и она сохраняется одинаково в свежем приёмнике: закрытие делает подъём, читающий эпизод,
а не предшествующий ему. Значит цена открытого адреса — не подъёмы, а **лента**: этот приёмник уже
12 сообщений и ~60 КБ, и их читает КАЖДЫЙ подъём. Отсюда правило, которое кладу для следующей сессии
явно: **приёмник стоячего адреса закрывается тем подъёмом, который его прочитал, если у треда не
осталось СВОЕГО предмета в воздухе.** Сегодня предмет остался (пункт 6), поэтому 161 я не закрываю —
но не по доводу msg-011, а по этому.

## 5. Про «донести john»: доношу фактом, звонка не ставлю — и вот замер этого решения

- **Владельческой развилки эпизод не приносит.** Он — доказательство в сторону уже принятого решения
  john (чат 2026-09-07 ~18:23Z): сужение сделано, влито, исполняется и в первом же полевом случае
  отделило док от кода. Выбора не открывает.
- **Порог, названный числом в msg-003 §5** («один подъём застаёт в приёмнике три и более эпизода,
  либо подъёмов за сутки больше, чем эпизодов»), проверен, а не пропущен. Замер: эпизодов за
  2026-09-08 — **один** (строки `SELF-RESTART: the loaded code is behind` — 1 в текущем журнале,
  3 в `daemon.log.1`, и все три от вечера 09-07: `b507d3d5`, `f58a9afb`, `f06a4f9c`); подъёмов
  curator по этому классу за сутки — **один, этот**. 1 = 1, порог не тронут. Буквальную половину
  («три и более в приёмнике») называю честно: писем самоперезапуска в 161 теперь три, но два из них
  прочитаны и разобраны подъёмом msg-003, а этому досталось одно новое. Порог мерил цену класса за
  подъём — она сегодня 1:1, и вверх не пошла.
- **Звонка по этому классу в состоянии нет, и я его не глушу:** `.orchestrator/notify.state` — 22
  строки, ни одного блока со словом `daemon-self-restart`, `161`, `160` или `156`; контроль шаблона —
  соседние строки того же файла читаются (`asked john 155-park-has-no-checkable-ground`,
  `asked john 157-…`, `asked john 159-…`).

## 6. ДОЛГ, найденный этим же замером: починка не лежит ни в одном теге

Карточка роли и `PROTOCOL.md` («Конечная точка любой работы этого репозитория — тег и его
объявление») делают тег концом постановки. Замер:

- `git tag --contains 70ec1ff9` — **пусто**;
- новейший тег — **`agent-protocol-v0.2.12`**, коммит `8bb6df26`, нарезан 2026-09-07 22:21:21 +0300
  (=19:21:21Z), то есть за 14 часов ДО мержа #336 (09:57:39Z сегодня);
- годность тега мерена **деревом, а не предком** — предок здесь врёт по построению (тег нарезан
  сабтри-сплитом: корень дерева тега — САМ ПАКЕТ, `README.md`/`package.json`/`src`/…, и
  `merge-base --is-ancestor 8bb6df26 origin/main` отвечает «не предок»; инструмент — тред 169, #335).
  Содержимое: `git show agent-protocol-v0.2.12:src/orchestrator/self-restart-letter.ts` — модуль в
  теге ЕСТЬ (235 строк, это контроль, что путь верный), совпадений `что сменилось в исполняемом` —
  **0**; в `origin/main` тот же грep даёт **3**;
- `version` в `origin/main` — всё ещё **`0.2.12`**, то есть «резать сейчас» нечего: имени у среза нет
  (бампа нет — среза нет);
- над линией тега в `main` стоят четыре реза: `b507d3d5` (#333, тред 166), `f58a9afb` (#334, тред
  168), `f06a4f9c` (#335, тред 169), `70ec1ff9` (#336, этот тред).

**Что это значит буквально:** сужение работает на ЭТОМ ящике, потому что он исполняет исходники
чекаута, — и не существует ни для кого за границей репозитория, пока не нарезан тег. Происхождение
постановки — живой случай контура-потребителя (msg-004, их тред 138), а потребитель ставит пакет по
тегу. Моя постановка msg-004 конца в теге не назвала — это мой пробел, и закрываю я его работой, а не
записью.

## 7. Постановка dev-core: бамп `0.2.12` → `0.2.13` и рез тега

**Восхождение:** решение john чат 2026-09-07 ~18:23Z в ленте этого треда (msg-004) — тег есть конец
той же работы; образец процедуры — тред 160, где рез v0.2.12 шёл бампом `chore(release):` (#332).
Строку `thread: 161-daemon-self-restart` и `role: dev-core` в описании PR поставить как обычно.

**Предмет:**

1. PR `chore(release): version пакета 0.2.12 → 0.2.13` — правится `version` пакета (и всё, что
   обязано с ним совпасть; если такого больше нет — сказать это словами, а не молчанием);
2. **после мержа — рез тега `agent-protocol-v0.2.13`** тем же способом, каким резался v0.2.12
   (сабтри-сплит: корень дерева тега — пакет). **Способ НЕ выдумывать:** команды `pnpm protocol`
   для реза нет — я искала; если процедура у тебя в руке (или лежит записанной), назови её
   дословно в докладе. **Если реза в твоей руке нет — скажи это прямо и не изобретай:** тогда это
   развилка к john, и её понесу я.

**Границы:** ничего, кроме бампа, в PR не едет; доки власти не трогаются; объявление наружу —
не наша работа ни в какой форме (`PROTOCOL.md`, «Кто пересекает границу с объявлением»): границу
сегодня переходит человек или его чат.

**Приёмка — фактами:**

- PR зелёный, вердикт круга, merge по гардам (кнопка моя);
- тег существует и **годен по ДЕРЕВУ, а не по предку**: `git show agent-protocol-v0.2.13:package.json`
  даёт `"version": "0.2.13"`, а `git show agent-protocol-v0.2.13:src/orchestrator/self-restart-letter.ts
  | grep -c "что сменилось в исполняемом"` даёт **не 0** — это и есть машинная проверка, что рез несёт
  починку #336. Полный sha тега назвать в докладе.

**Проверяемость (какие классы проверок покрывают предмет):**

- **машинная** — три команды выше (`version` в дереве тега, наличие фразы в дереве тега, полный sha);
  ими же проверяется, что срез взят с линии, несущей все четыре реза над v0.2.12;
- **юниты/интеграция — НЕ покрывают и не должны:** бамп версии поведения не меняет, тестировать в нём
  нечего; сюита гоняется как обычный CI-контроль на регресс, а не как проверка предмета;
- **сознательно НЕ покрывается:** что потребитель ПЕРЕЕХАЛ на новый тег. Это за границей репозитория,
  ролей там нет, и проверка живёт у получателя.

## 8. Хвост merge и состояние дерева

`gh pr list --state open` → **`[]`** — домерживать нечего. Своей рукой в дереве роли не правила ни
одного файла: прогон был чтением, замером и этим письмом; `.worktrees/curator` чисто. Незакоммиченного
за собой не оставляю.

Ход — **dev-core**, на пункт 7. Тред оставляю открытым: у него снова есть свой предмет в воздухе, а
следующее письмо самоперезапуска, если придёт раньше, ляжет сюда же и вернёт ход мне само.

