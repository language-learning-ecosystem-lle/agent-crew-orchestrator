---
from: curator
worker: claude-code
session: 41e90f8b-ba30-4163-8785-158140dce975
raised: 2026-09-09T20:40:58Z
date: 2026-09-09T20:47:40Z
expects: none
waiting-on: —
---

# Письмо `msg-034` полно во всех шести полях — john НЕ доношу: исполняемое сдвинулось (`cli.ts`, `snapshot.ts`). И отдельно померено: адрес НЕ форкнулся, хотя `176` предсказал форк — письмо легло сюда, потому что `161` открыт и не запаркован

Ход пришёл на письмо самоперезапуска (`msg-034`, `2026-09-09T20-37-25Z-github.md`). Постановка
стоячего адреса — «прочитать и, если отчёт полон, донести john», но слово john ~2026-09-08T14:32Z
(ленты `173`/`175`) заменило ack'и на условие: **звонить, только если письмо пришло на перезапуске
БЕЗ смены исполняемого.** Ниже — замер, потом развилка, потом два факта поля, которых постановка не
ждала.

## 1. Шесть полей письма против записи ящика — сходятся

Источники: `.orchestrator/self-restart.json`, `.orchestrator/daemon-code.json`,
`.orchestrator/self-restart-letters.json`, `.orchestrator/daemon.log` (эпоха + строка решения),
`ps` по живому pid, `git log`/`git diff --name-only` в контуре.

| поле письма | сказано | померено |
|---|---|---|
| контур | `/home/lle/projects/agent-crew-orchestrator` | `daemon-code.json.checkout` — то же |
| какой код был | `c1b95f155880` | `self-restart.json.from` = `c1b95f1558809d433b5b31df2cd0e63951ee77d7` |
| какой стал | `61a79818f119` | `target` = `61a79818f1190cd97ed86bd8ea4cb57ad7c540c4` |
| сколько отставал | 1 коммит | `behind: 1`; `git log c1b95f155..61a79818f` — ровно один, `#362` (тред 177) |
| сколько ждал сессии | 6 мин (410 с) | `drainSince 20:30:06Z` → `at 20:36:56Z` = **ровно 410 с** |
| когда пошёл | `2026-09-09T20:36:56Z` | `at` — то же, `went: true`, `attempts: 1` |
| сдвиг отпечатка | да, 6 путей, 5 названо + «…» | дифф — 7 путей, `docs/protocol-reference.md` отброшен верно (вне пакета), отпечаток = **6** |

Обрезка объявленная (потолок `NAMED = 5`, `self-restart-letter.ts:184`); необъявленный шестой —
`packages/agent-protocol/src/roles/zones.process.test.ts`. Дефектом не считаю по прежнему основанию:
из названного sha список выводится одной командой.

**Замок повтора — этого эпизода:** `self-restart-letters.json.signature` =
`"61a79818f119…\0 2026-09-09T20:36:56Z"`, `at: 2026-09-09T20:37:26Z`, то есть подпись именно этого
перезапуска, а не соседнего.

## 2. Развилка слова john: исполняемое СДВИНУЛОСЬ → звонка нет

`git diff --name-only c1b95f155880..61a79818f119` — 7 путей, и исполняемых среди них **два**:

- `packages/agent-protocol/src/cli.ts`;
- `packages/agent-protocol/src/orchestrator/snapshot.ts`.

Остальное — `packages/agent-protocol/README.md` и три теста
(`daemon.priority.process.test.ts`, `snapshot.test.ts`, `zones.process.test.ts`) плюс отброшенный
`docs/protocol-reference.md`.

**Значит это НЕ новый предмет**, и по слову john приёмка второй раз не спрашивается: ни парка, ни
`--expects ack`. Ход отпускаю никому. Единственное срабатывание условия за весь адрес — `msg-021` в
`176` (#361, оба пути диффа тестовые); после него это **третий эпизод подряд**, на котором условие
молчит (`msg-022`, `msg-023`, это письмо). Сам тот предмет стои́т запаркованным на john развилкой из
`176` (письмо curator 20:20:42Z), и **этим ходом я его не трогаю: прогон связан ровно тредом `161`.**

## 3. Ящик исполняет новый код — мерено ПРОЦЕССОМ, а не деревом

- `ps -o pid,lstart -p 3668528` под `TZ=UTC` → старт **20:37:07Z**; командная строка —
  `node … tsx … /home/lle/projects/agent-crew-orchestrator/packages/agent-protocol/src/cli.ts
  orchestrator up --foreground --ref origin/main --instance hetzner`;
- HEAD того чекаута = `61a79818f1190cd97ed86bd8ea4cb57ad7c540c4`, дерево чисто
  (`git status --porcelain` пуст) — то есть исходник, который процесс импортирует, и есть цель;
- `daemon-code.json` — `sha 61a79818f119…`, `pid 3668528`, `startedAt 20:37:16Z`;
- заголовок эпохи в `daemon.log` — `2026-09-09T20:37:08.767Z · pid 3668528`. **Подъём преемника —
  12,8 с** после `went` (ряд прежних эпизодов 12–13 с: 12,6 / 12,9 / 12,3).

Установленную копию (`node_modules`) руками не мерила: `#352` намеренно её не мерит, и письмо этого
больше не утверждает — мерила исходник, который демон исполняет.

## 4. «Дождался живых сессий, ни одной не порвал» — подтверждено строкой решения

`daemon.log`: `SELF-RESTART: the loaded code is behind 61a79818 (1 commit(s)), **leases 0, state
clean** — running 'restart --pull' as this box would by hand (attempt 1/2)`. Ноль лизов на момент
решения — это и есть «дождался»; тики дренажа перед ним печатали «this tick launches NOTHING … the
box is draining to restart».

**Жизнь дрейфа:** коммит `61a79818` датирован `20:29:32Z`, дренаж записан `20:30:06Z` = **34 с**
(ряд 9–40 с). Перезапуск пошёл через 410 с = **5,7 % порога** `CODE_DRIFT_OVERDUE_MINUTES = 120`.
**Пятнадцатый эпизод подряд под порогом:** механизм звонка о просроченном дрейфе (тред 141, #301)
этим адресом по-прежнему не проверяется по построению.

## 5. Адрес НЕ форкнулся — предсказание `176` закрыто фактом

Письмо curator в `176` (20:20:42Z, §5) поставило парк на john, назвав его ценой прямо: «следующее
письмо самоперезапуска ляжет НЕ сюда, а в новый приёмник, который откроет машина», и ожидало рождения
`187`. **Этого не случилось: письмо легло в `161`.** Причина машинная и читается в той же строке
кода, на которую `176` и ссылался — `--ensure-thread` берёт «whichever of its receivers is **open and
unparked** (opening one if none is)»: `161` открыт и не запаркован с моего письма 15:18:55Z, поэтому
открывать было нечего.

Что из этого следует и чего НЕ следует:

- **следует:** цена парка на стоячем адресе ограничена — форк рождается не от парка как такового, а
  от парка ПОСЛЕДНЕГО открытого незапаркованного приёмника. Пока жив хоть один такой тред, парк
  соседнего приёмника форка не даёт;
- **не следует:** развилка `176` этим не снята. Она не про «родится ли `187`», а про то, куда едет
  предмет «перезапуск ради неисполняемого коммита», и решает её john. Один из трёх названных там
  доводов (§5, «парк = форк») в этой конфигурации не сработал — и только он.

**Поэтому я снова не паркую `161` и снова отпускаю ход никому:** живой незапаркованный приёмник —
единственное, что сейчас удерживает адрес от разрастания, и он теперь ровно один.

## 6. Факт поля, которого постановка не ждала: ПЕРВЫЙ подъём на это письмо сожгла квота

Это письмо подняло пару `curator×161` дважды, и первый подъём не доставил ничего:

- сессия `f50d6227-15d7-4bb9-9fc0-779c6d796da2`, лиз с **20:38:15.824Z**, аккаунт **`shik-main`**
  (`/home/aco-hetzner/.claude-second`). В `init` уже `rate_limit_event`:
  `status: allowed_warning`, `rateLimitType: five_hour`, `utilization: 0.98`; дальше —
  `status: rejected`, `overageStatus: rejected`, `overageDisabledReason: org_level_disabled`;
- вердикт демона о ней дословно: **`the run finished: quota-exhausted`**. В ленту она не написала
  ничего (последнее письмо curator в треде до этого — моё от 15:18:55Z);
- мой подъём — лиз с **20:40:58.362Z**, и демон назвал аккаунт другим: **`lle-main` (failover,
  `/home/aco-hetzner/.claude`)**. Окно `five_hour` у `shik-main` отпускает в
  **`2026-09-09T21:20:00Z`** (`resetsAt: 1788988800`).

Два вывода, и оба — факты, а не просьбы: **отказ по квоте отличён демоном от решения сессии**
(«continuation — fresh: the previous run ended as 'quota-exhausted' — that is exhaustion or a
decision, not an external abort»), и **failover аккаунта в поле сработал** — доставку сделал он.
Деньги и аккаунты — зона john; своей рукой здесь не трогаю ничего и ни о чём не прошу, только
называю замер.

## 7. Чего этим ходом НЕ сделано

- **парк и вопрос в `176` не перемеряны**, `173`/`175` не гашены — прогон связан одним тредом;
- **хвост готовых PR не мету:** след гарда 5 обязан лечь в ленту треда-владельца, а своего PR у
  этого треда нет. Отдельно называю **#350 (`curator/143-card-letopis-to-journal`, тред 143)** — он
  мой, открыт, и его кнопка ждёт подъёма пары на тред 143;
- **потолок памяти роли не починен третий ход подряд** — демон напечатал его и в этом подъёме:
  индекс `curator` — 27868 байт против потолка 24576. Не чиню и говорю почему, а заодно кладу замер,
  чтобы следующий ход не выводил его заново: **136 строк, 137 заметок, средняя строка 139 символов,
  самая длинная 171** — выбросов нет, то есть «подрезать крючки» потолка не закрывает: чтобы снять
  3292 байта, надо УДАЛИТЬ порядка тринадцати заметок, а каждое удаление — суждение «предмет закрыт,
  знание больше не нужно», и вслепую оно необратимо. Это работа целого хода, а не хвост чужого:
  оплачивается она КАЖДЫМ подъёмом любой пары curator, поэтому долг называю прямо.

**Два факта поля (§5 и §6) положены в память роли** — дописаны в ТЕЛА уже существующих заметок, без
новой строки в оглавлении: потолок из-за этого хода не вырос ни на байт.

**Незакоммиченного за собой не оставляю:** ни одного файла репозитория этим ходом не правила —
только читала и мерила; рабочее дерево `.worktrees/curator` чисто. Тело письма — в своём
`mktemp -d -p /tmp`, вне чекаута почты и вне дерева роли.

waiting-on: ход не передаю никому — предмет письма закрыт замером, а стоячий адрес будит письмо
ИЗВНЕ (следующий самоперезапуск или слово john по развилке `176`).

