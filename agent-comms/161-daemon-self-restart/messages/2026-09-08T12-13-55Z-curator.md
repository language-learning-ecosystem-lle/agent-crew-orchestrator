---
from: curator
worker: claude-code
session: 5f1518d5-0c2f-47a5-87b0-66760e41ea30
raised: 2026-09-08T12:09:19Z
date: 2026-09-08T12:13:55Z
expects: ack
waiting-on: curator
parked-on: john
---

# john, доношу ЦЕЛОЕ, как и обещала в msg-011 §4: сужение письма влито (#336), бамп влит (#337), тег `agent-protocol-v0.2.13` = `6a1574f8` (protocolVersion 26) лежит на `origin`, и измеренная ветвь подтверждена уже ДВАЖДЫ в поле — второй эпизод сильнее первого: приехало 8 путей, письмо назвало 7, отброшен ровно доковый. Одна развилка твоя, не блокирующая: **расширять ли форму письма тремя полями (§4) — это НОРМА, а не починка, поэтому спрашиваю, а не ставлю**

Ход был на два письма: отчёт ящика о третьем самоперезапуске (`github`, штамп 2026-09-08T12:01:38Z)
и доклад `dev-core` о резе. Оба разобраны ниже фактами, снятыми моей рукой в главном чекауте, а не
пересказом. Предмета в воздухе у треда больше нет — поэтому звонок, отложенный мною на один такт
намеренно, ставлю этим письмом.

## 1. Семь полей письма против состояния ящика — каждое со своим источником

`.orchestrator/self-restart.json` дословно:
`{"target":"5b9795aea461d9e9fb5abe0ecaa9822b16002192","attempts":1,"at":"2026-09-08T12:01:10Z","from":"a632d95ee2b347254233d5801910b40ed0cba6d0","behind":1,"drainSince":"2026-09-08T11:56:52Z"}`

| поле письма | значение | чем подтверждено |
|---|---|---|
| контур | `/home/lle/projects/agent-crew-orchestrator` | `.orchestrator/daemon-code.json` → `checkout` |
| какой код был | `a632d95ee2b3` | `self-restart.json` → `from: a632d95e…0cba6d0` |
| какой стал | `5b9795aea461` | `daemon-code.json` → `sha: 5b9795ae…002192`, и он же `git rev-parse origin/main` |
| сколько отставал | 1 коммит | `self-restart.json` → `behind: 1`; журнал: «the loaded code is behind 5b9795ae (1 commit(s)), leases 0, state clean» |
| сколько ждал сессии | 4 мин (258 с) | `drainSince 11:56:52Z` → `at 12:01:10Z` = **ровно 258 с**, посекундно |
| когда пошёл | 2026-09-08T12:01:10Z | `self-restart.json` → `at` |
| что сменилось в исполняемом | 7 путей | §2 — это не сверка, а замер, и он самый ценный в письме |

**Расхождений — ноль.** Попытка ПЕРВАЯ (`attempts: 1` из 2), и это в письме по-прежнему не названо (§4).

**Перезапуск состоялся, а не объявлен** — тремя уликами: `daemon-code.json` → `sha 5b9795ae…`,
`startedAt 2026-09-08T12:01:28Z`, `pid 2922958`; живой процесс `ps -o pid,etimes,lstart,cmd -p 2922958`
→ `…/packages/agent-protocol/src/cli.ts orchestrator up --foreground --ref origin/main --instance
hetzner`, старт 14:01:21 местного = **12:01:21Z**; письмо (12:01:38Z) написано преемником, а не
умирающим процессом. Главный чекаут: `git rev-parse HEAD` = `5b9795ae…`, `git status --porcelain` пуст.

## 2. Сужение подтверждено ВТОРЫМ полевым эпизодом, и он СИЛЬНЕЕ первого

Первый эпизод (msg-002 §2) дал 5 путей в диффе против 4 в письме. Этот — крупнее и однозначнее:

- `git diff --name-only a632d95e 5b9795ae` → **8 путей** (`docs/protocol-reference.md`,
  `packages/agent-protocol/README.md`, `src/cli.ts`, `src/fs/body-location.ts` + тест,
  `src/fs/git-env.ts`, `src/thread/new-message.process.test.ts`, `src/thread/new-thread.process.test.ts`);
- письмо назвало **7 путей**, и единственный отброшенный — **`docs/protocol-reference.md`**.

То есть классификатор второй раз отработал на настоящем смешанном диффе и второй раз отбросил ровно
доковый путь. Это уже не единичный случай, а повторённое поведение — и оно ровно то, ради чего #336
писался.

**Многоточие в письме («…») — не обрезанный замер, а объявленный предел показа:** `NAMED = 5` в
`self-restart-letter.ts`, `describeExecutablePaths` печатает первые пять и «…», а ЧИСЛО путей
(«7 путей») остаётся честным и полным. Прочитано в исполняемом сейчас исходнике, не предположено.

**Тихая ветвь (WITHHELD) в поле по-прежнему НЕ доказана** — и этот эпизод объясняет, почему её так
трудно поймать: чтобы письма не было вовсе, дифф обязан не иметь НИ ОДНОГО пути под
`packages/agent-protocol` и не трогать манифесты, а такой рез (чисто доковый) с момента мержа #336 не
случился ни разу. Это состояние, а не задача; ждать его надо в журнале — в почте его нет по построению.

Дедупликация жива: письмо доставлено один раз (12:01:38Z), каждый следующий тик печатает
`letter — SUPPRESSED, nothing new to say … at 2026-09-08T12:01:38Z`.

## 3. Тег принят МОЕЙ рукой — четыре факта приёмки msg-002 §5, перемерены заново

Доклад `dev-core` (msg-004) я не принимаю пересказом; перемерила сама:

1. `git ls-remote --tags origin agent-protocol-v0.2.13` → **`6a1574f8cdd4a699ddcbf5dc62ef928a1eff39ad`**;
2. `git show agent-protocol-v0.2.13:package.json` → `"version": "0.2.13"`;
3. `git show agent-protocol-v0.2.13:src/orchestrator/self-restart-letter.ts | grep -c "что сменилось в
   исполняемом"` → **3**; контроль шаблона: тот же грep по `agent-protocol-v0.2.12` → **0**. Сужение
   #336 в теге ЕСТЬ, и ноль у соседа доказывает, что шаблон различает;
4. `git show agent-protocol-v0.2.13:src/schema/version.ts` → `CURRENT_PROTOCOL_VERSION = 26`.

Годность дерева померил исполнитель (дерево тега `3754ae26` найдено среди 393 деревьев линии,
принадлежит коммиту `a632d95e` = #337) — принимаю: способ ровно тот, что я и требовала, ДЕРЕВОМ, а не
предком. **Итоговой строки скрипта у нас нет ни у кого**: скрипт отказывает над существующим тегом
раньше её печати, и `dev-core` честно назвал свою подстановку реконструкцией, а не снятым выводом.
Числа, ради которого строка просилась, это не лишает: `protocolVersion 26` замерен из дерева тега.

**Чего тег НЕ несёт, чтобы никто не прочёл «v0.2.13 = текущий main»:** он срезан с `a632d95e`, а линия
ушла на `5b9795ae` — дверь места тела письма (#338, тред 170) в теге НЕТ, она поедет следующим бампом.

## 4. Развилка john — ОДНА, и она про НОРМУ, а не про починку

Три числа этого класса эпизодов живут только в журнале и файлах состояния, а в письме их нет:

1. **полная задержка от мержа**, а не только ожидание сессий (у прошлого эпизода: merge 11:09:33Z →
   код жив 11:20:29Z = 656 с против названных письмом 601 с — числа честные, но точка отсчёта разная);
2. **номер попытки** (`attempts: 1` из 2) — читателю письма не виден вовсе;
3. **цена очереди**: такт перезапуска не поднимает НИЧЕГО, и пары остаются преемнику (в этот раз —
   `dev-core×161-daemon-self-restart`, `curator×159-thread-number-has-no-door`).

Своей рукой я это не ставлю: **новое поле в форме сообщения — норма** (карточка роли, «Нормой
считается всё, что меняет поведение контура за пределами починки: новое поле… новая форма сообщения»),
а не починка, и полевой класс её не покрывает. Отсюда вопрос ровно один: **стоят ли эти три поля
своего расширения формы, или письмо остаётся как есть?** Молчание я прочту как «остаётся», и предмет
умрёт вместе с треадом — переспрашивать я не буду.

## 5. Почему звонок именно сейчас, и почему парк ничего не глотает

- **Отложила я его на один такт намеренно** (msg-002 §4), потому что парк замораживает тред, а от
  john рез тега не зависел никак. Теперь резать нечего: предмет закрыт;
- **звонок цел и звенит СВЕЖЕЙ строкой:** в `.orchestrator/notify.state` блока `161` /
  `daemon-self-restart` нет ни под `parked`, ни под `asked` — контроль, что читаю верно: соседние ключи
  того же файла видны (`parked john 029/143/155`, `asked john 155/159`);
- **парк не глотает следующий самоперезапуск.** Прочитано в двери, а не в памяти: `--ensure-thread`
  берёт приёмник, который «OPEN AND NOT PARKED», и «when there is none — closed, parked, unreadable,
  or never opened — the receiver is OPENED here». То есть припаркованный 161 форкает новый приёмник, и
  следующий эпизод поднимет curator сам.

## 6. Маршрутная находка: одна постановка жила в ДВУХ тредах, и дубль всё ещё в очереди

Замерено на ленте (`git show origin/comms:./<тред>/_meta.md`):

- **`170-cut-the-tag-with-the-letter-fix` — `status: closed`** (там рез и был исполнен и принят);
- **`171-cut-the-tag-with-the-letter-fix` — `status: open`**, тот же заголовок дословно, и он стои́т в
  очереди ящика `dev-core×171`, priority high, ждёт с 11:33:51Z — **на предмет, которого уже нет**.

Мой пробел здесь настоящий и я его называю: постановку реза я поставила в 161 (msg-013 §7), не
проверив, что тот же предмет уже несут два других треда с одинаковым слагом. Цена — целый такт
`dev-core`, ушедший на обнаружение этого, и ещё один впереди. Чужой тред отсюда не гашу: прогон связан
ровно тредом 161, а след приёмки обязан лечь в ленту владельца. Действие по 171 — подъём на НЁМ
(сессия прочтёт закрытый 170, доложит и закроет); если предпочтёшь, чтобы я сняла ему priority своей
рукой отдельным подъёмом — скажи, право `thread-priority` у меня есть.

## 7. Хвост merge и состояние дерева

`gh pr list --state open` → **#340** (тред 159, сторож звонит на два треда) и **#339** (тред 155, парк —
свойство треда). Своими кандидатами они мне не приходятся: след гарда 5 обязан лечь в ленту
треда-владельца, а этот прогон связан тредом 161. Кнопки по ним — отдельные подъёмы на 155 и 159.

Своей рукой в дереве роли не правила ни одного файла: такт — чтение, замеры и это письмо.
`.worktrees/curator` чисто, незакоммиченного за собой не оставляю; тело письма лежит в `/tmp/aco-161-H6Z684/`,
в НАСТОЯЩЕМ `/tmp` — вне чекаута почты и вне рабочего дерева роли (дверь #338).

Ход — **curator**: вопрос §4 несу я, тред паркую на john. Тег объявляет наружу человек или его чат —
строкой `agent-protocol-v0.2.13` = `6a1574f8cdd4a699ddcbf5dc62ef928a1eff39ad`, пакет
`agent-protocol@0.2.13`, `protocolVersion 26`.

