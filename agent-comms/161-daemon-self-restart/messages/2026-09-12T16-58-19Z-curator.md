---
from: curator
worker: claude-code
session: a282da01-304e-4676-b5ad-297f1574591b
raised: 2026-09-12T16:54:27Z
date: 2026-09-12T16:58:19Z
expects: none
waiting-on: —
---

# Третий самоперезапуск за 74 минуты: отчёт полон, исполняемое СМЕНИЛОСЬ (`tick.ts` — от точки входа), звонка отсюда нет. Новое против двух прежних эпизодов: придержанная сливом пара ДОЧИТАНА преемником — обещание проверено фактом

Ход пришёл на `msg-056` (письмо `github`, 16:53–16:54Z). Это третий самоперезапуск подряд в одном
часе: `ec166bc6→95ba1698` разобран в `msg-053`, `95ba1698→bbab2d9a` — в `msg-055`, этот ушёл на
`316991aa`. Развилка john закрывается замером в ту же сторону, что и дважды до этого.

## 1. Развилка john: исполняемое СМЕНИЛОСЬ — мерено диффом и точкой входа

`git diff --numstat bbab2d9a2673..316991aab9d7` — **6 файлов** (`+111/−8`). Вне пакета ровно один
(`docs/protocol-reference.md`, `+14/−0`), отброшен ВЕРНО, и **6 − 1 = 5** — то самое число, которое
назвало письмо; имена совпадают пофайлово (`README.md`, `daemon.process.test.ts`, `stall.test.ts`,
`tick.test.ts`, `tick.ts`). Среди пяти НЕ тест — два:

- `packages/agent-protocol/src/orchestrator/tick.ts` (`+13/−1`) — правлен `describeSkip`
  (`tick.ts:720`), ветка `exhausted`: счёт попыток и потолок закавычены (`'4' failed attempts …
  ceiling '3' ('default')`). **Файл лежит на живом пути, и доказывать это импортом не долго:**
  `cli.ts:579` — `} from "./orchestrator/tick.js";`, а `cli.ts` печатает сам `ps` в командной строке
  ящика;
- `packages/agent-protocol/README.md` (`+2/−2`) — пример в описании подогнан под новую форму строки.

**Условие john («письмо на перезапуске БЕЗ смены исполняемого — новый предмет с новым замером») не
срабатывает**, значит ветвь нормальная: доклад в ленту, ни парка, ни `--expects ack`. Приёмка
самоперезапуска подтверждена полем и второй раз не спрашивается — слово john ~14:32Z 2026-09-08.

**Честная граница этого замера, и она новая:** *сменившееся* — не значит *наблюдаемое отсюда*.
Правленая ветка `exhausted` в поле этого контура сейчас НЕ печатается ни разу: пара, вышедшая из
попыток, в очереди ровно одна (`curator×143-role-card-is-the-second-line`, `⛔ OUT OF ATTEMPTS — 4 of
3`), и она же **запаркована за решением john**, а парк выигрывает причину пропуска — печатается
`parked`, а не `exhausted`. Анкерованный греп по `^agent-protocol: candidate` после перезапуска даёт
45 строк пропусков и НОЛЬ строк `exhausted`. Так что новая форма строки здесь мерена диффом и
тестами PR, а не журналом ящика; журналом мерено другое — что файл загружен (ветка `parked` того же
`describeSkip` печатается преемником).

## 2. Отчёт полон: семь полей, вычисляемое сходится точно, цепь не рвётся

```
{"target":"316991aab9d772b5c3424bf930af9b6934f5bfdb","attempts":1,"at":"2026-09-12T16:53:14Z",
 "from":"bbab2d9a2673b937bbcdac6518576b0f6d414b91","behind":1,"drainSince":"2026-09-12T16:52:02Z","went":true}
```

Поле в поле: `from` = «какой код был», `target` = «какой стал», `behind: 1`, `at` = «когда пошёл
16:53:14Z», `went: true`, `attempts: 1` (журнал: `attempt 1/2`). **«Ждал сессии 1 мин (72 с)»:
16:53:14Z − 16:52:02Z = 72 с ровно.** **Цепь не рвётся:** `from` этого письма = `target`
предыдущего (`msg-056` ← `msg-054`, `bbab2d9a2673`).

**Ящик исполняет новый код — мерено процессом, а не деревом:** `daemon-code.json` — `sha
316991aab9d7…`, `pid 3335141`, `startedAt 16:53:34Z`; `ps -o pid,ppid,lstart,etime,cmd -p 3335141` —
процесс ЖИВ, поднят **16:53:25Z** (11 с после решения — третий эпизод подряд с тем же зазором),
`PPID 1019` (`systemd --user`), точка входа `…/packages/agent-protocol/src/cli.ts orchestrator up
--foreground --ref origin/main --instance hetzner`. **Дрейфа сейчас нет, мерено с двух сторон:**
`git rev-parse origin/main` = `316991aab9d7…` = исполняемый sha, и `git ls-remote origin
refs/heads/main` даёт тот же sha. **Письмо легло РОВНО одно:** `self-restart-letters.json` — подпись
`316991aab9d7…\0 2026-09-12T16:53:14Z`, `at 16:53:44Z`; дальше журнал печатает только `letter —
SUPPRESSED, nothing new to say … already posted … at 2026-09-12T16:53:44Z`.

## 3. Что нового дал ЭТОТ эпизод: придержанная пара действительно дочитана преемником

Дрейф завёл **#374** («вымерзшие пары схлопываются в ОДНУ причину», ветка
`dev-core/180-exhausted-folds-to-one`, тред `180`), `mergeCommit 316991aab9d7…`, `mergedAt
16:51:29Z`. Руку `mergedBy` не называет (`maysway` у обеих кнопок), но живую сессию журнал называет
ПОИМЁННО, и это **третий раз подряд сессия МОЕЙ роли**: `waits for the sessions that are live
(curator/180-selfheal-leaves-the-workspaces-behind)`. Цифры: merge `16:51:29Z` → `drainSince
16:52:02Z` (**+33 с**; было 23 с и 38 с — пример, не константа) → ход пошёл `16:53:14Z` → преемник
`16:53:25Z`. **Весь цикл кнопка→новый процесс — 116 с.**

**Слив придержал пару, и обещание журнала на этот раз ПРОВЕРЕНО** — в прошлом эпизоде проверить его
не удалось (придержанная пара к моменту ремонта ушла из очереди по своей ленте, `msg-055`, п. 3).
Здесь оба конца сошлись:

```
SELF-RESTART: this tick launches NOTHING (dev-core×180-… stay in the queue for the successor) — the
handover was judged on zero leases and a session started now would make this daemon drain for as long
as it lasts, outliving the repair's wait
```

— и **первая же строка плана у преемника**: `the plan of this tick: 1 launch, one per free role —
dev-core×180-selfheal-leaves-the-workspaces-behind` (в очереди преемника — строка 3/12, `waiting
since 2026-09-12T16:51:52Z`, то есть тот же кандидат, что придержан, со своим прежним штампом
ожидания). **Пара сливом не потребляется и не теряется** — она дочитывается новым процессом, ровно
как обещает комментарий ветки (`self-restart.ts`, пришла с `#297`, тред `141`; новой нормы нет).
Перенесено в память роли: заметка `own-merge-starts-a-drain-that-waits-for-me` получила третий случай
с этими цифрами и этим доказательством. **Цена по потолку памяти: +0 б** — правлено только тело,
крючок в оглавлении не тронут (`MEMORY.md` до и после — `29 929` б).

Установка пропущена законно: `pnpm install skipped — none of package.json, pnpm-lock.yaml,
pnpm-workspace.yaml differs across bbab2d9a..316991aa`. Выход — код `75` для супервизора,
`INVOCATION_ID` установлен, ремонт на месте, `leases 0, state clean`.

## 4. Чего этим ходом НЕ сделано — поимённо

- **тред не закрываю и не паркую.** Приёмники стоячего адреса перечислены по ленте почты только что
  (`git ls-tree origin/comms` по маске `*-daemon-self-restart` + `status:` из `_meta.md`) — их **12**,
  открытых **два**: `161` и `176`. `176` запаркован за решением john (очередь ЭТОГО такта: `⏸ PARKED
  behind a decision of john (R27)`, `waiting since 2026-09-09T19:50:25Z`). Значит **`161` по-прежнему
  единственный открытый и незапаркованный приёмник** — парк или закрытие здесь форкает адрес;
- **хвост готовых PR не мержу.** Открыто **три** (`gh pr list --state open`, 16:56Z): `#371` (тред
  188, метка `review`), `#370` (187, `review`), `#353` (180, `review`, открыт с `2026-09-09T14:05Z`).
  У `#371` и `#370` ящик сам печатает `guards 1-2 hold`, но оба треда стоят `⏸ PARKED behind a
  decision of john` — гард 3 у человека. **Ни один PR не принадлежит треду `161`**, а след гарда 5
  обязан лечь в ленту треда-владельца, прогон же связан ровно `161`. `#374` и `#373` за этот час
  смёржены и из хвоста ушли;
- **потолок памяти роли не починен:** `MEMORY.md` = **29 929 б против 24 576 (121,8 %)**, **146
  заметок** — без изменений с 16:38Z. Развилка прежняя (`memoryIndexAlarmFor` в конфиге, кнопка
  john), и отсюда её не поставить: парк последнего незапаркованного приёмника форкает адрес, а нового
  треда этот прогон не заводит;
- **пара `curator×143-role-card-is-the-second-line` вымерзла и отсюда не чинится** — `⛔ OUT OF
  ATTEMPTS — 4 of 3 failed`, поднять её может только рука (`orchestrator run --max-attempts` выше
  потолка). Называю фактом, потому что померила его этим ходом; предмет чужой (тред `143`, и он к
  тому же запаркован за решением john), и ставить по нему отсюда нечего.

## 5. Куда идёт ход

**Никому** (`waiting-on: —`). Это измеренное «незачем»: семь полей сошлись, исполняемое сменилось,
условие john не сработало, ремонт уже сделан ящиком, звонка отсюда нет. Следующее письмо в эту ленту
напишет сам ящик, когда снова сядет на новый код, — `161` открыт и незапаркован специально ради этого.

**Незакоммиченного за собой не оставляю:** ни одного файла репозитория этим ходом не правила — читала
ленту, мерила дифф, импорт, запись, процесс, журнал ящика (анкерованным грепом: без `^` он отдаёт мой
же вывод), очередь преемника, `_meta.md` всех 12 приёмников и открытые PR. Правлен один файл ПАМЯТИ
РОЛИ (`.orchestrator/memory/curator/own-merge-starts-a-drain-that-waits-for-me.md`, только тело) —
каталог `.orchestrator/` в `.gitignore`, на ветку почты его синхронизирует сам демон концом хода;
перечитан с диска после записи. `git status --porcelain -uall` пуст и в главном чекауте, и в дереве
пары. Тело письма — в своём `mktemp -d -p /tmp`, вне чекаута почты и вне дерева роли.

