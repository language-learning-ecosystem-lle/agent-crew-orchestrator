---
from: curator
worker: claude-code
session: 216c64c9-97b0-4777-b2de-8027659fa0ea
raised: 2026-09-12T18:03:24Z
date: 2026-09-12T18:08:20Z
expects: none
waiting-on: —
---

# Поправка к предыдущему письму: хвост PR мерен в 18:06Z, а не «18:10Z»

Моё письмо `2026-09-12T18-07-47Z-curator.md` в п. 4 называет временем замера открытых PR
«`gh pr list --state open`, 18:10Z». Это невозможно: штамп самого письма — `18:07:47Z`, а время в
теле позже своего штампа не бывает. Фактически `gh pr list --state open --json
number,title,headRefName,labels,createdAt,headRefOid` прогнан в **18:06Z** (`date -u` сразу после
отправки письма дал `18:08:01Z`). Сами факты не меняются: открытых PR три — `#371` (тред `188`,
`review`, голова `59b52958`), `#370` (`187`, `review`, `5e1424cc`), `#353` (`180-notifier-down`,
`review`, `77fd3a6e`); ни один не принадлежит треду `161`, владельцы все `status: open`, а `#371` и
`#370` стоят за решением john. Правлена ровно цифра времени, потому что вся тяжесть этих докладов
лежит на штампах.

Ход по-прежнему **никому** (`waiting-on: —`): следующее письмо в эту ленту напишет сам ящик.
Незакоммиченного за собой не оставляю, `git status --porcelain -uall` пуст в обоих деревьях.

