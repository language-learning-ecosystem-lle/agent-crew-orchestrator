---
from: curator
worker: claude-code
session: add8ae2e-d40b-4754-9452-0054f28e1392
raised: 2026-09-09T23:12:10Z
date: 2026-09-09T23:19:01Z
expects: none
waiting-on: —
---

# Отчёт полон, исполняемое СМЕНИЛОСЬ — по слову john это обычный эпизод, и звонка отсюда нет; сверх ленты предмет потолка памяти перемерен и вывод о нём перевернулся: лекарство тревоги меньше своей цены

Ход пришёл на `msg-050` (новых для меня — `1 of 50`, письмо `github`). Развилка john закрывается в
одну сторону за минуту, и в этот раз в СТАРУЮ: отпечаток несёт настоящий исходник, а не тест.
Поэтому основная работа такта — не письмо, а стоячий предмет памяти роли, который восьмой такт
подряд объявлялся «не сделан».

## 1. Развилка john: исполняемое СМЕНИЛОСЬ — и это измерено, а не выведено из письма

`git diff --name-only 0a451fc10f55..ec166bc6b490` — **четыре пути**, коммит `#369` («парк-режим
носит СВОЙ знак, а не отсутствие чужого», тред 155):

- `docs/protocol-reference.md` — вне пакета, отброшен ВЕРНО;
- `packages/agent-protocol/README.md`;
- `packages/agent-protocol/src/thread/index-doc.ts`;
- `packages/agent-protocol/src/thread/thread.test.ts`.

**Отпечаток — 3 из 4**, ровно то число, которое назвало письмо, и имена совпадают пофайлово.

**Исполняемое среди отпечатка ЕСТЬ, и названо оно не по имени файла, а по импорту:**
`index-doc.ts` втянут в ту самую программу, которая поднимается, —
`packages/agent-protocol/src/cli.ts:714`, `orchestrator/launch.ts:47`, `orchestrator/lease.ts:35`,
`orchestrator/tick.ts:44` (плюс `src/index.ts:297`). Это не тест и не манифест: перезапуск вёз
настоящий код. **Условие john («если письмо придёт на перезапуске БЕЗ смены исполняемого — это
новый предмет с новым замером») здесь НЕ срабатывает**, значит ветвь та же, что в норме: доклад в
ленту, ни парка, ни `--expects ack`. Приёмка самоперезапуска подтверждена полем и второй раз не
спрашивается — слово john ~14:32Z 2026-09-08.

## 2. Отчёт полон: шесть полей сошлись с записью, вычисляемое число сходится точно

```
{"target":"ec166bc6b4906a7221e9080b7881dd0dea159fea","attempts":1,"at":"2026-09-09T23:10:59Z",
 "from":"0a451fc10f55222a06efbb45097125736de33913","behind":1,"drainSince":"2026-09-09T23:07:32Z","went":true}
```

Поле в поле: `from` = «какой код был», `target` = «какой стал», `behind: 1`, `at` = «когда пошёл
23:10:59Z», `went: true`. «Ждал сессии 3 мин (207 с)»: 23:10:59Z − 23:07:32Z = **207 с**, округления
в свою пользу нет. Замок повтора — `self-restart-letters.json`, подпись
`ec166bc6b490…\0 2026-09-09T23:10:59Z`, `at 23:11:27Z`, то есть именно этот эпизод; журнал ящика
после этого печатает только `letter — SUPPRESSED, nothing new to say … already posted … at
2026-09-09T23:11:27Z` — двоения нет, письмо легло РОВНО одно.

**Ящик исполняет новый код — мерено процессом, а не деревом:** `daemon-code.json` — `sha ec166bc6b490…`,
`pid 221042`, `startedAt 23:11:19Z`; `ps -o pid,ppid,lstart,etime,cmd` — процесс ЖИВ, поднят
**23:11:10Z** (11 с после решения, как в прежних эпизодах), `PPID 1019` (`systemd --user`), точка
входа `…/packages/agent-protocol/src/cli.ts orchestrator up --foreground --ref origin/main --instance
hetzner`, то есть исходники контура. Дрейфа сейчас нет: `git rev-parse origin/main` = `ec166bc6b490…`
= исполняемый sha. **Цепь не рвётся:** `from` этого письма = `target` предыдущего (`msg-050` ← `msg-048`,
`0a451fc10f55`).

**«Ни одна сессия не была порвана» — это тоже замер, а не вежливость письма.** Журнал ящика,
такт перед решением: `daemon — SELF-RESTART: this tick launches NOTHING (curator×155-park-has-no-checkable-ground
stay in the queue for the successor) — the handover was judged on zero leases`. То есть ящик увидел
живую сессию, НЕ поднял новых и ждал, пока та кончится сама (`Turn complete` в её же журнале), и
только после этого пошёл. Дрейф, которого он ждал, завела кнопка ЭТОЙ ЖЕ роли: merge `#369` в
23:07:01Z, `drainSince` 23:07:32Z — **31 с спустя**.

## 3. Сверх ленты: предмет потолка памяти перемерен, и вывод по нему ПЕРЕВЕРНУЛСЯ

Предмет стоит с `msg-047` §6 без владеющего треда и с формулировкой «берёт его такт с запасом
окна». Такт с запасом — этот: развилка john закрылась за минуту. Померено:

- `MEMORY.md` = **29 016 б против потолка 24 576 (118,1 %)**, **141 заметка**; цена входа выросла
  до **4 440 б ≈ 22 удалённых заметок** (при средней 205 б);
- лекарство тревога называет дословно: «a note that **has moved into the role card**, or one the
  card now contradicts, is a note that should be **deleted, not shortened**».

**Прежде чем судить 141 заметку по одной, я померила два сита, которые дали бы класс даром. Оба
пусты:**

1. **гниль путей — НОЛЬ.** Цикл по всем 141 файлам: извлечь `(packages|docs|\.github)/…`, проверить
   `test -e` в чекауте. Сработал ровно один путь — `docs/roles/reviewer.md` в
   `verify-the-grep-pattern-not-its-result`, и он ложный: заметка ЦИТИРУЕТ его как пример
   собственной ошибки. Ни одна заметка не ссылается на исчезнувший файл;
2. **поводы (`thread:` во фронтматтере) — НОЛЬ из 141.** Механизм гашения по закрытию треда
   (`planExtinction`) здесь не гасит вообще ничего, и это не забывчивость: право требует проверки
   «предмет закрыт — заметка стала бесполезной?», а куча — про устройство мира.

**И главное — сам класс «уехало в карточку роли» проверен перечислением, а не образцом: взяты ПЯТЬ
кандидатов, чья тема стоит в карточке ДОСЛОВНО, отклонены ПЯТЬ.** Каждый раз одно и то же:
карточка несёт ПРАВИЛО, заметка — МЕХАНИЗМ, которого в карточке нет.

| заметка | что в карточке | что в заметке СВЕРХ карточки |
|---|---|---|
| `statement-of-work-ends-at-the-tag` | «конечная точка — тег и его объявление» (`curator.md:54-56`) | рез — рука РОЛИ, а не кнопка john (msg-014 треда `056`); `split-package.sh:97-99` отказывает по расхождению имени с `version` ДО `git tag` |
| `subagent-inherits-deny-not-allowlist` | «твои запреты записи он НАСЛЕДУЕТ» | держит их `permissions.deny` из `zoneDenyRules`, и читать его надо из argv ЖИВОЙ сессии, а не из кода |
| `park-forms-both-take-the-pr-number` | обе формы названы (`run:<N>`/`pr:<N>`) | обе берут номер PR, а снимаются РАЗНЫМ: `pr:` — только заголовком `merged-pr:` |
| `own-prompt-is-the-free-oracle-for-role-norms` | карточка = «что роли делать» | норму ХОДА роль читает не из дока, а из промпта (`runEndsNorm` в `launch.ts`) |
| `merge-with-match-head-commit` | процедура пяти гардов | `--match-head-commit` в карточке нет вовсе, а он и снимает сверку сорока hex глазом |

**Вывод, и он про ОБЪЁМ, а не про прилежание:** слои разные — карточка держит норму, заметка держит
замер, — поэтому «уехало в карточку» бывает единичным совпадением, но КЛАССОМ не бывает; 22 таких
совпадения из 141 взять неоткуда. Значит предмет не «руки не дошли»: **лекарство, названное самой
тревогой, меньше своей цены.** Развилка («поднять потолок / принять постоянный перевес / другой
механизм») операционной не является — `memoryIndexAlarmFor` живёт в конфиге, то есть это кнопка
john.

**Звонить об этом ОТСЮДА нельзя, и причина та же, что в `msg-049` §3:** парк последнего
незапаркованного приёмника стоячего адреса форкает адрес. Вопрос ставится из треда, у которого есть
свой ход, — я его отсюда не завожу (тред заводится постановкой, а прогон связан ровно `161`).
Всё, что перенесено через границу, перенесено единственным инструментом, который читает КАЖДЫЙ
подъём роли: заметка `memory-ceiling-measures-the-index` получила «Следствие 5» с обоими ситами,
таблицей пяти отклонённых кандидатов и выводом выше.

**Честная цена этого хода по потолку: +23 б** (29 016 → 29 039). Тело заметки в оглавление не
входит, но крючок я переписала («оба механических сита пусты, лекарство меньше цены: развилка к
john») — он на 23 б длиннее прежнего. Обмен считаю правильным: без него следующий такт потратит
окно на уже перечисленное, а именно это и происходило восемь тактов подряд.

## 4. Чего этим ходом НЕ сделано — поимённо

- **тред не закрываю и не паркую.** Замер по ленте почты (`git show origin/comms:./<тред>/_meta.md`,
  все 12 приёмников адреса): `003`, `156`, `160`, `162`, `163`, `164`, `165`, `167`, `173`, `175` —
  `closed`; `176` — `open`, но запаркован на john с 20:20:42Z (`notify.state`, строка `parked` без
  второго штампа = не снят); `161` — `open` и незапаркован. **`161` по-прежнему единственное, что
  удерживает адрес от форка**;
- **хвост готовых PR не мету.** Дешёвая половина померена (`gh pr list --state open`, 23:14Z):
  открыт РОВНО ОДИН — `#353` (тред `180-notifier-down`, метка `review` висит). Он не мой кандидат:
  след гарда 5 обязан лечь в ленту треда-владельца, а прогон связан `161`. `#367` и `#369` смёржены
  (последний — этой же ролью в 23:07:01Z и есть причина этого перезапуска);
- **потолок памяти не починен** — §3: он перемерен, класс лекарства перечислен и опровергнут,
  развилка названа, но она к john и не из этого приёмника.

## 5. Куда идёт ход

**Никому** (`waiting-on: —`). Это измеренное «незачем»: отчёт полон, поля сошлись, исполняемое
сменилось, условие john не сработало и звонка не требует. Ход у john стоит по другим адресам
(`176` с 20:20:42Z и ещё пять парков в `notify.state`), а следующее письмо в эту ленту напишет сам
ящик, когда сядет на новый код, — `161` открыт и незапаркован специально ради этого.

**Незакоммиченного за собой не оставляю:** ни одного файла репозитория этим ходом не правила —
читала ленту, мерила дифф, импорты, запись, процесс, журнал ящика и `_meta.md` приёмников. Правлены
два файла ПАМЯТИ РОЛИ (`.orchestrator/memory/curator/`: `memory-ceiling-measures-the-index.md` и
`MEMORY.md`) — каталог `.orchestrator/` в `.gitignore`, в дерево репозитория они не входят, на ветку
почты их синхронизирует сам демон концом хода. Оба перечитаны с диска после записи — это не
педантизм: 2026-09-09 в 22:32Z правку памяти этой роли молча затёр одновременный подъём ЭТОЙ ЖЕ
роли из чужой пары (`msg-047` §6-бис). `git status --porcelain -uall`
главного чекаута и дерева пары — пуст. Тело письма — в своём `mktemp -d -p /tmp`, вне чекаута почты
и вне дерева роли.

