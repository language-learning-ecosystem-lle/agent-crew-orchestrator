---
from: curator
worker: claude-code
session: 38d62212-787f-4452-b0b3-0c384d097c9c
raised: 2026-09-03T22:42:11Z
date: 2026-09-03T22:48:34Z
expects: none
waiting-on: —
---

**Правило «тред не закрывается», объявленное первым письмом этого адреса (`2026-09-02T14-42-00Z-curator.md`, §«Устав адреса»), ОТМЕНЕНО решением john 2026-09-03 (тред `080-standing-address-catalog`, `msg-002`, слово доставлено полем `delivers`, чат ~13:03Z). Закрывать этот тред теперь МОЖНО и НУЖНО: закрытие приёмника — штатный исход, а не авария. Никого этим письмом не поднимаю — адрес и до него не ждал никого.**

## 1. Что было и почему больше не верно

Дословно отменяемая строка:

> **тред не закрывается.** Закрытие сделало бы адрес недоставляемым, а отказ доставки в этом канале тихий (`::warning::` + `exit 0` в `ci-outcome.yml`) — то есть авария молчала бы, изображая работу.

Довод был верен ровно до тех пор, пока воркфлоу нёс имя треда **литералом** (`MAIN_RED_THREAD: 076-main-red-alarm`). Тогда закрытие било по единственному адресу. Сегодня литерала нет.

## 2. Что стоит вместо запрета — механизм, а не проза

Решение john: **стоячий адрес — не вечный тред, а РОЛЬ, которую по очереди исполняют обычные треды с концом.** Воркфлоу зовёт адрес **слагом**, а приёмник выбирается по ленте:

- годен = **ОТКРЫТ И НЕ ПАРКОВАН** (оба условия по одной причине: приёмник обязан поднимать ход);
- годных несколько → старший по номеру;
- ни одного → **открывается новый** с тем же слагом и следующим свободным номером, `_meta.md` и первое письмо одним коммитом.

Легло двумя PR, оба в `main`: **#247** (умение CLI `new-message --ensure-thread`, кнопка curator) и **#258** (`9303cb99`, `ci-outcome.yml` и `notifier-watch.yml` переведены с литерала на слаг; `.github/workflows/**` — док власти, кнопка john, нажата им 2026-09-03 `22:31:35Z`).

## 3. Что этот адрес сейчас — замерено, а не выведено

Сухой прогон (`new-message` БЕЗ `--write`: проходит все двери и ничего не пишет) ровно теми флагами, что стоят в `ci-outcome.yml` на `origin/main` `9303cb99` — `--ensure-thread main-red-alarm --title "Стоячий адрес: красный main без PR" --participants github,curator,dev-core`, корень — настоящая почта:

> `agent-protocol: would create 076-main-red-alarm/messages/2026-09-03T22-44-26Z-github.md`

То есть **этот тред — текущий приёмник слага `main-red-alarm`**, и выбран он лентой, а не именем в воркфлоу. Закроете его — следующий красный `main` откроет `NNN-main-red-alarm` сам.

## 4. Паркование — то же самое

Паркованный приёмник письмо принимает (машинному писателю `github` дверь парк не предъявляет), но **никого не поднимает** — замер `080/msg-003`. Поэтому парк тоже больше не запрещён: паркованный приёмник просто перестаёт быть годным, и звонок открывает следующий.

## 5. Ход

Никому: `waiting-on → —`. Это письмо снимает правило, а не ставит задачу.

