# Журнал роли curator — тред `209-notifier-down`

## 2026-09-14, такт curator по треду `209-notifier-down`

Ход пришёл с тремя предметами (письмо dev-core 16:42Z): маршрут #431/#433 мимо круга ревью,
метки на #443/#444, хвост. Кода не тронул — предметы оказались суждениями, а не работой.

**Вопрос «как провести PR, правящий `claude-review.yml`, мимо круга» ответа не требует: круга у
такого PR нет ПО НОРМЕ.** `PROTOCOL.md:772–785` (решение john 2026-08-19, тред `014-merge-model`)
говорит всё четырьмя пунктами: метка не вешается вовсе (п. 1), гард 1 закрывается ИМЕНЕМ в треде
(п. 3), гард 4 всё равно шлёт PR к john, и «человеческий ревьюер и кнопка здесь — одно лицо»
(п. 4). Маршрут класса — зелёный `checks` → метки нет → john читает и жмёт одним действием.
Полезное для роли здесь не сама норма, а то, ПОЧЕМУ вопрос возник: dev-core пришёл к тому же
выводом, замером самопропуска, — то есть вывел заново текст, который лежит в общем для всех ролей
файле. Норма положена в `PROTOCOL.md`, а не в `REVIEWER.md`, намеренно (последний абзац раздела):
её читатель — не `reviewer-pr`, которая в этом классе не поднимается ни разу. **Читатель нормы —
curator, и когда вопрос класса приходит письмом от другой роли, ответом служит НОМЕР СТРОКИ, а не
пересказ: пересказ роль перепроверит замером в третий раз.**

**Второй эскалации по одной развилке не завожу.** Вопрос «как провести #433 к кнопке» УЖЕ стоял
перед john моим же письмом сегодня — в треде `201-notifier-down`, `expects: ack`, и там развилка
ШИРЕ: один ремонт написан дважды (#436 тред 201 / #433 тред 205), PR конфликтуют, ни один не
надмножество. Пока john не назвал дом, у вопроса 209 ответа нет по построению: к кнопке может
ехать #436. Два письма об одной развилке в двух лентах — два места, каждое отвечаемое поодиночке,
и ответ расщепляется. **Проверка перед эскалацией: не стои́т ли этот вопрос уже, и не является ли
он ЧАСТЬЮ более широкого, уже стоящего.**

**Замер, который такт добавил: #431 × #433 конфликтуют.** `git merge-tree --write-tree --messages`
по головам → `exit=1`, CONFLICT в `.github/scripts/review-delivery.integration.sh`;
`claude-review.yml` при этом сходится сам. Базы разные (`e55976b5` и `43bd494f`) и обе отстают от
`main` (`b1a451bb`) — пара невырожденная, то есть чистый результат тут что-то значил бы. Причина:
оба дописывают блок в ОДИН якорь файла и оба двигают `CURRENT_STATE`. Следствие для кнопки john,
которого в ленте не было: какой ни нажми первым, второй требует ребейза ДО кнопки, а ребейз двигает
голову — значит **человеческое чтение по п. 3 нормы придётся повторить на новой голове**. Цена
известна ДО нажатия, а не после.

**Условие снятия отложенной метки — замер, а не часы.** dev-core отложил метки на #443/#444 до
18:50Z (сброс сессионного лимита), и расчёт верен: `reviewRunAnchor` (`merge/gate.ts:946`) якорит
только `completed && success`, круг сейчас вердикт доставит и не заякорит. Но «после 18:50Z» —
гарантия, которой нет: сброс возвращает окно, а не обещает, что его не выберут снова. Условие —
первое из двух: (а) john мёржит починку цвета круга (`continue-on-error: true` у обоих шагов
ревьюера — прочитано диффом #433, состояния (23)–(26) складывают цвет джобы по правилам GitHub),
и тогда круг зеленеет при доставленном вердикте БЕЗ всякого сброса; либо (б) 18:50Z. **И в обоих
случаях метка вешается после `gh run list`, показавшего круг, закрывшийся `success`, — не по
показанию часов.**

**Гард 3 объявлен ДО merge, и это дешевле, чем кажется.** #444 — кнопка curator (дверь: 3 пути,
доков власти нет), класс «полевой измеренный дефект, новой нормы не вводит». Условия 1 и 2 закрыл
этим тактом (первоисточник — шесть прогонов `completed/failure` этого треда поимённо; дифф прочёл
САМ: `state` остаётся `orphan`, код возврата тот же, предикат якоря не тронут, правка — только
текст отказа), условие 3 ждёт круга. Объявление сделано письмом, следующему такту его не выводить
заново — дописать пункт 3 и назвать все три в следе гарда 5. Класс непроверяем задним числом,
поэтому объявление стоит ровно столько, сколько стоит абзац, а неназванное задним числом стоит
всего восхождения.

**Хвост:** 18 открытых PR, треду 209 принадлежат два (#443 — STOP гарда 4, кнопка john; #444 —
STOP гардов 1 и 2). Кнопок не нажимал: ни один не проходит. Остальные 16 — чужие треды, прогон
связан одним тредом, след гарда 5 положить некуда; названо в письме вслух, молчание не оставлено.

## Такт 17:00–17:25Z — перепись меток и отмена собственного «парка не ставлю»

**Замер такта (17:00Z, `gh run list --workflow 'Claude PR Review' -L 40 --json headSha,conclusion`
против `headRefOid` девяти открытых PR с меткой `review`):** из девяти меток **пять** уже
сработали в КРАСНЫЙ круг (#442 `afa6ad76`/`34866710091`, #441 `b57a57d1`/`34865984206`, #440
`c32b6ee4`/`34864312409`, #428 `7e224d44`/`34849381027`, #426 `06297de5`/`34841729661`), а
**четыре** несут заякоренный зелёный (#437, #419, #416, #406). Триггер ревьюера —
`pull_request: types: [labeled]` без `synchronize`, поэтому висящая метка круга больше не родит:
лечение мёртвого круга — снять и повесить заново. Отсюда счёт, которого в ленте не было:
после сброса лимита 18:50Z в очередь за кругами встают **семь** меток (пять перевесок + #443/#444),
а не две.

**Побочно, и это факт для чужих тредов:** #437, #419, #416, #406 имеют заякоренный зелёный круг на
ТЕКУЩЕЙ голове — их гард 1 закрыт прямо сейчас. Кнопок не трогал: след гарда 5 обязан лечь в ленту
треда PR, а прогон связан тредом 209. Назвал их в письме, чтобы факт не пропал.

**Чему научил такт — про держатель отложенной работы.** Прошлый такт я согласился с dev-core не
ставить парк: 209 — стоячий адрес класса, парк заморозил бы письмо о следующей аварии. Это
рассуждение опровергается сложением двух замеров самого dev-core: (1) поднять тред может только
новое красное письмо уведомителя, (2) эпизод A показал, что окно открывается РЕЗКО по объявленному
часу и первый же круг после него зелёный. Значит красные письма — единственный будильник —
прекращаются ровно в тот момент, когда отложенная работа становится выполнимой. **Выбор «без
парка» не сохраняет тред живым, а гарантирует протухание.**

Механизма-часов не существует: форм парковки ровно три — `<человек>`, `pr:N`, `run:N`
(`packages/agent-protocol/README.md:2320,2352,2370`). `run:N` не к чему привязать; `pr:443` заперт
кольцом (снимался бы движением PR, а PR не двинется без метки, у которой нет будильника).
**Остаётся парк на человеке — и он же единственный держатель отложенной работы.** Цену назвал
вслух: пока парк стоит, письмо о ГЕНУИННО другой аварии (другой уведомитель — свой ключ глушения)
приедет и никого не поднимет; меньшей она является потому, что парк с `--expects ack` звонит сразу
и ограничен ответом john, а «без парка» протухает тихо и навсегда.

**Владельческая половина, которой парк оправдан:** основная учётка выбрала сессионное окно сегодня
ДВАЖДЫ, и семь заказанных кругов сопоставимы с целым окном (эпизод A пропустил 10 кругов за ~2 ч).
Это расход денег — граница «к john» дословно. Вопрос поставлен первой строкой и отвечается одним
словом: «обычно» / «придержи». Граница с тредом 201 названа в обе стороны: там вопрос о ПРЕДМЕТЕ
(какой из #436/#433 — дом ремонта), здесь о РАСХОДЕ, и ответ 201 этот вопрос не определяет.

**Двери (17:02Z):** #443 `4198a5fa…` — гард 1 STOP, гард 2 ok, **гард 4 STOP (кнопка john,
`.github/workflows/notifier-watch.yml`)**, REFUSED. #444 `3597592f…` — гард 1 STOP, гард 2 **ok**
(прошлый такт было `IN_PROGRESS`), гард 4 ok (3 пути, доков власти нет), REFUSED. Кнопок не
нажимал. Гард 3 по #444 стои́т как объявлен 16:50Z: условия 1 и 2 закрыты, условие 3 ждёт круга.

## Поправка того же такта (17:09Z) — «готовы для своих тредов» оказалось «готовы для кнопки john»

Письмо 17:06Z назвало #437, #419, #416, #406 кандидатами на кнопку СВОИХ тредов — по одному лишь
цвету круга. Перемерил дверью, а не глазом: гард 1 у всех четырёх **ok** (вердикт `approve` лежит
в окне закрытого успешного круга на текущей голове — `34862093710`, `34859842242`, `34859017638`,
`34853852326`), гард 2 ok, а останавливает всех четверых **только гард 4**: #437 — `PROTOCOL.md` и
две карточки, #419/#416 — `.github/workflows/notifier-watch.yml`, #406 — две карточки. **Все четыре
— кнопка john, ни одна не кнопка своего треда.**

**Урок, который дороже поправки:** зелёный круг я взял за признак готовности «для своего треда», то
есть вывел ЧЬЮ-ТО кнопку из цвета круга. Цвет круга отвечает гарду 1 и не говорит о гарде 4 ничего;
чья кнопка — читается диффом путей, и только дверь читает его данными из конфига. Перепись по
`gh run list` годится назвать МЁРТВЫЕ метки (её предмет), но не годится назвать готовность: у
готовности четыре гарда, а перепись видит один.

Очередь к кнопке john на 17:10Z — восемь PR трёх разных сортов: готовы сейчас (#437, #419, #416,
#406), ждут круга и окна (#443), круга не получат никогда по норме `PROTOCOL.md:772–785` (#431,
#433, #436). Своей кнопкой в очереди один — #444, ждёт круга наравне с #443.

Нота двери у всех четырёх готовых: база уехала ПОСЛЕ старта зачтённых `checks` (`main` = `b1a451b`
от 15:14:54Z; у #416 `checks` стартовали в 10:54Z). Не STOP, но и не пустая строка — и первый же
merge из четырёх обнулит сливаемость у остальных.
