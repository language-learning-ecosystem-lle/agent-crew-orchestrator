# Журнал роли curator — тред `191-watcher-run-may-never-start`

# Тред `191-watcher-run-may-never-start` — запись роли curator

Файл назван тредом (решение john 2026-09-13, тред `195-journal-tail-serializes-work`). Предмет
треда — предел гарантии смотрителя `notifier-watch.yml`; предмет ЭТОЙ записи — то, что такт curator
здесь замерил ПОПУТНО и что дороже самого предмета.

## 1. Самопропуск круга ревью мерится на merge-ref, а НЕ на голове PR

**Замер, 2026-09-14.** Метка `review` на #413 была перевешена на ту же голову
`80b8f39f2674afc75cf2ca6ed67e7950ba4c3e2f` (голова не двигалась — сверено до и после). Круг
[`34836883802`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34836883802)
кончился `completed/success` за **40 секунд** против обычных 4–5 минут, `verdict.md` не создан, все
три доставки погасли. Предупреждение прогона дословно: `Skipping action due to workflow validation:
… identical content to the version on the repository's default branch`.

Три `rev-parse` называют причину точно:

```
claude-review.yml, блоб:
  origin/main (970957c3)            d7c5c84aa870c2754968adea99a4eb918dcaa561
  refs/pull/413/merge               37770131d0a70820515d26aa2dfbcf969fdeb6d0   ← ИМ шёл прогон
  refs/pull/413/head  (80b8f39f)    0f98404d09ee2955d289798710872f86874b71b1
```

`refs/pull/413/merge` собран в `09:21:47Z` против базы `c21c1940f` (это #418). После него в `main`
сели пять коммитов, среди них #417 (`efd461c2f`, `10:53:38Z`), правивший ровно `claude-review.yml`.
**GitHub merge-ref с тех пор не пересобрал.** Гард действия против промпт-инъекции увидел
расхождение и самопропустился; круг зелен, вердикта нет.

**Что здесь ново по сравнению с уже известным.** Что раннер для `pull_request` берёт воркфлоу с
merge-ref, я вывела раньше (msg-013) и назвала выводом, а не замером. Круг его подтвердил, но убил
следствие, которое я из него вывела: «кнопка на #417/#418 разблокирует ВСЮ стоячую очередь,
достаточно перевесить метку». **merge-ref ОТСТАЁТ:** он пересобирается на движение ГОЛОВЫ PR, а не
на каждый merge в `main`. Перевешивание метки на стоячем PR поднимает круг СО СТАРЫМ воркфлоу.

**Подтверждение механизма — прямое, а не по намерению.** После `git merge origin/main` в ветку
(рука `dev-core`, голова `80b8f39f` → `b36abc10`) merge-ref пересобрался:
`b849218a4ffd7cfc770e13ab47d14d8a1c8a607c`, родители `970957c3` (текущий `main`) + `b36abc10`, блоб
`claude-review.yml` = `d7c5c84a` — тождествен `main`. Следующий круг (`34837984835`) прошёл
настоящим и дал вердикт.

**Цена ошибки измерена: один круг (~$1) без вердикта плюс такт.** Даровая проверка ПЕРЕД меткой,
одна команда на PR:

```
git rev-parse refs/pull/<N>/merge:.github/workflows/claude-review.yml
git rev-parse origin/main:.github/workflows/claude-review.yml
```

Совпали — метку вешать можно. Не совпали — сперва двигать голову (`git merge origin/main` в ветку,
push выходит fast-forward, force не нужен). Второй даровой признак, уже постфактум: **длительность
круга** — 40 с против 4–5 минут.

**Класс шире #413.** На 14.09 блоб расходится с `main` у #410, #406, #388, #411 — #417 сел позже их
утренних кругов. Вешать им метку вслепую значит купить ещё три-четыре пустых круга; каждый из них —
долг своего треда (`197`, `180`, `155`), не тихий хвост этого.

**Шагом маршрута это пока НЕ объявлено.** Обязательная проверка перед меткой — новый шаг, то есть
норма, а норма — слово john; здесь она записана как замер и как моя практика.

## 2. Нота двери о переезде базы гасится ЗАМЕРОМ, а не ребейзом — и пустого пересечения файлов мало

`merge-gate` печатает ноту, когда база уехала после старта зачтённых чеков. На #413 она стояла:
8 коммитов, `6312dfbf..970957c3`. Чем я её гасила:

1. **пересечение файловых множеств** PR и базы — пустое;
2. **целость слияния:** `git merge-tree --write-tree origin/main 80b8f39f` → exit 0, дерево
   `a13d532dc2fe462fc1faf595d26fae1a01b30cf9`; `git diff --name-status origin/main <дерево>` — ровно
   один путь PR, удалений ноль;
3. **и этого НЕ ХВАТИЛО.** `notifier-watch.yml` — ВХОД трёх сканирующих тестов, читающих
   `.github/workflows/**` текстом (`roles/workflow-run-names.test.ts`,
   `roles/standing-address.test.ts`, `roles/workflow-signatures.test.ts`). «Дифф только
   комментарный» в таком классе от прогона не освобождает: пустое пересечение имён файлов говорит о
   конфликте слияния, а не о поведении тестов. Прогон на СЛИТОМ дереве (стенд — `git diff --binary
   origin/main <дерево> | git apply --index` в своём чекауте, `TMPDIR=/tmp pnpm --filter
   agent-protocol exec vitest run` трёх файлов) → 3 файла / 17 тестов, ноль красных, 344 мс; возврат
   `git reset --hard HEAD`, контрольный `git status --porcelain` пуст;
4. **отдельный класс — «база тронула воркфлоу ЗАЧТЁННОГО прогона»** — проверен и не применился:
   `git log 6312dfbf..origin/main -- .github/workflows/checks.yml` пуст.

Вывод: переезд базы инертен ЗАМЕРЕНО. Полную сюиту не гоняла и называю это прямо — перекрыт ровно
стык сканирующих тестов. Нормой протокола это не объявлено: в `PROTOCOL.md` о движении базы нет ни
слова, и это моя практика, а не правило.

## 3. Записанный предел живёт в трёх местах, и находит их греп ОБЕЩАНИЯ, а не файла

Постановка называла ОДНО место записи — шапку воркфлоу. Сколько мест повторяют само обещание, не
мерил никто, пока `dev-core` в такте приёмки не грепнул обещание: нашлись ещё два — таблица почтовых
воркфлоу в корневом `README.md` (безусловное «отказ любого из четырёх уведомителей звонит письмом»
на первой странице репозитория) и `docs/install-notes.md` §2 («класс молчания закрыт не
внимательностью, а событием»). Первое погашено #428 рукой `dev-core`, второе — этим же PR моей
рукой, вместе с записью-ссылкой в `docs/protocol-reference.md`.

**Правило, меняющее поведение роли на приёмке:** записала предел обещания — грепни САМО ОБЕЩАНИЕ, а
не файл, который правился. Дешевле всего это стоит в такте приёмки: дифф уже смёржен, критерий
формулируется одной строкой грепа, а цена пропуска — первая страница репозитория, продолжающая
обещать ровно ту безусловность, ложность которой тред и доказал.

## 4. Ссылка в справочнике адресует, а не копирует

`docs/protocol-reference.md` получил запись-ссылку, а не копию текста: первоисточник — шапка
`notifier-watch.yml`, и правится он тем же диффом, что и сам воркфлоу. Копия в справочнике протухла
бы молча — тем же способом, каким протухла строка `README.md`. john назвал эту запись возможной, но
обязательной не делал (тред `191`, ветвь Б).

## 5. Тред кончился доставленным `approve`, под которым кнопки нет НИ У КОГО

Происхождение находки — чужой предмет (`180`/`205-primary-step-reddens-a-delivered-verdict`,
`201-notifier-down`), но замер сделан НА PR ЭТОГО треда и кончает его историю, поэтому лежит здесь.

**Факт.** На #428 (голова `7e224d44bf7f5b80158be9cae71e68a76eff3bea`) ревьюер вынес `approve` и
доставил его в тред (msg-030). Дверь на той же голове в `13:41Z` отказывает ДВУМЯ гардами:

```
STOP guard 1 · no CLOSED round of 'Claude PR Review' on 7e224d4:
     run 34849381027 (pull_request, completed/failure, 13:27:41Z…13:31:38Z)
STOP guard 2 · not green: review=FAILURE
ok   guard 4 · 2 changed path(s), none of them a document of power
     mergeable=MERGEABLE (UNSTABLE)
```

**Перепись ШАГОВ круга `34849381027`** (цвет круга здесь ничего не говорит, а шаги говорят всё): №8
`success`, **№9 `Run anthropics/claude-code-action@v1` — `failure`** (ОСНОВНАЯ учётка), №10 «Лимит
основной учётки» `success`, №11 «Ревью на ЗАПАСНОЙ учётке» `success`, №12–17 `success` — вердикт
вынесен, доставлен комментом, письмом и формальным review-статусом, транскрипт выгружен. Джоба
красная ровно потому, что `continue-on-error` у шага `reviewer` нет: исход джобы падает вместе с
основным шагом даже тогда, когда вердикт вынесла запасная учётка и доставка прошла целиком.

**Форма не единична, и это меняет решение о метке.** Подряд идущие круги `Claude PR Review`:
`34841729661` (12:07:51Z, #426), `34849381027` (13:27:41Z, #428), `34850008811` (13:33:50Z, #416) —
все три `failure`, и у последнего перепись шагов совпадает с #428 знак в знак (№9 `failure`, №10–17
`success`). До них шесть зелёных подряд (последний `34840576269`, 11:54:32Z). То есть лимит основной
учётки живой ПРЯМО СЕЙЧАС, и каждый новый круг в этом окне — переезд, красная джоба и вердикт,
который ни одного гарда не открывает.

**Правило, меняющее поведение роли на метке** (шире этого треда, потому и записано правилом):
**зелёный `checks` — не единственное условие метки; второе условие — живой круг.** Перед меткой
смотреть исход ПОСЛЕДНИХ кругов `Claude PR Review` (`gh run list --workflow 'Claude PR Review'
--limit 8`): идут красные подряд с переездом — метку не вешать, она купит вердикт, не годный гарду 1,
и заплатит за него (замер `$1,7804` за один вердикт, тред `180`). Это не отмена маршрута «метка
после зелёного `checks`», а его вторая половина: маршрут говорит, КОГДА метку вешать можно, и
молчит о том, когда её вешать НЕ СТОИТ.

**Что из этого следует для треда `191`:** предмет треда закрыт диффами (#413 смёржен, #428 и #430
стоят готовыми), а хвост держится не работой роли и не ревью, а исправностью круга. Кнопка на #428
откроется тем, что чинится в чужих домах: `#431` (`201-notifier-down`, отказ читается префиксом
`allowed`) и постановка `205-primary-step-reddens-a-delivered-verdict` (исход джобы перестаёт падать
вместе с основным шагом, когда вердикт вынесен И доставлен). Оба пути — `.github/workflows/**`, то
есть кнопка john; своей рукой отсюда не делается ничего, и держать ход в `191` под это незачем.

# Блоб merge-ref протухает от ДВИЖЕНИЯ MAIN, а не только от старой головы

Полевой случай 2026-09-15, такт curator в треде `191-watcher-run-may-never-start`, третье
срабатывание класса за двое суток (первые два названы в теле PR #447).

**Что было замерено.** В 09:11Z я повесила метку на #430, сверив блоб `.github/workflows/claude-review.yml`
в `refs/pull/430/merge` против `origin/main` (`117164193`) — блобы сошлись (`4eb5716d8`), круг
`34950754599` поднялся и довёз вердикт. В 09:23Z, войдя в следующий такт и собираясь вешать метку на
#447 (по зелёным `checks` из msg-040), я повторила ту же меру и получила расхождение:

```
origin/main          041730a84  blob b2babcfb9…   ← #445 «смерть основного шага … вслух» (тред 201)
refs/pull/447/merge  6c68bbc70  blob 4eb5716d8…   ← РАЗОШЛИСЬ
refs/pull/430/merge  df1bad05d  blob 4eb5716d8…   ← РАЗОШЛИСЬ
refs/pull/428/merge  6e3a7ed97  blob bd6095d96…   ← разошлись и раньше (нет #433)
```

Ни одна голова из трёх не двигалась. Протухли merge-ref'ы от того, что **сдвинулся `main`**: между
двумя моими замерами john смёржил #445, и этот PR трогал ровно `claude-review.yml`.

**Почему это важнее, чем «голова уехала».** Вчерашний случай (14.09) читался как «пришпиленный блоб
протух под висящей меткой» — то есть виновата была СТАРАЯ голова, и лекарство «двигать голову»
выглядело следствием. Сегодняшний случай показывает вторую, независимую причину: голова может быть
свежей, `checks` на ней — зелёными и только что доехавшими письмом, а блоб всё равно разойдётся,
потому что двинулась БАЗА. Отсюда практическое следствие, которого не давал вчерашний случай:
**мера годности — не свойство головы и не срок её давности, а сравнение ДВУХ движущихся объектов, и
меряется оно непосредственно перед меткой, а не один раз за такт.** Зелёные `checks`, приехавшие
письмом 12 минут назад, годности блоба не доказывают вовсе: `checks` мерят СВОЮ базу (см. класс
«красный `checks` мог быть уже починен на main» — тот же корень с другой стороны).

**Цена невнимательности здесь ровно одна и она тихая:** метка на разошедшемся merge-ref покупает
самопропуск `claude-code-action` («воркфлоу ветки ≠ дефолтная», гард против промпт-инъекции правкой
воркфлоу — `claude-review.yml`, строки ~788-800 и ~1096 на `041730a84`). Прогон при этом ЗЕЛЁНЫЙ, а
вердикта нет; гард 1 ничего не якорит, и такт потрачен.

**Лечение то же самое и оно дешёвое:** слить `origin/main` в ветку и запушить — merge-ref
пересчитывается, блоб сходится, `checks` перезапускаются сами. Замерено сегодня на обоих своих PR:
#430 `99c805c7c` → `0ee1530ba`, #447 `b9411687c` → `540694a52`, после пуша `refs/pull/430/merge`
`e12db8113` и `refs/pull/447/merge` `064081554` — блоб у обоих `b2babcfb9`, то есть `main`. Дифф PR
от merge-base при этом не меняется: у #430 те же два пути, у #447 тот же один.

**Побочный замер того же такта — деградация GitHub 09:25-09:28Z.** `gh pr edit` отвечал
`GraphQL: Something went wrong` дважды и 502 один раз; `git push` в `curator/191-label-checks-the-merge-ref-blob`
был отбит `remote rejected … (Internal Server Error)` трижды подряд, при том что пуш в соседнюю
ветку минутой раньше прошёл с первого раза. Оба действия прошли ретраем без единой правки команды.
Вывод для роли: `remote rejected (Internal Server Error)` на пуше — это НЕ отказ прав и не защита
ветки, и разбирать его как отказ доступа значит потратить такт; ретрай с паузой ставится раньше
диагноза.

# Блоб merge-ref лечится ЛЮБЫМ пушем головы — в том числе чужим и не ради этого; метка по-прежнему поднимает круг СОБЫТИЕМ

Такт 2026-09-15 09:44-09:50Z, тред `191-watcher-run-may-never-start`. Продолжение записи прошлого
такта (там — первый замер расхождения блоба `.github/workflows/claude-review.yml` между
`refs/pull/<N>/merge` и `origin/main` и цена метки на разошедшемся merge-ref: самопропуск
`claude-code-action`, зелёный прогон без вердикта).

## 1. Замер: все три merge-ref треда СОШЛИСЬ с `origin/main`, и третий — без моей руки

```
origin/main          041730a84  blob b2babcfb9…
refs/pull/430/merge  e12db8113  blob b2babcfb9…   ← сошёлся прошлым тактом (мой пуш слияния main)
refs/pull/447/merge  064081554  blob b2babcfb9…   ← то же
refs/pull/428/merge  56e897454  blob b2babcfb9…   ← СОШЁЛСЯ САМ: был bd6095d96 при merge-ref 6e3a7ed97
```

У #428 (PR роли dev-core) прошлым тактом блоб был старым — `bd6095d96`. Его никто не лечил: в
`09:42:03Z` dev-core запушил новую голову (`7e224d44b` → `e28aa801f`) по своей постановке — выпилить
журнальный файл из диффа и слить `origin/main`, — и merge-ref пересчитался вместе с пушем.

**Что из этого следует и чего не следует.** Merge-ref пересчитывается на ЛЮБОМ пуше головы, а не
только на пуше «ради блоба»: расхождение может зажить побочным эффектом чужого действия, сделанного
ради другого. Но лечение не гарантировано ничем — `main` продолжает ехать, и пуш мог случиться ДО
движения базы, а не после. Поэтому мера остаётся прежней: сверка двух блобов **непосредственно перед
меткой**, а не один раз за такт и не «он же вчера сошёлся». Сегодня я её повторила третий раз за
такт, в 09:47, и только после совпадения повесила метку.

## 2. Метка на #430 висела со старой головы и круга не подняла — третье полевое подтверждение

`gh pr view 430 --json labels` в 09:45Z: метка `review` НА МЕСТЕ, при этом на текущей голове
`0ee1530ba` круга нет вовсе — последний круг `34950754599` закрыт на прошлой голове `99c805c7c`
(вердикт `needs-fixes`, msg-039). Голова уехала 09:2xZ от моего пуша слияния; метка осталась висеть.

Перевешивание (`gh pr edit 430 --remove-label review`, пауза, `--add-label review`) в 09:47:4xZ
подняло круг **немедленно**: `34954489742`, `event=pull_request`, `head_sha=0ee1530ba…`,
`run_started_at=2026-09-15T09:47:50Z` — то есть задержки между событием и стартом круга здесь нет
(ср. класс самого этого треда: у `workflow_run`-смотрителя прогон может не начаться никогда).

Ничего нового к норме это не добавляет — карточка уже говорит «круг поднимает СОБЫТИЕ навешивания
метки, а не её наличие». Ценность записи в том, что случай полевой и читается ДАРОМ на любом своём
PR: **висящая метка при отсутствии круга на текущей голове — это не «круг был», а «круг был на
другом дереве»**, и `gh run list --branch <ветка>` с колонкой `headSha` отвечает на это одной
строкой.

## 3. Чего я НЕ сделала и почему: person-парк на john под живым кругом ревью не ставится

#447 получил `approve` (msg-044) и упирается в гард 4 — `docs/roles/curator.md` есть док власти,
кнопка john. Форма кнопки john из карточки — `--expects ack --parked-on john --waiting-on curator`.
Этот такт её не применил, и причина машинная, а не вкусовая:

- person-парк морозит тред ЦЕЛИКОМ и закрывает его **на вход**: письмо роли в паркованный тред дверь
  не пускает, а отказ тихий (замер 2026-09-02, тред `072`);
- в этом треде прямо сейчас живой круг ревью по #430, и вердикт по нему пишет `reviewer-pr` — **роль,
  а не машинный писатель** (у него карточка). Парк на john съел бы этот вердикт молча.

Правило, по которому такт решался, — «person-парк ставится там, где вопрос родился, КАК ТОЛЬКО у
треда не осталось живой работы». Живая работа есть (круг по #430, идущий `checks` по #428), поэтому
парк такта — машинный: `run:430`, он снимается самим письмом вердикта (пара `verdict:`/`pr:` в
шапке). Кнопка #447 названа в письме такта и встаёт парком на john в тот такт, когда в треде не
останется ничего движущегося.
