# Журнал роли curator — тред `180-notifier-down`

# Тред 180 — уведомитель: четыре полевых случая такта 15.09

Запись едет командой `journal write` в ветку почты (норма 206, слово john 14.09): старая запись того
же треда осталась в `docs/journal/curator/180-notifier-down.md` внутри открытого #406 и переезду не
подлежит. Долг, объявленный в `msg-070` («дописать после merge #406»), закрывается ЗДЕСЬ и больше не
ждёт кнопки: дом находки теперь стоит ноль.

## 1. Тело PR читается кругом на СТАРТЕ, и штамп правки меряется не `updated_at`

Круг `34851961887` (#406, 14.09) вернул `needs-fixes` на расхождение тела PR с диффом. Тело было
поправлено ДО конца круга — вердикт всё равно назвал старый текст: **снимок тела берётся на старте
прогона, правка посреди круга до вердикта не доезжает.** Порядок, который из этого следует и который
обязателен: сперва правка тела, ПОТОМ метка.

Прибор: момент правки тела мерится `userContentEdits` (штамп `editedAt`), а **`updated_at` PR для
этого не годится** — он показал `13:57:22Z`, то есть момент review-объекта, а не правки.

## 2. Молчание самоперезапуска бывает `WITHHELD` — и это ВТОРОЕ срабатывание, то есть класс

14.09 расхождение `self-restart.json` (новый штамп) и `self-restart-letters.json` (подпись прошлого
перезапуска) выглядело ровно как предмет этого треда — «уведомитель не позвонил». Дефектом оно не
было: журнал ящика печатает причину дословно, `daemon — letter — WITHHELD, the restart changed
NOTHING THIS DAEMON EXECUTES`, и переезд лежал вне отпечатка установки.

15.09 гард сработал ВТОРОЙ раз и тем самым перестал быть образцом: перезапуск `08:25:32Z`,
`b48f0bf77381..588281272c05`, письма нет, `self-restart-letters.json` держит подпись `a674e6c20…`
от 14.09 `15:12:57Z`. Строка причины — `daemon.log`, эпоха `2026-09-15T08:25:46Z` (pid 1962062).
Третье срабатывание того же гарда стои́т днём раньше — `b1a451bb21cd..b48f0bf77381`.

**Правило чтения:** расхождение mtime двух файлов самоперезапуска дефектом САМО ПО СЕБЕ не является.
Сперва греп `letter — WITHHELD` по ТЕКУЩЕЙ эпохе; только пустой ответ делает молчание предметом.

## 3. Письмо своей же прошлой сессии — не оракул состояния PR

`msg-071` (15.09) передало ход словами: «#406 … у него **не было круга** — метки нет, вердикта нет.
Вешай метку обычным маршрутом». Замер этого такта говорит обратное, и обе половины утверждения
ложны:

```
gh api …/issues/406/timeline  → 14.09 13:52:28Z labeled review
                                14.09 14:10:05Z unlabeled review
                                14.09 14:10:11Z labeled review   ← и с тех пор НЕ снималась
gh api …/actions/runs?head_sha=957e412b2
                                34853852326  Claude PR Review  event=pull_request  success  14:10:13Z
```

Метка стояла непрерывно, вердикт `approve` (`msg-069`) лежал в окне закрытого успешного круга, голова
не двигалась ни разу. Между `msg-071` и этим замером состояние измениться не могло — прогон 14.09
задним числом не появляется, — значит `msg-071` **прочло не то**, а не застало другое состояние.

Цена ошибки была бы не нулевой: по её букве следовало повесить метку, то есть СНЯТЬ и повесить заново
(наличие метки круга не поднимает) — и заплатить кругом за уже имеющийся `approve`.

**Правило:** постановка, пришедшая письмом — даже своей же роли, даже прошлым тактом, — называет
предмет, а не состояние. Состояние PR меряется дверью ПЕРЕД действием, и первый шаг по такой
постановке — `merge-gate`, а не то действие, которое письмо предписало.

## 4. `refs/pull/<n>/merge` отстаёт от `main`, и это видно по его РОДИТЕЛЮ

`msg-071` требовало перед меткой сверить блоб `.github/workflows/claude-review.yml` в
`refs/pull/406/merge` против `origin/main`. Замер:

```
git rev-parse refs/pull/406/merge:.github/workflows/claude-review.yml  → bd6095d968…
git rev-parse origin/main:.github/workflows/claude-review.yml          → 4eb5716d8e…
git rev-list --parents -n1 refs/pull/406/merge → 5b4311a89 b48f0bf77 957e412b2
git rev-parse origin/main                      → 588281272
```

Блобы РАЗНЫЕ, и причина читается не в блобе, а в родителе: база merge-ref — `b48f0bf77` (#431), а
`main` уже `588281272` (#433, правил тот же файл ревью). **merge-ref пересчитывается лениво и
отстаёт;** сверка блобов только показывает следствие, а диагноз даёт `git rev-list --parents -n1`.

Следствие для маршрута: **новый круг на таком PR исполнит УСТАРЕВШИЙ воркфлоу ревью.** Хочешь свежий
круг — сперва двинь голову (ребейз), иначе платишь прогоном за файл, который на `main` уже починен.
Для #406 это осталось теорией: круг уже отдан, гард 1 закрыт, поднимать нечего.

## 5. Состояние предмета треда на 15.09 08:3xZ

Уведомитель звонит. Эпоха `08:02:50Z` (pid 1933025) — `telegram: delivered` 6, строк `telegram:` с
НЕ-`delivered` хвостом 0. Эпоха `08:25:46Z` (pid 1962062) на момент замера прожила минуту и telegram-строк
ещё не имеет — это молодость эпохи, а не молчание. Счёт чищен от эха своей сессии (`grep -v '] tool \|] result '`):
журнал ящика печатает и мои собственные команды, и нечищеный счёт растёт от самого замера.

Оговорка прежняя и счётом не снимается: журнал мерит гейт ПЕЧАТИ — «не позвонил вовсе» им не ловится.

# Тред 180 закрыт — и три случая, замеренные на его закрытии (15.09 08:5xZ)

Дозапись к записи того же треда. Предмет `180-notifier-down` исполнен: #406 смёржен john
(`117164193a992bbbf234ec008f5912c00369ce06`, `~08:41Z`), правило стои́т в обеих карточках в `main`
(`docs/roles/curator.md:108-114`, `docs/roles/dev-core.md:103-109`), механическая половина закрыта
#419 (`4a63df99d`). Открытых PR со строкой `thread: 180-notifier-down` — ноль (перечислено
`gh pr list --state open`, 11 PR, все чужих тредов). Ниже — то, что замерено уже на закрытии.

## 6. Закрытие приёмника стоячего адреса ничего не стоит, потому что приёмником он давно не был

Слаг `notifier-down` держит ЧЕТЫРЕ треда: `077` (закрыт), `180`, `201`, `209`. Дверь
(`--ensure-thread notifier-down`) берёт ОТКРЫТЫЙ и НЕ паркованный приёмник, СТАРШИЙ ПО НОМЕРУ —
значит с момента рождения `201` (13.09) письма отказа уведомителя в `180` не ложились вовсе, а
сегодня приёмник — `209` (очередь 15.09: `curator×201` и `dev-core×209` стоя́т живыми, `priority
high`, парка нет).

**Отсюда то, что легко спутать: письмо `msg-074` (merge #406) ПРИЕХАЛО в `180` — и это не
противоречие, а ДРУГОЙ писатель.** Адресуют по-разному:

- уведомитель отказов — СЛАГОМ, через `--ensure-thread`: приёмник выбирается дверью, и он старший;
- `Merge Notify` — БУКВАЛЬНОЙ строкой `thread: <NNN-slug>` из тела PR: он пишет в тред-хозяин PR,
  каким бы старым тот ни был.

Практическое следствие для закрытия: закрыть тред стоячего адреса безопасно (следующее событие
откроет новый приёмник), но пока жив хоть один ОТКРЫТЫЙ PR со строкой `thread:` на этот номер,
закрытие отнимает у него канал доставки merge-письма. Перед закрытием — `gh pr list --state open` и
греп по `thread:`, а не только «живых предметов нет».

## 7. Своё эхо сдвигает ГРАНИЦУ ЭПОХИ, а не только счёт

Известное «журнал ящика отражает твой же вывод» шире, чем счёт строк. Замер этого такта:

```
grep -n "=== daemon epoch" daemon.log | tail -1      → 5050  ← СВОЯ ЖЕ строка `] tool Bash command=…`
grep -v '] tool \|] result ' daemon.log | grep -n …  → 4126  ← настоящее начало эпохи 08:49:31Z
```

Эхо содержит текст собственной команды, поэтому оно попадает в поиск МАРКЕРА, а не только в поиск
предмета. Окно замера при этом съезжает молча и всегда в одну сторону — к нулю событий («эпоха
молода»), то есть ровно к тому выводу, который ищет невнимательный читатель. **Порядок обязателен:
СНАЧАЛА вычистить `] tool `/`] result `, ПОТОМ искать границу эпохи, и только потом считать.**

## 8. Гард `WITHHELD` сработал четвёртый раз — на перезапуске к коммиту СВОЕГО ЖЕ merge

`self-restart.json`: `{"target":"117164193a99…","at":"2026-09-15T08:49:17Z","from":"e71204c57ee9…",
"behind":3,"drainSince":"08:42:12Z","went":true}`; `self-restart-letters.json` — по-прежнему подпись
14.09 (`a674e6c20…`, `15:12:57Z`). Расхождение mtime опять выглядит как предмет треда, и опять им не
является: `daemon.log` называет причину дословно — `letter — WITHHELD … e71204c57ee9..117164193a99
moves no path of the footprint`. Перезапуск поехал на коммит, которым сел САМ #406, — то есть на
диффе из двух карточек и журнала, где отпечатка ящика нет по построению.

Четыре срабатывания подряд (`b1a451bb..b48f0bf7`, `43bd494f..b20ac957`, `b48f0bf7..588281272`,
`e71204c5..117164193`) — гард работает штатно, отдельного предмета в нём нет.

## 9. Поле на момент закрытия

| счёт (эхо вычищено ДО поиска границы) | эпоха `08:38:16Z` pid 1994538 | эпоха `08:49:31Z` pid 2146550 |
| --- | --- | --- |
| `telegram: delivered` | 4 | 0 |
| `telegram:` с НЕ-`delivered` хвостом | 0 | 0 |
| `telegram.*(error\|fail\|undeliver\|refus)` | 0 | 0 |

Ноль в новой эпохе — её молодость (3 минуты на момент замера), не молчание. Оговорка не снимается
счётом: журнал мерит гейт ПЕЧАТИ, «не позвонил вовсе» им не ловится. Прямое свидетельство живого
уведомителя этого такта — `msg-074` в самом `180`: письмо `Merge Notify` о #406 доехало под своим
именем файла.
