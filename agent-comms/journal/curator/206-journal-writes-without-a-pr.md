# Журнал роли curator — тред `206-journal-writes-without-a-pr`

## Такт 14.09: журнал переехал в ветку почты — и первым же ходом это стоило подвинуть своё дерево

Предмет треда `206` закрыт с кодовой стороны: `journal write` слит в `main` коммитом `958308d45`
(PR #435, голова `992bbe1fc89d1b7b93df519d51ffc0aa67a82781`). Гарды названы в ленте треда; здесь —
то, что в ленту не помещается, потому что это не отчёт, а замер.

### Приёмка john снята ДВАЖДЫ, и второй раз — другой рукой

Слово john требовало одного: «следующая запись роли ложится без единого PR». `dev-core` снял это
из НЕВЛИТОЙ ветки — код команды тогда не лежал нигде, а запись уже была в `origin/comms`. Эта
запись — второе снятие, и оно про другое: она написана рукой `curator` из дерева, стоящего на
СЛИТОМ `main`, то есть тем путём, каким запись будет писаться каждый следующий такт любой ролью.
Два снятия отвечают на разные вопросы: первое — «путь работает мимо круга», второе — «путь работает
после мёржа, из обычного дерева обычной роли».

### Полевой факт такта: своё дерево не знает своего же мёржа

Дерево роли поднимается контуром на той голове, что была НА МОМЕНТ ПОДЪЁМА, и дальше не двигается.
Значит, собственная работа, слитая ЭТИМ ЖЕ тактом, в дереве такта отсутствует: после кнопки на #435
`ls packages/agent-protocol/src/journal/` в моём дереве отвечал `No such file or directory`, хотя
`origin/main` уже нёс команду. Чтобы воспользоваться тем, что сам только что принял, дерево
пришлось подвинуть явно (`git fetch origin main` + `git checkout <sha мёржа>` на чистом дереве —
дёшево и обратимо).

Это отдельная грань уже записанного «смёржено ≠ работающее»: там речь про демона, исполняющего
исходники своего чекаута, здесь — про СЕБЯ. Роль, принявшая инструмент кнопкой, инструмента в руке
не имеет, пока не заберёт его сама. Практическое следствие: любая приёмка вида «а теперь воспользуйся
тем, что смёржил» требует явного шага подъёма дерева и должна называть его, иначе следующий такт
прочитает `No such file` как поломку мёржа.

### Что осталось на john

Требование 4 постановки (ссылки в карточках ролей и текст нормы 11.09 в `PROTOCOL.md`) уехало PR
#437 — доки власти, гард 4 STOP без исключений. Метка `review` на нём повешена в `14:58:13Z`, после
зелёного `checks` (`14:56:32Z`) на той же голове `9cf02735928bdb6cc5b83fc3bc19390e3669d7b7`; кнопка
не моя. До его мёржа карточки ролей продолжают описывать СТАРУЮ форму журнала (`docs/journal/<роль>/`
через PR), хотя действует уже новая — расхождение известное, названное и живёт ровно один PR.

`merge-gate --journals` не тронут намеренно: пока старые пути `docs/journal/**` живы в главной ветке,
исключение нужно. Это факт, а не недоделка.

## Отмена нормы ловится ГРЕПОМ ПО ФРАЗЕ, а не чтением названного раздела (14.09, тред 206)

Решение `206` («журнал пишется командой в ветку почты — без PR») отняло предмет у нормы `187`
(«запись едет попутным диффом»). В PR #437 я закрыла это ЯВНО — абзацем в конце раздела `187` и
строкой «Что этим отменяется явно» в разделе `206`. Обе правки точные, обе по адресу названной
нормы — и обе не помогли: ревьюер вернул `needs-fixes` на `PROTOCOL.md:742`, где раздел `195`
(«файл на тред») своим словом повторял ту же мёртвую фразу: «запись по-прежнему едет сразу и
попутным диффом, меняется только адрес». Правка #437 вставила туда клаузу «АДРЕС С 14.09 ДРУГОЙ»
РЯДОМ с этими словами и их не тронула.

**Почему я это пропустила:** я правила ТУ норму, которую решение называет по имени, и раздел, куда
это решение кладётся. Отменяемая формулировка живёт не только в своём разделе — соседние нормы
цитируют её как основание («не отменяет ни `143`, ни `187`: по-прежнему сразу и попутным диффом»), и
цитата переживает отмену молча: она не помечена номером нормы, она читается как своя проза раздела.

**Правило, которое я отсюда беру в каждый такт отмены нормы:** отменяя формулировку, греп по САМОЙ
ФРАЗЕ (`grep -rn "попутн" PROTOCOL.md docs/roles/ REVIEWER.md docs/protocol-reference.md`), а не
чтение раздела, чью норму отменяешь. Замер этого такта: 7 попаданий, из них 4 — мои собственные
правки этого же PR, 1 — заголовок самой нормы, 1 — ссылка справочника на заголовок (законная), и
ровно 1 — живое противоречие в ЧУЖОМ разделе. Один греп стоит секунды и находит то, чего не видно из
правящегося раздела.

**Цена пропуска замерена:** круг ревью по #437 (прогон `34859074949`), вердикт `needs-fixes`, новая
голова, снятая и заново вешаемая метка, второй круг — и всё это за одну фразу в семи словах.
Ревьюер поймал её тем самым приёмом, который я не применила: он сравнил два места в ОДНОМ файле и
увидел, что PR запрещает противоречие в одном разделе и оставляет идентичное в другом.

## Дерево роли ПЕРЕЧЕКАУЧИВАЕТСЯ контуром на каждом подъёме — чекаут такта не переживает такт

Прошлый ход этого треда оставил дерево `curator@206-journal-writes-without-a-pr` на ветке PR
`docs/206-journal-in-mail-branch` (голова `01c85a975`) и доложил это в тред как состояние. Этот ход
поднялся и увидел дерево ЧИСТЫМ на `b1a451bb2` — свежем острие `main`, в detached HEAD.

Замерено рефлогом того же дерева, а не догадкой:

```
b1a451bb2 HEAD@{2026-09-14 17:25:18 +0200}: checkout: moving from docs/206-journal-in-mail-branch to b1a451bb21cd9bc59198838a403e7eaf6ed62873
01c85a975 HEAD@{2026-09-14 17:14:21 +0200}: merge origin/main: Merge made by the 'ort' strategy.
```

`17:25:18 +0200` = `15:25:18Z` — это момент ПОДЪЁМА этой сессии, а не действие прошлой. То есть
контур сам перевёл дерево с ветки PR на свежую голову `main` и отцепил HEAD.

**Что из этого следует для поведения роли:**

1. **Никакой чекаут, сделанный руками в такте, до следующего такта не доживает.** Прошлый ход этого
   треда забирал инструмент явно (`git fetch` + `git checkout 958308d45`) — правильно для СВОЕГО
   такта, но как «подготовка дерева для следующего» это не работает вовсе.
2. **Обратная сторона дешёвая и полезная: дерево приезжает на СВЕЖЕЙ голове `main` само.** Грань
   «своё дерево не знает своего же мёржа» (разбор выше в этом же файле) живёт РОВНО один такт: то,
   что смёржено этой кнопкой, следующий подъём уже несёт. Ловушка только внутри такта, который мёржил.
3. **Ветка PR остаётся локально целой, но HEAD от неё отцеплен.** Незапушенный коммит на ней после
   перечекаута — невидимая работа: не в `main`, не в PR, не в `git status`, который читает следующий
   ход. Отсюда правило: коммит ветки PR пушится В ТОМ ЖЕ такте, где сделан, без «допишу на следующем».

Цена незнания этого — не поломка, а ложный доклад: состояние дерева, названное в письме, к моменту
чтения письма уже неверно. Поэтому состояние дерева в докладе означает «на момент письма», а
следующий ход мерит его заново, а не верит ленте.

## Такт 15.09 08:2x–08:4xZ — нота НЕИЗМЕРЕННОЙ ПАРЫ гасится тем, ЧЕМ файл назван

Такт по `206` был ровно один ход: перемерить #437 на свежей базе (слово john 14.09 — «перед merge
перемерить `mergeable`, за сутки простоя базы уехали у многих») и позвать кнопку. Merge своей рукой
невозможен: гард 4 — STOP, три файла диффа суть доки власти.

### 1. Что сказала дверь, чего она не говорила вчера

`pnpm protocol merge-gate --ref origin/main --pr 437 --review-workflow 'Claude PR Review'` напечатал
ноту, которой на этом же PR вчера (`msg-014`) НЕ было:

> `note · pair: NOBODY MEASURED THIS PAIR — '.github/workflows/claude-review.yml' (landed in the
> base) names 'PROTOCOL.md' (changed by THIS pull request) by path: one side edits a file that
> EXECUTES a file the other side edits, and no run has ever read the two together`

Нота родилась не из PR — **PR не двигался вовсе** (голова `01c85a975` та же, что вчера). Её родил
переезд БАЗЫ: `537b296bf` (#433, тред 205) приземлил правку `.github/workflows/claude-review.yml`, и
дверь увидела пару «воркфлоу базы называет файл этого PR».

### 2. Чем нота гасится — и это не «прочитал и успокоился»

Дверь сама говорит, чего её признак НЕ обещает: ловится **ЛИТЕРАЛЬНОЕ ИМЯ ПУТИ** внутри воркфлоу или
шелла, и полноты нет. Отсюда мера, а не вера: **читать, ЧЕМ именно файл назван на той стороне.**

`git grep -n 'PROTOCOL.md' origin/main -- .github/workflows/claude-review.yml` → одно попадание,
строка 443:

> `ЭТОГО репозитория: \`docs/roles/**\`, \`PROTOCOL.md\`, \`REVIEWER.md\`,`

Это ПРОЗА промпта ревьюера — перечень путей доков власти, который воркфлоу вкладывает в промпт. То
есть воркфлоу называет `PROTOCOL.md` **путём**, а #437 меняет его **содержание**: ни переименования,
ни удаления, ни переезда в диффе нет (`git diff --stat` от merge-base — три файла, все `M`). Пара
безопасна по построению, и это замер, а не суждение о вероятности.

**Правило, которое отсюда следует и меняет поведение:** нота пары называет ФАЙЛ, а опасность живёт в
СПОСОБЕ упоминания. Погасить её — значит открыть ту сторону и прочитать, путь там несущий (читается,
исполняется, подставляется) или это перечень в прозе. Пересказ ноты своими словами не гасит ничего.

### 3. Нота переезда базы — вторым замером, по пересечению файлов

Дверь консервативно назвала и переезд базы (`5882812` закоммичен `2026-09-15T08:19:26Z`, credited
`checks` стартовали `2026-09-14T15:14:34Z`). Померила пересечением, а не глазом:

* `git log --oneline a674e6c203..origin/main` → четыре коммита (#438, #431, #433, журнальный хвост 205);
* `git diff --name-only a674e6c203..origin/main | grep -E '^(PROTOCOL\.md|docs/roles/)'` → **ПУСТО**;
* `git merge-tree --write-tree origin/main 01c85a975` → дерево `24a8121d3`, exit 0;
* `mergeable=MERGEABLE`, `mergeStateStatus=CLEAN`.

Сутки простоя базы слияние не изменили. Вердикт `approve` держится: он якорится на ГОЛОВУ, а голова
не двигалась (`headRefOid` = `01c85a9752138b6afa4c9f0df0ffce410b10a27c`).

### 4. Цена простоя названа числом

Требование 4 треда `206` стоит на кнопке john с 14.09 15:2xZ — **≈17 часов**. Пока оно стоит,
карточки ролей `curator` и `dev-core` описывают СТАРУЮ форму журнала (`docs/journal/<роль>/` через
PR), а работает уже новая: эта самая запись — **четвёртое** употребление `journal write`, и PR под
ней снова нет. Расхождение известное, названное и живёт ровно один этот PR.

## Кнопку нажали ВНУТРИ моего замера: письмо устарело за 73 секунды (15.09, тред 206)

Дозапись к разбору выше, и она дороже его. Пока я мерила #437 и писала письмо «ТРЕБУЕТСЯ КНОПКА
JOHN», john её **нажал**.

Хронология по штампам, а не по памяти:

* `08:29:24Z` — мой последний замер (`git status`, `merge-gate` уже отдал `guard 4 — STOP`);
* `08:28:31Z` — `mergedAt` у #437 (squash `e71204c57`, `mergedBy: maysway`);
* `08:28:57Z` — письмо курьера `github` о мёрже легло в тред;
* `08:30:10Z` — ушло МОЁ письмо, просящее нажать уже нажатую кнопку.

То есть merge случился **раньше** моего последнего замера, а я его не увидела: я мерила `merge-gate`
и `gh pr view` в начале такта (`08:27`), а потом сорок минут контекста писала текст по ТЕМ числам.

### Что сработало — и это единственная причина, по которой ошибка прожила две минуты

**Отправитель сам сказал:** `new-message … --write` напечатал ноту

> `1 message(s) landed under your own letter of 2026-09-15T08:21:01Z before this one — written by
> github, the last of them 2026-09-15T08:28:57Z. If what you have just sent says anything about the
> state of this thread, re-read it`

Эта строка — не украшение вывода, а ЕДИНСТВЕННЫЙ сигнал. Читать её обязательно, и читать как приказ:
перечитать тред немедленно, до любого следующего действия.

### Правило, меняющее поведение

**Состояние PR, вокруг которого строится письмо, перемеряется ПЕРЕД отправкой, а не в начале такта.**
Замер в начале такта отвечает на вопрос «что делать», отправка — на вопрос «что сказать», и между
ними лежит всё время, которое ты писала. Одна строка `gh pr view <n> --json state,mergedAt` стоит
секунду и снимает весь класс.

Смежное, замеренное тем же случаем: **парк на `john`, поставленный письмом, которое john уже
опередил, — парк в пустоту.** Он бы заморозил тред на человеке, которому нечего делать. Снимается он
здесь не событием, а моим же следующим письмом.

### Итог предмета 206

Все пять требований в `main` и проверены на СЛИТОМ дереве (`e71204c57`): карточки `curator` и
`dev-core` называют ветку почты, `docs/journal/**` назван архивом, пункт 1 нормы `187` объявлен
потерявшим предмет, исключение `--journals` держится над старыми путями и названо фактом. Тред
закрыт приёмкой. Налог, ради снятия которого john дал предмету наивысший приоритет, снят: эта запись
— четвёртая подряд, положенная без единого PR.
