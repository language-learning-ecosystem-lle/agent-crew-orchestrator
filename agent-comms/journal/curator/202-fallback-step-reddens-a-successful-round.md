# Журнал роли curator — тред `202-fallback-step-reddens-a-successful-round`

# Тред 202 — шаг переезда на запасную учётку краснил УСПЕШНЫЙ круг ревью

Запись curator, 2026-09-14. Предмет треда исчерпан; норма, родившаяся здесь, едет PR #440.

## Что было замерено

С мержа #399 (`1b08b81cf`, 2026-09-13 **17:33:21Z**, тред 193 — «круг ревью переезжает на запасную
учётку») **ни один круг ревью не был зелёным 15 ч 48 мин**, до мержа починки #418 в 2026-09-14
**09:21:40Z**. Шесть прогонов `Claude PR Review` подряд — `failure`.

Механизм вскрыт артефактами ДВУХ разных прогонов, а не пересказом: в обоих
`reviewer-execution-primary.json` нёс `subtype=success`, `is_error=false` и **записанный вердикт
`approve`** ($1,2175 у прогона `34775379733`/#409 и $0,8894 у `34775456728`/#388), а
`claude-execution-output.json` запасной учётки — `api_error_status=401`, `Invalid bearer token`, $0.
Дефектов оказалось два и они разделимы:

1. **детектор лимита судил по НАЛИЧИЮ записи** (`claude-review.yml`, строка 530:
   `select((.type? // "") == "rate_limit_event") | length > 0`), а SDK кладёт `rate_limit_event` в
   КАЖДЫЙ здоровый прогон. В обоих вскрытых транскриптах таких записей 10 и 4, и у каждой
   `rate_limit_info.status = "allowed"`, утилизация окна 1–9 %. Лимита не было ни в одном —
   переезд затевался в обоих;
2. **секрет `CLAUDE_CODE_OAUTH_TOKEN_FALLBACK` отвечал `401`** — непустой (иначе ветка
   `if [ -z … ]` отсекла бы переезд), но не аутентифицировался. Замерено тремя прогонами.

## Почему это была остановка, а не неудобство

**Гард 2 краснел от СВОЕГО ЖЕ круга.** Успешное ревью → `hit=1` → шаг переезда запускался не по делу
→ `401` → джоба красная → на голове встаёт чек `review=FAILURE`. Гард 2 исключений не знает и не
закрывается ни классом полевого дефекта, ни словом человека, — **кнопки merge не было НИ У КОГО,
включая john**. Заперто было 8 открытых PR; два физически доставленных вердикта `approve` выброшены.

Тупик снимало одно наблюдение john: **красный круг починке круга не мешает** — PR, правящий
`claude-review.yml`, автоматика не судит по построению (самопропуск, пока файл ветки не совпал с
дефолтной), метка не вешается, гард 1 закрывается чтением john, гард 4 — его кнопка.

## Порядок починки и его цена

john заказал ОБА, **детектор первым** (`msg-002`). Довод — мой замер, и он про деньги: починка
ТОЛЬКО секрета сделала бы хуже. Детектор продолжил бы срабатывать на каждом здоровом круге, запасная
учётка стала бы отвечать, и каждый круг платил бы **за два полных ревью вместо одного** (~$2 против
~$1). После починки детектора переезд оставался мёртвым до обновления секрета — и это было принято
осознанно: настоящий лимит за весь день случился ОДИН раз (`34774059182`, `api_error_status=429`).

Легло это двумя диффами, и дубля между ними нет: **#418 чинит ПРИЗНАК** (первая ветка судит
`rate_limit_info.status` — отказом считается значение, которое присутствует и не равно `allowed`;
неизвестная форма записи отказом НЕ считается), **#417 добавляет ВЕТО** (`PRIMARY_OUTCOME:
${{ steps.reviewer.outcome }}`, и при `success` переезда нет вообще, до чтения транскрипта). Любой
из двух поодиночке возвращает круг к зелёному; вместе они закрывают и случай «основная пережила
настоящий лимит и всё-таки выдала вердикт», которого признак в одиночку не ловит.

## Что стоит записи ОТДЕЛЬНО — и уехало в норму

**Слово john (`msg-002` §3), дословно:** «пакет, который вводит НОВЫЙ путь исполнения (переезд,
запасной транспорт, второй источник), принимается не мержем, а **первым срабатыванием этого пути**.
До него он не "влит", а "положен"». Сказано это признанием своей ошибки: john торопил влить #399
накануне, не проверив, что переезд работает вообще.

Норма целиком легла PR **#440** (`PROTOCOL.md` + строка в карточке curator) — док власти, кнопка
john. Долг был назван в ленте дважды (`msg-004` §8, `msg-005` §8) прежде, чем закрыт: **названный в
треде долг гниёт ровно до тех пор, пока под него не открыт PR**, и «беру первым же тактом» ни к чему
не обязывает никого, включая написавшего.

**Я применила этот урок к самой починке, и это оказалось не формальностью.** Мержа #418 было мало:
приёмку я заказала своей рукой — сняла и повесила заново метку `review` на #388 (голова
`0169bea8cb…`, круг `34828011351` поднялся через секунду после метки). Голову НЕ двигала намеренно:
круг берёт воркфлоу с merge-ref, поэтому починка из `main` доезжает до него без сдвига головы PR, а
сдвиг осиротил бы приёмку. #388 выбран не случайно — это тот самый PR, чей вердикт за $0,8894 был
убит красным шагом переезда, то есть круг ему был нужен заново при любом раскладе и лишних денег
приёмка не стоила.

**Класс закрылся пятью срабатываниями**, а не одним: все пять кругов после мержа зелёные, и в
новейшем шаг «Ревью на ЗАПАСНОЙ учётке» — `skipped`. Секрет john перевыдал ~10:26Z, вето влито
(#417) ~10:53Z.

## Мелочи, которые стоили такта

- **Общий append-only хвост `docs/journal/dev-core.md` сериализует мержи.** #418 и #417 конфликтовали
  с `main` и друг с другом **только** в нём — `claude-review.yml` сливался сам (замерено
  `git merge-tree --write-tree --name-only`, команда дерево не пишет). Мерж #418 предсказуемо вернул
  #417 в `CONFLICTING`, и избежать этого нельзя было ни при каком порядке. Переезд журнала в файл на
  тред (#408, а с 14.09 — команда `journal write` в ветку почты) этот класс и убирает;
- **дверь `pr open` требует `thread:` и `role:` ПЕРВЫМИ ДВУМЯ строками тела**, а не где-нибудь в нём:
  те же поля в хвосте — отказ по имени, и `gh` не вызывается вовсе. Стоило одной попытки на #440.

---

## 14.09 15:52–16:08Z — зеркальная форма села на СОБСТВЕННЫЙ PR этого треда; лимит основной учётки держится до 18:50Z

Такт связан тредом 202. Пришёл вердикт `approve` ревьюера на #440 (msg-011) — и круг, его
доставивший, **красный**. То есть норма, которую #440 вводит («новый путь исполнения принимается
ПЕРВЫМ СРАБАТЫВАНИЕМ, а не мержем»), не смогла быть влита ровно тем путём, о котором она написана.
Замер целиком ниже.

### 1. Круг `34864312409` — перепись ШАГОВ, а не цвет

`gh run view 34864312409 --json jobs`, голова `c32b6ee4efc48c83b13f1c743a1307ac9e09b898`,
`event=pull_request`, `15:46:01Z…15:51:46Z`, **completed/failure**:

| № | шаг | исход |
| --- | --- | --- |
| 9 | `anthropics/claude-code-action@v1` (ОСНОВНАЯ) | **failure** |
| 10 | Лимит основной учётки — установить факт | success |
| 11 | Ревью на ЗАПАСНОЙ учётке — переезд по лимиту | **success** |
| 12–17 | выход ревьюера, самопропуск, ТРИ доставки, артефакт | success все |

Форма совпадает знак в знак с `34841729661`/#426, `34849381027`/#428, `34850008811`/#416 — это
четвёртое срабатывание класса, и первое, где заложником стал PR самого треда 202.

### 2. Переезд был ЗАКОННЫЙ — судил транскрипт целиком, а не строку `evidence`

Правило из прошлого блока применено сразу. Шаг `limit` напечатал
`status=allowed_warning` — по одной этой строке разбор был бы «переезд по предупреждению», и он был
бы неверен. `reviewer-execution-primary.json`, последняя запись:

```
subtype: success, is_error: true, num_turns: 11, duration_ms: 68003,
api_error_status: 429, result: "You've hit your session limit · resets 6:50pm (UTC)"
```

Раскладка `rate_limit_event` в том же транскрипте: `allowed_warning` ×1, **`rejected` ×1**. Лимит
настоящий, переезд обязан был случиться, **#431 этого круга не изменил бы** — он сузил предикат, а
здесь предикат сработал по делу.

### 3. Цена одного вердикта — $0,9068, круг красный

Основная `$0.2648` (11 ходов, упала на стене) + запасная `$0.6420` (26 ходов, вердикт вынесен и
доставлен) = **$0.9068**. Вердикт верен по содержанию (сверен по REVIEWER.md, критерии 1–12), доехал
тремя доставками — и негоден обоим гардам: дверь на #440 отвечает
`STOP guard 1 · no CLOSED round … completed/failure` и `STOP guard 2 · not green: review=FAILURE`.
Гард 4 у #440 и так STOP (доки власти), но **теперь кнопки нет и у john** — чек `review` на голове
красный.

### 4. НОВОЕ И НЕСУЩЕЕ: у красноты появилось ОКНО, а не вероятность

Предыдущий блок понижал критерий перед меткой до вероятности («три красных подряд не значит, что
четвёртый красный»). Сегодняшний замер даёт то, чего тогда не было: **основная учётка назвала время
сброса — `resets 6:50pm (UTC)`**. Пока окно не сброшено, основная отвечает `429` на каждом круге,
переезд случается ЗАКОННО на каждом круге, и шаг №9 красит джобу на каждом круге.

**How to apply:** увидев в транскрипте основного шага `api_error_status: 429` с текстом
`resets <время>` — до этого времени метка `review` не вешается вовсе: круг красный ПО ПОСТРОЕНИЮ, и
это не вероятность, а расписание. Цена ошибки — ~$0,9 за вердикт, не годный ни одному гарду.
Проверяется одной командой на любом свежем красном круге:
`gh run download <id> && jq '(if type=="array" then . else [.] end)|last|{api_error_status,result}' reviewer-execution-primary.json`.

### 5. Дом починки уже стоит открытым PR — своего заводить не нужно

Перепись `gh pr list --state open` (15:58Z, 15 открытых) против находки:

- **#433** (`fix(ci-review): цвет круга решает доставка вердикта, а не исход шага ревьюера`, тред 205)
  — это РОВНО разрез (а) из прошлого блока, и это единственный PR, который снимает красноту;
- **#431** (`allowed_warning` — предупреждение, а не отказ, тред 201) — разрез (б); сегодняшнего
  круга не касается (см. §2);
- **#436** (`красное в круге значит «вердикт не доехал»`, тред 201).

У всех трёх `checks=SUCCESS`, `mergeable=MERGEABLE`, `CLEAN`, метки нет (правильно: PR по
`claude-review.yml` круг не судит по построению). Кнопка на всех трёх — john, гард 4.

### 6. Порядок мержа трёх починок — замерен `merge-tree`, а не обещан

| пара | merge-base | исход |
| --- | --- | --- |
| #433 × #431 | `e55976b5a` | CONFLICT — `review-delivery.integration.sh` |
| #433 × #436 | `e55976b5a` | CONFLICT — `review-delivery.integration.sh` И `claude-review.yml` |
| #431 × #436 | **`3cd64e456` = голова #431** | чистo — и это НИЧЕГО не доказывает |

Третья строка — вырожденная пара: `git merge-base --is-ancestor` подтверждает, что **#436 СОДЕРЖИТ
#431** (`git log origin/main..06bc195e2` даёт два коммита: `06bc195e2` поверх `3cd64e456`). То есть
починок в поле не три, а **две независимые**: `#433` и стопка `#431→#436`.

**Рекомендация порядка: #433 ПЕРВЫМ.** Довод — он единственный снимает красноту, а стопка
`#431→#436` уточняет предикат, который сегодня сработал по делу. Ребейз платит стопка, и он ровно
один при любом из двух порядков; `#431` отдельно жать не нужно — его содержимое приедет с `#436`.

### 7. Что осталось у #440 после всего

Голова `c32b6ee4…` несёт доставленный `approve` и красный `review`. Лечится ОДНИМ из двух, и оба
дешевле третьего:

1. **влить #433** → перевесить метку на ту же голову → круг зелёный (после 18:50Z — гарантированно,
   до — если основная успеет ответить);
2. **дождаться 18:50Z** и перевесить метку — ~$0,9 за круг и без починки контура.

Голову #440 двигать нельзя ни в одном из двух: это осиротит вердикт, который уже оплачен.

# 2026-09-15, такт curator: метка на #440 самопропустилась бы — merge-ref отставал от `main` на две ПРАВКИ САМОГО РЕВЬЮЕРА

Запись дополняет файл треда 202. Предмет такта был один: круг на #440 после того, как #433 влит
(msg-013), и #440 «ждёт своего круга». Метка на нём висела со старой головы — то есть кругом не была.

## 1. Что замерено ДО метки (две команды из [[label-needs-a-merge-ref-blob-matching-main]])

```
git rev-parse origin/main:.github/workflows/claude-review.yml   → b2babcfb972e992cde35a7256e4c8f841619ce46
git rev-parse refs/pull/440/merge:.github/workflows/claude-review.yml → bd6095d968fdf1eb1261a79e76ba4ce164a2159c
```

Блобы РАЗОШЛИСЬ. Merge-ref был `Merge c32b6ee4e into b48f0bf77` — база `b48f0bf77` предок `main`, но
отстала на два коммита, и оба трогают ИМЕННО файл ревьюера:

- `537b296bf` — #433, «цвет круга решает доставка вердикта, а не исход шага ревьюера» (тред 205);
- `041730a84` — #445, «смерть основного шага ревьюера называется вслух в шаге итога» (тред 201).

`git diff --stat pr440merge origin/main -- .github/workflows/claude-review.yml` → +82 строки.

## 2. Новое и несущее для этого треда: в ловушку загнала ПОЧИНКА, снявшая красноту

Четвёртый случай расхождения блоба за двое суток, но первый с такой причиной. Прошлые
(треды 191, 14–15.09) — старая голова либо движение базы вообще. Здесь база уехала **ровно тем
диффом, ради которого круг и заказывался**: #433 чинил красноту круга, и пока #440 не вобрал его,
круг на #440 не то что покраснел бы — его бы НЕ БЫЛО вовсе (самопропуск, прогон ЗЕЛЁНЫЙ, вердикта
нет, гард 1 не якорит ничего). То есть форма «жду починку → чиню → вешаю метку» имеет ТРЕТИЙ шаг,
который сам по себе не виден: **влить починку в ветку, а не только в `main`**.

Строка механизма (`.github/workflows/claude-review.yml` на `main`, шаг `guard`, ~строка 1300):
«файл `.github/workflows/claude-review.yml` в этом прогоне отличается от версии на дефолтной ветке,
и `claude-code-action` пропустил сам себя (гард против промпт-инъекции правкой воркфлоу)».

## 3. Что сделано рукой

`git merge origin/main` В ветку `curator/202-new-path-accepted-by-first-firing` (не rebase — force не
покупается, [[rebasing-an-open-pr-needs-no-force-push]]): конфликта нет, маркеров нет, push
fast-forward — `c32b6ee4e..ca5474226` БЕЗ `+` в выводе `git push --dry-run`.

Приёмка вклада двумя мерами, несущая — первая:

- `git diff --numstat origin/main HEAD` → `40 0 PROTOCOL.md`, `10 0 docs/roles/curator.md` — ровно
  вклад PR до склейки, предмет не уехал;
- `gh pr view 440 --json files` от самого GitHub — та же таблица (`+40/-0`, `+10/-0`);
- 89 удалений в `git diff c32b6ee ca5474226` — вклад САМОГО `main` (его же мера от c32b6ee до
  `origin/main` — 139 удалений), а не переписанный абзац.

После пуша блобы сошлись: merge-ref `adac0be97` = `Merge ca5474226 into 49653f41f`, блоб
`b2babcfb9` = `main`. Метка со старой головы СНЯТА (`--remove-label review`): снятие круга не
поднимает, а висящая метка лжёт читателю, что круг был.

## 4. Дверь на новой голове (`ca5474226`, 10:33Z)

- `STOP guard 1` — и формулировка двери ценна сама по себе: «a verdict older than the head commit
  (approve by github-actions at 2026-09-14T15:51:40Z; ca54742 committed 2026-09-15T10:22:07Z): a
  review submitted with no commit of its own is shown against whatever head the PR has now». Тот
  самый вердикт-сирота: объект `approve` на PR ВИДЕН, ответом про новую голову не является;
- `STOP guard 2 · checks=IN_PROGRESS` — прогон `34957600405` на `ca5474226`;
- `you guard 3` — тред 202, слово john в `msg-002` §3;
- `STOP guard 4` — `PROTOCOL.md` + `docs/roles/curator.md`, кнопка john; дверь называет ВОСЕМЬ доков
  власти данными из конфига;
- `mergeable=MERGEABLE (UNSTABLE)`.

## 5. Что НЕ сделано и почему

Метка не повешена: карточка требует зелёного `checks` на ТОЙ ЖЕ голове раньше метки, а прогон
`34957600405` живой. В руке его не ждала (железное правило `037`) — парк `run:440`, исход звонит
письмом с полем `run-outcome`.

## 2026-09-15 11:03Z — ПРИЁМКА ТРЕДА 202 СРАБОТАЛА В ПОЛЕ: переезд на запасную дал ЗЕЛЁНУЮ джобу при МЁРТВОМ основном шаге

Приёмка объявлена мной же в msg-013: «первый прогон, у которого шаг „Ревью на ЗАПАСНОЙ учётке“ не
`skipped`, обязан прийти с зелёной джобой при красном шаге ревьюера; вторая половина — строка о
переезде и вердикт в ОДНОМ комментарии». Обе половины замерены на круге **`34959992027`** (PR #440,
голова `ca54742268558896286e2a2a4bdb323cdf8efe92`, `event=pull_request`, 10:49:19Z…10:53:46Z),
`conclusion=success`.

### Половина 1 — джоба зелена, основной шаг мёртв

Шаг №11 «Ревью на ЗАПАСНОЙ учётке — переезд по лимиту основной» — **не `skipped`**, то есть переезд
сработал по-настоящему; это первый такой круг после мержа #433 (`~08:18Z`). Печать шага №16 «Итог
доставок» дословно:

```
Доставки вердикта: все три доставки прошли: письмо в тред, коммент в PR, формальный review-статус.
основной шаг ревьюера: failure · переезд на запасную: 1 · лимит основной: 1 · запись type=rate_limit_event со status=rejected
```

Плюс `::notice::Основной шаг ревьюера кончился 'failure', круг доработала ЗАПАСНАЯ учётка. Цвет джобы
судит доставка вердикта, а не этот шаг (тред 201).` До #433 этот же круг был бы КРАСЕН по построению,
и вердикт не годился бы ни гарду 1, ни гарду 2 (пятое срабатывание формы — 14.09 16:07Z).

### ЗАМЕР, КОТОРЫЙ ЛЕГКО ПОТЕРЯТЬ: смерть основного шага НЕ ЧИТАЕТСЯ переписью шагов

`gh api …/jobs` даёт шагу №9 (`anthropics/claude-code-action@v1`) **`conclusion=success`** — и это не
ошибка API, а работа `continue-on-error: true` (`claude-review.yml`, строка 271; шапка шага говорит
это прямо: «`continue-on-error` меняет `conclusion` шага на `success`»). То есть прежний приём
диагноза — «переписать ШАГИ джобы, упавший №9 при зелёных 10–17» (правило, действовавшее 14.09) —
с #433 **перестал работать**: по переписи шагов круг выглядит так, будто основная учётка отработала.

Единственное место, где смерть основного шага ещё сказана словом, — печать шага №16, и она читается
логом, а не JSON-ом шагов:

```
gh run view <id> --log -R <owner>/<repo> | grep "основной шаг ревьюера:"
```

Это ровно то, ради чего #445 (тред 201) добавлял `PRIMARY_OUTCOME` в шаг итога: без него перенос
значения красного стал бы молчаливым съеданием отказа. Замер подтверждает, что строка несущая.

### Половина 2 — переезд и вердикт в ОДНОМ комментарии

Коммент `github-actions` в PR #440 от 10:53:35Z несёт тело вердикта и хвостом — строку
`🔁 Круг доехал на ЗАПАСНОЙ учётке: основная ответила лимитом (запись type=rate_limit_event со
status=rejected), и сработал переезд — один на прогон, без цепочки повторов`. Та же пара приехала
письмом `reviewer-pr` в тред 202. Обе половины сошлись.

### Что это меняет в поведении роли

1. **Окно лимита больше не запрещает метку.** Правило 14.09 («найдя в транскрипте `result` с
   `resets <время>` при `429`, метку не вешать вовсе до этого времени») стояло на том, что круг
   внутри окна КРАСЕН и вердикт негоден гарду 1. С #433 круг внутри окна ЗЕЛЁН и вердикт годен —
   замерено здесь. Остаётся денежный довод (круг внутри окна стоит ценой запасной), но он больше не
   запрет, а цена;
2. **диагноз красного/зелёного круга с переездом — логом шага №16, а не переписью шагов** (см. выше);
3. **приёмка нового пути наступает первым срабатыванием, а не мержем** — это предмет #440, и сам #440
   стал своим же примером: починка (#433) была влита 08:18Z, а принята — здесь, 10:53Z, на первом
   настоящем лимите основной учётки.

### Состояние #440 на 11:03Z

`pnpm protocol merge-gate --ref origin/main --pr 440 --review-workflow 'Claude PR Review'`:
`ok guard 1` (approve на `ca54742` ВНУТРИ круга `34959992027` — то есть вердикт якорится прогоном, а
не объектом review), `ok guard 2` (`review=SUCCESS, checks=SUCCESS`), `you guard 3` (тред 202, слово
john в msg-002 §3), **`STOP guard 4` — `PROTOCOL.md` + `docs/roles/curator.md`, кнопка john**,
`mergeable=MERGEABLE (CLEAN)`.

**Нота двери о переезде базы отвечена замером, а не прозой.** Дверь назвала: база уехала после старта
зачтённых `checks` (`4ccc9e6` 10:41:20Z против старта `checks` 10:22:32Z). Мера: с базы `49653f41f`
до `origin/main` файлы предмета тронуты ОДИН раз — `docs/roles/curator.md` `+28/-0` (#446 тред 197,
#447 тред 191), `PROTOCOL.md` не тронут вовсе; `git merge-tree --write-tree origin/main ca54742` →
дерево `55e15d7ac`, **конфликта нет** (это не вырожденная пара: меряется голова PR против ЖИВОГО
`main`, а не два PR между собой). Предметы не пересекаются: #446 — про гард 3 в постановке, #447 —
про метку и merge-ref, #440 — про приёмку нового пути исполнения.
