# Журнал роли curator — тред `222-self-restart-report-wakes-a-role`

## Отчёт о самоперезапуске пишет ПРЕЕМНИК — то есть новый код (приёмка #465)

`#465` (П-1: письмо ящика о себе отпускает ход прочерком) влит `17:11:12Z`, squash
`d14dd4ff099d5c07412987cc136c6ab750bbd2db`. Гарды: вердикт `approve`
(`222-…/messages/2026-09-17T17-09-45Z-reviewer-pr.md`, якорь — круг `Claude PR Review`
`35250650983`, `event: pull_request`, голова `7dcbf4e3ed0e815b979ae7aa4f10604b98c6e95f`, окно
`17:06:06Z…17:09:53Z`), зелёные `review`+`checks` (`35248590690`) на той же голове, гард 4 — 4 пути,
доков власти нет. **Гард 3 закрыт СЛОВОМ JOHN, а не классом полевого дефекта:** постановку писала
роль curator (msg-001), и без слова john круг «сама поставила → сама приняла» был бы замкнут;
слово доставлено письмом `2026-09-17T16-34-40Z-curator.md` («НИКТО НЕ ПОДНИМАЕТСЯ НА ТАКУЮ МЕЛОЧЬ»,
сессия чата `~16:34Z`), и дифф кодирует ровно его.

**Находка, из-за которой приёмка П-4 не отодвигается на второй перезапуск.** Правило
«самоперезапуск исполняет СТАРЫЙ бинарь» (замерено 17.09 на цепочке `c1240cf6 → 9c4803d8 →
5cc15c23`) верно про ПОСЛЕДОВАТЕЛЬНОСТЬ рестарта: `down` → `pull --ff-only` → установка → подъём
исполняет процесс, спавненный ДО pull. Отчёт о перезапуске в эту последовательность не входит: его
кладёт преемник на своём тике в ветви `reading.kind === "match"` (`cli.ts`, комментарий «141,
package 3 — AND THIS IS WHERE THE SUCCESSOR RECOGNISES ITSELF» → `selfRestartEvent` →
`postSelfRestartLetter`), то есть демон, уже поднятый из подтянутого дерева. Значит для починки
ФОРМЫ письма полигон — ПЕРВЫЙ перезапуск после мёржа, и он приезжает сам: `self-restart.json`
несёт `target` = этот самый merge-коммит, `drainSince 17:11:12Z`, а `daemon.log` в ту же минуту
печатает «DRAINING TO RESTART … waits for the sessions that are live
(`curator/222-self-restart-report-wakes-a-role`)». Петля, о которой тред: перезапуск ждёт сессию,
которую сам же и породил.

**Следствие для формы хода.** Приёмка П-4 меряется на стоячем адресе `161-daemon-self-restart`
(новое письмо с `target: d14dd4ff…`, шапка на ДИСКЕ с `waiting-on: —`, `mail` не называет пары,
`daemon.log` — «raising nobody»), а события, которое разбудило бы этот тред, НЕТ по замыслу самого
диффа: письмо никого не поднимает. Парка поэтому нет формы (`run:`/`pr:` нечем — прогонов в полёте
нет, PR влит), и ход взят себе без парка. Пустым такт быть не может по построению: дренажащийся
ящик не поднимает ничего до перезапуска, значит первый же подъём пары приходит от преемника — уже
после эпизода. Один подъём, и он же приёмка и закрытие треда.

## Приёмка П-4 полем: отчёт о самоперезапуске больше не поднимает роль

Тред `222-self-restart-report-wakes-a-role`, такт curator 17.09 `17:18Z…17:40Z`. Здесь — замер, из
которого тред закрыт, и та его часть, что переживает предмет.

**Эпизод дал сам контур, и дал его МОЙ ЖЕ мёрж.** #465 влит `17:11:12Z` (squash `d14dd4ff099d`);
`self-restart.json` в `17:16:53Z` — `{"target":"d14dd4ff…","from":"db8a391d…","behind":1,
"attempts":1,"went":true}`. Демон-предшественник `db8a391d` дренажился, дождался закрытия сессии
`curator×222`, ушёл по стоп-флагу через 35 с, подтянул дерево (`git pull --ff-only — ok`,
`pnpm install — ok`) и поднял преемника: `=== daemon epoch · 2026-09-17T17:17:31Z · pid 2355145 ===`,
строка `code: d14dd4ff loaded`. То есть отчёт писал УЖЕ НОВЫЙ код — ровно как и предсказано в
msg-011 §3: последовательность рестарта исполняет старый бинарь, а письмо кладёт преемник на своём
тике в ветви `reading.kind === "match"`.

**Четыре критерия приёмки (msg-011 §4) сошлись все, и мерены порознь:**

1. письмо есть — `agent-comms/161-daemon-self-restart/messages/2026-09-17T17-17-47Z-github.md`,
   тело называет `d14dd4ff099d` («какой стал») и `db8a391d5f85` («какой был»);
2. шапка НА ДИСКЕ (`git show origin/comms:<путь>`, не рендер) — `expects: none`, `waiting-on: —`;
3. `cli mail --role` по всем трём ролям контура: `161` не назван НИ ОДНОЙ (`curator` — только `219`,
   `029`, `222`; `dev-core` и `devops` — пусто). В `daemon.log`: «letter — the self-restart is posted
   to the standing address 'daemon-self-restart', turn released ('—'): it is a record and raises
   nobody»;
4. план того же тика — «1 launch, one per free role — curator×222…», пары на `161` нет; на
   следующих тиках кандидатов тоже три (`219`, `029`, `222`), а строка письма ушла в
   «SUPPRESSED, nothing new to say … raising nobody».

**Почему это ГОДНОЕ срабатывание, а не «не могло провалиться».** Тот же путь на прошлом эпизоде
(`16:24:15Z`, цель `db8a391d`) написал `waiting-on: curator` — файл лежит рядом, шапки отличаются
одной строкой, — и поднял `curator×161` на семь минут и десять разделов замеров, а заодно задержал
следующий рестарт john'а («still waiting for pid … — 180s so far»). Два письма одного кода, до и
после диффа, с противоположным исходом: провал имел форму и не наступил.

**Что осталось верным про стоячий адрес `161`.** Он открыт и остаётся открытым, но пары на нём
больше не бывает никогда: письмо туда — запись. Читает его тот, кто пришёл по своему делу, или john
в чате. Искать там «поднятую сессию» — искать то, чего код больше не производит.

**Побочный замер такта, к предмету треда не относящийся:** в оболочке поднятой сессии curator нет
`node` на `PATH` вовсе (`node: command not found` на первой же команде почты), интерпретатор — в
`/home/lle/.nvm/versions/node/v24.18.0/bin`, а дом контура сессии читать нельзя, так что `find` его
не находит. Это НЕ дефект, который чинил `219`/#464 (там `PATH` правился у инструмента, спавненного
демоном). Записано в память роли; в тред это не предмет, и своего треда я на это не заводил.
