# Журнал роли curator — тред `198-mute-key-by-name-eats-other-bells`

# Нота о переезде базы гасится ДИФФЕРЕНЦИАЛЬНЫМ прогоном, когда почва красит всё подряд

Тред 198, кнопка john по #419 (2026-09-15T08:40:49Z, squash `4a63df99d`). Замер мой, 08:39…08:47Z.

## Почему «прогнать шаг на дереве слияния» само по себе не ответ

Нота двери говорит: зачтённый `checks` (`34854707365`, старт 14.09 14:18:02Z) мерил базу
`b20ac957d`, а `main` к моменту кнопки уехал до `e71204c57` — четыре коммита (#431, #433,
журнальный, #437). Прямой ответ — собрать слияние и прогнать по нему шаги джобы. Но на ЭТОМ ящике
шаг `review-delivery.integration.sh` не зеленеет НИ НА ЧЁМ: в копии дерева `origin/main`
(`git archive` в `/tmp`, без `.git` и без `node_modules`) он даёт 39 провалов, в рабочем дереве
роли на том же `main` — 37 провалов тех же состояний (`1 2 3 4 5 6 8 9 16 14 15`). Почва, а не
дифф. Прогон «на дереве слияния» в одиночку тут не доказывает ничего: он красен и без PR.

## Что доказывает — прогон ПАРЫ

Копия дерева слияния (`merge-tree --write-tree e71204c57 0ecfe30` → `0c9c1e5b6`) и копия чистого
`main`, один и тот же скрипт, одна и та же почва: **39 провалов, состояния совпадают посимвольно**.
Утверждение скромное и верное — PR в исходе этого шага не меняет НИЧЕГО. Красное остаётся красным
у меня и зелёным на раннере; про раннер замер не говорит и говорить не вправе.

## И половина работы снимается вообще без прогона — сверкой ПОТРЕБИТЕЛЕЙ

Дерево слияния отличается от дерева, которое `dev-core` прогнал полной сюитой сутки назад
(`57e7b5ca7`, база `b1a451bb2`), ровно девятью файлами базы: `review-delivery.integration.sh`,
`claude-review.yml`, `PROTOCOL.md`, `docs/protocol-reference.md`, две карточки ролей, три записи
журнала. Список потребителей правленого скрипта на `main` (`git grep -l 'notifier-mute\.sh'`) —
`foreign-name-watch.sh`, `review-delivery.sh`, `notifier-watch.yml`,
`notifier-mute.process.test.ts` — с этой девяткой не пересекается НИ ОДНИМ путём, а `grep
notifier-mute` в двух переехавших файлах CI даёт ноль попаданий. То есть у переезда базы нет
механизма дотянуться до предмета PR, и сюита суточной давности переносится. Прогон нужен был
ровно для одного шага — того, чей вход переехал.

**Порядок, который стоит повторять:** (1) `merge-tree` и дельта к ПРОМЕРЕННОМУ дереву, а не к
`main`; (2) пересечение дельты со списком потребителей правленых путей; (3) прогон парой — только
для того, что осталось после (2).

## Побочный замер, стоивший половины такта: состояние протухает, пока его меряешь

Кнопку john нажал 08:40:49Z — В СЕРЕДИНЕ моего замера. Я мерила дверь в 08:40Z (она честно
ответила `STOP` гарда 4), а в 08:43Z `gh pr view` отдал уже `MERGED`. Практическое следствие для
такта: письмо «требуется решение john» я не отправила и правильно сделала, что перед его
составлением сняла состояние ещё раз. Снимок PR, взятый до длинного замера, к концу замера
адресует несуществующий мир.

## 15.09, такт метки на #448: сверка предмета перед меткой и замер того, что приёмка #419 не придёт сама

**Замер приёмки #419 — главное этого такта, и он отрицательный по существу, а не «пока не пришло».**
`gh run list --workflow notifier-watch.yml --limit 100` на 2026-09-15T09:12Z: сто прогонов подряд в
окне `2026-09-14T16:52:00Z … 2026-09-15T09:12:52Z` (16 ч 21 мин), **conclusion `skipped` у всех ста,
non-skipped нет ни одного**. За 31 минуту с кнопки john по #419 (08:40:49Z) — 48 прогонов, все
`skipped`. Смотритель с гейтом дельты молчит на чистом дереве, и звонить ему не о чем, пока в поле не
провалится настоящий круг.

**Следствие, которое легко принять за «ждём»:** приёмка нового пути исполнения (два отказа РАЗНЫХ PR
в 900 с → ДВА письма) не наступает от времени. Её рождает авария, которой на дереве нет, поэтому у
треда `198` нет события, которое его закроет: «полевая приёмка ключа копится отдельно» означает
«копится СОБЫТИЕМ, которого может не случиться неделю», а не «приедет следующим тактом». Тред стои́т
открытым по случаю 3, и это его единственный живой предмет; когда случай 3 сядет, вопрос «чем
закрывается приёмка #419» придётся ставить прямо, а не ждать.

**Сверка предмета #448 перед меткой** (голова `94f8298b86928210e0a486bd4ec913194cba0c76`, дифф читан
`gh api …/pulls/448/files`, не пересказ доклада):

- **три пути, ровно объявленные:** `notifier-mute.sh` `+40/−0`, `notifier-mute.test.sh` `+95/−5`,
  `notifier-watch.yml` `+20/−1`. Журнала в диффе нет — он ушёл командой в ветку почты;
- **граница 3 постановки соблюдена:** формулировки письма остались в `.yml` (хунк `@@ -351,8 +351,27`),
  в `.github/scripts/**` не переехало ни одной;
- **граница 1 соблюдена:** слова `900` дифф не трогает, второго срока рядом с окном не заведено;
- **своего отбора у нового предиката нет** — `notifier_mute_muted_at` берёт id, уже отобранный
  `notifier_mute_muted_ids`, и набор по-прежнему даёт ОДИН предикат; расхождения счёта со списком не
  построить;
- **отказ чтения времени назван по имени** (`|| MUTED_ID_AT='время не прочитано'`), а не пустой
  строкой: пустая напечаталась бы голым числом, то есть вернула бы чинимый дефект. Письма отказ не
  роняет;
- **гард 4:** `.github/workflows/notifier-watch.yml` — кнопка john, как и планировалось с первого
  такта постановки.

**Чего этот такт НЕ доказал:** зелени `checks` на этой голове (прогон `34950922668` стартовал
09:09:09Z и на 09:12Z шёл), а значит метки на #448 ещё нет. Метка вешается только после зелени на
ТОЙ ЖЕ голове, поэтому ход ушёл за событием исхода с парком `run:448`.

## Такт 15.09: моя метка сожгла круг на #448 — merge-ref нёс ДОландинговый `claude-review.yml`

Замер целиком, потому что он про МОЮ руку, а не про исполнителя.

**Что сделала.** `checks` на голове `94f8298b86928210e0a486bd4ec913194cba0c76` сел зелёным
(прогон `34950922668`), обе половины условия метки сняла своей рукой в `09:41Z` — голова та же,
прогон по голове ровно один, круга ревью нет. Повесила `review` в `09:41:40Z`. Круг
`34953914394` поднялся в `09:41:42Z` и **закончился в `09:42:16Z` (34 с) без `verdict.md`** —
самопропуск.

**Почему — измерено, а не принято с прозы письма ревьюера.** PR #448 правит
`notifier-mute.sh`, `notifier-mute.test.sh`, `notifier-watch.yml` — `claude-review.yml` в его
файлах НЕТ, значит случай «правлю воркфлоу сам» исключён. Разошлось другое:

- `main` уехал в `041730a84` (`09:14:29Z`, #445 «смерть основного шага ревьюера называется вслух»),
  и #445 трогает **`.github/workflows/claude-review.yml`**: блоб на `main` — `b2babcf9`;
- база PR — `117164193`, блоб там `4eb5716d`;
- `refs/pull/448/merge` = `11e0e5cb`, **родители `[117164193, 94f8298b]`, дата `09:09:06Z`** —
  merge-ref пересчитан НЕ был, база в нём доландинговая, блоб `4eb5716d`.

Раннер для `pull_request` берёт воркфлоу с merge-ref — значит действие увидело файл, не совпадающий
с дефолтной веткой, и самопропустилось по гарду против промпт-инъекции.

**Новое знание, ради которого запись.** `mergeable=MERGEABLE` и `mergeStateStatus=CLEAN` **не
являются свидетельством свежего merge-ref**: dev-core снял их в `09:37Z` — через 23 минуты после
посадки #445 — и они были зелены, а merge-ref всё это время стоял на старой базе. Поля сливаемости
и merge-ref пересчитываются раздельно. Оракул — РОДИТЕЛИ merge-ref
(`gh api git/ref/pull/N/merge` → `commits/<sha>` → `.parents`), и только они.

**Чего стоила ошибка.** Один круг ревьюера (квота), один такт треда и одна лишняя посадка `checks`
впереди: лечение двигает голову. Дешёвая профилактика существовала и была мне известна —
`git diff origin/main <голова> -- .github/workflows/claude-review.yml` ПЕРЕД меткой: на этой голове
она непуста (`4eb5716d` против `b2babcf9`), то есть предсказывала самопропуск. Я её не прогнала,
потому что держала в голове «дифф PR этих путей не трогает» — а решает не дифф PR, а блоб на голове.
**Следствие для руки: команда гоняется как ЧАСТЬ навешивания метки, а не «при подозрении».**

**Что сделала дальше.** Метку сняла тем же тактом: она переживает новый пуш, а круг поднимает только
СОБЫТИЕ навешивания — метка, оставшаяся висеть, даёт тихий замок, при котором и уведомитель молчит.
Ветку подтягивает исполнитель (`.github/workflows/**` — мой `forbidden`, мержем `main` в ветку я бы
писала в запрещённый путь своей рукой).

**Происхождение класса — чужой тред.** Ровно этот класс стоит открытым PR #447 треда
`191-label-only-on-a-fresh-merge-ref` («метка вешается только на merge-ref, чей `claude-review.yml`
совпадает с `main`»). Мой случай — его ВТОРОЕ полевое срабатывание, замеренное сегодня и уже не на
предположении о merge-ref, а на его родителях. В ленту 191 я не писала: прогон привязан к 198
одному. Запись кладу сюда, происхождение называю прозой — форма «файл на тред, где ХОЖУ».

# Метка `review` повешена на #448: пара блобов измерена в ту же минуту, нота базы воспроизведена своей рукой

Такт из одного действия: метка. Ниже — то, чем оно было закрыто, и одно обобщение, которое
сокращает замер ноты базы на доковом переезде.

## 1. Что сделано

- пара блобов `claude-review.yml` (merge-ref против `main`) измерена в `10:28:58Z`, обе стороны
  `b2babcfb972e992cde35a7256e4c8f841619ce46`, голова `c6461a9508d2b114adbbbe260c7cb5738e8eee32`;
- метка `review` навешена в `10:29:06Z` (восемь секунд после замера — намеренно, свежесть пары
  оракулом не является, и мерить её надо в минуту навешивания);
- контрольное чтение: `labels=[review]`, голова та же, круг `Claude PR Review` `34958176136`
  (`event=pull_request`, `queued`, старт `10:29:08Z`). Круг поднялся событием метки — то есть
  самопропуска на этой голове действительно не будет.

Merge-ref при этом был ОТСТАВШИМ: первый родитель `7dd53c74`, `main` уже `49653f41`. Блобы совпали
всё равно — подтверждает свойство, названное dev-core (msg-033 §1): ветка `claude-review.yml` не
трогает, значит блоб в merge-рефе равен блобу `main` всегда, кроме случая «между пересчётом рефа и
меткой сел PR, правящий самого ревьюера». Свежесть рефа не оракул ни в одну сторону.

## 2. Нота базы под гардом 2 — чужой замер воспроизведён тождественно

dev-core погасил её в msg-033 §3. Проверила своей рукой теми же инструментами:

- `main` = `49653f41` (с их замера не уехал);
- `git merge-tree --write-tree c6461a950 origin/main` → exit 0, дерево `ab73caf5cf7edf54fd10df08222ddb2702dcf3cf` — тот же хеш, что у них, значит спорим об одном объекте;
- дерево, которое зелень читала фактически, — `refs/pull/448/merge^{tree}` = `3a6b5104a8020bf38626f9558817359ff39fefa0`;
- `git diff --numstat 3a6b5104 ab73caf5` → ровно два пути: `docs/install-notes.md` +5/−1, `docs/protocol-reference.md` +22/−0.

Дальше их вывод «читателя в прогоне нет» проверила не пересказом, а чтением файла воркфлоу.

## 3. Обобщение: шаг `checks`, читающий БАЗУ, ровно один, и он назван

`.github/workflows/checks.yml` — шаг «исключительный ресурс не разошёлся с базой»
(`if: github.event_name == 'pull_request'`, `node scripts/exclusive-freshness.mjs --base origin/$base
--head refs/remotes/pull/head`). Его предмет — НОМЕР ВЕРСИИ СХЕМЫ, и голову он тянет явно
(`refs/pull/<N>/head`), потому что на merge-ref общий предок с базой — сама база, и «в базе уехало»
вышло бы пустым всегда; это в файле объявлено комментарием как несущее, а не как придирка.

Все прочие шаги читают ЧЕКАУТ, то есть уже слитое дерево: `config check --ref HEAD`, lint,
typecheck, сюита. Отсюда правило чтения ноты, которого в перечнях раньше не было поимённо:
**переезд базы, состоящий ТОЛЬКО из доковых путей, зелень не обесценивает**, и замер сокращается до
трёх команд (merge-tree → numstat против дерева, которое зелень читала → греп уехавших путей на
чтение С ДИСКА). Греп по `*.test.ts`, `*.test.sh` и `.github/workflows/*.yml` дал здесь 28
совпадений — все до одного литералы в фикстурах (`gate.test.ts`, `base-note.test.ts`,
`pair-note.test.ts`, `gate.process.test.ts`, `self-restart.test.ts`) и проза комментариев
(`checks.yml:96`, `merge-notify.yml:22,27`). Ни одного `readFileSync` этих путей.

Комментарий `checks.yml:96` тут стоит прочитать отдельно, потому что он ловушка наоборот: он прямо
называет `docs/protocol-reference.md` файлом, «куда по дисциплине дописывает каждый предмет», и
объявляет, что дверь исключительных ресурсов против него НЕ ставится (62 текстовых столкновения
против 4 по номеру схемы). То есть переезд именно этого файла — заведомо вне предмета единственного
базочитающего шага.

## 4. Решение, которое просил dev-core (msg-033 §3)

Зелень на `c6461a950` под уехавшей базой зачитываю. Голову не пересдаём: пересдача сбросила бы
зелень и метку в начало ради переезда, у которого нет механизма дотянуться до предмета. Это МОЯ
практика, а не норма: в `PROTOCOL.md` слов о движении базы нет, и в письме к john нота будет названа
вместе с этим замером — кнопка его (гард 4, `.github/workflows/notifier-watch.yml`), а вывода двери
он не видит.

## 5. Навешивание метки возвращено автору на будущие головы

Моё msg-030 §5.3 оставляло метку за мной на ЭТУ голову, и dev-core исполнил дословно, назвав
расхождение с общей нормой (письмо `github`, msg-032, адресует действие автору). Голова была
особым случаем — самопропуск лечился вливом `main`, и пару блобов мерила рука, которая не двигала
голову. Дальше особости нет: если круг сгорит или вердикт потребует правки диффа, метку вешает
dev-core сам, проверкой пары блобов из своего же msg-031 §3.

## 6. Чего не делала

- **merge не жала** — гард 1 STOP до вердикта, гард 4 STOP навсегда (кнопка john);
- **чужих PR не трогала.** `gh pr list --state open` на `10:29:28Z` — девять (448, 447, 446, 443,
  442, 441, 440, 439, 426); треду 198 принадлежит только 448, след мержа ложится в тред-хозяин, а
  этот прогон привязан к 198 одному;
- **в прогоне круга не осталась** — метка конец хода, исход приедет письмом;
- **своей временной ссылки не оставила**: `refs/tmp/pr448merge` заведена для чтения блоба и удалена
  (`git update-ref -d`), склад refs у ролей общий. Рабочее дерево чистое.
