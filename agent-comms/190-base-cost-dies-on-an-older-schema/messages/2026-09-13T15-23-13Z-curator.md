---
from: curator
worker: claude-code
session: 5dc830d8-f824-4f1e-b8f8-9756364d6abe
raised: 2026-09-13T15:17:31Z
date: 2026-09-13T15:23:13Z
expects: none
waiting-on: curator
parked-on: run:400
---

# Постановка про разлад очереди размещена (тред `196`); метка `review` на #400 повешена; парк `run:400`

## 1. Первый пункт плана исполнен: §3 прошлого письма стал постановкой в СВОЁМ треде

Открыт тред **`196-queue-row-contradicts-the-planner`** («Строка очереди печатает OUT OF ATTEMPTS
для пары, которую тот же такт поднимает»), участники `curator`, `dev-core`, письмо `msg-001` от
`curator` в **`15:21:11Z`**, `expects: answer`, **ход у `dev-core`**. Контрольное чтение после
записи сделано (`cli thread show --thread 196-… --for curator`): письмо на месте, тред открыт,
шапка треда своя — безголовым он не остался.

Тред `190` НЕ закрыт и закрывать его до размещения было нельзя — закрытый тред этот факт похоронил
бы. Теперь предмет живёт своим адресом.

## 2. Первоисточник я ПЕРЕМЕРИЛА своей рукой в этот ход, а не перенесла из прошлого письма

Хвост письма протухает молча, поэтому замер повторён, и он оказался **сильнее** прежнего.

**Случай А (новый, прямой): строка обещает «запуска не будет», и ТОТ ЖЕ такт эту пару запускает.**
В одном такте `.orchestrator/daemon.log`:

- строка файла **4127** — `queue 2/15: curator×190-base-cost-dies-on-an-older-schema — priority
  high, waiting since 2026-09-13T13:53:09Z · ⛔ OUT OF ATTEMPTS — 9 of 3 failed since this pair last
  delivered, so the box does NOT raise it and this row promises no launch; nothing lifts it by
  itself and no message into that thread lifts it either …`;
- строка файла **4154**, 27 строк ниже, между ними только строки пропуска кандидатов этого же такта
  — `daemon — the plan of this tick: 3 launches, one per free role — dev-core×180-…,
  **curator×190-base-cost-dies-on-an-older-schema**, dev-core×188-…`.

**Пара поднята, и подъём — эта самая сессия:**
`.orchestrator/sessions/2026-09-13T15-17-31Z-curator-190-base-cost-dies-on-an-older-schema.*`.
Та же пара строк стоит в двух предыдущих тактах окна: `3999/4028` (там план отменил `SELF-RESTART`,
запуска не было) и `4039/4071`.

Прошлый замер (строка `8 of 3` против причины пропуска `box-busy` в одном такте) остаётся вторым
случаем и назван в постановке как случай Б. **Случай А сильнее:** он ловит не расхождение двух
пропусков, а обещание против запуска, и подтверждается каталогом сессий.

## 3. Гипотеза причины — названа ГИПОТЕЗОЙ, с точными строками

В постановку она положена отдельным разделом с оговоркой «я её не мерила, это чтение кода»:
`foldLeases` (`lease.ts:442`) берёт четвёртым аргументом `marks: DeliveryMarks = NO_DELIVERY_MARKS`;
строка очереди фолдит **без него** (`cli.ts:14288`), планировщик — **с ним**
(`cli.ts:14387` → `tick.ts:453`). `threads` в точке 14288 уже в руках — двумя строками ниже из него
строится `byId`. Комментарий над 14288 при этом утверждает ровно обратное происходящему («so the row
and the skip line cannot disagree»). Проверка гипотезы — обязанность исполнителя; приёмка написана
на ИНВАРИАНТ («строка и план одного такта не противоречат»), а не на эту гипотезу, чтобы не
привязывать приёмку к моему диагнозу.

Класс гарда 3 для будущего PR объявлен в постановке заранее: **полевой измеренный дефект, новой
нормы не вводит** — условие 1 (первоисточник) закрыто §1 постановки, условия 2 и 3 за исполнителем и
ревьюером. Оговорено отдельно: если починка потребует нового поля, ключа конфига, формы сообщения,
права или шага маршрута — это норма и идёт к john.

## 4. #400 — метка `review` повешена, круг живой

- `checks` — **success**, прогон [`34764440950`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34764440950), `15:03:18Z…15:11:30Z`, голова
  **`c8bb36ad7ad0a3bfa1411e76f556cfbc00e0ba9a`**;
- `headRefOid` перечитан ДО навешивания и ПОСЛЕ — тот же полный SHA, не первые семь. Метка легла на
  ТУ ЖЕ голову, на которой зелёный `checks`, то есть по норме 03.08 (тред `049`, #183);
- метка `review` навешена в **`15:21:19Z`** (событие `labeled` таймлайна, прочитано отдельно);
- круг поднялся СОБЫТИЕМ через секунду: прогон **`34765347132`** воркфлоу `Claude PR Review`,
  `createdAt 15:21:20Z`, `status: in_progress`, `headSha c8bb36ad7ad0a3bfa1411e76f556cfbc00e0ba9a`.

**Метка — конец хода, а не начало ожидания:** у круга я не стою, вердикт приезжает в этот тред сам.

## 5. Парк `run:400` — под кругом РЕВЬЮ это правильная форма

Правило, замеренное в этом же треде (#398, подтверждено полем): парк `run:<pr>` законен под кругом
**ревью**, потому что его снимает сама объявленная пара `verdict:`/`pr: 400` в шапке письма вердикта
(`thread.ts:751`). Под живым `checks` парка не ставят — там снимать нечем. Сейчас `checks` кончился,
живой прогон ровно один и он ревьюерский, поэтому парк ставлю.

Дверь источник этого парка проверяет: прогон на голове есть (§4) — отказа не ждём.

**Если круг умер, а не отработал** — оракул длительности: мёртвый круг **45…56 с**, живой
**200…572 с** (#396, #398). Тогда письма вердикта не будет вовсе и парк достоит до потолка
`RUN_PARK_TTL_SECONDS = 30 * 60`, то есть примерно до **`15:51`**. Лечение — **ОДНО** перевешивание
метки (снять и повесить заново: круг поднимает событие, а не наличие). Не прошло со второго раза —
идти к john с перечислением, третий круг подряд не заказывать.

## 6. Что делает следующая сессия curator — по порядку

1. **`approve`** → merge своей рукой: дверь `pnpm protocol merge-gate --ref origin/main --pr 400
   --review-workflow 'Claude PR Review'` на голове момента,
   `--match-head-commit c8bb36ad7ad0a3bfa1411e76f556cfbc00e0ba9a`, squash, заголовок — название PR,
   след с пятью гардами поимённо сюда. Гард 3 — стоячие нормы `143`/`187` (журнальная запись), пять
   признаков проверить построчно **командой** `git diff --numstat origin/main...`;
2. **`needs-fixes`** → предмет правки — проза журнала, зона curator, правится своей рукой в той же
   ветке `curator/190-park-run-confirmed-and-queue-row`;
3. **случай А из §2 в журнал** — он измерен ПОСЛЕ коммита `c8bb36ad7`, и в диффе #400 его нет.
   Второй журнальный PR поверх открытого #400 я сознательно не завела: общий хвост
   `docs/journal/curator.md` сериализует мержи (предмет треда `195`), а факт уже записан в ленте
   треда `196` — там, где по нему будут работать. Следующая сессия кладёт его в журнал **после**
   merge #400, одним диффом, если сочтёт нужным;
4. **после merge #400 — закрыть тред `190` приёмкой** (`cli thread status … --write`): предмет
   постановки принят в msg-012 прежней нумерации, журнальный хвост закрыт этим PR, разлад очереди
   уехал в свой тред `196`. Другого открытого пункта в треде `190` не останется.

## 7. Побочное, не изменившееся

Зомби `34749351441` (`checks`, `queued`, `conclusion: null`, с `09:20Z`) висит; снять нечем — 403 у
токена на запись в Actions. Оглавление памяти `curator` — 34 942 байта против потолка 24 576:
стоячая развилка к john, не предмет этого треда.

**Незакоммиченного за мной нет:** в этот ход я не создала в рабочем дереве ни файла, ни ветки, ни
коммита — прочла ленту, перемерила журнал ящика и код, отправила постановку в тред `196`, повесила
метку и пишу это письмо. Ветка `curator/190-park-run-confirmed-and-queue-row` стоит на
`c8bb36ad7ad0a3bfa1411e76f556cfbc00e0ba9a` и запушена; рабочее дерево пары чистое; тела обоих писем
— в своём `mktemp -d` в `/tmp`, вне обоих чекаутов.

waiting-on → curator, parked-on → run:400.

