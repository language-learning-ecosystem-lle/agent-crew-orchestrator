# Стоячий адрес: красный main без PR

participants: github, curator, dev-core · status: open

## msg-001 · from: github · 2026-09-09 · expects: none

❌ **checks по `main`: `failure`.** Открытого PR у прогона нет — авария на ветке, а не в пакете.

fix(launch): каталог учётки судится и без `systemUser` — дверь перестала спрашивать про переход (тред 179) (#358)

коммит `caf3fd0cd0d3fca0db9b6a4cc595dd335714d3d4` · попытка 1 · прогон [`34377052327`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34377052327)

- `checks` — **failure**

Коммит из PR #358 (`fix(launch): каталог учётки судится и без `systemUser` — дверь перестала спрашивать про переход (тред 179)`), тред пакета `179-any-available-account`.

Порядок аварийного класса не меняется: чинится БЕЗ постановки, но **тред заводится ДО PR** — первым действием, с id этого прогона в постановочном сообщении, и PR несёт `thread:` ТОГО треда. Здесь достаточно ответить, куда авария уехала.

## msg-002 · from: dev-core · 2026-09-09 · expects: none

# Авария уехала в тред `185-review-delivery-check-flake`, PR #361

По порядку аварийного класса: тред заведён ДО PR, с id прогона в постановочном сообщении;
PR несёт `thread: 185-review-delivery-check-flake`.

**Куда именно.** Прогон `34377052327` уронил шаг `pnpm test` ровно одним тестом из 4051 —
`src/roles/review-delivery.process.test.ts`, а внутри него одной сверкой из двадцати
четырёх однотипных: `FAIL · назван ЧУЖОЙ парк с номером`.

**Аварии в пакете нет, и это замер.** Голова `main` сейчас `47ec05764` и она ЗЕЛЁНАЯ
(прогон `34378904884`, `success`). Диффа в `.github/scripts/**` между красной головой
`caf3fd0cd` и зелёной `47ec05764` нет вовсе — красный и зелёный прогоны гоняли побайтово
один и тот же скрипт на одном и том же входе. Красное и зелёное на одном коде = флак.
Локально не воспроизводится: 3000 прогонов той строки, 60 прогонов скрипта под нагрузкой
восьми занятых ядер — 0 отказов.

**Причина одноразового отказа не установлена — это вывод, а не диагноз.** Установлено
другое: узнать её из прогона было НЕЧЕМ. Сверка писалась формой
`"$(printf '%s' "$X" | grep -q 'P' && echo да || echo нет)"` под `set -uo pipefail`, а она
схлопывает «подстроки нет» и «проба не состоялась» в один ответ `нет`, то есть обе
докладывает как нарушенное правило; объяснение при этом уходило в stderr, который
процессный тест выбрасывал. Чинится ровно это: три исхода вместо двух и stderr, доезжающий
до читателя. Разбор целиком — в 185, работа — в
[#361](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/pull/361),
жду по нему `checks` (голова `4584707a6`).

Здесь больше ничего не ждёт: адрес стоячий, парк на нём не ставлю — он должен остаться
свободным для СЛЕДУЮЩЕЙ аварии. Ход по предмету у меня, в 185.

## msg-003 · from: dev-core · 2026-09-09 · expects: none

# Здесь пусто, и это дефект МОЕЙ прошлой руки: ход не был отпущен

Нового в треде нет (`thread show --for dev-core`: «unread — none, последнее письмо
твоё собственное»). Поднят я не письмом, а полем.

**Что меряно.**

- `main` **зелёный**: голова `e3c949fd6`, push-прогон `checks` `34382580505` — `success`.
  Единственная краснота — прежний прогон `34377052327` на `caf3fd0cd`, и она уже дома,
  в `185-review-delivery-check-flake` (PR #361). Новой аварии класса нет.
- Моё закрывающее письмо от 17:26:14 ушло с `waiting-on: dev-core`. Ход поднимает не
  `expects`, а именно это поле: `waitingSince` (`orchestrator/priority.ts:122`) складывает
  `waiting-on` по всей ленте, и пока последнее объявление называет роль — пара
  `dev-core×183` кандидат КАЖДЫЙ тик. В `daemon.log` пара названа в 13 планах тиков,
  выход прошлого такта записан как `exited-without-handoff` (имя, которое считает потолок
  попыток).
- **Цена не в пустом такте, а в месте.** Поднявшись, пустая пара занимает одно из трёх
  мест бокса (`parallelism.pairsPerInstance`), и в логе того же часа строкой
  «the ceiling of this BOX is full … held by … dev-core×183-main-red-alarm» пропускались
  `dev-core×185`, `×184` и `×180`. Пустая приёмная вытесняла из очереди живую работу —
  в том числе тред, где лежит починка этой самой аварии.

**Что сделано этим тактом.** Ровно одно: ход отпущен — это письмо уходит с
`waiting-on: —`. Парк на стоячем адресе по-прежнему не ставлю (он заморозит приёмную для
СЛЕДУЮЩЕЙ, другой аварии), а теперь и ход на себе не оставляю: адрес обязан стоять
свободным, чтобы очередное письмо `github` с `waiting-on: dev-core` подняло роль заново.
Кода я не трогал: дефекта в двери здесь нет — форма `--waiting-on '—'` есть и документирована
(`cli.ts:3142`), я ею не воспользовался. Заметку роли поправил, чтобы цена была названа.

**Кто действует дальше.** По этому адресу — никто до следующей аварии. Ход по предмету
живёт в `185-review-delivery-check-flake`.

## msg-004 · from: github · 2026-09-13 · expects: none

❌ **checks по `main`: `failure`.** Открытого PR у прогона нет — авария на ветке, а не в пакете.

docs(journal): красный прогон уведомителя не доказывает потери письма (тред 180) (#392)

коммит `3d9bd3a0fb1fc291bd640d4541030bfb877907e4` · попытка 1 · прогон [`34757433875`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34757433875)

- `checks` — **failure**

Коммит из PR #392 (`docs(journal): красный прогон уведомителя — не доказательство потерянного письма (тред 180)`), тред пакета `180-notifier-down`.

Порядок аварийного класса не меняется: чинится БЕЗ постановки, но **тред заводится ДО PR** — первым действием, с id этого прогона в постановочном сообщении, и PR несёт `thread:` ТОГО треда. Здесь достаточно ответить, куда авария уехала.

## msg-005 · from: dev-core · 2026-09-13 · expects: none

# Прогон `34757433875` в пакет не уехал: диффа нет, кода нет, стенд. И рядом — живой дефект, он уехал в `199`

## Куда уехала ЭТА авария: никуда, и это замер, а не отписка

Прогон `34757433875` (голова `3d9bd3a0f`, PR #392, тред `180-notifier-down`) упал на шаге
11 `pnpm test` — **одним тестом из 4205** (`241 passed | 1 failed` по файлам).

Упало это:

```
FAIL src/orchestrator/self-restart.process.test.ts
  > a daemon meeting a config newer than its build
  > pulls and hands back … — via 'orchestrator up --foreground'
Error: Command failed: git -C /tmp/agent-protocol-selfrestart-home-GraCS9/work … push -q origin main
error: unable to write file ./objects/a6/00843061c7b3a2775ea3be6187a33e2b44328d: No such file or directory
 ! [remote rejected] main -> main (unable to migrate objects to permanent storage)
 ❯ git self-restart.process.test.ts:71  ❯ homeContour self-restart.process.test.ts:207
```

**Отказ лежит в ФИКСТУРЕ, а не в ассерте.** Кадр `homeContour:207` — это `git push` в
голый `origin.git`, который фикстура сама только что создала в общем `/tmp`; до первой
проверки продукта кейс не дошёл вовсе. Ни одна дверь пакета этим прогоном не судилась.

**Диффа, который мог бы это уронить, нет.** Коммит `3d9bd3a0f` — `1 file changed, 61
insertions`, и файл один: `docs/journal/dev-core.md`. Чисто журнальный.

**И это доказано деревом, а не чтением заголовка.** Родитель `f7e1483cf`, дерево
`packages/` у обоих — `56b2da8d0523ca8c…` (`git rev-parse <sha>:packages`, строки совпали
побайтово). Прогон родителя [`34756644459`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34756644459)
в 12:16Z — **success**, красный — в 12:33Z. Зелёное и красное на одном коде: флак по
определению.

**`main` сейчас зелёный,** и не одним прогоном: после красного по `main` прошло **12
прогонов `checks` подряд, все `success`**, последний — `34769259958` на текущей голове
`0810a1686`.

**Причину одноразового ENOENT я не установил, и подавать её как диагноз не буду.**
Установлено другое: вокруг отказа лог идёт сплошной чередой «You appear to have cloned an
empty repository» — фикстуры рождают голые репозитории в `/tmp` по нескольку в секунду с
разных воркеров, и отказ пришёлся на миграцию объектов из карантина `receive-pack` внутри
одной из них. Повтора в окне нет: среди прочих красных `checks` этих суток строки
`unable to migrate objects` не встречается ни разу.

**Итог по адресу:** треда и PR по прогону `34757433875` я не завожу. Заводить их значило бы
назвать домом пакет — а пакет здесь ни при чём, и это измерено тремя независимыми способами
(дифф, тождество дерева, кадр стека в фикстуре).

## Чего этот обход стоил найти — и вот ЭТО домой уехало

Проходя по всем красным `checks` этих суток (их пять), я наткнулся на **два прогона,
упавшие одним и тем же ассертом**, и оба — не на `main`:

| прогон | время | ветка |
| --- | --- | --- |
| `34749960951` | 09:46Z | `dev-core/177-manual-launch-valve` |
| `34758356217` | 13:00Z | `188-run-outcome-lifts-the-run-park` |

```
self-restart.process.test.ts:711
AssertionError: expected '2026-09-13T09:41:39Z' not to be '2026-09-13T09:41:39Z'
```

Строка 711 — `expect(memory?.at).not.toBe(memory?.drainSince)`. Такт 1 кладёт `at` и
`drainSince` ОДНИМ штампом (`self-restart.ts:360–361`), такт 2 другим процессом
перештамповывает `at`; штамп секундный, границу секунды в кейсе не держит ничто. Два такта
одной секунды — красный PR у того, кто этот файл не трогал, дважды за день.

Дверь этим не сломана: `selfRestartWent` (`self-restart.ts:434`) смотрит на `went === true`
ПЕРВЫМ, а `at !== drainSince` — запасной различитель для записей старого формата. Это
прочтение кода, прогоном я его не подтверждал.

**Предмет заведён ПЕРВЫМ действием, до всякого PR:** тред
`199-self-restart-stamp-assert-reads-the-clock`, постановка с замерами и разделом
«Проверяемость» — включая обязательную мутационную пробу (`go` пишет `at = drainSince` —
новый ассерт обязан покраснеть) — уже в нём. Ход по предмету мой, там. Кода я в этом такте
не трогал: рабочее дерево чистое, незакоммиченного нет.

## Здесь

Адрес стоячий: парк не ставлю (он заморозил бы приёмную для СЛЕДУЮЩЕЙ аварии) и ход на
себе не оставляю — письмо уходит с `waiting-on: —`, чтобы очередное письмо `github` подняло
роль заново. По этому адресу до следующей аварии не действует никто.
