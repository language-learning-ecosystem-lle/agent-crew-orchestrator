# README: §«Почтовые воркфлоу заморожены» — привести к факту (две двери из пяти живы)

participants: curator, john · status: open

## msg-001 · from: curator · 2026-08-18 · expects: none

постановка: **§«Почтовые воркфлоу заморожены» в `README.md` объявляет режим, которого у двух воркфлоу из пяти нет** — привести текст к факту. Слово john дано в чате ~18:16Z и доставлено в тред 007 сообщением `agent-comms/007-comms-derived-guards/messages/2026-08-18T18-17-16Z-curator.md`, п. 2: обе разморозки НАМЕРЕННЫ (кнопки john на #5/#6, 2026-08-17), **«README приводи к факту»** — правка доковая, круг обычный, кнопка merge — john.

Тред заведён отдельным, потому что 007 — про двери вокруг генератора производных, а это про текст README; 007 закрыт приёмкой влитого #12.

## 1. Факт — что измерено

Прочитано мной в файлах на `origin/main` 18:22Z, после fetch (триггеры, а не память; та же таблица снималась в 007 на час раньше и с тем же исходом):

| воркфлоу | `on:` на `origin/main` | README говорит | правда |
| --- | --- | --- | --- |
| `comms-derived.yml` | `push: branches: [comms]` (с `paths-ignore` на `_instances/**`) + `workflow_dispatch` | заморожен | **жив** — весь тред 007 стоит на его прогонах |
| `merge-notify.yml` | `pull_request: types: [closed]` + `workflow_dispatch` | заморожен | **жив** |
| `ci-outcome.yml` | только `workflow_dispatch` | заморожен | заморожен |
| `notifier-watch.yml` | только `workflow_dispatch` | заморожен | заморожен |
| `claude-review.yml` | только `workflow_dispatch` | заморожен | заморожен |

Класс дефекта тот же, что чинил п.1 треда 007 («дверь объявлена, дверь не поставлена»), только этажом выше: не в воркфлоу, а в тексте, по которому роль выбирает форму хода.

**Вторая устаревшая строка в том же абзаце**, найденная при этой же сверке: «Собственный CI репозитория — один `checks.yml` (lint + typecheck + test)». На `origin/main` (18:21Z) `checks.yml` несёт ещё два шага — `git identity` и `comms-derived синхронен с каноном` (строка 63, тред 007), плюс `fetch-depth: 0` в чекауте ради последнего. Правится тем же коммитом: абзац один, и оставить в нём половину неверной — значит завести сюда же второй заход.

## 2. Почему это не косметика

Обе живые двери меняют то, как роль ЗАКАНЧИВАЕТ ход, и меняют в разные стороны:

- **`ci-outcome.yml` заморожен** → исход CI в тред событием здесь НЕ приходит (вывод из триггера, отдельным замером не снимался). А стандартный текст, который роль читает при входе, обещает обратное: «вердикт ревьюера и исход CI приходят в тред событием и будят роль штатно». Роль, передавшая ход со словами «жду прогон», ждёт здесь события, которого не будет: **зелёный снимается своей рукой ДО передачи хода**;
- **`merge-notify.yml` жив** → парковка `--parked-on pr:<n>` здесь законна и снимается штатно. Это единственная причина её законности, и она держится на живом триггере, а не на обычае.

Первое я проверяла на себе в 007: выбирая форму парковки, по README я обязана была бы считать `merge-notify` мёртвым, а `pr:`-парковку — тихой заморозкой навсегда.

## 3. Что сделать

1. **`README.md`, §«Почтовые воркфлоу заморожены»** — привести к факту: назвать поимённо, что заморожено (`ci-outcome`, `notifier-watch`, `claude-review`) и что живо (`comms-derived`, `merge-notify`), с датой и причиной разморозки (кнопки john на #5/#6, 2026-08-17). Заголовок абзаца, обещающий заморозку всем пятерым, тоже правится — иначе оглавление врёт вместо текста;
2. **та же правка** — вторая строка абзаца про `checks.yml`: назвать шаги, которые там реально есть, СВЕРИВ их чтением файла на голове ветки, а не по этой постановке;
3. **операционное следствие из §2 — записать туда, где роль читает его перед ходом, а не в README**: `docs/protocol-reference.md` (словарь фактов этого репозитория). Формулировка — «исход CI событием не приезжает, зелёный снимает сама роль до передачи хода» и «`pr:`-парковка законна, пока жив `merge-notify`». Перед правкой убедиться, что раздел про этот репозиторий там есть; если места нет — сказать это в докладе, а не выдумывать раздел.

**Границы:** `.github/workflows/**` не трогать вовсе — предмет john, и трогать нечего: правится текст, а не режим. Разморозка `ci-outcome` или ревьюера в эту постановку НЕ входит и решается отдельно (это деньги и права).

## 4. Проверяемость

- **Юнит/интеграционного теста нет** — предмет правки текст; проверка на соответствие текста триггерам стендом не берётся, а берётся чтением. Названо, не пропущено;
- **прямой факт**: чтение `on:` каждого из пяти файлов на голове ветки PR и сравнение с новым текстом — построчно, в докладе таблицей;
- **зелёный `checks` на голове PR** — снимается СВОЕЙ рукой (`gh run list --commit <head>`), потому что `ci-outcome` заморожен: это ровно тот случай, который постановка и описывает;
- **побочная приёмка, названная в 007 и бесплатная здесь**: этот PR канон `.github/workflows/comms-derived.yml` НЕ трогает, значит шаг сверки обязан взять РАННИЙ ВЫХОД по пустому трёхточечному диффу и не спрашивать про копию вовсе. Это единственная ветка шага, не снятая живьём в 007 (бралась с доверием к прототипу LLE, прогон 30788651443). **В доклад идёт номер прогона и дословная строка раннего выхода из лога шага** — если шаг вместо выхода полез сверять, это дефект двери из 007, и он важнее самой доковой правки;
- **приёмка пакета — merge кнопкой john** (`claude-review.yml` заморожен, approve ревьюера взяться неоткуда, гарду 1 закрываться нечем). Тред закрывается по факту влитого PR, чтением базы.

## 5. Круг

ветка → правка → зелёный `checks` на голове (снять рукой) → PR с `thread: 008-readme-frozen-workflows` и `role: curator` в описании → сообщение в тред с номером PR, головой и таблицей проверок → кнопка john. Push пакет не закрывает.

Исполнитель — curator (зона своя: `zones check --ref origin/main --role curator --paths README.md` → «1 path(s) of 'curator' are inside its zone», проверено командой 18:21Z; `docs/protocol-reference.md` — тем же ответом).

waiting-on → curator.

## msg-002 · from: curator · 2026-08-18 · expects: ack

**Требуется решение john: (1) кнопка merge на PR #13 — README приведён к факту; (2) где жить операционному следствию (п.3 постановки), потому что все три кандидата — доки власти.**

PR **#13**, голова `7368210a8cd73ea589e28059505cc3dd9425d85a`, `checks` зелёный на ней (прогон `32171052646`, 18:30:04Z). Дифф — только `README.md` (+14/−2), `.github/workflows/**` не тронут ни байтом.

## 1. Сделано (п.1 и п.2 постановки)

§ «Почтовые воркфлоу заморожены» → «Почтовые воркфлоу: два живых, три замороженных», внутри — таблица по прочитанным триггерам. Сверка сделана заново чтением `origin/main` после fetch (18:26Z), не по постановке:

| воркфлоу | `on:` на голове PR | README говорил | README говорит теперь |
| --- | --- | --- | --- |
| `comms-derived.yml` | `push: branches: [comms]` + `paths-ignore: _instances/**` + `workflow_dispatch` | заморожен | жив |
| `merge-notify.yml` | `pull_request: types: [closed]` + `workflow_dispatch` | заморожен | жив |
| `ci-outcome.yml` | `workflow_dispatch` | заморожен | заморожен |
| `notifier-watch.yml` | `workflow_dispatch` | заморожен | заморожен |
| `claude-review.yml` | `workflow_dispatch` | заморожен | заморожен |

П.2 (вторая строка абзаца) — `checks.yml` прочитан на той же голове: шаги `pnpm lint`, `pnpm typecheck`, `pnpm test`, плюс `git identity` и «comms-derived синхронен с каноном», плюс `fetch-depth: 0` в чекауте ради последнего. Формулировка «один `checks.yml` (lint + typecheck + test)» заменена на этот список; событие названо тоже (`pull_request` + push в `main`).

**Поправка к собственной постановке, найденная сверкой.** Msg-001 говорит «кнопки john на #5/#6, 2026-08-17». По `git log origin/main` разморозка — ОДИН merge: **#6, `1a10f83`, 2026-08-18T10:37:50Z, «ci: разморозить comms-derived и merge-notify — репозиторий обслуживает сам себя (тред 082)»**; #5 (`b2ee091`) — про `.gitignore`, а 2026-08-17 — дата ЗАМОРОЗКИ (тред `080-extraction-prep`, шапка `claude-review.yml`). В README поехали проверенные значения, не значения постановки.

## 2. Не сделано: п.3 (следствие в `docs/protocol-reference.md`) — места нет, и это измерено

Постановка велела убедиться, что раздел про этот репозиторий там есть, и не выдумывать его. **Раздела нет, и файл отказывает такому тексту собственной шапкой** (`docs/protocol-reference.md`, строки 24–29): «Номера тредов и PR в этом тексте — история проекта-родителя… То, что верно только для установки проекта-родителя, живёт отдельно — в надстройке установки — и в этот файл не возвращается».

Оговорка честная: замеры ЭТОГО репозитория в файле есть (строки 162, 166, 170, 261 — генератор производных, курьер). Но они про поведение generic-КОДА, снятое здесь, а «`ci-outcome` здесь выключен» — про то, какие тумблеры включены на ЭТОЙ установке; это ровно тот класс, который шапка выселяет. Дописать туда — значит завести в generic-файле первый абзац, неверный для любого другого репозитория контура.

Что сделано вместо: **оба следствия названы в самом § README** («исход CI событием НЕ приезжает — зелёный роль снимает своей рукой `gh run list --commit <head>` до передачи хода»; «`merge-notify` жив — парковка `parked-on: pr:<n>` законна»). Это НЕ закрывает п.3: постановка просила именно не README, а место, которое роль читает перед ходом.

**Вопрос john.** Долговременный дом у этого следствия один из трёх, и все три — доки власти, то есть моей рукой не правятся:
- `PROTOCOL.md`, R-134 («вердикт ревьюера и исход CI приходят в тред СОБЫТИЕМ») — здесь это прямо неверно, и норму читает каждая роль;
- карточки ролей (`docs/roles/*.md`) — по одной строке в каждую;
- новый файл-надстройка установки (напр. `docs/install-notes.md`), на который сошлётся `PROTOCOL.md`.

Моя рекомендация — третий: он не трогает норму, дом у него растущий (следующая разморозка правит одну строку), а `PROTOCOL.md` получает одну ссылку вместо оговорки в норме. Решение — твоё; пакет заведу отдельным тредом после слова.

## 3. Побочная приёмка из треда 007 — снята живьём, дверь ведёт себя как задумано

PR канон `.github/workflows/comms-derived.yml` не трогает, и шаг сверки взял РАННИЙ ВЫХОД, не спрашивая про копию. Прогон `32171052646`, шаг «comms-derived синхронен с каноном», 18:30:04.99Z, дословно:

```
канон не тронут правкой этого PR (дифф от merge-base с origin/main пуст) — расхождение копии, если оно есть, принадлежит чужой правке; не спрашиваю.
```

Это единственная ветка шага, не снятая живьём в 007 (бралась с доверием к прототипу LLE). Дефекта нет: `git fetch origin comms` в логе после этой строки не выполнялся вовсе.

## 4. Что дальше

Ход у меня, потому что вопрос мой. Тред закрою приёмкой влитого #13 — чтением базы, а не цветом джобы. Парковка на john: нужен и клик, и слово по п.3.

waiting-on → curator.

## msg-003 · from: curator · 2026-08-18 · expects: none

доставка решений john (сессия чата, ~18:51Z) — парковка msg `18-31-42Z` снята:

1. **#13 СМЁРЖЕН — сверено вызовом**: `merged_at=2026-08-18T18:50:36Z`, squash в `main`. README у факта; тред 008 готов к закрытию по твоему порядку (чтением базы);
2. **Дом операционных следствий установки: `docs/install-notes.md`** — слово john, твоя рекомендация принята (не трогает generic-норму, растущий дом, `PROTOCOL.md` получает ссылку; паттерн — зеркало LLE-оверлеев `docs/*-lle.md`). Пакет: файл + два следствия (ci-outcome выключен → зелёный снимается рукой; merge-notify жив → `parked-on: pr:` законна) + строка-ссылка в `PROTOCOL.md`. Дифф заденет док власти (`PROTOCOL.md`) — merge кнопкой john; постановка за тобой отдельным тредом, как ты и наметила.

waiting-on → curator.
