---
from: curator
worker: claude-code
session: 9683688d-2af0-4380-b976-532826dcfbc9
date: 2026-08-18T18:57:13Z
expects: none
waiting-on: —
---

приёмка треда: **#13 влит в `main` — сверено чтением базы, а не докладом**; п.3 постановки вынесен в тред `009-install-notes`. Тред закрываю.

## 1. Приёмка — чем проверено

`gh pr view 13 --json state,baseRefName,mergedAt,mergeCommit,headRefOid` (2026-08-18 18:54Z):

- `state: MERGED`, **`baseRefName: main`** (цель merge — проверена отдельно, а не выведена из «смёржен»),
- `mergedAt: 2026-08-18T18:50:36Z`, голова `7368210a8cd73ea589e28059505cc3dd9425d85a`,
- `mergeCommit: c983abb751807db1975ca7adee87335f3c6d21db`.

Второй факт, независимый от ответа `gh`: после `git fetch origin main` голова `origin/main` = `c983abb docs(readme): §почтовых воркфлоу — по факту: два живых, три замороженных (тред 008) (#13)` — коммит лежит в базе, squash одним коммитом.

Итог по постановке (`.../messages/2026-08-18T18-22-53Z-curator.md`): п.1 и п.2 сделаны и приняты (текст § приведён к прочитанным триггерам, строка про `checks.yml` — к прочитанным шагам), п.3 закрыт решением john о доме следствий и передан на исполнение отдельным тредом. Побочная приёмка двери из 007 (ранний выход шага сверки на нетронутом каноне) снята живьём прогоном `32171052646` и доложена в `.../2026-08-18T18-31-42Z-curator.md` §3.

## 2. Что уехало дальше

Заведён тред **`009-install-notes`**, постановка — `agent-comms/009-install-notes/messages/2026-08-18T18-56-36Z-curator.md`: `docs/install-notes.md` (дом операционных следствий установки) + одна строка-ссылка в `PROTOCOL.md` возле R-134 + ссылочная строка в § README, чтобы не завести второй дом той же мысли. Ход там у curator.

**Находка, увезённая туда же (п.3 постановки 009), а не потерянная здесь:** при сверке зоны исполнителя `zones check --role curator` ответил «inside its zone» не только на `docs/install-notes.md` и `PROTOCOL.md`, но и на `biome.json` и `.github/workflows/checks.yml`, а отказал только на `packages/...` — то есть при непустом `writes` дверь судит, похоже, лишь по `forbidden`. Причину не называю: кода не читала, гипотеза — заявление, а не диагноз. Проверка поведения — предмет `dev-core` отдельным тредом, вопрос про строку `zones.writes` для нового файла — к john в 009.

## 3. Закрытие

Тред закрывается сразу за этим сообщением командой `thread status --status closed` (право `thread-status`): предмет — текст § README — влит и проверен в базе, незакрытых хвостов внутри треда нет, продолжение живёт в 009.

waiting-on → —

