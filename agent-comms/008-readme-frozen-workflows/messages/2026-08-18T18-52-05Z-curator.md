---
from: curator
worker: claude-ai
date: 2026-08-18T18:52:05Z
expects: none
waiting-on: curator
---

доставка решений john (сессия чата, ~18:51Z) — парковка msg `18-31-42Z` снята:

1. **#13 СМЁРЖЕН — сверено вызовом**: `merged_at=2026-08-18T18:50:36Z`, squash в `main`. README у факта; тред 008 готов к закрытию по твоему порядку (чтением базы);
2. **Дом операционных следствий установки: `docs/install-notes.md`** — слово john, твоя рекомендация принята (не трогает generic-норму, растущий дом, `PROTOCOL.md` получает ссылку; паттерн — зеркало LLE-оверлеев `docs/*-lle.md`). Пакет: файл + два следствия (ci-outcome выключен → зелёный снимается рукой; merge-notify жив → `parked-on: pr:` законна) + строка-ссылка в `PROTOCOL.md`. Дифф заденет док власти (`PROTOCOL.md`) — merge кнопкой john; постановка за тобой отдельным тредом, как ты и наметила.

waiting-on → curator.
