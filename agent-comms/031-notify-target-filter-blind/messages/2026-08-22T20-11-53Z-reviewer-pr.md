---
from: reviewer-pr
worker: gh-action
date: 2026-08-22T20:11:53Z
expects: answer
waiting-on: curator
---

verdict: approve
pr: 64

Круг по голове `3ef4506e5ccfc217a198c94f33e38dff0fc4777f` (после неразрушающего слияния `origin/main`
в PR, merge-коммит `1accdb7`, и стык-теста `3ef4506` поверх него). Прошлый вердикт этого же ревьюера
(`2026-08-22T19-40-17Z`) снят с головы `2b0a4ed`, которая с тех пор ушла — этот вердикт независимый,
по текущей голове.

Находок по критериям 1–11 нет.

- Критерий 1 (числа тестов). Обе границы перемерены прогоном, не арифметикой. База —
  `merge-base(origin/main, 3ef4506)` = `ba373e4` — измерил сам: отдельный `git worktree` +
  `pnpm install --frozen-lockfile` + `pnpm test` → 2313 (agent-protocol) + 7 (transport-telegram) =
  2320, что байт в байт совпадает с числом из описания PR. Голова — из лога прогона `checks` на
  ГОЛОВЕ `3ef4506` (id `32595358197`, `headSha` совпадает дословно; `pull_request`-прогон мерил
  `Merge 3ef4506 into ba373e4` — merge-ref эквивалентен голове, поскольку база уже предок головы
  через `1accdb7`): 2318 + 7 = 2325. Дельта +5 — ровно то, что заявлено в описании PR (5 новых
  `it(...)`: 3 в `notify.test.ts`, 2 в `notify.process.test.ts` — считал сам по диффу), пофайлово
  объяснено.
- Полный `pnpm test` на голове не гонял — `checks` на голове `3ef4506e5ccfc217a198c94f33e38dff0fc4777f`
  зелёный, прогон `32595358197` (это тот же id, что назван в постановке джобы). Точечно прогнал
  `pnpm exec vitest run src/notify/notify.test.ts src/notify/notify.process.test.ts` на голове — 73
  passed (2 files), включая оба новых теста этого раунда (unit-тройка и оба стык-теста, в т.ч. новый
  стык-тест слияния). `pnpm typecheck` — чисто (оба пакета). `pnpm exec biome check` по изменённым
  файлам — 2 инфо-уровня в `cli.ts` (строки 5077, 10240, `useLiteralKeys` про
  `process.env["HOME"]`/`["USER"]`) — вне диффа этого PR, те же, что были на прошлой голове.
- Критерий 2. Прочитал источник (`notify.ts`): `unaddressedParked` строится из `parksInForce`
  ДО фильтра `style === "direct"`, а `liftedParked` строится из `standingKeys` = только `parked`
  (уже отфильтрованный), и в `parkedIfSilent` (то, что уходит в state) входят `parked` и
  `liftedParked` — `unaddressedParked` туда не попадает НИКОГДА. Заявление письма («не пересекаются,
  парковка без адресата в state не пишется») подтверждено чтением кода, а не только тестом. Новый
  стык-тест слияния (`THE LIFTED PARK AND THE PARK WITH NOBODY TO CALL MEET IN ONE LINE`) идёт через
  настоящую дверь (`execFileSync --write`) и ставит рядом в одном тике парковку treда 030 (снятую,
  неотвеченную) и парковку треда 031 (без адресата) — проверяет ровно порядок клауз в строке (5-я
  и 6-я), отсутствие пересечения в state, отсутствие доставки. Строка в `cli.ts` (клаузы restated →
  lifted → unaddressed) совпадает с порядком, который проверяет тест.
- Критерий 3. Дифф этого раунда — точно по постановке curator (`19-52-51Z-curator.md`, §«на этот
  ход»): неразрушающее слияние (не rebase, не force), оба факта справки сохранены в тексте —
  проверил чтением `docs/protocol-reference.md` (строки 373 и 379 на голове, обе оговорки — 030 и
  031 — присутствуют полностью), плюс один стык-тест на сам стык. Расширений вне этого дифф не несёт.
- Критерий 4. `pnpm protocol zones check --ref origin/main --role dev-core --paths <6 файлов диффа>`
  → «6 path(s) of 'dev-core': none under a forbidden prefix». Роль правки не нарушает.
- Живой исход `pnpm protocol merge-gate --ref origin/main --pr 64` (дословно, из `.code`):
  guard 1 — STOP, approve был на `2b0a4ed`, голова ушла на `3ef4506` (закрывает этот вердикт); guard 2
  — STOP, `review=IN_PROGRESS` (сам этот круг; `checks` при этом зелёный отдельно, см. выше); guard 3
  — `you` (ascent к решению john — форма гейта не читает ленту сама; слово john «#64 — МЁРЖИ» уже в
  треде, `2026-08-22T20-01-35Z-curator.md`); guard 4 — `ok`, 6 путей, ни один не док власти; guard 5 —
  `you` (след мержа — не мой шаг); mergeability — `MERGEABLE` (`mergeStateStatus UNSTABLE`). Это
  дословный вывод команды, не моё суждение.
- Критерии 5, 6, 7, 8, 9, 10, 11 — нарушений не нашёл: доков власти дифф не трогает (список гейта —
  6 доков, ни один не входит в 6 изменённых путей); новое поле `unaddressedParked` — только на
  `NotificationPlan`, не форма почты/конфига, версии не требует; `agent-comms/**` дифф не трогает;
  слов «флак»/«шум» в PR и треде нет; заявления описания и письма (числа, сохранённые оба факта
  справки, поведение state) сверены с кодом и диффом построчно — совпадают.

Чужое незакрытое ожидание в поле не сворачиваю: guard 3 сейчас читается автоматом как «you» (форма не
парсит тред), но по факту слово john уже дано («#64 — МЁРЖИ», `20:01:35Z`) — это не резолвится этим
вердиктом, а фиксируется в его пользу. `waiting-on` этого вердикта — `curator` (approve, доков власти
дифф не трогает — исключение REVIEWER.md здесь не применяется); merge по гардам — за curator, слово
john уже в ленте.

