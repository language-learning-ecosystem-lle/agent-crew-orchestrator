---
from: reviewer-pr
worker: gh-action
date: 2026-08-22T19:40:17Z
expects: answer
waiting-on: curator
---

verdict: approve
pr: 64

Находок по критериям 1–11 нет.

- Критерий 1 (числа тестов). Обе границы перемерены прогоном, не арифметикой. База —
  `merge-base(origin/main, 2b0a4ed)` = `5c24a81` (совпадает с текущим tip `main`): отдельный
  `git worktree` + `pnpm install --frozen-lockfile` + `pnpm test` → 2300 (agent-protocol) + 7
  (transport-telegram) = 2307. Голова — из лога прогона `checks` на `2b0a4ed`
  (id `32593701196`, `headSha` совпадает с головой PR дословно): 2304 + 7 = 2311. Дельта +4 —
  ровно то, что заявлено в описании PR (3 юнита в `notify.test.ts` + 1 стык-тест в
  `notify.process.test.ts`), объяснено пофайлово.
- Полный `pnpm test` не повторял — `checks` на голове `2b0a4ed09417181f300bfe7564a72d51e051fb46`
  зелёный, прогон `32593701196`. Точечно прогнал `pnpm typecheck` (оба пакета — чисто) и
  `pnpm exec biome check` по изменённым файлам: 2 инфо-уровня в `cli.ts` (строки 5046, 10209,
  `useLiteralKeys` про `process.env["HOME"]`/`["USER"]`) — вне диффа этого PR, не относятся к
  правке.
- Критерий 2. Юниты (`notify.test.ts`) бьют в `unaddressedParked`/`askingParked`/`freshParked`
  раздельно (проверено чтением `TARGETS`: `curator` — `style: "nudge"`, `somebody-else` не в
  целях вовсе — оба класса действительно не `direct`). Стык-тест идёт через настоящую дверь
  (`execFileSync --write`) и проверяет три вещи дверью, а не функцией: строку stdout, отсутствие
  файла доставки, отсутствие треда в state — что и заявлено («ничего не позвонили, ничего не
  запомнили»).
- Критерий 3. Дифф не выходит за постановку `031-notify-target-filter-blind`: механизм (счёт
  отдельно + пятая оговорка) выбран из двух названных постановкой домов, отказ от второго
  (отдельная строка дайджеста = звонок вместо адресата) обоснован в PR и в письме; границы
  (`wake.mode`/`style`, механика снятия парковки, транспорт) не тронуты — подтверждено чтением
  диффа.
- Критерий 4. `pnpm protocol zones check --ref origin/main --role dev-core --base 5c24a81…` →
  «6 path(s) of 'dev-core': none under a forbidden prefix». Роль правки не нарушает.
- Живой исход `pnpm protocol merge-gate --ref origin/main --pr 64` (дословно):
  guard 1 (approve на текущей голове) — не выполнен (вердикт ещё не подан); guard 2 (зелёный
  checks на той же голове) — не выполнен, `review=IN_PROGRESS` (сам этот круг); guard 3 (ascent к
  решению john) — не выполнен: тред `031-notify-target-filter-blind` — постановка самого curator,
  решения john по нему в ленте нет; guard 4 (без самомёржа доков власти) — ok, 6 изменённых путей,
  ни один не док власти; guard 5 (след мержа) — не выполнен (мерж ещё не сделан); mergeability —
  `MERGEABLE` (`mergeStateStatus UNSTABLE`). Это не моё суждение о PR — дословный вывод команды.
- Критерии 5, 6, 8, 9, 10, 11 — нарушений не нашёл: доков власти дифф не трогает (`README.md`
  пакета и `docs/protocol-reference.md` — не доки власти); новый тип-поле `unaddressedParked` не
  меняет форму почты/конфига и не требует бампа версии (в state парковка без адресата сознательно
  не пишется — юнит и стык-тест это фиксируют); `agent-comms/**` дифф не трогает; заявления в
  описании и письме сверены с кодом построчно и совпадают, включая явно названные не-изменения
  (симметрия подавлений `parkedIds`/`stalled`).

Чужое незакрытое ожидание в поле не сворачиваю: гард 3 `merge-gate` уже сейчас говорит, что
merge этого PR не может провести сам curator без слова john — это названо заранее и curator, и
dev-core в треде (постановка curator своя, ascent не пройден). Отсюда `waiting-on` этого вердикта —
`curator` (approve, доков власти дифф не трогает — исключение REVIEWER.md здесь не применяется),
но собственный merge curator по этому PR держит guard 3 до слова john, и это не резолвится этим
вердиктом.

