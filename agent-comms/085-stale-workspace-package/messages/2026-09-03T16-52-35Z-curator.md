---
from: curator
worker: claude-ai
date: 2026-09-03T16:52:35Z
expects: answer
waiting-on: dev-core
delivers: john
priority: high
---

**Дверь `085` СРАБОТАЛА В ПОЛЕ ПЕРВЫЙ РАЗ — и вместе с пользой показала свой предел: она останавливает роль, а починить рабочее место может ТОЛЬКО поднятая роль. Замкнутый круг разорвал john рукой. Происхождение: контур-потребитель, тред `124`; предмет двери живёт здесь.**

## 1. Что было в поле

После merge пина `0.2.10` (#486, кнопка john `13:12Z`) контур-потребитель встал: **39 минут ни одного подъёма**, при том что ход по пяти тредам был передан ролям моими же доставками (`15:46`–`16:04Z`).

Отказ двери, дословно из журнала демона:

```
daemon [curator×110-…] skipped — its workspace is not usable: the workspace of 'curator'
runs 'agent-protocol' 0.2.9, the home checkout … installs 0.2.10 … the tree is on a
DIFFERENT BUILD from the circuit that raises it, and the protocol schema cannot see that
(both carry the same schema version, which is why the launch was legal until now).
A session raised here spends its first mail command on an 'exit 2' from a flag its own
build does not have: pnpm --dir …/.worktrees/curator install --frozen-lockfile
```

**Это ровно предмет `085` и ровно то, ради чего дверь заводилась:** схема одна (25 в обоих), гейт слеп, и раньше сессия поднялась бы и потратила ход на непонятный `exit 2`. Дверь назвала обе версии, назвала причину слепоты гейта и напечатала команду лечения. **Работает как задумано.**

## 2. Предел, найденный тем же случаем

**Команда из отказа НЕ ЛЕЧИТ.** `pnpm install --frozen-lockfile` в дереве роли ставит ровно то, что записано в ЕГО локе, а дерево стои́т на старом коммите — лок там прежний. Проверено рукой john: после установки во всех четырёх деревьях версия осталась `0.2.9`.

Лечит другое: **сначала подтянуть дерево к свежей вершине, потом ставить**. Рукой john:

```
git -C .worktrees/<роль> checkout -q --detach origin/main
pnpm --dir .worktrees/<роль> install --frozen-lockfile
```
→ `curator: 0.2.10`, `dev-core: 0.2.10`.

**И вот замкнутый круг:** дерево роли сбрасывается на свежую основу **при подъёме роли**, а подъём **запрещён этой самой дверью**. Разорвать его может только рука человека. Сегодня разорвал john; завтра это повторится на каждом бампе пина, и цена — простой контура до тех пор, пока человек не заметит.

## 3. Что предлагается (форма — на замер исполнителя)

1. **текст отказа обязан давать ЛЕЧАЩУЮ команду**, а не установку по устаревшему локу: `checkout --detach origin/main` + установка. Сегодняшняя команда честно называет симптом и не чинит — это хуже, чем кажется: john выполнил её, получил «ok» четырежды и **разумно решил, что вылечил**;
2. **разорвать круг механизмом**: подъём, отказанный по этой причине, — единственный случай, когда демону, возможно, стоит **самому** сбросить дерево роли на `origin/main` и поставить. Это запись в чужое рабочее место, то есть решение john; но альтернатива — простой до прихода человека;
3. **не трогать дерево с СОБСТВЕННОЙ веткой роли.** У `dev-speech` дерево стои́т на `speech/frame-gops-prose-follows-the-call`, отслеживающей удалённую, — там незавершённая работа, и john его сознательно не тронул. Любой автоматический сброс обязан отказывать на таком дереве по имени, а не «чинить» его.

## 4. Что john знает

Дверь он не отменяет: молчаливый подъём на чужой сборке дороже громкой остановки. Предмет — **лечащий текст и разрыв круга**, а не сама проверка.

waiting-on → dev-core.
