---
from: reviewer-pr
worker: gh-action
date: 2026-09-02T22:47:58Z
expects: answer
waiting-on: curator
verdict: approve
pr: 220
---

verdict: approve
pr: 220

Ревью по REVIEWER.md, тред `085-stale-workspace-package` (curator → dev-core, решение john «5 — заводи»).

## Проверки

- `pnpm typecheck` — зелёный (`agent-protocol`, `transport-telegram`).
- `pnpm exec biome check` по изменённым файлам — 2 инфо (`useLiteralKeys` на `process.env["HOME"]`/`process.env["USER"]`, cli.ts:6501 и cli.ts:12467), обе строки вне диффа этого PR — не комментирую (общий код-стайл, критерии его не касаются).
- Полный прогон тестов не повторял — `checks` на голове `b9d461edb72b71e065c597b878ab44b68ccd665d` зелёный, прогон `33691254246` (`packages/agent-protocol test: 192 passed`).
- Точечно прогнал новые/изменённые файлы: `vitest run src/orchestrator/workspace-package.test.ts src/orchestrator/workspace.process.test.ts` — 32 passed (4 + 28), совпадает с числом, заявленным в письме dev-core треда.
- `pnpm protocol zones check --ref HEAD --role dev-core --paths <6 путей диффа>` — все 6 вне `forbidden`, зелёный (критерий 4).
- `pnpm protocol merge-gate --ref origin/main --pr 220` — живой исход: guard 4 (документы власти) — `ok`, 6 изменённых путей, ни один не документ власти; guard 1/2 не выполнены (approve/green ещё не проставлены на эту голову — ожидаемо до этого вердикта); guard 3/5 — процедурные требования к curator (назвать решение john, оставить след мержа), не к этому PR.

## По критериям

1. **Числа тестов.** Заявленные в треде числа файлового уровня (32 для двух новых/изменённых файлов, суммарно 32+72 по пяти файлам) подтверждены прогоном; область названа вместе с числом (конкретные файлы). Общий счёт `checks` — 192 файла, без расхождений.
2. **«Ждём то, что проверяем».** `workspace-package.test.ts` бьёт в чистую функцию по каждому из четырёх исходов (отказ по имени, отказ по отсутствию, совпадение, оба вида молчания). `workspace.process.test.ts` дополнительно проверяет саму дверь через реальный процесс: код выхода 2, текст отказа (обе версии, пин, слово "schema version"), и главное — что попытка НЕ потрачена (`journalPath`/`cwd.txt` не созданы) — то есть тест не про возврат нижележащей функции, а про интеграцию в `settleRun` до лока и аренды.
3. **Скоуп.** Дифф точно соответствует постановке треда 085: сравнение манифестов вместо разбора пина — задокументированное и обоснованное отступление от буквы постановки (п.1), доложено явно в PR и в письме, входит в рамки «доложенного расхождения». Оба случая намеренного молчания (домашний чекаут без своей копии; ещё не созданный воркспейс) — из требований постановки, не самодеятельность. Расширений вне задания не вижу.
4. **Зоны.** Все 6 путей диффа — зелёные для `dev-core` (см. прогон выше).
5. **Доки власти.** `docs/protocol-reference.md` и `packages/agent-protocol/README.md` не документы власти (первый — явное исключение по REVIEWER.md, второй не входит в список); `cli.ts` и новые файлы — обычный код. Секретов, ослабления гардов, расширения прав инструментов, необратимых операций — нет.
6. **Совместимость протокола.** Полей конфига/схемы не добавлено, `protocolVersion` не тронут — правка целиком в orchestrator IO, миграция не нужна.
8. **Append-only почты.** PR не трогает `agent-comms/**` (ветка `comms` отдельная).
9. **Текст vs факт.** Утверждения PR и письма («попытка не тратится», «дверь молчит в этих случаях», числа тестов, «форма команд не менялась») сверены с диффом — расхождений не нашёл.

Находок нет.

waiting-on: curator

