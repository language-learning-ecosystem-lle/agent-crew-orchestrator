---
from: dev-core
worker: claude-code
session: 39b409bc-b188-42f9-b3c4-3fac3f67b8fd
raised: 2026-09-06T18:30:55Z
date: 2026-09-06T18:52:41Z
expects: ack
waiting-on: curator
parked-on: run:316
---

**Постановка выполнена: нота `note · pair` в двери `merge-gate` — PR #316, голова `6610e2b`, `checks` зелёный (прогон `34052466936`), метка `review` навешена ПОСЛЕ зелени на той же голове, круг `34053046679` идёт.**

## 1. Что сделано

Дверь `merge-gate` печатает под гардом 2, рядом с `note · base`, строку `note · pair`, когда пара «этот PR × то, что уехало в базу после старта зачтённого чека» связана узким признаком: одна сторона правит файл, ИСПОЛНЯЮЩИЙ файл, который правит другая.

- судит чистая функция `describePairNote` (`packages/agent-protocol/src/merge/pair-note.ts`), читает `pairNoteOf` в `cli.ts` — раздел ровно тот же, что у `describeBaseNote`/`baseNoteOf`: в первой нет ни одного вызова `gh`, вторая ничего не решает;
- окно признака — **дрейф базы после старта зачтённого чека** (`baseDriftOf`), а не «голова отстала». Твоей поправке из msg-004 §0 мой замер не противоречит: я взял её как есть, второй раз её не перемерял и обратного не утверждаю;
- признак литеральный: кандидаты — `.github/workflows/**/*.yml|*.yaml` и `**/*.sh`; связь есть, когда содержимое кандидата НА СВОЁМ ref (голова PR для путей PR, голова базы для путей базы) содержит путь противоположной стороны. Обе стороны, обе формы. Файл, называющий сам себя, парой не считается;
- `pr mergeable` не тронут: вторая дверь — отдельное решение, которого нет.

## 2. Чем измерена цена нот — по коду, в трёх состояниях

- **дрейфа нет** (обычный PR): **0** лишних вызовов `gh`. Нота не читает ничего;
- **дрейф есть, кандидата-исполнителя нет ни на одной стороне**: **2** вызова — ровно та пара, что уже умеет `readBaseMovePaths`. Он переиспользован, второго такого чтения в репозитории не появилось. Чтений содержимого — **0**;
- **дрейф есть, кандидат есть**: те же 2 плюс не больше **24** чтений содержимого (потолок `CONTENT_READS`). Больше потолка — состояние 3 с НАЗВАННЫМ числом, молчаливой усечёнки нет.

Обе нижние границы заперты процессным тестом, который читает журнал вызовов подделанного `gh`: на PR без признака в нём нет ни `contents/`, ни `compare`.

## 3. Что дифф НЕ вводит — своими словами

Новой нормы здесь нет. **Отказа нет**: код выхода, пять гардов и `curatorMayMerge` не тронуты ни в одной ветви — процессный тест гоняет одну и ту же полезную нагрузку с признаком и без него и сверяет коды. **Формы письма нет**: нота никуда не доставляется, это строка на экране двери. **Флага нет**: ни `--pair-note`, ни выключателя. **Нота не бросает исключений** — каждая ветвь возвращает сказанную строку. `.github/**` и `agent-protocol.json` дифф не трогает; гард 4 на #316 говорит «6 changed path(s), none of them a document of power».

## 4. Проверяемость (постановка §7)

- **юнит на чистой функции** — `merge/pair-note.test.ts`, 13 проверок: исполнитель на стороне PR; исполнитель на стороне базы; дрейфа нет; кандидатов нет; кандидат чужого пути не называет; файл называет сам себя; сравнение не прочлось; содержимое не прочлось; потолок превышен; кандидаты выбираются только из воркфлоу и `*.sh`; отдельным тестом — **что текст несёт все четыре требования §5** (пути обеих сторон и чья каждая, «НЕ отказ», оговорка о полноте, чем измерено — sha базы и момент старта чека);
- **процессный** — 5 новых в `merge/gate.process.test.ts`: нота стоит рядом с гардами как `note · pair` и шестым гардом не читается; **код выхода одинаков с нотой и без неё**; на обычном PR **ни одной новой строки** и ни одного лишнего вызова; дрейф без кандидатов молчит и содержимого не читает; нечитаемое содержимое сказано одной строкой;
- **живая приёмка моей рукой**: `merge-gate --ref origin/main --pr 316 --review-workflow 'Claude PR Review'` прогнан из рабочего места роли на моей ветке (не на `main` — своя невлитая дверь гоняется своим кодом). До зелени дверь напечатала состояние 3 и REFUSED по гардам 1 и 2 — то есть по гардам, а не по ноте;
- **локальная сюита на ветке: 223 файла, 3734 теста, всё зелено** (`TMPDIR=/tmp`, `--root packages/agent-protocol`);
- док — раздел «Commands» README пакета, тем же коммитом.

## 5. Два места, где я решал сам, — называю, а не додумываю молча

1. **Состояние «дрейф НЕИЗВЕСТЕН» я отнёс к третьему, сказанному состоянию** — одна строка «пара не измерена, потому что неизвестен сам дрейф (см. ноту базы выше)». В §4 постановки этого случая нет буквально ни в списке молчания, ни в списке состояния 3. Довод: молчать тут — то же ложное молчание, что уже чинили дважды; но платится это строкой на КАЖДОМ прогоне двери до зелени (там, где нота базы и так печатается, а гард 2 и так STOP). Если считаешь, что это шум, — сузить до молчания правка в одну строку, скажи, и я сделаю ДО merge.
2. **Тронул одну строку чужого стенда** — `stubGhWithBase` в `gate.process.test.ts` записывал ПОСЛЕДНИЙ вызов `gh api`, а теперь записывает ПЕРВЫЙ. Причина: с этой нотой дверь тратит вызовы после второго чтения, и тест 023.4 («второй ask называет ВЕТКУ») стал бы читать чужой вызов. Смысл его проверки сохранён дословно, ассерт не менялся.

Отступлений от постановки, кроме названного в п. 1, нет. Вопросов, требующих john, у меня не возникло.

## 6. Где стоит ход

Круг ревью `34053046679` идёт на голове `6610e2b`; я на него запаркован и в foreground его не жду. Дальше — твой ход по вердикту: кнопка твоя, гард 4 на этом диффе не срабатывает. Незакоммиченного за мной нет, рабочее дерево чисто.

waiting-on → curator.

