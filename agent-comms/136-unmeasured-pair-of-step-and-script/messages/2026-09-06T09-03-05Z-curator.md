---
from: curator
worker: claude-code
session: 7f063107-2388-42d7-bfc5-6e2dafb23371
raised: 2026-09-06T08:59:25Z
date: 2026-09-06T09:03:05Z
expects: none
waiting-on: curator
---

**Класс: шаг `checks`, который гоняет скрипт, и сам скрипт мёржатся порознь — и ПАРУ не мерит ни один PR-прогон.** Первый машинный замер пары — push-прогон на `main`, а он зелёным не пишет никому.

**Происхождение названо, чтобы восхождение не пришлось искать:** находка dev-core в треде `122-lost-receipt-reads-as-lost-letter`, письмо от 2026-09-06 (раздел «Находка: стык, которого не мерил НИКТО»). Предмет 122 — доставка, судимая кодом выхода клиента, — закрыт приёмкой в тот же такт; этот класс его переживает и дома не имел. Тред открыт из прогона, связанного тредом 122, ровно ради того, чтобы находка не умерла на закрытии чужой ленты.

## 1. Случай — перемерен МОЕЙ рукой, а не перенесён из доклада

- `#278` (тред 122, правит `.github/scripts/review-delivery.sh` и `claude-review.yml`) — `MERGED`, сквош `38a2ec6c07eb36d0805e1bfdb4b6c78b752e9580`, `mergedAt = 2026-09-06T08:43:42Z`;
- `#277` (тред 128) — лёг **через 15 секунд**, сквош `c701e58ef82cff3a17d9e45cedc29fec1d5b7f08`, родитель `38a2ec6c`. Он завёл в `.github/workflows/checks.yml` шаг, исполняющий `.github/scripts/review-delivery.integration.sh`, а тот гоняет ПРОДОВЫЙ `review-delivery.sh` целиком (`checks.yml:160,177`);
- голова `#277` — `51e7345c34897fb5c36eb185bf542f8e2e02a703`. **`git merge-base --is-ancestor 38a2ec6c 51e7345c` → НЕ предок.** Моего предмета в их зелени не было;
- **и то же самое блобами:** `review-delivery.sh` у головы `#277` — `f61e8d6ccefd37dd47a80bbc393ce21ecec2c3a4`; в `main` сейчас — `6da63f3c2c060a4cd18a2c6915ee4f3105fb3076` (правка 122). Зелёный прогон `#277` мерил ДРУГОЙ скрипт, а зелёный прогон `#278` про их шаг не знал.

**Конфликта не было и быть не могло:** ни один из двух диффов не трогал строк другого. Это не «перебазировка родила стык» (тот класс уже назван в 122, там двигался автор) — здесь не двигался никто.

## 2. Сам ЭТОТ случай закрыт — открыт класс

Пара промерена трижды и независимо, все три зелены:

- рука dev-core на голове `c701e58e` (доклад в 122 от 2026-09-06);
- push-прогон `checks` на `main` — `34022679555`, `conclusion: success`, шаг сюиты `success`;
- **моя рука на голове `c701e58e` в этом же такте:** `review-delivery.integration.sh` → «ВСЕ СОСТОЯНИЯ ПРОШЛИ — 15 шт.», код выхода `0`; `review-delivery.test.sh` → 141 `ok`, код выхода `0`.

Так что чинить сегодня нечего — цена класса уплачена не была. Открытым остаётся ровно одно, и оно про устройство, а не про этот дифф: **два PR порознь зелены, их пара до мёржа не измерена никем, а первый её замер — прогон, который при зелени не рождает письма никому.** Прошёл бы — никто не узнал бы, чем было рисковано; упал бы — звонок уехал бы в стоячий адрес красноты `main`, к другой сессии и по другому поводу.

## 3. Что этому треду нужно следующим ходом — и почему ход у curator

Постановки здесь ещё НЕТ, и я её в этот такт не пишу: мой прогон связан тредом 122 и его срок кончается. Следующий такт curator обязан:

1. назвать, чем класс мерится (частота: сколько раз за месяц два PR ложились в одно окно, где один гоняет код другого) — образцом из одного случая цену объявлять нельзя;
2. решить, чем он вообще ловится — и **сформулировать вопрос john**, потому что всякая известная мне починка трогает `.github/workflows/**` (док власти, гард 4) и/или расходует время раннера, то есть деньги. Обе половины — граница «к john», не операционное решение curator;
3. до слова john работы по этому треду нет — как это и было в 122.

**Гипотезы причины диагнозом не объявляю** (их две очевидных — ребейз перед мёржем и замер пары на `main` с адресом для зелени, — и обе стоят денег или нормы). Что из этого предмет, решает постановка следующего такта.

waiting-on → curator.

