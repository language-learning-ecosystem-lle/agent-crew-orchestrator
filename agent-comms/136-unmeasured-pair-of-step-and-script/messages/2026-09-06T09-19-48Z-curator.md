---
from: curator
worker: claude-code
session: 1a762d1d-3c47-4a04-9b7a-8f5f23e4bfe1
raised: 2026-09-06T09:13:26Z
date: 2026-09-06T09:19:48Z
expects: ack
waiting-on: curator
parked-on: john
---

**Требуется решение john: ловить ли класс «пара PR не измерена никем» — и если ловить, то которой из четырёх дверей? Цены всех четырёх измерены и названы ниже.**

Это ход, обещанный пунктом 3 письма `2026-09-06T09:03:05Z-curator.md`: класс перемерен не образцом, а перечислением, и вопрос сформулирован. Работы по треду до слова john нет.

## 1. Чем класс измерен — метод, а не доклад

Окно: **все 278 смёрженных PR, 2026-08-17T12:04:17Z … 2026-09-06T08:43:57Z (20 суток)**. Ни один случай не взят из чужого доклада — всё считано моей рукой в дереве curator:

1. `git fetch origin '+refs/pull/*/head:refs/remotes/pr/*'` — головы всех PR локально (рабочее дерево не трогается, чекаут чист);
2. `gh pr list --state merged --limit 400 --json number,mergedAt,headRefOid,mergeCommit,files`;
3. для каждого сквоша `S` на первом родителе `origin/main`: `P = S^` — это голова `main` НА МОМЕНТ мержа. Критерий: `git merge-base --is-ancestor $P $headRefOid`. **Ложь = слитое дерево не мерил ни один PR-прогон** (в зелени ветки не было того, что уже лежало в `main`);
4. вторая сторона пары = `git diff --name-only $(git merge-base $P $head) $P`; своя сторона = `... $(merge-base) $head`.

Воспроизводится этими четырьмя строками; числа ниже — их вывод.

## 2. Три яруса, и они разные по силе

| ярус | что считает | за 20 суток |
|---|---|---|
| **потолок** | голова PR не содержала тогдашней `main` | **187 из 278 (67 %)** |
| **середина** | из них: обе стороны правили ОДИН И ТОТ ЖЕ файл, текстово слилось молча | **96**, из них **42** — общий файл в `packages/**` (исходник, не тест: `cli.ts`, `orchestrator/launch.ts`, `notify/notify.ts`, `usage.ts`, `thread/index-doc.ts`, …) |
| **пол** | из них: одна сторона правит файл, который ИСПОЛНЯЕТ дифф другой стороны (runner/runee) | **4** |

Все четыре случая пола — поимённо, чтобы восхождение не пришлось искать:

- **#26** (2026-08-19) — `ci-outcome.yml` × `claude-review.yml`;
- **#217** (2026-09-03T12:33:48Z) — `comms-derived.yml` (строка `run: bash "${CODE}/.github/scripts/comms-derive.sh"`) × `comms-derive.sh`;
- **#258** (2026-09-03T22:31:14Z) — `ci-outcome.yml` × `claude-review.yml`;
- **#277** (2026-09-06T08:43:57Z) — тот самый: `checks.yml` → `review-delivery.integration.sh` → `source review-delivery.sh`.

**Пол — это ПОЛ, а не число случаев:** он ловит только литеральное имя пути внутри воркфлоу или shell-файла второй стороны. Ребро «тест импортирует модуль», «шаг зависит от поведения, не называя файла» этим шаблоном не ловится вовсе. Середина, наоборот, — не утверждение о поломке: 42 общих файла слились текстово, и что хоть один из них слился НЕВЕРНО, я не проверяла и не заявляю.

## 3. Насколько тесно окно — и цена, уплаченная до сих пор

Разрывы между соседними мержами (277 промежутков):

- **≤ 60 с — 30 промежутков, СТАРЫЕ ВСЕ 30**; ≤ 5 мин — 51, старые все 51; 5–15 мин — 54, из них 49 старых; > 1 ч — 48, старых 12.

То есть «пара в одном окне» — не редкость и не совпадение 6 сентября: тесное окно даёт стойкость почти со стопроцентной вероятностью, а редким остаётся только совпадение ПРЕДМЕТОВ.

**Цена класса, уплаченная за 20 суток: ноль, и это тоже замер.** Из четырёх случаев пола три попали в окно, где прогон `main` читается (`gh run list --workflow checks.yml --branch main --limit 100`): `#217` → `33755930172` `success`, `#258` → `33813441799` `success`, `#277` → `34022679555` `success`; `#26` — вне окна ста прогонов, конклюжн не читала и не выдумываю.

**Отдельно снимаю подозрение, которое напрашивается:** в этом же окне 43 из 100 push-прогонов `checks` на `main` красные. Это НЕ наш класс: краснота стоит сплошняком с 2026-09-02T13:35Z по 2026-09-03T12:31Z (46 прогонов, 3 зелёных, 43 красных), а после 12:32Z — 54 прогона подряд и все зелёные. Так выглядит стоячая поломка, а не пара; называть её ценой этого класса было бы ровно тем «текст против факта», что я ловлю у исполнителей.

## 4. Четыре двери и их измеренные цены — вопрос john

**А. Не ловить вовсе.** Цена: 4 узких / 42 широких непромеренных пары за 20 суток при нуле уплаченного. Детект остаётся прежним: первый замер пары — push-прогон на `main`, который на зелени не пишет никому, а на красноте звонит в стоячий адрес красноты `main` — к другой сессии и по другому поводу.

**Б. Требовать актуальную голову перед merge** (`update-branch` + свежая зелень). Цена измерена, и она худшая из четырёх: голову двинули бы **187 из 278** мержей → 187 лишних прогонов `checks` (~9 в сутки) и — дороже — **187 убитых вердиктов**: голова уехала, гард 1 требует нового круга, то есть 187 кругов модели за 20 суток. Плюс новый шаг маршрута = норма.

**В. Узкий гард в двери `merge-gate`** — пакетный код (`packages/**`, зона dev-core), `.github/workflows/**` не трогается, гард 4 ни при чём. Дверь считает дельту `main` с merge-base ветки и говорит ТОЛЬКО когда пара связана: общий файл либо runner/runee. По замеру сработала бы 4 раза узко / 42 раза широко за 20 суток, остальные ~145 старых мержей пропустила бы молча. Раннерного времени 0, кругов модели 0.

**Г. Мерить пару ПОСЛЕ мержа, с адресом для зелени** — push-прогон на `main` пишет письмо и при зелени (или при зелени со стойкой парой). Цена: `.github/workflows/**` — кнопка john по гарду 4 — плюс новая форма письма и новый повод звонить, то есть норма, плюс шум на каждом мерже.

**Ни одна дверь не является операционным решением curator, и в этом суть вопроса:** Б, В и Г вводят НОВЫЙ отказ или новую форму сообщения — это норма по определению из карточки («новая форма сообщения, новый шаг маршрута, снятие или сужение запрета»), а Б и Г вдобавок тратят деньги (раннер и круги модели). А — тоже решение, просто молчаливое. Класс полевого дефекта сюда не годится: чинить нечего, поведение контура ничему объявленному не противоречит.

**Рекомендация curator (рекомендация, не решение):** дверь **В** в форме ПРЕДУПРЕЖДЕНИЯ, а не отказа — на узком признаке (runner/runee), потому что это единственная из четырёх, которая не стоит ни раннерного времени, ни круга модели, ни правки дока власти, и единственная, чей замер (4 за 20 суток) говорит, что шуметь она будет раз в пять суток. Порог «общий файл» (42 за 20 суток, дважды в трое суток) я бы НЕ брала: он бьёт по `cli.ts`, который правят все, и превратится в фон.

## 5. Что этот ход НЕ сделал

Постановки нет и не будет до слова john — исполнителя (dev-core) звать не к чему, пока не выбрана дверь. Незакоммиченного за мной ничего: замер жил во временном каталоге вне чекаута, рабочее дерево роли чисто (`git status --porcelain` пуст), в репозиторий этот ход не пишет ни строки.

Требуется решение john — какая дверь. Ход остаётся у curator, тред запаркован на john.

waiting-on → curator.

