---
from: reviewer-pr
worker: gh-action
date: 2026-09-06T18:58:43Z
expects: answer
waiting-on: curator
verdict: approve
pr: 316
---

verdict: approve
pr: 316
waiting-on: curator

Полный прогон тестов не повторял — `checks` на голове `6610e2b1474e11f992f41b353e196415a7d851b8` зелёный, прогон [`34052466936`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34052466936).

Прогнано точечно (то, что срез не отменяет): `pnpm --filter agent-protocol exec vitest run src/merge/pair-note.test.ts src/merge/gate.process.test.ts` — 2 файла, 57 тестов, все `passed`; `pnpm --filter agent-protocol typecheck` — чисто; `pnpm exec biome check` по всем изменённым файлам — 0 находок в диффе (2 `info` вне диффа, в `cli.ts:7617,14110`, не тронутых этим PR).

Живой исход `pnpm protocol merge-gate --ref origin/main --pr 316`: `REFUSED` — гард 1 (approve на голове нет — этот вердикт его и создаёт), гард 2 (`review=IN_PROGRESS`, плюс `note · base`: база уехала после старта зачтённого чека), гард 3 и 5 — ход curator (трасса восхождения и трасса мержа). `note · pair` на этой паре НЕ напечатана — при живом дрейфе базы связь runner/runee измерена и её нет; это подтверждает состояние 2 (молчание при измеренном отсутствии) кодом этого же PR на живых данных. Гард 4 — чисто, 6 путей PR, ни одного дока власти. Дверь власти (`--power-docs` не понадобился, список голый): 8 путей — `agent-protocol.json`, 4 карточки ролей, `REVIEWER.md`, `PROTOCOL.md`, `.github/workflows` — из них дифф не трогает ни одного.

Критерий 1 (числа тестов): PR заявляет 13 юнит-проверок (`pair-note.test.ts`) и 5 процессных (`gate.process.test.ts`) — пересчитано по диффу построчно, оба числа совпадают.

Критерий 2 («ждём ровно то, что проверяем»): асserты `pair-note.test.ts` бьют в текст ноты (пути обеих сторон, слова «not a refusal», оговорку о неполноте, `drift.detail`), не только в факт срабатывания; процессный тест дополнительно запирает то, что юнит не видит — одинаковый код выхода с нотой и без, и ноль строк/чтений на обычном PR (`gate.process.test.ts`, тест «an ordinary pull request gets NOT ONE new line»).

Критерий 3 (скоуп): `thread: 136-unmeasured-pair-of-step-and-script` в описании PR прочитан целиком. Постановка dev-core (msg-004) — узкий признак runner/runee, печать без отказа, три сказанных состояния, воспроизведение `readBaseMovePaths`, потолок 24 читаемым числом. Дифф реализует всё перечисленное; расширений вне постановки не нашёл. Слово john (msg-003, «ТРЕТЬЯ, ПРЕДУПРЕЖДЕНИЕМ, УЗКИЙ ПРИЗНАК») восходит напрямую — класс полевого дефекта (критерий 12) в постановке явно не заявлен и не нужен здесь (curator сама это оговорила в msg-004 §1), поэтому критерий 12 к этому PR не поднимается.

Критерий 4/5 (зоны/доки власти): `pnpm protocol zones check --ref HEAD --role dev-core --paths <6 файлов диффа>` → «none under a forbidden prefix» — все шесть путей в `packages/agent-protocol/**`, зона роли. Ни один док власти (`.github/workflows/**`, `docs/roles/**`, `PROTOCOL.md`, `REVIEWER.md`, `agent-protocol.json`) не тронут.

Критерий 6 (совместимость протокола): новых полей конфига, новой формы письма, новых флагов нет — `describeMergeGate` получил необязательный параметр с дефолтом `[]`, обратная совместимость сохранена; сам факт подтверждён и вручную (прогон merge-gate выше отработал на обычных гардах без ноты `pair`).

Критерий 9 (текст vs факт): проверил три частных заявления диффа отдельно от прозы — «нота не бросает исключений» (все `gh`-чтения в `pairNoteOf` и в переиспользуемом `readBaseMovePaths` обёрнуты в try/catch, ни один путь не пробрасывает); «переиспользован `readBaseMovePaths`, а не написан второй раз» (импорт и вызов те же); «на обычном PR — ноль вызовов» (early return до входа в `readBaseMovePaths` при `drift.state !== "drift"`). Все три — верны по коду.

Критерий 11 (класс «дверь молчит»): постановка сама различает состояние 2 (измерено-и-нет → тишина) от состояния 3 (не измерено → одна строка) — оба покрыты тестами и никакой из них не выдаёт себя за другой; живой прогон merge-gate выше — образец состояния 2, а не мнимого «связи нет».

Расхождений с REVIEWER.md не нашёл. Мелкое наблюдение вне критериев: путь кандидата в URL `contents/${candidate.path}` не проходит через `encodeURIComponent` (в отличие от `ref` рядом), но множество кандидатов сужено регэкспом до `.github/workflows/**/*.yml|yaml` и `**/*.sh` — в реальных путях репозитория спецсимволов URL не бывает; находкой не считаю, называю на всякий случай.

---

Доставлено шагами прогона [`34053046679`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34053046679) по PR #316, голова `6610e2b1474e11f992f41b353e196415a7d851b8` (вердикт написан агентом ревьюера, доставка — джобой: тред 088).
Ход передан роли `curator` — так объявил сам вердикт.

