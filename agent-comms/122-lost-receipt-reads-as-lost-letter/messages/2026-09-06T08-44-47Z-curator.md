---
from: curator
worker: claude-ai
date: 2026-09-06T08:44:47Z
expects: answer
waiting-on: curator
delivers: john
priority: high
merged-pr: 278
---

**Слово john полем `delivers` (чат ~08:44Z): #278 СМЁРЖЕН его кнопкой. Доставку судит ЦЕЛЬ, а не код выхода клиента.**

Отказная ветвь — и только она — перечитывает цель и ищет свою запись; нашла → доставка засчитана, не нашла → `failed`, как прежде. **Успешная ветвь без единого лишнего вызова**, и это проверено СЧЁТЧИКОМ вызовов стаба, а не глазом.

**Починены все пять мест класса** (по доводу john: чинить один названный случай при четырёх таких же — чинить образец). Два из них закрываются одной правкой: письмо вердикта и письмо итога делят общую функцию.

## Что стоит унести из этого хода

**Перебазировка родила СТЫК, которого git не показал.** Соседний предмет (`125`) сменил смысл общей переменной: функция стала принимать АДРЕС вместо треда, а проба этого PR опознаёт своё письмо ПУТЁМ файла и собирала его из старого значения. На всей ветви эскалации она не нашла бы своего письма **никогда** — вырождалась бы в «не доехало» ровно там, ради чего писана. Обе стороны правили соседние строки, конфликта не было.

Починено, и цена стыка названа машинно: восемь проверок, из них одна — **«слаг пути НЕ даёт»**, то есть неверный вариант закреплён тестом, а не памятью.

**И честная оговорка о самом PR:** тело при создании было чужим — команда подобрала свежайший файл соседней сессии. Роль это назвала первой строкой и переписала, вместо того чтобы поправить молча.

**Живая приёмка сознательно не покрывается** (отказ шлюза по требованию не воспроизводится) — принято john заранее. Вырождение названо: запись не видна из-за задержки → поведение станет сегодняшним, хуже не будет.

waiting-on → curator.
