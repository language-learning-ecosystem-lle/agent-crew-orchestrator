---
from: dev-core
worker: claude-code
session: 8b60e00e-8409-47a7-a38d-14c9443386ae
raised: 2026-09-05T11:56:59Z
date: 2026-09-05T12:11:00Z
expects: answer
waiting-on: curator
---

**Сделано и уехало: PR #278, голова `975380b9`, ветка `fix/122-delivery-judged-by-target`. Жду `checks` по #278.** Метку `review` не вешаю — круга на этом PR не будет по построению, как и сказано в постановке; кнопка john по гарду 4.

## 1. Что в диффе

Отказная ветвь — и только она — перечитывает цель и ищет СВОЮ запись: нашла → доставка засчитана и джоба не краснеет, не нашла → `failed`, как сегодня. Шов из §3.1 стои́т: `delivery_confirmed` (чистый предикат с ИНЪЕКТИРУЕМОЙ пробой) + `delivery_attempt` (он же с поимённым учётом). Проба различает три исхода кодом выхода: `0` нашла, `1` нет, `≥2` отказала сама.

**(а) Новой нормы дифф не вводит.** Шаг возвращается ровно к тому, что он и так объявляет («доставлено / не доставлено»). Ни маршрута, ни прав, ни формы сообщения, ни конфига он не трогает. Границы 1–3 соблюдены: успешная ветвь без единого лишнего вызова (счётчик вызовов стаба), повторного POST/push нет ни на одной ветви — в том числе при отказе самой пробы, форма сообщений не менялась ни на маркер.

**(б) Пятое место ВЗЯТО** — по твоему же доводу john'а: тот же приём на том же канале в том же шаге. И называю то, чего в постановке не было видно: **снять пятое отдельно нельзя.** Письмо вердикта (674) и письмо итога (928) идут через ОДНУ функцию `deliver_to_thread`, и перечитывание встало ВНУТРИ неё — иначе `escalate_undelivered` успевала бы отправить ложный отказ раньше пробы. Значит либо оба места, либо ни одного: если john сочтёт добавку выходящей за своё слово, снимаются оба и едут три (коммент вердикта, статус, коммент итога). В описании PR это сказано.

**Отступление от формы постановки, одно, и оно доложено:** §3.2 предлагала опознавать письмо в тред номером прогона. Взят ПУТЬ своего файла в удалённой ленте — по тому же доводу, которым ты браковал одиночный литерал в комментах: в ОДИН тред за прогон уходят ДВА письма (вердикт и итог), и оба несут `RUN_ID`, то есть проба итога приняла бы письмо вердикта за своё. Второе, важнее: читается `FETCH_HEAD`, а не диск — к моменту пробы письмо уже закоммичено локально, и проба по диску отвечала бы «доставлено» ровно в том случае, ради которого писана (не уехало ничего). У комментов литерала ДВА и оба обязаны быть в одном комменте: номер прогона + постоянная фраза своего текста. В тело ради опознания не добавлено ничего.

Формальный статус — по тройке «автор + состояние + окно», как предписано; слабое место названо комментарием в коде. Окно снимается ПЕРЕД записью, а не от начала прогона, и промахивается только на расхождении часов раннера с GitHub — в безопасную сторону.

## 2. Проверяемость

`.github/scripts/review-delivery.test.sh`, раздел 11 — +29 проверок, сети в файле по-прежнему нет. Все четыре обязательных случая закрыты, включая «нулевой выход клиента → перечитывания НЕ происходит» **счётчиком вызовов стаба** и «проба отказала сама → `failed` НАЗВАННОЙ строкой» (проверяется текстом вывода, а не кодом). Сверх них: обе пробы `gh` на стабе — пара литералов, чужой прогон, отказ запроса и ОТДЕЛЬНО отказ разбора; проба статуса на фикстуре, где лежит `approve` ПРЕДЫДУЩЕГО круга (отсекается окном) и запись чужого автора; проба письма — на настоящем git с файловым remote, четыре состояния.

**Правка доказана мутацией, а не зелёным цветом:** убираю ранний выход на нулевом коде клиента — падают три проверки, включая счётчик; схлопываю три исхода пробы в два — падает проверка на названную строку отказа пробы.

**(в) Что осталось непокрытым в интеграционном, и молчания тут нет.** `review-delivery.integration.sh` НЕ трогался. Пробы письма в тред на живом чекауте почты в нём нет: он гоняет дверь напрямую, минуя `deliver_to_thread`, а новая ветвь живёт внутри неё и поднимается только на ОТКАЗЕ push — состояния, которого его стенд не воспроизводит вовсе. Естественно она туда не легла, и подпирать её выдуманным отказом push я не стал. Ветвь закрыта юнитом на настоящем git (remote — путь на диске), и это ровно то место, где «читается FETCH_HEAD, а не диск» и меряется.

Живая приёмка сознательно не покрывается — как принято john. Вырождение (задержка репликации → доставка объявится несостоявшейся, поведение станет сегодняшним) названо и в коде, и в `docs/protocol-reference.md`.

Доки — тем же коммитом: `docs/protocol-reference.md`, раздел «Доставку судит ЦЕЛЬ, а не код выхода клиента (тред 122)». README пакета не трогался: команд пакета правка не меняет.

## 3. Промах в моём такте, и он в таймлайне PR виден

`pnpm protocol pr open` я накормил ЧУЖИМ файлом тела: выбирал свежайший `pr.md` по всем `mktemp`-каталогам, и подобрался файл соседней сессии (`thread: 124-norm-number-method-unnamed`, `role: curator`). PR #278 создан с ним, я это увидел в выводе двери (она печатает прочитанные `thread:`/`role:`) и переписал тело своим следующим действием. Ветка, голова и заголовок мои с самого начала; пострадало только тело и только на минуту. В описании PR приписка об этом стои́т, чтобы правка тела не читалась как подгонка задним числом. Твоего PR по треду 124 это не касается — файл только прочитан.

## 4. Где стои́т предмет

- незакоммиченного нет, рабочее дерево чистое;
- `checks` по #278 идёт; метки `review` нет и не будет — круг на этом PR пустой по построению;
- кнопка — john по гарду 4. Ход тебе: донести решение по пятому месту, если он сочтёт добавку выходящей за своё слово, и провести PR к кнопке.

waiting-on → curator.

