---
from: curator
worker: claude-code
session: bf31d5ff-9fdf-425d-aa0c-82b300720b4f
raised: 2026-09-05T13:04:24Z
date: 2026-09-05T13:10:59Z
expects: ack
waiting-on: curator
parked-on: john
---

**Требуется решение john. Вопрос первой строкой: PR #278 стои́т готовым под твою кнопку — мёржишь ли ты его со ВСЕМИ ПЯТЬЮ местами класса, или пятое место снимается?** Развилка не свободная, и это замерено: пятое (письмо ИТОГА в тред) и второе (письмо ВЕРДИКТА в тред) идут через одну функцию `deliver_to_thread`, снимаются только вместе, и тогда едут ТРИ места. Твоё слово 2026-09-05 называло четыре — потому что четыре назвал тебе я; пятое перечислил dev-core при перемере, и я его довод принял (тот же приём, тот же канал, тот же шаг). Кнопка твоя по гарду 4 в обоих случаях.

## 1. Где стои́т PR — числами моей руки, а не пересказом доклада

- **PR #278**, ветка `fix/122-delivery-judged-by-target`, голова `fc4f0c6d4f44f2e315838f573ba79e177d4426f7`, база `main`;
- `mergeable=MERGEABLE`, `mergeStateStatus=CLEAN` — прочитано мной сейчас, а не взято из письма смотрителя;
- `checks` — **success** на этой голове, прогон `33966174144`;
- **дерево мержа РАВНО дереву ветки:** `merge-base origin/main FETCH_HEAD` = `ccf7691d91307632c7d87b58d88088aba0b2dd7a` = текущая голова `main`, и `merge-tree --write-tree origin/main FETCH_HEAD` даёт `588650786a4fb6a1813f78c54e32aa6673886026` — ровно дерево головы `fc4f0c6d`. Практически это значит, что зелёный прогон CI мерил ТО САМОЕ дерево, которое ляжет в `main`: обычной дыры «ветку мерили, слитое дерево не мерил никто» здесь нет по построению. Пока никто не мёржит в `main` ничего другого — она остаётся закрытой;
- дифф: `534` вставки, `11` удалений, четыре файла. **Удаления перечислены поимённо:** десять строк в `claude-review.yml` — это старые `if … then delivery_mark ok; else delivery_mark failed` ровно тех трёх мест, что правятся; одиннадцатая — своя же строка `local` в `review-delivery.sh`, заменённая расширенной. Чужого текста не удалено ни строки (`review-delivery.test.sh` — `224/0`, `docs/protocol-reference.md` — `51/0`).

## 2. Что говорит дверь

`pnpm protocol merge-gate --ref origin/main --pr 278 --review-workflow 'Claude PR Review'` → `REFUSED`:

- **гард 1 — `STOP`**: `no approve verdict on fc4f0c6`. Это ожидаемо и объявлено заранее: PR правит `.github/workflows/claude-review.yml`, круг такому PR не положен ПО ПОСТРОЕНИЮ (действие самопропускается, пока файл ветки не совпадает с дефолтной). Метка `review` на него не вешалась и не должна — она вернула бы пустой прогон, а не вердикт. **Гард 1 здесь закрывается человеческим чтением, и человек тут ты.**
- **гард 2 — `ok`**: `checks=SUCCESS` на `fc4f0c6`;
- **гард 3 — восхождение держится**: `thread: 122-lost-receipt-reads-as-lost-letter` в описании PR, решение в ленте — `msg-002` (твоё «РАЗРЕШАЮ», чат 2026-09-05 ~11:45Z);
- **гард 4 — `STOP`: `john merges this one — it changes .github/workflows/claude-review.yml`.** Отсюда и письмо;
- гарды 3 и 5 дверь печатает обязательствами, не «пройдено».

## 3. Три границы твоего решения — я сверил их ДИФФОМ, а не докладом

1. **успешная ветвь не трогается вовсе.** `delivery_confirmed` выходит `return 0` ДО единственного вызова пробы, если код клиента `0`; в тесте это держит счётчик вызовов стаба, а не глаз;
2. **повторной записи нет ни на одной ветви.** Проба только читает (`gh api …/comments`, `…/reviews`, чтение удалённой ленты); ни `gh pr comment`, ни `gh pr review`, ни `push` второй раз не зовутся — в том числе на исходе «проба отказала сама»;
3. **форма сообщений не менялась.** Опознание собрано из литералов, которые в теле УЖЕ есть (номер прогона + постоянная фраза своего текста), маркера ради опознания не добавлено. У формального статуса литерала нет вовсе, и там честно стои́т тройка «автор + состояние + окно», а слабое место названо комментарием в коде.

Три исхода пробы (`0` нашла / `1` нет / `≥2` сама отказала) в коде различены кодом выхода, и на каждом стои́т НАЗВАННАЯ строка в логе: «квитанция потеряна, а запись на месте» — `::warning::`, остальные два — `::error::`. Молчаливого зелёного шага после `502` не остаётся.

## 4. Что я перемерил СВОЕЙ рукой, а не принял докладом

Дерево ветки вынуто `git archive fc4f0c6d .github/scripts` в каталог вне обоих чекаутов — своё рабочее дерево при этом чистое, ничего не трогалось.

- **`bash review-delivery.test.sh` → `141/141`, `FAIL` ноль, код выхода `0`.** Число исполнителя сошлось.
- **Мутацией проверено, что тест ДЕРЖИТ первую границу, а не просто зеленеет.** Убрал ранний `return 0` на нулевом коде клиента — краснеют **пять** проверок, среди них обе на счётчике вызовов пробы («проба НЕ вызвана ни разу», «счётчик подтверждает: успешная ветвь пробу не зовёт»). dev-core называл три; моё число — пять, и расходится оно в безопасную сторону. Граница «успешная ветвь не трогается» держится машинно.
- **Проверил то, чего на этом PR не мерит НИКТО, и это отдельный повод.** Круга ревью здесь нет по построению, а `checks` (`.github/workflows/checks.yml`) не содержит ни `actionlint`, ни `yamllint`, ни `shellcheck` — то есть сам `claude-review.yml` до мержа не читает ни одна машина, и синтаксическая ошибка в нём легла бы в `main` и сломала бы КАЖДЫЙ следующий круг ревью. Замерил: YAML головы `fc4f0c6d` разбирается (`yaml.safe_load`, одна джоба `review`), и **все семь блоков `run:` проходят `bash -n`** — в том числе три правленых шага. Что при этом НЕ измерено и я это называю: `shellcheck` в моём контуре не установлен, поэтому статической проверки шелла глубже синтаксиса нет ни у меня, ни у прогона.

## 5. Шестое место того же корня — я его НАЗЫВАЮ и в этот PR НЕ беру

Постановка требовала не чинить молча найденное сверх перечня. Нашёл я, не исполнитель, поэтому говорю отдельно и с честной пометкой о цене слов: **это чтение кода, а не полевой замер.**

`comms_push` (`.github/scripts/comms-push.sh:78`) судит push тем же кодом выхода клиента. Но ущерб здесь ОБРАТНОГО знака: цикл попыток `deliver_to_thread` на неудаче делает `fetch` + `reset --hard FETCH_HEAD` и пишет письмо ЗАНОВО — а если предыдущий push на сервер лёг и потерялся именно ответ, в треде окажутся ДВА письма одного прогона. Новая проба этого не ловит: она стои́т ПОСЛЕ цикла и до неё дело не доходит — попытка №2 отчитается успехом.

**В PR #278 я это не беру и dev-core не поручал:** без этой починки дверь не отказала бы, предмет объявлен пятью местами, а сам приём в `comms-push.sh` этим диффом не тронут — расширение предмета внутри PR-двери здесь незаконно. Полевого случая у меня нет, поэтому и класс «полевой измеренный дефект» на него не натягиваю. Заведу отдельным предметом после того, как #278 ляжет.

## 6. Что происходит на каждый твой ответ

- **«все пять»** — жмёшь кнопку на #278 как есть (squash, заголовок уже в Conventional Commits). Я перечитываю цель (`base.ref`, скво́ш и его родитель) и закрываю тред приёмкой;
- **«пятое снять»** — я передаю ход dev-core: снимаются ОБА письма в тред (вердикта и итога), едут три места, ветка переголовляется, `checks` ждём заново, и PR снова приезжает к твоей кнопке. Цена — такт исполнителя и прогон; ничего необратимого;
- **молчание** — PR стои́т зелёным и сливаемым, ничего не портится, но каждый следующий `502` на уже прошедшей записи стоит полного круга ревью, как 2026-09-03.

**Парк `run:278` снимаю, называя его: он ждал `checks` по #278, и цвет приехал — `success` на `fc4f0c6d`, прогон `33966174144`. Вместо него встаёт парк на john под вопросом первой строки.**

Незакоммиченного у меня нет: кода я по этому треду не писал ни строки (`.github/**` мне запрещены зоной и границей «к john»). Парк на john, ход остаётся у меня.

waiting-on → curator.

