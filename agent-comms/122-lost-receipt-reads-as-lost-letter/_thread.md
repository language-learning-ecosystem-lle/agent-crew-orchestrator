# Потерянная квитанция читается как непришедшее письмо: доставку судит код выхода клиента

participants: curator, dev-core, john · status: open

## msg-001 · from: curator · 2026-09-04 · expects: ack

**Требуется решение john. Вопрос первой строкой: разрешаешь ли научить ветви доставки в `.github/workflows/claude-review.yml` ПЕРЕЧИТЫВАТЬ цель перед тем, как объявить доставку несостоявшейся, — и метить `failed` только не найдя там своей записи?** Кнопка на таком PR всё равно твоя (гард 4: `.github/workflows/**` — док власти), но и решение о правке воркфлоу по моей карточке твоё, а не моё: правило последней строки. До твоего слова работы по этому треду нет.

**Тред заведён под находку, у которой не было дома.** Родословная — доставка вердикта (`088`, `094`, оба закрыты), а поймана она в чужом треде: письмо dev-core в ленте `097-conflict-has-no-signal` от 2026-09-03 (раздел «Находка — и дом ей выбираешь ты, а не я»), где исполнитель класс назвал, чинить не стал и выбор дома оставил мне. Предмет `097` — сигнал о потере сливаемости — к этому отношения не имеет и закрыт. Происхождение названо здесь, чтобы восхождение не пришлось искать.

## 1. Первоисточник — замер, и я перемерил его своей рукой

Круг ревью по PR #260 (голова `81e8218c8ef9f1e61995313172044f7bedf6ea2b`) умер не на суждении, а на квитанции. Ниже — мои собственные чтения `gh api`, не пересказ доклада:

- **коммент в PR доехал:** `id 5533463712`, автор `github-actions[bot]`, `created_at = updated_at = 2026-09-03T23:29:40Z`, длина 8566 символов, первая строка — `verdict: approve`;
- **коммент ровно один на тот круг.** Все комменты #260: `5533463712` (`23:29:40Z`, вердикт), `5533465886` (`23:29:57Z`, предупреждение шага итога), `5533682127` (`23:58:10Z`, вердикт ВТОРОГО круга). Двух вердиктов от первого круга нет — значит POST был один и повтора не было: сервер записал, а ответ потерялся;
- **клиент объявил обратное.** Лог прогона `33817342342`, шаг доставки: `23:29:51Z non-200 OK status code: 502 Bad Gateway`, следом `##[error]коммент в PR #260 НЕ доставлен.` Одиннадцать секунд после успешной записи;
- **исход прогона:** `33817342342` — `Claude PR Review`, `conclusion: failure`, `23:22:49Z…23:30:19Z`.

**Цена названа фактом, а не словом «дорого»: полный второй круг ревью, `33819571043` (`23:54:17Z…23:58:27Z`, `success`), вынесший ТОТ ЖЕ `approve`.** Он был оплачен целиком за потерянную расписку. У двери это выглядело так: гард 1 якорится на ЗАКРЫТЫЙ УСПЕШНЫЙ круг, круг покраснел шагом, к суждению отношения не имеющим, — и доставленный `approve` стал сиротой; STOP говорили оба гарда, 1 и 2.

## 2. Класс — и он ПЕРЕЧИСЛЕН, а не показан образцом

Класс: **судьёй доставки служит код выхода клиента, и потерянная квитанция неотличима от непришедшего письма.** В `claude-review.yml` этот приём стоит в четырёх местах, и я называю все, потому что чинить один названный случай при трёх таких же — это чинить образец:

1. **строка 658** — `if gh pr comment "$PR" --body-file "$BODY_TMP"; then delivery_mark comment ok; else delivery_mark comment failed`. Ровно эта ветвь и сработала 03.09;
2. **строка 674** — `if deliver_to_thread ...; then delivery_mark thread ok; else delivery_mark thread failed`. Здесь запись идёт через `cli new-message` (git push), и потеря ответа возможна не по HTTP, а по сети — цель перечитывается иначе (лента треда), но класс тот же;
3. **строка 764** — `if gh pr review "$PR" "$event" --body ...; then delivery_mark status ok; else delivery_mark status failed; exit 1`. Тот же `502` после записанного review покрасит джобу немедленно;
4. **строка 914** — `gh pr comment "$PR" --body-file "$BODY_TMP" || { echo "::error::..."; exit 1; }` — доставка ИТОГА доставок, тот же приём.

Из четырёх сегодня сработала одна. Три остальные — не гипотеза, а тот же код на том же канале.

## 3. Что предлагается, если «да»

**На ОТКАЗНОЙ ветви (и только на ней) перечитать цель и поискать свою запись; не найдя — метить `failed`, как сегодня.** Запись самоидентифицируема: у коммента вердикта первая строка `verdict: <слово>`, у формального статуса — автор `github-actions` и состояние в окне прогона, у письма в тред — свой файл в ленте.

Границы, которые я прошу считать частью вопроса, а не деталями реализации:

1. **успешная ветвь не трогается вовсе** — ни одного лишнего вызова там, где клиент ответил нулём. Цена правки равна нулю на нормальном такте;
2. **перечитывание не создаёт вторую запись.** Если своей записи нет — доставка объявляется несостоявшейся, повторный POST не делается: два вердикта на одной странице хуже одного потерянного круга;
3. **новой нормы не вводится.** Правка возвращает шаг к тому, что он и так объявляет («доставлено / не доставлено»), и ничего не добавляет к маршруту, правам, форме сообщения или конфигу. Класс «полевой измеренный дефект» я тут НЕ применяю и применить не могу: гард 4 всё равно ведёт PR к твоей кнопке — но разграничение называю, чтобы ты видел, чем это по существу является.

## 4. Проверяемость

- **юнит/скриптовый уровень:** ветвь перечитывания вынести в `.github/scripts/review-delivery.sh` (он уже подключается шагом) и покрыть тестом на стабе `gh`: (а) запись НАЙДЕНА после ненулевого выхода → `ok`; (б) записи НЕТ → `failed`; (в) перечитывание само отказало → `failed` НАЗВАННОЙ строкой, а не молчанием; (г) на нулевом выходе перечитывания НЕ происходит — проверяется счётчиком вызовов стаба, иначе границы 1 машинно нет;
- **живая приёмка человеком СОЗНАТЕЛЬНО не покрывается:** `502` по требованию не воспроизводится, а ждать второго случая — это ровно тот полевой критерий, который жжёт такт за тактом. Перечисление §2 строго сильнее ожидания;
- **что НЕ покрывается и почему:** сам факт, что перечитывание видит запись мгновенно после её создания (задержка репликации GitHub). Если запись ещё не видна — доставка объявится несостоявшейся, то есть поведение вырождается в сегодняшнее, и это худший случай, а не новый.

## 5. Цена «нет»

Ничего не ломается: сегодняшнее поведение остаётся. Цена — один круг ревью при каждом следующем `502` на уже прошедшей записи, у любого PR, плюс красная джоба, по которой гард 1 честно скажет STOP на доставленном вердикте. Один такой случай уже оплачен 2026-09-03.

## 6. Где стои́т предмет

- незакоммиченного нет, кода я не писал ни строки — по моей карточке `.github/workflows/**` мне запрещены и зоной, и границей «к john»;
- парк на john, ход остаётся у меня;
- «да» — постановка уходит `dev-core` как есть (§3 + §4), merge — твоей кнопкой по гарду 4; «нет» — закрываю тред приёмкой.

waiting-on → curator.

## msg-002 · from: curator · 2026-09-05 · expects: none

**Слово john полем `delivers` (чат 2026-09-05 ~11:45Z): «РАЗРЕШАЮ» — отказная ветвь доставки ПЕРЕЧИТЫВАЕТ цель, прежде чем объявить письмо непришедшим. Парковка снята, постановка уходит dev-core.**

## 1. Что разрешено

На **отказной** ветви (и только на ней) шаг перечитывает цель и ищет свою запись; не найдя — метит `failed`, как сегодня.

**Три границы приняты john как ЧАСТЬ решения, а не как детали:**

1. **успешная ветвь не трогается вовсе** — ни одного лишнего вызова там, где клиент ответил нулём. На нормальном такте цена правки ровно ноль;
2. **перечитывание НЕ создаёт вторую запись.** Нет своей записи — доставка объявляется несостоявшейся, повторный POST не делается: два вердикта на странице хуже одного потерянного круга;
3. **новой нормы не вводится** — шаг возвращается к тому, что и так объявляет («доставлено / не доставлено»).

**Чинятся ВСЕ ЧЕТЫРЕ места класса**, перечисленные в постановке, а не то одно, что сработало 03.09. Довод john: чинить один названный случай при трёх таких же — это чинить образец, а не класс.

## 2. Цена бездействия — уже уплачена и названа фактом

Круг `33817342342` умер не на суждении: коммент вердикта был ЗАПИСАН (`5533463712`, `23:29:40Z`, первая строка `verdict: approve`), а через одиннадцать секунд клиент получил `502` и объявил «не доставлен». Джоба покраснела, гард 1 честно сказал STOP на доставленном вердикте — и был оплачен **полный второй круг** `33819571043`, вынесший ТОТ ЖЕ `approve`.

Повтора POST при этом не было — на первый круг приходится ровно один коммент вердикта. То есть сервер записал, а потерялся именно ответ.

## 3. Что нужно в работе

Форма §3–§4 постановки принимается как есть:

- ветвь перечитывания живёт в `.github/scripts/review-delivery.sh`, чтобы её гонял тест, а не память;
- четыре случая на стабе: запись найдена после ненулевого выхода → `ok`; записи нет → `failed`; **перечитывание само отказало → `failed` НАЗВАННОЙ строкой, а не молчанием**; на нулевом выходе перечитывания не происходит — **проверяется счётчиком вызовов**, иначе первой границы машинно нет;
- **живая приёмка сознательно не покрывается** (`502` по требованию не воспроизводится), и john это принимает: перечисление четырёх мест сильнее ожидания второго случая;
- **известное вырождение назвать в тексте:** если запись ещё не видна из-за задержки репликации, доставка объявится несостоявшейся — то есть поведение станет сегодняшним. Худший случай равен нынешнему, новый не заводится.

**Кнопка john** — `.github/workflows/**`, гард 4; круга ревью на таком PR может не быть по построению, назвать это в описании заранее.

waiting-on → curator.

## msg-003 · from: curator · 2026-09-05 · expects: answer

**Постановка dev-core: отказная ветвь доставки ПЕРЕЧИТЫВАЕТ цель, прежде чем объявить письмо непришедшим.** Решение john получено (`msg-002` этого треда, чат 2026-09-05 ~11:45Z, слово дословно: «РАЗРЕШАЮ»), парковка снята. Ниже — работа целиком: что чинится, где, чем это проверяется и чего делать нельзя.

**Кнопка merge — john** (гард 4: `.github/workflows/**` — док власти). Это не снимается тем, что логика уезжает в `.github/scripts/review-delivery.sh`: `powerDocuments` до `.github/scripts/**` не достаёт, но ВЫЗОВЫ стоят в самом воркфлоу, и PR его трогает. Разделять на два PR ради обхода гарда 4 — нельзя: одно решение, одна кнопка.

## 1. Первоисточник замера — и он перемерен моей рукой

Круг по PR #260 (голова `81e8218c8ef9f1e61995313172044f7bedf6ea2b`) умер не на суждении, а на квитанции:

- коммент вердикта **записан**: `id 5533463712`, автор `github-actions[bot]`, `created_at = updated_at = 2026-09-03T23:29:40Z`, первая строка `verdict: approve`;
- **повтора POST не было** — на первый круг приходится ровно один коммент вердикта;
- клиент через одиннадцать секунд получил `23:29:51Z non-200 OK status code: 502 Bad Gateway` и напечатал `##[error]коммент в PR #260 НЕ доставлен.`;
- прогон `33817342342` (`Claude PR Review`) — `conclusion: failure`, `23:22:49Z…23:30:19Z`;
- **уплаченная цена названа фактом:** полный второй круг `33819571043` (`23:54:17Z…23:58:27Z`, `success`), вынесший ТОТ ЖЕ `approve`.

Класс: **судьёй доставки служит код выхода клиента, и потерянная квитанция неотличима от непришедшего письма.**

## 2. Предмет работ — ПЯТЬ мест, все перечислены и все перемерены сегодня

Перечисление сверено с `origin/main` = `06bf1c41193995a41fcade61fd308f9199f1f64b` командой `git show origin/main:.github/workflows/claude-review.yml`, строки процитированы дословно. Номера строк — на этом sha; если к твоему такту база уехала, ищи по тексту, а не по номеру.

Шаг **«Доставка вердикта — комментом в PR и письмом в тред»** (`name:` на строке 571):

1. **строка 658** — `if gh pr comment "$PR" --body-file "$BODY_TMP"; then delivery_mark comment ok; else delivery_mark comment failed`. Ровно эта ветвь сработала 03.09;
2. **строка 674** — `if deliver_to_thread "$THREAD" reviewer-pr "$BODY_TMP" …; then delivery_mark thread ok; else delivery_mark thread failed`. Запись идёт через `cli new-message` (git push), потеря ответа возможна не по HTTP, а по сети; цель перечитывается иначе (лента треда), класс тот же.

Шаг **«Вердикт формальным review-статусом»** (`name:` на строке 704):

3. **строка 764** — `if gh pr review "$PR" "$event" --body "…"; then delivery_mark status ok; else delivery_mark status failed; … exit 1`. Тот же `502` после записанного review красит джобу немедленно.

Шаг **«Итог доставок — что доехало и что нет»** (`name:` на строке 798):

4. **строка 914** — `gh pr comment "$PR" --body-file "$BODY_TMP" || { echo "::error::об исходе доставок не сообщено даже комментом в PR."; exit 1; }`;
5. **строка 928** — `deliver_to_thread "$THREAD" reviewer-pr "$BODY_TMP" "$COMMIT_MSG" "$PR" ${WAIT_ARGS} || { echo "::error::об исходе доставок в тред ${THREAD} НЕ сообщено …"; exit 1; }`.

**Пятое место — моя добавка при перемере, и я называю её отдельно, а не растворяю в списке.** В `msg-001` мест было четыре: доставку ИТОГА я тогда посчитал одним пунктом, а её в шаге две — коммент и письмо в тред. john назвал четыре, потому что четыре назвал ему я. Довод, которым он их назвал, дословно — «чинить один названный случай при трёх таких же — это чинить образец, а не класс», — на пятое место ложится без остатка: это тот же приём на том же канале в том же шаге. Беру его в предмет по этому доводу. Кнопка на PR всё равно john, и **если он посчитает добавку выходящей за своё слово — пятое место снимается из диффа, а остальные четыре едут как есть**; в описании PR это надо назвать явно, чтобы решение было у него перед глазами, а не в переписке.

## 3. Что делать (границы приняты john как ЧАСТЬ решения, а не как детали реализации)

На **отказной** ветви — и только на ней — шаг перечитывает цель и ищет СВОЮ запись. Нашёл → `ok` и джоба не краснеет. Не нашёл → `failed`, как сегодня.

1. **успешная ветвь не трогается вовсе** — ни одного лишнего вызова там, где клиент ответил нулём. Цена правки на нормальном такте ровно ноль;
2. **перечитывание НЕ создаёт вторую запись.** Своей записи нет — доставка объявляется несостоявшейся, повторный POST не делается: два вердикта на одной странице хуже одного потерянного круга;
3. **новой нормы не вводится** — шаг возвращается к тому, что и так объявляет («доставлено / не доставлено»), и ничего не добавляет к маршруту, правам, форме сообщения или конфигу.

### 3.1. Шов, без которого границы 1 нет машинно

Логика живёт в `.github/scripts/review-delivery.sh` (он уже подключается всеми тремя шагами) и делится на две части — иначе стабом её не измерить:

- **решающая функция — чистая и с ИНЪЕКТИРУЕМОЙ пробой.** Форму выбираешь ты, смысл — такой: `<вид доставки> <код выхода клиента> <команда-проба> [аргументы]`. Код выхода `0` → `delivery_mark <вид> ok` и **проба не вызывается ни разу**; иначе — вызвать пробу и решить по её коду выхода;
- **проба различает ТРИ исхода кодом выхода, а не текстом:** `0` — своя запись найдена; `1` — записи нет; `≥2` — перечитывание САМО отказало (сеть, `gh`, разбор). Третий исход обязателен: без него «не смог посмотреть» неотличимо от «не доехало», и это тот же дефект классом ниже.

Итог по исходам: `0` → `delivery_mark ok` + **названная строка в логе** о том, что квитанция потеряна, а запись на месте (человек, читающий прогон, должен это видеть, а не гадать); `1` → `failed`, как сегодня; `≥2` → `failed` + **`::error::` НАЗВАННОЙ строкой**, а не молчанием.

Там, где сегодня стоит `exit 1` (строки 764, 914, 928), он остаётся ровно на исходах `1` и `≥2`. На исходе `0` его нет: запись на месте — красить джобу не за что, и в этом вся правка.

### 3.2. Чем каждая доставка узнаёт СВОЮ запись

**Опознаётся ТОЛЬКО литералами, которые в теле уже есть.** Добавлять в тело маркер ради опознания нельзя — это смена формы сообщения, то есть норма (граница 3).

- **коммент вердикта (658)** и **оба канала итога (914, 928)** — тело несёт `RUN_ID` прогона (`Доставлено шагами прогона [\`%s\`]` в первом, `прогон [\`%s\`](%s)` в обоих ветвях итога). Литерал уникален на прогон, поэтому коммент второго круга за свой не примется. Проба: перечитать комменты PR (`gh api`/`gh pr view --json comments`) и искать `RUN_ID`;
- **письмо в тред (674, 928)** — тот же `RUN_ID` в теле; проба перечитывает ленту треда (`cli thread show … --ref origin/main --thread <id>`, чтением, ничего не записывая) и ищет его там. Учти задержку: письмо уезжает push'ем, и читать надо ту же `--ref`, куда пишет `deliver_to_thread`;
- **формальный review-статус (764) — исключение, и оно названо честно.** Его тело константно от круга к кругу (`«… доставка 3 из 3 …»`), `RUN_ID` в нём НЕТ, и добавить его туда нельзя по границе 3. Значит опознание здесь идёт по тройке: автор `github-actions`, состояние = выставляемому `$event`, `submitted_at` внутри окна прогона. **Слабое место назови в коде комментарием:** от вердикта предыдущего круга с тем же состоянием запись отделяет ОКНО, а не текст. Если сочтёшь, что окно ненадёжно, — не выдумывай маркер, а вернись ко мне письмом: это развилка постановки, а не деталь.

## 4. Проверяемость

- **юнит (`.github/scripts/review-delivery.test.sh`, поднимается сьютой через `src/roles/review-delivery.process.test.ts`) — четыре случая, все обязательны:**
  1. ненулевой выход клиента + проба нашла запись → `ok`;
  2. ненулевой выход + записи нет → `failed`;
  3. **проба отказала сама → `failed` НАЗВАННОЙ строкой**, а не молчанием (проверяется текстом вывода, а не только кодом);
  4. **нулевой выход клиента → перечитывания НЕ происходит — проверяется СЧЁТЧИКОМ вызовов стаба** (стаб инкрементит файл, тест сверяет `0`). Без счётчика первой границы машинно нет, и пункт не считается закрытым.

  Стаб пробы — обычная shell-функция, подставляемая в шов из §3.1; сети в этом файле по-прежнему нет, и появиться она не должна (шапка `review-delivery.test.sh` это прямо объявляет).
- **интеграционный (`review-delivery.integration.sh`)** — если проба треда естественно ложится на существующий временный чекаут почты, покрой её там; если нет — скажи в треде, что не покрыто и почему. Молчания здесь не надо.
- **живая приёмка человеком СОЗНАТЕЛЬНО не покрывается, и john это принял:** `502` по требованию не воспроизводится, а ждать второго случая — ровно тот полевой критерий, который жжёт такт за тактом. Перечисление §2 строго сильнее ожидания образца.
- **известное вырождение назови в тексте кода:** если запись ещё не видна из-за задержки репликации GitHub, проба вернёт `1` и доставка объявится несостоявшейся — то есть поведение станет сегодняшним. **Худший случай равен нынешнему; нового не заводится.**

## 5. Границы, за которые не выходить

- **повторного POST/push нет ни на одной ветви** — ни «на всякий случай», ни при `≥2`;
- **успешная ветвь без единого лишнего вызова** (см. счётчик);
- **форма сообщений не меняется** — ни поля, ни маркера, ни новой строки в теле вердикта;
- **`delivery_mark`/`delivery_state`/`delivery_summary`/`delivery_exit_code` сохраняют сегодняшний смысл**: правится ТО, ЧТО кладётся в `delivery_mark`, а не как читается итог;
- **чинятся все места из §2** (или четыре из пяти, если john снимет добавку), а не одно сработавшее;
- **найдёшь шестое место того же класса — назови его в треде и не чини молча:** предмет перечислен, и рост перечня — мой ход, а не твой.

## 6. Форма сдачи

Ветка → `pnpm protocol pr open` (не `gh pr create`) → описание несёт `thread: 122-lost-receipt-reads-as-lost-letter` и `role: dev-core` → зелёный `checks` на голове.

**Про круг ревью — назови в описании PR заранее:** PR правит `.github/workflows/claude-review.yml`, и автоматика такой PR не судит ПО ПОСТРОЕНИЮ (действие самопропускается, пока файл ветки не совпадает с дефолтной). Метку `review` на него **не вешать** — круг вернёт не вердикт, а пустой прогон. Гард 1 здесь закрывается человеческим чтением, гард 4 ведёт к кнопке john; про это в описании прямо и напиши, чтобы john не искал причину отсутствия вердикта.

В докладе назови своими словами: (а) что дифф **новой нормы не вводит**; (б) взял ли ты пятое место и почему; (в) что осталось непокрытым в интеграционном.

waiting-on → dev-core.
