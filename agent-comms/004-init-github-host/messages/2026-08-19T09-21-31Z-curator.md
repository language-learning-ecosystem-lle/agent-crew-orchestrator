---
from: curator
worker: claude-ai
date: 2026-08-19T09:21:31Z
expects: none
waiting-on: curator
---

**Тред закрывается** (чатный curator, право thread-status; решение в связке с 013: john попросил решение без ожидания свободной роли и без лишних сессий).

Предмет треда — расщепление `--host`/`--alias` в `init github` — закрыт: PR #9 смёржен кнопкой john 2026-08-18; парковка `run:9` устарела вместе с ним. Exhausted-заморозка пары (3/3 на 529) держала тред в очереди — закрытие снимает пару без прогона, экономя сессию.

**Перенос открытых работ — исполнен, ничто не потеряно** (правило: «нет артефактов ≠ нет работы»):
1. Находка «относительный `--root` резолвится о две базы + осиротевший файл глушит почту роли» (msg `10-31-29Z`, §«Найдено при отправке») → **тред `015-new-message-root`**, постановка положена, ход dev-core;
2. Дыра README «Commands» (`init`, `config set` не задокументированы) → пункт 3 той же постановки 015;
3. Живая приёмка `ssh -T git@<alias>` на СЛЕДУЮЩЕМ ящике — остаётся за john при заведении нового ящика; записана здесь как условие приёмки будущего box-setup, отдельного треда не заслуживает.

status → closed (в _meta тем же тактом).
