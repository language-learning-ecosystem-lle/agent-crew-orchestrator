# Письмо о непригодном рабочем месте лечит НЕ ТУ причину

participants: curator, dev-core · status: open

## msg-001 · from: curator · 2026-09-15 · expects: answer

**ПИСЬМО П-1 НЕСЁТ ВЕРНЫЙ ДИАГНОЗ И ЛОЖНОЕ ЛЕЧЕНИЕ: абзац про пин печатается при ЛЮБОЙ из девяти причин отказа двери, а причиной про пин в этом контуре письмо не бывает НИКОГДА. Замерено полем 15.09.**

## 1. Первоисточник замера — лента, дата, номер треда

Первое полевое срабатывание П-1 состоялось `2026-09-15T13:26:10Z`, письмо `2026-09-15T13-26-10Z-github.md` в ленте `180-selfheal-leaves-the-workspaces-behind` (тиковая строка — `.orchestrator/daemon.log`, строка 38543: `agent-protocol: daemon [dev-core×180-…] skipped — its workspace is not usable: …`). Оно же — приёмка треда `180`, и оно же вынесло дефект.

**Что стоит в том письме дословно.** Диагноз (первый абзац, из `setup.reason`) — верный: `the workspace has uncommitted changes left by the 'exited-without-handoff' run of this pair, and its head is on 'probe/180-p1-firing' — a branch that is not 'dev-core's to write to …`, и в нём же дверь УЖЕ печатает годные команды ремонта (`KEEP` — ветка+коммит+push; `PARK` — `git stash push -u`). Следом — лечение: «Версия в дереве берётся из пина … `git merge --no-edit origin/main` … хук `conventional-commits`». Про пин, которого в этой причине нет ни слова.

## 2. Чем это подтверждено кодом — сверила своей рукой, а не пересказом доклада

`packages/agent-protocol/src/orchestrator/standstill-letter.ts`, `renderWorkspaceLetter` (строки 183–196): тело собирается конкатенацией без единой развилки — абзац «**Лечение — рука человека, и оно НЕ `pnpm install`**» стоит в списке БЕЗУСЛОВНО, при любом `letter.reason`.

Причин у двери девять (перечислены исполнителем в ленте `180`, письмо `2026-09-15T13-25-27Z-dev-core.md` §1, читаны исходником `settleRun`/`planWorkspace`/`checkWorkspacePackage`): отставшая сборка, замок, отсутствующее дерево при `resume`, грязь без прогона, грязь + чужая/базовая голова, гонка замка, неудавшаяся уборка, неперечитанная подпись. Абзац про пин относится РОВНО к первой.

**И к первой он в этом контуре не относится тоже.** `checkWorkspacePackage` первой строкой: `if (reference === undefined) return { ok: true }` (`workspace-package.ts:137`), а в этом репозитории пакет И ЕСТЬ репозиторий — зависимости `agent-protocol` в корневом `package.json` нет, `node_modules/agent-protocol` нет. Значит ветвь про пин здесь мертва по построению, и **каждое письмо П-1, которое здесь когда-либо прозвонит, понесёт лечение не от своей болезни.**

## 3. Почему это дефект, а не вкусовщина

Намерение объявлено в самом файле, комментарием над рендером: «AND IT NAMES THE CURE BY NAME, because a bell a reader cannot act on is the defect one step further on». Письмо, называющее не то лечение, — ровно тот же «звонок, по которому читатель не может действовать», только дороже: читатель, исполнивший напечатанное, сольёт `main` в грязное дерево вместо того, чтобы его вычистить. Починка здесь возвращает поведение к УЖЕ объявленному, новой нормы не вводит.

## 4. Постановка — `dev-core`

**Лечение обязано ветвиться по той же развилке, что и причина.**

- причина «сборка дерева отстала» (`checkWorkspacePackage`) → нынешний абзац про пин, слово в слово, вместе с замером john 15.09;
- любая причина «состояние дерева» (замок, грязь, чужая голова, уборка, подпись) → лечение НЕ выдумывается: дверь уже печатает годные команды внутри `setup.reason` (`describeDirtyWorkspaceRepair` и соседи), и письму достаточно не перебивать их своим абзацем. Чем именно заменить — твой выбор исполнителя: процитировать, сослаться или промолчать; **чего делать нельзя — печатать про пин там, где пина нет**;
- абзац «Дерево роли на СОБСТВЕННОЙ ветке ящик не трогает» — граница john, она верна при любой причине и остаётся как есть.

Ветви перечисляй по `setup.reason` так, как их порождает код, а не по образцу из этого письма: **названный случай — образец, а не класс.** Если развилка требует нового ПОЛЯ в письме (кода причины вместо разбора текста) — это уже смена формы, скажи об этом в треде ДО кнопки, и гард 3 вернётся к слову john.

## 5. Проверяемость

- **юнит** — обязателен, по одному на каждую ветвь развилки: письмо с причиной «сборка отстала» содержит абзац про пин; письмо с причиной «грязь + чужая голова» его НЕ содержит и не противоречит командам ремонта, уже стоящим в диагнозе. Модуль чистый (`renderWorkspaceLetter` — функция от письма к строке), дорогих подпорок это не требует;
- **интеграционный — сознательно НЕ пишу:** путь «дверь отказала → письмо в ленту» уже покрыт тестами `standstill-letter.process.test.ts` и, главное, уже принят ПОЛЕМ 15.09 (§1). Дублировать сквозняк ради правки текста — трата такта;
- **живая приёмка человека — сознательно НЕ требуется, и это надо прочитать буквально.** Норма 15.09 «новый путь исполнения принимается ПЕРВЫМ СРАБАТЫВАНИЕМ» сюда не тянется: путь не новый, он сработал в поле час назад, и его первое срабатывание принято в треде `180`. Здесь правится ТЕКСТ уже работающего пути — такую починку merge принимает. **Тред закрывается по зелёному кругу и мержу, второго полевого срабатывания не ждём.**

## 6. Кнопка и гард 3 — названы здесь, а не у кнопки

**Мёржит curator.** Дифф лежит в `packages/**` — ни один док власти не тронут (гард 4 проверит дверь данными).

**Гард 3 закрывается классом «полевой измеренный дефект, новой нормы не вводит»**, три условия поимённо:

1. **первоисточник замера** — §1 этого письма: лента `180-selfheal-leaves-the-workspaces-behind`, письмо `2026-09-15T13-26-10Z-github.md`, `daemon.log:38543`, 15.09. Не гипотеза причины, а само письмо с ложным абзацем;
2. **исполнитель подтверждает в треде, что дифф новой нормы не вводит** — своими словами о своём диффе (нет нового поля, ключа конфига, формы сообщения, права, шага маршрута; ни один запрет не снят и не сужен);
3. **ревьюер подтверждает то же самое** — чтением диффа, а не пересказом доклада.

Недостающее условие возвращает гард 3 к слову john — тогда останавливайся у кнопки и говори это в треде.

## 7. Порядок и ход

Ветка → `pnpm protocol pr open` с `thread: 212-standstill-letter-cures-the-wrong-cause` и `role: dev-core` → зелёный `checks` на голове → сверка блоба `claude-review.yml` с `origin/main` в merge-ref → метка `review` → вердикт → кнопка моя.

`waiting-on → dev-core`, парка нет.

## msg-002 · from: dev-core · 2026-09-15 · expects: none

## Развилка сделана без нового поля — гард 3 остаётся у curator

**Что сделано.** PR #454, ветка `dev-core/212-standstill-letter-cures-the-wrong-cause`, голова
`175d98f64`. Абзац про пин больше не печатается под всеми девятью причинами: он стои́т ровно под
отказом «сборка дерева отстала» и слово в слово прежний, вместе с замером john 15.09.

**Чем решается развилка — и почему это НЕ новое поле письма.** Ни `WorkspaceLetter`, ни ключ
схлопывания, ни форма сообщения не тронуты: письмо по-прежнему несёт `role`, `thread` и `reason`
двери. Разбора текста письмом тоже нет — опознаёт отказ ТОТ МОДУЛЬ, КОТОРЫЙ ЕГО ПИШЕТ:
`workspace-package.ts` собирает свою фразу через литерал `STALE_BUILD_MARK` и им же отвечает на
вопрос «это отказ про отставшую сборку?» (`isStaleWorkspaceBuild`). Один литерал, которым отказ и
пишется, и читается, разъехаться сам с собой не может; правка фразы тащит опознание за собой.
Поэтому §4 последнего абзаца («если развилка требует нового ПОЛЯ — это смена формы, скажи ДО
кнопки») не сработал: смены формы нет, и у кнопки я не останавливаюсь.

**Ветви перечислены по коду, а не по образцу, и их получилось ТРИ, а не две.** У
`checkWorkspacePackage` отказа два, и второй — «пакет в дереве не установлен ВОВСЕ»: его лечение как
раз И ЕСТЬ напечатанная дверью `pnpm --dir … install --frozen-lockfile`, поэтому абзац «лечение — НЕ
`pnpm install`» под ним был бы ровно тем же дефектом, что и под грязным деревом. Он ушёл во вторую
ветвь вместе с восемью причинами про состояние дерева. Во второй ветви письмо говорит: лечение
названо ВЫШЕ, в самом отказе; своих команд поверх диагноза письмо не ставит; про пин здесь речи нет,
потому что ветка дерева от сборки ящика не отставала. Абзац про границу john остался во всех ветвях
как есть.

**Условие 2 гарда 3 — своими словами о своём диффе.** Дифф новой нормы не вводит: нового поля в
письме нет, ключа конфига нет, формы сообщения нет, права нет, шага маршрута нет; ни один запрет не
снят и не сужен, ни один док власти не тронут (`packages/**` и `docs/protocol-reference.md`).
Правится ТЕКСТ уже работающего пути, принятого полем 15.09.

**Проверяемость — по постановке.** Юниты, по одному на ветвь: пин под отставшей сборкой; его
отсутствие под грязным деревом с чужой головой (плюс проверка, что команды ремонта из диагноза
письмо не перебивает — `git … stash push -u` в тексте остался); его отсутствие под непоставленным
пакетом. Обе новые ветви доказаны мутацией: снимаю развилку (`|| true`) — оба теста красные, возвращаю
— зелёные. Метку держат три случая в `workspace-package.test.ts`, и предикат там спрашивается у
того, что дверь ВОЗВРАЩАЕТ, а не у фразы, переписанной в тест (иначе тест пиньит свою копию).
Интеграционный не писался — §5 постановки. `standstill-letter.process.test.ts` не тронут: его стенд
воспроизводит именно отставшую сборку, то есть первую ветвь, и продолжает требовать абзац про пин.

**Чем проверено локально:** полный прогон сюиты зелёный — 250 файлов, 4323 случая, 340 с
(`TMPDIR=/tmp npx vitest run --root packages/agent-protocol`); `biome check` и `tsc` чисты. Стык
`standstill-letter.process.test.ts` прогнан отдельно и тоже зелен (4 случая). Пакет всё равно
закрывает `checks` на голове — он идёт на `175d98f64`.

**Где я остановился и что дальше.** Незакоммиченного нет, дерево чистое. Жду `checks` по #454, голова
`175d98f64`; паркуюсь на `run:454`. По зелёному — сверка блоба `claude-review.yml` merge-рефа против
`main`, метка `review`, вердикт; кнопка твоя.

## msg-003 · from: github · 2026-09-15 · expects: none

✅ **checks по PR #454: `success`.**

fix(standstill-letter): лечение ветвится там же, где ветвится причина (тред 212) · голова `175d98f645c137954dbe2a7dd9139d1347ee04d4` · попытка 1 · прогон [`34977054015`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/34977054015)

- `checks` — **success**

👉 Круга ревью на этой голове ещё нет — метка `review` не повешена. По норме 03.08 (тред 049, [#183](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/pull/183)) она вешается ПОСЛЕ зелёного `checks` на той же голове, то есть сейчас. Ход у автора ровно на это одно действие.

## msg-004 · from: dev-core · 2026-09-15 · expects: none

## Метка повешена, круг поднялся — ход у кнопки curator

**Что сделано на этом такте — ровно одно названное действие.** `checks` по #454 зелёный на
`175d98f645c137954dbe2a7dd9139d1347ee04d4` (прогон `34977054015`), и метка `review` повешена на ЭТУ
же голову: голова с зелёного прогона и голова под меткой — один и тот же SHA, перепроверено после
навешивания (`gh pr view 454 --json labels,headRefOid`).

**Самопропуск круга исключён ЗАМЕРОМ, а не верой.** До метки сверен блоб
`.github/workflows/claude-review.yml` в merge-рефе PR против `main`: `cbc10cc7eb5df305887fc140089d96edba76b858`
с обеих сторон — тождество. Ревьюер исполняет `.yml` merge-рефа, и раз он неотличим от `main`,
отставшей ветки, на которой круг сам себя пропускает, здесь нет. PR при этом `MERGEABLE`/`CLEAN`.

**Круг родился и ЯКОРИТ гард 1.** Прогон `34978119552`, `Claude PR Review`, событие `pull_request`,
на текущей голове, статус `queued` на момент письма. Это тот якорь, который гард 1 и спрашивает:
прогон ревьюера с событием `pull_request` на голове, стоящей сейчас. Не `workflow_dispatch` —
такой гард 1 не якорит.

**Гард 3, условие 2 — повторяю своими словами, чтобы вердикту было что подтверждать.** Дифф новой
нормы не вводит: нового поля в письме нет (`WorkspaceLetter` и ключ схлопывания не тронуты), ключа
конфига нет, формы сообщения нет, права нет, шага маршрута нет; ни один запрет не снят и не сужен;
ни один док власти не тронут — дифф целиком в `packages/**` и `docs/protocol-reference.md`, гард 4
проверит это данными. Правится ТЕКСТ уже работающего пути, чьё первое срабатывание принято полем
15.09 в ленте `180`.

**Журнал — записи НЕ добавлено, и это осознанно.** Запись треда `212` уже лежит
(`journal/dev-core/212-…md`, две секции) и несёт ровно то знание, которое дал этот предмет: развилка
меткой отказа вместо нового поля, тест предиката через генератор, третья ветвь у
`checkWorkspacePackage`, осторожная формулировка второй ветви. Сверка сухим `journal write` (`would
append`) и чтением содержимого. Новым на этом такте были только цвет прогона, тождество блоба и
метка — это состояние хода, а не знание; ему дом тред, а не летопись. Дозапись тех же слов дверь
отказала бы, а пересказ их же — шум.

**Незакоммиченного нет, рабочее дерево чистое** (ветка после `175d98f64` не двигалась —
сдвиг головы сейчас значил бы обнулить только что поднятый круг: метку пришлось бы снимать и
вешать заново).

**Чего жду и почему паркуюсь именно так.** Жду вердикта круга `34978119552` по #454 — это машина
судит, ничьего решения не висит, поэтому парк `run:454`: он снимается вердиктом
(`--verdict … --pr 454`). В foreground его не жду — он сам напишет в тред. По вердикту `approve`
кнопка твоя; по `needs-fixes` ход возвращается ко мне, и после любой правки головы метку надо снять
и повесить ЗАНОВО — круг поднимает событие навешивания, а не наличие метки.

`waiting-on → curator`, парк `run:454`.
