# Стоячий адрес: демон перезапустил себя на новый код

participants: github, curator, john · status: closed

## msg-001 · from: github · 2026-09-08 · expects: none

## Демон перезапустил себя на новый код — без руки, и вот чего это стоило

Ящик ДОЖДАЛСЯ живых сессий — ни одна не была порвана, он пошёл только после того, как закрылась последняя, — починил своё дерево и поднялся на новом коде. Ход никому не нужен для ремонта — он уже сделан; это отчёт о нём, потому что тихий самоперезапуск ничем не лучше тихого дрейфа.

- **контур:** `/home/lle/projects/agent-crew-orchestrator`
- **какой код был:** `784f4d044a2d`
- **какой стал:** `46288a6bc8fb`
- **сколько отставал:** 1 коммит(ов)
- **сколько ждал сессии:** 4 мин (255 с)
- **когда пошёл:** 2026-09-08T14:23:47Z
- **что сменилось в исполняемом:** 6 путь(ей) — packages/agent-protocol/src/cli.ts, packages/agent-protocol/src/orchestrator/self-restart-letter.test.ts, packages/agent-protocol/src/orchestrator/self-restart-letter.ts, packages/agent-protocol/src/orchestrator/self-restart.process.test.ts, packages/agent-protocol/src/orchestrator/self-restart.test.ts, …

**Ход curator — ровно на одно действие:** прочитать это и, если отчёт полон, донести john. Ремонта здесь нет: дрейф уже закрыт, а звонок о дрейфе (тред 141, #301) на этот ящик больше не придёт.

## msg-002 · from: curator · 2026-09-08 · expects: ack

Ящик перезапустил СЕБЯ на новый код (`784f4d04` → `46288a6b`) без руки — отчёт полон, я его перемерил, решения нет, нужен только ack.

Это доставка john по требованию, записанному в самом механизме: «какой код был, какой стал, сколько отставал, сколько ждал сессию — и, если что-то пошло не так, чего не хватило; тихий самоперезапуск ничем не лучше тихого дрейфа». Отчёт `github` (msg-001) полон, и вот чем это замерено — не пересказом письма, а фактами рядом с ним.

## Четыре поля письма сходятся с записью, а запись — с журналом

- `.orchestrator/self-restart.json`: `from` `784f4d044a2d…`, `target` `46288a6bc8fb…`, `behind` 1, `drainSince` `2026-09-08T14:19:32Z`, `at` `2026-09-08T14:23:47Z`. Разность — ровно 255 с, то есть «4 мин (255 с)» письма это не округление, а вычитание.
- `.orchestrator/daemon.log` несёт ТРОЙКУ строк пути ремонта, которой нет ни у чего другого: `SELF-RESTART: the loaded code is behind 46288a6b (1 commit(s)), leases 0, state clean — running 'restart --pull' … (attempt 1/2)` → `SELF-RESTART: git pull --ff-only — ok` (7 files changed, 379 insertions) → `SELF-RESTART: the tree is on 46288a6b — leaving with code 75`. Установка пропущена осознанно и сказано почему: ни один из трёх манифестов в диффе не двигался.
- **Ни одна сессия не порвана, и это видно поимённо.** Четыре минуты тиков `DRAINING TO RESTART` называли живую сессию — `curator/173-daemon-self-restart`, мою же предыдущую, — и каждый такой тик поднимал НОЛЬ пар. Ход по ремонту пошёл на первом тике после её закрытия.

## Отдельная проверка, которой в письме быть не может: ящик ИСПОЛНЯЕТ новый код

Смёрженное — не работающее, поэтому мерил процессом, а не деревом: `pid 966363` поднят `2026-09-08T14:23:58Z` — через 11 с после хода, — его argv это `packages/agent-protocol/src/cli.ts` ЭТОГО чекаута, `HEAD` чекаута `46288a6bc8fb…`, дерево чистое. То есть супервизор действительно поднял замену, и замена работает на новом коде, а не просто держит его на диске.

## Ящик перезапустился ради КОДА, а не ради доков — сужение треда 161 сработало в поле

В диффе `784f4d04..46288a6b` семь путей, письмо называет шесть: седьмой — `docs/protocol-reference.md`, и он отброшен как неисполняемый. Ровно тот полевой случай, ради которого сужение писалось (соседний контур слил живую сессию ради 64 строк markdown), только с обратным знаком: здесь под фильтром остался настоящий код, и письмо ушло правильно.

## Почему это письмо — первое своего рода

Оно написано УЖЕ НОВЫМ кодом: преемник поднялся на `46288a6b` и сам себя в записи опознал. А `46288a6b` — это #342 треда 173, влитый в `14:19:08Z`, то есть за 24 секунды до того, как ящик заметил дрейф и начал слив.

#342 ввёл ветку «ремонт не записан»: заголовок «Демон исполняет новый код — но самоперезапуска за ним не записано», строку «когда пошёл: не записано — хода по пути ремонта в памяти нет» и прямое предупреждение «совпал только SHA». Это письмо пошло по ЗАПИСАННОЙ ветке во всех четырёх местах сразу — заголовок, фраза о сливе, «сколько ждал сессии», «когда пошёл». Значит, ход по пути ремонта отметил себя в памяти сам, и слова «перезапустил СЕБЯ, без руки» опираются на записанный ход, а не на совпадение SHA, которым кончается и подъём чужой рукой.

**Починка треда 173 подтверждена в поле на своём первом же событии, через час после мёржа, без стенда и без чьей-либо руки.**

## Чего в письме нет (мелочь, называю, чтобы не считалось замолчанным)

Имя шестого пути обрезано многоточием (это `packages/agent-protocol/src/orchestrator/self-restart.ts` — потолок на число имён), и письмо не говорит, что седьмой путь был отброшен: это читается только словом «в исполняемом». Дефектом не считаю, постановки не завожу — но если тебе в таком письме нужна строка «и ещё N путей отброшено как неисполняемые», это одна задача dev-core, скажи слово.

## Цена

4 мин 15 с, в которые ящик не поднимал никого. Пары из очереди не потрачены — тики говорят это прямо, преемник читает их заново.

## Почему приёмник новый (175, а не 173) и что будет дальше

Стоячий адрес берёт приёмник, который открыт И НЕ запаркован; 173 был запаркован на тебе в `14:21:31Z` — за две минуты до хода, — поэтому доставка открыла 175. Это объявленное поведение, не дефект: письмо в запаркованный тред приняли бы и не подняли бы никого.

Следствие, которое стоит знать: я паркую и этот приёмник на тебя (иначе тебе никто не звонит), значит следующий самоперезапуск откроет 176. Приёмников этого адреса за сегодня уже три — 161, 173, 175, — и все три стоят на тебе. Прибраться здесь дёшево и это моё право (`thread-status`): скажи слово — закрою отработавшие приёмники, и адрес перестанет разрастаться.

**Решения john здесь нет — это отчёт.** Парк на тебе стоит ровно потому, что звонок в этом контуре есть только у парка; ход остаётся у curator.

## msg-003 · from: curator · 2026-09-08 · expects: none

**Слово john (чат 2026-09-08 ~14:32Z). Ответ на ЭТОТ адрес дан общим письмом в `173` — два адреса (`173`, `175`) закрываются одним словом.**

- **ack принят:** ящик перезапустил себя на новый код без руки, отчёт перемерен ролью. Приёмка самоперезапуска подтверждается полем и второй раз не спрашивается;
- **ложный отчёт (`173`) починен** — #342 в `main`; роль нашла ошибку в собственном письме, перемерив его. Третий такой случай за сутки;
- **поток писем законен, пока резы везут настоящий код:** оба сегодняшних перезапуска пришли на резах с исполняемыми правками (#341, #342). Сужение письма влито и накатано (`0.2.13`, `~11:53Z`) — если письмо придёт на перезапуске БЕЗ смены исполняемого, это новый предмет с новым замером.

**Закрывай этот адрес.**

waiting-on → curator.

## msg-004 · from: curator · 2026-09-09 · expects: none

**ПАРК СНЯТ — он отпал вчера. Ack john дан общим письмом в `173` (`2026-09-08 14:32Z`), и туда же продублирован указатель на этот адрес.**

Парк остался стоять по той же причине, что и у четырёх других сегодня: чатный curator доставил ответ полем `delivers`, а с #339 это снятием не является.

## Содержание ответа — чтобы адрес не требовал перехода

- **ack принят:** ящик перезапустил себя на новый код без руки, отчёт перемерен ролью. Приёмка самоперезапуска подтверждается полем и второй раз не спрашивается;
- **сужение письма работает, и теперь это измерено:** в контуре-потребителе `n=12` за пять часов, письмо ушло **одно** — ровно то, у которого сдвинулся отпечаток установки; одиннадцать удержаны по имени. Метод — непрерывная цепь пар `from..to` без разрывов;
- **и найдена неточность формулировки:** строка «что сменилось в исполняемом» обгоняет измерение — критерий смотрит на пути, чья правка МОГЛА БЫ сменить установленную копию. Находка увезена в `161` сегодня (`12:01:52Z`), починка — одна строка текста, критерий не трогать.

**Закрывай этот адрес.**

waiting-on → curator, парк снят.

## msg-005 · from: curator · 2026-09-09 · expects: none

**Адрес закрывается по слову john (msg-003, чат 2026-09-08 ~14:32Z: «Закрывай этот адрес»). Своего предмета у 175 не осталось — но перед закрытием я перемерила ЕДИНСТВЕННЫЙ предмет, который этот адрес отправил наружу, и он лежал в мёртвом канале. Починено.**

## 1. Что было в воздухе у 175 и чего не было

Ход пришёл сюда потому, что парк снят (msg-004), `waiting-on → curator`, а тред остался открыт. Лента прочитана целиком: ack john дан, приёмка самоперезапуска подтверждена полем, ремонта здесь нет. Одно действие — закрытие, ровно как названо в msg-003 и msg-004.

Соседние приёмники этого слага сверены: `161` и `173` стоят `closed`, `175` был последним открытым из объявленных здесь; `176` открыт и живёт своей жизнью — его я не трогаю.

## 2. Находка, из-за которой закрытие не было однокомандным: хвост 175 лежал в ЗАКРЫТОМ треде

msg-004 объявил, что находка о формулировке письма («что сменилось в исполняемом» обгоняет измерение) **увезена в `161` сегодня в `12:01:52Z`**. Это и было основанием считать хвост 175 пристроенным. Замер говорит другое:

- **`161` был закрыт `2026-09-08T15:40:33Z`** — коммит `90d9463b` в `origin/comms` по `161-daemon-self-restart/_meta.md`;
- **письмо с находкой легло `2026-09-09T12:01:52Z`** — на 20 часов ПОЗЖЕ закрытия, с `--expects answer` и `waiting-on → dev-core`;
- **закрытый тред не поднимает никого, и это не мнение, а построение кода:** `waitingOnOf` возвращает `undefined` на `status: closed` ДО чтения любых объявлений — `packages/agent-protocol/src/thread/receiver.ts:17`, то же сказано у соседних категорий в `packages/agent-protocol/src/thread/index-doc.ts:387-388`.

То есть дверь письмо приняла, `waiting-on → dev-core` записала, и **не позвала никого**. Предмет не «уехал в 161» — он лёг в мёртвый канал и тихо протухал.

**Предмет при этом настоящий и НЕ починен:** на `origin/main` строка стоит как стояла — `packages/agent-protocol/src/orchestrator/self-restart-letter.ts:278`, `«что сменилось в исполняемом: N путь(ей)»`. Ни один из четырёх открытых PR (#351, #350, #349, #346) её не трогает. Дубля находки в почте нет: греп по `«отпечаток установки»` по всему дереву почты даёт ровно два файла — письмо в `161` и msg-004 здесь, то есть закрытый тред и этот, закрываемый.

## 3. Починка — своей рукой, правом `thread-status`, без единого слова в чужую ленту

`161` **переоткрыт**: `thread status … --thread 161-daemon-self-restart --status open --write` → `161-daemon-self-restart/_meta.md committed and pushed to origin/comms`.

Контрольное чтение (ответ инструмента ≠ факт): `cli mail --role dev-core` теперь печатает `177-workspace-per-pair`, **`161-daemon-self-restart`**, `180-notifier-down` — предмет снова в очереди у того, кому его и адресовал автор письма.

Почему именно переоткрытие, а не новый тред: постановка уже написана и лежит в ленте `161` своим файлом (`2026-09-09T12-01-52Z-curator.md`) с названной починкой («одна строка формулировки, критерий не трогать»). Переоткрытие восстанавливает объявленный автором канал и не плодит копию текста; в ленту `161` я не написала ни слова — там не мой ход. Если следующая сессия сочтёт, что предмету нужен свой узкий тред (лента `161` длинная и разнотемная), это её право — предмет от этого не потеряется, он в очереди.

## 4. Класс, который здесь виден и который НЕ мой такт

Замерено в поле: **`new-message` принимает письмо в ЗАКРЫТЫЙ тред, пишет `waiting-on` и молчит о том, что подъёма не будет.** Автор письма получает «доставлено», а адресат не получает ничего — расхождение видно только тому, кто отдельно сверит `status:` треда. Это не про 175: свой предмет здесь исчерпан, а класс требует своего треда и своего замера. Кладу фактом для того, кто его возьмёт; постановки отсюда не делаю и своей рукой класс не веду — этот прогон связан одним тредом.

## 5. Хвост merge — назван, не тронут

`gh pr list --state open` → четыре: #351 (тред 180), #350 (тред 143), #349 (тред 178), #346 (тред 177). Двух из них автор — curator. **Ни одного не мёржу и меток не вешаю:** след гарда 5 обязан лечь в ленту треда-владельца, а этот прогон связан ровно тредом 175 — писать в 143/178 значило бы взять чужую почту. Кандидаты остаются как есть, чтобы их взял свой тред.

## 6. Состояние дерева

В `.worktrees/curator` не правила ни одного файла — такт целиком чтение, один флип статуса и это письмо; незакоммиченного за собой не оставляю. Тело письма — в настоящем `/tmp` (`/tmp/aco-175-gHoGGw/`), не в `TMPDIR` сессии.

**Адрес закрыт. Ход не нужен никому: `waiting-on → —`.** Живое из этого треда уехало не прозой, а очередью: `161` открыт и ждёт `dev-core`.
