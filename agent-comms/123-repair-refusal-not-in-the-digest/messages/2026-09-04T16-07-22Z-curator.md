---
from: curator
worker: claude-code
session: a2020d28-7b25-4afc-9c9c-122c92cc0dea
raised: 2026-09-04T16:00:50Z
date: 2026-09-04T16:07:22Z
expects: none
waiting-on: curator
---

**Кнопка john нажата: #270 в `main` (`ac22d87b5c7432c759204c7e2a784a2841c87294`, `mergedAt` 15:13:08Z, `baseRefName: main`). Обобщение принято на каноне ДВУМЯ независимыми проверками, и вторая — живая: норма видна ТАМ, ГДЕ ПРИМЕНЯЕТСЯ, и это померено, а не выведено. Предмет треда исчерпан целиком — этим ходом я его закрываю.**

## 1. Приёмка #270 — фактами, и вторая дверь названа отдельно

**Контрольное чтение цели** (ответ инструмента ≠ факт): `gh pr view 270` → `state: MERGED`, `baseRefName: main`, `mergeCommit: ac22d87b5c7432c759204c7e2a784a2841c87294`, `mergedAt 2026-09-04T15:13:08Z`, голова при merge — `461d5b31fcbf369638130dd7b31a1a1b8d653f0f`, та самая, на которой лежал `approve`. **Следа гарда 5 от меня по #270 нет и не должно быть** — merge не мой; факт merge несёт письмо `github` в этой ленте.

**Проверка 1 — текст на КАНОНЕ, читан `git show origin/main:<путь>`, не по докладу и не по своей ветке:**

- `PROTOCOL.md`, «Кто мёржит», абзац **«Чего гард 3 НЕ требует: публикация ПРИЧИНЫ внутри уже объявленного класса дайджеста — починка ВСЕГДА»** со ссылкой на слово john 2026-09-04 и тред `123`; четыре границы идут сплошным абзацем и все четыре на месте;
- `docs/roles/curator.md`, раздел «Merge — право с гардами», гард 3 — тот же абзац с четырьмя границами по пунктам и ссылкой на `PROTOCOL.md` как на целое;
- **находка первого круга держится на каноне:** граница 1 в ОБОИХ файлах несёт дословное «по-прежнему норма и по-прежнему **слово john**». То самое расползание между двумя каноническими текстами, из-за которого ревьюер вернул `needs-fixes`, в `main` не уехало.

**Проверка 2 — `checks` на самом merge-коммите:** прогон **`33888211222`**, воркфлоу `checks`, событие `push`, голова `ac22d87b…`, `conclusion: success`, 15:13:10Z. Полная сюита на слитом дереве в почве раннера.

**Проверка 3 (живая, и ради неё абзац вообще клали в карточку) — норма ВИДНА ТАМ, ГДЕ ПРИМЕНЯЕТСЯ.** Довод исполнения был такой: норма, положенная лишь в `PROTOCOL.md`, невидима в промпте поднимаемой сессии. Сегодня это перестало быть доводом и стало замером:

- **эта сессия (поднята 16:00:50Z, то есть через 47 минут после merge) читает абзац в СВОЁМ промпте** — четыре границы поимённо, дословно как на каноне;
- **и я померила, ИЗ КАКОГО файла он туда попал, а не предположила.** `grep -c "публикация ПРИЧИНЫ внутри уже"`: в рабочем файле ГЛАВНОГО чекаута (`/home/lle/projects/agent-crew-orchestrator`, `HEAD` = `4c048654`, то есть ДО #270) — **0**; в файле рабочего дерева роли (`.worktrees/curator`, `HEAD` = `80908d3e`) — **1**. Промпт роли рендерится из дерева РОЛИ на каноне, а не из чекаута демона, и потому норма доехала до места применения на первом же подъёме — независимо от того, догнал ли канон демон (§2 показывает, что не догнал).

Три проверки независимы: первая — содержание, вторая — чужой прогон в правильной почве, третья — доставка нормы потребителю. Ни одна не выведена из другой.

## 2. Живой замер машинерии, которую чинил #267 — половина, и я говорю, какая именно

На ящике ПРЯМО СЕЙЧАС существует `.orchestrator/daemon-drift.json` (снят 16:02Z):

```
refSha 80908d3e…, sha 4c048654…, ref origin/main, behind 3, since 2026-09-04T18:13:07+03:00,
why "no self-restart while sessions are live (dev-core, curator, dev-core/072-…, curator/123-…)
     — a graceful restart would wait for them, and that wait needs a human", at 16:02:08Z
```

**Что этим доказано:**

- **демон исполняет код #267** — `daemon-code.json`: `sha` = `4c048654dba97524d3936f400688f10fcebccfbb`, то есть squash самого #267, `checkout` — главный, `pid` 2982776. Смёрженное здесь равно работающему, и это прочитано файлом, а не выведено из merge;
- **общий писатель `publishDriftStandoff`, который #267 и завёл, публикует на живом ящике**, и опубликованное содержит ровно пять полей стенда плюс `why` — то есть формат, который читает курьер, на боевом выходе держится.

**Чего этим НЕ доказано, и я не выдаю одно за другое:** это ветвь **«до ремонта не дошли»** (`describeSelfRestartBlock`) — та, что публиковалась и до #267. Новая ветвь — «ремонт ОТКАЗАЛ» — на живом ящике сегодня не отработала: чтобы она отработала, самопочинка должна дойти до ремонта и на нём споткнуться, а здесь она до ремонта не идёт вовсе (живые сессии). Полевая приёмка новой ветви по-прежнему не сделана, ровно как сказано в прошлом письме, и ждать её образцом мы сознательно не будем.

Отдельно называю сам факт отставания, чтобы он не читался как авария: демон на 3 коммита позади канона (`4c048654` против `80908d3e`) и не перезапускается, пока живы сессии, — это объявленное поведение, названное своими словами в файле, который прочтёт курьер. Предметом этого треда оно не является.

## 3. Соседний предмет «локальная сюита краснеет из `.worktrees/`» — обещание прошлого хода исполнено

Прошлый ход оставил его названным и сказал: сначала пусть доедет #264, потом мерить, остался ли предмет. **#264 доехал** (`15e2a96b`, тред 120, «зонд объемлющего репозитория отказывает по имени»), и замер сделан этим ходом — тем же инструментом и в той же почве, что мерил в прошлый раз (`.worktrees/curator`, канон `80908d3e`), по ИМЕНАМ тех девяти файлов, что краснели:

- **семь файлов** (`fs/git.test.ts`, `testing/tmp-base.test.ts`, `orchestrator/{home,instance-flag,restart,run,systemd-install}.process.test.ts`) — **115 passed, 0 failed, exit 0**, 190 с;
- **два оставшихся** (`merge/gate.process.test.ts`, `read-under-sandbox.process.test.ts` — их имена я в прошлом письме сократила, и полные пути пришлось найти `find`ом, а не вспомнить) — **42 passed, 0 failed, exit 0**, 91 с;
- итого **9 файлов / 157 тестов / ноль красных** там, где 2026-09-04 краснели все девять (контроль на чистой базе `55b1ca48` тогда дал `16 failed`).

**Что этим измерено и что — вывод.** Измерено: класс, чувствительный к ОБЪЕМЛЮЩЕМУ репозиторию, из почвы `.worktrees/<роль>` на каноне `80908d3e` зелен целиком. Вывод (называю выводом, а не фактом): причина — #264, потому что его предмет и есть зонд объемлющего репозитория, а красным был ровно этот класс; в дереве между замерами лежат ещё #265 и #270, и по построению ни один из них к почве отношения не имеет (#270 — доки, #265 — парковочная дверь).

**Отсюда: отдельного дома соседнему предмету НЕ НУЖНО, и он закрывается не молчанием, а этим замером.** Предмет был «локальная сюита из `.worktrees/` краснеет на ЧИСТОМ `main`, тогда как CI на той же базе зелен»; после #264 она не краснеет. Инструмент, который в прошлом ходу был показан негодным, годен снова — и это ровно тот случай, когда «почву выбирают замером на заведомо зелёном дереве, а не цитатой из прошлого письма».

## 4. Хвост открытых PR — прогнан и назван

`gh pr list --state open` на 16:03Z: **#271** (тред `124`), **#272** (тред `125`), **#273** (тред `126`) — все принадлежат ЧУЖИМ тредам, и след гарда 5 по ним обязан лечь в их ленты, а этот прогон связан одним `123`: писать туда нечем. Список прогнан, назван, молчанием не оставлен.

**Своих кандидатов у меня не осталось ни одного.** За окно между прошлым и этим ходом из хвоста ушли #265 (тред 072) и #264 (тред 120) — их домержили такты их собственных тредов, ровно как прошлое письмо и предписывало.

## 5. Тред закрывается этим ходом — приёмка, а не уборка

Закрытие — третья дверь почты и оно же приёмка, поэтому исход назван ВЫШЕ, в ленте, а не в самом закрытии: доложить его закрытым тредом было бы некому.

- **предмет заведён** (msg-001): отказ самопочинки виден только в логе, `daemon-drift.json` о нём молчит;
- **предмет исполнен** — #267 в `main` (`4c048654`), влит моей рукой по гардам 1/2/4 и слову john на гарде 3, принят перечислением фактов в прошлом письме;
- **и куплено большее, чем предмет** — обобщение #270: тот же вопрос больше не придёт к john на каждом таком диффе, при условии, что все четыре границы держатся;
- **незакоммиченного нет:** рабочее дерево `.worktrees/curator` чистое, своей рукой кода в этом ходу не написано ни строки, тело письма — в `mktemp -d -p /tmp` вне любого чекаута (машинно: `rev-parse` оттуда — `not a git repository`).

**Что этот тред НЕ уносит с собой в закрытие** — ровно один пункт, и он не осиротеет: полевая приёмка новой ветви (§2) не сделана и сделана быть не может по требованию; её цена названа заранее постановкой (msg-001 §4) и вывешена там же как сознательно непокрытая. Заводить под ожидание образца отдельный дом — ровно та приёмка, что жжёт слот каждый такт и не наступает.

waiting-on → curator.

