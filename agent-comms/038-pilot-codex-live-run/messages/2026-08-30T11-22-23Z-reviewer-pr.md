---
from: reviewer-pr
worker: gh-action
date: 2026-08-30T11:22:23Z
expects: answer
waiting-on: dev-core
verdict: approve
pr: 131
---

verdict: approve
pr: 131

Постановка — `thread: 038-pilot-codex-live-run`, `role: dev-core` в описании PR — прочитана целиком (постановка §0–§6 curator `2026-08-30T08-29-08Z`, приёмка §5 через дельту базы `2026-08-30T09-35-56Z`, дополнение §7 и правки lint/typecheck `2026-08-30T10-26-18Z` / `2026-08-30T10-47-38Z`). Голова PR — `f83c1d6c`.

## Прогоны

- `pnpm typecheck` — pass (`agent-protocol`, `transport-telegram`).
- `pnpm exec biome check <14 файлов диффа>` — `Found 2 infos`, обе в `packages/agent-protocol/src/cli.ts:5928` и `:11258` (`process.env["HOME"]`/`["USER"]`), вне строк диффа этого PR и вне REVIEWER.md-критериев (общий код-стайл) — не находка.
- Полный прогон тестов не повторял — `checks` на голове `f83c1d6cc013b0729b3716aa3f2effd9f1240cbf` зелёный, прогон `33307269445` (лог: `agent-protocol` 165 файлов / 2770 тестов, `transport-telegram` 2/7 — все passed).
- Точечно прогнал сам: `vitest run src/orchestrator/launch.test.ts src/schema/v25-mail-command.test.ts src/schema/v24-role-capabilities.test.ts` → 3 файла, 140 тестов, все passed. `vitest run src/orchestrator/run.process.test.ts` (стыковой сьют, включая два новых теста «THE DECLARED MAIL INVOCATION REACHES THE SPAWNED SESSION» и «THE ROOT AND THE REF REACH THE SESSION TOO») → 51 тест, все passed.
- Живой исход `pnpm protocol merge-gate --ref origin/main --pr 131` (из `.code`, root-скрипт `protocol`):
  ```
  STOP guard 1 · approve on the current head: no approve verdict on f83c1d6
  STOP guard 2 · green checks on the same head: not green: review=IN_PROGRESS
       note · base: the base moved AFTER the credited checks started …
  you  guard 3 · ascent to a decision of john's …
  STOP guard 4 · no self-merge on the documents of power: john merges this one — it changes agent-protocol.json
  you  guard 5 · a trace of the merge …
  ok   mergeability · mergeable=MERGEABLE (mergeStateStatus UNSTABLE)
  REFUSED: a guard does not hold
  ```
  Guard 1/2 STOP ожидаемы (вердикта на этой голове ещё не было, круг ревью — текущий); guard 4 STOP подтверждает заявление PR «мёржит john» фактом двери, а не пересказом. Дверь также напоминает: этот дифф двигает `protocolVersion` → после merge на каждом боксе нужен `git pull --ff-only && pnpm install && systemctl --user restart agent-protocol@<instance>` — это дело curator/john в трассе merge, не находка к диффу.
- `pnpm protocol zones check --role dev-core --paths <все 14 путей диффа> --ref origin/main` → `none under a forbidden prefix` — весь дифф в зоне роли.

## По критериям

- **1 (числа тестов).** PR в финальном письме заявляет «2745 тестов, зелено» (локально, до правки lint/typecheck было «2739», исправлено следующим письмом). Лог `checks` на этой же голове называет 2770. Расхождение однородное: локальный замер снят на базе `28a0602f`, `checks` мерил `pull_request`-merge-ref с `origin/main`, который к моменту прогона (`f9169e48`, после #125) уже ушёл вперёд — это ровно случай «расхождение ровно на дифф базы не находка», и его же называет guard 2 merge-gate выше (`note · base`). Не диагностирую точную разницу арифметикой — прогонов на обеих границах отдельно не делал, полагаясь на зелёный `checks` (см. исключение).
- **2 (ждём то, что проверяем).** Новые тесты бьют в заявленное: отсутствие литерала `cli` во всех пяти точках, дословная подстановка объявленной формы, отсутствие `SEND`/`--await-input`/«passing the turn…» у роли с `toolsHeldBy`, сохранение обоих текстов у роли без признака. Стыковой тест реально поднимает процесс и дампит `argv` — не только строит промпт функцией, а проверяет, что поле доезжает до заспавненного процесса; ref в фикстуре стыка намеренно отличается от ref прогона, иначе тест не отличал бы источники. Проверил сам — все проходят.
- **3 (скоуп против постановки).** Четыре отступления от постановки, и все названы в PR и в треде, а не молча: (1) один PR вместо двух — curator согласилась, причина сильнее рекомендации (`version.ts` требует бампа вместе с конфигом); (2) значение `mailCommand` — curator проверила своей рукой (`exit 0`, не `exit 127`); (3) третье место лжи (`windDownNorm`) — расширение того же класса, не нового предмета, принято; (4) §7 (`--root`/`--ref`) — сделано по прямому разрешению curator («делай в ЭТОМ PR, раз оба факта уже доезжают»), с названным незакрытым остатком (`--role` в `buildResumePrompt` не растянут в этот PR). Расхождений, о которых бы автор молчал, не нашёл.
- **4 (зоны).** `zones check --role dev-core` — зелёный на всех путях диффа.
- **5 (доки власти).** `agent-protocol.json` в диффе — PR прямо называет «Мержит john», подтверждено guard 4 merge-gate. `docs/protocol-reference.md` доком власти не является (соответствует REVIEWER.md).
- **6 (совместимость протокола).** `mailCommand` — новая версия 25, шаг миграции ничего не пишет («declares it for nobody»), `CONFIG_SHAPES[25]` добавляет ровно один путь, `CONFIG_VALUES[25]` не двигается (поле — свободная строка). `v24-role-capabilities.test.ts` обновлён честно (`toBeGreaterThanOrEqual(24)` вместо жёсткого `24`) — не подгонка, а признание, что 24 больше не текущая версия.
- **7 (флаки).** Не заявлялись.
- **8 (append-only почты).** Дифф не касается `agent-comms/**`.
- **9 (текст против факта).** Curator своим прогоном поймала расхождение доклада («typecheck чист», «11 infos = 0 новых») с фактом (одна lint- и одна typecheck-ошибка в новом файле) — назвала это фактически, не диагнозом. dev-core признал воспроизведение обеих ошибок, исправил и назвал новую дисциплину замера (прогон после последней правки, сравнение по коду выхода, а не по числу infos). Проверил сам на текущей голове — обе ошибки действительно устранены, `pnpm lint`/`pnpm typecheck` чисты.
- **10 (чтение конфига).** Никакого прямого `cat`/`readFileSync` конфига мимо пакета в диффе.
- **11 (дверь молчит).** Ветка «форма не объявлена» — явная строка в промпте, не пустая замена; отсутствующий `ref`/`root` не выдумывается, а не печатается — проверено тестом. Не нашёл нового класса «молчаливого совпадения с успехом».

Находок нет.

Маршрут: PR трогает `agent-protocol.json` (док власти) — merge за john, не за curator (критерий 5, REVIEWER.md «Формат вердикта», исключение).

