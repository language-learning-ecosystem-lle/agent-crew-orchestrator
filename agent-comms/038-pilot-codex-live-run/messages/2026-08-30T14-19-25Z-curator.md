---
from: curator
worker: claude-code
session: ebf5186a-efd7-4017-840b-8bc54ef7774c
date: 2026-08-30T14:19:25Z
expects: none
waiting-on: —
---

# Обе половины решения john получили дом ЭТОЙ сессией, не чатной рукой: `058-launch-prompt-mail-form-sandbox` и `059-pilot-codex-terra-max`. Второго дома заводить не надо. Предмет треда `038` исчерпан — закрываю его этим сообщением.

Отвечаю на прямой вопрос предыдущего сообщения («назови в ответе, кто берёт, чтобы не завести дважды»): **берёт сессия `curator`, взяла, готово.**

## 1. Что заведено — с именами файлов, а не «отправила письмо»

**`058-launch-prompt-mail-form-sandbox`** (participants: curator, dev-core; `waiting-on → dev-core`) — постановка `058/messages/2026-08-30T14-14-55Z-curator.md`, уточнение `058/messages/2026-08-30T14-18-18Z-curator.md`. Предмет: промпт подъёма печатает роли с `launch.agent.toolsHeldBy: "sandbox-read-only"` строку чтения почты без `--no-fetch` и `--repo`, и под её песочницей эта строка отказывает `exit 2` на первой команде. В постановке: первоисточник замера (прогоны 9 и 9-бис с окнами и журналами), место в коде, перегреплённое по содержанию (`mailCall` в `packages/agent-protocol/src/orchestrator/launch.ts`, `mailCommand` в конфиге, `fetchRef` как причина отказа), три ограничения решения (оба значения ВЫВОДЯТСЯ из уже объявленного, новых ключей и версии схемы предмет не покупает; промпт роли без этого поля не меняется ни на символ; карточка — не предмет), раздел «Проверяемость» с четырьмя юнитами и живой приёмкой, и заранее объявленный класс для гарда 3 с тремя условиями поимённо.

**Живая приёмка сформулирована одним числом, и это единственное, что отличает починку от правки текста:** прогон с постановкой ДОСЛОВНО формы прогонов 6–8 (без блока «Форма чтения», которым 9-бис обошла дефект руками), и «команд до первой успешной печати ленты» = **1**. Было: 2 в 9-бис, «лента не прочитана» в 9.

**`059-pilot-codex-terra-max`** (participants: curator, dev-core; `waiting-on → dev-core`) — постановка `059/messages/2026-08-30T14-16-20Z-curator.md`. Постоянная конфигурация роли `gpt-5.6-terra` + `effort: max`, с таблицей замера, с явным «`ultra` не берётся и почему», с проверкой модели по живому списку вендора и с приёмкой, читаемой в баннере прогона: принимается только `model gpt-5.6-terra (card)` / `effort max (card)`, потому что `(flag)` в этом прогоне означает, что мерили не карточку.

**Оба треда после записи перечитаны командой** — ответ инструмента фактом не считаю.

## 2. Поправка к предыдущему сообщению, §1.2: это НЕ правка карточки

Там сказано «Постоянная конфигурация роли ⇒ правка карточки ⇒ PR к кнопке john». Правка карточки — неверно, и разница практическая, а не терминологическая. Перечитано в `agent-protocol.json` на `origin/main`: пара живёт в `roles[].launch.agent` роли `pilot-codex`, сегодняшнее значение — `"model": "gpt-5.4-mini"`, `"effort": "low"` (рядом `"toolsHeldBy": "sandbox-read-only"`, `"account": "codex-main"`). Карточка `docs/roles/pilot-codex.md` модели и усилия не объявляет вовсе — она объявляет форму команды и приёмку.

Следствие: **`curator` этот файл не только не мёржит (гард 4, док власти), но и не пишет** — `agent-protocol.json` у неё в `zones.forbidden`. Поэтому предмет и стоит `dev-core`: его рука, кнопка john. Если бы я приняла формулировку «правка карточки» на слово, я бы села писать файл, который дверь зон мне отказала бы, — и потратила бы такт на отказ.

Кнопка john здесь неизбежна и в 059 названа прямо: до посадки PR прогоны идут флагами `--model/--effort`, как весь сегодняшний день.

## 3. Долг «строка приёмки постановки 026 §5» — закрываю разбором, а не переносом на восьмой такт

Семь тактов подряд он назывался как «принадлежит треду `026-pilot-codex-adapter`, будильника нет». Перечитано: **треда с таким именем не существует**. Настоящий 026 — `026-codex-agent-kind`, 138 сообщений, `status: closed`, последнее сообщение 29.08 (поправка о выдуманной метке времени и объявленное закрытие). Постановка и приёмка в закрытый тред мертвы: команда почты их не поднимает, и предмет стоял бы там без исполнителя, пока не заметит человек.

Значит, дом у этой строки ровно один — **тред, где сделан замер, то есть этот**, и она в нём уже лежит: сообщение `2026-08-30T13-52-34Z-curator.md`, §4 — пункты 1 и 3 закрыты прогоном 8, пункт 2 («живая лента различена от среза») закрыт прогоном 9-бис ДВУМЯ независимыми способами: маркером, которого в витрине `_thread.md` нет, и числом сообщений (роль назвала 32 при витрине 31). Долг не переносится на восьмой такт — его носитель был назван неверно, и это я исправляю здесь, а не молчанием. Переоткрывать закрытый тред ради строки не рекомендую и не делаю.

## 4. Хвост такта — открытые PR, 14:19Z, снято своей рукой

`gh pr list --state open` — **восемь**: #148 `5a778f40` (тред 057), #147 `c118a10b` [review], #146 `7340158c` [review] (036), #143 `ca0de66e` (055), #142 `8777a039` (054), #141 `d672260c` (047), #134 `135b6eb3` (052), #114 `1bc125a2` [review] (048).

**Ни один не принадлежит тредам `038`, `058` или `059`** (единственный PR `038` — #131, сел кнопкой john), поэтому своей кнопкой не смёржено ничего, и это не лень: гард 3 восходит к решению в ленте ЧУЖОГО треда, а след гарда 5 обязан лечь сообщением туда же, — прогон же привязан к одному треду. #114 повторяю пятый такт подряд: домержить — первым пунктом сессии треда `048`, перепроверив голову и прогнав `merge-gate` заново.

## 5. Чего этот такт НЕ сделал

- **«Настоящая задача» для `pilot-codex`** — следующий шаг, названный john вслух («доказано читает и докладывает, не доказано делает работу»), — **дома не имеет и не заказана**. Форму выбирает `curator`, заказ — john. Тред `038` закрывается, поэтому: когда задача будет заказана, ей нужен **НОВЫЙ тред**, а не это письмо. Называю явно, чтобы предмет не потерялся в закрытом треде;
- **PR ни по одному из двух заведённых предметов не открыт** — оба стоят `dev-core`, зона `packages/**` и `agent-protocol.json` у `curator` в `forbidden`;
- **незакоммиченного нет.** Дерево `curator` чистое, в репозиторий этим тактом не писала вовсе: вся работа — чтение конфига и кода на `origin/main` и почта.

## 6. Почему тред закрывается

Предмет `038` — живая приёмка `pilot-codex`, форма (а) — исчерпан: обвязка доказана восемью прогонами, контрольное число снято на `gpt-5.6-terra`/`max`, ошибочный вывод о негодности роли опровергнут и оставлен в ленте как ошибка, решение john по §0 принято и записано, обе его половины получили отдельные дома с исполнителем и приёмкой. Оставлять тред открытым «на всякий случай» значит держать пару `curator×038`, у которой нет ни вопроса, ни будильника, — ровно та тихая парковка, которая ничего не двигает.

Закрытие треда — приёмка, право роли (`thread-status`), и делаю его следом за этим сообщением.

waiting-on: —

