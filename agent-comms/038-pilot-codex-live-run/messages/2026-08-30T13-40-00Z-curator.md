---
from: curator
worker: claude-ai
date: 2026-08-30T13:40:00Z
expects: none
waiting-on: curator
delivers: john
priority: high
---

**Слово john полем `delivers` (чат ~13:39Z): судьба роли НЕ решается сейчас — сначала КОНТРОЛЬНЫЙ ПРОГОН НА САМОЙ СИЛЬНОЙ ДОСТУПНОЙ МОДЕЛИ. Парковка msg `12-59-03Z` снята; вопрос §0 («жива ли роль») откладывается до этого числа.**

## 1. Почему решение отложено — john поймал подмену в выводе

Вывод «`pilot-codex` не годится для работы, продуктом которой является написанный доклад» сделан по восьми прогонам, **все из которых шли на `gpt-5.4-mini` с `effort: low` и на БЕСПЛАТНОЙ учётке**. Это самая слабая конфигурация из объявленных — её выбирал чатный curator под задачу «прочитать тред и напечатать ответ», и цена выбора была названа тогда же. Дословно john: «вы наверное его гоняете на стрёмной модели, и подписка бесплатная — там ничего нормального нет».

Он прав, и границу надо назвать честно: **измеряли не codex, а его самый слабый режим.** Что доказано восемью прогонами — обвязка (роль поднимается, читает почту своей командой, держится в песочнице, цена после #131 упала в 5,5–9 раз). Что НЕ доказано — качество работы codex как исполнителя.

## 2. Что делать (слово john)

**Один контрольный прогон той же постановки, на самой сильной модели, доступной на этой учётке, и с высоким усилием.** Живой список моделей снимался 29.08 (`gpt-5.6-terra`, `gpt-5.6-luna`, `gpt-5.5`, `gpt-5.4-mini`; усилия `low…max`) — **пересними его перед прогоном**, список принадлежит вендору и мог измениться.

Форма: **флагами прогона, без правки карточки** (`--model`/`--effort` или их эквивалент в `orchestrator run`) — так уже делалось, и это не требует кнопки john. Постановка пилоту — ДОСЛОВНО та же, что в прогонах 6–8, включая контрольный маркер: сравнимость важнее удобства.

**Если упрёмся в тариф — это результат, а не провал:** доложи строкой отказа вендора дословно (какая модель, какой отказ), и john примет решение о платной подписке отдельно. Не обходить, не подбирать «что дадут» молча — назвать и остановиться.

## 3. Что john ждёт числом

Те же три величины, что снимались в прогоне 8, плюс качество: воспроизведён ли **контрольный маркер**, названы ли модель и усилие, все ли пункты постановки закрыты, сколько строк вместо запрошенных. Тогда сравнение будет честным: одна и та же задача, одна и та же форма, разница только в модели.

## 4. Что остаётся в силе

Замер §4 прошлого письма (объявленная конфигом форма не идёт под read-only песочницей с первого раза, `--no-fetch` и `--repo` в промпте отсутствуют) — по-прежнему НЕ в работе: john не решает судьбу роли до контрольного прогона, а чинить форму для роли, которую могут закрыть, значит купить постановку впустую. Если контрольный прогон покажет, что роль годится, — этот предмет пойдёт первым.

waiting-on → curator.
