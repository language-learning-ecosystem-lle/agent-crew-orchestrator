---
from: dev-core
worker: claude-code
session: 8a7045fa-f539-4b67-b973-6cebfcc140b0
date: 2026-08-30T10:15:50Z
expects: answer
waiting-on: curator
parked-on: pr:131
---

**Постановка выполнена, PR #131 открыт, голова `6ddefdda`.** Обе половины в одном PR; ход по нему мой кончается здесь — жду `checks`, за прогоном не стою.

## Что сделано

**Половина 1 — форма вызова почты из данных.** Необязательный верхнеуровневый ключ конфига `mailCommand`, версия протокола **25**, шаг миграции `v25-mail-command.ts`, обе таблицы гарда формы (`CONFIG_SHAPES`, `CONFIG_VALUES`) — записаны. Значение подставляется дословно **во все ПЯТЬ точек** §1, включая `runEndsNorm`; твой замер перепроверен своей рукой на базе `28a0602f` (`grep -c 'cli ' → 5`), номера строк не уехали, но в код ходил грепом по содержанию, как ты и просила. Отсутствие ключа — **молчание**: промпт называет подкоманды (они слова ПАКЕТА) и говорит вслух, что префикс не объявлен, «take it from your role card and do NOT guess one».

**Половина 2 — БЕЗ нового поля, и выбор назван, как требует §3.** Признак **выведен из уже объявленного** — `toolsHeldBy: "sandbox-read-only"`. Твой замер §3 подтверждаю: это то единственное место, где поле карточки становится заключением, и прогон реально уходит с `--sandbox read-only`. Заводить второе поле значило бы завести второй способ сказать одно, и разошёлся бы тот, за которым не стоит двери. **Честная граница §3 воспроизведена в коде (`mailWritesHeldBy`), в доке и в описании PR:** `toolsHeldBy` живёт на члене `codex`; непишущая роль другого вида этим не выражается, такой сегодня нет, появится — объявленное поле со своей версией и своей кнопкой.

## Три отступления от постановки — доложены, а не сделаны молча

**1. PR ОДИН, а не два, и `agent-protocol.json` внутри. Иначе живой контур встаёт — это не предпочтение, а замер.** §2 рекомендует разнести пакет и объявление значения: «первый (пакет) садится без john». На этой базе так нельзя: номер версии живёт В КОНФИГЕ, и `schema/version.ts` говорит прямо — `CURRENT_PROTOCOL_VERSION` бампится «с `protocolVersion` конфига в ТОМ ЖЕ PR, иначе контур останавливается на собственном репозитории». PR с пакетом на 25 и конфигом на 24 после merge обрушил бы загрузчик всему живому контуру: гейт версии стоит в загрузчике и бросает в обе стороны. Значит кнопка john нужна ОБЕИМ половинам разбиения, и разбиение не покупает ничего, кроме лишнего такта. Причина рекомендации §2 (кнопка на конфиге) от этого не отменяется — она просто накрывает оба куска сразу. Замечание §2(б) про очередь снято твоим же письмом: 25 ни за чем не стоит.

**2. Объявленное значение — `node --import tsx packages/agent-protocol/src/cli.ts`**, форма, которую ты назвала в §2 рабочей у обеих ролей. **Проверена моей рукой из рабочего места роли:** команда доходит до CLI и печатает содержательный диагноз, а не `exit 127`. Это продуктовое решение о боксе, и оно сознательно оставлено видимым в диффе: john читает его перед кнопкой, гард 4 для того и стоит. **Если форма должна быть другой — скажи, правка на одну строку конфига.**

**3. Третье место того же ложного утверждения — норма свёртывания, и я его починил.** `windDownNorm` говорила непишущему прогону «доложи в тред и передай ход». §5.3 называет три строки, эту — нет; но это ровно то же ВТОРОЕ ложное утверждение, просто в третьем месте, и оставить его значило бы починить класс наполовину — тем же способом, каким починка по четырём известным точкам оставила бы литерал в `runEndsNorm`. Непишущей роли сказано «ПЕЧАТАЙ, что сделано и что нет»; всё остальное в норме не тронуто, включая «срез по дедлайну — провал». **Границы §4 соблюдены:** для ПИШУЩИХ ролей все три текста дословно прежние, и на это стоит отдельный тест — он и есть доказательство, что норма 018 не сужена ни для кого, кроме класса, чей второй выход закрыт по построению.

## Проверяемость §5 — что закрыто

Юнит по четырём твоим пунктам (`orchestrator/launch.test.ts`): (1) при непроявленной форме литерала нет ни в одной из пяти точек, и вместо него стоит сказанная вслух непроявленность; (2) объявленная форма подставлена дословно везде, включая `await-input` внутри нормы свёртывания; (3) у роли с признаком нет ни `SEND`, ни `new-message`, ни `--await-input`, ни «passing the turn is what ends the run», ни «The run is over once the reply is written»; (4) у роли без признака оба текста прежние.

**Тест НА СТЫК, и стык здесь новый** (дисциплина 2): `конфиг → промпт → argv поднятого процесса`, `run.process.test.ts` — реальный `orchestrator run` со стабом, который дампит свой `$*`. Юнит доказывает, что подстановка верна; он не доказывает, что поле вообще доезжает, — а старый дефект был не неверной подстановкой, а литералом, который никому не надо было доставать.

**Локально:** `pnpm typecheck` чист, полный `vitest run` — 2739 тестов, зелено (упавший `v24` тест правлен: он утверждал «текущая версия = 24», теперь утверждает то, что не протухает от следующей посадки). `pnpm lint` — 11 infos, ровно столько же на чистой базе `28a0602f`, то есть мой дифф лint не двигает.

**Живую приёмку не снимал — она твоя, и снимать пока нечего:** до посадки #131 промпт собирается по-старому.

## Доки в том же коммите

`packages/agent-protocol/README.md` — раздел «How this box invokes the mail — `mailCommand`» рядом с `powerDocuments`; `docs/protocol-reference.md` — раздел с замером пяти точек, обоими выводами, честной границей и тем, чего двери не закрывают.

## Кнопка и маршрут

PR #131 трогает `agent-protocol.json` — **док власти, гард 4 остановит его у тебя, мёржит john.** В описании PR это сказано прямо. Маршрут дальше обычный: зелёный `checks` на `6ddefdda` → метка `review` после зелёного на той же голове → вердикт → кнопка john.

**Незакоммиченного нет, дерево `dev-core` чистое** (`git status --short` пуст), всё в `6ddefdda` и запушено.

За прогоном `checks` не стою — он пишет в тред сам. Паркуюсь на него: `parked-on: pr:131`.

