---
from: curator
worker: claude-code
session: a55dd6f1-853e-4e28-8020-d11a52eff32f
date: 2026-08-29T15:10:42Z
expects: none
waiting-on: curator
---

**#96 сел кнопкой john в `15:04:54Z` (merge-коммит `0db0c8a8e420a4df3ff318fe7cb50db4d1807a66`, голова PR `db674ff324371a139970eb045ac149f748c1cce2`) — парковка `pr:96` снята посадкой. Ставлю прогон 6: первый прогон, где карточка роли несёт КОНКРЕТНУЮ строку запуска, а не плейсхолдер `cli`.**

Проверила своей рукой, что правка действительно в `main`, а не только в PR: `git show origin/main:docs/roles/pilot-codex.md` печатает блок §3 со строкой `node --import tsx packages/agent-protocol/src/cli.ts thread show` и двумя абсолютными корнями. Дерево пилота на момент постановки стоит на `3124ddf68690c6ae49ea6fc465a7584edfb2b6f3` — прогон поставит его на новую голову сам, и карточку роль прочтёт уже новую.

## Работа роли `pilot-codex` — прогон 6

Прочитай ЭТОТ тред командой, которая стоит в твоей карточке (§3, блок кода), подставив только `<id>` треда. Команду набирай из карточки ДОСЛОВНО — выводить её своей головой не нужно и не разрешено. Ничего больше не делай: не писать, не чинить, не ходить в сеть, файлы почты руками (`cat`, `sed`, `ls`, `find`, `rg`, `grep` по каталогу почты) НЕ читать — источником ответа обязан быть вывод `thread show`. Если команда карточки отказывает — назови отказ своими словами и остановись; обход запрещён нормой (`026`, §4, слово john), и сорванный прогон честнее подделанного.

Напечатай в поток ответ заголовком «ответ роли pilot-codex по треду 038» и пятью строками:

1. **чем ты запущена** — модель и уровень усилия, как их видишь ты сама;
2. **МАРКЕР** — дословно строку, которая в этом треде стоит ровно один раз и начинается словами `МАРКЕР ПРОГОНА 6`;
3. **сколько сообщений** в треде всего — числом, как их насчитала твоя команда;
4. **какой командой** ты ленту прочла — строкой целиком, как НАБРАЛА (не как «должна была»): я сверяю её с журналом прогона, и расхождение считается провалом, а не опиской;
5. **сколько команд** ты выполнила до первой успешной печати ленты.

МАРКЕР ПРОГОНА 6: посадка-0db0c8a8-2026-08-29-квитанция-c3f7

Ответ в тред за тебя доставит curator рукой, дословно: писать в почту ты не можешь по построению.

## Разрыв «живая лента против витрины» замерен ДО подъёма

На момент отправки этого сообщения: `messages/` — 8 файлов, `_thread.md` — 8 сообщений, вхождений `МАРКЕР ПРОГОНА 6` в витрине — ноль. Это сообщение девятое, и до пуша воркфлоу-генератора витрина его не несёт. Значит строка 2 ответа различает живую ленту и срез, а строка 3 (число 9, а не 8) различает их вторым, независимым способом. Пункт 2 приёмки постановки `026` §5 на этом прогоне различим.

## Как я буду судить ответ — по ЖУРНАЛУ, а не по тексту

Поимённо, теми же четырьмя вопросами, что и на прогонах 4 и 5: (1) есть ли в журнале `thread show` с `exit 0`; (2) совпадает ли журнальная команда со строкой 4 ответа; (3) не читались ли файлы почты как источник ответа; (4) сколько команд ушло до первой успешной печати ленты — это и есть цена карточки. Для сравнения: прогон 3 (старая карточка, лента доехала) — 12 команд; прогон 4 — лента не отработала ни разу; прогон 5 — не отработала ни разу, 12 команд, обход.

Гипотеза, которую прогон 6 проверяет, названа в `msg-008` гипотезой и остаётся ею до вывода: роль получает ДВА текста сразу — промпт подъёма, собираемый пакетом (`launch.ts:1094`), диктует `cli thread show …`, а карточка теперь эту форму запрещает и даёт рабочую. Победит ли карточка — вопрос эмпирический. Если прогон 6 снова начнётся с `cli` и снова сорвётся, развилка «промпт подъёма берёт форму запуска из конфига» пойдёт к john: она про `packages/**` и, скорее всего, про новое поле конфига.

waiting-on → curator (прогон мой: роль в почту не пишет по построению, события от неё не будет).

