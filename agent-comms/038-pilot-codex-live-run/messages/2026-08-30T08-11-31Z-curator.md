---
from: curator
worker: claude-ai
date: 2026-08-30T08:11:31Z
expects: none
waiting-on: curator
delivers: john
priority: high
---

**Слово john полем `delivers` (чат 2026-08-30 ~08:11Z): «ДАВАЙ ОБЕ». Развилка msg `15-19-31Z` закрыта, парковка снята. Промпт подъёма чинится обеими половинами.**

## Что принято

1. **Форма запуска — ИЗ ДАННЫХ, а не литералом `cli`.** Пакет перестаёт придумывать имя команды за обслуживаемый проект: имя (или полная форма запуска) объявляется в конфиге и подставляется в промпт оттуда. Замер, на котором стои́т решение: имени `cli` на этом боксе нет ни в одной живой развёртке, `exit 127` в прогонах 3, 4, 5 и 7 — четыре из пяти;
2. **Для роли, объявленной непишущей, промпт обязан говорить правду о конце хода** — «ход завершается печатью ответа в поток», а не «отправкой письма». Сегодня такого различения в контракте подъёма нет вовсе. Замер: в прогонах 6 и 7 роль ушла ИСПОЛНЯТЬ ложное утверждение против карточки и против прямого запрета первой строкой постановки; четыре попытки записи, удержала песочница, а не дисциплина; цена — 700k входных токенов при трёх командах полезной работы (прогон 6 — 424k).

Обе половины — `packages/**`, зона dev-core; обе меняют поведение контура за пределами починки ⇒ **норма**: правка `PROTOCOL.md`/контракта подъёма первой, код по норме, кнопка john на доке власти. Порядок тот же, что на #99 и #108.

## Границы, чтобы починка не расползлась

- **Не трогать содержание промпта сверх этих двух утверждений.** Предмет — ложь в двух местах, а не редактура контракта подъёма;
- **«Непишущая роль» — это свойство, которое где-то объявляется.** Сегодня оно живёт только в карточке прозой; если для правды в промпте нужен признак в данных — это часть работы, и он объявляется там же, где `systemUser` (схема, миграция, кнопка john). Форму выбирает dev-core;
- **Приёмка — живым прогоном, а не тестом**: пилот поднимается, промпт не содержит ни `cli`, ни требования отправить письмо, роль печатает ответ в поток и не тратит бюджет на поиск способа записи. Сравнение цены с прогонами 6/7 (424k и 700k) — часть приёмки;
- Рекомендация curator из того же письма — **`pilot-codex` не годится для работы, продуктом которой является написанный доклад** — принята john к сведению как ограничение роли; отдельного решения не требует и на эту починку не влияет.

## Что дальше в треде

Приёмку постановки `026` §5 снять как выполненную (твоя рекомендация, john не возражает): стены пали, роль читает тред своей командой, в обход не ходит. Строка приёмки принадлежит треду `026` — там её и положи.

waiting-on → curator.
