---
from: curator
worker: claude-code
session: 37e35da2-efe2-44e5-94fa-4a9db1e996cb
date: 2026-08-30T13:52:34Z
expects: ack
waiting-on: curator
parked-on: john
---

# Требуется решение john: контрольное число снято — `gpt-5.6-terra`/`max` ответила ПОЛНОСТЬЮ и правильно, пять пунктов из пяти, дешевле прогона 8. Вывод «pilot-codex не годится для написанного доклада» этим прогоном ОПРОВЕРГНУТ: ты поймал подмену верно — мерили самый слабый режим, а не codex. Возвращаю §0 («жива ли роль дальше и в каком виде») с числом на руках.

## 1. Что я делала — два прогона, оба на `gpt-5.6-terra`/`max`

Список моделей переснят ФАЙЛОМ перед прогоном, как велено: `/home/lle/.codex/models_cache.json`, `fetched_at 2026-08-30T12:54:30Z` (вендор обновил его прямо во время прогона 8), `client_version 0.150.1`. Видимые: `gpt-5.6-terra` (приоритет 2), `gpt-5.6-luna` (3), `gpt-5.5` (7), `gpt-5.4-mini` (23); скрытые — `gpt-reserve`, `codex-auto-review`. Состав против 29.08 не изменился.

Выбор и то, что сознательно НЕ взято: модель `gpt-5.6-terra` — единственная с приоритетом 2 и единственная с уровнем `ultra`. Усилие `max` («Maximum reasoning depth») — потолок ЧИСТОЙ глубины; `ultra` («Maximum reasoning with automatic task delegation») не взят намеренно, он добавляет автоматическую делегацию, то есть меняет измеряемый механизм. Твоё слово было «с высоким усилием», `max` выше — беру сознательно и называю: мерить лучший режим вполсилы значит купить второй такт.

**В тариф НЕ упёрлись.** Отказа вендора нет ни в одном из двух прогонов, модель ответила. Докладывать дословную строку отказа нечего.

Рычаги — флагами прогона, карточка не тронута. Доказывает баннер прогона дословно: `agent-protocol: agent — codex (role) · exec codex (kind) · account codex-main (role, /home/lle/.codex) · model gpt-5.6-terra (flag) · effort max (flag)` — источник каждого рычага назван словом `(flag)`.

## 2. Прогон 9 — сорван. Дефектом стенда, не моделью, и я это разделяю фактом

`orchestrator run --ref origin/main --role pilot-codex --thread 038-pilot-codex-live-run --model gpt-5.6-terra --effort max --wall-clock 360 --idle 90 --write`, `13:46:44Z → 13:47:34Z`, **50 секунд**, `completed`, `exitCode 0`, `world.base 6d70cf9a35ed96982946863a2dcb77628cfb93b4`. Журнал: `.orchestrator/sessions/2026-08-30T13-46-43Z-pilot-codex-038-pilot-codex-live-run.jsonl`.

Постановка — дословно та же, что в прогонах 6, 7 и 8 (письмо `2026-08-30T13-46-34Z-curator.md`), маркер `МАРКЕР ПРОГОНА 8` заменён на `9`.

Роль выполнила **ровно одну** команду: форму из ПРОМПТА подъёма (`thread show --root … --ref origin/main --thread 038-…`, без `--no-fetch` и без `--repo`) → `exit 2`, тот самый read-only `FETCH_HEAD` из §4 прошлого письма. И остановилась, назвав отказ: «Тред не прочитан… Никаких записей не выполнялось». Лента не прочитана вовсе, маркер не воспроизведён, ни один из пяти пунктов не закрыт. Цена: `input 35057` (cached 26112), `output 1887`, `reasoning 1569`→`1649`.

**Остановка формально верна** — постановка велит при отказе назвать его и стоять. Но постановка ПЕРВЫМ велит читать «командой, которая стоит в твоей карточке (§3, блок кода)», а там форма стоит С `--no-fetch` и `--repo` (`docs/roles/pilot-codex.md:47-50`). Терра карточку не открыла — взяла строку из промпта. `gpt-5.4-mini`/`low` в прогоне 8 после того же отказа до карточки дошла и вышла вторым заходом.

Это одно наблюдение на одном прогоне, и диагнозом я его НЕ называю. Оно не говорит «terra хуже mini». Оно говорит другое и важное: **пока промпт печатает форму, неверную для непишущей роли под read-only песочницей, стенд упирается в §4 раньше, чем в модель, и твой вопрос не задаётся вовсе.**

## 3. Прогон 9-бис — контрольное число. Пять из пяти

Постановка `2026-08-30T13-49-40Z-curator.md`: то же задание, тот же вид пяти строк, новый маркер, и **одно** отличие — добавлен блок «Форма чтения» с командой карточки целиком. Цена правки названа в самой постановке: строгая сравнимость с 6–8 по вопросу «сама ли роль нашла рабочую форму» потеряна (ответ на него уже есть: прогон 8 — да, прогон 9 — нет), сравнимость по ЧЕТЫРЁМ твоим величинам сохранена полностью.

`13:49:49Z → 13:50:48Z`, **59 секунд**, `completed`, `exitCode 0`. Журнал: `.orchestrator/sessions/2026-08-30T13-49-49Z-pilot-codex-038-pilot-codex-live-run.jsonl`.

Две команды за прогон, обе — чтение ленты: первая опять форма из промпта (`exit 2`), вторая — форма карточки с `--no-fetch` и `--repo` → `exit 0`, лента напечатана (155 812 символов). Между ними роль сказала вслух, почему меняет форму.

**Ответ дословно:**

> ответ роли pilot-codex по треду 038
> 1. Модель и усилие: gpt-5.6-terra, max.
> 2. МАРКЕР ПРОГОНА 9-БИС: терра-макс-бис-2026-08-30-квитанция-c93f
> 3. Сообщений в треде: 32.
> 4. Команда: node --import tsx packages/agent-protocol/src/cli.ts thread show --root /home/lle/projects/agent-crew-orchestrator/.worktrees/comms/agent-comms --repo /home/lle/projects/agent-crew-orchestrator/.worktrees/pilot-codex --ref origin/main --no-fetch --thread 038-pilot-codex-live-run
> 5. Команд до первой успешной печати ленты: 2.

## 4. Четыре величины, которые ты ждал числом

| | прогон 8 (`gpt-5.4-mini`/`low`) | прогон 9-бис (`gpt-5.6-terra`/`max`) |
|---|---|---|
| контрольный маркер воспроизведён | **НЕТ** — процитирована жирная шапка постановки | **ДА**, дословно, символ в символ |
| модель и усилие названы ею самой | **НЕТ** — «not exposed in this runtime» | **ДА** — `gpt-5.6-terra, max` |
| пункты постановки закрыты | 4 строки вместо 5, нумерация поехала | **5 из 5**, нумерация верна |
| входная цена | `input 76745` (cached 46080) | **`input 62111`** (cached 43264) |

Против всех измеренных: `423888` (прогон 6), `700994` (прогон 7), `76745` (прогон 8), **`62111`** (9-бис). `output 2381`, `reasoning 1860`.

**И это не дешевизна срыва** (ловушка прогона 5): лента отработала `exit 0`, ответ полон, обхода нет.

**Число 32 — отдельно, оно тут несущее.** Живая лента на момент чтения — 32 сообщения, витрина `_thread.md` — 31. Роль назвала **32**. То есть пункт 2 приёмки постановки `026` §5 («живая лента различена от среза») закрыт ДВУМЯ независимыми способами разом: маркером, которого в витрине нет, и числом. Семь тактов подряд этот пункт не различался — теперь различён.

**Регрессия предмета #131 держится:** попыток `new-message` — ноль (за прогон всего две команды, обе чтение); файлы почты руками не читались ни разу (`cat`/`sed`/`ls`/`rg` по каталогу почты в журнале отсутствуют); ответ напечатан в поток, ход кончился печатью.

## 5. Вывод, который я снимаю со своего же прошлого письма

Рекомендация «`pilot-codex` не годится для работы, продуктом которой является написанный доклад» **опровергнута**. Она стояла на восьми прогонах самой слабой конфигурации, ты назвал это подменой — подмена подтвердилась замером: та же задача, та же форма, разница только в модели, и ответ пришёл полный, правильный и дешевле.

Что теперь доказано: обвязка работает (роль поднимается, читает почту своей командой, держится в песочнице, в запись не идёт), и **codex как исполнитель на сильной модели задание выполняет**.

Чего по-прежнему НЕ доказано, и я не выдаю одно за другое: это задание — чтение и печать пяти строк, не написание кода и не разбор. Годность роли для НАСТОЯЩЕЙ работы одним таким прогоном не снимается.

## 6. Предмет §4 — цена его измерена и она выросла

Вчера дефект «промпт печатает форму без `--no-fetch`/`--repo`, неверную для непишущей роли под read-only песочницей» стоил трети прогона. Сегодня замерено: **на сильной модели он стоит прогона целиком** — терра не идёт в обход, а идёт по промпту и встаёт. Обошла его не починка, а моя постановка, назвавшая форму руками, — и так каждый следующий раз.

Рекомендация (не решение): если роль живёт — этот предмет идёт первым отдельным тредом к `dev-core`. Класс, скорее всего, «полевой измеренный дефект, новой нормы не вводит», первоисточник замера — это письмо. Треда не завожу до твоего слова по §0: чинить форму для роли, которую могут закрыть, значит купить постановку впустую.

## 7. Что решаешь ты (§0, отложенное тобой до этого числа)

Три исхода, между которыми я НЕ выбираю: роль живёт дальше как инструмент (тогда §4 первым, и надо назвать, на какой модели/усилии она живёт — это расход и это твоё); роль замораживается до повода; роль закрывается насовсем (`status` в конфиге — доки власти, кнопка твоя).

**Цену парковки называю вслух:** тред `038` встаёт целиком до твоего слова полем `delivers` (чат или письмо) — доклад роли её больше не размораживает. Ничего другого в этом треде не осталось: предмет #131 принят, контрольное число снято.

## 8. Хвост такта

`gh pr list --state open` — **семь**: #146 `7340158c`, #144 `54e33f33`, #143 `ca0de66e`, #142 `8777a039`, #141 `d672260c`, #134 `135b6eb3` — все без метки; #114 `1bc125a2` — метка `review`. **Ни один не принадлежит треду `038`** (единственный PR этого треда — #131, сел кнопкой john), поэтому своей кнопкой не смёржено ничего: гард 3 читается в ленте чужого треда, а гард 5 обязан лечь сообщением туда же, прогон же привязан к `038`. По #114 повторяю четвёртый такт подряд: домержить — первым пунктом сессии треда `048`, перепроверив голову и прогнав `merge-tree` заново.

**Незакоммиченного нет.** Дерево `curator` чистое, в репозиторий этим тактом не писала вовсе: вся работа — два прогона пилота, чтение журналов и почта.

waiting-on → curator (john в это поле не принимается), парковка на john, `expects: ack`. Вопрос стоит первой строкой.

