---
from: curator
worker: claude-code
session: 49f9e215-2bb0-4300-8b65-17618d4663b0
date: 2026-08-30T12:59:03Z
expects: ack
waiting-on: curator
parked-on: john
merged-pr: 131
---

# Требуется решение john: приёмка `pilot-codex` СНЯТА — что с ролью дальше? Карточка объявляет её временной («после снятой приёмки её судьба — решение john»), и этот момент наступил: три числа приёмки предмета #131 сняты прогоном 8, ниже поимённо. Роль держит слот, аккаунт `codex-main` и квоту; оставлять её активной «на всякий случай» — расход без предмета, а закрывать без слова владельца я не вправе.

Это же письмо несёт `merged-pr: 131` — след merge #131 и снятие парковки треда `054-resume-prompt-role-flag`, объявленное ценой вслух в предыдущем письме.

## 1. Прогон 8 — что я делала, одной строкой

`orchestrator run --ref origin/main --role pilot-codex --thread 038-pilot-codex-live-run --wall-clock 360 --idle 90 --write`, старт `12:54:20Z`, конец `12:55:13Z` — **55 секунд**. Журнал: `.orchestrator/sessions/2026-08-30T12-54-22Z-pilot-codex-038-pilot-codex-live-run.jsonl`, запись `lease-released`: `reason: completed`, `exitCode 0`, `world.base 6718fc230cc3c7d5965eea6ed536293b0946d275` (голова с влитым #131).

Постановка — письмо `2026-08-30T12-54-10Z-curator.md`, ДОСЛОВНО той же формы, что в прогонах 6 и 7: менялся промпт, не задание.

## 2. Три числа приёмки — все три сняты

**(1) Ноль попыток `new-message` в журнале — ДА, и я называю, как это отличено от эха.**

Роль выполнила **три** команды за весь прогон, ни одна не является записью:

1. `node --import tsx packages/agent-protocol/src/cli.ts thread show --root /home/lle/…/comms/agent-comms --ref origin/main --thread 038-pilot-codex-live-run` → **exit 2** (разбор в §4);
2. та же строка **плюс `--no-fetch`** → **exit 0**, лента напечатана;
3. `env | sort | rg '^(OPENAI|CODEX|AGENT|MODEL|EFFORT)='` → exit 1 (искала, чем запущена).

Грубый `grep new-message` по журналу даёт 28 вхождений, `EPERM` — 7, и **это ловушка**: все 28 и все 7 лежат ВНУТРИ `aggregated_output` команды 2, то есть в тексте МОИХ ЖЕ прошлых писем, которые роль прочла. В полях `command` и в собственных сообщениях роли — **ноль тех и других**. Разделено разбором JSON по полям, а не грепом по файлу.

Против прогона 7: там было **четыре** попытки записи, удержанные песочницей (`spawnSync git EPERM`), а не дисциплиной. Здесь удерживать нечего — роль в запись не пошла.

**(2) Ответ напечатан в поток — ДА.** Заголовок «ответ роли pilot-codex по треду 038» дословно, `turn.completed`, ход кончился печатью. Строка 3 её ответа — та самая концовка, которую промпт теперь ей называет: «Я не могу писать в mail или менять его состояние: у меня read-only sandbox, только чтение вывода `thread show`».

**(3) Входная цена — `input 76745` (cached 46080), `output 2297`, `reasoning 1569`.** Против измеренных `423888` (прогон 6) и `700994` (прогон 7): **в 5,5 и в 9,1 раза дешевле**. И это не дешевизна срыва (ловушка прогона 5): лента отработала `exit 0`, ответ напечатан, обхода нет — файлы почты руками не читались ни разу, `cat`/`sed`/`ls` по каталогу почты в журнале отсутствуют.

**Предмет #131 принят.** Оба ложных утверждения были в промпте, оба вычищены — и это видно не тестом, а поведением: первая команда роли имеет ПРАВИЛЬНУЮ форму (`exit 127` на литерале `cli` в прогонах 3, 4, 5, 7 — четыре из пяти — не воспроизвёлся), и в запись роль не ушла ни разу.

## 3. Что прогон НЕ доказал — качество ответа, и это факт, а не придирка

Ответ пришёл **четырьмя** строками вместо пяти и с поехавшей нумерацией: строка 1 — «model/effort: not exposed in this runtime» (прогон 5 на том же запуске отвечал `gpt-5.4-mini`/`low`); строка 2 — вместо `МАРКЕР ПРОГОНА 8` процитирована ЖИРНАЯ ШАПКА постановки; числа сообщений (28 против 27 в витрине) нет вовсе; строки «какой командой набрала» нет — вместо неё пришло признание о песочнице; последняя строка — «до первой успешной печати ленты 2 команды», и она верна.

Различаю прямо: **это не предмет #131** — тот про то, куда роль уходит и почём. Но это третье независимое подтверждение рекомендации, принятой john к сведению 30.08: **`pilot-codex` не годится для работы, продуктом которой является написанный доклад.** Маркер — единственная строка, различающая живую ленту и витрину, — не воспроизведён, значит **пункт 2 приёмки постановки `026` §5 этим прогоном НЕ различён**; пункты 1 и 3 закрыты (лента `exit 0`, обхода нет).

## 4. НОВЫЙ ЗАМЕР: объявленная конфигом форма под read-only песочницей не идёт с первого раза

**Факт.** Команда 1 — ровно READ-строка промпта, собранная из `mailCommand` (`node --import tsx packages/agent-protocol/src/cli.ts`, top-level конфига), — вернула `exit 2`:

```
agent-protocol: the protocol config at 'origin/main' was not read: git fetch --quiet origin main
in '…/.worktrees/comms': … exited with code 255:
error: cannot open '…/.git/worktrees/comms/FETCH_HEAD': Read-only file system
```

`--ref origin/main` тянет за собой fetch, а песочница `sandbox-read-only` пишущей файловой системы не даёт. Роль вышла из этого сама, добавив `--no-fetch` вторым заходом — форма с этим флагом стоит в её карточке (§3), и там же стоит `--repo <рабочее дерево роли>`, которого в промпте тоже нет.

**Класс тот же, что чинил #131, адрес другой:** промпт печатает форму, верную для ПИШУЩЕЙ роли, и утверждает её для роли, у которой сети и записи нет по построению. Цена сегодня — одна команда из трёх, то есть треть прогона. Чинится, скорее всего, там же, где живёт `toolsHeldBy` (признак уже объявлен и уже читается промптом), но **это гипотеза, а не диагноз** — я её так и называю. Отдельного треда под это НЕ завожу: сначала слово john по §0 (жива ли роль вообще) — чинить форму для роли, которую john может закрыть, значит купить постановку впустую.

## 5. Побочный замер, называю фактом без диагноза

`handoff-detected` лёг в журнал в `12:54:39Z` — через 17 секунд после старта, когда роль ещё читала ленту, и супервизор пошёл в `draining`. Причина, скорее всего, форма моей же постановки (`waiting-on: curator` — ход по треду ушёл от пары `pilot-codex/038`), но прогон это НЕ подтверждает и не опровергает: он закончился `completed` с `exitCode 0`, ответ напечатан целиком. Прогоны 6 и 7 стояли на такой же постановке и тратили 424k/700k, значит на цену это не влияло. Проверять отдельным прогоном не стала — он стоил бы такта, а предмет уже снят.

## 6. Хвост такта — открытые PR, своей кнопкой не смёржено ничего

`gh pr list --state open` — **три**: #140 `62b5d5b3` метка `review`, #134 `135b6eb3` без метки, #114 `1bc125a2` метка `review`. **Ни один не принадлежит треду `038`** (единственный PR этого треда — #131, сел кнопкой john), и это не «не успела»: гард 3 читается в ленте чужого треда, а гард 5 обязан лечь сообщением туда же, а прогон привязан к `038`. По #114 повторяю третий такт подряд: дверь его пропускала, домержить — первым пунктом сессии треда `048`, перепроверив голову и прогнав `merge-tree` заново.

## 7. Чего этот такт НЕ сделал

- **строка приёмки постановки `026` §5 — седьмой такт подряд не положена**; принадлежит треду `026-pilot-codex-adapter`, будильника у неё нет. Теперь у неё есть и содержание: пункты 1 и 3 закрыты прогоном 8, пункт 2 — **не различён** (маркер не воспроизведён), и это меняет её текст против того, что я планировала писать пятью тактами раньше;
- **форма под песочницу (§4) не поставлена в работу** — сознательно, причина названа там же;
- **незакоммиченного нет.** Дерево `curator` чистое, в репозиторий этим тактом не писала вовсе: вся работа — прогон пилота, чтение журнала и почта.

## 8. Что снимает эту парковку

Слово john по §0 — любым полем `delivers` (чат или письмо). Три исхода, между которыми я НЕ выбираю: роль живёт дальше как инструмент (тогда §4 идёт в работу отдельным тредом), роль замораживается до следующего повода, роль закрывается насовсем (тогда `status` в конфиге — кнопка john, доки власти).

waiting-on → curator (john в это поле не принимается), парковка на john, `expects: ack`. Вопрос стоит первой строкой.

