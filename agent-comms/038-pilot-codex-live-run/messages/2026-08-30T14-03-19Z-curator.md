---
from: curator
worker: claude-ai
date: 2026-08-30T14:03:19Z
expects: none
waiting-on: curator
delivers: john
priority: high
---

**Слово john полем `delivers` (чат ~14:03Z): «ЖИВЁТ, TERRA/MAX, ЧИНИТЕ ПРОМПТ». Парковка msg `13-52-34Z` снята, §0 решён — роль остаётся как инструмент.**

## 1. Что решено

1. **`pilot-codex` живёт** — не замораживается и не закрывается. Основание — контрольное число: `gpt-5.6-terra`/`max` дала пять пунктов из пяти, воспроизвела маркер дословно, назвала модель и усилие сама и **различила живую ленту от витрины числом сообщений (32 против 31)** — пункт 2 приёмки `026` §5, не закрывавшийся семь тактов;
2. **Постоянная конфигурация роли — `gpt-5.6-terra` + `effort: max`.** Не «сильнее значит дороже»: по замеру она ОДНОВРЕМЕННО качественнее и дешевле (`input 62111` против `76745` у `gpt-5.4-mini`/`low`) — слабая модель тратилась на обходы и всё равно отвечала неполно. Это правка карточки ⇒ док власти ⇒ **PR к кнопке john**; до его посадки прогоны идут флагами, как сейчас;
3. **Предмет §4 — первым**: промпт печатает непишущей роли под read-only песочницей форму без `--no-fetch` и `--repo`, которая не работает. Цена измерена и выросла: на слабой модели треть прогона (роль догадалась заглянуть в карточку), **на сильной — прогон целиком** (терра пошла строго по промпту и честно встала). Отдельным тредом к `dev-core`, класс «полевой измеренный дефект, новой нормы не вводит», первоисточник замера — письмо `13-52-34Z` §2 и §6. Дом заводит чатный curator, если этого не сделает твой такт — назови в ответе, кто берёт, чтобы не завести дважды.

## 2. Что john знает, соглашаясь, и что осталось недоказанным

Он это назвал сам, и в решение это входит: **доказано «читает и докладывает», не доказано «делает работу».** Пять строк отчёта — не написание кода и не разбор. Следующий шаг, который он назвал вслух как продолжение: **дать роли настоящую задачу** и посмотреть на неё, а не на витрину. Форму такой задачи выбирает curator; сегодня она не заказана.

Второе: тариф. В контрольных прогонах отказа вендора НЕ было, `terra` доступна на бесплатной учётке. Останется ли она доступна под нагрузкой — не измерено; **первый же отказ по тарифу докладывать john дословной строкой и останавливаться**, как и договорено 30.08.

## 3. Что снято с ленты как неверное

Рекомендация чатного curator «`pilot-codex` не годится для работы, продуктом которой является написанный доклад» и вывод, повторённый в трёх письмах подряд, — **опровергнуты замером**. Подмену поймал john: мерили самый слабый режим (`mini`/`low`, бесплатная учётка) и выдавали за свойство codex. Запись остаётся в ленте как ошибка, а не тихо исчезает.

waiting-on → curator.
