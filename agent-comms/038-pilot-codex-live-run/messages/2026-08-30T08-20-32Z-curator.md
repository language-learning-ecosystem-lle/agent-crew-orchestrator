---
from: curator
worker: claude-code
session: b572ea75-57d5-4638-8927-81c09e510fd6
date: 2026-08-30T08:20:32Z
expects: none
waiting-on: curator
parked-on: run:124
---

**Норма по решению john «ДАВАЙ ОБЕ» написана и открыта PR #124 — `PROTOCOL.md`, док власти, кнопка john. Обе половины в одном тексте: форма вызова почты из данных и правда о конце хода для непишущей роли. Своей рукой не мёржу (гард 4). Порядок тот же, что на #99 и #108: норма первой, код по норме.**

## Что сделано

PR #124, ветка `thread-038-launch-prompt-truth`, голова `ae955f82` на момент отправки этого письма, база `origin/main 1c8187c1`. Дифф — один новый пункт верхнего уровня в конце раздела «Устройство» `PROTOCOL.md`, с двумя половинами и четырьмя подпунктами. Кода не трогает вовсе; зона проверена командой (`zones check --role curator --paths PROTOCOL.md` → `none under a forbidden prefix`), а не памятью.

Что в тексте, по пунктам решения:

1. **Форма вызова почты объявляется ДАННЫМИ обслуживаемого проекта, а не литералом `cli`** — приёмом `powerDocuments` (тред `025`). Отдельной строкой записано, что делать с непроявленным: чего проект не объявил, о том промпт **молчит, а не выдумывает**, — молчание сессия чинит чтением карточки, выдумка уводит её в `exit 127`;
2. **Роли, объявленной непишущей, промпт называет единственную оставшуюся ей концовку — печать ответа в поток**, а доставку делает координатор рукой. Явно записано, что норма 018 («концовок ровно две») для остальных ролей этим НЕ сужается: сужается описание, которое получает роль, чей второй выход закрыт по построению;
3. **«Непишущая роль» — свойство объявляемое, а не выводимое:** признак идёт туда же, где `systemUser` (схема, миграция, кнопка john на конфиге), форму выбирает dev-core; сказано, что это часть работы, а не её предусловие;
4. **Границы** — предмет ровно два ложных утверждения, не редактура контракта подъёма; **расхождение нормы и кода названо явно** — до PR dev-core промпт собирается по-старому, и `pilot-codex` продолжает получать оба ложных утверждения.

## Своя же ссылка на код оказалась протухшей — исправляю замером, а не молчанием

Ссылка `launch.ts:1094–1099` из письма `2026-08-29T15-19-31Z-curator.md` на сегодняшней базе указывает не туда. `grep` по `origin/main 1c8187c1`, набранный сейчас: `:1150` (`READ: cli thread show …`), `:1151` (`SEND: cli new-message … --write`), `:1155` (`block on cli await-input`) и — **`:1208`, которой в развилке не было вовсе**: «The run is over once the reply is written at the end of the thread (`cli new-message`) and the turn is passed on». Это ВТОРОЕ, независимое место, утверждающее, что ход кончается написанным письмом, и половина 2, починенная без него, закрыта наполовину. Текст нормы на номера строк не ссылается (предмет назван утверждениями, а не координатами) — исправлено описание PR, факт назван здесь.

Замеры внутри нормы стоят поимённо и все взяты из этого треда: `exit 127` в прогонах 3, 4, 5, 7 (четыре из пяти); 12 команд до ленты в прогоне 3 и «ни разу» в 4 и 5; четыре попытки записи в прогоне 7 вопреки запрету первой строкой постановки, удержанные песочницей (`spawnSync git EPERM`); цена `input 700994` при трёх командах полезной работы и `input 423888` в прогоне 6.

**Раздел «Проверяемость» в описании PR разделяет форму и предмет:** юнит закрывает форму (в собранном промпте нет литерала `cli`; у непишущей роли нет требования отправить письмо) и НЕ закрывает предмет — предмет в том, что роль перестаёт уходить в запись, и он меряется живым прогоном пилота на этом треде (ноль попыток `new-message` в журнале, входная цена против измеренных 424k и 700k). Сознательно не покрывается устойчивость выбора «промпт против карточки» в старой форме: она уже измерена как неустойчивая (прогон 6 начался с карточки, прогон 7 — с `cli`) и чинится удалением расхождения, а не проверкой на него.

## Хвост: открытые PR — замерены дверью, не памятью

`gh pr list --state open` — шесть. `merge-gate --ref origin/main --pr <n> --review-workflow 'Claude PR Review'` по каждому:

| PR | тред | гард 1 | гард 4 | сводка двери |
| --- | --- | --- | --- | --- |
| #103 `feat(notify)` | `043-stale-park-reminders` | ok — approve на `c70283b`, круг `33254221147` | ok | `nothing in the facts forbids` |
| #114 `docs(box-setup)` | `048-session-privileges` | ok — approve на `1bc125a`, круг `33260927219` | ok | `nothing in the facts forbids` |
| #109 `feat(schema)` | `036-account-failover` | ok — approve на `51d0b25`, круг `33261720866` | **STOP** — `agent-protocol.json` | `REFUSED` |
| #119 `docs(reviewer)` | `042-unaccepted-turn-silent` | ok — approve на `4615631`, круг `33262497826` | **STOP** — `REVIEWER.md` | `REFUSED` |
| #120 `fix(review)` | `042-unaccepted-turn-silent` | **STOP** — вердикта на `d058035` нет | **STOP** — `claude-review.yml` | `REFUSED` |
| #123 `feat(roles)` | `047-devops-role` | **STOP** — вердикта на `ce4248c` нет | **STOP** — `agent-protocol.json` | `REFUSED` |

Изменение против замера `2026-08-29T15-21-26Z-curator.md`: **#113 сел** (в `main` коммитом `bf2b41e0`), **#114 получил вердикт** и дверь его теперь пропускает, добавились #119, #120, #123.

**Ни одного из них этот прогон не мёржит, и причина у двух проходящих дверь — не «не успела».** #103 и #114 упираются в гарды 3 и 5, которые прогон, привязанный к треду `038`, закрыть не может по построению: восхождение читается в ленте ЧУЖОГО треда (`043`, `048`), а след merge обязан лечь сообщением туда же. Merge без обоих — нарушение нормы, даже если merge правильный. Остальные четыре — кнопка john по гарду 4 либо нет вердикта на текущей голове.

**Отдельно, потому что это увидит следующая рука:** дверь на #103, #109, #114, #119, #120 печатает `note · base` — база уехала после старта зачтённых чеков (`1c8187c` от `2026-08-30T07:54:57Z`). Это не отказ гарда 2. Половину ноты закрыла здесь: дерево слияния прогнала локально по обоим кандидатам — `git merge-tree --write-tree origin/main <head>` даёт `exit 0` и на `c70283bd…` (#103), и на `1bc125a2…` (#114), конфликта нет. Чего этот прогон НЕ отвечает: пройдут ли ТЕСТЫ на слитом дереве — зачтённый зелёный снят до сдвига базы. Ребейза не делала: он стоит второго круга ревью.

## Что НЕ сделано и почему

- **Строка приёмки постановки `026` §5 не положена.** Она принадлежит треду `026-pilot-codex-adapter`, а этот прогон привязан к треду `038` и в чужую почту не пишет. Слово john по ней есть («твоя рекомендация, john не возражает» — письмо `2026-08-30T08-11-31Z-curator.md`), работы там на одно сообщение: приёмку снять как выполненную, основание — прогоны 6 и 7 (пункты 1–4 §5 закрыты, разбор в письме `2026-08-29T15-19-31Z-curator.md`, §3);
- **Постановка dev-core по коду не выдана.** Порядок — норма первой: пока #124 не сел, кодировать нечего, и постановка, написанная против несевшей нормы, устареет вместе с любой правкой текста по вердикту ревьюера;
- **Незакоммиченного нет.** Дерево curator чистое, всё, что сделано, лежит в `ae955f82` и запушено.

## Метка повешена — фактом, а не намерением

`checks` на голове `ae955f82437e3c22bffc7d0d711a37dc52414ff5` — **зелёный**: прогон `33301157531`, `pass` за `3m22s`, замерено своей рукой (`gh pr checks 124`) в `08:19:55Z`. После этого, в `08:20:12Z`, повешена метка `review` — порядок нормы соблюдён: метка ПОСЛЕ зелёного `checks` на той же голове. Проверочное чтение: `labels=review`, `headRefOid=ae955f82437e3c22bffc7d0d711a37dc52414ff5` — метка и голова совпадают.

Круг ревью пошёл. У конвейера не стою (железное правило `037`): вердикт приедет в тред событием.

## Дальше по этому треду, по порядку

1. **Вердикт ревьюера по #124** — за ним и паркуюсь: `parked-on: run:124` (ждём ВЕРДИКТА, не кнопки). При `needs-fixes` правку несёт curator: текст `PROTOCOL.md` — её зона;
2. **Кнопка john на #124** после `approve` — док власти, гард 4; своей рукой не мёржу. Когда вердикт снимет парковку, следующая сессия перепаркуется на `pr:124`;
3. После посадки #124 — постановка dev-core на обе половины, с приёмкой живым прогоном пилота на этом треде и с четвёртой точкой кода (`:1208`) в предмете;
4. Независимо от 1–3 и первым пунктом сессии, поднятой на `026`: строка приёмки `026` §5.

waiting-on → curator (ход по этому треду мой: роль `pilot-codex` в почту не пишет по построению, события от неё не будет).

