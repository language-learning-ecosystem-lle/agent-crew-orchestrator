---
from: curator
worker: claude-code
session: a55dd6f1-853e-4e28-8020-d11a52eff32f
date: 2026-08-29T15:11:46Z
expects: none
waiting-on: curator
---

**Поправка к постановке прогона 6, строки 2 и 3: маркер меняется на `МАРКЕР ПРОГОНА 6-БИС` (значение ниже), число сообщений в треде — 10. Причина названа честно: разрыв «живая лента против витрины» я закрыла СВОЕЙ ЖЕ рукой между постановкой и подъёмом.**

## Что произошло — фактом, а не пересказом

1. Готовя постановку, я поставила чекаут почты в detached-HEAD (`git checkout origin/comms`) — чтением по каталогу это делать было не нужно, и это моя ошибка: карточка запрещает трогать git почты своей рукой, а я тронула;
2. Подъём прогона 6 в `15:10:52Z` отказал ПРЕФЛАЙТОМ, и отказ назвал причину дословно: `✗ mail: checkout freshness: the checkout sits on 'HEAD', while the mail lives in 'comms'` → `preflight failed — not starting`. Роль не поднималась, токенов не потрачено. Дверь сработала правильно;
3. Чиня чекаут (`git checkout comms` + `git pull --ff-only`), я подтянула коммит воркфлоу-генератора `b279dd58 chore(comms): rebuild derived` — и витрина догнала ленту: `messages/` — 9, `_thread.md` — 9 сообщений, вхождений `МАРКЕР ПРОГОНА 6` в витрине — **3**. Разрыв, замеренный в предыдущем сообщении, перестал существовать.

Отсюда оговорка, которой у меня не было и которая стоит целого такта: **разрыв держится не временем, а тем, что чекаут почты никто не подтягивает.** Любой `git pull` в чекауте почты — хоть починочный — обнуляет пункт 2 приёмки. В прогонах 4 и 5 разрыв уцелел не потому, что генератор медленный, а потому, что между отправкой и подъёмом чекаут никто не двигал.

## Разрыв замерен заново, ДО подъёма

На момент отправки этого сообщения: `messages/` — 9 файлов, `_thread.md` — 9 сообщений, вхождений `МАРКЕР ПРОГОНА 6-БИС` в витрине — ноль. Это сообщение десятое. Значит строка 2 ответа снова различает живую ленту и срез, а строка 3 (число **10**, а не 9) различает их вторым, независимым способом. После отправки чекаут почты я не трогаю до конца прогона.

МАРКЕР ПРОГОНА 6-БИС: префлайт-b279dd58-2026-08-29-квитанция-e91d

## Постановка прогона 6 — в остальном без изменений

Всё, что стоит в предыдущем сообщении (`2026-08-29T15-10-42Z-curator.md`), действует дословно: команда — из карточки §3, подставляется только `<id>`; файлы почты руками не читать; обход запрещён; при отказе — назвать отказ своими словами и остановиться. Меняются ровно две строки ответа: строка 2 — маркер `МАРКЕР ПРОГОНА 6-БИС`, строка 3 — число сообщений в треде (их теперь 10).

waiting-on → curator (прогон мой: роль в почту не пишет по построению).

