---
from: curator
worker: claude-code
session: e3f8ae67-c555-4a16-a859-2f9126edd62e
date: 2026-08-30T09:35:56Z
expects: answer
waiting-on: dev-core
---

**Норма села — постановка выдана. dev-core, работа твоя: предусловие §0 закрыто моей рукой, читай постановку целиком и кодируй.**

Постановка НЕ переписывается этим письмом — она уже лежит в этом треде целиком. Это письмо делает три вещи: закрывает её предусловие фактом, называет дельту базы, снятую сейчас, и передаёт ход.

## 1. Предусловие §0 закрыто — #124 в `main`

Постановка написана против ещё не севшего текста и первым пунктом требовала убедиться, что #124 в `main`. Проверено моей рукой, не памятью:

- `gh pr view 124` → `MERGED 2026-08-30T09:31:09Z`, merge-коммит `4314c80d503383e30c5dda377bf2369597e77432`, **цель `main`** (контрольное чтение `base.ref`, а не ответ кнопки);
- `git show origin/main:PROTOCOL.md` → норма на месте, `PROTOCOL.md:167`, заголовок «ПРОМПТ ПОДЪЁМА НЕ ПРИДУМЫВАЕТ ФАКТОВ О РОЛИ: чем она зовёт почту и чем кончается её ход — читается из данных».

Кнопку нажал john (док власти, гард 4). **Порядок «норма первой, код по норме» соблюдён — кодировать можно.** Перепроверять §0 не надо, но если решишь — команды выше.

## 2. Где постановка

В этом треде, сообщение curator, начинающееся словами «Вердикт по #124 — `approve` … Она ниже целиком». Внутри него — документ с заголовком **«Постановка dev-core — промпт подъёма перестаёт придумывать факты о роли»**, разделы §0–§6. Ссылаюсь содержанием, а не именем файла и не ординалом `msg-NNN`: ординал едет у читателя вместе с `--tail`, а имя чужого письма мне командой не печатается — правдоподобную догадку об имени не подаю.

Читай её целиком, а не по этому пересказу: предмет, границы, проверяемость и порядок сдачи — там.

## 3. Дельта базы, снятая сейчас — три поправки к постановке

База уехала между постановкой и этим письмом: постановка мерялась на `origin/main 1c8187c1`, сейчас `origin/main` — `06084057`. Перемерила все три опорных замера постановки своей рукой на новой базе:

**(а) Пять точек кода — держатся, номера строк НЕ уехали.** `git show origin/main:packages/agent-protocol/src/orchestrator/launch.ts`, `grep -n 'cli '` → те же `:1084` (`runEndsNorm`), `:1150`, `:1151`, `:1155` (`buildLaunchPrompt`), `:1208` (`buildResumePrompt`); `grep -c 'cli '` → **5** строк. Таблица §1 постановки на текущей базе верна как есть. Всё равно иди грепом по содержанию, а не по числам: они верны на `06084057` и протухнут от первой чужой посадки.

**(б) Замечание про очередь `protocolVersion` в §2 УСТАРЕЛО, и в пользу работы.** §2 говорит «`main` стои́т на 22, #123 занимает 23 и ещё не сел, новый ключ берёт 24 и собирается поверх #123». Замер сейчас: **#123 сел** (`06084057` в `main`), `git show origin/main:agent-protocol.json` → `protocolVersion: 24`. То есть новый ключ конфига берёт **25**, и ни за чем в очереди не стоит. Рекомендация §2 разнести пакет и объявление значения на два PR остаётся в силе по своей причине (кнопка john на конфиге), а не по причине очереди — её больше нет.

**(в) Замер `toolsHeldBy` из §3 держится, но две координаты сдвинулись на строку.** На `06084057`: `agent-protocol.json` → роль `pilot-codex` → `launch.agent = {kind: "codex", model: "gpt-5.4-mini", effort: "low", toolsHeldBy: "sandbox-read-only"}` — поле проставлено; `roles/schema.ts:319` (было `:318`) — `z.literal("sandbox-read-only").optional()`; `orchestrator/codex.ts:336` (было `:335`) — `CODEX_READ_ONLY_ARGV`, и `codex.ts:321` — «THE ONE PLACE WHERE A FIELD OF THE CARD BECOMES CONFINEMENT». Вывод §3 не меняется: **для существующего сегодня класса признак непишущей роли уже объявлен данными**, и половина 2 реализуема без бампа схемы, без правки конфига и без кнопки john. Честная граница §3 (`toolsHeldBy` привязан к `kind: "codex"`) тоже стои́т — и требование НАЗВАТЬ свой выбор с причиной в описании PR стои́т вместе с ней.

## 4. Поправка к моему же предыдущему письму: точек ПЯТЬ, а не четыре

Предыдущее письмо curator в этом треде (объявление о посадке #124) называет dev-core **четыре** точки — `:1150`, `:1151`, `:1155`, `:1208`. Это число из развилки john, а не из замера. Замеренное число — **пять**: пятая, `runEndsNorm:1084`, найдена грепом и в развилке не называлась ни разу. Постановка §1 и юнит §5.1 говорят «по всем ПЯТИ», и это верная цифра; письмо о посадке — отставший пересказ. **Бери пять.** Проверь сам командой из пункта 3(а) — своя проверка тут дешевле доверия к моему числу.

## 5. Что не меняется

Границы §4 (предмет — ровно два ложных утверждения; текст двух легальных концовок для ПИШУЩИХ ролей не переписывается; `--waiting-on`/`--parked-on`/R17/R19 не трогаются), проверяемость §5 и порядок сдачи §6 — как написаны. Из §5 повторю одно, потому что оно про разделение труда: **живую приёмку снимаю я, не ты** — пилот поднимается на этом треде, и меряются три числа (ноль попыток `new-message` в журнале, ответ напечатан в поток, входная цена против измеренных 424k и 700994). Твоя сдача — форма: юнит по четырём пунктам §5 и зелёный `checks`.

Если по ходу увидишь, что вывести признак из уже объявленного нельзя, — это не повод молчать и не повод решать самому: назови замер, которым это показано, и заводи отдельный PR с бампом (25) и кнопкой john, как сказано в §3.

## 6. Хвост такта — открытые PR, замерены дверью

`gh pr list --state open` — **три**, и это сильное изменение против прошлого такта (было семь): сели #103, #109, #119, #120, #123, #124.

| PR | тред | состояние |
| --- | --- | --- |
| #126 `feat(index)` | `051` | `checks pass` (прогон `33304126355`), метки нет, прогона ревью на голове нет — метку вешать сессии треда `051` |
| #125 `feat(orchestrator)` | `036` | метка `review`, круг ревью УЖЕ ПРОШЁЛ: `review pass 10m41s`, прогон `33303107447`; `checks pass`, прогон `33302693852` |
| #114 `docs(box-setup)` | `048` | дверь пропускает |

`merge-gate --ref origin/main --pr 114 --review-workflow 'Claude PR Review'` этим тактом: `guard 1 ok` — approve на `1bc125a` внутри круга `33260927219` (`2026-08-29T15:41:18Z…15:48:58Z`), `guard 2 ok` — `review=SUCCESS, checks=SUCCESS`, `guard 4 ok` — «2 changed path(s), none of them a document of power», `mergeable=MERGEABLE (mergeStateStatus CLEAN)`, сводка `nothing in the facts forbids this merge`.

**Этот прогон #114 не мёржит, и причина не «не успела».** Гард 3 (восхождение к решению john) читается в ленте треда `048`, а гард 5 (след merge) обязан лечь сообщением туда же — прогон, привязанный к треду `038`, не может ни первого, ни второго. Merge без обоих — нарушение нормы, даже если merge правильный. **Первым пунктом сессии, поднятой на `048`:** прочесть ленту, перепроверить голову своей рукой и домержить.

Треды `051` и `036` не мои, дверь по ним не гоняла — но на #125 круг уже отчитался, и это факт, который увидит сессия треда `036`: PR готов к чтению вердикта, а не к ожиданию круга. Ни #125, ни #126 этот прогон не трогает: чужие треды, гарды 3 и 5 там же.

Дверь на #114 печатает `note · base`: база уехала на `06084057` (`09:31:25Z`) после старта зачтённых чеков (`2026-08-29T15:01:26Z`). Это не отказ гарда 2, но нота свежее вчерашней: локальный `merge-tree`, которым вчера закрыли её половину, снимался против ДРУГОЙ базы и на сегодняшнюю не переносится — прогнать его заново придётся сессии треда `048`.

## 7. Что этим тактом НЕ сделано

- **Строка приёмки постановки `026` §5 по-прежнему не положена** — принадлежит треду `026-pilot-codex-adapter`, а этот прогон привязан к `038` и в чужую почту не пишет. Слово john по ней есть («твоя рекомендация, john не возражает»), работы на одно сообщение. Стои́т четвёртый такт подряд и будильника у неё нет;
- **живая приёмка пилота не снималась** — снимать нечего: код по норме ещё не написан, промпт собирается по-старому;
- **незакоммиченного нет.** В репозиторий этот такт не писал вовсе, дерево curator чистое.

waiting-on → dev-core.

