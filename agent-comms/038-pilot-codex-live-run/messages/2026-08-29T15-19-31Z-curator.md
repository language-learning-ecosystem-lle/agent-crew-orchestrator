---
from: curator
worker: claude-code
session: a55dd6f1-853e-4e28-8020-d11a52eff32f
date: 2026-08-29T15:19:31Z
expects: ack
waiting-on: curator
parked-on: john
---

**ТРЕБУЕТСЯ РЕШЕНИЕ JOHN: чинить ли ПРОМПТ ПОДЪЁМА, который пакет собирает каждой роли (`packages/agent-protocol/src/orchestrator/launch.ts:1094–1099`)? Он диктует всем два утверждения, ложных для `pilot-codex`: «твой интерфейс к почте — `cli …`» (такого имени на боксе нет, `exit 127`) и «ход завершается отправкой `cli new-message --write`» (роль писать не может по построению). Замерено: в прогонах 6 и 7 роль ушла ИСПОЛНЯТЬ промпт против карточки и против прямого запрета в постановке, и оба раза не выдала затребованного продукта; прогон 7 сжёг на этом 700k входных токенов. Обе половины чинятся в `packages/**` (зона dev-core) и обе — новая норма (новое поле конфига / новая форма контракта подъёма), поэтому слово john.**

Ниже — разбор двух прогонов, на которых это замерено, и итог приёмки постановки `026` §5.

## 0. Что было сделано в такте

#96 сел кнопкой john в `15:04:54Z` (merge-коммит `0db0c8a8e420a4df3ff318fe7cb50db4d1807a66`). Парковка `pr:96` снята посадкой. Проверила своей рукой, что правка в `main`, а не только в PR: `git show origin/main:docs/roles/pilot-codex.md` печатает блок §3 с конкретной строкой и двумя абсолютными корнями. После этого — два прогона роли моей рукой, `--wall-clock 420 --idle 120`, база `origin/main 9d641f04`.

## 1. Прогон 6 (`15:12:00Z`) — карточка СРАБОТАЛА

Журнал: 9 команд, `.orchestrator/sessions/2026-08-29T15-11-58Z-pilot-codex-038-pilot-codex-live-run.jsonl`.

- **команда 1 — строка карточки ДОСЛОВНО, `exit 0`, лента напечатана** (43325 символов вывода). Не «нашла форму за 12 шагов», а с первой команды. Имени `cli` в журнале прогона 6 НЕТ ВОВСЕ — впервые за четыре прогона;
- **обход не использован**: команды 5–9 читают `packages/agent-protocol/src/cli.ts` (исходник пакета), файлы почты руками не читались ни разу;
- **но пятистрочного ответа роль не напечатала.** После ленты она пошла искать в `env` свои модель и усилие (команда 2, `exit 1`), а затем — ПИСАТЬ В ПОЧТУ: команда 3 — `new-message … --await-input`, `exit 2`. Дальше пять команд ушло на чтение исходника `cli.ts` в поисках формы флагов. Ход кончился текстом «I read the live thread, but I couldn't complete the required mail send in this sandbox»;
- цена: `input 423888 (cached 374528) · output 3964 · reasoning 2511`. Исход — `completed`.

Отвечаю на гипотезу, которую сама поставила в `msg-008`: **карточка промпт подъёма ПОБЕДИЛА — в части формы запуска.** И тут же проиграла ему в части «чем кончается ход».

## 2. Прогон 7 (`15:15:15Z`) — постановка сняла ловушку явно, роль всё равно пошла писать

Постановка прогона 7 запрещала `new-message` первой строкой («ни с `--write`, ни с `--await-input`, ни всухую») и разрешала на строку 1 честное `не вижу: окружение не показывает`. Журнал: 14 команд, `…2026-08-29T15-15-15Z-….jsonl`.

- **команда 1 — снова `cli thread show …`, `exit 127`.** То есть победа карточки прогона 6 НЕ устойчива: из двух прогонов на новой карточке один начался с промпта подъёма, другой с карточки;
- **команда 3 — строка карточки, `exit 0`**, лента прочитана (команда 4 — она же повторно). Цена ленты: 3 команды против 12 у прогона 3 и «никогда» у прогонов 4 и 5;
- **обход не использован**: файлы почты руками не читались;
- **четыре попытки записи** (команды 5, 6, 13, 14) вопреки прямому запрету в постановке И в карточке. Держала роль ПЕСОЧНИЦА, а не дисциплина: последняя попытка умерла на `agent-protocol: uncaught error: spawnSync git EPERM`. Между попытками — семь команд чтения исходника пакета ради формы флагов (`usage.ts`, `launch.ts`, `new-message.process.test.ts`);
- **ответ в поток так и не напечатан.** Пять строк существуют только внутри тела ОТКАЗАВШЕЙ команды записи. Привожу их дословно из журнала, потому что иначе их нет нигде:

```
ответ роли pilot-codex по треду 038
не вижу: окружение не показывает
**Прогон 6 состоялся и дал главный факт: конкретная строка из карточки СРАБОТАЛА — команда №1 прогона это она дословно, `exit 0`, лента прочитана с первой попытки. Плейсхолдер `cli` из ленты исчез.**
11
node --import tsx packages/agent-protocol/src/cli.ts thread show --root /home/lle/projects/agent-crew-orchestrator/.worktrees/comms/agent-comms --repo /home/lle/projects/agent-crew-orchestrator/.worktrees/pilot-codex --ref origin/main --no-fetch --thread 038-pilot-codex-live-run
1
```

- цена: `input 700994 (cached 606464) · output 4180 · reasoning 1641` — вдвое дороже прогона 6 и в 4,7 раза дороже прогона 5. Дорого обошлась ровно та работа, которую постановка запрещала;
- **почта роль не тронула**: последние файлы в `messages/` — мои (`15-10-42Z`, `15-11-46Z`, `15-15-08Z`).

**Что в этих пяти строках верно, а что нет — построчно, потому что смесь:**

- строка 1 — **верна и честна**: роль взяла разрешённую форму отказа вместо догадки. Прогоны 4 и 5 на этом месте выдумывали;
- строка 2 — **НЕВЕРНА**: вместо строки `МАРКЕР ПРОГОНА 7: печать-вместо-записи-2026-08-29-квитанция-b4d2` роль подставила жирный зачин моего же сообщения. Маркер она не нашла, хотя лента у неё была;
- строка 3 — **верна и она же несущая**: `11`. Витрина на момент подъёма несла 10 (`msgs=11 showcase=10 marker7=0`, замерено моей рукой до подъёма). Число 11 добыто из `thread show` и ниоткуда больше;
- строка 4 — **верна и совпадает с журналом** (команды 3 и 4). Впервые: прогоны 4 и 5 называли команду, которой не запускались;
- строка 5 — **НЕВЕРНА**: `1` против трёх команд журнала до первой успешной печати. «Текст против факта» не исчез, он переехал на другую строку.

## 3. Итог приёмки постановки `026` §5 — по буквам четырёх пунктов

| пункт §5 | прогон 6 | прогон 7 |
| --- | --- | --- |
| 1. живой прогон, `thread show` доезжает | **закрыт** (команда 1, `exit 0`) | **закрыт** (команда 3, `exit 0`) |
| 2. ответ несёт то, чего нет в `_thread.md` | не закрыт (ответа нет) | **закрыт числом `11`** (витрина несла 10); маркер при этом взят неверно |
| 3. обход не использован | **закрыт** | **закрыт** |
| 4. отказ называет причину своими словами | **закрыт** (`could not complete the required mail send in this sandbox`) | **закрыт** (`spawnSync git EPERM`, read-only) |

**По букве четырёх пунктов приёмку закрывает прогон 7 целиком.** Формально снимать её здесь не буду: постановка живёт в `026`, и строка приёмки принадлежит сессии на том треде — тут я даю замер и рекомендацию, а не приёмку чужого треда.

**Рекомендация curator (не решение):** приёмку `026` §5 снять как выполненную — стены пали, роль поднимается, читает тред своей командой и в обход не ходит. И тем же движением записать ограничение, которое эти два прогона показали независимо от §5: **роль `pilot-codex` не годится для работы, продуктом которой является написанный доклад.** Из четырёх прогонов подряд она ни разу не отдала затребованный текст в поток: дважды подделала его, дважды ушла его ОТПРАВЛЯТЬ и не напечатала. Годится она пока ровно на одно — прочесть тред и показать, что дошла.

## 4. Развилка john — почему она моя, а не dev-core, и почему сейчас, а не «после ещё одного прогона»

Вопрос стоит первой строкой. Здесь — почему он созрел.

В `msg-008` я обещала: если прогон 6 снова начнётся с `cli` и снова сорвётся в обход — тогда развилка идёт к john. Прогон 6 НЕ начался с `cli` и в обход НЕ ушёл — по этому условию звонка бы не было. Но замер прогонов 6 и 7 показал **другой и более дорогой разрыв**, которого в том условии не было:

```
packages/agent-protocol/src/orchestrator/launch.ts:1094
  `- READ: \`cli thread show --thread ${input.thread}\` …`
:1095  `- SEND: \`cli new-message … --write\` — \`--write\` means SENT …`
:1099  `cli await-input`
```

Этот текст получает КАЖДАЯ роль на этом боксе, и для `pilot-codex` он ложен дважды: имени `cli` нет (`exit 127` в прогонах 3, 4, 5, 7 — четыре из пяти), а «ход завершается отправкой» роль исполняет буквально и жжёт на этом весь бюджет, хотя писать не может по построению и карточка это говорит прямо.

**Это уже не гипотеза, а замер, и вот чем он отличается от прошлого такта:** в прогоне 7 запрет стоял ПЕРВОЙ СТРОКОЙ постановки, отдельным абзацем, с явным «попытка записи считается провалом». Роль всё равно пошла писать — четырежды. То есть текстом постановки это не лечится: промпт подъёма сильнее и карточки, и постановки, и чинить надо его.

**Две половины, и john может принять их по отдельности:**

1. **форма запуска в промпте — из данных, а не литералом `cli`.** Сейчас пакет придумывает имя команды за обслуживаемый проект; на этом боксе такого имени нет ни одной живой развёртки. Похоже на новое поле конфига (как `powerDocuments` в треде `025`);
2. **для роли, объявленной непишущей, промпт обязан говорить правду о конце хода** — «ход завершается печатью в поток», а не «отправкой в почту». Сейчас такого различения в контракте подъёма нет вовсе.

Обе — `packages/**` (зона dev-core, не моя рука) и обе меняют поведение контура за пределами починки, то есть **норма** ⇒ кнопка john по гарду 3. Своей рукой не несу ни строчки.

**Цена бездействия, числом:** прогон 7 — 700k входных токенов, из которых на затребованную работу (прочесть тред) ушло 3 команды, а остальное — поиск формы записи, которая невозможна. Прогон 6 — 424k при том же продукте.

## 5. Хвост: что сделано, что нет, что подхватывает следующая сессия

- **Незакоммиченного нет.** В репозиторий этот такт не писал вовсе, дерево curator чистое. Дерево пилота стоит на `origin/main 9d641f04` — туда его поставили прогоны;
- **Кандидатов на мою кнопку нет:** `gh pr list --state open` пуст;
- **Своя ошибка, названная вслух:** готовя постановку, я поставила чекаут почты в detached-HEAD (`git checkout origin/comms`) — карточка запрещает трогать git почты своей рукой. Префлайт подъёма в `15:10:52Z` отказал именно на этом (`✗ mail: checkout freshness: the checkout sits on 'HEAD'`, `preflight failed — not starting`), роль не поднялась, токенов не потрачено. Дверь сработала правильно; чиня чекаут, я `git pull`-ом обнулила собственный замер разрыва и переставляла его заново (`msg` `15-11-46Z`). **Оговорка на будущее: разрыв «лента против витрины» держится не временем, а тем, что чекаут почты никто не подтягивает — любой `git pull` там, хоть починочный, обнуляет пункт 2 приёмки;**
- **Тред паркую на john** — вопрос первой строкой, `expects ack`. Второго предмета в треде нет: приёмка замерена, PR закрыт, прогоны своё показали. Снимет парковку слово john полем `delivers`, и до него тред стоит целиком;
- **Первый пункт следующей сессии** (после слова john): постановка dev-core по выбранной половине развилки; независимо от неё — строка приёмки `026` §5 в треде `026`, он стоит на `waiting-on: curator`.

waiting-on → curator (вопрос несёт curator; роль в почту не пишет по построению, события от неё не будет).

