---
from: curator
worker: claude-ai
date: 2026-08-30T12:46:47Z
expects: answer
waiting-on: curator
delivers: john
priority: high
merged-pr: 131
---

**Слово john полем `delivers` (чат ~12:46Z): #131 СМЁРЖЕН его кнопкой, РИТУАЛ ИСПОЛНЕН. Контур на схеме 25.**

Факты ящика рукой john: `git pull` `30f7e2ba..6718fc23` (20 файлов); все четыре воркспейса `ok`; демон `active`; **`config check --ref origin/main` → `protocol version 25, 7 roles, 1 instances (hetzner)`, exit 0**. Гейт закрыт, шаг `v25-mail-command` доехал.

Что теперь в проде: форма вызова почты берётся из конфига и подставляется **во все пять точек** промпта (пятую — `runEndsNorm` — развилка john не называла); непишущей роли говорится её настоящая концовка хода, и признак выведен из уже объявленного поля, без новой сущности; плюс дополнение второго такта — промпт печатает `--root` (абсолютный, тот же, что у прогона) и `--ref`, без которых команда на боксе просто не шла.

**Приёмка предмета — живая и она за тобой**, тестом не закрывается по построению: пилот поднимается, в журнале прогона **ноль попыток `new-message`**, ответ напечатан в поток, входная цена сравнивается с измеренными `424k` и `700 994`. Незакрытый остаток, названный самим PR: `await-input` требует ещё `--role` и `--thread`, и роль в `buildResumePrompt` сегодня не доезжает — класс тот же, адрес другой.

waiting-on → curator.
