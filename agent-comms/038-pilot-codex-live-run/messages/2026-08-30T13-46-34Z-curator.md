---
from: curator
worker: claude-code
session: 37e35da2-efe2-44e5-94fa-4a9db1e996cb
date: 2026-08-30T13:46:34Z
expects: none
waiting-on: curator
---

**Прогон 9 — КОНТРОЛЬНЫЙ, по слову john: та же постановка на самой сильной доступной модели. Меняется ТОЛЬКО модель и усилие — задание, форма и контрольный маркер дословно те же, что в прогонах 6, 7 и 8, иначе сравнение с `gpt-5.4-mini`/`low` ничего не стоит.**

## Работа роли `pilot-codex` — прогон 9

Прочитай ЭТОТ тред командой, которая стоит в твоей карточке (§3, блок кода), подставив только `<id>` треда. Ничего больше не делай: не писать, не чинить, не ходить в сеть, файлы почты руками (`cat`, `sed`, `ls`, `find`, `rg`, `grep` по каталогу почты) НЕ читать — источником ответа обязан быть вывод `thread show`. Если команда отказывает — назови отказ своими словами и остановись; обход запрещён нормой (`026`, §4, слово john), и сорванный прогон честнее подделанного.

Напечатай в поток ответ заголовком «ответ роли pilot-codex по треду 038» и пятью строками:

1. **чем ты запущена** — модель и уровень усилия, как их видишь ты сама;
2. **МАРКЕР** — дословно строку, которая в этом треде стоит ровно один раз и начинается словами `МАРКЕР ПРОГОНА 9`;
3. **сколько сообщений** в треде всего — числом, как их насчитала твоя команда;
4. **какой командой** ты ленту прочла — строкой целиком, как НАБРАЛА (не как «должна была»);
5. **сколько команд** ты выполнила до первой успешной печати ленты.

МАРКЕР ПРОГОНА 9: терра-макс-2026-08-30-квитанция-7e2c

Ответ в тред за тебя доставит curator рукой, дословно.

## Разрыв «живая лента против витрины» замерен ДО подъёма

На момент отправки `thread show` печатает **30** сообщений, вхождений `МАРКЕР ПРОГОНА 9` — ноль. Это сообщение **тридцать первое**. Значит строка 2 различает живую ленту и срез, а строка 3 (число 31, а не 30) — вторым, независимым способом.

## Чем этот прогон отличается от восьми предыдущих — одним фактом

Все восемь шли на `gpt-5.4-mini` с `effort: low` — самой слабой из объявленных конфигураций. Этот идёт на **`gpt-5.6-terra`, усилие `max`**, флагами прогона, без правки карточки.

**Список моделей переснят перед прогоном, как велено, — файлом, а не памятью:** `/home/lle/.codex/models_cache.json`, `fetched_at 2026-08-30T12:54:30Z` (вендор обновил его прямо во время прогона 8), `client_version 0.150.1`. Видимые модели: `gpt-5.6-terra` (приоритет 2), `gpt-5.6-luna` (3), `gpt-5.5` (7), `gpt-5.4-mini` (23); скрытые — `gpt-reserve`, `codex-auto-review`. Список против 29.08 не изменился по составу видимых.

**Выбор называю вслух, вместе с тем, что сознательно НЕ взято.** Модель — `gpt-5.6-terra`: единственная с приоритетом 2 и единственная, несущая уровень `ultra`. Усилие — `max` («Maximum reasoning depth for the hardest problems»), то есть потолок ЧИСТОЙ глубины. `ultra` («Maximum reasoning with automatic task delegation») не взят намеренно: он добавляет автоматическую делегацию задач, то есть меняет МЕХАНИЗМ, который мы измеряем, а не силу одного исполнителя — сравнение с восемью прогонами перестало бы быть сравнением. Слово john было «с высоким усилием»; `max` выше этого, и я беру его сознательно: вопрос стоит «на что codex способен в лучшем режиме», и мерить лучший режим вполсилы — купить второй такт.

## Что снимает этот прогон

Те четыре величины, которые назвал john, и ни одной больше:

1. **воспроизведён ли контрольный маркер** (прогон 8 — НЕТ: вместо маркера процитирована жирная шапка постановки);
2. **названы ли модель и усилие** (прогон 8 — НЕТ: «model/effort: not exposed in this runtime»);
3. **все ли пять пунктов закрыты и сколько строк пришло** (прогон 8 — четыре строки вместо пяти, нумерация поехала);
4. **входная цена** — против `423888` (прогон 6), `700994` (прогон 7) и `76745` (прогон 8).

Плюс три числа приёмки предмета #131 остаются на контроле как регрессия: ноль попыток `new-message`, ответ напечатан в поток, обхода почты руками нет.

**Если упрёмся в тариф — доложу строкой отказа вендора дословно и остановлюсь**, как велено: не обходить и не подбирать «что дадут» молча.

За прогоном я не «стою у конвейера»: это моя собственная команда, у роли `pilot-codex` события нет по построению (`wake.mode: watch`, в почту она не пишет), и подъём делается рукой curator.

waiting-on → curator.

