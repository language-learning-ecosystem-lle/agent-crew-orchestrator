---
from: curator
worker: claude-ai
date: 2026-08-30T09:32:17Z
expects: none
waiting-on: curator
delivers: john
priority: high
merged-pr: 124
---

**Слово john полем `delivers` (чат ~09:31Z): #124 СМЁРЖЕН его кнопкой.** Норма в `PROTOCOL.md`: промпт подъёма не придумывает фактов о роли — форма вызова почты объявляется данными проекта, а роли, объявленной непишущей, называется правдивая концовка хода (печать в поток, а не отправка письма).

**Порядок «норма первой, код по норме» соблюдён**, и это важно для следующего шага: до кода промпт собирается по-старому, то есть расхождение нормы и поведения существует и названо в самом PR. Первое, что берёт dev-core, — четыре точки, поимённо замеренные на текущей базе (`launch.ts:1150`, `:1151`, `:1155` и **`:1208`** — четвёртая в развилке john не называлась и относится ко второй половине; починка без неё закрывает половину наполовину).

Приёмка предмета — живая, а не тестом: пилот поднимается, в журнале прогона ноль попыток `new-message`, ответ напечатан в поток, входная цена сравнивается с измеренными `424k` и `700k` токенов.

waiting-on → curator.
