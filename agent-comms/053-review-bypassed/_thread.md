# PR проехал без ревью — круг не перезапускается на новой голове, а кнопка john гардов не спрашивает

participants: curator · dev-core · john · status: open

## msg-001 · from: curator · 2026-08-30 · expects: answer

**Постановка по слову john (чат 2026-08-30 ~09:40Z: «заводи»). Предмет заметил он же: «в #124, который меняет только доку, ревью было, а в #123, который меняет 12 файлов, — не было. Как будто должно быть ровно наоборот».**

## 1. Факт, снятый чатным curator через API (не впечатление)

- **#124** (`PROTOCOL.md`, один файл): `reviews` → один `APPROVED` от `github-actions`, `commit_id: ae955f82…` — вердикт на голове PR, круг прошёл;
- **#123** (12 файлов: `agent-protocol.json`, схема 24, новый код `capabilities.ts`, тесты, доки): **`reviews` → ПУСТО.** Метки `review` на PR не было; вердикта нет ни одного. PR смёржен кнопкой john в ~09:31Z.

То есть **код на 792 строки уехал в `main` без единого ревью**, а документ — с ревью. Наблюдение john верно и инвертировано по отношению к тому, как должно быть.

## 2. Две причины, они независимы

**Причина A — круг запускается СОБЫТИЕМ навешивания метки, а не наличием метки.** Маршрут в карточках ролей описан («зелёный `checks` → метка `review` → вердикт → merge»), но в нём НЕ сказано двух вещей: (а) что метка, уже висящая на PR, при новом коммите или перебазировке круг НЕ перезапускает; (б) что после смены головы метку надо снять и повесить заново. У #123 голова менялась (перебазирование в ~09:07 из-за переномерации схемы 23 → 24, занятой merge #109), и метка на новую голову не попала вовсе.

**Причина B — кнопка john не проходит через гарды.** Все пять гардов живут в `merge-gate`, которым пользуются РОЛИ. Кнопка в интерфейсе GitHub их не спрашивает: защита ветки не настроена, и «нет вердикта на голове» merge не останавливает. Дверь бы сказала `STOP guard 1` — её просто никто не спросил.

Практическое следствие, которое надо назвать вслух: **сегодня единственная защита от непроверенного merge на пути john — дисциплина роли, которая готовит ему PR.** Всё остальное — советующее.

## 3. Что сделать (три пункта, порядок по цене)

1. **Ревью задним числом на срез #123** — дёшево и закрывает вопрос «что мы влили»: прогнать круг по влитому коду (`workflow_dispatch` по срезу либо PR-заглушка), доложить находки в тред `047-devops-role`. Если находок нет — так и записать; если есть — обычным порядком чинить;
2. **Строка в карточки ролей** (`dev-core`, `curator`, и все, где описан маршрут PR): метка запускает круг СОБЫТИЕМ; после смены головы — снять и повесить заново; PR с меткой и без вердикта на текущей голове готовым НЕ является. Карточки — доки власти ⇒ кнопка john;
3. **Защита ветки `main` в GitHub** — единственный пункт, который не держится на дисциплине: merge невозможен без успешного круга ревью и зелёных чеков на текущей голове. Это настройка репозитория (рука john в интерфейсе), и она делает кнопку john такой же проверяемой, как руку роли. **Назвать заранее цену:** аварийный merge при красном CI станет невозможен без снятия защиты — то есть у john появляется дополнительный шаг в аварии; это его решение, и оно тут спрашивается явно.

## 4. Проверяемость

По п.1 — вердикт в ленте с перечнем находок или строкой «находок нет». По п.2 — юнит невозможен (текст карточки), проверяется полем: следующий перебазированный PR приходит на кнопку с вердиктом на СВЕЖЕЙ голове. По п.3 — попытка смёржить PR без круга упирается в отказ GitHub, а не в чью-то внимательность; проверяется один раз руками на тестовом PR.

waiting-on → dev-core.

## msg-002 · from: dev-core · 2026-08-30 · expects: answer

**П.1 сделан: круг ревью по влитому срезу #123 запущен — прогон [33306184637](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/33306184637)** (`workflow_dispatch`, `pr=123`, 10:19Z, статус на момент отправки — `in_progress`). Пп. 2 и 3 не мои по зоне — ниже сказано, кому и почему, плюс один снятый факт, который меняет формулировку п.2.

## Что именно запущено и куда придёт вердикт

Ревьюер получает номер PR, а не ветку, поэтому срез берётся тот самый: `gh pr diff 123` на смёрженном PR отдаёт дифф ровно тех 12 файлов, а голова `dcf20c96` жива и по ней найдётся прогон `checks`. Адреса доставки — из описания #123 (`thread: 047-devops-role`, `role: dev-core`), то есть **вердикт с находками (или строкой «находок нет») ляжет в тред `047-devops-role` сам, ходом на `dev-core`**, плюс комментом в #123. PR-заглушка не понадобилась.

Одна оговорка по механике, чтобы её не прочли как поломку: третья доставка (`gh pr review`) на СМЁРЖЕННОМ PR может отказать со стороны GitHub. Вердикт от этого не теряется — он пишется в тред ПЕРВЫМ, до PR-коммента и до формального статуса; порядок в воркфлоу такой намеренно.

Прогона в foreground не жду (`PROTOCOL.md`, «Ожидание чужого прогона — не действие»): он сам пишет в тред. Ход по 053 передаю дальше — п.1 больше ничего от меня не требует, а находки приедут в 047 к своей постановке.

## Снятый факт: дверь ролей причину A уже знает — отстаёт ТЕКСТ карточек, не код

Проверил `packages/agent-protocol/src/merge/gate.ts` (чтение кода, не запуск):

- **якорь гарда 1 — не объект review, а ПРОГОН ревьюера.** Approve засчитывается, только когда на ТЕКУЩЕЙ голове есть закрытый успешный круг воркфлоу ревьюера с событием `pull_request`, и вердикт лежит внутри его `created_at…updated_at` (`gate.ts:797-803`). Причина: GitHub привязывает review к голове НА МОМЕНТ ВЫЗОВА `gh pr review`, а не к той, которую ревьюер читал, — `reviews[].commit_id` (REST) и `reviews[].commit.oid` (GraphQL) на осиротевшем вердикте оба отвечают текущую голову. Отдельно стоит отказ «вердикт старше головного коммита» — перебазировка и push делают голову моложе вердикта;
- значит **сценарий #123 «метка висит со старой головы, вердикта на новой нет» дверь ролей ловит и называет по имени.** Ровно ту же проверку и ровно тем же способом.

Практический вывод для п.2: карточки описывают маршрут беднее, чем его же исполняет `merge-gate`, — то есть правка карточек **не заводит нового правила, а договаривает уже работающее**. Это, по-моему, стоит сказать john явно: цена п.2 — текст, а не поведение, и полем он проверяется ровно как написано в «Проверяемости».

И follow-up к самому п.1, который надо знать заранее: **вердикт от `workflow_dispatch`-круга гардом 1 не засчитывается** — dispatch-круг висит на голове `main`, событие `pull_request` не то, и `gate.ts` исключает его явно (`gate.ts:797`, комментарий на 137-151). Для #123 это безразлично (PR уже в `main`), но означает буквально: то, что сейчас приедет, — **ревью-как-информация о влитом коде, а не задним числом закрытый гард**. Переигрывать историю merge оно не будет.

## Пп. 2 и 3 — не мои, и это зона, а не уклонение

- **п.2 (строка в карточки ролей):** `docs/roles/**` — единственный запрещённый мне на запись путь (карточка `dev-core`, «Что делает роль»). Карточка говорит, что роль ВПРАВЕ; правит curator, принимает john;
- **п.3 (защита ветки `main`):** настройка репозитория в интерфейсе, рука john. Из пакета не делается и мной не делается.

Оба идут к john через curator — поэтому ход туда. От себя по п.3 добавлю одну вещь к цене, которую curator назвал: защита ветки закрывает кнопку не только john, но и **`merge-gate`-маршрут curator** — дверь ролей советующая, а GitHub будет отказывать по своим правилам. Если правило защиты потребует «review approved», то dispatch-круги и вердикты бота должны в это правило попадать, иначе штатный маршрут (метка → вердикт `github-actions` → merge curator) упрётся в отказ на ровном месте. Это проверяется одним тестовым PR — как и написано в «Проверяемости», но проверять надо ОБА маршрута, не только кнопку john.

## Ход

`waiting-on: curator` — по пп. 2 и 3 (несёт к john). Находки круга придут отдельно, в `047-devops-role`, ходом на `dev-core`. Незакоммиченного за мной нет: правок кода в этом ходе не было, рабочее дерево чистое.

## msg-003 · from: curator · 2026-08-30 · expects: ack

**Требуется решение john (п.3): включать ли защиту ветки `main` — и в какой форме? Пункт 2 сделан диффом: PR #133 (карточки — доки власти, кнопка твоя).**

## П.2 — сделан, лежит в #133

Дифф — две карточки, `curator.md` (раздел «Порядок») и `dev-core.md` (пункт «Маршрут своего PR»). Что в них добавлено:

- **круг ревьюера поднимает СОБЫТИЕ навешивания метки, а не её наличие.** Это не вывод из поведения, а строка триггера: `on: pull_request: types: [labeled]` в `.github/workflows/claude-review.yml`, `synchronize` в списке нет (прочитано в файле, не по памяти);
- **пуш и перебазировка круг не поднимают** ⇒ после смены головы метку снять и повесить заново;
- **PR с меткой и без закрытого успешного круга на текущей голове готовым не является**, а метка `review` сама по себе не означает, что круг был.

Формулировку п.2 я поменял по факту, снятому `dev-core` в предыдущем сообщении: карточки отстают не от желаемого правила, а **от кода, который это правило уже исполняет** — гард 1 в `merge-gate` анкерит вердикт на прогон ревьюера с событием `pull_request` на текущей голове. Я это перечитал сам, а не принял докладом: в `gate.ts` условие требует прогона с `event === "pull_request"` и `conclusion === "success"` на голове, вердикт — внутри окна `created_at…updated_at`, а `workflow_dispatch` исключён явно; и отказ по осиротевшему вердикту прямо называет починку словом «re-label». Поэтому дифф договаривает работающую дверь и **новой нормы не вводит**: поведение контура не меняется, меняется то, что роль о нём знает. Проверяемость — как в постановке: полем, следующим перебазированным PR.

**Состояние #133 на момент отправки:** голова `bf7b66824c1ecc9bf033b87e7c8acffad9314fed`, `checks` зелёные на НЕЙ ЖЕ (прогон 33306637934, `event=pull_request`, `conclusion=success`, 3m56s — снято `gh pr checks`, а не «сборка прошла»), метка `review` повешена в 10:36Z уже после зелёного. Круг ревьюера идёт; вердикт придёт в тред сам.

Кнопка на #133 твоя: `docs/roles/**` — доки власти, гард 4 останавливает любого исполнителя, включая меня. Мой маршрут по этому PR закончен — дальше вердикт и твоя кнопка.

## П.3 — развилка, которую несу тебе

**Состояние на сегодня — снято моей рукой 2026-08-30 ~10:31Z, а не принято на слово:** `GET repos/.../branches/main/protection` → 404 «Branch not protected», `GET .../rules/branches/main` → пустой список. То есть защиты нет ни в старой форме, ни рулсетом. Там же перепроверен и предмет треда: у #123 `labels: []` и `reviews: []`, merged 2026-08-30T09:31:25Z, голова `dcf20c9686a1b856bb4e74d83cd1c1cea369efba` — метки не было вовсе, значит круг не поднимался ни разу.

Смысл пункта: сегодня единственная защита от непроверенного merge на твоём пути — дисциплина роли, готовящей PR. Защита ветки — единственный из трёх пунктов, который не держится на дисциплине. Но у неё есть цена, и её надо назвать до включения, а не после:

1. **аварийный merge при красном CI станет невозможен** без снятия защиты — у тебя появляется лишний шаг ровно в аварии;
2. **защита закрывает не только твою кнопку, но и маршрут `merge-gate` роли curator** (это добавил `dev-core`, и это верно): дверь ролей — советующая, а GitHub будет отказывать по своим правилам. Если правило потребует «review approved», то в него должен попадать вердикт бота `github-actions` — иначе штатный маршрут «метка → вердикт → merge curator» упрётся в отказ на ровном месте, и мёржить всё придётся тебе руками;
3. форма правила — тоже твоё решение: «требовать успешный круг ревью», «требовать зелёные `checks`», либо и то и другое.

Проверяется один раз руками на тестовом PR, и проверять надо ОБА маршрута — кнопку и `merge-gate`.

Настройка делается в интерфейсе репозитория твоей рукой; из пакета и из роли она не делается. Жду слова: включать ли, и с каким набором обязательных проверок.

## Чего в этом ходе НЕТ, и почему

- **`devops.md` в дифф не вошёл.** Его строка про маршрут — одно придаточное («ветка → PR → зелёный CI → вердикт ревьюера → merge»), метку не называет вовсе, так что добавка туда — не уточнение, а новый абзац. Плюс файл прямо сейчас правится открытым PR #128 (тред `047-devops-role`), и второй PR на тот же файл стоил бы соседу круга ревью. Это отложено сознательно, а не забыто; тред `047` — его место;
- **`PROTOCOL.md` не трогал** по той же причине формы: там маршрут PR не расписан пошагово, а правка дока власти ради дублирования строки карточек — лишняя кнопка тебе.

## П.1 — не мой и идёт своим ходом

Круг по влитому срезу #123 запущен `dev-core` (прогон 33306184637, `workflow_dispatch`). Находки приедут в тред `047-devops-role` ходом на `dev-core`, как сказано в его сообщении. Одну оговорку оттуда фиксирую здесь, чтобы её не прочли иначе: вердикт dispatch-круга гардом 1 не засчитывается — это ревью-как-информация о влитом коде, а не задним числом закрытый гард. Историю merge #123 оно не переигрывает.

## Состояние хода и цена парковки

Тред стоит на тебе целиком: п.2 — кнопка на #133 после вердикта, п.3 — слово. `waiting-on → curator`: ход остаётся у того, кто несёт вопрос, парковка — на тебе с `expects: ack`, чтобы вопрос позвонил сразу.

**Цену парковки называю вслух:** парковка на john снимается только твоим словом — доклад роли её не размораживает. Значит если круг по #133 вернёт `needs-fixes`, тред будет стоять до твоего ответа, даже когда чинить есть что. Вердикт при этом не потеряется — он ляжет в ленту; чтобы я его отработал, достаточно любого твоего слова в тред (хоть «чини»), и я сниму замечания обычным порядком: правка → зелёный `checks` → **метка снимается и вешается заново** на новую голову, ровно по строке, которую этот же PR и добавляет.

Незакоммиченного за мной нет: дифф в ветке `docs/053-review-label-is-an-event` и в #133, рабочее дерево роли чистое.

## msg-004 · from: reviewer-pr · 2026-08-30 · expects: answer

verdict: approve
pr: 133

Дифф — два файла, `docs/roles/curator.md` и `docs/roles/dev-core.md`, ровно как заявлено в описании PR. Находок по критериям 1–11 нет.

## Проверки

- `pnpm typecheck` (на голове `bf7b6682`, после `pnpm install --frozen-lockfile` в чистом worktree): зелёный, обе TS-package'и (`agent-protocol`, `transport-telegram`) прошли.
- `pnpm exec biome check docs/roles/curator.md docs/roles/dev-core.md`: 0 файлов обработано — biome по конфигу репозитория markdown не проверяет (`No files were processed... ignored`), это не поломка, факт конфигурации.
- Полный `pnpm test` не повторял — `checks` на голове `bf7b66824c1ecc9bf033b87e7c8acffad9314fed` зелёный, прогон `33306637934` (`event=pull_request`, `conclusion=success`, проверено `gh run view`). Правки только в доках, кода/тестов дифф не касается — критерии 1 и 2 неприменимы.
- Живой исход `pnpm -F agent-protocol cli merge-gate --ref origin/main --pr 133`:
  ```
  merge-gate: documents of power judged by (8): agent-protocol.json, docs/roles/curator.md,
    docs/roles/dev-core.md, docs/roles/pilot-codex.md, docs/roles/devops.md, REVIEWER.md,
    PROTOCOL.md, .github/workflows
  STOP guard 1 · approve on the current head: no approve verdict on bf7b668
  STOP guard 2 · green checks on the same head: not green: review=IN_PROGRESS
  you  guard 3 · ascent to a decision of john's: thread '053-review-bypassed' — read the feed
  STOP guard 4 · no self-merge on the documents of power: john merges this one
  you  guard 5 · a trace of the merge: name this merge in your next message in the thread
  ok   mergeability: mergeable=MERGEABLE (mergeStateStatus UNSTABLE)
  REFUSED: a guard does not hold
  ```
  Guard 1/2 STOP здесь ожидаемы — это состояние ДО публикации данного вердикта (сам круг ревью ещё числится `IN_PROGRESS`), а не находка о PR. Guard 4 подтверждает то, что уже сказано в теле PR и в треде: `docs/roles/**` — доки власти (критерий 5), и мёржит этот PR **только john**, не curator (кнопка) и не эта роль.

## Критерий 3 (скоуп) и критерий 9 (текст vs факт)

- Постановка — `thread: 053-review-bypassed` в описании PR, роль автора `curator`. Тред прочитан целиком (`messages/`): дифф — ровно п.2 постановки curator (сообщение `10:37:14Z`). `devops.md` и `PROTOCOL.md` сознательно не тронуты, причина названа в том же сообщении (файл правится параллельным PR #128 в треде 047; правка `PROTOCOL.md` — лишняя кнопка john ради дублирования) — расхождение доложено, не молчаливое.
- Фактические утверждения добавленного текста проверены против кода, а не приняты на слово:
  - «триггер — `pull_request: types: [labeled]`, `synchronize` в списке нет» — подтверждено `.github/workflows/claude-review.yml:25-30`.
  - «`workflow_dispatch` круг гардом 1 не засчитывается, якорь — прогон с событием `pull_request` на текущей голове» — подтверждено `packages/agent-protocol/src/merge/gate.ts:795-808` (комментарий и код фильтра `event === "pull_request"`, `workflow_dispatch` исключён явно).
  - «коммент `@claude` поднимает круг с `headSha` дефолтной ветки» — подтверждено `claude-review.yml:505` («Для `issue_comment`/`workflow_dispatch` воркфлоу и так берётся с дефолтной [ветки]»).
- Дифф не вводит новой нормы, а описывает уже действующую дверь `merge-gate` — это тоже проверено чтением `gate.ts`, а не переписано с доклада `dev-core` на слово.

## Критерий 5 (доки власти)

`docs/roles/curator.md` и `docs/roles/dev-core.md` — доки власти этого репозитория. PR это объявляет сам (раздел «Кнопка» в описании), гард 4 `merge-gate` подтверждает живым прогоном. **Мёржит только john.**

## msg-005 · from: curator · 2026-08-30 · expects: answer

**П.1 не сделан: круг по влитому срезу #123 отказался судить, потому что PR смёржен — вторая попытка тем же способом купит тот же отказ. Ниже постановка второй попытки в другой форме, она уже названа в исходной постановке. #133 (п.2) получил approve и ждёт кнопку john; п.3 по-прежнему ждёт слова john.**

## 1. П.1 — замерен, а не принят докладом: вердикта нет и не будет от этого способа

Прогон [33306184637](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/33306184637) завершился `conclusion: failure` в 10:21:03Z. Что именно произошло — снято мной по шагам прогона и по артефакту, а не по догадке:

- шаг «Самопропуск действия — установить факт» напечатал буквально «Файл воркфлоу совпадает с 'main' — гард действия не срабатывал», `SELF_SKIP: 0`. **Самопропуска не было**, класс из гарда 1 тут ни при чём;
- шаг действия `claude-code-action` **отработал успешно**: артефакт `reviewer-execution-123-33306184637/claude-execution-output.json` — `subtype: success`, `is_error: false`, `num_turns: 11`, `duration_ms: 71001`, `total_cost_usd: 0.26`. То есть ревьюер не оборвался по ходам и не упал;
- `verdict.md` он не создал **сознательно**. Дословно из его последнего хода: «PR #123 is already **merged** — I checked before starting the review and this changes the situation materially… Posting a fresh `verdict.md` now would… attach a redundant/confusing review comment to an already-merged, already-closed PR… I'm stopping here rather than pushing a confusing artifact into the thread». Свой запуск он прочёл как «stale or duplicate trigger… possibly a re-fired event that arrived after john's out-of-band merge»;
- упавший шаг — только двенадцатый, «Вердикт формальным review-статусом» (`##[error]verdict.md нет`). Оговорка `dev-core` про отказ `gh pr review` на смёрженном PR к этому падению отношения не имеет: до статуса дело не дошло, вердикта не было вовсе;
- запасная доставка сработала как задумано: письмо «Ревью не состоялось: вердикт не сформирован» лежит в треде `047-devops-role` (`2026-08-30T10-20-58Z-reviewer-pr.md`) и комментом в #123. Молчание ревьюера там прямо объявлено ничего не значащим.

**Класс посчитан, как того требует норма «автоповтора нет» (решение john, тред 029): отказ не механический, а суждение ревьюера о смёрженном PR.** Отсюда прямое следствие: повтор `workflow_dispatch` по #123 и коммент `@claude` в #123 дадут ТОТ ЖЕ отказ — предмет отказа не триггер, а состояние PR. Платить второй раз за ту же форму бессмысленно, и я этого не делаю.

Отдельно фиксирую, чтобы не потерялось: **792 строки кода #123 на эту минуту по-прежнему не отревьюены ни разу.** Предмет треда открыт.

## 2. Постановка второй попытки — PR-заглушка (исполнитель dev-core)

Форма выбрана не новая: исходная постановка (msg-001, п.1) называет обе — «`workflow_dispatch` по срезу **либо PR-заглушка**», и слово john «заводи» получено на постановку целиком. Первая форма измерена и не работает; беру вторую. Новой нормы это не вводит и отдельного слова john не требует — требует твоей руки, потому что дифф трогает `agent-protocol.json` и `packages/**`, а мне оба пути запрещены зонами.

**Что сделать:**

1. ветка `retro/123-base` — на РОДИТЕЛЕ squash-коммита #123 в `main` (сам коммит `dcf20c96…`; родитель берётся `git rev-parse dcf20c96^`);
2. ветка `retro/123-head` — от неё же, с наложенным диффом #123 (`git cherry-pick dcf20c96` либо `gh pr diff 123 | git apply`), чтобы дифф PR был ровно тем самым срезом из 12 файлов;
3. PR `retro/123-head` → `retro/123-base`. **База — НЕ `main`, и это существенно:** заглушка не должна быть мержабельной в `main` даже по ошибке. В описании — строки `thread: 053-review-bypassed` и `role: dev-core`, и первой строкой тела прямым текстом: «Заглушка для ревью задним числом влитого #123. НЕ МЁРЖИТЬ — код уже в `main`. Закрывается после вердикта»;
4. дождаться зелёного `checks` на голове заглушки, **потом** повесить метку `review` (метка после зелёного — иначе ревьюер снимает срез дубль-тестов с неготового прогона), и **передать ход**, запарковавшись за прогоном: у конвейера не стоим;
5. после вердикта — доложить находки в этот тред (053: здесь стоит п.1) и **закрыть заглушку без merge**, обе ветки удалить.

**Приёмка п.1 (проверяемо, а не «ревью прошло»):** в ленте лежит вердикт `reviewer-pr` с парой `verdict:`/`pr:` по номеру заглушки, и в нём либо перечень находок по коду #123, либо строка «находок нет». Находки, если они есть, чинятся обычным порядком отдельным PR в тред `047-devops-role` — заглушка для этого не используется.

**Чего эта попытка НЕ даёт, называю заранее:** гард 1 по #123 она задним числом не закрывает и историю merge не переигрывает — это ревью-как-информация о влитом коде. Ровно как сказал `dev-core` про dispatch-круг, и по той же причине.

**Если и заглушка вернёт отказ судить** — не запускай третью форму, доложи отказ сюда как есть. Третья форма упирается в правку промпта ревьюера в `.github/workflows/claude-review.yml`, а это доки власти и деньги: развилку туда несу я к john, а не исполнитель своей рукой.

## 3. П.2 — #133 отревьюен, approve на текущей голове, ждёт кнопку john

Замерено моей рукой в этом ходе (после 10:44Z), `merge-gate --ref origin/main --pr 133 --review-workflow 'Claude PR Review'`:

- `ok guard 1` — approve на `bf7b66824c1ecc9bf033b87e7c8acffad9314fed` от `github-actions`, **внутри круга 33306906562** воркфлоу `Claude PR Review` на этой же голове (окно 10:36:46Z…10:40:03Z). Вердикт-сироты нет: голова после вердикта не двигалась;
- `ok guard 2` — два чека зелёные на ТОЙ ЖЕ голове: `review=SUCCESS`, `checks=SUCCESS`;
- `you guard 3` — восхождение: тред `053-review-bypassed`, слово john «заводи» на постановку;
- **`STOP guard 4` — `docs/roles/curator.md` и `docs/roles/dev-core.md` доки власти: мёржит john.** Дверь вывела список из восьми путей данными и назвала оба файла поимённо;
- `mergeable=MERGEABLE (CLEAN)`.

**Своей кнопки на #133 нет и не будет — merge я не делал.** Вердикт ревьюера в ленте выше (`msg-004`), находок по критериям 1–11 нет. PR готов к твоей кнопке как есть.

## 4. Что стоит на john, и почему я снимаю парковку

На тебе два предмета, оба стоят с 10:37Z:

1. **кнопка на #133** — approve есть, гарды 1/2 зелёные, гард 4 указывает на тебя;
2. **п.3 — включать ли защиту ветки `main`, и с каким набором обязательных проверок.** Вопрос и его цена расписаны в моём предыдущем сообщении и не изменились: аварийный merge при красном CI станет требовать снятия защиты, и защита закроет не только твою кнопку, но и маршрут `merge-gate` роли curator — в правило должен попадать вердикт бота `github-actions`, иначе штатный маршрут упрётся в отказ.

**Парковку на тебе снимаю сознательно и называю цену этого решения:** парковка на john размораживается только твоим словом, то есть с ней тред стоял бы ЦЕЛИКОМ — включая п.1, у которого исполнитель есть и работа для него готова. Оба твоих предмета остаются в ленте и никуда не деваются, а звонок по ним я подниму заново парковкой с `expects: ack`, когда ход вернётся ко мне с докладом `dev-core`. Если хочешь ответить раньше — просто напиши в тред, ход я подберу.

`waiting-on: dev-core` — по разделу 2 этого сообщения (вторая попытка п.1 постановки, заглушка). Незакоммиченного за мной нет: в этом ходе я ничего не писал в код и доки, рабочее дерево роли чистое.

## msg-006 · from: dev-core · 2026-08-30 · expects: none

**Заглушка открыта: PR #135, голова `d6534558`, база `retro/123-base` — дифф ровно те 12 файлов #123. `checks` идёт (прогон 33307911434, `event=pull_request`, 11:02:16Z). Метку `review` вешает следующий такт dev-core — ПОСЛЕ зелёного и на этой же голове; у конвейера не стою, паркуюсь за прогоном.**

## Что сделано, по шагам постановки

1. **`retro/123-base`** — на родителе squash-коммита #123: `git rev-parse dcf20c9686a1b856bb4e74d83cd1c1cea369efba^` → `2edfa0ea04639e3c10501f64252024f9ff061599`. Ветка запушена этим SHA;
2. **`retro/123-head`** — от неё же, `git cherry-pick dcf20c96`. Взял cherry-pick, а не сам `dcf20c96`: коммит #123 уже лежит в `main`, и на его SHA висят прогоны, поднятые пушем в `main`, — заглушка на том же SHA собирала бы под собой чужие чек-раны и путала бы и гард 2, и ревьюера. Новый коммит `d6534558` даёт то же дерево на чистой голове. Отступление от буквы («cherry-pick **либо** `git apply`» постановка называет обе) — в пределах названного, но причина выбора сказана вслух;
3. **срез проверен, а не принят на веру:** `git diff --stat retro/123-base..retro/123-head` — 12 файлов, 785 вставок / 10 удалений, файл в файл со `git show --stat dcf20c96`;
4. **PR [#135](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/pull/135)**, `retro/123-head` → **`retro/123-base`** (не `main`). Первой строкой тела — «Заглушка для ревью задним числом влитого #123. НЕ МЁРЖИТЬ — код уже в `main`. Закрывается после вердикта», следом `thread: 053-review-bypassed` и `role: dev-core`. Заголовок — Conventional Commits.

Рабочее дерево роли вернул в исходное detached-состояние на `0ef84f24`, локальные ветки удалил: незакоммиченного за мной нет, всё живёт в origin и в #135.

## Почему ход уходит здесь, а не после зелёного

`checks` на голове заглушки идёт прямо сейчас. Ждать его в foreground запрещено (`PROTOCOL.md`, «Ожидание чужого прогона — не действие»; постановка говорит то же словами «у конвейера не стоим»), поэтому паркуюсь `run:135` и передаю ход себе же на следующий такт.

Разморозка тут штатная и предусмотренная кодом, не расчёт на удачу: зелёный `checks` на PR, который **ещё не несёт метку `review`**, — единственное исключение из «следового» класса уведомителя, он называет роль АВТОРА в `waiting-on` (`packages/agent-protocol/src/thread/thread.ts:730-738`, решение john 2026-08-03). То есть письмо о зелёном придёт с `waiting-on: dev-core`, снимет парковку и поднимет меня ровно на тот один ход, который у меня тут остался, — повесить метку. Адрес письма возьмётся из `thread: 053-review-bypassed` в описании #135, то есть придёт сюда.

Если `checks` покраснеет — уведомитель назовёт мою роль тем же полем, и я разберу красноту обычным порядком (дифф чужой, влитый, так что краснота была бы находкой сама по себе — её и доложу).

## Что остаётся мне по п.1 после метки

Метка → круг ревьюера → вердикт в этот тред (053: здесь стоит п.1) → **закрыть #135 без merge и удалить обе ветки `retro/123-*`**. Находки, если будут, чиню отдельным PR в тред `047-devops-role`; заглушка для починки не используется — как в постановке.

Оговорку постановки держу и не переигрываю: гард 1 по #123 это задним числом не закрывает, историю merge не меняет. И третью форму, если и заглушка вернёт отказ судить, не запускаю — доложу отказ сюда как есть.

## Что стоит на john (не моё, повторяю, чтобы не потерялось в ленте)

Кнопка на #133 (approve на `bf7b6682`, гарды 1/2 зелёные, гард 4 указывает на john) и п.3 — включать ли защиту ветки `main` и с каким набором обязательных проверок. Оба предмета — в сообщениях curator выше, я их не двигаю.

`waiting-on: dev-core`, `parked-on: run:135` — жду `checks` по #135, голова `d6534558f4aeb6567aba0a78ef9b6d5ca4c492cd`.

## msg-007 · from: dev-core · 2026-08-30 · expects: none

**`checks` по #135 зелёный — но метку я на него не повесил: голова заглушки несла файл воркфлоу ревьюера, отставший от `main`, а это ровно тот класс, на котором `claude-code-action` пропускает сам себя. Заглушка пересобрана: PR [#138](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/pull/138), голова `ed593acf`, дифф — те же 12 файлов #123. #135 закрыт без merge. `checks` идёт (прогон 33309447063, `event=pull_request`, 11:38:41Z); метку вешает следующий такт — ПОСЛЕ зелёного и на этой же голове.**

## 1. Что замерено, и что из этого выведено

**Замерено:**

- `checks` на голове #135 `d6534558f4aeb6567aba0a78ef9b6d5ca4c492cd` — зелёный: прогон `33307911434`, `event=pull_request`, `conclusion=success`, 4m4s (`gh pr checks 135` + `gh run view`, а не «сборка прошла»);
- `git diff origin/main d6534558 -- .github/` — **один файл, `.github/workflows/claude-review.yml`, минус 25 строк.** То есть голова заглушки несла версию воркфлоу ревьюера, отставшую от `main` на 25 строк (заглушка построена на родителе `dcf20c96`, а с тех пор файл правился);
- в самом воркфлоу этот класс описан по имени и с лечением: «вердикта нет, потому что действие пропустило само себя (гард `claude-code-action` на PR, правящем этот файл)» — `.github/workflows/claude-review.yml:850-866`, и шаг «Самопропуск действия — установить факт» на 520-580 сверяет файл ветки ровно с дефолтной веткой. Там же назван первый живой случай: **прогон 30643932101 по #106, «ветка отстала от main»**.

**Выведено (и подаётся как вывод, не как факт):** повесь я метку на #135 как есть — круг с высокой вероятностью пропустил бы сам себя, вердикта бы не было, и в тред приехало бы третье подряд «ревью не состоялось». Проверять это живым кругом я не стал: цена проверки — круг ревьюера, а лечение дешевле и известно.

Разница с прошлыми двумя отказами существенная и её стоит назвать: тот отказ был **суждением ревьюера** о смёрженном PR, этот — **гардом действия**, срабатывающим до модели. Разные предметы, разное лечение.

## 2. Что сделано вместо метки

1. **`retro/123-base` подтянута до `main` по файлу воркфлоу** — один коммит `d7316fbf`, содержимое взято `git checkout origin/main -- .github/workflows/claude-review.yml`, ничего своего не написано;
2. **новая голова `retro/123-head-2` = `d7316fbf` + `git cherry-pick dcf20c96`** → `ed593acf85f074045ec5f9071ade85a4f9c6f5f8`. Голова построена НАД синхронизированной базой намеренно: так файл воркфлоу на голове совпадает с `main` **и** дифф PR его не трогает — обе половины условия гарда закрыты сразу;
3. **срез сверен, а не принят на веру:** трёхточечный `git diff --stat origin/retro/123-base...ed593acf` — 12 файлов, 785 вставок / 10 удалений, файл в файл со `git show --stat dcf20c96`; `git diff origin/main ed593acf -- .github/` пуст. `gh pr view 138 --json files` отдаёт те же 12 путей без воркфлоу;
4. **PR [#138](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/pull/138)**, `retro/123-head-2` → `retro/123-base` (не `main`), первой строкой тела «НЕ МЁРЖИТЬ», следом `thread: 053-review-bypassed` и `role: dev-core`;
5. **#135 закрыт без merge** с комментом о причине замены, ветка `retro/123-head` удалена, устаревший прогон `checks` на снятой голове (33309377549) отменён — чтобы зелёное письмо с мёртвой головы не подняло такт на закрытый PR.

**Почему новый PR, а не force-push в `retro/123-head`:** правильная топология требовала переписать историю головы, а force-операции моя карточка отправляет к john. Новая ветка и новый номер PR стоят дешевле, чем восхождение ради черновой ветки, и ничьего слова не требуют.

**Отступление от постановки называю вслух:** база заглушки теперь не РОВНО родитель `dcf20c96`, а родитель плюс один коммит, поднимающий файл воркфлоу до `main`. Судимый дифф от этого не изменился ни на строку — изменилось только то, каким воркфлоу поднимется круг. Если curator считает, что заглушка обязана стоять на чистом родителе, скажи — но тогда ревью на ней, по измеренному выше, скорее всего не состоится.

**Побочный эффект, который стоит знать:** с версией воркфлоу из `main` ревьюер объявит вердикт ещё и шапкой (`--verdict`/`--pr`) — в старой версии файла этой пары не было. То есть вердикт по #138 приедет с лифтом, а не только телом.

## 3. Что остаётся мне по п.1

Зелёный `checks` → метка `review` на ту же голову → круг → вердикт в этот тред → **закрыть #138 без merge и удалить обе ветки `retro/123-*`**. Находки, если будут, чиню отдельным PR в тред `047-devops-role`; заглушка для починки не используется.

Оговорки постановки держу и не переигрываю: гард 1 по #123 это задним числом не закрывает, историю merge не меняет; третью форму, если и заглушка вернёт отказ судить, не запускаю — доложу отказ сюда как есть.

## 4. Почему ход уходит здесь

`checks` на `ed593acf` идёт прямо сейчас. Ждать чужой прогон в foreground запрещено (`PROTOCOL.md`, «Ожидание чужого прогона — не действие»), поэтому паркуюсь `run:138` и передаю ход себе же. Разморозка штатная и предусмотренная кодом: зелёный `checks` на PR **без метки `review`** — единственное исключение «следового» класса уведомителя, он называет в `waiting-on` роль АВТОРА (`packages/agent-protocol/src/thread/thread.ts:730-738`), а адрес письма берётся из `thread:` в описании #138, то есть придёт сюда. Покраснеет — разберу тем же порядком.

## 5. Что стоит на john (не моё, повторяю, чтобы не потерялось)

Кнопка на #133 (approve на `bf7b6682`, гарды 1/2 зелёные, гард 4 указывает на john) и п.3 — включать ли защиту ветки `main` и с каким набором обязательных проверок. Я их не двигаю.

Незакоммиченного за мной нет: всё живёт в origin, в `retro/123-base`, `retro/123-head-2` и #138. Рабочее дерево роли вернул в исходное detached-состояние на `f9169e48`, локальные ветки удалил, `git status` чист.

`waiting-on: dev-core`, `parked-on: run:138` — жду `checks` по #138, голова `ed593acf85f074045ec5f9071ade85a4f9c6f5f8`.

## msg-008 · from: dev-core · 2026-08-30 · expects: none

**Метка `review` повешена на #138 — на зелёный и на ту же голову `ed593acf`. Круг ревьюера идёт: прогон [33310891767](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/33310891767), `event=pull_request`, `headSha=ed593acf…`, 12:13:06Z. Вердикт придёт в этот тред сам; паркуюсь `run:138` и передаю ход себе же — у конвейера не стою.**

## 1. Что замерено перед меткой (а не принято по письму о зелёном)

- **`checks` зелёный на голове заглушки:** `gh pr checks 138` → `pass`, 3m54s; `gh run view 33309447063` → `conclusion=success`, `event=pull_request`, `headSha=ed593acf85f074045ec5f9071ade85a4f9c6f5f8`, окно 11:38:41Z…11:42:39Z. То есть прогон именно на ТОЙ голове, а не «на PR вообще»;
- **голова не двигалась:** `gh pr view 138` → `headRefOid=ed593acf85f…`, `state=OPEN`, `labels: []` (до метки), `mergeable=MERGEABLE`. Метка легла на тот же SHA, который зелёный, — то самое, чего не было у #123;
- **условие самопропуска закрыто с обеих сторон, перепроверено сейчас, а не по прошлому ходу:** `git diff --stat origin/main ed593acf -- .github/` — пусто (файл воркфлоу на голове совпадает с `main`), и дифф PR `.github/` не трогает;
- **судимый срез на месте:** `git diff --stat origin/retro/123-base...ed593acf` — 12 файлов, 785 вставок / 10 удалений, файл в файл со `git show --stat dcf20c96`.

## 2. Метка и первые шаги круга

Метка `review` повешена в 12:12Z (`gh pr edit 138 --add-label review`), после зелёного — порядок постановки соблюдён. Круг поднялся событием `labeled` через ~40 секунд.

Что из круга видно на момент отправки (шаги прогона, `gh run view --json jobs`):

- шаг 8 «Зелёный `checks` на ТОЙ ЖЕ голове — установить факт» — `success`: круг сам подтвердил зелёное на своей голове;
- шаг 9 `claude-code-action` — `in_progress`: действие РАБОТАЕТ, то есть до модели дело дошло;
- шаги 10–12 (самопропуск / отсутствие вердикта / формальный статус) — впереди, их исход я не предвосхищаю.

**Это состояние прогона, а не вердикт.** Про #135 сказано было «выведено» — здесь ровно так же: то, что действие стартовало, не обещает вердикта, оно только снимает класс самопропуска с высокой вероятностью. Судить будет лента.

## 3. Что остаётся мне по п.1 после вердикта

Вердикт в этот тред → доложить находки (перечень либо строка «находок нет») → **закрыть #138 без merge и удалить обе ветки `retro/123-*`** (`retro/123-base`, `retro/123-head-2`). Находки, если они есть, чиню отдельным PR в тред `047-devops-role`; заглушка для починки не используется — как в постановке.

Оговорки постановки держу и не переигрываю: гард 1 по #123 это задним числом не закрывает, историю merge не меняет — это ревью-как-информация о влитом коде. Третью форму, если и заглушка вернёт отказ судить, не запускаю: доложу отказ сюда как есть.

## 4. Почему ход уходит здесь

Круг ревьюера — прогон, который сам пишет в тред; ждать его в foreground запрещено (`PROTOCOL.md`, «Ожидание чужого прогона — не действие»). Паркуюсь `run:138`: дверь парковки спрашивает, есть ли живой прогон на этой голове, — он есть (33310891767, `in_progress`), значит парковка не повиснет без источника события. Адрес доставки вердикта — `thread: 053-review-bypassed` в описании #138, то есть сюда, ходом на `dev-core`.

## 5. Что стоит на john (не моё, повторяю, чтобы не потерялось в ленте)

Кнопка на #133 (approve на `bf7b6682`, гарды 1/2 зелёные, гард 4 указывает на john) и п.3 — включать ли защиту ветки `main` и с каким набором обязательных проверок. Я их не двигаю.

Незакоммиченного за мной нет: правок кода и доков в этом ходе не было, рабочее дерево роли чистое (detached на `30f7e2ba`), всё живёт в origin и в #138.

`waiting-on: dev-core`, `parked-on: run:138` — жду вердикт круга ревьюера по #138, голова `ed593acf85f074045ec5f9071ade85a4f9c6f5f8`.

## msg-009 · from: reviewer-pr · 2026-08-30 · expects: answer

verdict: approve
pr: 138

Ревью-задним-числом влитого #123 (дифф среза совпадает файл в файл со `git show --stat dcf20c96`: 12 файлов, +785/−10; `gh pr view 138 --json files` подтверждает те же пути без файла воркфлоу). Постановка — тред `047-devops-role`; роль автора — `dev-core`. Заглушка (#138) сама НЕ мёржится, поэтому вопрос «чья кнопка» из формата вердикта к ней неприменим — см. «Ход» ниже.

## Проверки

- `pnpm typecheck` на голове `ed593acf` (чистый `pnpm install --frozen-lockfile`): `Done`/`Done`, обе TS-package'и.
- `pnpm exec biome check` по всем 9 не-markdown файлам диффа: `Checked 9 files, No fixes applied`.
- Полный `pnpm test` на голове не повторял — `checks` на голове `ed593acf85f074045ec5f9071ade85a4f9c6f5f8` зелёный, прогон `33309447063` (`event=pull_request`, `conclusion=success`).
- Точечный прогон новых/изменённых тестов (`src/schema/v24-role-capabilities.test.ts`, `src/roles/devops-declared.test.ts`): `2 files passed, 32 tests passed`.
- Живой исход `pnpm -F agent-protocol cli merge-gate --ref origin/main --pr 138`:
  ```
  merge-gate: documents of power judged by (8): agent-protocol.json, docs/roles/curator.md,
    docs/roles/dev-core.md, docs/roles/pilot-codex.md, docs/roles/devops.md, REVIEWER.md,
    PROTOCOL.md, .github/workflows
  STOP guard 1 · approve on the current head: no approve verdict on ed593ac
  STOP guard 2 · green checks on the same head: not green: review=IN_PROGRESS
  you  guard 3 · ascent to a decision of john's: thread '053-review-bypassed'
  STOP guard 4 · no self-merge on the documents of power: john merges this one — it changes agent-protocol.json
  you  guard 5 · a trace of the merge
  ok   mergeability: mergeable=MERGEABLE (mergeStateStatus UNSTABLE)
  REFUSED: a guard does not hold
  ```
  Guard 1/2 STOP — ожидаемое состояние ДО публикации этого вердикта (сам круг ещё не завершён на момент вызова), не находка. Guard 4 подтверждает критерий 5 (ниже) — вопрос о кнопке всё равно снят: PR не мёржится (см. «Ход»).

## Критерий 1 (числа тестов) — вымерено обеими сторонами, не принято на слово

- **База** (`retro/123-base`, отдельный чекаут и прогон моей рукой): `pnpm test` → `agent-protocol` 163 файла / 2694 теста, `transport-telegram` 2/7.
- **Голова** (мerge-ref прогона `33309447063`, из его лога): `agent-protocol` 164 файла / 2718 тестов, `transport-telegram` 2/7.
- Разность (+1 файл / +24 теста) сходится построчно с диффом: новый файл `v24-role-capabilities.test.ts` — 17 `it(...)`, добавленный `describe` в `devops-declared.test.ts` — 7 `it(...)`, итого 24. Числа PR/треда (`dev-core`, msg `09:08:35Z`: «2718 passed… 164 файла ядра + 7 случаев транспорта») совпадают с фактом.

## Критерий 3 (скоуп) — сверено с постановкой треда 047

Состав `capabilities` (три глагола `log-tail`/`repo-refresh`/`disk-free`, закрытые списки без свободных строк, потолок `log-tail`=200, `repo-refresh` только `pull --ff-only`+install, без root, без `journal:`-целей) — ровно решение john, msg `2026-08-30T08:15:42Z` в треде 047 («состав (A) сейчас»), сузившее исходные пять пунктов до трёх. Молчаливого расширения или сужения нет: исключение двух глаголов (`service-restart`/`service-status`) названо в самом коде (`capabilities.ts`, `v24-role-capabilities.ts`) и в доках диффа с причиной (пользовательская шина systemd).

## Критерий 6 (совместимость протокола) — соблюдено

`protocolVersion` 23→24, миграция `ROLE_CAPABILITIES_STEP` (`from: 23`) зарегистрирована в `migrate.ts`, обе таблицы `CONFIG_SHAPES`/`CONFIG_VALUES` дополнены (не переписаны — append-only), схема `roleSchema` получает поле `capabilities` optional без default.

## Критерий 4 (зоны) — не нарушено

Зона `dev-core` (автор диффа #123): `forbidden: ["docs/roles"]`. Дифф `docs/roles/**` не трогает — зелёный по правилу «дверь судит только по forbidden».

## Критерий 5 (доки власти)

Дифф трогает `agent-protocol.json` — док власти этого репозитория, гард 4 `merge-gate` подтверждает живым прогоном. Для самого #123 это уже история (смёржен кнопкой john, `merged-pr: 123`, тред 053, curator `09:32:18Z`); для этой заглушки (#138) — см. «Ход».

## Критерий 9 (текст vs факт) — находка снята замером, действия не требует

Поле `summary` строки `devops` в `agent-protocol.json` **на голове #123 сразу после его merge** называло пять действий («перезапуск сервиса контура… статус сервисов»), хотя массив `capabilities` того же диффа объявляет только три — расхождение прозы и данных ровно того класса, против которого строился `capabilities.ts`. Проверил: это НЕ пропуск в код-ревью — расхождение уже поймано и исправлено в `main` follow-up PR **#127** (смёржен `2026-08-30T09:54:00Z`, через 23 минуты после #123), сверено прямым чтением `git show origin/main:agent-protocol.json` — поле сейчас называет глаголы именами словаря и не пересказывает ящик. Действий по этому вердикту не требует. Отдельно: `docs/roles/devops.md` до сих пор перечисляет все пять пунктов как «список закрыт» (не входило в дифф #123, дифф там не трогает `docs/roles/**` — зона дев-core это запрещает), но это уже отслежено и чинится открытым PR **#128** (approve стоит, ждёт кнопки john, тред 047) — не новая находка этого круга.

## Итог

Находок, требующих правки по этому кругу, нет. Единственное затронутое расхождение (summary vs capabilities) уже устранено параллельным PR и не относится к текущему состоянию `main`.

## Ход

Заглушка НЕ мёржится (заявлено в её же теле: «НЕ МЁРЖИТЬ — код уже в `main`. Закрывается после вердикта»), поэтому обычное правило «approve → curator» здесь неприменимо — мержить нечего. Постановка (тред 053, curator `10:49:07Z`, п.2, приёмка п.1) поручает автору заглушки закрыть PR без merge, удалить ветки `retro/123-*` и доложить находки (или их отсутствие) в тред 047 — это исполнимо ролью `dev-core`, автором #138. `waiting-on: dev-core`.

Отдельно, не моё и не двигаю: кнопка на #133 и решение john по п.3 (защита ветки `main`) — оба уже названы в теле треда 053 предыдущими ходами, ждут john независимо от этого вердикта.

## msg-010 · from: dev-core · 2026-08-30 · expects: answer

**П.1 постановки закрыт: круг ревью задним числом СОСТОЯЛСЯ — вердикт `approve` по заглушке #138 на голове `ed593acf`, находок по коду среза #123 нет. Заглушка закрыта без merge, обе ветки `retro/123-*` удалены. Предмет треда — 792 строки, уехавшие в `main` без ревью, — теперь отревьюен: не задним числом закрытым гардом, а как информация о влитом коде.**

## 1. Что замерено (приёмка п.1 по букве постановки)

Постановка требовала: «в ленте лежит вердикт `reviewer-pr` с парой `verdict:`/`pr:` по номеру заглушки, и в нём либо перечень находок по коду #123, либо строка "находок нет"». Лежит — `msg-009` выше: `verdict: approve`, `pr: 138`.

Снято моей рукой после его публикации, а не принято по письму:

- **круг завершён успешно на судимой голове:** прогон `33310891767`, `event=pull_request`, `conclusion=success`, `headSha=ed593acf85f074045ec5f9071ade85a4f9c6f5f8`, окно 12:13:06Z…12:23:47Z (`gh run view`);
- **все три доставки вердикта сработали, включая третью:** письмо в тред (`msg-009`), коммент в #138 и **формальный статус** — `gh pr view 138 --json reviews` → `github-actions`, `state: APPROVED`, `commit.oid = ed593acf85f…`, 12:23:43Z. На открытом PR отказа `gh pr review` нет; оговорка из моего msg-002 касалась только смёрженного #123 и здесь не применилась;
- **оба чека зелёные на той же голове:** `gh pr checks 138` → `checks pass` (прогон 33309447063, 3m54s), `review pass` (10m37s).

**Третья форма не понадобилась и не запускалась** — постановка запрещала её без слова john, и повода не возникло.

## 2. Находки по коду #123: их нет — и это перечень того, что ревьюер РЕАЛЬНО потрогал

Строку «находок нет» не пересказываю голой, потому что её вес — в проверках под ней. Из `msg-009`, проверки на голове `ed593acf`:

- `pnpm typecheck` на чистом `pnpm install --frozen-lockfile` — обе TS-package'и зелёные;
- `biome check` по всем 9 не-markdown файлам диффа — `No fixes applied`;
- **числа тестов вымерены С ОБЕИХ СТОРОН среза**, не приняты на слово: база `retro/123-base` — 163 файла / 2694 теста ядра, голова — 164 / 2718; разность (+1 файл / +24 теста) сходится ПОСТРОЧНО с диффом (17 `it` в новом `v24-role-capabilities.test.ts` + 7 `it` в добавленном `describe` в `devops-declared.test.ts`). Это ровно тот критерий 1, который в маршруте #123 был пропущен вместе с кругом;
- скоуп сверен с решением john от 08:15:42Z в треде 047 (состав (A): три глагола, закрытые списки, потолок `log-tail`=200, `repo-refresh` только `pull --ff-only`+install, без root) — молчаливого расширения нет;
- совместимость протокола 23→24: миграция `ROLE_CAPABILITIES_STEP` зарегистрирована, обе таблицы дополнены append-only, поле `capabilities` optional без default;
- зоны: дифф `docs/roles/**` не трогает.

**Единственное расхождение, которое круг нашёл, уже устранено до него:** поле `summary` строки `devops` в `agent-protocol.json` на голове #123 называло пять действий против трёх в массиве `capabilities` — ровно тот класс «проза врёт про данные», против которого `capabilities.ts` и строился. Ревьюер проверил прямым чтением `git show origin/main:agent-protocol.json`: починено follow-up PR **#127** (смёржен 09:54:00Z, через 23 минуты после #123). Действия по этому вердикту не требуются. Хвост в `docs/roles/devops.md` (пять пунктов как «список закрыт») ведёт **открытый PR #128** — проверил сейчас: `state: OPEN`, тред 047, не мой предмет и не новая находка.

**Отдельным PR в тред 047 чинить нечего** — постановка предусматривала такой PR только на случай находок. Их нет.

## 3. Что сделано с заглушкой

1. коммент в #138 с причиной закрытия и адресом разбора → PR **закрыт без merge**: `gh pr view 138` → `state: CLOSED`, `mergedAt: null`. Заявленное первой строкой её тела («НЕ МЁРЖИТЬ — код уже в `main`») соблюдено;
2. **обе ветки удалены:** `git push origin --delete retro/123-base retro/123-head-2` → `[deleted]` по обеим; `git ls-remote --heads origin 'retro/*'` пуст. `retro/123-head` (первая голова, #135) была удалена ещё в прошлом ходе — в origin от заглушки не осталось ничего.

Удаление веток — необратимое действие, и моя карточка такие отправляет к john. Восхождения тут нет намеренно: удаление названо ШАГОМ 5 самой постановки (msg-005 curator, 10:49Z), а слово john «заводи» дано на постановку целиком. Я исполняю названное, а не решаю заново; называю вслух, чтобы это не прочли как самовольную руку.

## 4. Что этот вердикт НЕ делает — оговорки постановки держатся

- **гард 1 `merge-gate` по #123 задним числом не закрыт** и история merge не переиграна: вердикт живёт на голове заглушки, а не на `dcf20c96`. Это ревью-как-информация о влитом коде — ровно как названо в постановке и в моих предыдущих ходах;
- **причину B треда (кнопка john гардов не спрашивает) п.1 не лечит вообще** — он закрывает последствие одного конкретного пропуска, а не дверь. Лечит её п.3, и он стоит на john.

Побочно стоит зафиксировать как измеренный факт, потому что он пригодится следующему такому случаю: **ревью влитого кода воспроизводимо делается заглушкой**, но цена входа — два отказа, оба названные по имени (суждение ревьюера о смёрженном PR; гард самопропуска на отставшем файле воркфлоу). Рабочая форма — PR на РОДИТЕЛЕ squash-коммита, база подтянута до `main` по `.github/workflows/claude-review.yml`, база не `main`, метка после зелёного на той же голове. Заводить ли из этого строку в док — вопрос дома находки, то есть твой; сам я в доки этим ходом не писал.

## 5. Ход

`waiting-on: curator` — п.1 постановки исполнен и принят по своей же «Проверяемости», отчёт выше; дальше судить приёмку тебе.

**На john по-прежнему стоят два предмета, и они не сдвинулись этим ходом** (проверил состояние сейчас, а не переписал из ленты):

1. **кнопка на #133** — `state: OPEN`, `mergedAt: null`; approve на `bf7b6682`, гарды 1/2 зелёные, гард 4 указывает на john (`docs/roles/**` — доки власти);
2. **п.3 — включать ли защиту ветки `main` и с каким набором обязательных проверок.** Цена названа в ленте дважды и не изменилась: аварийный merge при красном CI потребует снятия защиты, и защита закроет не только кнопку john, но и маршрут `merge-gate` роли curator — в правило должен попадать вердикт бота `github-actions`.

Парковку под них я не ставлю: звонок john — твой ход, как ты сам и написал, снимая её в прошлый раз.

Незакоммиченного за мной нет: правок кода и доков в этом ходе не было, рабочее дерево роли чистое (detached на `30f7e2ba`, `git status` пуст), в origin от заглушки не осталось ни PR, ни веток.

## msg-011 · from: github · 2026-08-30 · expects: none

PR #133 (docs(roles): круг ревью поднимает событие метки, а не её наличие — после смены головы метку перевешивать (тред 053)) **merged** by maysway → `main`.

## msg-012 · from: curator · 2026-08-30 · expects: answer

**Слово john полем `delivers` (чат ~12:46Z): #133 СМЁРЖЕН его кнопкой.** Карточки `curator` и `dev-core` договорили то, чего маршрут PR не говорил: круг ревью поднимает **событие** навешивания метки, а не её наличие; пуш и перебазировка круг не поднимают; после смены головы метку снять и повесить заново; PR с меткой и без закрытого круга на текущей голове готовым не является.

Пункт 2 постановки закрыт. Остаток треда — прежний:

1. **ревью задним числом на влитый #123** — идёт заглушкой #138 (мержу не подлежит по построению: база не `main`); после вердикта закрыть без merge, ветки `retro/123-*` снести, находки — отдельным PR в тред `047`;
2. **защита ветки `main` в GitHub** — единственный пункт, не держащийся на дисциплине; цена названа заранее (аварийный merge при красном CI потребует снять защиту) и решение остаётся за john. Класс подтверждён одним случаем (#123): вчерашняя запись про «второй случай» на #443 отозвана — там вердикт был.

waiting-on → dev-core.

## msg-013 · from: curator · 2026-08-30 · expects: ack

**Требуется решение john (п.3, стоит с 10:37Z): включать ли защиту ветки `main` — и с каким набором обязательных проверок? Пп. 1 и 2 закрыты: срез #123 отревьюен (approve по заглушке #138, находок нет), #133 смёржен твоей кнопкой в 12:44:09Z. Из трёх пунктов постановки открыт ровно один — твой.**

Парковку на тебе поднимаю заново с `expects: ack`, как и обещал в своём сообщении 10:49Z, снимая её тогда: ход вернулся ко мне докладом `dev-core`, работы, которую эта парковка заморозила бы, в треде больше нет.

**Одна поправка к предыдущему сообщению в ленте** (чатный curator, доставка слова john о кнопке на #133, ~12:46Z): его перечень остатка называет п.1 идущим («идёт заглушкой #138»). На момент той записи это уже было не так — вердикт лёг в 12:23:43Z, заглушка закрыта, ветки снесены. Письмо писалось по более раннему состоянию; предмет не спорный, но остаток треда сейчас — ОДИН пункт, не два.

## 1. П.1 принят — по своей же «Проверяемости», а не по докладу

Приёмка требовала: «в ленте лежит вердикт `reviewer-pr` с парой `verdict:`/`pr:` по номеру заглушки, и в нём либо перечень находок по коду #123, либо строка "находок нет"». Лежит — сообщение `reviewer-pr` от 2026-08-30, шапка `verdict: approve` / `pr: 138`, находок нет. (Ординалом на него не ссылаюсь: `--tail` двигает ординалы у читателя, а имён файлов `thread show` не печатает.)

Снято моей рукой сейчас, а не переписано из доклада `dev-core`:

- **вердикт не сирота и стоит на судимой голове:** `gh pr view 138 --json reviews` → `github-actions`, `state: APPROVED`, `commit.oid = ed593acf85f074045ec5f9071ade85a4f9c6f5f8`, 12:23:43Z. Круг `Claude PR Review`, прогон `33310891767`: `event=pull_request`, `conclusion=success`, `headSha=ed593acf85f…`, окно 12:13:06Z…12:23:47Z — вердикт лежит ВНУТРИ окна. То есть по форме это ровно тот вердикт, который засчитал бы гард 1, если бы заглушка была настоящим PR;
- **заглушка не смёржена:** `state: CLOSED`, `mergedAt: null` — заявленное первой строкой её тела «НЕ МЁРЖИТЬ» соблюдено;
- **в origin от заглушки не осталось ничего:** `git ls-remote --heads origin 'retro/*'` пуст.

Оговорку постановки держу и не переигрываю: **гард 1 по #123 задним числом это не закрывает**, история merge не переиграна. Что получено — знание о том, что мы влили: 792 строки #123 больше не «непроверенный код», а проверенный код с известным исходом.

Отдельно засчитываю то, чего постановка не требовала, а `dev-core` сделал: **числа тестов вымерены с ОБЕИХ сторон среза** (база 163/2694 → голова 164/2718, разность +1 файл/+24 теста сходится построчно с диффом: 17 `it` + 7 `it`). Это ровно критерий 1, пропущенный в маршруте #123 вместе с кругом, и он единственный, который нельзя было бы восстановить позже.

Расхождение `summary` vs `capabilities`, которое круг нашёл: устранено до него (#127, смёржен 09:54:00Z — через 23 минуты после #123), хвост в `docs/roles/devops.md` ведёт открытый #128 в треде 047. Ни то, ни другое действий по этому треду не требует и предмета 053 не касается.

**Удаление веток `retro/123-*` своей рукой `dev-core` назвал вслух и восхождение указал верно:** это ШАГ 5 моей же постановки, а не самовольная рука. Подтверждаю: названное исполнено, вопроса нет.

## 2. П.2 закрыт твоей кнопкой — контрольное чтение цели сделано

#133: `state: MERGED`, `mergedAt: 2026-08-30T12:44:09Z`, `baseRefName: main`, `mergeCommit: c5127d5304ecf3b2a7a901c7b1fcb58d845194d0`, голова `bf7b66824c1ecc9bf033b87e7c8acffad9314fed`. Своей кнопки на нём не было и быть не могло (гард 4, доки власти) — следа гарда 5 с меня не требуется, читаю цель как факт: строка «круг поднимает СОБЫТИЕ метки; после смены головы метку перевешивать; PR с меткой и без вердикта на текущей голове готовым не является» теперь живёт в `main`, в карточках `curator` и `dev-core`.

Проверяемость п.2 была объявлена полевой: «следующий перебазированный PR приходит на кнопку с вердиктом на СВЕЖЕЙ голове». Она НЕ закрыта этим ходом и закроется сама, первым же таким PR. Не выдаю её за закрытую.

## 3. П.3 — вопрос к тебе, и он не изменился

Состояние снято 10:31Z и с тех пор не пересматривалось: защиты нет ни в старой форме (`branches/main/protection` → 404), ни рулсетом (`rules/branches/main` → пусто). Причина B треда — «кнопка john гардов не спрашивает» — п.1 и п.2 не лечится вообще: п.1 закрыл ПОСЛЕДСТВИЕ одного пропуска, п.2 договорил дисциплину роли. Дверь как не спрашивалась, так и не спрашивается.

Цена, названная дважды и не изменившаяся:

1. **аварийный merge при красном CI станет невозможен** без снятия защиты — лишний шаг у тебя ровно в аварии;
2. **защита закроет не только твою кнопку, но и маршрут `merge-gate` роли curator:** дверь ролей советующая, GitHub откажет по своим правилам. Если правило потребует «review approved», в него должен попадать вердикт бота `github-actions` — иначе штатный маршрут «метка → вердикт → merge curator» упрётся в отказ на ровном месте, и мёржить всё придётся тебе руками;
3. **форма правила — тоже твоё решение:** «требовать успешный круг ревью», «требовать зелёные `checks`», либо и то и другое.

Проверяется один раз руками на тестовом PR, и проверять надо ОБА маршрута — кнопку и `merge-gate`.

## 4. Второй вопрос, дешёвый — рецепт ревью влитого кода в док

`dev-core` вынес из п.1 воспроизводимый рецепт и прямо оставил решение мне; решаю и несу тебе, потому что не уверен в восхождении, а правило последней строки у меня одно.

Рецепт (измерен, а не придуман): **ревью уже влитого кода делается PR-заглушкой на РОДИТЕЛЕ squash-коммита, база подтянута до `main` по `.github/workflows/claude-review.yml`, база — не `main`, метка вешается после зелёного на той же голове.** Цена входа — два отказа, оба названные по имени: ревьюер отказывается судить смёрженный PR (суждение, не механика — повтор купит тот же отказ), и гард самопропуска действия срабатывает на отставшем от `main` файле воркфлоу.

Дом строки — `docs/protocol-reference.md` (поведение кода, НЕ док власти, моя зона и моя рука, обычные гарды). Что спрашиваю: **строку завожу?** Своего слова john на неё нет, а на «дефект, новой нормы не вводит» она не тянет — это не починка сломанного, а запись нового знания. Скажешь «заводи» — сделаю сам обычным маршрутом; скажешь «не надо» — рецепт остаётся в этой ленте и в этом сообщении.

**В этом ходе PR по нему НЕ открыт, и это осознанно:** тред сейчас паркуется на тебе, а парковка замораживает его целиком — открытый PR всё равно стоял бы до твоего слова, только ещё и с висящим кругом ревью. Цену называю вслух, как и в прошлый раз.

## 5. Хвост открытых PR — прогнан, и почему ни одной кнопки

`gh pr list --state open` → три PR, и **ни один не мой кандидат**: #140 (`thread: 042-unaccepted-turn-silent`), #134 (`thread: 052-pr-template`), #114 (`thread: 048-session-privileges`) — у всех `role: dev-core`.

#140 прогнал дверью, потому что он выглядит готовым: `merge-gate --ref origin/main --pr 140 --review-workflow 'Claude PR Review'` → `ok` гарды 1 (approve на `62b5d5b` внутри круга 33311235228, окно 12:20:50Z…12:24:09Z), 2 (`review=SUCCESS`, `checks=SUCCESS`) и 4 (доков власти дифф не трогает), `mergeable=CLEAN`. **Кнопки не нажимаю по гарду 5:** след merge обязан лечь в тред, которому PR принадлежит, — в 042, а этот прогон привязан к 053 и писать туда не может. Дверь отдельно печатает ноту «база уехала после зачтённых чеков» (`6718fc2` в 12:45:59Z против старта `checks` в 12:10:44Z) — её закрывает прогон дерева слияния, и это работа того такта, а не этого.

Практический смысл строки: **#140 готов к кнопке и ждёт только своего треда.** Кладу это сюда, чтобы находка не умерла вместе с сессией.

## 6. Ход и незакоммиченное

`waiting-on: curator`, `parked-on: john`, `expects: ack` — ход остаётся у того, кто несёт вопрос. Оба вопроса стоят выше явно: п.3 (защита ветки, первой строкой) и строка рецепта в `docs/protocol-reference.md` (раздел 4). Любого твоего слова в тред достаточно, чтобы я подобрал ход.

Незакоммиченного за мной нет: в этом ходе я не писал ни в код, ни в доки — только читал и мерил. Рабочее дерево роли чистое.

## msg-014 · from: curator · 2026-08-30 · expects: none

**Слово john полем `delivers` (чат ~13:10Z) по п.3 — РЕШЕНИЕ ПРИНЯТО И УЖЕ ИСПОЛНЕНО ЕГО РУКОЙ. Парковка снята.**

## Что john включил

**Классическая защита ветки `main` в ЭТОМ репозитории, с одним требованием: merge только через PR.** Требование `approve` он **сознательно НЕ включал**. В контуре LLE такая защита стояла и раньше — то есть теперь оба репозитория закрыты одинаково.

## Что это меняет, и чего НЕ меняет — назвать прямо, чтобы никто не прочитал больше, чем есть

**Закрыто:** прямой push в `main` мимо PR. Класс «изменение приехало без круга и без следа» сузился до «PR без ревью», и это уже не тихий путь, а видимый.

**НЕ закрыто, и это остаётся правдой:** предмет треда — PR #123 уехал БЕЗ вердикта — этой настройкой не предотвращается. Merge через PR он и был. Без обязательного approve кнопка john по-прежнему проходит мимо гарда 1, и единственная защита на его пути — дисциплина роли, готовящей PR.

**Риск №2 из моего письма `10-37-14Z` (защита ломает маршрут `merge-gate` роли curator) при выбранной форме НЕ РЕАЛИЗУЕТСЯ:** роль мержит через API pull request, а не push'ем; требования approve нет, значит отказа GitHub на штатном маршруте не будет. Это и есть причина, по которой выбранная john форма — самая дешёвая из возможных: она берёт всю пользу «нет пути мимо PR» и не платит ни одной из трёх названных цен.

## Что теперь проверяется (твой ход)

1. **Штатный маршрут роли не сломан** — первый же merge рукой curator после включения; если GitHub откажет, доложить немедленно, это регресс настройки, а не PR;
2. **Прямой push в `main` отказывает** — проверять специально не нужно, ролям он и так запрещён нормой; фиксируется первым же случайным случаем;
3. **Строку о защите внести в `docs/box-setup.md`** (или туда, где описано устройство репозитория) — сегодня факт живёт только в чате и в этом письме, а завтра кто-то будет гадать, почему push отказал.

## Остаток треда

- **п.1** — заглушка #138 ждёт вердикта ревьюера; после него закрыть без merge, ветки `retro/123-*` снести, находки — в тред `047`;
- **обязательный approve** остаётся ОТКРЫТЫМ вопросом john, а не отклонённым: он сказал «пока не включал». Возвращаться к нему стоит с ценой в фактах — то есть если случай «PR без вердикта на кнопке» повторится.

waiting-on → curator.

## msg-015 · from: curator · 2026-08-30 · expects: none

**П.3 закрыт целиком: защиту john включил своей рукой, а третий его подпункт — «строку о защите внести в доки» — исполнен этим ходом; PR #145 зелёный и стоит под кругом ревью.** Остаток треда после этого хода: один невзятый вопрос (рецепт ревью влитого кода) и одна полевая проверка, которую закроет merge #145.

## 1. Защиту снял своей рукой — и в объекте есть то, чего письмо не называло

Слово john в ленте — доставка чата; сверка начинается с чтения, а не с письма. Снято сейчас, `gh api repos/:owner/:repo/branches/main/protection` (было 404 в 10:31Z, стало объект):

- `required_pull_request_reviews.required_approving_review_count: 0`, `dismiss_stale_reviews: false`, `require_last_push_approval: false` — **PR обязателен, вердикт нет.** Это ровно та форма, которую john назвал;
- **ключа `required_status_checks` в объекте НЕТ** — зелёного CI дверь GitHub не требует вовсе. Письмо этого не называло, а разница практическая: гард 2 роли как был единственным держателем «зелёные чеки на той же голове», так и остался;
- `allow_force_pushes: false`, `allow_deletions: false` — `main` нельзя ни перезаписать, ни удалить. Это сверх названного, и в плюс;
- **`enforce_admins: false`** — на администратора репозитория правило не распространяется. То есть «нет пути в `main` мимо PR» верно для ключа ящика и ролей, но не для владельца. Не возражение: закрывается ровно тот класс, который и был предметом (роль/ключ), — но читать шире, чем есть, эту настройку нельзя;
- `gh api repos/:owner/:repo/rules/branches/main` → пусто: форма старая (branch protection), не ruleset. Значит и снимать/менять её надо там же, где включена.

Вывод, который отсюда следует и с письмом не спорит: **предмет треда 053 этой настройкой не закрыт.** PR без вердикта она пропускает по построению. Обязательный approve остаётся открытым вопросом john, и цену ему я назову фактами, если случай повторится.

## 2. Что сделано этим ходом — PR #145, `docs/box-setup.md`

Новый раздел `4a` между `init github` (§4) и `doctor` (§5). Место выбрано не «по смыслу»: это единственный шаг страницы, который делается один раз на РЕПОЗИТОРИЙ, а не на каждый ящик, и стоит он сразу за шагом, выдающим ящику ключ, — чтобы отказ push'а читался как правило, а не как поломка ключа.

В разделе: форма, включённая john; две команды замера (объект и рулсеты) — состояние снимается API, а не галками UI; четыре факта выше дословно; чего защита НЕ делает (пяти гардов не заменяет, PR без вердикта пропускает, предмет 053 не предотвращает); и что штатный маршрут `merge-gate` → `gh pr merge --squash` ею не ломается, потому что merge идёт вызовом API pull request, а не push'ем.

Про `enforce_admins` в тексте прямо сказано, что отказ push'а ключом ящика — **следствие правила, а не замер**: специально его не проверяли. Своя гипотеза — тоже заявление, и в доке она помечена как гипотеза.

**Восхождение (гард 3):** решение john в ленте, доставка чата ~13:10Z, третий подпункт «Что теперь проверяется (твой ход)» — дословно «Строку о защите внести в `docs/box-setup.md` … а завтра кто-то будет гадать, почему push отказал». Класса «полевой дефект» здесь не заявляю и он не нужен: слово john есть.

**Состояние PR #145** (`thread: 053-review-bypassed`, `role: curator`):
- голова `8188be672015220b94db11936cc0a13918dd7fbc`, `mergeable: MERGEABLE`;
- `checks` **зелёный на этой же голове**: прогон `33314242038`, `headSha=8188be67…`, `completed/success`, 4m8s;
- **метка `review` повешена ПОСЛЕ зелёного** на той самой голове, в ~13:33Z. По карточке метка — конец хода, а не начало ожидания: в прогоне не остаюсь, паркуюсь на `pr:145`.

Дифф `docs/box-setup.md` — не док власти, зона роли зелёная (в `forbidden` его нет), значит после вердикта кнопка моя.

**Соседство названо в описании PR:** открытый #114 (тред 048) трогает тот же файл, но раздел `0.2`. Кто сядет вторым — гоняет дерево слияния своей рукой.

## 3. Расхождение в ленте — называю, но не переписываю

Письмо-доставка (~13:10Z) в «Остатке треда» называет п.1 идущим: «заглушка #138 ждёт вердикта ревьюера». На момент той записи это уже было не так — вердикт `approve` лёг в 12:23:43Z, заглушка закрыта без merge, ветки `retro/123-*` снесены, и всё это снято моей рукой в письме 12:5xZ. Письмо чата писалось по более раннему состоянию. Ничего не переигрывается — просто следующий читатель ленты не должен искать несуществующий хвост.

## 4. Невзятый вопрос — рецепт ревью влитого кода. Стоит, но парковки на нём сейчас НЕ ставлю

Слово john ответило п.3 и только его. Вопрос из раздела 4 моего письма 12:5xZ — **заводить ли в `docs/protocol-reference.md` строку про PR-заглушку на родителе squash-коммита** (база подтянута до `main` по файлу воркфлоу, ревьюер отказывается судить смёрженный PR) — остался без ответа.

Парковку с `expects: ack` на него сейчас не ставлю сознательно, и цену называю: **парковка заморозила бы тред целиком, а в нём идёт круг ревью #145.** Вопрос дешёвый и не срочный — подниму его следующим ходом, когда #145 сядет и замораживать будет нечего. Если john ответит раньше своим словом в тред — тем лучше.

## 5. Полевая проверка №1 из письма john — на ней теперь висит конкретный PR

«Штатный маршрут роли не сломан» проверяется первым merge рукой curator после включения защиты. **Этим первым merge будет #145** — других кандидатов у роли нет. Поэтому его нельзя потерять: если GitHub откажет на `gh pr merge --squash`, это регресс настройки, а не проблема PR, и докладывать надо немедленно.

## 6. Хвост открытых PR — прогнан, кнопок нет

`gh pr list --state open` → шесть: #144 (тред 042), #143 (055), #142 (054), #141 (047), #134 (052), #114 (048) — **у всех `role: dev-core` и все принадлежат чужим тредам**, кнопка по ним заперта гардом 5 (след merge обязан лечь в тред PR, а этот прогон привязан к 053). #140, который я в прошлом ходе назвал готовым, сел в `main` — `e82ddac5`, и это же голова, от которой строилась моя ветка.

## 7. Ход и незакоммиченное

Незакоммиченного за мной нет: правка `docs/box-setup.md` лежит коммитом `8188be67` в ветке `docs/053-main-branch-protection`, рабочее дерево роли чистое (`git status --porcelain` пуст).

`waiting-on: curator`, `parked-on: pr:145`. Следующий ход по этому треду: вердикт круга на #145 → гарды → merge со следом → и сразу за ним вопрос п.4 к john.

## msg-016 · from: curator · 2026-08-30 · expects: none

**Поправка к предыдущему сообщению — только поле парковки, содержание в силе.** Я объявил `parked-on: pr:145`, а ждать здесь надо не кнопку, а круг: `pr:N` снимается ровно одним заголовком `merged-pr: N` (дверь так и сказала при записи), тогда как кнопка на #145 — моя собственная, и до неё стоит вердикт ревьюера. Парковка на кнопку, которую сам же и нажимаешь, — заморозка без исхода.

Правильная форма — `parked-on: run:145`, и условие под ней живое, снято сейчас: круг `Claude PR Review`, прогон `33314452316`, `event=pull_request`, `headSha=8188be67…`, `in_progress`, старт 13:32:27Z. То есть парковка ставится ЗА идущим прогоном, а не позади уже случившегося исхода.

Ничего другого предыдущее сообщение не меняет: п.3 закрыт, PR #145 зелёный (`33314242038`, `8188be672015220b94db11936cc0a13918dd7fbc`) и под меткой `review`, вопрос про рецепт в `docs/protocol-reference.md` стоит невзятым, хвост открытых PR прогнан.

`waiting-on: curator`, `parked-on: run:145`.

## msg-017 · from: reviewer-pr · 2026-08-30 · expects: answer

verdict: approve
pr: 145

Дифф — один файл `docs/box-setup.md`, новый раздел `4a` (между «4. init github» и «5. doctor»). Постановка — `thread: 053-review-bypassed`, роль автора `curator`. Тред прочитан целиком (`messages/`, 14 сообщений). Раздел исполняет ровно пункт 3 решения john, доставленного полем `delivers` в сообщении `2026-08-30T13-10-56Z-curator.md`: «Строку о защите внести в docs/box-setup.md — сегодня факт живёт только в чате и в этом письме». Молчаливых расширений сверх этого пункта в диффе нет.

## Проверки

- `pnpm typecheck` (чистый `pnpm install --frozen-lockfile` в отдельном worktree на голове `8188be67`): `Done`/`Done`, обе TS-package'и.
- `pnpm exec biome check docs/box-setup.md`: 0 файлов обработано — «ignored», markdown вне конфига biome (тот же факт, что и на #133).
- Полный `pnpm test` не повторял — `checks` на голове `8188be672015220b94db11936cc0a13918dd7fbc` зелёный, прогон `33314242038` (`event=pull_request`, `conclusion=success`, проверено `gh run view`). Диффа кода/тестов нет — критерии 1 и 2 неприменимы.
- Живой исход `pnpm -F agent-protocol cli merge-gate --ref origin/main --pr 145`:
  ```
  merge-gate: documents of power judged by (8): agent-protocol.json, docs/roles/curator.md,
    docs/roles/dev-core.md, docs/roles/pilot-codex.md, docs/roles/devops.md, REVIEWER.md,
    PROTOCOL.md, .github/workflows
  STOP guard 1 · approve on the current head: no approve verdict on 8188be6
  STOP guard 2 · green checks on the same head: not green: review=IN_PROGRESS
  you  guard 3 · ascent to a decision of john's: thread '053-review-bypassed'
  ok   guard 4 · no self-merge on the documents of power: 1 changed path(s), none of them a document of power
  you  guard 5 · a trace of the merge
  ok   mergeability: mergeable=MERGEABLE (mergeStateStatus UNSTABLE)
  REFUSED: a guard does not hold
  ```
  Guard 1/2 STOP — ожидаемое состояние до публикации этого вердикта, не находка. **Guard 4 подтверждает живым прогоном: `docs/box-setup.md` документом власти не является** — мёржит по построению `curator` (см. критерий 5 ниже), не john.

## Критерий 5 (доки власти)

`docs/box-setup.md` в список доков власти этого репозитория (`PROTOCOL.md`, `docs/roles/**`, `REVIEWER.md`, `agent-protocol.json`, `.github/workflows/**`) не входит, и `merge-gate` это подтвердил (guard 4, выше). Значит исключение из правила «approve → curator» здесь не действует — мёржит роль `curator`, не john.

## Критерий 9 (текст vs факт) — одна неточность, не по делу PR

Раздел «Соседство» тела PR утверждает: «Открытый #114 (тред 048) тоже трогает `docs/box-setup.md`, но раздел `0.2` — другое место файла». Проверил диффом `gh pr diff 114`: #114 добавляет раздел `## 0a. Привилегии пользователя...`, а не `0.2` — номер в описании не совпадает с фактом. Расхождение мелкое и не задевает мержабельность (разделы `4a` и `0a` физически далеко друг от друга, конфликта нет), но раз критерий 9 требует сверки каждого утверждения описания, а не только про сам дифф — называю. Правки не требует, апрув не блокирует.

Факты объекта branch protection (`required_approving_review_count: 0`, отсутствие `required_status_checks`, `allow_force_pushes`/`allow_deletions: false`, `enforce_admins: false`), заявленные в новом разделе, независимо перепроверить не смог: `gh api repos/.../branches/main/protection` под токеном ревьюера отвечает `403 Resource not accessible by integration` (не 404 — значит защита, вероятно, есть, но объект этому токену не виден); `gh api repos/.../rules/branches/main` → пустой список, это совпадает с заявленным «рулсетов нет». Называю это как факт своей проверки, а не как находку: утверждения о защите исходят из решения и замера john/curator в самом треде (`13-10-56Z-curator.md`), не из воображаемого источника, и раздел прямо помечает, какая часть (`enforce_admins`) — вывод, а не замер.

## Критерии 1–4, 6–8, 10–11

Не применимы (документация, ни кода, ни тестов, ни изменений схемы/почты/зон не затронуто); зона `curator` (`writes`: `docs/roles`, `PROTOCOL.md`, `REVIEWER.md`, `docs/protocol-reference.md`, `docs/install-notes.md`; `forbidden`: `packages`, `.github/workflows`, `agent-protocol.json`) диффа не касается ни `writes`, ни `forbidden` — путь `docs/box-setup.md` вне обоих списков, зелёный по правилу «дверь судит только по forbidden».

## Итог

Находок, требующих правки, нет. Единственное отмеченное расхождение (критерий 9, номер раздела соседнего PR) не относится к предмету этого диффа и апрув не блокирует.

## msg-018 · from: github · 2026-08-30 · expects: none

PR #145 (docs(box-setup): защита ветки main — форма, замер API и чего она не закрывает (тред 053)) **merged** by maysway → `main`.

## msg-019 · from: curator · 2026-08-30 · expects: ack

**Требуется решение john (единственный открытый предмет треда): заводить ли в `docs/protocol-reference.md` строку-рецепт «ревью уже влитого кода» — PR-заглушка на родителе squash-коммита, база подтянута до `main` по файлу воркфлоу ревьюера, база не `main`, метка после зелёного на той же голове? Скажешь «заводи» — сделаю сам обычным маршрутом (файл не док власти, моя зона, моя кнопка); скажешь «не надо» — рецепт остаётся в этой ленте.** Всё остальное по треду закрыто: #145 смёржен моей рукой, и этим же merge закрыта твоя полевая проверка №1 — штатный маршрут роли защитой ветки НЕ сломан.

## 1. Merge #145 — след гарда 5, гарды поимённо

Дверь: `merge-gate --ref origin/main --pr 145 --review-workflow 'Claude PR Review'`, прогнана моей рукой в 13:41Z перед кнопкой:

- **гард 1 `ok`** — `approve` от `github-actions` на голове `8188be672015220b94db11936cc0a13918dd7fbc`, **внутри круга `33314452316`** воркфлоу `Claude PR Review` на этой же голове (окно 13:32:27Z…13:36:46Z). Вердикт-сироты нет: голова после вердикта не двигалась. Сам вердикт — сообщение `reviewer-pr` в этом треде, шапка `verdict: approve` / `pr: 145`, находок, требующих правки, нет;
- **гард 2 `ok`** — два чека зелёные на ТОЙ ЖЕ голове: `checks=SUCCESS` (прогон `33314242038`, 4m8s), `review=SUCCESS` (прогон `33314452316`, 4m14s). Полный SHA сверен дверью, не глазом;
- **гард 3 — восхождение к слову john:** решение john, доставка чата ~13:10Z полем `delivers` (сообщение `2026-08-30T13-10-56Z-curator.md` в этом треде), раздел «Что теперь проверяется», подпункт 3 дословно: «Строку о защите внести в `docs/box-setup.md` … а завтра кто-то будет гадать, почему push отказал». Класса «полевой измеренный дефект» не заявляю и он не нужен — слово john есть прямое;
- **гард 4 `ok`** — дифф один файл, `docs/box-setup.md`, доком власти не является; дверь вывела список из восьми путей данными и этого файла в нём нет;
- **гард 5 — этот раздел.**

`mergeable=MERGEABLE (CLEAN)`. Merge — squash, заголовок Conventional Commits.

**Контрольное чтение цели (ответ инструмента ≠ факт):** `gh pr view 145` → `state: MERGED`, `mergedAt: 2026-08-30T13:41:46Z`, `baseRefName: main`, `mergeCommit: 6d70cf9a35ed96982946863a2dcb77628cfb93b4`. Голова `main` — `6d70cf9a`, и раздел на месте: `git show origin/main:docs/box-setup.md` печатает `## 4a. Защита ветки main — рука john, разово, на РЕПОЗИТОРИЙ (не на ящик)` между §4 `init github` и §5 `doctor`. Ветка `docs/053-main-branch-protection` в origin снесена (`git ls-remote --heads origin 'docs/053-*'` пуст).

Одна оговорка про саму кнопку, чтобы следующий читатель не принял её за поломку: `gh pr merge 145 --squash --delete-branch` вернул `could not determine current branch: failed to run git: not on any branch` и `EXIT=1`. Это отказ УДАЛЕНИЯ ветки из detached-воркдерева роли, а не отказ merge — merge при этом состоялся (факты выше), ветку я снёс отдельным вызовом API.

## 2. Твоя полевая проверка №1 закрыта — фактом, а не рассуждением

Проверка звучала так: «Штатный маршрут роли не сломан — первый же merge рукой curator после включения защиты; если GitHub откажет, доложить немедленно, это регресс настройки». **Этим первым merge был #145, и GitHub не отказал.** Защита `main` включена (объект `branches/main/protection` живой, снят в 13:3xZ), merge роли прошёл — потому что роль мержит вызовом API pull request, а не push'ем, и требования `approve` в правиле нет. Риск №2 из моего письма `10-37-14Z` при выбранной тобой форме не реализовался, как и было предсказано в письме-доставке.

Две другие твои проверки: №2 (прямой push в `main` отказывает) специально не проверялась и не должна — фиксируется первым случайным случаем; №3 (строка в доки) — это и есть #145.

**Проверяемость п.2 постановки (карточки, #133) по-прежнему НЕ закрыта** и закроется сама: «следующий перебазированный PR приходит на кнопку с вердиктом на СВЕЖЕЙ голове». За закрытую не выдаю.

## 3. Сосед по файлу проверен своей рукой — #114 моя кнопка не уронила

`docs/box-setup.md` правит ещё и открытый #114 (тред 048). После посадки #145 прогнал дерево слияния: `git merge-tree --write-tree origin/main refs/curator/pr114` → exit 0, дерево `ff508b26`, ни одного конфликтного маркера. То есть #114 садится на новую голову `main` без починки, и ноты «база уехала» ему этот merge не создал.

Заодно закрываю находку ревьюера по критерию 9: в описании #145 я назвал раздел соседнего #114 «`0.2`», а он `## 0a. Привилегии пользователя…` — номер в описании неверен. Факт, ради которого он назывался (разделы физически далеко, конфликта нет), замером подтверждён; описание смёрженного PR не переписываю, а неточность фиксирую здесь, чтобы она не осталась висеть неназванной.

## 4. Хвост открытых PR прогнан — ни одной моей кнопки, и это гард 5, а не лень

`gh pr list --state open` → шесть: #144, #143, #142, #141, #134, #114. Строку `thread:`/`role:` каждого прочёл сам, а не принял из прошлого хода ленты: #144 → `042-unaccepted-turn-silent`, #143 → `055-mirror-post-0-2-6-to-lle`, #142 → `054-resume-prompt-role-flag`, #141 → `047-devops-role`, #134 → `052-pr-template`, #114 → `048-session-privileges`; у всех шести `role: dev-core`. **Ни один не принадлежит треду 053**, а след гарда 5 обязан лечь в тред своего PR — этот прогон привязан к 053 и писать в чужие треды не может. Поэтому дверью их не гоняю и кнопок не нажимаю: это работа тактов их тредов.

#140, который я в прошлом ходе оставил в ленте как «готов к кнопке и ждёт своего треда», сел в `main` (`e82ddac5`) — находка не умерла вместе с сессией и отработана.

## 5. Почему тред остаётся открытым, и что в нём стоит

Три пункта постановки (msg-001) закрыты все: п.1 — срез #123 отревьюен задним числом заглушкой #138, `approve`, находок нет; п.2 — #133 смёржен твоей кнопкой 12:44:09Z; п.3 — защита включена твоей рукой и задокументирована #145. Открытых работ в треде нет.

Стоит ровно два предмета, оба твои и оба вопросами, а не работой:

1. **вопрос первой строки** — строка-рецепт в `docs/protocol-reference.md`. Цена входа в рецепт измерена и стоит того, чтобы её записали: ревьюер отказывается судить смёрженный PR (это его суждение, а не механика — повтор купит тот же отказ), и гард самопропуска действия срабатывает, если файл воркфлоу на голове отстал от `main`. Оба отказа стоили по кругу ревью. Своего слова john на строку нет, а под «полевой измеренный дефект, новой нормы не вводит» она не подходит: это не починка сломанного, а запись нового знания;
2. **обязательный `approve` в защите ветки** — не отклонён, а отложен твоим словом «пока не включал». Предмет треда (PR без вердикта на кнопке) выбранной формой не закрыт по построению, и я к нему вернусь ценой в фактах, если случай повторится. Сейчас нового факта нет, поэтому и вопроса не задаю.

Закрыть тред приёмкой сейчас не могу и не хочу: в закрытый тред постановка мертва, а вопрос п.1 живой.

## 6. Ход и незакоммиченное

`waiting-on: curator`, `parked-on: john`, `expects: ack` — ход остаётся у того, кто несёт вопрос; парковка с `ack` звонит сразу и несёт первую строку. Любого твоего слова в тред достаточно, чтобы я подобрал ход.

Незакоммиченного за мной нет: правка `docs/box-setup.md` уехала в `main` коммитом `6d70cf9a`, рабочее дерево роли чистое (`git status --porcelain` пуст), локальных веток за собой не оставил.

## msg-020 · from: curator · 2026-08-30 · expects: none

**Слово john полем `delivers` (чат ~13:46Z): «ЗАВОДИ» — строка-рецепт «ревью уже влитого кода» пишется в `docs/protocol-reference.md`. Парковка msg `13-44-03Z` снята, единственный открытый предмет треда закрыт словом.**

## Что записать — и почему именно это, а не «как сделать заглушку»

Ценность рецепта не в шагах, а в **двух отказах, которые он обходит**, и оба измерены кругами ревью:

1. **Ревьюер отказывается судить смёрженный PR.** `workflow_dispatch` по номеру #123 прочёл «PR уже закрыт» и вердикта не написал (прогон `33306184637`, `conclusion: failure`). Это его суждение, а не механика: повтор той же формы купит тот же отказ;
2. **Гард самопропуска действия срабатывает, если файл воркфлоу ревьюера на голове отстал от `main`** — первая заглушка (#135) была закрыта именно поэтому; в репозитории это уже измерено раньше (прогон `30643932101` по #106).

Отсюда рабочая форма, и её надо записать целиком, иначе рецепт неполон: **PR-заглушка на РОДИТЕЛЕ squash-коммита; файл воркфлоу ревьюера подтянут до `main` на обеих ветках; база — НЕ `main`** (чтобы заглушку нельзя было влить даже по ошибке); метка вешается после зелёного `checks` на той же голове; после вердикта заглушка **закрывается без merge**, ветки сносятся. И честная граница, которая обязана ехать с рецептом: **гард 1 задним числом это не закрывает** — вердикт анкерится на прогон с событием `pull_request` на голове ТОГО PR; это ревью-как-информация о влитом коде, историю merge оно не переигрывает.

## Форма исполнения

Файл доком власти не является, зона твоя, кнопка твоя — обычным маршрутом, как ты и предложила. Класс «полевой измеренный дефект» не заявляется: это запись нового знания, и потому john спросили, а не поставили перед фактом. Слово получено.

## Что остаётся в треде после этого

Один предмет и он не работа: **обязательный `approve` в защите ветки** — отложен словом john «пока не включал», не отклонён. Возврат — по факту, если случай «PR без вердикта на кнопке» повторится. Тред держи открытым, пока рецепт не сядет; закрывать — твоим тактом после него.

waiting-on → curator.

## msg-021 · from: curator · 2026-08-30 · expects: none

**Слово john исполнено: рецепт «ревью уже влитого кода» записан в `docs/protocol-reference.md` — PR [#149](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/pull/149), голова `040cd76f85178e40ef67c7910f409c255aac391e`, `checks` идёт (прогон `33316998530`, `event=pull_request`, на этой же голове). Метку `review` вешает следующий такт — ПОСЛЕ зелёного и на ту же голову; у конвейера не стою, паркуюсь `run:149`.**

## 1. Что записано — и почему форма, а не «как сделать заглушку»

Новый раздел «Ревью уже влитого кода — заглушка на родителе squash-коммита (тред 053)» встал между «Ограничением ревьюера, заданным платформой» и «PR красный из-за состояния репозитория». Место выбрано не по смыслу вообще: рецепт обходит ровно тот же гард самопропуска действия, что и раздел выше, — только там гард ловит ПРАВКУ файла воркфлоу, а здесь его ВОЗРАСТ на голове заглушки. Соседние абзацы читаются как одна тема, а не как два разных сюжета.

Содержание — то, что названо в моём предыдущем сообщении, целиком и без добавок:

- **два отказа, каждый с номером прогона:** ревьюер отказывается судить смёрженный PR (`workflow_dispatch` по #123 → «PR уже закрыт», прогон `33306184637`, `conclusion: failure`; это его суждение, не механика — повтор купит тот же отказ), и самопропуск действия при отставшем на голове файле воркфлоу (первая заглушка #135: `git diff origin/main <голова> -- .github/` → `claude-review.yml`, −25 строк; класс измерен раньше — прогон `30643932101` по #106);
- **рабочая форма из шести шагов:** база = родитель squash-коммита ПЛЮС коммит, подтягивающий `claude-review.yml` до `main`; голова = `cherry-pick`, а не сам SHA (на влитом коммите висят прогоны, поднятые пушем в `main`, — заглушка на нём собирала бы чужие чек-раны); база НЕ `main`; срез сверяется трёхточечным замером против `git show --stat <sha>`; метка после зелёного на той же голове и передача хода; закрытие без merge, ветки снести;
- **граница, названная дословно:** гард 1 это задним числом НЕ закрывает — вердикт анкерится на прогон с событием `pull_request` на голове ТОГО PR; ревью-как-информация, историю merge не переигрывает;
- **побочный эффект:** с версией воркфлоу из `main` вердикт приезжает ещё и шапкой `verdict:`/`pr:`.

Все числа и SHA взяты из ленты этого треда (сообщения `dev-core` 11:0xZ–12:1xZ, вердикт `reviewer-pr` по #138, мой доклад 12:5xZ), а не из памяти сессии.

## 2. Восхождение (гард 3) — слово john прямое

Решение john в ленте, доставка чата ~13:46Z полем `delivers`, дословно «ЗАВОДИ» — на вопрос первой строки моего сообщения `13-44-03Z`. Класс «полевой измеренный дефект, новой нормы не вводит» не заявляю и он не нужен: это запись НОВОГО знания, а не починка сломанного, — потому john и спросили, а не поставили перед фактом. Файл доком власти не является (`docs/protocol-reference.md` назван таким прямо в карточке роли), зона роли зелёная, значит после вердикта кнопка моя.

## 3. Проверяемость — сказана в описании PR, повторю здесь коротко

Автоматического класса у этого диффа нет и заводить его не за что: документация. Машинно проверяется только то, что и так проверяет CI. Живая приёмка — чтение ревьюером: совпадает ли записанная форма с тем, что реально произошло в треде (номера прогонов, SHA, порядок шагов). Сознательно НЕ покрывается повторным живым прогоном заглушки: цена — круг ревьюера и две служебные ветки, а форма уже измерена один раз целиком.

## 4. Соседство по файлу — пятеро, и это не «конфликт», а порядок посадки

`docs/protocol-reference.md` правят ещё пять открытых PR, и я прочёл их описания сам, а не принял из ленты: **#148** (`057-circuit-ping-flaps`), **#146** (`036-account-failover`, метка `review`), **#142** (`054-resume-prompt-role-flag`), **#141** (`047-devops-role`), **#134** (`052-pr-template`) — у всех `role: dev-core`. Не трогают файл только #143 и #114.

Практическое следствие, а не тревога: разделы разные, но хвост файла общий, и **кто сядет вторым — гоняет дерево слияния своей рукой** сразу после чужого merge. Если #149 сядет не первым, я прогоню `merge-tree` по своей голове перед кнопкой.

## 5. Хвост открытых PR прогнан — ни одной моей кнопки

`gh pr list --state open` → семь: #149 (мой, не готов), #148, #146, #143, #142, #141, #134, #114. Строку `thread:`/`role:` каждого прочёл сам: все шесть чужих принадлежат чужим тредам и все `role: dev-core`. **Ни один не принадлежит треду 053**, а след гарда 5 обязан лечь в тред своего PR — этот прогон привязан к 053. Поэтому дверью их не гоняю и кнопок не нажимаю: это работа тактов их тредов.

## 6. Что остаётся в треде после этого хода

Один предмет и он не работа: **обязательный `approve` в защите ветки** — отложен словом john «пока не включал», не отклонён. Нового факта под него нет, вопроса не задаю. Тред держу открытым, пока #149 не сядет; закрытие приёмкой — такт после merge.

## 7. Ход и незакоммиченное

Незакоммиченного за мной нет: правка лежит коммитом `040cd76f85178e40ef67c7910f409c255aac391e` в ветке `docs/053-retro-review-recipe` в origin, рабочее дерево роли чистое (`git status --porcelain` пуст).

`waiting-on: curator`, `parked-on: run:149` — жду зелёного `checks` по #149 на голове `040cd76f…`. Разморозка штатная и предусмотрена кодом: зелёный `checks` на PR БЕЗ метки `review` — исключение «следового» класса уведомителя, оно называет в `waiting-on` роль автора, а адрес письма берётся из `thread: 053-review-bypassed` в описании #149, то есть придёт сюда. Следующий ход: метка `review` на зелёную голову → круг → вердикт → гарды → merge со следом. Покраснеет — разберу тем же порядком.

## msg-022 · from: curator · 2026-08-30 · expects: none

**Метка `review` повешена на #149 — на ту же голову `040cd76f85178e40ef67c7910f409c255aac391e`, ПОСЛЕ зелёного `checks` на ней, и круг поднялся событием: прогон `33318798271`, воркфлоу `Claude PR Review`, `event=pull_request`, `in_progress`, старт 15:08:31Z. У конвейера не стою — метка это конец хода, а не начало ожидания: паркуюсь `run:149` и передаю ход.**

Команда следующего такта, первой строкой, чтобы её не пришлось вспоминать:

```
pnpm protocol merge-gate --ref origin/main --pr 149 --review-workflow 'Claude PR Review'
```

Флаг `--review-workflow` — часть процедуры, а не опция: без него гард 1 молча отвечает `by-hand`.

## 1. Зелёный замерен своей рукой, а не принят из ленты

`gh pr checks 149` → `checks pass 4m10s`. Прогон прочитан отдельно: `33316998530`, `workflowName=checks`, `event=pull_request`, `status=completed`, `conclusion=success`, `headSha=040cd76f85178e40ef67c7910f409c255aac391e`, окно 14:29:27Z…14:33:42Z. `gh run list --commit 040cd76f…` до навешивания метки печатал ровно один прогон — этот; кругов ревью на голове не было вовсе.

То есть порядок карточки соблюдён буквально: зелёный `checks` → метка на ТУ ЖЕ голову. Голова между замером и меткой не двигалась — `headRefOid` после `gh pr edit --add-label review` тот же `040cd76f…`.

## 2. Круг действительно поднялся — это факт прогона, а не наличие метки

Метка сама по себе не означает, что круг был (карточка, «Круг поднимает СОБЫТИЕ навешивания»), поэтому перечитал прогоны на голове ПОСЛЕ навешивания: появился `33318798271` — `Claude PR Review`, `event=pull_request`, `status=in_progress`. Событие `labeled` сработало, вердикт приедет в этот тред сам.

## 3. Двое соседей по файлу сели в `main`, пока шёл мой зелёный — дерево слияния прогнано

С момента моего прошлого хода в `main` сели #144 (тред 042, 14:06:25Z), #141 (047, 14:39:48Z) и #146 (036, 14:52:31Z); первый и третий трогают `docs/protocol-reference.md` — тот самый файл. Голова `main` сейчас `81cad15ea9965aab1514963a54d710b2373095d8`.

Ноты «база уехала» это моему PR не создало, и это замер, а не расчёт:

- `gh pr view 149` → `mergeable=MERGEABLE`, `baseRefName=main`; `mergeStateStatus=UNSTABLE` — это идущий круг ревью, а не конфликт;
- локальный прогон дерева слияния против свежей `main` (фетч в именованные рефы, чтобы не трогать `FETCH_HEAD`): `git merge-tree --write-tree refs/curator/main-149 refs/curator/pr149` → exit 0, дерево `97df0ed35c2acd581ec0b7eaaf70a3cc185c4e0b`, конфликтных маркеров нет.

Ребейз не нужен, и это существенно: ребейз сдвинул бы голову, обесценил бы только что поднятый круг и стоил бы второго.

## 4. Хвост открытых PR прогнан — ни одной моей кнопки

`gh pr list --state open` → семь: #150 (тред 042, метка `review`), **#149 (мой, тред 053, круг идёт)**, #148 (057), #143 (055), #142 (054), #134 (052), #114 (048, метка `review`). Кроме моего — все чужих тредов, `role: dev-core`; след гарда 5 обязан лечь в тред своего PR, а этот прогон привязан к 053. Дверью их не гоняю и кнопок не нажимаю: это работа тактов их тредов.

## 5. Что остаётся в треде

Работы — одна и она в чужих руках: вердикт круга по #149. После него — гарды дверью, merge со следом гарда 5, и такт спустя приёмка треда закрытием.

Второй предмет не работа: **обязательный `approve` в защите ветки** — отложен словом john «пока не включал», не отклонён. Нового факта под него нет, вопроса не задаю.

## 6. Ход и незакоммиченное

Незакоммиченного за мной нет: правка лежит коммитом `040cd76f85178e40ef67c7910f409c255aac391e` в ветке `docs/053-retro-review-recipe` в origin, рабочее дерево роли чистое (`git status --porcelain` пуст). Локально за собой оставил два служебных рефа замера — `refs/curator/main-149` и `refs/curator/pr149`; дерева они не трогают.

`waiting-on: curator`, `parked-on: run:149` — жду вердикта круга `33318798271` на голове `040cd76f…`. Следующий ход: вердикт → `merge-gate` строкой выше → merge squash → след гарда 5 поимённо (вердикт, полный SHA головы, оба прогона, восхождение к слову john «ЗАВОДИ» из доставки ~13:46Z). Придёт `needs-fixes` — правлю тем же порядком и перевешиваю метку заново, потому что пуш круга не поднимает.
