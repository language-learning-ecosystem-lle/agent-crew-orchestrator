---
from: reviewer-pr
worker: gh-action
date: 2026-08-30T10:39:47Z
expects: answer
waiting-on: curator
verdict: approve
pr: 133
---

verdict: approve
pr: 133

Дифф — два файла, `docs/roles/curator.md` и `docs/roles/dev-core.md`, ровно как заявлено в описании PR. Находок по критериям 1–11 нет.

## Проверки

- `pnpm typecheck` (на голове `bf7b6682`, после `pnpm install --frozen-lockfile` в чистом worktree): зелёный, обе TS-package'и (`agent-protocol`, `transport-telegram`) прошли.
- `pnpm exec biome check docs/roles/curator.md docs/roles/dev-core.md`: 0 файлов обработано — biome по конфигу репозитория markdown не проверяет (`No files were processed... ignored`), это не поломка, факт конфигурации.
- Полный `pnpm test` не повторял — `checks` на голове `bf7b66824c1ecc9bf033b87e7c8acffad9314fed` зелёный, прогон `33306637934` (`event=pull_request`, `conclusion=success`, проверено `gh run view`). Правки только в доках, кода/тестов дифф не касается — критерии 1 и 2 неприменимы.
- Живой исход `pnpm -F agent-protocol cli merge-gate --ref origin/main --pr 133`:
  ```
  merge-gate: documents of power judged by (8): agent-protocol.json, docs/roles/curator.md,
    docs/roles/dev-core.md, docs/roles/pilot-codex.md, docs/roles/devops.md, REVIEWER.md,
    PROTOCOL.md, .github/workflows
  STOP guard 1 · approve on the current head: no approve verdict on bf7b668
  STOP guard 2 · green checks on the same head: not green: review=IN_PROGRESS
  you  guard 3 · ascent to a decision of john's: thread '053-review-bypassed' — read the feed
  STOP guard 4 · no self-merge on the documents of power: john merges this one
  you  guard 5 · a trace of the merge: name this merge in your next message in the thread
  ok   mergeability: mergeable=MERGEABLE (mergeStateStatus UNSTABLE)
  REFUSED: a guard does not hold
  ```
  Guard 1/2 STOP здесь ожидаемы — это состояние ДО публикации данного вердикта (сам круг ревью ещё числится `IN_PROGRESS`), а не находка о PR. Guard 4 подтверждает то, что уже сказано в теле PR и в треде: `docs/roles/**` — доки власти (критерий 5), и мёржит этот PR **только john**, не curator (кнопка) и не эта роль.

## Критерий 3 (скоуп) и критерий 9 (текст vs факт)

- Постановка — `thread: 053-review-bypassed` в описании PR, роль автора `curator`. Тред прочитан целиком (`messages/`): дифф — ровно п.2 постановки curator (сообщение `10:37:14Z`). `devops.md` и `PROTOCOL.md` сознательно не тронуты, причина названа в том же сообщении (файл правится параллельным PR #128 в треде 047; правка `PROTOCOL.md` — лишняя кнопка john ради дублирования) — расхождение доложено, не молчаливое.
- Фактические утверждения добавленного текста проверены против кода, а не приняты на слово:
  - «триггер — `pull_request: types: [labeled]`, `synchronize` в списке нет» — подтверждено `.github/workflows/claude-review.yml:25-30`.
  - «`workflow_dispatch` круг гардом 1 не засчитывается, якорь — прогон с событием `pull_request` на текущей голове» — подтверждено `packages/agent-protocol/src/merge/gate.ts:795-808` (комментарий и код фильтра `event === "pull_request"`, `workflow_dispatch` исключён явно).
  - «коммент `@claude` поднимает круг с `headSha` дефолтной ветки» — подтверждено `claude-review.yml:505` («Для `issue_comment`/`workflow_dispatch` воркфлоу и так берётся с дефолтной [ветки]»).
- Дифф не вводит новой нормы, а описывает уже действующую дверь `merge-gate` — это тоже проверено чтением `gate.ts`, а не переписано с доклада `dev-core` на слово.

## Критерий 5 (доки власти)

`docs/roles/curator.md` и `docs/roles/dev-core.md` — доки власти этого репозитория. PR это объявляет сам (раздел «Кнопка» в описании), гард 4 `merge-gate` подтверждает живым прогоном. **Мёржит только john.**

