---
from: curator
worker: claude-code
session: dcac32e3-0b29-4463-aa9a-c5191ec71e59
date: 2026-08-30T12:50:42Z
expects: ack
waiting-on: curator
parked-on: john
---

**Требуется решение john (п.3, стоит с 10:37Z): включать ли защиту ветки `main` — и с каким набором обязательных проверок? Пп. 1 и 2 закрыты: срез #123 отревьюен (approve по заглушке #138, находок нет), #133 смёржен твоей кнопкой в 12:44:09Z. Из трёх пунктов постановки открыт ровно один — твой.**

Парковку на тебе поднимаю заново с `expects: ack`, как и обещал в своём сообщении 10:49Z, снимая её тогда: ход вернулся ко мне докладом `dev-core`, работы, которую эта парковка заморозила бы, в треде больше нет.

**Одна поправка к предыдущему сообщению в ленте** (чатный curator, доставка слова john о кнопке на #133, ~12:46Z): его перечень остатка называет п.1 идущим («идёт заглушкой #138»). На момент той записи это уже было не так — вердикт лёг в 12:23:43Z, заглушка закрыта, ветки снесены. Письмо писалось по более раннему состоянию; предмет не спорный, но остаток треда сейчас — ОДИН пункт, не два.

## 1. П.1 принят — по своей же «Проверяемости», а не по докладу

Приёмка требовала: «в ленте лежит вердикт `reviewer-pr` с парой `verdict:`/`pr:` по номеру заглушки, и в нём либо перечень находок по коду #123, либо строка "находок нет"». Лежит — сообщение `reviewer-pr` от 2026-08-30, шапка `verdict: approve` / `pr: 138`, находок нет. (Ординалом на него не ссылаюсь: `--tail` двигает ординалы у читателя, а имён файлов `thread show` не печатает.)

Снято моей рукой сейчас, а не переписано из доклада `dev-core`:

- **вердикт не сирота и стоит на судимой голове:** `gh pr view 138 --json reviews` → `github-actions`, `state: APPROVED`, `commit.oid = ed593acf85f074045ec5f9071ade85a4f9c6f5f8`, 12:23:43Z. Круг `Claude PR Review`, прогон `33310891767`: `event=pull_request`, `conclusion=success`, `headSha=ed593acf85f…`, окно 12:13:06Z…12:23:47Z — вердикт лежит ВНУТРИ окна. То есть по форме это ровно тот вердикт, который засчитал бы гард 1, если бы заглушка была настоящим PR;
- **заглушка не смёржена:** `state: CLOSED`, `mergedAt: null` — заявленное первой строкой её тела «НЕ МЁРЖИТЬ» соблюдено;
- **в origin от заглушки не осталось ничего:** `git ls-remote --heads origin 'retro/*'` пуст.

Оговорку постановки держу и не переигрываю: **гард 1 по #123 задним числом это не закрывает**, история merge не переиграна. Что получено — знание о том, что мы влили: 792 строки #123 больше не «непроверенный код», а проверенный код с известным исходом.

Отдельно засчитываю то, чего постановка не требовала, а `dev-core` сделал: **числа тестов вымерены с ОБЕИХ сторон среза** (база 163/2694 → голова 164/2718, разность +1 файл/+24 теста сходится построчно с диффом: 17 `it` + 7 `it`). Это ровно критерий 1, пропущенный в маршруте #123 вместе с кругом, и он единственный, который нельзя было бы восстановить позже.

Расхождение `summary` vs `capabilities`, которое круг нашёл: устранено до него (#127, смёржен 09:54:00Z — через 23 минуты после #123), хвост в `docs/roles/devops.md` ведёт открытый #128 в треде 047. Ни то, ни другое действий по этому треду не требует и предмета 053 не касается.

**Удаление веток `retro/123-*` своей рукой `dev-core` назвал вслух и восхождение указал верно:** это ШАГ 5 моей же постановки, а не самовольная рука. Подтверждаю: названное исполнено, вопроса нет.

## 2. П.2 закрыт твоей кнопкой — контрольное чтение цели сделано

#133: `state: MERGED`, `mergedAt: 2026-08-30T12:44:09Z`, `baseRefName: main`, `mergeCommit: c5127d5304ecf3b2a7a901c7b1fcb58d845194d0`, голова `bf7b66824c1ecc9bf033b87e7c8acffad9314fed`. Своей кнопки на нём не было и быть не могло (гард 4, доки власти) — следа гарда 5 с меня не требуется, читаю цель как факт: строка «круг поднимает СОБЫТИЕ метки; после смены головы метку перевешивать; PR с меткой и без вердикта на текущей голове готовым не является» теперь живёт в `main`, в карточках `curator` и `dev-core`.

Проверяемость п.2 была объявлена полевой: «следующий перебазированный PR приходит на кнопку с вердиктом на СВЕЖЕЙ голове». Она НЕ закрыта этим ходом и закроется сама, первым же таким PR. Не выдаю её за закрытую.

## 3. П.3 — вопрос к тебе, и он не изменился

Состояние снято 10:31Z и с тех пор не пересматривалось: защиты нет ни в старой форме (`branches/main/protection` → 404), ни рулсетом (`rules/branches/main` → пусто). Причина B треда — «кнопка john гардов не спрашивает» — п.1 и п.2 не лечится вообще: п.1 закрыл ПОСЛЕДСТВИЕ одного пропуска, п.2 договорил дисциплину роли. Дверь как не спрашивалась, так и не спрашивается.

Цена, названная дважды и не изменившаяся:

1. **аварийный merge при красном CI станет невозможен** без снятия защиты — лишний шаг у тебя ровно в аварии;
2. **защита закроет не только твою кнопку, но и маршрут `merge-gate` роли curator:** дверь ролей советующая, GitHub откажет по своим правилам. Если правило потребует «review approved», в него должен попадать вердикт бота `github-actions` — иначе штатный маршрут «метка → вердикт → merge curator» упрётся в отказ на ровном месте, и мёржить всё придётся тебе руками;
3. **форма правила — тоже твоё решение:** «требовать успешный круг ревью», «требовать зелёные `checks`», либо и то и другое.

Проверяется один раз руками на тестовом PR, и проверять надо ОБА маршрута — кнопку и `merge-gate`.

## 4. Второй вопрос, дешёвый — рецепт ревью влитого кода в док

`dev-core` вынес из п.1 воспроизводимый рецепт и прямо оставил решение мне; решаю и несу тебе, потому что не уверен в восхождении, а правило последней строки у меня одно.

Рецепт (измерен, а не придуман): **ревью уже влитого кода делается PR-заглушкой на РОДИТЕЛЕ squash-коммита, база подтянута до `main` по `.github/workflows/claude-review.yml`, база — не `main`, метка вешается после зелёного на той же голове.** Цена входа — два отказа, оба названные по имени: ревьюер отказывается судить смёрженный PR (суждение, не механика — повтор купит тот же отказ), и гард самопропуска действия срабатывает на отставшем от `main` файле воркфлоу.

Дом строки — `docs/protocol-reference.md` (поведение кода, НЕ док власти, моя зона и моя рука, обычные гарды). Что спрашиваю: **строку завожу?** Своего слова john на неё нет, а на «дефект, новой нормы не вводит» она не тянет — это не починка сломанного, а запись нового знания. Скажешь «заводи» — сделаю сам обычным маршрутом; скажешь «не надо» — рецепт остаётся в этой ленте и в этом сообщении.

**В этом ходе PR по нему НЕ открыт, и это осознанно:** тред сейчас паркуется на тебе, а парковка замораживает его целиком — открытый PR всё равно стоял бы до твоего слова, только ещё и с висящим кругом ревью. Цену называю вслух, как и в прошлый раз.

## 5. Хвост открытых PR — прогнан, и почему ни одной кнопки

`gh pr list --state open` → три PR, и **ни один не мой кандидат**: #140 (`thread: 042-unaccepted-turn-silent`), #134 (`thread: 052-pr-template`), #114 (`thread: 048-session-privileges`) — у всех `role: dev-core`.

#140 прогнал дверью, потому что он выглядит готовым: `merge-gate --ref origin/main --pr 140 --review-workflow 'Claude PR Review'` → `ok` гарды 1 (approve на `62b5d5b` внутри круга 33311235228, окно 12:20:50Z…12:24:09Z), 2 (`review=SUCCESS`, `checks=SUCCESS`) и 4 (доков власти дифф не трогает), `mergeable=CLEAN`. **Кнопки не нажимаю по гарду 5:** след merge обязан лечь в тред, которому PR принадлежит, — в 042, а этот прогон привязан к 053 и писать туда не может. Дверь отдельно печатает ноту «база уехала после зачтённых чеков» (`6718fc2` в 12:45:59Z против старта `checks` в 12:10:44Z) — её закрывает прогон дерева слияния, и это работа того такта, а не этого.

Практический смысл строки: **#140 готов к кнопке и ждёт только своего треда.** Кладу это сюда, чтобы находка не умерла вместе с сессией.

## 6. Ход и незакоммиченное

`waiting-on: curator`, `parked-on: john`, `expects: ack` — ход остаётся у того, кто несёт вопрос. Оба вопроса стоят выше явно: п.3 (защита ветки, первой строкой) и строка рецепта в `docs/protocol-reference.md` (раздел 4). Любого твоего слова в тред достаточно, чтобы я подобрал ход.

Незакоммиченного за мной нет: в этом ходе я не писал ни в код, ни в доки — только читал и мерил. Рабочее дерево роли чистое.

