---
from: curator
worker: claude-code
session: 7d198517-fa51-48b4-9c8a-69f5acabb92e
raised: 2026-09-04T16:59:04Z
date: 2026-09-04T17:01:36Z
expects: ack
waiting-on: curator
parked-on: john
---

**Требуется решение john. Разрешаешь ли merge #272 — адресация эскалации ревьюера переведена с литерала `077-notifier-down` на слаг стоячего адреса `notifier-down`?** Дефект выведен ЧТЕНИЕМ КОДА, полем не замерен, поэтому класс «полевой измеренный дефект» я к нему не применяю и прошу слово. Новых прав, полей конфига и форм сообщения дифф не вводит; доков власти не трогает; гарды 1, 2 и 4 замерены дверью и стоят зелёными. Кроме твоего слова, кнопке не хватает ничего.

## 1. Что покупается этим merge

Сегодня `.github/scripts/review-delivery.sh` адресует эскалацию (звонок «вердикт ревьюера НЕ доставлен») литералом `077-notifier-down` — именем каталога. Стоячий адрес `notifier-down` по уставу (тред `080`, твоё решение 2026-09-03) предполагает, что текущий приёмник когда-нибудь ЗАКРОЮТ. В тот день письмо продолжит доставляться — дверь письмо в закрытый тред принимает, — но кандидатом тред не станет и никого не поднимет. Красноты не будет: будет тихая потеря звонка об отказе доставки вердикта. Цена наступает не от нагрузки, а от штатного закрытия треда.

После диффа адрес выбирает ДВЕРЬ: `--ensure-thread notifier-down` берёт старший открытый `NNN-notifier-down`, а если открытого нет — открывает следующий и им же поднимает роль. Ровно так уже адресует `notifier-watch.yml` — второй писатель того же класса звонков. Два писателя одного класса перестают адресовать по-разному.

## 2. Гарды — замерено дверью 17:00Z, не пересказ

`pnpm protocol merge-gate --ref origin/main --pr 272 --review-workflow 'Claude PR Review'`, `origin/main` = `80908d3e25c6ab820c517ac7b8d3b57dfd48d104`:

| гард | ответ двери |
| --- | --- |
| 1 · approve на текущей голове | **ok** — `approved on 2cfd2e1 by github-actions`, внутри круга [`33897798913`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/33897798913) воркфлоу `Claude PR Review` на ЭТОЙ голове (окно 16:55:26Z…16:58:58Z) |
| 2 · зелёный `checks` на той же голове | **ok** — `review=SUCCESS, checks=SUCCESS` |
| 3 · восхождение к решению john | **you** — это письмо |
| 4 · доки власти | **ok** — `4 changed path(s), none of them a document of power`; дверь вывела 8 доков власти (`agent-protocol.json`, четыре карточки ролей, `REVIEWER.md`, `PROTOCOL.md`, `.github/workflows`) |
| 5 · след merge | **you** — лягу следующим письмом в этот тред |
| сливаемость (не гард, факт) | `MERGEABLE` / `CLEAN` |

Голова PR #272 — `2cfd2e1fc7c8437ecc4dd36a62f8b349a7c8fb32`, перемерена мной `git fetch origin pull/272/head` в 17:00Z: с момента вердикта не двигалась.

Своей рукой, а не докладом: `git diff --stat origin/main...2cfd2e1f` — четыре файла, +265/−34: `.github/scripts/review-delivery.{sh,test.sh,integration.sh}` и `docs/protocol-reference.md`. `.github/workflows/**`, `agent-protocol.json`, `docs/roles/**`, `PROTOCOL.md`, `REVIEWER.md` — не тронуты ни одним. Литерал `[0-9]{3}-notifier-down` в `review-delivery.sh` на голове — **0 совпадений**, контроль того же шаблона на `origin/main` — **3**: чистота не от кривизны шаблона.

## 3. Почему прошу слово, а не закрываю гард 3 исключением

Класс «полевой измеренный дефект, новой нормы не вводит» держится на трёх условиях, и первое — первоисточник ЗАМЕРА: лента, дата, номер треда. У меня его нет. Предмет я вывел чтением кода `origin/main` (`review-delivery.sh:272`, `:360`, `:423`, `:472` против `notifier-watch.yml:165`, `:306`), поле здесь не мерило ничего: отказ доставки вердикта по требованию не воспроизводится, а ждать его — жечь такт. Гипотеза причины первоисточником не является, и своё чтение я за замер не выдаю. Значит гард 3 стоит в исходном виде — твоё слово в ленте этого треда. Правило последней строки применяю в ту же сторону: не уверен — значит к тебе.

## 4. Что дифф НЕ делает

- `077-notifier-down` не закрывается и не трогается — предмет адресация, а не текущий приёмник (в голове PR число `077` встречается пятью строками, и ни одна не адрес: историческая справка, данные проверки, ссылка на требование, контрольная строка грепа);
- `notifier-watch.yml` не правится — он уже адресует правильно;
- новых прав, полей конфига и форм сообщения нет: `--ensure-thread`/`--title`/`--participants` — существующие флаги двери, ими же адресует `notifier-watch.yml`;
- поведение при паркованном (но открытом) приёмнике осталось ТАКИМ ЖЕ: известное расхождение с дверью и односторонняя цена, названные в `notifier-mute.sh`. Эта работа его не расширяет и не сужает.

## 5. Два круга ревью, обе находки закрыты

Круг 1 (`33894357064`, голова `cd4a3942`) — `needs-fixes`, две находки, обе мелкие: (1) число проверок базы в описании PR («было 70», фактически 68, прирост +23, а не +21); (2) фраза в `docs/protocol-reference.md` «Приёмка — состояния (14)/(15) того же интеграционного прогона» реассертирована диффом, а прогон по этим состояниям на текущем `main` красный — по чужой причине (#265, тред `072`), но доку нельзя оставлять утверждение, которое не держится.

Обе починены двумя докс-коммитами (`a7f80ebf`, `2cfd2e1f`), кода не трогают. Круг 2 (`33897798913`, голова `2cfd2e1f`) — `approve`, **находок нет**, обе находки первого круга ревьюер перемерила своей рукой и закрыла.

Хвост находки 2, к этому диффу отношения не имеющий, — сюита `review-delivery.integration.sh` красна на чистом `origin/main` (8 состояний из 15, 24 проверки) и в прогоны `checks` не входит вовсе, поэтому её краснота никого не подняла и ни одного мержа не остановила, — вынесен отдельным домом: тред `127-verdict-delivery-suite-red-on-main`, `waiting-on: dev-core`. Сюда он не тащится ни строкой, и на решение по #272 не влияет: адресация от него не зависит.

## 6. Косметический хвост — назван dev-core, НЕ чиню

dev-core сама нашла в своём диффе две мелочи доки: слово «Вариант» осталось одно на строке (`docs/protocol-reference.md:4144`) и одна вставленная строка длиной 121 символ (`:4105`). Обе внутри одного абзаца markdown, в рендере склеиваются; `.md` не линтует ничто из `checks` (`biome check` на изменённых файлах не применяется — мерил ревьюер), и во всём файле 228 строк уже длиннее 100. Цена починки — новая голова, новый круг `checks`, снять-повесить метку, ещё один такт экипажа. Не стоит того; решение моё, осознанное. Поедет `review-delivery.sh` или этот док в правку по другому поводу — возьмётся попутно.

## 7. Что будет после твоего слова

Merge squash моей рукой (гарды 1, 2, 4 уже зелены), контрольное чтение `base.ref`, след гарда 5 поимённо в этом треде — вердикт, голова, чеки — и закрытие треда: предмет исчерпан. Скажешь «нет» или «переделать» — PR стоит, кнопка не жмётся.

---

**Чей ход.** Мой (`waiting-on → curator`): роль с `wake.mode: 'self'` контур разбудить не может, поэтому ход остаётся у того, кто несёт вопрос. Парк — `--parked-on john`, `--expects ack`: звонит сразу, без возрастного порога, первой строкой вопроса. Парк ставлю ПОСЛЕ вердикта, а не до, ровно чтобы не проглотить письмо `reviewer-pr` — полевой случай треда `072`; вердикт лёг, глотать больше нечего.

**Незакоммиченного нет:** в этот такт я не сделал ни одного коммита. Сделано — чтение ленты целиком, `merge-gate` (замер выше), сверка головы и диффа `git fetch origin pull/272/head`, греп литерала с контролем, проверка снятия парка `run:272` чтением своей почты, и это письмо. Рабочее дерево curator чистое, `HEAD` = `80908d3e`, временные файлы лежат в `/tmp` вне обоих чекаутов.

**Следующей сессии curator, если эта не доживёт:** ждать ровно слова john в этой ленте; когда оно есть — `merge-gate` перемерить (голова могла уехать, база двигается), нажать squash, положить след гарда 5 и закрыть тред.

