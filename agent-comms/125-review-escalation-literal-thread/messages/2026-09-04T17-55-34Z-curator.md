---
from: curator
worker: claude-code
session: c4a6f505-dcc4-4513-b495-f863b985600d
raised: 2026-09-04T17:45:26Z
date: 2026-09-04T17:55:34Z
expects: ack
waiting-on: curator
parked-on: john
---

**Требуется решение john. Разрешаешь ли merge #272 — адресация эскалации ревьюера переведена с литерала `077-notifier-down` на слаг стоячего адреса `notifier-down`? Голова `55179e28`, вердикт `approve` третьего круга, гарды 1, 2, 4 зелены дверью 17:46Z. Вопрос тот же, что в моём письме 17:49:45Z; это письмо ничего в нём не меняет и второго звонка не делает — оно дописывает два факта, померенных ПОСЛЕ него.**

## 1. Звонок состоялся — и это замерено, а не предположено

`.orchestrator/notify.state`, прочитан 17:50:05Z, через двадцать секунд после моего парковочного
письма:

```
parked	john	125-review-escalation-literal-thread	2026-09-04T17:49:45Z
```

Строки не было ни в 17:47Z, ни раньше. Значит вопрос к john доехал — впервые за 48 минут — и мой
прошлый вывод «первое письмо не прозвенело» подтверждён контролем на том же треде, той же роли и тех
же флагах.

**Это письмо звонка не повторяет.** Ключ парка у курьера — пара (человек, тред), а не метка времени
(`packages/agent-protocol/src/notify/notify.ts`, `parkedKey`, ~стр. 1073, дефект Д-2 треда `030`):
парк, уже объявленный, второй раз не звонит, а становится строкой в сводке. Поэтому первая строка у
этого письма та же самая — если сводка её перечитает, john увидит тот же вопрос, а не мой разбор.

## 2. Дом классу открыт — это сделано, а не оставлено следующему такту

В моём прошлом письме §1 я записала «открыть дом классу — первое, что берёт следующая сессия».
**Сделано в этот же такт: тред `129-park-bell-silent-letter`, `waiting-on: dev-core`**, происхождение
названо этим тредом. Пункт из списка «следующей сессии» СНИМАЮ, чтобы его не сделали дважды под другим
номером.

Там же — механизм, который я нашла уже после открытия, чтением кода (и в `129` он записан вторым
письмом): **парк на человеке, объявленный на своём ходу, снимается следующим письмом, назвавшим в
`waiting-on` кого-то другого** (`standingParkOf`, `packages/agent-protocol/src/thread/thread.ts` ~стр.
775; норма — тред `042`, решение john 2026-08-29), а курьер помнит только текущий состав, не журнал.
Письмо 17:01:36Z обогнали пять чужих писем (последнее 17:44:56Z) — парка к такту курьера уже не было.
Письмо 17:49:45Z никто не обогнал — оно прозвенело. Это объясняет оба замера одним правилом и снимает
мою прежнюю зацепку про `--parked-on run:272`.

## 3. Что из этого следует ЗДЕСЬ и сейчас — для следующей сессии curator

Парк на john по этому треду **жив ровно до следующего чужого письма в него**. Смотритель пишет сюда сам
(msg-016 — его письмо о `CONFLICTING`), и любое такое письмо снимет парк молча: напоминания прекратятся,
а строка в состоянии останется. Порядок для следующего такта, по шагам:

1. **прочитать `.orchestrator/notify.state`** — стои́т ли ещё `parked john 125-…`, и есть ли `remind
   john 125-…`;
2. **прочитать ленту** — легло ли после 17:49:45Z чужое письмо. Легло, а `remind` не растёт — **встать
   в парк заново** (`--expects ack --parked-on john`, первой строкой тот же вопрос). Второго звонка это
   не даст, пока ключ (john, `125`) в состоянии; даст, если чужое письмо парк сняло, — и тогда он и
   нужен;
3. **есть слово john** → `merge-gate` перемерить (база сегодня двигалась трижды), нажать squash,
   положить след гарда 5 поимённо — вердикт, голова, чеки, — и закрыть тред.

Ничего из этого не меняет предмета `125` и не трогает PR #272: гарды 1, 2, 4 зелены на `55179e28`,
кнопке не хватает только слова john.

## 4. Хвост открытых PR — прогнан, кандидатов у меня нет

`gh pr list --state open` в 17:52:54Z: три PR — #272 (этот тред), #273 (тред `126`), #271 (тред `124`).
Все три `MERGEABLE`; #273 и #271 стоят за словом john по СВОИМ тредам (`parked john 126-…` 16:40:25Z и
`parked john 124-…` 15:59:20Z — обе строки в состоянии, обе прозвенели), их гард 3 закрыт не мной и не
здесь. Домерживать в этот такт нечего.

---

**Чей ход.** Мой (`waiting-on → curator`), парк — `--parked-on john`, `--expects ack`, вопрос первой
строкой.

**Незакоммиченного нет:** ни одного коммита в этот такт. Сделано — чтение ленты, `merge-gate` 17:46:52Z,
сверка головы и путей диффа своей рукой, греп литерала с контролем, два чтения `notify.state` (17:47Z и
17:50:05Z), открытие треда `129` с контрольным чтением почты dev-core, чтение `standingParkOf` и
`parkedKey`, и два письма. Рабочее дерево curator чистое, `HEAD` = `454ecf42`, временные файлы — в
`/tmp` вне обоих чекаутов.

