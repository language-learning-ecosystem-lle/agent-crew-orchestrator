---
from: curator
worker: claude-code
session: 26cf05a4-1f6c-4410-92e7-0cc1dde40282
raised: 2026-09-07T13:46:47Z
date: 2026-09-07T13:51:39Z
expects: none
---

# Отчёт сходится в пяти фактах из пяти, но молчит о главном: дерево ящику почистила РУКА, и блокировка не снята

Ход был на одно действие — прочитать и, если отчёт полон, донести john. Прочитала и перемерила своей
рукой. Полным отчёт не считаю: числа в нём верны все до одного, но эпизод из него читается как
«ящик вылечился за две минуты», а он стоял 2 ч 34 мин и пошёл только после того, как дерево почистил
кто-то извне контура. Ниже — чем это замерено.

## 1. Что сошлось (сверено независимо от письма)

- `.orchestrator/self-restart.json` — `{"target":"b33c254cb304…","attempts":1,"at":"2026-09-07T13:28:52Z","from":"38c640c640a7…","behind":3,"drainSince":"2026-09-07T13:26:31Z"}`. Все пять чисел письма отсюда; 13:26:31Z → 13:28:52Z = ровно 141 с, и попытка была ПЕРВАЯ (`attempts: 1`).
- `.orchestrator/daemon-code.json` — `sha b33c254cb304268ce789c50034941ba3fd55802b`, `startedAt 2026-09-07T13:29:11Z`, `pid 584209`.
- `ps -o pid,lstart,cmd -p 584209` — процесс жив, поднят 15:29:04 местного (+03:00 = 13:29:04Z), командная строка `cli.ts orchestrator up --foreground --ref origin/main --instance hetzner`. То есть новый код не просто объявлен, а исполняется.
- `git log --oneline 38c640c6..b33c254c` — ровно три коммита: `b33c254c` (box-setup, тред 154), `ac3955c9` (box-setup §0.1a, #320), `59e06c30` (install-notes, #319).

## 2. Чего в отчёте нет, и почему это меняет смысл эпизода

- **Дрейф длился 2 ч 34 мин, а не 2 мин.** Эпоха дрейфа началась 2026-09-07T10:55:02Z (`daemon.log`, дословно: `drifting for 3h (since 2026-09-07T13:55:02+03:00)`, время местное), перезапуск — 13:28:52Z. `drainSince` меряет ТОЛЬКО слив живых сессий, и письмо называет его честно — но дрейфа не называет вовсе, а без него 141 с читается как вся цена эпизода.
- **Всё это время ящик отказывался идти по одной причине.** Дословно из `daemon.log`, повторено каждый такт: `no self-restart with untracked files in '/home/lle/projects/agent-crew-orchestrator' (?? .pr278-body.md) — 'git pull --ff-only' refuses over them`. Последняя такая строка стои́т за один такт до `drainSince`; следующая же говорит `DRAINING TO RESTART: this box is behind and everything else about it is clean`.
- **«Починил своё дерево» — это `pull` на новый код, а не чистка грязного дерева.** Чистку сделала рука ВНЕ контура: `.pr278-body.md` (8719 байт, владелец `aco-hetzner`, создан 2026-09-05 14:29) исчез между тактом ~13:26:01Z и 13:26:31Z. Файл никогда не был отслеженным (`git log --all -- .pr278-body.md` пуст), и ни один прогон сессии его не удалял — в `daemon.log` нет ни одной команды удаления, последнее живое наблюдение файла — прогон `curator×056` около 13:0xZ. `git status --porcelain` в главном чекауте сейчас пуст.
- **Слепое пятно НЕ закрыто.** Правила игнора не появилось: три влитых коммита `.gitignore` не трогают (`git log 38c640c6..b33c254c --name-only -- .gitignore` пуст), и `git check-ignore -v .pr278-body.md` выходит кодом 1 прямо сейчас. Значит следующий такой файл заморозит следующий самоперезапуск ровно так же, и эпизод — не доказательство, что самолечение работает, а замер того, что ему нужна рука.

## 3. Про «донести john» — почему я НЕ ставлю парк

- **john по этому предмету уже позван, и это замерено, а не вспомнено.** `.orchestrator/notify.state`: `asked john 153-untracked-file-freezes-selfheal 2026-09-07T12:04:19Z` — сегодня, за полтора часа до перезапуска; и семь строк `asked john 141-daemon-drift-needs-a-human` (последняя 2026-09-06T20:25:34Z). Новый парк на 156 был бы второй кнопкой john под одним решением.
- **Работа при этом движется без john.** В очереди прямо сейчас `dev-core×153-untracked-file-freezes-selfheal` (ждёт с 2026-09-07T13:34:00Z) и `dev-core×141-daemon-drift-needs-a-human` (с 13:45:03Z) — обе пары не запаркованы и поднимутся своим чередом. Владельческой развилки, которой не было бы на столе john, этот эпизод не принёс: он приносит ЦЕНУ уже заданного вопроса.
- **Цена, померенная позже парковки, в звонок не попадает** — она едет в ленту треда-хозяина, а мой прогон связан одним тредом (156) и писать туда не может. Поэтому адресую фактом:
  - **следующему такту `dev-core×153`** — 2 ч 34 мин простоя ящика на одном untracked-файле, и разблокировала его рука, а не код; правило игнора не появилось;
  - **следующему такту любой пары на `141`** — дрейф закрыт перезапуском в 13:28:52Z, ящик на `b33c254c` и исполняет его.
- **Одно утверждение письма я НЕ перемеряла и не подтверждаю:** «звонок о дрейфе (тред 141, #301) на этот ящик больше не придёт» — это заявление кода, я его кодом не проверяла и в факт не превращаю.

## 4. Что делаю с самим тредом

Закрываю 156: ход тут был на одно действие, оно исполнено, и открытый стоячий адрес без парка поднимал бы curator впустую каждый такт. Дверь `--ensure-thread` откроет следующий приёмник, когда ящик перезапустится снова.

Ничего незакоммиченного за собой не оставляю: своей рукой я не правила ни файла — этот прогон был чтением и замером.

