---
from: curator
worker: claude-code
session: 6d7663e5-0106-4271-9d3a-13e200a03b15
raised: 2026-09-15T13:33:01Z
date: 2026-09-15T13:39:49Z
expects: none
waiting-on: curator
---

# Приёмка §7 пуста ТРЕТИЙ такт — и замерено ПОЧЕМУ: у частой ветви кончилось топливо (открытых PR ноль), а не «ещё не сработала». Норму читаю буквально — НАЗЫВАЮ РУКУ: заказываю круг СЛЕДУЮЩЕЙ меткой `review` в любом треде, без PR ради формальности. К john не звоню — проверка 1 нашла ответ стоячей нормой

Такт — приёмка по msg-035 §3/§4 с третьим исходом из msg-036 §4. Оба зонда снова пусты. Новое в этом
такте не «опять ноль», а три вещи: **чем** пустота объясняется числами; **почему** прежний вывод
«ждём» больше не проходит по `PROTOCOL.md`; и **чьей рукой** срабатывание заказано, раз ждать норма
не разрешает.

## 1. Зонды — пусты, и разрешение пустоты РАЗНОЕ у двух ветвей

Замер 13:34…13:36Z, моей рукой.

**Зонд #449** (`Claude PR Review`) — **пусто**, и новейший круг ТОТ ЖЕ, что в прошлом такте:
`34971304243` `12:50:59Z` (`completed/success`). За 43 минуты после кнопки (`13:01:38Z`) — ни одного
круга.

**Зонд #450** (`Notifier Watch`, исход не `skipped`) — **пусто**. Новейший прогон `34975669842`
`13:32:39Z`, и всё до одного `skipped`.

**`gh pr list --state open` → `[]`** — третий такт подряд. Домерживать нечего, и это же ОБЪЯСНЯЕТ
первый ноль: круг поднимается событием навешивания метки на ОТКРЫТЫЙ PR, открытых PR нет, значит
зонд #449 пуст **ПО ПОСТРОЕНИЮ**, а не потому, что ветвь не берётся. Пустота говорит о репозитории,
а не о диффе. Это не то же самое, что «не сработало», и прошлые два такта я называла это слабее, чем
следовало.

## 2. Строки приёмки НЕ протухли — сверено против уехавшего `main`

`origin/main` = `c9f3b9cab`, с прошлого такта не двигался; `git diff --name-only c9f3b9cab
origin/main -- .github/workflows packages/agent-protocol/scripts` — **пусто**. Дословные строки
msg-036 §3 стоят, зонды годны без правки, команды §3/§4 msg-035 остаются в силе.

## 3. Очередь: тред НЕ одинок, и это меняет цену пустого подъёма

`cli mail --role curator` → четыре треда ждут curator: `180-selfheal-leaves-the-workspaces-behind`,
`029-opencode-agent-kind`, `200` (этот), `210-self-restart-waited-understates-the-standstill`.
Приоритет `low`, поставленный в 13:31:07Z, подъёма в 13:33Z не остановил — потолок пар на ящик
поднят до 5 (#453), так что `low` меняет ПОРЯДОК, а не факт подъёма при свободных слотах. Значит
пустой такт здесь не вытесняет работу соседей, но окно роли тратит. Третий подряд — это уже цена, а
не ожидание.

## 4. Почему «ждём» больше не проходит: обязанность 2 стоячей нормы

`PROTOCOL.md`, раздел «Новый путь исполнения принимается ПЕРВЫМ СРАБАТЫВАНИЕМ, а не мержем»
(строка 357), обязанность 2 — дословно: «**Срабатывание заказывает рука, а не ожидание.** Само оно
не приходит… Роль, поставившая работу, доводит до первого срабатывания **или называет в треде, чьей
рукой оно заказано**». И граница цены там же, решённая john: «лишний заказ срабатывания стоит одного
прогона, пропущенный — того простоя, из которого норма».

Моё прежнее основание (msg-035 §5, msg-036 §4) — «частая ветвь редкой не является, значит заказывать
не надо» — норму не отменяет: она говорит «заказывает рука», а не «заказывает рука у редкого
условия»; редкость объясняет, ПОЧЕМУ ожидание не работает, но разрешения ждать не выдаёт ни в одном
случае. А §1 показал, что и фактическая посылка рассыпалась: частая ветвь без открытых PR даёт ноль
кругов сколько угодно долго. **Отступаю от прежнего вывода и называю руку.**

## 5. Рука названа — и она НЕ покупает лишнего круга

**#449 (обе ветви).** Заказчик — **curator, следующей меткой `review`, которую я и так повешу на
первый же открытый PR ЛЮБОГО треда.** Круг отработает уже выкаченный код даром: воркфлоу едет с
`main`, дифф там с 13:01:38Z. PR ради формальности не открываю — это и есть разница между «заказать»
и «купить лишний круг», и норма требует первого, а не второго. Приёмка читается зондом §3 из ЛЮБОЙ
сессии (`gh run list` + `gh run view <id> --log | grep`), чужой ленты для неё не нужно — правило
остановки msg-035 §3 (три исхода) применяется дословно.

**Чтобы обязанность пережила сессию, она записана НЕ только здесь:** подъёмника у треда `200` нет, а
метку я буду вешать из ДРУГОГО треда, поэтому строка «частая ветвь пуста по построению, пока нет
открытых PR; заказывай её следующей меткой» уложена в память роли (она грузится в промпт каждой
сессии curator, в отличие от журнала). Это и есть механизм, которого треду не хватало два такта.

**#450.** Рука не годится ВОВСЕ, и это честный остаток: вход ветви — отказной прогон одного из
четырёх воркфлоу (`notifier-watch.yml:112–116`) ПЛЮС незавершённый прогон в окне. Изготовить отказ —
расход квоты раннера и краснота `main`, а `launch-params` роли не дано. Заказать нечем, ждать
материала нечем: последний отказной `Claude PR Review` — `34866710091`, `2026-09-14T16:08:20Z`, то
есть 21 ч 26 мин назад при темпе ~13/сутки 12–14.09.

## 6. Почему НЕ звоню john — проверка 1 дала ответ стоячей нормой

Перед парком на john обе проверки сделаны, обе грепом:

- **проверка 1 (стоячая норма):** `grep -n` по `PROTOCOL.md` → строка 357 и обязанность 2 отвечают
  на развилку целиком: не «спросить, ждать ли», а «назвать руку». Вариант «звонить» был бы просьбой
  разрешить то, что норма уже предписывает;
- **проверка 2 (не стоит ли предмет парком в своём треде):** `grep '^parked' .orchestrator/notify.state`
  → стоячие парки на john только у `029-opencode-agent-kind` и
  `211-parallelism-ceiling-config-says-three`; у `200` парка нет — он снят письмом `13:02:30Z`
  (`park-lifted: john`), а звонок по `200` был сегодня в `12:57:56Z`. Второй звонок из этого треда
  обязан нести ДРУГОЙ вопрос, а у меня его сейчас нет.

**Что станет звонком и когда — объявляю заранее, чтобы это не решалось настроением:** если к такту,
на котором приёмка #449 будет ЗАКРЫТА (частая ветвь прочитана), у #450 по-прежнему не будет ни
одного отказного прогона-входа, — развилка едет к john первой строкой: **принять #450 на измеренной
половине (юнит + чтение кода) либо сознательно оплатить изготовление события**. Раньше этого звонка
не будет: пока #449 не закрыт, вопрос неполон.

## 7. Чего НЕ сделала, явно

- **тред НЕ закрыт** — приёмки нет ни у одной половины (норма `202`);
- **PR ради срабатывания не открывала** — §5 объясняет, чем заказ отличается от покупки круга;
- **john не позван** — §6, обе проверки;
- **парка нет** — обе машинные формы (`pr:<n>`, `run:<n>`) требуют НОМЕРА PR, а его не существует;
  парк на john был бы вторым звонком под тот же предмет. Это тот самый случай «в полёте не осталось
  ничего»: ход у себя без парка;
- **приоритет оставляю `low`** — §3: он не отменяет подъёма, но и не вытесняет соседей.

**Журнал** — записан командой в ветку почты, файл треда: пустота частой ветви разрешается живостью
ГЕНЕРАТОРА (открытые PR), а не редкостью условия; обязанность «назвать руку» у ветви, которую
заказывает соседний тред, живёт в памяти роли, а не в ленте ждущего треда. В дифф не положен.

Ход остаётся у роли. `waiting-on → curator`.

