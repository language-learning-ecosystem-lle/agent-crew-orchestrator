---
from: curator
worker: claude-code
session: 26b1252d-7392-4135-8f84-81693b4e1f2d
raised: 2026-09-15T13:51:33Z
date: 2026-09-15T13:58:43Z
expects: none
waiting-on: curator
parked-on: run:454
---

# Топливо ЗАГОРЕЛОСЬ: метка на #454 легла в `13:54:57Z` рукой dev-core, круг `34978119552` бежит на голове — приёмка #449 впервые имеет материал со сроком. Первое действие такта, объявленное прошлым письмом, ОТМЕНЕНО замером: метка была не моя рука. Паркуюсь `run:454`, и лифт у парка — ПОТОЛОК, прочитанный кодом, а не вердикт

Прошлый такт объявил: «первое действие следующего такта — проверка #454: зелёный ли `checks`,
сошёлся ли блоб, висит ли уже метка; если метки нет и `checks` зелёный — повесить». Проверка
сделана целиком, и она не дала повесить метку — не потому, что условия не сошлись, а потому, что
рука оказалась чужой. Это разобрано в §2, до того как рука потянулась.

## 1. Замер такта — 13:51…13:56Z

- **`checks` #454 — ЗЕЛЁНЫЙ**: прогон `34977054015`, голова `175d98f645c137954dbe2a7dd9139d1347ee04d4`,
  `completed/success`, `updatedAt 13:53:29Z`;
- **блоб merge-ref сошёлся**: `origin/main:.github/workflows/claude-review.yml` и
  `origin/pr-454-merge:…` — оба `cbc10cc7eb5df305887fc140089d96edba76b858` при `origin/main`
  = `c9f3b9cab1778d484d6e71cfad760a581ab97735`. Самопропуска не будет;
- **метка `review` НА МЕСТЕ** — появилась в `13:54:57Z`, пока я мерила блоб. Не моей рукой;
- **круг `34978119552` бежит** — `Claude PR Review`, `event=pull_request`, `createdAt 13:54:57Z`,
  ветка `dev-core/212-standstill-letter-cures-the-wrong-cause`, `status=in_progress`;
- **зонд #449** (круги `Claude PR Review` после `13:01:38Z`): на момент `13:54:06Z` — пусто, новейший
  всё тот же `34971304243` `12:50:59Z`. **Через 51 секунду пустота кончилась**: круг выше и есть
  первый круг на новом коде. Читать его нечем, пока он не завершён, — §4;
- **зонд #450** (`Notifier Watch`, исход не `skipped`): **пусто**, пятый раз. Новейшие
  `34978016009` `13:54:01Z`, `34977772098`, `34977727142` — все `completed/skipped`.

## 2. Метка была НЕ МОЯ рука — измерено тремя источниками, а не решено вкусом

Прошлое письмо оставило себе обязанность «повесить, если к моему такту никто не повесил». Перед
рукой я спросила, кому принадлежит шаг, и получила три «да» подряд:

1. `cli mail --role dev-core` → в его входе стоят `211` и **`212`**; `cli mail --role curator` →
   `029`, `200`, `210`. Тред PR ждёт исполнителя, не меня;
2. **моя же постановка** (тред `212`, msg-001 §7) перечисляет маршрут дословно: «зелёный `checks` на
   голове → сверка блоба `claude-review.yml` с `origin/main` в merge-ref → метка `review` → вердикт →
   **кнопка моя**». Кнопка — моя; метка — шаг ИСПОЛНИТЕЛЯ;
3. **исполнитель объявил её своим следующим действием** (msg-002, последний абзац): «Жду `checks` по
   #454… **паркуюсь на `run:454`**. По зелёному — сверка блоба…, метка `review`, вердикт; кнопка
   твоя».

Третий пункт несущий: парк `run:454` под `checks` снимается полем `run-outcome: 454` примерно через
минуту после зелени, то есть **подъём автора ПОД МЕТКУ был заказан механизмом**, а не надеждой.
Пустой `labels: []` в `13:51Z` значил «ещё не наступило», а не «никто не сделает», — и поле это
подтвердило через три минуты.

**Чем это отменяет прошлое письмо:** не передумыванием, а тем, что «повешу, если никто не повесил»
проверяется ДВУМЯ замерами (вход автора + его парк), а не одним взглядом на пустой список меток.
Рука была бы перехватом чужого шага маршрута — и при такте, связанном ОДНИМ тредом. Правило уложено
в память роли и в журнал: вешать метку я буду из других тредов, и там оно и понадобится.

**Что из этого следует для §5 msg-037 («рука названа — моя следующая метка»).** Заказ исполнен, но
НЕ моей рукой: круг на новом коде поднят соседним тредом, ровно как и допускал msg-038 §2. Норма
`PROTOCOL.md` «срабатывание заказывает рука» закрыта — рука названа и сработала, а чья именно, норма
не спрашивает.

## 3. Парк — `run:454`, и его лифт я НЕ выдумываю: прочитан кодом

Три такта подряд парка не было. Сегодня он законен, и вот чем это отличается от msg-038 §4, где
номер PR уже существовал, а парк был неверен:

- тогда следующий шаг был **моей рукой** (метка) — парк, ждущий себя, дверь зовёт «запертым
  изнутри». Сейчас следующий шаг — **вердикт чужого круга**, моей руки в нём нет;
- тогда на голове не было живого прогона нужного типа. Сейчас круг `34978119552` бежит — дверь
  спрашивает у `gh` именно живой прогон и на голове без него отказала бы письму ЦЕЛИКОМ. Между
  зелёным `checks` (`13:53:29Z`) и поднятым кругом (`13:54:57Z`) этот промежуток не парковался
  ничем: **зелёный `checks` окно парка ЗАКРЫВАЕТ, а не открывает**.

**Но лифта у этого парка не будет, и это замер, а не опасение.** `standingParkOf`
(`packages/agent-protocol/src/thread/thread.ts:723–783`) идёт по `thread.messages` — **по ЭТОМУ
треду**, — и `roundsEnded` набирается из шапок этого же треда (`verdict:`/`pr: N`, `run-outcome: N`).
Комментарий там же называет асимметрию прямо: merge судится уровнем выше, `parkingOf`, против всей
почты, «the notifier writes into the PR's own thread, which is not this one». Письмо вердикта по #454
уедет в ленту `212`. Значит **мой парк снимет 30-минутный потолок `staleRunParks`**, то есть подъём
около `14:29Z`.

**И именно поэтому парк дешевле непаркованного хода, а не дороже.** Круг кончится ~`14:01Z`.
Непаркованный ход поднял бы пару в `13:58…14:02` (замеренный разброс 43 с … 5 мин) — на ЖИВОЙ круг,
пятым пустым подъёмом подряд, — после чего тот такт всё равно запарковался бы и всё равно ждал
потолок. Парк сейчас = **один подъём с материалом вместо двух, из которых первый пустой**. Цена
названа заранее: до ~`14:29Z` тред `200` заморожен и никого не зовёт; ничего, кроме исхода круга, в
нём в эти минуты не лежит.

## 4. Что делает следующий такт — командой и правилом остановки, без перечитывания треда

Круг `34978119552` к тому времени завершён. Приёмка #449 читается зондом msg-035 §3 дословно:

```bash
gh run view 34978119552 --log | grep -a 'checks на голове PR\|прогон checks:\|почему НЕТ:'
```

**Правило остановки — три исхода (msg-035 §3, строки `claude-review.yml` 213–217):**

- **приёмка частой ветви** — `checks на голове PR: ЗЕЛЁНЫЙ` при пустом `почему НЕТ:`. Ожидаемый
  исход: `checks` на `175d98f64` был зелёным ДО метки;
- **приёмка редкой ветви** — `почему НЕТ: в прогоне <id> джоба 'checks' ещё НЕ ЗАВЕРШЕНА (status
  '<X>') — исхода у неё пока нет`;
- **провал** — любое из: `НЕТ` при зелёном `checks` на той же голове; пустая причина при `НЕТ`;
  прежняя скобка «(или её там нет)»; `в прогоне <id> джобы 'checks' нет вовсе` там, где джоба есть.

**Если круг сгорел по лимиту** (длительность 45…56 с, вердикта нет) — приёмка не состоялась, зонд
переезжает на следующую метку; это не провал ветви, и путать их нельзя.

**Сразу после зонда — дешёвая половина хвоста:** #454 к тому времени может нести вердикт, и кнопка
по нему **моя** (гард 3 закрыт классом «полевой измеренный дефект», три условия названы в постановке
`212` msg-001 §6, условие 2 исполнитель закрыл в msg-002; условие 3 закроет вердикт ревьюера). Но
**мёржится он в треде `212`, а не здесь** — этот тред про приёмку #449/#450.

## 5. Дешёвая половина закрытия хвоста — исполнена, пуста

`gh pr list --state open` → один #454.
`merge-gate --ref origin/main --pr 454 --review-workflow 'Claude PR Review'` → **REFUSED**:
`STOP` гард 1 (нет `approve` на `175d98f`), **`ok` гард 2** (`checks=SUCCESS` на той же голове),
`ok` гард 4 (5 путей, ни один не док власти; дверь назвала 8 доков власти данными),
`mergeable=MERGEABLE (CLEAN)`, гарды 3 и 5 — обязательства. Домерживать нечего.

## 6. Почему НЕ звоню john — порог из msg-037 §6 не наступил, и он наступит СКОРО

Порог объявлен дословно: развилка по #450 («принять на измеренной половине — юнит + чтение кода —
либо сознательно оплатить изготовление события») едет к john на том такте, где приёмка #449 будет
ЗАКРЫТА, а у #450 по-прежнему не будет входа. #449 не закрыт — круг ещё бежит. **Но материал у него
теперь есть, и срок — минуты, а не сутки**, так что звонок по #450 с высокой вероятностью и есть
работа такта после следующего. Формулировка вопроса уже готова и лежит в §5 msg-037 и §3 msg-038
(фон отказов `Claude PR Review` — 3 из 111, ~0,6 в сутки, и вход ветви требует совпадения отказа с
незавершённым прогоном в ту же минуту).

Обе проверки перед звонком стоят как стояли: `.orchestrator/notify.state` даёт стоячие парки на john
только у `029-opencode-agent-kind` и `211-parallelism-ceiling-config-says-three` — ни один не про
этот предмет; последний звонок из `200` был сегодня `12:57:56Z`, про кнопки, и он отвечен.

## 7. Чего НЕ сделала, явно

- **тред НЕ закрыт** — приёмки нет ни у одной половины (норма `202`): у #449 круг бежит, у #450
  входа нет вовсе;
- **метку не вешала** — §2, рука чужая; и уже висела к моменту решения;
- **круга не ждала** — чужой прогон, который пишет в свой тред; вместо ожидания парк и передача хода;
- **#454 не мёржила** — гард 1 не закрыт (вердикта нет), и его тред `212`, не этот;
- **john не позван** — §6, порог не наступил;
- **приоритет оставляю `low`** — подъёма он не отменяет (#453), а тред эти полчаса стоит парком.

**Журнал** — записан командой в ветку почты, файл треда: обязанность «повешу метку, если никто не
повесил» проверяется входом автора и его парком; `standingParkOf` ходит по ЭТОМУ треду, значит парк
на чужой PR снимает потолок, и это надо называть сроком заранее; зелёный `checks` окно парка
закрывает. В дифф не положен.

Ход остаётся у роли, тред стоит парком `run:454` до ~`14:29Z`. `waiting-on → curator`.

