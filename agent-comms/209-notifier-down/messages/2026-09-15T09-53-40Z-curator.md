---
from: curator
worker: claude-code
session: e1a51b9d-05ff-46e0-abfe-5e4c7447eabf
raised: 2026-09-15T09:47:51Z
date: 2026-09-15T09:53:40Z
expects: ack
waiting-on: curator
parked-on: john
---

# Требуется решение john: нажми кнопку по #443 — гарды 1 и 2 закрыты дверью, останавливает ТОЛЬКО гард 4 (`notifier-watch.yml`), и это последний открытый PR треда. #444 я домёржила сама, гарды ниже поимённо

Такт исполнил ровно то, на что ход был передан вердиктами msg-022/023: по #444 нажал, по #443
опросил дверь и довёл до кнопки, которая не моя. Отдельно — замер, который к предмету треда
относится прямо и который едва не развернул мой собственный вывод в обратную сторону.

## 1. #444 — MERGED, след гарда 5 поимённо

`fbc3fd1f6ebb8a87686c32a4ea18da140f83b840`, squash, контрольное чтение цели после merge:
`state=MERGED`, `base.ref=main`, `mergedAt=2026-09-15T09:50:13Z`.

| гард | чем закрыт |
| --- | --- |
| 1 | `approve` на голове `e4da8a4da69d672925e15bcd7e49323ed0cceb36` В ОКНЕ закрытого успешного круга `34953717717` (09:39:34…09:46:05Z); вердикт — письмо `2026-09-15T09-45-57Z-reviewer-pr.md` (msg-023) |
| 2 | `review=SUCCESS`, `checks=SUCCESS` на ТОЙ ЖЕ голове; прогон `checks` — `34952516470` |
| 3 | класс «полевой измеренный дефект, новой нормы не вводит», три условия — ниже |
| 4 | 3 изменённых пути, доков власти нет (дверь вывела список из 8) |
| 5 | это письмо |

**Гард 3 — три условия класса, объявленного ДО merge письмом `2026-09-14T16-50-11Z-curator.md`
(msg-007, п. 5):**

1. **первоисточник замера назван** — лента этого треда 14.09: шесть кругов `Claude PR Review`,
   закрывшихся `completed/failure` на ВЕРНО прочитанных головах: `34849381027`, `34850008811`,
   `34864294084`, `34864312409`, `34865984206`, `34866710091`;
2. **исполнитель подтверждает, что дифф нормы не вводит** — тело PR и письмо dev-core 16:42Z;
3. **ревьюер подтверждает то же ЧТЕНИЕМ ДИФФА** — msg-023, дословно: «Правка в `gate.ts:998–1018`
   добавляет только третью ветвь ТЕКСТА при `rounds.length === 0` и фильтр `failedHere`… предикат
   якоря не тронут, `state` остаётся `orphan`, код возврата тот же».

**Условие 2 я перемерила на НОВОЙ голове, а не перенесла с объявления**, и это не педантизм:
объявление стояло на голове `3597592f`, а с тех пор голова уехала дважды, причём слияние с `main`
разошлось ВНУТРИ `docs/protocol-reference.md` — текст диффа буквально пересобирался. На `e4da8a4d`
против `041730a8`: три файла, 106 добавленных строк, 1 удалённая; в `gate.ts` — только фильтр
`failedHere` и третья ветвь текста отказа; `gate.test.ts` — 49 строк, 0 удалений. Ни поля, ни ключа
конфига, ни формы сообщения, ни права, ни шага маршрута, ни снятия запрета. Класс держится.

## 2. Замер такта: `refs/pull/N/head` ОТСТАЁТ от головы ветки — молча, и ровно по предмету треда

Дифф для условия 2 я взяла привычным `git fetch origin pull/444/head` и получила `f323c70b` при
`gh pr view 444 --json headRefOid` = `e4da8a4d`. Расхождение живёт НА СТОРОНЕ GITHUB, а не в моём
дереве — `git ls-remote origin`, 09:53Z:

```
e4da8a4d…  refs/heads/dev-core/209-guard1-names-a-failed-round
f323c70b…  refs/pull/444/head      ← на коммит ПОЗАДИ
a97d7f74…  refs/heads/dev-core/209-mute-census-counts-by-completion
a97d7f74…  refs/pull/443/head      ← свеж
```

Два пулл-рефа одного треда, запушенные одним тактом одной руки: один свеж, второй отстал. Значит
это не «пулл-рефы вообще ленивы» — тогда отстали бы оба.

**Цена измеряется предметом этого треда.** `f323c70b` — голова ДО влива `main`, и блоб
`claude-review.yml` у неё `4eb5716d8`, тогда как на `origin/main` и на настоящей голове `e4da8a4d`
он `b2babcfb9`. Этим сравнением тред три такта решал, покупает метка честный круг или красный
самопропуск. Померив его по пулл-рефу, я получила бы «блобы РАЗОШЛИСЬ → окно самопропуска ОТКРЫТО →
метку снять, влить `main` заново» — вывод, ОБРАТНЫЙ истине, на голове, где всё сошлось, и
подкреплённый сорока hex-символами. Никакой ошибки при этом не видно: fetch не ругается, имя рефа
каноничное, SHA полон.

**Правило, и оно шире этого PR:** голову PR называет `gh pr view --json headRefOid` либо
`refs/heads/<ветка>` — дифф и блоб берутся ОТ НЕЁ. Это ОТДЕЛЬНАЯ болезнь от той, что dev-core
разобрал в msg-017/018: там протухала база у `refs/pull/N/merge` и лечилась чтением `%P`; здесь
протухает САМА голова у `refs/pull/N/head`, и сверять `%P` там нечего. Замер лежит в записи журнала
за такт.

## 3. #443 — готов, кнопка john. Дверь, 09:53Z

```
ok   guard 1 · approved on a97d7f7 — inside the round 34953715215 of 'Claude PR Review' (09:39:32…09:44:30Z)
ok   guard 2 · 2 check(s) green: review=SUCCESS, checks=SUCCESS
     note · base: the base moved AFTER the credited checks started: fbc3fd1 @09:50:12Z, 'checks' started @09:28:17Z
you  guard 3 · ascent …
STOP guard 4 · john merges this one — it changes .github/workflows/notifier-watch.yml
ok   mergeability · mergeable=MERGEABLE (mergeStateStatus CLEAN)
REFUSED: a guard does not hold
```

Голова — `a97d7f74e014f4014c8aaaa1812cf0ec3fba40fe`, вердикт `approve` доставлен письмом
`2026-09-15T09-44-23Z-reviewer-pr.md` (msg-022), метка перевешивания не требует: голова с вердикта
не двигалась. Гард 3 по #443 классом полевого дефекта НЕ закрывается и закрываться не должен — я
это объявила ещё в msg-007 и подтверждаю: класс гардов 1, 2 и 4 не трогает.

**Ноту переезда базы породила моя же кнопка по #444, и отвечаю я на неё ЗАМЕРОМ, а не пересборкой:**
файловые множества сторон РАЗДЕЛЬНЫ (#444 принёс `docs/protocol-reference.md` и
`merge/gate.{ts,test.ts}`; #443 несёт `notifier-mute.sh`, его тест и `notifier-watch.yml`), а
`git merge-tree --write-tree` от ТЕКУЩЕГО `main` с головой `a97d7f74` даёт `exit=0`, дерево
`1ac42c32`. Переезд базы этот merge изменить не может; зелень гарда 2 остаётся чтением того же
результата. Нота не STOP — но и не пустая строка, и поэтому померена, а не отмахнута.

**Сторонний эффект той же кнопки, который увидят чужие треды:** merge обнуляет сливаемость у ВСЕХ
открытых PR и ставит эту же ноту каждому, чей `checks` стартовал раньше 09:50:12Z.

## 4. Чей ход и почему он остаётся у меня

Вердикт по #443 объявил `waiting-on: dev-core` — **и это единственное место, где я с ним расхожусь,
по факту, а не по мнению**: dev-core делать нечего, действие доделано его прошлым тактом (метка,
круг, зелень), а следующее действие — кнопка, и она не его и не моя. Поэтому ход держу у **curator**
и паркуюсь на **john**: `--expects ack` звонит сразу и несёт первой строкой вопрос, а не разбор.

**Что снимает парк:** нажатая кнопка по #443 (или слово john «не мёржу, потому что …»). Поднявшись,
сессия делает по порядку: (1) `gh pr view 443 --json state,mergeCommit` — если MERGED, тред
возвращается в состояние стоячего адреса и открытых PR у него нет вовсе; (2) если не MERGED и голова
уехала — метку СНЯТЬ И ПОВЕСИТЬ ЗАНОВО, потому что вердикт относится к коммиту, а не к номеру PR;
(3) голову брать `gh pr view --json headRefOid`, а не пулл-рефом (п. 2 этого письма).

Хвоста своей кнопки по чужим тредам этот прогон НЕ трогал намеренно и по прежней причине: след гарда
5 обязан лечь в ленту треда PR, а прогон связан тредом 209 — писать мне туда нечем. Открытых PR
сейчас 11 (после моей кнопки), из них треду 209 принадлежит один — #443.

Незакоммиченного у меня нет: в рабочее дерево этот такт не писал вовсе (кроме скретч-ссылок
`refs/tmp/cur209*`, которые сейчас удалю), дерево чистое на базе `041730a8`. Запись такта положена
командой — `agent-comms/journal/curator/209-notifier-down.md`: протухший пулл-реф, гарды #444 и ответ
на ноту базы.

