# docs/install-notes.md: надстройка установки — два операционных следствия и ссылка из PROTOCOL.md

participants: curator, john · status: closed

## msg-001 · from: curator · 2026-08-18 · expects: none

постановка: **завести `docs/install-notes.md` — надстройку установки этого репозитория — и увести туда два операционных следствия, которые роль обязана знать ДО передачи хода**; из `PROTOCOL.md` на неё ставится одна строка-ссылка.

Основание — слово john, доставленное в тред 008 сообщением `agent-comms/008-readme-frozen-workflows/messages/2026-08-18T18-52-05Z-curator.md`, п. 2: дом следствий — `docs/install-notes.md` (принята рекомендация из `.../2026-08-18T18-31-42Z-curator.md` §2: generic-норма не трогается, дом растущий, `PROTOCOL.md` получает ссылку вместо оговорки внутри нормы; паттерн — зеркало LLE-оверлеев `docs/*-lle.md`). Тред 008 закрыт приёмкой влитого #13.

Почему отдельный тред: 008 — про текст README, здесь — новый файл плюс правка `PROTOCOL.md`, то есть другой дифф и другая приёмка.

## 1. Что сделать

**(1) Новый файл `docs/install-notes.md`** — что в контуре ЭТОЙ установки включено, что выключено, и какое следствие это даёт роли перед ходом. Не пересказ README, а операционка: README отвечает «какой воркфлоу жив», install-notes — «что из этого следует для формы твоего хода». Два следствия, оба названы john:

- **`ci-outcome.yml` заморожен (`on: workflow_dispatch`) → исход CI событием в тред НЕ приезжает.** Роль снимает зелёный СВОЕЙ рукой (`gh run list --commit <head>`) ДО передачи хода; «жду прогон» здесь означает «жду того, чего не будет». Норма `PROTOCOL.md` R-134 («вердикт ревьюера и исход CI приходят в тред СОБЫТИЕМ и будят пару штатно») в этой половине здесь не выполняется — оговорка живёт в install-notes, норма не переписывается;
- **`merge-notify.yml` жив (`pull_request: types: [closed]`) → парковка `parked-on: pr:<n>` законна** и снимается штатно. Единственная причина законности — живой триггер, а не обычай: заморозят обратно — парковка станет вечной, и эта строка правится тем же коммитом, что и триггер.

Файл обязан нести **дату и способ сверки** («прочитаны `on:` пяти воркфлоу на голове ветки, ГГГГ-ММ-ДД ЧЧ:ММZ»), чтобы следующая сверка стоила одно чтение, а не раскопки. Заморозку ревьюера (`claude-review.yml`) назвать там же как факт установки — на ней стоит выключенное право merge у ролей (карточка `docs/roles/curator.md`, §«Merge»), и это третье следствие того же класса, бесплатное здесь.

**(2) `PROTOCOL.md` — одна строка-ссылка возле R-134** (норма про «ожидание чужого прогона — не действие», строка 134 на `origin/main` 2026-08-18): «какие из этих событий включены в конкретной установке, говорит надстройка установки; в этом репозитории — `docs/install-notes.md`». Требование к формулировке: **норма не меняется** — вставка называет, ГДЕ искать отклонение, а не вводит исключение. Дифф в `PROTOCOL.md` — только эта вставка, ни строкой больше.

**(3) `README.md` — одна ссылочная строка** в §«Почтовые воркфлоу: два живых, три замороженных» на `docs/install-notes.md`. Причина не косметическая: оба следствия УЖЕ лежат в этом § (#13), и без ссылки заводится второй дом той же мысли — расхождение двух текстов, которое некому ловить. Дублирование сократить в пользу install-notes README при этом НЕ обязано: перенос текста из README — отдельное решение, здесь только ссылка.

## 2. Границы

- **`.github/workflows/**` не трогать вовсе** — предмет john, и трогать нечего: правится текст про режим, а не режим. Разморозка `ci-outcome` или ревьюера в постановку НЕ входит (это деньги и права, отдельное решение);
- **`docs/protocol-reference.md` не трогать** — измерено в 008 (`.../2026-08-18T18-31-42Z-curator.md` §2): шапка файла (строки 24–29) выселяет установочные факты в надстройку, ради чего эта постановка и существует;
- `agent-protocol.json` и карточки ролей — не трогать (доки власти); см. п. 3, там вопрос, а не правка.

## 3. Зона исполнителя — измерено, и в измерении находка

Исполнитель — curator. `zones check --ref origin/main --role curator` 2026-08-18 18:56Z отвечает «inside its zone» на `docs/install-notes.md`, `PROTOCOL.md` и `docs/protocol-reference.md` — то есть запрета нет.

**Но тем же прогоном дверь ответила «inside its zone» на `biome.json` и на `.github/workflows/checks.yml`**, а отказала только на `packages/...` — по списку `forbidden`. При `zones.writes: ["docs/roles", "PROTOCOL.md", "REVIEWER.md", "docs/protocol-reference.md"]` это значит: **«внутри зоны» здесь читается как «не запрещено», а не «перечислено в `writes`»**. Причину я не называю — код пакета не читала, и гипотеза («`writes` не сужает») — заявление, подлежащее проверке, а не диагноз.

Два следствия, оба к john и оба НЕ блокируют эту постановку:
- **вопрос john:** дописать ли `docs/install-notes.md` в `zones.writes` роли curator (`agent-protocol.json` — док власти, моей рукой не правится). Исполнять постановку можно и без этого: запрета дверь не ставит, а слово на сам файл дано;
- **отдельный тред (не этот):** поведение `zones check` при непустом `writes` — если сужения нет, то либо это замысел и он не описан, либо дыра двери; предмет `dev-core`, ставится постановкой после проверки кода. Здесь — только зафиксировано.

## 4. Проверяемость

- **юнит/интеграционного теста нет** — предмет правки текст; соответствие текста триггерам стендом не берётся, берётся чтением. Названо, не пропущено;
- **прямой факт**: чтение `on:` каждого из пяти воркфлоу на голове ветки PR и сверка с каждым утверждением `install-notes.md` — в доклад таблицей, как в 008;
- **ссылка из `PROTOCOL.md` проверяется существованием файла** по относительному пути (`ls docs/install-notes.md` с головы ветки), а не глазом: битая ссылка в норме хуже отсутствующей;
- **зелёный `checks` на голове PR снимается своей рукой** (`gh run list --commit <head>`) — ровно по той причине, которую этот же PR и записывает в файл;
- **приёмка — merge кнопкой john**: дифф трогает `PROTOCOL.md` (док власти), ревьюер заморожен, гарду 1 закрываться нечем. Тред закрывается по факту влитого PR — чтением базы (`gh pr view --json state,baseRefName,mergedAt,mergeCommit` + `git log origin/main`), а не цветом джобы.

## 5. Круг

ветка → правка → зелёный `checks` на голове (снять рукой) → PR с `thread: 009-install-notes` и `role: curator` в описании → сообщение в тред с номером PR, головой и таблицей проверок → кнопка john. Push пакет не закрывает. Парковка ожидания кнопки — `--parked-on pr:<n>`: здесь она законна, `merge-notify` жив (это и есть второе следствие, применённое к самому себе).

waiting-on → curator.

## msg-002 · from: curator · 2026-08-18 · expects: ack

**Требуется решение john: (1) кнопка merge на PR #14 — `docs/install-notes.md` заведён, ссылки из `PROTOCOL.md` и `README.md` поставлены, `checks` зелёный на голове `30d3285`; (2) дописать ли `docs/install-notes.md` в `zones.writes` роли curator (`agent-protocol.json` — док власти, моей рукой не правится; п. 3 постановки).**

Постановка выполнена целиком. PR **#14**, голова **`30d32853b7d58e8b2f028db9e357071c4cb2b301`**, база `main`, дифф — 3 файла, +101/−1.

## 1. Что сделано

- **`docs/install-notes.md`** (новый, 99 строк): что в контуре ЭТОЙ установки включено, что выключено и что из этого следует для формы хода. Три следствия — два названные john и третье того же класса: `ci-outcome` заморожен → исход CI событием не приезжает (и парковка `run:<N>` здесь смысла не имеет); `merge-notify` жив → парковка `pr:<n>` законна, и причина законности — живой триггер, а не обычай; `claude-review` заморожен → гарду 1 `merge-gate` закрываться нечем, право merge у ролей выключено, кнопка у john. Файл несёт дату и способ сверки (блоки `on:` шести воркфлоу на голове `c983abb`, 2026-08-18 18:59Z) таблицей, чтобы следующая сверка стоила одно чтение;
- **`PROTOCOL.md`**: ровно +1 строка возле нормы «Ожидание чужого прогона — не действие» (строка 134). Норма не тронута ни байтом — вставка называет, ГДЕ искать отклонение установки, и говорит это прямо («норма этим не меняется»);
- **`README.md`**: одна ссылочная строка в конце §«Почтовые воркфлоу», чтобы у следствий не завелось двух домов. Текст из README в install-notes НЕ переносился — это отдельное решение, в постановку не входило.

`.github/workflows/**` не тронуты вовсе. `docs/protocol-reference.md` не тронут. `agent-protocol.json` и карточки ролей не тронуты.

## 2. Проверки — фактами, а не докладом

| что проверено | как | результат |
| --- | --- | --- |
| `on:` шести воркфлоу против таблицы в файле | чтение `.github/workflows/*.yml` на голове ветки | сошлось построчно: `checks` (`push: main` + `pull_request`), `comms-derived` (`push: comms` + `paths-ignore: agent-comms/_instances/**` + dispatch), `merge-notify` (`pull_request: types: [closed]` + dispatch), `ci-outcome` / `notifier-watch` / `claude-review` — только `workflow_dispatch` |
| ссылка из `PROTOCOL.md` не битая | `git ls-tree origin/docs/009-install-notes docs/install-notes.md` | blob `597a0e4` — файл есть по относительному пути |
| дифф `PROTOCOL.md` — только вставка | `git diff --stat` | `PROTOCOL.md | 1 +` (ни строкой больше) |
| зона исполнителя | `zones check --ref origin/main --role curator --base origin/main` | «3 path(s) of 'curator' are inside its zone» |
| зелёный на голове — своей рукой | `gh pr checks 14 --watch` + `gh run view 32174622525 --json headSha,conclusion,event` | `success`, `headSha` = `30d3285…`, event `pull_request`, 3m18s |

Юнит/интеграционного теста нет — предмет правки текст, соответствие тексту берётся чтением, а не стендом (названо в постановке, не пропущено).

## 3. Находка, оплаченная этим же прогоном: `gh run list --commit` на сокращённом sha лжёт молча

Постановка велела снимать зелёный `gh run list --commit <head>` — и на первой голове (`b28df60`) эта команда ответила **`[]` при зелёном прогоне на ровно этом коммите**. Не отказом — пустым списком, то есть «прогона нет» вместо «спрошено не то». Причина измерена тремя чтениями подряд: фильтру нужен ПОЛНЫЙ sha (`b28df60abc3ba…` возвращает прогон `32174235217`, `b28df60` — пусто).

Цена ошибки несимметрична ровно в ту сторону, которая опасна: пустой ответ неотличим от «ещё не запускался», и роль, послушавшая совет буквально, либо уйдёт ждать несуществующего события, либо решит, что зелёного нет. Поэтому находка ушла ВТОРЫМ коммитом в сам файл (голова `30d3285`), с полным sha, датой замера и двумя формами перепроверки (`gh pr checks <n>`, `gh run view <id> --json headSha,conclusion`). Это единственное отступление от буквы постановки, и оно в её же пользу.

## 4. Отступление от буквы постановки: парковка не `pr:14`, а звонок

Постановка велела парковать ожидание кнопки на `--parked-on pr:14`. Не сделано осознанно: `pr:`-парковка **никого не зовёт** — она законна там, где человек про PR уже знает и кнопка ожидаема. Про #14 john не знает ни от кого: единственный канал — это сообщение. Парковка, которая заморозила бы тред с невручённым вопросом, — тот самый тихий худший исход, которым карточка роли и предостерегает. Поэтому форма 008, отработавшая на #13: `expects: ack`, `parked-on: john`, `waiting-on → curator`, вопрос первой строкой. `merge-notify` при этом жив и merge приедет в почту штатно — законность `pr:`-формы это не отменяет, просто здесь она не тот инструмент.

## 5. Осталось для следующего хода (не блокирует #14)

- **вопрос (2) первой строки** — `zones.writes` для `docs/install-notes.md`. Дверь запрета не ставит (измерено выше), слово на сам файл дано, так что это уточнение прав, а не разблокировка;
- **два висящих указателя `PROTOCOL.md` на надстройку установки** — строка 128 («Номера PR и переезд репозитория») и строка 244 («Режим вахты — механика ожидания»): разделов с такими именами не существует ни здесь, ни где-либо в репозитории, обе ссылки писались на надстройку репозитория-родителя. Материал под них не выдуман — дыра НАЗВАНА отдельным разделом в конце `install-notes.md`. Закрывать — отдельным тредом, когда факты этой установки будут прочитаны;
- **поведение `zones check` при непустом `writes`** (п. 3 постановки): «внутри зоны» читается как «не запрещено» — предмет `dev-core`, отдельный тред, здесь только зафиксировано.

Тред 009 закрою по факту влитого PR — чтением базы (`gh pr view --json state,mergedAt` + `git log origin/main`), а не цветом джобы.

waiting-on → curator (ход у того, кто несёт вопрос), parked-on → john.

## msg-003 · from: curator · 2026-08-18 · expects: none

доставка решений john (сессия чата, ~19:53Z) — парковка msg `19-09-59Z` снята:

1. **#14 СМЁРЖЕН — сверено вызовом**: `merged_at=2026-08-18T19:53:49Z`, squash в `main`. `docs/install-notes.md` живёт, ссылки стоят; тред 009 готов к закрытию твоим порядком (чтением базы);
2. **Вопрос (2): ДА** — `docs/install-notes.md` дописывается в `zones.writes` роли curator (`agent-protocol.json`). Однострочный PR за тобой; merge — кнопка john (док власти).

Хвосты §5 (два висящих указателя PROTOCOL.md; поведение `zones check` при непустом `writes`) — отдельными тредами по твоему замеру, не блокируют.

waiting-on → curator.

## msg-004 · from: curator · 2026-08-18 · expects: ack

**Требуется решение john: кнопка merge на PR #15 — `docs/install-notes.md` дописан в `zones.writes` роли curator (`agent-protocol.json` — док власти), `checks` зелёный на голове `7621d4d`.**

Оба пункта решения из предыдущего сообщения отработаны.

## 1. Пункт (1): #14 — сверено чтением базы, а не докладом

`gh pr view 14 --json state,baseRefName,mergedAt,mergeCommit` → `MERGED`, база `main`, `mergedAt=2026-08-18T19:53:49Z`, merge-коммит `ac97bf4ca7cc51aaf9c6423063e137f3129e395a`. `git log origin/main -1` на свежем `fetch` даёт ровно `ac97bf4` — то есть слито в ту базу, куда целились, и это голова `main`. `docs/install-notes.md` живёт в дереве.

Тред при этом **НЕ закрываю**: из него же вышел PR #15 и стоит неслитым. Закрытие треда — приёмка, а приёмка при открытом собственном PR была бы приёмкой по косвенному признаку. 009 закрывается по факту влитого #15, тем же чтением базы.

## 2. Пункт (2): PR #15 — одна запись в `zones.writes`

PR **#15**, голова **`7621d4de8b1a93d5252384e9f7d57229c2d13b8d`**, база `main`. Дифф — один файл, `agent-protocol.json`: в `zones.writes` роли curator добавлено `"docs/install-notes.md"`. Больше в дифф не входит ничего: ни карточка роли, ни `PROTOCOL.md`, ни воркфлоу.

Описание PR несёт класс **Д-1** со ссылкой на сообщение-решение (`agent-comms/009-install-notes/messages/2026-08-18T19-54-06Z-curator.md`, п. 2) и на сообщение-вопрос (`.../2026-08-18T19-09-59Z-curator.md`) — ссылки именем файла, не ординалом.

| что проверено | как | результат |
| --- | --- | --- |
| конфиг читается строгой схемой ПОСЛЕ правки | `config check --ref docs/009-zones-install-notes` | `ok — protocol version 17, 3 roles, 1 instances (hetzner)` |
| дифф — одна запись и ничего больше | `git diff origin/main -- agent-protocol.json` | `writes` в столбец + `"docs/install-notes.md"` |
| формат файла | `npx biome check agent-protocol.json` | `Checked 1 file. No fixes applied.` |
| тесты пакетов не сломаны правкой конфига | `pnpm test` локально | 2037 passed (124 файла) + 7 passed |
| зелёный на голове — своей рукой | `gh run list --commit 7621d4de8b1a93d5252384e9f7d57229c2d13b8d --json headSha,conclusion,event` | `success`, event `pull_request`, run `32179836982` |
| #14 действительно в `main` | `gh pr view 14` + `git log origin/main -1` | `ac97bf4`, `mergedAt 19:53:49Z` |

## 3. Находка прогона: локальный `pnpm lint` НЕ равен раннеру, и молчит он в опасную сторону

Первая голова (`3d4b6d9`) дописала значение в ту же строку — и раннер отказал: форматтер biome переносит массив в столбец, когда строка перерастает ширину. **Локальный `pnpm lint` этой ошибки не показал**, зато показал 10 находок в `packages/**`, которых нет на раннере. Причина в выводе самой команды: на диске biome **2.5.8**, в lockfile — **2.5.4** (`The configuration schema version does not match the CLI version 2.5.8`), а раннер ставит `--frozen-lockfile`.

Цена несимметрична: локальный прогон даёт и ложное «чисто» на своём файле, и ложный шум на чужих — то есть роль, поверившая ему, либо толкнёт красное, либо пойдёт чинить не своё (`packages/**` — зона dev-core, у curator запрещена). Второй коммит (`7621d4d`) — ровно перенос массива в столбец, ни одного нового значения.

Это факт установки того же класса, что уже лежат в `docs/install-notes.md`, но дописывать туда его этим PR я не стала: дифф класса Д-1 обязан кодировать ТО решение и ничего больше. Отдельной строкой в §5.

## 4. Что не сделано и почему

- `merge-gate` на #15 не гонялся: ревьюер заморожен, гарду 1 закрываться нечем — дверь напечатает STOP по конструкции, и это её правильный ответ, а не сведение о PR. Кнопка у john по тому же порядку, что и на #14;
- парковка снова `parked-on: john`, а не `pr:15` — по той же причине, что на #14: про #15 john не знает ни от кого, кроме этого сообщения, а `pr:`-парковка никого не зовёт.

## 5. Хвосты (не блокируют #15)

- **расхождение локального biome с lockfile** (2.5.8 против 2.5.4) — кандидат в `docs/install-notes.md` отдельным следствием и/или задача dev-core на пин версии; отдельный тред;
- **два висящих указателя `PROTOCOL.md`** на несуществующие разделы надстройки (строки 128 и 244) — названы дырой в конце `install-notes.md`, закрывать отдельным тредом;
- **поведение `zones check` при непустом `writes`**: сегодня измерено ещё раз — на дифф с `agent-protocol.json` дверь ответила «1 path(s) of 'curator' are inside its zone», хотя `agent-protocol.json` в `writes` не перечислен. То есть «внутри зоны» = «не запрещено», сужения нет; предмет dev-core, отдельный тред.

waiting-on → curator (ход у того, кто несёт вопрос), parked-on → john.

## msg-005 · from: curator · 2026-08-18 · expects: none

доставка (сессия чата, 20:25Z): **#15 СМЁРЖЕН — сверено вызовом**: `merged_at=2026-08-18T20:25:32Z`, squash в `main`. `docs/install-notes.md` в `zones.writes` curator; тред 009 закрывается твоим порядком целиком — оба PR (14, 15) в базе.

waiting-on → curator.

## msg-006 · from: curator · 2026-08-18 · expects: none

приёмка треда: **#15 влит в `main` — сверено чтением базы, а не докладом**; хвосты §5 разведены по тредам 010 и 011, а третий из них СНЯТ измерением (диагноз в `.../2026-08-18T20-09-39Z-curator.md` §3 был неверен — ниже, п. 3). Тред закрываю.

## 1. Приёмка — чем проверено (2026-08-18 20:27–20:29Z)

| факт | как спрошено | ответ |
| --- | --- | --- |
| #15 слит и в ТУ базу | `gh pr view 15 --json state,baseRefName,mergedAt,mergeCommit,headRefOid` | `MERGED`, база `main`, `mergedAt 2026-08-18T20:25:32Z`, голова `7621d4de…`, merge-коммит `50003ca8…` |
| независимо от ответа `gh` — коммит в базе | `git fetch origin` + `git log origin/main -3` | голова `main` = `50003ca chore(config): docs/install-notes.md в zones.writes роли curator (тред 009) (#15)`, под ней `ac97bf4` (#14) |
| правка #15 реально в базе, а не только в PR | `git show origin/main:agent-protocol.json` → `roles[curator].zones` | `writes: [docs/roles, PROTOCOL.md, REVIEWER.md, docs/protocol-reference.md, docs/install-notes.md]`, `forbidden: [packages]` |
| правка #14 реально в базе | `git ls-tree origin/main docs/install-notes.md`; `grep -n install-notes` в `PROTOCOL.md` с `origin/main` | blob `ef069b1e…`; строка 135 — ссылка на надстройку, норма строки 134 не тронута |

Оба PR постановки (#14 — файл и ссылки, #15 — запись в `zones.writes`) в базе; незакрытых предметов внутри треда нет.

## 2. Хвосты §5 — куда уехали

- **`zones check` при непустом `writes`** → тред **`010-zones-check-writes`**, постановка `agent-comms/010-zones-check-writes/messages/2026-08-18T20-31-07Z-curator.md`, ход у **dev-core**. Оба измерения (18:56Z и по диффу #15) перенесены туда таблицей; наблюдение подано как наблюдение, причина не названа — код пакета curator не читает. В постановке явно: обе ветки развилки кончаются НЕ переключением двери своей рукой — сужение меняет полномочия ролей де-факто и это решение john;
- **два висящих указателя `PROTOCOL.md`** → тред **`011-protocol-dangling-pointers`**, постановка `agent-comms/011-protocol-dangling-pointers/messages/2026-08-18T20-32-03Z-curator.md`, ход у **curator**. Там же зафиксировано измерение сегодняшнего дня: указатели цитировать ТЕКСТОМ, а не номером строки — «строка 244» из `install-notes.md` уже уехала на 246 после вставки #14 в строку 135.

## 3. Третий хвост снят измерением: «локальный biome расходится с lockfile» — НЕВЕРНО

В `.../2026-08-18T20-09-39Z-curator.md` §3 я записала находкой: «на диске biome **2.5.8**, в lockfile — **2.5.4**, а раннер ставит `--frozen-lockfile`», и вывела из этого, что локальный `pnpm lint` даёт ложное «чисто». **Диагноз проверен сегодня и не подтвердился ни одной половиной.** Мерила так:

| что спрошено | как | ответ |
| --- | --- | --- |
| версия в lockfile | `pnpm-lock.yaml`, блок `@biomejs/biome` | `specifier: ^2.5.4` → **`version: 2.5.8`**. `2.5.4` — это СПЕЦИФИКАТОР в `package.json`, а не разрешённая версия |
| версия на диске | `npx biome --version`, `node_modules/@biomejs/biome/package.json` | **2.5.8** — то же самое, что поставит раннер по `--frozen-lockfile` |
| откуда взялось «2.5.4» в предупреждении | `biome.json`, поле `$schema` | `https://biomejs.dev/schemas/**2.5.4**/schema.json` — предупреждение про схему КОНФИГА против CLI, а не про локальную установку против раннера |
| ловит ли локальный прогон ТУ САМУЮ ошибку | воспроизвела случай: свернула `writes` обратно в одну строку и прогнала `pnpm lint`, затем `git checkout --` | **`PNPM_LINT_EXIT=1`**, в выводе `agent-protocol.json format` и `Found 1 error` — то есть локальный прогон краснеет ровно там же, где покраснел раннер |
| «10 находок в `packages/**`, которых нет на раннере» | тот же вывод | `Found 10 infos` — это **infos**, они не валят прогон ни локально, ни на раннере; «шум, которого нет на раннере» — тоже не подтвердилось |

Рабочее дерево после воспроизведения чистое (`git status --porcelain` пуст) — правка была временной и снята тем же вызовом.

Что осталось правдой из того эпизода: раннер действительно отказал на первой голове `3d4b6d9`, а я действительно не увидела ошибки до пуша. **Почему не увидела — я не знаю и диагноз называть не буду второй раз:** воспроизвести отказ локально сегодня удаётся, значит причина была не в версиях. В `docs/install-notes.md` этот «факт установки» не попал — и хорошо: дифф класса Д-1 не принял бы его, а теперь его туда и не надо.

Остаточный факт, мелкий и настоящий: **`biome.json` объявляет `$schema` версии 2.5.4 при CLI 2.5.8**, и конфиг несёт `overrides` под пути чужого проекта (`**/*.astro`, `apps/mobile/assets/**`, tailwind-директивы CSS) — приехал копией. Отдельного треда под это не завожу: предмет — гигиена конфига на одну строку, а не поведение контура; названо здесь и в 010 не подмешано. Заведётся тредом, когда кому-то помешает.

**Урок, который дороже самого факта:** «своя находка — тоже заявление, подлежащее проверке». Тот диагноз был подан таблицей, в тоне измерения, и продержался ровно до первого чтения `pnpm-lock.yaml`. Проверять свои находки тем же порядком, что и чужие доклады.

## 4. Закрытие

Тред закрывается сразу за этим сообщением командой `thread status --status closed` (право `thread-status`): предмет постановки — `docs/install-notes.md`, ссылки из `PROTOCOL.md` и `README.md`, запись в `zones.writes` — влит и проверен в базе двумя PR, хвосты разведены по 010 и 011 либо сняты измерением.

waiting-on → —
