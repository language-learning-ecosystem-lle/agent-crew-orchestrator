---
from: curator
worker: claude-code
session: 47f8406d-7ee2-4881-9885-f1119359dce1
date: 2026-08-19T10:02:55Z
expects: ack
waiting-on: curator
parked-on: john
---

**Требуется решение john — три вопроса, все три ниже развёрнуты: (1) кнопка на #27, гарды 1 и 2 зелёные, стоит только гард 4; (2) кнопка на #29 после вердикта ревьюера, там тоже только гард 4; (3) можно ли записать в `docs/install-notes.md` класс «после merge, тронувшего воркфлоу ревьюера, открытые ветки подтягивать к базе» — своей постановки мне для гарда 3 мало.**

Постановка `config check` шагом в CI **принята**. Проверила каждый факт доклада dev-core своей рукой, а не по тексту доклада.

## Приёмка #29 — по трём пунктам, фактами, снятыми мной

**1. Красный прогон на сломанном конфиге — `32239926681` подтверждён.** `conclusion: failure`, ветка `ci/config-check-probe`, голова `aab32baed9543830c87a03fd922f0f43eff752aa` (09:53:32Z). В логе шага «agent-protocol.json держится» — обе строки, и вторая из них и есть работа:

```
- role 'dev-core': instructions 'docs/roles/dev-core-typo.md' are declared, but are not there at HEAD
##[error]config check отказал: - role 'dev-core': instructions 'docs/roles/dev-core-typo.md' are declared, but are not there at HEAD
##[error]Process completed with exit code 1.
```

Третья строка — ровно тот безымянный отказ, который увидел бы читатель сводки PR; `::error::` ставит имя причины рядом с ней. Заявление «проверено на живом прогоне, а не задумано» — верное, я стою в этом логе.

**2. Зелёный прогон — `32239908690` подтверждён**: `success`, голова `3bd4f94e71c76ad39d65eabd2730002b5dcd2469`, 09:53:20 → 09:56:31Z.

**3. Цена по времени сверена по меткам того же лога:** шаг стартовал 09:53:38.83, на красном прогоне отказал 09:53:48.74 при старте 09:53:47.61 — то есть 1,1 с, как и заявлено. Заявление «в сеть не ходит» принимаю как ЗАМЕР (клон с мёртвым origin, `--ref HEAD` → ok, `--ref origin/main` → отказ по имени хоста), а не как чтение кода — dev-core сам провёл эту границу, и она правильная.

**Граница шага названа вслух и названа честно** — ловит схему, версию протокола, существование путей `instructions` на том же ref, владение инстансами; не ловит машинный конфиг бокса и конфиг чужого обслуживаемого проекта. Это ровно то, что постановка объявила сознательно не покрываемым, и «валидируем конфиг» без границы в докладе не прозвучало ни разу.

**Форма вызова названа, как требовала постановка:** `pnpm protocol config check --ref HEAD`, без `--no-fetch`, и довод за `HEAD` (на `pull_request` это merge-ref, то есть спрошено про то, что будет влито) — не вкусовой.

**Восхождение на месте:** описание #29 первыми двумя строками несёт `thread: 014-merge-model` и `role: dev-core` — гард 3 машинно проверяем.

**Зонд закрыт правильно.** PR #30 — `state: CLOSED`, `mergedAt: null`, draft: сломанный конфиг в базу не поехал. Прогон `merge-notify` на его закрытии — `32239991522`, `skipped` (закрытие без merge джобу не поднимает), то есть счёт красных им не сдвинут.

**Ветку зонда `ci/config-check-probe` не сношу и сносить не прошу пока** — вопрос к john третьим номером ниже я поднимаю не про неё: удаление ветки необратимо, а её единственная ценность (лог `32239926681`) в GitHub переживёт ветку. Пусть стоит до merge #29; после него скажу john отдельной строкой, и снесёт его рука либо он передаст это правом.

## Хвост открытых PR — прогнан перед передачей хода

`gh pr list --state open` → **#27**, **#28**, **#29**. **Своей рукой не смёржено ничего**, и вот почему по каждому:

- **#27** (`feat/014-role-github`, голова `ba5e25bfe1d3fcb38809f3c92cab37ddd4fe59a0`) — `merge-gate --ref origin/main --pr 27 --power-docs PROTOCOL.md,REVIEWER.md,.github/workflows`:
  - гард 1 — **ok**: `approved on ba5e25b` (вердикт ревьюера в этой же ленте, сообщение `reviewer-pr` с `verdict: approve, pr: 27`);
  - гард 2 — **ok**: `2 check(s) green: review=SUCCESS, checks=SUCCESS` (прогон `checks` `32238141635`, прогон ревьюера `32238455449` — оба на той же голове);
  - гард 4 — **STOP**: `john merges this one — it changes agent-protocol.json`;
  - mergeability — `MERGEABLE (CLEAN)`.
  То есть с прошлого моего хода PR дозрел полностью и упирается РОВНО в одну вещь — кнопку john;
- **#29** (`ci/config-check-step`, голова `3bd4f94e…`) — гарды 1 и 2 **STOP** по одной причине: прогон ревьюера `32240266146` ещё `in_progress` (сам `checks` зелёный — `32239908690`); гард 4 — **STOP**: `it changes .github/workflows/checks.yml`. Вердикта не жду и хода на него не трачу: он придёт сообщением в тред;
- **#28** — тред 015, не эта лента; отсюда не трогаю.

## Порядок кнопок: #27 первым, и это не вкус

Приёмка «первое уведомление о merge обязано лечь в тред своей постановки» измерима только на merge ПОСЛЕ посадки роли `github`. Если первым уходит #29, семнадцатый красный прогон `merge-notify` мы оплатим ничем: измерения он не даст, а починку отложит. Если первым уходит #27 — **merge #29 становится самим измерением**, и смотреть надо глазами в тред 014, а не в вывод `merge-gate`.

Счёт на этот момент — **16 красных `merge-notify` подряд** (сверено: `gh run list --workflow merge-notify.yml --limit 20` даёт 16 `failure` подряд до последнего `success` от 18.08 16:23Z, который записи тоже не сделал).

**Порядок безопасен, и это замер, а не расчёт:** тестовое слияние головы #29 в голову #27 (`git merge --no-commit --no-ff` в отдельном воркдереве) — `Auto-merging README.md, Automatic merge went well`, конфликта нет. Оба PR правят соседние абзацы `README.md`, поэтому проверила, а не предположила. Воркдерево снято.

Класса «ветка отстала → ревьюер молчит» здесь нет: #27 воркфлоу ревьюера не трогает, #29 трогает `checks.yml`, а не `claude-review.yml`, и вердикт по #29 снимается ДО любого из этих merge.

## Вопрос 3 — почему запись класса требует твоего слова

Класс сработал дважды за час (#25 и #27): после merge, тронувшего `.github/workflows/claude-review.yml`, `claude-code-action` пропускает себя на отставших ветках **молча** («Skipping action due to workflow validation»), то есть ревьюер по такому PR не отказывает, а не приходит. Лечение — одна строка в порядке: перед меткой `review` сверять `git merge-base --is-ancestor origin/main <head>` и подтягивать веткой.

Место записи — `docs/install-notes.md`, это моя зона и не док власти. Но гард 3 говорит: curator не мёржит то, что поставила сама без решения john в ленте, — а тут постановщик и исполнитель совпадут в одном лице. Поэтому прошу одно слово: **«пиши» — и я кладу это отдельным PR своим следующим ходом**, с обычным маршрутом и вердиктом ревьюера.

waiting-on → curator (ход остаётся у меня, потому что вопрос несу я; ждём решения john по трём пунктам выше).

