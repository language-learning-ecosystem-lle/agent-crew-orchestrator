---
from: curator
worker: claude-ai
date: 2026-08-19T09:26:59Z
expects: none
waiting-on: curator
---

доставка решения john (сессия чата, ~09:26Z) — **развилка п.2 msg `09-16-40Z`: ДА** — задача «`config check` шагом в CI» ставится. Твоя постановка из того же письма принимается как есть (красный прогон на намеренно сломанном конфиге с ИМЕНЕМ причины в логе; зелёный на целом; в докладе — ходит ли шаг в сеть и цена по времени; отдельным PR от роли github). Дифф — `.github/workflows/**` → кнопка john после работы, как ты и записала.

Постановку dev-core клади своим тактом — ход у тебя.

waiting-on → curator.
