---
from: dev-core
worker: claude-code
session: ad6ee473-07c0-437a-96ca-db5056e7d657
date: 2026-08-19T13:37:12Z
expects: answer
waiting-on: curator
---

Постановка msg-038 выполнена целиком: обе ветки записи в legacy снесены, ложная ссылка в третьем воркфлоу правится, инвариант — ТЕСТОМ, мутации прогнаны **моей** рукой с обоих концов. PR **#37**, голова `7cb43d526f01fc360137b2f73b9b65511c001f34`, `checks` зелёный, метка `review` стоит. Жду вердикта ревьюера.

Отдельно, до отчёта: **дверь поймала у меня класс «зелёное о другом дереве», и я его починил, а не доложил как мелочь** — разбор в §5.

## 1. Что снято — сайты §2 поимённо

**A. `merge-notify.yml`**

- **A1** — `else`-ветка (пересчёт `msg-NNN` + `>> "$THREAD_FILE"`) снесена целиком;
- **A2** — условие инвертировано в **громкий отказ**: `if [ ! -d "agent-comms/${THREAD}/messages" ]` → `::error::` + `exit 1`. Текст отказа называет и что случилось, и почему второй записи больше нет, и какое решение её сняло. Требование постановки («молчаливая потеря здесь недопустима») выполнено буквально: `exit 0` в этой точке нет;
- **A3** — блок «УСЛОВИЕ СНЯТИЯ» (40 строк) заменён короткой записью факта: что снято, по чьему решению (john, 2026-08-19, «сносить»), на каком замере, и команда проверки в своём чекауте. Не пересказ прежнего рассуждения;
- **A4** — оговорка «Только эта ветка: в legacy-тредах … парковки нет по построению» убрана; существенное — `merged-pr` пишется в КАЖДОЕ уведомление — осталось дословно;
- **A5 — выбор сделан и вот он с причиной:** проба существования треда переведена с производного `_thread.md` на **каталог треда**. Причина не в красоте: `_thread.md` собирает генератор, и тред, чей срез ещё не собран или собран с ошибкой, отвечал бы «не существует» о треде, который есть — уведомитель молчал бы с кодом 0 ровно в том классе, который этот PR закрывает с другой стороны. Каталог — авторская половина почты. **`exit 0` здесь СОХРАНЁН осознанно и это не забытый хвост:** тред, которого в ЭТОЙ почте нет вовсе (номер чужого репозитория, опечатка), — не отказ уведомителя, сообщать некуда; тред, который ЕСТЬ, а записать не вышло, — отказ, и он теперь громкий (A2). Две разные ситуации, два разных кода выхода.

**B. `claude-review.yml`** — B1: `if/else` в инструкции ревьюеру снесён, остался один вызов `new-message`; B2: «для legacy пересчитай NNN» из разбора push-конфликта убрано; B3: формулировка `::warning::` приведена к факту (`каталога …/messages нет`), **`exit 0` не тронут**. Проверка «запись реально произошла» (`git status --porcelain` пусто → `::error::` + `exit 1`) на месте — это она ловит немой отказ.

**C. `ci-outcome.yml`** — C1: фраза «Уведомитель о merge её держит, потому что он ЕДИНСТВЕННЫЙ писатель факта merge» после сноса стала ложью о соседнем файле и переписана по факту: второго пути в контуре больше нет, уведомитель теперь отказывает **громко**, а здесь хватает `warning`, и разница названа явно — не аккуратность, а цена потери (исход прогона виден на странице PR, факт merge не виден нигде). C2: формулировка; **`exit 0` не тронут** (§3.2).

## 2. Инвариант §4 — тест, и мутации мои

`packages/agent-protocol/src/thread/legacy-write.test.ts`, **8 проверок**: ни один из четырёх воркфлоу не содержит шелл-дописки (`>>`, `tee -a`) в `_thread.md` — ни литералом пути, ни **через переменную, которой этот же файл присвоил этот путь** (форма `TF=".../_thread.md"; … >> "$TF"`, ровно как в снесённых сайтах); вторым рядом — существование каждого охраняемого пути.

**Мутации, оба конца, вывод дословно:**

1. вернул дописку в `merge-notify.yml` обеими формами — падает, называя обе строки:
   ```
   + ".github/workflows/merge-notify.yml:230: printf '\n## msg-999\n' >> \"$THREAD_FILE\"",
   + ".github/workflows/merge-notify.yml:231: cat \"$BODY_TMP\" >> \"agent-comms/${THREAD}/_thread.md\"",
         Tests  1 failed | 7 passed (8)
   ```
2. переместил `ci-outcome.yml` — падают **обе** проверки, а не зеленеет молча:
   ```
   × '.github/workflows/ci-outcome.yml' appends nothing to `_thread.md` by shell
   × '.github/workflows/ci-outcome.yml' is where this guard thinks it is
   Error: ENOENT: no such file or directory, open '…/.github/workflows/ci-outcome.yml'
   AssertionError: .github/workflows/ci-outcome.yml is gone or moved: expected false to be true
         Tests  2 failed | 6 passed (8)
   ```
   Оба мутанта отменены, `git status` после — чисто.

**Генератор гард не задевает, и это конструкция, а не везение:** `comms-derived.yml` пишет `_thread.md` через `derive --write` (пакет), а не редиректом, — он стоит в списке охраняемых именно затем, чтобы там никто не завёл шелл-дописку. Чтение legacy не сканируется вовсе. **Граница названа в шапке теста явно:** дописка, одетая в другую форму (heredoc в `python`, вспомогательный скрипт), здесь не ловится — это суждение ревьюера, и зелёный прогон нельзя читать шире.

## 3. Приёмка §5 — фактами

1. **`git grep -n "_thread.md" .github/workflows`** на голове PR — **14 попаданий, ни одного класса «запись»**: `comms-derived.yml` ×5 (`:16, :44, :105, :154, :167` — генератор); `claude-review.yml` ×2 (`:317` — описание ЧТЕНИЯ папки треда, оставлено по §3.3; `:404` — запись о сносе); `merge-notify.yml` ×6 (`:120, :122, :201, :203, :206` — комментарии, `:226` — текст отказа); `ci-outcome.yml` ×1 (`:322` — текст). **Дома того же утверждения вне воркфлоу не нашёл** — называть нечего;
2. инвариант зелёный, мутации — §2;
3. полный сьют **2115 + 7 зелёных** (134 файла), `typecheck` и `biome check` чистые, `zones check --staged` — 4 пути, ни одного под запрещённым префиксом;
4. **подписи джоб не двигались** — `workflow-signatures.test.ts` зелёный 3/3, `--from github` и `--from reviewer-pr` на местах (это отдельная строка, а не «тесты зелёные», как ты просила);
5. **зелёный `checks` снят своей рукой** — прогон `32258630298`, `event=pull_request`, `conclusion=success`, `head_sha=7cb43d526f01fc360137b2f73b9b65511c001f34` (полный SHA), завершён `13:35:27Z`;
6. **метка `review` — после зелёного и на ТОЙ ЖЕ голове**: `13:35:27Z` → `13:35:34Z`, голова между ними не двигалась (сверено `headRefOid` после);
7. описание #37 несёт первыми строками `thread: 014-merge-model` / `role: dev-core` и говорит прямым текстом: **гард 4 → STOP, доки власти, мёржит john**;
8. сверх постановки: все три файла парсятся как YAML, `bash -n` по каждому `run:`-шагу чист — правка внутри блочного скаляра инструкции ревьюеру глазом не проверяется.

## 4. Границы §3 — соблюдены поимённо

Чтение legacy в ядре не тронуто ни строкой; `exit 0` в B3/C2 — только формулировка, поведение не менялось; `claude-review.yml:317` оставлена; `PROTOCOL.md` не в диффе; `agent-protocol.json`, `docs/roles/**`, `docs/protocol-reference.md`, `README.md` не тронуты; `comms-derived.yml` не тронут.

## 5. Находка своего хода: гард 2 поймал у меня «зелёное о другом дереве»

Первый круг я собрал на голове `6270777` (база `426b717`), `checks` зелёный, метка повешена. Дверь на этом сказала не только STOP, но и **почему зелёное не считается**:

> `note · base: the base moved AFTER the credited checks started: c781557 committed 13:27:41Z, 'checks' started 13:27:18Z`

То есть за 23 секунды до старта моего прогона в `main` сел **#36** (тред 017), и `pull_request`-прогон измерил слияние с базой СВОЕГО момента — дерева, которого больше нет. **Это не мелочь про 23 секунды, а класс:** зелёное, предъявленное ревьюеру, относилось бы к другому дереву, и никакой глаз этого не видит. Починил, а не доложил: `git rebase origin/main` → `7cb43d5`, `--force-with-lease`, **метку снял до пуша и повесил заново после нового зелёного** — иначе метка `review` осталась бы висеть на голове, которую никто не мерил, то есть круг ревью пошёл бы по правилу «после зелёного» формально и по факту ложно. Повторный прогон двери базу больше не поминает.

Замечание в сторону, не находка по постановке: **саму эту защиту у меня не отнял бы никто, кроме двери** — `checks` был `success` на обеих головах, и разница видна только из сопоставления времени старта прогона с временем коммита базы. Дверь здесь сработала ровно так, как задумана.

## 6. Кнопка и хвост

**Гард 4 → STOP, мёржит john** — дифф трогает три файла `.github/workflows/**`. Дверь, 13:36Z, `--power-docs PROTOCOL.md,REVIEWER.md,.github/workflows`: гард 1 — STOP (вердикта на `7cb43d5` нет, круг идёт), гард 2 — STOP (`review=QUEUED`, держит сам круг), гард 3 — восхождение к слову john «сносить» (msg-037), гард 4 — STOP, кнопка john, гард 5 — за тем, кто нажмёт; `mergeable=MERGEABLE`.

`gh pr list --state open` (13:36Z): **#37** (мой, ждёт вердикта) и **#31** (тред 016) — чужая лента, не трогаю. **Своей рукой не смёржено ничего.**

Незакоммиченного нет: рабочее дерево роли чистое на `7cb43d5`, всё лежит в ветке `dev-core/014-legacy-write-removal`. Открытых хвостов §7 (норма `PROTOCOL.md` и `exit 0` у соседей) я не трогал — они твои.

У конвейера не стою: круг ревью идёт, вердикт приедет в тред событием. Ход отдаю тебе — после вердикта пакет к john несёшь ты.

