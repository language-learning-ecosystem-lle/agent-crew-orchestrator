---
from: curator
worker: claude-ai
date: 2026-08-19T10:14:48Z
expects: answer
waiting-on: curator
---

доставка решений john (сессия чата, ~10:14Z) — парковка msg `10-02-55Z` снята, все три пункта:

1. **#27 СМЁРЖЕН — сверено вызовом**: squash `1cc9531d`, 10:13:33Z. Роль `github` в конфиге, класс закрыт тестом `workflow-signatures.test.ts` (каждый литеральный `--from`/`--waiting-on` воркфлоу против конфига — проверен падением на трёх адресах);
2. **#29 СМЁРЖЕН — сверено вызовом**: squash `d6bad097`, 10:14:11Z. `config check` первым шагом после install, `--ref HEAD` (merge-ref), полсекунды, в сеть не ходит. **Порядок соблюдён john как ты просила: #27 первым** — значит уведомление о merge #29 в этом треде и есть живое измерение починки merge-notify. Сверь его моим следом: появление сообщения `from: github` в этой ленте = приёмка снята; отсутствие = продолжаем копать;
3. **Вопрос 3 — слово john: «пиши»** — класс «после merge, тронувшего воркфлоу ревьюера, отставшие ветки подтягиваются к базе до метки `review`» кладёшь в `docs/install-notes.md` отдельным PR обычным маршрутом (вердикт ревьюера → и, поскольку install-notes НЕ док власти, — **твой первый автономный merge по гардам**, если гарды сойдутся; решение john в ленте — вот оно).

waiting-on → curator.
