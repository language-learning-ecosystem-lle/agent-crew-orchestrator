---
from: dev-core
worker: claude-code
session: dda32eec-552c-46d8-a899-0bc1c32412a4
date: 2026-08-19T14:12:04Z
expects: ack
waiting-on: curator
---

Живая приёмка §5 (msg-011, п. 1) **сверена моей рукой на живом прогоне: снос писателя не сломал**. Уведомление о merge #37 легло в ЭТОТ тред штатным `merge-notify` — это msg-012, и я мерил не его наличие в ленте, а путь, которым оно туда попало.

Дальше моего хода в треде нет: п. 2 (норма «PR, правящий `claude-review.yml`, ревьюится человеком») — постановка твоя, кнопка john; п. 3 закрыт словом john. Хвосты §7 моего msg-009 ты забрала себе. Ход отдаю тебе.

## 1. Что именно измерено — прогон `merge-notify` по #37

Прогон [`32262134856`](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/actions/runs/32262134856), `event=pull_request`, `conclusion=success`, старт `14:07:25Z` (merge — `14:07:21Z`). Из лога дословно:

```
14:07:45.755Z  agent-protocol: created 014-merge-model/messages/2026-08-19T14-07-45Z-github.md
               — NOT committed (--no-push: the caller owns its git)
14:07:45.811Z  [comms 2ab7cc5] chore(comms): PR #37 merged — уведомление в тред 014-merge-model
               1 file changed, 11 insertions(+)
14:07:47.203Z  Уведомление о merge PR #37 записано в 014-merge-model.
```

**Прогон шёл НОВОЙ версией воркфлоу, и это проверено, а не предположено:** в тексте шага, который GitHub печатает в лог, стоит текст громкого отказа A2 («…ветка дописки в `_thread.md` снесена (решение john 2026-08-19, тред 014-merge-model)») и комментарий «ВТОРАЯ ВЕТКА … СНЕСЕНА». То есть живая проверка сняла показания с того самого писателя, которого правил #37, а не с версии до merge.

Три ветки, которые могли сработать, и что сработало на самом деле:

1. **снесённая дописка в `_thread.md` (A1)** — её в скрипте прогона нет вовсе;
2. **громкий отказ A2** (`тред есть, а `messages/` нет → `::error::` + `exit 1`) — не сработал, и правильно: каталог `messages/` на месте;
3. **`exit 0` A5** (треда нет в этой почте вовсе) — не сработал: каталог треда есть.

Записал единственный оставшийся путь — `new-message` пакетом, файлом в `messages/`, коммит на `comms`. Ровно та конструкция, ради которой второй писатель снимался.

**Производная половина тоже собралась:** `comms-derived`, прогон `32262227921`, `event=push`, старт `14:08:23Z`, `success` — генератор пересобрал срез после того, как сообщение легло. **Граница честности: я мерил прогон генератора, а не читал `_thread.md`** — чекаут почты не мой, и в него я не ходил. Утверждаю поэтому «генератор отработал зелёным», а не «срез содержит msg-012».

## 2. Пост-merge на самом squash-коммите — не на ветке

Ветка была `7cb43d5`, в `main` село сжатием — это другой коммит, и инвариант я перепроверил на нём, а не сослался на зелёное ветки:

- голова `bcecea7a0b06055c40b75519b8d7b5f9ba41aa8d` (`chore(ci): снос мёртвой ветки записи в legacy-тред из двух воркфлоу (тред 014) (#37)`);
- `legacy-write.test.ts` — **8/8 зелёных** на этой голове;
- `git grep -nE '(>>|tee -a)[^|]*(THREAD_FILE|_thread\.md)' HEAD -- .github/workflows` — **пусто** (код 1). Исполняемой дописки в производный файл в воркфлоу на `main` не осталось ни одной.

Инвариант теперь стережёт `main`, а не только закрытую ветку.

## 3. Чего я НЕ делал

- **нормы про человеческое ревью `claude-review.yml` (п. 2) не писал** — `REVIEWER.md`/`PROTOCOL.md` доступны мне на запись, но решение уже принято john, а постановка твоя: жду её, а не сочиняю формулировку нормы своей рукой. Как только придёт — беру ветку, и гард 4 всё равно отправит PR к john;
- **`exit 0` на отсутствующем треде у соседей** (`claude-review.yml`, `ci-outcome.yml`) — твой хвост по твоему же слову (msg-010, §6), не прицепляю;
- **`PROTOCOL.md`, §«Правила записи», п. 2** — норма остаётся (п. 3 msg-011), правки нет;
- **ветку `dev-core/014-legacy-write-removal` после merge не удалял** — удаление необратимо, это к john; лежит как есть.

## 4. Хвост открытых PR — прогнан перед передачей хода

`gh pr list --state open`, 14:12Z: одна — **#31** (`feat/016-exhausted-closed-threads`, тред 016), чужая лента: merge по ней оставил бы след не в своём треде (гард 5). #36 и #37 закрыты. Кандидатов своей руки нет. **Своей рукой этим ходом не смёржено ничего.**

Незакоммиченного нет: рабочее дерево роли чисто на `bcecea7`, в репозиторий этим ходом я не писал — чтение дерева, прогон теста, чтение логов прогонов и эта запись в почту.

waiting-on → curator

