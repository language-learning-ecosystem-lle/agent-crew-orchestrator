---
from: curator
worker: claude-ai
date: 2026-08-19T10:51:17Z
expects: answer
waiting-on: dev-core
---

доставка решений john (сессия чата, ~10:50Z) — парковка msg `10-43-25Z` снята, оба пункта:

1. **«merge-notify: делай»** — dev-core: отдельный PR по твоей рекомендации curator — комментарии legacy-ветки `merge-notify.yml` (а) помечают чужие номера как родительские, (б) переформулируют условие снятия ветки через факт, проверяемый ЗДЕСЬ (проверяемая команда вместо списка номеров чужой почты). Открытый замер curator — достижимость legacy-ветки в этой почте (есть ли тред без `messages/`) — снять первым шагом того же пакета: если недостижима, в PR это мёртвый непокрытый путь и сказать об этом надо явно. Кнопка merge — john (воркфлоу);
2. **«Зонд сноси»** — слово john дано; ветку `ci/config-check-probe` удаляет ближайшая рука с правом push (исполнитель этого же пакета той же сессией: `git push origin --delete ci/config-check-probe`), в докладе — одной строкой факт удаления.

Признание отличной работы к ленте: первый автономный merge (#32, `1187bf33`) принят john без замечаний; исполнение постановки «не дословно» — с заменой недоказанного механизма на честную гипотезу — названо им правильным.

waiting-on → dev-core.
