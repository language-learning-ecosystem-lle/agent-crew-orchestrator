---
from: curator
worker: claude-code
session: 79acc66e-cf13-49c9-940e-22f953595995
date: 2026-08-19T14:21:15Z
expects: ack
waiting-on: curator
parked-on: john
---

**Требуется решение john — кнопка на #38** (норма «PR, правящий воркфлоу ревьюера, ревьюит человек»: гард 4 → STOP, оба файла диффа — доки власти). **И вторая половина того же звонка: пауза контура объявлена словом в треде, а слово даймона не останавливает — эта сессия поднята им же после msg `14-09Z`, пока пауза стоит.**

Пункт 2 твоих решений (msg `2026-08-19T14-09Z-curator`, развилка «(в) — ДА») исполнен постановкой и пакетом. Метку `review` НЕ вешала намеренно — круг ревьюера расходует ту самую недельную квоту, из-за которой стоит пауза.

## 1. Что записано и где

PR [#38](https://github.com/language-learning-ecosystem-lle/agent-crew-orchestrator/pull/38), ветка `curator/014-human-review-norm`, голова `f6e0afff305466e6ebaba5ffca07991659bed46b`, два файла:

- **`PROTOCOL.md`, §«Ограничение ревьюера, заданное платформой»** — сама норма и четыре следствия: метка `review` на такой PR не вешается вовсе; **зелёный чек с именем `review` вердиктом НЕ является** (джоба штатно зеленеет на самопропуске — гард 2 проходит там, где суждения нет вовсе, и различает их только гард 1); гард 1 закрывается ИМЕНЕМ в треде (кто прочёл, какая голова полным SHA, чем подтверждено); **новых полномочий curator это не даёт и не может дать** — `.github/workflows/**` уводит такой PR к тебе гардом 4 независимо от гарда 1, то есть человеческий ревьюер и кнопка здесь одно лицо;
- **`docs/roles/curator.md`** — то же исключение в гарде 1 и одна строка в «Порядке».

**Почему `PROTOCOL.md`, а не `REVIEWER.md`** (ты оставил выбор мне): `REVIEWER.md` читает роль `reviewer-pr`, а в этом самом классе PR она не поднимается НИ РАЗУ — норма, положенная туда, не была бы прочитана никем именно тогда, когда нужна. Свойство инструмента в `docs/protocol-reference.md` при этом остаётся на месте: оно описывает самопропуск, а норма говорит, чем закрывается гард merge — сегодня разошлись ровно эти две вещи, и два прогона (`32258559756`, `32258971907`) ушли на то, чтобы открыть заново известное.

## 2. Гарды по #38 — дверью, 14:20Z

`pnpm protocol merge-gate --ref origin/main --pr 38 --power-docs PROTOCOL.md,REVIEWER.md,.github/workflows`, дословно:

1. **STOP гард 1** · `no approve verdict on f6e0aff` — метка не вешалась, круга не было (см. §3);
2. **ok гард 2** · зелёный `checks` на ТОЙ ЖЕ голове: прогон `32263010133`, `event=pull_request`, `conclusion=success`, `head_sha=f6e0afff305466e6ebaba5ffca07991659bed46b` (полный SHA), завершён `14:19:44Z`, длительность 3m23s. База не двигалась — `origin/main` = `bcecea7` и на момент старта прогона, и сейчас; замечания двери про уехавшую базу нет;
3. **you гард 3** · восхождение — решение john в ленте этого треда, msg `2026-08-19T14-09Z-curator`, п. 2 («развилка (в) — ДА»), источник — сессия чата ~14:07Z. Описание #38 несёт первыми строками `thread: 014-merge-model` и `role: curator`;
4. **STOP гард 4** · `john merges this one — it changes PROTOCOL.md, docs/roles/curator.md`. Ожидаемый исход: роль не принимает документы, определяющие её собственные полномочия, и эта норма — как раз о полномочиях;
5. **you гард 5** · след — за тем, кто нажмёт.

`mergeable=MERGEABLE`, `mergeStateStatus=CLEAN`, `zones check --staged --role curator` — 2 пути, ни одного под запрещённым префиксом. Заголовок годен как есть, merge только squash.

## 3. Чего я НЕ делала своей рукой

- **метку `review` не вешала** — при действующей паузе по квоте круг ревьюера это расход той самой квоты. Пакет стоит готовым: скажешь «продолжаем» — метка вешается на ту же голову (зелёный `checks` на ней уже есть, порядок «метка после зелёного» соблюдён), скажешь «читаю сам» — гард 1 закрывается твоим чтением по норме, которую этот же PR и вводит;
- **#38 не мёржу** — гард 4;
- **#31** (тред 016) — чужая лента: merge по ней оставил бы след не в своём треде (гард 5). `gh pr list --state open`, 14:21Z: только #38 и #31, кандидатов своей руки нет. **Своей рукой этим ходом не смёржено ничего.**

## 4. Наблюдение, которое держу наблюдением, а не диагнозом

**Пауза контура (msg `14-09Z`) объявлена сообщением в тред — но даймон сообщений не читает.** Факт: после неё контур поднял dev-core (msg `14-12Z`) и меня (эта сессия), то есть тратил квоту ровно тогда, когда пауза стояла. Механизм, который останавливает подъёмы, у нас есть и он другой — `orchestrator hold <role>` (или `orchestrator down`); стоит ли его применить и к каким ролям — решение твоё, и это расход денег, поэтому своей рукой я его не трогаю. Называю по правилу последней строки: не уверена, важно ли — значит, важно.

## 5. Хвосты треда 014, не тронутые этим ходом

- `exit 0` на отсутствующем треде у соседей (`claude-review.yml`, `ci-outcome.yml`) — мой хвост, поставлю отдельной задачей, к этому пакету не цепляю;
- ветка `dev-core/014-legacy-write-removal` после merge #37 не удалена (удаление необратимо — к тебе);
- п. 3 (норма ручной дописки в legacy-тред остаётся) закрыт твоим словом, правок нет.

Незакоммиченного нет: всё лежит в ветке `curator/014-human-review-norm`, рабочее дерево роли чисто на `f6e0aff`. Ход держу на себе — вопрос несу я.

parked-on john · waiting-on → curator

