---
from: curator
worker: claude-code
session: fae1b8e9-9b5e-446e-a475-d3b4b6d005ff
date: 2026-08-19T13:42:39Z
expects: ack
waiting-on: curator
parked-on: john
---

**Требуется решение john — два: (1) кнопка на #37** (гарды 2–5 разобраны ниже, гард 4 — доки власти, кнопка твоя); **(2) чем закрывается гард 1, если автоматический ревьюер этот PR судить НЕ МОЖЕТ ПО ПОСТРОЕНИЮ** — GitHub отказывает действию ревью на любом PR, который правит сам `claude-review.yml`.

Круг ревью по #37 не состоялся дважды (`2026-08-19T13-31-43Z-reviewer-pr.md`, `2026-08-19T13-36-10Z-reviewer-pr.md`). Причина проверена мной в логе прогона, а не принята с доклада, и это не поломка: гард GitHub против промпт-инъекции правкой воркфлоу. Лечение, названное ревьюером («подтянуть `main` и повесить метку заново»), к ЭТОМУ PR неприменимо — дифф трогает сам `claude-review.yml`, поэтому версия ветки не совпадёт с дефолтной до merge, сколько ни перезапускай. Замкнутый круг: PR правит ревьюера — ревьюер отказывается судить PR.

## 1. Факт самопропуска — дословно из лога

Прогон `32258971907` (голова `7cb43d526f01fc360137b2f73b9b65511c001f34`), шаг `anthropics/claude-code-action@v1`, 13:36:05Z:

> `##[warning]Skipping action due to workflow validation: Workflow validation failed. The workflow file must exist and have identical content to the version on the repository's default branch.`

Первый круг — прогон `32258559756` на голове `6270777` — кончился тем же. `verdict.md` не создан, поэтому не сработала ни одна из трёх доставок вердикта. **Контур при этом НЕ промолчал:** отсутствие вердикта доставлено обоими запасными путями — комментами в PR (`github-actions`, 13:36:08Z) и сообщениями в тред (`2026-08-19T13-31-43Z-reviewer-pr.md`, `2026-08-19T13-36-10Z-reviewer-pr.md`). Это ровно то, ради чего запасной путь ставили, и он отработал.

Дифф #37 — четыре файла (сверено `git diff --name-only origin/main...7cb43d5`): `.github/workflows/ci-outcome.yml`, `.github/workflows/claude-review.yml`, `.github/workflows/merge-notify.yml`, `packages/agent-protocol/src/thread/legacy-write.test.ts`.

## 2. Гарды по #37 — дверью, 13:41Z

`pnpm protocol merge-gate --ref origin/main --pr 37 --power-docs PROTOCOL.md,REVIEWER.md,.github/workflows`, дословно:

1. **STOP гард 1 · approve на текущей голове — НЕТ**: `no approve verdict on 7cb43d5`. Формальных review в PR ноль (`reviews: []`). Это не отказ ревьюера в суждении, а отсутствие суждения — см. §1;
2. **ok гард 2 · зелёные чеки на той же голове**: `review=SUCCESS, checks=SUCCESS`. `checks` — прогон `32258630298`, `event=pull_request`, `conclusion=success`, `head_sha=7cb43d526f01fc360137b2f73b9b65511c001f34` (полный SHA), завершён 13:35:26Z;
3. **you гард 3 · восхождение — есть**: тред `014-merge-model`, слово john «сносить» (доставка `2026-08-19T13-11-05Z-curator.md`, сессия чата ~13:10Z). Описание #37 несёт первыми строками `thread: 014-merge-model` и `role: dev-core`;
4. **STOP гард 4 · доки власти**: `john merges this one — it changes .github/workflows/ci-outcome.yml, .github/workflows/claude-review.yml, .github/workflows/merge-notify.yml`. Ожидаемый исход, а не помеха;
5. **you гард 5 · след** — за тем, кто нажмёт; я положу его в тред следующим ходом.

`mergeable=MERGEABLE`, `mergeStateStatus=CLEAN`. Заголовок годен как есть: `chore(ci): снос мёртвой ветки записи в legacy-тред из двух воркфлоу (тред 014)`, merge только squash.

**Наблюдение своей руки, и я держу его наблюдением, а не диагнозом:** гард 2 засчитал `review=SUCCESS` на голове, где вердикта нет вовсе. Чек `review` зелёный потому, что ДЖОБА отработала успешно — она и должна, самопропуск действия она обрабатывает штатно и намеренно не поднимает ложной тревоги смотрителю (её собственная строка: «статус не выставлен, ложной тревоги смотрителю не поднимаем»). То есть «зелёный чек с именем `review`» ≠ «ревью состоялось», и различает их только гард 1 — здесь он и сработал, ничего лишнего не прошло. Гарды не выводятся друг из друга ровно в этом смысле. Стоит ли что-то менять — вопрос о воркфлоу, то есть твой; я его не решаю и в §4 называю отдельной строкой.

## 3. Что я перемерила своей рукой, а не приняла с доклада dev-core

Голова `7cb43d5`, 13:37–13:41Z, чтением дерева коммита (рабочее дерево роли не трогала):

1. **`git grep -n "_thread.md" -- .github/workflows`** — **14 попаданий, ни одного класса «запись»**, и перечень совпал с докладом поимённо: `comms-derived.yml` ×5 (генератор), `merge-notify.yml` ×6 (комментарии + текст громкого отказа `:226`), `claude-review.yml` ×2 (`:317` — описание ЧТЕНИЯ папки треда, оставлено по границе §3.3 постановки `2026-08-19T13-17-07Z-curator.md`; `:404` — запись о сносе), `ci-outcome.yml` ×1 (текст);
2. **`git grep -nE '(>>|tee -a)[^|]*(THREAD_FILE|_thread\.md)' -- .github/workflows`** — **пусто**. Исполняемой дописки в производный файл в воркфлоу не осталось ни одной;
3. **инвариант — прочла конструкцию, а не только чужой зелёный.** `legacy-write.test.ts`: 4 охраняемых файла × 2 ряда = 8 проверок. Первый ряд ловит дописку и литералом пути, и **через переменную, которой этот же файл присвоил этот путь** (`TF=".../_thread.md"; … >> "$TF"` — форма снесённых сайтов); второй ряд — существование каждого охраняемого пути, то есть переименование роняет тест, а не зеленит молча. Список файлов задан ПЕРЕЧНЕМ, а не globом, и причина названа в самом файле: glob, замолчавший вничью, — ровно тот отказ, от которого стоит второй ряд. Генератор в перечне стоит намеренно (он пишет `_thread.md` пакетом, `derive --write`, а не редиректом). **Граница названа в шапке теста явно**: дописка в другой форме (heredoc, вспомогательный скрипт) здесь не ловится;
4. **чего я НЕ делала:** полный сьют своей рукой не гоняла — `checks` на этой голове зелёный (прогон `32258630298`), а числа dev-core (2115 + 7, 134 файла) я не перепроверяла. Мутации инварианта прогнал dev-core и показал вывод с обоих концов; **своей рукой я их не воспроизводила** — это работа ревьюера, которого здесь не было, и я не выдаю своё чтение за его вердикт.

**Моё чтение — не approve.** Оно говорит «в фактах, которые я проверила, ничто не противоречит постановке», и не заменяет независимого суждения. Именно поэтому §4 — вопрос, а не рекомендация нажать.

## 4. Развилка, которую я не решаю

**Гард 1 требует вердикта ревьюера на текущей голове, а произвести его в этом контуре некому.** Три исхода, и выбор твой:

- **(а) человеческое ревью** — судишь ты, и ты же нажимаешь кнопку (гард 4 всё равно твой). Дёшево здесь и сейчас: пакет собран, факты §2–3 перемерены;
- **(б) сузить дифф** — вынести правку `claude-review.yml` в отдельный PR, чтобы остальные два воркфлоу и тест прошли автоматическим кругом. Проблему делит, но не снимает: выделенный PR упрётся в тот же гард и всё равно приедет к тебе человеческим ревью, а цена — лишний круг и второй пакет к тебе вместо одного;
- **(в) объявить нормой**, что PR, правящий `claude-review.yml`, ревьюится человеком, и записать это там, где следующий круг это прочтёт. **Это моя рекомендация**, и она не про #37, а про класс: сегодня мы потратили два прогона на то, чтобы заново открыть гард GitHub. Запись — в `REVIEWER.md` или `PROTOCOL.md`, то есть док власти: постановка моя, кнопка твоя.

Рекомендация curator ≠ решение john: без твоего слова я не ставлю ни (б), ни (в), и #37 стоит.

## 5. Третий вопрос, обещанный в ленте (постановка `2026-08-19T13-17-07Z-curator.md`, §7) — доношу здесь, чтобы звонок был один

**`PROTOCOL.md`, §«Правила записи», п. 2** по-прежнему предписывает для немигрированных тредов «дописывание секции в `_thread.md` вручную». После этого сноса норма и установка расходятся не фактом (ручная дописка человеком и ветка воркфлоу — разные писатели), но читаться будут вместе. **Твоё слово «в legacy НЕ ПИШЕМ» могло достать и до самой нормы — это моя гипотеза о границе твоего решения, а не диагноз.** Нужно одно слово: норма остаётся как есть (ручная дописка человеком законна) или снимается тоже. `PROTOCOL.md` — док власти: постановка моя, кнопка твоя, и в этот PR правка не входит ни при каком ответе.

## 6. Что я НЕ делаю своей рукой

- **#37 не мёржу** — гард 4. Роль не принимает документы, определяющие её собственные полномочия;
- **вердикт за ревьюера не пишу и приёмку его вердиктом не называю** — §3.4;
- **`exit 0` на отсутствующем треде** у соседей (`claude-review.yml`, `ci-outcome.yml`) — открытый хвост, и он МОЙ, не твой: поставлю задачей отдельно, когда встанет вопрос о его цене. К этому пакету не прицепляю.

## 7. Хвост открытых PR — прогнан перед передачей хода

`gh pr list --state open`, 13:41Z: **#37** (мой, гард 4 — STOP, кнопка твоя) и **#31** (`fix(notify)`, тред `016-exhausted-closed-threads`) — чужая лента, merge по ней оставил бы след не в своём треде (гард 5). Кандидатов своей руки нет. **Своей рукой этим ходом не смёржено ничего.**

Незакоммиченного нет: рабочее дерево роли чисто, в репозиторий этим ходом я не писала — чтение дерева коммита, прогоны двери и эта запись в почту.

parked-on john · waiting-on → curator

