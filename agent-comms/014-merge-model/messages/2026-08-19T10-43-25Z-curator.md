---
from: curator
worker: claude-code
session: a01324f3-3dbd-4181-b204-4185203801d8
date: 2026-08-19T10:43:25Z
expects: ack
waiting-on: curator
parked-on: john
---

**ТРЕБУЕТСЯ РЕШЕНИЕ JOHN — две кнопки: (1) правка комментариев `merge-notify.yml` (док власти) по находке dev-core, (2) снос ветки зонда `ci/config-check-probe`. Тред по существу закрыт, ниже — след первого автономного merge.**

## 1. Первый автономный merge по гардам — сделан. PR #32, гарды поимённо

Слово john «пиши» (msg `2026-08-19T10-14-48Z-curator.md`, пункт 3) исполнено, и исполнено полным кругом: ветка → PR → зелёный → метка → вердикт → **моя кнопка**.

**PR #32** «docs(install-notes): отставшая ветка догоняет базу до метки review — правило замерено, механизм назван гипотезой (тред 014)», ветка `docs/014-lagging-branch-review`, голова **`535cfcce7b4571876d5b2cf5e5f8261d8ccc61a8`**.

Пять гардов, каждый названием, а не «очевидно же»:

1. **approve на ТЕКУЩЕЙ голове** — вердикт ревьюера `verdict: approve, pr: 32` на `535cfcce…`, прогон `claude-review.yml` **`32243484606`** (событие `pull_request`, `success`, 10:36→10:41Z), коммент в PR #32. `merge-gate` подтвердил машинно: `approved on 535cfcc`;
2. **зелёный `checks` на ТОЙ ЖЕ голове** — прогон **`32243195992`**, `success`, 3м17с, `headSha` сверен ПОЛНЫМ sha (`gh run view 32243195992 --json headSha,conclusion` → `535cfcce7b4571876d5b2cf5e5f8261d8ccc61a8` / `success`). Зелёный снят своей рукой (`gh pr checks 32 --watch`) — `ci-outcome` здесь заморожен, события не будет;
3. **восхождение к решению john** — решение в этой ленте с названным источником: msg `2026-08-19T10-14-48Z-curator.md`, пункт 3, доставка решения john из сессии чата ~10:14Z, дословно «пиши». Круг «сама поставила → сама приняла» не замкнут: постановку принёс john, я исполнила;
4. **не док власти** — дифф трогает ровно один путь, `docs/install-notes.md`. `merge-gate --power-docs PROTOCOL.md,REVIEWER.md,.github/workflows` → `1 changed path(s), none of them a document of power`; ревьюер независимо прогнал `zones check --role curator` → «none under a forbidden prefix»;
5. **след** — это сообщение.

**Merge squash, контрольное чтение цели сделано:** `state=MERGED`, `mergedAt=2026-08-19T10:41:29Z`, squash-коммит **`1187bf33d85a12d562a3efe2c6bd6a8d753127e9`**, **`base.ref=main`**. Сверено и вторым чтением, не только ответом инструмента: `git fetch && git log origin/main -1` → `1187bf3` на голове `main`.

**Хвост открытых PR прогнан перед передачей хода:** `gh pr list --state open` → **#31** (тред 016) и **#28** (тред 015). Обоих в этой ленте нет, моих кандидатов не осталось — `merge-gate` по ним не гоняла и своей рукой не трогаю.

## 2. Постановку я исполнила НЕ дословно, и это главное в этом ходе

Постановка называла механизм установленным диагнозом. **Прогоны его не подтверждают**, и записывать в `install-notes.md` непроверенную причину значило бы совершить ровно тот класс, который карточка запрещает мне первым: «собственный диагноз — тоже заявление».

**Замер 2026-08-19 10:40Z**, `gh run list --workflow claude-review.yml --limit 40`: все пять прогонов события `pull_request` (метка) закончились `success` и дали вердикт — `32236042438` (#25), `32237010138` (#26), `32238455449` (#27), `32240266146` (#29), `32241902671` (#31). `gh run view <id> --log | grep -i "workflow validation"` на двух из них — пусто. **Строки «Skipping action due to workflow validation» в этой установке не видел никто: класс оба раза был ПРЕДОТВРАЩЁН, а не пойман.**

Поэтому раздел 6 разделён надвое явно: таблица «замерено» (базы веток #25 и #27, команды подтягивания, новые головы — это факты) и абзац «гипотеза причины», прямо говорящий, что механизм здесь не подтверждён и правило на нём не стоит. Правило стоит на другом: подтягивание стоит одной команды, а его отсутствие проверяется ценой круга ревью. Ревьюер это отдельно сверил (критерий 9) и подтвердил, что текст не выдаёт непойманный механизм за диагноз.

**Сверх постановки записаны две вещи, обе замерены сегодня:**

- **ловушка порядка, и она бьёт по праву merge:** `gh pr update-branch` создаёт НОВУЮ голову, а гарды 1 и 2 привязаны к КОММИТУ, а не к номеру PR. Значит очерёдность жёсткая: подтянуть → снять зелёный на новой голове → метка `review` → вердикт. Ветка, подтянутая после вердикта, отправляет PR на новый круг целиком;
- **как читать `gh run list --workflow claude-review.yml`:** прогоны с исходом `skipped` и головой ДЕФОЛТНОЙ ветки (`d6bad097`, `9dd947a7`, `54f79f48`, `5fd297e4`) — это собственный коммент ревьюера, поднявший `issue_comment` и отсеянный условием джобы, а НЕ отказ ревьюера. Различать по колонке `event`. Без этой строки следующая сессия прочитает их как «ревьюер пропустил себя» и подтвердит гипотезу выше ложным фактом.

## 3. Находка dev-core про `merge-notify.yml` — моё суждение и вопрос к тебе

Находка (msg `2026-08-19T10-2…Z-dev-core.md`) верная и по классу, и по цене: комментарии legacy-ветки `merge-notify.yml` ссылаются на артефакты РОДИТЕЛЬСКОГО репозитория — `PR #33` (здесь старший номер #32), `#131`, треды `023`/`042`, упразднённый `ROLES.md`. Это тот же класс, что раздел 4 `install-notes.md` уже назвал: чужие номера в тексте, приехавшем из монорепо. Он там объявлен НЕ подлежащим переписыванию — след решения принадлежит репозиторию, где решение принималось.

**Но здесь случай хуже общего, и разница существенная.** Обычный чужой номер — это ссылка, читаемая как родительская. А `merge-notify.yml:186` и `:209` формулируют **условие снятия целой ветки кода** («все open-треды мигрированы, сейчас 009/010») через треды, которых в этой почте нет. Условие, которое читателю нечем проверить, — не ссылка, а неработающий гард: ветку кода однажды либо снимут не проверив, либо не снимут никогда.

**Чего я НЕ проверила и не выдаю за проверенное:** достижима ли legacy-ветка в ЭТОЙ почте (есть ли тред без каталога `messages/`). dev-core оставил этот замер мне, я до него не дошла — `cli mail` в моей форме вызова не отработал, а копаться в форме команды на исходе окна значило бы отдать ход недоделанным. **Замер остаётся открытым и стоит одного вызова следующей сессии.** Если ветка недостижима — находка дороже комментариев: это ещё и мёртвый непокрытый путь.

**Решение за тобой, потому что `.github/workflows/**` — док власти, и правку даже одних комментариев там не делает ни curator (зона `forbidden`), ни dev-core своей волей.** Моя рекомендация — а не решение: сказать dev-core «делай» отдельным PR, где комментарии legacy-ветки (а) помечают чужие номера как родительские и (б) переформулируют условие снятия через факт, проверяемый ЗДЕСЬ (например «в почте нет тредов без `messages/`» — команда, а не список номеров). Кнопка на таком PR всё равно твоя: воркфлоу.

## 4. Ветка зонда `ci/config-check-probe` — твоя рука

Её единственная ценность — красный лог `32239926681` — ревьюером уже сверена поимённо в вердикте по #29, и лог переживёт ветку. Держать её больше незачем. Удаление необратимо и потому не моя рука: `git push origin --delete ci/config-check-probe` — одно слово, и я его не тороплю.

## Состояние треда

Постановка `config check` закрыта, класс подписей воркфлоу закрыт, приёмка `merge-notify` снята живым измерением (и подтверждена третий раз: merge #32 поднял прогон `32243911242`), правило про отставшую ветку в базе, право merge роли curator **подтверждено первым автономным применением**. По моей части тред пуст — открыты только две кнопки выше и один незакрытый замер (достижимость legacy-ветки).

`waiting-on → curator`: ход остаётся у того, кто несёт вопрос. Парковка на john.

