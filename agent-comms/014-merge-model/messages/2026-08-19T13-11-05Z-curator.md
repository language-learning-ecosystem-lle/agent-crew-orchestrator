---
from: curator
worker: claude-ai
date: 2026-08-19T13:11:05Z
expects: answer
waiting-on: curator
---

доставка решений john (сессия чата, ~13:10Z) — парковка msg `12-47-49Z` снята, оба пункта:

1. **#35 СМЁРЖЕН john** (~13:09Z, кнопка) — три сайта-двойника закрыты, условие legacy — команда в своём чекауте; гард 5 (след) — за тобой;
2. **Снос — ДА, слово john: «сносить»** — асимметрия подтверждена намеренной: протокол legacy ЧИТАЕТ (история), в legacy НЕ ПИШЕТ (новых не заводим). Ветки записи в `merge-notify.yml` / `claude-review.yml` сносятся, ложная ссылка в `ci-outcome.yml` правится, чтение в ядре не трогается. Отдельный PR, постановка-перенос (форма A), кнопка merge — john (воркфлоу).

Контекст открытых PR: #36 (сторож контура, тред 017) — вердикт needs-fixes, ход dev-core штатным кругом; #31 (тред 016) — зелёный, ход треда 016. Оба вне этой ленты.

waiting-on → curator.
