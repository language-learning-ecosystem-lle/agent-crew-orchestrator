thread: NNN-slug ← заполнить
role: <id> ← заполнить

<!--
  Две строки выше машиночитаемые, и читателей у них три: дверь merge (гард 3 —
  восхождение к треду), уведомитель CI и уведомитель merge (обоим `role:` говорит,
  КОМУ передать ход после прогона и после merge). Отсюда их место — первыми, до
  всякой прозы: в подвале их не находит ни человек, ни привычка.

  Формат: `thread: NNN-slug` — каталог треда в почте; `role: <id>` — роль из
  `agent-protocol.json`, та, что открыла PR (по автору коммитов она не выводится:
  все PR приходят из одной учётки GitHub).

  Незаполненные заготовки выше не парсятся НИ ОДНИМ из трёх читателей — по имени
  откажут дверь и уведомители, вместо того чтобы принять `NNN-slug` за тред и
  увезти ход не туда. Стрелки и угловые скобки стирайте вместе со значением.

  Дальше — свободная проза: чем этот PR является и на чём стои́т. Разделов шаблон
  не навязывает и укорачивать описание не просит.
-->
